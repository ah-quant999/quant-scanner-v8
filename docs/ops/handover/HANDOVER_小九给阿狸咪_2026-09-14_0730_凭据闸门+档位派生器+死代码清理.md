# 交接：小九 → 阿狸咪的工程师

**时间**：2026-09-14 07:30（周一·盘前）
**我方 commit**：`fa661dafb69079f10b5ee9d3a6e1548bf35ae622`（7 文件：6 改 1 删）
**触发**：主人 07:01 令「按你意见处理」——即我上轮列出的「① 补提交前敏感文件闸门」＋「② 4 项遗留（其中④建议作废）」

---

## 零、一句话结论

本批落地 **6 件事**（凭据闸门 / 观澜台失败码可见 / 档位派生器 / make_lite 去重 / 删孤儿脚本 / ④作废），
顺手**拆掉主树一颗数据回滚地雷**（陈旧暂存区 13 项，含 2 个比远端旧的文件）。
另**新发现两处隐患**（含 6 处脚本引用已废弃路径 `E:/workspace/quant-scanner-v8`，其中 4 处读的是那里的**旧 GitHub token**）—— 我**没有擅动**，列在第四、五节。
**需要你做 1 件事**：见第二节（装闸门，1 分钟）。

---

## 一、本批 6 件事

### 【1】🔴 提交前凭据闸门（P0 · 治两次同类事故的根因）

**背景**：本仓 `ah-quant999/quant-scanner-v8` 是 **public**。已两次把凭据提交进 main：

| 时间 | 文件 | commit | 现状 |
|---|---|---|---|
| 08-24 | `data/.mahoro_cookies.txt` | `500fd4c29` | 仍在公开历史（mahoro 08-28 已下线，cookie 已废） |
| 09-11 | `data/zsxq_token.json` | `a865d6f11` | **仍在公开历史**，暴露 61.4 h |

**根因是我上轮判错了**，本批更正：
> **不是** `v8_closing_data_refresh.py:68` 的 `git add -f data/*.js raw_data/*.json` ——
> 该 pathspec 含 `/`，git 下 `*` **不跨 `/`** 且有后缀要求，**根本匹配不到** `data/zsxq_token.json`。
> 真凶是**某次 AI 会话的手工全域 `git add -f`**。
> **判据**：那句 commit 文案「云端对账后仅保留本地较新…」在 `git log -S` **全历史里不存在于任何脚本**。
> ⇒ **防线必须做在提交闸门，做在某个脚本里挡不住手工操作。**

**新增三件套**：

| 文件 | 作用 |
|---|---|
| `scripts/secret_guard.py` | 判定本体。A 路径名 / B 强加 / C 内容 三类规则 |
| `.githooks/pre-commit` | 闸门钩子（含**无 Python 时**的 shell 兜底） |
| `scripts/install_git_hooks.py` | 一键安装器（幂等） |

**规则设计要点**（都踩过才知道为什么这么写）：

- **规则 A（路径名）**：文件名含 token/secret/cookie/credential/… 时——
  · 在 `data/` 或 `raw_data/` 下 → **无视扩展名一律拒绝**；
  · 否则仅当扩展名不在「源码/文档白名单」时拒绝。
  ⇒ 这样 `scripts/setup_credentials.py`、含 "TOKEN" 字样的 `.md` 都**不会**被误伤。
- **规则 B（强加）**：被 `.gitignore` 忽略、却仍出现在待提交集合、且位于
  `data/ raw_data/ out/ algorithms/out/ algorithms/data/` → 拒绝。
  ⇒ **这条专治 `git add -f` 绕过 .gitignore**，就是两次事故的直接机制。
  同时 `v8/*.py` 这类**故意** `-f` 加入的路径不在受保护目录里，不受影响 ✅
- **规则 C（内容）**：对非源码文件扫高置信凭据模式（GitHub PAT / `zsxq_access_token` / AWS / `sk-`）。

**自测证据（全部实跑）**：

```
① 伪造 data/_guard_selftest_token.json  → 拒绝，退出码 1   ✅
② 正常源码 3 个文件                     → 放行，退出码 0   ✅
③ 全量零误报：4001 个已跟踪文件 → 仅 1 例命中，且该例是「闸门自身缺陷」
④ 回归：中文名 TOKEN 文档 → 放行 ✅ ｜ 真凭据 4 例全拒 ✅ ｜ 正常文件 5 例全放行 ✅
```

**🔴 ③ 那条「闸门自身缺陷」值得单说**（是全量自检**抓出来的真 bug**，不是事后补的说明）：
> git 默认把**非 ASCII 路径**做八进制转义，`..._添加ZSXQ_TOKEN_Secret.md` 的扩展名被读成 `.md"`（带尾巴引号）⇒ 不在白名单 ⇒ **中文名文档被误判为凭据**。
> **修法**：所有 git 调用统一加 `-c core.quotepath=false`。**这条坑在别的地方同样成立**，你写涉及中文路径的 git 脚本时要一起防。

### 【2】观澜台失败码可见（治「1059 被静默当成 0 条研报」的误诊）

`algorithms/guanlan_extractor.py::api_fetch_topics` 原先「取空就重试、只打印返回空」——
**把服务端真实失败码完全吞掉**。而本接口两种失败性质**截然不同**：

| 情形 | 服务端返回 | 含义 |
|---|---|---|
| 未认证 / 凭据被注销 | **HTTP 401** | 真·token 失效 |
| 反第三方工具门禁 | HTTP 200 + `{succeeded:false, code:1059}` | **间歇性限流**，换请求头无用、间隔重试即通，**与 token 好坏无关** |

**09-14 实际踩过**：观澜台"停摆" → 自愈告警 → 误诊为"token 丢了"，
**真因是限流，token 完全有效**（伪造值 401 / 真值 200 对照实验已证）。

现改为每次失败都打印 `succeeded` / `code` / `info`，并对 401 单独点明「凭据可能已失效或被注销」。
顺带更正 L16 注释里 `zszxq_token.json` 的错拼（实际读 `data/zsxq_token.json`）。

冒烟：伪造 token → 三次均正确输出 `HTTP 401 ← 凭据可能已失效或被注销`。

### 【3】持有期档位派生器 `_holdLadder()`（遗留③ · 一劳永逸）

**原状**：TDX 卡、策略回顾卡 `_renderPeriodBacktestCard`、最佳策略卡 **三处各自硬编码**
`[1,3,5,10,20]`（5 档），而产物实测**已有 12 档**：

```
CRDS_BACKTEST        by_period → 12 档: 1,3,5,10,20,30,45,60,75,90,180,250
FOUR_VOLUME_BACKTEST by_period → 12 档: 同上
CANDIDATE_BACKTEST   by_period → 12 档: 同上
BACKTEST_TDX         summary   → 12 档
```
⇒ **T+30 以上长档永远上不了屏**，且三处口径各改各的（下次再加档又要改三遍）。

**现**新增 `_holdLadder()`（挂 `window`，**单一真源**），三处共用：
兼容 `by_period` 的数字键（`"5"`）与 `summary` 的 `win_rate_<N>d` 键；
上界 500 过滤（防股票代码 `600000` 误入）；去重 + 升序；空输入回落 `[1,3,5,10,20]`。
**以后增删档位自动上屏，前端不必再动。**

实测：四份产物全部派生 12 档；边界（`null`/空对象 → fallback、`600000` 被排除、乱序去重）全对；
**25 个 script 块语法错误 0**。

顺带把表头/单元格内边距 6px → 4px/3px + `white-space:nowrap`，保证 12 档下 `-19.53%` 不被挤压换行。

### 【4】`split_inline_data.py::make_lite` 改单向委托（遗留② · 去重）

两份重复实现已**漂移两次**（09-11 漏 `update_time`、09-14 漏 `entry_caliber_ver`），
且**分支覆盖实测不一致**：

| | 分支数 | 内容 |
|---|---|---|
| `split_inline_data.py::make_lite` | **4** | TDX / COMPREHENSIVE / GOLD_POOL / W52_HIGH |
| `update_v8.py::_make_lite` | **8** | 上述 4 个 + ANALYST_RATINGS / SUSPENSION_ALERT / IPO_DATA / SECTOR_PHASE_HISTORY |

缺的 4 个在本处**静默走 `return obj`** —— 与本工具「拆分时应裁剪」的预期不符。
现改为**惰性 import 转发**到 `update_v8.py::_make_lite`（唯一真源）。
等价性测试 **6/6 一致**（含非 dict 防御分支）。

> 该文件在 `DO_NOT_DELETE.md:117` 被列为「index.html 内联数据拆分工具 / 维护工具」，
> **无自动调用者**（7-30 一次性拆分用过），故保留、只去重。

### 【5】删除孤儿脚本（遗留①）

`algorithms/optimize_stop_target.py`（416 行）—— 全仓 `grep` 确认**无任何调用者**，删除。
产出物 `raw_data/stop_target_optimization.json` 若仍存在，属废旧产物，可后续一并清理（未擅动）。

### 【6】遗留④ 作废：长档**不**退出主线

主人 09-13 令「5…250 十档全上」。我先前「把 180/250 档改为不进主线」的建议**作废**，不再提。

---

## 二、🔴 需要你做的一件事（约 1 分钟）

**装闸门** —— `core.hooksPath` 是**本地配置、不随仓库分发**，所以 hook 文件已入库，但「启用」必须每台机各做一次：

```bash
cd <你的 v8 仓库>
git pull            # 先同步，确保 .githooks/pre-commit 与 scripts/ 到位
python scripts/install_git_hooks.py          # 安装（幂等）
python scripts/install_git_hooks.py --check  # 复查（可选）
```

**⚠️ 我为什么没有替你/在本机自动启用**：
本仓有 **4 处裸提交脚本**走在盘后链上
（`run_dividend_refresh.py` / `v8_closing_data_refresh.py` / `self_heal_monitor.py` /
`algorithms/strategy_four_volume_60m.py`）——
**闸门一旦误报，会直接断掉盘后生产**。
我已用全量 4001 文件验证零误报（且修掉了唯一那例自身缺陷），但仍建议你：
**装好后先观察一个完整交易日**（看盘后链有没有 commit 被拦），确认无误再当作常态。

**临时旁路**（仅确认误报时）：`SECRET_GUARD_ALLOW=1 git commit ...`

**自测**（建议真跑一次，确认它在你机器上生效）：
```bash
printf '{"token":"x"}' > data/_selftest_token.json
git add -f data/_selftest_token.json && git commit -m test   # 应被拒绝
git reset -q HEAD && rm -f data/_selftest_token.json         # 清理
```

---

## 三、顺手拆掉一颗数据回滚地雷（未在本批 commit 里，只动索引）

**发现**：主树暂存区积压 **13 项**陈旧条目（本地 HEAD 停在 **09-11 20:51**，落后远端 **537 个提交**）。
其中 **2 个文件比远端旧** ⇒ **任何一次裸 commit 都会回滚它们**：

| 文件 | 暂存/工作树 | origin/main | 差 |
|---|---|---|---|
| `algorithms/run_algorithms.py` | 76,882 B | 84,645 B | **旧 7.7 KB** |
| `.github/scripts/v8_stage_gate.py` | 44,072 B | 44,728 B | 旧 656 B |

**处置**（严格按 skill `v8-git-stale-index-landmine`）：
```bash
cp .git/index backup/git_index_stale_20260914_0703.bak   # 可回滚锚点
git reset -q HEAD                                        # 只清索引，不动工作树、不移 HEAD
→ staged=0
```
**零改动自证**：`index.html` / `data/HEALTH_CHECK.js` **sha256 校验全 OK**；
`data=111` / `raw_data=166` 数量不变。
**未擅自恢复或提交任何删除项**，未触碰 41 项他人 WIP。

---

## 四、新发现的两处隐患（我**没有擅动**）

### 隐患 A：6 处脚本引用**已废弃路径** `E:/workspace/quant-scanner-v8`

该目录**仍然存在**（非 junction、**无 `.git`**），里面只有 **69 个 09-10 前的旧交接/URGENT 文档**。
主树 `E:/qs_workspaces/quant-scanner-v8` 才是真身（217 项，09-14 03:05 更新）。

引用它的脚本：

| 脚本 | 引用内容 | 风险 |
|---|---|---|
| `v8_dispatch_fetch.py:39` | `TOKEN_PATH = ".../.workbuddy/v8_gh_token.txt"` | 🔴 读的是**该目录下的旧 token**（09-01 15:32），真身仓的是 09-04 18:08 ⇒ **若非同名同值，派发会 401** |
| `v8_cloud_watchdog.py:74` | 同上路径（候选列表） | 同上 |
| `v8_postclose_guard.py:40` | `TOKEN_FILE = ...` | 同上 |
| `guard_v8_freshness.py:217` | 同上路径（候选列表） | 同上 |
| `v8_closing_data_refresh.py:23` | `ROOT = "E:/workspace/quant-scanner-v8"` | 🔴 该脚本的 cwd/产物路径全指向废弃目录 |
| `algorithms/strategy_four_volume_60m.py:427` | `preview = r"E:/workspace/quant-scanner-v8/data"` | 预览路径失效 |

**已核实**：`v8_closing_data_refresh.py` **全仓无自动调用者**（只在旧交接文档里被提及）⇒ 疑似死脚本，
但它 `ROOT` 错 + `git add -f` 无守卫，属**潜在雷**。
**未改**：改 `ROOT` 会改变行为，且不确定是否还有隐蔽调用（如 WorkBuddy 自动化任务 prompt 里直接引用）。
**建议**：主人拍板后统一改为真身路径，或直接删除死脚本。

### 隐患 B：缓存戳 `?v=` 的回退机制（已知模式，本批再次触发）

本批我基于 `aa1bdcb4a` 建 worktree，推送时 base 已推进到 `5309e769d9`（期间含 CI build `d85f7842e`），
⇒ 我的推送把 `index.html` 的 `?v=1789340998` 与 `BUILD` 回退成上一轮值。

**已严格验证**：排除缓存戳/BUILD 行后，**剩余差异 100% 是我的 `_holdLadder` 改动（5 处）**，
**无任何他人内容被回退**（这正是上轮 `logic.html` 覆盖事故的检查口径，本次已按新流程执行）。
缓存戳由 CI build + 专门的「缓存戳实时对齐」自动化重写，属**自动恢复**。
**但推论**：任何基于「非最新基线」推送 index.html 的机器，都会制造这个窗口 ⇒
**动 index.html 前必须 `git fetch` 确认基线含对方最新提交**（本次我漏做了这一步，靠事后验证兜住）。

---

## 五、待拍板（交主人）

1. **隐患 A 的处置**：6 处废弃路径 —— 统一改真身路径 / 删死脚本 / 暂不动？（涉及 GitHub token 读取，建议优先）
2. **闸门启用节奏**：是否本机（小九）也立即启用 `core.hooksPath`，还是先观察一个交易日？
3. `raw_data/stop_target_optimization.json`（孤儿脚本的产出物）是否一并清理？
4. 双机共写热文件回退的**根治方向**（沿用上轮提议）：口径文案别硬编码进 `index.html`，
   改由产物字段（`primary_rule` / `method` / `full_ladder`）**派生渲染** ——
   本批 `_holdLadder` 已是这个思路的一个实例 ✅

---

## 六、复核方法（你那边可独立验证）

```bash
git fetch origin main && git rev-parse origin/main     # 应为 fa661dafb…

# ① 闸门三件套在位
git show origin/main:scripts/secret_guard.py         | head -5
git show origin/main:.githooks/pre-commit            | head -5
git show origin/main:scripts/install_git_hooks.py    | head -5

# ② 观澜台失败码可见
git show origin/main:algorithms/guanlan_extractor.py | grep -n "code=1059\|_why(d)\|urllib.error"

# ③ 档位派生器（应为 5 处：定义1 + 挂载1 + 调用3）
git show origin/main:index.html | grep -c "_holdLadder"

# ④ make_lite 已去重（应只剩转发，无重复实现）
git show origin/main:split_inline_data.py | grep -n "def make_lite" -A 4

# ⑤ 孤儿脚本已删
git cat-file -e origin/main:algorithms/optimize_stop_target.py 2>/dev/null \
  && echo "❌ 仍在" || echo "✅ 已删除"

# ⑥ 语法体检（25 个 script 块应 0 错）
# 用你自建的离线复现器跑一遍最稳
```

---

**小九** · 2026-09-14 07:30
