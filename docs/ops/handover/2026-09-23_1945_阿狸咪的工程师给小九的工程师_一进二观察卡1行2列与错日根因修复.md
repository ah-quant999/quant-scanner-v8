# 交接件 · 一进二观察卡：1行2列改版 + 错日根因修复 + 09-23 数据订正

- **发送方**：阿狸咪的工程师（`alimi-cn` 家机）
- **接收方**：小九的工程师（`lemoncat-cn` 单位机）
- **时间**：2026-09-23 19:45 北京时间
- **触发**：主人令「这卡改成 1行2列；基础是什么；核心/用法/限制配套写上；是盘前/盘后/盘中？能算出推荐买哪只吗？」——核查中另发现并修复一处**错日（假新鲜）**真 bug。

## 一、本次改动（4 个文件，commit `be677132`）

| 文件 | 改动 |
|---|---|
| `index.html` | 本卡列表由「竖排每行 1 只」改 **1行2列网格**（`grid-template-columns:repeat(2,1fr)`，单元=名称/代码/✓基础 + 行业·流通·换手 + 封单·首封时间）；新增**核心/用法/限制**块（全站统一紫框范式）；汇总行「今日主板首板」→ **「数据日 MM-DD 主板首板」**（去失真「今日」）；`?v` 令牌 1790163353 → 1790163965（单调） |
| `algorithms/fetch_board_first_limit.py` | **错日根因修复**：新增 `_trade_calendar/_is_trading_day/_last_closed_trading_day`；默认模式改取「最近一个**已收盘**交易日」（今天未过 16:00 CST ⇒ 取上一交易日）；回溯改**沿交易日历只试真交易日** |
| `data/BOARD_FIRST_LIMIT.js` | 数据订正：`date=20260923`、**total 35→32**、basic_pass 9→21（原为 09-22 池被错标 09-23） |
| `raw_data/board_first_limit.json` | 同上（raw 与 data 同步） |

## 二、错日根因（一劳永逸）

- **病灶**：东财 `getTopicZTPool` 对「未收盘的今天 / 未来日」一律吐**最新存量池**。
  实测：`date=20260924`（未来日）返回内容与 `20260923` **完全相同**（`001234/002238/600825`）；仅周六/周日是真返回空池。
- ⇒ 原「空池才回溯」护栏对**未收盘的今天/未来日永不触发**：盘前 08:20 用 `date=今天` 请求即拿到**上一交易日**的池，却按「请求日」打标 ⇒ 卡面写「今日」实为昨日。
- **铁证**：改前数据文件 35 只 ↔ `20260922` 主板首板池 **35/35 逐只全等**（池独有 0、文件独有 0）；↔ `20260923` 池交集 **0**。⇒ 内容=09-22，标=09-23。
- **修法**：默认模式先用交易日历算「最近已收盘交易日」，只对该日查询；空则沿交易日历回溯（≤7 交易日）。**绝不接受未收盘/非交易日**。

## 三、审计（三件套①）
- `index.html`：全部内联 `<script>` 抽提跑 `new Function` ⇒ **28 块 / 0 语法错**；纯 LF（CR=0）；19073 行。
- `algorithms/fetch_board_first_limit.py`：`py_compile` OK。
- **[10/10] index 核心标记守卫**：真源 `docs/ops/index_protected_markers.txt` 35 条 ⇒ 待推 index.html **命中 35/35、缺失 0** ⇒ 不阻断 deploy。
- 生成器实跑：`OK date=20260923 total=32 basic_pass=21`。

## 四、四方对齐（三件套②）
- **云端**：`v8_cn_fetch_cloud.yml` 本卡步注释已同步「已收盘交易日」口径（同 commit）。
- **双机任务**：本卡无本机任务（供数来自云端 cn fetch 链），无需错峰调整。
- **运维页**：健康面板按 `BOARD_FIRST_LIMIT` 键追踪新鲜度，键名未变；`update_time` 已刷新为 2026-09-23 19:38。
- **逻辑详解页**（`logic.html`）：**不含**该卡（0 命中）⇒ 无口径可冲突。

## 五、语义结论（回答主人）
- **链归属**：云端 **cn fetch 数据链**（非算法链、非双机本机任务）。
- **时机**：步名为「**每轮刷新**」——盘前 08:00–09:00 / 盘中每 20 分（09:00–11:40、13:00–14:40）/ 盘后 17:20·18:20·19:20 / 周末均可触发。
  **语义归「盘后」**：涨停池是收盘定稿的**日频**数据；修好后**盘前/盘中显示「最近已收盘交易日」**，**盘后显示当日**。
- **能否推荐买哪只**：**当前不能**。仅有「基础口径」（主板+首板+流通/换手/封板干净），K 线严格过滤（控盘指数/三线粘合/角度60°/涨停确认/量差/筹码穿刺）**尚未接入** ⇒ 本卡是**观察名单、非选股信号**。要产出买点，需接入云端 K 线步补全严格过滤，并按北极星做**离线回测（收益率+胜率）**验证后由主人拍板转正。

## 六、待对方（小九）应用项
- 无强制项。`index.html` / 生成器 / 数据 已在 `main` 生效，cn fetch 链下轮起即按新口径产数。
- 若小九侧另有本卡的判断逻辑，请以本件「五、语义结论」为准。

## 七、复核命令（给判据，不给私有路径）
```
git cat-file -e origin/main:data/BOARD_FIRST_LIMIT.js && echo "数据在库"
# 数据日与内容自洽：date 应=最近已收盘交易日；与当日涨停池主板首板逐只对码应全等
python -c "import json;d=json.load(open('raw_data/board_first_limit.json'));print(d['date'],d['total'],d['basic_pass'])"
grep -c "__renderBoardFirstLimit" index.html
```
