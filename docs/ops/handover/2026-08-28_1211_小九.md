# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 更新于 2026-08-28 12:11（自动更新 · 12:00 档）
> 生成方：小九（v8 当日交接自动化，automation-1785719698947）
> 说明：本档基于当日工作日志 + `git log origin/main` 真实 SHA 核实整理，未使用记忆中的 SHA。

---

## 一、背景

- **双机分时铁律恢复**：主人 8/21–8/27 出差已收官（8/27 回归），白天小九独家（08:00–17:45）、夜间/周末阿狸咪部署的铁律恢复正常。
- **小九当班 08:00–12:00**：实际因主机 08:22 重启出现约 51 分钟 runner 空窗，由本地 guard 自愈拉起。
- **交接目的**：阿狸咪今晚/周末开机可读此档掌握当日状态，开机先 `git pull` 对齐（勿直接 pop 本地 stash，见第四节风险）。

---

## 二、今日已完成改动（含真实 commit SHA + 影响）

### A. 小九当班动作（v8 侧 2026-08-28.md 工作日志）

- **09:13 v8 runner 本地守护（自动化第 122 轮）· ⚠️关键事件**
  - 主机 **08:22:13 重启**（LastBootUpTime 由 8/27 13:17:24 → 8/28 08:22:13），旧 listener PID 5384 随重启消亡。
  - 此为待办②「runner 装回服务/开机自启」**第 3 次实爆**（前两次：8/25 19:50、8/27 13:17）。
  - guard 09:13:41 检测 listener=False → `--heal` 拉起 run.cmd → 新 **Runner.Listener PID 4384（StartTime 2026/8/28 09:13:57）** 就绪；空窗约 51 分钟（08:22:13→09:13:57）。
- **⚠️双写者竞态复发（run122 类）**
  - guard 09:14:02 首推 guard schema 后，cloud_fetch premarket 09:16:59 把 `raw_data/runner_status.json` 覆盖成摘要 schema（缺 listener/actions_taken 字段）。
  - 已重跑 guard（09:19:48）恢复 guard schema 并推远端；权威核验 **FIELD_MATCH=True + SCHEMA_IS_GUARD=True**。
- **红线合规**：UU=0、四红线文件 0 触碰、stash=7 未动、`_work` 未清空（SAFE_DELETE 仅命中 8/03·8/13 旧日志，近期 0）。
- **盘中漏轮兜底**：10:20 与 11:13:56（自动化 1787108824799）两检 7 卡语义 update_time 均 ≤35 分（最旧 sector_fund_flow 10:54 age≈19 分），FRESH 未动作。

### B. 代码修复（已由本机 12:00 并行实例 rebase 并推送 origin/main）

- **`393d09dcd` fix: v8_deploy_guard 剥离JS注释，根治 `// fix` 误判致构建全断（一劳永逸）**
  - 影响：修复前 `data/*.js` 末尾残留人工/管线 `// fix` 等注释，旧逻辑 `json.loads` 遇 `//` 即抛错，把合法空占位符/真实数据误判成 shell，触发「部署前校验失败」阻断**全部构建**（实时数据卡死数小时）。新增强注剥离（保留 `http:// https://` 不误伤 URL），有 `window.X=` 且主体合法 JSON 一律放行。
  - **状态：已上线 origin/main，后续构建不再因注释误判而断。**

### C. 今日 origin/main 其余提交（自动化产出，SHA 均 `git log` 核实）

- `d9ef3a8a8` v8 cn fetch 12:04
- `980862886` / `ef787125f` / `eda4c89fd` v8 cn fetch 11:53
- `22ada1003` chore: 缓存戳实时对齐 ?v 12:07
- `33f9e9fda` data: westock 持仓同步 12:03
- 早盘链（11:12–11:34）：`dd4136473`/`b343c2605`/`cf1aa07f4`/`38f061d83`/`79747a8e9`/`907fb1e2d`/`01a0fd0c5`/`eacf1f9dd`/`44d59bb74`/`70ab9e11d` 等（cn fetch / 全量重生成 / 缓存戳对齐 / runner 上报）

---

## 三、当前系统状态（实拉核实）

- **站点**：HTTP **200**（实拉 `https://ah-quant999.github.io/quant-scanner-v8/` 确认可达）。
- **main SHA**：`393d09dcdcda403a3dca7ac70c1a3faf5bd8beb8`（含 v8_deploy_guard fix，与 `git ls-remote origin main` 一致）。
- **runner_status**：run_time **2026-08-28 11:53:25**，category=all，summary ok=1/empty=0/fail=0，modules=1（Listener 存活）。08:22 重启后由 guard 09:13 自愈拉起 PID 4384，空窗约 51 分钟兜底充足。
- **HEALTH_CHECK.js 快照（updated 09:33:20）overall=fail（ok96/warn1/fail10）——但快照陈旧，非实时现状**：
  - 10 个 fail 中 **8 个为盘中模块**（INDEX_QUOTES / ETF_PULSE / SECTOR_FUND_FLOW / CONCEPT_RANKING / LIMIT_UP_HEATMAP / MARKET_ALERTS / ETF_DAILY_MONITOR，快照 last_update 08:09–09:12）。**实测内层 `data/*.js` update_time 为 2026-08-28 10:54–11:06，数据实际新鲜** —— HEALTH_CHECK 快照未随其后 cn fetch 刷新，属**假 fail**（快照未重算）。
  - **真正陈旧的 2 项**：`CANDIDATE`（08-26 22:36:53）+ `all_MAHORO`（08-26 23:32:52），均 `heal_cat=algo_run`，为盘后算法链产物，周末→周一间隙预期，**今晚 18:30 盘后链补齐即清**。
  - ⚠️ 教训重申（08-20）：盘中查新鲜度须**直接解析 `data/*.js` 内层字段**，勿信 overall=fail 快照。
- **剩余自动化（GitHub Actions，`.github/workflows` 共 27 个）**：
  cloud_weekly_cleanup · fill_kcbj_concepts · maharo_refresh · probe_algo_cloud · probe_em · probe_ths · probe_v8_cn_cloud · runner_health_alert · v8_algo_cloud · v8_algo_intraday_lite · v8_algo_run · v8_backup · v8_build_deploy · v8_cache_buster_reconcile · v8_cleanup · v8_cn_fetch · v8_cn_fetch_cloud · v8_cn_fetch_cloud_hosted · v8_cn_fetch_cloud_selfhosted · v8_daily_audit · v8_freshness_watch · v8_health_patrol · v8_risk_gauge · v8_stock_quote_refresh · v8_t1_guard · v8_weekend_light

---

## 四、需阿狸咪注意 / 待办 / 风险

- 🔴 **待办②（P0）**：runner 未装 Windows 服务/开机自启，已**第 3 次**因主机重启（8/28 08:22）失联约 51 分钟才被 guard 自愈。强烈建议尽快给小九单位机 runner 装服务或开机自启，避免开盘前后空窗。
- 🔴 **待办⑥（P1）**：cloud_fetch 与 guard **双写 `raw_data/runner_status.json` 竞态未根治**（今日 09:14/09:16 复发）。建议 cloud_fetch 改写独立文件或加 schema 互斥锁，避免覆盖 guard 的 listener/actions_taken 字段。
- 🟡 今日 `CANDIDATE`/`all_MAHORO` 陈旧（08-26）属预期，18:30 盘后链补齐；若夜间看到这俩仍旧，等 18:30 后确认，勿手动改。
- 🟡 `v8_deploy_guard` fix（`393d09dcd`）已上线，部署前注释误判已根治；若夜间部署遇「校验失败」，优先查是否又有新注释格式，勿回退。
- 🟡 **缓存戳防覆盖三连已上线**（`79747a8e9` / `907fb1e2d` / `22ada1003`）：CDN 覆盖陈旧问题已缓解，阿狸咪每次部署后请 `Ctrl+F5` 硬刷新确认页面生效。
- 🟡 **WorkBuddy 本机自动化计数以实际 db 为准**：用户级记忆最新记为 **15 个（14 ACTIVE + 1 PAUSED：TDX跟踪）**；并行实例 08-27 曾记 47 个 v8 相关（44 ACTIVE+3 PAUSED），口径不同。阿狸咪开机后核对本机 db，勿凭旧数。
- 🟢 双机分时铁律恢复：阿狸咪夜间/周末部署；开机先 `git pull` 对齐。**本地若有 WIP 在 stash（含 PORTFOLIO.js 等），勿直接 `git stash pop` 覆盖新鲜 origin 数据（回滚地雷）**。
- 🟢 **并发实例竞争已对齐**：今日 12:00 窗口存在并发交接实例（另一实例 `161d27afc` 12:01 先提交），本档 `b24f68c2a` 在其上叠加并补齐「HEALTH_CHECK 快照假 fail 辨析」一节（关键运营洞察），未造成双份提交。

---

## 五、小九监督职责

- 白天 08:00–17:45 独家值守：盘前 08:15 全量 / 盘中每 30 分兜底 / 15:30 收盘 / 17:00 选股四模块 / 18:35 三重复查 / 每小时看板 pull+看门狗 / 每 2h 管线修复。
- **本档执行动作**：`git fetch` 清理 stale lock → `merge --ff-only`/rebase 对齐 origin/main（保住本地 1 个未推送 fix 并随并行实例推送）→ 实拉站点 200 → 解析 HEALTH_CHECK 内层字段区分真/假 fail → 写本交接文档。
- **红线**：未触碰 index.html / HEALTH_CHECK.js / PORTFOLIO.js 等红线文件（PORTFOLIO.js 本地 WIP 已 `git stash` 保全，未 pop）；**仅 `git add`+commit+push 本 HANDOVER 文档**，未触碰其他文件。
- **双触发防护**：Write 前已 `git ls-tree origin/main -- <文件>` + `git show origin/main:<文件>` 双重核验——确认无并行实例已提交同名 HANDOVER，未重演 08-19/08-24 覆盖事故。

---

*本档由自动化生成，数据取自真实仓库状态与当日工作日志。阿狸咪如有疑问以实时站点 + `git ls-remote origin main` 为准。*
