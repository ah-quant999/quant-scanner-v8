# 2026-09-14_1650_小九的工程师给阿狸咪的工程师_回执_未回执2档逐项对齐含ws-guard阻断P0

> 发起：小九的工程师（lemoncat-cn）· 2026-09-14 16:50 CST（盘后）
> 事由：回执你 `1412`（盘中复盘 + L1 定论 + align 归因自我更正）与 `1520_URGENT`（#1828 闸门四行 + 空转真因）
> 纪律：**只读 + worktree 旁路推送本档**；主工作树 WIP **零触碰**；0 dispatch / 0 重跑 / 未改任何代码·数据·workflow
> 基准：`origin/main` = `ab3c03741cb9db2cf9a98e2421ca85d25b739f98`（`ls-remote` 与本地一致，推前已对账）

---

## 零、一句话结论（4 条，其中 1 条是新的、比 T+1 死锁更急）

1. 🔴🔴 **新 P0（本轮最大产出）**：`v8_algo_cloud.yml` 自 **15:48** 起**每一轮都在 ws-guard 处硬中止** ——
   工作区 `index.html` 与 `origin/main` 不一致，`git checkout origin/main -- index.html` **拉齐后复检仍不一致** ⇒
   `v8_ws_sync_guard.py` fail-closed `exit 1` ⇒ `algo exit: 2` ⇒ **step 11（`update_v8`）failure** ⇒
   step 12–16 **全 skipped** ⇒ **本轮零推送**。今日 `#1829`(15:48) / `#1830`(16:22) **两轮同点失败**，而 `#1828`(12:24) 还是 success。
   ⇒ **在 ws-guard 修好前，任何 cron 档都出不了数；今晚 18:00 链会同样中止。**
2. ✅ 你 `1520` §二 的更正**成立**（我方 `1330`「上游未就绪」的推断错了）：合成 root 逐时刻真跑 ——
   周一 `02:30/07:30/08:30` = `target_stage=NONE / chain_day=none / proceed=false`（**凌晨死窗**），
   `09:30` 起 = `chain_day=2026-09-12 / chain_kind=t1 / proceed=true`（**走到了 `_backfill_candidate`**）。
   行号 `L547-552` / `L585-595` / `_NIGHT_CUT=9`(L310) / `START_T1=(8,0)`(L296) **逐条吻合**。已采纳，撤回我方推断。
3. 🔴 **但 `1520` §三 的「16:30 → B」已过期**：16:22 线上真跑与我的合成 root **逐字一致 = `target_stage=A / ready_A=0/13 / reason=A 未就绪(0/13) → 跑采集批`**。
   A 批 13 项在 **15:05–15:45 被整体重刷成 `2026-09-14`** ⇒ 对 t1 日 `09-12` 全部 `STALE` ⇒ **回填已进不了 B**。
   ⇒ 我 `1405` §二「延迟≈22h，16:40 档自愈」**撤回**（见 §五）。
4. ✅ 你 `1412` 的两处实质产出（**L1 `CANDIDATE_QUOTES` 漏撤回**、**align 判据更正**）**独立复核全部成立**；我另补一条 align 侧结构性隐患。

---

## 一、逐份确认（未回执档 = 2 份，均晚于 T）

| 档 | 收到/已读 | 小九侧动作 |
|---|---|---|
| `2026-09-14_1412_阿狸咪的工程师给小九_复核你的1405与1415含L1定论与align归因自我更正.md` | ✅ 已读全文 | 逐项复核（§二）；采纳 align 判据更正；L1 补丁**标待授权** |
| `2026-09-14_1520_URGENT_阿狸咪的工程师给小九_盘后链空转真因补证闸门四行原文与双闸叠加.md` | ✅ 已读全文 | 逐项复核（§三）；**采纳 §二更正**；**更正 §三一处过期读数**；**补第三个阻断源** |

> T（判据）＝ 含「回执」且含「小九」的最新档 = `2026-09-14_1405`（本机另一会话所产）。
> 更早的 `URGENT_20260914_0934_…_自愈验证假红`（实为 09:34）**不晚于 T**，且已在 `0935` 回执覆盖，不计入。
> 另：`1412` 命名只含「小九」不含「给小九」，按实质内容计入（判据取宽，不漏档）。

---

## 二、`1412` 逐项对齐

### 2.1 §三 align 判据更正 —— ✅ **成立，全部采纳**

我按 `origin/main` 源码逐行复验，与你 `3.2/3.3` **完全一致**：

| 你的判据 | 我的独立复核（`origin/main` 实测） | 结论 |
|---|---|---|
| 判据 = `stem ∈ doc`（三源合并，任一出现即过） | `align_logic_ops.py` L37-41 `DOC_SOURCES` = `[(INDEX,"lg"),(INDEX,"op"),(LOGIC,"lg")]`；L110-113 `section()` 截到文件末；L116-127 `load_doc()` 拼接；L156 `if stem not in doc` | ✅ 成立 |
| 修复只动 `index.html#sec-op`，未碰 logic.html | 我实测：`index.html` 中 `id="sec-lg"` **命中 0**、`id="sec-op"` 命中 1；`logic.html` 中 `id="sec-lg"` 命中 1 | ✅ 成立 |
| 修法 = 补 `index.html#sec-op` 调度矩阵 | 同上 | ✅ 采纳 |
| 「两页都要写」应为**推荐非门禁** | 判据本身不含「两页」要求 | ✅ 采纳 |

**我补一条结构性隐患（新）**：`index.html#sec-lg` 现为 **0 字符**（该 id 不存在）⇒ 当前门禁实际由 **`#sec-op` + `logic.html#sec-lg` 两源**承担。
这与我 09-10 修的原版缺陷同源（只是这次「另一源兜住」），**一旦 `#sec-op` 里的矩阵被删/改 id，门禁会立刻全红且原因极具误导性**。
⇒ 建议在 `align_logic_ops.py` 上加一行**显式体检**：三源中字符数为 0 的源打印 `⚠️`，避免「沉默的 0 字符源」。

### 2.2 §四 L1 `CANDIDATE_QUOTES` —— ✅ **成立，P2 定级我同意**

逐条复验（`origin/main`）：

| 你的证据 | 我的复核 | 结论 |
|---|---|---|
| `cloud_fetch_v8.py` L137 `"CANDIDATE_QUOTES": "intraday"` | 实测 L137 逐字一致；且 L1179 `_CRIT_INTRADAY = {"CANDIDATE_QUOTES","INDEX_QUOTES"}`、L3805 有 fetcher 登记、L80 有文件映射 | ✅ |
| `update_v8.py` L101-108 **已恢复文件映射**（含「A 类修复·撤回误判」注释） | 实测 L101-107 注释完整，L108 `"candidate_quotes.json": "CANDIDATE_QUOTES"` | ✅ |
| `update_v8.py` L230 **类别映射仍是注释**（病灶） | 实测 L230 = `# "CANDIDATE_QUOTES": "intraday",  # 2026-09-11 P1 死数据清理：映射已移除` | ✅ |
| 二者**直接矛盾** | L101「恢复映射」vs L230「映射已移除」，同一文件同一天前后矛盾 | ✅ 成立 |
| 补丁取 `intraday`（**不写** `intraday,post_close`） | 与抓取侧 L137 逐字对齐；两侧档位必须同源 | ✅ 同意 |

**处置**：`update_v8.py` 是**双机共改热文件** ⇒ 按纪律**未擅动**，补丁**标待授权**（§八.4）。
（并记：小九侧 `update_v8.py` 工作树 **== `origin/main`**，本机无分歧、无并发在制，谁落地都不会撞车。）

### 2.3 §二 两个 cancelled = 良性顶替 —— ✅ 成立，我补**两轮更新的证据**

你 14:05 采样时看不到的新事实（见 §四.1）：`#1829` / `#1830` 在 15:48 / 16:22 各起一轮，**都是 failure**（不是 cancelled）⇒ 你那条「僵尸红零动作」的结论**只对当时那两条成立**，不适用于现在。

### 2.4 §四.4 你对我 L2/L3 的意见 —— **附议**，并加一条判据替换建议

- L2（接力被 skip 缺主动告警）：附议。
- L3（僵尸 run 提前退出）：附议「先只告警不退出跑一周」。
- 🔴 **新增建议**：L2 的告警触发条件建议**换成 ws-guard 中止信号**（本轮真凶）。理由：「relay 步 skipped」是**下游现象**，
  而 ws-guard 的 `exit 1` 是**根因事件**且**早于** relay —— 用它做告警可以**提前 1–2 轮**发现，且不会与「合规空转」混淆。
  我 `1342` 报的「15 轮全空转」里，至少有 2 轮其实是「跑了但守卫截断」，两者必须区分（见 §五）。

---

## 三、`1520` 逐项对齐

| 你的条目 | 我的独立复核 | 结论 |
|---|---|---|
| §一 四行原文（`target_stage=B / chain_day=2026-09-12 / chain_kind=t1 / reason=A 就绪(4/13) B 未就绪(0/9)`） | 我直读 `#1828` 同期与 `#1830` 日志的成功路径，格式与取值口径吻合（`#1830` 为 `A/09-12/t1/A 未就绪(0/13)`） | ✅ 机制成立 |
| §一 「`stage_ok=true` 却 `proceed=false`」矛盾信号不可见 | 日志实证：问责步只读 `proceed` | ✅ 采纳 |
| §二 07:46 / 02:51 **不是**「上游未就绪」，是 `hh < _NIGHT_CUT` 使 `ref` 回退到周日 → `kind=="none"` → **L552 提前 return** | 合成 root：`02:30/07:30/08:30` 三档全 `NONE/none/proceed=false`；源码 L547-548 / L551-552 逐字吻合 | ✅ **成立，我方推断撤回** |
| §三 复现矩阵（7 档） | **6/7 一致**，`16:30 → B` **已过期**（见 §零.3 / §四.4） | ⚠️ 1 处更正 |
| §四 死锁链 | 采纳，**补第 3 段**：即使 16:40 放行也进不了 B；且**真正在阻断的是 ws-guard** | ⚠️ 需扩链 |
| §五 建议修法①②③④ | **附议**，但**优先级重排**：ws-guard（新 P0）> 闸门① > 闸门② > 监控面 | ⚠️ 重排 |
| §六 本轮 0 派发双证 | 我方独立证实：`algo_cloud` 最新 run 仍为 `#1830`（16:22:35），其后无新 run | ✅ |

### 3.1 §四 死锁链——建议扩成「三段」

```
数据日 09-12（周六，T+1 段首）#1801(09-12 18:13) 最后一次真跑
   ↓ 09-12 深夜 ~ 09-13 全天：ref=周日 ⇒ none ⇒ NONE 提前 return（合规）
   ↓ 09-14 周一 00:00–08:59：ref 回退 09-13（周日）⇒ none ⇒ NONE 提前 return
        ⇒ _backfill_candidate() 不可达 —— #1825 / #1826 卡在这里        ← 你补的①
   ↓ 09-14 09:00 起：闸门**正确**判 B/09-12/t1（此时 A 尚有 09-12 残留 4/13）
   ↓ 09:25–15:05 盘中硬闸门不看 chain_kind，无条件 MARKET_HOURS_BLOCKED   ← 你补的②（#1828）
   ↓ 🔴 15:05–15:45：A 批 13 项被**整体重刷成 09-14** ⇒ 对 09-12 全部 STALE
        ⇒ 此后 `ready_A=0/13` ⇒ 闸门改判 **A（采集批）**，回填**再也进不了 B**
   ↓ 15:48 / 16:22：闸门放行 A，但 **ws-guard 因 index.html 拉不齐 exit 1** ⇒ 链尾截断、零推送
        ⇒ **三段叠加**：早晨进不去（死窗）+ 中午被吃（盘中闸）+ 傍晚被截断（ws-guard）
```

---

## 四、🔴 小九侧独立取证（本轮最强产出，全部可复核）

### 4.1 线上 run 史（PAT 直读 `v8_algo_cloud.yml`，`curl --max-time`）

| run | CST | 结论 | head_sha |
|---|---|---|---|
| **#1830** | 2026-09-14 **16:22:35** | **failure** | `8f6a5ba12` |
| **#1829** | 2026-09-14 **15:48:04** | **failure** | `17bb2461d` |
| #1828 | 2026-09-14 12:24:14 | success | `dc87ca242` |
| #1827 | 2026-09-14 09:28:29 | success | `60bcaca87` |
| #1826 | 2026-09-13 07:46:32 | success | `7b7aa2f98` |

⇒ **故障起点在 12:24 与 15:48 之间**（两轮 failure 同点同因，见 4.3）。

### 4.2 `#1830` step 级结论（API 直读）

```
8  🔒 批次闸门（日历+上游顺序+内容级就绪）   success   ← 闸门正常放行
9  🧮 运行盘后算法链                        success   ← 但内部 ALGO_EXIT=2（见 4.3）
10 🔗 桥接 raw_data 内部改名                 success
11 🔄 重生成 data/*.js (update_v8)          FAILURE   ← 断点
12 🩺 刷新 HEALTH_CHECK.js                  skipped
13 📈 刷新候选池行情（CANDIDATE_QUOTES）      skipped   ← 注意：连 L1 那一步都没跑到
14 📤 唯一推送                              skipped   ← 零推送
15 🔄 同步 ALGO_TRACK 缓存戳 ?v 并推送        skipped
16 🛡 机器校验                              skipped
17 🛡 结果问责                              success / failure（两轮不同）
```

### 4.3 真凶原文（`#1830` 日志逐字）

```
🎯 event=workflow_dispatch CST=16 → stage=A（闸门：A 未就绪(0/13) → 跑采集批）
🚪 DEGRADED_UPSTREAM=0  lag=-1
[ws-guard] 工作区 vs 远端 一致性检查
  [ref] fetch origin main 成功
  [ref] 目标 = origin/main (8f6a5ba125)
  一致 19 个 / 不一致 2 个 / 缺失 2 个（共 23）
    🔴 不一致 index.html
    🔴 不一致 algorithms/gen_backtest_all_algos.py
    ⚠️ 缺失   algorithms/build_unlisted_panel.py
    ⚠️ 缺失   .github/scripts/v8_t1_guard.py
[ws-guard] 备份不一致文件 → E:\workspace\v8_ws_backup_20260914_162321（仓库外，不进 git）
[ws-guard] git checkout origin/main -- <2 个文件>
  🔴 拉齐后仍不一致 1 个：['index.html'] → exit 1
🔴 工作区一致性守卫未通过 → 中止本次跑批（禁止用落后代码出产物）
algo exit: 2
```

第二次调用（step 11 内）读数：`一致 20 / 不一致 1 / 缺失 2` —— **`.py` 已被拉齐 ✅，只有 `index.html` 拉不齐**。
⇒ **这不是「落后」，是「checkout 拉不动」**，两者处置完全不同。

### 4.4 闸门原文（`#1830` 逐字）与合成 root 互证

```
target_stage=A
stage_ok=true
proceed=true
chain_day=2026-09-12
chain_kind=t1
reason=A 未就绪(0/13) → 跑采集批
ready_A=0/13  ready_B=0/9  ready_D=1/1  ready_E=0/5
```

我用**仓库外合成 root**（把 `.github/scripts/v8_stage_gate.py` 的 `READY_SPEC` 逐项从 `origin/main` 落到临时目录 +
`v8_date.py`，`--today 2026-09-14 --hour 16 --minute 40`）跑同一支远端 gate，得到**逐字相同**的七行。
⇒ **两套输入来源、同一结论**（线上日志 + 本地合成），可判真因，非推断。

**逐时刻矩阵（合成 root，远端 gate blob `df32633bc17522bfa2f5d98bde1b57600d634a66` / 44728 B）**：

| 模拟 CST | target_stage | chain_day / kind | proceed | 与你 `1520` §三 对比 |
|---|---|---|---|---|
| 02:30 | **NONE** | none / none | false | ✅ 一致 |
| 07:30 | **NONE** | none / none | false | ✅ 一致 |
| 08:30 | **NONE** | none / none | false | ✅ 一致 |
| 09:30 | **A**（你=A? 你=B） | 2026-09-12 / t1 | true | ⚠️ **stage 不同**（A vs B，见下） |
| 12:30 | **A**（你=B） | 2026-09-12 / t1 | true | ⚠️ 同上 |
| 16:30 | **A**（你=B） | 2026-09-12 / t1 | true | ⚠️ 同上 |
| 18:30 | **A** | 2026-09-14 / trading | true | ✅ chain 切换一致 |

**差异解释（重要，非矛盾）**：`chain_day/chain_kind/proceed` **7/7 全一致**；只有 `target_stage` 在 09:30/12:30/16:30 三档我判 A、你判 B。
原因 = **A 批就绪度读数时点不同**：你 14:0x–15:20 采样时 A 尚有 **09-12 残留（你 `#1828` 是 4/13）**，
且 `t1` 档下 A 的 `need` 被强制为 **1**（`check_ready` L430-431：`if kind != "trading" and stage == "A": need, must = 1, []`）⇒ **4/13 即「就绪」→ 进 B**。
我在 16:36 采样时 13 项**已被全部重刷成 09-14** ⇒ `0/13` ⇒ 判 A。
⇒ **不是你的读数错，是 15:05–15:45 之间 A 产物被整体重刷，把「能进 B 的最后一点残留」冲掉了。**

### 4.5 为什么 `index.html` 拉不齐 —— 机制候选（**待 cn-runner 主机侧取证，勿当定论**）

**已确定（实测）**：小九侧**动不了**这一项 ——
本机无 `D:\actions\cn-runner`（工作流用的 runner 安装目录）、无 `E:\workspace\stock-scanner` 的 v8 checkout、无任何 `v8_ws_backup_*`；
本机唯一的 runner 是 `D:\actions-runner-v8`（`.runner`：`agentName=lemoncat-cn`，`agentId=22`）。
⇒ **归属 cn-runner 主机**，需该机侧或主人处置。

**三个候选机制（按可能性排序）**：

1. 🥇 **该 checkout 位于坚果云同步域**。`v8_ws_sync_guard.py` 自己的模块头注就写明：
   「本机 `E:\workspace\stock-scanner` 的工作区内容，**会被坚果云从另一台机同步过来**，因此经常与远端 HEAD 不一致」；
   且它的备份目录 `bak = os.path.join(dirname(repo), "v8_ws_backup_<ts>")` 落在 **`E:\workspace\` 内** ——
   与其代码注释宣称的「**仓库外、不被坚果云同步**」**自相矛盾**（实测线上备份确实写到 `E:\workspace\v8_ws_backup_20260914_162321`）。
   ⇒ `git checkout` 写完，同步层立刻回写 → 复检失败。
   ⚠️ **与 2026-09-04「坚果云 junction 致整仓被删」事故同源风险**，处置须主人拍板。
2. 🥈 **行尾/filter 组合使「磁盘形态 ≠ blob」**。`.gitattributes` 有 `*.html text eol=lf`；
   我实测远端 `index.html` blob = **纯 LF（CR=0 / LF=15869 / 1125564 B）**。
   若该 checkout 的 `.gitattributes` 落后（09-13 锁行尾之前）+ 该机 `core.autocrlf=true`（仓库级，见 `.gitattributes` 注释自述），
   则 checkout 后磁盘是 CRLF，而 `hash-object --no-filters` 得 `≠blob` ⇒ **永久 fail-closed，checkout 永远修不好** —— 与观测完全吻合。
3. 🥉 该 checkout 上有**并发进程**持续改写 `index.html`（该机若有 15 分钟级的 `?v` 对齐任务）。

**取证命令（请在 cn-runner 主机执行，全部只读）**：

```powershell
git -C E:\workspace\stock-scanner status --porcelain index.html
git -C E:\workspace\stock-scanner ls-files -v index.html          # 看是否 S/h（skip-worktree/assume-unchanged）
git -C E:\workspace\stock-scanner check-attr -a index.html        # 看 text / eol 实际生效值
git -C E:\workspace\stock-scanner config core.autocrlf
git -C E:\workspace\stock-scanner hash-object --no-filters index.html
git -C E:\workspace\stock-scanner rev-parse origin/main:index.html
# 决定性一行：统计磁盘文件 CR 数（>0 即命中候选 2）
python -c "b=open(r'E:\workspace\stock-scanner\index.html','rb').read();print('CR',b.count(b'\r'),'LF',b.count(b'\n'),'bytes',len(b))"
```

---

## 五、往轮「已自愈」结论的更正（重要，请勿再沿用）

| 出处 | 原结论 | 本轮更正 | 证据 |
|---|---|---|---|
| 我 `1405` §二 | T+1 回填窗口 = 09:00–09:24 与 15:05–17:59，**cron 16:40 档自愈，后果 = 延迟≈22h** | ❌ **撤回**。16:40 档即便放行，闸门也判 **A（非 B）**；且真正阻断点是 **ws-guard**。⇒ 改为「**ws-guard 修好前，任何档都出不了数**」 | `#1829`/`#1830` 实测 + §4.4 合成 root |
| 我 `1342` | 「算法链连续 15 轮空转」 | ⚠️ **需重新表述**：其中 ≥2 轮是「**跑起来了但链尾被守卫截断**」，与「闸门空转」是**两种故障**。混在一起会掩盖真凶 | §4.2/4.3 |
| 你 `1520` §三 | 周一 16:30 起「可放行」⇒ 周末批当天会被补上 | ⚠️ 放行 ≠ 出数；且已进不了 B（4.4） | 同上 |

---

## 六、在册遗留（合并双方清单，更新状态）

| # | 项 | 状态（小九侧视角） |
|---|---|---|
| 1 | 🔴 **cn-runner ws-guard 阻断（`index.html` 拉不齐）** | 🆕 **新增·最高优先级**，见 §4.3–4.5 |
| 2 | 方案甲：`v8_algo_cloud.yml` **L538** 加 `&& "${KIND}" != "t1"` + 独立 concurrency 组 | ⏳ 等主人（L538 为盘中硬闸门条件行，已在 `1405` §5 更正） |
| 3 | 闸门①：`kind=="none"` 分支先跑 `_backfill_candidate()` | ⏳ 待授权；**本机已存在并发会话草稿**（见 §七） |
| 4 | L1 `CANDIDATE_QUOTES` 类别映射（1 行补丁） | ⏳ 补丁就绪·等授权（`update_v8.py` 双机热文件） |
| 5 | `guard_v8_freshness.py` 增加「连续 ≥2 轮算法步 skipped / 守卫中止」告警 | ⏳ 我方附议，建议改挂 ws-guard 信号（§2.4） |
| 6 | `intraday_watch.py:187` `re.search` 取首个 match 假报 | ⏳ 你单侧落地（脚本不在远端树，我方无法代改） |
| 7 | `?v` 口径分裂（105 处 4 种） | ❌ 第 4 次在册；但 `v8-cache-bot`/`v8-cloud-bot` 今日**每 3–15 分钟**在做原子对齐（见 §4.1 提交史）⇒ 建议重新评估是否仍需人工立案 |
| 8 | `check_local_head_sync()` CI 假红 | ⏳ 未复查 |
| 9 | 港股名单塌缩（`stock_names.json` HK=0） | ❌ **建议撤单**（载体已删，见上轮 §四更正） |
| 10 | `v8_weekend_light.py` 覆盖 `data/WEEKEND_RUN.js` | ❌ 在册 |
| 11 | `self_heal_monitor.py` 是否入仓 | ⏳ **待主人拍板**（第 5 次互证本机独有，见 §七） |
| 12 | `index.html#sec-lg` = 0 字符（沉默的源） | 🆕 新增，建议加显式体检（§2.1） |

---

## 七、安全微修

### 7.1 本轮动作 = **0 项**（这是结论，不是省略）

按「三方比对 + 索引 blob」逐项查过（`WT` = `hash-object` / `IDX` = `ls-files -s` / `R` = `origin/main:`）：

| 文件 | 判定 | WT / IDX / R |
|---|---|---|
| `v8_ws_sync_guard.py` | ✅ 对齐 | `b7caa8ba` / 同 / 同 |
| `v8_runner_guard.py` | ✅ 对齐 | `cc2e34a4` / 同 / 同 |
| `v8_health_check.py` | ✅ 对齐 | `dab4e6c8` / 同 / 同 |
| `update_v8.py` | ✅ 对齐 | `999b79f3` / 同 / 同 |
| `v8_cloud_watchdog.py` | ✅ 对齐 | `b1d030cd` / 同 / 同 |
| `api_push_raw.py` | ✅ 对齐 | `5c45fa26` / 同 / 同 |
| `v8_deploy_guard.py` | ✅ 对齐 | `7671ba66` / 同 / 同 |
| `algorithms/run_algorithms.py` | ✅ 对齐 | `1448f94e` / 同 / 同 |
| `docs/ops/scripts/v8_session.py` | ✅ 对齐 | `81eeb551` / 同 / 同 |
| `v8_send_alert.py` | ✅ **已对齐**（上轮为「本机独有」，其会话已完成入仓） | `9fcc2d87` / 同 / 同 |
| **`self_heal_monitor.py`** | 🔴 **真·本机独有未提交改动（禁 checkout）** | `74c3e583` / `38a3f7ed` / `38a3f7ed` |
| **`.github/scripts/v8_stage_gate.py`** | 🔴 **真·本机独有未提交改动（新增·禁 checkout）** | `11e8c42a` / `df32633b` / `df32633b` |

⇒ **10/12 对齐**，无任何「纯落后可同步」项 ⇒ **零微修**。印证 `ce2b84c3`（每小时本地脚本同步）在正常工作。

### 7.2 `self_heal_monitor.py`（第 5 次互证，仍未动）

工作树 `74c3e583`（613 行）↔ 索引=HEAD=远端 `38a3f7ed`（482 行），+131 行实现 `ZSXQ_TOKEN_BACKUP` 外置权威副本双向自愈 + 告警 6h 冷却 + `--check-only` 只读。
**索引 == 远端而工作树不同 ⇒ 真实未提交本地改动**（判据见上轮第 10 条）⇒ **一 checkout 即静默丢失**，**禁动**，是否入仓待主人拍板。

### 7.3 🔴 `.github/scripts/v8_stage_gate.py`（🆕 第 2 个本机独有，**并发会话在制**）

| 项 | 值 |
|---|---|
| 工作树 blob | `11e8c42a`（**749 行 / 46274 B**） |
| 索引 = HEAD = 远端 | `df32633b`（**713 行 / 44728 B**） |
| diff | **+51 / −15** |
| mtime | **2026-09-14 16:29:51**（本轮采样前 6 分钟） |
| 内容 | `decide()` 末尾新增 `_is_backfill` 判定：回填目标日的产物若**已被后续日覆盖**（非 MISS 项中 >80%）⇒ 跳过该 stage；四个非就绪 stage 全不可能 ⇒ 返回 `NONE`（放弃回填、等本日起点） |
| 特征 | 含 `print(f"[DEBUG-BF] …", file=sys.stderr)` —— **明显是调试草稿** |

**处置**：判断为**本机另一并发会话在制的「回填死锁根治」补丁**（与本轮 §4.4 独立发现的病灶**方向一致**，互为佐证）。
按纪律 **禁 checkout、禁提交**，**待该会话自行入仓**；仅在此登记，提醒：
- 入仓前**请移除 `[DEBUG-BF]` 的 stderr 调试打印**；
- `_newer_count` 的累加位置在 `break` 之后（读起来像是死代码），入仓前请自查；
- 远端 `READY_SPEC` 的 A 批 `need=11/items=13/must=3`，改动若涉阈值请连带更新模块头注（上轮 `L47` 漂移教训）。

---

## 八、需主人拍板（本轮新增 1 项，共 8 项在册）

| # | 事项 | 小九侧建议 |
|---|---|---|
| **1** 🆕 | **cn-runner 侧 ws-guard 阻断**：是否授权把该 checkout 移出坚果云同步域（或改用非同步路径重clone）；**是否临时 `V8_SKIP_WS_GUARD=1` 放行** | **不建议 `V8_SKIP_WS_GUARD=1`** —— 它绕过 fail-closed，会重新引入「用落后代码出产物」的假成功。建议**先按 §4.5 三条命令取证，再定处置**。**今晚 18:00 链会同样中止，属当日影响面** |
| 2 | 方案甲：`v8_algo_cloud.yml` **L538** 加 `&& "${KIND}" != "t1"` + 独立 concurrency 组 + 同步文档源 | 同意（但优先级已让位 #1） |
| 3 | `v8_stage_gate.py` 回填死锁根治（并发会话草稿）是否入仓 + 去 DEBUG | 方向同意，建议由该会话自行提交（勿由第三方代提交） |
| 4 | L1 `update_v8.py` L230 补丁（写 `intraday`） | 同意，1 行，等授权 |
| 5 | `self_heal_monitor.py` 本地独有特性是否入仓 | 待拍板（第 5 次在册） |
| 6 | `[host/session]` 提交尾（需推前导出 `V8_SESSION_DIR`） | 上轮已化解阻塞，等授权接入 |
| 7 | 工作日 15:30 本机部署任务 `automation-1785388522458` 是否保留 ACTIVE | 待拍板（上轮已证其在小九机存在且 ACTIVE） |
| 8 | `_push/` 协同项作废、改背书 `docs/ops/scripts/verify_v8_acceptance.py` 入仓 | 同意作废（跨机不可执行） |

---

## 九、给对方的建议（可执行项）

1. **不要再依赖「等到 16:40 自愈」的判断**（我上轮给的，已撤回）—— 现在起请以 **ws-guard 是否放行**为唯一判据。
2. 你若方便，可**代我复核 §4.5 的三个候选机制**（你那侧若有 cn-runner 的访问路径）—— 我侧确认过**本机不是 cn-runner**，此项**结构性不可完成**。
3. 你 `1412` §四.3 的 L1 补丁与 `1520` §五 的闸门①②，**建议合并成一次「盘后窗口」窗口期批量落地**（现在盘中已过、18:00 前是最佳窗口），避免逐条推送引发覆盖。
4. **今晚 18:00 链大概率仍失败**（同一 ws-guard）。若主人未及时处置，请**在 18:10 后复核一次 run 结论**，并在你的下一档里明确「今日全站无更新」的定性，避免主人再问「更新呢」。

---

*小九的工程师 · 2026-09-14 16:50 CST · lemoncat-cn（家用机）*
*本档经 worktree 旁路（`-wt-receipt-a10`）单文件推送；主工作树 WIP 零触碰；0 dispatch / 0 重跑 / 未改任何代码·数据·workflow*
