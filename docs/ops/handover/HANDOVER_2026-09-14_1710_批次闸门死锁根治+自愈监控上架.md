# 交接文档 · 小九 →（双机/云端） 2026-09-14 17:10

> 主题：批次闸门回填死锁根治（#95）+ 自愈监控 zsxq_token 双写/丢失告警上架
> 机器：小九单位机（白天窗 07:45–17:45）；阿狸咪窗 18:00–次日 07:30

## 一、本次上架内容（已推 main，CI 自动构建）

| 文件 | 改动 | 性质 | 风险 |
|------|------|------|------|
| `.github/scripts/v8_stage_gate.py` | +77 行（新增 `_backfill_feasible` 助手，重写 step-3 回填可行性判定） | 🔴 P0 死锁根治 | 低（纯逻辑，已回归测试） |
| `self_heal_monitor.py` | +139 行（zsxq_token 外置副本双写 + 丢失 infra 告警 + `--check-only` 只读态） | 可靠性增强 | 低（try/except 包裹，告警失败不阻断主流程） |
| `docs/ops/handover/HANDOVER_2026-09-14_1710_*.md` | 本文档 | 交接 | 无 |

## 二、🔴 P0 根因与修复（批次闸门回填死锁）

**根因**：当下时刻 < 盘后链起点（交易日 18:00）时进入 `_backfill_candidate` 回填上一数据日；
若回填目标日（如 09-12）的产物已被后续日（09-13/09-14）的批次**全覆盖**，其 check_ready 永远判 STALE
→ 闸门每轮放行 A → A 跑完写「今日」新数据 → 旧坑永远填不上 → **无限循环（假成功）**。

**旧判定缺陷**：原可行性检查只看「该 stage 自身产物」日期，导致 D 批（FINAL_RECOMMEND_DATA.js 仍停在 09-12）
被误判「可回填」，重跑 D 却拉 09-13/09-14 上游 → 产出更新文件 → 永远填不上 09-12。

**修复**：新增 `_backfill_feasible(root, stage, tday)` —— 沿 **PREREQ 整条上游链** 收集输入文件，
只要任一输入日期**严格晚于**回填目标日 tday → 判「不可能回填」→ 跳过该 stage；
四个非就绪 stage 全不可能 → 返回 `NONE`（放弃回填，等本数据日 18:00 起点）。

**验证**（本地，已跑通）：
- ✅ 真实场景（09-14 16:xx，上游 A=09-14 / B=09-13 / D=09-12）：`target_stage=NONE, proceed=false`
  `reason=回填目标 2026-09-12 的 ['A','B','D','E'] 全被后续日覆盖->不可能->空转等本日起点`
- ✅ 回归：同日夜补跑（上游仍停 09-12）→ `target_stage=B`（合法补跑保留，run#1692 用途未退化）
- ✅ `py_compile` 通过；DEBUG-BF 临时打印已清除；无悬挂变量引用

## 三、自愈监控 zsxq_token（上轮未推，本次一并上架）

- 外置权威副本 `~/.workbuddy/v8_zsxq_token.json`（仓库之外，不被仓库清理波及）
- 仓库缺 + 外置在 → 自动回填（真自愈）；仓库在 + 外置缺 → 静默补写（双写保护）
- 两处皆无/格式非法 → `v8_send_alert` 发 **infra** 邮件，状态翻转即发、同态 6h 冷却去重
- 告警失败**绝不阻断**主流程；`--check-only` 模式保持只读语义

## 四、⏸ 本次**未做**（专家判定，附理由，待拍板）

| 项 | 状态 | 理由 |
|----|------|------|
| #96 新增「算法步跳过但报成功」连续 2 轮告警 | 暂缓 | 根因（回填死锁）已由 #95 根除；该告警属防御性增强，若阈值不当会触发**邮件风暴**（主人最忌），需明确消费者与阈值后再落。建议：后续在 `self_heal_monitor.py` 加「同 stage 连续 3 轮释放但产物无进展 → infra 告警（6h 冷却）」 |
| #97-1 `_slotByTs` 09:00–09:30 误判「盘中」 | 不实 | `9:00–15:30→盘中` 是 2026-08-15 既定口径（index.html L675 注释确认），开盘 09:30 前属开盘过渡窗，非 bug；改之会影响 15+ 处 `auto` 调用点，需主人确认语义再动 |
| #97-2 `_slotHtml` 死代码 | 已本地删除 | 原变量计算后从未被引用，已删除（满足「死代码彻底清理」）；**因 阿狸咪 17:04 已推 index.html 204 行改动，为避免热文件冲突/截断，本次暂未随 .py 一并推送，下轮同步时再上架** |
| #97-3 运维页 mahoro cookie「仅倒计时不校验数据产出」 | 暂缓 | 目标位置有歧义（maharo 卡 freshness 胶囊 vs 运维页 token 清单），需主人指定位再补 |

## 五、推仓防覆盖核对（已执行）

- ✅ `git fetch` → `origin/main` 由 `cb5cbf511` 前进至 `4c7507518`（阿狸咪 17:04 构建，含 index.html 204 行）
- ✅ 本地 main **0 领先 / 16 落后** → `git rebase origin/main` 干净快进，无本地提交被覆盖
- ✅ 2 个 `.py` 文件**不在**阿狸咪 65 文件改动清单 → rebase 无冲突
- ✅ 仅 `git add` 2 个 `.py` + 本文档，**未** `git add -A`（避开 raw_data/*.json 实时心跳/数据文件与 backup/logs/审计残留）
- ⏳ 推后复核：`git rev-parse origin/main` + 内容级 grep 确认本批改动落地（见推送后核对）

## 六、线上核对清单（阿狸咪/云端接班后）

1. `python .github/scripts/v8_stage_gate.py --root .` 在 18:00 后应开始放行今日 A 批（不应再卡 NONE）
2. Pages 构建成功（CI `v8_build_deploy.yml`），主站可访问
3. zsxq_token 双写：仓库 `data/zsxq_token.json` 与 `~/.workbuddy/v8_zsxq_token.json` 并存
4. 若收到「[v8自愈] zsxq_token 本地丢失」infra 邮件 → 按正文步骤补 token（生产零影响，云端兜底）
