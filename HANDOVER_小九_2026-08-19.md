# 交接文档 · 小九 → 阿狸咪 · 2026-08-19（周三）

> **更新于 2026-08-19 12:06 自动更新**（本机时间 CST）
> 本文由小九 12:00 档自动化生成，所有 commit SHA 均从 `git log origin/main` 实核，非记忆值。
> 数据源：`quant-scanner-v8/.workbuddy/memory/2026-08-19.md`（34KB，08:00–11:32 当班全量）+ 仓库/线上实核。
> stock-scanner 侧当日日志不存在（08-11 后断更），按惯例以 v8 侧为准。

---

## 一、背景

- 今天是**交易日（周三）**，小九白天独家值班（07:45–17:45），你 18:00 后接手。
- 今日是**高强度修复日**：主人连发 10+ 轮截图反馈，主线是「盘中数据卡在盘前 / 卡片空白 / 卡片加载中」。
- 今日 main 累计 **140 个提交**，其中小九人工修复 14 个、其余为云端 build/cn fetch/risk gauge 例行。
- 今日落 **18 份 URGENT 留痕**（`URGENT_小九_2026-08-19_*.md`）。
- **工作流铁律今日被主人改了**：08:32 主人明令「改完就直接上线，不要再问」。此后所有本地改动一律 `git add + pull --rebase --autostash + push`，不再逐项征询。**你今晚接手请沿用此约定。**

---

## 二、今日已完成改动（真实 commit SHA + 影响）

### A. 告警/自愈方针对齐（动量 OCR 卡不再轰炸）

| SHA | 内容 | 影响 |
|---|---|---|
| `a57555353` | `self_heal_monitor.py` P0-2：momentum 陈旧降级 WARN 免告警；`--json` 模式 stdout 纯净 JSON | 止住每 10 分钟一封的告警轰炸；三模式 exit=0 |
| `be306fa05` | `v8_health_check.py` 4 处对齐：MOMENTUM* 降级 warn / 邮件白名单拓宽 / parse_time 支持裸日期 / ts 兼容 generated_at | 看板红叉消失，`{ok:97,warn:5,fail:0}` |
| `504c5b764` | HEALTH_CHECK 刷新（overall=ok, fail=0） | 看板落地 |

**根因**：两个巡检脚本方针不一致——health_check 仍判动量 FAIL，self_heal 已豁免。**动量数据本身仍需主人喂盘后选股 PDF → OCR**，看板保留 warn 是预期状态，不是故障。

### B. 盘前全量 + runner 重启自愈

| SHA | 内容 |
|---|---|
| `a03db1b0c` | 08:15 盘前全量 cn fetch 14/14 全绿 + update 64js |
| `408eebadf` | HEALTH_CHECK 08:30 状态刷新（原 `fd5cfbecf` 因 rebase SHA 漂移） |
| `391768ef0` | H_AUTO_BUY 数据 + 4 个审计脚本入库（原 `cecbd0528` rebase 后 SHA） |

**⚠️ 本机 07:54:40 发生过系统重启**（LastBootUpTime 实锤）→ runner Listener 07:53 中断且**未自启**。守护脚本 heal 层拉起失效（Popen 嵌套 powershell 在沙箱失效），最终手动 `Start-Process Runner.Listener.exe` 直启成功。**当前 Listener PID 14408（08:15:20 起）存活。**

### C. 最终推荐门禁撤销

| SHA | 内容 |
|---|---|
| `51a6c986d` | 撤销 8/18「最终推荐盘前/盘中不出数据」门禁（index.html:12767 `if(_hr<15.5){...return}` 移除，仅 6 行） |

**注意**：这是**主人主动撤销自己 8/18 的决策**并明确承担风险（盘前 Top5 可能被误用为盘中决策）。原注释保留为审计留痕 + 恢复方法。若主人今晚又说「盘前不该出」，请先指出这是 8/19 09:06 他本人授权撤销的，不要直接改回。

### D. H 反推短线买点：真正打通全链（今日最大特性）

| SHA | 内容 |
|---|---|
| `6e197627f` | 新增 `algorithms/track_h_auto_buy.py`(220行) + 挂 `run_algorithms.py` ORDER + 注册 `api_push_raw.py` 三处 + index.html 渲染卡 + TRACK 种子 |

**踩过的坑（务必记住）**：先前 `be306fa05` 表面跑通，实际链断 4 处——① data/H_AUTO_BUY.js **无消费者**（index.html 没引用）② api_push_raw `walk_extra` 未注册 → 每日 cron 不推送 ③ `run_algorithms.py` ORDER 未含 → 不自动跑。
**今后契约**：主令「X 能每天自跑 + 跟踪 + 看板可见」= 写脚本 ✅ + 挂 scheduler ✅ + 注册 push 队列 ✅ + 写仪表盘消费者 ✅ + 累计历史 ✅，**五步一个都不能省**。

### E. 盘中链路一劳永逸（今日投诉最集中）

| SHA | 内容 | 根因 |
|---|---|---|
| `f2042e4cc` | ETF 日监控删除盘前早退占位 + 加 `_dataHint` 动态提示 | `if(_hr<9.5){return}` 让主人以为「数据没了」，其实数据完整 |
| `1f3b77f68` | index.html:423 加载 `SECTOR_FUND_FLOW_INTRADAY.js` + 重发新鲜数据 | **该文件从未被任何 script 标签加载**，`window.SECTOR_FUND_FLOW_INTRADAY` 永不定义 |
| `a41d1b89d` | 补真实 09:54 分时快照（凑够 ≥2 才渲染曲线） | — |
| `f0e91923f` | 云端 10:30 schedule 整轮跳过 → 本机救急重抓盘中 7 卡（10:54） | GitHub Actions 高负载时段整轮跳过 cron |
| `ad76d807e` | **三链路根治**：① `update_v8.py` DATA_SOURCES 补挂 `sector_fund_flow_intraday.json`→`SECTOR_FUND_FLOW_INTRADAY` + CATEGORY_MAP intraday ② `cloud_fetch_v8.py` em_clist/em_ulist_np 加 `_em_get_with_retry` 指数退避(0.5/1.5/3.0s+抖动) ③ 商品涨价榜 tryRun 加 setTimeout 兜底链 | 「fetch 写了 raw 但 build 没映射」类漏挂（与 8/18 OVERSEAS_MARKETS 同型） |
| `4a01c014f` `d8729a628` | 清理临时验证脚本 | — |

**🔴 关键教训（请你也遵守）**：任何新增 `raw_data/*.json` **必须同时挂 `DATA_SOURCES` + `CATEGORY_MAP`**，否则 raw 抓到了但 `data/*.js` 永不发布，卡片永远陈旧。建议后续加 grep 双向校验（`VAR_TO_RAW` ↔ `DATA_SOURCES` 一一对应），尚未落地。

### F. UI：取消隐藏式滚动/分页（主人偏好「一眼到底」）

| SHA | 内容 |
|---|---|
| `568e0c356` | 概念资金热力图 + 行业树图·细分赛道 `overflow:auto`→`visible`，全部展示（21/21 + 50/50 实渲染验证） |
| `67ba0da13` | 主力净额分时曲线：`_SF_PALETTE` 64 色 + `_sfHashIdx` 双 hash(FNV-1a+sdbm) + 碰撞跳过 → **21/21 线条颜色 100% unique**；legend 改 HTML 自渲染 flex-wrap，ECharts `legend.show:false` 彻底去掉「1/2」翻页 |

**主人偏好已确立且不再提醒**：禁止隐藏式滚动条、禁止 ECharts scroll legend 分页、禁止下拉折叠。与之前「不要大版面/2-3 列」一脉相承。

### G. 空白页/被覆盖：永久自愈轮询

| SHA | 内容 |
|---|---|
| `e1483142c` | **4 个 admin 页全空致命修复**：`habSum.T+1_rate` 被 V8 解析成 `T` + 非法字面量 `1_rate` → **整个 `<script>` 块语法崩溃** → `renderUnlisted/__renderOps/switchLg` 全部未注册；另修 `switchSec('st')` 调用从未定义的 `window._renderSecSt`（应为 `window.switchSt('tc')`） |
| `bbac395de` | `selfHealCeBody()` + `__selfHealJudgmentHeader()` **每 1 秒永久自愈轮询**，放弃 setTimeout 一次性猜测和 30 次重试上限 |
| `df6271315` | 暂未上架页空白永久自愈（unlistedPanel len<1000 自动重渲）※11:54，日志未覆盖窗口 |

**🔴 两条硬铁律**：
1. **含 `+` 的字段名必须用方括号** `obj['T+1_hit']`，**绝不能** `obj.T+1_hit` —— 会静默令整块 script 崩溃，极难察觉。
2. **任何卡头/卡片渲染都必须自带「空则重渲」自愈**，否则 100% 会被 cloud build / CDN 缓存 / v8CardHeader 截断 bug 反复弄丢。
3. 建议加 CI 卡点 `node --check` 对 index.html 全内联脚本做语法门禁（**尚未落地**）。

### H. 其他

- **08:30 主人令「全部模型改为 Hy3」**（这是 08-06「等主人通知」的那个通知）：本机 19 个未删除任务已**全部 `model_id=hy3`** ✅，另把软删的 `1785980310111` 也置 hy3。**⚠️ 你侧 automations 表需你自己同步切 hy3，本机只管本机。**
- `fc42c8f17` westock 持仓实时刷新（12:04，日志未覆盖窗口）。
- 看门狗第 265/269/271/273/274 轮均已留痕，六项全 OK。

---

## 三、当前系统状态（12:06 实核）

| 项 | 状态 |
|---|---|
| **线上站点** | ✅ HTTP 200，916 KB |
| **origin/main HEAD** | `f121d36f8`（v8 risk gauge 12:05） |
| **本地 main** | ✅ 已 ff 对齐 `f121d36f8`，与 origin 零分叉（0/0） |
| **健康检查** | ✅ **overall=ok · ok 99 / warn 4 / fail 0 / total 103** |
| 4 项 warn | 全部已知良性：`H_AUTO_BUY_TRACK`（无时间戳，缺审计登记）+ `MOMENTUM_FILTER` / `STOCK_MOMENTUM_STATE` / `_V2`（OCR 人工依赖，已降级免告警） |
| **本机 runner** | ✅ Listener PID **14408** 存活（08:15:20 手动拉起后未再掉），无 Worker=空闲 |
| **runner_status** | 11:29:14 intraday，**15/15 模块全 ok，0 non-ok** |
| **盘中 7 卡新鲜度** | ✅ 全部 11:25–11:29（INDEX_QUOTES 11:25:14 / SECTOR_FUND_FLOW 11:25:13 / CONCEPT_RANKING 11:25:15 / LIMIT_UP_HEATMAP 11:25:37 / ETF_PULSE 11:26:35 / MARKET_ALERTS 11:28:22 / OVERSEAS 11:29:14） |
| **分时曲线** | ✅ `SECTOR_FUND_FLOW_INTRADAY` 今日 **6 快照**（08:31/09:54/10:07/10:17/10:54/**11:25**）→ 云端 cadence 已自动发布，`ad76d807e` 的 DATA_SOURCES 补挂**已验证生效** |
| 最终推荐 | `FINAL_RECOMMEND_DATA` 07:21:51（盘前定稿，门禁已撤销 → 前端可见） |
| 云端节奏 | cn fetch 最新 11:29；build 11:07/11:13/11:18/11:24/11:26/11:32/11:36/11:56 密集正常。**12:00 无 cn fetch 属午休（11:30–13:00）预期行为** |
| 剩余自动化 | **20 个**：19 ACTIVE + 1 PAUSED（`1785718343599` v8 管线看门狗每 2h，历史暂停） |
| 心跳 | 最新 `2026-08-19 08:29:58 | xiaojiu | v8_premarket_full | DONE`（本档追加中） |

---

## 四、需你注意 / 待办 / 风险

### 🔴 P0 — 必须处理

1. **`STOCK_QUOTE` 陈旧 32 小时**：`data/STOCK_QUOTE.js` meta.update_time = **2026-08-18 04:07:12**（源 `akshare.stock_zh_a_spot` 新浪）。
   - **为什么严重**：`algorithms/auto_run_dn_algorithm.py`（H 反推）和 `scripts/momentum_common_filter.py` **都读 STOCK_QUOTE.js**。它陈旧 → 今日新上线的 H 反推买点和动量共识筛选**都在用昨天的行情算**。
   - 健康检查**没抓到**（该项被判 ok），属巡检盲区。
   - 建议：先确认 akshare 新浪源是否被限流，再决定补抓或换源。

2. **⚠️ 工作树有 3 项未提交改动（另一并行会话在做，我按指令未 add）**——你接手前先看清是否要继续：
   - `M .github/scripts/cloud_dispatcher.py`（+55 行）
   - `M self_heal_monitor.py`（+69/−2）
   - `?? .github/workflows/v8_algo_intraday_lite.yml`（新文件，未跟踪）
   - 用途：修「动量共识筛选卡好几天没动静」——根因是 `momentum_common_filter.py` **压根不在任何自动链里**，靠 cloud_dispatcher 在检测到「新 OCR 输入未重算」时幂等派发这个轻量 workflow。**逻辑看着是对的，但未提交=未生效。**

3. **小九已顺手修掉一个隐性失效**：`automation-1787108824799`（v8 盘中漏轮本机兜底，今日 10:50 新建）原 rrule `BYHOUR=1,2,3,4,5,6,7` —— WorkBuddy 的 rrule BYHOUR **是本地 CST 不是 UTC**（本任务 `BYHOUR=12,18` 就是 CST 12/18 点实证），所以它只会在**凌晨 01:00–07:30** 触发，而其 prompt 又规定「只在 09:30–15:00 动作」→ **每次触发都是空转，兜底等于没装**。已改为 `BYDAY=MO..FR;BYHOUR=9,10,11,13,14;BYMINUTE=0,30`（下次 13:00 生效）。**你若新建 rrule 自动化，一律按 CST 写小时。**

### 🟡 P1 — 观察/待授权

4. **云端东财 push2 抓取不稳定**：今日 10:07 失败、10:21 自愈、10:30 整轮 schedule 被 GitHub 跳过。`ad76d807e` 已加指数退避重试（B 方案），但 **GitHub 整轮跳过 cron 无法靠重试解决**。
   - 主人**明确否决 A 方案「本机常态兜底」**（省 TOKEN，云端才是主力）。
   - **待主人授权的最优解**：给本机配有限权限 GITHUB_TOKEN，让本机每 30 分只做 `workflow_dispatch`「按铃」（按铃 ≠ 抓取，符合主人省 TOKEN 意图）。本机目前**无 PAT，SSH key 不能 dispatch**。
5. **runner 开机不自启**：今晨系统重启后 runner 未自动拉起，需人工介入。两项待修：① `v8_runner_guard.py start_runner_process()` 改为直启 `Runner.Listener.exe`（绕过 run.cmd/PowerShell 嵌套，沙箱下会失效）；② runner 服务化/任务计划实现开机自启。
6. **动量三卡仍等主人喂盘后选股 PDF → OCR**，看板 warn 是预期，**别当故障去修**。
7. `H_AUTO_BUY_TRACK` 无时间戳 → 健康检查 warn，需补审计登记（小事）。
8. `.gitignore:8` 只写了 `_tmp_*/`（目录），同名临时**文件**（如今日出现的 `_tmp_all.js` 706KB）不被忽略会污染 `git status`，建议补规则 `_tmp_*`。
9. 🟡 今日某处把 `var series=` 写成 `series=`（隐式全局）。当前块无 `use strict` 不崩，但 index.html:6764 另有一处 `use strict`——将来合并/加严会直接报错。**凡新增变量必须显式 var/let。**

### 🔵 方法学（今日血泪，请沿用）

- **判新鲜看语义 `update_time`，绝不用 mtime**；且 **08:46 不叫「新鲜」——开盘 09:30，盘前时间戳就是断档**（小九 09:32 就误判过一次）。
- 「数据没了」先查 **render 早退代码**，再判缓存。今日两次都是 render 早退（ETF 日监控 `_hr<9.5`、最终推荐 `_hr<15.5`），跟缓存无关。答「Ctrl+F5 硬刷就行」是错的。
- **HEALTH_CHECK 有盲区**：它查 `raw_data/`（新鲜）给假 OK，而卡片读 `data/`（可能陈旧或未加载）→ 「看起来 ok 实则断」。
- 主人若忘了自己几小时前的决策，**先一句话定位矛盾点**（「这不是 bug，是按你 8/18 令执行」）+ 让他明确撤销/维持/折中，**不要自由发挥改代码**。
- rebase 冲突口径：`raw_data/runner_status.json` 永远 `--theirs`；`data/HEALTH_CHECK.js` 一律取远端云端版，别推本地重算版。
- 沙箱坑：`curl -o 落盘` 被拦截（error 23 且会挂后台）→ 一律 `curl | python` 管道。

---

## 五、小九监督职责（今晚→明晨）

- **12:00–17:45**：小九继续白天独家值班。盘中每 30 分兜底（freshness_guard）+ 每小时看门狗 + 15:30 收盘刷新 + 17:00 选股四模块全量部署，均自动跑。
- **17:45 后交由你**：18:00 起你为唯一部署者。小九 19:00 还有一档「每日审计修复+推仓+对齐」自动化会跑（`1785978303212`），若与你的部署撞车，以你的为准，小九侧只做审计不抢部署。
- **18:00 档交接文档**：小九会再自动更新本文件一次（覆盖 12:00–18:00 增量），你以最后一版为准。
- **你接手第一件事建议**：① 看 P0-1 STOCK_QUOTE 是否要补抓；② 决定 P0-2 那 3 个未提交文件是否上线（这是动量卡「好几天没动静」的正解）；③ 别碰 `raw_data/candidate.json`、`data/HEALTH_CHECK.js`、`v8/*.py`。
- 有异常直接在仓库落 `URGENT_阿狸咪_2026-08-19_*.md`，小九明早 08:00 交接检查会读。

---

*小九 · 2026-08-19 12:06 · 本档仅提交本文件，未触碰工作树其余 3 项改动*
