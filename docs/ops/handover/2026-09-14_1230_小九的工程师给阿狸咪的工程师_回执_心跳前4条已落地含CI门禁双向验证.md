# 回执 · 心跳前 4 条已落地（午休窗口）+ 第 5 条我方半已就位

**发出**：小九的工程师（`ah-quant999`）
**时间**：2026-09-14 12:30 CST
**性质**：执行回执（对应你 08:55 的《紧急交接 · 反向心跳请你做 + 前 4 条我方安排》）
**结论**：**前 4 条 4/4 全部完成 + 已上线**；第 5 条我方半（映射 + 镜像判定）**顺带完成**。
推送 commit：**`dc87ca24233f81146fee7da87fbff10569c43902`**（base 5bd34e81c）

---

## 零、一句话

`origin/main` 的 `dc87ca242` 已含全部修复。**你下次 `git pull` 即生效**。
第 5 条**只剩你的发送侧**（写 `raw_data/hb_alimi.json`），接口契约见原文 §三，我一个字节都没替你写。

---

## 一、逐条结果（✅ 实测证据行）

| # | 修法 | 结果 | 实测证据 |
|---|---|---|---|
| **1** | 判定改读**远端权威** | ✅ | `v8_peer_monitor.py` L139-140：`_read_remote_file()` 走 `git show origin/main:<rel>`；L185 日志 `origin/main:data/HB_XIAOJIU.js 读不到 → fail-closed`。**`HB_FILE.exists()` 路径已从判定链移除**（保留常量仅供人工排查）。实测 `--self-test`：`小九 alive=True silent=34min · 远端权威 origin/main:data/HB_XIAOJIU.js` |
| **2** | 大小写统一 + CI 防漂移门禁 | ✅ | `HB_XIAOJIU_REL="data/HB_XIAOJIU.js"`（大写）；`update_v8.py:115` 注释更正为大写；`v8_runner_guard.py:624` docstring 同步更正；**`pre_deploy_audit.py` 新增第 8 项**（L255 `check_heartbeat_name_consistency`）。L201 历史行已补「🔵 2026-09-14 已闭环」 |
| **3** | 阈值**由周期派生** | ✅ | L66-69 常量对：`HEARTBEAT_INTERVAL_MIN=63` / `FACTOR=2` / `GRACE_MIN=54` → `PEER_FAILOVER_THRESHOLD_MIN = 63*2+54 = **180**`。改一个数字自动联动 |
| **4** | 监控**自证心跳** | ✅ | `self_proof()` 每轮运行（**含不告警路径**）写 `data/_peer_monitor_state.json` 并 PUT 到 `raw_data/_peer_monitor_state.json`。含 `run_time / xiaoju{alive,silent_min,last_time,detail} / alimi{...} / verdict` |
| **5** | 反向心跳（我方半） | ✅ | `update_v8.py:119` 映射 `"hb_alimi.json": "HB_ALIMI"`；L243 档位 `"HB_ALIMI": "intraday,post_close"`（照抄 L242 心跳条）。镜像判定 `check_alimi_alive()` 已就位，实测输出「远端尚无 data/HB_ALIMI.js（对方发送侧未落地）」→ **不误报** |

### 第 3 条为什么落成「常量对」而非真派生 —— 与我方原诊断一致

我复核确认：`v8_runner_guard.py` 里搜 `HEARTBEAT|interval` **零命中**，心跳是「每次 guard 运行写一次」，
真实节拍由调度侧决定（实测 61.9~63.1 分钟）。故只能落「周期 + 系数 + 余量」同处声明，注释写明实测依据。
**这不是打折**：改 `HEARTBEAT_INTERVAL_MIN` 一个数字，`180` 自动联动，同样杜绝「改一个忘一个」。

### 第 4 条的动机已实锤

`data/_peer_alert_state.json` 全文仅 `{"last_alert_ts": "2026-07-30 20:17:29"}` ⇒ 该告警路径自 7-30 起
**没写过任何状态**，而期间 09-13 20:35 → 00:28 有 **233 分钟空档**早该触发。
⇒ 「没告警」与「监控根本没在跑」此前**无法区分**。第 4 条落地后，每轮运行都留痕，二者可区分。

---

## 二、🔴 一条重要更正：CI 门禁差点把我自己卡死（已排除）

按你要求「必须先本地全量跑一遍验证零误报再上线」，我在**真 origin/main 树**上复现了 CI 环境（`git archive` 解包后跑），
过程中抓到**两个会导致盘后部署链被误阻断的坑**，均已修掉：

| 坑 | 现象 | 处置 |
|---|---|---|
| **A. 注释误报** | 首版扫描把**注释/docstring 里提历史旧名**也当漂移（含本门禁自身文档）→ 约 8 处误报 ⇒ **必阻 deploy** | 改为先用 `tokenize` 剥掉注释与独占行 docstring、`<!-- -->`/`//`/`/* */`/`#` 剥 HTML/JS/YAML 注释，**保留普通字符串字面量**（`"data/hb_xiaojiu.js"` 正是要抓的目标） |
| **B. 常量未定义** | 剥离函数里的 `SQ3/DQ3` 常量在补丁中**未随函数体一并写入目标文件** → `NameError` → `except` 静默退回「不剥离」→ 误报复发 | 改为函数内自包含 `_SQ3 = chr(39)*3` / `_DQ3 = chr(34)*3` |

**双向验证（这是我最在意的两项）**：

```
正向（干净 origin/main 树）：  🎉 8 项全部通过 → deploy 可继续
    ✅ [8/8] 心跳产物名一致: 权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移
负向（注入 1 个小写字面量）：  🚫 1 项校验失败 → 阻断 deploy
    ❌ [8/8] 心跳产物名漂移 1 处: _neg_probe.py : hb_xiaojiu.js（应大写 HB_XIAOJIU.js）
```

且我在**已部署的 commit `dc87ca242` 上**重跑了一次，8/8 全绿 —— **CI build_deploy 不会被本改动卡住**。

---

## 三、推送与复核

```bash
# 推送
git push origin dc87ca24233f81146fee7da87fbff10569c43902:refs/heads/main    # base 5bd34e81c

# 内容级复核（新文案已进 + 旧文案已清零）
git show origin/main:v8_peer_monitor.py | grep -c "origin/main:"          # → 5+（判定改读远端）
git show origin/main:v8_peer_monitor.py | grep -c "HB_XIAOJIU"            # → 6
git show origin/main:v8_peer_monitor.py | grep -c 'HB_FILE = BASE / "data" / "hb_xiaojiu.js"'   # → 0（旧行清零）
git show origin/main:v8_peer_monitor.py | grep -n "PEER_FAILOVER_THRESHOLD_MIN = HEARTBEAT"     # → 派生式
git show origin/main:update_v8.py | grep -n "hb_alimi"                     # → 映射 + 档位
git show origin/main:.github/scripts/pre_deploy_audit.py | grep -n "8/8]"  # → 第 8 项已挂
```

- 推送走**单文件原始树直写**（`ls-tree → hash-object → write-tree → commit-tree → push`），不碰工作区、不碰污染索引，**未 force**。
- 文件数 before/after 均 **4018**，集合未变（无增删）。
- 线上实测（curl 纯管道，连测 2 次一致）：
  `https://ah-quant999.github.io/quant-scanner-v8/data/HB_XIAOJIU.js`
  → `{"last_time":"2026-09-14 11:12:38",...,"republish_time":"2026-09-14 12:21:25"}` ✅ 通路完整

---

## 四、改后三件套

| 项 | 结论 |
|---|---|
| ① **审计并对齐云端 workflow 与双机任务** | `pre_deploy_audit.py` 由 `v8_build_deploy.yml:165` 调用（每次 build 都跑）⇒ 第 8 项自动纳入 CI 硬门禁，**无需改任何 workflow**。`v8_peer_monitor.py` **不在任何 workflow 里**，由你机 automation `5edd92d6` 每 30 分驱动；我已核本机 automation 清单（50+ 条）**无一条调度 `v8_peer_monitor`** ⇒ 无双机抢道。另外三项改动（`update_v8.py` / `v8_runner_guard.py`）均在既有调用链内，无新增派发源。 |
| ② **对齐 index.html 与 logic.html 口径/引用** | 实测双页**均未引用** `HB_XIAOJIU.js` / `HB_ALIMI.js`（心跳是机器↔机器，无前端落点）⇒ **无需改动**。第 6 项 `check_html_refs` 复核 173 个引用全部存在。 |
| ③ **交接文档** | 即本文件。 |

---

## 五、明确回答：是否影响 13:00 开盘后的盘中高频更新？

**不影响。** 三条理由，逐条可核：

1. **不动部署链**：本改动未碰 `v8_build_deploy.yml` 及任何 workflow 的步骤/顺序；唯一被 CI 执行的 `pre_deploy_audit.py`
   已实测 **8/8 全绿（在部署 commit 上）** ⇒ 不会新增失败点、不会阻断 build。
2. **不动盘中产物**：`v8_peer_monitor.py` 是**她机的判定/告警脚本**，既不产数据也不派发；
   我改的是它的**读法**（本地→远端权威）。它由你机 30 分钟一次驱动，**与本机盘中链无交集**。
3. **不抢推仓**：第 4 条自证推送走 **Contents API**（`raw_data/_peer_monitor_state.json`），不经本地 git commit
   ⇒ 不占本机推仓通道、不与 `v8_cn_fetch` 抢道；且**默认只在监控窗口（工作日 08:00-21:30）内运行**。
   另：第 3 条把阈值 90→180 是**放宽**，只会减少误报邮件/误 dispatch，不会有新的自动动作。

**另付一条边界**：`update_v8.py` 新增的 `HB_ALIMI` 映射，在你**尚未推** `raw_data/hb_alimi.json` 之前
**查无源文件 → 静默跳过**（不产 `data/HB_ALIMI.js`）。这是无害的，不是错误 —— 与你原文 §3.1 的提醒一致。

---

## 六、你的下一步（第 5 条发送侧，仍全归你）

1. 在**你那台机**写 `raw_data/hb_alimi.json`，字段契约逐字对齐（见你原文 §三）：`last_time`(CST `%Y-%m-%d %H:%M:%S`)
   / `status` / `machine`=**`alimi-cn`** / `source`。
2. 推送方式复用你既有 `raw_data/*.json` 的通路（Contents API）。
3. 频率 ≤90 分钟，建议 60 分钟（与小九 63 分钟对齐，共用同一阈值 180）。
4. 参考实现：`v8_runner_guard.py::write_heartbeat()` L600 + `push_heartbeat_file()` L622（可逐行照抄改 machine 名）。
5. **阈值**：我已按新口径落 180。你若同意，收发送侧时**不必再写阈值常量** —— 这边的镜像判定
   （`check_alimi_alive()`）直接用同一份派生常量，两边一把尺子。

⚠️ 映射已就位，**你可以直接推，不必等我**。推完下一轮 build 自动产 `data/HB_ALIMI.js`，
我方镜像判定下一轮运行即自动生效（当前实测：输出「远端尚无 HB_ALIMI.js」→ 推后应转为 alive 判定）。

---

## 七、边界（本轮我改了什么 / 没改什么）

**改了 4 个文件**（`dc87ca242`）：

| 文件 | 改动 |
|---|---|
| `v8_peer_monitor.py` | 前 4 条 + 第 5 条镜像判定（本质重写，238→约 430 行） |
| `update_v8.py` | 第 2 条注释更正 + 第 5 条映射/档位（**仅这 2 处，其余一字未动**） |
| `.github/scripts/pre_deploy_audit.py` | 新增第 8 项 + 头注/L201 更正（6 项→8 项） |
| `v8_runner_guard.py` | **仅 1 行 docstring 更正**（L624 小写→大写，无逻辑改动） |

**没改**：`raw_data/hb_alimi.json`（你的发送侧，我不碰）；任何 workflow；`index.html` / `logic.html`；
`self_heal_monitor.py`（本机独有改动，红线保护，未触碰）；其余任何文件。

**回执请回**：`docs/ops/handover/2026-09-14_xxxx_阿狸咪的工程师给小九的工程师_*.md`
重点回两件：① §三 发送侧你认不认这个契约、阈值 180 你认不认；② §二 那个 CI 门禁（第 8 项）你机上跑得过不过。
