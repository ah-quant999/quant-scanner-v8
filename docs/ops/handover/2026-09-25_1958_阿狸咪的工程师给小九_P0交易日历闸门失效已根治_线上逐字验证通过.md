# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-25 19:58｜P0交易日历闸门失效已根治_线上逐字验证通过

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

`trading-day-gate-predeps-fallback`（P0）**已根治并线上逐字验证**：5 笔提交；休市日实测
`📅 数据日期=2026-09-24` / `is_trading_day=false` / `proceed=false` / 「📡 抓中国数据」= **skipped**。

---

## 一、根因（双因 —— 此前只登记了 ①）

① **步骤序缺陷**（原登记）：`v8_cn_fetch_cloud.yml` 的「🗓 解析数据日期」「🛡 交易日历闸门」
排在 `actions/setup-python` + 「📦 安装依赖」**之前** ⇒ `fetch_lhb` 顶层 `import akshare` 必失败
⇒ `v8_date` 静默兜底 `weekday()<5` ⇒ 休市日（2026-09-25 中秋）被判为交易日。

② **兜底源不可达**（本轮新查明，此前未登记）：即便把「静态权威日历」当兜底，该表当时
**只存在于 `guard_v8_freshness.py`**，而该模块顶部 `from update_v8 import ...` ⇒ 在
**不装第三方库的轻量 job**（`v8_build_deploy` 的 gate job）与**依赖未装的早期步骤**里
**根本 import 不进来** ⇒ 兜底等于没有。
⇒ 这解释了为何 `v8_build_deploy` 的 gate 在休市日恒判 `is_trading_day=true`（门禁 + 部署双缺位）。

## 二、修复（5 笔提交；全部「远端为基底 + 漂移断言 + 推后逐字节回读」）

| # | 提交 | 文件 | 内容 |
|---|---|---|---|
| 1 | `52b01b8b7b6a` | `.github/workflows/v8_cn_fetch_cloud.yml` | **fix(A)**：`setup-python` + 「安装依赖」整块上移到「解析数据日期」之前（步骤数 28 不变、step id 全不变） |
| 2 | `1f496c492974` | `v8_calendar.py`（**新增**） | 零依赖静态权威日历（`HOLIDAY_RANGES` / `in_holiday_range` / `covers`） |
| 3 | `24eac5f82ac1` | `v8_date.py` | **fix(B)**：兜底由 `weekday()<5` 改为回落 `v8_calendar`，并打印显式告警 |
| 4 | `85f109d50ef4` | `algorithms/fetch_lhb.py` | **fix(B)**：二阶兜底 `return True` → 回落 `v8_calendar`（命中法定休市 ⇒ False） |
| 5 | `341d6c055f96` | `.github/workflows/v8_build_deploy.yml` | gate 的 `sparse-checkout` 补 `v8_calendar.py`（否则 gate 仍读不到新模块，兜底照样失效） |

## 三、线上逐字证据（run `#2288` / id `36129677046` / head `52b01b8b7b6a`，job `fetch` = success）

步骤序已生效（日志早期即出现 `Download action repository 'actions/setup-python@v5'`）：

- `11:30:08.866Z 📅 数据日期=2026-09-24` ← **已回退到上一交易日**（修复前为 2026-09-25）
- `11:30:08.877Z V8_DATA_DATE: 2026-09-24`（其后全部 step 同值，不再「假今日」）
- `11:30:09.877Z is_trading_day=false`
- `11:30:09.877Z proceed=false (工作日遇节假日，跳过冗余抓取)`
- 「📡 抓中国数据 → raw_data/（selective）」= **skipped**

⇒ 本项 `verify_how` ①②③ 全部达成；④「休市日档数降至个位」自修复起不再新增空转档。

## 四、本机对照验证（仓外最小树，零第三方库，对照「旧版 vs 新版」）

- `v8_date.is_trading_day`：09-25 / 10-01 / 10-07 → **False**（修复前均 True）；09-28 / 10-08 / 2027-03-01 → True。**12/12 断言通过**
- `today_data_date()`：2026-09-25 → **2026-09-24**
- `fetch_lhb.is_trading_day`（mock `_get_trade_cal → None`）：09-25 / 10-01 → **False**，并打印显式告警

## 五、待验 / 残留（请小九复核）

1. **假今日残留未手工回改**：`data/*.js` 中 61 个含 `2026-09-25`（登记值）。本次**不手工回改**
   （避免「造数据」）；预期 **09-28（周一）post_close** 自然覆盖 ⇒ 请在该日复核 `date`/`enter_date` 是否归零。
2. **`build_deploy` gate 线上验证待自然触发**：`sparse` 补丁（`341d6c055f96`）之后尚无独立 run；
   下一次 push 触发的 `gate` job 应输出 `📅 2026-09-25 is_trading_day=false`、`trading=false`、`build` 执行。
   （逻辑已在本机最小树验证；线上属「待自然触发」，**未宣称已完成**。）
3. **`guard_v8_freshness.py` 内同名常量未合流**：其 `_HOLIDAY_RANGES_2026` / `_MAKEUP_DAYS_2026`
   与 `v8_calendar.py` 值**逐字同源**（已比对），但仍属副本 ⇒ 建议后续统一为 `from v8_calendar import ...`。
   ⚠️ **`_MAKEUP_DAYS_2026` 含 `2026-09-20` / `2026-10-10`** —— A 股周末一律不开市，交易日判定
   **不得**使用该表；`v8_calendar` 已刻意只用节假日区间（`in_holiday_range`）。
4. **其余同型点未动**：`v8_algo_cloud.yml`、`v8_stock_quote_refresh*.yml` 亦存在「日历调用在
   `setup-python` 之前」；因跑在 cn runner（持久环境已装 akshare）或 pip 在前，风险低，留待统一排期。

## 六、关联

- HANDOFF item：`trading-day-gate-predeps-fallback`（`todo` → `pending-verify`）
- 休市窗口：2026-09-25 ~ 09-27（上交所〔2026〕22号），09-28（周一）开市
- 执行时段：阿狸咪夜间窗口（18:00–次日 07:30）；本轮未派发任何冗余 workflow
