# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 📌 **12:05 自动更新**（automation-1785719698947 · 12:00 档）
> 18:00 档将整份覆盖重写，开头注明更新时间。
> 本文所有 commit SHA 均由 `git log origin/main` 实时核出，非记忆值。

---

## 〇、环境事实（本文所有路径以此为据，已实测 2026-09-02 12:05）

| 项 | 值 | 备注 |
|---|---|---|
| **真身仓库** | `E:/v8data/qs8-tmp` | 唯一有 `.git` 的工作副本 |
| 配置里的 cwd | `E:/workspace/quant-scanner-v8` | ⚠️ 坚果云软链**空壳、无 `.git`**，只能写文件，不能跑 git |
| 禁用副本 | `C:/Users/Administrator/qs8-tmp` | junction，08-31 10:39 曾被重建清洗，**不要用** |
| origin | `git@github.com:ah-quant999/quant-scanner-v8.git`（SSH） | 已绕开凭据助手挂死问题 |
| 主站 | https://ah-quant999.github.io/quant-scanner-v8/ | 从 `main` 分支发布，无 gh-pages 分支 |
| 推送手法 | **隔离 index 直推**（见 §4 P0-2） | 工作树脏，禁止 `git add -A` / `reset --hard` |

---

## 一、背景

今日（09-02，周三，交易日）是小九白班（07:45–17:45）第 5 天。全天主线：

1. **凌晨由阿狸咪完成一轮「涨停热力矩阵 10→30 日 + 调试专区卡片 + 盘后选股数据」改造**；
2. **小九白班接力，主线是「?v 缓存戳失配」与「卡片空卡/占位不渲染」两条顽疾的一劳永逸根治**；
3. 四次数据兜底守卫运行，**全部判定为假阳性**（本地副本旧、远端已新鲜）→ **零派发、零抓取、零推送**，只做「零索引变动同步」。

一句话结论：**站点全绿（112/112 通过、0 fail），runner 17/17 ok，无未处置事故。唯一待办是本机工作树 201 项分叉与三项需主人授权的红线操作。**

---

## 二、今日已完成改动（含真实 commit SHA + 影响）

### 2.1 阿狸咪窗口（00:00–07:30）已推送 origin/main

| SHA | 本地时间 | 改动 | 影响 |
|---|---|---|---|
| `f264bc818` | 04:00 | `fix(v8)`: 算法链加应急绕过开关 `bypass_time_gate` | 解开「选股脚本 ≥18:00 时间门控」导致的白天零产出 |
| `bd38dc891` | 04:11 | `fix(v8)`: 加回调试专区 3 卡（宏观 + 板块推荐 + 曲线新卡） | 恢复被误删卡片 |
| `ff96cf9d7` | 04:22 | `fix(v8)`: 涨停热力矩阵 10 日 → **30 日** | raw_data + data js + `?v` 全链路对齐，脚本锁定 30 日 |
| `ae2f5928f` | 04:42 | `data`: 自动提交 60m 共振数据 `FOUR_VOLUME_60M.js` | 四量终极 60 分钟口径补齐 |
| `a3e03fe70` | 05:25 | `data`: 盘后全量选股数据（**43 ok / 3 非核心未产出**），运维页转绿 | 选股四模块数据落地 |
| `0cec5bbbe` | 07:07 | `fix`: 涨停热度补 `top/total/ladder`（raw_data + JS + 生成器）+ 驾驶舱分档 A 股覆盖合并 `tier_b` | 涨停热度黄灯修复 |
| `c3c7501f2` | 07:17 | `fix(v8)`: 环境态势占位自愈 + **删除 3⭐大牛股猎手 X** + 修复涨停热度黄灯 | 占位自愈 + 精简 |

### 2.2 小九白班（07:45 起）已推送 origin/main

| SHA | 时间 | 改动 | 影响 |
|---|---|---|---|
| `8ed41b468` | 08:28 | `fix`: 涨停热力窗口自适应（只显有数据尾部列）+ 标题动态「近 N 日」 | 根治「前面空那么多」排版 |
| `7d87408ff` | 08:32 | `fix(v8)`: 今日判定 self-heal 增强（数据就绪即补渲染）+ `JUDGMENT_DATA ?v` 对齐真实哈希 | 今日判定不再空卡 |
| `b4a481e9a` | 08:40 | `fix`: align `JUDGMENT_DATA ?v` to `75655247d2` | 缓存戳失配补丁 |
| `ce32ce8c6` | 08:52 | `fix(暂未上架)`: 剥离股票目录与实验卡容器冲突，模块索引独立 + 股票目录归位已下架 | 容器冲突根治 |
| `14042e671` | 09:18 | `fix(暂未上架)`: 恢复调试专区原 3 卡（AI预测/潜力挖掘/ETF龙头参考）平铺，移除宏观/板块推荐/热门赛道死卡 | 卡片平铺重构 |
| `b9ccff391` | **09:22**（作者 `小九`） | `fix(dividend)`: restore `plan_date` + full field set in `refresh_dividend_cninfo.py` | 分红字段回归 |
| `cb924fe42` | 09:30 | `fix(v8)`: **reconcile 强制同步远端最新 main 后再算 `?v`** | 根治 checkout 缓存取到陈旧 data 导致误判已对齐 |
| `31918e2c8` | 09:37 | `fix(暂未上架)`: 新增热门赛道资金追踪曲线卡（第 4 张）+ 潜力挖掘卡防御 `try/catch` | 新卡 + 防崩 |
| `062813f67` | 09:37 | `fix(ui)`: 修复判定卡 `_esc` 未定义渲染失败（线上旧版作用域 bug）+ 同步今早暂未上架重构 | 判定卡不再整块挂掉 |
| `f0a094e34` | 10:11 | `fix(v8)`: `HUNTER_BACKTEST` 登记/调度/时间戳修复，补推 09:32 回测数据 | 大牛股猎手回测归位 |
| `3a8e07844` | 10:26 | `fix(push)`: blob 上传 **3→8 次重试** + 智能退避 + 失败 `::error` 告警 | 涨停热力静默丢数据根治 |
| `b26092804` | 10:34 | `fix(watchdog)`: **多文件抽查替代单一 HEALTH_CHECK 判据** + 派发防重 + `::error` 告警 | 涨停热力静默陈旧根治 |
| `1fbfe294f` | 10:35 | `fix(ui)`: 行业树图卡片尺寸与概念资金热力图对齐 | 版面对齐 |
| `64de19a97` | 11:07 | `fix(limit-up-heatmap)`: `generate()/main()` 加「前段连续 ≥5 列全 0」自动触发全量重建 | 根治 dates 齐但 data 缺 |
| `4c382717f` | 11:18 | `fix(limit-up-heatmap)`: `build_heatmap` 末尾剔除整列全 0 日期 + 完整性检查允许 ≤30 列 | 东方财富只保留近 30 日，容忍度对齐 |
| `ef121f8b7` | 11:27 | `fix(ui)`: 选股策略/暂未上架**全子 tab 自愈**，根治 defer 未注入导致的空卡 | 切 tab 空卡根治 |
| `0b7c7d625` / `de38e061c` | 11:52 | `fix(v8)`: 外部风险合并进今日判定（删孤儿重复卡）+ **主力净额分时曲线前段空白根治**（放宽选板块门槛 + 盘中口径守卫 + 自愈清理） | 今日主线最后一块拼图 |

> 另有云端自动提交若干：`v8 cn fetch` × 多轮、`v8 build` × 多轮、`?v` 缓存戳对齐、runner 状态上报。
> **今日 origin/main 累计 169 个 commit。**

### 2.3 小九自动化运行记录（4 次，全部零改动收尾）

| 时间 | 自动化 | 结论 |
|---|---|---|
| 08:17 | `1785898357370` 盘前数据兜底守卫 | 哨兵报「崩塌 1658→909」诊断为**缓存清理非事故**；**纠正既有指引**：须用 `guard_remote_integrity.py --reset-baseline` 重建基线，**不能用 `--fix`**（会走 `find_good_base` 把 749 个已清理缓存重新加回）。`STALE:5` 证伪为假阳性 → **零派发**。终态 staged 0 / UU 0 / 无新提交 |
| 08:26 | `1785888730897` 盘前云端全量守卫 | 云端 08:02 / 08:06 两 run 均 success，`cat="premarket"` 已执行 → 不派发。线上 HEALTH_CHECK 112 项仅 4 fail 且均为盘前已知项 |
| 10:37 | `1785898357370` 盘中智能触发守卫 | intraday `STALE:9` 假阳性（本地 09-01 14:41 / 远端 09-02 10:27~10:33）。`git show origin/main:raw_data/<f> > raw_data/<f>` 同步 9 文件 → 收尾 **FRESH** |
| 11:44 | `1785898357370` 午休智能触发守卫 | lunch `STALE:9` 再次证伪（远端 11:19~11:25 已新鲜）。**新增固定动作**：同步前逐个比对 `git hash-object` 是否命中历史 blob → **9/9 命中** `024a8c076`，证明覆盖安全。收尾 lunch/all 均 **FRESH**，staged 0 / UU 0 |

**可复用技法（本日新增，建议阿狸咪沿用）**：
- 零索引变动同步：`git show origin/main:raw_data/<f> > raw_data/<f>`（只改工作树，不动索引/HEAD）
- 同步前安全校验：`git hash-object <f>` 比对 `git log -8 origin/main -- <f>` 的历史 blob，命中才覆盖
- 判 dispatch 的 category：runs API **不返回 inputs**，须查 job 日志 grep `cat=` / `IN=`

---

## 三、当前系统状态（截至 2026-09-02 12:05 实核）

### 3.1 站点与数据 ✅ 全绿

| 指标 | 值 |
|---|---|
| 主站 HTTP | **200** |
| `HEALTH_CHECK` | gen **2026-09-02 12:01:14**，overall **ok**，**112 ok / 0 warn / 0 fail / total 112** |
| `RUNNER_STATUS` | run_time **2026-09-02 11:58:52**，category `intraday`，**17/17 模块 ok** |
| 大盘快照（线上 `INDEX_QUOTES.js`） | 上证 **3947.26 / −0.82%**（−32.63），涨 493 / 跌 1834 / 平 28，成交额 5654.11 亿 |
| 平均股价 | date=2026-09-02，均价 **28.1872（−0.90%）** |
| 最近一次 build | `3868185b6`（v8 build: 2026-09-02 12:01），pages 上线正常 |

### 3.2 origin/main

```
HEAD（origin/main） = 3868185b6   v8 build: 2026-09-02 12:01
今日 commit 总数    = 169
```

### 3.3 本机真身工作树（E:/v8data/qs8-tmp）⚠️ 已知分叉，未处置

| 项 | 值 |
|---|---|
| 本地 HEAD | `720171f13` |
| 相对 origin/main | **ahead 0 / behind 352**（纯落后，无本地待推提交） |
| staged（陈旧暂存区） | **0** ✅（地雷已清空） |
| UU 冲突 | **0** ✅ |
| 工作树改动 | **201** 项（`M` 150 / `??` 31 / `D` 20） |
| 仅本地有的文件（UNIQUE） | **1**：`scripts/guard_raw_antiregress.py` |

> 相比 09-01 的「157 项残留 + 14 UNIQUE」，本日 UNIQUE 从 14 降到 **1**，staged 从危险值降到 **0**，`reset --mixed` 地雷已停止。剩余 201 项仍属**本地旧版本分叉**，清理需主人授权（见 §4 P0-2）。

### 3.4 剩余自动化（小九侧，共 **25 个：22 ACTIVE + 3 PAUSED**）

**PAUSED（3）**— 恢复须主人明令授权：
- `automation-1788138940079` v8 本地镜像同步（index.html + data/*.js → E: 空壳）
- `automation-1786933089412` TDX 强势选股每日跟踪（三件套 v2）
- `automation-1785802406605` v8 本地看板同步（每小时 pull）

**ACTIVE 关键档位（22）**：

| 时间 | 任务 | ID |
|---|---|---|
| 每 20 分 | runner 本地守护（错误即自愈） | `1786696074524` |
| 每 10 分 | 自愈监控闭环（candidate 观澜台 + momentum） | `1786961773708` |
| 每 20 分（7–23 点） | 全定时任务更新监控（只读·异常告警） | `023a3030` |
| 08:15 | 盘前/每日全量云端守卫 | `1785888730897` |
| 8–16 点每 20/30/40 分 | 全时段数据兜底云端守卫（cloud first） | `1785898357370` |
| 9–15 点每 10/40 分 | 盘中漏轮云端守卫（监督·禁止本地直推 main） | `1787108824799` |
| 9/10/11/13/14/15 点整半 | westock 持仓云端同步 | `1787210164001` |
| 每小时（7–21 点） | 紧急交接通道（看门狗 + 自动修复） | `1785835481750` |
| **12 / 18 点** | **本任务：当日交接文档更新** | `1785719698947` |
| 15:30 | 收盘数据刷新（API 派发 post_close） | `1785388522458` |
| 16:30 | 港股收盘后 post_close 刷新部署 | `1788145998708` |
| 16:45 | 选股四模块云端守卫 | `1785404904506` |
| 19:00 | 每日审计 + 告警（禁止推仓） | `1785978303212` |
| 19:25 | 盘后算法链云端守卫 | `1788168934360` |
| 19:30 | James-Stein 行业级收缩回测 | `1787575410637` |
| 19:10 / 08:10 | 分红方案刷新（云端主跑·本机应急兜底） | `1786588094908` / `1786588096327` |
| 21:40 / 22:40 | 龙虎榜/共振日历云端守卫 | `1788172770889` |
| 21:45 | 盘后链每晚兜底 dispatch | `98e60e82` |
| 周末 15:30 | T+1 云端派发（禁止本机部署） | `1785396622150` |

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 P0（须主人授权，或夜间切勿误触）

**P0-1｜三处 `reset` 修复仍只在本机，未进 origin/main**
- 今日实测：`git grep "reset --mixed\|reset --hard" origin/main -- '*.py'` → **仅剩 2 处注释**（`scripts/reconcile_cache_busters.py:89`、`v8_rollback_guard.py:13`），**无活跃 `reset --mixed` 调用**。
- 但三个修复文件本身**都不在远端**：`_push_gate_20260901.py`、`_isolated_push_20260901.py`、`scripts/guard_raw_antiregress.py` → `git cat-file -e origin/main:<f>` 全部「远端无此文件」。
- 本机 `scripts/guard_raw_antiregress.py:155` 已是修复版（`git(["reset","-q","HEAD","--"] + regressions)`，不再是 `reset --mixed origin/main`），且它是当前**唯一的 UNIQUE 文件**。
- ⚠️ **阿狸咪夜间若做批量提交，务必只显式列路径，勿 `git add -A`** —— 否则会把本机 201 项旧版本分叉一起提交，造成整站回滚。

**P0-2｜201 项本地分叉待授权清理**
- 清理命令（红线，须授权）：
  ```
  cd /e/v8data/qs8-tmp && git fetch origin && git reset --hard origin/main
  ```
- **风险**：会抹掉唯一的 UNIQUE 文件 `scripts/guard_raw_antiregress.py`（P0-1 的修复载体）。**清理前请先 `cp scripts/guard_raw_antiregress.py /tmp/` 备份。**
- 推送手法（推荐，完全不碰工作树/HEAD）：
  ```
  BLOB=$(git hash-object -w <源文件>)
  GIT_INDEX_FILE=<temp> git read-tree origin/main
  GIT_INDEX_FILE=<temp> git update-index --add --cacheinfo 100644,$BLOB,<文件名>
  TREE=$(GIT_INDEX_FILE=<temp> git write-tree)
  COMMIT=$(GIT_INDEX_FILE=<temp> git commit-tree $TREE -p origin/main -m "<msg>")
  git push origin $COMMIT:main      # CI 每 2 分钟推进远端，失败则 fetch 后重建父提交重试
  ```
  推前必用 `git diff-tree --name-only -r $COMMIT origin/main` 校验改动文件数（应 = 1）。

**P0-3｜westock 连接器档位错配 → 持仓卡停在 08-31**
- 需主人在连接器管理页切到**企业档 `c07631a5`**（该档已是 `bound:true, enabled:true`），或把 `default` 档的 `connector:westock-mcp` 打开并重启会话。
- **AI 不要直接改这两个配置文件**（属授权动作）。未修复 → `1787210164001` 永远拿不到数据。

**P0-4｜多个自动化 cwd 仍指向 E: 空壳**（第 5+ 次复现）
- 建议统一改真身 **`E:/v8data/qs8-tmp`**（**不要用 `C:/Users/Administrator/qs8-tmp`**）。
- 至少 `1785719698947`（本档）、`1787210164001`、`1785835481750` 需改。

**P0-5｜`cloud_fetch_v8.py` 的 AVG_PRICE 改造滞留本机，仍未推送**
- 属**删功能 + 改云端主抓取链路**，按铁律需主人先授权。备份在 `_backup_20260901_未推送独有改动/`（13 文件 1.4M）。
- **阿狸咪不要擅自推**，也不要混进任何批量提交。若夜间要推，须先确认 `update_v8.py` 的 post_close/intraday 口径一并对齐。

### 🟡 P1

| ID | 问题 | 状态 |
|---|---|---|
| P1-0 | UNIQUE 从 14 降至 **1**，「全量对齐」仍不可用；`git add -A` / `reset --hard` / `rebase` 一律禁用 | 持续 |
| P1-1 | `avg_price_data.json` 守卫误报：AVG_PRICE 已改由 `scripts/fetch_avg_price.py` 在 post_close/weekend 跑，但 `freshness_guard` 的 intraday 清单仍在监控 → 每次盘中假报 STALE | **未修** |
| P1-2 | `_load_token()` 候选表不含 `qs8-tmp` 目录（`v8_health_check.py` 74–75 行）→ token 反复「丢失」。**今日实测远端与本地均无 `qs8-tmp` 字样** | **未修** |
| P1-3 | 并发写同一仓库：本机多自动化并行操作同一工作树。改动须尽快推远端，**推送前重新 `hash-object`** | 持续 |
| P1-4 | `ETF_SUBSCRIPTION_EM` 占位数据：`fetch_etf_subscription_em.py` 调用的 `ak.fund_etf_fund_info_em()` 返回列与期望不符，5 类净申购恒为 0 | 未修，需换接口 |
| P1-5 | `algorithms/run_algorithms.py` 有 **≥18:00 时间门控**；云端算法链曾连续 2 次 **150min timeout** → `STOCK_STOP_DATA` / `LHB_7D` 长期陈旧。今晨 05:25 已补推 43 ok / 3 非核心未产出，**瓶颈仍在** | 待主人决断：提 timeout 或拆链 |
| P1-6 | 仓库缓存膨胀隐患：`.gitignore` 已忽略 `raw_data/_rps_cache` 等目录，但**对已跟踪文件无效**，云端每轮 `git add raw_data/` 会持续滚进缓存。修法须 `git rm -r --cached`（红线，须授权） | 未处置 |

### 🟢 已闭环（今日）

- ✅ 涨停热力矩阵「前面空那么多」——三连修复（`8ed41b468` 窗口自适应 + `64de19a97` 全 0 段自动重建 + `4c382717f` 剔除全 0 列 + ≤30 列容忍）
- ✅ `?v` 缓存戳失配——三保险（生成器原子提交 + build 提交前守卫 + reconcile 重算 `cb924fe42`）
- ✅ 卡片空卡/占位不渲染——`ef121f8b7` 全子 tab 自愈 + `e048d698f` 环境综合永久轮询 + `062813f67` `_esc` 未定义修复
- ✅ 陈旧暂存区地雷——staged 归 0，UNIQUE 14 → 1
- ✅ 四次守卫假阳性全部证伪，零误派发、零误推送

---

## 五、小九监督职责（截至本档）

### 5.1 铁律（不得违反）

1. **不本地 deploy**：本站部署一律经 GitHub Actions；本机只做「监督 + 应急兜底派发」。
2. **不 `git add -A` / `reset --hard` / `rebase`**：P0-1/P0-2 未授权前，任何批量 git 操作都可能整站回滚。
3. **云端已新鲜即不重复派发**：判定顺序 = 远端内部时间戳 → workflow runs（有无 in_progress/queued）→ 才考虑 dispatch。
4. **改完即推送**：本机脚本改动若不及推远端，会被其他自动化进程改写（P1-3）。
5. **全站中国时间**：任何写 `update_time` / `gen_time` 的脚本须用 `ZoneInfo('Asia/Shanghai')`。
6. **红涨绿跌**：暗色主题红 `#ef5350` / 绿 `#26a69a`，胜率金色。

### 5.2 今日剩余档位（小九白班 07:45–17:45）

| 时间 | 事项 |
|---|---|
| 13:00–15:00 | 盘中漏轮守卫 `1787108824799`（每 10/40 分）+ 全时段兜底 `1785898357370` |
| 15:30 | 收盘数据刷新 `1785388522458`（API 派发 post_close） |
| 16:30 | 港股收盘后 post_close `1788145998708` |
| 16:45 | 选股四模块云端守卫 `1785404904506` |
| **18:00** | **本任务第二次运行（覆盖重写本文）** |

### 5.3 交给阿狸咪的夜间（18:00 后）重点

1. **19:00 审计** `1785978303212` 会出当日结论，请先看它再动手。
2. **19:15 / 19:25 盘后算法链**是最大风险点（P1-5 的 150min timeout）。若 `STOCK_STOP_DATA` / `LHB_7D` 又没产出，**不要本地补跑**，先记录、等主人决断（提 timeout / 拆链 / 用 `bypass_time_gate`）。
3. **不要动本机工作树的 201 项分叉**；如确需提交，走 §4 P0-2 的隔离 index 直推。
4. **westock 持仓卡**今日大概率仍停在 08-31（P0-3 未解），属已知状态，不必重复排查。

### 5.4 需主人拍板的三件事（沿用 09-01，仍未答复）

1. 是否授权 `reset --hard origin/main` 清理 201 项残留（P0-2）？
2. 是否让三处 `reset` 修复上线（P0-1）？
3. 是否统一把所有自动化 cwd 改到 `E:/v8data/qs8-tmp`（P0-4）？

---

*文档由 automation-1785719698947 于 2026-09-02 12:05（CST）自动生成。*
*下一档 18:00 覆盖重写。*
