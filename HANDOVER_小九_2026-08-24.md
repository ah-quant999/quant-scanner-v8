# HANDOVER 小九 → 阿狸咪（2026-08-24 周一 · 12:00 窗口）

> 更新于 2026-08-24 12:03 自动更新

## 一、背景

- **值守安排**：主人 8/21–8/27 出差中；小九白天独家值守（7:45–17:45），阿狸咪夜间/周末接管。本档为小九 12:00 交接，确保阿狸咪开机即知当日状态。
- **交易日**：今日周一交易日，盘前全量已跑，盘中监测自 09:40 起生效（09:10 前盘前空转）。
- **今日核心事件**：**P0 数据回滚地雷根治上线**（11:24 第362棒），详见第二节。

## 二、今日已完成改动（含真实 commit SHA + 影响）

以下 SHA 均经 `git merge-base --is-ancestor <sha> origin/main` 实测核实，**全部在 origin/main 上**，非记忆旧值。

| 真实 SHA | commit 主题 | 影响 / 说明 |
|---|---|---|
| `a6864b71f` | fix(git-safety): sync_westock_portfolio 收尾不再 reset --soft origin/main | **P0 根治**：杜绝主 index 停留旧树导致 100+ 文件数据回滚地雷（原会令 18:30 盘后链推出整站数据回滚 + 删 8 个 raw_data 文件）。已隔离索引单文件推送 `54fb6141b..a6864b71f` 上线并远端复核 |
| `54fb6141b` | v8 build: 2026-08-24 11:30 | 锚点 build |
| `85b17c1ee` | v8 risk gauge: 2026-08-24 11:28 | 风险仪表刷新 |
| `012496dd1` / `bf8f422c8` | chore(stock-quote): refresh STOCK_QUOTE via v8_stock_quote_refresh | STOCK_QUOTE 双次刷新 |
| `03459f028` | v8 cn fetch: 2026-08-24 11:33 | 盘中 cn 抓取 |
| `28c9b9372` / `464c6aa48` / `5c4e55abb` | v8 cn fetch: 2026-08-24 11:50 ×3 | 盘中 cn 抓取三连 |
| `ee03fa9de` | chore: 盘中算法追踪轻量刷新 11:53 | 算法追踪轻量刷新 |
| `68dc6b2ed` | chore: 缓存戳实时对齐 ?v → 防覆盖 (11:54) | 缓存戳防覆盖（一劳永逸根因，主人 08-14 转圈痛点） |
| `235dbe89f` | data: westock 持仓同步 (2026-08-24 12:02) | 持仓同步 |
| `81ba301f2` | v8 build: 2026-08-24 12:04 | **当前 HEAD / main 顶端** |

**运行侧（非 commit）关键改善**：
- **02:10 runner 本地守护第77轮 OK**（exit=0，零自愈）：Runner.Listener PID 1188 连续存活 **64.71h**；GitHub API 403 二级限流 + 云端每分钟派发风暴**彻底消退**（最近 40 runs 39 success/1 queued、零 failure、workflow_dispatch 仅 1）；**stash 堆积 27→0 已清空**，孤儿 UU 复发源已断（UU 长期 0）。

## 三、当前系统状态

- **站点**：GitHub Pages HTTP 200（看门狗 11:24 六路全 OK：cn_fetch 11:01 / build_deploy 11:13 / raw_data 11:18 / site 200 / auto_dispatch post_close / 0 封告警）
- **runner**：本地进程连续存活 64.71h；云端 Worker 正常；guard 第77轮 exit=0 OK
- **main SHA**：`81ba301f2`（12:04 build）
- **本地对齐**：HEAD == origin/main，**零分叉零落后**；工作树仅 untracked（backup/ / 日志 / 诊断 HTML），**无红线在途 M 改动**（index.html / HEALTH_CHECK.js / PORTFOLIO.js / BLOAT_CHECK.js 均干净）
- **剩余自动化**：16 个（**15 ACTIVE + 1 PAUSED**：TDX三件套 `1786933089412`）
- **看门狗健康**：ok75 / warn4 / fail27（fail 全 stale，周末→周一间隙预期，18:30 盘后链补齐后归零）

## 四、需阿狸咪注意 / 待办 / 风险

1. **【待授权·纵深防御】P0 地雷根治后两件事**：
   - ① 4 处裸 `git commit -m`（无 pathspec：run_dividend_refresh:49 / v8_closing_data_refresh:71 / self_heal_monitor:246 / algorithms/strategy_four_volume_60m:404）补 pathspec，防未来再连带暂存区。
   - ② 8 个磁盘已无但 origin/main 仍在的 raw_data（h_auto_buy_history 等 H 链路产物）是否历史授权清理。
2. **【不可逆·告知】`data/zsxq_token.json` 丢失**（00:00 自愈监控发现）：该文件被 `.gitignore:21` 忽略从未进 git → **git 无法还原**，全仓 find / backup/ 均无副本。本地自愈拉取候观澜台失效；**云端管线不受影响**（走 `ZSXQ_TOKEN` 环境变量，GitHub Secret 注入）。恢复需 `scripts/setup_credentials.py` 交互粘贴 token；可选加固（check_zsxq_token 加环境变量兜底）待放行。
3. **【观察】盘中漏轮兜底守护 BYHOUR 过滤未生效**：00:10~05:10 连续 6 轮无效唤醒（<09:30 窗口外空转但浪费调度），待裁决是否调整过滤逻辑。
4. **【预期·非故障】fail27 全 stale**：周末→周一间隙产物，18:30 盘后链补齐后归零，勿当故障修。
5. **【红线铁律】** 当前无在途 M；若后续 `git status` 出现大批量 `M `/`MM`/`D ` **首列**标记，先 `git reflog | grep 'reset: moving'` 判是否 soft reset 留下的陈旧 index 地雷，**勿当「待云端代发副产物」放过**（今日 11:24 已立此排查铁律）。
6. **【出差纪律】** 主人出差至 8/27，盯紧别乱改、本地云端对齐（沿用 8/20 铁律）。

## 五、小九监督职责

- 白天独家值守 7:45–17:45；盘中监测自 09:40 起有效；每小时交接棒（第362棒）持续。
- 已沉淀排查铁律：git status 大批首列 `M`/`MM`/`D` → 先判 soft reset 陈旧 index 地雷，再决定是否提交。
- 下一档 **18:00 交接**会汇总 18:30 盘后算法链跑批结果（候选池/四量/CRDS/H 链路等），届时刷新 main SHA 与系统状态。
- 若推送被云端 build 抢先：本地零分叉场景下 `git pull --rebase --autostash` 干净重放即可；若工作树有红线在途则改 plumbing 法（read-tree + 临时索引 + commit-tree -p origin/main），绝不碰工作区。
