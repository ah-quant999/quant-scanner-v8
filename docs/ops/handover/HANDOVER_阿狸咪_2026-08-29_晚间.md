# 交接：阿狸咪 → 小九（2026-08-29 晚间）

> 生成时间：2026-08-29 22:xx（阿狸咪夜班收尾）
> 覆盖：本次「Bug A 修复 + 防删子页面 + Tier 1/2 升级（IC门禁/regime门控/freshness SLA）+ 两个新 fetcher + DO_NOT_DELETE.md 推仓入库」全部工作
> 当前 main HEAD：`9da077a20`（已推 origin/main ✅）
> 关联：本次两座 commit `6f42d4445` + `9da077a20`

---

## 一、入库与防覆盖确认（🔴 重点，Nutstore 实时同步铁律）

- ✅ 两座 commit 已推 `origin/main`：`git diff origin/main --stat` 为空。
- ✅ 本次新增/改动文件全部入库：`index.html`（Bug A + 防删子页）、`scripts/*.py` ×6、`DO_NOT_DELETE.md`、`v8_cleanup.yml`、`data/DO_NOT_DELETE.js`、`raw_data/*.json` ×3（见 §2）。
- ⚠️ **Nutstore（坚果云）实时同步仍在监听本仓**：对本仓任何 `Edit`/`Write` 改动会在 2~10 秒内被云端旧版本回退。本次已用「原子 python 改写 + 改完即 commit + 立即 push」绕过，**工作树脏改动已无残留**。
- ⚠️ **小九本地若开 Nutstore 同步本仓目录，改完任何文件必须 `git commit` + `git push` 立即固化**，否则会被云端回退吞掉。push 前 `git fetch && git rebase origin/main`（pre-push 钩子已自动 rebase，但本地工作树干净是前提）。
- ⚠️ **两个 stash 待处置**（见 §6）：`stash@{0}` = nutstore_pollution_pre_push_1236、`stash@{1}` = WIP_Nutstore_pollution_reserve_2026-08-29，均为 Nutstore 污染前的本地缓存，**不要直接 pop 到已对齐的干净基**（会重新引入污染），按 §6 处置。

---

## 二、本次两座 commit 改了什么

### Commit `6f42d4445` — fix(v8): 修复已下架3卡卡壳 + 新增「逻辑详解·防删」子页

| 改动 | 位置 | 内容 |
|---|---|---|
| 🔧 Bug A 修复 | `index.html` ~9028-9030 | 补挂 `window.renderThermo/renderSixDim/renderCrisis`；根因：`renderDelisted()` 用 `if(window.renderThermo)` 调用，但三函数从未挂 window → 已下架 3 卡（热力/六维/危机）卡「加载中…」空白 |
| 🛡️ 防删子页面 | `index.html` 4 处 | ① line 467 `<script src="data/DO_NOT_DELETE.js?v=...">`；② line 4717 tab `🛡️ 防删`；③ line 5881 `<div class="lg-pane" id="lg-dnd">`；④ line 9034 `renderLgDnd()` 读 `window.DO_NOT_DELETE_HTML` 注入 |
| 📄 DO_NOT_DELETE.js | `data/DO_NOT_DELETE.js` | 由 `scripts/gen_do_not_delete_js.py` 从 `DO_NOT_DELETE.md` 渲染，注入 `window.DO_NOT_DELETE_HTML` + `window.DO_NOT_DELETE_UPDATED`（修复了 cache 戳失配 bug：写文件后重算 disk_sha 回写 index.html `?v`，现 sha10=c71f8b8739）|

### Commit `9da077a20` — feat(v8): Tier 1/2 升级 — IC门禁 + regime门控 + freshness SLA + 平均股价/ETF申购 fetcher + DO_NOT_DELETE.md 推仓入库

| 模块 | 文件 | 内容 |
|---|---|---|
| 🎯 IC 门禁 MVP | `scripts/apply_ic_gate.py` → `raw_data/ic_gate.json` | 读 `factor_ic_report.json`(371样/T1 win49.1%) + `factor_validate_report.json`(fixed T1 win49.7%) → 写门禁。**只产信号，不改选股逻辑** |
| 🎚️ regime 门控 | `scripts/apply_regime_gate.py` → `raw_data/strategy_regime_gate.json` | 读 `market_regime.json` regime.label + framework_match → 各策略 weight 乘子 |
| ⏱️ freshness SLA | `scripts/freshness_sla.py` → `raw_data/freshness_sla.json` | 扫 `raw_data/*.json` update_time 对比 SLA 表 → 标红+告警，**实测揪出 7 处隐疾**（闭环「图14卡8-04无人告警」根因）|
| 📈 平均股价 fetcher | `scripts/fetch_avg_price.py` → `raw_data/avg_price.json` + `data/AVG_PRICE.js` | 通达信 880003，东方财富 push2his secid=88.0003。**UI 暂未接，待下一步** |
| 💰 ETF 申购赎回（东财口径）| `scripts/fetch_etf_subscription_em.py` → `raw_data/etf_subscription_em.json` | akshare `fund_etf_fund_info_em`，按 5 类（股票/债券/货币/商品/跨境）聚合成亿元，**替换旧 `ETF_SUBSCRIPTION.js`（宽基+亿份）口径** |
| 🛡️ DO_NOT_DELETE.md 推仓入库 | `DO_NOT_DELETE.md` +4 段 | 🟠 应用层脚本(scripts/) 表（6个新脚本）、data/DO_NOT_DELETE.js 登记、raw_data 5 个新 json 登记、🔴 防覆盖指南（Nutstore 实时同步如何不死于回退的 6 条铁律 + 自动化解套路径）|
| 🔧 周日任务联动 | `.github/workflows/v8_cleanup.yml` | 在「清理缓存」步骤前新增「🛡 同步 DO_NOT_DELETE.js」步骤（python3 scripts/gen_do_not_delete_js.py）|

---

## 三、三个 Tier 1 门禁实测数据（今日首跑）

### 1. IC 门禁 `raw_data/ic_gate.json`
- **overall_action = `warn`**（非 pass）
- `ge3`：n_signals=371，T1_win=49.1 / T3_win=32.3 / T5_win=38.3，fixed_T1_win=49.7 / fixed_T3_win=25.9
- 触发原因：**`ge3` T3 胜率 32.3 < 阈值 35.0** → gate=warn，`ic_weight=0.5`（权重减半）
- `top5_累计` / `h_auto_buy_累计`：暂无累计样本（null）

### 2. regime 门控 `raw_data/strategy_regime_gate.json`
- regime = **利率平稳**（confidence=低，framework_match=中国平稳+美国上行=框架部分匹配）
- **overall_action = `warn`**，全部策略（ge3/top5/h_auto_buy/crds）weight = **0.95**（base 1.0 × framework 0.95）
- 注：`alt_groups_down_regime` 仅在 regime=利率下行 时启用，已写入 `market_regime.json`，gate 不重复记录

### 3. freshness SLA `raw_data/freshness_sla.json`
- **overall_status = `BREACH`**，7 处 breach：
  - `cockpit_backtest.json`：无 update_time 字段（NO_UPDATE_TIME）
  - `top5_track.json`：无 update_time 字段（NO_UPDATE_TIME）
  - `h_auto_buy_track.json`：**MISSING**（预期 post_close）
  - `stock_quote.json`：无 update_time 字段（NO_UPDATE_TIME）
  - `sector_fund_flow_intraday.json`：无 update_time 字段（NO_UPDATE_TIME）
  - `avg_price.json`：**MISSING**（新 fetcher 首跑后生成）
  - `etf_subscription_em.json`：**MISSING**（新 fetcher 首跑后生成）
- 已确认 OK：`lhb_data.json`(2026-08-29 08:43, age 4.3h)、`factor_ic_report.json`、`factor_validate_report.json`、`market_regime.json`、`sector_fund_flow.json`、`limit_up_heatmap.json`

> 💡 这 7 处正是「图14卡8-04没人告警」的根因闭环：NO_UPDATE_TIME / MISSING 类文件此前无任何新鲜度校验，现在 freshness_sla.py 已能标红。下一步把 freshness_sla.json 接入运维面板「数据健康」卡即可可视化。

---

## 四、两个新 fetcher（脚本已就位，云端首跑待明天）

| fetcher | 脚本 | 产出 | 状态 |
|---|---|---|---|
| 平均股价 880003 | `scripts/fetch_avg_price.py` | `raw_data/avg_price.json` + `data/AVG_PRICE.js` | 脚本语法 0 错，UI 暂未接 |
| ETF 申购赎回（东财）| `scripts/fetch_etf_subscription_em.py` | `raw_data/etf_subscription_em.json` | 脚本语法 0 错，待替换旧 `ETF_SUBSCRIPTION.js` 口径并接驾驶舱/ETF三合一卡 |

⚠️ 旧 `data/ETF_SUBSCRIPTION.js`（宽基指数+亿份）口径不一致，已被新脚本的东方财富分类（股票型/债券型/货币型/商品型/跨境型）+ 净申购赎回金额（亿元）取代。**前端驾驶舱/ETF三合一卡尚未改读新口径**，属下一步待办。

---

## 五、小九行动项 / 待办排期

### 🔴 必须做（下一步，MVP 已约定留待此处）
1. **下游接入 ic_gate.json**：`generate_top10.py` 的 `score_backtest` 改读 `ic_weight` 做微调；`gen_triple_consensus` / `calc_crds` 的 `select` 前读 `overall_action` 做准出门禁（warn→降权/限仓，block→不出）。**当前 MVP 只产 JSON 不改逻辑**，这一环接上才算闭环。
2. **下游接入 strategy_regime_gate.json**：`generate_top10.py` / `gen_triple_consensus` 每个策略算分前读 `weight` 做乘子。
3. **freshness_sla 可视化**：把 `raw_data/freshness_sla.json` 接入运维面板「数据健康」卡，BREACH 时整站告警（避免再出现「卡8-04无人知」）。
4. **ETF 新口径接前端**：`fetch_etf_subscription_em.py` 跑通后，驾驶舱/ETF三合一卡改读 `etf_subscription_em.json`，删除旧 `ETF_SUBSCRIPTION.js`。
5. **平均股价 UI**：`fetch_avg_price.py` 跑通后，在仪表盘合适卡片展示 880003 平均股价趋势。

### 🟡 建议做
6. `h_auto_buy_track.json` MISSING：确认 `run_algorithms` 是否产出该累计追踪文件，若缺失补生成逻辑。
7. 4 个 NO_UPDATE_TIME 文件（cockpit_backtest / top5_track / stock_quote / sector_fund_flow_intraday）：在各自 fetcher 落盘时补 `update_time` 字段，freshness SLA 才能正确计时。

### 🟢 已闭环
- Bug A（已下架 3 卡空白）已修 + Playwright 验证 RENDERED + 推仓。
- 防删子页面已上线，与 `gen_do_not_delete_js.py` + 周日 `v8_cleanup.yml` 步骤联动。

---

## 六、stash 处置（勿直接 pop）

当前 `git stash list`：
```
stash@{0}: nutstore_pollution_pre_push_1236
stash@{1}: WIP_Nutstore_pollution_reserve_2026-08-29
```
- 这两个 stash 是 Nutstore 把本地工作树污染/回退前，阿狸咪为保全改动做的缓存。
- **处置建议**：当前 HEAD 已对齐 `origin/main`（`9da077a20`）且所有改动已入库，**这两个 stash 已无保留价值**，直接 `git stash drop stash@{0} && git stash drop stash@{1}` 清理即可。
- ⚠️ **不要 `git stash pop`**：会把 Nutstore 污染内容重新合回干净基，重演「改完被回退」。

---

## 七、实现「最高胜率 + 收益率自动化」还差什么（闭环成熟度地图）

| 断点 | 现状 | 缺口 |
|---|---|---|
| 测算胜率→选股权重 | ic_gate 已产 ic_weight，但**未接入** generate_top10 | P0-1：下游读 ic_weight 微调分数 |
| 执行层 | 纯研究/选股，无下单 | P0-2：无 broker 接口（用户暂未要求，属明确边界）|
| risk 约束 | risk 只提示不约束 | P1-1：regime_gate weight 已产但未乘入选股 |
| regime 开关 | regime 已算但未接成开关 | P1-2：下游读 weight 乘子 |
| 数据新鲜度 | freshness_sla 已标红但**未可视化告警** | P1-3：接入运维面板 |
| 因子衰减监控 | IC 门禁静态阈值 | 后续：T1/T3 win 滚动监控，跌破自动降权 |

> 结论：本次把「测算胜率→门禁信号」和「regime→权重乘子」两条链**从 0 建到 MVP 产信号**，但**消费端（选股逻辑真正读这些信号）尚未接**——这是距离「最高胜率自动化」最近的一公里，落在 §五 的 1/2 两项。

---

## 八、附：本次全部非-build 人工提交（防覆盖核对用）

```
9da077a20 feat(v8): Tier 1/2 升级 — IC门禁 + regime门控 + freshness SLA + 平均股价/ETF申购 fetcher + DO_NOT_DELETE.md 推仓入库
6f42d4445 fix(v8): 修复已下架3卡卡壳 + 新增「逻辑详解·防删」子页
（以上均已在 origin/main，HEAD = 9da077a20；中间 ce8d49ebd/bba0ed1d9 等为云端 v8 build 自动提交，非人工）
```

---

## 深夜补充：待云端首跑验证

- 两个 fetcher 明日首次云端运行后，应生成 `raw_data/avg_price.json` + `raw_data/etf_subscription_em.json`，届时 freshness_sla 的 MISSING 类 2 项应转 OK。
- `ic_gate.json` / `strategy_regime_gate.json` / `freshness_sla.json` 需由对应 workflow 在盘后档调度运行（建议挂到 `v8_cn_fetch_cloud.yml` 或周日 `v8_cleanup.yml` 的算法段）。**当前这 3 个 json 是我本地首跑产物，已入库作基线**，下次自动跑会刷新。
