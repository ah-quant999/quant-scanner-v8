# HANDOVER · 小九 → 阿狸咪 · 2026-09-21（12:00 槽位）

> **更新于 2026-09-21 12:15 CST（自动更新 · 12:00 槽位）**
> 本档由小九每交易日 **12:00 / 18:00** 两班自动生成，确保阿狸咪开机即见当日状态。
> 🔴 **本文刻意不写「本文自身所在提交的 sha」** —— 本仓 `main` 被 CI 以分钟级 force-update，
> 写死 sha 分钟级即失效。需要时用文末 **§7 现取命令** 自取。
> 文中所有 `sha` 均为「**截至 2026-09-21 12:14 CST** 用 GitHub API `/commits?sha=main` 实测可见」；
> 若后续对不上，请以**内容特征**为准、sha 只作线索（09-20 已因此立过约）。

---

## 一、背景

- **日期/时段**：2026-09-21（周一，交易日）。小九白班窗口 **07:45–17:45** 正在跑；阿狸咪为夜间/周末班。
- **今日主线**：① 机构研究卡「一直解读 09-18 素材」根因连根修复；② 排程改点方案 B（08:00/08:20/08:50）
  落地 + **11:35 flush 回验通过**；③ **双机监控告警路径七周全死**被修复打通；④ 双机交接协议变更
  （状态源 `HANDOFF.yaml`，**禁止回执/催办档**）；⑤ 个股查询「所有查询都查不出来」一劳永逸修复。
- **协议变更要点（阿狸咪侧 `f1bac4601`，08:48 CST）**：
  `chore(ops): 双机交接协议变更——状态源 HANDOFF.yaml 上线，禁止回执/催办档`
  ⇒ 本档起**不再要求写回执档**；职责改为「状态快照 + 待办/风险登记」。
- **仓储 churn 实况**：今日 main 截至 12:14 已 **282 个提交**（含 build / 缓存戳 / 心跳等机器提交）。
  任何「写死 sha / 写死 tip」的表述都会在几分钟内过期。

---

## 二、今日已完成改动

### A. 小九侧（含本轮自主修复）

| 时刻 CST | commit | 主题 | 影响面 |
|---|---|---|---|
| ~07:0x | （见内容特征）| **每日洞察「月度汇总」口径行移位** —— 由「标题下方居中」改为与两卡同 grid 并列 | `index.html`；后被阿狸咪改写为「第二行通栏」形态（见 B 表 `4f87d6adf`），**最终线上形态以阿狸咪版为准** |
| 08:40 | `17cc640b1` | 交接档：机构研究卡解读 09-18 根因根治（素材冻结 / S4U 编码降级 / 日志禁用） | 文档 |
| 08:55 | （代码 + 交接档）| **观澜台素材链四层修复**：`algorithms/guanlan_extractor.py` 新增富文本实体解码 + 补 `title`；新建计划任务 `v8-guanlan-refresh`（06:40 → **08:00**）；`v8_health_check.py` 新增 `check_guanlan_material_freshness()`；出稿 automation `30e1b099` 注入「步骤 0 素材新鲜度护栏」 | 代码 + 计划任务 + automation prompt |
| 09:08 | `2bd1b0dac` | 交接档：机构研究卡**排程改点方案 B** + 发布闸门两处根治 | 文档 |
| 09:36 | `d33681bfb` | 交接档：staging 内容替换说明（方案 B） | 文档 |
| 09:36 | `f583eb808` | 交接档：**紧急 · 双机监控告警路径七周全死已修复** | 文档 |
| 09:51 | `0d53f5731` | 交接件：staging round-3 rebuild（基线被云端 churn 吞掉） | 文档 |
| **12:0x** | **本轮** | 🔴 **自主修复**：`v8_health_check.py` 的 `guanlan_material` **结构性假阳性**（详见 §三） | `v8_health_check.py` 199358 → **202605 B** |
| 11:57 | `ac75450c4` | **个股查询「所有查询都查不出来」一劳永逸修复** —— A2 懒加载器不再等 `window.load`（加载期 `window.STOCK_LIST` 恒空 ⇒ 任何输入都答「未找到」）；新增 `_slReady/_slEnsure`（未就位明说「加载中」+ 自兜底注入 + 到位自动重跑）；echarts 补 `defer` | `index.html`；交接档 `7b1eb2398` |

### B. 阿狸咪侧（供交叉确认，非小九产出）

| 时刻 CST | commit | 主题 |
|---|---|---|
| 09:00 | `4f87d6adf` | ui(每日洞察)：月度汇总口径说明自第 3 列移至**第二行通栏**（小九 07:0x 版的重做） |
| 08:48 | `f1bac4601` | **双机交接协议变更**：状态源 `HANDOFF.yaml` 上线，禁止回执/催办档 |
| 09:32 | `ab8cf5434` | **`v8_peer_monitor` 一劳永逸八项修复 —— 打通告警路径**（原七周静默死） |
| 09:26 | `03728f7ec` | fix(repo-hygiene)：行尾判据纠正 + 判据 C 撤回 + docs_ops 收口 + 日志写侧收紧 |
| 09:28 | `8c25436ff` | fix(eol)：修复上一条推送造成的 **4 个真·永久脏**（推送器补推边界 `fold_eol`） |
| 09:31 | `dfeb23b9c` | docs(audit)：修正 `pre_deploy_audit` 注释落差（如实改为「本轮已做/尚未做」） |
| 09:25 | `76431e643` | 登记 P0：`[10/10]` 锚点 `ms-col ms-note` 失效致 deploy 全阻断 |
| 11:37 | `f320fb311` | fix(ui)：清理因子实验室「第二步」删除残留（9 处） |
| 11:40 | `ca5d70e92` | **挂账 P0**：`baihechou-analysis-no-producer`（`BAIHECHOU_ANALYSIS.js` 三天零写入） |
| 12:01 | `24a7d9d97` / `9ee972fe8` | fix(ops)：双机状态卡对齐实况；恢复被上一提交回退的 `?v` 缓存戳 |
| 11:36 | `37f3f3aca` / `ef8cd1f5e` | `data: index.html / logic.html 经发布闸门核对 20260921`（= **11:35 flush 的落地印记**） |

---

## 三、🔴 本轮小九自主修复：`guanlan_material` 结构性假阳性（已上线）

### 症状
今日 12:03 CST 线上 `data/HEALTH_CHECK.js` 的 `summary.fail = 2`，其中一条为：

```
[guanlan_material] 观澜台素材新鲜度 · 内容审计 · fail
  素材文件不存在: out/guanlan_reports.json（guanlan_extractor.py 从未成功产出）
```

### 根因（本检查**当日上午 08:20 由小九自己引入**）
- `out/` 被 `.gitignore:51` 忽略 ⇒ **云端与自托管 runner 的工作区都不存在 `out/` 目录**；
- 而 `v8_health_check.py` 的**唯一运行环境就是 CI**（本机无该脚本的调度任务）；
- ⇒ 新加的检查在任何真实运行环境里**恒判 fail**，等于在健康面板钉一枚**永不熄灭的假红灯**，
  报文还误称「`guanlan_extractor.py` 从未成功产出」（实际它 08:08 刚成功刷新、素材最大日期 = 今日）。

### 修复口径：判「**本环境能不能判**」，而不是「文件在不在」
`v8_health_check.py::check_guanlan_material_freshness()` 改为三分支：

| 环境 | 判据 | 结果 |
|---|---|---|
| 本机（存在 `out/`，非 CI） | 原直判逻辑（读素材内**最大报告日**） | 保持原语义，真冻结仍判 `fail` |
| CI（`GITHUB_ACTIONS=true`） | 改用**已提交进仓**的 `data/maharo_insights.js` 里出稿自述的「素材窗口 A ~ B」作**次生信号**：`B >= 今日 或 >= 上一交易日` → `ok`；落后 → `fail` | 云端保留**真**信号 |
| CI 且出稿档未自述窗口 | 无法间接评估 | `limited`（不可判，**非故障**） |

### 实测四分支（净室复现，全部符合预期）

| 分支 | 构造 | 结果 |
|---|---|---|
| A 本机 | `out/guanlan_reports.json` 存在（max=2026-09-21） | `ok` "素材最新 2026-09-21（今日），共 20 条" ✅ |
| B CI | 无 `out/`，出稿档窗口末 = 2026-09-21 | `ok` "间接判据：窗口末端 2026-09-21…未落后" ✅ |
| C CI + 冻结 | 把窗口改成 `2026-09-14 ~ 2026-09-15` | **`fail`** "落后今日 5 个交易日" ✅（真红灯保留） |
| D CI + 无窗口 | 出稿档无「素材窗口」字样 | `limited`（**不再误判 fail**）✅ |

### 交付物与自证
- 文件：`v8_health_check.py` `199358 → 202605 B`（`+3247`），**`cr=0` / `bareLF=3347`**（纯 LF，与线上一致）；
- `python -m py_compile` **OK**；`difflib` 与线上真身严格 diff **仅命中该一处分支**（65 行 diff，无越界改动）；
- 入仓方式：**隔离索引净室推送**（`read-tree base → update-index → write-tree → GATE lost=0 → 非 force`）。

### ⚠️ 预期中的「未转绿」
线上 `HEALTH_CHECK.js` 要等**下一次云端 build/算法链重跑**才会用新脚本重生成。届时该条应由 `fail` 变 `ok`。
**阿狸咪复核点**：下一次 `HEALTH_CHECK.js` 的 `update_time` 之后，确认 `guanlan_material` 不再出现在 fail 列表。

---

## 四、11:35 flush 回验（今日待办 1 · **已闭环**）

以 **GitHub API 权威真身**（`/contents/<path>?ref=<tip>`，`Accept: raw`）实测，12:14 CST：

| 项 | `index.html` | `logic.html` |
|---|---|---|
| 字节 | `1,344,450` | `753,241` |
| 行尾 | **LF**（`cr=0` / `crlf=0` / `bareLF=19038`） | **CRLF**（`cr=8538` / `crlf=8538` / `bareLF=0`） |
| `07:15` 计数 | **0**（期望 0）✅ | **1**（期望 1，仅历史事故记录）✅ |
| `08:50` 计数 | **7**（期望 7）✅ | **1**（期望 1）✅ |
| 陈旧缓存戳 `?v=1789952802` | **0**（未回退）✅ | — |
| 防覆盖锚点 | `ms-col ms-note`=0 / **`ms-note-card`=4**（阿狸咪换锚后生效）✅ | — |

⇒ **方案 B 的排程口径（08:00 素材 / 08:20 日报 / 08:50 出稿）已完整落到线上，缓存戳零回退。**

---

## 五、当前系统状态（截至 2026-09-21 12:14 CST）

| 项 | 实测值 |
|---|---|
| 站点 | `https://ah-quant999.github.io/quant-scanner-v8/` **HTTP 200**，`index.html` `1,344,450 B`，`BUILD=38b4c4a1a` |
| main tip | `3db0f2486`（12:14 CST）—— 12:02 `7b1eb2398` → 12:11 `3e5797057` → 12:14 `3db0f2486`（**分钟级滚动**） |
| 今日 main 提交数 | **282**（GitHub API，since 00:00 CST） |
| 线上 HEALTH_CHECK | `update_time = 2026-09-21 12:03:15`，`overall = warn`，`ok 119 / warn 0 / fail 2 / limited 2 / info 1 / total 124` |
| ├ `fail` 明细 | ① `BAIHECHOU_ANALYSIS`（更新于 3 天前 06:31 > 1440min）→ **阿狸咪已挂账 P0 `ca5d70e92`**；② `guanlan_material` → **本轮已修，待 HC 重生成转绿** |
| └ `limited` 明细 | `MACRO_KSHAPE`（事件驱动，非故障）/ `UNLISTED_PANEL`（静态索引，非故障） |
| runner | `lemoncat-cn` **online** / `alimi-cn` **online**，两者 `busy=False` |
| 本机心跳 | `hb_xiaojiu.json last_time = 2026-09-21 11:53:12`（status=ok，距今约 21min，正常） |
| ⚠️ 对端心跳 | `hb_alimi.json last_time = 2026-09-20 21:57:02`（status=ok，距今约 **14.3h**）—— **观察项**，见 §6 |
| 关键产物 | `maharo_insights.js` 17208 B @08:40；`STOCK_QUOTE.js` @11:31:02；`INDEX_QUOTES.js` @11:30:26 |
| 自动化（本机 DB） | **ACTIVE 120 / PAUSED 21**；今日 `automation_runs` **137 条 ACCEPTED（134 success）· FAILED 0**（09-20 全天 56 条） |

---

## 六、需阿狸咪注意 / 待办 / 风险

### 🔴 需阿狸咪注意（优先）
1. **`guanlan_material` 转绿复核**：下一轮云端 `HEALTH_CHECK.js` 重生成后，确认该条已由 `fail` → `ok`
   （若仍 `fail`，请把整条报文回传，可能是出稿档未写「素材窗口」行）。
2. **`BAIHECHOU_ANALYSIS` 归属对齐**：阿狸咪已挂账 P0 `baihechou-analysis-no-producer`；
   小九侧今日 07:30 的「白河愁宏观卡每日更新」自动化**主动跳过推送**（理由：线上已含当日更新版 analysis
   且帖子文本逐字一致、零信息增量）⇒ **两方对同一张卡各有一套判断，请明确唯一责任方**，避免双写/双盲。
3. **`HB_ALIMI` 心跳陈旧**：`2026-09-20 21:57:02`，距 12:14 约 **14.3h**。
   已知阿狸咪 09:32 刚修复 `v8_peer_monitor` 告警路径（`ab8cf5434`）⇒ **本项按新判据可能触发告警**。
   小九**不下归因**（可能是心跳腿写入方未跑，也可能是阿狸咪班次窗口外的正常空档），请阿狸咪自查心跳腿。
4. **交接协议已切换**：`HANDOFF.yaml` 为唯一状态源、**禁止回执/催办档** ⇒ 阿狸咪侧旧自动化若仍在写回执档，请下线。

### 🟡 待办（延续 / 本轮新增）
1. **方案 B 的「盘中补充版」未落地**（候选 14:00 覆盖 50.5% / 15:10 覆盖 60.2%），接入点待主人确认。
2. **`v8_publish_gate.py` 是本机工具、不入仓**（`ls-tree` 6023 文件无此路径）⇒ 双机各自维护，push 同步不了；
   阿狸咪若需要同版闸门需单独取。
3. **`index.html` 里那份阿狸咪 WIP** 是否为其最终版，待阿狸咪确认（小九已把它叠加保留、未覆盖）。
4. **`v8-maharo-daily` / `v8-guanlan-refresh` 排程**：已分别改为 08:20 / 08:00（`LogonType=Interactive`，
   刻意不用 S4U）。下次运行时间实测 `2026-09-22 08:00:00`。
5. **任务计划程序事件日志**已启用（原 `IsEnabled=False` ⇒ 任务失败零记录），后续失败可在
   `Microsoft-Windows-TaskScheduler/Operational` 查到。

### 🔴 风险 / 纪律（今日新踩坑，均经实测）
1. **跨境 HTTP 半程截断会给出「看起来正常的小文件」**：本轮取线上 `data/maharo_insights.js` 时曾读到
   **7101 B**（真值 17208 B，md5 `11675e979f…`），三次重取后一致 ⇒ **判线上真身必须「重取 ×2 且 md5 一致」**，
   单次读数不可作基线。
2. **`HEALTH_CHECK.js` 的检查要在「它真跑的環境」里可判**：任何依赖 `.gitignore` 忽略目录
   （`out/`、`raw_data/` 部分）或本机私有路径的检查项，**在 CI 里必须降级为 `limited`**，
   否则会生产「永久假红灯」（本轮 `guanlan_material` 即实例）。
3. **`len(a)==len(b)` 绝不等于 `a==b`**（缓存戳 `?v=` 同为 10 位数字，替换后字节数完全不变）⇒
   判回退必须用 **md5 + 严格 diff + 目标串 `count()`**。
4. **`unified_diff(a,b)` 的 `-` 属左、`+` 属右** —— 正负号不可单独作结论，必须用 `count()` 交叉验证
   「新串 >0 且旧串 ==0」才算生效。
5. **shallow 克隆下 `merge-base --is-ancestor` / `git log` 恒假阴性** ⇒ 判「是否已收编」一律走
   **GitHub API `/commits?sha=main`** 或内容 blob 比对。
6. **`git log --oneline origin/main` 在 shallow 下只给 1 条**（`.git/shallow` 现 **80** 行）；本轮取历史全走 API。
7. **`read-tree <sha>` 报 `failed to unpack tree object`** ⇒ 先 `git fetch origin <sha> --depth=1` 补对象。
8. **推送一律走 SSH**（`git@github.com:…`）：HTTPS 会报 `could not read Username … terminal prompts disabled`（RC=128）；
   且**永不 force**。
9. **写仓窗口**：盘中 **09:15–11:30 / 12:45–15:00 禁写禁推**；本轮 12:15 前完成推送，属合规窗口内。
10. **`WorkBuddy` Bash 工具 `cwd` 每次调用重置** ⇒ worktree / 临时脚本操作必须单条命令内 `cd` 全链完成。

---

## 七、现取命令（不写死任何 sha）

```bash
# 1) 远端权威 tip（禁用 refs/remotes/origin/main，会被管线静默回卷）
cd /e/qs_workspaces/quant-scanner-v8 && git ls-remote origin refs/heads/main

# 2) 本档线上真身 + 字节数（内容不变量，替代写死 sha）
git cat-file -s $(git ls-remote origin refs/heads/main | cut -f1):HANDOVER_小九_2026-09-21.md

# 3) 判「本档是否已在 main 上」——走 API，别走 merge-base
#    GET /repos/ah-quant999/quant-scanner-v8/contents/HANDOVER_小九_2026-09-21.md?ref=main

# 4) HEALTH_CHECK 现状（判 guanlan_material 是否转绿）
curl -s https://ah-quant999.github.io/quant-scanner-v8/data/HEALTH_CHECK.js | grep -o '"summary":{[^}]*}'
```

---

## 八、小九监督职责（本班次不变）

| 职责 | 状态 |
|---|---|
| 白班（07:45–17:45）盘中链盯守 + run 生死巡检 | ✅ 运行中 |
| 12:00 / 18:00 交接档自动更新 | ✅ 本班次（12:00）完成 |
| `v8-guanlan-refresh`（08:00）/ `v8-maharo-daily`（08:20）本机排程 | ✅ 已注册、NextRun 09-22 |
| 观澜台素材新鲜度监控（本轮修复为 CI 可判） | ✅ 已上线 |
| 云端链路失败 / 死锁巡检（`v8_run_liveness_patrol` 口径） | ✅ 运行中 |
| 阿狸咪侧自动化**只读不写**（铁律） | ✅ 本班次零触碰 |

---

*小九 · 2026-09-21 12:15 CST · 自动生成 · 12:00 槽位*
