# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-20 10:12｜最终推荐口径统一（=因子榜前 5）+ 因子实验室迁入最终推荐页

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

`algorithms/final_recommend.py` 里的**第二套打分公式（`final_score = strength + resonance×1.5 + sec_add − weak_penalty`）已按主人令彻底删除（不留回退分支）**，最终推荐名单与顺序改为**直接取因子榜 `TOP10_DAILY.total_score` 降序前 5**；「🧪 因子实验室」整页从「选股策略」页迁到「最终推荐」页第 3 个子 Tab。**已上线并已用 D 批真跑验证生效**（新名单 `2026-09-20 09:50:20`）。

---

## 正文

### 1. 根因（主人为什么发火）

页面上呈现给主人的只有**一套**真实数据（因子计算 `TOP10_DAILY.js`），但「最终推荐」是 `final_recommend.py` **另起炉灶又算了一遍**，且两套之间没有任何排序层面的衔接：

```
final_score = strength + resonance × 1.5 + sec_add − weak_penalty
```

分值区间仅 5~8，其中 `resonance`（跨策略共振数）一项 ×1.5 即可定名次 —— 江波龙只因多命中一个策略（三重共识）就 +1.5，反超了因子分更高的洛阳钼业。

| 票 | 因子榜 | 名次 | 旧最终推荐 | 名次 |
|---|---|---|---|---|
| 洛阳钼业 603993 | **45.3** | #1 | 7.40 | #2 |
| 江波龙 301308 | 42.9 | #2 | **8.06** | **#1** |

主人原话：「统一口径！我要看得到的真实算出来的展示」「你的最终推荐从哪儿来的，我看不到」「**第二套内部算法清除干净，不得回滚覆盖**」。

### 2. 改了什么（3 个文件 + 一轮语义归位，全部已上线）

#### 2.1 第一批（commit `96f822880e48`，09-20 09:0x）

| 文件 | 改动 | diff |
|---|---|---|
| `algorithms/final_recommend.py` | ① 删公式本体 + 港股惩罚 `HK_PENALTY` 扣减 + `top` 二次重排（**活代码零残留**，仅注释保留说明）；② `weak_penalty` 台账同步改标「仅标注·不计分」；③ `final_score` 直接取因子榜 `total_score`；④ 排序键改为因子榜名次 | +64 / −23 |
| `index.html` | `st-fa` pane 整块（定位卡 + 每日因子计算 + 已纳入因子 + 当日因子计分榜，7066 字符）→ `final-pane-fa`；`final-tabs` 增第 3 个按钮 `data-final="fa"`；`st-tabs` 摘除 `data-st="fa"` 按钮。**未做左右改版**（主人令：先迁移、不做左右改版） | hunk 4 |
| `logic.html` | 口径表述同步：原「按跨策略共振 + 板块强度加权排序」→ 因子榜口径（行 1447 / 1483） | 2 处 |

#### 2.2 第二批（本轮，index.html 语义归位 / 死代码清理）

迁移后产生 3 处语义不齐，一并清干净（hunk 5 / +16 / −12，删除行零夹带）：

| # | 位置 | 改动 |
|---|---|---|
| ① | `sec-st` 头部 tag | 移除「因子实验室」（**用户可见文案**，它已不在本页） |
| ② | `window.switchSt` 内 `if(n==='fa')` 分支 | **删除**（st-tabs 已无 fa 按钮 ⇒ 死代码），注释同步为历史沿革 |
| ③ | `window.switchFinalTab` | **新增 `fa` 分支**：调 `__renderFactorPane()`（内部串起三卡）+ 复用 `__stRetryTab('fa')` 就绪重试 —— **渲染触发语义归位** |
| ④ | 行 7648 / 15748 注释 | `st-fa 子页` → `「最终推荐」页第 3 子 Tab · final-pane-fa` |

> 关于渲染：迁移**不产生功能缺陷**。页面级 `_renderPostReady()`（`DOMContentLoaded` 无条件执行）已含 `__renderFactorPane()`，且各渲染函数自带 15 次数据就绪重试；第二批的 `switchFinalTab` fa 分支只是**切换兜底重渲**，属加强而非补漏。

`sections` 变化：`sec-st`(=逻辑上 sec-op 的 st-tabs) st-pane 6 → 5；`sec-final` final-pane 2 → 3（`switchFinalTab` 是选择器驱动，id/class 写对即自动生效）。

### 3. 验证（可复现，非推断）

- **单元级真跑**：从真实源码抽出排序段 + 喂线上真实数据执行 →「改造后名次 = 洛阳钼业 → 江波龙 → 天华新能 → 金山办公 → 中信证券」，与因子榜前 5 **名次 + 分值（45.3/42.9/41.4/40.4/38.5）全一致**；反向对照旧算法 #1 为江波龙 ⇒ 改动确实生效。
- **D 批端到端真跑**（见 §4）：线上 `FINAL_RECOMMEND_DATA.js` `update_time = 2026-09-20 09:50:20`，5 只与因子榜**逐位一致**。
- **语法门禁**：28 段 inline JS / 995249 字符 / **0 错**。
- **结构验证**：3 final-pane / 3 final-tab / st-pane 5 / `data-st` 无 fa / sec-op tag 无「因子实验室」/ `final-pane-fa` 内含全部搬迁内容与「两步什么关系」新文案。
- **线上**：`index.html` sha256 与本地产物逐字节一致；推后防覆盖复检零覆盖（后续 `v8 build` 只改 `data/*.js` 缓存戳，归一化 sha 不变）。

### 4. D 批实跑结果（2026-09-20 周日休市，主人令「可以直接跑」）

- 派发：`v8_algo_run.yml`（cn 链，**不调用 `v8_stage_gate.py`**，直连 `run_algorithms.py --stage D` + `force_run=true`），run **#35481053530**（09:18:29 CST 起）。
- 结果：算法链 / 桥接 / `update_v8` / 缓存戳 / 健康检查 **全部 success**。
- 产物：`data/FINAL_RECOMMEND_DATA.js` `update_time = 2026-09-20 09:50:20`：

```
#1 洛阳钼业 603993  final=45.3
#2 江波龙   301308  final=42.9
#3 天华新能 300390  final=41.4
#4 金山办公 688111  final=40.4
#5 中信证券 600030  final=38.5
```

- 与 `TOP10_DAILY.js`（`update_time = 2026-09-20 09:20:01`）前 5 **逐位一致** ⇒ **口径统一已在线上真实生效**。

### 5. 🔴 注意事项（小九务必知，均为本轮实测）

**(a) 非交易日跑 D 批的三个已知副作用（本次实测确认）：**

1. `final_recommend.py` 的 FACTOR_LAB 新鲜判据要求 `update_time.startswith(今天)` —— **`_fl_dd` 有周末豁免（2026-09-17 加），但 `update_time` 这一路没有** ⇒ 非交易日必然空等满 `_fl_max_wait=20` 分钟后退化为「无因子源」（`fl=None`）。
2. `_quote_snapshot()` 要求行情快照 `update_time 日期 == 今天`：`STOCK_QUOTE.js` 停 `2026-09-18` ⇒ 回退 `CANDIDATE_QUOTES.js`（当日已刷新，可用）。
3. 产物 `update_time` / `close_date` 会被打成**非交易日日期**（周日），而 `TOP10_DAILY.js` 为当日 ⇒ 两卡「🦊 更新于」日期不同（**计分榜卡不显示日期，无可见分裂**）。

**(b) 两处闸门口径（避免误判「链坏了」）：**

- 云端链 `v8_stage_gate.py` 对周日判 `NONE`。实测 `run #35477406746`（09-20 07:55 CST）：
  `target_stage=NONE / proceed=false / reason=四批产物均已就绪 -> 空转（合规，真成功）`
- 要绕过它只能走 `v8_algo_run.yml`（cn 链），且**必须 `force_run=true`** —— 否则 09:00–17:59 时段 `_is_post_close_picking_ready()` 会拦截全部选股脚本（`_NEXT_DAY_CUTOFF_HOUR=9` 之前才放行）。

**(c) 你的新改动与本轮无冲突。** 你今早的 `fix(fusion): signal edge …`（`af2a2ed45d` / `f39e3bc96f`）改的是 `SIGNAL_EDGE` 权重区（行 239-342 / 813-820 / 1614-1643），与本轮 `final_score` 排序口径区**不同区域**，已核实本轮改动未被回退（旧公式活代码残留 = 0）。

### 6. 建议（未擅自执行，等主人拍板）

给 `update_time.startswith(_today)` 补上与 `_fl_dd` **同款的周末豁免**（同一代码块，2026-09-17 已为 `_fl_dd` 加过一次）—— 这样非交易日跑 D 批不再空等 20 分钟 + 丢因子源。涉及算法源码，需走「改后三件套」，本轮未动。

### 7. 顺带规避的风险

本机工作区 `algorithms/final_recommend.py` 存在**未提交脏改动**（把已按 09-19 主人令删除的「强势突破共振」整段加了回来）。本轮**基于远端干净基线重做**，未夹带该脏改动（落盘后 `git diff FETCH_HEAD` = +64/−23，与补丁一致）。小九若在本机工作区看到该段，请一律丢弃。
