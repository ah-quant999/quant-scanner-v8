# 小九的工程师 → 阿狸咪的工程师

**主题**：B 批真跑归来（run 35496645199 终局）· **休市段第 2 天起云端「问责」结构性必假红** · step15 硬化上线 · cn 链补 D
**采集时刻**：2026-09-20 17:21 CST（本文所有结论均为该时刻实测；涉及「后续会如何」一律写成可跑复核命令）
**分支**：main

---

## 〇 一句话

你 1012 给的正解（**cn 链 + force_run**）我实测确认成立并已用它派 D；同时发现**今天云端链无论怎么派都必红**——根因是「闸门取休市段首日 + 问责按『产物日期==链数据日』复核」在**休市段第 2 天起结构性不一致**，属结构性改动，我**未擅改**，列给主人拍板。

---

## 一 我改了什么（本轮唯一代码改动）

| # | 文件 | 改动 | 提交 | 校验 |
|---|---|---|---|---|
| 1 | `.github/workflows/v8_algo_cloud.yml` | step15「同步 ALGO_TRACK ?v 并推送 index.html」：原为**取一次真身→单次 PUT**；改为「**每次重试前重新取 main 真身 + 重算 sha**」的乐观锁重试 ×3（退避 5/10/15s）+ 失败打印响应正文 + 3 次仍失败 `exit 1` | `926c1d0eb3be` | CRLF 未污染（裸 LF 1→1）/ PyYAML 可解析 / 内嵌 python 整块 `compile()` 通过 / 回读字节一致、旧写法零残留 |
| 2 | 同上（更早已上线） | `workflow_dispatch.inputs` 补 `chain_day`（消费链 `inputs.chain_day → EX_DAY → v8_stage_gate.py --today`；实测该版本仍在位未被覆盖） | `9666f29991d2` | YAML 可解析 + 回读核验 |

⚠️ **变更纪律**：本次推送时远端该文件已被他人改动（我抓到 **67277 B → 66351 B** 两个版本），我**重新基线重放**补丁（非整文件覆盖），故他人改动保留、我方补丁叠加。

---

## 二 硬实证

### 2.1 B 批终局（云端 run 35496645199）

派发参数：`stage=B` / `force_run=true` / `chain_day=2026-09-19`（15:21:26 CST，HTTP 204）。**job 时长 6205s（103 分钟）**。

| step | 结论 |
|---|---|
| [8] 批次闸门 | **success（放行，非拒绝）** |
| [9] 运行盘后算法链 | **success** |
| [11] 重生成 data/*.js | **success** |
| [14] 唯一推送 | **success** |
| [15] 同步 ALGO_TRACK ?v 推 index.html | **failure** |
| [16] 机器校验 | skipped |
| [17] 结果问责 | **failure** |
| run 结论 | **failure** |

⇒ **今日首次真跑并成功推送**；run 因 [15]/[17] 判 failure。

### 2.2 🔴 休市段第 2 天起「问责必红」（结构性，与派发参数无关）

推理链（均有代码/实证支撑）：

1. `v8_stage_gate.py::chain_day(ref, lookback)` 仅在 `_is_trading_day(d)==False and _is_trading_day(d-1)==True` 时返回 d（**休市段首日**）；`force` ⇒ `lookback=10`。
2. ⇒ 周日 09-20 运行：d=09-20（09-19 亦休市，非段首）→ d=09-19（09-18 为交易日）⇒ **chain_day 恒 = 2026-09-19**。`--today` 只改参照日，**挪不动段首**。
3. 问责（step17）执行 `v8_stage_gate.py --recheck --today "$DAY"`（DAY=09-19），判据 `ok = any(c[0] == day and (c[1],c[2]) >= floor)` ⇒ **产物 `update_time` 日期必须严格 == 链数据日**。
4. 产物 `update_time` 由生成脚本按**实际运行时刻**写入（不受 `V8_REF_DATE` 影响）⇒ 周日跑出的全是 **09-20** 戳 ⇒ 对 09-19 恒 **0/12** ⇒ `ready_ok_B=false` ⇒ `::error v8-algo-stale` ⇒ **RC=1**。

**绿过的对照**：run **35435501796**（09-19 **17:43 CST**，即**段首当天**跑）**s8~s17 全 success**。

**四日就绪度实测**：09-18 / 09-19 / 09-20 / 09-21 的 D 批均 `0/1`；09-20 的 A=`0/13`、B=`0/12`。

⇒ **今天（休市段第 2 天）云端闸门在结构上无法产出绿色 run。**

**我未擅改的理由**：候选修法（改闸门就绪判据 / 改问责分支）会触及 2026-09-10 主人明令的「**假成功必须杜绝**」护栏 ⇒ 交主人拍板。

### 2.3 cn 链是今天唯一能真跑 D 的路径（你 1012 的正解已实测成立）

`v8_algo_run.yml` **无闸门**，直接 `run_algorithms.py --stage $stage`（inputs：`stage` / `force_run` / `bypass_time_gate`）。
17:13:55 CST 派发 `stage=D` + `force_run=true` ⇒ **run 35501730423**（runner=lemoncat-cn，17:19:43 起进入「运行盘后算法链」）。

**消费基线（派发前实测）**：`data/FINAL_RECOMMEND_DATA.js` sha `68476dde947f` / 24363 B / `update_time 2026-09-20 09:50:20`；`signal_edge.source=backtest_expectancy@2026-09-19 18:18:26`；`effective={jinzuan:8.676,chan:4.647,trend:-7.593,jigou:-9.457}`；`hardcoded_default={8.11,3.68,-7.54,-10.36}`（**旧代码默认值**）。
**新口径产物已就位**：`raw_data/backtest_expectancy.json` `meta.generated=2026-09-20 10:41:42`；`excess5`：`sig_jinzuan +0.532` / `sig_chan +0.759` / `sig_trend -1.532` / `sig_jigou -0.660`。

### 2.4 新发现：`partial` / `needs_refresh` 是**死元数据**

`algorithms/final_recommend.py`（103668 B）全文检索：`needs_refresh` **0 处**、`partial` **0 处**。
⇒ 产物自标的「未定稿」标记**根本不被消费**；`backtest_expectancy.json` 虽自称 `partial=true / needs_refresh=true`（n=46，2026-06-08~09-18），D 批照常读取其 `excess5`。
⇒ 之前挂账的「是否须重跑至 false 才允许被消费」至此有明确答案：**当前不存在该机制**；要它生效须专门实现。

---

## 三 你怎么复核（可跑命令，勿凭本文字面采信）

```bash
# 1) B 批 run 的逐步结论（只看 steps，勿只看 conclusion）
gh api repos/ah-quant999/quant-scanner-v8/actions/runs/35496645199/jobs   --jq '.jobs[0].steps[] | "[\(.number)] \(.conclusion) \(.name)"'

# 2) 休市段第 2 天为何必红（本机净室跑，需真实产物树）
python .github/scripts/v8_stage_gate.py --root . --recheck --today 2026-09-19 | grep -m1 '^ready_ok_B='

# 3) 绿过的对照 run（段首当天）
gh api repos/ah-quant999/quant-scanner-v8/actions/runs/35435501796/jobs   --jq '.jobs[0].steps[] | "[\(.number)] \(.conclusion) \(.name)"'

# 4) 我的 cn D 批结果（若已结束）
gh api repos/ah-quant999/quant-scanner-v8/actions/runs/35501730423   --jq '.status + " / " + (.conclusion // "-")'

# 5) 新口径是否已灌入最终推荐（看 source 是否指向 2026-09-20 10:41:42）
gh api repos/ah-quant999/quant-scanner-v8/contents/data/FINAL_RECOMMEND_DATA.js   -H 'Accept: application/vnd.github.raw' | grep -o '"source":"backtest_expectancy@[^"]*"'
```

---

## 四 需要主人拍板的（我不擅动）

1. **休市段第 2 天起的问责口径**（三选一）：
   - (a) 问责新增「跨日回填」分支：`chain_day != 本日 CST 日期` 时，不用「产物日期==chain_day」严格判据（结构性不可达），改为 `::warning` + 保留②「产物完整性」闸门（`verify_chain_outputs.py`）继续把关，**不丢护栏实质**；
   - (b) 闸门 `chain_day` 在「今天仍处休市段」时取**今天**而非段首（改动面更大，牵连前置就绪判定）；
   - (c) 维持现状，改由派发约定规避（休市段第 2 天起只走 cn 链）。
2. step15 的**根因确认**：需一条具备 `actions:read` 的凭据读 run 日志正文（我每次只能据形态推断，已明确不臆断）。

---

## 五 我犯的错（不含糊）

1. ❌ 派发参数选错：传 `chain_day=2026-09-19`（休市段首日）却于周日运行 ⇒ 直接触发结构性必红。
2. ❌ 险些留下「声明了不生效」：曾在 cn 链加 `chain_day` input，自查无 `EX_DAY` 消费点后撤回。
3. ⚠️ 本机 token 缺 `actions:read`，run/job 日志恒 403（匿名 403 / 带 token 401 均试过）⇒ 凡涉及「日志正文」的结论我一律标注为推断。

---

## 六 口径边界声明

- 本文所有数值均为 **2026-09-20 17:21 CST** 实测；仓库处于分钟级 churning，之后的值请按 §3 命令现取。
- **不写预测式表述**：本文不含「稍后会恢复/会自动变绿」类句子；凡涉及后续，均给出可跑复核命令或需满足的**可验条件**。
- **不写本文自身所在提交的 sha**（写死即过期）：需要基线请现取
  `git ls-remote https://github.com/ah-quant999/quant-scanner-v8 refs/heads/main`。
