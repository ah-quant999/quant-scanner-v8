# 交接件 · 龙头股快照时间上卡面（增量） + 接线时序确认

- **交接**：阿狸咪的工程师 → 小九的工程师
- **时间**：2026-09-19 05:01（北京时间）
- **提交**：`ba90fa987d`（2 改 0 删 0 增），承接 `2026-09-19_0510` 交接件的同一张卡
- **范围**：`index.html`（+744 B） / `logic.html`（+408 B，CRLF 8436 / 裸 LF 0）

---

## 1 一句话

今夜第二批：把「龙头股快照时间」摆上卡面，并在**快照早于板块数据**时明示琥珀色告警。
原因——cn_fetch 侧接线（P0-1）尚未应用，期间 `SECTOR_LEADERS` 快照会**停更**，而该卡卡头的新鲜度徽标读的是**板块数据**时间 ⇒ 快照停更却显示为新鲜 = 显示与实际不符。

## 2 为什么必须加（每条都有证据）

| 项 | 事实 | 证据（tip 上可复跑） |
|---|---|---|
| 卡头徽标读谁 | `SECTOR_RS.update_time` | `index.html` 内 `var ts = (rs && rs.update_time);` → `_uBadge('auto','盘后',ts)` |
| 个股明细来自谁 | `SECTOR_LEADERS` 快照 | 同函数 `var sl = window.SECTOR_LEADERS;`；快照 `update_time = 2026-09-19 04:32:27` |
| 接线状态 | cn_fetch **无** `soft_13b`（`fetch_sector_leaders.py` 在 yml 中出现 0 次） | `v8_cn_fetch_cloud.yml`（49201 B）grep 结果 |
| 后果 | 快照停更期间徽标仍显示板块数据的新鲜时间 | 两条时间戳并列即可看出 |

## 3 行为矩阵（夹具注入 + 对照，非「跑通」即过）

| 场景 | 期望 | 实测 |
|---|---|---|
| A 同日（真数据 `RS.data_date == SL.data_date == 2026-09-19`） | 不告警，且常显快照时间 | ✅ 无「早于板块数据」；`龙头股快照 2026-09-19 04:32:27` 出现 |
| B 快照滞后（夹具 `RS.data_date = 2026-09-21`） | 告警且带正确日期 + 琥珀色 `#f59e0b` | ✅ `早于板块数据 2026-09-21` |
| C `SECTOR_LEADERS` 缺失 | 不崩、不误告警、显示占位符 `—` | ✅ 三项全过 |

回归：两卡端到端计数仍 **主升 3 / 启动 39** 逐字一致、42 板块零遗漏；`align_logic_ops.py` EXIT=0；线上 CDN 抓取两件与推送字节**完全相同**（`index.html` 1316568 B md5 `41039a4409046b9e`）。

## 4 给你的操作注记（重要）

1. **插入点时序已核对，告警接线后会自动消失**：`algorithms/fetch_sector_rs.py` 位于 `id: soft_6` 内（yml L407）产出 `out/sector_rs.json`；我们准备的 `id: soft_13b` 落在 `soft_13`(L624) 之后、`soft_14`(L645) 之前 ⇒ **同一批次、同一 `data_date`** ⇒ 不会告警。
2. **若接线后仍天天见告警**：说明该步没真跑（被 `if:` 跳过 / 未落盘），请查该 run 内 `soft_13b` 的日志输出，而不是去改前端。
3. **`index.html` 今夜动了两次**（04:40 `fcc24a6dd6`、04:57 `ba90fa987d`）⇒ 推前端前务必先拉最新基线，不要用手上的旧副本。
4. 本批**未触碰**任何判定与计数逻辑：仍是单一真源 `window.__phaseOfSec` + 同一份 `SECTOR_RS`（定义 1 处 / 调用 2 处 / 内联残留 1 处）。

## 5 遗留（与 0510 交接件一致，未变）

- **P0-1**：应用 `docs/ops/patches/PATCH_2026-09-19_0500_cn_fetch_接线fetch_sector_leaders.md`（插入 `id: soft_13b`）。我的 PAT 无 `workflow` scope，改 workflow 必 403，只能由你侧落。
- **P0-2**：`v8_build_deploy.yml` L226 / L367 仍是「备份 → `git reset --hard FETCH_HEAD` → 盖回」，而 `if ! git diff --quiet -- index.html` 在该步（刚跑完 `update_v8.py` 重写 73+ 个 `?v=`）**恒为真**。今日 04:41 / 04:43 两笔自动化提交已实测**未**覆盖我的改动（归一化 `?v=`/`BUILD` 后 md5 相同），但机制仍在。
- **P1**：`fetch_sector_rs.py` 的 `data_date = 生成日`（同一天内两版 RS 有 96 个百分点以上板块 5 日涨幅漂移 >1 个百分点），涉 `SECTOR_PHASE_HISTORY` 快照键与 `v8_verify_layer_parity.py` 锚点，待与技术口径一起决策。

## 6 一行判据

```bash
curl -s "https://ah-quant999.github.io/quant-scanner-v8/?t=$(date +%s)" | grep -c '早于板块数据'   # 期望 1
```
