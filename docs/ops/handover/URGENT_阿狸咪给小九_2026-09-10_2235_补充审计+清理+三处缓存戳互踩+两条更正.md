# 🔴 紧急交接（补充）— 阿狸咪 → 小九

**签发**：阿狸咪（家机 alimi-cn） · **时间**：2026-09-10 22:35 CST
**主令**：主人令「确认盘前/盘中/盘后划分、确认盘后链一环套一环、全面审计、该删就删、不互踩不暴风不漏档、今晚必须全面过关、阿狸咪做不到的写交接单给小九」

> 本文与 21:55 那份（`..._2155_全链审计+盘后链13步简化+白天调度缺口.md`）配套，**以本文为准**（含两条更正）。

---

## 一、今晚阿狸咪实际做了什么（可核对，均已上 main）

| # | 动作 | 证据 |
|---|---|---|
| 1 | 盘后链 **19 步 → 13 步**，闸门成唯一决策点 | commit `5dd8d8bd0` |
| 2 | 修 4 个真缺陷：凌晨漏档 / 周末 T+1 被挡死 / 链尾假红灯 / 闸门崩溃假绿灯 | 同上 + `5f8c428e5` |
| 3 | 删 **4 个僵尸根目录 workflow**（`check.yml` `wf_algo_cloud.yml` `wf_cn_cloud.yml` `wf_live.yml`，8月27-30日旧快照，根目录 Actions 永不读、全仓 grep 零引用） | commit `b13d65180` |
| 4 | cron 注释改为诚实版：**cron 只是「唤醒点」，批次完全由闸门判** | 同上，L34-43 |
| 5 | 隔离 **`_tmp_zip_analysis` 等 99MB** 未跟踪临时物到会话隔离区（未删，可逆） | 本地 `C:\...\2026-08-28-21-48-18\_quarantine_20260910\` |
| 6 | 尝试取消卡死 32 天的僵尸 run `31312890017`（8月9日起 pending） | **失败：HTTP 409**，GitHub 侧孤儿记录，无法取消，无实际影响 |

---

## 二、🔴🔴 两条必须更正的事实（不要再用旧结论）

### 更正 1：家里机（alimi-cn）**已经能跑 `[self-hosted, cn]` 链了**

- 09-09 08:20 已静默安装 **Git 2.55 → `E:\Git`**，Machine PATH + User PATH 插入 `E:\Git\cmd`、`E:\Git\bin`，runner 重启后实测：`git=E:\Git\cmd\git.exe`、`bash=E:\Git\bin\bash.exe` **均可见**（日志 `D:\actions\cn-runner\_diag\git_env_fix_v2.log`）。
- 09-10 实测：`v8_lhb_fetch`（21:42 success）、`v8_stock_quote_refresh`、`v8_cn_fetch_cloud_selfhosted` 当日多条 **success**；本机 `Runner.Listener.exe` 现在在线（PID 9396，守护任务 `v8_cn_runner_guard` 运行中）。
- ✅ **结论：`runs-on: [self-hosted, cn]` 共享标签可以放心用，不要再加 `xiaojiu` 之类专属标签。** 双机都能接，谁的 runner 空闲谁接。
- ⚠️ 唯一注意：家里机 19:30 那段时间 runner 曾掉线（`guard.log` 显示 MISSING→自动拉起）。所以**任务必须幂等可重入**，这正是批次闸门的设计目标。

### 更正 2：GitHub cron 的可靠性是「按 workflow 各不相同」，不是全站 8%

- 实测（09-10）：`v8_algo_cloud` **前 4 档应触发 4 次，实际只触发 1 次**（21:07，21:00 那档延迟 7 分钟）。
- 但同一时段其他链的 schedule 有正常触发（全站近 2h 实测 9 次 vs 理论约 15 次）。
- ✅ **结论：cron 一律只当「彩票式备份唤醒」，绝不作为调度主力。** 真正主力 = 阿狸咪本机自动化「盘后算法链接力推进器」（每小时 :10，16:00-01:00）。你那边不要另建派发源。

---

## 三、盘前 / 盘中 / 盘后 权威划分表（照此核对，别凭印象）

| 时段 | 触发源（实测 cron） | 干什么 | 归属 |
|---|---|---|---|
| **盘前** 07:25-09:30 | 07:25 本机「开盘前防回滚自检」 | 防坚果云回滚/防线被冲 | 阿狸咪机 |
| | 08:25 `v8_cn_fetch_cloud` premarket | 盘前数据抓取 | 云端 ubuntu |
| | 08:30 / 09:30 周六 `v8_t1_guard` | 周六 T+1 兜底+巡检 | 云端 ubuntu |
| | 08:07 起每15分 `v8_cn_fetch_watchdog` | 抓取看门狗 | 云端 ubuntu |
| | 09:00 `v8_algo_intraday_lite`、`v8_risk_gauge`、`v8_stock_quote_refresh` | 盘中类首档 | 云端 ubuntu |
| **盘中** 09:30-15:00 | **09:40-15:00 每 20 分共 14 档 `v8_cn_fetch_intraday_lemoncat`** | 准点档调度器 → 派发 `cn_fetch_cloud` intraday | **小九 self-hosted cn（主力）** |
| | 09:00-15:50 每 10 分 `v8_intraday_snapshot` | 板块资金日内快照 | 云端 ubuntu |
| | 每 30 分 `v8_stock_quote_refresh` + `v8_risk_gauge` | 行情/风险温度计 | self-hosted cn / ubuntu |
| | 11:00 / 13:00 / 15:00 `v8_algo_intraday_lite` | 盘中算法追踪（轻量） | 云端 ubuntu |
| | 每 30 分 `v8_health_patrol`；每 15 分 `v8_cache_buster_reconcile` | 巡检 / 缓存戳对齐（全天） | 云端 ubuntu |
| **盘后** 15:05-次日01:00 | 16:30 `v8_cn_fetch_experiments` | 实验卡（暂未上架区） | 云端 ubuntu |
| | **16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 六个唤醒点 `v8_algo_cloud`** | **跑哪批由闸门定：A→B→D→E** | **`[self-hosted, cn]` 双机谁空谁跑** |
| | 17:20 / 18:20 / 19:20 `v8_cn_fetch_cloud` post_close | 盘后三档抓取 | 云端 ubuntu |
| | 17:30 `v8_lhb_fetch` | 龙虎榜 | self-hosted cn |
| | 20:30 `v8_cn_fetch_cloud_hosted`；22:30 `v8_daily_audit`；23:00 `v8_backup` | 兜底抓/审计/备份 | 云端 ubuntu |
| | 每小时 :10（16:00-01:00）本机「接力推进器」 | **唯一派发源** | 阿狸咪机 |

---

## 四、🔴 明早（09-11）你必须做的 4 件事

1. **核对今晚盘后链是否真跑完 D/E**（只看文件内 `update_time`，**不看 mtime**、**不看 run 的 success/failure**）：
   - D 批 → `data/FINAL_RECOMMEND_DATA.js`、`data/CRDS_CARD_DATA.js`、`data/TRIPLE_CONSENSUS.js`
   - E 批 → `data/CRDS_BACKTEST.js`、`data/BACKTEST_TDX.js`
   - 判据：`update_time` **≥ 2026-09-10 16:30**（凌晨 00:00-05:59 的产物按「前一自然日 24:xx」计）
2. **绝对不要重跑 A 批**。已到手的 `data/LHB_DATA.js` 是 09-10 18:29 的新鲜好数据，重跑 A 会把它冲掉。
3. **派发一律不带 `stage`、不带 `force_run`**，让闸门自己判。带 `stage` 会跳过顺序闸门 → 抢跑。
4. **自查你自己机上的 WorkBuddy 自动化清单**：任何会 `dispatch` 盘后算法链的任务都必须停掉（我这边已收敛为「接力推进器」单一派发源）。我不能替你查你那台。

---

## 五、发现但**今晚未改**（需你确认后再动，勿擅改）

### 🔴 三个缓存戳写入者互踩（今日实测证据）

| 写入者 | 位置 | 触发 |
|---|---|---|
| `v8-cache-bot` | `v8_cache_buster_reconcile.yml` | cron `*/15` + push 到 `data/**` `raw_data/**` |
| `v8-cloud-bot` | `v8_cn_fetch_cloud.yml` L626（hosted L369 / selfhosted L443 同款） | 抓取链收官时「生成器原子提交 缓存戳」 |
| `v8-cloud-bot` | `v8_build_deploy.yml` → `update_v8.py` 也重算缓存戳 | 每次 build |

**实证代价**：09-10 21:24-21:27 出现 **6 条同消息重复提交**（互踩 → push 422 重试风暴），近 2 小时 **58 次 Pages 构建**。
**建议**：收敛为 **1 个权威写入者**（建议保留 `v8_cache_buster_reconcile` 作唯一缓存戳权威，其余两处改为「读现有戳、不重写」）。**这条改动影响面大，等你确认后再动。**

### 🟡 其它待清理
- 仓库 `.git` 已 **968MB**（历史体积），根目录遗留已清 4 个；如需瘦身须重写历史，风险高，暂不动。
- 本机 untracked `algorithms/_valid_codes.json` 长期挂在 `git status` 里，无引用，待你确认后清。

---

## 六、一句话总结

盘后链「一环套一环」的新逻辑**是对的**：唯一闸门 → 顺序铁律 A→B→D→E → 上游未就绪则红灯拒绝+紧急报告 → 全就绪则 `NONE` 空转（真成功）。今晚 19→13 步化简、4 缺陷修复、僵尸清理都已上 main（`b13d65180`）。剩下的**唯一大风险不是逻辑，是 GitHub cron 不可靠** —— 所以调度主力必须压在本机接力推进器上，你只做「盯 + 复核」，不要再加派发源。
