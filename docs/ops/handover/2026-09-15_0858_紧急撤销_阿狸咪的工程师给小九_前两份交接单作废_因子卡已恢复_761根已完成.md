# 【紧急撤销】前两份交接单作废 —— 因子卡已恢复、761 根已完成

- **发件**：阿狸咪的工程师（家用机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-15 08:58（开盘前）
- **优先级**：🔴 最高 —— **请立刻停止前两份单里的任何动作**

---

## 一、请立即停止的两件事

| 前序交接单 | 原有指令 | 现状态 |
|---|---|---|
| `2026-09-15_0735_..._因子卡撤除与761根累积_开盘前执行.md` | 撤除「因子补缺与审计」卡 + 补 761 根 | 🔴 **作废** |
| `2026-09-15_0842_紧急_..._因子卡已撤除_761根累积待盘中后执行.md` | 盘中/收盘后补 761 根 | 🔴 **作废** |

**两项都已由我本人完成并上线，请勿重复执行；尤其请勿再执行「撤除因子卡」——那是一次误解，卡已恢复。**

---

## 二、事情经过（诚实交代）

1. 主人 06:35 令「不好就删」，我**理解错了对象**：把「卡内回测不达标的因子条目」误读成
   「整张卡」，于是把整卡（含生成器、产物、前端、逻辑页注册、调度、workflow input）一并撤除。
2. 主人 07:10 当场纠正：「我不是叫你删整个卡，是这卡里面准备跟踪的因子你不是说回测不好，
   这种的才删除，不是还有很多还没追加的么？这样才知道进度和备忘。」
3. 主人 07:33 进一步澄清：「是你准备回测可以追加的因子，准备有空再做的那种，都写在里面，
   一旦回测新因子不行再删。」

⇒ 该卡的**真实用途**是「**待回测候选因子台账**」（进度 + 备忘）：准备回测的因子先登记，
   回测达标则接入，不达标才从台账删掉那一条。

---

## 三、已完成（均已推送 main 并硬验证）

### ① 整卡恢复 —— `32e9484fe760`（Revert）
- 删前版逐字节恢复 6 个文件：`scripts/gen_factor_audit.py`、`scripts/gen_factor_progress.py`、
  `data/FACTOR_AUDIT.js`、`data/FACTOR_PROGRESS.js`、`raw_data/factor_audit.json`、
  `raw_data/factor_progress.json`；
  另恢复 `index.html`（卡容器 + `__renderFactorAudit` 全函数 + 3 处调用 + 2 个 script 标签）、
  `logic.html`（调度表注册行 + 数据源清单项）、`update_v8.py`、`algorithms/run_algorithms.py`、
  `.github/scripts/dedup_fetch_manifest.py`、`.github/workflows/v8_cn_fetch_experiments.yml`。
- **校验**：六文件树级 blob 与删前版**逐字节一致**；`index.html` 27 段 inline script 0 错；
  `logic.html` CRLF=8656 保持（未重演行尾事故）。
- 注：`index.html` 以删前版为底，并把 `?v=` 缓存戳对齐到当时 tip，**未回退缓存戳**。

### ② 候选因子台账登记 —— `b2f7f12d1992`（7 → 16 项）
在 `gen_factor_progress.py` 新增 `CANDIDATE_POOL`（9 项）与台账规则，清单由 7 → **16 项**：
- **A 组·数据就绪（`kline_cache` 已入仓，本机即可回测）**：
  12-1 跨周期动量 / 残差动量 / 最大日收益 MAX / 特质波动率 IVOL / 换手率趋势(20d/240d) / 隔夜跳空累积
- **B 组·需扩财报字段**：Piotroski F-Score / 应计质量 Accruals / 资产周转率

台账规则：①先登记才允许进 `generate_top10.py`；②达标（OOS IR>0.3 且 4/5 年胜率>55%）改 done；
③**不达标即从台账删除该条**；④每条在 `data_source` 写明「离能开跑还差什么」。
前端**零改动**（批次值仍为 1/2/3，现有 `batchBars[1,2,3]` 与 `factors.map` 自动覆盖）。

### ③ 761 根 + 「累积中」 —— `4298d0cffa10`
- 后端 `algorithms/factor_lab_backtest.py`：
  新增 `MIN_BARS = NEED_MIN + 2×max(HOLDS) = 761`；`FETCH_DAYS 700 → 800`；
  `_layer_stats(..., holds)` 对缺数据档**显式输出** `n_{h}d=0` 且 `win/avg/best/worst=null`
  （旧行为是整档消失，前端动态发现不到 ⇒ 主人看不到「这档还没数据」）；
  新增 `_hold_status()`；`main()` 输出 `data_accumulation` 累积进度账。
- 前端 `index.html` 档位表：无样本且 `status=accumulating` ⇒ 样本列显示「**累积中**」，
  表头徽章「⏳ N/M 档累积中（需 761 根·当前最长 X 根）」。
- **自查**：以 400 只正常（900 根）+ 60 只次新（300 根）自测，正确判出 20d 起
  `need_bars=301 > 300` ⇒ 可用票降 400/460、无样本档标 `accumulating`。

---

## 四、给小九的唯一请求

**什么都不用做。** 若已开始执行 0735 / 0842 单中的任何步骤，请：
1. **立即停止**，勿推任何相关提交；
2. 若已产生分支/临时文件，丢弃即可（我这边已完整覆盖）；
3. 若已推送，请在回执里告知 commit，我这边对齐。

---

## 五、遗留（不属本次撤销范围）

- `algorithms/factor_lab_backtest.py` 依赖 `raw_data/_rps_cache/`（`.gitignore` 内、不入仓）⇒
  云端 `git clean -ffdx` 后该目录不存在，脚本会 `sys.exit(1)`。**「跨日累积」是否需要把
  缓存入仓，涉及主人 2026-09-08「仅 kline_cache 一项」的令 —— 需另行请示主人扩令，
  小九与我均不得擅自扩。**

---

署：**阿狸咪的工程师**
