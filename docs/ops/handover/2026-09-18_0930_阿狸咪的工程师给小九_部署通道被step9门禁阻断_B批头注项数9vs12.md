# HANDOVER 阿狸咪的工程师 → 小九的工程师 · 2026-09-18 09:30（北京时间）

## 主题：🔴 P0 新 · `build_deploy` 自 **08:38 CST 起全链被 step9 门禁阻断**（B 批头注项数 `9 != 真源 12`）

> 发起：阿狸咪（夜班/周末班；本档系白班窗口内的**只读诊断 + 交接**，未改任何代码）
> 采集时刻：2026-09-18 09:2x~09:31（北京时间）
> 采集时真 tip：`f7a67e638e0846b6ee6e726cfcdef822fc8601ad`（`docs(handover): 小九回执 · 未回执1档逐项对齐…`，01:20:35Z）
> 署名：阿狸咪的工程师

---

## 0. TL;DR

| # | 事项 | 实测判据 | 状态 |
|---|------|---------|------|
| **1** | **门禁阻断部署通道** | `build_deploy` 最近 5 次「真跑构建」的 run **全在 step9 失败**（#6929/#6930/#6932/#6933/#6934） | 🔴 **P0 新** |
| **2** | 红的**起点**精确到提交 | 最后一次 build 真跑成功 = `#6928`（**07:56:59 CST**）；首个红 = `#6929`（**08:38:00 CST**） | ✅ 已定 |
| **3** | 致红提交 | `3bd6da8969`（**08:37:47 CST**，提交自身注释署名「2026-09-18 **小九**（双盲区根治·P0）」）⇒ **50 秒后** #6929 即红 | ✅ 因果链闭合 |
| **4** | **根因** | `v8_stage_gate.py` **L50 头注**写「B 选股批：**9 项**产物中 ≥8 项鲜活」，而真源 `READY_SPEC["B"]["items"]` 已 = **12 项** | ✅ 一字之差 |
| **5** | **修法** | 只改 L50 一处：`9 项` → `12 项`（门槛 `≥8` 与真源 `need=8` **已一致，无需改**） | ✅ 精确 |
| **6** | 连带后果（**讽刺点**） | 你同提交新增的 **step10「双盲区静态校验」排在红步之后 ⇒ 自上线起一次都没执行过** | 🔴 未交火 |
| **7** | 连带后果（次生） | step11「部署前校验 data 非空」、step12「📤 部署到 main」同期**全部不执行** | 🔴 通道断 |
| **8** | 线上是否停更 | **否**。其他链（cn_fetch / algo / cache-bot）各自 push main ⇒ Pages 内容仍在更新；缺的是 **build 机这条通道**（selective build 产物 + `?v` 对齐 + qv 自愈） | ⚠️ 待你判 |
| **9** | **回答你 §5.3 / O-4** | `backup-*` 分支**在远端不存在**（`git ls-remote` + branches API 双口径）⇒ **我方 0830 档「8 月账本挂在 backup-* 分支」表述有误，本档自纠**；扩样 P0 须**降级** | ✅ 结案 |
| **10** | 长期 P0 双向确认（好消息） | 四量 **`1447 / 近5年 / 07:11:01`** ✅ 已脱 3 年档；`BACKTEST_TDX.js` **10,280 B / 06:57:48**、`raw_data/backtest_tdx.json` **10,930 B**（自 76 MB 瘦身）✅；`v8_stage_gate.py` 远端 **CR=0 / LF=875** ✅ ws-guard 行尾死锁治本 | ✅ 三项闭环 |

---

## 1. 现场证据（全部可复现，只读）

### 1.1 run 级：build job 明细（不是只看 run 结论）

| run | 结论 | gate job | **build job** | 时间（CST） |
|---|---|---|---|---|
| #6935 | success | success | **skipped**（⇒ 此 success 为**假成功**，未构建/未部署） | 09:28 |
| #6934 | failure | success | **failure @step9** | 09:16 |
| #6933 | failure | success | **failure @step9** | 09:16 |
| #6932 | failure | success | **failure @step9** | 09:00 |
| #6931 | cancelled | cancelled | cancelled（并发顶替） | 09:00 |
| #6930 | failure | success | **failure @step9** | 08:52 |
| #6929 | failure | success | **failure @step9** | **08:38 ← 首个红** |
| **#6928** | **success** | success | **success ← 最后一次真跑成功的构建+部署** | **07:56** |
| #6925/#6924/#6923/#6922 | success | success | success | 07:50 / 07:28 / 07:16 / 06:52 |

⇒ **`build_deploy` 的「构建 + 部署」自 07:56 之后，再也没有成功跑过一次**（#6935 的 success 是 build skipped）。

### 1.2 step 级日志原文（run #6934 / job id `35294678362`）

```
v8 pre-deploy audit（CI 自动门禁，2026-09-05 启用；2026-09-11 扩至 5 项；2026-09-13 扩至 6 项；2026-09-14 扩至 8 项）
  ✅ [1/8] py_compile: 187 个 .py 文件 0 错误
  ✅ [2/8] new Function: 28 个 inline script 块 0 错误
  ✅ [3/8] data 完整性: data/*.js 数量=109, 全部 > 100B
  ✅ [4/8] align_logic_ops: EXIT 0
  ✅ [5/8] workflow YAML: 30 个 workflow 全部有效
  ✅ [6/8] HTML 数据引用: 178 个本地 script 引用全部存在
  ❌ [7/8] gate 头注一致: B 选股批 头注项数 9 != 真源 12
  ✅ [8/8] 心跳产物名一致
🚫 1 项校验失败 → 阻断 deploy！
```

⇒ **8 项里只红 1 项，且是纯文档一致性项**（不影响运行时裁决），却把整条部署通道掐了 53 分钟。

### 1.3 判据源码（`.github/scripts/pre_deploy_audit.py::check_gate_headnote`，L448-469）

```python
for ln in head.splitlines():
    if ln.strip().startswith(label):        # label = "B 选股批"
        head_line = ln; break
# (1) 项数
m = re.search(r"(\d+)\s*项", head_line)     # ← head_line 里第一个「N 项」= "9 项"
if m and int(m.group(1)) != len(items):     # len(items) = 12
    fails.append("%s 头注项数 %s != 真源 %d" % (label, m.group(1), len(items)))
# (2) 门槛
m = re.search(r"[≥>=]+\s*(\d+)\s*项", head_line)   # ← 匹配 "≥8 项"
if m and need is not None and int(m.group(1)) != need:   # need = 8 ⇒ 通过
```

### 1.4 涉事两处（远端 `main` 逐字）

`v8_stage_gate.py` **L50（头注，需改）**：
```
    B 选股批：9 项产物中 ≥8 项鲜活 + 三重共识/四量终极/逆势龙头（must）必新  ← 以 READY_SPEC["B"] 为唯一真源
```

`v8_stage_gate.py` **L215-264（真源，`3bd6da8969` 已扩至 12 项）**：
```
"items": [ TRIPLE_CONSENSUS / FOUR_VOLUME / CRDS_CARD_DATA / ai_insights_compare /
           factor_audit / factor_progress / valuation_percentile / index_value_framework /
           gold_pool / FOUR_VOLUME_60M / FOUR_VOLUME_HISTORY / FOUR_VOLUME_TRACK ]   ← 12 项
"need": 8
```
> ⚠️ 同处 L251 注释仍写「**10 项**容忍 2 项不新鲜」—— 加 2 项后该注释也**已过时**（应为「12 项容忍 4 项」）。**不阻塞门禁**（门禁只比对头注那行），但建议一并订正，免得下一轮又被它误导。

### 1.5 致红提交 `3bd6da8969`

```
author/committer: ah-quant999   2026-09-18T00:37:47Z (= 08:37:47 CST)
msg: fix(double-blind): register FOUR_VOLUME_60M/HISTORY/TRACK in stage gate + ALWAYS_PUSH, add static guard
files:
  .github/scripts/v8_stage_gate.py            +23 -0     ← 扩了 items，未同步头注
  .github/scripts/guard_double_blind.py      +294 -0
  .github/workflows/v8_build_deploy.yml       +10 -0     ← 新增 step10 双盲区静态校验
  .github/scripts/dedup_fetch_manifest.py     +10 -0
  data/FOUR_VOLUME_60M.js                    +177 -61
```

> 归因口径说明：`author` 字段是账号 `ah-quant999`（**两机共用同一 PAT ⇒ actor 不可区分**）。
> 但**该提交自身携带的注释署名**为「2026-09-18 **小九**（双盲区根治·P0 · 事故家族第三次复发）」（见 commit body 与 `v8_build_deploy.yml` L167-172）⇒ 我按此归因**小九侧**，如需更正则请你自证。

---

## 2. 影响面（精确到 step）

`v8_build_deploy.yml` 门禁之后的 step 序列：

| step | 名称 | 本次是否执行 |
|---|---|---|
| 9 | 🛡️ Pre-deploy audit | ❌ **失败点** |
| 10 | 🛡️ 护栏：双盲区静态校验（`guard_double_blind.py`，**你本轮新加**） | ⛔ **不执行** ⇒ 上线至今 0 次交火 |
| 11 | 🛡️ 部署前校验：关键 `data/*.js` 非空（`v8_deploy_guard.py`） | ⛔ 不执行 |
| 12 | 📤 部署到 main（selective build 产物 + `?v` 对齐 + qv 自愈） | ⛔ 不执行 |

**净效果**：你为「封死双盲区第四次复发」新立的护栏，被**排在自己会红的步之后**，等于**未上线**；同时 build 机这条部署通道整体停摆。

**未受影响**：主站内容仍在更新（`index.html` 实测 1,279,837 B、tip 已到 `f7a67e638e`），因为其他链直接 push `main`。⇒ 所以**没有立刻的站面事故，但通道断着**。

---

## 3. 修法（一行，低风险）

```diff
-    B 选股批：9 项产物中 ≥8 项鲜活 + 三重共识/四量终极/逆势龙头（must）必新  ← 以 READY_SPEC["B"] 为唯一真源
+    B 选股批：12 项产物中 ≥8 项鲜活 + 三重共识/四量终极/逆势龙头（must）必新  ← 以 READY_SPEC["B"] 为唯一真源
```

- **只改数字 `9 → 12`**；`≥8` 与真源 `need=8` 一致，**不要动**（动了会触发 (2) 门槛 FAIL）。
- 正则取的是 head_line 里**第一个**「N 项」⇒ 改后第一个匹配 = `12` ✓。
- 顺带建议（非阻塞）：同步订正 L251 注释「10 项容忍 2 项」→「12 项容忍 4 项」。
- **白班窗口**：这是你刚推的代码、属你侧文件，我**未改**（双机分时铁律：07:45-17:45 你独家）⇒ 请你落。若你希望我夜间落，回一句即可。

---

## 4. 回答你 §5.3 / O-4：`backup-*` 分支取证（**结案**）

```bash
$ git ls-remote origin 'refs/heads/backup-*'
（无输出）
$ git ls-remote --heads origin
9a899c8f81…  refs/heads/feat/v8-detach-v6
8ad539cdbf…  refs/heads/fix/block3-rename
e674fdd04d…  refs/heads/fix/lemoncat-sleep600
f7a67e638e…  refs/heads/main
```
branches API 二次确认：**仅上述 4 个分支**。

⇒ **结论**：
1. **远端不存在任何 `backup-*` 分支** ⇒ 你 §3.1 的更正**成立**；
2. **我方 0830 档「本机是浅克隆，8 月账本只挂在 backup-* 分支上，需要时我拉」= 事实错误**，本档**公开自纠**（我本轮才开始验证，此前系沿用旧判断，未核即写，属我的失误）；
3. 按你 §5.3 的判定规则 ⇒ **融合器扩样 P0 必须降级**，「扩样数据源从哪来」上升为**待主人拍板**（并入你 拍-A）。
4. 我侧同样**无** 09-03 之前的 `all_candidates` 落盘。

---

## 5. 顺带确认（本轮云复核，均为好消息）

| 项 | 实测 | 判定 |
|---|---|---|
| `data/FOUR_VOLUME_BACKTEST.js` | `total_signals=1447` / `signal_date_range="近 5 年"` / `update_time=2026-09-18 07:11:01` | ✅ **已脱 3 年降级档**（07:11 那次 algo 落盘未再降级） |
| `data/BACKTEST_TDX.js` | **10,280 B** / `update_time=2026-09-18 06:57:48` / `gold_pool_size=221` | ✅ 脱离停更 |
| `raw_data/backtest_tdx.json` | **10,930 B**（原 76,078,046 B） | ✅ 瘦身落地 |
| `.github/scripts/v8_stage_gate.py` | **CR=0 / LF=875 / blob `0412621d87`** | ✅ ws-guard 行尾死锁**治本**（不再是 CRLF 脏 blob） |
| `v8_ws_sync_guard.py` 白名单 | 两条 404 幽灵路径**已更正**为 `scripts/build_unlisted_panel.py` / `v8_t1_guard.py` | ✅ 已落盘 |
| `v8_ws_sync_guard.py::REPO` | 仍 `r"E:\workspace\stock-scanner"` | ⏸ 你 拍-D 仍挂着（非阻塞） |
| algo 链 | `#1937` success（07:56）、`#1936` success（06:46 行尾归正）、`#1938` success | ✅ 自 09-17 20:02 的断产已恢复 |

---

## 6. 边界声明（本轮**未**做什么）

| 类别 | 计数 |
|---|---|
| 本机 `git commit` | **0** |
| Contents API PUT（本档除外） | **0** |
| 派发 / 取消 run | **0** / **0** |
| 改动代码 / 阈值 / 权重 | **0** |
| 手工改数 | **0** |
| 触碰 `index.html` / `logic.html` / `data/**` / `raw_data/**` / `.github/workflows/**` | **0** |
| 本机 `git reset` / `checkout` | **0** |

诊断脚本置于**仓库外** `E:\workspace\_alimi_check_20260918\`（`read_docs.py` / `diag_gate7.py` / `diag_origin.py`），不进仓、不被同步。

---

*本交接件仅供研究参考，不构成个人投资建议。*

**阿狸咪的工程师** · 2026-09-18 09:30（北京时间）
