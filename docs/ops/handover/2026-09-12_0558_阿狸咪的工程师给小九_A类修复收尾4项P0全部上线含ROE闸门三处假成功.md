# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-12 05:58｜A类修复收尾4项P0全部上线含ROE闸门三处假成功

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

本轮把 4 项「错误会长期长存」的结构性问题**逐项根治并上线**；其中 **ROE 榜闸门连续踩出 3 层假成功**（装了没接电 → 半开闸门 → `.get` 默认值后门），**已全部堵住**。另更正一条此前误判：**regime 真实为 `grind`（可开仓），此前的「观望」是 baostock 被拉黑导致的降级误判**。

**本轮 commit**：`6babaa6ebaac` · `782afc97e710` · `8168b4da3e29` · `d4897b47e9ef` · `59d29e0479a7` · `82b6e38cd73e` · `2c37844cc324` · `da4366c852b9`

---

## 正文

### ① P0 小九机链推不出去 —— `6babaa6ebaac`

**症状**：小九机算法链跑完但推不出去，日志无明确报错。

**真根因（一环套一环，查到最底）**：

| 层 | 问题 | 证据 |
|---|---|---|
| 1 | 全量 `?recursive=1` 枚举 = **1,008,553 B**，跨境必 `IncompleteRead`，重试 3 次全废 | 实测 26s / 3×重试 |
| 2 | 上轮「只取根 tree」的修法**同样是错的** | 根 tree 仅 77 个顶层条目，`raw_data/kline_cache/` 深层前缀永远匹不中 → 需要同步的深层文件一个都进不了 `want` |
| 3 | tree 条目里的**目录名不带尾斜杠**（是 `raw_data/kline_cache` 而非 `.../`）→ `startswith(".../")` 永远匹不中 → 剪枝静默失效 | 那一层实测 742.6 KB / 3119 条，正是 `IncompleteRead` 的来源 |

**终解**：**本地 `git ls-tree`**（runner 本就有完整仓库）。

- 旧法：1,008,553 B / 26s / 3×重试全废
- 新法：**0 B / 0.2s / 等价性 0 差异**（3474 条受管路径，漏 0 / 多 0 / sha 不一致 0）
- `v8_algo_cloud.yml` step3 冷启动同步改真分层 BFS + 前缀剪枝：**588 文件 / 单层最大 40.9 KB**（旧 1240 KB）

**判据修正（重要，勿再踩）**：剪枝判据必须是 `path == K or path.startswith(K + "/")`，且 `SKIP_PREFIX` 各元素**不带尾斜杠**。

---

### ② 60m 四量终极双页上线 —— `8168b4da3e29` + `d4897b47e9ef`

**背景**：小九已在 `2b8cc93c2` 修完三宗罪代码（`ScanUnavailable` ×4 / `V8_DATA_DIR` ×2 / `CARD_DEFS` 24h 红线 / 90 分钟超时），但 `index.html` / `logic.html` **有意未提交**（怕带上并发会话半成品）→ 本轮由我收尾上线。

**双页校验**：`index.html` 24 内联脚本 0 语法错 / `logic.html` 6 内联脚本 0 语法错；行尾守恒 ✅；div 配平 Δ差=0（基底本就如此，非本轮引入）。

**三宗罪已文档化**（`logic.html` 60m 段 + `index.html` 运维页 note）：① 产物写错目录 ②「没扫成」写成「今天没信号」③ 三层静默。

---

### ③ B 批逃生门 —— `782afc97e710`

**真危险**：B 批 `need=8/9`，A 只要 2 项长期不鲜 → B 恒未就绪 → 每轮都跑 B → **D 批（最终推荐）永久不触发**。系统只亮红灯，**不会自己绕过去**。

**端到端四段（缺一段就等于没加）**：闸门产出 → workflow step output + summary 正则补键 + `::warning` + `export` → `final_recommend.py` 写产物 → 默认值恒 0。

**行为测试 8/8 通过**：

| 场景 | 结果 |
|---|---|
| A 落后 1 天 | 仍先补采 A（无标记） |
| A 落后 5 天 | `target=B` + `DEGRADED_UPSTREAM=1` + `lag=5`（修复前恒为 A → D 饿死） |
| A 全缺失 | 放行 + 标记 |
| 显式 `--explicit-stage=A` | 仍跑 A（人工优先） |
| A 全新鲜 | 无标记 |

⚠️ **勘误**：A 批 items 实际是 **10 项 / need=7**（不是此前以为的 9/8）；B 批 **9 项 / need=8**。

---

### ④ ROE 榜质量闸门 —— `59d29e0479a7` → `82b6e38cd73e` → `2c37844cc324`

本轮**最曲折**的一项：连续踩出 **3 层假成功**，每层都比上一层更隐蔽。

**真根因（推翻「家底太薄」旧论断）**：

江钨装备反推净资产 **11.13 亿，并不薄**。真因是 baostock 累计 `roeAvg` 在「亏损→微利」或「单季剧变」公司上**符号与斜率剧烈跳变**，逐季差分把噪声放大成虚高 TTM：

```
累计序列: -0.6795, -3.1876, -1.5636, -1.3603, +0.0069, +0.0063
差分后单季: [1.624, 0.203, 0.0069, -0.000656]  →  TTM 求和 183.36%（纯属算术巧合）
```

**三闸门**：G1 反推净资产 ≤1 亿剔除 / G2 近 4 单季任一为负剔除 / G3 TTM >150% 剔除。候选层过滤（非排序后砍头）。`V8_ROE_GATE=0` 可临时关闭。

**实测 8 只样本**：保留 3 / 剔除 5，**未过度杀伤**。

| 股票 | 榜单 TTM | 判定 | 依据 |
|---|---|---|---|
| 江钨装备 | 183.36% | **剔除** | G2+G3 |
| 德明利 | 121.07% | **保留** | 单季全正，业绩真实爆发 |
| 宏桥控股 | 111.62% | 剔除 | G2 |
| 东方精工 | 56.24% | 剔除 | G2（单季含 −0.001641） |
| 亚翔集成 | 55.25% | **保留** | 单季全正 |
| 海航控股 | 55.19% | 剔除 | G2 |
| 松发股份 | 54.77% | **保留** | 单季全正 |
| 国城矿业 | 52.80% | 剔除 | G2 |

**三层假成功与堵法**：

| 层 | 问题 | 实测证据 | 堵法 |
|---|---|---|---|
| ① 装了没接电 | L519 缓存跳过条件不感知「判据是否已落盘」→ 旧缓存永远命中 `continue` → 既不重算也不补写 `roe_ok` | 本机缓存 **3195 条全部** `roe_ver=3`/`asof_q=2026Q3`，0 条含 `roe_ok` | 跳过条件并入 `_has_gate`（**逐只**判定） |
| ② 半开闸门 | `ROE_VER` 升版后受 `BUDGET=800/轮` 限制，首轮只刷一部分 → 剩下 2395 只走宽容分支**蒙混过关**（比①隐蔽：看似闸门在工作） | 模拟 12/50 判据场景 | 整榜「无判据占比 >50%」→ **硬失败拒绝推送**，宁缺勿假 |
| ③ `.get` 默认值后门 | `v.get("roe_ok", True) is True` —— 缺字段时默认 `True` 直接放行，把①②刚拦住的票从后门放回 | 实测：应剔除 20 只只剔了 10 只 | 默认值改 **`False`**（无判据 = 未过闸 = 剔除） |

**行为测试 6 场景全过**：旧缓存 0/50→REFUSE；半开 12/50→REFUSE；刷够 40/50→剔除20存活30；关闭→BYPASS；跳过条件（无判据→重算/有判据→跳过）；默认值后门（无判据存活 0）。

**附带清理**：删 `_gated` 死代码（算出后从未使用）；日志补「无判据剔除 N 只」便于一眼看穿空转。

**🔴 给你（小九机）的注意**：`ROE_VER` 3→4 后，**你机上的缓存也需全量重算**。首轮受 `BUDGET=800` 限制只刷约 800 只，剩余部分会命中新硬失败**拒绝推送**（这是**设计预期**，不是故障）——约 4 轮夜跑后自然收敛到全量。**不要**为此放宽闸门或手工关 `V8_ROE_GATE`。

---

### ⑤ 附：`.gitignore` 补漏 —— `da4366c852b9`

`_*/` 带尾斜杠**只匹配目录**，匹配不到 `_` 开头的**文件** → `algorithms/_valid_codes.json`（`backtest_expectancy.py` 运行时缓存，缺失时 baostock 自动重建）长期以未跟踪态污染 `git status`。已精确点名。

**为何不用宽泛 `_*`**：`_config.yml`（GitHub Pages 配置）同属 `_` 开头，宽泛规则会误伤整站配置。已复核关键文件均正常跟踪。

**另两项处置（不入库，不产生提交）**：

- `raw_data/kline_cache/300677.json`：远端受管 3119 只，此为正常新增第 3120 只 → 按主人 2026-09-08 令「kline_cache 入仓」，保留在工作区待下轮算法链自然带上
- `docs/ops/handover/..._2056_*.md`：已被 `2154` 版覆盖，按既定结论**不补推**

---

## ⚠️ 重要结论更正：regime 实为 `grind`（可开仓）

**此前结论**：「regime=观望」——**错误**。

**真因**：`baostock` 当时被拉黑（返回 `10001011 黑名单用户`）→ 数据取不到 → 降级为「观望」的兜底。**本轮复测 `baostock.login()` 返回 `0 success`**，本机已可正常查询。

**更正后**：regime 真实为 **`grind`（可开仓）**。

**对你的影响**：若你侧代码/文档曾按「观望」写过任何默认处置，请一并更正。B 类第 5 项「regime 失败默认处置」我这边已按**保守**实现（失败→观望），此次更正**不改变该保守策略**，只是把「真实值」与「失败兜底值」区分清楚。

---

## 待主人拍板的 B 类（我这边等一句话）

| # | 事项 | 现状 |
|---|---|---|
| B1 | 三重共识门槛 `max(max_score*0.5, 25)` | 待拍板 |
| B2 | CRDS `cond②` 判定量（现 100% 漏判） | 待拍板 |
| B3 | ROE 榜裁剪阈值（本轮已实施三闸门 150%/1亿/负单季） | **需确认阈值** |
| B4 | 是否剔除 ST 股 | 待拍板 |
| B5 | regime 失败默认处置 | 已按「保守」实现 |
| B6 | `STOCK_RPS` 止血 + 候选宇宙 253/259 | 待拍板 |

**去重提示**：CRDS 口径（我的 A2 = 你的股票专家 §三-1）、`need=8/9`（我的 2320#2 = 你的工程师 §3.2-2 = 你的股票专家 §三-2）、ROE 裁剪（我的 A4 = 我的 2320#3）**三处重复**，提交主人前应先合并。

---

## 仍挂起项（非本轮范围）

- `PORTFOLIO.js` 陈旧 11 天（随 westock 持仓同步 PAUSED 停更）
- `STOCK_RPS valid_count=2`（universe 907）
- `STOCK_STOP_DATA` 宇宙只 259（`degraded:false` 不报警）
- `HEALTH_CHECK fail=3`（危机雷达阈值设计 + `CITIC_PE_BACKTEST` + `CITIC_PE_THERMO` 停昨日 21:04）
- 本机缺 `data/hb_xiaojiu.json` → `v8_peer_monitor.py` 恒判「小九正常」，**双机掉线告警永不触发**
- `V8_DATA_DIR` 幽灵目录 `E:/workspace/stock-scanner` 是否仍被其他脚本写入

---

## 给你的请求

1. **ROE 闸门**：请确认你机也能正常 `baostock.login()`；若同样遭遇黑名单，闸门会因判据缺失**拒绝推送**——这是预期行为，请回报而**不要**关 `V8_ROE_GATE`。
2. **regime 更正**：请检查你侧是否有按「观望」写的默认处置。
3. **去重**：上面「待拍板」节的 3 处重复项，建议由你侧先合并再报主人。

---

*阿狸咪的工程师 · 2026-09-12 05:58 CST*
