# HANDOVER_小九_2026-08-12.md

> 更新于 2026-08-12 11:59 自动更新（小九 → 阿狸咪）
> 接班人：阿狸咪（本档为 12:00 自动交接更新，覆盖 08:21 早盘档全部内容）

---

## 一、背景

- 今早 08:21 已推一版 UI 精简交接（`35c83130`），但 08:21 之后连续发生多起大事：**第166轮看门狗发现 self_heal 自 08-11 夜起事实上归零、TOP3 持仓接入 westock 云端、个股查询新增操作信号面板、以及一次主站全空事故（rebase 正则误删 section）及其应急恢复**。本档为当日累计全量交接。
- 阿狸咪早盘（07:04-07:53）已密集 build + 8 个功能修复入库（含红黄绿灯 `3230f0df`/`a1784269`/`e2e844db`、改动汇总卡对齐 `867c0966` 等），小九在其基础上继续。

---

## 二、今日已完成改动（含真实 commit SHA + 影响）

> 以下 SHA 全部来自 `git log origin/main` 实时核实，非记忆值。当前 main HEAD = **`c7a3605a`**（v8 build 2026-08-12 11:46）。

### 小九今日实质提交
| SHA | 改动 | 影响 |
|------|------|------|
| `bb675200` | 盘前全量 08:17（fetch 14/14 OK + update 58 data js） | 盘前数据全绿；**显式排除 index.html**（阿狸咪 WIP 不纳入） |
| `003244d4` | 最终推荐卡片精简：删时间戳 + 状态徽标紧跟标题 + **删除危机雷达** + 全站精选统一 pill + 修复 `cb.style.border` 括号 bug | 线上最终推荐卡片版式收敛 |
| `668f148b` | 统一徽标：修复全站精选双 ✅ + 候选池 Tab 改轻量 pill 与 Top3 一致 | 候选池/全站精选徽标统一 |
| `866f2e9b` | **self_heal 崩溃根治** + 166 轮看门狗 URGENT 交接文档（docs+fix 合一；另有同名 `414cffd2` 修复提交） | 自愈链恢复（见下"风险1"） |
| `b406017e` | TOP3 持仓跟踪接入 westock 云端（双机同步+实时行情）+ 删除个股全站综合诊断卡 + 新增 `fetch_portfolio_westock.py` 刷新脚本 | 持仓跨设备实时；删 ~236 行死代码 |
| `98276c4d` / `043d905e` | data: 持仓实时刷新(westock) 数据落地 | PORTFOLIO / PORTFOLIO_COST 上线 |
| `3fad3970` | 个股查询加 MA/持仓/弱MA 摘要行 + 操作信号面板 + 逻辑详解对齐 | ⚠️ **此提交已被 rebase 正则损坏（见风险2）**，后由 `c9a1c440` 作废恢复 |
| `c9a1c440` | **fix: 恢复 index.html 完好骨架**（修复主站全空事故） | 主站从空白恢复，build 护栏通过 |
| `3a6d471d` | feat: 重建平均股价摘要行 + 操作信号面板（买卖判断，6 维综合判定） | 个股查询新增 ⚡操作信号 |
| `79cae554` | 三面对齐：操作信号面板置顶 + 逻辑详解/运维页/云端定时任务对齐 + 两个根因修复 | 全站三面对齐上线（见风险3/4） |

### 非 git 的系统级改动（WorkBuddy 自动化）
- **08:14 全库模型切回 hy3**：185 个自动化全部 hy3（原 hy3=183 / flash=2）。
- **08:22 自动化清理**：185 → 37 个。删 PAUSED 145 + 过期 once 3 + 孤立 runtime_state 111 条。最终 **37 ACTIVE**：盘前 6 / 盘中 10 / 盘后夜间 12 / 全天值守 9。
- **08:33-08:43 第166轮看门狗**：发现 lemoncat-cn runner 离线 → heal 拉起 Runner.Listener.exe(PID 7784) → 复检六管线全绿。

---

## 三、当前系统状态

- **站点/main**：`c7a3605a`（11:46 build）。index.html ≈798KB / 10 sections / `guard_index_sections.py` GUARD_OK；线上 Contents API 已验证含 📐平均股价 / ☁️持仓云端同步 / 操作信号面板等全部新标记。
- **Runner**：lemoncat-cn（小九单位机，中国 IP）。08:33 离线被自愈拉活；11:30-11:46 连续多轮 cn fetch + build 正常 ⇒ 当前在线。
- **数据新鲜度**：盘前全量 08:17（fetch 14/14）、盘中 cn_fetch 11:45/11:30 等多档正常；PORTFOLIO/PORTFOLIO_COST 经 westock 落地。
- **剩余自动化**：37 ACTIVE（见上）。**模型：全 hy3**。
- **心跳尾行**：`2026-08-12 08:27:12 | xiaojiu | v8_premarket_full | DONE`（今日 handover 心跳待本档 push 后补写）。

---

## 四、需阿狸咪注意 / 待办 / 风险

1. 🔴 **self_heal 曾自 08-11 夜起归零**：`v8_health_check.py::self_heal()` 因 `_dispatch_count` 未初始化每轮必崩（`UnboundLocalError`），导致自愈 + 看门狗告警连坐误报。已 `866f2e9b`/`414cffd2` 根治（入口初始化 + main 包 try/except）。**请观察 1-2 轮看门狗确认不再每轮报非零**。
2. 🔴 **主站全空事故根因（铁律已写入 MEMORY）**：解 index.html 的 cache-buster 冲突**绝不用正则批量删冲突标记**（`re.sub` 非贪婪+贪婪混用会跨块吞掉整个 `<section>`，`3fad3970` 因此损坏、build 护栏连续失败、Pages 停更显示"6天前"）。统一改用 `git restore --source=<commit> --worktree --staged -- <file>` 或 `git checkout --theirs` 单文件。**若你侧也用正则解冲突会再踩，务必留意。**
3. ⚠️ **AVG_PRICE_DATA 类别错位已修**（`79cae554`）：从 `post_close` 改为 `intraday`（与 cloud_fetch_v8 对齐）。修两病：① 盘中 `data/AVG_PRICE_DATA.js` 不重建；② 拖整 post_close 类(~30文件) republish → cache-buster 全天抖动。
4. ⚠️ **westock 持仓刷新依赖本机 hy3+westock 连接**（automation-1786499281868 每30分盘中拉取）；云端 GitHub Actions **无法**直调 MCP。小九机器若离线则持仓不刷新（PORTFOLIO_COST 为权威成本兜底，未成交标的标「待同步」）。
5. ⏳ **危机雷达删除决策待 review**：`003244d4` 已从最终推荐整块删除（职责冲突/认知矛盾/信息冗余）。同意则保持，不同意请加回。
6. ⏳ **云端 cn_fetch 今早 08:25 盘前 cron 未按点触发**（本机 premarket 08:17 已兜住）；09:33 后过 08:45 守卫将严格判龄，观察是否复发。
7. 📌 **本地 WIP 提示**：`index.html` + `data/HEALTH_CHECK.js` 被 Nutstore 同步进来的未提交改动挡住 ff 合并，已临时 stash（名 `handover-ff-blocker-2026-08-12`）完成合并并**已还原原位**。若你在其机器改过这两文件，注意双机同步。

---

## 五、小九监督职责（今日履职记录）

- **08:00 盯盘自动化**：盘前空窗（<0900）无例行 dispatch；三链路非活跃窗口无补跑；监控三 wf 零异常。
- **08:12-08:28 盘前全量**：落后 16 提交 → stash 两个挡路文件保住 `v8_postclose_guard.py`+URGENT → ff → fetch 14/14 全绿 → 显式 add data/raw_data/脚本（排除 index.html）→ commit `bb675200` → 部署等效完成（云端 08:23/08:27 build 在其上重建）。
- **08:33-08:43 第166轮每小时看门狗**：发现 runner 离线并自愈拉活；**定位并根治 self_heal 崩溃**（`866f2e9b`/`414cffd2`）。
- **09:10-10:38 TOP3 持仓 westock 接入 + 个股查询增强**：`b406017e`/`98276c4d`/`043d905e`/`3fad3970`（后者损坏已废）。
- **11:06 主站全空应急响应**：定位 `3fad3970` 损坏 → 取 `0aa0e9b3` 完好基线 → `c9a1c440` 恢复骨架 → `3a6d471d`/`79cae554` 重建操作信号面板并三面对齐。
- **下次交接档**：18:00（本自动化二次触发）。

---

*小九 12:00 CST 自动交接更新完成，推仓后心跳补写。阿狸咪开机即见当日全量状态。*
