# HANDOVER 阿狸咪 → 小九 · 2026-08-12 早晨批量改动

> 发送方：阿狸咪（家里机）｜接收方：小九（单位机）
> 时间：2026-08-12 07:48 ｜ 优先级：P0（已全部入库，双机需拉齐）

---

## 一、总览

今天早上主人连续提出多项修改，**10 个功能 commit 已全部 push 到 `origin/main`**（本地 HEAD == 远端，无待推）。小九只需 `git pull --rebase` 拉齐即可。

## 二、10 个 commit 清单（按时间序）

| # | commit | type | 改动 |
|---|--------|------|------|
| 1 | `e2e844db8` | feat | 今日判定·环境综合卡新增红黄绿灯总览（🟢积极/🟡观望/🔴防守）+ 三大分析师灯点 |
| 2 | `3230f0df0` | feat | AI 市场速览每行红黄绿灯 + 后端 `gen_market_brief.py` 生成 signal 字段 |
| 3 | `a17842698` | fix | **危机雷达分数不变 bug**：`f_crisis_data()` global 维度写死 0.40 → 改 VIX+美债10Y 动态计算；每日洞察主分数/子指标加灯 |
| 4 | `67a85e583` | fix | **LHB 席位补回**：`fetch_lhb.py` DETAIL_LIMIT 40→90 + `stage_to_raw` 骨架覆盖检测 + 北向日历「🟡席位待补」标识 |
| 5 | `c1cab0450` | docs | 逻辑详解页 5 个 tab 对齐红黄绿灯说明 + 08-12 改动汇总卡 |
| 6 | `5a31a5ec0` | chore | 删除暂未上架「平均股价」冗余卡片 + `renderAvgPrice` IIFE（93 行净删） |
| 7 | `0e8195baf` | chore | 个股全站综合诊断明细 6 行竖排 → 2 列 grid |
| 8 | `883271603` | fix | **持仓跟踪 ETF 种子清理**：`_PF_SEED` 3 只 → 1 只（仅 688548 广钢气体），v4 迁移自动清理 159755/159665/159865 |
| 9 | `94fb828fd` | fix | Top3 `finalRecTime` 改 `setBadge` 标准胶囊 + 「今日推荐候选池」补齐更新于/已跑/已入仓三胶囊 |
| 10 | `23aa9f116` | fix | **候选池丢源守卫双向检查**：`stage_to_raw` 只查 dst>src 单方向 → 改 src_missing 双向；raw_data/candidate.json 手动恢复 6 源（观澜台61/maharo3） |

## 三、需要小九注意/执行的

### 1. git 拉齐（必做）
```bash
cd E:/workspace/quant-scanner-v8   # 或小九的 v8 仓库路径
git fetch origin && git pull --rebase origin main
```

### 2. 持仓 ETF 种子（重要）
- 主人令：**「不要 ETF，只留我录入的」**
- `_PF_SEED` 现只有 688548 广钢气体
- `_pfMigrateSeedV3` 升级为 v4：**自动从 localStorage 过滤 159755 锂电池ETF / 159665 芯片ETF / 159865 养殖ETF**
- 小九**自己浏览器**也需刷新「🧪 暂未上架」tab 触发 v4 迁移（本地 localStorage 里的 ETF 会自动清掉）
- ⚠️ 主人浏览器也要刷新，否则 ETF 还在

### 3. 危机雷达分数变化（数据侧）
- global 维度不再写死 0.40：改为基于 MACRO_DATA 的 VIX + 美债 10Y 日级动态计算
- **云端下次跑 `cloud_fetch_v8.py` 后分数会从 33.3 变为动态值（约 27~37 区间）**——不是 bug，是修复生效
- 若小九的 watchful 告警对「分数变化」敏感，注意这是预期行为

### 4. LHB 席位补回依赖云端
- 8/4/5/7/10 四天 seats 全空 → 已修 DETAIL_LIMIT 90 + 骨架覆盖
- **等云端 post_close 调度（17:30）跑 `backfill_lhb_history.py`** 后自动补回
- 前端在补回前显示「🟡 席位待补」兜底（不是报错）

### 5. 候选池 6 源恢复（重要）
- 主人截图「公开资讯源/研报来源 0 只」→ 根因是 `stage_to_raw` 丢源守卫单方向
- 已手动恢复：`raw_data/candidate.json` + `data/CANDIDATE.js` = 6 源（观澜台61 / maharo3）
- **云端下次 build 后若 CANDIDATE.js 又被 4 源覆盖 → 检查 stage_to_raw 守卫是否生效**（commit 23aa9f116 已改双向）

## 四、主人浏览器刷新指引

1. 常规操作：`Ctrl+F5` 强刷（?v= 时间戳已更新）
2. 持仓 tab：需刷新触发 v4 迁移清 ETF
3. 「观测候选股池」：刷新后应看到 6 源（主板100/创业100/科创100/港股12/观澜台61/maharo3）

## 五、遗留/观察项

- 港股成交前50 显示 12 只（akshare 港股接口挂，gold_pool 兜底只有 12）——预期降级，非 bug
- 「08-11 已入仓 0」是主人 8/9 定的「含 0 也显示」规则，保留
- 北向日历 4 天「席位待补」等云端 post_close 回填
- 危机雷达 global 动态化待云端验证分数是否正常波动

---

_阿狸咪 · 2026-08-12 07:48_
