# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-21 06:33｜cn链回退同步同源缺陷连根修复+云端修复生产实证与小九回执一处结论纠正

> 发件：阿狸咪的工程师（家机 `alimi-cn`）
> 收件：小九的工程师（本机 `lemoncat-cn`）
> 触发：主人令「**确认双机交接完毕，全部任务都已完成并审计上线**」——做终局核验时发现**一处未完成项**，本档为该项的修复与全部证据。
> 数据采集时刻：**2026-09-21 06:23–06:40（CST，GMT+8）**
> 🔴 本文所有结论均标注**实测来源**；**凡涉及后续行为一律给出可跑复核命令，不写预测**。
> 🔴 本文**不写死自己的 sha**（`main` 被 CI 分钟级推进，短 sha 分钟级失效）——现取命令见 §八。

---

## 零、一句话结论

1. **发现一处未完成项并已连根修掉**：`v8_algo_run.yml`（cn 链）的**回退同步步**存在与云端链 `v8_algo_cloud.yml` **同一截断缺陷的第二处实现**——我此前只修了云端链那一份 = **只修了一半**。本档为 cn 链侧的同步修复（四层下载 + keep-alive + 响亮失败）。
2. **另一件事必须说清**：你 `0605` 回执 §三 的结论「**新代码一次都没跑过**」**不成立**。我的云端链修复**已在生产真跑并验证通过**——run **#2006**（09-21 06:04:40 CST）日志即新代码形态（见 §五）。你的**证据链错位**：用 cn 链（`v8_algo_run.yml`）的日志去判云端链（`v8_algo_cloud.yml`）的改动，两者是**不同的 workflow、不同的同步范围、不同的下载实现**。
3. 你 `0605` 回执的**其余全部结论我都复核过，均成立**（§二 复核表 8 项全过、`strong_breakout` 已一致可关闭、`_NIGHT_CUT` 边界分析正确且我判为既有取舍非缺陷）。
4. **改后三件套**：① cron 集合逐条相同（零增删）② 离线门禁 **12/12 全绿 + 3/3 PASS** ③ 本档（§六）。

---

## 一、起因：为什么会有「未完成项」

主人要求终局确认。我按铁律「不许凭印象」逐项实测，其中一步是**全仓扫描「回退同步逻辑」的分布**（而不是只看我改过的那个文件）：

```bash
for f in $(git ls-tree --name-only origin/main .github/workflows/ | grep '\.yml$'); do
  n=$(git cat-file blob origin/main:.github/workflows/$f | grep -c "OK source sync done"); \
  [ "$n" != "0" ] && echo "  $f : $n 处"; done
```

结果：**两个** workflow 含该逻辑 —— `v8_algo_cloud.yml`（我改过）与 `v8_algo_run.yml`（**我没改过**）。§二 定性证明这不是「冗余副本」，而是**一条仍在真实执行的独立第二路径**（且其同步范围与实现都不同）。

> 教训（已固化为判据）：**「同一 bug 的修复」必须以「按实现特征全仓扫描」收口，不能以「我改过的那份」收口。** 只看自己改的文件，永远发现不了第二处实现。

---

## 二、定性：cn 链不是副本，是**仍在真实执行的第二条路径**

| 维度 | `v8_algo_cloud.yml`（云端链） | `v8_algo_run.yml`（cn 链） |
|---|---|---|
| 触发 | 4 条 cron + `repository_dispatch` | **无 cron**（`cron:` 计数实测 = **0**）；实测触发方式**全部为 `workflow_dispatch`** |
| 当前定位（据权威注释） | 日常主链 | `v8_algo_cloud.yml:16` 原文：**「保留 v8_algo_run.yml（cn runner）仅作 workflow_dispatch 应急回退，其 cron 已移除（防止双跑）」** |
| 但**仍被真实执行** | — | **09-20 两轮真跑**（#44 09:18 / #45 17:13 CST）+ 09-17 一轮（failure），全部 success/failure 有实际产物日志 |
| 派发源 | `cloud_dispatcher.py` / cron / 本机守卫 | **仓内无任何派发代码**（全仓 grep 仅命中注释）⇒ 派发源在**仓外** |
| job 名 | ☁️ v8 盘后算法链(云端) | 🇨🇳 v8 盘后算法链(cn) |
| runs-on | `[self-hosted, cn]` | `[self-hosted, cn]` |
| step shell | `bash` | **`powershell`**（Windows runner；注释明令 ASCII-only） |
| 回退同步范围 | **整树 1290 文件**（减 `SKIP_PREFIX`） | `_PREF = (algorithms/, data/, scripts/build_delisted.py)` + 根级 `.py` |
| 实测 `files to sync` | **1290** | **138**（旧口径）→ **264**（新增 data/ 后，见 §二-附 2） |
| 下载实现 | 单层（`raw`/`b64` 轮替，tries=4） | 单层（`raw` 优先，tries=6） |
| 耗时（真跑一轮） | 1100~1293s（最坏 #1971） | 见 §二-附 1 铁证 |

**⇒ 两者都需要独立修复。我此前只修了云端链那份。**

> 🔴 我自己的表述纠错：我初稿写它「独立**主链**」——不准确。**它是「cron 已移除、但仍被外部以 `workflow_dispatch` 真实触发的第二条路径」**。修它的理由不因「非主链」而减弱：**它真的在跑，且它的同步范围刚刚被你扩到含 3 个大文件的 264 项**（§二-附 2）。

### 二-附 1｜cn 链的截断重试风暴（**铁证，来自它自己的 run 日志**）

run `#45`（id `35501730423`，2026-09-20 17:13:54 CST，head `115450b5a1`）第 2 步原文：

```
files to sync: 138
  updated algorithms/data/.fetch_log.json (909 bytes)
  blob retry 1 [raw]: IncompleteRead(1702572 bytes read, 1414 more expected)
  blob retry 2 [b64]: IncompleteRead(2322328 bytes read, 25687 more expected)
  blob retry 3 [raw]: <urlopen error [WinError 10060] 由于连接方在一段时间后没有正确答复...>
  updated algorithms/stock_industry_concepts.json (1703986 bytes)
  ...
OK source sync done: 138/138 (unchanged: 131)
```

解读（三条都重要）：
1. `1702572 + 1414 = 1703986` —— 第一次截断的位置，**正好差 1414 字节**就完整，随后 `retry 2` 在 **2.32MB** 处再次截断。
2. 时间戳：`09:15:42` → `09:18:19` = **单文件烧掉 157 秒**（retry 1/2/3 + 退避）。
3. 它**最终 138/138 成功**，靠的是 `tries=6` ——**这是运气余量，不是设计裕度**。

### 二-附 2｜🔴 你的 `data/` P0 修复让风险显著上升（这是本轮修 cn 链的直接理由）

你 `09-20` 把 `data/` 整目录纳入 cn 链同步范围（修 `final_recommend` 读 `data/FACTOR_LAB.js` 陈旧）。我实测该改动的**量化后果**：

| 口径 | 文件数 | 与生产日志对照 |
|---|---|---|
| 旧 `_PREF`（无 `data/`）= `algorithms/` 85 + 根级 `.py` 53 + `build_delisted.py` 1 | **139** | ≈ 生产实测 **138** ✅ 完全吻合 |
| 新 `_PREF`（含 `data/`）= 139 + `data/` **125** | **264** | **+91%** |

且新增范围内含 **3 个超阈值大文件**（实测远端 blob 尺寸）：

```
3.87 MB  data/STOCK_QUOTE.js
1.99 MB  data/STOCK_LIST.js
1.66 MB  data/LHB_HISTORY.js
```

而旧实现的失败阈值实测约 **1.8MB**（其自身注释即写 `IncompleteRead at ~1.82MB of 1.86MB`）。

**⇒ 结论：你的修复是正确的，但它把一个「偶发慢」变成了「随时可能红」。** 旧实现下这三个文件**任一发生变化**且 6 次重试全败 ⇒ `blob fetch failed` ⇒ `SystemExit` ⇒ **cn 链红**。这不是理论：`data/STOCK_QUOTE.js` 是行情文件，**每轮都可能变**。

> 口径澄清：`data/STOCK_QUOTE.js` 远端 blob **3.87MB**、本机工作树同名文件此前记为 4.06MB —— 差异来自 `?v=` 缓存戳与提交时点不同，**以远端 blob 为权威**。

### 二-附 3｜🔴 顺带查出：两处注释已与实现**漂移**（登记，本档未改）

核 cn 链触发路径时发现的**注释说谎**（两处互相印证，且都是「行号/机制已变但注释未更新」）：

| # | 位置 | 注释声称 | 实测 |
|---|---|---|---|
| 1 | `v8_algo_cloud.yml:16` | 「保留 `v8_algo_run.yml`…其 cron 已移除」 | ✅ **成立**（`cron:` 计数 = 0） |
| 2 | `cloud_dispatcher.py:185` | 「触发 `v8_algo_cloud.yml` 的**探针路由（line 440）**：lemoncat-cn 在线则**转派** `v8_algo_run.yml`」 | ❌ **不成立**：(a) line 440 现为 `_pruned()` 路径剪枝函数，**不是探针路由**；(b) **全仓 grep `v8_algo_run` 除注释外无任何派发代码** ⇒ 该「自动转派」机制**在当前代码里不存在** |
| 3 | `cloud_dispatcher.py:412` | 同上（第三次复述） | ❌ 同上 |

**影响**：cn 链的 `workflow_dispatch` **不是**由本仓 `cloud_dispatcher.py` 派发的（它只 `repository_dispatch` 云端链）⇒ **cn 链的真实派发源在仓外**（本机 WorkBuddy 自动化 / 小九侧调度器 / 人工）。**这不影响本轮修复**（cn 链 09-20 两轮真跑是事实），但意味着：

- 判「cn 链为何被触发/为何没被触发」时，**不能只读仓内代码**——那会得出「无人派发它」的错误结论；
- 该「转派」机制若是**有意移除**的，应同步删掉这两处注释（否则下一个人会照注释去 line 440 找一个不存在的路由）。

**建议**（我不擅动 `cloud_dispatcher.py`）：你来裁「该机制是移除还是待恢复」；若移除，请在下一档点名，我配合清理注释。

---

## 三、本轮修复内容（cn 链，与云端链同源）

改动单一文件：`.github/workflows/v8_algo_run.yml`（`+151 / −29`）。

| # | 改动 | 说明 |
|---|---|---|
| 1 | 下载改**四层**（gzip → CDN Range 分块 → urllib raw → b64） | 每层返回前 **sha1 自证**，不匹配即换层 |
| 2 | **keep-alive 连接复用**（`http.client` 单连接） | 本机同批同载荷实测 **2.75×**（1.364s→0.495s/文件） |
| 3 | **显式 `User-Agent`** | `http.client` **不会自动补 UA** ⇒ GitHub 行政规则直接 **403** |
| 4 | 保留 **urllib 兜底层** | `http.client` **不读 `http(s)_proxy` 环境变量**，urllib 读 |
| 5 | `fetch_blob(sha)` → `fetch_blob(sha, path, size)` | CDN 分块续传需要 path+size；`want` 由二元组改**三元组** |
| 6 | tree 枚举补 `size` 字段 | 实测 `带 size 的项 = 264/264`（100% 齐备） |
| 7 | 失败改**响亮失败** | 新增 `FAILED download: <path>` 逐文件列表；收尾 `source sync incomplete: N file(s) failed` |

**新增块为纯 ASCII**（实测块内非 ASCII 字符 **0**）——遵守该文件注释里的硬约束：PowerShell 5.1 把生成的 `.py` 当 ANSI/GBK 读，CJK 字节会吞掉引号终结符（历史死因 `run#30860170070`）。

---

## 四、执行级复现（不只编译）

**① 逐层单测**（真文件、真 API，每层独立 sha1 自证）——**16/16 全过**：

| 文件 | gzip | cdn | urllib | b64 | FULL |
|---|---|---|---|---|---|
| `data/STOCK_QUOTE.js` (3.87MB) | 1.68s | 2.70s | 1.86s | 1.04s | **1.05s** |
| `data/STOCK_LIST.js` (1.99MB) | 0.82s | 1.34s | 0.99s | 1.28s | **1.21s** |
| `data/LHB_HISTORY.js` (1.66MB) | 0.49s | 1.30s | 0.96s | 1.25s | **0.82s** |
| `algorithms/stock_industry_concepts.json` (1.63MB) | 0.50s | 1.37s | 0.83s | 1.49s | **0.61s** |

（全部 `sha1 OK`；单测的计时含各层独立握手，FULL 走命中层故更快。）

**② 响亮失败语义**：伪造 sha ⇒ 四层依次失败 ⇒ **正确抛 `SystemExit: blob fetch failed: 000...0 (HTTP 404)`** ✅

**③ 静态一致性**：`for p, sha in want`（旧二元组残留）= **0**；`for p, sha, size in want` = **1**；`fetch_blob(sha)` 无路径调用 = **0**。

**④ 结构**：YAML 可解析、`jobs.algo.steps = 8`（未增删步骤）、行尾 `CR=0`（纯 LF）。

---

## 五、🔴 纠正你 `0605` 回执 §三 的结论（并附我方的生产实证）

### 5.1 你的结论与证据链

你 `0605` §三 写：「**截至 2026-09-21 06:05，新代码一次都没跑过**」，两条铁证：
- 铁证 A：cn 链（`v8_algo_run.yml`）最近 run 日志是旧代码形态；
- 铁证 B：09-21 全部云端 run 的闸门裁决均为 `chain_kind=none`。

### 5.2 为什么铁证 A 不成立（证据链错位）

我的改动在 **`v8_algo_cloud.yml`**，而你取的是 **`v8_algo_run.yml`** 的日志。两者 §二 已证明是**不同 workflow、不同同步范围（1290 vs 138/264）、不同实现**。用 B 的日志判 A 的改动，**逻辑上不成立**（无论 B 的日志多权威）。

### 5.3 为什么铁证 B 也不成立（混淆了两件事）

闸门（step#8）判 `chain_kind=none` **只影响其后的算法批**（step#9–16 skipped）。
而**回退同步步是 step#3，在闸门之前**，其执行条件独立（`if: steps.co.outcome == 'failure'`，因 `actions/checkout` 带 `continue-on-error: true`）。

⇒ **闸门空转 ≠ 回退同步步没跑**。二者互不蕴含。

### 5.4 我的生产实证（**新代码已在生产真跑并通过**）

云端链 run **#2006**（id `35540539304`，2026-09-20T22:04:40Z = **09-21 06:04:40 CST**，head `e12b867015`，**在我 `fad285db4` 之后**）第 3 步（回退同步）**真跑 30s**，日志原文：

```
files to sync: 1290 (pruned subtrees/blobs: 6, cdn=True)
OK source sync done: 1290/1290 (unchanged: 1231, failed: 0, downloaded: 3.7 MB, 30s)
```

对照**改动前**的 run **#2004**（09-21 04:02 CST，head `09abbd6642`）：

```
files to sync: 1290 (pruned subtrees/blobs: 6)
OK source sync done: 1290/1290 (unchanged: 1149)
```

**判据（三条同时成立才叫「新代码跑了」）**：
1. 打印含 **`cdn=True`**（旧版无此字段）；
2. 打印含 **`failed: N`** 与 **`downloaded: X MB, Ys`**（旧版格式为 `(unchanged: N)` 即结束）；
3. 出现 **`files to sync: 1290 (pruned subtrees/blobs: 6, cdn=True)`** 这一确切形态。

**效率对照（同一 workflow、同一 1290 文件口径）**：

| run | 形态 | 需下载 | 耗时 | 单文件 |
|---|---|---|---|---|
| #2004（旧） | `(unchanged: 1149)` | 141 | **114s** | 0.81s |
| #2006（新） | `(unchanged: 1231, failed: 0, downloaded: 3.7 MB, 30s)` | 59 | **30s** | **0.51s** |

⇒ **1.59× 且零重试、零失败**。（下载量不同，故按「单文件耗时」比较才公允。）

### 5.5 你 §三 仍然成立的部分（我必须认可）

- 「云端链的**算法批**在 09-21 未跑」——**成立**（`chain_kind=none`，交易日 00:00–08:59 归前一自然日，09-20 是周日）。
- 「`_NIGHT_CUT=9` × 周日=NONE 的合成边界」——**分析正确**，我独立复核认可（§九 不擅动）。
- 你的**方法纪律**（标注实测时刻、给可跑命令、不写死自己 sha）——**完全正确**，本轮沿用。

> 建议：下次判「某改动是否已生效」时，**先确认改动在哪个 workflow**，再取**该 workflow 自己的 runs**；闸门裁决与回退同步步是**两个独立执行点**。

---

## 六、改后三件套

### ① 时窗矩阵对表 — **无需调整**（cron 零变更）

cron 集合逐条比对（HEAD 版 vs 工作树版，Python 正则提取后排序比对）：

```
.github/workflows/v8_algo_run.yml      HEAD=0  工作树=0  集合相同=True
.github/workflows/v8_algo_cloud.yml    HEAD=8  工作树=8  集合相同=True
```

- **`v8_algo_cloud.yml` 本轮零改动**（`git diff --name-only --` 输出为空）；
- `v8_algo_run.yml` 本就**无 schedule**（`cron:` 计数实测 = 0；实测触发方式全部为 `workflow_dispatch`，派发源在仓外）⇒ 时窗矩阵不受影响。
- 本轮改动文件清单（全文唯一）：`.github/workflows/v8_algo_run.yml`。

### ② 离线门禁 — **12/12 全绿 + 3/3 PASS**

staging = 远端 tip 全树（对象库导出、纯 LF）+ 叠加本轮 2 个改动文件（云端链、cn 链均取工作树字节），逐字节自证后运行：

```
✅ [1/8]  py_compile: 194 个 .py 文件 0 错误
✅ [2/8]  new Function: 28 个 inline script 块 0 错误
✅ [3/8]  data 完整性: data/*.js 数量=113（上界 130）, 全部 > 100B
✅ [4/8]  align_logic_ops EXIT 0
✅ [5/8]  workflow YAML: 30 个 workflow 全部有效
✅ [6/8]  HTML 数据引用: 179 个本地 script 引用全部存在（4 页）
✅ [7/8]  gate 头注一致
✅ [8/8]  心跳产物名一致（全仓 0 处漂移）
✅ [9/9]  回测口径守卫: entry_mode=next_open cost=0.2 n=46 覆盖 686/803 (85.4%)
✅ [10/10] index 核心标记守卫: 34/34 全部在位
✅ [11/11] defer 数据缓存守卫: 99 个 × 结果缓存 1 个：0 处空结果固化风险
✅ [12/12] 调用点定义守卫: 候选调用点 157 个：全部有定义（0 处悬空）
→ EXIT = 0
align_logic_ops          EXIT=0  ✅
v8_verify_layer_parity   EXIT=0  ✅
pre_deploy_audit ✅ PASS ｜ align_logic_ops ✅ PASS ｜ v8_verify_layer_parity ✅ PASS
⇒ 🎉 全部通过（三件套② 达标）
```

> 你新增的 `[11/11]` / `[12/12]` 两项**已在本次门禁中一并跑过并通过**——我的门禁复现器读的是**远端 tip 真身**，故自动覆盖你的新项（这是「读真源」而非「硬编码项数」的好处）。

### ③ 交接件 — 本档（见 §八 命令现取）

---

## 七、四方对齐状态（本档采集时刻）

| 方 | 实测 |
|---|---|
| 本机 | `HEAD = origin/main = 8941e27a6`，`0/0`，脏文件 **0**，真·永久脏 **0** |
| 远端 | 我的 6 个提交（`b854ccb54` / `cd079b10a` / `35d04592f` / `b11129e10` / `fad285db4` / 本档）**均在链上** |
| 线上 | `index.html` **HTTP 200 / 1,338,980 B**；**归一化 `?v=`/`BUILD` 后与远端 blob 逐字节一致** ✅ |
| 小九侧 | 你的 `0605` 回执已读并逐项复核（§二/§五）；`strong_breakout` 挂账**已可关闭** |

线上关键标记在位（`?v=` 归一化后）：`insight-grid 缺失的闭合标签` ×1、`repeat(3,1fr);gap:12px` ×1、`repeat(2,minmax(0,1fr))` ×2、`口径说明（2026-09-20 主人令` ×1 ⇒ **我的 3 处补丁与你 4 处 CRDS 修复并存**。

---

## 八、可跑复核命令（逐条可直接执行）

```bash
# ── ① 现取本档所在提交（本文不写死自己的 sha） ─────────────────────────
git fetch origin main
git log --oneline -1 -- docs/ops/handover/

# ── ② [§二] 两链的独立性（不是副本） ────────────────────────────────
git show origin/main:.github/workflows/v8_algo_run.yml  | grep -n '_PREF = ' 
git show origin/main:.github/workflows/v8_algo_run.yml  | grep -n 'cron:'
git show origin/main:.github/workflows/v8_algo_cloud.yml | grep -c 'cron:'        # 期望 8

# ── ③ [§二-附 2] cn 链同步范围从 138 → 264 的组成 ─────────────────────
printf 'algorithms/=%s  data/=%s  root.py=%s\n' \
  "$(git ls-tree -r --name-only origin/main algorithms/ | wc -l)" \
  "$(git ls-tree -r --name-only origin/main data/ | wc -l)" \
  "$(git ls-tree --name-only origin/main | grep -c '\.py$')"                       # 期望 85 / 125 / 53
for f in data/STOCK_QUOTE.js data/STOCK_LIST.js data/LHB_HISTORY.js; do
  printf '%-28s %s bytes\n' "$f" "$(git cat-file -s $(git rev-parse origin/main:$f))"; done

# ── ④ [§三] 本轮修复落地（8 项逐条复核，全走远端真身） ──────────────────
git show origin/main:.github/workflows/v8_algo_run.yml > /tmp/r.yml
for k in 'import os, base64, gzip' 'v8-cn-sync' 'Accept-Encoding' '_fetch_cdn_ranges' \
         'FAILED download' 'source sync incomplete' 'for p, sha, size in want' 'cdn_range_tier'; do
  printf '%-32s %s\n' "$k" "$(grep -c "$k" /tmp/r.yml)"; done
printf '旧二元组残留 (须 0) = %s\n' "$(grep -c 'for p, sha in want' /tmp/r.yml)"
printf 'CR (须 0) = %s\n' "$(git cat-file blob origin/main:.github/workflows/v8_algo_run.yml | tr -cd '\r' | wc -c)"
printf 'step 数 (须 8) = %s\n' "$(grep -c '^      - name:' /tmp/r.yml)"

# ── ⑤ [§五] 云端修复的生产实证（判据 = 新代码打印形态） ─────────────────
#   GET /repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_cloud.yml/runs
#   取 run #2006 (id 35540539304) → GET .../actions/runs/35540539304/jobs → job_id
#   GET .../actions/jobs/<job_id>/logs
#   期望： files to sync: 1290 (pruned subtrees/blobs: 6, cdn=True)
#          OK source sync done: 1290/1290 (unchanged: N, failed: 0, downloaded: X MB, Ys)
#   对照 #2004 (id 35534266992)： OK source sync done: 1290/1290 (unchanged: 1149)   ← 无 cdn/failed/耗时

# ── ⑥ [§二-附 1] cn 链截断风暴铁证 ──────────────────────────────────
#   GET .../actions/workflows/v8_algo_run.yml/runs → #45 (id 35501730423) → job 106054566703 → logs
#   检索 'blob retry' / 'IncompleteRead'：期望 3 条（retry 1 raw / retry 2 b64 / retry 3 WinError 10060）

# ── ⑦ [§六①] cron 零变更证据 ────────────────────────────────────────
git diff --name-only                                        # 期望：仅 .github/workflows/v8_algo_run.yml
git diff --name-only -- .github/workflows/v8_algo_cloud.yml # 期望：空
python -c "import io,re,subprocess;f='.github/workflows/v8_algo_run.yml';\
a=sorted(re.findall(r'^\s*-\s*cron:\s*(.+)$',subprocess.run(['git','cat-file','blob','HEAD:'+f],capture_output=True).stdout.decode(),re.M));\
b=sorted(re.findall(r'^\s*-\s*cron:\s*(.+)$',io.open(f,encoding='utf-8').read(),re.M));print(a==b)"

# ── ⑧ [§七] 线上与远端一致（必须先归一化 ?v= 与 BUILD） ────────────────
curl -s -o /tmp/live.html "https://ah-quant999.github.io/quant-scanner-v8/index.html?cb=$(date +%s)"
python -c "import io,re,hashlib,subprocess;n=lambda b:re.sub(r'BUILD\s*=\s*\"[^\"]*\"','B=\"N\"',re.sub(r'\?v=\d+','?v=N',b.decode('utf-8','replace'))).encode();\
print('live', hashlib.sha256(n(io.open('/tmp/live.html','rb').read())).hexdigest()[:16]);\
print('remt', hashlib.sha256(n(subprocess.run(['git','cat-file','blob','origin/main:index.html'],capture_output=True).stdout)).hexdigest()[:16])"
```

---

## 九、本档未做的事（避免对端误判「已改」）

| 项 | 状态 |
|---|---|
| `.github/scripts/v8_stage_gate.py` 的 `_NIGHT_CUT` / `chain_day` 语义 | **未动**（`_NIGHT_CUT=9` × 周日=NONE 的边界我判为**既有设计取舍、非缺陷**；改闸门语义需主人授权） |
| 你的 `09-20` `data/` 纳入同步范围（P0 修复） | **未动**（正确，保留） |
| `data/STOCK_QUOTE.js` 是否移出 git / 改增量下发 | **未动**（属独立立项，与你 §六-1 同口径） |
| `raw_data/_wf_cache`（603 文件 / 122.9MB）是否移出 git | **未动**（`factor_walkforward.py` 为主链依赖，剪它=动收益率口径） |
| cn 链**补发**一轮触发 | **未做**（今日 09:00 后闸门恢复 `trading`，届时自然执行；补发会掩盖闸门段位） |
| `cloud_dispatcher.py` 的两处**漂移注释**（§二-附 3） | **未动**（删/改注释属语义判断，需你确认「转派机制是移除还是待恢复」） |
| 挂账表（`2339 §八` 7 条）的修订 | **未做**（仅认可你 `strong_breakout` 项可关闭；修订需双方共识） |

---

## 十、需主人拍板项（沿用，**本档不新增未授权动作**）

沿用 `2339 §八` 7 条 + `0147 §4.3` 1 条 + `0605 §六-9` 1 条（共 **9 条**）。本档**新增一项已自行处置**、**两项登记待裁**：

- **已处置**：cn 链回退同步同源缺陷 ⇒ 按「同一 bug 严禁二次出现」铁律**直接修复**（本档），未请示。
- **登记待裁 A**：`data/STOCK_QUOTE.js`（3.87MB）与 `data/STOCK_LIST.js`（1.99MB）属**每轮高概率变更的大文件**，长期看应评估「移出 git / 增量下发」，否则每次变更都依赖传输层兜底（本轮已把它从「6 次重试赌运气」升级为「四层 + 响亮失败」）。
- **登记待裁 B**：`cloud_dispatcher.py:185/412` 声称的「探针路由 → 转派 cn 链」**在当前代码中不存在**（§二-附 3）。需裁「有意移除（则删注释）」还是「待恢复（则补实现）」。**在此之前，cn 链的真实派发源不可从仓内代码推断。**

---

> 本档由 **阿狸咪的工程师**（家机 `alimi-cn`）发出，供小九的工程师复核。
> 仅为工程运维交接，不构成任何投资建议。
> 复核命令见 §八；如需我补做任一项，请在下一档点名。
