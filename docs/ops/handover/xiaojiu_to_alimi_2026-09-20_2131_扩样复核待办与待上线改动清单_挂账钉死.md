# 小九→阿狸咪 紧急交接：#221 扩样复核待办 + 待上线改动清单（挂账钉死）

**采集时刻**：2026-09-20 21:31（本机时钟，+8）  
**目的**：把当前跨机挂账一次性钉死，消除「不知道谁做没做、挂账漂在哪」的模糊态。  
**声明**：本件所有结论标注采集时刻，涉及「后续会怎样」一律给可跑复核命令，不写预测式承诺。

---

## 一、已上线确认（小九侧 + 双机共识，无需再动）

| 改动 | 真身证据 | 状态 |
|---|---|---|
| 改动1 动态 edge | `final_recommend.py` 含 `_load_signal_edge_dynamic` | ✅ |
| 改动2a 融合器去噪 | `V8_FUSION_NOISE_FILTER`：超额 −0.57/−0.78pp → +0.40/+0.87pp | ✅ |
| 改动5 backtest_expectancy 前置 | `run_algorithms.py` 含前置 | ✅ |
| 改动11 P5 兜底 | `generate_top10.py` 含 `兜底`+`P5` | ✅ |
| 改动12 alpha 精选源 | `6507c61e170b`：四量终极正 edge 资格门，V8_FUSION_ALPHA_SELECT=0 回退 | ✅ |
| 改动14 结案不改动 | 4 项口径一致；IMA_STRONG 2d 系防御性注释被误读 | ✅ |
| 回测口径四项改造 | HANDOVER 09-20_1045 已上线 + CI 口径守卫挂载 | ✅ |
| 本轮回测产物口径 | `backtest_expectancy.py` 四项口径 + 周五重跑产物已推 | ✅ |

> 本轮回测口径四项改造全链上线 + P4 权重补洞 + CI 挂载口径守卫（HANDOVER_小九_2026-09-20_1045）。

---

## 二、#221 扩样复核（⚠️ 阿狸咪负责，E 批，硬依赖未闭环）

**背景**：11 源 T+5 边际 edge 当前基于薄样本（9–12 天 / 632 次信号），幸存者偏差显著；**不可据短期调权重**，须扩样 40~60 日复核。

**扩样源**：`raw_data/history/top10_daily_*`（共 63 交易日）  
**扩样目标**：40~60 信号日  
**口径差异**：主人已标记（详见 09-18_1815 原交接件，勿重写结论）

**预定判定标准（阿狸咪跑完须逐条回执）**：
1. 3 正源扩样后仍正且 n 充足（n≥40）：`sig_jinzuan +1.179`(n136) / `sig_chan +0.898`(n289) / `p4_lowvol45 +0.338`(n67)
2. 8 负源扩样后仍负：`quality −3.266` / `sig_jigou −2.227` / `sig_trend −2.085` / `p4_resid10 −1.476` / `p4_resid5 −1.080` / `sector −0.525` / `p4_lowvol55 −0.153` / `fund −0.128`
3. 精选(3正源) vs 全量(11源等权) 均值差 `+1.58pp`（+0.805 − (−0.775)）是否收敛
4. 择时闸门独立价值：开仓 vs 闭仓 T+5 收益差 `+3.256pp`、胜率差 `+14.03pp`（n开537/闭95）是否复现

**交付物**：阿狸咪回执 = 各源扩样后 n 与 edge 表 + 是否触发权重调整建议。  
**状态**：❌ 截至 21:31 未回执，硬依赖未闭环。

---

## 三、待上线改动清单（全部挂「扩样复核后」）

| 改动 | 内容 | 阻塞 |
|---|---|---|
| 2b | 破斜率倒挂 | 等扩样复核 |
| 3 | 星级→融合器动态联动映射 | 等扩样复核 |
| 4 | 最终推荐自回测 + 闭环保 | 等扩样复核 |
| 6 | 三重共识星级「假0」时序竞态修复 | 等扩样复核 |
| 7 | 融合器上线闸门 | 等扩样复核 |
| 13 | 择时闸门提升为独立加权项（gate alpha +3.256pp 但踩红线 + 薄样本） | 悬停·等扩样复核 |

> 上述 6 项**未上线**，非「已做未推」，是「等 #221 复核结论」的主动挂起，避免薄样本下误调权重。

---

## 四、ETF 东财探测停止令（重申，待阿狸咪落实）

- 小九 21:25 已发 `40c82bb9d`：**ETF 需求早已闭环（09-11 主人 P1），请立即停止东财探测**。
- 阿狸咪 21:20 回执 `f34ea8339` 称「ETF 阻塞实证」——两边口径不一致。
- **请阿狸咪确认收到 21:25 停止令并落实**，避免重复探测浪费 token/带宽。
- 可跑复核命令（阿狸咪侧）：查 `data/ETF_SUBSCRIPTION*.js` / `etf_subscription_eastmoney.js` 的 `update_time` 是否已是 09-11 后真值。

---

## 五、不在本交接范围（已闭环，勿重复）

- 小九侧本轮：删「已否决·全市场画像因子」卡(`acc01f49`)、因子卡去两步(`ba93af187`)、清死代码注释(`d9b21784`)、产物并集推送(`8bc8275f`)、删 `__renderFactorLab` 死代码(`0d3482be`)——均线上核验未被覆盖。
- 双重覆盖复盘、CRLF 根治、家机防覆盖闸门、风险温度计裸 rebase 修复、周末三件修复——阿狸咪均已回执闭环。
