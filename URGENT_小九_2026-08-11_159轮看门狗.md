# URGENT 交接 · 第 159 轮看门狗（小九 → 阿狸咪）

- **时间**：2026-08-11 12:40–12:55（北京时间）
- **命令**：`v8_cloud_watchdog.py --heal --auto-dispatch --health-check --alert`
- **结论**：六管线全绿 EXIT 0，**0 封告警邮件**；健康检查 ok41/warn0/fail1 → 对齐后清零。
- **本轮根因级修复**：`api_push_raw.py` 防倒退守卫「静默失效」隐患（可致数据回退），已根治并验证。

## 一、六管线巡检（12:40:40）

| 管线 | 状态 | 明细 |
|---|---|---|
| self-hosted runner | ✅ OK | lemoncat-cn online=True / busy=False，免 heal |
| cn_fetch | ✅ OK | completed/success @ 12:05（age 35m） |
| build_deploy | ✅ OK | completed/success @ 12:21（age 20m） |
| raw_data 新鲜度 | ✅ OK | 末次提交 12:12（age 28m，阈值 1.5h） |
| 线上站点 | ✅ OK | HTTP 200 |
| auto_dispatch | ✅ OK | 数据新鲜（raw_data 0.5h / cn_fetch 0.5h），无 actionable 项，未派发 |

邮件：`[INFO] watchdog 跳过邮件：无管线异常且健康检查无超阈 ≥120min 的项` → **0 封**。

## 二、前端健康检查

- 初跑 @12:43:53：`ok41 / warn0 / fail1`，唯一 FAIL = `local_sync`（本地 8861cf2 落后 origin/main 7ca7d74）。
- 自愈按设计跳过（工作区有未提交产物，防 stash/pop 污染 data/*.js）。
- 处置：`data/HEALTH_CHECK.js` 取云端版 → `git merge --ff-only` → 对齐 `7ca7d74e`，FAIL 消除。
- **42 张数据卡全部新鲜，0 关键字段空值**（盘中 12:40，数据 12:12 入库）。

## 三、根因级修复：api_push_raw.py 防倒退守卫静默失效（核销 157 轮遗留②）

### 问题

`main()` 里 `existing`（远端 blob sha 表）是**防倒退守卫的唯一基线**，原代码用 `tfull.get("tree", [])` 兜底：

- 一旦 `GET /git/trees/{base_tree}?recursive=1` 网络失败，或 GitHub 返回 `truncated=true`，
- `existing` 静默变成空/残缺 → 所有本地文件都被判为「远端没有」→ **守卫 100% 失效**，
- 用 checkout 时刻的旧内容覆盖远端更新版本，且照常打印「✅ 已推送 / 防倒退跳过 0」**exit 0**，
- 这正是 **08-09 raw_data 大范围回退故障**的成因，从日志上完全看不出异常。

另有两处「单点致命」：

1. `cmt["tree"]["sha"]`、`r2["object"]["sha"]`、`cmt2["tree"]["sha"]` 直接下标取值，`api()` 返回错误 dict 时 **KeyError 未捕获崩溃**（blob 已全部上传完却前功尽弃）——与 157 轮 IncompleteRead 同一家族；
2. 创建 tree / commit 失败即 `sys.exit(1)`，**明明外层就是 3 次重试循环却不复用**，一次瞬时抖动 = 整批 raw_data 不落地。

### 修法

- 守卫基线不完整（`__error__` / 无 `tree` / `truncated=true`）→ **中止推送，绝不裸推**，日志明示原因；
- `cmt` 增加错误检查，取不到 base commit 直接带原因退出，不再 KeyError；
- 提交重试循环内 `r2 / cmt2 / new_tree / commit` 四步全部改为「失败 → 记录原因 → 退避 → continue 下一次重试」，3 次耗尽才 exit 1，并在末行打印 `last_err`。

### 验证（用实时说话）

新版 5 场景全 PASS：

| 场景 | 预期 | 实测 |
|---|---|---|
| base commit GET 网络失败 | exit 1 不崩溃 | ✅ exit 1，明确报错 |
| base tree GET 失败 | 拒绝裸推 exit 1 | ✅ 「防倒退守卫无基线，拒绝裸推」 |
| base tree truncated | 拒绝裸推 exit 1 | ✅ 「守卫基线残缺，拒绝裸推」 |
| 提交阶段前 2 次抖动、第 3 次成功 | exit 0 | ✅ 重试生效 |
| 创建 tree 三连失败 | exit 1 带原因 | ✅ 「3 次重试后仍失败：创建 tree 失败…」 |

旧版对照（`git show HEAD:api_push_raw.py`）：

- 场景 1 → `CRASH KeyError 'tree'`（traceback，无有效日志）；
- 场景 2 → **exit 0 且把本地 08-01 旧数据裸推覆盖远端 1 个 blob**，还打印「✅ 已推送 / 防倒退跳过 0」——回退故障复现。

`py_compile` 通过；raw_data 现有 494 文件，远未触及 tree 截断阈值，但截断守卫作为长期保险保留。

## 四、铁律遵守

- `index.html` / `HANDOVER_*.md` 未碰（本轮 ff 带入的 index.html 变更来自云端 build，非本地编辑）。
- 仅显式 add 本轮改动文件，未用 `git add -A`。
- 他方产物（backup/、data/*.bak_*）原样保留，不提交不删。

## 五、遗留事项

1. ⏳ 算法链卡片判 FAIL 时 `[algo_run]` 派发的实测效果——158/159 轮均全绿，仍无可触发样本，继续观察。
2. ⏳ 建议全仓排查其他「读-改-写」型推送脚本是否也存在「基线获取失败即静默降级」模式（本轮只修了 api_push_raw.py）。
3. ⏳ 云端高频小更新循环延续（12:05–12:21 又两次 fetch+build）。
4. ⏳ 134 轮 except-pass 改日志建议未落地。
5. ⏳ stash 残留 20+ 条待清理。
