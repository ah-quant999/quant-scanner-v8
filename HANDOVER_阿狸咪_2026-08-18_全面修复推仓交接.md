# HANDOVER · 阿狸咪 → 小九 · 2026-08-18 07:40（主人令：全面修复后推仓入库防覆盖 + 交接清楚）

> 发送方：阿狸咪（家里机）  接收方：小九（单位机）
> 优先级：P0（主人 07:20-07:34 连续三次反馈问题，本次已全部修复 + 入库）

---

## 一、主人今天反馈的三个问题（全部已处理）

### ① 07:20「这卡空了！ETF 三合一 3 子块加载中」
**根因**：`data/STOCK_QUOTE.js` 0 字节空文件（8/16-17 连续 2 天 fetch_stock_quote_v8.py 没跑成功，akshare 接口限流/断连）。ETF 三合一依赖 STOCK_QUOTE 数据。
**修复**：
- 新建 `.github/workflows/v8_stock_quote_refresh.yml`（轻量 refresh，5-10 分钟出结果；盘后 17:00 cron 兜底 + workflow_dispatch 应急）
- 应急用 westock-mcp（WorkBuddy 已连 connector，中国出口 akshare/eastmoney push2 全断时可用）补抓 14 只 tracking 最新价，推到远端
- **akshare / eastmoney push2 在 cn 出口全断**（Remote end closed connection），以后应急优先用 westock-mcp

### ② 07:20「小九做的实时数据新卡片『主力资金跟踪』被覆盖」
**结论**：**GitHub 远端 main + 本机 index.html 都没有"主力资金跟踪"卡片**（sec-rt 段只有 4 个 card：涨停热力矩阵/ETF 三合一/概念资金热力图/行业树图）。8/16 HANDOVER / memory 也无记录。
**行动**：已发 `URGENT_小九_2026-08-18_0720_找主力资金跟踪卡片.md`（远端 commit 0664762e）。**请小九确认**：
- 如果卡片做在单位机本机工作树（没推 main）→ 把 HTML 卡 + data 源 + render 函数发来，阿狸咪合并
- 如果主人记错（主力资金跟踪 = ETF 三合一日监控·主力净流入 T+1·盘后）→ 不是被覆盖，是数据未到位（盘后 17:00+ 才有 T+1 数据）
- **09:00 开盘前**需要给小九明确答复

### ③ 07:25「盘后数据也全没了！」（斐波那契/商品涨价/板块资金三合一）
**根因**：**CDN 缓存 + 浏览器内存缓存**（不是数据缺失！）。本机/远端 SH_FIB.js 等都有 8/14 数据，但 `index.html` 的 80 处 `?v=` 缓存戳与 data 文件实际内容 sha1 失配（61 个假失配来自 republish_time 中性化逻辑，真失配最终 1 个 IPO_DATA.js）
**修复**：
- `scripts/fix_cachebusters_cdn.py` 对齐 80 处 ?v=（下载 CDN 真实内容算 sha，CRLF→LF + republish_time 中性化）
- 推 index.html（远端 5bbe344b → 25716c57 最终对齐）
- **主人 hard refresh（Ctrl+Shift+R）** 即可看到数据

---

## 二、已入库的 commit（远端 main，今天 07:19-07:40）

| commit | 内容 |
|---|---|
| `82eae384` | STOCK_QUOTE.js 应急补抓 14 只 tracking（westock-mcp） |
| `1495e91c` | raw_data/stock_quote.json 应急补抓 |
| `8a00c422` | raw_data 压缩为紧凑格式 |
| `db74f8d5` | v8_stock_quote_refresh.yml（修正版，python -m pip 装 akshare） |
| `0664762e` | URGENT_小九_2026-08-18_0720_找主力资金跟踪卡片.md |
| `5bbe344b` | index.html 对齐 80 处 ?v=（fix_cachebusters_cdn） |
| `25716c57` | index.html 最终对齐（IPO_DATA.js?v=9e59a63b4c） |

## 三、防覆盖措施（已生效）

1. **云端 v8_daily_audit.yml 每日 22:30 自愈**：?v 失配 → fix_cachebusters_cdn.py（CDN 权威）重算 → Contents API 直推 index.html → 触发 Pages 构建
2. **index.html `<meta http-equiv="refresh" content="1800">`**：每 30 分钟整页刷新，绕过服务器 max-age=600 缓存头
3. **`?v=` 用内容 sha1 前 10 位**（非 mtime），已用 fix_cachebusters_cdn.py 全量对齐
4. **注意**：云端 build/healthcheck 每几分钟推 commit（07:34 build 曾把 IPO_DATA.js?v= 覆盖回旧值，07:40 已重新对齐）——**已知缺陷**：cn runner 直推 data 后 build 可能覆盖 cn-extra 文件 ?v，靠每日 22:30 自愈兜底

## 四、待小九处理

1. **「主力资金跟踪」卡片**：确认是否在单位机工作树，发代码给阿狸咪合并（09:00 前）
2. **SIX_DIM_RADAR.js 不存在**：六维共振雷达卡没有数据文件（health report 一直 fail），需要确认是否已下线或补数据源
3. **本地 git 对齐**：本机工作树落后远端 798 commit（今天全用 Contents API 直推），小九 pull 时注意先 stash 未提交改动

## 五、后续建议

- **renderFib 等 initPostMarket 函数没有 setTimeout 重试兜底**（对比 v8_cal 的 doV8CalRetry setTimeout 400/1200）——建议加，避免 CDN 慢时读到空数据
- **topup_top5_quotes.py**（主人截图建议）：fetch_stock_quote_v8.py 跑完后补抓 Top5 + tracking 报价，已用 _topup_tracking_quotes.py 应急实现，可固化为正式脚本

— 阿狸咪 2026-08-18 07:40
