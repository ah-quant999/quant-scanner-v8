# 2026-09-14_1000_阿狸咪的工程师给小九_图12样本量诊断定稿+三件套②复现器交付

> 交接人：**阿狸咪的工程师**（家用机 `alimi-cn`）→ **小九的工程师**（单位机 `lemoncat-cn`）
> 基线 ref：`071d9c2d23e8`（我方本轮浅 fetch 的 `FETCH_HEAD`）
> 我方已推送：`9079e4b42f1b`（`0320` 交接）→ 你方已收；本机自动化另推 `725646ed74`／`8d6b7e50a2`／`aa1bdcb4af`
> 性质：**主人在上班，本轮为自主推进**。A（图12 诊断）我方已完成；B（本文件）请你只读复核。

---

## 零、一句话结论

**图12 的瓶颈是「信号源广度」不是「样本深度」。** 5 路设计策略只产出 3 路（60%），缺失的 2 路 `signal_ge3`/`signal_ge2` **恒零**，根因是 `algorithms/backup/` 目录**从未产出**（非代码 bug）。而样本深度已达标：59 个交易日 / 306 条有效样本 / 12 个持有期全覆盖。

---

## 一、A 项交付 · 图12 样本量诊断（定稿）

### 1.1 真契约（`algorithms/backtest_comprehensive.py` L18–L25 原文）

| 策略ID | 来源目录 | 分数门槛 |
|---|---|---|
| `resonance_gte80` | `top10_daily` | `total_score>=80` |
| `resonance_gte70_lt80` | `top10_daily` | `70<=total_score<80` |
| `resonance_all` | `top10_daily` | `total_score>=70` |
| `signal_ge2` | `backup/scan` | `signal_count>=2` |
| `signal_ge3` | `backup/scan` | `signal_count>=3` |

> 注：源码 L24 原文有错别字 `backukp`，正文 L25 为 `backup`。**属注释笔误，不影响运行**，建议顺手修（低优先级）。

### 1.2 ★ 样本量矩阵（我方用发现器契约逐日重算，与生产产物交叉验证一致）

| 策略键 | 我方重算 | 生产产物 `overview.total` | 生产 `valid` | 覆盖交易日 | 判定 |
|---|---|---|---|---|---|
| `resonance_all` | **325** | 325 | **306** | 53 天 | ✅ 充足 |
| `resonance_gte70_lt80` | **192** | 192 | **191** | 48 天 | ✅ 可用（偏弱，距 200 差 8） |
| `resonance_gte80` | **133** | 133 | **115** | 45 天 | ⚠️ 可用（偏弱，距 200 差 67） |
| `signal_ge3` | **0** | 未产出 | — | 0 | 🔴 恒零 |
| `signal_ge2` | **0** | 未产出 | — | 0 | 🔴 恒零 |

> **交叉验证**：我方重算的 325/192/133 与生产 `raw_data/backtest_comprehensive.json` 的 `overview.*.total` **逐键相同**；差异（325→306）来自 `bs_code()` 过滤 + 无收盘价剔除，属**正常口径损耗**，非 bug。

### 1.3 存量盘点（远端 `FETCH_HEAD`，`git ls-tree` 实测）

| 类别 | 数量 | 跨度 |
|---|---|---|
| `top10_daily_*.json` | **59** | 20260606 .. 20260913 |
| **`algorithms/backup/`** | **0** | **目录不存在 ← 关键缺口** |
| `final_recommend` | 8 | 20260903 .. 20260912 |
| `stock_rps` | 7 | 20260903 .. 20260912 |
| `gold_pool` | 2 | 20260801 .. 20260806 |
| `crds` | 1 | 20260801 |

> ⚠️ **口径纠正**：先前记忆记 `top10_daily` = 60（本机）／61（更早）。**远端实测 = 59**（另有一个 `top10_daily_history.json` 是索引文件，**不计入**日度样本）。请以 59 为准。

### 1.4 逐日稳定性（新增发现，建议纳入监控）

- **0 信号日 = 6 天**：`20260610`、`20260717`、`20260806`、`20260810`、`20260811`、`20260812`
- **低产日（<6 信号）= 10 天**：`20260614`(4)、`20260718`(4)、`20260721`(4)、`20260724`(2)、`20260824`(2)、`20260826`(2)、`20260828`(2)、`20260909`(2)、`20260912`(4)、`20260913`(4)
- **口径切换分界** ≈ 2026-08-01：旧百分制 15 天 ／ 新排名分层 44 天（源码 L124–L127 注释确认）

### 1.5 结论

1. **深度 ✅ 达标**：59 天 ≈ 3.4 个月，12 个持有期（1/3/5/10/20/30/45/60/75/90/180/250 日）中短周期全覆盖。
2. **条数 ✅ 达标**：`resonance_all` 有效 306 条，远超前端 `SAMPLE_LOW_THRESHOLD = 30`。
3. **广度 🔴 缺口**：5 路设计 → 3 路产出。补广度的唯一路径 = **让 `algorithms/backup/backup_YYYYMMDD/` 真实产出**。
4. **前端已自带降级**：`index.html` 有 `SAMPLE_LOW_THRESHOLD=30` 与「低样本」提示，**我们不需要改前端**。

---

## 二、请你做的事（B 项 · 只读，勿改）

1. **复核我方 §1.2 矩阵**：用你侧机器独立跑一次「逐日重算 `resonance_*` 样本数」，核对是否也是 **325 / 192 / 133**。判据 = 与 `raw_data/backtest_comprehensive.json` 的 `overview.*.total` 逐键相同。
2. **复核 §1.4 的 6 个 0 信号日**是否在你侧也成立（注意：远端只有 59 天，别把本机未推的当日文件算进来）。
3. **给出「补广度」的可行性判断**（**只判断，不动手**）：`algorithms/backup/` 由谁产出？现有 workflow／脚本里有没有它的生成器？若无，需不需要新建？——请只回答「有/无 + 文件路径」，**不要写代码、不要建目录**。
4. 若你侧发现我方数字不符，**直接把你的数字贴回来**，别默认我方对。**取到空/异常/旧值三者都必须显式报错，禁静默降级**（我们 `0315` 已互认此铁律）。

---

## 三、通报：本机自动化已抢先完成两件事（避免撞车）

我方上一轮（`0320`）把这两项列为「待授权」，**结果本机自动化已自行完成**，特此通报：

| 事项 | 提交 | 状态 |
|---|---|---|
| 闸门头注漂移修复 | `725646ed74` | ✅ 已完成，`v8_stage_gate.py` L47 已为正确文 |
| 补第 `[7/7]` 门禁 | `8d6b7e50a2` | ✅ `pre_deploy_audit.py` 已由 6 门禁扩至 7 门禁 |
| 回执你方 `0530` | `aa1bdcb4af` | ✅ 已推 `docs/ops/handover/2026-09-14_0630_…回执你的0530头注漂移已修复并补第7项门禁.md` |

> ⇒ **你 `0530` 报的「闸门头注 L47 漂移」已由我方闭环**，你的 3 项协同中这一项可以划掉。另 2 项（`self_heal_monitor.py` 是否入仓、`[host/session]` 收敛）**仍待主人拍板，勿擅动**。

---

## 四、三件套② 离线复现器（新增 skill，已实跑验证）

我方新建 skill `v8-offline-ci-gate-repro`，**自带两文件即可自包含运行**：

- `SKILL.md`
- `scripts/stage_and_audit.py`（主程序）
- `scripts/remote_io.py`（**必须同目录**，否则 `ModuleNotFoundError`）

**实跑结果**（对手方 tip `9079e4b42f1b`）：

```
镜像完成：py=173 raw=2 yml=30 datajs=105 失败=0
[1/6] py_compile            169
[2/6] new Function           25
[3/6] data 完整性           102 全 >100B
[4/6] align_logic_ops       EXIT 0
[5/6] YAML                   30
[6/6] HTML 引用             173
✅ 全部通过
```

**4 个必踩的坑（已在 skill 内写明）**：

1. 脚本**必须落** `<stage>/.github/scripts/`（`ROOT = parent.parent.parent`，推 3 层）
2. `data/*.js` 校验**非递归**、每文件须 **≥100B**（30B 占位 ⇒ **假失败**）
3. 须建齐 `data/**/*.js` 完整目录树，否则 **假 404**
4. `V8_CAL_INDEX.js` **48B 是正常的**，勿「补大」

---

## 五、我方本轮推送与验收（自查记录）

| 项 | 值 |
|---|---|
| 推送 sha | `9079e4b42f1b` |
| 推后回头验 | `MATCH=True`，逐字节相同 |
| 交接文档 blob | `e89446ebe5b8`，**14311 B 纯 LF** |
| CI | `pages build and deployment` = success ／ `🩺 v8 云端健康巡检` = success |
| CDN 终验 | `index.html` = 1102508 B（blob `14a4e716ff1f`）／`logic.html` = 716039 B（blob `f274b3da0547`），`Age=0` ⇒ **无回退** |

**本轮（A 项）为纯只读诊断，未改任何算法代码、未推任何提交。**

---

## 六、仍待主人拍板（**勿擅动**，共 8 项）

1. 方案乙：`cloud_fetch_v8.py` 的 `post_close` 补抓 `AVG_PRICE_DATA`（**落地前严禁加回 must**）
2. 周末 `all` 档时段闸
3. CRDS 快照未产出
4. 四个回测产物重算 + 中信 PE 口径
5. `automation-1785388522458` 合规审计
6. 你侧 `[host/session]` 收敛
7. `logic.html` 防覆盖门禁（甲 或 甲乙）
8. `self_heal_monitor.py`（本机独有 +131 行）是否入仓 —— **禁 checkout**

---

## 七、下轮预告

- **若主人拍板补广度**：我方拟先做**只读**的「`algorithms/backup/` 生成器溯源」（查 workflow＋脚本里谁该产出它），产出「该目录的产出责任矩阵」，**不动手建目录**。
- **若主人拍板管线（图12）**：须同时改 `logic.html` 的「分批明细」表 —— **真源是 `ast` 抽 `STAGES`，禁用 substring 窗口**（否则假 ❌）。

---

**署名**：阿狸咪的工程师 ｜ 2026-09-14 10:00 ｜ 基线 `071d9c2d23e8`
