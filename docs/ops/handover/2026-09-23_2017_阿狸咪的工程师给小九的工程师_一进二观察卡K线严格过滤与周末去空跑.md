# 交接件 · 「主板首板·一进二观察」卡：K 线严格过滤接入 + 周末去空跑

- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-23 20:17 CST
- **类型**：功能接入 + 省流优化 + 前端改版（**已上线，无需你动手**；本件供你知情与后续维护）
- **权威口径源**：`docs/ops/DUAL_MACHINE_PROTOCOL.md`（协同总则）

---

## 一、主人令（原文）

1. 「这种格式可以一行6列，直接展示基础的，其他的折叠不要拉窗。」
2. 「周末跑了浪费吧。」
3. 「（K线严格过滤尚未接入 →）马上加，明早我要看到。」

## 二、改了什么（5 个文件）

| 文件 | 改动 |
|---|---|
| `algorithms/fetch_board_first_limit.py` | ① 接入 K 线严格过滤（六项忠实复刻）② 新浪日线取数 + 仓内缓存回退 ③ 新增增量闸门 `need_refresh()` ④ `KLINE_VER=1` 口径版本 ⑤ K 线重试封顶 `kline_try≤3` |
| `index.html` | 卡渲染器：**一行 6 列**、**严格达标置顶直接展示 / 基础达标直接展示 / 其余 `<details>` 折叠**、**删除 `max-height` 内滚动窗**；核心/用法/限制改为**实测口径**；页面级定性注释同步 |
| `.github/workflows/v8_cn_fetch_cloud.yml` | `td` 步骤输出 `is_trading_day`；一进二步加 `steps.td.outputs.is_trading_day != 'false'` **周末闸门**；步骤注释同步（五道护栏） |
| `data/BOARD_FIRST_LIMIT.js` | 09-23 数据（32 只 / 严格 16 / 基础 21），新增 `strict`/`kmiss`/`read`/`kline`/`ksrc` 字段 |
| `raw_data/board_first_limit.json` | 同上原始档（新增 `strict_pass`/`kline_ok`/`kline_na`/`kline_ver`/`kline_try`） |

### 2.1 K 线严格过滤的口径（**禁自创，来源已固化**）

忠实复刻「一进二」通达信公式，**两处权威源**：
- 口径解读：`E:\WorkBuddy交付\选股公式解读与审计_2026-09-21.html`
- 回测实现：`E:\_alimi_tools\backtest\bt_yijiner.py` 的 `components()`

六项 + 附加项（进入 `strict` 的全部条件）：

| 项 | 判据 |
|---|---|
| 涨停确认 | 类MACD `EMA8-EMA55` 金叉（`cai>=shen`）＋ 类KDJ `K+D` 回升（`ls>=gs`） |
| 大趋势 | `ZBJN17-ZBJN18>0` |
| 量差 **或** 控盘指数 | `MA(VOL,5) < 2.5×STD(VOL,10)` ／ `EMA(EMA(C,13),13)` 变化率×1000 > 0 |
| 量线粘合 | `max(MV5,MV20,MV60)/min(MV5,MV60) < 2.75`（原公式 `MIN(MIN(MV5,MV60),MV60)` 笔误，**忠实复刻**） |
| 三线粘合 | `max(M8,M13,M21)/min(M8,M13,M21) < 1.0618` |
| 角度60 | `|atan(MA60日变化率%)| < 30°` |
| 附加 | 量<6.18×昨量 · 昨日非涨停 · 小盘(流通<4亿股 或 市值<120亿) · 换手>2% |

🔴 **筹码穿刺分支已砍除**：依赖外部指标 `SSRP`（v8 无此源、定义未知），按 09-21 审计结论砍除，
该 OR 分支由「量差／控盘指数」承担（与 09-21 回测同口径）。

### 2.2 K 线取数源（**实测结论，非推测**）

- ❌ 东财 `push2his.eastmoney.com`（含 `52.` 编号子域）与 `akshare.stock_zh_a_hist`：本机**RemoteDisconnected**（WAF 拒）
- ❌ 腾讯 `web.ifzq.gtimg.cn`：**HTTP 501**
- ✅ **新浪** `money.finance.sina.com.cn/.../getKLineData?datalen=320`：返回当日真值
- 回退：仓内 `raw_data/kline_cache/<code>.json`（已入仓·零网络）；其尾根日期 < 信号日 ⇒ 判 `stale`，
  **不给过滤结论**（不拿旧 K 线冒充当日）
- 复权口径已比对：新浪原始与仓内 kline_cache 同日 OHLC **完全一致**；量单位差 100 倍（新浪=股／缓存=手），
  六项过滤全为**比值口径** ⇒ 不受影响

### 2.3 省流（主人令②）

- **周末**：加 `is_trading_day != 'false'` 闸门 ⇒ **零请求跳过**（此前 `category=all` 时必跑）
- **盘前/盘中**：涨停池是收盘定稿的日频数据 ⇒ 那些轮次不可能产出更新内容。生成器新增 `need_refresh()`
  **增量闸门**：只花 1 次交易日历请求即判断「已采集档 date 已跟上最近已收盘交易日 ＋ `kline_ver` 一致 ＋
  `kline_ok ≥ total×0.8`」⇒ 早退，**不发涨停池 / K 线请求**
- K 线重试封顶 `kline_try ≤ 3`：K 线源不可用时最多重试 3 轮，防「每轮重试」风暴
- 净效果：**每交易日只真抓 1 次**（收盘后首轮），其余轮次零请求

## 三、实测证据（禁止只看「跑通了」）

### 3.1 面板层增量对照回测（本轮自跑，与 09-21 同缓存同口径）

| 口径 | n | 均值% | 基准% | 超额% | 按日 t |
|---|---|---|---|---|---|
| 裸首板池 · A(信号日收盘买) T+1 | 7434 | +1.243 | −0.313 | +1.556 | **8.62** |
| 裸首板池 · **B(次日开盘买)** T+1 | 7434 | **−0.479** | −0.220 | −0.259 | **−2.55** |
| 严格分层 · B T+1 | 2280 | −0.382 | −0.230 | −0.152 | −0.53 |
| 严格分层 · B T+5 | 2228 | **+0.261** | +0.067 | +0.195 | 0.35 |
| 严格分层 · A T+5 | 2228 | +2.023 | −0.004 | +2.027 | 5.98 |

**严格分层相对裸首板池的同日配对增量**：A|T1 +0.232(t=1.57)｜A|T5 +0.467(t=1.46)｜A|T10 +0.501(t=1.22)｜
B|T1 +0.149(t=1.22)｜B|T5 +0.408(t=1.39)｜B|T10 +0.400(t=1.03)

**判定**：① 盲追首板在**次日开盘买**口径下**显著亏钱**（T+1 −0.48%、t=−2.55）；
② 严格过滤把方向掰正（T+5 −0.32%→+0.26%），**但增量 |t| 仅 1.0~1.6，未达显著**；
③ A 口径（不可成交的收盘价）超额显著。⇒ **严格过滤的价值是「降噪排序」，不是买点**。
📌 该结论已写进卡片「⚠ 限制」，**禁改成「推荐买入」**。

### 3.2 离线门禁（staging = 远端最新树 + 换入本轮 5 文件）

```
tip = 0e84385e9d3843bcc675749b20663aa0b16560a9
✅ [1/8] py_compile 193 个 .py 0 错      ✅ [2/8] new Function 28 块 0 错
✅ [3/8] data/*.js=110 全 >100B          ✅ [4/8] align_logic_ops EXIT 0
✅ [5/8] 31 workflow YAML 全有效          ✅ [6/8] 177 引用全存在
✅ [7/8] gate 头注一致                    ✅ [8/8] 心跳产物名一致
✅ [9/9] 回测口径守卫                     ✅ [10/10] index 核心标记 39/39
✅ [11/11] defer 缓存守卫 0 风险          ✅ [12/12] 调用点定义 161 个 0 悬空
✅ [13/13] 交接状态源完好                 ✅ [14/14] AI 入口在位
🎉 14 项全部通过 → EXIT 0
align_logic_ops EXIT 0 ｜ v8_verify_layer_parity EXIT 0（真错位 0）
```

🔴 **本轮 [10/10] 当场拦下一次「落后基线覆盖」**：我最初的 index.html 基于 tip `185cd4d8`，
而期间远端已前进到 `c0848560`（你推了 4 条新锚点：`推荐置顶同序`、`选股策略恢复加锁` 等）
⇒ 门禁报 `4/39 条核心标记消失`。**照推会顶掉你的改动**。已按新基线 `0e84385e` 重建后复跑全绿。

## 四、给你验的判据（**给判据不给脚本**，用你自己的等价工具）

| 判据 | 期望值 | 取数方式 |
|---|---|---|
| `pre_deploy_audit.py` | **14 项全绿 · EXIT 0** | 见 skill `v8-offline-ci-gate-repro` §九（staging 走本地对象库） |
| `align_logic_ops.py` | **EXIT 0** | 同上 |
| `v8_verify_layer_parity.py` | **EXIT 0，真错位 0** | 同上 |
| `data/BOARD_FIRST_LIMIT.js` 的 `strict_pass` | **16**（09-23） | 回读 blob 解析 JSON |
| `index.html` 中 `repeat(6,minmax(0,1fr))` | **≥1** | grep |
| `index.html` 中 `max-height:340px` | **0**（内滚动窗已删） | grep |
| 一进二步 `if:` 含 `is_trading_day` | **1 处** | grep workflow |
| `td` 步骤写 `is_trading_day` 到 `$GITHUB_OUTPUT` | **1 处** | grep workflow |
| 次日 08:25 盘前 run 该步 | **SKIP**（增量闸门判定已采集） | 看 CI step 结论 |

## 五、注意事项 / 已知边界

1. **不要**把本卡改成「推荐买入」或加评分星级 —— 实测 B 口径无显著 alpha（见 3.1）。
2. `KLINE_VER` 是口径版本号，**改过滤口径或换取数源必须 +1**，否则增量闸门会认为已采集而不重算。
3. 新候选若 K 线取不到（新股／停牌／源抖动），该只 `kline` 标 `na`/`stale`，卡片显示 `K?`/`K旧` 且**不给过滤结论**，
   这是**有意降级**，不是 bug。
4. 回测脚本与产物在**仓库外**：`E:\_alimi_tools\bfl2\bt_inc.py` · `bt_out\inc_summary.json`
   （按纪律不入仓，避免污染工作树）。
5. 本轮**未**新增 `docs/ops/HANDOFF.yaml` 条目：本改动属「已完成」而非「待办/待验」，
   故只用本交接件；若你认为需要立待验条目，请按 skill `v8-handoff-yaml-item-update` 同步三处
   （item 本体 + `meta.next_id_seq` + `HANDOFF.ledger.json` 的 `max_next_id_seq`），否则 `[13/13]` 会红。

---

本件为工程交接，仅供研究参考，不构成投资建议。
