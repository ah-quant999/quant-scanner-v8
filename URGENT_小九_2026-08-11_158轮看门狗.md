# URGENT 小九 → 阿狸咪 · 第 158 轮看门狗（2026-08-11 11:29–11:38）

## 一句话结论
六管线全绿、0 封告警邮件；本轮清掉两条历史遗留隐患——全仓核查确认 `IncompleteRead` 漏捕**无第二处**，并把 **13 张算法链卡片的自愈类别一次性改对**（原本派发 `post_close` 永远刷不到，还白占 25 分钟去抖锁）。

## 一、六管线巡检（watchdog EXIT 2，rc 来自健康检查子进程）

| 管线 | 状态 | 明细 |
|---|---|---|
| runner | ✅ | lemoncat-cn online=True / busy=False，免 heal |
| cn_fetch | ✅ | 11:06 completed/success（age 23m） |
| build_deploy | ✅ | 11:15 completed/success（age 14m） |
| raw_data 新鲜度 | ✅ | 11:15 提交（age 15m，阈值 1.5h） |
| site | ✅ | HTTP 200 |
| auto_dispatch | ✅ | 数据新鲜（raw_data 0.2h / cn_fetch 0.3h），无需派发 |

邮件：**0 封**。判定「无管线异常且健康检查无超阈 ≥120min 项」→ 跳过。

## 二、健康检查

- 初跑 @11:30:09 **ok 41 / fail 1** —— 唯一 FAIL = 「管线 / 本地与 origin/main 同步」，本地落后 origin **4 个云端提交**（`34a33c72 cn fetch 11:14` → `2180b6cc build` → `3f2782ab risk gauge` → `000e0839 build 11:15`），属瞬时态。
- 处置：`HEALTH_CHECK.js`（产物）取云端版 → `git merge --ff-only` 对齐 `000e0839`。
- 复跑 @11:36:44 **ok 42 / warn 0 / fail 0 全绿，EXIT 0**。

## 三、【根因级修复 · 一劳永逸】自愈类别全表核对，13 张卡片自愈空转

### 缺陷本质（与 155 轮 NT_DATA 同一类，但范围大 13 倍）
`v8_health_check.py::self_heal()` 按 `PAGE_TO_CAT[page]` 决定派发哪个 `cn_fetch` 类别：

```
"盘后数据" -> post_close
"选股策略" -> post_close
```

但 `cloud_fetch_v8.py::CATEGORY_MAP` 里 **`post_close` 只注册了 2 项**：`MARKET_FUND_FLOW_DATA`、`EXPERIMENT`。

也就是说，下列卡片一旦超阈判 FAIL，自愈动作派发的 `cn_fetch post_close` **根本不生产它们的数据**，属于：
1. 自愈永远无效（数据不会刷新）；
2. 更糟——每次空转都会写入 `post_close` 的 25 分钟 debounce 锁，**把真正需要 post_close 的派发挡在门外**。

### 实际产出方（已逐一反查确认，全部在 `algorithms/run_algorithms.py` 算法链内）

| 卡片 | 页面 | 真实产出脚本 |
|---|---|---|
| SH_FIB 市场温度计 | 盘后数据 | `fetch_sh_index_fib.py` |
| SIX_DIM_RADAR 六维共振雷达 | 盘后数据 | 同源 SH_FIB.js |
| CANDIDATE 候选池 | 盘后数据 | `build_candidate_pool.py` |
| GOLD_POOL 黄金池 | 盘后数据 | `build_candidate_pool.py` / `calc_crds.py` |
| LHB_DATA 龙虎榜 | 盘后数据 | `fetch_lhb.py` / `gen_lhb_7d.py` |
| INST_TRADE 机构买卖 | 盘后数据 | `fetch_inst_trade.py` |
| TRIPLE_CONSENSUS 三重共识 | 盘后数据 | `gen_triple_consensus.py` |
| FOUR_VOLUME 四量终极 | 选股策略 | `strategy_four_volume_60m.py` |
| COCKPIT_ADVICE 驾驶舱 | 选股策略 | `gen_cockpit_advice.py` |
| BIG_BULL_HUNTER 大牛股猎手 | 选股策略 | 派生自 LHB_DATA |
| TOP10_DAILY 全站精选 | 选股策略 | `generate_top10.py` |
| STOCK_RPS 相对强度 | 选股策略 | `calc_stock_rps.py` |
| CRDS_CARD_DATA 逆势龙头 | 选股策略 | `calc_crds.py` / `final_recommend.py` |

### 修法（两处）
1. **13 项 SOURCES 全部显式标注 `"heal_cat": "algo_run"`**，走 `_dispatch_algo_run()` 派发算法链工作流，并在盘后数据段头写明判据注释，防止后续增卡时重蹈覆辙。
2. **`check_data_cards()` 新增统一出口 `_emit(d, row)`**：原先 `heal_cat` 只在最后一个 `row` 分支透传，三个提前 `return` 分支（找不到文件 / 盘中误清空 / 时间解析失败）都会丢掉它 → 自愈回落到错误的 `PAGE_TO_CAT`。现四个出口全部统一继承。

### 验证
- `py_compile` 通过；
- 全表复核：SOURCES 共 31 项，已标 `heal_cat` **14** 项，**残留空转 0**；`results.append` 直连出口 **0** 个；
- 实跑 `v8_health_check.py --heal` → 产物 `data/HEALTH_CHECK.js` 中 14 个检查项均正确携带 `heal_cat=algo_run`。

## 四、【历史遗留项核销】157 轮建议①：IncompleteRead 漏捕全仓排查

对全仓 26 个使用 `urlopen` 的脚本逐一扫描异常捕获链，结论：**除已修复的 `api_push_raw.py` 外，其余全部带 `except Exception` 兜底**，`http.client.IncompleteRead` 不会逃逸。该遗留项**核销，无第二处同类漏洞**。

## 五、git

- 开局本地 `9f22c5f6` 工作区干净 → 落后 origin 4 提交 → `HEALTH_CHECK.js` 取云端版 + `merge --ff-only` 对齐 `000e0839`（本地 = origin，0/0）。
- 铁律遵守：`index.html`、`HANDOVER_*.md` 全程未碰；他方备份产物 `backup/stage_0811_0826/`、`data/*.bak_pre_20260811_*` 原样保留不提交不删。

## 六、遗留

1. 下轮观察：算法链卡片若再判 FAIL，验证 `[algo_run]` 派发是否真正刷新（本轮全绿，无 FAIL 可触发实测）；
2. 云端高频小更新循环延续（分钟级 build 连推）；
3. 134 轮 `except-pass` 改日志建议仍未落地；
4. `api_push_raw.main()` 宜改「失败文件记账、成功文件照常 commit」的部分成功语义（157 轮建议②）；
5. stash 残留 20+ 条，建议择期清理。
