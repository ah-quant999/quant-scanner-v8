# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-24 02:49｜错名二犯根治+三项裁决定案+E路径推送事故自纠

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

000807/000703/000833 错名确系「二犯」——23:22 一笔落后基线的 cn fetch 把陈旧 worktree 产物推上 main，把 22:47 的回填打回错名；本轮已**数据回填 173 处 + 派生/研报三个传播口全部过唯一校入口**（错名从此无法跨轮存活），另登记三项裁决定案（`postclose-reliability-trio`，P1·todo·shared），并自纠了一次我自己制造的 `E:/` 垃圾路径推送事故（已归位+删除）。

---

## 正文

### 1. 错名二犯根因链（已闭环）

- 09-23 22:47 回填正确名（commit `19e3f852b`）。
- **23:22** `2262d6587f`：一笔 update_time=**22:38**（早于修复提交 9 分钟）的 gold_pool.json 被推上 main —— 陈旧 worktree 产物被增量推送扫上，落后基线覆盖（`force:false` 拦不住，§35.5 同族）。
- 00:28 新鲜 derive 读到被污染的 prev 池，**继承分支原样保留错名** ⇒ 错名自持循环。
- 本轮修复（commit `f4f46bf1eff8`）：
  - 数据面：880 组碰撞对全库重扫回填 **24 文件 / 173 处 / 20 代码**（含 000807→云铝股份、000703→恒逸石化、000833→粤桂股份），§4.2 假阳性核查（hk 条目×6位码=0）+ 纯度证明全过；
  - 代码面三处：`build_candidate_pool.derive_and_save_gold_pool` 继承分支就地过 `resolve_clean_name`；`scanner` 扫描结果写入 + 研报金股入池/存量纠正过 `resolve_clean_name_s`（与 self_heal_monitor 同规，传播口零直写）。
- 门禁 pre_deploy_audit 14/14 全绿。

### 2. 三项裁决定案（主人令「和小九商量定」，已入 HANDOFF `postclose-reliability-trio` P1·todo·shared）

1. **post_close HEAL 守卫放宽**：硬窗口 16:00-21:30 → 16:00-20:30；对 post_close 独有四卡（主力净额分时/涨停热力/EXPERIMENT/SECTOR_LEADERS）开 21:30 后单次补跑白名单（25min 去抖保留）。
2. **6 连发收敛单发**：同 concurrency 组只容 1 running+1 pending，多余 run cancelled=假成功 ⇒ 多源定时触发合并为单发。
3. **step13 修法A**：推送与重建解耦，推送走增量文件集 Contents API（参照 cn fetch soft_14 自闭环）；改 workflow 须 classic token + schema 自检。

### 3. EXPERIMENT / SECTOR_LEADERS 红卡定性（同根因，今日自愈）

- 两卡生成链均为 **post_close 专属**（EXPERIMENT=update_v8 实验段；SECTOR_LEADERS=cn fetch soft_13b）；09-23 post_close 被拖到 21:11 且只落了 3 个 raw 文件 ⇒ 两卡停在 09-22。
- **非管线死亡**：今天 16:30/17:20 盘后链恢复时会自动回来；守卫放宽（定案①）防复发。
- 「主题概念资金热度」卡未死：SECTOR_FUND_FLOW 09-23 21:11 刚更新；冻结的是「业界/ROE 交叉（实验版）」段（EXPERIMENT）。
- 「宏观景气背景·K型分层」**建议保留**：MACRO_KSHAPE 09-23 21:15 刚巡检更新，数据驱动、无管线成本。

### 4. E:/ 垃圾路径推送事故（我方自纠，已闭环）

- 我用 push_multi 时参数序写反（应为 `远端路径=本地文件`）⇒ commit `3d1cbeb79969` 把 26 个文件建到 `E:/workspace/stock-scanner/...` 字面量路径（内容正确、路径错误）。
- 已修正（`f4f46bf1eff8`）：26 文件归位正确路径 + 26 个垃圾路径全删（base_tree sha=null）；远端 ls-tree `E:` 前缀 = 0。
- **给小九**：push_multi.py 已在 `E:\_alimi_tools\_handoff\`（仓外）；参数序坑我已记入技能，你若复用请以 `remote=local` 传参。

### 5. 请小九接手

- `postclose-reliability-trio` 三项施工（白天档改 workflow，classic token + schema 自检）；verify_how 见 HANDOFF。
- 下一交易日盘后顺看四卡 update_time 是否全前进（= 修法生效证据）。
