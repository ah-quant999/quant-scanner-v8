# HANDOVER 小九 → 阿狸咪（2026-08-21 周五·12:00 档）

> 更新于 2026-08-21 12:07 自动更新（小九 12:00/18:00 交接自动化 automation-1785719698947）
> 本档 = 12:05 并行草稿 + 12:07 本实例实拉 `origin/main` 核对：补全**线上 HEALTH_CHECK 当前 FAIL** 关键降级，并修正主线 SHA / WIP 数量。所有 SHA 经 `git cat-file` + `merge-base --is-ancestor origin/main` 逐条验真。

---

## 一、背景

- **主人出差窗口**：8/21–8/27 主人外出，**铁律「盯紧别乱改」生效**——非必要不手动改 `index.html` / 核心 `data/*.json`；任何改动一律走「改完即 commit + fetch + rebase origin/main + push 上线」流程，不要每轮问「是否上线」。
- **数据源**：stock-scanner 侧当日日志自 08-07 后断更；v8 侧 `.workbuddy/memory/2026-08-21.md` 截至 10:08 仅 1 条记录（10:06 自愈监控闭环）。今日全部产出来自**云端自动化管线**，**无 HH 人工修复 commit**。
- **本地/云端差距**：小九本机 `HEAD = 6a6934fc2` 落后 `origin/main = 5736b982b`（3 commit，纯落后无分叉）；工作树有 **215 个文件 staged WIP**（他人在途改动，属红线，**勿 restore / checkout 覆盖**）。
- **交接依据**：基于真实 `git log origin/main` + 本机 memory + 线上 `HEALTH_CHECK.js`/`runner_status` blob 交叉核对 + 自动化清单（实拉，非记忆 SHA）。

---

## 二、今日已完成改动（真实 commit SHA，截至 12:07）

> 全部为云端自动化产出，无人工干预。按主题归类（SHA 经 `git log origin/main` 实拉 + `cat-file` 验真）：

### A. 数据抓取 / 同步（盘中实时）
| SHA | 时间 | 内容 |
|---|---|---|
| `5736b982b` | 12:0x | v8 build（最新一笔，线上顶端，持续推进中） |
| `1e6923d61` | 12:05 | v8 build |
| `6a6934fc2` | 12:03 | westock 持仓同步 [变化] |
| `3e4877996` | 11:59 | v8 cn fetch（盘中抓取） |
| `9cefffd65` | 11:02 | westock 持仓同步 [变化] |
| `57c5c138d` | 10:13 | 智能触发补抓（盘中·guard）：ai_market_brief / avg_price_data / candidate_quotes |
| `6c943c381` | 11:06 | 智能触发补抓（盘中·guard）[STALE:9] |
| `16a23ebeb` | 11:13 | runner 状态上报 20260821-1113 |
| `c9aa9bc06` | 10:13 | runner 状态上报 20260821-1013 |

### B. 前端缓存戳对齐（一劳永逸·防覆盖）
| SHA | 时间 | 内容 |
|---|---|---|
| `c7613a1b5` | 12:04 | 缓存戳实时对齐 ?v → 防覆盖 |
| `f2afb889f` | 11:03 | 缓存戳实时对齐 ?v → 防覆盖 |
| `8591e4708` | 10:45 | 缓存戳实时对齐 ?v → 防覆盖 |
| `a4577315f` | 10:13 | 缓存戳实时对齐 ?v → 防覆盖 |

### C. 算法 / 行情刷新
| SHA | 时间 | 内容 |
|---|---|---|
| `a4319c395` | 11:49 | 盘中算法追踪轻量刷新 |
| `11a009f02` | — | refresh STOCK_QUOTE via v8_stock_quote_refresh |
| `dabfe4d97` | — | refresh STOCK_QUOTE via v8_stock_quote_refresh |
| `bb4fc8728` / `43572865a` | 10:xx | refresh STOCK_QUOTE via v8_stock_quote_refresh |
| `261bbd33a` | 11:27 | v8 risk gauge（风险仪表） |

### D. 云端构建（v8 build，每次数据落库后自动触发）
`5736b982b`(12:0x) · `1e6923d61`(12:05) · `c9380ff98`(11:36) · `c26b8faba`(11:28) · `0c0f5340f`(11:14) · `5f6d74c97`(11:08) · `588ebc049`(11:03) · `08e2f5d07`(10:29) · `9016198b8`(10:23) · `833a9a14a`(10:19) · `5d99282c4`(10:17) · `35f15bb05`(10:16) · `a2194df62`(10:14) · `e0a8d2210`(10:10) · `0eaf33f3d`(10:58) · `df9740b0e`(10:09) 等。

**影响**：线上 `main` 端始终为当日实时数据（持仓/行情/风险仪表/候选池补抓均刷新）；前端经缓存戳机制防 CDN 覆盖，开盘后可见当日值。

---

## 三、当前系统状态

| 维度 | 状态 |
|---|---|
| **站点** | GitHub Pages（main 分支），线上顶端 `origin/main = 5736b982b`（12:0x build，持续由云端推进） |
| **本机 HEAD** | `6a6934fc2`（落后 3 commit，纯落后无分叉） |
| **工作树** | 215 文件 staged WIP（他人在途，**红线勿动**）；ff-only 合并被其阻断 |
| **🔴 HEALTH_CHECK（线上 origin/main 实时）** | **overall = FAIL（100 ok / 3 fail）**——见下方明细表，均 `heal_cat=algo_run`，盘后算法链未如期刷新 |
| **self-hosted runner** | 进程=正常；服务=NOT_FOUND（GitHub 侧标记，不影响） |
| **cn 应急 fetch（小九本机）** | 最近一次失败——兜底角色 6h 未触发，**云端主线已接管**，数据由云端管线保障，不影响线上 |
| **云端管线** | 今日正常：cn fetch 09:54→build 09:57→westock 10:07；INDEX_QUOTES 09:35（age≈31min，未破 45min 红线） |
| **runner 在线** | `runner_status.json` run_time 11:58:47（category=all）；`RUNNER_STATUS.js` update_time 11:49:26（category=intraday） |
| **自愈监控（10:06）** | exit=0：观澜台 20只/168总、momentum 2026-08-20 OK、zsxq_token OK；冲突门禁 `<<<<<<<` 全仓 0；与 `origin/main` blob 交叉核对确认**无需 push**（避免陈旧本地反向覆盖云端） |

### 🔴 线上 HEALTH_CHECK FAIL 明细（务必关注）
| 项 | id | 页面 | 最后更新 | 超时 | 自愈类别 |
|---|---|---|---|---|---|
| 驾驶舱 | `COCKPIT_ADVICE` | 选股策略 | 2026-08-19 18:21:55 | 2503 min（>1415 阈值） | algo_run |
| FOUR_VOLUME_60M | `all_FOUR_VOLUME_60M` | 全量数据 | 2026-08-20 10:00:26 | 1565 min（>1440 红线） | algo_run |
| H_AUTO_BUY_TRACK | `all_H_AUTO_BUY_TRACK` | 全量数据 | 2026-08-20 10:59:51 | 1505 min（>1440 红线） | algo_run |

> 判读：3 项均为**盘后 `run_algorithms` 算法链产物**，停留在 08-19/08-20，未随今日/昨日盘后链刷新。健康巡逻应自动 `algo_run` 派发修复（heal_cat=algo_run）；若 18:30 算法链仍未跑，相关卡片（驾驶舱建议/四量终极60M/自动买入追踪）会显示陈旧值。**注意**：`all_*` 派生项判的是文件时间，直接核对 `data/*.js` 内层 `update_time` 才能确认真实陈旧。

### 剩余自动化（实拉清单，共 16 个：15 ACTIVE + 1 PAUSED）
- ✅ ACTIVE（15）：
  - `1787210164001` v8 westock 持仓云端同步（盘中30分）
  - `1787108824799` v8 盘中漏轮本机兜底（偏移扫描）
  - `1786961773708` v8 自愈监控闭环（10分钟级）
  - `1786696074524` 小九-v8 runner 本地守护（20分钟级）
  - `1786588096327` v8 分红方案刷新-盘前（08:15）
  - `1786588094908` v8 分红方案刷新-晚间（19:00）
  - `1785978303212` 小九-每日19点审计修复+推仓+对齐
  - `1785898357370` 小九-v8全时段数据兜底（freshness_guard）
  - `1785888730897` 小九-v8盘前全量自动化（08:15）
  - `1785835481750` 小九-v8每小时紧急交接通道（看门狗+自修复）
  - `1785802406605` 小九-v8本地看板同步（每小时 pull）
  - `1785719698947` 小九-12/18点 交接文档更新（本任务）
  - `1785404904506` 小九-17:00 v8选股四模块+全量数据部署
  - `1785396622150` 小九-周末15:30 v8选股四模块T+1更新部署
  - `1785388522458` 小九-15:30 v8收盘数据刷新
- ⏸ PAUSED（1）：
  - `1786933089412` 小九-TDX强势选股每日跟踪（三件套v2）——**按铁律保持 PAUSED，禁止删除；恢复须主人明令**

---

## 四、需阿狸咪注意 / 待办 / 风险

1. **🔴 线上 HEALTH_CHECK FAIL（P0）**：3 项 algo_run 算法产物陈旧（驾驶舱 08-19 / FOUR_VOLUME_60M 08-20 / H_AUTO_BUY_TRACK 08-20）。开机后先核查这三项是否已由盘后 `run_algorithms` 链刷新；若 18:30 算法链未跑，等自愈巡逻 `algo_run` 派发，或手动 dispatch `v8_algo_cloud.yml` / `v8_algo_intraday_lite.yml`。
2. **主人出差期（8/21–8/27）铁律**：盯紧别乱改。线上已自动保持当日实时数据，无需手动干预；若必须改，严格走「改完即 commit+fetch+rebase+push」且先问边界/护栏（不碰 index.html 结构、v6_memo.html、candidate.json 外核心数据源）。
3. **本机 215 文件 WIP 是他人在途改动**：勿 `restore` / `git checkout` 覆盖；若需同步本地看板，先 `git status` 看清范围，`1785802406605` 每小时 pull 会 autostash 处理，但大 WIP 下易出 UU，盘中不抢 pull。
4. **cn 应急 fetch 失败 ≠ 线上异常**：这是小九本机兜底角色，云端主线已接管数据抓取，线上数据正常。若线上卡顿，优先查云端 `v8_cn_fetch_cloud.yml` / `v8_build_deploy.yml` 运行日志。
5. **18:30 算法链跑批**：今日盘中无人工触发，盘后 `run_algorithms`（四模块 + LHB/龙虎榜 + 四量终极）由云端自动化负责；交接 18:00 档会复核是否如期落地（含上面 P0 三项是否被刷新）。
6. **LHB/龙虎榜刷新**：历史根因（无自动化跑 `fetch_lhb.py` / `run_algorithms.py`）已立为一劳永逸待办，盘后链需确认有任务在跑，避免再卡 08-04 旧值。
7. **交接文档幂等**：每 12:00/18:00 自动覆盖刷新；若阿狸咪开机未见当日文档，先 `git fetch` 再看 `origin/main` 上 `HANDOVER_小九_*.md` 最新一份。

---

## 五、小九监督职责（本机在飞，供阿狸咪知悉）

- **12:00 / 18:00 交接文档更新**（本任务 automation-1785719698947）—— 你正在读的就是它。
- **10:06 自愈监控闭环**（automation-1786961773708）exit=0，候选池/观澜台/origin/main 交叉核对健康。
- **runner 本地守护**（automation-1786696074524）每 20 分钟级，错误即自愈。
- **全时段数据兜底 freshness_guard**（automation-1785898357370）智能触发补抓。
- **19:00 审计修复 + 推仓 + 对齐**（automation-1785978303212）。
- **盘中漏轮本机兜底**（automation-1787108824799）+ **每小时紧急交接通道**（automation-1785835481750）。

> 小九白班（7:45–17:45）在飞；阿狸咪夜间/周末接管部署。本档为 12:00 白班快照，18:00 档将刷新盘后状态（含 P0 三项是否已被 18:30 算法链刷新）。

---

*生成方式：读取 v8 侧当日 memory + 实拉 `git log origin/main` + 线上 `HEALTH_CHECK.js`/`runner_status` blob + 自动化清单；仅更新本交接文档，未触碰其他文件（215 WIP 红线保留）。*
