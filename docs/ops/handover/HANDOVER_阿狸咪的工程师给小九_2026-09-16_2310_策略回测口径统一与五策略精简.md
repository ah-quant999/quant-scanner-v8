# 策略回测页「口径统一 + 只留 5 策略」重构 ｜ 阿狸咪的工程师 → 小九的工程师

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 时间：2026-09-16 23:10 CST
> 触发：主人 8 项诉求（截图为证）——核心一句是「**为什么口径不一？我只要真实数据最准确的回测，如果都是真实回测，你统一一个展示就好，不要不一致会引起误会！**」
> 改动面：**`index.html` 单文件**（1,256,357 B → 1,238,192 B）；**未动** data/、algorithms/、workflow
> 基线：`_r2` = 远端 tip `0e1dae6970dd` 的 `index.html`（blob `a4f746f52d24`）
> 验收：**本地真浏览器预演 PASS 34 / FAIL 0**（见 §4）

---

## 0. 一句话

主人点出的「口径不一」**不只是同一个表里有两列**——深查发现**聚合快照持有的是已被作废的污染值**：
总表「四量终极 T+250」显示 **90.7%（843 样本/近 3 年）**，而真值源已恢复为 **70.0%（651 样本/近 5 年/861 信号）**。
两者同页并存 ⇒ 正是主人说的「不一致会引起误会」。本轮把总表该行**按真值源强制同源**，并删去第二口径列，全页只剩一套口径。

---

## 1. 主人 8 项诉求 → 处置对照

| # | 主人原话（节选） | 处置 |
|---|---|---|
| ① | 「为什么口径不一？…统一一个展示就好」 | 删「主口径（不挑档·可横比）」第二列；**总表口径唯一化**；**四量行改为与真值源强制同源**（§2）；口径说明重写为「全页只有这一套」 |
| ② | 「展示每个算法最佳的收益率和胜率的持有期」 | 总表标题改为「**每个算法的最佳持有期（真实收益率与胜率）**」；逐档明细表常驻展开 |
| ③ | 「生命周期…高手明显更好，改为强势跟踪」 | `🧬 选股生命周期` → **`📡 强势跟踪`**，并挂灰胶囊「原高手强势股跟踪」保留映射 |
| ④ | 「其余两个效果不佳的直接下架」 | `ALGO_TRACK`（选股生命周期跟踪）与 `STRONG_BREAKOUT_BACKTEST`（强势突破）退出本页展示（**数据层照常回测，不动**） |
| ⑤ | 「这 3 卡直接删除」 | 删 `#lifeBody` / `#strongBreakoutBody` / `#lifeBackCard` / `#sbBackCard`（4 个容器，含 2 张回测表） |
| ⑥ | 「这句删除，不用折叠了，全部展开」 | 删「▸ 完整明细」`<details open>` 外壳，各策略逐档明细常驻平铺（实测 `<details>` 计数 = **0**） |
| ⑦ | 「按四量、三重、强势跟踪、因子实验室、逆势龙头展示就够了」 | 渲染顺序**显式化**为该顺序（不再依赖星级排序的偶然次序） |
| ⑧ | 「其他杂七杂八的都不要」 | 删综合回测卡、K线信号回测卡、「不列入本页回测排名」块、生命周期独立块、生命周期独立行、星级委员会规则块、持有期档位矩阵块、`_exList` 聚合死代码 |

---

## 2. 🔴 本轮最重要的发现：四量回测「污染值 vs 真值」

### 实证链

| 环节 | 值 | 来源 |
|---|---|---|
| 聚合快照（总表数据源） | T+250 = **843 样本 / 90.7%** / +147.46%，`signal_date_range="近 3 年"`，`source_time="2026-09-16 02:09"` | `data/BACKTEST_ALL_ALGOS.js`（`update_time=2026-09-16 03:32:07`） |
| 真值源（四量卡数据源） | T+250 = **651 样本 / 70.0%** / +83.81%，`range="近 5 年"`，861 信号 | `data/FOUR_VOLUME_BACKTEST.js`（`update_time=2026-09-15 17:25:48`） |

**因果**：`02:09` 正是云端刷新链 `cloud_fetch_v8.f_four_volume()` 用「3 年口径」覆盖 5 年真值的时刻（你 16:20 交接件 `b2b92c80b1` 已恢复源文件 + 加防降级守卫）。
但 **`BACKTEST_ALL_ALGOS.js` 是 03:32 生成的聚合快照，它读的是 02:09 那版被覆盖的数据 ⇒ 该快照至今仍持有那个已被作废的 90.7%**，且无人重跑。

⇒ 结论：**这不是前端 bug，是「聚合快照未随源文件重跑」的数据层滞后**。前端这轮先把四量行拉到与真值源同源（不造数、不插值），把矛盾从「同页两个数」变成「一个数」。

### 🔴 数据层待办（交给你，我未动 data/）

1. **重跑 `BACKTEST_ALL_ALGOS.js` 生成器**（根治其余卡）。逐卡核对结果如下：

| 卡 | 聚合 `source_time` | 源文件 `update_time` | 判定 |
|---|---|---|---|
| 四量终极 | 2026-09-16 02:09 | 2026-09-15 17:25:48 | 🔴 污染（已由前端同源兜住） |
| **因子实验室** | 2026-09-15 16:06 | 2026-09-16 **03:16:31** | 🟡 **陈旧**（源已更新、快照未重跑） |
| 逆势龙头 | 2026-09-16 03:16 | 2026-09-16 03:16:50 | ✅ 一致 |
| K线信号层 | 2026-09-14 02:07 | 2026-09-14 02:07:31 | ✅ 一致（卡已下架） |
| 三重共识 / 强势突破 | `None` | 取不到 | ⚪ 不可判 |

2. **把「强势跟踪」并入 `gen_backtest_all_algos.py` 的 `BACKTEST_ALL_ALGOS`**（见 §3）。
3. 建议给聚合生成器加一条**源一致性校验**：任一行 `source_time` 落后于其 `source_file` 的真实 `update_time` 时，要么重算、要么显式标注——否则今天这类「快照持有废弃值」还会复发。

---

## 3. 「强势跟踪」为什么是前端合成行（不是造数）

`IMA_STRONG_BACKTEST`（真实回测产物）尚未被 `gen_backtest_all_algos.py` 收进 `BACKTEST_ALL_ALGOS`，所以前端加了一个**适配器**把它接进总表：

- 数据 100% 来自 `window.IMA_STRONG_BACKTEST`（真实日K、已扣双边 0.30%），**取值口径与其余行完全一致**（样本 ≥ `MINS` 的档里取胜率最高者）；
- **同卡名去重**：`_bestList` 里若已有「强势跟踪」则不再合成 ⇒ 你把真行接进聚合后，合成行**自动消失**，无需再改前端；
- **绝不缓存**（实测踩坑）：`IMA_STRONG_BACKTEST.js` 是 `defer` 加载，内联脚本解析期先跑；若把「空结果」写进缓存，数据到位后总表也永远缺这一行。故每次实时取数（开销仅一次 13 档遍历）。
- 该行真实值：T+3 = **81.6%** / +8.42%（38 样本）—— 确实显著优于另两个算法（选股生命周期 T+5 32.2%、强势突破 T+1 44.7%），主人的判断有数据支持。

---

## 4. 验收证据（本地真浏览器预演，34/34）

方式：`proxy_serve.py`（本地吐**我的补丁 index.html** + 回源**线上真实 `data/*.js`**）→ `playwright-core` + 系统 Edge headless。
`index.html :: inline scripts = 28，语法错误 = 0`；`<details>` 计数 = 0；`btBody innerHTML = 42838 B`。

| 组 | 断言 | 结果 |
|---|---|---|
| A1–A19 | 折叠已删/两卡已删/两口径列只剩一套/5 策略在/强势跟踪行 = 81.6%/**四量行 = 70.0%（防污染回归）** | 19/19 ✅ |
| A17–A18 | **策略计数与名单同源**（5 个 vs 名单 5 个）、名单内策略均有实际渲染 | ✅ |
| B1–B11 | 生命周期 3 卡已删、强势跟踪卡已渲染（13709 B）、页签已改名、旧名 0 命中、回测表 5 档真实百分比 | 11/11 ✅ |
| 页面错误 | `(none)` | ✅ |

> A17 是**防复发断言**：改造前 `btSummary` 的计数从 `BACKTEST_ALL_ALGOS` 动态数（只数到 4），而括号里的名字是**硬编码 5 个** ⇒ 实测出现「说 4 个却列 5 个」。现改为计数与名单**同源派生**，两者不可能再打架。

---

## 5. 与你已完成工作的重叠（我主动撤除，避免互踩）

你 `61ed9dab0`（22:20）已在上游删除：
- 「综合回测（多策略×多持有期）」100 行 ← **与我原 E13 完全重叠** ⇒ 我已撤除该步；
- 「K线信号回测」57 行 ← **与我原 E15 完全重叠** ⇒ 我已撤除该步；
- emoji 转义泄漏修复（`\U0001f3af` → 🎯、`\U0001f9fa` → 🧺）⇒ 这两行所在块被我的删除操作**整块**移除，净效果一致（产出中 `\U0001f` 字面泄漏 = **0 处**）。

**另外**：你的 `7b2351028`（22:15）「部署步仅在确有未提交改动时才备份/恢复 index.html」——**这正是我 21:30 交接件请求的覆盖源治本修复，感谢你用有 workflow 权限的通道落盘**（我这侧 PAT 对 `.github/workflows/` 路径返回 403）。

---

## 6. 边界声明（我**没有**做什么）

- **未动** `data/*.js`（`ALGO_TRACK.js` / `STRONG_BREAKOUT_BACKTEST.js` 照常回测，只是 UI 下架）；
- **未动** `algorithms/`、`.github/workflows/`；
- **未动** `logic.html`——它的 §「去重删表/主口径」有一段描述已过时（★见下），但该文件正被另一活跃会话编辑（22:45 刚提交 +190 B），**为免踩踏我不碰**，特此报告交给你：
  - ★「若主口径胜率最高者与全局最佳不同，并列一句补充」——本页已删第二口径列，此描述过时；
  - ★「默认展开：全站 5 处 `<details>`…策略回测『完整明细』」——该 `details` 外壳已整段删除，此描述过时。
- **时窗矩阵**：本次为纯展示层重构，**不触及**抓取/算法批次的时窗，故未改 `logic.html` 的定时任务表。

---

## 7. 推送安全（本轮守卫两次立功，记录备查）

1. **第一次拦截**：制作补丁期间远端 `index.html` 被你改动（`0e1dae6970dd`，1256357 B）。**若直接推会静默覆盖你的改动**——被 `_p3_guard_base.py`（blob 漂移守卫，三重比对：tip 根 tree 的 blob sha / 本地 git-blob sha / contents API sha）拦下。⇒ 改用新基线重做补丁。
2. **第二次拦截**：CI 的 `?v=` 缓存戳重写（blob 变、**size 不变**=1256357）。⇒ 用 `_p3_sync_v.py` 只同步戳（106 处，数量逐一对齐）+ `BUILD`，并断言「差异**全部**属于缓存戳」才放行。
3. 推送走 **GitHub Git Data API**（blob → tree(`base_tree`=推送那一刻的 tip 根树) → commit → 单次 `PATCH refs/heads/main`，**`force:false`**），不产生本地对象库依赖、不动生产工作目录。

---

## 8. 遗留待办

| 优先级 | 事项 | 归属 |
|---|---|---|
| 🔴 P0 | 重跑 `BACKTEST_ALL_ALGOS.js`（因子实验室陈旧；根除「快照持废弃值」） | 数据层（你） |
| 🟠 P1 | 把「强势跟踪」并入 `BACKTEST_ALL_ALGOS`（前端合成行将自动退役） | 数据层（你） |
| 🟡 P2 | `logic.html` 两处过时描述（§6 ★） | 待 `logic.html` 编辑静默后处理 |
| 🟡 P2 | 聚合生成器加「源一致性校验」（§2.3） | 数据层（你） |

---

*本报告仅供研究参考，不构成个人投资建议。*
