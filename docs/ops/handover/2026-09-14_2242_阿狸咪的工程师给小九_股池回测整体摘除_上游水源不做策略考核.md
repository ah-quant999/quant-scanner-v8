# 交接单 · 股池回测整体摘除（候选池 / 金股池 = 上游水源，不做策略考核）

- **时间**：2026-09-14 22:42 CST
- **发件**：阿狸咪的工程师（家用机 cat-1）
- **收件**：小九的工程师（单位机 cat-2）
- **触发**：主人令（附截图，原话见下）

---

## 一、主人原话与病灶实证

> **「说了多少次了，金股池和候选股池是算法的上游水源，不是策略，不需要回测！！！」**

截图文案：`3. 金股池 T+1 仅 32% 胜率：查是样本偏差还是信号本身弱。`

**产地已用产物定格证**（`git show <远端>:raw_data/backtest_all_algos.json`，update_time `2026-09-14 16:02:15`）：

```json
"delist_advice": [{
  "card": "金股池", "label": "首次满足信号 T+1", "icon": "🪙", "rank": 4,
  "sample": 127, "win_rate": 32.28, "avg_return": -1.79,
  "reasons": ["胜率 32.28% < 红线 45.0%", "平均收益 -1.79% ≤ 红线 0.0%（已扣成本）"],
  "ask": "是否下架「金股池」并停止跟踪？"
}]
```

⇒ **上游水源被写进了「拟下架」提请**。同产物 `rows` 里 24 条池子行（候选池 12 + 金股池 12）全部 `cat="trade"`，`ranking_all` 里也有 5 条池子。

---

## 二、变更清单（单 commit：7 改 + 5 删 + 本单）

| # | 文件 | 改了什么 |
|---|---|---|
| 1 | `algorithms/run_algorithms.py` | `ORDER` + `STAGES["E"]` 摘除 `backtest_pools.py`（E 批 **12→11**、ORDER **51→50**） |
| 2 | `.github/scripts/v8_stage_gate.py` | `READY_SPEC["E"]`：**5 项 / need 4 / must 3** → **3 项 / need 2 / must=[BACKTEST_ALL_ALGOS.js]**；docstring 同步 |
| 3 | `algorithms/gen_backtest_all_algos.py` | 删两条池子 `SOURCES` + `parse_pool()` + `PARSERS["pool"]`；`primary_rule` / `known_gaps` / 文件头注释同步 |
| 4 | `v8_health_check.py` | 删 `CANDIDATE_BACKTEST` / `GOLD_POOL_BACKTEST` 两条健康登记（池子**上游数据**卡 `CANDIDATE`/`GOLD_POOL` 保留不动） |
| 5 | `scripts/v8_delist_advice.py` | **★ 新增 `POOL_CARDS` 豁免**：`judge()` + `_from_rows()` 双处跳过池子 —— 这条才是那条「拟下架」提醒的产地 |
| 6 | `index.html` | 删两个池子回测 `<script>` 标签；**护栏补 `黄金池` 别名**（见 §四） |
| 7 | `logic.html` | E 批脚本数 **12→11**；④ E 就绪清单改写为 **2/3 项 + must**；删除两池产物 |
| 删 | `algorithms/backtest_pools.py` · `data/CANDIDATE_BACKTEST.js` · `data/GOLD_POOL_BACKTEST.js` · `raw_data/candidate_backtest.json` · `raw_data/gold_pool_backtest.json` | 全删 |

**保留**（有意保留，勿删）：
- `index.html` 的 `__STAR_KIND` 基础股池护栏（7 处 `=== / !== 'pool'` 判定）—— 见 §四；
- `v8_health_check.py` 的 `CANDIDATE` / `GOLD_POOL` 上游新鲜度卡 —— 池子该被考核的是「有没有产出」，不是「胜率」；
- `backtest_tdx.py` 把金股池当**选股宇宙**用（这是上游水源的正确用法，不是回测池子本身）。

---

## 三、验证证据（全部本地实跑）

| 项 | 结果 |
|---|---|
| `py_compile` × 5 个 py | ✅ 全通过 |
| AST 复现模块级断言 `_STAGE_UNION == set(ORDER)` | ✅ **成立**（E 批 11 / ORDER 50）⇒ 盘后链启动不会崩 |
| `READY_SPEC["E"]` | ✅ `items=[CRDS_BACKTEST, BACKTEST_TDX, BACKTEST_ALL_ALGOS]` `need=2` `must=[BACKTEST_ALL_ALGOS]` |
| `SOURCES` 条目 | ✅ 6 条（三重共识/四量终极/逆势龙头/强势突破/K线信号层/因子实验室），**无池子** |
| node 全量 inline `<script>` `new Function` | ✅ **27 块 / 0 错**（与远端原版一致） |
| 行尾（二进制判） | ✅ `logic.html` 纯 CRLF 保持 · `index.html` 纯 LF 保持 |
| 替换器缺陷回归 | ✅ 无未展开反向引用落盘 |

---

## 四、诚实更正（我先前基于过期数据误判，已纠正）

我一度判断「护栏因 `金股池` vs `黄金池` 命名不一致而漏网」。**复核后更正**：

- 本地那份 `raw_data/backtest_all_algos.json` 是 **09-13 13:00** 的过期版，卡名写的是 `黄金池`；
- **远端 09-14 16:02 的新版卡名是 `金股池`**，两个名字都在 `__STAR_KIND` 里 ⇒ **线上护栏本来是生效的**（主对比表/档位矩阵/最优组合榜都已排除池子）；
- 真正漏的是 **`delist_advice` 下架提请**（聚合器 advice 循环无池子豁免）—— 已由 #5 修掉。

不过「两种写法都出现过」是事实 ⇒ 护栏仍补 `黄金池` 别名（#6），兼容任何历史/残留产物。

---

## 五、⚠️ 未在本地重算的产物（有意，非遗漏）

`raw_data/backtest_all_algos.json` + `data/BACKTEST_ALL_ALGOS.js` **未在本地重算**：

- 远端该产物是 **09-14 16:02** 的更新版，本地同源产物停在 09-13 ⇒ **本地重算＝降级**（会把全站回测数据拉回昨天）；
- 故按「改动生效靠重算」铁律，**交下一轮云端 E 批自然清掉 24 条池子行**；本轮不会出现「池子又回到页面」——前端护栏仍在（§二 保留项）。

---

## 六、给小九的三条

1. **禁止再挂回**：`backtest_pools.py` 已删。原 09-13「只要接入算法链的选股策略都要有回测」一令**已作废**（池子不是策略）。别照旧单子恢复。
2. **门禁口径已变**：E 批就绪 **3 项 / need 2 / must=[BACKTEST_ALL_ALGOS]**。若你的本机/脚本里还有「E 批 4/5 项」的旧判据，请同步，否则会一直等一个永不存在的产物而空转。
3. **`v8_delist_advice.py` 加了池子豁免**：如果你那边也在跑它，注意别把 `POOL_CARDS` 判断删掉 —— 否则「拟下架金股池」会复发。

---

## 七、下一轮请复核

- [ ] 下一轮云端 E 批后：`raw_data/backtest_all_algos.json` 的 `rows` / `ranking_all` / `delist_advice` 里**不再出现池子**（预期池子行 0 条）；
- [ ] `data/HEALTH_CHECK.js` 里不再有 `CANDIDATE_BACKTEST` / `GOLD_POOL_BACKTEST` 两卡；
- [ ] `data/CANDIDATE_BACKTEST.js` / `data/GOLD_POOL_BACKTEST.js` **不再被重建**（若被重建 = 有残余调用方没清干净，立即回报）。
