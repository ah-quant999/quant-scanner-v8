# 2026-08-17（周一）工作日志

## 开盘前例行审计交接（06:25 主人令"审计，防覆盖，各中对齐，交接"）
- **审计**：audit_weekly --cloud → 9 个 ?v 失配（cn-extra build 覆盖已知缺陷）+ 数据陈旧度 0 项（FINAL_RECOMMEND 今早 05:52 已自动更新）+ 4 项 workflow 不一致（本机 origin/main 缓存旧误报，忽略）。
- **防覆盖**：已按 CDN 权威修复 ?v（fix_cachebusters_cdn 79 处对齐，deploy commit `daba174e77`）。云端每日 22:30 自愈兜底（昨晚 daily_audit 22:30 success）。
- **各中对齐**：云端 workflow 全绿（daily_audit 22:30 / freshness 每小时 / reconcile 15 分钟 / health_patrol 全 success）；DB 11 行含 4 个已删任务软删残留（工具层真实 6 个 AI 类）；真实待主人确认 model 的 4 个（交接×3 + 心跳）。
- **交接**：写 HANDOVER_小九_2026-08-17_开盘前审计.md 并推 main（201）。
- **脚本优化**：audit_weekly 陈旧度检查改 Range 取 head 4KB（修复本机大文件下载超时误报"检查失败"），已推 main。
- 开盘关注：算法链 18:30 gate 放行后验证全量推送；动量筛选观察期（8-30 复核）。

## 19:30 自动交接检查 + Secret 已加 + PDF/止损待主人定（20:00-20:24 主人交互）
- **19:30 交接**：远端 HANDOVER_小九_2026-08-17.md 存在（18:30 手动全量刷新档，commit 2e8daef5e，本地=origin 零分叉）。HANDOVER_LOG 今日无条目，无 failed_steps/close_p1/p2。小九全天 15 项改动（regime 门控上线 / algo cloud 卡死根治 / 观澜台 token 接入+candidate自愈 6 源 393 / 盘前数据残留一劳永逸 / TDX强势突破卡升级 / v6 备忘录空白修复 / 信号徽章醒目化 / 板块资金分时曲线 / 强势突破页纠正 / 止损优化崩溃修复等）。本地=远端 0/0 健康 ok42+/warn5/fail0。无失败步骤需阿狸咪处理。
- **观澜台 P0 已解**：20:24 主人确认 ZSXQ_TOKEN Secret 已加到 https://github.com/ah-quant999/quant-scanner-v8/settings/secrets/actions 。云端 algo 下次跑自产观澜台，self_heal_monitor 退为兜底。
- **PDF 阻塞**：小九 P0 "STOCK_MOMENTUM_STATE.js 停 08-04 需 PDF OCR"——主人纠正"小九下午已收 PDF"，但家里机（阿狸咪）全盘搜过 0 个 2026-08 PDF（最新微信文件只到 2025-08 旧研报）。PDF 在小九单位电脑或微信，等主人转发到家里机 or 让小九明早跑。
- **止损止盈改 prevHighN 风险信号**：小九今天跑出 stop_target_optimization.json（18:26:55）→ tdx 最优 fixedP10/prevHighN60 胜率 46.59% / profit_factor 1.083，但**当前配置 fixedP10/rrK1.5 胜率 47.16% / profit_factor 1.28 / avg_return 1.444%**——新方案全面劣于旧，唯一胜出 median_rr 2.136 vs 1.5。已列表让主人拍板（top10 第 2 名 fixedP12/prevHighN60 胜率 49.55% 但 profit_factor 1.031）。未擅自改动，等主人决定。
- **3 个问题待主人回复**：①PDF 处理方案 ②止损止盈是否今晚改 ③build_deploy 偶发 failure 是否现在排查。

## 20:30 主人三项拍板 · 部署（commit e37b22f7f + Contents API 直推 5 文件）
- **PDF 收到**：主人发 6 个 PDF 路径（C:\\Users\\HH20210606\\xwechat_files\\GGcat999_b0f9\\msg\\file\\2026-08\\2026.8.10-14/17盘后选股.pdf）。OCR 管线：
  - 抽 8 页图片 + 2 页文字（fitz 直接读共识文字页，跳过 OCR）
  - 24 张 PNG 用 rapidocr_onnxruntime 抽取（40/40 成功，1m32s）
  - 解析为 6 天新数据（8.10-8.17，共 1152 只个股命中，46 共识）
  - IIFE 内部 var data 替换（vm 严格解析，266KB→591KB）
  - 验证：45 天｜42 天有共识｜421 共识股｜8.17 共识 18 只
- **止损止盈改 prevHighN60**：已改 + 推 main
  - algorithms/stop_target_profiles.json general+tdx target 改 prevHighN60，metrics 同步
  - algorithms/gen_stock_stop.py method_desc + docstring 同步
  - 跑 103 只成功 → STOCK_STOP_DATA.js 重生（method 标"2026-08-17 主人令改"）
  - 注意：回测上 prevHighN60 胜率46.59%/PF1.083 都劣于旧 fixedP10/rrK1.5 (47.16%/1.28)，仅 median_rr 2.136 更高 — 主人的选择，记录在册
- **build_deploy 排查**：GitHub Actions 拉历史 —— build_deploy 本身零失败；上游 `cn_fetch` 凌晨 04:13-07:43 连续 11 次失败（已自愈），`algo` 09:40+10:07 失败（commit 5df733955 已修）。**无需手动处理**。
- **观测候选股池卡片改一行**：index.html line 11358-11388 改 6 个 details 块 → 6 chip 一行（flex-wrap 自动换行）
  - 颜色编码：A 股活跃成交池=青 / 港股=橙 / 研报=紫 / 观澜=绿 / 0 只=灰
  - 点击 chip 仍可展开该源明细股票
- **候选池数据补足**：云端 19:49 那次只产 162 只（mootdx 部分超时回退兜底），家里机 20:39 重跑出 358 只（主板97/创业100/科创100/港股50/观澜16/maharo9）。手动 JSON→CANDIDATE.js（152KB），push 上线。
- **⚠️ 教训**：处理 IIFE 包装的 JS 文件，**禁止直接 open(..., 'w')**，先 vm 解析后定位 `var data = { ... };` 块做文本替换。我中途误覆盖一次，靠 .bak_20260817_2031 恢复（原文件 320 行 IIFE → 备份 266KB → 重写 591KB）
- **部署**：commit e37b22f7f 本地成功，git push 墙不通走 Contents API（5 文件：CANDATE/STOCK_STOP/STOCK_MOMENTUM_STATE/INDEX + stop_target_profiles.json），触发 Pages build queued。线上 CANDIDATE 358/总 ✓ 45天/421共识 ✓ 103只 stop ✓
- **晚间阿狸咪本机审计任务已删除**（按 memory 铁律 — 云端 v8_daily_audit 22:30 兜底），剩下 6 个 AI 类任务保留。

## 21:00 STOCK_MOMENTUM_STATE_V2 增强重跑（接续"没做完"部分）
- **V1 6 天新数据 → V2 K线验证重跑**（commit c5e798107 + Contents API 直推 2 文件）
  - consensus_codes.json 用 node vm 解析新 V1（JSON 格式，旧 parse_consensus.py 正则已失效——新 JS 键带引号）→ **391 只共识股（+31）/ 421 次**
  - K线：eastmoney push2his 被风控（Remote end closed）→ **腾讯 ifzq `web.ifzq.gtimg.cn/appstock/app/fqkline/get?param={sym},day,2026-06-01,2026-08-17,320,qfq`** 全量重拉 391 只成功（55根/只，含 8-17）
  - generate_v2.py：甲午月 226 / **乙未月 195（原159 +36）**；走势分类 观察260/强势82/连续涨不回撤72/可精选7
  - backtest_sector_confluence.py：N=421 样本，反向结论不变（主升/启动差，震荡最优）
  - enhance_exit_rules.py **退出纪律 rrK1.5 → prevHighN60**（主人令）：target=入选日前60日最高价；纪律回测 N=393：stop 24.4% / target 28.0% / none 47.6% / disc_mean +1.42%
  - 线上验证：391/421、periods 226/195、stopTarget=prevHighN60 ✅
- **⚠️ 教训 2**：JS 数据文件改 JSON 格式后，旧正则解析脚本（parse_consensus.py 等）全部失效，须用 node vm 或新 JSON 正则重写解析。
- **⚠️ 教训 3**：eastmoney push2his 批量拉 K 线会被风控断连，腾讯 ifzq 接口更稳（0 失败）。

## 21:05 SECTOR_RS 板块周期 3 天没更新（commit 802a04c3b + Contents API）
- **现象**：板块周期卡显示「主升9/启动22/震荡59 今日无新进入」灰色，数据 update_time 2026-08-14 17:24（3 天前）
- **根因链**：
  1. fetch_sector_rs.py 没有任何 workflow 主动调用（只在 run_algorithms 内部被 step_run 调，但 v8_algo_run workflow 有 cn runner 调度失效 bug）
  2. 即使跑了，stage_to_raw 把 out/ → raw_data/ 也没自动化
  3. update_v8.py 转 raw_data/ → data/SECTOR_RS.js 依赖 v8_build_deploy.yml（cn 直接 push 不跑这个）
  4. api_push_raw.py 走 raw_data/ 到 main，但没人触发 stage_to_raw
- **修复**：
  · 手动跑 fetch_sector_rs.py（同花顺，90 板块）+ stage_to_raw.main() + update_v8.py --category post_close
  · 新数据 21:10：主升18/启动10/震荡61/底部1（vs 旧 9/22/59）
  · 推 main 5 文件（SECTOR_RS.js / sector_rs.json / index.html / 2 workflows）
  · v8_cn_fetch.yml + v8_cn_fetch_cloud.yml 加 fetch_sector_rs + stage_to_raw 步骤（与 calc_commodity_elasticity 并列 post_close）
- **⚠️ 教训 4**：很多"看起来合理但永久无人触发"的算法脚本（如 fetch_sector_rs、calc_commodity_elasticity），平时不报错，等到主人质问才暴露。需要定期 audit "raw_data/* 有哪些是 mtime 超过 48h 且无 workflow 触发的？"

## 21:10 商品涨价弹性榜 LC/SA 国内期货（commit 3cd910188）
- 数据生成器一切正常，但完全没有 workflow 调用 + walk_extra 没注册 + 前端 _fillCommodityElasticity 不读 ce.unavailable 数组 → LC/SA 永远不显示
- 三个修复见上文（21:00-21:04 节）
- ⚠️ 家里机 eastmoney 期货接口被风控（Remote end closed），LC/SA 仍归 unavailable 区显示；云端 v8_cn_fetch.yml 加步骤后下次 post_close 会从云端试拉

## 21:16 主人追问：ETF + POTENTIAL_PICKS 不更新（commit f6d7492a0）
- **audit 7 个孤儿 raw_data**：stock_profile (95h)、weekend_meta_report (69h)、triple_history (69h)、gold_pool (52h)、w52_high (27h)、sh_sz_history (27h)、ETF 3件套 (27h) 等 — **全部无任何 workflow 触发**
- **修复**：
  · 新建 algorithms/calc_etf_intraday.py — 东方财富 push2 ETF 实时行情（intraday 数据源）
  · ETF 3件套 + POTENTIAL_PICKS 立即跑 + update_v8.py --category intraday 转 JS（4 个 ?v= 戳更新）
  · v8_cn_fetch.yml + v8_cn_fetch_cloud.yml 加 calc_etf_intraday.py 步骤
- **家里机 eastmoney 全面封** → error=True 兜底明示「数据源待接入/API 受限」+ 21:20 更新时间
- **⚠️ 教训 5**：raw_data/* 是孤儿数据的"事实标准"——mtime 是判断脚本有人管的可靠信号
- **⚠️ 教训 6**：v8_cn_fetch.yml 推 main 撞 409（GitHub 同时构建）—— GET 拉最新 sha 再 PUT

## 21:27 主人追问：⚠️ 三重共识/国际投行 0 只误报（commit d8bfedc08）
- 主人发 3 张截图：① 文件新鲜度卡 ② 两个 ⚠️ 警告卡 ③ 四量/驾驶舱 ✗
- **根因 1**：check_data_cards line 944 把 `stocks=[]` 当"关键字段空值" → warn
  弱市无严格共识时 stocks=[] 是合法状态（差 1 步的在 near_miss 里）
- **根因 2**：check_a_share_coverage line 1466 只要 1 个池 0 就 warn，不分"独立 0 vs 全 0"
  三重共识 + 国际投行 都是"严格共识类"池，弱市天然 0
- **一劳永逸修复**：
  · check_data_cards：区分 None / 空 list / 空 dict（line 934-945）— 空 list 算合法
  · check_a_share_coverage 两轮统计（line 1427+）：
    - 非交易日 / 唯一 0 池 / 2 严格类同 0 → ✅ ok
    - 2 池含非严格类 / n_zero≥3 / 全 0 → ⚠️ warn
    - 任何池 total>0 且 A股=0 → 🚨 URGENT
- **STRICT_POOL_NAMES 名单**（5 个"严格共识/选股"类池，弱市天然 0）：
  {"三重共识", "驾驶舱分档", "逆势龙头", "四量终极", "国际投行信号"}
- **效果**：三重共识/国际投行两条警告从 warn → ok ✅
- **剩余 21 个 fail 是真问题**：数据 mtime 27h+（孤儿数据，需云端跑算法链才能消除）

## 21:33 主人追问：逻辑详解 TAB 是空的（commit b4ec64c0f，-81KB）
- 主人发截图：点"逻辑详解"→ 子 tab 按钮区显示但主面板空白
- **根因**：逻辑详解 sec 内 10 个子 tab 9 个 pane 重复说明一级 nav tab 里的卡片内容，1100 行 81KB 冗余
- **一劳永逸重做**：删 9 个 pane → 保留 2 个
  - **📊 总览**（lg-ov）：1 张大总览卡（导航结构 + 数据源总表 + 审计铁律 + 部署流水线 + v6 永久铁律）
  - **📦 v6备忘录**（lg-v6）：永久铁律 DO_NOT_DELETE，guard_v6_memo.py 部署闸门保 v6_memo.html 完整
  - switchLg 函数不用改（已通用 getElementById）
- **影响**：index.html -81KB（790KB → 709KB），主体 10 个一级 Tab 不动
- v6_memo.html / v6_memo.golden.html / guard_v6_memo.py / v6 备份 tar.gz 全部不动（永久铁律）

## 21:41 主人追问：板块周期阶段迁移对比表要自动产出（commit 已生成，5 files）
- 主人 21:41 发板块周期截图（"旧 8-14 17:24 vs 新 21:10"），要"放进盘后数据出来后的更新里"
- **实现**（一劳永逸）：
  · `fetch_sector_rs.py` 加 `_save_phase_snapshot()`（line 19-69）：neodata 成功/同花顺成功后都追加 snap，保留 30 天，覆盖当日
  · `update_v8.py` 加 `sector_phase_history.json → SECTOR_PHASE_HISTORY.js` 映射
  · `index.html` Block 3 板块周期：从读 `localStorage`（清缓存就丢）改读 `window.SECTOR_PHASE_HISTORY.snaps`
  · 顶部新增「🔁 阶段迁移对比表」：阶段/旧(日期+HH:MM)/新/Δ，5 阶段行
  · `index.html` <head> 加 `SECTOR_PHASE_HISTORY.js?v=86228acdd2` 引用
  · `api_push_raw.py` walk_raw 全量推（自动含 sector_phase_history.json）
- **?v= 戳教训**：SECTOR_RS.js ?v= 42118db17a 是 update_v8 **neutral_sha**（剔除 republish_time），**不是 raw sha**。
  之前手动算的 6fc7992eb1（raw sha）会与 update_v8 跑出的戳冲突 → CDN 缓存键失效。
  ⚠️ 教训：不要手算 data/*.js 的 ?v=，**让 update_v8._rewrite_index_html_cache_busters 自动写**。
- **结果**：云端每日 post_close 自动累积阶段快照（不再需要主人手记对比表），明天跑就有"今日 vs 昨天"自动对比。

## 21:53 主人紧急令：交易日超 24h 必报+自愈闭环（写进铁律）
- **主人原话**：「交易日超出 24小时就要报警自愈闭环！写进铁律，周末假期要延算到T+1」
- **实现**：`v8_health_check._hard_cap_for_owner_rule(n=None)`（line 875）
  - 交易日 cap = 1440 min（24h 红线）
  - 非交易日 cap = `next_trade_day 18:30 - last_close` 分钟数（自适应长假，下限 24h）
    - 例：周五 15:30 出的数据，最迟应在「下周一 18:30」前出现新值 → 上限 ~75h（4500 min）
    - 长假（如国庆连休 7 天）自动顺延到 T+N 18:30，不写死
  - 接入方式：`adjust_max_age` 返回前 `min(max_age, _hard_cap_for_owner_rule())` 叠加
- **CARD_DEFS 顺手补 4 张盘后卡 heal_cat**：MARGIN_DATA / CFFEX_HOLDINGS / CRISIS_DATA / MARKET_FUND_FLOW_DATA → "premarket"
  （cloud_fetch_v8 实际把这 4 张注册在 premarket 而非 post_close，必须显式覆盖）
- **写入 MEMORY.md 永久铁律**（§双层审计后 新增"⏰ 数据陈旧铁律"）
- **立即验证**（22:30 跑）：29 fail 全命中 24h 红线（昨日 14-19 点出的数据到现在 ≥ 24h），按 heal_cat 自动派发对应 workflow——21 张盘后/选股策略走 algo_run，6 张实时数据走 intraday，4 张今日事件走 premarket
- **效果**：`v8_daily_audit.yml` 22:30 + `v8_health_patrol.yml` 1 分钟级 health_check 都自动命中此铁律，主人未来不再需要逐项问"为什么没更新"

## 22:00 主人怒令：每个前端的算法都全面审计（一劳永逸三件套，commit a72f4cdf2）
- **主人原话**：「今晚怎么发现这么多错的，每天你审计个毛线啊！每个前端的算法都全面审计！！！发现问题马上一劳永逸式修复，自愈闭环！」
- **真根因**：81 个 data/*.js **只有 28 个在 CARD_DEFS 审计范围**，53 个从未被审计（SECTOR_RS/COMMODITY_ELASTICITY/MAHORO/NORTH_FUND/STOCK_QUOTE/TRIPLE_HISTORY 陈旧 3 天没人报警）
- **修复 1：check_all_data_files() 全覆盖审计**
  - 自动扫描 data/*.js 全集，未登记 CARD_DEFS 的按通用规则（24h 红线/T+1 18:30）审计
  - load_window_var 兼容 IIFE 壳（STOCK_MOMENTUM_STATE var data）+ window 变量名≠文件名别名（STOCK_MOMENTUM_STATE_V2→ENHANCED / PORTFOLIO→PORTFOLIO_DATA / STOCK_RPS→STOCK_RPS_DATA）
  - 低频豁免名单 7 天红线：STOCK_PROFILE/WEEKEND_META_REPORT/PORTFOLIO/PORTFOLIO_COST/CONCEPT_ETF_MAP/OPTIMIZED_STRATEGY/BACKTEST_TDX/BLOAT_CHECK/HEALTH_CHECK/RUNNER_STATUS_HEALTH
  - CONCEPT_ETF_MAP 非严格 JSON（JS 注释+无引号键）→ 体积检查兜底
- **修复 2：run_algorithms ORDER 补孤儿**：calc_sentiment_cycle.py（→SENTIMENT_CYCLE.js）+ refresh_dividend_cninfo.py（→STOCK_QUOTE 分红），之前零调用
- **修复 3：防自愈风暴 _cn_runner_available()**
  - 风暴根因：alimi-cn offline 时 patrol 每分钟兜底派发 → Set up job 全失败（14:02-14:09 连续 8+ failure/cancelled）→ 死循环
  - 修法：_dispatch_cn_fetch / _dispatch_algo_run 派发前查 runner 在线状态，全 offline 跳过本轮
  - **已拉起家里机 runner：D:\actions\cn-runner（alimi-cn，昨天 19:01 后离线 → 已 online）**——家里机重启后 runner 服务不会自启，若 alimi-cn 又 offline 需手动跑 run.cmd
- **补跑**：已派发 v8_algo_cloud + v8_cn_fetch_cloud(all)（204），云端自动刷新 55 个陈旧 raw_data
- **审计工具**：`_tmp_audit_pipeline.py`（data↔脚本↔workflow 三向对齐，可复用）
- **教训**：审计必须覆盖「全部 data 文件」而非「登记过的卡」；新增数据文件若没同步登记 CARD_DEFS 就永远无人监控——check_all_data_files 从根上解决（未来新文件自动纳入）

## 22:18 主人质问「ETF 三合卡被覆盖」：calc_etf_intraday 写空数据覆盖小九真版本（commit 3b17d3b23）
- **主人截图**：「ETF 三合 - 盘中异动 + 资金热度 + 日监控」卡片，全是「暂无数据」+「eastmoney push2 失败: Remote end closed」
- **根因**：我 21:20 触发 `python algorithms/calc_etf_intraday.py` → 家里机 eastmoney 被风控 → fetch_etf_list() 返回 None → 脚本仍写 `{error:True, items:[], note:'失败...'}` 空结构 → 把小九下午 (7f264e91a) 的真版本（1564 只 ETF + 100+ items）覆盖了
- **一劳永逸修**：calc_etf_intraday.py line 73 网络失败时直接 sys.exit(0)，不写盘 + 提示云端 category=intraday 补救
- **恢复**：从 7f264e91a checkout 恢复 3 个文件（小九真版本）：
  - ETF_PULSE.js (1238B, ?v=a44f032ae3) / ETF_INTRADAY_HEAT.js (26371B, ?v=ecbd3bc569) / ETF_DAILY_MONITOR.js (1518B, ?v=4dce29fa4b)
  - index.html 3 处 ?v= 已更新
- **铁律写入 MEMORY.md**：所有「网络/上游失败」分支都禁止写空结构覆盖现有真数据 —— 必须 sys.exit(0)，下次整改 fetch_lhb/fetch_inst_trade/fetch_sector_rs/fetch_stock_quote_v8 等同类脚本
- **教训三连发**：①cn runner offline → 自愈风暴 ②CARD_DEFS 只覆盖 35% → 53 个文件无人审计 ③空结构覆盖真数据 → 今晚 3 类独立故障的共同根因都是「本地脚本缺防御」

## 22:22 主人质问「TOP5 跟踪卡 3 只为什么没数据」（commit ba248e820）
- **根因**：3 只 Top5 入场日 fetch_stock_quote_v8 没拉到当日收盘价（06862 海底捞港股 fetch 失败 + 301583/002552 A 股 fetch 时未收录）→ entry_price 写入 null → 后续 `if entry_price and last_close:` 永远 False → 盈亏%/最新价/峰值% 全锁在 null
- **一劳永逸修**：`gen_top5_track.py` line 213-219 — 每次维护追踪池时，若 entry_price 仍为 null 但 qmap 里有 close → 兜底用最近价作为 entry（前端会标"补"），让后续 fetch 补到数据时自动恢复
- **云端在跑**：v8_algo_cloud 22:24 running（我刚才 dispatch 的），跑完后会刷 stock_quote + top5_track → 兜底逻辑生效后 3 只不再空白
- **同模式威胁清单**：gen_algo_track.py / gen_lhb_7d.py / gen_triple_track.py 等追踪类脚本可能同问题，下次专项审计
- **整改建议**：fetch_stock_quote_v8 跑完后加 topup_top5_quotes.py 主动补抓 final_recommend Top5 + 当前 tracking stocks 的最新报价

## 22:25 主人质问「走势特征/K线复盘 +N 不可点」（commit 91f5c21c7）
- **主人截图**：「走势特征」+「K线复盘」两个卡片 +N more 全部点不了 + 感觉是"不折叠"
- **根因**：原 index.html line 9417/9497 用 `stocks.slice(0,8)` 只渲染前 8 条 + `<div>+N more</div>` 纯文本（**无 onclick**）→ 主人点了没反应，剩下 N-8 条根本没在 DOM 里 → 主人感受是"被覆盖"实际是"按钮坏着"
- **一劳永逸修复**：
  - 渲染**全部**（不 slice）+ 第 9+ 条默认 display:none
  - 「+N more」改真按钮（紫色 + cursor:pointer + onclick 切换 hidden + 文案变"收起▲"）
  - 走势特征 1 个按钮控制"领涨摘要"+"详细列表"两处第 9+ 行
- **铁律写入 MEMORY.md**：任何"+N more"类折叠提示必须用 `<button>` 或带 onclick 的元素，禁用纯 `<div>` 文案
- **同模式审计**：grep `+N more` / `+N 只` / `点击查看更多` / `展开全部` 等文案，确认都带 onclick
- **部署**：index.html sha `4497904b21`，Pages build queued，主人刷新即可用 +N 按钮展开全部股票

## 22:34 主人质问「平均股价被覆盖」(commit fd030de7a + 同步 80 个文件)
- **主人 5 张截图质问**:
  1. 平均股价头部 (29.39 +1.78% / vs MA20+MA60 / 样本 5281 只)
  2. 买/加:单日暴涨 1.8% · 追高风险(不显眼)
  3. 「③ 样本 5281 只 超卖轻色」+ 综合结论
  4. 文字:「我说的是小九下午新做的主力跟踪的卡被覆盖没了,我记得是10分钟刷新一次」
  5. 文字:「怎么又没了?」
- **真根因(不是覆盖)**:**本地 git main 落后云端 origin main 1 小时**——本机 GitHub push 网络封(TCP 443 reset),用 Contents API 推了部分文件但**没反向 fetch 同步云端任何变化**。主人刷新页面看到的是云端真版本(29.391),但本地 main 还停在 19:34 之前的旧版本
- **云端真版本确认**:`git log` 显示云端 main HEAD 是 `4e40e1d96 (8-17 21:26)`,AVG_PRICE_DATA.js 已是 29.391 +1.7849% (sha 44446804)
- **永久修复(一劳永逸)**:
  1. **Contents API 拉 80 个文件云端 → 本地**(用 PAT 走 API,不依赖 git 协议) — 包括所有 data/*.js + index.html,AVG_PRICE_DATA 29.391 现在本地就有
  2. **MEMORY.md 永久写入「⏰ 本地 ↔ 云端同步铁律」**:① Contents API 是唯一稳定同步手段 ② 每 5-10 分钟定时反向拉云端 → 本地 ③ 指标卡陈旧度报警(v8_health_check 增 check_origin_sync)④ 部署后必须 verify sha
  3. **平均股价卡买/加 + 卖/减 信号显眼化**:index.html line 1615 后面,绿色/红色双边框 + 阴影 + 加粗(padding 3px 8px + border 1.5px + box-shadow),挪到「样本 N 只」紧后面。删除原紧凑列表避免重复
- **额外发现(小九 bug)**:`f_avg_price` history 数据有重复(8-14/8-15/8-16 三天都是 28.5565),导致 ma20/ma60 算出来相等(应不同)。MEMORY.md 记录,下次专项改:history 写入前按日期去重 + 连续 3 天重复触发 re-fetch 自愈
- **本机网络封 GitHub 的永久方案**:用 Contents API(HTTP 走 GitHub API 主域 github.com 而不是 git 协议)已确认稳定可用
- **部署**:index.html sha `3fcc580b2b`,Pages build queued

## 22:55 主人令「阶段迁移对比改成一行一条」(commit a605fd7ec)
- **主人原话**：「写成一行，把每天从哪个阶段到哪个阶段写清楚即可，不是让你累积。比如黄金从主升转到震荡，把当天有变化的写明」
- **真根因**:原 line 6524-6585 是累积表（4 列「阶段/旧/新/Δ」），主人截图显示主升18→18±0 / 启动10→10±0 / 震荡61→61±0 / 退潮1→1±0 全是 ±0 没意义
- **一劳永逸改版**(index.html line 6524 起):
  1. 删除 4 列累积表（主升/启动/震荡/退潮/底部 旧 vs 新 vs Δ）
  2. 改成 `板块名 + 「🚀主升 → ➡️震荡」` 一行一条（中文拼音排序）
  3. 无迁移时显示 `📭 今日无板块阶段变化（旧日期 HH:MM → 新日期 HH:MM 期间）`
  4. 标题改 `🔁 今日阶段迁移（旧日期 HH:MM → 新日期 HH:MM）`（去掉「累积」字样）
  5. 末尾显示「共 N 个板块今日有迁移」
  6. 数据源 SECTOR_PHASE_HISTORY.js（云端持久化 snaps，作为数据源正常用；只是展示不累积）
- **删除旧变量**：`_prevCounts / _curCounts / _phaseOrder / _transCount` 已全部删除（grep 0 处后续引用）
- **部署**：index.html sha `efaf54b5e6`，Pages build queued
- **铁律写入 MEMORY.md**：阶段迁移类卡永远只显示「当天有变化」，不要恢复成累积表——主人看了嫌「累积」没意义

## 23:05 主人质问「全空了+导航按不了」(没改代码，是数据问题)
- **主人 8 张截图质问**：选股策略·三重共识多个子 tab 空 / 🔒 暂未上架空 / 顶部 nav 部分🔒
- **我审计了什么**（数据真根因）：
  - **COCKPIT_ADVICE / COCKPIT_TIER_RECOMMEND / FINAL_RECOMMEND_DATA / STOCK_RPS / CRDS_CARD_DATA / MAHORO / TRIPLE_CONSENSUS** — 7 个 `update_time` 都是 8-17 17:30-21:20 但 **`stocks.length = 0`**！**小九算法 8-17 弱市严格筛出 0 只**（不怪代码）
  - **POTENTIAL_PICKS / SENTIMENT_CYCLE** ut=8-17 14:29 0 只
  - **ETF_PULSE / ETF_INTRADAY_HEAT / ETF_DAILY_MONITOR** ut=8-16 14:17（cn runner 17:00-22:00 offline 时段没产出）
  - **FOUR_VOLUME** ut=21:20 stocks=1 → 4 个分类各 0 只（弱市符合）
- **真根因**：8-17 周一全天 algo + cn_fetch 几乎没成功跑（22:00 前 alimi-cn offline + lemoncat busy 失败）
- **不是覆盖**——CARD_DEFS 30 张卡审计正常，4 张盘后卡 heal_cat 已补，check_all_data_files 已覆盖 53 个未登记卡
- **已派发 3 个云端 workflow 补跑**（204）：v8_algo_cloud + v8_cn_fetch_cloud all + v8_cn_fetch_cloud post_close，预计 5-10 分钟云端刷出非 0 真值
- **教训**：「空卡」= 数据陈旧/弱市，不是覆盖。先 check `stocks.length` 确认。alimi-cn 17:00-22:00 offline 是根因
