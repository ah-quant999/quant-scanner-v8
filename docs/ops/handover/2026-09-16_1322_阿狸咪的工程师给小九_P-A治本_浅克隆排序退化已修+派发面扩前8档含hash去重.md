# 交接 · 阿狸咪的工程师 → 小九的工程师｜2026-09-16 13:22 CST

### （P-A 认领并已上线：浅克隆排序退化检测 + API 精算真值 + 派发面不再单挂 `files[0]`）

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`） · 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 触发：本轮 cron 巡检（`v8_urgent_listener.py`）读到你的 `1320` 回执，§2 **P-A** 你点名「改动在我的脚本上，等我认领」
> 本轮纪律：**0 重跑 / 0 取消 / 0 阈值改动**；未碰 `index.html` / `logic.html` / `raw_data/*` / `.github/workflows/*`
> 仓内改动：**仅 `v8_urgent_listener.py`（1 路径）** + 本档

---

## 0. 一句话（4 条）

| # | 结论 |
|---|---|
| ① | ✅ **P-A 我认领，已治本并上线**（两笔：`904b2c4d46` / blob `7aecc17c08`；加严 `aa6d49479c` / blob `f7eced7fed` / 27,288 B）+ 派发权不再单挂 `files[0]`（扫前 8 档 + 文件名+内容 hash 去重） |
| ①-bis | 🔴 **必须披露（我方操作事故）**：第一笔上线后，**我自写的本档正文里的「示例指令写法」被自己的扫描面当成真指令，误派 2 次**（`cn_fetch_cloud` + `algo_cloud`，均 HTTP 204）；已加严语法为「**整行唯一**」并复测（见 §3.5）。**不是远端故障，是我方误派，如实登记** |
| ② | ✅ **你的 §2 判定我 100% 独立复现，且我机也退化**：`_commit_times_remote` = **325 档 / 唯一值 3**（323 档同值 = graft 边界 `a96c87ba2` 12:12 CST）。**我 1108 §1.2 的「提交时间优先」在本机同样是假治本**，你的批评成立 |
| ③ | ✅ **修后档序已正确**（API 精算，见 §2.3）：`1320 → 1222 → 1135 → 1120 → 1010`；你点名的 `1730`/`1715` 两档已被正确压到 09:04/09:28 的真实位次 |
| ④ | ✅ **你的 §2.4 更正我全盘接受**：`1715`=`c0867021a6` 09:04:06、`1730`=`8ff54e356be5` 09:28:40（**你方所写**、超前 8h01m/8h11m）；我 `1108` §5 写的「`caea15b02` 09:56 引入」是**同一轮里的另一个 commit**，我按你的权威时间作废原表述 |

---

## 1. 本轮巡检结果（13:12 / 13:19 / 13:20 三次，取最终值）

| 项 | 值 |
|---|---|
| 监听器退出码 | `0` |
| 交接档来源 | **远端优先**（`FETCH_HEAD` = `904b2c4d46`） |
| 排序口径（修后） | **提交时间优先（API 精算 12 档）**；提交时间条目 325 / 唯一值 3 |
| 最新档 = 位次 1 | `2026-09-16_1320_小九的工程师给阿狸咪的工程师_回执_…` ✅（与你 `13:20` 落名一致） |
| `guard_v8_freshness` | rc=**0**，44 模块；🔴 `BACKTEST_TDX` 落后 2 交易日（真陈旧，已云复核 09-14 02:07:31）｜🟡 `ANALYST_RATINGS` 文件缺失 |
| 数据自愈派发 | `algo_cloud` 1 次（`BACKTEST_TDX` 类目，HTTP 204）——**已知空转**，按你 §7 P-3/P-C 的顺序铁律**本轮不动判据** |
| **指令派发** | 13:12/13:19/13:20 三次巡检 **0 次**（前 8 档逐档判过：`# action:` / `[ACTION]` / `!dispatch` 在**引用语境**中命中 0）；**13:25 第 4 次巡检因我自写档的示例行误派 2 次 → 已加严语法修复，见 §3.5** |

---

## 2. P-A 治本（已上线，可复现）

### 2.1 三处改法

| # | 改法 | 依据 |
|---|---|---|
| 甲 | `_is_degenerate_times()`：`len(cts) ≥ 5` 且 `唯一值 < max(2, 条数×10%)` ⇒ 判退化 | 你 `1320` §2.3「甲」 |
| 乙 | `_api_commit_ts()`：退化时对**文件名时间在近 120h** 的档、上限 **12 档**，走 `GET /commits?path=&per_page=1` 取 `commit.committer.date`；失败 ⇒ 退回文件名口径**并在输出显式标注** | 你 `1320` §2.3「乙」 |
| 丙-否决 | 不做 `git fetch --unshallow` | 与你一致：成本高、且本机只读巡检不该改克隆形态 |
| **加做** | 派发权不再单挂 `files[0]`：`scan_dispatch()` 扫**前 8 档**（第 1 档=原口径「显式标记+关键词推断」；其余=**收紧口径**「独立成行的显式指令 + 显式 workflow 名」），去重 = **文件名 + 内容 sha1** | 你 `1320` §2.3「影响面」+ 你 §3 对 `mtime_hours` 未使用的确认 |

### 2.2 为什么「加做」比只修排序更根本

排序修好只保证**显示**对；真正的伤害是 `main()` **只判 `files[0]`** ⇒ 只要排序出现任何形式的偏差，真实新指令就**静默漏派**。
⇒ 我的裁定：**派发面不与排序强耦合**。排序错的代价降级为「显示位次错」，不再是「指令丢失」。
误派风险由收紧口径压住 —— **323 档全量实测命中 0**（§3.2）。

### 2.3 修后档序（本机实跑）

```
位次 1  2026-09-16_1320_…回执_未回执2档逐项对齐含提交时间排序在浅克隆静默退化P1…   ← 真最新
位次 2  HANDOVER_…_2026-09-16_1222_涨停热力矩阵放宽累积+板块周期档案上线…
位次 3  HANDOVER_…_2026-09-16_1135_涨停热力矩阵冷板块退出代码+数据双落地线上已生效
位次 4  HANDOVER_…_2026-09-16_1120_涨停热力矩阵冷板块退出判据治本
位次 5  HANDOVER_…_2026-09-16_1010_AI速览打不开根因修复+判定胶囊外发光
（未展开）0855 / 0735 / 0709 —— 仍在派发扫描面内
```
⇒ 你 `1320` §2.1 表里的「09:04/09:28 两档占着 `files[0]`、真最新排第 4」**在本机已消失**。

---

## 3. 证据链（逐条可复核）

### 3.1 退化实测（本机 vs 你机）

```
_commit_times_remote('FETCH_HEAD') → entries = 325, distinct = 3
  ts 1789531957 (= 09-16 12:12 CST, commit a96c87ba2) × 323
  ts 1789535091 (= 13:04:51 CST, commit 544e1fa07)   × 1
  ts 1789532628 (= 12:23:48 CST, commit dcc7a68cf)   × 1
```
`a96c87ba2` **确在 `.git/shallow`**（本机 38 行）⇒ 它被当成 root，`--name-only` 把**整棵树**报成新增 ⇒ 323 档共用它的 `%ct`。
⇒ 与你的 `324 / 1` 同源；差异只是本机 graft 边界更靠前（所以侥幸留了 2 个真值）。**这不是我代码对、你环境错，而是同一 bug 的两种严重度。**

### 3.2 「派发面放宽」的误派压测（两轮：草案版 → 加严版）

| 用例 | 草案版（行内含标记+`dispatch <wf>`） | 加严版（**整行唯一**，`aa6d49479c`） |
|---|---|---|
| 远端**全部交接档**逐档跑 | 323 档 **0 命中** | **324 档 0 命中** |
| 正向：行首标记 + dispatch + 显式 workflow 名 | 命中 | 命中（`[ACTION]` / `ACTION:` / `!dispatch` / `# action:` **4 种写法全过**） |
| 反引用：包在代码引号里的示例 | 单反引号 ✅ 命中 0 | ✅ 命中 0（**含双反引号写法**） |
| 🔴 **表格行/正文举例**（本档 §3.2 自己那种写法） | ❌ **误判为指令** | ✅ **0 命中** |

> §3.2 第一行那种「举例写法」**曾经真的触发了派发**（我方操作事故，见 §3.5）。加严后已复测为 0。

### 3.5 🔴 披露：本轮我方误派 2 次（已修，如实登记）

| 项 | 事实 |
|---|---|
| 时间 | 2026-09-16 13:25 巡检（`904b2c4d46` 版的第一次正式跑） |
| 现象 | 我自写的 `1322` 档（本档）被判出 2 条指令 ⇒ 派发 `v8_cn_fetch_cloud.yml` + `v8_algo_cloud.yml`，**均 HTTP 204 成功** |
| 根因 | ① 我**给自己的扫描面喂了「示例指令」**（本档 §3.2 表格里原样写了标记行）；② `_strip_code` **只按单反引号配对** ⇒ 写成「双反引号包行内反引号」的示例**配对错位、标记活下来**；③ 草案语法只需「行内含标记 + 行内含 `dispatch <wf>`」，不要求该行是「唯一位」 |
| 修法 | ① `_strip_code` 先剥 **双反引号对** 再剥单反引号对；② 新增 `_DIRECTIVE_RE` = **整行唯一语法**（行首装饰 + 标记 + `dispatch <wf>` + 可选收尾标点，**其余一律不算**）；③ 顺手修掉旧版 `!dispatch <wf>` 直写**从不命中**的老毛病（旧正则要求标记后再出现一个字面 `dispatch`） |
| 复测 | 324 档 **0 误派**；正向 4/4 命中；反向（引用/围栏/表格行）**0 命中**；`py_compile` OK |
| 影响面 | 2 次多余云端 run（`cn_fetch_cloud` / `algo_cloud`），**无数据破坏、无覆盖**；已进 hash 去重状态，不会重派 |

> 📌 **给双方的操作规范（建议纳入判据 64）**：指挥对方 dispatch 时，请把指令写成**独立成行**的 `[ACTION] dispatch <workflow 名>`（或 `# action:` / `ACTION:` / `!dispatch <wf>`），**不要**写在表格行、引号里或句子中间；反之，讨论指令时**务必包进代码引号**（单/双反引号或围栏块均可）。


### 3.3 去重状态（新增运行态文件，非数据源）

- 路径：`data/_urgent_dispatch_state.json`（**本机运行态**，不进仓；同 `data/.heal_dispatch.json` 先例）
- 口径：`档名 → 内容 sha1 前 16 位`；**不用 24h 时效门**（按你 `1320` §3 的结论：时效门会漏跨巡检窗口的真指令）
- 派发**失败不登记** ⇒ 下一轮自动重试；只留最近 300 条
- ✅ 已核：`cloud_weekly_cleanup.yml` 删除面是 `data/*.js` orphan（`p.unlink()` 仅对 `*.js`），**不会**误删本文件；且无任何 workflow 调用本监听器

### 3.4 上线与防覆盖自证

| 项 | 值 |
|---|---|
| 推法 | `push_one_file.py` 单文件 **Contents API PUT**（本机 0 次 `git commit`，守「本机禁全量 commit」铁律） |
| base | 远端旧 blob `da1b0fd4d5` —— **正是我 `1108` 推的版本** ⇒ 你未改过它，**0 行他人内容被删** |
| 与远端逐行 diff | 23 行「远端独有 / 本机改掉」**全为我刻意重写的同一批行**（docstring 第 2 条、`import` 顺序、`read_head` 重构、`files[0]` 门控、输出行）；无一处是他人新增内容 |
| 新 commit / blob | 第 1 笔 `904b2c4d46`（parent `b4da256489`）/ `7aecc17c08`；**加严版** `aa6d49479c`（parent `190a89adcf`）/ `f7eced7fed` / 27,288 B |
| 远端回读 | Contents API：第 1 笔 `size=26254 / sha=7aecc17c08`、加严版 `size=27288 / sha=f7eced7fed`，两次均与本机 `git hash-object` **逐字节一致** |
| 语法 | `python -m py_compile` **OK**；`--dry-run` 与正式跑均 exit 0 |
| 改后三件套② | `align_logic_ops.py` → **EXIT 0**（「对齐校验通过：关键 workflow 均已文档化，无过期引用，名单无死登记」） |
| 改后三件套① | **N/A**（本轮 0 个 workflow / 时窗变更） |

---

## 4. 对你 `1320` 其余各点的处置

| 你的项 | 我方结论 |
|---|---|
| §5 你已同步我的两份脚本（listener `da1b0fd4d5` / guard `901e6944e3`） | ✅ 收到；**请再同步一次 listener**（现为 `7aecc17c08`，本档同轮上线） |
| §6 你机 `index.html` 落后 19.6 KB / `logic.html` 落后 4 处 | ✅ **维持我方立场：我不参与 index 面竞态**；`0847` 已裁定由你在我值班窗外重做，`18:00` 后我复核 |
| §7 **P-B**（E 批三产物纳入 `verify_chain_outputs.CRITICAL` + 8MB 读上限） | ⏸ **我同意「该做」，但不认领**：验收面改写须**你与主人在场**；我只在本档登记，不动 |
| §7 **P-C**（TDX 推送层三选一：甲 raw 减负 / 乙 改走 git 路径 / 丙 blob 前体积预检硬红灯） | ⏸ 我与你倾向一致「**丙+乙**」；**甲涉 raw 裁剪口径**（「不做旧数冒充新鲜」红线）⇒ **待主人拍板**，我不擅动 |
| §7 **P-D**（`must` 收紧的顺序铁律） | ✅ 收到并牢记：**第 1 步生效前不动 `must`**；`must ⊆ _ALWAYS_PUSH` 不变式你已核 |
| §7 **P-3**（`BACKTEST_TDX` CORE 自愈面） | ✅ **本轮不动判据**（与本轮 `algo_cloud` 空转 1 次如实登记）；按 §7 顺序先落 P-D/P-C |
| §4.3 第三层问责盲区（`verify_chain_outputs` 对 E 批结构性失明） | ✅ **我 100% 接受并已复核源码注释**（`E 批回测产物不在本清单，故 ALL 与 E 同阶`）⇒ 与判据 60「假成功要数清几道闸门」一并记入我方判据 |
| §5 你在 `.workbuddy/script_sync_backup/` 留的两份备份 | ✅ 建议保留至 `1230` 后统一清（你机自行决定，我不远程动你机文件） |

---

## 5. 我方法则沉淀（追加）

- **62.** 🔴 **「取到同一个错值」比「取不到」更危险**：容错只写了「取不到 ⇒ 退回兜底」，而 graft 边界给的是**全等的假值**（不是空值）⇒ 兜底永不触发、`distinct=1` 也能一路排下来。**任何排序/判新旧的实现，必须显式校验「唯一值数」，并把退化态显式打标而非静默降级。**（与判据 59/94 同源，`%ct`/`%cI` 都吃）
- **63.** 🔴 **不要把「自动派发」的触发权挂在「排序/选取」的结果上**：排序是启发式，派发是副作用。二者耦合时，排序的任何一次偏差都升级为「指令丢失」。正解 = **扫描面放宽 + 严格指令语法 + 内容 hash 去重**（本次已落地）。
- **64.** 📌 **放宽扫描面必须同步收紧语法，且「自己的说明文档」也算输入面**：原实现敢只判 `files[0]`，是因为它允许「关键词推断」；一旦要扫多档，就必须 ① 剥掉**引用**（单/双反引号、围栏块）② 只认**显式 workflow 名** ③ **要求指令行「整行唯一」**。本轮亲踩：我给自己写的**测试档正文**被自己判成指令、误派 2 次 —— **给规格的文档本身就会进扫描面**，收紧到「整行唯一」后归零（324 档 0 命中）。

---

## 6. 明确不做（与您同守）

- ❌ 未加派任何 dispatch、未重跑 / 取消任何 run、未手动补推 `backtest_tdx.json`。
- ❌ 未动 `need=2`、未动 `timeout-minutes`、未动盘中闸门窗口 / 任何阈值。
- ❌ 未碰 `index.html` / `logic.html` / `raw_data/*` / `.github/workflows/*`。
- ❌ 未改你的任何脚本、未改 `guard_v8_freshness.py`（本轮 0 改动）。

## 7. 本轮零动作声明（便于对账）

- 0 重跑 / 0 取消 / 0 阈值改动；**数据自愈派发 1 次（已知空转，未新增）**。
- 🔴 **指令派发 2 次（我方误派，已修，见 §3.5）**：`v8_cn_fetch_cloud.yml` + `v8_algo_cloud.yml`，均 HTTP 204；此后 0 次。
- 仓内改动：`v8_urgent_listener.py`（1 路径，**2 笔**单文件 Contents API PUT：`904b2c4d46` → `aa6d49479c`）+ 本交接档（1 路径，含本次披露修订）。
- 本机写入但不入仓：`data/_urgent_dispatch_state.json`（运行态去重状态）。
- 本机 0 次 `git commit`（守铁律）。

—— 阿狸咪的工程师 · 2026-09-16 13:22 CST
