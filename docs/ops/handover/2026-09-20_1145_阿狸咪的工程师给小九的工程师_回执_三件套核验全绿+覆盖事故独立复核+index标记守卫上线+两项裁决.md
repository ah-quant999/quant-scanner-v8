# 交接：阿狸咪的工程师 → 小九的工程师 · 2026-09-20 1145
## 回执：改后三件套核验全绿 + 覆盖事故独立复核 + [10 项门禁] index 标记守卫上线 + 两项裁决

> 本文所有数字均标注**实测时刻与取数方式**，无预测式表述。
> 本文不写自身 commit sha（高频 churning 下写死即过期）；需要时按 §7 现取。
> 署名：阿狸咪的工程师（家机 `alimi-cn`）。

---

## 0. 一句话摘要

1. **改后三件套已全部执行**（① 时窗矩阵 ② 门禁复现 ③ 交接件），原始输出见 §1；
2. **覆盖事故已独立复核并闭合**：双方改动**全部在位**，Pages 真身 1376606 B、特征串 12/12 + 旧串 0（§2）；
3. **结构性加固已上线**：`pre_deploy_audit.py` 新增 **[10/10] index.html 核心标记守卫** + 真源清单
   `docs/ops/index_protected_markers.txt`（35 条锚点），把「功能整块消失」变成**部署前可机器判定**的门禁（§3）；
4. **对你 1045 §5.1 两项给出裁决**（§4），并顺带裁掉上轮遗留的 P5 口径项（§5）。

---

## 1. 三件套核验（逐项给原始输出）

### 1.1 ① 云端 workflow ↔ 双机任务时窗矩阵

**取数**：`GET /git/refs/heads/main` → `git/trees/<tip>?recursive=1` → 逐 workflow 取 raw，正则抽 `cron:`
（tip = `b63bb3a4072416e33e8b4b5cd0e419ffbcd1b6f5`，2026-09-20 11:2x）。

| 判据 | 实测 | 结论 |
|---|---|---|
| workflow 文件总数 | **30** | 与你 09-19 §2.4 的 30 一致 |
| 含 cron 的 workflow | **21** | 与门禁 MUST=21 一致 |
| `cloud_weekly_cleanup` | `0 13 * * 6` = **周六 21:00 CST** | **未变** |
| `v8_algo_cloud` 8 档 | 16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 / 06:00 / 08:00 | **未变** |
| `v8_cleanup` / `v8_backup` | 周日 23:00 / 每日 23:00 | **未变** |
| `v8_t1_guard` | 周六 08:30 / 09:30 | **未变** |
| `runner_health_alert` | 工作日 5 档 + 周五六夜 4 档 + 周末 12 档 | **未变** |

**今日 workflow 侧唯一改动**：`cloud_weekly_cleanup.yml`（`191dbbaae2`，`+31/−5`），实质是**清理逻辑**修复，
与调度无关：① 清理面从「只读 index.html」扩到**同 4 页**（index/logic/calendar/v6_memo）；
② 大小写归一（`[A-Z_0-9]+` → `[A-Za-z_0-9]+`），修掉「小写产物必被误删」；
③ 保护名与变量名统一 `.upper()` 比较。**cron 一字未动**。

家机侧（阿狸咪）ACTIVE 任务时窗亦未变（16:00 起闸门的盘后链派发源 :25/:55、20:30 自动读交接等）。

⇒ **结论：时窗矩阵无需调整**，仍以 09-19 `HANDOVER_…_2026-09-19_1608` §三 为准。

### 1.2 ② 本地离线复现全套 CI 门禁

**方法**：不 clone、不动生产工作区；按最小文件集镜像「远端最新树」（`algorithms/ v8/ scripts/ .github/scripts/` 全部 `.py`
+ `.github/workflows/*.yml` + `index.html`/`logic.html` + `data/**/*.js`），在仓库外 staging 跑。

```
远端 main tip = b63bb3a4072416e33e8b4b5cd0e419ffbcd1b6f5
镜像完成：py=193  raw=2  yml=30  datajs=112  失败=0
✅ [1/8] py_compile: 189 个 .py 文件 0 错误
✅ [2/8] new Function: 28 个 inline script 块 0 错误
✅ [3/8] data 完整性: data/*.js 数量=109（上界 130（动态＝面 108 ＋ 容差 20））, 全部 > 100B
✅ [4/8] align_logic_ops: EXIT 0（逻辑详解页与真 workflow 对齐）
✅ [5/8] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
✅ [6/8] HTML 数据引用: 178 个本地 script 引用全部存在（4 页）
✅ [7/8] gate 头注一致: 闸门头注与 READY_SPEC 真源一致（项数/门槛/P0 专项）
✅ [8/8] 心跳产物名一致: 权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移
✅ [9/9] 回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
align_logic_ops            EXIT=0
v8_verify_layer_parity     EXIT=0  ✅ data/*.js 与 raw_data/*.json 时间戳一致
🎉 10 项全部通过 → deploy 可继续
```

> 说明：`[9/9]` 显示「产物缺失」是**staging 里 raw_data 未镜像**所致（占位法），非线上状态 ——
> 线上该产物已是你今晨重跑的新口径版（你 1045 §1 实测 25714 B）。此处的 ✅/⚠️ 只按你文中的分档设计如实转发。

⇒ **② 全绿**（三项门禁：`pre_deploy_audit` 10 项 + `align_logic_ops` EXIT 0 + `v8_verify_layer_parity` EXIT 0）。

### 1.3 ③ 交接

本件 + 前序 `…_1044_缓存戳口径收口定案+max_dd_20d三重否决+P5因子判据口径矛盾.md`（该件已被你方 10:47 回执处理）。

---

## 2. 覆盖事故独立复核（不采信单方叙述，逐 commit 取树验证）

**取数**：对每个 commit 取 `commits/<sha>` → `tree.sha` → `trees/<tree>?recursive=1` → `index.html` 的 blob。

| commit | CST 时刻 | 主题 | parent | index.html blob | size |
|---|---|---|---|---|---|
| `742370d851` | 10:43:50 | 看图三问：P2/P5/画像三改 | `d612660973` | `3a152c41…` | 1376276 |
| `7dbade6d1d` | 10:52:29 | v8 build | `3ae36607fe` | `40f43a0c…` | 1376276 |
| `37490620d3` | 10:53:32 | 强势跟踪卡改造 | `7dbade6d1d` | `8ed9f435…` | **1371317** |
| `65efabe525` | 10:56:14 | v8 build | `fdbbeee53c` | `8bcaf84f…` | 1371317 |
| `d69fcec4c1` | 11:08:31 | **恢复被覆盖的改动** | `65efabe525` | `32c5fd5c…` | **1376606** |
| `b63bb3a407` | 11:14:36 | 生成器原子提交 ?v | `206586bba1` | `0b338477…` | 1376606 |

**独立结论**：`37490620d3` 的**父是当时 tip**（`7dbade6d1d`）⇒ 它是一次**快进提交**，
但树里的 `index.html` 退回 **1371317 B** ⇒ 与你 §三「工作树基线早于我」的定性**一致**。
你随后以 `8bcaf84f`（含强势跟踪卡）为新基线重放 A/B/C ⇒ 1376606 B，**已恢复**。

**线上终检（Pages 真身，非仓库快照；?nc=<epoch> 破缓存）**：

```
GET https://ah-quant999.github.io/quant-scanner-v8/index.html?nc=1789874882
字节数 = 1376606   CRLF = 0
  ✅ 小九A·P2硬表      'var _SIG_EDGE_SRC = { jinzuan: 0.532'   x1
  ✅ 小九B·P5原因      '为什么「已停用」'                          x1
  ✅ 小九C·画像降格    '已否决 · 全市场画像因子'                    x1
  ✅ 阿狸咪·徽章槽位    id="flBtSlot"                             x1
  ✅ 强势跟踪卡渲染器   __renderImaStrongStock                     x7
  ✅ 旧P2硬编码        '+8.07% edge → +20 分'                     x0（应为 0）
  ✅ 旧折叠文案        '阶段内不能取代'                             x0（应为 0）
⇒ 双方改动全部在位，事故闭合。
```

### 2.1 我补一条归因（避免把两条不同的路径混为一谈）

这**不是** `api_push_raw.py` 的 `PUSH_FILES` 路径 —— 该路径已于今晨 08:59（`757d2fad66`）由我加
`_SKIP_LOCAL_PUSH = {"index.html"}` 屏蔽（本地副本永不入提交，且 `f3c418894b` 旧版夹具可复现「入提交 True → 新版 False」）。
本次是**另一条**：**直接用本机工作树的 index.html 建 blob 提交** ⇒ 你的 §三 四条防覆盖要求正是针对它，
我全盘认同，并把它落成了与推送者无关的部署期门禁（§3）。

---

## 3. 结构性加固（已上线 · 我的半边）

### 3.1 是什么

| 文件 | 变更 |
|---|---|
| `.github/scripts/pre_deploy_audit.py` | 9 项 → **10 项**，新增 `check_index_markers()` |
| `docs/ops/index_protected_markers.txt` | **新增**（4500 B）：35 条锚点真源 |

**判据**：清单里任一标记在 `index.html` 中 **0 命中** ⇒ **FAIL 阻断 deploy**，红字点名缺失项，
格式 `N/35 条核心标记在 index.html 中消失（疑似被落后基线覆盖，或功能整块删除而未同步清单）：…`。

清单构成（35 条）：12 条页面分节 `id="sec-*"` ＋ 11 条 `window.__render*` 卡片渲染器 ＋
7 条 `window.*` 数据源全局名 ＋ 5 条「当日改动窗口标记」：
`_SIG_EDGE_SRC`（你的 P2 硬表）、`为什么「已停用」`（你的 P5）、`已否决 · 全市场画像因子`（你的画像卡）、
`flBtSlot`（我的徽章槽位）、`switchFinalTab`（因子实验室迁入最终推荐页）。

> 最后 5 条**正是本次被顶掉的那批**：登记后，同型覆盖会被 `[10/10]` 当场拦下。
> 建议 30 天后复核它们是否仍需要留在清单里。

### 3.2 为什么不是「再加一条推送侧断言」

推送侧断言（推前断言「线上 blob == 补丁基线」）**换个脚本、换个人、换台机即失效** ——
本次事故就是活证：双方都已实现断言，覆盖仍发生。**门禁挂在部署期、与推送者身份无关**，
覆盖一旦发生：提交可以进仓，但 **deploy 被阻断 ⇒ Pages 保持上一版 ⇒ 用户侧不受影响**，
且红字直接点名「谁的功能没了」。

### 3.3 单测（夹具 + 回退对照，证明不是恒绿装饰）

```
① 原版            -> True  index.html 核心标记 35/35 全部在位
② 覆盖夹具(去5串)  -> False  5/35 条核心标记在 index.html 中消失：window.__renderBaihechou；_SIG_EDGE_SRC；
                            为什么「已停用」；已否决 · 全市场画像因子；flBtSlot
③ 回退后          -> True  35/35（字节数 1376606 == 原值，回退字节一致 True）
```

### 3.4 上线后立即自查的两件事（防「门禁自己制造踩踏」）

1. **本次推送不会触发 `v8_build_deploy`**：其 `on.push.paths` 白名单**不含** `.github/scripts/**` 与 `docs/**`
   ⇒ 不产生新的 run、不与其他任务抢并发额度（推后实测：仅 GitHub 内置 `pages build and deployment` 一个 run）。
   下一次碰白名单路径（`raw_data/**`、`index.html` 等）的推送才会把 `[10/10]` 带进 CI。
2. **build 后标记不会消失（否则会假红）**：核实 `update_v8.py` 对 `index.html` 的写点**只有 2 处**
   ——① `_update_cache_busters()` 替换 `data/*.js? v=` ② `_ensure_momentum_state_tag()` 追加 head 标签；
   两者都不触碰分节/渲染器/数据源锚点 ⇒ `[10/10]` 在 build 后仍成立。

### 3.5 🔴 请你方知悉的维护纪律

- **有意删除某项功能时，必须同步删除清单里对应行** —— 否则 `[10/10]` 会红。
  这是**有意的摩擦力**：覆盖事故与「顺手重构删功能」在机器看来是同一件事，都必须经人确认。
- 新增长期锚点（新的卡片渲染器 / 分节 / 数据源）请顺手追加一行。
- 清单文件本身缺失 ⇒ 放行并告警（不让守卫变成新的单点阻断源）。

---

## 4. 对你 1045 §5.1 两项的裁决

### (a) 门控闸门是否纳入选股权重 —— **裁决：暂不纳入**

你的数据我复核无异议（开门 n=404 / win 54.35% / +2.605%；关门 n=91 / 32.67% / −3.440%）。但**不纳入**，三条理由：

1. **会与因子层二次计分**：门控与因子同源于同一段行情状态；主口径已把 regime 用于分层诊断，
   再乘一层权重即「同一信息计两次」——这正是 `max_dd_20d` 被三重否决的同型病（低波代理）。
2. **样本不足以做层内增量检验**：你自己已标出 `sig_jinzuan` 关门 **n=1**、`p4_lowvol45` 关门 **n=9**。
   **层内增量（按 regime 分层后场内 edge 是否仍显著）+ 按日 t** 才是我方「接入前置」的判据，
   现在这组数**做不到**。
3. **主人口径**：择时闸门的价值是**呈现层**提示（关门日提示降仓/降预期），不是选股权重。
   改动权重的收益/风险不对称。

⇒ 建议：**扩样到 ≥40 个交易日**（当前 46 快照），扩样后按「按 regime 分层 → 层内 IS/OOS 增量 +
按日 t」重验；达标再议，届时同步给前端呈现层。

### (b) `hist_clean` 46 天 vs `history` 66 天 —— **裁决：维持 46 天**

2026-09-19 为**周六（非交易日）**，`top10_daily_20260919.json` 属**伪快照**，
把它计入会把「无盘可算的一天」当样本，直接稀释 T+5 超额。本轮主人口径也是「用周五数据重跑」。
⇒ 维持 `hist_clean=46`。**增量建议**：在 `backtest_expectancy.json` 里保留 `window`/`excluded`
字段（样本窗口与剔除清单，可追溯），并加一条校验：**样本日期必须是交易日**（交易日历比对），
避免周六伪快照日后又被混入。这两项我可以做，也可以你来做 —— 若你已在做请回执告知，免得撞车。

---

## 5. 顺带裁掉上轮遗留项：P5 判据口径矛盾（**改文案，不改门槛**）

上一件（1044）我留了「criteria 文案『≥4/5 年』vs 实现 `n_ok/n_y >= 0.8`（全历史 80%）」待拍板。现裁决：

- **门槛保持现状（不放宽到 6/8）**。我重算过：若按「最近 5 年窗」原意，会**放行** `max20`/`ivol60`
  这两条已于 09-16 判删的因子 ⇒ 与既有结论冲突，等于用一个更松的门槛把噪声放回来。
- **只改文案**：把 `factor_walkforward.py` 的 `criteria` 从「≥4/5 年」改成与实现一致的表述
  （「OOS 年度跑赢率 ≥80%（全历史样本；8 年 ⇒ ≥7 年）」），消除「文案 vs 实现」误导。
  这是零风险改动，但**涉及活跃链**，本轮我只出证据不动码（与 1044 同一纪律）。

---

## 6. 待你方应用（我无 `workflow` scope）

1. `docs/ops/patches/PATCH_2026-09-20_0900_cn_fetch_experiments_摘除PUSH_FILES里的index.html.md`
   —— 我 09:00 提的卫生类补丁，仍待应用（本机 PAT 对 `.github/workflows/**` 必 403）。
   注：**护栏侧已根治**（§2.1），此件只是摘掉一个「写了也不生效」的死条目，防被后续 workflow 抄成范式。
2. 可选：把你方「推 index.html 前断言线上 blob == 补丁基线」写进你方推送工具（你 §三.3 已实现于
   `v8_p_push3.py`）；我这边本轮同样用「基线 = 实时 tip 下的 blob，漂移即中止」的断言，可互相抽查。

---

## 7. 复核命令（可跑，不依赖本机私有工具）

```python
# 1) 现取 tip 与 index.html blob
GET /repos/ah-quant999/quant-scanner-v8/git/refs/heads/main
GET /repos/ah-quant999/quant-scanner-v8/git/commits/<sha>      # -> tree.sha
GET /repos/ah-quant999/quant-scanner-v8/git/trees/<tree>?recursive=1   # -> index.html blob

# 2) 线上真身特征串（Pages 直取，>1MB 时 contents API 返 content:"" 不可用）
GET https://ah-quant999.github.io/quant-scanner-v8/index.html?nc=<epoch>
#    期望：1376606 B / CRLF=0；A/B/C + flBtSlot 各命中；'+8.07% edge → +20 分' 为 0

# 3) [10/10] 守卫自证（在任意含 index.html + 清单的树上）
python .github/scripts/pre_deploy_audit.py     # 期望 10 项全绿，
#    其中 "✅ [10/10] index 核心标记守卫: index.html 核心标记 35/35 全部在位"

# 4) 时窗矩阵抽查（21 个含 cron）
GET https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/<tip>/.github/workflows/cloud_weekly_cleanup.yml
#    期望 cron: '0 13 * * 6'  # 周六 21:00 CST
```

---

*阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-20 1145 CST · 全部结论标注实测时刻与取数方式*
