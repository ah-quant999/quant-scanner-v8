# 删除「🔥 主板首板 · 一进二观察」卡（board-first-limit）

- **执行机 / 时间**：阿狸咪（家机）· 2026-09-24 01:44（北京时间）
- **结论**：该卡整条功能无正期望，按主人「不行就删除干净吧」彻底删除。

## 一、为什么删（已在上一轮实测裁决）
- 14:30 准点执行池不可行：14:30 可下单仅 5.5%、之后仍买得到仅 10.9%、能买到的那批净 −0.679%（t=−2.15）且 **100% 属弱票**；
  可成交性开盘半小时内定死（首次可成交中位 09:35、最后中位 10:50）；GHA 接力式 sleep 一档落定 20–25 分钟，准点做不到。
- **方向本身负期望**：全市场无偏 14,675 例「当日触及涨停」（含炸板）实测，打板成交 14,070 例净 **−1.052%（t=−11.25，17/17 个月全负）**。
- 唯一可能为正的是「预挂涨停价单」口径 n=1689 → +0.816%（t=+7.85）、12 正 1 负，但成本极敏感（1.5% 归零），须实盘小仓核准成交率。

## 二、本轮删除清单（index.html）
| # | 删除内容 | 锚点 |
|---|---|---|
| R1 | `data/BOARD_FIRST_LIMIT.js` 注入脚本行（含行尾注释） | `<script src="data/BOARD_FIRST_LIMIT.js` |
| R2 | 迁移注释 F（HTML 注释） | `🚚 2026-09-23 主人令…卡已迁至` |
| R3 | 渲染列表条目 | `['__renderThemeValue','__renderBoardFirstLimit']` → `['__renderThemeValue']` |
| R4 | 渲染器函数块 `function __renderBoardFirstLimit(){…}` | `// 🔥 主板首板 · 一进二观察卡渲染器（` → `window.__renderBoardFirstLimit = __renderBoardFirstLimit;` |
| R5 | 迁移注释 E 块 + HTML 容器 `h += '<div id="boardFirstLimitCard"…'` | `═══ 🔥 2026-09-23 主人令…由「📡 实时数据」迁入` |
| R6 | dispatch 注释行 | `//   原 tryRun('renderBoardFirstLimit'` |
| R7 | 派发区迁移注释行（// 行） | `// 🚚 2026-09-23 主人令：主板首板·一进二观察卡已迁` |

- 同步删除 `docs/ops/index_protected_markers.txt` 中标记行 `window.__renderBoardFirstLimit|主板首板观察卡渲染器|2026-09-22`。
- 同步删除孤儿数据 `data/BOARD_FIRST_LIMIT.js` 与 `raw_data/board_first_limit.json`。
- 删除后断言：`主板首板`/`一进二`/`BoardFirst`/`BOARD_FIRST_LIMIT`/`boardFirstLimitCard` 在 index.html 中 **=0**；`候选池`(其余卡) 保留 =54。

## 三、本地验证（推送前）
- `[10/10]` 受保护标记：剩余标记 0 缺失（原 39 → 现 38）。
- 内联脚本语法：28 个脚本 0 失败（新失败集 ⊆ 原失败集）。
- 行数：19,357 → 19,270（减 87）。

## 四、遗留（待小九处理，非本机可改）
- **后端生成器仍未停**：`algorithms/fetch_board_first_limit.py` 由盘中 cn fetch 工作流调用；其所在 `.github/workflows/**` 需 workflow scope 令牌（本机 PAT 无）才能改 → 小九用具备 workflow 写权限的令牌：
  ① 在对应 workflow 步移除 `python algorithms/fetch_board_first_limit.py`（或整步）；
  ② 删除 `algorithms/fetch_board_first_limit.py` 脚本本身。
  否则每日仍会重建已删除的 `data/BOARD_FIRST_LIMIT.js`（无前端引用，无害但属孤儿）。
- 实盘核准「预挂涨停价单」成交率后再决定是否另起轻量观察模块。

## 五、产物
- commit（见本仓 log，path=index.html）；tip 推进后请小九复核 `logic.html` 无需改动（本卡未写入逻辑详解页）。
