# HANDOVER · 小九的工程师 → 阿狸咪的工程师

**时间**：2026-09-20 12:24（CST）
**主题**：双机全量改动审计（200 提交）后续落地 —— 两处审计脚本修复上线 + 派发判定更正 + 新口径消费时点定论
**性质**：改后三件套（改了什么 / 怎么验的 / 你怎么复核）

---

## 〇、一句话

09-19~09-20 双机 200 提交全量审计已完成交付；本轮把审计中识别、属我职责的修复**全部落地**：
`verify_card_badges.py` 与 `verify_daily_audit.py` 两处**每日假红根因**已修复并推送（净室单文件直推），
经**线上最新树仿真云端**真跑，`v8_backup.yml` 审计步三项**预期全绿**（此前连续 5 天 09-16~09-20 硬失败）。

---

## 一、改了什么（2 个文件 · 均已上线）

### 1. `verify_card_badges.py` — 白名单补齐

- **远端 blob sha**：`76f8d8aabc74`（push 前基线 `0f464935a508`）
- **commit**：`14e7036a6707`
- **改动**：`NON_TIME_FRESH` 白名单补 `fvAlertCount` + `fvTrackTime`
- **根因**：四量终极「自己的一整套系统」（2026-09-15 主人令）照「严格三重共识」版式复制 6 张子卡，
  计数 ID 由 `ttAlertCount` 复制为 `fvAlertCount`，但白名单**只加了原版**，漏了四量副本
  ⇒ 每日误报 1 处漂移 ⇒ `v8_backup` 硬失败。
- **判据对称性铁证**（线上 index.html 逐行）：
  ```
  L3062  🔔状态迁移 &amp; 告警 · <span class="fresh" id="ttAlertCount">0</span>   → 白名单已有
  L3163  🔔状态迁移 &amp; 告警 · <span class="fresh" id="fvAlertCount">0</span>   → 漏（本次补）
  L3158  📈持仓盈亏跟踪 - 盘后 · <span class="fresh" id="fvTrackTime">—</span>   → 漏（本次补）
  ```
  `fvTrackTime` 实现上已用 `rel(track.update_time)` 输出相对时间胶囊（语义等同 `_uBadge`），
  并非卡片「更新于」时间戳 ⇒ 本就不该列入时间胶囊校验。

### 2. `verify_daily_audit.py` — 自动化判定升级为「身份 + 前缀」双判

- **远端 blob sha**：`73eb231dc2aa`（push 前基线 `66ccc046b39e`）
- **commit**：`c95a97824fbb`
- **改动三处**（锚点均唯一命中）：
  1. `get_today_origin_commits()` 的 `--format` 增加 `%an%x1f%ae%x1f%cn`，返回值扩为 `(sha, subject, meta)`
  2. 新增 `BOT_IDENTITIES` / `BOT_EMAIL_MARKERS` / `is_bot_identity()`
  3. `is_auto_commit(subject, meta=None)` = **前缀命中 OR 身份命中**（取或）；并加诊断输出（按身份识别出的条数）
- **根因**：线上近 400 条 commit 里，**bot 身份但前缀未入白名单的有 71 条**：
  `github-actions[bot]` 的 `v8 cn fetch:`（**64 条**）/ `audit:` / `audit-heal:` / `v8 healthcheck:`、
  `v8-cloud-bot` 的 `v8 risk gauge:`。
  被点名的 `0265c24 audit: 每日全站审核` 正是 `github-actions[bot]` 的自动产物，
  却被当「人工 commit」要求关键词在交接档命中 ⇒ 永不命中 ⇒ 「未提及 100% > 50%」⇒ 结构性硬失败。
- **为何不继续补前缀**：同仓有 **4 个 bot 身份**共写（`v8-cloud-bot` / `github-actions[bot]` /
  `v8-cache-bot` / `v8-backup-bot`），每新增一个 workflow 就漏一个前缀（历史已反复发生）。
  按**提交身份**判定后，新 workflow 只要用既有 bot 身份提交即**自动覆盖**，无需再补前缀。
- 🔴 **人工身份白名单（不得加入 BOT_IDENTITIES）**：
  `ah-quant999` / `xiaojiu` / `小九的工程师` / `阿狸咪的工程师` / `lemoncat-cn` / `2814546@qq.com`

---

## 二、怎么验的（三重验证 · 全部可复现）

### 验证 1：复现 → 修复 → 归零（`verify_card_badges.py`）

用**线上 index.html 真身**（blob sha `5e079934d540`，1376606 B）搭两个沙箱真跑：

| 版本 | rc | 校验 ID 数 | 漂移 |
|---|---|---|---|
| 原版（线上真身） | **1** | 41（.fresh=38 + 显式=3） | **2** · `fvAlertCount` / `fvTrackTime` |
| 补丁版 | **0** | 39（.fresh=36 + 显式=3） | **0** |

### 验证 2：单元级身份判定对比（`verify_daily_audit.py`）

12 条真实样本（线上实取）逐条比对，**5 条 bot 误判全部纠正、5 条人工零误伤**：

| 用例 | subject | author | 原版 | 补丁版 | 期望 |
|---|---|---|---|---|---|
| ① | `audit: 每日全站审核 2026-09-20 01:21` | github-actions[bot] | False ❌ | **True** ✅ | True |
| ② | `v8 cn fetch: 2026-09-20 12:07` | github-actions[bot] | False ❌ | **True** ✅ | True |
| ③ | `audit-heal: ?v 对齐 CDN 真实内容` | github-actions[bot] | False ❌ | **True** ✅ | True |
| ④ | `v8 healthcheck: 2026-09-19 21:06` | github-actions[bot] | False ❌ | **True** ✅ | True |
| ⑤ | `v8 risk gauge: 2026-09-20 12:06` | v8-cloud-bot | False ❌ | **True** ✅ | True |
| ⑧ | `fix(factor_walkforward): P5 年度门槛口径修正…` | ah-quant999 | False ✅ | **False** ✅ | False |
| ⑨ | `docs(handover): 09/20 12:00 交接自动更新` | xiaojiu | False ✅ | **False** ✅ | False |
| ⑩~⑫ | 主人/阿狸咪工程师 / lemoncat-cn | — | False ✅ | **False** ✅ | False |

> 注：`v8 build:` / `chore:` 两例原版已能识别（前缀本就在白名单），故「原版误判」实为 **5 条**，
> 与我上一轮口头说的「71 条」口径不同 —— 71 是「近 400 条 commit 里 bot 身份且前缀未覆盖」的**总量**，
> 具体到当日 09-20 命中 **19 条**。

### 验证 3：端到端真跑（整脚本 · 真实 git）

`ROOT=本机真实仓`（只读 git 操作），当日 09-20 origin/main **89 → 91 条 commit**：

| | 原版 | 补丁版 |
|---|---|---|
| 当日分类 | 自动化 40 + **人工 49** | 自动化 59 + **人工 30** |
| 按身份识别出（前缀未覆盖） | — | **19 条** |
| 未命中的「人工」commit | **6 条** | **3 条** |
| 其中 `v8 risk gauge:` 虚报 | **3 条**（全是 bot 的） | **0 条** ✅ |

### 验证 4（关键）：线上最新树仿真云端 → `v8_backup` 预期全绿

本机工作树**落后线上 64 个交接档**（本地 407 vs 线上 471；09-20 本地 **0** 个 vs 线上 **19** 个），
故**不能用本机树判定**。用 `git archive origin/main`（tip `c95a97824fbb`，418723840 B）导出净室后真跑：

| 项 | 修前（09-20 01:33 云端日志） | 修后（净室 = 线上最新树） |
|---|---|---|
| `verify_daily_audit.py` | **rc=1** 硬失败 | **rc=0** ✅ 「当天小九交接已存在」（21 份） |
| `align_logic_ops.py` | rc=0 | **rc=0** ✅ |
| `verify_card_badges.py` | **rc=1**（2 处漂移） | **rc=0** ✅ 漂移 0 |

**⇒ `v8_backup.yml` 审计步预期从「连续 5 天硬失败」变为「三项全绿」。**

---

## 三、你怎么复核（可跑命令）

```bash
# ① 取当前 tip（勿写死 sha）
git fetch origin main && git rev-parse --short origin/main

# ② 确认两处修复已在线上
python -c "import urllib.request,json; \
  print(json.loads(urllib.request.urlopen(urllib.request.Request(\
  'https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/verify_card_badges.py?ref=main',\
  headers={'Authorization':'Bearer <TOKEN>'})).read())['sha'][:12])"
# 期望：76f8d8aabc74 ；verify_daily_audit.py 期望 73eb231dc2aa

# ③ 用线上最新树真跑三项（勿用本机旧树判定）
git archive origin/main | tar -x -C /tmp/v8chk && cd /tmp/v8chk
python verify_daily_audit.py ; echo "rc=$?"
python align_logic_ops.py     ; echo "rc=$?"
python verify_card_badges.py  ; echo "rc=$?"
# 期望：三项 rc 均 0

# ④ 下次 v8_backup（23:00 CST）后核验实跑结论
#    看 step「🎯 v8 每日审计」是否绿；并核 HANDOVER_LOG.jsonl 的 success 字段
```

**自检判据**：若 ③ 中 `verify_daily_audit.py` 报 `无法判定（git fetch 失败）`，
那是**跨境网络 30s 超时**（我在净室复现到过），属 `exit 0 不阻断`；重跑即可。

---

## 四、我犯的两个错（如实记录，供你纠偏）

1. 🔴 **派发带 `force_run=true` 是错的选择**
   我 11:59 派发 `v8_algo_cloud.yml` 试图消费新口径产物，run `35487981331` / `35487986934`
   均 `completed/success` 但 **step9 `skipped`**（时长仅 347s/646s，属「正常拦截」而非真跑）。
   闸门实测输出：
   ```
   target_stage=NONE  stage_ok=true  proceed=false
   chain_day=2026-09-19  kind=t1  cst_hour=12
   reason=回填目标 2026-09-19 的 ['B','D','E'] 全被后续日覆盖->不可能->空转等本日起点
   ```
   **根因**：`force=true` 使 `lookback=10`，闸门向前找到 **09-19 周六**（段首）为回填目标；
   但 09-19 的上游已被 09-20 的 run 覆盖（`VINTAGE_LAG`：产物数据日 09-18≠链数据日 09-19）
   ⇒ `_backfill_feasible()` 判不可能 ⇒ `NONE`。
   **本机复算证明**：

   | 时点 | `force` | `chain_day` | 结果 |
   |---|---|---|---|
   | 周日 11:59（我实际） | true | `09-19 / t1` | 回填不可能 → 空转 |
   | 周日任意（正常） | false | `None / none` | 休市空转（合规） |
   | 周一 12:00~17:00 | false | `09-21 / trading` | 未到 18:00 起点 → 仍会试回填 09-19 → 不可能 |
   | **周一 18:30 ★** | false | `09-21 / trading` | **到本日起点 → A→B→D→E 正常起跑** |

   **⇒ 新口径产物（`backtest_expectancy.json`，`meta.generated = 2026-09-20 10:41:42`）
   将在 2026-09-21 周一 18:00 后由 D 批消费**，无需人工派发。我这次的派发**注定空转**。

2. 🟡 **一度用「本机旧树」判定「缺当日小九交接」**
   12-05 抓的备份日志（跑 09-20 01:33 run）显示缺交接 —— 但那是**当时**的状态。
   现线上 09-20 已有 **19 个**交接档（18 个含「小九」）。**旧日志 ≠ 现状**。

---

## 五、仍挂账（需你裁决或需扩样）

1. **`meta.partial=true` / `needs_refresh=true`** —— 新口径产物**自标未定稿**。
   请裁：是否需重跑 `backtest_expectancy` 至 `partial=false` 才允许被 D 批消费？
2. `p4_lowvol55`（`excess5=+0.235`）/ `sector`（`+0.153`）verdict=`watch`，待扩样复核。
3. `fund` `n_on5=37` 薄样本，不得单独加权。
4. 门控闸门是否纳入权重：建议先扩样 40~60 日。
5. `hist_clean` 46 天 vs `history` 66 天口径差异，待你确认。
6. 🆕 `verify_daily_audit.py` 的 `git fetch origin main` 超时 30s —— 跨境偶发超时会导致
   「无法判定 → 不阻断」（假绿）。**是否要升为「重试 3 次仍失败 → 软告警」？**（我未擅改）

---

## 六、口径边界声明（重要）

- 本档所有「blob sha / commit sha」均为**写作时刻实测值**，仓库高速 churning，**请用第三节命令现取**。
- 本档未写「本档自身所在提交的 sha」。
- 本档所有结论均标注**数据采集时刻**；凡涉「后续会如何」一律给**可跑复核命令**，不写承诺。
- 🔴 **能力 ≠ 生效**：我已验证「脚本改动已上线 + 线上最新树真跑三项全绿」；
  **尚未发生**的是「下一次 `v8_backup`（23:00 CST）实际跑绿」—— 那需跑完后核验。

---

**小九的工程师** · 2026-09-20 12:24 CST
