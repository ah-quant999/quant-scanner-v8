# 交接：金股池（gold_pool）推送断链 · 一劳永逸修复

- **发件**：阿狸咪的工程师（`alimi-cn`，家用机）
- **收件**：小九的工程师（`lemoncat-cn`，单位机）
- **时间**：2026-09-13 00:13 CST
- **对应 commit**：`7cb0dfad7097126f0e6c5aaac4a2491516ab7787`（main）
- **紧急度**：普通（数据内容正确；仅 `update_time` 停滞 + 闸门少一项容错名额）

## 一、结论（一句话）

金股池**有用**（4 条在役消费链），数据内容**完全健康**——253 只 = 今日合格 57 + 历史继承 196，
`history` 末尾 `2026-09-12` 有 57 只（当日信号已入池）。
病在**推送断链**：远端 `raw_data/gold_pool.json` 的 `update_time` 冻在 `2026-09-12 00:53:46`
（`candidate_total=875`），而**同一次运行**写出的 `raw_data/candidate_members.json`
已到 `2026-09-12 21:13:21`（877 只）。

## 二、硬证据

| # | 事实 | 取证方式 |
|---|---|---|
| 1 | 写盘正常、只有推送断 | 远端 877/21:13 vs 875/00:53；同脚本 `derive_and_save_gold_pool`（L1045）先于写 members 表（L1132） |
| 2 | 派生逻辑正常 | 隔离实验（路径全部重定向到临时目录，不碰仓库）：线上 877 候选 → 合格 57 + 继承 196 = 253，三文件全写成功（557865 B） |
| 3 | `.bak` 推了、主文件没推 | commit `ed0d5b659` / `485f1719b` 文件列表含 `raw_data/gold_pool.json.bak`，**不含** `gold_pool.json` |
| 4 | `scanner.py` 从不设 `update_time` | 远端真源 `grep -c update_time algorithms/scanner.py` = **0** |
| 5 | 闸门注释自己点过名 | `v8_stage_gate.py` L162-168：「另 4 张（factor_audit / factor_progress / index_value_framework / **gold_pool**）… 永远补不上 → 恒红」 |
| 6 | `soft_8` 是唯一产地且**不含 all** | `v8_cn_fetch_cloud.yml:401` `if: steps.cat.outputs.category == 'post_close'` |

## 三、根因（三层拦截 + 一处属性错误）

1. **dedup 伪变更**：金股池是 45 交易日继承池 ⇒ 同日重复跑 `stocks` 完全不变 ⇒ 剥时间戳后
   与远端几乎逐字节相同 ⇒ 判「伪变更」⇒ 不推送。
   2026-09-11 那轮修「9 张红灯卡」时**只补了 5 张，漏了 gold_pool**（闸门注释已点名同病）。
2. **`api_push_raw.py` 防倒退守卫**：判据 `path.endswith(".json")` + `update_time`。
   `.bak` 天然豁免 ⇒ 被推；主文件被拦。若产出带陈旧 `update_time`（见 3）⇒ `lts < rts` ⇒ **永久自锁拒推**。
3. **`scanner.py` 不刷 `update_time`**：两处落盘只设 `last_update`，产出继承 prev 旧值
   （本机实测 `.bak`：`update_time=2026-09-11 15:00:00`）⇒ 喂给 2 触发自锁。
4. **`v8_stage_gate.py:157` 注释属性错误**：写「**B 批** build_candidate_pool 原生派生」，
   但该脚本**不在 `v8_algo_cloud.yml`**（实测 grep 无命中），只在 `v8_cn_fetch_cloud.yml:414` 采集批运行。

## 四、本次改动（4 文件，commit `7cb0dfad`）

| 文件 | 改动 |
|---|---|
| `.github/scripts/dedup_fetch_manifest.py` | `_ALWAYS_PUSH` 加 `raw_data/gold_pool.json` + `data/GOLD_POOL.js`（10 → 12 条） |
| `api_push_raw.py` | 新增 `_NO_REGRESSION_GUARD`，豁免**累积继承型池类产物**（仅 gold_pool 两件；`lhb_history.json` 等 append 型**不**豁免，避免本地更短时覆盖真丢数据） |
| `algorithms/scanner.py` | 两处金股池落盘（L2714 / L2794 附近）显式 `pool["update_time"] = pool["last_update"]` |
| `.github/scripts/v8_stage_gate.py` | L157 注释属性纠正；**items 清单与 `need=8` 语义未动** |

- 推后自证 **9 项全 PASS**；四文件行尾均 **LF**（CR=0）
- `align_logic_ops.py` **EXIT 0**

## 五、⚠️ 小九必须注意

1. **不要用本机旧 checkout 覆盖这三个脚本**（会把修复打回去）。一律以 main `7cb0dfad` 为准。
2. **生效时点**：`build_candidate_pool.py` 只在 `soft_8`（`category == 'post_close'`，**不含 `all`**）里跑
   ⇒ **下次生效是周一 09-14 的 17:20 / 18:20 / 19:20 盘后档**。
   周日 09:00 的 `all` 档（`0 1 * * 0,6`）**不产金股池**。
   ⇒ 周一盘后请核验：`raw_data/gold_pool.json` 的 `update_time` 是否 ≥ 当日 17:20，
   且 `candidate_total` 是否跟随候选池（877 起）。
3. 若周一盘后 `update_time` 仍不前进 ⇒ 说明还有第 4 处拦点，
   请**保留当轮 run 推送段日志**（本轮日志已过期 404，无法回溯取证）。

## 六、待主人拍板（本次未改）

`scanner.py`（四信号 / 三足鼎立口径）与 `build_candidate_pool.py`（候选池 ∩ (研报 | 多源共振≥2)）
**双生产者争抢同一份 `out/gold_pool.json`**，谁后跑谁赢。
`三足鼎立`（四信号全满足）**现已停产**（scanner 不在采集批）⇒ 现存 196 只会随 45 交易日窗口自然出池。
⇒ 需主人定：金股池口径是否收敛为 `build_candidate_pool.py` 单套。

—— 阿狸咪的工程师
