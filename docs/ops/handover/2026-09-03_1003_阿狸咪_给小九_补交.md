# 九宝量化 v8 交接文档（阿狸咪 → 小九）

> 📌 **2026-09-03 10:03 CST（阿狸咪·夜间+清晨档 · 补交）**
> 本档为铁律第三步「推完留交接」的补写（此前漏交，已向主人认错）。小九白班在线（07:45–17:45），请据此掌握当前状态，避免重复/冲突操作。

## 〇、本档背景
- 阿狸咪夜班（18:00–07:30）+ 今晨 07:30–10:00 因主人连续追问，做了「斐波那契双卡合并」前端改造 + 「9/2 收盘数据补跑」兜底。
- 前端已推 main；数据补跑委托云端盘后算法链（v8_algo_cloud.yml）执行中，尚未推回。

## 一、已改并提交 main 的内容（改了什么）

| SHA | 时间(CST) | 改动 | 影响 |
|---|---|---|---|
| `a67f0fe28` | 09:30 | `refactor(已下架)`: 合并斐波那契双卡（窗口·当前回调状态 + F观测窗）为单卡，压缩版面 | index.html 前端，净减 22 行（45 增 / 67 删）；inline JS 23 段 0 错；**未动 data/ 与 scripts/**；CI 保护 index.html 人工改动已生效 |

> 仅前端，无数据口径变化，无需小九重算。

## 二、待云端跑批项（待云端跑批项 · 核心）

**根因回顾**：今晨发现 9/2 收盘盘后选股链未出——`TRIPLE_CONSENSUS.js` 停在 `update_time=2026-09-02 04:44`、`count=0`。根因 = GitHub schedule cron 静默失效 + 旧 19:30 兜底把 9/2 16:04 的 success run 误判为「今日已完成」。

**已触发云端 run（v8_algo_cloud.yml，并发组 `v8-algo-cloud` 单跑）**：

| run_id | 状态 | 创建(CST) | bypass_time_gate | 能否修复 TRIPLE |
|---|---|---|---|---|
| `33704085049` | in_progress | 09:32 | ❌ 无 | ❌ 否（<18:00 时间门会整段跳过选股算法） |
| `33704322896` | completed/cancelled | 09:36→09:55 | ❌ 无 | 已取消（避免无 bypass 白跑占位） |
| `33705601456` | pending（排队其后） | 09:55 | ✅ true | ✅ 是（会用 9/2 收盘重算 TRIPLE_CONSENSUS + FINAL_RECOMMEND） |

- `33705601456` 预计 **11:00–13:00 CST** 推到 main；推完坚果云镜像本地 `data/`，主站 Ctrl+F5 即见 9/2 收盘最新数据。
- **小九验收点（run 完成后）**：`TRIPLE_CONSENSUS.js` 的 `count` 应 > 0、`update_time` 应 ≥ 09:55；`FINAL_RECOMMEND_DATA.js` 应含 9/2 收盘。

## 三、铁律级待办（今晚 9/3 收盘 · 不再请示）

1. **9/3 收盘数据必须当晚出**：A股 15:00 收盘后，v8_algo_cloud.yml 必须成功跑完并产出最新 FINAL_RECOMMEND_DATA，最终推荐不得隔夜停在盘前版。
2. 已建/修两道兜底自动化（本机 WorkBuddy）：
   - `九宝量化-盘后算法链兜底触发(19:30)`：只看「今日 15:00 后有无 success run」，无则 repository_dispatch 触发（19:30 已过 18:00 门，无需 bypass）。
   - `九宝量化-盘后最终闸(21:30)`：若届时仍无今日收盘后 success 且无 in_progress，直接 workflow_dispatch **带 bypass_time_gate=true + force_run=true** 强制补跑，不再请示。
3. **时间门陷阱（必记）**：任何 18:00 CST 之前的补跑必须带 `bypass_time_gate=true`，否则选股算法整段跳过、白跑。今晨 `33704322896` 即因此失效。

## 四、未决项 / 风险

- 🟡 9/3 白天盘中数据：由小九现有盘中守卫负责，阿狸咪未动。
- 🟡 本机工作树若有其他分叉，勿 `git add -A`（Nutstore 铁律，防误删 data/）。
- ✅ 前端 Request G 已上线，无需小九改 index.html。

## 五、给小九的行动建议

1. 监控 `33705601456` 跑完 → 验收 `TRIPLE count>0`。
2. 今晚 19:30 / 21:30 两道兜底自动化会自动跑；若 9/3 收盘数据仍未出，21:30 闸会强制补跑（带 bypass）——小九无需手动干预，但请留意邮件/告警。
3. 若遇 GitHub schedule 又静默失效，直接走 API dispatch（`ref=main`, `inputs={force_run:true, bypass_time_gate:true}`），**不要本地跑算法**（东财 WAF 拦截 + `time_gate.check_cloud_only()` 禁止）。

---
*文档由阿狸咪于 2026-09-03 10:03 CST 补写，落盘 + 推 main。*
