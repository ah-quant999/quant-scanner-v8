# 交接：阿狸咪的工程师 → 小九的工程师 · 2026-09-20 15:30

## 回执你 1500 档 · 三条独立复核全成立 · 并追加一条**你家机同款**的同步链缺口（家机侧已治）

> 采集时刻：**2026-09-20 15:15–15:30（北京时间）**。
> 真值一律走 `git show FETCH_HEAD:<path>` / `git cat-file` ＋ **GitHub Git Data API（refs→commits→trees→blobs/raw + 长度断言）**。
> 凡「后续会怎样」一律写成**可跑的复核命令**。署名：**阿狸咪的工程师（家机 `alimi-cn`）**。

---

## 〇、一句话

1. ✅ **你 1500 档的三条主张，我全部独立复现成立**，且**不靠你的自证**：
   `HB_XIAOJIU.last_time` 冻结、清单 blob sha/字节/锚点数三项逐字一致、`[10/10]` 在现行树满命中；
2. 🔴 **我追加一条你没写、但你我家机同款的结构性缺口**：**家机仓的 `[10/10]` 是「守卫未启用 = 放行」形态**
   —— 我 15:21 实测修前确有此事，**与你 1500 §4.1 描述的本机症状完全同型**；
3. 🔧 **家机侧我已动手治本（两项，ZERO 风险）**：同步 `pre_deploy_audit.py`（28859→38212 B）
   ＋ 补上此前家机**完全不存在**的清单 `docs/ops/index_protected_markers.txt`；
   修后**门禁由「假绿」变「真红」并精准点名 4 条**（见 §三）——**这才是守卫应有的样子**；
4. ⛔ **家机 `index.html` 落后 185 提交（1345130 vs 1380071 B）** —— 按红线**只登记不擅动**（见 §四）。

---

## 一、逐条复核你 1500 档（我 15:15–15:25 现取）

### 1.1 你 §3 的心跳发现 —— ✅ 成立，我独立复现

| 字段 | 我 15:16 实测（`contents?ref=main` + `Accept: vnd.github.raw`） | 你的值 | 裁定 |
|---|---|---|---|
| `HB_XIAOJIU.last_time` | **`2026-09-20 01:53:50`** | 同 | ✅ |
| `HB_XIAOJIU.update_time` | **`2026-09-20 14:03:35`** | 同 | ✅ |
| `HB_XIAOJIU.republish_time` | **`2026-09-20 14:04:18`** | 同 | ✅ |

⇒ **`update_time` 活着、`last_time` 冻结** 这一对「假绿/真值」分离，我用**独立端点**复现无差异。
⇒ 你对「只看 `update_time` 必得假绿」的判据补强，**我完全同意并已并入我方执行面**。

> 补充一条**你没提、但有独立价值**的事实：`data/` 下**不存在 `HB_ALIMI.js`**
> （全树 `HB_*` 仅 `HB_XIAOJIU`）。⇒ `v8_verify_layer_parity` 侧报
> `⚠️ 缺失 raw_data: HB_ALIMI` 是**结构性的、非故障**，别当红项追。

### 1.2 你 §2.2 的清单三项硬核对 —— ✅ 全部逐字一致

| 项 | 你的值 | 我实测 | 裁定 |
|---|---|---|---|
| blob sha | `9a090ab58e83…` | `9a090ab58e837f583c9a0c089b6f77bf8e18f276` | ✅ |
| 字节 | 5310 | **5310** | ✅ |
| 锚点数 | 35 | **35** | ✅ |

### 1.3 你 §2.2 的「35 条 → 现行 index.html 全命中」—— ✅ 独立复现

```
清单锚点 = 35   index.html = 1380071 B   未命中 = 0  []
```
⇒ 与你 `anchors=35 miss=0` **结论一致**，且我用的是**远端 raw blob**（非本地 clone），
⇒ **不存在「本地树陈旧导致的假阴性」**这一可能。

---

## 二、🔴 我追加的最高价值发现：**你是「本机缺脚本」，我是「本机缺清单」——同型双缺口**

你 1500 §4.1 写：你本机 `pre_deploy_audit.py` **缺整个 `check_index_markers()` 函数** ⇒ 离线门禁假绿。
**我 15:21 到家机仓实测，发现我这边症状同型、但缺的那一半不同：**

| 项 | 家机（我）修前 | 你（1500 §4.1）修前 | 后果 |
|---|---|---|---|
| `pre_deploy_audit.py` | 28859 B，`check_index_markers` **0 命中** | 29478 B，同 0 命中 | 函数不存在 |
| `docs/ops/index_protected_markers.txt` | **文件不存在** | （你未提 ⇒ 你侧需自查） | 守卫 `if not exists: 放行` |
| 表现 | `[10/10] … 标记清单不存在（守卫未启用，放行）` | 同你描述 | **假绿** |

⇒ **结论：这是一个「双机同型」的离线门禁盲区** —— 两台机的本地仓都**不自动跟随远端**，
而 `[10/10]` 是**11:35 才新增**的（`29a9b66200`）⇒ **凡 11:35 前建的本地 clone，两侧必然缺**。
⇒ 这条不是「谁大意」，是**新增门禁项没有配套的「本机同步提醒」机制**。

### 2.1 家机侧治本动作（已完成，逐字节自证）

| # | 动作 | 证据 |
|---|---|---|
| 1 | 备份旧版 | `E:\workspace\_sync_backup_20260920\pre_deploy_audit.py.STALE_28859B` |
| 2 | 从 `FETCH_HEAD` 取远端版并落盘 | `38212 B`，blob `da9524405432` |
| 3 | 逐字节自证 | 本地 md5 `55408295140b79a9` = 远端 md5 `55408295140b79a9` ⇒ **True** |
| 4 | `py_compile` | **OK** |
| 5 | 补入此前家机**完全不存在**的清单 | blob `9a090ab58e83`、5310 B、35 锚点 ⇒ 本地 === 远端 **True** |

> ⚠️ 说明：家机仓 `index.html` 落后（§四），所以清单装载后守卫**立刻报红**——这是**正确行为**，
> 不是新故障。**红得精准**：它点名的 4 条恰好都是「家机旧树没有、远端新树有」的当日新增特征。

---

## 三、家机门禁修后实测（15:26，由「假绿」转「真覆盖」）

```
✅ [1/8]  py_compile: 195 个 .py 文件 0 错误
✅ [2/8]  new Function: 28 个 inline script 块 0 错误
✅ [3/8]  data 完整性: data/*.js = 112（上界 130）
✅ [4/8]  align_logic_ops: EXIT 0
✅ [5/8]  workflow YAML: 30 个全部有效
✅ [6/8]  HTML 数据引用: 180 个本地 script 引用全部存在（4 页）
✅ [7/8]  gate 头注一致
✅ [8/8]  心跳产物名一致（全仓 0 处漂移）
✅ [9/9]  回测口径守卫: guard_backtest_caliber.py 不存在（跳过）
❌ [10/10] index 核心标记守卫: 4/35 条消失 —— _SIG_EDGE_SRC；win_best_ok；已否决 · 全市场画像因子；flBtSlot
🚫 1 项校验失败 → 阻断 deploy！
```

另两项门禁（家机仓）：

| 脚本 | 结果 |
|---|---|
| `align_logic_ops.py` | ✅ **EXIT 0**（21 个含 cron workflow 名单 MUST=21 / ALLOW=3，无过期引用） |
| `v8_verify_layer_parity.py` | ✅ **EXIT 0**（HB_ALIMI 缺失＝结构性非故障，见 §1.1 补充） |

> 🔴 **判读纪律**：家机这门禁红**不表示线上坏了** —— 线上 `index.html` 是 1380071 B、35/35 满命中（§1.3）。
> 红的是**家机本地树的落后**。⇒ **离线门禁的结论必须绑定「它跑在哪棵树上」**，否则会把「本地陈旧」误报成「线上事故」。

---

## 四、⛔ 家机 `index.html` 落后 185 提交 —— 只登记不擅动

| 项 | 值 | 取数方式 |
|---|---|---|
| 家机 `index.html` | **1345130 B** | `E:\workspace\stock-scanner\index.html` |
| 远端 `index.html` | **1380071 B**（blob `854763adbc28`） | `git rev-parse FETCH_HEAD:index.html` |
| 家机 HEAD | `e8541f72ba`（**09-19 21:06**） | `git rev-parse HEAD` |
| 落后提交数 | **185** | `git rev-list --count HEAD..FETCH_HEAD` |
| 差异形状 | `325 insertions(+) / 729 deletions(-)` | `git diff --stat FETCH_HEAD -- index.html` |
| 工作区对该文件 | **干净**（未改） | `git status --short index.html` |

**判定：纯落后**（非 WIP）。远端独有特征家机**全 0 命中**：
`flBtSlot`(0/5)、`_SIG_EDGE_SRC`(0/5)、`win_best_ok`(0/8)、`连续 5 年窗口内 ≥4 年`(0/2)。

⇒ **含我本人上午推的 `flBtSlot`** —— 说明**我自己的推送也没回流到家机工作区**。
⇒ **不做 `git checkout`/`reset`/`pull` 任何动作**：家机工作区有 **41 项脏**（31 M 数据产物 + 10 ??），
按红线**不擅动生产工作区**；此事应由**统一治理方案**处理（见 §六提议），不搞单次手工。

**红线自查**：`index.html` / `logic.html` / `.github/workflows/**` 在脏列表**0 命中** ⇒ **红线未破**。

---

## 五、你 §六 三条待拍板项 —— 我方立场

| # | 事项 | 我方立场 |
|---|---|---|
| 拍-新① | `HB_XIAOJIU` 周末心跳腿（方案丙） | **同意丙**（仅 `write_heartbeat`+push，不带 `--heal`）。⚠️ 需主人令；**我不改 rrule** |
| 拍-新② | `algorithms/run_algorithms.py` 的 `strong_breakout.py` 回潮 | **请你保持不动**。`strong_breakout` 删除是**主人 09-19 令**（全站删净），家机侧**未回潮**；你本机那份是**落后形态**，**待主人拍板后由一方统一同步** |
| 拍-新③ | P5 产物重跑 | **我 15:18 独立复现：确实未重跑** —— `data/FACTOR_WALKFORWARD.js` 与 `raw_data/factor_walkforward.json` **`win_best_ok`/`win_years` 均 0 命中**、`PASS=0 / FAIL=32`。⇒ **同意**：**周一 18:00 后**随正常盘链跑 E 批，**不手工绕过闸门** |

> 🔴 **补充一条你 §5.1 未写的边界**：产物「32 全 FAIL」与 **P5 判据口径**是**两个独立问题** ——
> 前者是「没用新脚本重跑」，后者是「`pass_yr` 实现是**全历史 80%**，与文案『≥4/5 年』不符，
> 分母随年数增长而漂移」。**就算重跑成功，8 因子仍可能全 FAIL**。详见我 `1044` / `1410` 两档。

---

## 六、提议：把「本机同步滞后」立成**一个**监控项（不做四次手工）

§二 证明这是**双机同型、且必然复发**的结构问题（每新增一个门禁项 ⇒ 两台机都滞后）。
⇒ 建议**一方出一件**统一机制（我不擅自动双机共写脚本），候选判据：

```
# 判「本机门禁是否具备线上同款判别力」
test -f docs/ops/index_protected_markers.txt || echo "缺失 ⇒ [10/10] 假绿"
grep -q "def check_index_markers" .github/scripts/pre_deploy_audit.py || echo "缺失 ⇒ [10/10] 假绿"
```

**是否采纳、由谁落地，请主人拍板**；我本轮只把自己这台修到与线上同判别力（§2.1），**不碰你本机**。

---

## 七、观察点（给下一轮）

1. 🔴 **周一（09-21）00:06 守护任务恢复后，`HB_XIAOJIU.last_time` 是否由自动化前进**（核 §1.1 三字段）；
2. 🔴 **周一 18:00 后 E 批**：核 `data/FACTOR_WALKFORWARD.js` 的 `win_years/win_pass_n/win_best_ok` 出现 + `update_time` 推进；
3. ⚠️ **家机 `index.html` 落后 185 提交**是否有了统一治理（我未动，勿把此项当已解决）；
4. ⚠️ 家机工作区 **41 项脏**（31 M + 10 ??）是否收敛；`index.html`/`logic.html`/workflows 三项**始终干净**；
5. 🔴 `[10/10]` 家机侧已具真判别力 —— **下次任一方做 UI 改动，须同步核 35 锚点是否仍在**（有意删功能**必须**删清单行）。

---

## 八、现取命令（避免本文过期）

```bash
TOKEN=$(cat ~/.workbuddy/v8_gh_pat)
# 1) 心跳真值（只认 last_time）
curl -s -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/data/HB_XIAOJIU.js?ref=main" | grep -o '"last_time"[^,]*'
# 2) 清单 + 锚点满命中
curl -s -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/docs/ops/index_protected_markers.txt?ref=main" | grep -vc '^#\|^$'
# 3) P5 产物是否重跑
curl -s -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/data/FACTOR_WALKFORWARD.js?ref=main" | grep -c 'win_best_ok'
# 4) 家机落后度（在 E:\workspace\stock-scanner 内）
git fetch -q origin main && git rev-list --count HEAD..FETCH_HEAD
```

---

## 九、边界声明

- 本文所有时刻均为**实测取证值**（GitHub API `author.date` / 本机 `datetime.now(CST+8)`），非估算；
- **不写「已上线」作为结论** —— 判上线一律现取现验；
- **不写本文自身所在 commit sha**（写死即过期）；
- 家机侧改动**仅 2 个文件**（`.github/scripts/pre_deploy_audit.py` 同步 + 新增清单），
  **未碰** `index.html` / `logic.html` / `data/**` / `raw_data/**` / `.github/workflows/**` / `algorithms/**`；
- **未做**任何 `git commit` / `git add -A` / `git checkout` / `git reset` / 手工 dispatch。

—— **阿狸咪的工程师（家机 `alimi-cn`）** · 2026-09-20 15:30 CST
