# 阿狸咪的工程师 → 小九的工程师 · 交接（2026-09-20 20:38）

## 主题：overflow-y:hidden 12→9 复核结论 + 因子实验室迁移 4 标记存活确认

---

### 一、背景

家机工程师 20:22 硬抓线上 `index.html`，发现 `overflow-y:hidden` 计数从阿狸咪 15:29 修复版（`292764225c67`，12 处）变为 9 处。

初判为「小九侧 19:24 定向删了 3 处 `overflow-y:hidden`，可能令 3 个表格横滚时纵向溢出」。

经逐字节复核，**初判错误，特此纠正**。

---

### 二、纠正后的事实（git 实证）

| 版本 | overflow-x:auto | overflow-y:hidden（配 Y 表格容器） | 说明 |
|---|---|---|---|
| 阿狸咪修复版 `292764225c67`（15:29） | 16 | **12** | perf 12 处表格容器全配 Y |
| 当前线上 `acc01f49c`（20:38，小九） | 13 | **9** | 9 处表格容器**全部配 Y** |

**关键判别**：线上剩余 9 处表格容器**每一处都完整带 `overflow-x:auto;overflow-y:hidden`**，没有任何「只配 X 不配 Y」的退化表格。

12→9 的真实机制是：**3 个表格容器连同其 `<div … overflow-x:auto;overflow-y:hidden>` 被整段删除**，并非单独抠掉 `overflow-y`。即——

> 这是「删卡连带表格消失」，不是「定向删 Y 制造布局 bug」。

对应小九侧两次提交：
- `415b91fe1`（19:24，cloud-bot 代理）：「策略回测页 🧪 因子实验室回测卡整体移出本页」
- `acc01f49c`（20:38）：「删除『已否决·全市场画像因子』卡 + 因子计算卡去两步只留第一步」

三者均属主人在 09-20 下达的 **「v8 轻量化收尾 + 清除第二套算法」** 范畴，方向一致、合理。

---

### 三、阿狸咪侧迁移成果核验（4 标记全部存活）

家机工程师逐字节核验线上真身，因子实验室迁移（15:29）**未被波及**：

| 标记 | 线上计数 | 判定 |
|---|---|---|
| `if(name==='fa'){` 渲染分支（switchFinalTab 内） | 1 | ✅ 存活 |
| `final-pane-fa` 子页容器 | 1 | ✅ 存活 |
| `data-final="fa"` | 1 | ✅ 存活 |
| `st-fa` 残留（应=0） | 0 | ✅ 干净 |

另有专职防覆盖守卫自动化 `f01daa2c`（阿狸咪-v8 星级改动覆盖守卫，每 30 分·只读·仅异常报警）持续监控，上述 4 标记自 15:29 起始终存活、从未报警。

---

### 四、阿狸咪侧结论与处置

1. **12→9 不构成覆盖事故**，是九侧轻量化删卡的合理连带；**无需回滚、无需 re-patch**。
2. 阿狸咪侧**认可**九侧的两项清理（因子实验室回测卡移出、画像因子卡删除、因子计算卡去两步），与主人令方向一致。
3. 阿狸咪侧**未做任何 index.html 改动**（本地工作树除 `data/` 下 4 个 Nutstore 同步产物文件外，已与远端 ZERO_DIFF）。

---

### 五、提请小九侧确认 / 补记录（2 项）

1. **请补一份署名交接件**，说明 19:24 / 20:38 两次删卡的具体清单（哪些卡、哪些表格），并明确声明：上述动作**未触及**阿狸咪 15:29 的因子实验室迁移（final-pane-fa / data-final="fa" / switchFinalTab 的 fa 分支 / st-fa 4 项）。
2. **双机协作铁律重申**：两机都直接改 `index.html` 仍是覆盖温床。后续任何一机改 `index.html`，请遵守判据 166——推前 `git fetch` 以当前远端 tip 为基线重打补丁、只改必要处、推后回读逐字节比对。已有守卫 `f01daa2c` 只读监控，但**只防「迁移 4 标记被顶掉」，不防语义删卡**，删卡类动作仍需双方事前对齐。

---

### 六、附：待主人拍板事项（已建观察任务）

- P5 产物 `FACTOR_WALKFORWARD.js` 重跑观察：一次性自动化 `c6e8c391-4c92-439c-a27c-404d7fff2267`，**09-21 18:30 CST** 自动硬抓核验 `win_*` 三字段 / `verdict:PASS` / `update_time` 是否推进，达标与否均邮件 `2814546@qq.com`。
- 本机工作树全量对齐已完成（data/ 4 文件因 Nutstore 双机同步毫秒级重写列为例外，不发布 Pages）。

---

阿狸咪的工程师  
2026-09-20 20:38 CST
