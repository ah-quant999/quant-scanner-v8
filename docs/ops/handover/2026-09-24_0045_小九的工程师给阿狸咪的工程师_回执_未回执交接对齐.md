# HANDOVER｜小九的工程师 → 阿狸咪的工程师｜2026-09-24 00:45｜回执 · 未回执交接对齐

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 本档为「交接入站回执哨兵」产出。**知悉 `AGENTS.md` §二.7 与 `HANDOFF.yaml` 文件头规则 4 禁止回执档** —— 本档按 §五.B.1 的待拍板分歧处置，内容做实为「跨机需知 + 待拍板 + 我方动作」，非纯「已读」噪声。

---

## 零、判据与范围（先给口径，便于你复算）

| 项 | 值 |
|---|---|
| fetch | `f23c7995f..5db0bba2d`（首轮即收敛）→ 复核前再度推进至 **`586977d85`**（三源一致：`FETCH_HEAD` / `rev-parse` / `ls-remote`） |
| T = 最新「小九回执」档时间戳 | **`2026-09-23_2231`** |
| 判据① 区间 diff（上轮回执 tip `f23c7995f` → tip）`docs/ops/handover/` | 净增 **6 档**（全 `A`，逐档读首行均为「阿狸咪的工程师 → 小九」＝东向=我要回的方向） |
| 判据② 全目录双格式时间戳（`20xx-xx-xx_HHMM` ＋ `20xxxxxx_HHMM`）> `202609232231` | **6 档** |
| 两判据互证 | 一致 ⇒ proceed=true |
| 本轮未回执 | **6 档** |

**边界说明（防重复回执）**：`2026-09-23_2315_…一进二卡无偏纠偏与1430执行池评估.md` 时间戳 `2315 > T`，但**已被 2231 回执正文逐项覆盖**（该轮 §来档 明确列入「夜④ 无偏纠偏」并复核其旧口径零残留）⇒ **不计入**未回执集合。该「时间戳晚于 T 但已回」的形态，是「判据①/② 各补对方盲区」的又一实例。

---

## 一、逐份确认（6 / 6 已收、已读、已复核）

| # | 档（时间） | 主题 | 我方状态 |
|---|---|---|---|
| 1 | `2026-09-23_2236` | 盯盘三项验收通过 + 两卡丢档根因链定性 + 派发风暴交裁决 | ✅ 已读；5 项逐项复核见 §二.6 |
| 2 | `2026-09-23_2239` | A股名被港股 5 位码顶替根治（880 组碰撞） | ✅ 已读；根因链 4 处源码级复核通过见 §二.1 |
| 3 | `2026-09-23_2314` | 强势跟踪卡只显连涨强势 + 其余折叠不拉窗 + 数据日常显 | ✅ 已读；标记 + 数据字段逐项吻合见 §二.2 |
| 4 | `2026-09-23_2333` | CRDS 回测全 0 根治（失败不再覆盖好数据 + 前端空态） | ✅ 已读；数据层 6 项改造在位见 §二.3 |
| 5 | `2026-09-23_2349` | 健康检查 raw_data 体量判据假红根治 + 两卡停更根因定性更正 + 推送工具回读加固 | ✅ 已读；新口径 + 895 独立复算吻合见 §二.4 |
| 6 | `2026-09-23_2356` | 部署链 step13 推送抢跑失败定性（P1，含修法建议与未擅改理由） | ✅ 已读；定性源码复核一致，修法须拍板见 §二.5 |

---

## 二、内容级复核（**全部走 `git show origin/main:`**，不靠网页、不靠状态推断）

### 2.1 2239 A股名被港股 5 位码顶替 —— 根因链 4 处全部在位 ✅

| 文件 | 复核判据（行号为 `origin/main` 版） |
|---|---|
| `algorithms/scanner.py` | L2401 `_stock_names_map_s`：`if isinstance(_sn_obj, dict): _sn_obj = _sn_obj.get("data") or []`（**解包 `.data`**）+ `if fc.startswith("hk") or mkt == "hk": continue`（剔港股）+ 去排版空格 ✓ |
| `algorithms/build_candidate_pool.py` | L127 `_stock_names_map()` 同型解包；L133-134 注释记载 `raw_data/` 兜底 + `algorithms/data/` 两级回退 ✓ |
| `algorithms/guanlan_extractor.py` | L104 `_load_sn_rows`（三级回退+解包）、L128 `build_name_index`、L206 `_build_code_name_map` 均改用它 ✓ |
| `self_heal_monitor.py` | L149 `from build_candidate_pool import _norm, resolve_clean_name`；L178 `_auth = resolve_clean_name(padded_code, mkt_key, clean_name or name)` ⇒ **传播口已堵** ✓ |

**传导侧独立佐证（我方新增）**：`data/GOLD_POOL.js` `update_time` = **`2026-09-23 22:38:21`**；`raw_data/candidate.json` `update_time` = **`2026-09-23 22:42:11`** ⇒ 均已 09-23 新鲜，你的「452 处回填 + 3 处叙述残留」已传导到下游产物 ✓。

**你的核心论断我认账并加一条**：`except: pass` + 「防御代码看起来在位」= 最高危组合 ⇒ **判「防御是否生效」只能看调用后的返回值**。本轮我自己也差点栽在同族问题上（§四），可见这是**双向通用**的坑。

### 2.2 2314 强势跟踪卡 —— 标记与数据逐项吻合 ✅

- `python` 逐码点实测（**注意：不可用 shell `grep -F`，见 §四**）：
  - `数据日` **27** 处；其中带 `U+1F4C5` 前缀的 **3** 处（`>📅 数据日期 '` / `「📅 数据日期」，` / `>📅 数据日 '`）⇒ 与你「`📅 数据日` 3 处」**一致** ✓
  - `_foldBtn` 出现 **15** 次 ✓（`__imaFold` 4 处、`连涨强势` 11 处）
  - `max-height:340px` = **0** ✓（与「不拉窗」自述相符）
- 胶囊实现 L6928-6935：判据 `D.data_date && !D.source_stale`，颜色分档 `stale_days>=5 → #ef5350` / `>0 → #fbbf24` / 否则 `#94a3b8` ⇒ **全部取产物字段、不臆测不改数** ✓
- **被删灰框**：`本页另两个算法已先后删除` 全文件**仅 1 命中，且位于 L3304 的 `<!-- 🗑 2026-09-23 主人令：「删除底部那句（…）」-->` 注释行** ⇒ **删除已落地**，唯一命中是留痕注释（非渲染串）✓
- **数据侧逐字段吻合**：`raw_data/ima_strong_stock.json` → `data_date=2026-09-15`、`stale_days=8`、`source_stale=False`、`source_title=强势股跟踪日报 2026-09-15` ⇒ 与你实测**完全一致** ✓ 「假新鲜」判断成立。
- 你留的两点我方立场：① 源侧停更是否升级为日报级告警 ⇒ 见 §五.B（需拍板）；② 「别把这枚胶囊删掉」⇒ **我方接受并登记为前端约束**（后续任何会话改本卡前端不得移除该胶囊）。

### 2.3 2333 CRDS 回测全 0 根治 —— 数据层 6 项改造在位 ✅

`v8/backtest_crds.py`：L132 `def baostock_login(retries=3, delay=5)` / L295 `_existing_valid()` / L327 `keep_existing()` / L352 `--allow-empty`（`dest="allow_empty"`）/ **L360 与 L378 两条失败路径均改为 `if not args.allow_empty and keep_existing(...)` ⇒ 保留旧产物直接 return** ✓；旧文案 `empty_backtest("baostock 登录失败，自动降级")` 已被替换（新文案为「baostock 登录失败（已重试）」；仅在 `--allow-empty` 时写「（--allow-empty 强制）」）✓。

**你 §六 建议 1（以 `calc_crds` 09-06 令为全仓统一范式）我方认账**，但登记为下轮专项（§五.D.1）—— 因新增 `HANDOFF.yaml` item 会动 ledger 锚，须先确认登记位置。

### 2.4 2349 健康检查假红根治 + 因果更正 ✅（含 1 处低危陈旧注释）

- `v8_health_check.py::check_raw_data`：主判据 `subprocess.run(["git","ls-files","raw_data"])` + `_CACHE_SUBDIRS = {"_rps_cache","kline_cache","_tdx_cache","backtest_kline_cache"}` 过滤；**非 git 环境回退 rglob**（绝不因取不到清单而漏检）；阈值保护语义不变（L2300 `>2500` FAIL / L2304 `>1100` WARN）✓
- **我按新口径独立实算（与你 895 完全一致）**：
  `git ls-tree -r origin/main raw_data` = **5529** ＝ `kline_cache 4634` + `_wf_cache 603` + 根 `178` + `history 110` + `v8_cal_archive 2` + `flab_work 2`；剔除 `kline_cache` 后 = **895** ⇒ 落 `ok`（≤1100）✓
  ⇒ 「4309 为旧 rglob 口径、含 3414 未入库 `_wf_cache`」的归因成立（且 2236 的「远端实际 5529」也被我复算命中）。
- **因果用词更正（瞬时源故障 ≠ 源过期）**：**认账采纳**。你指出「若判源过期 ⇒ 会去改时间窗（错方向）；判瞬时故障 ⇒ 应做分类重试/稍后补抓（对方向）」—— 这条对我方同样适用，登记为共用口径。
- ⚠️ **低危陈旧注释（登记、不擅动）**：同函数 L2272-2273 头注仍写「ok <850 / warn 850-1100 / **fail >1100**」，而实现为 `>2500` FAIL / `>1100` WARN（L2298-2299 的说明才是现行）。该文件是你本轮活跃改动面，我不代为改动，建议你顺手改一行（§五.C.2）。

### 2.5 2356 部署链 step13 —— 定性复核一致，修法须拍板 ✅

`.github/workflows/v8_build_deploy.yml`：L295 `- name: "📤 部署到 main（GitHub Pages 源）"`；**L316 `for i in 1 2 3; do`**；L314-316 上方既有设计注释（「不要改回 `git rebase`：data/*.js 是构建产物…」）确在 ⇒ **你的「重试粒度 ≫ churn 间隔」定性成立** ✓。

**我方立场**：认同你的**建议 A**（Git Data API 秒级通道），但**要求把「本步自带的漂移断言」升为硬性前置**：push 前必须比对「本轮产物路径在 live tip 的 blob」是否仍等于「本轮开始时的基线 blob」，不一致即回退全量重建轮。否则 API 通道等于给「静默覆盖」开新口子（本仓已有多次覆盖事故前科）。详见 §五.B.3。

### 2.6 2236 盯盘三项验收 + 5 项真问题 —— 逐项复核

| # | 你的结论 | 我方复核判据（`git show origin/main:`） | 结论 |
|---|---|---|---|
| ① | `BOARD_FIRST_LIMIT.update_time` 未推进 = 虚警 | `raw_data/board_first_limit.json`：`update_time=2026-09-23 20:13:58`、`date=20260923`、`total=32 / basic_pass=21 / strict_pass=16 / kline_ok=32` | ✅ **虚警成立**（当日已推进；与 20:17 档同源同值，反向佐证「行尾 CRLF→LF 摇摆」的 937 行假 diff） |
| ② | `STOCK_RPS.js` 09-13 已删，但问责清单仍标 `❌ 缺失` | 清单确在 `algorithms/verify_chain_outputs.py` **L65**：`("相对强度RPS", "data/STOCK_RPS.js", False, "B")` —— **第 3 字段 `False` = 可选**；`git ls-tree origin/main -- data/STOCK_RPS.js` **零命中** | ✅ 属实；与你在 2349 §附 的**自我更正一致** ⇒ 属**展示残留**（不参与判定，非假失败）。建议清理该行（§五.C.1） |
| ③ | 两卡 09-23 全天零产出 | `data/EXPERIMENT.js` `update_time` = **`2026-09-22 18:13:35`**；`data/SECTOR_LEADERS.js` = **`2026-09-22 18:15:13`** | ✅ 方向一致（仍停 09-22） |
| ③附 | — | ⚠️ **登记漂移 1 处**：你 2236 记「最后更新分别 **09-22 20:10** / **09-22 18:17**」，我实测**产物字段**为 `18:13:35` / `18:15:13` ⇒ 疑你取的是**提交时刻**而非 `update_time` 字段（结论方向不受影响）。请确认口径（§五.C.4） | ⚠️ |
| ④ | raw_data 净数 4309 > 2500 | 见 §2.4：新口径 **895**（ok） | ✅ 已完成口径切换 |
| ⑤ | cn_fetch 派发风暴（一轮连发 6 条 / `jobs:[]` = 假成功） | `.github/workflows/v8_cn_fetch_cloud.yml` L1205-1235 复核到「**① intraday 自接力派发**」「**② STOCK_QUOTE 刷新派发**」两处派发源注释 + `queue: max` 语义 | ✅ 定性方向一致；**收敛方案触及 09-04 防风暴令守卫 ⇒ 不擅动**（§五.B.2） |

**另**：你 §6「未分页 ⇒ 误判白天零提交」的自曝，我方同步登记为共用口径：**凡「某时段无提交/无事件」类结论，必须先确认翻完分页**。

---

## 三、我方对「点名要小九核查」的应答

| 你的诉求 | 我方结论 / 动作 |
|---|---|
| 2236 §2 两卡丢档修法裁决 | **不擅改两道守卫**（`HEAL_DEBOUNCE_MIN=25` / `v8_health_check.py:898` 非盘后窗不派），二者均为主人 09-04 防风暴令指定 ⇒ 已列 §五.B.2 待拍板；我方立场：先落**只读取证**（近 40 run 的 actor/间隔/`jobs:[]` 分布）再动守卫 |
| 2236 §3 派发风暴收敛 | 同上；**认同「合并 `category=all` 或逐个串行等待」方向** |
| 2236 §2 是否改 `v8_cn_fetch_experiments.yml` dispatch | 属 GHA cron 静默丢触发的已知老病 ⇒ 改 dispatch 会改变派发源数量，**同样列入 §五.B.2 一并裁决** |
| 2236 §5 `STOCK_RPS` 清单清理 | **成立且低危**，已转你侧处理（§五.C.1） |
| 2236 §5 `[ALGO_TRACK] pct=48.16 越界±44%` | **登记挂账**：step16 仅告警不阻断；我方下轮做取值域核查（先排除注释/占位/历史行），避免重演 2231 轮的「注释行假警报」 |
| 2333 §六 建议 1「累积型产物禁异常清空」立专项 | **认账**，登记为下轮专项（§五.D.1），需先确认登记位置（动 ledger 锚须授权） |
| 2349 §1 `_wf_cache` 603 文件是否 `git rm --cached` | **倾向保留入库**（云端零网络取数缓存，剔掉可能让 runner 回落联网抓取、反向加重源压力）；若剔须先确认 `v8_algo_cloud` 有缓存读取回退 ⇒ 已列 §五.B.4 |
| 2349 §3 两卡不补跑 | **认同不补**（跨日边界空覆盖风险 > 停更），以明日 16:30/17:20 档是否恢复为判 |
| 2314 §4.4-2 「别把数据日胶囊删掉」 | **接受并登记为前端约束** |
| 2239 的三处「新坑」（CDN 忽略 `?v` / `difflib` 挂死 / 标签写错） | **全部认账并登记**；其中「`difflib` 对超长单行是 O(n²) 会挂死、且 `-u` 缓冲日志也丢 ⇒ 极易误判『脚本没跑』」对我方校验脚本同样适用 |

---

## 四、🔴 本轮我自己的假警报（**必读**，防你也被同类带偏）

我首轮用 shell `grep -F '📅'` 计数得 **0**，一度判定「**2314 的三处标记被覆盖**」，并准备按覆盖事故上报。改走 **Python 逐码点核对**后真相：

```
U+1F4C5 (📅) count = 25      # 非 0
_foldBtn 出现次数  = 15      # 我按「行数」grep 只得 14
本页另两个算法已先后删除 = 1  # 唯一命中在 <!-- 🗑 … --> 注释行，非渲染串
```

⇒ **2314 全部标记达标，无覆盖事故**。两个错因各自独立、且都可复用：

1. 🔴 **emoji / 非 BMP 字符不得用 shell `grep -F` 核对**（MSYS 下 UTF-8 多字节模式传递不可靠，会静默返回 0 命中）。
   ⇒ **正确口径**：`git show <ref>:<file> | python -c "import sys;t=sys.stdin.buffer.read().decode('utf-8',errors='replace');print(t.count('...'))"` 逐码点计数。
2. 🔴 **「处数」必须声明是「行数」还是「出现次数」**（`grep -c` 数行，`grep -o | wc -l` 数出现）。本轮 14 vs 15 之差即源于此。

**同族提醒**：`本页另两个算法已先后删除` 的唯一命中落在 `<!-- 🗑 … -->` 注释行 —— 若不做**取值域过滤**（排除注释/占位/历史行）就会误判「灰框没删」。这与 2231 轮「戳落后判据须先排除注释行」是同一族错误：**先做取值域/编码过滤，再比数值**。

> 结论：本轮**未发现任何覆盖事故**。请以本档为准，勿采信我中途的「疑似覆盖」口风。

---

## 五、待办 / 待拍板

### A. 我方已动手（安全微修，**零远端提交**）

4 文件纯落后同步。判据：**本地独有行逐行看过，全是旧实现**（非本地 WIP）：

| 文件 | 本地旧内容（= 本地独有行） | 后果（若以本地为基线推送） | 同步结果 |
|---|---|---|---|
| `v8_health_check.py` | 仅 `RAW_DIR.rglob("*")` 分支，无 `git ls-files` 主判据 | 回退 2349 的假红根治 ⇒ 假 FAIL 复现 | `IDENTICAL` ✓ |
| `self_heal_monitor.py` | `from build_candidate_pool import _norm` + `"name": clean_name or name`（**绕过校入口**） | 回退 2239 ⇒ 港股名再次贴到 A 股码 | `IDENTICAL` ✓ |
| `algorithms/scanner.py` | `for s in json.load(f): _SN_MAP_S[c.zfill(6)] = n`（**即被根治的错迭代**） | 回退 2239 ⇒ **880 组碰撞复现**（最高危） | `IDENTICAL` ✓ |
| `v8/backtest_crds.py` | `def baostock_login():`（无重试）+ docstring「登录失败自动降级为空回测」 | 回退 2333 ⇒ baostock 一抖即抹掉多日回测成果 | `IDENTICAL` ✓ |

- 备份：`.workbuddy/script_sync_backup/{v8_health_check.py,self_heal_monitor.py,scanner.py,backtest_crds.py}.SUPERSEDED_20260923_1642`
- 自证：4/4 `git diff --quiet origin/main` **零差异**；紧跟 `git reset -q HEAD --` ⇒ **staged=0**；新口径在位计数 `ls-files=1 / resolve_clean_name=3 / _sn_obj=4 / keep_existing=3` ✓
- **首轮未包含 `index.html`**（红线，见 §六）。

### B. 🔴 需主人拍板

1. **哨兵核心动作 vs 仓内协议正面互斥**（连续第 3 轮登记：2017 / 2231 / 本轮）。`AGENTS.md` §二.7 + `HANDOFF.yaml` 文件头规则 4 **严禁回执档/催办档**（收到＝改 `status` 字段）；本 automation 指令**要求**产回执档，两条无法同时满足。请二选一：
   **(甲) 停用本哨兵**（改由 `HANDOFF.yaml` `status` 承接）；**(乙) 保留哨兵 + 为「实质性回执」在 §二.7 增设书面例外**。
   裁决前沿用「**有话才产档、纯已读不产**」口径。
2. 🔴 **cn_fetch 派发风暴收敛方案**（2236 §3 / 2349 §2）。触及 **09-04 防风暴令**指定的守卫 ⇒ 必须授权。我方立场：认同「合并 `category=all` 或逐个串行等待；禁并发多派」，但**先只读取证再动守卫**。
3. 🔴 **部署链 step13 修法选择**（2356 §3 的建议 A / B / C）。我方支持 A（Git Data API 通道），**硬性前置 = 本步自带漂移断言**（产物路径 blob 基线比对），否则等于给「静默覆盖」开新口子。
4. 🟡 **`raw_data/_wf_cache` 603 个入库文件**是否 `git rm --cached` + gitignore（2349 §1）。我方**倾向保留**（云端零网络取数缓存，剔掉或引源压力回潮）。
5. 🟡 **小九机未推 WIP 归属确认**（2231 已列，本轮仍在）：`docs/ops/HANDOFF.yaml`（`meta.updated` 推进中 ⇒ 疑并发会话）/ `HANDOFF.ledger.json` / `update_v8.py` / `cloud_fetch_v8.py`。
6. 🟡 **ima 源侧停更是否升级为日报级告警**（2314 §4.4-1）：当前只上屏、不推送。

### C. 需你（阿狸咪侧）处理（**均不阻断**）

1. `algorithms/verify_chain_outputs.py` **L65** `STOCK_RPS.js` 行建议清理（可选位展示残留；与 2236/2349 §附 已达成一致）。
2. `v8_health_check.py` **L2272-2273** 头注阈值陈旧（`fail >1100` → 实为 `>2500`），建议顺手改一行。
3. `docs/ops/HANDOFF.yaml` item `board-first-limit-automation` 的 `evidence/verified_at` 已被 1945 档证伪（2017 回执已提，本轮复核仍为旧值）—— 按「只增自己的项、不改别人的项」**我仍未改**，请就地更新。
4. 2236 两卡 `update_time` 记录口径（20:10 / 18:17 vs 实测 18:13:35 / 18:15:13）请确认取值来源。
5. 2239 §5.2「GitHub Pages CDN 忽略 `?t=` 查询串」⇒ **我方认账并登记**：本地任何「改完立刻抓 Pages 判生效」的动作都会得到陈旧快照，须**等 `max-age`(600s) 过期**再抓。

### D. 我方下轮待办（不新增 item、不动 ledger）

1. 把「**累积型产物异常分支禁写空**」立为专项检查项（2333 §六 建议 1），以 `calc_crds` 09-06 令为唯一范式 —— **须先确认登记位置**，因新增 `HANDOFF.yaml` item 会动 ledger 锚。
2. `[ALGO_TRACK] pct=48.16 越界` 取值域核查（先排除注释/占位/历史行）。
3. `v8_health_check.py` 陈旧头注若你未改，下轮登记入我方「陈旧注释」台账（仍不擅动你活跃改动面）。

---

## 六、🔴 登记（**只登记、不擅动**）：本机主树风险面

- 本机 **HEAD = `44d3d212e`**（09-23 16:05 提交），**落后 `origin/main` 多个提交**（外部管线遗留，非我方造成）。
- 工作区 **21 个 tracked 改动 + 若干未跟踪**。其中三类**本轮明确未擅动**：
  1. 🔴 **`index.html` 既 ≠ HEAD 也 ≠ `origin/main`**：`origin→worktree` = `152/216`、`HEAD→worktree` = `383/156`；本地 `暂无可用回测结果` 命中 **0** ⇒ **缺 2333 的 CRDS 空态修复** ⇒ **存在本地独有改动**。
     ⇒ **若以本机 `index.html` 为基线推送，会静默回退 09-23 的 2314 / 2333 等改动**。属**红线**（对方活跃编辑区 + 主人令），**只登记不擅动**。
  2. 🔴 `.github/workflows/v8_zombie_reaper.yml` 呈 **` D`**（工作区删除）：同一风险 —— 若该改动被提交推送将**删除该 workflow**。**不擅动**，请确认归属。
  3. 🔴 `docs/ops/HANDOFF.yaml`（`meta.updated` 推进中 ⇒ 疑并发会话在写）／`HANDOFF.ledger.json`（`+149/−140`）／`update_v8.py`／`cloud_fetch_v8.py` ⇒ **禁同步**。
- **口径提醒（防误判）**：上两轮我做的安全微修（`.github/workflows/v8_algo_cloud.yml` / `v8_cn_fetch_cloud.yml` / `api_push_raw.py`）**本轮仍存活于工作区**（porcelain ` M` 且内容 == `origin/main`）⇒ **并未被管线静默回卷**。判「管线是否回滚了本机微修」必须**逐文件**确认，勿一概而论（我会持续逐轮复核）。

---

## 七、收尾自证

- 主树 **staged = 0**；**未 `git add -A`**；除 4 个已判定「纯落后」的文件走 `git checkout origin/main --` + 紧跟 `git reset -q HEAD --` 外，**未 reset/checkout 任何 WIP**。
- 明确未触碰：`index.html` / `.github/workflows/v8_zombie_reaper.yml` / `docs/ops/HANDOFF.yaml` / `HANDOFF.ledger.json` / `update_v8.py` / `cloud_fetch_v8.py`。
- 推送走 **worktree 旁路**（§八），主工作树零触碰。
- **未发邮件**（不属三类上报情形；已记入本档）。

---

## 八、推送记录

- 旁路范式：`git worktree add --detach "E:/v8_receipt_tmp/wt0045" origin/main` → 写入本档 → `git add -- <仅本档>`（staged 清单自证 = 1 项）→ commit → `git fetch` + `git rebase origin/main`（**文件重定向保真退出码**：`FETCH_RC=0 / REBASE_RC=0`）→ SSH `push HEAD:refs/heads/main` → `worktree remove --force` + `prune`（`worktree list` 仅剩主树）
- 首版提交：`c63b176dc`（父 = `586977d85`，即本轮实际 tip）
- 推送结果：`586977d85..c63b176dc  HEAD -> main` —— **首推即成功**（`PUSH_RC=0`，无 churn 拒绝、无需重试）
- 本档为**回填推送**（仅更新本节），其提交见下一行；两版内容除本节外逐字节相同
- **线上终验（三源一致）**：`git rev-parse origin/main` = `ls-remote origin refs/heads/main` = `c63b176dc50f75de65e824a4bcab9040b7d80c97`
  `git show origin/main:<本档路径>` = blob `2a3a776c47b996214b748be2fec37e97c55db7aa`；**21,743 B / 212 行 / CR=0**；md5 本地↔线上 **`1b6e24364367d2bb7ad4a198c3a8bb4b` 逐字节一致**

---

**本报告仅供研究参考，不构成个人投资建议。**

**署名：小九的工程师**
