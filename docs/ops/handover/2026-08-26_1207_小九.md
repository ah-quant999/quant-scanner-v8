# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 更新于 2026-08-26 12:07 自动更新（automation-1785719698947 · 12:00 档 · 权威修正重推版）
> 说明：12:05 同档并发实例已预建一版，但其 **main SHA 记为 9606d8ca4（已过时，真实数据 tip 为 885bef08c）+ 自动化计数错写 14 ACTIVE+1 PAUSED=15（实为 16 ACTIVE+1 PAUSED=17）**。本档以 `git ls-remote` + `git log origin/main` 实核重写，为最终权威版。

---

## 一、背景

- **日期/时段**：2026-08-26（周三），小九 12:00 当班跑批。
- **主人状态**：HH 出差中（8/21–8/27），全天基调＝一劳永逸修复 + 出差前健康检查（前几日已落地，本日无新人工修复诉求）。
- **双机分时铁律**：小九白天独家（7:45–17:45）；阿狸咪夜间/周末（18:00–次日 7:30 + 周末）部署 v8。本档交班后由阿狸咪接手夜间维护。
- **当日工作性质**：本日全部为**自动化跑批产出**，无 HH 人工修复提交。信息源 = v8 侧 `2026-08-26.md`（共 11 条自动化执行流水）+ `git log origin/main` 实核。stock-scanner 侧当日日志自 08-07 起断更，沿用 v8 侧。
- **本地 WIP 处理（重要·勿碰）**：本机原先约 100+ 项在飞 data 改动 + index.html/HEALTH_CHECK.js 等，已被并发实例 `git stash push -m handover-20260826-wip-preserve` **保全入 stash（stash@{0}）**，**未入库、未丢失**。**请勿 pop、请勿 drop、请勿 commit**——避免旧 data 覆盖新鲜 origin/main 数据。本档生成后工作树仅余 1 个无关 untracked 补丁（`backup/v8_runner_guard_start_process_fix_20260825.patch`），未纳入、未触碰。

---

## 二、今日已完成改动（真实 commit SHA + 影响，全部源自 `git log origin/main` 实核）

> 以下 SHA 均从仓库 `git log origin/main` 实时核实（未用记忆值）。本日**无 HH 人工修复提交**，全部为云端/本机自动化产出。

| SHA | 提交说明 | 影响 |
|-----|----------|------|
| `9b732ea8c` | docs(handover): 08/26 交接自动更新 12:05（并发预建版，本档为其修正重推） | 交接文档占位（HEAD 已被本档覆盖） |
| `885bef08c` | v8 build: 2026-08-26 12:04 | **数据真实最新 tip** · Pages 构建产物（缓存戳 ?v 实时对齐） |
| `9606d8ca4` | data: westock 持仓同步 (12:03) | 腾讯自选股持仓最新净值/盈亏注入 PORTFOLIO.js，前端持仓卡刷新（885bef08c 之祖先） |
| `7691db22e` | chore: 缓存戳实时对齐 ?v → 防覆盖 (11:53) | index.html 内 data/*.js 引用戳实时对齐，防 CDN 旧缓存覆盖 |
| `064a679f5` | chore: 盘中算法追踪轻量刷新 11:52 | ALGO_TRACK 盘中轻量刷新 |
| `50e14361a` | v8 build: 11:36 | Pages 构建产物 |
| `d9bd1935d` / `1e3bb82ba` | chore(stock-quote): refresh STOCK_QUOTE via v8_stock_quote_refresh | **STOCK_QUOTE 已刷新**（此前陈旧盲区，8/20 已点名根治） |
| `c7a96fa62` | v8 cn fetch: 11:34 | 盘中 28 模块中文源抓取 |
| `57f409fc3` | v8 build: 11:32 | Pages 构建产物 |
| `c8e4f4176` | v8 risk gauge: 11:30 | 危机雷达刷新 |
| `41e9db4d9` | v8 build: 11:14 | Pages 构建产物 |
| `6904f8f3a` | data: runner 状态上报 20260826-1113 | runner 心跳上报 |
| `28c2fb57c` / `f5e519232` | v8 build: 11:11 / 11:10 | Pages 构建产物 |
| `da7b26ff2` | chore: 缓存戳实时对齐 ?v → 防覆盖 (11:07) | 缓存戳防覆盖（早轮） |
| `ee95f759c` / `77e4440a0` | data: 智能触发补抓(盘中·guard) / 隔离 index 推送（10:08 全时段数据兜底产出） | 10:08 兄弟任务 STALE:9→intraday 15/15 补抓后隔离推送，为上面各项祖先 |

**关键自动化执行（来自 v8 侧当日日志）**：
- `10:08` 全时段数据兜底（automation-1785898357370）：STALE:9 → `cloud_fetch_v8 --category intraday` **15/15 模块成功**，新鲜度门禁 19/19 本地≥云端，隔离 index 推 `77e4440a0`；期间 1 个孤儿 UU `raw_data/runner_status.json` 被重写清除冲突标记；新坑（已沉淀 MEMORY.md）：Python Windows `open(...,"w")` 清单为 CRLF → bash `while read` 静默少推，必须 `tr -d '\r'` 且 push 前核对 diff 文件数。
- `10:33` 每小时紧急交接通道（automation-1785835481750）：六路全 OK，健康 rc2，0 封邮件；**上棒 09:29 的 9 项盘前 FAIL 全部转 OK**（10:09–10:15 落地），板块推荐/驾驶舱/概念排名内容戳复核闭合。
- `00:10`/`09:19` 盘中漏轮兜底（automation-1787108824799）：盘前空转正确（铁律1）；`10:19`/`11:12` 窗内 FRESH 不动作——缺口已被 10:08 兄弟任务先行补齐，正确避免重复重抓。
- `00:11` 小九-v8 runner 本地守护（automation-1786696074524，第109次）OK：guard --heal 退出码 0，Listener PID 6740 存活（自 8/25 19:50 重启后约 16h）。
- `03:00` 自愈监控闭环（automation-1786961773708，第38轮）exit=2：P0 三项全绿，exit=2 仅因 `zsxq_token.json` 本地缺失（连续第 38 轮同因，云端走 Secret 兜底 → 生产零影响）。

---

## 三、当前系统状态

- **站点**：`https://ah-quant999.github.io/quant-scanner-v8/` → **HTTP 200**（实拉确认，Content-Length ≈ 997KB）。
- **main SHA（origin/main 实核）**：推送前 = `9b732ea8c`（12:05 交接提交，本档为其修正重推）；**数据真实最新 tip = `885bef08c`（v8 build 12:04）**，之上为 `9606d8ca4`（12:03 westock 同步）。
- **健康总览**（来自 10:33 看门狗，最近一次全量核证）：`data/HEALTH_CHECK.js` → overall = ok，summary **ok 106 / warn 1 / fail 0 / total 107**，updated `2026-08-26 11:52:27`。唯一 warn = raw_data 文件数 >850 阈值（非数据异常）。
- **Runner**：
  - `lemoncat-cn`（单位机）：online，idle 兜底正常。
  - `alimi-cn`：离线（夜间由阿狸咪家用机接管）。
  - `v8_runner_guard` Listener PID 6740 存活（自 8/25 19:50 重启后约 16h），本机守护 00:11 第109次 OK。
- **剩余自动化（实核：16 ACTIVE + 1 PAUSED-TDX = 17 个）**：
  - **PAUSED（1）**：`1786933089412` 小九-TDX强势选股每日跟踪(三件套v2·盘中+盘后)
  - **ACTIVE（16）**：`1787575410637`(James-Stein回测) / `1787210164001`(westock持仓云端同步) / `1787108824799`(盘中漏轮兜底) / `1786961773708`(自愈监控闭环) / `1786696074524`(runner本地守护) / `1786588096327`(分红盘前) / `1786588094908`(分红晚间) / `1785978303212`(每日19点审计) / `1785898357370`(全时段数据兜底) / `1785888730897`(盘前全量08:15) / `1785835481750`(每小时紧急交接通道) / `1785802406605`(本地看板同步) / `1785719698947`(本交接文档12/18点) / `1785404904506`(17:00选股四模块) / `1785396622150`(周末15:30 T+1) / `1785388522458`(15:30收盘刷新)
  - 本日已跑（节选）：`1787108824799` 漏轮兜底(:10/:40) / `1786696074524` runner守护(每30分) / `1786961773708` 自愈闭环(每小时) / `1785898357370` 数据兜底(智能触发) / `1785835481750` 紧急交接通道(看门狗)。

---

## 四、需阿狸咪注意 / 待办 / 风险

1. 🔴 **【P0·待授权】`v8_runner_guard.py` 根治修复未入库**：本地修复（+39/-8，日志重定向分离 + Listener 轮询真判据）经 `00:11`/`03:00` 两轮交叉确认**仍仅存本地工作树（已在 stash `handover-20260826-wip-preserve` 中）**，origin/main 侧 guard 仍是坏版本（远端 `guard_start.out.log` 命中 0）。需**主人授权推送**才算根治——阿狸咪夜间接手后**请勿擅推**，待主人明确「上线」指令。
2. ⚠️ **local_sync 落后**：10:33 看门狗 FAIL=`local_sync` 落后 origin 约 60 个 raw_data 待 pull，但 **Pages 部署同步 OK、线上领先已部署 → 生产无碍**。阿狸咪开机后 `git pull` 即可对齐本地（注意先 `git stash` 保全本机 WIP 再 pull，勿覆盖 stash）。
3. ⚠️ **raw_data 文件数 901 > 850 阈值**：触发单 warn，非数据问题，属历史存量；轻量化收尾阶段可后续清理冗余 raw_data。
4. ℹ️ **`zsxq_token.json` 本地缺失**：导致自愈监控闭环 exit=2 连续 38 轮，但 `.gitignore` 永不入库、云端走 Secret 兜底 → **生产零影响**，仅本地调试日志报此 warn。
5. ℹ️ **STOCK_QUOTE 已刷新**：此前（8/20）点名的陈旧盲区，今日 `d9bd1935d`/`1e3bb82ba` 已刷新，前端持仓/行情相关卡恢复正常。
6. ℹ️ **缓存戳防覆盖已常态化**：`7691db22e`/`da7b26ff2` 两次提交将 index.html 内 data/*.js 引用戳实时对齐，根治「CDN 旧缓存覆盖新数据」痛点（主人 08-14 打开转圈根因）。

---

## 五、小九监督职责（交班说明）

- 本档由小九 12:00 自动跑批（automation-1785719698947）生成，无需人工干预；因 12:00 窗口同档并发实例竞争，本档为**最终权威修正版**（纠正了预建版的 main SHA 与自动化计数）。
- **本地 WIP 保全（红线）**：约 100+ 项在飞 data 改动 + index.html/HEALTH_CHECK.js + v8_runner_guard.py 修复已入 stash `handover-20260826-wip-preserve`（stash@{0}），**交班后由小九续作，请勿 pop 请勿删**（避免旧 data 覆盖新鲜 origin/main 数据）。
- **本档仅 add `HANDOVER_小九_2026-08-26.md` 一个文件**，未触碰其他文件，符合「只更新交接文档」铁律。
- 阿狸咪接手后重点：① 维持 STOCK_QUOTE/盘中数据新鲜度（10:08 兜底已证明有效）；② 待主人授权后再处理 v8_runner_guard.py 根治推送；③ 出差期间双机无缝交接保持站点 200；④ `git pull` 前务必先 stash 保全本机 WIP。
- 下个交接档：小九 18:00 档（若当日有新改动则更新；无则出状态快照）。
