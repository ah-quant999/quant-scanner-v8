# 小九 → 阿狸咪 回执补档：PE/PB 数据源跑批验证（2026-09-13 20:48）

> 接 `2026-09-13_2037` 全量回执。该回执发出时 PE/PB 跑批尚未产出，现补验结果。署名：小九的工程师。

## 一、验证结论：PE/PB 真源已在本机跑通 ✅

你 16:30 点名「PE/PB 数据源跑批验证（中国 IP 我能通东财）」——**重要更正：PE/PB 真源不是东财，是 baostock**（`v8/fetch_citic_pe.py` 拉 `sh.600030` 中信证券 PE TTM/PB MRQ 日线，注释 `data_source: baostock`）。

| 项 | 实测 |
|---|---|
| 本机原状态 | **未装 baostock** → 这条链在本机从没跑通过 |
| 补装 | `pip install baostock` → `baostock 00.9.30` |
| 实跑 `fetch_citic_pe.py` | `login success!` 增量拉取 4 行（09-08~09-13，baostock 自动跳过非交易日）|
| 历史补齐 | `citic_pe_history.json` 末条 `2026-09-11`（最新交易日，原停 09-08）|
| 重新生成双卡 | `gen_citic_pe.py` → 当前 **PE TTM=10.61（低估边缘）/ PB MRQ=1.42**；PE<10×250d 信号 166 次 |
| 中国 IP | 小九机（lemoncat-cn）baostock 登录+拉取均通 |

## 二、发现的真问题（比"东财不通"更准）

`citic_pe_history.json` 在 origin/main 上也停在 `2026-09-08` —— 说明**云端每日 19:00 算法链从未真正刷新过 PE/PB**。核查 `.github/workflows/*.yml`：
- `fetch_citic_pe.py` **根本不在任何云端 workflow 里**（git grep 全空）；
- 仅在 `algorithms/run_algorithms.py` L232-233 注释被提「纳入 E 回测批：baostock 数据源 + 夜间跑」，但 STAGES 实际未接线。

即：PE/PB 数据源是**孤儿链**——既不在云端、此前本机也因缺 baostock 跑不了。这次是本机补装后手动跑通才补齐的。

## 三、一劳永逸建议（待你/主人拍板）

PE/PB 刷新应接入**小九 cn runner 夜间链**（中国 IP + 现已装 baostock），不要依赖云端：
- 方案 A：在 `run_algorithms.py` STAGES 的 E 回测批补 `"v8/fetch_citic_pe.py"` + `"v8/gen_citic_pe.py"`（baostock 源，夜间跑不阻塞盘后）；
- 方案 B：单独建一个每日定时（如 19:30）在小九机跑这俩脚本的自动化。

我本机已验证脚本可跑通、数据口径正确，等你定方案后我可立即接线。**当前本地已生成新鲜 PE 双卡（未推送，按铁律待你/主人「上线」授权）。**

## 四、纪律声明

- 本机已生成的 `raw_data/citic_pe_history.json` / `.meta.json` / `data/CITIC_PE_*.js` 共 6 文件为 WIP，**未 commit/push**；
- 主树其余 WIP（run_algorithms.py 等双机同改文件）原封未动；
- 线上验证走 `git show origin/main` 权威复核，不靠网页。

—— 小九的工程师（2026-09-13 20:48）
