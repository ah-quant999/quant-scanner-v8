# HANDOVER 小九 → 阿狸咪 · 2026-08-05 18:00

> 今天最大的事：**中国数据抓取已迁到 GitHub 云端，不再依赖小九**。请务必读完「①架构变化」再动手。

---

## ① 🔴 架构变化：中国数据抓取云端化（今天完成，已实测跑通）

| 项 | 旧 | 新 |
|---|---|---|
| 抓取执行机 | 自托管 `lemoncat-cn`（小九） | **GitHub 云端 `ubuntu-latest`** |
| workflow | `v8_cn_fetch.yml`（14 个 cron） | **`v8_cn_fetch_cloud.yml`（14 个 cron，同 schedule）** |
| 旧文件 | 主力 | **改「应急回退」**（cron 全移除，仅 `workflow_dispatch` 手动触发） |
| 抓取语言 | PowerShell | **bash**（ubuntu 环境） |

- **探针依据**：`probe_v8_cn_cloud.yml` 实测 GHA 可达性 OK 10/11 —— push2delay 东财镜像 5/5 通、akshare 5/6 通（唯一 FAIL 是探针函数名写错，非网络）。
- **实测**：云端 intraday 跑通（run 30993551131），12 模块全绿，api_push_raw 推 57 文件 commit 44bc267f，线上数据刷新 17:34。
- **意义**：小九停电/离线 → **线上中国数据照常更新**。小九现在只负责：本机自动化（heartbeat/守卫/紧急通道）+ 盘后算法链（18:30/20:00 v8_algo_run，仍在 cn runner）。

### ⚠️ 阿狸咪必须遵守的新铁律
1. **改抓取逻辑只动 `v8_cn_fetch_cloud.yml`**（云端版），它是主力。
2. **`v8_cn_fetch.yml` 是应急回退，禁止加回 schedule**（只保留 workflow_dispatch）。
3. 云端抓取环境是 **ubuntu + bash**：新加步骤用 bash，别用 PowerShell。
4. `build_deploy` 的 `workflow_run` 列表已加 `"🇨🇳 v8 中国数据抓取(云端)"`——**以后改 workflow 名字必须同步更新这个列表**，否则抓取完不自动构建。
5. `cloud_fetch_v8.py` 已兼容 GHA：只读 `GITHUB_TOKEN` 环境变量，无本地 token 文件依赖；子进程用 `sys.executable` + 相对路径（ROOT）。

---

## ② 今天其余改动清单（全部已推 origin/main）

| Commit | 改动 | 说明 |
|---|---|---|
| `5fab916` | **板块资金流修复** | `f_sector_fund_flow()` 之前只查 po='1'（流入 TOP200），`sectors_out` 恒空导致「净额(行业)」只加不减（+1871 虚高）。现在升序+降序合并去重。 |
| `ea718a5` | **Runner 健康监控** | 新增 `runner_health_alert.yml`：云端每 2 小时自检 lemoncat-cn，离线开 Issue（标签 `runner-offline`），恢复自动关。工作日 13-21 点 + 周末 0-20 点（22 点后睡眠免打扰）。 |
| `ad585f2` | **GHA 可达性探针** | `probe_v8_cn_cloud.yml`（手动 dispatch 用） |
| `80d1f37` | **云端抓取 workflow** | `v8_cn_fetch_cloud.yml` 新建 |
| `ee7650e` | **自托管改应急** | `v8_cn_fetch.yml` cron 全移除 |
| `e7e819f` | **build_deploy 联动** | workflow_run 加云端名 |
| `2f1e3de` | **v8_health_check 401 修复** | 支持 `GITHUB_TOKEN`/`V8_GITHUB_TOKEN`/`GH_TOKEN` 环境变量（云端 runner 没有本地 token 文件） |
| `20c47da` | **.gitignore** | 忽略 `data/FOUR_VOLUME_BACKTEST.json`（四量终极回测测试产物） |
| 早前 | 个股查询升级 | `STOCK_PROFILE.js`（4801 只，概念/行业/板块 + 策略命中 3 卡） |
| 早前 | 全站时区统一 | `now_cst()` 全站、前端 `_parseAsCST` 等 |
| 早前 | 四量终极 GHA 链路 | `algorithms/data_source_gtimg.py`（腾讯 GTimg 日K，cloud runner 用） |
| 早前 | 自动化模型切换 | 白天/傍晚全站 = `deepseek-v4-flash`；23:30 后本机回 `hy3`（automation-1785920035232 一次性 23:30 执行） |

---

## ③ 遗留事项（阿狸咪可接手，注意标注）

1. **AI市场速览 `gen_time` 时区待修（小 bug，5 分钟）**：
   - `algorithms/gen_market_brief.py` 第 321/355/427 行用 `datetime.now()`（GHA 上是 UTC）→ 写出的 `gen_time` 是 UTC，前端按 CST 解析显示成"今日 09:33"（实际 17:33）。
   - 修法：顶部加 `from datetime import datetime, timezone, timedelta` + `now = datetime.now(timezone(timedelta(hours=8)))`，替换 3 处。改完 commit+push，下次 intraday 自动生效。
2. **大牛股猎手卡片缺「更新于」**：`index.html` `renderHunter()`（约 3373 行已取 `updateTime` 但没渲染）→ 在 Header 区（3381 行后）加 `<span class="fresh">更新于 …</span>`（用 `window._fmtAshareRel(updateTime)`）。两处 renderHunter 都要改。
3. **runner_health_alert schedule 可能被 GitHub 自动暂停**：今天连续 push test run 失败（10 次），疑似触发 GitHub 自动暂停 schedule。若 Actions 里该 workflow 的 schedule 不触发，去 Actions → runner_health_alert → 手动 unpause / re-enable。
4. **v8_cn_fetch_cloud 首个 schedule 待验证**：17:40 盘中档是否自动触发（GitHub 新建 schedule 有激活延迟）。今晚 18:30/20:00 v8_algo_run 是自托管（小九），照旧。
5. **全站精选「昨日 23:36」**：`COCKPIT_TIER_RECOMMEND.js` 是 v6 算法端产物（阿狸咪独立计算），v8 不直接抓。今晚 18:30 `v8_algo_run` 重跑后自动更新；若 18:30 没跑，手动 dispatch `v8_algo_run.yml`。
6. **四量终极回测产物**：`strategy_four_volume.py --backtest` 会生成 `data/FOUR_VOLUME_BACKTEST.json`（已 .gitignore，勿手动入库）。

---

## ④ 交接纪律重申

- **改 `index.html` / `update_v8.py` / workflow 文件必须即时 commit+push**（今天反复验证：不 push 会被云端 build/reset 覆盖）。
- 云端抓取跑的是 main 最新脚本（checkout），**本地没推 = 云端没跑**。
- 推送网络不稳时用 GitHub Contents API 直更（`api_push_raw.py` 同款逻辑），git push 失败不要硬试。
- 主人当前关注：AI市场速览时区、大牛股猎手更新于、runner_health_alert 激活状态。
