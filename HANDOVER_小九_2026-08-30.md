# HANDOVER 小九 → 阿狸咪 · 2026-08-30（12:00 档，合并凌晨档）

> 更新于 2026-08-30 12:00 自动更新（小九 12:00 档）。本档合并凌晨并行实例已提交的 `HANDOVER_小九_2026-08-30.md`（含主人令改动），并补入 12:00 系统实核状态；SHA 全部经 `git log origin/main` / `merge-base --is-ancestor` 核实，未用记忆值。

## 一、背景
- 日期：2026-08-30（周日）。主人 8/21–8/27 出差已收官，双机分时铁律恢复正常（小九白天 7:45–17:45，阿狸咪夜间/周末 18:00–次日 7:30；周日阿狸咪为主，小九跑盘前全量 + 交接）。
- 当日主改动由阿狸咪/并行实例在凌晨按「主人令 2026-08-30」完成（**观澜台→外资研投 全站重命名** + 选股策略 tab 重构 + 一劳永逸 UI/算法修复 + 死代码清理 + 已下架/暂未上架数据脚本 + 防回滚自检）。本 12:00 档为状态快照 + 系统实核，合并凌晨档内容，避免重复劳动。
- 小九本机截至 12:00 无新增手动日志（stock-scanner 与 v8 侧 `2026-08-30.md` 均不存在）→ 按「无新增改动」模式生成，重点转仓库当日实况 + 系统健康。

## 二、今日已完成改动（含真实 commit SHA · 全从 git log origin/main 核实）

### A. 凌晨·主人令（阿狸咪/并行，已入库 origin/main）
- `dfa9fdeaa` feat(v8): 选股策略默认首 tab 改为选股池，删除已下架重复「观测候选股池」，来源行改为点击展开
- `ac5086102` refactor(v8): 全站观澜台更名外资研投（算法/脚本/workflow/自愈监控，22 处）
- `430df68a1` data(v8): 候选池/金股池数据同步观澜台→外资研投命名
- `d8b3babed` fix(v8): 一劳永逸 5 项 UI 修复（banner 移走 / 8 列 / 交易时段 / 强势突破加动量共识 / data stocks）
- 影响：已下架不再重复展示候选池；选股策略默认进入「🧭 选股池」；用户可见「观澜台」字样全站清零（内部文件名/Secret 名保留，见第四节风险⑤）。
- 以上 4 个 SHA 已 `merge-base --is-ancestor origin/main` 验证全部在 main。

### B. 日内·一劳永逸修复 / 清理（小九/并行，已入库）
- `5e3015b38` 一劳永逸式全修（前端展示层 + 算法根因）
- `c7e37b97c` 13 项一劳永逸式修复（mega_patch_20260830_v2）
- `886d97a93` 一劳永逸式修复 #106–#109（时间标签防未来 / 暂未上架重构 3 卡 / 三卡沉已下架顶 / 删驾驶舱灯）
- `95a33aea0` LOW 级打磨 #13-15,#17-23（跨卡时点标注 / 估值代理 relabel / Fib 下一窗口含 active / 资金流 MA5,20 / 涨停其他桶沉底 / H 反推快照日期 / 四量非故障说明 / FOMC 枚举 / 申赎时点 / 板块 Block3 源标注）
- `d67507754` 删调试专区「个股动量状态」卡（17761 字节，主人明令：强势突破 pane 不在删除范围）
- `8114dab04` 清理实时数据死代码 + F1/F3/F8 健壮性（-430 行；health_check 注册新卡 + None 兜底防崩）
- `b40414121` 健康自检防御性兜底（check_* 返回 None 时 `or []` 防 build_report 崩）

### C. 数据 / 自愈 / 运维（已入库）
- `ec02066bf` 补已下架/暂未上架数据构建脚本（生成 DELISTED.js / UNLISTED_PANEL.js，health_check 注册对应卡）
- `4c02159ec` 新增 `v8_rollback_guard.py` 防回滚自检 + 重写小九交接文档（含 DELISTED 红灯修复 + 结构自愈闭环）
- `51d213c02` / `84f1288bf` DELISTED.js / UNLISTED_PANEL.js 数据更新 + HEALTH_CHECK 重算
- `5a5edd7b4` / `967d0bba6` chore: 缓存戳实时对齐 `?v` → 防覆盖（治本覆盖地雷）
- `9e44f7e07` 删除死文件 wf_health.yml（v8_health_patrol 陈旧未注册副本；自愈闭环改由云端 v8_cn_fetch_cloud post_close + ZSXQ_TOKEN 主链路，本地 self_heal_monitor 二次兜底）
- `25a6ecc53` / `dba815a5d` 交接文档（两天改动总览 + STOCK_QUOTE 开盘前刷新提醒）
- 当日另有 14+ 个云端自动化产出：v8 build（`2e4884ee9`/`6b53f3037`/`43808ec3d`/`d098aed0a`/`81d6e7066`/`71f512c0f`/`ea898fd4b`/`0cfa55d6b`/`461159751`/`dc96b276b`/`7533154a0`/`8c6db3891` 等）+ cn fetch（`709993d06`/`a19112e01`/`135e9d189`/`d353a7b88`/`84094a55b`/`ab45cd564`/`4230599bd`/`ed26deafd`/`04189db55` 等）+ stock-quote 刷新（`b523dbaef`/`57cda3f40`）+ risk gauge（`740289e7f`）。

## 三、当前系统状态（12:00 实核）
- **站点**：HTTP **200**（线上 `https://ah-quant999.github.io/quant-scanner-v8/` 实拉确认）
- **健康自检** `data/HEALTH_CHECK.js`：overall=**ok**，summary **106 ok / 1 warn / 0 fail**（107 项，updated 2026-08-30 11:50:34）
  - 唯一 warn：`AVG_PRICE_DATA`（平均股价），last 11:46:58 —— **新鲜、阈值边缘，非失败，观察即可**
- **runner**：run_time **2026-08-30 11:46:03**，category=all（28/28 模块全 ok，empty0/fail0）
- **main HEAD**：**`2e4884ee9`**（v8 build 2026-08-30 11:50），当日累计 **91 commits**
- **剩余自动化**：**17 个**（16 ACTIVE + 1 PAUSED：TDX 跟踪 `automation-1786933089412`）
- **双机**：周日阿狸咪为主，小九 12:00 当班，18:00 后由阿狸咪接管

## 四、需阿狸咪注意 / 待办 / 风险
1. 🔴 **cwd 空壳（环境 P0，待主人决策）**：自动化 cwd `E:\workspace\quant-scanner-v8` 仍非 git 仓库（仅含 `.workbuddy` + 旧 `HANDOVER_小九_2026-08-29.md` + 旧 `_heartbeat.log`），真实仓库在 `C:\Users\Administrator\qs8-tmp`。本档及数月来交接均在 qs8-tmp 实跑推送。请主人改 cwd 或恢复坚果云同步，否则每日交接仍落空（与 08-29 同日问题一致）。
2. 🟡 **缓存戳防覆盖已上线**（`5a5edd7b4`/`967d0bba6`）→ 阿狸咪开机后务必 **Ctrl+F5 硬刷新** 确认：外资研投（原观澜台）全站替换 / 已下架区 / 调试专区瘦身 / 死代码清理 / 选股策略 tab 重构 生效。
3. 🟡 平均股价 `AVG_PRICE_DATA` 仅 warn（新鲜），若盘中持续 warn 再排查，目前不影响主站。
4. 🟡 **STOCK_QUOTE 开盘前刷新**（`25a6ecc53`/`51d213c02` 提醒）：确认 `v8_stock_quote_refresh.yml` 盘中刷新链路正常，避免历史陈旧盲区（盘中查陈旧须解析 `data/*.js` 内层 `update_time`，别信 overall ok 派生项）。
5. 🟡 **文件命名未改**：`guanlan_*.json` / `ZSXQ_TOKEN` 内部文件名与 Secret 名仍保留 guanlan，仅用户可见中文标签改「外资研投」。彻底改名需主人授权。
6. 🟡 历史交接文档（`HANDOVER_*.md` 等）仍含「观澜台」字样，属历史记录，不影响主站。
7. 🟡 **小九本地 WIP 已 stash 未 pop**（111 项，含 index.html / HEALTH_CHECK.js / raw_data 在飞改动，stash 名 `handover-20260830-wip-preserve`）—— **勿直接 pop**，旧 raw_data 覆盖新鲜 origin 数据 = 回滚地雷；取回联系小九。
8. 🟢 防回滚自检 `v8_rollback_guard.py` 已上线（`4c02159ec`），观察其首轮运行是否拦住异常回滚。

## 五、小九监督职责（本档执行摘要）
- **交接**：每日 12:00 / 18:00 自动更新本档并推送（本任务 `automation-1785719698947`）。
- **巡检兜底**：每小时紧急交接通道(`1785835481750`) + runner 本地守护(`1786696074524`) + 盘中漏轮兜底(`1787108824799`) + 全时段数据兜底 freshness_guard(`1785898357370`) + 自愈监控闭环(`1786961773708`)。
- **数据管线**：盘前 08:15 全量(`1785888730897`) / 15:30 收盘(`1785388522458`) / 17:00 选股四模块(`1785404904506`) / 周末 15:30 T+1(`1785396622150`) / 19:00 审计修复+推仓+对齐(`1785978303212`)。
- **专项**：westock 持仓同步(`1787210164001`) / 分红刷新(`1786588096327`+`1786588094908`) / James-Stein 回测(`1787575410637`) / 本地看板同步(`1785802406605`) / TDX 跟踪(`1786933089412` · PAUSED)。
- **双机分时铁律严守**：白天小九独家，夜间/周末阿狸咪；本档为周日 12:00，小九当班，阿狸咪 18:00 后接管。

---
*本档由小九自动化生成，仅更新交接文档，未触碰其他文件；本地 WIP 保留于 stash 未提交。*
