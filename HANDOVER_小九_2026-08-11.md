# HANDOVER 小九 → 阿狸咪（2026-08-11 盘前档）

> 生成于 2026-08-11 08:33（08:15 盘前全量自动化 + 交接核对）
> 对应来件：`docs/ops/handover/HANDOVER_阿狸咪_给小九_2026-08-11_持仓防覆盖+数据审计.md`（16 commit 版）
> main HEAD：`12d4334d`

---

## 一、来件已收到并逐条核对

阿狸咪 06:19~07:16 的 16 个 commit 全部已 ff 同步到本地，第七节「防覆盖保护清单」已通读。

**本次盘前作业未触碰任何受保护项**：
- `index.html` 全程只被 `update_v8.py` 改了 cache-busting `?v=` 参数，逐次 diff 核实无逻辑行变化（`_uniSearch` / `_pfFindStock` / `renderPortfolio` / `showDiagDropdown` / `renderAvgPrice` / `renderEtfMap` 全部原样）。
- `data/CANDIDATE.js` 收工时仍为 **6 源 349 只**（观澜台 61 / maharo 3 / 港股 12 齐全），未被覆盖。

---

## 二、盘前全量结果（08:09-08:15）

| 环节 | 结果 |
|---|---|
| git ff 同步 | `b18787da` 已是最新，无冲突 |
| `cloud_fetch_v8.py --category premarket` | **14/14 全绿**，0 失败（2m34s） |
| `update_v8.py` | 58 个 js 重建 + cache-busting |
| push | `3e2796b1` → origin/main 成功 |
| `deploy_v8.py` | 「无变化，跳过部署」（先 push 后 deploy 的预期结果） |
| 线上验收 | index.html HTTP 200 / 774811B；JUDGMENT_DATA、MACRO_BRIEF、V8_CAL、ANALYST_RATINGS 均 `2026-08-11 08:12` |

心跳：`2026-08-11 08:14:47 | xiaojiu | v8_premarket_full | DONE`

---

## 三、你的待办清单处置情况

### ✅ 待办1 MARGIN_DATA / ETF_SUBSCRIPTION 停在 8/7 —— 已自愈，无需处理

今早 premarket fetch 已补齐，两者序列末尾均为 **2026-08-10**（周一，最近交易日）：
- `MARGIN_DATA.js` update 08:11:55，sh 序列 82 条，末三日 08-06 / 08-07 / 08-10
- `ETF_SUBSCRIPTION.js` update 08:12:51，58 条，末三日同上

（两融为 T+1 发布，8/11 早间最新只能到 8/10，属正常。）

### ✅ 待办2 TOP10_DAILY A 股缺失 —— 已根治 + 数据已恢复

**根因（与你推测的 mootdx 不同，在云端专用分支）**：

```
scanner.py L1419  CLOUD_RUNNER=true
  → data_source_gtimg.fetch_volume_top_stocks_gtimg()
    → _sina_rank()  新浪 hs_a 排行 API
       请求失败时 break 出循环、返回空列表（静默，不抛异常）
    → 外层 try/except 抓不到 → stocks=[] → A股股池 0 只
  → gold_pool 只剩港股 12 只 → TOP10_DAILY 全港股 → 全站精选卡红色告警
```

云端这条分支**完全没有兜底**（港股有 `_HK_FALLBACK_POOL`，A 股没有）。

**根治（commit `14c3f5c6`）**：给 `fetch_volume_top_stocks_gtimg()` 加三层保护
1. rank 全空 → `_fallback_from_candidate_pool()` 从 `raw_data/candidate.json` 回填三板活跃股
2. 部分板块缺失 → 只补缺失板块，已抓到的真实排行数据不动
3. 候选池路径三级回退：`raw_data/candidate.json` → `out/candidate_pool.json` → `algorithms/data/candidate_pool.json`

没有采用硬编码股票池——候选池由 `build_candidate_pool.py` 独立管线每日刷新，不会像硬编码那样老化。
验证：全断=300 只（三板各 100）/ 部分缺失=补齐且无重复 / 正常路径输出不变。

**数据恢复（commit `12d4334d`）**：本地重跑 `scanner.py full`（37s）→ gold_pool 79 只（A 股 61）→ `generate_top10.py` → **TOP10_DAILY 20 条，A 股 14 / 港股 6**，全站精选卡告警应已消失。

### ✅ 待办3 CANDIDATE.js 防覆盖根治 —— 已根治

**根因就是 `stage_to_raw.py`**。映射表里有 `"candidate_pool.json": "candidate.json"`，会用 out/ 覆盖 raw_data/。原有守卫 `_ts_date()` **只比较到「日」**，所以：
- 跨日覆盖能拦住 ✅
- **同一天内** out 旧版（如 08:00 的 4 源）覆盖 raw 新版（06:59 的 6 源）→ 完全拦不住 ❌ ← 你遇到的两次就是这个

**根治（commit `12d4334d`）**，加两层守卫：
- **(2a) 同日倒退守卫**：新增 `_ts_full()` 用完整 `YYYY-MM-DD HH:MM:SS` 比较。实跑时额外拦下 4 个此前一直在静默倒退的文件：`lhb_result` / `inst_trade` / `cockpit_advice` / `mahoro_signals`（这几个以前每轮都被 out 旧版覆盖，属隐性 bug）。
- **(2b) 丢源守卫**：比对 `source_dist`，若搬运会让源变少则保留 raw 版并打印丢失源名。正是你建议的方案。

实跑输出：`[guard] out更旧(2026-08-10) < raw(2026-08-11): candidate_pool.json` —— 候选池已被守住。

### ⏸️ 待办4 AVG_PRICE_DATA 空壳 —— 今日盘前属正常，非故障

当前 198B，内容为 `{"no_data":true,"premarket_cleared":true,...}` —— 是 `cloud_fetch_v8.py` 盘前主动清空盘中模块的结果（日志：🧹 盘前清空盘中模块当日数据），开盘后会由盘中管线回填。你审计时看到的空壳（云端 `f_avg_price()` 取样 <1000 只）需要开盘后再复核，盘前无法判定。

### ⏭️ 待办5/6 港股数据源、TOP3 硬性门槛 —— 未动

港股 akshare 双接口不通已确认；TOP3 `MIN_FINAL_SCORE` 属产品决策，等用户拍板。

---

## 四、今日 commit（均在 origin/main）

| 时间 | Commit | 内容 |
|---|---|---|
| 08:13 | `3e2796b1` | v8 盘前全量：fetch 14/14 + build 58 js |
| 08:30 | `14c3f5c6` | fix(gtimg)：新浪排行不可达时候选池兜底 A 股股池 |
| 08:33 | `12d4334d` | fix(stage_to_raw)：同日倒退守卫 + 丢源守卫；TOP10 恢复 A 股 |

---

## 五、给阿狸咪的提示

1. `stage_to_raw.py` 的两层新守卫会让部分搬运被拒（打印 `[guard]`），这是**预期行为**，不是故障——被拒说明 raw 侧数据更新更全。
2. 若日后确实需要用 out/ 旧版强制覆盖 raw，请显式删除 raw 目标文件再跑，不要摘守卫。
3. `data_source_gtimg.py` 的兜底依赖 `raw_data/candidate.json` 保持新鲜。这条链现在是：候选池 → 兜底 → gold_pool → TOP10，候选池若坏，下游全塌，优先级已升。

_小九 2026-08-11 08:33_
