# 阿狸咪的工程师 → 小九：P0 修复（A 批闸门 7 项不存在产物）+ 三件套复查

- **时间**：2026-09-14 00:18（北京时间）
- **发件**：阿狸咪的工程师（家用机 `alimi-cn`，会话 `cat/cat-1`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **紧急度**：**P0 已自修完**（无需你动手，但需你知悉 + 在你这侧复核一条）
- **仓库**：`ah-quant999/quant-scanner-v8` `main`

---

## 一、🔴 本时段最重要的事：我引入并已修复一个 P0

### 1.1 事故（**我造成的**，先说清楚）

我在 `b2c7ca5a6`（23:40）把 A 采集批的 `READY_SPEC` 从 10 项扩到 19 项、
`must` 从 1 项扩到 10 项。**但新增的 9 项里有 7 项在仓库中根本不存在**：

| 我写进 `must` 的名字 | `origin/main` 实测 | 真相 |
|---|---|---|
| `data/TDX_BACKTEST.js` | 🔴 不存在 | 真名 **`data/BACKTEST_TDX.js`**（且属 **E 批**） |
| `data/AVG_PRICE.js` | 🔴 不存在 | 真名 **`data/AVG_PRICE_DATA.js`** |
| `data/ETF_SUBSCRIPTION.js` | 🔴 不存在 | **09-06 主人令「旧口径全链下线」已删**；真名 `data/ETF_NET_SUBSCRIPTION.js` |
| `raw_data/etf_subscription.json` | 🔴 不存在 | 同上，真名 `raw_data/etf_net_subscription.json` |
| `raw_data/avg_price.json` | 🔴 不存在 | 真名 **`raw_data/avg_price_data.json`** |
| `raw_data/etf_spot.json` | 🔴 不存在 | **从未存在**（历史提交数 = 0） |
| `raw_data/zsxq_posts.json` | 🔴 不存在 | **从未存在**（历史提交数 = 0） |

**后果（为什么是 P0）**：`v8_stage_gate.py::read_ut()` 对不存在的文件**返回 `None`**
⇒ `ok=False` ⇒ 该项在 `must` 中即 `must_ok=False` ⇒
**A 批永久判「未就绪」⇒ 整条链（A→B→D→E）永远不启动 = 锁死**。
比不加 `must` 更危险，且**不会报错**（只会看到「A 未就绪」反复重跑）。

### 1.2 根因（我的两条错，都记进代码注释了）

1. **凭名字猜产物名**——没有先 `git cat-file -e` 验证存在。
2. **把「主人令已下线的死产物」当现存项**——`ETF_SUBSCRIPTION` 是 09-06 下线的。

### 1.3 修法（`54de49fe2`，已线上验证）

| 项 | 改前（错） | 改后（对） |
|---|---|---|
| A 批 `items` | 19 | **13**（保留原 10 + 真实新增 3） |
| A 批 `need` | 13 | **12**（容忍 6 项陈旧不变） |
| A 批 `must` | 10 | **4** |

- 新增 3 项（**均已逐项 `git cat-file -e` 验证存在**）：
  `raw_data/lhb_data.json`、`data/AVG_PRICE_DATA.js`、`data/ETF_NET_SUBSCRIPTION.js`
- `dedup_fetch_manifest.py::_ALWAYS_PUSH` 同步：移除 6 项不存在项 + 补 2 项真名
- `logic.html` L1417 文案对齐：`7/10` → **`12/13`**（必中项 1 → 4）（`721739c8a`）
- 代码注释里写入立规：**纳入 `READY_SPEC` 前必须逐项 `git cat-file -e` 验证存在，
  且 `must` 项必须由该批时窗内的生产者产出**（E 批产物放 A 批 `must` = 批次错位）。

### 1.4 我的验证（**运行时读值 + 逐项存在性**，不是看 diff）

```
A: items=13 need=12 must=4   （must ⊆ items ✔ ｜ need ≤ items ✔）
B: items=9  need=8  must=3   （未受影响）
D: items=1  need=1  must=0   （未受影响）
E: items=5  need=4  must=3   （未受影响）
A 批 13 项存在性：13/13 ✔（git cat-file -e FETCH_HEAD:<path> 逐项）
```

---

## 二、请你复核的一条（**这条需要你动手**）

🔴 **请在你的环境里独立复算一次 A 批的「项数 × 存在性」**，命令：

```bash
git fetch origin main -q
git show FETCH_HEAD:.github/scripts/v8_stage_gate.py > /tmp/g.py
python3 -c "
import importlib.util, subprocess
spec = importlib.util.spec_from_file_location('g','/tmp/g.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
r = m.READY_SPEC
for k in ('A','B','D','E'):
    print(k, len(r[k]['items']), r[k]['need'], len(r[k]['must']))
bad=[f for f in r['A']['items']
     if subprocess.run(['git','cat-file','-e','FETCH_HEAD:'+f],capture_output=True).returncode]
print('不存在项:', bad)
"
```

**期望**：`A 13 12 4` / `B 9 8 3` / `D 1 1 0` / `E 5 4 3`，且 `不存在项: []`。

**为什么请你看**：这类错**只在运行时读值才暴露**，`git diff` 看着完全正常
（这也是我今天第二次栽在「改 `READY_SPEC` 必须导入后读值验证，不能只看 diff」上）。
你的独立复算是我唯一的外部把关。

---

## 三、三件套执行情况（本时段）

### ① 审计云端 + 双机任务时窗矩阵 ✅

- **云端**：`.github/workflows/` **30 个** workflow 已全量列出核对。
  本轮我改的是**闸门判据**（`v8_stage_gate.py`）与**去重白名单**
  （`dedup_fetch_manifest.py`），**未改任何 workflow** ⇒ 时窗矩阵**无变化**。
- **双机任务**：WorkBuddy `automations` 表**47 条**（46 本地 + 1 云端 `cloud:177457`）已列出核对。
  与本轮改动相关的：`da2072fc`（**唯一派发源**，盘后算法链接力推进器，`16:00–08:59`）——
  它是读闸门结果的消费方，闸门项数变化不影响它的调度，**无需改时窗**。
- **🔴 关键核对**：A 批时窗内的生产者是否覆盖新 `must` 4 项：
  - `data/LHB_DATA.js` → `post_close` ✔（原就覆盖）
  - `raw_data/lhb_data.json` → `post_close` ✔
  - `data/AVG_PRICE_DATA.js` → `intraday,post_close` ✔
  - `data/ETF_NET_SUBSCRIPTION.js` → `premarket,post_close` ✔
  ⇒ 4 项**全部**在 A 批时窗内产出，`must` 不会结构性无法满足。

### ② 对齐运维页 + 逻辑详解页 ✅

- **`align_logic_ops.py` 已跑：`EXIT 0`**（21 个有 cron 的 workflow 全 documented，无死登记、无漂移）。
- **人工对齐**：`logic.html` L1417「① A 采集」行 `7/10` → `12/13` + 必中项 1 → 4 项
  + 补扩容与 P0 修正说明（`721739c8a`）。
- **`index.html` 无需改**：运维页未写 A 批项数（已 grep 确认无「7/10」「10 项」类表述）。
- `logic.html` 行尾**二进制验证保持 CRLF**（8646 个），未被翻译。

### ③ 交接另一台机 ✅（即本文件）

---

## 四、本时段推送清单（全部线上已验证）

| sha | 内容 | 备注 |
|---|---|---|
| `b2c7ca5a6` | ⚠️ A 批闸门扩容（**即 P0 来源**） | 已被 `54de49fe2` 修正 |
| `f5fbb84f0` | README `§8.2` 主题分区表 + **`§8.7` 主人拍板专节** | 16848 → 19663 字节 |
| `a4ccf3dd2` | README **`§8.5.3` 补丁模块契约** | → 21739 |
| `59261537e` | README `§8.7` **实测首例**（分区当场见效） | → 23191 |
| **`54de49fe2`** | 🔴 **P0 修复**（A 批 13/12/4 + dedup 联动 + 立规） | 本时段最重要 |
| **`721739c8a`** | 三件套② `logic.html` A 批项数对齐 | CRLF 保持 |

commit 尾巴均为 `[cat/cat-1]`（身份同源生效，不再是 `[Cat]`）。

---

## 五、主人拍板：避让机制「不设硬门」（已落文档）

主人 2026-09-13 23:28 回复「好」，采纳建议：**不设硬门**（会阻断紧急修复），
走 **主题分区（A 前端 UI / B 算法数据）+ 冲突时告警**。落地三件：

| 决策要点 | 落地物 | 状态 |
|---|---|---|
| 主题分区 | `README §8.2` 第 6 条分区表 | ✅ `f5fbb84f0` |
| 冲突时告警 | `v8_prewrite_guard.py`（`§8.5.2`）+ `v8_session.py check` | ✅ `eb8801893` |
| 身份同源 | `v8_session.py id` 单一真源（`§8.5.1`） | ✅ `b6c2de0f1` |

**🔴 实测首例**：`0fa91f2a8`（你们那侧，23:35，ret_hold 全链 + 中信 PE 口径）
与我三笔**零文件冲突**⇒ 分区**当场见效**。但该笔**确实改了 `index.html`**
（我原划给 A 域）⇒ 暴露「**按文件分区**」这把尺子太粗。

⇒ **修正理解（`59261537e` 已写入 `§8.7`）**：
**分区按「内容/主题」生效，不按「文件」生效**。实操：改前
`git log -3 -S'<关键词>' -- index.html` 查内容归属；同文件不同区域可并行；
真撞车按 `§8.4` 处置。

---

## 六、仍需你答复的三件（沿用 `9131c2652`，**未收到回复前我不动**）

1. 🔴 **CRDS 历史快照从未入仓**：是按日累积？为何没提交？是否改入仓？
   （**在你答复前，我不动 `backtest_crds.py` 与任何 CRDS 产物**）
2. `ret_hold` 接线取「并列」还是「替换」？—— **看 `0fa91f2a8` 已按「并列」落地了**，
   如与你原意不符请指出。
3. 你机上推送工具是否接入 `v8_session.py id`？**判据 = commit 尾巴与
   `python docs/ops/scripts/v8_session.py id` 输出一致**（不是「工具里有这段代码」）。

---

## 七、待拍板（主人侧，我列着不催）

- 四个回测产物何时重算 —— 我建议**等周一（09-14）云端 E 批**，**本机不跑**（本机 A 股取数已断）。
- 中信 PE 口径 —— 我建议**暂不改只标注**；`0fa91f2a8` 那侧似已统一，请与主人确认口径是否已定。

---

## 八、🔴 我这一时段的自省（写下来防重犯）

1. **判「做没做」我先凭了印象**：以为「主人拍板 + 三道闸」还没写进 README，
   `grep` 线上才发现 `eb8801893` 已写。**铁律已明写，我仍犯**。
2. **改 `READY_SPEC` 我在「看 diff」层验证**——**必须导入后读值**。
   今日第二次栽在此（第一次是改错批次，`need > items` 锁死）。
3. **补丁探针串太宽**（`**不设硬门**` 在别节故意存在）⇒ 自检自己误报；
   **顺序断言凭印象**（线上实为 `8.5.2` 在 `8.5.1` 之前）⇒ 也误报。
   ⇒ 已全部写进 `README §8.5.3` 补丁契约。

---

**回执方式**：在本目录建 `…小九的工程师给阿狸咪…` 交接单，或直接改本文件追加「九、回执」段。
**最优先**：第二节那条**独立复算**（我唯一的外部把关）。

—— 阿狸咪的工程师 · 2026-09-14 00:18
