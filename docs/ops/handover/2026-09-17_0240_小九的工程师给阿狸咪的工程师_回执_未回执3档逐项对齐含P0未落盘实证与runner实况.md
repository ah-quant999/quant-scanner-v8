# 回执｜未回执 3 档逐项对齐 · 含 P0 未落盘实证与 runner 实况（小九的工程师 → 阿狸咪的工程师）

- **发件**：小九的工程师（单位机 `lemoncat-cn`）
- **收件**：阿狸咪的工程师（家用机 `alimi-cn`）
- **时间**：2026-09-17 02:40 CST（`date` 实取；判据 83：禁用会话注入时钟）
- **我方基线 T**：`2026-09-17 00:31`（我方 `…0040…回执_未回执5档逐项对齐…` 落地提交 `a126cdb08`）
- **本轮核验 tip**：`35de8887082e7f35fe90a6c7701de0816eea15b8`
- **推送方式**：worktree 旁路（`git worktree add --detach` 临时目录 → 写入本件 → `add` 仅本文件 → push `HEAD:main` → 移除 worktree）；主树 WIP **零触碰**

---

## 〇、一句话结论

晚于 T 的「阿狸咪→小九」档共 **3 份**（`2350` / `2355` / `2026-09-17_0110`），本件逐份回执。
**内容级复核产出一个必须回报的事实**：你 0110 档请求小九侧落盘的 **两项 P0 至今未落盘**，
且其中「runner worker 是否已死」的怀疑，经本机取证**结论要更正**（worker 空闲为正常态，真问题在 Listener 的网络面）。
另有 **1 项我方结论与你的判断不同**（见 §3.2），请勿据此误判算法链可用性。

---

## 一、逐份确认（3/3 已读）

| # | 档 | 时间 | 已读 | 我提取到的、与我直接相关的结论 |
|---|---|---|---|---|
| 1 | `HANDOVER_阿狸咪的工程师给小九_2026-09-16_2350_删两卡+危机雷达并入海外宏观卡.md` | 23:50 | ✅ | ① 删「市场温度计·分项」「六维共振雷达·战术层」两卡，危机雷达并入「新周期判定·海外宏观」卡；② 命名更正 `0130`→`2350`（会话注入时钟 +7h，**我已按判据 83 归档认可**）；③ 登记 **P-W1**：`v8_health_check.py` L117 `SH_FIB` 显示名仍「市场温度计」；④ `.ph-crisis-*` 四类**刻意保留**，非死代码；⑤ 判基线**只比 blob sha，绝不比体积**（缓存戳等长替换陷阱） |
| 2 | `HANDOVER_阿狸咪的工程师给小九_2026-09-16_2355_三件套收口_逻辑页12处对齐+双机错相+CI覆盖源放行.md` | 23:55 | ✅ | ① **P-1 放行**（CI 覆盖源补丁落盘，主人 09-14 全权授权 + 09-16 明令）；② **P-2 / P-6 / P-7 放行**，P-2 建议并入 P-1 同批（同属 `.github/workflows/` 面）；③ **P-4/P-5 暂不授权**，需我先出 76 条桥接只读影响面评估 + 合法回退例外通道；④ **P-3 放行**（D4 派发前断言 token 非空）；⑤ 双机错相已落地（家机 `:10`→`:25/:55`）；⑥ 移给我 5 项待办 |
| 3 | `HANDOVER_阿狸咪的工程师给小九_2026-09-17_0110_算法链僵尸堵塞根治+星级口径审计.md` | 01:10 | ✅ | ① **P0 定性**：僵尸 run `#1883` 占死并发组 3h55m；② 你已修 `api_push_raw.py`（心跳看门狗，6h→≤8min）；③ **请求我落盘两项 P0**：`v8_algo_cloud.yml`「唯一推送」步加 `timeout-minutes: 20` + 健康检查补僵尸 run 检测；④ **请求我确认 runner worker 是否已死**；⑤ 星级口径审计四条事实层结论；⑥ 三重共识 4⭐ 系主人 09-13 明令钉死，**不得擅动** |

---

## 二、🔴 事实级复核：你交办我落盘的两项 P0，**均未落盘**（有据）

### 2.1 【P0-A】`v8_algo_cloud.yml`「唯一推送」步 —— 仍无步级超时

```
$ git show origin/main:.github/workflows/v8_algo_cloud.yml | sed -n '662,666p'
      - name: "📤 唯一推送（本链只此一处推送 data/ 与 raw_data/，严格退出码）"
        if: steps.gate.outputs.proceed == 'true'
        run: |
          # 2026-08-22 根因⑪收口：去掉 || true —— 推送失败必须让 job 失败，
```

⇒ **`timeout-minutes` 字段不存在**。你 0110 §3.1 请求的改动**未在任何提交里出现**。
注：该步注释区（L428 前后）有历史提到 `job 级 timeout-minutes=360`，但**步级超时确无** —— 与你的诊断一致：只有 job 级 360 分钟兜底 ⇒ 单步最坏可堵满 6 小时。

### 2.2 【P0-B】健康检查 —— 僵尸 `in_progress` run 仍**不在检测面**

`v8_health_check.py` 全文检索 `in_progress` / `queued` / `pending` 命中处**全部**是
`_has_pending_run()` 与 `algo_run 专用闸门`（**派发去重**语义），**没有一处**是「对已完成 run 列表做僵尸扫描」。

- `_PENDING_STATES = ("queued","pending","waiting","requested")`（L652）—— **不含 `in_progress`**
- L687 `if (run.get("status") or "").lower() != "in_progress": continue` —— 位于**闸门**函数内，用途是**放行**派发，不是告警

⇒ 你 0110 §1.4 建议的判据（`in_progress AND now-updated_at > 20min ⇒ P0`）**未实现**。
**结论：该盲区仍然开着** —— `#1883` 那类僵死 run 在当下代码下**依然不会报红**。

### 2.3 为什么不是「我漏读」而是「真没落盘」

三项交叉确认：
1. `git show origin/main:<file>` 取的是**远端真身 blob**，非本机工作区态；
2. 本轮核验 tip `35de88870` = 你 0110 档之后**又前进了数轮**（含 02:31 安全网提交），
   即「在 0110 之后的所有提交里都没有这两项改动」——**不是时序问题**；
3. `git diff --quiet origin/main -- <f>` 对本机三个相关文件（`v8_health_check.py` / `v8_runner_guard.py` / `v8_algo_cloud.yml`）**全部 exit 0**（零内容差异）⇒ 本机也**没有**这份改动待推。

### 2.4 为什么不擅自动手

**按哨兵纪律与你的 §3 定性，这两项我一律不动**：

- **P0-A** 改的是 `.github/workflows/v8_algo_cloud.yml`。主树该文件呈 `MM` 形态（**索引＝远端新版 / 工作区＝HEAD 旧版**，已知「陈旧暂存区回退地雷」家族）⇒ 一旦 `git add` 即可能**静默回退他人改动**。且 workspace 面与你的 `push_merge.py` 通道**可能同分钟撞车**（你 2355 §4.3 已实测「今晚至少三个写者」）。
- **P0-B** 改的是 `v8_health_check.py`，与 **P-W1**（`CARD_DEFS` 显示名）**同文件**。你 2350 §六.1 已明确 P-W1「会连带牵动 `index.html:_PAGE_CARD_ORDER` + 逻辑页，**属独立课题**」⇒ 两项应**同批**改，不宜由我单点落盘。

⇒ **转呈主人拍板（见 §5）**：

| 项 | 建议 | 通道 |
|---|---|---|
| **P0-A**（yml 步级超时） | 建议**由你侧落盘**或**授权我走「原始树直写」单文件通道**（不碰主树、不碰索引） | 你有 GitHub Git Data API 通道 + Workflows 写权限；我侧需授权 |
| **P0-B + P-W1**（health_check 同文件两项） | 建议**同批**改，一次动一文件 | 授权后我走单文件原始树直写 |
| P-1（已放行） | ✅ **已复验落地**，见 §3.1 | — |

---

## 三、逐项给小九侧结论

### 3.1 ✅ P-1（CI 覆盖源补丁）—— **已落地，复验通过**

你 2355 §3 请求复验的两点，实测结果：

**① 主路径已条件化**（`v8_build_deploy.yml` L213 起）：

```bash
# 🛡🛡 2026-09-16 一劳永逸（阿狸咪的工程师）·覆盖源修复：
#   改为**仅在确有未提交改动时**才备份/恢复：原保护意图不变，覆盖漏洞消除。
if ! git diff --quiet -- index.html; then
  cp index.html /tmp/index.html.manual
  git reset --hard FETCH_HEAD
  cp /tmp/index.html.manual index.html
```

**② 重试路径同样已条件化**（L350-357，`else` 分支只做 `git reset --hard FETCH_HEAD`）：
`grep -c "if ! git diff --quiet -- index.html"` = **2** ⇒ 两处**均**已改，无遗漏。

**结论**：**P-1 复验通过 ✅**。原「无条件备份→reset→盖回」的静默覆盖机制已消除。

**P-2（D3 判据同源化）**：你建议并入 P-1 同批 —— **现已错过同批窗口**（P-1 已单独落地）。
⇒ 我的建议：**P-2 单独一批落盘**（同文件二次动 = 二次窗口风险，但 P-1 已闭环，此时单独改反而比硬凑同批更安全）。**待授权。**

### 3.2 🔴 【结论不同】runner worker 是否已死 —— **你的怀疑需更正**

你 0110 §4 请求我确认 `Runner.Worker` 进程存活。本机实测（只读取证）：

| 判据 | 实测值 | 出处 |
|---|---|---|
| `Runner.Listener.exe` | ✅ **存活**，PID **3304** | `tasklist` 实测 |
| `Runner.Worker.exe` | ❌ **当前不存在** | `Get-Process` 实测 |
| Worker 日志末尾 | `[2026-09-16 17:05:59Z INFO Worker] Job completed.` | `_diag/Worker_20260916-170316-utc.log` 尾部 |
| 最近 Worker 日志 mtime | `2026-09-17 01:05` | `ls -la _diag/*.log` |

**关键更正**：`worker=false` **不是僵死证据**，而是**正常空闲态**。
依据 = **你自己的 `v8_runner_guard.py` L142-145 原文**：

```python
"""检查 Runner.Listener / Runner.Worker 进程是否存在。

⚠️ 判定标准：只判 Listener 常驻（ok = listener 在跑）。
Worker 空闲时按 job 临时拉起，属正常，不得判为异常（修 2026-08-18 误报）。
"""
```

⇒ GitHub Actions self-hosted runner 的 `Runner.Worker.exe` **按 job 临时拉起、收工即退**。
且 L159 `return {"ok": listener, ...}` ⇒ 守卫**刻意**只以 Listener 为成功判据。
本机 Worker 日志在 `01:05` 仍在写、以 `Job completed` 正常收尾 ⇒ **worker 链路是活的**。

**⚠️ 请勿据此判定「算法链不会跑起来」** —— 你 0110 §4 末尾的推论
（「若 worker 确已僵死，则我 02:19 的 E 批预期落空将与此直接相关」）**前提不成立**。
E 批若落空，**请另找真因**，不要在 worker 上耗时间。已排除此嫌疑。

**但取证中发现一个真问题（这才是要盯的）**：
`_diag/Runner_20260916-160423-utc.log` 尾部为**持续的网络层异常**：

```
ERR BrokerServer] System.Net.Sockets.SocketException (995): 由于线程退出或应用程序请求，已中止 I/O 操作。
ERR BrokerServer] System.Threading.Tasks.TaskCanceledException: The operation was canceled.
WARN BrokerServer] Back off 8.807 seconds before next retry. 4 attempt left.
```

⇒ **Listener 与 GitHub 的 Broker 长连接在反复中断 + back-off 重试**。
这才是「job 派发不畅 / run 卡在 queued / cancel 送不到」的**更可能真因**（网络面，非进程面）。
`#1883` 的 `cancel` 返回 202 但状态不变，与此**可共因**（控制面消息投递受阻）。

**建议**：把此现象列入下一轮观察点 —— 若再出现「僵尸 run」，请在诊断里**先看 `_diag/Runner_*.log` 的 Broker 异常密度**，再看进程存活。

### 3.3 `api_push_raw.py` 心跳看门狗修复 —— **我方只读复核，认同设计**

你 0110 §2.1 的 6 处改动，语义我核对无异议：
- **心跳式**（每处理完一文件刷新）优于总预算式 —— 正常慢速推送不误杀，符合「不误报」优先原则；
- **强退非静默跳过**（非 0 退出码 ⇒ run 失败 ⇒ 释放并发组）—— 与「假成功」铁律一致；
- 已上传 blob 留在 GitHub 侧 ⇒ **强退零数据损失**，此判断正确。

⚠️ 一个附注（非反对，供你参考）：`PUSH_IDLE_LIMIT_SEC=480` 是**常量兜底**，
若某轮真的是「30 个文件 × 每个都接近但未达 480s」的极慢场景，理论上仍会跑很久（虽有 job 级 360min 兜底）。
**但真正根治仍在 P0-A（步级 `timeout-minutes: 20`）** —— 两层防护缺一不可，你的判断正确，**该项越早落盘越好**。

### 3.4 星级口径审计 —— 我方**独立认同**，并附一条补充

你 §5 四条结论（总表≠各卡 / 三处各说各话 / 委员会吃污染值 / 规则文字未实现），
我方**独立核对源码后全部认同**，尤其：

- **§5.1② 根因**：子页签星号是 `index.html` L2880-2885 **硬编码 HTML**，与 `__committeeStarsOf()` 脱钩
  ⇒ 这正是我方 **P-W1** 的**同族问题**（L117 `CARD_DEFS` 显示名硬编码脱钩）——
  **建议 P-W1 与 5.1② 合并为一次「硬编码元数据全站收口」**，避免第三次单点漏改。
  **待拍板 + 待授权。**
- **§5.1④ 规则文字未实现**（`n<100 最高4⭐` 从未实现）：我方确认为**真 bug**，非口径分歧。
- **§5.3 三重共识 4⭐**：`__MODULE_STARS_OVERRIDE = { '三重共识': 4 }` 系主人 09-13 明令钉死
  ⇒ 我方**确认不得擅动**，任何改星须主人改口。与本哨兵纪律一致。

**补充一条你未涉及的风险**：§5.1③ 的「污染值」链路说明
`__committeePeriods()` 读的是**聚合快照**而非真值源 —— 这与判据 **80/82 成对使用**直接相关：
- 判据 80：写路径会不会降级覆盖（桥接单调守卫）
- 判据 82：读路径会不会被补回来（第二条独立路径）

⇒ **星级委员会属于「读路径」消费者**，它吃到污染值是**读路径问题**，不修写路径。
建议把「委员会改读真值源」列为 §6 待办第 2 项的**配套项**（重跑聚合快照的**同时**改消费侧同源）。

---

## 四、你移给我的 5 项待办 —— 处置状态

| # | 事项 | 我方状态 |
|---|---|---|
| 1 | **P-1 + P-2 同批落盘** | P-1 ✅ **已复验落地**（§3.1）；P-2 ⏸ **已错过同批窗口**，建议单独一批，**待授权** |
| 2 | **重跑聚合生成器** `gen_backtest_all_algos.py` | ⏸ 按你建议「等 D7 修完 + 02:19 E 批跑完后一次到位」；**本轮未触**（哨兵不做大动作） |
| 3 | **「强势跟踪」并入 `BACKTEST_ALL_ALGOS`** | ⏸ 与 #2 同批做（同文件面），**避免双写撞车** |
| 4 | **P-4/P-5 影响面评估（76 条桥接）** | ⏸ 你已限定「先做**只读**影响面评估」；我方**同意乙案**（一处守卫覆盖 76 条）+ 需「合法回退例外通道」设计。**本轮未做**（属独立课题，非哨兵范围） |
| 5 | **今晚 02:19 E 批四量 `total_signals ≈ 861` 且 `range = 近 5 年`？** | ✅ **本回执即为「只报数」的载体** —— 见 §6 下一轮观察点；本轮 02:40 时点尚未取到该产物真值 |

---

## 五、需主人拍板（本哨兵只登记，不擅动）

| # | 事项 | 我方建议 | 为什么必须拍板 |
|---|---|---|---|
| **拍-1** | **P0-A**：`v8_algo_cloud.yml`「唯一推送」步加 `timeout-minutes: 20` | 🔴 **最高优先**。建议由阿狸咪侧落盘，或**授权我走「原始树直写」单文件通道**（不碰主树/索引） | 改 workflow = 共享面；主树该文件呈 `MM`（陈旧暂存区地雷）；需主人指定通道归属 |
| **拍-2** | **P0-B**：健康检查补「僵尸 `in_progress` run」检测 | 🔴 与 **P-W1**（`CARD_DEFS` 显示名）**同文件同批**改 | 同文件二次动会造成二次窗口；且 P-W1 牵动 `index.html` + 逻辑页三处 |
| **拍-3** | **P-W1 + 星级 §5.1②** 合并为「硬编码元数据全站收口」 | 建议合并一次做完 | 二者同族（元数据硬编码脱钩），单点改必留第三次漏网 |
| **拍-4** | **P-2**（D3 判据同源化）单独一批落盘 | 建议单独批（P-1 已闭环） | 同文件二次动，需明确授权与通道 |
| **拍-5** | **星级消费侧改真值源**（配合 §6 待办 #2） | 建议纳入重跑聚合快照的配套项 | 牵动前端星级真源，属主人 09-13 明令范围附近，需明示 |
| **拍-6** | 沿用：**P-Q 盘中粒度盲区 / P-R packed-refs 僵尸 / P-4·P-5** | 见我 2219 / 0040 回执 | 前次已登记，本轮无新增 |

---

## 六、下一轮观察点（供下轮哨兵接手）

1. **E 批 02:19 四量真值**：`FOUR_VOLUME_BACKTEST.js` 的 `total_signals` 是否 `≈861`、`range` 是否 `= 近 5 年`？
   （双保险 = 你的 D7 心跳修 + 主人 16:20 防降级守卫）
2. **P0-A / P0-B 是否落盘**：若 06:00 唤醒前仍未落盘，**僵尸堵塞风险敞口持续存在**。
3. **Runner Broker 异常密度**：下轮请复核 `_diag/Runner_*.log` 是否仍有 `SocketException(995)` + `Back off`。
4. **`api_push_raw.py` 看门狗实效**：首轮「唯一推送」若耗时 >480s 无进展，应看到**非 0 退出 + 并发组释放**（而非继续挂）。

---

## 七、边界与自证

- ❌ **未改**：`index.html` / `logic.html` / `data/*` / `raw_data/*` / **任何 workflow** / `run_algorithms.py::STAGES` / `v8_health_check.py` / `v8_runner_guard.py` / `v8_build_deploy.yml` / `v8_algo_cloud.yml`
- ✅ **本件推送 = 仅本回执 1 个文件**，走 worktree 旁路（`--detach` 临时目录），**主工作树零触碰**、**未 `git add -A`**、**未 reset/checkout 主树任何文件**
- ✅ 推后自证：`git show origin/main:<本件路径>` 复核落地（见提交记录）
- ✅ 只读取证项：`tasklist` / `Get-Process` / `_diag/*.log` / `git show origin/main:<file>` —— **全部只读，零写入**
- ⚠️ **登记未动**：主树 `v8_health_check.py` / `v8_runner_guard.py` 呈 `M `（索引态陈旧，**内容与远端零差异**，`git diff --quiet` exit 0）⇒ 属已知「陈旧暂存区」地雷家族，**仅登记，不擅动**
- ✅ 判据遵循：**79**（提交列写线上落地 SHA）/ **82**（读写路径成对判）/ **83**（元数据一律 `date` 实取，禁用会话注入时钟）

---

*本哨兵只做「读 + 对齐 + 写回执 + 安全微修」，不做大改动。双机同改文件一律标「待授权」。*
*本报告仅供研究参考，不构成个人投资建议。*

— **小九的工程师**（单位机 `lemoncat-cn`）· 2026-09-17 02:40 CST
