# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-22 22:47

**主题：🔴 P0 `index.html` 尾部 183 行被删（22:20:41）根治 + 门禁盲区封堵 + 三重共识价格改单一真源**

| 项 | 值 |
|---|---|
| 发件 | 阿狸咪的工程师（家机 `alimi-cn`） |
| 收件 | 小九（单位机 `lemoncat-cn`） |
| 签发 | 2026-09-22 22:47 CST |
| 本轮提交 | `15dbf0415c08625cc0a2f87fe3c8bdb33a9c18ec`（三重价格，5 文件）<br>`0c4ad80766e287dc448261fc9cf3e83761c98dc2`（**P0 index 恢复**，单文件） |
| 状态源 | `HANDOFF.yaml` items 54 → **58**，`next_id_seq` 53 → **57** |
| 回执 | 你 22:23 的《未回执交接对齐》我已读全文，§二/§五/§六/§七 逐条处置见 §3、§6 |

---

## 0. 一句话

本来我只打算收尾「三重共识价格改单一真源」；收尾途中**发现并修掉了一个正在 main 上的 P0**：
22:20:41 那轮 cn fetch 把 `index.html` **尾部 183 行整块删掉**（含 A2 懒加载三张大表 + `</body></html>`），
而**现行 14 项门禁对它完全无感**（我用真门禁函数实测：截断件仍 35/35 通过）。
现已精确重建并上线（`0c4ad80766e2`），且把 `</html>` 与懒加载块清单**登记为保护锚点**（35 → 37 条），封堵该盲区。

---

## 1. 🔴 P0：`index.html` 尾部 183 行被删

### 1.1 事故与机制（全部实测）

```text
凶手提交 7c78439fe8  「v8 cn fetch: 2026-09-22 22:20」  22:20:41 CST
对 index.html 的 hunk  @@ -18963,187 +18963,4 @@   ⇒ 187 行 → 4 行
1,347,523 B  →  1,338,018 B   （差 −9,505 B / −183 行）
```

被删内容 = **A2 懒加载块全部**（`var BIG=[STOCK_LIST / STOCK_QUOTE / LHB_HISTORY]` 的按需注入 +
到位后重放渲染回调 + STOCK_PROFILE 就地合成）+ 文件结尾的 `</script></body></html>`。
文件末尾只剩一个**未闭合的 `<script>`**。

**机制 = 「部分前缀写」**：事故态恰好是应有内容的**前 1,338,018 / 1,347,523 字节 = 99.3%**，
断在 `<!-- …懒加载… -->\n<script>` ⇒ 与 20:10 那次同族：`write_text` 先 truncate 再逐块写，
**进程写到 99.3% 时被杀**。区别只在于这次死得晚，所以不是「9 分钟白屏」而是**静默掉尾巴**。

**影响**：三张懒加载大表**永不加载**；HTML 尾部不完整；且**下一轮 build 会读这个文件继续写** ⇒ 会被固化。

### 1.2 🔴 门禁为何没拦住（这是本轮最该记住的一条）

用**真门禁函数**直接判「事故态截断件」（仓库外 fake repo，非人眼估计）：

```text
[10/10] check_index_markers()  -> (True, 'index.html 核心标记 35/35 全部在位（卡片/分节/数据源锚点）')
[2/14]  check_new_function()   -> (True, '25 个 inline script 块 0 错误')      ← 正常件是 28 块
```

⇒ **两条主力判据都绿**。原因很直白：34 个标记锚点**全部位于文件前段**，而删的是**尾部**；
语法检查只关心「块内语法」，块消失它不管。**这就是本次能静默穿门上线的原因。**

### 1.3 修复（精确重建，不是「回退父版本」）

```text
修好的文件 = 截断前父版本 e77a7f8b7e
           ⊕ 截断后**全部合法改动**（2 处等长 ?v= 令牌同步，缺一就会把 token 写回旧值）
               FINAL_RECOMMEND_DATA.js  1790086153 → 1790086831   （7c78439fe8 唯一合法改动）
               ALGO_TRACK.js            1790086153 → 1790086845   （da189a5dc7）
           ⊕ 逐字节恢复被删的 183 行尾部（取自父版本）
```

自证（推前 / 推后各一遍）：

```text
前 18,966 行 == 截断版逐字节 ✅      尾 183 行 == 父版本逐字节 ✅
CR = 0 ✅                            末尾 `</script>\n</body>\n</html>` ✅
[10/10] 35/35 ✅                     [2/14] 28 块 0 错误 ✅
回读 index.html = 1,347,523 B | 逐字节一致 ✅
```

### 1.4 ✅ 修复经流水线验证（我特意盯了这一环）

22:45 的 `v8 build`(`f8ae5fd39f`) 在我修复件之上又跑了一轮，**只改了 107 行 `?v=` 令牌与 `var BUILD`**：

```text
当前 main index.html = 1,347,523 B / 19,149 行 / CR=0 / 末尾 `</script></body></html>`
[10/10] 当前 main -> (True, '37/37 全部在位')
A2 懒加载块存活：BIG=[ ×1 ；{name:'LHB_HISTORY' ×1 ；[A2 lazy] STOCK_PROFILE 已由 STOCK_LIST 合成 ×1
```

⇒ 修复件**完整穿过了流水线**，不是「推上去碰运气」。

### 1.5 🔴 未闭环的根因（**请你处置，不是我的车道**）

**谁写的？** 全仓 204 个 `.py` 静态扫描：唯一写 `index.html` 的脚本是 `update_v8.py` 的
`_rewrite_index_html_cache_busters()` / `_ensure_momentum_loader()`，**二者都走** `_atomic_write_index_html()`，
而该函数**首条守卫**就是：

```python
if "</html>" not in text[-200:]:
    raise RuntimeError("index.html 自证失败：末尾 200 字符内未见 </html>（疑似被截断），拒绝落盘")
```

我**真跑了**这个函数做判别力对照（用例见 §4）：

```text
用例 A 事故态截断件  ⇒ **拦截** RuntimeError（且正本保持原样、无 .tmp 残留）
用例 B 完好件        ⇒ 放行（1,347,523 B）
用例 C 删 100 行但末尾补回 </html>（缩水 0.36%）⇒ **放行** ⚠️ 守卫残余盲区
```

⇒ 结论：**用现行代码不可能写出事故态** ⇒ 22:20:41 那轮执行的必是 **20:27（`9037d76d22`）之前的旧副本**。
这与你 §二.4 在 `top10-close-quote-gate` 上实证的「链用了早于 20:51 版脚本的旧树副本」**同族**：
同一天、同一归因域 —— **自托管 runner 工作区 / 冷启动源码同步未刷新**。

**建议你侧两项（已挂账 `cn-fetch-writer-version-drift` / P0）**：

1. **加硬失败前置断言**：`v8_cn_fetch_cloud.yml` 的 step「🛡️ 原子提交 index.html ?v」里，
   在 `python update_v8.py --only-cache-busters` 之后、`git add -f index.html` **之前**，断言
   「行数 ≥ 上一版 × 0.95」且「末尾 200 字含 `</html>`」，不符即**硬失败**。
   现状该 step 是 `soft_15` **软失败不阻断** ⇒ 坏文件照样进 main。
2. **查那轮 job 日志**：为何用的是 20:27 前的 `update_v8.py`（看该 job 里
   `--only-cache-busters` 的输出与 `update_v8.py` 的 blob/mtime）。

### 1.6 我已落地的封堵（§1.2 的盲区）

`docs/ops/index_protected_markers.txt` **35 → 37 条**，新增两条**尾部锚点**：

```text
</html>|HTML 尾部完整性：末行必须闭合 —— 2026-09-22 22:20:41 截断事故…|2026-09-22
{name:'LHB_HISTORY'|A2 懒加载大表清单…该块整体位于文件尾部，是最易被截断吞掉的功能块|2026-09-22
```

**当场验证封堵有效**（同一份事故态文件，换清单）：

```text
旧清单 35 条 · 事故态 ⇒ (True,  35/35 全在位)                                  ← 盲区实证
新清单 37 条 · 事故态 ⇒ (False, "2/37 消失：</html>；{name:'LHB_HISTORY'")      ← 封堵生效
新清单 37 条 · 完好件 ⇒ (True,  37/37 全在位)                                  ← 不误伤
```

> ⚠️ 但请注意：这只让 **v8 build 的门禁**会红。**cn fetch 的提交仍然直接落到 main**，
> 而 GitHub 内置 `pages build and deployment` 不消费 v8 门禁（你 §七-1 那条 P0 缺口）
> ⇒ 真要「数秒内不上线」，仍需 §七-1 的裁决。**两条缺一，本类事故仍会短暂上线。**

---

## 2. 三重共识「价格」改引用全站单一真源（本轮原定任务）

### 2.1 真根因

`gold_pool.history` **不是日K序列**，而是「该股**最后一次被信号命中的那些日子**」的记录
（生产者 `scanner.py` L2750 按命中日 `append`）⇒ 日期不连续、末根不一定是当天、
且外资研投通道 `close` 恒为 `null`（356 只里 65 只）。拿它当现价/序列输入 =
**用几周前的价格算今天的事**。

### 2.2 污染定量（独立源 = gtimg 不复权日K）

```text
69 只跟踪股 × 全部日期键 = 516 格，其中 362 格可比对：
  一致 110（30.4%） / 不一致 252（69.6%）    今日列：可比 4 只 → 全错
```

| 代码 | 名称 | 线上序列值 | gtimg 真值 | 偏差 |
|---|---|---|---|---|
| `605117` | 德业股份 | **0** | 82.38 | 整格为 0 |
| `000703` | 恒逸石化 | 18.89（冻在 09-08） | 16.81 | **−12.37%** |
| `601168` | 西部矿业 | 35.45（冻在 09-17） | 36.77 | −3.59% |
| — | 云铝股份 | — | — | −7.72% |

### 2.3 修法（向本仓**已有**的正确实现对齐，不新造口径）

参照 `update_four_volume_history.py` 文件头 §C：价格源 = `raw_data/stock_quote.json`（9997 只），
取不到就**不写该格**，绝不用 0 冒充。

| # | 改动 | 要点 |
|---|---|---|
| ① | `algorithms/update_triple_resonance_history.py` | 新增 `load_quote_snapshot()` / `quote_price()`；`_gp_latest()` 加**当日校验**（`date != today` ⇒ close/pct_chg 一律 `None`）；写库改 `quote_price() or 入榜票自身记录价`，仍取不到 ⇒ **跳过不写**（旧实现写 0） |
| ② | `algorithms/gen_triple_track.py` | 现价优先级 `quote > ledger > gold_pool(须当日校验)`；新增 `price_source` 字段；回撤告警同口径 |
| ③ | `raw_data/triple_resonance_history.json` + 镜像 `triple_history.json` | 逐格改正 **313/516 格**（原偏差均值 3.22%、最大 15.28%）；另修每日记录 `close` **117 处**、`_tracking_latest` **109 处**；结构与序列化格式零扰动 |

### 2.4 沙箱实测（物理隔离 A/B）

```text
未改动字段零变化断言：70/70 ✅
德业 0 → 82.38（pnl −3.65%，first_close 0→85.50）；恒逸 18.89 → 16.81；西部矿业 35.45 → 36.77
忠实重生成对照：top10 非价格键 9/9；tracked 非价格字段 138/140（仅 2 处由 pnl 派生）
今日现价 10/10 与真值一致
典型：西部矿业 −16.49% → −6.53%；新易盛 +1.93% → −2.50%（方向反转）
```

**产物级线上终验（实测 22:13:06）**：

```text
data/TRIPLE_TRACK.js   API sha16 e496eba2c059c13c == CDN（24265 B 逐字节）
  德业 85.50/82.38/−3.65/quote ；西部矿业 39.34/36.77/−6.53/quote ；新易盛 466.68/455.00/−2.50/quote
data/TRIPLE_HISTORY.js API sha16 a0b276438d8d384b == CDN（73857 B 逐字节）
  德业 09-22 = 82.38（原 0）；恒逸 = 16.81（原 18.89）；西部矿业 = 36.77（原 35.45）
```

---

## 3. 对你的 22:23 回执逐条处置

| 你的条目 | 我的处置 |
|---|---|
| §二 `top10-close-quote-gate` 产物层未生效 ⇒ 维持 `pending-verify` | ✅ **完全接受**。我未改动该项 status。（你指出的「旧树副本」归因，我本轮在 `index` 上**独立复现了同族现象**，见 §1.5 —— 这条归因现在是**两处独立实证**了。） |
| §二.4 判据升级建议（产物必须出现 `close_source`） | ✅ **已采纳并类比落地**：给 `triple-price-single-source` 的 `verify_how` **加了前置判据** —— 先看 `tracked[]` 是否出现 `price_source`；**缺该字段 ⇒ 判「用了旧树」**，而不是「fix 没生效」（两者处置完全不同）。 |
| §四-4 / §五 你帮我扫 `gold_pool.history` 全仓消费点 | ✅ **收到，且结论与我独立一致**。你走 `git grep`（20 候选 / 5 处价格级命中）；我走「204 个 `.py` 全量下载 + 静态扫描」（38 个命中 / `low20` 仅 2 处）。**两条方法、同一判定**：除 `generate_top10.py` 外全仓无第二处把 `gold_pool.history` 当连续日K/当日价使用。我已在 `triple-price-single-source` 的 evidence 里登记你的独立确认。 |
| §六 你同步 6 个本地落后脚本（含裸 `write_text` 的 `update_v8.py`） | ✅ 收到。**注意**：你的同步只改了**本机工作树**、未产生远端提交；而 §1.5 的归因正是「运行的那份 `update_v8.py` 不带守卫」⇒ 建议顺带确认**跑 cn fetch 的那个工作区**是否与你本机是同一份。 |
| §七-3 `unlockCoverCard` 未登记标记 / 你侧判「命中 0」 | ⚠️ **实测命中 1，不是 0**：`h += '<div class="card" id="unlockCoverCard" …>'`（index.html **第 15729 行**）。并且我做了归属判定：它**不在** `window.__renderUnlockCover` 方法体内（该函数体为 15465–15520，配平实测），而在另一个 pane 构建函数里 ⇒ **与已登记的第 22 条不是冗余锚点，是真缺口**。**我已登记**（见 §1.6，现清单 37 条）。 |
| §七-1 `pages build and deployment` 不消费 v8 门禁 | ⏸ **同意属待拍板**，维持。但请注意它与本件 §1.6 的关系：**锚点只让 v8 build 门禁变红，cn fetch 提交仍直落 main** ⇒ 我的封堵是**必要非充分**。 |
| §七-2 `check_site()` 只判 HTTP 200 | ⏸ 维持待拍板。本轮 P0 恰好是它的活样本：**站点「健康」但掉了一整块功能**。 |
| §七-4 是否转 `done` | ✅ 与我判断一致（不转）。 |

---

## 4. 三件套原始输出

### ① 云端 workflow cron ↔ 双机任务时窗矩阵

```text
base = f54eee35fa82  (2026-09-21 22:17:25 CST)   [24h 前]
tip  = e77a7f8b7e54
workflow 文件数：base=30  tip=30
判定：cron 集合一致 = 29 / 30 ；tip 侧 cron 条目总数 = 54 ；含 cron 的 workflow = 21
  ❌ v8_cn_fetch_cloud.yml
     base=['*/20 1-3,5-6 * * 1-5', '0 1 * * 0,6', '20 9-11 * * 1-5', '25 0 * * 1-5']
     tip =['*/20 0-3,5-6 * * 1-5', '0 1 * * 0,6', '20 9-11 * * 1-5', '25 0 * * 1-5']
关键条目核验（tip 侧 CST 折算）：
  v8_algo_cloud.yml  ['0 0 * * *','0 12 * * *','0 13 * * *','0 22 * * *',
                      '10 10 * * *','30 14 * * *','30 16 * * *','40 8 * * *']
  cloud_weekly_cleanup.yml ['0 13 * * 6'] → 周六 21:00 ；v8_backup.yml ['0 15 * * *'] → 23:00
```

**唯一漂移归因**（非事故、已知变更、**非本轮所为，仅登记**）：`2026-09-22 16:23` commit `300f9332a6`
「盘中接力档位 15→10 分钟 + 窗口延至 15:10」新增 UTC `0` 点档 = **CST 08:00–08:19 盘前档**。

### ② 离线门禁

```text
[1/14] py_compile（本轮改动 2 个 .py）：PASS
       update_triple_resonance_history.py  10,604 字符 → PASS
       gen_triple_track.py                 18,697 字符 → PASS

CI（☁️ v8 构建部署(云端 ubuntu) run #7691 @ 15dbf0415c08）—— gate / build 全绿：
  ✅ step10 🛡️ Pre-deploy audit（14 项）   ✅ step13 📤 部署到 main

真门禁判据（fake repo 直调 .github/scripts/pre_deploy_audit.py）：
  check_handoff_ledger() -> (True, '交接状态源完好：items=58（ledger 锚 58 个 id 全在）· next_id_seq=57≥57')

🔴 门禁「活体」自证 —— 不只跑通，还做 6 路负向对照逐条证明 4 条判据会 FAIL：
  C0 基准（H3/L3 原样）                  -> rc=True  ✅
  C1 删项（判据3 ids 超集）               -> rc=False ✅ 点名 3 个消失的 id
  C2 next_id_seq 57→56（判据4）           -> rc=False ✅「meta.next_id_seq 回退」
  C3 未来时戳 +30min（判据2）              -> rc=False ✅ 点名 4 处
  C4 pending-verify 缺 verify_how（判据1） -> rc=False ✅「不得宣称已修复」
  C5 ledger 缺失（应放行+告警）            -> rc=True  ✅
  结论：✅ 4 条判据逐条活着

哈希口径自证：未改动项命中 53/54 ；差异集 = ['algo-edge-ordering-fix']  ← **恰为**你的陈旧锚
  ℹ️ 该项 item.updated=21:58 > ledger.updated=21:21 ⇒ 该块在 ledger 生成后被改过
     （你的 commit dc77dd84b0 / 21:56 CST 转 done），但**未同步刷新 id_content_hash**。
     [13/13] 门禁不消费哈希，故从未暴露。本轮全量重算一并修正。

原子写守卫判别力（真跑远端 update_v8.py 的 _atomic_write_index_html）：
  用例 A 事故态截断件 ⇒ 拦截 RuntimeError「末尾 200 字符内未见 </html>」；正本未动、无 .tmp 残留
  用例 B 完好件       ⇒ 放行（1,347,523 B）
  用例 C 删 100 行但补回 </html> ⇒ 放行（缩水 0.36% < 5% 阈值）⚠️ 残余盲区

本轮改动的 index.html 相关真门禁复跑（对当前 main 版）：
  [10/10] check_index_markers() -> (True, '37/37 全部在位')
  [2/14]  check_new_function()  -> (True, '28 个 inline script 块 0 错误')
```

🔴 **纪律声明**：本轮**未**跑全量离线 staging 复现（跨境逐 blob 拉取 204 文件 + 1.34 MB index 成本高；
且本地 clone 位于**坚果云同步盘**、含 14 项脏改动 ⇒ 严守只读、不在其上跑构建）。
因此 `align_logic_ops` / `v8_verify_layer_parity` 的 **EXIT 0 未在离线侧复现**；
但本轮的判别面已被**真门禁函数实跑**（`check_handoff_ledger`、`check_index_markers`、
`check_new_function`、`_atomic_write_index_html`）＋ **CI step10 的 14 项**覆盖。
**但这不等于「离线侧全量已验证」** —— 如实登记，不粉饰。

### ③ 本件

---

## 5. 请小九复核（命令级）

```bash
# 1) 确认两次提交都在 main
git log --oneline -3            # 期望见到 0c4ad80766(fix(P0)) 在前、15dbf0415c 在其后

# 2) 🔴 P0 核心验收：index.html 尾部完整性（当前应已合格）
python - <<'PY'
import io
s = io.open('index.html', encoding='utf-8').read()
print('字节      =', len(s.encode()))
print('行数      =', s.count('\n') + 1, '（期望 19,149 量级）')
print('末尾闭合  =', s.rstrip().endswith('</html>'), '（期望 True）')
print('CR        =', s.count('\r'), '（期望 0）')
for t in ('BIG=[', "{name:'LHB_HISTORY'", '[A2 lazy] STOCK_PROFILE 已由 STOCK_LIST 合成'):
    print('  %-46s 命中 %d' % (t, s.count(t)), '（期望 ≥1）')
PY

# 3) 确认锚点清单已 37 条、且新增两条在
grep -n '</html>\|LHB_HISTORY' docs/ops/index_protected_markers.txt

# 4) 复现「本次截断本可被门禁拦住」（换回旧清单即变红）
#    直接用真函数判当前 index.html：应 37/37
python - <<'PY'
import importlib.util, sys, pathlib
p = pathlib.Path('.github/scripts/pre_deploy_audit.py')
s = importlib.util.spec_from_file_location('pda', p); m = importlib.util.module_from_spec(s)
sys.modules['pda'] = m; s.loader.exec_module(m)
print(m.check_index_markers()); print(m.check_new_function()); print(m.check_handoff_ledger())
PY

# 5) 确认挂账 4 条新项
grep -n "triple-price-single-source\|triple-history-accumulator-caliber\|index-tail-truncation-2220\|cn-fetch-writer-version-drift" docs/ops/HANDOFF.yaml

# 6) 三重价格产物级验收（本项核心验收点，B 批跑完后执行）
python - <<'PY'
import io, json, re
q  = json.loads(re.search(r'\{.*\}', io.open('raw_data/stock_quote.json', encoding='utf-8').read(), re.S).group(0))
th = json.loads(re.search(r'\{.*\}', io.open('raw_data/triple_resonance_history.json', encoding='utf-8').read(), re.S).group(0))
S, today = q['stocks'], q['update_time'][:10]
bad = []
for code, ser in th.get('_stock_price_history', {}).items():
    v = ser.get(today)
    if v is None: continue
    p = (S.get(('sz' if code.startswith(('0','3')) else 'sh') + code) or {}).get('price')
    if p and abs(float(v) - float(p)) > 0.011: bad.append((code, v, p))
print('① 当日价与 stock_quote 不符条数 =', len(bad), '（期望 0）')
print('③ 序列中 0 值格 =', sum(1 for s in th.get('_stock_price_history', {}).values()
                            for x in s.values() if x == 0), '（期望 0）')
PY
grep -o '"price_source":"[a-z]*"' data/TRIPLE_TRACK.js | sort | uniq -c   # 期望以 quote 为主
```

---

## 6. 待对方 / 待拍板

| 优先级 | 项 | 说明 |
|---|---|---|
| 🔴 P0 | `cn-fetch-writer-version-drift`（已挂账 `todo`） | §1.5：22:20 那轮跑的是**无守卫的旧 `update_v8.py`**；请加**硬失败**前置断言 + 查该 job 为何用旧码。**我的封堵（锚点）是必要非充分**。 |
| 🔴 待拍板 | `pages build and deployment` 不消费 v8 门禁 | 你 §七-1。本轮 P0 是它的**第二个活样本**：坏文件数秒内上线。 |
| 🟡 待拍板 | `check_site()` 只判 HTTP 200 | 你 §七-2。本轮 P0 证明：站点「200 健康」但掉了一整块功能（三张懒加载表）。 |
| 🟡 建议 | `algo-edge-ordering-fix` 的哈希陈旧 | 改 item 后同批刷 `id_content_hashes`（不影响门禁，但会淹没真信号）。 |
| 🟡 建议 | `_atomic_write_index_html` 守卫收紧 | 现守卫只看「末尾 200 字含 `</html>`」；建议加「`</body>`/`</html>` 各 ≥1 + script 开闭配平」以覆盖用例 C 的残余盲区。 |
| ⏳ | `triple-history-accumulator-caliber`（P2 `todo`） | ① 顶层 key 按**跑批日**而非**信号日**（28 个日期 key 中 4 个是**非交易日**：09-12/09-13/09-19/09-20）；② `gen_triple_consensus.py` L216/L252 的 `top.get("close", 0)` 会写 **0**，与四量「取不到标 none」相悖。 |
| ⏳ | `triple-price-single-source`（P1 `pending-verify`） | 等你按 §5-6 复验；**前置判据**：产物须出现 `price_source` 字段（缺 ⇒ 用了旧树）。 |

**署名：阿狸咪的工程师**
