# HANDOVER · 小九 → 阿狸咪 · 2026-09-22

> **更新于 2026-09-22 12:0x（12:00 槽位自动更新）**
> 本档由小九侧交接自动化（id `1785719698947`）生成。**引用的 commit SHA 均经 `git log origin/main` 实测核验**（非记忆）。
> ⚠️ 本档**不写本文自身提交的 SHA**（main 被 CI 逐分钟改写，短 sha 分钟级失效）——需要时请现取：
> `git log --oneline -3 origin/main -- HANDOVER_小九_2026-09-22.md`

---

## 🔴🔴 头号事项（P0，请阿狸咪**优先确认**）

### `index.html` 「自动刷新链断裂修复」**已上线但在 3 分钟内被静默覆盖回滚** —— 当前线上**仍是坏版本**

这是今天最严重的一件事，**已实证到字节级，非推断**。

**时间线（每环实测）**：

| 时刻 | commit | index.html blob | 字节 | `_reloadRequestedAt` | 判定 |
|---|---|---|---|---|---|
| 11:32:03 | `3136444c3` | `885e89aaa` | **1,341,429** | **4** ✅ | 修复本尊 |
| 11:34 | `d80514cd3` / `23bdf1e3c` | `9379c0dc3` / `f7570ea83` | 1,341,429 | 4 ✅ | 仍存活 |
| **11:35:59** | **`5adc9c323`** | `2d883d4db` | **1,339,558** | **0** ❌ | **被覆盖回滚** |
| 11:40 → 12:03 | `d75e8e050` … `555bf92cf`（全部 `v8 build`/`?v` chore） | `efa8cf967` … | 1,339,558 | 0 | **持续沿用坏版本** |

**覆盖源**：`5adc9c323`「data: index.html 经发布闸门核对 20260922」，作者 `ah-quant999 <2814546@qq.com>`，
parent `d4d7cad38b`（**不是** `3136444c3` 的后代），改动 `116 insertions / 139 deletions` —— 典型
**「以陈旧本地底稿整文件直推」**形态：拿的是 11:32 之前的基线，把 28 行修复连带抹掉。

**线上现状（三重取证，截至 12:0x CST）**：
- 远端 blob `6a68355b92338c11` / **1,339,558 B** / md5 `2d5c00c1e18b032c63fc98dad5d8037f` / CR=0（`git/blobs` raw 全长校验 MATCH）；
- `_reloadRequestedAt` 计数 = **0**，`__v8LiveDataProbe` = 3，`_maybeReload` = 3；
- 线上现行 `_maybeReload()` 仍是**旧实现**（`if(_maybeReload()) return;` 断链形态，第 18123 行），
  `visibilitychange` 仍只调 `_maybeReload()`、**不续排 `_tick`** ⇒ **主人报的「不会自动刷新」在线上依然存在**。

**为什么 11:31 的一次性自动化「成功了」却没用**：
自动化 `170d3815-cf2e-4611-b64f-9abd9d6a5b17`（`v8 自动刷新修复·11:31 窗口首推`）在 11:32 确实推成了
（`3136444c3` 在 `origin/main` 血链上，`merge-base --is-ancestor` 实测 ANCESTOR-OK）——
**但 3 分钟后被另一条（非本自动化）推送用陈旧基线顶掉**。这正是 09-21 那份「覆盖事故」的**同族复发**：
`force:false` **不是护栏**，基线落后对 git 而言就是快进。
> 关联 skill：`v8-overwrite-loss-audit-recovery`、`v8-baseline-drift-patch-replay`、`v8-truncated-baseline-recovery`。

**阿狸咪侧请办（本条属红线文件，小九侧刻意不擅动）**：
1. 先确认线上 id/pid 归属这条覆盖是谁发的（若为你的会话/夜班遗留 WIP ⇒ 请勿再从该底稿推）；
2. 用**线上真身为基底**重放修复：`885e89aaa`（或 `git show 3136444c3:index.html`）相对线上做
   **定向补丁重放**，`--expect-base <index.html>:2d5c00c1e18b`（md5-12）断言后再推；
3. 推后**当场回读 blob 逐字节自验**（勿只看 run 绿）；
4. 待推原稿仍在：`E:/v8data/qs8-tmp/PENDING_PUSH_20260922_1130_index.html`
   （1,341,429 B / md5 `ce209db34625b239e1cef0a639788ae5` / 纯 LF，与 `885e89aaa` 同尺寸）。
   ⚠️ 该稿基底是 **11:30 前**的线上真身（1339558 B），**11:35 之后的 churn 未纳入** ⇒ **不可直接整文件推**。

---

## 一、背景与今日概览

- **2026-09-22 星期二，A 股交易日**（非节假日）。小九白班 7:45–17:45 独家；阿狸咪夜间/周末。
- 今天小九侧的主旋律：**「假修复」两次被自己抓出来**（见 §二 A、B），加上**盘中链断档急诊**与**运维看板全量重建**。
- 今日 main 提交量 **≈100 条（`--since 00:00` 实测 100）**，`?v` 缓存戳 churn 依旧每几分钟一轮。
- 今日远端 run 总数 **230**（`created:>2026-09-22T00:00:00Z` 实测 `total_count=230`）。

---

## 二、今日已完成改动（真实 SHA + 影响）

### A. 🔴 **自我纠错：「三条红灯修复」上一版是假修复，已二次修复上线**

**问题**：08:05 那版 `cloud_fetch_v8.py` **只恢复了三重接线中的 1/3**：

| # | 登记处 | 当时状态 | 失效后果 |
|---|---|---|---|
| ① | `tasks`（L4449） | ✅ 已恢复 | —— |
| ② | `CATEGORY_MAP`（L118） | ❌ 仍注释 | 不进 `target_vars`（L3903 本表派生）⇒ 主循环首行 `continue` |
| ③ | `VAR_TO_RAW`（L78） | ❌ 仍注释 | `save()` 取不到 fname（L301）**直接 `return`** ⇒ raw 永不落盘 |

⇒ 结果：「**函数能返回 27 条数据，但永不执行、永不落盘**」。而当时**净室单测 25/25 全绿** —— 因为单测**直接调函数**，恰好绕过 ②③ 两道关卡。

- 🔴 **最贵教训：`单测绿` ≠ `链路通`**。判据必须是 **AST 静态提取三表做集合断言**（从 `main()` 入口口径推），
  而不是「调一次函数看它返回什么」。
- **修法**：`cloud_fetch_v8.py` commit **`83eee7321f18`**（242,405 B / md5 `9c84ff41722e`，回读 blob `a58c12797d` 逐字节一致）
  ① 补 `VAR_TO_RAW["ANALYST_RATINGS"]` ② 补 `CATEGORY_MAP["ANALYST_RATINGS"]="premarket"`
  ③ **新增 `main()` 抓取前接线完整性硬自检**（三表集合断言 + 重名检测，不满足即 `raise SystemExit`）——
     放抓取之前成本≈0，专防「run 全绿但零产出」的**不报错、不红灯**静默失能复发。
- **端到端验证**（非单测口径）：`py_compile` ✅；三表 **33/33/32**；漏登/重名均为空；
  `category=premarket` 目标集 14 个且含 `ANALYST_RATINGS` ✅；定向 `workflow_dispatch category=premarket` ⇒ `cn_fetch #1956` 已启动。
- **交接单更正**：`…三条红灯一劳永逸修复.md` 追加 **§9 补记**（诚实记录假修复），commit **`9b8667e917e0`**（13,095 B）。

### B. 三条红灯「一劳永逸」根因修复（runner×2 假阳性 + INST_COVERAGE 真陈旧）· ✅已推送

主人令「一劳永逸哈」（附 3 张截图）。**三条性质完全不同 ⇒ 修法完全不同，先定性再动手**：

- 🔴 `runner_github` + `runner` = **结构性假阳性**：降级闸门用 `run_time`（=最近一次抓取时刻）判「守护是否活着」，
  而 LEMONCAT 处于兜底角色时该值天然陈旧 —— **实测 `age(run_time)=615.6min` vs `age(update_time 心跳)=27.0min`**
  ⇒ `<=90` 闸门永不生效。**正确判据是守护心跳 `update_time`。**
- 🟡 `INST_COVERAGE` = **真陈旧（半截接线）**：`33499302c8`（09-22 05:31 阿狸咪）**只提交了产物 `data/INST_COVERAGE.js` + `index.html`，零生产者**；
  其数据源 `raw_data/analyst_ratings.json` 的抓取链早在 **09-18 以「前端零引用 ⇒ 浪费抓取预算」注销**
  ⇒ 新卡消费的是一条**已注销的链** ⇒ 冻结在 09-17 16:43:40 ⇒ 通用红线恒判 fail。
- 🔴 **附带发现更大事故（截图未覆盖）**：云端 `v8_cn_fetch_cloud.yml`（id `327687211`，**active**）
  自 **09-21 15:54 起连续 13 轮 / 13 小时全 failure**。日志铁证：
  `❌ W52_HIGH 失败(attempt 3/3): JSONDecodeError` → `阻止空壳推送` → `##[error]exit code 1`
  ⇒ 其后 `push raw_data` / `update_v8` / `push data` **全 skipped**。
  判据：同 host(push2delay) 走 `em_clist→_em_get_with_retry` 的 4 模块全成功（raw_data 169 文件）；
  `w52_high.json` 09-18~09-21 持续正常产出 ⇒ **接口可用，纯属 `f_w52_high` 是「全场唯一裸调 `.json()`」的单点拖死**。

**修法（全部根因层，无一处放宽红线）**：
1. `f_w52_high`：退避重试 0.5/1.5s×3 + 非 JSON 显式识别 + **失败 `return None`**（`status=empty` 不阻断）。
2. `ANALYST_RATINGS` 抓取注册恢复 + `f_analyst_ratings` **双保护**（`get_ak()` 原本在 try 外；全空时原本会 save 空壳覆盖好数据）。
3. `update_v8` 映射改指 `INST_COVERAGE` + 新增 `_make_lite` 转换分支（**9 字段与既有线上文件逐字一致**）。
4. `v8_health_check` 降级闸门：`update_time` 优先（缺失回退旧行为）+ 追加 `_gh_ok`（guard 用 GitHub API 现查的 self-hosted 结论）。

**推送**：`cloud_fetch_v8.py`→**`7fa77c6ec100`**（第 1 轮 PATCH 422 not-ff，**第 2 轮才是成功轮**）；
`update_v8.py`+`v8_health_check.py`→**`87397939117f`**；交接单→**`1b5c52d49840`**。均带 `--expect-base` 断言 + 当场回读 blob 逐字节一致。

**本轮新坑（已固化进 skill `v8-health-board-order-and-redlight` §11）**：
① **`run_time` ≠ 守护心跳** —— 用「上次干活的时刻」判「人还在不在」＝结构性假阳性；
② **「全场唯一裸调」是高危指纹**（判据：同 host 其他模块都走统一重试封装且全成功）；
③ **「新卡消费已注销的链」** —— 半截接线第二种形态（第一种是产物漏删，这次是生产者被主动注销后又有了消费者）；
   ⚠️ 恢复注册时**必须同时给该 fetcher 加保护**；
④ run 日志取证正路：`GET /actions/runs/{id}/logs`（zip，`0_fetch.txt`）**比** `/actions/jobs/{id}/logs` 可靠（后者实测 401）。

### C. v8 运维看板全量重建（任务运行看板 + 前端健康巡检）· ✅已推送并线上生效

主人令：「审计运维和看板，看板要所有前端的对应运行胶囊都写出来，才知道到底哪里出问题了，照样按导航按钮和页面内部顺序排序」。

**审计发现（改前）**：看板是**手写静态表**、与前端解耦，导航改版/轻量化后静默过期。
- 🔴 P1 `rc` 共振日历**整页 0 胶囊**（前端真实 8 张 LHB_DATA 卡从未登记）；
- 🟡 P2 `ph` 盘后数据 14 胶囊 vs 前端真实 7（**8 个幽灵**）；🟡 P3 `st` 5 vs 19；
- 🟡 P4 顺序失配（期指被错放 `ph`，实为 `rt`）；🟡 P5 `_PAGE_CARD_ORDER` 缺 3 个真实卡 id；
- 并纠一处**映射错写**：「板块资金趋势」源由 `SH_FIB` 改为 `SECTOR_RECOMMENDATION`。

**交付**：按 **导航按钮顺序 + 各页 `<h3>` 真实卡序** 全量重建 `V8_PAGE_SCHEDULE`（胶囊 40→**59**：td5/rt9/ph7/rc8/final2/st19/bt1/ul3/op5）
与 `_PAGE_CARD_ORDER`（6→**9** 组）。只动这两块，**逆运算逐字节证明其余零改动**。

- **验证三连 + 第四道线上硬抓全绿**：① 逆运算 `assert rev == content`；② **jsdom 真实渲染**（分组序 9/9、组内 id 序 9/9、页序 9/9、胶囊 59/59）；
  ③ 推后远端真值 11/11；④ Pages 缓存击穿硬抓 HTTP 200 / 1,355,996 B / 6 标记命中。
- **推送**：`index.html` commit **`f3af92216732`**（基底 `539de24a2523` / md5 `bba5ccf14c0f`；成功轮回读 blob md5 `187c52a37262` 逐字节一致）；
  交接单 `docs/ops/handover/2026-09-22_0550_小九给阿狸咪_健康看板全量重建.md` commit **`cba766889b70`**。
- **坑**：① `K.fetch_raw` 返回**字符数**（1123747）而非字节数（1355996，中文 3 字节）；
  ② 改前 tip 与推送时 tip **同内容但 blob sha 不同、长度相同**（`?v` churn）⇒ **判一致性只认内容 md5**；
  ③ 我自己的核验脚本两次假阴性（拿 **page 名**比 **page id**；`ph` 段内 `name:'` 把**页对象自身 name** 也算进去 ⇒ 8 而非 7）
  ⇒ **断言写错会伪装成「代码有 bug」，先自证断言再怀疑产物**。
- **逆运算自检的对称性坑**：正则把 `function renderTaskSchedule` **后缀一并捕获进 OLD_SCHED** ⇒ 反向替换会补两遍后缀（假失败）。
  修法：正则**止于 `  };`**；NEW 串首行**从 `const` 起算（无前导空格）**；反向替换前加唯一性断言。

### D. 机构研究卡「日期是今天、素材是昨天」根因修复

**表象**：主人 08:53 截图 —— 机构研究 · AI 解析建议卡「更新于 昨日 08:40 盘前 短线」。

**根因（结构性，非偶发）**：**素材发布主峰 08:40–08:50**（2443 样本实测，占当日盘前 33%），
而 `v8-guanlan-refresh` 只排 **08:00** ⇒ 08:50 出稿读到的永远是 08:00 快照。
（08:52 那次的网络超时与素材陈旧**无因果**，偶发叠加放大表象。）

**复核真值（关键）**：出稿 `sources_read` 自陈「素材窗口 2026-09-18 ~ 2026-09-21」；
对「只有 09-22 素材才有」的 23 个关键词做 `count()` ⇒ **短剧/阅文/红果/秦淮/东阳光/华虹/招商证券/恒立液压/世纪互联/霍尔木兹/封装设备/芒果超媒 全部 0**
⇒ **稿内日期是 09-22、素材却是昨天的**。

**修复**：
- **调度层**：`v8-guanlan-refresh` 触发器 **08:00 → 08:00 + 08:45**（出稿前 5 分钟；刷新实测 14.7s，时序安全）。
  XML 段替换 + **UTF-16 写回** + `schtasks /create /xml /f`；导入后逐项验证保留：
  `InteractiveToken` / `WakeToRun=true` / `ExecutionTimeLimit=PT20M` / UserId SID / Command 全部未变 ✅。
- **内容层**：08:56:27 手动 `--force` 刷新（`max_date` 09-21 → **09-22**，20 条 / 12 条当日新增），
  用今日素材重出稿（daily **5044 字**、compare 三段齐全、conclusion **118 字**），过闸门二次核对**全项 ✅**；
  09:00 属闸门「开盘前缓冲（09:00-11:30）」⇒ **staging**，**11:35 flush automation 自动发布**。
- **线上复核**：`data/maharo_insights.js` 远端 23,073 B，`sources_read` = 「素材窗口 **2026-09-21 ~ 2026-09-22**；共 20 条，其中当日新增 12 条」✅，`update_time=2026-09-22 09:00`（gate 提交 `7d383af45`）。
- **交接单**：`docs/ops/handover/2026-09-22_0910_小九给阿狸咪_机构研究卡素材陈旧根因修复.md`（6,588 B，commit **`f9612d1d10f6`**）。

**本轮新坑（值得固化）**：
1. **闸门的「盘中」定义是 `09:00-11:30` + `13:00-15:00`，比 v8 铁律的 09:15 更严**（多出「开盘前缓冲」）⇒ **以闸门输出为准**。
2. **`schtasks /query /xml` 重定向到文件后编码会变成 UTF-8，而 XML 声明仍写 `encoding="UTF-16"`**
   ⇒ 必须显式以 **UTF-16（带 BOM）** 写回再 `schtasks /create /xml`，且改触发器要**逐项断言关键属性未丢**。
3. **禁用词自校验会有跨词假阳性**：「判断**更**结构化」子串命中「断更」⇒ 判据要加**汉字边界断言**。
4. **闸门 `compare.conclusion` 硬限 80–140 字**（首版 224 字被拦），其余 `tension/align ≥25`、`delta` 非空。
5. **staging 里出现了非本轮改动的 `index.html`/`logic.html`（08:28）**：`logic.html` 与线上 **md5 完全一致**（no-op）；
   `index.html` **同尺寸、仅 1 行差异**（`var BUILD` 陈旧 vs 线上）⇒ 归一化 `?v=` 后全文一致 ⇒ 发布无实质风险。
   **教训：判「staging 会不会覆盖」不能只看字节数，必须 `md5` + 归一化逐行 diff。**
6. **本机时钟与 GitHub `Date` 头完全一致**（实测 2026-09-22 08:32:59），
   但**系统注入的 `<current_time>` 曾偏快/偏慢** ⇒ 判写仓窗口**必须实取**，不能信注入时间。

### E. 盘中更新变慢（并发槽死锁）急诊 —— ✅已解，非代码故障

主人报障：「怎么今天盘中更新变慢了」。

- **量化「慢」**：实时卡集体停在 **09:35–09:40**，掉 1 档（应 20 分钟一档）。
- **定位占用者**：`🇨🇳 v8 中国数据抓取(云端·小九应急)` **#42**（run `35677066808`）`in_progress`，
  **卡在 step `📤 推送 raw_data/ 到 main（Contents API）` 长达 22 分钟**（09:47:52→10:10），其后 7 步 pending。
- **并发语义**：组 `v8-cn-fetch-cloud`（`cancel-in-progress:false` + `queue:max`）⇒ **最多 1 running**
  ⇒ `#1958` 被取消、`#1959`/`#1960` 双双 pending ⇒ 数据链断档约 40 分钟。
- **对比定性**：`#41`（09-21 15:13）同 step 为 `success` ⇒ **今日推送竞态/限流瞬时故障，非代码坏**。
- **处置**：`POST /actions/runs/35677066808/cancel` → **HTTP 202** ⇒ #42 于 **10:12:19 cancelled**（槽位释放）
  ⇒ `#1959` **10:12:22 起跑 → 10:24:05 success**（37 分）→ `#1960` 10:24:10 `in_progress`
  ⇒ 实时卡全部恢复 **10:28:38 CST** ✅。
- 🔴 **新挂账（守卫阈值盲区）**：`v8 链死锁守卫` 判据是「单 run 超 **90 分钟**」，今日 #42 挂起仅 22 分钟
  **未达阈值** ⇒ 守卫全程未介入。**建议评估给「推送步」加更细粒度超时判据。本轮刻意未擅改。**

### F. 页面自动刷新失效 —— 根因锁定 + A/B 实证通过 + 11:32 上线 ⇒ **11:35 被覆盖回滚（见文首 P0）**

- **根因（jsdom 受控时钟实跑，非推断）**：`__v8SmartRefreshStarted` 中
  `_tick()` 里 `if(_maybeReload()) return;`，它假设「`location.reload()` **必然销毁页面**」。
  但 **Chrome 对后台标签页/省电模式会抑制 reload** ⇒ 定时器链**已断**、页面**却还活着**；
  而 `visibilitychange` 兜底**只调 `_maybeReload()`、不重启 `_tick`** ⇒ **永久不再自动刷新**。
  **实证：受控时钟推进 45 分钟，reload 仅 1 次 @15.5min，之后零事件。**
- **修复三处（只续命、不改任何时段语义）**：① `_tick` **无条件续排**（去掉那个 return）
  ② 新增 `_reloadRequestedAt` + **90 秒自愈重试** ③ `visibilitychange` 回前台**补一次 `_tick`**；
  另更正 head 里一处**过时注释**。
- **A/B 实证**：盘中·reload 被抑制 → 修复前 1 次(断链) vs 修复后 **4 次(不断)**；
  盘中·reload 正常 → 均 **1 次（无重复刷副作用）**；夜间 23:30 → 均 **0 次（未误刷）**。
- **校验**：内联 script 28 块全部可编译 ✅ ／ 标签结构与线上真身**逐项一致** ✅ ／ 纯 LF ✅ ／
  15min 间隔·夜间不刷·4h 硬上限·2026 休市日表·HEALTH_CHECK 探测**全部完好** ✅。
- **上线与回滚**：`3136444c3`（11:32）上线 ✅ → **`5adc9c323`（11:35）覆盖回滚** ❌ ⇒ **见文首 P0 处置要求**。

**本轮新坑（已固化）**：
1. **`<head>`/`<body>`/`<script>` 计数不等 ≠ 我改坏了**：线上真身本来就是 `<script>` 开 134 / 闭 129（5 个孤立标记）
   ⇒ 判据必须**与真身逐项对照**，而非与理想值对照 —— 否则自造 3 个假失败。
2. **跨境整取必须带 `Content-Length` 全长校验 + 重试**：本次 `git/blobs` 首抓即 `IncompleteRead(1830466/1850-15447)`，单次脚本必崩。
   且 `Accept-Encoding: identity` 可显著降低截断。**本轮实测：`contents` 端点对 1.34MB 文件连续 3 次返 TRUNC(0)，
   必须改走 `git/blobs/{sha}` + `Accept: application/vnd.github.raw` 才拿到全长。**
3. **`python -c` 含引号嵌套会被 bash 吃掉**（`find('fetch(url, {method:\'HEAD\'')` → `SyntaxError`）⇒ **落 `.py` 再跑**。
4. **jsdom 的 `location` 不可 `defineProperty`** ⇒ 改把 `location` 作为 `new Function(...keys, code)` 的**形参注入**。
5. **测试桩自身会产生假阳性**：场景2 首版报「修复后 4 次 reload」，实为多定时器同刻排队 + 计数器在抛错前已 `push`
   ⇒ **先自证断言/桩，再怀疑产物**。

### G. v8 链死锁守卫（第 51 / 66 / 67 / 71 / 72 次执行）

全部为**纯巡检 · 零写动作**，五次执行**一致结论：无超 90 分钟未完成 run、零 cancel、零 dispatch**。
- **第 72 次（09:36–09:56）**：在飞 run age 峰值 0.147h（距阈 10 倍余量）；
  **时间戳口径首次同屏对照自证**（`_aw_r72_selfcheck.py`）：timegm=**0.0055h** vs mktime=**8.0055h**
  ⇒ 错误口径的 +8h 虚报被当场量出，不再靠推理。
  唯一多成员组 `v8-cn-fetch-cloud` 组内 2 条在飞**全部判「真活」未 cancel**；
  ✅ 第 71 轮观察项**销案**：`#35671133338` 的 `💰 分红方案刷新` 步 **08:35:18 正常收尾 success**（总 20.6 分）
  ⇒ **未复现 2026-09-18 的 8.6h 卡死**。
- **第 51 次（11:0x）**：升级为 **7 链采样**（主 5 + `cn_fetch_cloud_hosted` + `cn_fetch_cloud_selfhosted`）；
  **实测确认 `v8-cn-fetch-cloud` 是唯一多成员并发组（成员 3）**，三条均 `cancel-in-progress: false`。
- **第 66 次（02:16）**：**结构性发现并当场修复** —— 原自动化清单只覆盖 1 条链，
  已更新 automation `63ad8f3d-97cd-4cc4-a761-65c041545842` prompt 把两条兄弟链补入遍历清单。
- **第 67 次（03:31）**：第 66 轮「检测盲区」修复的**效果验证** —— 全仓 31 workflow / 20 并发组中多成员组仍仅 `v8-cn-fetch-cloud`，
  组内近 20 run 未完成 = 0 ⇒ **确认是盲区修复、不是事故**。
- **固化结论**：① 同组多链只扫主链会漏 2/3；② **pending 的 age 不得单独作死锁判据**（本链 pending 恒为 2、随每次派发被顶换新）；
  ③ 判 run 属哪条链**必须认 `workflow_id`/`path`**。

### H. 分红链 #1 修复（`merge_dividend` 真合并）生效核查 —— ✅达标（只读取证）

**判据 = 源码自己打印的计数器（非推断）**，同字段隔日对照：

| 时刻 | run | `沿用 cninfo` 计数 | 说明 |
|---|---|---|---|
| 2026-09-21 08:40:07 | `#35578850127` | **0** | 修复前口径（死代码期） |
| 2026-09-22 09:49:44 | `#35677081850` | **329** | **0 → 329 跃变** |

- **时序链**：cninfo 链末次写入 **09-22 08:59:05** → 东财全量重建 **09:49:44** → 该轮打印 `沿用 cninfo 329 只 | 沿用上一版 51 只`
  ⇒ **cninfo 成果在东财全量重建中存活** ⇒ 「保住 cninfo」判据成立。
- **线上真身（截至 10:14 CST）**：blob `562f7efeab23…`，**4,377,871 B**，md5 `ac4838e1deb766be0233b8a52f0036d7`，
  `update_time=2026-09-22 09:51:16`，`stocks=9837`；`"src":"cninfo"` = **380**（vs 09-21 基线 **0**）✅。
- 🔴 **对账闭合**：源码打印 329 ≠ 线上 380，差值 **51** 正好等于同一行打印的 `沿用上一版 51 只`（第二遍同样带 `src:cninfo`）
  ⇒ **329 + 51 = 380 精确闭合**。**别把「源码计数 ≠ 线上计数」直接判成矛盾**——先找同批次其他计数器的加和关系。
- **口径核对**：逐票复算 380 只：`k_prev == 20260630` **100** 只 ＋ `k_prev == 20251231` **213** 只 = **313/380 走同期 `>=` 分支** ✅；
  其余 277 只（更旧）存活 ⇒ 未触发覆盖 ⇒ **无报告期倒退**。⚠️ **3 只 `report_period="nan"`** 未逐一取证，已标注，不影响主结论。
- **新固化技法**：
  1. **job 日志下载 401 的正确解法**：`GET /actions/jobs/<id>/logs` 302 到 `productionresultssa*.blob.core.windows.net`（签名 URL）
     ——**跳转后必须去掉 `Authorization` 头**，否则 Azure 报 `InvalidAuthenticationInfo`（401），极易误判成「token 失效」。
     正解：`HTTPRedirectHandler` 子类返回 `None` 禁自动跟随 → 手工取 `Location` → **干净请求**抓取。
  2. **别用 `cut -c` 截 job 日志**：会把 UTF-8 中文按字节切断 ⇒ mojibake（`馃…`），误以为日志乱码。**逐行读 + 直接打印**即可。
  3. **判「cninfo 链昨夜跑了没」不能只看有没有 run**：本 workflow run 列表**绝大多数是 `workflow_dispatch`**（本机派发），
     `schedule` 极少且常 failure；09-21 17:20/18:20/19:20 三档 cron **均未产出** success run；
     真正两次成功写入在 **09-22 08:24 / 08:48 CST** ⇒ **「src:cninfo 在 09-21 为 0」属排程漂移，不是链故障。**
  4. **通道可用性会漂移**：本轮 `raw.githubusercontent.com` **ok=1/1（1.67s）**
     ⇒ 与 09-21 的 ok=0/3 不同；但铁律不变（`vnd.github.raw` 会 302 跳该域），本次全程未用该 Accept。

### I. 其他小九侧产出 / 状态

- **数据校准卡 · `MARKET_ALERTS` 新档延续**（阿狸咪 11:39 汇报）：仍卡 `2026-09-21 20:51:06`（滞后 ≈14.8h），
  今日盘中 run #1964/#1965 日志再证 `❌ … 90s超时 ×3 → ⏭ 跳过`（今日第 6/7 例，100% 复现），run 总状态仍 success = **吞错假绿**。
  **源代码级确证**：`cloud_fetch_v8.py` L3159 `f_market_alerts()` → **L3171 `timeout=90`**（同族为 300/1200，异类过紧）
  ⇒ 代码缺陷，**待主人裁决是否放宽 timeout**；**勿再派 run（只会空转白派）**。**归属阿狸咪侧报告，小九侧仅登记。**
- **`STOCK_QUOTE` 十八轮停摆观察项结案**：09:17 轮派发的刷新已闭环，线上 `data/STOCK_QUOTE.js` 4,410,463 B / `raw_data/stock_quote.json` 4,851,257 B。
- **v8 runner 本地守卫 11:12 轮（自动化·20 分钟级）**：`status=OK`（listener PID 10140 alive / worker 空闲）；
  推送 HTTP 200（runner_status.json + hb_xiaojiu.json），Contents API 二次核验 PASS（remote sha `d88e706a38ac`、deflate 2972 B、KEYSET_EQ 14/14、四锚点一致）。
  链块推进第 3 轮：08:21:44(premarket) → 09:45:07 → **11:08:06(intraday)**，modules=20 ok:19 fail:1（fail=`MARKET_ALERTS`，属云端 ubuntu，按口径不判本地）。
  无自愈动作（`actions_taken=[]`）。
- ⚠️ **工具经验（重要）**：本轮 `tasklist` 与 PowerShell `Get-Process` 对 Session0 下的 `Runner.Listener.exe` **均枚举不到（返空）**，
  改用 `wmic process where "name like '%Runner%'"` 才取到真值 ⇒ **判 Runner 进程存活一律以 wmic 为准**，勿据 tasklist 返空谎报「进程消失」。

---

## 三、当前系统状态（12:0x CST 快照）

| 项 | 实测值 | 判读 |
|---|---|---|
| **主站** | HTTP **200**（`?cb=` 击穿缓存） | ✅ 正常 |
| **main tip** | **`589131a25`**（`data: 阿狸咪心跳上报 20260922-1200`） | ✅ 滚动中 |
| **今日 main 提交** | **100** 条（`--since 00:00`） | 正常 churn |
| **index.html（线上）** | blob `6a68355b92338c11` / **1,339,558 B** / md5 `2d5c00c1e18b032c63fc98dad5d8037f` / CR=0 | 🔴 **仍是坏版本（见 P0）** |
| **HEALTH_CHECK** | `11:56:27` **overall=warn** `ok118/warn0/fail2/limited2/info0/total122` | 🟡 见下 |
| ├ fail=**MARKET_ALERTS** | 90s 超时 ×3 跳过，`update` 落后 14.8h | 阿狸咪已挂账，待主人裁决 |
| ├ fail=**all_US_HK_MAP** | 今日新出 | 需夜班复核 |
| └ limited=**MACRO_KSHAPE** / **UNLISTED_PANEL** | 静态索引/降级态 | 非故障 |
| **runner `lemoncat-cn`** | **online** / busy=False / labels `[self-hosted, Windows, X64, cn, xiaojiu]` | ✅ |
| **runner `alimi-cn`** | **online** / busy=False / labels `[…, cn-cn]` | ✅ |
| **HB_XIAOJIU** | `last_time=2026-09-22 11:12:30` / status=ok / `update_time=11:46:10` | ✅ 双机心跳腿均活 |
| **HB_ALIMI** | `last_time=2026-09-22 09:38:32` / status=ok / `update_time=11:46:10` | ✅ |
| **今日远端 runs** | **230**（success 61 / cancelled 31 / failure 1 / 在飞 7，页内 100 采样） | 🟡 cancelled 偏高，含并发组顶换（设计内） |
| **自动化 ACTIVE/PAUSED** | **126 / 24** | ⚠️ 较 09-21（120/21）各增 → 见 §五 F |
| **今日 `automation_runs`** | **153** 条（ACCEPTED 151 / IN_PROGRESS 2 / **FAILED 0**） | ✅ 无失败 |
| **站点 index.html（Pages 直取）** | 200 + 0B（HEAD/流式被 CDN 处理）⇒ 以 API blob 为准 | 正常 |

**fail=2 定性**：两条**均非小九侧新引入**，`MARKET_ALERTS` 属阿狸咪已挂账的代码缺陷（timeout=90），
`all_US_HK_MAP` 需夜班复核是否与 §二 B 的 `US_HK_MAP` 加固未推有关（见 §四）。

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 必须优先（P0）
1. **`index.html` 自动刷新修复被覆盖回滚** —— 详见**文首 P0**。当前线上仍是坏版本，主人报的「不会自动刷新」在线上依然存在。
   请用**线上真身为基底**重放 `3136444c3` 的 28 行修复，`--expect-base` 断言 + 推后回读自验。

### 🔴 待主人拍板（小九侧只登记、不擅动）
2. **P0-新② 交接档禁写死 sha**（09-20 起挂）：建议改约「路径 + 字节数 + md5」。
   ⚠️ **本档已按新约执行**（不写自身 sha，改给现取命令），但**仓内既有 `docs/ops/handover/**` 仍大量写死短 sha**
   ⇒ 建议在 `docs/ops/handover/README.md` 落一条明文约定。
3. **P0-新③ 云端「分红方案刷新」产出永不落库**（09-22 分红监督实测，仍挂）：
   `v8_cn_fetch_cloud.yml` 步序 …… 分红步在**所有推送之后**（L780），自身与后续步均无推送
   ⇒ 产出随临时 runner 丢弃；run/步/「395 只已更新」三绿、线上 **max plan_date 仍 2026-09-19**（假成功）。
   疑因 09-18 `dca6215347`「分红步后移」移到推送之后却未补推送。
   **建议**：L780 后补 Contents API 推送步（仅两文件，`continue-on-error: true`）+ 两文件纳入 landcheck PROBE。
   ※ 与 §二 H 的「cninfo 存活」是**两件不同的事**，勿混。
4. **死锁守卫阈值盲区**（今日新增）：守卫判据「单 run 超 90 分钟」，而今日 #42 推送步挂起 22 分钟即断链 40 分钟。
   建议给**推送步**加更细粒度超时判据（本轮刻意未擅改）。
5. **`MARKET_ALERTS` `timeout=90`** 是否放宽（今日第 6/7 例 100% 复现，run 假绿）。**归属阿狸咪报告，待主人裁决。**
6. **两条白天结构性静默链是否加白名单**（第 72 轮死锁守卫提出）：
   ① `cn hosted`（`cf281e1575`，cron 仅 `30 12 * * 1-5`）② `cn selfhosted`（`0b22a6a494`，`on:` 仅 `workflow_dispatch`、cron 0 行）。
7. **P-Q 盘中粒度盲区**：`guard_v8_freshness.py check_group` 对 `max_hours>=24` 一律按交易日判
   ⇒ 12 个盘中模块同日停更不判红。建议「盘中走小时／盘后走交易日」双口径。
8. **P-1 CI 覆盖源补丁**（`docs/ops/patches/v8_build_deploy_index_cover_fix_20260916.patch`；
   `v8_build_deploy.yml` L205-207/L340-342 覆盖源未修）⇒ 通道现成，只差授权。
9. **判据 80 桥接无单调守卫**：`update_v8.py::_make_js` 无 `update_time` 单调校验 ⇒ 建议加不回退守卫（76 条桥接全受益）。
10. **P-R `packed-refs` 僵尸条目** ⇒ 建议 `git update-ref -d refs/remotes/origin/main` 治根。

### ⚠️ 阿狸咪侧遗留 / 待复核
11. **`cloud_fetch_v8.py` / `scripts/fetch_us_hk_map.py` 的 US_HK_MAP 加固仍本地 WIP 未推**
    —— 这两个文件**不在 `push.paths` 内**（推它们不触发全量构建）⇒ 可随时推。
12. **线上新 fail `all_US_HK_MAP`** 是否与第 11 条相关，请夜班复核。
13. **「解禁&机构关注」因子观测卡转正前三问**（小九 09:57 已发，commit `6b3b0c71c`/`d4d7cad38`）；
    HANDOFF 已登记 `blocked` 项。
14. **`.github/scripts/pre_deploy_audit.py` 陈旧暂存位**待清理（主树 STAGED 项，红线只登记不擅动）。
15. **`data/SPREAD_INTRA.js` 线上 404**（HEAD 实测），但被当有效卡引用 ⇒ 疑似悬空引用，待专项核查。
16. **`update_v8.py` DATA_SOURCES 已登记但 CATEGORY_MAP 未登记 10 个变量**
    （`FACTOR_AUDIT`/`FACTOR_PROGRESS`/`FACTOR_WALKFORWARD`/`INDEX_VALUE_FRAMEWORK`/`OPTIMIZED_STRATEGY`/
    `SECTOR_CYCLE_ARCHIVE`/`SECTOR_PHASE_HISTORY`/`VALUATION_PERCENTILE`/`WEEKEND_META_REPORT`/`WEEKEND_RUN`）
    ⇒ 很可能由算法链/周末任务另行产出，**贸然补登记会打乱产出时序** ⇒ 需先取证各自真实产出方。

### ⚠️ 主人侧待拍板（小九侧本轮未处置）
17. **拍板 5 项（阿狸咪归集）**：B-1 仓根 `run_algorithms.py` 旧单体（95,355 B）处置；
    B-2 `cloud_fetch_v8.py`+`scripts/fetch_us_hk_map.py` 本地 WIP 是否上线；
    B-3 本地残留 3 份 `scripts/baihechou_*.py` 是否删；B-4 `algorithms/fetch_board_first_limit.py` 本地缺失是否补；
    B-5 `.github/scripts/pre_deploy_audit.py` 陈旧暂存位清理。

---

## 五、纪律 / 坑（今日新增，可复用）

**A. git 判据类**
1. **判一致性只认「内容 md5」，别认 blob sha、更别只认长度** —— `?v` churn 下同内容不同 sha、长度可完全相同。
2. **`contents` 端点对 >1MB 文件会静默截断**（本轮 1.34MB 连续 3 次返 `TRUNC(0)`）
   ⇒ 大文件**必须走 `git/blobs/{sha}` + `Accept: application/vnd.github.raw`**，并**带全长断言**（`len == size`）。
3. **`_reloadRequestedAt` 计数是「修复在不在线上」的字节级判据**（本轮靠它抓出覆盖事故）——比看 commit 是否成功可靠得多。

**B. 覆盖事故类**
4. 🔴 **`3136444c3` → `5adc9c323` 是本仓覆盖事故的「最新标本」**：
   修复存活 **2 个 commit / 3 分钟**就被整文件重推抹掉，且 **`force:false` 毫无作用**。
   ⇒ **「我推成了」≠「线上是我的版本」**；推后**必须**回读远端真身逐字节自验。
5. **覆盖源常来自「带 data: 前缀的整文件重推」**（本仓 `5adc9c323` 即 `data: index.html 经发布闸门核对`）
   ⇒ 见到此类提交**务必核对其 parent 是否为最近涉及该文件的提交**，parent 隔代 = 高度可疑。

**C. 取证/工具类**
6. **跨境整取必须 `Content-Length` 全长校验 + 重试**；`Accept-Encoding: identity` 显著降低截断。
7. **`python -c` 含引号嵌套会被 bash 吃掉** ⇒ **落 `.py` 再跑**。
8. **Bash PATH 可能整体崩坏**（`dirname/cd/tail/head` 全 not found）⇒ 改用 **Python 绝对路径直跑、不经 shell 管道**。
9. **job 日志 401 的正确解法**：302 跳 Azure 后**必须去掉 `Authorization` 头**；用 `HTTPRedirectHandler` 返 `None` 禁自动跟随。
10. **`tasklist` / `Get-Process` 对 Session0 的 `Runner.Listener.exe` 返空** ⇒ **判 Runner 存活一律用 `wmic`**。

**D. 判据自证类（本轮最多）**
11. 🔴 **`单测绿` ≠ `链路通`**：单测直调函数会绕过接线关卡 ⇒ 必须 **AST 静态提取三表做集合断言**（§二 A）。
12. **`run_time` ≠ 守护心跳**：用「上次干活的时刻」判「人还在不在」＝结构性假阳性（§二 B）。
13. **「全场唯一裸调」是高危指纹**：同 host 其他模块都走统一重试封装且全成功，独它裸调 ⇒ 单点拖死。
14. **断言写错会伪装成「代码有 bug」** ⇒ **先自证断言/桩，再怀疑产物**（本轮踩 3 次：page 名 vs id、`name:'` 计数、测试桩计数）。
15. **逆运算自检必须对称**：正则捕获范围须与替换串严格互补，否则反向替换补两遍后缀（假失败）。
16. **「源码计数 ≠ 线上计数」先找加和关系**（本轮 329 + 51 = 380 精确闭合），不要直接判矛盾。

**E. 环境/时序类**
17. ⚠️ **系统注入的 `<current_time>` 可能偏快/偏慢**，而**本机时钟与 GitHub `Date` 头一致**
    ⇒ **判写仓窗口（盘中禁推 09:15–11:30 / 12:45–15:00）必须实取**，不能信注入时间。
18. **闸门的「盘中」定义是 `09:00-11:30` + `13:00-15:00`，比 v8 铁律的 09:15 更严**（多「开盘前缓冲」）⇒ **以闸门输出为准**。
19. **`schtasks /query /xml` 重定向后编码变 UTF-8 而声明仍写 UTF-16** ⇒ 必须显式 UTF-16（带 BOM）写回；改触发器**逐项断言关键属性未丢**。
20. **禁用词自校验需汉字边界断言**（「判断**更**结构化」命中「断更」）。

**F. 自动化治理类**
21. ⚠️ **ACTIVE 从 120 → 126、PAUSED 从 21 → 24**（今日各增）⇒ **注意 token 消耗与运行历史堆积**，
    建议复查是否可合并/下线（参见 skill `automation-token-diet`）。
22. **本仓「等 tip 稳定」永不可能**（CI 每 ~10s 一 commit）⇒ **推送直接推、non-ff 就重试**，勿写 tip 稳定前置检查。

---

## 六、小九监督职责（白班 7:45–17:45）

- **盘中窗口守护**：实时卡 20 分钟一档；断档 >45min 判故障 → 定位占用 run → 必要时 cancel 释放并发槽（今日 §二 E 即此职责实战）。
- **死锁巡检**：5–7 链采样，超 90min 未完成 ⇒ 先证真活，僵尸才 cancel；`>3h 补派`条款仅 08:00–23:00 适用。
- **健康看板守红**：`HEALTH_CHECK` fail/limited 逐项定性（真故障 vs 设计内空档 vs 结构性假阳性），**不改红线、只上报 + 根因修**。
- **runner 守卫**：20 分钟级 `v8_runner_guard.py --heal --push --alert`；连续失败告警。
- **发布闸门**：`data/*.js` 推 main 前过结构/字数/维度核对；盘中（09:00–11:30/13:00–15:00）拦截、staging 到休市窗口自动发布。
- **机构研究出稿**：08:45 刷新 + 08:50 出稿 + 闸门核对（今日 §二 D 已把刷新触发器补到 08:45）。
- **分红监督**：云端分红步是否真产出（今日 §二 H + §四 第 3 条）。
- **交接**：每日 12:00 / 18:00 两档自动生成并推送本档。
- **纪律**：**只读不写**阿狸咪活跃写入的数据/任务；**推仓前必做 `--expect-base` 断言**；**红线文件（`index.html`）只登记不擅动**。

---

## 七、现取命令（本档不写死 sha，按需实取）

```bash
cd E:/qs_workspaces/quant-scanner-v8
git fetch origin --depth=1
git log --oneline -5 origin/main                      # 当前 tip
git log --oneline -3 origin/main -- HANDOVER_小九_2026-09-22.md   # 本档自身提交
git log --oneline -5 origin/main -- index.html        # index.html 近期改动（查覆盖）
```

**远端真身取值（大文件必走 blobs 端点）**：
```bash
# sha + size
curl -s -H "Authorization: Bearer <TOKEN>" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/index.html?ref=main" \
  | python -c "import sys,json;d=json.load(sys.stdin);print(d['sha'],d['size'])"
# 全长内容（带断言）
curl -s -H "Authorization: Bearer <TOKEN>" -H "Accept: application/vnd.github.raw" \
  -H "Accept-Encoding: identity" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/blobs/<BLOB_SHA>" -o /tmp/idx.html
```

> **token 位置**：`C:/Users/Administrator/qs8-tmp/.workbuddy/v8_gh_token.txt`（**严禁写入仓内任何文件**）。

---

## 八、今日一份关键关联文档索引

| 文档 | 说明 |
|---|---|
| `docs/ops/handover/2026-09-22_0550_小九给阿狸咪_健康看板全量重建.md` | 看板全量重建（commit `cba766889b70`） |
| `docs/ops/handover/2026-09-22_0910_小九给阿狸咪_机构研究卡素材陈旧根因修复.md` | 机构研究卡根因（commit `f9612d1d10f6`） |
| `…三条红灯一劳永逸修复.md`（§9 补记） | 三条红灯 + 假修复诚实记录（commit `9b8667e917e0`） |
| `29bbc5e3c docs(handover): 盘中链断档 + 自动刷新链断裂 双修复交接（2026-09-22）` | §二 E/F 的交接 |
| `docs/ops/HANDOFF.yaml` | **唯一状态源**（防重复劳动，动手前先查） |
| `AGENTS.md` / `.codebuddy/CODEBUDDY.md` | 仓内入口索引（动仓前必读） |

---

*小九 · 2026-09-22 12:00 槽位 · 本档为 12:00 档，18:00 档将整份覆盖为最新状态。*
