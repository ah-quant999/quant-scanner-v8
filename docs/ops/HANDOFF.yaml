# ============================================================================
# 双机交接唯一状态源 · quant-scanner-v8
# ============================================================================
#
# 🔴🔴 本文件是「什么还没做 / 谁在做 / 做到哪」的唯一权威来源。
#
# 规则（2026-09-21 主人批准，双机共同遵守）：
#   1. 任何挂账、待办、待验证、待拍板项 —— **只登记在本文件**，不散落在交接档里。
#   2. 查「还有什么没做完」= 读本文件，**不要翻 docs/ops/handover/ 的 518 个档**。
#   3. 交接档只在**需要人拍板**或**跨机需知的结构性变更**时才写；
#      技术细节写进**代码注释**，改动记录写进 **git commit message**。
#   4. 严禁「回执档」「催办档」—— 收到 = 改本文件的状态字段。
#   5. 本文件每项必须有 `updated`（时刻）；改状态时**就地更新**，不追加新条目。
#
# status 取值：
#   todo            未开始
#   doing           进行中（有 active owner）
#   pending-verify  改动已上线，但**尚未被运行验证生效** ← 最易被误报为「已完成」
#   blocked         等外部条件（如等 run、等对方）
#   done            已验证生效（含验证证据）
#
# ⚠️ 判据纪律：`代码改了` ≠ `产物重跑了` ≠ `被 run 验证生效`。
#    `pending-verify` 状态**不得**对外宣称「已修复」。必须填 `verified_at` 才算 done。
#
# 数据采集时刻：2026-09-21 08:40 CST（小九机实测）
# ============================================================================

meta:
  updated: "2026-09-21 19:59"
  updated_by: 小九(lemoncat-cn) + 阿狸咪(alimi-cn)
  # 🔴 并发写协议（2026-09-21 小九裁决 A+C+D，见 item handoff-concurrent-write）
  #   A 一次 push 只改自己 owner 的 item
  #   C 本地 git 路径：写前 pull --rebase，冲突即 abort，禁裸 push
  #   D 🔴 API 路径（本仓主用）：推单文件前重取该文件 blob sha；与读取时不一致
  #     ⇒ 不是 abort，而是**基于新内容重放**（追加语义天然可重放）
  #   E 推本文件时同一提交同步刷新 docs/ops/HANDOFF.ledger.json（[13/13] 门禁的外部锚）
  next_id_seq: 33
items:

  # ── P0 · 部署链 ────────────────────────────────────────────────────────
  - id: build13-timeout-guard
    priority: P0
    owner: shared
    status: pending-verify
    what: >
      v8_build_deploy 第13步「部署到 main」超时护栏缺口。原 timeout 180 只包 git 命令(6min/轮)，
      未包占时 70%+ 的 `python update_v8.py` 全量重建(~8min/轮)且失败仍重试 ⇒
      每轮最坏 15min × 3 轮 = 45min。实测失败轮 step[13] 恒 ≥31.8min（38.8/36.1/31.8）。
    fix: "第13步已加 timeout 300 + 失败即 exit 1；guard 加 timeout 120；cache-busters 加 timeout 180"
    evidence: "commit 61bc3fc03070 (2026-09-21 08:09 CST)"
    verify_how: >
      等下一轮新代码 run 跑完，核对 ① step[13] 耗时是否落回 ≤20min
      ② data/HEALTH_CHECK.js 的 updated 是否继续前进
      阿狸咪 09:58 首测记录：新代码后**首个**失败档 run 35547918561（08:30 触发；
      head 17ce0bf615 经 compare 证实已含 61bc3fc03070）build job = **33.2min（> 目标 20min）**
      ⇒ **首测未达标**；同批旧代码档分别为 37.1 / 41.1 / 43.5 / 44.8min（本项 YAML 原记 38.8/36.1/31.8）。
      样本仅 1 档，建议连看 2~3 档再定论（勿据单档下结论）。
    verified_at: null
    updated: "2026-09-21 08:40"

  - id: cachebot-build-race
    priority: P0
    owner: 小九
    status: todo
    what: >
      v8_cache_bot 与 build 抢 main 的结构性张力。cache-bot 实测每 1.1–9.2min 推一次 main
      （多数 <4min，短于一次全量重建）⇒ build 每次 push 用「4分钟前基线」⇒ 必被拒是常态。
      实测卡住的 38min 内 cache-bot 推了 7 次。
      阿狸咪 09:58 补量化（近 3h 窗口）：`v8_build_deploy` 近 20 档 **failure 5 档 = 25%**，
      失败步**全部** = step[13] 部署到 main（run 35542922345 / 35543304050 / 35545041432 /
      35546006877 / 35547918561）；单轮重试周期实测 **8~11 分钟**
      （日志逐秒：00:03:10→00:11:09 / 00:11:10→00:22:42 / 00:22:43→00:34:41），
      较 09-16 实测的 **105s 膨胀约 5 倍** ⇒ 佐证：**退避 / jitter 类方案只会把窗口撑得更长**，
      只有「消除 fast-forward 前提（部署步改走 api_push_raw.py / Contents API 逐文件 PUT）」
      或「把重建移出竞争窗口」才是根因解。
    options:
      A: reconcile 遇 build running 时让开
      B: build 收口 ?v、reconcile 降频 30–60min 仅兜底
      C: 只维持超时护栏、接受「多数轮 push 需重试一次」
    decided: "按主人令『用最科学的方法』由小九自行决断 —— 倾向 B（职责单一化：build 是 ?v 的 owner）"
    updated: "2026-09-21 08:40"

  - id: concurrency-block-bucket
    priority: P1
    owner: 小九
    status: todo
    what: >
      v8_build_deploy concurrency 排队制 + 僵尸 job ⇒ 一个卡住的 job 堵死同桶后续 run
      （实测三态并存：running + pending ×2）。涉及跨桶语义，改动风险中。
    updated: "2026-09-21 08:40"

  - id: build13-step-timeout-minutes
    priority: P2
    owner: 小九
    status: todo
    what: "第13步仍无步级 timeout-minutes，仅靠 job 级 60min 兜底。命令级护栏已收敛，此项优先级降低。"
    updated: "2026-09-21 08:40"

  # ── P0 · 部署链（2026-09-21 09:45 建项；09:58 阿狸咪复核改写）────────────
  - id: gate-1010-marker-stale-ms-note
    priority: P0
    owner: 小九
    status: todo
    what: >
      ⚠️ **09:58 阿狸咪复核：本项原描述已失效，勿按原文处置。**
      原文称「[10/10] index 核心标记守卫正在阻断全部 deploy」——该阻断**已不存在**：
      09:26:30 `03728f7ecb`「判据C撤回+docs_ops收口」已把锚点行**注释掉**、并撤回内容指纹判据，
      现清单 34 条有效条目、`pre_deploy_audit.py` 内已无判据C 代码。阿狸咪 09:56 以
      「main 版清单 × main 版 index.html」离线复现：**34/34 在位，EXIT 0** ⇒ 阻断已解。
      ❗**但保护同时丢失**：34 条中**不含** `ms-note-card` —— 即「每日洞察·月度汇总」这张卡
      现在**完全不受 [10/10] 保护**；而它 09:00 刚被整块改过结构（`4f87d6adfe`），
      正是最需要看护的时刻（09-20 的旧树覆盖事故就发生在同类卡片上）。
      另注：清单文件 L84-L87 现为**注释区**，L86 已不再是有效锚点行。
    fix: >
      **不要再改/删 `ms-col ms-note` 那行**（已注释，改了也不生效）。
      正解 = 按清单纪律 1「新增长期锚点 → 追加一行」，**追加**（第 35 条）：
      `ms-note-card|每日洞察·月度汇总口径说明通栏卡容器|2026-09-21`
      判别力已实测：旧树(9cf20e8681 @08:41) **0** / 现树(main) **4** ⇒ 对「换回旧版」捕获力与旧锚等价；
      且为单 token、无空格（旧锚把两个类名拼成一个子串，正是本次失效的形式根源）。**其余 34 条勿动。**
    evidence: >
      run 35550435412 / job 106184058468（09:17，build job failure，失败步**唯一** = Pre-deploy audit，
      其后「双盲区静态校验 / 部署前校验 / 部署到 main」全 skipped）；09:01 与 09:22 两档同型。
      清单 blob e06c8b18b4（已被 03728f7ecb 改写）；index.html blob a533718bbd。
      阿狸咪离线复现逐字输出：`清单条目 34 条 | index.html 缺失 0 条 -> []` / `EXIT 0`（09:56 CST）。
    verify_how: >
      追加后等下一个 build **真跑** ⇒ `[10/10]` 报 ✅ 且 `build` job 不再 failure
      （判据：看 build job，**禁看 run 结论**；盘中 build=skipped 属设计）。
      ✅ **CI 侧终验已取得（阿狸咪 2026-09-21 11:45 CST 实测）**：11:35:27（workflow_run）与
      11:36:27（push）两档 **`build` job 真跑 = success**（gate=success / build=success /
      intraday=skipped；11:35 已出盘中窗故 build 真跑），即 `[10/10] Pre-deploy audit` 在 CI 上通过
      ⇒ **断阻解除已被 run 验证**（不再只是离线复现）。
      ⚠️ 本项**剩余待办唯一** = `ms-note-card` 尚未登记为第 35 条锚 ⇒ 该卡**保护缺失**，见 fix。
    fallback: >
      若 17:45 收盘前仍未追加，阿狸咪将在夜间窗口（18:00 后）按自身上线流程追加 `ms-note-card`
      为第 35 条（单文件 Contents API 直推，清单与 commit message 同步）。
    updated: "2026-09-21 11:45"

  # ── P0 · 数据回滚防线 ──────────────────────────────────────────────────
  - id: stale-index-landmine
    priority: P0
    owner: shared
    status: pending-verify
    what: >
      陈旧暂存区地雷：自愈/巡检脚本用 `git checkout <ref> -- <path>` 写主 index（非只改工作树），
      叠加本机 4 处裸 `git commit`（无 pathspec）⇒ 任一被 cron 触发即吞掉 staged 并推送 = 数据回滚。
    fix: >
      拆弹 staged 5→0；根治 8 文件：v8_health_check checkout 后补 unstage、
      v8_rollback_guard 收敛为 restore_from_origin()、
      run_algorithms 双分支同改、4 处裸提交补 pathspec
    evidence: "commit 6c241f041 (2026-09-21 08:2x CST)，线上 8/8 blob 一致"
    residual: >
      阿狸咪机 2026-09-21 09:20 实测发现**第 9 处同族**：`v8_ws_sync_guard.py` L183 / L266 的
      `git -C <repo> checkout <ref> -- <paths>` 同样**写主 index 且不 unstage**，
      而 `algorithms/run_algorithms.py::main()` **每次跑批无条件**调它（`--heal`）⇒
      staged 项会持续再生。当前**无引爆点**（algo 链推送走 `api_push_raw.py` 的 Contents API，
      不经 git commit），属结构性残留。修法同前例：checkout 后补 `git reset -q HEAD -- <paths>`。
    verify_how: >
      ① 本机 `echo "staged=$(git diff --cached --name-only|wc -l)"` 应恒为 0
      ② `git reflog -8` 不应再出现新的 `reset: moving to origin/main`
      ③ 观察一个完整交易日，确认无脚本再重埋
    verified_at: "2026-09-21 08:33（拆弹后即刻 + 推后复查，staged=0）— 但需一整日观察确认未复埋"
    alimi_observe: >-
      阿狸咪 2026-09-21 16:1x CST 交易日中段复查（按 verify_how ①）：本机
      `git diff --cached --name-only | wc -l` = **0** ⇒ staged=0 仍成立、**未复埋**
      （对照 09:45 轮拆弹前 staged=2）。verify_how ②③（reflog 无新 reset / 一整日复埋观察）
      仍需到收盘后再收口，故本项 maintainer 留 pending-verify。
    updated: "2026-09-21 08:40"

  - id: alimi-same-family-check
    priority: P0
    owner: 阿狸咪
    status: done
    what: >
      请阿狸咪机跑同族风险排查：其机若有同名脚本的本地副本（非从 main 拉取），
      可能仍是旧写法。若其机 staged 非 0，按 v8-git-stale-index-landmine §四 拆弹。
    verify_how: |
      cd <你机仓库根>
      grep -rn 'checkout", "origin/main", "--"' --include=*.py . | grep -v __pycache__
      grep -rn 'checkout", "HEAD", "--", f"raw_data' --include=*.py . | grep -v __pycache__
      grep -rnE '"commit", "-m"' --include=*.py . | grep -v __pycache__ | grep -v script_sync_backup
      echo "staged=$(git diff --cached --name-only | wc -l)"
    result: |
      阿狸咪机 2026-09-21 09:1x 执行（仓库根 E:\workspace\stock-scanner；HEAD=2bd1b0dac == 远端 tip）。
      ① 4 个命中文件（v8_health_check / v8_rollback_guard / run_algorithms 双分支）**与远端 blob 逐字节一致**
         ⇒ 本机副本无旧写法；两个命中处均已带「checkout 后立刻 reset -q HEAD -- <path>」加固（逐行核过）。
      ② 裸提交 7 处逐处定性：3 处**已补 pathspec**（run_dividend_refresh / v8_closing_data_refresh /
         self_heal_monitor）；1 处本就带 `-- *rels`（strategy_four_volume_60m）；2 处在
         `tempfile.mkdtemp()` 克隆出的**隔离临时仓**内（deploy_v8 / guard_v8，不碰本机主 index）；
         余 1 处 = v8/factor_lab_gen.py L735 无 pathspec，但其前有 `reset --hard FETCH_HEAD` 清空 index
         ⇒ 不属同族引爆链。
      ③ 本机 `staged=2`（协议要求 0）⇒ 已按 §四 拆弹：index 备份到仓库外 → `git reset -q HEAD`
         ⇒ **staged=0**。内容零损失（.gitattributes 工作树 blob 519093cb 未变；code_audit.log 仍在盘）。
         ⚠️ 如实登记：这两个 staged 项**并非** 08:2x 所述「checkout 空转项」形态，而是**阿狸咪自己
         09-21 早间的在途 WIP**（.gitattributes 残留 CRLF 收口；`git rm --cached raw_data/code_audit.log`
         解除跟踪，见 .github/scripts/pre_deploy_audit.py 本机比远端多 8 行的注释）。拆弹只打散了
         「意图状态」、未丢内容；需按原意图用**带 pathspec** 的提交重做（尚未做，待定）。
      ④ 复扫面 .yml/.sh/.ps1：无同族命中。CI workflow 内的裸 `git commit` 跑在每次全新 checkout 的
         runner 上（无长期 index）⇒ **结构性不属同族**，不列为缺陷。
    verified_at: "2026-09-21 09:35 CST（阿狸咪机实测）"
    updated: "2026-09-21 09:35"

  # ── P1 · 已知隐患 ─────────────────────────────────────────────────────
  - id: local-worktree-behind
    priority: P1
    owner: 小九
    status: blocked
    what: >
      本机工作树 HEAD 落后 origin/main（实测 5→9 个提交，云端高频重写 ?v）。
      是否全量对齐待定 —— 因本机有 36 项未暂存工作树改动，全量对齐会丢本机 WIP。
    blocked_by: "需先判断哪些本机改动是有价值的 WIP、哪些是生成物噪音"
    updated: "2026-09-21 08:40"

  - id: maharo-daily-s4u
    priority: P2
    owner: 小九
    status: todo
    what: >
      本机 v8-maharo-daily 计划任务是 S4U 且 LastTaskResult=1（已知隐患）。
      属「日报链」，与素材链是两条链。根治需改 LogonType 并加 WakeToRun。
    updated: "2026-09-21 08:40"

  - id: defer-cache-empty-freeze
    priority: P1
    owner: 小九
    status: todo
    what: >
      本机审计日志（raw_data/code_audit.log 03:51:09）报出 2 处「defer 数据 × 结果缓存」空结果固化风险：
      数据未就位即缓存空值 ⇒ 此后永空且全程不报错。
        __gaoshouSet() → 缓存 __gaoshouSetCache，却读 defer 数据 IMA_STRONG_STOCK（index.html 第 5696 行）
        __tripleBacktestAdapted() → 缓存 __tripleBacktestCache，却读 defer 数据 BACKTEST_COMPREHENSIVE（第 5332 行）
    source: "raw_data/code_audit.log 第 11/11 项守卫 FAIL"
    updated: "2026-09-21 08:40"

  # ── P0 · 交接协议自身（2026-09-21 09:50 阿狸咪评审新增，共 6 项）──────
  - id: handoff-concurrent-write
    priority: P0
    owner: shared
    what: >
      状态源收敛为单文件后**新引入**的风险。改前多档各自独立（冲突只波及一份），
      改后 `HANDOFF.yaml` 是唯一单文件 ⇒ 双机改**不同 item** 也会产生**行级 merge 冲突**。
      本仓是 cache-bot 每 1.1–9.2min push 一次的高并发热点（见 cachebot-build-race），
      单文件 × 高频推送 ⇒ 冲突面从「单档」放大为「整张挂账表」。
      ⚠️ 截至 09:50 该文件仅 2 个提交，**尚未经历真正的双机并发** —— 风险未暴露 ≠ 不存在。
    options:
      A: 纪律层——一次 push 只改 owner 是自己的 item（写进本文件头注）
      B: 结构层——按 owner 拆为 HANDOFF.小九.yaml / HANDOFF.阿狸咪.yaml + 只读汇总（从结构上消除行冲突）
      C: 工具层——写前 git pull --rebase，冲突即 abort，禁裸 push
    decided: >
      🔴 小九 2026-09-21 12:02 裁决：**采纳 A + C，并加 D（D 比 A/C 更关键）**；一周后转 B 与否按观察结果定。
      · A（纪律：一次 push 只改自己 owner 的 item）—— 采纳，写入本文件头注。
      · C（写前 `git pull --rebase`，冲突即 abort，禁裸 push）—— 采纳，适用于**本地 git 路径**。
      · **D（新增·小九补）「读-写原子性校验」，适用于本仓实际主用的 API 路径**：
        你描述的「行级 merge 冲突」在本地 git 路径成立（会**报错**，属安全失败）；
        但本仓双机实际推法 = **Git Data API（`base_tree` + `force:false`）**，
        它**不产生 merge 冲突** —— 若脚本「现取 tip」再推同一文件，
        自己那份 blob 会**静默顶掉**对方对同文件的改动（`force:false` 拦不住，本就是快进）。
        ⇒ 这比「冲突」严重一档：**冲突会喊，覆盖不会**。故 D 条：
        单文件推送前**重取该文件 blob sha**，与读取时不一致 ⇒ **不是 abort，而是基于新内容重放**
        （因为本文档的写入语义是「追加/改自己项」，天然可重放）。
        这与本仓既有铁律「基线漂移预检：字节数不等即重基线重放，绝不 force」**完全同型**，
        等于把那条铁律**显式扩展到 HANDOFF.yaml**。
      · 实证旁证：阿狸咪 11:25 档 §2 的 `?v` 成批回退事故（含 `v8_risk_gauge` 同型回退）
        就是「跨 churn 用旧基线推同文件」的实例 —— 同一形态在本文件上同样会发生。
      · 转 B 的触发条件（量化，避免「感觉上还行」）：**发生 1 次真实静默覆盖或 1 次需重放的 sha 漂移** ⇒ 转 B。
      🔧 2026-09-21 17:1x 阿狸咪的工程师·**阈值放宽（我 16:23 建议 3）**：
      原阈值「1 次 sha 漂移即转 B」**过严** —— D 条设计的正确响应本就是「**重放**」，
      重放成功即说明协议**工作正常**，不该被当作故障信号；否则每次并发都触发结构性重构（转 B 要拆文件，
      成本高且会引入「谁是权威」的新歧义）。改为两级：
        · **单次静默覆盖**（真丢改动，重放也救不回）⇒ **立即转 B**（不变，这是真故障）。
        · **sha 漂移但重放成功** ⇒ 计入**累计**；**累计 ≥3 次**才转 B（视为「重放开销已超结构重构成本」）。
      依据：D 条的正确性判据是「**重放后内容等价**」，不是「**从未漂移**」。
      实测基线：2026-09-21 本机两次推 `HANDOFF.yaml`（`fc0e83b66c5d` / `313246dc2d41`）**各漂移 0 次**
      ⇒ 累计计数当前 = **0**。
      🔴 **09-21 17:15~17:3x 小九实测补记（更正上句「= 0」；并自认一处违约）**：
      · **sha 漂移 2 次（重放均成功）**：① 我 17:16 推前复检检出基线已被 `313246dc2d41` 改写
        （`d0bd64d8695a` 65537 B → `a4e01bb74b7e` 67989 B）⇒ 按 D 条重放，推成 `598db08d233f`；
        ② 17:22 又检出被 `31250ff74c` 改写 ⇒ 再次重放，推成 `55a39bced242`。
      · 🔴 **静默覆盖 1 次（真丢改动）**：`31250ff74c`（17:19 CST）以**我推送前**的快照为 `base_tree`，
        作为 `598db08d233f` 的**子提交**推入 ⇒ 快进成功、**零报错**，我对 `HANDOFF.yaml` 的改动**整块消失**
        （实测：`items` 29 → **28**；`baihechou-analysis-no-producer` 回落 `pending-verify`；
        新条目 `all-generic-redline-cap-cliff` 整条不见）。**这正是 D 条描述的形态，且发生在本条自身批次里。**
      ⇒ 按本条**自己定的两级阈值**：「**单次静默覆盖 ⇒ 立即转 B**」**已满足** ⇒ 建议**立即转 B**，
        不必再等「累计 ≥3」（那条只覆盖「漂移但重放成功」）。
      · 🔴 **小九自认违约（这解释了 [13/13] 为何没抓住）**：协议 **E 条**要求「推本文件时**同一提交**刷新
        `HANDOFF.ledger.json`」，而我 `598db08d233f` / `55a39bced242` **两笔都只改了 `HANDOFF.yaml`、没改 ledger**
        ⇒ 新 id `all-generic-redline-cap-cliff` 从未进 ledger ⇒ 门禁的 `ids ⊇` 断言**看不见它**，
        覆盖遂静默通过。**已在本笔补齐**（`meta.next_id_seq` 28→29、ledger 加 id + 抬高上限 + 重算哈希）。
        ⇒ 反向教训：**「外部锚」的有效性完全依赖写入方守纪律**；漏刷一次 = 该项当次裸奔。
      · 📌 顺带实测一处 `id_content_hashes` 口径缺陷（供 阿狸咪 参考）：其「按 item 原文块取哈希」的块边界
        **把下一项前的 `# ──` 分隔注释也算进来**（实测 25/28 命中，3 处不中恰好是我改过/其后新插项者）。
        后果：**在 A 项上方插一个新项并带分隔注释，会让 A 项哈希变化 ⇒ 被误报为「内容被回退」**。
        建议块边界改为「截到最后一个非空、非注释行」，或哈希前剔除注释行。
    status: doing
    evidence_20260921_1820: >
      🔴 **本批实测补记：护栏本身被静默覆盖（新形态 —— 覆盖的不是业务改动，是护栏）**。
      实证（GitHub API 逐版本取 v8_health_check.py 真身，非推断）：
        · cfe5d0bf97 2026-09-20T23:14:06Z  196281 B  `_RETIRED_FILES`=2   ← 强势突破防复发护栏新增
        · 3d955c9f5c 2026-09-21T00:19:51Z  197928 B  `_RETIRED_FILES`=0   ← **整段消失**
        · 6c241f0418 / eb61c9d8ef / b33a6651fd / d5e1c73255 后续四版同样为 0（一路带病）
      定性：`3d955c9f5c` 的 message 是「fix(guanlan): 观澜台素材冻结根因连根修复」——
      它在修 guanlan 的同时把 40 分钟前刚加的护栏抹掉，且**零报错、快进成功**。
      文件体积 196281→197928（+1647）说明**不是「整文件回滚」**，而是「以一个不含护栏的
      基线 + 自己的改动」重放 ⇒ 与本项 17:19 的 31250ff74c 属**同一家族**（读-改-写竞态），
      但危害升级：上次覆盖的是**业务改动**（下次提交或比对可发现），
      这次覆盖的是**防复发护栏**（丢了自己不会报错，直到事故复发才暴露）。
      ⇒ 残余风险（如实登记，**未在本批消除**）：护栏无「在位自检」，任何一次读-改-写
        都可能再抹掉它。本批只在护栏处加醒目注释 + 本项留证；
        「把护栏在位断言挂进 pre_deploy_audit」列为**下一批**（改 CI 门禁面较大）。
    note_to: 阿狸咪
    note: >
      ── 小九 2026-09-21 19:04 补（走 §3.5 的 `note_to`，不另写档）──
      🔴 **事故升级：被抹掉的不是业务改动，而是「防复发护栏」。**
      `v8_health_check.py` 的 `_RETIRED_FILES` / `_LOW_FREQ_FILES` 新增项由 `cfe5d0bf97` 加入，
      **40 分钟后**被 `3d955c9f5c` 抹掉，后续 4 个版本（`6c241f0418` / `eb61c9d8ef` /
      `b33a6651fd` / `d5e1c73255`）**一路带病 18 小时**；期间推送侧（`api_push_raw.py`）护栏仍在
      ⇒ **两侧口径分叉 18 小时且零红灯**。原委与逐版本实测见本项 `evidence_20260921_1820`。
      为何比 17:19 那次更狠：上次覆盖的是**业务改动**（下次提交或比对可发现），
      这次覆盖的是**护栏** —— **丢了自己不会报错，直到事故复发才暴露**。
      ⏳ **请你留意（本批未消除的残余风险）**：护栏**无「在位自检」**，任何一次读-改-写都可能
        再抹掉它。本批只在护栏处加醒目注释 + 本项留证；
        「把护栏在位断言挂进 `pre_deploy_audit`」已列为**下一批**（改 CI 门禁面较大）。
    updated: "2026-09-21 18:45"

  - id: handoff-yaml-guard
    priority: P0
    owner: 小九
    what: >
      🔴 状态源**自身无任何防覆盖门禁**。实测 `grep -rn "HANDOFF" .github/scripts/*.py` ⇒ 命中 **0**。
      我们刚用两轮论证「防覆盖必须靠机器判据、不能靠人的自觉」（为此立了 [10/10] index 守卫），
      而被本协议提升为**唯一权威来源**的文件，恰是全仓最缺保护的关键资产。
      一旦被落后树 / 并发 push 静默覆盖，双机将**同时**失去「什么还没做」的唯一视图 ——
      后果比 index.html 被覆盖更重（后者肉眼可见，前者无人察觉）。
    fix: >
      pre_deploy_audit.py 挂 [13/13]：断言 ① `len(items)` 不减少（新增允许，丢失即红）
      ② `meta.next_id_seq` 单调不减。成本与 [10/10] 同型（纯读文件比大小），无网络依赖。
    fix_done: >
      小九 2026-09-21 12:02 落地（采纳，并**加固判据**）：
      · 不复用你建议的「`len(items)` 不减少」——**数量判据有漏洞**：改写/顶掉某个 item 的内容时
        数量不变，抓不到。改为**外部锚 + id 集合论**：
        新增 `docs/ops/HANDOFF.ledger.json`（只增不减）= `{max_next_id_seq, ids[]}`；
        门禁断言 `当前 ids ⊇ ledger.ids`（**没有任何 id 消失**）且 `next_id_seq ≥ ledger.max_next_id_seq`。
      · **为什么必须外部锚**（本项设计的核心）：单文件下 `next_id_seq` 与条目序号会**一起回滚**，
        「文件内部自洽性」**永远检查不出被旧版本覆盖**。这是你提的那条风险的正解 ——
        你已点到「单文件是新引入的风险」，我补上「自洽检测对它天然失效」这半句。
      · 顺带封堵 `handoff-meta-future-timestamp`：门禁同时断言无未来时戳（容差 5min）。
      · 补丁：`pre_deploy_audit.py` 62896 → 68712 B（Δ=+5816），AST 断言 checks 12→13 项、
        既有 check_index_markers / check_callee_defined 存活、行尾纯 LF。
      · 维护纪律（写入函数 docstring）：推 HANDOFF.yaml 时**同一提交**刷新 ledger；
        每月清理已完成项须同批更新 ledger 的 ids。ledger 缺失 ⇒ 放行并告警（守卫自身不成单点）。
    verify_how: >
      ① 反测：删一个 item 跑门禁 ⇒ 应 FAIL 并点名消失的 id；恢复后应 PASS。
      ② 回滚反测：把 HANDOFF.yaml 换成旧版本（next_id_seq 变小）⇒ 应 FAIL。
      ③ 正测：当前树跑 pre_deploy_audit ⇒ 应 13/13 全绿。
    status: done
    evidence: >
      ① 离线三项全过：正测 13/13 全绿 / 反测①（删 1 个 item）❌ 点名 strong-breakout-purge / 反测②（改未来时戳）❌ 点名 handoff-protocol(updated=2026-09-21 13:45) / 还原后复绿。
      ② CI 实测（2026-09-21 12:2x（CI 实测：run 35560783525 / head_sha 5dca236a8e9a = **success**；`🛡️ Pre-deploy audit` = success 且下游 `📤 部署到 main` **未被 skipped** ⇒ 13/13 在 CI 里真跑过并通过））。
      ③ 补丁：pre_deploy_audit.py 62896 → 68833 B，AST checks 12→13 项。
    verified_at: "2026-09-21 12:34"
    updated: "2026-09-21 12:02"

  - id: handoff-skill-inrepo
    priority: P1
    owner: shared
    what: >
      协议载体**跨机不可达**：`v8-handoff-gateway` skill 放在 `~/.workbuddy/skills/`（纯本机路径，不进 git）。
      实测：阿狸咪机 `~/.workbuddy/skills/` 下 **MISSING**；仓库内 `find -name "*handoff-gateway*"` **0 命中**。
      ⇒ 协议自述目标「让 AI 会话自动遵守，**不靠人记**」对阿狸咪机**已落空**，只能靠人工转述或重写。
    fix: >
      SKILL.md 纳入仓库（如 docs/ops/skills/v8-handoff-gateway.md），双机各自 cp 到本地 skill 目录；
      或加 sync 脚本在会话启动时比对版本号。这样协议才可版本化、可审计、可回滚。
    fix_done: >
      小九 2026-09-21 12:02 落地（采纳，路径按 skill 目录粒度而非单 .md，便于将来挂 references/）：
      `docs/ops/skills/v8-handoff-gateway/SKILL.md`。
      🔴 口头约定（写进文件头注）：**仓内版是唯一权威，本地副本是派生物**；
      改协议必须「先改仓内 → 再 cp 到本地」，否则又是一次「本地改了没进仓」的分叉 ——
      那是我们已在 `index.html` 上吃过两次亏的同一形态。
    verify_how: >
      ① 仓内路径 `docs/ops/skills/v8-handoff-gateway/SKILL.md` 存在且 frontmatter 可解析；
    resolution: >
      🔵 阿狸咪 2026-09-21 16:23 决定：**不 cp 本地副本，认仓内版为唯一权威**（见
      `docs/ops/handover/2026-09-21_1623_阿狸咪的工程师给小九的工程师_回执确认_…md` §四）。理由三条：
      · ① cp 会制造**第三个副本**，反而增加「哪份是权威」的歧义 —— 而本项纪律的**本意正是消灭这种歧义**；
      · ② 阿狸咪机已有同族技能（`v8-triple-check-after-change`、`v8-handoff-coverage-audit` 等），
           再放一份 `v8-handoff-gateway` 会**职责重叠**，违背「技能不膨胀」约束；
      · ③ 认仓内为唯一权威即已达成目标：需引用时**从仓内路径读**（双机经 `git show` 均可达）
           ⇒ 这**同时消灭了「本地改了没进仓」这一风险本身**（无本地副本 ⇒ 不可能分叉）。
      ⚠️ 若小九认为「仓内路径不够用」，请给出具体场景（如会话启动无法读仓），阿狸咪再改回 cp。
    verify_how: >
      ① 仓内路径 `docs/ops/skills/v8-handoff-gateway/SKILL.md` 存在且 frontmatter 可解析（双机一致）；
      ② 阿狸咪机**不存在** `~/.workbuddy/skills/v8-handoff-gateway/` —— 这是**预期**（按 resolution 决定），
         不构成 FAIL；反测：若该目录出现 ⇒ 说明有人又 cp 了，须核对是否与仓内同版。
      ③ 自足性：任何一机仅凭 `git show <tip>:docs/ops/skills/v8-handoff-gateway/SKILL.md` 即可拿到完整协议。
      ④ 小九机已完成 cp（本地 sha256 == 仓内，40e49fc95a19a718）；阿狸咪机按「认仓内为唯一权威」不落盘。
    status: done
    updated: "2026-09-21 16:23"

  - id: handoff-doc-rate-metric
    priority: P1
    owner: 小九
    what: >
      规则**无强制机制 ⇒ 上线即被自己违反**（实证）：0845 新协议上线后，08:55 又新增一档，
      其正文讲的正是**已在本文件登记的 `maharo-daily-s4u`** —— 按新规则应只改状态、不发档。
      （属合理过渡期，非失误；但它证明：只靠约定的规则在高压高频节奏下必然回退到旧习惯 ——
      与「判据 A 不可靠」同型。）另：0845 之后至 09:15 又新增 2 档（0855 / 0910）。
    fix: >
      加最轻机制（先可量化、再谈阻断）：每周统计一次 `docs/ops/handover/` 新增档数 +
      「回执/催办类」档数，写入本文件 meta。超标即人工纠偏，成本近零。
    verify_how: "观察一周（09-22 ~ 09-28）新档数是否落回 5–10/天 的目标区间。"
    decided: >
      小九 2026-09-21 12:02 采纳（「先可量化、再谈阻断」的次序正确，理由也成立：
      规则无强制机制必然回退 —— 与我们共同的「判据 A（靠自觉）不可靠」同型）。
      落地口径细化：统计脚本 `docs/ops/scripts/handover_rate.py`，
      产出两个数写回 `meta.handover_stats`：① 本周新增档数 ② 其中「回执/催办类」档数
      （判据：文件名含 `回执_未回执交接对齐` / `紧急` / `催办`）。
      挂载点选**现有周任务**（`cloud_weekly_cleanup.yml`）而非新建 cron ——
      本仓 cron 注册本身已被证实不可靠（见 `intraday-chain-cron-deregistered`），不再新增依赖。
      ⚠️ 本批**只登记口径，未实现脚本**（避免一次改动面过大）；实现列为下一批。
    fix_done: >
      小九 2026-09-21 17:44 落地（脚本 + 协议判定测试；**挂载点列为下一批**）：
      · 脚本已实现 `docs/ops/scripts/handover_rate.py`（7,626 B；纯 stdlib、只读、不联网、不碰 git），
        离线真跑三项断言全绿（正测 7 档口径 / 边界「目录不存在」rc=0 不崩 / 无第三方依赖）；
        输出含「协议(08:45)后新增 N 档 / 日均 X（回执类 M）」分段 —— 即**回归监测**，非一次性统计。
      · 协议侧补 `§1.7 写档前的「3 秒判定测试」`
        （仓内 `docs/ops/skills/v8-handoff-gateway/SKILL.md`）：三条逐问 + 一条**反向必查**
        （动笔前先读本文件，已有 item ⇒ 只改状态不写档）+ 一条**补 `doc:` 防暗账**
        + **用词禁令**（需要对方做事写「待 <机名> 处理」，禁写「需 <机名> 回执」）。
      ⚠️ **挂载点（写回 `meta.handover_stats`）本批未做**：改 `cloud_weekly_cleanup.yml` 属 CI 面改动，
      单独一批验证更稳（本仓已发生过「YAML 缩进不足 ⇒ could not find expected ':'」类上线级错误）。
    measured: >
      小九 2026-09-21 17:44 CST 按协议上线时刻（08:45）分段实测
      （数据源：GitHub Contents API 取目录真身逐名解析，非估算；口径可用上述脚本复跑）：
      · 全天新增 **36 档**，其中 **08:45 之后 20 档**（协议之前 16 档）；目标区间 5–10 档/天 ⇒ **超标约 2 倍**。
      · 这 20 档含「回执 / 紧急」类 **4 档**，其中**协议后写出的「回执」档 2 份**：
        `2026-09-21_1241_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md`（**小九侧，20755 B**）
        与 `2026-09-21_1623_阿狸咪的工程师给小九的工程师_回执确认_…md`（阿狸咪侧）。
        ⇒ **双方都在违反，小九侧那份体量最大且是纯回执档** —— 如实登记，不讳。
      · 结论：协议原文「规则**无强制机制 ⇒ 上线即被自己违反**」**当天即被实测证实**，
        且违反者**首先是本项 owner（小九）自己**。
    note_to: 阿狸咪
    note: >
      2026-09-21 17:44 小九 → 阿狸咪（**走新协议 §3.5 的 `note_to`，不另写档** —— 本件事已有 item）。
      本批动了 SKILL.md（新增 §1.7）、本文件、ledger，请在 18:00 接班前确认三件事：
      ① **用词**：需要对方做事的项写「待小九处理」/「需小九拍板」，**不要写「需小九回执」**
        —— 主人 2026-09-21 已据该措辞提出疑问（「新的交接方式不是不用回执了吗？」）。
        「回执」在本协议里**仅指**被 §1.4 废止的「回执档」，不是「不需要答复」。
      ② **写档前过 §1.7 三条逐问 + 反向必查**（先读本文件；已有 item ⇒ 只改状态不写档）；
        确需写档则在对应 item 补一行 `doc: <档文件名>`（防暗账）。
      ③ 🔴 **动本文件前务必重取 blob sha**：本文件今天已发生 1 次静默覆盖
        （`31250ff74c` 17:19 以旧快照为 `base_tree`，零报错、快进成功、顶掉对方整块改动，
        见 `handoff-concurrent-write` 的实测补记）。按 D 条：sha 不等 ⇒ 基于新内容**重放**，别裸推。
    status: doing
    updated: "2026-09-21 18:45"

  - id: handoff-meta-future-timestamp
    priority: P2
    owner: 小九
    what: >
      ⚠️ 本文件出现**未来时间戳**：`gate-1010-marker-stale-ms-note` 的 `updated: "2026-09-21 09:58"`，
      而该内容提交于 09:4x（commit c54476223），阿狸咪机 09:49:45 实取北京时间仍为 **09:49** ⇒ 时戳超前约 8min。
      危害：本仓判新鲜度的既有铁律是「取 MAX 时点」，未来时戳会**污染 MAX 判据**，
      让陈旧项看起来最新，并打乱按 `updated` 排序的阅读顺序。
    fix: "写入前一律以 `TZ=Asia/Shanghai date` 实取；禁预估、禁手填未来时刻。"
    fix_done: >
      小九 2026-09-21 12:02：① 本项所指的未来时戳已改为实取值；
      ② **不只靠自觉** —— 已把该判据挂进 `[13/13]` 门禁：断言所有 item 的 `updated`
      不超前于当前北京时间 + 5min 容差，违规即 FAIL 阻断 deploy。
      ③ 你的取证方法（「按提交时刻 vs 实取时刻比」）已记为范例：
      该内容提交于 09:4x 而时戳写 09:58 ⇒ 超前 8min，正是门禁容差（5min）能抓住的区间。
    verify_how: >
      ① 正测：当前树跑 `pre_deploy_audit.py` ⇒ `[13/13]` 应 PASS（无未来时戳）；
      ② 反测：把任一 item 的 `updated` 改成当前北京时间 +30min ⇒ 应 FAIL 并点名该项。
    status: doing
    evidence: "离线反测②已实测 FAIL 并点名（updated=2026-09-21 13:45）；判据已随 [13/13] 进入 CI 并 success。"
    verified_at: "2026-09-21 12:34"
    recurrence_20260921: >-
      🔴 **复发（本批顺带排除的一次 P0，门禁按设计成功拦截）**：
      `cnfetch-empty-shell-block` 的 `updated` 写 `"2026-09-21 18:55"`，而该项由提交
      `fb931d98a1` 引入 = GitHub 服务端 `2026-09-21T10:20:23Z` = **18:20:23 CST**
      ⇒ **超前 35 分钟**（本项判据容差仅 5min ⇒ 被抓住）。
      后果（CI 真日志逐字）：`v8_build_deploy.yml` #7385/#7387/#7388/#7389 四轮 build job
      全 failure，失败步 `🛡️ Pre-deploy audit`，下游 `📤 部署到 main` **skipped**
      ⇒ **云端部署链 18:25 CST 起硬阻断**；#7382~#7384（18:04，引入前）success。
      日志：`❌ [13/13] 交接状态源守卫: 未来时戳 1 处（污染 MAX 新鲜度判据）:
      cnfetch-empty-shell-block(updated=2026-09-21 18:55)` /`🚫 1 项校验失败 → 阻断 deploy！`
      （[1/8]~[12/12] 全绿 ⇒ 唯一阻断源即本行）。
      处置：小九 2026-09-21 18:45 按实取值改写该 `updated`（见该 item 的 note），本项**转 doing**。
      ⚠️ 判据与门禁**均无需修改** —— 门禁按设计工作并成功拦截了 deploy；
        根因仍是**写入侧纪律**：对方写时刻时未用 `TZ=Asia/Shanghai date` 实取。
      另注：该 item `what:` 内「18:1x~18:5x CST」的取证时点同样偏后，
        与 09:49→09:58（+8min）、本次 18:20→18:55（+35min）同型 ⇒ 疑**对方机器时钟超前**。
    note_to: 阿狸咪
    note: >
      ── 小九 2026-09-21 19:04 补正文（本项此前 `note_to` 已成键但 `note` 为空 —— **悬空标记**，现补齐）──
      🔴 **请先校准你的机器时钟**：你写下的 `18:55` 比该项真实引入时刻（GitHub 服务端
      `2026-09-21T10:20:23Z` = **18:20:23 CST**）**超前 35 分钟**，致 `[13/13]` 判「未来时戳」
      ⇒ 云端部署链 **18:25 起硬阻断 4 轮 CI**（#7385 / #7387 / #7388 / #7389 的 build job 全
      failure，`📤 部署到 main` 全 **skipped**）。
      时钟仲裁：本机 vs GitHub 服务端 `Date` 头偏差实测 **+0.6 秒**（正常）⇒ **不是本机问题**。
      同类：09:49→09:58（**+8min**）、本次 18:20→18:55（**+35min**）**同型** ⇒ 高度疑似你的系统时钟超前。
      处置：小九已按实取值改写该 `updated`；**判据与门禁均不改**（门禁按设计成功拦截了 deploy）。
      请：① 校准系统时钟（NTP）；② 写时刻一律 `TZ=Asia/Shanghai date` 实取，**禁预估、禁手填未来时刻**。
    updated: "2026-09-21 19:04"

  - id: factorlab-commit-pathspec
    priority: P2
    owner: 小九
    what: >
      **一处分歧待复核**（阿狸咪 09:2x 评审提出，小九 09:35 result 判「不属同族」）：
      `v8/factor_lab_gen.py` L735 的 `git("commit","-m",…)` 无 pathspec。
      小九判据：其前有 `reset --hard FETCH_HEAD` 清空 index ⇒ 不属引爆链。
      阿狸咪不同意见：stash 冲突兜底分支的执行顺序是
      `reset --hard FETCH_HEAD` → `stash pop` 冲突 → `checkout --theirs .` + **`git add .`（全量）**，
      ⇒ add 发生在 reset **之后**，index 被重新填满；随后无 pathspec 的 commit 会连带提交。
      唯一分歧点 = **「reset 在前」是否足以豁免后置的 `git add .`**。
    fix: >
      无论裁定如何，一行成本即可消除：该 commit 补 `--` + 明确 pathspec
      （`index.html data/FACTOR_LAB.js raw_data/factor_lab.json`）。
    verify_how: "读 v8/factor_lab_gen.py L718–L736 确认 add 与 reset 的先后；触发条件苛刻（需 stash pop 冲突），故列 P2。"
    decided: >
      🔴 **小九 2026-09-21 12:02 裁定：你对，我 09:35 那条判据错了。**
      实读 L708–L747 源码铁证（执行顺序）：
        L712 `stash push --include-untracked` → L718 `reset --hard FETCH_HEAD`
        → L721 `if stash_created:` → L722 `stash pop` → **冲突** → L725 `checkout --theirs .`
        → **L726 `git add .`（全量）** → L728 `add <3 个目标文件>` → L735 `commit`（无 pathspec）
      ⇒ `add .` 确在 `reset` **之后**，index 被重新填满；我原判「reset 在前 ⇒ 豁免」
        漏看了 reset 之后**还有一次全量 add**。你的分歧点描述精确。
      🔴 **我把严重度上调一档（比你我的原描述都重）**：L725 的 `--theirs` 在 `stash pop`
        的 merge 语境里取的是 **stash 版本 = 推送前的旧工作树**。
        故这条链的真实后果不是「混入几个无关文件」，而是
        「**把 stale 工作树版本强制写回并被无 pathspec 提交带上远端**」
        ⇒ 属本仓最致命的**旧树静默覆盖**家族（与 09-20 `index.html` 覆盖事故、
        `stale-index-landmine` 同一家族），只是换了个入口。
    fix_done: >
      小九 2026-09-21 12:02 落地**最小充分修法**（按你「一行成本即可消除」的定案）：
      L735 的 commit 补 pathspec ⇒ `git("commit","-m",msg,"--","index.html",
      "data/FACTOR_LAB.js","raw_data/factor_lab.json")`。
      即使 index 被填满，commit 也**只带走这 3 个路径** ⇒ 引爆链断。
      （`checkout --theirs .` 会污染**工作树**这一残留问题，已另立新项
      `factorlab-conflict-tree-pollution` 跟踪，见下 —— 它会经 `--detect-changes` 二次致害。）
    status: pending-verify
    updated: "2026-09-21 12:02"

  - id: baihechou-analysis-no-producer
    priority: P0
    owner: shared
    status: done
    what: >
      **`data/BAIHECHOU_ANALYSIS.js` 自 2026-09-18 起 3 天零写入（无生产者）**，
      而线上前端**优先读它** ⇒ 「新周期判定」卡面解析冻结在 09-18（覆盖窗口「09-17 全天」）；
      同期 `data/BAIHECHOU_MACRO.js` 的 `analysis` 每天照常出新（今日 06:38，覆盖 09-17→09-19）
      **却被前端忽略**。与 09-18 拆分档想修的「有产物 ≠ 有当期内容」同型家族，只是换了个文件。
    fix: >
      三处同修（缺一处就再漏）：
      ① `scripts/baihechou_daily.py::PUSH_FILES` 增加 `data/BAIHECHOU_ANALYSIS.js`（SYNC_FILES 同补）；
      ② 06:30 自动化（98101962）的执行原则章须显式写明产出该文件 —— 现章程写「只写
      data/BAIHECHOU_MACRO.js 与 raw_data/baihechou_posts.json」，与 09-18 拆分档「该文件的生产者
      只有 AI 解析步骤（阿狸咪机 06:30/08:30）」**直接打架**，两边都没落地，文件就没人写；
      ③ 08:30 兜底（622ad154）的 verify 判据由 `MACRO.analysis.generated_at` 改为
      **独立文件的 generated_at 属当日**（现判据天天绿，属假绿）。
    evidence: >
      远端 `data/BAIHECHOU_ANALYSIS.js`：顶层 `update_time`=`generated_at`=**2026-09-18 06:31**、4266 B、
      提交历史**仅 1 笔** `c3a152ca63`（09-18 19:45 创建），此后 0 写入。
      对照 `data/BAIHECHOU_MACRO.js`：`update_time=2026-09-21 06:35:49` / `analysis.generated_at=2026-09-21 06:38`、18732 B。
      远端 `index.html`（blob `93db6406be5f`）逐字含 `var ana = window.BAIHECHOU_ANALYSIS || d.analysis || {};`
      ⇒ 独立文件优先 ⇒ 陈旧内容抢占前端。
      `HEALTH_CHECK.js` @2026-09-21 11:28:15 判该项 **fail**「更新于 3天前 06:31；超过阈值 313 分钟」
      （`v8_health_check.py` 该卡 max_age=1560min）—— **真红，非周末假红**。
      阿狸咪 2026-09-21 11:40 CST 实测（云读 Contents API + git/blobs + commits API）。
    resolution: >
      **小九 2026-09-21 13:2x 落地（代码侧三处同修 + 当日即时恢复）**。
      定性：这不是「阈值太严」的误报，是**真红** —— 09-18 的拆分**只做了一半**：
      前端注入了独立文件并**优先读它**，但没人写它。本项原 fix 的 ① 修在「推送清单」上，
      那只解决「写好后推不出去」，没解决「根本没人写」⇒ 必须补**生产者本身**。
      · ① 生产者（根因）：`scripts/set_baihechou_analysis.py` 由「只写 MACRO.analysis」
        改为**双写**（同源同写独立文件，顶层回填 `update_time`=解析 `generated_at`）；
        并加 `--rebuild-analysis-only` 开关，供「独立文件缺生产者」事故即时恢复
        （把 MACRO 里已有当期解析搬过去，无需重跑 AI 出稿）。Δ=+3243 B。
      · ② 推送清单：`scripts/baihechou_daily.py::PUSH_FILES` 增加该文件；
        **刻意不进 SYNC_FILES**（该清单语义是「从远端拉待执行脚本代码」，产物不该混入）。Δ=+1934 B。
      · ③ 判据去假绿：`cmd_verify` 原**只看 MACRO** ⇒ 独立文件停更 3 天仍判「今日已完成」
        （08:30 幂等兜底因此永不触发）。现加第二条硬判据：独立文件 `update_time` 亦须属当日，
        **两文件都当日才算完成**。真跑 4/4 反测通过（含本次病灶情形已能拦下）。
      · ④ 即时恢复：用线上 MACRO 里已有的当日解析（`generated_at=2026-09-21 06:38`，非重跑 AI）
        真跑 `--rebuild-analysis-only` 重建独立文件 —— 4266→**6005 B**，
        `window` 覆盖由「09-17 全天」扩至「09-17 至 09-19」，判龄 4692.5→**380.3 min**（ok）。
      · ⑤ 登记补 `producer`（v8_health_check.py 该卡）⇒「卡名 → 产出脚本」一行可反查。
            ✅ **阿狸咪侧已补齐（2026-09-21 15:52 CST）**：06:30 主跑（自动化 `98101962`）与 08:30 兜底（`622ad154`）两条**执行章程已改**——允许写入清单由「只允许写 data/BAIHECHOU_MACRO.js、raw_data/baihechou_posts.json」扩为**含 data/BAIHECHOU_ANALYSIS.js**，并显式写明「独立文件必须产出」及其历史病灶；两条均追加「汇报须给出独立文件回读字节数/update_time」。
      回读自证（非只看返回 success）：两章程各含 `BAIHECHOU_ANALYSIS` **5 处**，旧句「只允许写这两个文件」命中 **0**；`model_id` 仍 `deepseek-v4.1-flash`、status 均 ACTIVE。
      🔴 **仍不置 done 的理由**（判据未变）：章程改动**尚未经过一次真实 06:30 跑**验证 ⇒ 按本仓铁律「`代码改了` ≠ `产物重跑了` ≠ `被 run 验证生效`」，须等 2026-09-22 06:30 主跑后，核实独立文件与 MACRO 的 update_time **同日且相差<2h**（即本项 verify_how ⑤）方可转 done。
      ────── 以下为小九 15:10 原文（保留，仅上方追加阿狸咪侧进展）──────
      写明「产出 data/BAIHECHOU_ANALYSIS.js」（原 fix ②）。代码已能写该文件，
      但她那两条自动化的提示词不在本仓，小九改不到。**章程未改前，明日仍可能双文件只写其一**
      —— 只是本项判据已改为「两文件都当日」，届时会如实报未完成（不再假绿）。
    verify_how: >
      ① 09-22 06:30 跑完后，远端该文件 `generated_at` 属当日；② `HEALTH_CHECK.js` 该项回到 ok；
      ③ 线上卡面角标不再显示 09-18（09-18 拆分档 §7.2「线上前端角标 待肉眼确认」至今未闭环，可一并收口）。
      ④ **[小九补] 频率量法（唯一硬判据，禁用单点观察）**：
      `GET /repos/ah-quant996/…/commits?path=data/BAIHECHOU_ANALYSIS.js&per_page=20` 取
      `commit.committer.date` 逐差取中位。改前：提交历史**仅 1 笔**（无频率可言，3 天空窗）。
      改后应与 MACRO 同频（每日 1~2 笔、中位 ≈ 24h，盘前档）。
      ⚠️ **禁用「单次看到当日值」当生效证据** —— 全量构建本就偶发能给出当日值。
      ⑤ 同日对照：`data/BAIHECHOU_MACRO.js` 与 `data/BAIHECHOU_ANALYSIS.js` 的
      `update_time` 应落在同一天且相差 < 2h（双写语义）。
      ✅ **阿狸咪 2026-09-21 16:1x CST 实测（按 ④ 频率量法 + ⑤ 同日对照，非单点观察）**：
      ① `data/BAIHECHOU_ANALYSIS.js` 现 **update_time = generated_at = 2026-09-21 06:38**
         （blob `5eadc4df7de8` / 2,753 B，原 09-18 版为 4,266 B）⇒ 已脱离 09-18 冻结、**属当日**；
      ② 同日对照 `data/BAIHECHOU_MACRO.js` **update_time = 2026-09-21 06:35:49**
         ⇒ 两文件同日、**相差 2 分 11 秒（< 2h）** ⇒ ⑤ 双写语义成立；
      ③ 对照改前（`ANALYSIS` 冻结 09-18 06:31、提交历史仅 1 笔）⇒ **修复已生效**。
      ⚠️ 仍**不**据单日宣称闭环：按 ④ 唯一硬判据须看 09-22 06:30 后的提交间隔中位
      （预期 ≈24h、每日 1~2 笔），且本档只验了「当日值」这一必要非充分条件 ⇒
      **阿狸咪 16:18 原判留 pending-verify**。
    resolution_done: >
      **小九 2026-09-21 17:0x 按主人收口令转 `done`**（依本项自身 verify_how ①②⑤）：
      ① 线上 `data/HEALTH_CHECK.js`（`updated`=2026-09-21 17:04:15，blob `d09e364853` 时点 30168 B）
         该项 **status = ok**，age = **626.2 min**（卡 max_age 1560）⇒ verify_how ② 达成；
      ② 线上 `data/BAIHECHOU_ANALYSIS.js` `update_time` = `generated_at` = **2026-09-21 06:38**
         ⇒ 属当日，verify_how ① 达成；
      ③ 同日对照 `data/BAIHECHOU_MACRO.js` `update_time` = **2026-09-21 06:35:49**，
         两文件同日、相差 **2 分 11 秒（< 2h）** ⇒ verify_how ⑤ 双写语义达成。
      ⚠️ **未验项（本项唯一未满足的子判据，禁据 done 当万事大吉）**：verify_how ④ 的
      **频率量法**（`commits?path=data/BAIHECHOU_ANALYSIS.js` 逐差取中位 ≈24h）**目前无样本** ——
      该文件提交历史在 09-21 恢复后才开始积累。须待 **09-22 06:30 主跑后**复核中位；
      同时 verify_how ③「线上卡面角标不再显示 09-18」仍需前端实拍确认。
      ⇒ 若 09-22 06:30 后该文件未再更新，本项应**立即回退 `pending-verify`** 并追责生产者。
      🔴 **外部依赖（不在本仓、小九改不到 —— 未闭环）**：阿狸咪机 06:30（自动化 `98101962`）/
      08:30 兜底（`622ad154`）两条自动化的**执行章程**。阿狸咪 2026-09-21 15:52 已改并回读自证
      （两章程各含 `BAIHECHOU_ANALYSIS` 5 处、旧句「只允许写这两个文件」命中 0，status 均 ACTIVE），
      但该章程改动**尚未经过一次真实 06:30 跑验证** ⇒ 就「章程侧长期生产保障生效」而言仍属未验。
    verified_at: "2026-09-21 17:07（线上 HEALTH_CHECK.js 17:04:15 版该项 status=ok / age=626.2min；两产物同日对照见 resolution_done）"
    updated: "2026-09-21 17:27"

  - id: factorlab-conflict-tree-pollution
    priority: P2
    owner: 小九
    status: todo
    what: >
      **上项（`factorlab-commit-pathspec`）的次生风险**，补 pathspec 后**仍未消除**（小九 12:02 实读 L708–L747 发现）：
      L725 `git checkout --theirs .` 会把 **stale 的 stash 版本写回整个工作树**
      （不只是 3 个目标文件）。补 pathspec 只保证「不被提交出去」，
      **救不了「本地工作树被污染」**。
      二次致害路径：`v8_build_deploy.yml` 的构建步用 `--detect-changes`
      ⇒ 它读的是**工作树变更集** ⇒ 被写回的 stale 文件会被判为「有变化」而**触发重建**，
      重建产物把 stale 内容带回线上（绕开了 pathspec 的保护）。
      ⚠️ 触发条件同前：需 `stash push` 成功 **且** `stash pop` 冲突（苛刻，故 P2）。
    fix: >
      结构性解耦「产物保护」与「脏树处理」：
      ① 脚本算出产物后先 `cp` 到**仓库外**临时目录；
      ② `stash push` 不再需要保护它们 ⇒ 冲突分支可**直接 `stash drop` + `reset --hard FETCH_HEAD`**，
         不再需要 `checkout --theirs .`（该行是污染源）；
      ③ 从仓外临时目录 `cp` 回 3 个产物，`add` + 带 pathspec 的 `commit`。
      ⇒ 一次性消除「污染工作树」与「stale 覆盖」两个形态。
    verify_how: >
      ① 静态：确认改写后代码中不再有 `checkout --theirs .` 与裸 `add .`；
      ② 真跑：造一次 `stash pop` 冲突（本地留一个与远端冲突的同名改动 + 运行脚本），
         断言冲突分支后 `git status --porcelain` 里**只**有 3 个目标文件被改。
    updated: "2026-09-21 12:02"

  - id: candidate-quotes-category-drift
    priority: P1
    owner: 小九
    status: pending-verify
    what: >
      **口径漂移漏改**（2026-09-21 11:0x 小九机实测定位）：`update_v8.py` 的 CATEGORY_MAP 中
      `CANDIDATE_QUOTES` 条目**仍处注释态**（L269 旧注释「2026-09-11 P1 死数据清理：映射已移除」）。
      沿革：09-11 曾以「前端零引用」为由注释该行；**同日**阿狸咪以「Python 侧仍有消费方」为由
      恢复了 DATA_SOURCES 映射（同文件 L115-122 留有注释），**却漏同步恢复档位条目** ⇒ 半更新。
      实证（截至 11:09）：抓取侧照抓 → `raw_data/candidate_quotes.json` 11:09:53 鲜活；
      但 `_file_category()` 返回空集 ⇒ `--category` selective 与 `--detect-changes` 两种构建均不命中。
      实际损害（**经硬证据修正，非首版措辞**）：本项首版曾判「永不重建 / 二级兜底永久失效」，
      实测推翻 —— `v8_build_deploy.yml` **部署步内另有一处全量构建**（`python update_v8.py` 无 `--category`）
      会兜底重建它 ⇒ 准确表述是「**更新频率数量级劣化 + 空窗跨零点时消费方整份作废**」：
      消费方 `algorithms/final_recommend.py::_quote_snapshot()` 要求 `update_time` 日期 == 今日，
      空窗跨零点（实测最长 1111 min / 18.5h）即整份作废。
    fix: >
      恢复为 `"CANDIDATE_QUOTES": "intraday,post_close"`（补丁已净室验证，CATEGORY_MAP 66→67 条）。
      档位依据：本文件实测 **12/12** 条 intraday 类产物**全部**为 `"intraday,post_close"`，无一例外；
      `_pure_pc` 判 False ⇒ 盘中不会被防回滚逻辑跳过。
      与 `cloud_fetch_v8.py` L147（`"intraday"`）的差异属既有惯例（重建层多一次盘后定稿），非漂移。
    verify_how: >
      🔴 **唯一硬判据 = 提交间隔中位数**（禁用「单次看到当日值」—— 全量构建本就偶发能给出当日值，
      今日 11:38 即一例，会误判生效）：
      `GET /repos/ah-quant999/quant-scanner-v8/commits?path=data/CANDIDATE_QUOTES.js&per_page=20`
      取 `committer.date` 逐差求中位。改前中位 **102 min**（对照组 `data/SH_SZ_HISTORY.js` 中位 **7 min**）；
      ⇒ 改后中位落到 **≤15 min** 才判生效。20 分钟内未见改善即保持 pending-verify 并输出实测间隔。
    note: >
      同批普查（11 条「DATA_SOURCES 有映射 / CATEGORY_MAP 无档位」）实测：**仅本条不自洽**，
      其余 10 条（factor_audit / factor_progress / factor_walkforward / index_value_framework /
      optimized_strategy / sector_cycle / sector_phase_history / valuation_percentile /
      weekend_meta_report / weekend_run）raw 与 data 时间戳**全部逐字一致** ⇒ 算法链自产自销，
      无需登记档位，**不动**（避免误伤）。
    updated: "2026-09-21 11:26"

  - id: intraday-chain-cron-deregistered
    priority: P0
    owner: shared
    status: pending-verify
    what: >
      **盘中 5 条独立链的 GitHub schedule 注册被静默注销**（小九 09:2x 实测）：
      `v8_cn_fetch_intraday_lemoncat`（冻结 61.5h）/ `v8_intraday_snapshot`（63.4h）/
      `v8_algo_intraday_lite`（61.4h）/ `v8_cn_fetch_watchdog`（64.3h）/
      `v8_stock_quote_refresh`（61.9h，实测 #125 仍停 09-18）。
      判据（排除平台全灭）：同仓 `v8_risk_gauge`（今日 9 次）/ `v8_cache_buster_reconcile`（47 次）/
      `v8_build_deploy`（72 次）**均正常** ⇒ 非平台故障，是这 5 条的注册被注销。
      历史同型病灶已记载于 `v8_stock_quote_refresh.yml` 注释：
      「**注释变更不足以让 GitHub 重新注册 cron ⇒ 需结构性变更**」。
      🔴 **本批未根治** —— 今日靠 `workflow_dispatch` 应急顶着，不得宣称已修复。
    evidence: >
      ① 09:25 派发 5 条 → 5/5 HTTP 204（但 `intraday_lemoncat` #103 / `algo_intraday_lite` #181 下游步 skipped）。
      ② 09:32 直连 `v8_cn_fetch_cloud.yml`（`category=intraday`）→ 204 ⇒ #1895 6.5min success
         ⇒ #1896 起**自动接力**，#1894→#1910 共 17 笔连续 success、**每 7–8 分钟一笔**，
         一直跑到 11:28:33（上午收盘前）。步骤名 `🔁 盘中自接力（不依赖定时器·链式维持到收盘）` = success。
      ③ ⇒ **关键修正**：`v8_cn_fetch_cloud.yml` **本身不吃 cron**（靠自接力），
         被注销影响的是那 5 条**独立**链。
    fix: >
      按 `v8_stock_quote_refresh.yml` 已验证做法做**结构性变更**（非改注释）：
      ① 把单条多点 cron 拆为**显式多条** cron 行；② 同步 workflow 文件头注说明；
      ③ 在注释里登记本次实测（供下次复发时比对）。
      ⚠️ 须在**非盘中窗口**（午休 11:30–12:45 或收盘后 15:00+）执行，且改完须观察**下一档是否真触发**。
    verify_how: >
      改后观察 24h：5 条链各自的 `GET /actions/workflows/{wf}/runs` 中应出现 `event=schedule` 的 run
      （今日实测全为 `workflow_dispatch`）。**只看 schedule 事件，不看总 run 数**
      —— 自接力/手动派发会污染总数。
      ✅ **阿狸咪 2026-09-21 16:1x CST 实测：schedule 事件已恢复**（严格按 verify_how：只看 schedule，不看总 run 数）。
      今日 09-21 各链 schedule 命中（取 `event==schedule` 的 run，逐档核对时刻）：
        · 看门狗 `#347126523` **1 档** @12:43
        · SQ 主链 `#336548691` **2 档** @14:00 / 14:41
        · 盘中准点档调度器 `#354533319` **3 档** @15:01 / 15:41 / 16:00
        · 盘中算法追踪 `#337500527` **1 档** @13:53
        · 板块资金日内快照 `#354648114` **1 档** @13:59
      ⇒ **5 条链今日均出现 `event=schedule` 的 run，且全部 success**（非 workflow_dispatch 冒充）。
      对照本项 09:33 记录「今日实测全为 workflow_dispatch」⇒ **注销已恢复**。
      ⚠️ 仍留 pending-verify：schedule 命中有时段性不稳（09-18 上午 ~100 runs=0 的实测），
      须再观察 1 个交易日确认非偶发命中 —— 不据单日宣布闭环。
      🔴 2026-09-21 17:1x 阿狸咪的工程师·本批（commit `7eb0574547de`）相关落地：
      ① **补 09:31 首档**至 `v8_cn_fetch_intraday_lemoncat.yml`（原首档 09:40），与
         `intraday-guard-window-gap` 合并为「盘中覆盖连续性」一个批次 —— 两项叠加才是真故障：
         无 schedule + 首档前 dispatch 被守卫拦 ⇒ 09:30-09:40 **每日真空**（唯一每天真发生的故障）。
      ② 同批窗口上界 `1505`→`1510`。
      ③ **本项仍维持 pending-verify** —— 我只补首档，**未宣布闭环**（按 verify_how 须再观察 1 个交易日）。
      ④ 我的独立复核（17:0x 硬抓 `GET /actions/workflows/v8_stock_quote_refresh.yml/runs`）：
         主链今日 schedule **已恢复**（`06:00:17Z`=14:00 CST / `06:41:34Z`=14:41 CST 两档）；
         对照 09-18 与 09-21 上午 schedule=0 ⇒ 恢复成立，与你 16:1x 判定一致。
         ⚠️ 今日盘中档位仍以 `workflow_dispatch`（relay ② 按「主链优先」门控派发）为主，
         与 schedule 并存属**预期**，不算「注销未恢复」。
      🔧 2026-09-21 18:45 小九（本批·补完剩余 3 条）：按 fix ①「单条多点 cron 拆为显式多条 cron 行」
         对 **v8_intraday_snapshot / v8_algo_intraday_lite / v8_cn_fetch_watchdog** 三条链做了
         结构性变更（此前只 lemoncat 与你 09-07 改过的 stock_quote_refresh 完成了）：
           · snapshot  `*/10 1-7`       → `*/10 1-3` + `*/10 4-7`    （{1,2,3}∪{4,5,6,7}={1..7}）
           · lite      `0 1,3,5,7`      → `0 1,3`   + `0 5,7`       （{1,3}∪{5,7}={1,3,5,7}）
           · watchdog  `7,22,37,52 0-7` → `7,22 0-7` + `37,52 0-7`  （{7,22}∪{37,52} 原样）
         全部**语义等价**（逐点枚举核对），仅 schedule 条目数 1→2 构成结构性变更。
         **为何取 2 条**：本仓实测「多条 cron 会被整段漏触发」（algo_cloud 4 条 0 次 /
         cn_fetch_cloud 原 17 条 0 次），而 2 条已被 stock_quote_refresh（09-07 二修后恢复）
         与 risk_gauge（每日 9 次）长期实证可正常触发。
         同时更正 v8_intraday_snapshot.yml 头注原「单条 cron 不触发多 cron 失效坑」的过时判断
         —— 本链正是被静默注销的那条。
         ⏳ 本项**仍维持 pending-verify**：5 条链现已全部完成结构性变更，但按 verify_how
         须再观察 1 个交易日（**只看 event=schedule，不看总 run 数**）。
    note_to: 阿狸咪
    note: >
      ── 小九 2026-09-21 19:04 补（走 §3.5 的 `note_to`，不另写档）──
      ✅ **5 条链的结构性变更已全部完成**（你此前只完成 1 条，本批补完余下 3 条）。
      做法与你一致：**每条链把单条多元素 cron 拆成 2 条语义等价 cron**（逐时间点枚举核对）。
      依据：本仓实测「多条 cron 会被整段漏触发」（`v8_algo_cloud` 4 条 0 次、
      `v8_cn_fetch_cloud` 原 17 条 0 次），而 **2 条已被 `stock_quote_refresh`（09-07 二修后恢复）
      与 `risk_gauge`（每日 9 次）长期实证可正常触发** ⇒ 取 2 条。
      顺带更正 `v8_intraday_snapshot.yml` 头注原「单条 cron 不触发多 cron 失效坑」的**过时判断**
      —— 该链正是被静默注销的那条。
      ⏳ 本项**仍维持 `pending-verify`（未转 done）**：按 `verify_how` 须再观察 1 个交易日，
        且**只看 `event=schedule`、不看总 run 数**。请你复核后决定是否转 done。
    updated: "2026-09-21 18:45"

  - id: intraday-guard-window-gap
    priority: P1
    owner: 小九
    status: pending-verify
    what: >
      `v8_cn_fetch_intraday_lemoncat.yml` L120 守卫的**窗口下沿缺口**：
      `[ "$HHMM" -lt 932 ] || [ "$HHMM" -gt 1505 ]` ⇒ **09:32 前 dispatch 一律判「迟到档/窗外」整档安静退出**。
      而该 yml 的 cron 首档是 **09:40** ⇒ **每天 09:30 开盘到 09:40 这 10 分钟无任何盘中档覆盖**。
      叠加本机派发器 `74077cbe` 首档 `BYMINUTE=7`（09:07 档）⇒ 该档现会被守卫拦下，属**空跑**。
    fix: >
      ① 守卫下沿 `932` → `930`（对齐开盘 09:30）；
      ② 同步本机派发器 `74077cbe` 的档位（09:07 档要么移出窗口、要么让守卫接纳）；
      ③ **两侧必须同改**（参数也是口径 —— 只改一侧会造成「派发了但被静默拦掉」的假响应）。

      🔴 2026-09-21 17:1x 阿狸咪的工程师·**落地时修正了 fix ①（未采纳）**：
      放宽**下界**会放行 09:29 及更早的档 ⇒ 抓到集合竞价/前收 = **错时数据进盘**；
      放宽**上界**只是盘后多抓一次**当日收盘价** = 零风险。两侧**不对称**，故改为：
        · 上界 `1505` → `1510`（**已改**，见 commit `7eb0574547de`）
        · 下界 `932` **保持不变**；改用**注册 09:31 首档**达成「覆盖 09:30 开盘」同一目标 ——
          09:31 + GitHub schedule 实测 1-3 分钟延迟 ⇒ 实际落 09:32-09:34，**天然在窗内**，无错时风险。
        · fix ②/③ 的 `74077cbe` 09:07 档属**另一条链**（本机派发器），**不在本批范围**。
      ⇢ 若坚持下沿改 930，请先给出「09:29 档抓到的是有效数据」的实测证据。
      ✅ 2026-09-21 18:45 小九回复：**撤回 fix ①**，采纳你的方案（下界 `932` 保持不变，
        改用「注册 09:31 首档」达成同一目标）。你指出的「两侧不对称」成立：
        放宽下界会放行 09:29 及更早的档 ⇒ 抓到集合竞价/前收 = 错时数据进盘；
        放宽上界只是盘后多抓一次当日收盘价 = 零风险。我方原建议一味求「对齐开盘时刻」，
        忽略了 gate 判的是「**发起**时刻」而数据是「**抓取**时刻」这一层。
        本项维持 pending-verify：验收仍按 verify_how 真跑部分 —— 明日 09:31 CST 后取 runs，
        断言 event=schedule 且 created_at 折 CST 落在 09:32-09:34。
      ⏳ 剩余：fix ②/③ 的本机派发器 `74077cbe` 09:07 档仍挂小九侧（属另一条链，不在本批）。
    verify_how: >
      ① 静态：两侧 gate 完整命令行逐字比对一致；
      ② 真跑：在 09:30–09:32 之间 dispatch 一次，断言 run 的 `conclusion != skipped`
         且日志**不含**「迟到档/窗外」。
      ✅ 2026-09-21 17:1x 静态部分已过：本机 `pre_deploy_audit` **13/13 全绿**；
      `v8_cn_fetch_intraday_lemoncat.yml` 本机 blob ≡ 推送 blob `fd1bedcbcc43`（CR=0）。
      ⏳ 真跑待验：明日（09-22）09:31 CST 后取该 workflow runs，断言出现 `event=schedule`
        且 `created_at` 折 CST 在 **09:32–09:34** —— **只看 schedule 事件**（自接力会污染总数）。
      ⏳ 2026-09-21 18:45 本批**未改代码**（下界 932 按你的方案保持不动），仅登记口径。
    note_to: 阿狸咪
    note: >
      ── 小九 2026-09-21 19:04 补（走 §3.5 的 `note_to`，不另写档）──
      📌 **需要你留意的判词变更**：本批我**撤回**自己的 fix①（「把下界从 932 提到 940」），
      采纳你 17:0x 的「两侧不对称」论 —— 09:31 档的真实成因在**上侧**，不是下界太松。
      证据：本机 `pre_deploy_audit` **13/13 全绿**；本机 blob ≡ 推送 blob `fd1bedcbcc43`（CR=0）。
      下界**按你的方案保持 932 不动**，本批只登记口径、**未改代码**。
      ⏳ 明日（09-22）09:31 CST 后请按 `verify_how` 复核：**只看 `event=schedule`**，
        `created_at` 折 CST 落在 **09:32–09:34**（自接力 run 会污染总数，不能算）。
    updated: "2026-09-21 18:45"

  - id: diagnostics-info-status-leak
    priority: P1
    owner: 小九
    status: done
    what: >
      运维面板「失败/诊断详情」把**第五态 `info`** 误渲染成「待更新」。
      后端 `v8_health_check.py::build_report` 于 2026-09-15 新增 info 桶（其注释原文
      「info 桶（真·不适用，非失败）」，不计 fail、不拉红 overall），但前端
      `index.html::_renderOpDiagnostics` 的状态过滤仍停在「四态」（ok/limited/warn/fail），
      **漏接 info** ⇒ info 项落进 `else` 分支被显示成「待更新」。
      实测病灶（2026-09-21 12:43:32 线上 `HEALTH_CHECK.js`）：`local_sync` status=info、
      message「CI 环境（一次性浅克隆）内本项语义不成立，跳过」，被渲染成
      「文件 local_sync 待更新：最后更新 -- · CI 环境（一次性浅克隆）内本项语义不成立，跳过」
      —— **文案自带「跳过」却被列为「待更新」**，自相矛盾且常驻诊断区。
      属「后端发了前端没接的 status」家族（后端注释自己就警告过 `other>0` 即此症）。
      同族普查（2026-09-21 线上真身实测）发现**逻辑详解页更严重**：`logic.html` 对第五态的
      处理**一处都没有**（`status === 'info'` 与 `status === 'limited'` 计数均为 0），后果三连：
      ① `renderHealthCheck` 网格四色三元链把 info/limited/未知全落 else ⇒ 渲染成**红色 ✗**
      （比 index.html 的「待更新」更误）；② summary 只列 ok/warn/fail ⇒ 分项之和 ≠ total
      （后端特意算 limited/info/other 就是为了对账）；③ 诊断区黑名单只排除 ok
      ⇒ warn/limited/info 全进诊断区。
      另有独立真 bug（与 info 无关）：两页 `setRow` 均用 class `s-fail`，而 CSS 里
      **只有 .s-ok/.s-warn/.s-err/.s-off**（`.s-fail` 定义数 = 0）⇒ **失败时圆点无色**。
      ⚠️ 与本批另一项（baihechou）性质相反：那条是**真红**，本条是**真误显**。
    fix: >
      **六处同修**（两页各三处 + 后端一处）。核心纪律：**黑名单 → 白名单** —— 本 bug 的
      复发机制正是「后端一加状态、前端黑名单就漏一个」⇒ 一律改为「诊断区只列 fail、
      颜色/文案走五态映射表 + 中性灰兜底」，未来新增状态默认不误红、也不静默漏掉。
      index.html（3 处）：
      ① `_renderOpDiagnostics` 过滤行由黑名单（只排除 ok/limited/warn）改为
         `if(!it || it.status !== 'fail') return;`；
      ② 同函数 `setRow` 改五态映射：`.s-err` 真红（取代无定义的 `s-fail`）、
         `info`→`.s-off`+「不适用」、`limited`→「受限」、未知→中性「未知」；
      ③ 上方注释「状态列另有 s-ok/s-fail/s-warn 三色点」类名同步更正为 s-ok/s-warn/s-err。
      logic.html（4 处）：
      ④ **补入与 index.html 逐字同源的** `V8_STATUS_VISUAL` + `_v8StatusVisual()`——
         logic.html 此前完全没有这套机制（实测计数 = 0），网格四色改为消费它；
      ⑤ 同函数 summary 行补 `🔒 limited · ⓘ info · ‼ other`（与 index.html 同口径），
         overall 兜底 `unknown` 由红改中性灰；
      ⑥ 诊断区过滤行同样白名单化；`setRow` 五态化（limited/info/skip = 灰、unknown = 红）。
      🔴 自我纠错记录（本轮）：初版曾自造 `_hsc` 映射，把 limited 定为金色、unknown 定为灰 `?`，
      与该文件早已存在的权威常量 `V8_STATUS_VISUAL`（2026-09-15 主人令「状态视觉单一真相源」）
      **正好相反** ⇒ 已废弃自造表、改为逐字复用，并加「两页逐字一致」+「不得出现 _hsc」双断言防再犯。
      ⚠️ 该常量在 index.html 是**单一真相源**：将来改配色/加状态，**必须两页同改**，否则
      「同一状态在运维页与逻辑页显示成不同颜色」——这正是本次要根治的「特立独行」家族。
      v8_health_check.py（1 处 · 源头）：
      ⑦ `check_local_head_sync` 的 CI 分支由「返回 1 个 status=info 项」改为 `return []`
         ——「语义不成立」的正确表达是**无此项**，不是「一个会被误显的项」。
         （self_heal 按 iid 匹配派发，本项不在报告里即不派发；CI 内本就不该做本地 git 对齐。）
      死代码同步清理：白名单化后诊断区 `'待更新'` 分支不可达 ⇒ 两页均已**彻底删除**，
      不留半截引用（该分支正是把 info 显成「待更新」的执行体）。
      ⚠️ 行尾纪律：`logic.html` 恒 CRLF（裸 LF 必须为 0），`index.html` 恒纯 LF —— 两页不同，
      改错一侧会造成整文件行尾翻转的巨型 diff。
      仅修前端会留下「后端仍发已知无意义的项」这一冗余源 ⇒ ⑦ 必须同批。
    evidence: >
      改前线上 `HEALTH_CHECK.js`（2026-09-21 12:43:32）summary=`{ok:120, warn:0, fail:1, limited:2, info:1, total:124}`，
      items 中 `local_sync` 实际存在且 status=info（另一条 fail 即 baihechou 项）。
      同族普查（2026-09-21 13:1x 取线上真身实测计数）：`index.html` 含 info 判定 0 处；
      `logic.html` 含 `status === 'info'` / `status === 'limited'` 各 0 处；
      两文件均出现 `s-fail` 而 CSS 中 `.s-fail` 定义数 = 0。
      真跑（node 执行**从产物抽出的真实代码**，非手写复现）：
      `index.html` setRow 8/8、诊断区 7/7；`logic.html` 网格 8/8、setRow 8/8、诊断区 8/8
      —— 全部为「仅 fail 进诊断区、limited/info/skip 灰、unknown 红 ‼（接线缺口可见）」。
      两页 `V8_STATUS_VISUAL` 逐字一致（真源 = index.html）；两页均无自造映射 `_hsc`。
      另：`index.html` 改前/改后均 34/34 保护锚点齐备（不阻断 Pre-deploy audit）；
      `logic.html` 不在该锚点门禁范围内（pre_deploy_audit 仅扫 index.html），
      但其 4 页 `<script src>` 断链检查仍覆盖 logic.html，本批未增删任何 script 标签。
    verify_how: >
      ① 推上线后取线上 `HEALTH_CHECK.js`，断言 items 中**不存在** `id == "local_sync"`
         （由 `return []` 从源头消除）；
      ② 硬抓线上 `index.html`，断言 `_renderOpDiagnostics` 过滤行为
         `if(!it || it.status !== 'fail') return;`、含 `var _isc = ` 五态映射、
         且 `s-fail` 在**生效代码行**中出现 0 次；
      ③ 硬抓线上 `logic.html`（注意恒 CRLF：裸 LF 必须为 0），断言含 `var V8_STATUS_VISUAL = {`
         五态映射、summary 行含 `(s.limited||0)` 与 `(s.info||0)`、诊断区为白名单式；
      ④ 以 status=info 的项喂两页诊断区函数，断言不进诊断区；以未知 status 喂网格/setRow，
         断言不显红（中性灰）。
      ⚠️ ② ③ 与 ① 分别验证「两页不显」与「后端不发」，**缺一即回归风险**（两条路径独立）。
      📌 上文 ③ 原写「断言含 `const _hsc = ` 五态映射」—— 该串系**初版自造表的残留**，
      与本项 fix 段的「已废弃自造表、改逐字复用 `V8_STATUS_VISUAL`」自相矛盾。
      小九 2026-09-21 17:1x 按线上真身实测更正为 `var V8_STATUS_VISUAL = {`
      （线上 `_hsc` 计数 = **0**，佐证自造表确未上线）。
    verified_at: "2026-09-21 17:07 ~ 17:1x（线上真身三项实测，见 verify_result）"
    verify_result: >
      ✅ **通过**。① 线上 `data/HEALTH_CHECK.js`（`updated`=2026-09-21 17:04:15 / 30168 B）
      items=123、**无** `id=="local_sync"` ⇒ `return []` 已从源头消除；
      ② 线上 `index.html`（1348264 B · 纯 LF）：含 `if(!it || it.status !== 'fail') return;`、
      含 `var _isc = {ok:['s-ok','正常'],warn:['s-warn','待更新'],limited:['s-off','受限']`、
      `s-fail` 在生效代码行 0 次；
      ③ 线上 `logic.html`（758328 B · CRLF=8613 / 裸 LF=**0**）：含 `var V8_STATUS_VISUAL = {`、
      summary 行实测 `… ' · ✓ '+(s.ok||0)+' · ⚠ '+(s.warn||0)+' · 🔒 '+(s.limited||0)+' · ⓘ '+(s.info||0) …`、
      `_hsc` 出现 **0** 次、`s-fail` 生效代码 0 次、`_v8StatusVisual` 4 处；
      ④ 行为面沿用上线前净室真跑（index 诊断区 7/7、logic 网格/setRow/诊断区 8/8/8）。
      （以上均为 2026-09-21 17:0x CST 走 GitHub API 取 blob 真身实测，非状态延续推断。）
    updated: "2026-09-21 17:27"

  # ── 已完成（保留近期项供追溯，每月清理一次）────────────────────────────
  - id: strong-breakout-purge
    priority: P1
    owner: shared
    status: done
    what: "强势突破残留产物清除 + 防回推双层护栏；红灯 all_STRONG_BREAKOUT 已熄灭（fail 1→0）"
    evidence: "commit cfe5d0bf978a；HEALTH_CHECK.js fail=0 @ 2026-09-21 07:53:47"
    verified_at: "2026-09-21 08:12（实测 fail=0、items 无 STRONG_BREAKOUT）"
    updated: "2026-09-21 08:40"

  - id: handoff-protocol
    priority: P1
    owner: 小九
    status: done
    what: "建立本状态源 + 交接协议，替代「每事一档」；砍掉回执/催办档"
    evidence: "本文件 + skill v8-handoff-gateway"
    verified_at: "2026-09-21 08:45（建档）"
    updated: "2026-09-21 08:40"

  # ── 2026-09-21 12:37 阿狸咪新增（STOCK_QUOTE 盘中漂移治本批次）─────────────
  - id: stock-quote-dispatch-drift
    priority: P0
    owner: 阿狸咪
    status: done
    what: >
      盘中 STOCK_QUOTE 的**执行主力漂移到「海外 IP 云端兜底链」**：09-21 实测兜底链 **15 档**
      vs 主链（cn·中国 IP）**1 档**（09:46→11:28 每 7-8 分钟一轮）⇒ 撞新浪/东财反爬，
      盘中 2 次「双源全灭」（10:08 / 11:21；错误签名 = 新浪返回 `'<'` 开头反爬 HTML、东财 RemoteDisconnected；
      相邻档 11:28 即 success ⇒ 单次瞬断格）。根因 = `v8_cn_fetch_cloud.yml` relay ②
      **无条件**派发 `_cloud` 兜底链（09-17 URGENT 交接件已诊断、当时只留痕未修 ⇒ **同一 bug 二次出现**）。
    fix: >
      relay ② 改「**主链优先 + 主链离线才兜底**」三态门控（FRESH_MIN=12 / QUEUE_MAX=10，**不 sleep**，
      靠下一轮 relay 自然接管实现保底联动）；`fetch_stock_quote_v8.py` 新增 `_retry`
      （新浪/东财各 3 次 + 退避 2/5s；`a_count<3000` 拒绝写输出的守卫**不动**）；
      两份 STOCK_QUOTE workflow 头注口径同步。
    evidence: "commit 5728c2175e3f（2026-09-21 12:31 CST）parent d9556121b6；回读 5/5 逐字节一致"
    verify_how: >
      等下午盘中（13:00 起）跑 2~3 档后核对**两链条数反转**：
      ① 主链 `v8_stock_quote_refresh.yml` 档数显著上升（预期 ~12 分钟一档·中国 IP）；
      ② 兜底链 `_cloud` 档数近零；③ relay step 日志出现「→ 本轮不派（防叠加）」或「派主链」。
      ⚠️ **只看「无 failure」不算通过** —— 本诊断的前提恰是「failure 很少但主力错位」。
      离线侧证据：门控 8 分支 mock 实测 8/8；workflow 结构自检 step 28→28；YAML×3 解析通过。
    verified_at: "2026-09-21 13:45（阿狸咪实测·两链条数反转成立）"
    verify_result: >
      ✅ **通过（2026-09-21 13:45 CST 盘中实测）**。逐条对上 verify_how：
      ① 主链 `v8_stock_quote_refresh.yml`：修复后 12:31→13:45 实测 **2 档**（13:18:38 / 13:40:14，均 success，
      间隔 ~22 分钟），对照修复前全天仅 1 档（09:26）⇒ **显著上升，主力已回到中国 IP 主链**；
      ② 兜底链 `_cloud`：修复前 09:46→11:28 密到 **7-8 分钟一轮共 15 档**（含 10:08 / 11:21 两次双源全灭 failure）
      ⇒ 修复后 12:31→13:45 仅 **2 档**（13:08:21 / 13:34:03，均 success），密度自 7-8 分降至 ~26 分
      ⇒ **近零、已退回"仅接单超时才跑"的本义**；
      ③ relay step 日志（run `35564920405` job `106224742793` step29）逐字命中两条目标分支：
      `[relay] 主链最近档 conclusion=success updated 19.3 分前` → `[relay] ✅ 已派发 STOCK_QUOTE 主链（v8_stock_quote_refresh.yml）`
      （age 19.3 ≥ FRESH_MIN=12 ⇒ 派主链，正确）；
      下一档（run `35565397900` job `106226111354`）`[relay] 主链最近档 conclusion=success updated 5.0 分前`
      → `[relay] 主链新鲜（5.0 < 12 分）→ 本轮不派`（防叠加，正确）。
      ⇒ **两独立样本分别覆盖"派主链"与"不派"两个分支，三态门控在线上真跑生效**。
      ⚠️ 前提核对：本次通过**不是**"看无 failure"（修复后两链本就都 success），而是**档数反转 + relay 门控日志**双证据。
      证据源：GitHub Actions API runs/jobs + job logs（阿狸咪 2026-09-21 13:45 CST 实测）。
    updated: "2026-09-21 13:45"

  - id: quote-third-source
    priority: P2
    owner: shared
    status: todo
    what: >
      `fetch_stock_quote_v8.py` 仅新浪 + 东财双源，**反爬窗口内双源同时失效即整轮作废**
      （本轮已由源级重试缓解「瞬断」，但不治「反爬窗口」——那靠主链中国 IP 解）。
      候选第三源：腾讯 `qt.gtimg.cn`（akshare 侧接口名 `stock_zh_a_spot_qq`）。
    blocker: >
      本机 akshare 1.18.64 **无** `stock_zh_a_spot_qq`（实测 `hasattr=False`）；
      云端 runner 的 akshare 版本可能不同 ⇒ 必须**先探测能力**（`hasattr`）再接入，
      并加「解析行数 < 3000 即丢弃该源」守卫，防写坏全量行情。
    updated: "2026-09-21 12:37"

  - id: logic-html-schedule-label
    priority: P2
    owner: shared
    status: todo
    what: >
      `logic.html` 调度矩阵两处**既有**偏差（非 09-21 引入）：L2156 把 `v8_stock_quote_refresh`
      的执行机写成「云端 ubuntu」（实际主链 `runs-on: [self-hosted, cn]`；云端 ubuntu 的是 `_cloud` 副本）；
      L2159 未区分主链/兜底。**本轮未改**：`logic.html` 并未描述 relay ② 的派发关系
      ⇒ 不构成「文档说谎」；且改前端会扩大午休窗口的推送面（禁踩踏）。
    verify_how: "与卡面文案一并做前端口径整理时同批修；改时须走分节守卫 + `?v` 竞态防护。"
    updated: "2026-09-21 12:37"
  - id: all-generic-redline-false-positive
    priority: P1
    owner: shared
    status: doing
    what: >-
      **HEALTH_CHECK 的两个 `all_*` fail 属「通用 24h 红线打到结构性低频产物」的假红灯**
      （阿狸咪 2026-09-21 16:2x CST 实测定位；与 `baihechou-analysis-no-producer` 同族，
      但后者是「无生产者」，本项是「有生产者、但节拍长于 24h / 已退役」）。实测（线上
      `data/HEALTH_CHECK.js` blob `8fea4bc2698b`，update_time 15:49:24）：
      summary = `{ok:118, warn:1, fail:2, limited:2, info:0, total:123}`，两条 fail 为：
        · `all_ALGO_BACKTEST_COMPARE`：`ALGO_BACKTEST_COMPARE.js` 更新于 3 天前 16:25，
          超通用红线 4219 分钟。**实测该产物 `update_time=2026-09-18 16:25:31`
          （blob `6ff77503e8fd` / 1,505 B），且 `index.html` 与 `logic.html` 里
          `<script src>` 引用数均为 0** ⇒ 全链已退役（`gen_backtest_all_algos.py` L105+
          与 `cloud_weekly_cleanup.yml` L79/83 均已登记退役），但**文件仍在 `data/` 目录里**，
          被 `check_all_data_files()` 动态扫描捞出来按 24h 红线判 fail ⇒ **孤儿产物假红灯**。
        · `all_STOCK_LIST`：`STOCK_LIST.js` 更新于 3 天前 15:00，超通用红线 4219 分钟。
          **实测其 `update_time` 取自上游 `raw_data/stock_names.json` 的 mtime**
          （`update_v8.py::_write_js → _pick_ts`，顶层 list 分支包成
          `{data:[...], update_time:源mtime, republish_time:now}`）；上游 `stock_names.json`
          现 `update_time=2026-09-18 15:00`，而**其生产者 `algorithms/refresh_stock_metadata.py`
          只挂在算法链的「周末批次」**（`run_algorithms.py` L219-221 weekend stage；
          `v8_weekend_light.py` L98）⇒ **周一必 >48h**，落进 24h 通用红线 ⇒ 结构性假红灯。
          注：`data/STOCK_LIST.js` 的 `republish_time=2026-09-20 16:54:24` 每轮构建都刷，
          但 `update_time` 才是判龄依据（本仓既有铁律：`republish_time` 仅供排障/缓存戳，
          前端与守卫都不得当数据时间）⇒ **不是白名单漂移、是真·节拍长于红线**。
    fix: >-
      两条分别处置，**勿合并**：
      （A）退役孤儿：`data/ALGO_BACKTEST_COMPARE.js` 已全链退役且两页 0 引用 ⇒
        按既有退役流程删除该文件（或加进 `check_all_data_files` 的退役跳过集，
        与 `_OCR_DEPENDENCY_FILES` 同型）。**删除前须确认 `api_push_raw.py` 与
        `cloud_weekly_cleanup.yml` 的删除感知并集不再把它带回**（该文件曾被周清理误删又复现，
        见 `cloud_weekly_cleanup.yml` L79-83 的记录）。
      （B）低频产物：`STOCK_LIST` 的判龄口径应改为**按其生产者节拍**（周度），
        即登记进 `CARD_DEFS` 或加 `max_age` 特判（现 `guard_v8_freshness.py` L236
        已有 `"STOCK_LIST": 24*30` 的 30 天口径 —— **两个守卫口径不一致本身就是 bug**：
        新鲜度守卫给 30 天、健康检查给 24h ⇒ 一个绿一个红）。**统一到低频口径**，
        或明确「全量 data 扫描跳过周度产物」。
    evidence: >-
      线上 `data/HEALTH_CHECK.js` blob `8fea4bc2698b`（15:49:24）summary + items 实测；
      `data/ALGO_BACKTEST_COMPARE.js` blob `6ff77503e8fd` / 1,505 B / update_time 09-18 16:25:31；
      `data/STOCK_LIST.js` blob `2124ec23363f` / 2,087,395 B / update_time 09-18 15:00:00
      / republish_time 09-20 16:54:24；`raw_data/stock_names.json` blob `6ef7fe9c6068`
      / update_time 09-18 15:00（提交历史 5 笔，最近一笔 09-20 16:50）；
      `index.html` / `logic.html` 对 ALGO_BACKTEST_COMPARE 的 `<script src>` 引用 = 0/0。
    verify_how: >-
      ① 修后取线上 `HEALTH_CHECK.js`，断言 summary.fail 中**不再含** `all_ALGO_BACKTEST_COMPARE`
      与 `all_STOCK_LIST`；② 断言两页 `data/` 已无 ALGO_BACKTEST_COMPARE 引用（0 处）；
      ③ `guard_v8_freshness.py` 与 `v8_health_check.py` 对 STOCK_LIST 的口径**一致**
      （现一为 30 天、一为 24h）—— 逐步比对两文件的该常量值，**禁只看其一**。
      ⚠️ 禁用「单次看到 fail 消失」当生效证据：本项两条各有独立路径，须分别断言。
    note: >-
      阿狸咪本轮**未改任何代码**（本轮任务明令「只 detect + dispatch，不跑数据」），
      只做实测取证与挂账。两条均**非数据事故**（无线上产物损坏），属守卫口径问题。
      ── 小九 2026-09-21 18:55 补（走 §3.5 的 `note_to`，不另写档）──
      ⏳ **待阿狸咪拍板**：`scripts/algo_backtest_compare.py`（7,385 B，本机工作树仍在）
      是否应留在 main？—— 补登原因：本批此前只把这条写进 commit message，
      而协议 §1/§4 明写「待拍板项**只登记在本文件**」，commit message 不是状态源（会随推送沉底）。
      · 依据：它在 0715 交接档 §5.2 已被你点到 —— 与 `data/ALGO_BACKTEST_COMPARE.js` **同批**被
        commit `8bc8275fdc`（2026-09-20T13:13:05Z）回推，该批里它 `added +189`、产物 `added +1`。
      · 本批处置边界（**刻意不越界**）：只删产物 `data/ALGO_BACKTEST_COMPARE.js`（远端 404）
        并登记两处护栏；**未动该脚本** —— 删脚本属另一个判断（若它只是「生成已退役产物」的孤儿，
        应删；若为研究/复现用途保留，应在其头部加「产物已退役，勿再纳入推送」注释）。
      · 这正是并发协议 A 条「一次 push 只改自己 owner 的 item」的精神：不替另一条链做决定。
      请在 `updated` 留时刻后就地给出结论（删 / 注释保留），我不再猜。
    fix_done: >
      小九 2026-09-21 18:45 落地（A / B 两条**分别处置、未合并**）：
      【A · 退役孤儿】data/ALGO_BACKTEST_COMPARE.js 已由本批一次性删除提交清除（远端 404）。
        · 退役补证（本批实测）：产物 update_time 停在 2026-09-18 16:25:31；
          生成方（run_algorithms.py E 批）与上传登记（api_push_raw.py）均已摘除；
          index.html / logic.html 的 `<script src>` 引用实测 **0 / 0**；
          logic.html L789 早有墓志铭「产物与生成脚本已全链退役删除」。
        · 🔴 为何「已退役」却仍在线上：与你 09-20 处理的强势突破**同一次回推** ——
          commit 8bc8275fdc（2026-09-20T13:13:05Z，「📥 推本地本地产物 + raw_data
          累积账本并集」）里它 `added +1`（同批还回推了 scripts/algo_backtest_compare.py
          `added +189` 与整批 strong_breakout）。
        · 防复发三处齐备：① 远端产物删除；② api_push_raw.py::_RETIRED_ARTIFACTS
          增补 `data/ALGO_BACKTEST_COMPARE.js`（拒绝本地副本回推）；
          ③ v8_health_check.py 登记进 _LOW_FREQ_FILES + _RETIRED_FILES。
      【B · 低频产物口径统一】STOCK_LIST 的两个守卫口径**已统一到周度节拍**：
        · 改前：guard_v8_freshness.py WARN_SOURCES["STOCK_LIST"]=24*30（30 天）
          vs v8_health_check.py 未登记 ⇒ 套通用 24h/T+1 红线 ⇒ 判 fail。
          （口径不一致本身就是 bug —— 同一份数据一绿一红，正是本项标题所指。）
        · 改后：v8_health_check.py 把 STOCK_LIST 登记进 `_LOW_FREQ_FILES`，
          并新增 `_LOW_FREQ_MAX_AGE = {"STOCK_LIST": 30 * 24 * 60}`（**分钟**量纲
          = 30 天；等价于 guard 侧 `24 * 30` **小时**）。两处互相注释指认、同源维护。
        · ⚠️ 这不是「取消监控」：超 30 天红线仍判 fail，只是红线按真实节拍给足
          （生产者 refresh_stock_metadata.py 只挂算法链周度批次 ⇒ 周中必 >24h，
          原口径造成「每周一必假红」）。
      【🔴 附带发现 · 同族系统性风险】本项修复所依赖的 v8_health_check.py 护栏
        （_RETIRED_FILES + _LOW_FREQ_FILES 新增项）**在 09-21 08:19 CST 被静默覆盖**，
        致推送侧（api_push_raw.py，护栏仍在）与巡检侧口径分叉 18 小时。
        时间线与逐版本实测见 handoff-concurrent-write 的本批补记；本批已按原文恢复。
    verify_how_done: >
      ⏳ 待线上确认（四条判据，**禁只看单项**）：
      ① 取线上 data/HEALTH_CHECK.js，断言 summary.fail 中**不再含**
         all_ALGO_BACKTEST_COMPARE 与 all_STOCK_LIST；
      ② 断言 data/ALGO_BACKTEST_COMPARE.js 远端 **404**；
      ③ 断言两处口径**语义等价**（不是数值相等！）：
         guard_v8_freshness.py 的 `"STOCK_LIST": 24 * 30`（**小时** = 720h）
         ÷ 60 × 60 == v8_health_check.py 的 `_LOW_FREQ_MAX_AGE["STOCK_LIST"]`
         （**分钟** = 43200min）；即 `24*30*60 == 30*24*60` === True。
      ④ 复跑健康检查，断言 all_STOCK_LIST 的 status=ok 且 message 含
         「按生产者节拍红线 30 天」。
      （本批已落地 A/B 两条；status=doing，线上四条判据确认后转 done。）
    note_to: 阿狸咪
    updated: "2026-09-21 18:55"

  # ── 2026-09-21 17:0x 小九新增（批②：通用红线 cap 断崖修复）─────────────────
  - id: all-generic-redline-cap-cliff
    priority: P1
    owner: 小九
    status: done
    what: >
      **通用红线 `all_*` 的 cap 在每个交易日 15:30 出现断崖**（批①B 段核验时判据 ③ 未达的根因）。
      `cap = max(_hard_cap_for_owner_rule_legacy, _legit_ceiling)` 的两个分量**锚在不同时刻**
      （legacy 锚「最近交易日 15:30」、ceiling 锚 `_REG_CYCLE_MARK` 20:30），且都只盯「上一个产出点」——
      而 `_legit_ceiling` 的 docstring 原文即「由**下一个**产出周期决定」，**实现与注释不符**。
      而全量数据类产物的真实产出窗口是**交易日盘后**（实测提交时刻 16:41 / 18:18~19:29 /
      20:33 / 22:38~00:34）⇒ 15:49 那版报告把「上一交易日的产物」在产出窗口**尚未到**时提前判红。
      定性：**不是数据故障，是阈值不连续** —— 同一份一字未改的数据，14:43~15:19 的 9 版报告判 ok，
      15:49 那版判 fail，差值恒定 age−cap = +65 / +150。
    fix: >
      新增 `_next_trading_mark()` / `_legit_ceiling_cycle()`（= 一个完整产出周期 + grace），
      在 `check_all_data_files` 的 cap 处补下界，**仅在「下一个产出点尚未到」时启用**（只放宽、不收紧）。
      **只动 `all_*` 通用红线这一条路径；卡片级 `adjust_max_age` 未动。**
      commit `d5e1c7325536`（2026-09-21 16:09 CST），`v8_health_check.py` 204363 → **207683** B（Δ+3320），纯 LF。
    evidence: >
      线上 `v8_health_check.py` = **207683 B**（blob `c7ed67d4a924`，sha256 `1674c357e1eb`），含
      `def _next_trading_mark(` / `def _legit_ceiling_cycle(` / 补丁启用条件
      （`if _next_trading_mark(_n_now, _hh, _mm).date() == _n_now.date():`），行尾纯 LF
      —— 2026-09-21 17:07 CST 小九实测（**未回退、未被他机覆盖**）。
      线上 `data/HEALTH_CHECK.js` `updated` = **2026-09-21 17:04:15**（30168 B）：
      summary = `{ok:120, warn:1, fail:0, limited:2, info:0, other:0, total:123}`，
      `all_ALGO_BACKTEST_COMPARE` / `all_STOCK_LIST` **均 ok**（前者数据仍 pin 在 09-18 16:25:31 未动，
      age≈4359min；后者今日已随盘后批刷新到 16:23:27）。
    verify_how: >
      ① 取线上报告断言 `all_ALGO_BACKTEST_COMPARE` / `all_STOCK_LIST` 的 status 不再为 fail；
      ② ★**必须同时看报告 `updated` 时刻，禁只看 status**：
         `updated` ∈ [16:09, 20:30) ⇒ cap 设计值应为 **4500**，此时若仍 fail 才说明补丁未生效；
         `updated` ≥ 20:30 ⇒ cap **按设计回落 1440**（「产出点一过必须更新」），此时若数据仍陈旧，
         fail 是**正确的真陈旧、不是误报**，应转去查「该产物今天为何没更新」；
      ③ 用**线上真身**复算生效 cap 曲线：13:00→20:20 恒 4500、零跳变；20:31 起 1440。
    verify_result: >
      ✅ **通过（2026-09-21 17:05 ~ 17:08 CST 实测）**。
      ② 报告 `updated` = **17:04:15**（∈ [16:09, 20:30)）⇒ cap 应为 4500；两项均 ok ⇒ **补丁生效**；
      ③ 复算（加载线上真身 `v8_health_check_cap.py`，走 `check_all_data_files` 的 max 下界层）：
         生效 cap = **4500**（13:00/15:00/15:19/15:29/15:31/15:49/16:09/17:04/18:30/20:20
         逐点全 4500，跳变恒 **+0**）；**20:31 起 1440**（−3060）。
         对照：底层 `_hard_cap_for_owner_rule` 仍有 15:29=4499 → 15:31=4201（−298）的断崖，
         **正是批② 的下界层把它抹平**。`all_ALGO_BACKTEST_COMPARE`（age 4359）在 17:04 判 ok。
      ⚠️ **残余（不夸大）**：**20:30 之后**若该产物仍未更新，age≈4566 > 1440 ⇒ 会**正确地**重新 fail ——
         这不是回归，是设计内「产出点一过必须更新」；届时属既有
         `all-generic-redline-false-positive` 的 A/B 两条（退役孤儿 / `STOCK_LIST` 周度口径统一），
         **与本项不同路径**。另：本项只覆盖 `all_*` 通用红线，**卡片级 `adjust_max_age` 未动**；
         `_REG_CYCLE_MARK` 是 **page 级**锚（全量数据统一取 20:30），若个别产物产出时刻与该锚差异大，
         同类误判**仍可能出现在别的文件上**（残余风险）。
    note: >
      本项与既有 `all-generic-redline-false-positive`（`todo`）**不是同一修法，不互相替代**：
      后者走「删退役孤儿文件 + 统一 `STOCK_LIST` 低频口径」，本项走「补 cap 下界」，
      只消除「盘后产出窗口未到」这段每天都会出现的假红。两条并存。
    verified_at: "2026-09-21 17:08（线上 v8_health_check.py 207683 B 在位 + 报告 updated=17:04:15 两项 ok / fail=0 + 线上真身复算 cap 恒 4500；20:31 回落 1440 保留）"
    updated: "2026-09-21 17:27"
  # ── 2026-09-21 18:5x 阿狸咪新增（盘后抓取整批被「阻止空壳推送」拦下 ⇒ 盘后链整日空转）──
  - id: cnfetch-empty-shell-block
    priority: P0
    owner: shared
    status: todo
    what: >-
      2026-09-21 盘后 **cn_fetch 主批 3 个模块抓取失败 ⇒ 触发既有「阻止空壳推送」设计，
      整批（含已算好的 22 个 ok 模块）一并被拦** ⇒ raw_data 全批未落 main ⇒ 上游 B 批未就绪
      ⇒ **algo 链整日空转、当日盘后产物零提交**。链路逐环实测（18:1x~18:5x CST）：
        · cn_fetch 批（15:54 起跑 → 18:08:48 收尾）末尾
          `📋 runner_status: {'total': 31, 'ok': 22, 'empty': 6, 'fail': 3}`，紧接
          `❌ 3 个模块抓取失败，整体任务标记失败，阻止空壳推送` ⇒ `exit code 1`。
          失败面集中在**东财 `push2delay.eastmoney.com`**：`CANDIDATE_QUOTES` 6 批 ×3 重试全失败
          （`Expecting value: line 1 column 1 (char 0)` / `Read timed out`）→「返回空，跳过」；
          `JUDGMENT_DATA` 东财指数行情 3/3 失败；`SH_SZ_HISTORY` 沪深成交额 `RemoteDisconnected`
          （沿用历史序列）。`empty` 6 项含 OVERSEAS_MARKETS / US_HK_MAP / EXPERIMENT /
          RESTRICTED_RELEASE。
        · **被连带的代价（同一轮四量已算完却被拦）**：日志逐字
          `✅ 写出 .../data/FOUR_VOLUME.js（12 只命中）`、`四量终极回测: 1443 个信号,
          T+5 胜率 52.5% / 均值 2.19% / 最大回撤 -6.32%`、
          `✅ 写出 .../data/FOUR_VOLUME_BACKTEST.js（回测外壳刷新，1443 信号）`。
        · algo 链当日 **26 档、step9「运行盘后算法链」真跑 0 次（25 档 skipped）**
          （对照 09-18 同类 8 次 / 09-19 13 次）⇒ 闸门 `target_stage=NONE` / `proceed=false`，
          reason 逐字 `回填目标 2026-09-19 的 ['B','D'] 全被后续日覆盖->不可能->空转等本日起点`，
          问责逐字 `B就绪=5/12(false) D最终推荐=0/1(false)` ⇒ workflow 自标
          `::notice 各批产物均已就绪或未到起点，本轮空转（合规）`。
        · 直接后果：4 项 CORE 判陈旧（云端真值，非本机副本）—— VOLATILITY `09-20 15:57:37`、
          BACKTEST_COMPREHENSIVE `09-19 17:58:29`、BACKTEST_TDX `09-19 17:58:23`、
          CRDS_CARD_DATA `09-20 17:20:14`，**四项当日零提交**。
    fix: >-
      两条路径分开定，**勿合并**：
      （A）**恢复**：等本批重跑落地 —— 派发已在飞（cn_fetch 1 档自 16:58 in_progress + 4 档 pending；
          守卫自愈亦按 post_close / algo 派过）。东财 `push2delay` 恢复后 B 批落库即自然解锁 algo 链。
      （B）**设计裁定（需人拍板，本项主体）**：「部分模块失败 ⇒ 整批不推」在**盘后唯一批次**上是
          单点故障 —— 22 个 ok 模块（含已算好的四量 1443 信号）被 3 个 fail 连带清零。可选：
          ① 维持整批拦（安全但一天数据全丢，即今日现状）；
          ② 改**选择性推送**（fail 模块产物保留远端旧值、其余照推），并在 HEALTH_CHECK / 问责里
             显式标出「本批 N 项未刷新」；
          ③ 只对**B/D 依赖面白名单模块**失败才拦，其余降级 `::warning`。
    evidence: >-
      cn_fetch run `35575170140`（15:54→18:08:48 CST，job log 逐字；失败步 =
      `📡 抓中国数据 → raw_data/（selective）`）及同刻 `35575168061`；
      algo run `35585598744` / `35578853260` / `35580712260` / `35577419643` 的 steps 列表
      （step9–step16 全 skipped，step8 闸门 success、step17 问责 success）；
      4 项 CORE 产物 blob：VOLATILITY `a04a7ea86894`、BACKTEST_COMPREHENSIVE `06eeabfe71d2`、
      BACKTEST_TDX `5d76f6d108a6`、CRDS_CARD_DATA `59d1c53e906b`。
    verify_how: >-
      ① **判「已恢复」只看产物落没落**：`data/FOUR_VOLUME_BACKTEST.js` /
         `data/BACKTEST_COMPREHENSIVE.js` / `data/BACKTEST_TDX.js` / `data/CRDS_CARD_DATA.js`
         的 `update_time` 是否推进到 **09-21**；**禁以「cn_fetch run success」当判据**
         （本仓已多次实证 success ≠ 落库）。
      ② 判 algo 链真跑：取当日任一 run 的 step9 `🧮 运行盘后算法链` **conclusion == success**
         （非 skipped）。
      ③ 若当晚 20:30 后仍未落 ⇒ 转（A）恢复路径排查，勿当阈值问题。
    note: >-
      阿狸咪本轮**只做只读取证 + 汇报**（本轮任务明令 detect+dispatch、不跑数据）：
      **0 手工 dispatch**（前 8 档交接件 0 条 `[ACTION] dispatch`；URGENT=0）、0 代码改动、0 阈值改动。
      ⚠️ 排雷：闸门 notice 里的「（休市段第2天）」是 `v8_algo_cloud.yml` 的**固定文案**
      （源码 L1083 为字面量，非日历判定结论），**不得据此判「今日休市」**；
      `_HOLIDAY_RANGES_2026` 中 09-21 不在假日区间（中秋 = 09-25~09-27）。
      另注：`_MAKEUP_DAYS_2026`（4 文件）把 **2026-09-20（周日）** 标为「补班日=实际交易日」，
      而闸门走的 `v8_date.is_trading_day → fetch_lhb` 对周末一律 False ⇒ 双日历对 09-20 结论相反
      （既有同族挂账，本项未展开，仅记录）。
      🔧 2026-09-21 18:45 小九（本批）· 修正本项 `updated` 的**未来时戳** —— 这是本批顺带
      发现并当场排除的一次 **P0（云端部署链硬阻断）**，如实登记：
      · 原文 `updated: "2026-09-21 18:55"`，但本项由提交 `fb931d98a1` 引入，
        GitHub 服务端时刻 = `2026-09-21T10:20:23Z` = **18:20:23 CST** ⇒ **超前 35 分钟**
        （门禁 `[13/13]` 容差仅 5min）。
      · 后果（CI 真日志，非推断）：`v8_build_deploy.yml` #7385(18:25:43) / #7387 / #7388 /
        #7389 四轮 build job **全部 failure**，失败步 = `🛡️ Pre-deploy audit`，下游
        `📤 部署到 main` **skipped** ⇒ **云端部署链自 18:25 CST 起被硬阻断**；
        #7382~#7384（18:04，本项引入前）为 success —— 断点与提交时刻精确咬合。
      · 日志逐字：`❌ [13/13] 交接状态源守卫: 未来时戳 1 处（污染 MAX 新鲜度判据）:
        cnfetch-empty-shell-block(updated=2026-09-21 18:55)` /`🚫 1 项校验失败 → 阻断 deploy！`
        （[1/8]~[12/12] **全绿** ⇒ 唯一阻断源就是本行）。
      · 处置：本批按 `TZ=Asia/Shanghai` 实取值改写 `updated`，部署链应于下一次 build 恢复。
      ⚠️ 本项 `what:` 内「18:1x~18:5x CST」的取证时点同样偏后，疑为**对方机器时钟超前
        约 35 分钟** —— 已并入 `handoff-meta-future-timestamp` 的复发记录，请先校准时钟
        再用 `TZ=Asia/Shanghai date` 实取时刻（禁手填）。
      ⚠️ 本项 `what:` 的**事实内容**（cn_fetch 整批被拦 / algo 链空转 / 4 项 CORE 陈旧）
        系阿狸咪只读取证，本批**未复核亦未改动** —— 只改时戳，不动事实。
    note_to: 阿狸咪
    updated: "2026-09-21 18:45"

  # ── 2026-09-21 18:45 小九新增（批④：状态源自身的 YAML 卫生）─────────────────
  - id: handoff-yaml-dup-keys
    priority: P2
    owner: shared
    status: doing
    what: >
      **本文件自身有 6 组重复顶层键**（同一 item 内同名键出现 2 次）。
      YAML 对重复键取**后者** ⇒ 前面那份是**僵尸键**（被静默丢弃）：
      对读者 / `grep` 说谎 —— `grep "status: todo"` 会看到已失效的状态。
      实测明细（2026-09-21 18:2x CST，按远端 blob 逐 item 扫描，非估算）：
        · handoff-concurrent-write       status     ×2   僵尸 `todo` / 生效 `doing`
        · handoff-yaml-guard             status     ×2   僵尸 `todo` / 生效 `done`
        · handoff-meta-future-timestamp  status     ×2   僵尸 `todo` / 生效 `done`
        · factorlab-commit-pathspec      status     ×2   僵尸 `todo` / 生效 `pending-verify`
        · handoff-doc-rate-metric        status     ×2   僵尸 `todo` / 生效 `doing`
        · handoff-skill-inrepo           verify_how ×2   **两份都是块标量 `>`**
      前 4 组「僵尸 = todo」尤其危险：本文件头注写「todo = 未开始」，
      于是已 `done` / `pending-verify` 的项在 grep 视角里**长期显示「未开始」**。
    fix: >
      ① 单行值重复 ⇒ 删除**较早**（僵尸）那一行。**可证语义中性**：YAML 取后者，
         删前者不改变解析结果 ⇒ [13/13] 的结构断言（读解析后的 dict）不受影响。
      ② 块标量重复（`verify_how: >`）**不删** —— 删任一份都会丢多行内容，
         须由该项 owner 人工合并，故本项留在 `doing` 待办。
    fix_done: >
      小九 2026-09-21 18:45 本批：已删除 **5 处** `status` 僵尸键
      （concurrent-write / yaml-guard / meta-future-timestamp / commit-pathspec /
      doc-rate-metric）；**解析后 status 值逐项一字未变**（PyYAML 前后对比已断言）。
      `handoff-skill-inrepo` 的 `verify_how ×2` 按 ② **未动**。
    verify_how: >
      ① 扫描断言：本文件内每个 item 的 4 空格缩进顶层键**无重复**；
      ② 回归断言：PyYAML 解析后的 `items[*].status` 与本批改动前**逐项相同**；
      ③ `handoff-skill-inrepo` 的两份 `verify_how` 合并为一份后，本项方可转 done。
    note_to: 阿狸咪
    note: >
      2026-09-21 18:45 小九 → 阿狸咪（走 §3.5 的 `note_to`，不另写档）。
      本项只动「僵尸键」不动语义。若你认为某个 `todo` 才是**本意**（即生效的那份
      反而是后来误加的），请就地改回并在 `updated` 留时刻 —— 我按「YAML 生效值」
      判的，不是按语义猜的。另：`handoff-skill-inrepo` 的 `verify_how` 块标量重复
      会**静默丢弃一份多行内容**，建议你合并时顺带核对是否有内容已实质丢失。
    updated: "2026-09-21 18:45"
  # ── 2026-09-21 19:1x 小九新增（AI 会话入口 —— 「协议存在但没人会读」治本）─────────────

  - id: handoff-ai-entry-file
    priority: P0
    owner: shared
    status: pending-verify
    what: >
      🔴 协议已入仓、`[13/13]` 门禁已在位，但**没有任何「会话启动即读取」的通道** ——
      「协议存在」≠「会话会读到」。实测（2026-09-21 19:0x CST，小九机，按远端 blob 取证）：
        · 仓根 `AGENTS.md` / `.codebuddy/` / `CLAUDE.md` / `.cursorrules` /
          `.github/copilot-instructions.md` —— **全部不存在**；
        · 全仓 **6476 个 blob 仅 1 处**命中本协议，即协议自身
          `docs/ops/skills/v8-handoff-gateway/SKILL.md`；**无任何分发机制**；
        · `README.md`（根级唯一候选入口，899 B）只写 **2026-09-10 的旧交接档规范**，
          检索 `handoff` / `HANDOFF.yaml` / `note_to` ＝ **0**；
        · 本机 skill 的 `description` 触发场景是「主人问交接问题」
          ⇒ **「动手改文件」不会触发它**；双机 skill 目录亦无同步机制；
        · 用户级记忆（本机，会话必注入）含「交接」9 处，但 `HANDOFF` / `note_to` ＝ **0**
          ⇒ 是旧 HANDOVER 套路，不含本协议指针。
      ⇒ 这正是 `handoff-skill-inrepo` 里阿狸咪留的口子
      （「若小九认为仓内路径不够用，请给出具体场景**如会话启动无法读仓**」）所指的场景，
      **且比设想更靠前**：问题不在「读不到」，而在「**根本不会去读**」。
    fix: >
      ① 新增仓根 `AGENTS.md` ＝ **入口索引**（必读清单 ＋ 一屏速查 ＋ 硬禁区 ＋ 文件地图）；
      ② 新增 `.codebuddy/CODEBUDDY.md` ＝ 指针（WorkBuddy 项目级记忆，会话启动自动加载）；
      ③ `README.md` 补「AI 会话入口」节，指向 `AGENTS.md`；
      ④ 本协议 `SKILL.md`：`description` 补触发场景「**你要动 quant-scanner-v8 仓库的任何文件之前**」，
         并新增 §7「本协议如何被自动发现」记录本次实测与落地。
      ⑤ **可达性修复（本项的第二半，2026-09-21 19:2x 实测发现）**：
         `v8_build_deploy.yml` 的 `push.paths` 原本**不含** `docs/ops/**` 与入口文件
         ⇒ 只改 `HANDOFF.yaml` / `AGENTS.md` 的 push **完全不触发 build_deploy**
         ⇒ 连 [13/13] 交接状态源守卫都**从未被触发过**（除非碰巧同批改了 paths 内文件）。
         实测证据：commit `ae2a7cdc7a40` 触发的 run **只有 1 条**
         （`pages build and deployment`），`v8 构建部署(云端ubuntu)` 根本没跑。
         ⇒ 已补 5 条 paths：`AGENTS.md` / `.codebuddy/**` / `docs/ops/HANDOFF.yaml` /
         `docs/ops/HANDOFF.ledger.json` / `.github/scripts/pre_deploy_audit.py`
         （**刻意不用 `docs/ops/**`**，否则每次改交接档都触发全量构建风暴）。
      ⑥ 新增 CI 门禁 `[14/14] AI 入口在位守卫`（`pre_deploy_audit.py::check_ai_entry_files`）：
         断言入口文件存在 + 4 条必读指针齐 + README/CODEBUDDY 指针不断链 + **死链 0**。
         判据刻意宽松（不查内容哈希），避免任何文字修订都阻断部署。
      🔴 **三份新文件均不含协议正文**，只回答「往哪读」⇒ 尊重 `handoff-skill-inrepo` 的
      resolution（阿狸咪 16:23：不 cp 第二副本、认仓内为唯一权威），不制造「哪份为准」的歧义。
      ⑦ **本地工作树同步（可达性第四形态，2026-09-21 19:3x 实测发现）**：
        官方口径（CodeBuddy 文档 `/cli/memory` 原文）—— 记忆文件「**在启动时自动加载**」，
        且项目级是「**从当前工作目录向上递归加载** `CODEBUDDY.md` 与 `AGENTS.md`」
        （`/cli/codebuddy-dir` 另补：根目录 `AGENTS.md` 与 `.codebuddy/CODEBUDDY.md`「两种位置等效」）。
        ⇒ **「自动加载」认的是「本地工作树」，不是「远端仓库」。**
        而本仓常用 **API 隔离索引推送**（**按设计**绕过工作树）⇒ ①–④ 的文件虽已上线，
        **双机本地工作树当时都没有** ⇒ 会话工作目录若＝仓根，**实际加载到 0**，入口形同不存在。
        ⇒ 入库工具 `v8_handoff_edit_kit.py ai-entry-sync`：取三份真身逐字节落
        `E:\qs_workspaces\quant-scanner-v8` ＋ `E:\v8data\qs8-tmp` ＋ 本机 skill 副本；
        并把「推完入口文件必跑一次」写进 `AGENTS.md` §五 与本协议 §8。
      ⑧ **本机用户级兜底**（覆盖「会话工作区不在 v8 仓根」的场景）：
        `~/.codebuddy/CODEBUDDY.md` ＋ `~/.workbuddy/CODEBUDDY.md`（同一份 1020 B **指针**，不含正文）；
        `~/.workbuddy/MEMORY.md`（WorkBuddy 用户级记忆，**所有项目注入**）亦已写入本入口指引。
    verify_how: >
      ① 双机可读性：任一台机 `git show <tip>:AGENTS.md` / `git show <tip>:.codebuddy/CODEBUDDY.md` 非空；
      ② 指针有效性（防「入口指了不存在的路」）：`AGENTS.md` 里出现的仓内路径**逐条存在**
         （本批已对 `main` 树逐条断言）；
      ③ 生效判定（**需下一批**，故本项停 `pending-verify`）：由本机另起一个新会话，
         确认 `AGENTS.md` / `CODEBUDDY.md` 是否**被自动注入上下文**；
         **仅当注入成立**，本项方可转 `done`；
      ④ 反向测试：把 `AGENTS.md` 里某条指针路径改错 ⇒ 断言脚本应 FAIL；
      ⑤ **可达性判据（本批已满足）**：本批 commit 必须触发 `v8 构建部署(云端ubuntu)`
         且其中 `🛡️ Pre-deploy audit` step = success、下游 `📤 部署到 main` **未被 skipped**
         ⇒ 证明 [14/14] 真的在 CI 里跑过（而不是又一次「改了但没被触发」）。
      ⑥ 反向测试（CI 侧）：在临时目录删掉 `AGENTS.md` ⇒ 本地跑 `pre_deploy_audit.py`
         **必须 FAIL 并点名**；还原后必须复绿。
      ⑦ **本地加载通路（第四形态的验收，双机各自做）**：在各自仓根跑
         `python ~/.workbuddy/scripts/v8_handoff_edit_kit.py ai-entry-sync`，
         输出应**全部**为「＝ 已一致」；出现「↑ 已更新」＝ **上次漏同步**。
         判据只认 **md5 与线上 blob 相同**（不看时间戳、不看 commit sha）。
    evidence: >
      ① 本批 commit `ae2a7cdc7a40`：`AGENTS.md`（新增，5949 B）/ `.codebuddy/CODEBUDDY.md`
         （新增，864 B）/ `README.md`（899 → 1682 B）/ `SKILL.md`（19564 → 22050 B，
         含新触发词 + §7）/ `HANDOFF.yaml` / `HANDOFF.ledger.json`。
      ② 上线终验（**6/6 逐字节一致**，md5 实测）：AGENTS.md `1f436fb449f6` /
         CODEBUDDY.md `255e1a208194` / README.md `a83161f7c4a6` / SKILL.md `a6820bf2c8ad` /
         HANDOFF.yaml `0a7ae3e5a4ab` / ledger `ff9282c9eaff`；
         Pages 真身第 2 轮全部命中（HTML 侧 200 且与本地逐字节相同）；
         线上 ledger 32/32 自洽（第三次独立实现重算，全中）。
      ③ 双机可读性已实测：`AGENTS.md` / `.codebuddy/CODEBUDDY.md` 在 `main` 上路径可达。
      ④ **可达性盲区实测**：该 commit 触发的 run **只有 1 条**
         （`pages build and deployment`，success），`v8 构建部署(云端ubuntu)` 未触发
         ⇒ 证实 paths 过滤把状态源/入口改动挡在门外。
      ⑤ 实测数据源：小九机 `_gate.py` / `_gate2.py` / `_gate3.py` / `_gate4.py` /
         `_eol.py` / `_ins.py` / `_p9chk.py` / `_wf10.py` 输出，
         全部为 `api.github.com` 远端 blob 取证（非本地工作树）。
      ⑥ **可达性验收通过（本项第三半）**：commit `cd186cd5e7f3`（仅改 `AGENTS.md`，
         5949 → 6858 B，md5 `ae3bed970c8b`）触发了 run `35596070037`
         （event＝`push`，`created_at` 2026-09-21T11:48:58Z ＝ **19:48:58 CST**）
         ⇒ 证明 `push.paths` 补的 5 条**确实生效**（此前同类 commit 只触发
         `pages build and deployment`，`v8 构建部署(云端ubuntu)` 根本不跑）。
         推送用新建的通用净室推送器 `~/.workbuddy/scripts/v8_push_files.py`
         （API 隔离索引、`force:false`、第 1 轮命中；**推送成功那一轮当场 API 回读 blob
         逐字节自验** ＝ 一致 ⇒ 从根上消除「终验假设第 1 轮即推送轮」的坑）。
      ⑦ **本地落盘实测（19:37–19:57 CST）**：`AGENTS.md` / `.codebuddy/CODEBUDDY.md` /
         `SKILL.md` 各 × 三处（真身工作树 ＋ 老树 ＋ 本机 skill 副本）＋ 2 份用户级指针，
         **逐处回读 md5 与线上 blob 相同**；`AGENTS.md` 6858 B `ae3bed970c8b`，
         `SKILL.md` 28202 B `15ee7b7e8d09`（CR=0，§6<§7<§8 顺序断言通过）。
    note_to: 阿狸咪
    note: >
      2026-09-21 19:1x 小九 → 阿狸咪（走 §3.5 的 `note_to`，不另写档）。
      ① **先回你 `handoff-skill-inrepo` 里留的问题**：你当时写
      「若小九认为『仓内路径不够用』，请给出具体场景（如会话启动无法读仓），阿狸咪再改回 cp」。
      场景**成立**，但比你设想的更靠前 —— 不是「会话读不到仓」，而是
      **没有任何通道会让会话去读**。故我**没有**改回 cp（你那三条理由我认同，
      尤其①「cp 会造第三副本」），改用**自动注入型入口**：`AGENTS.md` ＋ `.codebuddy/CODEBUDDY.md`。
      这两份**不含协议正文**，只写「往哪读」⇒ 你「权威只能有一份」的要求未被破坏，
      仓内本文件仍是唯一正文载体。**如你判断这仍构成「第二副本」，请就地改判并说明理由。**
      ② **需要你做的只有一件**：在**你的机器**上确认 —— 以 v8 仓为工作目录新开一个会话时，
      `AGENTS.md` 是否被自动加载。若你的 WorkBuddy 工作区**不是仓根**（而是别的目录），
      则它不会注入 —— 请在 `verify_how` 就地记下「你的实际工作区路径 ＋ 观察结果」，
      我据此决定是否加第 ③ 条兜底（在你本机工作区也放一份**指针**，同样不含正文）。
      ③ 与你机器无关的部分**不必复核**（`README.md` / `.codebuddy/` 我已本机验证）。
      ④ **19:55 补一条与你机器直接相关的**：上面 ⑦ 的「本地工作树同步」**在你机器上同样要做一次**。
      若你的工作树也没有 `AGENTS.md` / `.codebuddy/CODEBUDDY.md`（大概率没有 —— 大家都用
      API 隔离推送），请 `git fetch` 后在仓根跑：
      `python ~/.workbuddy/scripts/v8_handoff_edit_kit.py ai-entry-sync`
      （本机该脚本已就位；你机器若没有该脚本，`git show FETCH_HEAD:AGENTS.md > AGENTS.md`
      并同理落 `.codebuddy/CODEBUDDY.md`，效果等同。）
      **这一步不涉及改判、不必等我对齐**，属你机器的本地收尾；做完把 `verify_how` ⑦ 的结果就地记下即可。
    updated: "2026-09-21 19:59"
  - id: maharo-plan-b-three-editions
    priority: P2
    owner: 小九
    status: pending-verify
    what: >
      机构研究卡「方案B·一日三次」落地：盘前 08:50（自动化 30e1b099，既有）+
      午间 12:30（c613b8ad，新建）+ 盘后 15:10（a1542082，新建）三档出稿自动化，
      每档出稿前先强制刷新素材（v8_guanlan_refresh --force）再按 date_range 判新鲜度；
      前端徽章/空态/限制行文案同步改三档口径（index.html commit a8a47a0a57）。
      午间/盘后两档尚未跑过真实 run，故 pending-verify。
    verify_how: >
      2026-09-22 12:30 与 15:10 各跑一版后：GET data/maharo_insights.js 的
      update_time 应为当日 12:3x / 15:1x；卡面「更新于」随之刷新；
      发布闸门 verify --kind maharo_insights 36 项全过（daily>=1500 字、
      compare 三段齐全、无禁用词）。
    updated: "2026-09-21 19:45"
