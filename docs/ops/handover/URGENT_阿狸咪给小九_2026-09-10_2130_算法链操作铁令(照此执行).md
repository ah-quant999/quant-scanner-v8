# 🔴 URGENT｜阿狸咪 → 小九：算法链操作铁令（照此执行）

**生效：2026-09-10 21:30 即刻起**
**适用：`.github/workflows/v8_algo_cloud.yml`（盘后算法链）**
**先决：动手前先 `git pull`。我 21:10–21:20 连推 4 个 commit 大改了该文件，你若还留着旧版改动，先 `git checkout HEAD -- .github/workflows/v8_algo_cloud.yml` 丢掉，别覆盖。**

---

## ⛔ 铁令一：派发一律不带参数

```bash
curl -s -X POST \
  -H "Authorization: token $TOKEN" \
  -H "Accept: application/vnd.github+json" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_cloud.yml/dispatches" \
  -d '{"ref":"main"}'
```

**跑哪批由闸门判，不由人判、不由钟点判。** 不要再写 `stage=`，不要再按小时猜。

`stage_ok=false` 时闸门会自己 `::error` + `exit 1` + 写 GITHUB_STEP_SUMMARY 紧急报告 —— 这是**正确行为**，不要绕过、不要手工补跑。

---

## ⛔ 铁令二：禁止清单（今晚血泪换的，条条有据）

| # | 禁止 | 后果 / 证据 |
|---|---|---|
| 1 | 人工指定 `stage=` | 今晚 21:07 的 **E cron 不带 stage** → 被旧「按钟点猜」逻辑猜成 E → B/D 一个没跑就抢跑回测（实锤 run `34480648858`，已取消） |
| 2 | 带 `bypass_stage_gate=true` | 绕过上游闸门 = 用陈旧数据算 = 假新鲜 |
| 3 | 恢复「静默闸门 V5」「探针路由」 | 已删、职责并入闸门。旧静默闸门固定看 B 批三模块 → **B 一跑完三模块就"新鲜" → D 批永远跑不了**，这就是「最终推荐一直补不回来」的深层原因 |
| 4 | 在 B/D 未产出当日 16:30 后数据前跑 E 回测族 | 今晚我 20:22 犯过：前 4 个回测脚本全部基于陈旧 B 数据白算（本轮已纠正，产物已还原） |
| 5 | 派发前不查是否已有链在跑 | 21:06:56 dispatch 与 21:07:01 schedule **5 秒撞车**，双跑同链 |

---

## 🧠 新逻辑：唯一决策点 = `.github/scripts/v8_stage_gate.py`

| 闸门看到 | target_stage | 说明 |
|---|---|---|
| 显式 `stage=X` 且**上游就绪** | X | 人工应急 |
| 显式 `stage=X` 但**上游未就绪** | — | ⛔ `stage_ok=false`：拒绝执行 + 红灯 + 紧急报告 |
| 未显式、A 缺 | A | 采集批 |
| A 就绪、B 缺（≥18 点） | B | 选股批 |
| B 就绪、D 缺（≥20 点） | D | 汇总批（final_recommend → 最终推荐） |
| D 就绪、E 缺（≥21 点） | E | 回测批 |
| 全就绪 | NONE | 空转（这才是真成功） |

**就绪判据 = 读产物内容里的 `update_time`**（不吃 checkout mtime）：今日盘后 = 日期 == 交易日 且 小时 ≥ 16。
**顺序从此是硬的**：A → B → **D** → E，下游绝不抢跑；上游没就绪就红着等人。

---

## 📍 当前状态（21:30，实查）

- `stage=B` run **`34480639973`** 正在**你的机器 lemoncat-cn** 上跑（21:07 起，step 11「运行盘后算法链」in_progress，含 `factor_lab_gen` 冷启动）。
- **你什么都不用做。** B 跑完后会自动接上：
  - 22:30 / 00:30 的接力 cron（今晚新加）→ 闸门判 **D** → 自动跑 final_recommend 刷新最终推荐
  - D 完 → 闸门判 **E** → 自动回测
- 21:30 的「盘后最终闸」自动化会先查有无链在跑 → 有在跑就跳过 ✅

---

## 🤝 分工边界

- 我的域：`v8_algo_cloud.yml`（盘后算法链）。
- 你的域：盘中 fetch 链（`v8_cn_fetch_cloud.yml` 等）。
- 你的 `6231ff76c`（派发源精简：删 `v8_slot_scheduler.py` + 2 个 workflow）与我的改动**文件无重叠** ✅ 方向一致，都在砍冗余设计，各砍各的域。
- ⚠️ 但请**别动 `v8_algo_cloud.yml`**；确需动，先在交接里留一句。

---

## 🔧 派发前 10 秒自查

```bash
cd /e/workspace/stock-scanner && git fetch origin main -q
C:/Users/HH20210606/.workbuddy/binaries/python/versions/3.13.12/python.exe .github/scripts/v8_stage_gate.py --root .
```

读 `target_stage / stage_ok / reason`。`stage_ok=false` 就别派。

---

## ⏳ 留给你复核（不紧急）

1. `dd85b0503`（21:04）推的 `CRDS_BACKTEST.js` 是**修复前**算的（B 今天从未真跑 → 上游陈旧）。B/D 重跑完成后需重算。
2. K 线缓存内容级日期闸我只加了 `calc_stock_rps.py` / `generate_top10.py`；同类消费者建议一并加：`backtest_expectancy.py` / `factor_ic_analysis.py` / `optimize_stop_target.py` / `v8/backtest_rps_offline.py`。
3. `verify_chain_outputs.py` 判据 `<06:00` 归上一自然日 → 建议改为「交易日 T 日 ≥16:30 才算 T 日盘后产出」。

---

**收到请在交接里回一句 ACK。** 有问题直接写来。

— 阿狸咪 2026-09-10 21:30
