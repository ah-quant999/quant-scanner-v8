# v8 每日19点审计报告 · 2026-09-07（周一 · 交易日）

**执行角色**：审计（本机只读检查 + 评估派发，禁止推仓）
**审计对象**：origin/main（当前部署态，本地落后 2 提交——均为今日 19:00 云端数据自动提交）
**高层结论**：🟡 结构全绿、数据侧异常——**算法链四卡停更 + 今日 GitHub cron 全灭 + 有 2 个 run 在飞（未派发，避免抢锁）**

---

## 1. Git 状态 ✅

| 项 | 结果 |
|---|---|
| 本地超前 commit | 0（无待推送） |
| 本地落后 origin/main | 2 提交（7da007812 / d13b8c615，均为今日 19:00 数据原子提交） |
| 工作树 | 干净（仅 6 个未跟踪审计/交接 md，无代码改动） |
| conflict marker | 无 |

## 2. index.html 结构审计 ✅（远端版 972,471 B）

| 检查项 | 结果 |
|---|---|
| script 配对（浏览器语义：剥注释+顺序解析） | **110/110 平衡**；裸计数 118/111 的差额均为 JS 字符串内动态加载器写法（与昨日远端完全一致，零回归） |
| 静态 id 重复 | 264 个 id，0 重复 |
| 静态区标签平衡 | div/table/tr/td/span/ul/li 全平衡 |
| guard_index_sections.py | ✅ GUARD_OK（体积 0.9MB < 1.2MB 上限） |
| guard_index_js_tdz.py | 4 处告警，与前两日**完全相同**，均为已知假阳性（_safeArr×2 / _renderPeriodBacktestCard / renderDataFreshness，调用点在函数体内或有 try/catch） |
| 远端 vs 本地 index.html 差异 | 纯 `?v` 缓存戳 47/47 行对称替换，无逻辑改动 |

## 3. data 注入审计 ✅ 无新增回归

- 有文件但未静态引用：`ALGO_BACKTEST_COMPARE`（死文件清理候选，挂起等处理）、`RUNNER_STATUS_HEALTH`（白名单）、`STOCK_PROFILE`（就地合成）。
- 被引用但无文件：`PORTFOLIO_COST`（08-30 已删，注释说明统一读 PORTFOLIO_DATA）、`ipo_score`（懒加载 fetch 兜底路径，有优雅降级）——均为已知设计。
- 进展：昨日挂起的 8 个死文件中 7 个（ALGO_TRACK/ANALYST_RATINGS/INDEX_HISTORY/LHB_7D/STOCK_RPS_DATA/CANDIDATE_QUOTES/STOCK_PROFILE 旧版）已从远端清除，仅剩 ALGO_BACKTEST_COMPARE。

## 4. 盘后数据新鲜度（交易日 · 基准=今日 15:00 收盘）🟡

| 数据 | update_time | 判定 |
|---|---|---|
| GOLD_POOL | 09-07 15:44 | ✅ |
| SH_FIB | 09-07 15:40 | ✅ |
| SECTOR_RS | 09-07 15:45 | ✅ |
| LHB_DATA | 09-07 18:52 | ✅ |
| TRIPLE_CONSENSUS | **09-04 23:09** | ❌ 停更（缺今日产出） |
| CANDIDATE | **09-06 16:18** | ❌ 停更（缺今日产出） |
| CRDS_CARD_DATA | **09-06 07:40** | ❌ 停更（缺今日产出） |
| TOP10_DAILY | **09-04 22:36** | ❌ 停更（缺今日产出） |

## 5. 算法链今晚运行实况（GitHub API 实时核实）🔴 关键发现

1. **今日 cron 全灭**：v8_algo_cloud 周一 0 条 schedule 触发（16:40 采集批 / 18:10 选股批均未自触发；昨日周日反而 3 条 success）。GitHub cron 静默失效顽疾再现。
2. 全靠手动 workflow_dispatch 兜底，今晚战况（CST）：
   - 17:35 repository_dispatch success（17 点不在选股窗口 gate 内，未产出三模块）
   - 17:40 / 18:07 两次 **failure**
   - 18:21 一次 cancelled + 同刻一次 **in_progress（仍在跑）**
   - 18:33 一次 **pending（排队中）**
3. **决策：未派发 trigger_algo** —— 已有 2 个 run 在飞/排队，再派发属抢锁+堆积（符合「不抢算法链的锁」约束）。内部 gate（交易日 && HH≥18 && 今晚未产出）也会自行放行在飞 run。

## 6. top10_daily 快照审计 🟡 与昨日持平

- 9 个快照未入库（06-14/06-15 + 08-14/08-21/08-24~08-28），与昨日完全一致，无新增；只报告未补 add。
- 20260901 快照文件仍缺（已知遗留）。今日（09-07）快照尚未生成——与算法链停更同源。

## 7. 已触发云端 workflow

- **无派发**。零 post_close / 零 algo dispatch（见第 5 节决策依据）。

## 8. 需主人关注 ⚠️

1. **20:00 汇总批(D) 与 21:00 回测批(E) 大概率也 cron 失效**（今日 schedule 全灭）。若 18:21/18:33 两个在飞 run 失败，今晚三模块（三重共识/候选池/CRDS）将断档至明早。**建议 20:30 后复查一次数据时间戳；若仍停在 09-04/09-06，需手动 workflow_dispatch 或授权派发**。
2. index.html 无严重语法/DOM 错误 → 未发邮件告警（本会话亦无 send_mail 能力）。
3. 代码零改动、零推送，无需主人处理上线。
