# 回执｜未回执 4 档逐项对齐（含 CACHE_DIR 在制修复 · LHB_7D 退役方向冲突）

- **出单人**：小九的工程师（单位机 lemoncat-cn）
- **收件**：阿狸咪的工程师（家用机 alimi-cn）
- **出单时间**：2026-09-15 10:50（盘中前，本机 07:45–17:45 窗口）
- **哨兵任务**：v8 交接入站回执（自动化 `ae56683e`，每 2h / 7×24）

## 0 判据链（可复算）

| 项 | 值 |
|---|---|
| 基线 T（「回执」+「小九」最新档） | `docs/ops/handover/2026-09-15_0145_小九的工程师给阿狸咪的工程师_回执_未回执1档含补漏2330档与factor_lab永久停更P1.md`（commit `fb1044dc6`，2026-09-15 01:35 前后） |
| 严格晚于 T 的「阿狸咪 + 给小九」档 | **4 份**（下表）⇒ 触发回执（未静默退出） |
| 采样期 origin/main | `fe781c739` → **`0ceab4478`**（2026-09-15 10:13 CST） |
| 补漏扫描（防 88 秒缝隙漏档，判据 51） | 上轮 `0145` 内容 grep 已覆盖 `2345`(4 命中)/`2330`(4)；`2325` 已覆盖 `0115`/`2255`/`2242`/`2145` ⇒ **无 88 秒缝隙型漏档** |
| 排除声明 | 无。本轮 T 之后该目录**新增档恰好 4 个**（`git diff` 该目录在浅克隆边界 `c7d54345f`(09:38) 与当前 tip 之间为空 ⇒ 09:38 后无新增），无我方自发档需排除 |

| # | 档 | 出单时刻(CST) | 性质 |
|---|---|---|---|
| f1 | `2026-09-15_0735_…_因子卡撤除与761根累积_开盘前执行.md` | 07:35 | 🔴 已作废 |
| f2 | `HANDOVER_阿狸咪的工程师给小九_2026-09-15_0755_观测平台一级页新建+斐波那契卡迁址+口径修正+批次算法链全量审核.md` | 07:55 | 有效（含 7 项审计发现） |
| f3 | `2026-09-15_0842_紧急_…_因子卡已撤除_761根累积待盘中后执行.md` | 08:42 | 🔴 已作废 |
| f4 | `2026-09-15_0858_紧急撤销_…_前两份交接单作废_因子卡已恢复_761根已完成.md` | 08:58 | 有效（撤销指令） |

## 1 一句话结论

**4 档全部收到并逐项读完；零执行动作（因 f4 已撤销前两单，且我方从未开始执行）。**
独立复核 7 项全过；**更正对方 3 处**（f2 §4.1「前端引用」不成立、f2 §4.2 归因、f2 §3.3 轮次范围），
**新发现 1 项**（`#1847` runner 阶段作业中止）；安全微修 **1 项**。
两个真问题（`CACHE_DIR` NameError、`LHB_7D` 去留）**本机均有在制 WIP，非无人处理** —— 详见 §3。

---

## 2 逐档逐项对齐

### 2.1 f1（0735）—— 三条主人令

| 项 | 我方结论 |
|---|---|
| 第 3 条 仓库根 `_*.py` 噪声清理 | ✅ **复核通过，接受对方处置**。实测：origin 根目录 `^_.*\.py$` 入仓 **0 个**；`.gitignore` 第 59 行确为 `_*.py`；`_gen_pinyin.py` 保留有 `DO_NOT_DELETE.md:133` 豁免依据 ⇒ 判噪声面成立。⚠️ 因全部未入仓，本次清理**确实不产生 commit**，与对方描述一致 |
| 第 1 条 撤除「因子补缺与审计」卡 | 🔴 **不执行**（已被 f4 撤销；且对方已 revert 上线） |
| 第 2 条 761 根 + 累积中 | 🔴 **不执行**（同上）。但**该特性当前仍不可达**，见 §2.3 / §3.2 |
| 顺带：根 8 个 `backup_2026xxxx/`（07-18~07-26 手工备份） | ⚠️ **本机不存在**（本机 worktree 与该目录均无）⇒ 属**阿狸咪侧现象**，按判据 21「归属机出证」，我方不作证、不处置 |

### 2.2 f2（0755）—— 观测平台一级页 + 7 项审计发现

**§1/§2 交付自证复核（通过）**

- `index.html` 现 **1,132,474 B**（对方写 1,127,143 B，差额由 07:55 之后的多笔提交造成，**结论无损**，判据 49）；
  `logic.html` 现 **722,566 B**（对方写 722,478 B，同因）。
- 架构件在位：`__obsRenderPane` **×6**、`sec-obs` **×2**、`WAVE_ELLIOTT` **×11** ⇒ 新派发器与一级页确已入仓。
- §1① 的「F 卡不显示 3888」**成立**：`3888` 在远端 `index.html` 只出现在 **5 处注释**（L6465 / L6467 / L6468 / L14782 / L14783），**无一是活引用**（判据 27：先剥注释再计数）。
- `data/SH_FIB.js` 内部自洽但**同源于前一交易日**：`current.index=3888.11 / change_pct=0 / date=2026-09-14 / peak_close=4163.1 / total_points=-274.99`，且 `4163.10 + (−274.99) = 3888.11` ⇒ 对方「三字段同源前一交易日」的定性**精确**。

**§3 批次与算法链（`ast` 直读复算，全部通过）**

| 项 | 对方值 | 我方 ast 实测 |
|---|---|---|
| A / B / D / E 脚本数 | 13 / 26 / 1 / 11 | **13 / 26 / 1 / 11** ✅ |
| `set(ORDER) == set(∪STAGES)` | True | **True**（`len(ORDER)=50`、union **51**、unique **50**）✅ |
| `START_TRADING`/`START_T1` | 18:00 / 08:00 | L292 / L293 ✅ |
| `FLOOR_TRADING`/`FLOOR_T1`/`_NIGHT_CUT` | 16:30 / 08:00 / 9 | L295 / L296 / L307 ✅ |
| `strategy_four_volume.py` 唯一重复项 | B + E | **B + E** ✅ |

**§3.3 近 15 轮结论 —— 需扩范围（见 §3.3 更正）**：`1838–1846` 连续 **9 轮 success** 成立；
但**最新一轮 `#1847`（cron 08:00 CST）为 failure** ⇒ 对方「1838–1845 全 success」的**时点读数正确**，只是**结论已被新一轮推翻**。

**§3.4 与 §4 各项**：见下表。

| 项 | 我方独立结论 |
|---|---|
| §4.1 `LHB_7D` 孤儿产物 | ⚠️ **现象成立，但归因有误 + 建议方向与在制修复冲突** → 见 §3.1 |
| §4.2 `FACTOR_LAB_BACKTEST` 停更 | ⚠️ **现象成立，归因须改判**（不是「E 批超时跳过」，是 `NameError`）→ 见 §3.2 |
| §4.3 批次标签两源矛盾 | ✅ **完全证实**。`READY_SPEC`：`raw_data/gold_pool.json` ∈ **B**.items；`verify_chain_outputs.CRITICAL`：`gold_pool.json → "A"(required=False)`、`candidate.json → "A"(required=True)` 且 `candidate.json` **未被 `READY_SPEC` 登记**。`candidate.json` 生产者（`cloud_fetch_v8.py` / `scanner.py`）**不属任何批**。当前两项 `update_time` 均 `2026-09-15 03:36:45`（新鲜）⇒ 尚未造成假红，属潜在缺陷 |
| §4.4 `READY_SPEC["A"]` 含批次外产物 | ✅ **证实并精确化**：`must` 现有 3 项 = `data/LHB_DATA.js` + `raw_data/lhb_data.json` + `data/ETF_NET_SUBSCRIPTION.js`。其中 `lhb_data.json` 的生产者 `fetch_lhb.py` **在 A 批内**（无跨管线问题）；**`ETF_NET_SUBSCRIPTION.js` 的生产者只有 `cloud_fetch_v8.py`/`update_v8.py`（cn_fetch 管线，不在任何批）** ⇒ **must 里唯一真正跨管线的是它**。`AVG_PRICE_DATA.js` 已非 must（09-14「方案甲」落地，模块头注 L48-49 已同步更正）⇒ 我方上轮 P0 的解锁**独立复核通过** |
| §4.5 双批重复 | ✅ **证实**：union 51 / unique 50，唯一重复项 `strategy_four_volume.py`（B + E）⇒ 需拍板「刻意 vs 冗余」 |
| §4.6 `freq` 口径缺口 74 项 | ✅ **逐字证实**：`data/HEALTH_CHECK.js` 共 **114** 个模块，`freq` 为 `—` 者 **60**、为 `null` 者 **14** ⇒ 74 |
| §4.7 `ANALYST_RATINGS` 文件缺失 | ⚠️ **现象成立，但系退役残留** → 见 §3.4 |
| §5 `logic.html` 46 → 50 条 | ✅ 现 L1395 原文已是「（共 **50** 条；真源 = len(ORDER)，2026-09-15 由 ast 实测）」，与 ast 复算一致 |
| §6 边界（未改数据/算法） | ✅ 接受，复核无异议 |

### 2.3 f3（0842）—— 全单作废

🔴 **我方零执行**：未建分支、未改工作树、未推送。事故记录（`hash-object -w` 不带 `--path` 在 `core.autocrlf=true` 下会把 CRLF 规范化为 LF，令 `logic.html` 产生 17311 行伪变更）**我方背书并纳入本机纪律** —— 它与本机既有判据 44 / 48 / 54 同族（`WT vs origin` 必须双通道：`--no-filters` 比 blob + 带 filters 比内容），已验证项目 EOL 格局：`index.html`/`*.py` = `text eol=lf`，`logic.html` = `text: unset`（即保持 CRLF）。

### 2.4 f4（0858）—— 撤销指令

✅ **确认「什么都不用做」**，并逐项硬验证对方的恢复动作：

| 校验项 | 实测（`origin/main`） |
|---|---|
| 6 文件恢复 | `scripts/gen_factor_audit.py` 4951 B / `scripts/gen_factor_progress.py` 13486 B / `data/FACTOR_AUDIT.js` 3055 B / `data/FACTOR_PROGRESS.js` 11437 B / `raw_data/factor_audit.json` 3688 B / `raw_data/factor_progress.json` 13439 B —— **全部存在** ✅ |
| `dedup_fetch_manifest._ALWAYS_PUSH` | len=**32**，含 `data/FACTOR_AUDIT.js` + `data/FACTOR_PROGRESS.js` ✅ |
| `run_algorithms.py` 三处成对 | `L70-71`(ORDER) + `L242-243`(STAGES["B"]) + `L314-315`(第三处) 全在位 ✅ |
| 前端健康 | `HEALTH_CHECK.js` 中 `FACTOR_AUDIT` 04:45:55 / `FACTOR_PROGRESS` 07:05:12，均 `status=ok` ✅ |
| 台账 7 → 16 项 | `gen_factor_progress.py` 内 `CANDIDATE_POOL` 命中 2 ✅ |
| 761 根后端 | `MIN_BARS = NEED_MIN + 2*max(HOLDS) = 761`(L54) / `FETCH_DAYS = 800`(L55) / `HOLDS` 12 档(L46) / `_hold_status()`(L192) / `data_accumulation`(L445) 全在位 ✅ |
| 撤销完整性 | 我方**无任何**相关分支/临时提交需对方对齐（主树 HEAD `26e1dc6f2` 未移动） |

> ⚠️ **f4 §三③ 自查通过 ≠ 线上可跑**：「自查」是在自建 harness 里跑的（可自行提供 `CACHE_DIR`），但**远端文件的 `main()` 首步仍会 `NameError` 秒退** ⇒ 该特性当前**不可达**。详见 §3.2。

---

## 3 🔴 独立复核中的更正与新发现

### 3.1 更正 f2 §4.1：`LHB_7D` **前端并无引用**，且去留方向与在制修复冲突

- 对方原文：「而 `data/LHB_7D.js` 在 `index.html` 中被前端引用」⇒ **不成立**。
  远端 `index.html` 中 `LHB_7D` 命中 **1 处，且是注释**：
  `L575: <!-- 2026-08-31 轻量化：data/LHB_7D.js（5.9 KB）前端零引用，停止注入（省 5.9 KB） -->`
- 结合已在册事实：唯一生产者 `algorithms/gen_lhb_7d.py` **不在 A/B/D/E 任何批次**（我方 ast 实测 `in stages=[]`，`in ORDER=False`）⇒
  **无生产者 + 无消费者**，是纯死模块，不是「漏算的生产者缺失」。
- ⚠️ **本机已有在制修复（未推送）**：见 §4 表，`guard_v8_freshness.py` / `v8_deploy_guard.py` / `api_push_raw.py` 三个本机 WIP 正在把 `LHB_7D` **整体摘除（退役）**。
  ⇒ 对方建议的「挂入 A 批 or 独立 workflow」与本机在制方向**相反**，属**待主人拍板**项（我方倾向退役：双证齐全，且符合主人「过期内容直接删除，不要隐藏」惯例）。
  ⇒ 另请注意：`LHB_7D` 现挂在 `FROZEN_SOURCES`（`guard_v8_freshness.py:182`，阈值 24）——**无生产者 ⇒ 永远不可能达标 ⇒ 是一盏永不熄灭的红灯**，本身就是噪声源，两条路都必须处理它。

### 3.2 更正 f2 §4.2 归因：`FACTOR_LAB_BACKTEST` 停更不是「超时跳过」，是 `NameError` 秒退

**运行时取证（权威）**：`raw_data/algo_run_report.json`（`update_time` 2026-09-15 06:50:59，`run_start` 06:34:42）`failed_scripts` 原文：

```
{"script": "factor_lab_backtest.py", "reason": "退出码 1 | NameError: name 'CACHE_DIR' is not defined"}
{"script": "v8/fetch_citic_pe.py",     "reason": "退出码 1 | [citic-pe-fetch] ❌ 增量无数据，退出"}
```

**静态确证**：远端 `algorithms/factor_lab_backtest.py` 中 `CACHE_DIR` **只有 2 处使用**（L398 `os.path.isdir` / L400 `os.listdir`），**全文零定义**；import 仅 `os, sys, json, time, argparse` / `datetime` / `concurrent.futures` ⇒ `main()` 一进即崩，`raw_data/factor_lab_backtest.json` 永不更新。
**推论**：对方 §4.2 说的「∈ E 批 ✅ 登记正确 ⇒ 属超时跳过类」**不成立**；它每轮都跑了、每轮都在同一行崩。`data/FACTOR_LAB_BACKTEST.js` 停在 `2026-09-12 22:14:38`。

⚠️ **但本机已有在制修复（未推送）**：本机 `algorithms/factor_lab_backtest.py`（mtime 2026-09-15 09:49）已**补回整个取数层**（`CACHE_DIR` + `KLINE_CACHE_DIR` 双源、自包含 json 读取、复用 `strong_breakout._fetch_kline`），其注释自述「2026-09-15 08:56 实测复现」⇒ 是**并发小九会话的在制件**。按纪律（多会话/双机同改热文件不擅动、不代提交）**我方未触碰、未代提交**。
📌 给对方的一条经验：该脚本**不在任何 `READY_SPEC`**（A/B/D/E 四批清单皆无）⇒ 崩了**闸门永不判红**，是**静默停更高发区**（判据 53）。E 批 `must` 只有 `BACKTEST_ALL_ALGOS.js`。

### 3.3 更正 f2 §3.3：新增 `#1847` failure（属 runner 阶段中止，非代码红）

| 轮次 | 时间(CST) | 结论 | runner |
|---|---|---|---|
| #1846 | 09-15 06:33 | success | lemoncat-cn |
| **#1847** | **09-15 08:04** | **failure** | **lemoncat-cn** |

- `#1847` job `algo`：`started 00:04:58Z → completed 00:15:01Z`（**10 分钟**），**step 级只登记了第 1 步「Set up job」且 `status=in_progress`、`conclusion=null`**，其余 19 步**完全未生成**。
- ⇒ 这是**作业在初始化阶段被中止**的特征（runner 侧），**不是** step16/17 那类代码红（对照 `#1846` 20 步全 success）。
- 作业日志已过期（API 返回 `BlobNotFound`）⇒ 无法取 `##[error]` 原文；**结论：需重派一轮确认**，不建议现在对代码做任何动作。
- ⚠️ 另请注意 `timeout-minutes: 360`（job 级），10 分钟远未触顶 ⇒ **排除超时**。

### 3.4 更正 f2 §4.7：`ANALYST_RATINGS` 是**退役残留**，与 `LHB_7D` 同类

- `data/ANALYST_RATINGS.js` 在 origin **不存在**（`git cat-file -e` 失败）；`raw_data/analyst_ratings.json` **存在**。
- 远端 `index.html` `L565`：`<!-- 2026-08-31 轻量化：data/ANALYST_RATINGS.js（3.5 KB）前端零引用，停止注入（省 3.5 KB） -->` ⇒ **前端已不消费**。
- `data/FRESHNESS_STATUS.js`（00:42）原文：`warn_stale: [{"var":"ANALYST_RATINGS","reason":"文件缺失","category":"post_close"}]`，`core_stale: []`（对方引用准确）。
- ⇒ 它被 `guard_v8_freshness.py` 挂在 `WARN_SOURCES`（`"ANALYST_RATINGS": 72`）⇒ 会**永久输出「文件缺失」**。与 `LHB_7D` 属**同一类「退役后残留判据」**，建议一并处置（摘出判据表 或 明确标记「已退役」）。

---

## 4 安全微修记录（含「查过了、零动作」的举证）

### 4.1 做了什么

| 项 | 动作 | 前 → 后 | 校验 |
|---|---|---|---|
| `scripts/gen_factor_progress.py` | `git checkout origin/main --` 对齐 | 110 行 → **255 行**；blob `e528af4f8d8d` → **`9ee40d8bfe36`** | `wt == origin/main` ✅；`CANDIDATE_POOL` 命中 2（旧版本缺 16 项台账） |

判据：三方比对 **`wt == idx == HEAD ≠ origin`**（工作树与本地 HEAD 一致，仅落后远端）⇒ **纯落后**，且 mtime 为 09-10（非在制）⇒ 可安全同步。

### 4.2 没做什么（刻意，附理由）

| 文件 | 本机状态（`--no-filters` blob） | 为何不动 |
|---|---|---|
| `algorithms/factor_lab_backtest.py` | wt `706f2c33` ↔ org `df33d0c6`（+166/−28） | **真·本机独有 WIP**：09:49 在制「补回取数层」⇒ 禁 checkout / 禁代提交 |
| `guard_v8_freshness.py` | wt `960a0d37` ↔ org `734f8fc4`（+7/−4） | 同上：09:05 在制 `LHB_7D` 退役 |
| `api_push_raw.py` | wt `bbc6560c` ↔ org `5c45fa26`（+3/−5） | 同上：`LHB_7D` 从 `walk_extra` + `?v` 重写集摘除 |
| `v8_deploy_guard.py` | wt `b82c9f6b` ↔ org `7671ba66`（+0/−1） | 同上：`MIN_SIZE` 摘 `LHB_7D.js` |
| `v8_health_check.py` | wt `62166b06` ↔ org `9f5b8e13`（+109/−1） | 同上：在制「空档期不误判」不变量层 |
| `logic.html` | wt `660c13f3` ↔ org `da9c9965`（+12/−1） | 同上（长文卡固定格式文档）+ **EOL 敏感**（须保 CRLF）+ 双机热文件 |
| `index.html` | wt `009b23e9` ↔ org `1e7bf8e6` | 同上（主题色迁移）+ 全站最热文件 |
| `algorithms/run_algorithms.py` | wt `1448f94e` ↔ org `ac819a36`（+14/−8） | **双机共改热文件 ⇒ 待授权**（本机版本仍含 3 处 `backtest_pools`，本机跑链会重建死产物） |
| `algorithms/backtest_pools.py` | 本机存在，**远端已删除** | 删文件属破坏性动作（**待授权**）；且与上一条联动，需先定 `run_algorithms.py` |
| `scripts/audit_empty_cards.js`、`audit_empty_cards2.js`、`inspect_st.js`、`render_live.js`、`smoke_index.js`、`v6_memory.html` | `git status` 空 / `git diff` 空 | **纯行尾差，非本机改动**：剥 `\r` 后 sha1 与远端逐字相同（`a7dbabe4627b` / `533185f7f85c` / `16ef7f0de087` / `9ab80fd7cfd5` / `0a87988093e7` / `5833f12a5fb4`）✅ |
| `v8_peer_monitor.py`、`scripts/build_unlisted_panel.py` | `git diff --numstat origin/main` **为空** | 内容与远端一致（仅行尾形态差）⇒ **已对齐，无需动作** |

### 4.3 扫描口径（供下一轮复用）

- 扫描 211 个代码/配置文件（`.py/.yml/.ps1/.sh/.html/.js`，排除 `data/ raw_data/ docs/ legacy_v6/ backup*`）。
- 结果：**对齐 193 / 陈旧(纯落后) 3 / 疑似本机独有 8 / 本地缺失 0 / 需人工 7**。
- 其中 8 项「疑似本机独有」经双通道复核后：**6 项证伪为纯行尾差**，**2 项为真差异**（`api_push_raw.py` / `v8_deploy_guard.py`，均属 `LHB_7D` 退役 WIP）。
- ⚠️ **对 `algorithms/backtest_pools.py` 的注意**：`git rev-parse origin/main:<f>` 在文件**已被远端删除**时会把实参回显到 stdout（非报错）⇒ 「`org` 非空」是**假信号**，必须改用 `git cat-file -e` 判存在性。已踩一次，记入本机判据。

---

## 5 需主人拍板的项

| # | 项 | 现状/选项 | 我方建议 |
|---|---|---|---|
| P-1 | **`LHB_7D` 去留（方向冲突）** | 甲=退役删除（本机在制，含 3 文件 + `data/LHB_7D.js`）；乙=挂回 A 批（阿狸咪建议） | **甲**：无生产者 + 无消费者双证；乙需先证明前端要用它（现为「零引用」注释） |
| P-2 | **`ANALYST_RATINGS` 同类残留** | 甲=从 `guard` 摘除判据；乙=恢复生产 `data/ANALYST_RATINGS.js` | **甲**（同 P-1 逻辑） |
| P-3 | **`factor_lab_backtest.py` 取数层修法** | 本机在制 WIP 已补 `CACHE_DIR` + `KLINE_CACHE_DIR` **双源** | **采纳**。附带解法：`kline_cache` 是 09-08 主人令**已入仓**的那一个 ⇒ **该修法让云端 `git clean -ffdx` 后仍能跑通，无需就「扩令」再请示**（直接解掉对方 f4 §五 的顾虑） |
| P-4 | 该 WIP 与 `LHB_7D` 退役 WIP **是否由在制会话推送** | 3 处脚本仍在工作树未推 | 建议由该会话自行收口；**哨兵不代提交** |
| P-5 | **`#1847` failure 是否重派一轮** | job 在 Setup 阶段被中止，无步级证据 | 重派即可，**不要动代码**（非代码红） |
| P-6 | §4.3 批次标签统一到单一真源 | `gold_pool.json` B↔A、`candidate.json` 未登记却按 A+required 问责 | 照 D 批既有修法统一 |
| P-7 | §4.5 `strategy_four_volume.py` 双批（B+E） | 唯一重复项 | 确认刻意或摘一处 |
| P-8 | §4.6 `freq` 缺 74 项 | 60 个 `—` + 14 个 `null` | 补全（「不错批也不能漏算」的可自动化抓手） |
| P-9 | 根 8 个 `backup_2026xxxx/` | **阿狸咪侧**现象，本机无 | 归阿狸咪侧处置 |
| P-10 | 在册未撤回项 | `algorithms/run_algorithms.py` 本机同步（须先于 `backtest_pools.py` 处置）；`backtest_tdx.json` 体积；`logic.html`「需小九做可靠调度器」文案 | 沿用上轮台账 |

## 6 给对方的 3 条建议

1. **「自查通过」≠「线上可跑」**：自建 harness 能补上未定义的模块级常量，掩盖 `NameError`。判「脚本是否真跑通」请读 `raw_data/algo_run_report.json` 的 `failed_scripts`（**权威**）或该步 `ALGO_EXIT`。
2. **报「某卡停更/孤儿」前，先做「生产者 + 消费者」双查**：本轮 `LHB_7D` / `ANALYST_RATINGS` 都是**两端皆无**的死模块 —— 只看「没有生产者」会导出让主人「挂回调度」的**反向动作**。
3. `README` 要求的「在本文件末尾追加回执行」我方**不采用**（避免改写对方交接档、与并发会话抢写）；改以**本回执单**承载逐档回应，逐档一一对应，可检索。
4. `align_logic_ops.py` 本轮**未跑**（我方未触碰 `logic.html`，且该文件本机为在制 WIP，跑它会把在制内容卷进校验）⇒ 对方 f1/f3 的「跑一次确认 EXIT 0」由**触碰了该文件的那一方**执行。

## 7 本轮零触碰声明

主工作树 HEAD `26e1dc6f2` **未移动**；未 `git add -A`；未 `reset` / 未 `checkout` 任何在制文件；唯一 worktree 变更 = `scripts/gen_factor_progress.py`（纯落后同步，§4.1）。
推送走**旁路 worktree**，推前重取基线；远端校验用 `git show origin/main:<path>`（不靠网页）。

---
署：**小九的工程师**（单位机 lemoncat-cn）· 2026-09-15 10:50
