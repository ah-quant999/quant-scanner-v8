# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-13 09:22｜宏观观测页3卡版式统一+频率标注+RPS守卫补全

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

**优先级**：常规（无阻塞）
**发件机**：阿狸咪（家用机 `alimi-cn`）｜**收件机**：小九（单位机 `lemoncat-cn`）

---

## 一句话结论

宏观观测页 3 卡已统一为「卡名置顶 → 核心/用法/限制 → 正文」，并标注各自更新频率（①日频 ②事件驱动 ③月频）；顺手补全了 `index.html` 里 `_buildRpsMap` 缺失的函数存在性守卫，Console 告警归零。已上线 2 笔提交，CDN 字节级 + 真浏览器双重终验通过。

---

## 正文

### 一、主人令

> 都是卡名置顶，你的分析放在卡名下面。宏观观测页3卡是日频、周频还是月频更新要标准清楚

### 二、改动 1：三卡版式统一为「卡名置顶 → 分析在下」

改前不统一（主人截图指出）：

| 卡 | 改前结构 |
|---|---|
| ① 📚 机构研究 · AI 解析建议 | 卡名 → 徽章 → 副标题（**无**核心/用法/限制块） |
| ② 🌐 新周期判定 · 海外宏观 + AI 大周期 | ❌ 核心/用法/限制 → **卡名**（分析在卡名上方） |
| ③ 🌍 宏观景气背景 · K 型分层 | ❌ 核心/用法/限制 → **卡名**（分析在卡名上方） |

改后三卡完全同构：

```
卡名 + 频率 badge + 数据源
  ↓
📌 核心 / 💡 用法 / ⚠ 限制
  ↓
正文（卡① 为动态注入的 body；卡②③ 为内联静态内容）
```

卡① 另删掉副标题里重复的「每日 08:30 更新」（已提升为独立 badge，避免同屏重复）。

### 三、改动 2：三卡频率标注（主人问「日频/周频/月频」的答案）

| 卡 | 频率 | 页面 badge | 依据 |
|---|---|---|---|
| ① 机构研究 · AI 解析建议 | **日频** | `📆 日更 · 每日 08:30` | `data/maharo_insights.js` 的 `daily.generated_at` 每日更新（09-13 为 `07:22`）；出稿自动化在小九机 07:15，正文 08:30 更新 |
| ② 新周期判定 · 海外宏观 + AI 大周期 | **事件驱动**（非日/周/月） | `📢 事件驱动 · 手动维护` | 无自动管线，是 2026-09-06 手动维护的历史快照；下次更新需人工触发 |
| ③ 宏观景气背景 · K 型分层 | **月频** | `📅 月度更新 · 每月 1 日` | 沧海一土狗 K 型景气图，每月 1 日随宏观数据手动更新；当前 7 月数据，下次 2026-10-01 |

> ⚠️ 卡② 严格说不属于「日/周/月」任一档，是**事件驱动**。我按事实标注，未强行归入周频。若主人要求三卡必须都归入日/周/月，请告知，需主人另行拍板。

### 四、改动 3（顺手修复）：`index.html` 的 `_buildRpsMap` 守卫补全

**根因取证**：

```
$ git grep -n "buildRpsMap" FETCH_HEAD
FETCH_HEAD:index.html:14803:  try{ rpsMap=window.__rpsMapByCode||(window.__rpsMapByCode=window._buildRpsMap()); }catch(e){...}
FETCH_HEAD:index.html:15074:  var _rmap = window.__rpsMapByCode || (window.__rpsMapByCode = window._buildRpsMap ? window._buildRpsMap() : {});
FETCH_HEAD:logic.html:8148:   function _buildRpsMap(){          ← 定义在这里
FETCH_HEAD:logic.html:8154:   window._buildRpsMap=_buildRpsMap;
```

**结论**：`_buildRpsMap` 定义在 **`logic.html`**，而 `index.html` 并不加载那段 JS ⇒ `window._buildRpsMap` 在 `index.html` 里**根本不存在**。

- L15074 写法正确（有 `window._buildRpsMap ? ... : {}` 守卫）→ 静默降级不报错
- **L14803 写法有缺陷**（无守卫直接调）→ 每次渲染抛 `TypeError`，被 catch 成告警刷屏

2026-08-13 那次「跨 IIFE ReferenceError」修复**只改了一半**，两处调用不一致。本轮把 L14803 对齐 L15074 写法。

**效果**：真浏览器 Console 里 `[stockCard] rpsMap 失败` 由每次渲染刷屏 → **0 次**。

### 五、上线记录

| 项 | 值 |
|---|---|
| commit 1 | `17b54b59f`（三卡版式统一 + 频率 badge） |
| commit 2 | `b76207d72`（`_buildRpsMap` 守卫补全） |
| 远端 `index.html` | **1094249 → 1095781 字节** |
| CDN 终验 | `curl` 200，`cmp` 与本地派生物**零差异** ✅ |
| 真浏览器终验 | 三卡全部 `nameFirst: true`；三 badge 命中；Console `rpsMap 失败` = **0** ✅ |
| 部署前 5 门禁 | ✅ py_compile 118 文件 0 错 / new Function 25 块 0 错 / data 102 个 / align_logic_ops EXIT 0 / workflow 30 个有效 |
| `align_logic_ops.py` | **EXIT 0**（本轮为 UI + badge 文案改动，不涉 cron，无漂移风险） |

### 六、需要小九回执/确认的事项

1. **卡② 频率口径**：我标为「事件驱动」。若主人要求三卡必须归入「日/周/月」三档之一，请告知（但不建议硬凑周频，事实就是无管线的历史快照）。
2. **`maharo_insights.js` 出稿任务归属**（承接 `2026-09-13_0836`）：07:15 出稿自动化在小九机，请确认任务名与断更告警是否已挂。
3. **`maharo_macro.js` 是否彻底弃用**（同上）：`fetch_ai_insights_compare.py` 已改为优先读 `maharo_insights.js`、回退 `maharo_macro.js`。若确已废弃，可考虑从回退链移除并删数据文件。
4. **`v8_peer_monitor.py` 缺 `data/hb_xiaojiu.json`**（承接上轮）：导致双机掉线告警**永不触发**。安全风险，需小九机侧产出该心跳文件或改判据。
5. **上轮 11 份交接待答项**：`2026-09-13_0836` 交接稿里列的清单，你的回执停在 `2026-09-11_2315`，请补齐。

### 七、未改动 / 边界声明

- 本轮**未碰** `.github/workflows/**`（纯前端版式与文案改动）
- 本轮**未碰** `data/**`（无数据文件改动）
- 本轮**未碰**算法链、调度、cron
- 本机 `E:\workspace\stock-scanner` 工作树落后远端，全程走 `FETCH_HEAD` 派生 + 仓库外暂存 + plumbing 直推，**未执行 `git add`**

---

—— 阿狸咪的工程师
