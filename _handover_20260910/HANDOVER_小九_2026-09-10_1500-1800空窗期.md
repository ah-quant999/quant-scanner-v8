# 小九机 15:00–18:00 可下线确认单（2026-09-10 生效）

> 主人令：交易日小九机 15:00 收盘后可下线，18:00 前须重新上线跑算法链。此期间由阿狸咪兜底，不得踩踏/风暴。

---

## 一、核心结论

| 时段 | 小九机 | 阿狸咪 | 说明 |
|---|---|---|---|
| 09:30–15:00 盘中 | 在线，14 档主派 + 兜底 | 每 30 分只读补漏 | 已生效，见双机分工交接单 |
| **15:00–18:00** | **可下线/休眠** | **全权接管** | 无必须小九机 IP 的任务 |
| 18:00–21:30 | 必须在线 | 仅监控/告警 | 算法链/LHB/STOCK_QUOTE pinned 小九机 |

**必须小九机 IP 的任务（18:00 后才开始）**：
- `v8_algo_cloud.yml` 算法链：18:10 选股+因子 → 20:00 最终推荐 → 21:00 回测
- `v8_lhb_fetch.yml` 龙虎榜
- `v8_stock_quote_refresh.yml` STOCK_QUOTE

**结论**：15:00–18:00 之间没有上述 pinned 任务，小九机可以不在线。

---

## 二、15:00–18:00 阿狸咪接管清单（小九机下线后她负责触发）

当前这些任务仍由小九机自动化 ACTIVE 触发，小九下线后必须改为阿狸咪机触发，否则空窗。

| 时间 | 任务 | 当前小九机自动化 | 迁移方式 | 是否必须有人做 |
|---|---|---|---|---|
| 15:30 | 收盘数据刷新 post_close | `1785388522458` | 阿狸咪机新建等价自动化 | ✅ 必须 |
| 15:50 | IMA强势股兜底 | `25b6ed29` | 阿狸咪机新建/接管 | ✅ 建议（盘中兜底延续） |
| 16:30 | 港股收盘后 post_close | `automation-1788145998708` | 阿狸咪机新建等价自动化 | ✅ 必须 |
| 18:00 | 当日交接文档更新 | `1785719698947` | 阿狸咪机新建等价自动化 | ⚠️ 文档，数据不依赖 |

> 其余盘中自动化（`22505803`、`7bec83c8`、`ad288000` 等）因 BYHOUR 只含 9–15 点，15:00 后不会触发；但小九机若 15:00 前还有未完成的 run，会拖到 15:05 左右结束，不影响。

---

## 三、小九机下线前必做（防止空窗/风暴）

1. **确认 15:00 档已跑完**：`v8_cn_fetch_cloud.yml` 15:00 run 完成/部署成功。
2. **暂停小九机 15:00–18:00 的触发项**（避免小九突然上线后双机重复派发）：
   - `1785388522458`（15:30 收盘刷新）→ PAUSED
   - `25b6ed29`（15:50 IMA）→ PAUSED
   - `automation-1788145998708`（16:30 港股）→ PAUSED
   - `1785719698947`（18:00 交接文档）→ 可保留或 PAUSED
3. **通知阿狸咪**：在她机器上 ACTIVE 对应 4 个自动化，并验证 15:30/16:30 能正常派发。

---

## 四、18:00 前小九机必须回来的原因

- `v8_algo_cloud.yml` 18:10 B 档选股+因子计算 pinned 到 `[self-hosted, cn]` = `lemoncat-cn`（小九机）。
- 阿狸咪机 `alimi-cn` PATH 缺 `bash`/`git`，算法链跑不了（已六连败实证）。
- 若 18:00 小九机不在线，**选股+因子整轮缺失**，最终推荐和回测也无输入。

**建议**：小九机 17:45 前重新开机/恢复网络，17:50 检查 runner 在线状态。

---

## 五、防踩踏/风暴铁律

1. **同一时刻只有一个主派发源**：盘中 `22505803`（小九机）已固定；15:00–18:00 若阿狸咪建调度，rrule 只能接管上述 4 个时间点，不要设每 5/10 分高频。
2. **派发前必查在跑 run**：阿狸咪任何兜底动作先调用 GitHub API 检查 `v8_cn_fetch_cloud.yml` 是否有 in_progress/queued/pending，有才跳过。
3. **不本地直推 main**：双机都只调度/兜底，数据走 `window.X` 注入，部署走云端 build。
4. **concurrency 锁仍在生效**：`v8_cn_fetch_cloud.yml` 与 `v8_cn_fetch_cloud_selfhosted.yml` 共用 `group: v8-cn-fetch-cloud`，同一时刻仅一条 fetch 写 main。

---

## 六、待主人/阿狸咪拍板

- [ ] 阿狸咪是否确认在她机器重建 15:30/15:50/16:30/18:00 四个触发项？
- [ ] 小九机下线前是否立即 PAUSE 上述 4 个小九机自动化？（建议阿狸咪跑通后再 PAUSE，防空窗）
- [ ] 小九机 17:45 回来是否由阿狸咪提醒/闹钟？

---

**生成时间**：2026-09-10 09:00 CST  
**制定**：小九机 AI 助手 ｜ **接收**：阿狸咪
