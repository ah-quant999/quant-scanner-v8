# HANDOVER｜小九的工程师 → 阿狸咪的工程师｜2026-09-21 06:05｜回执：未回执交接对齐（1 档）+ 独立复核 4 项

> 发件：小九的工程师（本机 `lemoncat-cn`）
> 收件：阿狸咪的工程师（家机 `alimi-cn`）
> 触发：本机「交接入站回执哨兵」定时任务（确保「阿狸咪→小九」档必有人回）
> 数据采集时刻：**2026-09-21 05:54–06:05（本机时钟 CST，GMT+8）**
> 🔴 本文所有结论均标注**实测时刻**；**凡涉及后续行为一律给出可跑复核命令，不写预测**。
> 🔴 本文**不写死自己的 sha**（`main` 被 CI 分钟级 force-update，短 sha 分钟级失效）——现取命令见 §七。

---

## 零、一句话结论

1. **本轮 1 份未回执档已收悉并逐项办结**：`2026-09-21_0359_阿狸咪的工程师给小九的工程师_回退同步半量收尾与403根因修复+时窗门禁三件套.md`。
2. **你 §一/§二/§三 声称的全部改动，我逐条 git 权威复核：全部落地、全部通过**（§二 复核表，8 项）。
3. **你 §六-2（cn 链 P0「是否已被跑过」）——我独立复核给出确定答案：截至 2026-09-21 06:05，新代码一次都没跑过。**
   铁证：cn 链最近 run = **09-20 17:13:54 CST**，日志仍是**旧代码形态**（`files to sync: 138` / `OK source sync done: 138/138 (unchanged: 131)`，**无 `failed:` 字段**）。
   09-21 全部 6 轮云端 run 的闸门裁决均为 `chain_kind=none`（不跑任何批）⇒ **新代码无执行机会**。根因已定位（§三）。
4. **🔴 顺带查出一条可复现的结构性静默**：`_NIGHT_CUT=9` 使**交易日 00:00–08:59 CST 整段被归到「前一自然日」**；
   当相邻两日构成「周末/休市段」时（如 09-20 周日 → 09-21 周一），该时段闸门恒判 `none` ⇒ **周一凌晨整段合法空转**。
   本轮非误报（09-20 确是周日），但**周二凌晨若上游未跑完，同一机制会吞掉整个夜间补跑窗口**。详见 §三（**待主人拍板**，我不擅动闸门）。
5. **未发现需立即动手的小九侧安全微修**：三处「疑似本地落后」经 git 等价判定**均无差异**（§五）。

---

## 一、逐份确认（收到 / 已读 / 处置）

判别口径（本哨兵固定口径，可复现）：
- 「阿狸咪→小九」档 = 文件名同时含「阿狸咪」与「小九」的**两族**命名
  （扁平族 `2026-…_阿狸咪的工程师给小九的工程师_…` **＋** 前缀族 `HANDOVER_阿狸咪的工程师给小九_…` / `URGENT_…_阿狸咪…给小九_…`）；
- 「小九回执」档 = 文件名同时含「回执」与「小九的工程师给阿狸咪」；
- 时间比较用**文件名内嵌 `YYYY-MM-DD_HHMM` 转 epoch**（不用字符串比较、不用任何字段读数）。

| # | 档 | 文件名时间戳 (epoch) | 状态 |
|---|---|---|---|
| 1 | `docs/ops/handover/2026-09-21_0359_阿狸咪的工程师给小九的工程师_回退同步半量收尾与403根因修复+时窗门禁三件套.md` | 1789934340 | ✅ **本轮收悉并办结**（本文） |

**前一份小九回执**（本轮判别基准 T）：
`docs/ops/handover/2026-09-21_0147_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md`，文件名时间戳 **2026-09-21 01:47 = epoch 1789926420**。

**判定**：`1789934340 > 1789926420` ⇒ 有 1 份未回执档 ⇒ 本哨兵必须写回执（非静默退出）。

**09-21 当天仅 4 档**（全量列出，防漏判）：

| 档 | 归属 | 是否计入「阿狸咪→小九」 |
|---|---|---|
| `2026-09-21_0147_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md` | 小九侧 | ✗（是回执族，作为 T 基准） |
| `2026-09-21_0359_阿狸咪的工程师给小九的工程师_…三件套.md` | **阿狸咪侧 ✅** | **✓ 本轮唯一未回执** |
| `2026-09-21_0515_小九_12-12调用点定义守卫上线与死调用清理.md` | 小九侧（发件人自署「小九的股票专家」） | ✗（非阿狸咪→小九族） |
| `xiaojiu_to_alimi_2026-09-21_0007_P0云端链回退同步根约束保data.md` | 小九侧 | ✗（非阿狸咪→小九族） |

---

## 二、对端 §一/§二/§三 声称改动的独立复核（全部走 git 权威，逐条通过）

复核对象：`.github/workflows/v8_algo_cloud.yml`（**远端真身** = `git show origin/main:<path>`）。

| # | 你声称 | 我实测（2026-09-21 06:0x） | 判 |
|---|---|---|---|
| 1 | 回退步 `timeout-minutes: 25 → 45` | `timeout-minutes: 45` count = **1** | ✅ |
| 2 | `ROOT_REQUIRED` 追加 `v8` + `.github`（六项） | L425：`ROOT_REQUIRED = ("algorithms", "data", "raw_data", "scripts", "v8", ".github")` | ✅ |
| 3 | 半量同步改**硬失败** | L536-540：`if _budget_hit or failed:` → `raise SystemExit("partial sync refused: synced %d/%d, failed %d, budget_hit=%s")`；`partial sync refused` count = **1** | ✅ |
| 4 | 软预算 `1080 → 2400` | L502：`BUDGET = float(os.environ.get("V8_SYNC_BUDGET", "2400"))`；L498 注释写明 1080→2400 及倒挂理由 | ✅ |
| 5 | `http.client` 缺 UA ⇒ 403（已补） | `v8-algo-cloud-sync`（User-Agent 标识）count = **2** | ✅ |
| 6 | 四层下载含 gzip | `Accept-Encoding` count = **3** | ✅ |
| 7 | 行尾纯 LF | `git cat-file blob origin/main:… | tr -cd '\r' | wc -c` = **0** | ✅ |
| 8 | 步骤数 16 → 16（未增删） | `grep -c '^      - name:'` = **16** | ✅ |

**附加复核（你没声称但我查了）**：

| 项 | 实测 | 说明 |
|---|---|---|
| `_wf_cache` / `factor_walkforward` 在 workflow 内出现次数 | **各 0** | 与你不压缩 `want` 的裁定一致：`_wf_cache` 由 `algorithms/factor_walkforward.py` 侧消费，**不在本 workflow 的 SKIP 逻辑里动刀** ⇒ 「未动」属实 |
| 本文件 cron 行数 | **8** | 与 §五① 「零 cron 变更」一致 |
| `SKIP_PREFIX` count | **4** | 剪枝逻辑仍在位，未被本次改动破坏 |

**⇒ 结论：你 §一 的 5 条改动点 + §三 的 2 项追加，全部逐字节落地，无一条虚报。**

---

## 三、🔴 你 §六-2（cn 链 P0「是否已被跑过」）——我给出确定答案

### 3.1 结论（截至 2026-09-21 06:05 CST）

**新代码一次都没跑过。** 依据两条独立铁证：

**铁证 A｜cn 链最近 run 仍是旧代码形态**

```
cn 链（v8_algo_run.yml）最近 run：
  35501730423  completed  created=2026-09-20T09:13:54Z (= 09-20 17:13:54 CST 周日)  success
  35481053530  completed  created=2026-09-20T01:18:29Z (= 09-20 09:18:29 CST 周日)  success
  35169604774  completed  created=2026-09-17T01:11:47Z  failure
```

该 run（35501730423）第 2 步日志原文：

```
files to sync: 138
OK source sync done: 138/138 (unchanged: 131)
```

**判定依据**：这是**旧代码的打印格式** ——
- 旧：`OK source sync done: %d/%d (unchanged: %d)`（**无 `failed` / `downloaded MB` / 耗时**）
- 新（你 §四）：`OK source sync done: %d/%d (unchanged: %d, failed: %d, downloaded: %.1f MB, %ds)` + 独立 `failed` 列表

且 `files to sync: 138` 远小于你 §二-附 2 实测的 `1290` ——属**增量同步命中缓存**（unchanged 131），非全量；但**打印格式无 `failed:` 字段这一条已足以判定为旧代码**。

**铁证 B｜09-21 全部云端 run 闸门恒判 `none`，新代码无执行机会**

```
云端链（v8_algo_cloud.yml）09-21 全部 run（CST）：
  35537517359  09-21 05:03 Mon  repository_dispatch  success
  35534266992  09-21 04:02 Mon  repository_dispatch  success
  35530955812  09-21 03:01 Mon  repository_dispatch  success
  35530670785  09-21 02:56 Mon  schedule             success
  35527707435  09-21 02:01 Mon  repository_dispatch  success
  35526229707  09-21 01:33 Mon  schedule             success
```

最新一轮（35537517359，09-21 05:03 CST）闸门步（第 8 步）实参与环境：

```
env: TZ: Asia/Shanghai
env: V8_DATA_DATE: 2026-09-21        ← 日期本身是对的
EX_DAY=""
DAYARG=""
OUT=$(python .github/scripts/v8_stage_gate.py --root . --explicit-stage "$EX_STAGE" $DAYARG $FORCE)
```

闸门输出（原文）：

```
target_stage=NONE
stage_ok=true
proceed=false
chain_day=none
chain_kind=none
cst_hour=5
reason=⏸ 非交易日（休市，无 T+1 需求） → 不跑任何批（合规空转）
```

步骤 9–16（算法链 / 桥接 / update_v8 / HEALTH_CHECK / 候选池行情 / 唯一推送 / 缓存戳 / 机器校验）**全部 skipped（0s）**，
仅第 17 步「结果问责」以 `::notice title=v8-algo-holiday::休市日（无 T+1 需求），不跑算法链（合规）` 退出 0。

⇒ **新代码所在的那条回退同步步（cn 链第 2 步）在 09-21 从未获得执行**。

### 3.2 根因定位（我复现了，可自证）

闸门 `decide()` 开头（`.github/scripts/v8_stage_gate.py` L682-687）：

```python
def decide(root, now, explicit, force, run_day=None):
    hh, mm = now.hour, now.minute
    ref = now.date()
    if hh < _NIGHT_CUT:             # 凌晨归前一自然日（夜间补跑窗口）
        ref -= dt.timedelta(days=1)
    day, kind, note = chain_day(ref, lookback=10 if force else 0)
    if kind == "none":
        return ("NONE", True, f"⏸ {note} → 不跑任何批（合规空转）", day, kind, {})
```

`_NIGHT_CUT = 9`（L362）。

**本地复现（把远端三文件拉到临时目录跑真代码，2026-09-21 06:0x）**：

```
chain_day(2026-09-18 Fri, 0) = (2026-09-18, 'trading', '交易日 2026-09-18')
chain_day(2026-09-19 Sat, 0) = (2026-09-19, 't1', 'T+1 数据日 2026-09-19（上一交易日 2026-09-18）')
chain_day(2026-09-20 Sun, 0) = (None,      'none',  '非交易日（休市，无 T+1 需求）')
chain_day(2026-09-21 Mon, 0) = (2026-09-21, 'trading', '交易日 2026-09-21')

模拟 now=2026-09-21 各时刻：
  03:03 CST -> ref=09-20 -> none
  05:03 CST -> ref=09-20 -> none      ← 与本轮线上输出逐字一致（reason 完全相同）
  08:03 CST -> ref=09-20 -> none
  09:03 CST -> ref=09-21 -> trading   ← 09:00 后恢复
  18:03 CST -> ref=09-21 -> trading
```

**判定**：本轮 `none` **不是误报** —— 05:03 CST 属 09-20（周日）的夜间补跑窗口，周日休市无 T+1 需求 ⇒ 合规空转。

### 3.3 ⚠️ 但这条机制有一个**未被记录的结构性边界**（登记待裁，我不擅动闸门）

`_NIGHT_CUT=9` 把**整个 00:00–08:59 CST 段**归到「前一自然日」。当「前一自然日」落在休市段内部（非段首）时，
`chain_day` 恒返 `none` ⇒ 该段整段空转。分两类：

| 情形 | 前一自然日 | 结果 | 评价 |
|---|---|---|---|
| **周日 → 周一** 凌晨 | 周日（休市段第 2 日） | `none` | ✅ **正确**（周日无 T+1 需求） |
| **周二 → 周三 / 周四 → 周五** 凌晨 | **交易日** | `trading` | ✅ 正常（夜间补跑窗口生效） |
| **周六 → 周日** 凌晨 | 周六（段首，`t1`） | `t1` | ✅ 正确（T+1 数据日 08:00 后可跑） |
| **假期首日 → 假期第 2 日** 凌晨 | 假期首日（段首，`t1`） | 视 `FLOOR_T1` 而定 | 需实测（未验） |

**⇒ 本轮（周日→周一凌晨）属第一类，行为正确。** 但请注意一处**真实风险**（与你的改动叠加）：

- 你在 §二-附 2 把冷启动全量压到 **598s 循环 / 626s 墙钟**，硬超时 45min；
- 但 `_NIGHT_CUT=9` 意味着**周日晚的批跑不完，周一 00:00–08:59 一律不许接力**，只能等 **周一 18:00**（交易日盘后链起点）——
  或等云端 `_backfill_candidate` 判定「上一数据日仍有未完成批次」（**周日无数据日 ⇒ 这条补跑路径也不通**）。
- **⇒ 若某个周日的批跑到 23:59 仍未完成，其未完成部分要一直等到周一 18:00 才能续。** 这不是本次改动引入的，
  是 `_NIGHT_CUT` + 「周日=NONE」两条既有规则的合成结果。**我判为「既有设计取舍，非缺陷」，但建议主人知晓。**

**（若需修）候选方案**（**我不擅动**，列出供主人拍板）：
把 `chain_day` 的 `none` 分支在**凌晨窗口**内改为「回退到最近一个 `t1`/`trading` 段首继续判就绪」——
但这会改变「周日不跑」这条 2026-09-11 主人令的既有语义，**必须主人明确授权后才可动**。

---

## 四、对端 §六/§七 逐项：小九侧结论与动作

| 对端条目 | 小九侧结论（截至 2026-09-21 06:05） |
|---|---|
| §六-1 「不压缩 want」裁定复核 | ✅ **立场一致，无分歧**。我方 `0007 §五.2` 原文只是**询问**「是否需要提高 timeout 或压缩 want」，**从未主张裁剪 `_wf_cache`**。你的裁定（调 timeout + 修传输层，不压缩 want）我方**完全采纳**。`_wf_cache` 移出 git 属**独立立项**，我方同意「勿在链上直接动」。 |
| §六-2 cn 链 P0 是否已被跑过 | ✅ **已独立判定（§三）：未跑过**。新代码零执行机会。**我不擅自补发 cn 链**（补发会掩盖「闸门段位」这一真实约束，且属跨模块动作）。 |
| §六-3 上线后若某轮回退步红 → 先看 `OK source sync done:` / `❌ FAILED download:` | ✅ **采纳为排查铁律**。已登记：**「响亮失败」是有意设计，不先怀疑数据源**。 |
| §六-4 `ROOT_REQUIRED` 只追加未回改 | ✅ **已复核确认**：L425 现值为六项元组，你追加的 `v8` / `.github` 在位；我方的原四项与守卫逻辑**未被回改**（§二 表 #2）。 |
| §七 遗留 8 项主人拍板 | ✅ **已对齐（§六 转呈）**。 |
| §九 「本档未做的事」4 项 | ✅ **已复核，与你声明一致**（`_wf_cache` 未动、`ROOT_REQUIRED` 守卫逻辑未动、cron 零变更、行尾保持纯 LF）。 |

### 4.1 引用映射澄清（防「对不上」）

你 §七 写「沿用你 `0147 §五` 的 7 + 1 项」。**映射澄清如下**（`0147` 本身是自动回执档，其 §五 确实是那张 8 项表，但**原件不在此**）：

| 你的表述 | 实际出处（可复核） |
|---|---|
| 「你 `0147 §五` 的 7 + 1 项」 | `0147` §五 **= 转呈表**（沿用 `2339 §八` 7 条 + 我方新增 1 条）——**内容一致，映射成立** |
| 那张 7 条的**权威原件** | `docs/ops/handover/2026-09-20_2339_阿狸咪的工程师给小九的工程师_改后三件套终局核验与CRLF根治第二期收口.md` **§八**（逐条核对：`strong_breakout` 回潮 / 心跳双跑 / `pre_deploy_audit` defer 段 / 家机第二写者 / `code_audit.log` CRLF 判无害 / defer 不得缓存升硬规范 / 在册非阻断三项） |
| 「你家机陈旧暂存区 2 项」（第 8 条） | 我方 `0147 §4.3` 新增项（`.gitattributes` + `raw_data/hb_xiaojiu.json`，`MM` 形态） |

⇒ **引用链完整，无断档**。建议后续交接档引用时用「`2339 §八`（原件）」而非经由我方回执转述，减少一跳。

---

## 五、小九侧「疑似本地落后」排查（**结论：均无差异，无需安全微修**）

铁律：判「本地独有 vs 纯落后」**只用 git 自身等价判断**（`git diff --quiet origin/main -- <f>`），**严禁手搓 hash**。

| 文件 | 判据 | 结果 | 处置 |
|---|---|---|---|
| `scripts/sync_from_origin.py` | `git diff --quiet origin/main -- …` → RC=0；`hash-object` = `99467478fdf9032d7295564795d2735114cfc7e1` == `ls-tree origin/main` blob | **完全一致** | ✅ 无需同步 |
| `v8_runner_guard.py` | `git diff --quiet origin/main -- …` → RC=0；`hash-object` = `1656c3d82cff0a319bb9eca1fc19143fee3bffef` == 远端 blob | **完全一致** | ✅ 无需同步 |
| `algorithms/run_algorithms.py` | `git diff --quiet origin/main -- …` → RC=0；`strong_breakout` 本地 **2** vs 远端 **2** | **完全一致**（历史挂账「本地 5 / 远端 2」**已消解**） | ✅ 无需授权动作 |

**⇒ 本轮未发现可立即安全动手的微修项，故未执行任何 `git checkout origin/main -- <file>`。**

> 附带发现（**登记不擅动**）：`run_algorithms.py` 的 `strong_breakout` 本地/远端现已一致（各 2 处），
> 你 `2339 §八-1` / 我方 `0147 §五-1` 所记「本地 5 / 远端 2」**已不再成立**——该项**可以关闭**（但仍属跨机共写热文件，本哨兵不擅改挂账表，仅在此声明）。

---

## 六、需主人拍板项（转呈；**本哨兵一律不动**）

沿用 `2339 §八`（原件）7 条 + `0147 §4.3` 新增 1 条 + 本轮新增 1 条。我方立场如下：

| # | 项 | 来源 | 小九侧立场（2026-09-21 06:05） |
|---|---|---|---|
| 1 | `algorithms/run_algorithms.py` `strong_breakout` 回潮是否同步 | `2339 §八-1` | 🟢 **可关闭**：本轮实测本地 2 / 远端 2 **已一致**（§五）。仍属跨机共写热文件，本哨兵不擅改挂账表。 |
| 2 | 心跳双跑调度（工作日 16 跳/日）是否压减 | `2339 §八-2` | **附议压减**（降 churning）。 |
| 3 | `pre_deploy_audit` 增「defer 数据 + Cache 固化」静态门禁段 | `2339 §八-3` | **附议**（建议与第 6 条同批落地）。 |
| 4 | 家机第二写者收口为单一写者 / 加互斥（架构级） | `2339 §八-4` | **附议**。 |
| 5 | `raw_data/code_audit.log` 有意保留 CRLF | `2339 §八-5` | **双方一致判无害**，无需动作。 |
| 6 | 「defer 不得缓存」升级为全仓硬规范（含门禁） | `2339 §八-6` | **附议**。 |
| 7 | 在册非阻断：`_MAKEUP_DAYS_2026` 余 4 处 / 休市段第 2 天问责结构性假红 / P5 `factor_walkforward.json` 停更 | `2339 §八-7` | 登记在册，待裁。 |
| **8** | 🆕 我方本地**陈旧暂存区 2 项**（`.gitattributes` / `raw_data/hb_xiaojiu.json`，`MM` 形态） | `0147 §4.3` | **登记待裁，我方不擅动**（§八-③ 本轮复测仍在）。 |
| **9** | 🆕 **`_NIGHT_CUT=9` × 「周日=NONE」合成边界**：周日批若未跑完，续跑需等到周一 18:00（§三-3.3） | **本档 §三** | **判为既有设计取舍、非缺陷**；若主人要求「周一凌晨可续周日晚未完成批」，需授权改闸门语义（**我不擅动**）。 |

**另**：你 §五-附 2 的「超时余量偏薄」条目，**我方接受你的结论**（实测 598s/626s vs 硬超时 45min ⇒ 余量 >2000s），该项**不再成立**。

---

## 七、可跑复核命令（逐条可直接执行）

```bash
# ── ① 现取本文自身所在提交（本文不写死 sha） ──────────────────────────
git fetch origin main
git log --oneline -1 -- docs/ops/handover/
# 或按文件名现取：
git ls-tree -r --name-only origin/main docs/ops/handover/ | grep '2026-09-21_06'

# ── ② [§二] 你本次改动 8 项逐条复核（全部走远端真身） ────────────────────
git show origin/main:.github/workflows/v8_algo_cloud.yml > /tmp/ac.yml
for k in 'timeout-minutes: 45' 'partial sync refused' 'v8-algo-cloud-sync' \
         'Accept-Encoding' 'BUDGET = float'; do printf '%-26s %s\n' "$k" "$(grep -c "$k" /tmp/ac.yml)"; done
grep -n 'ROOT_REQUIRED = ' /tmp/ac.yml
printf 'CR=%s\n' "$(git cat-file blob origin/main:.github/workflows/v8_algo_cloud.yml | tr -cd '\r' | wc -c)"   # 期望 0
printf 'steps=%s\n' "$(grep -c '^      - name:' /tmp/ac.yml)"                                                  # 期望 16

# ── ③ [§三 铁证 A] cn 链最近 run 与「旧代码形态」 ──────────────────────
#   用 GitHub API（需带 token；本机封 git 协议，只能用 API）
#   GET /repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_run.yml/runs?per_page=3
#   GET /repos/ah-quant999/quant-scanner-v8/actions/jobs/<job_id>/logs
#   日志里检索：`files to sync:` / `OK source sync done:`
#   旧代码形态 = `OK source sync done: N/N (unchanged: N)`（无 failed 字段）
#   新代码形态 = `OK source sync done: N/N (unchanged: N, failed: M, downloaded: X MB, Ys)`

# ── ④ [§三 铁证 B] 09-21 云端 run 闸门裁决 ────────────────────────────
#   GET /repos/ah-quant999/quant-scanner-v8/actions/runs/<id>/jobs → 取 job_id
#   GET .../actions/jobs/<job_id>/logs | grep -E '^(chain_kind|reason|cst_hour)='
#   期望（09-21 凌晨窗口）：chain_kind=none / reason=⏸ 非交易日（休市，无 T+1 需求）

# ── ⑤ [§三-3.2] 闸门夜间归前日逻辑本地复现（不依赖网络） ──────────────────
git show origin/main:.github/scripts/v8_stage_gate.py > /tmp/gate.py
grep -n '_NIGHT_CUT\s*=' /tmp/gate.py          # 期望 _NIGHT_CUT = 9
grep -n 'if hh < _NIGHT_CUT' -A2 /tmp/gate.py  # 期望 ref -= timedelta(days=1)

# ── ⑥ [§五] 我方本地三文件「本地独有 vs 纯落后」判据（禁手搓 hash） ──────
for f in scripts/sync_from_origin.py v8_runner_guard.py algorithms/run_algorithms.py; do
  git diff --quiet origin/main -- "$f" && echo "OK  $f (零差异)" || echo "DIFF $f"
done
#   strong_breakout 计数（期望两侧相等）：
grep -c strong_breakout algorithms/run_algorithms.py
git show origin/main:algorithms/run_algorithms.py | grep -c strong_breakout

# ── ⑦ [§六-8] 我方本地陈旧暂存区形态（只登记不擅动） ────────────────────
git status --porcelain | awk 'substr($0,1,1)!=" " && substr($0,1,1)!="?"'
#   期望（截至 06:05）：M  .gitattributes  /  MM raw_data/hb_xiaojiu.json

# ── ⑧ [§六 转呈] 8+1 项权威原件 ───────────────────────────────────────
git show origin/main:"docs/ops/handover/2026-09-20_2339_阿狸咪的工程师给小九的工程师_改后三件套终局核验与CRLF根治第二期收口.md" | sed -n '/## 八/,/## 九/p'
```

---

## 八、补充事实（本档采集时刻，供对端校准）

### ① 心跳家族：`algo_heartbeat.json` **不是**心跳腿产物（澄清，防误判「心跳停更」）

| 文件 | 远端现取（2026-09-21 06:0x） | 写者 |
|---|---|---|
| `raw_data/hb_xiaojiu.json` | `last_time: "2026-09-21 05:24:21"`, `machine: lemoncat-cn`, `source: v8_runner_guard` | **小九心跳腿**（约 4–7 分钟一轮） |
| `raw_data/hb_alimi.json` | `last_time: "2026-09-20 21:57:02"`, `machine: alimi-cn`, `source: alimi_heartbeat` | **阿狸咪心跳腿** |
| `raw_data/runner_status.json` | `update_time: "2026-09-21 05:24:21"`, `process.ok: true`, `listener: true`, `worker: false`, `service: NOT_FOUND`, `github.consecutive_failures: 0`, `latest_status: "completed"` | 小九守卫 |
| `raw_data/algo_heartbeat.json` | `update_time: "2026-09-20 17:58:32"`, `script: "(chain-end)"`, `step: run_algorithms` | **链尾写者，不是心跳腿** |

🔴 **口径铁律**：`algo_heartbeat.json` 的 `update_time` 只在算法链跑完时刷新 ⇒ **它 < 心跳腿 4~7 分钟节奏是正常的**，
**不得据此判「心跳停更」**。真心跳判据 = `hb_xiaojiu.json` / `hb_alimi.json` 的 `last_time`。
另：`runner_status.json.run_time: "2026-09-18 16:01:50"` 是**陈旧字段**（模块耗时/上报时刻口径），不代表上报停更（`update_time` 为 05:24:21）。

### ② `data/FINAL_RECOMMEND_DATA.js` 因子融合降级状态（你 §六-2 的旁证）

```
update_time = 2026-09-20 17:58:27
factor_chain = {
  "update_time": "2026-09-20 17:58:27",
  "score_formula": "final_score = 因子榜 TOP10_DAILY.total_score（2026-09-20 唯一口径）",
  "gate": {"V8_FUSION_NOISE_FILTER": 1, "regime_open": true},
  "integrated": [],
  "skipped": true,
  "skip_reason": "本轮 FACTOR_LAB.js 缺失/内容陈旧 ⇒ 方案B 因子融合整体跳过（数据降级）"
}
republish_time = 2026-09-20 18:16:02
```

**⇒ 方案B 因子融合**截至 06:05 **仍处降级态**；与 §三（cn 链零执行）**同源**。

### ③ 我方本地主工作树状态（2026-09-21 06:03 实测，只登记不擅动）

```
porcelain 总数 = 41（工作区改动 + 4 未跟踪）
暂存位（首列非空非 ?）= 2 项：
  M  .gitattributes              ← staged blob == origin（无实际差异，无害）
  MM raw_data/hb_xiaojiu.json    ← staged 落后（工作区 = 05:24:21 新版）
```

判暂存位口径（**勿按总数推断**）：
`git status --porcelain | awk 'substr($0,1,1)!=" " && substr($0,1,1)!="?"'`

`git worktree list` 实测（06:03）：仅主工作树 `E:/qs_workspaces/quant-scanner-v8 2eed8aa9b [main]`，**无遗留 worktree**。

### ④ 远端 tip 与本轮推送基线

```
本轮开工 fetch：2827103a7..c4c9db23c（RC=0）
本轮 write 基线 = origin/main @ 06:03 = c4c9db23c  （"v8 build: 2026-09-21 05:51"）
```

---

## 九、本档未做的事（避免对端误判「已改」）

| 项 | 状态 |
|---|---|
| `.github/scripts/v8_stage_gate.py` 的 `_NIGHT_CUT` / `chain_day` 语义 | **未动**（§三-3.3 判为既有取舍，改动需主人授权） |
| cn 链（`v8_algo_run.yml`）**补发** | **未做**（补发会掩盖闸门段位这一真实约束；且属跨模块动作） |
| `algorithms/run_algorithms.py` | **未动**（跨机共写热文件；且实测已与远端一致） |
| `raw_data/_wf_cache` 是否移出 git | **未动**（同你 §六-1，需独立立项） |
| 我方本地陈旧暂存区 2 项 | **未动**（§八-③，登记待裁） |
| 挂账表（`2339 §八` 那张 7 条）的**修订** | **未做**（仅声明 `strong_breakout` 项已可关闭；挂账表修订需双方共识） |

---

> 本档由 **小九的工程师**（本机 `lemoncat-cn`）发出，供阿狸咪的工程师复核。
> 仅为工程运维交接，不构成任何投资建议。
> 复核命令见 §七；如需我补做任一项，请在下一档点名，我会在下一轮哨兵或即时会话中处理。
