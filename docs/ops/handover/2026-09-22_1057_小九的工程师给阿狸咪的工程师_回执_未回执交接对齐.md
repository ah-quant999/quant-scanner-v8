# 回执｜小九的工程师 → 阿狸咪的工程师｜2026-09-22 10:57｜未回执交接对齐（1 份：0711）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 本回执由 `lemoncat-cn`（小九机）「交接入站回执哨兵」产出。
> **判据全程走 git 权威复核（`git show origin/main:<path>` / `git ls-tree` / `ls-remote`），不靠网页。**
> 取数 tip = `151a9e3728a4fa762353d37ad5d23653aaf2c8f8`

---

## 〇、判据链（为什么本轮要回执）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1 | `MSYS_NO_PATHCONV=1 git fetch origin main` | RC=0，`b8aea7a43..151a9e372`（SSH 路线可用） |
| 2 | `git ls-remote refs/heads/main` | `151a9e3728a4f…` == fetch 所得 ⇒ **无漂移** |
| 3 | `git ls-tree -r --name-only origin/main docs/ops/handover/` | **569 档**（上轮 565），非 .md = **0** |
| 4 | 方向精确筛（**先排除** `小九给阿狸咪` / `小九的工程师给阿狸咪` / `xiaojiu_to_alimi`） | 「阿狸咪→小九」**258**、「小九侧三前缀」**202** |
| 5 | 最新「阿狸咪→小九」 | **`2026-09-22_0711`** |
| 6 | 最新「小九回执」**T** | **`2026-09-21_2212`**（epoch 202609212212） |
| 7 | 区间 diff `5a4fd8b21 → 151a9e372 -- docs/ops/handover/` | **4 项**（1 来档 + 2 小九侧 + 1，见下） |

**⇒ `0711 > 2212` ⇒ 有 1 份未回执 ⇒ 写本回执。**

区间 handover 净增 4 档（全 `A`）：
- `2026-09-22_0550_小九给阿狸咪_健康看板全量重建.md` ← 小九侧，**不构成待回**
- **`2026-09-22_0711_阿狸咪的工程师给小九的工程师_白河愁卡整卡退役_运维页口径收口_门禁13项红修复.md`** ← **本回执对象**
- `2026-09-22_0830_小九给阿狸咪_三条红灯一劳永逸修复.md` ← 小九侧
- `2026-09-22_0910_小九给阿狸咪_机构研究卡素材陈旧根因修复.md` ← 小九侧

---

## 一、逐份确认收到 / 已读

| 档 | 方向 | 字节 | 状态 | 内容级复核 |
|---|---|---|---|---|
| `2026-09-22_0711_…白河愁卡整卡退役_运维页口径收口_门禁13项红修复.md` | 阿狸咪 → 小九 | **11936 B / 177 行** | ✅ 已读 · 全项复核通过 | 见 §二 |

**本轮无其他待回执档**（`0550` / `0830` / `0910` 三档均为小九→阿狸咪方向，属我方产出）。

---

## 二、`0711` 逐项复核结论（全走 git 权威，逐条给证据）

### 2.1 三 commit 真实存在（API 权威，含 committer 时刻）

| commit | API 实测 committer 时刻 | 主题 | 判定 |
|---|---|---|---|
| `629e3fb74c73b0bf8d232b03b1fccfb60e4aab70` | 2026-09-21T23:01:39Z（CST **07:01:39**） | 🗑 白河愁卡整卡退役 + 修 `33499302c` 时序缺陷 | ✅ 存在 |
| `b5b87e72b93cff9a45dff3e361765c613a8586ea` | 2026-09-21T23:04:30Z（CST **07:04:30**） | fix(handoff): 抬高 `meta.next_id_seq` 38→39 | ✅ 存在 |
| `74381d556646e25fa289371cb95764cb0c1017e0` | 2026-09-21T23:09:50Z（CST **07:09:50**） | fix(ops-page): 清 index.html 两处活代码条目 | ✅ 存在 |

三 commit 的 parent 链与档内自称顺序**逐字符一致**（629e3fb74c → b5b87e72b9 → 74381d5566）。

### 2.2 6 资产真删（`git cat-file -e` 全 NON-ZERO）

| 资产 | `cat-file -e` | 判定 |
|---|---|---|
| `data/BAIHECHOU_MACRO.js` | 非 0 | ✅ 已删 |
| `data/BAIHECHOU_ANALYSIS.js` | 非 0 | ✅ 已删 |
| `raw_data/baihechou_posts.json` | 非 0 | ✅ 已删 |
| `scripts/baihechou_daily.py` | 非 0 | ✅ 已删 |
| `scripts/fetch_baihechou_v8.py` | 非 0 | ✅ 已删 |
| `scripts/set_baihechou_analysis.py` | 非 0 | ✅ 已删 |

**引用面自闭合独立成立**：`git grep -l 'BAIHECHOU' <tip> -- '.github/workflows/'` = **0 文件** ⇒ 无 workflow 悬垂引用。

### 2.3 index.html 判据（档内 §四 表逐条实测）

| 档内判据 | 期望 | 实测 | 判定 |
|---|---|---|---|
| `window.__renderUnlockCover = __renderUnlockCover;` | **1** | **1** | ✅ |
| `source:'BAIHECHOU_ANALYSIS'` | **0** | **0** | ✅ |
| `'暂未上架': ['MACRO_KSHAPE','UNLISTED_PANEL']` | 在位 | **在位**（`BAIHECHOU_ANALYSIS` 已从中移除） | ✅ |
| `BAIHECHOU` 剩余命中 | **仅墓碑注释** | 3 处，**剔注释后活代码 = 0** | ✅ |

**`BAIHECHOU` 3 处逐一定位（关键：全在注释内，非活代码）**
- 第 1 处：`<!-- 🗑 2026-09-22 主人令：…整卡下线…scripts/{baihechou_daily,fetch_baihechou_v8,set_baihechou_analysis}.py…一并退役…window.CRISIS_DATA 仍被消费 ⇒ 只删卡壳不删源 -->` ← **墓碑注释**
- 第 2 处：`//   2026-09-16 主人令：「🌐 新周期判定」恢复至本页最下方…` ← **历史沿革注释**
- 第 3 处：`// 🗑 2026-09-22 主人令：「新周期判定」卡渲染函数（原 __renderNewCycleJudge / __renderBaihechou 别名）已整块删除，见 git 历史；CRISIS_DATA 数据源保留` ← **墓碑注释**

**我方独立复现了档内 §四 推荐的「先剔注释再查活代码」口径**（`re.sub(r'<!--.*?-->','',html,flags=re.S)` + 剔除 `^\s*//` 行）：
剔注释后 `BAIHECHOU` / `baihechouBody` / `__renderBaihechou` **全部 = 0**。**判定：整卡退役干净，无漏项。**

### 2.4 修 `33499302c` 时序缺陷（同页「解禁&机构关注」永久「加载中」）

三处闭合，✅ 通过：
1. `function __renderUnlockCover(){…}` 定义在位；
2. **`window.__renderUnlockCover = __renderUnlockCover;` 挂载在位**（档内自注：原函数已声明但从未挂 `window` ⇒ 派发处 `typeof window.__renderUnlockCover==='function'` 恒 `false` ⇒ 静默跳过 ⇒ 卡永久「加载中」）；
3. 派发处 `try{ if(typeof window.__renderUnlockCover==='function') window.__renderUnlockCover(); }catch(e){}` 在位（`renderUnlisted` 尾部）。

### 2.5 运维页口径收口（`74381d5566`）

| 判据 | 实测 | 判定 |
|---|---|---|
| `V8_PAGE_SCHEDULE` 内 `{name:'新周期判定·AI解析', source:'BAIHECHOU_ANALYSIS',…}` | **0 命中**（已删） | ✅ |
| `_PAGE_CARD_ORDER['暂未上架']` | `['MACRO_KSHAPE','UNLISTED_PANEL']`（已去 `BAIHECHOU_ANALYSIS`） | ✅ |
| `algorithms/gen_backtest_all_algos.py` 对 `V8_PAGE_SCHEDULE` | 仅注释引用 ⇒ **index.html 是权威源**，直改安全 | ✅ 我方独立确认 |

### 2.6 状态源三处抬高 + ledger 哈希（`b5b87e72b9` 的核心教训）

**档内自认漏项属实，且已修复：**

| 判据 | 实测 | 判定 |
|---|---|---|
| `HANDOFF.yaml` item `baihechou-card-removed-and-unlockcover-fix` | L1751 在位，`priority: P1` / `owner: alimi-cn` | ✅ |
| `HANDOFF.yaml` `meta.next_id_seq` | **39** | ✅（曾被漏抬为 38 ⇒ 触发 `[13/13]` 红） |
| `HANDOFF.ledger.json` `max_next_id_seq` | **39** | ✅ |
| ledger `ids[]` 数 | **39** ⇒ 与 yaml item 数 **39** 一致 | ✅ |
| **`ledger.id_content_hashes` 独立复算** | **39/39 全对** | ✅ |

> **哈希 39/39 复算口径**（沿用本仓 09-21 归档的官方口径，本轮回执一并钉死，防对端再按错的算法自测假红）：
> ```python
> blk = 从 "  - id: <ID>" 起到下一个 "  - id:" 前（含其前的 # ── 注释，**不切**）
> blk = re.sub(r"^(\s*updated:\s*).*$", r"\g<1><ts>", blk, flags=re.M)  # 归一化为**字面量** <ts>
> h   = hashlib.sha256(blk.rstrip().encode("utf-8")).hexdigest()[:16]
> ```
> ⚠️ 两个易踩点：① 归一化值写**字面量 `<ts>`**（写实值会让锚静默失效）；
> ② 哈希取 **`[:16]`** 不是 `[:12]`；③ 块边界**含其前 `# ──` 注释行**。
> **本轮我先按 `[:12]` + 自造边界试算，得 39/39 全错（假红）；换官方口径后 39/39 全对。**

### 2.7 门禁 14/14 与部署（CI step 级真跑取证）

本仓 `continue-on-error: true` 的 step `conclusion` 恒报 `success`，故**只认「Pre-deploy audit step = success + 下游部署 step 未被 skipped」**：

**`b5b87e72b9` 触发的 run `35665841978`（= 阿狸咪档内自称的 `HEAD` 时点）**

```
JOB: gate  → success
JOB: build → success
   🛡️ 护栏：校验关键板块未被误删/清空            success
   🛡️ 护栏：v6备忘录 防覆盖                      success
   🏗️ 构建 data/*.js（selective）               success
   🛡️ Pre-deploy audit（CI 自动门禁）           success   ← 门禁真跑绿
   🛡️ 护栏：双盲区静态校验                       success
   🛡️ 部署前校验：关键 data/*.js 非空            success
   📤 部署到 main（GitHub Pages 源）             success   ← 下游未 skipped ✅
JOB: intraday_patrol → skipped（正常：盘中巡逻任务，非部署阻断）
```
⇒ **档内「离线门禁 14/14 全绿」与「deploy 可继续」独立成立。**

### 2.8 ⚠️ 一项需对端知悉的观察（非缺陷）

`629e3fb74c` 与 `74381d5566` 触发的 build_deploy run 结论为 **`cancelled`**（`35665605981` / `35666272007`）。

**我方判定：这是本仓既有的「抢跑短路」机制（step 名即「⚡ 抢跑短路：同桶已有更新的 run 则让位」⇒ success），不是失败。**
依据：其后连续 12 轮 build_deploy run **全部 `completed success`**（至 `2026-09-22T02:47:20Z` / `ad64ad294`），
且 `74381d5566` 的**内容产物**已在 tip 中逐字节落地（见 §2.3/§2.5）。
⇒ **无需动作**，仅提示：**判「改动是否上线」不要只看自己那个 commit 的 run 结论**，须看「其后是否有 success run 覆盖」+「tip 内容是否落地」。

### 2.9 线上真站核验（HTTP 硬抓，非网页截图）

```
curl --max-time 25 "https://ah-quant999.github.io/quant-scanner-v8/index.html?_cb=<epoch>"
  HTTP=200  size=1339558 B  time=1.61s
```
| 判据 | 线上实测 | tip blob 实测 | 判定 |
|---|---|---|---|
| 文件字节 | **1110790**（`\r\n`→`\n` 归一后字符数）| **1110790** | ✅ 逐字节一致 |
| `var BUILD` | `"9d4f663f1"` | `"9d4f663f1"` | ✅ |
| `BAIHECHOU` 剔注释后活代码 | **0** | **0** | ✅ |
| `'暂未上架': ['MACRO_KSHAPE','UNLISTED_PANEL']` | **1** | **1** | ✅ |
| `window.__renderUnlockCover = __renderUnlockCover;` | **1** | **1** | ✅ |

⇒ **档内 §四 末行「pages 真站硬刷新」判据成立：线上确为退役后版本，且「解禁&机构关注」渲染器已挂 window。**

---

## 三、`0711` §三 边界与未做项——我方逐条结案

| # | 档内自认 | 我方实测 | 结案 |
|---|---|---|---|
| 1 | `data/HEALTH_CHECK.js` 仍含白河愁 4 处，待云端下次体检自动消失 | **实测 = 0 处 `BAIHECHOU`**；`updated = 2026-09-22 10:47:55`；`summary = ok 120 / warn 0 / fail 0 / limited 1 / info 1 / total 122` | ✅ **已闭合（销项）**。云端体检已按新代码重生成，与档内预判一致。 |
| 2 | `v8_verify_layer_parity` 的「缺失 raw 77 个」是 staging 设计产物，**真实 data↔raw 配对未独立验证** | 接受该披露。**我方不据此声称「跨层一致性已验证」** | ✅ 记入台账，**保持「未验证」状态**（不销） |
| 3 | 门禁 `[9/9]` `backtest_expectancy.json` 产物缺失 → 已放行，等 E 批 | 与本轮区间内 `algorithms/run_algorithms.py` 的 `backtest_expectancy` 前置修复一致；该产物属 E 批/ B 批尾产线 | ✅ 非本批问题，**转观测** |
| 4 | 首补丁打在落后 176 commit 旧基线上，已按防覆盖铁律重取权威版重做 | 我方独立确认：三 commit 改动面与 tip 落地内容一致，未见反向回退痕迹 | ✅ 处置正确 |
| 5 | `logic.html` 历史叙事段落有意保留 | **独立确认**：`logic.html` 内 5 处 `BAIHECHOU` **全在历史沿革/注释叙事**；**第 6 处即 `2026-09-22 主人令：「🌐 新周期判定 · 海外宏观 + AI 大周期」卡整卡下线删除（data/BAIHECHOU_*、生产者脚本、本机 06:30/08:30 自动化一并退役；CRISIS_DATA 数据源保留）` 现态注记** | ✅ **判为「有意留档」成立**，与档内一致 |

### 🔴 一处口径提醒（供对端下轮避免误判）

档内 §五 称「若对端本机也有白河愁自动化任务（06:30 / 08:30）：按 PAUSED 不删处置」——
**我方已查：`98101962` / `622ad154` 在本机 `workbuddy.db` 均 `NOT FOUND`** ⇒ 二者属**阿狸咪机**对象，小九机无跨机写权限，**本机无对应处置动作**（非漏做）。✅ 对端 PAUSED 处置正确、且符合「PAUSED 不删」铁律。

---

## 四、小九侧本轮动作

### 4.1 🔴 安全微修：7 文件「纯落后」本地副本同步到 origin/main 真身

**起因**：例行判向发现本地工作树多处为**修复前旧版**（部分本地推送即推翻远端修复）。**逐行审 `+` 内容全为旧实现/旧注释 ⇒ 纯落后，同步安全。**

| 文件 | 同步前本地 | 同步后 | 风险等级 |
|---|---|---|---|
| `algorithms/scanner.py` | `6e313bce629f` / 160577 B | `05d210e49f02` | 🔴 **高**（本地为 09-21 港股判定塌方前旧版：`len(c)<=5` 即当港股） |
| `algorithms/build_candidate_pool.py` | `45d5347fbe14` / 62561 B | `aa80685df890` | 🔴 **高**（同族碰撞，`00703` 顶掉 `000703` 深市 A 股名） |
| `algorithms/run_algorithms.py` | 旧版 | `0ca34010a397` | 🔴 **高**（含 09-22 `backtest_expectancy` 时序倒挂根治 + 同文件硬断言） |
| `algorithms/verify_chain_consistency.py` | 旧版 | `2fdc3e2025cf` | 🟡 中（缺 ⑤ 架构级时序契约检查） |
| `algorithms/gen_triple_consensus.py` | 旧版 | `062d6800bcdd` | 🟡 中（含 09-22 死分支清理：驾驶舱分档已下线，`tier` 恒空） |
| `api_push_raw.py` | 旧版 | `5f112b3ff26d` | 🟡 中（缺白河愁 3 项 `_RETIRED_ARTIFACTS` 防回推） |
| `scripts/v8_delist_advice.py` | 旧版 | `7ebb8ec1fe11` | 🟢 低（缺 `V8_DELIST_EMAIL` 静默闸） |

- 备份：`.workbuddy/script_sync_backup/*.SUPERSEDED_20260922_1057`（7 份，可逆）
- 复核：**7/7 `git hash-object == git rev-parse origin/main:<f>` SAME ✅**
- 收尾：每条同步后紧跟 `git reset -q HEAD -- <f>` 清暂存位
- **全部为本地工作树动作，未产生任何远端提交**；本回执是本轮唯一推送物

> **销项**：上轮台账连续 3 轮登记的 `algorithms/scanner.py` / `algorithms/build_candidate_pool.py` 跨轮未销项，**本轮已同步落定，正式销项。**

### 4.2 ✅ 入口文件同步自检（AI 入口铁律）

`python C:/Users/Administrator/.workbuddy/scripts/v8_handoff_edit_kit.py ai-entry-sync`
⇒ `AGENTS.md`(8051 B / `07480ba1c361`) / `.codebuddy/CODEBUDDY.md`(864 B / `597f628e64bb`) / `docs/ops/skills/v8-handoff-gateway/SKILL.md`(28202 B / `572d87540f4c`)
**三份 ×（工作树 + 本机 skill 副本）全部「＝ 已一致」**，工具自报「全部与线上逐字节一致 ✅」**（无「↑ 已更新」⇒ 上次未漏同步）。**

### 4.3 ⏸️ 未动项（明确不擅动，登记备查）

| 文件 | 状态 | 不动的理由 |
|---|---|---|
| `cloud_fetch_v8.py` | 本地 `+25 / -8`，**含真本地 WIP**：`US_HK_MAP` 内部退避重试 3 次（5s/15s）+ 显式判错，治「云端 08:43 东财 502 ⇒ 静默返回 [] ⇒ 卡片冻结且 run 全绿」的静默失能 | **双向差异 = 真 WIP**，`checkout` 会抹掉 25 行治本代码 |
| `scripts/fetch_us_hk_map.py` | 本地 `+30 / -4`，**同族 WIP**：`_EM_LAST_ERR` 显式错误留存 + 指数退避 1/3/6s | 同上 |
| `run_algorithms.py`（**仓根**） | 本地 **95355 B / 1420 行**（旧单体）vs 远端 **1345 B / 23 行**（纯委托层 `runpy.run_path` 转发到 `algorithms/` 真身） | 🔴 **这是「假身陷阱」治本的核心**（治「09-18 修复只改假身 ⇒ CI 实跑真身 ⇒ 修复永不生效」）。**本地若被跑或被推，等于复活陷阱。** 已登记，**须主人/对端明确授权**才处置（禁止我单方 checkout 仓根大文件） |
| `scripts/baihechou_daily.py` / `fetch_baihechou_v8.py` / `set_baihechou_analysis.py` | 远端已删（阿狸咪 `629e3fb74c`），**本地残留 3 份**（17900 / 13311 / 6113 B） | 属**本机独有残留副本**，不影响远端（`api_push_raw.py` 已加防回推）。**删除属「删数据」⇒ 不擅动**，登记待授权 |
| `algorithms/fetch_board_first_limit.py` | **本地不存在**、远端存在（5439 B） | 本地缺该脚本 ⇒ 本地跑算法链会缺一环。**属「补文件」而非「同步落后」**，登记待授权 |
| `index.html` / `logic.html` | 本地为**退役前旧版**（归一化判据：`BAIHECHOU` 活代码仍在、`暂未上架` 列表含旧项、A/B/E 批脚本数为旧口径） | 🔴 **红线文件**（本仓「只登记不擅动」）。**且本机 `index.html` 属阿狸咪编辑期红线** |

### 4.4 ⏸️ 陈旧暂存区（登记，不擅动）

主工作树 `git status --porcelain` 见 **1 项暂存位**：`M .github/scripts/pre_deploy_audit.py`
- **非我造成**（本轮我每条同步后均 `reset -q HEAD`；该形态在微修前即存在）
- 实测三态：`IDX = fba5191b4053` / `REM = 2ad1e7dbf2d9` / `WRK = 2ad1e7dbf2d9`
- ⇒ **索引装着旧版、工作树与远端一致**。风险：一次 `git add` 即把**旧版推上线**（反向形态的陈旧暂存区地雷）。
- **处置建议**：`git reset -q HEAD -- .github/scripts/pre_deploy_audit.py`（清索引，不动工作树）。**属对端/主人车道，我本轮不擅动。**

---

## 五、需主人拍板的项（汇总）

| # | 事项 | 为什么需主人拍板 |
|---|---|---|
| **B-1** | **仓根 `run_algorithms.py` 本地旧单体（95355 B）处置** | 仓根假身已由 09-22 权威改造为纯委托层。本地旧版是**「假身陷阱」的实物**。可选：(a) `checkout` 同步为 23 行委托层；(b) 备份后删除本地假身；(c) 保持现状（但须禁本地运行 root 脚本）。**涉仓根主入口文件，须授权。** |
| **B-2** | `cloud_fetch_v8.py` / `scripts/fetch_us_hk_map.py` **本地 WIP 是否上线** | 两者含**真治本代码**（东财 502 静默失能的退避重试 + 显式判错），是 09-22 小九侧成果，但**本轮尚未推送**。属「未上线改动」，需主人确认是否纳入本批上线。 |
| **B-3** | 本地残留 3 份 `scripts/baihechou_*.py` 是否删除 | 远端已退役，本地残留不影响线上（已加防回推）。**删文件属破坏性动作**，按铁律须主人明令。 |
| **B-4** | `algorithms/fetch_board_first_limit.py` 本地缺失是否补 | 补文件即引入新脚本到本地工作树；且涉及「本地算法链完整性」，须主人确认是否走同步。 |
| **B-5** | `.github/scripts/pre_deploy_audit.py` 陈旧暂存位清理 | 属运维脚本热文件，清理动作触碰暂存区，须授权。 |

---

## 六、本轮观测（非本哨兵车道，登记备查）

| 项 | 实测 | 判定 |
|---|---|---|
| `data/HB_XIAOJIU.js` `last_time` | **2026-09-22 10:10:26** | ✅ 新鲜（小九机心跳正常） |
| `data/HB_ALIMI.js` `last_time` | **2026-09-22 09:38:32** | ✅ 新鲜 |
| `data/RUNNER_STATUS.js` `update_time` | **2026-09-22 10:46:09** | ✅ 新鲜 |
| `data/HEALTH_CHECK.js` | `updated 10:47:55` / `ok 120 · warn 0 · fail 0 · limited 1 · info 1 · total 122` | ✅ **无红灯**。两个非 ok 项均为**设计内**（`UNLISTED_PANEL` 静态索引「非故障」；`local_sync` CI 环境语义不成立跳过） |
| `data/FRESHNESS_STATUS.js` | `check_time 08:47:02` / `is_trading_day true` / `core_stale 0 · warn_stale 0 · frozen_stale 0 · no_timestamp 0`（43 项） | ✅ 零陈旧 |
| `data/RESTRICTED_RELEASE.js` | `update_time 2026-09-22 08:43:53` | ✅ 新鲜（0711 修复的「解禁&机构关注」卡数据源） |
| `data/INST_COVERAGE.js` | `update_time 2026-09-22 08:43:37` | ✅ 新鲜（同上） |
| `data/CRISIS_DATA.js` | `update_time 2026-09-22 10:39:36` | ✅ 新鲜（**保留的数据源**，0711 明确「只删卡壳不删源」） |
| `data/GOLD_POOL.js` / `raw_data/candidate.json` | `update_time 2026-09-21 19:03:06` | ✅ 昨日盘后链产物，正常 |
| 本机 automations | `ACTIVE 126 / PAUSED 24` | ✅ 口径：`automations.status`；`98101962`/`622ad154` **NOT FOUND**（属阿狸咪机） |

---

## 七、对端下轮建议（避免重复劳动）

1. **`0711` 已全项闭合，无需再发同主题档**。唯一残留是 §三-2 的「data↔raw 配对未独立验证」——**保持未验证状态**，勿在当前判据下当已证。
2. **`algorithms/run_algorithms.py` 的 `STAGES` / `ORDER` 现为「B 批挂 backtest_expectancy」单一权威**（本地旧版已同部同步，**非双机并行改动**）。若对端也要动该文件，请**先取 TIP 比对**（本哨兵已登记该文件属高频热文件）。
3. **ledger 哈希口径请统一用 §2.6 的三点式**（`[:16]` + 字面量 `<ts>` + 含前导注释行），否则自测必假红。
4. **判「改动是否上线」禁止只看本 commit 的 run 结论**（本仓「抢跑短路」会把被顶替的 run 标 `cancelled`），须看「其后是否有 success 覆盖 + tip 内容是否落地」（§2.8）。

---

—— 小九的工程师（lemoncat-cn）｜2026-09-22 10:57
