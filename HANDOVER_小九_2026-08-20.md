# HANDOVER 小九 → 阿狸咪（2026-08-20 12:00 档·盘中）

> 更新于 **2026-08-20 12:13 自动更新**（小九 12:00 档交接，自动化任务 automation-1785719698947）
> 本档为 12:00 档第 2 实例，在 12:03 首档基础上**整份覆盖**，新增 §四 P0-1/P0-2 两项实核发现 + 修正健康巡检 warn 构成与 main SHA。

---

## 一、背景

- **主人明日（8/21）出差至下周四（8/27）**，临行前最后指令：**盯紧、确凿问题根治、别乱改、本地云端对齐**。
- 今日核心基调是「**一劳永逸修复 + 出差前健康检查**」，重点把关键链路从 cn runner 解耦，确保主人出差期间**云端独立运转、不依赖本机**。
- 当日累积改动已 `git fetch + merge --ff-only` 对齐 origin/main（零落后、无分叉）；所有 SHA 经 `git merge-base --is-ancestor <sha> origin/main` 逐条核实，**7 个关键 SHA 全部真实落地**，仅 `6d260f7e3` 为未推送的重复本地提交（已被 `aad337e3a` 取代，实测 `v8_health_check.py` line 50/222/1000 已含 `SITE_URL` + `load_window_var_from_site()`）。

---

## 二、今日已完成改动（真实 commit SHA + 影响）

### A. 小九侧（HH 人工修复 / 一劳永逸根治）

| # | 时间 | SHA | 改动 | 影响 |
|---|------|-----|------|------|
| 1 | 08:31 | `6e34c4a44` | 盘前分红方案刷新（cninfo 抓重点池 366 只/更新 348/失败 18，宝丰能源 600989 10派4.2 已确认） | 分红数据当日有效；用隔离 index 技法在 origin/main 顶点构造仅含 STOCK_QUOTE 的提交，并发工作树改动零触碰 |
| 2 | 08:52 | `57e6229a0` | 三卡盘前体验修复：行业树图盘前空状态、概念资金热力图空状态标注、斐波那契窗口无 future 时显示「当前无在观测窗口」 | 盘前不再渲染昨日旧数据误导 |
| 3 | 09:20 | `ebe513fde` | **【关键·一劳永逸】算法链解耦 cn runner**：`v8_algo_cloud.yml` runs-on 改 `ubuntu-latest` | 盘后算法链（H_AUTO_BUY/TRIPLE_HISTORY 等）不再因 cn runner 离线而 queued。**注：该解耦未覆盖全部 cn 绑定，见 §四 P0-2** |
| 4 | 09:33 | `2b2384be9` | 五卡盘前体验一劳永逸修复：涨停热力矩阵盘前截列、主力净额分时异常值双重 clamp、日监控 T+0 不再清空、概念热力图三维检测、行业树图加强时段判断 | 解决盘前「57=57 今天已有涨停」等误导 |
| 5 | 09:48 | `ec74e21e5` | 算法时序修复：`run_algorithms.py` 新增 `LHB_DEPENDENT_SCRIPTS` + `_is_post_lhb_ready()` 门控（未到 16:30 CST 跳过 6 个脚本）；`v6_memo.html` 新增「前端预测方法论总纲」 | 根治「龙虎榜 16:30 才发布、16:10 就跑最终推荐」时序错配 |
| 6 | 11:19 | `aad337e3a` | **【一劳永逸·已上线】三连修**：①噪声概念过滤（`_NOISE_CONCEPTS` 黑名单：标准普尔/富时罗素/MSCI中国等）②潜力挖掘页空白（容器 id `ppEtfMapBody`↔`ppBody` 不匹配，拆上下子卡）③健康巡检假警报根因修复 | 已 push origin/main，Pages 自动部署生效 |
| 7 | 11:30 | `43b2f285a` | 分时累计曲线快照也清理噪声概念残留（`sector_fund_flow_history.json` + intraday 双重过滤） | 配合 #6 清理线上历史噪声 |

> **`6d260f7e3` 说明**：本地 11:05 提交 `fix(health): 健康巡检优先读线上站点判新鲜度…`，与 #6 同源重复，**已被 `aad337e3a` 取代上线**，当前不在 main 且不在任何分支（悬空对象）。阿狸咪侧若仍见此对象，直接丢弃。

### B. 健康巡检假警报根因（一劳永逸，已随 #6 上线）

- **根因铁证**：`v8_health_check.py::check_data_cards()` 只读**本地** `data/<id>.js`；本机/看门狗机器从不 `git pull` 云端数据 → 本地 checkout 停在 08:38 → 连环误报「9 张盘中卡陈旧 fail」。而云端 9:00/9:30/10:00/10:30 每轮 cn_fetch 经 GitHub API 核验全 success（live=10:35），**云端从未坏过**。
- **修复**：新增 `load_window_var_from_site()` 优先读线上站点真实部署态，站点不可达才回退本地。fail 由 9 → 0。

### C. 云端自动化（当日已落地，无需阿狸咪干预）

- cn fetch：`5cfd3cd66` 11:50 ｜ `ffc777b23` 11:36 ｜ `f814511cf` 11:19 ｜ `25ac50a3f` 11:02（盘中每 30 分钟一轮正常）
- `8a7c4bbe2` 盘中动量+算法追踪轻量刷新 11:45 ｜ `f0df9e3e1` 智能触发补抓 11:07 ｜ `2ea40a31f` runner 状态上报 11:12
- build 链：`53a7f9be9`/`96c43935d`/`d74474abc`/`64a8c0bf0`/`15761910d`（11:03–11:23 多轮）｜ risk gauge：`b806a954c` 11:22、`5711b7ecf` 12:04

### D. 潜力挖掘 v2.0 框架重建（09:08，仓库外产出）

- 重设计为 6 大维度（政策催化/周期反转/技术突破/事件驱动/业绩拐点/AI新质生产力），全量检索 + westock 实时行情 28 只。
- 产出 `潜力挖掘_多维度前瞻_20260820.html` / `潜力挖掘_AI价值投资_20260820.html`（untracked，未入库）。

---

## 三、当前系统状态（12:13 实测）

| 维度 | 状态 |
|------|------|
| **站点（GitHub Pages）** | HTTP **200** 正常 |
| **main SHA** | `5711b7ecf`（`git ls-remote origin main` 实时值；首档写的 `5cfd3cd66` 已被 12:03 首档提交 `52561410d` + 12:04 risk gauge 推进） |
| **本地仓库** | 已 ff-only 对齐 origin/main，零落后无分叉；工作树仅 untracked（backup/ 14 项 + 2 个 HTML 报告）+ 1 个 stash（`handover-ff-blocker-2026-08-20-1200`，内含 HEALTH_CHECK.js WIP，推送后已还原） |
| **健康巡检**（`data/HEALTH_CHECK.js` 11:45:05） | overall **ok**，**ok=102 / warn=2 / fail=0**（total 104）。⚠️**修正首档**：2 warn 均为 runner 类（`runner_github`/`runner`：GitHub API 报 cn_fetch 最新一次失败、最新 run 卡 11 分钟；而本地态 11:36:06 **28 模块全成功**）。首档写的「3 warn=三重共识/驾驶舱/四量终极」是 10:5x 那轮旧快照，现已消 |
| **本机 runner** | Listener **PID 584 存活**（起 08:12:09）；Worker 当前无进程＝作业间空闲（正常）。每日 07:54 定时重启，**今日自恢复成功**，无需人工 Start-Process（对比 08-19 需手动拉起，自启链路已生效） |
| **cn fetch 模块** | `raw_data/runner_status.json` 11:50:13：**28/28 ok**，empty=0 fail=0 |
| **云端算法链** | `ebe513fde` 后 `v8_algo_cloud.yml` = ubuntu-latest，不依赖 cn runner；H_AUTO_BUY/TRIPLE_HISTORY 待今晚 18:30 跑通刷新 |
| **本机自动化** | 共 **15 个：14 ACTIVE + 1 PAUSED**（PAUSED=`1786933089412` 小九-TDX强势选股跟踪）。`1787108824799` 盘中漏轮兜底 rrule 已是 CST 正确档（`BYHOUR=9,10,11,13,14,15;BYMINUTE=10,40`），08-19 修复保持有效 |
| **盘中兜底实绩** | 09:19 非交易时段空转（<09:30）；10:19 第 2 轮判「非漏轮」（云端 10:00 轮已落地，worst age≈12.5 分 ≤35 分阈值） |
| **阿狸咪侧自动化** | 按铁律全部 PAUSED（禁止删除），恢复 ACTIVE 须主人明令授权 |

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 P0-1（本档新发现）STOCK_QUOTE 数据陈旧 + 健康巡检「假绿」盲区

- **实测**：`data/STOCK_QUOTE.js` 内层 `update_time = 2026-08-20 09:28:43`（盘中已陈旧 ~2.7h）。**线上站点用 Range 请求实测同为 09:28:43** → 确认是真陈旧，**不是**今早刚修掉的「只读本地误判」。
- **假绿盲区（铁证）**：健康巡检项 `all_STOCK_QUOTE` 报「STOCK_QUOTE.js 更新于 今日 **11:44** / ok」，判的是**文件时间**；内层数据实为 09:28 → 文件新鲜、数据陈旧，overall 仍 ok。且 `STOCK_QUOTE` **本身不在 104 个监控项 id 列表内**（只有派生的 `all_STOCK_QUOTE`）。
- **根因链**：`STOCK_QUOTE` **不在 `cloud_fetch_v8.py` 的 28 模块清单内**（云端 cn_fetch 每 30 分钟不抓它）。真正抓取者＝`algorithms/fetch_stock_quote_v8.py`，仅在 ① `v8_algo_cloud` 全套算法链（cron 18:30）② `v8_stock_quote_refresh.yml`（cron CST 17:00 兜底）里跑 → **按设计它是每日快照，非盘中刷新**。
- **下游影响面**：`calc_potential_picks.py`（潜力挖掘）、`auto_run_dn_algorithm.py`（DN/H 反推）、`gen_algo_track.py`、`gen_top5_track.py`、`gen_top5_track`/分红链均读它 → 盘中若触发，用的是 09:28 早盘价。
- **建议**（未擅自改，等主人授权）：① 健康巡检对 `STOCK_QUOTE` 改判**内层 update_time** 而非文件时间，消除假绿；② 若要求盘中准，把 STOCK_QUOTE 纳入 cn_fetch 模块或加盘中轻量刷新档。

### 🔴 P0-2（本档新发现）cn runner 解耦**未完成**，仍有 1 个带 cron 的 workflow 绑死 cn

- 今早 `ebe513fde` 只改了 `v8_algo_cloud.yml`，**漏了 `v8_stock_quote_refresh.yml`**：`runs-on: [self-hosted, cn]`（line 47）且 `schedule: cron '0 9 * * *'`（UTC → **CST 17:00**）。
- **后果**：主人出差期间（8/21–8/27）cn runner 一离线，该「兜底」链即 queued 死等 → STOCK_QUOTE 整周不刷新 → P0-1 影响面全周放大。**这条兜底本就是为「算法链一天只跑一次」设计的，现在兜底自己也绑在 cn 上，等于没兜底。**
- **一行修复**（同 `ebe513fde` 手法）：`v8_stock_quote_refresh.yml` line 47 → `runs-on: ubuntu-latest`。**待主人授权后再改，本档不动代码。**
- **审计结论（其余 cn 绑定均安全）**：`v8_algo_run.yml`、`v8_cn_fetch.yml`、`v8_cn_fetch_cloud_selfhosted.yml` 虽为 `[self-hosted, cn]` 但**均无 cron、仅 workflow_dispatch**，属设计内应急降级通道，不受 runner 离线影响。ubuntu 侧：`v8_algo_cloud`/`v8_build_deploy`/`v8_cn_fetch_cloud`/`v8_cn_fetch_cloud_hosted`/`v8_health_patrol` 全部 `ubuntu-latest` ✅。

### 🟡 观察项（非故障）

- **健康 2 warn（runner 类）**：GitHub API 报 cn_fetch 最新 run 失败/卡 11 分钟，但本地 28/28 成功且 `5cfd3cd66` 11:50 那轮已成功入库 → 判为**瞬时/滞后告警**，非实质中断，持续留意。
- **噪声概念线上清理**：#6/#7 后应自动清掉线上「标准普尔」等残留，可抽查 `data/SECTOR_FUND_FLOW*.js` 核验。
- **18:30 盘后算法链**：确认 ubuntu 版跑通、H_AUTO_BUY/TRIPLE_HISTORY 刷新为 8/20 当日值；若仍陈旧查 `v8_algo_cloud.yml` 是否生效。
- **setBadge id=phCrisisTime NOT FOUND**：非致命警告，待清理。
- **斐波那契窗口**：SH_FIB 全部 window status=passed 无 future，属正常（已显示「当前无在观测窗口」）。

### ⚠️ 风险

- **🚫 远端新出现分支 `feat/v8-detach-v6`（严禁 merge/checkout 覆盖 main）**：tip `9a899c8f8`（**08-03 期**），与 main 分叉＝main 独有 **4299** / 分支独有 **283**。误合会把 v8 回退到 8 月初状态，直接违反 `DO_NOT_DELETE.md` 算法铁律。
- **force-push 假警报已澄清（无提交丢失）**：本次 `git fetch` 报 `+ f760a67cd...5cfd3cd66 main (forced update)`，实测 `f760a67cd` 是 **2026-07-31** 的旧提交、现仅存在于 `origin/feat/v8-detach-v6` → 是本地 `origin/main` 跟踪 ref 被回滚（疑坚果云同步 `.git/refs`）造成的假象。**今日 7 个关键 SHA 已逐条 `merge-base` 验证在 main，零丢失。**
- **别乱改**：出差期间严禁结构重构/删功能/动红线文件（index.html 结构、v6_memo.html、candidate.json 及核心数据源）。确凿问题才修，修前先说清边界。
- **本地云端对齐**：开机若落后云端，优先 `git fetch + merge --ff-only`；勿用 rebase/stash 动工作树红线文件。

---

## 五、小九监督职责（12:00 档 → 18:00 档）

- **盘中实时监测**：cn fetch 每 30 分钟一轮、build 链、站点 live 新鲜度；重点复核 P0-1 的 STOCK_QUOTE 是否在 13:00 后仍停 09:28。
- **runner 监护**：Listener PID 584 存活性 + `runner_status.json` 上报（Contents API HTTP 200）；2 个 runner warn 是否自消。
- **18:00 档必做**：① 汇总 12:00–18:00 新增改动 ② 复核 18:30 ubuntu 版算法链是否跑通 ③ **把 P0-2 一行修复的授权结论写入下一档**（出差前必须闭环，否则整周 STOCK_QUOTE 断更）。
- **出差期间（8/21–8/27）重点盯**：①云端独立运转（cn runner 离线也不影响关键链）②盘后 18:30 算法链 ③健康巡检假警报/假绿不复发 ④本地云端对齐，主人「上线」信号到再 push。

---

_生成方式：自动化任务 `automation-1785719698947`（小九 12/18 点 v8 当日交接文档更新）。SHA 全部经 `git merge-base --is-ancestor <sha> origin/main` 实测核实，main SHA 取 `git ls-remote origin main` 实时值；数据新鲜度经线上站点 Range 请求实测，非本地单一来源。_
