# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-13 19:01｜星级委员会改多期加权+四量4⭐+修复JS语法残留

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

星级委员会规则从「单期持有期」改为「多期加权综合」，四量终极由 5⭐ 降至 4⭐；已修复因替换锚点过短导致的 JS 语法错误；完整跑过改后三件套（htmlcheck 0 失败 / align_logic_ops EXIT 0 / 本交接单）。

---

## 正文

### 1. 改动内容

- **commit `a3b13a64`**（main tip）：
  - `index.html` `__STAR_COMMITTEE_RULES` 改为多期加权综合规则：
    - 新增 `profiles`（short/medium/long）与对应权重；
    - 新增 `perPeriodGates`（胜率/收益/样本/夏普）；
    - 新增 `shortTermPenalty`：若 T+1 或 T+5 评分 ≤2，最高只能 4⭐；
    - 新增 `rankBonus`（enabled: false）：待统一回测框架后启用横向排名加成。
  - `__MODULE_STARS`：`fourvolume:5 → fourvolume:4`。
  - `__STRATEGY_BACKTESTS` 四量行：`stars:5 → stars:4`，desc 改为多期综合口径。

### 2. 四量终极评星计算（long 权重）

| 持有期 | 评分 | 权重 | 加权贡献 |
|---|---|---|---|
| T+1 | 2 | 10% | 0.20 |
| T+5 | 3 | 15% | 0.45 |
| T+30 | 4 | 25% | 1.00 |
| T+90 | 5 | 40% | 2.00 |
| T+180 | 5 | 10% | 0.50 |
| **综合** | — | — | **4.15（初步 5⭐）** |

因 T+1 评分=2 触发 `shortTermPenalty`，最终星级封顶 **4⭐**。

### 3. 严重 bug 与修复

- **问题**：`patch_star_multip.py` 替换锚点过短，导致 `__MODULE_STARS` 行和 `__STRATEGY_BACKTESTS` 四量行残留旧注释 / 旧 desc，产生 `Invalid or unexpected token` JS 语法错误。
- **修复**：commit `a3b13a64` 用整行替换清掉残留。
- **验证**：`htmlcheck.mjs` 25 段 inline script 0 失败。

### 4. 改后三件套

| 项 | 状态 | 凭证 |
|---|---|---|
| ① 审计 + 对齐云端/双机时窗 | ✅ | 仅改 `index.html` 静态规则，无 workflow/cron 变更 |
| ② 对齐运维页与逻辑详解页 | ✅ | `align_logic_ops.py` EXIT=0（21 workflow 全文档化） |
| ③ 交接另一台机 | ✅ | 本文件 |

### 5. 下一步（方案 D）

- 待统一回测框架后，所有策略输出同持有期（T+90）收益；
- 启用 `rankBonus`：T+90 横向排第一 +0.3 星，被超过则取消；
- 中期引入沪深300/中证500买入持有基准，计算信息比率。

---

**发送**：阿狸咪的工程师 @ `alimi-cn`
**接收**：小九 @ `lemoncat-cn`
