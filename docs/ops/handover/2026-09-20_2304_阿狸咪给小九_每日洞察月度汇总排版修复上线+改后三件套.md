# 阿狸咪 → 小九 · 每日洞察「月度汇总」排版修复上线 + 改后三件套

- **时间**：2026-09-20 23:04（北京时间，实取）
- **发送方**：阿狸咪的工程师（家机 `alimi-cn`）
- **接收方**：小九的工程师（单位机 `lemoncat-cn`）
- **关联提交**：`176d1a8341`（我方，本件主体）→ `01d0ff4d9`（你方，沿用我的 `index.html`，未改动）
- **主人指令原文**：「上线」

---

## 一、上线内容

**需求**（主人原话，配合 5 张截图）：每日洞察卡片里的**三方共振月度汇总**——
1. 把「口径说明」移到**标题下方**；
2. 净流入 / 净流出 TOP10 **两卡宽度对齐上方**「机构独买 / 游资独买」两卡。

**根因**（真实浏览器渲染数值实证，非推测）：

`index.html` 第 4629 行「机游共振（短线）」三卡容器 `insight-grid` **漏写闭合 `</div>`**，
导致其后的「月度汇总标题 + 两卡 + 口径说明」被浏览器吸入该**三列网格的第 2 行**，各自被压成一列：

| 元素 | 修复前实测 | 视觉表现 |
|---|---|---|
| 月度汇总标题 | 419px（col1） | 挤在左侧 |
| 月汇总两卡 | 419px 内再分两列 = 各 **201px** | 中间很窄 |
| 口径说明 | col3 | 跑到右侧 |

对照：上方「机构独买 / 游资独买」卡 = **419px**。

**改动（3 处，全在 `index.html` 4662–4742 区）**：

1. 补上缺失的 `h += '</div>'`，闭合 `insight-grid`；
2. 口径说明移到**标题正下方**（全宽、居中）；
3. 月汇总容器改三列网格（`grid-template-columns:repeat(3,1fr)`）⇒ **两卡各 419px = 上方两卡同宽**（右侧留 1/3，与上方三列网格对齐）。

**未改动**（有意保留）：机游共振日历的月度汇总（约 3892 行）、龙虎榜北向月度汇总（约 4287 行）——两处的裸 `<div class="month-summary">` 仍为原样，不受影响。

---

## 二、上线方式（防覆盖，零回退）

🔴 **关键动作：不以本机工作树为基线推送。**

实测：本机工作树 `index.html` 相对远端**落后**（缺你方当日 CRDS「两项/三项满足改名称左+CRDS右」两列布局等改动）。
若直接用本机工作树推送 ⇒ 结构性回退你的改动。

**实际做法**：

| 步骤 | 动作 | 证据 |
|---|---|---|
| ① 取基线 | `git cat-file -p FETCH_HEAD:index.html` → 落盘 | blob `69e7d923…`，**硬校验 == `FETCH_HEAD:index.html` 的 blob hash** |
| ② 重打补丁 | 在远端基线上精确替换 3 处，每处**唯一命中**校验 | `P1_grid_close/P2_move_note/P3_del_note` 各 `matched=1` |
| ③ 区段自证 | 产物月度汇总区 与 已渲染验证版 **逐字节 ==** | sha256 `b1d1c7b0…` / `ee91585f…` |
| ④ 门禁 | `check_inline_js.js` | 28 段 / **0 错** |
| ⑤ 推送 | Git Data API `base_tree`（只 `index.html`）、`force:false` | `176d1a8341`，推后回读 blob **逐字节 ==**（`51ab6286…`，1337548 B） |

**推送期一次非快进（422）**：第 1 轮遇 `260290c0dad0` 抢先 → 自动重拉重试 → 第 2 轮成功。
⚠️ **由此产生的唯一副作用**：我方基线早于 `260290c0dad0` 一次 `v8 build` 打戳，
故我的提交把该次 `?v/BUILD` 值**短暂推退**了一步。**已由 CI 自动自愈**（见下）。

---

## 三、线上验证（硬证据）

### 3.1 归一化逐字节比对（`?v=` / `BUILD` 归一后比）

按铁律：`?v=` 与 `BUILD` 会被 CI 重打，**必须归一化后再比**。

| 对象 | 归一化后 sha256（前16） | 字节 |
|---|---|---|
| 线上 `index.html`（`?nc=2` 破缓存取） | `261768b30a8c672f` | 1336525 |
| 我方推送源 | `261768b30a8c672f` | 1336525 |

⇒ **完全一致**：线上内容 = 我推的内容，仅缓存戳被 CI 更新。

### 3.2 缓存戳单调性（自愈确认）

| 时点 | `?v` | `BUILD` |
|---|---|---|
| `260290c0dad0`（被我短暂推退的那次 build） | 1789914966 | 9ad6bbc40 |
| **线上现状** | **1789915201**（更大 ✓） | **01d0ff4d9** |

### 3.3 真实浏览器渲染量测（线上，`1440×1000`）

| 指标 | 线上实测 | 期望 | 结论 |
|---|---|---|---|
| 月度汇总标题宽 | 1280 | 全宽 | ✅ |
| 月汇总容器宽 / 列 | 1280 / `418.66px × 3` | 三列 | ✅ |
| **两卡各宽** | **419 / 419** | **= 上方卡片 419** | ✅ |
| 上方机构独买/游资独买卡宽 | 419 / 419 | — | 对照 |
| 口径说明 | 宽 1280、`text-align:center`、位于标题下 **+31px** | 移到标题下方 | ✅ |
| 两卡位置 | 位于口径说明下 **+52px** | — | ✅ |
| 顺序 | 标题(876) → 口径(907) → 两卡(959) | 标题→口径→两卡 | ✅ |

### 3.4 受保护标记（`[10/10]` index 核心标记守卫，等价复现）

真源 `docs/ops/index_protected_markers.txt`（34 条），逐条核线上 `index.html`：

```
[线上 index.html] 命中 34/34   ✅ 全部命中
[本地 index.html] 命中 34/34   ✅ 全部命中
```

### 3.5 CI（云端权威门禁）

我方提交 `176d1a8341` → run `35517054753`「☁️ v8 构建部署(云端ubuntu)」= **success**

| job | 结论 |
|---|---|
| `gate` | **success** |
| `build` | **success**（step10「Pre-deploy audit」success；step7/8/11/12 四条护栏 success；step13 部署 success） |
| `intraday_patrol` | skipped（非盘中时段，正常） |

---

## 四、改后三件套

### ① 时窗矩阵对表（实时取远端树）

```
workflow 文件数 = 30
含 cron 的 workflow = 21
■ cloud_weekly_cleanup.yml   UTC 0 13 * * 6   → CST 0 21 * * 6   （周六 21:00）✓
■ v8_algo_cloud.yml          8 档：16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 / 06:00 / 08:00 ✓
■ v8_backup.yml               UTC 0 15 * * *   → CST 23:00       ✓
■ v8_cleanup.yml              UTC 0 15 * * 0   → CST 周日 23:00   ✓
```

**结论**：与本轮前基线（`docs/ops/handover/…2026-09-19_1608…双机时窗矩阵` §三）一致；
**本轮无 cron 变更** ⇒ **矩阵无需调整**。
今日 workflow 侧唯一事件 = 无（本轮只改 `index.html`）。

家机（阿狸咪）ACTIVE 自动任务：**本轮未新增 / 未改动任何任务**；
覆盖守卫 `f01daa2c`（每 30 分）、盘中巡检 `94279c7c`、暴风盯梢 `50eda171` 均 ACTIVE ⇒ **无错峰冲突**。

### ② 门禁

- **云端（权威）**：见 §3.5，`gate=success`、`build.step10 Pre-deploy audit=success`。
- **离线复现**：见 §五（原始输出）。

### ③ 本件（交接）

---

## 五、离线门禁复现（原始输出）

**staging**：仓库外 `E:\workspace\_gate_20260920_night`（**先清空再镜像**，遵 skill 坑⑤）
**镜像 tip**：`87e5523aa40d`（实时取）· 门禁所需 **399** 路径 · 跳过(已就绪) 259 · 新拉取 140 · **失败 0**
**`index.html` = 1337548 B**（含本次修复；CI 重打戳不改变长度）

### 5.1 `.github/scripts/pre_deploy_audit.py`

```
============================================================
v8 pre-deploy audit（CI 自动门禁…2026-09-20 扩至 9 项（回测口径守卫）；同日扩至 10 项（[10/10] index.html 核心标记守卫·防旧树覆盖））
============================================================
  ✅ [1/8] py_compile: 194 个 .py 文件 0 错误
  ✅ [2/8] new Function: 28 个 inline script 块 0 错误
  ✅ [3/8] data 完整性: data/*.js 数量=113（上界 130（动态＝面 108 ＋ 容差 20））, 全部 > 100B
  ✅ [4/8] align_logic_ops: align_logic_ops EXIT 0（逻辑详解页与真 workflow 对齐）
  ✅ [5/8] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
  ✅ [6/8] HTML 数据引用: 179 个本地 script 引用全部存在（4 页）
  ✅ [7/8] gate 头注一致: 闸门头注与 READY_SPEC 真源一致（项数/门槛/P0 专项）
  ✅ [8/8] 心跳产物名一致: 心跳产物名大小写一致（权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移）
  ✅ [9/9] 回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
  ✅ [10/10] index 核心标记守卫: index.html 核心标记 34/34 全部在位（卡片/分节/数据源锚点）
============================================================
🎉 10 项全部通过 → deploy 可继续
```

### 5.2 两个独立跨层脚本

```
=========== align_logic_ops.py ===========
📚 文档源（2026-09-10 起三源合并）:
   · index.html#sec-lg      0 字符
   · index.html#sec-op      616655 字符
   · logic.html#sec-lg      527044 字符
🔧 实际有 cron 的 workflow：21 个；名单：MUST=21 / ALLOW=3
✅ 对齐校验通过：关键 workflow 均已文档化，无过期引用，名单无死登记。
EXIT=0

=========== v8_verify_layer_parity.py ===========
✅ data/*.js 与 raw_data/*.json 时间戳一致，且无陈旧静默上线风险
EXIT=0
```

---

## 六、🔴 登记（需你知悉，但**请不要动**）

1. **本机工作树 `index.html` 落后远端**（家机 `alimi-cn`）：缺你方 CRDS 两列布局等改动，
   归一化后 sha `65e24c2f…`（1335741 B）vs 线上 `261768b3…`（1336525 B）。
   按纪律**只登记不擅动**（生产工作区有大量数据脏文件，禁 `checkout`/`reset`/`pull`）。
   本机 `HEAD=5bd826b60`，落后远端 tip **21 个提交**（`git rev-list --left-right --count HEAD...FETCH_HEAD` = `0 21`）。
2. **本机工作树 `index.html` 已含本次修复的 3 处**（本会话所改），仅缺远端其他改动 —— 后续若要在家机重建基线，请以上述结论为准。

---

## 七、待你应用项

**无**。本次改动仅 `index.html`（PAT 可写），已由我方直接推送并验证生效，无需你侧动手。

---

## 八、复核命令（判据，非本机私有工具）

| 判据 | 期望 | 取数方式 |
|---|---|---|
| 线上 `index.html` 含修复 | 命中 1 次 | `curl -s "https://ah-quant999.github.io/quant-scanner-v8/?nc=<epoch>"` → grep `告警串「补上「机游共振（短线）」insight-grid 缺失的闭合标签」`、`grid-template-columns:repeat(3,1fr);gap:12px` 各 = 1 |
| 两卡宽 = 上方卡宽 | 419 == 419 | 真浏览器渲染，量 `.month-summary .ms-col` 与 `.insight-card` 的 `getBoundingClientRect().width` |
| 口径说明位置 | 在标题下方、居中 | 比口径 div 与 `h3` 的 `getBoundingClientRect().top`（应 ≈ +31px） |
| `[10/10]` 标记守卫 | 34/34 | 用 `docs/ops/index_protected_markers.txt` 逐条核 `index.html` |
| CI 门禁 | `gate=success` | `GET /repos/ah-quant999/quant-scanner-v8/actions/runs?per_page=10` → 找 `head_sha=176d1a8341` |

---

> 本件由阿狸咪的工程师（家机）发出，供小九的工程师复核。
