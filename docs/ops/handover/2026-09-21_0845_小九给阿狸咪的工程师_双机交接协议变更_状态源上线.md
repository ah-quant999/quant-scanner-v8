# 小九 → 阿狸咪的工程师 · 双机交接协议变更（状态源上线）

- **发起**：小九（`lemoncat-cn`）· 2026-09-21 08:45 CST
- **性质**：🔴 **结构性变更**（改了双机共同依赖的交接口径）—— 属允许写档的两种情况之一
- **主人指令**：「最近双机联合改动大，是不是遇到很多交接方面的问题，你有什么办法解决么？不浪费时间也不浪费token」→ 小九提案 → 主人批准「你确认没问题就推吧」

---

## 一句话

**交接档从「每次改动都写」改为「只在需要人拍板时写」；
「什么还没做」统一收进 `docs/ops/HANDOFF.yaml`。**

---

## 一、为什么要改（实测数据，非感觉）

| 指标 | 实测值（2026-09-21 08:4x 采集） |
|---|---|
| `docs/ops/handover/` 档总数 | **518 个** |
| 近 7 天字节数 | **431 KB** |
| 单日峰值（09-20） | **62 档** |
| 单小时峰值（09-20 21 时） | **9 档** |
| 「回执_未回执交接对齐」纯握手档 | **22 个 / 417 KB / 均值 19 KB / 零技术内容** |
| 同一标题重复 | **7 次** |

### 三层病根

1. **把「聊天」当「交接」** —— 回执档（「收到」「已核验」）+ 催办档（「还没回我」）。
2. **同一件事拆成 3–5 档** —— 实测 `06:58(改动) / 07:05(收口) / 01:47(催办)` 是**同一件事**。
3. **状态散落 ⇒ 重复劳动** —— 今天 08:2x 我做防覆盖审计时，**无法从档里知道当前挂账**，
   只能重新实测一遍。**档没起到「不重复劳动」的作用，反而成了检索噪音。**

**真实代价**：每档 = 写 + push + 对方拉 + 读 + 回写，双机各一次；且永久占用 git 历史。

---

## 二、改什么（三条）

### 2.1 状态收进一个文件

**唯一权威来源**：`docs/ops/HANDOFF.yaml`

已录入 **11 项**（9 项未完成 + 2 项已完成供追溯）：

```
[pending-verify] build13-timeout-guard        shared
[todo          ] cachebot-build-race          小九
[todo          ] concurrency-block-bucket     小九
[todo          ] build13-step-timeout-minutes 小九
[pending-verify] stale-index-landmine         shared
[todo          ] alimi-same-family-check      阿狸咪   ← 请你看这条
[blocked       ] local-worktree-behind        小九
[todo          ] maharo-daily-s4u             小九
[todo          ] defer-cache-empty-freeze     小九
```

状态取值：`todo` / `doing` / `pending-verify` / `blocked` / `done`

🔴 **`pending-verify` 的定义**：改动已上线，但**尚未被运行验证生效**。
**此状态不得对外宣称「已修复」**，必须填 `verified_at` 才能置 `done`。
（这条是针对我们俩都犯过的错：`代码改了` ≠ `产物重跑了` ≠ `被 run 验证生效`。）

### 2.2 禁止回执档与催办档

| 你想做的事 | 正确做法 |
|---|---|
| 告诉对方「我收到了」 | 改 `HANDOFF.yaml` 对应 item 的 `status` 字段（**不发档**） |
| 告诉对方「我改完了」 | 写 commit message + 改 `status`（**不发档**） |
| 催对方「还没回我」 | 说明 item 的 `owner`/`verify_how` 没写清 ⇒ **修 item，不加档** |
| 汇报进度 | **commit message**（`git log` 天然可查） |
| 说明技术细节 | **代码注释**（改代码的人必然看到） |

### 2.3 只在两种情况下写 .md 档

1. **需要人拍板** —— 必须含「选项对比 + 明确推荐 + 理由」。
2. **跨机需知的结构性变更** —— 比如本档。

---

## 三、请阿狸咪执行（3 条）

### 3.1 接入状态源（最重要）

开工前读 `docs/ops/HANDOFF.yaml`，**不要先 `ls docs/ops/handover/ | head -30`**。
做完事更新对应 item 状态，**不发档**。

### 3.2 跑同族风险排查（`alimi-same-family-check` 项）

你机若有同名脚本的**本地副本**（非从 main 拉取），可能仍是旧写法：

```bash
cd <你机仓库根>
# 期望：非注释行命中 = 0（除定义体）
grep -rn 'checkout", "origin/main", "--"' --include=*.py . | grep -v __pycache__
grep -rn 'checkout", "HEAD", "--", f"raw_data' --include=*.py . | grep -v __pycache__
# 期望：4 处裸提交全部带 --
grep -rnE '"commit", "-m"' --include=*.py . | grep -v __pycache__ | grep -v script_sync_backup
# 期望：staged=0
echo "staged=$(git diff --cached --name-only | wc -l)"
```

**若 `staged` 非 0** ⇒ 按 `v8-git-stale-index-landmine` §四 拆弹：
`cp .git/index backup/…bak` → `git reset -q HEAD`（**严禁** `reset --mixed origin/main`）。

### 3.3 采纳 skill（可选但推荐）

`~/.workbuddy/skills/v8-handoff-gateway/SKILL.md` 是本协议的完整版（含格式、SOP、判据陷阱）。
你机若也有 skill 目录，建议放一份同内容 —— 让 AI 会话自动遵守，**不靠人记**。

---

## 四、旧档处置（不删）

- 518 个旧档**不删除**（是审计证据链）。
- 本目录 `README.md` 顶部已加指引：**当前挂账见 `../HANDOFF.yaml`**。
- 查旧档用 `grep -rl "关键词" docs/ops/handover/`（只出文件名，不读内容）。

---

## 五、预期效果

| 指标 | 改前 | 改后（目标） |
|---|---|---|
| 交接档 / 天 | 62 | **5–10** |
| 纯握手档 | 22 个 / 417 KB | **0** |
| 定位「什么还没做」 | 翻 518 个档 | **grep 1 个文件** |

---

## 六、本档自身就是新规则的示范

本档属于「**跨机需知的结构性变更**」—— 属允许的两种情况之一。
除此之外，本轮**不再发任何回执档**。你收到后**不必回档**，改 `HANDOFF.yaml` 里
`alimi-same-family-check` 的状态即可。

---

*撰写时刻：2026-09-21 08:45 CST ｜ 数据采集时刻：2026-09-21 08:40–08:45 CST*
*以上数据均为该时段实测；请以你自己执行 §3.2 命令的结果为最终依据。*
