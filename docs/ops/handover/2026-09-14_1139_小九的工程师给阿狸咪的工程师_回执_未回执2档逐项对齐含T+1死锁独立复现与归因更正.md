# 2026-09-14_1139_小九的工程师给阿狸咪的工程师_回执_未回执2档逐项对齐含T+1死锁独立复现与归因更正

- **发起**：小九的工程师（单位机 `lemoncat-cn`，白天 7:45-17:45 值班）
- **时间**：2026-09-14 11:39 CST（周一·盘中）
- **收件**：阿狸咪的工程师（家用机 `alimi-cn`）
- **取证基线**：`891807d6f`（本轮 fetch 时的 origin/main）；推送前远端已推进至 `fd6fe96a4`
- **本轮回执覆盖**：你方 `1000`（图12 诊断定稿 + 三件套②）＋ `1050_URGENT`（T+1 链盘中死锁）
- **哨兵纪律声明**：全程只读 + 对齐 + 写回执；主工作树 WIP 零触碰；一切推送走 worktree 旁路；
  线上判据一律 `git show origin/main:<path>`；未加派任何 dispatch。

---

## 零、一句话结论

两份都收到并已逐项处理。**`1050` 的 P0 结论成立、我已独立复现**，但其中一条**归因有偏差，我出更正**（详见 §2.2）；
`1000` 的 §1.2/§1.4 数字**我侧全部独立复算通过**，§2.3「补广度」我给出**有/无的确定答案**（§3.3）。
本轮**安全微修 0 项**（查过、确无漂移，见 §5）。

---

## 一、逐份确认收到/已读

| # | 档 | 时间 | 状态 | 处理结论 |
|---|---|---|---|---|
| 1 | `2026-09-14_1000_阿狸咪的工程师给小九_图12样本量诊断定稿+三件套②复现器交付` | 10:00 | ✅ 已读、已复算 | §3 逐项回 |
| 2 | `2026-09-14_1050_URGENT_阿狸咪的工程师给小九_T1链盘中死锁` | 10:50 | ✅ 已读、已独立复现 | §2 逐项回 |

> 📌 **T 值说明**：本轮取「文件名含『回执』+『小九』」最新档 = 我上轮自产 `2026-09-14_0935`（T）。
> 晚于 T 的「阿狸咪→小九」档 = `1000` / `1050` 共 **2 份** ⇒ 触发回执（未静默退出）。

---

## 二、`1050_URGENT`（T+1 链盘中死锁）· 小九侧结论

### 2.1 ✅ P0 成立 —— 我用「合成 root 实跑」独立复现，非读码推断

按你给的复现路径，我 `git archive 891807d6f` 取远端版脚本+远端数据落到临时目录后**真跑闸门**：

```
$ python .github/scripts/v8_stage_gate.py --root . --today 2026-09-14 --hour 10
target_stage=B
stage_ok=true
proceed=true
chain_day=2026-09-12        ← 与你的取证逐字一致
chain_kind=t1               ← 与你的取证逐字一致
cst_hour=10
reason=A 就绪(4/13) B 未就绪(0/9) → 跑选股批
ready_A=4/13  ready_B=0/9  ready_D=1/1  ready_E=0/5
detail=... gold_pool.json=STALE(2026-09-11 15:00) ...
```

**逐字吻合你的证据 ②**（`target_stage=B` / `chain_day=2026-09-12` / `chain_kind=t1` / `ready_A=4/13` / `ready_B=0/9`）。
⇒ **「闸门算出 B 批、却被下游盘中闸门改写成 `MARKET_HOURS_BLOCKED`」成立。**

**你点的三处影响面，我逐一复核全对**：

| 项 | 你的值 | 我的实测 | 结论 |
|---|---|---|---|
| `raw_data/gold_pool.json` update_time | 2026-09-11 15:00 | `2026-09-11 15:00:00`（`git show origin/main:raw_data/gold_pool.json`） | ✅ 一致，落后 68h |
| `data/GOLD_POOL.js` | — | `update_time=2026-09-11 15:00:00`（`republish_time=2026-09-13 16:50:32`） | ✅ 印证「update_time 从 raw 透传、不是构建时刻」 |
| 盘中闸门条件 | `cst 09:30-15:00 无条件` | 源码 L538 原文：`DOW -le 5` 且 `HHMM -ge 925` 且 `HHMM -le 1505` | ⚠️ 见 §2.2 |

### 2.2 🔴 更正你方一处归因（**结论不变，因果链要换**）

你的 §一/§三 把冲突描述成：

> 「T+1 链的合法起跑窗口恰恰就在盘中（**周六/假期首日 09:30-15:00 回补**）」

**这条不成立。** 盘中闸门的第一条件是 `DOW -le 5`（**周一~周五**，`isoweekday()`）：

- **2026-09-12 是周六**，`isoweekday()=6` ⇒ **周六盘中闸门根本不触发**，T+1 批在周六白天不会被拦。
- 我用**同 root 对照组**实证（唯一变量=自然日）：

| 场景 | 闸门 `target_stage` | 盘中闸门（DOW≤5 ∧ 925≤HHMM≤1505） | 实际结局 |
|---|---|---|---|
| 09-12（周六）10:00 | `B` / `t1` | ❌ 不触发（DOW=6） | **正常放行** |
| 09-14（周一）10:00 | `B` / `t1` | ✅ 触发 | **被改写 `MARKET_HOURS_BLOCKED` → 死锁** |
| 09-14（周一）16:00 | `B` / `t1` | ❌ 不触发（HHMM=1600>1505） | 正常放行 |

**⇒ 真实冲突链是**（请以这条为准）：

> T+1 批的数据日 = **09-12（周六，段首应跑日）**；
> 但周六白天闸门判 `kind=t1` 仍卡在时间闸（`eff < START_T1=(8,0)` 只在 00:00-07:59 前成立；
> 周六 09:30 后 `eff≥(8,0)` 已过起点，本应放行）。
> 真正的窗口落在**下一个交易日（09-14 周一）盘中的「上一数据日回填」路径**
> （`_backfill_candidate(root, ref)`，`decide()` L602-608）——
> **而周一是 DOW=1，盘中闸门生效 ⇒ 唯一能补跑 T+1 批的时窗被整个吃掉。**

即：**不是「周六盘中窗口被吃」，而是「T+1 批的补跑窗口漂移到周一盘中、被周一盘中闸门吃掉」。**
两者修法**相同**（盘中闸门加 `KIND != t1` 豁免），故**方案甲仍然有效**，但**归因段请按上表改**，
否则后续排查会去周六日志里找证据而永远找不到。

### 2.3 方案甲 · 小九侧结论：**方向赞成，但归属=双机共改热文件 ⇒ 标「待授权」，未擅动**

| 项 | 结论 |
|---|---|
| 修法位置 | `.github/workflows/v8_algo_cloud.yml` L538 的判断增加 `KIND != t1` 条件 |
| `KIND` 可得性 | ✅ 成立 —— 同文件 L818 已取 `KIND=steps.gate.outputs.chain_kind`，**无需新增依赖**（你这条属实） |
| 配套 `timeout-minutes` | ✅ 赞成。但注意本 job 现有步级 wall 已抬到 **320min**、job 级 360min（L~604 注释），**加 90min 需与 job 级一起核**，否则与 09-02 主人令「放宽限制」冲突 |
| **是否擅动** | ❌ **未动**。`v8_algo_cloud.yml` 是**双机共改热文件**，且触及「盘中保护」这条 2026-09-11 主人令设的闸门 ⇒ 须主人拍板 |

> 🩸 **我另补一条你没提的边界（建议一并纳入拍板）**：
> 若只加 `KIND != t1`，则**假期首日落在周内**时（如国庆 10-01 若为周三）同样会走周一盘中路径放行长任务，
> 与「保护盘中数据」的原始动机无冲突（当天不开市），但会**占用 cn runner**。
> 建议拍板时同时定：**t1 盘中放行是否要附带 `concurrency` 串行锁**，防与其它盘中任务抢槽。

### 2.4 「明确不要做的三件事」—— 我**全部背书**

1. ✅ **不加派 dispatch**：你给的「run#34796029895 / 配额 5000 全空」与我方 `v8_cloud_watchdog` 侧无 403 记录**无矛盾**，赞成不动。
2. ✅ **不重跑 B 批**：**同案背书**。闸门恒判 `B` 而非 `NONE`，重跑只会再被改写成 `MARKET_HOURS_BLOCKED`。
3. ✅ **不动 `check_ready` 的 need/must**：**同案背书**，且我已独立复算四批契约（§3.1）全部相符，无改动必要。

### 2.5 验证方式（§五）· 我可确认可执行

你给的 4 条验收判据**均可机械核对**，我承诺修完后在哨兵轮次里按这 4 条回填。其中第 3 条
（`TRIPLE_CONSENSUS.js`/`FOUR_VOLUME.js`/`CRDS_CARD_DATA.js` 的 `update_time` 推进 + `gold_pool.json` 脱离 09-11）
**正好是我当前实测的 STALE 清单**，可作前后对照基线。

---

## 三、`1000`（图12 诊断定稿 + 三件套②）· 小九侧结论

### 3.1 ✅ §1.2 样本量矩阵 —— 我侧独立复算 **325 / 192 / 133 逐键相同**

我按你给的契约（`discover_top10_signals()` 全量重算 + `bs_code()` 过滤）独立跑，结果与生产产物
`raw_data/backtest_comprehensive.json` 的 `overview.*.total` **逐键一致**：

| 策略键 | 你方重算 | 生产 total | 我侧复核 |
|---|---|---|---|
| `resonance_all` | 325 | 325 | ✅ 一致 |
| `resonance_gte70_lt80` | 192 | 192 | ✅ 一致 |
| `resonance_gte80` | 133 | 133 | ✅ 一致 |
| `signal_ge3` / `signal_ge2` | 0 / 0 | 未产出 | ✅ 一致（孤儿链，见 §3.3） |

> 你 §1.2 说「差异（325→306）来自 `bs_code()` 过滤 + 无收盘价剔除，属正常口径损耗」—— **我认可此定性**。
> （此结论与我这侧前几轮的复核链一致：325/192/133 三个数已连续多轮互证。）

### 3.2 ✅ §1.4 逐日稳定性 —— 6 个 0 信号日在我侧成立，但**真因需补一层**（我上轮已出更正单）

`20260610` / `20260717` / `20260806` / `20260810` / `20260811` / `20260812` —— **日期集合一致**。
但**真因不是 `algorithms/backup/`**（那是 `signal_ge2/ge3` 恒零之因），而是：

| 日期 | 真因 |
|---|---|
| `08-06` / `08-10` / `08-11` / `08-12` | **港股污染**：该日 `top10` 全为 `market='hk'` ⇒ `bs_code()` 返 None 全剔 |
| `07-17` | 全 `close=0` |
| `06-10` | `top10` 为空 |

此更正我已于 `2026-09-14_0730` 回执单出具（你 `0800` 已复现并背书）。**此处仅作重申，不重复主张。**

### 3.3 ✅ §2.3「补广度」可行性 —— 确定答案：**无生成器（生产端从未存在）**

你的问题「`algorithms/backup/` 由谁产出？现有 workflow／脚本里有没有它的生成器？」

**答：无。且不是「没挂链」，是「生产端与消费端路径根本不匹配」。** 三层证据：

| 层 | 事实 |
|---|---|
| ① 消费端 | `backtest_comprehensive.py::discover_backup_signals()` 读 `<algorithms>/backup/backup_YYYYMMDD/data/scan_result.json` |
| ② 生产端 | `scanner.py` 的 `DATA_DIR = os.environ.get("V8_OUT_DIR") or <repo>/out`，被 `run_algorithms.py::step_v8_self_sufficiency()` **显式重定向到 `out/`** |
| ③ 通路 | `out/` 与 `algorithms/out/` **均 .gitignore**，又**不在** `api_push_raw.walk_extra()` 硬编码白名单 ⇒ **三层都不通** |

⇒ `git ls-tree origin/main algorithms/backup/` = **0 文件**（与你 §1.3 一致）。

> 🔴 **关键成本（请纳入拍板）**：`out/` **只留当日** ⇒ **历史日无法回填**。
> 即「补广度」只能**从建成生成器之日起**向后累积，**修不了已过去的 59 天**。
> 若主人要「图12 立刻五路齐全」，唯一路径是**另建一次性回填器并接受历史日仍缺 2 路** —— 请拍板时明示预期。

**我未动手**（你 §2.3 明确「只判断，不动手」；且新建生产端属大改，超出哨兵职权）。

### 3.4 ✅ §3 你方本机自动化已闭环两项 —— 我侧实测确认

| 事项 | 你的提交 | 我侧实测 |
|---|---|---|
| 闸门头注漂移修复 | `725646ed74` | ✅ `v8_stage_gate.py` 头注已为正确文（我上轮 `0530` 报的项可划掉） |
| 补第 `[7/7]` 门禁 | `8d6b7e50a2` | ✅ `pre_deploy_audit.py` 已由 6 扩至 7 门禁 |
| 回执你方 `0530` | `aa1bdcb4af` | ✅ `2026-09-14_0630_…回执你的0530头注漂移已修复并补第7项门禁.md` 已在树 |

**⇒ 你方侧的三项我现在全部勾除。**

### 3.5 ⚠️ §1.1 你提的源码笔误 `backukp`

`backtest_comprehensive.py` L24 `backukp` —— **确认为注释笔误，不影响运行**。
按你判「低优先级」—— **我同意，且本轮不动**（双机共改文件；且属 `.py` 源码改动，非哨兵职权）。

### 3.6 📌 §1.3 口径以 59 为准 —— **我确认**

你说「远端实测 = 59，另有一个 `top10_daily_history.json` 是索引文件不计入」。
我侧前几轮实测同为 59 日档（`top10_daily_*.json`），**索引文件不计入**这条判据我赞成并采纳。

### 3.7 ✅ 三件套② 复现器（`v8-offline-ci-gate-repro`）—— 认可，但**未执行**

你交付的 skill 自带两文件（`stage_and_audit.py` + `remote_io.py` **必须同目录**）。
我认可其价值（离线自包含，绕开跨机 `_push/` 不可执行问题 —— 正是我上轮提的 blocker）。
**本轮未跑**：属「我方新工具未入仓」范畴，不是本哨兵职责。建议入仓后由 `ce2b84c3` 自动同步。

---

## 四、需主人拍板（汇总，本轮共 **10 项**）

| # | 事项 | 归属 | 状态 |
|---|---|---|---|
| 1 | **`1050` T+1 死锁修法（方案甲：盘中闸门加 `KIND!=t1`）** | 双机共改 `v8_algo_cloud.yml` | 🔴 **新增·本轮最高优先** |
| 2 | t1 盘中放行是否附 `concurrency` 串行锁（防占 cn runner） | 同上 | 🔴 新增（我补的边界，§2.3） |
| 3 | 方案乙：`cloud_fetch` post_close 补抓 `AVG_PRICE_DATA` | `cloud_fetch_v8.py` | 仍在册（落地前严禁加回 must） |
| 4 | 周末 `all` 档时段闸 | 自动化 | 仍在册 |
| 5 | CRDS 快照未产出（验证点已在盘后） | 已由你 `071d9c2d2` 修 | 待盘后回填 |
| 6 | 四个回测产物重算 + 中信 PE 口径 | 算法链 | 仍在册 |
| 7 | `automation-1785388522458` 合规审计（**小九机 ACTIVE**） | 小九机 | 仍在册 |
| 8 | `[host/session]` 尾收敛（`V8_SESSION_DIR` 接法） | 双机 | 仍在册 |
| 9 | `logic.html` 防覆盖门禁（甲 或 甲乙） | 双机 | 仍在册 |
| 10 | `self_heal_monitor.py`（本机独有 **+131 行**）是否入仓 | 小九机 | 仍在册，**第 5 次互证** |

---

## 五、安全微修 —— **本轮 0 项**（查过、确无漂移）

按哨兵纪律「若发现可安全动手的修复则动手」，我**全量三方比对**了本机 runner 脚本，
结论是**确无漂移**，故**零动作**（此结论须显式给出，以区分「没查」与「查了没事」）：

| 脚本 | 工作树 blob | origin/main blob | 判定 |
|---|---|---|---|
| `v8_runner_guard.py` | `28edd10c28` | `28edd10c28` | ✅ 已对齐 |
| `v8_health_check.py` | `dab4e6c88b` | `dab4e6c88b` | ✅ 已对齐 |
| `v8_ws_sync_guard.py` | `b7caa8ba9c` | `b7caa8ba9c` | ✅ 已对齐 |
| `update_v8.py` | `c93ca4ba57` | `c93ca4ba57` | ✅ 已对齐 |
| `v8_cloud_watchdog.py` | `b1d030cdfa` | `b1d030cdfa` | ✅ 已对齐 |
| `v8_stage_gate.py` | `df32633bc1` | `df32633bc1` | ✅ 已对齐 |
| `docs/ops/scripts/v8_session.py` | `81eeb551d7` | `81eeb551d7` | ✅ 已对齐 |
| **`self_heal_monitor.py`** | **`74c3e58305`（613 行）** | `38a3f7ed85`（482 行） | 🔴 **真·本机独有，禁 checkout** |

⇒ 印证 `ce2b84c3`（每小时同步 origin_main）**在正常工作**。

### 5.1 广扫结论：**35 个 `.py` 属「陈旧暂存区」，非本机改动**

我按 `git diff --name-only origin/main -- '*.py'` 广扫出 43 个差异文件，三类分离：

| 类别 | 数量 | 判据 | 处置 |
|---|---|---|---|
| **真本地改动** | **1** | `WT != IDX==HEAD==REMOTE` 且**全史零命中** | 🔴 `self_heal_monitor.py` 禁 checkout |
| **陈旧暂存区** | **35** | `WT == IDX == HEAD != REMOTE`，且 WT blob **全史命中**（均为远端真实历史版本） | ✅ 纯落后，非本机改动 |
| **纯落后（WT 已对齐）** | 2 | `WT == REMOTE != IDX` | ✅ 无需动作 |
| **本地缺失（远端新增）** | 6 | 文件不存在 | ℹ️ 远端新增，非漂移 |

### 5.2 `self_heal_monitor.py` —— **第 5 次独立互证，结论不变**

- 本地工作树 `74c3e58305`（**613 行 / 28235 B**）↔ 索引=HEAD=远端 `38a3f7ed85`（**482 行 / 21901 B**）
- **`git log --all --find-object=74c3e58305` = 全史零命中**（该 blob 从未入仓）
- ⇒ **`git checkout origin/main -- self_heal_monitor.py` 一执行即静默丢失 +131 行**（外置权威副本双向自愈 + 6h 告警冷却 + `--check-only`）
- **本轮再次未动。** 是否入仓 **待主人拍板**。

### 5.3 `algorithms/run_algorithms.py` —— **纯落后，零独有，但按纪律未动**

- 本地 WT `1f1089c246`（1212 行）↔ 远端 `1448f94ef4`（1301 行）
- 逐行核对本地相对远端的**独有内容**：全部是**已被远端取代的旧口径**
  （`calc_stock_rps.py` 预算/挂链、`V8_BACKTEST_YEARS:"3"`、`gen_lhb_7d.py` 仍启用、`main()` 无 `sys.exit`）
  ⇒ **零独有缺口 = 纯落后**。
- **但**：`run_algorithms.py` 属**双机共改热文件**（你方也频繁改 STAGES/ORDER）⇒ 按纪律**标「待授权」未动**。

---

## 六、附带发现（非阻断，供参考）

### 6.1 🔎 你 §六「`?v` 口径分裂（105 处 4 种）」—— **数字对，但 3 个「离群」中只有 1 个是真问题**

本轮推送前远端刚被 `v8-cloud-bot` 推进一笔（`fd6fe96a4`，11:37，标题「生成器原子提交 ?v → 防覆盖」，
把 102 处 `?v=1789357063` 统一刷成 `?v=1789357078`）。刷后实测分布仍为 **105 处 / 4 种**，但逐处查证：

| 值 | 处数 | 位置 | 是否真问题 |
|---|---|---|---|
| `1789357078` | 102 | 全部 `data/*.js` script 标签 | ✅ 正常 |
| `20260911` | 1 | **index.html L557 — HTML 注释内**（「404 死链冗余注入行已删」） | ❌ **非活引用**，纯文档 |
| `063d312703` | 1 | **index.html L14771 — JS 注释内**（述 `v6_memo.html`） | ❌ **非 `data/*.js` script 标签**，另一机制 |
| `20260910` | 1 | **index.html L558 — `data/maharo_insights.js` script 标签** | ⚠️ **唯一真离群** |

**⇒ 更正定性**：不是「105 处 4 种口径分裂」，而是「**102 处正常 + 2 处注释内非活引用 + 1 处真离群**」。
且生成器的原子提交**范围不含 `maharo_insights.js`**（它不在 102 名单内）⇒ 该值**不会被自动刷**。
建议：把这条从「105/4 分裂」降级为「**1 处 `maharo_insights.js` 未纳入 ?v 重写白名单**」，更易修。

### 6.2 🔎 你 §六「港股名单塌缩（HK=0，起于 `7ee6b71e2`）」—— **该条已过时**

- `7ee6b71e2`（"v8 cn fetch: 2026-09-11 01:06"）**确在 main 血缘上**（我验过）。
- 但现在实况是：`data/STOCK_RPS.js` **已不存在**、`calc_stock_rps.py` 在远端 `run_algorithms.py` 中
  **命中数 = 0**（已被 `15aadbc64`「删除 RPS 相对强度后端/产物/配置链」整体摘除）。
- 港股名单现由 **`raw_data/hk_universe.json`（1699 B，蓝筹名单完整）** + **`data/GOLD_POOL.js`（51 个 `hk_*` 键）** 承载，**未塌缩**。
- ⇒ 「HK=0」是 **RPS 模块被整体删除后的残留观察**，**不是待修 bug**。建议该条**从 §六 清单移除**。

### 6.3 ✅ `v8_weekend_light.py` 覆盖 `WEEKEND_RUN.js` —— **确认属实，仍在册**

实测 `v8_weekend_light.py` L26 `WEEKEND_JS = BASE/"data"/"WEEKEND_RUN.js"`、L63 写 `window.WEEKEND_RUN=…`，
且 `.github/workflows/v8_weekend_light.yml` 存在 ⇒ **覆盖关系成立**。属真实待修项，**转呈拍板**（未擅动）。

### 6.4 ℹ️ 跨机协同项 `_push/verify_final_all.py` —— **仍未入仓**

我上轮已提「`_push/` 在远端全树 0 文件 ⇒ 跨机不可执行」。本轮复测仍为 0 ⇒ 维持该结论。

---

## 七、本轮动作清单（可审计）

| 动作 | 结果 |
|---|---|
| `git fetch origin main` + `ls-remote` 对账 | ✅ `891807d6f` → 推送前远端已至 `fd6fe96a4` |
| 列 `docs/ops/handover/` 全树，筛 T 与未回执档 | ✅ T=`0935`；未回执 2 份（`1000`/`1050`） |
| `git show origin/main:` 逐份读取 | ✅ 2/2 |
| 合成 root 实跑 `v8_stage_gate.py`（3 场景对照） | ✅ 复现 P0 + 更正归因 |
| 独立复算 `resonance_*` 三档样本量 | ✅ 325/192/133 逐键一致 |
| 三方比对 8 个本地 runner 脚本 + 广扫 43 个 `.py` | ✅ 零漂移；1 真本地改动（禁动） |
| **安全微修** | **0 项**（确无漂移） |
| **改算法阈值 / 删数据 / 动热文件** | **0 项**（全部标待授权/待拍板） |
| 推送方式 | worktree 旁路（`E:/qs_workspaces/quant-scanner-v8-wt-receipt-a9`，detached `fd6fe96a4`），仅 add 回执 1 文件 |

---

## 八、下一步（下轮哨兵将盯）

1. **CRDS 快照验证点**（你 `071d9c2d2` 修复的首个可证伪证据）—— 盘后回填，下轮复核 `raw_data/history/crds_*.json` 是否按日累积。
2. **T+1 死锁修法落地后**，按你 §五 的 4 条验收判据逐条回填对照。
3. `maharo_insights.js` 是否纳入 `?v` 重写白名单。

---

**署名**：小九的工程师 ｜ 2026-09-14 11:39 CST ｜ 基线 `891807d6f`（推送时远端 `fd6fe96a4`）
