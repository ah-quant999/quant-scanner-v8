# 交接单 · 小九 · 2026-09-15 08:35（署名：小九的股票专家）

**主题**：艾略特波浪·深度分析卡「拐点标注」自适应避让修复 —— ①起下移不压线 / ⑤高完整可见 / 末端现值红点不再被裁

---

## 一、主人报告的病象（2026-09-15 08:22 截图）

> 「这个观测页的图的"1起"放到圆点下方，"5"后面是什么都被遮住看不到了。」

| 病象 | 实测（真实数据 + Node 沙箱渲染测量） |
|---|---|
| **①起 压在圆点上、且压住真实收盘线** | 标注包围盒 x`[100.6,120.6]` y`[113.6,123.6]`；该 x 区间真实收盘线 y=`107~124` → **重叠**；与圆点仅 2px 间隙 |
| **⑤高 的「高」字整个消失** | 标注包围盒 x`[1112.6,1132.6]` → **右越界 13px**（viewBox 右界 1120）；「高」占 `1122.6~1132.6` = 完整出框，视觉上只剩「⑤」 |
| **末端现值红点被切一半** | 圆心 x=`1120`、r=4 → 实际可见区间 `[1116,1120]`，右半 4px 被裁 |
| ①顶 / ②底 / ③顶 / ④底 | 实测未出框，但已属「侥幸」——无任何避让机制 |

---

## 二、根因（两条，缺一不可）

1. **零边距映射**：`var X = function(k){ return n <= 1 ? 0 : (k / (n - 1)) * VW; };`
   → 数据点被映到 `[0, 1120]`，最左点圆心在 `x=0`、最右点圆心在 `x=VW`。
   圆点 r=3/4、标注半宽 10px 全部**无处安放** → 必然出框。
2. **标注按 `marks.pos` 死板放置、零避让**：
   `var pos = (m.pos === 'bottom') ? 'dy="13"' : ((m.pos === 'right') ? 'dx="7"' : 'dy="-6"');`
   → 只看数据里写死的 `pos`，既不查边界、也不查是否压到真实收盘线。
   ①起 的 `pos="top"` 恰好在它上方就是下跌段收盘线 → 必然压线。
   ⑤高 的 `pos="right"` 恰好在图的最右端 → 必然向右出框。

---

## 三、修法（只改 `index.html` 1 个文件，1 个连续代码块 L6561–6589）

1. **左右留白**：`var PL = 26, PR = 60;` → `X(k) = PL + k/(n-1) * (VW - PL - PR)`
   - 左留白 26 容最左拐点标注；右留白 60 容最右拐点标注 + 末端现值红点。
   - 三条关键位虚线同步改为跨 `PL → VW-PR`（不再从 x=0 起，左侧留给关键位文字，更干净）。
2. **`marks.pos` 降级为「首选方向」**，新增三级避让（命中即换向，依次 top/bottom/right 或其优先序）：
   - ① 上下出框（`bb[1] < 2 || bb[3] > VH-2`）→ 换向；
   - ② 压自身圆点（±3.5/±3 包围盒相交）→ 换向；
   - ③ 压末端现值红点（`_rObst`）→ 换向；
   - ④ 压真实收盘线（`_onLine`：该 x 带内任一收盘价落在标注 y 带 ±1px）→ 换向。
3. **横向统一 `text-anchor="start"` + 夹紧左边界**：`bx = clamp(bx, 2, VW-2-w)`
   → 取消 `middle/start/end` 三套锚点分支，几何可直接测量、不再依赖 `dy/dx` 属性偏移。
4. **末端现值红点**改用 `_rcx = VW - PR`（=1060）→ 完整可见。

---

## 四、审计结果（全部 PASS）

**真实渲染验证**（Node vm 沙箱跑补丁后的 `__renderElliottWave` + 真实 `WAVE_ELLIOTT.js`，逐字测量包围盒）：

| 拐点 | 标注包围盒 x | 标注包围盒 y | 修复前 | 修复后 |
|---|---|---|---|---|
| ①起 | `[118.1,138.1]` | `[132.6,142.6]` | ❌ 压收盘线 | ✅ OK（已在圆点**下方**：标注顶 132.6 > 圆点 cy 128.6） |
| ①顶 | `[350.3,370.3]` | `[48.9,58.9]` | ✅ | ✅ OK |
| ②底 | `[529.3,549.3]` | `[107.3,117.3]` | ✅ | ✅ OK |
| ③顶 | `[922.8,942.8]` | `[12.6,22.6]` | ✅ | ✅ OK |
| ④底 | `[990.8,1010.8]` | `[63.8,73.8]` | ✅ | ✅ OK |
| ⑤高 | `[1053.7,1073.7]` | `[31.0,41.0]` | ❌ 右出框 13px | ✅ OK（右缘 1073.7 ≤ 1120，**「高」字完整**） |

- 专项断言：①起在圆点下方 ✅ · ⑤高完整可见 ✅ · 末端红点 `[1056,1064]` 完整 ✅ · 折线未出框（左留白 26 / 右 60）✅
- 全文件审计：27 个内联 script **0 语法错**；div **1518/1518 平衡**；补丁完整性 10 项全过；**纯 LF**（CRLF=0）
- 未改动：折线本体、卡头说明、6 列指标格、两情景文案、`WAVE_ELLIOTT` 数据契约

---

## 五、base / 推仓证据（防覆盖）

- **首次推送基线**：`1fb0f5c976c9f163f1145613c8d515d5ed5bf08e` → 保险①校验时**发现远端已前进**
  （`index.html` blob `bb9bc21c` ≠ 远端 `afee6e94`）→ **脚本按设计拒绝推送，未丢任何他人提交**。
- **重新对齐后基线（本次实际推送）**：`d4158b6ca7da444c6aa5318840276307c033baf6`（`index.html` = 1129958B）
  - 复核：新旧基线的 6 个差异块**全部落在「观测平台」tab 区**，艾略特卡片代码**未被他人触碰** → 补丁锚点完好、可安全重打。
  - 重打后回归：审计 ✅ / 真实渲染 ✅ / 差异块 5 处**全部落在补丁区 6572–6599 内，他处零改动** ✅
- 推送方式：Git Data API（`POST blobs → POST trees{base_tree} → POST commits → PATCH /git/refs/heads/main {force:false}`）
  → 父提交挂当前 tip，**纯 fast-forward，天然免疫并发覆盖**；任何竞速都会 422 拒绝而非丢数据
- 三保险：① 本地基线 blob == 远端 tip 的 `index.html` blob（不等即中止）；② `base_tree` 挂当前 tip（其余 3900+ 文件原样）；③ `force:false`
- 改动文件：`index.html`、本交接单

---

## 六、给阿狸咪的提醒

1. 本卡（`elliottWaveBody` / `__renderElliottWave`）原作者是阿狸咪的工程师，本次只动**拐点标注的几何放置**，
   **未改**数据契约（`WAVE_ELLIOTT.marks[].pos` 仍保留且仍被读取，只是从「硬指令」降级为「首选方向」）。
2. 若后续新增 `marks`，只需给 `pos` 一个**合适倾向**即可，越界/压线/压点会**自动避让**，无需再手调偏移。
3. `WAVE_ELLIOTT.js` 由小九机 `v8/fetch_wave_elliott.py` 每日 16:00 自动刷新，**marks 为人工浪型资产、脚本不重判**。
4. 本机工作树仍冻结（落后远端），**勿在本机 `git add -A` / `commit` / `push`**。

---

**交接人**：小九的股票专家
**接收人**：阿狸咪的股票专家
**时间**：2026-09-15 08:35 CST
