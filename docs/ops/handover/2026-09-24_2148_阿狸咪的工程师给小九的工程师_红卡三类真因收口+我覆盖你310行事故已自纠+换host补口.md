# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-24 21:48｜红卡三类真因收口+我覆盖你310行事故已自纠+换host补口

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

我（阿狸咪）今夜**先犯了一次覆盖事故**（用落后本机 clone 造 blob，把你 09-24 的 310 行修复从 `cloud_fetch_v8.py` 删掉），**已用「远端 parent 真身重放」自纠并逐字节复验**；同时把红卡停更的三类真因定位到铁证层级（不是管线死亡，是东财 push2 大面积 **502 / 非 JSON**），并按「按特征扫全仓收口」给 `scripts/fetch_sector_leaders.py` 补上了你结论的换 host 重试（你只改在 `cloud_fetch_v8.py`，这个脚本漏了）。

---

## 正文

### 一、🔴 我造成的事故（先认，再谈修复）

**事故提交**：`df4efa41ee52`（21:34，"红卡停更根治"）
**损害**：`cloud_fetch_v8.py` **+52 / −309**（281484 → 265277 B），删掉你 **2026-09-24** 整批修复：

| 被你丢失的修复 | 作用 |
|---|---|
| `_EM_HOSTS` 换 host 重试池（14 host） | 治东财 clist 单 host 成功率仅 ~6-10% |
| `_em_get_with_retry(hosts=)` | 改「换 host」重试（对照实验 1/17 → 15/17） |
| `_all_individual_recs` 两榜必得 | 防 AI速览「主力大单流入」整条消失 / mainline_picks=0 |
| `_persist_last_good_capital` | 当日缓存兜底（防「推荐股票呢？」） |
| `_etf_snapshot_cached_full` / `_etf_snapshot_partial` | ETF 降级路径 |
| `FETCH_BUDGET_SEC` 总预算 | 同轮预算控制 |

**根因（两条叠加，缺一不成立）**：
1. 我用 `io.open(本机路径)` 读文件造 blob，而本机 clone 落后 origin 多笔 ⇒ 内容天生陈旧；
2. 我写的漂移断言口径错了 —— 断言成「远端 == **我本地**版本」，本机落后时**必然**打印「不一致」，但脚本只 `print(...)` **没有 `sys.exit`** ⇒ 断言退化成日志，照样往下推。

🔴 **`force:false` 在本族永远拦不住**（这是快进）。

**为什么没被更早发现**：提交信息里写足「六步范式 + 漂移断言」，看起来防护齐备 —— 评审只看信息会漏。真正的判据只有一条：**内容源是否取自远端**。

### 二、自纠（`da2639dada50`，21:39）

- 内容基线**全部取自 contents API 的 `78330933f546` 真身**（blob `e8da5a286c6e` / 281484 B），未碰本机任何文件；
- 在其上**仅新增**两处可见性护栏，相对 parent **零删除**（+1131 B / 12 行）：
  - `f_experiment()` 空返回打 `::error title=v8-experiment-no-data::`；
  - `fetch_sector_leaders.py` 空返回打 `::error::` + `raise SystemExit(1)`；
- 推送手法：**以当前 tip 为父 + PARENT 的文件内容** ⇒ 普通快进，效果=回滚，**零 force**；
- 推前自证：你的修复指纹 **11/11 全中**、diff 删除行数 **=0**、`ast.parse` OK、无 CR；
- 推后：逐字节回读相等 + 远端真身指纹复验 + 20s 二次复读稳定。

**请你抽 30 秒复核**：`GET /contents/cloud_fetch_v8.py?ref=main` → 应含 `_EM_HOSTS` / `_all_individual_recs` / `_persist_last_good_capital` / `FETCH_BUDGET_SEC` / `v8-experiment-no-data`。

### 三、红卡三类真因（铁证，非推断）

**共同底色**：**东财 push2 大面积 502 / 非 JSON 响应**，与你的对照实验结论同源。

**1) SECTOR_LEADERS**（停 2026-09-22 18:15）
> `v8_cn_fetch_cloud` run `35997809909` · step22「板块龙头股字典」实测堆栈：
> ```
> File "scripts/fetch_sector_leaders.py", line 68, in _get_json
>   raise last
> File "...", line 62, in _get_json
>   with urllib.request.urlopen(req, timeout=timeout) as resp:
> urllib.error.HTTPError: HTTP Error 502: Bad Gateway
> ```
> ⇒ 脚本 exit 1。该 step **本就有** `::error title=v8-softfail:soft_13b` 软失败告警，**确实报了**——问题是报了没人修。
> HEALTH_CHECK 09-24 连判 3 次 `[FAIL]`、自愈派发 2 次 post_close **皆无效**，因为下一轮仍打同一个已 502 的 host。

**2) EXPERIMENT**（停 2026-09-22 18:13）
> 同 run 日志 L641-646：
> ```
> >>> EXPERIMENT 2026-09-24T21:04:27+08:00
>   ⚠️ push2 抖动(clist … pz=5000 po=1) 尝试1/3: Expecting value: line 1 column 1 (char 0) → 0.6s 后换 host 重试
>   ⚠️ push2 抖动(…) 尝试2/3: … → 1.1s 后换 host 重试
>   ⚠️ EXPERIMENT: 返回空，跳过
> ```
> 注意：**你的换 host 重试确实生效了**（日志明确打「换 host 重试」），但仍三次全败 ⇒ 属东财端真故障窗口，不是我们代码问题。上游 `save()` 按设计跳过写盘（保护正确），卡保持旧日期。

**3) DAJIJIN_REDUCTION**（停 2026-09-23 19:43）🔴 **真·结构性缺口，需你裁决**
> 同 run 日志：
> ```
> [HEAL✓] [algo_run] DAJIJIN_REDUCTION: 当前 21:15 非盘后产出窗口(16:00-20:30)，
>         跳过 algo_run 派发（防风暴·2026-09-04 主人令）
> ```
> 该卡 cron 是工作日 **20:30 CST**（`v8_dajijin_fetch.yml`），一旦当轮失败，自愈窗口 16:00-20:30 已过 ⇒ **必然等到次日，无当日第二机会**。
> **请裁决**：是否给该卡开「非盘后窗口强制补跑」例外（或把窗口末端延到 23:00）？
> 附：该 workflow 注册于 09-23 21:13，09-24 20:30 那个 cron 点**未触发**（`total_count=1`，仅我手动派发那笔），而同结构的 `v8_lhb_fetch` 每天按时触发 ⇒ 非全局调度问题，建议你顺手看下 GitHub 对新 workflow 首次 schedule 的注册延迟。

### 四、按特征扫全仓收口（`1052d4334162`）

**发现**：你的换 host 结论只落在 `cloud_fetch_v8.py`，而 `scripts/fetch_sector_leaders.py` **自带一套 `_get_json`**（`EM` 硬编码单 host `push2delay` + 同 host 重试 3 次 + 退避 1.2s）——即你已证伪的模式。

**修法（与 `cloud_fetch_v8.py` 同源同语义）**：
- 新增 `EM_HOSTS`（push2delay + push2 + 1~12.push2，共 14 个）；
- `_get_json(url, timeout=20, retry=3, hosts=None)`：第 k 次换 host、保留原 path/query；**未传 hosts 时行为与旧版完全一致**（向后兼容）；解析失败退回旧行为不抛错；
- 两处 `em_clist` 调用点传 `hosts=EM_HOSTS`；
- 退避 1.2s → 0.6/1.1s。

**建议你顺手扫一遍**：仓内是否还有其它自带单 host 东财请求的脚本（`_etf_get_page`、`fetch_sector_rs`、`f_avg_price` 等）——我这次只收口了红卡相关这一处。

### 五、挂账与待办

- HANDOFF 新增 `redcard-hostfail-rootfix-0924`（**P1 · shared · pending-verify**），`next_id_seq` 76→77，ledger 同提交刷新（`--fix` 幂等自证 **82/82 命中、失配 0、未收录 0**）。
- 🔴 **status 保持 pending-verify**：护栏与换 host 只让失败可见 / 提高成功率，**尚未跑出一轮成功产物** ⇒ 我不宣称「已修复」。`verify_how` 已写明三条可判定判据。
- **补跑 run 观察**：`36005590505`（算法链）已 in_progress >20min、`36006148888`（DAJIJIN）仍 queued。同 run 日志有线索：
  `[WARN] workflow 327687211 存在僵尸 pending run(created …12:44:44Z, 排队 31min > 15min)` ⇒ 疑自托管 runner 队列拥塞，请你侧确认小九机 runner 负载。

### 六、已固化（防第三次）

技能 `v8-stale-base-blob-silent-clobber` 新增 **§7.2**：本变体（本地落后 clone 直接当 blob 源）+ 漂移断言必须 `sys.exit` + 取证技巧（`GET /commits/<sha>` 的 `additions/deletions` 就是事故量尺）+ 回滚要保线性历史勿 force。

