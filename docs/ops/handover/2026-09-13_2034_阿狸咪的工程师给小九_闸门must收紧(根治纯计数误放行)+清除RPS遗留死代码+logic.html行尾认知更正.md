# 交接：闸门 must 收紧（根治「纯计数」误放行）+ 清除 RPS 遗留死代码 + logic.html 行尾认知更正

- **发件**：阿狸咪的工程师（家用机）
- **收件**：小九的工程师（单位机）
- **时间**：2026-09-13 20:34 CST
- **commit**：`e81736156570f804dd887102b322372d3589cd3d`（父 `f6aea4926`）
- **性质**：主人 **2026-09-13 明确拍板「改，清理」** —— 落实待办 P1 + 死代码清理

---

## 一、一句话结论

主人 09-12 令「BACKTEST_ALL_ALGOS 必新」**此前从未生效**（`must=[]` 而已），本轮把它与 B 批核心三项一起做成真正的 `must`；
同时发现 `must` 与去重器之间存在**会让整批永久锁死**的缺口，已用「不变式」一并闭环；
死代码清理完成，零行为变更。

---

## 二、P1：`READY_SPEC` 的 `must` 由 `[]` 收紧

### 2.1 根因（一句话）

`ready = must_ok and hit >= need` 里 **`need` 是纯计数**，表达不了「**哪几项**必新」——
陈旧项恰好是关键产物时，闸门照样判「已就绪」→ 空转 → 该卡整日停更，而**全链零红灯**。

### 2.2 改动（`.github/scripts/v8_stage_gate.py`）

| 批 | 改前 | 改后 | 语义 |
|---|---|---|---|
| **A** | `must=["data/LHB_DATA.js"]` | 不变 | 龙虎榜必新 |
| **B** | `need=8` · `must=[]` | `need=8` · `must=[TRIPLE_CONSENSUS, FOUR_VOLUME, CRDS_CARD_DATA]` | 三张**核心选股卡必新** + 仍容忍 1 项真失败 |
| **D** | `need=1` · `must=[]` | 不变 | 唯一产物 |
| **E** | `need=4` · `must=[]` | `need=4` · `must=[BACKTEST_ALL_ALGOS, CANDIDATE_BACKTEST, GOLD_POOL_BACKTEST]` | 3 must + CRDS/TDX **任一** = need 4，保留原「至少 1 项」语义 |

> E 批的三项都是「跑成功必有产物」的聚合器产物（`gen_backtest_all_algos.py` / `backtest_pools.py`），
> 正常不会把该批锁死成「永不就绪」。

### 2.3 🔴 新铁律（本轮最重要的发现，请务必记住）

> **不变式：`READY_SPEC[*]["must"] ⊆ dedup_fetch_manifest.py::_ALWAYS_PUSH`**

**为什么缺一即锁死**：

1. `must` 项的「新鲜」**只由 `update_time` 前进体现**；
2. 而 `update_time` 前进必须靠**真被推送**；
3. `dedup_fetch_manifest.py` 会丢弃「剥掉时间戳后与远端逐字节相同」的**伪变更**；
4. ⇒ 若某 `must` 项内容天然稳定（**实证风险路径：`FOUR_VOLUME` 长期命中 0 只 ⇒ `stocks` 恒为空**），
   它会被去重器丢弃 → `update_time` 恒旧 → `must` 恒不满足 → **该批每轮判「未就绪」→ 重跑 60~90min → 永久不收敛**。
5. **这比不加 `must` 更危险**（原状态只是不重跑，新状态是无限重跑）。

**故本轮同步改 `.github/scripts/dedup_fetch_manifest.py`**：`_ALWAYS_PUSH` 12 → **19 项**，
把**全部 `must` 项**（含 A 批 `data/LHB_DATA.js`，实测仅 23 KB）纳入，**不留特例**——
特例即隐身风险，且无法机器验证。

> 另：B 批 `need=8` 那段「因去重器丢弃伪变更、故需 8 项容错」的**历史约束已解除**
> （09-13 已补齐 `gold_pool` 等 6 项白名单），现役语义就是「容忍 1 项真失败」。

---

## 三、清理：删除随 RPS 下线失效的死代码

**文件**：`algorithms/auto_run_dn_algorithm.py`（`data/H_AUTO_BUY.js` 唯一生产者）

| 删除对象 | 为何是死代码 |
|---|---|
| `_unified_query_kline()` | 唯一实现者 `calc_stock_rps._query_kline` 随 RPS 删除 ⇒ 函数体已退化为**恒 `return None`** |
| `_SRC_UNIFIED` | 上者的全局延迟导入句柄 |
| `_vols_to_cache()` | **唯一调用点**位于上述待删块内 ⇒ 删除后即成不可达 |

**取数链保留三级**：①缓存(新鲜) → ②缓存(陈旧, degraded) → ③gtimg 老路径 ⇒ **零行为变更**。

> ⚠️ 说明：原设计「网络取到的 K 线回写缓存、逐晚收敛」的**能力随之彻底消失**（缓存现为只读）。
> 自 RPS 下线后该回写**从未实际发生过**（因为取数链恒返 None），故属「保持现状、非功能退化」。
> 若日后要恢复「逐晚收敛」，需连同取数链一起重建 —— **这是待办观察项，本轮未动**。

---

## 四、🔴 认知更正：`logic.html` 是 **CRLF**，`index.html` 是 **LF**

**实测（`e817361565`，逐字节统计）**：

| 文件 | 字节 | CRLF | LF-only | 结论 |
|---|---|---|---|---|
| `index.html` | 1,086,516 | 0 | 15,282 | **纯 LF** |
| `logic.html` | 711,017 | **8,640** | **0** | **纯 CRLF** |
| `v6_memo.html` | 165,312 | 0 | 1,453 | 纯 LF |
| `calendar.html` | 8,634 | 0 | 162 | 纯 LF |

**更正**：此前流传的「**远端双页均纯 LF**」是**错的** —— 两页行尾**不同**。

**风险**：若补丁脚本按「LF」硬编码处理 `logic.html`（归一化后不还原），
会把 8,640 行整文件改写 ⇒ diff 变成「8640 增 / 8640 删」的**整文件重写**，且极易与另一台机撞冲突。

**正解（本轮已按此执行）**：补丁一律用**检测式行尾守恒** ——
读二进制 → `had_crlf = b"\r\n" in raw` → 解码前归一 → 写出时按 `had_crlf` 还原。
本轮实测：`logic.html` 基底 CRLF=8640 → 产物 CRLF=**8640** ✅ 守恒。

---

## 五、推前验收证据（全绿）

| 项 | 结果 |
|---|---|
| `py_compile` | 3/3 ✅ |
| `logic.html` inline JS `new Function` | **0/83 错** ✅ |
| 死代码残留扫描 | **0 处** ✅ |
| 不变式 `must ⊆ items ⊆ _ALWAYS_PUSH` | **6/6** ✅ |
| `check_ready()` 行为场景 | **11/11** ✅ |
| `pre_deploy_audit.py` 五项 | **EXIT 0** ✅ |

**行为实测（P1 核心证据，构造产物时间戳直调 `check_ready`）**：

- E-`BACKTEST_ALL_ALGOS` 陈旧（其余 4 新鲜）→ 改前会判**就绪**，改后 **`ready=False`** ✅ ← 主人 09-12 令这才算真生效
- E-`CRDS_BACKTEST` 陈旧（非 must，TDX 补位）→ `ready=True` ✅ 保留「至少 1 项」语义
- E-`CRDS`+`TDX` 双陈旧 → `ready=False`（hit=3 < need 4）✅
- B-`TRIPLE_CONSENSUS` 陈旧（其余 8 新鲜）→ 改前 `ready=True`，改后 **`ready=False`** ✅
- B-`valuation_percentile` 陈旧（非 must）→ `ready=True` ✅ 容忍名额仍在

**推后核验**：远端 4 文件 SHA 与推送日志逐一对上 ✅；Pages `pages build and deployment` **success** ✅。

---

## 六、给小九的动作项

1. **务必知悉第四节的行尾事实** —— 任何 `logic.html` 补丁都必须用检测式行尾守恒，
   切勿按「LF」处理。（`index.html` 才是 LF。）
2. **新铁律落库**：以后凡往 `READY_SPEC` 加 `must`，**必须同一 commit 往 `_ALWAYS_PUSH` 加同名项**；
   已把该不变式写进两个文件的注释里，并在 `v8_stage_gate.py` 顶部就绪判据说明中留了指针。
3. **`logic.html` 判据表已同步**（B 批「三项必新」、E 批「三项必新」），
   `align_logic_ops.py` 已跑 **EXIT 0**；`index.html` 运维页无对应文案，无需改。
4. **待办观察项**：`_vols_to_cache` 删除后 K 线缓存只读不写，
   若希望恢复「逐晚收敛」，需连同取数链一起重建 —— 请评估是否要做。

---

阿狸咪的工程师 · 2026-09-13 20:34 CST
