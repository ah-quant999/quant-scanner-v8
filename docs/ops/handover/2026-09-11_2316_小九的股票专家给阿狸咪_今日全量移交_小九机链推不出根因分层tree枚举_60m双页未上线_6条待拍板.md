# 交接单 · 今日全量移交（已完成 / 未完成 / 待拍板）—— 请阿狸咪接手

- **发件**：小九的股票专家（单位机 `lemoncat-cn`）
- **收件**：阿狸咪（家里机 `alimi-cn`）
- **时间**：2026-09-11 23:16 CST
- **紧急度**：🔴 **URGENT** —— 含 P0：小九机算法链「**跑完推不出去**」，今夜已连续 3 档失败，盘后成果一个字上不了 main
- **数据基准**：`origin/main` @ **`36c474224b`**（GitHub REST / Actions API 实测，**不看本地工作区**）
- **主人原令**：「把还没做的，没回你的，和已完成，详细交接给家里阿狸咪，她来完成后续。」

---

## 零、一句话

本文 = **全量移交单**。三块：

| 块 | 含义 | 你要做什么 |
|---|---|---|
| **§一 已完成** | 今日已闭环并上线 | 无需动作，只作**对账凭据**（可直接 `git show` 复核） |
| **§二 未完成** | 今夜盘点出的 7 项未结 | **你接手执行** —— 每项都给了现象／证据／根因／修法／验收判据 |
| **§三 待拍板** | 问过主人、**主人还没回的 6 条** | **阻塞中**：未拍板前**不得动口径**（尤其 CRDS） |

> 🔴 **两条纪律先讲清**：
> 1. **§三 的 6 条一律冻结**。主人未答，任何一方都不得自行改评分口径／契约。我知道你会想顺手推，但主人明确说过「未拍板前不得改评分口径」。
> 2. 我**没有**替你做决定，也没替你把 §二 的代码写完 —— 我只把「卡在哪、怎么修、怎么验」摆到你能直接动手的粒度。**独立判断请你自己下**，若你认为我的根因分析有偏差，直接用实测数据推翻我，别顺着我写。

---

## 一、已完成（今日闭环，凭据可核，无需动作）

| # | 项 | 凭据（commit / 实测） |
|---|---|---|
| 1 | **四量终极 60m 三宗罪根治**（产物路径 / 假 0 命中 / 硬告警） | `2b8cc93c2`（父 `a31507de4`）· 净室原始树直写 · **未 force** |
| 2 | 60m 交接单（补全版：上线 SHA + 审计表 + 现场铁证） | `7f471b003` |
| 3 | 机游共振游资覆盖防覆盖 + 每日洞察时间锚 | `176aa911e` |
| 4 | 金股池 Plan B（B 批原生派生 + 闸门硬约束） | `dc2199c85` |
| 5 | ETF 真实行业分类 + AI 速览真实行业 TOP5 | `fdf86a86e` |
| 6 | 审计轨迹时区 / 账本回退 / 重复登记 三缺陷 | `10d961003` + `6717d2ece` |
| 7 | 跨层闸门「盘前 100% 阻断部署」根治（方向+时段双判 + `_today_cst()` UTC bug） | 见 `v8_verify_layer_parity.py` |
| 8 | 死数据清理 6 个 + 死 workflow 1 个 | `9a27eca71` |
| 9 | 派发死 ID 根治（3 脚本重定向到 `327687211`） | `717aca690` |
| 10 | 两个文案口径补漏（四量回测口径） | `1362308e2` |

### 三项已完成的**远端完好度实测**（我刚验的，你可复跑）

```bash
git show origin/main:algorithms/run_algorithms.py   | grep -c 'strategy_four_volume_60m.py": 5400'   # → 1
git show origin/main:algorithms/run_algorithms.py   | grep -c '计败不阻断'                              # → 4
git show origin/main:algorithms/strategy_four_volume_60m.py | grep -c 'ScanUnavailable'                 # → 4
git show origin/main:v8_health_check.py             | grep -c '"id": "FOUR_VOLUME_60M"'                # → 1
```

→ **`88deb6258644` / `9c8a0f33c78a` 你的两笔没有顶掉我的修复**（你在 20:13 URGENT 里点名要我复核的 §9-1，结论：无冲突）。

---

## 二、未完成 · 移交你执行（按优先序，逐项可动手）

### 🔴🔴 P0-1｜小九机链「**跑完推不出去**」——今夜已连续 3 档失败

**现象（Actions API 实测，非二手）**

| run | 时间(CST) | runner | 结果 |
|---|---|---|---|
| `v8_algo_cloud.yml` **#1772** | 21:41 → 22:16 | `lemoncat-cn` | step9 算法链 **success（真跑完）**、step10/11 success → **step12「唯一推送」failure** |
| `v8_algo_cloud.yml` **#1773** | 22:19 → 22:35 | `lemoncat-cn` | step2 checkout success → **step3「回退·API 同步源码」failure** → **step4–14 全 skipped** |
| `v8_algo_cloud.yml` **#1774** | 22:50 → 22:52 | `alimi-cn` | success，但 **step9 算法链 skipped**（闸门空转，合规） |

**#1772 推送步的原始报错**（我拉下来了，`api_push_raw.py` 输出）

```
git/trees/a280d3ae…?recursive=1 → IncompleteRead(983251 bytes read, 20911 more expected)   ×3
⚠️ 远端 base tree 不可用（…）→ 回退本地 git ls-tree 作守卫基线
❌ 获取 base tree 失败（远端 5xx 且本地兜底亦失败），拒绝裸推
```

**根因（我的判断，请用你自己的实测校验）**

`?recursive=1` 要求 GitHub 把**全仓一次性展开成一个响应**。本仓 3913 文件（`raw_data/kline_cache/` 3119 个 + `backup/` 69MB）→ 单响应 ≈ **1MB** → **跨境链路上必然半途截断**（`IncompleteRead`）。

- 你 20:10 的 `88deb6258644` 加了 `SKIP_PREFIX = ("backup/", "docs/", "raw_data/kline_cache/")` + 软预算 —— 方向对，但它只减少了**「要下载哪些 blob」**，**枚举层那一行走的还是 `recursive=1`**（见下方调用点），所以枚举本身仍然会断。
- 「本地兜底也失败」的原因：runner 上 **git 直连 github.com 不可用**（你我在 `v8_algo_run.yml` step2 都写过注释），`git ls-tree` 依赖本地完整对象库，而该机源码是 **API 同步**来的 → 无完整对象库。

**修法：把 tree 枚举改成「分层 / 分页」**

```
# 旧（必断）
GET /repos/{REPO}/git/trees/{sha}?recursive=1        # ← 1MB 单响应

# 新（每层几十 KB）
GET /repos/{REPO}/git/trees/{sha}                    # 顶层，只列一级条目
  → 对关心的子树再 GET /git/trees/{subtree_sha}      # 逐层展开
```

- **推送器**（`api_push_raw.py`）只需 `raw_data/` 子树 + 8 个 `data/` 白名单文件 → **展开 `raw_data/` 一棵即可**，响应从 ~1MB 降到几十 KB。
- **同步步**（workflow 内联 Python）需全仓（排除 `SKIP_PREFIX`）→ 先拿顶层，再对每个一级目录（排除 `backup/` `docs/` `raw_data/kline_cache/`）逐层展开。
- 建议同时保留 `truncated` 判定 + 现有本地兜底作为**二道防线**（但不要期望它顶用）。

**🔴 `recursive=1` 全仓调用点清单（9 处，一次改齐，别只改一处）**

| # | 文件 | 行 | 用途 |
|---|---|---|---|
| 1 | `api_push_raw.py` | **352** | ⭐ 链尾推送器（#1772 现场） |
| 2 | `.github/workflows/v8_algo_cloud.yml` | **261** | ⭐ #1773 step3 现场 / 冷启动同步 |
| 3 | `.github/workflows/v8_algo_run.yml` | 212 | cn 链同步 |
| 4 | `.github/workflows/v8_cn_fetch_cloud_selfhosted.yml` | 180 | cn 抓取链 |
| 5 | `.github/workflows/v8_stock_quote_refresh.yml` | 147 | 行情刷新 |
| 6 | `.github/workflows/v8_stock_quote_refresh_cloud.yml` | 141 | 行情刷新(云) |
| 7 | `scripts/reconcile_cache_busters.py` | 53 | 缓存戳对账 |
| 8 | `scripts/_fix_v_now.py` | 34 | 临时修 |
| 9 | `scripts/_verify_live_coherence.py` | 27 | 线上一致性验证 |

> 注：`backup/reset_2026-09-09_1530/v8_cn_fetch_cloud_selfhosted.yml:176` 是备份副本，不必改。

**验收判据**
1. 下一次 `lemoncat-cn` 跑盘后链，**step12 推送 success**；
2. 推送后 3–5 min 复查 main 上 `data/*.js` 的 `update_time` **前进**且文件大小**不缩水**（防「推送成功但内容被洗回旧版」）；
3. `raw_data/algo_run_report.json` 的 `update_time` 与链尾一致，`failed_scripts` 不含推送步。

---

### 🔴 P0-2｜`v8_algo_cloud.yml` step3「回退同步步」同根因（一并修）

#1773 的 step3 失败 = 上表 **#2**（L261）。它一挂，**step4–14 全 skipped**，整链除闸门判据外全部空转。

修法同上（分层枚举）。**验收**：下次 `lemoncat-cn` 轮到该步时 **success**，日志出现 `files to sync: N (skipped heavy/unused: M)`。

---

### 🔴 P1-1｜60m 双页写入**未上线**（稿件全文在下，可直接贴）

**现状实测**：远端 `index.html` / `logic.html` 搜「四量终极 60m」= **0 命中** —— 我写的两段**只在本地工作树**，而本地工作树**落后远端 65 提交**，直接提交会把 65 个提交的改动洗回旧版。

**目标位置**
- `index.html`：**运维页顶部**，「文件新鲜感」卡（`id="opsCandidateFreshBody"`）之后、`<!-- 🩺 数据健康总览` 之前（本地 L6499–6504）
- `logic.html`：「🔥 四量终极 - 说明」卡闭合之后（本地 L1678–1696）

**① `index.html` 待插入段（原样贴）**

```html
    <!-- 2026-09-11 一劳永逸：四量终极 60m 产物三宗罪修复（与 logic.html 双向对齐） -->
    <div style="font-size:12px;color:var(--dim);margin:0 0 14px;padding:8px 12px;background:var(--bg2);border:1px solid var(--border);border-radius:8px;line-height:1.7">📌 <b style="color:var(--gold)">2026-09-11 修复 · 四量终极 60m（🔔 60m 共振 · 最终推荐加分因子）</b><br>
      &nbsp;① <b style="color:#93c5fd">产物写错目录</b>：输出目录原为硬编码旧仓库绝对路径 → 日志打「✅ 写出 …」却写在<b>本仓库之外</b>，站点收不到新数据。<b>现恒为本仓库 <code>data/</code></b>（<code>V8_DATA_DIR</code> 可覆盖），并在日志开头回声 <code>DATA_DIR=…</code> 便于核对。<br>
      &nbsp;② <b style="color:#93c5fd">假 0 命中</b>：活跃股池为空（通达信失败 + 东方财富限流）时仍写出 <code>total:0</code> 新鲜产物，把「没扫成」伪装成「今天没信号」。<b>现扫描未执行 → 保留上次真实数据 + 退出码 1</b>；只有真 0 命中才写。<br>
      &nbsp;③ <b style="color:#93c5fd">硬告警</b>：本卡移出低频白名单、纳入健康检查正式卡按 <b>24h 红线</b>亮红灯；最终推荐门控由软告警升为「<b>计败不阻断</b>」；超时预算登记 <b>90 分钟</b>。<br>
      &nbsp;详见 逻辑详解页「🛠 四量终极 60m · 产物三宗罪与硬告警」。</div>
```

**② `logic.html` 待插入卡（原样贴）**

```html
      <div class="card">
        <h3><span class="ic">🛠</span>四量终极 60m · 产物三宗罪与硬告警（2026-09-11 一劳永逸）</h3>
        <div class="logic-h">背景</div>
        <div class="logic-p">「60m 共振」是最终推荐的多周期<b>加分因子</b>（卡片上的 🔔 紫色徽章）。2026-09-08 之后它不再产出新信号，且<b>连续多日零告警</b>。逐层审计出三个独立缺陷，已一次性根除。</div>
        <div class="logic-h">三宗罪</div>
        <table class="lg-table">
          <tr><th>#</th><th>缺陷</th><th>后果</th><th>根因</th></tr>
          <tr><td>①</td><td><b>产物写错目录</b></td><td>日志显示「✅ 写出 …」却写在<b>本仓库之外</b>的旧仓库目录 → 站点文件收不到新数据，而链级只看退出码与日志文本，等于「写成功却没产出」</td><td>输出目录被解析成<b>硬编码绝对路径</b>（v6 遗留的 <code>E:/workspace/stock-scanner/data</code>）；哪台 cn 机器装了那个旧仓库，哪台就中招（双机 active-active → 时好时坏）</td></tr>
          <tr><td>②</td><td><b>「没扫成」被写成「今天没信号」</b></td><td>活跃股池为空（通达信直连失败 + 东方财富限流冷却）时，仍写出 <code>total:0</code>＋<b>新鲜时间戳</b>的产物 → 前端显示「今日无 60m 共振」，实为数据源故障（<b>假数据</b>）</td><td>2026-09-03 为治「静默冻结」改成「无论命中多少只都写新鲜时间戳」，但未区分「<b>真 0 命中</b>」与「<b>根本没扫成</b>」</td></tr>
          <tr><td>③</td><td><b>三层静默</b></td><td>连续多日异常，<b>零告警</b>、无人察觉</td><td>健康检查把它放进<b>低频白名单</b>（24h 红线降为「>7 天才告警」）× 链尾闸门标「可选」× 最终推荐门控只是<b>软告警</b>（仅打印一行，不记账）</td></tr>
        </table>
        <div class="logic-h">现行口径（已生效）</div>
        <div class="logic-p">
        · <b>输出目录</b>：恒为<b>本仓库 <code>data/</code></b>（<code>V8_DATA_DIR</code> 可显式覆盖），与同族日线版完全一致；已删除硬编码旧路径分支，并在日志开头回声 <code>DATA_DIR=…</code>，任何机器都能一眼核对产物去向。<br>
        · <b>扫描未执行 → 硬失败</b>：活跃股池为空、或 60 分钟数据源登录失败时，<b>保留上次真实产物、绝不写假 0</b>，并以退出码 1 上报 → 进算法链失败账本。<br>
        · <b>真 0 命中 → 照写</b>：扫描确实跑完、当日无票，仍写出带新鲜时间戳的 <code>total:0</code>（2026-09-03 口径不变），属有效业务结论。<br>
        · <b>硬告警</b>：四量 60m 纳入健康检查<b>正式卡</b>，按 <b>24 小时红线</b>判定（已移出低频白名单）；最终推荐门控由「软告警」升为「<b>计败不阻断</b>」—— 仍不阻断最终推荐（保留「60m 非今日 → 回退日线版」降级），但陈旧必计入失败账本，运维面板与告警可见。<br>
        · <b>超时预算</b>：登记 <b>90 分钟</b>（原走默认 30 分钟，冷缓存逐只取 120 根 60 分钟 K 线时易被监督器误杀）。</div>
      </div>
```

> ⚠️ **双页上的红涨绿跌铁律**：我这两段**没引入任何涨跌配色**（只有 `--gold` / `--dim` / `#93c5fd` 中性信息色），不会与全站红涨绿跌冲突。

**验收**：`grep -c "四量终极 60m"` 在远端 `index.html` ≥1、`grep -c "三宗罪"` 在远端 `logic.html` ≥1；页面 Ctrl+F5 后运维页顶部可见该 note。

---

### 🔴 P1-2｜60m 修复**尚未被实际执行过**（验收点顺延）

我 22:35 上线 → 之后唯一带修复的 run **#1774 闸门空转**（step9 skipped）→ 算法根本没跑。
**验收点 = 下一次真正跑 B 批的 run**。届时看两处：
1. 日志有 `[60m] DATA_DIR=…` 回声，且路径是 runner 工作区（**不再出现 `E:/workspace/stock-scanner`**）；
2. 若股池为空 → 应出现 `🛑 四量终极60m 扫描未能执行… → 保留上次真实产物…本脚本以退出码 1 上报失败`，且 `data/FOUR_VOLUME_60M.js` 的 `update_time` **不变**（不再被假 0 覆盖）。

> **当前线上仍是假 0**：`data/FOUR_VOLUME_60M.js` = `update_time 2026-09-11 21:33:06` / `total: 0` / `stocks: []` —— 这是**修复上线前**的产物，属预期。

---

### ⚠️ P1-3｜本地工作树「陈旧暂存区」+ 落后 65 提交

```
M  algorithms/run_algorithms.py           ← staged，且内容 = origin/main 版
M  algorithms/strategy_four_volume_60m.py ← staged
M  v8_health_check.py                     ← staged
 M index.html / logic.html / self_heal_monitor.py / raw_data/runner_status.json  ← 未 staged
```

本地 HEAD `02e2eb78a`，`origin/main` 引用 `900997e7f`（旧），**远端实际 tip `36c474224b`**，`rev-list --left-right --count HEAD...origin/main` = **0 ahead / 65 behind**（纯落后，可直接 ff）。

**处理**（小九机侧，若你要我这边做请单独说）：
```bash
git fetch origin main
git merge --ff-only origin/main      # 被 algorithms/__pycache__/*.pyc 挡住 → rm -f 该 pyc 再 ff
git reset                            # 清 index（三文件内容已与 origin/main 一致，无内容损失）
```
> 🔴 **注意**：index.html / logic.html 是**另一个并发会话**在改（它把「文件新鲜感」卡从选股策略页迁到了运维页顶部）。**动这两个文件前先确认那个会话已收工**，否则会互相覆盖。

---

### ⚠️ P1-4｜仓库根目录未跟踪垃圾（需主人定「删 / 归档」）

11 个 root 级 md：`AUDIT_小九_2026-09-10_1900_每日19点审计.md`、`AUDIT_小九_2026-09-11_1900_每日19点审计.md`、`HANDOVER_2026-09-10_午休修复.md`、`HANDOVER_小九_2026-09-10_1500-1800空窗期.md`、`HANDOVER_小九_2026-09-10_双机分工.md`、`HANDOVER_小九给阿狸咪_2026-09-10_主力净额双管齐下.md`、`HANDOVER_小九给阿狸咪_2026-09-10_盘前卡上线.md`、`HANDOVER_小九给阿狸咪_2026-09-11_日监控卡迁移算法侧对齐.md`、`INTRADAY_0940_STATUS_20260910.md`、`INTRADAY_ERRATA_20260911.md`、`PRE_OPEN_AUDIT_20260910.md`

另：`backup/reset_2026-09-09_1530/`、`preview_premarket.html`、`机构研究日报.md`、`机构研究_关注词.txt`、`docs/ops/handover/.bak_2026-09-11_2100_本地未提交版.md`

→ **建议**：md 类归档到 `docs/ops/archive/2026-09/`，`backup/reset_*` 与 `*.bak_*` 直接删。**但删文件前请先问主人**（我这边只盘点，没动）。

---

### ⚠️ P2-1｜你在 20:13 URGENT 里**点名要我做的、我未完成的 4 项**（现原样退回给你）

| 你要求 | 我的状态 | 交给你的原因 |
|---|---|---|
| §7-4 运维页定时任务表 `freq` 对齐（现写 16:40/18:10/19:15/20:00；实际 cron = 16:40/18:10/20:00/21:00 + 接力 22:30/00:30） | ❌ 未做 | 与 §三-5 的「B 档派发模式」耦合，等主人拍板后一次改齐 |
| §7-2 `INDEX_HISTORY` 假新鲜（内容 `meta.update_time=09-08T16:54`、`klines` 末条 09-07，却被 republish 刷成 09-10 21:39）→ 提醒：**republish 只准写 `republish_time`** | ❌ 未改 | 同上，属「数据口径」类，主人未拍板前我不动 |
| §7-3 `?v` 口径分裂 | ✅ **已实测**：104 处 `?v=` → 99 处 unix(10位) + 2 处日期戳（`maharo_macro.js?v=20260911` / `maharo_insights.js?v=20260910`）+ **`index.html` L9953 给用户看的错误文案裸露模板占位符**（`data/DO_NOT_DELETE.js?v=<sha> 未就绪`） | 修复未做，**L9953 那条建议优先**（用户可见） |
| §7-5 盘中高频空转 A1~A4（门禁上移 / 派发侧统一门禁 / `cancel-in-progress` / 仅产物变更才触发构建） | ❌ 未做 | 涉及 workflow 面较广，你在家里机推更稳（我这边 git 直连被墙，只能走净室/API） |

> 补一句独立判断：`?v` 那 2 处日期戳**不一定是错的** —— 它们指向的两个文件由 `maharo` 生成器写，生成器可能确实用日期戳。**改前先确认生成器口径**，别为了「统一」把好的改坏。L9953 那条**确定是 bug**（用户可见的 `<sha>` 模板占位符）。

---

### ⚠️ P2-2｜遗留观察点：日线版 11 只 vs 60m 版 0 只

同晚、**共用同一 `fetch_volume_top_stocks()`**、两脚本运行仅差 **11 秒**，但日线版产出 11 只、60m 版股池 **0 只**。

**两种解释（我未定论，倾向①）**：
- ① **两脚本跑在不同机器**（cn 自托管 vs 云端 ubuntu）→ 东财对海外 IP 限流更狠（run#1763 日志显示两脚本产物路径前缀不同，支持此说）；
- ② 同机但落在东财**限流冷却窗口**两侧（日志有「连续失败3次, 进入冷却 300s」，冷却 5 min 完全覆盖 11 秒间隔）。

**复验时点**：P0-1 / P0-2 修完后。若仍「每轮 0 只而日线正常」→ 根因在**源可达性**，需给 60m 单独加重试或换同源（届时再议）。

---

## 三、🔴 待主人拍板（**阻塞中，未拍板前不得动口径**）

| # | 问题 | 背景与实测 | 我的建议 |
|---|---|---|---|
| 1 | **CRDS 大盘口径三选一** | 你已明确「**未拍板前不得改评分口径**」。同一张卡并存两套：卡片文案用 `get_market_context()`（综合口径等权）；条件②「大跌日逆势板」用 `detect_market_down()`（**上证指数**，阈值 -1.5%）。今晨同批候选实测：上证 -0.97% → **精选 0 只**；综合口径 → **5 只** | **A 综合口径等权**（你推荐的那个） |
| 2 | **B 批 `need=8/9` 契约 + 是否加防死锁** | 18:05 实测 7/9 未达标（**诚实红灯，非 bug**）。风险：若长期凑不满 → 闸门恒判 B → **D 批（最终推荐）被饿死** | 配「**上游落后 >2 交易日强制放行**」防死锁 |
| 3 | **哨兵类 3 个自动化是否也限交易安静窗** | `ad288000`（断档告警）/ `automation-1786961773708`（自愈监控）/ `automation-1786696074524`（runner 守护）—— 盘中健康基础设施 | **保留**（暂停 → 盘中更新失明） |
| 4 | **盘前速览卡升级要改什么** | 主人已选「文字描述」，但**还没给具体改点**（我这边读不了图，系统过滤） | 等主人文字标注 |
| 5 | **B 档 `workflow_dispatch` 压制** | B 档连续三轮（09-04 / 09-07 / 09-11）都被 `workflow_dispatch` 压制，**从未以 `repository_dispatch` 触发**；你 20:29 已补 06:00/08:00 唤醒点，但**模式未定** | 改为真派发（与云端派发源同源） |
| 6 | **Task 清单对账** | 现 140 条：**93 completed / 13 in_progress / 34 pending**；其中约 **25 条实际已闭环**（`gen_algo_track` 死代码已删、logic.html 已清、机游共振已推 `176aa911e`…）却没标结 → **清单失真** | 一次性对账收口（**不删任务，只标结**） |

> 🔴 **主人尚未回答任何一条**。我已在 23:20 的盘点单（`2026-09-11_2320_小九_今日未完成清单与待拍板.md`，**仅本地**）里问过，未获回复。
> 你接手后：**只有第 3 条（哨兵保留）可自行按既有口径执行**，其余 5 条**必须等主人一句话**。

---

## 四、双机注意点（今夜新增，血泪）

1. **`E:/workspace/stock-scanner` 是幽灵目录**（旧 v6 仓、无 `.git`）。它曾让 60m 产物被写进仓库外。**家里机若也有这个目录，请确认它不再被任何脚本当输出目标**（我已删掉那条硬编码分支，但别的脚本可能还有）。
2. **`V8_DATA_DIR` 优先级**：`strategy_four_volume_60m.py` 现在 `V8_DATA_DIR` > 仓库 `data/`。而 `v8_algo_run.yml` L138 把 `V6_DATA_DIR` 设成了 `E:\workspace\quant-scanner-v8\data` —— **两个变量名不一致**，别以为它们等价。
3. **并发会话会吃掉工作树里未提交的改动**（我今夜亲历：`run_algorithms.py` 的门控代码体被静默冲掉，只剩 docstring，`git diff` 才发现）。**改完必须立刻提交**，或直接在隔离环境（净室/worktree）提交。
4. **沙箱会拦 `~/.ssh`** → 本机所有 SSH git 操作必失败（`fetch`/`push` 全断）。需**前台 + 沙箱放行**执行；**后台任务不继承放行**。
5. **PowerShell 5.1 按 ANSI 解码 UTF-8** → 用它搜中文关键字会**假阴性**（我差点误判「改动丢了」）。中文判据一律用 Python 按 UTF-8 读。
6. **`git worktree add` 传 `/e/...` 会被解析成 `E:/e/...`**，且大仓 checkout 易超时留下残缺 worktree；被外部 `prune` 后**已提交的 commit 对象不会丢**（可从主仓 `cat-file` 取回）。
7. **净室原始树直推**（我今夜两次成功的方法）：以当时远端 tip 为父、`git update-index` 逐条替换目标路径后 `write-tree` + `commit-tree` + 推 ref；**自带两道防清仓自检**（① 零替换重建树叶子集必须与原树**完全相等**；② 替换后非目标叶子 sha **逐一不变**）。你若要复用可直接找我拿脚本。

---

## 五、验收判据总表（你做完后请逐条打勾）

| 项 | 判据 | 通过 |
|---|---|---|
| P0-1 推送根因 | 下一次 `lemoncat-cn` 盘后链 **step12 success**；推送后 3–5 min `data/*.js` `update_time` 前进且大小不缩水 | ☐ |
| P0-2 同步步 | 该步 success，日志出现 `files to sync: N (skipped heavy/unused: M)` | ☐ |
| P1-1 双页 | 远端 `index.html` 含「四量终极 60m」≥1、`logic.html` 含「三宗罪」≥1；Ctrl+F5 可见 | ☐ |
| P1-2 60m 修复生效 | 日志出现 `[60m] DATA_DIR=` 且**非** `E:/workspace/...`；股池空时 `update_time` **不变** | ☐ |
| P1-3 工作树 | `rev-list --left-right --count HEAD...origin/main` = `0 0`；`git status` 无 staged 残留 | ☐ |
| P1-4 垃圾 | 根目录未跟踪文件数 = 0（归档或删除，需主人拍板） | ☐ |
| P2-1 你派的 4 项 | 逐项闭环（`?v` L9953 优先） | ☐ |
| P2-2 观察点 | P0 修完后至少一轮 B 批：60m 是否仍 0 只 | ☐ |

---

## 六、附录：今夜 Actions 实况（Actions API 原样，供你对账）

```
=== v8_algo_cloud.yml 最近 8 条 ===
#1774  success    create=2026-09-11T14:50:09Z  upd=14:52:43Z  ev=workflow_dispatch   runner=alimi-cn     step9 SKIPPED
#1773  failure    create=2026-09-11T14:19:03Z  upd=14:35:23Z  ev=workflow_dispatch   runner=lemoncat-cn  step3 FAILURE
#1772  failure    create=2026-09-11T13:41:30Z  upd=14:16:47Z  ev=workflow_dispatch   runner=lemoncat-cn  step12 FAILURE
#1771  failure    create=2026-09-11T12:55:26Z  upd=12:55:26Z  ev=push
#1770  success    create=2026-09-11T12:22:12Z  upd=13:46:45Z  ev=workflow_dispatch
#1769  failure    create=2026-09-11T11:50:25Z  upd=13:27:48Z  ev=workflow_dispatch
#1768  cancelled  create=2026-09-11T11:47:29Z  upd=11:50:27Z  ev=workflow_dispatch
#1767  failure    create=2026-09-11T11:33:11Z  upd=11:55:37Z  ev=workflow_dispatch

=== v8_algo_run.yml 最近 8 条 ===
#42  failure  create=2026-09-08T14:02:19Z  runner=lemoncat-cn   ← 该 workflow 自 09-08 后未再运行
#41  success  create=2026-09-07T18:48:22Z
#40  success  create=2026-09-07T15:15:28Z
```

**远端产物现状（`origin/main`）**
```
data/FOUR_VOLUME_60M.js : update_time "2026-09-11 21:33:06"  total 0  stocks []   ← 修复上线前的假 0
raw_data/algo_run_report.json : update_time "2026-09-11 21:39:31"  run_start "2026-09-11 21:30:05"
                                ok 1  fail 0  failed_scripts []  skipped_by_time_gate []
```
> ⚠️ `ok=1` 而全链 `ORDER` 47 条 → 该轮**只跑了尾部极少数脚本**（闸门空转特征）。判据：`ok + fail ≪ 47` 即该轮未真跑整链。

---

## 七、我没做的（明确声明，免得你以为漏了）

- 我**没有**改 `api_push_raw.py` 的分层枚举（虽然根因清楚了）—— 留给接手方实现，避免与你并发改同一文件；
- 我**没有**提交 `index.html` / `logic.html`（并发会话在改，且工作树落后 65 提交）；
- 我**没有**动 §三 的任何一条口径；
- 我**没有**删根目录任何文件（只盘点）。

---

_小九的股票专家 · 2026-09-11 23:16 CST · 单位机 `lemoncat-cn`_
_数据基准 `origin/main` @ `36c474224b`；本单为只读盘点 + 移交，未改业务代码、未推业务提交（仅推本单本身）。_
