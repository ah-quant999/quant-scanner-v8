# 交接 · 小九 → 阿狸咪：2026-09-17 全天改动「改后三件套」总表

> **时间**：2026-09-17 14:30 CST（盘中·只推文档不碰本地工作树）
> **发起**：小九（单位机 lemoncat-cn）
> **收件**：阿狸咪（家机 alimi-cn）
> **基线上线 tip**：`e3a2a1382d5cc013f271247b156aa84d764d8258`（撰写时刻）
> **主人口令**：「好了后和今天上午改动修复的，都改后三件套，和阿狸咪交接清楚。」

---

## 零、一句话总纲

今天全天 **15 笔实质代码改动**，**全部已上线 main 并在最新 tip 上逐字节核验通过**；
「改后三件套」= ①云端/双机对齐 ②index↔logic 口径对齐 ③交接文档，
本文即为 **③**（①②的实测证据见下表）。**唯一未上线的是交接文档本身，本文推送即闭环。**

---

## 一、🔴 行尾铁律（今天踩过，务必照此）

| 文件 | 行尾 | 今日实测 |
|---|---|---|
| `index.html` | **LF** | CRLF 0 / bareLF 17743 ✅ |
| `logic.html` | **CRLF** | CRLF 8474 / bareLF **0** ✅ |
| `.github/scripts/v8_stage_gate.py` | **CRLF** | CRLF 833 / bareLF **0** ✅ |
| `.github/workflows/v8_algo_cloud.yml` | **CRLF** | CRLF 905 / bareLF **0** ✅ |
| `v8_cloud_watchdog.py` | **LF** | CRLF 0 ✅ |
| `data/*.js`、`scripts/*.py`、`algorithms/*.py` | **LF** | CRLF 0 ✅ |
| `docs/ops/handover/*.md` | **LF** | CRLF 0 ✅ |

**教训**：今天写锚点时一度把 `index.html`/`logic.html` 都按 CRLF 写 ⇒
`index.html` 命中、`logic.html` 未命中。**锚点必须按各文件真实字节写**，
且脚本要「命中数≠1 就 raise」拒绝盲改（今天的脚本正是这么设计的，未造成损坏）。

**另**：Windows 上 `io.open(..., "w", encoding="utf-8")` 会把 `\n` 写成 `\r\n`
⇒ **`data/*.js` 与一切 LF 文件必须二进制写**（`open(..., "wb")`）。

---

## 二、全天改动总表（15 笔，均已上线）

### A. 前端 / 页面（index.html ↔ logic.html 双页同步）

| # | commit | 时刻 | 改动 | 三件套核验 |
|---|---|---|---|---|
| 1 | `293b6c868a` | 10:37 | 🔴P0 修 `_fmtDateCST` 日期格式不匹配 → 分时卡 09:20 后永久清空 | 双页同改 ✅ |
| 2 | `0d53832523b4` | 06:32 | 宏观硬指标区改「1 行 3 列小胶囊」并下移 | 双页同改 ✅ |
| 3 | `b22d51b8b7ad` | 10:00 | AI速览 ETF 资金条「总额前置 + 流出另起一行」；分时卡 09:20 后不再显示上一交易日 | ✅ |
| 4 | `72cc034858cf` | 10:05 | logic 分时卡盘前清空口径与 index **严格对齐**（09:30→09:20） | ✅ |

**🔴 P0 #1 详情（最值得记住的一条）**
- 现象：主人截图「主力净额分时累计曲线」恒显示「📭 等待今日盘中数据」。
- **我曾误判为「数据端没刷新」——错**。实测数据端完全正常（`date=2026-09-17`、5 快照、`update_time=10:38:58`）。
- **真因**：守卫用 `_fmtDateCST(new Date())` 取「今日」，该函数内部走
  `toLocaleDateString('zh-CN', {timeZone:'Asia/Shanghai'})` ⇒ 返回 **「2026/09/17」斜杠格式**，
  而数据端 `d.date` 是 **「2026-09-17」横杠格式** ⇒ 全等比较**永不成立**。
- **佐证**：全站 `_fmtDateCST` 共 15 处调用，**只有这一处拿它做字符串比较**，其余全是展示 ⇒ 纯新引入 bug。
- **铁律**：`toLocaleDateString('zh-CN')` 返回斜杠格式，**绝不能与 ISO 横杠格式做全等比较**。
- **判据**：「数据正常但卡片空」⇒ **先查前端判据，别急着改数据端**。

### B. 算法链 / 数据层

| # | commit | 时刻 | 改动 |
|---|---|---|---|
| 5 | `9dbee7f52262` | 06:37 | 🔴 D 批门控新鲜度基线改用「数据日 16:30」——**根治最终推荐整夜不出** |
| 6 | `253b70bcc2aa` | 10:23 | 接入美联储议息数据链路 + 根治非法 JSON（裸 NaN）落盘（`cloud_fetch_v8.py`） |
| 7 | `66c2d8bb95b5` | 10:06 | AI速览 ETF 资金条「总额」改报**真·全市场合计** |
| 8 | `fcba708dba54` | 10:50 | AI速览 ETF 品种小块字号调小一档（**总额/标签字号不变**） |
| 9 | `7278bea5fba1` | 09:07 | gtimg 501 视为确定性失败不再重试 + 进程内拉黑失效域名（**根因④**） |
| 10 | `6e5984e422af` | 08:49 | 星级委员会算法重构（基准超额维度 + 样本量纯门槛 + n<100 封顶） |

**🔴 #5 详情（今天最大的根治）**
- 因果链：门控以 `run_start=datetime.now()` 判输入新鲜度 → D 批输入必然由 A/B 批**更早**产出
  ⇒ 判「陈旧」→ 重跑 A/B 生成器（`generate_top10.py` 要拉全市场 K 线，+25min）→ 复检可能仍陈旧
  ⇒ **跳过 `final_recommend` 不产出**，但 `continue-on-error` ⇒ **job 退出码 0 ⇒ 绿色假成功**
  ⇒ 线上 `FINAL_RECOMMEND_DATA.js` 整夜停在昨日。
- 修法：新增 `_freshness_baseline(stage)`；**全链**基线=`datetime.now()`（原口径不变，最严），
  **分批**（A/B/D/E）基线=**数据日 16:30**（与 `v8_stage_gate.py` 的 `FLOOR_TRADING=(16,30)` 同一常量）。
  昨日产物仍判 STALE（不放过陈旧），但不再要求「后批输入比后批自身还新」这个物理上不可能的事。
- 效果：**07:18:13 `final_recommend.json` 落地**，整夜不出数据问题闭环。

### C. 闸门 / CI（`.github/`）

| # | commit | 时刻 | 改动 |
|---|---|---|---|
| 11 | `5d2db36df9b6` | 09:00 | **E 批与 D 批解耦**（PREREQ: E←D 改为 E←B；SEQ_REF: E 基准改 `TRIPLE_CONSENSUS.js`） |
| 12 | `ccca0f7db6ae` | 11:17 | 闸门拒绝加 `v8-stage-gate-reject` 可区分标记，看板不再满屏红灯 |

**🔴 #12 的后续（今天新发现的重要事实）**
workflow 里其实**同时存在两套 title**：`v8-stage-gate-blocked`（旧）与
`v8-stage-gate-reject`（新，`ccca0f7db6ae` 加入）。**且脚本源码会被日志回显** ——
⇒ **grep 日志字符串判分流必然误判**。正确判据见 §四。

### D. 观测类新功能（主升板块 → 龙头股）

| # | commit | 时刻 | 改动 |
|---|---|---|---|
| 13 | `9cfd099baf84` | 13:01 | 前端：主升板块 → 龙头股（实时）卡片 |
| 14 | `4ce3379ef0ae` | 13:12 | A 批接入 `scripts/fetch_sector_leaders.py` |
| 15 | `075c365abec2` | 13:48 | 🔴 看门狗盘后算法链红灯分流（详见 §四） |

---

## 三、🔴 看门狗分流改造（今天最核心的技术改动，今天新上线）

### 3.1 为什么必须做

`v8_algo_cloud` 的 **「批次闸门正常拒绝」与「真故障」都以 `failure` 落库**
⇒ 看板一片红灯 ⇒ **真 P0 被埋掉**（主人最反感的正是这个）。

### 3.2 落地方式（关键：**不改 workflow 退出码**）

改 workflow 退出码会**动 CI 语义**（有风险）。改为**在看门狗侧做结论分流**：

`v8_cloud_watchdog.py` 新增 `classify_algo_run(run_id, run_conclusion=None)` → `(kind, why)`，
6 种 kind：

| kind | 触发条件 | 告警? |
|---|---|---|
| `ok` | 闸门步 success **且** 问责步 success | 否 |
| `gate_reject` | 闸门步 failure **且** 问责步 failure | **否**（合规拒绝） |
| `no_job` | `jobs` 列表为空（僵尸 run，被 concurrency 抢占） | 否 |
| `cancelled` | `run.conclusion != "failure"` 且非 ok | 否 |
| `real_failure` | `run.conclusion == "failure"` 且结论不完整/问责未过 | **是** |
| `unknown` | 取不到 step 结论（API 错误） | 是（宁可误报不漏报） |

### 3.3 🔴 四条边界（全部实测踩出，**缺一即误报，勿删**）

1. **非 `failure` 结论的 run 永不判 `real_failure`**
   `run#1896` 实证：`con=cancelled`，step8=success，step9=cancelled，**step17=failure**
   ⇒ 只看 step 会把「被取消」误判成真故障。
   **写法**：必须把 run 自身 conclusion 传进 `classify_algo_run()`。
2. **GitHub 对被跳过的步有时返回 `conclusion=None`（不是 `"skipped"`）**
   `run#1882` 实证：`con=failure`，step8=success，step9/14/17 全 `None`
   ⇒ 若把 `None` 当「通过」，**真 failure 会被吞成 ok**。
   **写法**：只把显式 `"success"` 当通过。
3. **空 job 列表 = 僵尸 run**
   `run#1889/1890/1892` 实证：`event=schedule` + `con=cancelled` + `jobs` 长度 0
   ⇒ 判 `unknown` 会保守误报，故单列 `no_job`。
4. **`kind==ok` 但 `con==cancelled`**
   `run#1888/1891/1895/1897` 实证：闸门与问责均 success，run 被外部取消
   ⇒ `check_algo_chain` 需**早返回 `ok=True`**。

### 3.4 权威判据：按 **step 结论**，不 grep 日志

```
GET /actions/runs/{id}/jobs  →  steps[].conclusion
闸门步名含：「批次闸门」或「盘中静默闸门」
问责步名含：「结果问责」
```
**⚠️ 两条「必须提醒」的对齐点**：
- **「盘中静默闸门」也要算进闸门步**（它 `exit 0` 属 success，但必须纳入判断集合）。
- **step 中 `::error title=` 有两套 slug**，且脚本源码会被日志回显 ⇒ **不可 grep**。

### 3.5 回归基线（阿狸咪改看门狗后必须复跑）

```
40 条真实 run：cancelled=4  gate_reject=3  no_job=4  ok=28  real_failure=1  → ALL PASS
改造前该窗口报 4 次误告警；改造后 0 误报 + 1 真故障正确捕获。
```
线上实跑（今天 14:03）：`check_algo_chain → ok=True is_failure=False`，
detail=`algo 链 completed/success @ 09-17 13:56 (age 8m)`。

> **给阿狸咪的话**：这段逻辑**不要简化**。四条边界每条都对应一个真实误判案例，
> 删掉任何一条都会重新开始误报/漏报。

---

## 四、主升板块 → 龙头股（新功能，今天上线）

### 4.1 交付物三件套

| 文件 | 字节 | md5 | 行尾 |
|---|---|---|---|
| `scripts/fetch_sector_leaders.py` | 10247 | `5bc6655f656e...` | LF ✅ |
| `data/SECTOR_LEADERS.js` | 867 | `6fbb4f972bd6...` | LF ✅ |
| `index.html`（渲染 + 注入） | 1257616 | — | LF ✅ |
| `algorithms/run_algorithms.py`（A 批接入） | 90160 | `377974dcf282...` | LF ✅ |

### 4.2 线上实测数据（Pages 硬抓，**真实**）

`update_time=2026-09-17 12:55:52`，`data_date=2026-09-16`，`phase=主升`，`sector_count=1`，`top_n=5`

**板块：元件（`BK0459`）｜5日 +10.21%｜20日 +10.52%｜成分 67 只**

| # | 名称 | 代码 | 价格 | 涨幅 |
|---|---|---|---|---|
| 1 | 澳弘电子 | 605058 | 48.76 | **+9.99%** |
| 2 | 中京电子 | 002579 | 20.36 | +3.61% |
| 3 | 海星股份 | 603115 | 94.65 | +3.26% |
| 4 | 世运电路 | 603920 | 44.03 | +2.95% |
| 5 | 崇达技术 | 002815 | 23.94 | +2.40% |

排序 = 板块内涨幅榜降序 ✅。字段为 `code/name/price/chg/main_net`（**非 `pct`**）。

### 4.3 设计与口径（主人明确指定，勿改）

- 只显示 **「主升」档**；每板块 **5 只**；排序 = **板块内涨幅榜**。
- 板块归属按 **同花顺行业分类**（`SECTOR_RS`），个股涨幅/价格为 **东方财富**口径。
- 落点：**只放「暂未上架」页**（`index.html` 的 `sectorLeadersBox`）。
- 更新时机：**盘后跑一次**。
- 展示：**板块行内并列**。
- **东财 `BK0459 元件` 内含 `BK1340 印制电路板`（48/48 全含）⇒ 禁模糊匹配**。

### 4.4 环境注意

`push2.eastmoney.com` 本机被拒 ⇒ **只有 `push2delay.eastmoney.com` 可用**。

---

## 五、改后三件套之②：index ↔ logic 口径一致性（实测）

| 关键词 | index | logic | 判定 |
|---|---|---|---|
| `_fmtDateCST` | 14 | 7 | ✅ |
| `09:20` | 7 | 4 | ✅ |
| `main_net` | 2 | 5 | ✅ |
| `强势跟踪` | 33 | 3 | ✅ |
| `暂未上架` | — | 38 | ✅ |
| `龙头` | — | 50 | ✅ |
| `SECTOR_LEADERS` | 7 | 0 | ⚠️ **非缺陷** |
| `主升板块` | 6 | 0 | ⚠️ **非缺陷** |

**「非缺陷」的判定依据**：`logic.html` 是**说明页**（散文叙述），
用「暂未上架」「龙头」等**业务词**描述，不出现**代码标识符**（`SECTOR_LEADERS`）与
**卡片短名**（`主升板块`）—— 这是**设计如此**，不是遗漏。

> **给阿狸咪的话**：以后做 index↔logic 对齐核验时，
> **不要把「code 标识符在 logic 页命中 0」当成缺陷**；应改用业务词做交叉验证。

---

## 六、遗留待办（**交给阿狸咪 + 主人**）

### 🔴 高优先（时间窗相关）

| # | 事项 | 时机 | 备注 |
|---|---|---|---|
| 1 | **清理陈旧暂存区 + 本地基线对齐线上 tip** | **15:00 收盘后** | 本地 HEAD 停在 09-16 19:44；陈旧暂存区 177 文件 = **106 行 CI 噪声**（105 条 `?v=` + 1 行 `BUILD`），**无逻辑增删**。`git reset` 即可，**禁 `git commit`** |
| 2 | 用**线上版** index.html 复核「看板层无需同步分流」结论 | 15:00 后 | 本轮基于 09-16 本地版 |
| 3 | 核 `MACRO_DATA.js` 裸 NaN 消失 + `fed_decision` 出现 | **17:20 盘后档后** | 现在线上仍有 **NaN×2 / `fed_decision`×0**（实测 14:25） |

### 📋 一般待办

| # | 事项 | 备注 |
|---|---|---|
| 4 | `gen_backtest_all_algos.py` 重跑 | 四量污染值聚合快照仍停 09-16 03:32:07；**真值源已刷新（09-17 07:18）**。**勿双写** |
| 5 | 「强势跟踪」并入 `BACKTEST_ALL_ALGOS` | — |
| 6 | P-2 + P-6 落盘（`v8_build_deploy.yml`） | — |
| 7 | P-4 / P-5 影响面评估（76 条桥接） | — |
| 8 | `SH_FIB` 显示名仍为「市场温度计」 | 待改 |
| 9 | `a_impact` 缺口：议息条目 `risk_factors=1` 仍走「中性」档 | 待修 |
| 10 | `SCRIPT_ENV` 按脚本名匹配（与注释不符） | **阿狸咪档 §5 提出，需主人确认** |
| 11 | `strategy_four_volume.py` 性能（~9.5s/只） | 及 `_gate_hardwait_four_volume` 的 `subprocess.run` **补显式 timeout** |
| 12 | `v8_build_deploy.yml` 并发声明复核 | 实测曾出现 2 个同时 `in_progress`（预期「1 跑 + 1 排队」） |
| 13 | `factor_walkforward.py` 归宿决定 | 接进主链 还是 保持手动？（目前**发动机从未点火**，16 因子全 `pending`） |
| 14 | `#1897` 的 `repository_dispatch` **派发源未查明** | — |
| 15 | **「沧海一土狗」文章** | 主人已定：**先放着，等 v8 收口再说**。全仓关键字匹配 0 文件，干净 |

---

## 七、给阿狸咪的三条特别提醒

### 7.1 🔴 判「文件是否真有改动」**不能用** `git status` / `git diff`

今天双重实证：
- `verify_data_sanity.py`：裸字节 md5 与 HEAD **完全相同**（16500 B 全同），
  但 `git hash-object` 给 `848de577`、`HEAD:` 给 `bde12379` ⇒ `git diff` 报 `+331/-331` **纯假阳性**。
- `index.html`：工作树 md5 = HEAD md5 = 远端 md5（**三方一致**），
  但 `git status` 报 `MM`、`diff` 报 `316/166`。

⇒ **只能用裸字节 md5 三方比对**。

### 7.2 🔴 API 隔离推送**不走本地工作树**（设计如此）

⇒ 推送后**本地 `git status` 看起来「落后一天」是正常的**，**不是覆盖事故**。
判据：`ls-remote origin main` 对表 + 我推的 commit 是否为最新 tip 的**祖先**。

### 7.3 🔴 写仓时间窗

只允许：盘前部署完（~08:58）→ 09:15 硬停、午休 11:30→13:00、15:00 后。
**写操作不跨 09:15 / 13:00 进盘中。**
⚠️ 今天 14:30 仍在盘中 ⇒ **本地写操作一律推迟**（本文档走 API 推送，不碰工作树，故合规）。

---

## 八、文件状态（撰写时刻，基线 `e3a2a1382d5c`）

| 文件 | 字节 | md5 | CRLF |
|---|---|---|---|
| `index.html` | 1257616 | — （>1MB 免 md5） | 0 ✅ |
| `logic.html` | 723335 | `15cd61326618...` | 8474 ✅ |
| `v8_cloud_watchdog.py` | 67575 | `4c488a8e0a8f...` | 0 ✅ |
| `algorithms/run_algorithms.py` | 90160 | `377974dcf282...` | 0 ✅ |
| `algorithms/gen_market_brief.py` | 41445 | `2475a8a002f1...` | 0 ✅ |
| `algorithms/data_source_gtimg.py` | 14306 | `9ef094575626...` | 0 ✅ |
| `algorithms/gen_market_bench.py` | 7032 | `776476ce955c...` | 0 ✅ |
| `.github/scripts/v8_stage_gate.py` | 54876 | `1b753db13a3e...` | 833 ✅ |
| `.github/workflows/v8_algo_cloud.yml` | 65326 | `8eaa56063142...` | 905 ✅ |
| `scripts/fetch_sector_leaders.py` | 10247 | `5bc6655f656e...` | 0 ✅ |
| `data/SECTOR_LEADERS.js` | 867 | `6fbb4f972bd6...` | 0 ✅ |
| `data/MACRO_DATA.js` | 2866 | `9797dcfc188c...` | 0 ✅ |

**15 笔 commit 已逐一核验仍在 main 历史中**（含 `9cfd099baf84` / `075c365abec2` /
`293b6c868ad6` / `9dbee7f52262` / `5d2db36df9b6` / `6e5984e422af` 等，全部 OK）。

---

## 九、本文档自身

- 推送方式：**隔离单文件 API 推送**（blob → tree(base_tree) → commit → PATCH ref，**`force:false`**）
- 行尾：**LF**（CRLF 0）
- 推送后请以 `git show <sha>:<path>` 或 GitHub API 复核字节数与 md5

---

*小九 · 2026-09-17 14:30 CST · 盘中只推文档不碰工作树*
