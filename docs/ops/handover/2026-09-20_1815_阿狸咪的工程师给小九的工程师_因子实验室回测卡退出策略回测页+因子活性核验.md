# 交接件 · 因子实验室回测卡退出策略回测页 + 因子活性核验（2026-09-20 18:15）

**发送**：阿狸咪的工程师（家机 alimi-cn）
**接收**：小九的工程师（单位机 lemoncat-cn）
**主人令**（原话）：「这个策略回测里的，它不是选股策略，不需要在回测更不需要展示。因子实验室只要在最终推荐页那边跟踪就好，但一定要确认它确认的加减分因子是活的跟着变动的且有效的。」

---

## 一、已完成：策略回测页「🧪 因子实验室回测」卡整体移除（index.html 14 处改动）

| # | 位置 | 改动 |
|---|---|---|
| E1 | 最终推荐页因子实验室定位卡文案 | 「与策略回测页的区别」改为墓志铭说明 |
| E2-E4 | 回测汇总 section 头注释 / sec-head tag / script 块头注释 | 去「因子实验室」，补主人令墓志铭 |
| E5 | `_renderFactorLabBacktest` 整段（12208 chars） | **整卡删除**（函数 + window 挂载） |
| E6 | 精简注释 | 去「因子实验室」 |
| E7 | `_inScope` 判据 | **追加按卡名排除**（数据层自标 trade/选股策略无法派生区分）⇒ 主对比表/最优组合榜/一句话结论均不再含因子实验室行 |
| E8-E9 | ②口径注释 + 「β 依赖」警示块（_betaWarn） | 撤销/删除（行已排除，警示无挂靠对象） |
| E10 | 一句话结论「读数提醒」 | 去「因子实验室见下方回测卡」 |
| E11 | `_PAGE` 清单 | 5 → 4（计数与名单同源收口） |
| E12-E13 | 渲染顺序注释 + 调用行 | 删 `__renderFactorLabBacktest()` 调用 |
| E14 | 最终推荐页注释「权威去处」 | 改指本页 🧪 因子实验室子页 |

## 二、刻意保留（勿删！）

1. **数据链不动**：`algorithms/factor_lab_backtest.py` 继续每日产出 `raw_data/factor_lab_backtest.json` → `data/FACTOR_LAB_BACKTEST.js`。**唯一消费方 = 最终推荐页「🧮 每日因子计算」卡的「✅ 回测已达标」徽章**（verdict_3star PASS→绿，产物投影铁律）。
2. 最终推荐页 🧪 因子实验室子页（fa tab）、「🧮 每日因子计算」、「🏆 当日因子计分榜」全部不动。
3. `data/FACTOR_LAB_BACKTEST.js` 的 `<script src>` 注入保留；运维页新鲜度行（freshTimeFactorLab）保留。
4. 35 条 [10/10] 锚点清单**零改动**（本轮推送后门禁复验 35/35 在位）。

## 三、推送与门禁（原始结论）

- **推送**：Git Data API base_tree 单路径 + `force:false`；窗口内二次核对 tip。commit **`caa68a55adbf5a400bdd0892636e9fa0da35cb85`**（parent `4c297c401e`）。
  推后回读 blob **1366981 B 逐字节一致 True**（sha256 34882701766d4831）。
  ⚠️ 本轮期间 index.html 高频改写：27 分钟内 tip 3c460effc → fee75e32a → 3af34db13 → 4c297c401e，第一版推送脚本被 blob 守卫**正确拦截一次**（`01fa0ae30d`→`db576a9814`）后改一站式流水线重打成功。**小九侧勿用本地旧树直推 index.html**（防覆盖事故同款）。
- **门禁**：`pre_deploy_audit` **10/10 全绿**（含 [10/10] 35/35 锚点、[2/8] 28 inline script 0 错）；`align_logic_ops` EXIT 0（MUST=21）；`v8_verify_layer_parity` EXIT 0。
- **语法**：28 个 inline script 块 new Function **0 错**；`<div>` 1557/1557、`<span>` 1349/1349 差 0；CRLF=0。
- 三件套①：本轮**未改任何 workflow** ⇒ 时窗矩阵无需调整（cron 权威值仍为 21）。

## 四、因子活性核验（主人令第二半，证据实测）

| 项 | 证据 | 结论 |
|---|---|---|
| 最终推荐评分产物 | `TOP10_DAILY.js` update_time **2026-09-20 15:46:36**（republish 16:54:24） | ✅ 当日跑批产出 |
| 因子扫描产物 | `FACTOR_LAB.js` update_time **2026-09-20 15:45:32** | ✅ 当日 |
| 回测徽章真源 | `FACTOR_LAB_BACKTEST.js` update_time 2026-09-19 18:20:27（T+1 数据日 09-19，`chain_day` 闸门口径） | ✅ 正常节奏 |
| 加减分真的在动 | top20 抽查：rank1 新易盛 score_quality+38/breakout+5；rank5 协创数据 **p4=−4**（残差动量+8.0%）；rank2 松发股份 research=+2（量能突破×2.0）；rank11 源杰科技 **p4=−6**；rank9 上海实业环境 backtest=−3、base=−5 | ✅ 有加有减、逐票逐因子明细随当日数据变动 |
| 计分引擎 | `generate_top10.py`：SIGNAL_SCORE + P3 研究因子 + P4 门控 + P5 walk-forward 达标因子，读 raw_data 当日数据重算 | ✅ 活链 |

**结论**：加减分因子 = 活的、每日跟着跑批链变动、且通过徽章/明细在最终推荐页可追溯。策略回测页今后只回测/展示选股策略（四量终极/三重共识/强势跟踪/逆势龙头）。

## 五、小九侧注意

1. 本轮 `_inScope` 按**卡名**排除因子实验室（数据层无法派生区分），已在代码注释引用主人令原话；若将来数据层给因子卡加 kind='research' 之类字段，可替换为派生判据。
2. 你侧若正在改 `v8_runner_guard.py`（方案丙扩 BYDAY 全周），与本次改动无冲突。
3. CI `v8_build_deploy` 由本次 push 触发，gate 10/10 应绿；若见红先看 step 级再定性（铁律 §52）。
