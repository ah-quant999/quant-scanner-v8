# 交接：阿狸咪 → 小九 · 强势跟踪卡「四点改造」改后三件套核验 + 一次覆盖事故复盘

> 采集时刻：**2026-09-20 11:35（北京时间）**
> 本件所有结论均标注取证方式；凡「线上是否生效」一律走 **Git API `git/trees` + `git/blobs`**（判据163），
> 不引 `raw.githubusercontent.com`（有 CDN 滞后）。

---

## 一、我这轮做了什么（主人令：「全部改好后，改后三件套，和小九交接」）

主人 2026-09-20 上午对**强势跟踪卡**提了四点改造，我已全部上线：

| # | 主人原话 | 实现 |
|---|---|---|
| 1 | 「这里重复了，上面那个显示前10删除，只要下方的」 | 删主卡「强势前 10 表」+ 删「📄 全部标的」按钮，卡内只留**一张**强势全表 |
| 2 | 「一样只显示强势的，其他折叠」 | 正常/回落/见顶/走弱四档收进**卡内自管折叠** |
| 3 | 「强势要按照这种方式排序，最强最推荐的放最上面」 | 新增买点分档排序：连涨强势 → 回调买点 → 观察期中 → 未标注 → 跌破基准（同档内涨幅降序） |
| 4 | 「只直接折叠展开，不要这种拉条」 | 折叠只切 `display`；`_tableHtml` 容器补 `overflow-y:hidden` |

**我的推送**：commit `37490620d`（`feat(强势跟踪卡): 去重复+买点分档排序+无拉条折叠（主人令 2026-09-20）`）

---

## 二、🔴 覆盖事故复盘（我已中招，责任在我）

### 2.1 事实链（截至 2026-09-20 11:30 实测，`git/commits` API）

| 时刻 (UTC) | commit | 内容 |
|---|---|---|
| 02:43:50 | `742370d85` | **你**：P2+P5+画像三改（`+76 / −18`） |
| 02:53:32 | `37490620d` | **我**：强势跟踪卡四点改造（`+239 / −273`），**父 = `7dbade6d1`** |
| 02:56:14 | `65efabe52` | CI build（噪声） |

**`37490620d` 的父链是 `7dbade6d1`，不含 `742370d85`** ⇒ 我推送时的工作树基线**早于你**，
把你 02:43 的 A/B/C 三改**整块顶掉**。

### 2.2 根因（这是铁律级教训，我已写入本机记忆）

> **`PATCH /git/refs/heads/main { force:false }` 拦不住「基线落后型覆盖」。**
> `force:false` 只拒绝**非快进**；而「我的基线落后于你」在 git 看来是**合法快进** ⇒ 静默覆盖他人。

### 2.3 我的失误点

我在推前做了「漂移守卫」（归一化 `?v=` 与 `BUILD` 后逐字节比），**确实检出了基线实质漂移**，
但我的处置是「以远端最新为基**重建补丁**」——**重建时用的是我拉取那一刻的 tip**，
而在我打补丁的**几十秒窗口内你又推了一次**。我**没有在 `PATCH ref` 前重新取一次 ref 并断言基底未变**。

### 2.4 你的恢复（已核实有效）

你以**当前线上真身**为新基线，用**精确锚点替换**重放 A+B+C，**双方改动均保留**：
- 恢复 commit：`d69fcec4`（`fix(恢复被覆盖的改动+修正): P2因子卡动态取值 + P5停用卡补原因 + 画像卡降格`）
- 你同时新增 `29a9b662`：`[10/10] index.html 核心标记守卫 + 真源清单`

### 2.5 我今后照做的三条（已固化为本机铁律）

1. **推前重取 ref → 取 `index.html` blob → 以它为基底打补丁**（不用本地工作树当基线）；
2. **`PATCH ref` 前再取一次 ref，断言「基底 blob == 线上当前 blob」**，不等则中止重取；
3. **推后立即回读 blob 逐字节自证**（不是只比 sha —— 缓存戳每轮都变）。

---

## 三、改后三件套核验结果（全部实测，非推断）

### 3.1 ① 时窗矩阵

**云端**（`git/trees?recursive=1` @ tip `292086715`）：workflow **30** 个，带 schedule **21** 个，cron **50** 条。
`align_logic_ops.py` 自报「实际有 cron 的 workflow：21 个；名单 MUST=21 / ALLOW=3」⇒ **两处口径吻合**。

关键时窗（北京时间 CST+8）：

| workflow | 北京时间窗 | 任务 |
|---|---|---|
| `v8_cn_fetch_cloud.yml` | 工作日 08:25 / 09-11、13-14 每20分 / 17-19:20 / 周末09:00 | 🇨🇳 中国数据抓取(云端) |
| `v8_cn_fetch_intraday_lemoncat.yml` | 工作日 09:40,10:00/20/40,11:00/20/40,13:00/20/40,14:00/20/40,15:00 | 🇨🇳 盘中准点档调度器(小九主力) |
| `v8_algo_cloud.yml` | 16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 / 06:00 / 08:00 | ☁️ 盘后算法链(云端) |
| `v8_cache_buster_reconcile.yml` | **每 15 分钟（全时段）** | 🛡️ 缓存戳实时对齐(防覆盖) |
| `v8_intraday_snapshot.yml` | 工作日 09-15 每10分 | 📈 板块资金日内快照 |
| `v8_health_patrol.yml` | 每日 00-23 :08/:38 | 🩺 云端健康巡检 |
| `v8_ima_strong_stock.yml` | 工作日 15:45 | 📡 ima 强势股自动同步 |

**本机（阿狸咪）** ACTIVE 自动化 **26** 个，与云端**无同刻冲突**：
- 我唯一派发源「盘后算法链接力推进器」= 每小时 `:25/:55`（闸门 16:00-08:59）⇒ 与大云端 16:40/18:10/20:00/21:00/22:30 **均错开 ≥10 分钟**
- 我的 `:30` 档（盘中巡检 / 自愈监控 / 心跳监控）与云端 `:08/:38` 巡检**错开**
- ⚠️ **一处需你确认**：我的「ima 强势跟踪全文快照」= **每日 17:00**；云端 `v8_ima_strong_stock.yml` = **工作日 15:45**。
  我 17:00 生成的是 `raw_data/ima_strong_stock_full.md` **快照**，你 15:45 跑脚本时读的是**当天更早的快照** ⇒
  你拿到的是**我前一晚/当天的旧快照**。这个先后顺序是否符合你的双通道设计预期，**请你确认**（见 §五 待确认项）。

### 3.2 ② 两页对齐 + 三项门禁

以 **最新远端 tip `292086715`** 逐 blob 重建离线快照（344 文件，**不下 tarball** —— 判据171）。
`index.html` 直接取远端最新（**我的四点改造已在内**），然后跑：

```
✅ [1/8] py_compile: 189 个 .py 文件 0 错误
✅ [2/8] new Function: 28 个 inline script 块 0 错误
✅ [3/8] data 完整性: data/*.js 数量=109（上界 130）, 全部 > 100B
✅ [4/8] align_logic_ops: EXIT 0（逻辑详解页与真 workflow 对齐）
✅ [5/8] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
✅ [6/8] HTML 数据引用: 179 个本地 script 引用全部存在（4 页）
✅ [7/8] gate 头注一致: 闸门头注与 READY_SPEC 真源一致
✅ [8/8] 心跳产物名一致: 权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移
✅ [9/9] 回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
🎉 9 项全部通过 → deploy 可继续
```

- `align_logic_ops.py` 单独跑：**EXIT 0** ✅
  （`index.html#sec-op` 624494 字符 / `logic.html#sec-lg` 527044 字符）
- `v8_verify_layer_parity.py` 单独跑：**EXIT 0** ✅
  （`data/*.js` 与 `raw_data/*.json` 时间戳一致，无陈旧静默上线风险）

### 3.3 ③ 本件（和小九交接）

---

## 四、线上语义完整性核验（不只看特征串）

我**没有**满足于「特征串命中」—— 你恢复时是精确锚点重放，**只要有一个锚点被吃错，内容就会静默残缺**。
故我按语义逐条验 `index.html`（blob `0b338477b1adb6ac67264b10f842831a08752d02`，1376606 B / 19278 行 / CRLF=0）：

| 判据 | 实测 |
|---|---|
| `_tierOf` 定义唯一 | ✅ 1 |
| `_sortStrong` 定义唯一 + 引用 2 处 | ✅ |
| 强势组按分档排序语句存在 | ✅ |
| `_tableHtml(_showRows)` 已删 / `_showRows` 归零 | ✅ 0 / 0 |
| `'📄 ` 可执行串归零 | ✅ 0 |
| `__imaRenderMain` 定义 1 / 调用 1 / `__imaFold` 定义 1 / `__imaGroupHtml` 定义 1 | ✅ |
| **执行顺序铁律**（声明 L7185 < 定义 L7189 < 折叠定义 L7222 < 调用 L7236） | ✅ 严格递增 |
| `_tableHtml` 容器 `overflow-x:auto;overflow-y:hidden;"><table` | ✅ 1 |
| 你的 A/B/C 三改仍在（`BACKTEST_EXPECTANCY` 2 / `_SIG_EDGE_SRC` 5 / 「已否决」2） | ✅ 未被误伤 |

**真浏览器终验**（Chromium，本地 `http.server` + 完整 `data/`，tip 同源）：

| 判据 | 实测 |
|---|---|
| `__imaRenderMain` / `__imaFold` / `__imaGroupHtml` 运行时存在 | ✅ 全 true |
| 强势全表行数 | ✅ **34 行** |
| 表头 | ✅ `代码 名称 行业 首入选 基准价 最新价 涨跌幅 回撤 连涨 状态 买点提示 v8 命中` |
| **分档排序单调** | ✅ `tierNonDecreasing = true`，实际序列 `0,0,0,2×19,-1×9,3,3,3` |
| 折叠三态 `none → block → none` | ✅ 正常(20行) / 回落(2行) 全对 |
| 折叠展开真实高度、无截断 | ✅ 正常 `h=589 / sh=589 / ch=589`；回落 `h=85`；行高 28px |
| **零纵向拉条**（host 子树 + life 子页全扫） | ✅ `scrollers = []`、`ovY_auto_count = 0` |
| 卡壳自身 | ✅ `overflow-y: visible`、`max-height: none`、`noClip = true` |
| 展开全部后仍无拉条 | ✅ host `1794px`、card `1925px`，均 `noClip` |
| 旧元素 `📄` / `强势前 10` | ✅ 全 false |

> 测量口径说明（判据169）：`#sec-st` / `#st-life` 祖先隐藏时 `offsetHeight` **恒为 0**，
> 不可据此判「没渲染」。本件所有高度均在**真正打开主区**（`switchSec('st')` + `switchSt('life')`）后测得。

---

## 五、待你确认 / 待主人拍板（不阻塞）

1. **【需你确认】ima 快照先后顺序**：我本机 17:00 生成 `raw_data/ima_strong_stock_full.md`；
   你云端 15:45 跑 —— 你读到的可能是**前一交易日的快照**。若期望你读当天最新，需把云端档位挪到我 17:00 之后，或把我提前。
2. **【P1·你已提】walk-forward 门槛口径抵触**：文档写「≥4/5 年」，代码 `pass_yr = n_ok/n_y >= 0.8`（8 年 ⇒ 需 7 年）。
   我同意你「当前偏严」，但**放宽前须先验是否会放进噪声**（我的立场：按铁律「不达标不许美化」，**宁严勿宽**）。
3. **【待主人拍板】强势回测是否复用四量口径**：主人曾选「做，按四量口径」，
   但实测 `IMA_STRONG_BACKTEST` **早已存在**（143 信号 / 30 日胜率 82.1%），
   且与四量/三重**共用同一渲染器** `_renderPeriodBacktestCard` ⇒ 建议**不重做**，等主人确认。
4. **【待主人拍板】全站另 10 处 `overflow-x:auto`**：本次只压了强势卡那一处；其余
   （四量/三重/生命周期等，L5579/5643/5699/5736/6032/6559/7445/7537/8592/17170 附近）**同类拉条风险尚在**，
   是否一并压掉待令。

---

## 六、可复跑复核命令

```bash
# A) 取线上 tip 与 index.html blob（判据163：只认 Git API）
GET https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main
GET https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/trees/<tip>?recursive=1

# B) 离线门禁（不下 tarball，逐 blob 拉门禁所需 344 文件）
python .github/scripts/pre_deploy_audit.py     # 期望 9 项全过
python align_logic_ops.py                       # 期望 EXIT 0
python v8_verify_layer_parity.py                # 期望 EXIT 0

# C) 真浏览器终验（须先补齐 data/ 并真正打开 #sec-st 与 #st-life）
#    注意：__renderImaStrongStock 被调用后 __imaRenderMain 才挂到 window
```

---

## 七、署名

阿狸咪的工程师 · 2026-09-20 11:35（北京时间）
