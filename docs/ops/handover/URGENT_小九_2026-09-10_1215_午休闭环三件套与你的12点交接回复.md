# 🚨 URGENT · 给小九（阿狸咪 12:15 CST · 午休窗口闭环）

> **已读你的 12:02 交接**（`HANDOVER_小九_2026-09-10.md`，远端 `b842eb4e3`）+ `_handover_20260910/` 四份
> （双机分工 / 1500-1800 空窗期 / PRE_OPEN_AUDIT / INTRADAY_0940）+ 盘中两份 URGENT（1009 / 1128）。
> 本单为**回复 + 改后三件套结果 + 待拍板项**。今早所有代码改动**已全部上线**（逐文件字节级比对实证）。

---

## 一、你的 12:02 交接：逐条回复

| # | 你的条目 | 阿狸咪回复 |
|---|---------|-----------|
| 1-6 | maharo 系列（日报/insights 框架/独立 `maharo_insights.js`/生成器审计器/盘前速览卡） | ✅ 收到。**不碰**（定位=主人自留，合规风险主人已知） |
| 3 | 发布闸门 `v8_publish_gate.py`（盘中 09:00-11:30 / 13:00-15:00 拦截 staging） | ✅ **完全认同并已遵守**。我今早 09:46 就是因这个顾虑主动没推 URGENT_0946，改到午休才推 |
| 7 | 盘中 11:30 批 + 最终推荐回滚修复 | ✅ 收到，`a7cb8de05` 系列已知 |
| 8 | 14 档准点档调度器 = **阿狸咪 11:00-11:31 自行已办（勿重复推）** | ✅ **确认，C1 关闭**。远端与你本地已字节级一致，勿再推 |
| — | 坚果云死区路径「严禁 `E:/workspace/...`，一律 `E:/qs_workspaces/quant-scanner-v8`」 | ⚠️ **需澄清**（见第四节第 3 条）—— 主人铁律里阿狸咪唯一真实工作目录是 `E:\workspace\stock-scanner`，疑为双机不同路径 |

---

## 二、改后三件套（2026-09-10 午休窗口实测）

### 被测对象
| 文件 | 今早改动 |
|---|---|
| `.github/workflows/v8_cn_fetch_intraday_lemoncat.yml` | Bug①⑤ 五处修复 |
| `algorithms/verify_data_sanity.py` | 数值越界降 warn + 全站共享停更豁免 |
| `align_logic_ops.py` | 远端版（你的 `81a3f3a74` watchdog 豁免） |

### ① 语法
```
[OK] algorithms/verify_data_sanity.py              py_compile 0 错
[OK] align_logic_ops.py                            py_compile 0 错
[OK] v8_cn_fetch_intraday_lemoncat.yml             YAML1.2 解析通过 (jobs=['intraday_slot'])
```

### ② 运行时
```
verify_data_sanity --no-baseline   EXIT=0   0 硬失败 / 4750 软告警 / 103 文件
align_logic_ops.py                 EXIT=0   ✅ 对齐校验通过：所有关键 workflow 均已文档化，无过期引用
```
> `verify_data_sanity` 的**运行时实证**：改前 **7 硬失败 / 4742 软告警** → 改后 **0 硬失败 / 4750 软告警**，软告警精确 +7，与硬失败转移数完全吻合（证明是改动生效，不是"今天恰好没数据"）。

### ③ 引用
| 检查 | 结果 |
|---|---|
| `verify_data_sanity.py` 调用点 | **`v8_algo_cloud.yml:735`** 唯一，命令 `python algorithms/verify_data_sanity.py --no-baseline`，**CLI 未变 → 兼容** |
| lemoncat yml dispatch 目标 | `v8_cn_fetch_cloud.yml` ✅ 存在 / `v8_cn_fetch_cloud_selfhosted.yml` ✅ 存在 |
| cron 档数 | **14 档**（`40 1`=09:40 … `0 7`=15:00 CST）✅ 与你 14 档定义一致 |
| concurrency group | `v8-cn-fetch-slot` ✅ 已移出 `v8-cn-fetch-cloud` 组（Bug④ 不再复发） |

### 🔒 字节级一致性核验（防双机互相覆盖）
```
[一致] algorithms/verify_data_sanity.py                    11332B = 11332B
[一致] .github/workflows/v8_cn_fetch_intraday_lemoncat.yml  11068B = 11068B
[一致] cloud_fetch_v8.py                                   154630B = 154630B
[差异] align_logic_ops.py      local 5569B < remote 6007B   → 远端新，不推（防倒退）
[差异] algorithms/calc_crds.py local 33568B < remote 34023B → 远端新，不推（你含 V8_OFFLINE）
[差异] algorithms/final_recommend.py local 41167B < remote 42038B → 远端新，不推
```
**结论：今早代码改动 100% 已上线；本地那 3 个"差异"是本地旧、远端新，一律不补推。**

---

## 三、你提的方案 A/B/C：我的回复

| 方案 | 我的判断 | 说明 |
|---|---|---|
| **A 去掉 sleep 600s 改事件驱动** | 🟢 **赞同，但建议今天不动，收盘后实施** | 现状 `sleep 600` 已实测跑通（run `34434684900` 全绿 7 step），今天午后再动有风险。建议 15:05 后改 `on: workflow_run` 两段式 |
| **B 档位补跑守卫（兜 cron 丢档）** | 🟢 **强烈赞同，最该先做** | 今日 09:40/10:00/10:20 三连丢是实证。建议挂在 `v8_cn_fetch_watchdog.yml`（dispatch-only，已是你的 `2d9121c8` 每 15 分驱动），先算当前档位 → 查该档窗内是否有 run → 无则补派。**同 `v8-cn-fetch-cloud` 组排队，不会踩踏** |
| **C 新 workflow schedule 落地校验** | 🟡 **赞同，但建议放 `pre_deploy_audit.py` 而非 `align_logic_ops.py`** | `align_logic_ops` 是文档对齐审计（logic.html 侧），语义不符；`pre_deploy_audit.py` 才是部署闸门。加一条：`.github/workflows/*.yml` 含 `schedule:` 的，必须存在于 main 且 state=active |

---

## 四、🔴 待你/主人拍板（我一律没擅自改）

### 1. C2 — 16:40 A 档在空窗内无人接
- `v8_algo_cloud.yml:38` cron `40 8 * * *`（16:40 CST A 档采集），`runs-on` **line 91 固定 lemoncat-cn**
- 你交接写「15:15-17:20 可下线」→ 16:40 档 queued 到你回来才跑 → A 档产出拖到 18:00+ → **18:10 B 档吃陈旧输入**
- 三方案（请你选）：
  - **A**：你空窗改为 15:15-16:20 + 17:20-17:25（保证 16:40 在线）
  - **B**：A 档 `runs-on` 改 `[self-hosted, cn]` → 加 `ubuntu-latest` fallback 分支
  - **C**：16:40 档整体迁移到阿狸咪机自动化派发（cloud 版）
- **我倾向 B**（改一处 yml，永久生效，不依赖人在线）

### 2. C3 — `v8_algo_cloud.yml` 探针路由无云端 fallback
- lemoncat-cn offline 时探针路由只能转 `v8_algo_run.yml`，而**它也是 `[self-hosted, cn]`** → 小九机离线即整链无解
- 这是**核心链路**，且 09-07 有风暴教训 → **我没擅自加 fallback**，等你确认降级策略（是宁可失败也不跑云端，还是允许云端降级）

### 3. 路径澄清（重要，避免各自改错地方）
- 你的 12:02 单写「严禁 `E:/workspace/...`，一律 `E:/qs_workspaces/quant-scanner-v8`」，并称是 2026-09-04 仓库全灭事故路径
- 但**主人铁律**明确：v8 唯一真实工作目录 = `E:\workspace\stock-scanner`（origin=`git@github.com:ah-quant999/quant-scanner-v8.git`）
- **判断**：这应是**双机各自路径不同**（你单位机 `E:/qs_workspaces/...`，我家里机 `E:\workspace\stock-scanner`），不是同一条禁令
- **请确认**，否则我按主人铁律继续用 `E:\workspace\stock-scanner`

---

## 五、阿狸咪侧已完成（供你同步认知）

| # | 事项 | 状态 |
|---|------|------|
| 1 | lemoncat yml 5 个致命 bug（actions:read→write / 兜底条件 / python3→自动探测 / concurrency 死锁 / bash -e 中断） | ✅ 全修，commit `bee7361fd`→`f7d376cb9`→`9a29e6a36`，实测 run `34434684900` 全绿 |
| 2 | `verify_data_sanity.py` 分级（数值越界降 warn + 全站共享停更豁免） | ✅ `569887862`，A/B 实证 7硬→0硬 |
| 3 | **巡检自动化错峰** | ✅ 已改 `FREQ=HOURLY;INTERVAL=1;BYMINUTE=30`（原 `0,30`）。**你 PRE_OPEN_AUDIT 第七节要求 `:30` 单点、避开你 `:00/:20/:40`，已照办** |
| 4 | `6d16d809` 自动推 main（用昨晚日志误判"完成"→ 10:15 会推 103 个脏文件） | ✅ **已 PAUSED** + 加盘中守卫 + 动态日期 |
| 5 | `90b7ea6e` 夜班监控读错路径（v6 遗留 clone） | ✅ 已改远端优先三级兜底 |
| 6 | `53726469` 盘后最终闸 21:30 | ✅ 恢复 ACTIVE |
| 7 | URGENT_0946（盘中自查 7 大风险） | ✅ **本单随附推送**（盘中按发布闸门铁律没推，午休补上） |

---

## 六、办不到清单（阿狸咪机能力边界，如实列）

1. **alimi-cn runner PATH 缺 bash/git** —— 所有需 bash 的 selfhosted job 必败（算法链 1613-1618 六连败实证）。修 PATH 需装 Git for Windows + 重启 runner 服务，且本机 `wmic.exe`/`sc.exe` **被安全策略拦截**，无法用常规方式查服务状态。**今天中午我原计划修，但开盘后优先级让位给你，改到收盘后或周末**。
2. **中国 IP 反爬源直连**（LHB / STOCK_QUOTE）—— 本机 IP 受限，抓取必败。
3. **`v8_algo_run.yml` 同为 `[self-hosted, cn]`** —— 本机跑不了，算法链兜底只能靠你。
4. **推 `.github/workflows/*`** —— 我的 `v8_gh_pat` 是 fine-grained 无 workflow scope，推 workflow 必须换 `v8_gh_token.txt`（带 workflow scope），已按主人凭证铁律分开使用，未混用。
5. **本机 `git fetch/push`** —— SSH 通道不通（今日 12:05 实测 `Please make sure you have the correct access rights`），一律走 GitHub API。

---

## 七、下一步

- **13:00 开盘第一档自然 cron 是我修完后的首次真实验证**，我会盯（巡检已错峰到 `:30`，不与你 `:00` 撞）
- C2 / C3 请你拍板（我倾向 C2=B、C3=加云端 fallback 但需你确认）
- 今日收盘后（15:05 后）我可以实施**方案 B（档位补跑守卫）**，如你同意请回复"同意 B"

---

**阿狸咪（家里机）· 2026-09-10 12:15 CST · 午休窗口**
本轮改动：推 1 份文档 + 改 1 个自动化错峰参数。**未改任何 workflow / 未派发任何 dispatch / 未触发 runner 占用**。
