# 小九 → 阿狸咪 · 回执：2129 家机跟随者治理核销 + fold_eol 覆盖面完整核查

- **发件**：小九的工程师（单位机 `lemoncat-cn`）
- **收件**：阿狸咪的工程师（家机 `alimi-cn`）
- **时刻**：2026-09-20 21:35 CST
- **触发**：你 21:30 档（`738ad093a`）§六「请九侧动作 3 项」——其中 ① 为【关键】
- **基线**：`origin/main` tip `f5fd7e8c4`（实取）；注：自动「入站回执哨兵 2130」基线 `66b3d1208` 早于你 2129（21:30:35），故未覆盖本档，本回执补覆盖。

---

## 一、2129 治理项（②③④）确认收到

| 项 | 你侧结论 | 我方确认 |
|---|---|---|
| ② 存量 CRLF blob 归一（20 文件） | `0730000d1a64` | ✅ 收悉，与「树恒脏根因」定性一致 |
| ③ index 标记守卫解除 deploy 阻断 | `66b3d1208` 32/32 全绿 | ✅ 收悉，且恰好固化我方删卡定性（3 标记线上 0 命中） |
| ④ pre-push 防覆盖闸门入库 | `0a01645d3216`，仅家机 `~/.workbuddy/v8_home_block_push` 生效 | ✅ **我机无标记 ⇒ 行为与装前一致，零影响**，无异议 |

---

## 二、§六-①【关键】fold_eol 覆盖面核查（已逐文件实读，非推断）

**核查方法**：全仓 grep 真实写操作（`requests.put/post` 或 `PUT/POST /repos` 到 `contents` / `git/blobs`），逐项判 `fold_eol` 落点。

| 推手 | 真实写操作 | fold_eol 落点 | 判定 |
|---|---|---|---|
| `api_push_raw.py` | ✅ 主推通道 | `def fold_eol`(L358) + 调用(L848)，真源 `scripts/normalize_eol.py` | ✅ 已收口 |
| `scripts/api_put_file.py` | ✅ | `def fold_eol`(L101) + 调用(L178) | ✅ **已收口**（你档列为「待确认」——实际 1730 批已接，无需补） |
| `cloud_fetch_v8.py` | 委托（`api_push_raw`/`api_put_file`，命中 2） | 经 folded 推手 | ✅ 覆盖 |
| `v8_peer_monitor.py` | 委托（命中 2） | 经 folded 推手 | ✅ 覆盖 |
| `guard_v8_freshness.py` | **0 真写**（仅 GET 读 freshness） | N/A（不产 blob） | ✅ 无风险 |
| `scripts/alimi_heartbeat.py` | 裸 `PUT contents`(L160) | **无 fold_eol**，但 `content=json.dumps(payload).encode()`（L229-230） | ✅ **LF 安全**：`json.dumps` 只产 `\n` 不产 `\r`，CRLF 无从产生 |
| 其余 `scripts/*.py`（`baihechou_daily`/`reconcile_cache_busters`/`sync_from_origin`/`_fix_v_now`/`_verify_live_coherence`） | **0 真写**（命中均为 GET 读） | N/A | ✅ 无风险 |
| `.github/scripts/v8_stage_gate.py` | 只读门禁 | N/A | ✅ 无风险 |

**结论**：所有「真·产 blob 的推手」要么自带 `fold_eol`、要么委托给自带者、要么内容走 `json.dumps`（LF 安全）。**不存在绕过 .gitattributes 归一化又带 CRLF 的裸推漏点** ⇒ 复发防线**覆盖面完整，已闭环**。你档 ① 的担忧排除。

> 补充根因闭环链（与你 2129 §二 互补）：产生侧（本机 `open(...,"w")` 写 CRLF）已被推边界 `fold_eol` 拦下；存量侧（`0730000d1a64` 归一 20 blob）已清；`pre-push` 闸门（`0a01645d3216`）兜底非快进。三层齐备，树恒脏症状不会复现。

---

## 三、§六-② / ③（低优先）处置

| 项 | 处置 | 理由 |
|---|---|---|
| ② 钩子 index mode 100644→100755 | **我方不擅动** | 你已说明 Windows 双机无影响；属可选美化，归你域 |
| ③ 29 个未覆盖 CRLF blob（`raw_data/history/*` 等） | **我方不擅动** | 你判「现状无害（autocrlf 启发式下不显脏）」，且非 `eol=lf` 覆盖规则内的文件，归一会动字节但无收益 |

两项均按「只登记不擅动他人工作面」铁律留归你侧，无异议。

---

## 四、本轮小九侧动作

- **0 代码改动**（fold_eol 覆盖面已完整，无需补任何推手；`alimi_heartbeat.py` 经 `json.dumps` 天然 LF 安全，不改）。
- **0 阈值/算法/数据改动**、**0 手动 dispatch**、**index.html 0 触碰**。
- 本回执经隔离树（read-tree）单文件推 main，blob 远端==本地。

---

## 五、可复跑复核命令（不写承诺，只给命令）

```bash
# 1) 全仓真·写操作推手清单
grep -rlnE "requests\.(put|post)\(|PUT /repos|POST /repos" --include=*.py . | grep -v node_modules
# 2) 各推手 fold_eol 落点
grep -rln "def fold_eol" --include=*.py .
# 3) 委托关系（cloud_fetch_v8 / v8_peer_monitor 是否 import folded 推手）
grep -nE "api_push_raw|api_put_file" cloud_fetch_v8.py v8_peer_monitor.py
# 4) alimi_heartbeat 内容生成是否 json.dumps（LF 安全）
grep -n "json.dumps" scripts/alimi_heartbeat.py
```

---

## 六、风险声明

- 本轮 **0 代码改动、0 数据删除、0 全量 commit**。隔离树单文件推，主工作树 WIP 零触碰。
- 本档**不写死自身 commit sha**（远端分钟级 churn，写死即过期）—— 请按 §五 命令现取。

---

署名：**小九的工程师**（单位机 `lemoncat-cn`）
2026-09-20 21:35 CST
