# HANDOVER_小九_2026-09-10.md — 小九 → 阿狸咪 交接文档

> **更新于 2026-09-10 12:02 自动更新**（小九交接自动化 1785719698947，12:00 槽位）
> 数据口径：origin/main 只读实测 + 当日双源工作日志（C 盘主日志 + E 盘工作区日志）合并。

---

## 一、背景

- 九宝量化 v8（`quant-scanner-v8`，GitHub Pages main 分支直读）。双机分时：小九日间 7:45-17:45 / 阿狸咪夜间+周末。
- 今日（09-10，周四，交易日）主线：**maharo 机构研究 insights「双机覆盖」事故的根因定位与一劳永逸根治** + 发布闸门（v8_publish_gate）落地。
- 昨日遗留：算法链根因①（alimi-cn 缺 bash）已实锤，runs-on 固定 lemoncat-cn；TRIPLE_HISTORY/VOLATILITY 产物链复核移交待办。

## 二、今日已完成改动（含真实 commit SHA）

| # | 改动 | 关键 SHA / 验证 | 影响 |
|---|------|----------------|------|
| 1 | **maharo 日报更新上线**：cookie 仍有效（days_left=29），拉今日 80 条机构源推送 | `87c383c1a5` → 绕过修复 `e04f645d28`；Pages build `34422682980` success | 线上 `updated_at=2026-09-10 08:43` |
| 2 | **maharo insights 质量缩水补救**：daily 732→2208 字，恢复「结论+主线+反证/风险+观察点+口径」框架 | `c5689d7f9c`（GitHub API 按 sha 权威核验） | 教训固化：手写 insights 不得退化为纯利好罗列 |
| 3 | **发布前二次核对闸门** `v8_publish_gate.py`（build/verify/publish/flush 四步 + 盘中时间窗拦截 09:00-11:30、13:00-15:00） | 落在 `C:/Users/Administrator/.workbuddy/`，配 2 个 flush 自动化（工作日 11:35 / 15:05）+ skill `v8-publish-gate` | **铁律生效：人工修订 data/*.js 一律走闸门，严禁直推** |
| 4 | **maharo 覆盖根治（最重要）**：insights 从 `maharo_macro.js` 拆出独立 `data/maharo_insights.js`（`window.MAHORO_INSIGHTS`），前端改读独立文件；双机覆盖问题永久消除 | 10:45 主人明令 override 盘中，force 推送 4 文件：`facf72e3db`(insights.js) / `3215a19bc3`(index.html) / `71575e618f`(compare.py) / `4e443e1c6d`(交接单)；GitHub API 核验 ✅ | 云端重生成 maharo_macro.js 不再影响前端显示 |
| 5 | **生成器全面审计器** `v8_generator_audit.py`：扫描所有输出 data/*.js 的脚本是否读本机私有文件 | 审计结论 ✅ 全部干净（maharo 为唯一 offender，已根治） | 新增/改生成器后必跑，输出「未检出」才算过关 |
| 6 | 盘前全局速览卡：`sec-final` 顶部新增「🌅 盘前全局速览」（基于 MAHORO_INSIGHTS + INDEX_QUOTES，无新数据源） | 三件套验证过；本地预览 `preview_premarket.html`（未跟踪） | **本地已就绪未上线**，等 15:05 休市窗口部署 |
| 7 | 盘中 11:30 批 + 最终推荐回滚修复（raw_data+data 成对直推） | 远端系列 commit 至 `a7cb8de05`；build `f53a9da08` (11:58) | 线上 AI_MARKET_BRIEF 11:57 / RUNNER_STATUS 11:44 新鲜 |
| 8 | 盘中14档准点档调度器 `v8_cn_fetch_intraday_lemoncat.yml`（C1 风险项） | **阿狸咪 11:00-11:31 自行已办**（勿重复推） | C1 关闭 |

### ⚠️ 本地仓库分叉已根治（本自动化处置）
- 12:02 发现本地 main ahead 1 / behind 73（本地 `7645837fe` 与远端同批数据重复）。经核实该 commit 内容全部为远端已包含的数据批（且远端更新）、index.html「WIP」36 行全是构建戳/`?v`/CRLF 噪声（无人工改动）→ mixed reset 对齐 + 仅恢复 15 个纯生成文件。原 commit 留 reflog。**现本地 = origin/main = `2a05fcefb`，工作树干净（仅未跟踪文件）**。

## 三、当前系统状态（12:02 实测）

| 项 | 状态 |
|----|------|
| 站点 | HTTP 200 ✅ |
| origin/main | `2a05fcefb`（handover: 盘中分工自查闭环与7大风险处置） |
| 本地 main | `2a05fcefb` ✅ 已对齐 |
| 线上 HEALTH_CHECK | updated 11:57:55，overall=fail（ok109/warn1/fail6/total116） |
| RUNNER_STATUS | 11:44:28 |
| 发布闸门 staging | 已空 ✅（今早 4 文件 10:45 已全部上线） |

## 四、阿狸咪注意 / 待办 / 风险

1. **待主人拍板（C2/C3/C6/C7）**：C2 小九 15:15-17:20 离线空窗→16:40 A档须覆盖（或云端 fallback 接 16:40）；C3 algo_cloud offline fallback；C6 自动化条件写死日期；C7 21:30 终闸 PAUSED 冲突。C1 已由阿狸咪办毕。
2. **发布闸门铁律**：任何人工修订 data/*.js / 前端 / 脚本，盘中（09:00-11:30、13:00-15:00）绝不直推，一律 `v8_publish_gate.py` staging → 11:35/15:05 自动 flush（二次核对后上线）。
3. **maharo insights 重生成**：必须沿用「结论+主线+反证/风险+观察点+口径分层」框架；别靠 `maharo_daily_pull.py` 的 write_js 自动注入（有 bug），用 importlib 复用 push_to_main 合并法。
4. **禁止再推 `maharo_macro.js` 的 insights**（前端已不读）；手写内容绝不内嵌进会被多机重生成的 data/*.js。
5. **Pages 静态缓存坑**：`?_cb`/`?v` 对 .js 无效，验证线上须 `git show origin/main:` 或 GitHub API，勿信 CDN 与本地 Write 即时内容。
6. **未跟踪文件（按纪律未动）**：HANDOVER_小九_2026-09-10_1500-1800空窗期.md、HANDOVER_小九_2026-09-10_双机分工.md、INTRADAY_0940_STATUS、PRE_OPEN_AUDIT、backup/reset_2026-09-09_1530/、preview_premarket.html。勿删，待小九处置。
7. **今日信号灯/算法链**：E 档三红灯已翻绿（08:46 看门狗 API 定性收口）；TRIPLE_HISTORY/VOLATILITY 产物链复核仍在待办。
8. **坚果云死区路径**：严禁 `/e/...`、`E:/workspace/...`（2026-09-04 仓库全灭事故路径），一律 `E:/qs_workspaces/quant-scanner-v8`。

## 五、小九监督职责（日间）

- 盘中盯防：HEALTH_CHECK / STOCK_QUOTE / cn_fetch 轮次新鲜度；看门狗告警定性。
- 发布闸门监督：盘中人工修订一律拦截 staging；11:35/15:05 flush 落地核对。
- 15:05 休市窗口：部署盘前全局速览卡（preview_premarket.html 三件套已验证）。
- 12:00/18:00 交接自动化（本任务）+ 19:00 每日审计。
