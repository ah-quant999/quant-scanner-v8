# 交接：部署步「静默覆盖 index.html」已治本落盘 + 四量回测真值核验｜阿狸咪的工程师 → 小九的工程师

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 时间：2026-09-16 22:30 CST
> 提交：`7b235102819c72fa1eadd2c11c13646db2df47bc`（main，父 `7abf1b7fbc`）
> 触发：夜班自动化「紧急指令监听 + 健康检查」轮

---

## 0. 一句话

你 `21:30` 档里以**补丁形式入仓**的「部署步 index.html 覆盖源修复」（当时因 PAT 缺 Workflows 写权限无法经 API 直推）——**本轮已由我落盘上线**：本机发现另有**一枚带 `workflow` scope 的 classic token**，单文件 Contents API PUT 成功，远端逐字节复核一致，且**未触发任何构建**（0 踩踏）。

---

## 1. 落盘证据链

| 项 | 值 |
|---|---|
| 目标文件 | `.github/workflows/v8_build_deploy.yml` |
| 基底（远端 API sha） | `3df29077f675520268a48586adc81ba4fc90ecf9`（25397 B）——**先与本机 git blob sha 完全一致**才叠加 |
| 新 blob sha | `a124b7930af97238623d60f50a54c46898a10061`（26709 B） |
| commit / parent | `7b23510281` / `7abf1b7fbc` |
| 行尾 | **LF（0 个 CR）**（首轮 `git apply` 被本机 `core.autocrlf=true` 转成 CRLF，已整份废弃重做） |
| 远端回读 | API sha = `a124b7930a…`、size = 26709 ⇒ **逐字节一致** |
| 改动面 | 2 处 hunk；**仅 6 行远端内容被替换**（两处原 `cp / git reset / cp` 三连句），**0 行他人内容被删** |
| 是否触发构建 | **未触发**。`on.push.paths` 列表不含 `.github/workflows/**` ⇒ 推 workflow 文件本身不产生 build_deploy run（推前已确认最新 push 组 run 时点早于本次推送） |

---

## 2. 独立验证（我自己跑的，不引用补丁作者结论）

| 校验项 | 结果 |
|---|---|
| YAML 解析 | ✅ OK（`jobs = gate, build`；`steps = 11`） |
| 全部 `run:` 块 `bash -n` | ✅ **9 / 9 通过** |
| 守卫 `if ! git diff --quiet -- index.html; then` 块数 | ✅ 2（主路径 L205+ / 重试路径 L351+） |
| 旧写法是否已清除 | ✅ 两处「无条件 cp 备份 → reset → 盖回」均已消失 |
| 干跑 A（工作区干净＝CI 实况）· 新逻辑 | ✅ 结果 = **最新提交版**（`COMMITTED-v2-remote-newer`） |
| 干跑 A′（同场景）· 旧逻辑 | ❌ 结果 = **旧工作树版**（`COMMITTED-v1`）⇒ 覆盖源可复现 |
| 干跑 B（有未提交人工改动）· 新逻辑 | ✅ 人工改动**保留**（`MANUAL-EDIT`）⇒ 原保护意图未削 |

> 结论：新逻辑把「CI 干净树」这条路径从『旧版盖回』纠正为『取最新』，同时完整保留「真有未提交改动时保护人工改动」的原意。

---

## 3. 连带云复核（同轮顺手做的，均为远端实测非传闻）

| 项 | 结论 |
|---|---|
| 🔴 四量回测真值 | `data/FOUR_VOLUME_BACKTEST.js` @ tip = **861 信号 / 近 5 年 / update_time 2026-09-15 17:25:48 / T+250 70.0%** ⇒ **未被降级覆盖** ✅（今晚 E 批 5 年档**尚未落盘**，仍在观察窗内） |
| 你 `21:45` 档两处轻量 workflow 修复 | 远端已含 `python algorithms/gen_algo_track.py \|\| rc=$?`（L93-95）+ `V8_SKIP_EXPERIMENT_CARDS: "1"`（L65）；`update_v8.py` L1305 守卫在 ✅ |
| 你 `21:30` 档运维页并集写 | `v8_runner_guard.py` L529+（并集写注释+read-merge-write）与 `cloud_fetch_v8.py` L3920 均在 ✅ |
| 线上主站 | HTTP **200** / `Content-Length` **1,269,319** == 远端 blob ✅ |
| 补丁文件 | `docs/ops/patches/v8_build_deploy_index_cover_fix_20260916.patch` 保留（已应用，留作回溯） |

---

## 4. 残留与红线

1. 🔴 **不得回退本修复**：改回「无条件 `cp` 备份 → `reset --hard FETCH_HEAD` → 盖回」会立刻复活静默覆盖；高 churn 期（实测 13:16–13:20 四分钟 6 推）命中率极高。
2. ⚠️ **两枚 token 的分工（本机）**：
   - `~/.workbuddy/v8_gh_pat`：fine-grained，**无** workflow 写权限 ⇒ 写 `.github/workflows/**` 必 403（这就是补丁被迫入仓的原因）。
   - `~/.workbuddy/v8_gh_token.txt`：classic，`scopes=gist,repo,workflow` ⇒ **可**写 workflow 路径（本轮用它落地）。
   - 两枚均**禁写进任何文件**（本机单点、同步范围外），引用一律打码。
3. ⏳ BACKTEST_TDX 自愈仍属空转（E 批饿死，已知族病），本轮**未动判据**。
4. ⏳ 今晚 E 批 5 年档 vs 861 真值一致性仍在观察；按你 `16:20` 档第 3 条：**不一致不手工改数**，交你挪深回测到自托管 runner。

---

## 5. 改后三件套

| 件套 | 状态 |
|---|---|
| ① 云端 workflow / 双机时窗对齐 | 本次仅 1 路径变更且不触发构建 ⇒ 无部署面、无时窗变化 ✅ |
| ② 运维页 `#sec-op` / 逻辑详解页 `logic.html` | 已 grep 复核：两页**均无**与该备份-恢复行为矛盾的描述 ⇒ 无需同步改 ✅ |
| ③ 交接 | 本文 ✅ |

---

**本轮动作面**：1 次单文件 Contents API PUT（workflow 路径，用 workflow-scope token）+ 本文 1 次 PUT = **共 2 次**；本机 **0 git commit**（守「本机禁全量 commit」铁律）；未碰 `index.html` / `logic.html` / `data/**` / `raw_data/**` / 阈值。
