# 🔴 URGENT 交接 · 第181轮 · 2026-08-13 13:12-13:30（云双 workflow 双 bug 根治+实时验证）

> 自动化：小九-v8每小时紧急交接通道(看门狗+自动修复) `automation-1785835481750`
> 执行机：小九（E:/workspace/quant-scanner-v8）· 时区 CST · 13:12 处于 07:00-22:00 活跃发信窗口
> 关联提交：`be463837`（删 fallback 键）→ `1821b1dd`/`63e64914`（runs-on 改数组形态）→ 均已推 `origin/main`

## 一、看门狗六管线结果（本轮触发）

| 管线 | 状态 | 说明 |
|------|------|------|
| runner | ✅ OK | lemoncat-cn online/idle |
| cn_fetch | ✅ OK（修复前最后成功 11:29） | 本轮 auto_dispatch 派发先 422，根治后已重新跑（见下） |
| build_deploy | ✅ OK | 11:38 |
| raw_data_fresh | ✅ OK | 12:19（53m < 1.5h） |
| site | ✅ OK | HTTP 200 |
| auto_dispatch | 🔴 FAIL | HTTP 422 `failed to parse workflow ... Unexpected value 'fallback-to-github-hosted-runner'` |

- 健康检查 rc=2，overall=**fail**（ok32/warn4/fail9）：9 FAIL 为盘中卡陈旧（已随本轮派发刷新），4 WARN 为 site_sync/弱市三池0只内容审计。
- 告警邮件：**1 封**（`【v8看门狗告警】1项异常 @ 13:12:29`，auto_dispatch 派发失败）。活跃窗口+真实故障，合理非静音。

## 二、🔴 根因（两层 bug，已定位+已验证）

commit `1fe4f8c7`（2026-08-13 **11:47:15** 推送）把 `v8_cn_fetch_cloud.yml` / `v8_algo_cloud.yml` 的 `runs-on` 由可用的 `ubuntu-latest` 改成映射形态：

```yaml
    runs-on:
      group: self-hosted
      labels: [cn]
      fallback-to-github-hosted-runner: true
```

**Bug ①（解析层 422）**：`fallback-to-github-hosted-runner` 仅对 larger/auto-scaling runner 有效，标准 self-hosted 触发 GitHub 工作流解析 422 → `schedule`/`dispatch` 全部无法触发（看门狗 auto_dispatch 422；候选池 algo_run 自愈派发同样 422）。

**Bug ②（执行层 1秒失败，更致命）**：`group: self-hosted` 无效。本仓 self-hosted runner `lemoncat-cn` 属 **Default 组**、标签为 `[self-hosted, cn]`、**无名为 self-hosted 的组**。映射形态匹配不到 runner → 任务创建后 **1 秒 `completed failure`（0 个 step）**。删掉 Bug① 的键后仍 1 秒失败，定位到 Bug②。

→ 两层叠加：自 11:47 起云双 workflow 完全停摆；**候选池 173–180 轮连续陈旧的根因正是 algo_run 派发 422 + 任务 1 秒失败**（此前误判为算法源 guanlan/maharo 空/失败，实为派发/执行链路被掐断）。

## 三、🟢 一劳永逸式修复（落根因+推仓+实时验证）

1. **代码层修**：两文件 `runs-on` 改为正确数组形态 `runs-on: [self-hosted, cn]`（既匹配标签 `cn`、又能抓中国源）；删除无效 `fallback-to-github-hosted-runner` 键；头部注释补全两层根因。
2. **校验**：PyYAML 解析全部 18 个 workflow 通过；API 读取 `origin/main` 确认 `runs-on: [self-hosted, cn]`、无两处非法键。
3. **commit + push**：`be463837` + `1821b1dd`（→`63e64914`）已推 `origin/main`。仅含 workflow 文件，**未碰红线 index.html / raw_data / HEALTH_CHECK.js**（index.html 经 autostash 拉取冲突后已 rescue 回原工作树态，保持 ` M` 未提交）。
4. **实时验证（用实时说话）**：修复后通过 GitHub API 直接 `workflow_dispatch` 触发——

| 验证项 | 结果 |
|--------|------|
| dispatch 接口 | cn_fetch / algo 均 **HTTP 204**（解析通过，无 422） |
| cn_fetch 最新运行 `31670390834` | **status=in_progress**（head=63e64914，runner 已接单执行，非 1 秒失败）✅ |
| algo 最新运行 `31670406159` | **status=queued**（head=b11a7d21，等 runner 空闲后执行）✅ |

→ 云双流水线已复活。盘中数据刷新（cn_fetch 在跑）+ 候选池 algo_run（排队）自愈链路恢复。

## 四、跨轮遗留更新

- ① git 分叉阻塞(r176) ✅ 已解除。
- ② **候选池 173–180 连续陈旧(r173-180)🔴→🟢 根因收敛**：本轮回填为「1fe4f8c7 两 bug 掐断派发+执行」，非算法源问题；修复后 algo 已重新派发，下轮(182)验证 candidate.json 是否刷新到今日。
- ③ final_market_distribution 港股集中(r168-177) ✅ 已解除。
- ④ r134 except-pass 建议 ⚠️ 仍未落地。
- ⑤ stash 残留（历史累积，未清理）。
- ⑥ 云端高频小更新致 local_sync 瞬态落后（良性，自愈跳过）。
- 工作树脏项：index.html（红线，已 rescue 回原态，` M` 未碰）、raw_data/*.json（当前 vs HEAD 干净，未纳入）、data/HEALTH_CHECK.js（看门狗输出，未纳入）。

## 五、下轮(182)跟踪点

1. cn_fetch 11:29 之后是否出现新成功运行（本轮 13:27 已派发在途，in_progress）。
2. **候选池 candidate.json 是否刷新到 08-13**（验证两层根因收敛，最关键）。
3. algo 运行 `31670406159` 出队后是否 success（确认候选池/算法链正常）。
4. 三池 0 只开盘后是否填充（弱市内容审计非故障）。

---
_本文件由每小时看门狗自动化生成，已 force-add 入库（根目录 URGENT_*.md 被 .gitignore:46 忽略需 -f）。_
