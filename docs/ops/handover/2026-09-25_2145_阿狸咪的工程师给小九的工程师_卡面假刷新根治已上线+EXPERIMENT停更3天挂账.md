# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-25 21:45｜卡面假刷新根治已上线+EXPERIMENT停更3天挂账

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主人报「今天主站有 T+1 数据，怎么全部没更新」——真因不是没部署，是**卡面「更新于」被刷成了构建时刻**：09-25 19:44 那轮提交（`0802df64e81c`）改了 17 个 `data/*.js`，**全部是 ±1 行**，只换了时间戳、业务数据一个字节没变。已根治并上线。

---

## 正文

### 一、因果链（铁证）

| 证据 | 实测值 |
|---|---|
| 09-25 19:44 提交 `0802df64e81c` | 改 17 个 `data/*.js`，**逐文件 +1/-1** ⇒ 仅时间戳变化 |
| 同提交的 raw 层 | **零产出**（无 raw_data 文件入列） |
| 09-22 那轮 `324a142bf114` | 是走 raw 推链的，带 22 个真实 raw 文件 |
| 主站 | HTTP 200 / 1,361,458 B，部署链路本身正常 |
| 前端观感 | 17 张卡「更新于」= 今天 19:44，内容仍是 T-1 |

**两条独立的假刷新路径，同一症状：**

1. `scripts/fetch_sector_leaders.py` 的 `payload["update_time"] = time.strftime(...)` = **落盘时刻**，与数据日期无关。实测 `data/SECTOR_LEADERS.js` 的 `update_time=2026-09-25 19:44:21` 而 `data_date=2026-09-24`。
2. `update_v8.py` 的 `_pick_ts()`：当 `existing` 为空时回退 `mtime_ts → now_ts`。而**云端 runner 每次 checkout 都会刷新 `raw_data` 文件 mtime** ⇒ 每轮构建把该卡「更新于」重写成本次 checkout 时刻，数据却没变。

第 2 条解释了为什么那 17 个文件都是 ±1 行：**只有时间戳那一行变了**。

### 二、已施工（commit `69166eab4dc0`，已上线）

| 文件 | 改动 |
|---|---|
| `scripts/fetch_sector_leaders.py` | `update_time` 取**源 SECTOR_RS 的数据时间**；源缺失才回落构建时刻，并显式写 `republish_time` 供排障。数据日期仍取 `src_date`，两者语义分离。 |
| `update_v8.py` `_pick_ts` / `_write_js` | 新增第三优先源 `_existing_file_ts`（`data/*.js` 已有 `update_time` 才是真实语义时间），置于 mtime 之前；仅 data 层首次构建时回退 mtime/now_ts，保持幂等。 |

推送前自证：`py_compile` 双绿 · 相对远端真身**白名单外零删除** · 小九 `EM_HOSTS` 等修复指纹全在位 · 漂移断言可 `sys.exit` 阻断。

### 三、复验口径（请小九明早复核）

1. 跑一轮 `v8_cn_fetch_cloud`，比对 17 个 `data/*.js` 的**内容哈希**：此前是「内容零变化、update_time 每轮刷新」，修复后应两者都不变。
2. `SECTOR_LEADERS` 卡面「更新于」应**等于源 `SECTOR_RS` 的 update_time**，不再是落盘时刻。

⚠️ 判「是否生效」**只看产物内容哈希，禁止看 run conclusion** —— 那轮 job 全绿，正因如此才一直没被发现。

### 四、顺带查实（附带更正我一处旧判断）

- **更正**：09-24 我报「SECTOR_LEADERS 内容还是 09-22 的旧数据」**是错的**。它内容已更新到 `data_date=09-24`，只是 `raw_data/sector_leaders.json` 停在 09-22/09-21 —— 该脚本是 **data→data** 链路（读 `data/SECTOR_RS.js` → 写 `data/SECTOR_LEADERS.js`，同时写 raw），两层不一致是发布路径差异所致，非内容陈旧。
- **`raw_data/sector_leaders.json` 与 data 层脱节 3 天**：raw 层停在 09-22 18:17，data 层 09-25 19:44。根因是 19:44 那轮只提交了 `data/*.js`，**没走 raw 推链**。未擅自改推送白名单（`_PROTECTED_RAW` 语义是「跳过不推」，改错会反向阻断）—— 留给你判断。

### 五、EXPERIMENT 卡停更 3 天（已挂账，非代码可解）

- `raw_data/experiment.json` 停在 `2026-09-22 18:13:35`；`data/EXPERIMENT.js` 最后提交 `0320100b3f33`（09-22 20:09）。
- 09-25 19:04 同轮日志：`⚠️ EXPERIMENT: 返回空，跳过` —— `save()` 按设计跳过写盘（防假刷新，口径正确）。
- 真因属**东财 push2/clist 端大面积 502 / 非 JSON 响应窗口**，非本仓代码缺陷。09-24 加的 `::error title=v8-experiment-no-data::` 护栏现在应能让失败**显式标红**（此前零可见信号）。
- 处置：明日复验该 `::error` 是否出现即可判断护栏生效；数据恢复后自动推进，**不以人工补跑充数**（禁伪造）。

### 六、待你裁决

1. `raw_data/sector_leaders.json` 的 raw/data 两层脱节，是否调整 raw 推送路径（**未擅自改**）。
2. `fetch_inst_trade.py` 今日 `退出码 1 | 全部接口重试仍失败，保留旧数据（如有）` —— 静默降级，建议同族加显式告警。

### 七、本次提交

| commit | 内容 |
|---|---|
| `69166eab4dc0` | 卡面假刷新根治（两文件） |
| `f2ce1271c3f2` | 悬空提交（blob 未持久化，已由 `cd56033bde4e` 重建，**无需处理**） |
| `cd56033bde4e` | HANDOFF + ledger 挂账两项，线性快进、`--check` 84/84 |

---

## 附：交接件自检

- [x] 内容基于已查证事实，无推测
- [x] 关键结论附命令/提交/时间戳
- [x] 明确标注未擅自处置项

