# v8 盘中双机兜底交接确认单

**日期**：2026-09-10（周四）08:10 CST  
**交接方向**：小九机 → 家里阿狸咪  
**当前状态**：A/B/C 四文件已全部落地 main，小九机今日白天仍在线值守至 15:30，之后由阿狸咪全面接管。

---

## 一、A/B/C 修复落地情况（全部完成）

| 文件 | 状态 | 关键改动 | commit |
|------|------|----------|--------|
| `cloud_fetch_v8.py` | ✅ 已落地 | `_has_critical_failures` intraday 仅 `CANDIDATE_QUOTES`/`INDEX_QUOTES` 致命；其余源失败不阻断整轮 | 已在 main |
| `.github/workflows/v8_cn_fetch_cloud_selfhosted.yml` | ✅ 已落地 | `📡 抓中国数据` 步恢复 `continue-on-error: true`；selfhosted 并发组改为与云端统一 `v8-cn-fetch-cloud` | 已在 main |
| `.github/workflows/v8_cn_fetch_watchdog.yml` | ✅ **今晨直推** | 移除 GitHub `schedule` cron；PROBE intraday 加入 `CANDIDATE_QUOTES.js`；新增 `cloud_state()`/`dispatch_cat()`，拥堵/刚失败改派 selfhosted | `20e6db62` |
| `guard_v8_freshness.py` | ✅ **今晨直推** | 加入 `SELFHOSTED_WORKFLOW_ID=336661558`；`_dispatch_cn_selfhosted()`；`_cloud_congested()`；intraday 拥堵/失败时改派 selfhosted；管线失败重派直接走 selfhosted | `f65ec799` |

---

## 二、阿狸咪今日执行清单（按截图）

| 时段 | 动作 | 状态 |
|------|------|------|
| 08:10 → 09:30 | 代码已落地；开盘前可再拉一次 main 核对四文件 | ✅ 已完成 |
| 09:30 → 11:30 | 监控实时数据，按预案 dispatch 盘内 yml | 待执行 |
| 11:30 → 14:55 | 出 SANITY 阈值改造方案给小九审，按拍板改 | 待执行 |
| 14:55 → 15:30 | 抓全链路摘要，回到主人这 | 待执行 |
| **15:30 起** | **全面接管**：所有 dispatch 从阿狸咪这边发；改源前先把 diff 拉给小九审 | 交接点 |

---

## 三、自动化状态（小九机当前）

> 本机 WorkBuddy db 中的自动化，阿狸咪机器上有一套独立副本。**下午 15:30 阿狸咪接管后，由她在自己机器上 ACTIVE 对应任务，然后本机再 PAUSE，避免空窗。**

| ID | 名称 | 当前状态 | 15:30 后 |
|----|------|----------|----------|
| `195d8839` | v8 盘中统一调度器(20分·单源·新鲜度驱动·双机兜底) | ACTIVE | 阿狸咪 ACTIVE 后小九 PAUSE |
| `2d9121c8` | v8 看门狗驱动(每15分·去GitHub cron) | ACTIVE | **必须保持至阿狸咪机器 ACTIVE 副本后再 PAUSE** |
| `e7b2e732` | 小九-v8 盘中「更新→部署」全链路自愈哨兵(每5分) | ACTIVE | 阿狸咪接管后 PAUSE |
| `1786961773708` | v8 自愈监控闭环(candidate观澜台+momentum状态·10分钟级) | ACTIVE | 阿狸咪接管后 PAUSE |
| `1787108824799` | v8 盘中漏轮云端守卫(监督·禁止本地直推main) | PAUSED | 保持 PAUSED |
| `1786696074524` | v8 runner 本地守护 | ACTIVE | 保持 ACTIVE（监控用） |
| `1785719698947` | 小九-12/18点 v8当日交接文档更新(给阿狸咪) | ACTIVE | 保持 ACTIVE（生成文档用） |

---

## 四、关键命令（阿狸咪备用）

如需要手动触发 selfhosted 兜底：
```bash
gh workflow run v8_cn_fetch_cloud_selfhosted.yml -f category=intraday --repo ah-quant999/quant-scanner-v8
```

手动触发看门狗：
```bash
gh workflow run v8_cn_fetch_watchdog.yml --repo ah-quant999/quant-scanner-v8
```

---

## 五、风险提示

1. **`v8_cn_fetch_watchdog.yml` 已移除 GitHub schedule cron**，完全依赖 WorkBuddy 自动化 `2d9121c8` 每15分触发。若阿狸咪机器上该任务未 ACTIVE，watchdog 将无人触发。
2. **selfhosted runner `alimi-cn` / `cn` label** 必须在线，否则拥堵改派会失败。fallback 仍留云端普通派发。
3. 今晨 08:20 HEALTH_CHECK 仍有多个 fail（IPO_DATA/JUDGMENT_DATA/ETF 类等），属开盘前数据尚未刷新，待 08:15 全量任务完成后会改善。

---

本报告仅供研究参考，不构成个人投资建议。
