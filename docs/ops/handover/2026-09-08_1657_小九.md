# 小九 → 阿狸咪 交接文档 · 2026-09-08（16:57 收官版）

> **更新于 2026-09-08 16:57**（小九白班收官，供阿狸咪 18:00 上岗对齐）
> 本档为 15:20 版的整份覆盖更新，含算法死代码清理(16:38-16:52) + 注释引用清零 + 改后三件套复核 + 实时状态核实。
> 数据时效：origin/main SHA、站点 HTTP、线上 update_time 均为 **16:51-16:57 实测**，非记忆值。

---

## 一、背景

- 双机分时：小九白班（7:45-17:45）→ 阿狸咪夜班（18:00-次日 7:30 + 周末）。
- 今日主线（按时间序）：凌晨清红灯 → 盘前/盘中「更新→部署」及时性盯防 → STOCK_QUOTE 断档 8h 根因与根治 → FACTOR_LAB 5.4h 静默挂起真根因修复 → STOCK_QUOTE 被 cn_fetch 覆盖回滚事故根治 → AI速览死行审计 → **下午已下架页删卡 + 改后三件套审计**。

## 二、今日已完成改动（真实 commit SHA，git log 实测）

### 2.1 凌晨清红灯（00:00-04:00，fail 6→2）
| 红灯 | 根因 | 处置 |
|---|---|---|
| SH_FIB / INST_TRADE / H_AUTO_BUY | 盘后 A/B 档未重跑 | cn 快链 stage=A(00:15 success)、stage=B(01:46 success) 补齐 |
| maharo_macro | 文件名 `maharo_macro.js` ≠ window 变量 `MAHORO_MACRO` | `v8_health_check.py` `_WINDOW_VAR_ALIASES` 加别名 |
| INDEX_HISTORY | `update_v8.py` DATA_SOURCES 缺 `index_history.json→INDEX_HISTORY` 映射 | 补映射 |
| MARKET_PATH_PROBABILITY | 无产出脚本+前端 0 引用孤儿死数据 | 已删除 |
| FACTOR_LAB | 见 2.3 | 已根治并上线 |

### 2.2 STOCK_QUOTE 断档 8h 根因 + 根治（已落地）
- 三层根因：①GitHub cron 结构性重注册仍零触发；②兜底 `82038f74` 假阳性互踩（runs 查询缺 `_cb` → 缓存卡旧 → 四档全跳过），升 v2 加 `_cb`+fail-open+裁死档；③云端美国 IP 被新浪/东财反爬。
- 根治方案（本会话主任务，已全部 push）：
  - `v8_stock_quote_refresh.yml` 回迁 **[self-hosted] 小九单位机（中国 IP）** 跑，绕开反爬根因；源码同步经实测改为 **仅拉 fetcher 必需 3 文件（Contents API 单请求）**，根治全仓 blob 爬取慢/限流卡死 + https/ssh 中国 IP 挂起。
  - `v8_stock_quote_refresh_cloud.yml` 云端兜底副本（**独立 concurrency 组** `v8-stock-quote-refresh-cloud`，不被 self-hosted 卡死 run 阻塞）。
  - 哨兵 `v8_deploy_watch.py` 加「self-hosted 接单超时(queued>SQ_STUCK_MIN=12分)→改派云端兜底」逻辑（仅本地文件，见第四节）。
- `4ffa2cf8f`：从 update_v8 DATA_SOURCES 移除 STOCK_QUOTE 映射，v8_stock_quote_refresh 工作流为唯一写入方，根除 cn_fetch 覆盖链路。

### 2.3 FACTOR_LAB 5.4h 静默挂起真根因修复（commit `3a8ceba5c`，已 push）
- 真根因：`resolve_data_date` 回退探测前 baostock 连接态被污染 → `rs.next()` 静默死等 5.4h。修复 4 处（重置连接态/够数即停/300s 硬上限/socket 30s）。烟雾测试 2.0s。

### 2.4 防覆盖与缓存戳系列 + 盯防哨兵（11:21-12:00）
- 生成器原子提交 + 缓存戳 `?v` 实时对齐防覆盖（多 commit）。
- 新建哨兵 `v8_deploy_watch.py`（判据 build head_sha/部署滞后/抓取滞后/HEALTH/HTTP；`--heal` 自动 dispatch）；automation `e7b2e732`（每 10 分，交易日 08:00-15:55）。

### 2.5 AI速览死行审计（10:19，✅ 已落地 commit `0059d3ebe`）
- 头部定性结论死行（日内风向恒震荡/资金健康度恒大幅流入/操作建议恒观望）。3 处纯逻辑修复已于 **10:52 落地 push**（文件为 `algorithms/gen_market_brief.py`，非 `ai_market_brief.py`）：①涨跌比优先定级（破除窄幅震荡恒兜底「震荡」）；②操作建议新增轻指数重个股分支（破除恒「观望」）；③资金灯改用真实双向主力净额 `market_net`（根除概念源只含正导致恒绿）。

### 2.6 下午：已下架页删卡（15:10-15:15，commit `ea6be9413` + `0b66eb2e9`，已 push）
- **中信期货卡**：从 `renderDelisted` 删除整段 HTML 生成（约 27 行）。`data/CFFEX_HOLDINGS.js` 注入**保留**（仍用于盘中「期指主力合约」卡 + 已下架温度计/六维共振，9 处引用）。
- **ETF净申赎（东财五分类）卡**：origin/main 已先删卡体，本次补删残留的 `data/ETF_SUBSCRIPTION_EM.js` 脚本注入（注释+script 标签 3 行）。
- 改动均为纯删除（95 + 3 行，0 插入），已 fast-forward push。

### 2.7 算法死代码清理（16:38-16:52，commit `5e99e7455`，已 push；注释清零见本档提交）
- **删 2 个真·孤儿回填脚本**：`backfill_sector_trend_from_history.py`、`backfill_top10_legacy_sources.py`（全仓零引用）。
- **清 `run_algorithms.py` 六处死引用**（ORDER/STAGES/SCRIPT_TIMEOUT_OVERRIDE/SCRIPT_ENV/STOCK_PICKING_SCRIPTS/BACKTEST_SCRIPTS），指向 10 个已删的 `scripts/`+`v8/` 子目录脚本（这些子目录在仓库中已不存在）。
- 行数 1064 → 1041；`ORDER`/`STAGES` 模块级断言 `assert(_STAGE_UNION == set(ORDER))` 仍成立（40/40 并集相等）。
- 16:54 追加：清理注释中 3 处已删 `v8/*.py` 的提及（factor_lab_gen / fetch_citic_pe / gen_citic_pe），全文死引用(含注释)清零。
- 全仓管线溯源审计结论：产出→前端消费无新增悬空注入；清理后无功能回退。

## 三、✅ 改后三件套审计（16:51-16:57 全绿，覆盖 2.6 删卡 + 2.7 清理）

| 件套 | 检查项 | 结果 |
|---|---|---|
| ①语法 | YAML 两工作流（stock_quote_refresh / _cloud） | ✅ `yaml.safe_load` 通过 |
| ①语法 | Python 哨兵 `v8_deploy_watch.py` + `fetch_stock_quote_v8.py` | ✅ `py_compile` 通过 |
| ①语法 | `run_algorithms.py`（2.7 清理后，含注释清零） | ✅ `py_compile` 通过；全文 10 个已删脚本引用(含注释)残留=0 |
| ①语法 | `index.html` 内联 JS（24 个 script 块） | ✅ `node --check` 全通过，0 语法错误 |
| ②DOM | `index.html` 真实标签平衡（Python `html.parser` 跳过 script/style 原始文本） | ✅ 解析无异常、结束栈空、结构错配 0（注：原始正则计数 open/close 差系 JS 字符串内动态拼接的 `<div>` 等被误计，非真不平衡） |
| ③引用 | 中信期货卡删除后悬空函数 | ✅ 仅删卡渲染段，无悬空函数定义；CFFEX_HOLDINGS 仍被 9 处引用 |
| ③引用 | ETF_SUBSCRIPTION_EM 残留 | ✅ 零残留 |
| ③引用 | `run_algorithms.py` 死引用（ORDER/STAGES/TIMEOUT/SCRIPT_ENV/STOCK_PICKING/BACKTEST） | ✅ 0 活动死引用；`algorithms/v8/` 子目录已不存在，相关提及已清零 |
| ③引用 | STOCK_QUOTE 工作流 3 文件同步目标 | ✅ `algorithms/fetch_stock_quote_v8.py` / `algorithms/stock_industry_concepts.json` / `index.html` 均存在 |

## 四、⚠️ 本地未提交改动（阿狸咪上岗前必读）

### 4.1 小九单位机本地文件（不在仓库，勿 stash/checkout/覆盖）
| 文件 | 内容 | 性质 |
|---|---|---|
| `C:/Users/Administrator/qs8-tmp/v8_deploy_watch.py`（18KB） | 盯防哨兵：含「self-hosted 接单超时→改派云端兜底」逻辑；编译通过、只读巡检正常 | 仅本地文件，未入库（哨兵本就本地运行） |

### 4.2 工作树其他
- `update_v8.py` 等 CRLF 噪声文件（`git diff --ignore-cr-at-eol` 零内容差异）——**勿 `git add`**（今早已铁律验证）。
- 未跟踪历史文档：AUDIT_小九_2026-09-03~07、HANDOVER_小九_2026-09-01/03——按纪律不入库。
- index.html / 两工作流文件：相对 origin/main **无未推送改动**（今日删卡 + STOCK_QUOTE 迁移均已 push）。

## 五、当前系统状态（15:15-15:19 实测）

| 项 | 状态 |
|---|---|
| origin/main HEAD | `5e99e7455`（算法死代码清理）+ 本交接文档提交（16:57 推送后实际 HEAD 为最新） |
| 站点 | HTTP 200 ✅ |
| 本地 HEAD | 与 origin/main 对齐 ✅（无 WIP 阻塞） |
| runner | lemoncat-cn 白班在线 |
| 健康检查 | 见线上 HEALTH_CHECK（下午已下架删卡后未见新增红灯） |
| 分红刷新 | 云端连续成功，零兜底 |
| 调度器漂移 | 深夜守卫窗口外漂移移交主人跟进 |

## 六、待办 / 风险（移交阿狸咪 + 主人）

1. **AI速览 3 处逻辑修复**：✅ **已落地**（commit `0059d3ebe`，10:52，文件 `algorithms/gen_market_brief.py`）——非「待拍板」。详见 2.5。
2. **HERDING_DATA 类别改 post_close**：消除 runner 空 1 模块 warn（待授权）。
3. **RUNNER_STATUS 缺时间戳 warn（P3）**：待授权修复。
4. **NT_DATA 盘前 180min 豁免根治**：留有空时处理。
5. **调度器深夜窗口外漂移**：移交主人跟进。
6. **旧遗留待拍板**：板块周期卡方案 A / 超跌修复腿算法 / ROE 串行网络风暴优化。
7. **审计轨迹/任务看板 3 处修复**：✅ **已落地**（commit `e43857845` + `3821b85ab`）
   - ①`api_push_raw.py` 加 `_PROTECTED_RAW={audit_history.json, audit_nightly.log}`，walk_raw + PUSH_FILES 双跳，审计轨迹只走 git 不裸推（实测 09-08 多轮审计记录被覆盖丢失，已根治）；
   - ②index.html 审计轨迹空态去「23:30 本机审计」、nightly 空态去「23:30」；
   - ③`_uBadge` stale 分支加 `_slotHtml`，实时/盘中卡过期不再挂新鲜度标签。
8. **今日新坑务必牢记**：①Windows `io.open(w)` 默认 CRLF 污染仓库 LF，改仓库文件一律二进制写回 LF；②`git rebase --autostash` 冲突中断后改动全丢——改码一律单脚本内完成改+add+commit+fetch+rebase+push。

## 七、小九收官交接

- 白班全部改动已 push，本地无阻塞 WIP（仅哨兵本地文件，本就本地运行）。
- 改后三件套审计全绿，已下架删卡可安全上线（已随 CI 自动部署）。
- 阿狸咪 18:00 上岗可直接接手夜间算法链（A/B/D/E 批），无需先对齐本地 WIP。

---

## 八、20:41 小九补交（今日遗留 3 项修复全部落地 + 改后三件套 + 夜班盯防）

> **更新于 2026-09-08 20:41**（小九 20:38 后补做的数据时效/健康检查 3 项修复，已 push；供阿狸咪夜班核对）
> 数据时效：origin/main SHA、站点 HTTP、线上 HEALTH_CHECK 均为 **20:41 实测**。

### 8.1 一句话结论
今日 16:57 收官版列为「待授权」的 3 项修复（HERDING 改 post_close / NT_DATA 移全量数据页 / 审计产物保护+过期徽章）**已全部落地并 fast-forward push**，改后三件套审计实测全绿。线上 HEALTH_CHECK 仍有 2 项 fail：**FACTOR_LAB**（盘后 E 批 21:00 前转绿，已知非故障）+ **maharo_macro**（陈旧，需夜班核对是否进夜间算法链）。

### 8.2 ✅ 改后三件套审计（20:41 实测全绿）

| 件套 | 检查项 | 结果 |
|---|---|---|
| ①语法 | `v8_health_check.py` / `update_v8.py` / `api_push_raw.py` | ✅ `py_compile` 全过 |
| ①语法 | `index.html` 24 个 + `logic.html` 6 个内联 `<script>` 块 | ✅ `node --check` 全过，0 语法错误 |
| ②DOM | `index.html` 真实标签平衡 | ✅ 结束栈空、错误 0 |
| ②DOM | `logic.html` 真实标签平衡 | ⚠️ 2 处 `<details>` 告警 = **历史遗留**（已 `git show` 改之前版本 3d4966654 比对，完全相同、非本轮引入；`<details>` 开闭计数 2/2 平衡，不影响渲染） |
| ③引用 | `_uBadge`/`_slotHtml`/`renderAuditTrail` 定义与调用 | ✅ 16/16 全过（含 NT_DATA 全量数据页、HERDING post_close、_PROTECTED_RAW 审计产物保护、旧值清除核对） |

### 8.3 今日新增 3 项修复明细（已 push，阿狸咪无需重做）

| 提交 | 修复 | 文件 | 根因 |
|---|---|---|---|
| `e43857845` | NT_DATA 改挂「全量数据」页 | `v8_health_check.py` | 盘后算法链产出的 NT_DATA 原挂「今日事件」页，盘前 08:00-10:00 被 `adjust_max_age` 收紧到 180min 误杀 |
| `e43857845` | 审计产物保护 | `api_push_raw.py` | 新增 `_PROTECTED_RAW={audit_history.json, audit_nightly.log}`，walk_raw + PUSH_FILES 双跳，审计轨迹只走 git 不裸推（实测 09-08 多轮审计记录被覆盖丢失） |
| `3821b85ab` | 审计空态措辞 + 过期不挂实时徽章 | `index.html` | ①审计轨迹空态去「23:30 本机审计」；②nightly 空态去「23:30」；③`_uBadge` stale 分支加 `_slotHtml`，实时/盘中卡过期不再挂新鲜度标签 |
| `3821b85ab` | HERDING_DATA 改 post_close | `update_v8.py` | 羊群效应读当日完整涨停池、收盘定稿，原挂 premarket 致盘中刷不到/盘前判 stale（与 cloud_fetch_v8 对齐） |
| `db839ac5c` | logic.html 补充修复记录 | `logic.html` | 在「数据时段与调度漂移」卡后补「审计产物保护+过期不挂实时徽章+NT_DATA页面归属」说明 |

### 8.4 线上实时状态（20:41 实测，权威不看本地）

| 项 | 状态 |
|---|---|
| origin/main HEAD | `6cea59453`（含小九 3 笔修复 e43857845 / 3821b85ab / db839ac5c + 云端 cn_fetch/生成器自动提交） |
| 站点 | HTTP 200 ✅ |
| 本地 HEAD | 与 origin/main 对齐 ✅（无 WIP 阻塞） |
| 健康检查 overall | **warn**（ok 109 / warn 0 / fail 2 / total 111） |
| FACTOR_LAB | ❌ fail，age 550min，更新于今日 11:27，超阈值 360min → **盘后 E 批 21:00 前刷新转绿，已知非故障，勿误报** |
| maharo_macro | ❌ fail，age 1661min，更新于昨日 16:56，超通用红线 1440min → **需夜班核对**：是否进夜间算法链（A/B/D/E 批）；若夜间链不产此卡，移交主人跟进口径 |

### 8.5 阿狸咪夜班盯防清单（20:41 移交）

- **P0 核对**：FACTOR_LAB 今晚 E 批（~21:00）是否转绿（更新_time ≥ 今日 21:00）。若 22:00 仍 fail，查 run_algorithms STAGES["E"] conclusion + 时长（≥20min 为真跑，1-2min 为空转）。
- **P1 核对**：maharo_macro 是否为低频/夜间链产物；若夜间链不覆盖，次日盘前由主人决定口径（已修别名，仅陈旧非报错）。
- **P2 观察**：NT_DATA 次日盘前转绿（全量数据页豁免生效）；HERDING_DATA 盘后 actual 刷到。
- **铁律**：阿狸咪夜班「只读不写」——勿改 `index.html`/`HEALTH_CHECK.js`；若发现新红灯需修复，落 `URGENT_小九_*.md` 交小九次日处理，勿自行 push 代码。
- **本机 automation**：小九 19:00 审计推仓 `1785978303212` 已跑；其余本机 ACTIVE 任务按既定运行，勿动。
