# HANDOVER｜小九：撞车处置回执（因子实验室已暂停）

- 时间：2026-09-09 08:35 CST
- 作者：阿狸咪（家里机）
- 触发：主人令「要！不能撞车！」

---

## 一、已处置：`60e1dbfd` 因子实验室 → **PAUSED**（未删除）

| 项 | 内容 |
|---|---|
| 任务名 | 九宝量化-因子实验室数据刷新(小九本机·baostock) |
| 原调度 | 每周六 10:00 |
| 行为 | 本机直跑 `v8/factor_lab_gen.py` → 生成 `data/FACTOR_LAB.js` + `raw_data/factor_lab.json` → 脚本内部原子 commit+push |
| 撞车对象 | 你新建的 **21:05 stage=E** 档（factor_lab_gen 挂在 E 档 D2-A2）<br>→ 同一产物**双跑双推**，且本机直跑**没有任何并发护栏**（workflow 有 concurrency group，直跑脚本没有） |
| 处置 | **PAUSED**（遵循主人铁律：阿狸咪任务禁止删除，只能暂停） |

**后果**：因子实验室现在**唯一**产出路径 = 你每晚 21:05 的 E 档。若 E 档某晚失败，`FACTOR_LAB` 会红灯且本机不再兜底——**请你的 E 档失败自检覆盖它**（你已有 23:50 只读验收 `ba2f027d`，建议把 FACTOR_LAB 纳入验收清单）。

---

## 二、其余我方自动化撞车排查结论（**均无撞车，勿重复处置**）

我逐条核查了本机 27 个自动化里所有会「派发算法链」或「直跑脚本并推送」的任务：

| 任务 | 调度 | 产物 | 护栏 | 结论 |
|---|---|---|---|---|
| `53726469` 盘后最终闸 | 21:30 | 派 `trigger_algo` | ✅ 内容级 FRESH 判定 + **同时查 v8_algo_cloud / v8_algo_run 两条链**，任一 in_progress/queued/waiting/pending 即不派发 | 不撞车（你 21:05 E 档跑 ~1h12m，21:30 会看到在跑→跳过） |
| `80a443a6` 算法链兜底触发 | 19:30 | 派 `trigger_algo` | ✅ 同上双链护栏 | 不撞车（早于你 21:05） |
| `1787141452867` 云端按铃 | 每 30min（8–22 点） | 派 `trigger_algo` | ✅ ①仅 19:15–23:30 按铃 ②**先查回测卡内容新鲜度**（BACKTEST_TDX / BACKTEST_COMPREHENSIVE / INDEX_HISTORY 是否今日）③陈旧才查 run，有 in_progress/queued/waiting/pending/requested → 跳过 | 不撞车，但见下方⚠️ |
| `9ee4a617` 每晚轻量审计 | 23:30 | 只读审计（比对一致性/小文件/db 备份） | 无写入 | 不撞车 |
| `99e0f3c0` 周六T+1兜底 | 周六 09:30 | T+1 数据补抓 | 不同产物 | 不撞车 |
| `b5793508` 周末轻量维护 | 六/日 19:30 | 元数据映射 + 部署 | 与你 E 档错开 1.5h，产物不同 | 不撞车 |
| `27b5306c` 数据新鲜度值守 | 每 30min | 派 `v8_cn_fetch_cloud`(heal) | 数据链，非算法链 | 不撞车 |
| `1788058041833` 自愈监控闭环 | 每 30min | 候选池/动量 + push | 产物与 E 档不同 | 不撞车 |
| `90b7ea6e` 夜班监控 | — | — | **validUntil 已过（2026-09-09 07:00）** | 已自动失效，无需处置 |

### ⚠️ 一个「次优但不致命」的点，供你判断

`1787141452867` 按铃在 **21:00** 那一轮：若当时回测卡陈旧且两条链都空闲，它会派 `trigger_algo`（完整链 A→B→D→E）；**5 分钟后你 21:05 的 E 档再派一条** → 因 `concurrency: v8-algo-cloud, cancel-in-progress: false`，两条会**排队不互踢**，但等于多跑一整轮完整链。

我的建议（**未擅自改，等你或小九定**）：给 `scripts/alimi_bell.py` 的 `_algo_need_bell` 加一条——**20:55–22:30 之间若最近 2 小时内已有任意 algo run（不论状态）则跳过按铃**，把这段窗口完整让给 E 档。

---

## 三、给你的两条确认项

1. **`runs-on` 恢复双跑**：家里机 Git 已装好（`E:\Git`，v2.55.0，Machine+User PATH 已写入，runner pid 12648 已用新 PATH 重启）。请把 `v8_algo_cloud.yml` / `v8_algo_run.yml` 的 `runs-on` 从 `[self-hosted, cn, xiaojiu]` 改回 `[self-hosted, cn]`。
2. **因子实验室兜底归属**：本机已停，FACTOR_LAB 只靠你 E 档。若你希望保留本机周六兜底，请给一个「E 档失败才跑」的条件化方案，否则维持现状。

---

## 四、我方边界声明

本机**不再**有任何针对 `factor_lab_gen.py` 的定时直跑。除上表列出的派发类任务外，其余自动化均为只读监控/交接读取，不产出数据、不推送。
