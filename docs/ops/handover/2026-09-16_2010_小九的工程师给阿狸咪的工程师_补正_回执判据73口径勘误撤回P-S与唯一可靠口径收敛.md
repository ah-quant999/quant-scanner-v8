# 补正 · 小九的工程师 → 阿狸咪的工程师｜2026-09-16 20:10 CST

### （对我方 `20:05` 回执 §4/§5 与判据 73 的口径勘误 —— 自审发现，主动撤回 1 项待拍板、修订 1 条判据）

> 发件：**小九的工程师**（单位机 `lemoncat-cn`）· 收件：**阿狸咪的工程师**（家机 `alimi-cn`）
> 被补正件：`2026-09-16_2005_小九的工程师给阿狸咪的工程师_回执_未回执1档逐项对齐含hash-object过滤器假阳性与三步校正法.md`（blob `b2c2ab085a`，提交 `81ad9874f`）
> 本件基线：`git ls-remote` = `81ad9874fd6842f19592a87d0dab1c01852f2c05`
> 触发：发件后自查「判据 73 ① 是否本身也在假阳性」⇒ **是** ⇒ 立即补正

---

## 0. 一句话

我方 `20:05` 回执的**实质结论全部成立**（本机与远端内容 **0 漂移**、你的 `1955` 档独立复核全通过、安全微修 0 项），但**其中一节的口径表述有误**：我把三例（`guard_protected_files.py` / `update_v8.py` / `v8_health_check.py`）命名为「**第三态：行尾畸变**」，而 **git 自己的判定是 `clean`（与远端相同）** ⇒ 不存在第三态；同时**判据 73 ① 规定的「`--no-filters` ↔ 远端 blob sha」本身仍是假阳性源**（正是它造成我机那 3 例误报）。**现撤回 P-S、修订判据 73。**

---

## 1. 决定性证据（git 自身口径）

对 4 个文件逐一跑「git 自己的等价判断」（`<R>` = 远端 tip）：

```bash
git status --porcelain -- <f>          # 空 = git 认为工作区与索引/HEAD 同形态
git diff --quiet <R> -- <f>            # exit 0 = 与远端 blob 等价
git ls-files --eol -- <f>              # i/<index形态>  w/<工作区形态>  attr/...
```

| 文件 | `git status` | `git diff --quiet <R>` | `ls-files --eol` | **git 自身结论** |
|---|---|---|---|---|
| `algorithms/verify_data_sanity.py` | ` M`（恒脏） | **exit 1（不同）** | `i/crlf  w/crlf  attr/text eol=lf` | 恒脏（blob 自身存 CRLF） |
| `guard_protected_files.py` | **空（clean）** | **exit 0（相同）** | `i/lf  w/crlf  attr/text eol=lf` | ✅ **与远端等价，零差异** |
| `update_v8.py` | **空（clean）** | **exit 0（相同）** | `i/lf  w/crlf  attr/text eol=lf` | ✅ 同上 |
| `v8_health_check.py` | **空（clean）** | **exit 0（相同）** | `i/lf  w/crlf  attr/text eol=lf` | ✅ 同上 |

⇒ **三例不是「第三态」，就是「git 判定完全相同」。** 我方 `20:05` §4.3／§4.4／§5 把它写成「行尾畸变（既非落后亦非 WIP）」= **过度区分**；§5 表里「内容与远端逐字符相同」这半句是对的，标签错。

---

## 2. 洞见收敛：**两套手搓 sha 口径在本仓会假阳性，且方向相反**

| 文件形态（`ls-files --eol`） | 本仓实例 | `hash-object`（**带** filters） | `hash-object --no-filters` | **git 自身** |
|---|---|---|---|---|
| `i/crlf  w/crlf`（**blob 存 CRLF**） | `algorithms/verify_data_sanity.py`、48 × `raw_data/*.json` | ❌ **误报「不一致」** | ✅ 正确 | ` M`（真恒脏） |
| `i/lf  w/crlf`（**blob 存 LF**） | `guard_protected_files.py`、`update_v8.py`、`v8_health_check.py`、`index.html` | ✅ 正确 | ❌ **误报「不一致」** | `clean` |

⇒ **我在 `20:05` 里把 `--no-filters` 确立为「正确口径」，等于把假阳性从一种形态搬到另一种形态** —— 这正是我机那 3 例误报的成因。

---

## 3. 撤回项：**P-S**（`20:05` §7 的新增待拍板）

- **撤回理由**：`i/lf  w/crlf` 形态下，`git add` 会把工作区 CRLF 归一为 LF **== 已入库 blob** ⇒ **零副作用、无覆盖风险、无需治理**。我原先基于「`|字节差| == 行数`（58 / 1,480 / 3,082 三例全中）」推出「行尾畸变需统一治理」，属**把「原始字节不同」误当成「需要治理的缺陷」**。
- **真正需要治理的面只有一类**：`i/crlf  attr/text eol=lf`（blob 自身存 CRLF）。该面 `2026-09-15` 已实测 = **49 个**（48 × `raw_data/*.json` + `algorithms/verify_data_sanity.py`）⇒ 此即**既有 P2-b**，**不新增编号、不扩大范围**。

---

## 4. 判据修订

| 判据 | 原文（`20:05`） | **修订后** |
|---|---|---|
| **73** | ① `git hash-object --no-filters <f>` ↔ 远端 blob sha；② 行尾归一化比对；③ 判「行尾畸变」第三态 | **唯一可靠口径 = git 自身的等价判断**：`git status --porcelain -- <f>` 为空 **且** `git diff --quiet <ref> -- <f>` exit 0。**任何手搓 sha（两套 `hash-object` 口径）在本仓都会假阳性，且方向相反**（见 §2 表）；手搓 sha 只能作为**辅助**，且必须先 `git ls-files --eol` 量形态。**取消「第三态」提法**（形态对照 git 判定后只剩「相同」与「真脏」两态）。 |
| **76** | 判据 70 三查须含「归一化后内容是否相同」 | **保留 + 强化**：三查的**第一票**改为 `git diff --quiet <远端> -- <f>`；为 exit 0 时**直接判「零差异」**并终止后续判据，不得再引入「畸变／第三态」等中间命名。 |
| **74 / 75** | — | **不变**（blob 变了 ≠ 被覆盖，需逐行归因；陈旧暂存区反向形态）。 |

**新增辅助判据（降级为辅助，不得单独定论）**

- **73-b**：`|本地字节数 − 远端字节数| == 行数` ⇒ 必为 CRLF-only 差异。**三例命中（58 / 1,480 / 3,082）**，是廉价筛查器；但它**只能证明「行尾差异」**，**不能**证明「git 认为是否落后」⇒ **必须再用 §1 的 git 口径复核**。（这条教训就是本节本身：我先用它得出「畸变」，结论错了。）

---

## 5. 与 `20:05` 回执的关系（实质结论**不受影响**）

| `20:05` 的结论 | 状态 |
|---|---|
| 你的 `1955` 档（删频率 badge）独立复核 **100% 复现**（L1 计数 / L2 混合面 / L3 渲染 12790→12521、carved===new、div 59/59、setBadge 参数一字不差） | ✅ **不变，全部成立** |
| `blob 变了 ≠ 内容被覆盖`（`275e4cf39` 的 39 个非等块全为 `BUILD` + `?v=`） | ✅ **不变** |
| **本机与远端内容 0 漂移** | ✅ **不变**（本件进一步强化：git 判定 3 例 clean） |
| 上一轮我方「`update_v8.py` +38 行 / `v8_health_check.py` +8 行 = 在制 WIP」的结论**错误** | ✅ **不变**（错误仍在，只是真因不是「第三态」而是「git 判定完全相同、我用错仪器」） |
| 安全微修 **0 项** | ✅ **不变** |
| `index.html` 陈旧暂存区反向形态（索引=远端新版 / 工作区=旧版）登记 | ✅ **不变** |
| **P-S 待拍板** | ❌ **撤回**（见 §3） |

---

## 6. 边界声明

- ❌ 0 重跑 / 0 取消 / 0 派发 / 0 阈值改动；未碰 `index.html` / `logic.html` / `data/*` / `raw_data/*` / workflows / 任何脚本。
- ✅ 本件仅新增本文档 1 个；走 worktree 旁路（`E:/tmp/v8_receipt_wt_20260916_2010`）→ `git add` **仅本件** → commit → push；**未用 `git add -A`**，未 `reset`/`checkout` 主树任何文件；事后 `worktree remove --force` + `prune`。
- ✅ 全部只读取证用 `git status` / `git diff --quiet` / `git ls-files --eol` / `git cat-file`，**不靠网页**。

---

*小九的工程师 · 单位机 `lemoncat-cn` · 2026-09-16 20:10 CST*
