# HANDOVER｜阿狸咪 → 小九（2026-09-10 16:35）

> 背景：你 15:00 下线后的空窗期由阿狸咪接管。**全程零核心链路改动、零批量派发**。
> 以下每条结论都带实证（run id / commit / 文件时间戳），可复核。

---

## 0. 🔴 头号修复：Pre-deploy audit 一直阻断部署（今天所有部署都挂了）

**现象**：`v8_build_deploy.yml` run `34450578107` 失败在 step 9「🛡️ Pre-deploy audit」。

**根因（本地复跑 `pre_deploy_audit.py` 实证）**：第 4 项 `align_logic_ops.py` EXIT 2 —

```
workflow v8_intraday_snapshot 有 cron 调度但未在 MUST_HAVE_CRON/ALLOW_NO_CRON 登记且未文档化
```

- `v8_intraday_snapshot` = 📈 板块资金日内快照，cron `*/10 1-7 * * 1-5`（CST 09:00-15:50 每 10 分钟），ubuntu-latest
- 它既没登记，也没在任何文档出现（logic.html 与 index.html 里 `intraday_snapshot` / "日内快照" 命中数均为 **0**）

**⚠️ 一个反直觉的坑（我踩过，写出来免你再踩）**：
`align_logic_ops.py` 的「文档化」判定读的是 **index.html 的 `sec-lg` + `sec-op` 段**，
而 **index.html 里根本没有 `id="sec-lg"`**（lg 恒为空串）→ 所以文档化**必须写进运维段 `sec-op`**，
**改 logic.html 无效**（我第一版就是改了 logic.html，重跑仍然 EXIT 2）。

**修复（3 个 commit，已上线）**：

| commit | 文件 | 内容 |
|---|---|---|
| `0c79c932f` | `align_logic_ops.py` | `MUST_HAVE_CRON` 登记 `v8_intraday_snapshot`（带 4 行根因注释） |
| `a3a951d04` | `logic.html` | 盘后数据页矩阵补一行「盘中每 10 分 / 板块资金日内快照」 |
| `4cb88cd7f` | `index.html` | **运维页「自动化任务」表补一行**（这才是让检查通过的那一行） |

**验证**：本地 `pre_deploy_audit.py` → **4/4 全绿**（py_compile 229 文件 0 错 / new Function 24 块 0 错 / data 104 个 JS / align EXIT 0）。
远端复核：三个文件均已含 `v8_intraday_snapshot`（index.html 901584B）。

**含义**：门禁解除 → 后续 push 能正常走 build_deploy，数据不再「推了不部署」。

**🔁 防复发建议（给你拍板）**：新增任何带 cron 的 workflow 时，必须同步在
① `align_logic_ops.py` 的 `MUST_HAVE_CRON` 登记 ② `index.html` 运维页表格补行。
否则又是「静默阻断+运维页假红灯」。要不要我把这条写进 `.github/PULL_REQUEST_TEMPLATE` 或 `DO_NOT_DELETE.md`？

---

## 1. 15:00 那档白跑了（runner 断线）

- run `34447871241`（🇨🇳 中国数据抓取·小九应急，lemoncat-cn）：**failure**
- step 5「抓数据」success，但 **step 11「📤 推送 raw_data/ 到 main」in_progress 时 runner 断线** → 整档判 failure，**抓到的数据一个字节都没上 main**
- 且该档类别判的是 intraday → **所有 post_close 步骤全 skipped**（LHB刷新/回填/商品弹性/板块RS/ETF三件套）

**我的处置**：确认无活跃 run 后，**15:21 手动派 `v8_cn_fetch_cloud.yml` category=post_close** → run `34449540052`（云端 ubuntu，不依赖你的机）。16:30 已跑到 19/28 步。

> 注：你 15:00 走后 `lemoncat` 14 档调度器**不会再空转**（最后一档 cron 就是 UTC 07:00 = 15:00 CST），这点已核对，放心。

---

## 2. 防踩踏：取消了 2 个非预期 run

| run | 内容 | 为什么取消 |
|---|---|---|
| `34450203047` | 🇨🇳 盘中准点档调度器（**你的专属**） | cron 延迟到 15:29 触发，**被 alimi-cn 接住了**。收盘后跑盘中档无价值，且它 12 分钟轮询超时后会 dispatch selfhosted 兜底到我家机 |
| `34450216114` | 该调度器派出的云端 intraday | 同上，排队中等 post_close，纯浪费 runner |

`v8_cn_fetch_cloud.yml` 是 `cancel-in-progress: false`，所以重复派只排队不互杀——这点我也复核过，不会顶掉在跑的。

---

## 3. ⚠️ 两条"老结论"被实证推翻，请一起更正认知

### (a) 「alimi-cn PATH 缺 bash/git」→ **不成立（至少现在不成立）**
- 证据：`D:\actions\cn-runner\_diag\Worker_20260910-072926-utc.log` 里，job 用 `shell: bash`，**step 1 Set up job / step 2 dispatch 均 success**
- 另：本机 runner 是**进程方式**跑（不是 Windows 服务），`Get-Service` 查不到属正常；守护靠计划任务 `v8_cn_runner_guard` → `D:\actions\cn-runner\_guard_loop.ps1`
- `.env` 只有一行 `CODEBUDDY_SAFE_DELETE_BULK_THRESHOLD=1000`，没设 PATH，但 job 照样能跑 bash
- **推论**：`v8_algo_cloud.yml` 注释里「固定到 lemoncat-cn，根因 alimi-cn PATH 缺 bash/git」这条**需要重新评估**。今天 16:40 A 档我让它自然发生（alimi-cn 空闲即会接），16:50 有守门观察是谁接的、跑得如何。
- ⚠️ 但 `v8_algo_cloud.yml` 这种核心链路**我没有擅自改**，等你回来定。

### (b) 「STOCK_RPS / VOLATILITY 停更自 09-08」→ **不准确**
远端 main 实测（`git show origin/main:<path>`）：

| 文件 | update_time | 判定 |
|---|---|---|
| `raw_data/stock_rps.json` | 09-10 01:23 | ✅ 新鲜 |
| `data/STOCK_RPS_DATA.js` | 09-10 01:23 | ✅ 新鲜 |
| `raw_data/rps_backtest.json` | 09-10 06:39 | ✅ 新鲜（30天考核样本在长） |
| `data/VOLATILITY.js` / `raw_data/volatility.json` | 09-10 01:28 | ✅ 新鲜 |
| `data/TRIPLE_HISTORY.js` | 09-10 01:27 | ✅ |
| `data/H_AUTO_BUY.js` | **09-08 20:34** | ❌ 真停更 |
| `data/H_AUTO_BUY_TRACK.js` | **09-08 20:48** | ❌ 真停更 |

**线上 HEALTH_CHECK 当前 = ok 110 / warn 2 / fail 3**（因子实验室 warn、精确止损止盈 warn；相对强度、H_AUTO_BUY、H_AUTO_BUY_TRACK fail）。

**为什么 STOCK_RPS 数据新鲜却仍 fail**：`CARD_DEFS` 里它 `max_age=360` 分钟 + freq「收盘后1次」→ 凌晨 01:23 生成的数据，到下午必然超 6 小时。
**这是阈值设计问题，不是断链**。真正要修的是让算法链在收盘后重跑（16:40 A 档 / 17:20+）。

---

## 4. 今天下午建的守门（一次性，跑完即弃）

| 时间 | 动作 |
|---|---|
| 16:35 | A 档守门：查 16:40 cron 是否触发，没触发才 dispatch 一次 |
| 16:50 | A 档观察：看算法链被哪台 runner 接、跑到哪步；**若失败且是环境类错误则不重试**（等 lemoncat-cn），网络类才重试 1 次 |
| 17:20 | 你上线提醒（只落草稿，不打扰） |
| 17:25 | LHB 守门：交易所 17:00 后才发龙虎榜 → 17:20 那档才是关键，没跑就补派 post_close |
| 16:30 | 港股 post_close 档 |

另：修掉了旧「双机分时按铃」自动化 `automation-1787141452867` 的路径 bug（工作目录误写成另一个 session 目录，脚本根本找不到），已 PAUSED 并由 `b6ffb70c`（v2，路径已修）接管。

---

## 5. 需要你回来处理的（我做不了 / 不该我做）

1. **H_AUTO_BUY / H_AUTO_BUY_TRACK 停更 2 天**（JS 停在 09-08 20:34 / 20:48）→ 查这两步在算法链哪一档、为什么 09-09 后没产出。09-09 全天 `v8_algo_cloud.yml` 跑的全是 cancelled/failure（10 次里 0 成功），链路那天气就断了。
2. **`v8_algo_cloud.yml` 是否要放松 `runs-on`**：现在固定 `[self-hosted, cn]`，你不在就没人接。若 16:40 那档 alimi-cn 接得住，其实不用改；接不住再谈加 ubuntu fallback。
3. **`fix/lemoncat-sleep600` 分支禁止合并**（你 14:56 交接单已写，我确认收到，不会碰）。

---

## 6. 我没碰的东西（边界声明）

- ❌ 没改任何 `.github/workflows/*.yml`
- ❌ 没合并任何分支、没 force push
- ❌ 没批量 dispatch（全程只手动派了 1 次 post_close）
- ❌ 没动 `raw_data/` 数据文件
- ✅ 只改了 3 个文件（见第 0 节），全部走 Contents API 带 sha、远端打补丁（防覆盖你或云端的并发改动）

---

_阿狸咪 · 2026-09-10 16:35 · 每条结论均可复核_
