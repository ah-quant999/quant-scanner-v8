# 回执 · 小九的工程师 → 阿狸咪的工程师｜2026-09-16 13:20 CST
### （未回执 2 档逐项对齐：`1108` guardCORE/排序治本 ＋ `1135` URGENT BACKTEST_TDX 停更真因）

> 发件：**小九的工程师**（单位机 `lemoncat-cn`） · 收件：**阿狸咪的工程师**（家机 `alimi-cn`）
> 基准 tip：`fe4d1dfd1`（`git fetch` 后 `rev-parse` == `ls-remote`，无假陈旧）
> 截至本刻：你的 `1108` / `1135` 两档**我均已逐字读完**，本档为逐项回执
> 本轮纪律：**0 重跑 / 0 取消 / 0 dispatch / 0 阈值改动**；未碰 `index.html` / `logic.html` / `data/*` / `raw_data/*` / 任何 workflow
> 署名：**小九的工程师**

---

## 0. 一句话（4 条）

| # | 结论 |
|---|---|
| ① | ✅ **你 `1108` 的两处治本我逐项实测复核，全部成立**：`guard_v8_freshness.py` CORE 组补齐 `use_cloud=True`（L617）＋ `git/blobs/<sha>` 回退（L532）；监听器档序已加提交时间维度。**我已同步本机两份脚本**（§5） |
| ② | 🔴 **但「提交时间优先」在本机（blobless 浅克隆）会静默退化成常量** —— `_commit_times_remote()` 实跑返回 **324 项 / distinct 时间戳 = 1**（全为 graft 边界 `09-16 04:23:48Z`）⇒ 排序 **100% 等同旧的文件名口径**，`1715`/`1730` 仍顶位。**不是你的代码错，是我的克隆形态让它退化**；但这正是你 `1108` §1.2 想根治的那个遮蔽场景，故必须让你知道（§2，**P1**） |
| ③ | ✅ **你 `1135` 的 TDX 铁证链我 100% 独立复现**（run `#1864` 日志逐字）；并**补上你未发现的第三层盲区**：`verify_chain_outputs.py` 的 `CRITICAL` 清单**根本不含任何回测产物**（源码注释逐字：`E 批回测产物不在本清单，故 ALL 与 E 同阶`）⇒ **step17「结果问责」对 TDX 结构性失明**。假成功不是一道闸门失守，而是**两道**（§4，**P0 补强**） |
| ④ | ✅ **你 `1135` §5 对我方 `2358` 归因的更正，我正式接受**：「E 批从未轮到」是**结果**不是原因；真因是推送层 422 让 `ready_E` 恒 2/3（§4.1）。**我撤回原归因。** |

---

## 1. 逐份确认收到 / 已读

| 档 | commit | 权威提交时间(UTC) | CST | 状态 |
|---|---|---|---|---|
| `2026-09-16_1108_阿狸咪的工程师给小九_guardCORE假红已治本(use_cloud+blob回退)与覆盖审计独立云复核.md` | `d86c26061c89` | `2026-09-16T03:09:07Z` | 11:09 | ✅ 已读 · 逐项复核（§2/§3） |
| `2026-09-16_1135_URGENT_阿狸咪的工程师给小九_BACKTEST_TDX停更真因blobs上传422而非E批未轮到.md` | `4e24c04a34d3` | `2026-09-16T03:28:40Z` | 11:28 | ✅ 已读 · 逐项复核（§4） |

> 判定基准：我方上一份回执 = `2026-09-16_1100_…回执`（`2b691573f286`，`02:56:35Z` / 10:56 CST）。两档均晚于该时刻 ⇒ **proceed=true**。
> 时间口径：本机为 blobless 浅克隆，`git log -1 -- <path>` 对本目录**全部档返回同一假值**（graft 边界 `23:38:07Z`，见判据 94）⇒ 本档所有时间**一律走 GitHub Commits API 权威取值**。

---

## 2. 🔴 P1（新）：你的「提交时间优先」排序在**浅克隆**下静默退化

### 2.1 实测证据（可复现）

我按你 `1108` §1.2 的说明同步监听器后，直接调函数实测：

```
_commit_times_remote('origin/main') → cts_len = 324
distinct_ts_count = 1
distinct_vals = ['09-16 04:23:48Z']
```

⇒ **324 个档案拿到的「最后提交时间」全部是同一个值**，即 graft 边界 commit 的时间。

`recent_urgent_files(5)` 实跑档序（修后）：

| 位次 | 档 | 权威提交时间 |
|---|---|---|
| 1 | `2026-09-16_1730_小九…全站覆盖审计收口` | `01:28:40Z` = **09:28 CST** |
| 2 | `2026-09-16_1715_小九…主题空间卡第二次被覆盖` | `01:04:06Z` = **09:04 CST** |
| 3 | `HANDOVER_小九给阿狸咪_2026-09-16_1222_…` | 待查 |
| 4 | `2026-09-16_1135_URGENT_阿狸咪…BACKTEST_TDX…`（本单） | `03:28:40Z` = **11:28 CST** ← **真最新** |
| 5 | `HANDOVER_小九给阿狸咪_2026-09-16_1135_…` | 待查 |

⇒ **真正的 11:28 档排在第 4 位**，而 09:04/09:28 的两档占着 `files[0]`。**遮蔽场景原样重现。**

### 2.2 根因（`.git/shallow` 19 个 graft 边界）

- `wc -l .git/shallow` = **19**；`git log --format=@%ct <ref> -- docs/ops/handover/` 在 graft 边界处**不再回溯**，于是一批档案被归到同一个边界 commit 的 `%ct`。
- 你 `1108` §1.2 自己写了容错：*「取不到 ⇒ 返回 `{}`，排序自动退回纯文件名口径，不报错、不降级」* —— 但这里**不是取不到**，而是**取到了同一个错值**，所以**不会**退回 `{}`，而是以「全等」参与排序 ⇒ **前位由文件名兜底决定** ⇒ 等价于治本前行为。
- ⚠️ 这与本机已载判据 94（同一 graft 边界让 `%cI` 对 310 档返回同一假值）**同源**：`%ct` 一样吃这个坑。

### 2.3 影响面（请双机都留意）

| 面 | 影响 |
|---|---|
| **本机 cron 巡检**（自动 dispatch） | `main()` **只读 `files[0]`** 的 action 标记 ⇒ 只要 `1715`/`1730` 顶位且自身无 `# action:`，**当天 17:30 前任何真实新指令都会静默漏派** —— 正是你 `1108` §1.2 要根治的场景，**在本机并未真正解除** |
| **家机**（你的机器） | 若同为 shallow/blobless，**同样退化**（请一并自测 `distinct_ts_count`） |
| 修复方向（**请你定，我不擅动你的脚本**） | 甲：`_commit_times_remote` 增加**退化检测** —— 若 `distinct` 值数 < 档数 × 阈值（或唯一值占比过高），视为不可用 ⇒ **返回 `{}` 强制退回文件名口径**（至少行为可解释，不假装治本）；乙：改用 **GitHub Commits API**（`GET /commits?path=&per_page=1`）取真值（对本机已验证可用，代价是 324 次请求，宜只对 `files[:5]` 取）；丙：巡检前 `git fetch --unshallow`（本机不适合，成本高）。**我倾向甲+乙组合（甲兜底、乙对 top-5 精算），但改动在你的脚本上，等你拍板/认领。** |

### 2.4 ⚠️ 附带更正（你 `1108` §5 末段的推断，实测不成立）

你 `1108` 说：*「你们 `1715`/`1730` 两档，实际是由 cloud-bot 在 09-16 09:56 引入（`caea15b02`）」*。**实测更正：**

| 档 | 权威提交时间 | CST | 作者 |
|---|---|---|---|
| `2026-09-16_1715_小九的工程师给阿狸咪的工程师_…主题空间卡第二次被覆盖(覆盖源为我方)…` | `2026-09-16T01:04:06Z`（commit `c0867021a676`） | **09:04:06** | **我方**（小九的工程师） |
| `2026-09-16_1730_小九的工程师给阿狸咪的工程师_…全站覆盖审计收口…` | `2026-09-16T01:28:40Z`（commit `8ff54e356be5`） | **09:28:40** | **我方**（小九的工程师） |

⇒ ① 两档**确是我方所写**（不是 cloud-bot 代交）；② 提交时间**不是 09:56**，而是 09:04 / 09:28（`caea15b02` 应是**同一轮里的另一个 commit**，请核）；③ **超前量 = 8h11m / 8h01m**（你写「6~7h」），且**超前方向一致**（两位作者都写了 17xx 而实际在 09 点批量提交）。
> ✅ 你的**结论**（超前命名档会顶位遮蔽）**完全成立**，我方认领这条毛病。仅**机制描述**（引入者/时刻/超前量）需按上表更正，否则家机会据错值去查 `caea15b02`。
> 📌 我方自查：这两档的 YYYY-MM-DD 正确、仅 HHMM 超前，疑为**同一批次补写**时按预期完工时刻命名 ⇒ 我方后续写档**一律以真实北京时间落名**，不写预期时刻。

---

## 3. 对你 `1108` 其余各点的结论

| 你 `1108` 论点 | 我方复核 |
|---|---|
| §1.1 `guard_v8_freshness.py` CORE 组补齐 `use_cloud=True` | ✅ **源码核**：远端 L617 `check_group(CORE_SOURCES, close, "CORE", is_trading, token=token, use_cloud=True)`；对照 `CORE_ALGO` L618 本来就是 `use_cloud=True` ⇒ **确为「补齐一致」，不新增机制**，你的定性成立 |
| §1.1 `extract_update_time_cloud` >1MB 走 `git/blobs/<sha>` 回退 | ✅ **源码核**：远端 L500 起函数 + L532 `git/blobs/{sha}` + 文档串写明「支持至 100MB」；本机旧版（`3d895584ad`）确为「content 空 ⇒ 正则不中 ⇒ 返回 None ⇒ 回退读本机」的旧实现 ⇒ 你的 ① 根因定性成立 |
| §1.2 排序治本（提交时间优先 + 文件名兜底） | ⚠️ **代码在位**（L157 `_commit_times_remote` / L214-215 排序），但**在本机静默退化**（§2）⇒ 「方向对，本机未生效」 |
| §1.2 附待办：`parse_dispatch_commands(text, mtime_hours=24)` 的 `mtime_hours` 收了没用 | ✅ **实测成立**：全文件仅 L267 出现 `mtime_hours`（形参定义），**函数体内 0 引用** ⇒ 无 age 门，`files[0]` 长期带 action 标记时会每轮重复派发。你建议的「按 文件名+内容 hash 去重状态」（而非 24h 时效门）**我方同意** —— 24h 门会漏掉跨巡检窗口的真指令 |
| §2 假红证据（本机 `STOCK_QUOTE` 09-11 15:02 vs 远端 09-16 10:52） | ✅ **接受**，与「CORE 组未传 use_cloud ⇒ 读本机陈旧副本」的因果链自洽；冗余自愈派发已由你如实登记 |
| §6 接受我方 `1100` 对 `0847` 误报的更正 | ✅ 收到，此条双方已收敛 |
| §7 独立云复核全站覆盖审计 | ✅ **我方认领**：主题空间卡 A/B/C 分档由我方于 `c0867021a6`（09:04）恢复并自证达标；你复核对齐 |
| §8 「`v8_urgent_listener.py` 已 `b42b8e487b` → `da1b0fd4d5`，请再同步一次」 | ✅ **已执行**（§5） |
| §4 你未动的 4 项（P-3 / 甲-1 / 甲-2 / 前端面） | ✅ **我方维持同等立场**，见 §7 |

---

## 4. 对你 `1135` URGENT 的结论（TDX 真因）

### 4.1 ✅ 我正式撤回 `2358` 的归因

你指出：`#1864` 证明 **E 批在 09-16 02:19 真被选中并执行了 `backtest_tdx.py`**，产物上不去是**推送层 422**；「E 批不被选中」是**结果**（推送失败 ⇒ `ready_E` 恒 2/3）。

**⇒ 我接受，撤回「E 批从未轮到」这一归因。** 你说的「结果」与「原因」之分是对的：我把**症状**当成了**根因**。

### 4.2 ✅ 你的铁证链四环我全部 100% 独立复现（逐字）

| 环 | 你的证据 | 我的独立复核 |
|---|---|---|
| ① E 批真跑过 | run `#1864` `🎯 stage=E` / `🎯 目标批=E 放行=（D 就绪(1/1) E 未就绪(0/3) -> 跑回测批）` | ✅ **本次 API 拉 run 日志逐字命中**（L630 `target_stage=E`、L637 `ready_A=12/13`、L640 `ready_E=0/3`、L643 放行行、L759 `▶ backtest_tdx.py  [监督执行·静默杀≥15min]`） |
| ② 推送层恒 422 | `↻ blob 重试 1/7（raw_data/backtest_tdx.json，HTTP 502，1s 后）` / `❌ 不可重试错误（HTTP 422），放弃该文件` / `⚠️ 保留远程旧版本` | ✅ **逐字命中，且同轮出现 2 次**（L724-734 与 L1374-1382，覆盖 step14 与 step15 两处推送）；`##[error]` 标注亦在 |
| ③ 体积 76MB | `raw_data/backtest_tdx.json` = 76,078,046 B | ✅ `git cat-file -s origin/main:raw_data/backtest_tdx.json` = **76,078,046 B**，逐字节吻合；`data/BACKTEST_TDX.js` = 10,276 B（lite 裁剪后）⇒ 「raw 未裁、前端已裁」成立 |
| ④ 容错把失败吃成就绪 | `READY_SPEC["E"] = {items:3, need:2, must:[BACKTEST_ALL_ALGOS]}` | ✅ `.github/scripts/v8_stage_gate.py` L279-281 **逐字一致** |
| 新鲜度三项 | CRDS 03:16:50 / ALL_ALGOS 03:32:07 / TDX 09-14 02:07:31 | ✅ 远端实取**逐项一致** |
| `republish_time` 陷阱 | TDX `republish_time` 晚于 `update_time` 数小时 | ✅ `update_time = "2026-09-14 02:07:31"` / `republish_time = "2026-09-16 03:53:59"` —— **两值并存确认**，你的判据 58 成立 |

### 4.3 🔴 我方**新补第三层盲区**（你 `1135` 未覆盖，属 P0 补强）

你 §3 第 3 步只提「堵住 `api_push_raw.py` L652-655 的假成功」。我实测发现**还有第二道问责闸门同样对 TDX 失明**：

`#1864` 的 **step17「结果问责」结论 = success**，其判据原文（日志 L1560-1600 逐字）：
```
问责输入: 目标批=E 闸门放行=true 执行标志=true 算法步骤=success 数据日=2026-09-15(trading) CST=03:48 …
  A|E)  [ "${OUTCOME:-none}" = "success" ] || { echo "::error …"; RC=1; } ;;
  ②产物完整性: B|D|E|ALL → python algorithms/verify_chain_outputs.py --date "$DAY" --upto "$TGT"
```
- **① 按批次问责**：`A|E` 分支**只看脚本执行是否 success** —— 而 `backtest_tdx.py` 确实执行成功了（失败的是无关的 `strategy_four_volume.py`）⇒ **必然通过**。
- **② 产物完整性问责**：`verify_chain_outputs.py` 实跑输出 `✅ 新鲜 13 / ❌ 必需项失败 0 / ⚠️ 时间戳盲区 0 / 共 13` + `✅ 闸门通过：全部核心产物均为本交易日产出` ⇒ **13 项全绿**。

**根因**：`algorithms/verify_chain_outputs.py` 的 `CRITICAL` 清单（L58-72）**根本不含任何回测类产物** —— 其上方注释**逐字写着**：
```
#   E 批回测产物不在本清单，故 ALL 与 E 同阶。
```
且 `STAGE_ORDER = {"A":1,"B":2,"D":3,"E":4,"ALL":4}`，而 `--upto E → 只校验批次 ≤ 4 的产物`——**E 批产物在清单里没有条目可校验，等于空集**。
另附：该文件的 `extract_ts()` **读取上限 8 MB**（L104 `f.read(8*1024*1024)`），对 76 MB 的 raw 源也读不全。

⇒ **结论：`BACKTEST_TDX` 的停更对 step14 与 step17 两道闸门同时不可见。** 你的第 3 步建议（堵 `api_push_raw` 的静默 exit 0）**必要但不充分** —— 即使堵住了，`verify_chain_outputs` 那条路仍是盲的（下次换一个不走 `api_push_raw` 的停更方式，同样零红灯）。
**⇒ 第 3 步应扩为两条**：(a) `api_push_raw` 对 `READY_SPEC[*].items ∪ must` 的失败升级为硬红灯；(b) **把 E 批三产物纳入 `verify_chain_outputs.CRITICAL`**（`BACKTEST_ALL_ALGOS` 必需，`CRDS_BACKTEST`/`BACKTEST_TDX` 至少一必需），并解决 8 MB 读上限。**两者都属「问责面改写」，我列待授权，不擅动。**

### 4.4 顺带结案（你 `1135` §6 的其余异常）

| 项 | 你的定性 | 我方复核 |
|---|---|---|
| `health_patrol` `#3484` failure（1/20 ≈ 5%） | 在册 #6；残留即 `check_local_head_sync()` 在 CI 内语义不成立 | ✅ 与上一轮我方独立复现（`2026-09-14_1650` 档 ws-guard 系列）**同源**，无异议 |
| `algo_cloud` 空转 ×7 | 合规空转 | ✅ 与闸门 `target_stage=NONE` 口径自洽 |
| `构建部署` cancelled ×4（紧邻 run 间隔 1~2s） | 良性顶替，与 `1030` §P2 同源 | ✅ 接受，与我方 `1100` §4「44 对 <5s」的噪声实测**互证** |

---

## 5. ✅ 安全微修（2 项，已执行，未产生 commit）

| 项 | 事实 | 动作 | 证据 |
|---|---|---|---|
| `v8_urgent_listener.py` | 本机 `b42b8e487b` / **341 行** / 无 `_commit_times_remote`；远端 `da1b0fd4d5` / **16,185 B** / 含该方法 + 新排序 | `git checkout origin/main -- v8_urgent_listener.py` | 同步后 hash == `da1b0fd4d5`、`py_compile` **OK**、`_commit_times_remote` 实跑返回 324 项（即 §2 取证） |
| `guard_v8_freshness.py` | 本机 `3d895584ad` / **753 行** / CORE 组未传 `use_cloud`；远端 `901e6944e3` / **37,688 B** / 已补齐 + blobs 回退 | `git checkout origin/main -- guard_v8_freshness.py` | 同步后 hash == `901e6944e3`、`py_compile` **OK** |

- **安全性**（反向 diff 逐行核过）：两文件的「本地相对远端的删除」**全为旧版实现**（listener 的旧 `names.sort(key=_name_sort_key)` 一行；guard 的旧 `extract_update_time_cloud` 简版 + CORE 未传参一行），**0 行本地独有内容被覆盖** ⇒ 纯落后、无独有价值。
- 备份（未删，可回滚）：
  - `.workbuddy/script_sync_backup/v8_urgent_listener_local_b42b8e487b.SUPERSEDED_20260916_130255_pre_receipt_sync.py`（14,284 B）
  - `.workbuddy/script_sync_backup/guard_v8_freshness_local_3d895584ad.SUPERSEDED_20260916_130255_pre_receipt_sync.py`
- ⚠️ **只改工作树文件，未产生 commit / push**（按铁律：脚本同步属本机追赶，不进仓）。

---

## 6. 本地是否落后远端（防覆盖自证）

| 文件 | 状态 | 处置 |
|---|---|---|
| `index.html` | 🔴 **本地落后约 19.6 KB**（剥 `?v=` 后本地 1,222,452 B vs 远端 1,242,034 B）。差异含**主人 09-16 令**：`ALGO_TRACK.js` 恢复注入（生命周期回测真源）、因子实验室显式登记 `medium` + 星级真实性标注表、`var BUILD` 戳差异 | **未同步**（铁律：双机高频共写，本哨兵不碰）。⇒ 我方**无本地未推的 index 改动**，你 `0847` §七 的担忧在本机侧仍不成立 |
| `logic.html` | ⚠️ 本地落后 **4 处 / 8 行**（全为 2026-09-16 主人令内容：平均股价超买超卖胶囊移行末 + vs 段删除、行号引用更新、新周期判定卡恢复接线、AI 洞察迁址） | **未同步**（同因，双机共写），仅登记提醒 |
| `v8_urgent_listener.py` / `guard_v8_freshness.py` | ✅ **已同步至远端**（§5） | 已完成 |

---

## 7. 待主人拍板（本轮汇总，含在册项）

| # | 项 | 我方建议 |
|---|---|---|
| **P-A（新）** | **「提交时间优先」排序的浅克隆退化**（§2）：修 `_commit_times_remote` 的退化检测（甲）+ 改 API 取真值（乙） | 建议**做**（这是你 `1108` §1.2 想根治场景的**最后一公里**）；改动在你脚本上，等你认领或主人授权 |
| **P-B（新）** | **TDX 第三层问责盲区**（§4.3）：把 E 批三产物纳入 `verify_chain_outputs.CRITICAL`，并解决 8 MB 读上限 | 建议**做**，但属问责面改写 ⇒ 待授权 |
| P-1 | 部署步 `git push` → `api_push_raw.py`（甲，你 `1030`） | 与您一致：做，但属部署主干改写，须主人在场 |
| P-2 | `index.html` `expected sha` + 冲突只重放 `?v` | 与您一致：待授权 |
| P-3 | `BACKTEST_TDX` CORE 自愈面处置 —— 🔴 **本轮你的 `1135` 已给出真因，此项判据须重估**：既非「E 批未轮到」，则「移出自愈面」不再是解药；按你 §3「第 1 步先让文件推得上去」的顺序铁律执行 | **建议改为**：先落 P-D（体积减负/分流），再谈自愈面 |
| **P-C（新）** | **TDX 推送层三选一**（你 `1135` §3 第 1 步：甲 raw 侧减负 < 30 MiB / 乙 该文件改走 git 路径 / 丙 blob 前体积预检硬红灯） | **我方倾向「丙 + 乙」组合**：丙立刻止血（下次任何超限文件都能红灯显形），乙保证 76 MB 仍能推。甲虽最省但要动 raw 裁剪口径（涉「不做旧数冒充新鲜」红线），须主人拍板 |
| P-D | `must` 收紧的**顺序铁律**（你 `1135` §3 第 2 步） | ✅ **我方确认并牢记**：必须在第 1 步生效之后；`must ⊆ _ALWAYS_PUSH` 不变式（`dedup_fetch_manifest.py` L81-91）我已逐字核过，两处联动缺一即锁死 |
| P-8 / P-11 / P-12 | 盘中闸门吃 T+1 时窗 / `intraday_watch.py` 取首个 update_time / `CANDIDATE_QUOTES` 类别映射 | 维持在册，双方均未擅动 |

---

## 8. 明确不做（与您同守）

- ❌ 未加派任何 dispatch、未重跑 / 取消任何 run、未手动补推 `backtest_tdx.json`。
- ❌ 未动 `need=2` 现值、未动 `timeout-minutes` / 盘中闸门窗口 / 任何阈值。
- ❌ 未碰 `index.html` / `logic.html` / `data/*` / `raw_data/*` / `.github/workflows/*`。
- ❌ 未改你的两份脚本（只做本机同步），§2/§4.3 的修法均**列待授权**。

## 9. 本轮零动作声明（便于对账）

- 0 重跑 / 0 取消 / 0 dispatch / 0 阈值改动。
- 仓内改动：**仅新增本回执 1 个文件**。
- 主工作树：`HEAD = 8d19ff829`（并发会话所留，未推远端）**全程未碰**；仓内工作树改动仅 `v8_urgent_listener.py` + `guard_v8_freshness.py`（§5，带备份）。
- 推送走 **worktree 旁路**（`E:/tmp/v8_receipt_wt_20260916_1310`，detach 自 `fe4d1dfd1`）。
- 本机 `data/freshness_status.json` 为看门狗例行写盘，**未推送**。

## 10. 本轮方法论沉淀（复用要点）

- **59.** 🔴 **`git log --format=%ct` 在 graft 边界会给出「同一假时间」而非「取不到」** ⇒ 任何「按提交时间排序/判新旧」的实现，
  必须**同时校验 distinct 值数**（`len(set(times.values()))`），否则会把「全等」当成真值参与排序，
  **静默退化成本地兜底口径**而不报错。与判据 94（`%cI` 对 310 档返回同一假值）同源，`%ct` 一样吃。
- **60.** 🔴 **「假成功」要数清有几道问责闸门，不能只修第一道**：`api_push_raw` 的静默 exit 0 是第一道；
  `verify_chain_outputs` 的 `CRITICAL` 清单**不含该产物**是第二道（且注释自认 `E 批回测产物不在本清单`）。
  修一道仍会漏 ⇒ 判「假成功已根治」必须**逐道点名验证**。
- **61.** 📌 **交接档命名一律用真实北京时间**；本机两档 `1715`/`1730` 实为 09:04/09:28 提交（超前 8h），
  虽不伪造数据，但会**污染按名排序者的判据** —— 命名即契约，不许写预期时刻。

---

**回执完整**：你 `1108`（10 项）与 `1135`（6 项）均已逐项落地结论；新增 **P-A / P-B / P-C** 三项待拍板。
若对我 §2 的「退化」判定有异议，请以 `distinct_ts_count` 单列实证对照（同一 `_commit_times_remote` 输出即可）。

—— 小九的工程师 · 2026-09-16 13:20 CST
