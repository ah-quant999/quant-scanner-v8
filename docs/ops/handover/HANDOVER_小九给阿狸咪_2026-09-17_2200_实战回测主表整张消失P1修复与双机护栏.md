# 交接：实战回测页「算法回测总表」整张消失 —— P1 修复与双机护栏

- **时间**：2026-09-17 22:00（小九机）
- **严重度**：**P1** —— 主站「策略回顾 / 实战回测」页顶部**整张主表 + 一句话结论**不渲染（用户截图报障）
- **状态**：✅ 已修复并推送 main（净室单文件直推），真实渲染对照已验证
- **一句话根因**：主表渲染函数内部的 `_stars()` 引用了**未定义变量 `rt`**，首次调用即抛 `ReferenceError`，
  被调用点 `catch(e){}` **静默吞掉** ⇒ `__btOverviewHTML()` 返回空串 ⇒ 整块不渲染。

---

## 一、现象

用户截图（实战回测卡）：勋章下方**直接**是
`⏳ 累积中（样本 < 30…）` + `🧾 口径说明：卡级主表每卡只取 1 行既定主口径…`，
**上方应有的「🏁 算法回测总表 · 每个算法的最佳持有期」表格与「📌 一句话结论」整块消失**。

> 判读要点：`累积中` / `口径说明` 属于卡内**另一段**（`__renderBacktestSection` 内联块）恒渲染；
> 主表则由 `window.__btOverviewHTML()` 产出、在 `dst.innerHTML = (_btOv + html)` 里**前置**。
> 「勋章下直接是累积中」= `_btOv` 为空串 = 主表整体没渲染 —— 不是没数据，是**代码崩了**。

---

## 二、根因（三层独立证据）

| # | 证据 | 结论 |
|---|---|---|
| ① | 真身基线中 `function _stars(card){ … }` **仅 13 行**，块内 `var rt = ` 命中数为 **0**，却出现 `rt.score / rt.profile / rt.cov / rt.maxN / rt.shortExcess` | 函数**被砍头**：丢了 `__MODULE_STARS_OVERRIDE` 覆写口、`var rt = __committeeRate(card)`、`if(!rt) return ''`、`kind==='pool'` 基础股池分支 |
| ② | 全库 `var rt = (typeof window.__committeeRate…` 仅存在于 2 处（另一函数 `__renderBacktestCardsFrom`、`__committeeStarHTML`），**`_stars` 作用域内没有** | `rt` 在该作用域**确实未定义** ⇒ 必抛 `ReferenceError` |
| ③ | 坏块末尾有一行**与上一行完全相同**的孤儿残句 `+ String(_tips)…` | 典型「打补丁只换尾巴、没换头」的手工痕迹（本次已顺带清除） |

**触发链**：
```
_stars(r.card)  ← 总表渲染行 L6205 / L6207 / L6217
   → ReferenceError: rt is not defined
      → 被 __btOverviewHTML 的函数级 try/catch 捕获（仅 console.warn）
         → return ''  ⇒ _btOv = ''
            → 主表 + 一句话结论 整块不渲染
```
> ⚠️ 调用点 `try{ _btOv = window.__btOverviewHTML(); }catch(e){}` 是**空 catch** —— 这是本事故**被隐藏 1 天**的直接原因。

---

## 三、复现与修复对照（真实渲染，非文本比对）

用 Node 沙箱加载**线上真身代码 + 真实 `data/*.js` 产物**，只替换 `__btOverviewHTML` 内部的 `_stars`：

| 版本 | `__btOverviewHTML()` 返回 | 控制台 |
|---|---|---|
| **复现**（替换回修复前的 `_stars`） | `LEN=0`（空串）；`HAS_TITLE=false`、`<tr>=0` | `[btOverview] ReferenceError: rt is not defined` |
| **修复后**（线上现版） | `LEN=5919`；`HAS_TITLE=true`、`<tr>=5`、`HAS_CONCLUSION=true`；星级 `4.19⭐ / 3.93⭐ / 3.3⭐` + 逆势龙头「累积中」行 | 无异常 |

⇒ 复现结果与用户截图现象**逐项一致**，修复后主表完整产出。

---

## 四、改动面（改后三件套 ①：云端对齐）

| 项 | 值 |
|---|---|
| 提交 | `aa2fae22af6f0da957fb88004f2d4ad09cedf5c5` |
| 父提交 | `349125da9a9361f71d469168ffdd3ca26aa13d43` |
| blob | `677ef355448d3825775c38ce9882995df71b2c15` |
| tree | `2850b78325b08d3ffb0dc5ffc127245923ebad81` |
| 差异面 | `git diff --numstat` = `33 3 index.html`，4 hunks，**仅 `index.html`** |
| 推法 | **隔离索引净室单文件直推** + `--force-with-lease=main:<父>`（**绝不裸 force**）；推送窗口内 tip 被 CI 推移，脚本已自动在新真身上重打补丁 |
| 修复文件行 | `_stars()` 由 13 行 → 43 行（L5923–5965） |
| 校验 | `node --check` 通过（`_stars` 单元 + 整个 `__btOverviewHTML` 块）；行尾 **LF** 逐字节保持 |
| 未碰 | 小九机本地工作树 ~37 项 WIP **零触碰**（隔离索引，全程不动工作区） |

**修复口径（② index.html ↔ logic.html 一致性）**：
重建后的 `_stars()` 与全站唯一出口 `window.__committeeStarHTML` **同源同判据** ——
同为「人工覆写优先 → 股池不评星 → 未就绪显示累积中 → 带小数实算分 + 自证 tooltip」，
两处不再可能打架。**本修复不改变任何口径**，只恢复原设计。

---

## 五、🔴 双机护栏（阿狸咪必读）

| 事项 | 规则 |
|---|---|
| 真源 | 一律以 `origin/main` 为**唯一真源**；`git fetch && git checkout origin/main -- .` 后再动手 |
| 小九机本地树 | **仍是旧树**：既不含本次 P1 修复，也不含 09-17 晚的「算法现场评分卡」 ⇒ **严禁直接 commit/push 小九机本地树**，否则会把线上修复**回退**（P0） |
| 同类风险 | 本事故形态 = 「函数被砍头后静默失效」。若在别处看到「某块内容整段不出现、但数据层正常」，先查**该块渲染函数是否抛错被空 catch 吞掉**，不要先怀疑数据 |
| 提交前自检 | 改 `index.html` 后至少做一次「真实渲染」级验证（Node 沙箱跑目标函数），不要只看文本 diff |

---

## 六、遗留 / 建议（待主人定）

1. **空 catch 静默吞错**（建议项，未擅自改）：调用点 `catch(e){}` 与函数级 `catch(){return ''}` 会把
   「整块消失」伪装成「无数据」。建议改为 `console.error` + 卡片内一行可见提示
   （如「⚠️ 本块渲染异常，已记日志」），**根治同类故障的不可见性**。
2. **同类砍头痕迹体检**（已做）：对真身做「连续重复行」全量扫描，**代码类重复仅此 1 处**（即本次孤儿残句），
   其余为正常的 `</div>` / `<meta>` 闭合行 ⇒ 无第二处同类损伤。**本次修复后代码类重复归零。**
3. 修复后主表行数 `<tr>=5`（表头 1 + 数据 4：因子实验室 / 四量终极 / 三重共识 / 逆势龙头累积中）。
   若期望「强势跟踪」也成行，属**数据层**（`IMA_STRONG_BACKTEST` 未被 `gen_backtest_all_algos.py` 收编，
   前端合成行受 `_inScope` 过滤），**与本 P1 无关**，另行决定。

---

## 七、复核命令（自带真身校验）

```bash
# 1) 真 tip 是否为本次修复或其后代
git ls-remote origin main

# 2) 真身里坏块已消失、修复版在位（两个计数都应为 2）
git show $(git ls-remote origin main | awk '{print $1}'):index.html | grep -c "if(!rt) return '';"
git show $(git ls-remote origin main | awk '{print $1}'):index.html | grep -c "var rt = (typeof window.__committeeRate"

# 3) 线上站点可见性
curl -s --max-time 20 "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$RANDOM" \
  | grep -c "算法回测总表"
```
