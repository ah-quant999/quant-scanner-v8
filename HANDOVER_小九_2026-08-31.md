# 交接文档 · 小九 → 阿狸咪

- **日期时间**：2026-08-31 ~12:20（周一，小九白天机 07:45–17:45 时段）
- **仓库**：`ah-quant999/quant-scanner-v8` @ `main`
- **推仓前基线**：`origin/main` 实际 tip（本次 `git fetch` 时取值，见第二章）
- **本批内容**：在基线之上新增 1 个 commit，含 B 批（一劳永逸报警根因）+ C 批（共振日历空壳根治 + 重要事件日历9月归档回看 + 云端/双机/运维/逻辑详解全方位对齐）。

---

## 一、已推仓修复总览（防覆盖校验，语法/逻辑验证通过）

### A 批（08:13 已推，HEAD≈`9ee23e8`/`fdfaa9a` 区间）

| # | 修复 | 文件 | 说明 |
|---|------|------|------|
| 1 | CRDS 逆势龙头「停更」放宽 | `index.html` | `window._tradingDaysBetween` 交易日计数；仅 **2+ 交易日**才告警；措辞「天」→「个交易日」。单元测 6/6 PASS。 |
| 2 | 平均股价映射修正（初版） | `update_v8.py` / `cloud_fetch_v8.py` | `AVG_PRICE_DATA` 由 `intraday` 改 `post_close`；避免 premarket 清空 → 空壳。 |
| 3 | ETF 申赎抓取重写 | `scripts/fetch_etf_subscription_em.py` | akshare 列错位崩溃→改用 `fund_etf_category_ths`+`fund_etf_flow_em`（五分类+净流入）。 |
| 4 | 今日事件·今日解禁 UI | `index.html` | 胶囊「未来7天 N 家」+ 空态 `text-align:center`；解禁日历/业绩预告乱码修（`'+var+'`→`${var}`）。 |

### B 批（本批新增，随本次 commit 推仓）— 一劳永逸报警根因修复

| # | 修复 | 文件 | 说明 |
|---|------|------|------|
| 5 | 健康自检·日历感知（一次性消除 5 个误报） | `v8_health_check.py`（4 处） | ① 运维 page 在 `adjust_max_age` 直接 return `def_max`，不再被 45min 盘中严格度夹逼；② 全量数据 page 日历感知 max_age（收盘后 8h 内 360min，否则按距收盘分钟+180）；③ `_hard_cap_for_owner_rule` 对 盘后数据/选股策略/全量数据 用日历感知 cap（距收盘分钟+180，下限 24h），运维 7天 cap；④ `AVG_PRICE_DATA` 由 `实时数据` 改 `盘后数据`。 |
| 6 | 宏观数据速览「11天前」显示修复 | `index.html` | `renderMacroSummary` 顶部时间胶囊改用 `MACRO_DATA.update_time`（今日盘前刷新），不再用 LPR 月频公布日（被 `_fmtAshareRel` 渲染成「11天前」）。LPR 真实公布日 08-20 仍逐行在胶囊内展示。 |
| 7 | 平均股价双写覆盖·根因根治 | `cloud_fetch_v8.py`（2 处） | ① 移除 `RAW_FILE_MAP` 的 `AVG_PRICE_DATA` 映射；② `f_avg_price` 首行 `return {}`（dead code，绝不再写 `avg_price_data.json`）。彻底杜绝全A等权近似覆盖 880003 真指数 MA 历史 → `position_vs_ma20/ma60` 不再恒为 null。 |

### C 批（本批新增，随本次 commit 推仓）— 共振日历空壳根治 + 重要事件日历9月归档回看

| # | 修复 | 文件 | 说明 |
|---|------|------|------|
| 8 | 共振日历（机游+北向）空壳·一劳永逸 | `index.html` | **根因**：`LHB_HISTORY` 经 defer 懒加载（文末 A2 loader），首屏(DOMContentLoaded)仅见 `LHB_DATA` 当日 → 只渲染 1 天(Aug28)显「空」。修复：① `renderJyCalendar`/`renderNorthCalendar` 加数据空态占位 + `window.__rcPollHistory` 轮询补绘（LHB_HISTORY 到位即重绘）；② 懒加载 onload 改为两个 tab 都重绘（`window.__rcRedrawBoth`），不再依赖当前激活 tab。验证：node 复现首屏仅 1 天 → 历史到位后两 tab 均 20 天。 |
| 9 | 重要事件日历·9月归档 + 回看 | `index.html` + `data/archive/` + `cloud_fetch_v8.py` | ① 当前 2026年8月 归档到 `data/archive/V8_CAL_2026-08.js`，索引 `data/archive/V8_CAL_INDEX.js`；② 前端卡加 ◀▶ 月份切换，`__v8calGoto/Prev/Next` 加载归档月回看（标签标「📌 归档回看」）；③ `f_v8_cal` 同步发布 `data/archive/V8_CAL_YYYY-MM.js` + 更新索引 → 每月切月旧月自动留档，前端可回看。验证：node 测 Sept live 时 ◀ 渲染 2026年8月(归档)。 |
| 10 | C 批·云端/双机/运维/逻辑详解对齐 | `index.html` + `HANDOVER_小九_2026-08-31.md` | ① 逻辑详解页新增 3 个平实矩阵（共振日历懒加载 / 重要事件归档回看 / 历史归档与双机同步），不写脚本/数据名；② 运维面板新增「历史归档」状态行；③ 核验云端 workflow：归档文件随 `data/` 目录由构建部署提交，由云端抓取任务生成并推送，周末清理只扫顶层 `data/*.js`、不递归子目录 → 归档不会被误删；④ 双机同步：本机每小时 pull 会拉取 `data/archive/` 更新，无需新建 automation。 |

> 验证记录：3 个 .py 文件 `py_compile` OK；`index.html` 25 个内联脚本块 0 语法错误；健康自检离线校验 `check_data_cards` 37 项 → 36 OK + 1 WARN（仅 AVG_PRICE，见下）；`check_signal_date_freshness` 3 项全 OK；`check_all_data_files` 62 个未登记文件全 OK；共振日历 node 复现（首屏1天→历史到位20天）、重要事件日历回看 node 测（Sept live 时 ◀ 渲染归档8月）均 PASS。

---

## 二、防覆盖执行记录（重点，下次推送务必照做）

- 本仓库为**浅克隆 + CI 持续自动提交**，`origin/main` 本地跟踪引用会滞后/失真。每次推送前必须：
  1. `git fetch origin main --depth 1` 取**真·远端 tip（FETCH_HEAD）**；
  2. 把本地改动 rebase / `reset --hard FETCH_HEAD` 到真 tip 之上；
  3. `git push --force-with-lease=main:<FETCH_HEAD> origin HEAD:main`（pin 到真 tip，远端若已前进则自动拒绝，绝不静默覆盖）。
- **历史踩坑**：曾用 `--force-with-lease=main:39a13cf` 把 CI 提交临时移出线性历史 → 立即 fetch 找回 + rebase 复原，最终 `main` 同时含修复与 CI 数据，无回滚。铁律：**禁止在 CI 提交之上直接 `--force`；永远先 fetch 真 tip 再 rebase。**
- 本批固定用上述三步；推送后线上 HEAD = 本批修复 commit（以 GitHub 实际为准），基线 `fdfaa9a` 数据保留。

---

## 三、阿狸咪（夜间/周末 18:00–07:30 + 周末）需关注

1. 部署前先 `git pull`（remote 已切 SSH，已验证可用）。若 `index.html` 有本地未提交改动 → **禁止触碰**（小九白天独家，冲突保留本地）。
2. **平均股价（AVG_PRICE_DATA）今夜自愈**：当前 `data/AVG_PRICE_DATA.js` 仍是旧全A僵尸（update_time 11:05、MA 空值），这是修复前最后一次 intraday 抓取的产物。本批 #7 已封死 `f_avg_price` 覆盖源；今夜 `post_close` CI 跑 `scripts/fetch_avg_price.py`（通达信 880003 真指数，120 天历史）即重写真实 MA，WARN 自动消除。**勿手动改 data/AVG_PRICE_DATA.js**，让管线自愈。
3. ETF 抓取脚本已重写（#3），下次跑批应正常产出五分类+净流入，**不再全 0**。请观察一轮确认数据非空。
4. **情绪周期·连板天梯「卡空了」= 盘前瞬态，非 bug**：该卡数据（SENTIMENT_CYCLE / LIMIT_UP_HEATMAP）在 09:40 首次盘中抓取前为空，开盘后自动渲染（已验证 data 非空：phase 退潮、score 72→81、ladder 有值）。**勿追这个 phantom bug**，除非 10:00 后仍空才报。
5. **勿触发 `git credential fill`**（Windows 凭据助手挂死 4–10 分钟）；remote 已为 SSH，凭证问题用 SSH key。
6. 阿狸咪自动化任务维持 **PAUSED**（除用户明令）；紧急指令监听只读不写。
7. **共振日历/重要事件日历（C批）**：共振日历空壳已根治（历史数据到位即双 tab 重绘 + 轮询补绘）；重要事件日历已加 ◀▶ 回看，8月归档在 `data/archive/V8_CAL_2026-08.js`。**9月切月无需手动干预**：盘前任务会自动发布 `data/archive/V8_CAL_2026-09.js` + 更新索引，前端 ◀ 即可回看8月。**对齐结论**：无需新建 workflow/automation——归档由云端抓取任务生成并推送，构建部署随 `data/` 目录提交，周末清理只扫顶层 `data/*.js`、不递归子目录，因此归档不会被误删；本机每小时 pull 同步也会拉取 `data/archive/`。

---

## 四、未决 / 待主人确认

- **今日事件「居中 / 天内N家」**：按截图已应用（#4 + #1 部分）。若主人所指「下方小字」为其他元素，待其 `Ctrl+F5` 核对后微调。
- **AVG_PRICE 真数据**：依赖今夜 `post_close` CI（沙箱无网络，本机无法现抓 880003）。若主人要求即时看到真实 MA，需等收盘后自动跑批，或手动在联网环境跑 `scripts/fetch_avg_price.py`。
- **推送授权**：本批按主人「审计无误后推仓入库防覆盖」指令推送，触发 GitHub Pages 自动发布。

---

*交接人：小九（白天机）｜生成于 2026-08-31 ~12:50｜B+C 批修复 + 云端/双机/运维/逻辑详解对齐 随本 commit 推仓*
