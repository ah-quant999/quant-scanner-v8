# HANDOVER — 小九：监控噪音治理 + 盘后算法链执行时间说明

**提交人**：阿狸咪（detect + dispatch）
**接收人**：小九（生产数据任务，单位机 / GHA 执行）
**日期**：2026-08-05 07:25
**优先级**：P0（直接影响告警可信度，是"每天要人盯着"的根因）

---

## 0. 先说结论：你没有失职，是监控在冤枉你

连续 14 轮巡检日志反复写「cn_fetch 连续第 N 轮停在 19:24」，让人以为你晚上没干活。**核对调度事实后确认这是误报**：

- `v8_cn_fetch.yml` 的 cron 只有 **周一~周五 08:25–15:30**（UTC `25 0` ~ `30 7`），周末仅 09:00 一轮。
- **19:24 之后到次日 08:25 之间根本没有 cron**，不存在"应该跑而没跑"。
- 但 watchdog 主检查用的是**固定 120 分钟阈值、无时段豁免**，于是夜间每轮必判 FAIL。
- runner `lemoncat-cn` 全程 `online=True`——**你一直在线，一直在执行**。

真正的问题不是执行，是**告警把"设计如此"当成"故障"**，10 条误报淹掉 1 条真信号，人只能靠肉眼盯。这一轮已从根上修掉。

---

## 1. 本轮已修（已推 origin/main，commit `3c6ab38`）

### 1.1 watchdog 时段豁免（`v8_cloud_watchdog.py`）

新增 `in_schedule_window(kind)`，非调度时段不再判 FAIL：

| 检查项 | 应有调度窗口（北京时间） |
|---|---|
| cn_fetch | 工作日 08:00–16:00（收盘 15:30 + 1h 容错）；周末 09:00–11:00 |
| build_deploy | 08:00–22:00（盘后算法链 20:00 后仍会触发构建） |
| raw_data | 08:00–22:00 |

窗口外仍会显示原始信息，但状态转 OK 并附「非调度时段，豁免」说明——**不丢信息，只是不再当故障报**。

### 1.2 健康检查阈值真 bug（`v8_health_check.py`）

`adjust_max_age()` 盘后数据分支原写法：

```python
if is_trade_day and h < 17.0:
    return min(def_max, 480)   # 本意"放宽到 480"
```

`def_max` 是 360，`min(360, 480)` 恒等于 **360**——**放宽逻辑从未生效**。这是夜间盘后数据每轮必 fail 的直接原因。

已改为：
- **19:00–23:00**：严格 360min（当晚算法链产出窗口，该报就报）
- **其余时段**：1500min（数据合理来自上一交易日 18:30，年龄可达 24h+）
- 周末：2880min

### 1.3 废弃字段误报

| 模块 | 原检查字段 | 问题 | 现在 |
|---|---|---|---|
| NT_DATA | `calendar` | `fetch_orphan_nt_data.py` 里**恒为 `[]`**，注释写明"已用 V8_CAL 代替" | 改查 `alerts`，卡片更名「市场提示」 |
| GOLD_POOL | `candidates` | 实际数据在 `stocks`（144 只），candidates 恒空 | 只查 `stocks` |

### 1.4 日历纳入监控（此前是监控盲区）

`V8_CAL`（财经日历月历）**此前完全没有健康检查项**。已新增，查 `weeks` + `month`，阈值 1500min。

### 1.5 风险温度计阈值自相矛盾

`status_for("USDJPY")` 原判定 `value < 158 → yellow`，导致 green 分支 note 写的"干预有效区 156–158"**永远进不去**。157.74 被标"警惕"，而文案说"平稳"。已改为 `<156 或 >158 → yellow`。

### 1.6 效果

```
修复前：ok19 / warn1 / fail10
修复后：ok28 / warn1  / fail2   (total 31，含新增日历项)
```

剩余项定性：
- **1 warn（真）**：三重共识 `stocks=0`，`near_miss=3`（迈瑞医疗/贵州茅台/云铝股份）。阈值已放宽（TOP10 硬门槛 ≥70 → rank≤10 + 相对分兜底），**代码已进仓，今晚 18:30 算法链跑完生效**。
- **2 fail（本机环境，非故障）**：Pages 站点被墙、无 `GITHUB_TOKEN`。云端 runner 无此问题。

---

## 2. 回答"能不能把盘后算法链改早一点"

**建议维持 18:30，不要提前。** 理由是数据源的发布时间卡死了下限：

| 数据源 | 最早可得时间 | 18:30 抓取是否安全 |
|---|---|---|
| 龙虎榜 `fetch_lhb` | 交易所约 **17:00–18:00** 发布 | ✅ 刚好，提前到 17:00 会抓空 |
| 机构交易 `fetch_inst_trade` | 同龙虎榜口径（17 点前只能取上一交易日） | ✅ |
| 期指持仓 CFFEX | 15:30 后 | ✅ |
| 融资融券 | 交易所约 **18:00–21:00** 发布 | ⚠️ 18:30 偏早，可能抓到昨日值 |
| K线/基本面/斐波那契 | 收盘即可 | ✅ |

结论：
- **提前没有收益，只有风险**——龙虎榜和机构交易会直接抓空，而这两个是候选池的核心输入。
- 真正偏早的是**融资融券**。`v8_algo_run.yml` 已有第二趟 cron **20:00**（UTC `0 12`）作为补跑，融资融券会在这一趟补齐，无需改动。
- 若某天 18:30 那趟失败，20:00 会自动补；两趟都失败才需要人工介入。

**所以：不改执行时间。** 之前"感觉没执行到位"是监控误报造成的错觉，不是调度时间问题。

---

## 3. 明早 08:00 请你确认（3 项，约 5 分钟）

1. **验证误报是否止住**
   ```
   python v8_cloud_watchdog.py --heal --health-check --alert
   ```
   预期：夜间/盘前时段 cn_fetch、build_deploy、raw_data 三项显示「非调度时段，豁免」，不再计入告警。若仍满屏红灯，回报我。

2. **确认 08:25 盘前 cn_fetch 正常触发**
   这是当天第一趟真实调度。08:00–16:00 属调度窗口，此时若 FAIL 是**真故障**，需立即处理。

3. **今晚 18:30 后复查三重共识**
   阈值放宽已进仓但未生效。今晚算法链跑完后确认 `TRIPLE_CONSENSUS.stocks` 是否 > 0；若仍为 0，说明放宽力度不够，需再评估 criteria。

---

## 4. 遗留待办

| 项 | 说明 | 优先级 |
|---|---|---|
| VIX / US10Y 数据源 | Yahoo 对本机与部分 IP 返回 403，已加 cookie 预热 + query1/query2 双域名 + stooq 第三源兜底。**云端 ubuntu 是否抓得到需明早验证**（本机被墙无法验证）。已加 stale 保护：抓不到时沿用上次值并标注，不会再用 None 覆盖已有数据 | P1 |
| `stash@{0}` | 连续多轮日志提到未处理，留待人工确认 | P2 |
| 科创板/北交所概念补全 | 见 `HANDOVER_小九_2026-08-05.md`，414 只科创板 concepts 为空、北交所未映射 | P1 |

---

## 5. 铁律提醒（勿踩）

- `data/*.js` 是构建产物，**冲突时不要 rebase**，直接 `git fetch` + `reset --hard FETCH_HEAD` + `python update_v8.py` 全量重建。
- 推送姿势：`git add -f` → `commit` → `git fetch origin` → `git rebase origin/main` → `push`。禁用 `safe_pull()`。
- 严禁把 v6 算法代码复制回 v8（v8 是带原生钩子的增强版）。
