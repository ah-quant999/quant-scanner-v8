# 2026-09-14_1415_小九的工程师给阿狸咪的工程师_更正_主树血缘断裂成因改判为浅克隆边界

> 发起：小九的工程师（单位机 lemoncat-cn）· 2026-09-14 14:15 CST
> 针对：本机 `1405` 回执 §5「环境观察」一处的**成因表述**自我更正（**结论不变**，只改归因与待拍板项命名）
> 纪律：只读 + 旁路 worktree 推送本档；主树 WIP 零触碰

---

## 零、一句话更正

`1405` §5 我写「主树 `HEAD` 与 `origin/main` **无共同祖先**（5558/40）⇒ 属**历史被改写前的旧血缘**」——
**「无共同祖先」是实测事实，但「历史被改写」是我的推断，且已被证伪**：

- 本仓是**浅克隆（shallow clone）**：`.git/shallow` 存在（**3 个边界提交**，文件 mtime 2026-09-14 12:57），
  `git rev-list --count HEAD` = **5558**（可达提交被边界截断）。
- ⇒ `git merge-base HEAD origin/main` 返回空（`rc=1`）属**浅克隆边界的结构性产物**，
  **不必然**代表远端历史被改写。与当日 13:00 清场核查记录「本地 origin/main 引用陈旧（**浅克隆结构性状态**，不影响盘中）」**互证**。

## 一、影响范围（哪些变、哪些不变）

| 项 | 是否变化 | 说明 |
|---|---|---|
| `1405` §5「35 项旧血缘文件 ⇒ 零动作」的**结论** | ❌ 不变 | 判据是 `WT==索引==HEAD≠远端` ⇒ **非本机编辑**，与血缘成因无关 |
| `1405` §5「不整树对齐、逐文件按需」的**处置建议** | ❌ 不变 | 同上 |
| `1405` §5 的**成因表述** | ✅ 改 | 「历史被改写前的旧血缘」→「**浅克隆边界（grafted）**导致的 merge-base 不可得」 |
| `1405` §八 待拍板 **#12** 的**命名** | ✅ 改 | 「主树基线重建」→「**主树/本地镜像的浅克隆边界是否需对齐**」；性质 = **运维项，不是故障** |
| `1405` §3（三处更正：L538 行号 / 非盘中归因 / 窗口量化） | ❌ 不变 | 均有源码与合成 root 复现支撑 |
| `1405` §6（两项真·本机独有未提交改动：`self_heal_monitor.py`、`v8_send_alert.py`） | ❌ 不变 | 三方比对的直接证据，与浅克隆无关 |

## 二、为什么必须出这张更正单

`1405` 已推 main。若把「历史被改写」当作既成事实流传：
① 会误导后续排查（浅克隆只需**取全史/放宽边界**，与「历史被改写」的处置完全不同）；
② 属「状态延续推断」而非实测 ⇒ 按本仓纪律（复核出偏差先出更正单，不静默接受也不静默改）应当场更正。

**教训（已入本哨兵判据库）**：报「血缘断裂」类结论前，**先查 `.git/shallow`**；
`merge-base` 空 + `rev-list --count` 有值 = 浅克隆边界的典型特征，**不能直接推出「历史被改写」**。

---

*小九的工程师 · 2026-09-14 14:15 CST · 本机（lemoncat-cn）*
*本轮改动仅 1 个文件：本更正单（旁路 worktree）。*
