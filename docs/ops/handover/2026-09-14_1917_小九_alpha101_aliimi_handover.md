# 交接单 · 小九 → 阿狸咪 · 2026-09-14 19:17（署名：小九的股票专家）

**主题**：`101_formulaic_alphas(1).pdf` 因子筛选路线图已出，需阿狸咪后续接力推进 / 主人拍板

---

## 一、核心结论（主人已过目，须牢记）

> **路线：阶段 1 本周先跑 #12/#33/#101 最短周期验证 IC→胜率；阶段 2 两周内补 #2/#3/#13/#42 合成「量价背离复合因子」；阶段 3 一月内补中长周期 #7/#34/#21，#24 作大势过滤；阶段 4 才做申万行业中性化。**

- 完整分档、逐 alpha 公式、数据口径表、与现有模块协同表见 `alpha101_v8_analysis.md`。
- 收益率优先、胜率其次；优先接入 **Alpha#12（量价背离）**。

---

## 二、阶段拆解与责任边界

| 阶段 | 时间窗 | 动作 | 交付物 | 建议执行方 |
|---|---|---|---|---|
| **1** | 本周内 | 在 `factor_lab_backtest.py` 新增 `ALPHA_12`、`ALPHA_33`、`ALPHA_101`，跑 T+1/T+5/T+10 回测，验证 IC→胜率 | 单因子 IC/胜率报告 | 阿狸咪（夜间/周末） |
| **2** | 两周内 | 接入 `ALPHA_2`、`ALPHA_3`、`ALPHA_13`、`ALPHA_42`；合成 #2/#3/#13/#42「量价背离复合因子」 | 合成因子回测曲线、IR 报告 | 阿狸咪 |
| **3** | 一月内 | 接入 `ALPHA_7`、`ALPHA_34`、`ALPHA_21`；`ALPHA_24` 作为大势过滤开关 | 长周期因子接入报告、大势开关规则 | 阿狸咪 / 主人拍板阈值 |
| **4** | 后续 | 对阶段 1–3 IC 最高的 3 个因子做申万一级行业中性化 | 中性化 IR 对比 | 待 P0–P2 跑通后再做 |

---

## 三、给阿狸咪的操作提示

1. **文件位置**：本次新增 `alpha101_v8_analysis.md` 在仓库根目录，已随本交接单一起推送到 `main`。
2. **数据口径统一**：
   - `vwap = amount / volume`（日级近似）
   - `adv20 = mean(amount, 20)`
   - `rank` 用当日截面 `pct=True`
   - `delta(x, d) = x_t - x_t-d`
3. **与现有模块关系**：
   - 不替换「异常量比」「四量终极」「CRDS」「三重共识」，作为新 source 并列。
   - 新增 alpha 源需先通过 IC 验证，再决定是否加入 `final_recommend.py` 共识池。
4. **风险控制**：
   - 首批只接日 K 可算的 alpha，不碰 `indneutralize` / 非整数参数 alpha。
   - 接入 `final_recommend.py` 前，先用回测验证单因子胜率 > 52%（主人以胜率说话）。

---

## 四、防覆盖声明

- 本次推送为新增文件（`alpha101_v8_analysis.md` + 本交接单），不涉及 `index.html` / `cloud_fetch_v8.py` 改动。
- 推送使用 Git Data API，`PATCH ref force:false`，parent 挂当前 `main` tip，纯 ff，无覆盖。
- 本机工作树仍冻结，阿狸咪如需修改，请基于远端 `main` 操作；小九本机不做 `git add -A` / `commit` / `push`。

---

## 五、base / 推送证据

- 推送前远端 tip：`e3e74649ca6d6a9eaec1a3a1224686c2ba362148`
- 新增文件：
  - `alpha101_v8_analysis.md`
  - `docs/ops/handover/2026-09-14_1917_小九_alpha101_aliimi_handover.md`
- 提交消息：`docs: alpha101 因子路线图 + 阿狸咪交接单（小九的股票专家）`

---

**交接人**：小九的股票专家  
**接收人**：阿狸咪的股票专家  
**时间**：2026-09-14 19:17 CST
