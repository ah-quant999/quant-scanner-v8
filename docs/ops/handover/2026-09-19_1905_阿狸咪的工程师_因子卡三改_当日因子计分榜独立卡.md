# 交接件：因子卡三改 + 新建「🏆 当日因子计分榜」独立卡

- **执行机**：阿狸咪的工程师（家机，周末班）2026-09-19 19:00 前后
- **提交**：`4f18cfbd21b3`（index.html，Git Data API base_tree 推送，`force:false`，推后字节级自证 ✅）
- **改动面**：仅 `index.html`（纯前端；未碰任何 data/*.js、生成器、workflow）

## 主人令三改（图像取证）
1. **删「📐 计分依据说明」长文块**（主人：「因子实验室里这句删除」）——已整块移除渲染；证据分级口径唯一收敛到「🧮 每日因子计算」卡底部的「依据强度分级」图例。
2. **① 段正名**（主人问「这些不是已纳入因子？」）——标题改为「① 算法链因子台账（共 N 项　● 真计入 final_score ｜ ○ 仅标注不计分）」。事实口径：3 项里**只有放量弱势真计分**（weak_penalty，本日 0 命中，证据=未回测）；异常换手率 / ROE_TTM 均**仅标注不计分**（薄样本）。
3. **② 迁出成独立卡**（主人：「这张专门成立一张，把当天进候选池股票的各种因子加减分全列出来，按分数从高到低排列，最顶部的就是最终推荐的几只了」）——新卡 **「🏆 当日因子计分榜」**，位于 st-fa 子页「🧮 每日因子计算」卡下方：
   - 数据源 `TOP10_DAILY.top20`（generate_top10.py 落盘，每票 12 维因子分 + 文字明细，产物原序=已降序，前端不重排）；
   - 逐行 12 列因子分（信号基础/增强/形态/资金/板块/机构/质量/回测反哺/5日突破/研究/P4/P5）+ 总分，红=加分/绿=扣分/灰=0；
   - 每行下挂文字明细行（form/fund/sector/inst/quality/research/p4/p5 detail）；
   - 与 `FINAL_RECOMMEND_DATA.stocks` 代码精确匹配打「🏷 最终推荐」标（本日：洛阳钼业 603993 行即带标）；
   - 旧 factor_trace 表（4 只、adj 恒 0）信息量不足，弃用；`var TR` 死变量与 traceBlock 构造块已删干净。

## 顺手清理
- `__renderFactorLab` 内 gateBanner 赋值后的孤儿表达式行 `+mkt.verdict+_regimeTxt2+'</div>';`（无作用死代码）删除。

## 验证（全绿）
- node 提取 28 段 inline `<script>` 逐段 `new Function`：0 错；
- Pages 线上直抓：字节数 1381616 与推后 blob 一致；
- 真浏览器（Chromium）：dsbHost 渲染 40 行（20 只×主行+明细行）、「最终推荐」标在、「算法链因子台账」标题在、「计分依据说明」已不在、②指针在。

## 给小九的注意点
- 10 分钟级 build cron 会重打 `?v=` 缓存戳（内容保留，与 09-19 双卡提交后行为一致），**无需干预**；
- 未改 `.github/workflows/**`、未改生成器 ⇒ 无 P0 遗留；
- 若主人后续要「全池（非 top20）逐票明细」，需 generate_top10.py 扩大 top20 落盘口径，属数据层改动，待令。
