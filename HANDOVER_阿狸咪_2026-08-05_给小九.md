# HANDOVER 阿狸咪 → 小九 · 2026-08-05 23:55

> **接手人：小九（单位机/GHA）**
> **场景**：主人明早上班后找你同步今晚的事，请先看完此文件。
> **关联文档**：本机读 `E:/workspace/stock-scanner/.workbuddy/memory/2026-08-05.md` 看完整流水；云端 main 根目录这份 `HANDOVER_阿狸咪_2026-08-05_给小九.md`。

---

## ① 阿狸咪今晚做了什么（按 commit 顺序）

| # | Commit | 内容 | 涉及文件 |
|---|---|---|---|
| 1 | `9c9348a` | **算法链迁云端 ubuntu**：新建 `v8_algo_cloud.yml`（cron 18:30/20:00 + workflow_dispatch）；`v8_algo_run.yml`(cn) **cron 全移除**，仅留 workflow_dispatch 应急 | `.github/workflows/v8_algo_cloud.yml`、`.github/workflows/v8_algo_run.yml` |
| 2 | `5afa301` | 任务运行看板清理 8 张无源卡（st 4 张策略卡 + op 4 张无源运维卡）+ CRDS 待机文案细化/字号放大 + 逻辑详解页 CRDS 新增"待机提示文案"表 | `index.html` |
| 3 | `c043395` | 大牛股猎手卡片名后加 `_rcRel(updateTime)` 徽章（统一 _fmtAshareRel）+ **build_deploy.yml workflow_run 列表补"☁️ v8 盘后算法链(云端)"**（防算法链推了 raw_data 但前端不刷新） | `index.html`、`.github/workflows/v8_build_deploy.yml` |
| 4 | `6ae1baf` | 逻辑详解页新增"📏 全站判定区间/分档一览（备忘）"卡片（5 张表）+ 危机雷达算法段升级为表格 | `index.html` |
| 5 | `cdf740c` | **修 schedule 失效**：cn_fetch(云端) 加 17:00/21:00 全量兜底 cron；v8_algo_cloud.yml 加 20:30/20:50 兜底 cron（与 8-03 算法链失效同源问题） | `.github/workflows/v8_cn_fetch_cloud.yml`、`.github/workflows/v8_algo_cloud.yml` |

**重要：所有改动都已 push 到 main（小九这边 fetch 即拉到）**

---

## ② 全链路审计结果（今晚 23:54 实测）

| 环节 | workflow | 状态 | 说明 |
|---|---|---|---|
| 触发源 | cron | ⚠️ **部分失效** | cn_fetch(云端) 17:30 后 0 次 schedule 触发（已修）；v8_algo_cloud 刚部署同样未注册（已修）；预计明早 08:25 是 cn_fetch 第一次 schedule 触发 |
| 抓取执行 | cn_fetch(云端) ubuntu | ✅ | 17:30 run#1 success（探针 12 模块全绿），依赖清华镜像装 akshare |
| 抓取执行 | v8_algo(云端) ubuntu | 🔄 | **run#1 23:44 启动，正在跑**（后台监控 task `EM5vd6`，完成后会通知并触发 build_deploy）|
| 数据生产 | cloud_fetch_v8.py / algorithms/run_algorithms.py | ✅ | 云端 ubuntu 上 akshare/baostock 可达；mootdx 设 SKIP_MOOTDX=1 直走 akshare |
| 推送 | api_push_raw.py | ✅ | 用 GITHUB_TOKEN 走 Contents API（cn_fetch 用 v8_cn_fetch.yml 里的，algo 用 run_algorithms.py 内部的） |
| 构建触发 | v8_build_deploy | ✅ | workflow_run 联动列表：cn_fetch(cn)+cn_fetch(云端)+algo_cloud+风险温度计（c043395 加上 algo_cloud）；push 触发：raw_data/** 等 |
| 构建执行 | v8_build_deploy ubuntu | ✅ | 23:53 run#266 success（响应 6ae1baf 推送），历史多次 success |
| 前端部署 | pages build and deployment | ✅ | 23:53 run#660 success |

**链路整体通**——主路径已验证，唯一风险是 cron 调度（已加冗余兜底）。

---

## ③ 算法链今晚情况

- 18:30（run#16）/ 20:00（run#17）原 cn runner 任务**双双失败**（cn runner 上 GitHub API 同步源码网络层不稳）
- 23:44 阿狸咪手动 dispatch 云端版 run#1，**预计 30-60 分钟跑完**（后端监控中）
- 跑完后会自动 build_deploy → 候选池/金股池/三重共识/四量终极 + 共振日历盘后明细 全部更新到 18:30+/20:00+ 时间戳

---

## ④ 给小九的明早操作清单（按优先级）

### ✅ 高优先级 — 必查

1. **08:25 验证 cn_fetch 第一次 schedule 触发是否成功**
   ```bash
   # 在 GH Actions UI 看 v8_cn_fetch_cloud.yml runs 列表
   # 或 curl：
   curl -H "Authorization: Bearer $(cat .gh_pat)" \
     https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/327687211/runs?per_page=3
   # 期望：08:25 之后出现 event=schedule 的 success run
   ```
   - ✅ 成功 → 链路正常，跳过此条
   - ❌ 没有 schedule run → 进 Actions UI 找 v8_cn_fetch_cloud.yml → 看 schedule 是否被 GH 自动暂停 → 手动 unpause / re-enable

2. **看盘后质量档是否更新（候选池/金股池/三重共识/四量终极）**
   - 如果云端 run#1 跑通 → 三重共识 stocks 应该 >0
   - 如果还是 8-04 数据 → 看 v8_algo_cloud.yml runs 状态（可能 run#1 失败了，需要修复）

3. **任务运行看板"盘后算法链"卡勾否**
   - source='TRIPLE_CONSENSUS'，slot 18:30/20:00
   - ✅ 都勾 → 算法链正常

### ⚠️ 中优先级 — 可选

4. **如果你要给 23:09 之后没有盘中档解释**：之前问"为什么实时数据在深夜还在更新"的根因
   - cn_fetch(云端) schedule 失效 → 一直没自动跑 → 但 raw_data 仍被推（watchdog/self_heal 之类触发）
   - 加冗余 cron 后会稳定每档触发（08:25 盘前、09:40-15:10 盘中、15:30 盘后、17:00/21:00 全量兜底）

5. **科创板/北交所概念补全**（之前 HANDOVER 的 P1 任务，今天 18:04 阿狸咪给过任务单）
   - 如果没动，按 `HANDOVER_小九_2026-08-05.md` 步骤执行（akshare 个股资料接口）

6. **VIX/US10Y 数据源**：Yahoo 403，加了双域名+stooq 兜底；云端 ubuntu 是否抓到需明早验证

### 📋 低优先级 — 备忘

7. **CI 踩坑铁律（之前 handover #5/#6）**：
   - 严禁 actions/checkout 在 cn runner（safe-delete 钩子 >50 文件）
   - 云端 runner 完全没这问题，所以 9c9348a 直接用 actions/checkout@v4
   - 数据写入用 `git reset --hard FETCH_HEAD + python update_v8.py` 不用 rebase（8-04 23:50 踩过的坑）

8. **MEMORY 文档已精简到 7KB**（8-04 23:55 改造），小九如要更新踩坑记录可以直接覆盖 MEMORY.md

---

## ⑤ 关于 24:00 模型切换

- 云端（小九）端：automation-1785920035232 一次性 23:30 执行，**已切换 deepseek-v4-flash → hy3**
- 本机（阿狸咪）端：automation-1785943946699 一次性 24:00 执行，**会切换全部 ACTIVE 任务从 flash → hy3**
- **不要建每日定时**（主人明确禁止），一次性切换跑完即结束
- 23:30-24:00 之间本机 8 个 hy3 任务继续跑（hy3 限流按小时重置）

---

## ⑥ 主人明早看什么

主人明早要看到：
- 任务运行看板：基本全勾（少数复盘类卡可能仍 ✗ 是预期）
- 候选池/黄金池/三重共识/四量终极：update_time = 今天 18:30+ 或 20:00+（不是 17:57）
- 共振日历盘后明细：当天（8-06）数据齐全
- 大牛股猎手：卡片名后"🕐 更新于 今日 18:30"（不再是 17:57）
- CRDS 卡片：待机文案五档细化 + 字号放大

---

## ⑦ 紧急联系方式

如遇阻塞（云端 algo 跑挂、cn_fetch schedule 仍失效、24:00 切 hy3 没生效）：

1. 先查 `E:/workspace/stock-scanner/.workbuddy/memory/2026-08-05.md` 看阿狸咪今天完整流水
2. 看云端 main `HANDOVER_阿狸咪_2026-08-05_给小九.md`（本文件，已 push）
3. 看 GH Actions runs 状态定位失败步骤
4. 本机/云端都跑 `python -c "import sqlite3; ..."` 看 automation_runs（如果是自动化失败）

---

*本交接文件位于 quant-scanner-v8 仓库根目录，随 git push 同步，小九 `git pull origin main` 即可读到完整内容。*
*阿狸咪（家里电脑）· 2026-08-05 23:55*

---

## ⑧ 08-06 00:00 补遗（本交接文档之后又提交了 1 个改动，务必看）

> 本段在 `7afea27`（本交接文档）**之后**提交，小九此前未知。

### 新增：16:30 港股收盘补抓档（commit `9ed0ce6`，08-06 00:00）
- **改了什么**：`v8_cn_fetch_cloud.yml` 新增 `30 8 * * 1-5`（北京 16:30）一档。
- **为什么**：港股 16:00 收盘（A股 15:00 已收盘）。原 15:30 盘后档只抓 A 股收盘口径，港股候选/恒指/盘中模块收盘值会滞后到次日。加这档补齐港股收盘数据。
- **影响前端**：恒指/港股候选/盘中模块收盘值卡片改为"15:30(A股) + 16:30(港股)"双档更新。
- **逻辑详解页**：已同步在「定时任务矩阵」补了 16:30 行（属 v8_cn_fetch_cloud 香港股收盘补抓）。

### 本次一并做的（防覆盖 / 对齐）
- 新建 `align_logic_ops.py`：抽取所有 workflow 的 cron，校验「逻辑详解页/运维页」是否已文档化；每晚 21:00 随 `v8_backup.yml` 自动跑，漂移写 HANDOVER_LOG 告警、不阻断备份。
- 修逻辑详解页定时任务矩阵漂移：把过时的「18:30 = v8_algo_run 小九待迁云端」改为真实主链「v8_algo_cloud（云端 ubuntu，20:30/20:50 兜底）」；补 v8_risk_gauge（危机雷达）、21:00 备份行；修正 runner_health_alert / safety_net 频率。
- 24:00 一次性自动化已将本机全部 3 个自动化模型切回 hy3（含紧急监听 / 19:30 交接检查）。

*阿狸咪（家里电脑）· 2026-08-06 00:10*