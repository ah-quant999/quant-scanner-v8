# 双机交接（阿狸咪 → 小九）2026-09-05 20:50 CST 【紧急】

## 背景（为什么紧急交接）
1. **因子实验室升 4⭐ 缺回测证据**：因子实验室停在 ★★ 的硬伤是「无独立分层回测（胜率/回撤）」。`algorithms/factor_lab_backtest.py` 已挂在 E 批回测全家（2026-09-04 主人令「都按你的建议做」），但 `raw_data/factor_lab_backtest.json` 从未产出过（链虽每天跑，该脚本或因超时/报错没落盘，或 backfill 模式被静默吞）。
2. **本机（阿狸咪家机）环境跑不动**：实测 mootdx 未装（`No module named 'mootdx'`）、东财 push2his 本机连不上（`ConnectionError RemoteDisconnected`）、仅 baostock 可用且源码「共享登录会话非线程安全，全局锁串行化」→ 382 只 × 单票 ~13s ≈ **1.5–3 小时**，且启动管道把输出憋死看不见进度。**主人令：本机做不到的紧急交接小九单位机跑。**
3. **周末 T+1 不能静默**：主人 2026-09-05 新令「周六和假期第一天都有 T+1 数据，还是要安排和交易日一样的任务」。`run_algorithms.py` 本就是 2026-08-29 科学模式（非交易日 backfill、跳过 fetch、计算照跑），今天周六应补跑上周五 T+1，不能因阿狸咪 PAUSED 漏掉。

## 小九请立即办（按序）
### 任务 A（紧急·回测，升 4⭐ 证据）
- 在 **小九单位机（lemoncat-cn）** dispatch `v8_algo_run.yml`（cn 版链）跑全链；若只想补回测，单独跑：
  `cd algorithms && python factor_lab_backtest.py`
- 环境预期：小九单位机 mootdx / 东财应通，三级兜底抓 700 日长历史比本机 baostock 串行快得多。
- 验收：`raw_data/factor_lab_backtest.json` 生成 → `update_v8.py` 会映射 `data/FACTOR_LAB_BACKTEST.js` → 前端因子实验室卡即可显示「异常量比五分位 + ROE Top30」回测结果。

### 任务 B（紧急·周末 T+1 补跑）
- 今天周六，按科学模式应为 **backfill** 模式（跳过 fetch_* 实时采集，计算照跑，数据日期改写上一交易日=2026-09-04 周五）。
- dispatch 全链后核对：TRIPLE_CONSENSUS / BACKTEST_COMPREHENSIVE / FINAL_RECOMMEND_DATA 等应脱离陈旧时间戳，前端 Ctrl+F5 抽查数据日期是否为 09-04/09-05 最新。

## 回测脚本要点（小九不必改代码）
- universe = `raw_data/_rps_cache/*.json`（重点池 A股 382 只）；本机该缓存个股仅 300 日，回测 `FETCH_DAYS=700` 会走三级兜底抓长历史。
- 因子 1 异常量比（缩量=强势）：五分位分层，升 3⭐ 判据 = Top−Bottom 利差>0 且 Top 层胜率≥55% 且 OOS 与 IS 同号。
- 因子 2 ROE_TTM 大市值 Top30 vs 全池等权（methodology_limit，当期分组回看，证据弱于量比因子）。
- **升 4⭐ 另需「异常换手扩全市场扫描」**，数据侧另行开启后复验（脚本头部注释已写明），本次若只出 3⭐ 证据亦算达标。

## 交接边界（勿动）
- 不在本机/坚果云内改 `factor_lab_backtest.py` 源码（已上 main，直接跑即可）。
- 不碰 `index.html` / `logic.html` / `data/*.js`（升星 UI 改动等回测结果出来、主人拍板后再走改后三件套）。
- 勿重复双跑云端 `v8_algo_cloud.yml` 与 cn 版（会互相覆盖 raw_data），本次优先 cn 版小九单位机。

## 当前状态
- 本机已停掉 27 分钟无产出的 baostock 串行回测任务（fYaTTy）。
- 本机另存腾讯接口并发回测兜底脚本 `backup/_factor_lab_quick.py`（仅本机环境差时备用，本次不跑）。
- token / 凭证不在此文档，按铁律单点存放。
