# 101 Formulaic Alphas 适配 v8 选股体系分析报告

> 文件来源：`101_formulaic_alphas(1).pdf`（Zura Kakushadze, WorldQuant, 2015）
> 分析目标：在 A股可用数据前提下，挑选能提升 v8 收益率（优先）与胜率的 alpha 公式，并给出接入路线。

---

## ⚡ 一图读懂：v8 接入路线图

| 阶段 | 时间 | 接入 Alpha | 目标 | 备注 |
|---|---|---|---|---|
| **阶段 1** | 本周先跑 | **#12 / #33 / #101** | 最短周期验证 IC → 胜率 | 只验证数据链路与单因子 IC |
| **阶段 2** | 两周内 | **#2 / #3 / #13 / #42** | 合成「量价背离复合因子」 | 等权合成，观察合成后稳定性 |
| **阶段 3** | 一月内 | **#7 / #34 / #21**，**#24** 作大势过滤 | 中长周期补充 | #24 做大势开关/不开仓条件 |
| **阶段 4** | 后续 | 申万一级行业中性化 | 提升 IR | 仅对阶段 1–3 IC 最高的 3 个因子做 |

> 完整分档、逐 alpha 公式、数据口径表、与现有模块协同表均在下方报告文件里。

---

## 一、筛选标准（四项硬门槛）

| 维度 | 要求 | 说明 |
|---|---|---|
| **A股数据可得** | 仅使用日 K（open/high/low/close/volume/amount）及可由日 K 派生的 vwap、returns、adv20 等 | 不依赖美股专有字段（如 GICS 行业中性化先不做，可用申万/东财行业后续替代） |
| **与现有因子低相关** | 避开 v8 已大量使用的 ROE、纯异常量比分层、四量终极同口径 | 避免重复建设 |
| **持有期匹配** | 优先 0.6–6.4 天（论文均值），对应 v8 的 T+1 / T+5 / T+10 回测档位 | 太长则衰减 |
| **收益率优先** | 在「简单可解释」与「历史夏普」之间取平衡，拒绝为复杂而复杂 | 主人要求收益率最重要 |

---

## 二、推荐接入清单（分三级）

| 优先级 | Alpha | 核心逻辑 | 预期作用 | 建议持有期 | A股适配难度 |
|---|---|---|---|---|---|
| **P0** | #12 | 量价背离：`sign(Δvolume) × (-Δclose)` | 放量跌=负向，缩量涨=正向；T+1 经典反转 | T+1 / T+5 | 极低 |
| **P0** | #2 | `rank(Δlog(volume))` 与 `rank((close-open)/open)` 的负相关 | 量价不同步时产生信号 | T+1 / T+5 | 极低 |
| **P0** | #33 | 开盘–收盘涨幅越大，次日越反转 | 日内动量次日反转 | T+1 | 极低 |
| **P0** | #101 | 日内实体占波幅比例 | 强势收盘次日延续/反转 | T+1 | 极低 |
| **P1** | #3 / #6 | 开盘价与成交量的负相关 | 开盘高+量小=潜在反转 | T+1 / T+5 | 极低 |
| **P1** | #13 | 收盘价排名与成交量排名的协方差为负 | 价量齐升后的冷却信号 | T+1 / T+5 | 低 |
| **P1** | #42 | `(vwap - close) / (vwap + close)` 排名 | 收盘相对 vwap 的偏离反转 | T+1 / T+5 | 低 |
| **P1** | #7 | 放量日触发 7 日价格动量/反转 | 成交量确认的价格动量 | T+5 | 低 |
| **P2** | #34 | 短期波动率相对长期下降 + 价格动量 | 波动率收敛后的突破 | T+5 / T+10 | 中 |
| **P2** | #21 | 8 日均线±标准差 vs 2 日均线 + 成交量过滤 | 均值回归+放量确认 | T+5 / T+10 | 中 |
| **P2** | #24 | 100 日趋势 vs 近期价格，切换长/短反转 | 长周期趋势过滤 | T+10 / T+20 | 中 |
| **P2** | #54 | 日内 (low-close)/(high-low) 加权反转 | 日内极端位置反转 | T+1 | 中 |

> 以上 12 个 alpha 已足够形成一个「量价背离 + 日内反转 + 波动率收敛」的互补组合，与 v8 现有「ROE/异常量比/四量终极/CRDS」形成正交。

---

## 三、逐个 alpha 详解

### P0-1 Alpha#12 — 量价背离（最推荐）

```
(sign(delta(volume, 1)) * (-1 * delta(close, 1)))
```

**逻辑**：今天相对昨天，若成交量增加（sign=+1）且价格下跌（delta<0），则 alpha 为正 → 买入信号；若成交量萎缩且价格上涨，alpha 为负 → 卖出/回避。

**为什么收益率高**：
- 这是论文中结构最简单的 alpha 之一，但属于「放量恐慌后反弹」的经典反转。
- A股散户主导的短期抛压极易产生这种背离。
- 计算成本几乎为零。

**v8 接入方式**：
- 在 `factor_lab_backtest.py` 中新增一个因子 `alpha_12`。
- 对每个股票计算 `alpha12 = sign(volume_t/volume_t-1 - 1e-9) * (-(close_t - close_t-1))`。
- 按 alpha12 分层（最强五分位组合），跑 T+1/T+5/T+10 回测。
- 接入 `final_recommend.py` 作为新 source，权重由 IC 决定。

---

### P0-2 Alpha#2 — 量价变化的秩相关

```
(-1 * correlation(rank(delta(log(volume), 2)), rank(((close - open) / open)), 6))
```

**逻辑**：过去 6 天，成交量 2 日对数变化与开盘收益率的秩相关为负时 alpha 为正。即「量增但开盘不强」或「量缩但开盘强势」都会给出反向信号。

**为什么适合 v8**：
- 使用 rank 而非原始值，对 A股极端成交量更稳健。
- 与 Alpha#12 互补：#12 看当日绝对量价方向，#2 看 6 天秩相关。

**v8 接入方式**：
- 新增因子 `alpha_2`。
- 需要 6 个交易日历史；T+1 可用。
- 可与 #12 等权合成一个「量价背离复合因子」。

---

### P0-3 Alpha#33 — 开盘–收盘反转

```
rank((-1 * ((1 - (open / close))^1)))
```

**逻辑**：当天收盘相对开盘涨得越多，alpha 越负（次日反转）；跌得越多，alpha 越正（次日反弹）。

**收益率来源**：A股 T+1 制度下，日内过度乐观/悲观常在一夜之间修正。

**v8 接入方式**：
- 计算 `alpha33 = -(1 - open/close)`，rank 后取最强五分位。
- 持有期 T+1，与 v8 现有 T+5/T+10 策略形成短周期补充。

---

### P0-4 Alpha#101 — 日内实体强度

```
((close - open) / ((high - low) + .001))
```

**逻辑**：实体占波幅比例。数值接近 +1 表示光头光脚大阳线，接近 -1 表示光头光脚大阴线。

**与 #33 的区别**：#33 看相对开盘价涨幅，#101 看实体在日内波幅中的位置。#101 更能识别「尾盘偷袭」或「开盘急跌后回收」。

**v8 接入方式**：
- 计算 `alpha101 = (close - open) / (high - low + 0.001)`。
- 既可单独分层，也可与 #33 合成「日内情绪强度」因子。

---

### P1-1 Alpha#3 / #6 — 开盘价与成交量的负相关

```
#3: (-1 * correlation(rank(open), rank(volume), 10))
#6: (-1 * correlation(open, volume, 10))
```

**逻辑**：过去 10 天，开盘价与成交量正相关越强，alpha 越负（反转）；负相关越强，alpha 越正。

**v8 接入方式**：
- #3 用 rank 更稳健，#6 更直接。可二选一或同时接入观察 IC。
- 建议先接入 #3（rank 版本）。

---

### P1-2 Alpha#13 — 收盘价与成交量的协方差

```
(-1 * rank(covariance(rank(close), rank(volume), 5)))
```

**逻辑**：过去 5 天，收盘价排名与成交量排名的协方差为负时 alpha 为正。

**v8 接入方式**：
- 与 #3 类似但用协方差而非相关性，捕捉线性共变幅度。
- 可与 #3、#12 合成「量价背离组合」。

---

### P1-3 Alpha#42 — vwap 偏离反转

```
(rank((vwap - close)) / rank((vwap + close)))
```

**逻辑**：收盘相对 vwap 的位置。收盘远高于 vwap → alpha 负（次日回落）；收盘远低于 vwap → alpha 正（次日修复）。

**v8 接入方式**：
- vwap 用 `amount / volume` 估算。
- 适合 T+1 反转，可与 #33、#101 合成「日内情绪反转」因子。

---

### P1-4 Alpha#7 — 放量 7 日动量

```
((adv20 < volume) ? ((-1 * ts_rank(abs(delta(close, 7)), 60)) * sign(delta(close, 7))) : (-1 * 1))
```

**逻辑**：当今日成交量超过 20 日均量时，看过去 60 天里 7 日价格变化的排名。sign 决定方向。

**v8 接入方式**：
- 需要 adv20（20 日成交额均值），A股可得。
- 适合 T+5 动量策略，作为现有动量因子的补充。

---

### P2-1 Alpha#34 — 波动率收敛 + 价格动量

```
rank(((1 - rank((stddev(returns, 2) / stddev(returns, 5)))) + (1 - rank(delta(close, 1)))))
```

**逻辑**：2 日波动率相对 5 日下降 + 价格上涨 → alpha 为正。捕捉「波动率收敛后的突破」。

**v8 接入方式**：
- 持有期 T+5/T+10。
- 可与「强势突破」模块共振。

---

### P2-2 Alpha#21 — 8 日布林带 vs 2 日均线

```
((((sum(close, 8) / 8) + stddev(close, 8)) < (sum(close, 2) / 2)) ? (-1 * 1) :
 (((sum(close, 2) / 2) < ((sum(close, 8) / 8) - stddev(close, 8))) ? 1 :
  (((1 < (volume / adv20)) || ((volume / adv20) == 1)) ? 1 : (-1 * 1))))
```

**逻辑**：价格突破 8 日布林带下轨 → 买入；突破上轨 → 卖出；中间区域由成交量是否大于 adv20 决定。

**v8 接入方式**：
- 可作为布林带反转策略，持有期 T+5/T+10。
- 计算略复杂，但逻辑清晰。

---

### P2-3 Alpha#24 — 长短期趋势切换

```
((((delta((sum(close, 100) / 100), 100) / delay(close, 100)) < 0.05) ||
  ((delta((sum(close, 100) / 100), 100) / delay(close, 100)) == 0.05)) ?
  (-1 * (close - ts_min(close, 100))) : (-1 * delta(close, 3)))
```

**逻辑**：100 日均线相对 100 日前涨幅 < 5% → 用 100 日最低价偏离做反转；否则用 3 日价格变化做反转。

**v8 接入方式**：
- 长周期因子，适合 T+10/T+20。
- 可作为大势过滤条件，而非直接选股。

---

### P2-4 Alpha#54 — 日内极端位置加权

```
((-1 * ((low - close) * (open^5))) / ((low - high) * (close^5)))
```

**逻辑**：收盘越接近低点、开盘越高 → alpha 越正（反弹）；反之越负。

**v8 接入方式**：
- T+1 反转。
- 注意 `open^5` 会使数值极端，建议先标准化再分层。

---

## 四、不建议首批接入的 alpha

| 类型 | 例子 | 原因 |
|---|---|---|
| **大量依赖行业中性化** | #48, #58–#97 多数 | `indneutralize` 需要稳定的行业分类；A股可用申万/东财，但增加复杂度，第二批再做 |
| **非整数/怪参数** | #58–#97 中多数 | 如 `Ts_Rank(..., 3.92795)`，参数过拟合风险高，且对 A股不一定稳健 |
| **adv 窗口过大** | 依赖 adv180/adv240 的 | A股小盘股成交量不稳定，长窗口 adv 噪声大 |
| **与现有因子高度重复** | #1, #5, #11, #25, #28, #41 等 | 与异常量比、四量终极的某些分层高度相关，优先接入互补性更强的 |

---

## 五、v8 实施路线图（推荐顺序）

### 阶段 1：本周内（先跑最短周期，验证数据链路）
1. 在 `algorithms/factor_lab_backtest.py` 新增 `ALPHA_12` 因子：
   - 输入：daily close/volume
   - 输出：alpha12 序列
   - 跑 T+1/T+5/T+10 回测
2. 同步新增 `ALPHA_33` 和 `ALPHA_101`（日内反转三剑客）。
3. 观察 IC 与胜率；若 alpha12 单因子 T+1 胜率 > 52%，接入 `final_recommend.py`。

### 阶段 2：2 周内（扩展量价背离矩阵）
4. 接入 `ALPHA_2`、`ALPHA_3`、`ALPHA_13`、`ALPHA_42`。
5. 将 **#2/#3/#13/#42** 等权合成「量价背离复合因子」，测试合成后是否比单因子更稳定。

### 阶段 3：1 个月内（中长周期补充）
6. 接入 `ALPHA_7`、`ALPHA_34`、`ALPHA_21`。
7. 接入 `ALPHA_24` 作为大势过滤（不开仓/减仓条件）。

### 阶段 4：行业中性化（视效果而定）
8. 对阶段 1–3 中 IC 最高的 3 个因子，加入申万一级行业中性化，观察是否提升 IR。

---

## 六、数据与计算口径建议

| 字段 | 计算口径 | 备注 |
|---|---|---|
| `returns` | `close_t / close_t-1 - 1` | 用前复权 close |
| `vwap` | `amount / volume` | 日级别近似 |
| `adv20` | `mean(amount, 20)` | 20 日平均成交额 |
| `rank(x)` | 当日截面对 `x` 做 `rank(pct=True)` | 与论文一致 |
| `ts_rank(x, d)` | 过去 d 天内 `x` 的时序排名 | 注意 d 取 floor |
| `stddev(x, d)` | 过去 d 天标准差 | 样本标准差或总体标准差均可，内部统一即可 |
| `delta(x, d)` | `x_t - x_t-d` | 与论文一致 |

---

## 七、与 v8 现有模块的协同

| 现有模块 | 关系 | 建议 |
|---|---|---|
| **异常量比（factor_lab）** | Alpha#12/#2/#13 与异常量比同属于「成交量异变」家族，但方向/窗口不同 | 不要替换，作为并列 source |
| **四量终极** | 四量已综合量/价/换手/资金，Alpha#33/#101 可作为其「日内情绪」补充 | 仅当 IC 不重复时接入 |
| **CRDS 逆势龙头** | CRDS 偏个股抗跌，Alpha#24 偏大势趋势过滤 | 可做大势开关 |
| **三重共识** | 三重共识是多策略共振，新增 alpha 源应先通过 IC 验证再决定是否加入共识池 | 不自动加入 |

---

## 八、结论

**收益率优先的前提下，首批应接入 4 个 P0 alpha：#12、#2、#33、#101。**

- 这 4 个都只需要日 K（open/high/low/close/volume/amount），计算简单，持有期以 T+1/T+5 为主，与 v8 现有因子正交。
- 其中 **Alpha#12（量价背离）** 最值得先做：逻辑最直白、A股散户特征最匹配、计算成本最低。
- 若 P0 验证通过，第二阶段扩展 P1 的 4 个 alpha，形成「量价背离 + 开盘收盘反转 + vwap 偏离」的完整短周期组合。

**不建议**在首批接入大量使用 `indneutralize` 或非整数参数的 alpha（#58–#97 区间），待主因子跑通后再做行业中性化实验。
