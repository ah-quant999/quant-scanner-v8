# 交接 · 阿狸咪 → 小九（2026-09-09 06:25 · v2 最终版）

## 一句话
**盘前清空盘中模块已彻底取消：`_clear_intraday_for_premarket` 对盘中变量【纯跳过】，什么都不写。** 已恢复今晨被六连清的 6 个变量。**请小九勿恢复清空、更勿补加标记。**

---

## 一、现象（比反馈更严重）
不是 05:38 清一次，而是**从 00:29 起每小时清一次、整夜六连清**（文件内 update_time 实证，非 mtime）：

```
00:29:19  01:30:16  02:32:55  03:34:07  04:36:49  05:38:26   ← 全是 no_data 空 stub
```

昨晚 21:0x 的盘后真实数据，从 00:29 就被清掉，整夜六连清，且会持续到 09:30。

## 二、根因
`cloud_fetch_v8.py::_clear_intraday_for_premarket()` 时间门控**只有上限、没有下限**：

```python
if h >= 9.5:   # 只挡「09:30 之后」
    return     # → 00:00–09:29 任何一次 premarket 跑批都会清空
```

而 `v8_cn_fetch_cloud_hosted.yml` / `_selfhosted` 等按「当前时间 < 09:30 → category=premarket」选档，
**premarket 档整夜约每小时被派发一次**（分钟漂移 48→51→52），于是每个整点清一次。
主人此前交代的「9:20 才清」从未真正生效——因为跑批是 hourly 派发、分钟漂移，**无法保证 9:20 有档**。

## 三、最终修复（v2，已推 main）
主循环改为**纯跳过**：

```python
for var, cat in CATEGORY_MAP.items():
    if "intraday" not in [...]: continue
    if var in KEEP_VARS: continue
    # 🛡 2026-09-09 主人令（一劳永逸·同类误伤第 6 次终结）
    continue        # ← 什么都不写：不写 stub、不打标、不动 update_time
```

即：**完整保留上一交易日真实值，等开盘后首次 intraday fetch 自然覆盖。**

### ⚠️⚠️ 为什么不能打 `premarket_cleared` 标记（v1 踩过的坑，务必牢记）
v1 我写成「保留真实值 + 打 `premarket_cleared` 标记」，看似合理，**实际等于没修**：

前端 `index.html` 多处**一见到该标记就渲染「盘前清空，开盘后更新」占位、而不渲染数据**：

| 位置 | 代码 |
|---|---|
| 概念资金热力 ~2199 | `var _cleared = !!(d && (d.premarket_cleared===true \|\| d.no_data===true))` |
| 行业树图 ~2324 | `var sfCleared = !!(SF && SF.premarket_cleared === true)` |
| 板块资金 ~10464 | `const sffLive = (sff.no_data===true \|\| sff.premarket_cleared===true) ? false : …` |

→ 一打标，保留下来的真实数据被前端重新藏起来，卡片照样空白。

**结论：此处必须与 KEEP_VARS 完全一致——什么都不写，直接 continue。**
（代码里已留醒目警示注释 `切勿在此写 premarket_cleared / no_data 标记`，请勿删除。）

### 顺带避开的另一个坑
`v8_health_check.py:1371` 有「盘中交易时段被标记 premarket_cleared → 10:00 后判 fail」的规则。
纯跳过不打标，此风险一并消除。

## 四、已恢复数据（12 个文件）
6 变量 ×（`raw_data/` 源 + `data/*.js`）**两层都改**——只改 JS 会被下一轮从 raw_data 覆写回空：

`INDEX_QUOTES` / `ETF_PULSE` / `ETF_INTRADAY_HEAT` / `CANDIDATE_QUOTES` / `MARKET_ALERTS` / `SECTOR_FUND_FLOW`

恢复基准 = 提交 `bb5be1ad8`（真实数据 2026-09-08 21:05–21:10）。

## 五、改后三件套（实测全绿）
| 项 | 结果 |
|---|---|
| ① 语法/编码 | Python 语法 **0 错误**；无 BOM；CRLF=0（纯 LF，符合仓库规范） |
| ② 结构/依赖/运行时 | 前端标记依赖已排查并规避；健康巡检无误报；SH_SZ_HISTORY / LIMIT_UP_HEATMAP / CAPITAL_FLOW_DATA / MACRO_DATA 四个自定义块**结构完整保留** |
| ③ 引用完整性+零污染 | 本地仓库 tracked 改动 **0 处**；`v8_deploy_guard` 仅「小体积合法 JSON 不误判 shell」，不依赖 stub 存在 |
| 数据层 | 12 文件实测：**无 `no_data`、无 `premarket_cleared`、update_time 均为 09-08 21:0x** |

## 六、请小九遵守（防覆盖）
1. **勿恢复任何盘前清空逻辑**——这是同类误伤第 6 次（08-11 ETF_DAILY_MONITOR / 08-26 CONCEPT_RANKING / 08-31 连板天梯 / 09-01 板块资金 / 09-08 MACRO_DATA / 09-09 本次）。历次都是把变量逐个挪进 KEEP_VARS 打补丁，本次从根上取消。
2. **勿"顺手"给保留数据补 `premarket_cleared` / `no_data` 标记**——理由见第三节，会让前端把数据藏起来。
3. 你本地若有 `cloud_fetch_v8.py` 未推改动，**请先 `git pull` 对齐再改**，避免覆盖本次修复（你 09-08 17:37 动过此文件，冲突风险实存）。
4. `data/*.js` 的 `?v` 缓存戳由 `v8_cache_buster_reconcile.yml`（每 15 分钟）自动对齐；主人端 Ctrl+F5 立即可见。
