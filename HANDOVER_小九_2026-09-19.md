# 📋 交接文档：小九 → 阿狸咪

- **更新于**：2026-09-19 12:20（北京时间，周六）**自动更新**（本档由 automation `1785719698947` 每日 12:00 / 18:00 两槽位自动生成）
- **发起机**：单位机 `lemoncat-cn`（小九） ｜ **目标机**：家机 `alimi-cn`（阿狸咪）
- **数据采集时刻**：**2026-09-19 12:05–12:18 CST**（所有结论均标注此口径；凡「后续会怎样」一律改写为可跑的复核命令，不作预测）
- **远端基线（现取，非记忆）**：`origin/main` tip = **`25e0babe282fd4d6126737f03d9ded67e1279157`**（`v8 build: 2026-09-19 11:58`）
  - 取证命令：`git ls-remote origin refs/heads/main` 与 `git fetch origin` 双路一致
- **本槽位性质**：**状态快照 + 白班累积改动汇总**（当日无新增结构性改动由小九独立提交；主体改动来自阿狸咪夜班/凌晨批次与自动化产出）

---

## 零、一句话结论

**周末（非交易日）**：主站与全部云端链正常，站点 HTTP 200；线上健康巡检 **全绿（ok120 / warn0 / fail0 / limited1 / info1，total 122）**；
runner `lemoncat-cn` online；新鲜度 43 项零陈旧。
**但有两个必须处理的实质问题**：① **盘后算法链今日 3 次 failure 全部同一根因 —— `🛡 结果问责` 步报「产物完整性闸门未通过」是误导，真因已在我侧探明并**现已修复**（见 §2.1，**阿狸咪已先行落地，我侧逐字复核确认**）；② **`build_deploy` 的 gate 只判时钟不判交易日（阿狸咪 1155 档）** ⇒ 周末盘中窗 `build` 结构性不跑但 run 顶层仍 `success`（假成功）—— **这是「主人周末手动部署诉求落空」的直接机制**，列待拍板（见 §3-P1）。

---

## 一、背景

- 今日为**周六（非交易日）**，`is_trading_day=false`，最近交易日 = `2026-09-18`。
- 双机分时：**小九白班 7:45–17:45**，阿狸咪夜班/周末 18:00–次日 7:30。本槽位（12:00）属小九班内。
- 小九本槽位职责：汇总当日状态、生成交接档、推送 main，**不做结构性改动**（详见 §6 边界声明）。
- 本仓 `ff-only merge` **预期失败**（浅克隆 + 本地 main 钉旧提交 `8e10679e5`，`unrelated histories`）⇒ 沿用「只读基线 + 临时 worktree 旁路」范式，**主树零触碰**。

---

## 二、今日已完成改动（按机别/时段分列，SHA 全部现取核实）

### 2.1 🔴 盘后算法链「问责步必崩」根治 —— **今日最实质的修复**（阿狸咪凌晨批次，我侧独立复核确认）

**事件时间线（全部可复现）**：

| 时刻 | 事件 | 证据 |
|---|---|---|
| 09-19 04:17–04:40 | 小九巡检查出：`v8_algo_cloud.yml` 每轮盘后链 `🛡 结果问责` 步**必然 failure**，报错文案「产物完整性闸门未通过（raw_data 源头层有陈旧/缺失）」 | 我侧探针 `v8_verify_args_probe.py`：`verify_chain_outputs.py: error: unrecognized arguments: --upto B` |
| 04:38 | 小九定性：**不是数据陈旧，是参数接口漂移**。溯源三提交 —— `c3c1fbca7e`(09-14) 引入 `--upto`；`cd1ccb3dd0`(09-18 21:32，阿狸咪「清除干净」批次)**误删**（`+16/-41` 连 `STAGE_ORDER` 一并删），而 workflow 调用点未同步 ⇒ 每轮必崩、**闸门实际已失效却伪装在工作** | 我侧 `v8_upto_rootcause.py` 三提交对照 |
| 09-19 05:21 | 阿狸咪落地修复提交 **`9bb4e1812`**（`fix(algo+build+ima): 算法链假failure根治 + build_deploy自取消根治 + ima新源接入 + 前端死调度清理`） | 现取 `git log` |

**我侧逐字复核（12:15 CST，走 Contents API 线上真身，非缓存摘要）**：

```bash
# 复核命令（可复跑）
curl -s -H "Authorization: Bearer $TOK" \
  https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/algorithms/verify_chain_outputs.py?ref=main | jq -r .content | base64 -d | grep -c upto
```
- `algorithms/verify_chain_outputs.py`：**16238 B**，`argparse` 参数实测 = `['--warn-only', '--date', '--upto']` ✅ **`--upto` 已恢复**
- `STAGE_ORDER` 在树 ✅ ｜ `.github/workflows/v8_algo_cloud.yml`：**66660 B**，`--upto` 出现 **1** 次（调用点保留）✅
- ⇒ **脚本侧参数面 与 workflow 调用面 已重新一致**，根因闭环。

**⚠️ 但：今日 3 次 failure 发生在修复落地之前，属历史残留，不是回归**（证据见下）：

| run# | created(CST) | 失败步 | 步态 | 对应 commit |
|---|---|---|---|---|
| `#35411267233` | 09:00 | `🛡 结果问责` failure（**20/20 步全跑完**） | 09:23:06→09:23:10 | `366864b1b`（08:48） |
| `#35415095001` | 10:15 | 同上（20/20） | 10:33:49→10:33:52 | `c9e592793`（10:13） |
| `#35418278839` | 11:20 | 同上（20/20） | 11:55:27→11:55:30 | `04e42a5cd`（10:42） |

- 关键判据：**「20/20 步全跑完 + 唯有末步问责 failure」** = 已知特征，与 `--upto` 漂移 100% 吻合；且 3 次 failure 的 head_sha 均**早于** `9bb4e1812`。
- ✅ **修复后首个成功的盘后链**：今日 `☁️ v8 盘后算法链` 有 **1 次 completed/success**（我侧 API 实测）。
- ⏳ **正在跑的**：`#35419380693`（in_progress，11:43 创建，job `algo` 11:55:46 开跑）—— **这是修复后首个完整轮次**，
  **请阿狸咪开机后优先复核它的 `🛡 结果问责` 步结论**：
  ```bash
  gh api repos/ah-quant999/quant-scanner-v8/actions/runs/35419380693/jobs \
    --jq '.jobs[].steps[] | "\(.name) -> \(.conclusion)"' | grep 问责
  ```
  **若仍 failure ⇒ 说明还有第二处漂移，请立即回报；若 success ⇒ 该 P0 正式结案。**

### 2.2 阿狸咪凌晨/上午批次改动（均已在远端，我侧抽查在位）

| SHA | 时刻 | 主题 | 我侧复核 |
|---|---|---|---|
| `9bb4e1812` | 05:21 | **算法链假 failure 根治 + build_deploy 自取消根治 + ima 新源接入 + 前端死调度清理**（四合一） | ✅ `--upto` 恢复（§2.1） |
| `40beaa247` | 05:34 | `fix(eol)`：行尾纠正 —— 上轮误将 4 个文件 LF 翻成 CRLF 致 workflow **0 job** | 我侧抽验 `logic.html` 远端仍为 **CRLF 全覆盖** ✅（`\r\n` 逐字节确认） |
| `89d0ee6ed` | 05:40 | `hotfix(build_deploy)`：真凶 = `with` 被错配到 `run` 步 ⇒ workflow 拒绝加载（0 job failure） | 与 `v8-publish-gate` / `v8-ci-step-truth-audit` 同族纪律 |
| `fcc24a6dd` | 04:40 | 「主升/启动 板块·龙头股」卡改为与「板块资金趋势」卡**同源同规则（单一真源）** | 我侧 `data/` 抽查无孤儿源头 |
| `ba90fa987` | 04:59 | 龙头股快照时间上卡面 + 快照滞后时明示告警 | 同上 |
| `8bcef5117` | 07:53 | `fix(sector)`：**`data_date` 改真 K 线日 + 快照清污 + 闸门侧数据日可见化** | ✅ **已见效，见 §4.2（重要：可销账两条待办）** |
| `162a42d53` | 07:18 | `data/maharo_insights.js` 经**发布闸门**核对上线 | ✅ 线上 `update_time = 2026-09-19 07:30` |
| `e20576cdf` | 06:13 | feat(premarket)：**新增「🌙 隔夜美股强势 · 映射 A/港股」卡** + 盘前 08:25 自动更新 | ✅ 线上 `data/US_HK_MAP.js` 200、`bias=隔夜美股偏强`；`index.html` 远端命中「隔夜美股强势」**6** 处 |
| `8164d8f62` | 07:20 | feat(crds)：逆势龙头页加**危机雷达判据条** + 危机-逆势有效性回测；回测汇总表分组去重 | 无异议 |
| `ae9b65b26` | 08:12 | fix(ima)：强势跟踪卡删除「明细受源端截断」长提示条（**主人令**） | 无异议 |
| `7b6c92678` | 08:35 | fix(ci+algo)：门禁 `data/*.js` 上界**改动态**（面 + 容差）；IMA 回测消费源覆盖度，消除「累积中」误归因 | ✅ 见 §4.3 口径确认 |
| `9bea32e8b` | 08:37 | fix(unlisted)：删除暂未上架·观测类的外层「蓝色圆弧括号」空壳卡（**主人令**） | 与 `v8-card-frame-and-theme-color` 同族 |

### 2.3 今日交接件流水（远端 `docs/ops/handover/`，共 12 份，现取枚举）

| 时刻 | 方向 | 主题 |
|---|---|---|
| 0040 | 阿→小 | 算法链改动独立审计与 P1 闸门盲点 |
| 0142 | 小→阿 | **回执**：未回执交接对齐（commit `46436d1241`，我侧复核 21023 B 逐字节一致） |
| 0501 / 0510 | 阿→小 | 龙头股快照时间上卡面 + 两卡同源改造已上线 / 接线补丁待应用 |
| 0555 | 小→阿 | **回执**（未回执 2 档逐项对齐） |
| 0650 / 0745 | 阿→小 | 隔夜美股映射卡上线 + 门禁上界根治 / 危机雷达判据条 + **index 第三次被盖回** |
| 0815 | 小→阿 | **回执**（25680 B，tip `fbe9097db`） |
| 0820 / 0900 | 阿→小 | 接 0555 回执：D-3 已根治 + D-4 口径更正 + P0 补丁可一键应用 / 接 0815 回执：三处实质纠正 + 拍2拍3 已做完 |
| **1010** | **小→阿** | **回执**（15357 B / LF-only，commit `c9e592793`，我侧 `cat-file -s` 复核 = 15357 ✅） |
| 1155 | 阿→小 | **build 闸门只判时钟不判交易日 · 周末盘中窗 build 静默 skip 与假成功** |

### 2.4 本机自动化产出（非人工改动，供台账）

- **07:15 机构研究·AI 解析建议出稿上线**：daily 净字数 **5913**（上一稿 5032），线上 `data/maharo_insights.js` **21494 B**，
  闸门输出 commit `162a42d538`。素材 20 条全为 `2026-09-18`（瑞银 7 / 大摩 3 / 花旗 3 / 小摩 3 / 杰富瑞 2 / 高盛 2）。
  本期主轴：**稀缺度被上修 vs 可持股性被下调**（VLCC 中东湾-中国日收益破 100 万美元/天、瑞银锂中周期 13–15 万元/吨、
  大摩中国 WFE 2027E 558 亿/2028E 686 亿美元；反向约束：小摩同报告建议「战术性降低仓位」、瑞银 ESS 出货 830GWh 已覆盖 2026–27 装机 83%）。
- **07:30 白河愁博士宏观观点卡更新**：21 条（主帖 7 / 回复 14），最新主帖 `2026-09-18 22:38`；文件 17164 → **17645 B**，
  提交 `8e10679e5`。线上复核 HTTP 200 / SIZE 17645 / `update_time = 2026-09-19 07:30:52`。
- **死锁巡检 ×4 轮**（01:00 / 02:20 / 03:25 / 04:27 / 07:45 / 10:09，纯巡检，仅 02:20 一轮含写动作）：
  02:20 判定真僵尸 `#35371751539`（并发组 `v8-algo-cloud` 堵死）→ cancel + dispatch 解封（新 run `#35379256842`）；
  其余轮次零写动作。**03:25 的经验已入长期记忆**：判僵尸必须看「steps 里已完成步的 `completed_at` 是否增长」。
- **本地 runner 脚本同步守卫 ×2**（00:48 第 99 次 / 04:50 第 100 次）：白名单 6 脚本 **6/6 与 origin/main 逐位一致**，零同步零动作。

---

## 三、当前系统状态（12:05–12:18 CST 实测）

### 3.1 站点与健康

| 项 | 实测值 | 取证方式 |
|---|---|---|
| 主站 HTTP | **200** | `curl -s -o /dev/null -w "%{http_code}" https://ah-quant999.github.io/quant-scanner-v8/` |
| **线上健康巡检** | **`2026-09-19 11:57:56` / `overall=ok`** / **ok 120 · warn 0 · fail 0 · limited 1 · info 1 · total 122** | 直取 `data/HEALTH_CHECK.js`（30861 B） |
| 唯一 `limited` 项 | `UNLISTED_PANEL 暂未上架模块索引`（静态索引 · 无自动刷新，**非故障**，文案自陈「模块去向由主人拍板」） | 同上 |
| 新鲜度 | `FRESHNESS_STATUS` `check_time = 09-19 06:18:11`；**core_stale 0 / warn_stale 0 / frozen_stale 0**（43 项全过） | `data/FRESHNESS_STATUS.js` |

### 3.2 Runner

| 项 | 实测值 |
|---|---|
| `lemoncat-cn`（单位机） | **online @ 2026-09-19 10:42:59**（`data/RUNNER_STATUS_HEALTH.js`，源 `runner_health_alert.yml`） |
| 进程自检 | `data/RUNNER_STATUS.js` `update_time = 11:54:37`，`process.ok=true` / `worker=false`（listener 正常） |
| GitHub 侧 | 最近 15 条 self-hosted run 中**连续失败 0 / checkout 失败 0**；最新 = `🇨🇳 v8 中国数据抓取(云端·小九应急)` `completed` |
| 角色说明 | 兜底角色 **6h 未触发**，云端主线接管中（runner 文案自陈） |
| `alimi-cn`（家机） | 夜班/周末窗口，**本槽位属小九班内，未做判定** |

### 3.3 今日 CI run 全景（API 实测，09-19 00:00 CST 起，60 条采样窗口内）

| workflow | 结果分布 |
|---|---|
| `pages build and deployment` | success **29** / cancelled **8** |
| `☁️ v8 构建部署(云端ubuntu)` | success **10** |
| `☁️ v8 盘后算法链(云端)` | in_progress **1** / failure **3**（§2.1，全部修复前残留）/ success **1** |
| `🇨🇳 v8 中国数据抓取(云端)` | success 1 |
| `🩺 v8 云端健康巡检(合并·自愈+体检)` | success 3 |
| `🌍 v8 实时风险温度计` / `🚨 Runner 健康监控` / `🛡️ v8 缓存戳实时对齐(防覆盖)` | success 1 / 1 / 2 |

- 今日 main 提交数（`git log --since="2026-09-19 00:00"`）：**≈102 条**（含 `?v` 防覆盖与各类 build 戳）⇒ churning 强度与历轮同量级。
- **unfinished run**：全仓仅 `#35419380693`（盘后算法链，正常在跑，**非僵尸**）。

### 3.4 关键产物新鲜度抽查（远端树现取）

| 产物 | update_time | 判定 |
|---|---|---|
| `AI_MARKET_BRIEF` | `2026-09-19 08:43:36` | ✅ |
| `LIMIT_UP_HEATMAP` | `2026-09-19 08:40:54` | ✅ |
| `CRISIS_DATA` | `2026-09-19 08:40:51` | ✅ |
| `INDEX_QUOTES` | `2026-09-19 08:38:19` | ✅ |
| `maharo_insights` | `2026-09-19 07:30` | ✅ |
| `US_HK_MAP` | 线上 200、`data_date = 2026-09-18`、`bias = 隔夜美股偏强` | ✅（**隔夜口径，非实时**，文案已自陈） |
| `SECTOR_PHASE_HISTORY` | 见 §4.2 | ✅ **周末幻影已消失** |

---

## 四、需阿狸咪注意 / 待办 / 风险

### 4.1 🔴 P0 · 优先复核（开机第一件事）

**复核 `#35419380693`（修复后首个完整盘后链）的 `🛡 结果问责` 步**：

```bash
gh api repos/ah-quant999/quant-scanner-v8/actions/runs/35419380693/jobs \
  --jq '.jobs[] | .steps[] | select(.name|test("问责")) | "\(.name) -> \(.conclusion)"'
```
- **success ⇒ `--upto` 漂移 P0 正式结案**（我侧已确认代码面一致，只差一次运行级验收）。
- **failure ⇒ 立即回报**，说明存在第二处漂移；**不要**再按「产物陈旧」方向排查（历史已证明那是误导文案）。

### 4.2 ✅ 可销账两条（阿狸咪 1155 档已提及，我侧独立复核 **确认成立**）

阿狸咪 1155 档指出「你 1010 档的 `SECTOR_RS` / `SECTOR_PHASE_HISTORY` 记录已归正，建议标已闭环」。**我侧复核结论：确认成立，可销账。**

```bash
# 复核命令
git show origin/main:data/SECTOR_PHASE_HISTORY.js | grep -oE '"date":"2026-09-19"' | wc -l   # → 0
git show origin/main:data/SECTOR_PHASE_HISTORY.js | grep -oE '"date":"[0-9-]+"' | wc -l      # → 25
```
- `SECTOR_PHASE_HISTORY`：快照 **25 条**（我 1010 档记 28），**`date=2026-09-19` 出现 0 次**，末档 = `2026-09-18`（真 K 线日）
  ⇒ **周末幻影（08-30 / 09-12 / 09-19）已从该文件消失**，`8bcef5117` 的清污**已生效**。
- `SECTOR_RS` `data_date` 已归正为真 K 线日口径。
- ⚠️ 我 1010 档写的「修复须等下一轮 post_close A 批才生效」**已被现实推翻** ⇒ **以本条为准**。
- ⚠️ 附注：阿狸咪 1155 档同时提出「周末幻影可能与周末 `intraday_patrol` 补派盘中链同源」——
  该关联**属候选来源、未经证实**（补派日志因 PAT 无 `actions:read` 返 401 拿不到）⇒ **请勿当作已证实结论使用**。

### 4.3 ✅ 口径确认：门禁上界动态化

阿狸咪 `7b6c92678`（08:35）把门禁 `data/*.js` 上界**改为动态（声明面 + 容差）**。
- 我侧 `1010` 档实测：tip `data/*.js` 实际文件数 = **111**，余量 19/130。
- 我上一轮提的「注册数 + 容差」建议**已被阿狸咪正确推翻并认账撤回**（`update_v8.py::DATA_SOURCES` 是 `raw_data/*.json → window VAR` 映射表，**不是** `data/*.js` 路径表；`77+20=97 < 111` 上线即打挂两条链）。
- ⇒ **采纳形态 `max(130, 声明面 + 20)`**，已在 tip 代码逐行核对。**本条无需再动，仅作口径留痕。**

### 4.4 🔴 P1 · 待拍板：`build_deploy` 闸门只判时钟不判交易日（阿狸咪 1155 档）

- **现象**：gate 判据（`v8_build_deploy.yml` L100–103）**只有 `HH:MM` 时钟窗，无星期/交易日维度**。
  ⇒ 周六/周日/法定节假日的 `09:25–11:30`、`13:00–14:59`（CST），**`build` job 结构性不跑**，而 run 顶层 `conclusion` 仍 `success`（**假成功**）。
- **直接伤害主人诉求**：**「周末手动跑更新和部署」恰好落在这两个窗内 ⇒ 手动 `workflow_dispatch` 也被 skip**（L131 白名单含 `workflow_dispatch`），且 run 是绿的 ⇒ 看不出没部署。
  - ⚠️ 本条与我既往记忆里的「周末由阿狸咪手动跑批」流程**直接冲突**，请主人明确取舍。
- **修法（我不落，待拍板）**：最小改动 = L100 后插 `DOW=$(TZ=Asia/Shanghai date +%u); [ "$DOW" -ge 6 ] && TR=0`；
  全量解 = 接交易日历（仓库已有交易日判定能力，但**接口是否可被 yml 轻量复用未经我逐字确认** ⇒ 别把此句当已验证事实）。
- **硬约束**：① 不得改 run 顶层结论语义；② 验证必须看 **`build` job** 结论，**禁看 run 结论**；③ 不引入 `schedule` 依赖（本仓 schedule 有延迟前科）。
- **归属**：改 `.github/workflows/**` 需 `workflow` scope ⇒ 阿狸咪 PAT **实测 403 无此权限**（其 1155 档悬空 tree 探针实测）⇒ **落在我或主人**。

### 4.5 ⏳ 待拍板合辑（沿用 + 本轮新增；均未落）

| 编号 | 事项 | 状态 |
|---|---|---|
| **P1（本轮新增）** | `build_deploy` gate 补交易日维度（§4.4） | 待主人授权落 workflow |
| **拍A** | 授权落 **P0-1/P0-2 补丁**（`index.html.manual` 实测仍 = **4**，bug 仍活） | 补丁脚本 `docs/ops/patches/apply_2026-09-19_p0_workflow_patches.py`（5898 B）已在树、含幂等判据 |
| **拍B** | 附议「`data_date` 暂不硬否决」（前置：先改 `_escape_gate` 按数据日判落后） | 待拍 |
| **拍C** | 附议板块周期卡补「迁移基线日期」显示（属 `index.html` 双机共改面） | 待拍 |
| **拍D** | `empty` vs `fail` 另落 `run_status` | 待拍 |
| **拍E** | 汇总表「累积中」误归因待三方对齐 | 待拍 |
| **拍F** | `HB_XIAOJIU.last_time` 观察点（见 §4.6） | 观察中 |
| **D-1** | P1 源码指纹短路（三处成对改 + 无 workflow 权限） | 待拍 |
| **D-2** | P2 去硬编码（双机共写热文件） | 待拍 |
| **D-4** | P5 缺失告警 | 待拍 |
| **P-Q** | 盘中粒度盲区：`guard_v8_freshness.py check_group` 对 `max_hours>=24` 一律按交易日判 ⇒ 12 个盘中模块同日停更不判红 | 待拍 |
| **判据 80** | `update_v8.py::_make_js` 无 `update_time` 单调守卫（76 条桥接全受益） | 待拍 |
| **P-R** | `packed-refs` 僵尸条目 ⇒ 建议 `git update-ref -d refs/remotes/origin/main` | 待拍 |

### 4.6 🆕 观察点（本轮新增，**只登记不动作**）

- **`data/HB_XIAOJIU.js` 的 `last_time` 与 `update_time` 含义相反**：
  实测（12:12 CST 线上直取）：`last_time = 2026-09-18 23:04:03`，`update_time = 2026-09-19 11:57:37`。
  根因：`v8_runner_guard.py::write_heartbeat()` **只写 4 键、不含 `update_time`**，后者由桥接补充。
  **周末属预期**（小九白天班，家机不跑）。⇒ **下一交易日若 `last_time` 仍停 `09-18`，再查 guard 调度。**
- **`build_deploy` 的 `pages build and deployment` cancelled 8 次**：与阿狸咪今日「事件分桶只隔开不同类型、同桶内仍互踢」的结论同型（今日 09-19 二轮治理 L73–86：`cancel-in-progress` true→false + 新增「抢跑短路」步）。**观察是否继续 cancelled。**

### 4.7 ⚠️ 纪律提醒（照旧，勿踩）

1. **`index.html` 是红线热文件**：阿狸咪 `0745` 档登记「index 第三次被盖回」；我侧三方实测（12:15）：
   `origin/main = 1349184 B` / `HEAD = 1349925 B` / **工作区 = 1288574 B，本地独有行 407 / 远端独有 1169** ⇒ **真 WIP，禁 `checkout`**。
2. **主树陈旧暂存区形态**：本槽位实测 **STAGED = 0 项**（porcelain 831 项全为工作区改动/未跟踪）。**判暂存位须用 `git status --porcelain | awk 'substr($0,1,1)!=" "`，勿按总数推断。**
3. **行尾**：`logic.html` 必须 CRLF 全覆盖（我侧远端逐字节确认 ✅）/ `index.html` 纯 LF。**Bash 工具 cwd 每次调用重置**，worktree 操作必须**单条命令内 `cd ... && ...` 全链完成**。
4. **`git checkout <ref> -- <path>` 必垫索引 + 可能写入 755 模式位**（本机 09-19 两轮实测）：收尾**必须** 紧跟 `git reset -q -- <path>` 并**复测 STAGED**；
   判「同步是否真完成」**不可只看 `git diff --quiet`**，须用 `cmp <(git show ref:path) path` 或 `git hash-object --no-filters`。
5. **`git show <ref>:<file> | grep -c <pat>` 返回值会被文件名污染**（文件名含数字时，`grep -c` 返 0 也打印文件名 ⇒ 拿到假的「1」）⇒ 加 `| awk '{print $1}' | head -1`。
6. **判 GitHub run「卡死」必须看步态**：比较 `steps[]` 里**已完成步的 `completed_at` 是否增长**；
   `updated_at` 冻结 + `age > 90min` **均不足以定性**（09-19 03:25 险些误杀已跑 10/12 步的唯一在飞链）。
7. **推送铁律**：一律 `git push origin main`（**严禁 `git push HEAD:main`** —— MSYS 会转坏成 `head` 主机名）；判 push 成败**取 `$?`**，
   任何 `if cmd | tail` 形态都在测 `tail` 的退出码（09-18 血训）；推后 `ls-remote` 复核。

---

## 五、小九监督职责（本槽位及后续）

| 职责 | 内容 | 状态 |
|---|---|---|
| 交接档生成 | 每日 12:00 / 18:00 两槽位，汇总当日改动 + 状态 + 待办 | ✅ 本档 |
| 入站回执哨兵 | 巡检 `docs/ops/handover/` 中「阿狸咪→小九」未回执件并逐项回执 | ✅ 今日已出 3 份（0142 / 0555 / 1010） |
| 死锁巡检 | 监测超 90 分钟未完成 run，判僵尸必须看步态 | ✅ 今日 6 轮，仅 02:20 含写动作 |
| 本地 runner 脚本同步守卫 | 白名单 6 脚本与 origin/main 逐位对齐 | ✅ 今日 2 轮（第 99 / 100 次） |
| 盘后链产物验收 | 修复后首个完整轮次的问责步复核 | ⏳ **移交阿狸咪（§4.1）** |
| workflow 侧修复 | 本仓 `.github/workflows/**` 改动需 `workflow` scope（小九有、阿狸咪 403） | 待主人授权（P1 等） |

---

## 六、本档边界声明（可核验）

- 本槽位小九侧 **未做任何结构性代码改动**，**未 commit 除本档以外的任何文件**。
- 本槽位 **未执行任何 `cancel` / `dispatch`**；**未触碰** `index.html`（1288574 B 零改动）/ `logic.html` / `data/*`（内容）/ `raw_data/*` / `.github/workflows/**` / 任何阈值。
- **主树 HEAD 仍为 `8e10679e5`**，STAGED = 0，porcelain 831 项**零触碰**（出档前后一致）。
- 诊断脚本均落在**仓库外**临时目录，不污染仓库。
- 所有状态结论均**现取实测**，已标注采集时刻；**本档不含任何预测式表述**。

---

*本档由自动化任务 `1785719698947` 生成 ｜ 下一槽位：2026-09-19 18:00（小九白班尾）*
