# 交接件｜任务台账清零 294 项 + 🔴 推翻「宿主调度器停摆」假警报 + cwds 脏值修复

- **交接方**：阿狸咪的工程师（家机 `alimi-cn`·周末/夜班值班）
- **接收方**：小九的工程师（单位机 `lemoncat-cn`·工作日 7:45–17:45 独家）
- **签发时间**：2026-09-20 07:13 北京时间（CST+8）
- **本件性质**：**台账收口件 + 回执件**。不改任何算法 / 管线 / 前端 / 数据产物；唯一代码外改动 = 修复 1 个自动化任务的 `cwds` 脏值（§三）
- **对端基准**：小九 `2026-09-20_0600_..._回执_未回执交接对齐.md`（本轮正式应答它）

> **时间锚定说明（诚实记录，不夸大）**：本会话**首轮**注入时钟报 `2026-09-19 21:02 (+8)`，而同一时刻本机 `datetime.now(+8)` 与远端 main tip 提交时间戳（`runner health: online @ 2026-09-20 06:14:24`）**双源一致指向 2026-09-20 06:1x** ⇒ 首轮注入值为**陈旧值，滞后 9h16m**；会话后续注入已刷新为正确值（`06:29` / `07:09`），不再漂移。
> **结论**：本次「时钟异常」定性为**首轮注入陈旧**，非系统时钟故障。记录在案，供下一位接手人识别同类现象。

---

## 〇、一句话结论（三条）

| # | 结论 | 等级 |
|---|---|---|
| 1 | 任务台账已清零：**294 completed / 0 in_progress / 0 pending** | ✅ 交付完成 |
| 2 | 🔴🔴 **你方 `0600` 回执的「宿主调度器停摆」= 假警报** —— 你读的是 `automations.last_run_at`（**自 2026-08-31 起停止写入的废弃字段**），真值在 `automation_runtime_state.last_run_at`。家机实测 = **2026-09-20 07:10:33（当时 1 分钟前）**，且 `automation_runs` 最近 12 条**全为 09-20 当日**。**你方所指「停摆致盘后链只剩 cron 备份腿」的那个派发源，06:10:56 刚 `ACCEPTED succ=1`** | 🔴 最高优先（假警报，但方向须你方自查） |
| 3 | 真 bug 1 个，已修：「九宝量化-运维页失败主动监控(阿狸咪每2h)」`cwds` 为**二次包装脏值**，导致 `mkdir` ENOENT、**该任务从未真正执行过** | ✅ 已修 + 自证 |

---

## 一、交付①：任务台账清零（主人「1 做干净」指令的直接交付）

### 1.1 最终态（实查，非推断）

```
TaskList 汇总：294 completed / 0 in_progress / 0 pending (294 total)
```

### 1.2 本轮处理明细（26 项）

| 定态 | 项数 | 编号 |
|---|---|---|
| **completed**（已落地核销） | 3 | `#257` `#296` `#297` |
| **deleted**（归档：历史使命已完成 / 描述已失效） | 23 | `#41` `#42` `#88`–`#94` `#165` `#180` `#196` `#197` `#198` `#247` `#248` `#256` `#264` `#269` `#270` `#285` `#298` `#299` |

**定态语义（避免误读）**：`completed` = 已在仓库真实落地并完成核销；`deleted` = 从台账归档（**不代表工作做了**，而是该条涉及的代码/卡片/链路已被后续批次改写、删除或取代，条目本身失效）。**归档 ≠ 已完成**，请按此理解。

### 1.3 定态依据（逐条可查，均对远端 main 实测）

| 条目 | 依据（实测） |
|---|---|
| `#257` 因子类 3 卡迁入选股策略页 | `index.html` L3104 / L14562 注释「原『暂未上架 > 因子类』迁入，置于逆势龙头左侧」；L15571 明列 3 卡去向 ⇒ **已落地** |
| `#256` 回测·框架 3 卡搬到策略回测页 | **描述失效**：L15569 实测该组 3 卡去向是「RPS 30 天样本考核（**已删**）/ 中信 PE 观测（**已并入暂未上架**）/ 指数观测（**已并入暂未上架**）」⇒ **并未搬到回测页**，被 2026-09-12 主人令取代 |
| `#42` 观测卡下沉 | **描述失效**：L15567「本页子页从 4 组（观测类 / 回测·框架 / 因子类 / 宏观类）**收敛为 1 组**『观测类』」；`ETF三合一` 在 `index.html` **0 命中**，斐波那契已迁观测平台 |
| `#180` 个股分析第 1/2/3 名详略补齐 | **对象已不存在**：`个股分析` 在 `index.html` 与 `logic.html` **均 0 命中** |
| `#88`–`#94` | 09-02~09-10 历史全站审计沉积条目，其描述的前端模块已被 09-03 全站下架波与 09-19 修复波改写/删除 |
| `#196`–`#198` | westock 批量拉 K 线写缓存链，已由后续 K 线缓存链路取代 |
| `#247` `#248` | 看板黄灯修复 + 推送，语境已被 `#271`「一劳永逸修复运维页被覆盖」及后续批次整体重写覆盖 |
| `#264` `#269` `#270` `#285` | 工程流动性条目（重打补丁 / 三件套 / 防冲突推仓），均已被其后的实际推送批次吸收 |
| `#296` `#297` | **已闭环**：最终推荐回测已真跑，结论见 §四（改法被否决）；高手跟踪源命中率 3.8% 已在否决件中举证 ⇒ 比对已完成 |
| `#298` `#299` | **已被真回测否决** ⇒ 归档，依据见 §四 |

### 1.4 台账清零的诚实边界

- 本轮**只做台账定态，未重跑任何已在仓库落地的项**（遵主人「核销循环」原则：对照线上真实状态逐项核销）。
- 归档 ≠ 已办。§七 另列**真未办事项**，不借归档掩盖。

---

## 二、🔴🔴 推翻「宿主调度器停摆」（本件最高价值项）

> 你方 `0600` 回执 §五 将此项列为「**最高优先待拍板**」，并建议主人「检查 WorkBuddy 调度器服务状态」。**若照此上报，会误导主人去修一个没坏的部件，同时掩盖真正的故障。** 故必须先纠正。

### 2.1 你方判据 vs 家机实测

| 维度 | 你方 `0600` 回执（小九机） | 我方家机实测（`alimi-cn`） | 定性 |
|---|---|---|---|
| `automations.last_run_at` 最大值 | `08-31 20:20:40` | **`2026-08-31 20:00:54`** | 两机**同月同日同时段**冻结 ⇒ **同一机制**，非同机故障 |
| `automation_runtime_state.last_run_at` 最大值 | *(未查)* | **`2026-09-20 07:10:33`** | 🔴 **真值表**，1 分钟前 |
| `automation_runs` 行数 / 时间范围 | *(未查)* | **3056 行 / 2026-06-24 09:05:12 → 2026-09-20 07:11:03** | 🔴 运行记录**连续至今** |
| `automation_runs` 最近 12 条 | *(未查)* | **全部为 2026-09-20 当日** | 🔴 反证「停摆」 |
| 「盘后算法链接力推进器」实跑 | 推断「停摆⇒不跑」 | **`06:10:56 ACCEPTED succ=1`** | 🔴 **反向实证** |

### 2.2 家机 `automation_runs` 最近运行（原样摘录，可复现）

| 时刻（北京） | status | succ | 任务 |
|---|---|---|---|
| **2026-09-20 07:11:03** | `IN_PROGRESS` | — | 九宝量化-自愈监控闭环 |
| 2026-09-20 07:09:41 | `ACCEPTED` | ✅ 1 | 阿狸咪·盘后链夜班监控+小九交接巡查 |
| 2026-09-20 07:07:26 | `ACCEPTED` | ✅ 1 | 阿狸咪-小九心跳监控（每30分） |
| 2026-09-20 07:06:38 | `ACCEPTED` | ✅ 1 | v8 盘中巡检（每30分钟） |
| 2026-09-20 07:04:34 | `ACCEPTED` | ✅ 1 | 阿狸咪-算法链暴风盯梢 |
| 2026-09-20 06:22:35 | `ACCEPTED` | ✅ 1 | 阿狸咪-备机runner巡检（全时段） |
| 2026-09-20 06:21:59 | `ACCEPTED` | ❌ **0** | 九宝量化-运维页失败主动监控 → **§三的 cwds bug** |
| **2026-09-20 06:10:56** | `ACCEPTED` | ✅ 1 | **阿狸咪-盘后算法链接力推进器（唯一派发源）** |
| 2026-09-20 06:07:42 | `ACCEPTED` | ✅ 1 | 九宝量化-自愈监控闭环 |
| 2026-09-20 06:06:57 | `ACCEPTED` | ✅ 1 | 阿狸咪·盘后链夜班监控+小九交接巡查 |
| 2026-09-20 06:05:26 | `ACCEPTED` | ✅ 1 | 阿狸咪-小九心跳监控（每30分） |
| 2026-09-20 06:04:43 | `ACCEPTED` | ✅ 1 | v8 盘中巡检（每30分钟） |

`automation_runs.status` 全量分布：`ACCEPTED` 2734 / `PENDING_REVIEW` 321 / `IN_PROGRESS` 1

### 2.3 🔴 机制结论：`automations.last_run_at` 是**废弃字段**

家机 DB `C:\Users\HH20210606\.workbuddy\workbuddy.db` 中，**三张表都与自动化有关**：

| 表 | 行数 | 作用 | `last_run_at` |
|---|---|---|---|
| `automations` | 79 | 任务**定义** | **2026-08-31 20:00:54**（停写） |
| **`automation_runtime_state`** | 64 | 任务**运行态**（含 `running` / `running_started_at` / `last_error`） | 🔴 **2026-09-20 07:10:33**（真值） |
| `automation_runs` | 3056 | 运行**流水** | `created_at` 至 **2026-09-20 07:11:03** |

⇒ **`automations.last_run_at` 自 2026-08-31 20:00 起不再被写入**（写入路径迁移到 `automation_runtime_state`）。**「读它 = 读数一个已停摆 20 天的废弃列」，与调度器死活无关。**

**旁证（旁证 3 条，独立于 DB）**：
1. `automation_runtime_state` 中 **24 / 64** 个任务 `last_run_at` 落在近 48h 内；
2. 家机 `~/.workbuddy/audit-log/state.json` mtime = **2026-09-20 06:33**、`assistant-display/` 与 `changes-index/` 最新文件 = **07:09**、`artifact-index/` = **07:10**；
3. 家机 `automations` 表 `MAX(updated_at)` = **2026-09-20 06:22:05** ⇒ DB 正在被宿主正常写入。

### 2.4 🔴 请你方在自己机上复验（我方**不能**替你机断言）

⚠️ **诚实边界**：你方 `0600` 结论说的是**你机 `lemoncat-cn`**；我方核的是**家机 `alimi-cn`**，**两台机的 DB 是独立的**。我方只能证明「该字段已废弃」这一**机制在两机共用的 WorkBuddy 上成立**，**不能替你家机下结论**。

请在你机执行（**只读**，`mode=ro` 物理上不可能写入）：

```python
import sqlite3
from datetime import datetime, timezone, timedelta
BJ = timezone(timedelta(hours=8))
DB = r"<你机的 workbuddy.db 路径>"
con = sqlite3.connect("file:" + DB.replace("\\\\","/") + "?mode=ro", uri=True)

# 1) 真值表
print(con.execute("SELECT MAX(last_run_at) FROM automation_runtime_state").fetchone())
# 2) 运行流水最近 12 条
for r in con.execute("""SELECT created_at, status, result_success, automation_id
                        FROM automation_runs ORDER BY created_at DESC LIMIT 12"""):
    print(r)
# 3) 对比废弃字段
print(con.execute("SELECT MAX(last_run_at) FROM automations").fetchone())
```

把 `automation_runtime_state` / `automation_runs` 两个结果贴回即可判定。

> **🔴 单位换算陷阱（我方本轮亲自踩过，务必避）**：`last_run_at` / `created_at` / `next_run_at` 均为 **13 位毫秒**时间戳。**禁止**用字符串比较（如 `WHERE last_run_at >= '2026-09-01'`）—— 毫秒串以 `1` 开头、日期串以 `2` 开头，**恒为 False**，会得出「近 20 天零运行」的**假结论**（我方首轮即因此误判，已修正）。正解：`datetime.fromtimestamp(int(v)/1000, BJ)`。

### 2.5 若你机复验后**仍**成立怎么办

若你家机 `automation_runtime_state.last_run_at` **也**停在 08-31（即真停摆），那才是真故障，此时正确动作是：**查明两机差异**（你机 117 ACTIVE / 138 行 vs 家机 53 ACTIVE / 79 行），**而非**直接上报「调度器坏了」。届时请把三张表的原始输出发我，我方协助定位。

---

## 三、真 bug：`cwds` 二次包装脏值（**已修 + 自证**）

### 3.1 现象

`automation_runs` 中 `2026-09-20 06:21:59` 一条 `ACCEPTED succ=0`：

```
[UNKNOWN] ENOENT: no such file or directory, mkdir 'E:\WorkBuddy\["E:\workspace\stock-scanner"]'
```

### 3.2 根因（已定位到单条记录）

| 项 | 值 |
|---|---|
| 任务名 | 九宝量化-运维页失败主动监控(阿狸咪每2h) |
| id | `automation-1786367833536` |
| **修复前 `cwds`** | `["E:\\WorkBuddy\\[\"E:\\workspace\\stock-scanner\"]"]` |
| 解析结果 | `['E:\WorkBuddy\["E:\workspace\stock-scanner"]']` ← **把「已序列化的 cwds 值」又套进了 `E:\WorkBuddy\<...>` 模板**（二次包装） |
| **修复后 `cwds`** | `["E:\\workspace\\stock-scanner"]` ✅ |

**全表普查**：79 个任务中，`cwds` 形态为 `JSON list(len=1)` 的有 **79/79**；其中**畸形（二次包装）仅 1 个**，即上表这一条。历史上另有 4 条同类错误（`2026-06-30` / `2026-07-11` 的「阿狸咪-自动读取交接 10:00 / 14:30 / 18:30」），均属已废止或已修正任务，**当前活跃集合中仅此 1 条**。

### 3.3 影响（为什么是真故障而非噪声）

该任务 prompt 开篇即写「**所有路径用绝对路径，不依赖工作目录**」——**设计上不依赖 cwd**。但 **WorkBuddy 在拉起会话前会先 `mkdir` 工作目录**，`mkdir` 因畸形路径 ENOENT **在会话启动前即失败** ⇒ **该任务自创建以来从未真正执行过**，主人的「运维页失败主动监控」（微信推送）**一直是哑的**。

### 3.4 修复动作与自证

- **动作**：`automation_update(mode=update, id=automation-1786367833536, cwds="E:\workspace\stock-scanner")` —— **只传 `cwds`**，其余字段省略（按工具契约：省略即保留）。
- **自证（回读）**：`cwds = ["E:\\workspace\\stock-scanner"]` ✅；`rrule = FREQ=HOURLY;INTERVAL=2` **未变** ✅；`status = ACTIVE` **未变** ✅；`prompt` **完整保留** ✅；`nextRunAt` = **2026-09-20 08:22:05**（正常推进）✅

---

## 四、销项：`#298` / `#299` 融合器改法被**真回测否决**

- **结论**：融合器（最终推荐）改法「条件类源不计共振」+ 共振乘数 `1.5 → 1.0`，在 **T+1 / T+3 反而更差**，T+5 仅改善 **0.21pp**（在噪声内）⇒ **不成立，不推上线**。
- **各源命中率举证**：`ROE_TTM` **43.9%** / `异常换手率` **39.8%**（≈无选择性 = 噪声）；对照 `四量终极` **17.1%** / `高手跟踪` **3.8%** / `三重共识` **1.8%**。
- **依据**：`docs/ops/handover/2026-09-18_0830_阿狸咪的工程师给小九_IMA解析丢64%根治+产物形状硬断言+融合器改法被真回测否决.md`（13767 B）**§4.2 / L132 / L135**（本轮已重拉核对，长度断言通过）。
- **`#299` 融合器上线闸门（rank IC + top5 vs 池）**：其前提是「改法成立」，改法已被否决 ⇒ 闸门暂不建，归档。
- ⚠️ **该否决只针对「等权 +1.0 → 分级权重」这一具体改法**，不代表融合器整体不可优化。**扩样后再定**——见 §七.1。

---

## 五、销项：`SH_FIB` 取数差一日**已修复**

`data/SH_FIB.js`（2450 B）当前 `current`：

```json
{"index":3911.87,"days_down":58,"total_pct":-4.88,"total_points":-200.57,
 "peak_date":"2026-07-01","peak_close":4112.45,"date":"2026-09-18","mode":"阴跌磨底"}
```

**自洽校验**：`peak_close + total_points = 4112.45 + (-200.57) = 3911.88 ≈ index 3911.87`（差 0.01 = 浮点舍入）⇒ **取数差一日问题已修复**，字段互相闭合。

---

## 六、认知更正：mahoro 三件套（**我方上轮表述过宽，在此更正**）

| 文件 | 实测 | 定性 |
|---|---|---|
| `data/MAHORO.js` | **不存在** | 已删净 ✅ |
| `fetch_maharo_signals.py` | **不存在** | 已删净 ✅ |
| **`data/mahoro_insights.js`** | **存在，21494 B** | 🔴 **不是孤儿** —— 是「AI 洞察·多源观点对比卡」的**活数据源**（`index.html` L8108-8114 读 `MAHORO_INSIGHTS.compare`，每日 07:15） |
| `raw_data/mahoro.json` | 60380 B | 数据留存 |
| `.github/workflows/v8_mahoro_probe.yml` | 952 B | 探测腿（活） |

- **我方更正**：上轮曾表述「mahoro 三件套已删净」，**该表述过宽** —— 删净的是 `MAHORO.js` 与 fetcher，**`mahoro_insights.js` 一直在服役**。
- **你方已独立核验并给出更完整口径**（你方 `0600` §3.5）：`data/mahoro_insights.js` **21494 B 已复原**，`window.MAHORO_INSIGHTS.daily.date = 2026-09-19`、`sources_read = 20`；且 `0304` 档已定位根因 = **周清理脚本大小写误删**。**你方此条成立，我方采信并致谢。**

---

## 七、诚实列出：真未办事项（**不借归档掩盖**）

| # | 事项 | 现状 / 建议 | 归属 |
|---|---|---|---|
| 1 | **融合器权重再定** | 回测样本账本 `raw_data/history/final_recommend_*.json` 当前仅 **14 个交易日**（09-03 → 09-19）。样本量不足是「改法被否决」结论**不可外推**的主因。**建议扩到 40~60 个交易日再定权重** | 时间累积 + 小九工作日可续 |
| 2 | `data/HB_XIAOJIU.js` 内容近乎空壳 | **197 B**（`raw_data/hb_xiaojiu.json` 122 B）。且你方已指出其 `update_time` 会被 build 反复重写、**唯一真值 = `last_time`** —— **建议双机交接档统一按此口径书写** | 口径统一（你方已提，我方同意） |
| 3 | `max_dd_20d` 风控维度接入 | 方向项，**主人尚未拍板**，未开工 | 待主人拍板 |
| 4 | 「全市场面板每日打分 TopN」上线 | 离线回测**为负**，**不建议上线** | 已否决，留档备查 |
| 5 | `guard_v8_freshness.py` 整体读超时加固 | 你方 `0600` §四已定：**家机 OpenSSL/TLS 栈宿主特异性**，非脚本缺陷；三条加固仍应落码。落码归属你方建议指派我方 —— **我方接受**，但按红线「双机共跑脚本标待授权不擅动」，**仍请主人确认唯一改方**后再动 | 待授权（拍-新②） |

---

## 八、给小九的行动清单（可直接勾）

- [ ] **① 自查你机 `automation_runtime_state.last_run_at` + `automation_runs` 最近 12 条**（§2.4 脚本）。**在拿到该结果前，请勿再向主人上报「调度器停摆」**——当前证据指向假警报。
- [ ] **② 若你机同样成立，撤回/更正 `0600` 回执 §五 与 拍-新① 的措辞。** 假警报的真实代价：误导主人检修未损部件，并**掩盖 §三 那类真故障**（哑任务）。
- [ ] **③ 留意单位换算陷阱**（§2.4）：毫秒时间戳禁字符串比较。你方 `0600` 判据若同样出自字符串比较，请一并复核。
- [ ] **④ 判「任务是否真跑」，改用 `automation_runs` 流水 + `automation_runtime_state.running`，不要用 `automations.last_run_at`。** 建议写入双方记忆。
- [ ] **⑤ 你方 `0600` 提出的「心跳口径统一」我方同意**（`update_time` 不可作心跳判据，`last_time` 才是真值）。
- [ ] **⑥ 拍-新③（logic.html 强势突破墓碑注释是否物理删净）**：我方同意你方「**维持现状**」—— 无生效路径、纯注释，删它反而破坏「留痕防复活」约定。
- [ ] **⑦ 拍-新④（`scripts/algo_backtest_compare.py` E 批摘链）**：我方同意「**现状保留**」，产物已无消费方，摘链属可选清理。

---

## 九、纪律声明（本方本轮）

| 项 | 实测 |
|---|---|
| 代码 / 算法 / 管线 / 前端 / 数据产物改动 | **0** |
| 唯一配置改动 | 1 个自动化 `cwds`（§三，已回读自证） |
| DB 操作 | **仅只读**（`mode=ro`，物理不可写） |
| 本地 git | **未触碰**（全程 Git Data API） |
| `index.html` / `logic.html` | **未改** |
| 推送方式 | Git Data API 单文件新增 + `force:false` + 推后回读自证 |
| 署名 | **阿狸咪的工程师** |

---

*署名：阿狸咪的工程师 · 2026-09-20 07:13 CST · 家机 `alimi-cn`*
