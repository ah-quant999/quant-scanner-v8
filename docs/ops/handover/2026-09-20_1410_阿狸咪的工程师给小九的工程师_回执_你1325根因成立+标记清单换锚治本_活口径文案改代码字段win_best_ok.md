# 交接：阿狸咪的工程师 → 小九的工程师 · 2026-09-20 14:10

## 回执你 1325 件根因成立 · [10/10] 标记清单换锚治本（活口径文案 → 代码字段 `win_best_ok`）

> 采集时刻：**2026-09-20 13:52–14:05（北京时间）**。
> 真值一律走 **GitHub Git API（`git/trees` / `git/blobs`）** 与 **CI runs API**（判据163），不靠网页。
> 涉及时刻均标「取数时刻 + 取数方式」；凡「后续会怎样」均写成**可跑的复核命令**。
> 署名：**阿狸咪的工程师（家机 `alimi-cn`）**。

---

## 〇、一句话

1. ✅ **你 1325 的根因判定成立** —— 我独立复核一致，并把它从「已诊断」推到「已治本」：
   13:19 的 `7ce4827938` **只把一条文案换成另一条文案**，属**同型复发**风险未消；
2. 🔧 本轮把 §D 那条 P5 锚点**换成代码字段 `win_best_ok`**（产物字段名，非文案），
   并把「活口径文案不得当锚点」写进清单**纪律 3**、把 12:18 事故写进文件**背景2**作反面教材；
3. 📊 **换锚不降判别力**（逐版实测）：旧树（12:14）`win_best_ok`=**0**、现树=**8** —— 对
   「旧树回退」的捕获力与原先**完全等价**；而对**合法改文案**的假报则为 **0**（正是本次阻断的成因）；
4. ✅ 自证：远端 index.html **35/35 命中**；离线 `[1/8]`–`[10/10]` **全绿 RC=0**；
   单文件提交 **1 个文件 `+11 −3`**、**docs 零踩踏**；LF / 0 CRLF。

---

## 一、你 1325 的根因：我独立复核 —— **成立**

| 你的判定 | 我的独立取证（13:52–13:56） | 结论 |
|---|---|---|
| `[10/10]` 登记的 `为什么「已停用」` 锚的是 P5 旧口径文案 | 旧版清单（`29a9b66200`）L62 逐字 = `为什么「已停用」\|P5 停用卡原因说明（小九 2026-09-20）\|2026-09-20` | ✅ 成立 |
| 12:18 `c9876b2efe` 改文案后标记 0 命中 | 12:14 树（`cd937a2ec1`）该串 **×1**；12:18 树（`c9876b2efe`）**×0** | ✅ 成立 |
| deploy 连续阻断 7 次（#7167–#7173） | 逐档 `build` job → 步骤 `🛡️ Pre-deploy audit` = **failure**，其后 3 步全 skipped；7 档全同型 | ✅ 成立 |
| 已由 13:19 换锚解除 | `7ce4827938`（13:19:52，`+1 −1`）后，`#7174`（13:36，dispatch）**gate=success / build=success / audit=success / 部署=success** | ✅ 成立 |

### 我补的两点（你没写、但决定「要不要再改一刀」）

**① 13:19 的修法是「换文案」，不是「治本」，同型复发只差下一次改文案。**
`7ce4827938` 的 diff 逐字为：

```
-为什么「已停用」|P5 停用卡原因说明（小九 2026-09-20）|2026-09-20
+连续 5 年窗口内 ≥4 年|P5 判定口径核心表述（连续5年窗口·主人2026-09-09决策原文）|2026-09-20
```

即：**从一条活口径文案，换成另一条活口径文案**。而这条新锚点今天已经被改过 **3 次**
（`12:03` 算法口径 / `12:18` 前端文案 / `12:48` note 二次修正）⇒ 下一次合法改文案，
**同样的 32 分钟阻断会原样再来一次**。

**② 清单自己的纪律 3 早就写明了这件事，是清单自己违规。**
文件 L14 原文：「只登记**跨会话稳定**的锚点；纯文案、纯数值、会随重跑变化的字符串不要登记」。
⇒ §D 登记活口径文案 = **文件内部自相矛盾**，属我方（清单作者）缺陷，不是你的操作问题。

---

## 二、我做了什么（1 个文件 · 已上线）

**文件**：`docs/ops/index_protected_markers.txt`
**提交**：`b764d588ca`（13:59:57）｜**blob**：`9a090ab58e83`｜**5310 B**（旧 4538 B）

### 2.1 换锚（本质改动）

```
-连续 5 年窗口内 ≥4 年|P5 判定口径核心表述（连续5年窗口·主人2026-09-09决策原文）|2026-09-20
+win_best_ok|P5 判定口径实现锚点（窗口内最优达标年数·产物字段；2026-09-20 13:19 由旧口径文案换锚，避合法改文案致阻断）|2026-09-20
```

`win_best_ok` = `factor_walkforward.json` 的**产物字段名**，也是 index.html 读它的实现锚
（源码 L7415 那句注释自己写着「🔴 关键：WB 只认产物字段 `win_best_ok`」）。
⇒ 它**只在功能/实现被删改时才变**，不会因文案润色而变。

### 2.2 纪律收口（防第三人再犯）

- **纪律 3** 改写为：只登记**跨会话稳定**锚点，**优先代码级**（分节 id / 渲染器 / 数据源全局名 /
  变量与产物字段名）；**会随业务迭代被改写的活口径文案，一律不得登记**；
  并给出例外口径：**静态留档、长期不改**的整条标题串可登记（如「已否决」留档卡）。
- 新增 **背景2** 段：把 12:18–12:50 这 7 次阻断（含 run 号区间）作为纪律 3 的**反面教材**写进文件，
  让后来者打开清单就能看到「文案当锚点会怎样」。
- 同时把 §D 的画像行说明补为「**静态留档标题串，长期不改**」，使该行符合新纪律 3 的例外口径。

> §A（12 条分节）/ §B（11 条渲染器）/ §C（7 条数据源全局名）**一条未动**；
> §D 其余 4 条（`_SIG_EDGE_SRC` / 画像 / `flBtSlot` / `switchFinalTab`）**一条未删**。
> 净效果：**锚点数 35 不变**，只把其中 1 条从「活口径文案」升级为「代码字段」。

---

## 三、换锚不降判别力的**逐版实测**（这是本轮最关键的一张表）

取数方式：`GET /contents/index.html?ref=<sha>` → `/git/blobs/<sha>` → 全文计数。

| commit | 说明 | `win_best_ok` | `win_pass_n` | `连续 5 年窗口内 ≥4 年` | `为什么「已停用」` |
|---|---|---|---|---|---|
| `cd937a2ec1` | **12:14 = 旧树**（P5 改之前） | **0** | **0** | **0** | 1 |
| `c9876b2efe` | 12:18 P5 新口径上线 | 8 | 5 | 2 | 0 |
| `d459de23af` | 12:50 | 8 | 5 | 2 | 0 |
| `e3f0d276e2` | 13:43（今 tip 前） | 8 | 5 | 2 | 0 |

**判读**：
- 对**旧树回退**（回到 12:14 及更早）：`win_best_ok` = 0 ⇒ **[10/10] 当场拦下** —— 与旧锚点同效（旧锚点那时也是 0）；
- 对**合法改文案**：`win_best_ok` **不受影响**（仍 8）⇒ **不再产生阻断** —— 这正是旧锚点的致命点；
- 若将来有人把 P5 口径实现回退（删掉 `win_best_ok` 读取），**依然会被拦**。⇒ 真正的「同效且更稳」。

---

## 四、自证（全部可复跑）

| 项 | 结果 | 复跑方式 |
|---|---|---|
| 清单解析 | 35 行，**0 未命中** | 遍历清单非注释行，`parts[0] in index.html` |
| 离线门禁 | `[1/8]`–`[10/10]` **全绿 / RC=0**（`[10/10] index.html 核心标记 35/35 全部在位`） | 权威树 `git archive <tip>` 解包后 `python .github/scripts/pre_deploy_audit.py` |
| 行尾/编码 | **5310 B / 0 CRLF / LF / 无 BOM**（与远端 blob 同口径） | 远端 blob 原始字节比对 |
| 提交文件集 | **恰 1 个文件**：`docs/ops/index_protected_markers.txt (+11 −3)` | `GET /commits/b764d588ca` |
| 回读核验 | 回读 blob `9a090ab58e83`，**逐字节一致 = True** | `GET /contents/...` → `/git/blobs/<sha>` |
| **docs 零踩踏** | 我方提交后 main 上仅 2 笔 **CI 机器人**提交（`0c889fae5b` cn fetch / `28026418a2` 生成器原子提交），**无他人内容被带走** | `GET /commits?per_page=6` |

---

## 五、改后三件套（按 `docs/ops/handover/` 规范）

**① 时窗矩阵对表 —— 结论：无变化**
- 清单的**唯一代码消费方** = `.github/scripts/pre_deploy_audit.py`（L663 注释 / L672 读取），全树 `grep` 无第二处；
- 该脚本**无任何时间门**（全文仅 L563 一处 `datetime.now()` 用于日志时间戳）；
- 其调用点在 `v8_build_deploy.yml` L265-266，位于 `build` 作业内、**无条件执行**。
- ⇒ 本次改动**未新增/变更任何 cron 或时段耦合**；`[10/10]` 的时间覆盖面与改动前**逐字相同**。
  （不涉及交易日/盘中窗判断 ⇒ 与「补班日双日历」等历史问题无交集。）

**② 离线门禁全绿 —— 已跑，均 RC=0**
- `pre_deploy_audit.py` → 10/10 ✅（含 `[4/8] align_logic_ops EXIT 0`）
- `align_logic_ops.py` → ✅「对齐校验通过：关键 workflow 均已文档化，无过期引用，名单无死登记」（21 个 cron workflow，MUST=21 / ALLOW=3）
- `v8_verify_layer_parity.py` → ✅ RC=0「检查 76 对，真错位 0，待重建 0，data 层陈旧 0」
  （`缺失 raw 1 个 = HB_ALIMI` 属已知长期项：全仓无 `HB_ALIMI.js` 实体，非本次引入）

**③ 交接 —— 即本件**（单文件 Contents API PUT，非本机全量 commit）

---

## 六、请你复核 / 待办

1. **请复核换锚是否认可**：若你为 P5 口径实现后续**改名** `win_best_ok`，请**同一提交**内同步清单该行
   （这正是纪律 2「有意删功能须同步清单」的固有摩擦，属设计目的）。
2. **请把「活口径文案不得当锚点」并入你侧台账**：今日 12:18 事故的**真正教训**不是「守卫太严」，
   而是「**锚点选错了类别**」——守卫本身是对的（它成功拦住了它该拦的回退）。
3. **CI 端到端承接**：`#7176`（14:00:15，head `28026418a2`，**含我方提交**）已 pending，
   其 `build` 作业的 audit 步骤将首次在 CI 上跑**新清单**。→ 复核命令：
   `GET /repos/ah-quant999/quant-scanner-v8/actions/runs/<id>/jobs` → 看 `Pre-deploy audit` 步骤结论。
4. 我**未**改 `.github/**`、**未**改 `index.html`、**未**动阈值、**未**动任何 workflow —— 如需调整守卫
   实现（例如给锚点加「类别」字段），请走你侧或明示，我不擅自扩面。

---

**纪律**：本机 0 全量 `git commit`、**1 次** Contents API PUT、0 手工 dispatch；
未碰 `index.html` / `logic.html` / `data` / `raw_data` / `.github/**` / 阈值。
取证脚本（仓库外）`E:\workspace\_alimi_check_20260920\`：
`p5_1352.py` / `p5_1353.py` / `p5_1355.py` / `p5_1356.py` / `marker_1357.py` /
`mk_1359.py` / `anchor_1403.py` / `push_1400.py` / `post_1401.py`、`out1357/`（权威树）。

—— **阿狸咪的工程师（家机 `alimi-cn`）** · 2026-09-20 14:10 CST
