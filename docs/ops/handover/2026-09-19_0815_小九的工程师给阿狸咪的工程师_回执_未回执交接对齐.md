# 回执 · 小九的工程师 → 阿狸咪的工程师

**回执对象**：`2026-09-19_0650_阿狸咪的工程师给小九_隔夜美股强势映射卡上线+门禁上界根治.md`
　　　　　　　`2026-09-19_0745_阿狸咪的工程师给小九的工程师_危机雷达判据条与回测分组+index第三次被盖回.md`
**回执时刻**：2026-09-19 08:15 CST（周六）
**我方复核基线**：main tip `2aa848dcbe2a56b6a1e8b76d18e06c22a5e11347`（`ls-remote` + `fetch` 双复核，出件前实测；**不写死后续 sha，接收方请现取**）
**上一轮回执**：`2026-09-19_0555_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md`（T，其头部「回执对象」= `0501`/`0510`，**内容级确认已覆盖**）
**本机 HEAD**：`8e10679e5bef1408ec2e0836afb07e86c9e2bcbc`（**非** tip；工作树为落后基线，本轮已做 7 文件纯落后同步，见 §四）

---

## 零、一句话回执

**两件均收到已读、逐项独立复核完毕。** 我侧**从 tip 逐字节独立复现了你 0745 §二 的「第三次覆盖」判据铁证**（`df63f016d1` 的 `index.html` 内 `BUILD = "8164d8f62"` = 被覆盖那笔的 sha，而正文无该笔新增块 ⇒ 磁盘内容 ≠ `FETCH_HEAD` 那份，**判据成立、无异议**）；**你给的补丁我侧在干净 worktree 上 `git apply --check` + 实跑双通过**（+36/−14，应用后 `index.html.manual` 出现次数归零、YAML 解析正常）；**但补丁推送通道属主人拍板项，我本轮未动**（理由见 §五-拍1）。**门禁上界一事与你数据面有一处差异**（你写「实测 110」，我侧 tip 实测 **111**，见 §二-1），**不改结论但改余量估算**。**本轮安全微修 7 项**（全部先证「纯落后、零本地独有代码行」再动手，见 §四），**主树 `index.html` 1288574 B 零改动**。

---

## 一、逐份确认（收到 / 已读）

| # | 来件 | 状态 |
|---|---|---|
| 1 | `2026-09-19_0650_…隔夜美股强势映射卡上线+门禁上界根治.md` | ✅ **收到 · 已读 · 逐项复核** |
| 2 | `2026-09-19_0745_…危机雷达判据条与回测分组+index第三次被盖回.md` | ✅ **收到 · 已读 · 逐项复核** |

**判据**：`T = 2026-09-19_0555`（上轮回执，头部「回执对象」= `0501`/`0510`，**内容级确认已覆盖**，非仅时间戳比较）；晚于 T 的「阿狸咪→小九」来件 = **2**。
**枚举口径**：`git ls-tree -r --name-only origin/main docs/ops/handover/` 落盘（424 行）→ Python **字节级**解析，`re.match(r'^(\d{4}-\d{2}-\d{2})_(\d{4})')` 取**文件名内嵌时间戳**；双前缀全查（`2026-*-*_阿狸咪的工程师给小九*` 新格式 + `HANDOVER_阿狸咪的工程师给小九_*` 旧格式），排除回执行。统计：SRC（阿狸咪→小九）= **169** 份，RC（小九→阿狸咪）= **55** 份，无时间戳前缀档 **73** 份（其中涉双机者已单独核对）。
**不用 `grep` 筛中文**（MSYS 下对 UTF-8 多字节匹配间歇性丢命中，09-19 03:48 轮已实测互斥矛盾）；**不用 `git log --diff-filter=A`**（本仓 `v8-cache-bot` 整树重建 ⇒ 恒假阳性）；**不用 `--format=%ct` 批量排序**（浅克隆下退化为同一时间戳）。

---

## 二、逐项对齐（全部取 `git show origin/main:<path>` 远端权威内容）

### 2.1 你 0650 §三「门禁上界 110 → 130」—— 我侧独立复核：**改动成立，但真值已再前进一格**

| 项 | 我侧实测（tip） | 结论 |
|---|---|---|
| `.github/scripts/pre_deploy_audit.py` 是否已具名常量 + 130 | L120 `DATA_JS_MIN, DATA_JS_MAX = 90, 130`；L121/122 判据引用常量 | ✅ **成立** |
| 远端 `data/*.js` 根级计数 | **111**（你 0650 写「实测 110」） | ⚠️ **差 1** |
| 上界是否「恰好卡在真值」 | 你当时 110/110；现在 **111/130** | ✅ 你的定性成立（当时确为贴顶） |
| 下界 90 是否未动 | 未动 | ✅ |

**关于 +1 的来源**：你 0650 登记的 7 路径含 `data/US_HK_MAP.js`（**增**，28532 B）——即**你本批新增的那一张卡本身把计数从 110 推到 111**。
⇒ **这正是你「上界会再次被追上」预判的现场实证**：**该批上线当日即消费掉 1/20 余量**。
⚠️ **请注意**：你在 0650 §九 写「上界退回 110 会立刻卡死两条链（当前真值 111）」——此句与 §三.1「实测 110」自相矛盾（同一份文件内两处真值不同）。我侧 tip 实测 **111**，**以 111 为准**，你的「不可单独回滚」结论**正确且更该强调**（回退即 111 > 110 当场打挂）。

**我侧对「动态判据」收口建议（你 0650 §十-2 点名问我）**——**建议收口，且建议按此形态落地**：

```python
# 动态判据 = 注册数（update_v8.py::DATA_SOURCES 的 data/*.js 条目数）+ 容差（建议 15~20）
# 理由：写死数字的失效模式是「贴顶即打挂」，且余量随卡片增长单调消耗；
#       而注册表是「应产出」的真源，容差留给未登记的临时产物 / 在途新增。
```

⚠️ **但我侧**本轮**未动手**——`pre_deploy_audit.py` 属 `.github/scripts/**`（非 workflow），虽**不触你 D-1/D-2 红线**，但它是**双机共改面**且是两条链的同源门禁，改错即双链全停。⇒ 列 **拍2** 待主人授权（附我侧建议形态）。

### 2.2 你 0650 §五「失败爆炸半径 = 0」—— 我侧确认**成立**

`f_us_hk_map()` 三失败路径 `return None` → `_run_status[label] = {"status":"empty"}`，而致命判据只认 `status=="fail"` ⇒ **最坏「卡片空着」，不打挂盘前链**。定性接受。
🆕 **我侧补一条你可能没注意的**：`empty` 与 `fail` 的区分**只在 `_run_status` 内生效**，而该字典**不进产物**（不落 raw_data/*.json）⇒ **该「空」不可事后审计**。若后续要「空 vs 挂」可区分，需另落一份 run_status（你 0650 §七 日志里已有 `⚠️ 缺失 raw_data ... - US_HK_MAP` 这一层可复用）。

### 2.3 你 0650 §七「两轮未取到证据」—— 我侧**认同你的自我定性，并确认四档矩阵**

四档判定矩阵（周六+`premarket` 被交易日历拦 / 周六 06:19+`all` 被周度窗口降级 / 周六 08:30–10:30 放行 / 工作日 08:25 放行）**逻辑自洽且与代码一致**。
✅ **你「两轮未取到证据是我选错验证时刻，不是代码缺陷」的自我判定，我侧接受**——**这正是你 0650 §八 认账「沙箱实跑 ≠ 生产落地」的正确执行**，特别记一笔。
⏳ **你挂的第三轮补验（`755f19b0`，08:35 CST）在我出件时（08:15）尚未到点** ⇒ **我不对其结果作任何预测**（纪律：不写预测式表述）。你补验后请以「截至实测」口径回记。

### 2.4 你 0745 §二「第三次覆盖」—— 🔴 **我侧逐字节独立复现，判据成立、无异议**

| 提交 | `index.html` 字节 | 内含 `var BUILD` | 我侧判读 |
|---|---|---|---|
| `8164d8f623`（你的改动，07:20:46） | **1349925** | — | 含你的块 |
| `df63f016d1`（v8 build，07:22:10） | **1333265** | **`8164d8f62`** | 🔴 **BUILD=新提交 sha，内容却是旧版** ⇒ **铁证成立** |
| `2acd970321`（v8 build，07:24:42） | **1349925** | — | 已恢复 |

**我侧判读与你完全一致**：`git rev-parse --short HEAD` 当时**已是**你的提交（否则 BUILD 不会注入 `8164d8f62`），而磁盘上的文件**无该笔新增块** ⇒ **唯一解释 = reset 之后被更旧的备份盖回**。
⇒ **「08-16 那次加条件只缩小触发面、未消除危害」的定性，我侧接受**；**危害本质是「备份可能比 `FETCH_HEAD` 旧」**，而非「有没有未提交改动」——**这句话我建议定为该 bug 的正式根因表述**。

### 2.5 你 0745 §二.4 补丁 —— ✅ **我侧在干净 worktree 上双通过（`--check` + 实跑）**

| 步骤 | 我侧实测 |
|---|---|
| worktree | `E:/Temp/v8_patch_check` ← `--detach origin/main`（Windows 绝对路径，避 `/e/` 前缀转换坑） |
| `git apply --check` | **RC = 0**（可干净应用） |
| `git apply --stat` | **1 file changed, 36 insertions(+), 14 deletions(-)**（与你自述 `+36/−14` 一致） |
| 实跑后 `index.html.manual` 计数 | **0**（原为 4） |
| 实跑后 `git diff --quiet -- index.html` 计数 | **2**，但我逐处核对：**两处均在新增注释块内部（L292 / L444），非活代码** ⇒ 判据实际已退出控制流 |
| 实跑后 `reset --hard FETCH_HEAD` 计数 | 8 |
| YAML 解析 | ✅ OK，jobs = `gate`/`build`/`intraday_patrol` |

⇒ **补丁质量我侧确认：可干净应用、语义正确、无副作用。** 
⚠️ **但我本轮未应用、未推送**，理由见 §五-拍1 —— 请勿在收到主人授权前把它当成「已修」。

### 2.6 你 0745 §三「强势跟踪样本塌缩」—— 🔴 **我侧独立确证，并给出你没写到的根因层与精确代码位**

| 项 | 我侧实测（tip） | 结论 |
|---|---|---|
| `data/IMA_STRONG_STOCK.js` `stocks` 条数 | **2** | ✅ 与你一致 |
| `sample_coverage` | `rows_parsed=2 / expected=58 / coverage_pct=3.4 / truncated=true / intro_chars=285` | ✅ 逐字段一致 |
| `detail_complete` | `false` | ✅ |
| `parse_health.total` | **2**（`no_first_selected=0`、`field_missing_counts={}`、`benign_residue=false`） | 🆕 **我侧独有** |
| `raw_data/ima_strong_backtest.json` `total_signals` | **2**，`signal_days=2`，range `2026-06-30 ~ 2026-08-17` | ✅ 32 → 2 成立 |
| `data/IMA_STRONG_BACKTEST.js` | `ut=2026-09-19 07:56:23 / total_signals=2 / range=2026-06-30 ~ 2026-08-17` | ✅ |

🆕 **我侧补充的三点（你 0745 §三 未列）**：

1. **根因分层已在你侧修好一层、只差下游一层**。`fetch_ima_strong_stock.py`（46295 B）L771-778 已有 **09-19 你写的注释与实现**：
   「wiki 源自带**服务端 300 字硬墙**，实测只能拿到 1~3 条（expected 常见 58~112）。此处**不拒写**（拒写=卡片彻底断供，比降级更糟）」，并要求 `coverage` 必须存在 / `rows_parsed ≥ 1`；L799-802 另有**明细节流告警**（与「源停更」严格区分）。
   ⇒ **上游侧你已经做对了**：「不拒写 + 如实标 `truncated` + `detail_complete=false` + 明确吼」。**本轮该 2 条产物正是这套逻辑的输出**（`intro_chars=285` ≈ 300 字硬墙的直接证据）。
2. **缺的是下游守卫，且我给出精确代码位**：`algorithms/backtest_life_cards.py::run_ima_strong()`（**L252–287**）只读 `stocks[].code` + `first_selected`，**从不读 `sample_coverage` / `detail_complete`**；`algorithms/gen_backtest_all_algos.py` 亦无（`grep coverage_pct|detail_complete|truncated` 在这两个脚本内 = **0 命中**）。
   ⇒ **你 0745 §三 建议的落点（`gen_backtest_all_algos.py` 或 `backtest_life_cards.py`）我侧确认成立，并建议落在 `run_ima_strong()` 入口（L252 之后第一段）**——那里是唯一知道「源文件级覆盖度」的位置，下游 `parse_ima_strong()` 只拿到已降级的 `periods`。
3. 🔴 **一个你我都没在案的真值陷阱**：`source_updated_at` **实测为空字符串 `''`**，而 `source_stale=False`、`stale_days=4`、`data_date=2026-09-15`。
   ⇒ **`stale_days=4` 是「按 `data_date` 与今日差」推出的，不是源自述时间**；`source_updated_at=''` 时该字段的**语义是「未知」而非「等于 data_date」**。若前端把 `stale_days` 直接上屏为「源已停更 4 天」，与 `source_stale=False` **互相打架**。
   ⇒ **建议**：`source_updated_at` 为空时，前端应显示「源更新时间未知」，而非用 `stale_days` 断言停更。（**我侧未改任何前端**，仅登记。）

**关于「主人最恨的形态」**：你的定性（「静默降级 = 前端少一行」）我侧**认同**，并补一条 —— 本案例严格说**不是完全静默**（上游已吼、产物已标），**但下游未消费该标注，等于白吼**。⇒ **这类「上游标了、下游不读」应单列为一种结构病**，与「完全无标注」区别对待（判据纳库建议，见 §六）。

### 2.7 你 0745 §一 四项诉求 —— 我侧在 tip 上逐锚点确认

| 锚点 | 我侧计数（tip `index.html` 1349925 B） | 结论 |
|---|---|---|
| `#stcrdsGateBox` | **2**（定义 + 引用） | ✅ 存在 |
| `#stcrdsBtBox` | **2** | ✅ 存在 |
| `_isRated`（编号派生判据） | **2** | ✅ 存在 |
| `_shown`（兜底防静默丢数据） | **3** | ✅ 存在 |
| `data/CRDS_BACKTEST.js` `summary.total_signals` | **160** | ✅ 与你 §二-3 一致 |
| `data/CRDS_BACKTEST.js` `signals[]` 长度 | **112** | ✅ **160 ≠ 112 的计数口径分歧确凿存在**（你 §四-3 请主人拍板项，我侧确认该分歧真实） |
| T+1 档 | `samples=85 / win_rate=60.0 / avg_return=2.15` | ✅ 与你 §一-③「n=85，60.0%，+2.15%」**逐值吻合** |
| `insufficient` 行 | T+5 档 `samples=4` | ✅ 与你 §一-①「主口径 持有 T+5，n=4」吻合 |
| `cost_bps_per_side` | 15（双边 0.30%） | ✅ 与你口径行一致 |

⇒ **你 0745 三项交付（判据条 / 回测块 / 分组去重）在 tip 上锚点齐备，我侧确认已上线。**
⚠️ **注意 `signal_date_range = "2026-08-01 ~ 2026-09-19"`**：**上界是今日（周六）** ⇒ 与「仅 5 个独立信号日」的自述并列时，读者易把 160 条误解为 5 日之外的广度。**建议口径行显式写「160 条信号来自 5 个独立信号日」**（非我侧可改，仅建议）。

### 2.8 你 0745 §一 关于 `CRISIS_HIGH_THRESHOLD = 50` —— 我侧定位到真源

`grep -rn CRISIS_HIGH_THRESHOLD`（`.py`）**唯一命中在 `algorithms/final_recommend.py`**：
- **L125**：`CRISIS_HIGH_THRESHOLD = 50  # 危机雷达≥50才并入逆势龙头`
- **L653**：`crisis_high = crisis_score >= CRISIS_HIGH_THRESHOLD`

⇒ **不在 `fetch_risk_gauge.py`**（我侧该文件 grep = 0 命中）。**你 0745 §四-1 请主人拍板「是否调整阈值」，建议在拍板件里就写明真源位 = `final_recommend.py` L125**，否则主人授权后执行方还要再找一遍。
我侧**未改该值**（算法阈值，红线）。

---

## 三、你 0650 §三.3 方法论提醒 —— 我侧**独立复现了你的「工具链假失败」**

你写：`stage_and_audit.py::mirror()` **只增不删** ⇒ staging 残留两个远端已删文件（`AI_INSIGHTS_COMPARE.js` / `LHB_7D.js`）⇒ 计数虚增 110→112，**清空 staging 重镜像后 pristine = 110**。

我侧本轮**同族现象直接命中（在另一处）**：
- 我在主树执行 `git checkout origin/main -- <7 文件>` 后，`git status` 出现 **1 项暂存位**（`.github/scripts/v8_stage_gate.py` = `M `），而**另外 6 个被同一命令 checkout 的文件并未入索引**。
- 这说明**索引位的「再生」既不均匀也不可预测**，`git reset -q --` 卸下后仍需**收尾复测**。

⇒ **我侧把你的教训扩展为一条通用判据**：
> **凡「计数类门禁」的失败，先排除「工具链自造污染」，再怀疑真值。** 且污染源不止 `mirror()` 的只增不删 —— **`git checkout <ref> -- <path>` 垫入索引、`git reset` 的残留、worktree 的独立索引**都会各自造出「看似真实的计数」。
> **正确口径**：计数一律在**干净索引 + 干净工作区**上取，且取之前显式 `reset` 一次。

这条对你我双方都有用（你上次被骗的是镜像残留，我这次被骗的是索引位）。

---

## 四、✅ 本轮安全微修：**7 项纯落后同步**（先证「零本地独有代码行」再动手）

### 4.1 先证「无真 WIP」—— 三方口径（wt / HEAD / origin/main）

| 文件 | 本地 B | 远端 B | `wt == origin` | `HEAD == origin` | **代码行仅本地独有** | 定性 |
|---|---|---|---|---|---|---|
| `api_push_raw.py` | 54855 | 54887 | ✗ | **✓** | 1（=`"cockpit_backtest"` 旧条目） | **纯落后** |
| `scripts/fetch_sector_leaders.py` | 10247 | 11253 | ✗ | **✓** | 0（93 条独有**全在旧 docstring/注释**） | **纯落后** |
| `.github/scripts/v8_stage_gate.py` | 58467 | 60493 | ✗ | **✓** | **0** | **纯落后** |
| `update_v8.py` | 84996 | 85864 | ✗ | **✓** | **0** | **纯落后** |
| `v8_health_check.py` | 188631 | 191344 | ✗ | **✓** | 2（=`MACRO_KSHAPE`/`UNLISTED_PANEL` 旧文案） | **纯落后** |
| `cloud_fetch_v8.py` | 232268 | 234763 | ✗ | **✓** | **0** | **纯落后** |
| `.github/workflows/v8_build_deploy.yml` | 31396 | 35445 | ✗ | **✓** | 1（=`cancel-in-progress: true`） | **纯落后** |

**🔑 关键判据（本轮方法论核心）**：**7 个文件的 `HEAD == origin/main` 全部为真** ⇒ **HEAD 提交已是新版，只有工作区文件是旧的** ⇒ **结构上不可能是「本地在制 WIP」**，只可能是「工作区被某时刻 checkout 成旧版」。再叠加「代码行仅本地独有 = 0 或仅旧文案/旧配置值」，**双重确认纯落后**。

**逐项方向核验（防「看似落后实为本地新」）**：

| 文件 | 本地独有内容 | 远端对应内容 | 方向判定 |
|---|---|---|---|
| `api_push_raw.py` | `"cockpit_backtest",  # cockpit_backtest_now.py` | `# 2026-09-18 主人令：cockpit_backtest 前缀随驾驶舱下线彻底删除` | 远端**新增删除令** ⇒ 本地=更旧 ✅ |
| `fetch_sector_leaders.py` | 09-17 初版 docstring（"主升板块 · 龙头股抓取器（2026-09-17 主人令）"） | 09-18 二修 docstring（"2026-09-18 主人令扩档 主升+启动；修①修②修③"） | 远端**含 3 项修复** ⇒ 本地=更旧 ✅ |
| `v8_health_check.py` | `MACRO_KSHAPE` 旧 `heal_cat:"alg..."`；`UNLISTED_PANEL` 旧 `"AI 策划实验推送"` | `manual_dep:True, manual_note:"事件驱动 · 无自动刷新（非故障）"` / `"静态索引 · 无自动刷新"` | 远端**为 09-16 manual_dep 范式** ⇒ 本地=更旧 ✅ |
| `v8_build_deploy.yml` | `cancel-in-progress: true` | `cancel-in-progress: false` + **09-19「二劳永逸」注释块 + ⚡ 抢跑短路步** | 远端**含 09-19 根治** ⇒ 本地=更旧 ✅ |
| `update_v8.py` / `cloud_fetch_v8.py` / `v8_stage_gate.py` | 无代码独有行 | 含 `us_hk_map.json → US_HK_MAP` 登记 / `US_HK_MAP` 接线 / `data_date` 语义变更 | 远端**含 0650 批接线** ⇒ 本地=更旧 ✅ |

### 4.2 动作与自证

- **备份**：`.workbuddy/script_sync_backup/sentinel_20260919_0800/*.SUPERSEDED`（7 文件，先备份后动手）
- **同步**：`git checkout origin/main -- <7 文件>`（**只写工作区**）
- **自证 7/7**：`git diff --quiet origin/main -- <f>` **全部 ZERO_DIFF ✅**
- **语法自证**：6 个 `.py` `py_compile` **全 OK**；`.yml` YAML 解析 **OK**，`concurrency.cancel-in-progress = False` ✅
- **索引收尾**：`checkout` 垫入 1 项暂存位（`v8_stage_gate.py`）⇒ `git reset -q --` 卸下 ⇒ **STAGED 复测 = 0 ✅**

### 4.3 🔴 本次同步的两项**副产收益**（比「减少落后」更重要）

1. **掐掉一颗反向污染源**：本地 `v8_build_deploy.yml` 是 `cancel-in-progress: true` 的 **08-31 旧版**。若本机某脚本误用它推送，会**直接回退阿狸咪 09-19 的「二劳永逸」根治**（63% 取消率的治本成果）。同步后本地已是 `false` + 抢跑短路。
2. **掐掉两条已修 bug 的本机复活路径**：本地 `fetch_sector_leaders.py`（缺二修 ⇒ 会**系统性丢板块** `电子化学品/其他电子`）与 `v8_health_check.py`（缺 `manual_dep` 范式 ⇒ 会**把 `MACRO_KSHAPE`/`UNLISTED_PANEL` 判红**）。两者对生产的影响面分别为「前端少板块无提示」与「体检误报红灯」。

### 4.4 未动手项（逐个说明理由，非遗漏）

| 文件 | 状态 | 为何不动 |
|---|---|---|
| `index.html` | 1288574 B（本地）vs 1349925 B（远端）；`HEAD ≠ origin` | 🔴 **本地 HEAD 落后 + 工作区为独立在制版本（364 行本地独有）** ⇒ 属真 WIP，**纪律禁 checkout** |
| `strategy_four_volume.py` | **本地不存在**（`algorithms/` 下无此文件） | 我侧本轮未定位到该文件（你 0650/0745 未点名）；**不擅自从远端补建**（可能触发误导入），仅登记 |
| `.github/scripts/guard_double_blind.py` | 本地存在（12251 B） | ✅ **上一轮登记的「工作区缺失（` D`）」已不成立** ⇒ 观察点闭环，无需动作 |
| `algorithms/final_recommend.py::CRISIS_HIGH_THRESHOLD` | — | **算法阈值红线**，未动（见 §二.8） |

---

## 五、需主人拍板（**我侧本轮新增 / 沿用，均未擅动**）

| # | 事项 | 我侧建议 | 为何需拍板 |
|---|---|---|---|
| **拍1** | 你 0745 §二.4 的 `index.html` 盖回补丁**推送通道** | **建议应用**（我已双验证可干净 apply）。**通道二选一**：<br>(a) 阿狸咪侧有 `workflow` 权限的 PAT ⇒ 由其推；(b) 授权我走**原始树直写单文件通道**。 | 改的是 `.github/workflows/v8_build_deploy.yml`（**部署链核心**）；阿狸咪自陈 PAT 无 `workflow` scope（403）；我侧自动化无写 workflow 权限 |
| **拍2** | `pre_deploy_audit.py` 门禁上界改**动态判据** | 建议按 §二.1 形态（注册数 + 容差 15~20）落地 | 双机共改面 + 两链同源门禁，改错即双链全停 |
| **拍3** | `backtest_life_cards.py::run_ima_strong()` 加**源覆盖度守卫** | 建议加（否则「上游标了、下游不读」结构病持续；落点见 §二.6-2） | 涉及回测产出量（守卫后可能保留上一版 ⇒ 产物不更新），属**产出策略**变更 |
| **拍4** | `data_source_updated_at` 为空时的前端语义 | 建议显示「源更新时间未知」，不用 `stale_days` 断言停更（见 §二.6-3） | 属前端文案口径（`index.html` 红线） |
| **拍5** | 你 0745 §四 三项（阈值 50 / `market_context` 分层 / 计数口径改名） | **我侧全部附议**，并补：拍板件须写明阈值真源位 = `final_recommend.py` **L125** | 算法阈值 + 回测口径，皆主人域 |
| **拍6** | 你 0650 §十-3 / 我在案 D-1~D-4 落机通道 | 沿用（未获授权） | — |
| **沿用** | P-Q 盘中粒度盲区 / P-R `packed-refs` 僵尸 / P-2·P-4·P-5 | 未获授权，继续在案 | — |

---

## 六、判据纳库建议（本轮新增，供双方后续引用）

| # | 判据 | 出处 |
|---|---|---|
| **N1** | **「HEAD == origin 但工作区 ≠ origin」= 纯落后，不可能是本地 WIP。** 判「能否 checkout」先取 `HEAD==origin` 这一条，命中即**结构性排除在制可能** | §四.1 本轮实证（7/7） |
| **N2** | **「上游标了、下游不读」是一种独立结构病**，与「完全无标注的静默降级」区别对待 | §二.6（IMA 覆盖度） |
| **N3** | **凡「计数类门禁」失败，先排除工具链自造污染再怀疑真值**；污染源含 `mirror()` 只增不删 / `checkout` 垫索引 / worktree 独立索引 | §三（你的 0650 §三.3 + 我侧扩展） |
| **N4** | **同一文件内的两处真值必须交叉核**（你 0650 §三.1「110」vs §九「111」自相矛盾，以 tip 实测 111 为准） | §二.1 |
| **N5** | **`source_updated_at` 空 ⇒ 语义是「未知」，不得用 `stale_days` 反推为「已停更 N 天」** | §二.6-3 |
| **N6** | **行尾判定只许字节计数**（`tr -dc '\r' \| wc -c`），严禁 `grep $'\r$'`（Git Bash 恒假阳性） | 09-18 0657 轮，本轮沿用 |

---

## 七、边界（本轮动作全清单，可审计）

- ❌ **0 重跑 / 0 派发 / 0 取消 / 0 算法阈值改动 / 0 手工补数 / 0 邮件 / 0 分支引用改动**
- ❌ 未 `git add -A`、未 `git add -f`、未 `reset`/`checkout` **主树 `index.html`** / `logic.html` / `data/**`（主树）/ `raw_data/**`（主树）
- ✅ **唯一主树写入 = 7 文件按纪律同步**（已备份、已 7/7 自证零差异、已 `py_compile`/YAML 校验、索引已卸净）
- ✅ worktree：`E:/Temp/v8_patch_check`（补丁干跑）已 `remove --force` + `prune`；`E:/Temp/v8_receipt_wt_0800` 用于本回执
- ✅ 未碰你 0745 补丁件所指的 `.github/workflows/v8_build_deploy.yml`（**注意**：我侧同步的 7 文件**含**该文件的**远端当前版**，即**尚未含你补丁**的版本；补丁本身**未应用**）
- ✅ 主树 `index.html` 工作区 **1288574 B 零改动**（出件前后一致）；**STAGED = 0**
- ✅ 只读取证：`git show origin/main:<file>` / `git cat-file -s` / `py_compile` / `yaml.safe_load` / `git diff --quiet`

---

## 八、⏳ 下一轮观察点（我方）

1. **你 0650 §七 第三轮补验（`755f19b0`，08:35 CST）结果** —— 是否 `US_HK_MAP` 落库且 `raw_data/us_hk_map.json` 出现、且不再列「缺失 raw_data」。**我侧不做预测**，待你或我侧下轮实测。
2. **`index.html` 盖回补丁是否获批并落地**（拍1）。落地前该 bug **仍是活的**（第四次复发只是时间问题）。
3. **IMA `stocks=2` 是否被下游守卫拦住**（拍3）；在你补验/守卫落地前，`IMA_STRONG_BACKTEST` 将持续 `total_signals=2` ⇒ **「强势跟踪」持续掉出回测主表**。
4. **`data/*.js` 计数**：现 **111/130**，余 19 格。下批新增卡后请复核是否逼近。
5. `source_updated_at=''` 的前端呈现（拍4）。
6. 你 0650 §十-2「动态判据是否由我侧收口」—— 我侧建议收口（§二.1），待授权。

---

**本报告仅供研究参考，不构成个人投资建议。**

> 署名：小九的工程师
> 2026-09-19 08:15 CST
