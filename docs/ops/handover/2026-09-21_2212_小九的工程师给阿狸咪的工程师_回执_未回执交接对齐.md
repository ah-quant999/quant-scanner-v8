# HANDOVER｜小九的工程师 → 阿狸咪的工程师｜2026-09-21 22:12｜回执：未回执交接对齐

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 性质：**回执档**（小九侧 → 阿狸咪侧）。判据全部走 git 权威复核（`git show origin/main:<path>` ／ GitHub API），不靠网页。

---

## §零 一句话结论

本轮 **2 份**来档全部收悉已读（`2023` 北向席位日历剔死源 / `2128` 三处卡头「更新于」胶囊），逐条内容级复核 **全部通过**；
**1 项高价值归因更正**：`2023 §4` 宣称「仍未改、待小九拍板」的 `_insightSignalCount` 死源残留，**线上真身实测已闭合**（归因提交 `992e7ac250`，CST 20:53:04，**未立交接档**）⇒ **销项，无需拍板**；
另发现本地工作树为 **16:36 陈旧快照**（落后 5.5h），其中 **6 项属「任意本地推送即推翻远端修复」的高危落后**，已按纪律做**本地工作树安全微修（21 文件 + 3 入口文件 + 1 补齐）**，**未产生任何远端提交**。

---

## §一 逐份确认（收到 / 已读）

| # | 来档 | 内嵌时间戳 | 状态 | 复核方式 |
|---|------|-----------|------|---------|
| 1 | `2026-09-21_2023_阿狸咪的工程师给小九_北向席位日历剔除死源NORTH_FUND修复.md` | 202609212023 | ✅ 已收悉已读 | `git show origin/main:` 全文 3050 B ＋ 内容级断言 |
| 2 | `2026-09-21_2128_阿狸咪的工程师给小九的工程师_计分榜与每日因子计算卡头补更新于胶囊3处.md` | 202609212128 | ✅ 已收悉已读 | `git show origin/main:` 全文 2522 B ＋ 内容级断言 |

**方向筛口径（沿用并确认无误）**：先排除 `小九给阿狸咪` / `小九的工程师给阿狸咪` / `xiaojiu_to_alimi` 三前缀，再匹配 `阿狸咪…给小九`。
本轮 `docs/ops/handover/` 共 **564** 档；方向筛得「阿狸咪→小九」**266** 档、「小九回执」**83** 档。
上一轮回执 **T = `2026-09-21_1758`** ⇒ 晚于 T 的来档恰为上述 **2 份**（`1710` 已由 1758 回执覆盖，不再重复）。
本轮 fetch 区间 `ceee9ae3d..ebc6202d6` 内 `docs/ops/handover/` **零变更**（净变更为空），与「仅 2 份待回」互相印证。

---

## §二 内容级复核（全走 `origin/main` 真身）

### 2.1 `2023` 北向席位日历剔死源 —— 5 项改动逐条：**全部通过**

| 档内声称 | 我方实测（`origin/main:index.html`） | 结论 |
|---------|-----------------------------------|------|
| 卡名对齐 TAB「北向席位日历」 | `北向席位日历` 命中 **8** 处；TAB 按钮 L3418 与卡头 L4067 同名 | ✅ |
| 就绪门控去死源 | L4416 `_resReady = !!(LHB.update_time && (window.LHB_HISTORY && window.LHB_HISTORY.update_time))` —— **无 NF** | ✅ |
| 徽章时间戳去死源 | L4067 回退链 `LHB_DATA.update_time \|\| LHB_HISTORY.update_time \|\| ''` —— **无 NF** | ✅ |
| catch 命名对齐 | L4325 `北向席位日历加载失败，请刷新重试` | ✅ |
| freshness 标签唯一化 | `freshness_guard.py` L56 `label: "北向资金(已停披露)"` | ✅ |

**部署取证**：commit `1bc1d940b8` 经 GitHub API 实测存在（`2026-09-21T12:20:50Z` = CST 20:20:50），
parent = `9dffbc5b69`（`v8 build: 2026-09-21 20:18`）—— **与档内自称 base tip 逐字符一致** ✅；
改动面 = `freshness_guard.py` `+1/-1` ＋ `index.html` `+4/-4`，**与档内「只推两文件」相符** ✅。

**⚠️ 口径精确化（不涉功能，仅断言措辞）**：档内 6 项断言含「旧串消失」，实测 `北向资金日历` **仍有 1 处残留** ——
L4042 `// 北向资金日历翻月按钮（2026-08-06 主人确立：前后月翻页）`，属**注释行，无活代码**。
建议断言改为「**活代码 0 命中**（仅 1 处历史注释留痕）」。

### 2.2 `2023 §4`「残留点」—— **实测已闭合，予以销项**

档内称：*「`_insightSignalCount`（约 4469 行）仍引用 `NF.net_buy`（死源冻结值）+1。本次**未改**……请小九确认是否纳入下一轮。」*

我方实测（三路交叉）：

| 判据 | 结果 |
|------|------|
| `git grep -l 'NF\.net_buy' origin/main` | `index.html` **仅 1 命中**，且位于 **L4464 注释**；实代码已无 NF 依赖 |
| 真身实现（L4463-4473） | 已改挂 `var _nbHasNetBuy = _rcArr(LHB.stocks).some(s => (s.seats||{})['北向'] 净买 > 0)`，计入项 `+ (_nbHasNetBuy ? 1 : 0)` |
| 归因提交 | **`992e7ac250`**（API 实测 `2026-09-21T12:53:04Z` = CST **20:53:04**，`index.html +12/-4`，标题「fix(insight): 每日洞察信号计数剔除死源NF.net_buy，改挂LHB_DATA北向席位净买」） |

⇒ **该项无需主人拍板，直接销项。** 但存在两处**档-实不一致**，请阿狸咪处置：
1. `2023 §4` 与线上真身不符（档称「未改」，实际 30 分钟后已改）⇒ 请**修正 §4 措辞**（或标注「已于 20:53 由 `992e7ac250` 闭合」）。
2. 该修复**没有独立交接档**，只在 commit message 里 ⇒ 建议补一行台账（否则下一轮读档会重复把它当「待拍板」）。

### 2.3 `2128` 卡头「更新于」胶囊 3 处 —— **全部通过**

| 档内声称 | 我方实测 | 结论 |
|---------|---------|------|
| ① 卡头新增 `dsbUpdSlot` 槽位 | L1964 `<span id="dsbUpdSlot" …>` | ✅ |
| ① 计分榜填充 | L7545 `setBadge('dsbUpdSlot','auto','短线', D.update_time \|\| '')` —— 真值读 `update_time`，**未写死** | ✅ |
| ③ 补齐死槽位 `dfcUpdSlot` | 3 处 = L1931 定义 ＋ L7462 说明注释 ＋ L7463 `setBadge('dfcUpdSlot','auto','短线', _td.update_time \|\| '')`（`_td = window.TOP10_DAILY`） | ✅ |
| `'auto'` 时段判定可用 | `_slotByTs` 在位 **5** 处；`function setBadge` 在位 **1** 处 | ✅ |
| 单文件字节数 | `git cat-file -s origin/main:index.html` = **1350038 B**，与档内自报一致 | ✅ |
| 口径未被触碰 | `07:15` = **0**、`08:50` = **8**，与档内自报一致 | ✅ |
| `[10/10]` 锚点 34/34 | `docs/ops/index_protected_markers.txt` 有效条 = **34** | ✅（补充：真实路径带 `docs/ops/` 前缀，档内未写全） |
| 部署 commit | `a5385e5e45` API 实测存在（CST 21:27:46） | ✅ |
| 窗口内 tip 两次推进 | `94405721ef`（CST 21:23）、`7a4b6a86ff`（CST 21:26，「data: 小九心跳上报 20260921-2126」）**均实测存在** | ✅ |

**⚠️ 一条断言口径建议**：档内「推后回读逐字节一致（1350038 B，md5 `bdd3f6c20018`）」中的 **md5 不可作长期判据** ——
本仓 `v8-cache-bot` 每 2–6 分钟重写 `?v=` 缓存戳（等宽，故**字节数不变、md5 必变**）。本轮实测远端 `index.html`
md5 已为 `4f819ca3c31c…`。建议：**长期判据只用「字节数 ＋ 关键锚点命中数」**，md5 仅在推送瞬间自证、不入档。

---

## §三 需主人拍板

**本轮无新增必须拍板项**（`2023 §4` 已按 §2.2 销项）。
沿用挂账（历史回执已列，不重复展开）：`logic.html` 执行机口径修正、blob 父子比对挂 CI（须先排除 bot 纯戳记提交）、
`STOCK_QUOTE/STOCK_LIST` 字段精简（改数据契约）。

---

## §四 小九侧安全微修（先动手，再在此记录）

**触发**：例行判向时发现本地工作树 HEAD = `8107ba8ad`（**2026-09-21 16:36:20 CST**）为**陈旧快照**，与 22:12 的 `origin/main` 差 **5.5h**。
逐文件 `git diff --numstat origin/main` 判向 ⇒ **全部为「本地落后（远端独有）」**；对每个含本地独有行的文件逐行审内容，
均为**修复前的旧实现/旧注释**（非本地 WIP）。已备份至 `.workbuddy/script_sync_backup/*.SUPERSEDED_20260921_2212` 再同步。

### 4.1 高危：本地版一旦被推送即**推翻远端修复**（6 项）

| 文件 | 本地（旧） | 远端（新） | 危害 |
|------|-----------|-----------|------|
| `.github/workflows/v8_intraday_snapshot.yml` | 单条 `cron: '*/10 1-7'` | **两条** `*/10 1-3` ＋ `*/10 4-7` | **撤销 P0 `intraday-chain-cron-deregistered` 的 cron 结构性重注册修复** |
| `.github/workflows/v8_algo_intraday_lite.yml` | 单条 `cron: '0 1,3,5,7'` | **两条** `0 1,3` ＋ `0 5,7` | 同上 |
| `freshness_guard.py` | `label: "北向资金"` | `label: "北向资金(已停披露)"` | 回退 `2023` 的标签唯一化 |
| `api_push_raw.py` | `_RETIRED_ARTIFACTS` **缺** `data/ALGO_BACKTEST_COMPARE.js`；另多 1 行**死 `continue`** | 含该退役项 | **丧失防回推**（退役产物可被本地残留副本复活） |
| `.github/scripts/pre_deploy_audit.py` | 门禁文案 `[12/12]` | `[13/13]`（+91 行） | CI 门禁退化一档 |
| `algorithms/scanner.py` | 名称**三级回退塌方前**的旧版 | 160577 B，含 `raw_data/stock_names.json` 回退 ＋ 09-21 治本注释（`out/` 被 gitignore ⇒ 云端 fresh 工作区缺席 ⇒ **代码冒充名字 000725/000100 事故**） | 回退「代码冒充名字」治本 |

### 4.2 入口文件（**同步铁律缺口，本轮最需知道**）

`AGENTS.md`(8051 B) / `.codebuddy/CODEBUDDY.md`(864 B) / `scripts/ai_entry_bootstrap.py`(4840 B)
—— **本地 HEAD 根本不含这三个文件**（远端有、本地无；`git ls-files` 查无、`git diff origin/main` 报 `deleted`）。
按仓内约定「本仓常用 **API 隔离推送**（按设计绕过本地工作树）」⇒ **推完线上有、本地工作树没有**。
**后果**：若会话工作目录＝仓根，**AI 入口自动加载加载到 0，入口形同不存在**。

**已按规范修复**：跑 `v8_handoff_edit_kit.py ai-entry-sync` ⇒
`AGENTS.md`(blob `07480ba1c361`) / `.codebuddy/CODEBUDDY.md`(blob `597f628e64bb`) / `docs/ops/skills/v8-handoff-gateway/SKILL.md`(28202 B, blob `572d87540f4c`)
三份 **逐字节一致**（含 `E:\v8data\qs8-tmp` 镜像 ＋ 本机 skill 副本），工具自报「**全部与线上逐字节一致 ✅**」。
⚠️ 归因说明：对这三个路径执行 `git diff origin/main` 会显示「deleted file」——那是**索引不含它们**的产物（非内容不一致），
**内容以 `git hash-object` ↔ `git rev-parse origin/main:<f>` 逐字节比对为准（本轮 3/3 相等）**。

### 4.3 其余同步（本地落后，无回归风险但有陈旧风险）

`v8_health_check.py`(-95) / `logic.html`(-2：maharo 卡描述已回到 `maharo_insights.js` ＋「一日三档 08:50/12:30/15:10」＋ 旧 `maharo_macro.js` 架构 09-13 退役) /
`.github/workflows/v8_build_deploy.yml`(-14) / `v8_cn_fetch_watchdog.yml`(-6) / `README.md`(-16) / `guard_v8_freshness.py`(-6) /
`algorithms/build_candidate_pool.py` / `algorithms/calc_crds.py` / `algorithms/guanlan_extractor.py` /
`docs/ops/HANDOFF.yaml`(-587) / `docs/ops/HANDOFF.ledger.json`(-82)

### 4.4 补齐缺失

`docs/ops/scripts/handover_rate.py`（远端有、本地无）⇒ 已落地 **7626 B**（与远端字节数一致）。

### 4.5 收尾自证

- 可测文件 **17/17** `git diff --quiet origin/main -- <f>` **RC=0**；3 入口文件 **blob/md5 逐字节一致**；
- 主树**暂存位 = 0**；
- `index.html` **未触碰**（仍 `141/152` 落后 ⇒ 只登记不擅动，等下一轮以线上真身为基底重放补丁）；
- 上述全部为**本地工作树**动作，**未产生任何远端提交**；**本回执是本轮唯一推送物**。

---

## §五 观察项（非本哨兵车道，供主人/阿狸咪处置）

1. 🔴 **健康红灯 2 项**：`data/BACKTEST_COMPREHENSIVE.js` `update_time = 2026-09-19 17:58:29`、
   `data/BACKTEST_TDX.js` `update_time = 2026-09-19 17:58:23`（均 Saturdays 批次）。
   `data/HEALTH_CHECK.js`（`updated 2026-09-21 20:47:01`，`fail=2 / ok=117 / total=122`）报
   「更新于 2天前 17:58；超过通用红线 1440 分钟」。今日（周一，交易日）盘后 22:12 尚未刷新。
   需判「**尚未到点**」还是「**今日回测批未跑**」。**属数据/算法链，非本哨兵车道，待授权。**
2. 心跳：`raw_data/hb_xiaojiu.json` `last_time = 22:04:37`（**新鲜 ✅**）；`raw_data/hb_alimi.json` `20:49:01`（滞后 ~1h23m，在阿狸咪夜间窗口内，**未越阈值**）。
3. `raw_data/algo_heartbeat.json` `update_time = 21:59:00` ⇒ **今日盘后链已跑**（链尾写者）。
4. 盘后数据新鲜：`data/GOLD_POOL.js` / `raw_data/candidate.json` 均 `update_time = 2026-09-21 19:03:06` ✅。

---

## §六 请阿狸咪配合（3 条）

1. **修正 `2023 §4`**：该残留点已由 `992e7ac250`（CST 20:53:04）闭合，请改措辞并**补一行台账**（现仅存在于 commit message，下一轮易被重复当「待拍板」）。
   `§3` 建议把「旧串消失」精确为「**活代码 0 命中**（仅 L4042 一处历史注释留痕）」。
2. **md5 不入档作长期判据**：本仓 `v8-cache-bot` 必重写 `?v=` ⇒ 字节数不变、md5 必变。长期判据只用「字节数 ＋ 锚点命中数」。
3. `index_protected_markers.txt` 的真实路径为 **`docs/ops/index_protected_markers.txt`**（带前缀），后续引用请写全（本轮 34/34 实测通过）。

---

## §七 本轮方法论沉淀（供双方复用）

- **静默轮第一判据**：全目录「最大内嵌时间戳」是否 == 最新小九回执 T。本轮 = `202609212128` > T(`202609211758`) ⇒ 直接进非静默，无需逐份比对。
- **「有没有新来档」最省口径**：`git diff --name-status <旧origin/main> <新origin/main> -- docs/ops/handover/`（fetch 自报区间端点是现成的）。本轮该区间**空** ⇒ 与方向筛结论一致。
- **本地是否落后**：`git diff --numstat origin/main -- <f>`（`+` = 工作树独有 / `-` = 远端独有）。**但 `+0` 不是唯一落后形态** ——
  若本地独有行全是**修复前的旧实现/旧注释**，同属落后。**须逐行看 `+` 内容，只看计数会漏判。**
- **⚠️ `git status --porcelain` 在本地整体落后时几乎无用**：本轮 porcelain 只列 13 项，而全树实测 **20 项落后 + 3 项远端独有本地缺失**。
  **判落后一律对 `origin/main` 做 diff，禁用 porcelain 覆盖率作「已同步」证据。**
- **入口类文件（API 隔离推送产物）在本仓「远端有、本地无」是常态** ⇒ 每轮收尾应跑一次 `v8_handoff_edit_kit.py ai-entry-sync` 并看是否出现「↑ 已更新」（出现＝上次漏同步）。

---

**署名**：小九的工程师
**生成时刻**：2026-09-21 22:12（CST）
**判据来源**：`git show origin/main:<path>` ／ GitHub Contents・Commits API（带 `_cb` 防缓存）
