# HANDOVER 阿狸咪的工程师 → 小九的工程师｜2026-09-17 08:30 · URGENT

## 监听器档序「两层排序」治本（判据 95）＋ 四量真值再遭 3 年降级档覆盖

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 本机基线：`FETCH_HEAD = 2138da9c19`（08:22 fetch）
> 本轮落地：`cc4f8a57d3`（父 `2138da9c19`，单文件 Contents API PUT，`v8_urgent_listener.py`）
> 触发：主人定时的「紧急指令监听 + 健康检查 + 自动 dispatch」自动化（第 420 轮），08:22 开跑

---

## 〇、一句话

1. **🔴 我自己的监控器在「瞎」**：上一步（P-A）给 graft 退化加的 **API 精算**，**自己制造了档序倒挂** —— 12 档**真最新**被沉到 354 档之末，位次 1 显示成**名字时间第 13 位**；`09-17_0645` 最新档与 `09-16_2230_URGENT` 档**整批不可见**，而**派发扫描面同一批被瞎掉**。已治本并上线（判据 95）。
2. **🔴 四量真值又被降级档覆盖了**：`data/FOUR_VOLUME_BACKTEST.js` 现为 **`1384 信号 / 近 3 年 / 07:18:12`**，**违反你 `1620` 的红线 2**（只允许 5 年档覆盖）。覆盖发生在 **07:19:19（`c3a7eb6700`）**，落点在 **algo-cloud run `35157685928`**；且**前一刻**（06:14 / 06:24 / 06:46 三处 tip）该文件**还是 `1400 / 近 5 年 / 05:23:13`**。**我未手工改数**（按你红线），交你判定与处置。

---

## 一、本轮治本：档序两层排序（判据 95）

### 1.1 现象（本机 08:22 实测）

| 项 | 值 |
|---|---|
| 监听器输出位次 1 | `2026-09-16_2133_小九_观测平台子页签字体放大显著化.md`（**错**） |
| 实际最新档 | `HANDOVER_阿狸咪的工程师给小九_2026-09-17_0645_…`（**未出现**） |
| 被漏掉的更新档 | **11 份**（`2245 / 2310 / 2350 / 2355 / 0110 / 0645` + `2219 / 2230URGENT / 2230小九 / 0040 / 0240`） |
| 自报状态 | `提交时间优先（API 精算）`、`degenerate: False` ⇒ **假成功标签** |

### 1.2 根因（硬证据）

诊断脚本（仓库外 `_alimi_diag_20260917/diag_sort.py`，只读）实测：

```
[cts] entries=354 unique=1
    value=1789603780 (2026-09-17 08:09:40)  count=354      ← graft 假值 = tip 提交时刻
[degenerate] True
[api] got 12
    HANDOVER_…_0645  graft=08:09:40  api=06:55:26
    2026-09-17_0240  graft=08:09:40  api=02:37:03
    …（12 档真值 22:16 ~ 06:55，**全部小于** graft 假值）
```

排序键是 `(cts.get(nm, 0), _name_sort_key(nm))` 倒序。原代码把 API 真值 **`cts.update(api)`** 混进同一张表 ⇒ **graft 假值（≈此刻）比任何真实提交时间都大** ⇒ 刚拿到真值的 12 档**被自己压到最末**；其余 342 档按假值并列，退化成文件名时间 ⇒ **显示面 = 名字时间第 13~20 位**。

⇒ 与判据 59/94 同源：**「取到同一个错值」比「取不到」更危险**；且这次是**治本动作本身**制造的倒挂，日志还打健康标签（假成功）。

### 1.3 修法（两层排序，不再跨量纲比大小）

```python
api_set = set()
…
if api:
    api_set, cts = set(api), dict(api)        # 禁 cts.update(api)
names.sort(key=lambda nm: (nm in api_set, cts.get(nm, 0),
                           _name_sort_key(nm)), reverse=True)
```

组 1 = 有 API 真值者（按真值倒序置顶）；组 2 = 其余（按文件名时间兜底）。**退化标记保持 `True`**，输出显式披露「⚠️ 检出 graft 退化（git 提交时间不可信），排序已分层兜底」；另顺手把写死的「前 8 档」改为 `ACTION_SCAN_N`。

### 1.4 验证

| 项 | 结果 |
|---|---|
| 语法 | `ast.parse` OK（611 行） |
| 修后位次 1~5 | `0645(1.7h) → 0240(5.8h) → 0110(7.3h) → 0040(7.8h) → 2350(8.6h)` ✅ **全为真最新** |
| 修复面 vs 真值 | 与「按文件名倒序前 10」的期望集**一致**（诊断脚本 `[EXPECTED]` 段对照） |
| 派发安全 | 修好前**另独立扫描**这 11 份新档全文（`[ACTION]` / `dispatch` 行）⇒ **0 条独立成行指令**，故修复后**不会**因「新档首次进入扫描面」而误派风暴 |
| 上线 | `cc4f8a57d3`；远端回读 `sha=9c4aaf6b82`、`size=29538` == 本机 `hash-object` **逐字节一致**；base 复核 `15adeb6b15`（= 远端当时 sha）⇒ **0 行你方内容被删** |
| 踩踏 | `v8_build_deploy.yml` 的 `on.push.paths` **仅 `raw_data/**`** ⇒ 推本 `.py` **不触发构建**，0 踩踏 |
| 三件套 ② | `index.html` / `logic.html` 均**无**描述该排序行为的文字（grep 命中 0）⇒ 无需对齐；`align_logic_ops.py` **EXIT 0** |
| 三件套 ① | 本次改动不涉任何 cron / 时窗 / 双机任务 ⇒ 时窗矩阵无需改 |

---

## 二、🔴 四量真值再遭 3 年降级档覆盖（交你判定）

### 2.1 现状 vs 你的红线

| 项 | 你 `1620` 的红线 | 现在（实测） |
|---|---|---|
| `signal_date_range` | 「只允许 **5 年**档（861 真值）或其复现产物覆盖」 | **`近 3 年`** ❌ |
| `total_signals` | 861（5 年真值） | **1384** ❌ |
| `update_time` | 09-15 17:25:48（真值） | **2026-09-17 07:18:12** ❌ |

### 2.2 时间线（全部 git/API 权威，非网页）

| 时刻(CST) | 证据 | `total / range / update_time` |
|---|---|---|
| 09-17 05:23 | E 批产出 | `1400 / 近 5 年 / 05:23:13` ✅ 符合你 §3「E 批 5 年档属正常覆盖」 |
| 06:14 / 06:24 / 06:46 | tip `445bf9eb60` / `338c3c321d` / `ccfb3c9f46` 逐版读取 | **仍为 `1400 / 近 5 年 / 05:23:13`**（未被降级） |
| **07:19:19** | commit **`c3a7eb6700`**（信息「v8 cn fetch: 2026-09-17 07:19」，按铁律**不可归因**） | **变为 `1384 / 近 3 年 / 07:18:12`** ❌ |
| 07:19:21 | algo-cloud run **`35157685928`** / job `105000904614` 日志 L840 | `⏳ 四量终极其日线版非今日(run_start=2026-09-17 06:27:27)，第1/3次重跑 strategy_four_volume.py 并等待90s` → `✅ 重跑 strategy_four_volume.py` |

**推论（有据）**：这条重跑来自 `run_algorithms.py` 的「四量日线版非今日」hardwait 重跑路径；该路径**不在 E 批 `SCRIPT_ENV` 注入范围内**（`run_algorithms.py` L392 只对**回测批**注入 `V8_BACKTEST_YEARS=5`）⇒ argparse **默认 3 年** ⇒ 写出 3 年档。

### 2.3 我**未证**的部分（照实列出，别当结论）

1. **守卫为何没拦住**：job 日志里**搜不到** `🛡 跳过覆盖` 也搜不到 `[warn] 回测外壳防降级检查异常`。但**该脚本自身的 stdout 是否进 CI 日志我没验证**（run_algorithms 可能重定向子进程输出）⇒ **「守卫失效」我不下结论**；可查方向：① CI workspace 里 `data/FOUR_VOLUME_BACKTEST.js` 在 07:19 那一刻的实际内容；② 是否先有一次 3 年写把 `_old_years` 变成 3，使后续 3 年写「同档放行」。请你在**你机 `/tmp` 或 `tmp/fv_backtest_5y_v2.py` 范式**下复核。
2. 数值口径：`1400`（5 年，E 批）与 `861`（5 年真值）**本就不一致** —— 正好命中你 §3 第 3 条（云端 GTimg 日线仅 ~641 根）。**这不是我这次发现的新问题**，但说明「5 年档」本身也需你按 §3 迁到自托管 runner 复现。

### 2.4 我做了什么 / 没做什么

- ✅ 只做**取证与披露**，**未手工改数**（你红线 2）。
- ✅ 未碰 `data/**`、`raw_data/**`、`index.html`、`logic.html`、任何 workflow、任何阈值。
- ⏳ **请裁定**：`data/FOUR_VOLUME_BACKTEST.js` 现在这版（`1384 / 近 3 年`）是**再次污染需回滚到 5 年档**，还是**暂留待 E 批重算**？回滚动作归你（白天窗口）还是归我（今晚）也请一句话定。

---

## 三、健康检查（本轮 `guard_v8_freshness.py`）

| 项 | 结果 |
|---|---|
| 返回码 | **0**，受检 **43 模块**（`ANALYST_RATINGS` 退役后口径） |
| 🔴 CORE | **1 个**：`BACKTEST_TDX` 更新于 09-14 02:07，落后 3 交易日（**真陈旧**，非假红；属你 `ARCHIVE 30.6` 的 E 批饿死家族，判据未动） |
| 🩹 自愈 | `algo` 类别 08:09 已有活跃 run（`#35165409428` pending）⇒ 命中「排队中/冷却中」**跳过重复派发**，未新增派发 |
| 自动派发 | 扫描面前 8 档 **0 条指令** ⇒ 本机 **0 额外 dispatch** |
| 云端 run 实况 | `🇨🇳 v8 中国数据抓取(云端)` in_progress（08:16 创建）｜`☁️ v8 盘后算法链(云端)` pending（08:09 创建）｜`pages build` 08:22 success |

---

## 四、纪律与下轮优先

- 本机 **0 git commit**（全程单文件 Contents API PUT **×2**：`v8_urgent_listener.py` + 本档）；未碰前端 / data / raw_data / workflow / 阈值。
- 下轮优先：① 你对我 §2 的裁定回执；② 核对 `BACKTEST_TDX`（仍空转）；③ `cn_fetch` 同并发组「顶替」不诚实问题（你 `0040` §2 的错相已落地，但**顶替本身**仍未治）。
- 📌 顺手订正：项目记忆里 `push_one_file.py` 的路径写作 `_v8_urgent_1733`，**实际是 `_v8_urg_1733`**（已改记忆）。

---

**附：本轮新增判据**
- **判据 95**：浅克隆下「graft 假值 + API 真值」**不得混入同一张排序表** —— 假值量级是 tip 时刻（最大），真值量级是历史时刻 ⇒ 必须**两层排序**；否则治本动作本身会让**真最新沉底**，且日志仍打健康标签（假成功）。任何「退化兜底」改动都必须用**「兜底后位次 1 是否等于真最新」**做端到端断言，而非只看「API 调用成功了几档」。
