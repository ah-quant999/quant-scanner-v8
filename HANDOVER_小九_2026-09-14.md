# 九宝量化 v8 交接文档 — 小九 → 阿狸咪（2026-09-14 12:00 槽位）

> 更新于 2026-09-14 12:36 自动更新（automation 1785719698947 · 小九 12:00/18:00 例行交接）
> 信息源：当日工作日志 `.workbuddy/memory/2026-09-14.md`（今日条目极多，本档为结构化提炼）+ git/线上实时实测。

---

## 一、背景

- 双机铁律：小九白天（07:45-17:45）独家 / 阿狸咪夜间+周末（18:00-次日07:30）独家；本档 12:00/18:00 各更新一次。
- 今日（周一）为高频修复日：凌晨回测口径治理 → 早盘凭据安全 P0 → 午休三卡修复 + 两起覆盖事故。夜间请你接手验证与收尾。
- 今日已有 8 份专项交接/回执文档在 `docs/ops/handover/`（索引见 §六），本档是全天汇总视图。

---

## 二、今日已完成改动（截至 12:36，全部真实 SHA）

### 1. 回测口径治理（P0 级，凌晨 00:00–02:30）

| commit | 内容 | 影响 |
|---|---|---|
| `a897a43a2` | ENTRY_CALIBER_VER=2 单一真源 + 宇宙外陈旧条目裁剪（66 只混口径全清）+ `update_v8.py _make_lite` 透传新字段 + 「60日回测」文案改派生式 | `stocks` 296→230、残留旧口径 0、`trend_up` T+5 胜率 52.0→51.7%；幂等验证通过 |
| `c6f6bce5f` | P0 样本窗口修复（8 文件） | 信号窗口延至 2026-09-09；信号 36939→**76292**；regime 76→**797** 交易日；`optimized_samples`→4051；`current_regime` 恢复刷新 |
| `f890f42d8` | 补回 `_dump_json` 定义（Edit 报成功未落盘事故） | E 批链 NameError 根治 |
| `d709cb294` | docs 补遗 | — |

- `raw_data/backtest_tdx.json` **126.9→72.6 MB**（紧凑序列化零损失）+ 95 MB 体积护栏 + 统一写出入口。
- 已重算：BACKTEST_COMPREHENSIVE / BACKTEST_TDX / CRDS_BACKTEST。**待云端补算：FOUR_VOLUME_BACKTEST / BACKTEST_ALL_ALGOS**（本地缺 mootdx 出废数据，已回滚未提交）。
- 中信 PE 回测口径统一为「次一交易日开盘买入」：`v8/fetch_citic_pe.py` 全量重拉 4055 行，`entry_fallback_n=0`；PE<10×250d 胜率 97.6→97.0%、均收 19.56→19.89%。
- ret_hold 严格持有口径全链接线（backtest_comprehensive / gen_backtest_all_algos / index.html 档位矩阵「严 X/Y」并列展示）。实测价值：T+45/T+60 旧口径恒等，严格口径才区分（-19.53%/-24.75%）。

### 2. 凭据安全 P0（07:01–07:45）

- 🔴 **zsxq token 曾泄漏进 public 仓库 61.4 小时**（09-11 16:25 `a865d6f11` 入库，blob `581808ce5a` 永留历史）。HEAD 已清；**处置铁律=吊销优先于历史清洗**。
- token 已补回双份（`data/zsxq_token.json` + `~/.workbuddy/v8_zsxq_token.json`）并端到端验证：观澜台 20 研报/50 推股恢复，`self_heal_monitor --check-only` 翻 ok。
- 🔴 **旧 token（C2 开头）实测仍存活** —— 主人换了 cookie 但未登出旧会话。**待主人登出所有设备**；届时新旧一起失效、双机都要接新 token（夜班可能遇到，属预期勿慌）。
- 凭据提交闸门已上线：`fa661dafb`（`scripts/secret_guard.py` 三层规则 + `.githooks/pre-commit` + `install_git_hooks.py`）+ `18f9c500c`（交接文档）。4001 个已跟踪文件零误报验证。**阿狸咪侧已启用 core.hooksPath 并实测拦截成功**（她 08:03 交接确认）；小九机暂未启用（盘后链有 4 个裸提交脚本，灰度观察一个交易日）。旁路：`SECRET_GUARD_ALLOW=1`。
- 根因更正（重要）：泄漏元凶是**某次 AI 会话的手工全域 `git add -f`**，不是 `v8_closing_data_refresh.py` 的 pathspec（git 的 `*` 不跨 `/`）⇒ 防线必须做在提交闸门，做在脚本里挡不住手工。
- 陈旧暂存区拆弹（skill `v8-stale-index-landmine` 实战）：主树 13 项陈旧条目（含 `run_algorithms.py` 旧 7.7KB）→ 备份 `backup/git_index_stale_20260914_0703.bak` 后 `git reset -q HEAD`，零改动自证通过。

### 3. 心跳双机治理（12:31 并发会话完成，我方）

- `dc87ca242`/`5bd34e81c`：前 4 条一劳永逸 —— ①判定改读远端权威（`git show origin/main:<心跳文件>`，根治 pull 失败读旧值）②`HB_XIAOJIU` 大小写统一 + CI 门禁第 8 项双向验证 ③阈值派生 180min（周期63×2+余量）④监控自证心跳；附带第 5 条我方半：`hb_alimi` 的 update_v8 映射与镜像判定。回执 `1abc65c17`/`55865f891`。
- **第 5 条发送侧（`raw_data/hb_alimi.json`）仍待你做** —— 接口契约见 `docs/ops/handover/2026-09-14_0855_小九的工程师给阿狸咪的工程师_紧急_...md`（machine: alimi-cn、CST last_time、≤90min/跳）。
- 背景：晨间「阿狸咪说小九没心跳」已实锤误报（心跳 61.9-63.1min 极稳）。

### 4. maharo 机构研究卡断更（上午只读取证 + 午休换稿）

- 根因四层：①计划任务 `v8-maharo-daily` **S4U 登录类型周末锁屏静默不跑**（09-12/13 缺块）②07:00 拉取 vs 07:15 出稿**时序错位**（间隔仅 15min，每日必现）③`maharo_daily_pull.py` 推 `data/maharo_macro.js`（已废弃文件）且 PATCH 只重试 409、遇 422 必败 ④健康检查只看 `update_time` ⇒「假新鲜」第三形态（时间戳新鲜≠内容新鲜，112/113 全绿掩盖断更 3 天）。
- 09:24 已用**活着的观澜台链**（`out/guanlan_reports.json`）换稿：daily 4132 字 → 11:35 自动 flush 上屏。
- automation `30e1b099` 已升级**含上游断更自检**（明日 07:50 起新 prompt 生效，断更自动邮件告警）。
- 10:50 侦察**推翻**上午两个错误结论：计划任务**在**（下次 9/15 07:00）；日报**今日 10:50 已刷新**（含 09-14 块、命中 56 条）—— 平日链路是活的，只有周末 S4U 静默 + 时序错位两个真问题。
- **修复素材就绪未执行**（Temp 下）：A `fix_A_task_logontype.ps1`（S4U→InteractiveToken）/ B `new_prompt_30e1b099.txt` / D `fix_D_push_patch.py`（停推废弃文件+422 纳入重试）/ F `fix_F_content_check.py`（内容维度健康检查）。

### 5. 覆盖事故两起 + 恢复（11:43 / 12:09，均已闭环）

- 事故 1（P0）：推送窗口内并发 git 命令致 `cat-file` 取到**残缺基线**（935KB），补丁推送后 index.html 1108023→938800，前三项修复全丢 → `rev-list --all` 全史扫描找回 `9542742f5` 唯一健康版本，重打补丁 `aee86d232`（1111310B）。
- 事故 2：同机另一 AI 会话 `2152baa3c` 从其旧工作树整文件快照推送 → 我方四项修复归零 → 以最新远端为基线合并重打 `46d994705`（1118866B），双方改动全保留。
- **新增三条永久铁律**：①`cat-file` 取基线必须校验字节数+特征串（index.html 完整版 110 万字节级，见 90 万级立即中止）②禁止并发执行多个 git 命令 ③打补丁前对基线做「新写法在位/旧写法清零」双向校验。
- 四项修复最终全在位：`fmtSigned()` 防 `++` 与十位截断、概念资金热力字号对齐行业树图、每日市场洞察改读四路纯盘后链路、**三重共识回测卡新增**（`__tripleBacktestAdapted()`，T+20 61.1%/+4.06%/Sharpe0.42⇒4星，稳定排在四量卡下方）。对方三处修复（`__v8EarlyRender`/`hbReload`/北京时区）全保留。

### 6. 盘后页 5 卡标签（并发会话 12:31 完成）

- `f7772c492`：`_uBadge` 加 `pageSlot` 第 5 参，盘后页 5 卡标签跟页面走 + 悬停显示真实时点；交接 `bbaddf6c1`。此为上午截图点名的遗留项，**已闭环**。

### 7. 取证发现（待主人拍板，**均未动**）

- **港股污染真因**：`algorithms/scanner.py` L937-990 `_HK_FALLBACK_POOL` = 92 只硬编码港股池（注释「API断连抢救兜底」），触发只 print 不落产物标记 ⇒ 与 strategy_four_volume 静默降级同反模式第 2 例。09-09 起每日稳定混入 6 只港股（非偶发）。
- **周末档重复计数**：59 个日档中 **17 个周末档**（28.8%），`discover_top10_signals()` 无交易日过滤 ⇒ **真实交易日深度 42 天（非 59）**；3 组名单逐位复刻自两次独立运行。建议「并列给出过滤后样本数」口径（沿用 ret_hold 处置）。
- **fetch_lhb.py 第三处同型**：`out/lhb_history.json` 写未跟踪目录，本机跑得出、云端 fresh checkout 清零（迁移 v6→v8 时改坏）。判定口径注意：看 `DATA_DIR` 实参，不是字符串形状。
- 涉及文件（scanner.py / generate_top10.py / backtest_comprehensive.py / fetch_lhb.py）全部双机热文件，**等拍板，勿单方面修**。

### 8. 阿狸咪侧今日完成（已复核确认）

- `calc_crds.py` 历史快照落盘真 bug 修复 `071d9c2d2`（写未跟踪 `out/history/` ⇒ CRDS 历史从未累积）。我方读取侧复核结案：`v8/backtest_crds.py` 两目录都遍历 + 全局去重，**无需再改**。
- A 批 must P0 修复、logic.html 分批明细对齐、离线复现器 `stage_and_audit.py`、凭据闸门装机实测。
- **她 4 项遗留的状态更新（全部闭环，无需再动）**：①孤儿脚本 `optimize_stop_target.py` 已删除（`fa661dafb`）②双 `make_lite` 已合并 ③TDX 12 档展示已由 `_holdLadder()` 派生器落地 ④「长档退出主线」已作废（主人 09-13 令十档全上）。

---

## 三、当前系统状态（12:36 实测）

| 项 | 状态 |
|---|---|
| 站点 | HTTP **200** ✅ |
| HEALTH_CHECK（线上） | 12:29:59 · **ok112 / warn0 / fail0 / limited1**（total 113）✅ 全绿 |
| origin/main tip | `34c3d65f4`（12:35 build，CI 推进中，以 ls-remote 为准） |
| 小九 runner | lemoncat-cn 心跳 11:12:38 ok；63min 节拍，截至 12:33 未跳新（<90min 阈值不判死，夜班留意即可） |
| 本地 main | ⚠️ 钉在 09-11 旧提交 `02e2eb78a` + 仓库为浅克隆（is-shallow=true）⇒ 与远端 merge-base 为空，`ff-only` 报 unrelated histories **属预期**；主工作区 69 项 WIP 一律未碰；本档经临时 worktree 旁路推送 |
| 心跳治理 | 前 4 条已上线；`hb_alimi` 发送侧待阿狸咪 |
| 小九剩余自动化 | 20min runner 守护 / 6h 云端看门狗 / 12:00+18:00 交接 / 09:15+12:45 清场核查 / 11:35+15:05 发布闸门 flush / 2h 入站回执哨兵 / 1h runner 脚本对齐 origin/main / 15min 缓存戳对齐兜底 |

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 高优先
1. **旧 zsxq token 仍存活**：待主人登出所有设备；失效后双机需换新 token，观澜台会再断一次（预期）。
2. **maharo 修复素材 A/B/D/F 未执行**（Temp 脚本就绪）：夜班窗口可请示主人后执行；automation `30e1b099` 新 prompt 明日生效。
3. **港股污染 + 周末档修复待拍板**：四个双机热文件，勿单方面修。
4. **RPS 回测卡归属待确认**：仅残缺版 `2cbadf2b2` 有痕迹、`data/RPS_BACKTEST.js` 远端不存在 ⇒ 恢复后该卡不出现，需你或主人确认是否本来就该有。
5. **平均股价三结论统一未做**：`verdict` 与「买/加」行灯色语义矛盾 + 与 `v8IntradayGate()` 跨卡矛盾。
6. **`E:/workspace/quant-scanner-v8` 废弃路径 6 处引用未改**（4 处读旧 token：v8_dispatch_fetch/v8_cloud_watchdog/v8_postclose_guard/guard_v8_freshness），改动会变行为，待拍板。

### ⚠️ 纪律与坑（今日新增/重申）
7. **本地 main 浅克隆脱钩**：勿在主树 rebase/merge/reset；单文件上线用旁路推送（skill `v8-raw-tree-push`）。
8. `backtest_tdx.json` **顶层加字段必须同步 `_make_lite`**（白名单裁剪，默认丢弃）。
9. 新 worktree 跑回测前必须先复制三件输入：`raw_data/_tdx_cache/` + `out/gold_pool.json` + `raw_data/history/gold_pool_*.json`（否则宇宙不一致）。
10. `logic.html` 是全仓唯一 CRLF blob 例外：仓库外改好入库必须 `git hash-object -w --path=logic.html`。
11. `len(python_str)` 是字符数不是字节数；中文文件名 git 输出需 `-c core.quotepath=false`。
12. 三条覆盖事故铁律（见 §5）+ 推送前 `git fetch` 确认基线含对方最新提交。
13. `core.hooksPath` 本机未启用（灰度观察）；真要旁路用 `SECRET_GUARD_ALLOW=1 git commit`。
14. 白天时间纪律：09:15 / 13:00 硬停写操作，清场核查 09:15 / 12:45；`E:/workspace` junction 永不复用。

---

## 五、小九监督职责（延续不变）

| 职责 | 节拍 |
|---|---|
| 交接文档更新（本档） | 每日 12:00 / 18:00 |
| runner 本地守护 | 20 分钟级 |
| 云端看门狗 + 邮件告警（2814546@qq.com） | 6h / 每小时 7-21 |
| 清场核查（只读） | 09:15 / 12:45 |
| 发布闸门 flush（staging 释放） | 11:35 / 15:05 |
| 入站回执哨兵（阿狸咪→小九交接档） | 每 2h |
| runner 脚本对齐 origin/main | 每 1h |
| 缓存戳对齐兜底 | 每 15min |

18:00 后你独家部署；小九转只读监督。夜间如有 P0 先邮件告警 + 交接档留痕，勿等小九在线。

---

## 六、今日专项文档索引（docs/ops/handover/）

| commit | 文档 |
|---|---|
| （凌晨批） | `HANDOVER_小九给阿狸咪_2026-09-14_0110_回测口径混用治本-...md` |
| `b4f83aa61` | `2026-09-14_0855_...紧急_反向心跳请你做+前4条午休落地.md` |
| `873037f03` | `HANDOVER_20260914_1157_xiaojiu_recover_triple.md`（事故1复盘+三条铁律） |
| `5b530576b` | `HANDOVER_20260914_1220_xiaojiu_merge_recover.md`（事故2合并闭环） |
| `bbaddf6c1` | 盘后页 5 卡标签 pageSlot 交接 |
| 阿狸咪 `7b7aa2f98` | 0800 港股周末档复现 + CRDS 历史 bug 修复 |
| 阿狸咪 `6af91a4aa` | 0900 白天接手清单（凭据闸门已装+未完成项移交） |
| `1abc65c17` | 回执：心跳前 4 条已落地 |

---
*本档由小九自动化生成并直接推 main；内容以当日工作日志为源，SHA 均经 git log 实测核对。*
