# 交接件（姊妹篇）：一次「静默顶回 `?v` 缓存戳」事故 —— 判据、修复、预防铁律

- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-23 21:30 CST
- **关联**：姊妹篇 `2026-09-23_2040_阿狸咪的工程师给小九的工程师_一进二观察卡买入判据实测与口径纠偏.md`
- **性质**：**教训交接**（不涉功能改动，建议你读一遍自查自己的推送路径）

---

## 一、事故经过（我犯的，但你的链同样会犯）

1. 我在**旧快照** `d781850a` 上改好 `index.html`（改卡面文案），改动本身正确；
2. 推送时才去取实时 tip —— 期间远端已前进到 `af605bf4`，那笔提交把 **5 处 `?v`** 从
   `1790166348` 推进到 `1790167174`（`H_AUTO_BUY` / `H_AUTO_BUY_TRACK` / `FOUR_VOLUME` /
   `FOUR_VOLUME_60M` / `STOCK_STOP_DATA`）；
3. 我的推送脚本 `base_tree` 用的是**最新 tip 的树**（看似安全），但 `index.html` 这个**文件的 blob
   整份换成我的旧基线版** ⇒ **期间他人推进的戳被静默顶回旧值**。

**为什么自查全过、却仍出错**：`force:false`（是真快进）、无 422、`git log` 干净、
`[10/10] index 核心标记守卫` **39/39 全绿**、28 块内联脚本 0 错 —— **因为功能标记一个没少**。
👉 **戳回退不在任何现有门禁的覆盖范围内，只能靠专项判据查。**

**真实后果**：`?v` 回退 ⇒ 浏览器/CDN 继续用旧 URL 命中旧缓存 ⇒ **数据文件不刷新**，
表现为「改了没生效 / 最终推荐永远是昨天的」—— 正是主人最在意的体感。

---

## 二、判据（可复跑，改完/推完都该跑一遍）

```bash
# 对每个 data/X.js：比较「文件末次提交时间」与「index.html 里它的 ?v」
git log -1 --format=%ct origin/main -- data/STOCK_STOP_DATA.js          # 1790167182
git cat-file blob origin/main:index.html | grep -o 'data/STOCK_STOP_DATA.js?v=[0-9]*'
#                                                                        1790166348 ⇒ 落后 834s，异常
```

🔴 **必须排除非 unix 秒的占位戳**：`data/maharo_*.js` 的 `?v=20260923`（**8 位**）是占位格式，
若一起判定会得出「落后 17 亿秒」的荒谬结论并把**好文件改坏**。判据：

```python
if not (len(tok) == 10 and tok.isdigit() and int(tok) >= 1_000_000_000):
    continue        # 跳过占位戳
```

我实测这一轮**积压 32 个落后文件**（差值恒为 1181s）。

---

## 三、权威口径（禁自创，我一开始差点用错）

| 项 | 权威值 |
|---|---|
| `?v` 语义 | **单调 unix 秒令牌**（2026-09-10 起，旧「内容哈希」口径已废除） |
| 唯一实现 | `update_v8.py::_data_file_update_time()` → `_UPDATE_TOKEN = str(int(time.time()))`，**一次构建所有 `data/*.js` 共用同一令牌** |
| 已停用 | `scripts/reconcile_cache_busters.py`（2026-09-20 停写护栏，跑它只会在 index 里写回废除口径） |
| 自动对齐 | `.github/workflows/v8_cache_buster_reconcile.yml`（每次 data push + schedule 每 15 分钟，跑 `update_v8.py --only-cache-busters`） |
| ⚠️ 可靠性 | **不可当唯一依赖**：本轮实测该链**积压 27 分钟未跑**（仓库多条链并发 `in_progress` 时 schedule 被排后） |

---

## 四、修复（我用的实现：`E:/_alimi_tools/bfl3/deploy5.py`）

1. **基线 = 实时 tip 的 `index.html`**（不是本机快照）；
2. 目标戳 = `max(最近 N 次提交里的历史最高戳, 该文件末次提交时间)`，**只提升、不下降**；
3. **四道自证**（缺一不敢推）：
   - ① 用 `re.sub(r'(\?v=)\d+', r'\1X', s)` 归一化后，产物与 tip **逐字节相同** ⇒ 证明**内容零改动**；
   - ② 变化行数 ≥ 替换处数（同一文件可能在 index 里出现多行）；
   - ③ 已提升文件的**旧戳残留 = 0**；
   - ④ 28 块内联脚本 `new Function` 0 错 + 纯 LF + 末尾 `</html>`；
4. `force:false` + **422 循环重试**（本轮 tip **半小时漂了 4 次以上**，每次必须重新取 tip）。

**结果**：落后数 **32 → 0**；commit `dc468fc8`；线上真身 1,352,965 B 与推送件一致。

---

## 五、预防铁律（比修复重要，请你的链也照此自查）

> **凡推 `index.html`：基线一律取「实时 tip 的该文件」，并用唯一锚点逐条 `replace` 生成产物。**
> **禁止**「先在本地快照上改好、再取 tip 推送」这个顺序 ——
> 后者必然把「快照 → tip」之间**所有人的改动**一并顶掉，而且**只在 `?v` 上留痕，极难发现**。

自查你的推送路径三问：
1. 生成 `index.html` 内容的**那一刻**，基线是哪一版？（必须是当时的最新 tip）
2. 推送前有没有 `git fetch origin main` 并**基于新 tip 重建树**？（`base_tree` 新 ≠ 文件 blob 新）
3. 推完有没有跑上面的**戳落后判据**？（功能标记门禁**查不出**这个问题）

---

## 六、我已固化的资产

- 技能 `github-pages-cachebuster-reconcile` **§7 变体 B**：形态 / 判据 / 权威口径 / 修复 / 预防；
- 技能 `v8-offline-backtest-retrospective` **§4.5**：买点回测的「不可成交 + 未来函数」双重自我欺骗（本轮同批产出，与本事故无关但同日发生）；
- 仅新增本文档，**未触碰 `index.html`**（避免再引入竞态）。
