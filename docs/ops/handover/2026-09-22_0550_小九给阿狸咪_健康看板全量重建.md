# 2026-09-22 05:50 小九 → 阿狸咪 · 运维看板全量重建（index.html 热点）

## 1. 一句话结论

主人令「审计运维和看板，看板要所有前端的对应运行胶囊都写出来，照样按导航按钮和页面内部顺序排序」——
已按 **导航按钮顺序 + 各页 `<h3>` 真实卡序** 全量重建 `V8_PAGE_SCHEDULE` 与 `_PAGE_CARD_ORDER` 两张表：
**胶囊 40 → 59**、补上「共振日历」整页 0 胶囊缺口、剔除「盘后数据」8 个幽灵胶囊。
已推送 **commit `f3af92216732`**，线上 Pages 硬抓 HTTP 200 已生效。验证三连全绿。

## 2. 各批改动清单

| 文件 | 位置 | 改前 → 改后 |
|---|---|---|
| `index.html` | `const V8_PAGE_SCHEDULE`（L~12489 起） | 页 16 个（td6/rt10/ph14/st5/op5 其余 0）→ **页 16 个全量、胶囊 59**（td5/rt9/ph7/rc8/final2/st19/bt1/ul3/op5） |
| `index.html` | `const _PAGE_CARD_ORDER`（`renderHealthCheck` 内） | 6 组 → **9 组**；组内补 `OVERSEAS_MARKETS`/`COMMODITY_ELASTICITY`/`ETF_INTRADAY_HEAT`/`SECTOR_RECOMMENDATION`/`ETF_NET_SUBSCRIPTION`/`W52_HIGH`/`RESTRICTED_RELEASE`/`PERFORMANCE_FORECAST`/`CRISIS_DATA`/`MARKET_REGIME`/`FACTOR_LAB`/`FOUR_VOLUME_BACKTEST`/`IMA_STRONG_BACKTEST` |

> 仅这两块被改。其余区域零改动（逆运算逐字节证明）。

## 3. 根因（结构性，不是"该更新了"）

1. **看板是手写静态表**，与前端解耦 —— 导航/卡片一变，表就静默过期，且**不会报错**（组内排序未命中时静默回落 `it.order`，主人看到的是"改了没生效"）。
2. **`ph` 幽灵的来源**：前端轻量化把 ph 从「大盘/融资融券/期指/候选池…」重构为「ETF 三合一 + 板块资金趋势 + 市场宽度…」，但看板表没跟改 ⇒ 8 个胶囊指向已不存在的卡。
3. **映射错写**：「板块资金趋势」原写源 `SH_FIB`（该项 2026-09-15 已迁「观测平台」），实为 `SECTOR_RECOMMENDATION`。
4. **`rc` 整页漏登记**：前端 8 张龙虎榜卡共用单一数据源 `LHB_DATA`，看板从未登记。

## 4. 闭环终验表（改前 / 改后 / 判据）

| 项 | 改前 | 改后 | 判据（可复现） |
|---|---|---|---|
| 胶囊总数 | 40 | **59** | `V8_PAGE_SCHEDULE` 块内 `source:'` 计数 |
| `rc` 胶囊 | 0 | **8**（全 `LHB_DATA`） | 块内 `source:'LHB_DATA'` == 8 |
| `ph` 胶囊 | 14（8 幽灵） | **7** | `id:'ph'…id:'rc'` 段内 `name:'` − 1 == 7 |
| `st` 胶囊 | 5 | **19** | 同上段内计数 |
| 板块资金趋势源 | `SH_FIB` | **`SECTOR_RECOMMENDATION`** | 子串 `name:'板块资金趋势', source:'SECTOR_RECOMMENDATION'` |
| 巡检分组序 | 旧序 | **= 导航按钮顺序** | jsdom 渲染分组名序列 == `_PAGE_ORDER` ∩ 有数据页 |
| 巡检组内序 | 后端 `it.order` | **= 各页 `<h3>` 真实卡序** | jsdom 渲染组内 id 序列 == `_PAGE_CARD_ORDER[页]` |
| 线上 Pages | — | **HTTP 200 / 6/6 标记** | 缓存击穿抓线上 `index.html` |

验证三连：① 逆运算逐字节一致 ✅ ② jsdom 真实渲染（分组 9/9、组内 9/9、页序 9/9、胶囊 59/59）✅ ③ 远端真值 11/11 ✅ ④ 线上 Pages 6/6 ✅

## 5. 防复发保险清单（**请勿单方面撤销**）

- `V8_PAGE_SCHEDULE.pages` 的**页序 = 导航按钮顺序**；`_PAGE_ORDER` 同源。
- `_PAGE_CARD_ORDER` 的 key = **看板 page 名（= CARD_DEFS.page）**，value = **页面 `<h3>` 真实卡序的 id**。
- 🔴 **一律用 id 建序表**：巡检项 `name ≠ 页面卡名`（`INDEX_QUOTES` 巡检叫「全球指数/股指期货」，页面叫「指数快览」）⇒ 按 name 匹配必然失配。
- 维护判据写死在注释里：**导航加页/改序 ⇒ `_PAGE_ORDER` 跟改；页面加卡/改序 ⇒ `V8_PAGE_SCHEDULE` 与 `_PAGE_CARD_ORDER` 两处跟改。**

## 6. 可复现验证方式

```python
# ① 逆运算：重建脚本内 assert rev == content（逐字节）
# ② 渲染：node + jsdom，注入覆盖全部 id 的 HEALTH_CHECK，调 renderHealthCheck()/renderTaskSchedule()
#    锚点：分组标题 "letter-spacing:.3px;\">" 后随 ">组名</div>"
#          组内项名 "font-size:12px;font-weight:600;color:var(--txt);\">" 后随 ">卡名</span>"
# ③ 远端真值：GET /git/blobs/<sha> 逐项 grep；Pages 缓存击穿抓取
```

## 7. 给阿狸咪的待办与提醒（🔴 最要紧）

- 🔴 **`index.html` 是双机共写热点**。你改之前**务必先 `git pull`**；push 前再核 tip 是否前进；**绝不 force**。
  （同型事故前科：`e49e106b8`、`347eaa0bca`。）
- ⛔ **不要**把本次重建的两张表"回滚成旧版"——旧版是**幽灵表**（指向已不存在的卡）。
- ⛔ 若你要动「盘后数据/选股策略/共振日历」的卡，请**同时**更新 `V8_PAGE_SCHEDULE` 与 `_PAGE_CARD_ORDER`，否则看板会再次静默错位。
- ℹ️ 本次改动**不改任何数据源、不改后端**，纯前端两张表的顺序与覆盖。

## 8. 推送信息 + 主人令存档

- commit：`f3af92216732cdafb09267d6e97810f95c93f653`
- 基底（推送前线上 tip）：`539de24a2523`（md5 `bba5ccf14c0f`，基底断言通过，未覆盖他人改动）
- 推送器：净室推送器（API 隔离索引 / `force:false` / 成功轮当场回读 blob **逐字节一致** md5 `187c52a37262`）
- 记录：`~/.workbuddy/v8_push_log/20260922_055249_PUSHED.json`
- 主人令：「审计运维和看板，看板要所有前端的对应运行胶囊都写出来，才知道到底哪里出问题了，照样按导航按钮和页面内部顺序排序」
