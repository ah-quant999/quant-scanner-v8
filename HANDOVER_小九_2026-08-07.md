# 小九 → 阿狸咪 · 2026-08-07 交接文档

> **更新于 2026-08-07 12:00 自动更新**（12:00 档定时交接，覆盖 11:57 档）
> 面向：阿狸咪（18:00 后 / 周末纯代码编辑，不跑 deploy_v8.py）
> 源：当天工作日志 `.workbuddy/memory/2026-08-07.md`（截至 11:27 全部改动；11:27 后无新增条目，本次为状态快照刷新）
> 本次新增：main HEAD 已推进 `99126c8 → 63db748`（v8 build 11:58）；其余内容与 11:57 档一致。

---

## 一、背景

今日是数据治理 + UI 紧凑化大改日。上午发生「健康巡检大面积失败」「实时数据全空（本地工作树事故）」「三卡/全量数据停更 3~8 天」三起事故，均已修复并收口。另按主人要求完成多轮 UI 紧凑化（今日判定卡、机游弹窗、连板天梯等）。远端 main 目前 HEAD=`63db748`，站点由 v8_build_deploy 持续构建上线（最新 11:58 success）。

**一句话给阿狸咪**：今日 UI/数据改动全部安全入库；唯一需要你关注的是「17:00 选股四模块任务仍 PAUSED」与「云端 schedule cron 不触发」两个遗留问题（见第四节），其余按常规只读即可。

---

## 二、今日已完成改动（真实 commit SHA 已核实，均在 origin/main 历史中）

### 数据治理与事故修复
| SHA | 内容 | 影响 |
|---|---|---|
| `7ac0777` | fix(CRDS)：calc_crds 兼容 dict 金股池、过滤港股(5位代码)、单票异常 try/except 兜底；补全 08-07 数据 | CRDS 不再崩溃；v8_algo_cloud 下次 18:30 会自动重生成 |
| `9e6b053` | 三重共识(3天→今日) + CRDS(6天→今日) 数据补更（含 crds_result.json→crds_card_data.json 文件名映射修正） | 两张卡恢复新鲜 |
| `831e99b` | 补回 NT_DATA 盘前刷新；build_deploy workflow_run 监听改 `cn·应急` 新名；cn_fetch_cloud case 匹配 1-8 | 健康检查「市场提示」恢复 ok；本机应急 runner 改 raw_data 后能自动触发构建 |
| `d01c475` | 修复 3 天停更：SH_FIB / SECTOR_RS / CANDIDATE / CRDS / 三重数据全部刷新（本机算法链补跑） | 实验选股/候选池/板块周期恢复 |
| `df952d8` → `a95c0f1` → `be9afb4` | 全量算法链修复三连：9 核心 + 4 孤儿(NT/停牌/板块资金/volatility_cache) + TOP10 + market_alerts 全部 8/4→8/7 | 12 张停更卡全部恢复 |
| `915c5af` | 全链刷新（后台 run_algorithms 22/22 成功）：GOLD_POOL 下游链 + COCKPIT_ADVICE + TRIPLE_HISTORY 全部 8/4→8/7 | gold_pool 下游不再是停更盲区 |
| `3bfe2c9` | fix(cloud)：重新实现盘前清空函数——**保留主力净流入(CAPITAL_FLOW_DATA)不再整张清空**，仅 premarket 阶段触发，其余 intraday 卡写 no_data 占位 | 修复「实时数据全空」事故根因；盘中/盘后绝不触发 |
| `5dc27ed` | fix(IPO)：今日上市按 list_date==today 区分 listed_today/tracking；删除「实时风险温度计」「决策中枢」两卡（主人早前要求，彻底清理 render 函数/CSS/任务看板条目/RISK_GAUGE 映射） | 今日上市 301707 N展芯 正确显示（首日约 +394%）；卡总数精简 |
| `00f745a` | fix：IPO 影子股匹配 + Tab 颜色改亮 | 已上线 |

### UI 紧凑化（主人拍板风格：不要大版面，2-3 列/单行紧凑）
| SHA | 内容 |
|---|---|
| `814998c` | 今日判定卡紧凑化：判断/危机/仓位/共识一行 + 决策四步一行 + 候选列显示实际价/止损/止盈 |
| `134a563` | 机游共振「净卖出」从真实负净值取，不再显示为正号 |
| `cc0655f` | 今日判定环境卡再压缩：结论置顶、美股/结论/警告单行、危险信号单行 |
| `6ec0d23` | 连板天梯 UI 微调：①分布图无划线确认 ②≥3板股票名加底色划线格 ③两个今日判定标题区分（环境综合 / 可入手候选） |
| `8c1efb9` | IPO 影子股不再过滤——不建议申购的 IPO 也展示关联影子股（如双英集团→宇树科技） |
| `9a7dd1c` | 温度计+决策中枢移入🔒暂未上架（与今日判定宏观/技术/情绪指标重叠，信号精简） |
| `8483488` | 机游弹窗单行：名称+代码+金额+涨幅同行，字体 11→13px |
| `9634627` | fix(health)：运维面板 CARD_DEFS 名称/顺序/覆盖与任务运行看板完全对齐 |
| `6ea4347` | 决策四步+三不原则合并一行横排，候选列表全宽 |
| `4897957` | 连板分布 ≥3板股票名写入条形数字格，删除独立高连板梯队区域 |
| `0e9dd06` | 温度计+决策中枢移入 🗄️已下架（数据停更 3 天 + 与今日判定重叠，只需看后者） |
| `66ce1be` | 删除暂未上架重叠卡：只保留 ⚖️今日判定·环境综合，移除 🎯可入手候选+决策纪律 |
| `4aecb48` | 「已跑完」胶囊消除昨日误报：数据日期==今天→「✅今日已跑完」，≤30天→「✅MM-DD 已完成」，统一 _runBadgeInfo 5 处胶囊 |

### 凌晨阿狸咪侧改动（已同步，供回顾）
- `5af000c` 暂未上架提升为一级 locked Tab；`41329f2` 合并今日宏观解读+环境态势为一张卡；`e215f7b` 接入时事滚动与真实美股隔夜数据；`d65fd82` 共振日历格子缩小字号放大；`7d421be` 今日判定风控位置+连板天梯字号；`4fc6684` 板块周期卡按周期分组 20 日涨幅排序；`5ed70d5` posAdvice 崩溃修复；`0a8cb3c` 胶囊统一 CST 日期；`5198980` 构建不再篡改「更新于」+ 已跑完胶囊修复。

---

## 三、当前系统状态

- **站点**：GitHub Pages v8 在线；v8_build_deploy 持续构建，最新 **11:58 success** 已上线（含 `00f745a`）。
- **main HEAD**：`63db748`（v8 build: 2026-08-07 11:58）。
- **本机应急 runner（cn·应急）**：正常工作，最新 cn fetch 11:47 已入库；workflow_run 监听已改新名，改 raw_data 后自动触发构建 ✓。
- **云端 schedule**：`v8_cn_fetch_cloud` 今日 schedule **仍未触发**（最后 schedule 为昨日 23:17），持续不稳定 → 当前靠本机应急 runner 兜底。
- **算法链**：v8_algo_cloud cron 已从 4 条精简为 1 条（08-06 22:36 部署），**今晚 18:30 为修复后首次窗口**，请观察是否触发。
- **健康巡检**：第六十四轮（11:44）六管线全绿 0 邮件；fail2→7 均为盘后数据陈旧（P0②），等数据源/云端刷新后自然恢复。
- **自动化**：14 个 v8 任务（8 ACTIVE + 6 PAUSED），model 全部 `deepseek-v4-flash`（8-06 主人令，回 hy3 待主人另行通知）。
- **工作树**：本地有一批 `data/*.js` 未提交修改（本机生成产物/应急 runner 中间态），**属正常，不要手动 add**；会由自动任务处理。

---

## 四、需阿狸咪注意 / 待办 / 风险

1. **【P0】17:00 选股四模块任务仍 PAUSED**（8/5 后未跑）——它是三重共识 + CRDS 的**唯一生成入口**，今早靠小九手动补跑才恢复。建议与主人确认是否恢复该任务（恢复后 gen_triple_consensus.py + calc_crds.py 每日自动跑，不再依赖手动）。
2. **【P1】calc_crds.py 输出名不匹配**：生成器输出 `out/crds_result.json`，但 update_v8.py DATA_SOURCES 映射的是 `crds_card_data.json`。建议：①改 calc_crds.py 输出名为 crds_card_data.json，或 ②17:00 任务 prompt 里加 `cp` 步骤。当前 raw_data 已手动对齐，但下次生成器运行仍会写错名。
3. **【P1】云端 schedule cron 不触发**：cn_fetch_cloud 今日 0 次 schedule；algo_cloud 新 cron 今晚 18:30 首窗口。若 19:00 仍无触发，需查 repo settings / Actions 权限或改外部定时器。
4. **【P2】盘后数据陈旧**：健康检查 fail7 均为盘后类（2-3 天前），依赖数据源恢复与云端 schedule；19:00 审计任务已有「停更>1天则本机补跑 run_algorithms.py」自愈逻辑兜底。
5. **【约定】部署闸门不变**：工作日 08:00~17:00 小九唯一部署者；阿狸咪白天 deploy 会被闸门 exit 0；18:00 后/周末你可纯代码编辑但**不跑 deploy_v8.py**。
6. **【模型】全部 deepseek-v4-flash**；**不要自行切回 hy3，等主人通知**。

---

## 五、小九监督职责（运行中）

- 12:00 / 18:00 交接文档自动更新（本任务）。
- 每小时看板 pull + 看门狗巡检（URGENT 轮次 53~64 持续中，六管线连续 20+ 轮全绿）。
- v8 盘后数据守护（self-heal，CST 16:00-21:00 每小时）：检测盘后关键数据非今日/runner 离线 → 自动 dispatch 补救。
- 19:00 审计：盘后数据停更 >1 天则本机补跑算法链自愈。

---

*本交接由自动化任务生成（12:00 档）。下一档 18:00 自动覆盖更新。*
