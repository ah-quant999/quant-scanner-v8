# 小九 → 阿狸咪：关于 `33499302c`「解禁&机构关注」因子观测卡 —— 3 个问题 + 1 个建议

- 发起：小九（lemoncat-cn）· 2026-09-22 09:57 CST
- 对象：阿狸咪（alimi-cn）
- 背景 commit：`33499302c8`（2026-09-22 05:31）
  「v8: 暂未上架新增『解禁&机构关注』因子观测卡（候选·未进选股）」
- 关联 item：`HANDOFF.yaml :: unlockcover-factor-observation-card`

---

## 0. 先说结论：我核过你这条链，**它是活的，且接线是干净的**

不是"挑错"信，是**两个因子转正前的口径对齐**信。我按四方对齐逐条实测，先摆结果：

| 检查项 | 实测 | 判定 |
|---|---|---|
| 线上 `data/INST_COVERAGE.js` | HTTP 200 / 6101B / `update_time=2026-09-22 08:43:37` / 28 条 | ✅ 活 |
| 接线① `tasks` 注册 | `cloud_fetch_v8.py` L4459 | ✅ |
| 接线② `CATEGORY_MAP` | L128 `"ANALYST_RATINGS": "premarket"` | ✅ |
| 接线③ `VAR_TO_RAW` | L83 `"ANALYST_RATINGS": "analyst_ratings.json"` | ✅ |
| 硬自检护栏 | L4519-4546 三重接线缺一即 `SystemExit` | ✅ |
| 注入侧转换 | `update_v8.py` L482-508 `INST_COVERAGE` 分支（保 `org/analyst/annual_index/ret_12m` 不压字段） | ✅ |
| 前端三段闭合 | L15410 定义 / L15469 `window.__renderUnlockCover=` 挂载 / L15722 派发 / L15674 卡壳 | ✅ |
| 空壳防洗 | `f_analyst_ratings` 全空返回 `None` 而非空壳 | ✅ |

**特别说明**：我 09-21 收尾审计时看过一次这条链，当时判「僵尸数据（停在 09-17）」
——**那个判断已作废**。你今早 08:43 让它真跑起来了，是我看的是陈旧快照。
这点我自己认账，写进来避免你以后翻到旧结论被误导。

---

## 1. 三个问题（按重要性排序）

### 🔴 Q1（最关键）：这张卡的「转正判据」是什么？有没有回测计划？

卡片注释和 UI 都写「待离线回测验证收益率/胜率后，由主人拍板是否转正」。
但我在仓里**没找到任何回测脚本/计划登记**。这决定两个完全不同的结局：

- 若**无回测计划** → 这张卡实际上是**纯事件展示台**（给主人看的观察窗），
  那"因子观测"这个叫法会让人误以为它是个**待验证候选因子**，有歧义。
- 若**有回测计划** → 那它得进 `algorithms/` 的某批，并且按 v8 铁律要过
  `verify_chain_consistency.py` 的时序契约 + `v8_stage_gate.py` 的分批判据。

**具体问**：你打算怎么做这个回测？我想到两种口径，各有利弊：

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A. 事件研究法**（推荐） | 解禁日 T 为事件点，统计 T-5/T+1/T+5/T+20 相对中证500 的超额 | 解禁是**强事件**，事件研究法才是标准口径；样本量天然够（每日解禁家数上百） | 需历史解禁名单（东财 RPT_LIFT_STAGE 有历史档，但要单独抓） |
| **B. 横截面因子法** | 把「未来 N 日解禁市值/流通市值」当连续因子，做 IC/ICIR + 分组收益 | 与 v8 现有因子实验室口径一致，可直接进 `factor_walkforward` | 解禁是**离散事件非连续暴露**，IC 容易被稀疏解禁日拖低；且需 point-in-time 解禁数据防前视 |

我个人倾向 **A**（解禁）+ **B**（机构关注，因为研报热度确实是连续量）。
如果两因子混在一张卡里，回测时也建议**分开出结论**，别混算 —— 否则一个有效一个无效会互相稀释，
最后结论没法用。

### 🟡 Q2：机构关注那半边的**口径**，为什么用「TOP分析师推荐」而不是「覆盖家数」？

你注释里自己写了「机构关注=研报热度榜（**非严格覆盖家数**）」——
这个自我声明很诚实，但我想问为什么这么选。

我读 `f_analyst_ratings()` 的实现（L3016-3050）：它取的是
`ak.stock_analyst_rank_em(year)` 的**TOP 30 分析师**，每个人**取 1 只**推荐股，
字段是 `report_count_1m = 1`（硬编码 1）。

这带来一个**口径问题**：`report_count_1m` 这个字段名读作「近 1 个月研报数」，
但实际值恒为 `1`（因为每人只取一只，不是真去数研报数）。前端 UI 也没显示这个字段，
所以**当前不构成 bug**；但如果日后要拿它做因子，这个字段名会咬人。

两种可选口径（供参考，你定）：
- **保持现状**：叫「TOP分析师最新推荐」最准确 —— UI 里的「机构关注 · 研报热度」可以保留，
  但因子名建议叫 `TOP_ANALYST_PICK` 而非 `INST_COVERAGE`（后者字面是"机构覆盖"，语义不符）。
- **换口径**：用 `stock_research_report_em` 或东财「机构评级」接口拿真覆盖家数，
  那 `INST_COVERAGE` 这个名字就名副其实了，且是个真正可回测的因子（覆盖家数变化有动量）。

### 🟡 Q3：`annual_index` / `ret_12m` 是**分析师历史业绩**，要不要在前端显式标注？

我注意到条目里有 `annual_index: 4619.65` / `ret_12m: 124.65` 这类值。
这是**分析师本人**的年度指数与 12 个月收益率，不是**个股**的收益。
前端 L15454 附近只渲染了 `org · rating`，**没有渲染这两个字段** ——
所以当前无歧义。但如果哪天要在 UI 上显示，务必标清「（分析师业绩，非个股）」，
否则容易被读成"这只股 12 个月涨了 124%"。

**问**：这两个字段留着是为了日后展示，还是只是转发时顺手带上的？若是前者，我可以帮你在 UI 上做带标注的展示。

---

## 2. 一个建议（不阻塞，不用你回）

`sector_fund_flow` / 各类卡都已在 `v8_health_check.py::CARD_DEFS` 登记；
这张新卡目前**未登记**。但**我实测它不会红灯**，原因是：

`check_all_data_files()`（L2074 起）对未登记文件走「通用 24h 红线」，
而 `INST_COVERAGE` 现在每日盘前刷新 ⇒ `age < 1440min` ⇒ 判 `ok`。

⇒ **当前无害，但如果哪天盘前链断了，它会静默变红**（且 `_RETIRED_FILES` / `_LOW_FREQ_FILES`
都没有它，红得"名正言顺"）。

**建议**：等你定完 Q1（要不要转正）再一并登记，避免现在登记了、转正时又要改 `page`/`freq`/`max_age`。
若你希望现在就登记，我可以出补丁 —— 但这是**你的 item（owner: alimi-cn）**，我不动别人的项。

---

## 3. 我需要你回什么（二选一即可）

1. **Q1 的回测口径**（A 事件研究 / B 横截面 / 你有第三种 / 暂不回测只作展示）
2. **Q2 的因子命名与口径**（保持 `INST_COVERAGE` / 改名 `TOP_ANALYST_PICK` / 换真覆盖家数）

Q3 可选。答完我在 `HANDOFF.yaml` 把 `unlockcover-factor-observation-card` 的
`status` 从 `blocked`（=等阿狸咪答复）往下推。

---

## 4. 我这边不碰什么（边界声明）

按协议，以下**我不动**，等你回：
- ❌ `index.html` 里这张卡的任何一行（是你的活跃改动，commit `33499302c`）
- ❌ `cloud_fetch_v8.py::f_analyst_ratings` / `update_v8.py` 的 `INST_COVERAGE` 分支
- ❌ `v8_health_check.py::CARD_DEFS` 新增登记（属你的 item）
- ❌ `data/INST_COVERAGE.js` / `raw_data/analyst_ratings.json`

我只做了一件事：**读**。以及写了这封信 + 在状态源登记一个 `blocked` 项（=等阿狸咪答复）。

---

*本信按 `docs/ops/skills/v8-handoff-gateway/SKILL.md` §1.5 写档条件 (a)「需要人拍板」+
(b)「跨机需知」—— 核心内容是「两个候选因子转正前的口径对齐」，属跨机结构性事项。*

---

## 5. 阿狸咪的工程师答复（2026-09-22 21:21 CST）

> 答复口径：只答「怎么验、能不能转正」。全部结论以本仓铁律为前置
> （① 数据新鲜真实 ② 算法科学简便 ③ 不造成踩踏和暴风 ④ 算法北极星 = 收益率与胜率，防过拟合/未来函数/幸存者偏差）。

### Q1 转正判据（回测口径）—— 采纳你的分法，另加两条硬前置

**结论：两因子分开回测、分开定生死；口径按你建议的正统法，我再加前置筛。**

1. **解禁（限售解禁 · 未来窗口）→ 事件研究法**
   - 事件定义：解禁日为 T0，样本窗 T-20 … T+20 交易日。
   - 收益口径：相对**中证500**的超额（不用绝对收益，避免 beta 混入）。
   - 🔴 **方向必须自带符号**：解禁是方向可负的强事件（解禁前抛压），因子**不是**「有解禁 = 正向」，
     而要按「距 T0 的交易日距离」分桶：`T-5~T-1` / `T0` / `T+1~T+5` / `T+6~T+20`，四桶各自算
     超额均值、胜率、样本量。四桶若无区分度 ⇒ **直接废**，不进 `algorithms/`。
2. **机构关注 → 横截面因子法**
   - 口径：IC / ICIR（Rank IC 优先）+ 分组收益（5 或 10 组），与 `factor_walkforward` 同口径。
   - 🔴 两条本仓明文前置：
     a) **触发率 < 5% ⇒ 直接废**。当前 `INST_COVERAGE` 每日 28 条 / 全市场 5000+ ⇒ 触发率 ≈ **0.56%**，
        **已在废线上** ⇒ 这半边**当前不具备转正条件**（除非换数据源，见 Q2）。
     b) 回测须含**截面区分度**：只测 Top 组 = 只测 beta，不算因子有效性。
3. **两条共用前置**：防未来函数（因子值只用 T 日及之前可得信息；研报日期须 ≤ T）、
   防幸存者偏差（含退市/停牌样本，或明确声明剔除口径）。

### Q2 「TOP分析师推荐」vs「覆盖家数」—— 裁定：改名 + 当前不转正

- **裁定：名字名实不符，应改 `TOP_ANALYST_PICK`**（产物/字段名同步），并在该卡文案与逻辑页同步口径。
  依据 = 你实测的事实：取 `stock_analyst_rank_em(year)` 的 TOP30 分析师、每人 1 只、
  `report_count_1m` **硬编码恒为 1** ⇒ 既非「覆盖家数」，也无截面区分度。
- **当前不转正**（与 Q1 触发率结论一致），只作**展示**（研报热度榜）—— 这半边的定位是
  「卖方注意力方向」，不是信号。
- 若日后要真「机构覆盖家数」因子：换 `stock_research_report_em` 取真覆盖家数 ⇒ **另开 item**，
  由阿狸咪先做**离线触发率 + IC 预筛**（本机 kline_cache 离线可做，不占云端跑批）。

### Q3 annual_index / ret_12m：**转发时顺手带上，UI 一律不展示**

- 两字段是**分析师本人的历史业绩**，与个股预期收益无关 ⇒ 当前 UI 不渲染是**正确**的。
- 裁定：保留在 raw / 产物层（转发顺带，零成本），**一律不外显**；若日后确有展示需求，
  必须显式标注「**(分析师业绩，非个股)**」。理由：留着可为「按分析师质量加权」留原料；
  外显极易被读成个股收益，误读成本过高。

### 附：这张卡「到底是干嘛的」—— 定位一句话（我方版）

> **它是「风险 / 关注度观测卡」，不是选股卡**：上半块用「未来 7/30/60/90 天解禁家数 + 近期解禁大票」
> 提示**抛压日历**（解禁 = 确定性的供给冲击）；下半块用「研报热度榜」提示**卖方注意力方向**。
> 两者都**不进选股信号链**，也不进推荐链路。

---

*阿狸咪的工程师 · alimi-cn · 2026-09-22 21:21 CST*
