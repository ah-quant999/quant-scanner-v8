# 交接单：抓取层与推送链永久修复（#170 / #171）

**日期**：2026-09-12（周六 · 阿狸咪值班）
**署**：阿狸咪的工程师
**交接给**：小九（lemoncat-cn，工作日机）

## 一、本批修复清单（已全部推 main，三查核验 done）

| # | 问题 | 根因 | 修复 | 文件 |
|---|---|---|---|---|
| #170① | 夜间备份审计 job 永久硬失败（红） | `verify_daily_audit.py` 白名单漏判 bot 前缀，备份/缓存戳对齐的自动 commit 被误判为「人工未提及」 | 白名单补 `chore(v8):` / `chore:` / `backup:` | `verify_daily_audit.py` |
| #170② | 实验卡（估值分位/因子审计/指数估值中枢）整日红灯 | 推送步 `git fetch origin` 偶发 TimeoutExpired → rebase 失败 → 3 次重试全败（run#34601506282 实证） | 推送步改 `api_push_raw.py`（Contents API，零 git fetch），`index.html` 已含于 PUSH_FILES | `v8_cn_fetch_experiments.yml` |
| #170③ | ima 同步偶发失败 | 同根因：`git fetch/reset` 超时杀 job + `git push HEAD:main` 偶发非快进 | 同步步改 `continue-on-error` + git 命令 `|| echo`；推送步改 `api_push_raw.py` | `v8_ima_strong_stock.yml` |
| #171① | 抓取层无市场日历闸门（连续休市段每日重抓相同数据） | cron 仅限工作日，节假日落在工作日仍触发 | 新增「🛡 交易日历闸门」步，复用 `check_trading_day.py`；**fail-open**（判失败/异常→仍继续）；仅「工作日+非交易日」跳过重抓；周末周度刷新(category=all)/手动全量 不闸 | `v8_cn_fetch_cloud.yml` |
| #171② | `v8_algo_run.yml` 过期误导注释（声称固定到专属 label 实际仍是共享 cn） | 注释与代码不符，专属 label 实测从未配置 | 注释修正为「共享标签 cn，双机谁空谁接」 | `v8_algo_run.yml` |
| #171③ | LHB 单飞 workflow 缺 concurrency group | 与其他 `[self-hosted,cn]` workflow 无去重 | 加 `concurrency: v8-lhb-fetch, cancel-in-progress:false` | `v8_lhb_fetch.yml` |

## 二、行为变更须知（小九请留意）

1. **日历闸门**：`v8_cn_fetch_cloud.yml` 在「工作日 + 非交易日（节假日）」会跳过主抓取步与分红刷新步（幂等闸门之后）。**交易日、周末周度刷新、手动 `all` 不受影响**。国庆/春节等长假期间若发现云端抓取静默，属预期（非 bug）。调休上班日若 `v8_date` 判断正确仍会跑。
2. **实验卡 / ima 推送**：已不再走 `git push HEAD:main`，改为 `api_push_raw.py`（GitHub Contents API）。调试时不再看 git 推送日志，改看 api_push_raw 的 `待推送文件` / `已推` 输出。
3. **LHB 单飞**：现已与自身历史 dispatch 去重，不会与其他 cn workflow 互踢。

## 三、验证（三查）

- ① 进程/run：6 个修复 commit 已推 main（见下方 SHA）。
- ② 数据/代码：`git show <sha>` 逐文件 diff 核验，改动仅限预期文件、行尾 LF 守恒、无字面 \U 转义残留。
- ③ 远端：`fetch` 后 `FETCH_HEAD` == push commit，match=True。

远端 main 顶 6 个 commit（新→旧）：

- `43f50f5d` 修正日历闸门步名字面 \U→🛡（收尾）
- `99bd81853` #171① 日历闸门
- `132069999` #170③ ima
- `096300a83` #170② 实验卡
- `57e79c87b` #171③② struct
- `4adde61bd` #170① 审计前缀

## 四、小九侧动作

- 无紧急动作。若工作日发现某卡空窗，先确认是否为「节假日跳过重抓」（看 run 日志 `proceed=false`）。
- 实验卡/ima 推送异常时，查 api_push_raw 输出而非 git push 日志。
