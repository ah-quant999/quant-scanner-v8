# 🚨 URGENT｜阿狸咪的工程师 → 小九的工程师｜2026-09-13 13:11｜心跳从未产出入库 + 双机工作区落后撤销远端修复 + RPS 考核回测结论

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主人 12:41 明令「**马上和小九紧急交接，让她马上处理，处理完也和你紧急交接**」。**三项 P0 需要你出工**：① 双机心跳 `raw_data/hb_xiaojiu.json` **从未入库过**（远端 `raw_data/` 与 `data/` 双双确认不存在）⇒ 阿狸咪侧 `v8_peer_monitor` 已是 fail-closed，**现在会每轮误报「小九掉线」**；② 你的 `v8_runner_guard.py --push` 是否真跑到心跳分支，只有你那边能自查；③ 双机共同隐患：**跑批用的是各自「本机工作区」文件，与远端不一致 ⇒ 远端已修的代码被静默撤销**（本机 09-13 12:5x E 批实测，证据见 §一 P0-2）。

---

## 一、P0：请你马上处理

### 🔴 P0-1 心跳 `hb_xiaojiu.json` 从未产出入库（真 bug，安全项）

三查证据（全部实测，非推断）：

| 检查项 | 命令 | 结果 |
|---|---|---|
| 远端 `raw_data/` | `git cat-file -e FETCH_HEAD:raw_data/hb_xiaojiu.json` | **不存在** |
| 远端 `data/` | `git cat-file -e FETCH_HEAD:data/hb_xiaojiu.js` | **不存在** |
| 本机 | `ls raw_data/hb_xiaojiu.json` | `No such file or directory` |
| 写入方接线 | `v8_runner_guard.py` L600 `write_heartbeat()` / L820 调用 / L622 `push_heartbeat_file()` / L832 调用 | 本机与远端**逐行一致**，接线**完整** ✓ |
| 消费方 | `v8_peer_monitor.py` L91–102 | **已 fail-closed**（09-12 改）：文件缺失 → 判小九【掉线】+ 告警 |
| `update_v8` 映射 | L108 `"hb_xiaojiu.json": "HB_XIAOJIU"` | 已登记 ✓ |
| gitignore | `git check-ignore -v raw_data/hb_xiaojiu.json` | **未被忽略**（可入库）⇒ 不是被忽略掉的 |

⇒ 结论：该机制自 09-12 建立至今**零产物**（远端从未出现过这两份文件）。可能原因（**请你自查后回执**）：

1. 你那台跑 `v8_runner_guard.py` 时**是否带 `--push`**？（不带则只写本地文件、不推送）
2. **`V8_SKIP_HB=1` 是否被设**？（设了就整段跳过心跳）
3. `push_heartbeat_file()` 内 `_load_token()` **是否取到 token**？（取不到会返回「无 GitHub token，无法推送心跳」，日志里应能看到）
4. 跑 guard 的那个 **WorkBuddy 自动化 / Windows 计划任务是否还 ACTIVE**？

⚠️ **影响面**：阿狸咪侧 `v8_peer_monitor` 现为 fail-closed ⇒ **每轮都会误报「小九掉线」**，告警信噪比归零；且「双机掉线自动接管」也失去可信判据。**请优先修这条。**

---

### 🔴 P0-2 双机「本机工作区 vs 远端」一致性（共同隐患，会互相撤销修复）

实测（本机 09-13 12:5x，`FETCH_HEAD = 8051178d794`）：

| 文件 | 本机工作区 | 远端 `FETCH_HEAD` | 差异性质 |
|---|---|---|---|
| `v8/backtest_rps.py` | `lookahead_days=380`；回撤 = **拼接累加**（`all_path` / `global_max_dd`） | `lookahead_days=LOOKAHEAD_DAYS`；回撤 = **逐信号峰谷再取均值** | 远端是我 09-13 的修复 |
| `algorithms/strategy_four_volume.py` | 有 `equity_path`，`HOLD_LADDER` 到 250 | 有 `equity_path` + `nets`，`HOLD_LADDER` 到 90 + 同源注释 | **两侧在改同一处** |
| 文件名哈希 | `git hash-object --no-filters <f>` | `git rev-parse FETCH_HEAD:<f>` | 抽查 6 个文件 **6/6 全部不等** |

⇒ 结论：**本机 E 批（12:58 写 `backtest_all_algos.json`）用的是工作区那份「旧回撤」代码**，会把 `RPS_BACKTEST` / `CRDS_BACKTEST` / `FOUR_VOLUME_BACKTEST` 的 `max_drawdown` 重新算成 −100% 以下的量纲错误值 —— **远端修复被从数据侧静默撤销**（典型「假成功」）。

**请你回执两件事**：

1. 你那台机的 worktree 与 `FETCH_HEAD` 是否一致？（逐文件 `git hash-object --no-filters <关键脚本>` vs `git rev-parse FETCH_HEAD:<f>`）
2. 你的跑批入口在跑之前**会不会先同步到远端**？若不会，我们需要在跑批入口加一道 **fail-closed 同步闸门**（我这边出方案，见 §五）。

---

### 🔴 P0-3 7 因子 walk-forward 回测（只有你能跑）

见 11:53 交接单 §一 P0，结论未变：`data/FACTOR_PROGRESS.js` 为 `total:7 / pending:7 / done:0`，**一个都没跑**。你在中国 IP 能拉 baostock / akshare，本机拉不了。

上线条件（主人 2026-09-09 决策，未变）：**`OOS IR > 0.3` 且 `4/5 年胜率 > 55%`**。跑完回填 `oos_ir` / `backtest_status` / `deploy_status` 进 `data/FACTOR_PROGRESS.js`，再 dispatch 重跑 `gen_factor_progress.py`。

---

## 二、我方本轮已上线（供你复核，不需你动手）

| commit | 内容 | 关键证据 |
|---|---|---|
| `33a4ebd487` | ① **删 `STOCK_PROFILE` 整链**（前后端 0 消费者；`data/STOCK_PROFILE.js` 1.33 MB 停产；`raw_data/stock_profile.json` **保留**，3 个真消费者不动）② **三线共振整链删除**（离线回溯 3120 只 × 一年 = 325,466 股票日，T+5 胜率 43.4% / 均值 −0.08%，破 45% 红线；`triple_count` / `triple_signals` 全仓零消费者）③ 主对比表 `avg_return` 列删除（**顺带修掉「表头 7 列 / 数据 8 td」的错位 bug**）④ 删 404 死链 `data/maharo_macro.js` ⑤ 清 8 个 `raw_data/*.json.bak`（2.9 MB）⑥ `raw_data/gold_pool.json` 清 454 处 `三线共振` 字段 | 线上九项自证全绿；`双线共振` 保留 15 处（`signal_count >= 2` 是金股池入池门槛，**未动**） |
| `f8a8f25960` | ① **回撤量纲一劳永逸**：三处（`strategy_four_volume` / `backtest_crds` / `backtest_rps`）由「多信号收益路径**首尾拼接累加**求峰谷」改为「**逐信号算峰值回撤再取均值**」——下界天然 ≥ −100% ② `backtest_rps` / `backtest_crds` 的 `lookahead_days=35` **硬编码**（覆盖了已定义的 `LOOKAHEAD_DAYS`）改引同源常量 ⇒ T+10/20/30/45/60/75/90 永久零样本根治 ③ `final_recommend` 的 6 个**展示类**字段 `safe_float` → `num_or_none`（消灭「取不到」与「真是 0%」不可区分）；排序/评分内部 7 处 `safe_float` **刻意不动** | 断言：旧回撤残留 0、新回撤三处各 1；`num_or_none` 5 处、展示字段 `safe_float` 0 处 |

⚠️ 承接 §一 P0-2：上表第 ② / ① 项的**产物**必须在「工作区与远端一致」之后重跑才会刷新。

---

## 三、RPS（相对强度）考核回测结论 —— 供你与主人参考，**未删任何东西**

主人问「**6 相对强度也做回测，看是否留**」。我方用本机 `raw_data/kline_cache`（3119 只，已复权、无跳日）独立复算，并加**全市场等权基准**（无基准无法判断有无 alpha）：

| 口径 | T+1 | T+3 | T+5 |
|---|---|---|---|
| A 档样本 | 159 | 96 | 28 |
| A 档胜率 | 38.99% | 39.58% | 35.71% |
| A 档均值 | −0.50% | −1.34% | −0.68% |
| **全市场基准胜率** | 56.72% | 47.89% | 48.28% |
| **全市场基准均值** | +1.26% | +0.77% | +1.64% |
| **超额均值** | **−1.76%** | **−2.11%** | **−2.32%** |
| 胜率差 | −17.73 pt | −8.31 pt | −12.57 pt |

**分信号日**（汇总是不同市况的混合，会掩盖真实结构）：

- 09-03：T+1 胜率 66.67% vs 基准 69.05%（**跑输**）；T+5 超额 −2.32%
- 09-04：T+1 46.88% vs 基准 65.93%（**−19.05 pt**）
- 09-07：T+3 22.22% vs 基准 33.70%（**−11.48 pt**）
- **09-08（市场转弱）：T+1 胜率 16.39% vs 基准 33.80%；均值 −3.18% vs −0.35%** ← 高动量股在拐点被血洗，而这正是最需要保护的时刻
- 09-12（周六归档，实为基于 09-11 收盘，入场日 09-14 未到）⇒ **不可测，未伪造成 0**

**分层检验**（A / B / C 同信号日、同持有期）：6 个可比组合里 **0 个「A 档明确最优」**，4 个「A 档最差」。即：相对强度**最强**的那一档，在同池内也是最差的。

另一条**独立口径**（你我都不需重跑）：`data/RPS_BACKTEST.js`（baostock 取数）为 `T+1 44.32% / −0.09%`、`T+3 41.07% / −0.27%`、`T+5 19.44% / −3.96%` ⇒ **三档全破双红线**（胜率 < 45%、均值 ≤ 0）。

⚠️ **但 RPS 是主人 2026-09-06 拍板的「30 天样本考核保护链」（约 2026-10 中截止）**，且框架明文规定「源不在算法链内 ⇒ 只入观察名单、**不提议下架**」⇒ **我方未删、也不会擅自删**。请你在未经主人明确授权前同样**勿删**。

附带说明：`RPS_BACKTEST` 的 `source_time` 停在 `2026-09-11 04:59`，主对比表里会一直显示「不新鲜」——这是「不在算法链内」的**必然结果**，不是 bug。

---

## 四、待你回执（09-11 起累计，我方 10+ 份未回执）

| # | 事项 | 出处 |
|---|---|---|
| 1 | `v8_health_check.py` 版本分歧，以哪版为准 | 09-13 01:27 交接单 §4.1（问过未回） |
| 2 | 宏观观测页 3 卡的更新频率口径（日 / 周 / 月） | 09-13 11:53 §一 P1-1 |
| 3 | 07:15 出稿任务归属哪台机 / 哪个 workflow | 09-13 11:53 §一 P1-2 |
| 4 | `data/maharo_macro.js` 是否弃用（我方已按「404 死链」移除前端引用；若你要保留请说） | 09-13 11:53 §一 P1-3 |
| 5 | 09-11 上轮 11 份待答项 | `audit/《今日未完成与待答清单_2026-09-11.md》` |
| 6 | **本轮新增**：§一 P0-1 的 4 个自查项、§一 P0-2 的 2 个回执 | 本单 |

---

## 五、本机本轮其它实测（供你避坑）

- 09-13 12:5x 本机跑了一次 `update_v8.py --category post_close`（由会话内 Bash 启动，进程链 `update_v8.py` → `v8_health_check.py`），重写 22 个 `data/*.js`；**未做任何 `git add` / `commit` / `push`**（该脚本只做只读 `git diff --name-only`）⇒ **不影响线上**。
- 但本机 `update_v8.py` L39 仍映射 `"stock_profile.json": "STOCK_PROFILE"` ⇒ 一旦 `raw_data/stock_profile.json` 有更新，会**重新生成已停产的 1.33 MB `data/STOCK_PROFILE.js`**。本次因源文件未变、走了「真实数据未变则跳过重写」分支，故未发生。**这与 §一 P0-2 是同一根因**。
- 本机另有两个 `E:\workspace\stock-scanner` 之外的 clone（`E:\workspace\workspace\quant-scanner-v8\`、`merged-dist\`）——按铁律**禁改**，仅提示勿混用。

---

**处理完请与我紧急交接回执。**
