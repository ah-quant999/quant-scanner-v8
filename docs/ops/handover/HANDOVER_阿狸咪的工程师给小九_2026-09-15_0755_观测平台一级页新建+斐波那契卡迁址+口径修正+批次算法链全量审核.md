# HANDOVER 2026-09-15 07:55 · 阿狸咪的工程师 → 小九

## 0 一句话结论

「🌍 观测平台」一级页已**上线并真浏览器验收通过**（commit `2c9a2cb6d60b`，父提交 `272a7470a7db`）。
随后按主人令做「更新时间批次 × 算法链」**全量审核**，跑出 **1 项孤儿脚本（红灯数据根因）+ 4 项需拍板/跟进**。
`logic.html` 一处过时数字已顺手修正。

---

## 1 本轮交付（已上线）

| # | 主人令 | 落地 | 证据 |
|---|--------|------|------|
| ① | 先修三处既有缺陷 | ①三个「高点」口径打架 → F卡显式标注口径（收盘价 06-22 收盘 4163.10 vs 最高价 05-14 最高 4258.86，注明「勿混比」）②F卡误显示前一交易日点位 → 前端用 `WAVE_ELLIOTT` 真实收盘序列**确定性修正**为 3885.33（-277.77 / -6.67%）③`renderFib` 死代码（16 处引用 / 0 处 id 定义 / 恒早退）→ 函数体 + 2 处调用点彻底删除 | 线上 F卡文案实测含「最新 3885.33（当日真实收盘 · WAVE_ELLIOTT）」；`has3888=false` |
| ② | 搬卡不合卡 | 「🧪 斐波那契F观测窗」整卡迁至「📊 艾略特波浪 · 深度分析」**下方**（原文搬运，禁手抄） | DOM 顺序实测 `elliottWaveBody@0 → fibWindowBody@1 → citicPeObserveBody@2 → indexObserveBody@3` |
| ③ | 改名 | 「大盘观察」→「大盘观测」（界面文案全改；残留 10 处均为注释，含 1 处行尾注释） | 线上源码串核对 |
| ④ | 新建一级 TAB「观测平台」 | 一级页 `sec-obs`，下挂两个**同级子页**：①🌍 宏观观测（左，2 卡）②📊 大盘观测（右，4 卡）。宏观观测由原一级页降级为子页；大盘观测 = 原「暂未上架 > 大盘观察」整页迁入 | 子 TAB 顺序实测 `0:🌍 宏观观测(active) \| 1:📊 大盘观测`；默认 `macro=block / market=none` |
| ⑤ | 旧入口清理 | 原一级入口 `__renderMacroPane` 全站 0 处调用点 ⇒ 彻底删除（**不留兼容别名**，避免死代码）；全站无 `location.hash` 深链路由 ⇒ 改名无断链风险 | 线上 `typeof window.__renderMacroPane === 'undefined'` |
| ⑥ | 暂未上架收敛 | 收敛为单子页（观测类 3 卡常显），删除 `ulPaneMarket` 整段 + 子 TAB 按钮行，派发器/自愈 probe 同步收敛 | 观测类 3 卡实测真渲染：`dbgEtfMapBody=5623 / themeValueBody=56093 / aiInsightsCompareBody=16772` |
| ⑦ | 文档同步 | `logic.html` 逻辑详解页同步 16 处（新架构 / 两子页去向对照表 / 口径警示 / 死代码说明） | `align_logic_ops.py` EXIT 0 |

### 渲染派发器（本轮新增的架构件，请知悉）

对齐「暂未上架」`__ulRenderPane` 成熟做法，新增 **`window.__obsRenderPane(tab)`** 作为观测平台两子页的**唯一渲染派发入口**：

- `__obsSwitch(btn, tab)` — 子页切换（含 `#sec-obs .obs-tab` active 态）
- `__renderObsPlatform()` — 平台骨架 + 子 TAB 按钮行 + 自愈启动
- `window.__obsActivePane` 默认 `'macro'`（宏观在左）
- 1.5s 自愈：`data-obspane` 标记幂等派发，防「骨架在 ≠ 内容在」

`dst` 判空**不写在重试逻辑之前**（这是 2026-09-14 那次「卡面永久加载中」的根因，已作为护栏固化）。

---

## 2 交付自证（全部实跑，非推断）

```
# 内联 JS 门禁（对即将推送的字节）
node gate_inline_any.js _push_stage/index.html  → checked inline scripts=27, errors=0
node gate_inline_any.js _push_stage/logic.html  → checked inline scripts=6,  errors=0
# 三源文档对齐
align_logic_ops.py  → EXIT 0
# 推后自证（GitHub API，绕 CDN）
tip(force) == 2c9a2cb6d60b                ✅
commit.parents[0] == 272a7470a7db          ✅（未改写历史）
index.html  remote 1127143 B == local      ✅ 字节级
logic.html  remote  722478 B == local      ✅ 字节级
# CDN 上线生效
curl Pages index.html → 1127143 B，cmp 字节级一致 ✅
# 真浏览器终验（线上）
子页顺序 宏观>大盘 ✅ / 默认宏观 ✅ / 大盘观测 4 卡真渲染
（14828 / 4056 / 6736 / 5094 B）✅ / 波浪卡 polyline 实测 700 点 ✅
已下架页：盘前横幅 + F卡 已移除，温度计·六维雷达等逐串计数未受影响 ✅
```

**注**：`push_atomic.py` 本次**不可用** —— 本机 `git fetch` 挂死 ⇒ 远端最新 tip 不在本地对象库 ⇒ `git read-tree <tip>` 报 exit 128。
改用 `push_via_api_obs.py`（GitHub Git Data API：blob → tree(base_tree) → commit(parents=[tip]) → 单次 PATCH refs/heads/main，`force=false`）。
该脚本已落在会话目录，**建议纳入 v8 常备工具**（本机 fetch 挂死是常态，不是偶发）。

---

## 3 批次与算法链全量审核结论

**真源（全部 `ast` 直读，禁 substring 猜窗口）**：
`algorithms/run_algorithms.py` → `STAGES`/`ORDER` ｜ `.github/scripts/v8_stage_gate.py` → `READY_SPEC`/`PREREQ`/时窗常量 ｜ `.github/workflows/*.yml` → cron/runs-on
审核快照：main tip `03dc8401c6`（期间小九在推，快照仅用于静态核对）

### 3.1 批次架构（真源）

| 批 | 脚本数 | PREREQ | 角色 |
|----|--------|--------|------|
| A | 13 | — | 采集（纯 fetch + 上游自产前置）；A 就绪是 B 门槛 |
| B | 26 | A | 选股/因子/池/策略 |
| D | 1 | B | 汇总（`final_recommend.py` → 最终推荐） |
| E | 11 | D | 回测 |

`set(ORDER) == set(∪STAGES)` = **True** ⇒ 分批模式**不丢脚本** ✅（模块级强校验成立）

### 3.2 时窗与执行机

| 项 | 真源值 |
|----|--------|
| `START_TRADING` / `START_T1` | 18:00 / 08:00 |
| `FLOOR_TRADING` / `FLOOR_T1` | 16:30 / 08:00 |
| `_NIGHT_CUT` | 9 ⇒ 00:00–08:59 归前一自然日 |
| 执行机 | `runs-on: [self-hosted, cn]`（**共享标签**，双机谁空谁跑）——与「禁据工作流名字判断执行机」铁律一致 |
| cron | `v8_algo_cloud.yml` 8 条（北京 16:40/18:10/20:00/21:00/22:30/00:30/06:00/08:00）**仅作唤醒备份**；实跑哪批由 `v8_stage_gate.py` 按内容级「缺什么跑什么」判定，全就绪则 `target=NONE` 空转 |
| `v8_algo_run.yml` | cron 已移除，仅 `workflow_dispatch` 应急（防双跑）✅ |

### 3.3 近 15 轮运行结论（Actions API）

`1838–1845` 全 **success**（北京 21:39 → 04:21）；`1832–1834`/`1837` failure、`1835–1836` cancelled 集中在**北京 16:58–20:16**（A/B 批时段）。

---

## 4 🔴 待跟进（按优先级）

### 4.1 [P0] `data/LHB_7D.js` 是**孤儿产物** —— 「漏算」确证

- 现象：`HEALTH_CHECK`（07:21 生成）报 `fail`：**更新于 2026-09-11 19:59（4 天前）**，超 24h 红线；`FRESHNESS_STATUS` 亦报 `frozen_stale`
- 根因（静态确证）：
  - `algorithms/gen_lhb_7d.py` **不在 A/B/D/E 任何批次**
  - 且 **30 个 workflow 中无一调用它**（`grep -rl gen_lhb_7d.py .github/workflows/` = 空）
  - 而 `data/LHB_7D.js` 在 `index.html` 中被前端引用
- ⇒ 该模块**没有调度者**，只能靠手工/过去某次遗留产物撑着
- 建议：挂入 A 批（与 `fetch_lhb.py` 同批，龙虎榜数据同源）或独立 workflow

### 4.2 [P1] `data/FACTOR_LAB_BACKTEST.js` 停更 3 天

- `HEALTH_CHECK` 报 `fail`：更新于 2026-09-12 22:14
- 生产者 `factor_lab_backtest.py` **∈ E 批** ✅（登记正确）⇒ 属「E 批未跑到该脚本 / 该脚本超时被跳过」类
- 与既有 P0「7 因子 walk-forward `0/7`」疑同源，建议合并排查

### 4.3 [P1] 批次标签**两源互相矛盾**（静态确证）

| 产物 | `v8_stage_gate.READY_SPEC` | `verify_chain_outputs.CRITICAL` |
|------|---------------------------|--------------------------------|
| `raw_data/gold_pool.json` | **B** 批（∈ B.items） | **A** 批（required=False） |
| `raw_data/candidate.json` | 未登记 | **A** 批（**required=True**） |

- `verify_chain_outputs.py` 按 `--upto "$TGT"` 决定「该产物是否本轮问责」⇒ A 轮次会把标为 A 的这两项纳入
- 而 `candidate.json` 的真实生产者（`algorithms/scanner.py` / `cloud_fetch_v8.py`）**不在任何批**（属 cn_fetch 独立管线）
- ⇒ 存在**A 轮次假陈旧**的风险（与文件内已注释记载的「D 产物致 B 轮假红」属**同型缺陷，D 已修、这两项未修**）
- ⚠️ **未证实**是否已造成实际假红（需 job 日志）。**矛盾本身是确凿事实**，建议按 D 的既有修法统一到单一真源

### 4.4 [P2] `READY_SPEC["A"].items` 含**批次外产物**

- `data/AVG_PRICE_DATA.js`（生产者仅 intraday）、`data/ETF_NET_SUBSCRIPTION.js`（生产者属 cn_fetch 管线）
- 当前 `need=11 < 13` 尚不致锁死，但 **`ETF_NET_SUBSCRIPTION.js` 已在 `A.must`** ⇒ 属**跨管线依赖**
- 历史 `5714cdef67ed` 即「must 项无本批生产者 ⇒ 整链锁死」，建议把 must 收敛为**本批脚本可产出**的项

### 4.5 [P2] `strategy_four_volume.py` 同属 **B 与 E 两批**（唯一重复项）

- 后果：E 批会重复执行一次日线选股（无害但耗时）
- 需确认是刻意（E 需要重跑）还是登记冗余

### 4.6 [P2] 「更新时间批次」口径缺口

`data/HEALTH_CHECK.js` 114 个模块中，**74 项（60 项「—」+ 14 项「?」）未声明 `freq`（批次/频率）** ⇒ 无法据此判「不错批」。建议补全，这也是主人「不错批也不能漏算」的可自动化抓手。

### 4.7 [P3] `ANALYST_RATINGS` 文件缺失

`FRESHNESS_STATUS`（00:42）报 `warn_stale`：「文件缺失」，category `post_close`。`core_stale=[]` ✅。

---

## 5 顺带修正（随本交接单一并推送）

`logic.html` 分批明细段原文「模块级强校验 `_STAGE_UNION == set(ORDER)`（共 **46** 条）」——
真源实测 `len(ORDER)=50`、`_STAGE_UNION` unique=**50**、重复项 `strategy_four_volume.py`。
**数字已过期**，本次改为 **50 条**。强校验结论本身（不丢脚本）成立，仅数字漂移。

---

## 6 边界（本轮没做、也不该由我做）

- **未改**任何数据源 / `data/*.js` / 算法脚本 —— 本轮只动 `index.html` + `logic.html` 两个前端文件
- **未动** `SH_FIB` 取数脚本层：数据层 `days_down=60 / peak_date / peak_close` 经实测**正确**，仅 `index / total_points / total_pct` 差一日；本轮在前端用真实序列**确定性修正**（不造假、不改数据源）。**fetch 脚本层的修法仍待小九定夺**
- 4.1–4.7 均为**审计发现**，未擅自实施修复（涉及算法链/调度，需与你的窗口合排）

---

*交接人：阿狸咪的工程师（家用机 alimi-cn）· 2026-09-15 07:55*
