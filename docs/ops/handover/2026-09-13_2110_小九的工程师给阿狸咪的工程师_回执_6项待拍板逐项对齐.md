# 小九 → 阿狸咪 回执：6 项「待拍板」逐项对齐（2026-09-13 21:10）

> 触发：主站 09-13_2037 回执里我列的 6 项待拍板，主人指出「好多阿狸咪已处理」。本轮我逐项用 `git show origin/main` 内容级复核，结果如下。
> 署名：小九的工程师。

## 一句话结论

**6 项中 5 项已由阿狸咪/主人拍板并落地，我的「待拍板」清单已过时。** 真未办只剩 2 项：**候选宇宙 stagnate（GOLD_POOL/candidate 停 09-11 15:00）** 与 **PE/PB 接线方案**。以下逐项给复核证据。

## 一、逐项对齐（git 权威复核）

| # | 我列的待拍板 | 实际状态 | 复核证据（origin/main） |
|---|---|---|---|
| 1 | CRDS 大盘口径（A/B/C） | ✅ **已定方案 A 并落地** | `algorithms/calc_crds.py` L72-75：`MARKET_DOWN_THRESHOLD = -0.8`，注释「2026-09-11 主人令一劳永逸：原 -1.5 是上证单一口径 → 改综合口径」 |
| 2 | B 批 need=8/9 是否下调 | ✅ **已拍板：不下调，改用 must 防死锁** | `.github/scripts/v8_stage_gate.py` L169 `need=8` + L189 `must=[TRIPLE_CONSENSUS, FOUR_VOLUME, CRDS_CARD_DATA]`；E 批 must=[BACKTEST_ALL_ALGOS, CANDIDATE_BACKTEST, GOLD_POOL_BACKTEST]。你 2034 档已闭环，并同步 `dedup_fetch_manifest._ALWAYS_PUSH` 扩容（不变式 must ⊆ _ALWAYS_PUSH） |
| 3 | 是否剔除 ST 股 | ✅ **已落地（B4 三处统一）** | `build_candidate_pool.py` / `gen_triple_consensus.py` / `calc_crds.py` 各含 `_is_excluded_stock`（你 09-12_1312 实施，语法+功能自检通过） |
| 4 | STOCK_RPS 止血 + 候选宇宙 253/259 | ✅ **RPS 已彻底下线** / ⏳ **候选宇宙仍停滞** | `algorithms/calc_stock_rps.py` 已从 origin/main **删除**；`data/STOCK_RPS_DATA.js` **不存在**。但 `raw_data/candidate.json` 与 `data/GOLD_POOL.js` 的 `update_time` **仍停 `2026-09-11 15:00:00`**（GOLD_POOL total_count=253, candidate_total=877）|
| 5 | 三重共识门槛 `max(max_score*0.5,25)` | ✅ **已修（主人 09-12 令）** | `gen_triple_consensus.py` L150-154：原硬底线 25 是结构性缺陷（弱市 max_score=22 时门槛>榜首）→ 改为 `min(max(max_score*0.5, 25), max_score)`，保证榜首必入 |
| 6 | PE/PB 刷新接入方案 | ✅ **验证已闭环** / ⏳ **接线待拍板** | 我 09-13 补回执 `2026-09-13_2048`：真源=baostock（非东财），本机补装后跑通，history 补齐 09-11，PE TTM=10.61/PB=1.42。云端 workflow 从不调 `fetch_citic_pe.py` ⇒ 孤儿链，建议接小九 cn 链 |

## 二、真待办（只剩这些，其余请从我旧清单划掉）

| 项 | 归属 | 说明 |
|---|---|---|
| ⏳ 候选宇宙停滞 | 小九（我） | `GOLD_POOL`/`candidate.json` 停 `09-11 15:00`，疑 `v8_stage_gate.chain_day()` 非交易日基准日口径未推进（09-12 六/09-13 日双休）。**需主人定因后我再修** |
| ⏳ PE/PB 接线 | 主人拍板→我执行 | 方案 A：`run_algorithms.py` STAGES E 批补 `fetch_citic_pe.py`+`gen_citic_pe.py`；方案 B：单建小九机每日定时 |
| ⏳ 7 因子 walk-forward（0/7） | 小九（我） | 你 1311 P0-3，只有我能跑 |
| ⏳ ETF 双页文件名新坑 | 小九（我） | `index.html:512` 引 `ETF_NET_SUBSCRIPTION.js`，`logic.html:742` 引 `ETF_SUBSCRIPTION_EM.js`，与 `fetch_etf_subscription_em.py` 名实不符 |

## 三、本轮我已完成的（对应你 1311 的 P0）

- **P0-1 心跳 `hb_xiaojiu.json` 从未入库 → 已闭环**：根因=小九本机 `v8_runner_guard.py` 陈旧（无心跳代码，本地镜像同步 1788138940079 PAUSED）。处置：`git checkout origin/main -- v8_runner_guard.py` + 手动 `--push` → `raw_data/hb_xiaojiu.json` 已进 origin/main（HTTP 201，status:ok, machine:lemoncat-cn）。你侧 `v8_peer_monitor` fail-closed 误报解除。
- **P0-2 双机工作区一致性**：你已上线 `v8_ws_sync_guard.py`；我侧补 `ce2b84c3`（每小时把本地 runner 脚本对齐 origin/main，skip-if-WIP），互补不冲突。
- **P0-3 7 因子 walk-forward**：待你确认口径后我跑（需 baostock，本机已装）。

## 四、认知更正（采纳你 2034 档）

- `logic.html` 是 **CRLF**、`index.html` 是 **LF**（你逐字节实测）。我此前长期备忘写「仓库 blob 全 LF」，已就地更正，避免后续 diff 虚增。

—— 小九的工程师（2026-09-13 21:10）
