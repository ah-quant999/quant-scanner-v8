# 小九 → 阿狸咪 下班交接单（2026-09-10 14:47 CST）

> 小九机按主人令「全部上线 + 交接清楚」后下线。本文档即最终交接依据，请阿狸咪逐条核对。

## 一、当前线上状态（全部已 deploy 到 main / GitHub Pages ✅）

| 项 | 状态 | 说明 |
|----|------|------|
| `?v` 单调令牌 | ✅ 已上线 | `update_v8.py` 模块级 `_UPDATE_TOKEN`，跨文件一致，根治回滚时浏览器陈旧缓存 |
| 最终推荐保护 | ✅ 已上线 | `update_v8.py` build() 跳过纯盘后产物（FINAL_RECOMMEND_DATA/crds/triple_consensus/candidate）在非 `--category post_close` 构建重写 |
| 看门狗修复 | ✅ 已上线 | `v8_cn_fetch_watchdog.yml` 删误杀 `cancel_stuck_cloud_runs()` + 阈值按窗口细化 + 探针含 CANDIDATE_QUOTES |
| STOCK_QUOTE 刷新 | ✅ 已上线 | 个股查询实时行情改读盘中 CANDIDATE_QUOTES 鲜源，逻辑见 logic.html `getLiveQuote()` |
| **lemoncat 盘中编排器** | ✅ **刚上线** | `sleep 600` → 实时轮询（最多12分/30s探测）+ 14档cron + 独立并发组 `v8-cn-fetch-slot` |

## 二、本次刚上线的改动（主人令「全部上线」）

**仅 1 个文件**：`.github/workflows/v8_cn_fetch_intraday_lemoncat.yml`（main SHA `15fb8b2d`）。
- 内容：self-hosted cn 盘中 14 档（9:40–15:00 CST，跳午休）统一编排；每档 dispatch 云端 → 实时轮询 → 超时/失败转 selfhosted 兜底；并发组从 `v8-cn-fetch-cloud` 移出为 `v8-cn-fetch-slot`（根治调度者与被调度者同组死锁）。

## 三、🔴🔴 致命坑：严禁整体合并 `fix/lemoncat-sleep600` 分支

该分支**已严重过期**（相对 main 有 151 文件差异），其中：
- `update_v8.py` 是 **-12 行** → 会把我刚上的 `?v` 令牌 + 最终推荐保护**整个删掉**！
- `index.html` +368、`data/*.js` 全是陈旧戳、`guard_v8_freshness.py`/`alimi_bell.py`/`watchdog.yml` 多为旧版。
- **若 `git merge fix/lemoncat-sleep600` 或整体 cherry-pick → 今天所有修复被洗回旧版（即"最终推荐又被回退"重现）。**

✅ 正确做法（已完成）：只把分支里那个安全的 lemoncat 单文件用 Contents API 原子提到 main，**其余陈旧内容一律未动**。
⚠️ 阿狸咪后续如需该分支上的其它独立改动（如 `v8_algo_cloud.yml` 的 cancel-in-progress、guard/alimi_bell 调整），必须**逐文件比对 main 当前版本后单独提取**，绝不可整分支合并。建议：该分支可直接删除或保留仅作考古。

## 四、⚠️ 双源处置（待阿狸咪确认，当前为安全冗余态）

lemoncat 上线后，盘中 intraday 有两路调度源并存：
1. **lemoncat**（self-hosted，小九机在线时由其 14 档 cron 触发）→ dispatch 云端
2. **WorkBuddy 自动化 195d8839 / 22505803**（云端按铃，每20分 dispatch 云端）

- **当前安全性**：两路都只 dispatch `v8_cn_fetch_cloud.yml`，由云端的 `v8-cn-fetch-cloud` 并发组**串行互斥** + 幂等守卫 → 不会损坏数据，仅浪费 runner（重复轮次）。
- **小九建议**：让 lemoncat 作为唯一盘中编排器（其设计即「纯依赖本 yml + 阿狸咪按铃兜底」），**PAUSE 云端按铃自动化 195d8839/22505803 之一**即可消除冗余。但若小九机离线、且阿狸咪按铃(60分阈值)兜底尚未验证可靠，**保留 WorkBuddy 云端按铃更稳**（它不依赖小九机）。
- **结论**：两路并存目前安全，阿狸咪按自身偏好择一收敛，不必紧急处理。

## 五、阿狸咪夜班 / 周末待办与监控重点

1. **盘后 E 档回测**（已建，待今夜首跑验证）：
   - `444581c6` v8盘后链E档派发（21:05·stage=E·repo_dispatch）→ 验证 21:05 后 data/backtest* 等 9 项产物刷新。
   - `ba2f027d` E档验收（23:50·只读）→ 列 data/ 全量筛 backtest 核对，不硬编码死名单。
2. **最终推荐保护验证**：今晚盘后构建（带 `--category post_close`）应正常刷新 FINAL_RECOMMEND_DATA；**盘中任何构建都不应改动它**（已保护）。若发现最终推荐被改，先查是否有人手动物理改了 raw → 不是 bug。
3. **lemoncat 小九机离线后**：其 14 档 cron 依赖 self-hosted runner，小九机下线即停。靠 WorkBuddy 云端按铃(若保留) + 阿狸咪按铃(60分阈值)兜底。请确认这两路至少一路 ACTIVE。
4. **阿狸咪自身铁律不变**：只读不写；HEALTH_CHECK.js 冲突保留本地；index.html 编辑期间小九不碰（已满足，小九下线）；stock-scanner 自动化保持 PAUSED（恢复 ACTIVE 须主人明令）。

## 六、已知残留风险（非紧急，已记录）

- `LHB_DATA` / 龙虎榜数据卡在 2026-08-04（无自动化跑 `fetch_lhb.py`）；FOUR_VOLUME 命中 0 只待验证真无信号/逻辑 bug。
- 周末 T+1 刷新不在缺口补齐器覆盖（仅靠 GHA 周末 cron），若该 cron 静默丢失则周末刷新缺。
- `fix/lemoncat-sleep600` 分支为过期分支，见第三节，切勿合并。

## 七、关键位置

- 推送脚本：`E:/v8data/qs8-tmp/_put_main_file.py`（Contents API 直推单文件，409 自动重取 sha 重推）
- token：`C:/Users/Administrator/AppData/Local/Temp/gh_tok.txt`
- 线上校验：`GET /repos/ah-quant999/quant-scanner-v8/contents/<path>?ref=main`
- 主站：`https://ah-quant999.github.io/quant-scanner-v8/` + `data/HEALTH_CHECK.js` 新鲜度

---
*交接人：小九（2026-09-10 14:47 CST）｜状态：可下线，所有修复已上线，致命坑已标红。*
