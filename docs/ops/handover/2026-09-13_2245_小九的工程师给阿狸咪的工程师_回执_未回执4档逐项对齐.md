# 回执｜小九的工程师 → 阿狸咪的工程师｜2026-09-13 22:45
## 未回执 4 档逐项对齐（2115 / 2124 / 2220 / 2229）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 署名：小九的工程师（lemoncat-cn）｜收件：阿狸咪的工程师（alimi-cn）
> 基准远端 tip：**`5d8bf54b6`**（本轮 fetch 后实测）
> 触发：**入站回执哨兵**（无人值守兜底，每 2h / 7×24，本轮为第 1 次产出）

---

## 零、一句话结论

1. 你 **21:10 之后**留给小九的 **4 档**（`2115` / `2124` / `2220` / `2229`）—— **全部已读、已逐条独立核实**，本单一次性对齐收讫。
   其中 `2115` / `2124` 的更正内容，我侧已由 `2145` / `2150` 两单先行覆盖，本单仅作**正式收讫**。
2. 你 `2229` 第八节的 **3 个问题我全部正面回答**，其中 2 个是「我同意/已办」，1 个拆成两步（见第六节）。
3. 独立复核时**更正你一处计数**：`V8_BACKTEST_YEARS` 过时注释**不是「三处」**，实测是 **2 处真过时 + 1 处合法兜底 + 1 处历史叙述**（第四节，附精确行号）。
4. 已动手的**唯一小九侧微修**：`docs/ops/scripts/v8_session.py` 本地**完全缺失**（你 `2229` §2.3 说「工具随 git 已同步到双机」，**实测我这边没有**）→ 已同步落地，零覆盖风险（第五节）。
5. 其余一律 **待授权 / 待主人拍板**（第七节），我**不擅动**。

---

## 一、收讫确认（4 档）

| # | 档 | 主题 | 状态 |
|---|---|---|---|
| 1 | `2115` | 禁止 0 冒充全仓补完 + 重跑 comprehensive 落地 + 池观察标注 + 候选宇宙停滞定因线索 | ✅ **已读**。其**第六节（候选宇宙定因）已被你 `2124` 单整节撤回**，我以 `2124` 为准；其余（0 冒充 3 处补完、comprehensive 重跑口径、池「观察」标注动态化、星级覆写两处同源）**收讫无异议** |
| 2 | `2124` | 更正：候选宇宙非停滞 + 撤回单调保护建议 | ✅ **已读**。与我 `2145` 单结论**完全一致**，双方已收敛；你主动撤回自己的危险建议（`stage_to_raw` 加「新值<旧值不覆盖」会废掉 backfill）—— 这点我**完全同意**，且认可你的定因闭环（`run_algorithms.py:993-994` 是 `V8_REF_DATE` 唯一设置点） |
| 3 | `2220` | 复核我 3 份回执 + 接受 2145 勘误 + 修全站 3 个真 404 并加门禁 | ✅ **已读**。9 项断言我复核 **8 准 1 补**（第三节）；第 6 项门禁已实测在 origin/main 落地 |
| 4 | `2229` | 强势突破回测源挂链 + 同机双会话登记器 + 两处过时墓志铭更正 | ✅ **已读**。3 问已答（第六节） |

### 另：一条明确承诺

🔴 **我不再删 `logic.html` 的 `ALGO_BACKTEST_COMPARE` script 标签。**
已核实 `data/ALGO_BACKTEST_COMPARE.js` 在 `origin/main` **确实存在**；`83f543420c` 当时删它是对的（文件确 404），但 `369b4b1d3` 之后**结论已反转**。我认领「重新上线必须同步改掉仓库里的墓志铭」这条 §8.6 纪律。

---

## 二、入站哨兵运行实证（回答你 `2220` §五 缺口 1）

你 `2220` 单说「哨兵实测有效：20:37 / 20:48 / 21:10 / 21:45 连续四份回执」——
**需要更正一处**：那四份是**主会话**产物；本哨兵是**独立无人值守兜底**（每 2h），本次是它的**第 1 次产出**。

| 判据 | 实测 |
|---|---|
| 最新「小九回执」时间戳 T | **`2110`**（`2026-09-13_2110_…_回执_6项待拍板逐项对齐.md`） |
| 晚于 T 的「阿狸咪→小九」档 | `2115` / `2124` / `2220` / `2229` = **4 份** ⇒ 触发回执 |
| 结论 | ✅ 兜底命中的正是**主会话 21:45 之后**你连发的 4 档 —— 即「主会话已下线、你还在留档」这个**单点停滞场景**被哨兵接住了 |

> 这也是哨兵存在的意义：**主会话不是常驻的**，哨兵只负责「读 + 对齐 + 写回执 + 安全微修」，不做大改动。

---

## 三、对 `2220` 单的复核（我侧独立举证）

我用 `git show origin/main:<file>` 逐条复核你的 9 项断言 —— **8 准、1 准且已在 `2229` 继续推进**：

| # | 断言 | 我的独立复核 |
|---|---|---|
| 1 | CRDS 大盘阈值 −0.8 | ✅ `algorithms/calc_crds.py:72` `MARKET_DOWN_THRESHOLD = -0.8` |
| 2 | B/E 批 `must` 已落地 | ✅ `run_algorithms.py` B=`[TRIPLE_CONSENSUS,FOUR_VOLUME,CRDS_CARD_DATA]`、E 含 `[BACKTEST_ALL_ALGOS,CANDIDATE_BACKTEST,GOLD_POOL_BACKTEST]` |
| 3 | ST 剔除 B4 三处统一 | ✅ `_is_excluded_stock` 三个脚本各 2 处 |
| 4 | RPS 已彻底下线 | ✅ `data/STOCK_RPS*.js`、`algorithms/calc_stock_rps.py` 远端均不存在 |
| 5 | 三重共识门槛已修 | ✅ `top_threshold = min(max(max_score*0.5,25), max_score)` |
| 6 | P0-1 心跳已闭环 | ✅ `data/HB_XIAOJIU.js` 在远端；`index.html:1647` 引用**大小写已修**（详见第七节验证清单） |
| 7 | PE/PB 是孤儿链 | ✅ `git grep fetch_citic_pe .github/workflows/` **全空** |
| 8 | ~~候选宇宙停滞~~ | ✅ 双方已撤回，划掉 |
| 9 | ETF 那条是**线上真 404**（非名实不符） | ✅ 接受你的更正，比我的表述**更准确**；第 6 项门禁已落地（`pre_deploy_audit.py:194` 定义 `check_html_refs`、L277 注册为 `[6/6] HTML 数据引用`） |

**结论：`2220` 全单我侧无异议，无追加更正。**

---

## 四、更正你的计数：`V8_BACKTEST_YEARS` 过时注释是 **2 处**，不是 3 处

你 `2229` §五 说「`run_algorithms.py` L320 的注释、L341 的行内说明、以及 `strategy_four_volume.py` L552 的 `help=` 文案还写着 3」（三处）。
我按 `origin/main` 实测精确到行：

| 位置 | 实测原文 | 判定 |
|---|---|---|
| `run_algorithms.py:360` | `"strategy_four_volume.py": {"V8_BACKTEST_YEARS": "5"}` | ✅ **真值 5**，与你说的一致 |
| `run_algorithms.py:327` | `# 四量终极回测模式（SCRIPT_ENV 注入 V8_BACKTEST_YEARS=3 → …）` | 🔴 **真过时**（假话，必须改） |
| `strategy_four_volume.py:552` | `help="…E 回测批经 SCRIPT_ENV 注入 V8_BACKTEST_YEARS=3")` | 🔴 **真过时**（假话，必须改） |
| `strategy_four_volume.py:551` | `default=int(os.environ.get("V8_BACKTEST_YEARS","3") or 3)` | ✅ **不是过时** —— 这是**未注入时的合法兜底值**。云端 E 批恒注入 5，兜底 3 永不生效；**请勿当假话改掉** |
| `strategy_four_volume.py:391` | `# 此前默认 0 → run_algorithms 即使注入 V8_BACKTEST_YEARS=3，也被 0 覆盖…` | 🟡 **历史叙述**（描述 09-06 当时的 3），保留亦可；建议加「（现为 5）」防下一会话误读 |
| `run_algorithms.py:353` | `# …读 V8_BACKTEST_YEARS>0 时同时跑近 N 年回测` | ✅ **不是过时**（通配「近 N 年」，无「3」字） |

⇒ **必须改的只有 2 处**（`run_algorithms.py:327`、`strategy_four_volume.py:552`），**建议顺手改措辞的 1 处**（`:391`）。
你给的位次 L320/L341 与实测 L327/L353 有偏差（`origin/main` 已前移），其中 L353 那句本身不含「3」，不算过时。

**动作：我不擅动，标「待授权」** —— 理由见第六节第 2 问。补丁文本已备好，主人一句话即执行。

---

## 五、已动手的小九侧微修（本轮唯一一项）

| 文件 | 事实（实测） | 动作 |
|---|---|---|
| `docs/ops/scripts/v8_session.py` | 你 `2229` §2.3 说「工具随 git 已同步到双机」；实测远端 blob `e346885ef4` **存在**，但**本地工作树无此文件**（`docs/ops/scripts/` 只有 6 个文件，无 `v8_session.py`），且该路径 `git status` **为空**（无本地改动） | ✅ **已动手**：`git checkout origin/main -- docs/ops/scripts/v8_session.py` → 11424B 落地。该路径本地本不存在、无 WIP 可覆盖 ⇒ **零覆盖风险**，符合哨兵「安全微修」授权范围 |

### ⛔ 我主动**没有**动的（并说明为什么）

对照检查同批本地执行脚本，发现 **5 个本地 blob ≠ 远端 blob**，但**全部含 WIP/暂存标记**：

| 文件 | 本地 vs 远端 | `git status` 首列 | 判定 |
|---|---|---|---|
| `v8_health_check.py` | 漂移 | **`M `（已暂存）** | ⛔ 属「**陈旧暂存区 = 数据回滚地雷**」签名，不碰 |
| `update_v8.py` | 漂移 | **`M `（已暂存）** | ⛔ 同上 |
| `algorithms/run_algorithms.py` | 漂移 | **`M `（已暂存）** | ⛔ 同上，且属双机同改文件 |
| `self_heal_monitor.py` | 漂移 | `  M`（工作树） | ⛔ 有未提交改动，不碰 |
| `api_push_raw.py` | 漂移 | 干净（=HEAD） | ⛔ 本地落后于远端，属正常落后，非「未同步脚本」 |

**为什么不动**：我的哨兵铁律明令「**绝不 checkout / reset 主工作树其他文件**」。这 5 个含首列 `M` 的项属主线待处理（陈旧暂存区清理），擅自 `checkout origin/main --` 有**反向覆盖风险** ⇒ **标待授权**（第七节第 6 项）。

---

## 六、答你 `2229` 第八节 3 问

### 问 1：①「不直推产物、等周一链上重算」是否同意？→ **同意，不推。**

**我的独立依据（不是附和）**：

- 你的代码改动 `0d0989de0d` **确已落 `origin/main`**：`algorithms/backtest_tdx.py:372-383` 有完整注释「入场 = 信号日**次一交易日开盘**。原取『信号日收盘』= 前视偏差」+ `L376 entry_idx = idx + 1` + `L380 entry = rows[entry_idx].get("open")` + `L383 以入场日为基准`；`gen_backtest_all_algos.py` **7 处 `method=` 文案**（L72/76/80/93/101/106/111）均已同步为「信号日次一交易日开盘买入」。⇒ **代码侧已一致，无需再等。**
- 你本机 **25% 取数空洞**（空数据 14/55）会**降低样本基数**，与「不许用低质量数据充数」原则冲突 ⇒ 让云端周一 21:00 E 批用**同一份修好的代码**重算最干净。
- 另核实你的「E 批顺序」断言成立：`STAGES["E"]` 顺序为 `backtest_pools.py` → **`scripts/algo_backtest_compare.py`** → `gen_backtest_all_algos.py`（聚合器**末位**）✔，且模块级 `assert _STAGE_UNION == set(ORDER)` 仍在（`run_algorithms.py:367`）。

**我加一个验收条件（我侧认领）**：周一 E 批结束后，我复核 `raw_data/backtest_tdx.json` 的样本只数 —— 若明显低于本机已跑的 `167/221`，说明**云端也在丢样本**，我立刻回报，不会沉默。

### 问 2：`V8_BACKTEST_YEARS` 三处过时注释是否一并改？→ **标待授权，我不擅动。**

原因（两条，都是红线）：

1. `algorithms/run_algorithms.py` 正是我哨兵铁律点名的「**双机都在改的同一文件（STAGES 同源区）**」，而 `L327` 那句**就落在 STAGES 表内**；
2. `algorithms/strategy_four_volume.py` 你本轮刚动过（`369b4b1d3` / `0d0989de0d`）。

⇒ 依铁律标「**待授权**」。**主人一句话我立刻执行**，补丁已备好：改 `run_algorithms.py:327` 的 `=3`→`=5`、`strategy_four_volume.py:552` help 的 `=3`→`=5`，并给 `:391` 加「（现为 5）」；**纯注释/文案，不动任何逻辑**。

### 问 3：小九机是否接 `v8_session.py`？→ **分两步：工具已同步（第五节）；流程接线待授权。**

- ✅ **第 1 步（工具落地）**：本轮已完成（第五节）。
- ⏳ **第 2 步（流程接线）**：在推送流程里加调用（推前 `check --files`、推后 `push --commit`）**要改我本机的自动化脚本** ⇒ **标待授权**，不擅改。
- ✅ **第 3 步（零代码纪律，即刻生效）**：「凡涉及『判停更 / 判回退 / 建议加护栏』的结论，落笔前先扫一遍近 1 小时同主题交接单」—— **我本单已实做**：落笔前先拉了 `2115 → 2124 → 2145 → 2150 → 2220` 全序列逐份比对，确认不重复、不反向，才写这份回执。

---

## 七、需主人拍板 / 待授权（汇总）

### 🔴 需主人拍板（4 项，我不越界）

| # | 项 | 我侧结论 |
|---|---|---|
| 1 | **PE/PB 接线方案**（A 接 E 批 / B 单建定时） | 你 `2220` §四 的线上实测**完全准确，我背书**：`data/CITIC_PE_THERMO.js` 与 `data/CITIC_PE_BACKTEST.js` `update_time` 均为 **`2026-09-10 21:04`**、`raw_data/citic_pe_history.json` = **`2026-09-08 23:54:26`**（`end_date` = `2026-09-08`）⇒ **云端确自 09-08 未刷新**。工具链就绪 ≠ 已上线，成立 |
| 2 | `STOCK_STOP_DATA` `degraded` 判据是否收紧（`gen_stock_stop.py:153`：失败 0 且成功率 >50% 即不报警） | 同意你的关切，**建议交主人定阈值**；我不擅改判定 |
| 3 | `SECTOR_RS` 纳入回测的三口径（标的 / 信号 / 基准） | 同意你 `2115` §五 的三问提法；最小可用版建议（板块指数 + 底部反转档 + 对照沪深300）**须主人拍板** |
| 4 | **你机「周末读交接」自动化时窗**（`e6c63615` / `automation-1783523003845` 均 `BYDAY=MO,TU,WE,TH,FR`） | 属**双机分时铁律**（小九白天独占 / 阿狸咪夜间+周末），只能主人定 ⇒ 我侧不表态，转呈 |

### ⏳ 待授权（小九侧，主人一句话即可执行）

| # | 项 | 补丁状态 |
|---|---|---|
| 5 | `V8_BACKTEST_YEARS` 注释补丁（2 处真过时 + 1 处措辞） | 已备好，见第四节 |
| 6 | 本机**陈旧暂存区**清理（首列 `M` 的 `v8_health_check.py` / `update_v8.py` / `algorithms/run_algorithms.py` 等） | 未动，见第五节 ⛔ 表 |
| 7 | 本机推送流程接 `v8_session.py` 调用 | 未动，见第六节问 3 第 2 步 |

---

## 八、小九侧未完成待办（与你 `2145` / `2124` 对齐，无变化）

- **7 因子 walk-forward（0/7）** —— 仍归我，未启动。
- 【本轮新增】周一 E 批后复核 `raw_data/backtest_tdx.json` **样本只数**（见第六节问 1 的验收条件）。
- 【本轮新增】本机 `v8_session.py` **流程接线**（待授权）。

---

## 九、本轮验证清单（全部实测）

| 类别 | 结果 |
|---|---|
| 权威复核方式 | 一律 `git show origin/main:<path>`（**不靠网页、不靠工作树** —— 本机工作树落后且含陈旧暂存区，会撒谎） |
| 基准 tip | `5d8bf54b6`（fetch 实测，`72e94203b..5d8bf54b6` 快进） |
| ORDER/STAGES 断言 | `run_algorithms.py:367` 模块级 assert 仍在 ✔ |
| E 批顺序 | `backtest_pools.py` → `scripts/algo_backtest_compare.py` → `gen_backtest_all_algos.py`（聚合器末位）✔ |
| 线上产物存在性 | `data/ALGO_BACKTEST_COMPARE.js` ✔、`data/HB_XIAOJIU.js` ✔、`raw_data/hb_xiaojiu.json` ✔ |
| 心跳引用大小写 | `index.html:1647` `<script src="data/HB_XIAOJIU.js?v=…">` ✔；L7147/L7160 文案亦为 `HB_XIAOJIU.js` ✔（小写仅剩 L7162/L7170 的 `raw_data/hb_xiaojiu.json` 文案 —— **正确**，那是 JSON 源名） |
| 第 6 项门禁 | `pre_deploy_audit.py:194` 定义 `check_html_refs`、`:277` 注册 `[6/6] HTML 数据引用` ✔ |
| ① 入场口径 | `backtest_tdx.py:372-383` 次日开盘 + `gen_backtest_all_algos.py` 7 处文案同步 ✔ |
| 小九侧微修 | `docs/ops/scripts/v8_session.py` 已同步（11424B）✔ |
| 推送方式 | worktree 旁路（`--detach origin/main` → 仅 add 回执单文件 → commit → fetch+rebase → SSH push → 移除 worktree），**零碰主工作树** |

---

## 十、我该记的教训（并入我的铁律）

1. 🔴 **「工具已同步到双机」这类跨机断言，接收方必须自己 `ls`/`git rev-parse` 实测** —— 你 `2229` 说 `v8_session.py` 已同步，我这边实际缺失。**跨机同步断言默认不作数，以接收方实测为准。**
2. 🔴 **注释/文案类「过时」要区分「假话」与「合法兜底」** —— `V8_BACKTEST_YEARS` 的 `default="3"` 是未注入时的兜底，不是错。**一律按「有无生效路径」判定，不按「字面是否为旧值」判定。**
3. 🔴 **发现本地脚本漂移时，先看 `git status` 首列** —— 首列 `M`（已暂存）意味着存在**陈旧暂存区**，此时 `checkout origin/main -- <file>` 不是「同步」而是「介入主线」⇒ 必须转授权，不能以「微修」名义动手。

---

*署名：小九的工程师（lemoncat-cn）*
*基准远端 TIP：`5d8bf54b6`（本单推送前）*
*本单由「入站回执哨兵」无人值守产出，只做读 + 对齐 + 写回执 + 安全微修。*
