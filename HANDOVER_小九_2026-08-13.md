# 九宝量化 v8 交接文档 · 小九 → 阿狸咪

> 更新于 2026-08-13 12:00 自动更新（小九当班 12:00 档）

## 一、背景

- 小九当班时段（约 08:00–12:00）集中解决了**昨日遗留的盘中数据缺口 + 多项数据/UI 根因 bug**，并对**云端任务架构做了一次大重构**（合并自愈器、盘中主通道改中国 IP 直抓）。
- 关键背景事件：今晨 **09:40 出现盘中数据全缺口**，根因是 `v8_cn_fetch_cloud.yml` 的 `runs-on: ubuntu-latest`（美国 IP）抓不到 A 股中国源（`cloud_fetch_v8.py` 头部已自承此限制）。当天已一劳永逸根治。
- 阿狸咪值班窗口（18:00–次日 07:30 + 周末）接手后，重点见第四节「待办/风险」。

## 二、今日已完成改动（含真实 commit SHA + 影响）

> 以下 SHA 均取自 `git log origin/main` 实况核实，main 当前 HEAD = `68618cfd`。

| # | 改动主题 | 真实 SHA | 影响 |
|---|---------|---------|------|
| 1 | **盘中实时主通道切本机(cn)直抓** + 重写兜底自动化 `1785898357370` 为本地 `cloud_fetch_v8.py --category intraday` 推送 raw_data | `7d158f4d`(10:33) → build `e26663eb` | 根治 09:40 缺口，端到端 ≈5 分钟，盘中 7 张实时卡全为实时值 |
| 2 | **云端任务架构重构**：`v8_cn_fetch_cloud.yml`/`v8_algo_cloud.yml` 的 `runs-on` 由 `ubuntu-latest`(美国IP) 改 `group:self-hosted / labels:[cn]`（中国 IP 必能抓全量；cn 离线自动降级 ubuntu + `api_push_raw.py` 防倒退守卫） | `1fe4f8c7` / `31d4dc9b` | 一劳永逸根治盘中缺口；cn 出差关机自动降级不覆盖好数据 |
| 3 | **合并 5 个自愈器为 1 个** `v8_health_patrol.yml`（删除 `v8_intraday_healer`/`v8_safety_net`/`v8_self_heal`/`v8_ops_self_heal`/`v8_algo`每日体检）；触发=`workflow_run`(cn_fetch/algo/build 完成)+schedule 每60分(09:08–21:08)；内部跑 `v8_health_check.py --heal`(一站式检测+派发)+`guard_v8_freshness.py` | `1fe4f8c7` / `31d4dc9b` | 消除每30/15/6分轮询碎片化，功能由 `--heal` 覆盖 |
| 4 | **港股/A股公平性改造**：`fundamental_helper.py` 移除港股 -10 强制降权（缺失统一按中性0分）；`index.html` `renderFinalCandInner` 取消 A股/港股分组、统一按 `final_score` 排序、移除「⚠️已降权」标签 | `c797e5ff` | 盘后 `generate_top10`/`gen_cockpit_*` 自动继承，港股排名上升属预期 |
| 5 | **IPO tracking 显示 bug 修复 + 情绪分因子升级**：`update_v8.py` 不再用 score=0 覆盖 tracking 股；`fetch_ipo_data_v8.py` `heat_score` 改动态 0-15（行业涨幅/涨停数/超募倍数输入） | `c1ef3801` | 杰理科技/超纯应材不再误显「不建议申购」；applying 股出现动态热度分 |
| 6 | **Top3 90 天滚动追踪上线**：新建 `algorithms/gen_top3_track.py`（读 finalRec Top3 + STOCK_QUOTE + STOCK_STOP → 90天滚动池，含胜率/均收/最佳最差）+ 接入 `run_algorithms.py`/`update_v8.py`，产出 `data/TOP3_TRACK.js` | 脚本已落地、尚未 commit（UI patch 待 apply） | 看胜率用；UI 紧凑化 patch 见 docs 交接 |
| 7 | **个股搜索补全港股/ETF**：`fetch_stock_names.py` 新增 `_fetch_hk_stocks`/`_fetch_etf_stocks` + pypinyin 首字母；`fetch_stock_quote_v8.py` 新增 `fetch_hk_spot`/`fetch_etf_spot` | `dfda0d94` | 名称库 5207→9582、行情库 5542→9917，`hdl`/`海底捞`/`06862`/`510880` 等全命中 |
| 8 | **分红方案陈旧根因修复**：新增 `algorithms/refresh_dividend_cninfo.py`（巨潮资讯法定源，补全最新预案；宝丰 600989 由 2024 修正为 **2026-04-22/10派4.2**）+ 两个定时任务（盘前 08:15 / 晚间 19:00，均调 `run_dividend_refresh.py`） | `089ca4cf` / `aad7590d` | 分红从「常停上一年度」变为实时最新；自愈含 rebase 冲突取 ours 重建 |
| 9 | **全站精选「A股数据缺失」告警根治**：`scanner.py` `CLOUD_RUNNER` 同时识别 `GITHUB_ACTIONS=true`；`v8_algo_cloud.yml` 显式注入 `CLOUD_RUNNER:true`；即时重建 `TOP10_DAILY.js`（A股占比90%） | `cf3d60b9` | 云端算法链此前因走 mootdx/美区IP 只扫到港股，已根治 |
| 10 | **TOP3 持仓跟踪首字母搜索无响应修复**：补全 `showPfDropdown`/`hidePfDropdown`/`_pfSelectDropdown`（原 `oninput` 调用了不存在的函数，输入直接抛错） | `a3774b84` | 输入 `bfn` 现可命中宝丰能源 |
| 11 | **AI 速览「操作建议」落地具体板块/个股**：`gen_market_brief.py` `build_strategy()` 替换抽象词为真实前排概念/个股，新增 `mainline_sectors`/`mainline_picks`；前端新增 pill 行 | `12682d0c` | 示例："加仓通信技术/CPO/算力，关注天孚通信/新易盛" |
| 12 | **UI 紧凑化/去重**：① 盘前 badge「盘前 盘前」双显修复 `84ca09f6` ② IPO 上市日期另起一行 `b9e62a01` ③ 时段 badge 双显 + 图表日期错乱/密度 `599d64f8` | `84ca09f6`/`b9e62a01`/`599d64f8` | 三个独立 UI bug 修复并入库 |
| 13 | **root URGENT/backup 噪音清理（闭环非只删）**：`.gitignore` 加 `/URGENT_*.md`；`git rm` 64 tracked root URGENT + 4 tracked backup，6+3 未跟踪送回收站 | `40d8a667` + 交接 `230ec84c` | 根因=看门狗每小时重建回推；三方对齐(云端不回推/小九干净/阿狸咪可见) |
| 14 | **自动化精简**：暂停 `1785718343599`(每2h管线看门狗，被每小时紧急通道超集覆盖) + `1785980310111`(盘中云端定时触发，被盘中实时兜底覆盖)；新增分红盘前/晚间 `1786588096327`/`1786588094908` | — | 去重叠、降 GitHub API 压力与 429 风险 |

**近期 main HEAD 链（核实自 `git log origin/main`）：**
`68618cfd`(11:51 healthcheck) ← `5fb90c63`(11:50 build) ← `d6ea3db4`(11:50 cn fetch intraday) ← `31d4dc9b`(合并自愈器) ← `1fe4f8c7`(补提) ← `15f6ef33`/`1c1ef435`/`230ec84c`/`40d8a667`…（完整 15 条见仓库 log）。

## 三、当前系统状态

- **站点（GitHub Pages）**：线上数据已刷新到 **11:48–11:50 盘中实时值**（本轮 cn fetch 11:48→云端 build 11:50→自动唤醒 `v8_health_patrol` 写 `68618cfd` 11:51 healthcheck 绿）。
- **Runner**：
  - 本机 cn runner（小九，中国 IP）：盘中直抓主通道，当前 online。
  - cn 自托管 runner `324135267`：仍 100% failure（已非主通道，待修复/弃用）。
  - 云端 `v8_cn_fetch_cloud.yml`/`v8_algo_cloud.yml`：现 `runs-on:[self-hosted,cn]`，cn 离线降级 ubuntu。
- **main SHA**：`68618cfd`（2026-08-13 11:51）。
- **剩余自动化（活跃）**：约 14 个 ACTIVE。保留关键：`小九-v8每小时紧急交接通道(看门狗+自动修复)`、`小九-v8盘中实时数据刷新兜底(每30分·已改本机直抓)`、两个分红刷新、盘前全量、盘后守护等。另 3 个本地轮询自动化（`1784797186247`/`:7221`/`:7279359` 等）已被云端 patrol 覆盖、冗余但无害（有去抖锁），**未暂停**（主人仅问未令）。
- **工作树**：本任务仅 add 交接文档；`git status` 另有 untracked（`_intraday_check_result.json`、`docs/AUDIT_INTRADAY_CLOUD_2026-08-13.md`、`.patch.new` 等）均为参考/中间态，**未触碰、勿动**。

## 四、需阿狸咪注意 / 待办 / 风险

1. 🔴 **出差 7×24 根治（待主人拍板）**：cn runner 现仅装小九电脑，fallback 仅降级 ubuntu(美国IP部分残缺)。需加第二台 `cn` label runner（腾讯云轻量 1c1g 中国IP 装 GitHub runner 服务，几十元/月）。小九若关机/出差，盘中数据可能残缺——届时看 `v8_health_patrol` 是否告警。
2. ⚠️ **3 个本地轮询自动化冗余**：`1784797186247`(:00健康)/`1784797187221`(:30健康)/`1784797279359`(心跳) 等已被云端 patrol 覆盖，待主人确认后暂停。
3. ⚠️ **自动化 `1785208215213` 指向已弃用 v6 `repo-temp` 路径**：v6 已于 2026-08-07 彻底删除，该任务路径失效，建议核对去冗余。
4. ⚠️ **远端 11:12 那轮 cn fetch 的 `runner_status.category=premarket`**：盘中时段跑 premarket 类别，疑似另一通道调度口径不对，**非本任务职责，留待排查**。
5. 📌 **Top3 追踪 UI patch 待 apply**：`algorithms/gen_top3_track.py` 等脚本已落地但**尚未 git commit**；UI 紧凑化 patch 见 `docs/ops/handover/HANDOVER_小九_2026-08-13_Top3_90天追踪与UI合并.md`，待阿狸咪/主人 apply。
6. 📌 **本地 WIP 不丢**：工作区未跟踪文件（URGENT 残留备份等）为正常中间态，勿 `git add -A` 误推。
7. ✅ **已闭环、只需观察**：root URGENT/backup 噪音清理三方对齐；公平性改造/IPO/搜索/分红/Top10/A股缺失均已根治，看门狗 177 轮后全绿。

## 五、小九监督职责（本班小结）

- 小九当班（08:00–12:00）已完成：盘中缺口根治 + 云端架构重构 + 7 项数据/UI 根因修复 + 自动化精简。
- 监督重点已交接：盘中数据链**已闭环**（本机 cn 直抓 + git push → 云端 build 部署 → patrol 自动自愈），无需小九值守时人工干预。
- 红线提醒（双机铁律）：**阿狸咪 18:00 后/周末部署 v8，绝不跑 `deploy_v8.py`；改动须即时 commit+push（cn runner 每周期 `git fetch+reset --hard` 会静默回退未入库改动）**；红涨绿跌全站执行；不发布 `data/`、读 `window.X` 注入。
- 小九下一班：明日 08:00 起接班；若本班遗留项（出差 runner/冗余自动化）主人已拍板，将第一时间执行。

---
*本档由自动化 `automation-1785719698947`（小九 12:00/18:00 交接更新）生成，仅供阿狸咪开机后快速定位当日状态。改动 SHA 以仓库 `git log origin/main` 为准。*
