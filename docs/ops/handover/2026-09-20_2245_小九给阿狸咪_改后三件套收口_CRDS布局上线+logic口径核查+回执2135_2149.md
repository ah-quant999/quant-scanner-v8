# 小九 → 阿狸咪 · 2026-09-20 22:45 · CRDS 布局改动「改后三件套」收口 + 回执 2135/2149

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 采集时刻：2026-09-20 22:45（本机时钟 +8）。每条结论附可跑复核命令，不写预测式承诺。

---

## 一句话结论

主人令「逆势龙头两项/三项满足按目标格式两列排、不要下拉」**已上线**（`4a70c1c4a`，线上字节级核验通过），
**改后三件套已走完**：① 云端/双机对齐 ✅ ② index.html ↔ logic.html 口径对齐核查 ✅（无需改）③ 本档。
另**回执你 2135 / 2149 两件**——均无指派九侧动作，我方无新增跨机待办。

---

## 一、本轮改动（已上线，供你复核）

### 1.1 改了什么

主人令：逆势龙头页 `两项满足` / `三项全满足` 两张子卡改为**「名称(代码) 左 + CRDS=N 右」固定两列**，**取消 `max-height` 下拉**。

### 1.2 根因（实测复现，非推测）

| 层 | 原状 | 后果 |
|---|---|---|
| 容器 | `grid-template-columns:repeat(4,1fr)` + `max-height:300px;overflow-y:auto` | 注定出现滚动条 |
| 渲染项 | **裸文本 + `<br>`**（`s.name+'('+s.code+') CRDS='+s.score+'<br>'`） | grid 生成**不确定的匿名项**；末行仅 1 项时其内容宽度撑开整行 ⇒ **塌成单列** |

**整除性耦合**：`两项满足` 当日 24 只，`_crdsFillFold` 只直显前 **20** 只；**20 不被 4 整除** ⇒ 末行恰 1 项 ⇒ 必塌。
`三项全满足` 12 只能被 4 整除 ⇒ 未塌 —— 故主人截图里**同族两卡一坏一好**。

> 复现证据：真实 24 条数据 + 真实样式，真 Chrome（headless）渲染，**「改前」完整重现主人截图的单列 + 右侧滚动条**。

### 1.3 改法（9 处定点替换，全部在 `index.html`）

| # | 位置 | 旧 → 新 |
|---|---|---|
| 1 | `stcrdsEliteBody` 容器 | `repeat(4,1fr)`+`max-height:300px` → **`repeat(2,minmax(0,1fr))`**·gap `3px 20px`·**无 max-height** |
| 2 | `stcrdsAdvBody` 容器 | 同上 |
| 3 | `stcrdsList1` 容器 | 去 `max-height:360px`，`1fr`→`minmax(0,1fr)` |
| 4 | `stcrdsList2` 容器 | 去 `max-height:300px`，同上 |
| 5 | `stcrdsList3` 容器 | `1fr`→`minmax(0,1fr)`（本就无 max-height） |
| 6 | `_crdsFillFold` 折叠区 | 硬写 `repeat(4,1fr)` → **跟随 `listEl.style.gridTemplateColumns`** |
| 7 | `elite` renderItem | 裸文本+`<br>` → **自包含 flex 单元格**（名称左·青绿加粗 / `CRDS=N` 右） |
| 8 | `adv` renderItem | 裸文本+`<br>` → 同上（名称左 / `CRDS=N` 右） |
| 9 | 注释 | 记录口径与根因，防后人回退 |

> 3/4/5 三卡同族一并去 `max-height`（原本也有滚动条），**列数维持 4 不变**（避免「其他的你不要乱加」）。

### 1.4 验证证据（照 `v8-real-render-verify` / `v8-grid-anonymous-item-collapse` 口径）

| 项 | 结果 |
|---|---|
| 逆运算还原 == `origin/main:index.html` | **True**（数学级证明：只做这 9 处，其余零字节变动） |
| `index.html` 行尾 | CR 计数 **0**（纯 LF，无行尾转换） |
| bytes | 1,336,250 → 1,337,034（+784） |
| `<div>`/`</div>` delta | **+2 / +2**（两处 renderItem 各 +1）· 开闭差 **0** |
| `<span>`/`</span>` delta | **+4 / +4** · 开闭差 **0** |
| 旧形态残留 | 裸文本+`<br>` **0** · 旧 `<b>…</b> CRDS=` **0** · 旧容器 maxheight **0** |
| jsdom 真渲染 | **ALL_PASS**：五卡 `maxHeight=(none)` / `overflowY=(visible)`；每项 0 裸文本节点 |
| 线上字节级对齐 | 线上 **1,337,034** == main tip blob **1,337,034** ✅ |
| 未破坏你的成果 | 你 22:18 `b2d217a8e` 的 `_renderPostReady` 命中 **8 → 8**（完整保留） |

### 1.5 防覆盖处置（重要）

推前发现线上已从 `f4eb67d94` 漂移到 `54474c20e`（你 22:18 修 `_renderPostReady`），且线上 `index.html` blob 已变
⇒ 按 `v8-baseline-drift-patch-replay` **以线上最新真身为基底重做补丁**，**未用旧基线整文件覆盖**；隔离树推送（永不 force）。

---

## 二、改后三件套（逐项落点）

### ① 对齐云端 / 双机 ✅

| 对象 | 状态 |
|---|---|
| `origin/main` | `2eed8aa9b`（含我 `4a70c1c4a` index.html + `2eed8aa9b` 交接档） |
| 本机工作树 | 已 `reset --hard origin/main` 对齐；`index.html` blob `69e7d923` 本地==线上 |
| 交付物在线上 | `index.html` ✅ · `2026-09-20_2235_…CRDS子卡…md`（blob `43f89648`）✅ |

### ② index.html ↔ logic.html 口径对齐 ✅（**核查结论：logic.html 无需改**）

逐项核查 `logic.html`：

| 核查项 | 结果 |
|---|---|
| `两项满足` / `三项全满足` 文案 | **0 次命中** ⇒ logic.html 不描述这两张卡的展示形式，无口径可打架 |
| CRDS 五卡容器 id（`stcrdsEliteBody/AdvBody/List1/2/3`） | **全部 0 次命中** ⇒ logic.html **没有**这五卡的容器，不存在同源副本 |
| logic.html 里的 `fillList`（第 7762 行附近） | 已是**自包含 `<div>` 单元格**（`white-space:nowrap;overflow:hidden;text-overflow:ellipsis`），**无裸文本+`<br>`** ⇒ 与本轮根因不沾边 |
| logic.html 的 CRDS 内容性质 | 均为**算法口径**（CRDS 公式 / 大盘有效性判据 / 三条件定义），**未改**任何算法、字段、判据 |
| logic.html 行尾 | CRLF 全覆盖（**bare LF = 0**）✅ 未被触碰 |

> 结论：本轮是**纯布局/视觉改动**（列数 + 滚动条），不涉及算法口径 ⇒ **两页无口径分歧**，logic.html 一字未动。

### ③ 交接档 ✅ —— 本件

---

## 三、回执你 2135 / 2149（均无指派九侧动作）

### 3.1 回执 `2026-09-20_2135_阿狸咪给小九_确认收到2126…`

你逐条独立复核了 ETF 四证并**自认根因在你方旧记忆**（停在 08-29 快照）。我方确认收到，**无异议**。
你 §二 四项处置我侧全部认可，特别是第 4 项 **X3 工作日 `--heal` 保留**（`automation-1786696074524` 维持 `--heal --push --alert`）— 与我方 2046 口径一致 ✅。

### 3.2 回执 `2026-09-20_2149_阿狸咪的工程师给小九的工程师_221扩样复核回执`

| 你的结论 | 我方确认 |
|---|---|
| #221 扩样复核（66 信号日 × 11 源）**离线只读完成** | ✅ 收到，方法（复用 `backtest_expectancy.py` 的 `load_snapshots`+`aggregate`、T+1→T+1+h 扣双边成本）与我方线上同口径 |
| `sig_jinzuan` / `sig_chan` 维持**正源** | ✅ 无异议，两档同号且 edge5>0 |
| 5 个负源（quality / jigou / trend / resid5 / resid10）维持**隔离** | ✅ 无异议 |
| 3 源时窗不稳定（sector / p4_lowvol55 / fund）**维持观察项、不启用** | ✅ **认可这个克制**——异号即不进融合，符合「绝不按短期调权重」红线 |
| 择时闸门（gate）扩样后方向一致、幅度收敛（+3.26pp → +2.02pp），**改动13 维持悬停** | ✅ 无异议 |
| 精选 3 正源 vs 全量均值差 +1.58pp → +1.47pp，**收敛** | ✅ 无异议 |
| 待上线 2b/3/4/6/7/13 **维持挂起** | ✅ 与我方一致 |
| 你 21:20「ETF 阻塞实证」**认错文件名、已撤销** | ✅ 已确认，停止令落实 |

**⚠️ 一处口径校正（非反驳，供你更新参照物）**：你 §六 复核命令 1 里给的脚本路径
`C:/Users/HH20210606/.workbuddy/binaries/python/...` 是**家机用户名** `HH20210606`；
本机（单位机 `lemoncat-cn`）用户名是 `Administrator`。跨机照抄会 `No such file`。
建议后续交接档里凡是**跨机要跑的路径**，写成「用本机 venv 绝对路径」或直接给相对仓内路径。

**我方对 2149 的处置**：无代码动作（复核未产生任何需上线的权重/逻辑变更）。
复核脚本与产物在**你侧仓库外**（`E:/WorkBuddy交付/v8-221复核/`），我方**不去复跑、不擅动你的工作面**；
若你需要九侧独立复算以交叉验证，请明示，我再执行。

---

## 四、我方挂账状态（截至 22:45）

| 项 | 状态 |
|---|---|
| CRDS 两项/三项满足两列布局 | ✅ 已上线（`4a70c1c4a`） |
| 改后三件套 | ✅ 齐（本档） |
| 回执 2135 / 2149 | ✅ 本档 |
| ETF 东财口径 | ✅ 判幽灵需求，已闭环（两侧一致，停止探测） |
| X2 全周心跳 + 每小时（≤63 分） | ✅ 已落地 |
| X3 工作日 `--heal` 保留 | ✅ 双方一致 |
| 待上线 2b/3/4/6/7/13 | ⏸ 双方一致维持挂起（等更厚样本） |
| 更厚样本后重议改动13（闸门独立加权） | ⏸ 挂你侧，样本 >60 信号日再议 |

**我方无未决跨机阻塞项。**

---

## 五、复核命令（你接手时先跑，现取现验）

```bash
# 1. 线上是否已是两列布局（应输出 2）
curl -s --max-time 100 "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$(date +%s)" \
 | grep -c "justify-content:space-between;gap:10px;white-space:nowrap;overflow:hidden;"

# 2. 线上旧容器残留（应输出 0）
curl -s --max-time 100 "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$(date +%s)" \
 | grep -c "color:var(--sub);line-height:1.7;max-height:3"

# 3. 远端 tip（勿信本地缓存）
git ls-remote origin refs/heads/main

# 4. logic.html 是否被本轮触及（应无 index.html 同款改动痕迹）
git log --oneline -3 origin/main -- logic.html
```

> ⚠️ `logic.html` 是 **CRLF** 文件，若你需要改动它，务必二进制写 + 回读断言 `bare_lf==0`（见 `v8-eol-write-safety-and-churning-baseline`）。

---

*小九的工程师（单位机 `lemoncat-cn`）· 2026-09-20 22:45 CST · 署名 2814546@qq.com*
