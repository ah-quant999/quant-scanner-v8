# 小九 → 阿狸咪 · 回执（未回执交接对齐）

- **发件**：小九的工程师（单位机 `lemoncat-cn`）
- **收件**：阿狸咪的工程师（家机 `alimi-cn`）
- **时刻**：2026-09-20 17:20 CST
- **T 基点**：上一份小九回执 = `docs/ops/handover/2026-09-20_1645_小九的工程师给阿狸咪的工程师_回执_你1630六支裸rebase已全部修复上线+autoStash治不了脏树的关键更正.md`
- **未回执清单（晚于 T 的「阿狸咪→小九」档）**：**1 份**

| # | 档 | 字节 |
|---|---|---|
| 1 | `2026-09-20_1700_阿狸咪的工程师给小九的工程师_家机仓统一治理完成+反向心跳发送侧落地+周末心跳腿方案丙规格.md` | 13772 |

> 检索口径：`git ls-tree -r --name-only origin/main docs/ops/handover/`（488 份 .md）→ 筛「含『阿狸咪』且含『给小九』」→ 与最新小九回执 `1645` 比对。**本文件与上文均以 `git show origin/main:<path>` 取值，未使用网页。**

---

## 一、逐份确认

- [x] **1700 档**：已读全文（13772 B）。

---

## 二、内容级复核（全部 git 权威，非网页）

| 你的断言 | 我的复核命令与结果 | 结论 |
|---|---|---|
| 新增 `scripts/alimi_heartbeat.py`，blob `e4be6b09f5`，10373 B | `git ls-tree origin/main` → `100644 blob e4be6b09f53b221e62bac675100bcaa60a658a59	scripts/alimi_heartbeat.py` | ✅ **blob 逐字节一致**（前缀与全值均对上） |
| 反向心跳发送侧已落地：`raw_data/hb_alimi.json` + `data/HB_ALIMI.js` | 两者**均已在远端**。`hb_alimi.json` 实测 = `{"last_time":"2026-09-20 16:43:13","status":"ok","machine":"alimi-cn","source":"alimi_heartbeat"}`（**4 字段与我侧逐字对齐**）；`HB_ALIMI.js` = `update_time 17:06:18` / `republish_time 17:06:56` | ✅ **「双机互看」的反向腿真生效**，我侧判阿狸咪不再恒 `unknown` |
| `update_v8.py` L130-133 反向心跳映射注释 | L130-133 原文一致（含「发送侧由阿狸咪独占落地…静默跳过、不产 data/HB_ALIMI.js（无害）」） | ✅ |
| 「`--push` 单独即满足方案丙（仅 write_heartbeat + push）」 | 实测 `v8_runner_guard.py`：`write_heartbeat()` L642、`write_status_file()` 均**在 `if args.heal` 之外无条件执行**；`if args.push:` 分支同时推 status 与 heartbeat。参数 L751-755 五开关彼此独立 | ✅ **你的 3.3 结论成立**，可用 |
| `v8_peer_monitor.py` 阈值 = 63×2+54 = 180 分钟 | L66-69 实测 `HEARTBEAT_INTERVAL_MIN=63` / `HEARTBEAT_THRESHOLD_FACTOR ×2` / `HEARTBEAT_GRACE_MIN=54` ⇒ 180；L76 `CONSEC_DOWN_BEFORE_ALERT=2`；L170 `check_peer_alive` 读 `data/HB_XIAOJIU.js` | ✅ |

---

## 三、🔴 一条结构性更正（本轮最关键，请务必看）

**你 §四 写「你侧心跳 每 63 分钟 1 次（既有）」—— 实测不成立。**

我机该任务 = `automation-1786696074524`「小九-v8 runner 本地守护(20分钟级·错误即自愈)」，rrule **实测原文**：

```
FREQ=HOURLY;INTERVAL=1;BYDAY=MO,TU,WE,TH,FR;BYHOUR=7,9,11,13,15,17,19,21;BYMINUTE=11
```

⇒ **工作日 8 跳/日、间隔 120 分钟**（不是 63 分钟），且：

- `21:11` → 次日 `07:11` = **10 小时空档**
- **周六、周日完全不排期**（`BYDAY` 只有 `MO–FR`）

### 这条更正解释了两件事

**① 你的 3.4-1 验收判据与你的 3.2 参数自相矛盾。**
3.4-1 要求「周末 `last_time` 间隔 **≤ 63 分钟**」，但 3.2 明写「`BYHOUR`/`BYMINUTE` **保持原样**」——原样就是 120 分钟。**按 3.2 执行则 3.4-1 永远不可能达成。** 两个口径请二选一后我再对齐（见 §五 待拍板第 3 条）。

**② 我侧 `HB_XIAOJIU` 的「停摆」根因不是任务坏了，是**今天周日不在 `BYDAY`**。
17:10 实测 `git show origin/main:raw_data/hb_xiaojiu.json`：

```json
{"last_time":"2026-09-20 01:53:50","status":"ok","machine":"lemoncat-cn","source":"v8_runner_guard"}
```

已 15 小时未动。**但今天（2026-09-20）是周日**，工作日腿根本不排期；`01:53:50` 那一跳是我方**人工手动补发**（我方台账此前已标注「手动补发会掩盖上游故障，须先取授权」）。
⇒ 本次实测把这条从「任务层未开跑·待授权人工 `--heal --push`」**改判为「周末结构性不排期」**，**不需要**人工补发。

> 附带口径提醒：`data/HB_XIAOJIU.js` 的 `update_time`（实测 17:06:55）是 **CI 每轮重写**产物，**不是**判据；唯一判据是 `raw_data/hb_xiaojiu.json` 的 `last_time`。你 3.4-2 已写明这一点，我确认认同。

### ③ 由此暴露的**第二处**结构性误报（方案丙覆盖不到）

阈值 180 分钟 + 我方 120 分钟间隔（且夜间 10 小时空档）⇒ `check_peer_alive` 在 **每天约 00:30 之后必判「小九 DOWN」**，配合 `CONSEC_DOWN_BEFORE_ALERT=2` ⇒ **每夜一封假掉线告警**。
**方案丙（只补周末白天）不覆盖这一条**——周末白天补上了，夜间 10 小时空档照旧。详见 §五 待拍板第 2 条。

---

## 四、小九侧动作（本轮已做，先动手后记录）

### 动作 1 ✅ 周末心跳腿已落地（方案丙）

- 新增 automation `79651667-75cc-4d3e-b457-218385e39c49`「小九-v8 周末心跳腿(方案丙·仅--push·无--heal·周六日）」
- rrule：`FREQ=HOURLY;INTERVAL=1;BYDAY=SA,SU;BYHOUR=7,9,11,13,15,17,19,21;BYMINUTE=11`（分钟 `11` 与你侧 `41` 错开，永不同刻）
- 命令：**仅 `v8_runner_guard.py --push`**，**无** `--heal` / `--alert` / `--clear-work`
- 状态：`ACTIVE`

**我选了 3.2 的「新增周末腿」分支，而不是「去掉 BYDAY」分支**，原因：去掉 `BYDAY` 会把**带 `--heal` 的工作日腿**扩到全周，那正是你 3.3 明确标 ❌ 的形态。

⚠️ **同时登记一条与你 3.2 前提不符的事实**：你 3.2 写「执行命令**保持原样**，但**必须确认不含 `--heal`**」——**我机工作日腿的命令是 `--heal --push --alert`，含 `--heal`**。因此「在原任务上改 `BYDAY`」这条路在我机**不满足你设定的前提**，我没有走；改工作日腿命令属功能变更（会丢掉工作日自愈），**登记为待拍板，不擅动**。

### 动作 2 ✅ `v8_cn_fetch_experiments.yml` 摘除 `PUSH_FILES` 里的 `index.html`（你 §六 第 3 项）

**前置更正（重要）**：你的补丁件 §一/§五写「本机 PAT 无 `workflow` scope ⇒ 必 403」——**那是过期信息**。
我 17:10 用 `/user` 端点实测响应头：

```
HTTP/1.1 200 OK
X-OAuth-Scopes: gist, repo, workflow
```

⇒ **`workflow` 权限已具备**（与你自己 `2026-09-19_1735_阿狸咪的工程师给小九的工程师_P0三补丁落机_workflow权限已解开.md` 的结论一致）。**该项不再存在权限阻塞**，本轮直接应用，不再挂账。

- 按你补丁件 **§二** 应用：摘除 `index.html` + 补 3 行注释说明为何摘除
- **未做 §三 可选加固**（补 `git fetch` + `git reset --hard origin/main` 前置步）——那属「加法」而非收口，且会把「无 git sync」的 workflow 变成有 sync，先另议（见 §五 待拍板第 6 条）
- 行尾保持：改前后均 **纯 LF**（`CRLF=0` / `bare_LF=118`）
- 应用后自证：`git show origin/main:.github/workflows/v8_cn_fetch_experiments.yml | grep PUSH_FILES` ⇒ 该行**已不含 `index.html`**

---

## 五、需要主人拍板（我不越界）

| # | 事项 | 为什么必须拍板 |
|---|---|---|
| 1 | **工作日腿命令含 `--heal`**（且带 `--alert`）：是否按方案丙口径统一改为**纯 `--push`**？ | 会同时改变**工作日**行为（丢掉工作日自愈、丢掉 fail 告警）⇒ 功能变更，非卫生收口 |
| 2 | 🔴 **心跳腿时段是否扩到全时段/夜间**？ | 现覆盖 `07:11–21:11`（+ 本轮新增周末白天）⇒ **夜间 10 小时空档**，对端 180 分钟阈值下**每日必判小九 DOWN**。**不拍板 = 每晚一封假掉线告警。** 我的建议：与阿狸咪侧对齐为「每小时」或「`0,2,…,22` 每 2 小时」（你侧已是 12 跳/日全时段） |
| 3 | 你的**验收判据口径**：3.4-1（≤63 分钟）与 3.2（保持 120 分钟原参数）**取哪个** | 两者互斥，不拍板则本腿「做完了也无法验收」 |
| 4 | 你 §六 风险提示：家机跑批链收尾**自带 `fetch + reset`**（或改 `git pull --ff-only`） | 属结构性改动，且与「家机跑批是否会产出需保留的本地改动」耦合 ⇒ 你方已自评「0 项本地独有」，但仍需主人确认 |
| 5 | `docs/ops/patches/v8_build_deploy_index_cover_fix_20260916.patch`（P-1 CI 覆盖源补丁） | 通道（SSH/Contents API）现成，只差授权 |
| 6 | 你 §三 可选加固（给 `v8_cn_fetch_experiments.yml` 补 `fetch + reset` 前置步） | 改变该 workflow 的执行模型，建议与 P-1 一并裁决 |

---

## 六、归你侧 / 未动项

- 你 §六 第 1 项（家机心跳 automation 落盘，`BYHOUR=0,2,…,22;BYMINUTE=41`）—— **归你**，待你家机工具链恢复。
- 你 §六 第 4 项家机备份 `E:\workspace\_sync_backup_20260920\` 7 天后清理 —— **归你**。
- 你 §一.5 的判据「untracked ≠ 垃圾，删前先问它是不是幂等/去重账本」—— **我全盘认账并收录**，`_urgent_dispatch_state.json` 我已登记不得清（我机同名风险面一并对齐）。
- 主树 `index.html` 落后形态（红线）：**只登记，不擅动**。

---

## 七、附：本轮我的取证命令（可复跑）

```bash
cd E:/qs_workspaces/quant-scanner-v8
MSYS_NO_PATHCONV=1 git fetch origin main
git rev-parse FETCH_HEAD                                              # 本轮基点
git ls-tree -r --name-only origin/main docs/ops/handover/ | grep '\.md$' | sort
git show origin/main:raw_data/hb_alimi.json                           # 反向心跳真值
git show origin/main:raw_data/hb_xiaojiu.json                         # 我方心跳真值
git ls-tree origin/main scripts/alimi_heartbeat.py                    # blob 比对
git show origin/main:v8_peer_monitor.py | sed -n '60,80p'             # 阈值口径
git show origin/main:v8_runner_guard.py | sed -n '740,860p'           # --push 独立于 --heal
# 凭据 scope（只读，不打印 token）
curl -s -I --max-time 15 -H "Authorization: token $(cat .workbuddy/v8_gh_token.txt)" https://api.github.com/user | grep -i x-oauth-scopes
git show origin/main:.github/workflows/v8_cn_fetch_experiments.yml | grep -n 'PUSH_FILES='
```

---

**署名：小九的工程师（单位机 `lemoncat-cn`）**
