# URGENT · 2026-09-14 11:58 阿狸咪的工程师给小九 · 构建部署门禁僵尸红 + align 两项真未对齐

> 阿狸咪的工程师（家用机）· 盘中安全巡检 · 非踩踏风暴 · 全程 0 dispatch

## 一句话结论

**失败 run 是僵尸红，已自愈，不需要动；但暴露的两条隐患必须修** —— ①「恢复完整版」用旧基线覆盖会带回已删死引用，门禁会红；② `logic.html` 缺 2 个 workflow 文档化，`align_logic_ops` 会**持续**卡死构建部署。

---

## 一、本轮巡检输入

- 时间：2026-09-14 11:54 CST（周一盘中）
- 活跃/排队 run：**0**；core 配额 **5000/5000 全空**；无 403
- 判定：**非踩踏风暴** → 全程 **0 dispatch**
- 巡检脚本报：近 3h 真实失败 1 次 → `34803781864` `☁️ v8 构建部署(云端ubuntu)` failure

---

## 二、根因 A（P1 · 已自愈 · 但机制必须封堵）

### 铁证链

失败 run `34803781864` 的失败 step = `[9] 🛡️ Pre-deploy audit`，job `103851520105`（runner = GitHub 托管机，非自托管）：

```
✅ [1/6] py_compile: 170 个 .py 文件 0 错误
✅ [2/6] new Function: 23 个 inline script 块 0 错误
✅ [3/6] data 完整性: data/*.js 数量=102, 全部 > 100B
❌ [4/6] align_logic_ops: 未对齐 2 项（详见根因 B）
✅ [5/6] workflow YAML: 30 个 workflow 全部有效
❌ [6/6] HTML 数据引用: 发现 8 处 404 断链:
    index.html -> data/DELISTED.js
    index.html -> data/ETF_SUBSCRIPTION.js
    index.html -> data/ETF_SUBSCRIPTION_EM.js
    index.html -> data/HERDING_DATA.js
    index.html -> data/MARKET_PATH_PROBABILITY.js
    index.html -> data/POTENTIAL_PICKS.js
    index.html -> data/RPS_BACKTEST.js
    index.html -> data/STOCK_RPS.js
✅ [7/7] gate 头注一致
🚫 2 项校验失败 → 阻断 deploy！
```

### 8 处 404 的真相（逐字节比对，非推断）

| 提交 | 时间 | index.html 字节 | 该 6 个引用出现次数 |
|---|---|---|---|
| `2cbadf2b2` feat(ui) | 03:47 | 822,928 | ETF_SUBSCRIPTION=8 / HERDING_DATA=3 / MARKET_PATH_PROBABILITY=6 / POTENTIAL_PICKS=9 / RPS_BACKTEST=4 / STOCK_RPS=19 |
| `aee86d232` fix(recover) | 03:55 | **957,833** | **全部 = 0** |
| 当前 main | — | 957,833 | 全部 = 0 |

**关键时序**：失败 run 由 `2cbadf2b2`（03:47）触发，03:49 跑到 audit 时用的正是那份 **82 万字节的残缺版**；9 分钟后 `aee86d232`（03:55）用 **1108023 基线**恢复成 95 万字节完整版，**这 6 个死引用已被一并清除**。`34804243954`（03:55，对应修复提交）随后已正常进入构建。

⇒ **结论：8 处 404 是「僵尸红」，跑到一半被新提交判死，main 当前是干净的，无需任何修复动作。** 请勿再为它加派 dispatch 或重跑。

### 🔴 但机制是隐患：恢复基线 = 死引用复活

`aee86d232` 的 `fix(recover): 恢复 index.html 完整版(1108023基线)` 属于**覆盖式恢复**。本次很幸运——恢复的基线恰好没有死引用；但**只要恢复的基线取自「删数据之前」的版本，8 处 404 会立刻全体复活**，且 `pre_deploy_audit` 会正确判红阻断 deploy，表现为「页面已上线但数据全不更新」的假死。

**一劳永逸修法（建议）**：把「引用存在性」检查**前置到写 index.html 的那一步**，而不是只在 CI 事后拦。
1. `fix(recover)` 类操作**禁止整文件覆盖**，改 `git checkout <base> -- index.html` 后**必须重跑** `python .github/scripts/pre_deploy_audit.py` 全绿才算完成；
2. 在仓库根加 `pre-commit` 钩子：只要 `index.html` 被 stage，自动跑 `pre_deploy_audit.py`，红了直接拒提交（比等 CI 3 分钟后红更早）；
3. 更彻底：`index.html` 里那 6 个 `<script src>` 标签本就是**死标签**（对应文件已按「前端零引用死数据」原则删除），应**直接删标签**，从源头消灭复发可能。

**复发特征**：`☁️ v8 构建部署(云端ubuntu)` run failure，失败 step 恒 = `[9] Pre-deploy audit`，日志 `[6/6] HTML 数据引用` 报 404，且所列文件在 `data/` 下确实不存在。

---

## 三、根因 B（P1 · **真未对齐 · 正在持续卡死构建部署**）

`pre_deploy_audit` 的 `[4/6] align_logic_ops` 报：

```
⚠️ 逻辑详解页/运维页 与 实际 workflow 存在未对齐项：
  - 中国数据抓取(hosted 备援)（v8_cn_fetch_cloud_hosted）：实际有 cron 调度，但文档源（逻辑详解页/运维页）未文档化
  - 高手 ima 强势股（v8_ima_strong_stock）：实际有 cron 调度，但文档源（逻辑详解页/运维页）未文档化
```

**远端实证（我已核对 main）**：

| workflow | 实际 cron | logic.html 提及次数 |
|---|---|---|
| `v8_cn_fetch_cloud_hosted.yml` | ✅ `30 12 * * 1-5` | **0** |
| `v8_ima_strong_stock.yml` | ✅ `45 7 * * 1-5` | **0** |

`index.html` 各提及 1 次，`logic.html` **完全没写**。⇒ **这是真 bug，不是误报**，且会让 `pre_deploy_audit` **每次**都红 → 构建部署**永久 blocked**。

**修法（二选一）**：
- **方案甲（推荐）**：在 `logic.html` 的时间/任务矩阵里补这两行（执行机填对应 runner、时间填实际 cron），并跑 `python .github/scripts/align_logic_ops.py` 验 EXIT 0；
- **方案乙**：若这两个 workflow 已不再需要，直接从仓库删除并把引用清干净。

⚠️ **这是本轮唯一必须动手的项** —— 根因 A 已自愈，但 B 会让今天**每一次**构建部署都红。请优先处理。

---

## 四、数据新鲜度判读（不构成新故障）

权威基准 = GitHub `main`（非本地，本地坚果云滞后是正常现象）。落后 11 个，**全部 `post_close`/`premarket` 类**：

| 文件 | update_time | 落后 | 性质 |
|---|---|---|---|
| GOLD_POOL | 2026-09-11 15:00 | 68.9h | 盘中判定，属 T+1 链死锁 |
| CANDIDATE | 2026-09-11 15:00 | 68.9h | 同上 |
| INST_TRADE | 2026-09-11 17:47 | 66.1h | 同上 |
| V8_POOL_TRACKER | — | 62.4h | 同上 |
| SECTOR_RS | — | 58.9h | 同上 |
| INDEX_HISTORY | **2026-09-14 11:41:19** | 39.3h | ✅ **今日已自愈**（meta 已刷到 11:41，脚本按首个 match 取到旧值故仍报落后） |
| SUSPENSION_ALERT | — | 38.2h | premarket 类 |
| SECTOR_FUND_FLOW_TREND | — | 38.1h | post_close 类 |

**判读**：这些**不是盘中故障**，正是 **10:45 那轮已交接的「T+1 链盘中死锁」**（`docs/ops/handover/2026-09-14_1050_URGENT_...T1链盘中死锁.md`）的产物 —— 盘中硬闸门无条件拦掉 `chain_kind=t1` 的合法时窗，导致 09-12 周末批至今推不动。**根因与修法见上一份文档，本份不重复。**

> 另注一条脚本口径瑕疵：`INDEX_HISTORY` 文件内**有两个** `update_time`（meta 侧 `2026-09-12T20:38:29` 与文件尾 `2026-09-14 11:41:19`），`intraday_watch.py:187` 用 `re.search` 取**首个** → 会**长期假报落后**。建议改为取**最后一个** match 或显式定位 `meta.update_time`。本轮已用第二个值确认其真实新鲜。

---

## 五、明确不做（防踩踏）

- 不加派任何 `workflow_dispatch`
- 不重跑 `34803781864`（僵尸红，已自愈）
- 不动 `data/` 任何 need/must 账
- 不为根因 A 单独提交（无对象可改）
- 不碰 `index.html`（坚果云同步 + 小九白天值班期）

## 六、遗留在册（沿用，未变）

| # | 项 | 状态 |
|---|---|---|
| 1 | `?v` 口径分裂 | 未修（第 5 次在册）：105 处 4 种口径 |
| 2 | `check_local_head_sync()` CI 假红 | 09-14 09:34 已交接，未复查 |
| 3 | 港股名单塌缩（HK=0） | 未修 |
| 4 | `v8_weekend_light.py` 覆盖 `WEEKEND_RUN.js` | 未修 |
| 5 | T+1 链盘中死锁 | 10:45 已交接，未见修复动作 |

---

*阿狸咪的工程师 · 2026-09-14 11:58 CST · 家用机*
*巡检脚本：`~/.workbuddy/v8_watch/intraday_watch.py` · 退出码 1（2 项异常：1 已自愈 + 1 真未对齐）*
