# 交接文档 · 阿狸咪接管（2026-08-13 18:20）

> 交接人：小九（白天独家 7:45-17:45 已结束，18:00 阿狸咪接管）
> 状态：**全部今日修改已推仓入库，云端/双机/运维页/逻辑详解页已对齐，无用文件已清理**

## 一、今日小九全部修改（已 push 到 origin/main，HEAD=5e4d14191）

| SHA | 时间 | 内容 |
|---|---|---|
| `ce13f07c4` | 18:17 | **v8 数据对齐修复**：重跑 final_recommend + build_candidate_pool，update_v8 全量重建 31 个数据注入文件 |
| `5e4d14191` | 18:19 | **chore 清理**：删除 13 个 `_` 前缀临时日志/孤儿脚本 |
| （云端 `v8 healthcheck` / `v8 build`） | 18:01-18:18 | 云端巡检自动提交（仅动 `HEALTH_CHECK.js` / `freshness_status.json`，阿狸咪独家维护，勿本地改） |

## 二、数据对齐根因 + 修复（用户原话：TOP3 没荣昌、已下架页陈旧）

**根因**：17:00 算法批（`59d76669f`）只更新了 `raw_data/*.json`，没跑 `update_v8.py` 重建 `data/*.js` → 前端注入文件停留在午前/中午。
**修复**（小九已执行，已入库）：
1. 重跑 `algorithms/final_recommend.py` → `raw_data/final_recommend.json`(17:55) + `data/FINAL_RECOMMEND_DATA.js`(TOP3 含 **荣昌生物 688331 rank2**)
2. 重跑 `algorithms/build_candidate_pool.py` → `out/candidate_pool.json`(18:12，351只) → 搬运 `raw_data/candidate.json`
3. `python update_v8.py --category post_close` → 重建 31 个 `data/*.js`（含 CANDIDATE / TRIPLE_* / COCKPIT_* / LHB_* / TOP3_TRACK 等）

**线上核验（Python urllib 直拉 Pages）**：
- `FINAL_RECOMMEND_DATA.js`：荣昌生物已在 TOP3 ✅
- `CANDIDATE.js`：`2026-08-13 18:12:03` ✅
- `TRIPLE_CONSENSUS.js`：`2026-08-13 17:16:01` ✅

## 三、命名契约（全站强制，铁律）

| 规则 | 正确 | 错误（禁） |
|---|---|---|
| 时间戳显示 | `window._fmtAshareRel(ts)` | `'更新于 '+dtStr` 裸拼 |
| 徽章时间戳 | `window._uBadge(slot,horizon,ts,sub)` | `'生成日期：'+update_time` |
| 任意日期 | 走上面两函数 | `String(update_time)` 裸显示 |
| 兜底 | 函数缺失则保留 ISO 不崩 | 写死日期字符串 |

## 四、今日清理清单（13 个文件，已删+提交）

- 5 个日志：`_algo_run.log` `_algo_run_1700.log` `_heartbeat.log` `_v8_watchdog.log` `_watchdog_run.log`
- 8 个孤儿脚本（全部 `grep` 确认无引用）：`_audit_dup_funcs.py` `_check_scripts.py` `_freshness_check_1700.py` `_merge_pick_newer_1700.py` `_push_urgent_api.py` `_trim_task_schedule.py` `_v8_cloud_dispatch_patrol.py` `_v8_intraday_check.py`
- `backup/` 目录已为空；`out/candidate_pool.json` 未被 git 追踪（构建产物，可忽略）

## 五、阿狸咪接管后「该做 / 不该做」

✅ **该做**
- 18:00 接班先 `git pull --rebase --autostash origin main`（小九已用此命令，勿用裸 `git pull --rebase`）
- 夜间/周末常规算法跑批、盘中刷新主通道（中国 IP 直抓 → 只 push `raw_data/` → 云端 `v8_build_deploy` 自动重建）
- `HEALTH_CHECK.js` / `freshness_status.json` 由云端巡检独家写，本地无需碰

❌ **不该做**
- 勿本地改 `HEALTH_CHECK.js`（会与云端巡检打架，push 必被拒）
- 勿改 `index.html` 除非用户明令「上线」（红涨绿跌铁律全站执行）
- 勿 `git add -A`；只 `git add <显式路径>`
- 候选池港股：小九已禁用 gold_pool 港股兜底（`build_candidate_pool.py` 的 `_hk_from_goldpool` 已 `return`），勿恢复
- 主站首屏 8-15s：用户明令不修，勿动 gzip / 精简 STOCK_QUOTE.js

## 六、待办 / 注意

- ⚠️ `data/CANDIDATE.js` 仍含 50 只「港股成交前50」（直取港股成交额前50，属既定源，非 gold_pool 兜底）。用户若要求候选池纯 A 股，再议。
- ⚠️ `raw_data/fundamental_quality.json`（三重共识/TOP10 质量分源）仍是 8-12，今日未重跑 `fetch_fundamental_quality.py`（需网络，风险高，小九未动）。若三重共识质量分异常，先查此文件。
- 本机 SSH remote 已切，push 走 `git@github.com:ah-quant999/quant-scanner-v8.git`，勿改回 https。
