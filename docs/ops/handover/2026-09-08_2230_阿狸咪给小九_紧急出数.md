# 🚨 紧急交接 · 阿狸咪 → 小九 · 2026-09-08 22:30 · 今晚必须出算法链数据

> 用户拍板：**今晚必须把缺位的卡全部补出，不等明天**。本交接单取代 22:15 版「不派 D 批」结论。
> 小九你在线、中国 IP 能跑 → 今晚 D+E 批全交给你本地跑，阿狸咪侧零并发。

---

## 一、背景（为什么今晚缺位）

| 批 | 设计时点 | 今晚实测 | 状态 |
|---|---|---|---|
| B 选股批 | 18:10 | 19:57 已出 | ✅ 正常 |
| D 汇总批(20:00) | 20:00 | **cron 静默失效 + 小九白天手动 dispatch runs 1610-1613 全 failure** | ❌ 未出 |
| E 回测批(21:00) | 21:00 | run 1614 22:01 failure | ❌ 未出 |

**缺位卡（用户今晚要看到）**：
- ③ **v8 选股生命周期**（ALGO_LIFECYCLE.js，旧 01:44）→ D 批 final_recommend.py 产
- ④ **回测汇总**（BACKTEST_SUMMARY.js，旧 02:49）→ E 批产
- ⑤ **中信 PE 极值温度计**（CITIC_PE_THERMO.js，旧 15:50）→ E 批 factor_lab_backtest 产

> FACTOR_LAB（②因子实验室）11:27 已新鲜，今晚**不重跑**。

---

## 二、小九你今晚执行（本地 Python，不走 GitHub Actions 云端）

**为什么本地跑**：云端 22:11 那次 `trigger_algo` 失败在第一步「🗓 解析数据日期」（`v8_date.today_data_date()` 云端环境不稳）；本地中国 IP 可解，且从根上避云端失败步。

```bash
cd /path/to/stock-scanner        # 你机器上的仓库根

# 放行 time_gate 本地门控（memory 铁律：本机跑算法必加）
#   Windows CMD:
set ALLOW_LOCAL_ALGO=1
#   Git Bash / WSL:
export ALLOW_LOCAL_ALGO=1

# ① 跑 D 批：产 FINAL_RECOMMEND_DATA.js + ALGO_LIFECYCLE.js（= ③ v8 选股生命周期）
python algorithms/run_algorithms.py --stage D

# ② 跑 E 批：产 BACKTEST_SUMMARY.js（= ④回测汇总）+ CITIC_PE_THERMO.js（= ⑤中信 PE 极值温度计）+ factor_lab_backtest
python algorithms/run_algorithms.py --stage E

# ③ 完事重算 ?v 缓存戳 + 部署
python v8/update_v8.py
python v8/deploy_v8.py
# 或手动推（显式 add，禁 -A）：
git add -f data/FINAL_RECOMMEND_DATA.js data/ALGO_LIFECYCLE.js data/BACKTEST_SUMMARY.js data/CITIC_PE_THERMO.js
git commit -m "fix: D+E 批补出 09-08 缺失卡 ③④⑤"
git push origin HEAD:refs/heads/main
```

**预计耗时**：D 批 8-12min（单脚本 final_recommend）→ ~22:40 ③ 上线；E 批 25-40min（6 个 backtest）→ **23:00 前 ④⑤ 全上**。

---

## 三、不用管的卡

- **② 因子实验室** FACTOR_LAB.js 11:27 已新鲜（10h 内），今晚不重跑。
- **① 机构研究·AI 解析建议** = `maharo_macro.js`（cookie 卡，不在算法链）：
  - 你机**有 maharo cookie** → 顺带 `python v8/maharo_daily_pull.py`
  - 你机**无 cookie** → 标"明早阿狸咪本机补"，今晚不动（已停更 1 日，多停 1 日不影响大局）

---

## 四、阿狸咪侧已为你做的（防风暴，零并发）

| 自动化 ID | 名称 | 状态 | 原因 |
|---|---|---|---|
| `automation-1787141452867` | 云端按铃 30min | **PAUSED** | 22:11 失败 trigger_algo 就是它；你本地跑不需要 |
| `90b7ea6e-...` | 夜班监控(每半点) | **PAUSED** | 防查/派撞车 |
| `9ee4a617-...` | 每晚审计 23:30 | **PAUSED** | 防 git 抢 |
| `2d3ac36e` | 23:00 最终推荐监控 | **ACTIVE** | 自动盯 FINAL_RECOMMEND_DATA 是否绿 |

**阿狸咪侧不做任何 dispatch、不抢 git push，纯盯链验证。**

---

## 五、验证

你 push 后，阿狸咪会立即 `git fetch + grep update_time` 验 ③④⑤ 上线，并给你"全绿"汇报。
或直接把 commit 哈希发她（WorkBuddy 紧急指令监听 `1783696499491` 只读在线）。

---

## 六、D2-A / D2-A2 / D2-C 一劳永逸修复（明早做，不急今晚）

| 项 | 文件:行 | 改法 |
|---|---|---|
| D2-A | `v8_health_check.py` L1243-1245 `adjust_max_age` | 仅 `def_max<=360` 收紧（FACTOR_LAB 1440 被压 360 误报红黄灯） |
| D2-A2 | `v8/factor_lab_gen.py` 入 `run_algorithms.py` STAGES["E"]+ORDER | 孤儿修复（因子实验室生成器不挂链） |
| D2-C | `index.html` L9537 `liveUpdated` | 改取 `HEALTH_CHECK.updated`，isStale 30→60 |

完整规格见磁盘 `URGENT_小九_2026-09-09_运维红黄灯一劳永逸修复.md`（未入库）。

---

*本单 commit 后推送 main，小九 `git pull` 或 GitHub 网页可见全文，无需截图。*
