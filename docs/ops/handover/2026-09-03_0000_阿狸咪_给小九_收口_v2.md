# 阿狸咪 → 小九 收口交接（2026-09-03 午间）

> 主人令：把"最新改好的"交给小九，下午由小九合并上线。阿狸咪不在白天窗口推 main，仅做交接。

## ✅ 必须上线（待小九合）

**变更：板块资金三合一 Block3 标题 `周期定位` → `板块资金趋势`（方案B 重命名）**
- 文件：`index.html`，**仅 2 行**，无 data 变动、无 `?v` 变动。
- 精确位置：约第 2349 行，`<!-- Block 3: 周期定位 -->` 区块内。

**改前：**
```html
      <!-- Block 3: 周期定位 -->
      <div class="sf-block sf-block-trend">
        <div class="sf-block-title"><span class="sf-block-icon">🔄</span>周期定位</div>
        <div id="sfBlock3"><div class="ipo-empty">加载中…</div></div>
      </div>
```

**改后（仅改第 3 行那一个词）：**
```html
      <!-- Block 3: 周期定位 -->
      <div class="sf-block sf-block-trend">
        <div class="sf-block-title"><span class="sf-block-icon">🔄</span>板块资金趋势</div>
        <div id="sfBlock3"><div class="ipo-empty">加载中…</div></div>
      </div>
```

**两种落地方式（任选其一）：**
- 方式A（手写，最稳）：在 index.html 第 2349 行附近把 `周期定位` 改成 `板块资金趋势`。
- 方式B（git）：本地 commit `4fea1a3db6` 是此改动（仅在阿狸咪机 .git，未推 GitHub）。若坚果云已把它同步到小九机，可直接 `git cherry-pick 4fea1a3db6`；否则用方式A。

## 🛡 小九收口步骤（白天窗口内执行）

```bash
# 1) 改完 index.html 后，仅 add 这一个文件（绝不加 -A，防坚果云误删带入）
git add index.html
git commit -m "feat(UI): 板块资金三合一Block3 周期定位→板块资金趋势(方案B)"

# 2) Nutstore 安全原子推：拉最新 → 用 ours 重放 → 推 main（失败重试）
git fetch origin main
git rebase -X ours FETCH_HEAD
for i in 1 2 3; do git push origin HEAD:refs/heads/main && break || sleep 3; done

# 3) 若 git push 在 github.com:443 挂死（部分阻断），改走 GitHub API 兜底：
#    python "C:/Users/HH20210606/.workbuddy/skills/github-api-push-fallback/scripts/push_via_github_api.py"
```

**校验**：node 审计 23 段 inline script 0 错；GitHub Pages 生效后 Ctrl+F5 看"板块资金趋势"标题即上线成功。

## ✅ 已上线，无需动
- 斐波那契双卡合并（`a67f0fe28`，仅前端，净减 22 行，无 data 变动）。

## 🚫 不要做
- **9/3 数据不要手动跑**（主人选 A：保留晚间正常链）。晚间自动跑批链已就位：
  `18:05 保底触发` → `19:30 兜底(查15:00后success)` → `21:30 最终闸(force+bypass)`。
- 时间门陷阱：18:00 前的补跑必须带 `bypass_time_gate=true`，否则选股段被跳过白跑。

## ❓ 待小九确认（非阻塞）
阿狸咪工作树曾有**未暂存**的 `.github/workflows/v8_algo_cloud.yml` 改动（"静默闸门"，防空转 dispatch 把 git 历史撑向 1GB 上限，约 23 行）。阿狸咪已 `git stash` 隔离在本机（stash msg=`v8_algo_wip_20260903`），未提交、未推送。
- 若这是**小九的修复**且尚未提交 → 请在小九机正常提交；
- 若小九机已是新版本 → 忽略即可。
阿狸咪不会自行提交该文件，避免双机冲突。

---
*交接人：阿狸咪 ｜ 时间：2026-09-03 12:28 CST ｜ 状态：本地 HEAD=a3fe7eee2（已对齐最新 main），仅待小九合 Block3 改名。*
