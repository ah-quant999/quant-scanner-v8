# HANDOVER｜小九 → 阿狸咪｜2026-09-19 13:02｜每晚磁盘清理任务跨机同步(脚本入仓+建任务步骤)

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> ⚠️ 本档遵守「**禁止预测式表述**」铁律：所有结论写成「截至 X 时刻实测 = Y」，涉及「后续会怎样」一律改为**可跑的复核命令**。本档**不写自身所在提交的 sha**。

---

## 一句话结论

**小九机上那个「每晚 23:00 三盘磁盘垃圾清理」任务已经成熟（脚本 27436B / 9 个批次 / 办公文档硬保护），但脚本过去只存在于小九机本地、不在 git 仓里，阿狸咪机拿不到。本次已把脚本改造为「跨机自适应版」并推入仓 `docs/ops/scripts/disk_clean_weekly.py`，同时给出阿狸咪机建立同名每日任务的完整步骤。**

---

## 一、主人原话与需求定性

> 「每晚的删除任务也都是删这些既占位置又无用无关联的垃圾文件吧？那你和阿狸咪分享一下，让她每晚也建个任务这么删。」

**需求拆解**：
1. 「删这些既占位置又无用无关联的垃圾文件」= 确认清理对象是**缓存/日志/临时/更新包残留**这类纯垃圾（不碰办公文档）。
2. 「和她分享一下」= 把**可执行的方案**给阿狸咪，不是只给一句提醒。
3. 「让她每晚也建个任务这么删」= 阿狸咪机上要有**每晚定时任务**，与主机同标准。

---

## 二、截至 2026-09-19 13:02 的实测现状（小九机）

### 2.1 已在跑的任务

| 项 | 实测值 |
|---|---|
| 任务名 | 磁盘垃圾每日清理（C/D/E 盘全覆盖·每晚23:00·真删不进回收站·工作文件硬保护） |
| automation id | `0c743285-affb-4dd7-acdb-617ca70435d1` |
| 调度 | 每日 23:00（`FREQ=DAILY;BYHOUR=23;BYMINUTE=0`） |
| 状态 | ACTIVE |
| 脚本真身 | `E:\workbuddy-data\scripts\disk_clean_weekly.py`（27436 B，纯 LF） |
| 经 junction 访问 | `C:\Users\Administrator\.workbuddy\scripts\disk_clean_weekly.py`（同一文件） |

### 2.2 最近一次真跑记录（脚本自带审计日志 `_clean_log.json`）

| 项 | 实测值 |
|---|---|
| 模式 | `go`（真删） |
| 起止 | 2026-09-18 23:01:23 → 23:01:42 |
| 释放 | 110,494,244 B ≈ **0.103 GB** |
| 删除条目 | **1869 条** |
| 分批次 | 批1=9.4KB / 批6=17.9MB(80条) / 批7=92.4MB(1784条) / 批8=197KB(3条) |
| 跳过办公文档 | **0 条**（该轮无命中） |
| 失败 | 2 条（批8） |

### 2.3 清理对象是什么（批次清单）

| 批 | 名称 | 内容 |
|---|---|---|
| 0 | 脚本行为验证·事故遗留 | 自检遗留 |
| 1 | 陈旧临时文件 | 各盘 tmp 下 mtime>7天 |
| 2 | 畸形根目录 | `C:\c\...` / `E:\e\...` 这类 POSIX 路径误落盘 |
| 3 | 安装包缓存与陈旧备份 | 安装包 / 旧备份 |
| 4 | `E:\Temp` 陈旧项 | 预算 3600s |
| 5 | C 用户临时与缓存 | Temp / pip Cache / CrashDumps / INetCache |
| 6 | `.workbuddy` 会话日志 | >7天，重要文件硬保护 |
| 7 | `.workbuddy` 缓存目录 | traces / logs / file-history / file-tree-manifests，预算 3600s |
| 8 | **C 盘应用缓存**（打中 C 盘的关键批） | 24 个白名单子目录：npm-cache / pip Cache / INetCache / Explorer 缩略图 / CrashDumps / D3DSCache / Temp / kingsoft\wps\cache / Tencent / sogoupdf\cache / MeituApp\Cache / winToolBox\cache / Chrome Cache / 360Chrome Cache / 各类 `*-updater` 残留 / Roaming\Tencent\Logs / secoresdk\logs / Coze\Cache / QQ\Cache |

### 2.4 三盘空间真值（截至 2026-09-19 13:02，属性名访问）

| 盘 | 总 | 已用 | **可用** | 判定 |
|---|---|---|---|---|
| **C:** | 100.00 GB | 72.78 GB | **27.22 GB** | 🔴 最紧 |
| **D:** | 183.00 GB | 28.44 GB | **154.57 GB** | ✅ 最宽 |
| **E:** | 181.63 GB | 26.88 GB | **154.74 GB** | ✅ 宽 |

> 自检 `total - used - free = 0.0000` 通过。
> **禁止按位置解包 `shutil.disk_usage()`** —— 官方顺序是 `(total, used, free)`，历史上按 `(total, free, used)` 解包导致结论整体反转并被主人连纠三次。

---

## 三、本次交付物（已入仓）

### 3.1 新增文件

```
docs/ops/scripts/disk_clean_weekly.py     跨机自适应版（29770 B，纯 LF）
```

**相对小九机本地版的唯一改动**：把 7 处写死的 `C:\Users\Administrator\...` 改为**自动探测本机用户目录**。

| 改造点 | 原写法 | 现写法 |
|---|---|---|
| 批5 四条路径 | `"C:\\Users\\Administrator\\AppData\\Local\\Temp"` 等 | `os.path.join(_HOME, "AppData", "Local", "Temp")` 等 |
| 批6 root | `r"C:\Users\Administrator\.workbuddy\projects"` | `os.path.join(_HOME, ".workbuddy", "projects")` |
| 批7 WB | `r"C:\Users\Administrator\.workbuddy"` | `os.path.join(_HOME, ".workbuddy")` |
| 批8 `_C_APPDATA` | `r"C:\Users\Administrator\AppData"` | `os.path.join(_HOME, "AppData")` |

**探测优先级**：`env USERPROFILE` → `env HOMEDRIVE+HOMEPATH` → 向上 walk → 兜底。

### 3.2 新增能力：启动横幅自动暴露 junction 真相

脚本启动时会打印：

```
  -- 本机路径探测（跨机自适应）--
     USERPROFILE = C:\Users\Administrator   [env:USERPROFILE]
     .workbuddy     REPARSE -> E:\workbuddy-data
     AppData        normal dir
  -- note: if .workbuddy is a junction, batch 6/7 free the TARGET drive, not C --
```

**为什么必须加这个**：小九机 `.workbuddy` 是 junction → `E:\workbuddy-data`，所以**批 6/7 清的其实是 E 盘，不是 C 盘**（2026-09-18 曾因此把「C 盘清理」方向搞错）。阿狸咪机是否为 junction **未知**，故脚本不替使用者判断，而是**把事实打印出来由执行者当场核对**。

### 3.3 依赖：**零第三方包**

脚本只用 `os / sys / io / time / json / stat / shutil / argparse / ctypes`，**全是标准库**。阿狸咪机只要有 Python 3 即可运行。

---

## 四、阿狸咪机建任务步骤（可照抄）

### 步骤 0：先确认你的 Python 路径

```bash
# 阿狸咪机需自行确认本机 python 绝对路径，不要照抄小九机的
where python
# 或
python -c "import sys; print(sys.executable)"
```

> 小九机用的是 `C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe`。**阿狸咪机路径大概率不同，必须实测替换。**

### 步骤 1：拉取仓库，获得脚本

```bash
cd <你的 quant-scanner-v8 工作树>
git pull
# 确认文件到位
ls -la docs/ops/scripts/disk_clean_weekly.py
```

### 步骤 2：先干跑（**不要直接 --go**）

```bash
python docs/ops/scripts/disk_clean_weekly.py --batch 8
```

**看三件事**：
1. 横幅里 `USERPROFILE` 是否指向你机上的正确用户目录；
2. `.workbuddy` 那行是 `REPARSE -> ...` 还是 `normal dir`（决定批 6/7 释放哪个盘）；
3. 三盘可用空间数字是否合理。

再全批干跑一遍：

```bash
python docs/ops/scripts/disk_clean_weekly.py --batch all
```

### 步骤 3：确认无工作文件命中后真跑

```bash
python docs/ops/scripts/disk_clean_weekly.py --batch all --go
```

> ⚠️ **参数是单数 `--batch`，不是 `--batches`**（曾踩坑：`unrecognized arguments: --batches 7`）。
> ⚠️ 建议**逐批上**：`--batch 7 --go` → `--batch 8 --go`，便于观察每批效果。

### 步骤 4：建每日定时任务

在**阿狸咪机的 WorkBuddy 自动化**里新建一条，参考小九机 prompt 结构（按你机实际路径替换）：

**任务名建议**：
```
磁盘垃圾每日清理（C/D/E 盘全覆盖·每晚23:00·真删不进回收站·工作文件硬保护）
```

**调度**：每日 23:00

**prompt 建议内容**：

```
执行三盘磁盘垃圾清理，覆盖 C/D/E 三个盘，**严禁触碰工作文件**。

## 执行命令（唯一入口，注意用你本机的 python 绝对路径）
<你的python绝对路径> <你的工作树>/docs/ops/scripts/disk_clean_weekly.py --batch all --go

## 批次含义
- 批 0：脚本行为验证·事故遗留
- 批 1：陈旧临时文件
- 批 2：畸形根目录
- 批 3：安装包缓存与陈旧备份
- 批 4：E:\Temp 陈旧项（预算 3600s）
- 批 5：C 用户临时与缓存
- 批 6：.workbuddy 会话日志（>7天·重要文件硬保护）
- 批 7：.workbuddy 缓存目录（预算 3600s）
- 批 8：C 盘应用缓存（24 个白名单子目录）

单独重跑某批用 `--batch 8 --go`（参数是**单数 `--batch`**）。

## 铁律（不得突破）
1. 绝不触碰工作文件：.doc/.docx/.xls/.xlsx/.ppt/.pptx 等即使超期也永不删
2. 真实删除，不进回收站：同盘回收站不释放空间，脚本用 ctypes DeleteFileW 永久删除
3. 只删文件，绝不删目录本身（保留桶结构）
4. 白名单硬锁 + realpath 前缀二次校验，防目录穿越/软链逃逸
5. 年龄闸门 >7 天：新鲜文件一律不动
6. 跳过 reparse point（junction/symlink）
7. 删除失败（被占用/无权限）跳过并记录，不中断

## 报告格式
跑完后报告：各批释放量 + 三盘 before/after 可用空间 + 失败条数 + 被跳过的办公文档数。
三盘空间必须用 shutil.disk_usage() 的【属性名】.total/.used/.free，绝不按位置解包。
```

### 步骤 5：跑完复核

```bash
# 审计日志（脚本自动落盘在同目录）
cat docs/ops/scripts/_clean_log.json
```

---

## 五、🔴 红线（两台机都必须守）

| # | 红线 | 原因 |
|---|---|---|
| 1 | ❌ **绝不触碰办公文档** | `OFFICE_EXT` 硬保护 `.doc/.docx/.xls/.xlsx/.ppt/.pptx/.wps/.et/.dps` 等，且 >64KB 一律 skip。主人明令「防护的应该是我的办公文档」 |
| 2 | ❌ **绝不删目录本身** | 只删过期文件，保留桶结构服务下次写入 |
| 3 | ❌ **绝不越界** | 目录级白名单 + `realpath` 前缀双校验，防目录穿越 / 软链逃逸 |
| 4 | ❌ **真删不进回收站** | 同盘回收站**不释放空间**，必须 `ctypes DeleteFileW` 永久删除 |
| 5 | ❌ **不碰 `e:\workspace\`** | 用户报告/文档所在，属绝对禁区 |
| 6 | ⚠️ **先干跑再 --go** | 首次在你机上跑，必须先看清单确认无工作文件 |

---

## 六、与 2026-09-18 交接档的关系

`HANDOVER_小九给阿狸咪_2026-09-18_1500_*.md` 第七节第 2 条曾写：

> 「批 7 任务：脚本 `C:\Users\Administrator\.workbuddy\scripts\disk_clean_weekly.py` 属**小九机专属路径**，阿狸咪机上若有同名任务请勿照搬路径，需各自确认本机 `.workbuddy` 目录位置。」

**本档是对该条的升级**：从「提醒你路径不同、请自行确认」→ 升级为「**脚本已改造为跨机自适应版并推入仓，直接 pull 即可用**」，把「自行确认」这一步由脚本自动完成（启动横幅打印）。

---

## 七、复核命令（不写承诺，只给可跑命令）

```bash
# 1) 三盘真实空间（属性名访问，禁位置解包）
python -c "import shutil;[print(d,round(shutil.disk_usage(d).free/2**30,2),'GB free') for d in ['C:\\\\','D:\\\\','E:\\\\']]"

# 2) 枚举关键 junction（判断清理实际落在哪个盘）
python -c "import os;FA=0x400;[print(p, bool(os.stat(p,follow_symlinks=False).st_file_attributes & FA), os.readlink(p)) for p in [r'C:\Users\Administrator\.workbuddy']]"

# 3) 干跑看清单（确认无工作文件命中）
python docs/ops/scripts/disk_clean_weekly.py --batch all

# 4) 查看上次真跑审计日志
python -c "import json;d=json.load(open('docs/ops/scripts/_clean_log.json',encoding='utf-8'));print(d.get('mode'),d.get('started'),d.get('finished'),d.get('total_freed_bytes'))"
```

---

## 八、本档未做的事（明确声明，不含预测）

1. **未在阿狸咪机上实跑** —— 小九机无法访问阿狸咪机文件系统，步骤 2~5 需阿狸咪自行执行。
2. **未替阿狸咪建任务** —— WorkBuddy 自动化是**每台机各自的本地表**，小九机建的 automation 不会出现在阿狸咪机上。
3. **未改小九机现有任务** —— 小九机现有 `0c743285` 任务继续按原样跑（其 prompt 里引用的是 `C:\Users\Administrator\.workbuddy\scripts\...`，**该路径在小九机有效**，无需改动）。
4. **未验证阿狸咪机 `.workbuddy` 是否 junction** —— 需阿狸咪跑步骤 2 的横幅自行确认。

---

## 九、待确认项

| # | 项 | 复核方式 |
|---|---|---|
| 1 | 阿狸咪机 python 绝对路径 | `python -c "import sys;print(sys.executable)"` |
| 2 | 阿狸咪机工作树路径 | 自行确认 quant-scanner-v8 克隆位置 |
| 3 | 阿狸咪机 `.workbuddy` 是否 junction | 跑脚本看横幅 `REPARSE ->` 或 `normal dir` |
| 4 | 阿狸咪机三盘空间基线 | 跑脚本看启动时的三盘数字 |

---

## 十、【2026-09-20 06:3x 追加】批 8 白名单重大修正（**阿狸咪务必重拉脚本**）

> 🔴 如果你在 2026-09-20 之前已 `git pull` 过本档所述脚本，**请重新拉取** ——
> 批 8 的目录白名单在 2026-09-20 做了**修正**，旧版的这一批近乎空转。

### 10.1 修正原因（截至 2026-09-20 06:30 实测）

对批 8 白名单做了「**存在性 + 内容语义**」双重验证后，抓出 **6 条问题路径**：

| 原版条目 | 实测情况 | 处置 |
|---|---|---|
| `Local\kingsoft\wps\cache` | **路径不存在** | 移除 |
| `Local\Tencent` | **路径不存在**（真身在 `Roaming\Tencent`） | 移除 |
| `Local\sogoupdf\cache` | **路径不存在** | 移除 |
| `Local\MeituApp\Cache` | **路径不存在** | 移除 |
| `Local\winToolBox\cache` | **路径不存在** | 移除 |
| `Roaming\secoresdk\logs` | **路径不存在** | 移除 |

> ⚠️ **`os.path.isdir()`=False 时脚本是 `continue`（静默跳过）** —— 写错的路径**不会报错**，
> 只是永远什么都不做。表现为「清理跑了但空间没动」，极难发现。
> **「没报错」≠「生效了」**，这是本类任务最容易踩的坑。

另有一条**半正确但危险**的：

| 原版条目 | 实测情况 | 处置 |
|---|---|---|
| `Local\sogoupdf\fastocrx` | 实际 **95% 是 exe/dll**（opencv + `.mnn` OCR 模型） | **移除** |

`fastocrx` 名字看起来像缓存目录，实际是**搜狗PDF 的 OCR 程序组件**。
若按原计划删除，会让搜狗PDF 的 OCR 功能失效。**已移除**，同理移除 `ktool_update`（33% exe/dll）。

### 10.2 修正后的白名单规模

- 旧版：24 条（含 6 条无效/危险）
- **新版：30 条**（全部通过「存在性 + 零 exe/dll」验证）

新增的有效条目：`npm-cache\_npx`（实测 1.11GB，旧版漏掉的大头）、
Chrome / Edge 的 `Code Cache`·`GPUCache`·`ShaderCache`、`tencent\beacon`、若干日志目录。

### 10.3 修正版在小九机的实测效果（截至 2026-09-20 06:28）

| 项 | 实测值 |
|---|---|
| 干跑命中 | **1.63 GB / 28575 条** |
| 真删后 C 盘 | 27.39 GB → **29.01 GB**（净增 **1.62 GB**） |
| 耗时 | 17 秒 |
| 失败 | 3 条 |
| 跳过办公文档 | **0 条** |
| 复核口径 | Windows 原生 `Get-Volume`：total=100.00 / used=70.99 / **free=29.01** GB |
| 回收站回流检查 | C 盘回收站 **0 项 0 MB**（确认 `DeleteFileW` 真删未回流） |

### 10.4 阿狸咪在你的机上该做什么（可跑命令，不含预测）

```bash
# 1) 重拉（拿到修正版脚本）
git pull

# 2) 确认新白名单已就位（应输出 30）
python -c "import ast;t=open('docs/ops/scripts/disk_clean_weekly.py',encoding='utf-8').read();t2=ast.parse(t);w=[ast.literal_eval(n.value) for n in ast.walk(t2) if isinstance(n,ast.Assign) and getattr(n.targets[0],'id','')=='_C_CACHE_SUBDIRS'][0];print(len(w))"

# 3) 干跑看你本机的实际命中量
python docs/ops/scripts/disk_clean_weekly.py --batch 8

# 4) 若命中合理且无工作文件 → 真跑
python docs/ops/scripts/disk_clean_weekly.py --batch 8 --go
```

> 🔴 **新增白名单前必须做双重验证**（存在性 + 扩展名分布），否则会重复踩本节的坑：
> ```python
> import ast, os
> wl = ast.literal_eval(...)          # 用 ast 提取，别肉眼读代码
> for sub in wl:
>     root = os.path.join(APPDATA, sub)
>     assert os.path.isdir(root), '路径不存在: ' + sub   # 闸门①
>     # 闸门②：exe/dll/.mnn 占比 > 25% ⇒ 疑似程序本体，拒绝
> ```

### 10.5 本节的事实边界（不含预测）

- ✅ **已做**：白名单修正 + 小九机实跑验证 + 推 main（线上已核验逐字节一致）+ 小九机 automation prompt 同步更新。
- ❌ **未做**：未在阿狸咪机实跑；未替阿狸咪更新她机上的 automation prompt（若她已建任务，需她自行按 10.4 重拉脚本即可，**脚本修正对任务无影响**，因为任务调用的是脚本真身而非内嵌清单）。
- ⚠️ **本节的删除量为小九机实测值，不代表阿狸咪机的量** —— 两机 AppData 内容不同，须各自干跑确认。

