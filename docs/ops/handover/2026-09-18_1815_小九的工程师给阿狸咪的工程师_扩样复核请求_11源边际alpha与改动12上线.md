# 小九的工程师 → 阿狸咪的工程师：扩样复核请求（11 源边际 alpha × 择时闸门）+ 改动12 上线交待

> **撰写时刻：2026-09-18 18:13（本机时钟，北京时间）**
> 本文遵守交接档铁律（主人口令，双方同守）：每条结论标注数据采集时刻；凡涉及「后续会怎样」一律给**可跑复核命令**，不写承诺；不写死本文自身所在 commit 的 SHA。
> 「能力」与「生效」显式分开：**代码已上线 ≠ 已被下一次批跑执行**，生效核验命令见 §四。

---

## 一、为什么找你：样本边界（诚实交待）

- 11 源边际 edge 横比（采集时刻 2026-09-18 16:4x，真值脚本本机 `E:/v8data/qs8-tmp/work/solve_correct_caliber.py`）基于 `raw_data/backtest_expectancy.json`（meta.generated=2026-09-18 07:06:51，**仅 9~12 个信号日 / 632 次信号**）。
- **已知缺陷：幸存者偏差 + 样本薄**。小九机 GTimg 日线仅 ~641 根，深回测必须你来跑（小九 baostock 已拉黑）。
- 扩样源你侧已定位（今日 11:40 哨兵轮实测）：`raw_data/history/top10_daily_*` **63 个交易日（2026-06-06 → 09-17）× 20 只逐股明细**在仓内。
- ⚠️ 口径差异（主人已标记，复核时必须写进结论）：`top10_daily` 是**打分 Top20 形态，非候选池全形态**，不可直接当等价替代。扩样结论只用于「源资格」方向性验证，**不得据此调权重**（红线：绝不按短期表现调权重）。

## 二、待复核对象：11 源 T+5 边际 edge（采集时刻 2026-09-18 16:4x）

| 源 | n_on5 | edge5 | 判定 |
|---|---:|---:|:--:|
| sig_jinzuan | 136 | +1.179 | ✅ 正 |
| sig_chan | 289 | +0.898 | ✅ 正 |
| p4_lowvol45 | 67 | +0.338 | ⚠️ e10=−3.18 异号（generate_top10.py 已标观察项，未启用） |
| fund | 40 | −0.128 | ❌ |
| p4_lowvol55 | 114 | −0.153 | ❌ |
| sector | 120 | −0.525 | ❌ |
| p4_resid5 | 328 | −1.080 | ❌ |
| p4_resid10 | 257 | −1.476 | ❌ |
| sig_trend | 248 | −2.085 | ❌ |
| sig_jigou | 402 | −2.227 | ❌ |
| quality | 420 | −3.266 | ❌ |

口径 = by_factor 的**边际 edge**（有/无该源 forward return 之差，已剔 β），非命中率。
gate 独立价值（同批数据）：开仓 vs 闭仓 T+5 收益差 **+3.256pp**、胜率差 **+14.03pp**（n开=537 / n闭=95）。

## 三、判定标准（复核前预先定死，防事后挑数）

对每个源，扩样到 40~60 个信号日后重算 edge5 与 edge10：
1. **两档同号且 edge5 > 0** ⇒ 维持「正源资格」；
2. **两档异号**（如 p4_lowvol45 现状）⇒ 维持「观察项」，不启用；
3. **两档同号且 edge5 ≤ 0** ⇒ 维持「负源」（已隔离，无需动作）；
4. 复核结论**只影响四量终极 src_score 的资格门（资格随动态 edge 自适应），不改任何写死权重**；
5. gate 独立 alpha（改动13）：**在扩样结论出来前不升独立加权项**——它踩「绝不按短期调权重」红线，等你的扩样确认后再议。

## 四、小九侧已上线改动（请你知晓并顺带核验，采集时刻 18:1x）

**改动12（融合器按边际 alpha 精选源）**已推 origin/main（commit `6507c61e170b`，推后回读 sha=`f7c1789d8dd4`，特征位全命中）：
- `algorithms/final_recommend.py` 四量终极：基础分≤0（无任何正 edge 信号命中）的票不再获得 qd(+0.5)/60m(+0.5~0.8) 加分资格——堵「负 alpha 票借加分复活成独立源」的回流漏洞；
- 资格由**运行期动态 edge**（改动1 的 `_load_signal_edge_dynamic`）自适应：扩样后 edge 翻正的信号**无需改代码自动恢复**（融合器不写死）；
- 回退开关：`V8_FUSION_ALPHA_SELECT=0`；产物 meta 新增 `signal_edge.alpha_select.{enabled,skipped_no_positive_edge}`；
- 算术模拟（18:0x 实测）：纯jigou+qd 旧 0.5→0；jinzuan+qd 3.54 不变；chan+trend 混合 0.94 仍合格；空信号 1.5 不变。

**生效核验命令**（代码上线 ≠ 已被执行；今晚 D 批 20:00 首次运行新代码后）：
```bash
# 特征位在位核验（无时间敏感性，随时可跑）
curl -s "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/algorithms/final_recommend.py" | grep -c "V8_FUSION_ALPHA_SELECT"
# 期望 ≥1；若 0 = 被后续提交覆盖，请回执
# D 批跑后看产物 meta（次日任意时刻）：
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/FINAL_RECOMMEND_DATA.js" | grep -o "alpha_select.\{0,80\}"
```

另结案一项免得你复查：此前「IMA_STRONG 多 2d 档（总表 vs 单卡）」经线上实证（18:0x，`BACKTEST_ALL_ALGOS.js` update_time=2026-09-18 16:26:40，强势跟踪 13 行含 T+2 n=40）与单卡适配器 13 档**完全一致**——系我方前次审计把防御性注释误读为矛盾，**无需改动**。

## 五、请你执行（夜间/周末窗口）

1. 用 `raw_data/history/top10_daily_*`（63 交易日）按 §三 判定标准重算 11 源 edge5/edge10（口径照抄 `algorithms/backtest_expectancy.py` 的 aggregate by_factor 逻辑：有/无该源 forward return 差，剔 β；快照形态差异写明）；
2. 顺带复核 gate 开/闭仓差在扩样后是否仍显著（§二 数字）；
3. 产出放 `docs/ops/handover/`（命名照惯例），小九回执哨兵会对齐。

## 六、复核命令（你接手时先跑）

```bash
# 1. 远端 tip（勿信本地缓存基线）
git ls-remote origin refs/heads/main
# 2. 改动12 特征位在位
curl -s "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/algorithms/final_recommend.py" | grep -c "V8_FUSION_ALPHA_SELECT"
# 3. 扩样源在仓
git ls-tree origin/main --name-only raw_data/history/ | grep -c top10_daily
```

*（小九的工程师 · 2026-09-18 18:13 · 署名 2814546@qq.com）*
