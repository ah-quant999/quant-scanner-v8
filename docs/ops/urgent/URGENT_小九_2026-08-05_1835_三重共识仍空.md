# 🚨 URGENT — 三重共识 stocks 仍为空（2026-08-05 18:35 复查）

## 一、线上实时状态（2026-08-05 18:35 抓取）

- URL: https://ah-quant999.github.io/quant-scanner-v8/data/TRIPLE_CONSENSUS.js
- **`count: 0`，`stocks: []` —— 三重共识严格清单仍为空，阈值放宽**（TOP10≥70 → rank≤10 & score≥max(max_score×0.5,25)）**未产生任何命中**
- `update_time: 2026-08-05 17:57:16`（今天 17:57 生成过）
- `data_time: 2026-08-04 23:36:10`（⚠️ **底层 TOP10 数据时间仍是昨天**）
- `near_miss: 3`（迈瑞医疗/贵州茅台/云铝股份，均差 1~2 步）

## 二、根因分析（两层）

### 根因 1：今天(08-05)算法链未产出新 TOP10 → 三重共识读的是旧数据
- 线上 `data_time = 08-04 23:36:10`，与本地 `raw_data/top10_daily.json` 的 `update_time` 完全一致 → **线上读到的就是 08-04 的旧 top10_daily**
- 本地时间戳佐证：`raw_data/` 中 fetch 类文件（analyst_ratings、capital_flow 等）已更新到 **Aug 5 17:57**，但核心算法产物（`top10_daily.json`、`cockpit_tier_recommend.json`、`gold_pool.json`、`backtest_comprehensive.json`、`candidate.json` 等）全部停在 **Aug 4 23:51**
- 今天 17:57 的推送只是把旧 raw_data 打包成 data/*.js 上传（api_push），**scanner.py → gold_pool → generate_top10.py → gen_cockpit_tier_recommend.py → fetch_fundamental_quality.py 这条核心链今天没有成功产出新数据**

### 根因 2：即便用旧数据，严格三重交集也为空
- 08-04 的 TOP10 前10 与驾驶舱 A 档（9只）交集仅 **1 只：富瀚微(300613)**
- 本地 `out/fundamental_quality.json` 与 `raw_data/fundamental_quality.json` **均不存在** → 本地无法复现线上结果（线上生成环境有该文件，near_miss 中 `in_good_fund:true` 可证）
- 严格共识 = TOP10精选 ∩ 驾驶舱A档 ∩ 基本面A档，三集合交叠过窄，历史上连续为空

## 三、raw_data/triple_track.json 状态

- 文件存在，`update_time: 2026-08-04 22:30:20`（同样停在 08-04）
- `strict_count: 0`，`near_count: 3`（迈瑞医疗/贵州茅台/云铝股份，全 near 无 strict）
- ⚠️ tracked 中 `industry/sectors` 字段为空（迈瑞/茅台行业未映射出来），`catalysts` 仅云铝有「中报预增」
- 回测全部为 0（无样本）

## 四、algorithms/_fill_kcbj_concepts.py 运行结果

- **结论：当前被数据源封死，无法成功运行**
- GHA 工作流 `fill_kcbj_concepts.yml` 为手动 dispatch，无自动 schedule
- 注释明确：东财源(akshare)在 unit 机与 GHA 均不可达(502/RemoteDisconnected)，同花顺无成分股函数
- 现状：`stock_industry_concepts.json` 共 5211 只，**科创板空概念 414 只**（688xxx 开头无 concepts）
- 对三重共识为空**不是根因**（富瀚微等非科创股概念其实有映射），但科创板概念缺失会影响展示层丰富度

## 五、下一步建议（按优先级）

1. **[P0] 核查 08-05 盘后算法链为何没跑/失败**：查看 GHA `v8_algo_run.yml`（本地 cn 自托管 runner）今日运行日志；重点确认 scanner.py、generate_top10.py、fetch_fundamental_quality.py 是否执行/报错。三重共识空的第一根因是「上游数据没更新」，不是阈值本身。
2. **[P0] 补跑今日算法链**：手动触发 `run_algorithms.py`（或重跑 v8_algo_run 工作流），确认 raw_data/top10_daily.json 的 update_time 刷新到 08-05 后，再重新生成并推送 TRIPLE_CONSENSUS.js。
3. **[P1] 阈值放宽幅度评估**：现 top_threshold = max(max_score×0.5, 25)，08-04 max_score=39.2 → 阈值 25，TOP10 前10 全部达标，**阈值已不再卡人**；瓶颈在「TOP10 ∩ A档 ∩ 基本面A档」三集合并集过窄。建议：
   - 观察 2-3 个交易日真实数据再定；若持续为 0，考虑将 A 档改为「A档或B档」（与 near_miss 口径一致），或把基本面从「A档」放宽为「A/B档」。
4. **[P1] 补齐 fundamental_quality 产出链路**：确认 fetch_fundamental_quality.py 在云端生成 out/fundamental_quality.json 并推送到线上环境；本地 out/ 目录几乎为空（仅 lhb_seats.json），排查 out/ 是否被清理。
5. **[P2] 概念补全**：等东财源恢复后手动 dispatch `fill_kcbj_concepts.yml`（--apply --max 0），可补 414 只科创板空概念。

---
生成时间: 2026-08-05 18:35 | 来源: 自动化任务「小九-v8今晚18:35复查三重共识」
