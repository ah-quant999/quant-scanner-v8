# HANDOVER｜阿狸咪的股票专家 → 小九｜2026-09-11 21:39｜CRDS卡两处同源大盘判断删一留一+徽章标签纠错(已上线)

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

CRDS 卡两处「大盘判断」经查**同源同口径**（都取自 `CRDS_CARD_DATA.market_context`），按主人令留信息完备的「大盘判断卡」、删口语化重述的待机黄条；顺带纠正留存卡的徽章标签错配（`mc.today_pct` 是综合强度却被标成「上证」）。已上线 `d20de4264367`。

---

## 正文

### 1. 主人令与排查结论

主人令：「这两个判断我到底看谁的？哪个最有效就留哪个，另一个删除」

| | ① 待机黄条 | ② 大盘判断卡 |
|---|---|---|
| DOM | `#stcrdsStandby` | `#stcrdsMarketContext` |
| 显示条件 | 仅 `cond2_list.length === 0` | 有 `summary` 即显示 |
| 数据源 | `mc.validity` | `mc.validity` + `mc.today_pct` + `mc.summary` |
| 呈现 | 口语化一句话（按档位 5 分支） | 档位徽章 + 今日涨跌幅 + 完整摘要（综合强度/上证/涨跌家数） |
| 处置 | **删除** | **保留（权威位）** |

**关键事实：不是两套口径。** 两条都出自 `algorithms/calc_crds.py` 的 `get_market_context()` 产出的同一个对象。

**留 ② 的三条依据**：
1. 信息完备（① 是它的口语化子集）；
2. `index.html` 源码注释（2026-08-30 主人令）已钦定「用单一权威源 `mc.validity` 在大盘判断卡内（`stcrdsMarketContext`）直接呈现」——当时删的就是与它重复的弱市兑现卡，① 属同类冗余；
3. ① 的文案是硬编码分支，② 的 `summary` 由算法实时生成（不会陈旧）。

### 2. 变更清单（2 个 commit）

| commit | 文件 | 内容 |
|---|---|---|
| `76c80d96043c` | `index.html` | 删 ① 骨架 1 行 + JS 渲染块 13 行；徽章标签纠错 1 处 |
| `76c80d96043c` | `logic.html` | 删内嵌 JS 副本 13 行；徽章标签纠错 1 处 |
| `d20de4264367` | `logic.html` | 文档「待机提示文案」表 → 「无信号时怎么读」说明（上笔漏改，补推） |

**顺带纠错（留 ② 必须修）**：徽章右侧 `'"上证 "+mc.today_pct'` → `'"综合强度 "+mc.today_pct'`
根因：`mc.today_pct` = `_composite_market_score()` 综合强度，真上证在 `mc.sh_pct`；原写法致同行自相矛盾（示例：`上证 -1.01%` 与 `（上证-1.18%）` 同屏）。

**随删除消亡的过时文案**：① 的 `else` 分支残留「近期上证无单日跌超 **1.5%**」——阈值实为 **-0.8%** 且已改综合口径（09-11 口径合一），无需另行修。

### 3. 审计（三件套①）

- inline `<script>` `new Function`：index 24 段 / logic 6 段 → 语法 **0 错误**
- HTML 标签开闭 diff **逐项与原始对比一致**（原文件 JS 字符串噪声不变）
- `difflib` 逐行差异 = 预期 3 段，无其它改动
- 行尾：index.html 纯 LF(0 CRLF) / logic.html CRLF(8651)，与原始一致
- `data/*.js` 唯一引用：index 106 / logic 95（CI 区间 90~110）
- **CI 门禁排查（全部不触发）**：`pre_deploy_audit` 四项 = py_compile / index.html inline `new Function` / data 数量 / `align_logic_ops`（只校 cron 与文档对齐）；`audit_empty_cards.js`、`smoke_index.js` 只引用数据变量 `window.CRDS_CARD_DATA`，**不引用任何 DOM id**

### 4. 对齐云端与双机（三件套②）

- `.github/workflows/**` 对 `stcrdsStandby` / `crdsStandby` **零引用** → 不涉 cron / 派发源 / 双机任务
- 无算法链、无数据产出改动 → 小九侧 `git pull` 即得，**无需同步动作**

### 5. 页面写入（三件套③a/b）

- `index.html`：删代码 + 纠错标签
- `logic.html`：删代码 + 纠错标签 + **文档段同步**

### 6. 上线验收

远端 `d20de4264367` 实测：

| 项 | index.html | logic.html |
|---|---|---|
| `stcrdsStandby` | 0 ✅ | 1（仅文档说明性引用）✅ |
| `'上证 '+` 标签 | 0 ✅ | 0 ✅ |
| `'综合强度 '+` 标签 | 1 ✅ | 1 ✅ |
| `待机提示文案` | 0 ✅ | 0 ✅ |
| `无信号时怎么读` | — | 1 ✅ |
| 行尾 | LF ✅ | CRLF 8651 ✅ |

### 7. 提醒小九

1. **本机 529 UU pile 仍在**（`data/*.js` / `raw_data/kline_cache/*.json` 等运行产物与远端 build 戳双向冲突）。我未擅自解冲突、未 `git stash drop`，备份在 `stash@{0}`，请您 review 后决定取舍。
2. **推送期间 main 被 CI build 漂移**（`33fef5a06 v8 build: 2026-09-11 21:32`）→ 推送脚本靠「外层循环重取 base + 幂等重打补丁」成功。**本机到 api.github.com 较慢**（单次 push 约 3 分钟，首次 raw 读取超时一次）。
3. **经验**：改前端两页时，若 `logic.html` 也内嵌同一份 JS 副本（如 `renderV8Crds`），**必须同时改**——即使该副本在逻辑页是死代码（无 `stcrds*` 骨架），留着会在将来"复活"。
4. **卡位置终态**：CRDS 卡内「大盘判断」**只此一处**。后续若再加环境类提示，**先删旧再建新**。

---
**署名**：阿狸咪的股票专家（2026-09-11 夜·周五交接班）
