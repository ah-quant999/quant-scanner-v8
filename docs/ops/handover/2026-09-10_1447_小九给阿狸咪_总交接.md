# 交接单 · 小九 → 阿狸咪（2026-09-10 14:47 小九离线前）

> 小九 15:00 离线，17:25 前回接龙虎榜，18:00 算法链须在线。本单为离线前**最终总交接**。

## 一、今日已上线（全部 rebase + push，无 force，均在 origin/main）

| commit | 内容 | 状态 |
|---|---|---|
| `fbc19d623` | 🌅 盘前全局速览卡：置于「暂未上架·宏观观测」顶部（admin 实验卡，普通用户无感） | ✅ |
| `4c96f0af1` | 🛠 主力净额分时守卫放宽：`intraday_snapshot.py` 口径只卡「今天」+ 陈旧 40→90min | ✅ |
| `dc11a2892` | 🛠 双管齐下治本：① `soft_9` 重试 3→5 / sleep 10→6；② 新增 `v8_intraday_snapshot.yml` **独立高频快照 job**（每10min盘中触发，独立并发组，解耦 fetch 排队/取消风暴，准时写「目标档期时刻」点） | ✅ |
| `fa3b62c74` | 🛠 P0 防回退：`update_v8.py` 纯盘后产物非 `post_close` 构建跳过重写（根治盘中把今日真推荐洗成旧版） | ✅ |

> 校验：`git merge-base --is-ancestor <commit> origin/main` 四个均 ✅；HEAD=origin/main=683450119。

## 二、数据实时性（用实时说话，14:47 实测）

- 主力净额卡 `SECTOR_FUND_FLOW_INTRADAY` 快照分布：`10:12, 11:46, 12:17, 12:38, 13:56, 14:23`（共6点）。
- **下午档已正常出点**（13:56/14:23）→ 双管齐下修复见效。
- **上午仅 10:12 一点是历史遗留**（上午云端 intraday 抓取档期错位，单靠守卫救不了「档期错位」）—— 明早起 `v8_intraday_snapshot.yml` 独立 job 每10min准时写点，应能填补。
- 云端 14:46 仍在推数据（watchdog ?v 正则 fix），数据管线健康。

## 三、阿狸咪离线期（15:00–17:20）须盯 / 办

1. **盯明早主力净额卡**：15 日 09:30 后看快照是否每 10–20min 一点。若仍稀疏 → 查 `v8_intraday_snapshot.yml` 的 Actions run（本机 GitHub HTTPS 不通，需阿狸咪在云端 Actions 面板查）。
2. **小九 17:25 前回接龙虎榜**、**18:00 算法链须在线**——若小九未及时回，阿狸咪按双机铁律补位。
3. **勿手动发长时 in_progress 的 cn_fetch run**（触发去重压掉后续槽位）。

## 四、待主人拍板项（C2/C3/C6/C7，未经授权勿动）

- **C2**：小九 15:15–17:20 离线 → 16:40 A 档（盘后算法链）须覆盖，空窗拆「15:00–16:20 可下线 + 16:40–17:25 必须在线」或云端 fallback。
- **C3**：`v8_algo_cloud.yml` 在 lemoncat-cn offline 时无 fallback。
- **C6**：某自动化条件写死日期 `2026-09-09` 永不触发。
- **C7**：21:30 终闸强制补跑当前 PAUSED 任务，与「阿狸咪任务须 PAUSED」铁律冲突。
> 以上四项主人未明令，保持现状，等拍板。

## 五、铁律重申（推送前必读）

- ❌ **严禁** `data/*.js` 本地直推 → 一律走云端 build 管线。
- ❌ **严禁** `git push --force` → 推送前必须 `git rebase origin/main`。
- ✅ `index.html` / `*.py` / `*.yml` 改动后过三件套（语法 / DOM 标签平衡 / 引用复核）再推。
- ✅ 双机各自本地文件可能打架（如 maharo insights 旧案）→ 优先读仓库已提交版，勿被本机旧文件覆盖。

## 六、workflow 文件说明（无丢失风险）

- `v8_cn_fetch_intraday_lemoncat.yml` / `v8_cn_fetch_watchdog.yml` 当前与 `origin/main` **完全一致**（无未提交改动），仍含 `sleep 600`。
- 阿狸咪远端分支 `fix/lemoncat-sleep600`（实时轮询替代 sleep600）**尚未合 main**；小九未动这两文件，他决定何时合并。
- 小九本机无这两文件的丢失/未提交改动。

## 七、本地未跟踪产物（不入库，仅本地参考）

仓库根目录 `HANDOVER_*.md`、`preview_premarket.html`、`INTRADAY_0940_STATUS_20260910.md`、`PRE_OPEN_AUDIT_20260910.md`、`backup/` —— 均为小九本地诊断/沟通产物，非主站文件，请勿提交。

---
**小九签字**：ah-quant999 · 2026-09-10 14:47（离线）
**阿狸咪确认回执**：________________（pull 后填）
