# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-19 20:23｜强势突破全站删除+缠论买点账本上线

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

2026-09-19 主人令两件套：①「强势突破」**全站删除**（胜率 14.4% 破红线）——前端死函数/数据注入/算法/数据文件/调度/回测卡/健康检查项一并移除（`scripts/gen_strong_breakout.py` **保留**：它是动量共识 V2 的水源，与强势突破卡解耦）；②「缠论买点」**信号账本上线**（55 条，信号日 09-16/09-17），累积期不评星不上卡。

---

## 正文

### 一、强势突破删除清单（本次 commit）

**前端 index.html**
- 删 `data/STRONG_BREAKOUT.js` / `data/STRONG_BREAKOUT_BACKTEST.js` 两处 `<script>` 注入
- 删死函数 `window.__renderStrongBreakout` 整段（2026-09-16 下架后容器 DOM=0，早已不被调度）
- 删 `_renderPostReady` 中对它的 try 调度
- 删 `sigColor` 三元链中 `signal_type==='强势突破'` 分支

**logic.html**
- 执行矩阵删「7 强势突破」行（动量共识改 7 号并登记删除原因）；「8 源」改「7 源」
- 算法对比表删「6 ⚡强势突破」行（动量共识改 6 号）
- 生命周期卡关系表：强势突破行改「已删除」登记；标题/关系文案/编号约定/常见误读同步改写
- 融合来源表、数据流文本、数据流清单同步移除

**Python 侧**
- 删 `algorithms/strong_breakout.py`（B 批选股生成器本体）
- `run_algorithms.py`（根 + algorithms/ 两份）：ORDER / B 批 STAGE / 选股门控白名单 三处条目移除
- `final_recommend.py`：第 8.6 节「强势突破共振」整节移除（`load_js("STRONG_BREAKOUT.js")` 不复存在）
- `backtest_life_cards.py`：`run_strong_breakout()` 回测卡移除（仅剩 ima 卡）
- `update_v8.py`：`strong_breakout_backtest.json` 映射与 post_close 类别移除
- `v8_health_check.py`：STRONG_BREAKOUT_BACKTEST 健康检查项移除（否则删除后永久红灯）
- `factor_lab_backtest.py`：`from strong_breakout import _fetch_kline` → **内联同款实现**（gtimg 双域名主源→新浪兜底，逐行一致），因子实验室取数不断链

**数据文件删除**
- `data/STRONG_BREAKOUT.js`、`data/STRONG_BREAKOUT_BACKTEST.js`
- `raw_data/strong_breakout.json`、`raw_data/strong_breakout_backtest.json`、`raw_data/strong_breakout_2026*.json`（11 个日档）

**明确保留（防误删）**
- `scripts/gen_strong_breakout.py`：负责**动量共识 V2**（STOCK_MOMENTUM_STATE/V2，P0-2 监控）与 45 天滚动账本 `raw_data/strong_breakout_history.json`（该账本现无消费方，属残留，后续可评估清理）
- index.html 中的历史注释 trail（2026-09-16 下架审计记录）保留

### 二、缠论买点账本上线

- 新增 `v8/fetch_chan_buy_point.py`（幂等解析器：知识库全文 md → 结构化信号账本，不联网）
- 新增 `raw_data/chan_buy_point_full.md`（两期全文合并快照）/ `chan_buy_point_full.json` / `chan_buy_point_signals.json`
- 实测：**55 条信号**（09-16 38 只 / 09-17 17 只），与 2026-09-19 下午可行性结论一致
- 评星口径：K 线覆盖仅 15/54、信号日后样本 ≤2 根 ⇒ **不登记 `__STAR_PROFILE_OF`、不上前端卡**；随每日「盘后选股 PDF」自然累积，样本 ≥30 天再回测评星
- logic.html 对齐矩阵已登记该通道（本机取全文 → 账本 → 云端复用解析器）

### 三、门禁与验证

- index.html 28 段 inline script `new Function` 全量编译：0 错；logic.html 6 段：0 错
- 全部改动 .py `py_compile` 通过（7 个文件）
- 运行时残留 grep：`window.STRONG_BREAKOUT` / `from strong_breakout import` / `run_strong_breakout` / 健康检查项 全部归零（仅剩历史注释 trail）
- logic.html 保持 CRLF 行尾不变（判据 138）

### 四、后续注意（小九侧）

1. 下一个交易日 B 批起**不再跑** strong_breakout.py；若运维面板出现 STRONG_BREAKOUT 相关红灯即为旧缓存，硬刷新即可
2. `raw_data/strong_breakout_history.json` 仍由 gen_strong_breakout（动量）滚动写入，暂留；是否清理由主人拍板
3. 缠论账本每日更新入口：本机跑 `python v8/fetch_chan_buy_point.py --from-dir <全文目录>`（全文由知识库连接器取回落 _holdq）
