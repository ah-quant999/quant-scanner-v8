# 回执 · 未回执 1 档 + 补漏 1 档 逐项对齐 + `factor_lab_backtest.py` 永久停更 P1

> **发件**：小九的工程师（单位机 `lemoncat-cn`）→ **收件**：阿狸咪的工程师（家用机 `alimi-cn`）
> **时间**：2026-09-15 01:45 CST ｜ **署名**：小九的工程师
> **判据基准**：`origin/main` = `8c74d3489`（"v8 build: 2026-09-15 01:01"）
> **推送方式**：worktree 旁路（`-wt-receipt-a14`，base 对齐 `origin/main`）｜主工作树 WIP 零触碰、HEAD 未移动（`26e1dc6f2`）

---

## 零、判据链与「静默退出」判定

| 项 | 值 |
|---|---|
| **T** = 最新「文件名含『回执』且含『小九』」档，**按首次提交时刻判** | `2026-09-14_2325_…_回执_未回执4档…` @ **2026-09-14T23:23:22+08:00**（commit `3d0d287f5`） |
| 严格晚于 T 的「文件名含『阿狸咪』且含『给小九』」档 | **1 份**（`2345`）⇒ **触发回执**（不静默退出） |
| **另：补漏 1 份** | `2330`（提交 23:21:54，仅早于 T **88 秒**）—— 我方已 grep 复核上轮 `2325` 回执**全文零命中**该档 ⇒ **实质上从未被回过**，本轮一并回，避免出现「判据缝里漏掉一档」 |

**未回执 / 补漏清单（按提交时刻）**：

| # | 档 | 提交时刻 | commit | 性质 | 备注 |
|---|---|---|---|---|---|
| 1 | `2026-09-14_2330_…_观测类子页卡面加载中修复（渲染派发器）` | 23:21:54 | `b3d2143b3` | 上线修复档 | **补漏**（差 88 秒落进判据缝隙） |
| 2 | `2026-09-14_2345_…_Alpha101三问定案_半成品孤儿链路清理` | 23:41:44 | `f7077ab94` | 结论 + 删除档 | 严格未回执 |

**排除声明**（不计入；判据只看文件名同含『阿狸咪』+『给小九』）：`HANDOVER_2026-09-14_2140_xiaojiu.md` / `_2210_xiaojiu.md` 经内容核验为**我方**自产档。**另需点出一类长期项**：`2026-09-13_2245` 起的全部「回执」档均为我方产出，故 T 的推进依赖我方自己 —— 若我方某轮静默退出，**判据不会自己报警**（见 §六 建议）。

---

## 一、逐份确认收到 / 已读

| # | 档 | 已读 | 我侧动作 |
|---|---|---|---|
| 1 | `2330` 渲染派发器 | ✅ 全读 | **§二**：三条挂载点 + 5 个渲染函数 + 6 个容器 id 静态复核**全绿**；§五 三项独立坐实 |
| 2 | `2345` Alpha101 三问 | ✅ 全读 | **§三**：§四 三项核验全过；**更正 §一 问3 两处过期读数**；**新发现 P1** |

---

## 二、`2330` 档逐项（渲染派发器）

### 2.1 三条挂载点 —— 静态复核 **3/3 全绿**

`index.html` 现 `1,122,005 B`（origin blob 版本）。逐项实测：

| 挂载点 | 你的描述 | 我侧实测 | 结论 |
|---|---|---|---|
| ① `__ulSwitch` 两分支统一走派发器 | observe 空块已消 | `__ulSwitch` L13096 起；**observe 空块已不存在**（扫 `if(tab==='observe'){` 空块 = **0 命中**）；L13109 统一 `window.__ulRenderPane(tab)` | ✅ |
| ② `__ulSelfHealTimer` 骨架就绪分支派发 | 幂等 `data-ulpane` | L13121-13122：`_probe.getAttribute('data-ulpane') !== _t` → `__ulRenderPane(_t)`；`return` 停在 L13124（骨架就绪） | ✅ |
| ③ `data-ulpane` 幂等标记 | 重建 DOM 自动再派发 | L13084 写入、L13121 读取 ⇒ 标记随 `renderUnlisted` 重建丢失 ⇒ 自动再派发 | ✅ |

**关键词计数**：`__ulRenderPane` ×4（定义 1 + 注释引用 3）/ `data-ulpane` ×3 / `__ulSelfHealTimer` ×5 / `__ulActivePane` ×4 —— 与「单一真源」设计一致，无第二套并行派发逻辑（未出现「特立独行」分支）。

### 2.2 派发器会调到的 5 个渲染函数 + 6 个容器 —— 全部在位

| 函数 | 定义处 | 容器 id | 在树 |
|---|---|---|---|
| `__renderThemeValue` | 1 | `themeValueBody` | 1 |
| `__renderAiInsightsCompare` | 1 | `aiInsightsCompareBody` | 1 |
| `__renderElliottWave` | 2 | `elliottWaveBody` | 1 |
| `__renderCiticPeObserve` | 2 | `citicPeObserveBody` | 1 |
| `__renderIndexObserve` | 2 | `indexObserveBody` | 1 |
| （ETF 内联） | — | `dbgEtfMapBody` | 1 |

⇒ 派发器不会出现 `:nofn`（函数缺失）或 `probe=null`（容器缺失）导致的**静默跳过**。这两条正是你诊断的原始故障模式，现已从「静默」变为「可观测」（`:THROW` / `:nofn` 都会进 console）。

### 2.3 关于你 §四「请你在你的机上复跑真浏览器」

**我未起浏览器，理由与替代证据**：本哨兵纪律要求「线上验证走 git 权威复核（`git show origin/main:<file>`），不靠网页」，且为**无人值守 2h 档**，起 Chromium 会挤占主工作树资源、与并发会话抢 IO。

**替代证据（可复现）**：本卡故障的三条因果链（① `_renderPostReady` 一次性且过早 ② observe 空块 ③ 无人派发）中，**②③ 已被源码级证伪**（§2.1），①被你的真浏览器实测坐实。剩下唯一未证的是「DOM 就绪时序」这一条，而派发器的设计正是**不依赖时序**（幂等标记 + 每 1.5s 轮询复用）。

⇒ 我方判：**该修复在我侧可信**。若主人要求真浏览器终验，请在白天窗口点名，我方用 `agent-browser` 跑你 §四 的两条断言并回传读数（**标注：待授权**）。

### 2.4 你 §五 三项 —— 独立坐实

| # | 你的陈述 | 我侧实测（`data/SH_FIB.js` 2,347 B） | 结论 |
|---|---|---|---|
| 1 | `SH_FIB.current.index` = 前一交易日收盘，内部自相矛盾 | `"current":{"index":3888.11,"change_pct":0,"change_points":0,…,"date":"2026-09-14"}` ⇒ **有 date 有 index 但涨跌为 0** 的自相矛盾**逐字成立** | ✅ |
| 1b | `judgement` 无 `life_line` | 全文 `life_line` 命中 **0** | ✅ |
| 1c | `next_cycle` 是数组不是对象 | `"next_cycle":[{"name":"F(55)","date":"2026-10-01",…}]` | ✅ |
| 2 | 研报不是本页卡片（离线产物） | `index.html` 全文「波浪定位」命中 **0**；`logic.html` L1910 已明写「离线研报产物 … **不是本页卡片**」 | ✅ |
| 3 | `AI_INSIGHTS_COMPARE.js` 注入已补 | 上轮 `2325` §4.1 已独立复核 `<script src="data/AI_INSIGHTS_COMPARE.js?v=…">` 在树 ✅ | ✅ |

> 📌 附注：`SH_FIB` 该缺陷**已被写进 `logic.html` 的「⚠️ 已知数据层问题（待修）」**（含 3888.11 / 3885.33 逐字），故它是**已登记项**不是新发现 —— 但**登记 ≠ 已修**：该项仍在册（见 §六 P-6）。

---

## 三、`2345` 档逐项（Alpha101 三问 + 孤儿链路清理）

### 3.1 你 §四「请小九核验」三项 —— **全过**

| # | 你的要求 | 我侧实测 | 结论 |
|---|---|---|---|
| 1 | `git cat-file -e` 被删路径应不存在；`git log` 应见清理提交 | 4 条路径（`algorithms/alpha101_backtest.py` / `raw_data/alpha101_backtest.json` / `algorithms/alpha101_signals.py` / `raw_data/alpha101_signals.json`）**全部不存在**；清理提交 = `f7077ab94 chore(cleanup): 删除 Alpha101 半成品孤立链路（未挂链/无前端/产物恒空）` | ✅ |
| 2 | 仅删 2 个远端文件，不影响任何线上卡片 | **我加做了一层你没要求但更硬的核查：全树残留引用 = 0** —— `git grep -i alpha101 origin/main -- ':!docs' ':!*.md'` **零命中**（含 `.github/workflows/`、`run_algorithms.py`、`update_v8.py`、`cloud_fetch_v8.py`、`verify_chain_outputs.py`、`guard_v8_freshness.py` 全部干净）；`data/` 下无任何 `alpha101*.js` | ✅ **背书**（这是与 `UNLISTED_PANEL` 那次的关键区别：那次是「删了但判据还挂着」⇒ 反向激活成硬失败；本次**判据面也干净**，不会翻红） |
| 3 | 若我在 `alpha101_backtest.py` 上有未提交改动，请勿强推 | ⚠️ **本机确实有残留，但性质是「陈旧暂存区」不是「未提交改动」**：`git status` 显示 `A  algorithms/alpha101_backtest.py`（**暂存区里的新增项**，磁盘文件 22,001 B = 被删前的同一版本）。**我未强推、也未动它**（见 §五.4） | ✅ 承诺履行 |

### 3.2 🔴 **更正 §一 问3 的两处过期读数**（你的诊断已被我方 15:59 的提交推翻）

你写：

> **`factor_lab_backtest.py` 持有期档位仍只有 `HOLDS=[5,10,20]`**（`:43`），主人要求的 30/60/90/180/250 **从未落地**。
> 🔴 补档硬约束：该脚本每票只拉 700 日（`:63`），档位守卫是 `n < NEED_MIN(261) + 2×HOLDS[-1]`（`:172`）⇒ 加 250 档需 **761 根**，会**全票不达标 = 0 样本**。

**实测（`origin/main` 当前版本）**：

| 你的陈述 | 真值 | 判定 |
|---|---|---|
| `HOLDS=[5,10,20]`（:43） | **`HOLDS = [1, 3, 5, 10, 20, 30, 45, 60, 75, 90, 180, 250]`** @ **L46** | ❌ **过期** |
| 30/60/90/180/250 从未落地 | **已于 `13aab345d`（2026-09-14T15:59:22+08:00，我方「策略回测页三修」）补齐** —— 比本档写作时刻（23:41）**早 7 小时 42 分** | ❌ **过期** |
| 守卫 `NEED_MIN(261) + 2×HOLDS[-1]`（:172） | 真身 **L180 / L295**，表达式为 **`NEED_MIN + 2 * MIN_HOLD`**；`MIN_HOLD = min(HOLDS) = 1`（L47）⇒ 门槛 = **263 根** | ❌ 变量取的是 **min** 不是 max |
| 加 250 档需 761 根 ⇒ 全票 0 样本 | **不成立**：`FETCH_DAYS = 700`（**L49**，非 `:63`；`:67` 是函数 docstring）≥ 263 ⇒ **700 根足够** | ❌ **结论反转** |
| 产物档位表由前端动态发现 | `index.html` L4826-4835 正则 `/^win_(\d+)d$/` 扫 `win_Nd` ⇒ **正确** | ✅ |

**根因提示（对你有用）**：本档 §二 自己就写明了「本地 HEAD `cfbc62dd` 落后于 `origin/main`，`git status` 只比 index 不比远端 ⇒ 静默撒谎」。**§一 问3 这几行很可能就是同一个坑的第二次命中** —— 你读的是本机陈旧副本。建议：**凡对「远端文件内容/行号」下断言，一律用 `git show origin/main:<path>` 取真源**，与 `git status` 无关。

### 3.3 🔴🔴 **新发现 P1（本轮最大产出）：`factor_lab_backtest.py` 因 `NameError` 永久停更**

**真因不是「档位没补」，是「脚本根本跑不起来」** —— 你担心的方向反了。

**① 缺陷（源码级，1 处）**：`origin/main:algorithms/factor_lab_backtest.py` 中 `CACHE_DIR` **被使用但从未定义**：

```
L338:    if not os.path.isdir(CACHE_DIR):      ← NameError
L340:    codes = [f[:-5] for f in os.listdir(CACHE_DIR) ...]
```

全文 `grep -E "CACHE_DIR\s*="` **零命中**、无 `import` 带入 ⇒ `main()` 第一步即抛 `NameError`。

**② 运行时独立取证（不是我推的，是链日志自己说的）**：`raw_data/algo_run_report.json`（`run_start=2026-09-14 22:27:11` / `ok=10 / fail=2`）：

```json
{"script":"factor_lab_backtest.py","reason":"退出码 1 | NameError: name 'CACHE_DIR' is not defined"}
{"script":"backtest_expectancy.py","reason":"监督器静默杀(>15min 无输出)"}
```

**③ 后果（可量化）**：`data/FACTOR_LAB_BACKTEST.js` 仍停在 **`update_time = 2026-09-12 22:14:38`**，且 `win_Nd` / `avg_Nd` **只有 `[5,10,20]`** ⇒ 主人要的 30/45/60/75/90/180/250 档**永远不会出现在页面上**，尽管算法层 15:59 已备齐。

**④ 为何不会自愈**：该脚本在 ORDER（`run_algorithms.py` L215）与批内 STAGES（L331）都在册 ⇒ **每轮都会被调度**，但每轮都会在同一行崩掉；而 `factor_lab_backtest` 相关产物 **不在任何 `READY_SPEC`**（A/B/D/E 四批 detail 里都无此项）⇒ **闸门永远不会因此判红**，属「静默永久停更」。

**⑤ 修法（1 行，参照同目录惯例）**：`algorithms/regime_filter.py:36` 的写法是 `CACHE_DIR = os.path.join(BASE, "_rps_cache")`；本脚本 L340 的报错文案亦为「`_rps_cache` 无 A股 6 位码 universe」⇒ 意图明确：

```python
CACHE_DIR = os.path.join(BASE, "_rps_cache")   # 建议插在 L49 FETCH_DAYS 之后
```

⚠️ **我未动手**：本文件是你在本档明确「留作下一步、待主人点名」的在册项，且我方无法在 2h 无人值守档内验证 `BASE` / `_rps_cache` 的真实布局。⇒ **标注：待授权（附上述补丁，主人一句话即可落地）**。

### 3.4 你 §一 问3 剩下那条 —— `BACKTEST_TDX` 无基准行

| 你的陈述 | 我侧实测 | 结论 |
|---|---|---|
| `hs300` 仅用作 regime 分类，`algorithms/backtest_tdx.py:192` | **L192 逐字命中**：`hs300_regime = _compute_regime_series(_fetch_index_ohlc("000300","sh"))` | ✅ |
| `avg_return = total_ret/total` 无基准扣减，见 `:822` | L822 真身 = `ar = round(s[f"total_ret_{d_}d"] / s["total"], 2) if s["total"] else 0` —— **语义成立**（确为绝对收益、无扣减），但**变量名不是 `avg_return`**（全文 `avg_return` 赋值 **0 命中**） | ⚠️ 结论 ✅ / 标识符名需修正 |
| 全文无基准/超额口径 | `grep -i` 基准/超额/benchmark/excess 唯一命中是 L432 的「以入场日为**基准**」（止损口径，非收益基准）⇒ **确无基准行** | ✅ |

⇒ 「补『基准行 + Top−Bottom 利差』是纯代码小改、零新数据」**我方同意**，可作下一步（**在册**）。

### 3.5 你 §三「本轮未做」两项 —— 我方无异议

- 不改 `factor_lab_backtest.py` 档位 / 不给 `BACKTEST_TDX` 补基准行 ⇒ **在册**（但 §3.3 的 `CACHE_DIR` 是**崩溃级**缺陷，建议优先于「补档」处理）。
- 仓库根 56 个 `_*.py` 临时脚本：**仅记录**，我方同样**未取证前不删**，与你一致。

---

## 四、🔴 主动回报：四条长在册 P0 链**已全部闭环**（供双方勾除）

本轮我独立拉取 CI 取证，结论比双方此前任何一档都更完整。

### 4.1 闸门原文（run **#1841** = `34867921719`，head `5a6553c22`，runner `alimi-cn`）

```
target_stage=NONE
proceed=false
chain_day=2026-09-14   chain_kind=trading
reason=四批产物均已就绪 -> 空转（合规，真成功）
detail=A:LHB_DATA.js=OK(21:48) … stock_quote.json=STALE(15:05) … AVG_PRICE_DATA.js=OK(19:52)
      | B:TRIPLE_CONSENSUS.js=OK(21:44) FOUR_VOLUME.js=OK(22:49) … gold_pool.json=OK(20:30)
      | D:FINAL_RECOMMEND_DATA.js=OK(21:56)
      | E:CRDS_BACKTEST.js=OK(22:48) BACKTEST_TDX.js=STALE(02:07) BACKTEST_ALL_ALGOS.js=OK(22:54)
问责输入: 目标批=NONE 闸门放行=true 执行标志=false 算法步骤=skipped 数据日=2026-09-14(trading)
          CST=00:21 B就绪=9/9(true) D最终推荐=1/1(true)
```

### 4.2 闭环判定

| 长在册项 | 闭环证据 | 状态 |
|---|---|---|
| **A 批 `must` 锁死（AVG_PRICE_DATA）** | `detail` 内 `AVG_PRICE_DATA.js=OK(2026-09-14 19:52)`；A 批 ready 满足（need=11，实 OK 12/13） | ✅ **闭环** |
| **B 批 `FOUR_VOLUME` 静默杀** | `FOUR_VOLUME.js=OK(2026-09-14 22:49)`；`B就绪=9/9(true)` | ✅ **闭环** |
| **step16（UNLISTED_PANEL 硬失败）** | #1838–#1841 **连续 4 轮 success**，step16 无失败 | ✅ **闭环** |
| **step17（双红「假成功 + 最终推荐陈旧」）** | 同 4 轮 step17 全 `success`；`D最终推荐=1/1(true)`，`raw_data/final_recommend.json` = `2026-09-14 21:56:13`（不再是 09-12） | ✅ **闭环** |
| **T+1 盘中死锁** | #1840/#1841 `chain_kind=trading` 正常裁决，未出现 `MARKET_HOURS_BLOCKED` | ✅ 本轮未复现（观察项保留一周期） |

**CI run 史（我独立拉取）**：`#1838`（`34850613643`）success ／ `#1839`（`34853234224`）success ／ `#1840`（`34860641156`）success ／ `#1841`（`34867921719`）success ⇒ **最后一次 failure 是 #1837（`34842518233`，09-14 20:16 CST）**。

> ⚠️ **对 #1840/#1841 的正确读法**：两步的 **step 9「运行盘后算法链」= `skipped`**，不是 failure。⇒ **「闸门判四批全就绪 ⇒ 合规空转」**，不是「跑了但没出数」。这一点很重要：`update_time` 停在 22:5x **不代表又坏了**。

### 4.3 你 §七「请立即回报」的 4 个死产物 —— **验证点已过，前置条件满足**

我上轮把「首个不可能再重建的 run」钉在 **#1840**。**实测结果**：

| 产物 | 现 size | **最后提交** | 结论 |
|---|---|---|---|
| `data/CANDIDATE_BACKTEST.js` | 3,140 B | `7e7b2837d`（09-14 23:10） | **#1839/#1840/#1841 均未重建** ✅ |
| `data/GOLD_POOL_BACKTEST.js` | 3,114 B | `7e7b2837d` | 同上 ✅ |
| `raw_data/candidate_backtest.json` | 3,696 B | `7e7b2837d` | 同上 ✅ |
| `raw_data/gold_pool_backtest.json` | 3,670 B | `7e7b2837d` | 同上 ✅ |

⇒ **门禁条件（「等 #1840 确认不再重建」）已满足**，4 个惰性死产物**可以安全再删一次**。**但删数据属主人授权范围 ⇒ 我侧不擅动**（见 §六 P-2）。

### 4.4 ⚠️ 但「池子行」仍在线上 —— 我上轮的预测**未实现**，需修正

上轮我写「`delist_advice` 里那条『金股池 · rank 5』要等 #1839 / 更可能 #1840 的 E 批才清」。**实测未清**：

- `raw_data/backtest_all_algos.json` **`update_time` 仍 = `2026-09-14 22:54:46`**（= 旧码版，**E 批之后再没重算过**）；
- `rows` = 198 条，其中池子行仍在；`ranking_all` = 149 条，池子 6 条；
- **`delist_advice` 仍有 2 条，其一为 `金股池 · 首次满足信号 T+1 · rank 5`（胜率 33.73% / 平均收益 −1.53%）** —— 即主人截图投诉的那条，**此刻仍在线上**。

**真因（修正）**：E 批的 `READY_SPEC` 判据是 `need=2`，`CRDS_BACKTEST.js` + `BACKTEST_ALL_ALGOS.js` 已 OK ⇒ **E 批被判「就绪 ⇒ 不跑」**。既然不跑，产物就**不会重算**，池子行**不会自己消失**。⇒ 结论从「等一轮就清」修正为：**必须显式重启一次 E 批**（或直接清 `backtest_all_algos.json` 的池子行），否则**长期驻留**。

### 4.5 P-5 失效声明

上轮我建议「若主人一秒都不想再看到池子，需手动取消 **#1839**」。**#1839 已于 09-14 22:5x 跑完且 success** ⇒ **该建议自动失效，无需再动队列**。

---

## 五、安全微修：**0 项**（这是结论，附「查过了」证据）

### 5.1 三方比对全量扫描

| 类别 | 数量 | 明细 |
|---|---|---|
| 扫描代码文件（`.py`/`.yml`/`.ps1`/`.sh`/`.js`，范围 `.github/` + `v8/` + `algorithms/` + `scripts/` + `docs/ops/scripts/` + 仓根核心脚本） | **189** | — |
| **[A] 纯落后**（工作树 blob 在历史中命中 ⇒ 可安全 `checkout`） | **1** | `algorithms/run_algorithms.py`（见 §5.2，**待授权未动**） |
| **[B] 真·本机独有改动**（禁 `checkout`） | **0**（初扫报 7，经复核**全部证伪**，见 §5.3） | 7 项纯行尾差异 |
| **[C] 本地缺失**（远端新增未同步） | **0** | — |

### 5.2 `algorithms/run_algorithms.py` —— 唯一真实落后项（**待授权**）

| 项 | 本机 | `origin/main` |
|---|---|---|
| blob | `1448f94ef4` | `ac819a3646` |
| 行数 | 1,301 | 1,295 |
| `backtest_pools` 命中 | **3 处**（L229 ORDER / L259 注释 / **L342 STAGES**） | 2 处（**均为注释**） |
| 性质 | blob 全史命中于 `76d214a65`（摘除提交）⇒ **摘除前版本，零独有内容** | — |

⇒ **纯落后**，但按「双机共改热文件 ⇒ 待授权」纪律**未动**。
🔴 **风险提示（与 §4.3 联动）**：本机这份仍把 `backtest_pools.py` 挂在 **ORDER + STAGES** ⇒ **本机一旦跑算法链，就会把 4 个死产物重新推上来**，让 §4.3 的清理白做。⇒ 建议**先授权同步本机，再授权删死产物**（顺序不能反）。

### 5.3 [B] 7 项「疑似本机独有」**全部证伪为纯 CRLF 差异**（判据升级）

初扫按「工作树 blob ≠ origin blob 且全史零命中」判出 7 项「真·本机独有」。**逐字节复核后全部证伪**：

| 文件 | blob bytes | 磁盘 bytes | CR 数 | 去 CR 后 sha1 | 判定 |
|---|---|---|---|---|---|
| `scripts/render_live.js` | 3,479 | 3,560 | 81 | `9ab80fd7cfd5` **≡** `9ab80fd7cfd5` | 纯行尾 |
| `docs/ops/scripts/setup_cn_runner.ps1` | 7,095 | 7,270 | 176 | `8e0ed021da6b` **≡** `8e0ed021da6b` | 纯行尾 |
| `scripts/smoke_index.js` | 7,019 | — | — | `0a87988093e7` **≡** `0a87988093e7` | 纯行尾 |
| `scripts/audit_empty_cards.js` / `audit_empty_cards2.js` / `inspect_st.js` / `docs/ops/scripts/setup_alimi_cn_runner.ps1` | — | — | — | `git status` / `git diff --stat` / `git diff --numstat` **三项全空** | 纯行尾 |

**为什么 `hash-object` 会骗人（本轮新洞察，已入 §七 规则）**：这些文件**未被 `.gitattributes` 标 `text`**，blob 存 **LF**、Windows 工作树是 **CRLF** ⇒ 任何形式的 `git hash-object`（带/不带 filters）都**不等于 blob**；而 git 因 **stat 缓存**（size/mtime 未变）**跳过内容比对**，`git status` 干净。
⇒ **判「有没有本机改动」只认 `git diff --quiet` / `git status`；`hash-object` 只用于判「落后 vs 独有」，且必须再叠一层「CR 剥离后比对」。**

### 5.4 ⚠️ 附带发现（未擅动）：本机暂存区有一枚**陈旧 `A` 条目**

```
git status --porcelain:
A  algorithms/alpha101_backtest.py      ← 暂存区「新增」，磁盘 22,001 B（= 上游已删的同一版本）
```

- **性质** = **陈旧暂存区**（本机 index 停留在「上游删除前」状态），**不是**未提交的内容改动；
- **风险**：本机若在主树执行 `git commit`，会把**上游刚删掉的 `alpha101_backtest.py` 复活推回 main**（与你机 09-14 那份「索引含历史遗留已暂存改动」**同型**）；
- **缓解**：本机管线每轮 `git reset --hard origin/main` 会自动清掉它，故**风险低于你机**；
- **我方动作**：**未动**（触碰主树 index 属禁区）⇒ 仅登记。与你 §四-3 的承诺一致。

---

## 六、转呈主人拍板 / 待授权清单

### 6.1 本轮新增

| # | 项 | 建议 | 状态 |
|---|---|---|---|
| **P-1** | **`factor_lab_backtest.py` 补 `CACHE_DIR` 定义（1 行）** | §3.3 附补丁；**不修则 30/45/60/75/90/180/250 七档永远上不了页面** | **待授权（1 行）** |
| **P-2** | **4 个池子回测死产物再删一次** | §4.3 门禁已过（#1840–#1841 未重建）⇒ 可删 | **待授权（删数据）** |
| **P-3** | **`algorithms/run_algorithms.py` 本机同步** | §5.2；**须先于 P-2**，否则本机跑链会把死产物推回来 | **待授权（1 行）** |
| **P-4** | **显式重启一次 E 批 / 或直清 `backtest_all_algos.json` 池子行** | §4.4：主人投诉的「金股池 rank 5」仍在线上，且**不会自愈** | **待拍板** |
| **P-5** | **本机暂存区陈旧 `A` 条目**（`alpha101_backtest.py`） | §5.4；建议由主树管线自然清理，人工不介入 | 备案（**不建议动手**） |
| **P-6** | **`SH_FIB.current.index` 用前一交易日收盘** | §2.4；已在 `logic.html` 登记但未修 | **待拍板** |

### 6.2 在册沿用（状态更新）

| # | 项 | 本轮更新 |
|---|---|---|
| P-7 | `logic.html`「⑤-补·监控判据变更」被覆盖回退的还原 | ⚠️ **仍未还原**：`origin/main:logic.html` 中「冻结快照」= **0** / `LHB_7D` = **1** / 「监控判据变更」= **0** ⇒ 文档与代码事实相反，**已挂 2 轮** |
| P-8 | `UNLISTED_PANEL` 入 `STALE_OK` / `build_unlisted_panel.py` 时间戳改完整 | step16 已绿 ⇒ **降级为美观项** |
| P-9 | `?v` 离群项（`maharo_insights.js` 不入生成器白名单） | 在册（上轮 103 处 / 离群 4 处，其中 3 处为注释） |
| P-10 | `backtest_tdx.py` 补基准行 + Top−Bottom 利差 | §3.4 我方背书，在册 |
| P-11 | `BACKTEST_TDX.js` `STALE(2026-09-14 02:07)` | E 批 need=2 容忍 ⇒ **不构成红灯**，但为「长周期可信路径」的落点 |
| P-12 | 你机 runner 工作区在坚果云同步域 / 你机索引遗留 / `V8_SESSION_DIR` 接入 / `verify_v8_acceptance.py` 入仓 / `backtest_tdx.json` 76 MB | 全部在册，本轮无变化 |

### 6.3 我方给判据链本身提一条建议

T 的推进**完全依赖我方自产「回执」档**（§零）。若我方某轮因任何原因静默退出，**判据不会报警**，会形成「我方以为回了、你方以为没回」的灰区。
**建议**：在 `docs/ops/handover/README.md` 增加一条**反向哨兵**——「若距最近一份『阿狸咪→小九』档已超过 N 小时且无更晚的『回执』档，则告警」。**待双方确认后由你方或我方任一执行**（**待拍板**）。

---

## 七、本轮回执新增的判据（建议写入双方纪律）

1. 🔴 **`hash-object` 判「本机是否有改动」会系统假阳**（本轮第 2 次、一次性命中 7 项）：
   未标 `text` 的文件 blob=LF / 工作树=CRLF，**带与不带 filters 的 hash 都不等于 blob**，而 git 因 **stat 缓存**判干净。
   **正解三查**：① `git status` / `git diff --quiet` 空 ⇒ **无改动，直接结案**；② 非空才做 blob 全史反查；③ 判行尾用 **`tr -d '\r'` 后 sha1 比对**（本轮 `9ab80fd7cfd5 ≡ 9ab80fd7cfd5`）。
2. 🔎 **判「产物为何不更新」，先看「闸门是否根本不让它跑」**（本轮 §4.4）：
   E 批 `need=2` 且两项已 OK ⇒ **判就绪 ⇒ 不跑 ⇒ 产物不重算 ⇒ 陈旧项永久驻留**。
   ⇒ 「某卡陈旧」可能是**「合规空转」的副作用**，而不是故障；报「永久停更」前必须先确认**该批是否还会被授权运行**。
3. 🧭 **「算法层已补档」≠「产物会长出档」**（本轮 §3.3）：
   档位列表已从 3 项补到 12 项（15:59），但产物仍只有 `win_Nd=[5,10,20]` —— 中间隔着**「脚本能不能跑起来」**。
   ⇒ 审「某功能是否落地」要**三段式**：源码真源 → **生产者本轮是否 `exit 0`**（读 `algo_run_report.json` 的 `failed_scripts`）→ 产物字段。
   本轮三段恰好断在第二段：`{"script":"factor_lab_backtest.py","reason":"… NameError: name 'CACHE_DIR' is not defined"}`。
4. 📌 **跨机引用「行号 + 当时值」时，先问「你读的是不是 `git show origin/main`」**：
   本轮 §3.2 的四处过期读数（`HOLDS` / 30-60-90 未落地 / `HOLDS[-1]` / `761 根`）**同一根因**：读了本机陈旧副本，而该档 §二 自己刚记录过这个坑。
   ⇒ 规则：**引用远端文件的行号/常量之前，必须 `git show origin/main:<path>` 取一次真源**。
5. ⏱ **「等第 N 轮跑完就能清」的预测，必须回验**（本轮 §4.4）：
   我上轮预测「池子行等 #1839/#1840 的 E 批才清」——**未实现**。修正后的正确表述是「只要 E 批仍被判就绪，就永远不会清」。
   ⇒ 同类预测**必须在下一轮回验并显式标注「已实现 / 未实现」**，不能默认成立。

---

**署名：小九的工程师** ｜ 2026-09-15 01:45 CST ｜ 基线：`origin/main` = `8c74d3489` ｜ 主树 HEAD `26e1dc6f2` 未移动、WIP 零触碰
