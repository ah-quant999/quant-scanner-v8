# 交接：上一批遗留三项 P1/P2/P3 全部完成

- **从**：小九的工程师（小九单位机 / lemoncat-cn）
- **给**：阿狸咪的工程师（阿狸咪机 / alimi-cn）
- **时间**：2026-09-18 11:35 CST
- **主人指令**：（附图）「都要」
  指上一批交接档《2026-09-18_1135_K型分层卡搬迁》第 ⑥ 节列出的三项遗留 —— P1/P2/P3。
- **本轮性质**：① 新增护栏（P1）② 修复算法层缺陷（P2）③ 机制核实（P3）

---

## ① 本轮三件套改动清单

| 项 | 文件 | 字节 | 行尾 | 提交 |
|---|---|---|---|---|
| P1 | `.github/scripts/guard_intraday_firstrun.py` | **26018**（新增） | LF | `9691883ba091` |
| P1 | `.github/workflows/v8_build_deploy.yml` | 27897 → **31396** | LF | `9691883ba091` |
| P2 | `algorithms/fetch_sh_index_fib.py` | 17537 → **22923** | LF | `7609747149ff` |

- 两个提交均经**净室直推**（GET ref → POST blobs → trees{base_tree} → commits → PATCH ref force=false），**零碰本地工作树**。
- 线上终检：两文件字节数与本地**精确一致**；`LIVE TIP` 分别等于我方 commit，**无并发覆盖**。

---

## ② P1 · 盘中链首跑探活护栏

### 背景（2026-09-18 事故，全部实测取证）

09:2x 主人反馈「开盘了，实时数据还都是旧数据」。五层根因：

| 层 | 事实 |
|---|---|
| ① | 三条盘中链今日 run 数 = **0** |
| ② | **不是** cron 配置错、**不是** runner 离线 —— 全仓当日 event 分布：`dynamic 84 / workflow_dispatch 34 / push 16 / workflow_run 13 / **schedule 仅 3**` ⇒ **GitHub schedule 大面积哑火** |
| ③ | 现有守卫为何没拦住：`v8_cn_fetch_watchdog` 只有 schedule 入口 ⇒ **自身随 schedule 一起哑**；`v8_health_patrol` 的 schedule 部分也哑，仅靠 `workflow_run` 跑了 5 次 |
| ④ | 只看「数据陈旧度」也不够 —— 10:58 实测盘中卡全部 ok，但那是 `cn_fetch_intraday_lemoncat`（14 档定点）喂的，与该链是否跑无关 |
| ⑤ | 连带发现：`v8_intraday_snapshot` 全历史仅 12 条 run，**每天固定只跑 2 次**（而非 `*/10` 应有的 42 次）⇒ **GitHub 对高频 cron 存在降频** |

> **核心教训：任何以 schedule 为唯一入口的守卫，都会在 schedule 哑火时一起哑。**

### 设计（逐条对齐上面五层）

1. **不依赖 schedule** —— 走 `push / workflow_run / workflow_dispatch` 三路
2. **独立 job `intraday_patrol`**，条件 `if: needs.gate.outputs.trading == 'true'`
3. **双判据** —— 每链 = (今日**有效** run 数 ≥ 1) AND (其绑定产物 age ≤ 阈值)
4. **识别假成功** —— run 顶层 `success` 但工作 job 全 `skipped`
5. **识别「设计上盘中不跑」** —— `morning_only` 模式
6. **防自激** —— 锁文件落 `RUNNER_TEMP`，**不落仓**
7. **永不阻断** —— 异常 `exit 0`，调用处再包 `|| true`
8. **`--self-test`** —— 8 例状态机判定矩阵，CI 里先自检再探活

### 🔴 最关键的落地约束（勿改回 build job）

```
build job         的 if 含  trading != 'true'   ⇒ 交易时段整体 skipped
intraday_patrol   的 if 是  trading == 'true'   ⇒ 只在盘中跑
```

**探活的使命恰恰是「盘中检查」** ⇒ 若挂进 `build`，盘中必然不执行 = 形同虚设（等于再造一个双盲区）。

**这条约束已被实跑证据证明**（见下方 run `35302788003`）：

| job | conclusion |
|---|---|
| `gate` | success（trading=true）|
| **`intraday_patrol`** | **success** ← 盘中真的跑了 |
| `build` | **skipped** ← 正确的互补行为 |

### 实跑输出（CI 日志原文，非勾叉）

```
[首跑探活] --self-test 状态机冒烟（不触网）
  PASS mode=intraday      due=True  out=False n=1 skip=0 age=5     → OK   (期望 OK)
  PASS mode=intraday      due=True  out=False n=0 skip=0 age=5     → WARN (期望 WARN)
  PASS mode=intraday      due=True  out=False n=0 skip=0 age=900   → FAIL (期望 FAIL)
  PASS mode=intraday      due=True  out=False n=1 skip=1 age=900   → FAIL (期望 FAIL)
  PASS mode=intraday      due=True  out=False n=1 skip=0 age=None  → OK   (期望 OK)
  PASS mode=intraday      due=False out=False n=0 skip=0 age=900   → WAIT (期望 WAIT)
  PASS mode=morning_only  due=True  out=True  n=1 skip=1 age=900   → INFO (期望 INFO)
  PASS mode=morning_only  due=True  out=True  n=0 skip=0 age=900   → FAIL (期望 FAIL)
[首跑探活] ✅ 自检 8/8 全通

[首跑探活] CST=2026-09-18 11:20:07  周4
  [OK  ] v8_cn_fetch_intraday_lemoncat.yml  run=1(skip=0)  产物 INDEX_QUOTES.js FRESH(11.4min)
  [INFO] v8_algo_intraday_lite.yml          run=1(skip=1)  产物 ALGO_TRACK.js STALE(896.1min)  (morning_only·已出窗)
           ℹ 今日 1 次 run 全被 trading 闸门 skip（预期：盘中不跑本链）
  [OK  ] v8_intraday_snapshot.yml           run=1(skip=0)  产物 SECTOR_FUND_FLOW_INTRADAY.js FRESH(1.5min)
[首跑探活] ✅ 全部盘中链活性与产物新鲜度均正常
```

### 三条链的判据设计依据

| workflow | gate_mode | due_min | 绑定产物 | 依据 |
|---|---|---|---|---|
| `v8_cn_fetch_intraday_lemoncat.yml` | intraday | 09:55 | INDEX_QUOTES.js | 14 档定点，盘中应有持续 run |
| `v8_algo_intraday_lite.yml` | **morning_only** | 09:05~09:25 | ALGO_TRACK.js | 🔴 该 job 有硬闸门 `if: needs.gate.outputs.trading != 'true'` ⇒ **设计上盘中不跑**，只放行 09:00 盘前 / 15:00 盘后。若按 intraday 判，盘中每轮都误报 |
| `v8_intraday_snapshot.yml` | intraday | 10:30 | SECTOR_FUND_FLOW_INTRADAY.js | 已知被降频 ⇒ 判定时刻后压 + `throttled: True` 标记 |

### 开发过程中被实跑揪出的 5 个缺陷（v2→v6）

| # | 缺陷 | 修法 |
|---|---|---|
| A | 把「抓取失败」当「陈旧」—— 文案渲染成「陈旧 -1.0min>45min」（-1 不 > 45，自相矛盾）| 三态 `FRESH/STALE/UNKNOWN`，`UNKNOWN` 单列不计告警 |
| B | 把「设计上盘中不跑」当「陈旧」⇒ 每轮误报 | 新增 `gate_mode: morning_only` |
| C | skip 探测判据写错：`all(...)` 对**全部** job 判定，而 gate job 恒 success ⇒ 永远 `skip=0` | 只看「实际工作 job」（排除 `gate`）|
| D | 🔴 **`IncompleteRead`** —— 取 `runs?per_page=100`（1.26MB）跨境响应半程截断 | `_read_all()` 重试拼接 + `per_page` 降到 30 |
| E | 锁文件落 `data/.workbuddy/` ⇒ 写锁触发 push → 触发 build → 再探活 …… **自激循环** | 改落 `RUNNER_TEMP` |

---

## ③ P2 · 斐波那契观测窗「窗口真空」根治

### 现场（2026-09-18 11:22 实测线上 `raw_data/sh_fib.json`）

```
update_time = 2026-09-17 18:08
windows: 7 档 全部 status="passed"（05-25 ~ 07-16），无一带 observation
reversal_date = None（从未 confirmed）
next_cycle = []
judgement.key_window = ""
judgement.summary = "从20260701高点跌57天（-5.76%），阴跌磨底"
```

⇒ **窗口真空已持续 ≥64 天**（F(55)=07-16），**零告警** ⇒ 典型双盲区。

### 根因三层

```
L19  FIB_BASE_DATE = "2026-05-22"  硬编码，从未更新
     而脚本自算峰值 = 2026-07-01  ⇒ 基准比真实转折点早 40 天
L28  窗口 = base + n 天（n ∈ [3,5,8,13,21,34,55]）⇒ 05-25 ~ 07-16，全在今日之前
L207 build_windows 的 "passed" 分支**不写 observation**
     ⇒ _roll_forward 的 obs_candidates 恒空
L202 reversal_date 仅在 status="triggered" 时赋值 ⇒ 恒 None
⇒ 三层叠加 ⇒ L282 `if not anchor: return tracked_wins, []` **静默返回空**
```

### 🔴 为什么不能只改 FIB_BASE_DATE（关键）

实测：**即便把基准换成脚本自算的 `2026-07-01`，7 档仍全过期**（F(55)=08-25，距今 -24 天）。

原因：`FIB_LEVELS` 最大只有 **55 天**，而当前距任何合理基准都已 **>55 天**。
⇒ **单轮窗口模型在此必然失效，必须靠 `_roll_forward` 续命；而续命链恰恰断在第三层。**
⇒ 所以修 `_roll_forward` 是**唯一**的正确落点。

### 改动

| # | 位置 | 改动 |
|---|---|---|
| 1 | `calc_next_cycle()` | 支持**多轮递推**（`cycle=1..6`），不再「一轮不中即空」 |
| 2 | `_roll_forward()` 锚点 | 补到**四级**：`triggered` → `reversal_date` → `observation` → **最后窗口日**（无条件）⇒ 彻底封死 `anchor=None` 的静默返回 |
| 3 | `_roll_forward()` 失败路径 | 写 `roll_diag`，**静默 → 可见** |
| 4 | 新增 `resolve_base_date()` | 基准日由**实测峰值自动推导**（兜底才用常量） |
| 5 | `main()` | 按 symbol 动态解析基准，打印实际基准日 |
| 6 | `build_fib_json()` | 无 reversal 时也试算 `next_cycle`；续命失败写 `judgement.roll_error` |

### 本地验证（现场数据复刻 + **前后对照**）

```
[1] calc_next_cycle('2026-07-16', today)
    产出 1 档：F(55) 2026-10-01  cycle=1      ✅ 全部未来窗口
[2] _roll_forward(windows, None, today)
    滚动窗口数=1  未来=1  活跃=0  诊断=[]     ✅ 续命成功
[3] 对照 · 修复前旧逻辑（复刻）
    旧逻辑产出：(空)  ← 恒空（现场缺陷复现）  ✅ 对照成立
[4] resolve_base_date
    推导基准 = 2026-07-01（兜底常量 2026-05-22）✅ 正确识别峰值
```

### 线上终检

- 字节 **22923** = 本地 ✅；CRLF=0 ✅
- 特征串：`max_cycles=6`=1 / `resolve_base_date`=4 / `anchor_src`=7 / `no_next_cycle`=1 / `roll_error`=2 / `last_window`=1 ✅

---

## ④ P3 · SH_FIB.js 20:00 D 批重建机制核实

### 结论：**机制真实存在、会按期触发，不是双盲区** ✅

完整链路（逐段取证）：

```
algorithms/fetch_sh_index_fib.py     ← 挂在 A 批（run_algorithms.py ORDER L145 / STAGES["A"] L301）
    ↓ 写 out/sh_index_fib.json（脚本 DATA_DIR = ../out，自身不写 update_time）
algorithms/stage_to_raw.py           ← 按 V6_TO_V8 重命名（L65-66）+ 注入 update_time（L127-128）
    ↓ raw_data/sh_fib.json
update_v8.py                         ← "sh_fib.json":"SH_FIB"（L78-79 / L299-300 "SH_FIB":"post_close"）
    ↓ D 批（盘后 20:00）重建
data/SH_FIB.js
```

**实测佐证**：线上 `raw_data/sh_fib.json` 与 `data/SH_FIB.js` 的 `update_time` **均为 `2026-09-17 18:08`**，两者一致 ⇒ 该链正常跑通。

### 顺带发现（留待评估，本轮未改）

1. **类别口径疑点**：`fetch_sh_index_fib.py` 挂在 **A 批（盘前）**，但 `update_v8.py` 把它登记为 **`post_close`**。产物 `update_time` 是 A 批执行时刻、而 js 重建走 D 批 ⇒ 两者时间不同源。**当前不构成故障**，但口径不统一。
2. **`run_algorithms.py` L410 有硬断言**：`assert _STAGE_UNION == set(ORDER)` ⇒ **今后改 `ORDER` 必须成对改 `STAGES`**，否则模块级崩链、盘后链启动即死（注释 L156/L259/L287 已多处警示）。
3. `v8_intraday_snapshot` 被 GitHub 降频（每天仅 2 次 vs 应 42 次）这一**隐性单点**仍未处理 —— 目前其产物由他链兜底，未显故障。**建议后续评估：改低频 cron 或并入他链。**

---

## ⑤ 踩坑记录（本轮新增，已同步进 skill / memory）

| # | 坑 | 后果 | 规避 |
|---|---|---|---|
| 1 | 🔴 **`IncompleteRead`** —— 跨境 HTTP 响应半程截断（取 1.26MB 的 runs 列表时实测）| 可能静默拿到残缺 JSON；`json.loads` 抛错被吞 ⇒ 降级为「不可读」| `_read_all()` 重试拼接（`e.partial`）+ 降低 `per_page` |
| 2 | 🔴 **`if/else` 链新增分支时旧 `else` 未删** ⇒ `if/else/else` 三连 | **SyntaxError**，脚本完全不可用 | 新增分支**必须放进原 `else` 内部**，不可并列；改完 `ast.parse()` 必过 |
| 3 | 🔴 **断言值按「行数」估算**（本会话第 2 次犯）| `count()` 实测 29 vs 估 4 ⇒ 断言 raise，救回误判 | 断言值**一律 `count()` 实测**，禁估算 |
| 4 | ⚠️ 线上文件已**被他人改过**（`v8_build_deploy.yml` 19217B → 27603B）| 若按旧基线打补丁会**覆盖别人改动** | **每次动手前先取线上真身**，禁用缓存基线 |
| 5 | ⚠️ 「run 顶层 `success`」≠「job 真的跑了」| `intraday_patrol` 差点挂进 `build`（盘中恒 skipped）| 判 job 活性要看 **job 级 conclusion**，不看 run 级 |
| 6 | ⚠️ GitHub 用**推送后**的 `paths` 规则判定 | 本轮改了 `.github/workflows/` 仍触发了 build（因同时改了白名单内路径）| 新增脚本**必须同步加进 `paths`**，否则改脚本不触发部署 |

---

## ⑥ 留给阿狸咪的待办

| 优先级 | 项 | 说明 |
|---|---|---|
| P1 | **`v8_intraday_snapshot` 降频评估** | 每天仅 2 次（应 42 次）。当前由他链兜底，属隐性单点。建议改低频 cron 或并入他链 |
| P2 | **SH_FIB 类别口径统一** | `fetch_sh_index_fib` 挂 A 批、`update_v8` 登记 `post_close`。建议择一对齐 |
| P3 | **观察 P2 修复后的首个 D 批** | 今晚 20:00 后查 `data/SH_FIB.js`：`next_cycle` 应非空、`judgement.key_window` 应显示「以末窗推算下轮」，否则查 `judgement.roll_error` |
| P4 | **P1 护栏的 `V8_GH_TOKEN` 权限确认** | 自动补派需 `actions:write`。若该 secret 无权限，脚本会降级为「只报告」（不失败），但补派不生效 |

---

## ⑦ 双机提示

- 本轮改动**全部净室直推**，**不涉及本地工作树** ⇒ 你 `git pull` 会看到上述两个 commit。
- 若你本地也有 `algorithms/fetch_sh_index_fib.py` 或 `.github/workflows/v8_build_deploy.yml` 的未提交改动，**请先比对再合并** —— 我取的是**当时线上真身**（`v8_build_deploy.yml` 27603B 版）为基线，未覆盖你此前的改动。
- **盘中（09:25-11:30 / 13:00-14:59）不要动 `v8_build_deploy.yml`** —— `build` job 本就不跑，改它只会在收盘后集中生效，但改坏 `YAML` 会影响 `gate` / `intraday_patrol`。改前请 `yaml.safe_load()` 自检。

---

## ⑧ 反查索引

| 概念 | 落点 |
|---|---|
| 探活护栏脚本 | `.github/scripts/guard_intraday_firstrun.py` |
| 探活挂载点 | `.github/workflows/v8_build_deploy.yml` → job `intraday_patrol` |
| 与 build 的互补条件 | 同上：`trading == 'true'` vs `trading != 'true'` |
| 状态机自检 | `guard_intraday_firstrun.py --self-test` |
| 三态判据 | 脚本内 `fresh` 变量（`FRESH/STALE/UNKNOWN`）|
| morning_only 定义 | 脚本 `CHAINS[1]["gate_mode"]` |
| 锁文件 | `$RUNNER_TEMP/v8_intraday_firstrun_lock.json`（**不落仓**）|
| Fib 续命入口 | `algorithms/fetch_sh_index_fib.py` → `_roll_forward()` |
| Fib 基准推导 | 同上 → `resolve_base_date()` |
| Fib 阶段映射 | `algorithms/stage_to_raw.py` → `V6_TO_V8`（L65-66）|
| Fib 类别登记 | `update_v8.py` → `"SH_FIB": "post_close"`（L299）|
| 批次硬断言 | `run_algorithms.py` L410 `assert _STAGE_UNION == set(ORDER)` |

---

## ⑨ 现取命令

```bash
# 取线上探活脚本
curl -sL --max-time 60 "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/.github/scripts/guard_intraday_firstrun.py" -o g.py
# 取线上 build_deploy（看 intraday_patrol）
curl -sL --max-time 60 "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/.github/workflows/v8_build_deploy.yml" | grep -n -A3 "intraday_patrol:"
# 取线上 fib 脚本（看续命逻辑）
curl -sL --max-time 60 "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/algorithms/fetch_sh_index_fib.py" | grep -n "anchor_src\|max_cycles\|resolve_base_date"
# 看今晚 D 批是否修好窗口真空
curl -sL --max-time 60 "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/raw_data/sh_fib.json" | python -c "import json,sys; d=json.load(sys.stdin); print('next_cycle=',len(d['next_cycle'])); print('key_window=',d['judgement'].get('key_window')); print('roll_error=',d['judgement'].get('roll_error'))"
```

---

**本轮状态**：P1 ✅ 完成并实跑验证 ｜ P2 ✅ 完成并线上终检 ｜ P3 ✅ 取证完成（机制正常，附 3 项待评估）
