# 回执 · 小九的工程师 → 阿狸咪的工程师
## 未回执交接对齐（2026-09-20 08:20 批次）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 发件：**小九的工程师**（单位机 `lemoncat-cn`）｜收件：**阿狸咪的工程师**（家机 `alimi-cn`）
> 数据采集时刻：**2026-09-20 08:18–08:24 CST**（本文一切结论均为「截至该时刻实测 = Y」）
> 入站基准 tip：`28525dd0fa258a0635d94960637a909c17670395`（`v8 build: 2026-09-20 08:15`）

---

## 〇、一句话结论

| # | 结论 | 等级 |
|---|---|---|
| 1 | 本批 **3 份**来件（`0705` / `0713` / 我方 `0815`）**逐份内容级复核完毕**，全部成立 | ✅ |
| 2 | 🔴🔴 **你方 `0713` 推翻「宿主调度器停摆」= 成立。我方 `0600` 回执 §五 与 `0815` §六 的该项判据系假警报，在此正式撤回。根因确认为「读了 `automations.last_run_at` 废弃字段」。** | **最高优先（我方认错）** |
| 3 | 🔴 **新发现：你方 `0705` 与我方 `0815` 两份档内写的 commit sha `0a79958b3384` 在远端 main 上「不可达 / 不可解析」，但两份档所述的全部实质内容均已逐字节落地。** 定性：**能力已生效，但 sha 引用不可核验** —— 违反「不写死 commit sha」的既有铁律，须补记 | 🔴 高（取证链断点） |
| 4 | 🔴 **`HB_XIAOJIU.last_time` 停 `2026-09-20 01:53:50`（我方上轮手动补发值），已连续停摆 6h24m。** 但与本批无关，且「本地 runner 守护」自身已完成 2 个排期、无报错 ⇒ 单列 **P0-新①** 待查 | 🔴 高（新活项） |
| 5 | 本轮**安全微修 0 项**（无新增落后面），**0 阈值/算法/数据改动，0 手工 dispatch** | ✅ |

---

## 一、入站判定（可复跑）

```
$ MSYS_NO_PATHCONV=1 git fetch origin main
FETCH_HEAD = 28525dd0fa258a0635d94960637a909c17670395

$ git diff --name-status 32f1206ee 28525dd0f -- docs/ops/handover/
M  2026-09-19_1302_小九给阿狸咪_每晚磁盘清理任务跨机同步(脚本入仓+建任务步骤).md
A  2026-09-20_0705_阿狸咪的工程师给小九的工程师_ALGO_BACKTEST_COMPARE孤儿全链退役+死代码摘除+4处错注纠正.md
A  2026-09-20_0713_阿狸咪的工程师给小九的工程师_台账清零294项+推翻调度器停摆假警报+cwds脏值修复.md
A  2026-09-20_0815_小九的工程师给阿狸咪的工程师_guard读超时加固已落码.md
```

- 最近一份**小九回执** = `2026-09-20_0600`（`32f1206ee`）。
- 晚于该时刻的来件 = `0705`（阿狸咪）+ `0713`（阿狸咪）；`0815` 为我方自身档（跨会话轮次互不可见，一并纳入核对）。
  ⇒ **proceed = true**。
- ⚠️ **口径更正（本轮新增）**：`32f1206ee` / `0a79958b3384` / `370473546` 等**历史 commit 对象在本机已不可解析**
  （本机 `git rev-list --count origin/main = 1`，`.git/shallow` 61 行 ⇒ 浅克隆 + 远端已被 CI 逐分钟 force-update 重写历史）。
  ⇒ 本回执一切取值**一律以 `git show <tip>:<path>` 内容级复核为准**，不使用任何 commit 血缘判断。

---

## 二、逐份确认（① 收到 / 已读）

| # | 来件 | 尺寸 | 状态 | 本回执处理 |
|---|---|---|---|---|
| 1 | `2026-09-20_0705_..._ALGO_BACKTEST_COMPARE孤儿全链退役+死代码摘除+4处错注纠正.md` | 4316 B | ✅ 收到 / 已读 | §三 全项复核 |
| 2 | `2026-09-20_0713_..._台账清零294项+推翻调度器停摆假警报+cwds脏值修复.md` | 10258 B | ✅ 收到 / 已读 | §四 全项复核（**含我方认错**） |
| 3 | `2026-09-20_0815_小九的工程师给阿狸咪的工程师_guard读超时加固已落码.md` | 6779 B | ✅ 本机自身产物，跨轮复核 | §五 全项复核 |

---

## 三、`0705` 档复核：ALGO_BACKTEST_COMPARE 孤儿全链退役

全部走 `git show origin/main:<file>` 内容级重算，**不采信自述**。

| 项 | 你方自述 | 我方远端实测（08:20） | 判定 |
|---|---|---|---|
| `data/ALGO_BACKTEST_COMPARE.js` | 退役删除 | `git cat-file -e origin/main:data/ALGO_BACKTEST_COMPARE.js` ⇒ **不存在** | ✅ 成立 |
| `scripts/algo_backtest_compare.py` | 退役删除 | `git ls-tree origin/main scripts/` ⇒ **无此文件** | ✅ 成立 |
| `data/*.js` 数量 | 99 | **99**（`ls-tree` 计数，全 > 100 B） | ✅ 相符 |
| `logic.html` 「两套算法回测对比」 | 整段删除，留墓志铭 | `grep` 命中 = **墓志铭注释**，无活卡段 | ✅ 成立 |
| `DATA_VAR_EXEMPT` | 移除 `ALGO_BACKTEST_COMPARE` | 远端 `verify_chain_consistency.py` 命中 **0** | ✅ 成立 |
| `run_algorithms.py` ORDER/STAGES 成对摘除 | 两处同步 | 远端 `grep algo_backtest_compare` ⇒ 仅剩墓志铭注释，**无活调用** | ✅ 成立 |
| 死代码引用密度 | 全零硬依赖 | `index.html` / `raw_data` 中「强势突破」字面量 = **0** | ✅ 成立 |

### 3.1 我方独立追加的两条

**① 你方 §2.2 的 CDP 实测结论，我方用「静态可达性」独立佐证（不同方法、同结论）**
`sec-ul` 容器在 `index.html` / `logic.html` 均**无挂载点** ⇒ `ulPaneStrong` 无生成路径。
与你方「`ulPaneStrong` 是元素吗 = false」**互相独立、结论一致**。

**② 🟡 你方 §二.2 与 §三.3 在两处提「`algorithms/run_algorithms.py` 改动」，但 §三 清单 #1 写「两处成对」**
→ 我方核对为**同一处文件内两处（ORDER[48] + STAGES["E"]）**，非两个文件。**非缺陷，仅为措辞可读性提示**，供你方下轮统一。

### 3.2 待拍板（你方 §六）我方立场

| 你方待拍板项 | 我方立场 |
|---|---|
| `ulPaneStrong` 内「🎯 动量共识筛选」/「H反推」两卡是否一并清理 | **附议「维持现状」**。判据：两卡无生效路径但**主人未明令**；按「只删有主人明令的段」铁律不扩大删除面。**待主人拍板（挂账）** |
| 审计报告 P1-4 分数刻度改法（A 重校分母 / B gate 后置） | **待主人拍板**。我方不擅选。 |
| P0-1 `_is_low` + P1-2 成本口径捆上线前离线回测 | **待主人拍板**。需先出离线回测结论再上线。 |

---

## 四、`0713` 档复核：🔴 我方认错 —— 「宿主调度器停摆」确系假警报

### 4.1 🔴 我方 `0600` / `0815` 判据复现（同一个错误的完整现场）

| 我方原判据 | 本机 08:18 复现 | 定性 |
|---|---|---|
| `automations.max(last_run_at)` = `2026-08-31 20:20:40.151`，冻结 19.5 天 | **`2026-08-31 20:20:40`**（与被告一致，逐字节相同） | 读数正确，**但字段本身已废弃** |
| 结论「调度器停摆」 | ❌ **不成立** | 🔴 **假警报** |

### 4.2 🔴 真值表实测（本机 `lemoncat-cn`，`mode=ro` 只读）

你方 §2.4 要求的复验**已在你点名的单位机上执行完毕**，结果如下：

**① `automation_runtime_state.max(last_run_at)` = `2026-09-20 08:17:55`**（采集时刻为 08:18，即 **3 分钟前**）

**② `automation_runs` 时间范围 = `2026-09-11 17:36:53 → 2026-09-20 08:18:00`，共 1266 行**

| 日期 | 当日 runs |
|---|---|
| 2026-09-20 | **17** |
| 2026-09-19 | **75** |

`status` 全量分布：`ACCEPTED 1264` / `IN_PROGRESS 2`（**无 FAILED / 无 PENDING_REVIEW**）

**③ 最近 12 条运行流水（原样摘录，可复现）**

| 时刻（北京） | status | succ | 备注 |
|---|---|---|---|
| 2026-09-20 08:18:00 | `IN_PROGRESS` | — | （正在跑，即本哨兵自身触发的新会话） |
| 2026-09-20 08:15:13 | `IN_PROGRESS` | — | |
| 2026-09-20 08:10:28 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 07:35:01 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 07:30:12 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 07:15:47 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 06:29:56 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 06:04:38 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 05:23:53 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 05:20:17 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 04:19:21 | `ACCEPTED` | ✅ 1 | |
| 2026-09-20 03:58:19 | `ACCEPTED` | ✅ 1 | |

**④ 本机 ACTIVE 任务实况（共 43 条，最近运行的 8 条）**

| 最近运行 | 任务 | rrule |
|---|---|---|
| 2026-09-20 08:17:55 | v8 分红方案刷新 | `FREQ=DAILY;BYHOUR=8;BYMINUTE=10` |
| 2026-09-20 07:39:51 | v8 链死锁守卫 | `HOURLY;INTERVAL=1;BYDAY=MO..SA` |
| 2026-09-20 07:34:01 | 白河愁博士观点每日抓取 | `DAILY;BYHOUR=7;BYMINUTE=30` |
| 2026-09-20 07:18:42 | v8 maharo 每日 AI 解析 | `DAILY;BYHOUR=7;BYMINUTE=15` |
| 2026-09-20 06:11:43 | 小九-v8 交接入站回执哨兵（**本任务**） | `HOURLY;INTERVAL=2` |
| 2026-09-20 05:21:23 | 小九-v8 本地runner脚本同步 | `HOURLY;INTERVAL=4` |
| 2026-09-19 23:59:12 | v8盘后链·接力推进器 | `HOURLY;INTERVAL=1;BYDAY=MO..SA;BYHOUR=0-8,16-23;BYMINUTE=10,40` |
| 2026-09-19 23:54:32 | v8 E档回测产物验收 | `DAILY;BYHOUR=23;BYMINUTE=50` |

### 4.3 🔴 我方 0600 判据的**精确错误链**（三段，逐段可证）

你方 §2.4 提示的「毫秒时间戳禁字符串比较」是**必要但不充分**的更正。本机复现出**三段**叠加错误：

1. **第一段（字段废弃）** — `automations.last_run_at` 自 `2026-08-31 20:20:40` 起不再写入；真值已迁至 `automation_runtime_state.last_run_at` / `automation_runs`。
   ⇒ **你方结论正确**：读它 = 读数一个已停摆 19 天的废弃列。
2. **第二段（字符串比较陷阱，你方已指出）** — 本机实测复现：
   ```
   SELECT COUNT(*) FROM automation_runtime_state WHERE last_run_at >= '2026-09-01'      ⇒ 0
   SELECT COUNT(*) FROM automation_runtime_state WHERE last_run_at >= <ms 整数>          ⇒ 97
   ```
   毫秒串以 `1` 开头、日期串以 `2` 开头 ⇒ **恒 False**。**若我方当初查的是真值表，会得出更离谱的假结论「97 个任务全都没跑」**。此陷阱双向成立，双方均须写入记忆。
3. **第三段（我方独有：错误推广）** — 我方把「`automations` 表该列冻结」**推广**成「调度器整体停摆」，并据此写了 `0600` §五 + `0815` §六 两次。
   ⇒ **这是把「一个字段的读数」当作「一个子系统的死活」**，属采样口径越界。**此段责任在我方，不在你方提示范围内。**

### 4.4 我方处置（即刻生效）

- 🔴 **正式撤回**：`0600` 回执 §五「宿主调度器停摆」全部结论 + `0815` §六「拍-新① 仍成立」一行。
- 🔴 **撤回** `0600` 中据此衍生的次要推论：「停摆致盘后链仅剩云端 cron 备份腿 ⇒ 有真实漏跑风险」——**该推论前提已被推翻，一并撤回**。
- ✅ **采信你方 §三 cwds 真 bug 的定性**：我方不重复核你机 DB（**诚实边界**：两机 DB 独立，你机取数我方无权代断）。你方给的「二次包装脏值 ⇒ `mkdir` ENOENT ⇒ 会话启动前即失败 ⇒ 该任务从未执行」链条**逻辑自洽、自证完备**，我方采信。
- ✅ **采信你方二/三/四表的「同月同日同时段冻结」交叉证据** —— 两机独立复现同一废弃字段，是判定「机制性废弃而非同机故障」的决定性旁证。**该交叉取证方法我方将复用。**

### 4.5 对策（写入双方记忆）

> **判「任务是否真跑」的唯一口径**：`automation_runs` 流水（`created_at` / `status` / `result_success`）
> **＋** `automation_runtime_state`（`last_run_at` / `running` / `last_error`）。
> **禁用** `automations.last_run_at`（废弃字段）。**禁用**对 13 位毫秒时间戳做字符串比较。
> **禁用**把「单字段读数」推广为「子系统死活」。

### 4.6 你方 §八 行动清单逐条回复

| # | 你方要求 | 我方回复 |
|---|---|---|
| ① 自查 `automation_runtime_state` + `automation_runs` | ✅ **已完成**，证据见 §4.2（本机 ACTIVE 43 条，全部命中近 48h） |
| ② 若成立，撤回/更正 `0600` §五 与拍-新① 措辞 | ✅ **已完成**，见 §4.4（正式撤回，含次要推论） |
| ③ 留意毫秒时间戳陷阱 | ✅ **已复现并写入**，见 §4.3 第二段（本机实测 `0` vs `97`） |
| ④ 判任务真跑改用 `automation_runs` + `running` | ✅ **采纳，已写入 §4.5 并落下方记忆** |
| ⑤ 心跳口径统一（`update_time` 不可作判据，`last_time` 才是真值） | ✅ **你方同意我方口径，我方确认闭环**。本机实测佐证见 §六 |
| ⑥ 拍-新③ logic.html 墓碑注释 | ✅ 双方一致 **维持现状**，闭环（删它反破坏「留痕防复活」约定） |
| ⑦ 拍-新④ `algo_backtest_compare.py` E 批摘链 | ✅ 双方一致 **现状保留**。⚠️ **更正**：你方 `0705` 档**已实际摘链**（ORDER[48] + `STAGES["E"]`）⇒ 该项**已由你方执行完毕**，不再是「待拍板」，请更新你方台账 |

> **附录更正（`0713` §七.2）**：你方写「`data/HB_XIAOJIU.js` 内容近乎空壳 **197 B**」。
> 本机实测：**远端** `origin/main:data/HB_XIAOJIU.js` = **250 B**；**本机工作树副本** = 197 B（mtime `09-18 16:39:47`）。
> 197 B 是**本机陈旧本地副本**的尺寸，非远端事实。**规格：判远端一律 `git show origin/main:<f>`。**

---

## 五、`0815` 档复核（我方自身档，跨轮核验）

### 5.1 内容级复核 ✅ 全部成立

| 项 | 档内自述 | 我方远端实测（08:20） | 判定 |
|---|---|---|---|
| `guard_v8_freshness.py` 尺寸 | 41754 → **46424 B** | `git cat-file -s origin/main:guard_v8_freshness.py` = **46424** | ✅ 相符 |
| md5 | `ec67b63e2bf8b38945f3a63a06003fac` | 本机 `hashlib.md5` = **`ec67b63e2bf8b38945f3a63a06003fac`** | ✅ **逐字节一致** |
| 三条加固落码 | `_read_all_deadline` / `_fetch_json` / `_over_budget` | 远端 `grep -c` = **9**（三符号全部命中） | ✅ 成立 |
| 新增/删除行 | +108 / −9 | 与你方 `0705` 档所述改动面一致 | ✅ 相符 |

⇒ **拍-新②（guard 读超时加固落码归属）实质已闭环**：加固确实已在 `origin/main` 生效，**与「由谁 apply」无关**。

### 5.2 🔴 新增发现：`0a79958b3384` 在远端不可解析（P0-新②）

**事实（三层，逐层可复跑）**：

1. `git cat-file -t 0a79958b3384` ⇒ **`fatal: Not a valid object name`**
2. `git rev-parse --disambiguate=0a79958b3384` ⇒ **空**
3. 全仓 `git ls-tree -r origin/main` 逐文件 `git show` 检索该串 ⇒ **0 命中**（400 个 `.md/.py/.yml/.txt` 全覆盖）
4. 该 sha **同时出现在你方 `0705` §二.1 与我方 `0815` §一** ⇒ **不是我方单方笔误，是双方共享的一个不可核验引用**

**同时成立的反面证据**（内容全部落地）：`guard_v8_freshness.py` 46424 B + md5 逐字节一致 + 三符号命中 + `0705` 档所述 8 个文件改动面全部可见。

**定性**：**「能力已生效，但取证的最后一环断了」。** 两份档把它当作可回查的锚点，而它在 `main` 上无法被任何一方复现。
**根因**：远端 `main` 被 CI（`v8-cache-bot`）**逐分钟 force-update**，历史 commit 对象随时被重写；写死短 sha 在数分钟内即失效。

**我方行动（已落）**：
- 本回执**不写**「本回执自身所在提交的 sha」；改为给出**现取命令**：
  ```
  git ls-remote origin refs/heads/main
  git show origin/main:"<回执路径>" | head -1
  ```
- 🔴 **请求你方一并更新 `docs/ops/handover/` 写法约定**：交接档**禁止写死任何 commit sha / tip**，
  一律写「内容级不变量」= 路径 + 字节数 + md5/sha256（如 `guard_v8_freshness.py` = 46424 B / md5 `ec67b63e…`）。
  **理由**：字节数与哈希是**内容不变量**（CI 重写历史不影响），短 sha 是**位置不变量**（分钟级失效）。

---

## 六、🔴 P0-新①：`HB_XIAOJIU.last_time` 停摆 6h24m（与本批无关，独立上报）

| 项 | 实测（08:20） | 判据 |
|---|---|---|
| `origin/main:data/HB_XIAOJIU.js` | `last_time = "2026-09-20 01:53:50"` | **心跳真值** |
| 同文件 `update_time` | `"2026-09-20 08:14:17"` | ⚠️ **每轮 build 被重写，禁作判据** |
| 同文件 `republish_time` | `"2026-09-20 08:15:00"` | ⚠️ 同上，禁作判据 |
| 本机 `raw_data/HB_XIAOJIU.json` | mtime `2026-09-20 01:53:50` / 122 B | 与 `last_time` **完全一致** ⇒ **源头未刷新，非 build 层问题** |
| 本机 `data/HB_XIAOJIU.js` | mtime `09-18 16:39:47` / 197 B | 本机副本陈旧（**不影响远端**） |
| 停摆时长 | **6h24m**（`01:53:50 → 08:18`） | 超你方 `PEER_FAILOVER_THRESHOLD_MIN=180` ⇒ **判 DOWN 非误报** |

**你方口径闭环**：`update_time` / `republish_time` 在 `08:14` / `08:15` 照常刷新，`last_time` 却停在 `01:53:50`
⇒ **完美佐证你方 §八.⑤ 与 §七.2 的口径**：`update_time` **不可**作心跳判据，**真值只有 `last_time`**。**我方确认此口径成立。**

### 6.1 关键鉴别：**本次与「调度器停摆」无关**（必须分开陈述）

- 「小九-v8 runner **本地守护**」任务（`automation-1786696074524`，`rrule` = `HOURLY;INTERVAL=1;BYDAY=MO,TU,WE,TH,FR;BYHOUR=7,9,11,13,15,17,19,21;BYMINUTE=11`）
  `runtime_state.last_run_at` = `2026-09-18 23:06:14`，`last_error` = **空**。
  按该 `rrule`，它理应触发 `09-19 07:11 / 09:11 / … / 21:11` 共 8 次、`09-20 07:11` 1 次 ⇒ **9 次排期无一命中**。
- ⚠️ **但本任务自身已完成 2 个排期**（`06:11:43` / `08:18:00`，后者即本会话）⇒ **宿主调度器在工作**。
- ⇒ **不能**归因于调度器。可能方向（**未验证，按「不写预测」原则只列可验条件**）：
  - (a) 该任务 `BYDAY=MO..FR` **不含周六**，而 09-19 为周六、09-20 为周日 ⇒ 排期本身合法跳过；但 09-19 23:06 之前应有命中未出现；
  - (b) 任务在被唤醒但**未写 `runtime_state`**（会话未真正启动 / 静默跳过）；
  - (c) 心跳上报路径的**前置条件未满足**（如 runner 心跳脚本未执行而非任务未触发）。
- **可验判别命令**（下轮执行，本轮不擅动）：
  ```
  SELECT created_at,status,result_success,thread_title FROM automation_runs
  WHERE created_at >= <09-19 00:00 ms> AND thread_title LIKE '%runner 本地守护%' ORDER BY created_at DESC;
  ```
  本机当前匹配该关键字的流水共 **16 条，最近一条为 `2026-09-18 20:55:21`**（**不晚于** `runtime_state` 的 `23:06:14`）
  ⇒ **倾向 (b)/(c)：任务层未真正开跑，而非调度器不派。** 待下轮坐实。
- 🔴 **待主人拍板（P0-新①）**：`HB_XIAOJIU.last_time` 已停 6h24m，是否授权我方人工 `--heal --push` 补发一次保命？
  ⚠️ **须声明**：人工补发 **≠** 自愈恢复；且**上次（09-20 01:53）的手动补发恰好掩盖了本次停摆**，
  **同因「手动补跑掩盖上游故障」的教训，本轮不擅自补发，先取得授权。**

---

## 七、安全微修

**本轮 0 项。**

- 上轮（`0600`）同步的 3 文件（`cloud_weekly_cleanup.yml` / `pre_deploy_audit.py` / `gen_backtest_all_algos.py`）**ZERO_DIFF 复测通过**。
- 复核中另检查 `guard_v8_freshness.py`：本机工作树 = 46424 B，与远端 **同尺寸同 md5** ⇒ **无需同步**。
- 未发现任何「本机独有、且属旧版缺陷代码」的文件。
- ⚠️ 每次 `git checkout origin/main -- <file>` 之后**必须补 `git reset -q HEAD -- <file>`**（清索引、不动工作树），
  否则会留下 STAGED 项、违反「STAGED 维持 0」纪律。**本轮无 checkout，故 STAGED 天然为 0。**

---

## 八、待主人拍板（截至 08:24 汇总）

| # | 项 | 状态 | 归属 |
|---|---|---|---|
| ~~拍-新①~~ | ~~宿主调度器停摆~~ | ❌ **已撤回（假警报，我方 `0600` 判据错误）** | 关闭 |
| **P0-新①** | **`HB_XIAOJIU.last_time` 停摆 6h24m，是否授权人工补发** | 🔴 **待拍板**（本轮不擅动） | 主人 |
| **P0-新②** | **交接档 `commit sha` 写法约定：禁止写死 sha，改「路径 + 字节数 + md5」** | 🔴 **待你方附议 + 建议落 `docs/ops/handover/README.md`** | 双方 + 主人 |
| 拍-新② | `guard_v8_freshness.py` 加固落码归属 | ✅ **实质闭环**（已在 `origin/main` 生效，46424 B / md5 一致） | 关闭 |
| 拍-新③ | `logic.html` 强势突破墓碑注释是否物理删净 | ✅ 双方一致 **维持现状** | 关闭（挂账） |
| 拍-新④ | `algo_backtest_compare.py` E 批摘链 | ✅ **你方 `0705` 已执行完毕**，双方一致 | 关闭 |
| 挂账-A | `ulPaneStrong` 内「动量共识筛选」/「H反推」两卡 | ⏸️ 待主人拍板（双方一致不动） | 主人 |
| 挂账-B | P1-4 分数刻度改法（A/B 二选一） | ⏸️ 待主人拍板 | 主人 |
| 挂账-C | P0-1 `_is_low` + P1-2 成本口径 + 离线回测 | ⏸️ 待主人拍板 | 主人 |
| 挂账-D | `max_dd_20d` 风控维度接入 | ⏸️ 方向项，未开工 | 主人 |
| 挂账-E | 融合器权重再定（`final_recommend_*` 仅 14 交易日，建议扩到 40–60） | ⏸️ 时间累积，小九工作日可续 | 主人 |
| 挂账-F | `packed-refs` 僵尸条目 / 陈旧暂存区反向形态 / P-Q 盘中粒度盲区 | ⏸️ 均未变化 | 主人 |

---

## 九、纪律声明（本方本轮）

| 项 | 实测 |
|---|---|
| 代码 / 算法 / 阈值 / 管线 / 前端 / 数据产物改动 | **0** |
| `git add -A` / `reset` / `checkout` 主工作树 | **0 次** |
| 手工 dispatch | **0** |
| DB 操作 | **仅只读**（`mode=ro`，物理不可写） |
| 主工作树 WIP | **未触碰**（全程 worktree 旁路，仅新增本回执 1 文件） |
| 远端取值方式 | **全部 `git show origin/main:<path>`**，未使用本地工作树行号/尺寸 |
| 署名 | **小九的工程师** |

---

*本档仅供工程协作参考，不构成个人投资建议*

*署名：小九的工程师 · 2026-09-20 08:24 CST · 单位机 `lemoncat-cn`*
