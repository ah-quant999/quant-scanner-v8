# HANDOVER — 小九：补全科创板/北交所概念数据

**提交人**：阿狸咪（detect + dispatch）  
**接收人**：小九（生产数据任务，请在单位机/GHA 执行）  
**日期**：2026-08-05 06:40  
**优先级**：P1（影响三重共识/个股查询页面的概念展示）  

---

## 1. 问题现象

`algorithms/stock_industry_concepts.json` 中：

- **科创板**：已映射 610 只，但 **414 只 concepts 为空**（只有 industry/board）。
- **北交所**：**完全未映射**（当前文件中 0 只）。
- 主板/创业板：分别只有 11 / 1 只概念为空，可顺手补，不是重点。

后果：三重共识、个股查询、金股池等页面在展示科创板/北交所股票时，**概念标签缺失**。

---

## 2. 任务目标

补全 `algorithms/stock_industry_concepts.json`：

1. 为 **414 只已映射但 concepts 为空的科创板股票**填充概念列表。
2. 为 **全部北交所股票**新增 `{board:"北交所", industry, concepts}` 记录。
3. （可选）顺手补全 11 只主板 + 1 只创业板的空概念。

最终文件需保持现有格式：

```json
{
  "688001": {"board":"科创板","industry":"计算机、通信和其他电子设备制造业","concepts":["..."]},
  "430047": {"board":"北交所","industry":"...","concepts":["..."]}
}
```

---

## 3. 数据源建议（按优先级）

### 主源：akshare
推荐接口（请按实际可用性选择其一）：

- `akshare.stock_individual_info_em(symbol="688001")` — 东财个股资料页，含概念。
- `akshare.stock_board_concept_cons_em(symbol="688001")` — 个股所属概念板块。
- `akshare.stock_zh_a_spot_em()` 配合 `akshare.stock_individual_info_em()` 批量抓取。

注意：东财 `fs=m:90+t:2` 分类查询在前序验证中被 reset，**不要依赖该接口做运行时补全**；本次批量更新静态文件可以单点请求个股资料页，但要加限速（单点 ≤2s 重试、异常回退）。

### 备选
- `akshare.stock_individual_info_em` 不可用 → 换 `akshare.stock_board_concept_name_ths()` / `akshare.stock_board_concept_cons_ths()` 同花顺概念。
- 同花顺也挂 → 用 Wind / westock-mcp / tdx-connector 等已配置 connector 补最后缺口。

### 港股
本次重点是 A 股科创板/北交所；港股不在本任务范围。

---

## 4. 实现步骤

1. 在单位机或 GHA 临时 workflow 新建脚本 `algorithms/_fill_kcbj_concepts.py`：
   - 读取 `algorithms/stock_industry_concepts.json`。
   - 识别：
     - code 以 `688`/`689` 开头且 `concepts` 为空 → 科创板待补；
     - code 以 `8`/`4`/`92` 开头且不在映射中 → 北交所待新增。
   - 逐只调用 akshare 个股资料接口获取概念。
   - 输出临时 JSON 供人工抽检。

2. 人工抽检 20~30 只（尤其 688 开头高分行业 + 北交所样本），确认概念合理。

3. 合并回 `algorithms/stock_industry_concepts.json`。

4. 验证：
   - 科创板空概念数量 = 0；
   - 北交所数量 > 0；
   - 文件仍能正常 JSON load；
   - `gen_triple_consensus.py`/`gen_triple_track.py` 本地跑一次无异常。

5. 提交到 `quant-scanner-v8` 的 `main`：
   ```bash
   git add -f algorithms/stock_industry_concepts.json
   git commit -m "data: 补全科创板/北交所概念映射"
   git fetch origin && git rebase origin/main
   git push origin main
   ```

6. 触发或等待下一次 `v8_algo_run`（18:30 自动跑），让新映射进入 `triple_consensus.json` / `candidate.json` / `GOLD_POOL.js` 等下游产物。

---

## 5. 验收指标

```text
algorithms/stock_industry_concepts.json
├── 科创板映射数：610（不变）
├── 科创板空概念数：0
├── 北交所映射数：>0
└── 主板/创业板空概念数：尽量 0
```

---

## 6. 纪律提醒

- 这是**生产数据任务**，必须由**小九（单位机）/ GitHub Actions** 执行并推送到 `ah-quant999/quant-scanner-v8` 的 `main`。
- 阿狸咪**不跑数据抓取、不直接推送该 JSON 的生产版本**（当前本机文件仅供分析）。
- 推送前确认 `git add -f`（该文件在 `.gitignore` 白名单内，不加 `-f` 会被忽略）。
- 不要把大段未验证的 akshare 数据直接合并；抽检后再合。

---

## 7. 如有阻塞

如果 akshare 批量获取仍然失败/被封，先暂停并回传日志；不要硬上。阿狸咪会协助评估换源或分段抓取方案。
