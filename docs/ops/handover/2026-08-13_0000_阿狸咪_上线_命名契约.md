# 交接给阿狸咪 · 2026-08-13 盘后上线 + 命名契约

> 发起人：小九（HH 令：都上线 + 交接清楚，命名一开始就要对应，免得事后留尾巴）
> 本机：E:\workspace\quant-scanner-v8 ｜ HEAD=origin/main=9e674665f
> 状态：数据已推送；云端 build run（31686030271）正在把 20 个算法 regenerated data/*.js 自动提交上线

---

## 一、本次已上线内容（小九手动提交 9e674665f）

| 文件 | 内容 | update_time |
|---|---|---|
| `data/STOCK_QUOTE.js` | A股/港股/ETF 收盘快照 9917 只 | 17:05:55 |
| `data/STOCK_RPS.js` | 个股 RPS | — |
| `raw_data/fundamental_quality.json` | 基本面质量 350→405 只 | 17:01:54 |
| `raw_data/stock_quote.json` | 行情源（STOCK_QUOTE 上游） | — |
| `raw_data/stock_rps.json` | RPS 源 | — |
| `raw_data/top10_daily.json` | 每日 top10 | — |
| `raw_data/backtest_comprehensive.json` | 回测 | — |
| `raw_data/cockpit_backtest.json` | 驾驶舱回测 | — |
| `raw_data/history/top10_daily_20260813.json` | 今日 top10 归档 | — |
| `raw_data/history/top10_daily_history.json` | top10 历史 | — |
| `raw_data/build_candidate_debug.json` | 候选构建 debug | — |

**云端 build run 随后会自动追加**（勿手动重复提交，会抢锁）：
`data/FINAL_RECOMMEND_DATA.js` `data/LHB_7D.js` `data/STOCK_STOP_DATA.js` `data/TOP3_TRACK.js`
`raw_data/cockpit_advice.json` `cockpit_tier_recommend.json` `crds_card_data.json` `final_recommend.json`
`gold_pool.json` `inst_trade.json` `lhb_7d.json` `lhb_data.json` `lhb_history.json` `mahoro.json`
`market_alerts.json` `nt_data.json` `sector_fund_flow_trend.json` `sector_rs.json` `sh_fib.json` 等

---

## 二、红线（双方都遵守，避免互相踩）

| 红线 | 小九 | 阿狸咪 |
|---|---|---|
| `index.html` | **绝不**纳入自己提交（cloud build 拥有） | 可改，但改前先看本契约第三节的命名 |
| `data/HEALTH_CHECK.js` | 不碰（cloud build 生成） | 不碰 |
| `algorithms/.scanner.lock` | 不提交（运行时锁） | 不提交（否则卡死对方机器扫描） |
| `algorithms/data/.fetch_log.json` | 不提交（运行时日志） | 不提交 |
| `raw_data/_rps_cache/` | 不提交（算法缓存，云端重建） | 不提交 |
| `v8/*.py` | 提交需 `git add -f`（.gitignore 忽略整目录） | 同左 |

---

## 三、🔴 命名契约（铁律：改 index.html 必须对应，禁止各写各的）

### 3.1 相对时间「更新于」胶囊 —— 全站唯一实现

**任何卡片显示"更新于 今日/昨日/N天前"必须调用下面这个函数，禁止各处自己 `new Date()` 算：**

```
window._fmtAshareRel(ts)        // index.html:568 定义
  → 输入 update_time 字符串(如 "2026-08-13 15:46:07")
  → 输出 "今日 15:46" / "昨日 15:46" / "3天前 15:46" / 具体日期
```

**胶囊渲染器（统一用这个，别再造一个）：**
```
_uBadge(...)                    // index.html:7374 注释确认「全站更新于胶囊统一复用 _uBadge/_fmtAshareRel」
```

### 3.2 各处已用的相对时间变量名（改之前先 grep 全站，别重名/别漏改）

| 变量 | 位置 | 用途 | 状态 |
|---|---|---|---|
| `_fcRel` | index.html:12003 | 最终推荐候选相对时间 | 已上线(39226a17f) |
| `_rcRel(ts)` | index.html:3124 | 共振卡相对时间 | 已上线 |
| `_tcTime` | index.html:8866 附近 | 今日上市**顶部主卡**时间戳徽章（保留，唯一时间戳） | 已上线 |
| `_tcNearTime` / `_tcExtTime` | index.html:8866-8868 | 今日上市**子卡**时间戳 span（已清空，设计上不显示） | 已上线 |
| `_tt` (ttTime) | index.html:8905 | 今日上市子卡时间戳 span（已清空） | 已上线 |

> ⚠️ 上述 3 个子卡 span（`_tcNearTime`/`_tcExtTime`/`_tt`）是**故意清空**的——同一板块只在顶部主卡 `_tcTime` 显示一个"更新于"。阿狸咪改今日上市卡时**不要恢复子卡时间戳**，否则会出现"同一板块多个更新于"的重复 bug（HH 8-13 明确反对过）。

### 3.3 数据字段契约（fetch 端 ↔ render 端必须同名）

**`window.STOCK_QUOTE.stocks[code]`**（index.html 直接读）：
```
{name, price, change, pct, prev_close, open, high, low, volume, amount, snapshot_time, board, industry, concepts}
```
board ∈ {A股/创业板/科创板/北交所/ETF/港股/...}；render 端按 board 决定涨跌幅颜色（红涨绿跌）。

**`window.IPO_DATA.stocks[i]`**（今日上市/中长线候选卡）：
```
{code, name, board, status, total_return, change_pct, latest_price, score, arbitrage_score, fundamental_score}
status ∈ {listed_today(今日上市), tracking(追踪), applying(申购中), pre_listing(待上市)}
```
> 已知坑（见第四节 C）：候选池 `status=tracking` 段**漏了 score/arbitrage_score/fundamental_score 计算**（现全 0/null），且中长线候选出现全港股——改 fetch_ipo_data_v8.py 时务必让 tracking 段也走评分，且 gold_pool 需过滤港股。

---

## 四、已知问题 A/B/C/D（状态，别重复修错方向）

| 问题 | 现象 | 根因 | 状态 |
|---|---|---|---|
| **A** 今日上市转债错位 | 3 只转债 latest=1300 / total_return=1200% 全相等（假数） | `process_bond_listed()` 没设 `board="可转债"`；报价接口挂时 fallback 给 1300 | 待修 fetcher（未动） |
| **B** tracking 段无评分 | 5 只追踪股 score=0/arb=null/fund=null | tracking 段没走 calculate_scores | 待修（未动） |
| **C** 中长线候选全港股 | 5 只全港股(06882/00883/01109/01093/01209) | gold_pool 候选池过滤漏港股 + tracking 无评分 | 根因待查（未动） |
| **D** 三重历史 9 天无记录 | 追踪起始 8-04，之后 0 条 | 待查 history 归档逻辑（是否"严格>0 才写章节"） | 待查（未动） |

> 以上 4 个 HH 已下令"先查后改、不要重复修"，小九只取证未改代码。阿狸咪若要修，先读 `fetch_ipo_data_v8.py` 和 `algorithms/update_triple_resonance_history.py`，改完两端（fetch+render）命名必须对齐第三节。

---

## 五、阿狸咪待办（18:00 后）

1. **装第 2 台 cn runner**（双机冗余）：跑 `docs/ops/scripts/setup_alimi_cn_runner.ps1`，让 `runs-on:[self-hosted,cn]` 自动选中两台机器，午休/夜间中国数据不断档。
2. 装完后 `verify_cn_runner.py --repo ah-quant999/quant-scanner-v8` 确认 ≥2 台 cn runner online。
3. 改 index.html 任何"更新于"/字段显示前，先对照**第三节命名契约**，改完 `Ctrl+F5` 硬刷验证。

---

## 六、验证记录（小九 17:20 推送）

- `git push origin HEAD:main` → `d1043c771..9e674665f` ✅
- `git rev-parse HEAD == origin/main` ✅ 已上线
- 云端 build run 31686030271 进行中，会自动提交 20 个 regenerated data/*.js
- 未提交：index.html / HEALTH_CHECK.js / .scanner.lock / .fetch_log.json / _rps_cache（均按红线）
