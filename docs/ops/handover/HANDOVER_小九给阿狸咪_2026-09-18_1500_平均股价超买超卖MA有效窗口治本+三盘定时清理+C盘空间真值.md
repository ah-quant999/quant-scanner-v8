# 交接档 · 小九 → 阿狸咪 / 下一会话

**撰写人**：小九（白天班 · 中国 IP 单位机）
**撰写时刻**：2026-09-18 15:00（盘后窗口）
**批次主题**：① 平均股价「超买超卖」MA 有效窗口治本 ② 三盘定时清理任务落地 ③ C 盘空间真值纠正

> ⚠️ 本档遵守「**禁止预测式表述**」铁律：所有结论均写成「截至 X 时刻实测 = Y」，凡涉及「后续会怎样」一律改为**可跑的复核命令**。
> 本档**不写自身所在提交的 sha**（写死即过期），需要时按下文命令现取。

---

## 一、批次总览（三个线上提交）

| # | 文件 | 线上字节 | 变更 | 提交主题 | 状态 |
|---|---|---|---|---|---|
| 1 | `cloud_fetch_v8.py` | 231907 | `+34/-0` | `fix(avg-price): MA 有效窗口治本 —— 样本够即用，不再「积累中」` | 已推 main |
| 2 | `index.html` | 1288676 | `+46/-9` | `fix(avg-price): MA 有效窗口治本 —— 样本够即用，不再「积累中」` | 已推 main |
| 3 | `logic.html` | 735087 | `+53/-18` | `docs(logic): 同步平均股价「超买超卖」有效窗口口径` | 已推 main |

**推送方式**：净室直推（GitHub Contents/Blobs/Trees API），**零碰本地工作树**，行尾按仓库约定保持（`index.html` 纯 LF / `logic.html` CRLF 全覆盖）。

### 现取复核命令（替代写死 sha）

```bash
# 取当前 main tip（不写死 sha）
C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe - <<'PY'
import json,urllib.request,ctypes,ctypes.wintypes as wt
class CREDENTIALW(ctypes.Structure):
    _fields_=[('Flags',wt.DWORD),('Type',wt.DWORD),('TargetName',wt.LPWSTR),('Comment',wt.LPWSTR),
              ('LastWritten',wt.FILETIME),('CredentialBlobSize',wt.DWORD),('CredentialBlob',ctypes.POINTER(ctypes.c_byte)),
              ('Persist',wt.DWORD),('AttributeCount',wt.DWORD),('Attributes',ctypes.c_void_p),
              ('TargetAlias',wt.LPWSTR),('UserName',wt.LPWSTR)]
cred=ctypes.POINTER(CREDENTIALW)()
ctypes.windll.advapi32.CredReadW('git:https://github.com',1,0,ctypes.byref(cred))
tok=ctypes.string_at(cred.contents.CredentialBlob,cred.contents.CredentialBlobSize).decode('utf-16-le').rstrip('\x00')
op=urllib.request.build_opener(urllib.request.ProxyHandler({}))
r=urllib.request.Request('https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main',
    headers={'Authorization':'Bearer '+tok,'Accept':'application/vnd.github+json','User-Agent':'wb'})
print('MAIN TIP =', json.loads(op.open(r,timeout=60).read())['object']['sha'])
PY
```

```bash
# 取线上真身字节数（注意：走 Pages 直取，不用 git blobs API——跨境易截断）
curl -s --max-time 60 "https://ah-quant999.github.io/quant-scanner-v8/index.html?t=$(date +%s)" -o /tmp/i.html && wc -c /tmp/i.html
```

---

## 二、平均股价「超买超卖」MA 有效窗口治本

### 2.1 背景与用户令

用户原话：
> 「这个平均股价的超买超卖干嘛要累积够了才算啊？不对吧」
> 「**肯定A，我肯定都选能治本的**」

现象：卡片显示「⏳ 均线积累中 N/20日」，用户看不到任何超买超卖判定。

### 2.2 曾经的 A 线（通达信 880003）—— **已彻底作废**

**截至 2026-09-18 实测证据**：

| 源 | 请求 | 实测结果 |
|---|---|---|
| 东方财富 `push2his.eastmoney.com` | K 线接口 | ❌ `RemoteDisconnected`（本机 + 云端**均被断连**） |
| 东方财富 `push2.eastmoney.com` | 实时接口 | ❌ 同上 |
| 东方财富 `push2delay.eastmoney.com` | K 线接口 | ⚠️ 可达，但返 `klines:[]`（**不支持该码**） |
| 东方财富 `push2delay` 实时快照 | `secid=1.000003` | ⚠️ 可达但 `rc:100 data:null` |
| 新浪财经 | `880003` | ⚠️ 返回空 |
| 腾讯 `qt.gtimg.cn` | `880003` | ⚠️ **无此码** |

**根因**：`880003` 是**通达信自有编码**（非交易所标准代码），**无任何公开 HTTP 源**。
云端跑最新代码后取产物实测 → **零个 880003 字段**（`880003` count = 0）。

**结论：A 线作废，不要再去试。**

### 2.3 腾讯逐只回填方案 —— **也失败**

尝试：用 `web.ifzq.gtimg.cn` 逐只取全 A 股历史，自算等权平均股价。

失败证据：
- 单只请求耗时 0.27s 可行
- 12 线程跑 5917 只 ⇒ 约 **5000 次请求后被封 IP（HTTP 501）**
- 成功率从 **100% 断崖跌到 15%**

**结论：腾讯源禁批量高频，该方案废弃。** 产出脚本 `tmp/backfill_avg_price.py` 已标废。

### 2.4 最终方案（真治本）：**MA 有效窗口机制**

**核心思想**：不再强行等 20 天。**样本够多少天，就用多少天的真实历史算术均值**，绝不外推填充。

数据层（`cloud_fetch_v8.py`）新增：

```python
_MA_LADDER = ((60, "MA60"), (20, "MA20"), (15, "MA15"), (10, "MA10"), (5, "MA5"))
ma_eff = ma_eff_window = None
for _w, _nm in _MA_LADDER:
    if len(prices) >= _w:
        ma_eff = round(sum(prices[-_w:]) / _w, 4)
        ma_eff_window = _nm
        break
pos_eff = (avg_price - ma_eff) / ma_eff * 100 if ma_eff else None
ma_short = {}
for _w, _nm in _MA_LADDER:
    if len(prices) >= _w:
        ma_short[_nm.lower()] = round(sum(prices[-_w:]) / _w, 4)
```

返回值新增 5 个字段：

| 字段 | 含义 |
|---|---|
| `ma_eff` | 当前生效窗口的均线值（真实历史算术均值） |
| `ma_eff_window` | 生效窗口名，如 `"MA15"` |
| `position_vs_ma_eff` | 现价相对生效均线的偏离（%） |
| `ma_short` | 所有已满足窗口的均线字典，如 `{"ma5":..,"ma10":..,"ma15":..}` |
| `ma_eff_ready` | 是否已可用 |

**旧字段保留**（`ma20` / `ma60` / `position_vs_ma20` / `position_vs_ma60` / `ma20_ready` / `ma60_ready`），保证向后兼容。

### 2.5 🔴 关键：阈值必须按窗口缩放（含一次方向纠错）

**原理**：短窗口均线更贴近现价 ⇒ 偏离的**绝对值更小**。

**实测平均绝对偏离**（截至 2026-09-18）：

| 窗口 | 平均绝对偏离 |
|---|---|
| MA5 | 1.27% |
| MA10 | 1.63% |
| MA15 | 2.44% |

若沿用 MA20 的 ±3/±5 阈值，则 MA15 永远触发不了。

**系数 = 窗口 / 20**（短窗口 ⇒ 系数 <1 ⇒ 阈值更小）：

| 窗口 | 系数 | 偏高(超买) | 高位(超买) | 偏低(超卖) | 低位(超卖) |
|---|---|---|---|---|---|
| MA5 | 0.25 | > +0.75 | > +1.25 | < −0.75 | < −1.25 |
| MA10 | 0.50 | > +1.50 | > +2.50 | < −1.50 | < −2.50 |
| MA15 | 0.75 | > +2.25 | > +3.75 | < −2.25 | < −3.75 |
| MA20 | 1.00 | > +3.00 | > +5.00 | < −3.00 | < −5.00 |
| MA60 | 3.00 | > +9.00 | > +15.00 | < −9.00 | < −15.00 |

> 🔴 **踩坑记录**：首版误写 `var _k = 20/_wNum`（方向**反了**，系数>1 ⇒ 阈值变大），MA15 阈值算成 4.0，真实偏离 2.44% 仍不触发。
> **被真实渲染验证当场打回**，后修正为 `var _k = _wNum/20`。
> **教训：任何阈值/系数改动，必须用真实数据反算验证，不能凭直觉。**

前端（`index.html`）判定源三级回退 + 阈值缩放：

```js
var _pEff=ap.position_vs_ma_eff, _effW=ap.ma_eff_window;
var _p20=ap.position_vs_ma20, _p60=ap.position_vs_ma60;
var _pv  = (_pEff!=null) ? _pEff : ((_p20!=null) ? _p20 : _p60);
var _pvT = (_pEff!=null) ? ('·'+(_effW||'')) : ((_p20!=null) ? '·MA20' : (_p60!=null ? '·MA60' : ''));
var _wNum = 20;
if(_pEff!=null){
  var _mm=String(_effW||'').match(/(\d+)/);
  if(_mm) _wNum = Math.max(5, Math.min(60, parseInt(_mm[1],10)));
} else if(_p20==null && _p60!=null){ _wNum = 60; }
var _k = _wNum/20;              // 🔴 窗口/20（短窗口→阈值更小）
var _t1 = 3*_k, _t2 = 5*_k;
```

未就绪阈值也从 20 降到 **5**：`if(!_verdict){ _ovTxt='⏳ 均线积累中 '+_hd+'/5日（满15/20日自动升档）'; }`

### 2.6 验证链（全部通过）

| 验证项 | 方法 | 结果 |
|---|---|---|
| JS 语法 | 抽内联 `<script>` 跑 `node --check`（28 个块） | ✅ 0 错 |
| 有效窗口逻辑 | 7 组用例（样本数 3/5/10/15/20/60/61） | ✅ 全过 |
| 阈值缩放 | 8 项断言（各窗口 ±t1/±t2 边界） | ✅ 全过 |
| **真实数据渲染** | Node vm sandbox 加载真实产物 | ✅ **「🟠 偏高·超买·MA15」**（修复前为「积累中」） |

---

## 三、三盘定时清理任务

### 3.1 用户令

> 「我选这个，然后**每天定时清理的任务三个盘都要加上，不准动我工作的文件！**」

### 3.2 落地方式：**复用而非重写**

发现已有自动化 `automation-0c743285`「磁盘垃圾每日清理（C/D/E 盘·每晚23:00）」**早已 ACTIVE**，脚本 `C:\Users\Administrator\.workbuddy\scripts\disk_clean_weekly.py` 更成熟（含 `OFFICE_EXT` 办公文档硬保护 + reparse point 防护 + `hard_rm` 真删引擎）。
⇒ **删掉自己新写的 `v8_disk_cleanup.py`，改为给成熟脚本补缺口。**

### 3.3 脚本三处改进（截至 2026-09-18 实测）

**① 新增批 7 —— 覆盖真正的大头**

```python
_WB_CACHE_DIRS = ("traces", "logs", "file-history", "file-tree-manifests")
```

- 目录级白名单**硬锁**：`os.walk` 起点即白名单根 + 循环内二次 `realpath` 前缀校验，防目录穿越/软链逃逸
- **只删单个文件，绝不删目录本身**（保留桶结构，服务下次写入）
- 年龄闸门 >7 天 + 办公文档硬保护 + reparse point 跳过，与批 1~6 同标准
- 注册：`ALL_DEFAULT = ["0"..."7"]` + `BATCHES["7"] = (...)`

**② 预算分档**（原 900s 不够）

```python
# 实测 2026-09-18：批7 有 42051 条小文件，900s 只跑完 3848 条 ⇒ 提到 3600s
if k in ("4", "7"):
    budget = time.time() + 3600
else:
    budget = time.time() + 900
```

**③ 按体积降序**

```python
items = sorted(items, key=lambda x: -x[1])   # 预算耗尽时保证空间实打实释放
```

**④ 修 `collect()` 跨盘崩溃**：`os.path.relpath(rfp, WB)` → `os.path.relpath(rfp, rroot)`
（原因：对 E 盘路径求相对 C 盘 ⇒ `ValueError: path is on mount 'E:', start on mount 'C:'`）

### 3.4 实测释放成果

| 批次 | 释放 | 备注 |
|---|---|---|
| 批 7（两轮） | 约 **600 MB** | 仍在续删中 |
| 批 4（E:\Temp） | **154 MB** | **跳过 1 条含办公文档（保护生效 ✅）** |

### 3.5 红线（用户明确要求，不得突破）

- ❌ **绝不触碰工作文件**：`OFFICE_EXT` 保护 `.doc/.docx/.xls/.xlsx/.ppt/.pptx` 等，>64KB 一律跳过
- ❌ **绝不删目录本身**，只删过期文件
- ❌ **绝不越界**：目录级白名单 + `realpath` 前缀双校验
- ❌ **真删不进回收站**：同盘回收站**不释放空间**，必须 `ctypes DeleteFileW` 永久删除
- ❌ safe-delete-bulk-guard：单轮 ≥50 文件即 `exit 2` 中止整批

### 3.6 任务名（已更新）

> 磁盘垃圾每日清理（C/D/E 盘全覆盖·每晚23:00·真删不进回收站·工作文件硬保护）

---

## 四、🔴🔴 C 盘空间真值纠正（重大教训）

**用户三次纠正**，最终定论：
> 「**你整个弄反了！C盘不宽，D盘最宽！**」

### 4.1 错误根源

`shutil.disk_usage()` 返回的是 named tuple，**官方文档明确顺序为 `(total, used, free)`**。
我连错三次：
1. 按 `(total, free, used)` 解包 ⇒ 结论整体反转
2. 打印表头把「已用/可用」两列贴反 ⇒ **顺着错表头读出「C 盘宽裕」**，且与自己结论矛盾
3. 向用户复述时又反一次

### 4.2 🔴 铁律

> **唯一可信：属性名访问 `.total` / `.used` / `.free`，绝不按位置解包。**

```python
u = shutil.disk_usage("C:\\")
print(u.total, u.used, u.free)   # ✅ 属性名
# t, f, u = shutil.disk_usage(...)   # ❌ 禁止按位置解包
```

### 4.3 三盘真值（2026-09-18 实测）

| 盘 | 总 | 已用 | **可用** | 判定 |
|---|---|---|---|---|
| **C:** | 100.00 GB | 77.40 GB | **22.60 GB** | 🔴 **最紧** |
| **D:** | 183.00 GB | 28.24 GB | **154.76 GB** | ✅ **最宽** |
| **E:** | 181.63 GB | 58.33 GB | **123.29 GB** | 中等 |

**现取命令**：

```bash
C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe -c "
import shutil
for d in ['C:\\\\','D:\\\\','E:\\\\']:
    u=shutil.disk_usage(d)
    print(f'{d} total={u.total/2**30:.2f} used={u.used/2**30:.2f} free={u.free/2**30:.2f}')
"
```

---

## 五、待续 / 复核项

| # | 项 | 状态（截至 2026-09-18 15:00） | 复核命令 |
|---|---|---|---|
| 1 | 云端产物出现 5 个新字段 | ⏳ **未完成**——线上 `AVG_PRICE_DATA.js` 五字段 count 全 0。原因：`AVG_PRICE_DATA.js` 由云端跑，最新 run `35315463560` 的 `head_sha=0293696ba161`（**早于我推送**）⇒ 用旧代码。已 dispatch 新 run（HTTP 204）基于最新 tip 重跑 | 见 5.1 |
| 2 | 批 7 续删 | ⏳ 进行中（第 3 轮） | `tmp/_go7c.txt` |
| 3 | 前端 / 逻辑页线上生效 | ✅ **已完成**——`index.html` 1288676B / `logic.html` 735087B，与本地补丁字节数完全一致；`ma_eff_window`×1 / `position_vs_ma_eff`×2 / `_hi1`×0 / `_lo1`×0（旧判定代码级清零） | 见 5.2 |

### 5.1 验证云端 5 字段（本档不预测，只给命令）

```bash
C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe -c "
import urllib.request,time
op=urllib.request.build_opener(urllib.request.ProxyHandler({}))
u='https://ah-quant999.github.io/quant-scanner-v8/data/AVG_PRICE_DATA.js?t=%d'%int(time.time())
b=op.open(urllib.request.Request(u,headers={'User-Agent':'Mozilla/5.0'}),timeout=60).read().decode('utf-8','replace')
for k in ['ma_eff','ma_eff_window','position_vs_ma_eff','ma_short','ma_eff_ready']:
    print(k, b.count(k))
print(b[:400])
"
```

**判定标准**：五个字段 count 均 ≥1，且 `ma_eff_window == \"MA15\"`（当前样本量 15 条时）。

### 5.2 验证前端 / 逻辑页线上生效

```bash
C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe -c "
import urllib.request,time,hashlib
op=urllib.request.build_opener(urllib.request.ProxyHandler({}))
for n in ['index.html','logic.html']:
    u='https://ah-quant999.github.io/quant-scanner-v8/'+n+'?t=%d'%int(time.time())
    b=op.open(urllib.request.Request(u,headers={'User-Agent':'Mozilla/5.0'}),timeout=60).read()
    print(n, len(b), hashlib.md5(b).hexdigest())
"
```

**判定标准**：`index.html` = 1288676B / `logic.html` = 735087B。

### 5.3 尚未动的项（明确声明未做，不含预测）

- **P2**：`SH_FIB` 类别口径统一 —— **本档撰写时未动**
- **P3**：D 批后验证（今天 15:05 盘后）
- **B 线**：`FOUR_VOLUME`（四量终极）命中 0 只的真伪验证

---

## 六、本轮新踩坑速查（供下一会话避雷）

| # | 坑 | 症状 | 正解 |
|---|---|---|---|
| 1 | `shutil.disk_usage` 位置解包 | 结论整体反转，连错三次 | **只用属性名** `.total/.used/.free` |
| 2 | `len(str)` ≠ 字节数 | `assert len(src)==229489` 误报 `183536`（中文 3 字节） | 用 `os.path.getsize()` |
| 3 | HTML 不能用 `ast.parse` | `SyntaxError: leading zeros in decimal integer literals` | 抽内联 script 跑 `node --check` |
| 4 | CRLF 文件匹配串用 `\n` | `logic.html` 五处补丁**全 0 命中** | 先 `t.replace("\r\n","\n")` 匹配，落盘再还原并断言裸 LF=0 |
| 5 | 腾讯源批量高频 | 约 5000 请求后**封 IP（HTTP 501）** | 该方案废弃 |
| 6 | `collect()` 跨盘 relpath | `ValueError: path is on mount 'E:', start on mount 'C:'` | 相对**白名单根**求相对路径 |
| 7 | 批 7 预算不足 | 900s 只跑完 3848/42051 条 | 提到 3600s + **按体积降序** |
| 8 | GitHub `runs?per_page=40` | `IncompleteRead`（跨境半程截断） | 循环 `read(65536)` + 捕获 `e.partial` 拼接；`per_page` 降到 20-25 |
| 9 | Bash PATH 崩坏 | `mkdir`/`ls`/`sleep`/`dirname`/`head` 全 `command not found`（127） | 改用 Python `os.makedirs` + 绝对路径 |
| 10 | `wmic` 被安全策略拦截 | `PROGRAM BLOCKED BY SECURITY POLICY` | 改用 `tasklist` + `grep -i` |
| 11 | `--batches` 参数名错误 | `unrecognized arguments: --batches 7` | 正确参数是 **`--batch`**（单数） |

---

## 七、给阿狸咪的明确事项

1. **`AVG_PRICE_DATA.js` 与本轮三个提交无冲突**，属纯新增字段（旧字段全保留），**无需回避**。
2. **批 7 任务**：脚本 `C:\Users\Administrator\.workbuddy\scripts\disk_clean_weekly.py` 属**小九机专属路径**，阿狸咪机上若有同名任务请勿照搬路径，需各自确认本机 `.workbuddy` 目录位置。
3. **C 盘吃紧是事实**（可用 22.60GB），清理任务**已挂在每晚 23:00**，阿狸咪夜班时段（18:00–次日 7:30）如遇 C 盘告警，可直接跑：
   ```bash
   python C:\Users\Administrator\.workbuddy\scripts\disk_clean_weekly.py --batch 7 --go
   ```
4. **不要把「脚本改了」当成「产物已重跑」**——本轮 `cloud_fetch_v8.py` 改了但云端产物尚未跟上（见第五节第 1 项），这是两件事，须分开陈述。
