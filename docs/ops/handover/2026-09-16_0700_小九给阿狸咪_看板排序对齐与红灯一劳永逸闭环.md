# 交接 · 看板排序对齐 + 红灯一劳永逸（小九 → 阿狸咪 / 双机）

> 生成时间：2026-09-16 06:55（小九值班）
> 基线 tip：`89093e0071a4feb20bfb00bc23878f92402aa197`（`v8 build: 2026-09-16 06:30`）
> 本轮我的提交：`9edeb72fd1`（ETF 卡名）· `01efe7566e11`（看板改造）

---

## 一句话结论

主人两条令已**全部闭环并在线上实测通过**：

1. **ETF 卡名去掉「二合一」** —— 卡标题已从 `💰 ETF 二合一 - 盘中异动 + 主力净额分时` 改为 `💰 ETF 盘中异动 · 主力净额分时`（index.html 6 处 + logic.html 5 处，共 11 处）。
2. **看板跟着最新导航重排 + 红灯灰灯一劳永逸** —— 分组/组内顺序已按导航 TAB 真序与页面真实卡序重排；红灯 **2 → 0**，灰灯 **1 → 0**（渲染层过滤）；后端巡检 `fail: 0`、`overall: ok`。

**阿狸咪请注意**：这两批改动都落在 `index.html` 的**热点函数区**（`renderHealthCheck()` 与 ETF 卡区），而阿狸咪在同一时段频繁改同文件。**本批已线上稳定约 1.5 小时且经真身核验未被覆盖**，但请在后续改动前先 `git pull` 对齐，避免重演 09-15 `e49e106b8` 的覆盖事故（当时把 A/B/C 分档折叠覆盖掉了）。

---

## 一、ETF 卡名改动（提交 `9edeb72fd1`）

### 为什么改

「二合一」是**历史命名残留**：该卡原为 Block1+Block2+Block3 三块拼装，此后 Block2（资金热度·净申赎）、Block3（日监控·主力净流入）已先后迁出至「盘后数据」页，卡内只剩两块，且父标题与卡内两个子块标题（`⚡盘中异动·量能脉冲` / `📈主力净额分时累计曲线`）**语义重复**。主人令：卡名不再作块数描述。

### 改动清单（11 处）

| 文件 | 位置 | 改前 | 改后 |
|---|---|---|---|
| `index.html` | L2409 卡标题 | `💰 ETF 二合一 - 盘中异动 + 主力净额分时` | `💰 ETF 盘中异动 · 主力净额分时` |
| `index.html` | L2402 区块注释 | （无） | +2 行改名历史说明 |
| `index.html` | L10457 盘面分析注释 | 旧名 | 新名 |
| `index.html` | L11452 数据源 note | 旧名 | 新名 |
| `index.html` | L12606 渲染入口注释 | 旧名 | 新名 |
| `index.html` | L2722 迁出说明注释 | 旧名 | 新名 |
| `logic.html` | L992 表格行 | `<tr><td>ETF 二合一</td>` | `<tr><td>ETF 盘中异动 · 主力净额分时</td>` |
| `logic.html` | L1019/4644/5528/6398 | 注释与 note | 新名 |

> ✅ **线上实测**：`grep -c 'ETF 盘中异动 · 主力净额分时'` = 2（卡标题 + 数据源 note）；`phEtfTime` 卡头确为新名。
> ⚠️ 线上仍能搜到 1 处「二合一」——**是本次新写的注释**（`📝 2026-09-16 主人令：卡名由「ETF 二合一 - …」改为「ETF 盘中异动 · 主力净额分时」`），属改名记录，**非卡名残留**，勿误判为未改净。
> 📁 `docs/ops/*` 历史归档文档中的旧名**有意不动**（归档不改）。

---

## 二、看板排序对齐（提交 `01efe7566e11`）

### 根因

组内顺序原先**只按后端 `it.order`**（= `v8_health_check.py::CARD_DEFS` 的登记序），**与页面真实卡序无关**，导航改版后必然错位。

### 🔴 关键坑：巡检项 `name` ≠ 页面卡名 —— 必须用 `id`

第一版补丁按 `name` 关键字匹配，**渲染后纹丝不动**（静默回落 `it.order`）。实测失配：

| id | 巡检里的 name | 页面上的卡名 |
|---|---|---|
| `INDEX_QUOTES` | 全球指数 / 股指期货 | **指数快览** |
| `SH_FIB` | 市场温度计 | **市场宽度** |

⇒ **排序表一律用 `id` 建**（id 稳定，name 会漂）。

### 改动（`index.html` 三处）

**① 新增 `_PAGE_CARD_ORDER`（id 序真源，6 组）**

```js
const _PAGE_CARD_ORDER = {
  '实时数据': ['INDEX_QUOTES','CFFEX_HOLDINGS','LIMIT_UP_HEATMAP','ETF_PULSE','ETF_INTRADAY_HEAT',
               'SECTOR_FUND_FLOW','CONCEPT_RANKING','MARKET_FUND_FLOW_DATA','MARKET_ALERTS','AVG_PRICE_DATA'],
  '盘后数据': ['SH_FIB','ETF_DAILY_MONITOR','MARGIN_DATA','FACTOR_LAB','FOUR_VOLUME_BACKTEST',
               'CRISIS_DATA','MARKET_REGIME','CANDIDATE','GOLD_POOL','LHB_DATA','INST_TRADE',
               'TRIPLE_CONSENSUS','SECTOR_RECOMMENDATION'],
  '选股策略': ['GOLD_POOL','CANDIDATE','TRIPLE_CONSENSUS','FOUR_VOLUME','CRDS_CARD_DATA','FOUR_VOLUME_60M',
               'ALGO_TRACK','TRIPLE_HISTORY','FOUR_VOLUME_HISTORY','FOUR_VOLUME_TRACK','VOLATILITY',
               'STOCK_STOP_DATA','H_AUTO_BUY','H_AUTO_BUY_TRACK'],
  '今日事件': ['JUDGMENT_DATA','MACRO_DATA','IPO_DATA','V8_CAL'],
  '策略回测': ['BACKTEST_ALL_ALGOS'],
  '运维': ['DO_NOT_DELETE','UNLISTED_PANEL'],
};
```

**② 组内排序改走 `_cardRank`（未列入的自动排末尾，防新增卡掉队）**

```js
var _cardSeq = _PAGE_CARD_ORDER[page] || [];
function _cardRank(it){
  var _ix = _cardSeq.indexOf(it.id);
  if(_ix >= 0) return _ix;
  return 1000 + (it.order == null ? 999 : it.order);   // ← 未列入项的兜底位次
}
byPage[page].slice().sort(function(a,b){
  var ra = _cardRank(a), rb = _cardRank(b);
  if(ra !== rb) return ra - rb;
  return (a.order==null?1e9:a.order)-(b.order==null?1e9:b.order);
}).forEach(function(it){ ... });
```

> ✅ 上表已逐字比对 `origin/main` 实际内容（取法见第五节），可直接使用。

**③ CI 环境 info 项渲染层过滤（双保险）**

```js
if(Array.isArray(H.items)){
  H.items = H.items.filter(function(it){
    if(it.status !== 'info') return true;
    return !/CI\s*环境|语义不成立/.test(String(it.message || ''));
  });
}
```

### 排序效果（真实渲染实测）

**分组顺序**（＝导航 TAB 真序的子集）：
今日事件 → 实时数据 → 盘后数据 → 选股策略 → 策略回测 → 运维 → 全量数据 → 管线 → 内容审计

导航 `data-tab` 真序（`index.html` `<nav>`）：
🌍 观测平台 → 📅 今日事件 → 📡 实时数据 → 🌙 盘后数据 → 🧲 共振日历 → 🏆 最终推荐 → 🔍 个股查询 → 🎯 选股策略 → 🧪 策略回测 → 🔒 暂未上架 → 🔒 已下架 → 🔒 运维 → 🔒 逻辑详解

**组内顺序**（抽样）：

- 实时数据 10 项：全球指数/股指期货 → 股指期货持仓 → 涨停热度 → ETF 盘中异动 → ETF 资金热度 → 板块资金流向 → 概念排名 → 市场资金流向 → 市场预警 → 平均股价
- 盘后数据 13 项：市场温度计 → ETF 日监控 → 融资融券 → 因子实验室 → 四量终极回测 → 危机雷达 → 市场利率环境 → 候选池 → 黄金池 → 龙虎榜 → 机构买卖 → 三重共识 → 板块推荐
- 选股策略 11 项：四量终极 → 逆势龙头 → 四量终极60m → 算法追踪 → 三重历史 → 四量历史 → 四量跟踪 → 波动率 → 精确止损止盈 → 精确自动买入(H反推) → 精确自动买入追踪

---

## 三、红灯灰灯一劳永逸（同批 `01efe7566e11`）

### 两个红灯的真根因：不是「该更新」，而是「不该存在」

```
2026-09-14 主人令：候选池 / 金股池是算法上游水源，不是策略 ⇒ 不需要回测
  └─ 当时已做：摘除 CARD_DEFS 登记 + 删除 algorithms/backtest_pools.py
  └─ 🔴 漏做：两份产物 data/CANDIDATE_BACKTEST.js / GOLD_POOL_BACKTEST.js 没删
       ⇒ 残留文件被 check_all_data_files() 通用扫描按 1440min 红线判 fail
```

### 处理

| 动作 | 内容 |
|---|---|
| 删 4 个孤儿文件 | `data/CANDIDATE_BACKTEST.js`、`data/GOLD_POOL_BACKTEST.js`、`raw_data/candidate_backtest.json`、`raw_data/gold_pool_backtest.json` |
| 加护栏 | `v8_health_check.py::_LOW_FREQ_FILES` 增 `"CANDIDATE_BACKTEST", "GOLD_POOL_BACKTEST"`（防旧产物被放回致红灯复发） |
| CI 灰灯 | 前端渲染层过滤 `status==='info' && /CI 环境|语义不成立/`（`local_sync` 在 CI 一次性浅克隆内无意义） |

> 🔒 **护栏不是「降红线」**：`_LOW_FREQ_FILES` 只影响 `check_all_data_files()` 通用扫描；这两条**已从 `CARD_DEFS` 摘除**，不再走 `check_data_cards` 的 `max_age` 判定。加护栏的目的是「即使有人把旧产物放回，也不该冒红灯」——因为它们本就不该存在。这与 09-11 `FOUR_VOLUME_60M` 移出白名单是**相反方向**的处置，勿混淆。

### 闭环终验（后端巡检已重跑 · 2026-09-16 05:11:47）

线上 `data/HEALTH_CHECK.js` 真实解析：

```json
{"updated":"2026-09-16 05:11:47","overall":"ok",
 "summary":{"ok":112,"warn":0,"fail":0,"limited":1,"info":1,"other":0,"total":114}}
```

| 层 | 改前 | 改后 | 判据 |
|---|---|---|---|
| 后端 `summary.fail` | **2** | **0** | 线上文件解析 |
| 前端红符号 `‼` | 2 | **0** | jsdom 加载线上真身渲染后计数 |
| 孤儿回测红灯 | 2 条 | 消失 | `CANDIDATE_BACKTEST` / `GOLD_POOL_BACKTEST` / 「候选池回测」渲染后零命中 |
| CI 环境灰灯 | 占位 | 已过滤 | 「CI 环境」「语义不成立」「local_sync」零命中 |
| 渲染项数 | 114 | 113 | 114 − 1 = 113，账目自洽 |

**剩余 2 条非 ok 均为设计如此，请勿当故障处理**：

| 状态 | id | 页面 | 说明 |
|---|---|---|---|
| `limited` | `UNLISTED_PANEL` | 运维 | 由 `scripts/build_unlisted_panel.py` **手动**生成（AI 策划实验模块去向，主人推送）⇒ 本就手动项 |
| `info` | `local_sync` | 管线 | CI 一次性浅克隆内语义不成立 ⇒ 前端已过滤，不占位 |

---

## 四、三层防复发保险（请勿单方面撤销）

| # | 层 | 位置 | 防什么 |
|---|---|---|---|
| 1 | 后端白名单护栏 | `v8_health_check.py::_LOW_FREQ_FILES` | 防旧产物被放回导致红灯复发 |
| 2 | 前端 id 序表 | `index.html::_PAGE_CARD_ORDER` + `_cardRank` | 防导航再改版后看板再次错位；未列入 id 自动排末尾 ⇒ 防新增卡掉队 |
| 3 | 前端渲染层过滤 | `index.html::renderHealthCheck()` 的 items filter | 防 CI 环境项在云端/盘后快照里复活占位 |

> ⚠️ 若后续因页面改版需调整顺序：**只改 `_PAGE_CARD_ORDER` 的 id 列表**即可，不要去改 `it.order`（那是后端登记序，动它会牵连 `CARD_DEFS`）。

---

## 五、验证方式（可复现，非转述）

| 验证 | 方法 |
|---|---|
| 线上文件真身 | **切不可用** `/contents/<path>`（>1MB 返回**空 content**）⇒ 走 `git/trees?recursive=1` 拿 blob sha → `git/blobs/<40位全sha>`。🔴 **传 12 位短 sha 会被 422 硬拒**（`The sha parameter must be exactly 40 characters`） |
| 逆运算还原 | 补丁落盘后把 new→old 反着替换回来，`assert rev == old`（逐字节）⇒ 证明只做了定点替换 |
| 真实渲染 | jsdom 加载**线上真身** `index.html` + 注入**线上真身** `HEALTH_CHECK` → 调 `renderHealthCheck()` → 断言分组序 / 组内序 / 过滤项 0 渲染；必须 `process.exit(0)`（页面有 setInterval 会挂死） |
| 样式锚点（稳定） | 分组标题尾巴 `letter-spacing:.3px;`；组内项名 `font-size:12px;font-weight:600;color:var(--txt);">` |
| 🔴 反推断纪律 | **不要**从 build job 的 `steps[]` 反推 `health_check` 何时跑。实测 run `35023893818` 的 build job **只有 8 个业务步、根本没有 health_check 步** ⇒ 该文件由**另一条巡检链**写入。判「重跑没」**只看 `data/HEALTH_CHECK.js` 自身 `updated` 时间戳** |

脚本留档：`E:/v8data/qs8-rename/verify_final.js` + `extract_order.py`（本机小九侧，未入库）。

---

## 六、给阿狸咪的待办 / 提醒

1. **🔴 `index.html` 是本轮双机共写热点**：本单两批改动都在里面（`renderHealthCheck()` 与 ETF 卡区）。阿狸咪后续改 `index.html` 前**务必先 `git pull`**，`git push` 前再核一次 tip 是否前进；**绝不 force**。
2. **阿狸咪 05:20 提到 `AVG_PRICE_DATA.js` 隐患**（`ma20 === ma60` 在 `ma20_ready=false` 时仍写同一数字）——**我未改数据层**（同意其判断：前端已不展示，但建议数据层未就绪时写 `null`）。此条**仍挂待办**，归属数据层，请阿狸咪或后续批次接手。
3. **阿狸咪 06:03 提到 `__v8RichLines` 是热点公共入口**（`renderMahoroCard()` 内 `_richSplitLabels()` + `renderLines()`，挂载 `window.__v8RichLines`）——我**本轮未碰该函数**，无冲突。
4. **本机（小九）临时工作区**：`E:/v8data/qs8-rename/`（未入库，勿同步）。

---

## 七、推送信息

| 项 | 值 |
|---|---|
| 方式 | GitHub **Git Data API**（blob → tree `base_tree` → commit 带 `parents:[tip]` → PATCH ref `force:false`），**全程未用 `git push`** |
| 防并发 | 三次遇到远端 tip 前进（`df603aaf26`→`569985ef0f`→`a8331c521f`→`f9a66d8062`→`702a4b6fc9`→`01efe7566e11`），**每次均在新 tip 上重施加改动**后再推 |
| 提交 | `9edeb72fd1`（ETF 卡名：`index.html` + `logic.html`）<br>`01efe7566e11`（看板改造：`index.html` + `v8_health_check.py` + 删 4 孤儿文件） |
| 线上核验 | `index.html` = **1194811** 字节；`_PAGE_CARD_ORDER`×3 / `_cardRank`×1 / CI 过滤层×1 全命中；`_LOW_FREQ_FILES` 护栏在位 |
| 中文 commit message | ⚠️ 走 Contents API PUT 时中文 message 会**触发 500 空 body**；走 Git Data API 无此问题（但若用 PUT，须换 ASCII） |

---

## 八、主人令存档（本批依据）

| 时间 | 原话 | 处置 |
|---|---|---|
| 09-16 | 「这卡名字不能再二合一了，改一下」 | ETF 卡名去「二合一」（11 处） |
| 09-16 | 「这个看板的前端健康巡检，导航页这两天变化很大，这个也要跟着最新的导航自己最新的子页面里的顺序重新排序。然后现在还有几个红灯灰灯，一劳永逸修复。」 | 本单第二节 + 第三节 |
| 09-15 | 「以后改完审计完就可以直接上线，不用一直问我了」 | 已固化为项目铁律 §2 第 8 条：改动 + 审计全过 ⇒ 直接推 main，不再逐项问主人；仍走自愈推送 + 防覆盖重试，绝不 force |
