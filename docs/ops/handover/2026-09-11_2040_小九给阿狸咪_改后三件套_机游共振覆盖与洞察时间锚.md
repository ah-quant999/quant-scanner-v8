# 2026-09-11 20:40 小九给阿狸咪 · 改后三件套对齐交接（机游共振游资覆盖 + 每日洞察时间戳）

> 主人 20:24 起两令：
> ① 「机游共振的游资怎么又不写知名游资那些了？被覆盖了？昨天还有——一劳永逸防覆盖。」
> ② 「每日洞察这些数据其实是昨天的，但是你的更新于是跟着 2 个共振日历以前刷新的——一劳永逸式修复。」
> ③ 「改后三件套 = 审计+对齐云端和双机的任务、对齐运维页和逻辑详解页的写入+交接给另一台机。不要再弄错了！」
>
> 本交接严格按三件套口径重写。两改均已落库（`algorithms/stage_to_raw.py` / `index.html` / `logic.html` / `raw_data/lhb_history.json`）。

## ① 审计 + 对齐云端和双机的任务

### 今日改动清单（已本地落库，待 push）
| # | 改动 | 文件 / 行 | 性质 |
|---|---|---|---|
| ① | `append_lhb_to_history()` 新增 `_quality()` 评分；覆盖判定改为 `new_quality > old_quality` 才覆盖；骨架数据始终允许覆盖 | `algorithms/stage_to_raw.py:132,154,183` | 一劳永逸·防覆盖 |
| ② | `_save_json()` 统一 `newline='\n'` + `indent=1`，规避 Windows CRLF 与仓库 LF+1 空格缩进冲突 | `algorithms/stage_to_raw.py:109` | 工具链修正 |
| ③ | `renderInsight()` 时间戳「取最大」→「取最小」；LHB/NF 改用 `data.date` 锚定交易日 | `index.html:3861-3921` | 一劳永逸·时间锚定 |
| ④ | LHB/NF 非今日数据时段胶囊降级「盘后」 | `index.html:3916-3921` | UI 同步 |
| ⑤ | 09-11 当日 `lhb_history` 条目重跑覆盖（质量 117→291），游资别名恢复 | `raw_data/lhb_history.json` | 数据回填 |
| ⑥ | logic.html 双向卡片写入（每日洞察时间戳锚定 / 龙虎榜 richer 覆盖机制） | `logic.html:1160, 1330` | 文档同步 |
| ⑦ | 运维页（index.html opTab1）写入今日两改 note，与 logic.html 对齐 | `index.html:6496-6499` | 文档同步 |

### 云端 / 双机任务对齐审计
- **算法链节奏不变**：本次修复不新增/不删任何 workflow、不动 cron、不动 batch 节奏。A 批 `fetch_lhb.py` → `stage_to_raw.py` → 写 `raw_data/lhb_data.json` + `data/LHB_DATA.js` + 追加 `lhb_history` 的链路完整保留。
- **闸门无需变更**：`v8_verify_layer_parity.py` / `v8_health_check.py` 不需调整（仅 append 数据变 richer，不影响 STALE/READY 判据）。
- **小九机 vs 阿狸咪机 双机铁律**：本次仅动 `algorithms/stage_to_raw.py` + `index.html` + `logic.html` + `raw_data/lhb_history.json`，未触碰任何 workflow 配置文件、未触碰对方机器工作树。云端 cron 与阿狸咪机 PAUSED 任务均无需调整。
- **无新增派发者 / 兜底**：修复是被动等待 fetch 数据变完整后覆盖，不增加派发频率，符合「派发源收敛铁律」。

### 待阿狸咪机核实（本会话无法跨物理机）
- ⚠️ **`align_logic_ops.py` 双登记机制**：本次未新增带 cron 的 workflow，但建议你复核：现 MUST_HAVE_CRON 名单 21 项是否仍齐全（本次修复不在名单内、亦不需新增）。
- ⚠️ **`fetch_lhb.py` detail_limit**：本次未改 `DETAIL_LIMIT=90`（已 2026-08-12 落地），仅修了 stage_to_raw 侧覆盖判定。请你 `Ctrl+F5` 硬刷线上后查「机游共振」详情页是否已恢复知名游资（章盟主/赵老哥/方新侠/溧阳路·孙哥 等 alias）。

## ② 对齐运维页（index.html）和逻辑详解页（logic.html）的写入

| # | 页 | 卡位置 | 标题 |
|---|---|---|---|
| ① | logic.html | 第 1160 行 | 🔍 每日洞察卡时间戳锚定修复（2026-09-11 一劳永逸） |
| ② | logic.html | 第 1330 行 | 🔁 龙虎榜历史累积 richer 覆盖机制（2026-09-11 一劳永逸） |
| ③ | index.html 运维页 opTab1 | 第 6496-6499 行 | 📌 2026-09-11 今日两改（一劳永逸）note |

### logic.html 卡片内容要点
- **每日洞察卡时间戳锚定**（现象 / 根因 / 修复 / 文件）：卡片时间随最旧数据走；LHB/NF 用 `data.date` 锚定；非今日数据时段胶囊降级「盘后」。
- **龙虎榜历史累积 richer 覆盖机制**（现象 / 根因 / 修复 / 文件）：`has_real_data` 跳过逻辑 → `_quality()` 评分覆盖；骨架数据始终允许覆盖；`_save_json` LF 一致性；09-11 数据回填质量 117→291。

### 运维页 note 要点
- 一段式 note，分两小节概括两改：
  - ① 每日洞察卡时间戳锚定：`renderInsight()` 约 L3861，LHB/NF 用 `data.date`，非今日降级「盘后」。
  - ② 龙虎榜 richer 覆盖：`append_lhb_to_history()` L132 新增 `_quality()`，09-11 质量 117→291。
- 每条都指向 logic.html 对应卡片，便于另一台机 Ctrl+F5 后对照。

### 与既有页面的一致性
- 运维页「数据健康总览」/「审计轨迹」/「自动化任务」表均无需变更（修复不涉及这些层）。
- logic.html 的危机雷达灯 / 共振日历懒加载对齐 / 北向席位日历席位补回 等既有卡片均无需变更，本次仅追加。

## ③ 交接给另一台机（本文件即交接）

### 文件改动汇总
- `algorithms/stage_to_raw.py`（53 行净变更 · `_save_json` LF + `append_lhb_to_history` richer 覆盖）
- `index.html`（29 行净变更 · `renderInsight()` 时间锚定 + 运维页 note）
- `logic.html`（34 行净变更 · 两张新卡片）
- `raw_data/lhb_history.json`（1968 行净变更 · 09-11 数据回填 + `update_time` 刷新；属 LF 一致化后的实际数据差异）

### 验证方式（另一台机执行）
1. **本地 `git fetch origin main && git log --oneline -5`**：核对 SHA 是否同步。
2. **硬刷 `Ctrl+F5`** 后查「机游共振」详情页：游资标签应恢复「章盟主/赵老哥/方新侠/溧阳路·孙哥」等 alias（09-11 当日 58 只均含真实别名）。
3. **硬刷** 后查「每日洞察」卡片：
   - 时间应与卡内最旧数据日期一致（如龙虎榜还是 09-10 数据，则时间显示 09-10 的 15:30 而非今天）；
   - 时段胶囊在非今日数据时应为「盘后」而非「今日盘后」。
4. **`append_lhb_to_history()` 复跑**：直接 `python -c "from algorithms.stage_to_raw import append_lhb_to_history; append_lhb_to_history()"` 应打印 `⏭️ 跳过 龙虎榜 2026-09-11（new_quality=291 ≤ old_quality=291）` ——表示已收敛，无再次覆盖。
5. **算法链整体跑一轮**（可选）：让云端 `v8_algo_cloud` 跑一次完整 A→B→D→E 批，监控闸门是否仍 100% 绿。

### 双机注意点
- 阿狸咪机工作树若已有 `stage_to_raw.py` 本地未提交改动，请先 commit/push 后再 `git pull` —— 否则会被 `git pull --rebase` 拒绝。
- 阿狸咪机的 `PAUSED` 任务（含 stock-scanner 系列）**禁止**在小九端触碰（双机分时铁律：白天小九、夜晚阿狸咪）。
- 若阿狸咪夜班想复跑 fetch_lhb：建议带 `python fetch_lhb.py --detail-limit 90 --rebuild-history`，可直接触发 `append_lhb_to_history()` richer 覆盖逻辑；不要直接 edit `lhb_history.json`（会绕开质量评分）。

### 非本班遗留（待主人裁决）
- 「图14 四路资金流向对比」实读 `window.LHB_DATA`（龙虎榜）而非 FOUR_VOLUME；`raw_data/lhb_data.json` 历史显示 2026-08-04 后未见更新——主人记忆中有记，但本班未触及。
- `FOUR_VOLUME` `update_time` 8/7、命中 0 只，需主人拍板是否真无信号 vs 逻辑 bug。

—— 小九 · 2026-09-11 20:40