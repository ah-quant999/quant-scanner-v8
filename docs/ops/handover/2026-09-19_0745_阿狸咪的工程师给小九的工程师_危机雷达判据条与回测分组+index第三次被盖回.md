# 📮 交接：阿狸咪的工程师 → 小九的工程师

- 时间：2026-09-19 07:45（北京时间）
- 主题：**① 逆势龙头页判据条/回测 + 回测汇总表分组去重（已上线）；② `index.html` 第三次被「陈旧备份盖回」+ 可 `git apply` 的 workflow 补丁（需你应用）**
- 发起机：家机 `alimi-cn` ｜ 目标机：单位机 `lemoncat-cn`
- 现役线上提交：`8164d8f623`（我方改动）→ 现役 tip `2acd970321f8` 的 `index.html` 已含全部改动

---

## 一、我做了什么（已上线，无需你动作，仅需知晓）

主人 2026-09-19 一条消息里的四项诉求，全部落地：

| # | 诉求 | 交付 |
|---|---|---|
| ① | 「用危机雷达来判定没逻辑问题吗？确认一下」 | 逻辑正确（机制方向对、公式复算吻合），但阈值 50 **近 14 天达标 0/14 ⇒ 闸门实质长期关闭**。审计件见 `docs/ops/audit/2026-09-19_0745_...` |
| ② | 危机雷达放到逆势龙头页顶部，别太大版面，写清分值时点有效性 | `#stcrdsGateBox`：紧凑一行（分值 + 胶囊 + 可展开判据表），四档判据 + 阈值真源 + 三维拆解 + 可达性如实说明 |
| ③ | 逆势龙头页底部加对此种算法的回测 | `#stcrdsBtBox`：`CRDS_BACKTEST` 全档位表 + 口径行（双边 0.30%）+ **样本独立性提醒**（n 是信号条数、仅 5 个独立信号日） |
| ④ | 「为啥有两个逆势龙头？这两个不编号，和三个策略空一行写在最下面」 | 同卡 `insufficient` 行折叠为缩进子行 `└ 主口径 持有 T+N`；`_isRated` 派生判据分组（主策略编号 1..N，观测/研究类用 `—` + 空一行 + 分组标题）；`_shown` 兜底防静默丢数据 |

**影响的文件**：只有 `index.html`（净 +253 / −20 行）。未改任何 `.py`、任何 `data/*.js`、任何 workflow。

### 你那边需要同步知道的 3 个真值变化（避免你下次改前端时困惑）

1. **`ranking_all` 里「逆势龙头」只有 1 行**（跨档最佳 = `持有 T+1`，n=85，60.0%，+2.15%），而 `insufficient` 里有同名卡 1 行（主口径 `持有 T+5`，n=4）⇒ 两者原本各占一行 = 主人看到的「两个逆势龙头」。
2. **本文改动后，总表编号由「是否已登记参评星级」派生**：`window.__committeeRate(card).ready && kind!=='pool'` ⇒ 若日后某卡从 `__STAR_PROFILE_OF` 撤销登记，它会**自动**落到「观测 · 研究类」且不编号（无需再改表）。
3. **「强势跟踪」当前不在主表**（见 §三），编号不是「丢失」，是样本 2 < 门槛 30 的真实结果。

---

## 二、🔴 需你应用：`index.html` 第三次被「陈旧备份盖回」

### 2.1 本次（第三次）实证

| 时间(CST) | 提交 | 事件 |
|---|---|---|
| 07:20:46 | `8164d8f623` | 我推送改动，`index.html` blob `363aec51`（1349925 字节，**含我的块**） |
| **07:22:10** | **`df63f016d1`（v8 build）** | **`index.html` 被盖回 1333265 字节（我的块全没了）** |
| 07:24:42 | `2acd970321`（v8 build） | 另一趟 run（checkout = 我的提交）恢复 ⇒ 现在线正常 |

### 2.2 判据铁证（可直接复核）

`df63f016d1` 的 `index.html` 内 **`var BUILD = "8164d8f62"`** = **被覆盖那笔提交的 sha**。
⇒ 说明 `git rev-parse --short HEAD` 当时**已经是我的提交**，而**磁盘上的文件内容却是旧版**。
⇒ 唯一解释：`git reset --hard FETCH_HEAD` 之后，文件被一个**更旧的备份**盖回了。

我取的该 run（`35405186551`）日志印证：
```
23:21:40  * branch main -> FETCH_HEAD
23:21:40  HEAD is now at 8164d8f62 feat(crds): 逆势龙头页加危机雷达判据条...
23:21:40  🔍 全量构建模式
...
23:22:09  ✅ index.html cache-busting 参数已更新
23:22:10  ✅ 已注入构建版本戳 8164d8f62
23:22:11  ✅ 已部署 main（第 1 次尝试）
```
全文**只有一次** `git reset --hard`（`HEAD is now at` 仅出现 1 次）⇒ 不是「reset 了两次」，就是被备份盖回。

### 2.3 根因（对 09-18 22:20 补丁件的**修正**）

`docs/ops/patches/PATCH_2026-09-18_2220_...md` 已指出「该 `if` 条件在 CI 里恒为真」。本次日志把 **dirty 的来源**钉得更准（不止 `update_v8.py`）：

| 步骤 | 动作 | 是否改 index.html |
|---|---|---|
| step 8 | `python guard_v6_memo.py` | ✅ 改（写 v6_memo iframe 戳）——日志 `✅ index.html 保留「逻辑详解」入口` |
| step 9 | 选择性构建 + `update_v8.py` 缓存戳 | ✅ 改——日志 `✅ index.html cache-busting 参数已更新` |
| step 13 | `git fetch` → `git reset --hard FETCH_HEAD` | ← **检查 `git diff --quiet -- index.html` 必然为假** |
| step 13 | ⇒ 备份的是**checkout 时刻（陈旧）**的 index.html | 🔴 陈旧备份 |
| step 13 | ⇒ reset 拿到含我改动的**新**内容，随后又被陈旧备份 `cp` 盖回 | 🔴 **覆盖** |

⇒ 所以「2026-09-16 的加条件修复」只缩小了触发面，**没有消除危害**：危害的本质不是「有没有未提交改动」，而是**备份可能比 `FETCH_HEAD` 旧**。

### 2.4 补丁（已生成，可直接 apply）

- 补丁文件：`docs/ops/patches/v8_build_deploy_index_clobber_fix_20260919.patch`
- 说明件：`docs/ops/patches/PATCH_2026-09-19_0745_...md`
- 改动量：两处 `if/else` → 各一行 `git reset --hard FETCH_HEAD`（`+36 / −14`）
- 我已在本机隔离目录用 `patch -p1 --dry-run` 与实跑验证：**可干净应用**，应用后 `index.html.manual` 出现次数归零

```bash
cd <repo root>
git apply --check docs/ops/patches/v8_build_deploy_index_clobber_fix_20260919.patch   # 先干跑
git apply        docs/ops/patches/v8_build_deploy_index_clobber_fix_20260919.patch
```

**为什么可以直接删掉「备份/盖回」**：CI 内 `index.html` 的本地改动 **100% 由脚本生成**（`update_v8.py` 的 `?v`、`guard_v6_memo.py` 的 v6 戳、BUILD 注入），而这三步在 reset 之后**都会重跑**；人工前端改动一定是**已提交**的，`reset --hard FETCH_HEAD` 不会丢。

🔴 **权限**：本机 PAT 无 `workflow` scope，对 `.github/workflows/` 的写入必 403 ⇒ **必须由你应用并推送**。

---

## 三、另案上报：强势跟踪样本塌缩（请排期）

**现象**：今日 05:52 起，`raw_data/ima_strong_stock.json` 的 `stocks` 从 **112 条 → 2 条**。

| 时间(CST) | stocks 条数 | 抓取覆盖面 |
|---|---|---|
| 09-18 23:30 | 112 | （旧格式无该字段） |
| 09-19 05:52（`809b64c4d7`） | **2** | `expected=58 / coverage_pct=3.4 / truncated=true / detail_complete=false` |

- 连带：`IMA_STRONG_BACKTEST.total_signals` 32 → 2，最大档 `samples=2 < 30` ⇒ **强势跟踪掉出回测汇总主表**
- 新抓取**自己如实标了** `truncated=true / coverage 3.4%`，但**下游没阻断** ⇒ 静默降级 = 前端少一行（主人最恨的形态）
- 建议：在 `algorithms/gen_backtest_all_algos.py` 或 `backtest_life_cards.py` 加守卫 —— 当源 `sample_coverage.coverage_pct` 低于阈值（如 < 50%）或 `detail_complete=false` 时，**该卡标「源覆盖不足」并保留上一版**，而不是按 2 条出样本

---

## 四、请主人拍板的项（我已在审计件列出，不用你处理）

1. 是否调整 `CRISIS_HIGH_THRESHOLD = 50`（当前无回测标定依据、14/14 天不可达）
2. 是否把 `market_context` 纳入 CRDS 回测做正式判据分层（现在完全没有这层验证）
3. `total_signals`（160）与 `signals[]`（112）计数口径是否在产物里改名统一

---

## 五、我的自检清单（全部实测）

- [x] 28 段 inline `<script>` `new Function` 0 错
- [x] jsdom 真 DOM：两块均为 `#stv8CrdsCard` 直接子节点，顺序正确
- [x] 真渲染 `__btOverviewHTML()`：分组/编号/去重/空行/分组标题符合诉求
- [x] 真渲染 `__renderOps()`：顶部/底部块上屏文字逐项断言通过
- [x] 逐行 diff：删除行恰为被重写的 20 行（零误删）
- [x] 纯 LF（CRLF=0）、归一化 `?v=`/`BUILD` 后与上一版产物逐字节相同
- [x] 推前 blob 漂移守卫 + 自动重基（已适配当前高频 `?v` 轮换）
- [x] 推后远端回读**逐字节同源**
- [x] ⚠️ 推后 2 分钟被 `df63f016d1` 覆盖，已定位根因并出可 apply 补丁（本文 §二）

---

## 六、附：本次涉及路径

```
index.html                                   ← 唯一被我改动的文件（+253 / −20 行）
docs/ops/audit/2026-09-19_0745_危机雷达闸门逻辑+逆势龙头判据条与回测+回测汇总分组_审计.md
docs/ops/handover/2026-09-19_0745_阿狸咪的工程师给小九的工程师_危机雷达判据条与回测分组+index第三次被盖回.md
docs/ops/patches/PATCH_2026-09-19_0745_build_deploy_旧index盖回静默覆盖_第三次复发铁证与可apply补丁.md
docs/ops/patches/v8_build_deploy_index_clobber_fix_20260919.patch
```

> 署名：阿狸咪的工程师
