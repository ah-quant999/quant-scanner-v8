# 2026-09-21 小九 → 阿狸咪：分红数据链三项根治（合并 / 单位 / 口径）

> **数据采集时刻：2026-09-21 15:2x CST**。本文所有结论均为**截至该时刻的实测值**；
> 涉及「后续会怎样」的，一律给**可跑的复核命令**，不写承诺。
> 本文**不写自身所在提交的 sha**（churn 仓库里写死即过期）；需要时用文中命令现取。

---

## 一、一句话结论

`data/STOCK_QUOTE.js` 的分红字段**一直是「东财单源 + 每 30 分钟被整体重建」**：
cninfo 那条更权威的「已实施方案」链路**从来没在线上留下痕迹**，而且前端把
`cash_ratio`（**每 10 股派现元数**）当成百分比显示，还把真实股票（吉比特 10 派 100 元）
误报成「待核」。本批**三项一并根治**，已实测验证。

---

## 二、根因（三条，均有线上实证）

### 根因 1｜cninfo 成果被无条件覆盖（这是主因）

`algorithms/fetch_stock_quote_v8.py::merge_dividend()` 里写的是：

```python
existing = quote_data[code8].get('dividend') or {}   # ← quote_data 是本次【全新构建】的
```

`quote_data` 由 `fetch_all_spot()` 从零构建 ⇒ **`existing` 恒为空** ⇒
「已有 cninfo 结果就保留」是**死代码**。而 `write_outputs()` 会把整个
`data/STOCK_QUOTE.js` 重写一遍。

叠加运行频率现实：

| workflow | cron | 频次 |
|---|---|---|
| `v8_stock_quote_refresh.yml` / `_cloud.yml` | `0 1-8 * * 1-5` + `30 1-8 * * 1-5` | **每天 16 次**（09:00–16:30 每半小时） |
| `v8_cn_fetch_cloud.yml` 内 `refresh_dividend_cninfo.py --budget=900` | 见该文件 | 一天数次 |

⇒ **cninfo 成果存活不足 30 分钟**。

**线上实证（截至上述时刻，抓 Pages 的 `data/STOCK_QUOTE.js`）**：

```
有 dividend 的票 = 3550 只
其中带 cninfo 特征字段（type / announce_date / record_date）的 = 0 只   ← 一只都没留下
```

### 根因 2｜`cash_ratio` 单位被当成百分比

实测（对照同一记录的 `desc`）：

| 票 | `desc` | `cash_ratio` |
|---|---|---|
| 平安银行 | `10派2.49元` | **2.49** |
| 宁德时代 | `10派14.11元` | **14.11** |
| 吉比特 | `10派100元` | **100.0** |

⇒ 语义是「**每 10 股派现元数**」。而前端三处按 `%` 显示：

- 财务行 `{label:'分红比例', val:d.cash_ratio.toFixed(2)+'%'}`
- 个股查询 chip `'💰'+dv.cash_ratio.toFixed(1)+'%'`
- 某卡 chip `'💰'+dv.cash_ratio.toFixed(1)+'%·'+prog`

后果：吉比特显示「**分红比例 100.00%**」——读起来像**股息率 100%**。

并且 `_dividendCheck` 规则 2 `cashRatio > 80` 注释写的是「分红比例 > 80%」，
实为「每 10 股派现 > 80 元」⇒ **吉比特被误报「待核」**（实测 `ok=false`）。

### 根因 3｜东财报告期窗口只有 6 期

`_recent_report_dates(6)` 从今天（2026-09-21）往前只覆盖到 **2025一季报**，
**2024 年报（20241231）被整体排除**。实测该期东财另有 **3676 只** 有分红。

---

## 三、改了什么（4 个文件）

| 文件 | 改动 | 字节变化 |
|---|---|---|
| `algorithms/fetch_stock_quote_v8.py` | ①补 `import re`（新增 helper 用）②窗口 6→8 期 ③新增 `_rp_key` / `_load_prev_dividends` ④`merge_dividend` **真合并** ⑤第二遍「东财无数据的票沿用上一版」 | **+3737 B** |
| `algorithms/refresh_dividend_cninfo.py` | ①新增 `_rp_tag`：把 cninfo「报告时间」规范成与东财同形（`2026-06-30` → `2026半年报`）②`plan` 增加 **`src: 'cninfo'`** | **+833 B** |
| `index.html` | ①财务行 `分红比例 X%` → **`每10股派现 ¥X`** ②两处 chip `💰X%` → **`💰10派X元`** ③`_dividendCheck` 规则 2 改为**单位自洽校验** ④注释块同步 | **+816 B** |
| `logic.html` | 同口径同步 2 处（**纯 CRLF 保持，`bareLF==0`**） | **-1 B** |

### 统一后的优先级口径（根因 1 + 3 的收口）

```
东财 stock_fhps_em（近 8 个报告期，覆盖全市场）→ 打底，src='em'
        ↓ 合并（不再整段覆盖）
cninfo stock_dividend_cninfo（重点池：持仓+候选池+金股池+手动关注）→ src='cninfo'
规则：report_period 较新者胜出；同期或无法比较 ⇒ cninfo 优先（经实施公告核实，更权威）
东财近 8 期均无该票 ⇒ 沿用上一版（不删除）
```

### 🔴 规则 2 为什么必须改（不是「顺手改判据」）

原规则 `cashRatio > 80` 的前提是「cash_ratio 是百分比」，而这个前提**是错的**。
若只改显示、留着这条规则，就会继续**误报吉比特**这类真实高派现票（10 派 100 元对 348 元股价属正常）。
故改为**单位自洽**校验：`desc` 解析出的每股派现 与 `cash_ratio/10` 必须一致（容差 5%），只抓真矛盾。

实测对照（jsdom 真实渲染 + 线上真实记录，仅覆盖 `dividend`）：

| 用例 | 改动前 | 改动后 |
|---|---|---|
| 吉比特（10派100元） | 财务行「分红比例 100.00%」+ **待核（误报）** | 「每10股派现 ¥100.00」+ **无待核** |
| 平安银行 | 「分红比例 2.49%」 | 「每10股派现 ¥2.49」 |
| 宁德时代 | 「分红比例 14.11%」 | 「每10股派现 ¥14.11」 |
| 浦发银行（`desc` 10派2.49 与 `cash_ratio` 50 矛盾） | **漏报**（无待核） | **正确报出**「方案 0.25 元/股 vs 每10股派现 50.00 元」 |

---

## 四、Python 侧实测（真实调用 akshare，非推断）

```
✅ 合并分红配送：2/5 只 | 沿用 cninfo 1 只 | 沿用上一版 1 只
   ① sz000001  cninfo 的 report_period / cash_ratio / desc / type
              / announce_date / record_date / eps / bvps 全部保留，src=cninfo
   ② sz301999  东财 8 期均无 ⇒ 被第二遍沿用（旧行为：整条丢失）
   ③ sh600989  东财有数据 ⇒ src=em
   ✔ sz000002 无分红的票不会被凭空造出 dividend
```

报告期窗口实测：**6 期命中 3550 只 → 8 期命中 4181 只**（+631 只）。

---

## 五、过程中被测试挡下的一个致命缺陷（记录备查）

补丁初版里 `_rp_key` 用了 `re`，而 `fetch_stock_quote_v8.py` **原本没有 `import re`**。
该异常会被 `merge_dividend` 的 `except Exception` 吞掉 ⇒ **全市场分红数据会整体消失**。
「真实调用 `merge_dividend`」的测试一跑即暴露（`⚠️ 分红合并失败: name 're' is not defined`），
补 `import re` 后通过。**教训：改产出脚本，必须真跑一次，不能只看语法/锚点。**

---

## 六、复核命令（现取，勿写死 sha）

```bash
# 1) 取当前权威 tip
curl -s https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main

# 2) 线上 data/STOCK_QUOTE.js 的门槛断言（应满足，否则视为本批未生效）
#    report_period 命中数 > 4000（8 期窗口）；"2023年报" 命中数 = 0
curl -s https://ah-quant999.github.io/quant-scanner-v8/data/STOCK_QUOTE.js | \
  grep -o 'report_period' | wc -l

# 3) 线上 index.html 的门槛断言
#    应出现 label:'每10股派现' 与 10派'+String(+dv.cash_ratio.toFixed(2))+'元
#    不应出现 label:'分红比例' / cash_ratio.toFixed(1)+'%' / || cashRatio > 80 ||
curl -s https://ah-quant999.github.io/quant-scanner-v8/index.html | \
  grep -c "label:'每10股派现'"

# 4) cninfo 链路是否真的留下痕迹（这是本批最关键的一条）
#    期望：带 "src":"cninfo" 的票数 > 0（当日 cn_fetch 跑过之后）
curl -s https://ah-quant999.github.io/quant-scanner-v8/data/STOCK_QUOTE.js | \
  grep -o '"src":"cninfo"' | wc -l
```

---

## 七、踩坑提示

1. **`index.html` / `logic.html` 处于分钟级 churn**（云端 `生成器原子提交 ?v → 防覆盖`）。
   本批补丁一律**运行时现取 TIP**，锚点必须**唯一命中**（`count(old)==1`）才替换，否则中止。
2. **`logic.html` 必须保持纯 CRLF**：判据 `count('\n') - count('\r\n') == 0`（`bareLF==0`）。
   本批实测 `crlf == 行数`、`bareLF == 0`。
3. **`data/*.js` 含 `NaN`**（Python `json.dumps` 默认 `allow_nan=True`）：浏览器按 JS 字面量执行合法，
   但 **node 的 `JSON.parse` 会拒** ⇒ 一切数据校验脚本用 `vm.runInContext`。
4. **跨机器上取旧的 `algorithms/fetch_stock_quote_v8.py` 做基线会漏改动**：
   本地工作树可能落后（实测本地 22666 B vs 线上 25024 B）。**一律以线上真身为基线**。
5. 新增了 `import re` —— 若你机上有并行改动，注意别把这一行当作冗余删掉。

---

## 八、本批未动项（等你或主人拍板，勿擅自改）

1. **`refresh_dividend_cninfo.py` 的 `--budget=900` 是否够**：重点池若超预算会优雅收工、剩余票留待下轮；
   本批未改预算值。
2. **`cash_ratio` 的 chip 颜色阈值（>=30 金色 / >=10 绿色）**：这两个数字当初是按「百分比」选的，
   本批**只改标签与单位、没动阈值**。单位纠正后它们变成「每 10 股派现 30 元 / 10 元」，
   语义仍说得通（高派现高亮），但若要按「派现率」重新定档，需主人明示。
3. **`logic.html` 的 `renderQuote` 代码节选仍落后主站**：该节选**不含** `_dividendCheck` / `divCheck` / `perShare`
   （实测 `logic=0`，`index=5`）。本批只同步了「每 10 股派现」口径，**未整段搬核查逻辑**。
4. **`20240930` 等更早报告期是否继续纳入**：本批纳入到 2024 三季报（8 期）。

## 九、 补丁批 #2（同日 16:2x · 小九）：`logic.html` 字段说明口径残留已清

**发现方式**：终验时逐字节 grep 线上 Pages，`index.html` 的「分红比例」= **0**，
而 `logic.html` 仍残留 **1** 处，位于「综合质量分」卡的**字段说明**文字里：

```
- 分红比例来自东财 stock_fhps_em
+ 每10股派现来自东财 stock_fhps_em
```

**为什么必须改**：同一字段在主站已更名，逻辑详解页若沿用旧名 ⇒ 两页口径打架，
违反「两页同源」；且旧名会被读成「百分比」，正是本次纠正的那个错误语义。

**截至 2026-09-21 16:2x 实测**：`logic.html` 行尾 `crlf=8613 bareLF=0`（纯 CRLF 守恒）；命中 `分红比例` = 0、`每10股派现` = 3。

**复核命令（现取，勿写死 sha）**：

```bash
curl -s https://ah-quant999.github.io/quant-scanner-v8/logic.html \
  | grep -c '分红比例'          # 期望 0
curl -s https://ah-quant999.github.io/quant-scanner-v8/logic.html \
  | grep -c '每10股派现'        # 期望 3
```

---
