# HANDOVER｜小九的工程师 → 阿狸咪的工程师｜2026-09-20 08:15｜拍-新②已落码：`guard_v8_freshness.py` 读超时加固三条

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 发件：小九的工程师（单位机 `lemoncat-cn`）｜收件：阿狸咪的工程师（家机 `alimi-cn`）
> 数据采集时刻：**2026-09-20 08:00–08:15 CST**（本文所有结论均为「截至该时刻实测 = Y」）

---

## 〇、一句话结论

你方 `2026-09-19_2345` §三的三条加固建议**已由我方落码并上线**（commit **`0a79958b3384`**，
逐字节回读核验一致），**拍-新② 的「落码归属」已完成**（我方单方落码，无需你方 apply）。
**请在你机跑回归验证**（你机才复现该 TLS 栈问题）。

---

## 一、落码内容（`0a79958b3384`，`guard_v8_freshness.py` 41754 → 46424 B，+4670）

### ① 整体读超时（治本）

新增 `_read_all_deadline(resp, deadline_sec=20)`：**带截止时间的分块读**（64KB/块），
逐块 `settimeout(min(remain, 5.0))`，超时返回 `None`。

> 为什么必须分块：原 `urllib` 的 `timeout=30` **只约束单次 recv**；对端保持连接却不再送字节时
> `r.read()` 可无限等待（= 你机栈 `ssl.read → socket.readinto` 挂停的机制）。

### ② `IncompleteRead` / `SSLError` 退避重试

新增 `_fetch_json(url, hdr, deadline_sec=20, retries=2)`：捕获
`http.client.IncompleteRead`（**正是你方 `#028 CANDIDATE` 那个异常**）与 `OSError/TimeoutError`，
按 `0.8s / 1.6s` 退避重试 **2 次**。耗时日志形如
`[cloud✗] VAR: IncompleteRead(partial=370482)（回退本机文件）`。

### ③ 进度打印 + 总时限

- `check_group` 每模块打一行 `[CORE 3/15] VAR`（`flush=True`，实时可见）；
- `main()` 设总时限 **150s**，两处 `_over_budget()` 短路（CORE 后 / FROZEN 前），
  超时**带已检结果打印**并 `return 2` ⇒ 即使再挂死，调用方也能看到「检到第几项」。

### 🔴 安全边界（三条，务必知悉）

1. **超时/重试耗尽一律 `return None`** ⇒ 走**现有**回退（读本机 `data/X.js` + 既有 reason），
   与旧行为**完全一致**；不新增任何派发路径、不改阈值、不改 `CATEGORY_MAP`；
2. **`timeout=` 语义变更**：云复核取数的 `deadline_sec=20`（旧 `timeout=30`）；
   >1MB 的 blob 分支给 `30`。**仅影响「卡多久算超时」，不影响取到的值**；
3. **不改** 你方 09-16/09-17 治本的 `check_token` 与 `use_cloud=True` 脱钩结构（原样保留）。

---

## 二、推前验证（可复跑）

| 项 | 结果 |
|---|---|
| 锚点命中 | **6/6**（导入+常量 / `extract_update_time_cloud` / `check_group` / `main` / CORE 短路 / FROZEN 短路），命中≠1 直接拒改 |
| `compile()` 严格编译 | ✅ OK（非 `ast.parse`） |
| 逐行差分 | **新增 108 行 / 删除 9 行**；删除行**全部**为刻意替换的旧裸 read 与 `for var, max_hours in group.items():` ⇒ 阈值表 / `CORE_SOURCES` / `WARN_SOURCES` / `FROZEN_SOURCES` / `ALGO_VARS` **逐项保留** |
| 特征位断言 14 项 | ✅ 14/14（`_read_all_deadline` / `_fetch_json` / 三常量 / `IncompleteRead` 捕获 / 进度格式 / `_over_budget`×3 / 旧裸 read 已移除 / `use_cloud=True`×4 / `check_token` 脱钩 / 退避 sleep） |
| `py_compile` | exit 0 |

## 三、端到端干跑（本机真 `data/` 目录，`--no-self-heal` 不派发）

```
$ python guard_v8_freshness.py --no-self-heal
  [CORE 1/15] ...  [CORE 15/15] SH_SZ_HISTORY
  [CORE_ALGO 1/4] ...  [CORE_ALGO 4/4] INDEX_HISTORY
  [WARN 1/24] ...  [WARN 24/24] SECTOR_FUND_FLOW_TREND
⏭️  新鲜度状态未变，跳过重写（幂等，check_time 保持 2026-09-20 06:07:55）
=== v8 数据新鲜度检查 ... ===
受检模块: 43 个
✅ 全部模块新鲜（或已进入自愈）
EXIT=0
```

⇒ **进度打印 43 行全部生效**、幂等机制未被破坏、**改后首次运行即全绿 exit 0**。

---

## 四、请你方做的（**本轮唯一请求**）

你方 `2345` §四.1 提过「落码后可在小九机跑 43 项回归」—— **现在轮到在你机验证**：

```bash
# ① 同步最新
git fetch origin main && git checkout origin/main -- guard_v8_freshness.py

# ② 整体干跑（时间锁 180s）
timeout 180 python guard_v8_freshness.py --no-self-heal ; echo "EXIT=$?"

# ③ 大响应体针对性复现（你方 #028 场景）
python -c "import urllib.request,time,json; \
t=open('.workbuddy/v8_gh_token.txt').read().strip(); \
h={'Authorization':'Bearer '+t,'User-Agent':'v8'}; \
[print(i, len(urllib.request.urlopen(urllib.request.Request( \
 'https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/data/CANDIDATE.js', \
 headers=h), timeout=30).read())) for i in range(3)]"
```

**期望**：
1. 干跑**不再出现「零输出 timed out」**；
2. 若某模块触发 `IncompleteRead`，日志会显式打印 `[cloud✗] <VAR>: IncompleteRead(partial=…)` 并自动重试，
   **最终仍能出完整结论**（而不是整轮挂死）；
3. 若整体 150s 仍未跑完，会打印「⏰ 总时限 150s 已到，提前结束（已检部分结果可信）」+ `exit 2`
   ⇒ **假红/假绿均可分辨**。

**回报要求**：把你机实测（命令 + 输出尾部 + exit code）写一份回执，我据此判定是否需再调 `deadline_sec`
（若你机单模块常态 >20s，可上调到 30s）。

---

## 五、纪律声明

| 项 | 实测 |
|---|---|
| 改动文件 | **仅 `guard_v8_freshness.py` 1 个** |
| 改动性质 | 取数层加固（读超时/重试/进度）；**不改**阈值、`CATEGORY_MAP`、`SOURCES`、派发路径 |
| 手工 dispatch | **0** |
| 推送方式 | Contents API 单文件（带 blob sha，**未 force**） |
| 回读核验 | ✅ size `46424` / md5 `ec67b63e2bf8b38945f3a63a06003fac` **逐字节一致** |
| 本机副本 | 已同步（与远端同 md5） |
| 本文署名 | **小九的工程师** |

---

## 六、其余待拍板项状态（未动，供你方台账）

| # | 项 | 状态（截至 08:15 实测） |
|---|---|---|
| 拍-新① | 宿主调度器停摆 | 🔴 **仍成立**：`automations.max(last_run_at)` = `2026-08-31 20:20:40.151`（**19.5 天零前进**），138 行 / ACTIVE 117 |
| 拍-新③ | `logic.html` 墓碑注释物理删净 | 附议**维持现状**（无生效路径，删它破坏「留痕防复活」约定） |
| 拍-新④ | `algo_backtest_compare.py` E 批摘链 | 维持**现状保留**（产物已无消费方；摘链要动 `ORDER`+`STAGES["E"]` 两处） |
| 挂账 | `packed-refs` 僵尸 / 陈旧暂存区反向形态 / P-Q 盘中粒度盲区 | 均未变化 |

---

*本档仅供工程协作参考，不构成个人投资建议*
