# 🚨 URGENT · 阿狸咪的股票专家 → 小九 · CRDS 提速「改后三件套」收口 + 今晚链红灯真因

- **发件**：**阿狸咪的股票专家**（家机 `alimi-cn` · `D:\actions\cn-runner`）
  > 命名约定（2026-09-11 主人令）：**「阿狸咪」= 家机，「阿狸咪的股票专家」= 本机上的 AI 专家**；同理「小九」= 单位机。报告正文一律用「<机器>的股票专家」署名，与机器本体区分。
- **收件**：小九（单位机 `lemoncat-cn` · `D:\actions-runner-v8`）
- **签发**：2026-09-11 20:13 CST
- **触发**：主人问「昨晚今早修改修复的，未改后三件套的马上做，然后和小九交接清楚」→ 再问「和小九交接了吗」
- **数据基准**：**GitHub main 分支 Contents API（权威）**，不看本地工作区（坚果云不拉远端）

---

## 0. 三句话结论

1. **交接：此前没交。** 我最后一次给你的是 `2026-09-11_0735`，它只覆盖了 `24eef0e70 / a738120c7 / 5052fdaa3 / 691de0086`；而 **07:35 之前/之后还有 6 个提交只推了仓、既没做文档对齐也没有交接** —— 本轮补齐。
2. **今晚 17:30 / 18:05 / 19:33 三连红灯不是同一个原因**，我拆成两条，把结构性的那条修了（`88deb6258644`）。
3. **1505 那份的 P0-②（闸门脚本空输出）未复现**，可以放下，不用修。

---

## 1. 此前「漏交接」的 6 个提交（全部已在 main，逐 sha 查证）

| commit | 时间(CST) | 内容 | 此前是否交接 |
|---|---|---|---|
| `fe7b0e5962c0` | 09-11 07:12 | CRDS 取数提速重构：113min → 分钟级 | ❌ 未提 |
| `170fbf10528e` | 09-11 07:19 | 修缓存键冲突（`sh000001`指数 ↔ `sz000001`平安银行）+ 大盘口径纠偏 | ❌ 未提 |
| `f83fdd33b222` | 09-11 07:22 | 修盘前新鲜度闸门（缓存此前**永不命中**）+ mootdx 名字映射并发竞态 | ❌ 未提 |
| `99d1411e1496` | 09-11 07:24 | CRDS 产物刷新（提速后首跑）+ 跨层一致 | ❌ 未提 |
| `9e1792ae672f` | 09-11 07:34 | 修 `run_algorithms` 致命 `NameError: logging` —— 链一授权就崩，整链不落数 | ❌ 未提 |
| `efd9ad72646a` | 09-11 07:37 | 批量修 pyflakes F821「未定义名」崩链隐患（5 文件） | ❌ 未提 |

**你无需复查这 6 个是否落地**：我逐个 `GET /repos/.../commits/{sha}` 查证均在 main；且直接读远端 `algorithms/calc_crds.py`（55879 B）核验 `_KLINE_CACHE_DIR / _EXPECTED_END / _is_index_like / _TDX_DISABLED / ThreadPoolExecutor / V8_CRDS_WORKERS / V8_CRDS_BUDGET / proxy.finance.qq.com / cache_fresh` **11 个关键标识全部命中**。

---

## 2. 三件套①：文档对齐（此前 **0 命中**，已补）

**改前实测**：`index.html` 与 `logic.html` 里 `kline_cache` / `缓存优先` / `并发` / `预算` / `budget` 命中数 **全部为 0** —— 昨夜今晨的 CRDS 取数重构完全没进文档。CI 没拦住，因为 `align_logic_ops` 只校验「workflow cron ↔ 文档登记」，**不校验取数描述**。

**本轮补**（commit `9c8a0f33c78a`，共 7 处）：

| 文件 | 处 | 内容 |
|---|---|---|
| `logic.html` | CRDS 段「输入与数据源」 | 重写为**四级取数链**：① `raw_data/kline_cache/` 缓存优先（内容级新鲜度闸门：末根K线须 ≥ 最近一个**已收盘**交易日）② 腾讯 `proxy.finance.qq.com` 快源前置（0.15s/只；`web.ifzq.gtimg.cn` 被域名级 WAF 拦，同 API 换域可达）③ mootdx 直连（连续 5 次失败即**熔断**）④ 东财 push2his 兜底；并写明 **12 线程 + 8 req/s 限速 + 1500s 墙钟预算**、**缓存键铁律**（`sh000001` 与 `sz000001` 撞键 → 指数不读不写缓存、不走 mootdx） |
| `logic.html` | 备忘区 ① + 表头 | 「基于上证今日涨跌幅」→「基于 `index_quotes` **综合口径**今日涨跌幅」 |
| `logic.html` | CRDS 段「大盘环境有效性判断」 | **新增口径说明块**：同一张卡并存两套大盘口径（见 §5 待拍板） |
| `index.html` | 双机状态「执行节点」 | 由「云端 ubuntu-latest（主链：cn_fetch + algo_cloud + build_deploy）」→ 更正为 **cn_fetch / build_deploy = 云端 ubuntu-latest；algo_cloud = self-hosted cn 双机** |
| `index.html` | 双机状态「小九」行 | 「仅应急 dispatch，不再跑 cron 主链」→ 更正为 **lemoncat-cn 承接 algo_cloud（与阿狸咪共享 cn 标签，谁空谁跑）+ 应急 dispatch** |
| `index.html` | 定时任务表 algo_cloud 行 note | 「已迁 GitHub Actions 云端 ubuntu-latest」→ 更正执行机 + 补登记源码同步回退策略 |

> ⚠️ 这三处执行机记述此前是**错的**，且正是今天排障时最误导人的一条：`v8_algo_cloud.yml` L104 明确写着 `runs-on: [self-hosted, cn]`（L158 那句「08-20 改成 ubuntu-latest」是**过期注释**），job 日志 `runner_name` 实测为 `lemoncat-cn` / `alimi-cn`。

## 3. 三件套②：审计（本地 `pre_deploy_audit.py`，**4/4 全绿**）

```
✅ [1/4] py_compile: 227 个 .py 文件 0 错误
✅ [2/4] new Function: 24 个 inline script 块 0 错误
✅ [3/4] data 完整性: data/*.js 数量=104, 全部 > 100B
✅ [4/4] align_logic_ops: EXIT 0（逻辑详解页与真 workflow 对齐）
🎉 4 项全部通过 → deploy 可继续
```

## 4. 三件套③：推仓 + 本交接（见文末红线）

---

## 5. 今晚三连红灯：两条**不同**真因（含铁证）

### 因①【结构性 · 我已修】checkout 失败 → 「API 回退同步」步 10 分钟必然超时 → 步骤 4~14 全 skipped → 问责红灯

**铁证 run `34594585246`（19:33 · runner=`lemoncat-cn`）**

```
step 2  📥 Checkout 源码…                          success（但 outcome=failure，见下）
step 3  📥 回退·经 GitHub API 同步源码…              ❌ failure  ← 病灶
step 4~8 🔧/🗓/🐍/📦/🔒                            skipped
step 9~14 🧮/🔗/📈/📤/🔄/🛡                         skipped
step 15 🛡 结果问责                                  ❌ failure
```

日志原文（决定性）：

```
files to sync: 3913
  blob retry 1 [raw]: IncompleteRead(1591273 bytes read, 31123 more expected)
  blob retry 1 [raw]: IncompleteRead(1787961 bytes read, 50265 more expected)
  blob retry 1 [raw]: IncompleteRead(1265162 bytes read, 60044 more expected)
  blob retry 1 [raw]: IncompleteRead(3414043 bytes read, 3841 more expected)
##[error]The action '📥 回退·经 GitHub API 同步源码（仅 checkout 失败时）' has timed out after 10 minutes.
```

**机制**：`actions/checkout` 带 `continue-on-error: true`，其 `outcome` 在 cn runner 上**恒为 `failure`**（git→github.com 被墙：日志有 `Recv failure: Connection was reset` / `Failed to connect to github.com port 443 after 21098 ms`）→ 触发 `if: steps.co.outcome == 'failure'` 的回退步；而该步要**逐 blob 拉全仓 3913 个文件 / 175.8 MB**，10 分钟结构性不可能（11:45:23 起 → 11:55:19 被打断，期间只处理了 4 个大文件的重试）。

**main tree 实测构成**（决定修法）：

| 目录 | 文件数 | 大小 | 算法链是否需要 |
|---|---|---|---|
| `raw_data/kline_cache/` | 3119 | 57.3 MB | ❌ 缓存，`calc_crds` 冷缓存实测 **91s 可跑**（有网络兜底） |
| `backup/` | 28 | **69.3 MB** | ❌ 历史备份，无任何脚本读取 |
| `docs/` | 174 | 1.2 MB | ❌ 交接文档 |
| 其余（`raw_data`/`data`/`algorithms`/`raw_data/history`/root/`scripts`/`.github`/`v8`…） | ~592 | ~48 MB | ✅ 必需 |
| **合计** | **3913** | **175.8 MB** | — |

**同因另一次**：run `34584579958`（17:30 · `lemoncat-cn`）。

### 因②【合规红灯 · 非 bug】18:05 本机那轮链**真跑了**，是 B 就绪 7/9 < need=8

**铁证 run `34587472102`（18:05 · runner=`alimi-cn`）**：链路一路跑到 `bridge_raw_data.py`，末尾只有一条错误：

```
##[error]B 批跑完但选股三模块仍非本数据日盘后产出（7/9，执行=success）—— 假成功，必须排查
```

→ 你今天把 B 的 readiness 从 3 项扩到 **9 项 / need=8**，这个门**工作正常**，只是这轮没凑满。**它不是故障，是诚实红灯。**

> 🔴 **风险提醒（我不动，等你定）**：`need=8/9` 若长期凑不满，`READY_SPEC` 未就绪 → 闸门恒判 B → **D 批（最终推荐）在 09:00~15:59 死区外依然会被饿死**。是否配合 `13:48` 那份已提的「上游落后 > 2 交易日强制放行」防死锁，请你拍。

### 澄清：1505 那份的 P0-②「闸门脚本空输出」**未复现**

19:33 日志里闸门**正常产出**判定，没走 crash 分支：

```
target_stage=B
reason=A 就绪(7/10) B 未就绪(1/9) → 跑选股批
```

（`reason` 中文在 runner 日志里乱码，是 runner 控制台编码问题，属**显示**异常，非逻辑异常。）→ **该条不用修**，别去动 `v8_stage_gate.py`。

---

## 6. 我改了什么（commit `88deb6258644`，**只改回退步**，未碰任何业务逻辑）

| # | 改动 | 理由 |
|---|---|---|
| ① | `timeout-minutes: 10 → 25` | 3913 文件 / 175.8 MB 在 10 分钟内结构性不可能 |
| ② | 排除 `backup/` + `raw_data/kline_cache/` + `docs/` | 少 3321 文件 / 127.8 MB → 剩 ~592 文件 / ~48 MB |
| ③ | `fetch_blob` 改 **b64 优先**（原 raw 优先） | `raw` 端点在 >1MB 文件上实测 `IncompleteRead` 截断；b64 是 JSON 响应、长度可校验 |
| ④ | 单请求超时 `180 → 90s`；`tries 6 → 4` | 挂死请求快速失败，避免单文件吃掉 3 分钟 |
| ⑤ | 脚本内 **18 分钟软预算**，用尽即收尾并打日志 | 避免被 GitHub 硬杀在半途，且日志有结论 |
| ⑥ | 日志增打 `skipped heavy/unused: N` | 可观测 |

**安全性论证（这条最重要）**：链尾推送 `api_push_raw.py` **L315 明确「任何模式都不从 tree 中删除（`base_tree` 继承未列出路径，天然保留）」**，L320-331 对缺失文件只 warn 跳过 → **排除目录绝不会把远端那 3119 个缓存文件删掉**。另：YAML 已过 `yaml.safe_load` 解析。

**触发点**：下一档唤醒（**21:00**）就会走新逻辑。若 `lemoncat-cn` 的 git 仍被墙，回退步应在 25 分钟内跑完并让链真正执行。

---

## 7. 未改项（等你 / 等主人，我不擅动）

| # | 事项 | 现状 | 已交接轮次 |
|---|---|---|---|
| 1 | **B `need=8/9` 契约** | 18:05 实测 7/9；长期凑不满会饿死 D 批 | 13:48 / 15:05 + 本文 |
| 2 | `INDEX_HISTORY` 假新鲜 | 内容 `meta.update_time=09-08T16:54`、`klines` 末条 **09-07**，却被 republish 刷成 `09-10 21:39` → 提醒：**republish 只准写 `republish_time`** | 10:27 / 13:48 / 15:05 |
| 3 | `index.html` `?v` 口径分裂 | 100 处 unix 时间戳 + 日期戳 ×2 + `data/DO_NOT_DELETE.js` 的 `<sha>` **模板占位符泄漏** | 10:27 / 13:48 / 15:05 |
| 4 | 运维页定时任务表 `freq` 文案 | 写「16:40/18:10/19:15/20:00 四批」，实际 cron 唤醒点 = **16:40 / 18:10 / 20:00 / 21:00 + 接力 22:30 / 00:30**。我只改了 note 里的执行机错误，**`freq` 数字没动**（怕影响 `align_logic_ops`），请你一并对齐 | 本文 |
| 5 | 盘中高频空转（A1~A4） | 1505 已提：盘中门禁上移到 job 最前 / 派发侧统一门禁 / `cancel-in-progress` / 仅产物变更才触发构建 | 13:48 / 15:05 |

## 8. 🔴 主人待拍板（**未拍板前不得改评分口径**）

**CRDS 同一张卡并存两套大盘口径**（已如实写进逻辑详解页）：

| 用途 | 函数 | 口径 |
|---|---|---|
| 卡片「有效性判断」文案 | `get_market_context()` | `raw_data/index_quotes.json` **综合口径**（上证/深证成指/创业板指/科创50 等权 + 全市场涨跌家数比，2026-08-19 主人令） |
| 条件②「大跌日逆势板」判定 | `detect_market_down()` | **上证指数** 近 10 日 K 线（`MARKET_DOWN_THRESHOLD = -1.5%`） |

同批候选实测对照（今晨已跑，零网络）：上证 **-0.97% → 精选 0 只**；深证成指/创业板指/中证500/综合口径 → **5 只**；历史误用的平安银行 → 11 只。
**方案 A 综合口径等权（推荐）/ B 中证500 / C 保留上证 —— 等主人拍板。**

---

## 9. 请你复核 3 件事

1. **拉 main 看** `88deb6258644`（回退步）与 `9c8a0f33c78a`（文档对齐），确认没顶掉你今天的改动。
2. **盯 21:00 / 22:30 / 00:30 三档**：回退步是否在 25 分钟内跑完、链是否真正执行（`step 9 = success`）、B 是否收敛到 8/9、D 批是否产 `FINAL_RECOMMEND_DATA`。
3. **别回退 `88deb6258644`**（除非读完本文 §6 的安全性论证）。

## 10. 我本轮遵守的红线

- ✅ token 只从 `C:\Users\HH20210606\.workbuddy\v8_gh_pat`（普通文件）与 `v8_gh_token.txt`（workflow scope）读取，**未打印、未写入任何文件**
- ✅ 未使用 `git add -A`；未 cp 备份进仓库；两笔提交均经 **Git Database API**（blob→tree→commit→PATCH ref）推送，**未 force**
- ✅ 新鲜度判定只看文件内 `update_time`，**不看 mtime**
- ✅ 改 `index.html` 前先拉远端为准，改后原地同步本地，未整文件覆盖元数据

—— **阿狸咪的股票专家**（家机 `alimi-cn`）2026-09-11 20:13 CST
