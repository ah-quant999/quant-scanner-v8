# 交接件 · 8991c96ad「落后基线静默回退」5 项 · 精确重建与锚点封堵

- 发件：**阿狸咪的工程师**（家机 alimi-cn）
- 收件：**小九的工程师**（单位机 lemoncat-cn）
- 时间：2026-09-23 20:25（CST+8）
- 类别：**P0 覆盖事故（第 7 类：落后基线静默回退）** — 已修复、已上线、已封堵
- 相关提交：`8991c96ad`（肇事）/ `c08485603`（修复）

## 一、结论一句话

`8991c96ad`（2026-09-23 12:05，自述「净室推送：API 隔离索引，不碰主工作树」）用**落后基线快进提交**静默回退了 **5 项**已上线内容；已用**唯一锚点精确重建**并快进推送 `c08485603`，线上已恢复（远端 API 与 CDN 双验逐字节一致），并登记 **4 条防覆盖锚点**封堵复发。

## 二、铁证（6 条，均可复跑）

1. `GET /repos/ah-quant999/quant-scanner-v8/compare/c08485603...main` → `status=ahead, ahead_by=3, behind_by=0, merge_base=c08485603` ⇒ **历史未被强推改写**（排除 force push），我的提交仍是 main 祖先。
2. `GET /repos/.../commits?path=index.html&since=2026-09-23T00:00:00Z` ⇒ `8991c96ad` 是当日唯一一笔「非缓存戳」改动 index.html 的提交。
3. 远端 main `index.html`（`contents?ref=main` + `Accept: application/vnd.github.raw`）= **1,347,482 B**；7 项功能标记 count 全 ≥1；旧 bug 串 `location.replace(b + sep + 'fresh=')` count=**0**。
4. CDN（`https://ah-quant999.github.io/quant-scanner-v8/index.html`）与 API blob **逐字节相同**（`API==CDN: YES`）。
5. `docs/ops/index_protected_markers.txt` 4 条新锚点 count 全 =1；门禁 `[10/10] check_index_markers` = **39/39**。
6. 门禁 `pre_deploy_audit.py` = **14/14 全绿**（推送前预检，exit 0）。

## 三、被回退的 5 项（已逐条回植；14 个唯一锚点，每条断言 `count(old)==1`）

| # | 项 | 说明 |
|---|---|---|
| 1 | `_reloadForData` | 恢复 09-22 小九根治「一直刷新…连页面都看不了」（旧 BUG 版自行 `location.replace` 已除） |
| 2 | `switchSec` 真锁页「内容层」守卫 | `_lkTab`：非管理员拒绝切入真锁页 |
| 3 | `v8_boot_sec` 真锁页恢复校验 | `_sk && typeof isAdmin` |
| 4 | nav 选股策略 / 策略回测 恢复加锁 | 09-22 主人令 |
| 5 | 因子榜「推荐置顶同序」 | FR 权威序（`_frIdx` / `_rest` / `_extraByCode` / `_recRows` / `_notInPool`） |

**有意删除、不恢复**（主人拍板）：`unlockCoverCard`（整函数）与 `INST_COVERAGE`（`<script src>` 行）。重建时已显式排除。

## 四、修复与上线

- 手段：**隔离 worktree 净室推送**（`git worktree add --detach <最新tip>`，显式 `git add` 目标路径，不碰主工作树 99 个 ` M`）。
- 精确重建：**禁**「回退整份父版本」；14 条唯一锚点 replace + `count(old)==1` 断言；`io.open(..., newline="")` 保 LF。
- 推送真值：`5cac8668f..c08485603  HEAD -> main`（push_exit=0）；`remote_tip == my_commit == c08485603eed8b42da35351f7e6a848b8d8b0cb2`；`ls-remote` 回读一致。
- 中途远端再前进（60567b3e3 → 48a918fc4 → 5cac8668f）⇒ rebuild-on-advance 重试循环，第 2 次成功。

## 五、防覆盖封堵

`docs/ops/index_protected_markers.txt` 新增 4 条（日期=当日 2026-09-23）：

```text
推荐置顶同序|因子计分榜「以最终推荐为权威序」口径（2026-09-23 曾遭 8991c96ad 落后基线静默回退）|2026-09-23
2026-09-22 主人令：选股策略恢复加锁|选股策略/策略回测 09-22 恢复加锁（仅管理员可见）|2026-09-23
真锁页「内容层」强制守卫|switchSec 真锁页内容层守卫（_lkTab，非管理员拒绝切入）|2026-09-23
2026-09-22 小九根治「一直刷新，连页面都看不了」|防「一直刷新」根治（只记录数据戳、绝不自行导航）|2026-09-23
```

门禁 `[10/10]` 由 35/35 升至 **39/39**。

## 六、待办 / 请小九配合（两条）

1. 🔴 **锚点法固有缺口：拦不住「同一文件内部换回旧版」**（锚点还在，只是某段被换回旧实现）⇒ 正解 = **blob 父子比对**（父 blob ≠ 本 blob 时，差异必须含「非 `?v=` / `BUILD`」的实质内容），**尚未实现**，建议列为门禁 [15/15]。
2. 🔴 **所有「净室推送 / API 隔离索引」脚本，推送前必须 `git fetch origin main` 并以最新 tip 重建树**。本次即因本机 clone 停在旧 tip `68670e6a6`、直接复用了它的树 ⇒ 落后基线。**`force:false` 拦不住这一类**（父提交是最新 tip ⇒ 天然 fast-forward，无 422、无告警、`git log` 里你的提交还在）。

---

> 复跑入口（仓库外临时区）：`E:/_alimi_tools/tmp_compare/gh_probe10.py`（线上现态）、`gh_probe11.py`（compare behind_by + 锚点）、`gh_probe12.py`（后续提交文件清单 / index.html 戳记判定）。
