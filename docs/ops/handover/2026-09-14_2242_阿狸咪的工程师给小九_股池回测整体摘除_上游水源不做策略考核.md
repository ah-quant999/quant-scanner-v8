# 交接单 · 股池回测整体摘除（候选池 / 金股池 = 上游水源，不做策略考核）

- **时间**：2026-09-14 22:42 CST
- **发件**：阿狸咪的工程师（家用机 cat-1）
- **收件**：小九的工程师（单位机 cat-2）
- **触发**：主人令（附截图，原话见下）

---

## 一、主人原话与病灶实证

> **「说了多少次了，金股池和候选股池是算法的上游水源，不是策略，不需要回测！！！」**

截图文案：`3. 金股池 T+1 仅 32% 胜率：查是样本偏差还是信号本身弱。`

**产地已用产物定格证**（`git show <远端>:raw_data/backtest_all_algos.json`，update_time `2026-09-14 16:02:15`）：

```json
"delist_advice": [{
  "card": "金股池", "label": "首次满足信号 T+1", "icon": "🪙", "rank": 4,
  "sample": 127, "win_rate": 32.28, "avg_return": -1.79,
  "reasons": ["胜率 32.28% < 红线 45.0%", "平均收益 -1.79% ≤ 红线 0.0%（已扣成本）"],
  "ask": "是否下架「金股池」并停止跟踪？"
}]
```

⇒ **上游水源被写进了「拟下架」提请**。同产物 `rows` 里 24 条池子行（候选池 12 + 金股池 12）全部 `cat="trade"`，`ranking_all` 里也有 5 条池子。

---

## 二、变更清单（单 commit：7 改 + 5 删 + 本单）

| # | 文件 | 改了什么 |
|---|---|---|
| 1 | `algorithms/run_algorithms.py` | `ORDER` + `STAGES["E"]` 摘除 `backtest_pools.py`（E 批 **12→11**、ORDER **51→50**） |
| 2 | `.github/scripts/v8_stage_gate.py` | `READY_SPEC["E"]`：**5 项 / need 4 / must 3** → **3 项 / need 2 / must=[BACKTEST_ALL_ALGOS.js]**；docstring 同步 |
| 3 | `algorithms/gen_backtest_all_algos.py` | 删两条池子 `SOURCES` + `parse_pool()` + `PARSERS["pool"]`；`primary_rule` / `known_gaps` / 文件头注释同步 |
| 4 | `v8_health_check.py` | 删 `CANDIDATE_BACKTEST` / `GOLD_POOL_BACKTEST` 两条健康登记（池子**上游数据**卡 `CANDIDATE`/`GOLD_POOL` 保留不动） |
| 5 | `scripts/v8_delist_advice.py` | **★ 新增 `POOL_CARDS` 豁免**：`judge()` + `_from_rows()` 双处跳过池子 —— 这条才是那条「拟下架」提醒的产地 |
| 6 | `index.html` | 删两个池子回测 `<script>` 标签；**护栏补 `黄金池` 别名**（见 §四） |
| 7 | `logic.html` | E 批脚本数 **12→11**；④ E 就绪清单改写为 **2/3 项 + must**；删除两池产物 |
| 删 | `algorithms/backtest_pools.py` · `data/CANDIDATE_BACKTEST.js` · `data/GOLD_POOL_BACKTEST.js` · `raw_data/candidate_backtest.json` · `raw_data/gold_pool_backtest.json` | 全删 |

**保留**（有意保留，勿删）：
- `index.html` 的 `__STAR_KIND` 基础股池护栏（7 处 `=== / !== 'pool'` 判定）—— 见 §四；
- `v8_health_check.py` 的 `CANDIDATE` / `GOLD_POOL` 上游新鲜度卡 —— 池子该被考核的是「有没有产出」，不是「胜率」；
- `backtest_tdx.py` 把金股池当**选股宇宙**用（这是上游水源的正确用法，不是回测池子本身）。

---

## 三、验证证据（全部本地实跑）

| 项 | 结果 |
|---|---|
| `py_compile` × 5 个 py | ✅ 全通过 |
| AST 复现模块级断言 `_STAGE_UNION == set(ORDER)` | ✅ **成立**（E 批 11 / ORDER 50）⇒ 盘后链启动不会崩 |
| `READY_SPEC["E"]` | ✅ `items=[CRDS_BACKTEST, BACKTEST_TDX, BACKTEST_ALL_ALGOS]` `need=2` `must=[BACKTEST_ALL_ALGOS]` |
| `SOURCES` 条目 | ✅ 6 条（三重共识/四量终极/逆势龙头/强势突破/K线信号层/因子实验室），**无池子** |
| node 全量 inline `<script>` `new Function` | ✅ **27 块 / 0 错**（与远端原版一致） |
| 行尾（二进制判） | ✅ `logic.html` 纯 CRLF 保持 · `index.html` 纯 LF 保持 |
| 替换器缺陷回归 | ✅ 无未展开反向引用落盘 |

---

## 四、诚实更正（我先前基于过期数据误判，已纠正）

我一度判断「护栏因 `金股池` vs `黄金池` 命名不一致而漏网」。**复核后更正**：

- 本地那份 `raw_data/backtest_all_algos.json` 是 **09-13 13:00** 的过期版，卡名写的是 `黄金池`；
- **远端 09-14 16:02 的新版卡名是 `金股池`**，两个名字都在 `__STAR_KIND` 里 ⇒ **线上护栏本来是生效的**（主对比表/档位矩阵/最优组合榜都已排除池子）；
- 真正漏的是 **`delist_advice` 下架提请**（聚合器 advice 循环无池子豁免）—— 已由 #5 修掉。

不过「两种写法都出现过」是事实 ⇒ 护栏仍补 `黄金池` 别名（#6），兼容任何历史/残留产物。

---

## 五、⚠️ 未在本地重算的产物（有意，非遗漏）

`raw_data/backtest_all_algos.json` + `data/BACKTEST_ALL_ALGOS.js` **未在本地重算**：

- 远端该产物是 **09-14 16:02** 的更新版，本地同源产物停在 09-13 ⇒ **本地重算＝降级**（会把全站回测数据拉回昨天）；
- 故按「改动生效靠重算」铁律，**交下一轮云端 E 批自然清掉 24 条池子行**；本轮不会出现「池子又回到页面」——前端护栏仍在（§二 保留项）。

---

## 六、给小九的三条

1. **禁止再挂回**：`backtest_pools.py` 已删。原 09-13「只要接入算法链的选股策略都要有回测」一令**已作废**（池子不是策略）。别照旧单子恢复。
2. **门禁口径已变**：E 批就绪 **3 项 / need 2 / must=[BACKTEST_ALL_ALGOS]**。若你的本机/脚本里还有「E 批 4/5 项」的旧判据，请同步，否则会一直等一个永不存在的产物而空转。
3. **`v8_delist_advice.py` 加了池子豁免**：如果你那边也在跑它，注意别把 `POOL_CARDS` 判断删掉 —— 否则「拟下架金股池」会复发。

---

## 七、下一轮请复核

- [ ] 下一轮云端 E 批后：`raw_data/backtest_all_algos.json` 的 `rows` / `ranking_all` / `delist_advice` 里**不再出现池子**（预期池子行 0 条）；
- [ ] `data/HEALTH_CHECK.js` 里不再有 `CANDIDATE_BACKTEST` / `GOLD_POOL_BACKTEST` 两卡；
- [ ] `data/CANDIDATE_BACKTEST.js` / `data/GOLD_POOL_BACKTEST.js` **不再被重建**（若被重建 = 有残余调用方没清干净，立即回报）。


---

## 八、收尾补记（交接单写完后才发现的一处**漏网消费面**，已修）

### 8.1 漏网项：`dedup_fetch_manifest.py::_ALWAYS_PUSH` 的两条幽灵条目

**发现路径**：交接单推完后（`76d214a65`）做「全树残留 token 审计」，发现
`.github/scripts/dedup_fetch_manifest.py` 白名单里仍留着
`data/CANDIDATE_BACKTEST.js` / `data/GOLD_POOL_BACKTEST.js` 两行，
**且标注 `READY_SPEC["E"].must`** —— 而该 must 已随 E 批收敛（5/4/3 → 3/2/1）消失、产物已删除。

**为什么之前那轮没查到**：我的消费面清单只覆盖了「会**读产物**的代码」（生成器/闸门/健康检查/前端/下架提请），
漏了「**登记产物名**的配置表」。后者不读产物，只在推送去重时按名单放行 ⇒ 静态扫描不易命中。

**性质判定（不夸大，也不缩小）**：
- 本表不变式是**单向** `READY_SPEC[*].items ∪ must ⊆ _ALWAYS_PUSH`（超集安全）
  ⇒ 幽灵条目**不破坏**闸门收敛，**不是 P0**；
- 但它是**死配置**，且该文件自己 L132-138 已记录过一次同类事故
  （2026-09-13 该表含 7 项线上不存在的产物，含 ETF_SUBSCRIPTION 类死产物）
  ⇒ 按「零幽灵」清除，属**同类复发**，不是新问题。

**修复**：删除该 2 行，原位留历史说明注释；条目数 **34 → 32**。提交 `9aacd5715`。

### 8.2 CI 实测证据（不是推断）

| 证据 | 值 |
|---|---|
| 我的主体提交 | `76d214a65`（22:43） |
| 云端构建提交 | `8d7787cb7`「v8 build: 2026-09-14 22:51」**存在于 main** ⇒ 构建步未被门禁拦下 |
| 构建 workflow 门禁步 | `.github/workflows/v8_build_deploy.yml` L165 `run: python .github/scripts/pre_deploy_audit.py`，**无 `|| true`**（禁假 success）⇒ 该步绿 = 6/8 门禁全过 |
| 离线复现 `pre_deploy_audit` | **8/8 全过 EXIT 0**（py_compile 172 / new Function **27 块 0 错** / data 101 / align_logic_ops EXIT0 / YAML 30 / HTML 引用 168 / gate 头注一致 / 心跳名一致） |
| 离线复现 `align_logic_ops` | **EXIT 0** |
| 离线复现 `v8_verify_layer_parity` | **EXIT 0** |
| 行为护栏 | 24 项专项复检全绿（见 8.3），且在**最新 tip** `9aacd5715` 上复跑仍全绿 |

其中 `[6/8] HTML 数据引用：168 个本地引用全部存在` 是关键一条 —— 它正是会抓「悬空 `<script src>`」的门禁，
它绿即**证明两个池子回测的外链标签删干净了**（否则会报 404 引用）。

### 8.3 给小九的复核判据（**给判据，不给脚本**）

本机验证脚本在仓库外私有目录（`git ls-tree origin/main | grep _pools/` = 0 行），你结构性拿不到；
请用**你自己的等价工具**按下表逐项验（全部是「取远端 `origin/main` 的 blob/树」只读操作）：

| # | 判据 | 期望 |
|---|---|---|
| 1 | `data/CANDIDATE_BACKTEST.js` / `data/GOLD_POOL_BACKTEST.js` / `raw_data/{candidate,gold_pool}_backtest.json` / `algorithms/backtest_pools.py` | **5 项全部不在树中** |
| 2 | `algorithms/run_algorithms.py` 内嵌 `ORDER` 与 `STAGES` 并集（AST 抽真源，**禁 substring**） | **50 / 50**，且并集 == `ORDER` 集合 |
| 3 | `README_SPEC["E"]` —— 即 `v8_stage_gate.py::READY_SPEC["E"]`（`ast.literal_eval`，兼容 `AnnAssign`） | `items` 3 项 / `need` 2 / `must` = `[data/BACKTEST_ALL_ALGOS.js]` |
| 4 | `gen_backtest_all_algos.py::SOURCES` 条目数（AST 抽，数 `dict(card=...)`） | **恰好 6**，且无 `候选池/金股池/黄金池` |
| 5 | `index.html` 中池子回测 `<script src=...>` 标签 | **0 个**（正则判 `<script…src="data/(CANDIDATE\|GOLD_POOL)_BACKTEST.js`）<br>⚠️ 勿用「整文不含该文件名」——注释里**有意**提到旧名，会假失败 |
| 6 | `index.html` 的 `__STAR_KIND` | 含 `候选池/金股池/黄金池` 三个 `→ 'pool'`；`=== 'pool'` 护栏 **≥7 处** |
| 7 | `scripts/v8_delist_advice.py` 的 `POOL_CARDS` | 出现 **≥3 处**（1 定义 + 2 消费点） |
| 8 | `logic.html` E 回测批脚本数 | **11**；`<td>12</td>` 不应再出现在该行 |
| 9 | `v8_health_check.py` 登记 | **无** `CANDIDATE_BACKTEST`/`GOLD_POOL_BACKTEST`；**仍保留** `{"id": "CANDIDATE"` 与 `{"id": "GOLD_POOL"`（上游新鲜度卡） |
| 10 | `.github/scripts/dedup_fetch_manifest.py::_ALWAYS_PUSH` | 条目 **32**；**无**上述两产物名（除注释）；且 `READY_SPEC[*].items ∪ must ⊆ _ALWAYS_PUSH` **成立** |
| 11 | 行尾（**只能字节级数 CR**，禁用 `text=True` 通道，见下） | `index.html` CR=0 且 LF>0；`logic.html` CR == LF > 0；`.py` CR=0 |
| 12 | `pre_deploy_audit.py` | **8/8**；三个跨层脚本 EXIT 0 |

🔴 **行尾判据的通道陷阱（本轮实测，两个方向都出错）**：用
`subprocess.run(..., text=True)` 读 blob 会做**通用换行转换**（`\r\n` → `\n`）
⇒ 期望 LF 的判据**恒绿**（真空）、期望 CRLF 的判据**假红**。
行尾一律另开**字节**通道（不加 `text=True`）。

### 8.4 待下一轮验证（不变）

- [ ] 下一轮云端 E 批后：`backtest_all_algos.json` 的 `rows`/`ranking_all`/`delist_advice` 中池子行 **0 条**；
- [ ] `data/HEALTH_CHECK.js` 不再有 `CANDIDATE_BACKTEST`/`GOLD_POOL_BACKTEST` 两卡
      （当前那份是 22:30 生成的，早于 22:43 改动 ⇒ 属**正常滞后**，非漏改）；
- [ ] 两个池子回测产物**不被重建**（被重建 = 仍有残余调用方）。
