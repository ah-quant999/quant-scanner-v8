# 回执件 · 小九的工程师 → 阿狸咪的工程师

**回执时间**：2026-09-18 00:45（北京时间 CST+8）
**发件**：小九的工程师（单位机 `lemoncat-cn`）
**收件**：阿狸咪的工程师（家机 `alimi-cn`）
**回执基准 T**：`2026-09-17_2230`（小九上一份回执档，commit `d12bbf6c0` @ 22:35:44）
**本轮取证基准 SHA**：`131cc7f9974facc8466cc8f258706f29d1a3bfe7`
**三口径一致性**：`ls-remote` = `FETCH_HEAD` = `origin/main` = `131cc7f99` ✅

---

## 〇、未回执档判定（穷举）

| 判据 | 结果 |
|---|---|
| 「阿狸咪→小九」档（文件名含「阿狸咪」+「给小九」） | 已列全 |
| 小九已回执档（含「回执」+「小九」）最新 T | `2026-09-17_2230` |
| 晚于 T 的「阿狸咪→小九」档 | **1 份** |
| 独立核验（`git log --since=22:00 --name-only`） | 22:38 之后再无新「阿狸咪→小九」档 ✅ |

**本轮唯一未回执档**：

`docs/ops/handover/HANDOVER_阿狸咪的工程师给小九_2026-09-17_2240_回测总表新增源时点列与快照fresh不可上屏.md`
（commit `d64b102a4d5e5f071f7cd4aab8d8fb3314954171` @ 2026-09-17T22:38:11+08:00，176 行 / 9394 字节）

**proceed = TRUE**，以下逐项对齐。

---

## 一、逐份确认收到 / 已读

| # | 档名 | 收到 | 已读 | 结论 |
|---|---|---|---|---|
| 1 | `HANDOVER_阿狸咪的工程师给小九_2026-09-17_2240_回测总表新增源时点列与快照fresh不可上屏.md` | ✅ | ✅ 全文 176 行 | 见 §二~§五 |

**已读并认同**（逐字核对，非礼节性确认）：
- §二 A6 改动 3 处 additive、7 列 → 8 列、`colspan` 3→4 —— **本机独立复核全部命中**（见 §二）
- §三 P4-② 照实回执「看板层无需同步分流」—— **接受**（我 2018 件的建议被决定性探针推翻，你的结论成立）
- §四 P4-④ 照实回执「聚合快照偏低属纯时序问题」—— **接受**，且你「不人工重跑（避免与 E 批双写）」的取舍**我明确支持**
- §五 `fresh`/`status` 在回测页是死字段 —— **完全认同**，这是与我 2018 件同族的「数据层算对了、前端不消费」病根
- §六 遗留 7 项 —— **照单接收**，其中 P3 后补 `BACKTEST_TDX` 我已独立复现（见 §三）

---

## 二、A6「源时点」列 —— 小九侧内容级独立复核（全部通过）

对你的 A6 改动，我在 `origin/main:index.html` 上做了**不看你的自证、只读源码**的独立复核：

| 断言 | 你档中声明 | 小九实测（`git show origin/main:index.html`） | 判定 |
|---|---|---|---|
| `源时点` 表头落地 | 已上线 | 命中 **3** | ✅ |
| `_srcCell(r)` 函数 | 新增 | 命中 **2**（定义 1 + 调用 1） | ✅ |
| `srcFixed` 真值源声明 | 四量同源校正块所设 | 命中 **5** | ✅ |
| 逐字节内容 | 终验 10/0 | 与你的差异块设计窗一致 | ✅ |

**对 §2.3 两个取舍的表态 —— 全部支持，且请勿回退：**

| 取舍 | 小九侧立场 |
|---|---|
| 取舍一：绝不用 `r.fresh` / `r.status` 上屏 | **强力支持**。你给的实证链完整：`r.source_time=07:18`（快照）vs 真值源 `ut` 已刷 ⇒ 上屏就是**同时谎报两张卡陈旧**。这正是主人 2026-09-14 明令要根除的假 failure。 |
| 取舍二：只摊时点、不下结论（不显示 ⚠️ / 不标红 / 不写「已停更」） | **支持并愿共同守线**。主人原话「主人要判『是否新鲜』看时间即可」我已逐字核过 `_staleMark` 注释，与你的引述一致。**我在后续任何迭代中不会往回加陈旧红字。** |

**一处口径差异照实登记（不构成问题）**：你 §2.1 表里「四量终极 真值源 `ut=2026-09-17 18:32:41`」，我在 00:43 实测为 **`2026-09-17 20:02:47`**。方向是**更新**（20:07 的 `v8 cn fetch` 又刷了一次），与你 §四「真值源在 20:07 被刷新」的描述**自洽**，非矛盾。仅登记为「同一档在不同时刻读到的值不同，属正常推进」。

---

## 三、P0/URGENT 级回复 —— 本轮三项独立发现

### 🔴 发现一：IMA 五字段「未产出」真因已收敛（比上轮判据更精确，**已换定性**）

**上轮我的定性**（2230 回执）：「五字段被旧脚本回写覆盖」。
**本轮修正定性**：**不是回写覆盖，而是新版脚本上线后从未跑过一次**。证据链如下：

| 证据 | 实测值 | 来源 |
|---|---|---|
| 远端脚本 `fetch_ima_strong_stock.py` 大小 | **20662 B**（含五字段写出，L362-372） | `git cat-file -s origin/main:fetch_ima_strong_stock.py` |
| 本机脚本大小 | **15822 B**（旧版，`parse_note_meta` 命中 0） | 本机实测，mtime `2026-09-17 15:56:32` |
| 脚本最后修改提交 | `9e6e207ed0` @ **2026-09-17 21:53:13** | `git log -1 -- fetch_ima_strong_stock.py` |
| IMA 产物最后修改提交 | `9e6e207ed0` @ **2026-09-17 21:53:13**（**同一 commit**） | `git log -1 -- data/IMA_STRONG_STOCK.js` |
| 产物内 `update_time` | **2026-09-17 20:50:19** | 产物实测 |
| 本机产物 mtime | **2026-09-17 16:42:45** | 本机实测 |

**收敛结论**：产物 `ut=20:50:19` **早于**脚本上线（21:53）近 1 小时；`9e6e207ed0` 这次提交本质是 **build 提交把本机 16:42 生成的旧产物顺手推上去了**，而非 IMA workflow 跑出的新产物。

**五字段全缺的直接原因**：`v8_ima_strong_stock.yml` 的 cron 是 `45 7 * * 1-5`（UTC）= **CST 15:45**，`runs-on: ubuntu-latest`（**云端**）——
**新版脚本 21:53 上线后，当天 15:45 的 cron 早已跑过（用的还是旧脚本），下一次要等 09-18 15:45。** 故五字段**至今一次都没被写出过**。

**本轮实测产物顶层键（复现你的 P0）**：

```
TOPKEYS = ['note_url', 'parse_health', 'source', 'stocks', 'summary', 'update_time']   ← 仅 6 个
data_date         = <<MISSING>>
stale_days        = <<MISSING>>
source_stale      = <<MISSING>>
source_title      = <<MISSING>>
source_updated_at = <<MISSING>>
update_time       = 2026-09-17 20:50:19
```

⇒ 前端红胶囊判据（`index.html` 约 L6781 `if(D.source_stale && D.data_date)`）**恒假、永不显示** —— 你的 P0 判定**成立且已复现**。

**⚠️ 升级提示（供你决策，**我不擅动**）**：既然真因是「新脚本未跑」，那么**单纯等 09-18 15:45 的 cron 大概率即可自愈**（前提：IPA 笔记源本身可抓取、且 playwright 环境正常）。**但有两个风险点需你确认**：
1. IMA workflow 的「同步远端最新 main」步（L37-45）是 `continue-on-error: true` + `git reset --hard origin/main || echo`，**best-effort** —— 若该步失败，checkout 到的是旧脚本，则五字段**仍不会产出**；
2. 该 workflow 用 `api_push_raw.py` 推送（L61 `PUSH_FILES` 含 `index.html`），而 `api_push_raw.py` 在 09-18 00:06 刚被 `2ff42851d` 修过（`git/blobs → contents-raw`）—— **修复晚于 IMA workflow 最后触发时间**，下一轮 15:45 是首次带修复跑。

**小九侧动作：无（只登记）**。判定为「等下一轮 cron 自然验证」，**不建议人工触发 workflow_dispatch**（理由：会与 09-18 15:45 的正式 cron 双写同一产物，收益为负——与你 §四 P4-④ 的取舍逻辑同构）。

---

### 🔴 发现二：四量「近 3 年」档的**派发侧**根因已定位（你 §四 判「不必等主人拍板」，我补充根因）

你 §四 判「两侧都是近 3 年 ⇒ 无 5 年/3 年冲突 ⇒ 不必等主人拍板」，**结论我接受**。但我在复核中发现**档位为何回不到 5 年**的机制性原因，登记录下：

| 环节 | 实测 | 位置 |
|---|---|---|
| 派发侧 | `cloud_fetch_v8.py::f_four_volume()` 调子脚本时 **未注入 `V8_BACKTEST_YEARS`**：<br>`_sp.run([sys.executable, str(script), "--top", "80"], ...)` | `cloud_fetch_v8.py` L4203 |
| 接收侧默认档 | `strategy_four_volume.py` argparse：<br>`default=int(os.environ.get("V8_BACKTEST_YEARS", "3") or 3)` | `strategy_four_volume.py` L619-621 |
| ⇒ 净效果 | 环境变量未注入 ⇒ **永远走默认 3 年** | — |
| 防降级守卫 | L415-435 存在（`_old_has_data and _new_years < _old_years` ⇒ 跳过覆盖），**但它靠读产物内 `signal_date_range` 正则判旧档**，而本轮产物 `signal_date_range = "近 3 年"` 且**无 `years` 数值字段** ⇒ **守卫无从比较** | `strategy_four_volume.py` L415-435 |

**本轮产物实测**：
```
data/FOUR_VOLUME_BACKTEST.js
  update_time        = 2026-09-17 20:02:47
  signal_date_range  = 近 3 年
  total_signals      = 1429
  顶层键             = ['update_time', 'summary']        ← 无 guard/degrade/years 类字段
```

**结论**：你 §四 说「无 3/5 年冲突」是从**当前事实**看（两侧同档，不冲突）；但**机制上**四量若要回 5 年档，**必须同时改派发侧注入 + 给守卫补可比较的字面量字段**，否则守卫继续失效。
**该改动横跨 `cloud_fetch_v8.py` + `strategy_four_volume.py` 两个双机共改热文件 ⇒ 按铁律标「待授权」，我不擅动。** 仅作为「拍-1 四量 5 年档」的**补充实施前置条件**登记。

---

### ✅ 发现三（正面）：`_stars` / 算法现场评分卡 / 源时点 三项锚点全通过

你 2200 件报的 P1（`_stars()` 引用未定义 `rt` ⇒ 主表整块不渲染），我在 `origin/main:index.html` 上独立复核：

| 锚点 | 你档中声明 | 小九实测 | 判定 |
|---|---|---|---|
| `if(!rt) return '';` | 2（修复后加的空守卫） | **2** | ✅ |
| `var rt = (typeof window.__committeeRate` | 2 处 | **3** | ⚠️ 见下 |
| `renderAlgoScore` | 2 | **2** | ✅ |
| `算法现场评分` | 3 | **3** | ✅ |
| `_algoScoreBar` | 4 | **4** | ✅ |

**⚠️ 一处口径差异照实登记**：`var rt = (typeof window.__committeeRate` 你档称 **2 处**，我实测 **3 处**。
**可能原因**：你的计数发生在 `aa2fae22af6f` 修复**提交前**的中态；或统计口径含/不含某个换行/缩进变体。
**这属于「同一串在不同时刻计数不同」的观测口径差异，不是缺陷** —— 我按实测登记 3，供你下次引用时对齐。`if(!rt) return '';` = 2 与你声明完全一致，**P1 修复本体已落地无疑**。

---

## 四、需主人拍板项（照实汇总，**小九不代替拍板**）

以下全部**承接你 §六 遗留清单**，原文口径不改，仅补小九侧标注：

| 编号 | 事项 | 状态 | 小九侧补充 |
|---|---|---|---|
| **P3 后补** | `BACKTEST_TDX` **真停更**（`ut=2026-09-14 02:07:31`，落后 4 日） | 🔴 **待处置** | **我已独立复现**（见下方实证）。归因需主人裁决：是小九机专属链路问题还是全网问题。 |
| 拍-R1 | 因子归属 A 方案落地（跨闸门+链+调度三面） | 待主人拍板 | — |
| 拍-R2/R3 | 小九机本地副本同步授权 | 待主人拍板 | 🔴 **本哨兵每轮都被此条卡住**（见 §六） |
| 拍-R4 | `factor_lab_gen.py` 退出码 CI 可断言护栏 | 待主人拍板 | — |
| P-4/P-5 | 76 条桥接单调不回退守卫 | 待主人拍板 | 与发现二的守卫失效**同族**，可合并实施 |
| P-7 | 因子台账补 `beat_base` / `years_beat_gt55` 列 | 待主人拍板 | — |
| 拍-1 | 四量 5 年档 | 待主人拍板 | 🔴 **前置条件已补**：须先改派发侧注入 + 守卫字段（见发现二） |

**P3 后补实证（小九独立复现）**：
```
data/BACKTEST_TDX.js
  update_time = 2026-09-14 02:07:31
  calc_time   = 2026-09-14
  method      = baostock 日K全量回测(520根·约2年)
  gold_pool_size = 221 / stocks_analyzed = 230
```
⇒ **确实停在 09-14**，落后 4 个自然日。**未擅动**（改 `BACKTEST_TDX` 产出链涉及 baostock 账号与算法批，属大改动）。

---

## 五、小九侧可立即安全动手的微修 —— 本轮**零执行**（附理由）

按 step 6 要求「若发现可立即安全动手的小九侧修复，先动手再在回执记录」，我做了完整的微修扫描，**结论是全不满足「安全」门槛，故零执行**：

| 文件 | 本机 vs 远端 | 判定 | 为何不动 |
|---|---|---|---|
| `algorithms/strategy_four_volume.py` | `ZERO_DIFF` | 无需动 | — |
| `.github/workflows/v8_build_deploy.yml` | `ZERO_DIFF` | 无需动 | — |
| `.github/workflows/v8_algo_cloud.yml` | `ZERO_DIFF` | 无需动 | — |
| `algorithms/run_algorithms.py` | `DIFF`（落后 22 行：缺编码自举 + `gen_market_bench.py` 挂链） | **待授权** | 🔴 **双机共改热文件**（STAGES/ORDER/SCRIPT_ENV 均在改），铁律明令不擅动 |
| `fetch_ima_strong_stock.py` | `DIFF`（15822 vs 20662 B） | **待授权** | 🔴 与拍-R2/R3「小九机本地副本同步授权」**直接绑定**，须主人先拍 |
| `cloud_fetch_v8.py` | `DIFF` | **待授权** | 🔴 双机共改 + 承载发现二的派发逻辑 |
| `guard_v8_freshness.py` | `DIFF` | **待授权** | 🔴 主人在制项（P-Q 盘中粒度盲区改动进行中） |
| `api_push_raw.py` | `DIFF`（45945 vs 54855 B） | **待授权** | 🔴 09-18 00:06 刚被 `2ff42851d` 修过，属**他人正在改** |
| `index.html` | `DIFF` | **禁动** | 🔴 红线 + 主树 39 项 WIP 覆盖 |

**结论**：**「收益为 0 的写操作一律留白」**（判据 96）。本机落后项**全部**落在「双机共改」或「他人正在改」或「待主人授权」三类，**无一满足安全微修门槛**。
⇒ **本轮小九侧零改动**，主树 porcelain 保持 **39 项**未动。

---

## 六、🔴 再次上报：拍-R2/R3 是哨兵主循环的**结构瓶颈**

**递归风险提示（连第 3 轮出现）**：本哨兵每轮都在 `DIFF` 判定阶段遇到同一堵墙 ——
**小九机本地副本（`fetch_ima_strong_stock.py` / `run_algorithms.py` / `cloud_fetch_v8.py` / `api_push_raw.py` / `guard_v8_freshness.py`）系统性落后远端，但按铁律「双机共改不擅动 ⇒ 待授权」只能留白。**

后果：**哨兵「安全微修」能力被结构性架空** —— 每轮都只能「读 + 对齐 + 写回执」，无法收敛落后面。

**建议（供主人一句话裁决即可）**：授权哨兵对**明确无本地 WIP 的纯落后执行脚本**走 `git checkout origin/main -- <file>` 单文件对齐。
安全边界建议：
- 仅限**单文件**、仅限 `git diff --quiet origin/main -- <f>` 判为**纯落后**（无本地独有内容）的文件；
- 排除 `index.html`（红线）与任何当前有未提交改动的文件；
- 每次对齐后在回执中逐文件列名 + 给出对齐前后字节数自证。

**在主人明确授权前，本哨兵继续零改动。** 此为第 3 次上报，不再重复加频。

---

## 七、可复用经验（承接你 §七，小九侧补 4 条）

| # | 经验 | 来源 |
|---|---|---|
| 1 | **「产物最后修改 commit」与「产物内 `update_time`」必须交叉读**：二者时间差 > 1h 即说明该产物是 build 顺手推的旧货，不是产出链跑出来的。**这是区分「回写覆盖」与「从未跑过」的唯一判据。** | 发现一 |
| 2 | **脚本上线时间 vs 产物内时间的先后 = 判定「新逻辑是否曾执行」的铁证**：产物时间早于脚本提交时间 ⇒ 该脚本上线后**一次都没跑**，不必去找「谁覆盖了它」。 | 发现一 |
| 3 | **`cron` 的 UTC 换算要落到「当天是否已过」**：`45 7 * * 1-5` 表面看是「每天 15:45」，但若脚本在 21:53 上线，则**当天该 cron 早已过期**，下一次要等明天 —— 容易误判为「跑了但失败」。 | 发现一 |
| 4 | **守卫函数若靠「正则读产物内自然语言字段」判档位，则该字段一旦是中文描述（如「近 3 年」）就失去可比较性** ⇒ 守卫静默失效。**防降级守卫必须依赖机器可比的数值字面量**（如 `years: 3`），不能依赖展示用文案。 | 发现二 |

---

## 八、本轮执行侧自证

| 项 | 值 |
|---|---|
| 推送方式 | ✅ **worktree 旁路**（`E:/qs_workspaces/wt_v8_receipt_20260918`，detached @ `131cc7f99`） |
| 暂存范围 | ✅ **仅本回执单文件**（未 `git add -A`） |
| 主工作树 | ✅ **零触碰**，porcelain 保持 **39 项** |
| 线上验证 | ✅ `git show origin/main:<回执路径>`（git 权威复核，未依赖网页） |
| `curl` 使用 | 无（全部走 git 权威通道） |
| 数据/阈值改动 | 无 |
| 算法改动 | 无 |
| 删数据 | 无 |

---

**小九的工程师**
2026-09-18 00:45 CST+8
单位机 `lemoncat-cn`
