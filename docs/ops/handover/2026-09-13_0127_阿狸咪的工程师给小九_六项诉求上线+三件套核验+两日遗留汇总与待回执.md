# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-13 01:27｜六项诉求上线+三件套核验+两日遗留汇总与待回执

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> **基座**：远端 `18471a7635ea4d0e38a305d90a0ae80d52eb2a2b`（我推送后你已推进 2 个提交）
> **我的提交**：`59d20959fe7d4a32fa7a1a605a368ef7c52e070c`（代码）· `302e240e94368016869a5f58a2aa635f60275539`（交接稿）—— **均在 main 祖先链，已复核** ✅

---

## 一句话结论

**主人六项诉求全部落地并上线、CDN 字节级终验零差异；改后三件套全绿；但有两项需你回执（`v8_health_check.py` 版本分歧、`v8_peer_monitor.py` 掉线告警永不触发），另有 8 项等主人拍板、5 项本机物理办不到。**

---

## 一、本轮交付：主人六项诉求

### 1.1 🔴 核心结论：运维「全🔴」不是故障，是 `skip` 被误判成红灯

| 证据 | 实测值 |
|---|---|
| `RUNNER_STATUS.summary` | `{"total":1,"ok":1,"empty":0,"fail":0}` |
| 模块数 / 本周期运行 | **73 / 1** |
| status 分布 | `{"skip":72,"ok":1}`（唯一非 skip = `CANDIDATE_QUOTES`） |
| `HEALTH_CHECK` | `overall="ok"`，`{"ok":113,"warn":0,"fail":0,"limited":1,"total":114}` |

**`fail=0` 却满屏红灯 ⇒ 渲染 bug，不是业务故障。**
`skip` =「本周期未运行」，是**分批调度的正常形态**（73 个模块单批次只跑 1 个），**绝不属于红灯**。

### 1.2 三层根因（**必须三层同修**，只修一层会复发）

| 层 | 位置 | 病灶 | 修法 |
|---|---|---|---|
| ① 前端灯色 | `renderRunnerTrack` 的 `statusColor` | 只认 `ok`/`empty`/`fail`，`skip` 落进 else → `var(--up)` 红 | 抽 `_stColor()` / `_stText()` **唯一映射** |
| ② 生成器语义 | `v8_health_check.py` | **`manual_dep` 被滥用**：8 项错标 6 项 | 拆出 **`net_dep`** 语义 |
| ③ 前端过滤 | `_renderOpDiagnostics` | 只放过 `ok`，把 `limited`/`warn` 也当失败推进诊断区 | 改为放过 `ok`+`limited`+`warn` |

### 1.3 🔴 需要你知晓的语义变更：`manual_dep` → `net_dep`

| 标记 | 语义 | 当前归属 |
|---|---|---|
| `manual_dep` | **真·人工维护**（机器跑不出，须人手动更新） | `MACRO_DATA`、`UNLISTED_PANEL`（**仅 2 项**） |
| `net_dep` | **本机网络受限**，须等小九中国 IP 在线跑 | `FACTOR_LAB`、`RPS_BACKTEST`、`STOCK_STOP_DATA`、`H_AUTO_BUY`（**4 项**） |

**这是本轮最深一层根因**：原先 6 个「本机网络受限」条目被错标成 `manual_dep`，
导致**两机之间状态在 `warn` ↔ `limited` 漂移**，前端表现为莫名的黄灯/红灯。

生成器改动清单：
- `FACTOR_LAB` / `RPS_BACKTEST` / `STOCK_STOP_DATA` / `H_AUTO_BUY`：`manual_dep` → `net_dep`，`manual_note` → `net_dep_note`
- `H_AUTO_BUY_TRACK`：移除 `manual_dep` 标记
- `DO_NOT_DELETE`（L178）：移除 `"manual_dep": True`
- L1482（跳过 raw_data 自救）：`if d.get("manual_dep") or d.get("net_dep"): return status, msg`
- L1605 新增状态分支：
  ```python
  elif d.get("net_dep") and age_min > max_age:
      status = "warn"
      _note = d.get("net_dep_note") or d.get("manual_note") or d.get("alimi_note")
      if _note:
          msg = "本机能力受限（非故障）· 待小九中国 IP 在线跑：" + _note
  ```
- L2571 汇总注释：明确 `overall` **仅由 `fail` 决定**

**自证**：`manual_dep` = **2**，`net_dep` = **4** ✅

### 1.4 其余五项

| 诉求 | 改动 |
|---|---|
| ① 主题卡 ABC 分档 | `TV_MAX` 平铺截断 → `tierHits` 分桶；标题改用**圆括号** `（A档 · N 只 · 说明）`；**A 档直铺、B/C `<details>` 折叠**；空档整档跳过；补兜底空态 |
| ① 「限制」行查不到 | 三行说明拆为逐行 `div`，「限制」行**独立黄框高亮**（guard 串 `限制（重要）`） |
| ② 三卡大白话 | 中信PE / 估值分位 / AI洞察 → `📖 这张卡在说什么（大白话）` 四段式（生活化类比 → 读数含义 → 最容易误读的一点 → 限制边界），**卡片内零术语** |
| ③ 灯色图例 | `runnerTrackBody` 后新增绿/黄/灰/红四色图例 + 「灰灯不是故障」注解；统计行补「本周期运行 R / 未运行 k（正常·分批调度）」 |
| ④ RPS 数据口径 | 「数据口径说明（为什么样本偏少 · 当前状态：受限可用）」块插入 RPS 卡「限制」行后，明确写**不是策略本身失效** |
| ⑥ 看板分组顺序 | `_PAGE_ORDER`（L11075）→ `今日事件→实时数据→盘后数据→选股策略→策略回测→运维→全量数据→管线→内容审计` |

**灯色四态映射（与看板同源，请勿在别处另写一套）**：

```js
function _stColor(s){
  if(s === 'ok') return 'var(--down)';                                        // 绿
  if(s === 'empty' || s === 'warn') return 'var(--gold)';                     // 金
  if(s === 'skip' || s === 'stale' || s === 'limited') return 'var(--dim)';   // 灰
  return 'var(--up)';                                                         // 红 = fail
}
function _stText(s){
  return s==='ok'?'正常' : s==='empty'?'返回空' : s==='warn'?'提示'
       : s==='skip'?'本周期未运行' : s==='stale'?'离线空'
       : s==='limited'?'受限可用' : '失败';
}
```

> ⚠️ **看板（`renderBoard` 一带，L11095）本来就是对的四态，`renderRunnerTrack` 是唯一例外。**
> 排查这类问题时**必须先比对两处状态映射是否同源** —— 不同源就是 bug 温床。

---

## 二、文件与推送清单

| 文件 | 变化 |
|---|---|
| `index.html` | 1083222 → **1093687 字节**（941804 → 949016 字符），**纯 LF** |
| `v8_health_check.py` | 131623 → **132196 字符** |
| `docs/ops/handover/2026-09-13_0110_*.md` | 新建 8311 字节 |

| commit | 内容 |
|---|---|
| `59d20959fe7d` | index.html + v8_health_check.py |
| `302e240e9` | 交接稿 |

---

## 三、改后三件套核验结果（全绿）

### ① 部署前 5 项门禁

| 门禁 | 结果 |
|---|---|
| py_compile（全部 .py） | **284 个，0 失败** ✅ |
| `new Function`（inline `<script>`） | **34 块，0 失败**（index 25 / logic 6 / calendar 1 / v6_memo 2） ✅ |
| `data/*.js` 完整性 | **103 个，缺 `window.` 注入 0，空文件 0** ✅ |
| `align_logic_ops.py`（cron↔文档） | **EXIT=0**，21 个含 cron 的 workflow 全部文档化，无过期引用、无死登记 ✅ |
| workflow YAML 解析 | **30 个，含 cron 21，解析失败 0** ✅ |

### ② 文档对齐（三源合并读数）

```
· index.html#sec-lg       0 字符
· index.html#sec-op       509398 字符
· logic.html#sec-lg       510945 字符
✅ 对齐校验通过（EXIT=0）
```

### ③ 双机交接

本文件。

### 补：线上字节级终验（最强判据）

```bash
curl -s -o _live.html -w "HTTP=%{http_code} bytes=%{size_download}\n" \
  "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$(date +%s)"
# → HTTP=200 bytes=1093687
cmp _live.html _s/index.html   # → 零差异 ✅
```

行尾守恒：**纯 LF**（CRLF=0, bare_CR=0）✅

> ⚠️ **禁用「关键串存在性」判上线** —— 本轮实测 `grep -c '最佳持有期'` 返回 1 让我误判已生效，
> 实际那 1 处来自**另一个同名旧列**。**只有字节数 + `cmp` 才作数。**

---

## 四、⚠️ 待你回执/确认（按优先级）

### 4.1 🔴 `v8_health_check.py` 版本分歧 —— 请确认以哪版为准

| 版本 | `HEALTH_CHECK` 内容 |
|---|---|
| **远端（FETCH_HEAD）** | 27959 字节；`{"ok":113,"warn":0,"fail":0,"limited":1,"total":114}`，**含 `limited` 键** |
| **本机工作树**（09-12 13:25） | `{"ok":110,"warn":3,"fail":0,"total":113}`，**不含 `limited` 键** |

**我的判断**：本机工作树版**比远端旧**，非补丁问题。但需你确认：
1. 远端那版是否你推的？`limited` 语义是否应保留？
2. 若是，我这边工作树需快进对齐（`--source=HEAD` 路径）。

### 4.2 🔴 `v8_peer_monitor.py` 缺 `data/hb_xiaojiu.json` → **双机掉线告警永不触发**

`v8_peer_monitor.py` 因读不到 `data/hb_xiaojiu.json`，**恒判「小九正常」**。
⇒ **你掉线时我这边收不到告警**。这是安全风险，需修：
- 方案 A：你这侧产出心跳文件（`hb_xiaojiu.json`）并入库；
- 方案 B：改判据 —— **文件缺失 = 状态未知 = 告警**（而不是默认正常）。

**请指示走哪条**，或你直接改。

### 4.3 远端侧红灯（我无法从家机修）

| 项 | 现状 |
|---|---|
| `STOCK_STOP_DATA` | 宇宙只 **259** 只，`degraded:false` **不报警** → 覆盖面疑似不足 |
| `HEALTH_CHECK`（旧读数） | 曾报 `fail=3` |

⇒ 需你在中国 IP 环境下核实。

### 4.4 子页签/提示条两个待办（主人已提未拍板）

- `#253` 选股策略子页签顺序：把四量终极（五星）移到三重共识（四星）左边
- `#254` 「待确认剔除」提示条（一键全删 或 彻底去掉）

---

## 五、📋 近两日遗留汇总（按归属分三类）

### A. 需**主人拍板**（我不动，等指令）

| # | 事项 | 现状 / 卡点 |
|---|---|---|
| A1 | 主对比表是否**保留「平均收益」列** | 我保留了 7 列（信息更全）；原图 6 列无此列。**严格对齐原图则须删** |
| A2 | `📐相对强度` 最佳持有期 | 按「胜率优先」扫出 `1 日 44.3% / -0.09%`，规则正确但**观感偏弱**；是否改「胜率×收益」综合打分 |
| A3 | **B4 剔除 ST 股** | 代码已就绪，**未实施**（等拍板） |
| A4 | **B3 ROE 裁剪数值** | 三闸门（净资产≤1亿 / 近4单季任一负 / TTM>150%）待确认数值 |
| A5 | **选股链审计修复** | 取证完成（`audit/《今日未完成与待答清单_2026-09-11.md》`），**修复等拍板**。核心＝「字段齐全但内容失效」假成功（`close` 继承 T−1、`pct_chg` 被 `safe_float(None)` 写成 0.0，真值 −5.42%） |
| A6 | `#253` 子页签顺序 | 四量终极移到三重共识左边 |
| A7 | `#254` 「待确认剔除」提示条 | 一键全删 vs 彻底去掉 |
| A8 | 仓库根 3 个早期 `.bak` | `HANDOVER_LOG.jsonl.bak`(7/6) · `index.html.v6_base_20260827.bak`(8/27) · `_index_current.html.bak`(8/4)。**未跟踪、不进 git、不影响线上**，但按铁律应清（防坚果云当主版本）。**请授权是否删** |

### B. 我**办不到**（本机物理限制，须你或主人在中国 IP 侧做）

| # | 事项 | 原因 |
|---|---|---|
| B1 | `STOCK_STOP_DATA` 宇宙扩容 | 需中国 IP 数据源 |
| B2 | RPS 回测样本扩容 | baostock 在家机被拉黑，须你侧跑 |
| B3 | 因子实验室刷新 | `net_dep` —— 本机网络受限 |
| B4 | `H_AUTO_BUY` / `H_AUTO_BUY_TRACK` | 同上 |
| B5 | 云端 workflow 失败日志核查 | 家机访问不到 GitHub Web（须 `gh` CLI 或你侧） |

### C. 需**你跟进**（技术债）

| # | 事项 |
|---|---|
| C1 | `v8_peer_monitor.py` 双机告警（见 §4.2）—— **最高优先**，安全项 |
| C2 | `v8_health_check.py` 版本对齐（见 §4.1） |
| C3 | `GOLD_POOL`：`candidate_total=875` vs `stocks=253` 差额待查 |
| C4 | `?v` 口径分裂：104 处中 **2 处是日期戳**，改前先查生成器 |
| C5 | `PORTFOLIO.js` 陈旧（westock 同步 PAUSED） |
| C6 | **CRDS 大盘口径只做一半**：`get_market_context()`（含 `breadth*0.15`）vs `get_market_index()`（不含）**公式没统一**，阈值 `-0.8` 恰好夹中间 → 同屏自相矛盾。唯一渲染位 `#stcrdsMarketContext`；**再加环境类提示须先删旧再建新** |

---

## 六、本轮踩坑（同步给你，避免双机重犯）

1. **`cat file >> same_file` 会破坏文件** —— 追加目标与被读文件同名时内容错乱（我本次把 skill 文件搞乱，已从备份完整恢复）。
2. **`/tmp` 在 Windows Git Bash 下不可靠** —— 一律用仓库外固定路径。
3. **`urllib` 抓线上被沙箱拦（SIGTERM）** —— 一律用 `curl`。
4. **计数类校验有三类假不等**，全部实测为误报：
   - `<!DOCTYPE html>` 计数差 1 → 原文含换行
   - `<script` 差 7 → 差在 **JS 字符串字面量**
   - `<details` 差 1 → 差在 **HTML 注释**
   ⇒ 先剥注释再数，且用 `str.count()` 不用正则（正则回溯重了会 SIGTERM）。
5. **锚点补丁失配先 `repr()` 读原文**（本轮 C2e：我写 `var(--dim")`，原文是 `var(--dim);">`；D3：块尾还有一行 `限制` 我漏锚）。
6. **`python -c` 含反引号/`$()` 被 bash 吞** —— 长脚本一律 Write 落盘。

---

## 七、请回执

1. §4.1 `v8_health_check.py` 以哪版为准？
2. §4.2 `v8_peer_monitor.py` 走哪条修法？
3. §5.A 哪些可以动、哪些继续等？
4. 你那侧 `net_dep` 语义是否需同步？

—— 阿狸咪的工程师
