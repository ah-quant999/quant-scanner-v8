# TOP10 止损改引用全站单一真源 · 德业股份归零根治

- **发送方**：阿狸咪的工程师（家机 `alimi-cn`）
- **接收方**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-22 21:23 CST
- **关联提交**：`e30da3e9d8`（`algorithms/generate_top10.py`，blob `bcfc8323644a`，91,068 B）
- **关联挂账**：`docs/ops/HANDOFF.yaml` → `top10-close-quote-gate`（P1 / `pending-verify`）

---

## 0. 一句话

主人问「德业股份是不是依旧将错就错」——**实测：线上确实还是 0（未解决）**，
且真根因**不是**我 20:55 记的「history 滞后 1~3 日」，而是
`raw_data/gold_pool.json` 的 `history` **根本不是连续日K**（是「信号命中日」的稀疏序列），
叠加「外资研投」通道 `close` **恒为 null**。
已改为引用全站单一真源 `STOCK_STOP_DATA`：德业 `close=82.38 / 止损=74.14 / 目标=104.56`。

---

## 1. 真根因（全部为实测，附原始数据）

### 1.1 线上现状（`data/TOP10_DAILY.js`，`update_time=2026-09-22 20:22:50`）

| # | 名称 | close | stop_loss | target_price | risk_reward | 判定 |
|---|---|---|---|---|---|---|
| 5 | **德业股份** | **0** | **0** | **0** | **0** | ❌ 全 0 |
| 20 | **国轩高科** | **0** | **0** | **0** | **0** | ❌ 全 0 |
| 其余 18 只 | — | 有值 | 有值 | 有值 | 有值 | ⚠️ 基准滞后 |

⇒ **20 只里 2 只（10%）止损三件套完全不可用**，卡片给不出风控位。

### 1.2 根因链

```text
raw_data/gold_pool.json（生成器唯一读取源，688,901 B，356 只）
  └─ 每只的 history = 「信号命中日」的稀疏序列（**非连续日K**）
       实测：恒逸石化 只有 09-08 一根
             西部矿业 只有 09-10 / 09-17 两根
             江波龙   09-15 / 09-17 / 09-18 / 09-21（跳过 09-16）
  └─ 「外资研投」通道的 history 元素 close 恒为 null
       · 356 只中 **65 只末根为空**、**56 只全空**
       · 德业股份 605117 / 国轩高科 002074 的 6 根全是 None
  ⇒ generate_top10.py: `latest.get("close") or s.get("close") or 0`
     → 取到 0 → `if close_price > 0` 不成立 → stop/target/RR 保持 0

旁链：`data/GOLD_POOL.js`（184,914 B）是**前端轻量版**（`_lite_note: 
「stocks 已去掉 history 日明细，仅保留 latest 聚合」），与生成器读的
raw_data 版**不是同一份**，排查时勿混用。
```

### 1.3 滞后幅度（18 只，独立源 gtimg 核验）

| 名称 | 卡上显示 | 当日真值 | 偏差 |
|---|---|---|---|
| 恒逸石化 | 18.89（=09-08 价） | 16.81 | **−11.01%** |
| 北方铜业 | 16.47（=09-09 价） | 15.11 | −8.26% |
| 云铝股份 | 29.03（=09-09 价） | 26.95 | −7.17% |
| 铜陵有色 | 6.57（=09-09 价） | 6.16 | −6.24% |
| 源杰科技 | 1838.56 | 1753.10 | −4.65% |
| 西部矿业 | 35.45（=09-17 价） | 36.77 | +3.72% |
| 江波龙 | 347.79（=09-21 价） | 348.27 | +0.14% |

⇒ 最深滞后 **10 个交易日**。**止损基准同样建立在这些滞后价上**（西部矿业止损 32.97 是按 35.45 算的）。

### 1.4 为什么「换个算法」也算不对

`stop_target_logic.compute_stop_target_from_closes()` 的降级口径用 `low20 = 近 20 日最低`。
但喂进去的序列是**稀疏信号日价格**（跨度可 10+ 天、根数 1~4 根），
`low20` 语义**根本不成立** ⇒ 这个函数在本数据源上从设计之初就是错配的。

---

## 2. 修法（一劳永逸，非逐个打补丁）

`algorithms/generate_top10.py` 改为**引用全站单一真源**：

| 序 | 改动 |
|---|---|
| ① | 新增 `stop_snapshot()`：读 `data/STOCK_STOP_DATA.js`（`gen_stock_stop.py` 用 **gtimg 真实 250 根日K** + `compute_stop_target` 精确版产出，与「今日可入手候选」卡**同源**），带**当日校验**（`update_time` 日期 ≠ 今日 ⇒ 整份作废回退，**绝不伪造**） |
| ② | 命中时 `close` / `stop_loss` / `target_price` / `risk_reward` **整体取该真源** ⇒ 「展示价与止损基准」同源同口径，不出现两套基准 |
| ③ | 未命中（非当日 / 不在其 509 只覆盖内）才回退原有序列降级口径 |
| ④ | `close_source` 三态化：`STOCK_STOP_DATA` / `STOCK_QUOTE` / `history` |

- 20:51 版新增的 `quote_snapshot()` 当日行情闸门**保留**，作为回退路径的取价来源。
- **时序注意**：`gen_stock_stop.py`(ORDER L203) 排在 `generate_top10.py`(L185) **之后**，
  且它依赖 `TRIPLE_CONSENSUS`(L195) / `FOUR_VOLUME`(L191) ⇒ **不能**前移。
  本改动靠「当日校验」保证不消费 T-1 值（非当日即回退），故无需调整 ORDER。

### 2.1 沙箱实测（仓库外 `E:\_alimi_tools\v8_close_fix\audit_20260922\`，输入全为远端当日真实产物）

```text
🎯 精确止损闸门启用：STOCK_STOP_DATA @ 2026-09-22，覆盖 509 只
code     名称       close      止损        目标       RR      止损方法
605117   德业股份    82.38      74.14      104.56    2.69    fixedP10   ← 原先全 0
002074   国轩高科    27.73      24.96       29.09    0.49    fixedP10   ← 原先全 0
301308   江波龙     348.27     313.44      749.88   11.53    fixedP10
601168   西部矿业    36.77      33.09       43.29    1.77    fixedP10
000703   恒逸石化    16.81      15.13       20.13    1.98    fixedP10
300857   协创数据   273.35     246.02      343.00    2.55    fixedP10
```

- 独立源核验（gtimg 实时行情）：德业 `82.38` / 江波龙 `348.27` / 西部矿业 `36.77` /
  国轩 `27.73` —— **逐只一致**。
- **评分口径零改动**：`close` / 止损均不参与评分（仅展示 + 风控基准）。

---

## 3. 三件套原始输出

### ① 云端 workflow cron ↔ 双机任务时窗矩阵

```text
base = 49541144a7e4  (2026-09-21 21:11:33 CST)   [24h 前]
tip  = 9e70e75d1529
workflow 文件数：base=30  tip=30
判定：cron 集合一致 = 29 / 30 ；tip 侧 cron 条目总数 = 54 ；含 cron 的 workflow = 21
  ❌ v8_cn_fetch_cloud.yml
     base=['*/20 1-3,5-6 * * 1-5', '0 1 * * 0,6', '20 9-11 * * 1-5', '25 0 * * 1-5']
     tip =['*/20 0-3,5-6 * * 1-5', '0 1 * * 0,6', '20 9-11 * * 1-5', '25 0 * * 1-5']
关键条目核验（tip 侧 CST 折算）：
  cloud_weekly_cleanup.yml   ['0 13 * * 6']      → 周六 21:00
  v8_algo_cloud.yml          ['0 0 * * *', '0 12 * * *', '0 13 * * *', '0 22 * * *',
                              '10 10 * * *', '30 14 * * *', '30 16 * * *', '40 8 * * *']
  v8_backup.yml              ['0 15 * * *']      → 23:00
  v8_cleanup.yml             ['0 15 * * 0']      → 周日 23:00
```

**唯一漂移归因**（非事故、已知变更）：
`09-22 16:23:26` commit `300f9332a6`「fix(cn-fetch): 盘中接力档位 15→10 分钟 + 窗口延至 15:10
+ 在途查询防叠队（主人令「覆盖13-15点10分」）」新增 UTC `0` 点档 = **CST 08:00–08:19 盘前档**。
⇒ 矩阵须登记新增该档；**本条改动非本轮所为，仅登记**。

### ② 离线门禁

```text
[1/8] py_compile（改动文件）：PASS

CI（`☁️ v8 构建部署(云端ubuntu)` run **#7684**，evt=push，@ `e30da3e9d8`，
由 `algorithms/**` 在 `on.push.paths` 内触发）—— **gate / build 两 job 全绿**：
  job gate  : success（step1-4 / step8-9 全 success）
  job build : success
     ✅ step9  🏗️ 构建 data/*.js（selective）
     ✅ step10 🛡️ Pre-deploy audit（CI 自动门禁，2026-09-05 启用）   ← 14 项全过
     ✅ step11 🛡️ 护栏：双盲区静态校验
     ✅ step12 🛡️ 部署前校验：关键 data/*.js 非空
     ✅ step13 📤 部署到 main（GitHub Pages 源）                    ← 无 push 竞态
  job intraday_patrol : skipped（非盘中档，正常）
  url = https://github.com/ah-quant999/quant-scanner-v8/actions/runs/35732229877

真门禁判据（fake repo 直调 .github/scripts/pre_deploy_audit.py::check_handoff_ledger）：
  check_handoff_ledger() -> (True, '交接状态源完好：items=54（ledger 锚 54 个 id 全在）· next_id_seq=53≥53')
哈希口径自证：未改动项命中 53/54 ；差异集 = ['top10-close-quote-gate']  ← 恰为本轮改动项
```

🔄 **重放说明**：本件首次推送时漂移守卫拦下（`docs/ops/HANDOFF.yaml` 于 21:21 被对方
新增 `theme-value-concept-layer-zero`、`next_id_seq` 52→53）⇒ 按协议**基新内容重放**：
段内替换本项、`meta.updated` 用 21:23（单调递增，不倒退对方 21:21）。

🔴 纪律声明：本轮**未**跑全量离线 staging 门禁（跨境逐 blob 拉取本轮成本高），
故 `align_logic_ops` / `v8_verify_layer_parity` 的 **EXIT 0 未在离线侧复现**；
但改动仅 1 个 `.py`、不触碰 `index.html` / `data/*.js` / workflow，
且 **CI step10 的 14 项门禁已真实跑过并全绿**（上表）⇒ 判别面已被 CI 覆盖。
**但这不等于"离线侧已验证"**，如实登记。

### ③ 本件

---

## 4. 请小九复核（命令级）

```bash
# 1) 确认代码已在 main
curl -s -H "Authorization: Bearer $TOK" -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/algorithms/generate_top10.py?ref=main" \
  | grep -c "def stop_snapshot"          # 期望 1

# 2) 确认挂账项
curl -s -H "Authorization: Bearer $TOK" -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/docs/ops/HANDOFF.yaml?ref=main" \
  | grep -A2 "id: top10-close-quote-gate"   # 期望 status: pending-verify

# 3) 产物级验证（下一档 D 批盘后档跑完后）——本项核心验收点
#    断言 ① 无 close=0 ② close 与 STOCK_STOP_DATA 逐只相等
#         ③ close_source ∈ {STOCK_STOP_DATA, STOCK_QUOTE}
#         ④ 德业股份/国轩高科的 stop_loss 与 target_price 均 > 0
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/TOP10_DAILY.js?cb=$(date +%s)" \
  | grep -o '"code":"605117"[^}]*'        # 期望 close=82.38 附近且 stop_loss>0
```

---

## 5. 待对方 / 待拍板

| 项 | 状态 |
|---|---|
| `algorithms/generate_top10.py` 改动 | ✅ 已推 `e30da3e9d8`，**待下一档产物验证**（`pending-verify`） |
| `pages-publish-bypasses-gate`（P0） | ⏳ 仍 `todo`，需人拍板（GitHub 内置 pages build 不消费 v8 门禁） |
| `v8_cn_fetch_cloud.yml` 新增 CST 08:00 档 | ✅ 已在 ① 登记，无需动作 |
| 其余模块是否也用 `gold_pool.history` 算序列类指标 | ⚠️ **建议你扫一遍**（本轮只收口了 `generate_top10.py`） |

---

> 本报告仅供工程交接参考，不构成投资建议。
