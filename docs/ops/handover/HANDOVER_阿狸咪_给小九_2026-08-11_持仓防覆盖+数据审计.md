# HANDOVER_阿狸咪_给小九_2026-08-11_持仓防覆盖+数据审计

> **时间**：2026-08-11 06:41
> **发送方**：阿狸咪（周末/节假日）
> **接收方**：小九（工作日）
> **状态**：待小九确认

---

## 一、紧急修复：持仓录入匹配错误（CRITICAL）

### 问题描述
用户在 TOP3 持仓跟踪卡录入「广钢气体」(拼音 `gqqt`)，结果匹配到了**浦发银行** (sh600000)！

### 根因（经典 JS 陷阱）
`_pfFindStock()` 函数有**独立的搜索逻辑**，未接入统一搜索内核 `_uniSearch()`。

```javascript
// 用户输入 'gqqt'（纯字母）时：
var nc = normCode('gqqt');  // nc = '' （空串！）
sc.indexOf(nc) === 0;       // '688548'.indexOf('') === 0  → 永远为真！
// → 每只股票都得 3 分基础分 → 第一只碰巧命中的（浦发银行）就赢了
```

### 修复内容（commit `e25e349c8` → `51dd83f`）

| 修改 | 说明 |
|------|------|
| `_pfFindStock()` | 改为优先调用 `window._uniSearch(q, 1)` |
| fallback 守卫 | 增加 `nc` 检查：`if(nc && sc===nc)` — 空串时不走 code 匹配 |
| `doStockDiag()` fallback | 清理未定义变量引用 (`isA/ql/qc/best` → 会抛 ReferenceError) |
| 持仓录入框 | 新增下拉联想 `showPfDropdown()` / `hidePfDropdown()` |

### ⚠️ 小九注意事项

1. **绝对不要删除或覆盖 `_uniSearch()` 函数**（位于 index.html ~5204 行，个股查询 IIFE 内）
2. **绝对不要恢复 `_pfFindStock()` 的旧逻辑**（独立搜索、无 nc 守卫）
3. **新增的持仓下拉联想 DOM 元素**：
   - `#pfDropdown`（下拉列表容器，absolute 定位在输入框下方）
   - `window.showPfDropdown(q)` / `window.hidePfDropdown()`
4. **所有 5 处搜索入口现在统一复用 `_uniSearch()`**：
   - `findStocks()` — 个股查询下拉
   - `findOne()` — 个股查询选中
   - `showDiagDropdown()` — 诊断区下拉联想
   - `doStockDiag()` — 诊断区执行搜索
   - `_pfFindStock()` — 持仓录入匹配

---

## 二、CANDIDATE.js 再次被云端覆盖（第 2 次）

### 现象
前端显示「公开资讯 0 只」「研报来源 0 只」—— 观澜台 + mahoro 又丢了。

### 链路
```
out/candidate_pool.json (6源337只✅)
  ↓ 被云端管线覆盖为旧版
raw_data/candidate.json (4源150只❌)
  ↓ update_v8.py 生成
data/CANDIDATE.js (4源150只❌)
```

### 修复
用 `candidate_pool.json` 覆盖 `raw_data/candidate.json` → `update_v8.py --category post_close` 重建

### ⚠️ 小九注意事项

1. **这是第 2 次发生**（上次 commit `7d0a44368`，这次 `935300338`）
2. **根因**：云端 `v8_algo_cloud` 管线会重新生成 `raw_data/candidate.json`，但它的 A 股扫描有时失败，导致只产出 4 源旧版
3. **根治建议**（可选）：在管线脚本中加判断 —— 如果新版的 source_dist 缺少观澜台/mahoro，则保留旧版不覆盖
4. **临时方案**：如果发现 CANDIDATE.js 变回 4 源 150 只，执行：
   ```bash
   cp out/candidate_pool.json raw_data/candidate.json
   python3 update_v8.py --category post_close
   git add data/CANDIDATE.js raw_data/candidate.json && git commit && git push
   ```

---

## 三、全站数据真实性审计结果

### 审计范围
68 个 `data/*.js` 文件，逐个检查 JSON 解析、update_time、实际数据内容。

### 结果汇总

| 状态 | 数量 | 详情 |
|------|------|------|
| ✅ 正常 | **65 个** | 有真实数据、有有效 update_time |
| ⚠️ 已知空壳 | **1 个** | AVG_PRICE_DATA.js（103B，仅时间戳）|
| ⚠️ 格式特殊 | **1 个** | CONCEPT_ETF_MAP.js（注释+JSON 混合）|
| ⚠️ 今日未跑 | **1 个** | TRIPLE_HISTORY.js（最新 2026-08-09）|

### 已知异常详情

| 文件 | 问题 | 原因 | 影响 |
|------|------|------|------|
| AVG_PRICE_DATA.js | 仅元数据无 avg_price 字段 | 云端 `f_avg_price()` 取样 <1000 只返回空 | 平均股价卡显示 `--` |
| TOP10_DAILY.js | 仅 12 只港股、0 只 A 股 | A 股活跃股池扫描失败（mootdx 连接问题）| 全站精选卡红色告警 |

### 结论
**无弄虚作假**。65/68 文件数据真实有效。3 个异常均有明确技术原因（云端数据源连接问题），非人为造假。

---

## 四、今日所有 Commit 汇总

| 时间 | Commit | 内容 |
|------|--------|------|
| 06:19 | `e949ec275` | 持仓预录 2 ETF + _pfFindStock ETF 兜底 |
| 06:24 | `b0f934b99` | 并排布局 + 已计算日期 + 拼音搜索统一内核 |
| 06:30 | `61e9f1689` | 候选池与 Top3 数据统一 + 两张卡迁移 |
| 06:38 | `e25e349c8` | **CRITICAL** 持仓匹配错误修复 + 诊断卡死代码清理 |
| 06:41 | `51dd83f` | CANDIDATE.js 6 源修复 + 全站审计 |

---

## 五、待办事项（移交小九）

- [ ] **TOP10_DAILY A 股缺失**：排查 `v8_algo_cloud` 管线为何 A 股扫描失败（mootdx 连接？），修复后全站精选告警应自动消失
- [ ] **CANDIDATE.js 防覆盖**：考虑在管线中加保护逻辑，避免 6 源数据被 4 源旧版覆盖（已发生 2 次）
- [ ] **AVG_PRICE_DATA 空壳**：确认 `f_avg_price()` 取量阈值是否合理，或增加兜底数据源

---

_文档结束。小九收到后请确认，如有疑问随时沟通。_
