# 双机分工交接 · 小九 → 阿狸咪（2026-09-10 17:39）

> 经小九实测审计（git fetch 对齐远端 + 关键数据 `update_time` 抽样 + `fix/lemoncat-sleep600` 分支 diff 核实），现状无误。
> 本文本为**双机分工权威定稿**，整合小九 17:24 收尾回执 + 阿狸咪 17:17 下班交接，以分工矩阵为核心，供阿狸咪 18:00 夜班接管。
> 纯文档新增，零业务代码 / 数据改动，零踩踏风险。

## 一、双机时段分工矩阵（明确，避免踩踏/风暴）

| 时段 | 主力机 | 职责 | 禁区 |
|---|---|---|---|
| 工作日 7:45–17:45 | **小九**（lemoncat-cn·单位机·中国 IP） | 算法链 / LHB / STOCK_QUOTE / cn 抓取主力；盘中数据 guard；发布闸门 flush | 不派发会撞阿狸咪在跑 run 的任务 |
| 夜间 / 周末 18:00–次日 7:30 | **阿狸咪**（alimi-cn·家机） | 盘后 / 夜班 / 周末主力；手动接管兜底 | **绝不本机直推 main**；数据一律走云端 build |
| 交接窗口 17:45–18:00 | 双方 | 小九收口、阿狸咪就位 | 同一时刻只一个派发源 |

## 二、本机自动化「停 / 留」定稿（防踩踏 + 防风暴，实测 ID 复述）

### 🔴 PAUSED（5 个 — 踩踏/风暴源，勿轻启）
| ID | 名称 | 暂停原因 |
|---|---|---|
| `22505803` | 小九机盘中云端调度器(14档·20分) | OFF-HOURS；双源铁律「推前先停防互踢」，明日小九回归须 re-enable |
| `d1b92500` | D档派发(20:00·含最终推荐) | 阿狸咪夜窗，暂停防互踩；由 `1788168934360`(19:25 守卫) 兜底覆盖 |
| `444581c6` | E档派发(21:05·回测批) | 阿狸咪夜窗，同上暂停 |
| `e3d25def` | 阿狸咪改码冲突护栏+选股三卡 | 会按阿狸咪改码推选股三卡互踩；阿狸咪只读不改码，暂停即安全 |
| `1785835481750` | 每小时紧急交接通道(auto-fix) | 自动修复类，误触发会雪崩（风暴源），暂停 |

### ✅ ACTIVE（7 个 — 主人明确「别停」）
| ID | 名称 | 为何必须保留 |
|---|---|---|
| `automation-1788172770889` | 龙虎榜/共振日历云端守卫(21:40/22:40) | **LHB 唯一保障源**（云端 cron 静默失效、阿狸咪按铁律 PAUSED），停了当晚共振日历断档 |
| `automation-1788168934360` | 盘后算法链云端守卫(19:25) | 确保每日算法链真实跑满（force_run 破静默闸门），覆盖 A/B/D/E 全链，是 D/E 档暂停后的实际兜底 |
| `98e60e82` | 盘后链兜底守卫(21:45) | FINAL_RECOMMEND 当日落地核查 + cn 版兜底 |
| `automation-1786588094908` | 分红晚间(19:10) | 分红监督 + 应急兜底 |
| `automation-1785719698947` | 12/18 点交接文档更新 | 小九→阿狸咪交接机制，18:00 自动更新本交接单（生成自当日 memory） |
| `automation-1787575410637` | James-Stein 回测(19:30) | 小九本机本地回测，不推 main，安全 |
| `75e8cfe5` | A档派发(16:40·采集批) | 小九 16:40 窗口专属，非夜窗，保留 |

### 踩踏/风暴结论
- **唯一主线** = 云端 cron + 上述小九专属守卫。
- 阿狸咪按铁律 PAUSED（不自动推）；若手动接管夜班，为唯一手动推源，不与小九自动化撞车。
- D/E 档暂停后由 19:25 守卫兜底 → 盘后链（含最终推荐 + 9 项回测）不中断。
- 风暴根除：自动修复类（`1785835481750`）已暂停，不会自激重试雪崩。

## 三、🔴🔴 致命坑（实测证实，比预估更严重）

- `fix/lemoncat-sleep600` 分支相对 main：**186 文件差异，+150462 / -150632 行**（非文档原估 151 文件）。
- `update_v8.py` 分支版**缺失今日修复**：无 `?v` 令牌模块级变量、无 `_pure_pc` 纯盘后跳过逻辑（小九 `fa3b62c74` P0 防回退）、无 `OVERSEAS_MARKETS` 映射（`7d61e36b`/`7daabe166` 根治）。
- **禁止整分支合并 / cherry-pick**；lemoncat 这一处改动（sleep600→实时轮询）已单文件原子提 main（`15fb8b2d`），其余陈旧文件一律未动。
- **唯一风险监控点**：观察 `?v` 时间令牌是否每构建必变（证明缓存戳修复仍生效）；若发现 FINAL_RECOMMEND `update_time` 倒退，优先查是否有人误合并 lemoncat 过期分支。

## 四、当前数据状态（实测 17:39，git show origin/main）

| 文件 | update_time | 说明 |
|---|---|---|
| `FINAL_RECOMMEND_DATA.js` | 2026-09-10 04:57:09 | 今早版，D 档夜班待出新 |
| `CRDS_CARD_DATA.js` | 2026-09-10 04:10:39 | 今早版，正常 |
| `SECTOR_FUND_FLOW_INTRADAY.js` | 2026-09-10 15:49:26 | 收盘快照已补 ✅，主力净额卡健康 |
| 主站小九 4 修复 | — | 盘前卡 / 主力净额守卫 / 独立快照 job / P0 防回退，均在线 |

## 五、阿狸咪夜班待办（18:00 起）

1. **夜班主线**：盘后链由小九 19:25 守卫自动兜底跑满（含最终推荐 + 回测）；手动介入走云端 `workflow_dispatch` / `repository_dispatch`，勿本机直推 main。
2. **双源处置**：lemoncat(14档 cron·self-hosted) 与云端按铃均只 dispatch cloud，由 `v8-cn-fetch-cloud` 并发组串行互斥 → 安全冗余；择一收敛即可（建议保留云端按铃）。
3. **LHB**：21:40/22:40 小九守卫自动核查+兜底，无需动；连续两晚缺失再人工排查 `v8_lhb_fetch.yml` runner。
4. **明日小九回归**：小九会 re-enable `22505803` 等盘中调度；你夜班结束前勿改 `update_v8.py` / `index.html`。
5. **明早主力净额卡**：盯 `v8_intraday_snapshot.yml` 独立 job 09:30 后快照连续性（治上午稀疏）。
6. **僵尸 run**（34458748715 / 34458729899 / 34457460597）若仍 in_progress，夜班 `cancel` 再派（需 API，本机 HTTPS 不通由云端处理）。

## 六、待主人拍板项（保持现状，等明令）

- **C2**：小九 15:15–17:20 离线 → 16:40 A 档（盘后算法链）须覆盖，空窗拆「15:00–16:20 可下线 + 16:40–17:25 必须在线」或云端 fallback。
- **C3**：`v8_algo_cloud.yml` 在 lemoncat-cn offline 时无 fallback。
- **C6**：某自动化条件写死日期 `2026-09-09` 永不触发。
- **C7**：21:30 终闸强制补跑当前 PAUSED 任务，与「阿狸咪任务须 PAUSED」铁律冲突。

## 七、防踩踏 / 防风暴铁律（复述，务必遵守）

- 同一时刻只一个派发源；每次派发前必查「是否有在跑 run」，有则跳过（守门自动化已内置此闸）。
- 只推「我方改了且对方没动过」的文件；对方动过的（`data/*.js` / `raw_data` / `index.html` / `maharo_insights`）一律保留对方版。
- 阿狸咪绝不本机直推 main；数据一律走云端 build。
- 发布闸门：盘中(13–15 点)严禁本地直推；休市窗口由发布闸门 flush。

---
*本单由小九 17:39 实测审计后生成；18:00 自动交接任务会基于当日 memory 增量更新。*

小九签字：ah-quant999 · 2026-09-10 17:39
阿狸咪回执：________________（pull 后填）
