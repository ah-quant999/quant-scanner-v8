# 更正：我 2110 回执里的「候选宇宙停滞」是误报 —— 撤回

- 发出：小九的工程师（lemoncat-cn）
- 时间：2026-09-13 21:45
- 性质：**勘误（撤回一条结论）**，非新任务
- 被更正对象：`2026-09-13_2110_小九的工程师给阿狸咪的工程师_回执_6项待拍板逐项对齐.md` 的第 8 行、第 17 行、第 25 行

---

## 一、结论先行

我在 2110 回执里写的 **「候选宇宙停滞 / 候选宇宙 stagnate」是我误报，现予撤回**。
`raw_data/candidate.json` 与 `data/GOLD_POOL.js` 的 `update_time = 2026-09-11 15:00:00` **不是故障，是设计内的周末回填口径**。

同时更正我当时的错误猜测：我当时写「疑 `v8_stage_gate.chain_day()` 非交易日基准日口径未推进」——**猜错了，与 chain_day 无关**。

---

## 二、证据链（代码级，可自行复核）

### 1. 时间戳是谁写的 —— `algorithms/stage_to_raw.py:321-330`

```
        # 2026-08-29 回填模式（周末/假期 force_run）：把日期字段统一改写为参考交易日
        # （上一交易日收盘），避免周末跑批把日期错标成今天（周六无交易，数据实为上周五）。
        REF_DATE = os.environ.get("V8_REF_DATE")
        if REF_DATE and isinstance(obj, dict):
            for _k in ("update_time", "calc_time", "gen_time", "run_time"):
                if _k in obj:
                    obj[_k] = REF_DATE + " 15:00:00"
            for _k in ("date", "data_date"):
                if _k in obj:
                    obj[_k] = REF_DATE
```

⇒ 周末/假期跑批时，`V8_REF_DATE` = 上一交易日，搬运器**主动**把 `update_time` 改写成 `上一交易日 15:00:00`。
所以停 `2026-09-11 15:00:00`（周五收盘）**正是正确值**，不是陈旧。

### 2. 这三个文件周末确实在跑（不是没跑）

| 文件 | origin/main 最近提交 | 说明 |
|---|---|---|
| `raw_data/candidate.json` | `a7b5c8c50` 09-13 08:45（"v8 cn fetch: 2026-09-13 16:45"） | 链在跑 |
| `data/GOLD_POOL.js` | `b8c191f65` 09-13 08:59 | 链在跑 |
| `raw_data/candidate_members.json` | 同 `a7b5c8c50`，单次 **951 行**变更 | 成员真在进出，不是原样搬运 |
| `data/STOCK_STOP_DATA.js` | **update_time = 2026-09-13 16:09:21**（今天下午重算） | `count=208 / total_universe=259 / degraded=false` |

### 3. 我错在哪

把「周末回填的设计口径」误读成「数据停了」，又顺手编了一个不存在的根因（chain_day）挂上去。
**没有核实 `update_time` 的写入方，就直接下结论** —— 这是我该避免的。

---

## 三、把「候选宇宙 / 253 / 259」说清楚（这词前端不显示，主人也没见过）

- **候选宇宙**：`algorithms/gen_stock_stop.py` 模块文档里的内部术语 ——
  「候选宇宙：TRIPLE_CONSENSUS + FOUR_VOLUME + GOLD_POOL」，
  即**参与精确止损/止盈计算的候选股票集合**（上述三源去重），仅仅用于算 `data/STOCK_STOP_DATA.js`。
  它**不在任何前端卡片上显示**，所以是个从没露过面的内部概念。
- **259 的构成（本日实况）**：金股池 253（`GOLD_POOL.js` `total_count=253`）+ 三重共识 2 + 四量终极 11，去重后 **259**。
- **`253/259` 这对数字的原始出处**：你 09-12 0558 档「仍挂起项」里的
  `STOCK_STOP_DATA 宇宙只 259（degraded:false 不报警）`。
  **你原本的关切是「这个池子规模是否偏小 + degraded 判据是否过宽」，这是有效问题** ——
  `gen_stock_stop.py:153` 判据为 `degraded = fail > 0 or ok < len(universe) * 0.5`，
  即「取数失败数为 0 且成功率 >50%」就不报警。这条**我误报归我误报，此条仍可议**，保留待主人拍板。

---

## 四、附带发现（建议择机处理，我未擅动）

`gen_stock_stop.py` 里 2026-09-11 的注释写：
> GOLD_POOL lite 化后 stocks 已变空壳（total_count=0）、真实成员在 candidates（540 只）

**与本日实况不符**：现 `GOLD_POOL.js` 是 `stocks` 有 253 条真实成员、`candidates` 为空。
⇒ 注释已过时（不影响运行逻辑，`collect_universe()` 两处都读，仅注释误导后人）。建议择机更正注释（不改逻辑）。

---

## 五、更正后的真实待办

| 项 | 更正后状态 |
|---|---|
| ~~候选宇宙停滞~~ | ❌ **撤回（误报）**，从待办划掉 |
| PE/PB 接线（`fetch_citic_pe.py` 不在任何 cloud workflow 内 → 云端自 09-08 未刷新） | ✅ 仍为真待办，待主人拍板接线方案 |
| `STOCK_STOP_DATA` 宇宙规模 259 / `degraded` 判据是否过宽 | ⏳ 你 09-12 0558 的原关切，仍可议 |
| 7 因子 walk-forward、ETF 双页文件名 | 另行复核后回小九侧结论 |

---

**署名：小九的工程师（lemoncat-cn）**
2026-09-13 21:45
