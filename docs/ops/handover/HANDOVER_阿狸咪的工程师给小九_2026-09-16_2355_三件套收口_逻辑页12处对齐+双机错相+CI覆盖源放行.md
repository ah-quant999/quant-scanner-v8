# 交接｜三件套收口 · 逻辑页 12 处对齐 + 双机错相 + CI 覆盖源放行｜阿狸咪的工程师 → 小九的工程师

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 时间：2026-09-16 23:55 CST
> 前端主改动落地：`e9f785505`（23:03，策略回测页口径统一 + 只留 5 策略 + 四量按真值源同源）
> 本件配套提交：`logic.html`（12 处描述对齐）+ 本交接件
> 纪律：全程 **GitHub Git Data API**（不碰本机工作树、不 `git add`、不 `reset`）；推送前先做 blob 漂移守卫、推后做零覆盖自证。

---

## 0. 一句话

主人令「**改后三件套，和小九交接清楚，再读一下小九的交接，双机云端对齐**」——本件把这四件事一次性收口：
**① 时窗矩阵审计**（发现并修掉一处真实撞车：家机推进器与小九机新推进器**同为 :10**）；
**② 逻辑页与主站对齐**（`align_logic_ops` EXIT=0 + `logic.html` 12 处描述漂移修正）；
**③ 交接件**（本件）；**④ 读你的交接**（2219 回执 / 2230 盘后链 / 2245 补正，见 §1）。
另按主人 09-14 全权授权，**放行 P-1（CI 覆盖源补丁落盘）**——见 §3。

---

## 1. 我读了你的哪些交接（本轮的「再读小九的交接」）

| 件 | 时间 | 我提取到的、与我直接相关的结论 |
|---|---|---|
| `2026-09-16_2219_小九的工程师给阿狸咪的工程师_回执_未回执3档逐项对齐…` | 22:19 | ① 撤回「你的提交不在 main」（**浅克隆假阴性**，判据 78）⇒ 核验一律走内容面 blob 匹配，**禁用 `merge-base --is-ancestor` / `git log %p`**；② **账目更正：「A 批 must 死锁」09-14 已闭环**（`AVG_PRICE_DATA` 为非 must，13/11/3）；③ `data/*.js` = 源 raw 的**无条件投影**（桥接无单调守卫，判据 80）；④ 6 项安全微修（含 4 处「陈旧暂存区」回退地雷）；⑤ P-1~P-7 待拍板 |
| `2026-09-16_2230_小九_盘后链结构性缺口一劳永逸修复` | 22:30 | ① 18 小时唤醒真空 + 显式 `stage` 陷阱 + 「yml 承诺的本机推进器从未存在」三层根因；② 新增**接力推进器**（16:10–次日 08:40，每 30 分，`BYMINUTE=10,40`）；③ **铁律：所有盘后链自动唤醒一律不带 stage**；④ 四道串联闸门（幂等 / 不打扰在跑的 / 冷却期 / 零成本空转） |
| `…2245…三处链路缺陷修复`（含 22:19 后 +94 行补正） | 22:45 | §8 把 **D7 与我 `e9f785505` 的前端兜底**接成一条完整因果链：**前端同源 = 数据层另一半（E 批被静默杀 → 真值源不前进）**；并明确「聚合快照本体仍需重跑，`I did not touch data/`」 |
| `2026-09-16_2133_小九_观测平台子页签字体放大显著化` | 21:33 | 全局唯一改点 CSS `.obs-tab`（与我改的 `#st-*` 页签区零重叠） |

**我方账目同步更正**：记忆里「A 批 must 死锁 P0」按你的 §5.3 改为**已闭环（09-14 收敛）**；后续不再用它解释 E 批空产（真因查派发 403 与各批自身 must）。

---

## 2. 三件套① 时窗矩阵审计 —— 发现一处**真实撞车**并已修

### 2.1 审计方法

执行机判定**只看 `runs-on`**（禁用 workflow 名字含「云端」这类沿革名推断）；cron 一律从 UTC 换算 **CST+8**；双机侧逐条读自动化 `rrule` + prompt 内的**时窗守卫**。

### 2.2 云端（`.github/workflows/` 30 个，只列有 cron 的关键链）

| 执行机 | workflow | CST 时点 | 说明 |
|---|---|---|---|
| `[self-hosted, cn]` | `v8_algo_cloud` | 16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 / 06:00 / 08:00 | 盘后算法链主链（A/B/D/E 由闸门裁） |
| `[self-hosted, cn]` | `v8_algo_run`（仅 dispatch） | — | cn 人工/兜底入口 |
| `[self-hosted, cn]` | `v8_stock_quote_refresh` | 每小时 :00、:30（09:00–16:30 CST） | 轻量行情刷新 |
| `[self-hosted, cn]`+ubuntu | `v8_cn_fetch_intraday_lemoncat` | :40/:00/:20 各档（09:40–15:00） | 盘中准点档（小九机为主） |
| `[self-hosted, cn]` | `v8_lhb_fetch` | 17:30 | 龙虎榜单飞 fetcher |
| `ubuntu-latest` | `v8_cn_fetch_cloud` | 08:25 / 09:20–11:20（每20分，UTC `*/20 1-3,5-6`）/ 周末 09:00 | 中国数据抓取主链 |
| `ubuntu-latest` | `v8_cn_fetch_watchdog` | 每 15 分（08:07–15:52） | 抓取看门狗（唯一状态型兜底） |
| `ubuntu-latest` | `v8_health_patrol` | 每 30 分（:08/:38） | 云端健康巡检（自愈+体检） |
| `ubuntu-latest` | `v8_risk_gauge` | 每 30 分 | 风险温度计 |
| `ubuntu-latest` | `v8_intraday_snapshot` | 每 10 分（09:00–15:00） | 板块资金日内快照 |
| `ubuntu-latest` | `v8_ima_strong_stock` | 23:45 | **IMA 强势股同步（＝我「强势跟踪」的数据源）** |
| `ubuntu-latest` | `v8_daily_audit` / `v8_backup` / `v8_weekend_light` / `v8_cleanup` / `v8_weekly_cleanup` | 22:30 / 23:00 / 周末 19:30 / 周日 23:00 / 周日 21:00 | 审计与清理 |
| `ubuntu-latest` | `v8_cache_buster_reconcile` | **每 15 分（全天）** | 缓存戳实时对齐（防覆盖） |
| `ubuntu-latest` | `v8_health_patrol`、`runner_health_alert`、`v8_t1_guard` | — | 监控/守门 |

> **空窗结论**：抓取链 15:00–17:20 空窗（历史已知，由 cn fetch 兜底覆盖）；盘后链 22:40–16:40 的 18 小时真空**已由你的接力推进器消除**（§2.3）。

### 2.3 🔴 双机错相（**本轮实际修掉的一处撞车**）

| 派发源 | 归属 | 修复前 | 修复后 |
|---|---|---|---|
| 盘后链接力推进器 | **小九机** `lemoncat-cn` | 16:10–08:40，`:10 / :40` | 不变 |
| 盘后链接力推进器 | **家机** `alimi-cn`（`da2072fc`） | 每小时 **`:10`**，16:00–08:59 | **`:25 / :55`**（`BYMINUTE=25,55`） |

**为什么必须错开**：两者都是**幂等派发源**（跑哪批由云端闸门裁定），但若在**同一分钟**同时判闸门，会出现「同分钟双源同时 dispatch」→ 并发组 `v8-algo-cloud`（`cancel-in-progress: false`）只容「1 跑 + 1 排队」→ 第二个 pending 被后续派发顶掉 → **cancelled 风暴**（历史教训）。错相后整体节奏 **:10(你) :25(我) :40(你) :55(我)**，每 15 分钟一个唤醒点、**无同分钟撞车**。

**家机不可替代的两点**（已写进任务 prompt，勿改回）：
1. **周日**：你的推进器 `BYDAY=MO,TU,WE,TH,FR,SA`（不含 SU）⇒ 周日 16:00–次日 08:59 由家机兜底（家机守卫只看时点、不看星期）；
2. **08:55** 这一档兜住你 08:40 之后的尾巴 —— 防「B 批 06:30 才跑完 → D 饿到次日 16:40」。

### 2.4 家机自动化审计（含一处**停用**）

| 任务 | 时窗 | 派发/写能力 | 判定 |
|---|---|---|---|
| `da2072fc` 接力推进器 | 16:00–08:59（每小时，窗口内有守卫） | 派发（幂等·不带 stage） | ✅ 保留，**已错相到 :25/:55** |
| `50eda171` 暴风盯梢 | 16:00–次日 02:00 | **只读** | ✅ |
| `90b7ea6e` 夜班监控+交接巡查 | 16:00–02:00 | 仅「真基础设施失败」补派一次（带状态文件去重） | ✅ |
| `94279c7c` 盘中巡检 | 09:30–11:30 / 13:00–15:00 | 写 URGENT 文档 | ✅ |
| `automation-1788058041833` 自愈监控闭环 | 工作日 18–22 时 + 07 时（**已排除小九独家时段**） | 条件 `git push` | ✅ |
| `053d3f00` A1 锚点看守 + E 批扩档验收 | 每 2h，无守卫 | 条件补写 | 🔴 **已停用**：判据双向失效（`opsCandidateFreshBody` 在 index.html 计数 = **0** 已 4 天；`BACKTEST_COMPREHENSIVE.js` 的前端卡 09-16 已整卡删除）⇒ 纯空转 |

> 铁律遵守：家机 `stock-scanner` 名下任务**保持 PAUSED、未删除**；停用项改名留痕 `[已完结停用·2026-09-16]`，未删。

---

## 3. 🔴 放行：P-1（CI 覆盖源补丁落盘）—— 按主人全权授权，直接落，不必再等拍板

| 项 | 内容 |
|---|---|
| 补丁 | `docs/ops/patches/v8_build_deploy_index_cover_fix_20260916.patch`（blob `baec49be0`） |
| 你已验证 | 「在**最新 tip** 上 `git apply --check` 退出码 **0**，无漂移、可干净落盘」 |
| 你更正的通道 | **本机 SSH 推送不受 fine-grained PAT 的 Workflows 权限限制**（该限制仅作用于 PAT/App 经 HTTPS 建 tree 的场景）⇒ 落盘通道现成 |
| 覆盖源仍在 | `v8_build_deploy.yml` L205-207（主路径）与 L340-342（重试路径）仍是**无条件** `cp index.html /tmp/index.html.manual` → `git reset --hard FETCH_HEAD` → `cp` 盖回 |
| 授权依据 | 主人 **09-16 明令**「运维页一直在被覆盖成旧版！**给我一劳永逸式修复！不许再覆盖了！**」+ 主人 **09-14 全权授权**（「所有决策（含此前『待拍板/待授权』项）由阿狸咪的工程师按专家判断直接裁定+执行，禁逐次请示」） |
| **结论** | **P-1 = 已放行，请落盘**。落盘后请回执：① `git show <tip>:.github/workflows/v8_build_deploy.yml \| sed -n '200,215p'` 覆核条件化；② 用一次真实并发场景验证「有新 push 时不再盖回」 |

**同时授权（同类，均为「主人已明令/零风险」项）**：
- **P-2（D3 判据同源化）**：你已静态证实「写入口径与判据口径不同源」⇒ 采纳你建议的甲/乙案，改判据。⚠️ 但注意：**改判据 = 改 workflow 判据**，与你 P-1 同属 `.github/workflows/` 面 ⇒ **建议并入 P-1 的同一次落盘**，避免两次动同一文件造成二次窗口。
- **P-6（互斥断言）、P-7（因子台账补列）**：低风险、纯增量 ⇒ 放行。
- ⚠️ **P-4 / P-5 我暂不授权**：二者择一且牵动 `update_v8.py::_make_js`（**76 条桥接共享**）+ 构建 `git add` 面，属**双机共写核心面**。建议**先做一次只读影响面评估**（列出受影响的 76 条桥接里哪些的源 raw 存在「退化写」可能路径），再决定乙案（单调不回退守卫）还是甲案（raw 白名单）。我倾向**乙案**（一处守卫覆盖全 76 条，符合「科学简便」），但需要你先给出**不会误伤「合法回退」（真有人要下调数字）**的例外通道设计。
- **P-3（D4 定性）**：同意你的证伪（`actions: write` 与 `V8_GITHUB_TOKEN` 均已位，真因在 PAT 生效面）⇒ 放行「派发前断言 token 非空、为空则响亮报错」。

---

## 4. 三件套② 逻辑页与主站对齐（本件推送内容）

### 4.1 门禁结果（本地离线复现，镜像远端 main tip）

| 门禁 | 退出码 | 说明 |
|---|---|---|
| `pre_deploy_audit`（8 项） | **✅ PASS** | 「🎉 8 项全部通过 → deploy 可继续」 |
| `align_logic_ops` | **EXIT=0** | 文档源实测 `index.html#sec-op` 567,662 字符 / `logic.html#sec-lg` **523,442 字符** ⇒ 逻辑页**真被纳入校验**（非假通过） |
| `v8_verify_layer_parity` | **EXIT=0** | `data/*.js` 与 `raw_data/*.json` 时间戳一致，无陈旧静默上线风险 |

> 🔴 **一处必须说明的过程细节**：首次镜像时 `logic.html` 因 `IncompleteRead` **被截断了 190 字节**（731,623 / 731,813）。按「单次 API 失败禁作判据」铁律，我用 `raw.githubusercontent.com`（CDN）+ tree API 拿 sha/size 重新拉取并**逐字节校验**后重跑 —— 上面那行 `523,442 字符` 是**完整文件**的结果。若不做这一步，`align_logic_ops` 会因「文档源缺失只记录不报错」而**假通过**。

### 4.2 `logic.html` 12 处描述漂移修正（本件一同推送）

主站 `e9f785505` 改了 UI，逻辑页文案仍写旧版 ⇒ 逐处对齐（全部为行内替换，**CRLF 8,676 处守恒**，内联脚本 6 个语法错误 0）：

| # | 位置 | 原描述 | 修正 |
|---|---|---|---|
| A1 | 策略回测·改口径表 | 「若主口径胜率最高者与全局最佳不同，**并列一句补充**」 | 删除并列机制，改「**只此一套口径**，第二口径列已删除」 |
| A2 | 同表·默认展开行 | 「策略回测『完整明细』、综合回测 4 桶胶囊…全部加 `open`」 | 更新为 09-16 现状：策略回测折叠壳**整壳删除**、综合回测卡已删；现存 `details` 仅 AI速览 / 原始条目 |
| A3 | 口径声明 | 「两段口径分开计算、分开标注，**不得混读**」 | 改为「**2026-09-16 起为单一口径**」（每策略取自身最佳持有期，样本 ≥30） |
| A4 | 主对比表阅读顺序 | 「星级 / **主口径** / 样本 / 胜率 / 最佳持有期」 | 删除「主口径」列 |
| A5 | ④ 完整明细 | 「▾ 完整明细（默认展开）…已加 `open`」 | 改为「**常驻展开 · 无折叠壳**」+ 记录主人 09-16 原话 |
| A6 | 口径提醒 | 「**三段口径不同，不得混用**」 | 改为单一口径 + 标注**四量终极按真值源 `FOUR_VOLUME_BACKTEST.js` 强制同源**（防聚合快照持旧口径值） |
| A7 | 前端位置 | 「明细**折叠在 `<details>` 内**」 | 改为「直接内联渲染（无折叠壳）」 |
| A8 | 如何跟踪回测信号 ② | 「见本 tab 上方『📈 K线信号 9×5』块」 | 标注该块**已于 09-16 整块删除**（公式照常触发，数据层不受影响） |
| A9 | 如何跟踪回测信号 ③ | 「见本 tab 上方『🔬 综合回测·策略×持有期』块」 | 标注该块**已于 09-16 整块删除** |
| B1 | 全站 3 处卡名 | 「📡 高手强势股跟踪」 | 「📡 **强势跟踪**（原名「高手强势股跟踪」）」 |
| B2 | 强势突破卡行 | 「卡下方另有『⚡ 强势突破 · 回测 N 日胜率』真实回测」 | 标注**该回测卡已随本页三卡精简下架**（数据层照常回测） |

> 说明：A8/A9 与 B2 涉及**你 09-16 已删的卡片**（综合回测卡 / K线信号卡），我一并补齐，避免逻辑页继续指向不存在的块。**若你已在另一分支改了同段落，以你为准，回执告知即可。**

### 4.3 ⚠️ 制作窗口内撞上**第三个并发写者**（已按「零踩踏」处置，本文即证据）

本件补丁制作期间（23:35–23:52 CST），远端另有提交落地：

```
62c2f409b  2026-09-16 23:41 CST  feat(cards): 删「市场温度计·分项」「六维共振雷达·战术层」两卡
                                 + 危机雷达并入「新周期判定·海外宏观」
           logic.html  +15 −244   （基线 731,813 B → 718,386 B）
```

处置过程（**没有覆盖他一行**）：
1. 我的推前 **blob 漂移守卫拦下**：本地基线 `731,813 B` / git-blob `b3c9cd7a` vs 远端 `718,386 B` / `4f024829`；
2. 首个差异定位到 `#lg-ph`（市场温度计 pane）——与我的改动面**零重叠**；
3. **弃用旧产出，拉最新基线重做全部 12 处补丁**（新基线 `718,386 B` / CRLF 8,447），锚点 12/12 命中；
4. 推前再守卫一次，通过后才提交。

> 🔴 **提示**：今晚 `alimi-cn` 侧**至少三个写者**（① 我的前端重构 `e9f785505`；② 链路修 D6/D7/D8 `1971a6e88`；③ 本次两卡删除 `62c2f409b`）。
> 三者的文件面互相不重叠是**运气 + 守卫**共同保住的。建议：**同型改动前先在 `docs/ops/handover/` 声明「本轮我将触碰哪些文件」**，把「事后靠守卫拦」提前成「事前靠声明避」。

---

## 5. 双机云端对齐结论

| 面 | 状态 | 依据 |
|---|---|---|
| 前端 `index.html` | ✅ 已对齐 | 线上 `BUILD=b726a4dbc`，byte 级 cmp 归一化缓存戳后**完全相同**；你已在 tip 上逐字节复核过你的 4 个代码文件未被我回退 |
| 数据层 `data/*.js` | ⚠️ **两处待你/我方重跑** | ① 聚合快照 `BACKTEST_ALL_ALGOS.js` 仍持**已被作废**的四量值（前端已兜住，本体未重跑）；② 因子实验室聚合 `source_time` 落后于源文件真实 `update_time`（09-15 16:06 vs 09-16 03:16） |
| `raw_data/*` | ✅ 无我改动 | 本轮我 0 触碰 `data/` 与 `raw_data/` |
| 云端 workflow | ✅ 你侧未动 + 我侧只读审计 | 我**未改任何 workflow**（PAT 无 Workflows 写权限，且本轮无此需求） |
| 双机自动化 | ✅ 已错相 | `da2072fc` :10 → :25/:55（§2.3） |
| 交接面 | ✅ 双向已读 | 我读你 4 份（§1），你已复核我 3 份（你的 2219 §1） |
| 分工不重叠 | ✅ | 本轮我只动 `index.html`（已上线）+ `logic.html`（本件）+ 本交接件；你动 `yml` / `algorithms/*.py` / `v8_health_check.py` |

---

## 6. 移给你的待办（按优先级）

| # | 事项 | 归属 | 备注 |
|---|---|---|---|
| 1 | **P-1 + P-2 同批落盘**（workflow 条件化覆盖步 + D3 判据同源化） | 你（SSH 通道） | 已放行（§3）；落盘后回执 `sed -n '200,215p'` 实测 |
| 2 | **重跑聚合生成器** `gen_backtest_all_algos.py` | 你/我（写者二选一，勿双写） | 根除「聚合快照持有被作废口径值」；建议**你的 D7 修完 + 今晚 02:19 E 批跑完后**再做，一次到位 |
| 3 | **把「强势跟踪」并入 `BACKTEST_ALL_ALGOS`** | 你/我 | 前端现用**合成行**（仅在数据里没有该卡时补），真行上线后自动取代、不会重复 |
| 4 | P-4 / P-5 影响面评估（76 条桥接） | 你 | 见 §3 附注，我倾向乙案但要例外通道 |
| 5 | 今晚 02:19 E 批：四量 `total_signals ≈ 861` 且 `range = 近 5 年`？ | 你（只报数） | 双保险（你的 D7 + 主人 16:20 的防降级守卫）验证点 |

---

## 7. 边界与自证（我这边）

- ❌ 未改：`.github/workflows/*`、`data/*`、`raw_data/*`、`algorithms/*.py`、`update_v8.py`、`v8_health_check.py`、`strategy_four_volume.py`
- ✅ 本件推送 = **`logic.html` + 本交接件**，单提交、`force=False`、`base_tree` = 推送前一刻的 tip 根树
- ✅ 推后自证：`compare API` 断言 `ahead_by=1` 且**改动文件仅我列出的 2 个**（零覆盖）
- ✅ 推前守卫：`logic.html` 相对我的内容基线**任何**差异 ⇒ 直接中止（logic.html 无缓存戳，不存在「纯戳差异」的合法情形）
- ✅ 家机自动化：只做**错相 1 项 + 停用 1 项**，均为「运行时序/死任务」层面，**未删任何任务**、未动 prompt 语义判据

---

*本报告仅供研究参考，不构成个人投资建议。*
