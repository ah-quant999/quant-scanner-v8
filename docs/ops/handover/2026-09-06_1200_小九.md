# 交接文档：小九 → 阿狸咪（2026-09-06）

> 更于 2026-09-06 12:00 自动更新（automation-1785719698947）。本档由小九白班自动生成，覆盖当日截至 12:00 的全部状态。阿狸咪夜班/周末接班请先读本档。

---

## 一、背景速览

- 九宝量化 v8（quant-scanner-v8）为当前唯一主力版本，v6 已弃用。
- 部署链：本地改好 → 主人明示「上线」→ commit+push main → GitHub Pages（CI `v8_build_deploy.yml`）自动发布。
- 双机分时铁律：小九白天独家 7:45-17:45；阿狸咪夜间 18:00-次日 7:30 + 周日全天。今日（周日）按周末排班为阿狸咪时段，白天 12:00 前的动作仍由小九完成。
- 数据铁律：不发布 data/ 目录，前端只读 `window.X` 注入；`git add -f v8/*.py` 保证阿狸咪 pull 同步脚本。

## 二、今日已完成改动（真实 SHA，git log 实测）

### 1. AI预测卡下架 + 死代码清理（11:21-11:27，白班小九会话）

| SHA | 内容 | 影响 |
|---|---|---|
| `34317cd0` | feat: 下架 AI预测卡（walk-forward 回测全参数 INVALID）+ 清理死代码 | index.html 移除 AI预测卡；108 组参数扫描全 INVALID，判定无有效信号，果断下架 |
| `e25b8b74` | chore: E批移除 path_probability_backtest.py | 删除孤儿回测脚本 |
| `3ba9c433` | chore: 移除 PATH_PROB_BACKTEST 映射 | 生成器映射表同步清理 |
| `e85521ae` / `fbf637e8` | chore: 删除 AI预测卡回测孤儿数据 | data 层孤儿数据清干净 |
| `c4da4b11` | feat: logic.html 下架 AI预测卡 + 清理死代码 | 逻辑说明页同步 |
| `d4599c9d` / `df71a0c9` | docs: AI预测保护段更新为 INVALID→已下架 | 说明文档补 108 组扫描结论 |

**当前 main HEAD：`df71a0c9`**（docs: logic.html AI预测段补 108组扫描 INVALID→下架结论）

### 2. 数据管线（云端 bot 自动）

- `4ffd2db3` / `1cddb328`：v8 cn fetch + risk gauge @ 09-06 10:58，正常。
- `0f20ec99` / `2534e980`：v8 build + cn fetch @ 09:43/09:24，正常。
- `2ac042ab`：runner health online @ 10:13:33。

### 3. 监督任务（本机自动化，零写仓）

- **06:14 阿狸咪护栏+三卡盯盘（第三十五轮）**：HEAD `f728ed08` 时点持平，5修复标记在位；三选股卡维持 09-04 周五值（周日休市属预期）→ 正常零告警，overall=ok 106ok/3warn/0fail。
- **08:10 分红刷新监督**：云端分红步连续 4 次成功（周六 328/326/328/328 只），未触发兜底、未本地推送、未发邮件。stock_quote.json 数据提交至 09-05 21:23（`7174cae6`）；顶层 update_time 09-04 只随行情步更新，**勿误判陈旧**。

## 三、当前系统状态（12:00 实测）

| 项 | 状态 |
|---|---|
| 站点 HTTP | ✅ 200 |
| main HEAD | `df71a0c9`（11:27） |
| index.html | 966,329 B |
| lemoncat-cn runner | ✅ 在线（Runner.Listener 进程在跑，10:13 health online） |
| alimi-cn runner | 周日为阿狸咪班次，待其上线路 |
| 云端数据管线 | ✅ 09:43 / 10:58 两轮正常 |
| 告警 | 上午零告警 |

## 四、需阿狸咪注意 / 待办 / 风险

1. **本地有未提交变更，按纪律不动**：`M algorithms/run_algorithms.py`（小九白班遗留，可能是下架清理的收尾，接班后先 diff 确认再决定，勿盲目 checkout 覆盖）。
2. **本地 5 个未跟踪旧文件**（HANDOVER 09-01/09-03、AUDIT 09-03/04/05）：历史留档，不 git add、不删除，维持现状。
3. **AI预测卡已下架**：前端、logic.html、生成器映射、回测脚本、data 孤儿数据均已清干净。夜班若发现任何残留引用/报错，可清理，但勿动其他卡片。
4. **顶层 update_time 口径**：stock_quote.json 顶层 update_time 只随行情步更新（09-04），数据本体到 09-05 21:23，属正常，勿触发误告警。
5. **周日休市**：三选股卡数据停在 09-04 周五值属预期，不要当故障修。
6. 常规夜班纪律不变：只读监督任务（紧急指令监听）不写仓；护栏+三卡盯盘每轮记录到工作区日志。

## 五、小九监督职责（日间交接后仍在后台运行）

- 12:00 / 18:00 交接文档自动更新（本档即 12:00 槽位产物）。
- 分红刷新监督（08:10 槽位）+ 云端兜底。
- 阿狸咪护栏 + 三卡盯盘轮检（06:14 槽位，第三十五轮已闭环）。
- 网络检测 / 部署守卫 / 6h 看门狗 / 邮件告警 7-21 时段照常。

---
*由小九自动生成本档并 push main。仅 git add 本交接文档，其他文件未动。*
