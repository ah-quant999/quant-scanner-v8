# HANDOVER 小九 · 2026-08-03 阿狸咪周末 v8 整改与上线确认

> 作者：阿狸咪（家中机，周末代班）　对象：小九（单位机，周一 08-03 回归）
> **结论先行**：v8 前端 UI 这周末的所有整改已 100% 上线（main + GitHub Pages），周一你上班**不会被覆盖**，无需做任何前端部署动作。

---

## 一、这周末阿狸咪在 v8 上做的一次性整改（全部已 push main）

都是主人看图后发的**临时人工指令**，纯前端 UI + 1 个备份 workflow，**不涉及算法、不需要你续跑**：

| 时间 | 改动 | commit |
|---|---|---|
| 8-01 收尾 | B任务：4孤儿原生化进 algo_run，sync_legacy 退役；A任务：删死页 | `7d08e64` |
| 8-02 多轮 | 顶栏 3 tab 锁图标统一 / 27 概览拆 3 卡片(盘前盘中盘后) / 战术驾驶舱 3 行 / 超买超卖改名 / F窗口展开下转 / ETF商品v6快照兜底 / fmtYi单位万亿bug修复 / 中信期货4品种改紧凑表格 / 更新于紧跟卡片名 / 新建 v8_backup.yml 每日备份 + 逻辑详解对比表同步 | `74afcff` `4a1adb0` `df073f8` `2254714` `6935c8d` `8e0e906` `4f86253` |

完整逐条见 v6 仓 `automation-1783696499491/memory.md` 的 8-02 各时段记录。

**关键：这些都是「一次性」人工指令，不是定时任务，周一你不需要、也不应该重跑它们。**

---

## 二、为什么不会被你（小九）覆盖 —— 架构铁证

1. **v8 是独立仓** `quant-scanner-v8`，GitHub Pages 源 = **main 分支**（不是 gh-pages，2026-08-01 改造后改为 main 出）。
2. **所有 v8 workflow 只动 `raw_data/` 和 `data/`，绝对不碰 `index.html`**：
   - `v8_build_deploy.yml`：push 触发 → `update_v8.py` 只生成 `data/*.js` → 只 `git add -f data/` 推回 main。index.html 纹丝不动。
   - `v8_cn_fetch.yml` / `v8_algo_run.yml` / `v8_self_heal.yml`：只动 raw_data/。
   - `v8_sync_v6_data.yml`：定时已**停用**（注释明确「v6 已停，定时同步会覆盖新鲜 v8 数据」），仅 `workflow_dispatch` 应急。
3. **你的本地自动化（WorkBuddy）操作的是 v6 仓 `stock-scanner`**，不是 v8 仓。你跑 `update_data_v2.py` / `deploy_now.py` 部署的是 **v6 自己的 Pages**，和 v8 是两套独立站点，互不影响。
4. **阿狸咪的周末监督自动化**（`1785510427927`）`validUntil=2026-08-02T23:59` **今天已到期**，周一自动不跑；且它只兜底 raw_data/（从 v6 复制真实数据），铁律禁止手动 deploy，**不动 index.html**。
5. 周一真正 ACTIVE 的本地自动化只有 2 个且都是**只读**（紧急指令监听 `1783696499491`、自动交接检查 `1783523003845`），不写代码、不部署。

→ **结论：周一你上班，v8 前端没有任何渠道被旧版覆盖。**

---

## 三、v8 数据新鲜度链路（周一你回归后正常）

- v8 盘中/盘后数据由 **v8 云端 Actions**（`v8_cn_fetch` / `v8_algo_run` 等）按 cron 跑：需中国 IP 的跑在「中国节点」，其余跑在「云端 ubuntu」。
- v6→v8 同步桥已停用定时，v8 现在**原生**产出 raw_data/，不依赖 v6。
- ⚠️ 唯一注意：如果你在 v6 仓手动跑 `update_data_v2.py`，**不要**顺手启用 `v8_sync_v6_data` 的定时——会把陈旧 v6 数据覆盖新鲜 v8。应急才手动 dispatch。

---

## 四、已知待办（非一次性，留给后续；不是你周一必须做的）

1. **ETF 商品分类**：当前是 v6 7-29 快照兜底（`raw_data/etf_intraday_heat.json` 标 `stale_source`），待把 v6 `fetch_etf_intraday_heat.py` 的 akshare `fund_etf_spot_em`+6大类逻辑移植到 v8 `cloud_fetch_v8.py::f_etf_intraday_heat`，去掉兜底。
2. **全站精选数据时效**：`index.html` 行 2113 标注「数据源停在 2026-07-30，待 v6 跑批同步」——等 v6 跑批后这条会自动刷新；若长期不刷需查 sync 桥。
3. **AI 市场速览 A 阶段**：行 569/3592 仍是演示写死数据，B 阶段接 JSON 才真实。
4. **逻辑详解页行 3876**：仍写 v6 的「safe_pull→update_data_v2 --fast→deploy --force」自修复流程，未更新到 v8 的 `v8_self_heal.yml` 机制——说明文案滞后，建议改。
5. **心跳监控上云表格措辞**（行 3579）：「⚠️ 仅本地 PAUSED」是历史措辞，v8 实际无需本地 peer_health（Actions 日志即心跳）——主人尚未拍板是否改，待定。
6. **北向资金**：港交所 2024-05 起停止披露 top_buy，系统已标「停更」，属设计内，无需处理。

---

## 五、阿狸咪周末任务说明（一次性，已收尾）

- 我的周末任务 = **代班 v8 监督**（自动化 `1785510427927`，今天 23:59 到期）+ **响应主人临时 UI 整改指令**。
- **这周末的 UI 整改全部是主人看图后的人工指令，一次性，不是我的定时任务，也不是要你续跑的任务。**
- 我的监督自动化今天到期，周一不再跑；v8 日常跑批已由你（云端 Actions + 中国节点）接管，回归平稳。
- 家中机我（阿狸咪）自 7-23 起的断线状态仍持续，所以周末 v8 监督改由云端 Actions + 这个临时本地自动化兜底；周一你回归后，v8 全链路恢复你托管。

---

## 六、周一你回归后**强制核对**（5 分钟，比 30 秒更严）

> 主人 8-02 21:52 选 C 方案（今晚不动 / 周一自然检验），但截图里主人也承认「今晚看不到结果」是风险。
> 所以周一你回来必须**主动**按这份清单跑一遍，**不要等主人提醒**。任何一项异常立刻回滚到上次稳定的 commit。

1. **基础核对**（30 秒内可完成）
   - 浏览器开 `https://ah-quant999.github.io/quant-scanner-v8/` hard refresh（Ctrl+Shift+R）
   - 看：顶栏 3 个 🔒 tab（共振日历/运维/逻辑详解）/ 27 概览 3 卡片（盘前盘中盘后 行内 name 紧贴 / 5 维度 grid 等分铺满整行）/ 中信期货 4 品种紧凑表格 / 逻辑详解对比表「每日备份 ✅」
   - 看 GitHub Actions：最近 `v8_cn_fetch` / `v8_build_deploy` / `v8_algo_run` 全部绿色，无黄色 warning

2. **今晚没验的潜在风险强核**（这些只能等周一你自然跑才能发现）
   - **STOCK_LIST.js 拼音首字母**（commit `ea1bd9a`）：主人截图指过搜索「mryl」找不到迈瑞医疗，我用 pypinyin 补了 5202 条 `py` 字段。**周一你跑通后，亲自在搜索框打 `mryl` / `mnjk` / `pfyh` 验一次**——若仍失败，备查 `STOCK_LIST.js.bak`（本地备份，未上传 GitHub）
   - **逻辑详解「中国节点产出哪些前端」表**（commit `11c38b1`）：表里 4 行（盘前 / 盘中 / 盘后抓取 / 盘后算法链）是我从 yml 注释读的，**可能漏了某些新加但没标 cn runner 的脚本**。周一你跑完比对一下表与 `v8_cn_fetch.yml` 实际 step 列表
   - **表格行内 grid 等分**（commit `d99be04`）：CSS `table-layout:fixed + grid-template-columns:repeat(5,1fr)` 在 Chrome/Edge 都验过，但**移动端 Safari 没验**。主人只在桌面看，若用手机访问发现挤成一行，立刻降级回 flex
   - **v8_algo_run 的 4 类上游已原生化（2026-08-02 深夜已落地，非断供）**：`gold_pool / scan_result / watch_result / guanlan_* / mahoro_signals` 现由 v8 自产——`scanner.py`/`guanlan_extractor.py`/`fetch_maharo_signals.py` 经 `V8_OUT_DIR` 钩子写 `out/`，`run_algorithms.py::step_v8_self_sufficiency()` 调度，`step_seed_inputs()` 默认 no-op（`V6_SEED=1` 才回退 v6 重灌）。**周一 18:30 这 4 类会由 v8 自产，不再是数据空洞**；你只需确认面板正常出数，无需从 v6 补。
   - **顶栏 3 tab 锁图标**（commit `6935c8d`）：用 🔒 emoji，可能在 Windows/Mac/Android 显示大小不一。**主人若截图说太大/太小/错位**，改回 SVG

3. **若发现异常，回滚步骤**（30 秒）
   ```bash
   cd /e/workspace/quant-scanner-v8
   git log --oneline -10  # 找可疑 commit
   git revert <commit>    # 单 commit 撤回；或
   git reset --hard <稳定commit>  # 整批撤回（先 safe_pull 备份当前 main）
   git push origin main --force
   ```

## 七、阿狸咪周末任务说明（一次性，已收尾）

- 我的周末任务 = **代班 v8 监督**（自动化 `1785510427927`，今天 23:59 到期）+ **响应主人临时 UI 整改指令**。
- **这周末的 UI 整改全部是主人看图后的人工指令，一次性，不是我的定时任务，也不是要你续跑的任务。**
- 我的监督自动化今天到期，周一不再跑；v8 日常跑批已由你（云端 Actions + 中国节点）接管，回归平稳。
- 家中机我（阿狸咪）自 7-23 起的断线状态仍持续，所以周末 v8 监督改由云端 Actions + 这个临时本地自动化兜底；周一你回归后，v8 全链路恢复你托管。

— 阿狸咪 · 2026-08-02 21:55（补：主人 21:52 选 C 不动，把今晚没验的潜在风险清单化进 HANDOVER 第六节）
