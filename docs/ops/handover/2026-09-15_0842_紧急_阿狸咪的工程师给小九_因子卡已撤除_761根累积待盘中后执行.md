# 🔴 紧急交接：因子卡已撤除（已上线）· 761 根累积「待执行」

- **出单人**：阿狸咪的工程师（家用机 alimi-cn）
- **接手人**：小九的工程师（单位机 lemoncat-cn）
- **出单时间**：2026-09-15 08:42
- **执行窗口（主人令）**：**中午休市（11:30–13:00）或下午收盘后（15:00 之后）**。
  **盘中（09:30–11:30 / 13:00–15:00）严禁执行、严禁打扰** —— 主人 2026-09-15 08:38 令：
  「盘中高频更新是最高等级优先，全员不得打扰！」

---

## 一、第 1 项「因子补缺与审计」卡 —— ✅ 已由阿狸咪完成并上线

按主人 09-15 06:35 令「不好就删，你这里也没删啊」执行完毕，**共推 2 个提交**：

| 提交 | 内容 |
|---|---|
| `e34883d9c` | 撤除该卡：**24 处编辑**（index.html 7 处 / logic.html 2 / update_v8.py 4 / run_algorithms.py 6 行 / dedup_fetch_manifest.py 4 / workflow 5）+ 删 6 文件 |
| `319abc925b07` | 🔴 修复 `logic.html` 行尾被静默改写（见下方「事故记录」） |

**硬验证（远端 `git show`）**：
- 6 个文件全部确认删除：`scripts/gen_factor_audit.py` / `gen_factor_progress.py` / `data/FACTOR_AUDIT.js` / `FACTOR_PROGRESS.js` / `raw_data/factor_audit.json` / `factor_progress.json`
- `index.html` 中 `FACTOR_AUDIT|FACTOR_PROGRESS|factorAuditBody|__renderFactorAudit` **残留 = 0**
- `index.html` 全部 **27 段 inline `<script>` 跑 `new Function`，0 错**（改前后同段数、同 0 失败）
- `logic.html` 行尾还原为 **CRLF 8655**（= 原 8656 − 1，因删 1 行）

**⚠️ 需你留意**：`run_algorithms.py` 尚存 **5 处注释**提到 `FACTOR_AUDIT/FACTOR_PROGRESS`（在讲「另 4 张卡挂链缺失」的历史注释里），
**属注释、无功能影响**，本次刻意保留以免破坏上下文。若你要清可顺手清，非必须。

### 🔴 事故记录（值得写进经验）

`e34883d9c` 第一次推送时，我用了 `git hash-object -w <临时文件>` 入库。
在 `core.autocrlf=true` 下，**不带路径的 `hash-object` 会按默认属性把 CRLF 规范化为 LF** ——
而 `logic.html` 在 `.gitattributes` 里是 **`text: unset`（本应保持 CRLF）**，
结果该文件被整体改写行尾 ⇒ 产生 **17311 行伪变更**（内容其实只差 1 行）。
已用 `319abc925b07` 还原（断言「忽略行尾后与线上版本逐字节一致」才推）。

> **教训（务必遵守）**：凡非 LF 行尾文件（本项目 = `logic.html`）走 `hash-object` 入库，
> **必须** `git hash-object -w --no-filters <file>`；`--no-filters` 与 `--path=` **互斥**，只能二选一。
> 另：`.gitattributes` 中 `index.html` / `*.py` 是 `text eol=lf`，`logic.html` 是 `text: unset`。

---

## 二、第 2 项：`factor_lab_backtest.py` 抓取窗口 700 → ≥761 与「累积中」 —— ⏳ 待你执行

### 主人原话

> 「那就去获取761根，拿不到就写累积中，直到累积到为止。」

### 现状（已实测）

| 位置 | 现值 | 问题 |
|---|---|---|
| `algorithms/factor_lab_backtest.py:45` | `FETCH_DAYS = 700` | 加 250 档需 `NEED_MIN(261) + 2×250 = 761` 根 ⇒ **700 不够，会全票 0 样本** |
| `:43` | `HOLDS = [5, 10, 20]` | 主人要的 **30/60/90/180/250 从未落地** |
| `:172` | `if n < NEED_MIN + 2 * HOLDS[-1]: continue` | 这是**静默跳过**——不满足就整票丢弃，前端看不到原因 |

### 改造要求（三条，缺一不可）

1. **抓取窗口提到 ≥761**（建议 `FETCH_DAYS = 800` 留余量）。
   注意三级兜底链 `_query_kline(code, market, FETCH_DAYS)`（mootdx→东财→baostock）的**单源历史上限**——
   若某源拿不到 761 根属正常，走第 2 条，**不得因此丢弃该票**。

2. **`HOLDS` 扩到 `[5, 10, 20, 30, 60, 90, 180, 250]`**。
   前端档位表是**动态发现** `win_Nd` 键的（无白名单，见 `index.html` 因子回测卡）⇒ 算法层补上即自动显示，**前端无需改档位代码**。

3. 🔴 **不足必须写「累积中」，禁止静默 0 样本**（这是主人这条令的核心，也是「禁 0 冒充」铁律）：
   - 每档位算 `n_eff`；`n_eff == 0` 时 **不得输出 `win_rate = 0`**（否则前端显示「0% 胜率」＝假数据），
     应输出 `null` + 状态字段，例如：
     `{"accumulating": true, "have": N, "need": 761, "progress": N/761}`
   - 前端该档位显示「累积中 N/761」，而非 0%；
   - 若要做到「**直到累积到为止**」，需要**跨日累积缓存**（把每次抓到的 K 线并入本地长历史），
     否则每天重抓仍是同样长度、永远到不了 761。建议落 `raw_data/_factor_hist_cache/`。
     🔴 **但该目录若不入仓，云端 `git clean -ffdx` 会清掉、累积归零**；若要跨机累积，
     必须在 09-08 主人令「仅 `kline_cache` 入仓」之外**另行请示主人扩令** —— 这条我已核过，请勿擅自扩。

4. **只能你跑**：阿狸咪侧 A 股取数全断（红线：`A股=0 且 总数>0 ⇒ 报警`，禁伪造数据）。
   你的自动化「九宝量化-因子实验室数据刷新(小九本机·baostock)」（id `60e1dbfd…`，周六 10:00，现 PAUSED）正是落点，改完可考虑恢复。

### 顺带（低优先，可缓）

仓库根另有 **8 个 `backup_2026xxxx/` 目录**（07-18 ~ 07-26 手工备份）疑似噪声，未取证未删，等你或主人定性。

---

## 三、铁律复述（本次务必遵守）

1. **盘中（09:30–11:30 / 13:00–15:00）严禁执行本单、严禁推送、严禁打扰** —— 主人最高优先令。
2. 改动含 `index.html` 时：推前**必须**跑「提取全部 inline `<script>` → `new Function`」**0 错**。
3. 跑批期间（`git status -s raw_data/` 有 ` M`）**不得在工作树编辑 `index.html`**；一律走
   FETCH_HEAD 派生 + 独立 `GIT_INDEX_FILE` + `commit-tree`（不碰本机 index）。
4. **推前必重取基线**（`git fetch origin main`）—— 双机抢推靠它挡刀（本次我推送后 80 秒就被
   `v8-cache-bot` 叠了一个缓存戳提交，血缘正常）。
5. 非 LF 行尾文件入库用 `--no-filters`（见上方事故记录）。
6. 做完跑 `align_logic_ops.py` 确认 **EXIT 0**（logic.html 与运维页对齐）。

---

## 回执

执行完毕后请在本文件末尾追加：
`> [小九] YYYY-MM-DD HH:MM 执行完毕：<逐项结果 + 校验输出摘要>`
