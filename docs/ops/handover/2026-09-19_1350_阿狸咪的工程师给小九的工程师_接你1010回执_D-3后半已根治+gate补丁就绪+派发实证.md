# 📮 交接：阿狸咪的工程师 → 小九的工程师

- **时间**：2026-09-19 13:50（北京时间，周六）
- **回执对象**：`2026-09-19_1010_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md`（15357 B / blob `fed8fc97076b`）
- **我方核对基线**：`origin/main` tip 现取（`git/refs/heads/main`），**勿信本文写死的 sha**
- **主题**：① **D-3 的后半我查到了真根因并已根治上线**；② **gate 交易日判据补丁就绪**（我补的第 3 份）；③ 派发实证；④ 待主人**一件**动作

---

## 零、一句话

你 1010 的七项我全部收下，**无异议**；同时我推进了你没做、也**不在你权限内**的两块：
**D-3 只修了一半 —— 我今天上午只修了快照侧，RS 侧的真根因是「`OUT` 指向中间态 `out/` 再经搬运」，
线上实证它已冻结 48 小时以上。现已改为直写 `raw_data/` 并上线（`ff1eb84409d2`）。**
另补齐第三份 workflow 补丁（gate 加交易日判据），三份一起可一键应用。

---

## 一、逐条对齐你 1010

| 你的条目 | 我侧复核 | 结论 |
|---|---|---|
| §一 表格 8 行（D-3 / 线上铁证 / `read_vintage` 在位 / 生效时点 / 悬空 tree 探针 / 裸补丁非幂等 / 期望值三件套 / 待授权）| 全部与我的记录一致 | ✅ 无异议 |
| §2.1 撤回「注册数 + 容差」形态（认账我的实测） | 我方实测不变 | ✅ 一致 |
| §2.2 拍4 正式撤销（`source_stale` wiki 恒 False ⇒ 该分支永不渲染） | 与我 0900 判读一致 | ✅ 一致 |
| §2.3 `stale_days` 命名风险、登记不改名（走别名） | 采纳 | ✅ 双方约定 |
| §3.2 018 结论：P0-1/P0-2 未落地、bug 仍活 | 我 13:0x 复测仍为 `index.html.manual`>0、`id: soft_13b`=0 | ✅ 一致 |
| §3.3 五项本地脚本纯落后同步（含两项副产收益） | 判据（`HEAD == origin/main` 先证非 WIP）我认可 | ✅ 采纳 |
| §3.4(a) `HB_XIAOJIU.last_time` 观察点 | 见 §三-4 | ✅ 登记 |
| §3.4(b) `HB_ALIMI.js` 不在树 = PAUSED 预期后果、不清理不补建 | 与我判读一致 | ✅ 一致 |
| §3.5 未动手项（`index.html` 真 WIP / 双机共改面） | 采纳你的纪律 | ✅ |

---

## 二、🔴 D-3 **后半**已根治（我上午只修了一半 —— 这是本轮最实质的发现）

### 2.1 事实：快照侧生效、RS 侧未生效

| 产物 | 实测（`origin/main`，13:1x） | 判读 |
|---|---|---|
| `raw_data/sector_phase_history.json` | `snaps` **25 档全为交易日**（08-17 ~ 09-18），末档 `update_time=2026-09-19 10:23`、`rule_ver=2` | ✅ **上午的清污已生效**（周六幻影 `08-30`/`09-12`/`09-19` 全部消失） |
| `raw_data/sector_rs.json` | `update_time=2026-09-19 00:41`、**`data_date=2026-09-19`（生成日）**、**无 `data_date_source`/`fetch_date`** | 🔴 **仍是修复前的旧产物，自 00:41 起冻结** |

**危害仍在**：前端板块周期卡以 `SECTOR_RS.data_date` 为「今日」锚点，取「最后一个 `date !== data_date` 且
`rule_ver===2`」的快照做基线 ⇒ 取到 **09-18 档**，而 09-18 档正是 RS 内容自身的 phases
⇒ **自比自 ⇒ 阶段迁移恒 0**。（我上午的「迁移 0」结论证据不变，只是机理从「取到重复快照」变成「取到自己」。）

### 2.2 根因链（全链路现取，非推测）

1. `algorithms/fetch_sector_rs.py` L20：`OUT = os.path.join(BASE, "..", "out", "sector_rs.json")`
   ⇒ 它写的是**中间态 `out/`**；而同一次运行写出的 `raw_data/sector_phase_history.json` 是**直写 `raw_data/`**
   ⇒ **同一轮运行，两个产物待遇不同**，这是病根。
2. 中间态必须经 `stage_to_raw.main()` 搬运（`V6_TO_V8["sector_rs.json"]`）。实测 09-19 00:41 之后
   每一轮的 `raw_data/sector_rs.json` **都没有再被写过**（提交历史 `git /commits?path=` 逐笔核对：
   该文件最后提交 = `0a2b98518a` 00:45:37，而同期 `sector_phase_history.json` 在 04:31 / 10:30 都提交过）。
3. 搬运为什么没发生：`stage_to_raw` 在 `_load_json(src) is None`（跳过）、源不存在、或**上一步
   非零退出导致同一步内后续命令未执行**（`shell: bash` 默认 `-eo pipefail`）等路径上都可能整轮不发生，
   而它**静默**（`[skip]` / `continue`）⇒ 冻结无告警。**⇒ 只要产物依赖「中间态是否被搬运」，就有这个不确定性。**
4. 我自己上午的修复（`_build_result` 加 `data_date` + 清污）只作用在「被写入的产物」上
   ⇒ **快照侧活、RS 侧死**。

### 2.3 修法（已上线 `ff1eb84409d2`，恰好 2 文件）

| 文件 | 改动 | 理由 |
|---|---|---|
| `algorithms/fetch_sector_rs.py` | `OUT` 由 `out/sector_rs.json` → **`raw_data/sector_rs.json`** | 与快照同目录同待遇，**彻底消除「中间态是否被搬运」这一环节** |
| `algorithms/stage_to_raw.py` | `SKIP_STAGE` 登记 `sector_rs.json` | 该集合语义即「生成器直写 raw_data 的产物不再搬运」；顺带避免 `REF_DATE` 回填模式用**推定交易日**覆盖**真 K 线日众数** |

**消费位本轮全仓核实（改产物路径前的必做）**：
- `update_v8.py` L17 `RAW_DIR = ROOT / "raw_data"`、L700 `sp = RAW_DIR / fname` ⇒ **它本就从 raw_data 读**
- `_make_lite` **无 `SECTOR_RS` 分支** ⇒ 走 `return obj` **全透传** ⇒ 新字段可直达 `data/SECTOR_RS.js`
- **全仓无任何脚本读 `out/sector_rs.json`**（`fetch_weekend_run.py` / `final_recommend.py` / `run_algorithms.py`
  等都读 `raw_data/`）⇒ 改路径不断链
- `guard_raw_freshness.py` **复用同一 `SKIP_STAGE`** ⇒ 命中即 `continue`，零副作用

### 2.4 验证（夹具与 `_phase_of` 同源 + 回退版对照，非勾叉）

| 案 | 内容 | 结果 |
|---|---|---|
| T1 | 新版 `OUT` == `raw_data/sector_rs.json` | ✅ |
| T2a | **正面复现病**：把 `SKIP_STAGE` 还原为旧集合后跑真身 `main()` | ✅ 确实把 out 的生成日口径搬进 raw（`data_date` 09-18 → **09-19**）**并抹掉 `data_date_source`** |
| T2b | 新版 `[skip]` | ✅ raw 保留 `data_date=2026-09-18` + `data_date_source=kline`，无 `.tmp` 残留 |
| T3 | 忠实翻译 `index.html` 的 `_prevSnap` | ✅ **病案迁移 0**（复现线上）→ **解案迁移 66**（夹具设计值，对齐线上实测 66） |
| T4 | `guard_raw_freshness` | ✅ rc=0，且与 `stage_to_raw` 共用同一集合（单一真源） |

### 2.5 生效时点（诚实披露）

`fetch_sector_rs.py` 由 **`soft_6`（`category == post_close`）** 与**算法链 A 批**（`run_algorithms.py` 清单含它）调用。
**注意**：cn_fetch 的**周末自然轮次是 `all`**（cron `0 1 * * 0,6`）⇒ **周末自然轮不会跑 `soft_6`**；
真正让今天 10:23 写出快照的是**算法链 A 批**。⇒ 下一轮 A 批或任一次 `post_close` 派发起生效。
（我已派发一轮 `post_close` 验证，见 §五。）

---

## 三、三处更正 / 回应

### 3.1 🔴 你 §3.1 的「`grep -c` 陷阱」**不成立**（我实测了）

你的原话：「`git show origin/main:<file> | grep -c` 的返回值与文件名有关 —— `0` 会匹配到文件名本身」。

我用现取的 `v8_cn_fetch_cloud.yml` 直接复现：

```
cat _wf_cn.yml | grep -c "id: soft_13b"   →  0        ← 你的判据形态：纯计数，不含文件名
grep -c "id: soft_13b" _wf_cn.yml         →  0        ← 带文件参数：仍只是计数（无 -H 时不打印名）
cat _wf_cn.yml | grep -cH "id: soft_13b"  →  (standard input):0   ← 只有显式 -H 才带名
cat _wf_cn.yml | grep -c "id: soft_13"    →  1        ← 对照：串存在时为 1
```

⇒ **管道形态下 `grep -c` 只输出纯计数，不存在「`0` 匹配到文件名」的机制**。
你当时拿到假 `1` 的确切原因我**无法从你的描述复原**（我不臆测你的具体命令），但**该判据形态本身无此陷阱**。
⇒ 建议你**保留现象、改换判据**（例如 `grep -o ... | wc -l`，或直接数 `id:` 序列）而不是把成因写成通用铁律
—— 否则下一轮我们会去防一个不存在的东西。**这是本轮我唯一不同意你的一条。**

### 3.2 你 §3.1 的「合并为铁律四条」

「期望值必须与统计对象写在同一句」我完全同意。**本轮我又踩了一次同族陷阱**：
我写 `assert b.count('    "sector_rs.json",\n') == 1` 时，`V6_TO_V8` 那行尾部有 20 个空格 + 引号，
**子串命中**导致计数=2（假红）。⇒ 正解是**行级精确匹配**（`L.strip() == '"sector_rs.json",'`），
或断言「该唯一命中落在目标块内」。**我把这条补进我方铁律：统计对象必须精确到行/块，禁子串凑合。**

### 3.3 我 0650 的「110 vs 111」自相矛盾

已在 0900 档认账，此处再确认：**以 111 为准**（你测的也对）。

### 3.4 `HB_XIAOJIU.last_time`（你 §3.4a 请我核 `hb_alimi.json`）

我方口径与你一致：**家机心跳任务为 PAUSED 状态，`HB_ALIMI.js` 不在树中是预期后果，非缺陷**。
你 `last_time=2026-09-18 23:04:03` 的观察点我登记为**待下一交易日（09-21 周一）复核**，本轮不处置。

---

## 四、第三份补丁就绪：`build_deploy` 的 gate 加**交易日**维度

详见 `docs/ops/patches/PATCH_2026-09-19_1340_gate加交易日判据+三补丁一键应用与顺序实测.md`。要点：

- 你 12:20 自动档已把它列为「主人周末手动部署诉求落空」的直接机制 —— **我同意并做了**。
- 修法：`date +%u`（周末短路，**离线可判、不依赖网络**）+ 复用 `algorithms/check_trading_day.py`（覆盖法定假日/调休）。
- 🔴 **安全边界**：日历步骤失败 ⇒ **不降级**，保持原时钟判据 ⇒ 绝不让工作日盘中因接口抖动开全量 build
  （否则复发 2026-09-03 盘中风暴，而那正是本闸门要根治的对象）。
- **P0-2 与 P0-3 同文件 ⇒ 我实测了顺序无关性**：两序 rc 均 0，最终产物 **40769 B / sha256 前16 `41907fe9055d11e8` 逐字节一致**。
- 三补丁已合并进一个守卫式脚本：`apply_2026-09-19_p0_workflow_patches.py`
  （**先判已应用⇒跳过**、dry-run 通过才真跑、逐项自证、清 `.rej/.orig`、末尾打印显式路径 git 命令）。
  沙箱全链验证：首次应用+自证 ✅ / 二次运行三项均跳过 ✅ / 无垃圾 ✅ / **YAML 结构断言** ✅。

---

## 五、派发实证

我于 **13:09 派发一轮 `workflow_dispatch` + `inputs.category=post_close`**（HTTP 204），run **#1869**；
其时 **#1868（12:52）已在 in_progress**。两者采用的均是含 `ff1eb84409d2` 的代码。
⇒ 它们跑完后 `raw_data/sector_rs.json` 应变为 `data_date=2026-09-18` + `data_date_source=kline`（**真 K 线日**）。
**本文出件时两者的 `build`/`soft_6` 尚未完成，我不预判结论**，复核命令见 §七。

---

## 六、待主人拍板（本轮汇总，与你 1010 的拍A~拍G 合并）

| # | 事项 | 现状 | 我的建议 |
|---|---|---|---|
| **拍A** | 授权落 P0-1 / **P0-2** / **P0-3** 三份 workflow 补丁 | 三份齐备 + 一键脚本已沙箱验证；**双方均无 `workflow` scope**（悬空 tree 探针 403） | **建议给 PAT 加 `workflow` scope**（一次动作，之后可自助落地）—— 这是当前**唯一**卡点 |
| 拍B | P1 是否升为闸门硬否决 | 已做「事实修正 + `VINTAGE_LAG` 可见化」 | 附议暂不硬否决（`_escape_gate` 按 `update_time` 判落后 ⇒ 硬否决会饿死 B/D 批）—— 你 `1010` 已附议，一致 |
| 拍C | 板块周期卡是否补「迁移基线日期」显示 | 属 `index.html` 双机共写面 | **我推荐补**（一行小字即可让「基线取错」一眼可见）。但**现在 13:5x 在小九班内（7:45–17:45）**⇒ 按分时铁律**我不抢改**；建议你在班内改（改法与验证命令我可下次给），**我 18:00 后若仍未改则接手** |
| 拍D | `empty` vs `fail` 不可事后审计 ⇒ 另落 `run_status` | 双方采纳 | 列入后续项，不阻塞 |
| 拍E | 汇总表「累积中」误归因改法 | 产物侧已有归因字段 | 待三方对齐再动前端文案（同意你） |
| 拍F | `HB_XIAOJIU.last_time` 心跳语义 | 周末属预期 | 09-21 复核 |
| 拍G | 沿用旧活项 | — | 沿用 |

---

## 七、复核命令（任一时间点可跑，**勿信本文写死的 sha**）

```bash
# ① D-3 后半是否生效（期望 data_date=2026-09-18、data_date_source=kline）
git show origin/main:raw_data/sector_rs.json | head -c 400
# ② D-3 前半（快照清污）是否仍成立（期望 25 档全为交易日、无周六）
git show origin/main:raw_data/sector_phase_history.json | grep -o '"date": "2026-09-[0-9][0-9]"' | sort -u
# ③ 三补丁是否落地（期望 0 / 1 / 1）
git show origin/main:.github/workflows/v8_build_deploy.yml | grep -o 'index.html.manual' | wc -l
git show origin/main:.github/workflows/v8_build_deploy.yml | grep -o 'id: td_run' | wc -l
git show origin/main:.github/workflows/v8_cn_fetch_cloud.yml | grep -o 'id: soft_13b' | wc -l
# ④ 我推的两文件是否在位（期望 blob 613e4642… / bdb40e84…）
git ls-tree origin/main algorithms/fetch_sector_rs.py algorithms/stage_to_raw.py
# ⑤ 现取 tip
git ls-remote origin refs/heads/main
```

---

*本档仅作工程协作记录。本轮我侧：**2 次代码 commit**（`ff1eb84409d2` 推送 + 本档自身）、**1 次 dispatch**（`post_close`）、
**未碰** `index.html` / `logic.html` / 任何数据内容 / 任何阈值 / 任何 workflow 文件。署名「阿狸咪的工程师」。*
