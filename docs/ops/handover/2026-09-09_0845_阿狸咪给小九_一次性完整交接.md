# HANDOVER｜阿狸咪 → 小九：一次性完整交接（2026-09-09 08:45）

> 主人令「还有什么要一次性交接完整的」→ 本文件为**收口版**，涵盖 09-08 夜班至 09-09 早班全部事项。
> 所有结论均带实测证据（commit sha / 行号 / 字段时间戳），未凭印象。

---

## 0. 当前客观状态（08:34 实测）

| 项 | 值 | 证据 |
|---|---|---|
| 线上 overall | **fail** | `data/HEALTH_CHECK.js` summary `{ok:108, warn:0, fail:3, total:111}`，updated `2026-09-09 08:34:00` |
| 红灯 3 项 | 因子实验室（盘后数据）、BACKTEST_COMPREHENSIVE（全量数据）、CRDS_BACKTEST（全量数据） | 同上 items 遍历 |
| 阿狸咪本地 clone | 工作区 **0 改动**，落后 origin/main 28 提交（仅未 pull，无未推内容） | `git status --porcelain` = 0 条 |
| 家里机 runner | Runner.Listener 活着，Git 已装 `E:\Git` v2.55.0，两级 PATH 已写入 | 见 §1.2 |

**3 个红灯全部是 E 档产物** → 今晚 21:05 E 档跑完应转绿，属预期，勿单独补跑。

---

## 1. 我方已完成并已推 main（**你无需再做，避免重复**）

### 1.1 盘前清空「一劳永逸」修复（06:15 / 06:25 两版）
- 根因：`cloud_fetch_v8.py::_clear_intraday_for_premarket` 时间门控**只有上限**（`h >= 9.5` 才拒绝），00:00–09:29 任何 premarket 跑批都清；premarket 被 hourly 巡检整夜派发 → **00:29/01:30/02:32/03:34/04:36/05:38 连清 6 轮**。
- 修复：主循环改为**纯跳过**（`continue`，不写 stub、**不打任何标记**）。
- ⚠️ 关键坑（v1 曾踩）：给数据打 `premarket_cleared` 或 `no_data` 标记 → 前端 `index.html` **12 处**一见标记就渲染「盘前清空」占位、**不渲染数据**，等于没修。已留代码警示注释。
- 数据恢复：6 变量 ×（raw_data 源 + data/*.js）**共 12 文件**，基准提交 `bb5be1ad8`（09-08 21:05–21:10 真实值）。
- 交接文档：`HANDOVER_阿狸咪_2026-09-09_盘前清空一劳永逸修复.md`（根 + docs 双份）

### 1.2 家里机 Git 环境根治（08:19 完成）
- 装 `E:\Git`（Git-2.55.0.5，遵主人「装 E 盘」）；Machine + User 两级 PATH 均含 `E:\Git\cmd` / `E:\Git\bin`；计划任务 `v8_cn_runner_guard` 已重启，Runner.Listener 新 pid。
- 已推翻你 06:45 URGENT 的两处诊断（非 WSL 截胡：无 `System32\bash.exe`；runner **非服务**，是计划任务 + 登录触发 → 继承用户会话 PATH）。
- 交接文档：`HANDOVER_小九_2026-09-09_0822_家里机Git环境根治完成_请恢复双跑.md`（commit `30ec39c83` / `5a2e5d67a3`）

### 1.3 撞车处置（08:35）
- `60e1dbfd` 因子实验室（周六 10:00，本机直跑 `factor_lab_gen.py` + commit+push，**无并发护栏**）→ **PAUSED**（未删除，遵主人铁律）。
- 已核：你已把 `v8/factor_lab_gen.py` 挂进 **STAGES["E"] 首位**（在 `factor_lab_backtest.py` 之前）+ ORDER L190 + 5400s 超时 + 3600s 静默预算 → **暂停本机兜底后 FACTOR_LAB 仍有产出路径，安全**。
- 其余 8 个派发/推送类自动化逐条核查**均不撞车**（含双链护栏、内容级新鲜度护栏），详见 `HANDOVER_小九_2026-09-09_0835_撞车处置_因子实验室已暂停.md`（commit `dba5bc102d` / `b164822405`）。

---

## 2. 待你执行（3 项，按优先级）

### P0-1｜恢复双跑 `runs-on`
把 `v8_algo_cloud.yml` L91、`v8_algo_run.yml` L128 的 `runs-on` 从 `[self-hosted, cn, xiaojiu]` 改回 `[self-hosted, cn]`，并移除"根治注释"那行。家里机环境已具备（§1.2）。
> 注意：此改动属 workflow 文件，需用带 workflow scope 的 token 走 Git Database API。

### P0-2｜D2-C 只修了一半（你 URGENT 里的项）
`index.html` L9561 实测仍为旧写法：
```js
const liveUpdated = cands.length ? cands.sort().pop() : nowStr;   // ← 未加 hcUpdated
```
- ✅ 已修：`isStale = staleMin > 60`（L9566，原 30 → 已改 60）
- ❌ 未修：`cands` 仍混算 `window.HEALTH_CHECK.updated`（30min 节奏）→ 取陈旧 max 误报「已停更 N 分钟」的根因还在
- 建议按你 URGENT 原文补：`const hcUpdated = window.HEALTH_CHECK && window.HEALTH_CHECK.updated;` 后 `liveUpdated = hcUpdated || (...)`。

### P1-3｜DELISTED 仍是孤儿（暗灯遗留，未闭环）
- `run_algorithms.py` 中 `build_delisted` 命中 **0**（STAGES/ORDER 均无）
- 仅 `v8_algo_run.yml` 引用了 DELISTED 名字
- `data/DELISTED.js` update_time 停 **2026-09-08**，size 830B
- 需你定：**下线**（前端改走 CANDIDATE，删数据+移除巡检条目）or **挂链**（补进 STAGES/ORDER）。

---

## 3. 待你拍板（2 项）

### 3.1 云端按铃让位 E 档（建议采纳）
`1787141452867`（每 30 分钟）在 **21:00** 轮若回测陈旧且两链空闲会派完整链，**5 分钟后你 21:05 E 档再派一条** → `concurrency: cancel-in-progress:false` 下**排队不互踢**，但白跑一整轮。
建议给 `scripts/alimi_bell.py::_algo_need_bell` 加：20:55–22:30 内若近 2 小时已有任意 algo run（不论状态）则跳过按铃。
（已核：该脚本 `_dispatch` 实码**已走 repository_dispatch/trigger_algo**，不绕开探针；第 5 行注释说 workflow_dispatch 已过时，以代码为准。）

### 3.2 FACTOR_LAB 兜底归属
本机已停 → FACTOR_LAB **唯一**产出 = 你的 E 档。若 E 档某晚失败（如 09-09 00:04 被静默杀那次），会红灯且本机不再兜底。
选项：① 维持现状，把 FACTOR_LAB 纳入你 23:50 只读验收清单；② 给我一个「仅当 E 档失败才跑」的条件化本机兜底方案。

---

## 4. 我方边界声明（避免你误判「阿狸咪在跑」）

**会派发/推送的自动化**（共 9 个，其余全为只读监控/交接读取）：

| ID | 调度 | 行为 | 护栏 |
|---|---|---|---|
| `53726469` | 21:30 | 派 `trigger_algo` | 内容级 FRESH + **双链**(cloud/cn) 在跑即跳过 |
| `80a443a6` | 19:30 | 派 `trigger_algo` | 同上双链护栏 |
| `1787141452867` | 每 30min(8–22) | 派 `trigger_algo`(repo_dispatch) | 仅 19:15–23:30 + 回测卡内容新鲜度 + 有 run 跳过 |
| `27b5306c` | 每 30min | 派 `v8_cn_fetch_cloud`(heal) | 数据链，非算法链 |
| `1788058041833` | 每 30min(8–18) | 自愈：候选池/动量 + push | 产物与 E 档不同 |
| `99e0f3c0` | 周六 09:30 | T+1 数据补抓 | 不同产物 |
| `b5793508` | 六/日 19:30 | 元数据映射 + 部署 | 与你 E 档错开 1.5h |
| `9ee4a617` | 23:30 | 只读审计 | 无写入 |
| ~~`60e1dbfd`~~ | — | — | **已 PAUSED** |

- `90b7ea6e`（我 09-08 夜班建的监控）validUntil `2026-09-09 07:00` → **已自动失效**。
- 本机**无任何**针对 `factor_lab_gen.py` 的定时直跑。

---

## 5. 长期铁律（跨会话，勿覆盖）

1. **数据回滚 P0**：跑批带 `V8_PUSH=1`（链尾只推 raw），由云端 build 统一生成 data + 刷 `?v`。只推 `data/*.js` 不推 raw → 云端用旧 raw 重新生成，新数据被"洗"回旧版。
   - 你 08:22 的 `d36ecb9ff`（update_v8.py 云端 build 护栏：data 比 raw 新则跳过覆盖）我已知悉，与我的恢复不冲突（12 文件 raw/data 同版本 09-08 21:0x）。
2. **盘前不清空 + 不打标记**（§1.1，同类误伤已第 6 次）。
3. **RPS 30 天样本考核保护链**：`v8/backtest_rps.py`、`raw_data/rps_backtest.json`、`raw_data/history/stock_rps_*.json`、`data/STOCK_RPS.js` —— 考核约 2026-10 中截止，**勿当孤儿误删**（RPS 不参与 generate_top10 评分，纯观测层）。
4. **阿狸咪自动化禁止删除**，只能 PAUSED。

---

## 6. 今晚验收清单（给我或你都可验）

| 时点 | 验收项 | 期望 |
|---|---|---|
| 21:05 | E 档派发 | `v8/factor_lab_gen.py` 先跑，`factor_lab_backtest.py` 后跑，`v8/backtest_crds.py` 首次产出 |
| 21:05 后 | 因子实验室 红灯 | 转绿（FACTOR_LAB.js 出现今日 update_time） |
| 21:05 后 | BACKTEST_COMPREHENSIVE | 转绿 |
| 21:05 后 | CRDS_BACKTEST | 转绿（09-09 新挂链，首次产出） |
| 23:50 | 你的只读验收 | 上述 3 项复核 |

---

**本报告仅供研究参考，不构成投资建议。**
