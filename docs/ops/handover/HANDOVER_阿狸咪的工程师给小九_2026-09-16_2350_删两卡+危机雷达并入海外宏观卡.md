# 交接：删两卡 + 危机雷达并入「海外宏观」卡（前端卡片合并）

> **发件**：阿狸咪的工程师（家用机 `alimi-cn`）
> **收件**：小九的工程师（单位机 `lemoncat-cn`）
> **时间**：2026-09-16 23:50 CST
> **主人令**：2026-09-16 23:08 —「那如果把危机雷达和这卡合并不就得了，删这2卡」（附两张截图：① 我对「海外宏观 + AI 大周期」卡的口径批评 ② 「市场温度计」「六维雷达」两个卡头）
> **推送**：`62c2f409bdbba42fbedd40c23c9eae4fe0497c8e`（父 `5227665b7f1f`，2 文件）
> **线上**：已生效（硬抓 Pages `HTTP=200` / 1228935 B）
> **📌 命名更正**：本件首次推送时名为 `…_2026-09-17_0130_…`（我误信了会话注入时钟；当时真实时间为 **2026-09-16 23:47 CST** —— 本机 `date` 与 GitHub/百度/Cloudflare 三个外部 `Date` 头一致口径）⇒ 本次更正为 `…_2026-09-16_2350_…`，**正文内容一字未改**。若你已读过 0130 那份，无需重读，仅知悉此更正即可。

---

## 一、先说我做了什么（一句话）

把「📊 危机雷达」的**硬指标**并进「🌐 新周期判定 · 海外宏观 + AI 大周期」卡（新增「📊 宏观硬指标 · 危机雷达（尾部风险）」区），
删掉「🌡️ 市场温度计 · 分项」「🕸️ 六维共振雷达 · 战术层」两卡，并把那句**凭空添加**的假口径删掉。

---

## 二、事实核查（处置前先查清，全部有据）

| 实体 | 真实位置 | 数据源（远端真值） |
|---|---|---|
| 🌡️ 市场温度计 · 分项 | `renderDelisted` →「🔒 已下架 → 🌐 下架雷达组」3 列网格第 1 格（宿主 `#phThermoBody`） | `SH_FIB` — `update_time 2026-09-16 18:23` ✅ 新鲜 |
| 🕸️ 六维共振雷达 · 战术层 | 同上第 2 格（宿主 `#radar` SVG，`renderSixDim`） | `SH_FIB` + `CRISIS_DATA` ✅ 新鲜 |
| 📊 危机雷达 | 同上第 3 格（宿主 `#phCrisisBody`，`renderCrisis`） | `CRISIS_DATA` — `2026-09-16 18:53:50` ✅ 新鲜 |
| 🌐 新周期判定 · 海外宏观 + AI 大周期 | 「🔒 暂未上架 → 📊 观测类」最下方（宿主 `#baihechouBody`，`__renderNewCycleJudge`） | `BAIHECHOU_MACRO` — `2026-09-16 06:30:54` ✅ |

**三卡数据源都新鲜** ⇒ 不是「卡空了所以删」，是主人主动精简。

### 🔴 那句「凭空加的系统」——证据

原卡数据源行写着：

```
· 自建宏观风险状态机（30Y 美债 / SOX / VIX / IG-HY 信用利差 / 美元日元 / 实际利率）
```

而 `window.CRISIS_DATA` 的**实现层原文**是：

```json
"global": {"cat":"全球","score":0.432,"desc":"全球风险情绪=基于VIX(17.0)+美债10Y(5.00%)动态计算"}
"note":"经济维度=中国PMI真实值(49.4)；货币维度=中国银行USD/CNY中间价(6.7628)；全球维度=基于MACRO_DATA的VIX(17.0)+美债10Y(5.00%)日级数据动态计算"
```

⇒ 真实只有 **VIX + 美债 10Y**，**没有** 30Y 美债 / SOX / IG-HY 利差 / 美元日元 / 实际利率。
⇒ 该串与逻辑页（「六维共振雷达 - 说明」里的全球维度算法）**相互矛盾** ⇒ 已删。

---

## 三、改动面（2 文件 / 17 处）

### `index.html`（-9 处，1228935 B；行尾全 LF）

| # | 位置 | 动作 |
|---|---|---|
| 1 | L8294 | 「六维雷达 SVG」CSS 注释 → 改为「已删说明」 |
| 2 | L9960-10157 | **删** `renderThermo` / `renderSixDim` / `renderCrisis` 三函数整段（198 行）→ 留 9 行说明注释 |
| 3 | L11597-11604 | **删** 三函数的 `window.*` 暴露（8 行）→ 留 3 行注释 |
| 4 | L11902-11904 | **删** 运维矩阵表「盘后数据」组「市场温度计」「六维共振雷达」两条 |
| 5 | L11925 | **删** 同表「危机雷达」条 |
| 6 | L13146-13148 | **删** 盘后数据页 `initPostMarket` 里三处 `safe()` 调用（宿主在已下架页 ⇒ **恒早退的死调用**） |
| 7 | L14555 / L14557 | **改** 数据源行：删假口径串、改标真值（`宏观硬指标 window.CRISIS_DATA（货币=USD/CNY 中间价 / 经济=中国制造业 PMI / 全球=VIX + 美债10Y 动态计算）`） |
| 8 | L14561 | **增** ②b 宏观硬指标区（37 行 JS，综合分 + 货币/经济/全球三维条 + `note` 口径原文 + `update_time`） |
| 9 | L15377-15393 / L15443-15445 | **删** 已下架页「🌐 下架雷达组」3 列网格（17 行）与 `renderDelisted` 内 3 处调用 |

### `logic.html`（-8 处 + 1 子串，718386 B；行尾全 CRLF）

同构删除（三函数 / 暴露 / 矩阵两条 + 危机雷达条 / 雷达组网格 / 三处调用），另：

- 「🌡️ 市场温度计 - 说明」整卡**删除** → 留注释
- 「🕸️ 六维共振雷达 - 说明」→ **改写为「📊 宏观硬指标 · 危机雷达 - 说明」**，删「六个维度」段，
  **保留全部危机雷达算法正文**（算法/全球维度动态化/分档表/每日洞察红黄绿灯/指标危险分，一字未改）
- 「🔒 暂未上架 · 📊 观测类」段落补 2026-09-16 主人令（第二批）说明

### 合并卡硬指标区口径（与旧卡逐字一致）

```
综合危机指数 = 货币×40% + 经济×35% + 全球×25%
分档 ≥70 危险 / ≥50 警惕 / ≥30 关注 / 其余 安全
展示复用 .ph-crisis-index / -cats / -row / -bar 四个类（与已下架页同款条形样式，故 CSS 未删）
空态铁律：CRISIS_DATA 未注入 / score 缺失 ⇒ 如实空态，不填 0、不编造
```

---

## 四、门禁证据（全部可复现）

| 门禁 | 结果 |
|---|---|
| 补丁**逆替换逐字节还原** == 原文件 | ✅（两文件） |
| **删除面 22 项代码指纹**（三函数名 / `window.*` 暴露 / 6 个 DOM id / 3 条渲染告警文案 / 3 条矩阵登记 / 假口径串）两文件 0 命中 | ✅ |
| **新增面**：`class="ph-crisis-index"`、`宏观硬指标 · 危机雷达（尾部风险）`、`宏观硬指标 window.CRISIS_DATA` 各 1 命中 | ✅ |
| **行尾谱纯净** | index 全 LF ✅ / logic 全 CRLF（8447/8447）✅ |
| **inline script 语法**：`new Function()` × 28（index）+ 6（logic） | **0 错** ✅ |
| **功能级**：真值渲染综合分 **33**（后端 `score=32.9`，行内公式复算自洽）；空值 / 半空值均走**如实空态** | ✅ |
| **改动面 diff**：我改前那版 → 我的产物 = **11 个差异块（= 我 9 处编辑，2 处被拆块），零额外差异** | ✅ |
| **未覆盖他人改动**：现 `main` 版 vs 我的产物 = 38 差异块，**非 `?v` 差异 = 0 行** | ✅ |

### 🔴 一个必须记下的插曲（防覆盖机制真的救了一次）

`push_merge.py` 第 1 轮判基线时报 `index.html 远端=149fb8410d00 / 基线=acbd60e4a55a ❌ 已变`，
脚本**自动重下 + 重打补丁**后才推。事后查证：**真凶是 23:33 的 `v8-cache-bot` 缓存戳对齐提交**
（`chore: 缓存戳实时对齐 ?v → 防覆盖`，等长替换 `?v=` 秒数 ⇒ 体积不变 1238192 B，极易误判成"没变"）。
⇒ **教训**：判基线**只能比 blob sha，绝不能比文件体积**。

---

## 五、数据层零改动（只删卡壳、不删源）

- **`SH_FIB` 未删** — 仍由「🧪 斐波那契F观测窗」（观测平台）消费；艾略特波浪卡借其 `mode` 字段
- **`CRISIS_DATA` 未删** — 仍由**每日洞察（八路汇合卡）**与**情绪周期卡**消费
- 未动任何生成器、未动 `data/*.js`、未动 `.github/workflows/**`

---

## 六、登记项（未做，交主人/你定夺）

1. 🔴 **`v8_health_check.py` 的 `CARD_DEFS` 里 `SH_FIB` 的显示名仍是「市场温度计」**（L117），
   而该卡已删 ⇒ 健康面板会出现「已不存在的卡名」。
   考虑过改为「斐波那契基准（SH_FIB）」+ `page` 由「盘后数据」改「观测平台」，
   但这会连带牵动 `index.html` 的 `_PAGE_CARD_ORDER`（L12553 `'盘后数据':['SH_FIB',…]`）与逻辑页对齐，
   **属独立课题** ⇒ 本轮**未动**，登记待办。**CRISIS_DATA 的「危机雷达」名保留**（合并卡内该模块仍叫危机雷达，名副其实）。
2. `.ph-crisis-*` 四个 CSS 类**刻意保留**（合并卡复用），不要当死代码清掉。
3. 「🔒 已下架」页原雷达组位置现为**注释占位**（该页「成交金额」「涨跌家数」两卡保留不动）。

---

## 七、给小九的对齐事项

1. 我改的是 **`index.html` + `logic.html`** 两个文件，**没有**动 `v8_health_check.py`、**没有**动任何 workflow。
   若你手上有基于旧版 `renderDelisted` 的改动，请以 `62c2f409` 为基线。
2. 「🌐 新周期判定」卡的渲染入口仍只有 `window.__renderNewCycleJudge`（别名 `__renderBaihechou` 保留）；
   硬指标区是**卡内 ②b 段内联 IIFE**，不吃新数据源、不新增文件 ⇒ 无需你侧配置。
3. 如果后续要让硬指标区**独立成数据文件**（而不是读 `CRISIS_DATA`），告诉我，我再改。
4. 今晚 22:43 我推的 `1971a6e88`（D6/D7/D8 三处链路缺陷修复）**未被本次改动触碰** —
   已逐字节确认四个代码文件（yml / run_algorithms / strategy_four_volume / v8_health_check）与我的补丁产物一致。

---

## 八、我这边接下来的动作

- 盯 `62c2f409` 触发的 `构建部署` CI 结果（15:41:24 起跑，in_progress）→ 若红，立即定位并回报，**不掩盖**
- 下次晨间验收任务（09:45 那笔只读任务）会一并复核本卡在**浏览器里的真实渲染**（硬抓 HTML 已验，DOM 渲染待浏览器实测）

*（本交接单由阿狸咪的工程师自动生成 · 事实均来自远端 main 真身 blob / Pages 硬抓 / 本地门禁输出，无推测项）*
