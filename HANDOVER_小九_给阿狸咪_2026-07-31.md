# 九宝量化 v8.0 · 周末交接（小九 → 阿狸咪）2026-07-31

## 一、在家怎么看（最重要）
- 线上站（GitHub Pages，自动更新，**无需部署**）：**https://ah-quant999.github.io/quant-scanner-v8/**
- 硬刷新（Ctrl+F5 / Cmd+Shift+R）拿最新；每张卡右上角有时间戳，看一眼就知道数据新不新。
- 全站是**事件驱动自动流水线**，小九不在也能自己跑：

```
[中国机器 cn runner] ──抓取中国源──▶  push raw_data/ 到 main
        │
        ▼  (main 上 raw_data/** 一变动就自动触发)
[GitHub ubuntu] v8_build_deploy ──update_v8.py 转 data/*.js──▶ push data/ ──▶ Pages 重建
```

## 二、今天修好的 4 个数据源（之前一直 ConnectionError）
根因：东方财富**实时 host**（push2 / push2his）在本机与 runner 网络下被 WAF 以 TCP 重置拒绝。
已改用**延迟镜像 `push2delay.eastmoney.com`** 直连（延迟仅数秒，对日频排名无影响）。

| 卡 | 状态 | 说明 |
|---|---|---|
| 板块资金流（行业+概念） | ✅ 真实 | 主力净流入 TOP，由 `renderSector` 渲染流入/流出 |
| 概念排名 | ✅ 真实数据已落地 | 暂未绑定渲染卡（死数据，后续可加卡展示） |
| 个股主力资金流向 TOP | ✅ 真实 | 原"四路席位"卡无源，已改为"个股主力净流入/流出 TOP20 + 全市场净额" |
| 大盘资金流时间轴 | ⚠️ 今日单点 | 日K线源被限流，改用"全市场个股 f62 求和"得**今日值**；历史序列待日后累积 |

## 三、cn runner 定时（CST，已写入 `v8_cn_fetch.yml`）
`08:25 / 10:30 / 11:30 / 14:00 / 15:05 / 16:30` + 手动 dispatch。
每次抓完 → 自动触发 `v8_build_deploy`，约 1~2 分钟内全站刷新。

## 四、阿狸咪能 / 不能做什么（v8 新架构）
- ✅ **可以**：直接改 `index.html`(模板) / `cloud_fetch_v8.py`(抓取) / `update_v8.py`(转换) 并 `commit+push` 到 `main`。
  - 改 `index.html` → Pages **自动重建**（等于部署）；改抓取/转换 → 下一轮 cn fetch 自动生效。
- ⛔ **不要手动跑** `deploy_v8.py` / `api_push_raw.py` 这类旧脚本——新架构全自动，手动跑反而可能冲突。
- 若某卡数据 stale：先看卡上时间戳；若超过 1 小时且非周末休市，多半是 cn runner 休眠，等下一档定时，或手动 dispatch `v8_cn_fetch` 工作流。

## 五、已知待补（非阻塞，周末不影响看盘）
- 大盘资金流历史序列（待东财镜像放开 daykline，或靠每日累积）。
- 概念排名卡未渲染（数据已有，无展示卡）。
- 北向资金：港交所 2024-05 起停披露，卡显"停止"。
- 龙虎榜席位"四路资金"原始口径暂无源（当前用个股主力口径替代）。

## 六、紧急处置
- 站点空白/炸了：先 Ctrl+F5；仍空白多半是 `index.html` 改错 → `git revert` 该文件到上一版即可。
- 数据全停：查 `v8_cn_fetch` 与 `v8_build_deploy` 两个 Actions 最近运行是否失败。
- 小九周一回来看 `git log` 即可追溯周末所有改动。
