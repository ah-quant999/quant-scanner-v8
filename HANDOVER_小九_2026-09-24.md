# 交接文档 · 小九 → 阿狸咪 · 2026-09-24

> **更新于 2026-09-24 12:22（小九 12:00 槽位自动更新）**
> 本档由小九每日 **12:00 / 18:00** 两槽位自动生成/整份覆盖。
> 权威状态源仍是仓内 `docs/ops/HANDOFF.yaml`；本档只做**当日增量交接**。
> 所有 SHA 均为本轮**现取**（`git log --oneline origin/main` + 对象存在性复核），**未使用记忆值**。

---

## 一、背景与时窗

| 项目 | 值 |
|---|---|
| 交易日 | **2026-09-24（周四，A 股正常交易日）** |
| 当前时窗 | 小九白班 7:45–17:45 独家；**12:22 处盘中区间**（红线写仓窗口 09:15–11:30 / **12:45–15:00**） |
| 本轮推送范围 | **仅仓根本交接档**（非业务文件），不触碰 `index.html` ⇒ 符合盘中纪律 |
| 远端 tip（现取） | **`25604fb59`**（2026-09-24 12:16） |
| 今日 main 提交 | **22 条** |
| 本机 HEAD | `96de66877`（＝今日 11:37 小九 ETF 根治提交） |
| 本机工作区 | **18 项 WIP，全程零触碰**；暂存位 0 |

**ff-only**：本轮 `git fetch origin` **成功**（SSH DNS 今日恢复），`git merge --ff-only origin/main` 因本地 9 个已修改文件（含 `raw_data/_peer_monitor_state.json` / `ai_market_brief.json`）**原子中止**（非 unrelated-histories）⇒ 零副作用，改走**净室隔离推送**。

---

## 二、今日已完成改动（真实 SHA + 影响）

### 2.1 🔴 头号：ETF 数据链死锁「一劳永逸」根治 — commit `96de66877`（11:37，小九）

- **根因**：东财对单 IP 限流；请求层 `_etf_get_page` **零重试**，`em_clist` 仅**同 host 退避重试**（实测无效）⇒ 单轮 17 页只拿 1~8 页 ⇒ ETF 快照跨轮累积**永远拼不齐（13/17）** ⇒ 返回 `None` ⇒
  **`ETF_DAILY_MONITOR` / `ETF_PULSE` 双双停摆 23.6h**；`AVG_PRICE_DATA`(20.3h) / `CONCEPT_RANKING`(19.4h) 同因 `em_clist` 受困。
- **修法（4 处 · `cloud_fetch_v8.py`）**：① 新增 `_EM_HOSTS`/`_ETF_HOSTS` **14-host 池**；② `_em_get_with_retry` 改**换 host 重试**（退避 0.4/0.8/1.2s）；③ `em_clist` 传 `hosts=_EM_HOSTS`；④ `_etf_get_page` 单页**换 host 重试**（最多 4 host，每次刷新 `params["_"]` 防缓存命中故障节点）。
- **本地真跑验证**：`_etf_snapshot_inner` 从空缓存**单轮拼齐 17/17 页 / 1625 只 / 17s**（旧版 13/17 → 零产出）；对照实验换 host 15/17 vs 同 host 1/17。
- **✅ 上线后实测转绿**（本轮 12:17 线上 HEALTH_CHECK 复核）：`ETF_PULSE` / `ETF_DAILY_MONITOR` / `AVG_PRICE_DATA` / `CONCEPT_RANKING` / `STOCK_QUOTE` **全部 fail → ok**。

### 2.2 同源加固：个股资金榜换 host 抓取 — `eaa357f09`（12:12）
`增强：个股资金榜抓取 3x 换 host 退避重试 + 今日最近成功批次兜底，根治推荐关注空白（不把旧版当新版）`

### 2.3 AI 速览两处陈旧/降级盲区根治 — `7d0fe5df7`（12:03）
`根治 AI 速览「ETF 资金贴昨日值 + 推荐关注消失」两处陈旧/降级盲区（净室实证 11 项全绿）`

### 2.4 landcheck 东财源组判红 → 接小九 cn 兜底 — `29ab26b9f`（11:58）
`fix: landcheck 东财源组判红接小九 cn 兜底 + 亚太概览 null 防 0.00 + logic 口径勘误 + HANDOFF 登记（东财系盘中停摆根治）`

### 2.5 B 组「开盘前收口」：`v8_health_check.py` P1 + P2① — `6cdb9dc19`（09:02 提交并推送，09:06 实测为远端 tip）
- **P1**：`check_all_data_files()` 的 `all_*` 动态项 `heal_cat` 由硬编码 `algo_run` 改为按 `_POST_CLOSE_DATA_FILES` 动态判定 `post_close`/`algo_run` ⇒ 根治「两卡永不复原」。
- **P2①**：`algo_run` 硬窗口 `16:00-21:30` → **`16:00-20:30`**；`post_close` 路由新增**窗口外白名单单次补跑**（保留 `HEAL_DEBOUNCE_MIN=25`）。
- **②③ 主动挂起**（交阿狸咪夜间窗口/裁决）：② 派发源 6 连发→单发（涉 9 个 workflow）；③ `build_deploy` step13 解耦（高风险部署重写）。
  理由：开盘前窗口不足以稳妥验证，按铁律**不裸推未验证高风险改动**。

### 2.6 HANDOFF 台账收口 — `b3841dabf`（11:44）+ 本地待推
- `b3841dabf`：`etf-monitor-clist-fid-invalid` status **pending-verify → done**（七版 ETF 拼页终验通过）。
- 本地已落盘（**午休档推送**）：`postclose-reliability-trio` status `todo → in_progress` + 追加 `xiaojiu_handoff_20260924` note；`recalc_ledger` **校准通过（75 项一致）+ 哈希重算**。

### 2.7 阿狸咪侧今日来档（小九已逐项内容级复核 + 回执）
| 来档 | 内容 | 小九复核 |
|---|---|---|
| `0144` | 删除一进二候选池卡 | R1–R7 全落地 ✅ |
| `0249` | 错名二犯根治 + 三项裁决定案 + E 路径推送事故自纠 | 三项独立复核通过 ✅ |
| `0301` | 夜间对齐闭环 + 授权 A 组/B 组变更 | 落点实证 + 一处口径纠正 ✅ |

小九回执 3 份（`2026-09-24_0045` / `0249` / `0457`），均已推 main（含逐字节自证）。今日 handover 目录 **6 档**。

---

## 三、当前系统状态（现取，2026-09-24 12:1x–12:2x CST）

| 项目 | 读数 | 判定 |
|---|---|---|
| 主站 `index.html` | HTTP **200** / **1,358,890 B** | ✅ |
| `logic.html` | HTTP **200** | ✅ |
| 远端 tip | `25604fb59` | ✅ |
| 今日 main 提交 | 22 | 正常 |
| **HEALTH_CHECK** | `2026-09-24 12:17:39` **overall=warn** `ok117 / warn0 / fail2 / limited2 / total121` | 🟡 |
| fail 明细 | `all_EXPERIMENT`(09-22 18:13:35 · 2524min) ＋ `all_SECTOR_LEADERS`(09-22 18:15:13 · 2522min) | 见 §四.1 |
| limited 明细 | `UNLISTED_PANEL`（静态索引·非故障）／`guanlan_material`（无次生证据·降级） | 非故障 |
| runner `lemoncat-cn` | **online** / busy=False | ✅ |
| runner `alimi-cn` | **online** / busy=False | ✅ |
| `raw_data/runner_status.json` | `update_time 2026-09-24 12:13:31` / `status=ok` / `listener=true` / `consecutive_failures=0` | ✅ |
| 自动化 | **ACTIVE 133 / PAUSED 24**；今日已跑 **42** 个任务、近 1 小时 **22** 个 | ✅ 无停摆 |
| 邮件告警 | 本轮无「不能自愈且当前无法解决」项 ⇒ **未发邮件** | — |

> **口径声明（遵铁律）**：任务活跃度用 `automation_runtime_state.last_run_at`（现取最新 `2026-09-24 12:16:05`）**＋** 必要时的 `automation_runs` 流水；
> **未使用**已废弃的 `automations.last_run_at`，**未**对 13 位毫秒戳做字符串比较，**未**把单字段读数推广为「子系统死活」。
> （本轮 `automation_runs` 按 ISO 串查 09-24 得 0 条，属该表毫秒戳口径问题，**不据此判「没跑」**。）

---

## 四、需阿狸咪注意 / 待办 / 风险

### 4.1 🔴 fail2 未收敛（第 8+ 轮，属**生产者层**，非 runner 层）
`EXPERIMENT` / `SECTOR_LEADERS` 停 **2026-09-22 18:13:35 / 18:15:13**（≈42h）。走 `algo_run` 链，**今日 ETF 盘中链根治未覆盖它**。
经验：判 `v8_algo_cloud` 故障须先取 `runner_name`（近期该链 #2075=lemoncat-cn、#2076/#2077=alimi-cn），**不得默认归小九机**。

### 4.2 待施工：B 组 ②③（高风险，盘中禁写）
- ② 派发源 **6 连发 → 单发**（涉 9 个 workflow）
- ③ `build_deploy` step13 解耦（部署重写）
- 硬性要求：**漂移断言升为前置**。

### 4.3 🔴 红线潜伏项（待主人裁，勿擅动）
1. `v8_runner_guard.py` L96-100 `TOKEN_PATHS` **第 3 项仍是坚果云 junction 废路径** `E:/workspace/stock-scanner/.workbuddy/v8_gh_token.txt` ⇒ 直接违反 09-04 事故红线；当前因前两项存在而未命中，属**潜伏**。建议**删项**（token 正本＝`~/.workbuddy/v8_gh_token.txt`）。
2. 今日 00:53 本机发生**一次凭据暴露**：`python -c` 双引号内反引号被 bash 当命令替换执行，回显某 PAT 首行 ⇒ 建议**轮换该 PAT** 并同步仓内与 `qs8-tmp/` 两份副本。

### 4.4 跟踪项 C：`build_deploy` step13 `📤 部署到 main` 间歇 failure
根因：concurrency 组名含 `${{ github.event_name }}` ⇒ `push` 与 `workflow_run` **分属不同组、不互斥** ⇒ 抢推必有一败（**非结构性**；11:10:53 已有 success，11:37 后近 12 条十连 success）⇒ 仍判「观察中」。

### 4.5 其它登记项（勿擅动）
- 本机 `_work` 实测 **25851 文件 / 11G**（历史台账 121 文件 / 6.1MB，口径不一致，待主人裁；SAFE_DELETE 命中 0 ⇒ 未清空）。
- 自动化触发间隔实测 **63–80 min ≠ 标称 20 min**（老账）。
- 本机分叉清理备份留 `E:/v8data/qs8-tmp/repo_trash_2026-09-24/`（内含 `board_first_limit.py +398 行` 地雷），**待主人裁决保留/清理**。
- `board_first` 若确认退役须**三处联动**（删 cn_fetch step ＋ 删 `V8_SF_board_first` env ＋ 从 `for sid` 列表移除）＋ 删 fetcher ＋ 刷 ledger。**若只删前端卡，fetcher 会经 `git status` 增量推送把它回灌上线。**
- `index.html` 本地版本**既 ≠ HEAD 也 ≠ origin** ⇒ **红线，只登记不擅动**。

---

## 五、小九监督职责（今日余下时段）
1. **盘中高频监控已挂自动化**：`v8盘中高频更新监控(小九)`（ACTIVE，交易日 09/10/11/13/14 点每 15 分）—— 取线上 `FRESHNESS_STATUS.js` 权威 intraday 清单，仅对 **8 个高频文件**严格判定，日级/盘后定稿文件只快照不误报；异常才告警，正常静默写日志。
2. **盯 ETF 三件套自愈落地**：代码已根治，数据刷新依赖云端下一轮 run（本机无 API 通道触发）⇒ 观测 `ETF_PULSE` / `ETF_DAILY_MONITOR` 的 `update_time` 是否续更。
3. **午休档推送**：`HANDOFF` trio 交接件 + `postclose-reliability-trio` 台账更新（本地 stash 中）。
4. **18:00 槽位**：再出一版交接，汇总下午改动。

---

## 六、本轮方法论沉淀（新增纪律，防复发）

1. **`/tmp` 双解歧义**：MSYS `curl` 落 `/tmp` → `C:\Users\...\Temp`；**原生 Windows Python** 落**当前盘** `E:\tmp` ⇒ 同一路径两程序读到**不同文件**，会静默拿到上轮陈旧副本。**本机一切取数落盘禁用 `/tmp`，一律用 `E:/v8data/qs8-tmp/` 绝对路径**；卡值与前轮矛盾时先重取复核。
2. **`grep -F` 计 emoji / 非 BMP 字符恒假 0**（Windows/MSYS UTF-8 多字节模式静默失配）⇒ 一律 Python 逐码点计数；「处数」**必须声明是行数还是出现次数**。
3. **`git rev-parse "origin/main:<path>"` 在 Git Bash 会被 MSYS 路径转换破坏**（解出字面量 `origin\main;...`）⇒ 取远端 blob 一律 `git ls-tree origin/main -- "<path>" | awk '{print $3}'`。
4. **Contents API 对 >1MB 文件默认 `Accept` 返回 `content=""`** ⇒ b64decode 得 0 字节 / md5 空串 ⇒ 必须 `Accept: application/vnd.github.raw`。**取回 0 字节先疑取数通道，勿先疑产物。**
5. **`contents` 端点对 >1MB 文件会静默截断** ⇒ 大文件走 `git/blobs/{sha}` + **全长断言 `len==size`**。
6. **软失败汇总步把 step id 硬编码两处**（env 映射 ＋ `for sid in` 循环列表）⇒ 删任何带 `continue-on-error` 的 step **必须三处联动**，否则留悬空引用。
7. **`run.updated_at` 只在 job 边界更新** ⇒ 判僵尸必须 job 级多轮 `stepAge`；**`sha 恒定` 不可作防覆盖判据**（`runner_status.json` 被 `v8_cn_fetch_cloud` 每 ~10min 双写、且用 minified 序列化 ⇒ 须回**字段级**判定）；**排队中 run 的 `run_started_at == created_at`** ⇒ 执行龄须用 `job.started_at` 校正。
8. **`index.html` 的「WIP」多半是构建戳 / `?v` / CRLF 噪声** ⇒ 先用 `git diff --ignore-cr-at-eol` 核实再定性。
9. **`porcelain 非空 ≠ 有 WIP 待同步`** ⇒ 凡 porcelain 非空必先与 `origin/main` blob 直算再定性。
10. **GitHub 侧 vs 本机侧必须分离归因**：`v8_build_deploy` / `v8_cn_fetch_intraday_*` 的触发失败、排队积压均属**云端**，**不得默认归本机 runner**。

---

## 七、现取命令（本档**不写死自身 SHA**，`main` 被 CI 逐分钟 force-update，短 sha 分钟级失效）

```bash
# 远端 tip（唯一权威）
TOK=$(cat ~/.workbuddy/v8_gh_token.txt | tr -d '\r\n')
git ls-remote origin refs/heads/main

# 本档是否已上线 + 字节数
curl -s -H "Authorization: token $TOK" -H "Accept: application/vnd.github+json" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/HANDOVER_小九_2026-09-24.md?ref=main&_cb=$(date +%s)" \
  | python -c "import sys,json;d=json.load(sys.stdin);print(d['sha'],d['size'])"
```

---
*生成：小九自动化「交接文档自动更新」· 12:00 槽位 · 全程零触碰主工作树 WIP（18 项）· 净室隔离推送*
