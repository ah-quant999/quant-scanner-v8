# URGENT：小九机今日盘中+盘后分工问题清单（2026-09-10 09:46 阿狸咪自查）

> **场景**：主人 09:06 让阿狸咪趁盘中空闲「全面审核阿狸咪机今天开始开启的盘中检查兜底和盘后算法链主力的每一个任务有否问题，做不到的趁早列出原因和清单和小九紧急交接」「可以先查，等休市或收盘后再改，才不会踩踏和风暴」
>
> **现状**：今天已开盘 15+ 分钟，本机所有 4 项盘中检查兜底自动化 ACTIVE 中；已盘后数据全齐；小九机 15:00–17:20 按今早交接单下线、17:25 必回跑 17:30 龙虎榜、18:00 必在线跑算法链。
>
> **本轮原则**：盘中只查不改，所有问题列清楚待 11:30 休市后逐项处置。

---

## 一、本机 4 项盘中检查兜底（ACTIVE）

| ID | 名称 | cron | 现状 | 风险/待办 |
|---|---|---|---|---|
| `94279c7c` | v8 盘中巡检（每30分钟·防踩踏风暴） | 每小时 0/30 分 | ACTIVE（08:58 上线） | ✅ 已修自身 3 个 bug；不派发、只诊断+告警 |
| `automation-1787141452867` | 阿狸咪-双机分时主力/兜底按铃 | 每小时 30 分 | ACTIVE | 跑 `scripts/alimi_bell.py`（窗口判断在脚本内）—— 主人截图已明示「30min 主力」即此 |
| `5edd92d6` | 阿狸咪-小九心跳监控+自动接管 | 每小时 0/30 分 | ACTIVE | 跑 `v8_peer_monitor.py`，**掉线自动 dispatch `v8_safety_net.yml` + `v8_self_heal.yml`** |
| `50eda171` | 阿狸咪-算法链暴风盯梢 | 每小时 5 分 | ACTIVE | ✅ 只读监控暴风+告警 |

---

## 二、本机 3 项盘后算法链主力（ACTIVE）

| ID | 名称 | cron | 现状 | 风险/待办 |
|---|---|---|---|---|
| `90b7ea6e` | 阿狸咪·算法链夜班监控与小九交接巡查 | 每小时 0 分 | ACTIVE（18-23+6 点窗口） | **⚠️ 见下 C1** |
| `6d16d809` | 阿狸咪-本机算法链完成监控+自动推送 | 每小时 15 分 | ACTIVE | **⚠️ 见下 C2** |
| `a8c1662a` | 九宝量化-小九交接汇报18:00 | 每日 18:00 | ACTIVE | ✅ 只汇报 |

另：`53726469` 「九宝量化-盘后最终闸(21:30·强制补跑)」**当前 PAUSED**——今天 E 档（21:00 回测）失败无自动补跑，仅靠 `90b7ea6e` 夜班监控手工 dispatch 兜底。

---

## 三、🚨 紧急高危问题（已逐一实证）

### C1 🚨 盘中准点档调度器 `v8_cn_fetch_intraday_lemoncat.yml` 不在 main 上

- **实证**：
  - 本地 `git ls-tree -r main` 含此文件（commit `1d36f6eb1`）
  - 远端 `git ls-tree -r origin/main` **不含此文件**（HTTP 404）
  - `git fetch origin main` 后 `FETCH_HEAD = fefee3ac9`，workflows 数 = 30（本地 main = 31）
- **影响**：此 yml 设计 cron 触发盘中 14 档准点档（09:40/10:00/.../15:00 CST），**GitHub cron 永不触发**。
- **为什么没触发**：小九今早 09:04 交接单说"已重配 14 档"，但实际从未推到 main（commit `7117ea753` 只改了 `scripts/alimi_bell.py` 32 行，没动 yml）。
- **注释 `align_logic_ops.py` 已承认**：今早 07:29 commit `ff071020d` 「align_logic_ops 将 v8_cn_fetch_intraday_lemoncat 移入 ALLOW_NO_CRON」—— 但 yml **仍不在 main**，注释说"dispatch-only 准点档"是设计，但**没人 dispatch 它**！
- **当前实情**：盘中按铃只有 `automation-1787141452867` 阿狸咪 30min 主力 + `v8_cn_fetch_cloud_selfhosted.yml` 兜底，**没有小九 14 档**。
- **建议（待主人定夺）**：
  - **方案 A**：把 lemoncat yml 推到 main（用 Git Database API + 带 workflow scope 的 token `v8_gh_token.txt`，Contents API 不行），14 档 cron 才能生效
  - **方案 B**：让 `automation-1787141452867` 阿狸咪按铃在盘中 14 个点 dispatch lemoncat yml workflow_dispatch（云端按铃 + lemoncat 接力），保持 dispatch-only 设计

### C2 🚨 小九机"15:00–17:20 可下线"有空窗漏洞

- **实证**：v8_algo_cloud.yml 第 38-41 行 cron：
  - `40 8 * * *` (16:40 CST) A 档采集批
  - `10 10 * * *` (18:10 CST) B 档选股批
  - `0 12 * * *` (20:00 CST) D 档汇总批
  - `0 13 * * *` (21:00 CST) E 档回测批
  - 全部 `runs-on: [self-hosted, cn]` 固定 `lemoncat-cn`（yml L91 注释明示）
- **影响**：
  - 小九 15:00 下线 → **16:40 A 档 cron 触发时 lemoncat-cn 不在线** → queued
  - 17:25 小九回来后 queued run 才会跑 → **A 档产出拖到 18:00 之后**
  - **18:10 B 档选股批的输入缺失/陈旧** → 选股策略全错
- **小九 09:12 修正版只考虑了 17:30 龙虎榜**，**漏算了 16:40 A 档**。
- **建议（待主人定夺）**：
  - **方案 A**：小九机 16:30 必须回来接 A 档（空窗 15:00-16:20 + 17:20-17:25 + 17:30-18:00）
  - **方案 B**：A 档 B 档 D 档 E 档全用云端（去掉 `lemoncat-cn` 限制），但会丢中国 IP 反爬保护
  - **方案 C**：A 档改为云端跑（20 分钟足够），B/D/E 档仍小九机

### C3 🚨 `v8_algo_cloud.yml` 在 lemoncat-cn offline 时无 fallback

- **实证**：v8_algo_cloud.yml L91 `runs-on: [self-hosted, cn]` 固定 `lemoncat-cn`；注释明示"恢复双跑条件：家里机装 Git for Windows 且 runner 服务 PATH 可见 bash 后，移除本 label"
- **影响**：lemoncat-cn 不在线 → 4 个 cron 全部 queued → **算法链全断**（已知 09-09 1621-1628 期间 alimi-cn 单点六连败）
- **探针路由（L434-490）**：在 schedule/dispatch 时探测 lemoncat-cn 状态，**但只能转派到 v8_algo_run.yml（也需 cn runner）**——**没有 fallback 到 ubuntu-latest**！
- **建议（待主人定夺）**：
  - **方案 A**：探针路由 fallback 加 `runs-on: ubuntu-latest` 分支（lemoncat-cn 不在线时降级云端，丢部分中国 IP 特性但保链路不断）
  - **方案 B**：保持现状，但加 `automation 1784797xxx` 监控 `v8_algo_cloud` queued 超 5 分钟自动 dispatch `v8_algo_cloud` workflow_dispatch with `force_run=true`（让云端 runner 接单——但 runs-on 是 cn 会失败！必须先修 yml）

### C4 ⚠️ `90b7ea6e` 夜班监控读 HANDOVER 路径错误

- **实证**：prompt 写「小九交接文档（重读对象）：
  - `/e/workspace/workspace/quant-scanner-v8/HANDOVER_小九_2026-09-08_暗灯处置路线追认.md`
  - `/e/workspace/workspace/quant-scanner-v8/HANDOVER_小九_2026-09-08_紧急_runner排查.md`」
- **问题**：这两个路径是**阿狸咪历史 workspace** 里的另一个 clone，**不是当前工作目录** `/e/workspace/stock-scanner/`！且文件可能早就不存在（这是 09-08 旧路径）。
- **影响**：夜班 18-23 点触发时，找不到文件不报错，但**主路径失效**——小九今早 09:04/09:12 推的 `HANDOVER_小九_2026-09-10_1500-1800空窗期.md` 在 `_handover_20260910/` 下，**这条提示里没写！**
- **建议（待主人定夺）**：
  - **方案 A**：把 prompt 里的路径改为 `/e/workspace/stock-scanner/_handover_*/HANDOVER_小九_*.md` 通配，让 v8_algo_cloud 监控能抓到最新交接
  - **方案 B**：把夜班监控的"小九交接重读"功能删掉（夜班只监控算法链就够）

### C5 ⚠️ 本地 HEAD 与远端 main 严重分裂

- **实证**：
  - 本地 HEAD = `7117ea753`（前晚 09-09 23:16，仅改了 `scripts/alimi_bell.py`）
  - 远端 HEAD = `fefee3ac9`（今早 09:20）
  - 远端领先 7 个 commit：`5393f4c66` V8_OFFLINE 完整实现、`ff071020d` align_logic_ops lemoncat 移出 MUST_HAVE_CRON、`048142909` STOCK_STOP_DATA manual_dep 标注、`241053f3d` HANDOVER_小九_1500-1800空窗期 等
- **影响**：本地工作树 6 项未提交修改（`algorithms/calc_crds.py`、`final_recommend.py`、`verify_data_sanity.py`、`align_logic_ops.py`、`cloud_fetch_v8.py`、`HANDOVER_LOG.jsonl`），**`6d16d809` 自动推 main 时会触发 rebase 冲突**。
- **幸运**：今天 09:15 `6d16d809` 触发时 FINAL_RECOMMEND_DATA.js update_time=`2026-09-10 04:57:09`（不是 prompt 写死的 `2026-09-09`），条件 2 字符串不匹配 → **未自动推 main**（暂时安全）。
- **风险**：今天 18:00+ 算法链跑完后，如果 FINAL_RECOMMEND/TRIPLE_CONSENSUS 写回数据，触发 `6d16d809` → 推 main → rebase 冲突 → **可能把 lemoncat yml（本地 main 有）或 V8_OFFLINE 完整实现（远端有）弄丢**！
- **建议（待主人定夺）**：
  - **方案 A**：今天 11:30 休市后立刻 rebase 本地 main 到 origin/main，然后 push；与远端对齐
  - **方案 B**：先 PAUSE `6d16d809` 到明天，避免今天推送冲突
  - **方案 C**：把 `6d16d809` prompt 改成"先 rebase origin/main 再 add/push"——但 prompt 已经是这样写的，问题在于本地 main 与 origin main 不可 fast-forward（多了 2 个本地 commit）

### C6 ⚠️ `6d16d809` 自动推 main 的 prompt 已陈旧

- **实证**：prompt 写「2. `data/FINAL_RECOMMEND_DATA.js` 的 update_time 变为 `2026-09-09`（今日）」
- **影响**：写死字符串 `2026-09-09`，今天 `2026-09-10` 永远不匹配条件 2 → 只能依赖条件 1/3 触发。
- **条件 3 模糊**：「`data/TRIPLE_CONSENSUS.js` / `data/GOLD_POOL.js` update_time 变今日」——"今日"实现细节未知，可能写死也可能用 datetime.now()。
- **建议（待主人定夺）**：
  - **方案 A**：把 `2026-09-09` 改成 `{{ TODAY }}` 模板字符串，提交时 WorkBuddy 渲染
  - **方案 B**：把完成判定全部改成"update_time 在过去 2 小时内"——鲁棒

### C7 ⚠️ `53726469` 盘后最终闸(21:30·强制补跑) 被 PAUSED

- **实证**：今日 21:30 E 档（回测批）失败无自动补跑，只能靠 `90b7ea6e` 夜班监控手工 dispatch 兜底。
- **影响**：21:00 E 档 cron 触发 → 跑失败 → 21:30 没人补跑 → 09-11 早上开盘前回测数据缺失
- **建议（待主人定夺）**：
  - **方案 A**：立即 ACTIVATE `53726469`，让 21:30 强制补跑
  - **方案 B**：把"21:30 强制补跑"逻辑合并到 `90b7ea6e` 夜班监控里
  - **方案 C**：维持现状，但加 GitHub 监控，E 档失败立即邮件告警（`automation-1784797xxx` 系列已存在？）

---

## 四、今早 09:04 / 09:12 小九两份交接的核对

- **HANDOVER_小九_2026-09-10_1500-1800空窗期.md**：今早 09:04 创建 + 09:12 修正
- **已确认事实**：
  - 15:00–17:20 可下线 ✅
  - 17:25 前必回跑 17:30 龙虎榜 ✅
  - 18:00 必在线跑算法链 ✅
  - 必须小九机 IP 的任务：17:30 龙虎榜、18:10/20:00/21:00 算法链 ✅
- **小九交接单缺失的事实**：
  - ❌ 16:40 A 档采集批 cron 需要 lemoncat-cn（C2 已说）
  - ❌ lemoncat yml 未推到 main（C1 已说）

---

## 五、本轮审计的「办不到」清单（如实告知主人+小九）

1. **本机 PATH 缺 bash/git**（历史教训）：`alimi-cn` runner 跑任何需要 bash 的脚本会 `bash: command not found`，无法补 lemoncat-cn 缺的活。
2. **本机无法对中国 IP 反爬服务做直连**：东财/akshare/baostock/腾讯 K 线都需中国 IP（除 maharo 已删外），本机跑会 ConnectionError 或数据失真。
3. **`v8_algo_run.yml` 也是 `[self-hosted, cn]` 跑**：本机跑不了，只能等小九或云端。
4. **6d16d809 推送前无法确证 lemoncat yml 在 origin main**：必须等小九推完 C1 方案后再能验。

---

## 六、阿狸咪能立刻做（盘中不踩踏）

- ✅ **已做**：每 30 分钟只读巡检（`94279c7c`），有异常立刻告警+写 URGENT
- ✅ **已做**：盘中按铃按 `scripts/alimi_bell.py` 窗口逻辑（`automation-1787141452867`）
- ✅ **已做**：暴风盯梢（`50eda171`，只读）
- ✅ **已做**：小九心跳监控（`5edd92d6`，掉线自动 dispatch rescue）
- ⏸ **盘中不做**：rebase、push、改 workflow、改 prompt——主人令「可以先查，等休市或收盘后再改，才不会踩踏和风暴」

---

## 七、建议的休市后修复优先级（待主人拍板）

| 优先级 | 问题 | 修复方案 | 预计耗时 | 风险 |
|---|---|---|---|---|
| 🔴 P0 | C1 lemoncat yml 不在 main | 推 lemoncat yml 到 main + 改 alimi_bell 派发它 | 30min | 中（须用 workflow scope token） |
| 🔴 P0 | C5 本地 main 与远端分裂 | rebase 本地 main 到 origin/main | 15min | 高（rebase 冲突可能丢 V8_OFFLINE） |
| 🟡 P1 | C2 16:40 空窗漏洞 | 与小九协商把空窗缩为 15:00-16:20 + 17:20-17:25 + 17:30-18:00 | 0（仅改 HANDOVER） | 低 |
| 🟡 P1 | C3 algo_cloud 无 fallback | 探针路由加 ubuntu-latest 分支 | 20min | 中（云端跑会丢 IP 保护但保链路） |
| 🟢 P2 | C4 90b7ea6e HANDOVER 路径 | 改 prompt 路径为 stock-scanner/_handover_* | 5min | 低 |
| 🟢 P2 | C6 6d16d809 prompt 陈旧 | 把硬编码 `2026-09-09` 改为「过去 2 小时内」 | 10min | 低 |
| 🟢 P2 | C7 53726469 PAUSED | ACTIVATE 或合并到夜班监控 | 5min | 低 |

---

## 八、给主人的「办不到清单」（主人问的"做不到要趁早列出原因和清单"）

| 事项 | 原因 | 替代方案 |
|---|---|---|
| 让 lemoncat yml 在云端 cron 触发 | yml 不在 main 上，GitHub 看不到 | 推 yml 到 main（小九/Git Database API），或 alimi_bell 主动 dispatch |
| 让 alimi-cn 跑 v8_algo_run | alimi-cn PATH 缺 bash/git，跑必败 | 等小九机 + 家里机装 Git for Windows |
| 让 final_recommend.py 跳过 V8_OFFLINE 半成品 | 远端 main 已修（5393f4c66），本地 HEAD 落后 | rebase 本地 main 到 origin/main |
| 让盘中 14 档准点档触发 | lemoncat yml 不在 main | C1 方案 A/B |

---

**生成时间**：2026-09-10 09:46 CST ｜ **阿狸咪自查｜请求主人/小九确认**
