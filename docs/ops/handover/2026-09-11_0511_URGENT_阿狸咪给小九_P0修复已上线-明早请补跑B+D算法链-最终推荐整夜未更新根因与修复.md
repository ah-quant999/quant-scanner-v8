# 📋 紧急交接：P0 修复已上线 — 明早请补跑 B+D 算法链

> **发件**：阿狸咪
> **收件**：小九
> **时间**：2026-09-11 05:11 CST
> **级别**：🔴 URGENT
> **提交**：远端 `eca0aa713`（含 P0 fix `a47770208` + 瘦身 `76e54f6c1`）

---

## §0 一句话结论

**最终推荐整夜没更新的真凶找到了**：`final_recommend.py` 和 `calc_crds.py` 用了 `V8_OFFLINE` 变量但**全仓从未定义过它**（疑似某次 rebase 把定义行丢了）→ D 批一跑就 NameError 崩 → 最终推荐永远停在昨天。**已修复并上线**，明早你只需要触发一次算法链，闸门会自动判定跑 B（补 CRDS）→ 再触发一次跑 D（出最终推荐）。

---

## §1 今夜事故时间线（实测）

| 时间 | 事件 | 证据 |
|------|------|------|
| 22:24 | run#1685 派发，跑 B 批（23 支） | GitHub API |
| 22:30 | 我的 logic.html + 闸门卡写入完成并推送 | `73e053358` |
| 23:55 | run#1685 接单（alimi-cn），B 批开始 | runner 心跳 |
| 00:06 | calc_stock_rps.py 卡在东财不可达（HTTP 000），silent_sec 累积 | 心跳 silent_sec=768 |
| 00:21 | calc_stock_rps.py 被监督器 kill（>900s）续跑下一支 | 心跳切换 |
| 00:30 | run#1686 cron 唤醒 → 被 concurrency 排队 | run#1686 pending |
| 02:30+ | 多轮 cron 唤醒 → 全部 cancelled（concurrency 排队溢出） | 11 个 cancelled |
| 03:48 | run#1691 派发，**仍在用旧代码**（HEAD=9517f9049，我的 fix=a47770208 还没被 checkout） | runner workdir git log |
| 04:52 | B 批跑到第 22 支 track_h_auto_buy，silent 425s | 心跳 |
| 04:55 | 我发现 runner 没拿到 fix → P0 修复虽已在 main，但 runner 用的是旧 checkout | `grep V8_OFFLINE` = 空 |
| 05:01 | 我取消 run#1691（旧代码，D 必崩）→ 本机补跑 calc_crds.py | GitHub API cancel HTTP=202 |

---

## §2 P0 修复内容（已上线 `a47770208`）

### 真凶 1：`V8_OFFLINE` 未定义 → NameError

- `algorithms/final_recommend.py` L40 和 `algorithms/calc_crds.py` L49 使用 `V8_OFFLINE`，但全仓 **0 处定义**（grep 确认：2 处使用、0 处定义）
- 疑似 commit `5393f4c66`「V8_OFFLINE 完整实现」在 rebase/合并中把定义行丢了
- 后果：D 批（`final_recommend.py`）一跑就 NameError → `data/FINAL_RECOMMEND_DATA.js` 永远停旧版
- **修复**：两文件各补 `V8_OFFLINE = os.environ.get("V8_OFFLINE", "0") == "1"`（默认在线）

### 真凶 2：`api_push_raw.py` 5xx 不重试 → 算完零推送

- 防倒退守卫的唯一基线来自 `GET /git/trees/{sha}?recursive=1`
- `api()` 的 3 次重试只覆盖网络异常，**HTTPError 一律立即返回**
- GitHub 偶发 500 时守卫 `sys.exit(1)` → 整条链算完却一个字都推不上 main
- **修复**：① api() 对幂等 GET 的 5xx/429 走退避重试；② 新增 `_local_tree_fallback()` — runner 已 checkout 同一 commit，`git ls-tree` 的 blob sha 与 Git Data API 完全等价，守卫基线永不缺失（仍绝不裸推）

### 瘦身提交（`76e54f6c1`，-42.4MB）

- 771 个运行时遗留移出版本跟踪（`git rm --cached`，磁盘文件 100% 保留）
- 根治三条回潮路径：`api_push_raw.py` 目录剪枝 + 两处 `git add -A` 前补 `git rm --cached` + `DO_NOT_DELETE.md` 豁免解析漏洞修复

---

## §3 明早你需要做的（按顺序）

### ① 触发算法链（让闸门自动判）

```bash
# 方法 A：GitHub Actions 页面手动 dispatch（留空 stage = 闸门自动判）
# 方法 B：API dispatch
curl -X POST -H "Authorization: token <PAT>" \
  -H "Accept: application/vnd.github+json" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_cloud.yml/dispatches" \
  -d '{"ref":"main"}'
```

闸门会判定：A 已就绪（数据采集已完成）→ B 未就绪（CRDS 停旧）→ **跑 B 批**

### ② B 批跑完后再触发一次 → 闸门判 B 就绪 → 跑 D 批

D 批 = `final_recommend.py`（单脚本，~5-10 分钟）→ 产出 `data/FINAL_RECOMMEND_DATA.js` → 推送上线

### ③ 验收

```bash
git fetch origin main -q
for f in FINAL_RECOMMEND_DATA CRDS_CARD_DATA TRIPLE_CONSENSUS FOUR_VOLUME; do
  git show FETCH_HEAD:data/$f.js | grep -o 'update_time[^,}]*' | head -1
done
```

四个都应该是 `2026-09-11` 当天的时间戳。

---

## §4 我正在做的

1. ✅ P0 修复已推送上线（`a47770208`）
2. ✅ 已取消旧代码的 run#1691
3. 🔄 本机补跑 `calc_crds.py`（修复版，PID 16616，已跑 11min）→ 产出后推 CRDS 到 main
4. ✅ 瘦身提交已上线（`76e54f6c1`，-42.4MB）
5. ✅ 自动化值守已建（`9119c1d4`，每小时 :40，含缺批自动派发）
6. ⏳ CRDS 推上去后 → 派发新 run → 闸门看到 B 就绪 → 直接跑 D

---

## §5 铁律提醒

- **V8_OFFLINE** 默认 `"0"` = 在线模式。如果你需要离线跑（无外网），设 `V8_OFFLINE=1`
- **推送守卫**：`api_push_raw.py` 现在有 5xx 重试 + 本地兜底，但**绝不裸推**的底线没变
- **瘦身回潮**：`api_push_raw.py::walk_raw()` 现在会剪枝 `__pycache__/_rps_cache/_tdx_cache/_archive` + 过滤 `.bak/.tmp/.pyc`。如果链推完后 `_rps_cache` 又回潮，说明 runner 用了旧代码——请确认 runner 已 fetch 到 `a47770208` 之后的 commit
- **东财不可达**：今夜 `push2his.eastmoney.com` HTTP=000（3 次重测均不可达），`calc_stock_rps.py` 因此被杀。你的机器（lemoncat-cn）如果能连东财，RPS 可以在你的机器上补跑

---

## §6 待你确认

1. **你机 git/bash 是否正常**（我机实测 git 2.55.0.windows.3，东财不可达）
2. **盘中调度器**选 (a) 靠闸门判陈旧度 还是 (b) 专属标签钉死 `xiaojiu`
3. **我压缩 ⏰ 卡时有无漏你的条目**（你原 `⏰ v8 定时任务矩阵` 16329 字符整块保全在仓库外，需要贴回随时说）

---

> 铁律：不许凭印象；每条结论必须带命令或时间戳举证；不确定就写「未查到」。
