# 交接件：主板首板·一进二观察卡「买入判据」实测落地 + 同页口径纠偏

- **发件**：阿狸咪的工程师（家机 `alimi-cn`，夜间窗口）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-23 20:40 CST
- **性质**：纯前端文案/展示改动（1 个文件），**不改数据源、不改算法、不改 workflow**
- **触发**：主人问「所以到底要怎么看可买入？」——上轮卡面只说「非买点」但没给可执行用法，本轮回测补上

---

## 一、本轮改了什么（就 1 个文件）

`index.html`（唯一变更文件；`data/BOARD_FIRST_LIMIT.js` 与 `raw_data/board_first_limit.json` **本轮未变**，故其 `?v` 缓存戳**有意不动**，不制造无意义失效）：

| 位置 | 改动 |
|---|---|
| 卡渲染器 `_guide`「💡 用法」 | 新增每格「量比」说明 + **量比→次日高开概率**（实测） |
| 卡渲染器 `_guide` 新增一行 | **🎯 怎么买（唯一有实测支持的用法）**——三步法，全部为实测数字 |
| 卡渲染器 `_guide`「⚠ 限制」 | 换为**不可成交性证据**（旧「1909 只面板层」口径 → 新「2280 信号」口径） |
| `_cell()` 每格第 3 行 | 新增 **量比**（`流通x亿·换y%·量z`）；量比 ≤0.5 橙色加粗 + 悬停预警 |
| 顶部统计行 | 新增 **「次日易高开 N 只（量比≤0.5，慎追）」** |
| 副标题（含空数据兜底分支） | `· 观察分层` → `· 候选池（非买点）` |
| 迁移说明注释（约 15699 行） | 旧口径 `−0.48%/t=−2.55` → 新实测口径（防同页口径矛盾） |

---

## 二、为什么改：本轮真跑的「买入判据」回测

口径**完全沿用** `E:/_alimi_tools/backtest/bt_yijiner.py` 的 `components()`（公式未改一字），样本 **2280 个严格达标信号**，覆盖 2025-08-22 ~ 2026-09-17，K 线源 = 仓内 `raw_data/kline_cache`（零网络）。

### 2.1 最重要的一张表：把「不可成交」剔掉后真相

| 拆分 | n | T+1（开盘买→当日收盘） | 说明 |
|---|---|---|---|
| 一字板 `one_word` | 97 | 买不到 | T+3 **+9.651%**（t=7.35）——但 `op=cl=hi=lo`，**散户买不进** |
| 盘中封回 `open_then_seal` | 352 | +5.153%（t=22.17） | ⚠️ **未来函数**：`cl==hi`（收盘封板）是**收盘后**才知，不能作为买点依据 |
| 自由成交 `open_free` | 1831 | **−1.450%**（t=−10.88） | 真正可成交的世界 |
| **A. 全部严格达标** | 2280 | −0.382%（t=−1.97） | — |
| **B. 仅「可成交」** | **2183** | **−0.386%（t=−2.04，显著为负）** | 卡面「限制」引用的就是这一行 |

⇒ **卡面全部「看着能赚」的收益，都来自买不到的一字板、或需要收盘才知道的前视样本。** 剔掉后，次日开盘买是显著亏的。

### 2.2 唯一的决策窗口：次日 9:25 竞价（gap）

| 竞价高开 gap | n | T+1 | 按日 t |
|---|---|---|---|
| ≤ −2%（明显低开） | 246 | **+1.051%** | 1.63（未达显著＝弱证据） |
| (−2%, 0] | 671 | −0.261% | −0.63 |
| (0%, 2%] | 532 | −0.001% | −0.15 |
| (2%, 4%] | 331 | −0.355% | −1.07 |
| (4%, 6%] | 171 | −0.406% | −1.10 |
| **(6%, 9.8%]** | 158 | **−3.142%** | **−7.03（铁证）** |
| >9.8%（涨停开） | 171 | −1.582% | −5.77（且 56.1% 全天一字，买不到） |

**规则**：高开 >2% 一律放弃（整体 −1.148%、胜率仅 35.9%）；只有低开才有正期望（弱证据、仅可试仓）。

### 2.3 盘后唯一可用的预警：量比（＝当日量÷昨日量，即卡面/原公式口径）

⚠️ **口径纠偏（重要）**：起初我用「量÷5日均量」跑，与卡面 `read.vol_ratio`（量÷昨量）**不一致**，已按卡面真实口径重跑，结论方向一致但数值以本表为准。

| 量比 | n | 次日 gap 中位 | gap>2% 占比 | 次日直接涨停开 | 次日全天一字 |
|---|---|---|---|---|---|
| **≤0.5** | 92 | **6.93%** | **80.4%** | **43.5%** | **28.3%** |
| (0.5, 0.8] | 126 | 2.43% | 53.2% | 23.8% | 13.5% |
| (0.8, 1.2] | 310 | 1.37% | 41.3% | 7.1% | 3.5% |
| (1.2, 2.0] | 666 | 0.56% | 34.5% | 6.3% | 3.2% |
| (2.0, 3.0] | 535 | 0.52% | 31.6% | 2.8% | 1.7% |
| (3.0, 6.18] | 551 | 0.23% | 29.6% | 4.0% | 2.4% |

全样本基准：gap 中位 0.82% ｜ 一字板 4.3%。`corr(量比, gap) = −0.182`。
⇒ **缩量首板次日大概率高开/一字，是「别惦记」的预警，不是「买点」**。量比 ≥2 者 69.4% 次日平价开盘，才是可考虑对象。

### 2.4 出场

| 持有 | T+1 | T+2 | T+3 | T+5 | T+10 | 3% 止损 |
|---|---|---|---|---|---|---|
| 均值（全样本） | −0.382% | −0.084% | +0.112% | +0.261% | +0.113% | +0.085% |
| 按日 t | −1.97 | 0.01 | 0.12 | 0.02 | −0.67 | 0.25 |

**3% 紧止损触发率 63.6%**（止损后仍 −0.089%）⇒ 一进二不能用紧止损；**持有 1~3 日为观察窗口**，拉到 T+10 无明显优势。

---

## 三、给小九的注意点

1. **本轮只动 `index.html`**（1352965 B）。若你同时改了它，请以「区间整段移植」方式合并 —— 我改的是 `_guide` 三段 + `_cell()` 一行 + 统计行 + 两处注释/副标题，锚点：`🎯 怎么买`、`次日易高开`、`_vr`/`_vrc`。
2. **`data/BOARD_FIRST_LIMIT.js` 的 `?v` 有意未推进**（内容未变），别误当作漏改。
3. **量比口径**记牢：卡面 `read.vol_ratio` = `V[t]/V[t-1]`（与原公式 `V < V.shift(1)*6.18` 同源）。日后再做量能分析，必须用这个口径，不要用 MA5 口径（我踩过一次）。
4. 新增可复用资产（仓外，未入仓）：`E:/_alimi_tools/bfl2/bt_buy.py`（三维判据回测）、`supp2.py`（可成交性拆分）、`supp4.py`（卡面口径纠偏）、`bfl3/render_check2.js`（**含边界夹具**：0.50 触发 / 0.51 不触发）。

---

## 四、三件套证据（本轮）

| 项 | 结果 |
|---|---|
| ① 审计 | 离线全门禁 **14/14 EXIT 0**（staging = 远端最新树 + 换入本轮 index.html）；`[10/10]` 核心标记 **39/39 在位**；`[2/8]` 28 块内联脚本 0 错；`align_logic_ops` EXIT 0；`layer_parity` EXIT 0 |
| ② 四方对齐 | 逻辑详解页 `logic.html` 该卡 **0 命中**（无冲突）；运维页 `V8_PAGE_SCHEDULE` **未登记**该卡（无冲突）；云端 workflow 该卡步注释已含 `is_trading_day` 闸门（本轮未动）；`HANDOFF.yaml` 已有该卡条目 |
| ③ 推仓入库 | Git Data API，基于实时 tip、`force:false`、只列 `index.html`，逐字节回读自证 |

**运行时验证**（不只看语法）：把新渲染器与线上真实数据在 node 里执行 —— 3 个 6 列网格、量比入格 32/32、无滚动窗；并用**夹具**证明量比预警分支边界两侧正确（0.50 纳入、0.51 排除）。
