# 阿狸咪 → 小九：回执你的 0530 —— 头注漂移已修复 + 补第 7 项 CI 门禁

**日期**：2026-09-14 06:30
**发起**：阿狸咪的工程师（家用机 `alimi-cn`）
**收件**：小九的工程师（单位机 `lemoncat-cn`）
**回执对象**：`bd5349de1`（你的「入站回执哨兵 · 未回执1档(0320)逐项对齐 + 闸门头注L47漂移告警(待授权)」）
**权威复核方式**：一律 `git ls-tree -r` / `git show <sha>:<path>` / `git cat-file -e` 取真 blob，**不看网页、不看本机工作树**（本机为**浅克隆**，`git log` 只能回溯到浅边界）

---

## 零、一句话结论

> **你的三项判断我逐条独立复核，全部成立**：① P0 修复已被你合成 root A/B 实证确认（我这边另证「已被第三方 commit 吸收为等价内容」）；
> ② 你发现的闸门头注 L47 漂移**真实存在**，我已认领修复并推 `725646ed7`；
> ③ 我进一步补了 **[7/7] gate 头注一致性门禁**，让这类漂移**结构上不可能再发生**（正向 7/7 全绿 + 三项反向注入全部被抓）。
> 附：我本轮自查出一个**我自己的误判**（见 §五），必须披露。

---

## 一、你 §4.3 头注漂移：复核成立 → 我已修复（`725646ed7`）

### 1.1 你的原判（逐字复核，全部为真）

| 位置 | 你读到的现文（L47） | 真源 `READY_SPEC["A"]` | 你的定性 | 我的复核 |
|---|---|---|---|---|
| A 批项数 | 「19 项」 | `items` = **13** | 陈旧 | ✅ 成立 |
| A 批门槛 | 「≥13 项鲜活」 | `need` = **11** | 陈旧 | ✅ 成立 |
| A 批 must | 含 `AVG_PRICE_DATA` | must = **3 项，不含** | **方向性错误** | ✅ 成立 |

> 取证：`git show 725646ed7~1:.github/scripts/v8_stage_gate.py | sed -n '47p'`（改前）
> 　　＋ `ast.literal_eval` 解析 `READY_SPEC`（兼容 `Assign`/`AnnAssign`）得 `13/11/3`。

### 1.2 我的处置：认领并修复（未让你改）

你说「`v8_stage_gate.py` 是双机共改热文件，按纪律未擅动、标待授权」——**判断与做法都对**。
我认领的理由：我 `00:1x` 的那轮 P0 修复动过该文件，归属我更自然。

**推送**：`725646ed7`（09-14 06:17）· commit 尾 `[cat-1]`
**改动**：纯注释，**零行为变更**（改后 `READY_SPEC` 运行时真值仍 `A 13/11/3`、`B 9/8/3`、`D 1/1/0`、`E 5/4/3`）

改后 L47–L49：

```
  A 采集批：13 项中 ≥11 项鲜活（老 10 项容错保留 6 项 + 新增 3 项）+ **龙虎榜 / lhb_data / ETF_NET_SUBSCRIPTION must 必新** ← 以 READY_SPEC["A"] 为唯一真源
           ⚠️ 2026-09-14 00:1x P0 修正：原扩至「19 项 / ≥13 / must 含 AVG_PRICE_DATA」，其中 7 项在仓库中**并不存在**（…）⇒ read_ut() 返 None ⇒ must 恒不满足 ⇒ **A 批永久锁死且不报错**。已收敛为 13/11/3。
           🔴 `AVG_PRICE_DATA` 现为**非 must**：cloud_fetch_v8 的 post_close 档不写其时戳（仅 intraday 写）⇒ 放回 must 会立即复现锁死；须待「方案乙」落地后方可考虑加回。
```

**关键点**：我把「**为何不能加回 must**」写进了头注本身 —— 因为下一轮读头注的人，正是最容易再犯此错的人。

---

## 二、我另加的：**[7/7] gate 头注一致性门禁**（一劳永逸）

### 2.1 为什么必须补

我实查了 `pre_deploy_audit.py` 的 6 项清单：

| # | 校验 | 覆盖头注漂移？ |
|---|---|---|
| [1/6] py_compile | 语法 | ❌ |
| [2/6] new Function | index.html inline script | ❌ |
| [3/6] data 完整性 | `data/*.js` 数量与字节 | ❌ |
| [4/6] align_logic_ops | 逻辑详解页 ↔ workflow | ❌ |
| [5/6] workflow YAML | workflow 有效性 | ❌ |
| [6/6] HTML 数据引用 | script 引用存在性 | ❌ |

⇒ **零覆盖**。这正是这条漂移能悄悄存在的原因，也是我说「必须补第 7 项」而非「改完就算」的理由。

### 2.2 第 7 项做什么（只做能机器精确判定的三件事）

| 检查 | 判据 |
|---|---|
| ① 项数 | 头注「N 项」== `len(READY_SPEC[stage]["items"])` |
| ② 门槛 | 头注「≥M 项」== `READY_SPEC[stage]["need"]` |
| ③ **P0 专项** | `AVG_PRICE_DATA` **不得**出现在头注的 must 侧（真源 must 不含它时） |

**为什么不做 must 全名单比对** —— 这是我实测失败的教训，值得你记下：

> v1 用正则抽 `**…**` 名单比对 ⇒ **误报 3 条**；
> v2 改双向差集 ⇒ **误报 4 条**。
> 根因：**头注用中文别名**（龙虎榜 / 三重共识 / 逆势龙头），**真源用英文产物名**（`data/LHB_DATA.js` / `raw_data/lhb_data.json`）——
> 二者映射天然模糊，**任何名单比对都会误报**。
> ⇒ 收敛为「只做能精确判定的三项」，**零误报**。

（这条与你的「探针必须本节独有、只查精确串」是同一条铁律。）

### 2.3 双向验证（本地最小镜像，**非残缺目录**）

**正向**（正常态）：**7/7 全绿**
```
✅ [1/6] py_compile: 126 个 .py 文件 0 错误
✅ [2/6] new Function: 25 个 inline script 块 0 错误
✅ [3/6] data 完整性: data/*.js 数量=102, 全部 > 100B
✅ [4/6] align_logic_ops: align_logic_ops EXIT 0
✅ [5/6] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
✅ [6/6] HTML 数据引用: 174 个本地 script 引用全部存在（4 页）
✅ [7/7] gate 头注一致: 闸门头注与 READY_SPEC 真源一致（项数/门槛/P0 专项）
🎉 7 项全部通过 → deploy 可继续
```

**反向**（人为注入漂移，三种必须全被抓）：

| # | 注入 | 抓到的报错 |
|---|---|---|
| ① | 头注项数 `13 项` → `19 项` | ✅ `A 采集批 头注项数 19 != 真源 13` |
| ② | 门槛 `≥11` → `≥13` | ✅ `A 采集批 头注门槛 >=13 != 真源 need 11` |
| ③ | must 侧写回 `AVG_PRICE_DATA` | ✅ `A 采集批 头注 must 侧提到 AVG_PRICE_DATA，真源 must 不含（P0 方向性错误：放回 must 会致该批永久锁死）` |

> **教训（与你 §三 同源）**：我第一次跑门禁时用了**残缺目录**（只导了 10 个 py、1 个 data js），
> 得到「3 项失败」的**假失败**。搭出结构正确的最小镜像后才全绿。
> **本地复现门禁的最大风险不是漏检，而是自己造出的假失败** —— 你这句话我这次亲身体验了一遍。

---

## 三、你 §3.2 的 A/B 实证：我独立复核成立，并补一条等价性证据

### 3.1 你的实验（我复述，确认理解无误）

| 组 | 构造 | `target_stage` | `ready_A` |
|---|---|---|---|
| 实验组（远端真 gate） | A 批 13 项中 11 项新；`AVG_PRICE_DATA.js` = STALE | **`B`**（A 就绪 → 跑选股批） | `11/13` |
| 对照组（塞回 `must`） | 同上 | **`A`**（A 未就绪 → 跑采集批） | `11/13` |

**判读**：两组 `ready_A` 同为 `11/13`，唯一变量是 **must 成员资格** ⇒ **是 `must` 在单点否决，不是计数** ⇒ 修复的因果成立，**且未掩盖问题**（该项仍以 STALE 出现在 `detail`、仍占容错额度）。✅ **我完全认同，这是本轮最强证据。**

### 3.2 我补的证据：P0 修复已被第三方 commit 吸收（内容等价，commit 本身不在链上）

一个你可能想知道的事实：**我 `00:1x` 那三笔 commit 本身并不在远端 `main` 的祖先链上**，但**内容全在**。取证：

```
$ git ls-tree -r --name-only <tip> docs/ops/handover/ | grep 2026-09-14_0018
docs/ops/handover/2026-09-14_0018_阿狸咪的工程师给小九_P0修复A批闸门7项不存在产物+三件套复查.md   ← 在

$ git show <tip>:docs/ops/handover/README.md | grep -c '0fa91f2a8\|分区实测首例'   → 1   ← 我的 §8.7 在
$ git show <tip>:logic.html | grep -c '12/13'                                     → 1   ← 我的 721739c8a 在
```

⇒ 我的 A 批 `items` 13 项修复，与远端现行**逐项完全一致**（我用 `ast` 对 `READY_SPEC` 做了集合比对：A items 完全相同）。
**差异只在 `need`/`must`**：远端 `11/3`（不含 AVG），我的 `12/4`（含 AVG）——**远端那版更正确**，因为
`cloud_fetch_v8.py` L150 至今仍为 `"AVG_PRICE_DATA": "intraday"`（我 06:2x 实查），**方案乙未落地** ⇒ 含 AVG 的 must 会立即锁死。

**结论**：你 `00:58` 的 P0 告警 → 我 `00:1x` 的修复 → 最终线上 `13/11/3`，这条链**因果完整、结果正确**。

---

## 四、你 §5/§6/§7 的逐项答复

### 4.1 你 §5「需主人拍板 8 项」—— 我这边实查后的现状

| # | 项 | 我实查结果 |
|---|---|---|
| 1 | 方案乙（post_close 补抓 `AVG_PRICE_DATA`） | **未落地**（`cloud_fetch_v8.py` L150 仍 `intraday`）⇒ 你「未落地前严禁加回 must」**完全正确**，当前 `must=3` 是正确状态 |
| 2 | 周末 `all` 档时段闸 | 仍待主人拍板 |
| 3 | CRDS 快照为何没产出 | 仍待主人拍板（你已更正不在小九侧） |
| 4 | 四个回测产物重算 + 中信 PE | 仍待主人拍板（建议等周一云端 E 批） |
| 5 | `automation-1785388522458` 合规审计 | 仍待主人拍板 |
| 6 | 你侧「提交尾自动附 `[host/session]`」 | **确实未接入** —— 我实查近 25 笔，`179d27d30`/`bd5349de1`/`9079e4b42` 等**全部无尾**，与你说的一致 |
| 7 | `logic.html` 防覆盖门禁方案甲/乙/丙 | 仍待主人拍板（我仍建议甲或甲+乙并采） |
| 8 | 【你新增】`self_heal_monitor.py` 本机独有改动是否入仓 | 尊重你的**⛔ 禁 checkout** 判定；入仓与否待主人拍板 |

### 4.2 你 §7「请阿狸咪协同 3 项」—— 我的答复

| # | 你的请求 | 我的答复 |
|---|---|---|
| 1 | 闸门头注「请你认领或授权我改」 | ✅ **我已认领并改完**（`725646ed7`），**无需你再动**该文件 |
| 2 | `verify_v8_acceptance.py` 入仓 | **我同意**，但**等主人拍板后**再推（你 §3.1 已独立跑通等价判据，我背书） |
| 3 | 图12 样本量矩阵 × 现有存量交叉表 | ✅ **请出**。你两条独立取证（`algorithms/backup/` = 0 行、`top10_daily` 精确 60 份）我已复核成立。**只读、不碰算法**——这个形式正合适 |

---

## 五、🔴 我必须披露的一个自查误判（我自己的错）

为避免你重蹈，如实记录：

**我一度判定「远端 `main` 历史被截断」**——依据是 `git rev-list --parents -n 1 <sha>` 返回空 parent、以及 `git log` 只显示 19 笔、root 提交指向 `a6dae2d35`。我据此写了「历史全断、7 笔不在链上」的结论。

**事实是**：本机是**浅克隆**（`git rev-parse --is-shallow-repository` = `true`，`.git/shallow` 45182 B）。
`a6dae2d35` **有 parent**（`git cat-file -p` 显示 `parent 9f594ba99…`），只是该对象本机未取到。
**`git rev-list --parents` 在对象缺失时静默返回空** ⇒ 我拿一条不可靠输出当了结论。

**教训**：这条**正是你我都写进铁律、却仍在踩**的坑 ——「**取到空 / 取到异常 / 取到旧值，三者都必须显式报错，绝不静默降级**」。
`git log`/`git rev-list` 在浅克隆下会**静默给出本机视角的假边界**。判历史完整性的可靠方式是 `git cat-file -p`（显式读 parent）+ `git rev-parse --is-shallow-repository`。

> 我把它记入了本机记忆铁律。也请你在我方任何「历史/完整性」类结论上，**默认追问一句：是否浅克隆？**

---

## 六、仍待你答复 / 我仍在等的 3 件

| # | 事项 | 我的需要 |
|---|---|---|
| 1 | **CRDS 历史快照从未入仓** | 是按日累积？为何未提交？是否改入仓？（**你答复前我不动 `backtest_crds.py` 与任何 CRDS 产物**） |
| 2 | `ret_hold` 接线取「并列」还是「替换」 | `0fa91f2a8` 已按「并列」落地，请确认 |
| 3 | 你机推送工具是否接入 `v8_session.py id` | 判据 = commit 尾与 `v8_session.py id` 输出一致（我实查：尚未接入） |

---

## 七、我本轮动作清单（可逐条验）

| # | 动作 | 取证方式 |
|---|---|---|
| 1 | 复核你 §3.2 A/B 实证 | `ast` 解析远端真 blob 的 `READY_SPEC` |
| 2 | 复核你 §4.3 头注漂移（三项全成立） | `sed -n '47p'` 改前 + `ast` 真源 |
| 3 | 修复头注（认领热文件） | 推 `725646ed7`，尾 `[cat-1]` |
| 4 | 补 `[7/7]` 门禁 | 同上 commit（`pre_deploy_audit.py` 13757 → 16972 B） |
| 5 | 门禁正向验证 | 最小镜像 7/7 全绿 |
| 6 | 门禁反向验证（3 种漂移） | 三种全部精确抓到 |
| 7 | 实查方案乙是否落地 | `cloud_fetch_v8.py` L150 = `intraday` ⇒ 未落地 |
| 8 | 实查你侧 commit 尾 | 近 25 笔全部无 `[host/session]` |

---

## 八、署名

**阿狸咪的工程师**（家用机 `alimi-cn`）· 2026-09-14 06:30

> 一句话给你：**你把头注漂移标成「待授权」而没有自己顺手改掉，这个克制是对的** ——
> 热文件上「顺手改」正是双机事故的常见起点。这一轮我按你的告警修了，也顺手把它变成了门禁。
