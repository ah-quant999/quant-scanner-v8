# CRDS「逆势龙头 · 真实回测」整卡全 0 根治：失败不再覆盖有效成果 + 前端空态兜底

| 项 | 内容 |
|---|---|
| 交接 | **阿狸咪的工程师 → 小九** |
| 时间 | 2026-09-23 23:33（北京时间） |
| 提交 | `01a24e8e3`（main，基于实际 tip `c41d700da`） |
| 改动文件 | `v8/backtest_crds.py`（+117/−21）、`index.html`（+18/−0） |
| 离线门禁 | **14/14 全绿**（`exit=0`） |
| 行为实测 | 4 场景**逐项断言**通过（见第四节） |
| 渲染实测 | 抽 `renderCrdsGateAndBt` 在 Node 里喂「空产物」与「线上真实数据」，**13 项断言全过** |

## 一、主人令原文（2026-09-23 23:19）

> 「这个为什么都是空的？一劳永逸修复」（截图：`逆势龙头 · 真实回测` 卡，T+1~T+30 样本 n 全 0，各列全「—」，
> 口径行显示「信号 0 条，其中 0 条有可用 K 线、进入回测 | 0 个独立信号日 | —」）

## 二、🔴 真根因（铁证，非推断）

### 2.1 产物曾**两次被写成全 0**，`method` 字段自证原因

拉 GitHub API 逐版本核对 `raw_data/crds_backtest.json`：

| 提交 / 时刻 | `total_signals` | T+1 `samples` | `method` 字段 |
|---|---|---|---|
| `44d3d212e` / **2026-09-23 16:05:56** | **0** | **0** | `… · baostock 登录失败，自动降级` ← **主人截图那份** |
| `141060a5b` / 2026-09-23 06:14:19 | 249 | 136 | 正常口径 |
| `f01b2675d` / **2026-09-18 01:22:46** | **0** | **0** | `… · baostock 登录失败，自动降级` |
| `384064e67` / 2026-09-18 07:09:32 | 112 | 59 | 正常口径 |

⇒ 好数据与全 0 **交替出现**：**每次 baostock 偶发抽风，就把积累多日的回测成果整份抹成 0**。

### 2.2 截图与 `empty_backtest()` 输出**逐字段吻合**

`empty_backtest()` 产出的正是：`total_signals=0`、`signal_date_range="—"`、12 档（`HOLD_PERIODS` =
`[1,3]+[5,10,20,30,45,60,75,90,180,250]`）全部 `samples:0` 且统计量 `None` ⇒ 前端渲染成 12 行「0 ⚠ / —」。

### 2.3 代码缺陷：**失败即写 0 覆盖**（与同仓主人令直接冲突）

`v8/backtest_crds.py::main()` 有**两条**路径无条件覆盖写盘：

1. `load_signals()` 返回空 ⇒ `empty_backtest("无历史 advanced 信号")` ⇒ 写 `raw_data/crds_backtest.json` + `data/CRDS_BACKTEST.js`
2. `baostock_login()` 失败 ⇒ `empty_backtest("baostock 登录失败，自动降级")` ⇒ 同上

而**同仓 `algorithms/calc_crds.py::_write_empty_crds_output` 早在 2026-09-06 主人令就已修成正确写法**：

> 「🛡 2026-09-06 主人令：数据源异常时空产物**不再覆盖**旧数据 —— **空卡比 stale 更伤**。」

⇒ **这是口径漂移**：同一类场景（数据源异常），`calc_crds` 保留、`backtest_crds` 覆盖。
本产物还是**累积型**（一次运行覆盖 8/1 以来的全部历史信号），覆盖代价更大。

### 2.4 附带诱因：baostock 并发互踢

全仓 **21 个脚本**使用 baostock（含 `calc_crds` / `backtest_comprehensive` / `factor_lab_backtest` / `final_recommend` …），
算法链跑批时同账号并发登录互踢高发 ⇒ **单次 `login()` 失败即判死**属误伤。

## 三、修法（一劳永逸）

### 数据层 `v8/backtest_crds.py`

| # | 改动 |
|---|---|
| ① | `baostock_login()` 加 **3 次重试**（每次先 `logout` 释放会话再重连，间隔 5s），降低「偶发抽风」被当「源失效」的概率 |
| ② | 新增 `_existing_valid()`：判据 = **`total_signals>0` 或任一档有样本**（累积型产物的「有效成果」定义） |
| ③ | 新增 `keep_existing(reason)`：有有效旧成果 ⇒ **保留旧产物、不写盘**，打印 `[KEEP]` 告警（含旧 `update_time` / 信号数 / 档位数） |
| ④ | 两条失败路径改为「`not args.allow_empty and keep_existing(...)` ⇒ 直接 return」 |
| ⑤ | 新增 `--allow-empty`：**只有显式加它**才允许人工强制清零（可审计） |
| ⑥ | docstring 更新过时口径（原文「baostock 登录失败时自动降级为空回测（避免 CI 挂死）」已删） |

### 前端 `index.html`

全档无样本时**不再排 12 行 0 表格**（主人截图那种「一片 0」既没信息量又像坏了），改一行空态说明，
并把 `method` 里的**真实原因原样透出**；有数据时渲染口径**完全不变**（12 档 + 样本独立性提醒照旧）。

## 四、验证证据（可复现）

### 4.1 行为实测（沙盒 + 强制模拟登录失败）

| 场景 | 期望 | 结果 |
|---|---|---|
| 无历史信号 + 有旧好数据 | 保留、不覆盖 | ✅ `[KEEP] 无历史 advanced 信号` + md5 不变 |
| **登录失败 + 有旧好数据（= 09-23 事故场景）** | 保留、不覆盖 | ✅ `[KEEP] baostock 登录失败（已重试）` + md5 不变（旧 248 信号 / 6 档完好） |
| `--allow-empty` | 才写空 | ✅ `total_signals=0` 且 `method` 标注「--allow-empty 强制」 |
| 从未有过成果 | 写诚实空骨架 | ✅ `method` 标注「首次运行，尚无成果」 |
| `--dry` | 不受影响 | ✅ `[DRY] date range: 2026-08-01 ~ 2026-09-23, signals=92` |

### 4.2 Node 渲染实测（抽 `renderCrdsGateAndBt` + mock DOM）

- 喂**空产物**：显示「📭 暂无可用回测结果（baostock 登录失败，自动降级）」+ 累积型说明；**无 `<table>`、无任何 `T+n` 行** ✅（7 项）
- 喂**线上真实数据**（248 信号 / 227 明细 / 10 个独立信号日）：正常渲染 **12 档**表格 + 样本独立性提醒 ✅（6 项）

### 4.3 门禁与远端

- `python -m py_compile v8/backtest_crds.py` OK；`.github/scripts/pre_deploy_audit.py` → **14 ✅ / 0 ❌ / exit=0**
- 远端 `git show origin/main:index.html` 与本工作树**逐字节一致**（1,132,886 字符）；最后改动该文件的提交就是我（CI 未覆盖）
- 远端 `v8/backtest_crds.py`：旧调用 `empty_backtest("baostock 登录失败，自动降级")` 命中 **0**；`_existing_valid`/`keep_existing`/`allow_empty`/`retries=3` 均在位

## 五、横向核查结论（本次一并做了，结论是「只有这一处漏了」）

| 对象 | 结论 |
|---|---|
| `algorithms/calc_crds.py::_write_empty_crds_output` | ✅ **已正确**（09-06 主人令已落地，本次对齐它） |
| `algorithms/final_recommend.py` | ✅ 其「降级」均为**降级标记透传**（09-17 主人令：绝不静默用旧值）—— 正确实践 |
| `algorithms/gen_four_volume_track.py::_write_empty` | ⚠️ **观察项（未改）**：`load_json` 对 `JSONDecodeError` 也回退 `{}` ⇒ 账本**文件损坏**时同样写空壳。但此时丢的是**已损坏的数据**而非好数据，且产物带 `alerts` 提示，风险等级低 ⇒ 本次不动，留给你判断 |
| 其余累积型回测脚本（`backtest_comprehensive` / `backtest_tdx` / `gen_backtest_all_algos` / `backtest_expectancy` / `factor_lab_backtest` / `gen_triple_track`） | ✅ **无**「失败即写 0」模式 |

## 六、给你（小九）的两点

1. **这次的教训值得进你的检查清单**：凡是写**累积型**产物（覆盖多日历史）的脚本，异常分支**一律不得写空** ——
   判据是「已有有效成果 ⇒ 保留」。`calc_crds` 09-06 那条令**应当成为全仓统一范式**，而不是单点修复。
2. `docs/ops/HANDOFF.yaml` 未动（不新增 item，避免动 ledger 锚）。若你认为该立为 P0 专项（「累积型产物禁止异常清空」），
   请按你的口径登记，我不越权改 ledger。

> 另：本机为**浅克隆**（`is-shallow`=true）⇒ `git log <file>` 判不了更新史，本次全部走
> GitHub API `commits?path=…` + `raw.githubusercontent.com` 取历史版本内容。

---

**本报告仅供研究参考，不构成个人投资建议。**
