# 交接：四量回测真值被降级覆盖事故恢复 + 防降级守卫（2026-09-16 16:20）

## 事故
- 主人发现四量终极页「回测 N 日胜率」卡数字与 09-15 验收真值对不上。
- 取证：线上 `data/FOUR_VOLUME_BACKTEST.js` 于 **09-16 02:09** 被云端刷新链
  `cloud_fetch_v8.f_four_volume()` 重跑覆盖 —— 该路径**不注入 `V8_BACKTEST_YEARS`**
  → `strategy_four_volume.py` argparse 默认 3 年 → 3 年/1400 信号覆盖了
  E 批 5 年真值（861 信号/近5年）。
- 卡片标题写死「近5年」、数据变「近3年」，自相矛盾。

## 处置（commit `b2b92c80b1`，已上线，Pages 实抓 861/近5年 ✅）
1. `data/FOUR_VOLUME_BACKTEST.js` 恢复为 09-15 真值 blob `a50d29574c`
   （861 信号 / 近5年 / T+5 49.2% / T+250 70.0%）。
2. `algorithms/strategy_four_volume.py::write_four_volume_backtest_js` 加**防降级守卫**：
   已存在更高 years 的分层真值时，低档回测或无回测空壳**一律不得落盘**
   （同档/升档才允许覆盖）。四用例干跑全过（3年撞5年跳过/空壳跳过/同档放行/无文件正常写）。

## 红线（阿狸咪夜班必读）
1. 🔴 **任何路径重跑 `strategy_four_volume.py` 都不得手动删除/绕过防降级守卫**；
   云端刷新链默认 3 年口径属于已知降级源，守卫会自动跳过（日志见「🛡 跳过覆盖」）。
2. 🔴 **`data/FOUR_VOLUME_BACKTEST.js` 只允许 5 年档（861 真值）或其复现产物覆盖**；
   3 年/1400 信号版本是降级污染，勿手工恢复。
3. E 批（~21:00）以 `V8_BACKTEST_YEARS=5` 跑出同档 5 年回测属正常覆盖；
   若今晚 E 批写出的 5 年数字与 861 真值不一致（云端 GTimg 日线只有 ~641 根 ≈2.5 年，
   深回测窗口可能不足），**不要手工改数**，交回小九把深回测挪到自托管 runner
   （mootdx→BaoStock 全历史，范式 `tmp/fv_backtest_5y_v2.py`）。

## 待办顺延
- 今晚观察 E 批 5 年档产出与 861 真值是否一致（不一致按上面第 3 条处理）。
- 前端卡标题「近5年」为硬编码，真值恢复后已重新一致；若未来改区间档位需同步改标题。
