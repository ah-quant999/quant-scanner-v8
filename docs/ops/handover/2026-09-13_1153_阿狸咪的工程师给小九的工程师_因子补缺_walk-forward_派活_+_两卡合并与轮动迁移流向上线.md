# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-13 11:53｜因子补缺 walk-forward 派活 + 两卡合并与轮动迁移流向上线

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主人拍板的三项已于今日 11:46–11:51 全部上线（`5e21217f4` / `757f5f976`），**其中「因子补缺」这一项需要你出工**：7 个因子的 walk-forward 回测一个都还没跑（`done 0/7`），而这是缺失因子接入 `generate_top10.py` 的**唯一前置条件**——你在中国 IP，能拉 baostock / akshare，这台机跑不了。

---

## 一、需要你做的事（按优先级）

### 🔴 P0：跑 7 因子的 walk-forward 回测（主人明确「先跑 walk-forward 再谈加入」）

**背景**：主人问「标红的缺失项是不是可以开始加入了」，答复是**不能**。三条硬证据：

1. `data/FACTOR_PROGRESS.js`（更新 2026-09-12 22:23）：`total:7 / pending:7 / done:0`，三批次 `B1 3/3 pending`、`B2 3/3 pending`、`B3 1/1 pending` ⇒ **一个都没跑**
2. 卡上自己写死的红线：「须先跑 walk-forward，**禁止直接硬编码高分**」
3. **已经跑过的那一层**（`FACTOR_LAB_BACKTEST.abnormal_volume`）本身就不达标：

   | 指标 | 实测值 | 门槛 | 判定 |
   |---|---|---|---|
   | `spread_oos_pct`（样本外利差） | **−1.982** | > 0 | ❌ |
   | `top_layer_win_10d` | **52.4%** | ≥ 55% | ❌ |
   | `spread_in_sample_pct` | +0.215 | — | 样本内正 |
   | `quarterly_stability` | `"?"` 组 66 期 `avg_spread −0.899`、`positive:false` | 稳定为正 | ❌ |
   | `point_dates` | 67 个时点**只有 1 个非空**（`2023-11-15`） | point-in-time | ❌ 几乎无覆盖 |

   另有 `roe_largecap`：`excess_10d −0.258` / `excess_20d −0.299`（均负）。
   ⇒ **典型「样本内显著、样本外失效」过拟合**。我已在卡上把原先「均经 v2 回测验证显著…样本内外一致」的夸大描述改成如实口径。

**上线条件（主人 2026-09-09 22:31 决策，未变）**：`OOS IR > 0.3` 且 `4/5 年胜率 > 55%`。

**7 因子清单**（`FACTOR_AUDIT` + `FACTOR_PROGRESS` 同源）：

| 批 | 因子 | 标签 | 数据源 owner |
|---|---|---|---|
| B1 | 总市值 | 规模 | ? |
| B1 | 盈利收益率 E/P | 价值 | ? |
| B1 | GPOA（毛利/总资产） | 质量 | ? |
| B2 | 经营现金流/总资产 | 质量 | ? |
| B2 | 负债/总资产 | 质量 | ? |
| B2 | 60 日成交额中位数 | 流动性 | ? |
| B3 | 60 日波动率 | 规模/低波 | ? |

> ⚠️ `owner` 字段在数据里是空的 / `?`，我**没有查到**责任人是谁。请你确认这 7 项由谁负责，或直接按批次自己承接。

**跑完后回填**：`oos_ir` / `backtest_status` / `deploy_status` 回填进 `data/FACTOR_PROGRESS.js`，再 dispatch 重跑 `gen_factor_progress.py`。**卡本身不回填数据**（只做展示与跟踪）。

---

### ⏳ P1：待你回执的旧项（09-11 起累计，我方 10+ 份未回执）

| # | 事项 | 说明 |
|---|---|---|
| 1 | 卡频率口径 | 宏观观测页 3 卡的更新频率（日/周/月）需标准清楚 |
| 2 | 07:15 出稿任务归属 | 归属哪台机、哪个 workflow |
| 3 | `maharo_macro.js` | 是否弃用 |
| 4 | `v8_peer_monitor.py` 缺 `data/hb_xiaojiu.json` | **掉线告警永不触发**（恒判「小九正常」），这条是真 bug，建议优先看 |
| 5 | 09-11 上轮 11 份待答项 | 见 `audit/《今日未完成与待答清单_2026-09-11.md》` |

---

## 二、我方本轮已上线内容（供你复核，不需你动手）

### ① 「因子截面观测」卡整改（`5e21217f4`）

| 项 | 改前 | 改后 |
|---|---|---|
| 更新于胶囊 | 在卡头下方独立一行 | **移到卡名行内紧跟**（与全站一致） |
| 观测仓标签 | 「观测仓 · 调试与回测中」（灰） | **「观测仓 · 回测未达标」（红）** |
| 限制语 | 「当前 2⭐ 观测仓，不可用于选股评分」 | 保留，并补「已产出回测结果但未通过门槛」 |
| 简介描述 | 「均经 v2 回测验证显著…**样本内外一致**」 | **「但独立分层回测的样本外（OOS）利差为负、Top 层胜率未过 55% 门槛 ⇒ 属样本内显著、样本外失效，不构成 edge 证据」** |

> 主人原问「它现在不是观测仓了吧，都已经进入算法链了」——我核实后：**该卡仍标 2⭐，且自挂回测数据显示 OOS 未达标，尚未转正**。所以没有摘掉「观测仓」定性，但把「调试中」改成如实反映「回测未达标」。如果你那边有它已转正的依据，请指出。

### ② 两卡合并为「因子补缺与审计」（`5e21217f4`）

主人问「这两张是不是一个意思」→ **确认重复**（同一个 7 因子清单、同一个缺失集合、同一条「禁止硬编码高分」红线，仅视角不同）。合并为一张：

- **上半**：7 因子覆盖状态表（`FACTOR_AUDIT` 口径：`total:7 / missing:5 / partial:2 / covered:0`）
- **下半**：walk-forward 补缺进度（`FACTOR_PROGRESS` 口径：`total:7 / pending:7 / done:0` + 3 批次进度条 + 7 因子进度表）
- 已删除 `__renderFactorProgress` 函数、`#factorProgressBody` 容器及**全部 4 处调用点**（`__renderFactorPane` / `_renderPostReady` / `renderStTab` / `switchSt`）

### ③ 「轮动路线 TOP3」→「🔀 轮动迁移流向」并移位（`5e21217f4`）

主人选**方案 C**，并拍板：

| 项 | 决定 |
|---|---|
| 位置 | 从 Block2「收盘复盘」**移到 Block3「💰 资金净额趋势」正上方** |
| 流出方口径 | **5 日净额转负**（原为「今日净额为负」） |
| 流入方口径 | 今日净额为正 |
| 无配对时 | **整卡隐藏、不占位** |

**修掉的旧卡缺陷**（值得你知道）：原卡 `out3` 只取 `sectors_out`。全市场普涨日 `sectors_out = []`，但 `in3` 仍有值 → 表格照常渲染，出现「**流出列整列空白 + → 流入列有值**」的错配展示；且「流出」「流入」两列按**同序号并列，并无因果对应**（第 1 名流出并不流向第 1 名流入），有误读风险。

**联动关系**：与「💰 资金净额趋势」**同源同批**（均读 `SECTOR_FUND_FLOW_TREND` 的 `sectors_in` / `sectors_out`），上方看「谁从哪流到哪」，下方看「各板块 5 日 / 20 日净额排行」，互补。

### ④ 逻辑详解页同步（`757f5f976`）

- 卡清单：`FACTOR_AUDIT` / `FACTOR_PROGRESS` 两条 → 合并为一条「因子补缺与审计」
- 板块资金趋势段新增「🔀 轮动迁移流向」小节（位置 / 配对口径 / 设计意图 / 旧口径缺陷 / 联动关系）

---

## 三、⚠️ 本轮踩到的坑（请你注意，尤其第 1 条）

### 🔴 1. logic.html 换行符被改写事故（已修复，但你要防同类）

**事故**：我的中间提交 `50565601c` 把 `logic.html` 全文换行符从 **LF 改成 CRLF**（远端 `CRLF 0 / LF 8770` → 变成 `CRLF 8692 / LF 8692`），git diff 显示 **8692 增 / 8770 删** 的整文件伪改动，**掩盖了真实内容变更**（实际只改了 4 行），并使后续冲突面放大。

**根因**：`io.open(..., newline='')` 读入后，写入时按平台默认转换了行尾；且我手工在写入串里带的 `\r\n` 与源文件本身的 LF 不一致。

**修复**：`757f5f976` 以原始字节为基底，改**二进制模式**打补丁，行尾严格沿用源文件既有 LF。

**你的防范**：给 `logic.html` 打补丁前后，务必核验 `CRLF` 计数。当前三文件均为纯 LF：

| 文件 | 字节 | CRLF | LF |
|---|---|---|---|
| index.html | 1100172 | **0** | 15362 |
| logic.html | 712330 | **0** | 8772 |
| calendar.html | 8634 | **0** | 162 |

### 🔴 2. 双机并行推送撞车（本次实际发生）

我 11:46 推 `5e21217f4` 后，CI 在 11:46 自动产出 `88435dadf v8 build`（改 `index.html` 202 行 + 10 个 `data/*.js`）。我推 `logic.html` 时基底已变为 `88435dadf`。

**这次侥幸没出问题**（我用 `read-tree` 以最新基底派生，只替换 `logic.html`，build 的改动完整保住，已验证 `git diff 88435dadf 50565601c` 只有 `logic.html` 一个文件）。但**推前必 `git fetch` 复查基底 sha** 这条铁律今天又验证了一次必要——一天内远端 tip 变了 3 次。

---

## 四、验收记录（供你复核，全部已过）

| 项 | 结果 |
|---|---|
| 部署前 5 门禁（`py_compile` / `new Function` / data 完整性 / `align_logic_ops` / YAML） | ✅ |
| `align_logic_ops.py` | **EXIT 0** |
| inline script 语法（25 块 `new Function`） | **0 错** |
| CDN byte 级 `cmp` — index.html | ✅ 一致（差异仅 `BUILD` 版本戳，CI 注入正常） |
| CDN byte 级 `cmp` — logic.html | ✅ 完全一致（712330 B） |
| 线上真浏览器终验（agent-browser） | ✅ 全过：轮动迁移流向渲染 / 位于资金净额趋势之前 / 旧卡全站消失 / 因子卡已合并 / 无「加载中」/ `#factorProgressBody` 已删 |

---

## 五、相关提交

| commit | 内容 |
|---|---|
| `5e21217f4` | 因子两卡合并 + 观测卡整改 + 轮动迁移流向移位（index.html） |
| `88435dadf` | CI 自动 build（非我方，中间插入） |
| `50565601c` | 逻辑详解页同步（**含换行符错误，已被下一提交修正**） |
| `757f5f976` | **修正 logic.html 换行符事故**（LF 回退，内容等价） |

---

## 六、待主人拍板（与你无关，仅供参考进度）

1. 「轮动迁移流向」新卡今日实数据只配出 **1 行**（成飞概念 5日 −1.97亿 → 通信设备 今日 +39.65亿）——因为 CDN 最新 `sectors_out = 0`（全流入）。这是设计预期（无轮动则少显示），但主人可能会觉得卡面空。
2. CRDS 回测 3 方案（建议 1：加样本量警示）+ `crds_history.json` 仅 188 B **快照未累积**是真 bug
3. 仓库根 3 个未被跟踪的 `.bak` 是否删

---

*交接人：阿狸咪的工程师 ｜ 2026-09-13 11:53 ｜ 本机：alimi-cn（家用机）*
