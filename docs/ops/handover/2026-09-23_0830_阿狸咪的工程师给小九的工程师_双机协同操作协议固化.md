# 交接件 · 双机协同操作协议固化（权威原文建立）

- **发送方**：阿狸咪的工程师（`alimi-cn` 家机）
- **接收方**：小九的工程师（`lemoncat-cn` 单位机）
- **时间**：2026-09-23 08:30 北京时间
- **主题**：把主人反复强调的协同铁律固化为唯一权威协议，避免主人对每个专家重复说明

## 一、本次改动（仓库内容）
- **新增** `docs/ops/DUAL_MACHINE_PROTOCOL.md` —— 双机协同操作协议**权威原文**。
  - 0 总则：自主决策（只在不确定意图时问）、发现问题一劳永逸修复、修好审计后直接上线、全程中文。
  - 1 邮件纪律：禁频繁发邮件，只对「不能自愈且无法解决」的问题发 2814546@qq.com。
  - 2 改后三件套：**1 审计 / 2 云端·双机·运维页·逻辑详解页 四方对齐 / 3 推仓入库防覆盖 + 交接**（与既有 主人定式 2026-09-21 完全一致，此处固化）。
  - 3 双机分时与署名；4 引用与权威链（本文件=原文，skill `v8-triple-check-after-change`=执行清单）。
- 同步已：`~/.workbuddy/MEMORY.md` 增补「双机协议 & 改后三件套 / 邮件纪律 / 自主决策 / 全程中文」跨项目偏好；skill `v8-triple-check-after-change` 一·补 首行加「权威原文 = `docs/ops/DUAL_MACHINE_PROTOCOL.md`」。

## 二、审计（本次为纯文档改动）
- 代码门禁：**N/A**（未动 index.html / 算法 / workflow / 守卫脚本）。
- 推送自证：Git Data API 基于 `origin/main` tip、`force:false`、仅新增本文件 + 本交接件；推后逐字节回读一致。

## 三、四方对齐核验
- 本文件是「四方对齐」的**对齐载体本身**；运维页 / 逻辑详解页 / 云端 workflow / 双机任务时窗**未引入任何新口径**，无冲突。
- 既有「三件套」四维度的既有实现（时窗矩阵、离线门禁、交接件规范）维持不变，仅补「运维页 / 逻辑详解页」两维的明文要求。

## 四、待对方（小九）应用项
- 拉取 `origin/main` 后，`docs/ops/DUAL_MACHINE_PROTOCOL.md` 即生效；双方家机/单位机 skill（同名 `v8-triple-check-after-change`）已指向本文件。
- 若小九侧 skill 一·补 未含「权威原文」指针，按上补一行即可（非强制，仅一致性）。

## 五、复核命令（给判据，不给私有路径）
```
git cat-file -e origin/main:docs/ops/DUAL_MACHINE_PROTOCOL.md && echo "协议文件已入库"
git log --oneline -1 origin/main -- docs/ops/DUAL_MACHINE_PROTOCOL.md
```
