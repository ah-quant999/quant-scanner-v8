# 交接 · 阿狸咪 → 小九（2026-09-09 06:15）

## 一句话
**盘前清空盘中模块已「去破坏化」——从此盘前不再清空任何数据，只保留上一交易日真实值并打标记，等开盘后首次 intraday fetch 自然覆盖。已恢复今晨被误清的 6 个变量。**

## 现象（主人 06:02 质询）
主人发现「盘前才清空的那批」现在就空了，且此前已交代过「等到 9:20 附近才清空，或干脆别清空等新数据覆盖」。

## 根因（已查证，非推测）
`cloud_fetch_v8.py` 的 `_clear_intraday_for_premarket()` 时间门控**只有上限**：

```python
if h >= 9.5:   # 仅挡 09:30 之后
    return     # 00:00–09:29 任何时刻跑 premarket 都会清空
```

而 `v8_cn_fetch_cloud_hosted.yml` / `_selfhosted` 等按「当前时间 < 09:30 → category=premarket」选档，**premarket 档整夜约每小时被派发一次**，于是每个整点都清一次。

实测 2026-09-09 凌晨被连清 6 轮（文件内 update_time 实证）：

```
00:29:19  01:30:16  02:32:55  03:34:07  04:36:49  05:38:26   ← 全是 no_data 空 stub
```

即：**昨晚 21:0x 的盘后真实数据从 00:29 起就被清掉，整夜六连清，且会持续到 09:30。**

## 为什么选「彻底不清空」而不是「9:20 才清」
- premarket 跑批是**被 hourly 巡检派发**的，分钟数在漂移（48→51→52…），**无法保证 9:20 附近一定有一档**；
- 若加 `[09:00, 09:30)` 下限，整夜跑批会全部跳过 → 结果仍是「从不清空」，但依赖运气、且 09:5x 那档会跳过，行为不确定；
- 主人明令「**或者就别清空等新数据上线直接覆盖也可以**」→ 直接做成确定性行为。

## 修复内容（commit 已推 main）
1. **`cloud_fetch_v8.py`**：`_clear_intraday_for_premarket()` 主循环由「写空 stub」改为「保留真实值 + 标记」，与既有 KEEP_VARS 语义完全一致：
   ```python
   cur = _load_judgment_raw(var, fname) or {}
   if not cur or cur.get("no_data"):
       continue
   cur.pop("no_data", None)
   cur["premarket_cleared"] = True
   cur["note"] = note_map.get(var, "盘前保留上一交易日值，开盘后自动刷新")
   save(var, cur)
   ```
   → 卡片**永不再出现空窗**，也不再依赖任何时间点。
2. **恢复 6 个变量**（`raw_data/` 源 + `data/*.js` 双层面，只改 JS 会被下一轮从 raw 覆写回空）：
   INDEX_QUOTES / ETF_PULSE / ETF_INTRADAY_HEAT / CANDIDATE_QUOTES / MARKET_ALERTS / SECTOR_FUND_FLOW
   恢复基准 = 提交 `bb5be1ad8`（真实数据 2026-09-08 21:05–21:10）。

## 校验（三件套，实测通过）
- ✅ `cloud_fetch_v8.py` Python 语法 0 错误
- ✅ 新逻辑已落地、破坏性 `stub = {"no_data": True...}` 已移除
- ✅ 12 个文件线上实测：无 `no_data`、update_time 均为 `2026-09-08 21:0x`

## 请小九注意
1. **勿恢复旧清空逻辑**——这是同类误伤第 6 次（08-11 ETF_DAILY_MONITOR / 08-26 CONCEPT_RANKING / 08-31 连板天梯 / 09-01 板块资金 / 09-08 MACRO_DATA / 09-09 本次）。
2. 若你本地有 `cloud_fetch_v8.py` 未推改动，请先 pull 对齐再改，避免覆盖本次修复。
3. `premarket_cleared` 标记打在真实数据上（与既有 CAPITAL_FLOW_DATA / MACRO_DATA 做法一致），HEALTH_CHECK 走既有 `_clear_premarket_marker` 清除，无新增 fail 风险。
4. data/*.js 的 `?v` 缓存戳会由 `v8_cache_buster_reconcile.yml`（每 15 分钟）自动对齐；主人端硬刷 Ctrl+F5 立即可见。
