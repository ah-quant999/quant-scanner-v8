# 九宝量化 v8 交接文档 · 小九 → 阿狸咪

**交接时间**：2026-09-01 08:03（CST，周二）
**交接人**：小九（白天机，07:45–17:45 独家）
**接收人**：阿狸咪（夜间/周末机，18:00–次日 07:30 + 周末）
**线上健康度（07:28 实时）**：`overall=ok`，`summary={ok:110, warn:1, fail:0, total:111}`
**本地 git 树**：HEAD 与 origin/main 同步（落后 0 提交）

---

## 一、当前状态（一句话）

全天审计任务**全部闭环**，全站 **110/111 绿灯**（仅 1 个 warn 是老债，见第三节）。盘前 08:15 全量已生效（打新/今日判定 06:26–06:30 已刷新），可放心交班。

---

## 二、昨日（08-31）已落地的根因修复（均已上 main，防覆盖推仓 + API 回读校验）

| 模块 | 根因 | 修复 | commit |
|---|---|---|---|
| 共振日历（机游共振/北向席位）无数据 | 四重根因：①两个 self-hosted runner 全 offline ②抓取早于东财 17:30 发布 ③`backfill_lhb_history.py` 无重试 ④`done` 集合把 error 占位算"已完成"→当日永不再抓 | `backfill_lhb_history.py` 加4次退避重试+新浪兜底+`_is_real()`只认真实完整数据+`--only`单日补抓；workflow 加 20:30 晚间补抓档；共振日历标题加数据日期徽章 | `e62671302b71` / `d106f743325a` / `abfd2fabde8c` |
| 8 张截图 UI 四连修 | 9月导航按钮失效/业绩预告百分比离谱/宏观胶囊冗余时间/选股池版式 | index.html 4 处渲染函数修复 | `d5abc62ef12c` |
| 主站数据停滞 14:27（重大事故） | 四层：①`workflow_dispatch` 入参未优先于 `schedule`→手动派发退化成 all 全量 ②fetch job timeout 25min 不够 ③推送 step timeout 更紧 ④HEALTH 逐文件线上比对 108 次 HTTP 性能黑洞 | workflow 改 dispatch 入参优先 + timeout 放宽 + CI 内跳过逐文件比对 | `b781d68c4a2e` 等 5 个 |
| `v8_cn_fetch_cloud_hosted.yml` 缺 data 推送步骤 | 兜底链跑 success 但 data/*.js 永远推不上 main→前端看不到 | 移植主版闭环推送步骤 | `f5e4fe431207` |
| MARGIN_DATA（融资融券）红灯 | CATEGORY_MAP 错配 `premarket`（融资融券 16:15 才发布，盘前抓不到、post_close 又不抓）→全天停在陈旧值 | 两处改 `"premarket,post_close"` | `1d285fb`（SSH 推送） |
| 卡片时段错配（M1/M2） | CFFEX_HOLDINGS / AVG_PRICE_DATA 被错改 post_close→盘中显示昨值 | 改回 intraday + 加入 KEEP_VARS | `5a5c5f5a` / `7a32c81a` |

---

## 三、需阿狸咪知晓的待办 / 老债（非紧急，勿动红线）

### 🟡 warn 项（1 个，老债）
- **`all_HUNTER_BACKTEST`**（全量数据页）：`HUNTER_BACKTEST.js` 无 `update_time/date/generated` 时间戳，HEALTH 无法判龄→标 warn。**这是审计登记缺失，不是数据故障**，前端卡片正常。修复法：给 `HUNTER_BACKTEST.js` 注入时间戳字段（属轻量特性补全，可暂缓，避免 v8 膨胀）。

### 🔴 铁律红线（阿狸咪窗口必守）
1. **`index.html` 阿狸咪编辑期间小九禁止触碰**；本次交接小九未改 index.html。
2. **HEALTH_CHECK.js 冲突保留本地不动**（双机各自生成，不跨机覆盖）。
3. **所有 v8 自动化任务中，阿狸咪侧的 stock-scanner 任务须显式 PAUSED**（禁止删除）；恢复 ACTIVE 须小九明令授权。
4. **阿狸咪-紧急指令监听铁律：只读不写**。
5. 双机分时：小九白天独家（07:45–17:45）；阿狸咪夜间/周末（18:00–次日 07:30 + 周末）部署 v8；6h 看门狗 + 自动交接 12:00/18:00。

### ⚠️ 已知环境坑（避免二次踩）
- **两个 self-hosted runner（alimi-cn / lemoncat-cn）目前全 offline** → 依赖它们的 `v8_lhb_fetch.yml` 实质已死，龙虎榜改走云端 `v8_cn_fetch_cloud_hosted.yml`（20:30 CST 补抓档）。
- **本机 WorkBuddy automation 与 GitHub Actions schedule 都可能静默停摆**（8-31 多次印证）→ 关键兜底靠「人工/脚本主动检查+补派」，不单信 cron。
- **写接口次级限流**：高峰连续写会 401（GET 正常）→ 绕过用 SSH git push（仓库 679MB 用 partial clone）。
- **禁止 dummy 内容诊断 PUT**（8-31 曾误覆盖 `cloud_fetch_v8.py` 成 4 字节，已还原）。

### 📌 昨日未推的 6 个文件变更（来自更早轮次，非本次审计）
`calendar.html / calendar_seed.json / v8_cn_fetch.yml / cloud_weekly_cleanup.yml / v6_memo.html / sync_v6_to_v8.py` —— 仍在本地未提交。若与本次无关，**勿动**；如需推请小九明令。

---

## 四、交接结论

✅ 全站健康（110 ok / 0 fail / 1 warn-老债）
✅ 昨日 8 张截图审计 7 项全部闭环 + 共振日历四重根因根治
✅ 盘前 08:15 全量已生效（打新 06:26 / 今日判定 06:30 已刷新）
✅ 本地 git 与 origin/main 同步
⏳ 阿狸咪窗口：守红线、盯 20:30 龙虎榜补抓档、w️arn 老债可暂缓

**可放心交班。**
