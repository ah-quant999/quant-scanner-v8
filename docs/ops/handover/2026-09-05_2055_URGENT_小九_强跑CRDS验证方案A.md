# URGENT HANDOVER 阿狸咪 → 小九（2026-09-05 20:55 紧急·CRDS 方案A 强跑验证）

## ⚠️ 主人指令（最高优先级）

主人于 **2026-09-05 20:55** 下达紧急指令：

**立刻派小九 cn 机跑一次完整算法链（含 calc_crds），验证方案A（合并所有候选源去重）落地后的真实扫描宇宙——从原 47 金股扩到 474 宽候选。**

方案A 已上线 commit `a8e21b69e`（远端真验证 HEAD=FETCH_HEAD），本地+远端一致。  
当前 12:49:42Z (20:49 北京) 跑批的 checkout 早于本次 push，仍会用旧逻辑扫 47，**等这次收尾后立刻再起一轮用新代码跑**。

---

# action: algo

## 一、跑批任务（小九必做）

| 项 | 值 |
|---|---|
| 触发时间 | 收到本 URGENT 后立即（最迟 21:30 北京前启动） |
| 跑批脚本 | `algorithms/run_algorithms.py`（走现有 ORDER 链路，含 calc_crds） |
| 工作目录 | `/home/runner/work/quant-scanner-v8/quant-scanner-v8` |
| 时间门禁 | **bypass=True**（绕过 18:00 门禁，因本次重点是验证 474 扫描宇宙，不必等傍晚） |
| 龙虎榜门控 | 正常（跑批会自带 LHB gate，周末 LHB 无数据不影响 calc_crds 本身） |

## 二、必须看到的产出

跑批完成后，**必须** 在产物里看到：

| 文件 | 期望值 |
|---|---|
| `raw_data/crds_card_data.json` | `total_scanned > 0`（最关键，证明 474 候选源生效） |
| `raw_data/crds_card_data.json` | `update_time` 在跑批当天（北京时间） |
| 跑批日志（`logs/run_algorithms_*.log`） | 出现 `[合并完成] 总 N 只 (金股 X + 候选 Y)` 行，X ≥ 40、Y ≥ 400 |
| `out/crds_recommendations.json` 或类似 | 有真实 CRDS 推荐（非空数组） |

## 三、若 21:30 仍未跑通（小九回报）

- 跑批里出现「所有数据源均不可用」→ 检查 `data/CANDIDATE.js` 是否被 Nutstore 删（应有 474 条 total）
- 跑批里出现 `[数据源] ... 0 只` → 检查源文件 json 解析（v8 cn fetch 是否正常）
- 跑批正常但 `total_scanned` 仍 0 → 报告，不要静默 PASS

## 四、回报（监听桥自动捕获）

跑批 `conclusion` 完成后，监听桥会读产物并写回报到 `raw_data/crds_card_data.json` 头部摘要。本次只关注：**`total_scanned > 0` + 时间戳是今晚**。

落地报告送达：**阿狸咪**（本会话）。

---

派发来源：阿狸咪（云端备机）· 2026-09-05 20:55 北京  
执行机：小九（lemoncat-cn）  
根因 commit：`a8e21b69e`（方案A）  
verify commit：`a8e21b69e`（远端 FETCH_HEAD 真上线）