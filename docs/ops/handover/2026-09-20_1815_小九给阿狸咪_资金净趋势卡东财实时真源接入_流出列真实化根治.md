# 小九的工程师给阿狸咪的工程师 · 💰 资金净趋势卡「东财实时真源接入」一劳永逸根治上线交接

- **交接时间**：2026-09-20（见文件名时间戳）
- **发起方**：小九（单位机 · 白天班）
- **接收方**：阿狸咪（夜间/周末班）
- **性质**：主人令「一劳永逸根治」· 数据真实性缺陷已修 · 已上线 · 请知悉并纳入你的巡检口径
- **主人原话**：「这个是不是还是错的被覆盖回来啦？查改一劳永逸修复！」（截图 = 实时数据页 > 💰 资金净趋势卡，5日/20日两列流入有数据、流出=暂无）
- **结论先行**：**不是 CRLF 覆盖事故**，是**数据真实性缺陷**（旧本地稀疏 history + 已停用同花顺估算公式虚高 + 旧口径未重生 ⇒ `sectors_out` 恒空、净值虚高/反向）。已用**东财 push2delay 权威实时源**一劳永逸根治。
- **改动文件**：
  1. `algorithms/fetch_orphan_sector_fund_flow.py`（新增 `fetch_eastmoney_realtime_fund_flow()` + 主源选池 + 5 处编辑）
  2. `raw_data/sector_fund_flow_trend.json`（重生，`update_time=2026-09-20 18:15`）
  3. `data/SECTOR_FUND_FLOW_TREND.js`（重生，56KB，与 `update_v8.py` 同格式精准写出）
- **未改动**：前端 `index.html`（卡片渲染逻辑 `_dayOK` 满窗过滤 + `net_5d`/`net_20d` 符号重分类已在 09-18 前序 commit `a9d2702a12` 就位，**本次零前端改动**）

---

## 一、根因（为什么流出列恒「暂无」、净值虚高/反向）

四层因果叠加，全在**数据侧**，与 CRLF / 行尾 / 覆盖无关：

| 层 | 旧口径问题 | 后果 |
|---|---|---|
| ① 当日覆盖度 | akshare `stock_sector_fund_flow_rank` 当日仅 ~40 板块 | 选池天然不全 |
| ② history 累加 | 本地稀疏 `raw_data/history/*.json` 累加 `nets[-5:]` 跨 13+ 交易日，却硬编码 `net_5d_days=5` | 窗口值被「跨多日累计」污染，满窗过滤 `_dayOK` 把大多数板块判为「不满窗」⇒ 不展示 |
| ③ 估算公式 | 已停用的同花顺口径 `成交额 × 涨幅 / 100` 当净额 | 净值**虚高且方向与真实主力净额相反** |
| ④ 口径陈旧 | 09-18 旧口径数据未重生 | 线上跑的是陈旧虚高产物 |

⇒ 最终 `sectors_out` 恒空（没有任何板块进流出列），净值方向错乱。卡片前端本身正确（它按 `net_5d` 符号重分类），**锅全在喂给它的数据**。

---

## 二、一劳永逸正解：东财 push2delay 权威实时源

- **接口**：`https://push2delay.eastmoney.com/api/qt/clist/get`
- **参数**：`fs=m:90+t:3`(概念)/`m:90+t:2`(行业)，`fields=f12,f14,f62,f164,f166,f170,f172`，`fid=f3`，`fltt=2`，`po=1`，`np=1`
- **字段语义**（单位元，÷1e8 得亿元）：
  - `f62`=今日净额 / `f164`=5日 / `f166`=10日 / `f170`=20日 / `f172`=60日
  - **含净流出**（这正是旧源缺失的关键）
- **分页上限**：`push2delay` 每页**硬上限 100 条**（传 `pz=1000` 仍只回首 100）⇒ 须 `while True` 逐页翻（`pn` 递增，`len(diff)<PAGE` 才 break），才能覆盖全市场 **999 板块**。
- **实测可用性**：`push2`/`push2his` 被限流，唯 `push2delay` 可用；本轮本地实跑取满 999 板块。

### 代码落点（`fetch_orphan_sector_fund_flow.py`）
1. 新增 `fetch_eastmoney_realtime_fund_flow()`（插在 `fetch_from_neodata()` 之后、`fetch_sector_flow()` 之前）：逐页翻概念+行业，取 999 板块，`is_valid_sector_name` 过滤后写 `out[name]`（含 `net/net_5d/net_10d/net_20d/net_60d` + 对应 `_days` 真实窗口值 + `source="东财实时"`）。
2. `fetch_sector_flow()` 内 5 处编辑：
   - 顶部加 `em_primary=False`；在 `if ak is not None:` 前插**东财主源选池**（5日/20日 双向各 Top30 去重，最多 80 板块入 `top_list`，置 `em_primary=True`）
   - 两处 `if ak is not None` 加 `and not em_primary`（主源生效时跳过 akshare 补位）
   - `result["top_list"] = top_list if em_primary else top_list[:40]`（主源不截断到 40）
   - **THRESHOLD 分流**：主源按 `net_5d` 符号分流（≥0 进 `sectors_in`，<0 进 `sectors_out`），**保证流出列有真实净流出**；旧 akshare 链路保留作 `em_primary=False` 时的 fallback

---

## 三、实测数据（本批重生 · `update_time=2026-09-20 18:15`）

- 东财实时主源获取 **999 板块**（概念+行业）
- `sectors_in = 30`、`sectors_out = 50`（**50 条全部 `net_5d < 0`**）
- 流出 TOP5（5日净额，亿元）：

| 板块 | 类型 | net_5d | net_20d |
|---|---|---|---|
| 东方财富热股 | 概念 | **−249.17** | +2.62 |
| HS300_ | 概念 | **−217.81** | −97.84 |
| 周期股 | 概念 | **−186.33** | −34.63 |
| 上证180_ | 概念 | **−166.66** | −26.96 |
| AH股 | 概念 | **−158.26** | −28.33 |

- 流入 TOP5（5日净额，亿元）：

| 板块 | 类型 | net_5d | net_20d |
|---|---|---|---|
| 半导体概念 | 概念 | **+336.69** | −295.87 |
| 国产芯片 | 概念 | **+274.47** | −276.99 |
| 半导体 | 行业 | **+244.77** | −223.34 |
| 电子 | 行业 | **+240.77** | −322.67 |
| 先进封装 | 概念 | **+168.88** | −111.16 |

> ⚠️ 注意：5日强流入的板块（半导体/国产芯片/电子）其 `net_20d` 为负 ⇒ 近 20 日其实在流出。这是**真实多空分歧**，不是 bug。卡片「5日」「20日」两列本就独立，切勿把两列当成同一方向的累计。

---

## 四、线上生效取证（本次复验实测，可自行复跑）

**截至本次复验实测**：

| 项目 | 值 |
|---|---|
| 本批 commit | `553759574e`（已并入 main 历史） |
| 当前 main tip | `70d9a5e8f93bc9a0cd4234ae508e1e249bd136bd`（后续有其他提交，本批内容仍在线上生效——**以数据真身为准**） |
| 远端 `data/SECTOR_FUND_FLOW_TREND.js` · `update_time` | `2026-09-20 18:15` |
| 远端 `sectors_in` / `sectors_out` | `30` / `50`（50 条全 `net_5d<0`） |
| 每条记录 `source` | `东财实时` |
| `v8_build_deploy` 运行 | `35504894885` 结论 **success**（head = 本批 commit `553759574e`） |

### 复跑命令（现取，不写死）
```bash
# 1) 看 main 当前 tip（会随高频 churning 变动，不要当成固定事实）
curl -s https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main

# 2) 抓线上数据真身，查关键字段（应如下）
curl -s --max-time 60 -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/data/SECTOR_FUND_FLOW_TREND.js?ref=main" \
  | python -c "import sys,json,re; t=sys.stdin.read(); t=t[t.find('=')+1:].rstrip(';'); o=json.loads(t); \
  print('update_time',o.get('update_time')); print('in',len(o.get('sectors_in',[])),'out',len(o.get('sectors_out',[]))); \
  print('out_all_neg5d', all(s.get('net_5d',0)<0 for s in o.get('sectors_out',[])))"

# 3) 查 Pages 缓存戳（?v=）是否 ≥ 本批重生时刻对应戳
curl -s --max-time 40 "https://ah-quant999.github.io/quant-scanner-v8/index.html" \
  | grep -o "SECTOR_FUND_FLOW_TREND\.js?v=[0-9]*"
```

> ⚠️ 上表 commit / tip 是**本次复验实测值**，主分支高频变动；**不要当成固定事实沿用**，以复跑命令现取为准。判定「是否生效」的唯一硬据 = 数据真身的 `update_time` + `sectors_out` 非空且全 `net_5d<0`。

---

## 五、给你的巡检建议（请纳入夜间/周末巡检口径）

1. **本卡「流出」列现在恒有真实数据**，是设计态，不是故障。
   若某日又出现「流入有数、流出=暂无」 ⇒ **先查 `em_primary` 是否降级到了 akshare fallback**
   （即东财 push2delay 接口不可达 / 取到的板块 < 100 ⇒ 主源失效，回退旧链路会重现旧缺陷）。
   复跑命令：`python algorithms/fetch_orphan_sector_fund_flow.py` 看 stdout 是否打印
   `🟢 东财实时主源生效：选池 N 个板块` 还是 `⚠️ 东财实时主源失败，回退 akshare 链路`。
2. **本卡不是覆盖事故**：主人截图误以为「又被覆盖回来」，实测是**数据真实性缺陷**，根因已在第二节列清，已根治。后续若再出现同类现象，先按第 1 条查主源可用性，再按覆盖事故流程排查。
3. **判据数值未改**：`_dayOK` 满窗过滤阈值、`THRESHOLD=1.0` 亿元分流阈值均沿用既有值，本次**只换数据源、不动判据**（符合主人「不擅改判据」铁律）。
4. **前端 `index.html` 零改动**：卡片渲染口径（符号重分类、满窗过滤）在 09-18 前序已就位，本次未碰一行前端。

---

## 六、本次遗留 / 明确未做的事

| 项 | 状态 |
|---|---|
| 旧 akshare / 同花顺 fallback 链路 | **保留**作 `em_primary=False` 时的降级（可后续清理，本次不擅删） |
| `raw_data/factor_walkforward.json` 用新脚本重跑 | **挂账**：等盘后算法链（需 `stage=E` 且前置 B 批就绪）自然重跑，当前仍是 09-19 18:25 旧产物，前端走降级分支显示「待算法重跑」= 有意设计，非缺陷 |
| `fetch_orphan_sector_fund_flow.py` 其余函数 | **未改**（只新增 1 函数 + 编辑 `fetch_sector_flow` 内 5 处） |
| 卡片 UI / 文案 | **未改** |

---

## 七、一句话结论

💰 资金净趋势卡的「流入有数、流出暂无 + 净值虚高/反向」**不是覆盖事故，是数据真实性缺陷**；
已用**东财 push2delay 权威实时源**（999 板块、含净流出、窗口值真实）设为**主源**、旧 akshare 链路降级 fallback，
本批重生 `sectors_in=30`/`sectors_out=50`（50 全 `net_5d<0`，流出 TOP：东方财富热股 −249 亿 / HS300_ −218 亿 / 周期股 −186 亿），
线上 Pages 已生效（`update_time=2026-09-20 18:15`，build_deploy success）。判据数值零改动，前端零改动。
