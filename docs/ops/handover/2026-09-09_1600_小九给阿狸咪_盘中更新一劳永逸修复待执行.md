# 交接单：v8 盘中更新·一劳永逸修复（待阿狸咪执行）

- **日期**：2026-09-09 16:00（小九机下午下线）
- **交班方**：小九机（单位机，中国 IP）
- **接手方**：阿狸咪（家里机 alimi-cn）
- **目标**：13点盘中数据停滞 70min 事故的根治方案（A/B/C 全做，云端为主、双机兜底兜得住）

---

## 一、当前线上状态（已确认 OK，无需重做）

- ✅ **15:00 收盘数据已上线**：run #1451 completed success（15:17:55），HEALTH_CHECK 刷新 `2026-09-09 15:19:51`，CANDIDATE_QUOTES ok / age 22min，INDEX_QUOTES ok。
- ⚠️ 该轮 3 个 FAIL 为**回测 E 档产物**（BACKTEST_COMPREHENSIVE / BACKTEST_TDX / LHB_7D），属盘后算法链，与盘中实时卡无关，忽略。
- 小九机下午下线，**后续全部由阿狸咪接手**。

---

## 二、事故根因（13点空窗 70min）

1. 公共 `ubuntu-latest` 队列拥堵（run 1449/1450 卡在队列等待），13:00 slot 未被任何调度派发 → 盘中数据 70min 空窗。
2. `selfhosted` 接手时整轮 abort：intraday 任一非核心源（涨停/ETF/题材）失败 → `cloud_fetch_v8._has_critical_failures` 整轮非零 → job 被 kill → 后续 `api_push_raw/update_v8/health_check` 全 skipped。
3. 看门狗 `PROBE["intraday"]` 缺 `CANDIDATE_QUOTES.js` → 候选池陈旧看门狗无感。

---

## 三、已为你准备好的 4 个参考成品（审核后直推 origin/main）

**位置 A（坚果云，已同步，优先）**：`C:/Users/Administrator/Nutstore/v8_handover_20260909/`
**位置 B（main 备份，pull 即见）**：仓库根 `_handover_20260909/`

> 这 4 个文件是**小九已应用好编辑的成品**，你直接审核 → 直推即可，无需重新设计 diff。

| 文件 | 改动要点 |
|---|---|
| `cloud_fetch_v8.py` | `_has_critical_failures`：新增 `_CRIT_INTRADAY={"CANDIDATE_QUOTES","INDEX_QUOTES"}`，**intraday 仅这两个核心源失败才令整轮非零**；盘前/盘后/全量维持原语义。根治 selfhosted 整轮 abort。 |
| `v8_cn_fetch_cloud_selfhosted.yml` | 「📡 抓中国数据 → raw_data/」步**恢复 `continue-on-error: true`**（2026-08-12 曾移除）。单源失败不再 kill job，配合上方修改兜得住。 |
| `v8_cn_fetch_watchdog.yml` | ①**删 GitHub `schedule` cron**（改 WorkBuddy 自动化 `2d9121c8` 每15分 `workflow_dispatch` 驱动，去 schedule 静默丢失依赖）；②`PROBE["intraday"]` 补 `CANDIDATE_QUOTES.js`；③云端拥堵（`already_running>=2`）**改派 selfhosted**（`v8_cn_fetch_cloud_selfhosted.yml`）。 |
| `guard_v8_freshness.py` | ①`CORE_SOURCES` 加 `"CANDIDATE_QUOTES": 35`；②`_is_market_open` 过滤扩到 `CANDIDATE_QUOTES`（非交易时段不误报）；③新增 `_dispatch_cn_selfhosted()` + `_cloud_congested()`，自愈路由 `cat=="intraday" and _cloud_congested` 走 selfhosted。 |

---

## 四、推送到 origin/main 的操作（绕过本机 reset，必用 Contents API）

`git reset --hard origin/main` 周期任务会冲掉工作树改动，**不要靠 `git commit+push`**，用 Contents API 直推：

```bash
cd C:/Users/Administrator/Nutstore/v8_handover_20260909
PY=python   # 需能 import requests（缺则 pip install requests）
TOK_FILE=C:/Users/Administrator/AppData/Local/Temp/gh_tok.txt   # 确保有效 40位 PAT

# 4 个文件逐个直推（脚本自动处理 409 重取 sha）
python _put_main_file.py cloud_fetch_v8.py                                   ./cloud_fetch_v8.py                                   "fix(intraday): _has_critical_failures 仅核心源致命"
python _put_main_file.py .github/workflows/v8_cn_fetch_cloud_selfhosted.yml ./v8_cn_fetch_cloud_selfhosted.yml "selfhosted: 抓中国数据步恢复 continue-on-error"
python _put_main_file.py .github/workflows/v8_cn_fetch_watchdog.yml         ./v8_cn_fetch_watchdog.yml         "watchdog: 去 GitHub cron + 拥堵改派 selfhosted"
python _put_main_file.py guard_v8_freshness.py                             ./guard_v8_freshness.py                             "guard: CANDIDATE_QUOTES 阈值 + selfhosted 拥堵路由"
```

> 若阿狸咪机缺 `_put_main_file.py` 或 token，可复制坚果云同目录的 `_put_main_file.py`，或改用 `gh api` / GitHub 网页编辑直推。推送后等 2-3 分钟，复查 `HEALTH_CHECK.js` 刷新时间确认未被回滚（文件变小/时间倒退即为异常）。

---

## 五、已存活的自动化（无需改，阿狸咪机已加载）

| ID | 名称 | 当前状态 | 阿狸咪动作 |
|---|---|---|---|
| `195d8839` | v8 盘中统一调度器(20分·单源·双机兜底) | PAUSED（74077cbe） | 接手后按需 **ACTIVE**；每10分查新鲜度、漏 slot 自动追发、CLOUD_AGE>=25 改派 selfhosted |
| `2d9121c8` | v8 看门狗驱动(每15分·去 GitHub cron) | 待确认 | 接手后 **ACTIVE**（每15分 dispatch watchdog.yml，替代已删的 GitHub cron） |

> 这两自动化存在本地 WorkBuddy db，未被 git reset 冲掉，仍存活。

---

## 六、P0-1 风险（待阿狸咪确认后动，小九机不动）

- **去 `xiaojiu` label**：`v8_algo_cloud.yml:91` / `v8_algo_run.yml:128`。
- **前置条件**：alimi-cn 曾因 PATH 缺 `bash`/`git` 六连败。**务必先确认 alimi-cn 的 bash/git 在 PATH**，再执行去 label，否则 selfhosted 派发会失败。
- 小九机下线，label 保留 `xiaojiu` 不影响（小九机不再跑）。

---

## 七、小九机下线检查清单

- [x] 15点收盘数据已上线（HEALTH_CHECK 15:19:51）
- [x] 4 参考成品 + 交接单 + 推送脚本已交阿狸咪（坚果云 `v8_handover_20260909/` + main `_handover_20260909/`）
- [ ] 阿狸咪确认 `195d8839` / `2d9121c8` 状态（PAUSED→ACTIVE 由其决定）
- [ ] 阿狸咪执行第四节 4 文件直推
- [ ] 盘中观察主站更新 20 分一次、无 >35min 空窗

---

## 八、推送后验证（阿狸咪执行）

1. 盘中（09:30-15:00）主站更新是否 ~20 分一次、无 >35min 空窗。
2. `HEALTH_CHECK.js` 刷新时间 < 20min。
3. watchdog 日志出现「派发 cloud」或「🇨🇳 云端拥堵→改派 selfhosted」正常。
4. `guard` 日志：CANDIDATE_QUOTES 陈旧时派发 `cn_fetch(intraday)` 或 `cn_selfhosted(intraday)`。

---

*本报告仅供研究参考，不构成个人投资建议。*
