# 阿狸咪 → 小九｜2026-09-08 白天班作业清单

> **交接时间**：2026-09-08 08:06 ｜ **接手人**：小九（白天班 07:45–17:45）
> **背景文档**：`HANDOVER_2026-09-08_06-50_alimi-to-xiaojiu.md`（本班成果 + 第七章软失败补记，**请先扫一遍**）
> 今天是**交易日**。

---

## 〇、一句话状态

链路整体 **warn**（不是 fail）。100 个 `data/*.js` 里只有 **5 个超 24h**，其中只有 **FACTOR_LAB 是真待办**，其余 4 个已确认是低频/静态文件。

| 文件 | update_time | 陈 | 判定 |
|------|-------------|-----|------|
| **FACTOR_LAB** | 2026-09-05 14:05 | **2.8 d** | 🔴 **今天必补跑** |
| PORTFOLIO | 2026-08-31 13:04 | 7.8 d | 🟡 持仓手填，正常；但被 `_LOW_FREQ_FILES` 无条件放行了（见 P1-3） |
| SECTOR_PHASE_HISTORY | 2026-08-17 | 21 d | ⚪ 疑似停更/低频，需你确认是否还在用 |
| CONCEPT_ETF_MAP | 2026-08-18 | 21 d | ⚪ 映射表，低频；同样被无条件放行 |
| DO_NOT_DELETE | 2026-09-07 01:31 | 1.3 d | ⚪ 静态文档，无害 |

**已恢复的好消息**：CRDS `total_scanned=425`（09-08 00:20），不再是 0 —— 长期挂着的「CRDS 0 命中」**可以摘牌了**（我会在记忆里更新，你复核一眼）。

---

## 一、P0 —— 今天必须出结果

### 1. FACTOR_LAB 补跑（陈 2.8 天，因子实验室卡红）

- **真根因已修**：`v8/factor_lab_gen.py` 新增 `resolve_data_date()`（原 `last_trade_day()` 工作日返回今天 → baostock 无当日数据 → universe=0 → 保护性中止）。**不是冷启动慢，是日期取错。**
- **昨夜实跑没落盘**：02:56 起跑（ROE 阶段，BUDGET=1500），到 08:00 `data/FACTOR_LAB.js` 仍是 09-05 → **跑批中途被 kill 或异常退出，且没有留下可诊断的日志**。
- **你的任务**（本机跑，中国 IP，别丢云端 —— 云端美国 IP 抓 baostock/东财不可靠）：

```bash
cd /e/workspace/stock-scanner
# 建议先小预算探活，确认 universe 不为 0 再放大
V8_FLAB_BUDGET=200 python v8/factor_lab_gen.py 2>&1 | tee /tmp/flab.log
```

- **验收（三条全过才算完）**：
  1. `data/FACTOR_LAB.js` 的 `update_time` = **今天 09-08**；
  2. 日志里 universe 行数 **≥ 1000**（= 0 就是日期又取错了，直接回找我）；
  3. 缓存目录 `raw_data/kline_cache/` 有本轮新写入（判据：文件 mtime 是今天）。
- **若中途被 kill**：把 `/tmp/flab.log` 尾部 30 行贴进交接，别只写「跑了没出数」——这是昨晚最大的信息黑洞。
- 探活 OK 后，`V8_FLAB_BUDGET=1500` 再跑一轮补全；**入库时记得 `git add -f`**（`v8/*.py` 被 .gitignore 拦，`raw_data/kline_cache/` 已解除忽略但历史文件需显式 add）。

---

## 二、P0 —— 软失败告警改造的「首日实跑验收」（我昨夜改的，白天第一轮才见真章）

`e19fa9eb7`：4 个 `v8_cn_fetch*.yml` / 42 个软失败步，从「静默溜走」改成「软失败但强制 `::error::`」。

**白天会跑的定时链路**（CST）：

| 时间 | workflow | 你要看什么 |
|------|----------|-----------|
| 每小时 :10 / :40 | `v8_freshness_watch` | 是否出现 `v8-softfail:*` annotation |
| 每小时 :08 / :38 | `v8_health_patrol` | 同上 + job 是否仍绿（**软失败不阻断 = 预期行为**） |
| 13:00 / 15:00 / 17:00 | `runner_health_alert` | cn runner 是否在线 |
| 每 15 min | `v8_cache_buster_reconcile` | `?v` 缓存戳是否对齐 |
| 20:30 | `v8_cn_fetch_cloud_hosted` | 夜班的，但**你下班前先确认它被 dispatch 了** |

**验收口径（记住这个数）**：

> **annotation 数 = 软失败步数 + 1（汇总步）**
> 对不上 = 有人新埋了没登记的软失败步，立刻补 `id: soft_N` + 退出码守卫 + 汇总列表。

**看到告警的正确姿势**：job 是绿的但 Summary 里有「📢软失败汇总告警」→ 点开看是哪些步软失败 → **该补跑的补跑**，不要因为 job 绿就当没事。这正是这次改造要堵的漏洞。

---

## 三、P1 —— 白天有空就推（都是我盘点出来、昨夜没做的）

| # | 项 | 问题 | 建议动作 |
|---|----|------|----------|
| 1 | **串行网络风暴** | `fetch_fundamental_quality`（570 只 × 8 查询，**零超时**）、`refresh_dividend_cninfo`、`fetch_lhb`、`build_candidate_pool`、`backtest_rps` | 照 H_AUTO_BUY 的修法抄：取数统一走 `calc_stock_rps._query_kline`（mootdx→东财→baostock + 熔断）+ `ThreadPoolExecutor`，并登记 `SCRIPT_TIMEOUT_OVERRIDE` |
| 2 | **SMTP 告警渠道是哑的** | 配置里根本没有 SMTP 凭据 → 所有「邮件告警」实际不送达 | 要么补凭据（**放 `~/.workbuddy/`，禁止写进仓库**），要么改用 `::error::` + Summary 作为唯一真实渠道并删掉假邮件代码 |
| 3 | **`_LOW_FREQ_FILES` 无条件 OK** | PORTFOLIO 陈 7.8 d、CONCEPT_ETF_MAP 陈 20.6 d 都一声不吭 | 给低频文件设**真实阈值 + 登记理由**，别一律放行 |
| 4 | **SECTOR_PHASE_HISTORY 陈 21 天** | 疑似该文件的生成器已经不跑或已被摘链 | 查 `align_logic_ops.py` 和生成器是否还注册；不用就走「下架」流程，别留僵尸文件 |

> ⚠️ 改 `algorithms/run_algorithms.py` 的 `STAGES` **必须同步改 `ORDER`**（模块级 `assert(_STAGE_UNION == set(ORDER))` 是全链总闸，漏一个整链 import 崩，且报错只在 traceback 集合差里，很不直观）。这是我今早刚帮你填的坑（`gen_lhb_7d` 漏挂 ORDER）。

---

## 四、卫生（顺手 5 分钟）

1. **删 `...` 文件**：仓库根有个误产出的 `...`（health check 日志重定向，676B）。我 `rm` 和 `Remove-Item` 都删不掉（安全删除工具报 `0x80070057`），你手动清一下。**它未跟踪，不影响提交**，但看着脏。
2. **未跟踪文件**（无害，你决定去留）：`raw_data/algo_chain_report.json`（跑批报告）、`HANDOVER_阿狸咪_给小九_2026-09-06_周末夜班.md`（旧交接，建议归档到 `docs/ops/handover/` 后提交）。
3. **Nutstore 铁律**：显式 `git add <具体路径>`，禁 `-A`；编辑→提交→推送**单条命令原子**；仓库内不放 `.bak`。

---

## 五、时段交接（别踩线）

- **16:40 A 批**：算法链首段，你盯；**18:10 B 批**已在我班（18:00 起），所以 **17:30 前请写一份「白天→夜班」交接**，重点写：FACTOR_LAB 是否补出来、软失败首日验收结论、P1 推了哪几项。
- 今晚 **21:00 E 段**是 **CITIC PE 双卡 + `gen_lhb_7d` 的首次真跑**（之前因 P0 + 未挂 STAGES 从没跑成）→ 你 17:30 交接里提醒我一句，我夜里盯 `data/LHB_7D.js` 是否从 09-04 恢复、两张中信 PE 卡 update_time 是否刷新。

---

## 六、我这班（09-07 夜 → 09-08 早）交出来的东西

| commit | 内容 |
|--------|------|
| `10c598e40`…`b8f8eec86` | 中信 PE 双卡（数据 + 前端 + 接 E 段） |
| `3c7b46ce6` | 主题卡复古朴素改版 |
| `c54c5f44d` / `68428e9d3` | **P0**：`gen_lhb_7d` 漏挂 ORDER 导致整链 import 崩 + STAGES/ORDER 自校验 |
| `9ea7dea8f` | CITIC PE 全链防孤儿注册（weekly_cleanup 不误删） |
| `e19fa9eb7` | **42 个软失败步 → 软失败但强制 `::error::` 告警**（三层告警 + 顺带修 3 类真 bug） |
| `bb0cca5a1` | 交接补记（第七章软失败 + 三件套实测全绿） |

**三件套实测（07:10，全绿）**：py_compile 0 错 / index.html 23 段 + logic.html 6 段 inline `new Function` 0 错 / `align_logic_ops.py` EXIT 0 / data 三口径 100·103·103 一致 / 29 个 workflow YAML 0 错。

---

**交接人**：阿狸咪（2026-09-08 08:06）
**接手人**：小九
**本报告仅供研究参考，不构成投资建议。**
