# URGENT 交接｜小九 → 阿狸咪｜2026-08-11 19:28 第165轮看门狗

## 一句话结论
**六管线全绿 + 健康检查 ok42/warn0/fail0（罕见满绿，含 local_sync）；0 封告警邮件。**
趁无故障落实 159 轮遗留②，在 `v8_postclose_guard.py` 抓出并根治一个**与 159 轮同族的「基线获取失败即静默降级」致命缺陷**。

---

## 一、看门狗巡检（EXIT 0）

| 管线 | 状态 | 明细 |
|---|---|---|
| runner | ✅ | lemoncat-cn online=True, busy=False（免 heal） |
| cn_fetch | ✅ | completed/success @18:14（age 1.2h） |
| build_deploy | ✅ | completed/success @18:21（age 1.1h） |
| raw_data 新鲜度 | ✅ | 末次 commit 18:21（age 1.1h / 阈值 1.5h） |
| site | ✅ | HTTP 200 |
| auto_dispatch | ✅ | 数据新鲜、无 actionable 项，免派发 |

- **健康检查 @19:29：ok42 / warn0 / fail0，overall=ok**。42 张数据卡全新鲜、0 关键字段空值、部署同步一致。
- 本地 HEAD == origin/main = `e64aa9f5`（0/0），连 160-164 轮长期 FAIL 的 `local_sync` 本轮也是绿的。
- **0 封邮件**：19:28 已过夜间静音（22:00-07:00）截止，但无管线异常、无超阈 ≥120min 项 → 设计内正确静默。

---

## 二、根因级修复（一劳永逸）：盘后守卫「基线静默降级」

### 缺陷位置
`v8_postclose_guard.py` — `file_update_time()` / `stale_in_group()` / `main()`

### 根因
```python
# 旧版
def file_update_time(path):
    try:  ...  return update_time
    except Exception as e: return f"ERR:{e}"      # ← 失败降级为字符串

def stale_in_group(files, today):
    if ut and not ut.startswith(today) and not ut.startswith("ERR"):
        stale.append(...)                         # ← ERR 被排除在「陈旧」之外
```
远端 update_time 是守卫判定陈旧的**唯一基线**。一旦 contents API 全挂（网络抖动 / token 过期 / 403 限流），17 个文件全返 `ERR:` → 两个 stale 列表都空 → 脚本打印
**「✅ 盘后数据已是最新，无需补救」并 exit 0**。

**守卫 100% 静默失效，日志与退出码完全看不出异常。** 调用它的自动化
`automation-1785918538840` 每日只记录「退出码 0，无 stderr」→ 这个失效**永远不会被发现**，
盘后算法链一断就会像 08-09 那样静默陈旧数天。与 159 轮 `api_push_raw.py`
「防倒退基线静默变空」属同一家族。

### 旧版缺陷实测复现（用实时说话）
```
[2026-08-11 16:00:00 CST] 盘后守护检查
  fetch 产物陈旧: 0 个
  算法链产物陈旧: 0 个
  ✅ 盘后数据已是最新，无需补救          ← 17 个文件一个都没读到，却宣称最新
[TEST] dispatch=0 退出码=0 → ❌ 守卫已失效且无痕
```

### 修法（4 处）
1. **`api_get()` 加 3 次退避重试**，异常元组显式含 `http.client.HTTPException` + `json.JSONDecodeError`
   —— 157 轮教训：`IncompleteRead` 继承 HTTPException，**不是 OSError 子类**，不列出就会逃逸。
   404/401/403 等确定性状态码不重试，直接上抛交语义层处理。重试耗尽后**抛出，绝不返回降级值**。
2. **`file_update_time()` 改返回 `(kind, value)` 四态**：`ok` / `nofield` / `missing(404)` / `err`。
3. **`stale_in_group()` 改返回 `(stale, errors)`**：`missing`/`nofield` 计入陈旧（真问题，旧版会静默吞掉 404）；
   `err` 单列上报，**绝不并入「新鲜」**。
4. **`main()` fail-loud**：有 errors 时打印「拒绝判定『已是最新』，本轮检测结果不可信」+ 逐项原因；
   无确认陈旧项但基线不完整 → **`sys.exit(2)`**（不打印 ✅）；派发失败同样 exit 2 让上游可见。

### 验证（6 场景全 PASS + 真实回归）
| 场景 | 期望 | 结果 |
|---|---|---|
| ① 全部新鲜（正常路径回归） | exit 0 + 打印 ✅ | PASS |
| ② 基线全失败（网络） | exit 2，无 ✅ | PASS |
| ③ 403 限流 | exit 2，无 ✅ | PASS |
| ④ 404 远端缺文件 | 计陈旧并派发×2 | PASS |
| ⑤ 真陈旧（昨日） | 派发×2，exit 0 | PASS |
| ⑥ IncompleteRead 抖动 2 次 | 第 3 次重试成功，exit 0 | PASS |

- `py_compile` 通过；
- **真实 dry-run @19:37**：17 文件全部读到今日 update_time → `✅ 盘后数据已是最新` exit 0，**正常路径零回归**；
- 临时验证脚本 `_t_guard_*.py` 已删除，不入库。

---

## 三、git 与铁律遵守
- 开局本地 == origin `e64aa9f5`（0/0），无 ff 需求、无 stash 操作。
- 显式 add：`v8_postclose_guard.py`、`data/HEALTH_CHECK.js`(产物)、URGENT_164/165。
- **`index.html` 全程未碰**（本轮干净，无并行会话改动）；`HANDOVER_*.md` 未碰。
- 他方备份产物（`backup/`、`data/*.bak_*`、`raw_data/*.bak_*`）按铁律原样保留，不提交不删。

---

## 四、遗留 / 下轮关注
1. **URGENT_160~163 四份交接文件在仓库中消失**（现存 154-159 + 164/165）。这几轮 memory 明确记录「已落盘未 commit」，
   未 tracked 文件不会被 git 操作删除 → 疑被 `cloud_weekly_cleanup` 或某清理脚本误删。**建议下轮排查清理脚本的删除白名单是否误伤 URGENT_*.md。**
2. 建议把 `v8_postclose_guard.py` exit 2 接到告警通道（当前上游自动化只记录退出码，不发邮件）。
3. 云端高频小更新循环延续（本轮 1.1h 未推，较前几轮平缓）。
4. 134 轮 `except-pass` 改日志建议仍未落地。
5. `git stash` 残留 **32 条**（属备份，按铁律不擅自清理，需主人授权）。
6. algo_run/post_close 派发实测样本仍缺（158/159/165 连续全绿无可触发）。
