# 交接单 · 小九 → 阿狸咪（2026-09-10 17:17 下班交接）

> 小九已下线。本单为下班交接，涵盖：今日全部修复已上线状态、本机自动化"停/留"最终定稿（防踩踏/风暴）、致命坑、阿狸咪夜班待办。

## 一、今日全部修复已上线（main，无需再动）

| 改动 | main SHA / 位置 | 验证 |
|------|------|------|
| `?v` 单调部署令牌（`_UPDATE_TOKEN`，根治"数据回滚→浏览器陈旧缓存"） | `update_v8.py` 模块级 | 线上 FINAL_RECOMMEND_DATA `?v` 为时间令牌 ✅ |
| 最终推荐回退保护（纯 `post_close` 产物，非 `--category post_close` 构建跳过重写） | `update_v8.py` build() | 18min 后台监控：盘中 build 未洗回 04:57 真数据 ✅ |
| 看门狗误杀根治（删 `cancel_stuck_cloud_runs`，阈值按窗口细化） | `v8_cn_fetch_watchdog.yml` | 12:56 实跑通 ✅ |
| lemoncat `sleep 600`→实时轮询（单文件原子提，未整分支合并） | `.github/workflows/v8_cn_fetch_intraday_lemoncat.yml` `15fb8b2d` | `sleep 600` 残留=否，独立并发组 `v8-cn-fetch-slot` 就位 ✅ |

**本地仓库状态**：`HEAD = origin/main = cdefa5044`，无未推送提交，小九可安心下线。

## 二、本机自动化「停 / 留」最终定稿（防踩踏 + 防风暴）

### 🔴 PAUSED（5 个 — 踩踏/风暴源）
| ID | 名称 | 暂停原因 |
|------|------|------|
| `22505803` | 小九机盘中云端调度器(14档·20分) | 现 OFF-HOURS（仅 9-15 触发）；双源铁律"推前先停防互踢"，明日小九回归须 re-enable |
| `d1b92500` | D档派发(20:00·含最终推荐) | 阿狸咪夜窗，暂停防与阿狸咪互踩；功能由 `1788168934360`(19:25 守卫) 兜底覆盖 |
| `444581c6` | E档派发(21:05·回测批) | 阿狸咪夜窗，同上暂停 |
| `e3d25def` | 阿狸咪改码冲突护栏+选股三卡 | 会按阿狸咪改码推选股三卡，与阿狸咪互踩；按铁律阿狸咪只读不改码，暂停即安全 |
| `1785835481750` | 每小时紧急交接通道(auto-fix) | 自动修复类，误触发会雪崩（风暴源），暂停 |

### ✅ ACTIVE · 小九专属保留（7 个 — 主人明确"别停"）
| ID | 名称 | 为何必须保留 |
|------|------|------|
| `automation-1788172770889` | 龙虎榜/共振日历云端守卫(21:40/22:40) | **LHB 唯一保障源**（云端 cron 静默失效、阿狸咪按铁律 PAUSED），停了当晚共振日历断档 |
| `automation-1788168934360` | 盘后算法链云端守卫(19:25) | 确保每日算法链真实跑满（force_run 破静默闸门），覆盖 A/B/D/E 全链，是 D/E档暂停后的实际兜底 |
| `98e60e82` | 盘后链兜底守卫(21:45) | FINAL_RECOMMEND 当日落地核查 + cn 版兜底 |
| `automation-1786588094908` | 分红晚间(19:10) | 分红监督 + 应急兜底 |
| `automation-1785719698947` | 12/18点 交接文档更新 | 小九→阿狸咪交接机制，18:00 自动更新本交接单（生成自当日 memory） |
| `automation-1787575410637` | James-Stein 回测(19:30) | 小九本机本地回测，不推 main，安全 |
| `75e8cfe5` | A档派发(16:40·采集批) | 小九 16:40 窗口专属，非夜窗，保留 |

### 踩踏/风暴结论
- **唯一主线** = 云端 cron + 上述小九专属守卫。
- **阿狸咪按铁律 PAUSED**（不自动推）；若其手动接管夜班，则为唯一手动推源，不与小九自动化撞车。
- **D/E档暂停后由 19:25 守卫兜底** → 盘后链（含最终推荐 + 9 项回测）不中断。
- **风暴根除**：自动修复类（`1785835481750`）已暂停，不会自激重试雪崩。

## 三、🔴🔴 致命坑（务必遵守，否则今天修复全被洗回旧版）

**`fix/lemoncat-sleep600` 分支已过期，禁止整分支合并 / cherry-pick。**
- 该分支相对 main 有 **151 文件差异**，`update_v8.py` 是 **-12 行**（会把我刚上的 `?v` 令牌 + 最终推荐保护删掉），`index.html` +368，多为过期内容。
- 整合并入 = 今天全部修复被洗回旧版（最终推荐再次回退、缓存戳失效）。
- ✅ 已安全处理：lemoncat 这一处改动（sleep600→实时轮询）**已单文件原子提 main**（`15fb8b2d`），其余陈旧文件一律未动。
- 若后续要收口 lemoncat 分支：只提那一个 workflows 文件，绝不碰 update_v8.py / index.html / data/。

## 四、阿狸咪夜班待办（18:00 起）

1. **夜班主线**：盘后链由小九 19:25 守卫自动兜底跑满（含最终推荐 + 回测）。你如需手动介入，走云端 `workflow_dispatch` / `repository_dispatch`，勿本机直推 main。
2. **双源处置**：lemoncat(14档 cron·self-hosted) 与云端按铃现存，均只 dispatch cloud，由 `v8-cn-fetch-cloud` 并发组串行互斥 → 安全冗余；择一收敛即可（建议保留云端按铃）。
3. **LHB**：21:40/22:40 小九守卫自动核查+兜底，你无需动；若连续两晚缺失再人工排查 `v8_lhb_fetch.yml` runner。
4. **明日小九回归**：小九会 re-enable `22505803` 等盘中调度；你夜班结束前勿改 `update_v8.py` / `index.html`（防护栏误判，且我已暂停 `e3d25def`）。
5. **唯一风险监控点**：观察 `?v` 时间令牌是否每构建必变（证明缓存戳修复仍生效）；若发现最终推荐 `update_time` 倒退，优先查是否有人误合并 lemoncat 过期分支。

---
*本单由小九 17:17 生成，18:00 自动交接任务会基于当日 memory 增量更新。*
