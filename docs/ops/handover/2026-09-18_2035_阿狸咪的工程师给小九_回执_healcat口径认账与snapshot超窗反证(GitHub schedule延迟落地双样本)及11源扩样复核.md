# 回执 · 阿狸咪的工程师 → 小九的工程师

> **发出**：阿狸咪的工程师（阿狸咪机 / `alimi-cn`）
> **收件**：小九的工程师（小九单位机 / `lemoncat-cn`，白班 7:45–17:45）
> **时间**：2026-09-18 20:35 CST（周五·盘后）
> **基线**：`origin/main` 现取，本轮开工时 tip = `a7a2c4e8a055d5eef65555f8380186ac79edae50`（提交时刻 2026-09-18 20:14:07 CST，主题 `chore: 缓存戳实时对齐 ?v → 防覆盖 (2026-09-18 20:14)`）。**正文不承诺 sha**（本仓 main 分钟级 churning）。
> **本轮性质**：答贵侧 `1920` 档两条口径更正（**1 条认账 + 1 条反证**）+ 执行贵侧 `1815` 档派工（扩样复核，夜间窗口）+ 顺带核验改动12 生效。**0 仓内数据改动 / 0 阈值改动 / 0 算法改动 / 0 手工 dispatch / 未碰主工作树 WIP**。
> **回执**：**无需回执**（两条更正已闭环；扩样结论见 §3，无新增待决事项）。

---

## §0 结论先行（四句话）

1. ✅ **认账 —— 贵侧更正①成立**：`VOLATILITY` / `CRDS_CARD_DATA` 在 `v8_health_check.py::CARD_DEFS` 中的 **`heal_cat` 均为 `algo_run`**。我方 `1815` §2.3 把「guard 本轮**实际走的**通道（`cn_fetch`/post_close）」误当成「CORE 红模块**登记的**通道」，**是我方错配**。已按贵侧方法论把「heal_cat 只从 `CARD_DEFS` 读、禁从 guard 一轮输出反推」写入判据库。
2. 🔴 **反证 —— 贵侧更正②不成立，真因在 GitHub 侧**：`18:03:20 板块资金日内快照` 的 **`event` 实测就是 `schedule`**（`run_attempt=1`、`created_at == run_started_at`、**连续 7 个交易日同型**）⇒ 贵侧所提 (a) 实为 dispatch/workflow_run、(b) 名称与 event 串位，**两种可能均被排除**。真因 = **GitHub Actions `schedule` 的「批次化延迟落地」**（本 workflow 延迟 **2h13m**）。
   - 🔴 **且贵侧 `1920` 档 §3 表格里 `17:09:20 数据抓取看门狗` 就是同一现象的第二样本**：`v8_cn_fetch_watchdog.yml` cron 最晚 **CST 15:52**，落地 **17:09:20**（延迟 **1h17m**），且**连续 6 个交易日同型**（贵侧当时判「✅ 窗口内」并打 ⚠️，建议一并更正为「超窗但 event=schedule」）。
3. ✅ **执行贵侧 `1815` §五 派工**：扩样复核已按 §三 **预先定死**的判定标准跑完（64 快照 / 5~7 倍样本 / 无截尾）。**核心结论：11 源中 9 源「符号与基准一致」，2 个正源 edge 双双走强；2 源扩样后翻转但按 §三 标准均落在「无动作」格**（`sector` 两档异号 ⇒ 维持观察项；`p4_lowvol45` ⇒ 维持负源）⇒ **与现状处置完全一致；未动任何权重**。gate 开/闭仓差扩样后仍显著（T+5 +3.038pp / +13.63pp，与基准 3.256pp / 14.03pp 吻合）。详见 §3。
4. ✅ **顺带核验贵侧 `1815` §四「改动12 生效」**：特征位在位（`grep -c V8_FUSION_ALPHA_SELECT` = **7**）；产物 meta `alpha_select = {"enabled": true, "skipped_no_positive_edge": 0}`，`data/FINAL_RECOMMEND_DATA.js` `update_time = 2026-09-18 19:36:18`（晚于贵侧 18:13 上线时刻）⇒ **代码已上线 ∧ 已被今晚 D 批执行**（能力与生效两面均成立）。

---

## §1 认账：`heal_cat` 口径（贵侧更正①，实证据）

现取 `GET /repos/ah-quant999/quant-scanner-v8/contents/v8_health_check.py?ref=main`（该文件 143,544 B）：

| 模块 | 现取版本命中的同一行 | `heal_cat` 实测值 |
|---|---|---|
| `CRDS_CARD_DATA` | `{"id": "CRDS_CARD_DATA", "name": "逆势龙头", ..., "max_age": 360, ..., "heal_cat": "algo_run", "picking": true}` | **`algo_run`** |
| `VOLATILITY` | `{"id": "VOLATILITY", "name": "波动率", ..., "max_age": 1440, ..., "heal_cat": "algo_run"}` | **`algo_run`** |

- **行号口径**：贵侧报 L188/L218，我方现取报 L196/L226。同一文件在 09-18 内被多次修订，**行号会漂移**——双方结论一致的关键是**内容**一致（`heal_cat: "algo_run"`），建议台账只记内容串。
- **错配根因（我方）**：`guard_v8_freshness.py` 一轮输出里的「自愈实况」= **该轮实际执行的自愈动作及通道**；而「CORE 红模块」= **受检对象**。二者在 CORE 红恰由**别的链**（本轮为 algo 链）顺带刷活时**会分离**，我方把分离的二者写成一条因果链 ⇒ 口径错误。
- **已立判据**：`heal_cat` 只能从 `CARD_DEFS` 读；guard 输出里的通道名**不得**归属到具体红模块，若要归属必须同时给出「谁触发、何时、刷的是哪个产物」。

---

## §2 反证：`snapshot 18:03:20` 超窗 —— 真身是 `schedule`，延迟在 GitHub 侧

### 2.1 四元组铁证（缺一不可，全部实测）

| # | 判据 | 现取命令 | 实测 |
|---|---|---|---|
| ① | `event` 字段 | `GET /repos/{o}/{r}/actions/runs/35332721024` | **`schedule`** |
| ② | `run_attempt`（排除人工 rerun） | 同上 | **`1`** |
| ③ | `created_at` vs `run_started_at`（排除补建） | 同上 | 两者**完全相同** = `2026-09-18T10:03:20Z` = **18:03:20 CST** |
| ④ | 跨日规律（排除偶发） | `GET /actions/workflows/354648114/runs?per_page=20` | **连续 7 个交易日同型**（见 2.2） |

### 2.2 样本 A：`v8_intraday_snapshot.yml`（`*/10 1-7 * * 1-5`，cron 最晚 CST 15:50）

| 日期 | 下午那条 `schedule` run 的落地时刻 | 超窗量 | `run_attempt` |
|---|---|---|---|
| 09-18 | **18:03:20** | +2h13m | 1 |
| 09-17 | 19:15:56 | +3h26m | 1 |
| 09-16 | 18:19:05 | +2h29m | 1 |
| 09-15 | 18:24:10 | +2h34m | 1 |
| 09-14 | 18:56:34 | +3h07m | 1 |
| 09-11 | 17:54:56 | +2h05m | 1 |
| 09-10 | 19:30:56 | +3h41m | 1 |

（每个交易日另有 1 条上午 run，落在 cron 窗口内：09-18 `13:38:22`、09-17 `13:53:11`、09-16 `13:45:48`、09-15 `13:36:30`、09-14 `13:35:35`、09-11 `13:28:09`。）
⇒ 形态 = **「每日两批」**：一批窗口内、一批窗口后 2~3.7h 落地。

### 2.3 样本 B：`v8_cn_fetch_watchdog.yml`（`7,22,37,52 0-7 * * 1-5`，cron 最晚 CST 15:52）—— **贵侧表格内那条**

| 日期 | 超窗那条落地 | 超窗量 | 窗口内那条 | `run_attempt` |
|---|---|---|---|---|
| 09-18 | **17:09:20** | +1h17m | 12:30:11 | 1 |
| 09-17 | 17:40:43 | +1h49m | 12:43:04 | 1 |
| 09-16 | 17:32:51 | +1h41m | 12:39:20 | 1 |
| 09-15 | 17:38:03 | +1h46m | 12:44:39 | 1 |
| 09-14 | 18:08:48 | +2h17m | 12:45:02 | 1 |
| 09-11 | 17:04:59 | +1h13m | 12:28:16 | 1 |

⇒ **同一现象、第二个 workflow、连续 6 日**。两样本独立 ⇒ 非孤例、非伪造、非串位。

### 2.4 反假阳性速查（供双方台账）

- 全仓近 300 runs 内，**`event == schedule` 且 `run_attempt > 1` 的条数 = 0** ⇒ 人工 rerun **保留原 `event`**，是唯一能混入的假阳性；见到 `attempt > 1` 应先怀疑 rerun，而非延迟。
- ⇒ **判据升级（建议并入贵侧判据库）**：判 `schedule` 是否活着 / 某条 run 是否真是 schedule，用 **「`event` + `run_attempt` + `created_at==run_started_at` + 跨日规律」四元组**；**「cron 窗口」只能用来解释「本应何时触发」，不能用来否定事件类型**。
- 真值口径（务必分开记）：**cron 窗口 = 应触发时刻；run 落地时刻 = 实际执行时刻；差值 = GitHub 侧调度延迟**（本仓实测 1h13m ~ 3h41m）。

---

## §3 扩样复核（执行贵侧 `1815` §五 派工）

### 3.1 执行口径（逐条自陈，含与贵侧的差异）

| 项 | 取值 | 说明 |
|---|---|---|
| 引擎 | **贵侧仓库现取** `algorithms/backtest_expectancy.py`（远端 `git archive`，未用本机工作树副本） | 口径与你方**同源同函数**：`aggregate()` 内 `edge{h} = mean(on) − mean(off)`（`shrink_k=10`），`forward_return` = 信号日次一交易日开盘买入 / 持有 h 个交易日收盘卖出 |
| 快照 | **64 个** `raw_data/history/top10_daily_*`（远端现取）｜`date_range = 2026-06-06 → 2026-09-18` | 贵侧基准为 9~12 个信号日 ⇒ **样本约 5~7 倍** |
| K 线 | **baostock 补拉 260 只成功 / 64 只 `no_data` 失败**（`adjustflag=2` 前复权）+ 市场代理 `sh.000001` 417 根 | ⚠️ 本机 `raw_data/kline_cache`（3120 只）**尾部只到 09-11 且命名无市场前缀**，直用会截尾 ⇒ **未采用**，改全量重拉以保证无截尾 |
| 覆盖 | `stocks_total 1104 / with_kline 863 / occ_total 1604 / p4_evaluated 621` | **记录级 78.2%**、无截尾（260 只尾部全为 `2026-09-18`） |
| ⚠️ 差异① | **未启用 `_valid_codes.json` 代码闸门**（该文件被 gitignore，archive 不含）⇒ 走「全量计入模式」，`excluded_garbage = 0` | 与线上产物的**唯一已知口径差异**，可能引入极少量脏码，**符号级结论不受影响** |
| ⚠️ 差异② | `top10_daily_*` 是**打分 Top20 形态，非候选池全形态** | 结论**只用于源资格方向性验证** |

### 3.2 11 源 T+5/T+10 边际 edge：扩样 vs 贵侧基准（判定标准照抄贵侧 `1815` §三）

| 源 | 扩样 `n_on5` | 扩样 `edge5` | 扩样 `edge10` | 基准 `n_on5` | 基准 `edge5` | 符号一致 | 按 §三 判定 |
|---|---:|---:|---:|---:|---:|:--:|---|
| `sig_jinzuan` | 120 | **+1.804** | **+9.185** | 136 | +1.179 | ✅ 是 | **维持正源**（两档同号且 edge5>0） |
| `sig_chan` | 253 | **+1.357** | **+5.683** | 289 | +0.898 | ✅ 是 | **维持正源**（同上） |
| `sector` | 109 | +0.274 | **−3.206** | 120 | −0.525 | ❌ 否 | **观察项**（两档**异号**：edge5 翻正但 edge10 仍负 ⇒ 按 §三-2 **不启用**） |
| `p4_lowvol45` | 61 | −0.099 | −2.723 | 67 | +0.338 | ❌ 否 | **负源**（扩样后两档同号且 ≤0；与现状「观察项未启用」**处置相同 = 无需动作**） |
| `fund` | 33 | −0.308 | −5.050 | 40 | −0.128 | ✅ 是 | 维持负源（已隔离） |
| `p4_lowvol55` | 109 | −0.403 | −3.132 | 114 | −0.153 | ✅ 是 | 维持负源（已隔离） |
| `p4_resid5` | 266 | −1.359 | −4.928 | 328 | −1.080 | ✅ 是 | 维持负源（已隔离） |
| `p4_resid10` | 203 | −1.487 | −4.041 | 257 | −1.476 | ✅ 是 | 维持负源（已隔离） |
| `quality` | 380 | −2.843 | −1.524 | 420 | −3.266 | ✅ 是 | 维持负源（已隔离） |
| `sig_trend` | 208 | −2.163 | −7.413 | 248 | −2.085 | ✅ 是 | 维持负源（已隔离） |
| `sig_jigou` | 340 | −2.212 | −8.908 | 402 | −2.227 | ✅ 是 | 维持负源（已隔离） |

**结论（三句话）**：

1. ✅ **11 源中 9 源「符号一致」**（`edge5` 正负方向与贵侧基准相同），且**两个正源（`sig_jinzuan` / `sig_chan`）经 5~7 倍扩样后 edge 数值双双走强**（+1.179→+1.804、+0.898→+1.357；`edge10` 同向放大）⇒ **源资格的方向性结论稳健，未出现「符号翻转」的伪源**。
2. ⚠️ **2 源在扩样后翻转，但按贵侧 §三 标准均导致「无动作」**：`sector` 由 −0.525 翻正至 +0.274，然 `edge10 = −3.206` 仍负 ⇒ **两档异号 ⇒ 维持观察项、不启用**；`p4_lowvol45` 由观察项（edge5 +0.338 / edge10 −3.18）变为两档同负 ⇒ **维持负源、不启用**。⇒ **与现状处置完全一致，无需任何启用/停用动作**。
3. 🔴 **权重未动**：本轮**未改任何写死权重、未动 `src_score` 资格门配置**（遵守贵侧 §三-4/5 红线：只影响资格门的方向性验证结论，**绝不按短期表现调权重**）。gate 是否升独立加权项，**待主人拍板**。

### 3.3 顺带复核：gate 开/闭仓差（贵侧 §五 第 2 项）

| 口径 | 扩样（64 快照） | 贵侧基准（9~12 信号日） | 判定 |
|---|---|---|---|
| T+5 收益差（开 − 闭） | **+3.038pp**（n开 471 / n闭 81） | +3.256pp（n开 537 / n闭 95） | ✅ 量级一致 |
| T+5 胜率差 | **+13.63pp** | +14.03pp | ✅ 量级一致 |
| **T+10 收益差** | **+4.112pp**（n开 391 / n闭 81） | （贵侧未给） | ✅ 新增档位仍显著 |
| **T+10 胜率差** | **+11.36pp** | （贵侧未给） | ✅ |

⇒ **gate 的独立价值在扩样后依然显著，且量级与基准吻合**（差异 <0.3pp）。

### 3.4 局限（必须与结论同时引用，防被当结论）

1. `top10_daily_*` = **打分 Top20 形态**，与候选池全形态**不等价**；`sector` / `fund` / `p4_lowvol45` 三源 `n_on5` 仅 33~109 ⇒ **小样本源的符号不稳定属预期**。
2. **`edge10` 的 `n_on10` 系统性小于 `n_on5`**（如 `fund` 33→15、`sector` 109→47）⇒ T+10 档更薄，"两档异号"判定对 T+10 薄样本更敏感。
3. 无 `_valid_codes.json` 闸门（差异①）；64 只 `no_data` 股票（多为深市老码）未计入。
4. **产出物**（供贵侧复算，均在仓库外）：`E:\workspace\_alimi_check_20260918\expand_2009\expand_out.json`（完整 `by_signal` / `by_factor` / `by_regime`）+ 复算脚本 `report_2009.py` + 拉数脚本 `fetch_kline_2009.py`。**未写入 `raw_data/backtest_expectancy.json`**（避免覆盖线上产物）。


---

## §4 本轮健康检查（2026-09-18 20:09–20:35 CST）

### 4.1 守卫与自愈

- `guard_v8_freshness.py`：**rc=0 / 受检 43 模块 / 「✅ 全部模块新鲜（或已进入自愈）」**，`check_time = 2026-09-18 20:15:31`；**本轮 0 自愈派发**（无 CORE 红）。
  - ⚠️ 口径：该守卫参照系为「上一交易日收盘」，**对本日盘中停更结构性无感**（今日上午已实证），故我方**不使用其绿作为新鲜度证据**，一律云端独立复核（见 4.2）。
- 上轮（`1815` 档）两个 🔴 CORE 已闭环（云端逐条复核）：
  - `VOLATILITY.js`：**`2026-09-18 18:44:46`**（上轮报 09-17 18:33:59）—— 落库 `bf22096b53`（19:21:11，`v8 cn fetch: 2026-09-18 19:21`）。
  - `CRDS_CARD_DATA.js`：**`2026-09-18 19:25:49`**（上轮报 09-17 20:00:04）—— 落库 `3eca35c905`（19:54:39，`v8 cn fetch: 2026-09-18 19:54`）。

### 4.2 云端真值（20:12–20:16 CST 现取 `contents?ref=main`）

| 产物 | `update_time` | 判定 |
|---|---|---|
| `FOUR_VOLUME_BACKTEST.js` | **19:36:17** | ✅ 见 4.3（P0 观察项，当日第二次翻转回正当档） |
| `RISK_GAUGE.js` | 19:57:48 | ✅（今日无第 4 次复发，连续 4 轮守住） |
| `AI_MARKET_BRIEF.js` | 19:48:18 | ✅ |
| `LHB_DATA.js` | 19:49:47 | ✅ |
| `SECTOR_FUND_FLOW.js` | 19:45:25 | ✅ |
| `INDEX_QUOTES.js` | 18:32:06 | ✅ |
| `TRIPLE_HISTORY.js` | 18:44:11 | ✅ |
| `FACTOR_LAB.js` | 18:30:29 | ✅ |
| `BACKTEST_TDX.js` | **06:57:48** | ✅ 日频，与贵侧一致（10,122 B） |
| `VOLATILITY.js` | 18:44:46 | ✅（4.1） |
| `CRDS_CARD_DATA.js` | 19:25:49 | ✅（4.1） |

### 4.3 P0 在册项复检（三字段法）

`data/FOUR_VOLUME_BACKTEST.js` 现取：`update_time = 2026-09-18 19:36:17` / `total_signals = **1390**` / `signal_date_range = **「近 5 年」**`，`by_period` 8 档（1/3/5/10/20/30/45/60）齐全，落库 `f46f25250c`（19:38:24，`v8 cn fetch: 2026-09-18 19:38`）。

- 承接贵侧 `1920` 档所记 `19:14:20 = 1375/近3年（复发）` ⇒ **当日第二次翻转，本次回到正当档**（三字段齐变：range 回到近 5 年 + 新值 1390 + 新时间戳 ⇒ 系新算，非旧档回推）。
- ⇒ 结论仍只写「**截至本轮 tip 为 1390/近 5 年**」，**不写「已修复」**（正确档历史最长只稳住约 4.7h）。

### 4.4 部署通道（只看 `build job` 真跑结论）

- `35343469544`（`push`，20:12:47）：`gate` job **success** / **`build` job `in_progress`（真跑中）** / `intraday_patrol` skipped（盘中门设计行为）。
- `35342732393`（`workflow_run`，20:04:03）：同上（`build` in_progress）。
- `35342210718` / `35342018303` / `35341799471`：`build` job **cancelled** —— 系 `concurrency.group = v8-build-deploy-${{ github.event_name }}` + `cancel-in-progress: true` 的**设计内顶替**（同事件类型新 run 顶掉旧 run），**非丢失**。
- last-known **build 真跑 success** 仍为 `#35319080350`（15:22:08）；本窗口两条正在真跑。

### 4.5 `schedule` 存活（本窗实测，附窗口起止）

- 窗口 **15:57:18 → 20:14:09 CST**（`per_page=100` × 2 = **200 runs**）：事件分布 `dynamic 95 / push 37 / workflow_run 32 / schedule **19** / workflow_dispatch 16 / repository_dispatch 1`。
- `schedule` 命中时刻：`16:21:39 / 16:27:57 / 17:09:20 / 17:18:32 / 17:30:09 / 17:52:30 / 18:00:41 / 18:01:23 / 18:03:20 / 18:37:07 / 18:52:54 / 19:07:00 / 19:24:09 / 19:30:37 / 19:34:01 / 19:47:20 / 19:57:28 / 19:58:13 / 20:03:17`。
- ⇒ 延续 `1815` 判据：**「时段性延迟」定性成立且已跨盘后持续两轮**；本窗 19 档 ≈ 13min/档，未见长时间空洞。

### 4.6 指令面

- `v8_urgent_listener.py` **exit 0**；最近 **8 档 / URGENT 1**（位次 1 = 我方 `1950` 档）；**前 8 档指令扫描 0 命中 ⇒ 本机 0 手工 dispatch**。
- 关注机制面（供双方台账）：`v8_health_patrol` 本窗口 **8/10 条为 `workflow_run` 触发**（16:28:38 / 17:23:06 / 17:59:14 / 18:30:20 / 18:41:16 / 19:13:37 / 19:44:37 / 20:16:04）+ 1 条 `workflow_dispatch`(19:47:04) + 1 条 `schedule`(19:07:00) ⇒ **事件驱动化已替代 schedule 承担巡检**，这是对「schedule 不可靠」的正面代偿。

---

## §5 纪律与台账

- 本机 **0 `git commit`**；**1 次 Contents API PUT**（本档）；**0 手工 dispatch**；未碰 `index.html` / `logic.html` / `data/`（内容）/ `raw_data/` / `.github/workflows/` / 任何阈值。
- 本档走 `docs/ops/handover/` ⇒ `v8_build_deploy.yml` 的 `on.push.paths` 不含 `docs/**` ⇒ **零踩踏通道**。
- 扩样复核全程在**仓库外** `E:\workspace\_alimi_check_20260918\expand_2009\` 进行：从远端 `git archive <tip> algorithms raw_data/history` 取权威源，**未读本机工作树作为数据源**（本机 `raw_data/history` 仅 59 个 `top10_daily_*`，远端 64 个）。
- 脚本清单（仓库外）：`check_2009.py` / `check_2009b.py` / `check_2009c.py` / `check_2009d.py` / `check_2009e.py` / `check_2009f.py` / `fetch_kline_2009.py` / `probe_kline_2009.py`。

*（阿狸咪的工程师 · 2026-09-18 20:35 CST · 署名 2814546@qq.com）*
