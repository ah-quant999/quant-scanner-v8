# P5 口径修正引发 CI 硬阻断 —— 根因取证与处置交接

**撰写方**：小九机 · 2026-09-20 13:25 CST
**事件级别**：P1（deploy 链被硬阻断，站点未受损）
**接收方**：阿狸咪机 / 后续会话

---

## 一、一句话结论

`[10/10] index 核心标记守卫`（阿狸咪 2026-09-20 11:35 CST 新增）登记的核心标记
`为什么「已停用」`，锚定的是 **P5 卡旧口径文案**；小九 12:18 CST 按主人口径修正该文案后，
标记 0 命中 ⇒ 守卫判 FAIL ⇒ **deploy 链自 12:18 起连续阻断 7 次**。

**定性：既非覆盖事故，也非守卫设计错误，而是「标记登记粒度过细」** ——
把一条**口径必然会被修正的文案**登记成了不可变锚点。

---

## 二、时间线（全部为实测取证，非推断）

| 时刻 (CST) | commit | index.html | 标记 `为什么「已停用」` | 说明 |
|---|---|---|---|---|
| 11:35 | `29a9b66200` | 1376606 B | ✅ x1 | 阿狸咪新增 [10/10] 守卫 + 真源清单 |
| 12:14 | `cd937a2ec1` | — | ✅ x1 | **小九第一版补丁的基底** |
| 12:18 | `c9876b2efe` | 1379616 B | ❌ **x0** | 小九第一版补丁上线 → 标记消失 |
| 12:18 起 | — | — | — | `构建部署` step10 FAIL，连续 7 次（#7167–#7173） |

**消失点取证命令**（可复跑，不依赖本文时刻）：
```python
# 逐 commit 查标记命中（见 v8_q_mk_when.py）
GET /repos/ah-quant999/quant-scanner-v8/commits?path=index.html&per_page=20
# 对每笔的 index.html blob 统计 "为什么「已停用」" 命中数
```

---

## 三、根因细节

### 3.1 标记锚的是旧口径原文

`29a9b66200` 时点 index.html 内该行为：
```
+ '<br><b style="color:#cbd5e1;">为什么「已停用」：</b>判定 = IR_OOS &gt; 0.3 <b>且</b> 年度跑赢率 ≥ 80%'
```

小九第一版按主人 2026-09-09 22:31 决策原文修正为：
```
'（门槛：连续 5 年窗口内 ≥4 年达标）'
```
⇒ 旧串 `为什么「已停用」` 整段被替换，标记归零。

### 3.2 守卫的红字原文

```
❌ [10/10] index 核心标记守卫: 1/35 条核心标记在 index.html 中消失
   （疑似被落后基线覆盖，或功能整块删除而未同步清单）：为什么「已停用」
🚫 1 项校验失败 → 阻断 deploy！
```

### 3.3 为什么 force:false 与推送侧断言都没拦住

- 小九的推送是**正常快进**（父提交 = 当时 tip），`force:false` 本就允许；
- 推送侧断言「线上 blob == 补丁基底」**通过**了（基底 `cd937a2ec1` 确实含标记）；
- ⇒ 断言只能防「基线落后」，防不了「**合法改动触发了过细标记**」。

**这恰恰说明守卫是有效的** —— 它成功拦截了一个「看似正常」的改动，只是因为标记选得过于具体，
拦错了对象。**守卫机制本身应保留，只需修标记选择。**

---

## 四、已执行的处置

### 4.1 清单修复（已推送）

| 项 | 值 |
|---|---|
| 文件 | `docs/ops/index_protected_markers.txt` |
| commit | `7ce4827938b6` |
| 旧行 | `为什么「已停用」\|P5 停用卡原因说明（小九 2026-09-20）\|2026-09-20` |
| 新行 | `连续 5 年窗口内 ≥4 年\|P5 判定口径核心表述（连续5年窗口·主人2026-09-09决策原文）\|2026-09-20` |
| 验证 | 34/35 → **35/35** 全在位 |

**为什么不删标记**：删了就永久丢失这道防覆盖能力。
改为登记**不随口径漂移失效**的锚点 —— 新锚点是口径**核心语义**（也是主人决策原文），
口径若再变，此处仍会红（符合守卫「有意删除须同步清单」的设计目的）。

### 4.2 index.html 侧（已推送）

| 项 | 值 |
|---|---|
| 改动 | P5 note 拆为「回测达标情况（全部候选因子）」+「本卡实际计分范围：仅 2 个」 |
| commit | `d459de23af54`（第二版） |
| 前序 | 第一版 `c9876b2efe`（why 行内新口径 + `_fwAll/_fwN/_fwPass/_fwPassNames/_fwWorst` 统计块） |
| 验证 | Node vm 四场景真执行通过；标签闭合增量全部来自补丁自身 |

---

## 五、遗留项（未完成，必须交代）

### 5.1 `factor_walkforward.json` 仍是旧产物 ⚠️

```
实测 update_time = 2026-09-19 18:25:37
8 个因子 verdict 全 FAIL
win_years / win_pass_n / win_best_ok 三字段全为 None
```

**原因**：`#1991`/`#1992` 虽 `conclusion=success`，但 **step9「运行盘后算法链」是 skipped**
—— 闸门**合规拒绝执行**（红灯拒绝，非故障）。

> 🔴 **这是「run.conclusion=success ≠ 工作 job 真跑了」的又一次实证。**
> 判「有效 run 数」必须排除 `gate` 并检查 step9 是否为 skipped。

**⇒ 脚本改了 ≠ 产物已用新脚本重跑。** 前端因此走**降级分支**，
会如实显示「（产物未含窗口字段，待算法重跑）」—— 这是**有意设计，不是 bug**。

### 5.2 待办

1. 等 B 批就绪后**显式派发 `stage=E`**（不可绕过闸门强推）；
2. 推后实测产物含 `win_years/win_pass_n/win_best_ok` + 4 因子转 PASS；
3. 前端降级文案自动消失（无需再改前端）。

---

## 六、给阿狸咪的建议（非要求）

**[10/10] 守卫的标记选择原则**建议补一条：

> 标记应锚定**结构性不变项**（`id="sec-*"` / `window.__render*` / 数据源全局名），
> **避免**锚定「会随口径/文案迭代而改写的展示字符串」。
> 当前 35 条中 34 条已是结构性锚点，仅此 1 条是文案型 —— 已修为语义型。

另建议：守卫红字增加**「该标记最近一次被有意修改的 commit」**提示，
使受理方能一眼区分「覆盖事故」与「合规改写未同步清单」。

---

## 七、现取命令（避免本文过期）

```bash
# 1) 当前 tip
curl -s -H "Authorization: Bearer $TOKEN" \
  https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main

# 2) 清单当前内容（核 35 条）
curl -s -H "Authorization: Bearer $TOKEN" \
  https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/docs/ops/index_protected_markers.txt

# 3) 最近构建部署 run 成败
curl -s -H "Authorization: Bearer $TOKEN" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/runs?per_page=10"

# 4) 产物字段（核 win_best_ok 是否出现）
curl -s -H "Authorization: Bearer $TOKEN" \
  https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/raw_data/factor_walkforward.json
```

---

## 八、边界声明

- 本文所有时刻均为**实测取证值**（GitHub API 返回的 `author.date` / run `created_at`），非估算；
- 不写「已上线」作为结论 —— **判上线一律现取现验**；
- 不写本文自身所在 commit sha（写死即过期）；
- 「脚本已改」与「产物已重跑」在本文中**严格分列陈述**，不混为一谈。
