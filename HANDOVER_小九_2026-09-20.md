# HANDOVER · 小九 → 阿狸咪 · 2026-09-20（周日）

> **更新于 2026-09-20 12:00（12:00 槽位自动更新）**
> 数据采集时刻：**2026-09-20 12:02–12:07 CST**。本文所有读数均为该时刻实测，**不写预测式表述**。
> 若你打开时已过数小时，**以文末「现取命令」重新取值为准**，勿直接引用本文数值。

---

## 一、背景

今天是**周日（非交易日）**。小九白天班（7:45–17:45），阿狸咪夜间+周末。今日小九侧主要工作两条线：

1. **看板三问三改**（主人 09:5x 发 3 张截图）—— P2 因子卡口径陈旧（P0 级）、P5 停用卡原因缺位、全市场画像卡看不懂；
2. **一次覆盖事故的发现与恢复** —— 阿狸咪侧推送时基线落后，把小九 02:43 的改动整块顶掉，小九 11:00 前完成双方改动合并恢复。

同时，凌晨/上午双方有大量**交接入站回执哨兵**轮次（我方 5 轮回执 + 阿狸咪 8 份来件），围绕「心跳停摆」「sha 不可核验」「调度器停摆」三项互相纠错，**我方已正式撤回一条假警报**（详见第四节）。

---

## 二、今日已完成改动（含真实 commit SHA + 影响）

### 2.1 已上线（小九侧）

| # | 改动 | commit | 影响 |
|---|---|---|---|
| A | **P2「每日因子计算」改三级取值链**（回测产物 → 源码硬表镜像 → 未载入） | `d69fcec4c` | 修正卡片量级夸大 5~10 倍（-4 分显示成 -18 分） |
| B | **P5「walk-forward 达标因子」补「为什么停用 + 停用≠废弃」** | `d69fcec4c` | 卡文案补齐，回答主人「P5 是不是可以删干净」 |
| C | **全市场画像卡降格为「已否决 · 证伪留档」** | `d69fcec4c` | 顶部补导语，说明「画像因子只在小池子有效」 |
| A/B/C 交接档 | 三问三改说明 | `1b84ce769` | `docs/ops/handover/2026-09-20_1100_xiaojiu_to_alimi_lookat3_p2p5portrait_fix_and_overwrite_incident.md` |
| D | **「隔夜美股强弱·映射 A/港股」卡按强弱四档重排**（强势/中期强·短期回调/短期反弹 全展开，弱势折叠） | `4d01769ec` | 新增 `TIERS` + `_sortTier`（反向置顶 → d5 降序 → d20 降序）；86 项条目守恒 |
| D 交接档 | 四档重排说明 | `b7e201cef` | — |

**改动 D 关键判据**：唯一改动点 `window._fillUsHkMap`；`DEF_ROWS=10` 保留不删；数据侧（tag/mom/inverse）零改动。
线上终检：Pages 直取 **1,367,897 B**，`_fillUsHkMap` 函数体 **md5 `d0b0befb20bf3418d04162e3304eba4e`** 两侧一致。

### 2.2 覆盖事故（今日最重要发现，已恢复）

**事实（UTC）**：

| 时刻 | commit | 内容 | index.html |
|---|---|---|---|
| 02:43:50 | `742370d85` | 小九：P2+P5+画像三改 | `+76/-18` |
| 02:53:32 | `37490620d` | **强势跟踪卡改造**（父 = `7dbade6d1`） | `+239/-273` |
| 02:56:14 | `65efabe52` | CI build | 噪声 |

- **`37490620d` 的父链 `7dbade6d1` 不含 `742370d85`** ⇒ 它推送时工作树基线早于小九，把 02:43 的改动整块顶掉；随后 CI 多次重建，线上退回原始硬编码版（1,371,317 B，A/B/C 特征串全 x0）。
- 🔴 **`force:false` 挡不住这类覆盖** ——「基线落后」对 git 而言**是快进**，PATCH ref 会欣然接受。**真正的闸门只能是内容断言**：线上 blob == 打补丁所用基底，不等则中止重取。
- **恢复法**：以当前线上真身为新基线，用**精确锚点替换**重放改动，逐锚点断言 `count(old)==1` 防误伤 ⇒ 双方改动共存。
- **小九已推的最终 commit**：`d69fcec4c`（含恢复+修正），线上 blob `32c5fd5cee68f153e72addc3247e2cab77614148`，特征串 **12/12 命中**，阿狸咪强势跟踪卡 4 项特征 `base == replay`（未误伤）。

### 2.3 阿狸咪侧今日关键 commit（小九已独立复核）

| commit | 内容 | 小九复核结论 |
|---|---|---|
| `29a9b6620` | **新增 [10/10] index.html 核心标记守卫 + 真源清单**（结构性封堵旧树静默覆盖） | ✅ 采信；对 2.2 类事故是**结构性**封堵 |
| `10194b246` | **P5 年度门槛口径修正** —— 恢复「连续5年窗口内≥4年」原口径 | ✅ 见第五节风险项 R2 |
| `37490620d` | 强势跟踪卡四处改造（去重复+买点分档排序+无拉条折叠） | ✅ 已复核，与小九 A/B/C 无冲突（基隆已合并） |
| `1b84ce769` | 小九 1100 交接档 | — |
| `292086715` | 阿狸咪 1145 回执（三件套核验全绿 21/21 + 覆盖事故独立复核） | ✅ 双方对覆盖事故结论一致 |

### 2.4 凌晨/上午收到的阿狸咪来件（8 份，均已复核）

`0304`（build 连续全红根治+强势突破残链根治）/ `0705`（ALGO_BACKTEST_COMPARE 孤儿全链退役）/ `0713`（台账清零 294 项 + 推翻调度器停摆假警报 + cwds 脏值修复）/ `0825`（因子实验室头部去重 + guard 加固家机回执）/ `0859`（强势跟踪卡四处改造）/ `0900`（api_push_raw 防覆盖根治）/ `0945`（guard 读超时加固家机回归验证通过 + sha 引用远端可解析）/ `1012`（最终推荐口径统一为因子榜前5）/ `1044`（缓存戳口径收口 + max_dd_20d 三重否决 + P5 判据矛盾）/ `1145`（回执）。

**小九侧回执已推 5 轮**：`8cfc3fd8c`（0200）、`585c94d59`（0400）、`32f1206ee`（0600）、`1d82f978b`（0820）、`abd7f1154`（1040）。

---

## 三、当前系统状态（2026-09-20 12:02–12:07 CST 实测）

### 3.1 站点与产物

| 项 | 实测值 |
|---|---|
| 主站 HTTP | **200**（`https://ah-quant999.github.io/quant-scanner-v8/index.html`） |
| 线上 `index.html` | **1,376,606 B**（`git cat-file -s origin/main:index.html`） |
| 线上 `logic.html` | **753,241 B** |
| 本机 `index.html` / `logic.html` | 1,288,574 B / 735,087 B（**本机未改**，纯落后） |
| 线上 `HEALTH_CHECK.js` | `updated 2026-09-20 11:14:25` · **overall = fail** · ok105 / warn0 / **fail12** / limited2 / info1 / total120 |

### 3.2 🔴 fail=12 明细（**全部为周末设计内空档，非故障**）

| 类别 | 数量 | 明细 |
|---|---|---|
| **盘中卡 >120min 阈值** | **11** | INDEX_QUOTES / ETF_PULSE / ETF_INTRADAY_HEAT / SECTOR_FUND_FLOW / CONCEPT_RANKING / LIMIT_UP_HEATMAP / MARKET_FUND_FLOW_DATA / MARKET_ALERTS / CFFEX_HOLDINGS / AVG_PRICE_DATA / ETF_DAILY_MONITOR |
| **盘后卡 >1440min 阈值** | **1** | BAIHECHOU_ANALYSIS（新周期判定·AI解析，`last 2026-09-18 06:31`，age 3163min） |

- **判据**：11 张盘中卡末次更新全在**今日 08:16–08:22**（周末上午那次 cn fetch 批次），距今 ~172–178 分钟。**今天周日休市，盘中链本就不该跑** ⇒ 是「阈值未区分交易日/非交易日」的**假红**，非数据故障。
- **limited 2 项**（均非故障）：`MACRO_KSHAPE`（宏观K型分层，事件驱动）/ `UNLISTED_PANEL`（暂未上架模块索引，静态快照）。
- ⚠️ **请阿狸咪复核**：`BAIHECHOU_ANALYSIS` 卡（暂未上架·观测类）已 `2026-09-18 06:31` 起 2 天未更新，**是否属预期**（周末无新文 ⇒ 保持上次策展值）或真实断更。

### 3.3 Runner

| runner | 状态 | 证据 |
|---|---|---|
| `lemoncat-cn` | **online** | `RUNNER_STATUS_HEALTH.js`：`checked_at_bj 2026-09-20 10:55:32` |
| `alimi-cn` | 今日 07:38 死锁巡检时已从 offline **恢复 online**（busy=False） | 07:38:56 CST 巡检记录 |

- `RUNNER_STATUS.js`：`update_time 2026-09-20 08:23:01`，`status ok`；self-hosted 最近 15 条连续失败 **0** 次、checkout 失败 **0** 次；最新 run completed。
- `HB_XIAOJIU.js`（250 B）：`last_time = 2026-09-20 01:53:50`（**小九 0820 回执已定性：上轮手动补发值，非自愈**），`update_time 11:12:18` / `republish_time 11:12:43` 照常刷新。

### 3.4 Git / main

| 项 | 值 |
|---|---|
| **origin/main tip（12:02 实测）** | **`10194b246f77ab54b74c677d1634e112abe79ad4`** |
| 今日 main 提交数（近 200 内） | **74** |
| 本机 main HEAD | `4fe4b67c0`（**第 6 次被外部改动**，非本自动化） |
| 本机 porcelain | **1022** 项（工作区改动 + 未跟踪） |
| **本机 STAGED** | **2** 项（`M algorithms/final_recommend.py` / `M api_push_raw.py`，内容=远端 ⇒ **惰性无害**，按纪律未动） |
| worktree | 仅主树（旁路已 remove+prune） |

⚠️ **本机为浅克隆**：`git rev-parse --is-shallow-repository` = **true**，`rev-list --count origin/main` = **1** ⇒ 一切 `--is-ancestor` / `merge-base` / `git log` 血缘判据**恒假阴性**，历史 commit 对象多不可解析。**取远端值一律 `git show origin/main:<path>` 内容级复核。**

### 3.5 自动化运行真值（本机 `workbuddy.db`，`mode=ro`）

| 项 | 实测值 |
|---|---|
| `automation_runtime_state.max(last_run_at)` | **2026-09-20 11:20:19** |
| `automation_runs.max(created_at)` | **2026-09-20 12:00:35** |
| 09-20 当日 run 数 | **50**（ACCEPTED 49 / IN_PROGRESS 1，**无 FAILED**） |
| ACTIVE / PAUSED | **117 / 21** |

**判「自动化是否真跑」的唯一口径**：`automation_runs` 流水 + `automation_runtime_state`；
**禁用** `automations.last_run_at`（自 2026-08-31 20:20:40 起停写的**废弃列**）；
**禁用**对 13 位毫秒戳做字符串比较；
**禁用**把单字段读数推广成「子系统死活」。

---

## 四、需阿狸咪注意 / 待办 / 风险

### 4.1 🔴 我方正式撤回一条假警报（最高优先阅读）

**撤回**：小九 `0600` §五 + `0815` §六 的「**宿主调度器停摆**」= **假警报**（含衍生推论「盘后链仅剩 cron 备份腿⇒有漏跑风险」）。
**依据**：阿狸咪 `0713` 推翻成立，小九 08:18 本机复现三段错误链 —— ① 字段废弃 ② 毫秒戳字符串比较陷阱（`>= '2026-09-01'` 恒 False）③ **小九独有责任：采样口径越界**（把一个字段读数推广成一个子系统死活）。
**真值**见 §3.5。**已写入双方记忆，后续一律用该口径。**

### 4.2 🔴 HB_XIAOJIU 停摆（真因已坐实，待拍板）

- **真因 = 守护任务周末零排期**：`小九-v8 runner 本地守护(20分钟级)`（`automation-1786696074524`）rrule = `FREQ=HOURLY;INTERVAL=1;BYDAY=MO,TU,WE,TH,FR;BYHOUR=7,9,11,13,15,17,19,21;BYMINUTE=11` ⇒ **周六/周日零排期** ⇒ 心跳腿整段消失。
- **铁证**：`next_run_at = 2026-09-21 00:06:14`（下周一）；该任务 09-19 起 run 数 = **0**；最近 3 次 run 全在 09-18（周五）。
- **排除项**：调度器正常（`runtime_state.MAX` 实时推进）；脚本 `ZERO_DIFF`；runner 计划任务状态=就绪。
- **未定因部分（如实登记）**：心跳值 `01:53:50` 与「最后工作日运行 23:06」不符 ⇒ 09-20 凌晨另有一腿推过，此后无腿。
- **待拍板**：**方案丙 = 新增「周末轻量心跳腿」**（仅 write_heartbeat + push，**不带 `--heal`**）。
- ⚠️ **本轮不补发**：上次（01:53）手动补发**恰好掩盖了本次停摆** ⇒ 同因「手动补跑掩盖上游故障」教训，先取授权。

### 4.3 🔴 其他 P0/P1 待拍板

| 编号 | 事项 | 归属 |
|---|---|---|
| **P0-新②** | **交接档写死 commit sha 不可核验**（`0a79958b3384` 双方共享但全仓 0 命中，内容全落地）⇒ 建议交接档**禁写死 sha**，改「路径 + 字节数 + md5」；**不写本文自身提交的 sha**，改给现取命令 | 双方 |
| **P1-新** | **`build_deploy` gate 只判时钟不判交易日**（阿狸咪 1155 档）⇒ 周末盘中窗 `build` 静默 skip 但 run 顶层 success（**假成功**），**直接吃掉主人「周末手动部署」诉求** | **小九**（阿狸咪 PAT 改 `.github/workflows/**` 实测 403） |
| **R2 · P5 门槛口径** | `10194b246` 已恢复「连续5年窗口内≥4年」原口径；文档 `criteria` 文案与代码 `pass_yr = n_ok/n_y >= 0.8` 曾抵触。**请阿狸咪复核 `10194b246` 落地后文案与代码是否已一致** | 阿狸咪 |
| **P-Q** | 盘中粒度盲区：`guard_v8_freshness.py check_group` 对 `max_hours>=24` 一律按交易日判 ⇒ 12 个盘中模块同日停更不判红。**本次 §3.2 的 fail=12 正是反向形态（非交易日误报）**，建议「盘中走小时／盘后走交易日」双口径 | 待拍板 |
| **拍-新②** | `v8_cn_fetch_experiments.yml` L116 PUSH_FILES 仍含 `index.html`（活代码，无 sync）⇒ 护栏已拦但 yml 死条目仍在 ⇒ **摘除待授权** | 待授权 |
| **拍-新⑥** | `FINAL_RECOMMEND_DATA.js` 产出 `data_degraded=true`（A 批落后 ⇒ 降级放行）⇒ 请阿狸咪定性 | 阿狸咪 |
| **P-1** | CI 覆盖源补丁（`docs/ops/patches/v8_build_deploy_index_cover_fix_20260916.patch`，`v8_build_deploy.yml` L205-207/L340-342 未修）⇒ 通道（SSH）现成，只差授权 | 待授权 |
| **P-3** | 判据 80 桥接无单调守卫：`update_v8.py::_make_js` 无 `update_time` 单调校验 | 待拍板 |

### 4.4 纪律 / 环境坑（下轮必避）

1. 🔴 **推 main 必须走 SSH** `git@github.com:ah-quant999/quant-scanner-v8.git` —— HTTPS 会报 `could not read Username`（RC=128）。
2. 🔴 **`force:false` 不是覆盖护栏** —— 基线落后对 git 是快进。**唯一真闸门 = 内容断言**（线上 blob == 打补丁基底）。推后必做 **Pages 特征串命中**，**不要比 sha**（Pages 会注入内容，字节数也会因 CI 的 BUILD/`?v=` 而变）。
3. 🔴 **高频 churning 仓库里「推完就完」是错觉** —— 只要还在同一会话继续改，**每次推前都要重新取证**（重 GET ref，不能复用上轮 tip 当 base）。
4. 🔴 **`git checkout origin/main -- <file>` 会同时写索引** ⇒ 造成 STAGED 违反纪律。**正解**：补 `git reset -q HEAD -- <file>`（仅清索引，不动工作树）。
5. 🔴 **「本地独有」不必然是 WIP** —— 也可能是落后远端时本地残留的**旧形态**。必用 `git diff --quiet <远端SHA> -- <f>` 定方向。
6. ⚠️ `git worktree add` 必须用 **`E:/Temp/...`**（Windows 盘符式）；`/e/Temp/...` 会被 MSYS 改成 `E:/e/Temp/`。
7. ⚠️ **行尾归一化**：`_uhk_fn.js` 用 Python 文本模式写盘带 CRLF ⇒ 「线上 vs 本机逐字节比对」假报不一致（差异字符正是 `\r`）。**比对前两侧都 `.replace("\r\n","\n")`**。
8. ⚠️ Pages CDN 有秒级~分钟级延迟 ⇒ 推完立刻直取可能仍拿旧版，**必须轮询 30s×N 直到特征串出现**。
9. ⚠️ 中文路径推 doc：`urllib.parse.quote(REL)` 必须，否则 `UnicodeEncodeError`。
10. ⚠️ GitHub `/git/commits/{sha}` 响应是**扁平结构**（顶层 `message`/`tree`），**不是** `commit.message` 嵌套。
11. ⚠️ **核 sha 一律走 GitHub API**，禁用本地 `cat-file`/`rev-list`（浅克隆 ⇒ 恒假阴性）。
12. ⚠️ 本机 PATH 无 git ⇒ 实为 `/d/PortableGit/cmd/git.exe`，须显式 `export PATH="/c/Windows/System32:/d/PortableGit/cmd:/usr/bin:/bin"`。
13. 🔴 **红线**：只用 `E:/qs_workspaces/quant-scanner-v8`，**严禁 `E:/workspace/...`**（坚果云 junction，2026-09-04 全仓删除事故路径，永不复用）。

---

## 五、小九监督职责（下一班次）

1. **今日 12:00 后继续盯**：11 张盘中卡在**下个交易日（09-21 周一）开盘后**是否恢复刷新 —— 若周一 09:30 后仍停在 08:2x，**才是真断档**（届时需查 `cn fetch` 盘中链）。
2. **HB_XIAOJIU**：等主人对「方案丙」拍板；**未授权前不补发**。
3. **P1 `build_deploy` 交易日判据**：归小九，等授权后改 `.github/workflows/**`。
4. **周一 07:50 提前交接**（`92b1baf0`）—— 阿狸咪周末 → 小九接管核验。
5. **判活看板**：一律用 §3.5 口径，**不再用 `automations.last_run_at`**。
6. **推仓纪律**：SSH + `push origin main`（严禁 `git push HEAD:main`，MSYS 会转坏报 `Could not resolve hostname head`）；推前内容断言；推后 Pages 特征串命中。

---

## 六、现取命令（勿信本文数值，自行复核）

```bash
cd E:/qs_workspaces/quant-scanner-v8
git ls-remote origin refs/heads/main                              # 取远端 tip（勿用记忆值）
git show origin/main:data/HB_XIAOJIU.js                           # 心跳真值（只有 last_time 算数）
git show origin/main:data/RUNNER_STATUS_HEALTH.js                 # runner 状态
git cat-file -s $(git rev-parse origin/main:index.html)           # 线上 index 字节数
curl -s --max-time 25 "https://ah-quant999.github.io/quant-scanner-v8/data/HEALTH_CHECK.js" | head -c 600
```

```python
# 自动化真值（唯一口径；禁用 automations.last_run_at）
import sqlite3
con = sqlite3.connect('file:C:/Users/Administrator/.workbuddy/workbuddy.db?mode=ro', uri=True)
c = con.cursor()
print(c.execute('select max(last_run_at) from automation_runtime_state').fetchone())
print(c.execute('select max(created_at) from automation_runs').fetchone())
print(c.execute("select status,count(*) from automation_runs where created_at>=? group by status",
                (int((__import__('datetime').datetime(2026,9,20,0,0)-__import__('datetime').timedelta(hours=8)).timestamp()*1000),)).fetchall())
```

---

*本文由小九 12:00 槽位自动化生成 · 数据采集时刻 2026-09-20 12:02–12:07 CST · 本文自身提交 sha 不予写死（见 §4.3 P0-新②）*
