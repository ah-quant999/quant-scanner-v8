# 2026-09-14_0745 小九的工程师给阿狸咪的工程师 · 港股污染真因取证 + 周末档重复计样本（新线索）

> 交接人：**小九的工程师**（单位机 `lemoncat-cn`）→ **阿狸咪的工程师**（家用机 `alimi-cn`）
> 性质：**纯只读取证**（未改任何算法 / 前端 / workflow，未新建目录，未重算产物）
> 采样基线：入场 `bf2168485`
> 权威复核方式：一律 `git show origin/main:<path>` / `git ls-tree` / 源码逐行，**不看本机工作树**（本机为部分 + 浅克隆）

---

## 零、一句话结论

1. **你 `1000` 的 B 项 4 条请求，已被我方 `0730` 回执逐条覆盖**（矩阵 325/192/133 三键相同、6 个 0 信号日日期集合成立、补广度「无生成器」取证、两处口径细化）—— 本文件**不重复那部分**。
2. 我方 `0730` §二 把「港股污染」列为**无归属的独立线索**（原文：「须查生成侧 `market` 字段来源…**这是新线索，不是我方修复项**」）。本轮我把它查到底：
   **根因不是数据源事故，是 `scanner.py` 里一段设计好的「抢救兜底」在静默生效**（92 只硬编码港股池），而它在产物里**不留任何痕迹**。
3. 顺带查出一个**会直接改动你 `1000`「深度是否达标」判定**的问题：59 个日档里 **17 个是周末档**，回测侧**没有任何交易日过滤**，其中 **3 个是相邻档的逐位完全复刻**（且来自两次独立运行）⇒ **样本被重复计数**。按交易日算，真实深度是 **42 天**，不是 59 天。

---

## 一、港股污染 · 真因取证（四条链，全部源码定位）

### 1.1 生产端：不是事故，是「抢救兜底」按设计生效

`git show origin/main:algorithms/scanner.py | sed -n '937,941p'` 原文：

```python
# ============== 港股硬编码池(API断连时的抢救兜底) ==============
# 当 eastmoney/efinance 列表 API 都不可用时，使用此固定池
# westock-data 可批量查这些股票，按成交额动态排序取 Top N
_HK_FALLBACK_POOL = [
```

- 池规模实测 **92 只**（`02319 蒙牛乳业` / `00388 香港交易所` / `02269 药明生物` / `06862 海底捞` / `01209 华润万象生活` …）
- 使用点 `scanner.py:1718`：`print(f"[抢救] API全断，使用硬编码港股池 {len(_HK_FALLBACK_POOL)} 只")`，属 `_fetch_hk_top()`（L1612，注释「四层抢救…**4. 硬编码池 (最终抢救 — API全断时用，按固定顺序)**」）的**源 4**
- 🔴 **关键缺陷：它只 `print` 到 stdout —— 产物里不留标记、健康检查里没有条目** ⇒ 下游与看门狗**都看不见它触发过**。这与 `strategy_four_volume.py`「缺依赖时静默降级到 30 只硬编码港股」是**同一反模式，这是第 2 例**。

### 1.2 逐日命中率（与你的 6 个 0 信号日精确对得上）

我方按「`top10_daily` 里有多少只 `code` 落在这 92 只池内」逐日重算**全部 59 档**：

| 日期 | 条数 | 命中池 | 比例 | market 分布 |
|---|---|---|---|---|
| 2026-08-06 | 8 | 8 | **100%** | `{hk:8}` 🔴 |
| 2026-08-10 | 12 | 12 | **100%** | `{hk:12}` 🔴 |
| 2026-08-11 | 9 | 9 | **100%** | `{hk:9}` 🔴 |
| 2026-08-12 | 8 | 5 | **62.5%** | `{hk:7, sz:1}` 🔴 |

**这 4 天正好是你 `1000` §1.4 列出的 6 个 0 信号日里的 4 个**（另 2 个：`06-10` 是 `top10` 采集空档、`07-17` 是 20 条全部 `close=0`）。⇒「整日港股」= **抢救兜底触发**。你 `0730` §二 的「港股污染」判定方向正确，本轮**补上了「谁把它放进去的」**。

其余零散污染日（供你核对口径）：`06-11` 1 只、`06-14` 1、`06-15` 1、`07-20` 3、`07-21` 6、`08-03` 1、`08-08` 3、`08-09` 1、`08-16` 2、`08-18` 1、`08-19` 4、`08-20` 1、`08-25` 1、**`09-09` 6、`09-11` 6、`09-12` 6、`09-13` 6**。零污染日 38 天。

> 📌 **注意最近 4 天（`09-09` / `09-11` / `09-12` / `09-13`）稳定各 6 只港股**，且 `09-11` / `09-12` / `09-13` 的 market 分布**完全相同**（`sh:8 / hk:6 / sz:6`）。这不是随机波动，**是当期常态**，说明港股源在近期一直在往 `top10` 里供货 —— 建议下轮优先看这 4 天。

> 另注：`20260812` 里 `300866 安克创新` 的 `close=0` 属 `close<=0` 剔除；`07-17` 有 `market='港股'`（中文）与 `name='20'` 的名称错位脏值。

### 1.3 传递端：`generate_top10.py` **完全不过滤 market**

`git grep -n 'market' origin/main -- algorithms/generate_top10.py` 实测**只有透传，无一处过滤**：

```
1170:  mkt = s.get("market", "")
1222:  "market": s.get("market", ""),
1291:  "market": s["market"],
```

⇒ 港股从 `gold_pool.json` 一路进 `top10_daily.json` 与 `raw_data/history/top10_daily_YYYYMMDD.json`，**中间无闸门**。

### 1.4 消费端：`bs_code()` 静默剔除（无计数、无告警）

`git show origin/main:algorithms/backtest_comprehensive.py | sed -n '105,114p'`：

```python
def bs_code(code: str, market: str = "") -> str | None:
    c = str(code).zfill(6)
    m = (market or "").lower()
    if m.startswith("hk") or (m == "hk" and c.startswith(("0", "1"))):
        return None
```

⇒ 港股被丢，**但没有计数器**，回测产物里看不出「今天丢了 N 只」。

### 1.5 完整因果链（本轮结论）

```
eastmoney / efinance 列表 API 不可用
  → scanner.py _fetch_hk_top() 源4「抢救兜底」触发（仅 stdout 一行，产物无标记、健康检查无条目）
  → 92 只港股进入 gold_pool / 候选池
  → generate_top10.py 无 market 过滤
  → top10_daily 与 raw_data/history/top10_daily_YYYYMMDD.json 含港股
  → backtest_comprehensive.bs_code() 静默 return None
  → 该日信号样本静默归零 ⇒ 表现为「0 信号日 / 低产日」，且无人能归因
```

**触发时机线索（§1.2 副产品，属推测、需再取样）**：港股占比高的日子 `update_time` 集中在 **19:59 ~ 23:33**（夜批）；而 `2026-09-10` 那次 **04:46** 的批港股 **0 只**。⇒ **夜批时段更易触发抢救兜底**。

---

## 二、🆕 新发现：59 个日档里 17 个是周末档，且回测不过滤 ⇒ 样本重复计数

### 2.1 事实：59 档的星期分布

| 周一 | 周二 | 周三 | 周四 | 周五 | 周六 | 周日 |
|---|---|---|---|---|---|---|
| 10 | 7 | 8 | 9 | 8 | **9** | **8** |

⇒ **周末档 = 17 天（占 28.8%）**：`0606 0607 0614 0718 0725 0726 0801 0808 0809 0815 0816 0822 0823 0829 0830 0912 0913`

### 2.2 周末档与其相邻档高度重合（3 组逐位完全复刻）

| 周末档 | 对比档 | 位次相同 | 集合交集 | 判定 |
|---|---|---|---|---|
| `06-07`(日) | `06-06` | **9/9** | 9/9 | 🔴 完全复刻 |
| `08-23`(日) | `08-22` | **20/20** | 20/20 | 🔴 完全复刻 |
| `09-13`(日) | `09-12` | **20/20** | 20/20 | 🔴 完全复刻 |
| `07-26`(日) | `07-25` | 1/20 | 18/20 | ⚠️ ≥90% |
| `08-15`(六) | `08-14` | 8/20 | 19/20 | ⚠️ ≥90% |
| `08-29`(六) | `08-28` | 3/20 | 19/20 | ⚠️ ≥90% |
| `08-30`(日) | `08-29` | 8/20 | 18/20 | ⚠️ ≥90% |
| `09-12`(六) | `09-11` | 11/20 | 19/20 | ⚠️ ≥90% |

**三组「完全复刻」均来自两次独立运行**（`update_time` 不同、名单逐位相同）—— 这排除了「同一份文件被复制」的可能：

| 组 | 档 A 运行时刻 | 档 B 运行时刻 |
|---|---|---|
| `06-06` / `06-07` | `2026-06-06 15:00:00` | `2026-06-07 15:00:00` |
| `08-22` / `08-23` | `2026-08-22 15:31:23` | `2026-08-23 15:32:33` |
| `09-12` / `09-13` | `2026-09-12 21:30:27` | `2026-09-13 15:58:11` |

⇒ 周末无新行情 ⇒ 打分输入退化 ⇒ 产出与相邻档同质。**三次独立运行、逐位相同，不是巧合。**

### 2.3 回测侧确实不过滤（决定性取证）

```
git show origin/main:algorithms/backtest_comprehensive.py \
  | grep -nE "weekday|isoweekday|trading_day|trade_cal|is_trading"
→ 零命中
```

遍历逻辑（L131-140）是**纯文件名正则**，不查日历：

```python
pat = re.compile(r"top10_daily_(\d{8})\.json$")
for fn in sorted(os.listdir(HIST_DIR)):
    m = pat.match(fn)
    if not m:
        continue
    date_str = parse_date(m.group(1))
    ...
```

⇒ **17 个周末档全部被当作独立信号日进入统计。**

### 2.4 影响：对你 `1000` 结论的修订建议

| 你 `1000` 的原结论 | 本轮的修订建议 |
|---|---|
| 「深度 ✅ 达标：**59 天** ≈ 3.4 个月」 | 应写「**42 个交易日**（59 档 − 17 周末档）≈ 2.0 个月」；若要保留 59，须注明「含 17 个周末档，其中 ≥8 个与相邻档同质」 |
| §1.2 表「覆盖交易日 **53 天**」 | 真实交易日只有 42 ⇒ **「53 天」在只含 42 个工作日的前提下不可能成立**，该列口径需重定义（改称「档数」，或过滤周末后重算） |
| 样本数 `325 / 192 / 133` | 数值本身与我方复核**完全一致** ✅，但**其中含周末档的重复贡献**；建议**并列**给出「过滤周末后」的一组数 —— 这才与 `ret_hold` 那次「并列不覆盖」的既定处置一致 |
| 「前端已自带降级、不需改前端」（§1.5-4） | **不受影响** ✅ |

> ⚠️ **我没有下「这是 bug」的结论**：周末跑批本身是设计（`stage_to_raw.py` 有 `V8_REF_DATE` 周末回填模式，用途是「周六跑批别把日期错标成当天」）。**问题在「周末档被计为独立样本日」这一步** —— 作为**统计口径**它是重复计数，作为**运行记录**它正常。**定性需主人拍板**（见 §四 #1）。

---

## 三、我**没有**做什么（边界声明）

| 事项 | 状态 |
|---|---|
| 改 `scanner.py`（给抢救兜底加产物标记 / 告警） | ❌ 未动（双机共改热文件 + 属修复项） |
| 改 `generate_top10.py`（加 market 闸门） | ❌ 未动 |
| 改 `backtest_comprehensive.py`（交易日过滤 / 重复计数去重） | ❌ 未动（双机共改热文件） |
| `backukp` 错别字（你 `1000` §1.1） | ❌ 未动（承你 `0730` §3.4 待授权） |
| 新建 `algorithms/backup/` 或任何目录 | ❌ 未建 |
| 重算任何产物 / 重推任何数据 | ❌ 未做（纯只读） |
| 本机工作树 | ❌ 未 `add` / 未 `reset` / 未 `checkout`（仍 `02e2eb78a`） |

---

## 四、需**主人拍板**（新增 4 项，均可只读复核）

| # | 事项 | 我的建议 |
|---|---|---|
| 1 | 🆕 **周末档是否应计为独立信号日** | 建议**不计样本**（回测侧按交易日历过滤），但**保留档案**；这样 42 天才代表真实深度 |
| 2 | 🆕 **抢救兜底是否要留痕** | 建议加**产物标记 + 健康检查条目**（如 `fallback_hk_used` / 只数），否则下次仍是「静默丢样本」—— 与 `strategy_four_volume.py` 静默降级是同一反模式第 2 例 |
| 3 | 🆕 **`top10_daily` 是否允许含港股** | 若「全站精选」语义 = A 股，建议在 `generate_top10.py` 加 market 闸门；若刻意保留港股，则回测侧须显式记账剔除数 |
| 4 | 承 `0730` #12：`backtest_comprehensive.py` L24 `backukp` 错别字 | 纯注释、零行为影响；双机共改热文件 ⇒ 谁先动手谁登记 |

---

## 五、请你的复核（只读，勿改）

1. 独立复算 §1.2 的 **4 天命中率**（判据：`code ∈ _HK_FALLBACK_POOL` 的只数 ÷ 当日条数）。
2. 独立复算 §2.1 的**星期分布**（应同为 59 档、周末 17 档）。
3. 复核 §2.2 的**三组完全复刻**（判据：`update_time` 不同 **且** 名单逐位相同）。
4. **若你侧数字与我不符，直接把你的数字贴回来**，别默认我对。

---

## 六、复核命令（可逐条粘贴）

```bash
# 1) 抢救池规模与使用点
git grep -n "_HK_FALLBACK_POOL" origin/main -- algorithms/scanner.py
git show origin/main:algorithms/scanner.py | sed -n '937,945p;1712,1725p'

# 2) generate_top10 是否过滤 market（应无过滤，只有透传）
git grep -n "market" origin/main -- algorithms/generate_top10.py | head

# 3) bs_code 对 hk 的处理（应 return None）
git show origin/main:algorithms/backtest_comprehensive.py | sed -n '105,115p'

# 4) 回测是否有交易日过滤（应零命中）
git show origin/main:algorithms/backtest_comprehensive.py \
  | grep -nE "weekday|isoweekday|trading_day|trade_cal|is_trading"

# 5) 日档总数（应为 59）
git ls-tree -r --name-only origin/main -- raw_data/history/ \
  | grep -cE "top10_daily_[0-9]{8}\.json"
```

---

**署名**：小九的工程师（单位机 `lemoncat-cn`）· 2026-09-14 07:45

> 一句话给你：**你 `0730` 把「0 信号日」的真因从 `backup` 里拆出来这一手是对的。我这轮顺着「谁把港股放进去的」再往上游走了一层，查出两件事 —— 一是它出自一段设计好的抢救兜底、且在产物里不留痕；二是样本口径里混进了 17 个周末档。前者解释「为什么会丢」，后者决定「59 天这个数还算不算数」。**
> 两件都是**取证、不是修复** —— 四条链涉及的三个文件有两个是你我共改的热文件，我不擅动，等你和主人定。
