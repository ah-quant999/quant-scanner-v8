# 🚨 URGENT｜阿狸咪的股票专家 → 小九｜2026-09-11 21:54｜529UU冲突对账结论+3个远端侧故障+回执你2问

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 主人令：「你和小九紧急交接一下，对个账，看要留哪些」——本文件即该对账的完整结论。

---

## 一句话结论

**529 项 UU 一项都不用留。** 逐文件对账硬证据：`pile`（本机 stash 里的改动）中「比远端 main 更新」的文件 = **0 个**，远端不存在的路径 = **0 个** → 保留 pile 零收益，本机工作区按远端权威版本清理即可。

但在清理前的对账过程中，**揪出 4 个「当前线上/远端侧」的故障**（不是本机问题），需你在自家机核查 —— 见 §四。另回执你 21:00 交接里点名要阿狸咪机核实的 2 项 —— 见 §五。

---

## 一、529 UU 的构成

| 类 | 数量 | 性质 |
|---|---|---|
| `raw_data/kline_cache/*.json` | 466 | 本地 K 线缓存（可再生；且早已被排除出 API 同步名单） |
| `data/*.js` | 53 | 生成器产物 |
| `raw_data/*.json`（非 cache） | 8 | 生成器产物 |
| `data/freshness_status.json` | 1 | 运行时状态 |
| `.github/workflows/v8_algo_cloud.yml` | 1 | 代码 |
| **合计** | **529** | |

🔴 **即时危险（已确认）**：529/529 个文件**全部含 `<<<<<<<` 冲突标记** → 本机工作树里的 `data/*.js` 目前是**坏文件**（JS 语法错误），本地直接开 `file:///E:/workspace/stock-scanner/index.html` 会大面积卡片失效。线上（CDN）不受影响。

---

## 二、三层对账（决定性证据）

三个口径：`ours` = 本机 HEAD `ca4adc9ce`（= 远端祖先，落后 26 commit）｜`pile` = `stash@{0}` 里的本机改动｜`远端` = `bcc6d2445`（远端 main 当前）

```
▶ pile 与远端完全相同的：            0
▶ pile update_time 比远端新：        0   ← 决定性
▶ pile update_time 比远端旧：       58
▶ 远端不存在该路径（pile 独有）：     0
```

交叉复核（`data/*.js` 53 个，`ours`=HEAD 口径）：**HEAD 新 49 / pile 新 0 / 同刻 2 / 无 update_time 2**。

→ 两条独立路径得到同一结论：**pile 里没有任何「远端没有」或「比远端新」的内容。**

---

## 三、放大镜：14 个「pile 更旧但字节更多」的文件

单看字节数会误判成「远端缩水、应留 pile」。逐个开内容查实后，**没有一个是「pile 内容更新」**：

| 文件 | pile | 远端 | 判定 |
|---|---|---|---|
| `data/WEEKEND_META_REPORT.js` | `guard_tripped:true`（口径塌缩 9622→6822） | `guard_tripped:false`（6824→6824） | **远端已修**，pile 是旧故障态 |
| `data/SECTOR_RS.js` | `data_available:true` 250 元素 74345 B | `data_available:false` 0 元素 481 B | ⚠️ 见 §四-1（**远端今晚新故障**） |
| `data/STOCK_RPS.js`<br>`data/STOCK_RPS_DATA.js`<br>`raw_data/stock_rps.json` | `valid_count` 38 / 38 / 705 | `valid_count` **2** | ⚠️ 见 §四-2 |
| `data/STOCK_STOP_DATA.js` | 455/486 `degraded:true` | 208/259 `degraded:false` | ⚠️ 见 §四-3 |
| `data/HEALTH_CHECK.js` | 111 项 `fail=0` 全绿（19:14 快照） | 112 项 **`fail=2`**（21:43） | ⚠️ 见 §四-4（pile 会**掩盖**这 2 个 fail） |
| `data/MOMENTUM_FILTER.js`、`data/RUNNER_STATUS.js`、`data/MARKET_FUND_FLOW_DATA.js`、`data/MARKET_REGIME.js`、`data/SECTOR_RECOMMENDATION.js`、`data/COMMODITY_ELASTICITY.js`、`raw_data/h_auto_buy_history.json` | 字节略多 | 更新 | **远端胜**（同交易日旧快照） |

**关键**：这 14 个全部是**同一交易日的旧快照**，不是「更新」。所以「字节多」≠「该留」。

另注：`data/RUNNER_STATUS.js` 两版是**两套 schema** —— pile 版含 `process/service/worker_logs/github` 自检段，远端版只有 `modules/hostname:LEMONCAT/summary`。**疑似两个生成器写同一文件名**，建议你确认哪个是权威（见 §四-3 备注）。

---

## 四、⚠️ 对账中揪出的 4 个「远端侧」故障（请你在自家机查）

### 1. 🔴 SECTOR_RS 今晚被清空 —— 历史首次

| 提交 | `data_available` | 元素数 | 字节 | update_time |
|---|---|---|---|---|
| `704adf574`（cn fetch 21:28） | **false** | **0** | 481 | 21:23 |
| `4db1f196c`（cn fetch 20:54） | true | 250 | 74345 | 20:47 |
| `33266d7df`（20:20） | true | 250 | 74345 | 20:13 |
| `9da593e06`（build 20:19） | true | 250 | 74345 | 20:13 |
| `24d06c81a`（19:48） | true | 250 | 74345 | 19:41 |
| `3743f7599`（19:13） | true | 250 | 74345 | 19:06 |
| `ebedcfd0a`（18:39） | true | 250 | 74345 | 18:31 |
| `ee268b722`（18:03） | true | 250 | 74345 | 17:56 |

→ **前面 7 个版本全 `true`/250 元素，21:28 那次首次变 `false`/0 元素**，`republish_time 21:25:34`。板块 RS 卡今晚是空的。
→ 盘后（21:2x）不该为空，像是某个 republish 步骤拿「空值」覆盖了实值 —— 与 memory 里 `premarket_cleared` / `no_data` 一类的**占位覆盖陷阱**同型。请查这个时间点是谁写的。

### 2. 🔴 STOCK_RPS 有效样本持续塌缩：705 → 38 → 2

| 提交 | update_time | `valid_count` | `universe_count` | 字节 |
|---|---|---|---|---|
| `342602bd4` | 2026-09-11 05:11:12 | **705** | 886 | 388877 |
| `049925d5f` | 2026-09-11 09:21:44 | 38 | 886 | 21139 |
| `37710c324` | 2026-09-11 18:40:16 | **2** | 907 | 1324 |

→ `valid_count=2` ≈ 等于没有信号，RPS 功能实质失效。
→ **附带疑点**：`data/STOCK_RPS.js` 内部写的变量名是 `window.STOCK_RPS_DATA`，与 `data/STOCK_RPS_DATA.js` **重名**（两个文件写同一个 window 变量）。是否属设计？还是其中一个该改名？请确认。

### 3. ⚠️ STOCK_STOP_DATA 宇宙 790→259，且 `degraded` 反而从 true 变 false

| 提交 | update_time | `count` | `total_universe` | `degraded` |
|---|---|---|---|---|
| `37710c324` | 2026-09-11 19:00:01 | 757 | **790** | **true** |
| `ef38b2166` | 2026-09-11 20:42:11 | 208 | **259** | **false** |

→ 宇宙掉了 67%（790→259），而自检标记从 `true` 变成 `false`（= 不再认为自己降级）。**降级了却不报警**，是典型的「宇宙只剩几只」陷阱（与 GOLD_POOL lite 化同型）。请确认 259 是否**有意收窄**；若否即为 bug。

### 4. ⚠️ HEALTH_CHECK 远端 `fail=2` —— 且 pile 会把它藏掉

| 口径 | updated | overall | ok | fail | total |
|---|---|---|---|---|---|
| 远端（线上） | 2026-09-11 21:43:38 | **warn** | 110 | **2** | 112 |
| 本机 pile | 2026-09-11 19:14:10 | ok | 111 | 0 | 111 |

→ 远端多出的第 112 项正是今晚新增，且如实报了 2 个 fail。
→ **本项我特意不沿用旧惯例**：此前定过「HEALTH_CHECK.js 冲突保留本地不动」——但这次保留本地会把 2 个红灯**藏掉**，故按「远端新」处理，并把 2 个 fail 交给你定位。

---

## 五、回执你 21:00 交接里点名要阿狸咪机核实的 2 项（均已实查）

### 回执 1：`align_logic_ops.py`「在本仓库不存在」= **误判**，它存在

- **实际路径**：仓库**根目录** `align_logic_ops.py`（blob `0490201c47af63bea6446068bf44218ef1159fed`，9065 B）——**不在 `.github/scripts/`**，这就是你 grep 不到的原因。
- **双登记机制已落地**（源码实证）：
  - L44 注释：`🔴 新增带 cron 的 workflow 必须双登记：(1) 本名单 (2) 文档源。`
  - L46：`MUST_HAVE_CRON = { ... }`（另有 `ALLOW_NO_CRON`）
  - L39：`(INDEX, "op", "index.html#sec-op")` → **运维页登记位是 `index.html` 的 `sec-op` 段**（不是 logic.html）
  - 双向校验：L151 方向1「MUST_HAVE_CRON 必须 (a) 有 cron (b) 已文档化」；L167 方向2「已文档化但未登记名单（漏登记）」→ 两个方向都会 drift 报错
- **门禁接线**：`.github/scripts/pre_deploy_audit.py` L13 注释第 4 项、L117-125 `check_align_logic_ops()`、L225 `("[4/5] align_logic_ops", ...)`，`EXIT 0` 才算过。

→ **结论：双登记机制在，无需补建。** 唯一建议：把「脚本在仓库根目录」这点写进 README，免得再被误判一次。

### 回执 2：阿狸咪机删除阈值 = **2000**，与你机一致 ✅

`D:\actions\cn-runner\.env` L4：`CODEBUDDY_SAFE_DELETE_BULK_THRESHOLD=2000`

---

## 六、清理配方（待主人一句「清」即执行；请你确认无异议）

**实测现状**：`.git/MERGE_HEAD`、`.git/rebase-merge`、`.git/rebase-apply`、`.git/CHERRY_PICK_HEAD` **均不存在** → stash pop 冲突只留下索引里的未合并条目，**因此不能用 `git merge --abort`**（会报无合并进行中）。

正确顺序（本机执行，仓库外先做安全垫）：

```bash
cd /e/workspace/stock-scanner

# 0) 安全垫：把 stash 导出成 bundle 存到仓库外（坚果云同步区之外）
#    stash@{0} = be0dc8b716ae18985d163c5e94b135879443696c（= pile 快照，父提交 e1cf42494）
git bundle create "/c/Users/HH20210606/WorkBuddy/pile_backup_20260911.bundle" \
    be0dc8b716ae18985d163c5e94b135879443696c

# 1) 确认对账对象
git stash list                             # 应为 alimi-596-pile+256u-pre-rebase-2026-09-11

# 2) 用远端权威覆盖全部冲突路径
git fetch origin main
git checkout FETCH_HEAD -- .

# 3) 清掉索引里残留的未合并条目（不动工作树）
git reset

# 4) 让工作树 = 索引 = 远端
git checkout -- .

# 5) 丢弃 pile（对账已证明零损失）
git stash drop stash@{0}

# 6) 快进 26 个 commit
git pull --ff-only origin main
```

⚠️ 本机是坚果云**实时同步**目录：**禁止 `git add -A`**；清理期间**不跑生成器**（否则改动会被秒回滚）；补丁/脚本一律放仓库外。

---

## 七、待主人 / 你裁决

1. **清理是否执行** —— 我已按兵不动，等主人一句话；若你认为配方需改，请直接改在本文件下方回执。
2. **卡位置口径三方不一致**（容易互相迁来迁去，建议一次定死）：
   - 你 21:00 交接写「文件新鲜感卡迁至**选股策略页**候选股池上方」；
   - 远端 `index.html` 今日实际是「**运维页 → 数据健康总览** → 文件新鲜度表」内；
   - 今晚 `370fd04a3` 已删掉与它重复的「运维页顶部独立卡」。
   → 请确认**终态唯一位置**，我按你的回执对齐文档。
3. `data/STOCK_RPS.js` 与 `data/STOCK_RPS_DATA.js` 变量重名（§四-2）—— 是否要其中一个改用 `window.STOCK_RPS`？
4. `data/RUNNER_STATUS.js` 两套 schema（§三备注）—— 哪个生成器是权威？

---

## 附：本次对账的原始证据文件（均在阿狸咪机仓库外，可复现）

- `.../reconcile/recon.py` → `recon_report.txt`（三口径逐文件对账）
- `.../reconcile/recon_vs_remote.py` → `recon_vs_remote.txt`（pile vs 远端，决定性）
- `.../reconcile/inspect_suspect.py` → `suspect_content.txt`（14 个嫌疑文件的双版内容）
- `.../reconcile/history_check.py` → `history_check.txt`（SECTOR_RS / RPS / RUNNER_STATUS 版本史）

---

> **署名**：阿狸咪的股票专家（2026-09-11 夜·周五交接班）
