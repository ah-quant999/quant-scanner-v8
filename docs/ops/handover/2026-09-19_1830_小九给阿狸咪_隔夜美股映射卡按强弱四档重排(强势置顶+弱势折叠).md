# 小九的工程师给阿狸咪的工程师 · 隔夜美股强弱映射卡「按强弱四档重排」上线交接

- **交接时间**：2026-09-19（见文件名时间戳）
- **发起方**：小九（单位机 · 白天班）
- **接收方**：阿狸咪（夜间/周末班）
- **性质**：主人令前端改动 · 已上线 · 请知悉并纳入你的巡检口径
- **改动文件**：`index.html`（单文件，唯一改动点 = `window._fillUsHkMap`）
- **未改动**：数据侧（`scripts/fetch_us_hk_map.py` 生成器 / `data/US_HK_MAP.js` / `cloud_fetch_v8.py::f_us_hk_map`）**一律未动**

---

## 一、主人原话与落地口径

> 主人原话：「这个越强势的放最顶部，按强势、中期强短期反弹、短期反弹全部展示，弱势的折叠。」
> （截图对象 = 运维面板 > 暂未上架 > 📡 观测类 >「隔夜美股强弱 · 映射 A/港股」卡）

拆成三条落点：

| # | 主人要求 | 落地实现 |
|---|---|---|
| 1 | 越强势的放最顶部 | 卡内**档位顺序 = 强弱降序**：`强势` → `中期强·短期回调` → `短期反弹` → `弱势`；且档内再按 5 日涨幅降序 |
| 2 | 强势 / 中期强短期反弹 / 短期反弹 **全部展示** | 这**三档全部默认展开**，不再截断、不再隐藏 |
| 3 | 弱势的**折叠** | **仅**「弱势」档默认折叠（`display:none`），点按「展开弱势 N 项」展开 |

> ⚠️ 主人说「中期强短期反弹」，数据侧实际 tag 为 **`中期强·短期回调`**（含间隔号）。按数据侧原值透传，未改名、未臆造新档名。

---

## 二、改动前 vs 改动后（口径对照）

### 改动前（旧口径）
```
每组内部二分为「5日上涨」与「其余」两块：
  · up   = d5 > 0 且非反向  →  只取前 10 条（DEF_ROWS=10）直接展示
  · rest = up 剩余 + other(含所有下跌/反向)  →  全部塞进一个折叠块
文案：「展开其余 N 项（含走弱标的）」
```
**问题**：排序口径是「是否上涨」而非「强弱档位」；`up` 数组**根本没排序**（既非 d5 降序也非档位序），
所以「强势档」标的可能被压在「短期反弹」标的下面 —— 这正是主人要求「越强势放最顶部」的原因。

### 改动后（新口径）
```
档位强弱降序（TIERS）：
  ① 强势              open:true    ← 默认展开
  ② 中期强·短期回调     open:true    ← 默认展开
  ③ 短期反弹           open:true    ← 默认展开
  ④ 弱势              open:false   ← 仅此档折叠
档内排序（_sortTier）：
  反向(inverse)标的置顶 → mom.d5 降序 → 同值按 mom.d20 降序
每个档头带：色点 + 档名 + 「N 项」计数（弱势档额外标「· 默认折叠」）
```

**排序口径与数据侧对齐**：数据侧 `sort_rule = "按 5 日涨幅降序（同值按 20 日）"`，
新前端排序**逐字同口径** ⇒ 前端与数据侧不打架。

---

## 三、反向标的（inverse）处理 —— 本次唯一新增的判定

本次实测数据里**恰有 1 个反向标的**：

| us | 名称 | tag | d5 | 语义 |
|---|---|---|---|---|
| `YANG` | 三倍做空富时中国 ETF-Direxion | 强势 | +1.29% | **它涨 = 中国资产跌**，方向与右侧映射标的**相反** |

**旧口径**：反向标的被 `!x.inverse` 排除在 `up` 之外 ⇒ 落进折叠块，容易被忽略。
**新口径**：反向标的不再被排除，而是**置顶到本档最前**并保留原有「反向 · 方向相反」徽章，
使「名义 tag=强势，实际含义=看空中国」不会被误读。

> 数据侧 `inverse` 字段口径**未改动**，前端只是改变了它在展示序里的位置。

---

## 四、实测数据（2026-09-20 08:23:01 那份 `US_HK_MAP`）

数据源：`data/US_HK_MAP.js`（`update_time = 2026-09-20 08:23:01`，`data_date = 2026-09-18`，`bias = 隔夜美股偏强`），
共 86 项（`ADR 31 + ETF 55`）。

### 分档明细（ADR 组）

| 档位 | 项数 | 展示 |
|---|---|---|
| 强势 | 2 | 展开 |
| 中期强·短期回调 | 1 | 展开 |
| 短期反弹 | 11 | 展开 |
| **弱势** | **17** | **折叠** |

### 分档明细（ETF 组）

| 档位 | 项数 | 展示 |
|---|---|---|
| 强势 | 10 | 展开 |
| 中期强·短期回调 | 6 | 展开 |
| 短期反弹 | 17 | 展开 |
| **弱势** | **22** | **折叠** |

合计 `2+1+11+17 + 10+6+17+22 = 86` ✅ **条目守恒，无丢失**。

### 折后首屏可见量对比

| | 旧口径 | 新口径 |
|---|---|---|
| ADR 组可见 | 10 | **14**（2+1+11，弱势 17 折叠） |
| ETF 组可见 | 10 | **33**（10+6+17，弱势 22 折叠） |

---

## 五、线上生效取证（可自行复跑）

**截至本次交接撰写时刻**实测：

| 项目 | 值 |
|---|---|
| commit | `4d01769ec5d79560e2ed42accb4f08e7ab190ae3` |
| 是否当前 main tip | 是（`GET /git/ref/heads/main` 实测） |
| 远端 `index.html` blob sha | `84663d339ce87b3ac47c21b0f5ed3a7d173d24f6` |
| 改动前基线 blob sha | `bead35e881990a54f4fb85b1aca821847da020cc`（= 我取真身时远端值，**推前校验一致，无漂移**） |
| 字节数 | 1366370 → **1367897**（+1527） |
| Pages 线上 md5（`_fillUsHkMap` 函数体，CRLF 归一） | `d0b0befb20bf3418d04162e3304eba4e`（与本地**逐字节相同**） |

### 复跑命令（现取，不写死）
```bash
# 1) 看 main 当前 tip
curl -s https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main

# 2) 抓线上真身并查新口径特征串（应全部命中，旧文案应 0 命中）
curl -s --max-time 60 "https://ah-quant999.github.io/quant-scanner-v8/index.html?v=$(date +%s)" \
  | grep -c "var TIERS = \["                     # 期望 1
curl -s --max-time 60 "https://ah-quant999.github.io/quant-scanner-v8/index.html?v=$(date +%s)" \
  | grep -c "展开弱势 "                            # 期望 2
curl -s --max-time 60 "https://ah-quant999.github.io/quant-scanner-v8/index.html?v=$(date +%s)" \
  | grep -c "展开其余"                             # 期望 0
```

> ⚠️ 上表的 commit / blob sha 是**撰写时刻实测值**，主分支高频变动，**不要当成固定事实沿用**；以复跑命令现取为准。

---

## 六、给你的巡检建议（请纳入夜间巡检口径）

1. **本卡「弱势档折叠」是设计态，不是故障**。
   若巡检脚本有「卡内条目数 < items 总数即判 FAIL」这类规则，本卡会**误报**：
   折叠态下弱势档虽在 DOM 里，但 `display:none`。
   ⇒ 判数据完整性请**直接数 DOM 行数**（折叠块内的行也在 DOM 中），不要按可见条目数判。
2. **本卡数据链未改动**：`US_HK_MAP` 仍为 `premarket` 单档、08:25 盘前抓取；
   本卡是**隔夜**口径，**禁止**写成「实时/今日」（原卡内文案已如此声明，勿改）。
3. **若本卡又出现「强势标的被压在下面」**：请先确认线上 `index.html` 是否含 `var TIERS = [`，
   若缺失 = 被回退，按覆盖事故流程处理并在交接档记录。

---

## 七、本次遗留 / 明确未做的事

| 项 | 状态 |
|---|---|
| 数据侧 tag 判定（`强势/中期强·短期回调/短期反弹/弱势`） | **未改**，沿用现口径 |
| `mom` 多周期字段（`d1/d3/d5/d10/d20/d60`） | **未改** |
| `inverse` 字段判定逻辑 | **未改**，仅改变前端展示位置 |
| 卡内口径 / 闸门对账 / 免责脚注 | **未改**（原文保留） |
| 自愈轮询（600ms×30 补渲） | **未改** |
| 折叠事件委托（`.uhk-toggle`，只挂一次） | **未改**，只是 `data-t` / `data-lbl` 的取值对象从「组级折叠块」换成「弱势档折叠块」 |
| `DEF_ROWS` 变量 | **保留但已不再被引用**（未删除，避免影响其他可能引用点） |

> 「保留未删」不等于「死代码已清理」。若后续确认全站无他用，可另行清理；本次严格限于主人令范围，不多动一行。

---

## 八、判定本档是否「有问题」的几条硬口径

1. **标签名以数据侧为准**：`中期强·短期回调`（不是「中期强短期反弹」）。前端透传，不改写。
2. **`inverse` 标的（如 YANG）tag 可以是「强势」，但其语义是看空中国资产** —— 这是数据侧既有口径，
   前端已用置顶 + 徽章双保险，勿把它当成 bug。
3. **折叠 ≠ 丢数据**：折叠块在 DOM 中，可用 `document.getElementById('uhkT0_3').innerHTML` 取证。
4. **本卡无信号时整卡 `display:none`**（不产空壳），这是既有护栏，勿改。

---

## 九、一句话结论

「隔夜美股强弱·映射 A/港股」卡已按主人令改为**强弱四档降序 + 前三档全展 + 仅弱势折叠**，
档内 5 日降序（同值 20 日）、反向标的置顶标注；
86 条全渲染无丢失，线上 Pages 已生效（函数体 md5 与本机逐字节一致）。
数据侧零改动，不涉及任何数据口径变更。
