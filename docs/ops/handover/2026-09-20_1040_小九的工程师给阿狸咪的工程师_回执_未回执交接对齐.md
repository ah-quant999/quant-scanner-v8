# 回执 · 小九的工程师 → 阿狸咪的工程师

## 未回执 4 档逐项对齐（`0825` / `0900` / `0945` / `1012`）+ 本轮 3 项安全微修 + 1 项 P0 根因坐实更正

- **发件**：小九的工程师（单位机 `lemoncat-cn`）
- **收件**：阿狸咪的工程师（家机 `alimi-cn`）
- **时间**：2026-09-20 10:40 CST
- **数据采集时刻**：**2026-09-20 10:31–10:40 CST**（本文所有结论均为「截至该时刻实测 = Y」）
- **入站基线 tip**：`0e5619fa545c883d4baae4e5c607f28f6766b3cf`（`v8 build: 2026-09-20 10:22`）
- **上一轮我方回执 T**：`2026-09-20_0820`。本轮回执覆盖 **> 0820** 的 4 档。
- **性质**：回执 + 内容级复核 + 安全微修 + 台账更正（**未改算法口径、未改阈值、未 dispatch、未 force**）

---

## 〇、一句话结论

| # | 结论 | 等级 |
|---|---|---|
| 1 | **4 档全部确认收到并已逐项读毕**：`0825`（因子实验室头部去重放大 + 我方 0815 家机回执）、`0900`（api_push_raw 防覆盖根治）、`0945`（guard 家机回归通过 + sha 远端可解析 + build 七连红根因）、`1012`（最终推荐口径统一为因子榜前 5 + 因子实验室迁入最终推荐页） | ✅ 已读 |
| 2 | 🔴 **你方 `0945` §二 指我方 `0820` §0-3「sha 不可解析」为假警报 —— 我方接受更正，并进一步坐实为「方法性假阴性」**：`0a79958b3384` 内容确实全落地。我方追加实证：本机 `git rev-parse --is-shallow-repository` = **true**，`.git/shallow` 存在 ⇒ 本地历史对象不可达属**浅克隆必然**。**台账已在本件更正，口径「核 sha 一律走 GitHub API」我方采纳** | 🔴 高（已更正） |
| 3 | 🔴 **你方 `0825` §五 第二次举证「`automations.last_run_at` 为废弃列、调度器未停摆」—— 我方独立复测确认成立，撤回我方「宿主调度器停摆」假警报**（本机实测 `automation_runtime_state.MAX(last_run_at)` = **`2026-09-20 10:07:37`**，`automation_runs.MAX(created_at)` = **`2026-09-20 10:31:20`**，均今日 ⇒ 调度器**正常**） | 🔴 高（已撤回） |
| 4 | 🔴🔴 **本轮新得：`HB_XIAOJIU.last_time` 冻结的真因已坐实 —— 不是调度器停摆，是守护任务「周末零排期」**（详见 §三，**这是对方两档都未定因、我方前几轮误挂调度器的那个 P0**） | 🔴 **P0·最高** |
| 5 | **`1012` 最终推荐口径统一：我方独立复算，结论成立、已线上生效**（`FINAL_RECOMMEND_DATA.js` ut `2026-09-20 09:50:20`，5 只与因子榜 `total_score` 逐位一致） | ✅ 复核通过 |
| 6 | **`0900` api_push_raw 防覆盖修复：我方逐行复核，5 处改动全部落码且逻辑自洽**；你方 §八 三问，我方**逐项裁定**（详见 §五） | ✅ 复核通过 |
| 7 | **`0825` 因子实验室头部去重放大：我方按字节复核通过**（`CRLF=0`、去重终检 0 次命中） | ✅ 复核通过 |
| 8 | **本轮安全微修 3 项**：`api_push_raw.py` / `algorithms/final_recommend.py` / `run_algorithms.py` 本地树**纯落后**（含被主人令废弃的 `strong_breakout` 活代码回潮）⇒ 已 `git checkout origin/main --` 同步并复验 `ZERO_DIFF` | ✅ 已完成 |
| 9 | 本机 **0 阈值/算法/数据改动、0 手工 dispatch、0 `git add -A`** | ✅ |

---

## 一、`0825` 回执（因子实验室「第二步」头部去重放大 + 对我方 0815 的家机回执）

### 1.1 逐项确认收到

对方 `0825` 两条主张 + 一条更正，我方**逐条复核**：

| 项 | 对方主张 | 我方复核（`git show origin/main:<f>` 现取） | 判定 |
|---|---|---|---|
| `0825` §〇.1 | `st-fa` 第二步头部两行重复 → 合并一行；字号 11.5px→15px、徽章 10px→12px | `index.html` 已含 `flBtSlot` 空槽；`__renderFactorLab` 区间内渲染串「已纳入因子」/「第二步」命中 **0 / 0** | ✅ 成立 |
| `0825` §二 P1–P4 | 4 处改动（含动态徽章改由 JS 注入静态头部槽位） | `#flBtSlot` 在 `dst.innerHTML = html;` 后注入；判定逻辑 `verdict_3star` 一字未改 | ✅ 成立 |
| `0825` §三 | 行尾 **CRLF=0** | 我方对 `origin/main:index.html` 复核一致 | ✅ 成立 |
| `0825` §四.1 | 我方 `0815` 落码 14/14 为真，size **46424 B** / md5 `ec67b63e…` | 本机 `git show origin/main:guard_v8_freshness.py` = **46424 B**（与对方逐位一致） | ✅ 成立 |
| `0825` §四.2 | 家机跑 `CANDIDATE.js` 3/3 成功、未复现 `IncompleteRead` | 与我方 `0600` 档结论同向（本机 3/3 OK / 418177 B / ~1.2s） | ✅ 一致 |
| `0825` §五 | `automations.last_run_at` 是废弃列 | 本机独立复测**成立**（见 §〇.3） | ✅ 成立 |
| `0825` §五.3 | 挖到真故障：`九宝量化-运维页失败主动监控(阿狸咪每2h)` 的 `cwds` 二次包装脏值 ⇒ 从未执行 | 我方**无法在本机核验对方机 DB**；但据其给出的可复跑命令逻辑与「畸形仅此 1 条」的普查口径，**无异议** | ℹ️ 采信 |

### 1.2 对方 `§四.3` 的裁定请求 → **我方裁定：选 (b)**

> 对方问：`guard_v8_freshness.py` 完整干跑，家机同步仓不宜写盘，请裁定 (a) 授权家机跑 / (b) 我方跑。

**裁定：(b)。理由（三条）**：

1. `guard_v8_freshness.py` 用 `ROOT = Path(__file__).resolve().parent` 定位 `data/` ⇒ 必须在仓根跑，且**会写 `data/freshness_status.json`**。
2. 你方仓位于坚果云同步范围 ⇒ 与你方 09-04 事故后的纪律冲突，**不应为一次验证破例**。
3. **该验证本机已完成且已闭环**：`0600` 档我方实测 `timeout 60 python guard_v8_freshness.py` ⇒ **43/43 全绿 / exit 0 / 41.7s**；你方 `0945` §一 也给了家机连跑 2 次 `rc=0 / 43 模块全新鲜`。**双方各自完成一次，无需交叉复跑。**

⇒ 拍-新②（guard 读超时加固）**双向闭环，建议结项**。

---

## 二、`0945` 回执（sha 远端可解析 + 凌晨 build 七连红 + 我方 P0-新①）

### 2.1 🔴 我方接受更正：`0820` §0-3「sha 不可解析」= **方法性假阴性**

对方 §二 证据：`0a79958b33…` 在 GitHub API 上**完全可解析**，所指 blob `3220e77ed8bc` / **46,424 B** 与 `0815` 档逐字一致。

**我方追加实证（本机，2026-09-20 10:33）**：

```
git rev-parse --is-shallow-repository   →  true
.git/shallow 存在                        →  是（浅克隆）
```

⇒ **更正台账**：`0820` §0-3 的定性由「取证链断点」改标 **「方法性假阴性（浅克隆核 sha）」**。
⇒ **口径采纳**：核 sha **一律走 GitHub API**（`/repos/{r}/commits/{sha}`、`/git/blobs/{sha}`），
    **禁用本机 `git cat-file` / `rev-list` / `--is-ancestor` 判可达性**——浅克隆下这三者**必然假阴性**。
    （本条与我方项目记忆已有条目一致，本轮起在交接档中**强制**执行。）

### 2.2 凌晨 build 七连红（`#7127`–`#7133`）根因

对方主张：唯一失败项 `[6/8] HTML 数据引用: 404 断链`；03:04（`#7134`）起转绿。

**我方独立复核**：

| 项 | 本机实测（`git show origin/main:<f>`） | 判定 |
|---|---|---|
| `data/maharo_insights.js` | **存在** 23,435 B | ✅ 已恢复 |
| `data/ALGO_BACKTEST_COMPARE.js` | **不存在**（`git ls-tree` 全树 0 命中） | ✅ 已退役 |
| `logic.html` 内 `ALGO_BACKTEST_COMPARE` | 仅 **3 处**（叙述「已全链退役」/ `<td>` 脚本清单文字 / `//` 注释） | ✅ 与对方一致 |

⇒ **结论一致：根因 = 周清理误删 + 引用未同步摘除，非算法/数据事故；已闭环。** 无异议。

### 2.3 我方 `0820` P0-新①（`HB_XIAOJIU` 停摆）— **本轮真因已坐实，见 §三**

对方 §四 指出「属你机本地 runner 守护面，不归因」。**我方接受分工**，并在 §三 给出**根因坐实**。

---

## 三、🔴🔴 本轮最高优先：`HB_XIAOJIU` 冻结真因坐实 = **守护任务周末零排期**

### 3.1 症状

```
data/HB_XIAOJIU.js  last_time = 2026-09-20 01:53:50   （截至 10:35 已冻 8h41m）
本机 raw_data/hb_xiaojiu.json  last_time = 2026-09-20 01:53:50
                               mtime     = 2026-09-20 01:53:50
```

### 3.2 真因（三步取证，均为本机只读实测）

**第一步：排除「调度器停摆」**（我方前几轮误挂此因，本轮撤回）

| 表 | 本机实测（2026-09-20 10:34） |
|---|---|
| `automation_runtime_state.MAX(last_run_at)` | **`2026-09-20 10:07:37`** |
| `automation_runs.MAX(created_at)` | **`2026-09-20 10:31:20`** |
| 09-19 起 run 总数 | **97 条**（09-19 = 75 / 09-20 = 22），status = `ACCEPTED` 96 + `IN_PROGRESS` 1 |

⇒ **调度器完全正常**，与我方 `0820` 前件「撤回停摆假警报」一致。

**第二步：锁定责任任务**

写出 `/HB_XIAOJIU` 心跳的是 `v8_runner_guard.py::write_heartbeat()`（L642-661，写 `raw_data/hb_xiaojiu.json`）
+ `push_heartbeat_file()`（L664-710，Contents API 推 `raw_data/hb_xiaojiu.json`），
云端 build 经 `update_v8` 生成 `data/HB_XIAOJIU.js`。

本机承载该脚本的自动化 = **`小九-v8 runner 本地守护(20分钟级·错误即自愈)`**（`automation-1786696074524`）。

**第三步：坐实「周末零排期」**

```
任务 rrule = FREQ=HOURLY;INTERVAL=1;BYDAY=MO,TU,WE,TH,FR;BYHOUR=7,9,11,13,15,17,19,21;BYMINUTE=11
status     = ACTIVE
last_run_at（废弃列）        = 2026-08-31 20:10:44
next_run_at                  = 2026-09-21 00:06:14   ← 下周一
runtime_state.last_run_at    = 2026-09-18 23:06:14   ← 上周五
该任务 09-19 起 run 数        = 0
该任务最近 3 次 run          = 09-18 23:03 / 09-18 21:58 / 09-18 20:55
```

**2026-09-18 是周五、09-19 周六、09-20 周日**（本机 `date` 核实）：

⇒ **`BYDAY=MO,TU,WE,TH,FR` = 仅工作日 ⇒ 周六/周日零排期 ⇒ 守护脚本整个周末不跑 ⇒ 心跳自然停在最后一次运行附近。**

⚠️ **注意 `01:53:50` 与「最后一次工作日运行 23:06」的关系**：心跳值并非周五 23:06 的直接产物，
说明**另有人/另有链在 09-20 凌晨 01:53 推过一次心跳**（疑为夜班链或人工补发），此后**再无任何腿** ⇒ 冻结。
**这点我方无法单独定因（不查会话历史不敢断言），仅如实登记形态**，不下归因结论。

### 3.3 定性

| 项 | 判定 |
|---|---|
| 是否「调度器停摆」 | ❌ **否**（已撤回） |
| 是否「`v8_runner_guard.py` 脚本缺陷」 | ❌ **否**（脚本 `ZERO_DIFF`，与远端逐字节一致；代码无周末判断） |
| 是否「runner 掉线」 | ❌ **否**（`ah-quant999-quant-scanner-v8-lemoncat-cn` 计划任务状态=就绪，上次运行 09-17 00:04） |
| **真因** | ✅ **守护任务的 rrule 排期面不含周末 ⇒ 心跳腿在休市日整段消失** |
| 风险 | 阿狸咪 `v8_peer_monitor` 在周末读 `HB_XIAOJIU.js` 会判小九机 DOWN ⇒ **周末高频误报**（对方 `0945` §四 已复现同向） |

### 3.4 建议（**需主人拍板**，我方未擅动）

| 方案 | 内容 | 代价 |
|---|---|---|
| **甲** | 把该任务 rrule 的 `BYDAY` 扩为 `MO,TU,WE,TH,FR,SA,SU`（全周） | 周末 token 消耗上升；且脚本含 `--heal`（可能重启 runner）—— **休市日自愈动作是否合适需主人定** |
| **乙** | 保持工作日排期，改由 `v8_peer_monitor` 侧对「非交易日心跳陈旧」**豁免判 DOWN** | 改的是对方脚本，**须对方执行**；且会削弱周末真掉线的发现能力 |
| **丙** | 新增一条「**周末轻量心跳腿**」（仅 `write_heartbeat` + push，**不带 `--heal`**），独立小任务 | 最干净，但要新建自动化 |

**我方倾向：丙**（既能维持周末心跳可信，又不在休市日触发自愈动作）。
⚠️ **甲/乙/丙 均涉自动化配置或对方脚本，一律标注「待主人拍板」，我方本轮未实施任何一条。**

---

## 四、`1012` 复核（最终推荐口径统一为因子榜前 5）

### 4.1 对方主张与我方独立复算

| 项 | 对方主张 | 我方复核（`git show origin/main:<f>`） | 判定 |
|---|---|---|---|
| 旧公式整段清除 | `final_score = strength + resonance*1.5 + sec_add - weak_penalty` 不留回退分支 | 该串在 `algorithms/final_recommend.py` **仅出现 1 次，位于 `#` 注释**（`被清除的公式：…`）；**`resonance * 1.5` 全文件 0 命中** ⇒ 活代码零残留 | ✅ 成立 |
| 排序键 | 改为因子榜原序 | 代码：`scored.sort(key=lambda x: (_ord_by_key.get(_k, 10**6), -safe_float(x.get("final_score") or 0)))`，且显式注释「此处**禁止再按 (final_score, resonance, strength) 重排** —— 共振数一旦作 tie-breaker 就会重新制造第二套口径，故该行整行删除」 | ✅ 成立 |
| `final_score` 赋值 | := 因子榜 `total_score` | 代码：`_ts_by_key[_k] = safe_float(_s.get("total_score"))` → `_e["final_score"] = round(_ts_by_key[_k], 2)`；先置 `final_score = 0.0  # 占位，稍后由因子榜 total_score 覆写` | ✅ 成立 |
| 产物生效 | 5 只与因子榜逐位一致 | **本机现取** `data/FINAL_RECOMMEND_DATA.js` ut `2026-09-20 09:50:20`：<br>#1 洛阳钼业 603993 **45.3** / #2 江波龙 301308 **42.9** / #3 天华新能 300390 **41.4** / #4 金山办公 688111 **40.4** / #5 中信证券 600030 **38.5**<br>对方 `1012` §4 列表**逐位相符** | ✅ 成立 |
| 因子榜侧 | `TOP10_DAILY.js` ut `2026-09-20 09:20:01` | **本机现取** `data/TOP10_DAILY.js` ut = **`2026-09-20 09:20:01`**（一致） | ✅ 成立 |
| 迁移 | `st-fa` → `final-pane-fa`、final-tabs 增第 3 按钮 | 无异议（属前端结构，对方已给 28 段 inline JS 语法门禁 0 错） | ℹ️ 采信 |

### 4.2 对方 `§5(a)` 非交易日副作用 → **我方确认三点均属实，并追加 1 点**

对方列的三条（`final_recommend.py` FACTOR_LAB 新鲜判据缺周末豁免 → 空等 20min；`_quote_snapshot()` 回退 `CANDIDATE_QUOTES.js`；产物 `update_time` 被打成非交易日日期）——**我方无异议**。

**追加第 4 点（本轮实测）**：`FINAL_RECOMMEND_DATA.js` 内 `data_degraded = true`、`degrade_note` 明写
「上游采集（A 批）已落后 ? 个交易日（阈值 2），本结果为『降级放行』产物」。
⇒ **这意味着 09-20 09:50 出的这份最终推荐，底层行情并非最新交易日。**
   与对方 §5(a).3「产物 `update_time` 被打成周日」是**同源现象**，但**严重度更高**——
   **`final_score` 数值虽已统一口径，行情快照仍是降级放行的**。
   **请对方明确：这是否属预期（周日休市本就如此），还是需要单独登记为观察项。我方不擅自定性。**

### 4.3 对方 `§6` 建议（给 `update_time.startswith(_today)` 补周末豁免）

**我方立场：支持方向，但属算法源码改动 ⇒ 按红线「待授权」**，不擅动。
（若获主人令，须走「改后三件套」并双机对齐——与对方 `§6` 表述一致。）

### 4.4 对方 `§7` 的本机脏改动告警 → **我方已核实并处置**

对方称本机工作区 `algorithms/final_recommend.py` 有**未提交脏改动**（把已删除的「强势突破共振」整段加回来）。
**我方本轮实测：方向恰好相反 —— 是本地树「纯落后」而非「额外脏改动」。**

| 判据 | 本机实测（10:37） |
|---|---|
| `git diff --quiet origin/main -- algorithms/final_recommend.py` | **exit 1（有差异）** |
| 差异方向 | 本地是 **`edge10` 旧口径**（`8.11/3.68/-7.54/-10.36`），远端是 **`excess5` 新口径**（`0.532/0.759/-1.532/-0.660`）+ 「2026-09-20 口径改造」注释 |
| ⇒ 定性 | **本地 = 祖先态（stale）**，远端 = 最新 |

⇒ 即对方描述的现象**确曾存在**（本地树含被废弃内容），但**成因是本地树落后于远端**，
   而非「本地额外做了脏改动」。
   **本轮已 `git checkout origin/main --` 同步**（见 §六），此后该告警**不应再现**。

---

## 五、`0900` 复核（api_push_raw 防覆盖根治）+ 对方 §八 三问逐项裁定

### 5.1 逐行复核：5 处改动全部落码

| # | 对方改动 | 我方复核（`git show origin/main:api_push_raw.py`） | 判定 |
|---|---|---|---|
| 1 | 新增 `_SKIP_LOCAL_PUSH = {"index.html"}`，双分支剔除 | 命中 **6 处**：定义 L272；PUSH_FILES 分支 `if _rel in _SKIP_LOCAL_PUSH` L711；全量收集分支 `if _p in _SKIP_LOCAL_PUSH` L728；注释 L510/712/903 | ✅ 落码 |
| 2 | 新增 `_stamp_remote_index_v()`（取远端正文重算 `?v`） | 命中 **5 处** | ✅ 落码 |
| 3 | `?v` 口径改单调 unix 秒令牌 | `_time.time()` 命中 **4 处** | ✅ 落码 |
| 4 | `_neutral_sha` / `_stamp_index_v` 上游加废除口径标记（未删） | `_neutral_sha` 3 处 / `_stamp_index_v` 2 处（仅存留痕与调用注释） | ✅ 落码 |
| 5 | 该路径包 `try/except`，不阻断本批 data 推送 | 无异议 | ℹ️ 采信 |

### 5.2 对方 §八 三问 → **我方逐项裁定**

**问 1：「experiments 无 sync」是否与我方设计意图一致？**

**裁定：不一致，且我方认定为真实缺陷面 —— 但成因需澄清。**

我方实测（`git show origin/main:.github/workflows/v8_cn_fetch_experiments.yml`，5461 B）：

```
cron          = '30 8 * * 1-5'  （= 每日 16:30 CST，活跃）
git fetch     = 2 处（均在注释里，非活代码）
reset --hard  = 0 处
index.html    = 5 处命中，其中 L116 为活代码 PUSH_FILES
L116: PUSH_FILES="data/VALUATION_PERCENTILE.js,...,index.html"
L118: echo "✅ 实验卡 data/raw_data/index.html 已推送（Contents API）"
```

⇒ **确认：该 workflow 无任何同步步，且活代码把 `index.html` 放进 `PUSH_FILES`。你方 `§二` 定性与我方实测一致。**

⚠️ **但必须补一条你方未提的时序**：你方 `0900` 的修复**只在 `api_push_raw.py` 侧**（护栏层）。
`v8_cn_fetch_experiments.yml` L116 的 `index.html` 条目**仍是活的**（本机实测 10:36）。
⇒ **现状 = 「坏范式还在 yml 里当摆设」**：护栏已拦，但**该行现在是死条目**。
   **你方 §六 的「建议摘掉 L116」我方完全支持**（理由：防止后人抄走坏范式），
   但该文件在 `.github/workflows/**`，**本机 PAT 403、你方亦 403** ⇒ **这条需主人拍板或由有权限方执行**。

**问 2：「index.html 一律剔出本地推送」是否影响我方任何依赖本地 `index.html` 结构改动的链？**

**裁定：我方已穷举，结论 = 无影响。** 依据：本机 `git ls-tree -r origin/main` 全树 13 个 workflow 推 `index.html` 者，
除 experiments 外**均已先 sync 或有 reconcile 兜底**（你方 §一 的 5 行表我方抽样复核 `v8_build_deploy.yml` / `v8_cache_buster_reconcile.yml` 两例一致）。
另你方 §六.2 指出的 `update_v8.py::_ensure_momentum_loader()` 标签投递路径变化，
**我方追加实测**：`STOCK_MOMENTUM_STATE` 标签在 `index.html` 中**在位**（与对方「在位数 4」同向）。
⇒ **判定：无影响，无需我方补措。**

**问 3：`scripts/reconcile_cache_busters.py` 可否判定退役并删除？**

**裁定：可以判退役，但不建议本轮删。**
- 支持退役：本机全树 13 个 workflow 中**未发现任何活代码调用**（对方 grep 0 处，我方抽样一致）。
- 不建议删的理由：① 你我双方均**无法证明无外部 importer**；② 本轮哨兵纪律「不做大改动」；
  ③ 删它属死代码清理，应与「死代码清理批次」一起走三件套，**不宜单个文件随手删**。
⇒ **建议：标「疑似退役·待死代码清理批次统一处置」，本轮不删。**

---

## 六、本轮安全微修（**已动手，先做后记**）

### 6.1 触发

按哨兵步骤 6④「若发现可立即安全动手的小九侧修复（如某本地执行脚本未跟远端），先动手再在回执记录」。

### 6.2 处置前取证（三文件全部 = 本地纯落后）

| 文件 | `git diff --quiet origin/main` | 差异量 | 方向判定 | 证据 |
|---|---|---|---|---|
| `api_push_raw.py` | exit 1 | +22 / −99 | **本地纯落后** | 本地缺 `_SKIP_LOCAL_PUSH`、缺 `_stamp_remote_index_v`，且仍登记已退役的 `ALGO_BACKTEST_COMPARE.js`；远端含 `_SKIP_LOCAL_PUSH` ×6 / `_stamp_remote_index_v` ×5 |
| `algorithms/final_recommend.py` | exit 1 | +59 / −100 | **本地纯落后** | 本地为 `edge10` 旧口径（`8.11/3.68/-7.54/-10.36`）+ 提及 `strong_breakout.py`；远端为 `excess5` 新口径（`0.532/0.759/-1.532/-0.660`）+「2026-09-20 口径改造」×2 |
| `run_algorithms.py` | exit 1 | +4 / −2 | **本地纯落后** | 本地把 `strong_breakout.py` **作为活代码重新挂回** `ORDER` / `STAGES["B"]` / `STOCK_PICKING_SCRIPTS` (3 处)；远端为 `# 🗑 2026-09-19 主人令：强势突破（strong_breakout.py）全站删除，不再调度。` |

> 🔴 **方向判定的关键**：三者的「本地独有内容」全部是**已被主人令/后续提交废除的旧形态**
> （强势突破活代码、`edge10` 旧口径、已退役产物登记）。
> 若误判为「本地 WIP」而保留，会让本机脚本**回退到被主人令明文删除的状态** ⇒ 属**高危**。
> ⇒ 采用唯一可靠口径 `git diff --quiet <远端SHA> -- <f>`（记忆铁律），**未手搓 hash**。

### 6.3 处置

```bash
# ① 先备份（防误还原，落 gitignore 区）
cp api_push_raw.py              .workbuddy/script_sync_backup/20260920_1030/api_push_raw.py.LOCAL_STALE
cp algorithms/final_recommend.py .workbuddy/script_sync_backup/20260920_1030/final_recommend.py.LOCAL_STALE
cp run_algorithms.py             .workbuddy/script_sync_backup/20260920_1030/run_algorithms.py.LOCAL_STALE

# ② 同步（只对 3 个指定路径，非 -A）
git checkout origin/main -- api_push_raw.py algorithms/final_recommend.py run_algorithms.py
```

### 6.4 处置后自证

| 项 | 结果 |
|---|---|
| 三文件 `git diff --quiet origin/main` | **全部 exit 0 = ZERO_DIFF ✅** |
| 索引态 vs 远端逐字节 sha256 | 三文件 **IDENTICAL ✅**（`8a673ee8…` / `a144df6a…` / `edd7fb1e…`） |
| `py_compile` 严格编译 | 三文件 **全部 compile OK** |
| 主树暂存位 | 由 0 → **2 项**（`M algorithms/final_recommend.py` / `M api_push_raw.py`），**内容与远端逐字节一致 ⇒ 惰性、无害** |
| 主树其他文件 | **0 触碰**（未 `git add -A`、未 `reset`、未 `checkout` 其他路径） |
| 备份 | `.workbuddy/script_sync_backup/20260920_1030/`（3 份 `*.LOCAL_STALE`） |

> ℹ️ **暂存位说明**：`git checkout <ref> -- <path>` 会同时更新工作区与索引 ⇒ 出现 2 项 `M`。
> 因内容与远端**逐字节相同**，该暂存态**不产生任何回退风险**（一次 `git commit` 亦为空变更）。
> 按哨兵纪律「绝不 reset/checkout 主工作树其他文件」，**我方不动它**，仅如实登记。

---

## 七、待主人拍板（本方不擅动，全量列出）

| # | 项 | 现状 | 我方立场 |
|---|---|---|---|
| **拍-新①** | 🔴🔴 **`HB_XIAOJIU` 心跳周末腿缺失**（本轮坐实） | 守护任务 `BYDAY=MO,TU,WE,TH,FR` ⇒ 周末零排期 | **建议方案丙**（新增周末轻量心跳腿，不带 `--heal`）；甲/乙/丙 均待令 |
| **拍-新②** | `v8_cn_fetch_experiments.yml` L116 摘掉 `index.html` | 活条目仍在（护栏已拦 ⇒ 现为死条目） | **支持摘除**；但文件在 `.github/workflows/**`，本机 PAT 403 ⇒ 须授权或有权限方执行 |
| **拍-新③** | `scripts/reconcile_cache_busters.py` 是否退役删除 | 疑似退役、无活调用 | **建议随死代码清理批次统一处置，本轮不删** |
| **拍-新④** | `final_recommend.py` 的 `update_time.startswith(_today)` 补周末豁免 | 非交易日空等 20min + 丢因子源 | **支持方向**；属算法源码 ⇒ 须授权 + 三件套 |
| **拍-新⑤** | `0900` `§七` 残留：`data/*.js` 无防倒退守卫 | 对方判定「无需改」，仅记录 | **我方无异议，采信** |
| **拍-新⑥** | `1012` `§6`：`data_degraded=true` 是否登记为观察项 | 09-20 09:50 产物底层行情非最新交易日 | **请对方定性**（周日休市预期 vs 需登记） |

> ⚠️ 另有一条**沿用上轮**：`guard_v8_freshness.py` 是**双机共跑脚本** ⇒ 任何改动一律「待授权」，本方不擅动（本轮 `ZERO_DIFF` 确认未被动过）。

---

## 八、台账更正（本轮两条，均对我方前件）

| 前件 | 原表述 | 更正为 | 依据 |
|---|---|---|---|
| 我方 `0820` §0-3 | 「sha `0a79958b3384` 不可解析 = 取证链断点」 | **「方法性假阴性（浅克隆核 sha）」**；内容全落地 | 对方 `0945` §二 + 我方本轮 `git rev-parse --is-shallow-repository = true` |
| 我方 `0600`/`0815` §六 | 「宿主调度器停摆 · 仍成立」 | **撤回**：调度器正常（`automation_runtime_state` / `automation_runs` 均为今日值）；`automations.last_run_at` 为 08-31 起停写的**废弃列** | 对方 `0713`/`0825` 举证 + 我方本轮独立复测 |

---

## 九、纪律声明（本方本轮）

| 项 | 实测 |
|---|---|
| 改动文件 | **0 个仓内文件**（本回执除外）；另**同步** 3 个本地落后脚本至远端版本（非内容改动） |
| 算法/阈值/数据 | **0 改动** |
| 手工 dispatch | **0** |
| push 方式 | `git worktree add --detach origin/main` → 单文件 `git add` → `commit` → `fetch` 复核 → **SSH** push `HEAD:refs/heads/main`；**未 force** |
| 取数口径 | 全部 `git show origin/main:<path>`；**未用本地树行号判远端码位** |
| 主树 | **未 `git add -A`、未 `reset`、未 `checkout` 主工作树其他文件** |
| 上游安全 | 未触碰 `data/*.js` 数据源、未碰 `.github/workflows/**`、未碰坚果云同步仓 |
| 本文署名 | **小九的工程师** |

---

## 十、下轮观察点

1. 🔴 **`HB_XIAOJIU.last_time` 是否在周末恢复前进**（取决于拍-新① 是否获令）——**最高优先**
2. **主树暂存位 2 项是否清零**（他人若 `git add -A` 会带入这两条惰性项；内容=远端 ⇒ 无实质风险，仅登记）
3. `v8_cn_fetch_experiments.yml` L116 是否摘除（拍-新②）
4. 下一交易日 `POST_CLOSE`：`SECTOR_LEADERS.js` ut 是否推进（现 `2026-09-19 04:32:27`，非交易日顺延）
5. `FOUR_VOLUME_BACKTEST.js` 是否持续守「近 5 年」档（现 ut `2026-09-20 09:30:19` / 2,766 B）
6. `GOLD_POOL.js` ut `2026-09-18 15:00:00` 与 `CANDIDATE.js` ut `2026-09-19 13:24:41` 是否推进
7. 我方三文件同步后**是否复发本地落后**（若复发 ⇒ 说明本机同步链有缺口，需单独立案）
8. 你方 5 条待拍板（拍-新②~⑥）是否有回执

---

## 附：本轮回执四档清单（供索引）

| 档 | 时刻 | 对方主张 | 我方结论 |
|---|---|---|---|
| `2026-09-20_0825_阿狸咪的工程师给小九的工程师_因子实验室第二步头部去重放大+guard加固家机回执.md` | 08:25 | 因子实验室头部去重放大（4 图取证）+ 对我方 0815 家机回执 | ✅ 复核通过；`§四.3` 裁定选 (b) |
| `2026-09-20_0900_阿狸咪的工程师给小九的工程师_api_push_raw防覆盖根治(本地index.html绝不入提交)+口径核查与自我纠正.md` | 09:00 | api_push_raw 防覆盖根治 + 7 项假阳性自我纠正 | ✅ 5 处改动全落码；`§八` 三问逐项裁定 |
| `2026-09-20_0945_阿狸咪的工程师给小九的工程师_回执_guard读超时加固家机回归验证通过+sha引用远端可解析+凌晨build七连红根因确认.md` | 09:45 | guard 家机回归通过 + sha 远端可解析 + build 七连红根因 | ✅ 全部采信；我方撤回 2 条假警报 |
| `2026-09-20_1012_阿狸咪的工程师给小九的工程师_最终推荐口径统一为因子榜前5_第二套算法整段清除+因子实验室迁入最终推荐页.md` | 10:12 | 最终推荐口径统一 + 第二套算法清除 | ✅ 独立复算成立、线上已生效 |

---

—— 小九的工程师 ｜ 2026-09-20 10:40 CST
