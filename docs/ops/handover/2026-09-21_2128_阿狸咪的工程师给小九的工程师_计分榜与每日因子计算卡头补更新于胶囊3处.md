# 交接件：当日因子计分榜/每日因子计算 卡头补「更新于」胶囊（3 处）

- 时间：2026-09-21 21:27:43（北京时间，阿狸咪夜间窗口）
- 提交：`a5385e5e45`（index.html 单文件，+11/-0 行级 3 处，1350038 B，cr=0）
- 署名：阿狸咪的工程师给小九的工程师

## 主人令
主人 21:19 截图指示：「当日因子计分榜」卡头（🏆 当日因子计分榜 / 当日榜单 / 候选池 top20·按总分降序）**加上「更新于」**。

## 改动 3 处（零功能变更、零数据变更）
1. **卡头新增槽位**：`🏆 当日因子计分榜` 卡名后加 `<span id="dsbUpdSlot">`（全站规范：卡名置顶 + 更新于紧随）。
2. **计分榜填充**：`__renderDailyScoreboard` 数据就绪后 `setBadge('dsbUpdSlot','auto','短线', D.update_time)` —— `auto` 时段由 `_slotByTs` 按真实 `update_time` 动态判（09-12 主人令标准），真值读 `TOP10_DAILY.update_time`，不写死。
3. **补齐同源死槽位**：🧮 每日因子计算卡的 `dfcUpdSlot`（09-19 主人令「更新于紧随卡名」时建了槽位但**从未被填充**，全仓零调用）—— 本次在 `__renderDailyFactorCalc` 末尾补 `setBadge('dfcUpdSlot','auto','短线', TOP10_DAILY.update_time)`。

## 口径说明
- 两卡同源：均为 `algorithms/generate_top10.py` 引擎产物投影，`update_time` = `TOP10_DAILY.js` 的产物真值（本机 2026-09-20 17:19:36 批次）。
- 计分榜页脚原有「· 更新于 …」文字说明保留（先例：艾略特波浪卡页脚+卡头胶囊并存），卡头胶囊是主展示。

## 校验（原始输出）
- 3 处 patch 唯一性断言：**各匹配 1 次**
- inline script 语法：**0 错**（128 标签 / inline 28 / 外链 100，前后一致）
- `[10/10]` 锚点：**34/34**，改前 miss=0 / 改后 miss=0 / 新增 miss=0
- 小九口径同步未被触碰：`07:15`=0 / `08:50`=8（随线上演进）
- 推送：Git Data API `base_tree` 只列 index.html、`force:false`、fast-forward 成功
- 推后回读 **逐字节一致** ✅（1350038 B，md5 bdd3f6c20018）
- CI（run of a5385e5e45）：见本文件推送前最新 step 级结论（附于会话汇报）

## 过程备注
- 推送脚本初版 POST 401：`api()` 封装在带 method 时新建 Request 丢失 Authorization 头（GET 复用原对象故正常）——已修复并写入 skill。
- 推送窗口内远端 tip 两次推进（`94405721ef` 21:23 / `7a4b6a86ff`），脚本自取 tip 自适应，未发生覆盖。
