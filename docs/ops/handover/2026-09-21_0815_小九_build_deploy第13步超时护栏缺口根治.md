# 小九 → 阿狸咪 · `v8_build_deploy` 第13步「部署到 main」超时护栏缺口根治

- **撰写方**：小九的股票专家（`lemoncat-cn`，`2814546@qq.com`）
- **数据采集时刻**：2026-09-21 08:00–08:12 CST（本机时钟经 `datetime.now(utc)+timedelta(hours=8)` 机器换算校验，Δ=0s）
- **上游交接**：本档承接 `docs/ops/handover/2026-09-21_0715_小九_强势突破残留产物清除与防回推防红灯复发双层护栏.md`

---

## 一、起因：主人截图「运维和看板 还有失败和红灯」

主人所截红灯 = `all_STRONG_BREAKOUT`（`HEALTH_CHECK.js` 全局 `fail=1` 仅此一项）。该红光**已在上游交接档中闭合到「产物已删 + 断回推通道 + 防复发」三层**，线上 14/14 僵尸产物已 404。

但本轮排查发现：**红光不灭的拦路虎是更深一层的故障 —— 部署链落不了地**。本档专门处理这一层。

---

## 二、真因（全部实测，非推断）

### 2.1 本轮推翻的前一轮结论（重要）

上游交接档写「第13步是唯一没有步级 `timeout-minutes` 的步 ⇒ 挂账待修」。**该结论在事实层面正确，但归因不完整**：

- 加固提交 `301b30acd5b7`（`fix(ci+audit): [11/11] defer 数据缓存守卫上线 + 推送步硬超时根治长阻塞`，**09-21 04:03:32 CST**）**已经**给第13步加了护栏。
- 因此「加护栏」这条挂账**已被阿狸咪方完成**，不应再作为待办。

### 2.2 护栏加了但**没兜住**（本轮核心发现）

加固上线后，第13步**仍然失败**。近 40 轮逐 run 取证：

| run id | 结论 | created (CST) | step[13] 耗时 |
|---|---|---|---|
| `35543304050` | failure | 06:59:53 | **38.8 min** |
| `35541752440` | failure | 06:27:37 | **36.1 min** |
| `35542922345` | failure | 06:51:30 | **31.8 min** |
| `35534551600` | failure | 04:08:03 | 39.0 min |
| `35530486569` | failure | 02:53:07 | 40.4 min |
| `35528401723` | failure | 02:14:12 | 39.5 min |
| `35537151823` | failure | 04:57:02 | 35.6 min |
| —— 对照：成功轮 —— | | | |
| `35540146008` | success | 05:56:13 | 20.6 min |
| `35540712807` | success | 06:08:05 | 20.4 min |
| `35534831881` | success | 04:13:13 | 18.4 min |
| `35531200503` | success | 03:06:16 | **8.6 min** |

**判据**：失败轮 step[13] **恒 ≥ 31.8 min**；成功轮 ≤ 20.6 min。失败步**无一例外**是本步。

### 2.3 为什么上一版护栏没兜住 —— 它只包了两个 git 命令

第13步 `for i in 1 2 3; do ... done` 循环体内，每轮的实际动作与护栏状态：

| # | 动作 | 耗时量级 | 加固前护栏 |
|---|---|---|---|
| ① | `git fetch main` | ≤ 3 min | ✅ `timeout 180` |
| ② | `git reset --hard FETCH_HEAD` | ~0 | 无需 |
| ③ | **`python update_v8.py`（全量重建）** | **≈ 5–8 min** | ❌ **无** |
| ④ | **`python guard_v6_memo.py`** | ~0.2 min | ❌ **无** |
| ⑤ | **`python update_v8.py --only-cache-busters`** | ~1 min | ❌ **无** |
| ⑥ | `git push HEAD:main` | ≤ 3 min | ✅ `timeout 180` |

⇒ **`timeout 180` 只覆盖 ①⑥，合计 6 min/轮；而时间实际有 70%+ 花在 ③④⑤ 上，全部无界。**
每轮最坏 ≈ 15 min，三轮 ≈ 45 min —— 与实测 31.8~40.4 min 完全吻合。

更关键：③ 原写法为裸调用，**失败/卡住后仍继续下一轮** ⇒ 每轮白烧 ~8 min，这是 30 min+ 的直接成因。

---

## 三、本轮改动（已上线）

**文件**：`.github/workflows/v8_build_deploy.yml`
**提交**：`61bc3fc03070ea33b7ec37c5ddf61746e42919af`（`2026-09-21T00:09:42Z` = 09-21 08:09:42 CST）
**签名**：`ah-quant999 <2814546@qq.com>`
**尺寸**：42450 B → **44761 B**；blob sha `870134b628a8b48c50640ddc1632bb8ae28d9b24`

改动逐条：

1. **`timeout 300 python update_v8.py`**（重试路径全量重建）+ **失败即 `exit 1`**
   - 原为裸调用。理由：重建是**不可省**步骤，它失败则本轮产物不可信，继续重试无意义。
2. **`timeout 120 python guard_v6_memo.py`** + `|| echo 非致命`
   - v6 戳对齐属锦上添花，超时不应拖死部署。
3. **`timeout 180 python update_v8.py --only-cache-busters`**（主路径）
4. 补注释说明护栏缺口成因与预算（供后来者不再重踩）。

**校验**：
- `yaml.safe_load` 严格解析通过；`jobs = ['gate','build','intraday_patrol']`
- 行尾纯 LF（`CRLF=0`，符合本仓 `.gitattributes`）
- 8 项结构断言全绿（三处 timeout 包裹 / 重建失败致命 / 保留 `for i in 1 2 3` / 保留三次失败兜底）
- 推送后取远端真身复核：**远端 == 我方补丁 `True`**，四项关键串全部命中

---

## 四、改后耗时预算（预期，尚未被任何 run 验证）

| 路径 | 改前 | 改后（预期） |
|---|---|---|
| 单轮 | 最坏 ~15 min（无界） | ≤ 3+5+1+2+3 = 14 min（有界） |
| 三轮全落空 | ~45 min（实测 31.8~40.4） | 重建失败即 `exit 1` ⇒ 提前收敛 |

**⚠️ 必须显式声明的边界**：以上为**预算推算**，不是实测结论。截至本档撰写时刻，**尚无任何一轮新代码 run 跑完**。改动的有效性须由「下一次 step[13] 的耗时」与「`HEALTH_CHECK.js` 的 `updated` 是否前进」两项实测确认。

---

## 五、交接给阿狸咪的事项

### 5.1 请复核本改动

`.github/workflows/v8_build_deploy.yml` 是本仓**双机共写热文件**，我方已带自身 sha 直推 main（非 force）。请确认与您 04:03 的加固意图一致、无语义冲突。

### 5.2 建议您评估的**下一层**根因（我方未动，因触及设计取舍）

即便加齐超时，**根本张力仍在**：`v8_cache_buster_reconcile`（`v8-cache-bot`）每 15 min + 每次 `data/`/`raw_data/` push 都推 main，实测推进间隔 **1.1 / 1.3 / 3.1 / 7.2 / 9.2 min**（多数 < 4 min，即短于一次全量重建耗时）。

⇒ build 每次 push 用的都是「4 分钟前的基线」⇒ **被拒是常态** ⇒ 重试又要重跑全量。停摆窗口实证：卡住的 38 min 内 cache-bot 推了 **7 次**。

可选方向（**需主人拍板，不属本轮授权范围**）：
- **A**：让 reconcile 让开 build 窗口（build running 时 reconcile 跳过）；
- **B**：由 build 收口 `?v`（`update_v8.py --only-cache-busters` 已在做），reconcile 降频到 30–60 min 仅作兜底；
- **C**：仅维持本轮超时护栏，接受「多数轮 push 需重试一次」的现状。

### 5.3 另一处仍存在的结构性风险（未改）

`concurrency.group = v8_build_deploy-<event>` + `cancel-in-progress: false`（09-19 排队制）⇒ 一个卡住的 job 会**堵死同桶后续 run**。本轮实测：两轮 build job 双双卡第13步 30~38 min，另 2~3 轮 pending 排队。

若采纳 5.2 的 A/B，此风险会显著下降。**本轮未动 concurrency 配置**，因它涉及跨桶语义，不宜在未拍板前改。

### 5.4 与红灯的关系（请勿混淆）

- `all_STRONG_BREAKOUT` 红灯的**产物层**已闭合（14/14 已 404）。
- 红灯的**显示层**要等一次成功的 build 落地才会消失 —— 判据：`data/HEALTH_CHECK.js` 的 `updated` 前进且 `summary.fail` 由 1 → 0。
- 截至 08:12 CST：`updated` 仍 **`2026-09-21 06:44:40`**、`fail` 仍 **1**；`v8-cloud-bot` 最后一次 `v8 build:` 提交仍停在 **06:48**。

---

## 六、本轮前后事实对照（防误读）

| 项 | 上游档（07:15 CST） | 本档（08:12 CST） |
|---|---|---|
| 第13步步级 `timeout-minutes` | 「唯一没有的步」 | **事实仍无**（仅 3 处 timeout：L93 gate=5、L151 build job=60、L548 intraday=10） |
| 第13步命令级护栏 | 未提及 | 04:03 已有 `timeout 180` 包 push/fetch；**但未包 python 步** |
| 「加护栏」挂账 | 待办 | **部分已由阿狸咪完成；缺口部分本轮补齐** |
| 失败步定位 | 「第13步卡住」 | **精确：step[13] failure，31.8~40.4 min** |
| 改后有效性 | —— | **未验证**（无新代码 run 跑完） |

---

## 七、上报主人的三句话（要点）

1. 截图红灯 = `all_STRONG_BREAKOUT`，其**产物层**已闭合（线上 14/14 已删）；显示层要等部署落地才消失。
2. 真正拦路的是**部署链第13步超时护栏有缺口**：`timeout` 只包了 git 命令（6 min/轮），**没包耗时占 70%+ 的 `update_v8.py` 全量重建**（~8 min/轮、且失败仍继续重试）⇒ 每轮最坏 15 min、三轮 45 min，实测失败轮恒 ≥ 31.8 min。
3. **本轮已补齐并直推 main**（`61bc3fc03070`）；**改后有效性尚未实测**，须等下一轮 run。更深层的「cache-bot 与 build 抢 main」张力**未动**，列 5.2 供拍板。

---

> 本档仅供运维交接参考，不构成投资建议。
