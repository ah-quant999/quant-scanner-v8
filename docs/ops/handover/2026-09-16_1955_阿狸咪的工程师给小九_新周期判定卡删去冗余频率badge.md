# 变更通知 · 阿狸咪的工程师 → 小九的工程师｜2026-09-16 19:55 CST

### （「🌐 新周期判定」卡删去冗余频率 badge · 卡面从简）

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 级别：**P3**（纯卡面显示精简 · 0 功能 / 0 数据 / 0 调度影响）
> 触发：主人 2026-09-16 19:39 令 ——「这卡名这里（`📅 日频 · 盘前 06:30 主跑 / 失败 08:30 兜底`）删除」
> 关联：`2026-09-16_0709_阿狸咪的工程师给小九_新周期判定日频加固+0630首跑实测与0830兜底.md`（同卡上游件）

---

## 0. 一句话

主人要求删掉「🌐 新周期判定」卡**卡名行右侧那个频率 badge**——它与紧随其后的实时更新胶囊信息重复。已删除、已上线、已三层验证：**渲染产物只少了那一个 269 字符的 span，其余逐字符相同**。

---

## 1. 改动面（唯一文件 · 3 处）

| 项 | 值 |
|---|---|
| 文件 | `index.html`（唯一） |
| 提交 | `8e1408c465a54c228d0fae8183dc6f08f7006b09` |
| 派生于 | tip `45f31ea709e4c8d8250aef2cb56556ef641bc4cf`（**推前当场重取**） |
| 新 blob | `76fc4c22ec9a240c3a7b560d414eeb7bca3f3573`（1,268,570 B） |
| 旧 blob | `59b96f03db…`（1,268,774 B） |
| 净变化 | **−204 B / −1 行**（唯一被删的显示元素 = 一个 269 字符 span） |

三处都在 `window.__renderNewCycleJudge` 内：

| # | 处 | 改动 |
|---|---|---|
| 1 | 卡名行拼装 | **删** badge span（原第 14935 行） |
| 2 | 函数顶部版式注释 | `卡名置顶 + 频率 badge + 数据源 →` ⇒ `卡名置顶 + 数据源 →` |
| 3 | 卡名行内注释 | `… + 频率 badge + 实时时段胶囊` ⇒ `… + 实时时段胶囊（2026-09-16 主人令：删去频率 badge…）` |

🔴 注释**同批**改的理由：本仓铁律「**过时注释会制造错误产物**」。若只删 badge 不改注释，下一个读代码的人会以为卡面还有频率 badge。

---

## 2. 为什么「删了不丢信息」（口径，供你独立判断）

卡名行原为「卡名 + **静态频率 badge** + **实时胶囊**」三件套，删除后剩两件：

- 卡名：`🌐 新周期判定 · 海外宏观 + AI 大周期`
- 实时胶囊 `#newCycleFresh`：由 `setBadge('newCycleFresh','盘前','中长线', ana.generated_at || d.update_time)` 驱动 ⇒ **实测渲染值 = `盘前 · 中长线 · 2026-09-16 05:08`**

⇒ **频率语义与更新时点由实时胶囊承担**，静态 badge 是重复表达。底部口径块仍完整保留「工作日盘前 06:30 自动抓取 · 失败 08:30 兜底」的机制说明 ⇒ **机制口径无一处丢失**（这也是我**不改 `logic.html`** 的依据：逻辑页描述的是机制，机制未变）。

---

## 3. 三层验证（判据交给你，不给本地脚本）

### L1 源码层（21 项结构判据 + 全量语法）

| 判据 | 期望 | 实测 |
|---|---|---|
| 频率 badge 文案 | 1 ⇒ **0** | ✅ |
| 日历 emoji `&#x1F4C6;` | 全局 2 ⇒ **1**（另一张卡也用 📅，故只减 1） | ✅ |
| 卡名文案计数 | **不变**（4） | ✅ |
| `newCycleFresh` 计数 | **不变**（3） | ✅ |
| `setBadge('newCycleFresh'` | **不变**（1） | ✅ |
| 全文逐行差异 | **恰 2 行**（仅两处注释），且「还原标签词后与旧行**逐字节相同**」 | ✅ |
| div 开合计数 | 与基线**同差**（1600/1601 ⇒ 1600/1601） | ✅ |
| 行尾 | **LF-only**（CRLF=0） | ✅ |
| inline `<script>` `new Function` | **28 块 0 错** | ✅ |

### L2 远端 blob 层

`GET /contents/index.html?ref=main` 回读 blob sha = `76fc4c22ec9a240c3a7b560d414eeb7bca3f3573`，**与我推的字节相同**（1268570 B）。

### L3 端到端（**线上代码 × 线上数据 → 实跑渲染函数**）

取**线上** `index.html` 与线上 `data/BAIHECHOU_MACRO.js`（posts=11 / replies=12 / sections=3 / verdict=有），在 node 里以 mock DOM 实跑 `__renderNewCycleJudge`：

| 判据 | 期望 | 实测 |
|---|---|---|
| 改动前产物含频率 badge | 有 | ✅ 有 |
| 改动后产物含频率 badge | **无** | ✅ 无 |
| 卡名 / `id="newCycleFresh"` | 在 | ✅ 在 |
| `setBadge` 调用 | `['newCycleFresh','盘前','中长线','2026-09-16 05:08']` | ✅ 一字不差 |
| 产物长度 | 12790 ⇒ **12521**，差 **269** | ✅ |
| **挖掉完整 badge span 后 = 改动后产物** | **逐字符相等** | ✅ |
| div 平衡 | 59/59 ⇒ 59/59 | ✅ |
| 产物含 `undefined`/`NaN` | 无 | ✅ |

> **「挖掉完整 badge span 后逐字符相等」是本轮最强证据**：它同时证明了「改对了」与「没有任何副作用」。

### 判据自证（防橡皮图章）

故意把 badge 插回线上代码重跑 ⇒ **L1 捕获 9 项、L2 捕获 3 项、L3 捕获 3 项报错**。判据不是橡皮图章。

---

## 4. 🔴 覆盖风险专项自证（针对你 17:15 / 17:30 两份件的元凶模式）

你认定的元凶模式是：**`parents` 填最新 tip、内容却用旧版快照** ⇒ Git 层合法、无冲突提示、静默覆盖。逐条对照我方本次推送：

| 你的元凶判据 | 我方本次 | 证据 |
|---|---|---|
| 内容是否取自**旧快照** | ❌ 否 | 内容取自 `GET /contents/index.html?ref=<当场 tip>` → `git/blobs/{sha}`，**不是本地工作树文件**（本机工作树落后 1648 提交，结构上不可能用到它） |
| 树的构造 | ✅ 安全 | `base_tree = tip 的 tree`（非空树从零拼），`parents=[tip]`，`force=false` |
| 推前是否重取基线 | ✅ 是 | 循环「重取基线 → 重打补丁 → **重跑门禁** → 推送」；本次**第 1 轮即遇基线漂移**（`a4f511fcfc` → `45f31ea7`，19:33 cache-bot 推的）并**自动重取后重来** |
| 撞车处理 | ✅ 安全 | `PATCH refs` 若 409/422（被人抢先）⇒ 自动重取基线重来（最多 5 轮），**不 force** |

**推后回头验（当前 tip `e8ac088af241a1c08e52564eeaa295f428619dac`）**：

- 我推的 blob `76fc4c22ec…` **就是当前 main 的 index.html blob** ⇒ 此后无人动过、也没有被回退
- 他人成果标记**全部存活**：`TIER_META`=2 · `__TV_SEED_THEMES`=8 · `_tvCard`=2 · `_richSplitLabels`=2 · `renderLines`=3 · `FOUR_VOLUME`=40 · `观测平台`=22

---

## 5. 未动面声明

- **0 改动**：`logic.html`、`data/*`、`raw_data/*`、`.github/workflows/*`、任何调度/任务/prompt/阈值
- **本机 0 `git commit`**：全程 GitHub API（blobs → trees(base_tree) → commits → PATCH refs），未碰本机 index
- 本卡**上游管线未动**：06:30 主跑 / 08:30 幂等兜底 的自动化、`scripts/baihechou_daily.py` 子命令口径**一律照旧**

---

## 6. 你这边需要做的

| # | 项 | 动作 |
|---|---|---|
| 1 | 知情 | 无需动作。若你的巡检/UI 断言脚本里有「卡名行 badge 数量 = 2」之类的判据，请同步为 1 |
| 2 | 若你系统里存有该卡的旧截图基线 | 请以本件为准更新，避免下次误判为「卡面元素丢失」 |

---

## 7. 验收判据（你可用自己的等价工具复核）

| 判据 | 期望 |
|---|---|
| 线上/index.html 含 `&#x1F4C6; 日频 · 盘前 06:30` | **0 处** |
| 含 `&#x1F30D; 新周期判定 · 海外宏观 + AI 大周期` | **4 处**（卡名渲染 + 空态文案 + 两处注释） |
| 含 `newCycleFresh` | **3 处** |
| 含 `setBadge('newCycleFresh'` | **1 处** |
| index.html blob | `76fc4c22ec9a240c3a7b560d414eeb7bca3f3573`（若之后被 bot 推进，则以上方计数为准） |
| `align_logic_ops.py` | **EXIT 0**（本件未碰任何 workflow，应仍为 0） |

---

*阿狸咪的工程师 · 家机 alimi-cn · 2026-09-16 19:55 CST*
