# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-25 22:20｜部署链硬阻根治_构建步补未定义变量_CI8280全绿

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

`main` 部署链在 **21:34–22:18 CST 被硬阻断约 44 分钟**（连续 5 轮 build 失败），
真因是 `update_v8.py` 第 776 行引用了**从未定义的变量 `obj_had_ts`**（`NameError`）。
已就地补定义并推送 `316d4c878b`；CI **#8280**（head = 该 sha）**gate + build 双绿、14/14 步全通过、
已部署 main、发布后体检通过**。本项**已闭环，无需派发/复跑**。

- **发件**：阿狸咪的工程师（家机 `alimi-cn`，2026-09-25 22:20 CST）
- **性质**：跨机需知的结构性变更（影响双方共用的部署链），**不需要拍板**
- **状态**：以 `docs/ops/HANDOFF.yaml` item 状态字段为准

---

## 正文

### 一、故障证据链（全部逐字，可复核）

**1.1 现象：run 结论 ≠ 病名**

| run | head | gate job | build job | 失败步 |
|---|---|---|---|---|
| #8274 | `426c4352a6` | success | **success** | — |
| #8275 | `69166eab4d` | success | failure | **step9 🏗️ 构建 data/*.js（selective）** |
| #8276 | `455794d129` | success | failure | 同上 |
| #8277 | `cd56033bde` | success | failure | 同上 |
| #8278 | `8e145b39b4` | success | failure | 同上 |
| #8279 | `4c4743ae24` | success | failure | 同上 |

> 🔴 `#8274 → #8275` 的分界提交 = `69166eab4d`（「卡面假刷新根治」）。
> **门禁 job 始终 success ⇒ 不是 `[13/13]` 阻断**；沿历史判据「**失败步不同 = 病不同**」。

**1.2 失败栈帧**（run #8279 · build job `108104741908`，逐字）

```
Traceback (most recent call last):
  File ".../update_v8.py", line 1704, in <module>
    sys.exit(main())
  File ".../update_v8.py", line 1694, in main
    _post_build_extras()
  File ".../update_v8.py", line 1364, in _post_build_extras
    _emit_freshness_status_js()
  File ".../update_v8.py", line 1355, in _emit_freshness_status_js
    _write_js("FRESHNESS_STATUS", obj)
  File ".../update_v8.py", line 798, in _write_js
    lite_obj["update_time"] = _pick_ts(existing, lite_obj,
  File ".../update_v8.py", line 776, in _pick_ts
    if not existing and not obj_had_ts and _existing_file_ts:
NameError: name 'obj_had_ts' is not defined
##[error]Process completed with exit code 1.
```

**1.3 为什么上一轮「自证」没抓到**
`69166eab4d` 的 commit message 自证写的是「**py_compile 双绿**」——
`py_compile` 只做语法编译、**不解析名字**，`NameError` 属运行期错误 ⇒ 静默漏检。
本轮改用**零依赖 AST 未定义名扫描**，一次命中。

### 二、修法（最小改动，语义不变）

`update_v8.py` `_pick_ts` 内、原 776 行**之前**补 5 行（+240 字节，其余零改动）：

```python
        # obj_had_ts = 本次载荷自身是否已带时间戳（缺失定义导致 NameError 已补）
        obj_had_ts = bool(
            isinstance(obj, dict)
            and (obj.get("update_time") or obj.get("calc_time"))
        )
        if not existing and not obj_had_ts and _existing_file_ts:
            return _existing_file_ts
```

- **优先级链完全保持**：`existing` > `_meta.last_update` > 文件历史 `update_time` > `mtime` > `now`。
- 到达 776 行时 `existing` 必为假，且两个调用点的 `existing` 均由 obj 的
  `update_time/calc_time` 推导 ⇒ 本补法与原注释语义**等价**，不改任何既有分支。
- 推送方式：**Git Data API 六步**（远端 contents API 取基底 → 新建 blob → `base_tree` 只替换该
  1 个路径 → commit(parent=真 tip) → `force:false` → 回读逐字节）。漂移断言：推前校验远端
  `update_v8.py` blob == 基底 `10fdc3d1cba7`，不等则 `sys.exit` 阻断。

### 三、离线自证（推前跑完，四项）

| 项 | 结果 |
|---|---|
| 逐行 diff | 相对远端真身仅 `+5 行`，白名单外零删除 |
| `py_compile` | 通过 |
| **AST 未定义名扫描** | 修复后 0 命中（仅 `__file__` 属模块级 dunder 的扫描器误报） |
| `_pick_ts` 新旧对照单测 | **旧版 5/7 用例抛 `NameError`；新版 7/7 通过** |

单测参数：`now_ts=mtime_ts=2026-09-25 22:00:00`、`文件历史 ts=2026-09-22 18:15:13`；
覆盖 ①非空 existing ②`_meta.last_update` 优先 ③文件历史 ts 兜底 ④回落 mtime ⑤列表分支
⑥全空回落 now ⑦obj 自带 ts 不崩。

### 四、线上终验（run #8280，head = `316d4c878b`）

```
JOB gate  success
JOB build success   #9  🏗️ 构建 data/*.js（selective）      success   ← 原失败步
                    #10 🛡️ Pre-deploy audit                success
                    #11 🛡️ 双盲区静态校验                    success
                    #12 🛡️ 关键 data/*.js 非空              success
                    #13 📤 部署到 main（GitHub Pages 源）    success
                    #14 🩺 发布后体检                        success
JOB intraday_patrol skipped（09-25 中秋休市，正确）
```

逐字摘录：

```
⏭️  FRESHNESS_STATUS 数据未变，跳过重写（幂等）      ← 假刷新修复亦在生效（无 traceback）
✅ 已部署 main（第 1 次尝试，GitHub Pages 自动重建）
[14:18:52] ✅ 体检通过：线上 index.html 完整（1361458 字节，BUILD=316d4c878b，标记 38 全命中）
```

主站复核：`HTTP 200 / 1,361,458 B`；线上 `index.html` 与远端 blob **逐字节一致**
（sha256 `3535d0041c8d418a`）。

### 五、影响面与遗留

- **影响**：21:20–22:18 CST 之间 `main` 无法完成部署（5 轮空跑）；**无数据损坏**——
  进程在写盘前即退出，`data/*.js` 保持旧版。
- **主站未白屏**：`index.html` 全程 200，仅「最新数据未能按时上线」。
- **回归风险**：该变量在 `_pick_ts` 内为局部定义、作用域不外泄；两个调用点行为不变。
- ⚠️ **同类风险点（对方须知）**：本仓 `py_compile` 类自证**无法**覆盖 `NameError`。
  建议推送前统一跑 AST 未定义名扫描（实现要点：函数级收集 Store/参数/import/global 绑定，
  报告未绑定的 `Load`；`__file__` 等模块级 dunder 需白名单）。

### 六、不需要你做的事

1. `HANDOFF.yaml` 的 `card-fake-freshness-timestamp-rootfix-0925`（P0）先前被本故障**连带阻断**
   （构建根本不跑 ⇒ 无法验证「假刷新是否根治」），现已解除阻断；其 `verify_how` 仍待云端跑一轮后
   比对 `data/*.js` 内容哈希。
2. `experiment.json` / `sector_leaders.json` 两红卡仍停在 **2026-09-22 18:13:35 / 18:15:13**，
   真因是**东财 push2 上游 502**（非本仓缺陷）。09-25~09-27 为**中秋休市**，
   下一自然恢复判据 = **09-28（周一）post_close**，其间勿报成新故障。

---

*签发：阿狸咪的工程师 · 2026-09-25 22:20 CST · 全部结论附可复核命令/栈帧/sha，未用「状态延续」推断。*
