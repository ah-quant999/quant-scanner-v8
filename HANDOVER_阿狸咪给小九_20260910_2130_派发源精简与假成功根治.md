# HANDOVER｜阿狸咪 → 小九｜2026-09-10 21:30｜派发源精简 + 假成功根治

**📄 完整版（含未处置清单 / 待你回执 3 问 / 交付通道两个坑 §8）→ `docs/ops/urgent/URGENT_阿狸咪给小九_20260910_2130_派发源精简与假成功根治.md`**

**结论（30 秒版）**
1. 盘中风暴踩踏根因 = **同一并发组被 4 个源抢派**；「假成功」根因 = **16 个软失败步 + run 仍全绿**。两者已根治，commit `6231ff76` 已上 main，构建 `run#34481651217` **success**（含 Pre-deploy audit 通过）。
2. **删** `v8_freshness_watch.yml`（与 health_patrol 重复）、`v8_slot_scheduler.yml` + `.github/scripts/v8_slot_scheduler.py`（已休眠）。
3. **改** `v8_health_patrol.yml`：心跳派发阈值 `STALE_MIN` **6 → 30**。
4. **新增** `v8_cn_fetch_cloud.yml` 末尾 step `landcheck`「数据落地实证终检」：不看步骤报错，只看远端 `update_time` 是否真推进；**0 个推进 → `::error v8-fake-success` + exit 1（run 变红）**。
5. **本机自动化** `27b5306c`（数据新鲜度自动值守）→ **PAUSED**（与已删 freshness_watch 完全重复；按铁律只暂停不删）。

**🔴 新铁律：派发源只留「一条主线 + 一个兜底」，不得再新增派发者**
- 盘中唯一主线 = `v8_cn_fetch_intraday_lemoncat.yml`（14 档 + 三重守卫）
- 唯一状态兜底 = `v8_cn_fetch_watchdog.yml`（陈旧 >45min 才动手）
- 主体定时 = `v8_cn_fetch_cloud.yml` 自身 3 条 cron
- 理由（今日实证）：`v8_cn_fetch_cloud` 共 **47 run / 25 cancelled**，双 actor 同分钟互顶（13:10:04 两条、12:32 两条）。GitHub 同并发组只留 1 running + 1 pending，第 3 个派发把 pending 顶掉 → cancelled（多数**连 job 都没生成**）。
- 要补缺口 → 调上面三个的阈值/守卫，**禁止新开 workflow 当第四个派发者**。新增带 cron 的 workflow 必须**双登记**（`align_logic_ops.py: MUST_HAVE_CRON` + `index.html` 运维段），否则 Pre-deploy audit 失败 → **全链阻断部署**。

**⚠️ 两条必须记住的**
- `cloud run 变红 ≠ 部署失败`：`v8_build_deploy` 用 `workflow_run: types:[completed]`（不筛结论）+ push 触发，实测不阻断。看到红先看是否 `v8-fake-success`。
- **runner 标签**：实测两台机**都挂 `cn`** → `[self-hosted, cn]` 会被抢单（今日准点档 14 档 **11 档落在 alimi-cn**）。**`xiaojiu` 才是小九专属标签**。另：`v8_algo_cloud.yml:516-520` 注释称"家里机 PATH 缺 bash/git"与本机日志实证（`shell: bash` 且 success）矛盾，**请重新评估，勿照抄旧注释**。

— 阿狸咪（hostname=`Cat`，本机实测时间 2026-09-10 21:30）
