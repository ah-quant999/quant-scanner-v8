# 九宝量化 v8 交接文档（小九 → 阿狸咪）

- **生成时间**：2026-08-03 18:11（小九手工重写版），**含 18:31 补充：两张截图 bug 修复 + 定时任务跟踪看板上线**
- **状态**：阿狸咪家中机长期决策已关机、不接手任何部署/救火。本文档供其开机 `git pull origin main` 后了解当日状态（务必先 pull，18:31 改动已在 origin/main）。

---

## 一、背景

- 阿狸咪 8/1~8/2 在 `quant-scanner-v8` 完成 v8 前端大整改 + 数据原生化（数十 commit），并定下「家中机保持关机、不接手」长期决策（commit `d8ea7e0`）。
- 小九今日（8/3）聚焦：修复云端抓取断链、建自动部署闭环、清退 v6、澄清逻辑详解页、持续监督、补 safety_net 兜底。

---

## 二、小九今日已完成改动（按 commit，均已 push origin/main）

### 1. 修复 cn_fetch 推送失败（早起根因）
- `5b26d06` fix(ci): api_push_raw 改 force=False + 增强错误日志
- `3f35173` fix(ci): cloud_fetch_v8 支持 `all` 兜底 + 两脚本加 UTF-8 防 GBK emoji 崩溃
- 验证：cn_fetch run `30774819457` success；raw_data commit `43fad0e`（08:39）；build `58097a9`（08:41）

### 2. 自动部署闭环（A）
- `06fb5f3` / `a390419` fix(ci): build_deploy 监听 cn_fetch `workflow_run` 自动部署，绕过 GITHUB_TOKEN push 不触发下游 workflow 的限制
- 这是 GitHub 原生机制，cn_fetch 抓完即自动 build+deploy，无需小九手动 dispatch

### 3. 本机看门狗（B）
- `a390419` ops: 新增 `v8_cloud_watchdog.py`（本机只监督不部署，检查 runner/cn_fetch/build_deploy/raw_data/site）+ 自动化 `1785718343599`（HOURLY 08–22）
- 后续扩展 `--heal`：runner offline 且本地无进程时自动拉起 `Runner.Listener.exe run`，对应 commit `5115f83`

### 4. 运维面板 + 逻辑详解页对齐
- `9574ae7` 部署 SHA / 触发方式 / 双机状态 / 自动化任务表
- `14dc3d1` 「中国节点」5 处措辞改为「self-hosted runner · GitHub 自动调度 · 非手动」，消除「小九手动主力」误导

### 5. v6 自动化清理（全部与 v8 无关）
- DELETE 9 个：1784767168275（个股元数据）、1784711432581（危机雷达兜底）、1784524253654 / 1784524243949（candidate_pool 看门狗）、1784086687244（neodata 17:31）、1784084629462（neodata token）、1783903850358（夜间周末值守）、1783303308242（打新 09:25）、1782691770053（08:00 交接）
- `1779668598510`（C 盘空间预警邮件）已不存在，无需删
- 清理后剩余自动化仅 5 个，全部为 v8 服务

### 6. 宏观 + 风险静态/动态卡片（今日事件 / 盘后数据区）
- `7d0881d` 「今日宏观解读」静态卡片（美日联合干预日元分析，报告归档仓库根）
- `34c5bf5` 「极端风险联动策略·盘后决策框架」静态卡片
- `8adf585` 将联动策略卡并入「决策中枢」卡内（后于午后调整见 §11）
- `ec69508` 「🌡️ 实时风险温度计」动态卡（USDJPY/VIX/US10Y/USDCNH 自动抓取，每 30min）

### 7. 布局修复 + AI 市场速览动态化 + AI_MARKET_BRIEF 接入
- `0c587e7`：
  - 「实时风险温度计」整卡移到「AI 市场速览」卡下方
  - 删除 AI 卡内硬编码指数行（修复上下数据矛盾 bug，B 阶段已由 gen_market_brief.py 动态生成）
  - **真接入 AI_MARKET_BRIEF 自动刷新管线**：update_v8.py 加映射 + cloud_fetch_v8.py 盘中/全量抓完调用 gen_market_brief.py（此前两处「已接入」是幻影编辑，从未落盘）

### 8. 盘中调度微调：首档 09:40 / 末档 15:10
- `c3dbc48` v8_cn_fetch.yml cron 加 `40 1`(09:40)、`10 7`(15:10)；case 同步；逻辑详解页定时任务矩阵同步改 09:40–15:10（10 档 30min）

### 9. 🛡️ safety_net 兜底调度（今日重点新增，防数据中断）
- `19a6c472` 新增 / 更新 `.github/workflows/v8_safety_net.yml`
- **目的**：当 GitHub schedule 偶发跳过、或 cn_fetch 网络失败导致数据中断时，自动触发一次兜底 dispatch，无需人工干预。
- **防崩溃设计（回答"加来加去会不会崩"）**：
  1. 只查不抓：本 workflow 只做状态检查 + dispatch，不跑任何数据脚本
  2. 盘中限时：仅工作日 09:00–17:00 CST 运行（21 条 cron，每 20min），夜间/周末静默
  3. 成功即退：最近 1 小时已有 v8_cn_fetch 成功 run → 直接跳过
  4. 防并发：当前已有 v8_cn_fetch 在运行（queued/in_progress/waiting）→ 不重复触发
  5. 去重窗口：最近 20 分钟内已启动过 v8_cn_fetch → 不再 dispatch，避免连环重试雪崩
  6. 自身串行：`concurrency.group=v8-safety-net`，防止重叠检查导致重复 dispatch
- **已验证**：手动 dispatch 测试 run `30804404210` → success，`dispatch v8_cn_fetch` 步骤因「最近1小时已有成功」正确显示 skipped
- **结论**：以后若 schedule 再跳过或 cn_fetch 失败，20–40 分钟内会自动补跑一次，不会连环触发

### 10. 🐛 盘后「🌐 全球宏观」整片空白修复（debd6e0，已上线）
- **用户反馈**：盘后数据「宏观观测」卡片的全球宏观区域（离岸人民币/美元指数/VIX/黄金/白银/铜/原油）全空。
- **根因（双重）**：
  1. `raw_data/macro_data.json` 的 `global_macro` 曾被写成空 `{}`（cn runner 抓新浪接口时未落数）；
  2. 旧代码把商品写在 `global_macro.gold` **顶层**，但前端 `renderMacro()` 读的是 `gl.commodities.gold`，层级不匹配 → 即使有数也空白。
- **修复**：`cloud_fetch_v8.py` 的 `f_macro_data()` 重写——`_fetch_sina_global_macro(retries=2)`（完整 headers/超时20s/重试/日志），`_set_gm()` 安全转换，**商品统一写入 `commodities` 嵌套对象保持前端兼容**；akshare `fx_spot_quote()` 兜底（新浪失败时 USDCNH/DXY 仍有数）。
- **验证**：本地重跑已稳定返回 7 项实时（usdcnh 6.7522 / dxy 99.841 / vix 15.97 / gold 4110.763 / silver 58.276 / copper 652.16 / oil 79.507，日期均 2026-08-03），`data/MACRO_DATA.js` 已含真实值。

### 11. 🐛 资金流时间轴标题误显「今日」+ 新增「定时任务跟踪」看板（debd6e0 + f7b5a57，已上线）
- **资金流标题修复**：原 `mffFormatUpdateTime` 优先用文件 `update_time` 经 `rel()` 显示"今日"，掩盖数据实际末值（周末停在 07/31）。改为基于日线最后一条 `date` 显示"数据截至 YYYY-MM-DD"，仅最后日期==今天才显示"今日"。
- **用户追加诉求：补「相关联的定时任务跟踪」**——实现全链路：
  - `cloud_fetch_v8.py`：`run()` 每次写 `_run_status[label]={status:ok/empty/fail,msg}`；`main()` 末尾生成 `raw_data/runner_status.json`（run_time/category/hostname/modules/summary）。
  - `update_v8.py`：映射 `runner_status.json→RUNNER_STATUS`，跨时段每次构建强制带入。
  - `index.html`：head 引 `data/RUNNER_STATUS.js`；双机状态卡改名「**双机状态 / 定时任务跟踪 - 运维**」，新增 `renderRunnerTrack(rs)`——展示中国节点最后抓取时间、整体状态色块（正常/部分空/有失败）、模块计数，并高亮监控 MACRO_DATA / MARKET_FUND_FLOW_DATA / INDEX_QUOTES / ETF_INTRADAY_HEAT / SECTOR_FUND_FLOW 五个关键模块彩色点，一眼定位哪个模块空了/失败。
  - commit `78d8dea` → rebase → `f7b5a57` 已 push origin/main。
- **注意**：仓库当前 `raw_data/runner_status.json` 为示例（hostname xiaojiu-local），下一个 cn runner 周期会 `reset --hard` + 重建出真实 runner 状态覆盖它，属正常。

---

## 三、当前系统状态（18:40 核实）

- 站点：`https://ah-quant999.github.io/quant-scanner-v8/` → HTTP 200
- main SHA：`f7b5a57`（最新，含 18:31 全球宏观修复 + 定时任务跟踪看板；上一个 build `91a21ff` 18:25）
- 中国节点 runner（lemoncat-cn）：在线 · GitHub 自动调度（watchdog `--heal` 兜底离线自启）
- 剩余自动化（5 个，全 v8）：看门狗 `1785718343599`、09:00 / 15:30 / 17:00 / 周末 15:30 v8 部署
- **safety_net 已上线并验证**（workflow id `325287416`，active）
- **新增「定时任务跟踪」看板**：位于运维区「双机状态 / 定时任务跟踪」卡，数据来自 `data/RUNNER_STATUS.js`（由 `raw_data/runner_status.json` 经 update_v8.py 生成），下一个 cn runner 周期自动刷新真实状态
- 本地 `E:\workspace\quant-scanner-v8`：已同步 origin/main，工作树干净，无未提交改动（不会被 runner `reset --hard` 覆盖）

---

## 四、需阿狸咪注意 / 待办 / 风险

- **长期决策**：家中机保持关机、不接手任何部署/救火（`d8ea7e0`）。如需开机了解状态，**先 `git pull origin main`** 即可看到本交接文档与最新代码（含 18:31 全部改动）。
- **⚠️ 阿狸咪接手铁律（针对 18:31 新组件）**：
  1. **只改不部署**：阿狸咪 = 纯代码编辑（18:00 后 + 周末），**绝不跑 `deploy_v8.py`**；唯一部署者是小九。
  2. **改完立即 commit + push**：v8 的 `index.html` / `cloud_fetch_v8.py` / `update_v8.py` / `v8_cn_fetch.yml` 改后必须立刻 push。原因有二：①Edit 工具曾出现"显示成功但实际未改文件"幻影；②cn runner 每周期 `git fetch + reset --hard origin/main`，**未入库改动会被静默覆盖**。
  3. **勿破坏「定时任务跟踪」看板**（用户明确要求，勿删）：保留 `index.html` 内 `runnerTrackBody` 这个 `<div id>`、`renderRunnerTrack()` 函数、`<head>` 的 `<script src="data/RUNNER_STATUS.js">` 标签；保留 `cloud_fetch_v8.py` 的 `_run_status`/`runner_status.json` 生成逻辑与 `update_v8.py` 的 `RUNNER_STATUS` 映射——它们是运维排障入口。
  4. **勿破坏全球宏观数据结构**：若改 `f_macro_data()`，商品必须写入 `global_macro.commodities.*`（嵌套对象），前端 `renderMacro()` 只读 `gl.commodities.*`；改顶层字段会导致宏观卡再次空白。
- **已知待办（非紧急，源自她周末 HANDOVER）**：ETF 商品分类仍 v6 兜底、北向港交所 2024-05 停更（设计内）、`lhb_data.json` 仅 7/31 单日（待 18:30 中国节点原生化）。
- **风险缓解**：GitHub Actions schedule 偶发会跳过某个整点（今日 10:30/11:30/15:00 均出现过）→ 已有 safety_net 在 20–40 分钟内自动兜底，不会长期断数据。
- **残留问题**：`SH_SZ_HISTORY`（成交额历史折线）仍用 push2his.eastmoney.com，被 WAF 挡，折线可能为空/旧；当前成交额数字正常。

---

## 五、小九监督职责（不变）

- 看门狗每小时查 runner / cn_fetch / build_deploy / raw_data / site；异常告警，不自动覆盖数据。
- 不手动跑数据、不手动部署（除非用户明确要求）。
- safety_net 已自动兜底 cn_fetch 失败场景，看门狗重点监控 runner 在线与站点 HTTP 200。

---

## 六、今日关键时间线（便于阿狸咪回看）

| 时间(CST) | 事件 |
|---|---|
| 08:35 | cn_fetch 断链修复验证通过，自动部署闭环生效 |
| 09:42 | 看门狗首次抓到 runner 离线，已 `--heal` 拉起 |
| 10:30–15:10 | 盘中调度微调后 10 档 30min（09:40 首刷） |
| 13:30 | AI 速览指数矛盾 bug 修复上线 |
| 15:10 | AI_MARKET_BRIEF 真正接入管线（盘中自动刷新） |
| 17:48 | 风险温度计移位 + AI 卡删除指数行上线 |
| 18:02 | safety_net 兜底调度上线并验证 |
| 18:11 | 交接文档重写，推 origin/main |
| 18:25 | cn runner 自动 build（91a21ff） |
| 18:31 | 全球宏观空白修复 + 资金流标题修正（debd6e0） |
| 18:40 | 新增「定时任务跟踪」看板（f7b5a57），交接文档补充 |
