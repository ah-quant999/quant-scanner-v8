# 交接：staging 第三轮重建（基线被云端 churn 吞掉）+ 机构研究卡排程方案 B 就绪

- 交付方：小九（本机，白天班）
- 接收方：阿狸咪（夜班/周末班）
- 本档性质：**状态说明 + 复核命令**。不含任何预测性表述；所有数值均标注采集时刻。

---

## 一、一句话结论

截至 **2026-09-21 09:47 CST** 实测：`~/.workbuddy/v8_pending/` 内 `index.html` / `logic.html` 两份 staging
**均已重建完毕并锚定在当时的权威 TIP `c54476223d`**，等待 11:35 flush 自动发布。
**「机构研究·AI 解析建议」出稿时刻 07:15 → 08:50 的改动，截至该时刻尚未在线上生效。**

---

## 二、为什么需要第三轮重建（本档最重要部分）

前两轮 staging 分别败于「陈旧 `?v=` 缓存戳」与「`logic.html` 行尾被转」。
这第三轮是**另一种更隐蔽的失效**：**改动从未提交，在等 flush 期间被云端 churn 提交丢掉基线**。

### 2.1 实测漂移链（`index.html` 的 blob 变化）

| 采集时刻 (CST) | TIP | 提交 | `index.html` blob |
|---|---|---|---|
| 09:06 | `99ee9b5c21` | （staging 建立时的基线） | `a533718bbd` |
| 09:38 | `3a39e31797` | `chore: 生成器原子提交 ?v → 防覆盖`（author `v8-cloud-bot`） | `53f3d8355b` ← **变了** |
| 09:42 | `3da2aed377d8` | `chore(stock-quote): refresh STOCK_QUOTE ...`（`github-actions[bot]`） | 未变 |
| 09:44 | `c54476223d` | `docs(handoff): ... gate-1010-marker-stale-ms-note ...`（`ah-quant999`） | 未变 |
| 09:46 | `c5aeb8b8404d` | — | 未变 |

`09:38` 那次 churn 的内容，实测**全部是** `?v=1789952802` → `?v=1789954729`（108 处），**无一行真内容改动**。

### 2.2 后果

`99ee9b5c21` 时期的 staging 若在 flush 时原样发布，会把 `?v=` 从 `1789954729` **整体回退到** `1789952802`。
（`?v=` 用于 CDN 破缓存；整批回退若与 CDN 缓存交互，会让全站外链数据文件取到旧版本。）

### 2.3 两个判读陷阱（本轮同时踩中，写下来供后续复用）

**陷阱一 · 字节差 `+0` 是假绿。**
`?v=1789952802` 与 `?v=1789954729` **同为 10 位数字**，替换 108 处后**总字节数一字不差**（均 `1340726`）。
⇒ **`len(a) == len(b)` 绝不等于 `a == b`。** 判等必须用 **md5 + `?v=` 集合 + 严格 diff 行数**。

**陷阱二 · `unified_diff` 方向不能凭正负号读。**
`unified_diff(a, b)` 中 `-` 属**左侧 a**、`+` 属**右侧 b**。
本轮我一度把 `diff(BASE, TIP)` 里的 `+ 每日 08:50` 误读为「改动已上线」，方向读反，白走一轮。
⇒ 判「某串是否已生效」的**唯一口径**是 `count()` 直接统计：`tip.count(新串) > 0 且 tip.count(旧串) == 0`。

---

## 三、第三轮重建后的实测值（采集时刻 2026-09-21 09:46:06 CST）

| 项 | `index.html` | `logic.html` |
|---|---|---|
| 线上字节 / md5 | `1340726` / `0bda8710fa76de2c33389b9f82bc64ea` | `753241` / `7ac6318efce6d2083ecfdd97135a198d` |
| 待发字节 / md5 | `1340726` / `d94a574e6eb7996fbc360a63c02d5e87` | `753241` / `20e115814e0565fc94a5e5edab8d533e` |
| **字节差** | **`+0`** | **`+0`** |
| 线上行尾 `cr/crlf/bareLF` | `0 / 0 / 18977`（纯 LF） | `8538 / 8538 / 0`（纯 CRLF） |
| 待发行尾 `cr/crlf/bareLF` | `0 / 0 / 18977`（纯 LF） | `8538 / 8538 / 0`（纯 CRLF） |
| **严格 diff 行数** | **`14`**（= 7 处 × 2 行） | **`2`**（= 1 处 × 2 行） |
| `07:15` 线上 → 待发 | `7` → **`0`** | `2` → **`1`** |
| `08:50` 线上 → 待发 | `0` → **`7`** | `0` → **`1`** |
| `BASE_TIP` | `c54476223da24176c93066fe2a3fe68ac7c1e6b2` | 同左 |

`logic.html` 保留的**唯一** `07:15`，是 2026-09-04 事故记录原文
（"……丢失 07:15 之后的约 29 个提交/173 个文件"），**有意保留**，不是漏改。

---

## 四、改动清单（两页口径一致）

### `index.html` 7 处

| # | 位置 | 改前 → 改后 | 前端可见 |
|---|---|---|---|
| 1 | 宏观观测卡·数据源注释 | 每日 07:15 → **08:50** | 注释 |
| 2 | `PENDING` 空态文案 | 每日 07:15 → **08:50** | ★ |
| 3 | 日更徽章 | 日更 · 每日 07:15 → **08:50** | ★ |
| 4 | 展示型卡说明 | 由每日 07:15 → **08:50** 的 AI 步骤 | ★ |
| 5 | 频率标注注释 | 每日 07:15 → **08:50** 出稿 / 08:30 更新正文 | 注释 |
| 6 | 限制行 | 每日 07:15 → **08:50** 出稿，正文 08:30 更新 | ★ |
| 7 | 渲染函数注释 | 每日 07:15 → **08:50** AI 阅读后撰写 | 注释 |

### `logic.html` 1 处

| # | 位置 | 改前 → 改后 |
|---|---|---|
| 1 | 逻辑详解页·AI 出稿侧说明 | 每日 07:15 → **08:50** 出稿任务已把上述版式写进 prompt |

**明确未动**：`08:30`（正文更新时刻）全部保持原样；`?v=` 集合保持 `{1789910460, 1789954729}`；`BUILD` 未动。

---

## 五、复核命令（请照此验证，不看 run 结论）

### 5.1 flush 之后（11:35 或 15:05 之后）验线上真身

```
curl -s "https://ah-quant999.github.io/quant-scanner-v8/index.html" | findstr /C:"08:50" /C:"07:15"
curl -s "https://ah-quant999.github.io/quant-scanner-v8/logic.html" | findstr /C:"每日 08:50 出稿任务"
```

期望（**以 `count()` 计**）：
- `index.html`：`07:15` = **0**、`08:50` = **7**
- `logic.html`：`07:15` = **1**（仅事故记录）、`08:50` = **1**

### 5.2 同时验「`?v=` 未回退」

```
curl -s "https://ah-quant999.github.io/quant-scanner-v8/index.html" | findstr /C:"1789952802"
```
期望：**0 命中**。若 > 0，说明发布时又带了过期基线，需立即重建。

### 5.3 取线上权威真身的正确姿势（**别用 `origin/main:<path>`**）

- 现取 sha：`git ls-remote origin refs/heads/main`
- 或走 API：`GET /repos/ah-quant999/quant-scanner-v8/contents/index.html?ref=<sha>`
  带 `Accept: application/vnd.github.raw`
- **必做断言 `len(raw) >= 阈值`**（`index.html` ≥1MB / `logic.html` ≥500KB）；
  `len == 0` 一律判「取失败」，**绝不当「线上是空的」**。

---

## 六、本次涉及的技术坑（供双方复用）

1. **Bash 工具 PATH 崩坏**：`dirname` / `cd` / `date` / `ls` 全 `command not found`（exit 127）
   ⇒ 改走 Python(subprocess) + git 绝对路径，结果落盘后用 Read 读。
2. **脚本禁写死 sha**：本轮 TIP 在 5 分钟内漂了 4 次；写死 `3a39e31797` 的脚本被运行时守卫正确拦下。
3. **跨境取大文件半程截断**：抛 `IncompleteRead(1333247 read, 7479 more expected)`
   ⇒ `read(65536)` 循环 + `Content-Length` 全长校验 + 有界重试 6 轮。
4. **`.git/shallow.lock` 可能有活锁**（age 几十秒可能是真在跑）⇒ `fetch` 报
   `Unable to create ... File exists` ⇒ **必须检查 fetch 的 rc**。

---

## 七、待办（未动项）

1. **11:35 flush 后回验**：按 §5.1 + §5.2 执行，两项都要过。
2. **方案 B 的「盘中补充版」未落地**：候选 14:00（覆盖当天盘前素材约 50.5%）/ 15:10（约 60.2%），
   接入点与最终时刻**待主人确认**，本机未动。
3. **请阿狸咪核对**：本机 `index.html` 工作树与其机上内容不同步（`?v=1789951282` / `BUILD="87ffdf188"`
   vs 线上 `?v=1789954729`）—— 本机工作树是 WIP 版，**未参与本次 staging**。
4. 登记未动项：`index.html` 的 `<script>` 开合数为 **135/130（差 5）**，属**既存现象**（改前改后同值），
   非本次引入。

---

## 八、产出物位置

- staging：`C:\Users\Administrator\.workbuddy\v8_pending\`
  - `index.html.content` / `index.html.meta.json`
  - `logic.html.content` / `logic.html.meta.json`
- 旧 staging 备份：`E:\v8data\qs8-tmp\repo_trash_20260921\`
  （`index.html.content.STALE_0945.20260921` / `logic.html.content.STALE_0945.20260921` 等）
