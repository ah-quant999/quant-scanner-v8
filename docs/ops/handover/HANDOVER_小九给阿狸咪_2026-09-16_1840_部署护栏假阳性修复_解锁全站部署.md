# 交接单 · 小九 → 阿狸咪（2026-09-16 18:40）

## 一、背景：今天做的两件相关改动

| # | 事项 | 状态 |
|---|---|---|
| 1 | AI速览「ETF资金条」品种块：代码从名字后改到**名字下方小字提亮**（主人令） | 生成器已上线 main，待部署重生生效 |
| 2 | **发现并修复 `v8_build_deploy` 护栏假阳性**——它系统性阻断了全站部署（含 #1 的生效） | 已修复并推送 main |

> 第 1 件详见前一份交接单 `HANDOVER_小九给阿狸咪_2026-09-16_1800_AI速览ETF资金条品种块改版_代码另起一行提亮小字.md`（commit `34acc76a3849`）。本单聚焦第 2 件——它是「然后可以上线」真正卡住的那道闸。

---

## 二、故障现象（首跑 FAIL）

看门狗/CI：`v8 构建部署(云端ubuntu)`（workflow id `324135263`）在步骤
`🛡️ 护栏：校验关键板块未被误删/清空` 反复 failure：

```
run #6671 failure  head=5e7ce413f
run #6672 failure  head=5e9b9ad5e
run #6673 failure  head=a9069f57e
run #6674 failure  head=9a78cd296
run #6675 cancelled head=1fda1a3e0   ← 小九的护栏修复 commit（被下一笔 cn_fetch 推送 cancel-in-progress 取消）
run #6676 in_progress head=b4ecf78cb ← 18:20 盘后 cn_fetch 推送触发，含护栏修复 + 新生成器
```

⚠️ 站点 HTTP 200、页面能开 → 看似"没坏"，但**后续 cn_fetch 抓到的新数据无法经 build_deploy 上线 → 数据将持续陈旧**。这是静默生产阻断，必须当轮修。

---

## 三、根因（一句话）

`guard_index_sections.py` 的 `MAX_INDEX_BYTES = 1_250_000` 阈值过低。

- `index.html` 当前真值 **1,262,734 字节（≈1.20 MB）**，已**稳定**跨最近 12+ 个 commit（非突增/非数据内联）。
- 这 1.20 MB **全部是内联 JS 代码**；`data/*.js` 仍经 `<script src="data/...">` **外置**（实测 100 处 `src=` 引用，无内联数据块）。
- 阈值原按 **2026-08-08 真值 ≈680KB × 2 倍余量** 标定；如今代码增长到 1.20 MB，阈值已低于真实体积 → 误判「体积超标」阻断部署。
- 该护栏本是为防 **2026 年那次 6.8MB 数据内联事故**（data/*.js 被内联进 index.html），但纯净代码增长同样触发了它 → 假阳性。

---

## 四、修复（精准放宽，未删硬要求）

只改一处阈值，结构性校验全部保留：

```python
# guard_index_sections.py  L40（原 1_250_000）
MAX_INDEX_BYTES = 2_500_000  # 2.5 MB（2026-09-16 重新标定：真值 1.20MB × 2 余量）
```

标定哲学延续原设计「真值 × 2」：1.20MB × 2 ≈ 2.4MB → 取 2.5MB。
仍远超 6.8MB 内联事故（**2.7× 余量**），并为轻量化收尾期的代码增减留足空间。

---

## 五、改后验证（三件套 + 必做负向）

在临时目录用**真实 origin/main index.html** 跑编辑后护栏，5 项全过：

| 用例 | 期望 | 结果 |
|---|---|---|
| 正向：真实 index.html（1,262,734 字节） | rc=0 通过 | ✅ 通过 |
| 负向1：sec-st 被截断为极短 | rc=1 仍拦 | ✅ 仍拦（板块校验 intact） |
| 负向2：+6.5MB 内联数据块（模拟 6.8MB 事故） | rc=1 仍拦 | ✅ 仍拦（真·内联事故可捕） |
| 负向3：恰越界 +1.34MB（→2.6MB） | rc=1 拦 | ✅ 拦 |
| 正向4：恰不越界 +1.14MB（→2.4MB） | rc=0 过 | ✅ 过 |

结论：**阈值精准、结构性硬要求未放水**。

---

## 六、推送与闭环

- 推送方式：`scripts/api_put_file.py` **单文件**推送 `guard_index_sections.py`（带当前 blob sha，409 冲突自动重试），**未整仓 commit/push**。
- 远端核验：`git show origin/main:guard_index_sections.py` → `MAX_INDEX_BYTES = 2_500_000` ✅
- 已触发 `build_deploy` #6676（head `b4ecf78cb`，含护栏修复 + 新 `gen_market_brief.py`）。`cn_fetch` 已用新生成器产出 `raw_data/ai_market_brief.json`（含 10 处 `<br>`），待 build_deploy 转成 `data/AI_MARKET_BRIEF.js` 即生效。
- 监控：小九后台轮询 build_deploy 至 success，并核验 `data/AI_MARKET_BRIEF.js` 出现 `<br>`（ETF 改动随本次构建上线）。

---

## 七、给阿狸咪的红线（勿动 / 勿回退）

1. ❌ **勿把 `MAX_INDEX_BYTES` 回退到 1.25MB** —— 会立刻再次阻断全站部署（index.html 真值已 1.20MB）。
2. ⚠️ 若将来 `index.html` 真的巨型化（>2.5MB），**先查是否 data/*.js 被内联回流**，再决定是否调阈值——勿无脑调大。真内联要治本（数据外置），不能只放宽护栏。
3. 🔧 护栏脚本这类「修复生产阻断」的单文件改，继续走 `scripts/api_put_file.py` 单文件推送（带 blob sha 冲突自检），勿整仓 commit/push。
4. 🔗 关联：ETF 改动生成器 `algorithms/gen_market_brief.py` 已在 main（blob `579d3f90a38d`），本次 data 重生后会带 `<br>`；前端 `index.html` 以 `innerHTML` 渲染，无需改前端。

---

## 八、待办

- [ ] 盯 `build_deploy` #6676（或其后被触发的一笔）结论 = success。
- [ ] 线上 `data/AI_MARKET_BRIEF.js` 出现 `<br>` → ETF「代码另起一行」正式生效，闭环。
- [ ] 类推排查：其余护栏脚本（关键板块校验、部署前 data/*.js 非空校验）是否存在同类「阈值/字面量随真值漂移」隐患（主人「一劳永逸，严禁同样 bug 二次出现」）。
