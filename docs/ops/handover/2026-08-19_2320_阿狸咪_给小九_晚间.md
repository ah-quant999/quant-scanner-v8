# HANDOVER 阿狸咪 → 小九（2026-08-19 周三 · 夜间档）

> **写于 2026-08-19 23:20 阿狸咪（家里机）**
> 主人明早（8/20）出差一周，要求「今晚全部修复完 + 交接明明白白 + 双机运营无忧」。
> 本档汇总 8/19 全部 13 项改动 + 明日待办 + 出差期间双机值守约定。
> 所有改动均已落 main（经 Contents API，家网封 github:443 故不用 git push）。

---

## 一、今晚已改并上线（13 项，均已生效）

| # | 改动 | 文件 | 状态 |
|---|------|------|------|
| 1 | **云端按铃 30min 幂等** | `scripts/alimi_bell.py` + 自动化 1787141452867 | ✅ ACTIVE |
| 2 | **路径概率预测卡**（5年K线+江恩+缠论+形态匹配） | `scripts/fetch_index_history.py` + `algorithms/market_path_probability.py` + `data/INDEX_HISTORY.js` + `data/MARKET_PATH_PROBABILITY.js` | ✅ ORDER 已补 |
| 3 | **板块推荐框架**（宏观+板块池+实时跟随） | `scripts/fetch_macro.py` + `algorithms/market_regime.py` + `algorithms/sector_recommendation.py` + `data/MARKET_REGIME.js` + `data/SECTOR_RECOMMENDATION.js` | ✅ ORDER 已补 |
| 4 | **孤儿清理**：删 MACRO.js 4 处 | update_v8 DATA_SOURCES/CATEGORY_MAP + api_push_raw _EXTRA_FILES + 云端 DELETE | ✅ |
| 5 | **四星终极卡空 bug**：renderFourVolume 合并读 60m | index.html | ✅ |
| 6 | **资金流时间轴卡恢复**（已下架→已恢复） | index.html renderDelisted | ✅ |
| 7 | **3 张新卡挂载修复**（IIFE 内必须显式挂 window） | index.html | ✅ |
| 8 | **health_patrol YAML 复活**（死 37h 根因） | `.github/workflows/v8_health_patrol.yml` + `scripts/write_self_heal_escalation.py` | ✅ |
| 9 | **算法时间守门 time_gate** | `algorithms/utils/time_gate.py` + final_recommend.py 入口 | ✅ |
| 10 | **资金验证卡字段体检 disclaimer** | index.html（fetcher 补字段 = 你明早待办） | ✅ render 侧 |
| 11 | **SfIntradayChart Y 轴 999999 修复** | index.html（isFinite 兜底 ±50 亿） | ✅ |
| 12 | **动量共识卡删重复时间戳** | index.html | ✅ |
| 13 | **逻辑详解页 8/19 汇总写入** | index.html lg-op | ✅ |

## 二、明日（8/20）小九到岗待办（按优先级）

### 🔴 P0（必须做）
1. **资金验证 fetcher 补全字段**：`SECTOR_FUND_FLOW_TREND.js` 54 条板块只有 7 条有 net_10d 字段（10/20/60 日「流出」列空真因）。找到生成该文件（trend_5d/10d/20d/60d）的算法脚本，让所有板块都有完整 net_5d/10d/20d/60d；改完跑一次 algo_cloud 重出数据。
2. **algo_cloud 网络瞬断根治**：8/18 起 15 次失败（8/19 8 次含 18:30 主档），根因 = cn runner（lemoncat-cn）经 GitHub API 同步源码时大文件（STOCK_QUOTE.js 4MB 等）`IncompleteRead` → 10 分钟超时。建议：同步源码步骤加大文件重试/分块，或只同步 algorithms/ + 核心脚本（data/*.js 不入算法机）。
3. **确认 health_patrol 复活**：09:25 cn_fetch 完成后应触发 workflow_run → patrol 自愈+巡检恢复。若仍无 run 或仍 failure → 查 .github/workflows/v8_health_patrol.yml 是否 YAML 问题（8/18 d12e5e95 引入，已修）。

### 🟠 P1（出差期间影响）
4. **历史孤儿清理**（主人最在意「别又漏这漏那」）：
   - 孤儿 data 10 个：ALGO_TRACK / ANALYST_RATINGS / BLOAT_CHECK / CANDIDATE_QUOTES / DELISTED_STOCKS / LHB_7D / PORTFOLIO / RUNNER_STATUS_HEALTH / STOCK_MOMENTUM_STATE_V2 / STOCK_RPS（有文件但前端 0 处 window.X 引用）
   - 孤儿算法 ~16 个（存在但 ORDER 无引用）：backfill_*/calc_commodity_elasticity/calc_etf_intraday/calc_potential_picks/gen_top5_track/regime_filter/optimize_stop_target 等
5. **STOCK_MOMENTUM_STATE.js 陈旧**（停 8/4，9 交易日）：待主人喂 PDF → OCR 管线刷新。

### 🟡 P2（出差期间可延后）
6. runner 开机自启（lemoncat-cn 未注册 Windows 服务，07:54 重启后掉线需人工拉起）：`v8_runner_guard.py start_runner_process()` 改直启 Runner.Listener.exe + 服务化。
7. 指数 880003 平均股价 fetcher（待接入）。
8. GITHUB_TOKEN 按铃方案已落地（脚本在，无需你动）。

## 三、出差期间双机值守约定（主人 8/20-8/26 在外）

- **小九白天**（7:45-17:45）：独家部署；盘中每 30 分 freshness_guard + 每小时看门狗 + 15:30 收盘刷新 + 17:00 选股四模块全量部署。
- **阿狸咪夜间/周末**：唯一部署者（18:00-次日 7:30 + 周末）；每晚 19:30 读你交接档。
- **云端按铃**（我装的）：每 30 分检查 cn_fetch/algo 是否漏跑，漏则 dispatch 补按——主人出差期间防「云端跳轮」的保险。
- **health_patrol 复活后**：自动巡检 + 自愈 + 告警邮件（2814546@qq.com）。
- **「改完直接上线」铁律**（8/19 主人令）：不再逐项问，改完 commit+push 走 Pages 部署。
- **红涨绿跌铁律**全站执行；**动护栏文件前先说边界**。

## 四、防覆盖红线（退仓入库）

- ⚠️ 今晚 13 项改动已全部落 main（Contents API 22:24-23:20），**本地 origin/main 引用可能落后**（家网 git fetch 443 被封）→ 你到岗先 `git fetch --all && git pull --rebase --autostash` 对齐。
- 老红线不变：`raw_data/candidate.json`、`data/HEALTH_CHECK.js`、`v8/*.py` 别碰；rebase 冲突 `runner_status.json` 取 `--theirs`、`HEALTH_CHECK.js` 取云端版。
- 新数据（INDEX_HISTORY/MARKET_PATH_PROBABILITY/MARKET_REGIME/SECTOR_RECOMMENDATION）已注册 DATA_SOURCES/CATEGORY_MAP/_EXTRA_FILES 三处，**勿删勿改映射**。
- `scripts/alimi_bell.py` + 自动化 1787141452867 是主人授权的按铃，**勿 PAUSED**。

## 五、当前系统状态（8/19 23:20 快照）

- 站点：200（916KB）；云端 main HEAD = 8/19 23:1x（lg-op 汇总）。
- health_patrol：已修 YAML，等明早 09:25 首次 workflow_run 验证复活。
- algo_cloud：18:30 主档今日失败（cn runner 网络瞬断）→ 按铃 + 本机应急兜底。
- 四星终极：60m 1 条（中石科技）已可见；日线版已补 ORDER 明晚自动跑。
- 资金验证：render 已兜底 + 体检 disclaimer；fetcher 字段补全 = 你 P0-1。

---

*阿狸咪 · 2026-08-19 23:20 · 交接给小九明早 08:00 交接检查读取*
