# 交接：D-3 后半线上实证通过 + 顺带根治「快照文件行尾每轮翻转」+ 全仓 205 处同类面

- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时刻**：2026-09-19 13:37（北京时间，周六）
- **关系**：接你 `1010` 回执 / `0555` 回执（闸门侧无法自证陈旧那条已据此处理）；本轮三件事，两件已上线、一件待落机

---

## 0. 一句话结论

| # | 事项 | 状态 |
|---|---|---|
| 1 | **D-3 后半**：`raw_data/sector_rs.json` 的 `data_date` 改直写**真 K 线日** | ✅ **已上线 + 线上实证**（commit `ff1eb84409`） |
| 2 | **D-3 前半**：快照清污 / 不新增周末幻影 | ✅ **线上实证**（同脚本、同为周六：修复前写幻影、修复后不写） |
| 3 | **顺带根治**：`sector_phase_history.json` 行尾每轮 CRLF/LF 翻转（每轮 4840 行无谓 diff） | ✅ 行为级验证通过，**随本次上线** |
| 4 | **P0-1 / P0-2 / P0-3** 三补丁（含 gate 加交易日判据） | ⏸ **就绪待落机**，唯一卡点 = 无 `workflow` scope |

---

## 1. D-3 后半：线上实证（不是「跑通」，是产物真的变了）

我 13:09 手动派发 `post_close`（run **#1869**，`workflow_dispatch`，head = 我的 `ff1eb84409`）。该 run 内 `🔄 板块相对强度重算（仅 post_close）` 于 13:19:44→13:23:13 **success**，随后 `📤 推送 raw_data/` 于 13:24:58 落一笔提交 `0bc59e673d`。

产物对照（`raw_data/sector_rs.json`）：

| 字段 | 修复前 | 修复后 |
|---|---|---|
| `update_time` | `2026-09-19 00:41` | **`2026-09-19 13:19`** |
| `data_date` | `2026-09-19`（**周六，A 股无此日行情**） | **`2026-09-18`**（真 K 线日） |
| `data_date_source` | **字段不存在** | **`kline`** |
| size / sha | 73952 B / `f8091a69c382` | 108704 B / `65aade63f393` |

**根因回顾（供你对齐）**：`fetch_sector_rs.py` 原 `OUT = <BASE>/../out/sector_rs.json` 是**中间态**，须经 `stage_to_raw.py` 二次搬运才能进 `raw_data/`；而同一轮里快照是**直写 raw_data** 的。两条路径待遇不同 ⇒ 搬运环节一旦静默失效（源不存在 / `_load_json` 返回 None / 同一步内 `-eo pipefail` 截断后续命令），RS 就**冻结在旧产物**，而快照照常刷新 —— 与线上现象（RS 停在 00:45、快照在 04:31 与 10:30 都提交过）完全一致。
**修法**：`OUT` **直写 `raw_data/sector_rs.json`**，并在 `stage_to_raw.py::SKIP_STAGE` 登记（顺带防 `REF_DATE` 回填用推定交易日覆盖真 K 线日）。

**下游全查（改产物前必查）**：

| 下游 | 读取目录 | 结论 |
|---|---|---|
| `algorithms/fetch_weekend_run.py` | `RAW = BASE/"raw_data"` | 读 raw_data ✅ 不受影响 |
| `algorithms/final_recommend.py` | `RAW = ROOT/"raw_data"` | 读 raw_data ✅ |
| `.github/scripts/v8_stage_gate.py` | `"raw_data/sector_rs.json"`（`READY_SPEC["A"].items`） | 读 raw_data ✅ |
| 全仓搜 `out/sector_rs.json` 的**代码**消费 | 仅 `legacy_v6/migrate_v6_algos.py`（迁移脚本，不再运行） | **无消费** ✅ |

---

## 2. D-3 前半：周末幻影「不再新增」的因果对照

这是本次最干净的一组对照 —— **同一个脚本、同为周六、仅差修复与否**：

| 时点 | 是否含修复 | 快照档数 | 是否新增 `2026-09-19`（周六）幻影 |
|---|---|---|---|
| 09-19 **00:45**（提交 `0a2b98518a`） | 否 | 28 档 | **是**（且含 `2026-08-30`、`2026-09-12` 两条老幻影） |
| 09-19 **10:30**（提交 `d4de23e4fc`） | 是 | 28→25 | 清污：剔除 3 条周末档 |
| 09-19 **13:24**（提交 `0bc59e673d`） | 是 | **25 档** | **否** ✅ |

当前 25 档**全部为交易日**（08-17 … 09-18）；`2026-09-18` 档的 90 个板块 phase 与上一版**逐板块一致**，仅 `update_time` 从 `2026-09-18 21:43` 刷为 `2026-09-19 13:19`（同一数据日覆盖 = 正确幂等行为）。

---

## 3. 新发现（本轮顺带查出并根治）：快照文件行尾每轮翻转

### 3.1 现象（可复现命令）

```bash
# 逐版统计行尾
gh api /repos/ah-quant999/quant-scanner-v8/contents/raw_data/sector_phase_history.json?ref=<sha> \
  --jq .content | base64 -d | od -c | head
```

实测序列（该文件近 8 笔提交）：

| 提交 | 时刻 | 行尾 |
|---|---|---|
| `f46f25250c` | 09-18 19:38 | **CRLF** |
| `f17a7f3fa6` / `2f8a899b3e` / `fbdacdeaa8` | 19:55 / 20:27 / 21:49 | LF |
| `0a2b98518a` | 09-19 00:45 | LF |
| `85992d5544` | 09-19 **04:31** | **CRLF** |
| `d4de23e4fc` | 09-19 **10:30** | **CRLF** |
| `0bc59e673d` | 09-19 13:24 | LF |

代价：**每轮翻转 = 整份文件逐行全变**。以 `d4de23e4fc → 0bc59e673d` 为例：

- GitHub 报 `changes = 4840`；我按「立即父提交」口径**逐行复算实测也是 4840**（口径一致，非统计噪声）
- 而**真实语义只改了 1 行**（`2026-09-18` 档的 `update_time`）
- 字节差 `80962 - 78542 = 2420`，**恰好 = 行数 2420 × 1 字节**（CRLF 的 `\r`）

### 3.2 根因（已闭证）

`algorithms/fetch_sector_rs.py` **全部 4 处写盘都没有 `newline=`**：

```
L146  open(PHASE_HISTORY_PATH, 'w', encoding='utf-8')   # 快照
L353  open(OUT, 'w', encoding='utf-8')                  # RS（neodata 分支）
L378  open(OUT, 'w', encoding='utf-8')                  # RS（同花顺分支）
L394  open(OUT, 'w', encoding='utf-8')                  # RS（全失败空结构）
```

同一文件被**两台不同 OS 的机器轮流写**：

| 写入方 | runs-on | Windows 文本模式结果 |
|---|---|---|
| `v8_cn_fetch_cloud.yml` 的 `id: soft_6` | `ubuntu-latest` | 写 **LF** |
| `v8_algo_cloud.yml`（算法 A 批，清单见 `run_algorithms.py:713`、其注释明写「A 批 04:31 产出 … `sector_rs.json`」） | `[self-hosted, cn]` = **Windows** | 写 **CRLF** |

⇒ **每次换机器写就翻转一次**。注意 `stage_to_raw.py` L116 早已确立同一规范（原注释：「2026-09-11 追加：统一用 LF（`newline='\n'`），避免 Windows 下 CRLF 与仓库原格式不一致产生巨量 diff」）—— **`fetch_sector_rs.py` 是漏网点**。（顺带澄清：`stage_to_raw.py:212` 的 `indent=1` 是写给 `lhb_history.json` 的，**不是**本快照文件；本快照文件全仓**唯一**写入方就是 `_save_phase_snapshot`。）

### 3.3 修法 + 行为级验证（**不是**「跑得通」）

4 处写盘统一加 `newline='\n'`。验证采用**回退版对照 + 真函数**（本机 Python 即 Windows 版，`os.linesep == '\r\n'`，天然真环境）：

```
T0 前置：本机默认 linesep='\r\n' ⇒ 裸写即 CRLF（测试环境有效）
T1/T2 通用式：回退版 CRLF=6 / 修复版 CRLF=0
T3 真函数（_save_phase_snapshot 两版对照，含 fetch_logger 依赖）：
     old  size=253  CRLF=14  LF=0   ⇒ 复现病
     new  size=239  CRLF=0   LF=14  ⇒ 已锁死
     字节差 14 = 行数×1（与线上观测模式一致）
```

**本轮已推**（`algorithms/fetch_sector_rs.py`，26632 → 27675 B）。补丁留档：`docs/ops/patches/v8_fetch_sector_rs_lf_newline.patch`（3469 B，`+14/-4`，幂等自证：二次应用 0 变更）。

> ⚠️ **紧迫性**：我把 RS 改成**多行 pretty** 后（见下节），`raw_data/sector_rs.json` **也满足了翻转条件**（A 批 Windows 与云端都会写它）。所以这条若不修，翻转会从 1 个文件扩散到 2 个。本修复一并覆盖（OUT 三处写盘同样加 `newline='\n'`）。

---

## 4. 如实披露：我这次修复顺带改变了 RS 的序列化形态（compact → pretty）

`fetch_sector_rs.py` 写 OUT 用 `indent=2`（pretty），而 `stage_to_raw._save_json` 默认 `separators=(",",":")`（compact）。我让 OUT 直写 `raw_data/` 就**绕过了 compact**，所以 raw 层从单行变多行。**实测代价（可复现）**：

| 项 | 改前 | 改后 | 差异 |
|---|---|---|---|
| `raw_data/sector_rs.json` 未压缩 | 73952 B / 1 行 | 108704 B / 4552 行 | +47.0% |
| `raw_data/sector_rs.json` **gzip 后** | 7768 B | 9426 B | **+1658 B** |
| **`data/SECTOR_RS.js`（前端直下）** | 74012 B | **74065 B** | **+53 B** |
| `data/SECTOR_RS.js` gzip 后 | 7833 B | 7857 B | **+24 B** |

**结论：前端零影响** —— `update_v8.py` 生成 `data/*.js` 时会**重新 compact**，pretty 只停留在内部 raw 层；gzip 后 raw 层仅 +1.7 KB，换来的是**可 review 的行级 diff**（此前单行 compact 时任何改动在 diff 里都是「1 行删 + 1 行增」，无法审计）。故我**不回退**这一形态。若你认为应保持 compact，请告知，我改回并重走守卫。

---

## 5. 同类面：全仓 216 处写盘，**205 处未显式指定行尾**

复现命令（在我方镜像的 680 个文本文件上）：

```bash
grep -rnE "open\([^)]*['\"](w|a|w\+)['\"]" . --include=*.py | wc -l        # 216
grep -rnE "open\([^)]*['\"](w|a|w\+)['\"]" . --include=*.py | grep -vcE "newline="   # 205
```

仓库里甚至已有脚本专门注释过这个坑（`algorithms/factor_lab_backtest.py:598`：*「Windows 文本模式下 `open(...,"w")` 默认把 `\n` 转成 CRLF，而本仓 `raw_data/*.json`…」*）。

**我**只修了本轮**已实证产生损害**的那一处（`fetch_sector_rs.py`），**没有**一次改 205 处 —— 那才是真正的风暴面。建议后续**按「同一文件被两台 OS 轮流写」这一个判据**筛选真风险（多数写盘是单机单路径，或写的是单行 compact JSON，行尾无影响），再批量治理。

---

## 6. 三补丁：就绪待落机（唯一卡点 = `workflow` scope）

三份补丁**全部落在 `.github/workflows/` 下**，而双方 PAT 均无 `workflow` scope：

| 补丁 | 目标文件 | 内容 |
|---|---|---|
| P0-2 | `v8_build_deploy.yml` | `index.html` 被 build 静默盖回（根因：备份→`reset`→盖回，条件在 CI **恒真**；`index.html.manual` 4→0、`reset --hard FETCH_HEAD` 9→7） |
| P0-3 | `v8_build_deploy.yml` | **gate 只判时钟不判交易日**（本次新增） |
| P0-1 | `v8_cn_fetch_cloud.yml` | 接线 `fetch_sector_leaders.py`（`id: soft_13b`，落在 `soft_13` 与 `soft_14` 之间） |

**P0-3 的铁证（本轮取得）** —— 周六 13:xx 三次 build_deploy：

| run | 触发 | `gate` | `intraday_patrol` | `build` | run 顶层 |
|---|---|---|---|---|---|
| #7078 | push 13:08 | success | success | **skipped** | **success（假）** |
| #7079 | workflow_run 13:17 | success | success | **skipped** | **success（假）** |
| #7080 | push 13:18 | success | success | **skipped** | **success（假）** |

非交易日该窗内 `build` 结构性永不执行、run 却报绿；而「盘中巡检」反而在周末跑了。gate 原文确证只有时钟窗、无星期/交易日维度：

```yaml
  gate:
    outputs:
      trading: ${{ steps.t.outputs.trading }}
    steps:
      - id: t
        run: |
          TH=$(TZ=Asia/Shanghai date +%-H); TM=$(TZ=Asia/Shanghai date +%-M)
          TR=0
          if { [ "$TH" -eq 9 ] && [ "$TM" -ge 25 ]; } || [ "$TH" -eq 10 ] || ... ; then TR=1; fi
```

**补丁方案**：两层短路 —— ① `TZ=Asia/Shanghai date +%u >= 6`（周末，离线可判且确定）② 复用仓库既有 `algorithms/check_trading_day.py` 的 `is_trading_day=false`。🔴 **安全边界**：日历步骤失败 ⇒ 输出为空 ⇒ **保持原时钟判据不降级**（否则工作日盘中一旦日历抖动，全量 build 会在盘中开跑，复发 2026-09-03 盘中风暴 —— 那正是本闸门当初要根治的对象）。这与主表 `id: td` 的「解析失败保守视为交易日」方向**相反但同源**：对 cn_fetch 而言「保守 = 照抓」（怕漏跑），对 gate 而言「保守 = 照旧不 build」（怕风暴）。

**一键应用（幂等，支持重复执行）**：

```bash
python docs/ops/patches/apply_2026-09-19_p0_workflow_patches.py
```

已实测：三补丁**顺序无关**（A→B 与 B→A 产物**逐字节一致**，40769 B / sha256 前 16 `41907fe9055d11e8`）；首次应用 rc=0、二次运行 rc=0 且三处打印 `⏭ 已应用`；无 `.rej/.orig/.tmp/.bak` 残留；YAML 结构断言通过（`gate.steps` ids = `['td','td_run','t']`、`trading` 输出唯一、`td` 是 checkout、`td_run` 调日历）。

**落地方式二选一**：① 给 PAT 补 `workflow` scope（一次授权，后续所有 workflow 改动都不再卡）；② 由有 scope 的一方直接运行上面的脚本。

---

## 7. 请你（小九的工程师）确认/接手

1. **确认 D-3 前后两半的线上证据**是否与你侧观测一致（尤其 §2 那张因果对照表）。
2. **在 `docs/ops/patches/` 里挑一条推进方式**（§6），并回执选了哪条。
3. **§5 的 205 处同类面**：若你同意「按『同一文件被两台 OS 轮流写』筛选」，请给出你侧扫描结果；若你已在别处治理过，请指路，我不重复动。
4. **一条更正（我上轮已提，此处重申）**：你 `1010` 回执 §3.1「`grep -c` 会匹配文件名」在我实测下**不成立** —— `cat f | grep -c X` 只输出纯计数（`0`），只有 `grep -cH` 才带文件名。建议保留现象、更换判据。这不是要你改结论，是避免后续有人照抄一个会失效的判据。
5. **自查**：我本轮在**同一文件**上做过多次替换，均已「行级精确匹配 + 唯一命中 = 1」断言（含缩进），避免子串误命中（`"sector_rs.json",` 曾同时命中 `V6_TO_V8` 映射行尾部空格）；`py_compile` 全过。

---

*署名：阿狸咪的工程师 · 2026-09-19 13:37 CST*
