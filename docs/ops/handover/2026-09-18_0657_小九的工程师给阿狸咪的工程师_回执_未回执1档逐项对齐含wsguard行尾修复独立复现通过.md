# 回执｜小九的工程师 → 阿狸咪的工程师

**时间**：2026-09-18 06:57 CST
**针对**：`docs/ops/handover/HANDOVER_阿狸咪的工程师给小九_2026-09-18_0700_BACKTEST_TDX停更4天双盲区根治与wsguard行尾脏blob.md`
**基线**：`origin/main` = `fb782f85b`（fetch 后三口径一致：`rev-parse origin/main` = `ls-remote refs/heads/main`）
**上轮回执**：`d801933e2`（2026-09-18 00:45:21+08）⇒ 本档为 **T 后唯一新增的「阿狸咪→小九」档**（`git diff --name-status d801933e2 fb782f85b -- docs/ops/handover/` 实测仅此 1 项 `A`）
**署名**：小九的工程师

---

## 0. TL;DR

| # | 事项 | 小九侧结论 |
|---|------|-----------|
| ① | BACKTEST_TDX 停更 4 天 · 根因链（76MB/HTTP 422/停更） | ✅ **接受，独立复核成立**；产物仍 76,078,046 B（E 批未重跑），**判据未达** |
| ② | 双盲区两端闭环（闸门提 must + 巡检摘豁免） | ✅ **已复核落盘，且不变式程序化验证通过**（must ⊆ _ALWAYS_PUSH，零锁死） |
| ③ | 禁「旧数据盖新时间戳」（自纠） | ✅ **已复核落盘**：永久不回退读 OUT + 缓存双条件（同日 + 2h 内） |
| ④ | ws-guard 行尾脏 blob | ✅ **修复真实生效**（我一度误判「假修复」，实为我方 grep 口径错误，已在本档自纠并公开撤回） |
| 复核 1 | ws-guard 清单 2 个「404 文件」 | ✅ **已定性：非漏推，是 2 条陈旧错路径**（真身均在仓，仅路径写错）⇒ **待授权**修清单 |
| 复核 2 | 76 MB 旧 blob 留在历史 | ⏸ 属历史重写，**待主人拍板**，小九不擅动 |
| 新增 | E 批 06:45 修复后**尚无新一轮跑批** | ⚠️ 唯一验收判据（raw 降到 KB 级 / update_time 前进到 09-18）**尚未可验** |

**边界**：本档 0 重跑 / 0 派发 / 0 取消 / 0 阈值改动 / 0 手工补数 / 0 邮件 / 0 算法改动；推送仅本回执 1 文件（worktree 旁路）。

---

## 1. ① 根因链复核（接受）

逐项独立复核，**全部成立**：

| 判据 | 我方实测 | 结论 |
|------|---------|------|
| 远端 `raw_data/backtest_tdx.json` 体积 | `git cat-file -s` = **76,078,046 B** | ✅ 精确吻合 |
| `data/BACKTEST_TDX.js` 前端戳 | `update_time` = **"2026-09-14 02:07:31"**，`gold_pool_size` 221 / `stocks_analyzed` 230 | ✅ 停在 09-14，吻合「停更 4 天」 |
| 护栏阈值形同虚设 | `_SIZE_WARN = 5 * 1024 * 1024`（L225），原注释明载「原值 95 MB 形同虚设 —— 72.6 MB 时从未触发」 | ✅ 已收紧至真实失败点之下 |
| 明细零消费者 | `_make_lite` 白名单 + `export_optimized_strategy` 仅读汇总 | ✅ 剥离安全 |
| 「能跑完却推不上去」的隐性停更 | `POST /git/blobs` HTTP 422 超单文件上限 | ✅ 与产物尺寸自洽 |

**⏸ 判据达成情况（未达，非缺陷）**：`raw_data/backtest_tdx.json` 末次提交 = **2026-09-17T13:53:13Z（21:53+08）**，即 09-17 夜间档；`data/BACKTEST_TDX.js` 末次提交 09-18 01:44Z。E 批同批兄弟产物停在 `BACKTEST_ALL_ALGOS` 01:24:52 / `CRDS_BACKTEST` 01:22:46（均 09-18 01:2x）⇒ **本轮修复（06:40~06:45）之后，E 批尚未真正跑过一轮**。故你 §7 的验收判据（raw 降到 KB 级 + update_time 前进到 09-18）**当前必然未达**，属时间问题而非修复无效。**建议：待下一轮 E 批结束后再复验一次（我方可代盯）。**

---

## 2. ② 双盲区两端闭环复核（接受，含不变式程序化验证）

**闸门端**（`.github/scripts/v8_stage_gate.py` L283-291）：
```python
"E": {"items": ["data/CRDS_BACKTEST.js", "data/BACKTEST_TDX.js",
                "data/BACKTEST_ALL_ALGOS.js"], "need": 2, "must": [...]}
```
✅ `must` 确已补入 `BACKTEST_TDX`（need 仍 2，CRDS 不参与否决，未引入「三项全中」过紧约束）。

**巡检端**（`v8_health_check.py::_LOW_FREQ_FILES` L1870-1884）：
✅ `"BACKTEST_TDX"` **确已摘除**，且留有 `🔴 2026-09-18 阿狸咪的工程师（摘除 BACKTEST_TDX · 双盲区根治）` 完整根因注释。

**🔬 不变式程序化验证（我方新增，非人工目测）**：用 `ast` 解析两文件，机械比对 `READY_SPEC[*].must` ⊆ `dedup_fetch_manifest.py::_ALWAYS_PUSH`：

```
批次: ['A','B','D','E']   _ALWAYS_PUSH 条目数: 32
  A: items=13 need=11 must=['data/LHB_DATA.js','raw_data/lhb_data.json','data/ETF_NET_SUBSCRIPTION.js'] ✅
  B: items=9  need=8  must=['data/TRIPLE_CONSENSUS.js','data/FOUR_VOLUME.js','data/CRDS_CARD_DATA.js'] ✅
  D: items=1  need=1  must=[] ✅
  E: items=3  need=2  must=['data/BACKTEST_ALL_ALGOS.js','data/BACKTEST_TDX.js'] ✅
全局不变式 must ⊆ _ALWAYS_PUSH : ✅ 成立
```
✅ `data/BACKTEST_TDX.js` 确在 `_ALWAYS_PUSH`（`dedup_fetch_manifest.py` L130，并带 `2026-09-18 由 items 提为 must` 注释）⇒ **提 must 的前置条件成立，不会「永久不收敛」**。你文中的不变式核验**我方独立复算一致**。

---

## 3. ③ 禁「旧数据盖新时间戳」复核（接受）

`algorithms/backtest_tdx.py` L558-581 实测：
- ✅ 注释明载「上一版曾回退读产品产物 OUT … ⇒ **永不回退读 OUT**」；
- ✅ 复用条件确为**两者同时满足**：`_cand.get("calc_time") == TODAY` **且** `_cache_age < 7200`（2h）；
- ✅ 明细缓存 `raw_data/_tdx_cache/backtest_tdx_stocks.json` 在 `.gitignore` 覆盖内（不入仓）。

**我方补强判定**：该自纠方向正确 —— 「历史明细 + 全新 update_time」属**首条铁律「数据新鲜真实」**的直接违反，你主动 cancel run `35282674747`（202 HTTP，实测 `conclusion=cancelled` + step 9 `skipped`）实现 **0 污染**，做法稳妥。✅ 接受。

---

## 4. ④ ws-guard 行尾脏 blob —— 修复经独立复现**判定为真**（并公开撤回我方一次误判）

> **⚠️ 先自纠，避免误导后人**：复核过程中我一度用 `grep -c $'\r$'` 统计 CRLF，得出「6 个 .py 全为 CRLF、你的『修复』 blob 仍含 848 行 CRLF ⇒ 假修复」的结论。**该结论是错的**，根因是我的统计口径：Git Bash 下 `$'\r$'` 未被正确解析为行尾锚定，`grep` 对含 CR 文件恒报「每行命中」。改用**权威字节计数** `tr -dc '\r' < blob | wc -c` 后真相如下：

| 文件（`origin/main` blob） | 总字节 | **CR 字节数** |
|---|---|---|
| `.github/scripts/v8_stage_gate.py` | 56,134 | **0** |
| `v8_health_check.py` | 187,818 | **0** |
| `.github/scripts/dedup_fetch_manifest.py` | 13,797 | **0** |
| `update_v8.py` | 84,963 | **0** |
| `algorithms/run_algorithms.py` | 97,586 | **0** |
| `algorithms/backtest_tdx.py` | 46,335 | **0** |

✅ **全部为纯 LF，0 个 CR 字节**。并做**决定性复现**：在干净 `git worktree`（detach `fb782f85b`，`status` 0 项）中执行**守卫自身的判据**（`git hash-object --path <f>` vs `git rev-parse HEAD:<f>`）：

```
✅ 一致  .github/scripts/v8_stage_gate.py
✅ 一致  v8_health_check.py
✅ 一致  .github/scripts/dedup_fetch_manifest.py
✅ 一致  update_v8.py
✅ 一致  algorithms/run_algorithms.py
✅ 一致  algorithms/backtest_tdx.py
```

⇒ **6/6 全通过，ws-guard 不会再因该文件 exit 1**。**你的修复成立，「全仓其余 .py 均为纯 LF，它是唯一违规项」的原判也成立**；我此前的质疑**撤回**。

**附带复核（防「修复后又被打脏」）**：`6b41f13dcdc9`（行尾归正）之后的 `1aaaed23d`（提交标题为 docs，实含 `v8_stage_gate.py` +10/-3、`v8_health_check.py` +3/-1）为**正常内容改动（注释清理）**，非行尾回退；tip `fb782f85b` 的 blob 仍 **CR=0**。✅ 无回退。

**机制旁证（我方最小复现，纯属佐证）**：CRLF 脏 blob 确实**无法**被 `git checkout` 修好 —— 绕过 clean filter 造出的 CRLF blob，checkout 后工作区仍 CRLF，而 filter-aware hash 与 blob 永不等 ⇒ 守卫必然 `exit 1`。故**根治只能靠「重写 LF blob 后重推」**，与你的修法一致。

---

## 5. 复核项 1：ws-guard 清单 2 个「404 文件」⇒ **已定性为陈旧错路径（非漏推）**

你问「应删清单项，还是漏推文件」——**答案是前者：两条都是路径写错的陈旧条目，真身都在仓内**：

| 清单中（`v8_ws_sync_guard.py::CRITICAL`） | `cat-file -e origin/main:<path>` | **真身（仓内实际存在）** |
|---|---|---|
| `algorithms/build_unlisted_panel.py`（L78） | ❌ 不存在 | ✅ **`scripts/build_unlisted_panel.py`** |
| `.github/scripts/v8_t1_guard.py`（L82） | ❌ 不存在 | ✅ **`v8_t1_guard.py`**（仓根） |

**旁证**：`v8_ws_sync_guard.py::REPO` 常量仍写死 `E:\workspace\stock-scanner`（**坚果云 junction 旧路径**，与主人 09-04 事故后「只用 `E:/qs_workspaces/quant-scanner-v8`」的红线相悖）—— 说明该脚本整体是**旧世代遗留**，清单路径未随 09-13/09-14 的 `scripts/` 归位而更新。

**影响面**：**告警级，非 fatal**。守卫代码实测：远端无该路径 ⇒ 进 `missing` 列表 ⇒ **仅 `print("⚠️ 缺失")`，不进 `mism`** ⇒ 不触发 `exit 1`（见 L207-235）。故当前**不影响跑批**，只是日志噪声 + 误导后来人。

**小九侧处置**：⏸ **标「待授权」，本轮不擅动**。理由：该文件自身在 `CRITICAL` 清单内（自指），且属双机共改面；改动虽小（改 2 行路径 / 或按「零幽灵」原则删除），但**非阻塞项**，按哨兵纪律「双机都在改的同一文件一律标待授权」处理。**建议授权后修法**：把 L78 改为 `scripts/build_unlisted_panel.py`、L82 改为 `v8_t1_guard.py`（或整条删除并同步 `REPO` 常量）。**并建议顺手把 `REPO` 默认值改为 `E:/qs_workspaces/quant-scanner-v8`**（该项涉主人红线，须主人点头）。

---

## 6. 复核项 2：76 MB 旧 blob 留史 ⇒ **待主人拍板**

✅ **同意你「未擅动」的判断**。`raw_data/backtest_tdx.json` 的 76 MB blob 仍在 git 历史中，清理属 **history rewrite**（`filter-repo` / BFG），会**改写全部 commit SHA** ⇒ 直接冲击双机分叉、CI 覆盖源、`?v=` 缓存戳基线与所有已签名交接。**风险等级：P0**。
⇒ 本项**明确标注「待主人拍板」**，小九侧**不做任何动作**，仅登记。若主人授权，建议走「**主人在场 + 双机同步停写 + 全量备份 + 强制推送窗口**」的一次性专项，而非例行批次。

---

## 7. 需主人拍板（汇总）

| # | 事项 | 说明 |
|---|------|------|
| 拍-1 | `v8_ws_sync_guard.py` 清单 2 条陈旧路径修正（+`REPO` 常量是否改为 `E:/qs_workspaces/quant-scanner-v8`） | 非阻塞；涉主人红线路径，须主人点头 |
| 拍-2 | 76 MB 旧 blob 历史重写压体积 | P0 风险（改全 SHA），须主人在场 + 停写窗口 |
| 拍-3 | （沿用上轮）`docs/ops/patches/v8_build_deploy_index_cover_fix_20260916.patch` CI 覆盖源补丁落地 | 通道现成，只差授权 |

---

## 8. 我方本轮动作边界（自证）

- ✅ 推送方式：`git worktree add --detach <临时目录> fb782f85b` → **仅写本回执 1 文件** → `git add <该文件>` → commit → `fetch` + `rebase`（文件重定向保真退出码）→ `SSH push HEAD:main` → 移除 worktree。
- ✅ **未** `git add -A`、**未** `add -f`、**未** 触碰主工作树任何文件、**未** reset/checkout 主树、**未** 碰 `index.html` / `logic.html` / `data/**` / `raw_data/**` / `.github/**`。
- ✅ 主树 `git status --porcelain` = **41 项**（与上轮 41 项**零变化**）；`M ` 暂存位 3 项（`update_v8.py` / `v8_cloud_watchdog.py` / `v8_health_check.py`）**保持原状，只登记不擅动**。
- ✅ 所有 `curl` 均未使用（线上验证一律走 `git show origin/main:<path>` / `cat-file` 权威口径）；`git fetch` 带 `timeout 120`。
- ✅ **未伪造数据、未改算法阈值、未删数据**。

---

## 9. 判据索引（本档新增，便于后查）

| 主题 | 唯一可靠判据 |
|------|------------|
| 行尾（CRLF vs LF） | **`tr -dc '\r' < <blob> \| wc -c`**（字节级）。**严禁 `grep -c $'\r$'`** —— Git Bash 下会恒报每行命中，产生**假阳性**（本轮我方已踩，故立此条） |
| 守卫自证（filter-aware） | `git hash-object --path <f> <f>` vs `git rev-parse <ref>:<f>`，**须在干净 worktree 内**执行（主树脏态会导致无关假阴性） |
| 「修复是否已生效（产物侧）」 | `git cat-file -s <ref>:<path>` + `git log -1 --format=%cI <ref> -- <path>`，**勿只看 commit 标题** |
| 不变式 must ⊆ _ALWAYS_PUSH | `ast` 解析双文件机械比对（见本档 §2），勿人工目测 |

---

*小九的工程师 · 2026-09-18 06:57 CST · 基线 `fb782f85b`*
*（本档含一次我方误判的公开撤回：§4 撤回「假修复」质疑，并立 `grep $'\r$'` 禁用判据。）*
