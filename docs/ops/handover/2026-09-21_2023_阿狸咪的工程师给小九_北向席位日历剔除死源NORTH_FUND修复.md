# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-21 20:23｜北向席位日历剔除死源NORTH_FUND修复

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

「北向席位日历」卡已剔除死源 NORTH_FUND（2024-05 停披露）的误用：卡名对齐 TAB「北向席位日历」、门控与徽章改挂真实龙虎榜源 LHB_HISTORY、freshness_guard 标签唯一化为「北向资金(已停披露)」。已按防覆盖（Git Data API base_tree + force:false）只推 index.html + freshness_guard.py 两文件，未误推其余 83 个产物。代码级已回读校验（6 断言全绿），待线上实时核验；另有一处残留引用待小九拍板。

---

## 正文

### 1. 改动是什么（结构化）
- **卡名对齐**：原卡名「北向资金日历」→「北向席位日历」，与 TAB 按钮一致（该卡本质是龙虎榜北向席位动向，非全市场北向资金）。
- **就绪门控去死源**：「每日洞察」卡 `_resReady` 原要求 `LHB.update_time && NF.update_time`（NF=死源），改为 `LHB.update_time && LHB_HISTORY.update_time`（真实龙虎榜双源）。避免死源停刷导致卡片永久卡在「等待就绪」。
- **徽章时间戳去死源**：卡名徽章原回退取 `NF.update_time`，改为回退取 `LHB_HISTORY.update_time`（无则空），不再显示死源陈旧时间戳。
- **标签唯一化**：`freshness_guard.py` 中 `north_fund.json` 标签 `北向资金` → `北向资金(已停披露)`，与现役源区分。
- **catch 命名对齐**：卡片渲染失败文案的「北向资金日历」→「北向席位日历」。

### 2. 根因（为什么是一劳永逸）
NORTH_FUND 自 2024-05 停披露（`stopped:true`，仅每日机械刷时间戳），本不应作为任何卡的逻辑依赖。此前该卡把死源当作「北向席位」就绪判据，存在两类隐患：① 卡片命名误导（叫「北向资金」实为龙虎榜席位）；② 门控/徽章依赖停更源，一旦时间戳链异常即永久等待。本次让该卡完全脱离 NF，根因闭合。

### 3. 部署证据
- commit `1bc1d940b8`（force:false 推至 main，基于实时 tip `9dffbc5b69`）。
- 推送后已回读 blob 逐字节校验 6 项断言全绿（卡名、徽章去 NF、门控去 NF、catch 命名、旧串消失、freshness 标签）。
- 推送已触发 `v8_build_deploy.yml` 重注入 BUILD + 重算 `?v=` 缓存戳，CDN 会刷新。

### 4. 残留点（待小九拍板）
「每日洞察」卡 `_insightSignalCount`（约 4469 行）仍引用 `NF.net_buy`（死源冻结值）+1。本次**未改**——避免未经拍板擅自扩大行为。建议后续替换为 LHB_HISTORY 的北向席位净买额，使该计数也脱离死源。请小九确认是否纳入下一轮。

### 5. 双机需知
- 本卡现已不依赖 NORTH_FUND；即便日后 NORTH_FUND 彻底下线，不影响此卡渲染。
- freshness_guard 的「北向资金(已停披露)」标签仅作监控展示，不参与卡逻辑。

