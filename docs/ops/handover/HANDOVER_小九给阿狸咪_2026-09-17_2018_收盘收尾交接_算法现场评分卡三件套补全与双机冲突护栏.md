# 交接 · 小九 → 阿狸咪：2026-09-17 20:18 收盘收尾

> **时间**：2026-09-17 20:18 CST（收盘后·写窗开放）
> **发起**：小九（单位机 lemoncat-cn）
> **收件**：阿狸咪（家机 alimi-cn）
> **主人口令**：「这两天做好的都改后三件套了吗？没做的、我没回复的，都交接给家里阿狸咪，免得双机冲突。」
> **当前线上 tip**：`4300b1750fbc738234caec7551a513ce92fb1bc7`（ls-remote 实查）

---

## 零、一句话总纲

两天（09-16 全天 + 09-17 全天）的实质改动 **已全部做过改后三件套**；
唯一缺口是 **09-17 19:55 推的「个股查询·算法现场评分卡」缺 ③ 交接文档**
—— **本档即为该卡的 ③，并一并完成全天三件套总检 + 双机冲突护栏**。
**最要紧的是 §三：小九机本地工作树已与 origin/main 严重分叉，阿狸咪切勿直接提交本地树。**

---

## 一、全天各批改后三件套状态总检

| 批 | 交接档 | ①云端对齐 | ②双页口径 | ③交接文档 | 状态 |
|---|---|---|---|---|---|
| 09-17 14:30 全天 15 笔实质改动 | `HANDOVER_小九给阿狸咪_2026-09-17_1430_全天改动改后三件套总表…` | ✅ | ✅ | ✅ | **闭环** |
| 09-17 15:30 全站口径统一待办批（⑨⑩⑪⑫⑭） | `…_1530_全站口径统一待办批落地.md` | ✅ | N/A（后端 py） | ✅ | **闭环** |
| 09-17 17:30 共振日历时间胶囊一劳永逸修复 | `…_1730_共振日历时间胶囊一劳永逸修复.md` | ✅ | ✅ | ✅ | **闭环** |
| 09-17 18:40 AI速览 ETF 资金条删行业段 + 恢复生成器 | `…_1840_AI速览ETF资金条删行业段_并恢复被截断的生成器.md` | ✅ | ✅ | ✅ | **闭环** |
| 09-17 19:55 个股查询·算法现场评分卡 | **本档** | ✅ | N/A | ✅（本档） | **本档补全闭环** |

> 结论：**两天所有改动均已满足三件套**，无遗漏。

---

## 二、算法现场评分卡三件套补全（本次收尾项）

### 2.1 背景
09-16 记忆遗留规格：个股查询页新增「算法现场评分」卡，前端实时算（零体积、不落盘）、
三层结构（结论置顶 / 双口径 / 因子依据）。09-17 主人令「今晚做完」。

### 2.2 三件套核验
| # | 项 | 结果 |
|---|---|---|
| ① | 对齐云端/双机 | commit `68feff357c7b68fed1f474cfebfea811f0d9d5c2`，父 `8a14018e6074`，diff 恰好 `M index.html`（+125/0）；ls-remote 现 tip=`4300b175…` 且 **68feff357 是其祖先（未被覆盖）**；Pages 硬抓 `renderAlgoScore`=2 / `算法现场评分`=3 / `_algoScoreBar`=4 → **已可见** ✅ |
| ② | index↔logic 口径 | logic.html 为说明页，不持 code 标识符（`renderAlgoScore`/`_algoScoreBar`=0 属设计如此，非缺陷）；本卡为个股查询页单页新功能，**无双页同源需求** → **N/A**。若需在逻辑详解页补业务说明，列为待阿狸咪决定项（非阻塞） |
| ③ | 交接文档 | **本档** |

### 2.3 实现要点（勿删/勿简化）
- 双口径：①策略命中（TOP10_DAILY / GOLD_POOL / CANDIDATE / CRDS_CARD_DATA / FOUR_VOLUME，hitN≥3→双强共振）；②算法综合分 0-100（技术面动量 + 基本面 ROE/营收增速 + 资金面概念净流入/龙虎榜机构游资净买，权重 0.4/0.3/0.3）。
- 前端实时计算，不落盘，无需云端链路；`node --check` 语法校验通过。

---

## 三、🔴 双机冲突护栏（本档核心，必读）

### 3.1 现状（20:18 实查）
`git status --porcelain` 显示 **37 个未提交项**（modified + untracked）：
- `M index.html` / `M algorithms/run_algorithms.py` / `M cloud_fetch_v8.py` / `M v8_health_check.py` / `M data/*.js` / `M raw_data/*.json` 等已改未提交；
- 大量 `??` 未跟踪（今日各交接档、临时脚本 `fix_life_titles.py` / `life_push.py` / `push_handover*.py` / `validate_life_tmp.mjs` / `v8_run_patrol.py` 等）。

**关键冲突点**：本地工作树 `index.html` 里 `renderAlgoScore = 0`
—— 即**算法现场评分卡只存在于 origin/main（API 隔离推送），不在本地工作树**。
本地 `index.html` 是 09-16/早 09-17 的旧版，缺全天多笔改动。

### 3.2 🔴 给阿狸咪的操作守则（避免冲突）
1. **以 `origin/main` 为唯一真源**。阿狸咪机 `git fetch` + `git pull origin main` 后拿到的就是完整真身（含算法现场评分卡），**不要**从小九机本地工作树取代码。
2. **禁止直接提交小九机本地工作树**。若误把本地 `index.html`（旧版、缺算法卡）commit/push，会把线上算法现场评分卡覆盖掉 ⇒ P0 回退。
3. 小九机本地 37 个未提交项 = 大量 CI 噪声（`?v=` 缓存戳 / BUILD 短 SHA）+ 已通过 API 推上线的旧拷贝 + 未跟踪临时脚本。**下次小九机本地开发前必须先 `git fetch` + `git checkout origin/main -- .` 或 `git reset --hard origin/main` 对齐基线**，再在其上叠加。
4. **未跟踪临时脚本**（fix_life_titles.py / life_push.py / push_handover*.py / validate_life_tmp.mjs / v8_run_patrol.py）是小九机本地调试残留，**无保留价值**，可删；勿推 main。
5. 写仓时间窗照旧：只盘前部署完(~08:58)→09:15 硬停、午休 11:30→13:00、15:00 后。

---

## 四、遗留 / 未做 / 用户未回复 → 移交阿狸咪清单

### 🔴 高优先（时间窗相关）
| # | 事项 | 状态 | 执行方 |
|---|---|---|---|
| 1 | **小九机本地工作树对齐 origin/main**（清 37 项陈旧 + 基线对齐） | 未做（需小九机下轮本地开发前做） | 小九机 |
| 2 | 用**线上版** index.html 复核「看板层无需同步分流」结论 | 未做 | 阿狸咪 |
| 3 | 核 `MACRO_DATA.js` 裸 NaN 消失 + `fed_decision` 出现 | 09-17 14:25 线上仍有 NaN×2 / fed_decision×0；未复验 | 阿狸咪 |

### 📋 一般待办（来自 14:30 总表遗留，15:30 批已消化 #8/#9/#10/#11/#13）
| # | 事项 | 备注 |
|---|---|---|
| 4 | `gen_backtest_all_algos.py` 重跑 | 四量污染值聚合快照停 09-16 03:32；真值源已刷新 09-17 07:18。**勿双写** |
| 5 | 「强势跟踪」并入 `BACKTEST_ALL_ALGOS` | — |
| 6 | P-2 + P-6 落盘（`v8_build_deploy.yml`） | — |
| 7 | P-4 / P-5 影响面评估（76 条桥接） | — |
| 12 | `v8_build_deploy.yml` 并发声明复核 | 实测曾 2 个同时 in_progress |
| 14 | `#1897` 的 `repository_dispatch` 派发源未查明 | — |
| 15 | 「沧海一土狗」文章 | **主人已定：先放着，等 v8 收口再说**。全仓关键字匹配 0 文件，干净 |

### ❓ 用户（主人）未回复 / 未拍板 → 交阿狸咪按判断执行或后续追问
| # | 待决项 | 背景 |
|---|---|---|
| A | `factor_walkforward` 是否持续点火 | 15:30 批已挂 E 批最末，但「发动机从未点火、16 因子全 pending」仍存；主人未明确「接主链常跑」还是「保持手动」 |
| B | `SCRIPT_ENV` (stage,script) 二元组是否全量铺开 | 15:30 批已在 run_algorithms.py 落地，原「需主人确认」项已先行实现；如主人另有口径以主人令为准 |
| C | 算法现场评分卡是否需在 logic.html 补业务说明 | ② 判定 N/A（非阻塞）；若主人后续要逻辑详解页覆盖，再补 |

---

## 五、改后三件套之②：index ↔ logic 口径（本批结论）

算法现场评分卡为个股查询页单页新功能，无双页同源需求 → **② N/A**。
其余全天批次的 ② 已在各自交接档核验（业务词交叉验证，code 标识符在 logic 页=0 非缺陷）。

---

## 六、防误删 / 特别提醒

- **看门狗分流四条边界（14:30 总表 §3.3）不要简化**，每条对应真实误判案例。
- **判文件是否真改动不能用 `git status`/`git diff`**，只用裸字节 md5 三方比对（14:30 总表 §7.1）。
- **API 隔离推送不走本地工作树**，推送后本地 `git status` 落后一天是预期，非覆盖事故（判据：`ls-remote` 对表 + 我推 commit 是否为最新 tip 祖先）。
- 本机 Bash 工具 PATH 已崩，git 操作一律走托管 Python 直调绝对路径 `git.exe`（见 §八）。

---

## 七、文件快照（本档）
- 路径：`docs/ops/handover/HANDOVER_小九给阿狸咪_2026-09-17_2018_收盘收尾交接_算法现场评分卡三件套补全与双机冲突护栏.md`
- 行尾：**LF**（CRLF 0）
- 算法现场评分卡：commit `68feff357…`，现 tip `4300b175…` 含其为祖先 ✅

---

## 八、本文档自身（推送方式）
- 隔离单文件 API 推送（`blob → tree(base_tree) → commit(parents=[tip]) → PATCH ref`，**`force:false`**）。
- 推后请以 GitHub API / `git show <sha>:docs/ops/handover/...` 复核。
- 本机 git 操作经：`C:/Users/Administrator/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe`
  （Bash 工具 PATH 崩坏，禁用 shell git）。

---

*小九 · 2026-09-17 20:18 CST · 收盘后写窗 · 双机冲突护栏已写入 §三*
