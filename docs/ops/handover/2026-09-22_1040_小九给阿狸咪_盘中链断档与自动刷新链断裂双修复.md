# 2026-09-22 · 小九 → 阿狸咪：盘中链断档 + 自动刷新链断裂 双修复

**时刻**：2026-09-22 10:40 CST ｜ **执行机**：小九（本机）｜ **范围**：诊断 + 一项待推修复

---

## 一、主人报障

> 「不对啊，怎么今天盘中更新变慢了，也没自动刷新页面了！先处理盘中更新！」

两件事：**① 盘中更新变慢** ② **页面自动刷新失效**。

---

## 二、① 盘中更新变慢 —— 已解决（非代码故障，是槽位死锁）

### 定性证据

| 时刻 | 事实 |
|---|---|
| 09:47:33 | `cn_fetch #1959` 派发，一直 **pending** |
| 09:47:19 | `🇨🇳 v8 中国数据抓取(云端·小九应急)` **#42**（run `35677066808`）`in_progress` |
| 09:47:52→10:10 | **#42 卡在 step `📤 推送 raw_data/ 到 main（Contents API）` 长达 22 分钟**，其后 7 步 pending |
| 10:03:12 | `#1960` 派发，同样 pending |
| — | 并发组 `v8-cn-fetch-cloud`（`cancel-in-progress:false` + `queue:max`）⇒ **最多 1 running**，#42 独占槽位 |
| — | 实时卡集体停在 **09:35–09:40**，掉 1 档（应 20 分钟一档） |
| — | 期间 `#1958` 被取消（cancelled） |

**根因**：#42 的推送步撞上 GitHub Contents API 竞态/限流而长时间挂起，**独占唯一并发槽** ⇒ 后续 cn_fetch 全部排队 ⇒ 数据链断档约 40 分钟。
对比 `#41`（09-21 15:13）同 step 为 `success` ⇒ **今日瞬时竞态，非代码坏**。

### 处置与结果

1. `POST /actions/runs/35677066808/cancel` → **HTTP 202** ⇒ `#42` 于 **10:12:19 completed/cancelled**，槽位释放。
2. `#1959` 于 **10:12:22 起跑**，10:24:05 **success**（耗时 37 分钟）。
3. `#1960` 于 10:24:10 转入 `in_progress`。
4. **实时卡全部恢复**：`INDEX_QUOTES` / `CONCEPT_RANKING` / `SECTOR_FUND_FLOW` / `CAPITAL_FLOW_DATA` / `ETF_PULSE` / `HEALTH_CHECK` = **10:28:38 CST**，节奏回到 20 分钟档。

**结论**：**"变慢"已解决，无需改代码**。属「单 run 长时间挂起独占并发槽」类故障，与既有 `v8 链死锁守卫`（单 run 超 90 分钟自动 cancel+重派）同族，但**今日挂起仅 22 分钟、未达 90 分钟阈值**，故守卫未介入 —— 这是**守卫阈值盲区**，建议后续评估是否把「推送步超时」纳入更细粒度判据（本轮未改，先挂账）。

---

## 三、② 页面自动刷新失效 —— 根因已锁定 + 修复已实证，**待 11:31 窗口推送**

### 根因（jsdom 受控时钟实跑实证，非推断）

`index.html` 里 `__v8SmartRefreshStarted`（智能时段硬刷新）的 `_tick`：

```js
function _tick(){
  if(_maybeReload()) return;          // ← 元凶
  var iv = _targetIntervalMs();
  setTimeout(_tick, iv === 0 ? 5*60*1000 : 60*1000);
}
function _maybeReload(){
  var iv = _targetIntervalMs();
  if(iv === 0) return false;
  if((Date.now() - _last) >= iv){
    _last = Date.now();
    try{ location.reload(); }catch(e){}
    return true;                      // ← 假设「reload 必然销毁页面」
  }
  return false;
}
```

**致命假设**：`_maybeReload()` 返回 true 后 `_tick` 直接 `return`，**不再 `setTimeout` 续排**。整套逻辑建立在「`location.reload()` 一定销毁页面」之上。

但 **Chrome 对后台标签页 / 省电模式会抑制 `reload`**（页面 kept alive），此时：
- 定时器链**已断**，
- 页面**却还活着**，
- `visibilitychange` 兜底只调 `_maybeReload()`、**不重启 `_tick`**，

⇒ **永久不再自动刷新**。

**实证（受控虚拟时钟推进 45 分钟，模拟 reload 被抑制）**：`reload` 仅被调用 **1 次 @15.5min**，之后零事件 —— **链彻底断死**。这精确对应主人感受「也没自动刷新页面了」。

### 已做的修复（三处，只续命、不改任何时段语义）

| # | 改动 | 作用 |
|---|---|---|
| ① | `_tick` 改**无条件续排**（去掉 `if(_maybeReload()) return;`） | 根除断链 |
| ② | 新增 `_reloadRequestedAt` + **90 秒自愈重试** | reload 被抑制时兜底重试 |
| ③ | `visibilitychange` 回前台时**补一次 `_tick`** | 双保险补链 |

外加更正一处**过时注释**（head 里写「旧《数据变化即刷》已移除」，实际 `__v8LiveDataProbe` 确实存在于文件后段 idx≈1061701，是刷新第二腿）。

### A/B 实证结果

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 盘中 · reload **被抑制**（主人遇到的场景） | 🔴 45min 仅 1 次，链断裂 | ✅ **4 次**（15.5/17.5/30.5/32.5min），链不断 |
| 盘中 · reload **正常生效** | ✅ 1 次 | ✅ **1 次**（无重复刷副作用） |
| 夜间 23:30（不刷窗口） | ✅ 0 次 | ✅ **0 次**（未误刷） |

校验：内联 script 28 块全部可编译 ✅ ／ 标签结构与线上真身**逐项一致** ✅ ／ 行尾纯 LF ✅ ／ 关键机制（15 分钟间隔、夜间不刷、4h 硬上限、2026 休市日表、HEALTH_CHECK 探测）**全部完好** ✅

### 交付物与排队

- **待推稿**：`E:\v8data\qs8-tmp\PENDING_PUSH_20260922_1130_index.html`
  - 1341429 字节 ／ md5 `ce209db34625b239e1cef0a639788ae5` ／ 纯 LF
  - 基底 = 线上真身 md5 `a62d02a055e2a5fd5e4ff6484f744f2c`（1339558 B）
- **已建一次性自动化**：`v8 自动刷新修复·11:31 窗口首推`（id `170d3815-cf2e-4611-b64f-9abd9d6a5b17`）→ 11:31 自动推送 + Pages 终验。

### ⚠️ 给阿狸咪的硬提示

1. **11:31 推送前必须重取线上 index.html 真身比对基底 md5**。若你在此期间改过 `index.html`（双机共写热点文件），**不得直接覆盖**，须把三处改动**以补丁形式重放到最新真身**再推。
2. 推送后务必硬抓 Pages 终验：线上需含 `_reloadRequestedAt`，且 `if(_maybeReload()) return;` 必须**消失**。
3. 本修复**只改 `index.html` 一个文件**，不涉及数据链、不涉及 workflow。**请勿同时改同一文件的同一区块**。

---

## 四、附带发现（本轮未处理，仅挂账）

1. **`data/SPREAD_INTRA.js` 线上 404**，但页面/巡检把它当有效卡 —— 疑似悬空引用，待专项核查。
2. **并发槽守卫阈值盲区**：`v8 链死锁守卫` 判据为「单 run 超 90 分钟」，今日 #42 挂起 22 分钟未达阈值故未介入 ⇒ 建议评估为「推送步」加更细粒度超时判据。
3. **`?v=` 缓存戳健康**：线上 `?v=1790043834`（10:28 档），unix 秒令牌形态正确、持续更新 —— 这条腿无问题，无需改动。
4. `BUILD` 注入值（`9d4f663f1`）落后于 main tip 属正常：`update_v8.py --only-cache-busters` 只重写 `?v`、不碰 BUILD，只有全量 build 才注入 BUILD。**不构成 bug**。

---

**一句话交接**：盘中变慢 = 槽位死锁（已解），自动刷新失效 = `_tick` 断链（根因已实证 + 修复已 A/B 通过，11:31 自动上线）。
