# 交接：小九的工程师 → 阿狸咪的工程师 · 2026-09-21 0715

## 强势突破残留产物彻底清除 + 防回推/防红灯复发双层护栏（主人截图红灯一劳永逸）

> 采集时刻：**2026-09-21 06:50–07:18（北京时间）**。
> 真值一律走 **GitHub API 远端真身**（本机工作树落后，不采信本地）。
> 署名：**小九的工程师（本机 `lemoncat-cn`）**。
> 上一件（我方）：`2026-09-21_0515_小九_12-12调用点定义守卫上线与死调用清理.md`

---

## 〇、一句话

1. ✅ **主人截图红灯已定位并根治**：`all_STRONG_BREAKOUT` fail（超过通用红线 3974 分钟）；
2. 🔴 **根因是「删了又被推回」的僵尸循环** —— 你 09-19 已按主人令删除，**次日 `8bc8275fdc` 用本机工作树把 21 个文件整批 `added` 回推复活**；
3. 🔧 **本轮三层闭合**：删 14 个僵尸产物 + `api_push_raw.py` 加 `_RETIRED_ARTIFACTS` 防回推 + `v8_health_check.py` 加白名单防复发；
4. ✅ 单测 **26 例零误伤**；健康检查夹具对照 **fail(4003min) → ok**；推送夹具对照 **入队 7 → 入队 3**；
5. 📋 **请复核 §五 两项**（其中 §5.2 涉及你机需同步）。

---

## 一、症状（主人截图 · 2026-09-20 22:48 提供）

运维面板「失败/诊断详情」红灯，两条同源：

| # | 位置 | 原文 |
|---|---|---|
| 1 | 失败/诊断详情 | 文件 `all_STRONG_BREAKOUT` 失败：最后更新 2026-09-17 18:51:32 · `STRONG_BREAKOUT.js` 更新于 **4天前** 18:51；超过通用红线 **3948 分钟**（主人铁律：交易日 24h / 非交易日 T+1 18:30） |
| 2 | 同页卡片 | 🔴 `STRONG_BREAKOUT` — 同文案 |

**线上实测复核（`data/HEALTH_CHECK.js`，取数 07:17）**：

```json
"updated": "2026-09-21 06:44:40"
"summary": {"ok":121,"warn":0,"fail":1,"limited":1,"info":1,"total":124}
```
```json
{"id":"all_STRONG_BREAKOUT", "status":"fail", "last_update":"2026-09-17 18:51:32",
 "age_min":5033.1, "message":"STRONG_BREAKOUT.js 更新于 4天前 18:51；超过通用红线 3974 分钟"}
```

⇒ **`fail` 全局仅此 1 项，即主人截图所指。** 与截图 3948 分钟的差额 = 取数时刻差（截图 22:48 / 我取数 07:17 次日）。

---

## 二、🔴 根因：不是「没删」，是「删了又被推回」

### 2.1 你 09-19 的删除是**正确且生效**的

```bash
# 07d9690eb4 (2026-09-19T12:30:00Z, ah-quant999)
"🗑️ 强势突破全站删除(胜率14.4%破红线,主人令) + 🆕 缠论买点信号账本上线(55条,累积期不评星)"
```
- `run_algorithms.py` L229：`# 🗑 2026-09-19 主人令：强势突破（strong_breakout.py）全站删除，不再调度。`
- `v8_health_check.py` L206：`# 🗑 STRONG_BREAKOUT_BACKTEST 健康检查项已随 2026-09-19 主人令删除强势突破而移除。`

### 2.2 但次日被整批推回

```bash
# 8bc8275fdc (2026-09-20T13:13:05Z, ah-quant999)  parent = 0d3482be4e
"📥 推本地本地产物 + raw_data 累积账本并集（防并发覆盖）：strong_breakout/cockpit/top5_track/allsite 等新产物；..."

files (23):
  algorithms/strong_breakout.py       added +265
  data/STRONG_BREAKOUT.js             added +2
  data/STRONG_BREAKOUT_BACKTEST.js    added +1
  raw_data/strong_breakout*.json      added ×15
  scripts/algo_backtest_compare.py    added +189   ← 同日刚摘链的脚本，也被推回
```

**机制（源码实测）** —— `api_push_raw.py` 两条收集路径**都只看「本地磁盘有没有」，不看「是否已被主人令退役」**：

| 路径 | 代码 | 为何推回 |
|---|---|---|
| ① PUSH_FILES 分支 | `MANIFEST=$(git status --porcelain raw_data/ data/ \| awk '{print $2}')` | 工作树里那份**被删文件对 git 仍是「已删除的追踪文件」**，照样出现在 porcelain 输出 ⇒ 入队 |
| ② 全量回退 | `walk_raw()` 用 `os.walk("raw_data")` | 只按算法词根前缀 + 后缀过滤，`strong_breakout*` 不在排除表 ⇒ 全量推回 |

### 2.3 复活后如何变成红灯

产物回到 main ⇒ 落入 `v8_health_check.py::check_all_data_files()` 的**全扫**（L1959 `for p in sorted(DATA_DIR.glob("*.js"))`）⇒ `STRONG_BREAKOUT` 既不在 `CARD_DEFS` 也不在 `_LOW_FREQ_FILES` ⇒ 按「通用 24h 红线」判定 ⇒ **fail**。

### 2.4 附带发现：第二颗定时炸弹

`all_STRONG_BREAKOUT_BACKTEST` 当时 **ok**（停在 2026-09-18 16:29），但同属已退役产物 ⇒ **3 天后必然转 fail**。本轮一并清除。

### 2.5 附带发现：两处**虚报注释**

| 文件 | 原文 | 实情 |
|---|---|---|
| `v8_health_check.py` L206 | 「健康检查项已随主人令移除」 | **只摘了登记，产物未删** |
| `update_v8.py` L1414 | 「STRONG_BREAKOUT_BACKTEST 已随 2026-09-19 主人令删除强势突破而移除」 | **同上** |

⇒ 本轮已在 `v8_health_check.py` 该行附「纠正虚报部分」注释。

---

## 三、本轮修法（三层闭合）

### ① 产物侧：删除 14 个僵尸产物（约 420 KB）

```
data/STRONG_BREAKOUT.js                                32075 B
data/STRONG_BREAKOUT_BACKTEST.js                        3071 B
raw_data/strong_breakout.json                          42382 B
raw_data/strong_breakout_backtest.json                  3817 B
raw_data/strong_breakout_20260903.json                 37385 B
raw_data/strong_breakout_20260904.json                 10422 B
raw_data/strong_breakout_20260907.json                 68849 B
raw_data/strong_breakout_20260908.json                  4267 B
raw_data/strong_breakout_20260910.json                  2085 B
raw_data/strong_breakout_20260911.json                 22800 B
raw_data/strong_breakout_20260914.json                 47339 B
raw_data/strong_breakout_20260915.json                 25948 B
raw_data/strong_breakout_20260916.json                 87068 B
raw_data/strong_breakout_20260917.json                 42382 B
```

### ② 推送侧：`api_push_raw.py` 新增 `_RETIRED_ARTIFACTS` + `_is_retired()`

与 `index.html` 的 `_SKIP_LOCAL_PUSH` **同一范式**，区别：`_SKIP_LOCAL_PUSH` 是「另有远端正文来源」，本表是「该产物已无生产方 ⇒ 永不该再出现」。

```python
_RETIRED_ARTIFACTS = {
    "data/STRONG_BREAKOUT.js",
    "data/STRONG_BREAKOUT_BACKTEST.js",
    "raw_data/strong_breakout.json",
    "raw_data/strong_breakout_backtest.json",
}
_RETIRED_PREFIXES = ("raw_data/strong_breakout_20",)      # 逐日留档 11 个
_RETIRED_KEEP = {"raw_data/strong_breakout_history.json"} # ⚠️ 滚动账本，必须放行
```
拦截点**两处**（PUSH_FILES 分支 + 全量 `walk_raw` 分支），与 `_SKIP_LOCAL_PUSH` 完全对称。

**⚠️ 维护约定（重要）**：仅登记**经主人令退役**且**已从生产链摘除**的产物；不确定的一律不登记 —— **宁可漏拦，不可误伤**（误登记会让真产物永久推不上去，是更严重故障）。

### ③ 巡检侧：`v8_health_check.py` 加防复发护栏

- `_LOW_FREQ_FILES` 增 `"STRONG_BREAKOUT", "STRONG_BREAKOUT_BACKTEST"` —— 仿 `CANDIDATE_BACKTEST`/`GOLD_POOL_BACKTEST` 已有范式，防「有人把旧产物放回」导致红灯复发；
- 新增 `_RETIRED_FILES` 子集 + 文案分流 —— 让巡检说真话（「**已随主人令退役**」而非「低频/手动维护文件」）。

---

## 四、验证（全部可复跑）

### 4.1 单测：`_is_retired` 26 例零误伤

| 类别 | 例数 | 结果 |
|---|---|---|
| 应拦截（14 个真实退役产物） | 14 | ✅ 全命中 |
| 应放行（11 个在跑产物 + 滚动账本） | 11 | ✅ 全放行 |
| **误判** | — | **0** ✅ |

放行样本含：`raw_data/strong_breakout_history.json`、`data/STOCK_MOMENTUM_STATE(_V2).js`、`data/FOUR_VOLUME.js`、`data/CITIC_PE_THERMO.js`、`raw_data/backtest_tdx.json`、`raw_data/cockpit_backtest.json`。

### 4.2 健康检查夹具对照

| 版本 | `all_STRONG_BREAKOUT` |
|---|---|
| 原始版（未打补丁） | **fail** — 「超过通用红线 **4003** 分钟」 ← 与线上 3974 同型 |
| 补丁版 | **ok** — 「**已随主人令退役**（产物已删；仅防『旧副本被放回』而复亮红灯）」 |

### 4.3 推送夹具对照（PUSH_FILES 分支）

| 版本 | 入队文件 | 拦截 |
|---|---|---|
| 原始版 | **7 个**（含 4 个退役产物） | ❌ 泄漏 |
| 补丁版 | **3 个** | ✅ 拦住 4 个退役产物；`strong_breakout_history.json` / `FOUR_VOLUME.js` / `index_quotes.json` **照常放行** |

### 4.4 线上真身终验（取数 07:15）

| 项 | 结果 |
|---|---|
| 14 个僵尸产物 | **14/14 已删**（API 全 404）✅ |
| `raw_data/strong_breakout_history.json` | **保留** 32157 B ✅ |
| `algorithms/strong_breakout.py` | **保留** 11255 B ✅ |
| `scripts/gen_strong_breakout.py` | **保留** 29138 B ✅ |
| `api_push_raw.py` | **72434 B** ✅ `compile OK` |
| `v8_health_check.py` | **196281 B** ✅ `compile OK` |
| 关键护栏/判据函数（防删连带） | **13/13 全在** ✅ |

文件均 `crlf=0`（纯 LF），与仓内约定一致。

---

## 五、🔴 请你复核两项

### 5.1 你机工作树里的残留副本（**核心**）

本轮护栏能保证「**不再被推回**」，但**你机工作树里那 14 个文件仍在**（护栏只是拒绝入队）。
- 影响：**无功能影响**（不会上线、不会判红）；
- 建议：可择机清理，使 `git status` 更干净；**不清理也不影响任何链路**；
- ⚠️ **切勿**把 `raw_data/strong_breakout_history.json`（滚动账本）一并清掉 —— `gen_strong_breakout.py` 仍在写它。

### 5.2 你 09-20 摘链的 `scripts/algo_backtest_compare.py` 也被回推了

`8bc8275fdc` 里它 `added +189`。请确认：
- 它当前是否仍应在 main？（我方判据：你 09-20 摘链理由是「唯一消费卡属主人 09-02 明令删除的模块」+ CDP 实测零生效路径）
- 若是**误回推**，请告知，我方可按同一 `_RETIRED_ARTIFACTS` 范式一并登记。

### 5.3 复核命令（现取，勿依赖本文任何 sha）

```bash
# ① 14 个产物应全 404
for f in data/STRONG_BREAKOUT.js data/STRONG_BREAKOUT_BACKTEST.js \
         raw_data/strong_breakout.json raw_data/strong_breakout_backtest.json; do
  curl -s -o /dev/null -w "%{http_code} $f\n" \
    -H "Authorization: token $TOK" \
    "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/$f?ref=main"
done

# ② 护栏在位
curl -s -H "Authorization: token $TOK" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/api_push_raw.py?ref=main" \
  | python -c "import sys,json,base64;print(base64.b64decode(json.load(sys.stdin)['content']).decode().count('_RETIRED_ARTIFACTS'))"

# ③ 红灯是否真灭（等下一轮 v8_health_check 跑完后看 summary.fail）
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/HEALTH_CHECK.js?nc=$(date +%s)" \
  | head -c 200
```

---

## 六、明确边界（本轮**未**做）

1. **未**改动 `run_algorithms.py`（`strong_breakout` 活条目两机同为 0，无需动）；
2. **未**删除 `algorithms/strong_breakout.py` 与 `scripts/gen_strong_breakout.py` —— 前者已摘链保留供回溯，后者**仍在跑**（产出 `STOCK_MOMENTUM_STATE(_V2)`，是另一个在用的卡）；
3. **未**触碰任何双机共写文件以外内容；**未**手工 dispatch；
4. **未**对 `algorithms/strong_breakout.py` 等本机落后文件做全量对齐（工作树整体落后问题仍挂账，待主人拍板）；
5. ⚠️ **诚实声明**：本轮修的是「**删产物 + 断复活路径 + 防复发**」；「红灯实测变绿」需等 `v8_health_check.py` 在新代码下重跑一轮 —— 本轮提交已触发 `v8_build_deploy` 链（run `35544038485`），**截至本文撰写时该 run 仍 pending**，故**不得宣称红灯已实测消除**。复核方式见 §5.3 ③。

---

## 七、一条命令自证

```bash
curl -s -H "Authorization: token $TOK" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/commits?path=api_push_raw.py&per_page=1" \
  | python -c "import sys,json;c=json.load(sys.stdin)[0];print(c['sha'][:10], c['commit']['message'].splitlines()[0])"
# 期望首行含「强势突破残留产物彻底清除 + 防回推/防红灯复发双层护栏」
```

---

**纪律**：本机 **0 次全量 `git commit`**、**0 次 `git add -A`**、**0 次手工 dispatch**、**0 次算法/阈值改动**。
推送仅经 Git Data API 净室单提交（`force: False`），并做过**基线漂移预检**（实测漂移 1 个文件 = `index.html`，属无关文件漂移 ⇒ 重基线后推送）。

—— **小九的工程师（本机 `lemoncat-cn`）** · 2026-09-21 0718 CST
