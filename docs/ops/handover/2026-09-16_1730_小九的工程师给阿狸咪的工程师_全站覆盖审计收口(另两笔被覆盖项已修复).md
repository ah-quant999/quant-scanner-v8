# 交接单 #2 — 全站覆盖审计收口：另两笔被覆盖项定位与修复

- **时间**：2026-09-16 17:10（本机时钟）
- **发起**：小九的工程师（lemoncat-cn）
- **收件**：阿狸咪的工程师（alimi-cn）
- **级别**：P1（成果被静默回退，已修复上线）
- **关联**：交接单 #1（`2026-09-16_1715_..._主题空间卡第二次被覆盖(覆盖源为我方)+修复与守卫升级.md`）

---

## 一、结论先行

主人令「这两天的改动很大，查一下是否都是最新版，别一直被覆盖回旧版」→ 已做**全站三级审计**（提交级 → 标记级 → 特征级）：

**定论：两天 28 笔实质提交中，仅 3 笔被覆盖，全部指向同一元凶 `e852c5dee3`（09-16 07:23，我方小九提交）。**

| # | 被覆盖项 | 来源 | 修复提交 | 状态 |
|---|---|---|---|---|
| 1 | 主题空间卡 A/B/C 分档 + 主题动态化 | 阿狸咪 `347eaa0bc` | `c0867021a676` | ✅ 已上线 |
| 2 | 宏观观测富文本渲染（`_richSplitLabels` + `renderLines`） | 阿狸咪 `01802ea755` | `341842ae369f` | ✅ 已上线 |
| 3 | 新周期判定口径文案 10 处 | 阿狸咪 `8c445187f1` / `2eb66a6c3e` | `341842ae369f` | ✅ 已上线 |

其余 25 项**全部存活**（含四量死锁根治、涨停热力矩阵六项、超买超卖、观测平台降级、UTC+8、两卡合并等）。

---

## 二、覆盖源认定（本轮新增证据）

`e852c5dee3`（我方 07:23「涨停热力矩阵修复」）的**父提交是 `2d112e9a9c`（`TIER_META`=2，正确）**，但**其提交内容 `TIER_META`=0** ⇒ **内容级回退**。

时间线：

```
8c445187f1  07:13  TIER=2
2d112e9a9c  07:16  TIER=2
e852c5dee3  07:23  TIER=0   1203227 → 1196771  (-6456 B)   ← 元凶
（此后所有提交，含 bot，全部停在 TIER=0）
```

**性质判据**：这不是 rebase 式覆盖（那会被拒或分叉），而是 **`parents` 填了最新 tip、内容却用了旧版快照** ⇒ Git 层完全合法、无任何冲突提示 ⇒ **静默覆盖**。

---

## 三、第二笔修复详情（`01802ea755` 宏观观测渲染）

**病灶行区间**：`function _richSplitLabels(` 起 → `function renderLines(` 之后的 6 空格 `}` 收尾。

补丁来源 = `347eaa0bc` 相对其父 `dfa11e3bf1` 的 **12 个差异块**，原样重放。

| 函数 | 修复前 | 修复后 |
|---|---|---|
| `_richSplitLabels` | 965 字符（只认 `标签：`） | **1156 字符** |
| `renderLines` | 3061 字符 | **4091 字符** |

关键新特征串（推后线上核验全中）：

- `o.span` ×1 —— 重叠判定
- `var _bold = false` ×1 —— 加粗状态
- `var mCirc = raw.match` ×1 —— 圈号 ①②③ 识别

---

## 四、第三笔修复详情（`8c445187f1` 新周期口径 10 处）

| 旧文案 | 新文案 |
|---|---|
| 每日盘前 06:30 抓原文 / 07:00 AI 解析 | **工作日**盘前 06:30 主跑、08:30 幂等兜底，由本机自动化完成 |
| 日频 · 盘前 06:30 抓取 / 07:00 解析 | 日频 · 盘前 06:30 主跑 / **失败 08:30 兜底** |
| 原文暂未抓取（每日盘前 06:30 自动任务） | …（**工作日**盘前 06:30 自动任务 · 失败 08:30 兜底） |
| `var PEND = '当期解析待出稿（每日盘前 07:00 AI 步骤写入…）'` | `'当期待出稿（工作日盘前与原文同批写入）'` |
| `AI洞察对比（新周期判定 已按主人令移除）` | `🌐 新周期判定（AI洞察对比 已于 2026-09-15 迁往「🌍 观测平台」）` |

线上核验计数：`工作日盘前 06:30 主跑`×3 / `失败 08:30 兜底`×4 / `工作日盘前与原文同批写入`×1。

---

## 五、本轮修复提交

| 提交 | 文件 | 尺寸 | 内容 |
|---|---|---|---|
| `c0867021a676` | `index.html` | 1213509 B | 主题空间卡（第 1 笔） |
| **`341842ae369f`** | `index.html` | **1215563 B** | 宏观观测渲染 + 新周期口径（第 2、3 笔） |

推后核验：**逐字节相等 = True**，sha256 双方一致（`0097c6f42e6bacb6292558ce`），6/6 特征串全中。

---

## 六、给阿狸咪的三条硬提醒

1. **推 `index.html` 前必须重读 `origin/main` tip**，并且**推送件必须基于该 tip 生成**。本轮我方 `e852c5dee3` 正是「看了新 tip、用了旧快照」⇒ 请勿重蹈。
2. **禁止用本地缓存的 `index.html` 副本直接推**。我方已把守卫固化进 `push_fix2_safe.py`：推前对比 `tip != base_tip` ⇒ **自动重跑补丁脚本重打**，绝不硬推。
3. **`?v=` 缓存戳会高频漂移**：`v8 build` 每 ~15min 提交一次，`index.html` 常见 ±103 行 / 41 块的纯版本号变更。**比对时须先剥离 `?v=`**，否则会误判成「内容不同」。

---

## 七、遗留（需阿狸咪侧确认）

- 主题卡「主题动态化」已实证真在工作（种子 7 + 动态 5 = 12 个主题），但**动态主题名会随盘中资金流变化**，属预期行为，非 bug。
- 若阿狸咪侧有 **09-16 07:00–08:00 期间基于 `e852c5dee3` 之后 tip 的本地未推改动**，请先 rebase 到 `341842ae369f`，避免再次内容级回退。
