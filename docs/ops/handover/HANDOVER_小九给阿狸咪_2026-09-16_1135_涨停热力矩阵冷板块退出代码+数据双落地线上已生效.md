# 交接：小九 → 阿狸咪 | 2026-09-16 11:35
## 涨停热力矩阵冷板块退出 —— 代码 + 数据双落地，线上已生效

---

## 一、本轮完成（主人令「马上午间休市了，先推仓上线，对齐别覆盖，也别被覆盖」）

| 阶段 | 提交 | 内容 |
|---|---|---|
| ① 代码修复 | `c94686fd` | 生成器判据改为「近 5 日合计 ≤ 2 ⇒ 退出」（两处分支） |
| ② 交接档 | `d221355b6` | 根因 + 新判据 + 判据仿真 + 验证步骤 |
| ③ **数据落地** | `1f9efa7c9` | 手动跑一班，矩阵 **15 → 13 板块**，线上已生效 |

**线上实效（硬抓 Pages `/data/LIMIT_UP_HEATMAP.js`）**：
```
update_time = 2026-09-16 11:25:13
dates 末列  = 09/16 | 列数 25
板块数      = 13
行名 = 光伏 / 其他 / 半导体 / AI算力 / 电力 / 通信设备 / 医药 /
       消费电子 / 机器人 / 新能源车 / 军工 / 白酒消费 / 地产链

券商       : ✅ 已从线上消失
固态电池    : ✅ 已从线上消失
```
生成器运行日志同步印证：
```
📊 今日(09/16): 91 只涨停
🧹 退出冷板块（近5日合计 ≤2）：固态电池(0)、券商(1)
✅ 热力矩阵: 25 日 × 13 板块
```

---

## 二、🔴 本轮最重要的发现：本机三个热文件是**旧版**，绝不能推

推送前做「对齐双向核对」时发现，本机工作树与远端**不一致且本机落后**：

| 文件 | 本地 blob | 远端 blob | 判定 |
|---|---|---|---|
| `index.html` | `586b6c6e` | `84e33cfe` | 归一化后**完全一致** ⇒ 仅 `?v=` 差异，无内容风险 |
| `logic.html` | `3e31e5f0` | `c29441ee` | ⚠️ **本地落后**（远端 4 行是更新过的文案，如文档指向 ~1512 行） |
| `guard_v8_freshness.py` | `3d895584` | `901e6944` | ⚠️ **本地是回退版** —— 远端有你 30 行新代码（「>1MB 文件 Contents API 回退 blob API」治本） |

判定手法（**归一化比对法**，即护城河工具）：
```python
# 把 ?v=\d+ 与 var BUILD 归一化后取行集合
only_remote = [x for x in remote_lines if x not in local_set and x.strip()]
only_local  = [x for x in local_lines  if x not in remote_set and x.strip()]
# 远端独有 ⇒ 覆盖会抹掉它；本地独有 ⇒ 推上去是新增
```

**结论与动作**：本批推送**严格限定单文件**，用**范围守卫**硬拦：
```python
FORBIDDEN = ["index.html", "logic.html", "guard_v8_freshness.py",
             "fetch_limit_up_heatmap_v8.py", "v8_ws_sync_guard.py"]
# 建树后对账
got = set(git diff-tree -r --name-only tip tree)
assert got and got <= {"data/LIMIT_UP_HEATMAP.js"}   # 必须非空且无计划外文件
```
推送后复核：本批实际改动 `['data/LIMIT_UP_HEATMAP.js']` ✅ —— **无任何红线文件被触碰**。

> ⚠️ **阿狸咪注意**：你那份 `guard_v8_freshness.py` 的 blob API 治本**只在你那台机/远端**，
> 小九本机仍是旧版。小九**不会推**这个文件，但**下一轮小九跑 `update_v8.py` 时会触发
> `v8_ws_sync_guard.py` 自动拉齐**（`index.html`/`logic.html` 在 CRITICAL 清单里）——
> 这是**预期行为，不是覆盖事故**。

---

## 三、数据转换链路（本轮走通的路径，供参考）

```
fetch_limit_up_heatmap_v8.py
        ↓ 产出
raw_data/limit_up_heatmap.json          （脚本自身输出）
        ↓ update_v8.py --detect-changes
data/LIMIT_UP_HEATMAP.js                （前端注入 window.LIMIT_UP_HEATMAP）
```
- 映射关系在 `update_v8.py` L86：`"limit_up_heatmap.json": "LIMIT_UP_HEATMAP"`
- `update_v8.py` 跑批前有**工作区一致性守卫**（`v8_ws_sync_guard.py --heal`），
  会把 CRITICAL 清单里的文件（含 `index.html` / `logic.html`）`git checkout <ref> -- <文件>` 拉齐到远端。
  **fail-closed**：拉不齐则中止跑批。
- ⚠️ 跑 `update_v8.py` 耗时约 **4m40s**（本次实测），**必须包时间锁**（`timeout 280`）。

---

## 四、复核清单（阿狸咪可直接复用）

```bash
REPO=E:/qs_workspaces/quant-scanner-v8

# 1) 线上数据是否已含 13 板块、无券商/固态电池
curl -s --max-time 45 -H "Cache-Control: no-cache" \
  "https://ah-quant999.github.io/quant-scanner-v8/data/LIMIT_UP_HEATMAP.js?_cb=$(date +%s)" \
  | python -c "import sys,json,re; s=sys.stdin.read(); d=json.loads(re.search(r'=\s*(\{.*\})',s,re.S).group(1)); n=[x['name'] for x in d['sectors']]; print(len(n), n); print('OK' if ('券商' not in n and '固态电池' not in n) else 'FAIL')"
# 期望：13 [...]  OK

# 2) 生成器代码是否在远端两处都改了
git -C $REPO show origin/main:fetch_limit_up_heatmap_v8.py | grep -c "HEAT_MIN = 2"
# 期望：2

# 3) 三个热文件本机是否落后（落后属正常，勿推）
for f in index.html logic.html guard_v8_freshness.py; do
  L=$(git -C $REPO hash-object $f); R=$(git -C $REPO rev-parse origin/main:$f)
  [ "$L" = "$R" ] && echo "一致 $f" || echo "本机落后 $f（勿推）"
done
```

---

## 五、遗留（待主人拍板，本轮未动）

1. **板块周期档案（多年回溯）** —— 需放宽三处 30 日截断：
   `fetch_limit_up_heatmap_v8.py` L429 / L583、`fetch_sector_rs.py` L88。**待拍板**。
2. **今日盘中调度器 0 班** 问题未根治（见 1010 交接档）。
3. 新判据阈值 `STALE_WINDOW=5` / `HEAT_MIN=2` 为实测定档；若后续板块数变化导致误杀，
   **优先拉长 `STALE_WINDOW`**（比调阈值更稳）。
4. **`fetch_limit_up_heatmap_v8.py` 有两处同构分支**，改判据必须**两处同改**（本轮已同改）。

---

**小九 / 2026-09-16 11:35**
