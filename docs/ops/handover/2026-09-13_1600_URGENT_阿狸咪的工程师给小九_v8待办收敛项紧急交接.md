# 🚨 URGENT｜阿狸咪的工程师 → 小九｜2026-09-13 16:00｜v8待办收敛项紧急交接

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

经本轮实测远端 TIP `9734b8247` 复核，旧「22 项待推清单」已过时；**3 项已由小九实装（B3 ROE、B4 剔 ST、#9 子页签顺序）**。现收敛出 **14 项待拍板项 + 6 项归小九项 + 1 个 ETF 双页文件名新坑**。请小九按下面三类处理，能做先做，做不了/定不了的回传给阿狸咪工程师裁定。

---

## 一、已按专业判定直接处理（无需小九动作，但请知晓）

| 项 | 判定 | 理由 |
|---|---|---|
| #2 `?v` 版本戳统一 | ✅ 已 plumbing 推 `88adc6a4` | logic.html 78 处 `?v=` 已统一为 `1789284978`，双页主戳对齐 |
| #6 `backtest_tdx.json` 62MB | ✅ 保留不裁剪 | 超 50MB 是「正确性代价」而非造假；裁剪会丢回测样本 |
| #18 破锁阈值 12→35 分钟 | ❌ 撤项 | 远端 `v8_cloud_watchdog.py` 无 12 分钟值（仅 15/120/1500），盲改会误报/失守 |
| #21 金股池卡恢复 | ❌ 不恢复 | 口径已并入其他卡，恢复造成冗余 |
| #15 CRDS 大盘口径 | ✅ 采纳小九方案 A | 由小九确认方案 A 代码是否已落地；若已落地则划掉 |

---

## 二、小九可直接做/优先做的项（做完不必回传，推了就行）

### 2.1 原归小九的 6 项（P0/P1）
1. **`hb_xiaojiu.json` 从未产出** — 全站仅剩 1 个真 404 根因（`data/hb_xiaojiu.js`）。
2. **`STOCK_STOP_DATA` 宇宙 259**。
3. **`STOCK_RPS` valid_count=2**。
4. **7 因子 walk-forward（0/7）**。
5. **`GOLD_POOL` candidate_total=875 vs stocks 差额**。
6. **`v8_health_check.py` 版本对齐**。

### 2.2 新增/划归小九的项

| # | 内容 | 为什么归小九 | 建议动作 |
|---|---|---|---|
| #5 | 相对强度是否纳链 | 涉及全站算法链接线，阿狸咪不碰任务面 | 拍板纳/不纳；若纳，给出接入点 |
| #12 | 主对比表「平均收益」列 | 数据口径需算法定义（加权平均？等权？持有期？） | 定义口径后阿狸咪补前端 |
| #13 | 相对强度最佳持有期口径 | 算法链输出依赖 | 确定档位-窗口映射 |
| #14 | 选股链审计修复 | 核心缺陷在 `close` 继承 T−1 / `pct_chg` 被 `safe_float(None)` 写 0；涉及选股链数据层 | 小九先修复上游数据生成；阿狸咪跟前端/回测一致性 |
| #16 | B 批 need=8/9 是否下调 | 算法链门控阈值 | 给出目标 need 值 |
| #17 | 三重共识门槛 | 同上，算法阈值 | 给出新门槛或维持 |
| #22 | `PORTFOLIO.js` 陈旧 | 生成器在小九侧 | 刷新或确认已弃用 |
| #24 | 并发 session 分工 | 双机/云端 runner 架构 | 明确阿狸咪机 vs 云端 vs 小九机的分工边界 |
| ETF 新坑 | `index.html:512` 引 `data/ETF_NET_SUBSCRIPTION.js`，`logic.html:742` 引 `data/ETF_SUBSCRIPTION_EM.js`；`fetch_etf_subscription_em.py` 名实不符 | 数据源/生成器归属小九 | 统一命名 + 同步双页引用；或确认旧文件是否废弃 |

---

## 三、做不了/定不了 → 请回传阿狸咪工程师裁定

| # | 内容 | 卡在哪里 | 需要小九回传的信息 |
|---|---|---|---|
| #4 | 强势突破修法并入 `backtest_pools.py` | 强势突破脚本疑似孤儿，是否真无回测需小九确认 | 强势突破当前由哪个脚本产出？是否有回测数据？ |
| #10 | #254「待确认剔除」提示条一键全删 | 阿狸咪没定位到 `#254` 具体指哪一处；删错会误伤 | 给出 `#254` 所在文件/行号/截图 |
| #19 | 前端刷新 B 分区 | 「B 分区」具体指哪张卡的 B 档/B 分区？主题空间/三重共识/其他？ | 给出目标卡片名和期望行为 |
| #20 | `CATEGORY_MAP` 补登 | 需小九确认 `update_v8.py` 与 `cloud_fetch_v8.py` 两边缺失了哪些模块映射 | 列出应补登的模块名 + 时段归属 |
| #23 | `fetch_etf_subscription_em.py` 名实不符 | 改名是破坏性重构（双页引用、CI、nutstore 同步） | 确认新文件名 + 是否同步替换旧 `ETF_NET_SUBSCRIPTION.js` |

---

## 四、小九推进时请同步核对

1. **远端 TIP 持续在变**：阿狸咪本地 HEAD `8240ae2cb` 已落后远端 140+ commit；小九推新代码后，阿狸咪会重新 fetch 复核。
2. **任何改动走 plumbing 推**：阿狸咪机禁 `git add -A`，小九机也建议显式路径 add；双机都须推后回验 byte 级。
3. **完成项请直接回复本交接单文件名 + commit sha**，不必单独写长文档。

---

## 五、阿狸咪工程师的专业判断（供参考）

- **#18 必须撤**，不是技术问题，是「代码里没这个值」。
- **#6 必须保留**，62MB 是正确性代价，不是脂肪。
- **#14 优先级最高** upstream，因为它影响选股链信号真实性。
- **ETF 文件名不一致** 是新坑，应并入 #23 一起处理，不要两张卡引不同文件。
- **小九若对 #15 方案 A 已落地**，直接划掉；若未落地，请优先做。

---

*署名：阿狸咪的工程师*  
*基准远端 TIP：`9734b8247`*
