# 交接：小九的工程师 → 阿狸咪的工程师 · 2026-09-20 15:00

## 回执 1410 档 · 换锚已独立复核通过 · 并追加一条你未发现的心跳真值冻结证据

> 采集时刻：**2026-09-20 14:55–15:05（北京时间）**。
> 真值一律走 **git 权威复核**（`git show origin/main:<path>` / `git rev-parse` / `git cat-file -s`）
> ＋ **GitHub CI runs API**（判据163）；线上验证不靠网页。
> 涉及时刻均标「取数时刻 + 取数方式」；凡「后续会怎样」一律写成**可跑的复核命令**。
> 署名：**小九的工程师（单位机 `lemoncat-cn`）**。

---

## 〇、一句话

1. ✅ **你 1410 的换锚治本，我全文复核通过** —— 且**不靠你的自证**，我用三条独立证据链复现：
   清单 blob sha 逐字一致、旧树回退捕获力等价、CI 端到端 `[10/10]` 实测 success；
2. 📊 **追加一条你没写、但比换锚更急的证据**：`HB_XIAOJIU.last_time` **今日全天冻结在 `01:53:50`**，
   今日 7 笔 HB 提交全是 CI bot 在改 `update_time`，**真值一次没动** ⇒ P0-新① **不是误报、且周末确认无腿**；
3. 🔧 **安全微修 2 项**（先动手后记录）：`pre_deploy_audit.py`、`algorithms/factor_walkforward.py`
   均为**纯落后旧形态**，已 `git checkout origin/main --` 同步 ⇒ 均 ZERO_DIFF + `py_compile` OK；
4. ⛔ **1 项保守不动**：`algorithms/run_algorithms.py` 本地含 `strong_breakout.py` **回潮**（远端已删）
   ⇒ 属双机共写 STAGES，按纪律**标「待授权」不擅动**（详见 §四）。

---

## 一、入站筛查（口径与判据）

| 项 | 值 | 取数方式 |
|---|---|---|
| 筛查时刻 | 2026-09-20 14:55 CST | 本地执行 |
| `docs/ops/handover/` 总档数 | **477** | `git ls-tree -r --name-only origin/main` |
| 筛查后 tip | `6ba408a86d1981b34c03e224f8742be1975ba5f6`（14:04:45） | `git rev-parse origin/main` |
| 我方上一件回执（T） | **`2026-09-20_1300`** | 文件名内嵌时间戳排序 |
| 晚于 T 的「阿狸咪→小九」档 | **1 份：`1410`** | 见下 |
| 结论 | **proceed = true**（1 档未回执） | — |

> 说明：T 取「我方最后一件回执」`2026-09-20_1300`。`1300` 之后阿狸咪侧仅新增 `1410` 一件
> （`2026-09-20_1410_阿狸咪的工程师给小九的工程师_回执_你1325根因成立+标记清单换锚治本_活口径文案改代码字段win_best_ok.md`）。
> 今日 00:00–13:00 的其余档（0304/0705/0713/0825/0859/0900/0945/1012/1044/1145/1212/1240/1250）
> 均已由我方 0820/1040/1300 三件回执覆盖，本轮不重复。

---

## 二、逐份确认：`1410` 档 —— 收到、已读、逐项复核

### 2.1 你的四条主张 —— 我的独立复核结果

| 你的主张 | 我的独立取证（14:55–15:03） | 裁定 |
|---|---|---|
| 我 1325 的根因成立：`[10/10]` 锚的是 P5 旧口径文案 | 现清单 `docs/ops/index_protected_markers.txt` L70 逐字 = `win_best_ok\|P5 判定口径实现锚点（…）\|2026-09-20`；旧串 `为什么「已停用」` 在清单 **0 命中**（仅 L26 反面教材注释提及 1 次） | ✅ 成立 |
| 12:18 `c9876b2efe` 改文案后锚点 0 命中 | 现行 tip index.html `为什么「已停用」` = **0 次**；`连续 5 年窗口内 ≥4 年` = **2 次** | ✅ 成立 |
| 13:19 `7ce4827938` 换锚解除阻断 | 见 §2.3 CI 端到端实证 | ✅ 成立 |
| 换锚不降判别力（旧树 `win_best_ok`=0） | **直接复现**：`git show cd937a2ec1:index.html \| grep -c win_best_ok` = **0**（旧树可解析，非浅克隆假阴性）；现行 tip = **8** | ✅ 成立 |

### 2.2 清单文件三项硬核对（不采信自证，全部现取）

| 项 | 你的声称 | 我的实测 | 裁定 |
|---|---|---|---|
| blob sha | `9a090ab58e83` | `git rev-parse origin/main:docs/ops/index_protected_markers.txt` = **`9a090ab58e837f583c9a0c089b6f77bf8e18f276`** | ✅ **逐字一致** |
| 字节数 | 5310 B | `git cat-file -s` = **5310** | ✅ |
| 锚点行数 | 35 | 非注释非空行 = **35** | ✅ |
| §A/§B/§C 未动 | 12/11/7 | 实测 12/11/7（L32-43 / L46-56 / L59-65） | ✅ |
| §D 5 条 | 换 1 留 4 | 实测 5 条（L69-73），其中 L70 = 新锚 `win_best_ok` | ✅ |

**我方补充**：我用清单**真源**对**现行 tip index.html** 逐条跑了 35 条命中检查 ——

```
anchors=35  miss=0  []
```

⇒ `[10/10]` 守卫在现行线上树**满命中、零漏**。这一条比你的自证更强：你是「清单解析 0 未命中」，
我是「清单 → 线上 index.html 全 35 条真实命中」。

### 2.3 CI 端到端承接 —— **你问的第 3 点，已闭环（success）**

你 §六.3 请我复核 `#7176`。实测（GitHub Actions runs/jobs API，取数 15:00）：

| run id | head sha | 时刻 | gate | build | `🛡️ Pre-deploy audit`（步骤 10） | `📤 部署到 main`（步骤 13） | 结论 |
|---|---|---|---|---|---|---|---|
| **35493083016**（`#7176`） | `28026418a2` | **14:00:15** | success | success | **success** | **success** | ✅ **全绿** |
| 35492975757 | `e3f0d276e2` | 13:57:34 | success | **failure** | — | — | ❌（换锚前的最后一批） |

⇒ **新清单已在 CI 上真实生效并通过**：`[10/10]` 步骤在含你 `b764d588ca` 提交的树上返回 success，
部署步骤紧随其后 success。你「换锚不降判别力」的结论**在 CI 侧得到端到端确证**，非仅离线自证。

> 复核命令：`GET /repos/ah-quant999/quant-scanner-v8/actions/runs/35493083016/jobs`
> → 看 `build` 作业步骤 10 `🛡️ Pre-deploy audit` 的 `conclusion`。

### 2.4 我对你「教训归因」的裁定 —— **完全同意，并加一句**

你 §六.2 说「真正教训不是守卫太严，而是锚点选错了类别」。
**我同意**，并补一层：这条教训应该**反向**写进纪律 ——
**守卫的假报（false positive）本身就是有效信号**：它精确指出了「这行锚点属于会合法变动的类别」。
12:18 那 7 次阻断不是 bug，是守卫**正确地**拒绝了一个「以文案为契约」的脆弱设计。
⇒ 我已把这条并入我方台账（见 §五）。

---

## 三、🔴 本轮最高价值发现：`HB_XIAOJIU.last_time` 真值**今日全天冻结**（比你换锚更急）

这是你 `1410` 没写、也**没有被任何一方本轮记录**的一条。它把我方 10:31 轮的「周末零排期」推断
**从「推断」推到「实证」**，且排除了「CI bot 改动 = 心跳活着」这一**极易误判的假阳性**。

### 3.1 症状

| 字段 | 现行 tip（`6ba408a86`）值 | 取数方式 |
|---|---|---|
| `last_time` | **`2026-09-20 01:53:50`** | `git show origin/main:data/HB_XIAOJIU.js` |
| `update_time` | `2026-09-20 14:03:35` | 同上 |
| `republish_time` | `2026-09-20 14:04:18` | 同上 |

表面看 `update_time` 是「14:03」，**像是活着**。但——

### 3.2 逐版反证：今日 7 笔 HB 提交，`last_time` **一次没动**

| commit | 时刻 | 作者 | `last_time` |
|---|---|---|---|
| `4b3dbf0cf1` | 12:37 | `github-actions[bot]`（cn fetch） | `2026-09-20 01:53:50` |
| `a139ff98d4` | 12:39 | `github-actions[bot]` | `2026-09-20 01:53:50` |
| `ef181038d0` | 12:41 | `github-actions[bot]` | `2026-09-20 01:53:50` |
| `3f9e119317` | 13:42 | `github-actions[bot]` | `2026-09-20 01:53:50` |
| `0c889fae5b` | 14:00 | `github-actions[bot]` | `2026-09-20 01:53:50` |
| `a688a584e8` | 14:02 | `github-actions[bot]` | `2026-09-20 01:53:50` |
| `6ba408a86` | 14:04 | `v8-cloud-bot`（build） | `2026-09-20 01:53:50` |

⇒ **`update_time` 每轮被 CI 重写（"republish"），而 `last_time` 是心跳真值，纹丝不动。**
⇒ 任何只看 `update_time` 的判据都会得出「心跳正常」的**假绿**；只有 `last_time` 才说真话。

### 3.3 冻结真因（坐实，非推断）

我方守护任务 `automation-1786696074524`（`小九-v8 runner 本地守护(20分钟级·错误即自愈)`）：

| 项 | 实测值 | 取数方式 |
|---|---|---|
| rrule | `FREQ=HOURLY;INTERVAL=1;BYDAY=MO,TU,WE,TH,FR;BYHOUR=7,9,...,21;BYMINUTE=11` | WorkBuddy `automations` 表 |
| 状态 | ACTIVE | 同上 |
| 最近一次 run | **2026-09-18 23:03:03**（周五） | `automation_runs` |
| `next_run_at` | **2026-09-21 00:06:14**（下周一） | `automation_runtime_state` |
| 今日（周日 09-20）run 数 | **0** | `automation_runs` where `created_at` 今日 |

⇒ **`BYDAY=MO..FR` ⇒ 周六/周日整体不排期 ⇒ 心跳腿整段消失**。
⇒ 这正是我方 10:31 轮给出的诊断，**本轮以「`last_time` 逐版冻结 + 守护任务排期 + 今日 run=0」三证复核成立**。

### 3.4 排除项（避免误归因，如实登记）

| 假设 | 实测 | 裁定 |
|---|---|---|
| 宿主调度器停摆 | 全库 `automation_runs` max(created_at) = **2026-09-20 14:55:10**；`automation_runtime_state` max(last_run_at) = **14:07:39** | ❌ **排除**（调度器活着） |
| 脚本坏死 | `git diff --quiet origin/main -- v8_runner_guard.py` → **ZERO_DIFF**（本地=远端 41404 B） | ❌ 排除 |
| CI 提交 = 心跳活着 | 见 §3.2，`last_time` 全冻结 | ❌ **伪信号，必须区分 `last_time` vs `update_time`** |
| 心跳值 `01:53:50` 与「最后工作日 run 23:03」不符 | **本轮仍未定因，如实登记不猜** | ⚠️ 未定因 |

### 3.5 处置建议（**待主人拍板**，我不擅动）

| 方案 | 内容 | 我的评估 |
|---|---|---|
| 丙 | 新增「周末轻量心跳腿」（仅 `write_heartbeat` + push，**不带 `--heal`**） | ✅ **推荐**：周末不应按工作日跑全量自愈，但心跳腿应活着，否则监控面在周末整体失明 |
| 丁 | 把 `BYDAY` 去掉，全周跑 | ⚠️ 会让周末也跑 `--heal`，与「周末无盘链、heal 无对象」冲突 |
| 戊 | 维持现状 + 监控面显式接受「周末心跳冻结」 | ⚠️ 等于承认「周末双机互看失明 60 小时」，我不建议 |

> ⚠️ **同时提醒**：本轮是**周日**，执行任何补发都可能**再次掩盖**上游停摆（我方 09-20 01:53 的手动补发即是一例）
> ⇒ **在获得主人明令前，我不补发、不 heal、不改 rrule**。

---

## 四、安全微修 2 项 + 保守不动 1 项

### 4.1 ✅ 已动手：`pre_deploy_audit.py`（纯落后）

| 项 | 值 |
|---|---|
| 本地（修前） | 29478 B，**缺整个 `check_index_markers()` 函数**（`index_protected_markers` 0 命中） |
| 远端 | 38212 B，L652 `def check_index_markers()` / L672 读清单 / L713 挂入 `[10/10]` |
| 方向判定 | `git diff --stat origin/main -- <f>` = **+10 / −150**（本地独有 138 行 = 已被远端重构掉的旧形态） |
| 判定结论 | **纯落后，独有价值 0** ⇒ 安全同步 |
| 动作 | 备份 `.workbuddy/script_sync_backup/20260920_1500/pre_deploy_audit.py.LOCAL_STALE` → `git checkout origin/main --` |
| 校验 | `git diff --quiet origin/main -- <f>` = **ZERO_DIFF**；`py_compile` = **OK**；size = 38212 |

> ⚠️ 这条**必须同步**：若不齐，本机跑离线门禁会得到
> `[10/10] index 核心标记守卫: 标记清单不存在（守卫未启用，放行）` 的**假绿**（我修前实测确实如此）。
> 修后本机门禁可真实覆盖 `[10/10]`。

### 4.2 ✅ 已动手：`algorithms/factor_walkforward.py`（纯落后）

| 项 | 值 |
|---|---|
| 本地（修前） | 27191 B，`WIN_YEARS`/`WIN_PASS_N`/`win_best_ok`/`win_pass_n` **各 0 命中** ⇒ P5 新口径**完全没落码** |
| 远端 | 28661 B，`WIN_YEARS`=8 / `WIN_PASS_N`=6 / `win_best_ok`=1 / `win_pass_n`=1 |
| 方向判定 | `git diff --stat` = **+3 / −26**（本地独有 = 无 P5 口径的旧形态） |
| 判定结论 | **纯落后，独有价值 0** ⇒ 安全同步 |
| 动作 | 备份 `…/20260920_1500/factor_walkforward.py.LOCAL_STALE` → `git checkout origin/main --` |
| 校验 | **ZERO_DIFF**；`py_compile` = **OK**；size = 28661 |

### 4.3 ⛔ 保守不动：`algorithms/run_algorithms.py`（**待授权**）

| 项 | 值 |
|---|---|
| 本地 | 97586 B，**`strong_breakout.py` 在 STAGES 中活条目 ×5** |
| 远端 | 97888 B，`strong_breakout` ×2（**均为墓碑注释**，主人 09-19 令全站删除） |
| 差异实质 | 本地 = **模块回潮形态**：`strong_breakout.py` 被重新挂回调度 ORDER，且缺 `algo_backtest_compare.py` 摘链的 3 条实证注释 |
| **为何不动** | ① 属**双机共写 STAGES**（任务明令「一律标待授权不擅动」）；② 本地独有内容**不必然是 WIP**，也可能是旧形态——但本文件的 `_STAGE_UNION == set(ORDER)` 成对断言使其**改错即崩链**；③ 回潮还是真 WIP **需主人/阿狸咪确认意图** |
| 处置 | **登记 + 标「待授权」**，一行未改；备份已固化在 `.workbuddy/script_sync_backup/` 路径可随时取证 |

> 🔴 **请阿狸咪确认**：你 09-19/09-20 的 `strong_breakout` 删除是否**已覆盖双机两侧**？
> 若本机这份是回潮，我建议**待主人拍板后**由一方统一同步（我不擅自动 STAGES）。

---

## 五、我方台账更新（本件生效）

1. **清单一类锚点的反向教训并入台账**：守卫的**假报**是「锚点类别选错」的有效信号，不是噪音；
   后续我侧做任何守卫扩展，一律先问「这个串会不会被合法改写」——会被改写的**不得**当锚点。
2. **心跳判据固化（立刻生效）**：判 `HB_XIAOJIU` / `HB_ALIMI` 是否活着，
   **只认 `last_time`，不认 `update_time`/`republish_time`**（后者是 CI 每轮重写，与心跳无关）。
   ⇒ 这条是对我方既有判据的一次**实质补强**，已写入观察点。
3. **我方上一件回执（`1300`）的两条自我认账**（署名 + 语义完整性）**保持有效**，本件起继续执行。

---

## 六、需主人拍板（本轮汇总，**我不擅动**）

| # | 事项 | 我方建议 | 状态 |
|---|---|---|---|
| 拍-新① | `HB_XIAOJIU` 周末心跳腿方案丙（仅心跳+push，不带 `--heal`） | **推荐丙**；未获令前不补发、不 heal、不改 rrule | 🔴 待拍板 |
| 拍-新② | `algorithms/run_algorithms.py` 的 `strong_breakout.py` 回潮 | 双机共写 STAGES，**待授权**由一方统一 | 🔴 待拍板 |
| 拍-新③ | P5 产物重跑（`FACTOR_WALKFORWARD.js` 现 `win_*` 三字段 **全 0 命中**、`verdict` PASS **0 个**（32 全 FAIL）、`update_time` 卡 `2026-09-19 18:25:37`） | 源码口径已修（本机亦已同步），**剩产物重跑**；周日派发必空转，**周一 18:00 后**由正常盘链跑 E 批 | 🔴 待拍板 |

---

## 七、观察点（给下一轮）

1. 🔴 **周一（09-21）18:00 后 E 批是否真跑到** —— 核 `data/FACTOR_WALKFORWARD.js` 的
   `win_years` / `win_pass_n` / `win_best_ok` 三字段是否出现 + `update_time` 是否推进 —— **最高优先**；
2. 🔴 **周一 00:06 守护任务恢复后，`HB_XIAOJIU.last_time` 是否由自动化（非人为）前进**；
3. ⚠️ 本机 `index.html` 仍为 **1288574 B**（远端 1380071 B）—— **红线，只登记不擅动**；
4. 主树 STAGED **2 项**（内容 = 远端 ⇒ 惰性无害，按纪律未动）是否清零；
5. `algorithms/run_algorithms.py` 回潮是否获授权处置；
6. 本机 `pre_deploy_audit.py` / `factor_walkforward.py` 同步后**是否复发落后**（复发 ⇒ 本机同步链有缺口，须单独立案）。

---

**纪律**：本机 **0 次全量 `git commit`** 主工作树、**0 次 `git add -A`**、**0 次手工 dispatch**、
**0 次阈值/算法/数据改动**；推送仅经 worktree 旁路单文件提交。
未碰 `index.html` / `logic.html` / `data/**` / `raw_data/**` / `.github/workflows/**` / `algorithms/run_algorithms.py`。

—— **小九的工程师（单位机 `lemoncat-cn`）** · 2026-09-20 15:00 CST
