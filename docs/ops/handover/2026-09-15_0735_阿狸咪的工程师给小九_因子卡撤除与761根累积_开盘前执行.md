# 交接：因子卡撤除 + 761 根累积 —— 开盘前工单

- **出单人**：阿狸咪的工程师（家用机 alimi-cn）
- **接手人**：小九的工程师（单位机 lemoncat-cn）
- **时间**：2026-09-15 07:35（阿狸咪夜窗 07:30 收尾，小九 07:45 接手）
- **背景**：主人 2026-09-15 06:35 令 —— ①「因子补缺与审计」卡不好就删；②`factor_lab_backtest.py` 去获取 761 根，拿不到就写「累积中」直到累积到；③仓库根 `_*.py` 查清是否噪声，是就删。

---

## ✅ 已完成（阿狸咪本机，无需你动手）

### 第 3 条：仓库根 `_*.py` 噪声清理

**取证四路全过**，判定为噪声并删除：

| 判据 | 结果 |
|---|---|
| 入仓状态 | **56/56 全部未入仓**（`.gitignore:59` 规则 `_*.py`），0 个被 git 跟踪 |
| 仓库内引用 | 仅 6 个被"提及"，引用方**全是** `backup_2026*/` 副本、`docs/` 历史交接/审计、`.workbuddy/memory` —— **无一是代码调用** |
| workflow / scripts / v8 | `grep` 零命中（`.github/`、`scripts/`、`v8/` 无任何调用） |
| 自动化任务 | **69 个 automation prompt 逐个精确整名比对：0 命中** |

- **删除 55 个**（`_fix_*` / `_check_*` / `_poll_*` / `_patch_*` / `_push_*` / `_verify_*` 等，共 330KB）
- **保留 `_gen_pinyin.py`** —— 🔴 `DO_NOT_DELETE.md:133` 明确豁免（STOCK_LIST.js 拼音字段生成器，重生成 STOCK_LIST 时必须重跑）
- 备份在仓库外：`C:\Users\HH20210606\WorkBuddy\2026-08-28-21-48-18\_a101\deleted_backup\root_scripts\`
- ⚠️ 因全部未入仓 ⇒ **本次清理不产生任何 commit，线上零影响**
- 📌 顺带发现（**未处理**，等你或主人定性）：仓库根另有 **8 个 `backup_2026xxxx/` 目录**（07-18 ~ 07-26 手工备份），也是噪声候选。

---

## 🔧 待执行（第 1 项）：「因子补缺与审计」卡整体撤除

### 为什么删

该卡 = 主人截图里那张「7 项待补 / done 0/7 / 完成率 0%」的展示卡。
- 产物 `FACTOR_PROGRESS` 全 7 项 `backtest_status: pending`、`oos_ir: null`，**owner 全是"小九"，即它不会自己前进**；
- 卡上自己就写着「walk-forward 一个都还没跑（done 0/7），故缺失项暂不具备接入条件」；
- 09-09 的上线门槛（OOS IR>0.3 且 4/5 年胜率>55%）从未有一项达成 ⇒ **是一张永不动弹的进度表**。
- 主人令：不好就删。

### 🔴 执行铁律

1. 改动含**线上主站 `index.html`** ⇒ 做完必须跑「提取全部 inline `<script>` → `new Function(code)`」校验，**0 错才算过**。
2. 现处**跑批中**（`git status -s raw_data/` 大量 ` M`）⇒ 阿狸咪侧一律走 FETCH_HEAD 派生 + 独立 `GIT_INDEX_FILE`，**不碰本机工作树**；若你要在工作树改，先确认跑批已结束。
3. 三处必须**同步**：`ORDER` / `STAGES["B"]` / 第三处列表 —— `run_algorithms.py:240` 自己写了「必须与 STAGES["B"] 成对修改，否则模块级 `assert(_STAGE_UNION == set(ORDER))` **崩链**」。
4. 推前**重取基线**（`git fetch origin main`），防双机抢推。

### 逐处清单（行号基于 2026-09-15 07:20 的远端 `index.html` @ `162857bce`）

#### A. `index.html`（4 处）

| 行号 | 动作 | 原文 / 锚点 |
|---|---|---|
| 7158–7311 | **整段删** | `  // ===== 📊 因子审计：v8 选股 vs 质量价值中小盘多因子…` 起，到 `  window.__fpRetry = 0;` 止（含 `window.__renderFactorAudit` 全函数体 + 09-13 合并后残留的注释块） |
| 7313 | **子串删** | 在 `_renderPostReady` 里删掉 ` try{ window.__renderFactorAudit(); }catch(e){}` |
| 14823 | **整行删** | `hm += '<div class="card" id="factorAuditBody" style="margin-bottom:16px;border-left:4px solid #22c55e;">…加载中…</div>';` |
| 14829 | **整行删** | `try{ if(typeof window.__renderFactorAudit==='function') window.__renderFactorAudit(); }catch(e){ console.warn('因子审计卡渲染失败', e); }` |
| 1713 | **整行删** | `<script src="data/FACTOR_AUDIT.js?v=…" defer></script>` |
| 1715–1716 | **整行删** | `<!-- 2026-09-10 主人令：因子补缺 walk-forward 进度跟踪卡… -->` + `<script src="data/FACTOR_PROGRESS.js?v=…" defer></script>` |

> ⚠️ 行号会随删除漂移 ⇒ **务必从大行号往小行号删**（14829 → 14823 → 7313 → 7158-7311 → 1715-1716 → 1713）。
> 14825-14826 的 `stFaHost` 宿主保留（`factorLabBody` 卡仍用）；只删 `factorAuditBody` 那一行。

#### B. `logic.html`（2 处）

| 行号 | 动作 | 锚点 |
|---|---|---|
| 1932 | **整行删** | `<tr><td>🧪 因子补缺 · walk-forward 进度（<code>factorProgressBody</code>）</td></tr>` |
| 2013 | **子串删** | 删 ` · <b>因子补缺与审计</b>(<code>FACTOR_AUDIT</code>+<code>FACTOR_PROGRESS</code> 合并为一张：上半覆盖状态／下半 walk-forward 进度)` |

> L1598 讲的是「因子实验室」迁入选股策略页，**与本题无关，不要动**。

#### C. 生成器与注册（6 处 + 2 文件删除）

| 文件 | 行号 | 动作 |
|---|---|---|
| `v8/update_v8.py` | 139–140 | 删注释 + `"factor_audit.json": "FACTOR_AUDIT",` |
| `update_v8.py`（仓库根同名映射表） | 145–146 | 删注释 + `"factor_progress.json": "FACTOR_PROGRESS",` |
| `algorithms/run_algorithms.py` | 70–71 | 删 ORDER 两行（`scripts/gen_factor_audit.py: 900` / `gen_factor_progress.py: 900`） |
| 同上 | 242–243 | 删 `STAGES["B"]` 两行（**必须与上面成对**） |
| 同上 | 314–315 | 删第三处列表两行 |
| `.github/scripts/dedup_fetch_manifest.py` | 62–63 | 删 `"data/FACTOR_AUDIT.js",` / `"data/FACTOR_PROGRESS.js",` |
| `.github/workflows/v8_cn_fetch_experiments.yml` | 12 / 89–90 | 删注释行 + `if [ "${{ inputs.run_factor_audit }}" != "false" ]; then … fi` 整块（**同时清理 workflow_dispatch 的 `run_factor_audit` input 定义**） |
| **删除文件** | — | `scripts/gen_factor_audit.py`、`scripts/gen_factor_progress.py`、`data/FACTOR_AUDIT.js`、`data/FACTOR_PROGRESS.js` |

> `raw_data/factor_audit.json` / `factor_progress.json` 是否入仓需你复核（`git cat-file -e FETCH_HEAD:<f>`）；入仓则一并删。
> `.github/scripts/v8_stage_gate.py:217` 只是**注释**里提了这两个名字，无害，可留可清。

#### D. 已确认不会触发假告警 ✅

两个每日自检自动化我都读过 prompt，**判据均不含因子卡**：
- 「开盘前防回滚自检（7:25）」只校验 index.html 的 8 项标记（持仓建议/已下架注入/暂未上架注入/nav解锁/炸板6列/无stcrdsWeakSpec/AVG_PRICE_DATA/调试专区无个股动量状态卡）+ 8 个关键文件存在性；
- 「v8 跨文件对齐审计（7:30）」只查 驾驶舱/全站精选/大牛股猎手 与调度矩阵。
- 另：`.github/scripts/pre_deploy_audit.py`（CI 7 门禁）**不校验**该卡。

⇒ 删卡是安全的，但 **`index.html` 的 8 项回滚标记一个都不能碰**。

---

## 🔧 待执行（第 2 项）：`factor_lab_backtest.py` 抓取窗口 700 → 761+ 与「累积中」

### 现状与根因（已实测）

- `algorithms/factor_lab_backtest.py:45` → `FETCH_DAYS = 700`；
- `:43` → `HOLDS = [5, 10, 20]` —— **主人要的 30/60/90/180/250 从未落地**；
- 档位守卫需 `NEED_MIN(261) + 2 × HOLDS[-1]`；加 250 档 ⇒ 需 **761 根**，而只拉 700 根 ⇒ **全票 0 样本**。

### 主人原话

> 「那就去获取761根，拿不到就写累积中，直到累积到为止。」

### 设计要求

1. **抓取窗口**：`FETCH_DAYS` 700 → **≥761**（建议 800，留余量）。注意三级兜底链（mootdx→东财→baostock）的**单源历史上限**——若某源拿不到 761 根，属正常，走第 2 条。
2. **档位**：`HOLDS` 扩到 `[5, 10, 20, 30, 60, 90, 180, 250]`（前端档位表是**动态发现** `win_Nd` 的，算法层补上即自动显示，无需改前端）。
3. 🔴 **不足必须写「累积中」，禁止静默 0 样本**（这是主人这条令的核心）：
   - 每票每档位算 `n_eff`（有效样本数）；`n_eff == 0` 时**不得输出 `win_rate = 0`**（禁「0 冒充」铁律），应输出 `null` + 状态字段，例如 `{"status": "accumulating", "have": N, "need": 761, "progress": N/761}`；
   - 前端该档位显示「累积中 N/761」，而非显示 0% 胜率；
   - 若要做到「直到累积到为止」，需要一个**跨日累积缓存**（把每次抓到的 K 线并入本地长历史），否则每天重抓仍是同样长度、永远到不了 761。建议落在 `raw_data/_factor_hist_cache/`，**注意该目录是否入仓**——不入仓则云端 `git clean -ffdx` 会清掉，累积会归零；若要跨机累积，必须在 09-08 令「仅 `kline_cache` 入仓」之外另行请示主人扩令。
4. **本机跑不出数据**：阿狸咪侧 A 股取数全断（红线：`A股=0 且 总数>0 ⇒ 报警`），所以这条**只能你（小九）跑**。
5. 你的自动化「九宝量化-因子实验室数据刷新(小九本机·baostock)」（id `60e1dbfd…`，周六 10:00，现为 PAUSED）正是这条的落点，改完可考虑恢复。

---

## 📌 上轮结论的两处诚实更正（避免误读）

1. 我曾断言「Alpha101 资产完全孤立」—— **错了**。存在 `algorithms/alpha101_signals.py`（后经核实为**未入仓孤儿**，内容 `universe_size:0` / `signals:[]`，即"挂链但恒空"的假接入），已于 09-14 提交 `f7077ab9` 清除。
2. 我上轮说「共识组合无多头超额」的**依据部分不成立**：那是基于 12 个**简化改写**因子且无利差门槛；把 82 个因子逐字实现后，现象变成「门槛越高超额单调越大且 IS/OOS 同号」（K≥12 T+30 超额 +1.38%）。**但补算按日 t 值后 |t|=1.70 < 2，仍不显著** ⇒ 结论（不接入）成立，**依据已更正**。详见 `2026-09-14_2345_…Alpha101三问定案…md`。

---

## 回执要求

做完第 1、2 项后，请在本文件末尾追加一行：`> [小九] YYYY-MM-DD HH:MM 执行完毕：<逐项结果 + 校验命令输出摘要>`，并跑一次 `align_logic_ops.py` 确认 EXIT 0（logic.html 与运维页对齐）。
