# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-23 22:36｜盯盘三项验收通过+两卡丢档根因链定性+派发风暴交裁决

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

今晚盯盘：**三项既有修复线上验收全部通过**（step14 并发推送 / step15 防覆盖 / 本机 align_gate 未阻断部署）；
同时查出 **5 项真问题**，其中 3 项**牵涉 09-04 防风暴令与多源派发源，我按主人令未擅改，请你裁决**：

1. 🔴 **EXPERIMENT / SECTOR_LEADERS 今日全天零产出** —— 根因链已闭合（§2），**修法待你定**。
2. 🔴 **cn_fetch 派发风暴**：近 40 run 取消率 **42%**，一轮连发 6 条 ⇒ 只有 1–2 条能跑 ⇒ **假成功**（§3）。
3. 🔴 **raw_data 净数 4309 > fail 阈值 2500**（远端实际 5529 / 全仓 6641）⇒ 压在 Trees API 推送通道上（§4）。
4. ⚠️ `STOCK_RPS.js` 09-13 已删，但问责清单仍标 `❌ 缺失`（假失败，§5）。
5. ℹ️ `BOARD_FIRST_LIMIT.update_time` 未推进 = **虚警**（非我的修复回归，§1 附）。

> **本轮我未改任何生产代码**（只读定性）。§1 是既有提交的线上复验，§2–§5 是待裁决项。

---

## 1. 三项验收通过（附日志原文）

### ① step14 并发推送（`15404ba6566f`）线上生效
run `35866103777`(#2072) 日志：
```
🚀 并发推送启动：81 个文件 / 并发 6 / 软闸 720s
📊 并发推送收工：耗时 23s / 未变化 12 / 防倒退 57 / 待更新 12 / 失败 0
```
同 run step9 内另一次：`174 个文件 / 并发 6` → `耗时 29s / 未变化 69 / 防倒退 78 / 待更新 27 / 失败 0`。

### ② step15 防覆盖修复（`1c069110e274`）线上生效
同 run：`push index.html ALGO_TRACK ?v: 200 v= 1790173483`
—— 只改 `?v`、保留主线手改（日志自述「改为取【当前 main 的 index.html】原文，只把 ALGO_TRACK ?v 改掉再 PUT」）。
step16 = success（`✅ 新鲜 13 / ❌ 必需项失败 0 / ⚠️ 时间戳盲区 0 / 共 14`）；step17 = success。

> ⚠️ **API 的 step 状态回填滞后**：该 run 的 job 已 `completed/success`，但 API 仍把 step14 显示为
> `in_progress`、step15 显示为 `pending`。**判据必须取日志，勿信 step 状态。**

### ③ 本机 align_gate 推送未阻断部署
`build_deploy #7936`（head = 本机 `c3198edd3b`）build job：
step10 `Pre-deploy audit` = success、step13 `部署到 main` = success。

### 附：`BOARD_FIRST_LIMIT.update_time` 未推进 = 虚警（非回归）
22:18 提交 `e003374342` 动了该路径 `+917/-917`，但**两版归一化行尾后逐字节完全相同**
（含 `update_time 20:13:58`）⇒ 脚本走了增量闸门 `SKIP`（正确行为）。
真身是 **CRLF→LF 行尾摇摆**：该提交含 937 行纯假 diff（`raw_data/board_first_limit.json` 917 +
`raw_data/_peer_monitor_state.json` 20），二者皆**本机 Windows 写入方**产物（`open(...,'w')` 未传 `newline=''`）。

---

## 2. 🔴 两卡丢档根因链（EXPERIMENT / SECTOR_LEADERS）—— 请你裁决修法

**事实**：`data/EXPERIMENT.js` / `data/SECTOR_LEADERS.js` 在 **09-23 全天零提交**
（最后更新分别 09-22 20:10 / 09-22 18:17）。

**根因链（逐环有日志原文）**：
```
① 派发风暴（§3）⇒ post_close 档从 17:20 拖到 21:11 才跑（run #2058，category=post_close）
② 21:15 东财 push2 已不可得：
   ⚠️ push2 抖动(clist m:0+t:6+f:!2,… pz=5000 po=1) 尝试1/3,2/3: Expecting value: line 1 column 1 (char 0)
   同轮 ⚠️ ETF_DAILY_MONITOR: 返回空 ｜ ⚠️ W52_HIGH 重试 3 次仍失败(502 Bad Gateway)
   ##[group]Run python scripts/fetch_sector_leaders.py → _get_json 抛异常（L274→213→130→68）
③ f_experiment 空返回 ⇒ 不写 raw_data/experiment.json
④ experiments #10（21:46 success）只跑 update_v8 ⇒ 幂等跳过：
   ⏭️ EXPERIMENT 数据未变，跳过重写（幂等） / ✅ experiment.json → EXPERIMENT.js（内容等同旧版）⇒ JS 停更
⑤ health_check 报 [FAIL] 全量数据/EXPERIMENT 超过通用红线 1440 分钟
⑥ HEAL 想补派，被两道守卫各拦一次：
   ① HEAL_DEBOUNCE_MIN=25「最近 25 分钟内已有 algo_run(created 13:18:00Z, in_progress)」→ 跳过
   ② v8_health_check.py:898「当前 21:47 非盘后产出窗口(16:00-21:30)，跳过 algo_run 派发」→ 跳过
```

**我的判断（供参考，未执行）**：
- ①③④⑤ 属**结构性**：`post_close` 档靠「时效」取胜，对派发延迟零容忍 —— 风暴一起它必丢。
- ⑥ 两道守卫**均为主人 2026-09-04 防风暴令指定** ⇒ **我不擅改**。
- 另注：`v8_cn_fetch_experiments.yml` 名义 cron `16:30 CST`，但实测 `#4–#10` **全部落在 21:04–23:14**
  （延迟 5–7 小时）⇒ 属仓内已记录的「GHA cron 静默丢触发」老病；是否改 dispatch 请你定。

---

## 3. 🔴 cn_fetch 派发风暴（取消率 42%）

- 近 40 run：**17 cancelled / 16 success**；actor 几乎全为 `ah-quant999`（双机共用 token）。
- **一轮连发 6 条**（间隔 5–17 秒，cron 不可能产生）：20:38 / 21:10 / **22:25** 各一组；
  6 条中仅 1–2 条能跑（`#2058`+`#2059` success），其余 **`jobs: []` 变 cancelled**。
- 判据（技能 `gh-dispatch-storm-diagnosis`）：**cancelled 且 `jobs` 为空 = 排队被顶掉**，不是运行失败。
- `v8_cn_fetch_cloud.yml:1210-1230` 自述**派发源三处**（原生 cron / 外部兜底 / 本接力），
  且并发组 `queue: max` ⇒ 被 cancel 的是**僵尸 pending**，但**该条代表的 category 就丢了**
  = **假成功**（派发方拿到 HTTP 204 却以为已刷新）。
- **建议修法**（技能二节原文）：按类别派同一 workflow 时必须**合并为单次 `category=all`** 或
  **逐个串行等待**；**禁并发派发多条**。
- 连带证据：`algo_cloud #2073`（21:50:44 创建）**22:26:06 才真正开跑**（排队 36 分钟），
  印证仓内注释「把排队时长伪装成 run 很慢」。

---

## 4. 🔴 raw_data 膨胀踩到 fail 阈值

- 判据源 `v8_health_check.py:2270-2294`：ok <850 / warn 850–1100 / **fail >2500**（排除 4 个缓存子目录）。
- runner 实测**净数 4309**（>2500 ⇒ fail；日志出现 3 次）；我方 `git ls-tree -r FETCH_HEAD raw_data` = **5529**
  （含缓存目录），全仓 **6641**。
- 属慢性膨胀（非今晚新发），但**正压在 Trees API 推送通道上**，需分流/归档。

---

## 5. 其它待跟进

- `data/STOCK_RPS.js` **09-13 已按令删除**（commit：删除 RPS 相对强度后端/产物/配置链），
  但 algo step17 问责清单仍列它并标 `❌ 缺失`（可选位）⇒ **假失败 / 告警疲劳**，建议清理清单。
- `[ALGO_TRACK] pct=48.16 超当日涨跌幅物理边界±44.0% @记录(300243)`（step16 仅告警不阻断）⇒ 数值越界待查。

---

## 6. 我这轮的自我纠错（避免你被我的中间结论带偏）

- 🔴 **我曾据「未分页的首 100 条提交」得出「09-23 白天零 cn fetch 提交」的错误结论**；
  全量取数后真相：**全天 400 笔提交（18–35 笔/小时）、其中 cn fetch 82 笔**。
  ⇒ 凡「某时段无提交 / 无事件」类结论，**必须先确认翻完分页**，否则会把「视野边界」当「事实边界」。
- 🔴 **时间口径**：注入的 `<current_time>`（09-22 21:00）**落后约 90 分钟且差一天**；
  且 `TZ=Asia/Shanghai date` 在本机 Git Bash **不生效**（仍输出 GMT）⇒ 须 `date -u` 手工 +8。
  本件真值 **09-23 22:36 CST**。

---

## 7. 待验证（本件发出时仍在跑）

- `algo_cloud #2073`：21:50:44 创建、22:26:06 实际开跑，本件发出时仍处 **step9「运行盘后算法链」**
  （22:31:20 起）。其 step14/15/16/17 待完成后我方复验（并发推送与 ?v 修复的第 2 个样本）。
