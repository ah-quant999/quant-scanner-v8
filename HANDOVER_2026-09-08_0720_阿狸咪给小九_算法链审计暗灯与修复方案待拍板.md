# 阿狸咪 → 小九 交接｜算法链只读审计：4 个「暗灯」+ 修复方案待拍板

> 时间：2026-09-08 07:20（阿狸咪夜班尾）｜性质：**只读审计，未改任何代码**
> 主人令：先交接给小九，白天做。**未拍板前不动算法逻辑、不删文件**

---

## 0. 一句话结论

算法链**主干健康**（09-07 全链 ok=13 / failed_required=0，体检 06:16 全绿），但审计出 **4 个体检视野外的「暗灯」** 和 **6 类系统性漏洞**。昨晚 P0 已闭环一个（LHB_7D）。其余等主人拍 A/B/C 方案后动手。

---

## 1. 当前基线（07:15 对账）

| 项 | 值 |
|---|---|
| 远端 main HEAD | `bb0cca5a12`（小九 07:15 handover 补记） |
| 本地 HEAD | 与远端**一致**，工作区仅 2 个未跟踪文件 |
| 体检 `freshness_status.json` | check 06:16：core/warn/frozen stale **全 0（全绿）** |
| 算法链 09-07 报告 | ok=13，failed_required=0，ts_blind=0 |
| 今日 | **交易日**（last_trade_close 2026-09-07 15:30） |

---

## 2. ✅ 已闭环（不用再管，只需复验）

| 项 | 证据 |
|---|---|
| P0 `gen_lhb_7d` 漏挂 ORDER | **修复生效**：`data/LHB_7D.js` update_time = **2026-09-08 01:44:05**（此前卡 09-04）。今晚 B 段再验一次即可 |
| 中信 PE 双卡上线 + 接 E 段 | `data/CITIC_PE_THERMO.js` / `CITIC_PE_BACKTEST.js` 已在 data/；E 段已挂 fetch+gen（含断点续跑） |

---

## 3. 🔴 四个暗灯（**体检查不到**，freshness 只扫 core/warn/frozen 三类）

| # | 项 | 现象（实测证据） | 定性 | 建议动作 |
|---|---|---|---|---|
| 1 | **DELISTED** | `raw_data/delisted_stocks.json` 有 **5 条真实数据**（龙资源 01712 / 驴迹科技 02900 等，delisted_date 2026-09-02）；而 `data/DELISTED.js` = `{total:0, recent:[]}`，update_time 停 **09-01**；前端 `index.html:554` 注入、`:12432` 读 `window.DELISTED_STOCKS` | **`build_delisted` 全仓无调度**（`grep` run_algorithms.py **0 命中**）→ 孤儿脚本，不是"写失败" | 二选一**需拍板**：挂回链 / 按轻量化整链下线 |
| 2 | **INDEX_HISTORY** | raw `index_history.json` meta.update_time = **09-08 01:56:52（新）**；但 `data/INDEX_HISTORY.js` 内 update_time = **09-04 18:37:33（旧 4 天）** | raw↔JS **断链**：`update_v8.py:212` 有映射 `"INDEX_HISTORY": "post_close"` 却没落成 JS | 排查（**只查不改**）：是 update_v8 未触发，还是读了旧源 |
| 3 | **FACTOR_LAB** | `raw_data/factor_lab.json` 与 `data/FACTOR_LAB.js` update_time **完全一致 = 09-05 14:05:32** | **不是 bug**：raw↔JS 同步正常，只是生成器 `v8/factor_lab_gen.py` 冷启动 50–90min、挂 E 批，非每夜必成 | E 批加"失败/超时兜底 + 陈旧告警"；当前**已不阻塞** final_recommend，不必急救 |
| 4 | **MARKET_PATH_PROBABILITY** | `data/MARKET_PATH_PROBABILITY.js` 存在（1775B）；**全仓 .py 搜不到生成脚本**；前端 `grep MARKET_PATH index.html` = **0 引用** | **死数据孤儿**：AI 预测卡回测管线移除时删了生成器，文件却残留在 git，每次 checkout 带回 | 删文件 + 清映射 + 清保护名单 |

### ⚠️ 方法学修正（重要，避免踩坑）

- **mtime 不可信**：09-08 06:46 有一大批 `data/*.js` + `raw_data/*.json` 被**同时刷新 mtime**（checkout/同步所致），看起来像"刚写过"，实际内容可能是几天前的。
  → **判新鲜度一律读文件内 `update_time`，别用 `ls -l`。**
- 昨晚阿狸咪正是被 mtime 误导，把 #4 误判为"06:46 复刻"、#3 误判为"写失败"；**今天已按字段时间戳修正**。交接以此版为准。

---

## 4. 六类系统性漏洞

1. **STAGES ↔ ORDER 双表必须同步** —— 已被 P0 实证：漏挂一个就 `assert` 崩整段。建议加机器校验（见方案 C）。
2. **`except: pass` 静默吞错** —— `v8/factor_lab_gen.py` 有 6 处。
3. **DO_NOT_DELETE.md 文档漂移** —— 称 DELISTED「0 引用」，实际 `index.html` 有 2 处引用（:554 注入 / :12432 读取）。
4. **孤儿无巡检** —— `build_delisted`、`MARKET_PATH` 都是这么悄悄变成孤儿的。
5. **体检覆盖不全** —— `freshness_status.json` 只扫 core/warn/frozen，上述 4 暗灯全在视野外，绿灯≠没问题。
6. **生成器的批次归属靠注释约束** —— 无机器校验（如 factor_lab_gen 冷启动 50–90min 是否适配所在批次窗口）。

---

## 5. 三个方案（**主人未拍板，小九先别动逻辑**）

| 方案 | 范围 | 耗时 | 说明 |
|---|---|---|---|
| **A 最小修** | 删 MARKET_PATH + 清映射；修 INDEX_HISTORY 落 JS；DELISTED 待拍板 | 1–2h | 不动 FACTOR_LAB、不动文档 |
| **B 中修** ⭐ 阿狸咪推荐 | A + FACTOR_LAB E 批兜底告警 + DO_NOT_DELETE 全量校对「0 引用」声明 + 孤儿巡检脚本 | 半天 | 覆盖 4 暗灯 + 漏洞 3/4 |
| **C 全审** | B + `except:pass` 改日志 + 写脚本改按 raw 触发 + 新增 CI `verify_chain_consistency.py`（STAGES↔ORDER 机器校验） | 1 天 | 一劳永逸，防同类再犯 |

---

## 6. 待主人拍板 3 问（**阿狸咪不得代答**）

1. 选 **A / B / C**？（阿狸咪推荐 **B**）
2. MARKET_PATH 死数据：只删文件收口，还是要挖"当初谁把它复刻回 data/"的源头？
3. DELISTED：①挂回链恢复真实 5 条，还是 ②按轻量化整链下线（含前端 `DELISTED_STOCKS` 相关代码一并删）？

---

## 7. 小九白天执行清单（**不需拍板即可做**）

- [ ] 今日交易日：常规跑全链；重点盯**今晚 21:00 E 段中信 PE 首跑**，验断点续跑是否只拉增量（看 `raw_data/citic_pe_history.meta.json` 的 `last_date`）
- [ ] 今晚 B 段复验 `LHB_7D` 是否稳定停在 09-08（P0 二次确认）
- [ ] **只读排查**：`update_v8.py:212` 映射为何没把 09-08 的 index_history 落成 JS
- [ ] **只读排查**：`MARKET_PATH_PROBABILITY` 在 `update_v8.py` / `cloud_weekly_cleanup.yml` / `DO_NOT_DELETE.md` 里还有无映射或保护名单残留
- [ ] ⛔ **不要在主人拍板前**改任何算法逻辑、删任何文件

---

## 8. 铁律提醒（血泪）

- 改 `STAGES` 必须同步改 `ORDER`，否则模块级 `assert` 崩全段（09-08 P0 教训）。
- 判新鲜度**读文件内 `update_time`**，不信 mtime。
- workflow 文件走 **Git Database API**（`v8_gh_token.txt`，带 workflow scope）；普通文件走 **Contents API**（`v8_gh_pat`）。
- 坚果云同步环境：**禁止 `git add -A`**，只能显式 add 具体路径。
- 凭证禁止写死在任何文件里，脚本运行时读取。

---

_阿狸咪 2026-09-08 07:20 · 夜班尾交接 · 只读审计，零代码改动_
