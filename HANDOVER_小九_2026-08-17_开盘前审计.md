# HANDOVER — 2026-08-17（周一）开盘前审计交接

> 交接方：本机 → 小九（周一工作日主班）/ 主人
> 时间：06:25 CST（开盘前例行审计 · 审计 → 防覆盖 → 各中对齐 → 交接）

## 一、审计结果（audit_weekly --cloud）
- **?v 失配 9 个**（STOCK_RPS / FOUR_VOLUME / FOUR_VOLUME_60M / STOCK_STOP_DATA / FINAL_RECOMMEND_DATA / ALGO_TRACK / STOCK_MOMENTUM_STATE(V2) / MOMENTUM_FILTER）→ **已按 CDN 权威修复**（79 处对齐，commit `daba174e77`，Pages 构建中）。
  - 这是**已知缺陷**：cn runner 直推 data 后，后续 build 会把 cn-extra 文件 ?v 覆盖回旧值（昨晚已修 2 次均被 build 覆盖）。**云端每日 22:30 自愈兜底**（v8_daily_audit.yml 内嵌 audit_weekly --cloud + fix + 直推）。开盘前已手动修好。
- **数据陈旧度 0 项**（新增检查）：STOCK_QUOTE=08-16 19:17 / LHB=08-14 / ALGO_TRACK=08-16 19:40 / **FINAL_RECOMMEND=08-17 05:52（今早已自动更新）**，均 ≥ 最近交易日 08-14。
- **workflow 不一致 4 项** = 本机 origin/main 缓存旧误报（cn git 墙 fetch 失败），云端跑不出现，忽略。
- 审计脚本陈旧度检查已优化为 **Range 取 head 4KB**（不再因大文件下载超时误报"检查失败"），已推 main。

## 二、防覆盖 / 云端对齐
- 云端 workflow 昨晚全部 success：`v8_daily_audit.yml`（22:30）、`v8_freshness_watch.yml`（每小时）、`v8_cache_buster_reconcile.yml`（15 分钟）、`v8_health_patrol.yml`（workflow_run）。`v8_t1_guard`/`v8_weekend_light` 周六/周末才跑，无 run 正常。
- 浏览器内存缓存铁律（昨晚已修）：index.html head 已加 `<meta http-equiv="refresh" content="1800">`（30 分钟自动刷新），用户不再看到 N 天前数据。

## 三、双机自动化（DB 残留 11 行，工具层真实 6 个 AI 交互类）
- 真实在跑（工具层）：自动读取交接×3（10:00/14:30/18:30）、自动交接检查 19:30、小九心跳监控+自动接管（每 30 分）、紧急指令监听（每 2 小时）——**全留本机**。
- **DB 与工具不同步**：已删的 4 个数据类（数据新鲜度/T+1巡检/T+1兜底/周末轻量维护）在 DB 仍显示 ACTIVE=NULL（软删残留，不影响调度）；audit_weekly 报的 8 个 NULL 含这 4 个残留。
- **真实待主人 UI 确认 model=deepseek-v4-flash 的 4 个**：阿狸咪-自动读取交接×3 + 阿狸咪-小九心跳监控+自动接管。

## 四、开盘关注
1. **算法链 18:30 交易日历 gate 放行**：验证全量推送（最终推荐今早已 05:52 更新过，观察 18:30 后是否再刷）。
2. **动量筛选观察期**（见 HANDOVER_小九_2026-08-16_动量筛选观察期.md）：第四张「🎯 动量共识筛选」卡每天随 build 自动重算，每周日跑对比报告，2026-08-30 复核日决策矩阵。
3. 若盘中见「个股查询/某卡显示旧日期」→ 浏览器 30 分钟自动刷新已兜底，普通 F5 即可。

## 五、铁律提醒
- 修 ?v 一律用 `scripts/fix_cachebusters_cdn.py`（CDN 权威），勿用 reconcile（墙下取过时缓存）。
- 算法输出「全港股/A股=0 且总数>0」= 上游 bug 立即报警。
- 非交易日 stocks=0 正常，不得误报。
