# 交接：三重共识连续 8 天为 0 根因修复（晚间）

**时间**：2026-08-12 19:50 左右
**提交**：`045ac1c84`（已 push main，本地 == 远端）
**主人触发**：「三重共识」→ 发现前端三重共识/今日可入手候选 count=0 持续 8 天

## 问题链（为什么三重共识一直 0）

```
stage_to_raw.py V6_TO_V8 自 08-04(commit c862e2177) 起把
  out/candidate_pool.json 改名搬运为 raw_data/candidate.json
        ↓
fetch_fundamental_quality.py 却读 raw_data/candidate_pool.json（永不存在）
        ↓
build_universe 只剩 gold_pool（gold_pool 一直是全港股，A股靠 candidate_pool）
        ↓
A 股基本面 grade 全空（今日 fundamental_quality 5 只全是港股/昨日 9 只全是港股）
        ↓
TOP10 score_quality 恒 0 → A 股评分被拉低 → TOP10 只剩港股（今日 max_score=24 < 门槛25 → 0 入围）
驾驶舱 A 档今日 0 只（技术双真筛选，弱市真实状态，非 bug）
        ↓
三重共识严格档（TOP10 ∩ A档 ∩ 基本面A）必为 0，near_miss 也为 0
```

## 已修复（2 个 py + 1 个数据）

1. **`algorithms/fetch_fundamental_quality.py`**
   - `build_universe()` / `build_name_map()`：候选文件改为 `candidate_pool.json → candidate.json → gold_pool.json` 依次读取（candidate.json 兜底）
   - 本地已重跑生成：**352 只，A=36 B=30 C=143 D=126**（修复前 A=0）

2. **`algorithms/scanner.py`**
   - `load_candidate_pool()`：路径兼容 `out/candidate_pool.json → raw_data/candidate.json → algorithms/data/candidate_pool.json`（旧路径自 08-04 起永不存在的 bug，导致金股池候选限制失效退化为旧逻辑）

3. **`raw_data/fundamental_quality.json`**：新生成的 352 只正确版本（云端 18:42 的 5 港股旧版已在 rebase 冲突中取新版本）

## 小九需要知道

- **下次云端跑算法链**（fetch_fundamental_quality 用新代码）→ 基本面恢复 → TOP10 quality 分恢复 → A 股能进 TOP10 → 三重共识恢复
- **今日 count=0 仍会显示**（今天数据是修复前生成的），明天盘后生成后恢复
- 今日 TOP10 只有港股是「修复前 quality 分 0」+「金股池今日仅 5 港股（弱市无 A 股共振≥2，真实状态）」双重结果
- 若明天三重共识仍 0：检查 ① fetch_fundamental_quality 是否用新代码跑 ② 金股池 A 股数量 ③ TOP10 max_score 与门槛
- ⚠️ 数据文件纪律：**不要提交本地旧 data/*.js / raw_data/top10_daily.json 等**——本地是昨日数据，会覆盖云端今日新数据。只提交 py 修复 + 当天重新生成的 raw_data 文件
