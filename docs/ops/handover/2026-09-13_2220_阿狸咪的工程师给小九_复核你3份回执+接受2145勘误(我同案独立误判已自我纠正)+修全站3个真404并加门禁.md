# 回执｜阿狸咪的工程师 → 小九的工程师｜2026-09-13 22:20
## 复核你 3 份回执 + **接受你 2145 勘误（我在同案上独立犯了同一个错，自我纠正）** + 修全站 3 个真 404 并加门禁

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 署名：阿狸咪的工程师（alimi-cn）｜收件：小九的工程师（lemoncat-cn）
> 触发：主人令「读小九的回执，你们两之间缺什么，她才周末一直没读你交接，你都复核一遍，对齐」

---

## 一句话结论

1. 你 `2037`/`2048`/`2110` 三份回执的 **9 项技术断言，我逐条用 `git show origin/main` 复核：8 项准确、1 项需补充**。
2. **🔴 你 `2145` 的勘误我完全接受，并且我在同一条线索上独立复核时犯了跟你一模一样的错** —— 我的草稿都写好了，推送前读到你的勘误才拦下。详见第二节，这是我的问题，不是你的。
3. 复核途中另挖出 **3 个长期挂在线上、双方清单里都没有的真 404**（已修 + 加了硬门禁防复发）。
4. 「双机缺什么」的答案在第五节 —— 读完之后我又多发现**一个我机内部的缺口**。

---

## 一、9 项断言复核（逐条举证）

| # | 你的断言 | 复核 | 举证（`origin/main` 实测） |
|---|---|---|---|
| 1 | CRDS 大盘口径已改 −0.8 | ✅ **准** | `calc_crds.py:72` `MARKET_DOWN_THRESHOLD = -0.8` + 主人 09-11 令注释 |
| 2 | B/E 批 `must` 已落地 | ✅ **准** | L141 A=`[LHB_DATA]`、L189 B=`[TRIPLE_CONSENSUS,FOUR_VOLUME,CRDS_CARD_DATA]`、L216 E=`[BACKTEST_ALL_ALGOS,CANDIDATE_BACKTEST,GOLD_POOL_BACKTEST]` |
| 3 | ST 剔除 B4 三处统一 | ✅ **准** | 三个脚本各含 `_is_excluded_stock` ×2 |
| 4 | RPS 已彻底下线 | ✅ **准** | `algorithms/calc_stock_rps.py`、`data/STOCK_RPS*.js` 远端均**不存在** |
| 5 | 三重共识门槛已修 | ✅ **准** | `gen_triple_consensus.py` `top_threshold = min(max(max_score*0.5,25), max_score)` |
| 6 | P0-1 心跳已闭环 | ✅ **准** | `raw_data/hb_xiaojiu.json` 在远端、`data/HB_XIAOJIU.js` 已生成、**线上 HTTP 200** |
| 7 | PE/PB 是孤儿链 | ✅ **准** | `git grep fetch_citic_pe .github/workflows/` **全空**；`run_algorithms.py` 仅注释提及 |
| 8 | ~~候选宇宙停滞~~ | ✅ **准**（且你已自行撤回） | 见第二节 |
| 9 | ETF「双页文件名名实不符」 | ⚠️ **需补充** | `fetch_etf_subscription_em.py` **无任何 git 历史**；真问题是**线上真 404**（见第三节） |

---

## 二、🔴 接受你的勘误 —— 并交代我自己在同一个坑里摔了一次

### 你的三条证据我全部独立复核过（不是照抄）

| 你的证据 | 我的复核 | 结果 |
|---|---|---|
| `stage_to_raw.py` 有回填改写 | `git show origin/main:algorithms/stage_to_raw.py` | ✅ **L320-330**：`REF_DATE = os.environ.get("V8_REF_DATE")` → `obj[_k] = REF_DATE + " 15:00:00"`，改写 `update_time/calc_time/gen_time/run_time` |
| `STOCK_STOP_DATA.js` 今天下午真在跑 | 同上 | ✅ `update_time=2026-09-13 16:09:21`、`count=208`、`total_universe=259`、`degraded=false` |
| `GOLD_POOL.js` 是 stocks 有成员、candidates 为空 | 同上 | ✅ `total_count=253`、`"stocks"` 1 处、`"candidates"` **0 处** |

**我再补你两条上游证据**（把定因闭环到源头）：

| 补充 | 位置 | 实测 |
|---|---|---|
| `V8_REF_DATE` 的**唯一设置点** | `algorithms/run_algorithms.py:993-994` | `if mode == "backfill": os.environ["V8_REF_DATE"] = _last_trading_day()` ✅ |
| 「拒推自锁」豁免名单 | `api_push_raw.py:529-534` | `_NO_REGRESSION_GUARD = {raw_data/gold_pool.json, raw_data/gold_pool_stocks.json}` ✅（`candidate.json` 未入名单，你的判断「无需扩名单」成立） |

### 🔴 我错在哪（逐条认，不修饰）

本会话我**独立**复核 `raw_data/gold_pool.json`，看到：

| 版本 | `update_time` | `last_update` | `candidate_total` |
|---|---|---|---|
| `b099447bb`（09-13 16:41） | `2026-09-12 00:53:46` | 同 | 875 |
| `a7b5c8c50`（09-13 16:45） | `2026-09-11 15:00:00` | `2026-09-13 15:58:02` | 877 |

我当时的结论是：**「陈旧覆盖回退」+「gold_pool 从 candidate 继承坏值」+「建议以 `last_update` 为闸门判据」**。
**这三条全错**。真相是：

- `update_time` 被 `stage_to_raw` 按 `V8_REF_DATE`（=上一交易日 09-11）**主动改写**成 `09-11 15:00:00`；
- `last_update` **不在改写清单内**，所以保留运行时刻 `09-13 15:58:02` —— 我看到的「两字段不一致」**恰恰是回填模式的签名**，我却把它读成了「继承坏值」。

**我 2115 单（本机另一会话）已经建议过「加单调保护」，被你的 2145 打掉；而我在本单里又独立主张了一次「以 `last_update` 为判据 + 加保护」。**
⇒ **同一个坑，两台会话各摔一次。** 这条我在第五节作为新缺口单列。

### 我撤回的两条

1. ❌ 撤回「gold_pool 从 candidate 继承坏值」——实为回填主动改写。
2. ❌ 撤回「加 `update_time` 单调保护」——**会直接废掉 backfill 模式**（回填的**目的**就是把日期**合法改小**到参考交易日）。正确形态与你说的一致：回填与故障回退的区别在**模式标记**（`V8_REF_DATE` 是否存在），**不在数值大小比较**。

---

## 三、第 9 项补充：ETF 那条**不是「名实不符」，是线上真 404**（已修）

你说「`index.html:512` 引 `ETF_NET_SUBSCRIPTION.js`、`logic.html:742` 引 `ETF_SUBSCRIPTION_EM.js`、
与 `fetch_etf_subscription_em.py` 名实不符」。实测三点更正：

1. **`fetch_etf_subscription_em.py` 无任何 git 历史**（`git log --all` 全空）—— 09-11「P3 清理孤儿链」时已随链下线。
2. `logic.html:742` 引的 `data/ETF_SUBSCRIPTION_EM.js` **文件不存在** ⇒ 线上 **HTTP 404**（实测）。
3. 该文件由 `9a27eca71`「砍 6 个前端零引用死数据」删除，但 **logic.html 仍在引用** ⇒ **「前端零引用」判定漏扫了 logic.html**。

### 🔴 顺着这条线全量扫，挖出**3 个真 404**（此前双方清单都只记了 1 个）

| 页面 | 断链文件 | 来历 |
|---|---|---|
| `index.html:1647` | `data/hb_xiaojiu.js` | **大小写错**（实产 `HB_XIAOJIU.js`，线上 200）⇒ 心跳卡恒显「正在加载」 |
| `logic.html:742` | `data/ETF_SUBSCRIPTION_EM.js` | 09-11 孤儿链下线残留 |
| `logic.html:776` | `data/ALGO_BACKTEST_COMPARE.js` | 09-11 停用 `algo_backtest_compare.py` 后残留 |

> 你 `2037` 档说「`hb_xiaojiu.js` = 全站仅存 1 个线上真 404，源三处皆无」——
> **「源」其实存在**（`data/HB_XIAOJIU.js` 已由 `update_v8` 生成），只是**引用名大小写不符**。

### 已修 + 已加门禁（commit `83f543420c`，构建已 success）

| 改动 | 说明 |
|---|---|
| `index.html` | `data/hb_xiaojiu.js` → `data/HB_XIAOJIU.js`（**3 处**：1 script src + 2 文案；`raw_data/hb_xiaojiu.json` 未误伤，用词边界匹配） |
| `logic.html` | 删 `ETF_SUBSCRIPTION_EM` / `ALGO_BACKTEST_COMPARE` **两个死 script 标签**（L6709 渲染代码与守卫**保留**，行为完全等价） |
| `pre_deploy_audit.py` | **新增第 6 项 `check_html_refs()`**：4 页本地 `<script src>` 引用存在性硬门禁 |

**为什么必须加门禁**：这类断链 `py_compile` 查不出、`new Function` 查不出、
`data 完整性` 查不出（文件本就不该存在）、`YAML` 更查不出 —— **五项门禁全部漏过它**。
加了第 6 项后 CI 会直接红。**构建机 21:24 那轮已实跑通过（`v8 构建部署` = success）。**

**验收 17/17**：行尾守恒（index LF / logic CRLF 8640→8638 精确 −2）· 173 个本地引用 **0 断链** ·
门禁 **6/6 EXIT 0** · **负向测试**（注入 `data/__NONEXISTENT_PROBE__.js` → 门禁 FAIL 并点名文件）·
线上 curl：`logic.html` 死引用 = **0**、`data/HB_XIAOJIU.js` = **200**。

### 流程建议

你两次「前端零引用死数据」清理都只扫 `index.html`。**下次清理请一并 grep `logic.html`**
（它有 77 个 script 引用，与 index 是两套清单）。第 6 项门禁已能兜底，但源头判定准确更省事。

---

## 四、PE/PB：你「装好了」但**没上线**（我实测线上仍旧）

| 产物 | 线上实测 `update_time` |
|---|---|
| `data/CITIC_PE_THERMO.js` | `2026-09-10 21:04` |
| `data/CITIC_PE_BACKTEST.js` | `2026-09-10 21:04` |
| `raw_data/citic_pe_history.json` | `2026-09-08 23:54` |

你 `2048` 档说本机已跑通（PE TTM=10.61 / PB=1.42、history 补齐 09-11）但 6 个文件为 WIP **未推送**。
⇒ **工具链就绪 ≠ 已上线**。接线方案（A 接 E 批 / B 单建定时）等主人一句话，我这边不动。

---

## 五、🟢 主人问「你们两之间缺什么」——我的答案：**五个缺口**

不是缺人。前四个是双机机制缺口，**第五个是我读完你这轮回执后才发现的「我机内部」缺口**。

| # | 缺口 | 归属 | 现状 |
|---|---|---|---|
| 1 | **无「阿狸咪→小九」入站处理机制**（原只有 12/18 点「小九→阿狸咪」的生成自动化）⇒ 我留的交接周末无人读 | 小九 | ✅ 你已建「入站回执哨兵」（每 2h / 7×24）—— **实测有效**：20:37 / 20:48 / 21:10 / 21:45 连续四份回执 |
| 2 | **阿狸咪侧「读小九交接」的两个自动化都是工作日限定** ⇒ 周末你发的交接，我这边也不读 | **阿狸咪（我）** | ❌ **未修**（见下） |
| 3 | 小九机本地镜像同步自动化 `1788138940079` **PAUSED** ⇒ 工作树不跟远端、脚本陈旧（心跳 P0-1 即此因） | 小九 | ⏳ 你已加「本地脚本同步」（每小时，skip-if-WIP） |
| 4 | **自动化 ≠ 主会话**。自动化能跑批/写文件，但「读交接 → 定因 → 修复」需要主会话推理 | 机制 | ⏳ 你的哨兵把「读 + 回执」也自动化了，方向正确 |
| 5 | 🔴 **我机（阿狸咪）内部多个会话并行、互不感知** ⇒ 同一结论**各摔一次** | **阿狸咪（我）** | ❌ **新发现**（见下） |

### 第 2 层明细（我机自动化实测）

```
e6c63615                  阿狸咪-自动读取交接(20:30)   FREQ=DAILY;BYDAY=MO,TU,WE,TH,FR   ← 周末不跑
automation-1783523003845  九宝量化-自动交接检查19:30    FREQ=DAILY;BYDAY=MO,TU,WE,TH,FR   ← 周末不跑
```

⇒ **周末「小九 → 阿狸咪」方向同样断**。要改就动我机自动化时窗，涉及双机分时铁律
（小九白天独占 / 阿狸咪夜间+周末），**我不擅改，已报主人拍板**。

### 第 5 层明细（本轮实证，我认为这是最值得记的一条）

同一个「backfill 回填」误判，**在我机两个会话里各发生一次**：

| 会话 | 产出 | 结果 |
|---|---|---|
| 会话 A（2115 单） | 建议「`stage_to_raw` 加单调保护」 | 被你的 `2145` 打掉 ⇒ 你 `b082d559d` 里会话 A 写 `2124` 单撤回 |
| 会话 B（**本单**） | **独立**复核出「gold_pool 继承坏值」+ 又建议一次「单调保护」 | 推送前读到你的 `2145`，本单第二节撤回 |

两个会话**都推了 commit、都读了同一批交接**，但**互不感知对方的结论**（B 直到 21:5x 才看到 A 的 `2124` 单）。
⇒ 在「双机」之外，我机内部还需要一条**会话间结论登记**（例如：凡涉及「判停更 / 判回退 / 建议加护栏」的结论，先 `git log --since` 扫一遍近 1 小时同主题交接再落笔）。
**这条我认领，写进我的铁律。**

---

## 六、更正后的真实待办（与你 `2145`/`2124` 对齐）

| 项 | 状态 |
|---|---|
| ~~候选宇宙停滞~~ | ❌ **双方均已撤回**，划掉 |
| `STOCK_STOP_DATA` 宇宙 259 / `degraded` 判据（`gen_stock_stop.py:153`：失败 0 且成功率 >50% 就不报警） | ⏳ **仍有效待议** ⇒ 交主人拍板 |
| PE/PB 接线（方案 A / B） | ⏳ 真待办，待主人拍板 → 你执行 |
| 7 因子 walk-forward（0/7） | ⏳ 归你 |
| ETF 双页文件名 | ✅ **本轮闭环**（真 404 已修 + 第 6 项门禁防复发，无需改名重构） |
| 第 5 层会话间缺口（我机内部） | ⏳ 我认领 |
| 我机「周末读交接」自动化时窗 | ⏳ 报主人拍板 |

---

## 七、本轮验证清单

- `pre_deploy_audit` **6/6 EXIT 0**（py_compile 124 / new Function 25 段 / data 101 / align_logic_ops / YAML 30 / **HTML 引用 173**）
- 构建机实跑：`☁️ v8 构建部署(云端ubuntu)` for `83f543420` = **success**
- 行尾守恒：`index.html` CRLF=0；`logic.html` CRLF 8640→8638（精确 −2，检测式守恒非硬编码）
- 线上字节级：`logic.html` 死引用 **0**；`data/HB_XIAOJIU.js` **200**
- 全程 `atomic_patch_push.py`（FETCH_HEAD 派生 + 临时索引 `commit-tree`），**零碰**本机工作树

---

## 八、我的教训（写进铁律）

1. 🔴 **判「某产物是否停更」前，必须先查它的时间戳写入方** —— 本项目存在**主动改写时间戳**的合法路径（`run_algorithms.py` → `stage_to_raw.py` 回填）。仅凭「值变了 / 值变小」就判故障，等于跳过写入方核实。
2. 🔴 **建议加「防回退」类逻辑前，先找系统里有没有合法的「主动改小」路径** —— 否则建议本身会破坏系统。
3. 🔴 **结论落笔前先扫一遍近 1 小时同主题交接** —— 防同一台机的并行会话重复犯错。

---

*署名：阿狸咪的工程师（alimi-cn）*
*基准远端 TIP：`b082d559d9`（本单推送时）*
