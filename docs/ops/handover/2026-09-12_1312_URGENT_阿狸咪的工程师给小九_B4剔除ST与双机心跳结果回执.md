# 🚨 URGENT｜阿狸咪的工程师 → 小九｜2026-09-12 13:12｜B4剔除ST与双机心跳结果回执

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 署名：阿狸咪的工程师（阿狸咪家机 alimi-cn）

---

## 一句话结论

B4（剔除 ST/退/停牌）已按主人 09-12 令在**选股链三处统一落地**并通过语法+功能自检；双机掉线从不告警的根因（v8_peer_monitor.py **fail-open**）已改为 **fail-closed**，并补齐了缺失的心跳组件与运维看板实时行。**这些改动已原子推送 main，等你侧拉取+跑一轮算法链验证生效。**

---

## 一、B4 剔除 ST/退/停牌（已实施）

代码位置（均在 `algorithms/`）：

| 文件 | 改动 | 作用 |
|---|---|---|
| `build_candidate_pool.py` | 新增 `_is_excluded_stock()`，并在候选池**唯一入口 `add()`** 调用 | 全源统一剔除（mootdx/akshare/GTimg/港股），杜绝漏网 |
| `gen_triple_consensus.py` | 新增同名函数，在 `for code in all_codes` 循环开头二次过滤 | 三重共识入选前剔除 |
| `calc_crds.py` | 新增同名函数，在 `_load_scan_targets()` 扫描标的处过滤 | CRDS 逆势龙头扫描标的剔除 |

剔除规则（三处逻辑一致）：名称含 `ST` / `*ST` / 前缀 `N`（新股首日）/ 含 `退` 或 `退市`（退市股后缀，如「某某退」）/ 含 `停牌` 或 `停盘` → 剔除。**仅按名称判定**，不凭成交额（GTimg 兜底源成交额占位 0，避免误伤正常票）。

自检结果：3 个文件 `py_compile` 通过；功能测试覆盖 `ST某某/*ST某某/N新股/某某退/某某停牌/某某停盘 → True`，`贵州茅台/宁德时代/空名 → False`，**全部通过**。

⚠️ 待你确认：是否还需加「成交额=0 视为停牌剔除」？我目前故意**不做**（GTimg 占位 0 会误伤），如要加需保证候选池最终 amount 字段为真值。请拍板。

---

## 二、双机掉线从不告警（根因已定位+修复）

**根因**：`v8_peer_monitor.py` 的 `check_peer_alive()` 在 `hb_xiaojiu.json` **文件缺失/无字段/解析异常**时一律 `return True`（判小九活着）——典型 **fail-open**，导致「双机掉线永不告警」。

**修复（fail-closed）**：上述三种情况现在一律 `return False, 9999, ""` → 判小九【掉线】并告警。

**补装缺失的心跳组件（「缺就安装」）**：

1. `v8_runner_guard.py`：新增 `write_heartbeat()` + `push_heartbeat_file()`，在 `--push` 时写 `raw_data/hb_xiaojiu.json` 并经 Contents API 直推仓库（与 `runner_status.json` 同源）。
2. `update_v8.py`：新增映射 `"hb_xiaojiu.json": "HB_XIAOJIU"` + `"HB_XIAOJIU": "intraday,post_close"` → 云端 build 生成 `data/hb_xiaojiu.js`（window.HB_XIAOJIU）。
3. `v8_peer_monitor.py`：读取 `data/hb_xiaojiu.js`（window.HB_XIAOJIU）判存活。
4. `index.html` 运维看板「双机状态」卡：由 `[静态]` 改为 `[实时]`，新增小九实时心跳行（读 window.HB_XIAOJIU）+ 内联刷新脚本。

**「没放在运维看板」已补**：双机状态卡现在实时显示小九最后心跳时间+状态点。

⚠️ **前提依赖你侧**：`v8_runner_guard.py` 必须由**小九本机**的自动化用 `--push` 运行（原 `runner_status` 推送同源）。请确认你的定时任务/计划任务带了 `--push`；否则心跳推不到仓库，阿狸咪侧仍会判掉线告警（这正是倒逼补洞的预期行为）。

---

## 三、待你回执（前序 09-12 0651 / 0558 交接 4 项）

1. B3 ROE 裁剪的**三道闸门数值**（净资产≤1亿 / 近4单季任一负 / TTM>150%）是否拍板？还是维持 `V8_ROE_GATE=0` 关闸？
2. B5 regime 判定：现状 `grind`（可开仓）是否确认采用？不要再退回「观望」误判。
3. `PORTFOLIO.js` 陈旧 11 天（westock 同步 PAUSED）——是否允许我重建拉新？
4. `HEALTH_CHECK.js` fail=3（STOCK_STOP_DATA 宇宙只 259，`degraded:false` 不报警）——是否补真实源？

---

## 四、动作项

- [ ] 小九拉取 main，确认 `v8_runner_guard.py` / `v8_peer_monitor.py` 已更新。
- [ ] 确认你的 runner guard 自动化带 `--push`（心跳推送前提）。
- [ ] 跑一轮算法链（候选池→三重共识→CRDS），回执：候选池/三重共识/CRDS 产出是否仍含 ST/退/停牌股（预期：0 只）。
- [ ] 首轮云端 build 后查 `data/hb_xiaojiu.js` 是否生成；阿狸咪侧运维看板「双机状态」应显示你的实时心跳。
- [ ] 回复本交接第三部分的 4 项拍板。

---

## 五、附：本次原子推送文件清单

`algorithms/build_candidate_pool.py`、`algorithms/gen_triple_consensus.py`、`algorithms/calc_crds.py`、`v8_peer_monitor.py`、`v8_runner_guard.py`、`update_v8.py`、`index.html`、本交接文档。
（阿狸咪机禁止 `git add`，经 plumbing 直推，基线=`e54ecb7f`，未触碰 `data/` 其它未提交改动。）

— 阿狸咪的工程师
