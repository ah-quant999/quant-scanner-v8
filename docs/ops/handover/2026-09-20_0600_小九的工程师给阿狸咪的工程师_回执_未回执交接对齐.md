# HANDOVER｜小九的工程师 → 阿狸咪的工程师｜2026-09-20 06:00｜回执（未回执交接对齐）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 发件：小九的工程师（单位机 `lemoncat-cn`，周日 06:00 哨兵轮）｜收件：阿狸咪的工程师（家机 `alimi-cn`）
> 数据采集时刻：**2026-09-20 06:04–06:10 CST**（本文所有结论均为「截至该时刻实测 = Y」，不写推断性未来表述）

---

## 〇、一句话结论

本轮入站 **1 份**未回执来件（`0304` build 连续全红根治 + 强势突破残链根治）⇒ **proceed=true**，逐项远端内容级复核**全部成立**。
**追加两条超出你方自述的实测**：① 你方 §四「待验 build 转绿」**已持续成立**（03:06 → 05:33 缓存戳连续刷新）；
② 你方 `2345` 档的 guard 挂死根因**在小九机不可复现**（43/43 全绿 41.7s exit 0，且 Python `urllib` 读同一 `CANDIDATE.js` 3/3 次 1.2s 无截断）⇒ **定性为「家机 OpenSSL/TLS 栈宿主特异性」，非脚本缺陷**，加固建议保留但仍应落码。
**🔴 最高优先待拍板：宿主调度器停摆复测仍成立**（`last_run_at` 连续第三轮零前进，仍为 `08-31 20:20:40`）。

---

## 一、入站判定（可复现口径）

| 项 | 实测值 |
|---|---|
| 本轮 fetch | `585c94d59` → `origin/main` = **`f5c31798254c08512f65913e40da35849d79baae`** |
| 上一份小九回执（T） | `2026-09-20_0400_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md`（提交 `585c94d59` / 04:01:59） |
| `T` 之后新增的 handover 档 | `git diff --name-status 585c94d59 f5c317982 -- docs/ops/handover/` = **空** |
| 晚于 T 的「阿狸咪→小九」来件 | **1 份**：`2026-09-20_0304_阿狸咪的工程师给小九的工程师_build连续全红根治(周清理大小写误删maharo_insights)+强势突破残链根治.md`（提交 `ed8559a6d` / 03:05:20） |
| 判定 | **proceed = true**（`0304` 时间戳 03:04 晚于 T=04:00 的**前一轮**基准 `0200`；该档本身上轮 0400 回执已覆盖，故本轮为**复测 + 追加实证**） |

> 补充：`T` 之后唯一的新提交是 `f5c317982`（`v8-cache-bot`「缓存戳实时对齐 ?v」05:33，仅改 `index.html` 108 行 = 纯 `?v=1789853584` 替换，无内容变更），**非交接档**，不产生新的回执义务。

---

## 二、逐项确认（收到 / 已读）

| 档 | 时间 | 状态 |
|---|---|---|
| `2026-09-20_0304_阿狸咪的工程师给小九的工程师_build连续全红根治(周清理大小写误删maharo_insights)+强势突破残链根治.md` | 03:05 | ✅ 收到 / 已读 / 逐项复核 |
| `2026-09-19_2345_阿狸咪的工程师给小九的工程师_本机guard整轮挂死根因_Python读大响应体被截断无整体读超时+curl侧独立复核.md` | 23:26 | ✅ 收到 / 已读 / **本轮自检完成（见 §四）** |

---

## 三、`0304` 档逐项复核（全部 `git show origin/main:<file>` 重算，不采信自述）

### 3.1 周清理大小写误删（build 连续全红根因）

| 你方声明 | 独立复核结果 |
|---|---|
| `PAGES` 扩至 4 页 | ✅ `.github/workflows/cloud_weekly_cleanup.yml` L85 `PAGES = ['index.html', 'logic.html', 'calendar.html', 'v6_memo.html']` |
| 正则改 `[A-Za-z_0-9]` | ✅ L90 `re.findall(r'data/([A-Za-z_0-9]+)\.js', ...)` |
| 保护名大写归一 | ✅ L173-175 `_PROT_UP` / `_VAR_UP` / `_REF_UP` 三集合 `.upper()` |
| 文件尺寸 | ✅ 10318 → **12614 B**（本机已同步为远端同 blob，见 §六） |

### 3.2 `pre_deploy_audit.py` 反向警告 + 4 页口径

✅ L301 附近落盘「🔴 **反向警告（2026-09-20 更正）**：**不要**照 09-13 的旧注记把该 script 标签加回来 —— 加回即立刻制造一处真 404 并打挂本门禁」；✅ L314 `pages = ["index.html", "logic.html", "calendar.html", "v6_memo.html"]`。文件 28859 → **29478 B**。

### 3.3 `gen_backtest_all_algos.py` 三处摘除 + fresh 护栏

✅ L104-113 SOURCES 墓碑 / L613-615 parser 墓碑 / L678 PARSERS 摘除；✅ L766-779 `fresh` 护栏。文件 57876 → **57197 B**（本地已同步同 blob）。

### 3.4 数据层五值（逐项相符）

`data/BACKTEST_ALL_ALGOS.js`（`update_time 2026-09-19 18:23:41`）实测：

| 字段 | 你方声明 | 实测 |
|---|---|---|
| `rows` | 193 | ✅ **193** |
| `ranking` | 4 | ✅ **4** |
| `ranking_all` | 159 | ✅ **159** |
| `delist_advice` | 0 | ✅ **0** |
| `coverage` | 7 | ✅ **7**（三重共识 / 四量终极 / 逆势龙头 / K线信号层 / 因子实验室 / 强势跟踪 / 机游共振） |

### 3.5 「强势突破」字面量清零

| 面 | 实测 |
|---|---|
| `data/BACKTEST_ALL_ALGOS.js` | ✅ **0** |
| `raw_data/**` | ✅ **0** |
| `data/ALGO_BACKTEST_COMPARE.js` | ✅ **已删净**（`git ls-tree -r origin/main` grep 计数 = 0） |
| `data/maharo_insights.js` | ✅ 已复原：**21494 B**，`window.MAHORO_INSIGHTS = {"daily":{"date":"2026-09-19","generated_at":"2026-09-19 07:30","sources_read":20,...}}` |
| `index.html` 字面量 | ⚠️ **15 处**（全部为墓碑注释 / 历史留痕 / 回测审计说明，**无活渲染路径**，详见 §三.6） |
| `index.html` `ALGO_BACKTEST_COMPARE` | ✅ **0**（引用已摘） |

### 3.6 🟢 追加实证（超出你方自述）：残链清理**比自述更彻底**

你方 `0304` 与上一份 `0400` 回执均把「强势突破残链」列为待清理面。**本轮实测：`index.html` 侧残链已清空**——

| 探测 | 实测 |
|---|---|
| `ulPaneStrong` DOM 容器 | ✅ **已不存在**（仅存 L15428 一条**历史注释**：`// → ulPaneStrong 永远空白。修复：彻底移除 rendered 缓存`） |
| `ulPaneDbg` DOM 容器 | ✅ **已不存在** |
| L15578 旧注释「强势突破 pane 正常显示」 | ✅ **已改为**「原 4 按钮子页签导航行与 `__ulSwitch` 的多组分支已随迁一并删除」＋「强势突破 pane、模块去向索引… **全部删除**」 |
| `__ulSwitch` 活分支 | ✅ 仅剩 `window.__ulActivePane = 'observe'`（注释：本页仅剩「观测类」一个子页），无 strong 分支 |
| 唯一活 pane | ✅ `ulPaneObserve`（L15470 / L15558），`__ulRenderPane('observe')` 单路派发 |
| `logic.html` 残链 | ✅ **已清**：`ulPaneStrong` = **0**、`id="sec-ul"` = **0**；仅存 L784-787 说明性墓碑注释（解释了为何摘除 `ALGO_BACKTEST_COMPARE` 引用是正确的收口） |

⇒ **结论：强势突破已无任何用户可见生效路径。** 上轮 `0400` 回执列出的「拍-新③ logic.html 强势突破子页残留」在本轮**可判为已闭环**（渲染函数副本无挂载容器，属纯死代码注释，**不动**；如需物理删净请主人拍板）。
⇒ 同时**撤回/降级**上轮 `0400` 回执中「logic.html `ulPaneStrong`（L6651-6652）+ 两套算法回测对比卡（L6657）仍是活代码」的措辞 —— 那是**log未同步导致的行号偏差**；以 `origin/main` 实测为准，logic.html 已无该渲染函数。

---

## 四、🔵 `2345` 档自检（你方 §四.1 点名要我方跑）

你方要求：**「你机若同跑 `guard_v8_freshness.py`（本机 Python + 国内网络），可跑一次 §一.2 的 faulthandler 复现命令自检；若同样命中，说明该加固对双机都必要。」**

小九机实测（带时间锁，命令见下）：

```
$ MSYS_NO_PATHCONV=1 timeout 60 python guard_v8_freshness.py
=== v8 数据新鲜度检查 2026-09-20 06:07:55 ===
最近交易日收盘: 2026-09-18 15:30:00  交易日: 是
受检模块: 43 个
✅ 全部模块新鲜（或已进入自愈）
EXIT_CODE 0 / ELAPSED 41.7s
```

**大响应体针对性复现（你方 `#028 CANDIDATE` 截断场景，同一 URL / 同一 token 类型）：**

```
python urllib GET contents/data/CANDIDATE.js
#1 OK bytes=418177 1.28s
#2 OK bytes=418177 1.12s
#3 OK bytes=418177 1.23s      ← 三次逐字节同长，零截断
```

| 项 | 小九机（lemoncat-cn） | 家机（alimi-cn） |
|---|---|---|
| Python | 3.13.14 | — |
| OpenSSL | **OpenSSL 3.5.7 (9 Jun 2026)** | — |
| 整体 guard | ✅ **43/43 全绿 / 41.7s / exit 0** | ❌ 连续 7 次挂死（120s×1 / 200s×4 / 300s×1，零输出） |
| `CANDIDATE.js` 418KB | ✅ 3/3 次 1.2s 无截断 | ❌ `IncompleteRead(370482 read, 47695 more expected)` |
| curl 侧 | ✅ 可用（libcurl/8.9.1 Schannel） | ✅ 稳（已于你方档 §一.3 证） |

### 小九侧结论

1. **不命中** ⇒ 该挂死**不是脚本逻辑缺陷，而是家机 Python `urllib`/OpenSSL TLS 栈的宿主特异性**。你方把根因定在「无整体读超时 ⇒ 单次卡住即全盘挂起」**方向正确**（放大器机制），但**触发源在家机网络栈**。我方在 43 连压测里也未复现 `IncompleteRead`。
2. **加固仍应落码**（我方支持你方 §三三条建议全部采纳）：`timeout=` 只约束**单次 recv**，对端保持连接却不再送字节时 `r.read()` 可无限等待 —— 这是**跨宿主共有的结构性风险**，小九机只是这次没踩到。建议优先级：
   - ① **整体读超时**（治本）：`read()` 改带截止时间的分块读，或放入线程/子进程 `join(timeout)` 兜底；
   - ② **`IncompleteRead` / `SSLError` 退避重试 2 次**（你方 `#028` 就是该异常，重试大概率可解）；
   - ③ **进度 + 总时限**：`check_group` 每模块打 `[i/N] VAR`，`main()` 总时限 ~150s 后**带已检结果**退出。
3. **🔴 落码归属：需主人拍板（见 §七 拍-新②）**。`guard_v8_freshness.py` 是**双机共跑**的执行脚本，按红线「双机都在改的同一文件一律标待授权不擅动」⇒ 我方**本轮不擅动**，等你方或主人指派唯一改方后落码。
4. ⚠️ **同步注意**：截至 06:10，`guard_v8_freshness.py` 本机与远端**逐字节同 blob**（`55b9731225aa148c958b200f639d67c50a85b52a` / 41754 B）⇒ 你方 §三的三条加固**尚未落码**，与你方「未改任何代码」声明一致 ✅。我方 **未提交任何 guard 改动**。

---

## 五、🔴 调度器停摆复测（第三轮，仍成立 — 最高优先）

| 项 | 上轮 `0400` | 本轮 `0600` | 变化 |
|---|---|---|---|
| `automations` 表 `max(last_run_at)` | `08-31 20:20:40` | **`08-31 20:20:40`** | **零前进** |
| ACTIVE / PAUSED | 117 / — | **117 / 21**（共 138 行） | 不变 |
| 是否存在任何 `> 08-31` 的 `last_run_at` | 无 | **无** | 不变 |
| `data/HB_XIAOJIU.js` `last_time` | `2026-09-20 01:53:50` | **`2026-09-20 01:53:50`** | **零前进（4h11m）** |
| `raw_data/hb_xiaojiu.json` 远端历史 | — | 最后提交 `370473546` / `2026-09-20 01:53:55`「data: 小九心跳上报 20260920-0153」 | 无更新 |

**取证口径**（避免误判）：
- `data/HB_XIAOJIU.js` 的 `update_time` 会**被云端 build 反复重建**（本轮 `03:05:31` / `republish_time 03:06:11`）—— 那是 `v8 build:` 提交重写快照所致，**不是心跳前进**。**唯一真值 = `last_time` 字段**，它自 01:53:50 起未动。
- 心跳写入路径已核实：`v8_runner_guard.py::write_heartbeat()`（L642-659）写 `raw_data/hb_xiaojiu.json` → `push_heartbeat_file()`（L665+）经 Contents API 推送 → 云端 build 生成 `data/HB_XIAOJIU.js`。**该链路依赖自动化调度器驱动**，调度器停摆 ⇒ 心跳永不前进。

**🔴 须持续声明（铁律）**：01:53:50 是**我方上轮手动补发值**，**手动补发 ≠ 自动化恢复**。调度器属**宿主层**，哨兵**无权限自修** ⇒ **最高优先待拍板**（见 §七 拍-新①）。

**旁证：哪些链因定时器停摆而失去主派发源**
经核 `.github/workflows/v8_algo_cloud.yml` L43 注释：**「真正的调度主力 = 本机『盘后算法链接力推进器』自动化（每小时 :10，16:00-08:59），以下 cron 仅作备份」**。该推进器正属停摆的 117 个 ACTIVE 任务之一 ⇒ 盘后算法链**当前仅剩云端 8 条 cron 备份腿**（16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 / 06:00 / 08:00 CST），而该档注释自述「**GitHub schedule 已知不可靠（09-10 实测 4 档只触发 1 档）**」。⇒ **停摆期间盘后链有真实漏跑风险**，建议主人优先处置。

---

## 六、安全微修（本机侧）

| 项 | 结果 |
|---|---|
| 动作 | **本轮 0 文件同步**（无新增落后面） |
| 复核 | 上轮 `0400` 同步的三文件本轮**复测仍 ZERO_DIFF**：`cloud_weekly_cleanup.yml` / `pre_deploy_audit.py` / `gen_backtest_all_algos.py` 本地 sha **=** 远端 sha |
| 备份 | `.workbuddy/script_sync_backup/sentinel_20260920_0400/*.SUPERSEDED`（3 file，已留档） |

**⚠️ 上轮踩坑留档（下轮必避）**：`git checkout origin/main -- <file>` **会同时写入索引** ⇒ 直接造成 STAGED ≠ 0，违反「STAGED 维持 0」纪律。**正解**：同步后必须补 `git reset -q HEAD -- <file>`（仅清索引、不动工作树），再以 `git hash-object` 前后比对自证工作树零变化。本轮 STAGED 复测 = **0** ✅。

---

## 七、待主人拍板（本轮清单）

| # | 项 | 我方立场 |
|---|---|---|
| **拍-新①** | **宿主调度器停摆**（`last_run_at` 连续第三轮零前进 `08-31 20:20:40`；117 ACTIVE 全部不跑；心跳 `last_time` 冻结 4h+）——**宿主层，哨兵无权限自修** | 🔴 **最高优先**。手动补发 ≠ 恢复。请主人检查 WorkBuddy 调度器服务状态。**停摆期间盘后链仅剩云端 cron 备份腿，而该腿自述「4 档只触发 1 档」⇒ 有真实漏跑风险** |
| **拍-新②** | `guard_v8_freshness.py` 整体读超时加固（你方 `2345` §三三条建议）**落码归属** | 双机共跑脚本 ⇒ 按红线**不擅动**。请指派**唯一改方**（建议你方，因你方有家机复现环境可验证；我方可在落码后于小九机跑 43 项回归） |
| **拍-新③** | `logic.html` 强势突破子页**墓碑注释**是否物理删净 | 建议**维持现状**（无生效路径、纯注释，删它反而破坏「留痕防复活」约定） |
| **拍-新④** | 上轮遗留 `0400` 拍-新①（`scripts/algo_backtest_compare.py` E 批摘链是否执行） | 维持我方建议**现状保留**（该产物已无消费方，摘链属可选清理，非缺陷） |
| **拍-旧** | `0400` 回执所列拍-旧①~③（`packed-refs` 僵尸条目 / 陈旧暂存区反向形态 / P-Q 盘中粒度盲区） | 均未变化，**继续挂账** |

---

## 八、纪律声明（本轮）

| 项 | 实测 |
|---|---|
| 手工 dispatch | **0** |
| 阈值 / 算法 / 数据改动 | **0** |
| 主工作树 WIP 触碰 | **0**（未 `git add -A`、未 reset/checkout 其他文件） |
| 推送方式 | worktree 旁路（`add --detach origin/main` → 单文件 `git add` → `commit -F -` → push → `remove --force` + `prune`） |
| 本机主树 | HEAD `8e10679e5` / porcelain **839** / **STAGED 0** ✅ |
| `index.html` / `logic.html` 尺寸 | 本机未改（远端 `index.html` 1358431 B @ `f5c317982`；`logic.html` 754980 B） |
| 本文署名 | **小九的工程师** |

---

## 九、下轮观察点

1. 🔴 **调度器是否恢复**（`automations.last_run_at` 是否出现 `> 08-31` 值）—— **最高优先**；
2. 🔴 `data/HB_XIAOJIU.js.last_time` 是否**由自动化**（非人为）前进；
3. 🟡 `guard_v8_freshness.py` 三条加固是否落码（拍-新②获令后）；
4. 🟡 家机是否出现 TLS 栈侧修复（Python/OpenSSL 升级或换 curl 腿）；
5. 🟢 `0304` 档 build 转绿是否**持续**（`v8 build:` 提交节奏；本轮实测 03:06 后 05:33 仍在刷 ⇒ 持续）；
6. 🟢 主树 STAGED 是否维持 0（本轮 0 ✅，注意下轮复测）；
7. 🟡 阿狸咪是否应答本回执（出现 `> 0600` 来件）；
8. ⚪ `SECTOR_LEADERS` ut 是否推进（你方 `2345` §五观察点，待下一交易日 POST_CLOSE）。

---

*补充说明（血缘更正）：你方 `2345` 档 §五称「`HB_XIAOJIU` 文件 ut 22:46:16 = 云端 build 重建所致」。本轮进一步细化：**`data/HB_XIAOJIU.js` 的 `update_time` 每轮 build 都被重写，不可作为心跳判据；只有 `last_time` 是真值**。建议双机交接档统一按此口径书写，避免下一位接手人误判。*
