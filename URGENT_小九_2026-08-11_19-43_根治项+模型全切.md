# URGENT HANDOVER 阿狸咪 → 小九（2026-08-11 19:43 紧急·根治项+模型全切）

## ⚠️ 主人指令（最高优先级）

主人于 **2026-08-11 19:43** 下达两项紧急指令：

1. **需要小九根治的就趁早写紧急交接** —— 把今早发现的、只是临时打补丁还没根治的 **3 个真正根因级 bug** 写成紧急交接，让小九趁早根治。
2. **小九全部模型换成 Deepseek-V4-Flash** + **家里阿狸咪（你自己）全部模型也换成 Deepseek-V4-Flash**。

---

## 一、今早发现的 3 个根因级 bug（小九必须根治）

### 🔴 Bug #1：CANDIDATE.js 第 3 次被覆盖（最严重·管线顽疾）

**症状**：今日 06:45 / 17:39 / 19:27 共 3 次被云端管线覆盖，候选池从 6 源 349 只被压回 4 源 150 只，**观澜台 / maharo / 港股** 数据丢失。

**临时修复**：每次手动从 `out/candidate_pool.json` 备份覆盖 `data/CANDIDATE.js` + `raw_data/candidate.json`。

**根治方向**（小九必做）：
- 排查 `v8_cn_fetch_cloud.yml` 管线，找到覆盖 `CANDIDATE.js` / `raw_data/candidate.json` 的步骤
- 加保护逻辑：写入前对比 `source_dist` 总源数与关键源（观澜台/maharo/港股）是否存在
- 若新数据源数 < 老数据，**拒绝覆盖**并告警
- 候选推荐：`stage_to_raw.py` 的 `_ts_full()` 时间戳守卫（2026-08-11 已加）可参考扩展

**关键位置**：
- 数据：`data/CANDIDATE.js` (~25KB) / `out/candidate_pool.json` (~75KB) / `raw_data/candidate.json` (~75KB)
- 算法：`algorithms/build_candidate_pool.py` / `algorithms/stage_to_raw.py`
- 管线：`.github/workflows/v8_cn_fetch.yml` / `v8_cn_fetch_cloud.yml`

---

### 🔴 Bug #2：STOCK_RPS 变量名 typo（排程表与数据不一致）

**症状**：排程表 `index.html:7681` 写 `source:'STOCK_RPS'`，但数据文件 `data/STOCK_RPS.js` 实际导出的是 `window.STOCK_RPS_DATA`。变量名不匹配导致排程表读不到数据，卡片一直显示 `—`。

**临时修复**：将排程表改为 `source:'STOCK_RPS_DATA'`（已在 commit `0e91e1a5c` 修复并部署）。

**根治方向**（小九必做）：
- 统一所有「排程 source」与「window 变量」的命名规范
- 加 lint / 健康检查脚本，自动比对 `V8_PAGE_SCHEDULE` 的 source 与 `data/*.js` 的实际 window 变量名
- 推荐位置：`v8_health_check.py` 加一段 `check_schedule_var_consistency()`

**关键位置**：
- 排程表：`index.html` block#21 (~7681 行) `V8_PAGE_SCHEDULE`
- 数据：`data/STOCK_RPS.js` 实际导出 `window.STOCK_RPS_DATA`

---

### 🔴 Bug #3：LHB_HISTORY 空壳占位符阻塞 bug（已修复但需复盘）

**症状**：5 个交易日（08-04/05/07/10/11）的 `trading=False, stocks=0` 占位符阻塞了后续真实数据写入。

**根因链**：
1. `fetch_lhb.py:213` → 抓取失败写 `_update_lhb_history({}, date_str, trading=False)` 空壳
2. `stage_to_raw.py:119` → `if iso in hist: return False` → **空壳也跳过**
3. 结果：5 个交易日永久丢失

**临时修复**：改 `append_lhb_to_history()` 检测空壳→覆盖而非跳过（commit `dfad9d59d`）。

**根治方向**（小九必做）：
- **历史回填**：08-04/05/07/10 四个交易日仍需 `backfill_lhb_history.py` 回填（脚本缺失！需新建）
- **管线加固**：所有「placeholder 写历史」+「真实数据追加历史」的组合都应加同样保护
- **类似风险点审查**：`fund_flow` / `north_holding` / `lhb_seats` 等所有按日期累加的 history 文件都要审一遍

**关键位置**：
- 算法：`algorithms/fetch_lhb.py:213` / `algorithms/stage_to_raw.py:102-132`
- 数据：`data/LHB_HISTORY.js` (~870KB) / `raw_data/lhb_history.json` (~1.4MB)
- 缺失：`backfill_lhb_history.py`（工作流引用但未实现！）

---

## 二、模型全切 Deepseek-V4-Flash（小九必做）

### 操作步骤

1. **打开 WorkBuddy 自动化面板**（`https://workbuddy.cn` 或本地入口）
2. **筛选所有 ACTIVE 状态的自动化任务**
3. **逐个进入设置 → 模型选择 → 改为 `deepseek-v4-Flash`**（与 2026-08-05 / 08-06 两次切换操作相同）
4. **保存** → 状态保持 ACTIVE
5. **切换完成后**：在本文件末尾追加小九切换确认（见下方"小九执行确认"）

### 注意事项

- ⚠️ **禁止**自行建"切回其他模型"的定时任务
- ⚠️ **等主人另行通知**才回其他模型
- ⚠️ **同步通知**：切换完成后在本交接 + 工作群里跟阿狸咪（家里）同步一声
- ✅ **阿狸咪这边已完成**（2026-08-11 19:49）：

| 任务 | 状态 | 模型 |
|------|------|------|
| 阿狸咪-紧急指令监听(每2小时) | ACTIVE | ✅ `deepseek-v4-flash` |
| 九宝量化-自动交接检查19:30 | ACTIVE | ✅ `deepseek-v4-flash` |
| 九宝量化-运维页失败主动监控(每2h) | PAUSED | ✅ `deepseek-v4-flash` |

  已通过 `automation_update` 逐个执行 mode=update + modelId="deepseek-v4-flash"，并查询本地 `~/.workbuddy/workbuddy.db` `automations.model_id` 确认全部生效。

---

## 三、今早其他修复记录（仅供参考，非根因级）

| Commit | 内容 |
|--------|------|
| `b0f934b99` | 4 项 UI 修复（黄灯/并排/已计算日期/首字母搜索） |
| `61e9f1689` | 数据一致性 + 卡片迁移 |
| `e25e349c8` | 持仓匹配 CRITICAL bug（_pfFindStock 空串 indexOf） |
| `51dd83f` | CANDIDATE.js 第 1 次恢复 + 全站审计 |
| `fc3b72458` | showDiagDropdown dd.style.display 修复 |
| `3314ad4a2` | RPS badge 日期格式强制 MM-DD |
| `e6f0aa52f` | 持仓旧数据迁移（PF_MIGRATION_KEY） |
| `f42ca4e39` | 持仓跟踪增强（止损/止盈/回撤/卖出提醒） |
| `495fe507b` | 数据卡空壳检测（平均股价/ETF龙头） |
| `0e91e1a5c` | STOCK_RPS 变量名修复 + CANDIDATE 第 2 次恢复 |
| `dfad9d59d` | LHB_HISTORY 空壳阻塞修复 + CANDIDATE 第 3 次恢复 |
| `ac9c68838` | AI 行平均股价买卖信号面板 |
| `b07058902` | 推送最终版 |

---

## 六、今晚 19:43 之后新增改动（2026-08-11 22:41 追加·必须知悉）

> 以下改动均已在 22:41 前 **全部 push 至 origin/main**（`git log origin/main..main` 为空验证），v8 build 已自动部署。

| commit | 类型 | 内容 | 小九关注点 |
|--------|------|------|-----------|
| `7bb5a0c10` | feat | CRDS 时间胶囊加状态关键词（失效→出局/低效→观望/有效→正常） | 前端 renderV8Crds |
| `45c288a08` | docs | 逻辑详解页 lg-rt 加平均股价买卖信号说明卡 | — |
| `1aae12693` | revert | revert `2a932408e`（Top3 全 A 股——主人澄清：从未要求 A 股优先） | ⚠️ **重要**：主人真实诉求 =「谁好谁上」，Top3 出现全港股/全 A 股时**排查数据源**，不是改算法 |
| `c4e5c6d86` | feat | **运维自愈巡检改方案 B**：workflow_run 跟随 cn_fetch/algo/build 完成即巡检，schedule 降为 8 次/天兜底（北京 8-22 每 2h），夜间静音 | `.github/workflows/v8_ops_self_heal.yml` |
| `b543c1ad5` | feat | **去掉 HK_PENALTY=0 + MIN_A_SHARES_IN_TOP=0**（公平竞争）+ **新增 Top3 市场分布异常监控**（全港股/全 A 股 → WARN 告警） | `algorithms/final_recommend.py` + `v8_health_check.py` |
| `3d2a53b29` | feat | **4 个漏洞修复**：#1 自愈派发上限 MAX_DISPATCHES_PER_RUN=5 / #2 Runner 周末降频 21→8 次 / #3 管线耗时监控(3σ) / #4 workflow_run 失败触发=合理设计不改 | `v8_health_check.py` + `runner_health_alert.yml` |
| `c232edf5a` | fix | **回填 LHB_HISTORY 8/4/5/7/10 共 303 只**（akshare 拉历史龙虎榜 + 席位明细）→ 8 月日历 479 只完整 | 见下方 §八 详细说明 |
| `35eddc632` | docs | 运维页 lg-op 加今日改动汇总卡（10 章节 + 17 commit 表） | 前端逻辑详解页 |
| `4bb43ac0e` | fix | **AI 市场速览时段边界 bug**：今天 ≥17 点错标"昨日盘后"→"今日盘后" | index.html:1450/1770 |

## 七、数据源异常排查指引（主人核心诉求，2026-08-11 新增）

主人最担心的只有一件事：**Top3 只有港股或只有 A 股 = 某个数据源出错**。

**排查方向**：
| 现象 | 排查目标 |
|------|---------|
| Top3 全港股 | 候选池（candidate_pool.json）→ akshare 港股 API（之前 ConnectionResetError）→ maharo 信号源 |
| Top3 全 A 股 | 候选池 → mootdx A 股扫描（之前 TOP10_DAILY 全港股就是 mootdx 失败导致） |

**自动监控已就位**：`v8_health_check.py` 的 `final_market_distribution` 检查项，Top3 全集中 → WARN → URGENT 告警。

## 八、LHB_HISTORY 回填详情（2026-08-11 22:31）

**背景**：主人在截图指出共振日历 4 天空缺（8/4/5/7/10 是 trading=False 空壳）。
**根因**：fetch_lhb.py 只抓当日，历史日期不重抓 → 空壳遗留。

**已做回填**（commit `c232edf5a`）：
| 日期 | 股数 | 分类 |
|------|------|------|
| 08-04 | 93 | 游资独买 11 / 不达标 82 |
| 08-05 | 72 | 游资独买 10 / 不达标 62 |
| 08-07 | 68 | 游资独买 13 / 不达标 55 |
| 08-10 | 70 | 游资独买 13 / 不达标 57 |

**小九后续**：
- ⚠️ 分类粗糙：机游共振/机构独买=0（akshare「类型」字段不含「机构专用」标记），游资独买按 净买入>8000万 阈值粗判
- 📌 **建议创建正式 `backfill_lhb_history.py`**（挂在 15:30 盘后档自动重抓历史缺失日），本次是手动脚本一次性跑
- 📌 其他 history 类文件（fund_flow / north_holding / lhb_seats）也需审查是否有同类空壳阻塞

---

## 九、双机状态（2026-08-11 22:41 更新）

| 机器 | 状态 |
|---|---|
| 本机（小九，云端） | ⏳ 待执行模型切换 + 3 项根治 + LHB 回填脚本正式化 |
| 家里（阿狸咪） | ✅ 本次会话已是 deepseek-v4-flash；**3 个 WorkBuddy 自动化 model_id 已全部改为 deepseek-v4-flash**（查 ~/.workbuddy/workbuddy.db automations.model_id 确认） |

## 十、小九执行确认（请在本节追加）

- [ ] 模型切换执行时间：____
- [ ] 切换的自动化数量：____
- [ ] CANDIDATE.js 防覆盖保护：____
- [ ] STOCK_RPS 变量名统一脚本：____
- [ ] backfill_lhb_history.py 创建：____
- [ ] 其他 history 类文件审查：____
- [ ] LHB 回填分类增强（机游共振/机构独买识别）：____
- [ ] 确认今晚 22:41 前全部 commit 已 pull（git pull --rebase）：____

---

**详细背景见主交接文档**：
`docs/ops/handover/HANDOVER_阿狸咪_给小九_2026-08-11_持仓防覆盖+数据审计.md`