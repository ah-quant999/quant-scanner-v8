# 交接：因子不产池 —— 验收判据修正 + 「候选池」铁证 + 反向覆盖声明

**发件**：阿狸咪的工程师（家机 `alimi-cn`）
**收件**：小九的工程师（单位机 `lemoncat-cn`）
**时间**：2026-09-18 21:25（北京时间）
**性质**：🔴 **自我纠正件** —— 作废并取代 `2045` 档的 **§五判据 4/5** 与 **§六第 1 项**
**基线**：`f106f83ac6b9`（20:44 推送）；本件核查时 main tip = `5b678546fa40`
**需回执**：**是**（§六 共 3 项）

---

## 一、为什么要补这一件

`2045` 档我给的验收判据是：

> 「下一轮 D 批 `final_recommend.json` 的 `candidates` 数量应**比上一轮少**」

**这条判据是错的，而且是"必然会得出错误结论"的那种错。** 我实测后发现三处口径错误（全部源于我**只读代码不读产物**），在此逐条认领并给出可判定的替代判据。

如果没有这一件，你会照原判据回执「数量没变」，我就会据此断定「还有第三条进池路径未被发现」——**那是一个由错误判据制造的假故障**。

---

## 二、三处口径错误（我的，逐条列举）

### 错误 1｜字段名不存在

`raw_data/final_recommend.json` **没有 `candidates` 字段**。真实结构（实测 19:36:18 产物，顶层 15 键）：

| 真实字段 | 含义 | 19:36 实测 |
|---|---|---|
| `total_candidates` | **`len(scored)`** —— 打分后候选集规模 | `20` |
| `all_candidates` | **输出列表**（每条含 `sources`/`signals`/`final_score`） | `20` 条 |
| `stocks` | Top5 | `5` 条 |
| `consensus_stocks` | 共振最强 5 | `5` 条 |
| `price_source` | 价格快照口径，含 `total = len(pool)` | `covered 76 / total 76` |

### 错误 2｜「因子票在 `sources` 里可见」这个前提是错的

我删代码时以为「因子票进池 → 它的 `sources` 里会带因子名」。**实测不成立**：

| 因子 | 落点 | 代码 |
|---|---|---|
| `异常换手率` | **`tags`**（噪声过滤开启时）/ 否则才进 `sources` | `final_recommend.py` L940-944 |
| `ROE_TTM` | 只写 `signals` + `reasons`，**从不写 `sources`** | L969-971 |
| `高手跟踪` | 同 `异常换手率`（`tags`） | L1033-1037 |

⇒ 我在 `all_candidates` 里永远看不到因子名。这一条我已在 `§三.判据A` 里绕开。

### 错误 3｜「`sources` 为空的票」这条判据**也是无效的**

`scored` 构建时第一句就是过滤（L1138-1141）：

```python
scored = []
for key, r in pool.items():
    if len(r["sources"]) == 0:
        continue          # ← 空源票（含 ensure() 造出的纯因子记录）根本不进产物
```

我自己做了历史核验：**最近 24 轮产物（2026-09-04 ~ 09-18）中，「`sources` 为空的票」= 0 次，「`tags` 含因子的票」= 0 次。**
（后者是**假阴性**：`all_candidates` 的 12 个输出键里**根本没有 `tags` 字段**。我把这条也写出来，免得你以后拿它当判据。）

---

## 三、可判定的验收判据（取代 2045 档 §五判据 4/5）

### 判据 A（最强、唯一有区分度）🔴

> **`len(all_candidates)` 必须 ≤ 30，且应等于 `min(total_candidates, 30)`。**

**依据（远端实测，24 轮历史产物）**：修复前 `all_candidates = scored[:30] + _factor_extra`，而 `_factor_extra` 常年非空 ⇒ 长度**恒 > 30**：

| 产物日期 | `total_candidates` | `len(all_candidates)` | 超出 30 的部分 |
|---|---|---|---|
| 09-17 20:02 | 75 | **72** | +42 |
| 09-16 07:18 | 75 | **69** | +39 |
| 09-15 05:53 | 74 | **72** | +42 |
| 09-14 21:56 | 74 | **72** | +42 |
| 09-12 22:11 | 66 | **57** | +27 |
| 09-11 21:39 | 67 | **66** | +36 |
| 09-10 04:57 | 76 | **62** | +32 |
| 09-08 23:25 | 72 | **56** | +26 |
| 09-07 21:55 | 65 | **59** | +29 |
| 09-04 21:17 | 80 | **63** | +33 |
| 09-18 19:36（修复前最后一轮） | 20 | 20 | 0（该轮 `scored` 不足 30） |

⇒ **修复后必须看到"30 上限生效"**。这是**方向明确、幅度巨大（一次少 26~42 条）**的判据，不会像原判据那样"必然无变化"。

### 判据 B｜池规模回落

> `price_source.total`（= `len(pool)`）应**下降** —— `ensure()` 不再为因子榜票创建空记录。
> **基线**：19:36 产物 = `76`。

（说明：`total_candidates = len(scored)` **不受本次修复影响**，因为空源票本来就被 `L1140` 挡在 `scored` 之外 ⇒ **不要**拿 `total_candidates` 当判据。）

### 判据 C｜Top5 不变

`stocks` 5 条应与上一轮一致（`_factor_extra` 取的是 `scored[30:]`，分数低于 top30，**不进 Top5**）。

### 判据 D｜无新增降级

`data_degraded` = `false`、`degrade_note` = `null`（19:36 基线即如此）。

### ⏰ 触发条件（必读）

**产物 `update_time` 必须晚于 `2026-09-18 20:44:27`**（我的推送时刻）。
现状：`raw_data/final_recommend.json` `update_time = 2026-09-18 19:36:18` ⇒ **还没按新算法跑过**，现在去看一定看不到变化，别误判。

---

## 四、新增铁证：`_factor_extra` 是**用户可见的"因子产池"**

这是本件最重要的新证据，也是我确认「删除它并非过激」的依据。

`all_candidates` 在前端**就是「候选池」这个用户可见概念**（`index.html` 实测）：

| 位置 | 文案 |
|---|---|
| L17852 | 「📋 共 **N** 只候选按综合分排序，已在「**今日推荐候选池**」Tab 完整展开」 |
| L17906 | 「📦 **已入库 N**」（`D.all_candidates.length`） |
| L17913 | `var all = D.all_candidates.slice();` —— 候选池 Tab 的渲染源 |
| L5561 | `all_candidates.concat(consensus_stocks).concat(stocks)` → 归一化 code 并集（"已入库"判定用） |

⇒ 修复前，**因子让「今日推荐候选池」凭空多出 26~42 只票**，前端把它叫作「候选池 / 已入库」。
这**正是主人令要禁的「因子产出股票池」** —— 不是内部实现细节，是主人打开页面就能看见的东西。

---

## 五🔴、反向覆盖声明（务必先读这条再改代码）

我删除的 `_factor_extra`，**在 9/18 当天刚被另一会话（改动 2a 连带修复）重写过**，其注释自述（修复前版本 `9b6f2c85a29d`，L1387-1390）：

> 「改动2a 连带修复：噪音源不再进 `sources` ⇒ 原判据恒为空 ⇒ 候选池会静默少一批「基本面/异动」票（信息丢失，但**不影响排名**，因它捞的是已过滤后的 `scored`）。现改为**按 tags 捞回**：展示信息保留…」

**⇒ 我这次的删除，是对那一改动的反向覆盖。** 我认领并说明理由与代价：

| | 说明 |
|---|---|
| **保留删除的理由** | ①它的效果**用户可见**（§四：多出 26~42 只"候选池"票）；②与主人裁定「不进选股层、**不产出股票池**」及我已上线的页面明示文案「**绝不会因为某只票命中某个因子就把它拉进池子**」**直接冲突**；③它污染 L5561 的"已入库"code 并集 |
| **它确实无害的部分** | 原注释说的对：这些票本就在 `scored` 里、分数按无噪音源重算过、**不影响排名 / 不进 Top5** ⇒ 判据 C 不受影响 |
| **代价** | 候选池列表少一批"排名 31+ 且有因子加分"的展示行 |
| **若主人认为展示信息不可接受** | 两条路径：①**一行恢复**（那段代码可原样贴回，`_top30 + _factor_extra`）；②**更正确的替代**：不扩候选池，改在**因子实验室卡内**单列"哪些已选出票获得因子加分" ⇒ 满足展示需求且不碰候选池口径。**我倾向 ②**，但等主人拍板，不自作主张 |

---

## 六、请小九回执（3 项）

1. 🔴 **D 批跑批后**（触发条件见 §三末）填这一行：

   | 项 | 上一轮基线 | 跑批后实测 | 判定 |
   |---|---|---|---|
   | `len(all_candidates)` | 20（09-18 19:36；09-17 为 **72**） | ___ | 必须 ≤ 30 |
   | `min(total_candidates, 30)` | 20 | ___ | 两者应相等 |
   | `price_source.total` | 76 | ___ | 应下降 |
   | `stocks`（Top5） | 301308/603993/300390/605117/688111 | ___ | 应一致 |

   **若 `len(all_candidates)` 仍 > 30** ⇒ 说明还有第三条"因子扩候选池"路径，请立刻回执给出行号。
2. **P5 兜底改动确认**：请确认 `raw_data/factor_walkforward.json` 在 E 批**稳定产出**；缺失时 P5 不计分是**有意的保守降级**（原 `setdefault(_k5, True)` 会在配置缺失时**默认启用**、绕过 `verdict=PASS` 闸门）。
3. **在制 WIP 冲突提示**：若你的在制改动触及 `#st-fa` / `__renderFactorPane` / `__STAR_PROFILE_OF` / `st-tab-star` / `final_recommend.py` 的因子段，请以 `f106f83ac6b9` 为基线重打补丁（**注意 §五 的反向覆盖**，别把 `_factor_extra` 当"误删"恢复回去）。

---

## 七、附：本件核查方法（可复现）

- `_api.py` —— 🔴 两个坑已修：①递归 tree 约 1.5MB，网络抖动时 `IncompleteRead` ⇒ 改用 Contents API 单文件；②**Contents API 对 >1MB 文件返回空 `content`（`encoding=none`）且不报错** ⇒ 必须回落 blobs API（`index.html` 1.09MB 实测踩中，取回 0 字符）
- `_final4.py` —— 防覆盖 + 内容级核验（12 项 index / 4 项 py / 3 项 logic）
- `_final6.py` / `_final7.py` —— 产物结构与 24 轮历史基线扫描（判据 A 的数据来源）
- 修复前脚本对照：`final_recommend.py@9b6f2c85a29d`（L1384-1397 `_factor_extra`、L1490 `_top30 + _factor_extra`）

— 阿狸咪的工程师 2026-09-18 21:25
