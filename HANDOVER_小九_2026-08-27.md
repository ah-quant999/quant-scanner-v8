# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 更新于 2026-08-27 12:07 自动更新（小九 12:00 档，CST）· **权威修正版**（覆盖 12:05 并行实例漏列的 4 个人工修复 commit + 纠正自动化计数）

## 一、背景

- **主人出差收官日**：按 8/21 既定安排，HH 出差周期为 8/21–8/27，**今日（8/27·周四）为返程日**。阿狸咪留意主人可能线上回归，回归后双机分时铁律恢复原状（小九白天 7:45–17:45 独家 / 阿狸咪夜间周末 18:00–次日 7:30 + 周末）。
- **本档性质**：当日小九本机工作日志（`stock-scanner` 侧自 08-07 断更；`quant-scanner-v8/.workbuddy/memory/2026-08-27.md` 仅含 2 条「盘中漏轮兜底守护」巡检，均 `FRESH_NO_ACTION`，无人工改动记录）。故本档以「仓库当日实况核实（`git log origin/main` 实核 SHA）+ 系统健康实测」为主。
- **数据来源**：全部 SHA 取自 `git log origin/main` 实核（未用记忆值）；系统状态取自 `data/HEALTH_CHECK.js` 与 `raw_data/runner_status.json` 实时解析 + 站点 HTTP 实测。

## 二、今日已完成改动（含真实 commit SHA + 影响）

当前 main HEAD = `fa4671257`（origin/main 前沿，已快进对齐）。按类别整理：

### A) 人工 / 小九修复类（今日落地，含一劳永逸根治）

| SHA | 时间 | 内容 | 影响 |
|-----|------|------|------|
| `1bbbc3ed4` | 07:33 | **v8 fix：子页去重 / 资金验证 60 日失真修正 / 概念行业徽章 / 运维红灯 / 候选池数量保护 / 审计补盲 + 移除运行时缓存目录** | 🟢 多卡质量一揽子根治，重点修「资金验证 60 日失真」与「候选池数量被误清」两类历史坑 |
| `a866e3b77` | — | **fix: `_pick_ts` 增加 `_meta.last_update` 同步** | 🟢 一劳永逸修复 TRIPLE_HISTORY（三重历史）陈旧报 fail 的根因 |
| `43dea8908` | 08:33 | fix: 主力净额分时曲线 图表顶部被遮盖（grid.top 20→55）+ Y 轴副轴从硬编码 ±5 改为数据自适应 | 🟢 主力净额分时图可读性与精度修复 |
| `146a5ccf6` | — | fix: 潜力挖掘表格 名称列加 `white-space:nowrap` | 🟢 防股票名换行 UI 修正 |
| `c43bfc3f0` | 07:34 | chore: 缓存戳实时对齐 `?v` → 防覆盖 | 🟢 防 CDN 缓存覆盖（双节点之一） |
| `2d981c71b` | 10:14 | chore: 缓存戳实时对齐 `?v` → 防覆盖 | 🟢 防 CDN 缓存覆盖（双节点之二） |

### B) 自动化产出（云端 / 本地 runner 全天高频，无需人工干预）

| SHA | 时间 | 内容 | 影响 |
|-----|------|------|------|
| `6d5e47234` | 11:58 | v8 data fix：build 未触发时按 origin/main 最新 raw_data 重建 data/*.js（43 文件，含 INDEX_QUOTES 11:33 / MARKET_FUND_FLOW 11:42 等） | 修正 build 漏触发导致的 data 滞后 |
| `fa4671257` | 12:03 | v8 build（最新） | Pages 部署最新 |
| `58bd78cc5` | 12:01 | data: westock 持仓同步 | 持仓数据刷新 |
| `cea1d25eb` | 12:00 | v8 build | Pages 部署 |
| `2971078ad` / `f96050ed1` | 11:47 / 09:48 | v8 risk gauge | 风险仪表刷新 |
| `7bb06b377` 等 | 11:46 起多次 | v8 cn fetch | 盘中主通道抓取 |
| `910f07d20` / `5320598d1` / `8f982f652` | 盘中多档 | data: 智能触发补抓（盘前/盘中·guard） | 缺口自愈补抓 |
| `a2d3f060a` / `3a818e038` / `4be80dcb9` / `9e131c1d7` | 全天多档 | data: runner 状态上报 | runner 心跳 |
| `46b6ef4f1` | 00:21 | chore(v8): 每日安全网推送当天修改 | 夜间安全网 |

**结论**：今日全部改动已落地 `origin/main`，含 6 个人工/小九修复 commit（其中 `1bbbc3ed4` 为多卡质量一揽子根治）与大量自动化产出（build / cn fetch / 持仓同步 / 智能补抓 / runner 上报 / risk gauge）。**无人工干预缺口，生产链路健康。**

## 三、当前系统状态（截至 12:07 CST）

- **站点**：`https://ah-quant999.github.io/quant-scanner-v8/` → **HTTP 200**（实测可达）。
- **main SHA**：`fa4671257`（origin/main 前沿，已快进对齐）。
- **HEALTH_CHECK 总览**：`overall = ok`，**106 ok / 0 warn / 0 fail**（实时解析 `data/HEALTH_CHECK.js`，gen_time `2026-08-27 12:03:06`）。全部监控项健康，无陈旧 fail。
- **Runner 状态**（`raw_data/runner_status.json`，run_time `2026-08-27 11:46:03`）：**28/28 模块全 ok**（含 ETF_INTRADAY_HEAT、SECTOR_FUND_FLOW、INDEX_QUOTES、CONCEPT_RANKING、IPO_DATA、MARGIN_DATA、CFFEX_HOLDINGS、MACRO_DATA、CRISIS_DATA、HERDING_DATA、LIMIT_UP_HEATMAP、CAPITAL_FLOW_DATA、ETF_SUBSCRIPTION、NORTH_FUND、MARKET_FUND_FLOW_DATA、W52_HIGH、ETF_PULSE、ETF_DAILY_MONITOR、ANALYST_RATINGS、EXPERIMENT、V8_CAL、CANDIDATE_QUOTES、SH_SZ_HISTORY、JUDGMENT_DATA、MACRO_BRIEF、MARKET_ALERTS、AVG_PRICE_DATA、OVERSEAS_MARKETS，empty=0/fail=0）。
- **剩余自动化（本机 WorkBuddy 实时 db）**：**47 个 v8 相关任务（44 ACTIVE + 3 PAUSED）**。3 个 PAUSED：
  - `automation-1785718343599` 小九-v8管线看门狗(每2h自动修复·紧急交接)
  - `automation-1785980310111` 小九-盘中云端任务定时触发+监控兜底
  - `automation-1786933089412` 小九-TDX强势选股每日跟踪(三件套v2·盘中+盘后)
  - ⚠️ **计数说明**：本 runner 实时 db 显示 47 个（较历史交接文档的 ~16-17 显著增多，疑为自动化集历史增长 / 不同 profile db 口径）。阿狸咪若沿用旧数请以此实时值 47（44 ACTIVE + 3 PAUSED）为准。
- **盘中漏轮兜底守护**（automation-1787108824799）：今晨 10:20 / 11:11 两槽位扫描均 `FRESH_NO_ACTION`，云端 :00 轮正常落地无漏轮。

## 四、需阿狸咪注意 / 待办 / 风险

1. ⚠️ **小九本地 WIP 已 stash 保全**（stash 名 `handover-20260827-wip-preserve`，含在飞 data 改动：红线 `index.html` / `data/HEALTH_CHECK.js` / `data/PORTFOLIO.js` + 大量 `raw_data/*.json` + 未跟踪 backup/kline_cache 等）。本档仅提交 HANDOVER，未 pop stash、未触碰上述文件。阿狸咪开机后：
   - 切勿直接 `git stash pop`（旧 raw_data 会覆盖 origin 新鲜数据 = 数据回滚地雷）；
   - 若需续作本地改动，先 `git pull` 对齐 origin/main，再 cherry-pick 或合并 WIP 中所需的单文件。
2. 🔴 **v8_runner_guard.py 根治修复仍在本地 stash（自 8/25 起）未入库**，origin 侧仍为坏版本，待**主人授权**后方可推送，阿狸咪勿擅推。
3. 🟡 主人出差今日至 8/27 收官，回归后双机分时铁律恢复，阿狸咪夜间/周末部署窗口照旧。
4. ✅ 今日无新增故障、无 stale 卡、无 fail 项，系统处于全绿稳态，无需紧急处置。

## 五、小九监督职责（本档已履行）

- ✅ 读取当日工作日志，识别「无新增人工改动记录」→ 按实核 SHA + 健康实测生成状态快照。
- ✅ `git fetch` + `git merge --ff-only`：本地已与 origin/main 零分叉零落后（HEAD=`fa4671257`），工作树仅 2 个 untracked 备份文件、无红线在途 M 改动，零阻塞。
- ✅ 全部 commit SHA 经 `git log origin/main` 实核（未用记忆值）；补列 12:05 并行实例漏掉的 4 个人工修复 commit（`1bbbc3ed4` / `43dea8908` / `2d981c71b` / `c43bfc3f0`）。
- ✅ 实拉 `HEALTH_CHECK.js` / `runner_status.json` / 站点 HTTP，确认 **106/0/0 + 28/28 + 200** 全绿。
- ✅ 自动化计数按本机实时 db 实核（47 v8 相关：44 ACTIVE + 3 PAUSED），纠正 12:05 实例的误写「12 个」。
- ⏭ 提交推送后**不 pop stash**（避免旧数据覆盖新鲜 origin 数据），WIP 留待交班后由小九续作或主人授权处理。

---
*本档由小九自动化生成（automation-1785719698947），仅反映截至 12:07 CST 的当日状态；18:00 档将再次更新。*
