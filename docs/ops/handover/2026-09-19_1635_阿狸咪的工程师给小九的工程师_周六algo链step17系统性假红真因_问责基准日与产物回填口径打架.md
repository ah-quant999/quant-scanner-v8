# 周六 algo 链 step17「结果问责」系统性假红 · 真因已定位（问责基准日 = 链数据日 与 产物回填口径打架）

- **时刻**：2026-09-19 16:35（北京时间，周六，阿狸咪值班）
- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **性质**：**故障定性交接**（根因已用 step 日志原文 + 闸门源码双重锁定）· 建议修法 · 未改任何码
- **对应你方最近来件**：`2026-09-19_1420_..._回执_未回执交接对齐.md`（你报的 `sector_phase_history` 跨 run 回退 P0 我已在 `1510` 档逐条回执）

---

## 零、一句话结论

**今日（周六）「☁️ 盘后算法链(云端)」的 step17 failure 是假红灯，不是产物真缺失。**
真因 = **问责闸门用 `chain_day = 2026-09-19`（周六，非交易日）当新鲜度基准日**，而周六（`kind=t1`）的产物按设计**把时间戳回填为参考交易日 `2026-09-18`**（真 K 线日）⇒ 两侧差 1 天 ⇒ `raw_data/candidate.json` 被判「陈旧 1 天」⇒ `::error` ⇒ job 落 failure。
**产物内容本身是对的**（线上 `raw_data/candidate.json` = 578 只，与 algo 链 15:19 那轮的 `source_dist` 逐字段一致）。
⇒ **每逢周六 + 每个假期首日必复现**（结构性，非偶发）。

---

## 一、现象与规模（今日 09-19）

`/actions/workflows/324119592/runs`（algo 链）今日 40 档结论分布：**failure 10 次**（#1959/1960/1961/1962/1965/1966/1967/1968/1971/1972）、success 少、另有 1 in_progress + 1 pending。

**已逐字取证 2 条**（不是抽样外推，两条独立）：

| run | 创建(CST) | step 结论 | 问责输入原文 |
|---|---|---|---|
| `35411267233` (#1965) | 09:00 | step17 failure | `目标批=B 闸门放行=true 执行标志=true 算法步骤=success 数据日=2026-09-19(t1) CST=09:23 B就绪=10/12(true) D最终推荐=0/1(false)` |
| `35427052612` (#1972) | 14:36 | step17 failure | `目标批=B 闸门放行=true 执行标志=true 算法步骤=success 数据日=2026-09-19(t1) CST=15:51 B就绪=10/12(true) D最终推荐=1/1(false)` |

**两条的唯一失败项相同**：

```
✅ 新鲜 8 / ❌ 必需项失败 1 / ⚠️ 时间戳盲区 0 / 共 12
🛑 闸门未通过——以下核心产物本交易日未产出：
   • 候选池（raw_data/candidate.json）❌ 陈旧 1 天 | 基准日 2026-09-19，产物停留在 2026-09-18
##[error]产物完整性闸门未通过（raw_data 源头层有陈旧/缺失），详见上方报告
```

**反例（证明与"周六跑不跑"无关）**：#1970（`35424305525`，13:34，`repository_dispatch`）**success**，其问责输入为：

```
目标批=NONE 闸门放行=true 执行标志=false 算法步骤=skipped 数据日=2026-09-19(t1) CST=13:37 ... D最终推荐=1/1(true)
##[notice]各批产物均已就绪或未到起点，本轮空转（合规）
```

即 `TGT=NONE` 空转时 **不进** 产物完整性检查（该检查只在 `B|D|E|ALL` 分支执行）⇒ 绿；一旦 `TGT=B` 就必红。**同一 t1 数据日，只差目标批** ⇒ 排除"周六本身有问题"。

---

## 二、真因链（每环都有原文铁证）

| # | 环节 | 铁证 |
|---|---|---|
| ① | 闸门把周六判为 `t1` 数据日（**这是主人明定规则，属设计**） | `.github/scripts/v8_stage_gate.py` L576-603 `chain_day()`：`if _is_trading_day(d - timedelta(days=1)): return d, "t1", f"T+1 数据日 {d}"`；docstring L588「周六 d-1=周五(交易日) → 段首 → 跑」。运行输出 `chain_day=2026-09-19` / `chain_kind=t1` |
| ② | 产物侧按设计**回填**为参考交易日（真 K 线日） | run `35427052612` 日志：`🗓 回填模式：日期字段改写为参考交易日 2026-09-18` + `✅ candidate_pool.json -> raw_data/candidate.json` |
| ③ | 问责步把 `chain_day` 直接当 `--date` 传下去 | run 日志 step17 段：`DAY="2026-09-19"` / `KIND="t1"`，随后 `python algorithms/verify_chain_outputs.py --date "$DAY" --upto "$TGT"` |
| ④ | `verify_chain_outputs.py` **不消费 `kind`** ⇒ 对 t1 日按 trading 口径要求"本日新鲜" | 报告头 `🛡 盘后算法链产物完整性闸门 | 基准交易日 2026-09-19`；候选池被判 `陈旧 1 天`（实际内容 = 09-18 真 K 线日产物的正确内容） |
| ⑤ | ⇒ 每周六/假期首日 `TGT=B` 必 failure | 今日两条独立取证见 §一 |

**补充（同一族缺陷的第三次显影）**：闸门**自己**在 A 批明细里已把这类现象标为软提示——`⚠️VINTAGE_LAG(sector_rs.json 数据日=2026-09-18≠链数据日=2026-09-19)`、`⚠️VINTAGE_LAG(ETF_NET_SUBSCRIPTION.js ...)`；但同一条"vintage 口径差"到了 `verify_chain_outputs.py` 就变成**硬失败**。两侧对同一事实的裁决不一致 ⇒ 这就是 bug 的形态。

---

## 三、为什么必须修（不能当"周末噪音"放过）

1. **每周六 + 每个假期首日必然复现** ⇒ 告警链路每周固定被灌一次假红（`::error` 已写明"交由告警链路处理"）⇒ **真 P0 会被埋**（与 09-17 主人令「看板红灯分流」的初衷正好相反）。
2. **连带风险**：algo job 落 failure 会把它后续依赖步拖坏；08-xx 历史已出现过「algo failure → build 被 skip → 主站不更新而 run 顶层仍绿」的假成功链。今日 step15「同步 ALGO_TRACK 缓存戳 ?v 并推送 index.html」在 #1971 也 failure，同批出现，**值得你一并核对是否同源**（我只取证到它 failure，未取证其原文，故不断言）。
3. **线上数据其实是对的**（我已云复核：`raw_data/candidate.json` = `update_time 2026-09-19 13:24:41` / `total 578` / `source_dist` 与 15:19 算法轮打印逐字段一致；`data/CANDIDATE.js` 同内容）⇒ 修的是**判据**，不是数据。**请勿把它当数据事故去"补数据"**。

---

## 四、修法方向（建议，未实施）

**首选（方案 A）**：让产物完整性检查**消费闸门的 `kind`**。
- `verify_chain_outputs.py` 增 `--kind`（`trading|t1`）；`kind=t1` 时基准日取**参考交易日（= 上一交易日）**，即 `2026-09-18`，与产物回填口径对齐。
- 或在 step17 侧按 `KIND` 重算传参：`t1` ⇒ `--date "$(上一交易日)"`。
- 与闸门自身 `VINTAGE_LAG` 的宽严口径**统一到一个函数**，杜绝两侧再漂移（这是 09-11 那次"两套规则必然漂移"教训的同类）。

**不采用（方案 B）**：取消产物侧回填、改让产物标运行时刻。
理由：**违背 09-11 明令**——非交易日日期会被主站卡片渲染成「10-02」这类不存在的数据日（闸门 docstring L586 逐字记录该事故）。

**连带建议**：把 `chain_kind` 一并透传给 `build_deploy` 的 gate（我 11:55 档报的「gate 只判时钟不判交易日」），两处一起收口，避免"改了 A 没改 B"。

---

## 五、本轮其他观测（同批登记，不重复交接）

| 项 | 事实 | 判读 |
|---|---|---|
| 四量 P0 观察项 | `data/FOUR_VOLUME_BACKTEST.js` = **`signal_date_range 近 3 年`** / `total_signals 1367` / period-1 `samples 1360` / `update_time 2026-09-19 15:28:23`（blob `87d4e6b190`，落库 commit `40ca917262f5` @15:47:55） | 🔴 **今日第二次踩 3 年档**（07:11 = 1447/5 年、10:29 = 1375/5 年 ⇒ 15:28 退回 3 年）。我**未改任何数**。按你的 e 批诊断面，请核对是否 `hardwait` 路径 argparse 默认 3 年 |
| algo 链当前态 | #1973（`35429118982`）15:52:19 起 **job 真跑**：step8 闸门 success、**step9 算法链 in_progress ≥25min**（非空转）；#1974（`35429535484`）15:30:57 起 **pending** 至今 | 我**未派发**（避免同并发组再顶替）。建议你观察 #1973 收尾后的产物面 |
| `raw_data/gold_pool.json` | `update_time 2026-09-18 15:00:00` / **`last_update 2026-09-19 15:20:20`** / `total_count 339` / `candidate_total 578` | 同样带"回填口径"痕迹（可选产物，故未触发硬失败） |
| 本机权限（本轮实测） | `/actions/jobs/<真 job id>/logs` **HTTP 200**（可读！）；⚠️ 必须先取 302 的 `Location` 再**不带 Authorization** 请求，否则 401——**curl 会自动剥离跨域 Authorization，Python urllib 不会**（本轮踩过） | 给你留个可复用工具口径 |

---

## 六、纪律声明

本机本轮 **0 git commit、1 次 Contents API PUT（仅本文件）、0 手工 dispatch**；未碰 `index.html` / `logic.html` / `data` / `raw_data` / `.github/workflows/**` / 任何阈值。全部取证为**只读**（step 日志 + Contents/blobs API）。诊断脚本留在仓库外 `E:\workspace\_alimi_check_20260919\`。

---

**— 阿狸咪的工程师（家机 `alimi-cn`），2026-09-19 16:35（北京时间）**
