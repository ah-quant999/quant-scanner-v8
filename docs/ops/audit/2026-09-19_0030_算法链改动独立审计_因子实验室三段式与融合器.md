# 独立审计 · 2026-09-18 算法链改动（因子实验室三段式 + 融合器）

> 审计人：阿狸咪的工程师（家机 `alimi-cn`）
> 审计时刻：2026-09-19 00:30（北京时间）
> 审计基线：main tip `a3993a13836b`（2026-09-18 23:2x 实取）
> 审计对象：2026-09-18 全部触及算法链的 4 个提交 + 双页 UI 改动
> 方法：**三重验证** —— ① 静态审计（引用完整性 / AST 作用域 / 语法编译）② **沙箱实跑**（真代码 + 真输入）③ **对照实验**（夹具注入证明因果）

---

## 零、一句话结论

**代码逻辑没问题** —— 静态审计全过、沙箱实跑 `EXIT=0`、对照实验证明「bug 真实存在 + 修复真实生效 + 开关真实可用」；
但查出 **1 个结构性盲点（P1）+ 1 处口径矛盾（P2）**，并澄清「D 批今晚为何没更新」= **闸门合规空转**（非故障）。

---

## 一、审计范围（今日触及算法链的提交）

| 时刻 | commit | 触及 | 性质 |
|---|---|---|---|
| 19:45 | `c3a152ca63` | `v8_health_check.py` | 黄灯灰灯一劳永逸 |
| 20:12 | `dedeebbd7c` | `scripts/fetch_sector_leaders.py` | 启动板块静默丢失根治 |
| **20:44** | `f106f83ac6` | **`final_recommend.py` + `generate_top10.py`** | 因子=加分项非选股策略 + **P5 兜底绕闸门修复** |
| **22:34** | `f17edd67df` | **`final_recommend.py` + `index.html` + `logic.html`** | 因子实验室卡三段式重构 |

---

## 二、静态审计（对象：tip 的 `algorithms/final_recommend.py`，99,868 B / 1,707 行）

| # | 检查项 | 方法 | 结果 |
|---|---|---|---|
| 1 | 被删符号是否仍有活引用 | 全文扫非注释行 | ✅ `NOISE_SOURCES`/`FUSION_NEG_ALPHA`/`V8_ROE_DEMOTE`/`_ROE_OLD_SCALE`/`_ROE_NEW_SCALE`/`_roe_score` **全部 0 引用**（否则运行时 `NameError` 打死整条链） |
| 2 | 语法编译 | `py_compile` | ✅ `rc=0` |
| 3 | **符号作用域**（新增 4 个） | **AST 定位每个 Name 的写/读所属函数** | ✅ 全部读写**同在 `main()`**（`_factor_in_pool` 仅 L735–744，是纯成员判断），**无 `UnboundLocalError`**；`_WEAK_PENALTY_ON` 为模块级全局，读出正常 |
| 4 | `_factor_chain_meta` 是否所有路径都赋值 | 读 if/else 结构 | ✅ `if`（L1053）/ `else`（L1112）**双分支穷尽** |
| 5 | `factor_trace` 硬索引安全 | 核 `scored` 元素形状 | ✅ `x["key"]/["name"]/["final_score"]/["resonance"]/["sources"]` 均必有（`scored.append({**r, "key":…})`，`r` 由 `ensure()` 建含 `name/sources`） |
| 6 | `_weak` bug 断言的**前提**是否成立 | 读 `name_utils.norm_code` | ✅ **成立**：`norm_code` 只 `replace` sh/sz/bj/hk/_ **不剥点** ⇒ `norm_code("sh.600479") == ".600479"`，而 pool key = `norm_code(code).lstrip(".") == "600479"` ⇒ 原写法 `if key in _weak` **恒 False** |
| 7 | 双页 inline script 语法 | node `--check` 逐段 | ✅ index 28 段 + logic 6 段 = **34 段，FAIL=0** |
| 8 | 死代码残留性质 | 上下文判定 | ✅ `__flPopup`/`🟢可关注`/`转正判据`/`renderAlgoCard` 等在**index 中仅存于 `//` 与 `/* */` 墓碑注释内**，非活代码 |

### 2.1 一处需澄清的「断言前提」陷阱（给后续接手方）

交接件正文把 bottom 榜写作 `['000560','003040',…]`（**纯数字**）。但`data/FACTOR_LAB.js` 实测 **70/70 个 code 全部带前缀带点**（`sh.600479` / `sz.300413`）。

⚠️ 这个展示差异会误导：**若按"纯数字"去理解，会得出「原代码没 bug」的错误结论**（本次审计的第一个对照实验就因此翻车，重做后结论反转）。
⇒ 判 `_weak` 是否失配，**必须看 FACTOR_LAB 原始格式**，不能看交接件里剥过点的展示。

---

## 三、沙箱实跑（真代码 + 真输入）

**方法**：从 tip 拉 23 个依赖（9 个 `data/*.js` + 12 个 `raw_data/*.json` + `fundamental_helper.py` + `algorithms/utils/time_gate.py`）到隔离沙箱，跑 tip 版 `final_recommend.py`。**不碰生产仓库**。

| 项 | 结果 |
|---|---|
| 退出码 | **`EXIT=0`** ✅ |
| STDERR | **空** ✅ |
| 产出 | `top3=2 total_candidates=20`，写出 json + js + history 三份 |
| `FACTOR_LAB.js` data_date | `2026-09-18`（真实当日，**无需夹具**即走新代码 `if` 分支）|

### 3.1 判据 7 达成（交接件标「待下一轮 D 批」，本轮**已代验**）

| 产品字段 | 结果 |
|---|---|
| `factor_chain` | ✅ 存在（`integrated` 3 项 + `gate` + `evidence_note` + `dedup_note`）|
| `factor_trace` | ✅ 存在（**list**，4 条，含 `code/name/final_score/resonance/sources/actions/adj_total`）|
| `all_candidates[0].factor_actions` | ✅ 存在 |

**实测命中**（证明新逻辑真在工作，不是空壳）：

| 因子 | role | scored | adj | **n_hit** | evidence |
|---|---|---|---|---|---|
| 放量弱势 | 扣分 | True | −0.5 | **0** | 未回测 |
| 异常换手率 | 仅展示 | False | 0.0 | **0** | 薄样本(9 信号日/85 命中) |
| ROE_TTM | 仅展示 | False | 0.0 | **4** | 薄样本(9 信号日/89 命中) |

⇒ `ROE_TTM` 命中 4 只 → `factor_trace` 恰好 4 条、`adj_total=0.0`，**与「仅展示不计分」的设计完全一致**；`放量弱势 n_hit=0` 与交接件「bottom ∩ pool = 0」断言吻合。

---

## 四、对照实验（证明因果，非仅"跑通"）

交接件的核心主张是「**修好了 `_weak` 格式 bug**」，但**从无人证明修好后真的能触发** —— 这正是它自己批评的「注释说有、实际没有」。
故用**夹具注入**（把池内票 `603993` 以**真实格式 `sh.603993`** 塞进 `FACTOR_LAB.abnormal_turnover.bottom`）做三组对照：

| 实验 | 源码版本 | 夹具 | `n_hit` | `trace.adj_total` | `signals` 含「放量弱势」 | 判定 |
|---|---|---|---|---|---|---|
| **E1** | tip 修复版 | `sh.603993` | **1** | **−0.5** | True | ✅ **修复真实生效** |
| **E2** | 回退版（去掉 `.lstrip('.')`） | 同一夹具 | **0** | **0.0** | **False** | ✅ **bug 真实存在**（复现「29 轮 0 命中」机制） |
| **E3** | tip 修复版 + `V8_FACTOR_WEAK_PENALTY=0` | 同一夹具 | **1** | **0.0** | True | ✅ **开关真实可用**（标签照展示、不减分）|

⇒ **判据 8、判据 9 达成**；且证明「修复」不是装饰性改动。

---

## 五、其余改动审计

| 对象 | 结论 |
|---|---|
| `generate_top10.py`：`P5_ON.setdefault(_k5, True)` → **`False`** | ✅ **方向正确**：堵住「`factor_walkforward.json` 缺失/解析失败时**默认启用** P5 因子 = 未验证就给分」的静默假成功，符合「宁缺毋假」。⚠️ 副作用见 §6.3 |
| 小九 `window.v8SectorFlow`（L9301） | ✅ **逻辑自洽**：缓存键 `update_time`（数据更新即重建）、`sectors_in` 优先、窄口径（非宽基）优先且 `minKeep` 兜底、按净额**绝对值**降序、`limit=0` 返全量。**幂等**（`window.v8SectorFlow = window.v8SectorFlow \|\| (…)`）+ 4 个调用点全部 `? … : []` **空值守卫** |
| 线上指纹交叉核验 | ✅ index `v8SectorFlow`=**16** / logic=**9**，与小九交接件 v3 所称**逐字一致** |
| `logic.html` 融合器升级补段（19:44 档遗留 #3） | ✅ **已完成**（20:04 `30f9adb68c`）：`融合器逻辑升级`=1、`边际` 从 0→**5**、`资格门`=2、`动态 edge`=2 |

---

## 六、发现的问题

### 6.1 🔴 P1 · 结构性盲点：改了算法真源，闸门却「永不重跑」

**现象**：`raw_data/final_recommend.json` 的 `update_time` 停在 **19:36**，而 20:44 / 22:34 两次改动了 `final_recommend.py`。

**取证**（`v8_algo_cloud.yml` 22:27 run `35356355660` 闸门日志）：
```
target_stage=NONE
stage_ok=true
proceed=false
chain_kind=trading
cst_hour=22
reason=四批产物均已就绪 -> 空转（合规，真成功）
ready_A=13/13   ready_B=11/12   ready_D=1/1   ready_E=3/3
```
⇒ 闸门判「D 批产物已就绪」→ 算法步 `if: proceed == 'true'` 为假 → **「🧮 运行盘后算法链」及其后全部步骤 `skipped`**；而「🛡 结果问责」是 `if: always()` → **报 success**。

**这不是闸门故障，是合规空转**（产物确实存在）。但暴露一个**盲点**：
> 闸门就绪判据基于「**产物存在且日期新鲜**」，**不感知「源码已变更」** ⇒ 算法真源代码改动后，只要当日产物已在，**该日不会再跑**，新代码要等次日自然过期才生效。

**影响**：`alpha_select`（改动12）、`factor_chain`/`factor_trace`（22:34）等新逻辑，**当晚均未在生产落地**；若当日恰有改动引入运行时错误，**要到次日才会暴露**（而次日暴露时的"旧产物"会掩盖归因）。

**建议**（不擅自改 workflow —— PAT 无 `workflow` scope）：给闸门加一条**源码指纹短路**：把 `algorithms/*.py` 的 blob sha 摘要写入产物 meta，闸门比对「产物记录的真源指纹 vs 当前源码指纹」，**不一致 ⇒ 强制重跑该批**。或最简：派发时带 `force_run=true`。

### 6.2 ⚠️ P2 · 同一产物内两处口径矛盾（异常换手率）

| 字段 | 位置 | `V8_FUSION_NOISE_FILTER=1`（默认） | `=0`（回退态） |
|---|---|---|---|
| `factor_actions[]. 异常换手率` | L963–971 | `scored=False, adj=0.0` | **`scored=True, adj=round(sc,2)`** |
| `factor_chain.integrated[abn]` | L1078 附近 | `scored=False, adj=0.0` | **写死 `scored=False, adj=0.0`** |

⇒ 回退态下，前端「当日加减分排列」的 `adj_total` 会显示非 0，而「已接入因子」表仍显示 **0 分** —— **同一产物两个字段给出互相矛盾的口径**，恰恰违背主人要这段的初衷（「才会知道到底逻辑有没有出错」）。
**默认态无影响**（两处一致为 0）。

### 6.3 📌 需登记的行为变更（非缺陷）

1. **`_weak` 修复后**：「放量弱势」从「**永不匹配**」变为「**可匹配**」。实测今日交集 0 ⇒ 短期无行为变化；但**若某日候选池出现 bottom 榜票，该票将被扣 0.5 分**，直接改变排序。⇒ 交接件已诚实登记待办（bottom 榜 walk-forward 回测），**在出显著负 edge 前不应加权**。
2. **`P5_ON` 默认改 `False` 后**：若 `factor_walkforward.json` 缺失，P5 两个 K 线因子（amt60/turntrend，+6/+3）将**静默不加分** ⇒ TOP10 质量下降但**无告警**。属**有意保守**，但需确认该文件在 E 批稳定产出（否则是"沉默的降级"）。

### 6.4 运维观察（非代码问题）

- **21:08 `35348504730` run = `failure`，失败点在「Set up job」**（runner 环境准备阶段），**与算法代码无关**。
- `raw_data/algo_chain_report.json` **陈旧至 2026-09-10 06:57**（且仍列已下架的「全站精选TOP10」），**不能作为今晚链路的判定依据** —— 判链路死活须用 `algo_heartbeat.json`（今晚 `update_time=20:49:24, ok=12, fail=1`）+ workflow run 日志。

---

## 七、遗留清单状态（19:44 档「六、遗留待办」逐项）

| # | 项 | 状态 |
|---|---|---|
| 3 | logic.html 补段（CRLF） | ✅ **已完成**（20:04） |
| **9** | **今晚监控：D 批首跑新代码 → 查 `alpha_select`** | ⚠️ **未达成**（闸门空转，见 §6.1）；**但已由本轮沙箱实跑 + 判据 7 代验** |
| 2 | #221 扩样复核（63 交易日 × 11 源 edge） | ⏳ **未做**（大工程，需专门排期） |
| 1 | 改动13 择时闸门升独立加权项 | ⏸ 悬停（等 #221 结论，勿踩「按短样本调权重」红线）|
| 4/5/6/7/8 | 改动 2b/3/4/6/7 | ⏳ 待排期（改动3 依赖改动6）|

---

## 八、本地可复现判据

```bash
# 1) 被删死代码零活引用（排除注释）
grep -n "NOISE_SOURCES\|FUSION_NEG_ALPHA\|V8_ROE_DEMOTE\|_roe_score(\|_ROE_OLD_SCALE\|_ROE_NEW_SCALE" \
  algorithms/final_recommend.py | grep -v "^\s*[0-9]*:\s*#"        # 期望：无输出

# 2) _weak 已修（与 pool key 同口径）
grep -n "norm_code(x.get(\"code\")).lstrip" algorithms/final_recommend.py   # 期望：命中 1 行

# 3) 开关存在（3 处：定义 / factor_actions / weak_penalty）
grep -c "_WEAK_PENALTY_ON" algorithms/final_recommend.py                     # 期望：6

# 4) FACTOR_LAB 的 code 真实格式（判 _weak 失配的前提！）
python -c "import re;t=open('data/FACTOR_LAB.js',encoding='utf-8').read();c=re.findall(r'\"code\":\"([^\"]+)\"',t);print('带点比例=%.1f%%'%(100*sum('.' in x for x in c)/len(c)))"
#   期望：100.0%  ← 若为 0%，则「原代码无 bug」的相反结论才成立

# 5) 行尾铁律
python -c "for f in ['index.html','logic.html']:b=open(f,'rb').read();print(f,'CRLF',b.count(b'\r\n'),'bareLF',b.count(b'\n')-b.count(b'\r\n'))"
#   期望：index.html CRLF=0；logic.html CRLF>0 且 bareLF=0
```

---

## 九、结论与建议动作

| 项 | 判定 |
|---|---|
| 今日算法链改动的**逻辑正确性** | ✅ **通过**（三重验证：静态 / 实跑 / 对照实验）|
| 是否踩「未来函数 / 幸存者偏差 / 按短样本调权重」红线 | ✅ **未踩**（`_weak` **不改数值**只加开关；`P5` 只收紧默认值；薄样本一律不计分）|
| P1 闸门盲点 | 🔴 **建议补「源码指纹短路」**（需有 `workflow` scope 方应用）|
| P2 口径矛盾 | ⚠️ 建议把 `factor_chain.integrated[abn]` 的 `scored/adj` 改为**读同一变量**而非写死 |
| 行为变更 | 📌 `_weak` 修复 + `P5_ON=False` 两项须**在交接档显式登记**（避免后续"为什么推荐变了"无从归因）|

---

**本报告仅供研究参考，不构成个人投资建议。**

**署名**：阿狸咪的工程师 · 2026-09-19 00:30（北京时间）
**基线**：main tip `a3993a13836b`
**方法论**：离线沙箱实跑（不碰生产）+ 夹具对照实验（证因果，非仅"跑通"）
