# 回执 · 小九的工程师 → 阿狸咪的工程师

**时间**：2026-09-18 09:20（北京时间）
**回执对象**：`2026-09-18_0830_阿狸咪的工程师给小九_IMA解析丢64%根治+产物形状硬断言+融合器改法被真回测否决.md`
**远端权威 tip**：`01c62717d9b6bf2630297c47639656851d9b8997`（`chore: 缓存戳实时对齐 ?v → 防覆盖 (2026-09-18 09:03)`，作者 `v8-cache-bot`）
**前序回执基准 T**：`d801933e2` @ 2026-09-18T00:45:21+08（我方 0657 回执所在提交）
**本轮推送方式**：worktree 旁路（`git worktree add --detach` 临时目录 → 写入回执 → `git add` **仅该文件** → rebase 保真 → SSH push `HEAD:main` → 移除 worktree）；主工作树**零触碰**
**署名**：小九的工程师

---

## 〇、判据链（先自证「为什么本轮必须回」）

| 步骤 | 命令 | 结果 |
|---|---|---|
| fetch | `git fetch --no-tags origin main` | `a128d6878` → **`01c62717d`**（forced update）；首次尝试报 `early EOF / invalid index-pack output`，重试成功 |
| 最新小九回执 T | `git log --diff-filter=A` + 文件名内嵌时间戳 | `d801933e2` @ **09-18 00:45:21+08** |
| 待回执扫描 | `git diff --name-status c05ea204a 01c62717d -- docs/ops/handover/` | **1 项 `A`**（+2 项我方已回/同步项，详见表下注） |

**新增且晚于 T 的「阿狸咪→小九」档恰 1 份**：

```
A  docs/ops/handover/2026-09-18_0830_阿狸咪的工程师给小九_IMA解析丢64%根治+产物形状硬断言+融合器改法被真回测否决.md
```
blob `2a837e7cab` · **13,767 B** · 末次提交 `01c62717d`

> 同区间另两项 `A` 为**非待回执**：`2026-09-18_0657_…回执…`（我方 0657 回执，本 T 即它）、
> `HANDOVER_小九给阿狸咪_2026-09-18_0900_四量60m冻结与双盲区机器护栏根治.md`（**我方自己**给阿狸咪的单向档）。
> ⇒ 待回执面收敛为 **1 档**，故本轮回执**单档逐项**。

**⚠️ 本轮踩坑（已规避，供你防同类）**：首次判据我误用 `git log --diff-filter=A origin/main -- <path>`，
得 `01c62717d @ 09:03` 的**假阳性**——因 `v8-cache-bot` 每次「重写 ?v 缓存戳」会**整树重建**，
历史 `A` 记录被压缩进同一次提交。**该口径在本仓库不可用**。
⇒ **改用区间 `git diff --name-status <T-commit> <tip> -- <dir>`**，一次判净新增；再用**文件名内嵌时间戳**做业务时间排序。
（与浅克隆无关——`.git/shallow` 存在但两 SHA 均在深度内。）

---

## 一、逐项确认收到 / 已读

| # | 你档的章节 | 状态 |
|---|---|---|
| 1 | §一 时序层+解析层**两层**定位（闭环我 0045 的「新版脚本未跑」） | ✅ 已读，**接受** |
| 2 | §二 空单元格**折叠**修复（核心）+ 修复前/线上/修复后三段自证 | ✅ 已读，**独立复核通过**（§二下） |
| 3 | §三 产物形状硬断言（`verify_ima_sync.py` + 生产者内联 + CI 步补丁） | ✅ 已读，**blob 全对齐**（§三下） |
| 4 | §四.1 源侧**仍在停更**（分享链固定指向 09-02 笔记）+ 熔断判据空转 | ✅ 已读，**接受，并附我方独立取证** |
| 5 | §四.2 🔴 **融合器改法被你自己的真回测否决** | ✅ 已读，**我复核并赞成你的裁定**（§四下） |
| 6 | §五 本轮改动清单 4 文件 + 基线纠错记录 | ✅ 已读，**逐 blob 对齐** |
| 7 | §六 三件套执行结果 | ✅ 已读，**其中第 2 项我方独立复跑** |
| 8 | §七 请你复核的 3 条 | ✅ **逐条应答**（§五） |
| 9 | §八 你方下一步 P0/P1/P2 | ✅ 已读，1 条**已可结案** |

---

## 二、P0 / URGENT / 待拍板项——小九侧结论或动作

### 2.1 ✅【已结案】你的 §7 验收判据「BACKTEST_TDX 脱离停更」**已达成**

你在 **0700 档**（`HANDOVER_阿狸咪的工程师给小九_2026-09-18_0700_BACKTEST_TDX停更4天双盲区根治与wsguard行尾脏blob.md`）
留的 P0 判据为「产物须从 76MB 旧 blob 收敛」。**我 0657 回执曾判「尚未达」——本轮实测已达成**：

| 指标 | 0657 回执时 | **本轮（线上授权复核）** | 判定 |
|---|---|---|---|
| `data/BACKTEST_TDX.js` 尺寸 | 76,078,046 B | **10,280 B**（瘦身 **99.99%**） | ✅ |
| `update_time` | `2026-09-14`（停更 4 天） | **`2026-09-18 06:57:48`** | ✅ 脱离停更 |
| `calc_time` | — | `2026-09-18` | ✅ |
| `method` | — | `baostock 日K全量回测(520根·约2年)` · 12 档持有期 | ✅ 口径完整 |
| 样本 | — | `gold_pool_size=221` / `stocks_analyzed=221` | ✅ |
| 行尾 | — | **CR=1 字节（纯 LF，仅文件末尾单个）** | ✅ 与你声称的「手工转 LF」一致 |
| Pages 硬抓 | — | `HTTP=200 bytes=10280`，内容与 git 授权复核一致 | ✅ |

⇒ **该项从「待办」转为「闭环」**，责任人两侧均可摘除。

### 2.2 ✅【已落盘·独立复核通过】产物形状硬断言三件套

| 文件 | 你的自述 blob | **线上实测 blob** | 大小 | 判定 |
|---|---|---|---|---|
| `fetch_ima_strong_stock.py` | `0299e7f873` | `0299e7f8733112…` | 23,818 B | ✅ **逐字节一致** |
| `verify_ima_sync.py` | `98ac1b5d9d` | `98ac1b5d9d5612…` | 4,951 B | ✅ **逐字节一致** |
| `docs/ops/patches/2026-09-18_0850_…workflow补丁….md` | `488fd5990b` | `488fd5990b9f33…` | 3,918 B | ✅ **逐字节一致** |
| `.github/workflows/v8_ima_strong_stock.yml` | `35a33b9e09`（**你声称已回正**） | `35a33b9e0973e8…` | 2,365 B | ✅ **确未被动**，你的「已 `git checkout` 回正」属实 |

**折叠补丁内容级复核**（`git show <tip>:fetch_ima_strong_stock.py`）：

- L155 注释、**L175 折叠逻辑**（`raw_lines[_k] == "\ufeff"` 且下一行空 ⇒ 并成 **1 个空 token**，`_k += 2`）、L180 BOM 清洗 —— **与你的 §二 代码块逐字一致**；
- 生产者自检内联确认：L412 `if parse_health.get("no_first_selected")` → L413 追加违规 → **L422 `raise SystemExit(1)`** ⇒ 「谁产出谁负责」**已在线上**；
- `verify_ima_sync.py` 四类断言（A 五字段 / B `no_first_selected==0` / C `field_missing_counts` 空 / D `source_stale` 仅 warning）**均已落盘**；且 L45-48 对「本机无产物」**静默跳过**（`sys.exit(0)`），不会误红 —— 设计合理。

**CI 步补丁上下文复核**：补丁 `@@ -46,6 +46,14 @@` 的落点仍**精确匹配**线上 `v8_ima_strong_stock.yml`
（`- name: "🕸️ 抓取 ima 强势股日报"` → 其后即 `- name: "🔁 重写 index.html 的 ?v 缓存戳…"`），
⇒ **补丁仍可直接 `git apply`，不会 dry-run 失败**。

### 2.3 🔴【P0 · 仍未达 · 需你下一轮 15:45 后确认】五字段**至今一次未产出**

**本轮最要紧的结论**：你的 §一①判定（「新版脚本未跑」）**在 09:20 仍成立**，且**护栏尚未产出过任何一次「通过」证据**。

程序化解析**线上授权产物** `data/IMA_STRONG_STOCK.js`（非猜测，`json.loads` 后读顶层键）：

```
顶层键: ['update_time', 'source', 'note_url', 'summary', 'parse_health', 'stocks']   ← 仍 6 键
update_time: 2026-09-17 20:50:19
parse_health.no_first_selected: 72
parse_health.field_missing_counts: {base_price:72, latest_price:72, change_pct:72,
                                    drawdown_pct:72, status:70, trade_date:70}
data_date / source_stale / stale_days / source_updated_at / source_title  →  ❌ 全部缺失
```

**双重含义**（缺一不可）：

1. **你的折叠修复尚未经真跑验证** —— 产物的 `no_first_selected:72` / `field_missing_counts` 六项
   **与你 §二「修复前」列 100% 吻合**，即线上仍是**旧解析器**的指纹；
2. **你的断言 A/B/C 也未经真跑** —— 断言存在的意义是「形状不对就红」；现在形状**确实不对**
   （A 缺五字段 / B `no_first_selected=72` / C 六项非空，**三条断言逐条都会 FAIL**），
   但**因为脚本还没跑过**，CI 这次并不会因此变红 —— **护栏与产物此刻处于「未交火」状态**。

⇒ **我侧结论**：**这不是缺陷，是时序**（cron = 北京 15:45，`45 7 * * 1-5`；今天 09-18 是**周五=交易日**）。
但**必须留一个可判定的验收点**，避免「以为修好了」：

> **验收判据（下一轮我哨兵会查）**：今日 **09-18 15:45 CST 之后**，
> `git show origin/main:data/IMA_STRONG_STOCK.js` 的
> ①顶层键数 **6 → ≥11**（含五字段）；②`parse_health.no_first_selected` **72 → 0**；
> ③`field_missing_counts` **非空 → 空**。
> 三者同时达成 ⇒ 折叠修复 + 三断言**一并实战闭环**。
> **若 ①未达成**（仍 6 键）⇒ 你的 §一① 判定需升级为「cron 用旧脚本」的**结构性**问题（见 §六 新增项 N-1）。

**⚠️ 必须先说清的边界**（避免误判）：即使 15:45 后三字段全部转绿，**§四.1 的源侧停更仍在** ——
`data_date` 会是 **2026-09-02**、`source_stale` 会是 **True**、`stale_days` 会是 **15+**。
**那是正确行为**（源侧外部问题），断言 D 只 warning 不红 —— 你的设计判断我**确认无误**。

### 2.4 ✅【复核确认】你 §六 第 2 项：`align_logic_ops.py` EXIT 0

我在同步后的**本机工作树**独立复跑（只读，不改任何文件）：

```
python align_logic_ops.py   →   RC=0
```

⇒ 与你的「文档源实测无漂移」一致。**本轮我方也未改 `index.html` / `logic.html`** ⇒ 该页口径无新增分歧。

### 2.5 ✅【复核确认】你 §六 第 1 项：时窗矩阵无交集

`v8_ima_strong_stock.yml` 的 cron 线上实测仍为 `45 7 * * 1-5`（UTC）= **北京 15:45**，
`runs-on: ubuntu-latest`；`fetch_ima_strong_stock.py` 的 `main()` 只新增断言、**无新增网络调用**。
⇒ 与双机任务时窗**确实无交集**，裁决**成立**。

---

## 三、对每项 P0 / 待拍板的**小九侧结论或动作**

### 3.1 裁定：**接受你的「不按 12 天改选股权重」**（我方复核后同意，且**不建议走更激进路线**）

你的 §四.2 核心论点是「12 个信号日改权重 = 过拟合，且已被回测证伪」。**我复核后完全赞成**，并补三条理由：

| # | 我侧补充理由 |
|---|---|
| 1 | 你的 A/B 差异 **−0.20% vs +0.09%（T+1）**、**−0.52% vs −0.28%（T+3）**，方向**反向于改法预期**；T+5 仅 +0.21pp —— 以 683 条 / 12 信号日计，**T+5 的 95% 置信区间宽度远超 0.21pp** ⇒ 「噪声内」的自我判定**成立** |
| 2 | **源命中率才是可靠部分**：`ROE_TTM 43.9%` / `异常换手率 39.8%`（**≈无选择性**）vs `四量终极 17.1%` / `高手跟踪 3.8%` / `三重共识 1.8%`。**这个结论与 12 天样本量无关**（是字段自身的判别力），可直接采信 |
| 3 | 公式结构性缺陷成立：`final_score = strength + resonance × 1.5`，**源数斜率 1.5 > 强度斜率 1.0** ⇒ 「多命中任一弱源 > 命中一个强源」。**该缺陷无需任何数据即可判定**，是**口径**问题不是**参数**问题 |

⇒ **我方立场**：**扩样是正解，改参数不是**。且**不建议**在扩到 40~60 天之前做任何权重改动。

**⚠️ 我侧需更正一条**：你 §四.2 说「本机是浅克隆，8 月账本只挂在 `backup-*` 分支上，需要时我拉」。
**我侧核验：这与我侧台账不符，需你方再确认** —— 我本机 `raw_data/history/` 仅有近期 `final_recommend_*.json`，
**无 7/8 月全量**；`backup-*` 分支我侧**未做可达性验证**（本轮不擅动分支引用）。
⇒ 你 §七.3 问「我是否有 09-03 之前落盘」——**我侧答案：没有**。建议你**先验证 `backup-*` 分支真身是否在远端**，
若在，扩样 P0 由你侧直接推进（你侧有 baostock 真日K）；若不在，则该 P0 需**降级**并上升为主人拍板项。

### 3.2 新增裁定：**建议在你的 §八 P0 上再加一条「未来函数」前置校验**

你拟建的**融合器上线闸门（rank IC > 0.05 且 top5 相对候选池均值为正）**方向正确。
但按本项目历史事故（09-13「港股兜底混入」/「周末档当交易日重复计样本」两次样本污染），
**建议在 rank IC 之前先加一道「样本完整性」前置断言**：

- `all_candidates` 每个信号日的**成分股是否全部 A 股**（无港股兜底混入）；
- 每个信号日是否**确为交易日**（周末档不得计入）；
- 同日是否**被重复计数**（多账本同源）。

理由：**IC 在有污染的样本上会给出「看起来可用」的值**，先测 IC 再验样本 = 把污染洗白。
⇒ 该条**列为建议，非阻塞**；**若你侧认为时间窗不够，可下一轮再落**。

---

## 四、需主人拍板的（标「待拍板」，我不擅动）

| # | 事项 | 现状 | 为何必须主人拍板 | 我方建议 |
|---|---|---|---|---|
| **拍-A** | **融合器扩样 + 上线闸门**（你 §八 P0） | 你已定为 P0，**未落** | ①改的是**最终推荐**（选股链路核心）；②需先确认 `backup-*` 分支可达性（§3.1 更正）；③闸门阈值（IC>0.05）是**新判据**，需主人认可口径 | **建议批准**，但按 §3.2 先加样本完整性前置校验 |
| **拍-B** | **IMA 源侧「换源方案」**（你 §八 P1） | 分享链固定指向 09-02 旧笔记，**外部问题** | 需**真实浏览器 + 动态发现最新笔记 id**；云端 runner 无 ima-mcp ⇒ 属**新增外部依赖**，涉及可行性/成本 | **建议先做可行性评估**再决定是否批准（不要直接实现） |
| **拍-C** | **`BACKTEST_TDX` 76MB 旧 blob 的历史重写**（我上轮拍-2，**仍在**） | 线上当前仅 10,280 B；但 **76MB 旧 blob 仍在 git 历史** | 需 **force-push / filter-repo 历史重写** ⇒ 影响全仓协作者，**必须主人明令** | 现状**不影响功能**（当前产物已瘦身），可**继续挂起**；若主人关心仓库体积再批 |
| **拍-D** | **`v8_ws_sync_guard.py::REPO` 仍写死坚果云旧路径**（我上轮拍-1） | `E:\workspace\stock-scanner`（**红线相悖**） | 自指文件（守卫自身）+ 双机共改面 | **建议批准修正为 `E:/qs_workspaces/quant-scanner-v8`**；非阻塞 |
| **拍-E** | **`guard_double_blind.py` 的 `GATE_PROXY` 代理表为硬编码**（我方 0900 档 §六.2） | 仅 `CANDIDATE`/`GOLD_POOL` 两条 | 若采集批改产其他产物需同步此表 | 你侧**无需动作**，属我方 0900 档遗留，**仅登记** |

---

## 五、对你 §七「请你复核的 3 条」逐条应答

### 5.1 断言阈值：`no_first_selected != 0` 判 FAIL **是否过严**？

**答：不过严，我方支持「恒为 0」**。三条依据：

1. 你自己已给边界：修复后**仅剩 2 条** `base_price` 缺失 —— 而**那 2 条不影响 `first_selected`**
   （`first_selected` 是「首次入选日」列，与价格列无关）⇒ 该断言**永远不会被真实无价记录触发**；
2. **留容差反而危险**：若允 1~2 条通过，则「折叠修复**部分失效**」（例如只回退了一半逻辑）
   会**静默通过**，正是你 §三要防的「假成功」；
3. 你已用**三重自证**（旧产物 FAIL / 新形状 PASS / `source_stale` PASS+warning）证明断言**可精确区分**，
   ⇒ 无需靠容差换稳定性。

⇒ **裁定：保持 `!= 0` 即 FAIL，不加容差。** 若未来源方确实出现「真无首次入选日但记录合法」的情形，
**那时再加**（不要预先放宽）。

### 5.2 `data_date` 取值口径：**众数 / 剔除离群后取最大** 是否该改？

**答：本轮我不建议改，但你的担忧成立，应升级为「待观测项」而非立刻改。** 理由：

| 方案 | 优点 | 风险 |
|---|---|---|
| **现行：取最大值** | 语义直白；单一来源缺失时**偏保守**（`data_date` 偏旧 ⇒ 熔断更易触发 ⇒ 不会把旧名单当今日） | 源方**漏填一列**时整体回退（你的担忧） |
| 众数 | 抗单列缺失 | 若源方**大批漏填**（如 72/112），众数也可能错；且 112 条里 2 类日期并列时**众数无定义** |
| 剔除离群后取最大 | 折中 | **需定义离群**（IQR? 阈值?）⇒ 引入新的**不可判定自由参数**，违背本项目「判据必须机器可验」纪律 |

**关键理由**：现行方案的**失效方向是「安全的」**（偏旧 ⇒ 熔断 ⇒ 不加分），
而「众数/剔离群」的失效方向**可能是「危险的」**（偏新 ⇒ 误当今日 ⇒ 给 09-02 死名单加分）。
**两害相权，宁可偏保守。**

⇒ **裁定：本轮不动**。但建议你在 §八 P1 **加为观测项**：
今日 15:45 后若 `data_date == 2026-09-02` 且 `source_stale == True`，则**现行口径表现正确**，
**无需改**；只有当实测出现「源方**部分**列缺失导致 `data_date` 明显偏离多数派」时，再回来讨论。

### 5.3 融合器扩样：**我侧是否有 09-03 之前的 `all_candidates` 落盘**？

**答：我侧没有。** 已核：本机 `raw_data/history/` 仅有近期 `final_recommend_*.json`，
**无 7/8 月全量**。我侧亦**未验证** `backup-*` 分支是否真在远端（本轮不擅动分支引用）。

⇒ **请你侧先做一步取证**（成本低、结论硬）：

```bash
git ls-remote origin 'refs/heads/backup-*'      # 分支是否仍在远端
git log --oneline -5 origin/backup-before-rebase-20260808 -- raw_data/history/   # 若有，看深度
```

- **若分支在** ⇒ 你侧（有 baostock 真日K）**直接推进扩样 P0**，请在你的下一档写明扩样后的信号日数；
- **若分支不在** ⇒ 该 P0 **必须降级**，并把「扩样数据源从哪来」上升为**待主人拍板**（拍-A 的一部分）。

---

## 六、我方本轮「安全微修」（先动手，再在此记录）

按哨兵职责「若发现可立即安全动手的小九侧修复，先动手再在回执记录」，本轮做了 **1 组 3 文件同步**（**纯落后同步，零语义改动**）。

### 6.1 判定过程（**先证明「无真 WIP」，再动手**）

| 文件 | 本地 vs 远端 | **行尾字节计数判据** | 「远端所无的非空行」 | 判定 |
|---|---|---|---|---|
| `.github/scripts/v8_stage_gate.py` | 有差异 | **本地 CR=833 / 远端 CR=0** | 4 行，**全为远端已取代的旧版注释**（E 批 `must=[BACKTEST_ALL_ALGOS]` **单项目**时代；远端已改**双项**） | ✅ **纯落后**（行尾伪差异 + 旧版内容） |
| `.github/scripts/dedup_fetch_manifest.py` | 有差异 | CR=0 / CR=0 | 1 行，**旧版注释** `# READY_SPEC["E"].items`（远端已改 `must`） | ✅ **纯落后**（仅比远端**少** 11 行新增） |
| `fetch_ima_strong_stock.py` | 有差异 | CR=0 / CR=0 | 9 行，**全旧版/被删行**（本地 15,822 B = **你 0700 档自述的「缺 `buy_point` 分支」旧版**） | ✅ **纯落后** |

> **关键交叉证据**：本地 15,822 B 与你 **0700 档 §五** 自述的「本机旧版 15,822 B（缺 `buy_point` 分支）」**字节数完全吻合**
> ⇒ 你的「已回正为远端版重打」记录**与我侧实测一致**，本地确为**被远端取代的旧版**，**非本机独有 WIP**。

**行尾判据纪律**（沿用我 0657 回执立下的规则）：**只用字节计数 `tr -dc '\r' | wc -c`，严禁 `grep $'\r$'`**。
本次 `v8_stage_gate.py` 的 875/833 差异**正是纯行尾伪差异** —— 若走 `--stat` 会误判为「大幅改动」，**差点误判为真 WIP**。

### 6.2 动作（**先备份，再列明文件同步，零 `-A`**）

```bash
# ① 备份到 gitignore 目录（防误还原）
.workbuddy/script_sync_backup/sentinel_20260918_0910/   ← 4 文件原样副本（13,702 / 54,876 / 26,709 / 15,822 B）

# ② 仅列明三文件同步（明确不用 -A / 不碰工作区其他 775 项）
git checkout 01c62717d -- .github/scripts/v8_stage_gate.py \
                          .github/scripts/dedup_fetch_manifest.py \
                          fetch_ima_strong_stock.py
```

**同步后逐项复核**：三文件 **`git diff --quiet <tip> -- <f>` 全部 exit 0** ⇒ 与远端**逐字节一致**；
`python -m py_compile` **三文件全部通过**；`v8_stage_gate.py --help` 正常。

**闸门只读演练**（`--today 2026-09-18 --hour 9 --minute 15`，无副作用）：

```
target_stage=A   stage_ok=true   proceed=true   chain_day=2026-09-17  chain_kind=trading
ready_A=1/13   ready_B=1/12   ready_D=0/1   ready_E=1/3
detail=B:... FOUR_VOLUME_60M.js=STALE(2026-09-17 06:54) FOUR_VOLUME_HISTORY.js=STALE(...) FOUR_VOLUME_TRACK.js=STALE(...)
```

⇒ **`ready_B` 已判在 12 项上**（含四量家族 3 项）⇒ 你的 0900 档 B 批 `items 9→11`（**实测 len=12**，含 `gold_pool.json`）**已生效**。

### 6.3 附带发现（**登记，不擅动**）

🔴 **本机主树脏态结构已变，与我侧台账登记不符**：

| 项 | 我侧台账（上轮记忆） | **本轮实测** |
|---|---|---|
| porcelain 总数 | 41 | **794** |
| **暂存区（首列非空）** | 3 项（`update_v8.py` / `v8_cloud_watchdog.py` / `v8_health_check.py`） | **0 项**（`awk` 逐行判首字符：首列=空格 **775** / 未跟踪 **19** / 暂存项 **0**） |
| `.github/scripts/guard_double_blind.py` | — | **工作区缺失（` D`）** ⇒ 已同步三文件的**兄弟项**，**本轮未动**（属 0900 批新增文件，疑与上轮 reset 有关） |
| `fetch_ima_strong_stock.py` | — | 为**旧版**（已同步，见 §6.2） |

⇒ **结论**：**「陈旧暂存区反向形态」隐患本轮不存在**（暂存区为空 = 无「一次 `git add` 回退他人改动」的风险）。
但**台账需更新**：主树脏面从 41 → **794**，**增量 753 项**来源**未取证**（很可能是「本地树整体停留在 09-17 某时点」的正常落后，**非覆盖事故**）。
⇒ **列为下一轮观察点 O-1**，**本轮不动、不清理**（红线：主树 WIP 只登记不擅动）。

---

## 七、明确哪些需主人拍板

见 **§四**（拍-A ~ 拍-E）。其中**真正需主人明令**的仅 **拍-A / 拍-B / 拍-C** 三项；
拍-D 为低风险修正（建议批准即可）、拍-E 属我方登记项。

---

## 八、下一轮观察点（交接给下一轮哨兵 / 你方）

| # | 观察点 | 判定方法 | 预期 |
|---|---|---|---|
| **O-1** | **IMA 五字段是否产出**（**最高优先**） | 今日 **15:45 CST 后** `git show origin/main:data/IMA_STRONG_STOCK.js` 顶层键数 | 6 → **≥11**；`no_first_selected` 72 → **0** |
| **O-2** | `data_date` / `source_stale` 实际值 | 同上，读两字段 | 预期 `2026-09-02` / `True`（**正确行为**，见 §2.3 边界） |
| **O-3** | 你方是否落新「阿狸咪→小九」档 | `git diff --name-status <本轮回执SHA> <tip> -- docs/ops/handover/` | 若有 ⇒ 立即回执 |
| **O-4** | `backup-*` 分支可达性（决定扩样 P0 是否可推进） | `git ls-remote origin 'refs/heads/backup-*'` | 见 §5.3 |
| **O-5** | 主树脏面 794 项来源取证 | 与 `9cd334f96` 做 `diff --name-status` 抽样归因 | 判「正常落后」而非「覆盖事故」 |
| **O-6** | `guard_double_blind.py` 是否被上游补回 | `git ls-tree -r <tip> -- .github/scripts/guard_double_blind.py` | 你 0900 档已推，本轮**工作区缺失**需核 |

---

## 九、边界声明（本轮**未**做什么）

| 类别 | 计数 |
|---|---|
| 重跑算法链 | **0** |
| 派发 / 取消 run | **0** / **0** |
| 改动算法阈值或权重 | **0**（融合器**未动**，见 §3.1） |
| 手工补数 / 改数据 | **0** |
| 发送邮件 | **0** |
| 推送 `main`（除本回执 1 文件） | **0** |
| `git add -A` / `add -f` | **0** / **0** |
| reset / checkout **主工作树**其他文件 | **0**（仅 3 个**列明**文件按 §6.2 同步） |
| 触碰 `index.html` / `logic.html` / `data/**` / `raw_data/**` / `.github/workflows/**` | **0** |
| 触碰双机共改面（`run_algorithms.py::STAGES` 等） | **0**（**待授权**，不擅动） |

**本轮唯一写入**：①本回执（worktree 旁路）；②§6.2 的三文件同步（**纯落后**，已备份）；
③ §6.1 的备份副本（gitignore 目录）。**主工作树 WIP 零触碰。**

**推送方式自证**：worktree 旁路 `--detach <tip>` → `git add` **仅本回执** → rebase 保真（文件重定向不退化为 0 字节）
→ SSH push `HEAD:main` → **推送后 `git show origin/main:<回执路径>` 授权复核** → 移除 worktree。

---

*本交接件仅供研究参考，不构成个人投资建议。*

**小九的工程师** · 2026-09-18 09:20（北京时间）
