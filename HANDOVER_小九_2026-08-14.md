# 九宝量化 v8 交接文档 · 小九 → 阿狸咪

> 更新于 2026-08-14 11:55 自动更新（12:00 档）
> 生成方：小九（北京时间当班），存档于 quant-scanner-v8 仓库根目录
> main HEAD（本次核实）= `000976b69`（11:55 v8 healthcheck，证明管线存活）

---

## 一、背景

- **日期/当班**：2026-08-14（周五），小九北京时间当班（07:45–17:45），阿狸咪夜间/周末（18:00–次日 07:30 + 周末）接手。
- **今日主线**：① 接收并校验阿狸咪昨晚（08-13 夜 ~ 08-14 凌晨）全部改动已入库、未丢失；② 盘中数据管线突发大缺口（self-hosted cn runner `lemoncat-cn` 自 08-13 13:14 起累计 ~45h 故障），主导根治；③ 按主人 6+ 张截图反馈做 UI 一致性修复与海外市场接入。
- **关键外部事实**：GitHub Actions `cn_fetch_cloud` / `algo_cloud`（self-hosted cn）自昨日 13:14 起**每轮 failure，0 成功**；盘中数据唯一出数通道 = 本机中国 IP 直抓（ah-quant999 提交）。

---

## 二、今日已完成改动（含真实 commit SHA + 影响）

> 下列 SHA 均来自 `git log --oneline origin/main` 实时核实，未使用记忆值。
> ⚠️ 注：部分早盘 commit（`02ce6cda6`/`5f84b7bfc`/`9c217e93c`）在 rebase 中 SHA 漂移，其改动已并入 main（已 grep 确认代码落地），下方按"实际承载提交"标注。

### A. 阿狸咪昨晚改动 — 已确认安全入库（08:38 实地核查）
- `git status` 工作树干净、`HEAD == origin/main` → 她 08-13 夜~08-14 凌晨全部 commit 已 push 落地，本地无未提交可丢。
- **覆盖事故已真修好**：她今早 `e5cfa01b5` 修复「05:40 cloud_fetch 把 main 上 backtest_tdx 从 08-13 新版(296只/14万行)覆盖成 08-04 旧版(144只)」。`api_push_raw.py` walk_raw() 已显式排除 `backtest_tdx/backtest_comprehensive/cockpit_backtest/optimized_strategy/top3_track` 五类算法产物；`raw_data/backtest_tdx.json`+`data/BACKTEST_TDX.js` 当前 calc_time=2026-08-13、stocks_analyzed=296（新版已恢复且纳入 main）。

### B. 盘前全量上线（08:07–08:24）
- `ed922eabd` chore(v8/dividend)：每日 cninfo 分红方案刷新（重点池 298 只，更新 279 / 跳过 19，宝丰能源 600989 10派4.2）。
- `f0738b3fc` v8 premarket full 2026-08-14：cloud_fetch 14/14 全绿 + update_v8 重建；JUDGMENT/MACRO/V8_CAL 均 2026-08-14 今日 → ALL_TODAY。

### C. 全量上线（09:28–09:32，含红线 index.html）
- `8bbf6faed` v8 上线（19 files, +554/-353）：
  - **商品弹性真实性修复**：`calc_commodity_elasticity.py` 重写，金属价 4/10 有真实价（gold/silver/copper/oil），其余 6 项标 `available:false` + 原因，**杜绝假无信号**（磷酸盐/维生素/稀土原来硬编码 1.0 → dev_pct 永 0）。
  - **名称归一化去重**：新建 `algorithms/name_utils.py`（norm_code / strip_entitlement_prefix / STANDARD_NAME_MAP / fix_name），统一替换 final_recommend + build_candidate_pool + guanlan_extractor + scanner 共 4 份重复逻辑。
  - **pipeline 护栏**：`api_push_raw.py` walk_raw 排除表改"词根前缀"自动匹配（backtest*/cockpit_backtest*/optimized_strategy*/top3_track*），根除"漏加回表再被覆盖"根因。
  - **SH_SZ 日期排序修复**：成交金额图 X 轴字典序乱序（显示 9/29）+ akshare 47 条未来日期 → 改数值序 + 过滤未来日期，`data/SH_SZ_HISTORY.js` 128→81 条、单调无未来。
  - **市场洞察 badge 动态时段**：消除硬编码"盘后"永远显示。
  - **字体放大 + 删除 `(ffc)` 裸文本泄露**（index.html:1141 裸文本渲染到页面左上角）。
  - **海外市场新卡 OVERSEAS_MARKETS**：东财 ulist 抓 日经/恒生/KOSPI/台湾 + 今日事件页新卡。
- 推送经 3 冲突（COMMODITY_ELASTICITY/HEALTH_CHECK/index.html）→ 全 `--theirs` → 8bbf6faed，工作树干净。

### D. UI 一致性微调（已 grep 确认落地于 main，SHA 已漂移）
- 恒生指数 + 今日判定「亚太」pill 内嵌行（`02ce6cda6` 原记录，现并入后续提交）。
- 删除每只股票卡片「回测&跟踪」区块（集中到策略回测页）：`fdc2990ff`。
- 每日洞察字体统一 12px + 打新速览 badge 动态化：`822bb36ac`。
- 大盘三指数 pill 方角 5px 样式统一（含 `41eeaeab4` 行业流入行「盘中实时」蓝色 pill 标签）。
- 删除独立海外卡、仅留今日判定内嵌一行（原 `9c217e93c`，现并入）。

### E. 一劳永逸式数据缺口根因修复（11:34–11:50）
- `f12a939a8` fix：一劳永逸式数据缺口修复：
  - **新增 `freshness_guard.py`**（数据新鲜度守卫，270 行）：反应式检查 raw_data 年龄，盘中 25min/盘前 180min/盘后 480min 阈值；午休 11:30–13:00 宽松 65min；退出码 0=新鲜跳过/1=陈旧补抓/2=非交易时段。
  - **OVERSEAS_MARKETS 改 premarket→intraday**：亚太盘中实时刷新。
  - **自动化升级为智能触发**：主通道 `1785898357370`（:00/:20/:40）+ 兜底 `1786678282916`（:10/:30/:50）均接 freshness_guard，最大间隔 10 分钟。
- 修复后即手动补抓验证：`bef14fba3` data 午盘补抓 11:50（15/15 全绿：sector_fund_flow 11:49 / index_quotes 11:48 / concept_ranking 11:48 / overseas 11:50 / avg_price 11:49），涨1906/跌3198/平179，沪6401亿+深7472亿=13873亿。守卫 9/9 全部新鲜 ✅。

### F. 阿狸咪保底通道配置（11:48）
- `fa1a48600` docs：新增 `docs/ALIMI_SETUP_GUIDE.md`（完整架构图 + 可直接复制的自动化 prompt + RRULE `:15/:45` 错峰 + 前置检查 + 故障排查表 + 分工边界 + 首次启用清单）。**请阿狸咪按此在其家用机部署同款直抓兜底自动化**，形成双机冗余。

---

## 三、当前系统状态

| 维度 | 状态 |
|------|------|
| **站点（GitHub Pages）** | 在线，`000976b69`(11:55) 后云端 build `7a505590f` + healthcheck 已触发；JUDGMENT/MACRO/SECTOR_FUND_FLOW 均今日时间戳 |
| **main HEAD（真实 SHA）** | `000976b69`（2026-08-14 11:55 v8 healthcheck） |
| **self-hosted cn runner `lemoncat-cn`** | 🔴 仍故障（08-13 13:14 起 ~45h 全 failure）；依赖它的 GitHub `cn_fetch_cloud`/`algo_cloud` 全红 |
| **本机直抓（主通道）** | ✅ 正常，11:50 末次补抓全绿；freshness_guard 智能触发已上线 |
| **看门狗/healthcheck** | 正常运行（11:35–11:55 多轮 healthcheck 全绿） |
| **剩余自动化（本机小九侧）** | 主通道智能触发(1785898357370) + 兜底直抓(1786678282916) + 盘前全量 + 盘后算法链 + 分红盘前 + 交接文档(本任务) 等 |
| **未提交改动（工作树）** | 仅 `_cn_log.txt`（未跟踪，非数据文件，**不纳入本次提交**） |

---

## 四、需阿狸咪注意 / 待办 / 风险

1. 🔴 **self-hosted cn runner 仍是全局最大风险**：`lemoncat-cn` 已挂 ~45h。当前靠本机直抓兜底维持盘中数据，但若小九本机离线/关机，盘中数据将断流。→ 建议：阿狸咪开机后排查 runner 服务状态（是否进程挂死/凭据过期），或临时切 fallback `ubuntu-latest` 跑 cn_fetch。
2. 🟠 **阿狸咪保底通道待部署**：`docs/ALIMI_SETUP_GUIDE.md` 已就绪，但其家用机尚未配置同款直抓自动化 → 双机冗余未形成。→ 请在夜间按指南部署 `:15/:50` 错峰自动化。
3. 🟡 **部分早盘 commit SHA 漂移**：`02ce6cda6`/`5f84b7bfc`/`9c217e93c` 已不在 origin/main（rebase 重写历史），但 grep 确认其改动（恒生内嵌/大盘pill/删独立海外卡）已落地 main。→ 勿凭旧 SHA 找提交，按功能搜代码。
4. 🟡 **主抓取通道现状**：盘中主通道=本机直抓（非 GitHub self-hosted），GitHub cn_fetch 仅辅助。若见 GitHub 侧 cn_fetch 全红属已知（runner 故障），数据以本机直推出数为准。
5. 🟢 **盘中数据缺口已自愈**：11:50 补抓后全绿，freshness_guard 每 10 分钟反应式监测，不会再出现 89 分钟陈旧无人知。

---

## 五、小九监督职责（本档自动记录）

- ✅ 已确认阿狸咪昨晚改动完整入库、覆盖事故已根治（护栏写进 `api_push_raw.py`）。
- ✅ 已主导盘中数据缺口根因审计（6 故障点）→ 上线 `freshness_guard.py` + 智能触发自动化改造，并即时补抓验证全绿。
- ✅ 已按主人 6+ 张截图反馈完成 UI 一致性修复（字体/动态badge/日期排序/(ffc)泄露/大盘pill/海外卡合并/恒生内嵌）。
- ✅ 已为阿狸咪产出保底通道部署指南，降低单点依赖。
- ⏳ 交接后（12:00 后）小九当班至 17:45，持续监控盘中数据新鲜度；18:00 阿狸咪接手后，本任务 18:00 档将再次更新此文档。
- 📌 红线提醒：index.html 今日已含小九授权改动并上线；阿狸咪部署时若 pull 遇冲突，算法产物/backtest 类文件一律以 main 为准（walk_raw 已排除，不会被 cloud_fetch 覆盖）。

---
_自动生成 · 下一档更新：2026-08-14 18:00（阿狸咪接手前）_
