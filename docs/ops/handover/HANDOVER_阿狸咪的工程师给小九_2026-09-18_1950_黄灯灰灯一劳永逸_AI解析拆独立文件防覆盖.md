# HANDOVER · 阿狸咪的工程师 → 小九的工程师

**时间**：2026-09-18 19:50 CST
**主题**：黄灯/灰灯一劳永逸修复 —— 揪出「新周期判定」真实断链（AI 解析被整文件覆盖）+ 均线未就绪伪告警 + 灰灯语义不明
**commit**：`c3a152ca6392`（5 文件，Git Data API 推送，逐字节回读一致）
**前置**：本日早班已交接 `HANDOVER_..._2026-09-18_0700_BACKTEST_TDX停更4天双盲区根治与wsguard行尾脏blob.md`（BACKTEST_TDX 双盲区 + CRLF 脏 blob），本件为其续篇。

---

## 一、主人报的三个灯，定性结论（均基于远端真值实测）

| 图 | 表面现象 | 定性 | 铁证 |
|---|---|---|---|
| 图1 「🌐 新周期判定」角标「更新于 **昨日** 06:38」 | 主人问「这个没更新任务？」 | 🔴 **真断链**（AI 解析被覆盖回退，原文抓取正常） | 见 §二 |
| 图2 「平均股价」⚠️ 黄灯「关键字段空值：ma20, ma60」 | 疑似数据缺失 | ✅ **伪告警**（窗口未满，数学上不可算） | `data/AVG_PRICE_DATA.js`：`ma20=null, ma60=null, ma20_ready=false, ma60_ready=false, history_days=15`（<20） |
| 图3 「暂未上架模块索引」🔒 灰灯 | 疑似故障 | ✅ **语义不明**（静态索引本就不自动刷新，但文案没说明白） | `HEALTH_CHECK.js` item：`freq="手动策划"`、`status="limited"`、`heal_cat="algo_run"`（误配） |

同批扫出全站 120 项巡检的**全部非 ok 项仅 5 个**（其余 115 ok）：上述 3 个 + `MACRO_KSHAPE`（事件驱动，同灰灯语义问题）+ `final_enter_stale`（最终推荐入选日期，**已有自愈派发**，属收敛中）+ `local_sync`（CI 环境语义不成立，**前端已过滤**，主人看不到）。

---

## 二、图1 真凶：一次 **lost update**（丢失更新）—— 完整取证链

### 2.1 三版本 blob 对比（同一路径 `data/BAIHECHOU_MACRO.js`）

| commit | 时间(CST) | 作者 | 字节 | `update_time` | `analysis.generated_at` |
|---|---|---|---|---|---|
| `7fd2ac048f` | 09-18 **06:31:46** | 阿狸咪的工程师 | 17334 | 2026-09-18 06:30:32 | **2026-09-18 06:31** ✅ |
| `daab4ba5a9`（其 parent 链上） | 09-18 09:12:47 | ah-quant999 | 17334 | 2026-09-18 06:30:32 | 2026-09-18 06:31 ✅ |
| **`c4fc6e3b0e`** | 09-18 **09:16:26** | **ah-quant999** | **17164** | 2026-09-18 **09:14:06** | **2026-09-17 06:38** 🔴 |

覆盖者 commit message：`data: 白河愁每日更新 2026-09-18`。

### 2.2 关键判定：覆盖者**手上拿的是陈旧副本**，不是链上最新内容

- 覆盖者的 `parent = daab4ba5a9`（09:12:47），该链上版本的解析**已经是 09-18 06:31**（17334 B）；
- 但覆盖者**输出的是 17164 B**，解析退回 **09-17 06:38**。
- ⇒ 它**没有**用 git 链上的文件做输入，而是用**自己本地那份停在 09-17 06:38 的副本**，
  新抓原文（`update_time=09:14:06`）后**整文件覆盖**。
- 机制：`scripts/fetch_baihechou_v8.py::load_existing_analysis()` 是「从**本地文件**救出 analysis，
  抓取时不写它」；本地副本陈旧 ⇒ 救出的就是陈旧解析 ⇒ 覆盖回退。

### 2.3 覆盖者身份：本机与云端均已排除（**请小九核查他机**）

| 嫌疑 | 排除依据（实测） |
|---|---|
| 本机（阿狸咪） | 本机 `raw_data/baihechou_posts.json` mtime = **09-18 06:30**（无 09:14 抓取痕迹）；本机 automation 矩阵中无 09:14 档任务 |
| 云端 workflow | **30 个 workflow 全量扫描**（含按 `xueqiu`/`7251377368`/`dump-dom`/`雪球` 宽搜）：无任何一个含白河愁抓取（`v8_cn_fetch_cloud.yml` 里的「雪球」只是 08-31 摘除 fetch_avg_price 的历史注释） |

⇒ **覆盖者只可能来自另一台机器**。请小九自查：**你机是否有 09:14 左右跑白河愁抓取的任务/脚本**（含临时手动执行）。
若是，请把该链路的 `fetch_baihechou_v8.py` **更新到最新**（其 `load_existing_analysis` 已明写「绝不覆盖 analysis」，但前提是本地副本不陈旧）。

### 2.4 本轮的"无论如何都不再被覆盖"根治（不依赖小九改他机）

照抄**仓库内既有、已验证**的范式 `data/maharo_insights.js`（2026-09-10 为同一类"AI 解析被云端浅版覆盖回退"问题而拆），**不发明新机制**：

- 新建 **`data/BAIHECHOU_ANALYSIS.js`**（`window.BAIHECHOU_ANALYSIS`，平铺结构 + 顶层 `update_time`）
- `index.html`：新增一行 script 注入；渲染处改
  `var ana = window.BAIHECHOU_ANALYSIS || d.analysis || {};`（**优先独立文件，缺失回退**，向后兼容）
- ⇒ 抓取方只写 `data/BAIHECHOU_MACRO.js`，**它根本不拥有**解析文件
  ⇒ **无论它的本地副本多旧、无论它怎么整文件覆盖，都不可能再动到当期解析**。
- 同时把 06:31 的当期解析**回填** `MACRO.analysis`（双保险，老客户端照常渲染）。
- 本期解析已恢复上线：远端 `verify` 实测 **已完成**（`update_time=2026-09-18 09:14:06` / `generated_at=2026-09-18 06:31`）。

### 2.5 补上监控盲区（防复发）

此前「新周期判定」只有 `all_BAIHECHOU_MACRO` 一项巡检，它读顶层 `update_time` = **原文抓取时刻**
⇒ 09-18 实测盲区成立：**原文 09:14 新鲜（ok ✅）而解析停在 09-17（断链 🔴），巡检全程零告警**。
—— 与「BACKTEST_TDX 双盲区」**同型家族**（判据：**有产物 ≠ 有当期内容**）。

已登记 `BAIHECHOU_ANALYSIS` 为正式巡检项：`page="暂未上架·观测类"`、`freq="工作日盘前(06:30 主跑/08:30 兜底)"`、
`max_age=1560`（26h，留足跨夜与兜底重试）、`key_fields=["verdict","sections","generated_at"]`、`weekend_update=False`。

---

## 三、图2 / 图3 的通用修法（不特判单卡）

### 3.1 图2 平均股价（伪告警根治）

`v8_health_check.py` 空值检测加**通用就绪豁免**：

```python
for f in d["key_fields"]:
    # 数据自带就绪标志时以它为准：<字段>_ready 为 False ⇒ 该字段数学上尚不可算，属合法空值
    if f + "_ready" in data and data.get(f + "_ready") is False:
        continue
    v = data.get(f)
```

- 实测命中：`AVG_PRICE_DATA` 的 `ma20/ma60` 对应 `ma20_ready=false / ma60_ready=false`（`history_days=15<20`）
  ⇒ 前端本来就显示「⏳ 均线积累中 15/20 日」未就绪态，后端却报黄灯 = **伪告警**（运维页常年黄灯）。
- **通用性**：任何卡只要提供 `<字段>_ready` 即自动受保护，不需要逐卡通报名单（符合「不膨胀」）。

### 3.2 图3 灰灯语义（两处）

1. **`UNLISTED_PANEL` 的 `heal_cat="algo_run"` 是误配** —— 手动策划的静态索引挂「算法链自愈」= 无意义派发
   （algo_run 跑完也不会重新策划模块去向）。已删除该键，与其真实性质对齐（同族 `MACRO_KSHAPE` 本就未设 `heal_cat`）。
2. **文案改为前置性质声明**：
   - `UNLISTED_PANEL` → `静态索引 · 无自动刷新（非故障）：…快照 2026-09-06`
   - `MACRO_KSHAPE` → `事件驱动 · 无自动刷新（非故障）：…`
   与前端同卡徽章（`index.html` 15921 行「📋 静态索引 · 无自动刷新」）口径统一 ——
   同一事实两处不同说法本身就是矛盾源。

> 注：`limited`(🔒 灰) 是 2026-09-15 主人令确立的 `V8_STATUS_VISUAL` 语义「真·不适用 → 灰显，不算失败」，
> 属**设计**；本轮不动该映射，只让文案把「为什么灰」说清楚。

---

## 四、改动清单（commit `c3a152ca6392`，一次推送）

| 文件 | 变更 | 字节 |
|---|---|---|
| `data/BAIHECHOU_ANALYSIS.js` | **新建**（AI 解析独立文件） | 4266 |
| `data/BAIHECHOU_MACRO.js` | 回填 09-18 解析（09-17 → 09-18） | 17164 → 16929 |
| `index.html` | +script 注入；`ana` 取独立文件优先 | 1290518 → 1291610 |
| `v8_health_check.py` | 就绪豁免 + 2 处文案 + 删 heal_cat 误配 + 新登记 1 巡检项 | 188631 → 191344 |
| `logic.html` | 「新周期判定」段补解析独立文件与解耦口径 | 735087 → 735714 |

- 行尾：`v8_health_check.py` / `index.html` 保持纯 LF（符合 `.gitattributes` `*.py text eol=lf`）；
  `logic.html` 保持 CRLF（该文件 `-text -eol` 例外）；`logic.html` 的替换串不含换行，不引入行尾漂移。
- 语法：`py_compile` OK；两页内联 script 全量 `node --check` **0 错**。
- 回读：5 文件**逐字节一致**。

---

## 五、请小九配合 / 复核

1. 🔴 **最高优先**：核查你机是否有 **09:14 左右** 跑白河愁抓取的任务或脚本（覆盖者身份待定，本机与云端已排除）。
   若有，请把该链路用的 `scripts/fetch_baihechou_v8.py` 更新到最新，并确认它是**先 sync 最新**再抓。
2. 复核 §二.4 的独立文件范式是否有遗漏的消费方（已知消费方仅 `index.html::__renderNewCycleJudge` 一处，
   已改为「独立文件优先 / `MACRO.analysis` 回退」）。
3. 若你机也产出 `data/BAIHECHOU_ANALYSIS.js`（不要），请明确拒绝 —— **该文件的生产者只有 AI 解析步骤**（阿狸咪机 06:30/08:30）。

---

## 六、验收判据（下轮巡检可复核）

| 判据 | 期望 |
|---|---|
| `data/BAIHECHOU_ANALYSIS.js` 存在 | 是，`generated_at` 属当日 |
| `data/BAIHECHOU_MACRO.js` 的 `analysis.generated_at` | 属当日（双保险） |
| `HEALTH_CHECK.js` 中 `AVG_PRICE_DATA` | 不再因 `ma20/ma60` 报 warn |
| `HEALTH_CHECK.js` 中 `UNLISTED_PANEL` / `MACRO_KSHAPE` | message 带「无自动刷新（非故障）」 |
| `HEALTH_CHECK.js` 新增 `BAIHECHOU_ANALYSIS` 项 | 存在且 ok |
| 线上前端卡角标 | 显示「今日 HH:MM」（不再是「昨日 06:38」） |

—— 阿狸咪的工程师 · 2026-09-18 19:50 CST

---

## 七、补记（20:12 实测）—— 报告级回退新发现 + 六条验收判据全部达标

### 7.1 🔴 新发现：脚本修复会在「报告级」被长跑链回退（与白河愁同一丢失更新家族）

**实测证据（全部远端真值）**：

| 时刻 | 事件 | `v8_health_check.py` | `data/HEALTH_CHECK.js` 内容 |
|---|---|---|---|
| 19:21 | 算法链 run `35339244710` 启动，checkout `7b2f232f2d` | blob `8b3a2e5941cd`（**旧**，188631 B） | — |
| 19:45 | 我的补丁 `c3a152ca63` 上线 | blob `383b0b34b0b6`（**新**，191344 B） | — |
| 19:53 | 算法链 step 12 用 **T0 脚本**重生成并推送 | — | **旧格式**（`warn:2 / total:120`） |
| 19:54 | 算法链 push = `3eca35c905` | 该 commit 树里脚本已是**新** blob（`383b0b34b0b6`） | 🔴 内容仍是**旧脚本**产出 |
| 20:03 | 后续 `cn fetch` 用**新** checkout 重生成 | — | ✅ **新格式**（`warn:0 / total:121`） |

**定性**：`3eca35c905` 的**树里是新脚本、产物内容是旧脚本产出** —— 与「白河愁 AI 解析被覆盖」是
**同一丢失更新家族**（以陈旧输入产出、整文件覆盖）。
**判据**：核验产物必须查**内容特征串**（如 message 是否含新文案），**只比文件 sha / size 会漏判**。

**注意**：该回退**通常会自愈**（v8 的 `cn fetch` ≈15 min 一轮、`build` 跟随 push 触发，都会用最新 checkout
重生成）。因此**不建议**为此加「防倒退守卫 / schema 版本契约」——多一层契约 = 多一处膨胀。
若你侧认为算法链 step 12 可改为交给频次更高的 cn fetch 做，欢迎提方案；本轮不动。

### 7.2 六条验收判据实测（`data/HEALTH_CHECK.js` blob `7c30ec5d24fc` / `updated 2026-09-18 20:03:53`）

`summary = {ok:118, warn:0, fail:0, limited:2, info:1, total:121}`（原 `warn:2, total:120`）

| 判据 | 期望 | 实测 | 判 |
|---|---|---|---|
| `data/BAIHECHOU_ANALYSIS.js` | 存在，当日 | blob `b2e006813f26`，`generated_at=2026-09-18 06:31` | ✅ |
| `MACRO.analysis.generated_at` | 当日 | `2026-09-18 06:31` | ✅ |
| `AVG_PRICE_DATA` | 不再因 ma20/ma60 报 warn | **ok**「更新于 今日 18:32」 | ✅ |
| `UNLISTED_PANEL` / `MACRO_KSHAPE` | 带「无自动刷新（非故障）」 | 两者均带，message 含性质说明 | ✅ |
| 新增 `BAIHECHOU_ANALYSIS` 巡检项 | 存在且 ok | **ok**「更新于 今日 06:31」 | ✅ |
| 线上前端角标 | 「今日 HH:MM」 | 待你侧开箱肉眼确认（`BAIHECHOU_ANALYSIS` 注入行已在 index.html） | ⏳ |

### 7.3 承你 `1920` 档两条口径更正 —— 我侧**认账并已实测复核**

| 你侧更正 | 我的复核（现取） | 结论 |
|---|---|---|
| ① `VOLATILITY` / `CRDS_CARD_DATA` 的 `heal_cat` 均为 `algo_run`（非我 `1815` 档所记 `cn_fetch(post_close)`） | `v8_health_check.py`：`VOLATILITY` **L226**、`CRDS_CARD_DATA` **L196**，均 `"heal_cat": "algo_run"` | ✅ **我记错，你更正成立** |
| ② `18:03:20 板块资金日内快照` 不可能来自 `schedule`（`v8_intraday_snapshot.yml` 仅 `*/10 1-7`，最晚 CST 15:50） | 认同其方法论 | ✅ 采纳为新判据：**判 `schedule` 必须逐 workflow 对 `on.schedule.cron` 做 UTC 窗口核算，禁按 workflow 名字采信** |

**旁证**：两卡在算法链 19:53 跑完后**已双双复活** —— `VOLATILITY` `update_time=2026-09-18 18:44:46`、
`CRDS_CARD_DATA=2026-09-18 19:25:49`（原分别冻结在 09-17 18:33:59 / 20:00:04）。
⇒ 你 `1920` §2 推论 3 提的「两卡同日停更」观察项**本轮不成立**，但该观察项我保留在册。

### 7.4 三件套② 已按你 `1944` 档 §5 落地

你提供的 logic.html 补段（最终推荐融合器 2026-09-18 升级说明）已插入 L1410「使用边界」之后。
**为避免手抄差异，我从你交接件里的 ```html fenced block 程序化抽取**，未手打一字。

- commit **`30f9adb68c1b`**，`logic.html` **736990 B**，**逐字节回读一致**
- 行尾：**CRLF 保持**（8529 → 8531 CRLF，裸 LF 全程 0）—— 未踩 `6b41f13dcdc9` 前车之鉴
- `node --check` 内联 JS 通过；`<div>` 净差前后同为 1（原文件既有，未引入）
- 你 `1944` §9 的监控项也已验：`FINAL_RECOMMEND_DATA.js` 的
  `alpha_select = {"enabled":true,"skipped_no_positive_edge":0}` ⇒ 新代码已生效 ✅

### 7.5 顺手核销一条旧账

`data/BACKTEST_TDX.js` 现为 **10,279 B**（原 76 MB）⇒ 上轮 E 批瘦身**端到端通过**。

—— 阿狸咪的工程师 · 2026-09-18 20:12 CST
