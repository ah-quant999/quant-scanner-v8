# 交接文档 · 观测平台两个子页签「字体放大 · 显著化」

- 时间：2026-09-16 21:33（已收盘，写仓窗口内）
- 执行机：小九（白班）
- 提交：`b1b7b669c2` —— ui(obs): 观测平台两个子页签字体放大显著化
- 推送方式：**净室重放**（取最新远端真身 `660e8c3c0` → 在其上重放补丁 → fail-closed 非 force；远端前进则自动重试，绝不整体覆盖他人改动）
- 改动文件：**仅 `index.html`**（1268570 → 1268843 字节，+273）

## 主人原话
> 「把这两个子页面的导航字体放大，显著一些」
> （截图：一级页「🌍 观测平台」下的两个子页签「🌎 宏观观测」/「📊 大盘观测」）

## 改动点（全局唯一一处：CSS `.obs-tab`）
| 属性 | 改前 | 改后 |
|------|------|------|
| `font-size` | 13px | **15px** |
| `font-weight` | 700 | **800** |
| `color`（未选中） | `var(--sub)` 灰 | **`var(--txt)` 亮** |
| `padding` | 8px 18px | **10px 24px** |
| `border-bottom` | 2px | **3px** |
| `:hover` | `var(--txt)` | **#fff** |
| `.active` | 仅颜色 + 下划线 | **加同色 `text-shadow` 微光** |

选中态语义色保持不变：宏观观测 `#34d399`（绿）／大盘观测 `#c8a2e8`（紫）。

> 说明：该子页签的高亮机制是「CSS + classList」（2026-09-15 修复落地），渲染侧不写死行内色 ⇒ **只改 CSS 即可全量生效**，无需触碰 `__renderObsPlatform` / `__obsSwitch`。

## 验证（全部通过）
| 项 | 结果 |
|----|------|
| 锚点唯一性 | OLD 命中 **1** / NEW 命中 **1** / 旧写法残留 **0** ✅ |
| 结构完整性 | `<script>` / `</script>` / `obs-tab` / `macro` / `market` 计数与基线**全等** ✅ |
| 行尾 | CRLF = 0（`index.html` 纯 LF 保持）✅ |
| 远端真身 | 新 index.html = **1268843** 字节；新写法 1 / 旧残留 0 ✅ |
| 推送安全 | 基于**最新**真身重放、fast-forward 非 force、零覆盖 ✅ |

## 改后三件套
| 件套 | 内容 | 状态 |
|------|------|------|
| ① 对齐云端/双机 | 提交 `b1b7b669c2` 已推 `origin/main` | ✅ |
| ② `index.html`↔`logic.html` 口径 | `logic.html` **无 `obs-tab` 结构**（该页无平行子页签 UI，仅逻辑说明文案提及），口径对齐不适用 | ✅ |
| ③ 写交接文档 | 本文件 | ✅ |

## 阿狸咪须知
1. **本批只改 CSS 一处**（`.obs-tab`），**未动**数据、渲染逻辑、其他样式与任何脚本。
2. 拉齐后**勿用旧缓存整体覆盖 `index.html`**——否则本次样式会丢（该文件 Ci 每 2~3 分钟带 `?v=` 缓存戳 churning，属正常）。
3. 本批**无需**你改动、重跑任何脚本或恢复任何自动化任务（你名下 stock-scanner 任务保持 PAUSED，恢复须主人明令）。
4. 若夜间发现子页签样式异常，先按本文件核对（新写法关键串 `padding:10px 24px;font-size:15px;font-weight:800;color:var(--txt)`），必要时邮件告警主人，**勿擅自重推 main**。
