# HANDOVER 小九给阿狸咪 · 2026-09-16 12:22

## 涨停热力矩阵「30 日截断放开 + 板块周期档案」上线（含累积型文件防误删护栏）

**主人令（2026-09-16）**

> 「几几年几月什么板块启动，该板块到什么时间结束，这样可以记录或者回查好多年，甚至可以算出每个板块的周期。」
> 「1 放宽，你准备查几年的？2 哪种快准且省 TOKEN 就用那种」
> 「按你建议做，用于累积的文件一定要打上不得误删之类的重要文件标签。我选的哪种快准且省 TOKEN 就用那种。」

**已上线两个 commit**

| commit | 内容 |
|---|---|
| `13375d421` | 后端：放开 30 日截断 + 板块周期档案生成器 + 防误删护栏 |
| `47ab952c7` | 前端：`index.html` 卡底新增「📚 板块周期档案」折叠区 |

---

## 一、为什么要做（调研结论）

主人问「你准备查几年的？」。三条路实测：

| 路径 | 实际可达 | 说明 |
|---|---|---|
| 东财 API 直取 | **仅约 22 个交易日（1 个月）** | `stock_zt_pool_strong_em` / `stock_zt_pool_em` 实测 8/26 之前全 0 |
| git 历史回补 | 1.5 个月 | `raw_data/limit_up_heatmap.json` 有 **576 个版本**，最早 2026-07-30 |
| 持续累积 | **每年 +1 年，无限** | 本次改造目标 |

**关键结论：数据源的 1 个月窗口才是真约束，体积不是。**
单列约 73 字节 ⇒ **1 年 17KB / 3 年 52KB / 5 年 87KB**。历史一旦错过**无法重新拉取**，所以必须现在就转永久累积。

**token 成本：零。** 全程走现有 Python 链路（`build_sector_cycle_archive.py` → `update_v8.py`），不消耗 AI token。

---

## 二、后端改动（`13375d421`）

### 2.1 放开三处 30 日截断

| 文件 | 位置 | 原写法 | 现写法 |
|---|---|---|---|
| `fetch_limit_up_heatmap_v8.py` | `main()` L487 附近 | `new_dates = new_dates[-30:]` | 删除，永久保留 |
| `fetch_limit_up_heatmap_v8.py` | `generate()` L708 附近 | 同上 | 删除，永久保留 |
| `algorithms/fetch_sector_rs.py` | L87-88 | `snaps = snaps[-30:]` | 删除，永久保留 |

### 2.2 🔴 两个「越攒越少」自杀点（必须同改，否则放宽无效）

1. **`_check_dates_integrity` 的 `extra` 判据**
   ```python
   extra = actual_set - expected_labels          # ❌ 旧
   if extra: return False, f"包含非最近30日日期 {sorted(extra)}"
   ```
   ⇒ 只要出现「近 30 日之外」的日期就判异常 ⇒ 触发**全量重建** ⇒ 历史被抹掉。
   **2026-09-16 实证**：回补到 39 列后跑生成器，日志报「日期序列异常」→ 39 列被砍到 **15 列**。

   现改为：删除 `extra` 判据，只拦**未来日期**（新增 `_is_future_label()`）；列数上限 `> 30` → `> 1200`。

2. **全量重建抹掉积累**
   在 `new_dates, new_sectors = build_heatmap(days_data)` 之后新增**历史合并**：
   以已有历史为底，新拉数据覆盖同日期，**保留旧日期**。

### 2.3 联动接线

`fetch_limit_up_heatmap_v8.py` 新增 `_gen_cycle_archive()`，在两处收尾打印后调用（`main()` / `generate()`）。
`cloud_fetch_v8.py` 走 `f_limit_up_heatmap()` → `hm.generate()` ⇒ **生产链路自动联动**。

**接线已端到端实测**：
```
📊 今日(09/16): 92 只涨停
🧹 退出冷板块（近5日合计 ≤2）：固态电池(0)、券商(0)
✅ 热力矩阵: 39 日 × 13 板块
✅ 板块周期档案: 26 个周期 (07/20~09/16, 39 列) → data/SECTOR_CYCLE_ARCHIVE.js
```

### 2.4 存量回补

`raw_data/limit_up_heatmap.json`：**25 列 → 39 列**（07/20~09/16）。

⚠️ **回补踩坑（务必记住）**：首次回补**污染了数据** —— git 历史里涨停矩阵有**两套口径**：
- 早期（7 月底）：`dates` 用 `"2026-07-31"` + **行业名**板块（约 53 个）
- 现期（8 月中起）：`dates` 用 `"MM/DD"` + **概念名**板块（13~15 个）

混用 ⇒ 日期错乱 + 板块暴增到 **64 个**。
**修复**：加两道口径闸门 —— ① 日期必须全为 `^\d{2}/\d{2}$`；② 板块名必须有交集且 `len(ver) <= 30`。宁少勿污。

---

## 三、🔴 累积型文件防误删护栏（主人特别强调）

主人原话：「用于累积的文件一定要打上不得误删之类的重要文件标签」。**五层护栏**：

| 层 | 载体 | 作用 |
|---|---|---|
| 1 | 文件头横幅 | `data/SECTOR_CYCLE_ARCHIVE.js` 顶部 🔴 警告块，写明「删除无法恢复」 |
| 2 | `PROTECTED_FILES.json` | 受保护清单（3 个文件），含 `why` / `generator` / `never` 字段 |
| 3 | `.gitattributes` | `-diff -merge` 标记，防误合并 |
| 4 | `guard_protected_files.py` | 校验脚本，缺文件 **exit 2** 并打印 `git checkout <sha> -- <file>` 恢复命令 |
| 5 | 生成器自身 | 每次重跑都会重新写回，天然自愈 |

**受保护 3 个文件**：
- `data/SECTOR_CYCLE_ARCHIVE.js` — 板块周期档案（前端注入）
- `raw_data/limit_up_heatmap.json` — 涨停热力矩阵累积长表
- `raw_data/sector_phase_history.json` — 板块 RS 快照序列（`fetch_sector_rs.py` 产出）

**实测有效**：模拟删掉一个文件后跑守卫 →
```
❌ 缺失 1 个受保护文件
   data/SECTOR_CYCLE_ARCHIVE.js
   恢复：git checkout <上一次提交> -- data/SECTOR_CYCLE_ARCHIVE.js
EXIT=2
```

当前状态（12:20 核验）：
```
受保护文件校验：齐全 3 / 共 3
  ✅ data/SECTOR_CYCLE_ARCHIVE.js
  ✅ raw_data/limit_up_heatmap.json
  ✅ raw_data/sector_phase_history.json
```

---

## 四、前端改动（`47ab952c7`）

`index.html` 涨停热力矩阵卡底折叠区，在「🗂 展开历史矩阵」下方新增**同级按钮**：

```
🗂 展开历史矩阵（共 39 个交易日，默认只看最近 20 日）
📚 展开板块周期档案（26 个周期 · 13 个板块，可长期回查）
```

**周期档案表格列**：板块 / 周期 / 启动 / 退潮 / 持续 / 活跃 / 峰值

**渲染规则**：
- 按板块分组，组内按轮次（第 1 轮 / 第 2 轮…）纵向排列 ⇒ 一眼看出轮动节奏
- 排序：**进行中板块优先**（当红主线置顶），其余按最近一次周期结束时间倒序
- 每行标 `进行中`（红）/ `已退潮`（灰）
- 峰值列按热度着色（≥8 红 / ≥5 橙 / ≥3 / <3）

**真实渲染实测**（vm sandbox + 真实数据，非勾勾叉叉）：

| 项 | 值 |
|---|---|
| 主表 HTML | 8,982 字符 ✅ |
| 历史矩阵 HTML | 14,773 字符 ✅ |
| 周期档案 HTML | 8,039 字符（27 行）✅ |
| inline script 块 | 28 个全部语法通过 ✅ |

样例输出：
```
 1. 其他      | 第1轮 进行中 | 07/20 | 09/16 | 39日 | 39日 | 296
 8. AI算力    | 第1轮 已退潮 | 07/21 | 08/19 | 18日 | 17日 |   9
 9. AI算力    | 第2轮 进行中 | 08/25 | 09/16 | 17日 | 16日 |  25
12. 半导体    | 第3轮 进行中 | 09/08 | 09/16 |  7日 |  7日 |  14
```
→ 这就是主人要的「几月启动、几月结束、算出周期」，并能看出 AI 算力退潮后第 2 轮重启。

**周期判定口径**（`build_sector_cycle_archive.py`）：
- `HOT_MIN = 3` 单日 ≥3 家算活跃日
- `CYCLE_GAP = 3` 活跃日间隔 >3 个交易日断为新周期

---

## 五、推送过程（双向防覆盖实战）

### 5.1 方向一「别覆盖」—— 归一化比对

推 `47ab952c7` 前发现**远端已被推走两个 commit**（`13375d421` → `a96c87ba2` → `c65bc9094`）。
审计发现远端对 `index.html` 改了 **102 处**，但**归一化后「新增 0 行 / 删除 0 行」** ⇒ 纯 CI 缓存戳 `?v=` + `BUILD` 注入，**无实质改动**。

本地 vs 远端双向断言：
- 远端独有 = **0 行** ⇒ 我不会覆盖任何人
- 本地独有 = **68 行**，100% 归因于周期档案功能 ⇒ 别人也没覆盖我

**处置**：把本地 `index.html` 的 CI 注入值（`?v=` 104 处 + `BUILD`）对齐到最新远端，**保留我方 68 行新增**，再推。

### 5.2 方向二「别被覆盖」—— 本机旧版文件硬拦

本机三个热文件经判为**旧版/回退版**，本批**严格排除**：

| 文件 | 归一化比对 | 判定 |
|---|---|---|
| `index.html` | 远端独有 0 / 本地独有 0 | 一致（CI 只改戳）✅ 可推 |
| `logic.html` | 远端独有 **4** | ❌ 本机旧版，**禁止推** |
| `guard_v8_freshness.py` | 远端独有 **26** | ❌ 本机回退版（远端有阿狸咪「>1MB 回退 blob API」治本），**禁止推** |

### 5.3 范围守卫

两次推送均带 `got ⊆ 白名单` 断言：
- `13375d421`：diff 出 **11 个文件**，全部命中白名单
- `47ab952c7`：diff 出 **1 个文件**（`index.html`），命中白名单

`47ab952c7` 首次推送被 `! [rejected] (fetch first)` —— 远端又漂移了，**重取 tip 重试成功**（fail-closed，非 force）。

### 5.4 线上核验

| 项 | 结果 |
|---|---|
| `origin/main` | `47ab952c7`，`merge-base --is-ancestor` ✅ |
| 线上 `index.html` | HTTP 200，**1,223,848 字节**（与本地完全一致） |
| 线上 `limitUpCycleBtn` | 命中 2 ✅ |
| 线上 `SECTOR_CYCLE_ARCHIVE.js` | HTTP 200，JSON 可解析，**39 列 / 26 周期 / 13 板块** ✅ |
| 线上档案横幅 | ⚠️ CI 压掉了头部注释（**符合仓库已知 CI 格式化行为**），数据本体 100% 正确 |

**⚠️ 给阿狸咪的提醒**：`data/SECTOR_CYCLE_ARCHIVE.js` 上线后**头部注释横幅会被 CI 压掉**。
判据**不要用字节数或 `grep 禁止删除`**，要用 `JSON.parse` 后的**内容**（`columns` / `cycle_count` / `cycles`）。

---

## 六、给阿狸咪的待办 / 注意事项

1. **本机 `logic.html` / `guard_v8_freshness.py` 仍是旧版** —— 若你要动这两个文件，**先拉齐再改**，别拿本机版本直接推。
2. **`raw_data/limit_up_heatmap.json` 已转永久累积** —— 不要再加回 `[-30:]` 之类截断，会自毁积累。
3. **新增受保护文件清单** `PROTECTED_FILES.json` —— 任何清理脚本/白名单必须**跳过**这 3 个文件；
   建议在你的清理任务里先跑 `python guard_protected_files.py --clean-check`。
4. **`update_v8.py` 已加映射** `"sector_cycle.json": "SECTOR_CYCLE_ARCHIVE"` —— 双机都要有，否则前端读不到。
5. 周期档案目前覆盖 **07/20~09/16（39 列）**，随每日热力矩阵刷新自动增长，**无需额外维护**。

---

## 七、未办事项（等主人拍板，不在本批）

- 个股查询「算法现场评分」卡 —— **云端链路选择待主人回答**（自托管 runner 按需触发 vs GitHub Actions `workflow_dispatch`）。其余已定：现场查零体积 + 三层结构（结论置顶 / 依据 / 双口径）。
- 孤儿渲染函数 CI 护栏是否上线 + `renderCandidateCard` 死代码是否删除。
- 风险温度计 cron 死班整改；今日盘中调度器 0 班**未根治**。

---

*小九 · 2026-09-16 12:22*
