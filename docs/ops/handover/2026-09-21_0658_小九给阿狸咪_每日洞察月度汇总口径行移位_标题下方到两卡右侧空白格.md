# 小九 → 阿狸咪 · 2026-09-21 06:58 · 每日洞察·月度汇总口径行移位（标题下方 → 两卡右侧空白格）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 采集时刻：2026-09-21 06:58（本机时钟 +8）。每条结论附可跑复核命令，**不写预测式承诺**。

---

## 一句话结论

主人令「把每日洞察底部的口径行，移到流入流出卡右侧的空白处」**已上线**
（`321b7b98b`，线上字节级核验通过）。改动集中在 `index.html` 的**每日洞察 · 月度汇总**区块（共振日历 TAB3），
**2 处定点替换**，**未涉任何算法/数据/判据**，logic.html 无需跟进。

---

## 一、改了什么

### 1.1 主人原话与诉求

> 「每日洞察底部的，把口径那行文字移到下方流入流出卡右侧的空白处」（附图：口径行在两卡**上方居中**）

**背景**：该区块 `.month-summary` 早已是 **3 列网格**（`repeat(3,1fr)`，为对齐上方「机游共振（短线）」三卡），
但里面**只有 2 个 `.ms-col`**（净流入TOP10 / 净流出TOP10）⇒ **第 3 列恒为空**，
而口径行当时浮在标题下方居中 ⇒ 视觉上「中间一条说明 + 右侧一块空白」。

### 1.2 改法（2 处定点替换，全在 `index.html` 每日洞察 IIFE 内）

| # | 位置 | 旧 → 新 |
|---|---|---|
| 1 | 行 4717–4722（旧） | 删除「标题下方居中」的口径块（3 行 `h +=` + 1 行注释），注释改为备案新口径 |
| 2 | 行 4740 之后（新） | 在两卡**之后**追加 `.ms-col.ms-note` 口径块 ⇒ 落位 grid **第 3 列 = 右侧空白格** |

**新增块（落位第 3 列）**：
```js
h += '<div class="ms-col ms-note" style="display:flex;flex-direction:column;justify-content:center;align-items:center;text-align:center;line-height:1.9">';
h += '<div style="font-size:10.5px;color:var(--dim)">口径：龙虎榜席位「机构 + 北向 + 游资」三方净买卖，同一股票当月多次上榜按金额累加</div>';
h += '<div style="font-size:10.5px;color:var(--dim);margin-top:6px">当月共 ' + _mDays.length + ' 个交易日</div>';
h += '<div style="font-size:10px;color:var(--dim);margin-top:10px;opacity:.85">仅供参考，不构成投资建议</div>';
h += '</div>';
```

> 复用 `.ms-col` 类 ⇒ **自动继承**卡片的 `background/border/border-radius/padding`，与两卡**视觉完全一致**
> （不新造样式、不特立独行，符合「code 行为一致」铁律）。

---

## 二、改前真实渲染复现（证明改的是真现场景）

按 `v8-real-render-verify` 口径，用**真实数据 + 真实样式 + 真实 IIFE**在真 Chrome（headless）渲染：

| 项 | 内容 |
|---|---|
| 数据 | `data/LHB_HISTORY.js`（1,739,612 B，**全量真实历史**） |
| 渲染 | `file://` 内联注入（⚠️ `file://` 下 Chrome **不加载外链 `<script src>`** ⇒ 必须内联） |
| 改前结果 | **完整复现主人截图**：口径行在两卡上方居中、**右侧第 3 列空白**（真实数据 2026-09-18 · 14 个交易日） |
| 改后结果 | 口径行落入**右侧空白格**，与两卡同高、内容垂直居中，三列对齐 |

> ⚠️ **踩坑记录**：`window._rcDays()` 不是简单读 `H.days`，而是 **`LHB_HISTORY` 以日期字符串为 key 的对象**，
> 需 `Object.keys(H).filter(k => /^\d{4}-\d{2}-\d{2}$/.test(k))` + 过滤 `trading===false` 后排序。
> 首版按 `H.days` 取 ⇒ `days.length===0` ⇒ IIFE 直接 `return` ⇒ **页面空白**（4.4KB 截图）。
> 另：IIFE 内的 `h` 是闭包变量，抽成独立函数时**必须自己声明 `var h=''` 并 `return h`**。

---

## 三、验证证据

### 3.1 补丁自检（数学级）

| 项 | 结果 |
|---|---|
| 逆运算还原 == base | **True**（新串逐处 replace 回旧串后逐字节 == base ⇒ 只做这 2 处，其余零字节未动） |
| `<div>` 开/闭 delta | **+3 / +3** · 开闭差 **0** |
| `<span>` 开/闭 delta | **+0 / +0** · 开闭差 **0** |
| 行尾 | **CR=0**（纯 LF） |
| bytes | 1,338,980 → **1,339,435**（+455） |
| `diff-tree` 差异文件 | **恰好 `['index.html']`**（零误伤） |
| 幂等键 | `class="ms-col ms-note"` 已在 base 则跳过重推 |

### 3.2 jsdom 真 DOM 断言

`verify_ms.js` 跑在改后真实渲染页上：**PASS=19 · FAIL=0 · ALL_PASS ✅**

关键断言：
- `.month-summary` 子元素顺序 = **`["COL","COL","NOTE"]`** ⇒ 口径块在**两卡之后**（= 第 3 列）✅
- 口径块 `display:flex` / `justifyContent:center` / `alignItems:center` ✅
- 三列网格 `repeat(3,1fr)` ✅
- **脚本源码中口径定义 == 1**（无重复）✅
- 旧注释 / 旧居中容器 **已清除**（count == 0）✅
- 无致命 jsdomError ✅

> ⚠️ **断言口径陷阱（本轮踩中）**：`d.body.innerHTML` **同时包含 `<script>` 源码与渲染结果**，
> 直接对 innerHTML 数关键词会**双计**（源码 1 + 渲染 1 = 2 ⇒ 误报「重复」）。
> **正确做法**：渲染结果用 `cloneNode(true)` 后 `remove()` 掉所有 `<script>` 再数 `textContent`；
> 源码则单独从 `querySelectorAll('script')` 取。

### 3.3 线上真身核验（改了 ≠ 生效）

| 项 | 实测 |
|---|---|
| 线上 `index.html` HTTP | **200** |
| live bytes | **1,339,435** |
| tip `321b7b98b:index.html` bytes | **1,339,435** |
| `BYTES_MATCH` | **True** ✅ |
| md5（归一化 `?v=`/`BUILD`） | live `2e3a5f76e…` == tip `2e3a5f76e…` ✅ |
| 线上 blob | `24450542d9d1a854ab064e7fcdf83580f4a7348a` == 本地 ✅ |
| 行尾 | CR=0 ✅ |
| 旧形态 | `自标题右侧移至标题下方`=0 · 旧居中容器=0 ✅ |
| 新形态 | `class="ms-col ms-note"`=**1** · 口径文案=**1** · 新注释备案=**1** ✅ |
| 线上逐行真身 | 行 **4740–4743** 即新口径块（4 行，文案齐备）✅ |

---

## 四、推送与防覆盖

| 项 | 值 |
|---|---|
| base（推前重取） | `65be41864e364d38b323dfc552f83dcc931b99be`（`ls-remote` 权威 sha） |
| 新 commit | **`321b7b98b050c183ca26057d50e33314ecdced18`** |
| 方式 | 隔离树（`GIT_INDEX_FILE` → `read-tree <sha>` → `update-index --add --cacheinfo` → `write-tree` → `commit-tree -p <sha>` → `push`），**非 force** |
| 推后 tip | `321b7b98b` == 期望 ✅ |

> 🔴 注意：`origin/main` 本地引用仍不可靠（上轮已证会静默消失，`update-ref` 报 rc=0 却不创建）。
> 本次全程 **以 `ls-remote` 的权威 sha 当 base**，**未使用 `origin/main`**。详见 skill `v8-raw-tree-push` §第 0 条。
> 另：本次 fetch 到 06:58 时 base 已从 `bb6215ba4`（06:45 首次取证）漂到 **`65be41864`** ⇒
> **每轮推送循环开头都重取 `ls-remote` 是必须的**，脚本已内建「重取 base + 重打补丁 + 幂等键检测」。

---

## 五、口径核查（`index.html` ↔ `logic.html`）

| 核查项 | 结果 |
|---|---|
| `logic.html` 是否有该区块的副本 | **无**（本轮只动 `index.html` 每日洞察 IIFE 的字符串拼接） |
| 是否涉算法/口径语义变更 | **否**——口径**文案与数值口径一字未改**，仅**位置**从标题下方移到右侧第 3 列 |
| 是否需同步 logic.html | **不需要**（纯布局/视觉位置调整） |

> 说明：本卡口径行的**内容**（三方净买卖定义、当月累加规则、交易日计数）**完全未变**，
> 移动的是 DOM 落位 ⇒ 两页**无口径分歧**。

---

## 六、复核命令（你接手时先跑，现取现验）

```bash
# 1. 远端权威 tip（勿信本地缓存 / 勿信 SHA 记忆）
git ls-remote origin refs/heads/main

# 2. 线上新口径块是否就位（应输出 1）
curl -s --max-time 100 "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$(date +%s)" \
 | grep -c 'class="ms-col ms-note"'

# 3. 线上旧居中口径容器是否已清除（应输出 0）
curl -s --max-time 100 "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$(date +%s)" \
 | grep -c 'margin-top:8px;font-size:10px;color:var(--dim);text-align:center;line-height:1.6'

# 4. 口径文案全站只 1 处（应输出 1）
curl -s --max-time 100 "https://ah-quant999.github.io/quant-scanner-v8/index.html?nocache=$(date +%s)" \
 | grep -c '口径：龙虎榜席位'
```

> ⚠️ 三条硬口径：
> ① 抓 1.3MB 线上文件 `--max-time` **必须 ≥100s**，且**禁 `-o` 落盘**（沙箱会拦），用纯管道；
> ② 判「某卡内是否有 X 形态」**禁用行号区间**，按**函数归属**检索；
> ③ 判覆盖**只认内容 blob 逐字节比对**（SHA 消失 ≠ 内容丢失）。

---

*小九的工程师（单位机 `lemoncat-cn`）· 2026-09-21 06:58 CST · 署名 2814546@qq.com*
