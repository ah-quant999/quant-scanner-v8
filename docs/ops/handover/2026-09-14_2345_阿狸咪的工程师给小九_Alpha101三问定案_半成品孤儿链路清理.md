# Alpha101 三问定案 + 半成品孤儿链路清理

- **发起**：阿狸咪的工程师（家用机 `alimi-cn`，夜间窗口）
- **时间**：2026-09-14 23:45
- **基线**：`FETCH_HEAD = af6ba2e16ec099ff150186688f2be568aa36ddaa`（origin/main）
- **上游指令**：主人 2026-09-14「1、2 按你推荐的做，3 可以先回测看情况再入仓么？3 结论已出，不好就删。」

---

## 一、三问结论（先定判据、后跑，全部有落盘证据）

### 问 1｜Alpha101 因子改作「Bottom 层排除项」——**不接入**

判据（先定后跑）：①覆盖率≥80% ②触发率 5%~60% ③被 flag 组 T+5/T+10 按日配对显著落后 ④剔除后组合收益提升且胜率不降。

| 检验 | 结果 | 判定 |
|---|---|---|
| 覆盖率 | 61.9%（554/895），**未达 80%** | ❌ 结论降级 |
| 触发率 K≥3 | 31.8%（随机基线 20.3%，+11.5pt） | ✅ |
| T+20 利差（K≥3） | −6.07% ~ −7.75%；按日配对 p=0.007/0.012；安慰剂分位 0% | ✅ |
| **T+5 / T+10 利差** | 仅 −0.69% / −0.84%；配对 p=0.353/0.433/0.715 | ❌ **不显著** |
| **样本外分半** | 前半 6/6 成立（T+20 −6.07~−7.75%）；**后半 8/8 反转**（+0.04~+4.17%） | ❌ **OOS 不同号** |
| beta 归因 | flag 组 β 1.23~1.35 vs 未 flag ~1.42，**只能解释约 1pt**，剩 6pt 是噪声 | ❌ 非 alpha |

⇒ 方向存在但**不稳定、且不能归因于截面 alpha**；按「上线前置须回测验证提升收益率+胜率」，**不接入**。

### 问 2｜扩充候选池（忠实实现 82 个 Alpha101 因子）——**不接入**

方法：按 101 Formulaic Alphas 附录 A.1 逐字实现 **82 个**可算因子（另 19 个依赖 `IndNeutralize`/`cap` 明确列名跳过），自写词法/语法树解析器 + 面板向量化，一次算完全部历史（413 日 × 711 只）。判据含 **Top−Bottom 利差>0 且 OOS>0**。

| K（同命中数） | 触发率 | T+10 超额 | T+20 超额 | T+30 超额 | **T+30 按日 t 值** | IS/OOS |
|---|---|---|---|---|---|---|
| ≥1 | 97.1% | +0.01% | +0.03% | +0.03% | +1.15 | ✅ |
| ≥3 | 79.2% | +0.05% | +0.13% | +0.16% | +1.24 | ✅ |
| ≥8 | 27.7% | +0.27% | +0.53% | +0.75% | +1.36 | ✅ |
| K≥12 | 8.8% | +0.34% | +0.61% | **+1.38%** | **+1.70** | ✅ |
| K≥20 | 0.3% | −0.98% | −3.79% | −3.27% | −0.78 | 命中率不可用 |

- **全部 |t| < 2**，无一在 5% 水平显著（t 值按「每日超额」序列算，日期为独立单位，规避重叠窗口伪显著）；
- 唯一 |t|≥2 的一行（K≥1, T+20, t=+2.01）超额只有 **+0.03%**，经济上等于 0；
- 满足「触发率 ≥5%」的档位只到 K≤12，而 K≥12 的 t=1.70 **仍不显著**、命中率仅 8.8%；
- ⇒ **无统计显著的多头超额，不接入**。

> ⚠️ **诚实更正**：本人 2026-09-15 0115 交接单结论为「共识组合无多头超额，暂不接入」。该结论方向正确，但其依据是**12 个简化改写因子**且**未加利差门槛**；本轮用忠实实现 82 因子重跑后，发现「门槛越高超额单调越大且 IS/OOS 同号」这一现象，一度倾向修正结论。补算**按日 t 值**后确认：**现象真实但未达显著，原结论（不接入）成立**。特此更正并留痕，避免后续误读为「已验证有超额」。

### 问 3｜长历史数据入仓（`_rps_cache`）——**否掉，且「不好就删」已执行**

三本账（`_rps_cache`，413 条日线）：

| 档位 | 独立样本点 | 可信度 |
|---|---|---|
| T+30 | 10 | ✅ 可信（但仅 4/8 因子分半同号） |
| T+60 | 4 | ❌ |
| T+90 | 2 | ❌ |
| T+180 | 0 | ❌ |
| T+250 | 0 | ❌ |

- **瓶颈是历史长度本身只有 413 条，不是「云端读不到」** ⇒ 把 `_rps_cache` 入仓**不提高任何长周期结论的可信度**；
- 且 `.gitignore:39` 忽略 `_rps_cache/` 系 2026-09-08 主人令（「仅 kline_cache 一项入仓」），入仓即违令；
- ⇒ **入仓这条路否掉**。

**长周期可信的正确路径（无需任何新数据入仓）**：

1. **`BACKTEST_TDX` 已有一条真实长周期面**：`baostock 520 根(≈2年) × 230 只`，**T+250 有效点 21495**（T+90 35144 / T+180 27155）。但 ——
   🔴 **它只出绝对收益，没有基准/利差行**（`hs300` 仅用作 regime 分类，`algorithms/backtest_tdx.py:192`；`avg_return = total_ret/total` 无基准扣减，见 `:822`）。因此 `trend_up` T+250=+0.87% **小于** `trend_down` T+250=+6.31% 这类反直觉值无法判读 —— 测的是 **beta 不是 alpha**。
   ⇒ **补「基准行 + Top−Bottom 利差」是纯代码小改、零新数据**，直接把已有 21495 点变成可判读的长周期证据。
   ⚠️ 同时注意：21495 是**重叠观测**，独立单位 ≈ 230 只 × ⌊520/250⌋≈460 ⇒ 胜率 95%CI ≈ ±4.6pt，不是 ±0.7pt。
2. **`factor_lab_backtest.py` 持有期档位仍只有 `HOLDS=[5,10,20]`**（`:43`），主人要求的 30/60/90/180/250 **从未落地**。前端档位表是**动态发现**（`index.html:4826-4835`，扫 `win_Nd` 键）⇒ 算法层补多少档就显示多少档，**只差算法层**。
   🔴 补档有个硬约束：该脚本**每票只拉 700 日**（`:63`），而档位守卫是 `n < NEED_MIN(261) + 2×HOLDS[-1]`（`:172`）⇒ 加 250 档需 **761 根**，会**全票不达标 = 0 样本**。**补档必须同步提升抓取窗口**。

---

## 二、本次执行的「删」（主人令：不行的就删除，剔除噪声留下精华）

已核实为**完全孤立的半成品链路**，且偏离路线图（`alpha101_v8_analysis.md` 明确指定落点为「在 `factor_lab_backtest.py` 中新增因子」）：

| 文件 | 删除前状态 | 证据 |
|---|---|---|
| `algorithms/alpha101_backtest.py` | 远端已入仓 22001 B | **未登记** `run_algorithms.py` ORDER/STAGES；**无前端引用**；**无 `data/*.js` 映射** ⇒ 产物云端永不更新 |
| `raw_data/alpha101_backtest.json` | 远端已入仓 69187 B | 同上；且其 T+60/90/180/250 结论经本轮验证**不可信** |
| `algorithms/alpha101_signals.py` | **本地孤儿**（未入远端） | 消费上面二者，产出恒空（本地删除） |
| `raw_data/alpha101_signals.json` | **本地孤儿** | 内容 `universe_size: 0`、`signals: []` = **「挂链但恒空」假接入**（本地删除） |

**保留** `alpha101_v8_analysis.md`（小九写的 4 阶段路线图，仍有参考价值）。

> ⚠️ 判「是否入仓」本轮踩过坑：本地 `git status` 显示这 4 个文件全为 `??`（未跟踪），一度误判「都没入仓」。实际是**本地 HEAD `cfbc62dd` 落后于 origin/main `af6ba2e1`**，`git status` 只比 index 不比远端 ⇒ **静默撒谎**。改用 `git cat-file -e FETCH_HEAD:<path>` 才得真相。**判入仓一律只认远端对象。**

---

## 三、本轮未做（明确不作，避免膨胀）

- 不改 `factor_lab_backtest.py` 档位、不给 `BACKTEST_TDX` 补基准行 —— 两项均为**跨三处改动**（生成器 + `data/*.js` + 前端/logic.html），且**产物须由云端交易日产出**（本机跑批不产产物）。留作下一步，待主人点名。
- 未动仓库根 56 个 `_*.py` 临时脚本 —— 观察到疑似噪声堆积，**仅记录**，未取证前不删。

---

## 四、请小九核验

1. `git cat-file -e af6ba2e1:<被删路径>` 应为 **不存在**；`git log --oneline -1` 应见清理提交。
2. 本次**仅删 2 个远端文件**，无任何生成器/前端改动 ⇒ **不影响任何线上卡片**（该链路本就无前端入口）。
3. 若你在 `alpha101_backtest.py` 上有未提交改动，**请勿强推**，改为在 `factor_lab_backtest.py` 里实现（对齐路线图）。
