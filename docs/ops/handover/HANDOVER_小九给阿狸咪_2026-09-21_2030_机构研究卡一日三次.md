# HANDOVER · 小九 → 阿狸咪 · 机构研究卡「方案B·一日三次」落地收口

> 发件：小九（2026-09-21 20:30 CST）
> 收件：阿狸咪（夜间 18:00–次日 07:30 + 周末 值班）
> 主题：机构研究·AI 解析建议卡 出稿频次从「一日一版（仅盘前 08:50）」升级为「一日三档（盘前 08:50 / 午间 12:30 / 盘后 15:10）」，并完成改后三件套 + 双页口径对齐。

## 零、一句话总纲

机构研究卡已按主人 09-21 拍板「一天一共 3 次」落地：三档自动化全部 ACTIVE、前端徽章/空态/限制行文案已改三档口径并在 churn 链下存活、logic.html 旧架构口径已对齐到真身；唯一未闭环是 **12:30 / 15:10 两档尚未跑过真实 run**（建于晚间，首次出稿在 09-22），HANDOFF 该项停留 `pending-verify`。

## 一、行尾铁律表（本批涉及文件）

| 文件 | 行尾 | 验收 |
|---|---|---|
| `index.html` | LF | 本次未改（只读核验） |
| `logic.html` | CRLF | 裸 LF = 0 ✓（8613 CRLF 不变） |
| `docs/ops/HANDOFF.yaml` | LF | CRLF = 0 ✓ |
| `docs/ops/handover/*.md`（本文档） | LF | CRLF = 0 ✓ |

## 二、本批改动总表（圈定：主题 + 时段 + 交接档，不靠 git author）

| # | 文件 / 对象 | 改动 | 落地方式 | 状态 |
|---|---|---|---|---|
| 1 | 自动化 `30e1b099` | 盘前 08:50 出稿（既有，沿用） | WorkBuddy 自动化 | ACTIVE |
| 2 | 自动化 `c613b8ad` | 午间 12:30 出稿（新建·方案B第三档） | WorkBuddy 自动化 | ACTIVE |
| 3 | 自动化 `a1542082` | 盘后 15:10 出稿（新建·方案B第二版） | WorkBuddy 自动化 | ACTIVE |
| 4 | `index.html` | 6 处文案改三档口径（主卡徽章/对比卡徽章/限制行/注释/空态/说明） | Contents API 单文件直推 `df1b85376a` | 已上线·存活 |
| 5 | `docs/ops/HANDOFF.yaml` | 登记 `maharo-plan-b-three-editions`（pending-verify）+ 修正 commit 引用 | 隔离单文件 API | 已上线 |
| 6 | `logic.html` | 2 处旧架构口径对齐（数据源 + 一日三档） | 隔离单文件 API（本交接同批推送） | 已上线 |
| 7 | 本文档 | 交接归档 | 隔离单文件 API | 新建 |

## 三、核心技术改动深挖

### 3.1 为什么是三档（方案B·一日三次）
- 主人 09-21 拍板：「那12点30再加一次，一天一共3次」→ 定档 **盘前 08:50 + 午间 12:30 + 盘后 15:10**。
- 实测覆盖提升：盘前 08:50 覆盖素材主峰 61.5%（改前 07:15 仅 14.2%）；盘后 15:10 覆盖 60.2%（+755 条）。午间 12:30 补盘中峰（11–14 时）。
- 每档出稿前先 `v8_guanlan_refresh.py --force` 强刷素材，再按 `date_range` 判新鲜度；weekly/monthly 沿用 base 不重算。

### 3.2 覆盖事故根治（本批最深坑）
- 首推 `a8a47a0a57`（徽章 6 处）回读逐字节一致，但被后继 `0edaa61d7`「v8 cn fetch 19:41」链用**提交前 checkout 的落后基线**整文件回推，静默冲掉我的改动（09-20 同型 `0d3482be4` 事故先例）。
- 修复：基于最新 tip 重放补丁 → `df1b85376a`（VERIFY OK 1348386 B）。监视器 `QrBsGm` 跑满 8 轮（20:07–20:21，HEAD 推进 8 次），徽章计数恒 `badge=3`、未被再覆盖 ⇒ 已根治。

### 3.3 logic.html 口径对齐（三件套②抓出的真问题）
- **真身（index.html 权威）**：机构研究·AI 解析建议卡读 `window.MAHORO_INSIGHTS`（=`data/maharo_insights.js`），由小九机自动化一日三档产出；旧 `maharo_daily_pull.py`→`maharo_macro.js`（每日 08:30）架构已于 2026-09-13 随注入行删除退役。
- **logic.html 过期口径**：1357 / 2044 两行仍写「数据源 `data/maharo_macro.js` … 每日 08:30 由本机 maharo_daily_pull.py 拉取生成」，与真身打架。
- 已改：两行统一为「`data/maharo_insights.js`（window.MAHORO_INSIGHTS），由小九机自动化一日三档（盘前 08:50 / 午间 12:30 / 盘后 15:10）产出；旧架构已于 2026-09-13 退役」。旧文案残留计数 = 0。

## 四、新功能 / 交付物（线上实测）

- 三档自动化 `30e1b099` / `c613b8ad` / `a1542082`：rrule 分别为 `DAILY 08:50`、`WEEKLY 工作日 12:30`、`WEEKLY 工作日 15:10`，全部 ACTIVE。
- 前端徽章（HEAD `c9a2af7a3` 实测）：主卡徽章「日更 · 盘前 08:50 / 午间 12:30 / 盘后 15:10」、限制行「三档出稿」、对比卡同源同步，旧「每日 08:30 出稿」残留 = 0。

## 五、之② index↔logic 口径核验结果

| 关键词 | index.html | logic.html | 判定 |
|---|---|---|---|
| 盘前 08:50 / 午间 12:30 | 3 | 0（业务词，非标识符，正常） | ✅ |
| 每日 08:50 / 12:30 / 15:10 | 3 | 0（业务词，正常） | ✅ |
| 数据源 maharo_insights | 是（真身） | 改前写 macro.js（错）→ 改后 insights.js | ⚠️→✅ 已修 |
| 一日三档出稿 | 是 | 改前 08:30（错）→ 改后 三档 | ⚠️→✅ 已修 |

> 规则复核：code 标识符（如 `maharoCard`）在 logic 说明页命中 0 属正常（说明页用业务词）；本次不一致是**数据源与频次**两级业务事实，已对齐。

## 六、遗留待办

| 时间窗 | 项 | 负责人 | 说明 |
|---|---|---|---|
| 09-22 12:30 | `c613b8ad` 首次真实出稿 | 自动化自跑 | 查 `data/maharo_insights.js` update_time 应为当日 12:3x |
| 09-22 15:10 | `a1542082` 首次真实出稿 | 自动化自跑 | 查 update_time 应为当日 15:1x；卡面「更新于」随之刷新 |
| 09-22 收口 | HANDOFF `maharo-plan-b-three-editions` pending-verify → done | 小九 | 两档跑通 + 闸门 36 项全过后转 done |
| 挂账（根因） | cn fetch 链用提交前 checkout 整文件回推，可能再冲他人改动 | 仓内协调 | 本卡表现已不回退，但根因未除；参照 09-20 `0d3482be4` 根治方向 |

## 七、给阿狸咪的特别提醒

1. **写仓窗口铁律**：09:15–11:30 / 12:45–15:00 禁推 main；12:30 午休窗、15:10 收盘后窗可直推（不进 staging）。本批推送已在 20:30 非窗口期完成。
2. **覆盖事故判据**：若未来发现 index.html 机构研究卡徽章被改回「每日 08:30」，**先 `git show origin/main:index.html` 取真身、比对 HEAD**，别凭 Pages curl（跨境截断 + 巡检 slots 列表里的 08:30 会干扰）。真因是某链用落后基线回推，不是本批改动失效。
3. **验证方式**：改后三件套的「线上真身核验」一律用 `git show origin/main:<path>` 或 Pages 直取（分块读防截断），不靠状态推断。
4. **环境坑**：本机 `python3` 子进程调 `git` 在沙箱 PATH 异常；取文件真身请用 Bash `git show` 直连，勿用 python subprocess 调 git。

## 八、文件状态快照（推送前本地）

| 文件 | 字节 | 行尾 | 备注 |
|---|---|---|---|
| `E:/.../logic_now.html`（→ `logic.html`） | 758654 | CRLF 8613 / 裸LF 0 | Δ+326 vs origin |
| `E:/.../HANDOFF_now.yaml`（→ `docs/ops/HANDOFF.yaml`） | 133288 | LF | Δ+75 vs origin |
| 本文档 | — | LF | 新建 |

## 九、本文档自身

- 推送方式：隔离单文件 API（blobs/trees/commits/ref，force=false，422 换 tip 重推最多 6 轮），与 logic.html、HANDOFF.yaml 同批一个 commit 推送。
- 校验方法：推送后 `git fetch --depth=1 origin main` + `git show origin/main:<path>` 逐字节比对本地；徽章存活用 `grep -c "盘前 08:50 / 午间 12:30"` 在 origin/main 上计数应为 3。
- 不在本文档写代码细节、不留回执档；状态变更走 `docs/ops/HANDOFF.yaml`。
