# 小九 → 阿狸咪 ｜ 防覆盖审计 + 每日洞察口径行移位三件套收口

- **发起方**：小九（白班机 · 2026-09-21 07:05 CST）
- **接收方**：阿狸咪（夜/周末机）
- **主题**：主人令「每日洞察口径行移位」上线的**防覆盖审计**结果与交接
- **性质**：审计报告 + 交接（**无待办**）

> ⚠️ 本文一律写「截至某时刻实测值」，不写预测式表述、不写本文自身所在提交 sha。

---

## 一句话结论

主人令「每日洞察·月度汇总口径行移位」改动**已上线且抗覆盖**：线上 `index.html` blob 与我方推送方**逐字节一致**，`data/` `raw_data/` `algorithms/` 三个数据目录 **TIP_only 全为 0（零内容丢失）**。本次审计**未发现任何覆盖事故**。

---

## 一、改了什么（承 06:58 交接档）

主人原话：

> 「每日洞察底部的，把口径那行文字移到下方流入流出卡右侧的空白处」

**根因**：`.month-summary` 早已是 `repeat(3,1fr)` 三列网格（为对齐上方「机游共振（短线）」三卡），但内部**只有 2 个 `.ms-col`** ⇒ 第 3 列恒为空。口径行原本浮在标题下方居中。

**改法（2 处定点替换，仅 `index.html`）**：

| # | 位置 | 动作 |
|---|---|---|
| 1 | 标题下方居中口径块 | **删除**（3 行 `h += ...`） |
| 2 | 两卡**之后**（= grid 第 3 列） | **插入** `<div class="ms-col ms-note">`，复用 `.ms-col` 类继承卡片外观；`flex` + `justify-content:center` 实现同高垂直居中 |

---

## 二、防覆盖审计（本次核心 · 截至 2026-09-21 07:05 实测）

### 2.1 线上权威基线

```
ls-remote origin refs/heads/main
→ 481fc4dd1a5ddeec4f1c61f062bb7522edd34bce   (refs/heads/main)
```

> 🔴 一律以 `ls-remote` 的权威 sha 为准 —— `refs/remotes/origin/main` 会静默消失，
> 不可当 base 使用。

### 2.2 交付物 blob 三方对表（**核心判据**）

| 交付物 | 线上 tip blob | 我方推送 commit blob | 期望值 | 一致 |
|---|---|---|---|---|
| `index.html` | `24450542d9d1` | `24450542d9d1` | `24450542d9d1` | ✅ **True** |
| `docs/ops/handover/...0658....md` | `a9e0c1285fee` | `a9e0c1285fee` | `a9e0c1285fee` | ✅ **True** |

> 🔴 **判覆盖只认内容 blob 逐字节比对，不认 commit SHA** ——
> 云端「回执式整合提交」会收编 commit ⇒ **对象消失但内容未丢**。

### 2.3 覆盖闸门：`tip_set - local_set`

| 目录 | TIP 文件数 | 本地 HEAD 文件数 | **TIP_only（真丢失）** |
|---|---|---|---|
| `data/` | 125 | 125 | **0** ✅ |
| `raw_data/` | 4993 | 4996 | **0** ✅ |
| `algorithms/` | 85 | 85 | **0** ✅ |
| `.github/workflows/` | 30 | 30 | **0** ✅ |
| `docs/ops/handover/` | 529 | 511 | 18（**均为云端新推**，非丢失） |

**全仓合计**：TIP=6024 / HEAD=6008 / `TIP_only`=19。

`TIP_only` 的 19 条经逐条甄别，**全部为「本地 HEAD 落后」性质**：

- 18 条 = `docs/ops/handover/` 云端新写的交接档（含本文所回执的 0658 档）
- 1 条 = `backup/raw_data_backup_20260921_0146.tar.gz`（云备份快照）
- **数据/算法/workflow 类文件 零丢失**

**本地独有（未推送）3 条**，均为 `.bak` 残留，**非交付物、无需推送**：

```
raw_data/gold_pool.json.bak
raw_data/lhb_data.json.bak
raw_data/sector_rs.json.bak
```

### 2.4 热文件未被回退

| 项 | blob |
|---|---|
| 推前基线 `65be41864` 的 `index.html` | `c794f96e8fb86094e98a6a02fbc159156c850b07` |
| 我方推送后 `321b7b98b` 的 `index.html` | `24450542d9d1a854ab064e7fcdf83580f4a7348a` |
| **当前线上 tip 的 `index.html`** | **`24450542d9d1a854ab064e7fcdf83580f4a7348a`** |

⇒ 推送后经历 `v8 build` / `v8 risk gauge` / 缓存戳对齐等多个 bot 提交，`index.html` **仍是我方那个 blob**，**未被任何后续提交回退或覆盖**。

### 2.5 线上真身内容核验（抓 blob 全量文本）

```
LIVE index.html blob size = 1339435 bytes / 1110304 chars
```

| 关键串 | 期望 | 实测 |
|---|---|---|
| `ms-col ms-note` | 1 | **1** ✅ |
| `2026-09-21 主人令：口径说明落位第 3 列` | 1 | **1** ✅ |
| `当月共 ` | 1 | **1** ✅ |
| `自标题右侧移至标题下方`（旧形态） | 0 | **0** ✅ |
| `CR_count` | 0 | **0** ✅ |

> `仅供参考，不构成投资建议` 实测 2 次 = 本卡口径 1 次 + 站内其它卡 1 次（既有文案，非重复缺陷）。

行尾：`CR=0` / `bare_LF=18962` ⇒ **纯 LF，符合 `index.html` 行尾铁律**。

---

## 三、改后三件套收口状态

| # | 三件套项 | 状态 | 证据（截至 2026-09-21 07:05 实测） |
|---|---|---|---|
| ① | 对齐云端/双机 | ✅ | `index.html` 线上 blob == 我方 blob；云端已收编 |
| ② | 对齐 index.html ↔ logic.html 口径 | ✅ **无需跟进** | 本次仅动 `index.html` 每日洞察 IIFE；`logic.html` **无该区块副本**；**口径文案与数值口径一字未改**（仅位置变化） |
| ③ | 写交接档 | ✅ | 06:58 档（`a9e0c1285`）+ 本文 |

---

## 四、复核命令（可跑，非承诺）

> 🔴 交接档禁止预测式表述 —— 以下均为**现取命令**，请以你自己执行时的实测值为准。

```bash
# 0) 取线上权威 sha（不要用 refs/remotes/origin/main）
git ls-remote origin refs/heads/main

# 1) 取 tip 真身并核验 index.html blob（与 24450542d9d1 比对）
git fetch origin <上一步拿到的 sha> --depth=1
git rev-parse FETCH_HEAD:index.html

# 2) 覆盖闸门：tip_set - local_set（各数据目录应为 0）
git ls-tree -r --name-only FETCH_HEAD data/      | wc -l
git ls-tree -r --name-only FETCH_HEAD raw_data/  | wc -l
git ls-tree -r --name-only FETCH_HEAD algorithms/| wc -l

# 3) 线上真身内容核验（Pages 直取，--max-time 必须 >=100s）
curl -s --max-time 120 -o /tmp/live.html \
  https://ah-quant999.github.io/quant-scanner-v8/index.html
grep -c "ms-col ms-note" /tmp/live.html                          # 期望 1
grep -c "自标题右侧移至标题下方" /tmp/live.html                   # 期望 0
```

**核验通过判据**：第 1 步 == `24450542d9d1a854ab064e7fcdf83580f4a7348a`；
第 2 步三个目录 `TIP_only` 均为 0；第 3 步两个 `grep` 分别得 1 与 0。

---

## 五、踩坑提示（本轮实测，供后续复用）

1. 🔴 **shallow 克隆下 `git merge-base --is-ancestor` 会给假阴性**：
   本轮 `321b7b98b` 实际**已在 main 上**（GitHub API 证实），但该命令返回 rc=1。
   **判「是否已收编」走 GitHub API `/commits?sha=main` 或内容 blob 比对，不要靠 `merge-base`。**
2. 🔴 **`git log` 在 shallow 下只给 1 条**（`481fc4dd1` 的父是 `4e752855f`，
   但本地只有 depth=1 的那个对象）⇒ **看历史必须走 GitHub API**。
3. 🔴 本轮 Bash 工具 PATH 崩坏（`dirname/grep/head/tail` 全 not found，exit 127）
   ⇒ 全程改 **Python(subprocess) + git 绝对路径**，结果写文件后用 Read 工具读。

---

## 六、待办

**无。** 本轮回执即收口。

另注（**非本轮引入、仅登记不擅动**）：`docs/ops/handover/` 已有 529 份档，
若主人后续要求做交接档归档整理，再另行发起 —— 本文不做任何删改动作。

---

*交接档撰写时刻：2026-09-21 07:05 CST ｜ 数据采集时刻：2026-09-21 06:45–07:05 CST*
*以上所有结论均为该时段内实测值；请以你自己执行第四节命令的结果为最终依据。*
