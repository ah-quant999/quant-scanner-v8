# 小九 → 阿狸咪 交接文档 · 2026-09-08（12:00 全量版）

> **更新于 2026-09-08 12:04 自动更新**（automation 1785719698947 · 12:00 槽位）
> 本文件为 10:35 版（FACTOR_LAB 修复专项）的整份覆盖更新，含上午全部改动 + 实时状态核实。
> 数据时效：origin/main SHA、站点 HTTP、线上 update_time 均为 **12:02-12:04 实测**，非记忆值。

---

## 一、背景

- 双机分时：小九白班（7:45-17:45）→ 阿狸咪夜班（18:00-次日 7:30 + 周末）。本档供阿狸咪 18:00 上岗时快速对齐当日状态。
- 今日主线（按时间序）：凌晨清红灯 → 盘前/盘中「更新→部署」及时性盯防 → STOCK_QUOTE 断档 8h 根因与根治 → FACTOR_LAB 5.4h 静默挂起真根因修复 → STOCK_QUOTE 被 cn_fetch 覆盖回滚事故根治 → AI速览死行审计。

## 二、今日已完成改动（真实 commit SHA，git log 实测）

### 2.1 凌晨清红灯（00:00-04:00，fail 6→2）
| 红灯 | 根因 | 处置 |
|---|---|---|
| SH_FIB / INST_TRADE / H_AUTO_BUY | 盘后 A/B 档未重跑 | cn 快链 stage=A(00:15 success)、stage=B(01:46 success) 补齐 |
| maharo_macro | 文件名 `maharo_macro.js` ≠ window 变量 `MAHORO_MACRO` | `v8_health_check.py` `_WINDOW_VAR_ALIASES` 加别名 |
| INDEX_HISTORY | `update_v8.py` DATA_SOURCES 缺 `index_history.json→INDEX_HISTORY` 映射，raw 新鲜但 js 停 09-05 旧壳 | 补映射 |
| MARKET_PATH_PROBABILITY | 无产出脚本+前端 0 引用的孤儿死数据 | 已删除 |
| FACTOR_LAB | 见 2.3 真根因修复 | 已根治并上线 |

### 2.2 STOCK_QUOTE 断档 8h（01:51→09:58）三层根因 + 根治
1. GitHub cron 三修后今晨仍零触发——**结构性重注册也不保证恢复**，弃 cron 依赖；
2. 兜底脚本 `82038f74` 假阳性互踩：runs 查询缺 `_cb` → 缓存卡旧 24h → 四档全部误判跳过 → 升 v2（`74077cbe`：加 `_cb` + fail-open 派发 + 裁 15 点后死档）；
3. 云端美国 IP 被新浪/东财反爬 → 09:50 首次手动派发失败，09:56 重试 success → 线上 update_time 09:58:49 闭环。
- **11:13 追加事故与根治**：手动补抓的新鲜版（11:13）被 cn_fetch 用陈旧 raw_data/stock_quote.json **覆盖回 09:58** → `4ffa2cf8f`（11:45 前后）从 update_v8 DATA_SOURCES 移除 STOCK_QUOTE 映射，**v8_stock_quote_refresh 工作流为唯一写入方**，根除覆盖链路。
- **12:02 实测线上 STOCK_QUOTE update_time = 11:37:27** ✅ 正常出数。

### 2.3 FACTOR_LAB 5.4h 静默挂起真根因修复（10:05-10:35，commit `3a8ceba5c`，push 91571c37e..2ea0903a2）
- **真根因**：`resolve_data_date` 在「先查 0 行日→再查回退日」时 baostock 连接态被污染 → `rs.next()` 静默死等 5.4h（非冷启动慢/限流）。
- 修复 4 处：回退探测前 `bs.logout()+bs.login()` 重置连接态 / 够数即停 / 总超时 300s 硬上限 / socket 超时 20→30s。
- 验证：烟雾测试 2.0s（原 5.4h）；生产跑批实跑生效（数据日 09-07，重点池 378、异常换手率 453）。
- 跑批：`V8_FLAB_BUDGET=800` 增量续跑，缓存 `raw_data/flab_work` 入仓共享，脚本自带原子 commit+rebase+push（仅 add index.html / data/FACTOR_LAB.js / raw_data/factor_lab.json）。
- 定时刷新已进 main：`971f18f72`（异常换手率重点池 + ROE 全市场主板）。

### 2.4 防覆盖与缓存戳系列（11:21-11:59）
- `49cfd6092` / `c86d91ee3` / `016088640`：生成器原子提交 + 缓存戳 `?v` 实时对齐防覆盖。
- `28b8d0bc9`（11:59）：最新 build，**当前 origin/main HEAD**。

### 2.5 盯防哨兵（新基建）
- 新建 `C:/Users/Administrator/qs8-tmp/v8_deploy_watch.py`：判据 build head_sha 覆盖 main / 部署滞后 / 抓取滞后 / 线上 HEALTH 新鲜度 / HTTP；`--heal` 自动 dispatch build_deploy。
- 分时段阈值：盘前 20min、盘中 10min、其余 30min；配套 automation `e7b2e732`（每 10 分钟，交易日 08:00-15:55，带自愈）。

### 2.6 AI速览「好几天没变动」审计（10:19，主人截图质询）
- 数据本身每天刷新（gen_time 10:05:29），是**头部定性结论死行**：①「日内风向」恒震荡（classify_sentiment 阈值兜底）；②「资金健康度」恒大幅流入（main_net 口径无区分度）；③「操作建议」恒观望（连锁 bug）。
- 修复方案 3 处纯逻辑升级已出（涨跌比优先定级 / 窄幅偏多分支 / 净额口径+300亿阈值），**待主人拍板是否现在改+上线，未动**。

## 三、当前系统状态（12:02-12:04 实测）

| 项 | 状态 |
|---|---|
| origin/main HEAD | `28b8d0bc9`（v8 build 11:59） |
| 站点 | HTTP 200（0.48s）✅ |
| 线上 HEALTH_CHECK | update_time 11:59:09 ✅ 新鲜 |
| 线上 STOCK_QUOTE | update_time 11:37:27 ✅（断档根治后正常） |
| 本地 HEAD | `4ffa2cf8f`（落后 origin/main 1 个 build 提交，见 4.1） |
| runner | lemoncat-cn 白班在线（上午 11:43 上报正常） |
| 健康检查（11:04 看门狗） | 108 ok / 1 warn / 1 fail：NT_DATA 翻绿结案；残留 FACTOR_LAB 待今晚 E 批；新 warn=RUNNER_STATUS 缺时间戳（P3 待授权） |
| 分红刷新 | 云端连续 5 连成功（05:04→07:48，3712 只含分红），零兜底 |
| 盘中漏轮守卫 | 10:04 首次窗口内有效执行：盘中 7 卡全新鲜（age ≤26min）；11:08 发现 age 63-70min 但云端 run 34181672696 in_progress → 按铁律等待，未派发 |
| 调度器漂移 | 深夜守卫连续 17 次窗口外漂移触发（约 1h 间隔），调度器积压嫌疑，已移交主人 |

## 四、⚠️ 本地未提交改动（阿狸咪上岗前必读）

### 4.1 小九单位机工作树 WIP（待主人「上线」令，任何人不得 stash/checkout/覆盖）
| 文件 | 内容 | 性质 |
|---|---|---|
| `index.html`（11 行） | ①「⚠️ 已停更 N 分钟」红色徽章（数据 >30min 陈旧时显示，点击硬刷新）；② HEALTH_CHECK 前端 fetch 缓存击穿修复（`_ts=` 时间戳） | 真实功能改动 |
| `v8/factor_lab_gen.py`（67 行） | 推送加固：推送前 stash 全工作树（含未跟踪）→ 强同步 origin/main，杜绝 rebase 因 WIP 失败导致数据算好上不了线 | 真实加固改动 |
| `.github/workflows/v8_stock_quote_refresh.yml`（10 行） | **回迁 [self-hosted] 小九单位机（中国 IP）**——云端美国 IP 反爬是断档根因之一；不再依赖 GitHub schedule，统一由哨兵每 10 分派发 | 真实改动 |
| `.github/workflows/v8_stock_quote_refresh_cloud.yml`（未跟踪新文件） | 云端兜底工作流：runner 接单超时 → 哨兵改派云端 | 新文件 |
- 12:00 交接自动化实测：`git fetch origin` 成功，`git merge --ff-only` 因上述 index.html WIP **被 git 原子中止（工作树未动，无损失）**。本地落后 origin/main 1 个 build 提交，待 WIP 上线后一并对齐，**阿狸咪不要强行 stash/merge**。

### 4.2 工作树其他「modified」
- `update_v8.py` raw diff 2402 行，但 `git diff --ignore-cr-at-eol` **零内容差异**——纯 CRLF 行尾噪声，勿 `git add`。
- 未跟踪旧文件：AUDIT_小九_2026-09-03~07、HANDOVER_小九_2026-09-01/03/08——历史文档，按纪律不入库。

## 五、待办 / 风险（移交阿狸咪 + 主人）

1. **FACTOR_LAB 今晚 E 批验证**：看门狗残留唯一 fail，跑批链路今晚出结果后确认翻绿。
2. **HERDING_DATA 类别改 post_close**：消除 runner 空 1 模块 warn（待授权）。
3. **RUNNER_STATUS 缺时间戳 warn（P3）**：待授权修复。
4. **AI速览 3 处逻辑修复**：方案已出，待主人拍板。
5. **NT_DATA 盘前 180min 豁免根治**：留有空时处理。
6. **调度器深夜窗口外漂移**（守卫 1h 间隔触发 17 次）：调度器积压嫌疑，移交主人跟进。
7. **旧遗留待拍板**：板块周期卡方案 A / 超跌修复腿算法 / ROE 串行网络风暴彻底优化。
8. **今日新坑务必牢记**：①Windows `io.open(w)` 默认 CRLF 污染仓库 LF（虚增 2384 行 diff），改仓库文件一律二进制写回 LF；②`git rebase --autostash` 冲突中断后 autostash 记录消失、改动全丢——改码一律单脚本内完成改+add+commit+fetch+rebase+push。

## 六、小九监督职责（白班剩余时段）

- 持续盯防：部署哨兵 `v8_deploy_watch.py`（automation e7b2e732，每 10 分钟）+ 盘中漏轮守卫（1787108824799）+ 看门狗（1785835481750）+ 19:00 审计。
- WIP 四文件守到主人「上线」令后按单脚本原子链提交推送。
- 本档 18:00 槽位将再次自动更新（automation 1785719698947）。
