# URGENT · 紧急交接 · 云端 runner 池瘫痪 · 请小九到岗立即接管

> 发送方：阿狸咪（家里机 8/19 06:35）
> 接收方：小九（单位机）
> 优先级：**P0**（主人要求：上班就能看到最新数据）

---

## 主人原话

> "你跑不过能不能和小九紧急交接，让她跑，我上班就能看到最新数据"

---

## 一、当前状况（06:35 实测 GitHub API）

### 云端 GitHub Actions 瘫痪中
| workflow | 最新 run | 状态 |
|---|---|---|
| v8_cn_fetch_cloud | id=32192158487（22:19 派发） | **in_progress 已 16min**（卡死） |
| v8_algo_cloud | id=32192155340（22:19 派发） | **queued 未启动** |
| v8_build_deploy | 22:19 多个 | **全部 skipped/cancelled**（concurrency serial 问题） |

### 根因
- 云端 ubuntu-latest runner 池紧张/调度异常，阿狸咪 06:15-06:35 已 cancel 5 个卡死 run + 重派 3 次，仍无法疏通
- 已确认 **Pages 部署 06:19 完成**（Last-Modified 22:19 GMT）：`ETF_PULSE.js` update=8-18 23:31、`FINAL_RECOMMEND_DATA.js` update=8-19 06:04（凌晨 algo 已自动跑过一次！）——**主人硬刷可见 8-18 盘后数据**

## 二、请小九到岗后立即执行（09:00 前完成）

1. **dispatch v8_algo_cloud 重试**（云端，可能已恢复）：
   ```bash
   curl -X POST -H "Authorization: Bearer <token>" -H "Accept: application/vnd.github+json" \
     https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_cloud.yml/dispatches \
     -d '{"ref":"main"}'
   ```
2. **若云端仍卡 → 本机直跑**（单位机）：
   ```bash
   cd E:/workspace/quant-scanner-v8
   python algorithms/final_recommend.py        # 最终推荐 + 动量共识
   python build_candidate_pool.py               # 候选池
   python calc_potential_picks.py               # 全站精选
   python calc_stock_rps.py                     # 相对强度
   python calc_crds.py                          # 逆势龙头
   python stage_to_raw.py && python api_push_raw.py   # 推 raw_data 触发云端 build
   ```
3. **cn_fetch 若仍卡**：dispatch v8_cn_fetch_cloud category=all（或本机 v8_cn_fetch_cloud 应急）
4. **确认 build_deploy**：raw_data 推送后自动触发，或手动 dispatch `v8_build_deploy.yml`
5. **验证**：curl https://ah-quant999.github.io/quant-scanner-v8/ 看 Last-Modified 更新

## 三、主人重点关注的卡（昨日问"怎么没重新跑"）

- **动量共识筛选**（8-16 修正版，主人昨天 06:15 追问没更新）
- **最终推荐 Top 5**（凌晨 06:04 已跑过，需确认新数据）
- **ETF 三合一**（盘中异动/资金热度/主力净额）
- **盘后数据**（斐波那契/商品涨价/板块资金三合一）

## 四、红线提醒

- ❌ 不要手动改 index.html 缓存戳（等 build 自愈）
- ❌ 不要删 build_deploy concurrency（结构性问题，今天先应急）
- ✅ 数据刷新优先走云端 workflow；云端瘫痪才本机直跑 + push
- ✅ 完成后在 HANDOVER 记录

## 五、阿狸咪侧已做（勿重复）

- 06:15 派发三件套（cn_fetch/algo/build_deploy）→ 云端卡死
- 06:30 cancel 5 个卡死 run（algo 4 + cn_fetch 1）→ 重派仍卡
- Pages 06:19 已部署 8-18 盘后数据版

— 阿狸咪 2026-08-19 06:35
