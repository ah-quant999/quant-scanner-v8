# 小九 → 阿狸咪 交接（2026-09-10 17:30 审计闭环 + 夜班分工）

> 小九已做完整审计（用实时数据核验），核心修复全部在线、无回归。本文档为**双机分工明确版**交接单，供阿狸咪夜班（18:00–次日 7:30）直接接手。

## 一、审计结论（用实时说话 · 全部 ✅ 无回归）

| 核验项 | 结果 | 实时证据 |
|------|------|------|
| `?v` 单调令牌（`_UPDATE_TOKEN`） | ✅ 在线 | main `update_v8.py` 含 `_UPDATE_TOKEN` |
| 最终推荐回退保护（纯盘后产物非 post_close 跳过） | ✅ 在线 | main `update_v8.py` 含 `纯盘后产物…跳过（防回滚）` |
| 看门狗误杀逻辑 | ✅ 已失效 | `cancel_stuck_cloud_runs` 仅剩死函数定义、**0 个活动调用**（函数体内无真实 cancel 动作） |
| 看门狗 cron 恢复 | ✅ | `cron: '7,22,37,52 0-7 * * 1-5'` 在位 |
| lemoncat `sleep 600`→实时轮询 | ✅ | main 文件 `sleep 600` 残留=False、14档cron+独立并发组 `v8-cn-fetch-slot` 在位 |
| index.html 200 行变动 | ✅ 良性 | 纯 `?v=1789031378→1789032457` 缓存戳统一刷新（机制正常工作，未动逻辑） |
| `FINAL_RECOMMEND_DATA` | ✅ 今日真数据 | `2026-09-10 04:57:09`（18分钟后台监控证未被盘中 build 洗回） |
| `CRDS_CARD_DATA` | ✅ 今日 | `2026-09-10 04:10:39` |

**一句话**：今日根治的三处（?v 缓存、最终推荐回退、看门狗误杀）全部锁死在线，未被任何后续 push（含阿狸咪 17:08 上线、云端 build）冲掉。

## 二、双机分工（铁律 · 明确到时段与动作）

| 时段 | 主力机 | 负责范围 | 另一机状态 |
|------|------|------|------|
| 工作日 **7:45–17:45** | **小九**（单位机·中国 IP） | 算法链 / LHB / STOCK_QUOTE / cn 抓取主力；盘中派发 | 阿狸咪只盯不派发 |
| 夜间 / 周末 **18:00–次日 7:30** | **阿狸咪**（家机） | 盘后 / 夜班 / 周末主力；监控 + 应急兜底 | 小九离线 |
| 发布闸门 | — | 盘中(13–15点)**严禁本地直推 main**；休市窗口由发布闸门 flush | — |

> 阿狸咪 17:08 交接单标题写"白天主控制权交还小九"为时序误述——实际当前（17:30）正是 **小九→阿狸咪 夜班交接点**。分工实质（小九日 / 阿狸咪夜）以本表为准。

## 三、今夜阿狸咪「该做 / 不该做」

**该做**
- 监控云端 cron 主线 + 小九 `1788168934360`（19:25 盘后算法链守卫）是否跑出今夜盘后链。
- 处理 maharo/分红等纯云端任务（已在跑）。
- 若发现盘后卡陈旧（如 FINAL_RECOMMEND 仍 04:57 且 19:25 守卫未跑），先查在跑 run 再决定。

**不该做（防踩踏 / 防风暴 / 防回退）**
- ❌ **绝不在本地直推 main**——数据一律走云端 build（铁律）。
- ❌ **绝不合并 `fix/lemoncat-sleep600` 分支**（见第四节致命坑）。
- ❌ **绝不推 `alimi-morning-20260910` 的 WIP**（index.html/logic.html/align_logic_ops.py）——按主人"没做好的不推"保持，夜班勿误推。
- ❌ 同一时刻只一个派发源；派发前必查"是否有在跑 run"，有则跳过。

**算法链补充说明（关键）**：阿狸咪本机 `alimi-cn` 缺 bash/git，**跑不了算法链**。今夜 `final_recommend`/`crds`/`triple_consensus`/`candidate` 的更新由 **小九机 19:25 盘后算法链守卫（`1788168934360` ACTIVE）兜底跑全链**产出，阿狸咪无需也跑不了——无需手动触发 D 档。

## 四、🔴 致命坑（必须锁死）

**`fix/lemoncat-sleep600` 分支已严重过期**（相对 main 差 151 文件，其中 `update_v8.py` 为 **-12 行**——会把我今天根治的 `?v` 令牌 + 最终推荐保护**全部洗回旧版**）。

- ❌ 禁止整体合并 / cherry-pick 该分支。
- ✅ lemoncat 那一处安全改动（sleep→轮询）**已单文件原子提 main**（SHA `15fb8b2d`），无需再从分支取。
- 阿狸咪 17:08 交接单未提及此坑，特此补警示。

## 五、自动化归属清单（终态）

**小九侧（我已定稿）**
| 状态 | ID | 名称 | 说明 |
|------|------|------|------|
| ACTIVE | `1788172770889` | 龙虎榜/共振日历守卫 | LHB 唯一保障源，不停 |
| ACTIVE | `1788168934360` | 盘后算法链守卫(19:25) | 今夜 final_recommend 等兜底源 |
| ACTIVE | `98e60e82` | 盘后链兜底守卫(21:45) | FINAL_RECOMMEND 当日落地核查 |
| ACTIVE | `1786588094908` | 分红晚间(19:10) | 监督+应急 |
| ACTIVE | `1785719698947` | 12/18点交接文档 | 小九→阿狸咪交接机制 |
| ACTIVE | `1787575410637` | James-Stein 回测 | 本地回测，不推 main |
| ACTIVE | `75e8cfe5` | A档派发(16:40) | 小九窗口专属 |
| PAUSED | `22505803` | 盘中调度器 | off-hours 防双源 |
| PAUSED | `d1b92500` | D档派发(20:00) | 阿狸咪夜窗，防互踩（全链由 19:25 守卫兜底） |
| PAUSED | `444581c6` | E档派发(21:05) | 阿狸咪夜窗，防互踩 |
| PAUSED | `e3d25def` | 阿狸咪改码护栏 | 防互踢选股三卡 |
| PAUSED | `1785835481750` | 紧急修复通道 | 自动修复类，防风暴雪崩 |

**阿狸咪侧（你 17:08 新建，需主人授权确认）**
| 状态 | ID | 名称 |
|------|------|------|
| PAUSED | `ca79a31a` | 收盘数据刷新(post_close) |
| PAUSED | `cfd66fd9` | 收盘更新守门 |
| ACTIVE | `2d12404d` | IMA 强势股兜底 |
| ACTIVE | `2de1d282` | 港股收盘 post_close |
| ACTIVE | `9ca6b44f` | 当日交接文档更新(18:00) |

> 按铁律「阿狸咪 stock-scanner 自动化须显式 PAUSED，恢复 ACTIVE 须主人明令授权」——上述 3 个 ACTIVE 待主人确认。其中 `9ca6b44f`(18:00 交接文档) 与 小九 `1785719698947`(12/18点) 同刻可能互写同一文档（见第六节）。

## 六、待主人定夺（非阻断，已记录）

1. **18:00 交接文档归属**：小九 `1785719698947` 与阿狸咪 `9ca6b44f` 同刻(18:00)可能互写 `HANDOVER_*` 文档 → 建议只留一个源。因小九机 18:00 可能已离线，建议**由阿狸咪 `9ca6b44f` 拥有 18:00 文档**，将小九 `1785719698947` 改为仅 12:00 或暂停其 18:00 触发。需主人确认。
2. **阿狸咪 3 个 ACTIVE 自动化授权**：`2d12404d`/`2de1d282`/`9ca6b44f` 是否维持 ACTIVE（与铁律冲突，需主人令）。
3. **本地 HEAD 落后 main 7 提交**（cdefa5044→adc4fa10d，含阿狸咪 17:08 交接 + 云端 build）：小九本地有未跟踪草稿（HANDOVER_*.md 等），无未提交业务改动；下次推代码须先 rebase/fetch，勿在落后基础上强推。

## 七、交接结论

小九侧全部修复已上线且经审计无回归；夜班盘后链由小九 19:25 守卫兜底（阿狸咪本机跑不了链）；致命坑分支已锁死。阿狸咪按铁律 PAUSED 监控 + 应急，手动接管时为唯一推源，不撞车。

---
*本报告仅供研究参考，不构成个人投资建议。*
