# HANDOVER｜阿狸咪的股票专家 → 小九｜2026-09-11 21:27｜删运维页重复的候选股池独立卡(已上线)

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

**主人令「运维有两个候选股池，把下面那个删了」已执行并上线**：删除运维页顶部重复的「💾 文件新鲜感」独立卡（`opsCandidateFreshBody`），候选股池来源级口径**归一到**「数据健康总览」卡 → 「文件新鲜度」表内的「候选股池（来源级）」行。commit `370fd04a3c01516d5dd1513e664f6b6b8840e36c`，改动仅 2 文件、纯删除 40 行。

---

## 正文

### 1. 根因（同一信息两处渲染）

运维页 `opTab1` 曾同时存在两块内容完全重复的「候选股池来源级」：

| # | 位置 | 元素 id | 渲染内容 | 判定 |
|---|------|---------|----------|------|
| ① | 运维页顶部、数据健康总览卡**上方**的独立卡 | `opsCandidateFreshBody` / `opsCandidateFreshTime` | 「🧭 候选股池（来源级）」+ 六来源彩色标签 + 「最后更新：…」 | ❌ **本轮删除** |
| ② | 「数据健康总览」卡 → 「文件新鲜度」表内一行 | `freshRowCandidate` + 子行 `freshCandidateSources` | 「🧭 候选股池（来源级）」+ 时间列 + 状态列 + 六来源标签子行 | ✅ **保留** |

演进留痕：`c11796499`（主人令：移到运维页数据健康总览内元数据巡检上方）→ `3452f989c`（又把该卡「提升为顶部独立卡」）→ 于是出现两处。本轮按主人令回到「只留在数据健康总览内」。

### 2. 变更（commit `370fd04a3c01516d5dd1513e664f6b6b8840e36c`）

```
 index.html | 40 ----------------------------------------
 logic.html |  2 +-
 2 files changed, 1 insertion(+), 41 deletions(-)
```

**index.html**（纯删除，无新增逻辑）：
- 删 HTML 独立卡骨架（含注释，6 行）
- 删 `renderRunnerTrack()` 内的 `_renderOpsCandidateFresh()` 函数定义 + 调用（31 行）
- 删引用已删函数的过时注释（3 行）

**logic.html L2137**（文档同步）：
- 原：「来源级监控『文件新鲜感』卡…为**运维页顶部独立卡（数据健康总览卡上方）**（id=opsCandidateFreshBody）」
- 新：「来源级监控由**运维页『数据健康总览』卡 →『文件新鲜度』表内『候选股池（来源级）』行**承载（id=freshRowCandidate + 子行 freshCandidateSources）……**一劳永逸**：此前与它重复的顶部独立卡（opsCandidateFreshBody）已删除 —— 同一信息不再两处渲染。」

### 3. 审计证据（pre-deploy 同级）

| 项 | 结果 |
|---|---|
| inline `<script>` 语法（`new Function`） | 24 段，**0 错误** |
| `data/*.js` 引用数 | 132 处 / **106 个唯一文件**（区间 90~110 ✅） |
| HTML 标签开闭 diff | `div/script/table/tbody/thead/tr/td/th/span/h3/style/section` **逐项与原始一致**（原有 JS 字符串噪声不变，非本次引入） |
| 内容差异 | **仅 3 段 delete**（L6493-6498 / L9958-9988 / L10085-10087），**无其它任何改动** |
| 行尾 | `index.html` 保持**纯 LF（CRLF=0）**；`logic.html` 保持 **CRLF（8671）** |
| 残留检查 | `opsCandidateFresh` 在 index.html = **0**；`_renderOpsCandidateFresh` = **0** |

> ⚠️ 过程中踩过一次坑并已修正：Python `Path.read_text/write_text` 在 Windows 上默认做通用换行翻译（LF↔CRLF），首次改完把 index.html 从纯 LF 写成了全 CRLF（14806）。已改为 **bytes 级读写**（`read_bytes().decode()` + `write_bytes(t.encode())`）从 git 原始版本重做，最终 CRLF=0 与原始一致。**这也是 `f8e4430c6` / `dbdd1af91` 当年修 logic.html 行尾的同一个坑，建议在脚本模板里固定成 bytes 级读写。**

### 4. 影响面排查（改后三件套②）

| 消费者 | 结果 |
|---|---|
| `.github/workflows/**` | ✅ **零引用**（`grep -rn opsCandidateFresh .github/` 为空）→ 不涉 cron / 派发源 / 双机任务，无需对齐调度 |
| `.github/scripts/**` + `v8/**` 审计脚本 | ✅ 零引用 → 不触发 pre_deploy_audit / align_logic_ops |
| `logic.html` 文档 | ✅ 已同步（见 §2） |
| 双机 | 小九侧 `git pull` 即得；本机因工作区存在大量未提交 pile（见 §5）改用 **Git Database API 推送**，未触碰本地工作区 |

### 5. 遗留与提醒

1. **本机工作区有 529 个未解决冲突（UU）+ pile**：来自 `git stash pop` 后 `data/*.js`、`raw_data/kline_cache/*.json` 等运行产物与远端 build 戳的双向修改。**阿狸咪未擅自解冲突、未 `git stash drop`**，stash 备份保留在 `stash@{0}`。**请小九或主人 review 后再决定这批 pile 的取舍。**
2. **致谢 + 自省**：本机 20:56 推送的 `3ce9dd972` 曾把 `v8_algo_cloud.yml` 顶格一行、致整份 workflow 对 GitHub 无效，已由小九 `3d728c906` **P0 抢修**。阿狸咪已知悉，后续改 workflow 一律先跑 YAML 有效性校验（对应 `259968b3c` 把 pre_deploy_audit 扩至 5 项）。
3. **卡位置的本轮终态**：候选股池来源级只在「数据健康总览 → 文件新鲜度 → 候选股池（来源级）行」渲染一处。若后续想再迁位置，**请先删旧的再建新的**，避免又一次双处渲染。

---

**签名**：阿狸咪的股票专家（2026-09-11 21:27 · 阿狸咪夜班）
