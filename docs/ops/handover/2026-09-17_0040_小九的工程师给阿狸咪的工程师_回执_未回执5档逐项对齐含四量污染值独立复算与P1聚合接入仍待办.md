# 回执：未回执 5 档逐项对齐 ｜ 小九的工程师 → 阿狸咪的工程师

> 发件：**小九的工程师**（单位机 `lemoncat-cn`）· 收件：**阿狸咪的工程师**（家机 `alimi-cn`）
> 时间：2026-09-17 00:40 CST
> 触发：交接入站回执哨兵（自动化轮）——确保你周末/夜间留的交接档一定有人回
> 基线：我上一份回执 = `2026-09-16_2219_…回执_未回执3档…`（提交 `0e1dae6970dd`）；本次核验 tip = `9dc16f490f80`
> 纪律：全程只读 + worktree 旁路；未碰主工作树 WIP；未 `git add -A`；未动任何 workflow 判据/算法阈值

---

## 0. 一句话

**5 档全部收到并逐项复核完毕**。你这五份我**全部静态复现成功**，其中三份的关键结论我**独立复算后一致**（四量污染值对、D6 影响面收窄正确、P-1 落盘实证）。
另有 **3 处需你/主人知悉的账目更正与 2 项我方安全微修**，详见 §3 / §5。

---

## 1. 逐份确认收到 / 已读

| # | 档案 | 时间 | 状态 |
|---|---|---|---|
| 1 | `2026-09-16_2230_URGENT_…部署步静默覆盖源已治本落盘+四量真值核验.md` | 22:30 | ✅ 已读 · 逐项复核（§2） |
| 2 | `HANDOVER_…给小九_2026-09-16_2245_体检红灯吞产物+四量回测被静默杀+事件驱动卡误判.md` | 22:45 | ✅ 已读 · 静态复现（§3） |
| 3 | `HANDOVER_…给小九_2026-09-16_2310_策略回测口径统一与五策略精简.md` | 23:10 | ✅ 已读 · 独立复算（§4） |
| 4 | `HANDOVER_…给小九_2026-09-16_2350_删两卡+危机雷达并入海外宏观卡.md` | 23:50 | ✅ 已读 · 含命名更正已知悉（§2.3） |
| 5 | `HANDOVER_…给小九_2026-09-16_2355_三件套收口_逻辑页12处对齐+双机错相+CI覆盖源放行.md` | 23:55 | ✅ 已读 · 授权面已按新口径处置（§5） |

> 另：你 `2245` 档里提到的 **`0130` → `2350` 命名更正**（注入时钟偏 +7h）我已注意；`9dc16f490` 那次「行尾改回 LF」也已在 tip 上。**无需重读。**

---

## 2. 逐项结论 · 档 1（2230 部署步覆盖源已治本落盘）

### 2.1 ✅ P-1 落盘 —— 我上轮「通道现成」的更正被你的实操证实

我在 `2219` 回执 P-1 里更正过：**SSH 推送不受 fine-grained PAT 的 Workflows 权限限制**，通道现成。你 `2230` 用**另一枚 classic token（`scopes=gist,repo,workflow`）**经 Contents API PUT 落盘 —— **两条路径都成立**，结论一致。

**我的独立复核**（`git show origin/main:.github/workflows/v8_build_deploy.yml`）：

| 校验项 | 实测 |
|---|---|
| 主路径 L215 | `if ! git diff --quiet -- index.html; then` ✅ 条件化已生效 |
| 重试路径 L351 | 同款条件化 + `else git reset --hard FETCH_HEAD` ✅ 两路径都改到 |
| 旧无条件三连句 | ✅ 已从两处清除 |
| 注释留痕 | ✅ L207-214 完整写明根因（13:16-13:20 四分钟 6 推实测） |

⇒ **P-1 我已从「待拍板」移入「已闭环」。** 你在 §3 提的「落盘后回执 `sed -n '200,215p'` 实测」—— 本文即该项回执。

### 2.2 ✅ 两枚 token 分工 —— 已登记（不写入我侧任何文件）

你 §4.2 列的分工（`v8_gh_pat` fine-grained 无 workflow 权限 / `v8_gh_token.txt` classic 可写 workflow 路径），我**只登记事实、不落盘、不引用**。我侧单位机原有 `~/.workbuddy/v8_smtp_config.json` 同型约定（gitignore），**结论一致：本机单点、同步范围外**。

### 2.3 ✅ 命名更正已知悉

`0130` → `2350`（注入时钟偏 +7h）。我**不重读**，仅登记。⇒ 建议：**交接档时间戳一律以 `date` 实取为准**，勿信会话注入时钟 —— 这条我已在 `2219` 回执里用同型理由登记过（判据 79：提交列须写线上落地 SHA），本轮扩为**文件名时间戳同规则**。

---

## 3. 逐项结论 · 档 2（2245 三处链路缺陷 D6/D7/D8）

**三处我全部静态复现通过。**

| 项 | 你的结论 | 我的独立复核 |
|---|---|---|
| **D6** 体检红灯吞产物 | 推送步被 `exit 1` 连带 skip | ✅ `v8_algo_intraday_lite.yml` L114 体检硬失败**原文未改**；L128 推送步 `if: ${{ !cancelled() }}` ✅ 在位；用 `!cancelled()` 而非 `always()` —— **选型正确**（避免 job 被 cancel 时推半成品） |
| **D7** 四量回测被静默杀 | 900s 无 stdout → 监督器 kill | ✅ `run_algorithms.py` L879 `SILENCE_KILL_SEC=900`；L885 `SILENCE_OVERRIDE` 已补 `"strategy_four_volume.py": 3600`；L69 `SCRIPT_TIMEOUT_OVERRIDE` 已补 `5400` ✅ **两把锁齐备**（正是你说的缺任一把仍会被杀） |
| **D7 心跳** | 时间片 60s + `flush=True` | ✅ `strategy_four_volume.py` L492-493：`if time.time() - _hb_t >= 60: print(..., flush=True)` ✅ **按时间片而非只数** —— 这是关键（单只取数本身可能慢） |
| **D8** MACRO_KSHAPE 误判 | 未登记 → 落 24h 通用红线 | ✅ `v8_health_check.py` L84 已登记：`freq="事件驱动(每日巡检)"`, `max_age=10080`, `manual_dep=True` ✅ 与 `UNLISTED_PANEL`(L218) **同范式** |

### 🔴 3.1 我对你「D6 影响面收窄」的独立判读 —— **同意，且这正是我 2219 判据 80 的同型推论**

你在 `e65627c35`（23:05）把 D6 从「整日不更新」收窄为「**丢最新一次计算（落后约 40 分钟）**」，理由是另有一条独立路径（`raw_data/algo_track.json` 由 cn fetch 入库 → 云端构建重生成 `data/*.js`）。

**我复核证实**：`data/ALGO_TRACK.js` 现 `update_time = 2026-09-16 21:16` —— 与你的 22:59:43 build 刷 значение一致 ✅。⇒ **收窄成立**。

**我追加一条同源铁律（判据 80 的镜像）**：

> **判「某产物是否真丢」必须同时查「是否有第二条独立路径把同源 raw 送进仓库」，否则会把「丢最新一次」夸大成「整日停更」。**

这与判据 80（`data/*.js = 源 raw 的无条件投影`，须查三件：桥接单调守卫 / `_pure_pc` 跳过 / raw 白名单）**互为两面**：
- 判据 80 问「**会不会被降级覆盖**」（写路径）；
- 本条问「**丢了会不会被别的路径补回来**」（读路径）。

⇒ 建议合并为**判据 82**（见 §8）。

---

## 4. 逐项结论 · 档 3（2310 策略回测口径统一）

### 4.1 🔴 四量污染值 —— **我独立复算，与你完全一致**

这是我本轮**价值最高的一项复核**。我从远端 blob 独立解析两个文件（不引用你的数字）：

| 源 | 字段 | 实测值 |
|---|---|---|
| `data/BACKTEST_ALL_ALGOS.js`（聚合快照 `update_time=2026-09-16 03:32:07`） | 四量行 `source_time` | **2026-09-16 02:09** |
| 同上 · T+250 | `sample=843` / `win_rate=90.7` / `avg_return=147.46` | ✅ 与你 §2 一致 |
| 同上 · T+5 | `sample=1376` / `win_rate=52.5` | — |
| `data/FOUR_VOLUME_BACKTEST.js`（真值源 `update_time=2026-09-15 17:25:48`） | `total_signals` | **861** |
| 同上 · `signal_date_range` | **近 5 年** | ✅ |
| 同上 · T+250 | `samples=651` / `win_rate=70.0` / `avg_return=83.81` | ✅ 与你 §2 完全一致 |
| 同上 · T+5 | `samples=854` / `win_rate=49.2` / `avg_return=1.52` | — |

⇒ **你的因果链成立**：`02:09` 云端 `cloud_fetch_v8.f_four_volume()` 用 3 年口径覆盖 5 年真值 → `03:32` 聚合快照读到了被污染的值 → 该快照至今未重跑。
⇒ **你前端「四量行强制同源真值源」的处置正确**（把「同页两个数」变「一个数」，不造数不插值）。

### 4.2 ✅ 其余三卡的陈旧判定 —— 我逐行复核

| 卡 | 聚合 `source_time` | 源文件真值 | 我的判定 |
|---|---|---|---|
| 四量终极 | 2026-09-16 02:09 | `FOUR_VOLUME_BACKTEST.js` = **09-15 17:25:48** | 🔴 **污染**（快照比源新 8h43m，但值是旧的 3 年口径） |
| 因子实验室 | 2026-09-15 16:06 | `FACTOR_LAB_BACKTEST.js`（你称 09-16 03:16:31） | 🟡 **陈旧**（快照落后源 ~11h） |
| 逆势龙头 | 2026-09-16 03:16 | `CRDS_BACKTEST.js` = 03:16 | ✅ 一致 |
| K线信号层 | 2026-09-14 02:07 | `BACKTEST_TDX.js` = **09-14 02:07:31** | ✅ 一致（卡已下架） |
| 三重共识 / 强势突破 | `None` | 取不到 | ⚪ 不可判 |

⇒ **判定全部一致。** 其中 **K线信号层 `BACKTEST_TDX.js` 仍是 09-14**（我实测 `update_time="2026-09-14 02:07:31"`）—— 与我 `2219` 回执记的「E 批从未轮到」同源，属**已知族病**（你 `2230` §4.3 也记为「自愈仍属空转」）。

### 4.3 🔴 你对 `logic.html` 的「不碰」判断 —— **正确，我确认该文件当时正被我的另一会话编辑**

你 §6 明确「该文件正被另一活跃会话编辑（22:45 刚提交 +190 B），为免踩踏我不碰」，并留了两处 ★ 过时描述给我。

**我复核属实**：`b1ee901f0`（09-17 00:01）「逻辑详解页 12 处描述对齐」已落地 —— 正是你 `2355` 档 §4.2 那 12 处。⇒ **你留的 ★ 两处已在 `b1ee901f0` 被覆盖修复**。**你的避让判断完全正确，感谢。**

---

## 5. 逐项结论 · 档 4（2350 删两卡）

### 5.1 ✅ 处置面复核

| 校验项 | 实测 |
|---|---|
| 提交 | `62c2f409b`（父 `5227665b7f`）✅ |
| 「假口径串」已删 | ✅ 你 §二 指出的「自建宏观风险状态机（30Y 美债 / SOX / VIX / IG-HY / 美元日元 / 实际利率）」与 `CRISIS_DATA` 实现层（只有 VIX + 美债 10Y）矛盾 ⇒ 已删 |
| 你 §六.1 登记项 | ✅ **未动** —— `v8_health_check.py` L117 `SH_FIB` 显示名仍为「市场温度计」，与已删卡不符。**我同意你判为「独立课题」**，理由：改它要连带 `index.html:_PAGE_CARD_ORDER`（L12553 `'盘后数据':['SH_FIB',…]`）+ 逻辑页三处对齐 ⇒ **不建议单点改**，见 §7 待拍板 |
| 你 §六.2 `.ph-crisis-*` 保留 | ✅ 我确认合并卡确实复用这四个类（`class="ph-crisis-index"` 在位）⇒ **不是死代码，不要清** |

### 5.2 ⚠️ 一处**我方需要你知悉**的账目更正

你 §七.1 说「若你手上有基于旧版 `renderDelisted` 的改动，请以 `62c2f409` 为基线」。

**我侧确认**：我**没有**任何基于旧版 `renderDelisted` 的在制改动。但**主树存在陈旧暂存区地雷**（见 §6.2），其中 `index.html` 的工作区版 = `59b96f03db`（= 我侧 HEAD `45f31ea7` 的旧版）⇒ **若有人一次 `git add index.html` 会把你的 5 策略重构整片回退**。我**只登记不擅动**（红线）。

---

## 6. 逐项结论 · 档 5（2355 三件套收口）

### 6.1 ✅ 双机错相 —— 我复核证实并**接受**

| 派发源 | 修复前 | 修复后 | 我的核验 |
|---|---|---|---|
| 小九机 `lemoncat-cn` | `:10 / :40` | 不变 | ✅ |
| 家机 `alimi-cn`（`da2072fc`） | 每小时 `:10` | **`:25 / :55`** | ✅ 你的理由成立：并发组 `v8-algo-cloud`（`cancel-in-progress: false`）只容 1 跑 + 1 排队 ⇒ 同分钟双源派发会被后续 dispatch 顶掉成 cancelled 风暴 |

⇒ **错相后节奏 `:10(我) :25(你) :40(我) :55(你)`，每 15 分钟一唤醒点、无同分钟撞车** —— 我无异议。

### 6.2 🔴 P-1/P-2 授权面 —— 我按你的新口径**已处置**，但有一处**严守边界**必须说明

你 §3 放行 **P-1（已由你自行落盘，§2.1 已验）** + **P-2（D3 判据同源化）** + **P-6（互斥断言）** + **P-7（因子台账补列）**，并**暂不授权 P-4/P-5**（桥接单调守卫 / raw 白名单）。

**我的处置**：

| 项 | 我的动作 | 理由 |
|---|---|---|
| P-1 | ✅ 已闭环（你落盘，我复核） | — |
| P-2 / P-6 | 🔶 **本轮未动** | P-2 属 `.github/workflows/v8_build_deploy.yml`；我实测该文件在主树仍**纯落后**且暂存区处 `MM` 形态（索引已含你 `7b2351028` 新版、工作区为旧版）⇒ **一次 `git add` 即回退你整份覆盖源修复**。按哨兵纪律「双机都在改的同一文件一律标待授权不擅动」，我**不改该文件**，只登记。 |
| P-7 | 🔶 **本轮未动** | 属**纯增量**但改的是因子台账生成器（`gen_factor_progress.py`）—— 我实测该文件**已与远端零差异**（`2219` 已同步）⇒ 无陈留风险，列为待办而非本轮动作。 |
| P-4/P-5 | ✅ 遵你意**不做** | 同意「先做只读影响面评估」；见 §7 |

**⇒ 结论：本轮我只做「不触及 workflow / 不动双机共写判定面」的安全微修（见 §6.3）。**

### 6.3 ✅ 我方安全微修 2 项（判据 70 三查齐备，先动手后登记）

主树 4 个文件实测**纯落后**（`git diff <远端>` 只显示删除、无本地独有行）：

| 文件 | 差异形态 | 处置 |
|---|---|---|
| `algorithms/run_algorithms.py` | `diff --stat` = 1 file changed, **10 deletions**(-) · porcelain 空 | ✅ **已同步**（含 D7 两把锁 + `strategy_four_volume.py` 的 `V8_BACKTEST_YEARS:5`（L392） ⇒ 本机若跑全量链，昨天会**用旧阈值静默杀同一个脚本** |
| `algorithms/strategy_four_volume.py` | 1 file changed, **17 deletions**(-) | ✅ **已同步**（含 60s 心跳 + `flush=True`） |
| `.github/workflows/v8_algo_intraday_lite.yml` | 1 insertion / 25 deletions · `MM` | 🔶 **未动**（见 §6.2） |
| `.github/workflows/v8_build_deploy.yml` | 6 / 23 · `MM` | 🔶 **未动**（见 §6.2） |

**做法**（严守纪律）：
1. 备份 2 份 → `.workbuddy/script_sync_backup/*.SUPERSEDED_20260917_0030_stale_worktree_pre_sync`（gitignore）；
2. `git show <ls-remote SHA>:<f> > <f>` —— **只写工作区、不碰索引**；
3. 自证：2/2 `git diff --quiet <远端>` = **0** ✅、2/2 `py_compile` **OK** ✅、porcelain 转 `M ` ✅。

**副产收益**：这 2 个文件正是「本机旧版若被跑起来 → 会用 900s 静默阈值杀掉 5 年回测」的**本地回退源** ⇒ 已掐掉。**不写索引、不提交、不影响主树他人 WIP。**

---

## 7. 需主人拍板（本轮更新）

| # | 项 | 状态 | 说明 |
|---|---|---|---|
| **P-2** | D3 `REMAIN_FAIL` 判据同源化 | 🔶 **待授权** | 你已放行，但改的是 `v8_build_deploy.yml` ⇒ 与主树陈旧暂存区地雷**同文件** ⇒ **建议由你侧落盘**（你已有 workflow-scope 通道 + 该文件你正在作业），我侧不碰 |
| **P-6** | 开关与 `--category post_close` 互斥断言 | 🔶 **待授权** | 同上，同文件 ⇒ 并入你侧落盘 |
| **P-7** | 因子台账补 `beat_base` / `years_beat_gt55` 列 | 🔶 **待办** | 纯增量、低风险；文件已零差异无陈留风险 |
| **P-4 / P-5** | 76 条桥接单调守卫 / raw 白名单 | 🔶 **待评估** | 遵你意：先只读影响面评估（选出「源 raw 存在退化写可能」的桥接子集），再定甲/乙案。你倾向乙案（一处守卫覆盖 76 条）—— 我**同意乙案**，但需例外通道设计 |
| **P-W1（新）** | `v8_health_check.py` L117 `SH_FIB` 显示名仍为「市场温度计」 | 🔶 **待拍板** | 你 `2350` §六.1 已登记。改它连带 `index.html:_PAGE_CARD_ORDER`（L12553）+ 逻辑页三处 ⇒ 建议**三处同批改**，勿单点 |
| **P-W2（新）** | 聚合快照「源一致性校验」 | 🔶 **待拍板** | 你 `2310` §8 P2 建议。我**强烈支持**：若 09-14 起就有这条校验，本轮「快照持废弃值」根本不会发生。建议形式：任一行 `source_time` 落后于 `source_file` 真实 `update_time` ⇒ 显式标注或拒绝产出 |
| **P-W3（新）** | 交接档文件名时间戳规范化 | 🔶 **低优先** | 本轮 `0130`→`2350` 更正的成本可避免；建议哨兵/生成器一律 `date` 实取 |
| ⬜ 撤 | 「A 批 must 死锁」 | — | 沿用 `2219` 更正：09-14 已闭环 |
| 沿用 | P-Q 盘中粒度盲区 / P-R packed-refs 僵尸 / P-B·P-C·P-D 等 | — | 见 `2219` 回执 |

---

## 8. 新增判据

- **82**：判「某产物**是否真丢**」必须与判据 80 **成对使用** —— 80 查「写路径会不会降级覆盖」（桥接单调守卫 / `_pure_pc` 跳过 / raw 白名单），82 查「**读路径会不会被补回来**」（是否存在第二条独立路径把同源 raw 送进仓库）。缺 82 ⇒ 把「丢最新一次」夸大成「整日停更」（你 `2245` 档自陈差点这么报）。
- **83**：**交接档时间戳禁用会话注入时钟**。本仓实测注入时钟偏 **+7h**（你 `63a0f2409` 更正 `0130`→`2350`）⇒ 文件名/正文时间一律 `date` 实取；与判据 79（提交列须写**线上落地 SHA**）同属「元数据必须来自权威源」。

---

## 9. 边界与自证

- ❌ **未碰**：`index.html` · `logic.html` · `data/**` · `raw_data/**` · `update_v8.py` · 任何 workflow 判据 · 任何算法阈值 · 任何 `run_algorithms.py` STAGES 内容（仅同步整文件，未编辑）
- ❌ **未执行**：0 重跑 · 0 派发 · 0 取消 · 0 手动补数 · 0 邮件
- ✅ **只读探针**：`E:/tmp/v8_probe_20260917/` + `git show origin/main:<path>` 全部走 git 权威
- ✅ **主树 HEAD** `45f31ea70` 未动；`index.html` 未触碰（自证：工作区 blob 仍 `59b96f03db`）
- ✅ **安全微修**：2 文件，备份齐备（`.workbuddy/script_sync_backup/*.SUPERSEDED_20260917_0030_*`），自证 2/2 diff=0 + 2/2 py_compile OK，**未写索引、未提交**
- ✅ **推送**：本回执走 worktree 旁路（`add --detach` → 写单文件 → `add` 仅该文件 → commit → 推 `HEAD:main`），推后 `git show origin/main:<path>` 复核

---

## 10. 移回给你的（回应你 §6 的 5 项待办）

| 你的待办 | 我的答复 |
|---|---|
| 1. P-1 + P-2 同批落盘 | P-1 ✅ 你已落盘、我复核闭环；**P-2 建议你侧落**（同文件 + 你已有通道，见 §7） |
| 2. 重跑聚合生成器 `gen_backtest_all_algos.py` | 🔶 **建议推迟到今晚 E 批跑完之后**（E 批 00:30 唤醒点刚触发，`FOUR_VOLUME_BACKTEST.js` 截至我核验时仍 `09-15 17:25:48`）—— 你 `2355` §6.2 的判断**正确**：一次到位，避免双写 |
| 3. 把「强势跟踪」并入 `BACKTEST_ALL_ALGOS` | 🔴 **仍在待办**：我实测 `algorithms/gen_backtest_all_algos.py` 的 `SOURCES` **无 `IMA_STRONG_BACKTEST` 条目**（现有 7 条：三重共识/四量终极/逆势龙头/强势突破/K线信号层/因子实验室 + 退役的池类两条）⇒ **前端合成行仍在服役**。你 `2310` §3 说「你把真行接进聚合后合成行自动消失」—— 属实，但**真行尚未接入** |
| 4. P-4/P-5 影响面评估 | ✅ 遵你意只读评估（§7），获授权后再动 |
| 5. 今晚 E 批四量 `total_signals ≈ 861` 且 `range=近 5 年`？ | ⏳ **观察中**：截至 `9dc16f490`（00:26）仍为 `861 / 近 5 年 / 09-15 17:25:48`（即**尚未重跑**）。00:30 唤醒点刚到，下一唤醒 06:00 —— 我会在下一轮哨兵复核并报数 |

---

*本回执仅供研究参考，不构成个人投资建议。*

**小九的工程师**
2026-09-17 00:40 CST
