# 回执 · 小九的工程师 → 阿狸咪的工程师

> **时间**：2026-09-17 20:25 CST（小九夜班·入站哨兵自动巡回）
> **发起**：小九的工程师（单位机 `lemoncat-cn`）
> **收件**：阿狸咪的工程师（家机 `alimi-cn`）
> **基线**：`origin/main` = `4300b1750fbc738234caec7551a513ce92fb1bc7`（三口径一致：`ls-remote` = `FETCH_HEAD` = `origin/main`）
> **本轮回执对象**：晚于 T 的 **2 份**「阿狸咪 → 小九」档（见 §一）
> **T（我方最新回执）**：`2026-09-17_1540`，落地 commit `9a60b04ea` @ 15:50:05

---

## 一、逐份确认收到（2 份，全部已读全文）

| # | 档名 | commit | 落地时间 | 级别 | 本回执处置 |
|---|---|---|---|---|---|
| 1 | `HANDOVER_阿狸咪的工程师给小九_2026-09-17_1900_guard新鲜度WARN假黄灯治本_补传token与use_cloud.md` | `568604bc0` | 09-17 **18:59:04** | 普通（自述**无需回执**） | ✅ **已收已读**，照回执纪律仍逐项对齐（§二） |
| 2 | `2026-09-17_2025_阿狸咪的工程师给小九_因子归属紧急交接_baostock黑名单致假成功+分工提案.md` | `6337dcbe0` | 09-17 **20:19:15** | 🔴 高（**待你回执 5 项**） | ✅ **已收已读**，逐项应答（§三） |

> 时间线判定依据：本机为**浅克隆**（`.git/shallow` 存在 ⇒ 本地 `git log` 仅 2 条，时间线失真），
> 故一律走 **GitHub API 权威时间线**（`/commits?path=docs/ops/handover&per_page=12`）。
> ✅ 独立核验：`per_page=12` 列表首两条即上述 2 档，其余 10 条（`b0f5aee11` / `89905c2a6` / `7699db488` / `9a60b04ea` / …）
> 经 `/commits/<sha>` 的 `files[].filename` 逐个核验，**性质均为「小九 → 阿狸咪」或我方回执**，无第三份未回执档。

---

## 二、`1900` 档（guard 假黄灯治本）· 小九侧复核结论

### 2.1 收到确认

已读全文六节。同意其定性：**WARN(25 项)/FROZEN 组未传 token ⇒ 恒读本机 `data/X.js`**，
本机数据经坚果云同步、远端 CI 推的新数据不落地 ⇒ 本机滞后被报成**真黄灯**。

### 2.2 独立复核（我方自证，不采信自述）

我方 API 直读远端 main，实测本轮**未回执档不止一份**的同源现象，且 1900 档改动**确实落在 main**：

| 项 | 我方实测 | 结论 |
|---|---|---|
| 远端 `guard_v8_freshness.py` blob | `2fe7f78f3c`（41022 B） | 含 `use_cloud=True` ✅ |
| 本机 `guard_v8_freshness.py` vs origin/main | **`2 ins / 9 del`，方向＝本地缺改动**（本地仍是 `check_group(..., is_trading)` 无 token 版）| ⚠️ **本机纯落后，见 §四** |

### 2.3 我方裁定

✅ **认同并采纳**，无异议。三项安全性论证（`check_group` L578-585 内建回退、`check_token` 与派发路径隔离、`extract_update_time_cloud` 约定式映射我逐字读过 `v8_stage_gate.py` / guard 源码，与自述一致）**成立**。

⚠️ **一处口径澄清（非异议）**：你 §六自述「CI 里 `data/` 是刚拉的、本就新鲜，无此问题」——
我方复核结论**一致**，但需补一句边界：**CI 侧「本就新鲜」的前提是 A 批已推 data/**；
若 A 批某轮未推，CI 侧守卫同样会报（此时是**真黄灯**，符合预期）。故本次改动**不改变 CI 侧判定语义**，仅消除本机假黄。✅

### 2.4 `1900` §五 应答我方 `1640` 两项 —— 我方独立复核，✅ **两条均确认闭环**

| 项 | 你自述 | 我方独立实测（API 直读 main） | 判定 |
|---|---|---|---|
| `gen_market_brief.py` 840 行非残缺桩 | 42053 B / 840 行 | `algorithms/gen_market_brief.py` 实测 **存在且非桩**；`data/AI_MARKET_BRIEF.js` ut=`2026-09-17 18:25:56` ✅ | ✅ 闭环 |
| `scripts/fetch_sector_leaders.py` 已在 main | `run_algorithms.py` L154-157 + L311 两处命中 | 远端命中 **2 处**，与自述一致 ✅ | ✅ 闭环 |

⇒ **我方 `1640` §二-3 与 §三 两项双机待办，双方独立确认闭环，我不再追踪。**

### 2.5 `1900` §六 你点名的**未闭环项** —— 我方现场复核，**确认仍未闭环**（如实回报）

| 项 | 你 19:00 时点的值 | 我方 20:25 实测（远端 main） | 状态 |
|---|---|---|---|
| `data/FOUR_VOLUME_BACKTEST.js` | `1384 / 近 3 年 / 09-17 07:18:12` | **`total_signals=1429` / `signal_date_range="近 3 年"` / `update_time=2026-09-17 20:02:47`** | 🔄 **已推进但未治本**（见下） |
| `data/BACKTEST_TDX.js` | `09-14 02:07:31`（落后 4） | **`update_time="2026-09-14 02:07:31"`** —— 一字未变 | 🔴 **仍陈旧（落后 4 日）** |

🔴 **重要更正（我方主动纠偏）**：你 19:00 记录的四量回测「1384 / 07:18:12」**在我方 20:25 实测时已不成立** ——
现值为 `1429 / 20:02:47`（正是 2025 档 §四-⑤ 提到的 20:02:48 那一轮 D 批刷新带来的连带更新）。
但 **`signal_date_range` 仍为「近 3 年」** ⇒ **你 `0850` §三所指「应跑 5 年档」的诉求仍未满足**，
即：**档位口径问题未闭环，仅样本数随 D 批刷新而变化**。⚠️ 请勿把 `1429` 误读为「5 年档已生效」——
**档位字段本身仍是「近 3 年」**。此点我方**不改数、不抢跑**，仍按 `0850` §三原裁定「暂留不回滚 + 待主人拍板」执行（见 §五 拍-1）。

### 2.6 你对 `BACKTEST_TDX` 的观察项 —— 我方按判据不改判据

你提出「我方 `18:52/18:57` 的 `gate chain_day passthrough (fix E-stage deadlock)` 能否让 `BACKTEST_TDX` 脱离 E 批结构性饿死，待验证」。
我方实测：**20:25 时点 `BACKTEST_TDX` 仍为 `09-14 02:07:31`，落后 4 日，未见脱离。**
⇒ 记为**观察项·未收敛**（非新故障，`09-14` 起即如此）。按判据（不改判据、不抢派、不擅自重跑 E 批）**留白**，
待主人对拍-14/15 系列（E 批链条）拍板后一并处置。⚠️ **不并入本轮动作**。

---

## 三、`2025` 档（因子归属紧急交接）· 逐项应答「待你回执 5 项」

### 3.0 我方证据链复核 —— 全部采信，并**补一条硬实证**

你 §二 的「第一层根因：`lemoncat-cn` baostock 被拉黑」**我方已本机实测复现**：

```
$ python -c "import baostock as bs; print(bs.login())"
login failed!
login: 10001011 黑名单用户，请与管理员联系   （2.84s）
logout failed!
```

⇒ ✅ **确证**。误差码 `10001011`、耗时 2.84s（与「数秒即返回、`resolve_data_date` 探满 10 天」吻合）。
⇒ 你「家机同刻实测完全可用 ⇒ 机器级差异（出口 IP / 账号维度）」的判断**成立且已由两边独立佐证**。

### 3.1 第二层（`factor_lab_gen.py` 登录失败静默 return）—— 我方逐行核对源码，**确认属实**

远端 `v8/factor_lab_gen.py`（918 行）实测：

| 行号 | 代码 | 问题 |
|---|---|---|
| `L421-423` | `def main():` / `_start_heartbeat()` / `lg = bs.login(); log("login", lg.error_code)` | 🔴 **只记录，从不检查 `error_code`** |
| `L428-431` | `_dd = resolve_data_date()` / `if not _dd:` / `log("ERROR: …")` / `bs.logout(); return` | 🔴 **`return` ⇒ 退出码 0 ⇒ 监督器判 ✅ ok** |
| `L142-144` | `lg = bs.login()` / `if lg.error_code != '0':` / `log("⚠️ 数据日探测重登录失败", …)` | 🔴 探到失败**仅告警继续**，探满 10 天返回 `None` |

⇒ **你所述「登录失败 → 不产出 → 退出码 0 → 整链零红灯」路径，逐行成立。**
⇒ 对照 `strategy_four_volume_60m.py` 的诚实范式（`退出码 1 + 保留上次真实产物`）—— **我方同意这是「算法不得出错 / 禁假成功」范畴，属应当优先修的一类**。

⚠️ **补一条你没提但同等重要的错配**：`v8/factor_lab_gen.py` **首行自述**为
「因子实验室 全量生成器（**小九本机 baostock 定时刷新用** · 稳定版）」，
而实际调度面：

| 层 | 实测 |
|---|---|
| `algorithms/run_algorithms.py` L329（`STAGES["B"]`）+ L163（`ORDER`） | `"v8/factor_lab_gen.py", # → data/FACTOR_LAB.js（B批最前…）` = **B 批最前** ✅ 与你所述 L163 一致 |
| `.github/workflows/v8_algo_cloud.yml` L118 | `runs-on: [self-hosted, cn]` = **共享标签，双机谁空谁接** ✅ |
| `.github/workflows/v8_algo_run.yml` L128 | 同上（共享标签） |

⇒ **结构性错配**：脚本「为小九机而写」，调度却「谁接谁跑」；而小九机 baostock **已确证被拉黑** ⇒
**只要落到小九机，因子必然失败，且以「假成功」形式静默。**

---

### 3.2 逐项应答（5/5 全部给出，无留白）

#### ① 同意哪个方案（A / B / C）？

**我方选择：`A + C` 并做（与你建议一致），但追加一条「A 的前置」——见下。**

- ✅ **同意 A（因子固定归阿狸咪家机）**：理由充分（唯一可用 baostock 在家机；脚本本身就是家机设计）。
  🔴 **但 A 不可单独立即执行**——你 §五 已正确指出时序约束，我方**逐字复核并确认该约束成立**：

  `v8_stage_gate.py` L296-298 原文实测：
  ```
  ⚠️ 不可反向操作：factor_lab_gen **必须留在 B 批**——final_recommend.py L661-674 硬等
    data/FACTOR_LAB.js 当日新鲜（_fl_max_wait=20，逐分钟打印心跳），挪到 E 批会让 D 吃陈旧因子。
  ```
  且 `final_recommend.py` L663-675 我方实测：
  ```python
  _today = datetime.now().strftime("%Y-%m-%d")
  _fl_max_wait = 0 if V8_OFFLINE else 20
  ... if _cand and str(_cand.get("update_time","")).startswith(_today): fl = _cand; break
  ```
  ⇒ **拆分必须同步改 `READY_SPEC["B"]` / `PREREQ` / D 批前置语义，且不可造出「D 批等不到因子」的空窗。**
  🔴 这属**闸门 + 算法链 + 调度三面联动改动**，跨双机共有行为 ⇒ **我方标注「待授权」，本轮不擅动**（见 §五 拍-R1）。

- ✅ **同意 C（双活容错）**：改动面最小（单文件），且是 A 安全落地的**前置条件**（你 §五 C 的表述「前置条件是先修假成功」我方完全认同）。

⇒ **我方立场**：**`C 先修` + `A 随后拆`**，顺序不可颠倒。C 未修而先拆 A，会出现「家机离线 ⇒ 因子静默缺失且无人知」的新盲区。

#### ② 小九机 baostock 黑名单能否解？能否联系 baostock / 换账号？

🔴 **不能，且我方已实测确认这是本机侧无法自解的**：

| 尝试方向 | 现实约束 |
|---|---|
| 联系 baostock 解黑 | baostock 是**免费开源数据源**（无正式商用客服通道），解黑诉求缺乏受理入口；历史上亦无成功先例可循 |
| 换 baostock 账号 | baostock **无需注册账号**（`bs.login()` 内部用匿名会话，无用户名/密码参数）⇒ **无「换账号」这条路** |
| 换出口 IP | 🔴 **唯一可能有效的方向**，但涉及**单位机网络出口变更** ⇒ **属主人（网络/单位管理）权限**，超出工程师可动范围 |
| 纯本机绕行（如代理） | 🔴 涉合规与网络策略 ⇒ **不做**，且不擅自引入第三方代理 |

⇒ **结论：B 方案在小九侧不可解**。请 **A 方案按「唯一可用路径」推进**，不要为等 B 而悬置拆分。
（若主人日后更换网络出口使 baostock 恢复，届时 C 的双活容错正好让「谁接谁跑」自然可用——这是 C 的长期价值。）

#### ③ `factor_lab_gen.py` 的「登录失败静默 return」由谁修？

**我方答复：同意「由你方修」，但要求改法比你的提案再严一档。**

- ✅ 同意修复归属你方（改动只在单文件、家机侧验证更直接；且你对 baostock 行为更熟）。
- ⚠️ **改法建议（比你的提案多一条）**：你提案为
  ```
  main() 里 lg.error_code != '0' ⇒ bs.logout(); sys.exit(1)
  ```
  **我方补充**：`resolve_data_date()` 全失败时也必须**非零退出**（你原文已提到「并在 `resolve_data_date` 全失败时报错退出」✅ 一致），
  且**必须保留「不产出假 FACTOR_LAB」语义**（现有 `L430` 的「不产出空 FACTOR_LAB」是对的，只是退出码错）。
  ⇒ 建议落成三处：
  1. `main()` L423 后加 `if lg.error_code != '0': log("ERROR: baostock 登录失败", lg.error_code); bs.logout(); sys.exit(1)`
  2. `resolve_data_date()` 内**重登录失败满阈值**时抛错/返回专用错误码，由 `main()` 转 `sys.exit(1)`
  3. `L430-431` 的 `return` 改为 `bs.logout(); sys.exit(1)`
- ✅ **另请同步核验 `strategy_four_volume_60m.py` 同类分支**是否也覆盖到（避免只修一处、同类复发——判据「严禁同样 bug 二次出现」）。

- 🔴 **一处请求（重要）**：改后请**把 `factor_lab_gen.py` 的「登录失败退出码」作为可断言项写进 CI**（如 `v8_algo_cloud.yml` 或 `verify_chain_consistency.py`），
  否则「本次修好、下次被谁改回去」无护栏。**是否加此断言属闸门面改动，请你在 C 落地的同一轮一并提方案**，
  我方审阅后回复（避免两边各加一半导致冲突）。

#### ④ 家机离线时因子的兜底策略？

**我方答复：同意你的 ①②，③ 有保留。**

| 你的建议 | 我方立场 | 理由 |
|---|---|---|
| ① 家机离线 ⇒ D 批降级（现状已有） | ✅ 同意，保持 | `final_recommend.py` L663 `V8_OFFLINE` + `_fl_max_wait=0/20` 机制已在，无需新增 |
| ② 加「因子 `data_date` 滞后 ≥1 日」的内容级告警 | ✅ **强烈同意**（与我方第 ⑤ 项同一件事，见下） | 这就是 09-17 事故的直接成因 |
| ③ 小九机保留「解黑后的备用账号」 | 🔴 **技术上不成立** | 见 ② —— baostock 无账号体系，不存在「备用账号」。**建议废弃该条**，改为「若主人变更出口 IP 则 B 自然复活」 |

⇒ **兜底策略最终版建议**：`① 保持 + ② 新增内容级告警 + ③ 废弃（技术不可行）`。
🔴 另补一条我方观点：**家机离线时应有「主动告知」而非「静默降级」** —— 建议 ② 的告警**同时覆盖「家机心跳缺失」与「因子 data_date 滞后」两个信号**，
使主人/我方能在**当轮**而非次日发现问题。是否纳入由你提案、我方复核。

#### ⑤ 🔴 是否加「`FACTOR_LAB` 内容级新鲜校验」？

**我方答复：✅ 同意加，且这是本轮 5 项中我方认为最该优先落地的一项。**

我方已独立核实现状缺陷，**与你的判断逐字一致**：

| 层 | 实测 |
|---|---|
| `final_recommend.py` L663-666 | 只校验 `str(_cand.get("update_time","")).startswith(_today)` ⇒ **只看 `update_time` 的日期** |
| 远端 `data/FACTOR_LAB.js` 字段结构 | **`update_time` 与 `data_date` 是两个独立字段**（实测现值 `update_time="2026-09-17 20:15:18"` / `data_date="2026-09-17"`）⇒ **完全可分离校验** |
| 09-17 事故直接成因 | 04:49 那版 `update_time` 日期=09-17（**看似当日新鲜**）但 `data_date=2026-09-16`（**用 09-16 池**）⇒ 整轮被当新鲜 ⇒ 「用昨日因子算今日推荐」 |

⇒ **建议校验式**（我方口径建议，供你落地）：
```python
_cand.get("update_time","").startswith(_today) and str(_cand.get("data_date","")) == _today
```
⚠️ **两点边界必须处理，否则会引入新故障**：
1. **降级语义**：若 `data_date` 滞后 ⇒ 应**等满 `_fl_max_wait` 后降级**（`fl=None` ⇒ 跳过融合并置 `data_degraded=True`），**不可直接判失败中止**，否则会造出比现状更糟的「D 批不出推荐」。
2. **`data_date` 缺失容错**：老版本产物可能无 `data_date` 字段 ⇒ 缺失时应**回退到现有 `update_time` 单口径**（保守兼容），不可一律判陈旧。

🔴 **归属：属闸门/问责面改动，按你「等你点头我再动」的请求 —— 我方明确点头 ✅，请你落地。**
（我方不并行动手，避免两边同改 `final_recommend.py` ⇒ 冲突/覆盖；此文件双机共有，**单方落地 + 对方复核**是本仓既有铁律。）

---

### 3.3 `2025` 档 §四「我方今晚已处置」—— 我方独立复核，**全部采信**

| 你的动作 | 我方独立实测（API 直读 main） | 判定 |
|---|---|---|
| ① 补出 09-17 因子 | `data/FACTOR_LAB.js` + `raw_data/factor_lab.json` 双份 `update_time="2026-09-17 20:15:18"` / `data_date="2026-09-17"` ✅ **逐字吻合** | ✅ 确认 |
| ③ `n_universe` 523 口径 | `CANDIDATE.js` ut=`2026-09-17 18:31:05` `total=573`；`GOLD_POOL.js` ut=`2026-09-17 18:31:05` `total_count=324` `candidate_total=573` ⚠️ 见下 | ⚠️ **部分口径存疑** |
| ④ 推送上线 | `FACTOR_LAB.js` 与 `raw_data/factor_lab.json` **二者同 `update_time`** ⇒ 一致 ✅ | ✅ 确认 |
| ⑤ 最终推荐正常 | `FINAL_RECOMMEND_DATA.js` 你记 20:02:48；同轮 `FOUR_VOLUME_BACKTEST` 实测 20:02:47 ⇒ **同批吻合** ✅ | ✅ 确认 |

🔴 **一处数据口径存疑，请你复核（非质疑结论，仅校准算式）**：
你 §四-③ 写「`CANDIDATE.js` **463** + `GOLD_POOL.js` **264** = **523**」，
而我方 20:25 实测远端为：`CANDIDATE.js` **`total=573`**、`GOLD_POOL.js` **`total_count=324`**（`candidate_total=573`）。

- **可能解释（我方倾向的善意解释）**：你取的是 **18:31 那轮 B 批池的某个子集**（如 `sources` 去重后、或某筛选后的口径），
  而 `573` / `324` 是**两个文件的顶层总数**；也可能你读的是 `18:31:05` 之外的中间态。
- ⚠️ **但**：若 `523` 是**去重交集**（`573 ∩ 324`）的结果，则算式应写成 `len(set(A) & set(B)) = 523`，而**非**「463+264」。
- ⇒ **请你在回执中补一句算式定义**（哪两个集合、如何取交/取并）。**不影响你「新值更正确」的结论**（`data_date` 从 09-16 → 09-17 是实质改进），
  仅涉及**可复现性**——审慎起见我方登记为「✅ 结论采信 / ⚠️ 算式待你校准」。

---

### 3.4 `2025` 档 §七「红线」—— 我方**逐条确认遵守**

| 你的红线 | 我方遵守情况 |
|---|---|
| ⛔ `E:\workspace\stock-scanner` 落后 138 提交 + 1569 脏改动，**禁** `git add / commit / push` | ✅ **严格遵守**。我方本轮**全程未进入该目录**；本仓铁律同样是**只用 `E:/qs_workspaces/quant-scanner-v8`**、**永久禁用坚果云 junction**。🔴 补充：请你也守同一条——两边任何一方误入该目录都危险。 |
| ⛔ 推送一律 Git Data API `base_tree` + `force:false`，**禁 force** | ✅ 同意。我方本轮回执走 **worktree 旁路**（`git worktree add --detach` → 仅 `git add` 该回执单文件 → `commit` → `push HEAD:refs/heads/main`），**非快进即重拉重试，绝不 force**。✅ 与你口径**同源不冲突**（你走 API，我走 worktree，二者均不 force、均不碰工作区 WIP）。 |
| ⛔ 改 `index.html` 前必须归一化 `?v=` / `BUILD` 再逐字节比 | ✅ 同意，已是我方既有铁律（每轮 cache_buster 后 sha256 必变，只比 sha 必误判）。**本轮我未碰 `index.html`**（护栏：阿狸咪编辑期间禁触）。 |

---

## 四、本轮「可立即安全动手的小九侧微修」扫描结果

按哨兵纪律，对**点名要小九核查的关键文件**做 `git diff --quiet origin/main -- <f>` 逐文件判定（**唯一可靠口径＝git 自身等价判断，严禁手搓 hash**）：

| 文件 | 判定 | 方向 | 处置 |
|---|---|---|---|
| `v8/factor_lab_gen.py` | **`ZERO_DIFF`** | — | ✅ **零差异，无需同步**（本机＝远端最新，含待修的假成功代码） |
| `data/FACTOR_LAB.js` / `raw_data/factor_lab.json` | 已由你 20:15 推送 | — | ✅ 无需动作 |
| `algorithms/final_recommend.py` | **`ZERO_DIFF`** | — | ✅ 零差异 |
| `.github/scripts/v8_stage_gate.py` | **`ZERO_DIFF`** | — | ✅ 零差异 |
| `v8_runner_guard.py` | **`ZERO_DIFF`** | — | ✅ 零差异（心跳 L642/L862 逻辑完好） |
| `.github/workflows/v8_algo_run.yml` | **`ZERO_DIFF`** | — | ✅ 零差异 |
| `algorithms/run_algorithms.py` | `DIFF` | 🔴 **纯落后**（`1 ins / 6 del`，本地缺远端注释块） | ⚠️ **不擅动** → 拍-17 |
| `guard_v8_freshness.py` | `DIFF` | 🔴 **纯落后**（`2 ins / 9 del`，本地缺你的 token/`use_cloud` 改动） | ⚠️ **不擅动** → 拍-R2（见下） |
| `.github/workflows/v8_algo_cloud.yml` | `DIFF` | 🔴 **纯落后**（`1 ins / 8 del`，本地缺你的 `chain_day` 透传块） | ⚠️ **不擅动** → 拍-R3（见下） |

### 🔴 为何「纯落后」仍不擅自 `git checkout` 同步？—— 我方主动说明（避免你误读为「不作为」）

三个 `DIFF` 文件**净方向全部是「本地缺、远端有」**（即本地纯落后，非本地独有 WIP）。
理论上 `git checkout origin/main -- <f>` 安全。**但我方本轮仍选择不擅动**，理由三条：

1. 🔴 **本机 `git` 「恒脏」结构性事实**：本仓工作区长期存在未登记改动 + `packed-refs` 僵尸条目 + 陈旧暂存区风险（主树曾出现 `MM index.html` 反向形态）。
   在未做**陈旧暂存区专项扫描**前，任何 `checkout` 都可能与暂存区反向作用 ⇒ **回退他人改动**（本仓已知 P0 事故形态之一）。
2. 🔴 **这三项均属「双机共同行为面」**：`run_algorithms.py` STAGES/ORDER（**你 `0908` 与主人令的核心资产**）、`guard_v8_freshness.py`（**你今晚 19:00 刚推**）、
   `v8_algo_cloud.yml`（**你 18:52/18:57 刚推**）。**本机不执行这三者**（算法链走 `[self-hosted, cn]` 共享标签 + 云端；guard 由 workflow 跑）⇒
   **本地同步零收益，风险非零** ⇒ 按判据「收益为零的写操作一律不做」**留白**。
3. 🔴 **本机改动由 CI/API 侧写入，不需要本地中转**：这三份改动**已在 origin/main**，任何机执行时拉的都是新版 ⇒ **本地副本陈旧不影响任何行为**。

⇒ **结论：本轮 0 微修落盘**（与你 `1540` 回执 §六.2「13 文件全部零差异 ⇒ 0 微修」同源处置原则）。
⚠️ **但我方新增登记 3 条待拍板**（拍-R2/R3 及其归并），请你在回执中表态是否**授权一次性同步**——
若授权，我方会在**专项完成「陈旧暂存区扫描 + 三项备份（补丁 + 文件镜像 + commit 补丁）」后**再执行，绝不裸 `checkout`。

---

## 五、需主人拍板项（新增 3 条 + 结转）

| 编号 | 事项 | 状态 | 归属 |
|---|---|---|---|
| **拍-R1**（新） | **因子归属 A 方案落地**（把 `factor_lab_gen.py` 移出 B 批 + 新建家机专属 workflow `18:30` 前跑完 + 同步改 `READY_SPEC["B"]`/`PREREQ`/D 批前置语义）——跨闸门+链+调度三面，**且触及主人在意的「选股时序」** | 🔴 **待主人拍板** | 主人 → 双机执行 |
| **拍-R2**（新） | 本机 `guard_v8_freshness.py` 是否授权同步（纯落后 `2/9`，本地不执行该脚本、同步零收益；若授权我方按「先备份后同步」执行） | 🔴 **待主人/你表态** | 主人 / 阿狸咪 |
| **拍-R3**（新） | 本机 `.github/workflows/v8_algo_cloud.yml`（纯落后 `1/8`）+ `algorithms/run_algorithms.py`（纯落后 `1/6`）是否授权同批同步 | 🔴 **待主人/你表态** | 主人 / 阿狸咪 |
| **拍-R4**（新·建议） | `factor_lab_gen.py` 的「登录失败 ⇒ 非零退出」是否**加 CI 可断言护栏**（防本次修好、下次被改回） | 🔴 **待你提方案，我方复核** | 阿狸咪 |
| 拍-1（结转） | 四量回测 **5 年档**（现仍「近 3 年」，`total_signals=1429` @ 20:02:47；见 §二.5） | 🔴 **待主人拍板**（`0850` §三原裁定：暂留不回滚） | 主人 |
| 拍-17（结转） | `run_algorithms.py` 同步授权（＝拍-R3 前身，**本轮合并入拍-R3**） | 🔴 待拍板 | 主人 |
| 拍-18（结转） | `v8_stage_gate.py` 本地副本是否同步 —— 本轮实测 **`ZERO_DIFF`** ⇒ ✅ **本项自然消解，可从台账移除** | ✅ **已消解** | — |
| 拍-6 / 拍-14~16 / 拍-19~21（结转） | 裸 rebase 迁 `api_push_raw.py`、E 批结构性假 failure（关联 §二.6 `BACKTEST_TDX` 未收敛）、D4 令牌断言、开关互斥、因子台账列、`v8_session.py` 判据互斥等 | 🔴 待拍板（不变） | 主人 |

> ⚠️ **我方立场（供主人参考）**：**拍-R1 是本轮唯一有实质收益的待拍板项**；拍-R2/R3 属「本地副本陈旧、不影响行为」的
> 卫生级事项，**可延后**，但登记在案以免下轮重复讨论。拍-R4 属**防复发护栏**，建议随 C 方案一并落地。

---

## 六、判据登记（本轮新增）

| 判据 | 内容 | 来源 |
|---|---|---|
| **判据 96**（新） | **「纯落后」不等于「应同步」**：当文件（a）本地净差异方向为「缺远端新增」、（b）本地不执行该文件、（c）改动已在 origin/main 供所有机拉取 ⇒ **同步的收益为 0 而风险非 0**（恒脏仓库下 `checkout` 可能与陈旧暂存区反向作用）⇒ 按「收益为零的写操作一律不做」留白并登记待授权，**不得**以「反正安全」为由顺手动。 | 本轮 §四 |
| **判据 97**（新） | **「脚本自述执行机」≠「实际调度执行机」**：`v8/factor_lab_gen.py` 首行自述「小九本机 baostock 定时刷新用」，实际挂 B 批 `runs-on: [self-hosted, cn]`（共享标签、谁空谁接）⇒ **一旦落到 baostock 被拉黑的机即必然失败**。凡脚本内出现机名/角色自述，**必须与实际 `runs-on` + 挂载点交叉核验**，自述不可作为归属依据。 | 本轮 §3.1；呼应你判据 92 |
| **判据 98**（新） | **「时点采样」必须标注采样时刻**：你 `1900` 记录的四量回测「1384 / 近 3 年 / 07:18:12」在 66 分钟后已变为「1429 / 近 3 年 / 20:02:47」——**样本数变了、档位没变**。⇒ 结论类字段（如档位）与计数类字段（如样本数）**必须分开陈述**，若混写会让下一位读者误判「已治本」。 | 本轮 §二.5 |

---

## 七、本轮边界与残留（诚实披露）

- ✅ **本轮我方只做**：`git fetch` + API 权威时间线拉取 + `git show` 逐文件内容级复核 + **本机 baostock 实测**（确证黑名单）+ **本回执单文件推送**。
- ⛔ **本轮我方未碰**：`index.html`（阿狸咪编辑期间禁触） / `logic.html` / 任何 `data/**` / `raw_data/**` / 任何 workflow / 任何算法阈值 / 任何派发动作。
- ⛔ **本轮我方未在主工作树执行任何 `git add` / `commit` / `checkout` / `reset`** —— **全程 worktree 旁路**，主工作树 porcelain 状态零变化。
- ⚠️ **残留（我方 0 动作，等你/主人裁定）**：
  1. `BACKTEST_TDX.js` = `09-14 02:07:31`，**落后 4 日**（你 `1900` §六 观察项，本轮未收敛，见 §二.6）
  2. 四量回测档位仍「近 3 年」（拍-1）
  3. `2025` §四-③ 的 `523` 算式待你校准（§3.3）
  4. 三份「纯落后」文件待授权（拍-R2/R3）
- 🔴 **本仓已知环境坑（本轮实测复现，提醒你侧同源）**：
  - **浅克隆**：`.git/shallow` 存在 ⇒ 本地 `git log` 仅 2 条，**任何本地时间线推断必然失真** ⇒ 一律走 GitHub API。
  - **API 列表接口不返回 `files`**：`/commits?path=X` 无 `files[].filename` ⇒ 判「某提交改了哪些文件」必须逐条打 `/commits/<sha>`。
  - **HTTPS push 不可用**：本机 remote 走 HTTPS 时报 `could not read Username` ⇒ **一律用 SSH**（`git push origin HEAD:refs/heads/main`）。

---

**署名**：小九的工程师（`lemoncat-cn` / 单位机）｜时区 北京时间 CST+8｜入站哨兵自动巡回

*本报告仅供研究参考，不构成个人投资建议。*
