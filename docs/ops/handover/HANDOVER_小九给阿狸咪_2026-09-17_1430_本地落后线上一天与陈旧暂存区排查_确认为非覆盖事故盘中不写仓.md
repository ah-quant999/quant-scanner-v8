# HANDOVER · 小九 → 阿狸咪
## 2026-09-17 14:30 · 本地工作树落后线上一天 + 陈旧暂存区排查（**非覆盖事故**）

---

## 一、触发原因

在核查「看板层是否需要同步分流口径」时，探查本地 `index.html` 发现：

```
本地 index.html  1269966 B  md5=81e3e0c83230601d7c2e002048e480c2
  _fillSectorLeaders 命中 = 0   ← ⚠️ 本地没有 09-17 推的改动！
线上 tip 上 index.html  blob=c7b3f76829d98c6b  size=1257616
```

差异 **12350 B**，且本地缺 `_fillSectorLeaders`。表面看像「改动丢了」，实际不是。

---

## 二、排查结论：**非覆盖事故，属预期状态**

### 2.1 三个 SHA 的时间序列（用 GitHub API 实查）

| 位置 | SHA | 时间 (CST) | 说明 |
|---|---|---|---|
| 本地 `HEAD` | `45f31ea709e4` | **09-16 19:44** | 昨晚最后一次本地提交 |
| 本地 `origin/main` 引用 | `7a01407b84ac` | 09-17 13:24 | 陈旧引用（未 fetch 到最新） |
| **线上真实 tip** | `b80ce6456242` | 09-17 13:56 | `v8 cn fetch: 2026-09-17 13:56` |

**三者是连续推进，不是互相覆盖。**

### 2.2 我推的内容在最新 tip 上**全部完好**

```
v8_cloud_watchdog.py           67575 B  4c488a8e0a8f702ed2cf5dfb64b91726  ✅ MATCH
scripts/fetch_sector_leaders.py 10247 B  5bc6655f656e3e2df9e5af93717184a5  ✅ MATCH
data/SECTOR_LEADERS.js            867 B  6fbb4f972bd6693846bb9b9e2bd97ad0  ✅ MATCH
algorithms/run_algorithms.py    90160 B  377974dcf282b1a2844028862631be2b  ✅ MATCH
index.html  blob=c7b3f76829d98c6b  size=1257616  （= 我推的版本）
```

### 2.3 Pages 主站硬抓 100% 通过

```
https://ah-quant999.github.io/quant-scanner-v8/
  index.html  1257616 B  md5=9265ef1cbd5ef34cdad5f16c7d827d09
  _fillSectorLeaders = 2   sectorLeadersBox = 1
  主升板块 = 6             SECTOR_LEADERS = 7
  data/SECTOR_LEADERS.js  867 B（真实数据：元件 BK0459 5日+10.21% 20日+10.52%）
```

### 2.4 根因（预期行为，非故障）

```yaml
# 我的推送全程走 GitHub Git Data API 隔离单文件推送：
#   blob → tree(base_tree) → commit(parents=[tip]) → PATCH ref(force:false)
# ⚠️ 这条路径【完全不走本地工作树】⇒ 本地 index.html 当然不会有改动。
#    这是设计如此，不是丢失。
```

---

## 三、陈旧暂存区排查（**不是地雷，是正常噪声**）

`git status` 显示 **177 个文件变更**，大量 `MM` 双态。逐一核查 `git diff --cached` 内容：

```
index.html | 482 ++++----      166 insertions(+), 316 deletions(-)

删除行样本：
-  var BUILD = "7369a6497";   ← 旧 BUILD 短 SHA
-  <script src="data/XXX.js?v=1789558388" ...>   ← 旧缓存戳（×105 行）

新增行样本：
+  var BUILD = "0e1dae697";   ← 新 BUILD 短 SHA
+  <script src="data/XXX.js?v=1789569135" ...>   ← 新缓存戳（×105 行）
+  /* 2026-09-16 主人令：观测平台两个子页签「字体放大、显著一些」 *.obs-tab{...font-size:15px...}
```

**判定：106 行 = 105 条 `?v=` 缓存戳 + 1 行 `BUILD`，全部是 CI 注入噪声，无实质逻辑增删。**

对照本仓铁律：
> 推后 md5 变 ≠ 丢失（CI 注入 BUILD + ?v=~102 行）

---

## 四、处置决定：**盘中止写，不做任何本地写操作**

### 4.1 现在时刻 14:30，**处于盘中（13:00-15:00）** ⇒ 触写仓时间窗红线

```
🔴 写仓时间窗铁律：只盘前部署完(~08:58)→09:15硬停、午休11:30→13:00、15:00后。
   现在 14:30 ∈ 盘中 ⇒ 禁止任何本地写操作（reset / checkout / fetch / pull）。
```

### 4.2 本轮**未执行**的本地操作（刻意不做）

| 操作 | 为何不做 |
|---|---|
| `git fetch` | 盘中；且 `origin/main` 陈旧引用需 `--update-shallow` 才准 |
| `git reset --hard origin/main` | 盘中 + 会丢掉 177 个文件的工作树状态 |
| `git reset`（清暂存区） | 盘中 + 暂存区内容已确认无害，无紧急度 |
| `git checkout -- index.html` | **绝对不行** —— 会把本地 20 行未提交改动抹掉 |

### 4.3 遗留风险（供 15:00 收盘后处理）

1. **本地落后线上 1 天**（`HEAD=45f31ea7` vs `tip=b80ce645`）。
   下次本地开发**必须**先对齐基线，否则会在旧基线上改、推时被 CI 打回。
2. **陈旧暂存区仍在**（177 文件）。虽然当前内容是噪声，但**必须清掉**：
   ```bash
   git reset            # 清暂存区，保留工作树（安全）
   # 清完再核对 git status 只应剩真正的本地改动
   ```
   ⚠️ **禁止** `git commit` —— 会把 105 条旧缓存戳 + 旧 BUILD 提上去，导致线上 `?v` 回退。
3. **本地 index.html 是 09-16 版**（1269966 B），比线上少 `_fillSectorLeaders` 等 09-17 全部改动。
   收盘后应从线上取最新真身覆盖本地，或做基线对齐。

---

## 五、对「看板层分流」的结论（原待办 ①）

探查本地 `index.html` 关键词：

```
conclusion = 3   闸门 = 11   v8_algo_cloud = 10   build_deploy = 6
algo_chain = 0   结果问责 = 0   run_number = 0    合规拒绝 = 0
```

⇒ **前端运维面板并不直接解析 algo run 的 conclusion / step 结论**，
它读的是数据产物（`data/HEALTH_CHECK.js` 等）。**因此看板层无需同步分流口径** ——
分流只需落在看门狗（已上线）。

⚠️ 但此结论基于 **09-16 版 index.html**（本地那份）。为稳妥，
**建议 15:00 后用线上版 index.html 复核一次**（见四.3）。

---

## 六、本批最终文件状态（线上 = 目标）

| 文件 | size | md5 | 状态 |
|---|---|---|---|
| `v8_cloud_watchdog.py` | 67575 B | `4c488a8e0a8f702ed2cf5dfb64b91726` | ✅ |
| `scripts/fetch_sector_leaders.py` | 10247 B | `5bc6655f656e3e2df9e5af93717184a5` | ✅ |
| `data/SECTOR_LEADERS.js` | 867 B | `6fbb4f972bd6693846bb9b9e2bd97ad0` | ✅ |
| `algorithms/run_algorithms.py` | 90160 B | `377974dcf282b1a2844028862631be2b` | ✅ |
| `index.html` | 1257616 B | blob `c7b3f76829d98c6b` | ✅ |

**本批 6 个提交**：
`9cfd099baf84` · `4ce3379ef0ae` · `fb77efac2005` · `b549e12782d6` · `075c365abec2` · `a49ad878bd9f`

---

*小九 · 2026-09-17 14:30*
