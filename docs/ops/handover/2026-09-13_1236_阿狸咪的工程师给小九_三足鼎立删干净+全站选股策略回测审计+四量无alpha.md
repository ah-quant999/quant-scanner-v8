# 2026-09-13_1236 · 阿狸咪的工程师 → 小九

> 主题：**三足鼎立删干净 + 全站选股策略回测审计 + 四量终极独立复算＝无 alpha**

---

## 一、已上线（我推的，无需你动手）

| commit | 内容 |
|---|---|
| `9653c8972e` | 删干净「三足鼎立」+ 修正虚假来源标签（14 处改动，`match=True`） |

### 为什么要删（实测依据）

复用 `scanner.py` **自身**四信号函数，对 `raw_data/kline_cache` 3120 只 × 约 250 根日K（**325,466 个股票日**）全量回溯 ⇒ `signal_count == 4` 触发 **0 次**。

卡死点＝「金钻」：含金钻的三腿组合全年最稀有仅 **7 次**（缠论∧金钻∧上涨趋势）；而缠论∧机构变红∧上涨趋势有 **1231 次**（＝全部三线共振）。三条腿天然发生在趋势的不同阶段，互斥。

交叉自证：拿 `gold_pool.json` 里 scanner 每天真实写入的 `signal_count` 反查复算，09-08/09-09 逐条 **100% 一致**；真实分布 `{0:46,1:175,2:337,3:10}` 也从无 4。

### 顺带揪出的真 bug（你可能也受影响）

`scanner.py` 把 `["三足鼎立"]` 当 **sources 兜底默认值**写死 ⇒ 凡池中查不到记录的股票都被标成「三足鼎立」。**产物铁证：253 只里 196 只带此标签（77%）**，而真信号全年 0 次。前端把 sources 渲染成来源 chips 并按来源分组 ⇒ 长期展示不实信息。

**处置**：三处入池标签 → `["信号≥2"]`（真实门槛）；两处兜底 → `[]`；新增 `_migrate_legacy_sources()` 挂 `load_gold_pool()` 两处 return ⇒ 历史 196 只旧标签**就地改名**（幂等·去重保序）。

> ⚠️ **请勿**再往 `sources` 里塞无信息量的常量标签——`_merge_sources` 只追加不删，会永久残留。

---

## 二、🔴 需要你办的（P0，影响主人看到的数字）

### P0-1 CRDS：`lookahead_days` 常量没接线 ⇒ T+30~T+90 永久 0 样本

`v8/backtest_crds.py`：

```python
LOOKAHEAD_DAYS = 150                                            # L60 已定义
def fetch_kline_around(..., lookahead_days=LOOKAHEAD_DAYS):     # L176 默认值已接 ✅
    ...
    rows = fetch_kline_around(code, signal_date, lookback_days=8,
                              lookahead_days=35)                # ❌ L295 硬编码 35 覆盖默认值
```

L58 注释自己写着「原 35 只够 ~24 个交易日，90 交易日档会静默零样本」——**但修复没接线**。35 自然日 ≈ 24 交易日 < 30 ⇒ T+30/45/60/75/90 恒 0，与 `CRDS_BACKTEST.js` 实测完全吻合。

**修法**：L295 的 `lookahead_days=35` → `lookahead_days=LOOKAHEAD_DAYS`。

### P0-2 RPS：同一个 bug，而且**完全没修**

`v8/backtest_rps.py`：L142 函数默认值 `=35`、L245 调用 `=35`，`LOOKAHEAD_DAYS` **常量根本没定义**。`v8/backtest_rps_offline.py:104` 同样是 35。

### P0-3 四量终极 `max_drawdown` 是**伪值**

`strategy_four_volume.py:488-498` 把每笔 net 收益**直接累加**再算峰谷 ⇒ 得到 `−133.56%`。**回撤的数学上界是 −100%**（本金亏光），−133% 概念上等于「亏掉本金再倒欠 33%」。

我的复算三口径对照（T+20）：累加正序 **−1255.59%** / 按时序累加 **−6702.72%** / **逐笔持仓期内回撤中位 −14.01%**。三种「累加」口径互差 5~10 倍且都远超 −100% ⇒ 说明它是「多笔独立收益的累计盈亏点数」，**不是回撤**。

> 对照：`backtest_comprehensive.py:390-399`（三重共识）算法**正确**（单笔持仓期内价格峰谷），且聚合层**诚实写 `null`**。

**修法**：改用与 `backtest_comprehensive.py` 同口径（逐笔持仓期内价格峰谷），或聚合层诚实写 `null`。

---

## 三、🔴 四量终极（★★★★★）独立复算：**无 alpha**（最重发现）

用 `strategy_four_volume.py` **自己的信号函数**在 kline_cache（3120 只 × 近 1 年 = 325,466 股票日）独立复算，并加**全市场随机基准**对照：

| 持有 | 信号胜率 | 基准胜率 | 胜率差 | 信号均值 | 基准均值 | 判定 |
|---|---|---|---|---|---|---|
| T+5 | 47.1% | 46.6% | +0.5 | +0.51% | +0.13% | ≈基准 |
| **T+20** | **48.7%** | **47.7%** | **+1.0** | +1.29% | +0.92% | ≈基准 |
| T+60 | 33.1% | 33.3% | −0.2 | −2.49% | −2.55% | ≈基准 |
| T+90 | 34.9% | 31.5% | +3.4 | −0.66% | −2.68% | ≈基准 |

**10 个持有期全部 ≈ 随机**（胜率差 ≤3.4pp、均值差 ≤2.02pp）。

对比官方（3 年 · 活跃池 210 只）：T+20 **58.5%/+8.11%**、T+90 **72.2%/+39.91%**，胜率**完美单调上升**——但在另一窗口里直接翻转为下降。

**归因**（三条）：
1. **股票池生存者偏差**：`fetch_volume_top_stocks()` 取「**当前**活跃 top 210」去回测**过去 3 年**，天然筛掉没落者。
2. **样本期 beta**：2023-09~2026-09 含大牛市段，长持有期自动吃 beta。
3. **重叠样本**：1465 个信号分布在 210 只里，同股票连续多日命中是常态 ⇒ 样本不独立。

> ⚠️ 官方 `FOUR_VOLUME_BACKTEST.json` **只有 summary 聚合、无逐条记录** ⇒ 无法做样本独立性检验。若要让结论可审计，建议输出逐笔（`signal_date/code/entry/ret_N`）。

---

## 四、全站策略回测现状（系统已有 `BACKTEST_ALL_ALGOS`，7 源 / 40 行）

| 策略 | 主口径 | 样本 | 胜率 | 均值 | 判定 |
|---|---|---|---|---|---|
| 三重共识 | 共振≥80·T+20 | 119 | **61.9%** | **+2.30%** | ✅ 保留（rank 1） |
| 四量终极 | T+5 | 1435 | 50.9% | +2.16% | ⚠️ 见 §三 |
| 相对强度 RPS | T+5 | 36 | **19.44%** | **−3.96%** | ❌ 已烂透 |
| 逆势龙头 CRDS | T+5 | **4** | 100% | +26.88% | ⚠️ 样本 4 |
| K线信号层 | T+1 | 800 | 53.9% | +1.21% | ✅ 保留（5日 58.3%） |
| 因子实验室 | T+10 价差 | 673 | 52.6% | 价差 −0.194% | ⚠️ 研究型 |
| 强势突破 | — | **null** | — | — | ❌ 源文件不存在 |
| 候选池 / 黄金池 / 机游共振 | — | — | — | — | ⚪ **零回测** |

### 🔴 `delist_advice` 恒空的政策盲区

下架判据是「卡级 · 样本≥30 · **且源在算法链内**」。后果：**RPS T+5 胜率 19.44% / 均值 −3.96%**（同时击穿 45% 与 0% 两条红线），却因「不在链内」被豁免 ⇒ 只进 `watch_list`，`delist_advice` 是**空数组**。**烂策略因为「没进链」反而不被清退。**

---

## 五、请回执 / 请拍板

| P | 事项 | 归属 |
|---|---|---|
| P0 | CRDS / RPS `lookahead_days` 接线（一行） | 你（白天独家） |
| P0 | 四量 `max_drawdown` 口径修正 | 你 or 我，请示主人 |
| P1 | 强势突破删 / RPS 降级 | 请示主人 |
| P1 | `delist_advice` 政策盲区修法 | 请示主人 |
| P2 | 四量终极补「基准对照」列 | 谁做都行 |
| P2 | 候选池/黄金池/机游共振补回测 | 谁做都行 |
| P2 | 「三线共振」去留（T+5 已破 45% 红线，1231 样本） | 请示主人 |

**待你回执（历史遗留）**：
1. `data/hb_xiaojiu.js` 心跳文件——远端仍不存在 ⇒ 双机掉线告警**恒不触发**
2. `v8_health_check.py` 版本分歧（我 01:27 问过，未见回复）

---

## 六、红线遵守

- 全程**未在仓库内 `git add`**，未改工作树（FETCH_HEAD 派生 + 临时索引 `commit-tree` 直推）
- 脚本/产物在会话目录（`_fvcheck/`、`_btfocus/`、`_tripod/`），仓库内**零 `.bak`** 新增
- **未擅自修改任何策略逻辑**——四量/CRDS/RPS 的 bug **只报不改**，等主人拍板
- 自查披露：`9653c8972e` 提交信息中 `A1 删 定义` 一行少了引用文字（bash 吞掉反引号内容）；**代码 100% 完整**（dry-run + 线上 grep 双验证）

---

阿狸咪的工程师 · 2026-09-13 12:36
