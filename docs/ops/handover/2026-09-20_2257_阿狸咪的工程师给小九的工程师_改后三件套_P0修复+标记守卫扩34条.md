# 阿狸咪 → 小九 · 2026-09-20 2257 · 改后三件套（P0 修复 + 标记守卫扩 34 条）

> 规范：`docs/ops/handover/README.md`。时间**实取**北京时间（本机 `datetime.now(CST+8)`）。
> 本轮范围：`b2d217a8e8`（恢复 `_renderPostReady`）、`01d0ff4d96`（标记守卫 32→34 条 + P0 交接档）。
> 三件套每项附**原始输出**，不写预测式承诺。

---

## 一句话结论

本轮改动 = `index.html` 引导函数恢复 + `docs/ops/index_protected_markers.txt` 扩 2 条锚点，**均不涉及 workflow** ⇒
**① 时窗矩阵无需调整**（逐条对表一致）；**② 门禁三件全绿，且跑在远端 tip 树上**（非本机旧树）；
**③ 本档即交接件**。另登记一项本机状态：**阿狸咪本机 clone 为浅克隆**（详见 §五）。

---

## ① 时窗矩阵对表（原始输出）

采集：实时 `git/ref/heads/main` → tip `9d989f5f5`，逐 workflow 走 raw 取原文，正则 `^\s*-\s*cron:\s*['"]([^'"]+)['"]`，UTC **+8** 折算 CST。

```
实时 tip = 8d6882a65   （采集时点，随后被 CI 推进至 9d989f5f5）
workflow 文件总数      = 30
含 cron 的 workflow 数 = 21
cron 条目总数          = 50
```

关键条目断言（实测 → 期望）：

| 项 | 实测 cron | CST | 基线（09-19 §三） | 判 |
|---|---|---|---|---|
| `cloud_weekly_cleanup.yml` | `0 13 * * 6` | 周六 21:00 | 21:00 周六 | ✅ |
| `v8_algo_cloud.yml` | 8 档：`40 8` / `10 10` / `0 12` / `0 13` / `30 14` / `30 16` / `0 22` / `0 0` | 16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30(+1) / 06:00(+1) / 08:00 | 同 8 档 | ✅ |
| `v8_backup.yml` | `0 15 * * *` | 23:00 日频 | 23:00 | ✅ |
| `v8_cleanup.yml` | `0 15 * * 0` | 周日 23:00 | 周日 23:00 | ✅ |
| `v8_lhb_fetch.yml` | `30 9 * * 1-5` | 17:30 工作日 | 17:30 | ✅ |
| `v8_cn_fetch_intraday_lemoncat.yml` | 14 档 `{0,20,40} {1,2,3,5,6}` + `0 7` | 09:40–15:00 14 档 | 同 14 档 | ✅ |
| `v8_t1_guard.yml` | `30 0 * * 6` / `30 1 * * 6` | 周六 08:30 / 09:30 | 08:30 / 09:30 | ✅ |
| `runner_health_alert.yml` | 3 条（`0 5,7,9,11,13 * * 1-5` / `0 16,18,20,22 * * 5,6` / `0 0,2,4,6,8,10,12 * * 6,0`） | 工作日 13/15/17/19/21；周六日 08–20 每 2h；周五六夜 +1 日 00/02/04/06 | 同 | ✅ |

**交叉验证（独立第二源）**：门禁二 `align_logic_ops.py` 自带计数——

```
🔧 实际有 cron 的 workflow：21 个；名单：MUST=21 / ALLOW=3
✅ 对齐校验通过：关键 workflow 均已文档化，无过期引用，名单无死登记。
```

⇒ 我的矩阵脚本实测 **21** 与 `align_logic_ops` 的 **MUST=21** **双向一致**。

### 本轮「workflow 侧唯一改动」的取证（结论：零改动）

我经手 4 笔提交，逐笔核 `.github/workflows/` 改动文件数：

```
b2d217a8e -> workflows 改动文件数=0     （fix(index): 恢复 _renderPostReady）
01d0ff4d9 -> workflows 改动文件数=0     （fix(gate)+docs: 标记守卫 32→34）
176d1a834 -> workflows 改动文件数=0     （fix(insight): 每日洞察月度汇总排版）
5bd826b60 -> workflows 改动文件数=0     （fix(hooks): scripts/install_git_hooks.py）
```

⇒ **结论：矩阵无需调整**（无新增/删除/改动任何 cron）。

### 家机（阿狸咪）ACTIVE 自动化侧

本轮新增 1 项并登记入矩阵：

| 自动化 | CST | rrule | 冲突判定 |
|---|---|---|---|
| 阿狸咪-本机心跳上报（反向心跳发送侧） | 每 2h **:41** | `HOURLY;INTERVAL=2;BYMINUTE=41` | 与既有档位 `:00`(自愈) `:05`(暴风盯梢/星级守卫) `:15` `:25/:55`(派发源) `:30`(按铃/心跳监控/巡检) `:35`(星级守卫) **全不重叠** ✅ |

其余 ACTIVE 项与 09-19 §3.2 一致，无错峰冲突。

---

## ② 离线门禁三件（跑在**远端 tip 树**，非本机旧树）

🔴 **为什么不直接在家机仓里跑**：家机工作区 `index.html` 采集时**落后于远端**（缺你 22:20 的 CRDS 两列改动，归一化后差 52 行 hunk）⇒
在旧树上跑门禁得到的结论**不对应线上**。按 §②-补「门禁结论必须绑定它跑在哪棵树上」，本轮改走**仓库外 staging + 远端 tip 整树导出**：

```
git archive FETCH_HEAD | (python tarfile) -> _stage
extracted files = 6011
index.html bytes = 1337548        （== 远端 tip index.html，逐字节）
staging 含 .github/scripts/pre_deploy_audit.py ✅  docs/ops/index_protected_markers.txt ✅
```

门禁一 `pre_deploy_audit.py`（**10 项**）原始输出：

```
  ✅ [1/8] py_compile: 194 个 .py 文件 0 错误
  ✅ [2/8] new Function: 28 个 inline script 块 0 错误
  ✅ [3/8] data 完整性: data/*.js 数量=113（上界 130（动态＝面 108 ＋ 容差 20））, 全部 > 100B
  ✅ [4/8] align_logic_ops: align_logic_ops EXIT 0（逻辑详解页与真 workflow 对齐）
  ✅ [5/8] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
  ✅ [6/8] HTML 数据引用: 179 个本地 script 引用全部存在（4 页）
  ✅ [7/8] gate 头注一致: 闸门头注与 READY_SPEC 真源一致（项数/门槛/P0 专项）
  ✅ [8/8] 心跳产物名一致: 心跳产物名大小写一致（权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移）
  ✅ [9/9] 回测口径守卫: entry_mode=next_open cost=0.2 n=46 date_range=['2026-06-08','2026-09-18'] 覆盖=with_kline 686/803 (85.4%) occ_total=1703
  ✅ [10/10] index 核心标记守卫: index.html 核心标记 34/34 全部在位（卡片/分节/数据源锚点）
============================================================
🎉 10 项全部通过 → deploy 可继续
EXIT=0
```

门禁二 `align_logic_ops.py`（独立跑）原始输出：

```
📚 文档源（三源合并）：index.html#sec-lg 0 / index.html#sec-op 616655 / logic.html#sec-lg 527044 字符
🔧 实际有 cron 的 workflow：21 个；名单：MUST=21 / ALLOW=3
✅ 对齐校验通过：关键 workflow 均已文档化，无过期引用，名单无死登记。
EXIT=0
```

门禁三 `v8_verify_layer_parity.py` 原始输出：

```
🔍 跨层一致性校验完成：检查 77 对，缺失 raw 0 个，缺失 data 0 个，无法校验 5 个，
   真错位 0 个，待重建 0 个，data 层陈旧 0 个
   时段判定：当前 CST 2026-09-20 22:56，已过盘后重建窗口结束点 (20:30)
  ⚠️ raw_data 无日期字段，无法校验：SECTOR_PHASE_HISTORY / INDEX_HISTORY / HB_XIAOJIU / HB_ALIMI / IMA_STRONG_BACKTEST
✅ data/*.js 与 raw_data/*.json 时间戳一致，且无陈旧静默上线风险
EXIT=0
```

> 注：`[10/10]` 报 **34/34**，与我方 `01d0ff4d96` 扩至 34 条一致（新增 `function _renderPostReady` 与其 `DOMContentLoaded` 调用点两条锚点）。
> 该守卫已用**回退版 A/B 实测可拦**：放入回退版 ⇒ `❌ 1/34 条核心标记消失：function _renderPostReady` 阻断 deploy；修复版 ⇒ `✅ 34/34`。

---

## ③ 改动清单与交接事项

### 3.1 我方本轮落仓

| 提交 | 内容 | 状态 |
|---|---|---|
| `b2d217a8e8` | `index.html` 恢复被 `0d3482be4` 误删的 `_renderPostReady` 定义（+868B），修策略回测页 `ReferenceError` 整页不渲染 | ✅ 线上 |
| `01d0ff4d96` | `docs/ops/index_protected_markers.txt` 32→34 条 + P0 交接档 `2230` | ✅ 线上 |
| `176d1a834` | `index.html` 每日洞察月度汇总排版修复 | ✅ 线上（本轮独立复核存活） |
| `5bd826b60` | `scripts/install_git_hooks.py` 补齐双钩子安装 | ✅ 线上 |

### 3.2 请小九侧留意的两点

1. **删死代码纪律（P0 教训）**：`0d3482be4` 在删 `__renderFactorLab` 时把 `function _renderPostReady` 定义整行带走，而 L8353 的
   `document.addEventListener('DOMContentLoaded', _renderPostReady)` 仍在 ⇒ **函数引用作 addEventListener 第二参数＝求值即抛**，
   异常发生在 `<script>` 顶层 ⇒ 该块后续语句与其后注册全部不生效 ⇒ `__renderBacktestSection`/`__renderLifecycleSection` 等 **11 个渲染器**从不上屏。
   **纪律：删任何函数前必须 grep 该标识符的调用点；被 `addEventListener`/`DOMContentLoaded` 引用的引导函数，删函数必须同步改调用点。**
2. **你 22:20 的 CRDS 两列改动与我方修复已在最新 tip 共存无冲突**（本档 §①/§② 的远端树取证即证）。

### 3.3 无需你侧动作

本轮**不含**：workflow 变更、`.github/scripts/**` 变更、算法/数据管线变更。无跨机阻塞项。

---

## 四、复核命令（现取现验，路径用相对仓内路径）

```bash
# 1) 远端权威 tip（勿信本地缓存 / 勿信 SHA 记忆）
git ls-remote origin refs/heads/main

# 2) 标记守卫真源条数（应含新增两条锚点）
grep -c . docs/ops/index_protected_markers.txt
grep -n "_renderPostReady" docs/ops/index_protected_markers.txt

# 3) 引导函数在最新 tip 存活（应各 1）
git show FETCH_HEAD:index.html | grep -c "function _renderPostReady"
git show FETCH_HEAD:index.html | grep -c "DOMContentLoaded', _renderPostReady"

# 4) cron 数双向一致（门禁二自带计数，应与矩阵实测同数）
python align_logic_ops.py | grep "实际有 cron 的 workflow"
```

---

## 五、本机状态登记（只登记，不擅动）

| 项 | 实测 | 说明 |
|---|---|---|
| 阿狸咪本机 clone 形态 | **浅克隆**（`.git/shallow` 存在，`rev-list --count HEAD`=39，最早可见 09-20 19:24） | ⇒ **跨日 `git log --before` / 树级历史比对在本机不可用**；本机不得作为「历史变更」的判据源 |
| 本机 HEAD vs 远端 | `5bd826b60`（21:39）< `9d989f5f5` | 跟随者仓常规滞后 |
| 本机工作区 `index.html` | **已追平远端**（md5 `4e44eab1aa`，1337548B == 远端 tip） | 追平前逐行核过「本地独有=0」（12 行差异全是被远端替换的旧形态，无未推送独创内容） |
| 本机 `data/*.js`、`raw_data/*.json` 脏项 | 跑批链实时写入 | 正常态，**不本地提交、不 reset** |

> 🔴 由此提醒：**判「本机是否落后/是否覆盖」在阿狸咪侧必须走远端 API / `git ls-remote`，不能依赖本机 git 历史**（浅克隆会给出结构性错误答案）。

---

*阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-20 22:58 CST（时间实取本机 `datetime.now(CST+8)`）*
