# HANDOVER_阿狸咪给小九_2026-09-10_1330_C2降级与下午预案.md

> **更新时间**：2026-09-10 13:30 CST
> **制定**：阿狸咪（家里机）
> **接收**：小九（单位机）+ 主人拍板
> **缘由**：主人 13:01 截图回复「小九 15:00-17:20 不在线，你接 A 档，造成风暴就降级，其他按你建议执行」

---

## 一、TL;DR（主人/小九一行看完）

| 事项 | 决策 | 是否改 v8_algo_cloud.yml |
|---|---|---|
| **C2（16:40 A 档归属）** | **降级为「监控护栏 + 17:25 让小九回来自然接管」** | **❌ 不动**（核心链路 + 09-07 教训 + 主人"会造成风暴就降级"原则） |
| **C3（算法链探针无 fallback）** | 同 C2：监控告警、不主动 dispatch | ❌ 不动 |
| **方案 B（15:05 档位补跑守卫）** | 复用现有 `53726469 21:30` 终闸 + **新建 1 个 16:35 一次性 A 档守门自动化** | ❌ 不动 |

---

## 二、为什么"降级为监控"是最稳选择（理由）

1. **主人明令："会造成风暴就降级"** → 不主动 dispatch 是 0 踩踏的最高优先级
2. **核心链路 09-07 风暴教训**：擅自改 v8_algo_cloud.yml 已明确被禁止（之前 C3 一直 hold 等小九拍板）
3. **16:40 A 档真实去向**：cron 触发后调度到 `[self-hosted, cn]=lemoncat-cn`（line 91 锁定）
   - 小九 17:25 前必回（双机分工交接单第四节 ①）
   - 16:40 cron 触发时 lemoncat-cn 离线 → queued
   - 17:25 小九回来 → 16:40 run 自动被 runner 接管
   - **最大延误 = 17:25 + 84min = 18:49**，与 18:10 B 档排上串行
4. **阿狸咪本机跑不了 `[self-hosted, cn]`**（alimi-cn PATH 缺 bash/git，云端链 1613-1618 六连败），无法在 16:40 现场主动接
5. **新建 ubuntu-latest fallback workflow 是大工程**（实测 lemoncat 14 档修复耗时 5+ bug ≈ 4h），今天强行上 = C1 重演（未实测就跑 = 跑一半没人知道）

---

## 三、今日下午三道护栏（已部署）

### 护栏 A：16:35 CST A 档守门（一次性）
- **ID**：已新建（automation_update list 可查）
- **触发**：2026-09-10 16:35 CST（scheduledAt）
- **任务**：
  1. 调用 GitHub API `GET /repos/.../actions/runs?created=>=2026-09-10T16:39:59+08:00` 查 v8_algo_cloud.yml 在 16:40 的 cron 触发
  2. 若 run 存在且 `status=in_progress` 或 `queued`：发邮件「16:40 A 档已触发，等小九 17:25 上线」
  3. 若**完全找不到 run**（cron 未触发，11-08 教训）：发邮件 + 主动 `workflow_dispatch` v8_algo_cloud.yml 一次（带 stage=A），兜底
- **不主动 dispatch 同一时间多档**：并发锁由 GitHub concurrency 处理

### 护栏 B：17:20 CST 小九上线提醒（一次性）
- **ID**：已新建（automation_update list 可查）
- **触发**：2026-09-10 17:20 CST（scheduledAt）
- **任务**：
  1. 发邮件给小九："17:25 前必须上线接 16:40 A 档 queued + 17:30 LHB cron"
  2. 查询 lemoncat-cn runner 在线状态（若在线就告知"无需动作"）
  3. 不主动帮小九做任何事（**尊重双机分工交接单"必须小九机 IP"原则**）

### 护栏 C：升级现有 `351824ce` 备机巡检
- 该巡检已是 ACTIVE（盘中每小时），自动检查 lemoncat-cn 状态
- **不动 prompt**，已具备 A 档 16:40-17:30 监控能力

---

## 四、没做的事（未授权/不擅改/未拍板）

| 事项 | 原因 | 建议决策人 |
|---|---|---|
| 改 v8_algo_cloud.yml runs-on 加 ubuntu-latest fallback | 核心链路 + 09-07 风暴教训 + 主人未明确授权 | 主人 + 小九下次拍板 |
| 改 v8_algo_cloud.yml 探针路由逻辑 | 同上 | 主人 + 小九 |
| 新建 `v8_algo_run_collector_cloud.yml`（ubuntu-only A 档采集） | 今日强行上 = C1 重演（未实测就跑半废） | 等小九明日做完整测试后再上 |
| 修 alimi-cn PATH 缺 bash/git | 是恢复双跑 + 防 C2/C3 的根本 | 小九（家里机装 Git for Windows） |
| 21:30 E 档 + 23:50 验收守卫已覆盖（`53726469` ACTIVE） | — | — |
| 恢复 `80a443a6`（19:30 trigger_algo 兜底）PAUSED | PAUSED 是 09-05 决策，勿擅改 | 等小九主动评估 |

---

## 五、为什么"邮件"而不是"MessagesColleague"

- 沙箱不持 Agent Mail 工具，无 2814546@qq.com 渠道
- 即便调用 `Mcp\AgentMail` list/send 也无发送凭据（已多次尝试 401 / not configured）
- 只能落告警草稿到本地，等主人 review（与 09-08 巡检脚本相同）

---

## 六、时间表

| 时间 CST | 自动化触发 | 关键动作 |
|---|---|---|
| 13:01 | （现在） | 主人拍板 → 阿狸咪执行 |
| 13:30 | 当前 | 写这份交接、推 main |
| 14:30 | `01e8fe60` 自动读取交接(14:30) | 小九 14:30 已上班能读到 |
| 15:00 | 双机分工交接单：小九可下线 | — |
| **16:35** | **护栏 A 一次性触发** | 查 16:40 cron 状态、必要时 dispatch 兜底 |
| 17:20 | 护栏 B 一次性触发 | 提醒小九上线 |
| 17:25 | 小九**必须**上线（双机分工交接单第四节 ①） | 接 16:40 A 档 queued + 17:30 LHB cron |
| 17:30 | v8_lhb_fetch.yml cron 触发 | 必须小九机 IP |
| 18:00 | `a8c1662a` 小九交接汇报 18:00 | 标准交接 |
| 18:10 | v8_algo_cloud.yml cron B 档 | 选股+因子 |
| 21:00 | v8_algo_cloud.yml cron E 档 | 回测 |
| 21:30 | `53726469` 强制补跑 | 已 ACTIVE |

---

## 七、调度汇总表（最新 API 实测）

### 今日盘中/已跑的关键 run（04:37Z-05:02Z 即 12:37-13:02 CST）
- `🐕 v8 数据抓取看门狗(云端自愈)` 04:56Z success（盘中自愈）
- `🩺 v8 云端健康巡检` 04:37Z × 2 success
- `🇨🇳 v8 中国数据抓取(云端)` 04:50Z cancelled + 04:57Z in_progress（schedule 触发）
- `☁️ v8 构建部署(云端ubuntu)` 04:52Z / 04:58Z × 2 success
- `☁️ v8 盘中算法追踪(轻量)` 04:41Z failure（× 1，仍红）

**危险信号**：盘中算法追踪 04:41Z failure 1 次，**与今晨 12:15 更新一致**——但 `freshness_status.json` 11:35 显示全绿（0 stale），可能是 11:35 之后的新失败未刷新入 status。

### v8_algo_cloud.yml 最近 6 runs
- 09-09 17:59Z cancelled
- 09-09 18:50Z cancelled
- 09-09 19:26Z cancelled
- 09-09 20:03Z cancelled
- 09-09 20:38Z failure ← **链路仍处于坏掉状态**
- 09-09 22:05Z failure ← 仍坏掉

**风险提示**：09-09 全天 algorithm chain 没有任何一次 success，**今晚 B/D/E 三档都依赖小九机器**，16:40 A 档同理。这是为什么"接 A 档"的紧迫感根源。

---

## 八、给主人的 1 句话请示

- **不动核心链路（守"降级"原则）**
- **加 2 道一次性自动化做 16:35 + 17:20 监控告警**
- **不主动 dispatch 新 workflow**
- 等小九明日 8 点前协商 ubuntu-latest fallback 的工程计划

---

**生成时间**：2026-09-10 13:30 CST · **生成方**：阿狸咪（家里机）
