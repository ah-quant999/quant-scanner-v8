# HANDOVER · 小九 → 阿狸咪
## 2026-09-17 14:15 · 看门狗「盘后算法链红灯分流」+ 主升板块龙头股交接文档补推

---

## 一、本次改动（主人令）

> 「处理掉吧，盘后链正好验真伪。还有什么没上线的都可以上线了。还有啥没处理好的？我没回复的？」

本批共 **3 个提交**：

| # | 提交 | 内容 |
|---|---|---|
| 1 | `9cfd099baf84` | 主升板块·龙头股（index.html + SECTOR_LEADERS.js + fetcher） |
| 2 | `4ce3379ef0ae` | A 批接入 `fetch_sector_leaders.py` |
| 3 | `fb77efac2005` | 交接文档（主升板块龙头股三件套） |
| 4 | `b549e12782d6` | 看门狗分流改造（**首版**） |
| 5 | `075c365abec2` | 看门狗分流改造（**最终版**，含 3 处边界收紧） |

---

## 二、看门狗分流改造（本次核心）

### 2.1 背景：盘后链「整夜不出」事故的真因

2026-09-16 夜里盘后算法链看似全线红灯、整夜没产出。实测查明：

```
run#1891~1920 期间，每轮 dispatch 都被「批次闸门」以「上游未就绪」**合规拒绝**，
run.conclusion 一律 = failure ⇒ 看门狗旧逻辑 `con == "failure"` 就判 is_failure=True
⇒ 每轮告警 ⇒ 真 P0 被红灯噪声淹没。
```

**权威证据（run#1898 实测）**：
```
step8  🔒 批次闸门 = failure   ← 闸门按设计拒绝
step9  🧮 运行盘后算法链 = skipped
step14 📤 唯一推送 = skipped
step17 🛡 结果问责 = failure
##[error]上游未就绪，已拒绝执行 E 批 —— ⛔ 显式 E 但前置 B 未就绪(0/9)
```

对比真成功轮（run#1907）：`step8=success / step17=success`。

### 2.2 改造内容（`v8_cloud_watchdog.py`）

**新增 2 个函数**：

- `_algo_step_conclusions(run_id)` —— 取 run 的 (闸门步结论, 问责步结论, 失败步名, **job 数**)
- `classify_algo_run(run_id, run_conclusion)` —— 返回 `(kind, why)`

**kind 取值与判定规则**：

| kind | 触发条件 | 动作 |
|---|---|---|
| `gate_reject` | 闸门 failure 且 问责 failure | ✅ 不告警（合规拒绝） |
| `ok` | 闸门 success 且 问责 success | ✅ 通过 |
| `no_job` | job 列表为空（被 concurrency 抢占） | ✅ 不告警（合规空转） |
| `cancelled` | run.conclusion ≠ failure 但步结论不完整 | ✅ 不告警（被外部取消） |
| `real_failure` | **仅当 run.conclusion == failure** 且闸门已放行而问责失败 | 🔴 **告警** |
| `unknown` | step 结论取不到（API 错误） | 🔴 保守按真故障处理 |

**`check_algo_chain()` 结论段改造**：在 `is_failure` 判定前先跑 `classify_algo_run`，对
`gate_reject` / `no_job` / `cancelled` 三个 kind **早返回 ok=True**（不触发告警/自愈），
detail 内保留判据原文以供追溯。

### 2.3 🔴 三处关键边界（实测踩出来的，务必别改回去）

**① 非 failure 结论的 run，永不判 real_failure**（run#1896 实证）
```
run#1896 con=cancelled，step8=success，step9=cancelled，step17=failure
⇒ 若只看 step 会把「被取消」误判成真故障
⇒ 硬门：con != "failure" 时直接归 cancelled
```

**② GitHub 对被跳过的步有时返回 `conclusion=None`（不是 skipped）**（run#1882 实证）
```
run#1882 con=failure，step8=success，step9/14/17 conclusion 全 = None
⇒ 若把 None 当「通过」，真 failure 被吞成 ok（**漏报真故障**）
⇒ 只把显式 "success" 当通过；None 一律视为「无结论」
⇒ con=failure 且结论不完整 ⇒ 兜底判 real_failure（宁可误报不漏报）
```

**③ 空 job 列表 = 僵尸 run**（run#1889/1890/1892 实证）
```
schedule 触发后被 concurrency 抢占 → job 从未启动，jobs 数组为空
⇒ 什么都没跑，判故障纯属误报 ⇒ 单列 no_job
```

### 2.4 为什么不 grep 日志字符串

闸门拒绝的 `::error title=` 历史上**有两套**：`v8-stage-gate-blocked`（step8 老文本）
与 `v8-stage-gate-reject`（step17 新文本）。**且 workflow 脚本源码会被日志回显**
（`echo "::error title=..."` 整行出现在日志里）⇒ 字符串匹配必然误命中。
**step 结论是 GitHub 权威字段，不受文案变更影响** ⇒ 选它作判据。

---

## 三、验证记录（全部实测，非声明）

### 3.1 离线单测 8/8

| 样本 | 场景 | 期望 | 实测 |
|---|---|---|---|
| #1898 | 闸门合规拒绝 | gate_reject / ok=True | ✅ |
| #1897 | cancelled + 闸门问责均 success | ok / ok=False | ✅ |
| #1907 | 真成功 | ok / ok=True | ✅ |
| #1902 | 闸门合规拒绝 | gate_reject / ok=True | ✅ |
| #1899 | 真成功 | ok / ok=True | ✅ |
| 构造 | 闸门放行 + 问责 failure | real_failure / ok=False | ✅ |
| 构造 | 闸门放行 + 问责 failure | real_failure / ok=False | ✅ |
| 构造 | 盘中静默闸门拒绝 | gate_reject / ok=True | ✅ |

### 3.2 全量回归 40 条真实 run — ALL PASS ✅

```
统计: cancelled=4  gate_reject=3  no_job=4  ok=28  real_failure=1
不变量① real_failure 仅在 con=failure 时出现 ....... PASS ✅
不变量② 闸门合规拒绝必 ok=True（不告警） ........... PASS ✅
```

改造前该窗口会报 **4 次误告警**（#1902/#1898/#1887 闸门拒绝 + #1882 漏报被吞）。
改造后：**3 次闸门拒绝 → 不告警；1 次真故障 → 正确告警**。

### 3.3 端到端实跑

```
check_algo_chain() →
  ok         = True
  is_failure = False
  detail     = algo 链 completed/success @ 09-17 12:53 (age 56m)
```

### 3.4 线上真身核验

```
tip = 075c365abec2685ce5f666c3b88d7b34d396c5b9
v8_cloud_watchdog.py  67575 B  md5=4c488a8e0a8f702ed2cf5dfb64b91726
CRLF=0  bare_LF=1294  → LF OK ✅
分流断言 4/4 True ✅
```

---

## 四、本批全部文件状态（线上 = 本地）

| 文件 | size | md5 | 行尾 |
|---|---|---|---|
| `v8_cloud_watchdog.py` | 67575 B | `4c488a8e0a8f702ed2cf5dfb64b91726` | LF |
| `scripts/fetch_sector_leaders.py` | 10247 B | `5bc6655f656e3e2df9e5af93717184a5` | LF |
| `data/SECTOR_LEADERS.js` | 867 B | `6fbb4f972bd6693846bb9b9e2bd97ad0` | LF |
| `algorithms/run_algorithms.py` | 90160 B | `377974dcf282b1a2844028862631be2b` | LF |

---

## 五、注意事项 / 踩坑

1. **推送撞 CI churning 是常态**：本轮首推 422 非快进（tip 在 2 秒内变了）。
   推送脚本已内置「换 tip 重推」循环（最多 6 轮），**绝不 force**。
2. **日志端点（`/actions/jobs/{id}/logs`）返回 302 到 Azure Blob**：
   带 `Authorization` 头会污染 SAS 签名 ⇒ 401。
   **正确姿势：先取 302 Location，再用无鉴权头直取**。
3. **GitHub 大响应跨境截断**：`/actions/workflows/{id}/runs?per_page=60` 会半途断
   （`FileNotFoundError` / `IncompleteRead`）⇒ 分块读 16KB + 重试。
4. **`run.conclusion` 与「真实故障」不是一回事**：改造后已分流，但**看板层**
   （`index.html` 运维面板）若仍按 conclusion 判红灯，需另行同步 —— **待确认**。
5. **step 名匹配用的是中文子串**（`批次闸门` / `盘中静默闸门` / `结果问责`）：
   若 workflow 改了 step 文案，`classify_algo_run` 会落 `unknown` ⇒ 保守告警（安全方向）。

---

## 六、遗留待办（下一步）

| # | 项 | 状态 |
|---|---|---|
| ① | 看板层（`index.html` 运维面板）同步分流口径 | 待确认是否也需要 |
| ② | `MACRO_DATA.js` 裸 NaN + 无 `fed_decision` | 等 17:20 盘后档核验 |
| ③ | `#1897` `repository_dispatch` 派发源未查明 | 待查 |
| ④ | `SCRIPT_ENV` 按脚本名匹配问题 | 待主人确认 |
| ⑤ | P-2 + P-6 落盘（`v8_build_deploy.yml`） | 待办 |
| ⑥ | 重跑 `gen_backtest_all_algos.py`（勿双写） | 待办 |
| ⑦ | 「强势跟踪」并入 `BACKTEST_ALL_ALGOS` | 待办 |
| ⑧ | P-4/P-5 影响面评估（76 条桥接） | 待办 |
| ⑨ | `SH_FIB` 显示名仍为「市场温度计」 | 待办 |
| ⑩ | `a_impact` 缺口：议息条目走「中性」档 | 待办 |

---

*小九 · 2026-09-17 14:15*
