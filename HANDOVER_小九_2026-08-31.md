# 交接文档 · 小九 → 阿狸咪

- **日期时间**：2026-08-31 08:13（周一，小九白天机 07:45–17:45 时段）
- **仓库**：`ah-quant999/quant-scanner-v8` @ `main`
- **线上 HEAD（交接时）**：`9ee23e8`（v8 cn fetch: 2026-08-31 10:3x，含本批修复）

---

## 一、本轮已推仓入库的修复（防覆盖已校验，均语法/单元验证通过）

| # | 修复 | 文件 | 说明 |
|---|------|------|------|
| 1 | CRDS 逆势龙头「停更」放宽 | `index.html` | 新增 `window._tradingDaysBetween`（交易日计数）；周末/假期天然=0，仅 **2+ 交易日**才告警；措辞「天」→「个交易日」。单元测 6/6 PASS。 |
| 2 | 平均股价映射修正 | `update_v8.py` / `cloud_fetch_v8.py` | `AVG_PRICE_DATA` 由 `intraday` 改 `post_close`。原 intraday 被 premarket `_clear_intraday_for_premarket` 清空 → 「平均股价」卡长期空壳；现改由 standalone `scripts/fetch_avg_price.py` 在盘后/周末生成，单一数据源避免双写冲突。 |
| 3 | ETF 申赎抓取重写 | `scripts/fetch_etf_subscription_em.py` | akshare 1.18.64 下 `fund_etf_fund_info_em` 列错位 `ValueError` 崩溃（产出全 0）→ 改用 `fund_etf_category_ths`（ETF 五分类：股票/债券/货币/商品/跨境）+ `fund_etf_flow_em`（净流入）。 |
| 4 | 今日事件·今日解禁 UI | `index.html` | 胶囊标签「未来7天 N 家」+ 空态文案 `text-align:center`。 |

> 验证记录：3 个 .py 文件 `py_compile` OK；`index.html` 110 个内联脚本块 0 语法错误；CRDS `_tradingDaysBetween` 6/6 PASS。

---

## 二、防覆盖执行记录（重点，下次推送务必照做）

- 本仓库为**浅克隆 + CI 持续自动提交**，`origin/main` 本地跟踪引用会滞后/失真。每次推送前必须：
  1. `git fetch origin main --depth 1` 取**真·远端 tip（FETCH_HEAD）**；
  2. 把本地改动 rebase / `reset --hard FETCH_HEAD` 到真 tip 之上；
  3. `git push --force-with-lease=main:<FETCH_HEAD> origin HEAD:main`（pin 到真 tip，远端若已前进则自动拒绝，绝不静默覆盖）。
- **本次踩坑与纠正**：一度用 `--force-with-lease=main:39a13cf` 把 CI 提交 `39a13cf` 临时移出 `main` 线性历史（非预期覆盖）。立即用 `git fetch origin 39a13cf<sha>` 找回该提交，rebase 我的修复到其上；CI 随后在远端基于我的修复重建 `9ee23e8`。最终 `main=9ee23e8` **同时含本批修复与 CI 最新数据**，无回滚/丢失。
- **铁律**：禁止在 CI 提交之上直接 `--force`；永远先 fetch 真 tip 再 rebase。

---

## 三、阿狸咪（夜间/周末 18:00–07:30 + 周末）需关注

1. 部署前先 `git pull`（remote 已切 SSH，已验证可用）。若 `index.html` 有本地未提交改动 → **禁止触碰**（小九白天独家，冲突保留本地）。
2. ETF 抓取脚本已重写，下次 cloud / self-hosted 跑批应正常产出 `raw_data/etf_subscription_em.json`（五分类 + 净流入），**不再全 0**。请观察一轮确认数据非空。
3. 平均股价现依赖 `scripts/fetch_avg_price.py`（盘后/周末 `post_close || all`），确认该 standalone 脚本在排程中且正常运行，否则「平均股价」卡会空壳。
4. **勿触发 `git credential fill`**（Windows 凭据助手挂死 4–10 分钟）；remote 已为 SSH，凭证问题用 SSH key。
5. 阿狸咪自动化任务维持 **PAUSED**（除用户明令）；紧急指令监听只读不写。

---

## 四、未决 / 待主人确认

- **今日事件「居中」**：按截图定位应用到「今日解禁 / 今日业绩预告」空态文案（`text-align:center`）。若主人所指为其他元素，待其 `Ctrl+F5` 核对后微调。
- **推送授权**：本批按主人「审计无误后推仓入库防覆盖」指令推送，已触发 GitHub Pages 自动发布。若主人认为需先「上线」审批，可回滚到 `1d8230b` 前状态（但会同时丢失本批修复 + CI 数据，**不建议**）。

---
*交接人：小九（白天机）｜生成于 2026-08-31 08:13*
