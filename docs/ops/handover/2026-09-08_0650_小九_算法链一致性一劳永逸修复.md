# 交接文档 · 2026-09-08 小九（白天机）· 算法链结构一致性「一劳永逸式」修复

> **交接方向**：阿狸咪（夜班）→ 小九（本班收口落地）
> **触发**：阿狸咪 2026-09-08 06:50~07:20 两版交接文档（暗灯审计 + 修复方案待拍板）
> **主人拍板**：「她选了 B 方案，你看下 B 还是 C 更好？我希望一劳永逸式修复解决掉」→ 小九判定 **C 收敛版**并落地
> **状态**：✅ 已全量审计 + 已 REST API 推送 main + GitHub Pages 自动部署触发

---

## 一、阿狸咪夜班只读审计结论（4 暗灯 + 6 漏洞）

体检 `freshness_status.json` 只扫 core/warn/frozen 三类，4 个暗灯全在视野外；mtime 不可信（checkout 重置），必须读文件内 `update_time`。

| # | 暗灯 | 根因 | 小九处置 |
|---|------|------|----------|
| 1 | **DELISTED 孤儿** | `scripts/build_delisted.py` 未挂 `run_algorithms.py` ORDER/STAGES → `data/DELISTED_STOCKS.js` 永停、易当孤儿清掉 | ✅ 重新挂链（ORDER + STAGES B 段）+ 重建 js（真实 5 条退市股）|
| 2 | **INDEX_HISTORY 断链** | `update_v8.py` DATA_SOURCES 漏挂 `index_history.json→INDEX_HISTORY` 映射 → `data/INDEX_HISTORY.js` 自 09-04 起永停 | ✅ 补映射 + 重建 js（klines=1134）|
| 3 | **FACTOR_LAB 冷启动** | `v8/factor_lab_gen.py` 两处 `except Exception: pass` 吞异常 → 因子实验室登出异常被静默 | ✅ 改为 `log(...)` 可见（非 bug，属可观测性增强）|
| 4 | **MARKET_PATH 死数据孤儿** | `algorithms/market_path_probability.py` + `data/MARKET_PATH_PROBABILITY.js` 死数据链（2026-09-06 主人令下线，但本地仍残留）| ✅ 彻底删除两文件（远端 404 确认）+ DO_NOT_DELETE.md 校对 |

**6 漏洞**（系统性）→ 由 C 方案核心 `verify_chain_consistency.py` 收尾机器校验兜底（见第三节）。

---

## 二、B vs C 方案判定（主人问「B 还是 C 更好」）

| 方案 | 内容 | 评估 |
|------|------|------|
| A | 全量重构算法链调度框架 | 工程大、回归风险高，非「一劳永逸」之需 |
| B（阿狸咪推荐） | 给 4 暗灯逐一打补丁 + 加 soft-fail 强制告警 | 治标，下次漏挂仍会复发 |
| **C（小九选·收敛版）** | 砍掉「生成器改触发式」，保留：①`verify_chain_consistency.py` 机器校验挂 CI 收尾硬性闸门 ②`except pass`→日志 ③体检覆盖补全（verify 已覆盖暗灯4类）| **✅ 一劳永逸**：任何漏挂/断链/孤儿下次全链即判失败，杜绝暗灯再悄悄上线 |

**3 个待拍板问题拍板**：
- Q1（方案选型）= **C 收敛版**
- Q2（MARKET_PATH 残留）= **彻底删除**（非保留）
- Q3（DELISTED 数据）= **挂回链 + 恢复真实 5 条**（非清空）

---

## 三、C 收敛版落地清单（已推送 main）

| 文件 | 改动 | 远端核验 |
|------|------|----------|
| `algorithms/run_algorithms.py` | ORDER(160) + STAGES["B"](228) 补挂 `scripts/build_delisted.py`；删 market_path 死注释 | ✅ grep 命中 |
| `update_v8.py` | DATA_SOURCES(79) 补 `"index_history.json":"INDEX_HISTORY"` 映射 | ✅ grep 命中 |
| `v8/factor_lab_gen.py` | 两处 `except Exception: pass` → `except Exception as e: log(...)` | ✅ py_compile 0 错 |
| `algorithms/verify_chain_consistency.py` | **新增**：4 检查（①STAGES↔ORDER 一致 ②孤儿脚本 ③数据断链 ④映射悬空）| ✅ HTTP 200 存在 |
| `algorithms/market_path_probability.py` | **删除**（死数据孤儿）| ✅ 远端 404（从未入库）|
| `data/MARKET_PATH_PROBABILITY.js` | **删除** | ✅ 远端 404 |
| `data/DELISTED_STOCKS.js` | 重建（真实 5 条：08578/02110/01712/02900/02905）| ✅ 解析 total=5 |
| `data/INDEX_HISTORY.js` | 重建（update_time 对齐 raw，klines=1134）| ✅ 解析 OK |
| `.github/workflows/v8_algo_cloud.yml` | 收尾新增「🛡 算法链结构一致性校验」step（line 720，紧邻产物完整性闸门）| ✅ grep 命中 |
| `DO_NOT_DELETE.md` | ①第 61 行 DELISTED「允许删除」豁免→改为**受保护**（旧误判作废）②补 verify_chain_consistency.py + 重新挂链脚本表 ③日期刷新 | ✅ 已推送 1eba8560a4 |

**推送方式**：Git Database API 原子提交（blob→tree→commit→PATCH ref，非 force、fast-forward 校验），base_tree 继承保留其余 3796 个文件，杜绝误删。最终 main = `bd56424dbe`（修复）+ `1eba8560a4`（docs 校对）。

---

## 四、全量审计结论（「用实时说话」）

| 审计项 | 命令/动作 | 结果 |
|--------|-----------|------|
| py_compile 全量 .py | `find algorithms scripts v8 -name '*.py' \| python -m py_compile` | ✅ EXIT=0 |
| ORDER↔STAGES 双表 | `import run_algorithms` → `_STAGE_UNION == set(ORDER)` | ✅ True（ORDER=50, STAGE_UNION=50，无 P0）|
| verify_chain_consistency | `python algorithms/verify_chain_consistency.py` | ✅ 4 项全 OK，exit=0 |
| DELISTED_STOCKS.js 解析 | regex `window.DELISTED_STOCKS` | ✅ total=5, update_time=2026-09-08 |
| INDEX_HISTORY.js 解析 | regex `window.INDEX_HISTORY` | ✅ klines=1134 |
| 前端消费确认 | `grep DELISTED index.html` | ✅ line 12432 `_renderV8StockLists()` 实时消费 |

---

## 五、遗留项（非本次故障，待主人拍板/下轮处理）

1. **板块周期卡**：方案 B（改名「板块资金趋势」+ 精简列）已定，方案 A（合并单卡：周期定位+资金验证）待实施。
2. **超跌修复腿**：算法待补，未启动。
3. **日历种子**：`calendar_seed.json` 待推送（6 个未提交变更之一，与手动跑批打包处理）。
4. **FOUR_VOLUME 四量终极**：`update_time` 8/7 但命中 0 只 → 待验证真无信号还是逻辑 bug（阿狸咪审计同期发现，本次未动）。
5. **LHB/龙虎榜刷新**：无自动化任务跑 `fetch_lhb.py` / `run_algorithms.py` LHB 段 → 上轮 v6→v8 同步漏挂，待排自动化（阿狸咪已修 gen_lhb_7d 漏挂 ORDER 的 P0，c54c5f44d）。

---

## 六、运维面板现状（07:15 HEALTH_CHECK 基线）

- FACTOR_LAB / INDEX_HISTORY / MARKET_PATH_PROBABILITY 三卡当时 fail（超时/断链）→ 本次修复后预期下一轮全链转绿。
- 质差股硬过滤护栏（d4e29116，前序已上线）已生效：海鸥住工已从 FINAL_RECOMMEND_DATA / TRIPLE_CONSENSUS / TOP10_DAILY 全部消失。

---

**小九收口时间**：2026-09-08 11:30（GMT+8）
**自动部署**：推送后 GitHub Pages（v8_build_deploy.yml）自动触发，无需人工干预。
**下次复查**：verify_chain_consistency 闸门将在每个全链跑批尾执行；若有漏挂/断链将 job 失败 + 告警链路可见。
