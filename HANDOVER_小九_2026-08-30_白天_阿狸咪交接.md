# HANDOVER 小九 · 2026-08-30 白天（阿狸咪交接 · 续 HANDOVER_小九_2026-08-30.md）

> ## ⚠️ 第一条也是最重要的一条：当前 `main` 即权威版本，**禁止回滚 / 禁止覆盖**！
> - 这两天（8/29–8/30）我（阿狸咪）的所有改动已**全部 commit + push 到 `main`**，当前权威 HEAD = **`84f1288bf`**（含云端 build 更新的 STOCK_QUOTE.js）。
> - 本地 `git reset --hard`、云端 build 的 `git reset --hard FETCH_HEAD` 都会**保留**我的 `index.html`——因为我的版本已在 main 上，FETCH_HEAD 就是我。
> - **切勿** `git checkout <旧commit> -- index.html`、勿 `git revert` 这些提交、勿 `git push --force` 旧版本，否则会把已修好的逻辑退回旧版（时间标签防未来 / 暂未上架3卡 / 死代码清理全没了）。
> - 若发现任何"卡片空白/被覆盖"迹象，**先看本文件 HEAD，不要回滚**，先排查数据源。

---

## 一、这两天（8/29–8/30）我改了什么

按主人令「一劳永逸式修复 + 全面审计 + 清理死代码」，分 4 批落地：

### 批次 A — 前端展示层 + 算法根因（8/29 深夜–8/30 凌晨）
| 提交 | 内容 |
|---|---|
| `5e3015b38` | 一劳永逸全修（前端展示层 + 算法根因） |
| `c7e37b97c` | 13 项一劳永逸（mega patch） |
| `886d97a93` | #106–#109：①**时间标签防未来显示**（`_fmtAshareRel` 加护栏 + `scripts/momentum_common_filter.py` 改用 `v8_date.today_data_date()` 真实交易日，根治"周日上午显示今日16:29"）②**暂未上架·强势突破只留3卡**，其余归调试专区（修了提前闭合 `</div>` 导致两子页雷同的 root cause）③斐波那契/商品弹性三卡移至已下架·盘前条幅下方 ④**删驾驶舱双灯**（无回测证据） |
| `95a33aea0` | #110 11 项 LOW 打磨：跨卡时点标注 / 估值→估值(回调代理) / Fib下一窗口含active / 资金流MA5,20 / 涨停"其他"桶沉底 / H反推快照日期 / 四量非故障说明 / FOMC枚举 / 申赎时点 / 板块Block3源标注 |

### 批次 B — 死代码清理 + 健壮性（8/30 上午）
| 提交 | 内容 |
|---|---|
| `8114dab04` | 删实时数据区 **~430 行死代码**（旧 LHB 月历 `renderMonthCal` / 历史条 `renderHistoryBar` / `_showLhbDay` / 四张分析卡 / 旧大牛股猎手脚本——其 DOM 容器已不存在，由 sec-rc 新日历接管）；AI速览缺数据不再崩；平均股价去不可达 return；删 `_jyCalNav` 重复定义 |
| `b40414121` | `v8_health_check.py`：注册新卡(DELISTED/UNLISTED_PANEL/AVG_PRICE_DATA) + `check_*` 返回 None 时 `or []` 兜底防崩 |

### 已对齐 / 无遗留
- 本地已 `rebase` 到 origin（HEAD `84f1288bf`，**领先/落后 = 0/0**），工作树干净，无悬空改动。
- 历史提到的 6 个待推文件（calendar.html 等）早已入库，无遗留。
- STOCK_QUOTE 已由云端 workflow 自动更新到 `2026-08-30 10:31:12`，无需本地再处理。

---

## 二、✅ 已触发并完成 STOCK_QUOTE 刷新（原“明天唯一风险”已解除）

- **阿狸咪已手动触发**：`v8_stock_quote_refresh.yml`（workflow_dispatch），run #39，传 `skip_trading_day_gate=true`（周日非交易日强制跑）。
- **结果**：completed **success**，`data/STOCK_QUOTE.js` 已更新到 **`2026-08-30 10:31:12`** ✅
  - 个股查询/实时数据明天开盘不会再显示 8/24 陈旧快照。
- **小九可复用的命令**（白天若发现 STOCK_QUOTE 停滞，应急手动触发）：
  ```bash
  cd /e/workspace/stock-scanner
  TOKEN=$(cat ~/.workbuddy/v8_gh_token.txt)
  curl -X POST -H "Authorization: token $TOKEN" -H "Accept: application/vnd.github.v3+json" \
    https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_stock_quote_refresh.yml/dispatches \
    -d '{"ref":"main","inputs":{"skip_trading_day_gate":"false"}}'
  ```
  - 交易日传 `false`，非交易日应急传 `true`。
  - 触发后用以下命令查最新 run：
  ```bash
  curl -sS -H "Authorization: token $TOKEN" -H "Accept: application/vnd.github.v3+json" \
    "https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/runs?workflow_id=v8_stock_quote_refresh.yml&per_page=3"
  ```
- 其余数据源均新鲜（已核实）：
  - `LHB_HISTORY`：7/1–8/28，共 58 个交易日 ✅
  - `LHB_DATA`：`2026-08-29 08:43:06` ✅
  - `V8_CAL`：`2026-08-30 09:46:56` ✅（今日事件日历已刷新）

---

## 三、双机铁律提醒（给小九）

1. 你是**白天独家（7:45–17:45）**；阿狸咪夜间/周末。分工互不踩踏。
2. 你的 CI build（`v8_build_deploy.yml`）会 `cp index.html /tmp → reset --hard → cp 回来`，**手动 UI 改动安全**，不会冲掉我的 index.html。
3. **但**：不要主动 `git revert` / checkout 我的提交。我的改动是主人的"一劳永逸"成果，回滚会复发旧 bug。
4. 另一 clone `E:/workspace/workspace/quant-scanner-v8` **禁止改动**（铁律）。
5. `git add -A` **禁用**（坚果云秒删磁盘文件 + 误删 data/）；一律显式 `git add <具体文件>`。

---

## 四、小九 todo（开盘前/开盘后）

- [x] **阿狸咪已执行**：手动 dispatch `v8_stock_quote_refresh.yml`，`STOCK_QUOTE.js` 已刷新到 **2026-08-30 10:31:12**。
- [ ] **开盘前**：确认 `data/STOCK_QUOTE.js` 仍新鲜（检查 `update_time` 是否为最近交易日），若陈旧再用上面的 curl 应急触发一次。
- [ ] **开盘后**：核对「今日事件」「共振日历」两页渲染正常、浏览器 console 0 错误。
- [ ] 若见任何"空白/被覆盖"，先查本文件 HEAD（`84f1288bf`），**勿回滚**。

---

## 五、本批次涉及的主要文件（供核对，勿 revert）

- `index.html`（多段脚本：时间护栏、暂未上架3卡、死代码清理、估值relabel、Fib/资金流/涨停/四量/FOMC/申赎/Block3 标注）
- `scripts/momentum_common_filter.py`（真实交易日逻辑）
- `v8_health_check.py`（防御性兜底）
- 数据文件由云端 workflow 自动重算，无需手动处理（我的快照版仅占位）
