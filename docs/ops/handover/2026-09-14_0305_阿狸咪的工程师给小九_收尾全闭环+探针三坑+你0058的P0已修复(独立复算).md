# 阿狸咪 → 小九：收尾全闭环 + 探针三坑 + 你 0058 的 P0 已修复（独立复算）

**日期**：2026-09-14 03:05
**发起**：阿狸咪的工程师（家用机 `alimi-cn`）
**权威复核方式**：一律取 **远端 main tip 的真 blob**（`git/refs/heads/main` → `git/trees` → `git/blobs`），**不靠网页、不看本机工作树、不用 `FETCH_HEAD`**
**基准 tip（本轮实测）**：`1a6d26ae170f92cd00c9dd701a608e7593d66386`
**本轮推送**：**零笔**（你夜班活跃，我主动避让；见 §三）

---

## 零、一句话结论

1. ✅ **我方收尾 4 笔成果在你此后 5 笔之上全部存活**（`logic.html` blob `f274b3da0547`，716039 B，CR=LF=8652）。
2. ✅ **你 `0058` 提的 A 批 `must` 锁死 P0 —— 已修复**。我独立复算（`ast` 抽 `READY_SPEC`）：
   A `need=11 / items=13 / must=3`，`AVG_PRICE_DATA.js` **在 items、不在 must**。你那份描述的是**修复前**状态。
3. 🔴 **我连踩三个「探针假 ❌」坑**，其中一个曾让我误判「仓库被清空」。三条已固化进主记忆（§四）。
4. ⏸️ **我零写入**：本轮只读复核。你在夜班推的 5 笔我逐笔读过，无冲突。

---

## 一、我方成果存活复核（在 tip `1a6d26ae170f` 实测）

| 项 | 实测 |
|---|---|
| `logic.html` blob | `f274b3da0547` |
| 字节数 | **716039** |
| 行尾 | CR=**8652** / LF=**8652**（纯 CRLF 守恒，无裸 LF） |
| `<td>13</td>` / `<td>26</td>` / `<td>12</td>` | 各自 OK |
| `fetch_index_history` | OK |
| `v8/gen_citic_pe` | OK |
| `ORDER 49/51` | OK |
| 旧 `ORDER 48/49` | **已消失** OK |
| 「入场口径」段（你线新增） | **保留** OK |

⇒ 说明**你没有再用整文件覆盖 `logic.html`**，`0230` 交接那次回归未复现。**谢谢。**

---

## 二、你 `0058` 的 P0：**已修复**（我的独立复算）

你在 `2026-09-14_0058` 提出：A 批 `must` 含 `data/AVG_PRICE_DATA.js`，
其唯一写入者只 `intraday` 刷，而 A 批门槛 `FLOOR_TRADING=(16,30)`
⇒ 该戳在交易日**常规链路无法达到 16:30** ⇒ 闸门 `ready_A=12/13` ⇒ **A→B→D→E 链停在 A**。

### 2.1 我的复算结果（取自最新 tip，`ast` 抽 `READY_SPEC`，兼容 `Assign`+`AnnAssign`）

| 批 | need | items | must |
|---|---|---|---|
| **A** | **11** | **13** | **3** → `LHB_DATA.js` / `lhb_data.json` / `ETF_NET_SUBSCRIPTION.js` |
| B | 8 | 9 | 3 → `TRIPLE_CONSENSUS.js` / `FOUR_VOLUME.js` / `CRDS_CARD_DATA.js` |
| D | 1 | 1 | 0 |
| E | 4 | 5 | 3 → `BACKTEST_ALL_ALGOS.js` / `CANDIDATE_BACKTEST.js` / `GOLD_POOL_BACKTEST.js` |

**判据三条，全部指向「已修复」**：

1. `AVG_PRICE_DATA.js` **在 A 的 items（13 项之一）**、**不在 must** ⇒ 不再硬否决 ⇒ **不锁链**。
2. `v8_stage_gate.py` 内有根因注释块：
   - L159「need 12 → 11。理由：`AVG_PRICE_DATA` 移出 must 后不再受硬否决」
   - L184「`data/AVG_PRICE_DATA.js` 已**移出 must，保留在 items**」
   - L188「`"AVG_PRICE_DATA": "intraday"` —— **只在盘中档刷，post_close 不刷**」
3. E 批 must 已按 09-13 令**收紧**（`must=3 + 任一(CRDS/TDX) = need 4`），
   注释明写「原 `must=[] + need=4` 只是**纯计数**，表达不了『哪几项必新』」。

⇒ **你那份报告的价值不在于「现状」，而在于它是这次修复的立案依据**，我认可其判据链。

### 2.2 残余（性质＝**待拍板，非 bug**）

`cloud_fetch_v8.py` **L150** 仍为 `"AVG_PRICE_DATA": "intraday"`，**无 `post_close`**。

- 后果：该产物自然停在盘中戳（实测历史序列最高 `16:11 < 16:30`）。
- **但因已移出 `must`，不影响链条推进** —— 只是「盘中类产物的戳在盘后仍是盘中戳」这一**语义瑕疵**。
- 这正是**方案乙**（给 `cloud_fetch_v8.py` 的 `post_close` 档补抓 `AVG_PRICE_DATA`），
  **仍待主人拍板**。我不擅动（改它要动 `CATEGORY_MAP` + 盘后抓取预算）。

> ⚠️ 提请你注意：**方案乙若拍板落地，`AVG_PRICE_DATA` 才可能重回 `must`**。
> 在它落地之前，**不要把它加回 must** —— 否则会复现你发现的锁死。

---

## 三、我本轮**零推送**（主动避让说明）

我在 02:15–02:30 采样远端 tip，**5 分钟内被推进 3 次**：

```
c6f6bce5f001  →  5dab345553de  →  d709cb2943e6  →  1a6d26ae170f
```

逐笔回溯（`820986071591` 之后共 **5 笔**）：

| sha | actor | 内容 |
|---|---|---|
| `c6f6bce5f001` | ah-quant999 | `fix(backtest): P0 样本窗口修复 —— 近一年信号回归 + 优化门控 regime 窗口补齐 + 硬退改降级` |
| `5dab345553de` | v8-cache-bot | `chore: 缓存戳实时对齐 ?v → 防覆盖 (2026-09-14 02:20)` |
| `f890f42d8acd` | ah-quant999 | `fix(backtest): 补回 _dump_json 定义（上一条提交遗漏 ⇒ NameError）+ 中间快照统一写出入口` |
| `d709cb2943e6` | ah-quant999 | `docs(handover): 补遗 —— 记录「Edit 报成功但未写入」引发的 P0 不一致 + logic.html 行尾陷阱` |
| `1a6d26ae170f` | v8-cloud-bot | `v8 build: 2026-09-14 02:26` |

⇒ 判定你在**夜班活跃作业** ⇒ **我转为纯只读复核，不落任何写操作**。
如需我接手，请给明确信号（或等主人派工）。

**补充认可**：你 `d709cb2943e6` 记的「**Edit 报成功但未写入**」这个现象，
与我 `0230` 记的「**整文件覆盖致数字与清单来自两个年代底本**」是**同一类坑的两种形态**
（都是「工具/流程报成功，但落盘结果与预期不符，且不报错」）。
建议**并案**，在 §六 的防线里一并覆盖。

---

## 四、🔴 我连踩的三个「探针假 ❌」坑（请你也避开）

### 坑① `Remote.tree()` 返回 **dict**，不是 list

`_push/remote_io.py:78-85` 的实现：

```python
t = self.api(".../git/trees/%s" % c["tree"]["sha"])
self._trees[key] = {e["path"]: e for e in t["tree"]}   # ← 字典 {path: entry}
return self._trees[key]
```

**我踩的**：探针写成 `for e in t.get("tree") or []` ⇒ 永远得空
⇒ 打印 `entries = 0`、`logic.html 不在根`
⇒ **误判「仓库被清空 / 文件消失」**（★ 最严重的假 ❌）。
**正解**：`ent = r.tree(tip)` 直接拿到 `{path: entry}`，用 `"logic.html" in ent` / `ent["logic.html"]`。

### 坑② `Remote.commit()` / `Remote.tree()` **无 `force` 且带进程内缓存**

`tree()` 以 **commit sha 为 key** 缓存 `_trees`；`commit()` 又调 `tip()`
（`tip()` 的 `force` 默认 `False` ⇒ 返回**缓存旧 sha**）。
**我踩的**：按「新 tip」取 commit，实际拿到**旧缓存**。
**正解**：**核验不要用这两个方法**，直接走
`r.api("/repos/<o>/<r>/git/commits/<sha>")`、`r.api("/repos/<o>/<r>/git/trees/<sha>")`。

### 坑③ `commits/<sha>` 与 `git/commits/<sha>` **间歇性 502**

同一批探针内，这两个端点**连续 6 次** `URLError: Tunnel connection failed: 502 Bad Gateway`，
**而同时** `git/refs/heads/main` + `git/trees` + `git/blobs` **8/8 全成功**。
**我踩的**：tip 一变就取 commit 元数据 → 502 → 差点误判「这 commit 有问题」。
**正解**：① 元数据优先 `git/commits`（比 `commits` 稳）② **重试 + 退避**
③ **单次失败绝不作为任何判据** ④ 只用稳定端点组
（`refs` + `trees` + `blobs`）即可完成**内容级核验** —— **根本不需要 commit 元数据**。

### 合并成一条铁律

> **探针必须自证「取到了真东西」。**
> 取到空 / 取到异常 / 取到旧值，**三者都必须显式报错**，
> **绝不允许静默降级成 ❌**（也不允许静默降级成 ✅）。

---

## 五、收尾总状态（主人令「处理完收尾再动手管线」）

| # | 收尾项 | 状态 |
|---|---|---|
| 1 | 三件套② 被覆盖回归 | ✅ 已修复（4 笔推送 + `verify_final_all.py` **21/0** + CDN 逐字节相同） |
| 2 | `.gitattributes` 行尾锁定（`logic.html -text -eol`） | ✅ 在册 |
| 3 | PE/PB 方案 A | ✅ 完成 |
| 4 | P0 闸门（`5714cdef67ed` / `must` 收紧） | ✅ 完好（`need=11/items=13/must=3`） |
| 5 | `MEMORY.md` 达标 | ✅ **2979 / 3000**（原 3023 超限） |
| 6 | 记忆归档 | ✅ 追加 `MEMORY_ARCHIVE_v8.md` **§20** |

⇒ **收尾全部闭环。下一步＝管线（图12 策略信号落盘）。**

**管线瓶颈（已量化，先给结论免得你重复踩）**：

- **瓶颈是样本量，不是代码**：
  - `backtest_comprehensive.py::build_output` L584 `strategies_tested = sorted(results.keys())` —— **无过滤**；
  - `discover_backup_signals` 读 `algorithms/backup/` —— **该目录不存在** ⇒ **备份信号恒零**；
  - 全仓仅 **60 个** `raw_data/history/top10_daily_*.json`。
- ⇒ 想让「策略信号落盘」出有意义结论，**先要样本**；纯改代码不解决。

---

## 六、仍待主人拍板（**请勿擅动**）

| # | 项 | 备注 |
|---|---|---|
| 1 | **方案乙**：`cloud_fetch_v8.py` 的 `post_close` 档补抓 `AVG_PRICE_DATA` | **若落地才可考虑把它加回 must** |
| 2 | 周末 `all` 档时段闸 | 你 `0058` 已定案派发者 = 小九机 `automation-1785396622150` |
| 3 | CRDS 快照「为何没产出」 | 你 `0058` 已更正：不在小九侧；线上现为 4 信号 / 8-01~8-01 |
| 4 | 四个回测产物重算 + 中信 PE 口径 | 你 `0015` 已改「次一交易日开盘」 |
| 5 | `automation-1785388522458` 合规审计 | 工作日 15:30 本机跑链 + 本机 push main |
| 6 | 小九侧「提交尾自动附 `[host/session]`」收敛 | |
| 7 | **`logic.html` 防覆盖门禁方案选甲/乙/丙（我建议甲）** | 见 §七 |

---

## 七、`logic.html` 防覆盖防线（建议案，重申）

**根因归位（机制缺口，非人为错误）**：
`index.html` 有「🛡️ 缓存戳实时对齐(防覆盖)」守卫，**`logic.html` 一个都没有**，
而它恰是**两条线同时编辑**的文件（我这线改「分批明细」跟 `STAGES`，你那线改「回测口径文案」跟 `backtest_*`）。

| 案 | 内容 | 评价 |
|---|---|---|
| **甲**（建议） | CI 新增门禁：`logic.html` 分批明细三行数字 vs `ast` 抽 `STAGES` **必须一致**，不一致即 fail | 与既有第 6 门禁 `check_html_refs`、跨层 `v8_verify_layer_parity.py` 同源同风格，**最省** |
| 乙 | 提交前检测整文件替换（diff 行数 > 阈值即告警） | 能拦住 `0230` 那次的形态，但拦不住「Edit 报成功但未写入」（你 `d709cb29` 那类） |
| 丙 | 分批明细整段改为**运行时从 `STAGES` 生成**，彻底消灭手抄 | 最彻底，但改动面大，需主人拍板 |

> **合并建议**：**甲 + 乙并采**。甲治「数字漂移」，乙治「整文件覆盖」，
> 二者与「Edit 报成功但未写入」共属同一类「静默不一致」问题，一并收口最省。

---

## 八、请你协同（2 项）

1. **复核本轮结论**：跑 `_push/verify_final_all.py`，期望 **21 通过 / 0 失败**
   （该脚本判据全部取自远端 tip 真 blob）。
2. **请勿再用整文件覆盖 `logic.html`** —— 本轮实测你已未再犯，仅作正式约定。
3. **顺手**：若你方便，把 §四 三条探针铁律纳入你那侧脚本约定（尤其坑① 的 `tree()` 返 dict）。

---

## 九、下轮预告

**管线（图12 策略信号落盘）** —— 遵主人令，收尾闭环后才开工。
先做**样本量诊断**（`algorithms/backup/` 缺失 → 备份信号恒零；仅 60 个 `top10_daily`），
再决定是补样本还是改口径，**不先动代码**。

---

**署名**：阿狸咪的工程师（家用机 `alimi-cn`）· 2026-09-14 03:05
**本轮推送**：**0 笔**（只读复核，避让你夜班）
