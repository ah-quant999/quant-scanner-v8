# HANDOVER｜小九的股票专家 → 阿狸咪｜2026-09-11 23:28｜本机工作树尾巴统一移交_双页待贴全文与物理边界

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 上一份（主单）：`2026-09-11_2316_小九的股票专家给阿狸咪_今日全量移交_小九机链推不出根因分层tree枚举_60m双页未上线_6条待拍板.md`
> **本单是它的收口补充，只增加「本机工作树尾巴统一移交」一节，其余以主单为准。**

---

## 一句话结论

主人 23:27 令「**统一阿狸咪做**」：小九机工作树的一切遗留（双页待贴、陈旧暂存区、落后 74 提交、根目录未跟踪垃圾）**全部并入阿狸咪的待办**，小九侧**不做任何提交 / 不 ff / 不 reset / 不清暂存区**。
**但有一条物理边界必须先对齐：双机文件系统不互通 —— 阿狸咪无法操作小九机的 `E:/qs_workspaces/quant-scanner-v8`。** 因此本单把这项拆成「她侧可直接执行的动作」与「只能在小九机侧执行、现阶段按令冻结的动作」两栏，见 §三。

**小九侧从此刻起只做一件事：旁监督本机是否被自然对齐（不主动动手）。**

---

## 一、主人令原文（可追溯）

> 「统一阿狸咪做」
> —— 2026-09-11 23:27，回答小九上一轮的提问：「要不要我顺手 ff 对齐 + 清暂存区？还是把工作树整体也让给阿狸咪统一收拾？」

**小九的理解（若理解有偏，请阿狸咪直接指出）**：不是「工作树交给她远程操作」（做不到），而是「**这条尾巴不要再生出新的待拍板事项，统一记入她的任务池，由她决定何时以何种方式收口**」。

---

## 二、小九机现场快照（2026-09-11 23:28，实测非推断）

| 项 | 实测值 |
|---|---|
| 本机时间 | `2026-09-11 23:28:24` |
| 本地 HEAD | `02e2eb78a chore: 缓存戳实时对齐 ?v → 防覆盖 (2026-09-11 20:50)` |
| 远端 main tip | `1313d4e3b6cbed7b5f15cf0178c86610af4e02e5` |
| 领先 / 落后 | **0 ahead / 74 behind**（纯落后，无分叉 → 无内容冲突，只有「要不要带上去」的问题） |
| 我上轮推送的交接单 | `6479e01f3` 已在链上（更早已上线 `2b8cc93c2` 代码 / `7f471b003` 交接单补全） |

### 工作树未提交（`git status --porcelain` 原样，未加工）

```
M  algorithms/run_algorithms.py            ← 首列 M = 已暂存（陈旧暂存区地雷）
M  algorithms/strategy_four_volume_60m.py  ← 同上
M  v8_health_check.py                      ← 同上
 M index.html                              ← 次列 M = 未暂存（双页稿之一）
 M logic.html                              ← 同上（双页稿之二）
 M raw_data/runner_status.json             ← 运行时产物，勿提交
 M self_heal_monitor.py                    ← 另一并发会话的 WIP
?? AUDIT_小九_2026-09-10_1900_每日19点审计.md
?? AUDIT_小九_2026-09-11_1900_每日19点审计.md
?? HANDOVER_2026-09-10_午休修复.md
?? HANDOVER_小九_2026-09-10_1500-1800空窗期.md
?? HANDOVER_小九_2026-09-10_双机分工.md
?? HANDOVER_小九给阿狸咪_2026-09-10_主力净额双管齐下.md
?? HANDOVER_小九给阿狸咪_2026-09-10_盘前卡上线.md
?? HANDOVER_小九给阿狸咪_2026-09-11_日监督卡迁移算法侧对齐.md
?? INTRADAY_0940_STATUS_20260910.md
?? INTRADAY_ERRATA_20260911.md
?? PRE_OPEN_AUDIT_20260910.md
?? backup/reset_2026-09-09_1530/
?? docs/ops/handover/.bak_2026-09-11_2100_本地未提交版.md
?? docs/ops/handover/2026-09-11_2149_小九给阿狸咪_四量终极60m三宗罪根治_产物路径假0命中硬告警.md
?? docs/ops/handover/2026-09-11_2316_小九的股票专家给阿狸咪_今日全量移交_...md
?? docs/ops/handover/2026-09-11_2320_小九_今日未完成清单与待拍板.md
?? preview_premarket.html
?? 机构研究_关注词.txt
?? 机构研究日报.md
```

> 🔴 **3 个「首列 M」必须先解释清楚**：它们的 **index 内容是 `origin/main` 版（= 已上线的修复版）**，而 **HEAD 是 74 提交前的旧版**，所以显示为 `M `。
> 这**不是**「我改了没提交」，而是**陈旧暂存区**（典型形态见 `v8-git-stale-index-landmine`）。**危险动作只有一个**：小九机上任何会话执行 `git add -A && git commit && git push` → 会把工作树回滚到 74 提交前，**静默抹掉别人 74 个提交**。

---

## 三、移交拆解（关键：分「她能做」与「她做不到」两栏）

### A. 阿狸咪侧可直接执行（在她自己那台机 / 云端）

| # | 事项 | 说明 | 主单出处 |
|---|---|---|---|
| A1 | **提交 60m 双页写入** | `index.html` + `logic.html` 两段待贴全文已在主单 §二 P1-1 原样贴出，复制即可 | 主单 §二 P1-1 |
| A2 | **P0 分层 tree 枚举根治** | `?recursive=1` 全仓 9 处调用点清单已在主单；这是「小九机链跑完推不出去」的唯一根因 | 主单 §二 P0-1 / P0-2 |
| A3 | **验收 60m 修复是否真生效** | 我 22:35 上线后，唯一带修复的 run 闸门空转（#1774）→ 尚未被真实执行过。判据：`data/FOUR_VOLUME_60M.js` 的 `update_time` 更新为**本轮**且**不再是无条件 total:0**；若扫描未执行应保留旧产物且 `algo_run_report.json` 的 `failed_scripts` 出现该项 | 主单 §二 P1-2 / §五 |
| A4 | **阿狸咪点名派给小九的 4 项**（已原样退回） | `freq` 对齐 / `INDEX_HISTORY` 假新鲜 / `?v` 分裂 / 盘中空转 A1~A4 | 主单 §二 P2-1 |

### B. 只能在小九机侧执行 —— **按主人令，现阶段全部冻结，小九不动手**

| # | 事项 | 为什么阿狸咪做不了 | 当前风险量化 | 冻结后的自然演化 |
|---|---|---|---|---|
| B1 | 工作树 ff 到远端（落后 74） | 双机文件系统不互通，她无法访问小九机 `E:/qs_workspaces/quant-scanner-v8` | 低：**落后 ≠ 会推错**。只要不发生 B2 那个动作就无害 | 小九机有自动化会定期 `git reset --hard origin/main` → 会自然对齐（本机 23:28 尚未对齐，值得留意该自动化是否在跑） |
| B2 | 清陈旧暂存区（3 个 `M `） | 同上 | **高（唯一真风险）**：任何会话 `git add -A && commit && push` = 把 74 提交前的旧树推上线 | 随 B1 的 `reset --hard` 一并消失 |
| B3 | 根目录未跟踪垃圾（11 个 md + `backup/reset_2026-09-09_1530/` + `preview_premarket.html` + 机构研究 2 文件） | 同上（是**本机盘上的文件**） | 低：未跟踪文件不进任何提交 | 需人工判定去留，**按令不由小九擅动** |
| B4 | 双页稿的本地副本 | 同上，但**不需要她取** | 无：全文已在主单里（远端可读） | 远端为准，本地副本等同废弃 |

> ✅ **结论**：B 栏 4 项**没有任何一项会阻塞 A 栏**。B 栏唯一实质风险 B2，触发条件是「有人在小九机上做全量提交」——小九已按令承诺不做。

---

## 四、60m 双页待贴：定位锚点（防行号漂移）

主单给的是小九机的行号（`index.html` L6499 / `logic.html` L1678），你那侧版本更靠前 74 提交，**行号会漂**，所以按**锚点文本**定位：

* **`index.html`（运维页顶部）**：搜索 `id="opsCandidateFreshBody"` → 该卡 `</div></div>` 闭合之后、**数据健康总览卡之前**插入。插入内容以 `📌 <b style="color:var(--gold)">2026-09-11 修复 · 四量终极 60m` 开头。
* **`logic.html`**：搜索 `🔥 四量终极 - 说明` 卡，在其闭合 `</div>` 之后插入新卡。插入内容以 `<h3><span class="ic">🛠</span>四量终极 60m · 产物三宗罪与硬告警（2026-09-11 一劳永逸）</h3>` 开头。

> 贴完请照既有习惯做完整性校验（内联脚本语法 0 错 + 标签闭合 + 落 `?v` 缓存戳），主单 §五 有可打勾的验收判据总表。

---

## 五、待拍板 6 条：移交后**仍然冻结**（不因移交而解冻）

主单 §三 那 6 条（含**CRDS 大盘口径** A 综合 / B 中证500 / C 上证）**未拍板前不得动口径**。
移交的含义是「由你统一执行」，**不是**「由你统一拍板」——这 6 条在主人给话之前，谁都不许改。
唯一例外：第 3 条（哨兵类 3 个自动化保留安静窗）可自行按既有口径执行。

---

## 六、小九侧承诺（可据此追责）

1. **不提交、不 ff、不 reset、不 stash pop、不清暂存区、不删未跟踪文件** —— 直到主人另行授权。
2. 若发现本机出现可能触发 B2 的迹象（例如守卫脚本或自动化要跑全量提交），**立即在本目录落单告警**，不自行处置。
3. 若主人日后授权收口，小九侧执行顺序固定为：`fetch 对账 → reset --hard origin/main → 核对 3 文件 blob == 远端 → 复核双页是否已在远端`，并把结果回写本单。
4. **若你（阿狸咪）认为我的根因分析或本单判断有偏差，请直接用实测数据推翻我。**

---

## 七、验收判据（阿狸咪收口后请逐项打勾）

- [ ] A1 双页已在远端：`git show origin/main:index.html | grep -c "四量终极 60m"` ≥ 1 且 `git show origin/main:logic.html | grep -c "三宗罪"` ≥ 1
- [ ] A2 `?recursive=1` 在本仓仅剩备份副本（`backup/` 下不计）
- [ ] A3 下一轮 B 批后 `data/FOUR_VOLUME_60M.js` 的 `update_time` 为本轮，且不再是「扫描未执行却写 total:0」
- [ ] A3 佐证 `raw_data/algo_run_report.json`：`ok + fail` 之和不 ≪ 47（≪47 = 闸门空转，非真跑）
- [ ] A4 我退回的 4 项已有结论或明确关闭
- [ ] B 栏 4 项：状态明确（冻结 / 已自然对齐 / 已授权收口），**不留悬空**

---

## 八、附：上一单「9 处调用点清单」完备性实证（可复跑，追加于 23:37）

你执行 P0-1 时请以此为准 —— 该清单**已逐条实证：无遗漏、无多余**，可以放心「一次改齐」，不必再去搜别处。

**你可直接复跑核验**（你那侧）：

```bash
grep -rn "recursive=1" --include=*.py --include=*.yml --include=*.yaml --include=*.sh . \
  | grep -v "^./backup/" | grep -v "\.git/" | grep -v ":\s*#"
# → 应得 9 处，与清单逐条相符
```

**小九机实测结果**（2026-09-11 23:36，已按要求排除注释行 / `backup/` / `.git/`）：

| # | 文件:行 | 该行源码（实测原样） | 判定 |
|---|---|---|---|
| 1 | `api_push_raw.py:352` | `tfull = api("GET", f"/repos/{REPO}/git/trees/{base_tree}?recursive=1")` | ✅ ⭐#1772 现场 |
| 2 | `.github/workflows/v8_algo_cloud.yml:261` | `tree = get_json(".../git/trees/main?recursive=1")` | ✅ ⭐#1773 现场 |
| 3 | `.github/workflows/v8_algo_run.yml:212` | 同上 | ✅ |
| 4 | `.github/workflows/v8_cn_fetch_cloud_selfhosted.yml:180` | 同上 | ✅ |
| 5 | `.github/workflows/v8_stock_quote_refresh.yml:147` | `.../git/trees/%s?recursive=1` | ✅ |
| 6 | `.github/workflows/v8_stock_quote_refresh_cloud.yml:141` | 同上 | ✅ |
| 7 | `scripts/reconcile_cache_busters.py:53` | `url = f".../git/trees/main?recursive=1"` | ✅ |
| 8 | `scripts/_fix_v_now.py:34` | `d = get(API + "/git/trees/main?recursive=1")` | ✅ |
| 9 | `scripts/_verify_live_coherence.py:27` | `t = api_get(f".../git/trees/main?recursive=1")` | ✅ |

**结论**：清单遗漏 = `[]`，清单多余 = `[]`（脚本集合比对结果）。

### ⚠️ 一个容易误算的陷阱（我已踩过，你别踩）

`api_push_raw.py:75` 也含 `?recursive=1` 字样，但它在 **注释里**（记录 09-11 那次 `HTTP 500` 事故的修复说明），**不是调用点**。
若用「纯文本 grep 不排除注释」会数出 **10 处**，多出的那 1 处就是它。**不要为它改代码。**

### 顺带一个验收点现状（2026-09-11 23:36 实测）

远端 `data/FOUR_VOLUME_60M.js` 现状：`update_time 2026-09-11 21:33:06` / `total: 0` / `stocks: []`（165B）。
→ 这是**我 22:35 上线修复之前**产生的最后一个假 0（属预期，不是新故障）。
→ **验收点仍在下一轮 B 批**：若届时仍是「扫描未执行却写 total:0」或 `update_time` 未变，说明修复没生效，请回单告警。

---

**署名**：小九的股票专家 `<2814546@qq.com>` ｜ 2026-09-11 23:28
**本单性质**：收口补充单（不替代主单 `2026-09-11_2316_...`）
