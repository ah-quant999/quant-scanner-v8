# HANDOVER 小九 → 阿狸咪 · 2026-08-30（12:00 档 · 合并并行实例 + 深度核查）

> **更新于 2026-08-30 12:12 自动更新**（自动化 `automation-1785719698947` · 12:00 档）
> 本档**合并**同档并行实例 12:06 已推送的版本（`f87cad138`）与凌晨档内容，并**补入深度核查结论 + 修正两处判断**（见 §四 P1-①、§四 P2-⑥）。
> 所有 SHA 均经 `git log origin/main` / `merge-base --is-ancestor` 实核，**未用记忆值**。

---

## ⚠️ 第一条铁律：`origin/main` 即权威，禁止回滚

- **唯一正确恢复姿势**：`git fetch origin main && git reset --hard origin/main`
- 禁止：`git revert` 今日提交 / `git checkout <旧commit> -- index.html` / `git push --force` 旧版本
- 禁止：`git add -A`（坚果云秒删磁盘文件 + 误删 `data/`），一律显式 `git add <具体文件>`
- 禁止改动另一 clone `E:/workspace/workspace/quant-scanner-v8`
- **禁止 `git stash pop`**，见 §四 P1-③（旧 `raw_data` 覆盖新鲜数据 = 回滚地雷）

---

## 一、背景

| 项 | 值 |
|---|---|
| 日期 | 2026-08-30（**周日 · 休市**） |
| 双机分时 | 小九白天 7:45–17:45；阿狸咪夜间/周末 18:00–次日 7:30。**周日阿狸咪为主**，小九跑盘前全量 + 交接，18:00 后交还 |
| 主人行程 | 8/21–8/27 出差已收官，双机分时铁律恢复正常 |
| 当日主改动来源 | 凌晨由阿狸咪/并行实例按「主人令 2026-08-30」完成；日内为一劳永逸修复 + 清理 + 云端流水线 |

**⚠️ memory 日志缺口**：`E:\workspace\stock-scanner\.workbuddy\memory\2026-08-30.md` **不存在**（该仓最新 2026-08-20）；v8 侧当日日志亦不存在（最新 2026-08-29）。
→ 故本档**不是**「无新增改动」：今日 `origin/main` 实有 **92 笔**提交，内容全部由 `git log origin/main` + 线上实测反推核实。

---

## 二、今日已完成改动（真实 SHA + 影响）

### A. 凌晨 · 主人令（已入库，4 笔均经 `--is-ancestor` 验证 ✅）
| SHA | 内容 |
|---|---|
| `dfa9fdeaa` | 选股策略默认首 tab 改为**选股池**，删除已下架重复「观测候选股池」，来源行改为点击展开 |
| `ac5086102` | **全站「观澜台」更名「外资研投」**（算法/脚本/workflow/自愈监控，22 处） |
| `430df68a1` | 候选池/金股池数据同步观澜台→外资研投命名 |
| `d8b3babed` | 一劳永逸 5 项 UI 修复（banner 移走 / 8 列 / 交易时段 / 强势突破加动量共识 / data stocks） |

**影响**：已下架不再重复展示候选池；选股策略默认进入「🧭 选股池」；用户可见「观澜台」字样全站清零（**内部文件名/Secret 名仍保留**，见 §四 P2-⑤）。

### B. 平均股价 880003 方案A（真实通达信）
| SHA | 内容 |
|---|---|
| `067c311f8` | 平均股价改真实通达信 880003（方案A）：重写 `fetch_avg_price` 抓 120 日真实日K 算 MA20/60，挂云端流水线 |
| `e6cae1ef6` | 方案A 前端护栏（`available≠true` 不显示假信号）+ 数据源脱敏 / `isAdmin` / 标签 880003 |
| `197f0ea7e` | merge：合并云端 build 与本地方案A/cleanup 修复 |

> ⚠️ **此批留有真实缺陷，见 §四 P1-①**：MA20/MA60 实际退化，120 日日K 未真正落地。

### C. weekly cleanup 误删根治（防数据丢失，重要）
| SHA | 内容 |
|---|---|
| `a8ab70ec6` | 根治 weekly cleanup 误删：映射大小写 + 变量名双判定，**77 文件误删不再复发** |
| `881586b92` | 补 `STOCK_RPS_DATA` 的 script 引入（相对强度页此前**恒空**）+ cleanup 白名单防误删 7 个命名不一致文件 |
| `3bc4190e1` | merge：解决 `STOCK_RPS_DATA.js` 冲突，采用云端最新生成版本 |

### D. 一劳永逸式修复 + 审计落地
| SHA | 内容 |
|---|---|
| `5e3015b38` | 一劳永逸式全修（前端展示层 + 算法根因） |
| `c7e37b97c` | 13 项一劳永逸式修复（`mega_patch_20260830_v2`） |
| `41adeeb15` | 13 项收尾：强势/调试分家 + 炸板 8 列 + 去源脱敏 + `isAdmin` + 客户可见区精简 |
| `886d97a93` | #106–#109：时间标签防未来显示 / 暂未上架重构 3 卡 / 三卡沉已下架顶 / 删驾驶舱灯 |
| `95a33aea0` | LOW 级打磨 #13-15、#17-23：跨卡时点标注 / 估值代理 relabel / Fib 下一窗口含 active / 资金流 MA5,20 / 涨停「其他」桶沉底 / H 反推快照日期 / 四量非故障说明 / FOMC 枚举 / 申赎时点 / 板块 Block3 源标注 |
| `b8b6c8e77` | 审计后三项落地 |

### E. 死代码清理 + 健壮性
| SHA | 内容 | 影响 |
|---|---|---|
| `8114dab04` | 删实时数据区 **~430 行**死代码（旧 LHB 月历/历史条/四张分析卡/旧大牛股猎手）+ F1/F3/F8 健壮性 | 减重、防崩 |
| `b40414121` | `v8_health_check.py` 防御性兜底：`check_*` 返回 `None` 时 `or []` 防 `build_report` 崩 | 消除历史崩溃 bug |
| `d67507754` | 删调试专区「🚀 个股动量状态」卡（17,761 字节；主人明令：强势突破 pane **不在**删除范围） | 调试专区现仅 3 卡 |
| `9e44f7e07` | 删死文件 `wf_health.yml`（`v8_health_patrol` 陈旧未注册副本） | ✅ 已核实不在 `origin/main` |

### F. 🚨 任务看板 DELISTED 红灯修复
| SHA | 内容 |
|---|---|
| `ec02066bf` | 新建生成器 `scripts/build_delisted.py` + `scripts/build_unlisted_panel.py`（强制 `update_time=今日` + `meta` 字段）✅ 两脚本已核实存在 |
| `51d213c02` / `84f1288bf` | 重 build `DELISTED.js` / `UNLISTED_PANEL.js` + 重算 `HEALTH_CHECK.js` |

### G. 🛡️ 防回滚自检
| SHA | 内容 |
|---|---|
| `4c02159ec` | 新增 `v8_rollback_guard.py`（结构完整性自检，非仅数据新鲜度）✅ 已核实存在于 `origin/main` |

### H. 缓存戳 / STOCK_QUOTE / 交接文档
| SHA | 内容 |
|---|---|
| `5a5edd7b4` / `967d0bba6` / `49a0cc083` | 缓存戳实时对齐 `?v` → 防覆盖（10:42 / 10:33 / 08:37），治本覆盖地雷 |
| `b523dbaef` / `57cda3f40` | 经 `v8_stock_quote_refresh` 刷新 `STOCK_QUOTE` |
| `dba815a5d` / `25a6ecc53` / `569f82822` | 交接文档（两天改动总览 / STOCK_QUOTE 开盘前提醒 / 选股池对齐+观澜台更名） |
| `f87cad138` | 交接文档 12:00 档（并行实例，**已被本档合并覆盖**） |

### I. 云端自动流水线
其余约 60 笔为机器人自动提交：`v8 build:`（`2e4884ee9`/`6b53f3037`/`43808ec3d`/`d098aed0a` 等）、`v8 cn fetch:`（`709993d06`/`a19112e01`/`135e9d189`/`d353a7b88` 等）、`v8 risk gauge:`（`740289e7f`）、`runner health:`、`audit: 每日全站审核`、`chore(v8): 每日安全网推送`、分红方案刷新等。

---

## 三、当前系统状态（12:12 实核）

| 项 | 值 | 判定 |
|---|---|---|
| 站点 | `https://ah-quant999.github.io/quant-scanner-v8/` | ✅ **HTTP 200**（1.86s） |
| `index.html` 体积 | **983,554 字节** | ✅ 非空/非截断 |
| `origin/main` HEAD | **`f87cad138`**（前值 `2e4884ee9` = `v8 build 11:50`） | ✅ 权威 |
| 今日提交数 | **92 笔** | ✅ 流水线活跃 |
| 健康自检 | `overall=ok` · **106 ok / 1 warn / 0 fail**（107 项，updated 11:50:34） | ✅ 无 fail |
| 算法运行 | run_time **11:46:03**，category=all，**28/28 模块 ok**（empty 0 / fail 0） | ✅ |
| 本机工作树 | `C:\Users\Administrator\qs8-tmp`，HEAD == `origin/main`，**落后 0 笔，工作树干净** | ✅ 已对齐 |
| 剩余自动化 | **17 个**（16 ACTIVE + 1 PAUSED） | ✅ |

### 自托管 runner
| Runner | 状态 |
|---|---|
| `lemoncat-cn` | ✅ **online** / idle |
| `alimi-cn` | ⚪ **offline** / idle（周末阿狸咪机未开，云端主链路兜底） |

### Workflow（近 18 次）
✅ **全部 success** —— `☁️ v8 构建部署(云端ubuntu)`、`🇨🇳 v8 中国数据抓取(云端)`、`🩺 v8 云端健康巡检(合并·自愈+体检)`、`🌍 v8 实时风险温度计`、`pages build and deployment` 均 success（其中 1 笔 pages build `cancelled`，属重复触发的正常取消）。

### 关键数据新鲜度（读 `data/*.js` 内层 `update_time`，非派生项）
| 数据卡 | update_time | 判定 |
|---|---|---|
| `STOCK_QUOTE` | 2026-08-30 10:31:12 | ✅ 已解除 8/24 陈旧快照风险 |
| `V8_CAL` | 2026-08-30 11:43:22 | ✅ |
| `AVG_PRICE_DATA` | 2026-08-30 11:46:01（republish 11:50:28） | ⚠️ 见 P1-① |
| `DELISTED` / `UNLISTED_PANEL` | 2026-08-30 | ✅ 红灯已消除 |
| `LHB_DATA` | 2026-08-29 08:43:06 | ✅ 周末休市不更新，属预期 |
| `NT_DATA`（市场提示） | 2026-08-29 09:05 | ✅ 休市不更新，属预期 |

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 P1-① 平均股价 MA20/MA60 **实际退化**（**修正并行实例判断**）
> 并行实例 12:06 档记为「新鲜、阈值边缘、非失败、观察即可」——**该判断偏浅**。深挖数据体后确认存在真实缺陷：

- **实测**：`history` 仅 **1 条**、`ma20 == ma60 == avg_price == 28.4421`、`prev_avg_price = null`、`count = 5286`。
- **含义**：§二-B 宣称「抓 120 日真实日K 算 MA20/60」**未真正落地**。MA 由单日历史算出，等于当日均价本身，`position_vs_ma20/60 = -0.0001` **无参考价值**。
- **health_check 报的空值告警**（`avg_price, ma20, ma60, position_vs_ma20, position_vs_ma60`）时间戳 11:46:58 早于 republish 11:50:28，**空值部分属读取竞态**（现已有值）；但 **MA 退化是独立的真实问题，不会自愈**。
- **建议排查方向**：`fetch_avg_price` 的历史累积逻辑 —— 每次覆盖而非追加？还是 120 日日K 拉取失败后**静默降级**为单日？
- 前端护栏（`available≠true` 不显示）已生效，**不会对外显示假信号**，故非 P0，但不应记为「观察即可」。

### 🟠 P1-② 自动化 cwd 仍是空壳（连续 2 天，待主人决策）
- 配置 cwd `E:\workspace\quant-scanner-v8` **不是 git 仓库**（仅 `.workbuddy/` + 交接文档 + `_heartbeat.log`）。
- 真实仓库 = `C:\Users\Administrator\qs8-tmp`；token 与源码均须从此处取，本档亦在 qs8-tmp 实跑推送。
- 建议：改自动化 cwd 指向 `qs8-tmp`，或恢复坚果云同步。

### 🟠 P1-③ **两个 stash 未 pop —— 严禁 `git stash pop`**
| stash | 内容 |
|---|---|
| `stash@{0}` | `handover-20260830-wip-preserve`（**111 项**：`index.html` / `HEALTH_CHECK.js` / 大量 `raw_data/*.json` 在飞改动） |
| `stash@{1}` | `handover-20260829-wip-preserve`（**昨日遗留，同样未 pop**） |

- 本轮开始时工作树曾有 **106 个 tracked 修改 + 5 untracked**，`git merge --ff-only` 被阻断（*"local changes would be overwritten"*）；并行实例已将其 **stash 保全**并把 HEAD 对齐 `origin/main`，**数据零丢失**，现工作树干净。
- ⚠️ **勿直接 pop**：旧 `raw_data` 会覆盖新鲜 origin 数据 = **回滚地雷**。需取回请联系小九/主人定夺。

### 🟡 P2-④ 「开盘前防回滚自检 7:25」自动化**本机查无此项**
- 昨日交接称已注册自动化「九宝量化-开盘前防回滚自检(阿狸咪 7:25)」，但**本机 WorkBuddy 17 条清单中无此项**。
- 脚本 `v8_rollback_guard.py` 本体确认已在 `origin/main` ✅，缺的是**调度注册**。
- **请阿狸咪确认**该自动化是否注册在阿狸咪机器自己的 db 上且为 ACTIVE。若两台机器都没有，则「结构回归自愈闭环」缺一环，该脚本沦为无人调用的死脚本。

### 🟡 P2-⑤ 内部命名未改（仅中文标签改了）
- `guanlan_*.json` 文件名与 `ZSXQ_TOKEN` Secret 名**仍保留 guanlan**，仅用户可见中文标签改「外资研投」。彻底改名需主人授权。
- 历史 `HANDOVER_*.md` 仍含「观澜台」字样，属历史记录，不影响主站。

### 🟡 P2-⑥ `wf_health.yml` 处置**已反转**（纠正昨日指引）
- 该文件今日已由 `9e44f7e07` **正式删除**（已核实不在 `origin/main`）。
- 昨日交接文档写的是「若 `git status` 见 `D wf_health.yml` 就 `git checkout origin/main -- wf_health.yml` 取回」——**该指引今日起作废**。若本地再出现该文件，属陈旧副本，**勿再取回**。

### 🟡 P2-⑦ 开机后务必硬刷新验证
- 缓存戳防覆盖已上线（`5a5edd7b4`/`967d0bba6`），阿狸咪开机后 **Ctrl+F5 硬刷新**确认生效：外资研投全站替换 / 已下架区 / 调试专区瘦身 / 死代码清理 / 选股策略 tab 重构。
- 建议顺手跑 `python v8_rollback_guard.py --check-only`（应 exit=0）。

### 🟡 P2-⑧ 本档 12:00 档被**并发跑了两次**（重复劳动）
- `f87cad138`（12:06 并行实例）与本档（12:12）为同一自动化 `automation-1785719698947` 的两个并发实例。
- 建议主人确认该自动化是否在两台机器/两处 db 重复注册，避免每档重复推送与内容互相覆盖。

### 🟢 P2-⑨ 其他
- 周末 `alimi-cn` runner offline 属正常；云端 `v8_cn_fetch_cloud` post_close + `v8_health_patrol` 承担主链路自愈，本地 `self_heal_monitor` 仅二次兜底。
- `小九-TDX强势选股每日跟踪(三件套v2)`（`automation-1786933089412`）为 **PAUSED**，恢复须主人明令授权。
- 盘中查陈旧须解析 `data/*.js` 内层 `update_time`，**别信 overall ok 派生项**。

---

## 五、小九监督职责

| 类别 | 任务（自动化 ID） | 频率 |
|---|---|---|
| 交接 | 本档更新（`1785719698947`） | 每日 12:00 / 18:00 |
| 巡检兜底 | 紧急交接通道看门狗（`1785835481750`） | 7–21 时每小时 |
| 巡检兜底 | runner 本地守护·错误即自愈（`1786696074524`） | 20 分钟级 |
| 巡检兜底 | 自愈监控闭环 candidate+momentum（`1786961773708`） | 10 分钟级 |
| 巡检兜底 | 盘中漏轮兜底（`1787108824799`）/ 全时段 freshness_guard（`1785898357370`） | 盘中 |
| 数据管线 | 盘前全量 08:15（`1785888730897`） | 每日 |
| 数据管线 | 收盘刷新 15:30（`1785388522458`）/ 选股四模块 17:00（`1785404904506`） | 交易日 |
| 数据管线 | 周末 T+1 15:30（`1785396622150`）/ 19:00 审计推仓对齐（`1785978303212`） | — |
| 专项 | westock 持仓同步（`1787210164001`）/ 分红刷新（`1786588096327`+`1786588094908`）/ James-Stein 回测（`1787575410637`）/ 本地看板同步（`1785802406605`） | — |

**小九承诺**：不 `git revert` / 不 `checkout` 阿狸咪提交 / 不 `push --force` / 不 `git add -A` / 不 `stash pop` / 不动 `E:/workspace/workspace/quant-scanner-v8`。
**本次仅提交本交接文档一个文件**，未 `git add` 任何其他文件，未触碰 `raw_data/*.json` 与 `HEALTH_CHECK.js`。

---
*本档由小九定时自动生成于 2026-08-30 12:12（12:00 档，合并并行实例 `f87cad138` + 深度核查）。下一次更新：今日 18:00。*
