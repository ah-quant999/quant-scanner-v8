# 🚨 URGENT — v8 盘中巡检发现「自愈验证 step 结构性假红」+ `?v` 口径分裂未修

- **日期**：2026-09-14（周一）09:34 CST
- **发起**：阿狸咪的工程师（家用机，夜间/周末/盘中巡检）
- **接收**：小九的工程师（单位机，白天 07:45–17:45 值班）
- **本轮是否踩踏风暴**：❌ **不是**。活跃/排队 run = 1、GitHub core 配额 5000/5000 全空、无 403。
  **全程未加派任何 dispatch**（遵守防踩踏铁律）。
- **本轮是否已改码**：❌ 未改。仅只读诊断 + 本交接文档（走 Contents API 推送，未用本机 git push）。

---

## 一、本轮巡检结论摘要

| 项 | 结果 |
| --- | --- |
| 退出码 | 1 |
| 活跃/排队 run | 1（`☁️ v8 盘后算法链(云端)` in_progress） |
| 近 3h 真实失败 | 1 次：`☁️ v8 构建部署` run 34793880663 cancelled（同秒另有 success 34793881832 = 并发组互顶，非故障） |
| data/*.js 落后 | 12 个（权威源 = GitHub main） |
| 判读基准 | GitHub main 分支（已取 58/60 个文件） |
| 加派 dispatch | 0 |

---

## 二、🔴 根因 A（P0 新发现）：`health-patrol` 的「🔁 自愈验证」step 是**结构性假红**

### 2.1 现象（实证）

`v8_health_patrol.yml` 近 60 run：**50 success / 6 failure / 4 cancelled**。
6 次 failure **100% 同一 step**：

```
🔁 自愈验证：10 分钟后回查仍 fail → 升级告警  → failure
```

| 时间（UTC） | run id |
| --- | --- |
| 2026-09-14T00:37:59 | 34793309357 |
| 2026-09-11T15:41:14 | 34617595669 |
| 2026-09-11T15:07:11 | 34614213076 |
| 2026-09-11T14:36:54 | 34611176981 |
| 2026-09-11T14:05:49 | 34608109907 |
| 2026-09-11T13:28:21 | 34604488497 |

### 2.2 直接触发链

该 step 逻辑（`v8_health_patrol.yml`）：派发自愈后 `sleep 600`，再跑
`python v8_health_check.py --no-heal`，用

```bash
REMAIN_FAIL=$(grep -c '"status": "fail"' verify_output.txt ...)
if [ "$REMAIN_FAIL" -gt 0 ]; then
  echo "::error::自愈闭环未达成 — $REMAIN_FAIL 项 10 分钟后仍 fail"
```

判红。09-14 那一轮的 `verify_output.txt` 里，**唯一 fail 项就是 `local_sync`**。

### 2.3 根因（真凶）：`check_local_head_sync()` 判的是**云端临时容器**，不是任何一台真机

证据链：

1. `v8_health_check.py::check_local_head_sync()` 判据是
   `git rev-parse HEAD` vs `origin/main`，若 `git diff --name-only <local>..origin/main -- raw_data/` 非空
   → `status = "fail"`（原注释明确写：「raw_data 真的落后 → 数据卡陈旧的二级根因 → fail + 自愈拉取」）。
2. 但 `v8_health_patrol.yml` 的 `health-patrol` job 是
   **`runs-on: ubuntu-latest` + `actions/checkout@v4` + `fetch-depth: 1`** —— 云端一次性浅克隆容器。
   该容器 checkout 时的 HEAD = **触发那一刻的 main**；而本仓 build/生成器**每几分钟就在推新 commit**，
   所以容器里跑的 health_check 永远看到「本地 < sha > /= origin/main」，且
   `fetch-depth: 1` 让那句 `git diff local..origin/main -- raw_data/` 在容器里**极易非空**
   → 直接落进 `status = "fail"` 分支。
3. 该 fail → `--heal` 派发 build_deploy 对齐（对云端容器无意义）→ `sleep 600` 后回查**必然仍 fail**
   → `::error::自愈闭环未达成` → step exit 1 → **整个 patrol run 判 failure**。

### 2.4 危害（为什么必须修，不是美观问题）

- **假红淹没真红**：patrol 是本仓唯一的「一站式诊断 + 自愈派发」中枢。
  它每轮被自己的架构缺陷判红 → 邮件升级告警噪音 → 真故障（如 09-11 那批）来临时**无法从告警里区分**。
- **自愈闭环被污染**：因该 fail 会触发 `_heal_local_sync()` → 派发 build_deploy，
  这在云端容器语境里是**纯空转派发**，属踩踏燃料（09-11/09-12 的 cancelled 高峰有它一份）。
- **09-14 00:37:59 这轮最典型**：`gate` job success、`health-patrol` job 只因这一个 step 判红，
  其余步骤（含真正的巡检 + 缺口兜底派发）全部正常。

### 2.5 🎯 一劳永逸修复方案（**给方案，不建议重跑**）

**方案（推荐，最小改动）**：给 `check_local_head_sync()` 增加「是否在 CI 内」短路。

- 判据：环境变量存在 `GITHUB_ACTIONS=true`（云端 runner 天然带）时，
  `local_sync` 一律返回 `status = "info"`，message 注明
  「运行于 CI 容器（浅克隆），本地/远端差属预期，不代表任何真机落后」。
- 依据：该检查的**唯一合法语义**是「检查**运维机**（小九/阿狸咪）的本地仓库是否落后」，
  这只能在**运维机本地**跑（如 `v8_health_check.py` 的本地定时调用）才有意义；
  在 CI 容器里跑**语义上就不成立**。（同类判据本仓已有先例：
  2026-08-18 注释里主人就曾要求把「本地落后」降为 info。）

```python
def check_local_head_sync():
    ...
    # 🛡 2026-09-14 阿狸咪根因修复：CI 容器内本检查语义不成立。
    #   health-patrol 跑在 ubuntu-latest + fetch-depth:1 浅克隆，
    #   HEAD 必然 != origin/main（build/生成器每几分钟推一次），
    #   且 fetch-depth:1 使 raw_data diff 极易非空 → 落 fail 分支
    #   → 自愈验证 10 分钟回查必仍 fail → patrol 整轮假红。
    if os.environ.get("GITHUB_ACTIONS") == "true":
        return [{
            "id": "local_sync", "name": "本地与 origin/main 同步", "page": "管线",
            "status": "info",
            "message": f"运行于 CI 容器（浅克隆 fetch-depth:1），HEAD {local[:7]} 与 "
                       f"origin/main {remote[:7]} 的差异属预期，不代表运维机落后",
        }]
```

**方案（可选加固）**：`health-patrol` job 已用 `runs-on: ubuntu-latest`（云端，非自托管 cn runner）。
若希望保留「运维机落后」这项巡检能力，应把它**移到运维机本地定时任务**（小九/阿狸咪各自的
WorkBuddy 自动化里跑 `v8_health_check.py`），而不是塞进云端 workflow。

**验证方式（不许靠重跑交差）**：
修好后任选一个盘后时窗，观察 patrol 连续 3 轮结论；`local_sync` 应呈 `info`，
且不再出现 `::error::自愈闭环未达成`。若仍红，需重新取 `verify_output.txt` 定位**新的** fail 项 id。

---

## 三、🔴 根因 B（P1 遗留未修）：`index.html` 的 `?v` 缓存戳**仍是 4 种口径并存**

- 实测（远端 main，blob `83de64dc07fbaeb8f37aa9c41152da33a120f11d`，文件 953,608 字节）：
  105 处 `?v=`，**4 种不同值**：

| 值 | 出现次数 | 性质 |
| --- | --- | --- |
| `1789348837` | 102 | unix 时间戳（生成器「原子提交 ?v」写的） |
| `20260911）` | 1 | 文本污染（疑似注释/文档串被误当引用） |
| `20260910` | 1 | **停在昨天**（09-11 起就未变） |
| `063d312703` | 1 | sha10 残片（模板泄漏） |

- 这与 09-10/09-11 交接的**同一根因**（两个写入方互顶：`v8_stock_quote_refresh.yml` 写 sha10、
  云端生成器「原子提交 ?v」写 unix 时间戳）→ CDN 可能吐旧副本。
- 09-11 时是「103 处 4 种」，**今天 105 处 4 种 —— 未修，且随引用增长在恶化**。
- 一劳永逸口径：**全站统一为内容 sha10，彻底禁用 unix 时间戳**；并对 `20260911）`、`063d312703`
  两处做定点清理（它们不是合法缓存戳）。

---

## 四、✅ 本轮已确认自愈 / 已收敛项（无需动作，仅备案）

| 项 | 上轮状态 | 本轮实测 |
| --- | --- | --- |
| `data/INDEX_HISTORY.js` 假新鲜 | ❌ meta=09-08T16:54、klines 末条 09-07 | ✅ **已自愈**：meta.update_time = 2026-09-14 09:15:08、republish 09:16:05、klines 末条 **2026-09-11**（= 上一交易日，正常） |
| `☁️ 盘后算法链(云端)` 盘中高频空转 | ❌ 09-11 15 次/62 分钟，闸门 `MARKET_HOURS_BLOCKED` 全 skip 但判绿 | ✅ **已收敛**：近 24h 约每小时 1 次 schedule，无盘中高频；且已加 `DEGRADED_UPSTREAM` 逃生门 + `v8-stage-gate-crash` 硬红灯（09-12 加） |
| algo_cloud 近 50 run | — | 26 success / 13 cancelled / 11 failure；**11 次 failure 集中在 09-12~09-13**，主因是「🛡 结果问责」判红（假成功防护生效，属**正确**行为）；09-13 07:47 后已全绿，09-14 仅 1 次 dispatch success |
| `v8_health_patrol` 盘中闸门 | — | ✅ 设计正确：`if: needs.gate.outputs.trading != 'true'`（09:25-11:30 / 13:00-15:00 整轮跳过），本轮 09:33 盘中不触发 patrol |
| `v8_stock_quote_refresh.yml` L379 `m.group(3)` | ✅ 09-11 已修 | ✅ 保持已修 |

---

## 五、待小九决策/执行清单

| # | 优先级 | 事项 | 建议 |
| --- | --- | --- | --- |
| 1 | **P0** | `check_local_head_sync()` CI 短路（根因 A） | 按 §2.5 打补丁；**只在 `v8_health_check.py` 内改，不动 health_patrol 的闸门逻辑** |
| 2 | **P1** | `?v` 统一为 sha10（根因 B） | 全站统一内容 sha10 + 定点清理 2 处非法值 |
| 3 | P1 | 09-12 周末巡检两个待修项（见项目 MEMORY） | ① 港股名单塌缩（`stock_names.json` HK=0 起于 `7ee6b71e2`）② `v8_weekend_light.py` 覆盖 `data/WEEKEND_RUN.js` |
| 4 | P2 | 巡检调度收敛 | 建议把本自动化的 cron 收敛为「周一~周五 09:35-11:25 与 13:05-14:55」，可省约 90% 空跑 token（**等主人授权，不擅改**） |

---

## 六、本轮数据落后清单（权威 = GitHub main，内容级 `update_time`）

| 文件 | 分类 | 落后 | 阈值 |
| --- | --- | --- | --- |
| `GOLD_POOL` | post_close | 66.6h | 30h |
| `CANDIDATE` | post_close | 66.6h | 30h |
| `INST_TRADE` | post_close | 63.8h | 30h |
| `V8_POOL_TRACKER` | post_close | 60.1h | 30h |
| `SECTOR_RS` | post_close | 56.6h | 30h |
| `INDEX_HISTORY` | post_close | 36.9h | 30h |
| `SUSPENSION_ALERT` | premarket | 35.8h | 30h |
| `SECTOR_FUND_FLOW_TREND` | post_close | 35.8h | 30h |

> 说明：全部为 **post_close / premarket 类**，落后起点落在 **09-11（周五）盘后 ~ 09-12（周六）**，
> 即**周末 + 周一开盘前**的自然断档，非盘中故障。周一（今日）盘后批次应自然补齐；若 09-14 盘后仍未补，再单独追。

---

## 七、本轮未做的事（明确划线）

- ❌ 未加派任何 dispatch（并发健康，无风暴）。
- ❌ 未修改任何代码/数据文件。
- ❌ 未使用本机 `git push`（走 Contents API）。
- ❌ 未打印、未写盘任何 token 内容。
- ⚠️ 本地工作区 `data/` 滞后 main 约 53 个文件 → **属坚果云不拉远端，非数据故障**，不计异常。

---

*阿狸咪的工程师 · 2026-09-14 09:34 CST · 只读巡检，等小九接手*
