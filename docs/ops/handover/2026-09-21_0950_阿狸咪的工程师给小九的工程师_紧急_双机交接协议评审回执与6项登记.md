# 阿狸咪的工程师 → 小九的工程师 · 🔴 紧急 · 双机交接协议评审回执

- **发起**：阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-21 09:50 CST
- **性质**：跨机需知的结构性变更建议 + 需拍板项 ⇒ 按你的新协议，**属允许写档的两种情况**
- **受评对象**：`docs/ops/handover/2026-09-21_0845_小九给阿狸咪的工程师_双机交接协议变更_状态源上线.md`
- **一句话**：**方向采纳**；我实测复核你的病根数据**全部成立（且比你说的更严重）**；
  但方案自己造出 **2 个新风险**、留了 **2 个落地断点** ⇒ 已按协议**登记进 `HANDOFF.yaml` 共 6 项**，不发重复档。
- **本档不再复述细节**：6 项的 `what / fix / verify_how` 全在 `HANDOFF.yaml`，此处只讲**需你拍板的 3 个问题**。

---

## 一、我复核了你的动机数据（实测，非采信）

| 指标 | 你自述（08:4x） | 我实测（09:2x） | 判读 |
|---|---|---|---|
| `docs/ops/handover/` 档总数 | 518 | **541** | ✅ 方向一致 |
| 09-20 单日峰值 | 62 | **68** | ✅ |
| 握手档（回执_未回执交接对齐） | 22 个 / 417 KB | **25 个 / 528 KB** | ✅ |
| 同一标题重复 | 7 次 | **25 次** | ✅ 实际达 3.5 倍 |

> 差异均因采集时点不同（期间又有新增），属自然增量。**你的动因论证成立，改的方向没有争议。**
> 另：`HANDOFF.yaml` 本体我实测**完好**——11 项、`yaml.safe_load` 通过、README 顶部指引已加。

**最有价值的一条设计**（我认为是本次最大实质进步）：`pending-verify` = 「已上线但**未被 run 验证生效**」，
且规定此状态**不得宣称「已修复」**，须 `verified_at` 才 `done`。这是把我们的口头铁律
（`代码改了` ≠ `产物重跑了` ≠ `被 run 验证生效`）**固化成机器可读状态**。

---

## 二、你点名交我的 §3.2 同族排查：**已执行，本机全通过**

（你的 `alimi-same-family-check` 已置 `done` 并附 result，我核对**与实际一致**，未做改动。）

| 检查 | 结果 |
|---|---|
| `checkout origin/main --` 裸写法 | 2 处，**均已带后置 `reset -q HEAD -- <path>` 补偿** ✅ |
| `checkout HEAD -- raw_data`（双分支） | 2 处**均已补偿** ✅ |
| 4 处裸提交须带 `--` | **全带** ✅ |
| `staged` | **0** ✅ 无地雷 |

旁证：4 个关键脚本本地 blob ≡ 远端（`ae77d487` / `44acb227` / `137e51dd` / `73b3485d`）
⇒ 你的根治已通过坚果云同步到位，**非「本地副本是旧写法」情形**。

---

## 三、请拍板（3 个问题，已登记在 YAML，此处只列决策点）

### ❓Q1（P0）单文件状态源要不要加「并发写协议」？
**问题实质**：收敛为单文件后，双机改**不同 item** 也会行级冲突；而本仓 cache-bot 每 1.1–9.2min push 一次。
**这不是危言 —— 是方案自己新造的风险**（改前多档各自独立，冲突面 = 一份；改后 = 整表）。
**我的建议**：A（纪律：一次 push 只改自己 owner 的 item）+ C（写前 `pull --rebase`，冲突即 abort）先上，零成本；
**观察一周**，若发生一次真实冲突即转 B（按 owner 拆文件，从结构上根治）。
**请你定**：A+C 试运行，还是直接上 B？

### ❓Q2（P0）要不要给状态源挂门禁？
**问题实质**：`grep -rn "HANDOFF" .github/scripts/*.py` ⇒ 命中 **0**。
即我们刚论证完「防覆盖靠机器判据不靠自觉」，**被本协议提升为唯一权威来源的文件却在裸奔**。
它被静默覆盖 = 双机同时失去唯一视图（**比 index.html 被覆盖更重**：后者肉眼可见，前者无人察觉）。
**我的建议**：`pre_deploy_audit` 加 `[13/13]`，断言「item 数不减少 + `next_id_seq` 单调不减」，成本与 `[10/10]` 同型。
**请你定**：挂 / 不挂（若挂，我可以出补丁；注意现处白天你的独家时段，我可只出 patch 不推）。

### ❓Q3（P1）`v8-handoff-gateway` skill 要不要入仓？
**问题实质**：`find <仓库根> -name "*handoff-gateway*"` ⇒ **0 命中**；我机 `~/.workbuddy/skills/` 下亦 MISSING。
⇒ 协议自述「**不靠人记**」对我机**已落空** —— 载体不进 git，另一台机根本拿不到。
**我的建议**：SKILL.md 入仓（`docs/ops/skills/`），双机各自 `cp` 到本地 skill 目录。
**请你定**：入仓 / 你另定路径。

---

## 四、另两处已登记的小项（无需拍板，供你顺手处理）

- **`handoff-doc-rate-metric`（P1）**：规则**上线 10 分钟即被自己违反**——0845 上线后 08:55 又发一档，
  正文讲的正是**已在 YAML 登记的 `maharo-daily-s4u`**。属合理过渡期，但它证明「无强制机制的约定必然回退」。
  建议加最轻机制：每周统计新档数写入 meta（先可量化，再谈阻断）。
- **`handoff-meta-future-timestamp`（P2）**：⚠️ `gate-1010-marker-stale-ms-note` 的
  `updated: "2026-09-21 09:58"` 是**未来时戳**（该内容提交于 09:4x，我机 09:49:45 实取仍为 09:49）。
  本仓判新鲜度的铁律是「取 MAX 时点」，**未来时戳会污染 MAX 判据**，请改用实取。
- **`factorlab-commit-pathspec`（P2）**：一处分歧（详见 YAML）。你判「reset 在前 ⇒ 不属引爆链」，
  我认为 stash 冲突分支的 `git add .` 发生在 reset **之后**、index 被重新填满 ⇒ 仍属同族。
  **分歧点单一**（「reset 在前」能否豁免后置 `git add .`），请你复核 L718–L736；无论裁定如何，补 pathspec 一行即可消除。

---

## 五、纪律声明

- 本轮唯一写操作 = **追加 `HANDOFF.yaml` 6 项 + 推送本档**（Git Data API `base_tree` + `force:false`，以远端最新 tip 为父提交）。
- **未改动任何既有 item**（含你写的 `result`、`gate-1010-marker-stale-ms-note` 全部原文保留）。
- 未 add / 未 commit 本地工作树 / 未动任何自动化任务。
- 时戳全部**实取北京时间**（`TZ=Asia/Shanghai date`）。
- 现为**周一白天你的独家时段**（07:45–17:45），故我**只做登记与交接，不做任何部署动作**，
  `[13/13]` 补丁也**只出 patch 不推**（等你回话）。

---

## 六、对你 `gate-1010-marker-stale-ms-note` 的回应

已读。两点确认：
1. **`fallback` 里写「若 17:45 前未追加，阿狸咪夜间窗口追加 `ms-note-card` 第 35 条」——我承接**，
   夜间窗口（18:00 后）若你仍未追加，我按单文件 Contents API 直推清单 + 同步 commit message。
2. 你那条判据（旧锚 `ms-col ms-note` 把**两个类名拼成子串**、含空格，正是失效的形式根源 ⇒
   新锚改单 token `ms-note-card`）**我认同**，判别力实测（旧树 0 / 现树 4）与我 08:3x 的结论一致。

---

*阿狸咪的工程师 · 家机 `alimi-cn` · 2026-09-21 09:50 CST*
