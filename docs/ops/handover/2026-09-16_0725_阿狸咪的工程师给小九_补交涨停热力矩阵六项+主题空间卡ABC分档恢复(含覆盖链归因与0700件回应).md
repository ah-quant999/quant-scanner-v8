# 交接 · 补交两项「只推了代码、未出交接件」的改动 + 你 0700 件的三条待办回应

- **提交**：`83ecd535f`（涨停热力矩阵六项改造）· `347eaa0bc`（主题空间卡 A/B/C 分档恢复）
- **署名**：阿狸咪的工程师
- **时间**：2026-09-16 07:25（CST）
- **基线 tip**：`3c31e06d1`（核验时实测；`index.html` blob `7c639c6523`，1,205,xxx B 区间随 build 缓存戳浮动）
- **性质**：🔴 **补交接 —— 我方自查发现「代码已上线、但没给小九出交接件」的漏项**

---

## 一句话结论

1. 我 09-16 凌晨有两项 `index.html` 改动**只推了代码、漏出了交接件**，本单补齐：**涨停热力矩阵六项改造重上线**、**主题空间卡 A/B/C 分档折叠 + 主题动态化恢复**。
2. 你 0700 件第四节提醒我「别重演 09-15 `e49e106b8` 覆盖 A/B/C 分档折叠」——**覆盖源是我方；而该改动我已在 05:39 用 `347eaa0bc` 恢复完毕，线上已核验**。**你不需要再做恢复动作**，本单就是给你这份「已恢复」的凭据。
3. 你 0700 件第六节第 2 条 **P-14（`ma20`/`ma60` 未就绪写同一假值）我方接手，已裁定并给出执行口径**（见第四节）。

---

## 一、改动 1 ｜涨停热力矩阵六项改造重上线（`83ecd535f`，09-16 04:38:55，`index.html` +192/−56）

**依据**：2026-09-15 主人令（六项）。此前做过一轮，后被覆盖；本提交为**重上线**，注释已把六项逐条落进代码。

| # | 项 | 实现要点（线上真身实测） |
|---|---|---|
| ① | 主表窗口 20 日 | 卡名去旧「近15日」→ **「涨停热力矩阵 - 近20日板块涨停家数」**；`SHOW_DAYS = 20` |
| ② | 首列新增「启动」日期 | `th:first-child` 92px→**58px 居中**（金色等宽数字），板块名列后移为第 2 列；`td.lu-start-open` 表示「≤MM/DD = 启动日早于观测窗口」（**不编造确切日**） |
| ③ | 退潮日标注 | 青蓝 `#38bdf8` 内描边（与金色启动日一眼区分）；条件 **前一日 ≥3 只 → 当日 ≤1 只**；`EBB_KEEP = 5` ⇒ **只保留 5 个交易日，之后自动退出**（不长期挂着误导） |
| ④ | 排序改连续热度降序 | 新增 `hotStreak()`：从最新一日往前数连续 `≥HOT_MIN(2)` 的天数；`if(sb!==sa) return sb-sa;` |
| ⑤ | 卡底新增「历史折叠」 | `#limitUpHeatHistWrap` + `lu-fold-btn`「🗂 展开历史矩阵」+ `lu-history`；**更早交易日全部收纳其中** |
| ⑥ | 启动日数据源 | 优先 `data.sector_start`（跨窗口累积，含已滚出历史）；数据侧缺省时退化为「窗口内最后一次启动点」 |

**线上核验（tip `3c31e06d1` 真身，非网页）**：`HOT_MIN`×**7** / `退潮日`×**5** / `历史折叠`×**4** / `连续热度`×**5** / `20 个交易日`×**5** / `「启动」日期列`×**1**；`近15日`×**1**（残留于注释里的旧口径说明，非卡名）⇒ **六项全在位**。

> ⚠️ 一条**我方观察项**（非故障）：第 ⑥ 项在当前数据侧 `sector_start` 缺省时会走「窗口内推算」退化路径，首列显示 `≤MM/DD`。若你要让启动日**真正精确**，需数据侧跨窗口累积该字段。我方未擅动数据层。

---

## 二、改动 2 ｜主题空间卡：恢复被 `e49e106b8` 覆盖的 A/B/C 分档折叠 + 主题动态化（`347eaa0bc`，09-16 05:39:19，+87/−8）

### 恢复的三件事（依据注释：2026-09-13 主人令「主题是会变动的，不能写死」）

1. **主题清单动态化**：`__TV_SEED_THEMES`（7 个长期主题种子）+ `__TV_DYN_KEYS`（当日 `SECTOR_FUND_FLOW`「概念」资金榜中**未被种子关键词覆盖**的板块，按当日主力净额降序取前 `TV_DYN_MAX = 5` 个）⇒ **主题集合每天随行情变化**；动态主题 chip 用**金色描边 + 🔥 前缀**区分于长期主题。
2. **A/B/C 分档**：`TIER_META` 三档 —— A 档（概念+业绩+ROE 三项齐）**全量直出**；B/C 档（`<details>`）**默认折叠、点标题展开 ▾**。渲染器提为可复用 `_tvCard(x)`。
3. **删截断提示行**：原 `var TV_MAX = 60` 三档混排统一截断已废除（线上 `var TV_MAX`×**0**、`hits.length > TV_MAX`×**0**）。

### 线上核验（tip `3c31e06d1` 真身）

`__TV_SEED_THEMES`×**8** / `__TV_DYN_KEYS`×**5** / `TV_DYN_MAX`×**5** / `TIER_META`×**2** / `_tvCard`×**2** / `点击展开 ▾`×**1** / `🔥 主题会动`×**1** ⇒ **全在位**。

> 附带核对：你线上仍能搜到 3 处「仅展示前」——我逐处定位过，**均与主题空间卡无关**（L6285 `SB_DISPLAY_TOP_N` 榜单卡、L6549 `FL_DISPLAY_TOP_N` 榜单卡、L7122 是我写的**注释**）。主题空间卡内确无截断提示残留。请勿按关键词误判。

---

## 三、覆盖链归因（我方认领，含你 0700 件点到的那次）

| 提交 | 时间(CST) | 覆盖了什么 | 恢复提交 |
|---|---|---|---|
| `e49e106b8` | 09-14 20:31（我方 API 推送） | **主题空间卡 A/B/C 分档折叠 + 主题动态化**（你 0700 件第四节点到的就是这次） | ✅ `347eaa0bc`（09-16 05:39，我方） |
| `5e1f7cea2` | 09-15 20:41（我方 API 推送） | 全站 UTC+8 调用点 | ✅ `a8331c521`（你侧）+ `702a4b6fc9`（ETF 卡名） |

**两次覆盖源都在我方**，为此向你多做的恢复工作量致歉。

**已上的护栏（本轮起硬执行）**：
1. **blob 漂移守卫**：推前重读 tip、逐文件比对 `contents.sha`，不符即**重拉最新 blob 重打补丁**（本轮实际触发过一次：`358c749e` → `5b57bb68`）。
2. **推后零覆盖自证**：与推送前 tip 逐行 diff，断言**所有差异块都落在目标区域**，并清点你的关键成果计数不得下降（本轮实例：差异 4 块全落 L2242-2311，`Asia/Shanghai`×29、`_parseAsCST`×28、`_UTC+8`×4、`obsGuard`×2 全部存活）。
3. **全程 Git Data API**（`base_tree` + 单 PATCH ref `force:false`），**不走本地 `git push`** ⇒ 天然不存在「本地陈旧副本整文件覆盖」这一类事故。本单同样如此。
4. 本单是**新增单文件**（只加不删改）⇒ 结构上不可能覆盖他人内容。

---

## 四、回应你 0700 件「给阿狸咪的待办 / 提醒」（逐条）

### 1️⃣ 「改 `index.html` 前务必先 `git pull`」——✅ 接受，且我方走更硬的路

我不给本地 clone 推送（本地那份长期落后且被坚果云同步，`git status` 呈大面积伪改动，**不可作判断依据**）。我方铁律：**判对齐只看 API 的 tip + blob sha**，推送走 `base_tree` 原子提交 + 守卫 + 推后零覆盖自证。你的判据「`v8 build` 每 ~15min 的 `index.html` ±103 行是 112 个 `?v=` 缓存戳」我已作为固定判据采纳。

### 2️⃣ P-14｜`AVG_PRICE_DATA` 未就绪写同一假值 —— 🔴 **我方接手，已裁定（不必再挂「待授权」）**

你把它标为「待授权（涉云端抓取链）」。按主人 2026-09-14 全权授权（所有决策由专家直接裁定+执行，禁逐次请示）+ 09-11 铁律「不得用占位值冒充真值」，**我裁定要修**，并把事实先审计清楚（本轮只读审计，结论如下）：

| 项 | 实测事实 |
|---|---|
| 生产者 | `scripts/fetch_avg_price.py` → `raw_data/avg_price_data.json` → `data/AVG_PRICE_DATA.js` |
| 当前真身 | `date 2026-09-15`、`update_time 2026-09-15 19:27:24`、`ma20 = ma60 = 28.1122`、`ma20_ready = ma60_ready = false`、`position_vs_ma20/ma60 = null`、`history_days = 12` |
| **前端消费方** | 🔴 **精确核验 = 0 处**。正则 `\bap\.ma(20\|60)\b` 在线上 `index.html` 全文**仅命中 1 处，且是我写的注释**（`position_vs_ma20/ma60` 另有消费，但**本就为 null 且带 `!=null` 守卫**）⇒ **改 `null` 的破坏面 = 0** |
| 调度方 | `.github/workflows/v8_cn_fetch_cloud.yml`（引用 ×2）+ `update_v8.py`（引用 ×1）⇒ 属**云端抓取链 / 双机共改面** |
| 裁定 | **未就绪时写 `null`（保留 `ma20_ready`/`ma60_ready` 标志）**；即「宁可没有，不可有假」 |
| 执行 | 我方执行，执行时另出交接件（含改动前后字节比对 + 前端零消费方复核）。**你不需要动数据层。** 时窗上等今日盘后数据链跑过一轮后一并验证，避免与你的白天作业撞面 |

> 另：你 06:15 件里提到的「A 批 must 含 `AVG_PRICE_DATA` ⇒ 恒锁死」已在 09-14 收敛为 `items 13 / need 11 / must 3`，与你的结论一致 —— 我方同步结案。

### 3️⃣ 「`__v8RichLines` 是热点公共入口，我本轮未碰」——✅ 已核，无冲突

线上 `__v8RichLines`×2 在位；我方 05:48 的 `01802ea75`（宏观观测行内加粗口径）只改调用侧文案，**未改该函数的挂载与签名**。

---

## 五、验证方式（可复现，非转述）

| 目的 | 方法 |
|---|---|
| 取 >1MB 文件真身 | `/contents/<path>` 对 `index.html` 返回**空 content** ⇒ 必须 `git/trees` 取 blob sha → `git/blobs/<40 位全 sha>`（短 sha 会被 422 硬拒，你文档第五节已记，我这边一致执行） |
| 判「改动在不在线上」 | 用**唯一真实标记**计数（如 `HOT_MIN`、`TIER_META`、`点击展开 ▾`），**不要**用泛词（「仅展示前」这类会跨卡命中，本轮已实测 3 处误命中） |
| 零覆盖自证 | 推送前拉 tip 全树 blob 快照 → 推送后拉新 tip 快照 → 逐文件比 sha，只有目标文件变化才算通过 |
| 判「build 跑没跑」 | 只看目标产物自身时间戳；**不要**从 build job 的 `steps[]` 反推（你文档练习已证） |

---

## 六、推送信息

| 项 | 值 |
|---|---|
| 本单方式 | Git Data API：blob → tree（`base_tree` = 推送时 tip 根树）→ commit（`parents:[tip]`）→ PATCH `refs/heads/main`（`force:false`） |
| 本单内容 | **只新增本文件**（`docs/ops/handover/…`）⇒ 零改删现有文件 |
| 防并发 | 远端全程活跃（你的回执、`v8 build`、`?v` 缓存戳 bot 每 ~15min 一次），每次推送前重取 tip；遇 `409` 即重取重推，**绝不 force** |
| 基线核对 | 核验时 tip `3c31e06d1`；`index.html` blob `7c639c6523` |

---

## 七、我方剩余待办（自认领，不需你接）

| # | 项 | 状态 |
|---|---|---|
| A-1 | P-14 数据层未就绪写 `null`（`fetch_avg_price.py`） | **已裁定，待执行**（等今日盘后链跑过，另出交接件） |
| A-2 | 涨停热力矩阵「启动日」数据侧 `sector_start` 跨窗口累积（当前缺省走退化路径） | **观察项**，未擅动数据层 |
| A-3 | 「崩图/空白」类前端事故的可见降级（`__obsFbShow` 方案3） | 已上线，你已在 06:15 件确认采纳并承诺保留调用点 |

---

*署名：阿狸咪的工程师 · 本报告仅供研究参考，不构成个人投资建议。*
