# 强势跟踪卡「假新鲜」根因交接 — 源笔记停更 15 天，页面零提示

- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-17 21:15（北京时间）
- **触发**：主人 20:41 截图质疑「这个你确定没有有读到？都没变化啊！」
- **结论一句话**：**不是抓取坏了，是源头死了。** v8 每天忠实抓一份 15 天前就静止的笔记，
  前端却因 `update_time`（抓取时刻）显示「更新于 今日 15:53 盘后」= **假新鲜**。

---

## 1. 铁证链（每条都可复现）

### ① 源笔记自己写着它的日期

真浏览器（agent-browser）打开源分享页 `ima.qq.com/note/share/_A0YNbqJ8AmbI5kRZ1ZmMQ`：

```
document.title = "强势股跟踪日报 2026-09-02"
页面正文：创建人：梧谷枫灯
          更新时间：2026.09.02 15:41
表格每行「最新交易日」列 = 2026-09-02（全表一致）
```

⇒ 源是 **2026-09-02 那一期**，距今 **15 天**。

### ② 产物价格指纹：20 次抓取一字未改

`raw_data/ima_strong_stock.json` 全量可追溯版本（`prices = (code, base_price, latest_price, change_pct)` 的 sha256 前 12 位）：

```
09-02 22:00 → 价格指纹 28b747bcaa1b   （09-02 那期，抓取时刻 09-02 15:41）
09-03 17:57 → 价格指纹 207238cb37e4   ← 此后**再未变化**
   …… 中间 09-04 / 09-07 / 09-08 / 09-09 / 09-10 / 09-11 / 09-14 / 09-15 / 09-16
      共 20 次抓取，价格指纹全部 = 207238cb37e4
09-17 15:53 → 价格指纹 9975f0b90650   ← 变的是**解析器修复**（见 §3），不是新数据
```

### ③ 卡的「最新价」整列都不是行情

拿本机 baostock 真实日K逐只比对（复权口径 adjustflag=3）：

| 判据 | 结果 |
|---|---|
| `最新价` == 当日（09-17）真收盘 | **0 / 112** |
| `最新价` == 09-16 真收盘 | **0 / 112** |
| 精确吻合日分布 | 09-02 有 6 只、09-01 有 1 只、08-28 有 1 只，其余「全段无吻合」 |
| 例：哈药股份 600664 | 卡里 9.26；真实近 5 日 6.99 / 7.30 / 7.12 / 7.36 / **7.42** |
| 例：一鸣食品 605179 | 卡里 31.20 = **09-02 真收盘**（精确吻合） |

⇒ 整列对应的是 **09-02 那天**的盘中/收盘快照。

### ④ 前端把「抓取时刻」当成了「数据时刻」

`index.html` 强势跟踪卡（`__renderImaStrongStock`）第 6692 行：

```js
if(typeof _uBadge === 'function') html += _uBadge('auto','短线',(D.update_time||''));
```

`D.update_time` 是**抓取时刻**，于是胶囊显示「🦊 更新于 今日 15:53 · 盘后 · 短线」，
而抓到的内容停在 09-02。**每行的 `trade_date`（最新交易日）根本没被用上。**

### ⑤ 全站陈旧告警 09-14 已被删除 ⇒ 无法自救

`index.html` 第 ~1090 行原文：

```
// 🛡 2026-09-14 主人令（一劳永逸）：原「陈旧红胶囊」分支**整段删除**。
//   ── 为何删：本仓数据以**日频**为主，「今日批次尚未产出」是正常中间态而非故障，
//      按 SLA 判停更必然误报（主人 09-14 原话：「你这本来就是日频的」）。
```

⇒ 该删除**当时是对的**（SLA 判日频必误报）。但它同时让「源真的死了」这类事故
变得**不可见**。所以本次修复用的是**内容级判据**（源自己标注的数据日），不是 SLA 猜测。

---

## 2. 责任归属：这不是抓取端的错

抓取链本身是健康的：

| 项 | 事实 |
|---|---|
| 唯一入口 | `.github/workflows/v8_ima_strong_stock.yml`（全仓只有它 `fetch_ima_strong_stock.py`） |
| 调度 | `cron: 45 7 * * 1-5` = **北京时间 15:45**，周一至周五 |
| 执行机 | `runs-on: ubuntu-latest`（云端托管，非自托管） |
| 每日提交 | 09-14 15:52 / 09-15 15:52 / 09-16 15:53 / 09-17 15:53 —— **天天都跑了** |
| URL | `DEFAULT_URL` 写死在脚本第 29 行，指向 09-02 那一期 |

⇒ 抓取**成功**、提交**成功**、数据**陈旧**。三者同时成立，这才是「假成功」的完整形状。

---

## 3. 顺带自曝：解析器在 09-17 修好后仍有残留

- 09-17 15:53 的产物首次带 `parse_health`（= 我方新解析器上线），`no_first_selected = 72 / 112`。
- 09-17 那 443 处字段变化里，`industry=72 / latest_price=70 / buy_point=95` —— 正是「串位」那批
  被重新对齐。**方向是对的，但仍未归零**。
- 源头结构：源页行序为 `代码 名称 行业 首次入选 基准价 最新价 涨幅% 回撤% 连涨 状态 买点提示 最新交易日`（12 列）。
  **空单元格在内层 `innerText` 里会被塌缩掉**，导致 `buy_point`（`kind='text'`，**无条件接受任何值**）
  吃掉 `最新交易日` ⇒ 两者同时为空。例：600664 哈药股份 `trade_date=''`、`buy_point=''`。
- **建议**：给 `buy_point` 加形状校验（不得是 `^\d{4}-\d{2}-\d{2}$`），或改用 `extract_via_table`
  的 DOM 路径优先（DOM 列序不会塌缩）。**我不单方面改，等你意见。**

---

## 4. 我方补丁（**已写好、已过门禁、未推**）

两个文件，5 处锚点替换，`py_compile` 零错 + 内联 `<script>` 28 块 **0 语法错**：

### A. `fetch_ima_strong_stock.py`（+3,862 B）

1. 新增 `parse_note_meta()`：从源页正文提取 `source_title` / `source_updated_at`。
2. `fetch()` 回传 `meta`。
3. `main()` 计算 **`data_date`**（取每行「最新交易日」最大值；全缺则回退源自述日期）、
   **`stale_days`**、**`source_stale`**，落进产物。
4. 显式告警打印：`⛔ 源停更告警：……已 N 天无新数据。本次抓取成功但内容与上一期相同
   （源侧停更，不是抓取失败）。`

**语义红线**：`update_time` 仍是抓取时刻（**不改**，避免破坏管线级审计）；
新增的是**源自身的时间刻度**。

### B. `index.html`（+1,582 B，**纯追加，不改任何既有行**）

在强势跟踪卡 `_uBadge(...)` 之后追加一枚**内容级**红胶囊，仅当 `D.source_stale` 为真时出现：

```
⚠️ 源停更 15 天 · 数据日 2026-09-02
```

悬停提示带全链路：源笔记名 / 源自述更新时间 / 最新交易日 / 距今天数 / 「源侧停更，非抓取失败」。

**为什么这次不会重蹈 09-14 删掉的误报**：判据是**源自己在页面上写明的日期**，
不是 SLA 猜测。源没更新 ⇒ 才报；日频「今日批次尚未产出」⇒ 不动。

---

## 5. 为什么今晚没推（请小九确认后接续或等我）

21:10 干跑漂移守卫，**实测拦截**：

```
当前 tip ac0b3af5 (09-17 21:01:07)
✅ 漂移守卫 fetch_ima_strong_stock.py 与基线逐字节一致（15822 B）
⛔ 漂移守卫拦截：index.html 远端已变（基线 1272067 B / 远端 1272067 B）
```

**size 完全相同、字节不同** —— diff 216 行全是 `?v=1789649734 → 1789650067` 与
`BUILD = "d720ddd3c" → "648e9e427"`，即缓存戳轮换。

⇒ `index.html` 正处于 **~10 分钟一次**的轮换中（`update_v8.py --only-cache-busters`，
`v8_cn_fetch` / `v8_cache_buster_reconcile` / `v8_build_deploy` 三处共用）。
此刻推我方 index.html 补丁有两个真实风险：**① 与非快进撞车 ② 被一个更早取基底的
长跑 job 回写覆盖（跨 actor 丢失更新）**。故按铁律「远端活跃期只读不写」**不插队**。

**推送脚本已就绪**：`_imatrack_push.py`（Git Data API `base_tree` + `force:false` +
漂移守卫 + 422 自动重读重试 + 推后回读逐字节自证）。

**时机**：等本轮 CI 链静默（约 22:30 后）或主人点名，**一次推两个文件 + 本交接件**。

---

## 6. 需要你回执的 3 项

1. **源 URL 谁有最新的？** 源笔记标题带日期（「强势股跟踪日报 **2026-09-02**」），
   极可能是**作者每日新发一期**、`DEFAULT_URL` 被写死锁在 09-02 那期。
   若你或主人手上有 09-17 的分享链接 → 我立刻换上去（这是治本）。
   若作者确实停更 → 本卡应长期挂红胶囊或下架，**不能继续假装每日新鲜**。
2. **`index.html` 补丁由你合还是我推？** 你今晚在密集改 index.html，
   由你顺手合入可避免撞车；我推也行，但要等链路静默。
3. **`buy_point` 形状校验**（§3）改不改、怎么改。

---

## 7. 红线

- 不改 `update_time` 语义（仍是抓取时刻）。
- 不改选股/排序/任何既有字段。
- 不 `force` 推、不 `git add -A`、补丁与脚本全部放**仓库外**。
- 源停更属**源侧**事实，**不得**通过伪造/回填掩盖。

*本报告仅供研究参考，不构成个人投资建议。*
