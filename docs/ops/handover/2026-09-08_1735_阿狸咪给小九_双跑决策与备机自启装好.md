# 阿狸咪 → 小九 交接（2026-09-08 17:35）

> 主人已拍板：双跑接受，小九=主机(primary)，阿狸咪=备机(backup)。本交接同步此决策 + 备机自启进度 + 监控自动化。

## ① 双跑决策（主人 17:30 拍板）
- **接受 active-active 双跑**，启用双机热备。
- **角色分工**：小九单位机（中国 IP、稳定）= **主机/primary**；阿狸咪家里机（网络不稳）= **备机/backup**。
- **技术现实（务必知悉）**：GHA 同 `cn` label 是 active-active，无主备优先级。小九稳定会自然多接任务；若某次活落到阿狸咪且因网络抖动失败，GHA **不会**自动改派小九，需你在小九侧手动重跑该 job。
- **给你的 action**：把 IP 敏感 workflow（`v8_algo_cloud`、`v8_stock_quote_refresh` 等）的 `runs-on` 从 `ubuntu-latest` 改回 `cn`，启用双机热备。label 配置你定（建议小九 `cn`、阿狸咪 `cn` 同 label 即可）。

## ② 阿狸咪备机 runner 自启（已装好，17:23 实测全绿）
- 安装目录 `D:\actions\cn-runner`，二进制 `bin\Runner.Listener.exe`。
- **计划任务** `v8_cn_runner_guard`（ONLOGON 触发器，用户登录后自动启）→ State=Running ✅
- **守护** `D:\actions\cn-runner\_guard_loop.ps1`：每 30s 巡检 `Runner.Listener`，没了拉起 `run.cmd`（60s 冷却）；日志 `_diag\guard.log`。
- **电源**：已设「高性能」+ 关休眠（`powercfg /hibernate off`）。
- ⏳ **待主人跑一次**（已就绪，非管理员脚本会拒绝）：`D:\actions\cn-runner\install_runner_autostart_admin.ps1` 同目录的 `disable_s3_sleep_admin.ps1` —— 关 S3 待机（否则空闲 30min 机器睡，runner 不响应）。

## ③ 新增 3 个 WorkBuddy 自动化（监控阿狸咪备机 runner）
| 名称 | 频率 | 触发 |
|---|---|---|
| 阿狸咪-备机runner巡检(盘中每小时) | 交易日 9-15 点每小时 | `FREQ=HOURLY;INTERVAL=1;BYDAY=MO-FR;BYHOUR=9..15` |
| 阿狸咪-备机runner巡检(非盘中每2h) | 交易日 7-8、16-21 每2h | `FREQ=HOURLY;INTERVAL=2;BYDAY=MO-FR;BYHOUR=7,8,16..21` |
| 阿狸咪-备机runner巡检(周末每2h) | 周末 8-20 每2h | `FREQ=HOURLY;INTERVAL=2;BYDAY=SA,SU;BYHOUR=8,10,12,14,16,18,20` |

- 逻辑：本机 `Get-Process Runner.Listener`；在线→静默；离线→①后台自启 `run.cmd`/`schtasks /Run v8_cn_runner_guard` ②**邮件告警 2814546@qq.com**「备机 runner 离线，请远程登录重启」③agent-mail 不可用则写 `backup/runner_alert.log`。
- 夜间 22:00-07:00 不跑（主人要求）。
- 注意：整机关机时本任务无法跑（预期内，小九仍为主机兜底）。

## ④ 时间线（备查）
- 16:18 有人手动启动 runner（PID 12408，谁启的查不到）。
- 17:05 该进程消失（无兜底）→ 暴露"手动起不可靠"。
- 17:23 主人跑 `install_runner_autostart_admin.ps1` → 自启+守护装好，新 runner PID 11732 起，守护 PID 3220 在跑。
- 17:35 双跑决策 + 监控自动化建好。

## ⑤ 不在我执行范围（你自管）
- qs8-tmp 本地脏树清理（你本地 HEAD 落后 2077，推会回滚线上，勿碰）。
- S3 关待机脚本需你本机管理员跑（沙箱无权限）。
