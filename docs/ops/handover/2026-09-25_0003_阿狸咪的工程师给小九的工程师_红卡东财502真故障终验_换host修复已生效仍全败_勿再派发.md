# 🔴 HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-25 00:03｜红卡东财502真故障终验：换host修复已生效仍全败 · 勿再派发

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 挂账唯一状态源：`docs/ops/HANDOFF.yaml` → 本件对应 item `redcard-hostfail-rootfix-0924`（**未改状态，仍 pending-verify**）

---

## 一句话结论

**两红卡（EXPERIMENT / SECTOR_LEADERS）停更 53.3h 的根因已完成「铁证级终验」= 东财 push2 全域 502 真故障（非我方管线/非执行序/非自愈窗口）**：小九与阿狸咪已合的「换 host 重试」修复（`1052d4334162` / `_EM_HOSTS`）**经实测确实在运行**，但 14 个 host 全部 502 ⇒ **属上游真故障，不可自愈**。按任务书第 3 条「已用上新代码仍失败 ⇒ 属东财端真故障，出交接件记 HANDOFF，**不反复派发**」处置。**建议：停止对这两张卡的自动重派，等待东财侧恢复后自然收敛。**

---

## 一、终验证据（全部取自远端远端真身，非本机 clone）

### 1.1 修复已确实部署并在运行（先排除「跑的是旧代码」）

| 判据 | 实测值 |
|---|---|
| 修复提交 | `1052d4334162` @ **09-24 21:44:23 CST**「SECTOR_LEADERS 换 host 重试」 |
| 变更文件 | `scripts/fetch_sector_leaders.py` `+41 -5` |
| run head 是修复的后代？ | cn_fetch **#2177/2178/2179** 全部 `compare 1052d4334162...head` = **ahead / behind=0** ✅ |
| 修复代码在位？ | 远端 `scripts/fetch_sector_leaders.py` L57 `EM_HOSTS = (push2delay, push2, 1..12.push2)`（**14 host**）、L72 `_get_json(..., hosts=)` 换 host 重试、L166/L187 两处 `em_clist` 调用点已接入 ✅ |

### 1.2 换 host 重试**确实执行了**，但 14 host 全败（决定性铁证）

`v8_cn_fetch_cloud` **#2178**（head `0683a33600`，作业 id `107697441096`）job 日志逐字：

```
⚠️ 东财 push2 抖动(push2delay.eastmoney.com) 尝试1/3: HTTP Error 502: Bad Gateway → 0.6s 后换 host 重试
⚠️ 东财 push2 抖动(push2.eastmoney.com)        尝试2/3: HTTP Error 502: Bad Gateway → 1.1s 后换 host 重试
（第 3 次同样 502，重试耗尽）
```

同档另有旁证（说明是**东财整体抖**，非单接口）：

```
⚠️ 301718 / 301660 / 113710 行情获取失败: HTTP Error 502: Bad Gateway
⚠️ W52_HIGH 抖动 尝试1/3: ValueError: 非 JSON 响应（559B，前 80 字：'<html>…<title>502 Bad Gateway')
❌ eastmoney push2 失败: HTTP Error 502: Bad Gateway — 不写盘，保留现有 raw_data/etf_*.json
```

### 1.3 两张卡各自的失败形态（同源、可复现）

- **EXPERIMENT**（`cloud_fetch_v8.py::f_experiment`）：`##[error]EXPERIMENT 抓取返回空(东财 push2delay em_clist 未返回任何记录)——本次不写盘…` → `⚠️ EXPERIMENT: 返回空，跳过`。
  ⇒ **09-22 修的可见性护栏按设计生效**（不再是静默），失败已显式标红。
- **SECTOR_LEADERS**（`scripts/fetch_sector_leaders.py`）：`build()` → `fetch_em_boards()` → `_get_json()` L94/L104 抛 `HTTPError: 502` → 脚本 `exit 1`。
  ⇒ 但该 step 的 **Actions 结论仍显示 `success`**（因 `soft_13b` 是 `continue-on-error: true` + 内部 `exit 1` 被吞）——**「step 绿 ≠ 产物新」的第 N 次实证**，判健康请一律读**产物 `update_time`**。

### 1.4 自愈已触发但无效（口径打架，非本项核心）

`v8_health_check` 同档实测：

```
[FAIL] 全量数据/EXPERIMENT: 更新于 2天前 18:13；超过通用红线 1440 分钟
[FAIL] 全量数据/SECTOR_LEADERS: 更新于 2天前 18:15；超过通用红线 1440 分钟
[HEAL✓] [post_close] EXPERIMENT, SECTOR_LEADERS: 已自动派发刷新 (已派发 cn_fetch category=post_close (HTTP 204))
[HEAL✓] …近 25 分钟内已派发，跳过重复
```

⇒ 自愈**派发成功**（HTTP 204），但重派的 run 跑到同一脚本仍打同一个已 502 的东财 ⇒ **派发再多次也不产出**。这正是任务书第 3 条要「不再反复派发」的情形。

### 1.5 产物现状（本轮实测，均为远端 contents API 真身）

| 产物 | blob | update_time | age | 判定 |
|---|---|---|---|---|
| `raw_data/experiment.json` | `ce3a07ffee6c` | 2026-09-22 18:13:35 | **53.3h** | 🔴 超阈 2.2× |
| `raw_data/sector_leaders.json` | `efd9983a732f` | 2026-09-22 18:15:13 | **53.3h** | 🔴 超阈 2.2× |

内容**非空**（EXPERIMENT 五名单各 15/15/15/15/7 只；SECTOR_LEADERS `sector_count=65 / leaders_fail_count=0`）⇒ 属「**停更**」而非「洗空」，无需数据修复、**禁伪造补漏**。

---

## 二、对本项 status 的建议（**未擅自改 YAML**，请小九裁决）

`HANDOFF.yaml::redcard-hostfail-rootfix-0924` 现为 `pending-verify`，其 `not_done_note` 已写「尚未跑出一轮成功产物 ⇒ 禁宣称已修复」——**本轮结论与之一致，故状态不动**。但建议增补两条口径：

1. **「换 host 修复」的验证判据已达成**：修复确已部署 + 确在运行（§1.1/§1.2）。剩余 0/，卡在**上游真故障**，不再是我方待验证项 ⇒ 建议把 `verify_how` 第 1 条（「确认日志出现换 host 重试且不再 502 退出」）标注为**已验证（换 host 已出现；502 仍在，属外部）**。
2. **建议增设处置口径**：「东财全域 502 期间，对 `EXPERIMENT`/`SECTOR_LEADERS` 的自动重派**降频/暂停**（避免每轮 cn_fetch 白烧 30+min 预算与 runner 队列），恢复判据 = 东财接口恢复后首轮自然产出」。是否落地请小九拍板。

---

## 三、本轮其余巡检结论（一并知会，无需处置）

| 项 | 结论 |
|---|---|
| 主站可达 | ✅ HTTP 200 / **1,361,458 B** / 与仓内 `index.html` blob `557c1c027a02` **逐字节一致**（`</html>`×1、19,315 行） |
| 五链近档 | algo 12 档 11 success + 1 cancelled；cn_fetch 5 pending + 1 in_progress（**积压 5 档 = 超任务书「≥2 pending 只报不派」阈值**，本轮**只报告未派发**）；build_deploy 2 failure = **部署非快进竞态**（`ahead 38/42, behind 0`，同族非数据事故）；health_patrol 12/12 s；risk_gauge 12/12 s |
| 其余关键产物 | 全部今日：FINAL_RECOMMEND 19:34 / TRIPLE 19:16 / FOUR_VOLUME 21:55 / CRDS_CARD 19:19 / BACKTEST_COMPREHENSIVE 21:44 / CRDS_BACKTEST 21:54 / FACTOR_LAB 18:16 / LHB 23:23 / SECTOR_RS 23:25 / STOCK_MOMENTUM_STATE 23:54 — **均在阈内** |
| HEALTH_CHECK 面板 | `{ok:115, warn:0, fail:3, limited:3, total:121}`；3 fail = `DAJIJIN_REDUCTION`(09-23 19:43) + 本件两卡。**DAJIJIN 属在册项**（自愈窗口盲区，见 `postclose-reliability-trio`），本轮不重复开工 |
| 小九 runner | `lemoncat-cn status=offline`（`alimi-cn online`）⇒ 小九机下线，`[self-hosted,cn]` 任务由 alimi-cn 串行承接（pending 积压 5 档属「谁空谁跑」正常现象，**非停摆、无需人工补派**） |

---

## 四、给下一轮 / 小九的最小行动项

- [ ] **不要再派发** EXPERIMENT / SECTOR_LEADERS 的补跑（东财 502 期间无意义；派发只会增加堆积）。
- [ ] 东财恢复后**首轮 cn_fetch 自然跑成**即写盘 ⇒ 届时核对 `raw_data/experiment.json` 与 `sector_leaders.json` 的 `update_time` 前进到 2026-09-25+，即可把本项 `pending-verify → done`。
- [ ] 若希望**主动探测**东财恢复（而非等 30min 一轮）:可用 `probe_em.yml`（仓库已有）——**该探测不产出数据、不占 post_close 名额**，建议作为唯一探测手段。是否启用请小九拍板（我方本轮**未派发任何 workflow**）。
- [ ] `DAJIJIN_REDUCTION` 的自愈窗口盲区仍待裁决（属 `postclose-reliability-trio`，非本件）。

---

**署名**：阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-25 00:03 CST
**本轮写操作**：仅本交接件 + `HANDOFF.yaml` 追加（均走 Git Data API、以远端为基底、推后逐字节回读）。**未派发任何 workflow、未改任何数据/代码。**
