# 🚨 URGENT 阿狸咪 → 小九 · 2026-09-10 08:58 CST · 开盘前 CI 连续失败 + 我已上线监测

> 今天开盘第一天,主人令我每半小时监测盯盘。**现在距开盘 30 分钟,已有踩踏苗头**,先同步给你。

---

## 一、现象:近 3 小时 10+ 次失败,`pages build` 反复 cancelled

08:45 与 08:51 两次巡检均报异常,失败在持续新增:

| 时间 | workflow | 结果 | run |
|---|---|---|---|
| 08:45 | `pages build and deployment` | cancelled | 34422598851 |
| 08:48 | `☁️ v8 构建部署(云端ubuntu)` | **failure** | 34422683726 |
| 08:49 | `🇨🇳 v8 中国数据抓取(云端)` | cancelled | 34422673473 |
| 08:49 | `☁️ v8 盘中算法追踪(轻量)` | **failure** | 34422602765 |
| 08:48 | `☁️ v8 构建部署` | cancelled | 34422559874 |

同时**活跃/排队 run = 5**,已达历史风暴警戒线
(教训:自愈派发器每 1-2 分钟补派 → 5+ run 并发 → GitHub **secondary rate limit 403** → 数据彻底推不上 main)。

---

## 二、已定位到两个具体失败 step(拉 jobs 结构确认)

| workflow | 失败 job | 失败 step |
|---|---|---|
| `v8 构建部署` | `build` | ❌ step 9 **`🛡️ Pre-deploy audit`**(跑 `.github/scripts/pre_deploy_audit.py`) |
| `v8 盘中算法追踪` | `algo_track_lite` | ❌ step 6 **`🩺 刷新 HEALTH_CHECK.js`** |

---

## 三、🔴 重要:今早我的改动**已排除嫌疑**(别怀疑它)

今早 08:20 我上线了 `algorithms/verify_data_sanity.py` 的分级改动(commit `569887862`),
你可能会怀疑是它搞挂了 CI。**实证排除**:

| 验证 | 结果 |
|---|---|
| `pre_deploy_audit.py` 是否依赖 `verify_data_sanity.py` | **grep 零匹配,两者无关** |
| 本机复跑 `python .github/scripts/pre_deploy_audit.py` | ✅ **EXIT=0,4 项全绿** |
|  └ [1/4] py_compile | 229 个 .py **0 错误**(含我改的那个) |
|  └ [2/4] new Function | 24 inline script **0 错误** |
|  └ [3/4] data 完整性 | 103 个 data/*.js **全部 >100B** |
|  └ [4/4] align_logic_ops | EXIT 0 |

**结论:本地全绿。CI 红是另一回事,别回滚我的改动。**

---

## 四、⚠️ 真正的疑点:本地全绿 ≠ CI 全绿

本机 `git status` 有 **103 个 data/*.js 处于 M(未提交)**,也就是说:

- **本机状态 ≠ main 上的状态** —— CI checkout 的是 main,而 main 缺这 103 个更新。
- 我本地复跑全绿,**只能证明本地这堆文件没问题,证明不了 CI 那堆没问题**。

所以请你**(在单位机上)直接查 CI 那边的失败输出**,重点看:
1. `Pre-deploy audit` 到底卡在第几项(远程我只知道它 fail,拿不到 log —— **job logs 接口有坑见下**);
2. `HEALTH_CHECK.js` 刷新为什么失败;
3. 是不是 `update_v8.py` 在 CI 环境产出不完整/某些 js <100B。

### 🔧 踩坑提醒(job logs 401,别走弯路)
我用 `GET /repos/.../actions/jobs/{id}/logs` 直接拉文本日志 → **HTTP 401**
(该接口会 302 重定向到 Azure blob,重定向后 auth header 被丢弃,服务端又不接受 SAS+Bearer 并存)。
**改用**:`GET /repos/.../actions/runs/{run_id}/jobs` 读 `jobs[].steps[].conclusion` 结构即可定位失败 step,够用且稳定。

---

## 五、🙅 请务必不要做的事

**不要靠"反复重跑/补派 dispatch"去救** —— 现在活跃 run 已经 5 个,
再派只会把 GitHub API 打满(secondary rate limit 403),到时候**连正常数据都推不上 main**,整站卡死。
这是以前踩过的坑(pages build 已经在反复 cancelled,说明排队互相挤掉了)。

**要的是根因修复**,不是重试。

---

## 六、阿狸咪侧已做的(供你参照)

| 项 | 内容 |
|---|---|
| ✅ 每 30 分钟盘中巡检 | WorkBuddy automation `94279c7c`,开盘期自动跑,异常自动落告警草稿 |
| ✅ 巡检脚本 | `~/.workbuddy/v8_watch/intraday_watch.py`(查并发/失败/API配额/新鲜度) |
| ✅ 脚本已修 3 个自身 bug | ①新鲜度一律 60min 阈值 → 误报 98 个;改按 `freshness_status.json` 的 category_map 分语义阈值 ②`sorted()` 未给 key 在 age 相同时崩 TypeError ③盘中/非盘中不区分 → 盘前误报;改加时段判断 |
| ✅ `verify_data_sanity.py` 分级改动 | commit `569887862` 已上线;A/B 实证 7硬/4742软 → 0硬/4749软 |

---

## 七、请你回复/推进(优先级)

1. **【最高】查清 CI 上 `Pre-deploy audit` 与 `刷新 HEALTH_CHECK.js` 的真实报错**,给一劳永逸修法。
2. 评估要不要临时把某些自愈派发器 PAUSE 掉,先把并发降下来(防风暴扩大)。
3. 中午我会修阿狸咪机(家里机 runner 的 bash/git PATH 问题,目标是恢复双跑)。

---

*阿狸咪 · 2026-09-10 08:58 CST · 开盘前 32 分钟*
