# 交接文档 · 小九 → 阿狸咪（2026-09-04）

> 更新于 2026-09-04 12:05 自动更新（12:00 档首发）。
> 数据来源：当天工作日志（真身仓库 memory）+ origin/main 实时 git log + 线上 HEALTH_CHECK/RUNNER_STATUS 实测，全部 SHA 为实时核实，非记忆值。

---

## 一、背景

- 项目：九宝量化 v8（quant-scanner-v8），GitHub Pages 主站，CI 每 ~2 分钟自动推进 origin/main。
- 双机分工：小九白天独家主机（7:45–17:45，守卫/监督/数据兜底）；阿狸咪夜间（18:00–次日7:30）与周末接管部署。
- 本文档每 12:00 / 18:00 由小九自动更新，覆盖式重写，供阿狸咪开机后快速对齐当日状态。
- ⚠️ 路径铁律：`E:/workspace/quant-scanner-v8` 是坚果云软链空壳（无 .git）；**真身仓库 = `E:/v8data/qs8-tmp`**。git 操作一律进真身。

---

## 二、今日已完成改动（含真实 SHA）

| 时间 | 事项 | SHA / 结果 | 影响 |
|---|---|---|---|
| 08:39 | 盘前守卫初判 STALE:4（ipo_score/margin_data/analyst_ratings/macro_data） | 远端核对为真 FRESH（06:58–07:01，龄 99–102min < 阈值） | **假阳性**，零派发零推送；本地落后 ~1527min 纯本地滞后 |
| 09:51–10:22 | 盘中守卫 STALE:9，远端真陈旧（08:00 后未刷新） | 云端 cn_fetch 有活跃 run（09:34 起）→ 零派发跟随等待 | 云端 09:34/10:04 两轮自行补齐 → FRESH；10:19 CST 已上线（上证 3976.35 +0.87%） |
| 10:32 | **根治盘前东财 `'-'` 占位符崩溃**（主人 10:35 令「继续」授权） | `de27275a8` 已在 origin/main（ah-quant999，09/04 10:32） | `cloud_fetch_v8.py` 新增 `_em_clean_value/_em_clean_rows`，在 6 个东财 diff 入口统一归一 → 下游 40 处 `or 0` 解析点自动生效；08:52 崩溃 payload 复现验证通过 + 正常数据反向验证未误伤；四重护栏全过（结果树 2563==父、改动恰 1 文件） |
| 11:06 | 候选股池分板块取数修复（创业板 cyb/科创板 kcb/主板 sh_a+sz_a） | `f59fe3be3`（ah-quant999，11:06） | 根治主板挤占创业·科创名额导致观测候选股池黄标 |
| 11:48 | 选股生命周期「走弱桶+待确认剔除」改弹窗（主人令） | `3b1430378`（v8-cloud-bot，11:48） | UI 交互变更，已随 CI 上线 |
| 11:45 | **主人令：全部自动化模型切 GLM-5.3-Flash** | 本机 36/36 任务完成（单字段 UPDATE，只读复核一致，integrity_check=ok） | 小九侧已完成；**阿狸咪侧任务须她侧同步**（本机只管本机表） |

今日 origin/main 累计 **148 个 commit**（截至 12:05 实测）。

---

## 三、当前系统状态（12:05 实测）

| 项 | 状态 |
|---|---|
| 站点 HTTP | **200** ✅ |
| HEALTH_CHECK | **106 ok / 0 warn / 3 fail**（fail 项：因子实验室、四量终极回测、IMA_STRONG_STOCK）⚠️ |
| RUNNER_STATUS | **17/17 ok**（update_time 12:00:21）✅ |
| origin/main HEAD | `fc40d6e06`（09/04 12:01，STOCK_QUOTE 刷新，持续被 CI 推进） |
| 真身仓库 HEAD | `720171f13` 未动；ahead 0 / behind ~1002；staged 0 / UU 0 ✅ |
| 红线文件 | index.html / HEALTH_CHECK.js / PORTFOLIO.js 保持本地 ` M` 分叉（既有，未触碰） |
| 远端文件数 | ~2563（基线 934→2563，raw_data 1834 累积所致，非清库） |

剩余自动化（小九侧 36 任务已全切 glm-5.3-flash）：盘前 08:15 全量 / 盘中每 30 分守卫 / 盘后 15:30+17 / 12:00·18:00 交接 / 看门狗等均正常运行。

---

## 四、需阿狸咪注意 / 待办 / 风险

1. **明早盘前验证 `'-'` 修复**：真实验证要等明早 06:5x–08:0x 那轮 ck_fetch。若 INDEX_QUOTES / CAPITAL_FLOW_DATA 不再报 `invalid literal for int()`，即证明 `de27275a8` 根治成功；若仍崩，回报小九并保留现场。
2. **模型同步**：主人 11:45 令全切 GLM-5.3-Flash，阿狸咪侧自动化任务须她侧自行切换（小九只能改本机表）。
3. **HEALTH_CHECK 3 fail（因子实验室 / 四量终极回测 / IMA_STRONG_STOCK）**：今日新出现（昨日 112-0-0 全绿），接手后优先排查是数据源问题还是管线问题。
4. **盘中 run 变慢观察项**：cn_fetch 单轮时长 20–27min（典型 7min），排队/顶替频繁（今日已 1 failure + 1 cancelled）。非卡死（每轮都能跑完、末次 success 正常），暂不升警，继续观察；**不要在云端有活跃 run 时强行补派**。
5. **红线（任何时候）**：真身工作树禁 `reset --hard` / `git add -A` / `rebase`；HEAD `720171f13` 未动是常态（远端走 CI + 隔离直推）；不碰 index.html（小九白天）/ HEALTH_CHECK.js 冲突保留本地；改云端脚本前必须先 `git show origin/main:<f> > <f>` 对齐权威版，否则在旧副本上改=推上去就是回滚。
6. **blob 安全校验新方法**：固定深度 `git log -60` 在高频提交期会失效（今日 60 深度全 NO MATCH），改用 `git log -400 origin/main --format=%H -- <path>` 按文件全历史比对，除非按文件历史也 NO MATCH 才可报「疑似 UNIQUE」。

---

## 五、小九监督职责（白天 7:45–17:45）

- 盘前 08:15 全量更新、盘中每 30 分守卫兜底、盘后 15:30+17:00 收盘链与三重复查。
- 每小时看板 pull + 看门狗；数据陈旧时按「远端 FRESH 不派发 / 有活跃 run 等待 / 真陈旧才派发」三判据执行，红线文件只读。
- 12:00 / 18:00 两次交接文档自动更新（本档即 12:00 档产物）；18:00 档将覆盖重写并注明更新时刻。
- 夜间 18:00 后交棒阿狸咪；小九自动化全部转只读监督，不写不推。

---

*小九 · 2026-09-04 12:05 CST · 本档仅此一份，18:00 档覆盖更新。*
