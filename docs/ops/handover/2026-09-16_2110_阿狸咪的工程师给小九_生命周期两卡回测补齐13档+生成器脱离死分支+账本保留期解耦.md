# 生命周期两卡回测补齐 13 档 + 生成器脱离死分支 + 账本保留期解耦｜阿狸咪的工程师 → 小九的工程师

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 时间：2026-09-16 21:10 CST
> 提交：`d47cb3689ef347fc50d1cc091b22b4494ad7ffdd`（main，父 `b01dfdfd6d`）
> 触发：主人令——「@图1 这两张卡怎么只有 T+10，要这些 @图2」（图2 = T+5/10/20/30/45/60/75/90/180/250 档位表）

---

## 0. 一句话

主人指的两张卡（**⚡ 强势突破** / **📡 高手强势股跟踪**）的回测表此前只到 **T+10**，现已补齐到全站规范 **13 档**；补齐过程中查出**两个结构性根因**并一并修掉：**① 生成器被写在 `else` 死分支里 ⇒ 正常路径永不执行**（两卡回测其实长期只在人工跑过时才更新，`data/*.js` 的 `update_time` 是桥接重写造成的「假新鲜」）；**② 信号账本 45 天滚动窗口 ⇒ T+45 以上档位结构性不可能有样本**。修后**高手强势股跟踪卡的长档立刻可读出真实样本**（T+30：32 笔 / 90.6% / +33.33%）。

---

## 1. 改动清单（4 文件，均已逐字节回读校验）

| 文件 | 动作 | blob | 说明 |
|---|---|---|---|
| `algorithms/backtest_life_cards.py` | 改 | `bb1d4cf14b` | `HORIZONS` = `[1,2,3,5,10]` → `[1,2,3,5,10,20,30,45,60,75,90,180,250]`（保留既有 T+1/2/3 短线档不删） |
| `scripts/gen_strong_breakout.py` | 改 | `471a089fe2` | 新增 `LEDGER_KEEP_DAYS = 380`，**只放宽落盘账本保留期**；`WINDOW_DAYS=45` 展示口径**一字未动** |
| `update_v8.py` | 改 | `6d9885b745` | `run_experiment_cards()` 里 `backtest_life_cards.py` 的调用**移出 `else` 死分支**到正常路径 |
| `logic.html` | 改 | `5caac708fa` | 两卡「数据从哪来」单元格原写「…**无内置回测**」（09-16 起已不成立）→ 如实登记真实回测与档位口径 |

---

## 2. 根因（都已取证，不是推测）

### 根因 A —— 档位写死，长档永远上不了屏
`algorithms/backtest_life_cards.py:51`：`HORIZONS = [1, 2, 3, 5, 10]  # 受信号账本窗口限制，短档先落地`。
而全站规范 ladder（`backtest_comprehensive` / `backtest_tdx` / `gen_algo_track` / `strategy_four_volume` / `gen_backtest_all_algos` 五处同源）是 `[5,10,20,30,45,60,75,90,180,250]`。前端渲染器 `lifeRenderBacktest()` 本就**遍历 `periods` 的全部键**（档位自动上屏）⇒ 只需补数据侧，前端零改动。

### 根因 B —— 45 天账本窗口 ⇒ T+45 以上**结构性**不可能有样本
`scripts/gen_strong_breakout.py` 的 `WINDOW_DAYS = 45` 同时约束了**两件事**：展示统计（uni / periods / `STOCK_MOMENTUM_STATE`）与**落盘账本**。而长档回测读的就是这份账本 —— 45 自然日 ≈ 31 交易日，**T+45 及以上的信号在到期前就被剪掉**，故那几档只会永远显示「未成熟」。
⇒ 解耦：展示窗口仍 45 天（**屏上口径零变化**），落盘账本保留 380 天（与 `backtest_comprehensive.py` 的 `LOOKAHEAD_DAYS=380` 同源，覆盖 250 交易日 ≈ 365 自然日 + 余量）。
⚠️ 该账本当前**唯一消费方**是 `algorithms/backtest_life_cards.py`，不发布前端 ⇒ 扩保留期不改任何屏上数字。

### 根因 C —— 生成器被误放在 `else` 死分支（**最要紧**）
`update_v8.py` 的 `run_experiment_cards()` 里，调用结构原本是：

```python
if momentum_self_py.exists():        # scripts/gen_strong_breakout.py —— 它永远存在
    ... 跑 gen_strong_breakout ...
else:
    print("⚠️ 缺失 scripts/gen_strong_breakout.py，跳过")
    life_bt_py = ... / "algorithms" / "backtest_life_cards.py"
    ... 跑 backtest_life_cards ...     # ← 正常路径**永不执行**
```

实测佐证：`raw_data/strong_breakout_backtest.json` 的 `generated` = `2026-09-16 17:32:27`（人工那一次），而 `data/STRONG_BREAKOUT_BACKTEST.js` 的 `update_time` = `19:47:13` —— 后者只是 update_v8 桥接重写，**内容没重算**，属「假新鲜」。
而该脚本自己的 docstring 明写：**「由 update_v8.py 的 experiment 段调用（紧随 scripts/gen_strong_breakout.py 之后，确保读到当日最新信号账本）」** ⇒ 契约未落地，本次按契约归位。失败仍**只告警不阻断主链**；真停更由 `v8_health_check.py` 已登记的两条（`STRONG_BREAKOUT_BACKTEST` / `IMA_STRONG_BACKTEST`，`max_age=1440`）兜底。

### 附带发现 D（未改，供你决策）—— 20:00 health_patrol 的**每晚结构性假 failure**
今晚 `run 35093379431` 失败在步骤「🔁 自愈验证：10 分钟后回查仍 fail → 升级告警」。日志实据：脚本自己判定的是「2 项已进入自愈/冷却 → **全部模块新鲜**」（`VOLATILITY` 已派发 cn_fetch(post_close) HTTP 204；`BACKTEST_TDX` 因「算法链已有活跃 run #35090461554」跳过重复派发），但 workflow 用 `REMAIN_FAIL=$(grep -c '"status": "fail"' verify_output.txt)` ⇒ **仍判 ≥1 ⇒ `::error::` failure**。
⇒ 判据与写入口径**不同源**：脚本说「已自愈」，workflow 只认 JSON 里残留的 `"status": "fail"`。这与主人「禁假 failure」铁律冲突，建议后续把 `REMAIN_FAIL` 一并扣除「已进入自愈/冷却」的项再判。

---

## 3. 验证（全部可复现）

| 层 | 做法 | 结果 |
|---|---|---|
| 补丁门禁 | 锚点唯一性 + `ast.parse` + 档位/常量/接线/行尾谱 25 项 | **FAIL=0**（含 2 项自证：注入错档位后判据必须报错） |
| 保留期解耦 | **从补丁后文件原文抽出剪枝代码块并真实 `exec`**（非重打逻辑），合成 400 天历史 | **8/8 通过**：展示 46 天（45 天窗口 + 当日）/ 账本 381 天；「账本 ⊋ 展示」「展示未被污染」均成立 |
| 生成器可跑 | 最小沙箱（远端账本 + 8 只真实 K 线） | 退出码 0，两份产物 `horizons` 均为 **13 档** |
| 真实预览 | 全量沙箱（远端账本 + **184/216** 只真实 K 线） | 见下表 |
| 推送 | API 通道 + 推前重取基线（**第 1 轮即撞基线漂移，自动重来**）+ 推后逐字节回读 | 4 文件**逐字节相同**，改动文件恰 4 个，父提交 = 基线（未覆盖任何人） |

**全量沙箱实测（真实日K · 扣双边成本 0.30%）**

`📡 高手强势股跟踪`（信号 40 笔，2026-06-09 ~ 08-24）：

| 档位 | 样本 | 胜率 | 平均收益 |
|---|---|---|---|
| T+10 | 38 | 76.3% | +16.38% |
| **T+20** | **36** | **80.6%** | **+27.22%** |
| **T+30** | **32** | **90.6%** | **+33.33%** |
| **T+45** | **14** | **100.0%** | **+38.76%** |
| **T+60** | **4** | **100.0%** | **+43.10%** |
| T+75 ~ T+250 | 0 | — | 未成熟（如实标注） |

`⚡ 强势突破`（信号 132 笔，2026-09-03 ~ 09-16）：T+1 44.7% / T+2 63.3% / T+3 61.5% / T+5 50.0%，**T+10 及以上暂「未成熟」** —— 该卡信号账本 09-03 才起跑，走满 T+10 需到 10 月上旬、走满 T+20 需 10 月中。**这是真实状态，脚本对未走满的档一律 `samples=0 + immature=true` 且全字段 `null`，绝不填 0 冒充。**

---

## 4. 需要你（小九）知道的 5 件事

1. **你的 `1918` 档说「勿重复跑 `algorithms/backtest_life_cards.py` 或覆盖数据文件」** —— 遵照：本次**没有覆盖任何生产产物**，只改了生成器；产物等下一次生成窗口（每日约 **17:3x**，即 `update_v8.run_experiment_cards` 那次）自动重算。你机 build 触发时会自然跑到（幂等、只读 kline_cache）。
2. **接线变了**：以后 cloud build 的 experiment 段**每次都会**跑这两个回测（原为「永不跑」）。产物无变化时不会产生 diff，不额外制造提交。
3. **账本会长大**：`raw_data/strong_breakout_history.json` 保留期 45 → 380 天，最终约 1 MB 量级（当前 23 KB）；只影响仓库体积，**前端不读它**。
4. **长档成熟节奏**：强势突破 T+10 约 10 月上旬成熟、T+20 约 10 月中；IMA 卡 T+75 及更远需等新信号走满（预计 10 月）。
5. **可选跟进（我没动）**：上述「附带发现 D」的 health_patrol 判据同源化；以及 `BACKTEST_TDX` 内容 `update_time` 仍停在 **2026-09-14**（落后 3 交易日，属真停更，与本次改动无关），今晚自愈因「算法链已有活跃 run」被跳过，值得你我在 21:00 的 E 批后复查。

---

## 5. 复现命令（家机 / 你机通用）

```bash
# ① 档位与保留期（静态）
grep -n "^HORIZONS" algorithms/backtest_life_cards.py
grep -n "WINDOW_DAYS\|LEDGER_KEEP_DAYS" scripts/gen_strong_breakout.py

# ② 接线（关键：调用必须在 else 之外、缩进 4 空格）
grep -n "backtest_life_cards.py ({datetime.now" update_v8.py
sed -n '1350,1380p' update_v8.py

# ③ 产物（下一次生成窗口后应看到 13 档）
grep -o '"horizons":\[[^]]*\]' raw_data/strong_breakout_backtest.json raw_data/ima_strong_backtest.json
```

—— 阿狸咪的工程师 · 2026-09-16 21:10 CST
