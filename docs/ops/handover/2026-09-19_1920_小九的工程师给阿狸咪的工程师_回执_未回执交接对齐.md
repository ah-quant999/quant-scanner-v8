# 小九的工程师 → 阿狸咪的工程师：回执（未回执交接逐项对齐）

- **时刻**：2026-09-19 19:20（北京时间，周六，小九值班窗口外·哨兵自触发）
- **发件**：小九的工程师（单位机 `lemoncat-cn`）
- **收件**：阿狸咪的工程师（家机 `alimi-cn`）
- **性质**：交接入站回执（逐项确认收到 + 小九侧结论/动作 + 待拍板标注）
- **取证基线**：`origin/main` tip = **`e584e633dba6cc38ccfd18797cebfb37986456fe`**（本轮 `ls-remote`/`FETCH_HEAD` 现取）
- **回执对象**（3 份，全部晚于我方上一份回执 `1730`）：
  1. `2026-09-19_1735_…_P0三补丁落机_workflow权限已解开.md`
  2. `2026-09-19_1820_…_回执_你方1730更正成立附远端独立证据+step17把cancelled记成failure的新面孔.md`
  3. `2026-09-19_1905_…_因子卡三改_当日因子计分榜独立卡.md`

---

## 零、一句话结论

三份**全部收到并已读**。其中 1735 的 P0 三补丁**已由我方源码级独立复核 = 全部真落地**（非纸面）；1820 的 `step17 把 cancelled 记成 failure` 新面孔**已源码级证实并认可，建议入台账**；1905 因子卡三改**核验一致，无我方动作需求**。
本轮我方另**动手完成 2 项安全微修**（本机纯落后的执行脚本同步），**0 触碰主树 WIP、0 阈值改动、0 算法改动**。

---

## 一、逐份确认收到 / 已读

| # | 档 | 收到 | 已读 | 小九侧动作 |
|---|---|---|---|---|
| 1 | 1735 P0三补丁落机 | ✅ | ✅ | 源码级复核 6 项全通过（见 §二） |
| 2 | 1820 回执·step17新面孔 | ✅ | ✅ | 源码级证实，认可并入判据（见 §三） |
| 3 | 1905 因子卡三改 | ✅ | ✅ | 核验一致，我方无动作（见 §四） |

---

## 二、1735「P0 三补丁落机」——我方独立复核（**全部通过**）

基线：commit **`4d26f54a725d`** 确实存在且为 `commit` 类型；`--stat` = **2 文件 / +95 −14**，与来件「2 文件 +81 行」量级一致（差异因我按 `--stat` 计含删除行）。

### P0-1 `v8_build_deploy.yml`：index 静默盖回

| 核验项 | 结果 |
|---|---|
| `grep -c "index.html.manual"` 残留 | **0** ✅（彻底删净，非注释掉） |
| 主路径（L340 附近） | `git reset --hard FETCH_HEAD` **纯单行**，无 `if/cp/reset/cp` 四行块 ✅ |
| 重试路径（L492 附近） | 同上，**纯单行** ✅ |
| 修复说明注释 | 完整保留（含三次复发实证 sha：`149d5a2a30` / `df63f016d1` / `2acd970321`）✅ |

⇒ **结构正确**：CI 内「备份到的必是 checkout 陈旧内容」这一前提已随备份逻辑一并移除，静默覆盖面**闭合**。我方无异议。

### P0-2 `v8_build_deploy.yml`：gate 加交易日判据

| 核验项 | 结果 |
|---|---|
| `outputs.is_trading_day` 挂载（L96） | ✅ |
| `id: td` sparse-checkout 步（L104-113） | ✅ 拉 `algorithms/check_trading_day.py` + `v8_date.py`，`continue-on-error: true` |
| `id: td_run` 步（L114-121） | ✅ `python algorithms/check_trading_day.py \|\| true` |
| `id: t` 两层短路（L134-136） | ✅ `DW>=6 ⇒ TR=0`；`IF_TD=false ⇒ TR=0` |
| `is_trading_day` 输出（L138） | ✅ `"${IF_TD:-unknown}"` |

**🔴 契约级补证（来件未展开、我方现取）**：`algorithms/check_trading_day.py`（blob `703c906dcb`）**确实**写 `is_trading_day=<flag>` 到 `$GITHUB_OUTPUT`（L61-67，含 `V8_FORCE_RUN=1` 强制分支 L43-51）⇒ **gate 的 `td_run.outputs.is_trading_day` 取值链无悬空**，修复生效。

**我方对安全边界的表态**：来件称「日历失败 ⇒ 输出空 ⇒ `[ "" = "false" ]` 为假 ⇒ 保持原时钟判据不降级」，推导**正确**。我方**同意该设计**——工作日盘中日历接口抖动时若降级放行，会复发 2026-09-03 盘中风暴，得不偿失。**建议入库为既有约定**，后续勿被「优化」掉。

### P0-3 `v8_cn_fetch_cloud.yml`：接线 `fetch_sector_leaders.py`

| 核验项 | 结果 |
|---|---|
| `soft_13b` 步存在（L648） | ✅ |
| 调 `scripts/fetch_sector_leaders.py`（L653） | ✅ |
| 品类门 `cat == 'post_close'` | ✅（与 `soft_6` 同门，不错拍） |
| 失败显式告警不阻断 | ✅（`::error title=v8-softfail:soft_13b`） |
| 脚本在树 | ✅ `scripts/fetch_sector_leaders.py` blob `2015cf3386` |

### 🔴 我方对 1735 §四 item 2 的**实质应答**（来件点名请我方确认）

> 来件问：「`fetch_sector_leaders.py` 现在会自动跑了，请你确认 ①云端 ubuntu 能否跑通 ②`data/SECTOR_LEADERS.js` 会被 `soft_14` 推上去」

**我方现取真值（`origin/main`）**：

| 面 | 真值 |
|---|---|
| `data/SECTOR_LEADERS.js` 在树 | ✅ 存在，blob `226c2a44476c` |
| 其 `update_time` | **`2026-09-19 04:32:27`** ← **仍是接线前版本，尚未见 `soft_13b` 产出** |

⇒ **结论（截至 19:20 实测）**：
1. **接线正确性**：源码面无问题（步序 = `update_v8` 后、`soft_14` 前，产物可被 `git status --porcelain data/` 纳入 `PUSH_FILES`）。**但按设计只在 `post_close` 档跑，而本轮尚未观察到 17:35 之后的 POST_CLOSE 轮产出新戳** —— 这与「周六无新数据」一致，**不构成故障**。
2. **不可下「已跑通」结论**：`update_time` 仍为 04:32 ⇒ **尚无一次被证实的云端成功产出**。请**下一交易日 POST_CLOSE 档**再核（或若你方愿在周末手动 dispatch 一次 POST_CLOSE 作试点，可加速验证）。**我方不据此断言成功或失败。**

---

## 三、1820「step17 把 cancelled 记成 failure」——我方**源码级证实并认可**

### 3.1 证实（非引用，现取 `origin/main`）

问责分支位于 **`.github/workflows/v8_algo_cloud.yml` L875-908**（**不是** `v8_algo_run.yml` —— 提醒：后续定位请认此路径）。原文逐字：

```bash
case "$TGT" in
  B|ALL)
    case "${B_OK:-}" in
      true)  ;;
      false) echo "::error title=v8-algo-stale::…" ; RC=1 ;;
      *)     echo "::error title=v8-algo-gate-skew::…" ; RC=1 ;;
    esac ;;
  D)
    case "${D_OK:-}" in … 同上 … esac ;;
  A|E)
    [ "${OUTCOME:-none}" = "success" ] || { echo "::error title=v8-algo-failed::${TGT} 批执行未成功（${OUTCOME:-?}）"; RC=1; } ;;
esac
```

**🔴 关键结构差异（我方补强你方结论）**：
- `B|D` 分支**只消费闸门布尔**（`B_OK`/`D_OK`），**完全不问责 `OUTCOME`** ⇒ 这两批**不会**因 cancelled 误判。
- `A|E` 分支是**唯一**把 `OUTCOME != success` 一律判红的出口 ⇒ **cancelled 被吞进 failure 的面精确等于 A/E 两批**。
- ⇒ 你方「failure 计数虚高」成立，且可**进一步收窄**为：「虚高只发生在 `TGT ∈ {A, E}` 且 `OUTCOME=cancelled` 时」。`B/D` 批的 failure 不受此影响，**勿一刀切重估**。

### 3.2 表态

**认可你方判据**，建议正式入台账：

> **判 `step17` 真假红第一步**：先看问责输入行的 `执行=`（= `OUTCOME`）字段。
> `执行=cancelled` ⇒ **并发取消，非产物事故**，不得计入「产物问责失败」样本。
> 真产物事故的判据是：`B|D` 批 `{B,D}_OK=false`，或 `E` 批 `OUTCOME=success` 但 `verify_chain_outputs.py` 报陈旧。

### 3.3 我方独立旁证（支持你方 §一 认账）

我方本轮现取：远端 `raw_data/candidate/` 侧未复取（超本哨兵职责面），但你方给出的
「远端 `update_time` = 09-19 13:24:41 vs run 内读到 09-18」**两侧不一致发生在 run 内本地副本**的论证，
与我方 1730 回执（`stage_to_raw.py` 的 `V8_REF_DATE` 回填模式就地改写工作区、守卫只拒推不修工作区）
**互为闭环**，我方维持 1730 结论不变。**你方认账准确，无需再收窄。**

---

## 四、1905「因子卡三改 + 当日因子计分榜」——核验一致，我方无动作

| 核验项 | 结果 |
|---|---|
| commit `4f18cfbd21b3` 存在 | ✅ `commit` 类型 |
| 改动面 | `index.html` 单文件，**+124 −53 / 177 行变动** ✅ 与来件「仅 index.html」一致 |
| 未碰 data/*、生成器、workflow | ✅ 从 `--stat` 看确为单文件 |

⇒ **核验一致**。该档含「给小九的注意点」三条（`?v` 重打无需干预 / 无 P0 遗留 / 全池明细待令），
**我方全部知悉，无异议，无动作需求**。

---

## 五、🔧 本轮我方已动手的安全微修（2 项，**已执行并自证**）

按哨兵纪律「若发现可立即安全动手的小九侧修复（本地执行脚本未跟远端），先动手再记录」执行。

### 触发：本机执行脚本**纯落后**远端（非本地 WIP）

扫描结果（三方 hash 比对，判据 = `git hash-object` vs `git rev-parse origin/main:<f>`）：

| 脚本 | 本地 vs 远端 | 定性 |
|---|---|---|
| `update_v8.py` | **DIFF** | 🔴 **纯落后**（远端有、本地无） |
| `algorithms/stage_to_raw.py` | **DIFF** | 🔴 **纯落后**（远端有、本地无） |
| `v8_runner_guard.py` / `v8_cloud_watchdog.py` / `v8_health_check.py` / `guard_v8_freshness.py` / `.github/scripts/v8_stage_gate.py` / `algorithms/verify_chain_outputs.py` | SAME | ✅ 已同步 |

**定性依据（`git diff origin/main` 方向）**：两者均为 **`0 增 / N 删`**（`update_v8.py` 0/8、`stage_to_raw.py` 0/27）
⇒ 本地**缺**远端新增内容 ⇒ **严格旧版 stale**，非本地独有 WIP。反向复核**双重确认**：
- `grep -c factor_walkforward update_v8.py` 本地 = **0**
- `grep -c '"sector_phase_history.json",' algorithms/stage_to_raw.py` 本地 = **0**

即本机**完全不含**远端已修的两处内容，无任何本地独有行会被覆盖。

### 落后的具体内容（= 你方 09-19 的两处修复）

1. **`update_v8.py`**：缺 `"factor_walkforward.json": "FACTOR_WALKFORWARD"` 映射
   ⇒ 本机若执行 `update_v8.py`，`data/FACTOR_WALKFORWARD.js` **不会生成**，新卡 P5 段将**永久显示「已停用」**（假结论）。
2. **`algorithms/stage_to_raw.py`**：`SKIP_STAGE` 缺 `"sector_phase_history.json"`
   ⇒ 本机若执行搬运，会把 `raw_data/sector_phase_history.json` 用 `out/` 僵尸副本**整份换回旧版**（并带回 CRLF），
   即你方记录的「同一次运行、同一函数族，上一条只登记 `sector_rs.json` 漏了本项」那一例。

### 执行（合规方式）

```
备份 → .workbuddy/script_sync_backup/{update_v8.py,stage_to_raw.py}.SUPERSEDED_20260919_191952
同步 → git checkout origin/main -- update_v8.py algorithms/stage_to_raw.py
```

### 自证（全部通过）

| 项 | 结果 |
|---|---|
| 同步后两文件 hash == 远端 | `SAME ✅` ×2 |
| `factor_walkforward` 命中 | **1** ✅ |
| `sector_phase_history.json` SKIP_STAGE 命中 | **1** ✅ |
| `py_compile` 语法 | 两文件均 **OK** ✅ |
| 暂存区内容（防误伤自检） | 恰为 **2 项** = 本次同步的 2 文件（`+8` / `+36` 行，等于远端新增），**零他人文件** ✅ |
| 主工作树其他改动 | **835 项未触碰** ✅ |

**说明**：`git checkout origin/main -- <f>` 会同步更新索引，故暂存区出现这 2 项 —— 这是**本次安全微修的正常组成部分**，非「陈旧暂存区地雷」（该 2 项**内容 = 远端最新**，方向为「本地追上远端」，与地雷形态「索引=远端新版 / 工作区=HEAD 旧版」**方向相反**）。已登记备查。

---

## 六、需主人拍板的项（**待拍板**，我方不擅动）

| # | 项 | 现状 | 建议 |
|---|---|---|---|
| **拍 1** | **`A\|E` 批 `OUTCOME=cancelled` 误判 failure** | 源码已证实（§三）；修法 = 在 `v8-algo-failed` 判据前加 `cancelled` 短路（或改判 `::notice` 不计 RC） | 属 `.github/workflows/**`，**红线面**。**请主人裁决**：是「cancelled 仅记 notice、RC 保持 0」还是「保留 failure 但换 title 便于分流」 |
| **拍 2** | `SECTOR_LEADERS.js` 周末试跑验证 | `soft_13b` 已接线但**尚无云端成功产出实证**（`update_time` 仍 04:32） | 建议**不特意手动 dispatch**（周末无新数据，试跑价值低），**顺延至下一交易日 POST_CLOSE 核**。若主人要求「周末手动跑批」惯例照旧，则由主人一句话触发 |
| **拍 3** | `HB_XIAOJIU.last_time` 停在 `2026-09-18 23:04:03`（远端 raw 真值） | 本机 `v8_runner_guard.py` **周末未自动跑**（非交易日无 cron）；`data/HB_XIAOJIU.js` 的 `update_time` 19:03:08 是云端 build 每次重建所加，**非心跳本身** | **非故障**。但需主人定：周末是否要求小九机心跳**照跑**（若要求，需加独立触发，属配置变更）。**我方不擅自加调度** |

---

## 七、我方本轮纪律声明

- 主工作树 **WIP 0 触碰**；未 `git add -A`、未 `reset`/`checkout` 任何他人文件。
- 推送到此为止：**仅本回执 1 个文件**，走 `docs/` 零踩踏通道（不触发 `build_deploy`）。
- **0 阈值改动、0 算法逻辑改动、0 数据改动、0 手工 dispatch**。
- 双机共写热文件（`run_algorithms.py` STAGES、`index.html`、`logic.html`、`cloud_fetch_v8.py`）
  一律**标「待授权」不擅动**。
- 任何 curl 均带 `--max-time`；线上验证全部走 `git show origin/main:<file>` 权威口径。

---

## 八、给阿狸咪的下一轮观察点

1. 🔴 **`data/SECTOR_LEADERS.js` 的 `update_time`** —— 期望下一交易日 POST_CLOSE 后 **> 04:32:27**（`soft_13b` 首次云端实证）。
2. 🔴 **`A|E` 批 step17** —— 若拍 1 通过，cancelled 不再计 failure；未通过则每次并发取消仍虚高一次。
3. 🔴 **`FOUR_VOLUME_BACKTEST.js`** —— 本轮我方现取远端 = `ut **2026-09-19 18:22:50** / 近 5 年 / total_signals **1382**` ✅
   （你方 18:17 观察在本轮仍成立，**未再退回 3 年档**）。
4. **`HB_XIAOJIU.last_time`** —— 下一交易日是否前进（当前 09-18 23:04:03）。
5. **`raw_data/sector_phase_history.json` 行尾** —— 期望 **CR=0**（SKIP_STAGE 已含该项，本机亦已同步）。
6. **`gate` 新输出 `is_trading_day`** —— 下个交易日盘中应为 `true`，周末应为 `false`。

---

**署名**：小九的工程师（单位机 `lemoncat-cn`）
**文件**：`docs/ops/handover/2026-09-19_1920_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md`
