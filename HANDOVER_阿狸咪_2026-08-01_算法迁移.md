# 交接文档 · v8 算法迁移（阿狸咪 2026-08-01 周末）

## 结论（回答主人的问题）
- **复制还是移动？** → **选择性复制**（不是整体移动，也不是整体复制）。
  - 只把 v8 真正需要的「盘后算法产出脚本」(~17 个) + 2 个本地依赖复制到 `v8/algorithms/`；
    不复制 v6 的 `batch_update.py` 大编排器（那才是 v6 臃肿本体）。
  - v6 仓库**保留 1 周**作为应急回退与上游输入源，不直接删除（满足「一周后该累积的留着」）。
- **要不要等周一的小九？** → **不用**。本周末（阿狸咪）已完成迁移并本地跑通，
  cn 自托管 runner 上的 `v8_algo_run.yml` 周一会自动按 17:00 盘后定时原生运行。

## 为什么必须迁移（根因）
- v6 已停止每日运营。旧的 `v8_sync_v6_data.yml` 同步桥依赖 v6 持续产出；
  一旦 v6 停更，桥会把**陈旧 v6 数据覆盖掉新鲜 v8 数据** → 已**停用其定时**，
  仅保留 `workflow_dispatch` 应急手动触发。
- GitHub **美区云端 runner 无法访问** EastMoney/akshare/Tongdaxin（网络封锁），
  所以 A股算法必须在 **cn 自托管 runner（本地机器）** 执行 → `v8_algo_run.yml` 用 `runs-on: [self-hosted, cn]`。

## 架构（迁移后）
```
v8/algorithms/                # 从 v6 选择性复制的算法脚本（data/ 路径已改写为 out/）
  ├─ *.py (17 个算法产出脚本)
  ├─ fundamental_helper.py / fetch_logger.py   # 本地依赖
  ├─ out/                     # staging：脚本写 v6 文件名到这里
  ├─ run_algorithms.py        # 编排：重灌 v6 输入→按依赖顺序跑→stage→push
  └─ stage_to_raw.py          # 按 V6_TO_V8 重命名为 v8 raw_data/ 命名 + 注入 update_time
v8/raw_data/                  # v8 前端消费的「v8 命名」数据（由 stage_to_raw 产出）
v8/.github/workflows/v8_algo_run.yml   # cn runner，17:00 CST 盘后定时
```

## 运行顺序（依赖）
fetch_fundamental_quality → fetch_stock_names → fetch_sh_index_fib → fetch_inst_trade
→ fetch_sector_rs → fetch_lhb → calc_crds → build_candidate_pool（读 guanlan/maharo）
→ backtest_tdx（读 gold_pool）→ backtest_comprehensive → cockpit_backtest_now
→ generate_top10 → gen_cockpit_tier_recommend（读 scan_result）
→ gen_cockpit_advice → update_triple_resonance_history → gen_triple_consensus → gen_triple_track
→ stage_to_raw → api_push_raw

## 过渡期（约 1 周）仍由 v6 供给的上游输入
`run_algorithms.py` 每轮从 `V6_DATA_DIR` 重灌到 `out/`：
- `gold_pool.json` / `scan_result.json` / `guanlan_watchlist.json` / `guanlan_reports.json` / `maharo_signals.json`
- 之后这些应由 v8 原生抓取器取代，即可完全脱离 v6。
- `maharo_signals` 需登录，过渡期从 v6 同步；长期需 v8 侧登录方案（待办）。

## 本地验证（已跑通）
- `python migrate_v6_algos.py` 复制+改写路径（data→out）。
- `V8_PYTHON=<python> V8_PUSH=0 python algorithms/run_algorithms.py` 端到端跑通，
  `raw_data/` 产出正确 v8 命名文件。生产用 `V8_PUSH=1` 经 API 推送。

## 周一小九交接要点
- v8 盘后算法已不依赖 v6；如 v8_algo_run.yml 在 cn runner 失败，先看 runner 是否在线、
  akshare/baostock/mootdx 是否安装，再考虑应急 `v8_sync_v6_data.yml` 手动同步。
- 勿重新启用 `v8_sync_v6_data.yml` 的定时（会覆盖新鲜数据）。
- 剩余待办：把 5 个上游输入也改成 v8 原生（脱离 v6）。
