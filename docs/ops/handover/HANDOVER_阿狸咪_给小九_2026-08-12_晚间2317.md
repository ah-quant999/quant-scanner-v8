# 交接：小九 —— 2026-08-12 晚间全量工作（23:17）

**交接人**：阿狸咪
**时间**：2026-08-12 23:17（CST）
**远端**：`origin/main` = `0b66dd629`（本地已同步）
**今天 commit 数**：311（含云端 build）

---

## 一、⚠️ 最重要：8 天 bug 的 4 个隐藏根因（今晚全发现）

| # | 根因 | 修复 commit | 现象 |
|---|------|------------|------|
| 1 | **scanner.load_candidate_pool 路径 bug**（候选池错位→A股信号缺失）| `045ac1c84` | 三重共识 8 天为 0 |
| 2 | **check_data_cards 显式豁免空 stocks** `if v==[] and f=='stocks': continue` | `957e2368f` | 全港股/0只 永远不被健康检查发现 |
| 3 | **check_signal_date_freshness 只盯 Top3**（其他池全港股无人查）| `957e2368f`（加 check_a_share_coverage）| 黄金池全港股 5/5 持续 8 天 |
| 4 | **renderTradeSignals `_in` 用 arr.some 但 stocks 是 dict** | `df6dedf2c` | 个股查询「什么都查不了」（JS 崩静默）|

**主人紧急令**：「以后全是港股或者任何一个算法跑出是 0 的，马上回溯哪里有错！」

## 二、自动化闭环（主人确认「双机关机网站也能自动化」）

```
数据抓取(v8_cn_fetch_cloud 云端 cron)
  08:25 盘前 / 盘中每30分 / 17:20 盘后 / 17:00+21:00 全量 / 周六日09:00
  ↓
算法链(v8_algo_cloud 云端 18:30 主档)
  ↓
构建部署(v8_build_deploy push/workflow_run 触发) → GitHub Pages 自动部署
  ↓
健康检查/自愈(每轮巡检 + 盘中自愈器 + 运维看板) → 发现问题自动派发
  ↓
每日全站审核(v8_daily_audit 22:30) → 邮件告警(只报问题)
  ↓
每日备份(v8_backup 23:00)
```

**本机 WorkBuddy 只是冗余兜底**（每 30 分钟 dispatch 检查），双机关机网站照常更新。

## 三、今晚新增/修改的关键文件

### 新增
- `.github/workflows/v8_daily_audit.yml` —— 每日全站审核（22:30 CST，比备份早 30 分钟）
  - 跑 `v8_health_check.py --alert --site`（heal 默认开）
  - 审核记录写 `raw_data/audit_history.json`（保留 90 天，前端可见）
  - 邮件策略：**全部自愈不发邮件，只发有问题的**
- `raw_data/audit_history.json` —— 审核历史（今日首条 22:58）

### 修改
- `v8_health_check.py`：
  - `check_a_share_coverage()` 新函数：扫 8 大池 A 股覆盖
    - 总数=0 → 弱 warn
    - **总数>0 且 A股=0 → 🚨 URGENT 告警**
  - `self_heal()` 加「A 股覆盖 URGENT 自动派发 algo_run」段（0.5 节）
  - `send_report_email()` 全部自愈不发邮件（`elif healed → return False`）
- `cloud_fetch_v8.py` `f_v8_cal()`：补美国宏观数据日 + Jackson Hole
  - PPI/零售销售(14日) / 密歇根(第2周五) / 工业生产(15日) / 费城联储(第3周五) /
    领先指标(20日) / 消费者信心(第4周三) / 新屋销售(25日) / 耐用品(27日) / Jackson Hole(8/28-29)
- `update_v8.py`：**cache-busting 正则修复**
  - `[A-Z_]+` → `[A-Z0-9_]+`（V8_CAL/W52_HIGH/TOP10_DAILY 含数字，原来永远不加 ?v= 缓存戳 → 浏览器永远缓存旧版！）
- `index.html`：
  - `_safeArr()` 全局工具（dict/array 兼容，16 处隐患全改）
  - `renderTradeSignals`：操作信号小卡化 + 删错位的全市场主线板块（挪到候选池底部）
  - `renderQuote`：删平均股价回归段（全市场均价不该在个股查询）
  - `renderCard`（IPO）：已上市显示跟踪期表现（🔴破发/🟢涨/⚪震荡），不再显示申购建议
  - 查询系统 try/catch 错误显示（不再「什么都查不了」假象）

## 四、数据任务时间表（主人 23:14 问）

| 任务 | 时间 |
|------|------|
| 🇨🇳 数据抓取 | 08:25 / 盘中每30分 / 17:20 / 17:00+21:00 / 周六日09:00 |
| ☁️ 算法链 | 18:30 主档 |
| 🧾 全站审核 | 22:30 |
| 📦 备份 | 23:00 |

**今晚 22:36 算法链 run#125 卡 39 分钟**（异常）——已取消 run#125/126/127 + 23:17 重新派发。
若再卡：查 scanner 是否某 API 无限重试。

## 五、状态确认

- ✅ 本地工作区干净（仅 dist/ 构建产物 untracked，正常）
- ✅ 本地 == 远端 `0b66dd629`（云端 build 23:17 已同步）
- ✅ 远端 index.html 已含 V8_CAL.js?v=20260812231703（缓存戳修复生效）
- ✅ 远端 data/V8_CAL.js 含 Jackson Hole/PPI/零售/密歇根
- ✅ stash 有 5 个残留（全是 data/* 构建产物，安全，可清理）
- ⚠️ 黄金池仍是 7 港股/1 A 股（scanner 修复后需等 23:17 算法链跑出）

## 六、主人偏好备忘

- **部署/Git**：改完直接 push main，不用问
- **输出纪律**：不编数据、不脑补、不确定就说不知道
- **「以后都是不要靠右放」**：信息 pill/标签靠左，不用 margin-left:auto
- **日历**：V8_CAL 每工作日 08:25 自动更新（f_v8_cal 自动生成）
- **审核邮件**：全部自愈不发，只看有问题的
