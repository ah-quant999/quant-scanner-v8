# 交接：策略回测主对比表「对齐 + 补齐」

- **发件**：阿狸咪的工程师（alimi-cn）
- **收件**：小九（lemoncat-cn）
- **时间**：2026-09-13 20:08 CST
- **关联提交**：`f1bcbc577b9bdd4e27bfc765b4433e005f5688e3`（main）

## 一句话
线上「回测汇总 → 主对比表」此前**少两张卡 + 数字与线上源自相矛盾**；已按线上源重算聚合，并把 候选池 / 黄金池 补进主表。

## 根因（实测，非推断）
1. **数字自相矛盾**：线上 `data/BACKTEST_ALL_ALGOS.js`（09-13 13:00 版）里 三重共识 = `n=31 / 胜率 66.7%`；
   而线上 `data/BACKTEST_COMPREHENSIVE.js` 长期是 `resonance_all=311 / resonance_gte80=119`
   （git 历史 09-08～09-12 稳定在 299~312 / 106~119）。
   ⇒ 该聚合产物是用**本机未推送的截断源**（本地那份 = 82 / 31）生成的，与线上源不一致。
2. **两卡整卡缺失**：`候选池` / `黄金池` 行的 label 仍是「持有 T+X」，
   与 `SOURCES` 里 `primary`（「首次进池 T+5」/「首次满足信号 T+1」）不匹配
   ⇒ `is_primary` 全 False ⇒ 两张卡不进主对比表（主人：「策略也少了」）。

## 改了什么
| 文件 | 改动 |
|---|---|
| `data/BACKTEST_ALL_ALGOS.js` | 用**线上源**重跑聚合（`--today 2026-09-12 --kind t1`，同数据日）；主表变 4 行 |
| `raw_data/backtest_all_algos.json` | 同步重生成（推 data 前必推 raw_data） |
| `algorithms/gen_backtest_all_algos.py` | `primary_rule` 由**手写死**改为**从 SOURCES 派生**（删 RPS 后曾残留「相对强度＝T+5」，且漏了候选池/黄金池）⇒ 永不漂移 |
| `index.html` | `_STARKEY` 补 `候选池` / `黄金池`；`__MODULE_STARS` 补 `candidate:2, goldpool:2` |

## 主表新口径（FETCH_HEAD 回读实测）
| # | 策略 | 星级 | 主口径 | n | 胜率 | 最佳持有期 |
|---|---|---|---|---|---|---|
| 1 | 三重共识 | 4⭐ | 共振≥80（严格） | 119 | 61.9% | T+20 |
| 2 | 四量终极 | 4⭐ | 持有 T+5 | 1454 | 50.7% | T+90（71.8%） |
| 3 | 候选池 | 2⭐ | 首次进池 T+5 | 293 | 48.81% | T+5 |
| 4 | 黄金池 | 2⭐ | 首次满足信号 T+1 | 127 | 32.28% | T+1 |
| — | 逆势龙头 | 弱市观测 | 持有 T+5 | 4 | 100%（样本<30 不排名） | — |

- 候选池 / 黄金池 取 2⭐ 的依据：主口径胜率均**未过委员会 50% 的三⭐门槛**（48.81% / 32.28%）。
- 新产物带出 1 条**卡级下架建议**：黄金池（胜率 32.28% < 红线 45%、平均收益 −1.79% ≤ 0，n=127）。按既定规则交主人定夺。

## ⚠️ 请小九注意（重要）
- 本机工作区（`E:\workspace\stock-scanner`）里 `data/BACKTEST_COMPREHENSIVE.js`（update_time 09-13 12:26）
  与 `data/CRDS_BACKTEST.js` 是**未推送的截断版**（82/31、逆势龙头 n=7），
  与线上长期值（311/119、CRDS n=4）不符 ⇒ **禁止推送这两个本地文件**，否则会把线上源打回截断态。
- 本机 `data/FOUR_VOLUME_BACKTEST.js` 反而比线上旧（本地 09-12 12:45 / 线上 09-13 16:04），同样禁推本地版。

## 验证（阿狸咪侧实测）
- `htmlcheck.mjs`：index.html 25 段 inline 脚本 **0 失败**、8 类标签净差全 0。
- `align_logic_ops.py`：**EXIT 0**（21 个 cron workflow 全文档化，无过期引用、无死登记）。
- 行尾：`index.html` CRLF=0（LF 守恒）；重生成的 `data/*.js` CRLF=0。
- `py_compile algorithms/gen_backtest_all_algos.py`：OK。
- 推送走 `atomic_patch_push.py`（FETCH_HEAD 派生 + 临时索引 `commit-tree`），**零碰**本机 190 M 工作树。

## 未做（有意留白，等主人拍板）
- `三重共识` 星级维持 4⭐（09-12 主人令「严格档样本偏少降 4⭐」）；若主人要按「最佳持有期」口径升 5⭐，需明示后再改。
- `四量终极` 维持 4⭐（09-13 多期加权 + 短周期惩罚）。
