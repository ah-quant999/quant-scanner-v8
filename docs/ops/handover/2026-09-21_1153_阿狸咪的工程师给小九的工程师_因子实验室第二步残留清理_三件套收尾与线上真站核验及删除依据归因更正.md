# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-21 11:53｜因子实验室「第二步」残留清理 —— 改后三件套收尾 · 线上真站核验 · 一处归因更正

> 🔴 **前置（必读）**：本改动**已于 11:37:43 由本机另一会话提前上线**（原计划 11:45）。
> 上线过程、422 元组 bug、dry 盲区两条判据见同目录
> `2026-09-21_1145_阿狸咪的工程师给小九的工程师_因子实验室第二步残留清理上线9处_含422元组bug与dry盲区判据.md`。
> **本件不重复**那部分，只做三件事：**① 三件套原始输出 ② 线上真站硬 HTTP 核验 ③ 一处归因更正**。

> 📌 **归档定性（对齐 README 2026-09-21 新规）**：本件按**跨机需知的口径更正**归档（含一处删除依据归因更正 +
> 一条判据修正），**不是回执档、不是进度汇报**。**未**新增 `HANDOFF.yaml` 挂账项 —— 本改动已上线、**无待小九动作**。

---

## 一句话结论

改动**零覆盖、零推退**：`f320fb3114` 之后远端 6 个新提交（含 3 次 `v8 build`）**只改了 `?v` 缓存戳**，
归一化后与 11:37 推送版 **sha256 完全一致**；改后三件套**三项全绿**（矩阵 30/30 无漂移、离线门禁 12 项 EXIT 0、本件）；
**Pages 真站硬 HTTP 抓取确认 9 处改动在生产生效、小九 7 处口径同步健在**。

- 发件：阿狸咪的工程师（家机 `alimi-cn`）
- 收件：小九的工程师（单位机 `lemoncat-cn`）

---

## 一、线上状态复核（只读 · 原始输出）

采集时刻 **北京时间 2026-09-21 11:45:47**。

```
[2] 最近 10 个提交 (main)：
   1) 49f8576bfb  2026-09-21T03:42:43Z  v8-cloud-bot  v8 build: 2026-09-21 11:42
   2) cc0497955c  2026-09-21T03:41:55Z  ah-quant999   docs(handover): 因子实验室第二步残留清理上线（9 处）…
   3) ce788ff146  2026-09-21T03:41:40Z  ah-quant999   chore(handoff): 阿狸咪更正 gate-1010 项 …
   4) ca5d70e92d  2026-09-21T03:40:24Z  ah-quant999   chore(handoff): 阿狸咪挂账 P0 baihechou-analysis-no-producer …
   5) ea999c7dc0  2026-09-21T03:39:15Z  v8-cloud-bot  v8 build: 2026-09-21 11:39
   6) 9ba3f284d1  2026-09-21T03:38:40Z  v8-cloud-bot  v8 build: 2026-09-21 11:38
   7) f320fb3114  2026-09-21T03:37:43Z  ah-quant999   fix(ui): 清理因子实验室「第二步」删除残留（9 处）[阿狸咪的工程师]  <<< 本次改动
   8) ef8cd1f5e4  2026-09-21T03:36:30Z  ah-quant999   data: logic.html 经发布闸门核对 20260921
   9) 37f3f3acac  2026-09-21T03:36:24Z  ah-quant999   data: index.html 经发布闸门核对 20260921
  10) d4640d6928  2026-09-21T03:34:31Z  v8-cloud-bot  chore: 生成器原子提交 ?v → 防覆盖 (2026-09-21 11:34)

  f320fb3114 在位: ✅ 是 (第 7 位)
  其后新提交数: 6（均为 f320fb3114 之后产生，其中 v8 build 类 3 个，属正常）
```

### 真身自证（`Accept: application/vnd.github.raw`，非 contents base64）

```
blob  = 2870296fdc17b233490a00460457bbfc047afe03
tree 记录 size = 1340660 B
raw  实际 len = 1340660 B
断言 len(bytes)==size : ✅ PASS
md5   = d2456a206108e99f2c829a321a44fe47
cr 数 = 0            （index.html eol=lf）
```

### 🔴 线上 blob 已不是 11:37 的 `3250da0dbde6` —— 这是**预期**，且已证明无实质变化

3 次 `v8 build`（11:38/11:39/11:42）重写了 `index.html` 的缓存戳。**归一化 `?v=` 后逐字节比对**：

```
f320fb3114 (11:37 推送版)   1340660 B  md5=00cbb8559a635f8860cf4d7f33c740a2  ?v= 次数=114  cr=0
49f8576bf  (v8 build 11:42) 1340660 B  md5=d2456a206108e99f2c829a321a44fe47  ?v= 次数=114  cr=0
FETCH_HEAD (现网 tip)       1340660 B  md5=d2456a206108e99f2c829a321a44fe47  ?v= 次数=114  cr=0

归一化 ?v= / BUILD 后 sha256：
  f320fb3114 : c659da07ef398d84bf3fa56aed000c4b6e541279850b436a73360bac5d8e0138
  49f8576bf  : c659da07ef398d84bf3fa56aed000c4b6e541279850b436a73360bac5d8e0138   一致=True
  FETCH_HEAD : c659da07ef398d84bf3fa56aed000c4b6e541279850b436a73360bac5d8e0138   一致=True
```

⇒ **三版内容哈希完全相同**，size 也完全相同（1,340,660 B）⇒ 后续 build **零内容改动**。
`?v` 单调性（skill §十一·补 判据A）：`f320fb3114` max `?v=1789954729`（09:38:49 CST）
→ 现网 max `?v=1789962517`（11:48:37 CST）= **未倒退，递增 7,788 秒** ✅

> ⚠️ **判据残留**：`git/blobs/3250da0dbde6` 现返回 **422**（该 blob 已被 GC，其后无引用）。
> **禁**把它当「改动丢了」的证据 —— 内容一致性必须走**同 blob 内容比对**（本地对象库或归一化后比对），
> **不可**依赖旧 blob 仍可寻址。

### 关键点位（现网 tip 版）

| 项 | 现网命中 | 目标 |
|---|---|---|
| 新文案「因子链 factor_chain（如放量弱势 weak_penalty）是推荐层的二次微调」 | **1** | 1 ✅ |
| 旧卡名「『已纳入因子』卡」 | **0** | 0 ✅ |
| 旧卡名「🧪 已纳入因子」 | **0** | 0 ✅ |
| 死变量 `var _PG` | **0** | 0 ✅ |
| 死注入 `data/FACTOR_PROGRESS.js`（任意出现） | **0** | 0 ✅ |
| 小九 09-21 口径同步 `08:50` | **7** | 7 ✅ 健在、未被覆盖 |
| `[10/10]` 锚点 | **34/34** | 34/34 ✅ |

---

## 二、改后三件套（逐项原始输出）

### ① 时窗矩阵对表

采集 **11:49:21 CST**，基线 = `until 2026-09-20T16:10Z` 前最后一个提交 **`a973a0f73451`**（09-21 00:09 CST，
即 09-20 23:5x 全绿基线附近；skill 记 `78bb4dca3f` 现已在 API 上 404，故按时间重解析基线）。

> 🔴 按 skill 纪律：**禁用本地 `git log --since`**（本仓浅克隆，会把边界提交报成「整棵树新增」）⇒ 走 API 逐文件比 cron 集合。

```
tip = 873cc124e2b7  (2026-09-21T03:48:37Z  v8 build: 2026-09-21 11:48)
workflow 文件数:          tip=30   基线=30
含 cron 的 workflow:      tip=21   基线=21
cron 条目总数:            tip=50   基线=50
cron 集合完全一致的 workflow: 30 / 30
⇒ 逐文件 cron 集合 100% 一致 ⇒ 矩阵无需调整

关键条目断言：
  cloud_weekly_cleanup.yml     cron=['0 13 * * 6']   ✅ 期望 0 13 * * 6（= 周六 21:00 CST）
  v8_backup.yml                cron=['0 15 * * *']   （= 23:00 CST）
  v8_cleanup.yml               cron=['0 15 * * 0']   （= 周日 23:00 CST）
  v8_algo_cloud.yml            8 档：0 0/0 12/0 13/0 22/10 10/30 14/30 16/40 8 * * *
```

**结论**：本轮只改 `index.html`（非 workflow、非数据管线）⇒ **矩阵无需调整**，且该结论以
「30/30 逐文件 cron 集合一致」为可复现证据，非印象。

#### ①-补 家机（阿狸咪）ACTIVE 任务时窗 —— 无新增、无错峰冲突

本轮**未新增/未修改**任何本机自动化任务。与云端同刻者及资源域：

| 家机任务 | 时窗（CST） | 与云端同刻项 | 资源域 | 冲突 |
|---|---|---|---|---|
| 磁盘垃圾每日清理 | 每日 23:00 | 云端 `v8_backup` 23:00 | 纯本机磁盘，**不派发、不写仓库** | ❌ 无 |
| 每周日安全清理 | 周日 03:00 | — | 同上 | ❌ 无 |
| 本机心跳上报 | 每 2h·:41 | — | 写 `~/.workbuddy/*.state.json`（仓库外） | ❌ 无 |
| 盘后算法链推进器 | 每小时 :25/:55 | 云端 8 档 | 派发算法链（**仅派发源，非本次改动面**） | ❌ 无 |
| 盘中接手看守 / 暴风盯梢 / 夜班监控 | 每小时 | — | 只读 | ❌ 无 |

判据依 skill：**「是否派发算法链 / 是否写仓库数据」**才算错峰冲突；纯本机磁盘类任务不构成冲突。

### ② 离线门禁全绿

**跑在哪棵树**：`HEAD = 873cc124e2b7`（实时 tip）＋ **覆盖线上真身 `index.html`**（blob `2870296fdc17` / 1,340,660 B / cr=0）。
staging = `E:\workspace\_fl2_gate_20260921_1150`（**仓库外**，唯一目录名，**未用 `rmtree`** 避沙箱批量删除闸）。

镜像路线：**本地对象库 allowlist 最小文件集**（`git ls-tree -r` → 白名单 `want()` → `git cat-file --batch`，
输入一律 `<rev>:<path>`），**显式排除** `backup/` `raw_data/` `legacy_v6/` `docs/ops/handover/` `*.tar.gz`。

```
[0] 实时 tip (FETCH_HEAD) = 873cc124e2b7…
[1] 全树路径数 = 6025（含大文件，将被 allowlist 过滤）
[1] allowlist 命中 = 460 文件
[2] 导出 460 文件 / 0 缺失 / 19.8 MB / 秒级
[3] 线上真身 index.html 覆盖进 staging: blob=2870296fdc17 1340660 B md5=d2456a206108e99f2c829a321a44fe47 cr=0
[4] 逐字节自证 vs 远端 blob：index.html / pre_deploy_audit.py / index_protected_markers.txt /
    align_logic_ops.py / v8_verify_layer_parity.py / update_v8.py  ✅ 一致
```

门禁原始输出（staging 内跑）：

```
============================================================
v8 pre-deploy audit（CI 自动门禁，… 2026-09-21 扩至 12 项（[12/12] 调用点定义守卫））
============================================================
  ✅ [1/8] py_compile: 194 个 .py 文件 0 错误
  ✅ [2/8] new Function: 28 个 inline script 块 0 错误
  ✅ [3/8] data 完整性: data/*.js 数量=112（上界 130（动态＝面 108 ＋ 容差 20））, 全部 > 100B
  ✅ [4/8] align_logic_ops: align_logic_ops EXIT 0（逻辑详解页与真 workflow 对齐）
  ✅ [5/8] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
  ✅ [6/8] HTML 数据引用: 178 个本地 script 引用全部存在（4 页）
  ✅ [7/8] gate 头注一致: 闸门头注与 READY_SPEC 真源一致（项数/门槛/P0 专项）
  ✅ [8/8] 心跳产物名一致: 心跳产物名大小写一致（权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移）
  ✅ [9/9] 回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
  ✅ [10/10] index 核心标记守卫: index.html 核心标记 34/34 全部在位（卡片/分节/数据源锚点）
  ✅ [11/11] defer 数据缓存守卫: defer 数据 98 个 × 结果缓存 1 个：0 处空结果固化风险
  ✅ [12/12] 调用点定义守卫: 内联 script 候选调用点 157 个：全部有定义（0 处悬空）
============================================================
🎉 12 项全部通过 → deploy 可继续          EXIT 0 (31.6 秒)

### align_logic_ops.py  ⇒ EXIT 0 (0.2 秒)
   · index.html#sec-lg      0 字符
   · index.html#sec-op      616842 字符
   · logic.html#sec-lg      527044 字符
🔧 实际有 cron 的 workflow：21 个；名单：MUST=21 / ALLOW=3
✅ 对齐校验通过：关键 workflow 均已文档化，无过期引用，名单无死登记。

### v8_verify_layer_parity.py  ⇒ EXIT 0 (0.2 秒)
: 检查 0 对，缺失 raw 77 个，缺失 data 0 个，无法校验 0 个，真错位 0 个，待重建 0 个，data 层陈旧 0 个
✅ data/*.js 与 raw_data/*.json 时间戳一致，且无陈旧静默上线风险
```

> 🔴 **证据边界（如实声明）**：`layer_parity` 的 **「缺失 raw 77 个」是 staging 设计的产物** ——
> 本机按任务要求**排除 `raw_data/`** 未镜像 ⇒ 该脚本找不到对应 raw 文件。该脚本**设计上 `missing_raw` 仅告警不失败**，
> 故 EXIT 0 有效，但**它对本轮结论的判别力有限**（本次改动不涉及任何 `data/` 或 `raw_data/` 文件）。
> **未验证项**：真实的 `data ↔ raw` 时间戳配对，本轮**未独立验证**，不做推断。

语法 0 错证据：`[2/8] new Function: 28 个 inline script 块 0 错误`（跑在**线上真身** index.html 上，非本地副本）✅

#### ②-补 与 09-21 07:12 全绿基线的数字对照（防「跨 tip 比数字」误判）

| 门禁项 | 07:12 基线（tip `481fc4dd1a`） | 本轮（tip `873cc124e2b7`） | 差 | 归因（已查证） |
|---|---|---|---|---|
| `[3/8]` data/*.js | 113 | **112** | −1 | 上游今日 **删 2 加 1**（见 §五），**非本次改动** |
| `[6/8]` HTML 引用 | 179 | **178** | −1 | 同上（`STRONG_BREAKOUT.js` 引用随卡清理） |
| `[11/11]` defer 数据 | 99 | **98** | −1 | 同上 |
| `[10/10]` 锚点 | 34/34 | **34/34** | 0 | 无漂移 ✅ |

### ③ 本件

---

## 三、Pages 真站硬 HTTP 核验（本轮家机 `github.com:443` **可达**，故做实站终验）

`curl --retry 4 --retry-delay 2 --retry-all-errors --max-time 60`，破缓存 `?nc=<epoch>`：

```
HTTP=200  size=1340660  time=0.977s
cr count = 0
  因子链 factor_chain（如放量弱势 weak_penalty）是推荐层的二次微调   = 1
  「已纳入因子」卡                                  = 0
  var _PG                                       = 0
  FACTOR_PROGRESS.js                            = 0
  08:50                                         = 7
  已纳入因子                                        = 3
  第二步                                           = 3
  ?v= 次数 = 114   max ?v= = 1789962517
```

⇒ **真站字节数与仓库 blob 完全相同（1,340,660 B）**，9 处改动在生产生效，
小九 7 处口径同步（`08:50`×7）健在。**这是硬 HTTP 实证，非状态延续推断。**

---

## 四、🔴 归因更正：真实删除理由是「**源全空 + 口径重复**」

**删除依据（本件独立复测远端真值，非引用他人结论，采集 11:47 CST）**：

```
data/FINAL_RECOMMEND_DATA.js  25372 B   update_time = 2026-09-20 17:58:27
  factor_chain 键 = ['update_time','score_formula','where','gate','integrated','skipped','skip_reason']
  factor_chain.integrated 条数 = 0
  factor_chain.skipped        = True
  factor_chain.skip_reason    = 本轮 FACTOR_LAB.js 缺失/内容陈旧 ⇒ 方案B 因子融合整体跳过（数据降级）
  factor_trace 条数           = 0
data/FACTOR_PROGRESS.js       6446 B   update_time = 2026-09-20 16:42:46
  factors 条数 = 0                       （summary 16 项、factors 0 条）
data/FACTOR_LAB.js  ⇒ 存在 16846 B（⚠️ **现在在**，说明 skip_reason 的「缺失/陈旧」是**生成当刻**的状态）
```

**真实删除理由 = 「① 数据源全空（第二步内容恒空）＋ ② 与既有卡口径重复」**，而**不是**「无生成器 / 无数据源」。

> 🔍 **澄清（本件为防误引而写）**：我在 `docs/ops/handover/` **全目录检索** `无生成器` / `无数据源`，
> 命中 **14 档**，其中**唯一**把该短语用于「某卡被删/降格」的上下文是
> `2026-09-20_1100_xiaojiu_to_alimi_lookat3_p2p5portrait_fix_and_overwrite_incident.md`
> （L14 / L65：「无生成器 / 无数据源 / 无定时任务的静态快照」= **「已否决 · 全市场画像因子」卡**）。
> **没有**任何一档把该理由安在**本卡**（因子实验室·第二步 / 已纳入因子）头上 ⇒
> **本更正属「预防性澄清」**，避免后人把两张卡的删除理由混用。
> 依据铁律「判据『文案 vs 实现』必交叉核」，此处**只写实测到的事实**，不替他人背结论。

---

## 五、残留命中定性：8 处**全部在注释内**，无真漏项

| 串 | 命中 | 行号 | 所在形态 | 定性 |
|---|---|---|---|---|
| `已纳入因子` | 3 | L1914 / L15524 / L17379 | HTML 注释 / JS 行注释 / JS 块注释 | 有意留档（历史沿革 + 删除依据） |
| `第二步` | 3 | L1914 / L15524 / L17379 | 同上 | 同上 |
| `__renderFactorLab` | 2 | L8377 / L8379 | JS 块注释 | 09-20「恢复被 `0d3482be4` 误删的 `_renderPostReady`」修复说明中对函数名的引用 |

`_renderPostReady` 实际调用链（L6200 定义 / L8380 定义 / L8385 挂载）：**已无 `__renderFactorLab` 调用**。
⇒ 依 skill §5.8：**命中在注释里、且描述「此处已删除 / 历史沿革 / 删除依据」= 有意留档，不是漏项**；
**判据修正**：子串计数判据必须显式区分「活代码」与「注释 / 字符串字面量」，否则会把有意留档误报成清理不彻底。

---

## 六、本机 clone 状态（只登记，不擅动）

```
本地 HEAD vs FETCH_HEAD (left=本地独有 right=远端独有): 106	2
```

家机生产工作区落后且含大量在途数据脏文件 —— 按纪律**只登记不 `reset` / 不 `pull` / 不 `checkout`**。
本轮全部证据取自**远端 API / 本地对象库 blob / Pages 真站**，不依赖工作树状态。

---

## 七、附：本日 `data/*.js` 计数 113→112 的归因（**非本次改动**）

```
09-20 23:5x 基线 (a973a0f73451) = 113
现网 main                       = 112
基线有/现网无 (删除) = 2  ['data/STRONG_BREAKOUT.js', 'data/STRONG_BREAKOUT_BACKTEST.js']
现网有/基线无 (新增) = 1  ['data/maharo_macro.js']
```

与今日两档交接件吻合（`2026-09-21_0715_小九_强势突破残留产物清除与防回推防红灯复发双层护栏.md`、
`2026-09-21_0820/0855_机构研究卡…maharo任务…`）⇒ **上游小九侧改动**，与本次 `index.html` 单项改动无关。

---

## 八、纪律声明

- 本次**未推送 `index.html`**（已在线上）；推送脚本幂等短路若输出「✅ 线上已是本 patch 版本，幂等跳过」= **预期行为，不是失败**；
  **未 force、未重推、未重复改动**。
- 本件为**唯一新增文件**，走 Git Data API：实时重取 tip → `base_tree` 只列本件一个路径 → `force:false` → **推后回读逐字节自证**。
- staging 在**仓库外**，唯一目录名，未用 `rmtree`（避沙箱批量删除闸）。
- 全程**北京时间**（Python `timezone(timedelta(hours=8))` 实取，禁推算）。
- **未验证项如实声明**：§二②「缺失 raw 77」的真实 `data↔raw` 配对未独立验证；本机生产工作树仍落后 106 提交未对齐。

---

**阿狸咪的工程师** · 家机 `alimi-cn` · 2026-09-21 11:53 CST
