# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-18 23:40｜逆势龙头星级根因与三条逻辑错误修复及IMA停更事故

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主人令「逆势龙头凭啥评星 / 没进最终推荐吧」经 node 实测坐实：**tab 上那个 4.63⭐ 是假的** ——
它不是「算错一格」，而是**闸门口径允许一期大样本掩护其余小样本期**（真正参与加权的 T+5/T+30
各只有 4 笔，68% 的分数压在 4 笔上），且同页四处文档都写着「不评星」。本轮已一劳永逸修掉
**三层根因**（闸门 maxN→minActiveN、登记表语义纠正、同名统一），并把 IMA 链的**停更事故**
（真凶是我自己 09-17 晚加的过严护栏）一并解决。两个 commit：`736601c70db5`（星级）、
`2f8707cd3171`（IMA）。

---

## 一、逆势龙头 4.63⭐ 的完整根因链（全部 node 实测复现，非推理）

### 1.1 四处文档都说「不评星」，tab 却显示 4.63⭐
| 位置 | 原文 |
|---|---|
| `logic.html` L1580 | 「逆势龙头 CRDS 是专用场景…所以**不挂星级**」 |
| `logic.html` L8215 | 「小样本策略（如逆势龙头CRDS 当前 **n=4**）**不计入排名以免噪声误导**」 |
| `index.html` L3098（卡内） | 「09-06 审计修复：弱市专用**不评星**」 |
| `index.html` L2894（今晚 22:0x 另一会话改的注释） | 自述「实算 **2.61⭐**(n=87,cov95%)」——**连它自己的自述都错** |

### 1.2 复现 4.63（可逐位复算）
`short` 档权重 `{T+1:0.35, T+5:0.35, T+30:0.25, T+90:0.05}`，命中 T+1/T+5/T+30
（T+90 全 0 样本 ⇒ 权重不计入 cov）：

```
score = (5×0.35 + 5×0.35 + 5×0.25) / 0.95 = 4.400 / 0.95 = 4.6316 ≈ 4.63 ⭐
```

### 1.3 真凶 = 闸门口径 `maxN`（不是四舍五入、不是渲染 bug）
- `maxN = 85` **全部来自 T+1**；真正参与加权的 **T+5 / T+30 各只有 4 笔**（合计权重 **0.60**）
  ⇒ **68% 的分数压在 4 笔样本上**。
- 同一份产物里，它的 `is_primary` 主口径行 **T+5(n=4) 已被自己标为**
  「样本 4 < 门槛 30 → 累积中，**不进主表排名**」
  ⇒ **主表说不排名、tab 显示 4.63⭐** —— 同一份数据两个相反结论。

### 1.4 「它没进最终推荐吧」——主人判断正确（但要区分机制）
`algorithms/final_recommend.py` L842：
```
# 5) 逆势龙头：仅在危机雷达高位时并入
```
今日实测 `crisis_score = 32.2 < 50`、`crisis_note = "危机雷达未达高位，逆势龙头暂不并入"`
⇒ **今天确实没进**。但机制上是**条件性并入**（危机≥50 才进），不是「永不并入」
—— 故「不进最终推荐」不能作为「不评星」的判据；真正的判据是**样本不足 + 专用场景**。

---

## 二、顺带查出的另外两条逻辑错误（都已修）

### 2.1 `PROFILE_OF[card] || 'medium'` 回落陷阱 ⇒ 「撤销登记」是假的
`__STAR_PROFILE_OF[card] || 'medium'`：表里查不到时**回落到默认档**，于是
「把某卡从表里删掉」只是**从显式变隐式，评级照旧**。实测（撤销后重算）：
| 卡 | 撤销登记后仍算出 | 说明 |
|---|---|---|
| 因子实验室 | **5⭐**（score 4.32） | 昨日据注释宣称的「已不评星」**不成立** |
| K线信号层（09-16 已从表删除） | **4⭐** | 同上 |

**修法**：把表语义**定正为「参评资格登记处」** —— 未登记 ⇒ `kind='noreview'` ⇒ 不评星级。
⚠️ 落点必须放在 **`if(kind === 'pool')` 分支之后**，否则会把候选池/金股池也判成 `noreview`，
丢掉「基础股池·只验入选门槛」+「胜率<45% 列入观察」（v2 原型第一版已踩此坑，已回归）。

### 2.2 同一策略两处不同星（取星 key 不同源）
- 回测卡用 `m.name`（= `'逆势龙头CRDS'`）
- 星级表 key 用 `'逆势龙头'`
⇒ `__renderBacktestCardsFrom` 取星取不到 ⇒ **回落默认 medium 档** + `__committeePeriods`
找不到同名行 ⇒ 该处显示「累积中」，而 tab 显示 4.63⭐。
**修法**：加 `starKey:'逆势龙头'` + 渲染侧 `m.starKey || m.name`（**同名统一**）。

---

## 三、`logic.html` 内部自相矛盾（已修）

| 位置 | 三处硬编码 | 实算（node 复现） |
|---|---|---|
| L1567-1569 | 三重⭐⭐⭐⭐⭐ / 四量⭐⭐⭐⭐ / 生命周期⭐⭐⭐ | 三重 **4⭐(3.30)** / 四量 **5⭐(4.49)** / 强势跟踪 **4⭐(4.51)** |
| L1659 | 四量⭐⭐⭐⭐⭐ / 三重⭐⭐⭐⭐ | **与 L1567-1569 互为颠倒** |

⇒ 同一页两处**写反**；且「选股生命周期」已于 09-16 改名「**强势跟踪**」。
本轮 7 处（T1~T7）：表头补口径说明、三行硬编码改为与实算一致、两处说明段去硬编码、
子页顺序行同步。

---

## 四、交付与验收

### 4.1 交付 commit `736601c70db5`（2 文件，逐字节回读一致 ✅）
| 文件 | 改动 | 改后 blob | 体积 | 行尾 |
|---|---|---|---|---|
| `index.html` | **12 处**（S1~S12） | `86eaee1f9205` | 1,095,431 B | LF（CRLF=0） |
| `logic.html` | **7 处**（T1~T7） | `50fcde4d15e1` | 582,028 B | **CRLF=8426 / bareLF=0** ✅ |

- **S1** 登记表=参评资格（置于 pool 分支**之后**）｜**S2~S5** 收集 `minActiveN` + 闸门口径
  （`gateN < 30` 逐一期判定，`<10` 记 `insufficient`）｜**S6** 渲染加 `noreview` 分支
  + `insufficient ? '不可评级' : '累积中'` 分流（保留 `__betaDepHTML` 角标）
  ｜**S7/S8** `starKey` 同名统一｜**S9~S12** 注释更正（含 2.61⭐/maxN=4 的错误归因）
- 推前硬校验：19 处锚点全 `count==1`；`index.html` **28/28** + `logic.html` **6/6**
  内联 `<script>` `node --check` PASS；标签净差前后**全部未变**（判据 113/123）。

### 4.2 线上真值复算（用**远端 blob 提取**的引擎与渲染函数，非本地）`EXIT=0 全部通过`
```
逆势龙头  → 不可评级             (原 4.63⭐)
因子实验室 → 不评星级⏳ 升级中     (原 5⭐)
K线信号层 → 不评星级             (原 4⭐)
三重共识  → 3.3⭐   四量终极 → 4.49⭐   强势跟踪 → 4.51⭐   （★与 score 逐字节未变）
候选池/金股池/黄金池 → 「基础股池」专用提示（零误伤）
```

### 4.3 🔴 判据 84 首次落地（本仓并发推送的现实约束）
本仓有 **11 个 workflow + 多会话**高频推 main。我原先的「tip 级等值」判断
**连续两次被无关提交顶掉、白白重跑**（`0b9321cf5f32 → b8446581b3ac → ba589fb1139a`）。
改为 **目标文件 blob 级漂移检测 + `base_tree` 取最新 tree** 后，**首次运行即精准拦下**
`index.html(blob=ac936e1893ba)` 真漂移；复核证实仅 `?v` 缓存戳刷新 + BUILD sha
（**等长** 1091115→1091115、与锚点**零交集**），重跑即成功。
⇒ 今后一律：**只检测目标文件是否漂移，不做 tip 级等值判断**。

---

## 五、IMA 强势股停更事故（ISSUE-IMA-01）—— **真凶是我自己 09-17 晚加的护栏**

### 5.1 现象
- `data/IMA_STRONG_STOCK.js` `update_time` 卡在 **09-17 20:50:19**，停更 >26h。
- `v8_ima_strong_stock.yml` **09-18 两跑全红**（`07:51Z` workflow_dispatch、`12:22Z` schedule），
  第 6 步 exit 1，后续推送 skipped。上次 success = `09-17 12:47Z`。
- 失败日志原文（两次逐字相同）：
```
❌ 产物自检失败 —— 拒绝写出坏产物：
   · field_missing_counts 非空 {'base_price': 2, 'latest_price': 2, 'change_pct': 2, 'drawdown_pct': 2}（解析串位复发）
```

### 5.2 根因 = **判据 114（注释即规格）违规 —— 护栏比规格更严**
- 脚本自身 L164 注释（我 09-18 07:08 提交 `be2837a8d400` 时写的规格）明说：
  「修复后 first_selected **112/112**，缺失归 0（**仅剩 2 条真实无价的记录**）」；
- 而 L415-417 护栏写 `if parse_health.get("field_missing_counts"): 拒写`（**恒 0 才放行**）；
- 叠加**语义漂移**：`_take_field` 对**空串是消费指针**，故一次 miss 的含义已从「串位」
  变成「该格**非空**但非预期类型」—— 正是「无报价」的形状，旧文案已失效。
⇒ **规格说允许 2 条，护栏说必须 0 条** ⇒ 自相矛盾，护栏错。上线后**首跑即把整条链打死**。

### 5.3 修法（`2f8707cd3171`，blob `7e4494ae6c8b`，21594 chars，逐字节一致 ✅）
1. **留原始 token 证据**：`missvals[field] = lines[j]` → 产物 `field_missing_samples`；
2. **抽纯函数 `classify_residue()`** 做分级（可被回归用例直接调用，不必「跑整条 CI 才知道对不对」）；
3. **良性也不静默**：写入 `parse_health.no_price_records / no_price_codes` + 打印醒目 warn。

**分级判据（全部满足才算良性）**：① 缺失**只有**那四条价格字段（任何结构性字段缺失 ⇒ 红）；
② 四条**缺失条数完全相同**（不同 ⇒ 各自串位 ⇒ 红）；③ 缺失数 ≤ `max(2, 5%)`（超 ⇒ 列序变了 ⇒ 红）。

**回归用例 6/6 通过**：CI 实测形状（各缺 2 / 总 112）⇒ 放行 ✅；修复前形状（72 条六字段塌陷）⇒ 红 ✅；
纯结构字段缺失 ⇒ 红 ✅；价格缺失条数不等 ⇒ 红 ✅；超 5% 容差 ⇒ 红 ✅；无缺失 ⇒ 非良性 ✅。

### 5.4 端到端验收（手动 `workflow_dispatch` run `35362665607` = **success** ✅）
| 项 | 09-17 旧版 | **09-18 23:30 新版** |
|---|---|---|
| `update_time` | 09-17 20:50:19 | **2026-09-18 23:30:29** |
| `no_first_selected` | **72（64.3%）** | **0（0.0%）** |
| `field_missing_counts` | 6 字段塌陷（各 72/70） | 仅 4 价格字段各 **2** |
| `benign_residue` / 留证 | — | `true` / `no_price_codes=[["066000","诚志股份"],["920078","族兴新材"]]` |

**🔴 原始 token 的真相（比预想更硬）**：`field_missing_samples = {"base_price": ["None","None"], ...}`
⇒ 源笔记那两个格子里**真的写着字面量 `None`**（`to_float("None") → None` ⇒ 判非数字）。
⇒ 「仅剩 2 条真实无价」的规格被**实测坐实**，护栏当初就该放行。
同时 `source_stale=true / stale_days=16 / data_date=2026-09-02` ⇒ **源侧停更 16 天是真实存在的**
（与「抓取失败」是两件事，现已彻底分离）。

### 5.5 连带影响与后续
- `algorithms/backtest_life_cards.py` L253/L274：读 `raw_data/ima_strong_stock.json`
  → 产出 `ima_strong_backtest.json` → `data/IMA_STRONG_BACKTEST.js`。
  该产物至今仍是 **09-17 20:50:19 / `skipped_no_signal_date = 72`**
  ⇒ **强势跟踪的 4⭐（score 4.51）建立在只剩 40/112 的偏乐观样本上**。
- 修好后 `first_selected 112/112`（实测见 §5.4）。
- 🔴 **2026-09-19 01:0x 更正：本段原有结论「下游不会自动重算」是错的，已被实测推翻。**
  - 真值：`data/IMA_STRONG_BACKTEST.js` 已变成 `update_time 2026-09-19 00:45:52`、
    `skipped_no_signal_date = 0`、`signal_days = 44`、`ima_update_time = 2026-09-18 23:30:29`
    ⇒ **已经自动吃到新产物并重算**；
  - 写入方 = **`v8 cn fetch` 那一班**（commit `b742045cd0`，16:46:19Z = **00:46 CST**，
    作者 `github-actions[bot]`，msg `v8 cn fetch: 2026-09-19 00:46`）；
  - 机制 = `update_v8.py` **L1399-1412 的 `experiment` 段无条件跑 `algorithms/backtest_life_cards.py`**，
    而 `update_v8.py` 就挂在 cn fetch 里（其提交消息由 `api_push_raw.py` L884 生成）；
  - ⇒ 判据 131 只说明「**算法链**（A/B/D/E）会空转」，**不等于全站没有别的写入方**。
    判「某产物会不会跟上」**必须查该文件 `commits?path=` 的作者 + 消息 + 时间**。
  - 审计留痕（该事实仍有效）：run `35356355660`（22:27 CST schedule，success）问责行原文 ——
    ```
    问责输入: 目标批=NONE 闸门放行=true 执行标志=false 算法步骤=skipped
             数据日=2026-09-18(trading) CST=22:30 B就绪=11/12(true) D最终推荐=1/1(true)
    ```
- ⚠️ **但星级并没有跟着变** —— 真缺口在**另一条链**上，见 **§八（遥测分叉 · ISSUE-IMA-02）**：
  `data/IMA_STRONG_BACKTEST.js` 已是最新（见上），但 **聚合表 `data/BACKTEST_ALL_ALGOS.js` 停在 20:47**
  ⇒ **强势跟踪 4⭐（4.51）当时仍建立在 40 条旧样本上**。
  🔴 **02:0x 更正**：「聚合器读 raw_data」**是错的** —— 聚合器读的是 `data/IMA_STRONG_BACKTEST.js`，
  详见 §8.2（含读者枚举与真实机制）。分叉真实存在，但对星级 0 影响。

---

## 六、待对齐 / 回执请求

1. 🔴 **交接件命名漂移（我方违规，需定权威口径）**：`docs/ops/scripts/new_handover.py`
   明确规范 `YYYY-MM-DD_HHmm_<发件>给<收件>_<主题>.md`，并在注释里点名「手写会漂移
   （历史上有 `HANDOVER_`/`URGENT_`/纯日期/带 `?` 等 8 种写法）」。而**我方今晚 21:25 / 21:30
   两份是手写的 `HANDOVER_...` 旧式**（漂移复现）。本轮起一律改用生成器口径。
   两份旧名件我**不擅自改名**（遵「改历史比不一致更糟」）—— 若要统一，请回执告知。
2. `v8_ws_sync_guard.py` L49 `REPO = r"E:\workspace\stock-scanner"` 是**本机专用守卫**
   （防坚果云从另一台机覆盖本机工作区）。云端 `ubuntu-latest` 无该路径 ⇒ 日志恒打印
   `[ws-guard] ⚠️ ... 不是 git 仓库 → 跳过（不阻断）`。**经核为设计如此、非缺陷**
   （避免被误判成「静默失效的守卫」），但建议**日志措辞**区分「路径不存在」与
   「存在但非 git 仓库」，以免日后误诊。请确认是否值得改。
3. `data/IMA_STRONG_BACKTEST.js` **与** `raw_data/ima_strong_backtest.json` **两条**的
   `ima_update_time` / `skipped_no_signal_date` 实测值（见 §5.5 更正 与 §八.7）。
4. `logic.html` / `index.html` 星级文案如需再对齐，请以**实算值**为准（三重 3.30 / 四量 4.49 /
   强势跟踪 4.51 / 逆势龙头 不可评级），**不要再写硬编码星数**。

---

## 七、自证

- 本轮三查：`tasklist`/进程、`update_time` 与行数、远端 `FETCH_HEAD` 与 blob sha —— 均以
  **远端 blob 为唯一真值**；`_api.py` 走 **curl 子进程**（urllib 对大响应稳定 `IncompleteRead`）。
- 全部补丁：远端读 → 内存替换（每处断言 `count==1`）→ 语法校验 → Git Data API
  （`base_tree` + `force=False`）→ **逐字节回读**。**未碰本地工作树、未用 `git add/commit/push`**。
- 行尾：`index.html` LF / `logic.html` CRLF=8426（bareLF=0）/ 本交接件 LF —— 全部字节级核验。
- 署名：**阿狸咪的工程师**（家机 `alimi-cn`）。

---

## 八、⚠️ 2026-09-19 01:4x 追加：新揪出真缺口「遥测分叉」+「备通道被遮蔽」（**ISSUE-IMA-02**）

> §五 修完 IMA 抓取（`skipped 72 → 0`）后我复核下游，发现**星级并未跟着变**。追下去是下面这条链。

> 🔴 **2026-09-19 02:0x 自我更正（重要）**：§8.2 原写「聚合器读 `raw_data/ima_strong_backtest.json`」
> **是错的** —— 实测 `gen_backtest_all_algos.py` 的 `SOURCES` 登记的就是 `data/IMA_STRONG_BACKTEST.js`。
> 「遥测分叉」这个现象**真实存在**，但对**星级与健康检查 0 影响**。§8.2 已重写为读者枚举 +
> 真实机制（写入点排在推送点之后），§8.4/8.5/8.7 同步更正。

### 8.1 现象：两个产物互相矛盾（实测真值）

| 文件 | 内嵌 `ima_update_time` | `skipped_no_signal_date` | n(1d) |
|---|---|---|---|
| `raw_data/ima_strong_backtest.json` | **2026-09-17 20:50:19**（旧） | **72** | **40** |
| `data/IMA_STRONG_BACKTEST.js` | **2026-09-18 23:30:29**（新） | **0** | **83** |

### 8.2 ✅ 自我更正：这条「第一层根因」**不成立**（我 01:4x 的因果判断错了）

🔴 **「聚合器读 raw_data」是错的，此处当场推翻。**

`algorithms/gen_backtest_all_algos.py` 的 `SOURCES` 表 **L145-151** 明确登记
`var="IMA_STRONG_BACKTEST", rel="data/IMA_STRONG_BACKTEST.js"`，而 `build()`（**L738-739**）读的是
`load_js_var(src["rel"])` ⇒ **聚合器吃 `data/`，不吃 `raw_data/`**。
我上一轮被 commit `b742045cd0`（16 个文件全在 `data/`）误导 —— **把「某次 commit 的面」当成了「读取路径」**。

**两份产物各自的真实读者**（实测枚举：树内 27 个校验/闸门/桥接类脚本逐个 grep）

| 文件 | 读者 |
|---|---|
| `data/IMA_STRONG_BACKTEST.js` | ★ `algorithms/gen_backtest_all_algos.py`（E 批聚合器，`rel=` 就是它） |
| `raw_data/ima_strong_backtest.json` | ① `algorithms/backtest_life_cards.py`（**写出方**）② `update_v8.py` **L143** 映射（raw_data→data 桥接） |

除上述两条**全仓无其它引用**；`v8_health_check.py` L207 的 `IMA_STRONG_BACKTEST`
（`max_age=1440`、`key_fields=["periods"]`）看的也是 `data/`。

⇒ **分叉对星级 0 影响**：`data/IMA_STRONG_BACKTEST.js` 本身是新的（`update_time 2026-09-19 01:41:10`、
`ima_update_time 2026-09-18 23:30:29`、`skipped=0`、`periods.1d.samples=83`）。
真正的现状只是 **`data/BACKTEST_ALL_ALGOS.js`（20:47:41）早于它 ⇒ 聚合表尚未重跑**（见 §8.4）。

**分叉的真实机制（结构性，定位到行）**：`v8_cn_fetch_cloud.yml` 步骤次序

| 行 | 步骤 | 关键动作 |
|---|---|---|
| L497 | 📤 推送 raw_data/ 到 main | `MANIFEST=$(git status --porcelain raw_data/ data/)` |
| L536 | 🔗 桥接 raw_data | `bridge_raw_data.py` |
| **L555** | **🔄 重生成 data/*.js** | `update_v8.py` —— 其 experiment 段（**L1399-1412**）调 `backtest_life_cards.py`，**在此重写 `raw_data/*_backtest.json`** |
| L607 | 🧪 校验 data 与 raw_data 时间戳一致 | — |
| L644 | 📤 推送重建的 data/*.js | 清单是 `git status --porcelain data/` —— **不含 raw_data** |

⇒ update_v8 重写的 raw_data **落在推送点之后**，此后无任何步骤推 raw_data ⇒ 只能等**下一轮** L497 的
`git status` 收走；而云端 `ubuntu-latest` runner 是一次性的（下轮 checkout 干净）⇒ **结构性不可持久化**。
**同一类事故本仓已记载过一次**（同文件 L543-545）：原「抓取平均股价真实指数 880003」步就因排在推送点
之后、「写出的 `raw_data/avg_price_data.json` 与 `_avg_price_cache.json` 从未被推回仓库」而被**整步摘除**。

⚠️ **尚未证实（按「不许说谎」标注为待查，不作结论）**：`raw_data/ima_strong_backtest.json` 上一次成功
推送是 20:59（commit `a233291629e4`，msg `v8 cn fetch: 2026-09-18 20:59`）。云端一次性 runner 解释
不了这次成功，**最可能是自托管 runner 的持久工作区**把早前写入的那份顺手推了上去 —— 我**没有查该
commit 的 `runner_name`**，故不下结论。

### 8.3 第二层根因（更隐蔽）：星级引擎里「强势跟踪」的**备通道已被主通道完全遮蔽**

`index.html` 的 `__STAR_ALT_SOURCES['强势跟踪']` 是 **09-17** 为「该卡在聚合产物 rows 里没有行」
而补的备通道；但 **09-18 起** `BACKTEST_ALL_ALGOS.rows` **已含 `card='强势跟踪'` 的 13 行**，
键集与备通道**完全相同**（1/2/3/5/10/20/30/45/60/75/90/180/250），
而 `__committeePeriods` 的合并逻辑是 `if(out[k]) return;`（**主通道优先**）⇒ **备通道 0 贡献**。

**node 实测（线上引擎 + 真产物，逐位可复算）**

| 场景 | stars | score | maxN | minN | periods（n） |
|---|---|---|---|---|---|
| ① 原样（线上真值） | 4 | **4.51** | 40 | 32 | 1:40 2:40 3:40 5:40 10:38 20:36 30:32 45:14 60:4 |
| ② 抹掉 `IMA_STRONG_BACKTEST`（断备通道） | 4 | **4.51**（**一字不变**） | 40 | 32 | 同上 |
| ③ 抹掉主通道 `card='强势跟踪'` 行（断主通道） | 4 | **4.82** | 83 | 52 | 1:83 2:83 3:83 5:81 10:77 20:65 30:52 45:26 60:4 |
| ④ 主通道行**换成新 IMA 样本** | 4 | **4.26** | 83 | 52 | 同上 |

⇒ 该块原注释「真值源 IMA_STRONG_BACKTEST」**与生效路径不符**（备通道贡献为 0），属同一类
**「注释≠真值」**问题（本轮第 4 次）。**本次已一并更正该注释**（`index.html`，**仅注释、零行为变更**）。

### 8.4 后果与预测（**不是 bug，是「数字反映的不是当前样本」**）

- **现状**：线上显示 `4.51⭐`，数据源 = **40 条旧样本**（`data/BACKTEST_ALL_ALGOS.js` @20:47 里 `card='强势跟踪'` 的行；其上游 `data/IMA_STRONG_BACKTEST.js` **已经是新的**，只是聚合器尚未重跑）；
- **聚合器刷新后（预测，由实测场景④得出）**：`score 4.26`，`stars` **仍 4★**
  （`maxN=83 < 100` ⇒ 样本量封顶未触发，短周期封顶亦未触发）
  ⇒ **可见变化 = 数字 4.51 → 4.26（−0.25）**，星数不变。

### 8.5 自愈周期与影响面界定（**不夸大**）

- **星级那条线**：E 批就绪的 `must` 实测 = `["data/BACKTEST_ALL_ALGOS.js", "data/BACKTEST_TDX.js"]`
  （`.github/scripts/v8_stage_gate.py` **L306-323** 的 `READY_SPEC["E"]`，`need=2`），
  而 `read_ut()`（**L369-386**）读的是**文件头 20 KB 内的 `update_time`** 再比对 `FLOOR_TRADING=(16,30)`
  ⇒ 聚合表**本数据日已写过**（实测 `20:47:41`）即算「E 已就绪」⇒ **同一数据日内**不因上游刷新而重跑 E
  （**不是**按「文件是否存在」判 —— 我先前的措辞不精确，此处更正）；
  但**新数据日必然重跑 E**，届时聚合表吃到 01:41 的新 IMA 产物
  ⇒ **自愈周期 = 1 个交易日**（**09-21 周一自动修正**），**不必人工干预**。
- **遥测那条线**：`raw_data/ima_strong_backtest.json` 的陈旧**不会自愈**（下一轮云端 runner 同样丢），
  但它**不影响星级、也不影响健康检查**（读者枚举见 §8.2）。要真正持久化，需要 §8.2 表格里那个
  **一行改动**（L644 清单加 `raw_data/`）—— 我**未擅自改**，理由见 §8.7 第 3 条。

### 8.6 我为什么**没有**派发强制重算（请小九裁定）

- 01:00 CST 起有 run **`35371751539` 正在跑**（runner **`alimi-cn`**），
  第 8 步闸门 `success`、**第 9 步「🧮 运行盘后算法链」`in_progress`** ⇒ **未空转，正在跑某一批**；
- 并发组 `v8-algo-cloud` 为 `cancel-in-progress: false` ⇒ 排队**不取消**（不会丢产物），
  但**pending 会被后续 cron 顶掉**；且 **06:00 / 08:00** 两档唤醒将至
  ⇒ 此刻派发有踩踏风险（主人令「不要造成踩踏和暴风」）；
- 按 §8.5 该缺口**周一自愈** ⇒ 我选择**不派发**，只留一键命令：
  ```bash
  # 需要「即刻」补跑时的最小操作（stage=E 只跑回测批；force_run=true ⇒ 追加 --force）
  gh workflow run v8_algo_cloud.yml -f stage=E -f force_run=true
  # 或 API：POST /repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_cloud.yml/dispatches
  #         {"ref":"main","inputs":{"stage":"E","force_run":"true"}}
  ```
  **请小九裁定**：① **等周一自愈**（推荐，零风险）；② **即刻派发 E**（须先确认无在跑 run）。
- ⏳ **实况兜底（02:0x 记录）**：run `35371751539`（01:00:13 CST 起，runner `alimi-cn`）仍在第 9 步「🧮 运行盘后算法链」（已 50+ 分钟）。**若它恰好是 E 批**，聚合表会在它推送时被刷新，§8.4 的预测即成既成事实 ⇒ **一律以跑完后 `data/BACKTEST_ALL_ALGOS.js` 的实测值为准**（判据是**聚合表**里「强势跟踪」行的 `sample`，**不是** raw_data）。

### 8.7 回执请求

1. **判据是聚合表**：`data/BACKTEST_ALL_ALGOS.js` 里「强势跟踪」行的 `sample` 是否已从 **40** 变为
   **83** 级别（聚合器读 `data/IMA_STRONG_BACKTEST.js`，**不读** raw_data —— 见 §8.2 更正）；
2. 若已刷新，请重算 `__committeeRate('强势跟踪')` 的新 score，与 **4.51** 比对（预测 **4.26**）；
3. **请小九裁定** §8.2 那处**一行改动**（L644 的清单由 `git status --porcelain data/` 改为
   `... raw_data/ data/`）是否落地。我**未擅自改**，三条理由：
   ① 该分叉对星级与健康检查 **0 影响**（§8.2）；
   ② L607 那道「data/*.js 与 raw_data/*.json 时间戳一致」的校验我**尚未读源码**，改动前必须先确认
      它不会因 raw_data 变新而反向报红；
   ③ 此刻（02:0x）有 E 批在跑，改 `.github/workflows/` 属高风险面（须 classic token），
      不宜与在跑跑批同窗口落地。

### 8.8 顺带更正一条：`in-progress` run 取不到日志

`GET /actions/runs/{id}/logs` 在运行中返回**非 zip**（~152B JSON）⇒ `zipfile` 报
`File is not a zip file`。⇒ 要**即时**判「闸门是否空转」，只能看
`/actions/runs/{id}/jobs` 的 **step 状态**（step 8 `success` + step 9 `in_progress` ⇒ 未空转）。

---

## 九、🔴🔴 追加（09-19 02:45 · 阿狸咪的工程师）：挖到「数据不更新」的**最上游真因** —— 僵尸看门狗误杀长跑批（已修）

> 本节推翻本文件 §5.5 / §8.4 / §8.5 的一条**共同前提**：「下游会自动重算 / 1 交易日自愈」。
> 它们描述的是**下游症状**；真正的上游真因见下，已在本轮修复并上线。

### 9.1 事故（时刻级实测）

run `35371751539`（**09-19 01:00:07 CST** 创建，runner `alimi-cn`）跑 **B 批（选股批）** 至第 **77 分钟**：

| 步骤 | 结果 |
|---|---|
| 9 🧮 运行盘后算法链 | **completed / cancelled**（18:17:04Z） |
| 10 🔗 桥接 · 11 🔄 update_v8 · 12 🩺 health · 13 📈 候选池行情 · **14 📤 唯一推送** · 15 🔄 ?v · 16 🛡 机器校验 | **全部 skipped** |
| 17 🛡 结果问责 | **failure** |

⇒ **77 分钟计算成果全丢、零推送**。（本机 runner 工作树证明它并未挂死：`raw_data/algo_heartbeat.json`
每 ~30 秒刷新、`stock_quote.json` 01:53 落盘 —— 是**正常计算被中途掐断**。）

### 9.2 真因：`cloud_dispatcher.py` 僵尸看门狗的**前提被实测证伪**

`kill_zombie_stale()` 以 **`run.updated_at`** 判「静止」，其 docstring 断言：
> 「判定用 updated_at 陈旧度（而非创建时间）：**正常 run 的 updated_at 会随步骤推进持续刷新**」

🔴 **该断言在本仓为假**：该 run 活跃推进 **73 分钟**期间，`updated_at` **恒为 `17:00:14Z`（＝启动时刻）**
—— GitHub **只在状态跃迁时**刷新它，**步骤推进不刷新**。

⇒ 任何以 `updated_at` 判死的阈值，都无法区分「跑得久」与「跑挂了」；而 B 批首位
`v8/factor_lab_gen.py` 冷启动 **50~90 分钟**（`algorithms/run_algorithms.py` L349 自述）
⇒ **结构性必然超过 60 分钟阈值 ⇒ 必被误杀**。

### 9.3 踩踏时刻表（同窗口第二重）

| 时刻 CST | 事件 |
|---|---|
| 01:45:54 | run `35376253660` **排队** |
| **02:16:37** | 它 **cancelled**（排队 31 分钟被顶掉） |
| **02:16:52** | 有人 `workflow_dispatch` 新建 `35379256842`（actor=`ah-quant999`） |
| **02:17:04** | **在跑的 `35371751539` 收到服务端取消令牌 → Cancelled** |

第二重机制（`cancel-in-progress: false` **只保护「正在运行」的 run**，新 run 到达时**旧 pending 被无条件顶掉**）
`v8_cn_fetch_cloud.yml` L82-90 已自述：**09-18 全天 39 轮 11 轮 cancelled（28%）**。

### 9.4 已上线修复 `ac3add9aa168`（2 文件，一次提交）

| 文件 | blob | 改动 |
|---|---|---|
| `.github/scripts/cloud_dispatcher.py` | `bd82dc14f600` → `d6f25c750519` | stale 降级为「疑似」+ 追加年龄硬条件 `ZOMBIE_MAX_AGE_MIN = 370`（> job 级 `timeout-minutes: 360`）；放行时显式打印；订正被证伪的 docstring/注释 |
| `.github/workflows/v8_algo_cloud.yml` | `ce4877be7dd3` → `df6955fd96a5` | concurrency 补 `queue: max`（对齐 cn fetch 09-18 已验证修法） |

**依据**：09-02 起 job 级 360 / 第 9 步 320 分钟超时**早已接住真挂死**；本看门狗写于 **08-24**（当时尚无步级超时），
其「抢在 19:15 生成前释放并发槽」的**原始职责已被取代**，继续按 60 分钟抢跑只剩误杀（净损失 >> 收益）。

**推前后自证**：`py_compile` OK；`PyYAML` 解析 `{"group":"v8-algo-cloud","cancel-in-progress":false,"queue":"max"}`；
改动面**恰 2 文件**（+37/−6、+7/−0）；**权限位 100644 改前改后一致**；**逐字节回读一致**；
行尾逐文件自适应（`.py`=LF、`.yml`=**CRLF** 912/912 守恒）；
`v8_build_deploy` / `v8_cache_buster_reconcile` 的 `on.push.paths` **均不含**这两文件 ⇒ **零意外重建**。

### 9.5 顺带钉死两条「取证纪律」（请小九同步）

1. **`api_push_raw.py` 的提交消息是硬编码** `"v8 cn fetch: " + now_cst()` ⇒ **算法链第 14 步推的提交也长这样**。
   判「某笔提交是谁推的」**只能**查 `/actions/runs/{id}/jobs` 的 **`steps[*].completed_at`**（本次实测 Δ+1 秒即定死）
   或 `runner_name`；**禁**凭 commit message / `head_sha`（`head_sha` 反查常 0 命中）。
2. **token 双轨**：`~/.workbuddy/v8_gh_pat` 是 fine-grained，创建含 `.github/workflows/` 的 tree 会
   **403「Resource not accessible by personal access token」**；须改用 **classic** `~/.workbuddy/v8_gh_token.txt`
   （`gho_`，`X-OAuth-Scopes: gist, repo, workflow`）。⚠️ 见到该 403 **不要**误判成分支保护而去 `force`。

### 9.6 仍待小九裁定（我未擅自做，守「不要造成踩踏和暴风」）

1. **`data/BACKTEST_ALL_ALGOS.js`（星级真源）仍停 09-18 20:47:41**，尚未吃到 n=83 新样本
   ⇒ 「强势跟踪」score 预测 **4.51 → 4.26**（stars 仍 4★，`maxN=83 < 100` 未触封顶）。
   它只能由 **E 批**（`gen_backtest_all_algos.py`，`STAGES["E"]`）重算；`update_v8.py` L1456-1459 有墓志铭
   明确**不在构建链恢复调用**。⇒ **等新数据日（09-21）自然重跑**（我未强制派发）。
2. **cn fetch `soft_14`（L644）清单仍只收 `data/`** ⇒ 该 workflow 内 `update_v8` 写的 `raw_data/` 不可持久化。
   但已被**算法链第 14 步**（清单收 `raw_data/ data/`）兜住 —— 前提是**算法链能跑到第 14 步**（9.4 已修）。
   是否仍要补 L644 清单，请裁定（我倾向**不补**，避免双写与 `L607` 一致性校验互相牵扯）。
3. 本文件 §8.7 第 3 条（`raw_data` 分叉）**现已由 9.4 的修复覆盖**，其「1 交易日自愈」的预测**作废**。

—— 阿狸咪的工程师 2026-09-19 02:45 CST
