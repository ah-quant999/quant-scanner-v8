# HANDOVER 小九给阿狸咪 — 排版整改已上线 + E盘清理收口（含真删）— v3 更新 22:20

> 采集时刻：2026-09-18 22:20（小九机）。
> v3 更新：主人 21:57 拍板三项（① 上线 ② 推交接文档 ③ E盘回收站真删），**本版为三项全部完成后的实测记录**。
> 本档只陈述「截至采集时刻实测」，不写任何「会自动/会恢复/会怎样」式预测。
> 收件人：阿狸咪（夜班 18:00–次日 07:30 + 周末）。发件人：小九（白班 7:45–17:45）。

## 零、一句话总纲
主人 2026-09-18 21:57 拍板三项：① 排版整改上线 ② 交接文档推 main ③ E盘回收站真删。
**三项已全部完成**（见下）。你今晚/明天无需接手任何清理或上线动作，仅作知悉即可。

## 一、改后三件套（三项全绿）
| # | 项 | 状态 | 实测依据（2026-09-18 22:08–22:20） |
|---|---|---|---|
| ① | 对齐云端/双机（上线） | ✅ 已完成 | commit `3ae83a186937e1bd161b28253690dc43ae028ea1`（22:05）；main tip = 该 commit；Pages 硬抓 `index.html`=1297015B / `logic.html`=729595B，与推送内容逐字节一致；`window.v8SectorFlow` 线上 index=16 次 / logic=9 次 |
| ② | 对齐 index.html ↔ logic.html 口径 | ✅ 已完成 | 双页均含 `window.v8SectorFlow`；index 保持 LF、logic 保持 CRLF |
| ③ | 写交接文档 | ✅ 本档已推 main | 落 `docs/ops/handover/`，单独 commit + push main |

> ② 现已是「本地 + 线上」双向对齐（不再是仅本地）。

## 二、本次上线做了什么（技术细节，供复核）
- **关键风险已识别并规避**：本地 HEAD（`8c9562d5`）落后远端 **109 个提交**；远端 `index.html` 已被改 **70 处**、`logic.html` 改 **10 处**（CI 缓存戳 `?v=` + 他人改动）。**若直接用本地文件覆盖远端，会静默丢掉这 70 处改动**。
- **采用三方合并**（base=本地HEAD / ours=本地工作树 / theirs=远端 tip）：
  - `index.html`：合并 1297015B，冲突 0，我的改动 17 块全落地，远端 70 块全保留
  - `logic.html`：合并 729595B，冲突 0，我的改动 10 块全落地，远端 10 块全保留
  - EOL：index 纯 LF（crlf=0）、logic 纯 CRLF（crlf=8426=lf）
- **隔离 API 推送**（Git Data API + `force:false`）：只覆盖 `index.html`/`logic.html` 两条路径，其余文件逐字节不动；父提交恒挂当轮 tip（纯 ff，抢输只会 422 不会覆盖）。
- **推送后按自己 commit sha 拉回**复核：远端两文件 sha256 与本地完全一致。
- **Pages 线上核验**（22:08）：`index.html` 1297015B、特征串 `v8SectorFlow`=16；`logic.html` 729595B、=9 ⇒ **线上已生效**（非仅提交进仓库）。当时 CI `#7037/#7038` 为 in_progress，属正常。

## 三、上线改动内容
- 个股查询「关联板块」排版整改：原 `💧 关联板块` 独立行删除，**并入 `🏷` 胶囊内部**（1 行 5 列，概念/行业/关联板块同列一目了然）
- 新增全局 `window.v8SectorFlow`（修旧 `renderRelatedFlow` 三处 bug：胶囊拼接 / 数据源 / 去重）
- 双页口径同步（index.html 与 logic.html）

## 四、E 盘清理与「真删」（重要认知修正）
### 4.1 真相（受控实验实锤）
- E 盘挂 **坚果云 Minifilter**（`Nutstore Minifilter Driver Service`），它把 E 盘上的删除操作**重定向进 E 盘回收站**（同盘进回收站不释放空间）。
- 今日在 E 盘"删掉"的 WorkBuddy 缓存（`shell-snapshots` 9205 / `traces` 8270 / `file-history` 6451 / `artifact-index` 3026 / `logs` 1499 / `cache` 1120 / `changes-*` …）**实际全部进了 `E:\$Recycle.Bin`**，空间并未释放。
- **更正**：早先「POSIX 整目录删能绕过坚果云限速」的结论**只对回收站内成立**；在 E 盘普通路径上，POSIX 删除同样被坚果云拦截并重定向进回收站（v2 档里写反了，以此版为准）。

### 4.2 真删方案（已验证）
- **回收站目录 `E:\$Recycle.Bin` 不受坚果云限速**：受控实验实测 `DeleteFileW` **2292 文件/秒**，且 `$I` 增量 = 0（不再被重定向）。
- 脚本：`C:/Users/Administrator/AppData/Local/Temp/v8_rb_purge.py`（白名单前缀制，`--go` 才真删，幂等可重跑）。

### 4.3 本次真删范围（白名单，宁少不多）
- **删**：来源前缀命中 `e:\workbuddy-data\`、`e:\temp\`、`e:\tmp\`、`e:\_v8_iso_e*`、`e:\_v8_diskbench_e\`
- **保留（硬红线 DENY）**：`e:\workspace\`（含 `reports\龙虎榜\龙虎榜专业版-2026-04-13.docx`、`reports\外资投行周报\外资投行周报-2026-04-30-60day.docx` —— 绝不触碰）、`e:\qs_workspaces` 非 tmp 路径

### 4.4 真删实测结果（2026-09-18 22:13–22:20）
- 处理条目 **25311 / 25311**（失败 **0**）
- 删除文件 **268826** + 删除目录 **97835** + 删除 `$I` **25311**
- 速率 **1123 文件/秒**
- **E 盘可用：147.246 GB → 155.260 GB（Δ = +8.014 GB，真释放）** ✅
- 回收站残留仅 9 条：`e:\workspace\reports` 用户文档 2 条 + 仓库内少量 `_tmp_*` 临时/锁文件（按白名单保留）。

## 五、双机冲突规避
- 小九机清理动作已全部结束，E 盘当前无破坏性操作、无后台删除进程。
- `index.html`/`logic.html` 已上线；仓库其余本地 `M` 均为算法/数据文件（`algorithms/*.py`、`data/*.js`、`raw_data/*.json`），**非本次整改**，不要混进同一次提交。
- 阿狸咪无需接手；如需动 E 盘，知悉「删普通路径会被坚果云转回收站、要真释放须清回收站内实体」即可。

## 六、环境坑提醒（本批新踩）
1. **Bash 工具 PATH 崩坏**：`dirname`/`cd`/`head` 全 command not found → 一律用 Python 绝对路径跑 git / 清理。
2. **反引号被 shell 吃掉**：含 `` ` `` 的文本用 Write 工具或 Python 落盘，不用 `python -c "..."` 拼长串。
3. **坚果云限速**：E 盘普通路径删除 21 文件/秒且被重定向进回收站；**删回收站内实体不受限（1123–2292/s）**，要真释放 E 盘空间须走回收站真删。
4. **行尾铁律**：`index.html`=LF、`logic.html`=CRLF、`docs/ops/handover/*.md`=LF。
5. **三方合并前必须比对「远端 vs 本地基线」差异块**：相交块数 = 0 才可叠加；否则必须人工看差异（本次实测 index 70 块 / logic 10 块，全不相交）。
6. **回收站 `$I` 解析有目录/文件两种偏移**：目录路径 @28、文件 @24，混用会解析出乱码来源，进而误判删除范围。

## 七、复核命令清单（均可直接跑，无需联网）
```
# 1) 本地未提交改动（应无 index.html / logic.html）
C:\Users\Administrator\.workbuddy\binaries\PortableGit\versions\1.2.0\cmd\git.exe -C E:/qs_workspaces/quant-scanner-v8 status --short

# 2) 线上两页是否含本次改动
#    Grep 工具搜 https://ah-quant999.github.io/quant-scanner-v8/index.html 的 window.v8SectorFlow

# 3) E 盘可用空间（真删后 >155GB；真删前基线 147.246GB）
python -c "import shutil;print('%.2f GB'%(shutil.disk_usage('E:/').free/1073741824))"

# 4) 回收站残留（应只剩 e:\workspace 的少量条目）
#    PowerShell: Get-ChildItem 'E:\$Recycle.Bin' -Recurse -Filter '$I*' | Measure-Object

# 5) 真删脚本（如需补删其他缓存目录，幂等可重跑）
C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe C:/Users/Administrator/AppData/Local/Temp/v8_rb_purge.py --go
```

## 八、本文档自身
- 路径：`docs/ops/handover/HANDOVER_小九给阿狸咪_2026-09-18_2013_排版整改待上线与E盘清理收口.md`
- 行尾：**LF**（已二进制写，禁 CRLF）。
- 推送：本文件单独 commit + push main（SSH，绝不 force）；不随 index.html/logic.html 混推。
- 校验：远端 `docs/ops/handover/` 出现本文件名即阿狸咪可 `pull` 收到。
