# 九宝量化 v8 当日交接文档（小九 → 阿狸咪）

> 更新于 2026-08-24 12:03 自动更新（北京时间，小九当班 12:00 档）

## 一、背景

- 当前日期：2026-08-24（周一）。双机分时铁律：小九白天独家（7:45–17:45），阿狸咪夜间/周末（18:00–次日 7:30 + 周末）部署 v8。
- **主人 8/21–8/27 出差中**：期间小九白天独家值守、阿狸咪夜间/周末接管，沿用 8/20 出差纪律（盯紧别乱改、本地云端对齐）。
- 本档由小九 12:00 自动化生成，汇总截至 12:03 的当日累积改动，供阿狸咪开机后快速对齐当日状态。
- 当日日志主场落 `quant-scanner-v8/.workbuddy/memory/2026-08-24.md`（stock-scanner 侧同名文件不存在，按惯例以 v8 侧为准）。

## 二、今日已完成改动（含真实 commit SHA + 影响）

> 所有 SHA 均于本档生成时从 `git log origin/main` 实核，非记忆值。当前 main HEAD = `81ba301f2`（本机已 ff 对齐）。

### A. 🔴 P0 数据回滚地雷根治（头号改动）
- **根因**：`sync_westock_portfolio.py` 收尾用 `git reset --soft origin/main` → 只移 HEAD 不重排主 index，且该函数注入了独立临时 index（`GIT_INDEX_FILE=.git/tmp_westock_idx`），改成 `--mixed` 也只写临时 index，主 index 依旧停留旧树。HEAD 追上高频推进的 origin/main 后，主 index 旧树差异全部呈现为「已暂存的旧版本」（本次 157 个，含 index.html 62± 行旧缓存戳 + 8 个 raw_data 暂存删除项）。
- **危害链**：仓库 4 处不带 pathspec 的 `git commit -m`（run_dividend_refresh:49 / v8_closing_data_refresh:71 / self_heal_monitor:246 / algorithms/strategy_four_volume_60m:404）会连带提交整个暂存区 → 18:30 盘后链将推出整站数据回滚 + 删 8 个文件。
- **修复（已上线 main）**：
  - `a6864b71f` **fix(git-safety): sync_westock_portfolio 收尾不再 reset --soft origin/main**，杜绝主 index 停留旧树导致的 100+ 文件数据回滚地雷。
  - `54fb6141b..a6864b71f` 隔离索引单文件推送已上线并远端复核。
  - 拆弹标准动作（零文件内容改动、完全可逆）：`cp .git/index backup/git_index_stale_<ts>.bak` → `git reset -q HEAD` → `sha256sum -c` 证明关键文件字节未变。
- **排查铁律沉淀**：`git status` 出现大批 `M `/`MM`/`D ` 首列标记（已暂存）时，先查 `git reflog | grep 'reset: moving'` 判是否 soft reset 遗留的陈旧 index，勿当「待云端代发的副产物」放过。

### B. zsxq_token 丢失（不可从 git 还原）
- 00:00 自愈监控闭环发现 `data/zsxq_token.json` TOKEN_FILE_MISSING（8/21 23:00 尚为 len=53，周末消失）。
- **新故障模式**：该 token 被 `.gitignore:21` 忽略 → 从未进 git 跟踪 → git 无法还原，全仓 find 与 backup/ 均无副本。凡 gitignored 凭据丢失即不可逆，须仓外另存。
- **影响边界**：`heal_candidate_guanlan()` 硬依赖本地 token 文件 → 本地自愈拉取链路失效（候选池再丢观澜台只能告警）。云端管线不受影响：extractor 优先读环境变量 `ZSXQ_TOKEN`（GitHub Secret 注入）。
- **恢复**：`scripts/setup_credentials.py` 交互粘贴 token（wx.zsxq.com → F12 → authorization）。可选加固（未擅动，待放行）：`check_zsxq_token()`/`heal_candidate_guanlan()` 增加 `ZSXQ_TOKEN` 环境变量兜底。

### C. runner 本地守护第 77 轮（02:10）✅ 关键改善
- `v8_runner_guard.py --heal --push --alert` 退出码 0，零自愈。Runner.Listener PID 1188 连续存活 **64.71h**。
- **GitHub API 403 二级限流 + 云端每分钟派发风暴彻底消退**：最近 40 runs 结论 39 success/1 queued、零 failure；workflow_dispatch 仅 1（风暴期曾 10 分钟 18 次）。
- **stash 堆积从连续 6 轮的 27 条清空为 0**，孤儿 UU 复发源已断（UU 长期 0）。
- 坑位：`git status --porcelain | grep -c '^UU '` 返回 0 时 grep exit=1 会截断整条 `&&` 链 → 门禁串行检查须用 `;` 或 `|| true` 兜底。

### D. 实时数据 / 缓存 / 抓取（main 已含）
- `68dc6b2ed` / `5173d517b` chore: 缓存戳实时对齐 ?v → 防 CDN 覆盖。
- `bf8f422c8` / `012496dd1` chore(stock-quote): refresh STOCK_QUOTE via v8_stock_quote_refresh（修正 STOCK_QUOTE 陈旧链路之一）。
- `f277d1035` data: 智能触发补抓(盘中·guard) 20260824-1104（sector_fund_flow/index_quotes/concept_ranking）。
- `ee03fa9de` chore: 盘中算法追踪轻量刷新 11:53；`235dbe89f` data: westock 持仓同步 12:02。
- 多轮 `v8 cn fetch`（5c4e55abb/464c6aa48/28c9b9372/03459f028/84a6ae7f1/d0317c27b/608d093df/3b6a39881）、`v8 build`（81ba301f2/5017c5e39/b751778e6/54fb6141b/8a4aacefa/a098327a0/6ae477bc2）、`v8 risk gauge`（85b17c1ee）均为常规流水线产出。

## 三、当前系统状态

| 项 | 状态 | 说明 |
|---|---|---|
| 站点 | ✅ HTTP 200 | ah-quant999.github.io/quant-scanner-v8，Content-Length 992148 |
| main SHA | `81ba301f2` | 12:04 build，本机已 `git merge --ff-only` 对齐 |
| HEALTH_CHECK | ⚠️ overall=fail | summary ok94 / warn0 / fail13 / total107 |
| runner 本地 | ✅ ok | 最近抓取 2026-08-24 11:50:17，1 模块全成功 |
| runner GitHub API | ✅ ok | 最近运行正常 |
| 剩余自动化 | 全部 hy3 | 14 ACTIVE + 1 PAUSED（TDX 跟踪）；本机 12 个 v8 任务在跑（08-20 查库） |

### HEALTH_CHECK 13 个 fail 解读（关键，勿误判为事故）
**全部 13 个 fail 均为盘后算法链产物（heal_cat=algo_run），last_update 多为 8/20–8/22，属「周末→周一间隙」预期陈旧，18:30 盘后链补齐即清。** 与今日 11:24 日志「fail 全 stale，周末→周一间隙预期，18:30 盘后链补齐」一致，**非新增故障**。

重点 fail 项（id / 最近更新 / age）：
- `CANDIDATE` 候选池 — 2026-08-21 22:50（3673min）
- `STOCK_RPS` 相对强度 — 2026-08-22 09:07（3056min）
- `all_FINAL_RECOMMEND_DATA` — 2026-08-22 07:26（3157min）
- `all_FOUR_VOLUME_60M` — 2026-08-22 08:54（3069min）
- `all_H_AUTO_BUY` — 2026-08-21 15:31（4112min）
- `all_H_AUTO_BUY_TRACK` — 2026-08-22 07:58（3125min）
- `all_LHB_7D` — 2026-08-22 07:56（3127min）
- `all_LHB_HISTORY` — 2026-08-21 18:03（3960min）
- `all_SECTOR_RECOMMENDATION` — 2026-08-22 07:59（3124min）
- `all_STOCK_STOP_DATA` — 2026-08-20 23:53（5050min）
- `all_TOP5_TRACK` — 2026-08-21 23:34（3629min）
- `all_TRIPLE_HISTORY` — 2026-08-22 00:12（3591min）
- `all_VOLATILITY` — 2026-08-21 23:17（3646min）

**盘中实时卡全部 ok**（age 31–35min 新鲜）：INDEX_QUOTES / ETF_PULSE / ETF_INTRADAY_HEAT / SECTOR_FUND_FLOW / CONCEPT_RANKING / LIMIT_UP_HEATMAP / MARKET_FUND_FLOW_DATA / MARKET_ALERTS 等。
**昨日盘后卡**（TRIPLE_CONSENSUS/COCKPIT_ADVICE/TOP10_DAILY/FOUR_VOLUME/CRDS 等 age ~1231min，昨日 15:32）属正常节奏——这些须等今晚 18:30 盘后链刷新。

> 注：上方 ok94/warn0/fail13 为 `data/HEALTH_CHECK.js` 页面指标（访客所见）；看门狗自身 11:24 报 ok75/warn4/fail27（其内部门禁口径，fail 同为周末间隙 stale）。二者口径不同但结论一致——均预期 18:30 盘后链补齐后归零。

## 四、需阿狸咪注意 / 待办 / 风险

1. **🔴 18:30 盘后算法链须如期跑**——这是清掉 13 个 fail 的唯一路径。若 18:30 没跑（或自愈未派发 algo_run），候选池/相对强度/龙虎榜/四量终极等维持陈旧，将误导选股决策。
2. **zsxq_token 恢复（按需）**：本机若需本地自愈拉取观澜台，须重跑 `scripts/setup_credentials.py` 粘贴 token；云端管线不受影响。
3. **纵深防御待授权**：4 处裸 `git commit -m`（run_dividend_refresh:49 / v8_closing_data_refresh:71 / self_heal_monitor:246 / algorithms/strategy_four_volume_60m:404）仍无 pathspec，a6864b71f 已根因修复但纵深防御未做——建议后续补 pathspec。
4. **8 个磁盘已无但 origin/main 仍在的 raw_data 是否历史授权清理（待授权）**：h_auto_buy_history 等 H 链路产物。
5. **本地 WIP 处理说明（重要）**：本实例执行时本地有 131 个已修改跟踪文件（含红线 index.html / HEALTH_CHECK.js / PORTFOLIO.js 的本地态）+ 31 未跟踪，均为本地 runner 实时中间态。为交付本档并**避免重演 11:24 数据回滚地雷**，已 `git stash push -m handover-20260824-wip-preserve` 保 WIP → ff-merge 到新鲜 main → **未用旧 WIP 覆盖新鲜 origin/main 数据**（origin/main 的 data/*.js 已是 11:50–12:04 云端新鲜态）。stash 未丢弃（可 `git stash pop` 取回旧态，但会覆盖新鲜 data，谨慎）。
6. **【观察·待裁决】盘中漏轮兜底守护 BYHOUR 过滤未生效**：00:10~05:10 连续 6 轮无效唤醒（<09:30 窗口外空转但浪费调度），已记入 automation 记忆，待裁决是否调整过滤逻辑。

## 五、小九监督职责

- 12:00 / 18:00 交接文档自动更新（本档即 12:00 档）。
- 盯盘期运行看门狗 + runner 守护：今日已确认 403 派发风暴消退、stash 堆积清零（27→0）、Listener 存活 64.71h。
- **18:30 盘后链后复核**：盯 HEALTH_CHECK 是否 13 个 fail 全清；若仍 fail，立即 dispatch post_close + algo_run 补救。
- 红线合规：本档全程未 commit/push 除 HANDOVER 外的任何文件，未触碰 index.html / HEALTH_CHECK.js / PORTFOLIO.js / BLOAT_CHECK.js 红线内容。

---
*本档由自动化生成，仅汇总状态不执行部署。v8 铁律：本地改好不上线，主人明确「上线」才 commit+push → GitHub Pages 自动发布。*
