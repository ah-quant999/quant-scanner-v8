# 2026-08-06 18:00 下班前交接 — 小九 → 阿狸咪

## 今天已完成改动（共 14 个 commit）

| commit | 内容 |
|---|---|
| `122101e` | UI 修复：今日判定/宏观/共振日历/运维/任务看板等 8 处 |
| `9505f62` | AI市场速览 stale 守卫 + v8_cloud_watchdog 改 cn fetch 真实追踪 |
| `3a083bd` | ETF 资金热度卡修复（石油关键词 + ETF代码） |
| `e79020f` | IPO 影子股「选 A」并入打新卡 |
| `a58ff43/8ab6811` | IPO 影子股换真实数据 |
| `f1f02a1` | GitHub schedule cron 收敛修复（11:30 漏档根治） |
| `fd141f0` | AI市场速览 cache-busting + v8_build_deploy 加 index.html 提交 |
| `87fd55d` | 共振日历 4 tab try-catch 容错（修 ReferenceError） |
| `873c820` | **feat(ops)** 新增云端盘中自愈器 v8_intraday_healer（防 GitHub schedule 漏档） |
| `e7f21df` | 运维+逻辑详解页对齐（含盘中自愈器记录） |
| `9e561db` | IPO recommend 修正 + 影子股自动生成（按 v6_memo 0.8亿阈值） |
| `70625f7` | 北向席位详情 3 列网格 + 紧凑行高 |
| `4bba5ec` | 北向席位详情改版 |
| `f66540b` | 机游共振详情 3 列网格 + 紧凑 |
| `8703cfd` | **fix(rc)** 机游共振日历 dy.date→dy.iso（slice bug） |
| `70625f7/3097400/5a19679` | 北向/机游详情多次改版 |
| `31b8e03` | 紧急撤回 model 备忘 |
| `cfe7eea` | 机游共振'最近交易日'按自然月分组 + 修 wkIdx bug |
| `534e4a2` | 撤回逻辑页"本机模型"备忘 |
| `bdef8b2` | 机游共振详情合并分组为单列大字 |
| `e2669df` | 加 16:30 港股档 + 弹层 v6 六块 |
| `59708f3` | 「红三兵→家人」改名+全部 insight tag 加 hover tooltip |
| `fa18666` | 北向席位周度/月度按自然月 |
| `cbad468` | 弹层 v6 六块 + 字号大 + 阈值 0.8亿 |
| `5f5522e/cbad468` | 弹层 6 块横排 3 列 + 空格占位 |
| `dadf29d` | 机游共振'最近交易日+周度+月度'严格按当前日历月 |
| `fcdd7e8` | 暂未上架加 4 个待开发卡片 |
| `12e1648` | 周度汇总月初月末截断周全覆盖 |
| `bb71459/53af979` | 逻辑详解 sub-tab 顺序调整 |
| `ca0b8a6/94a59e2` | **当月只能看到当月**（机游共振日历） |
| `62790f2` | **本周度月初月末截断周覆盖**（机游共振+北向） |
| `bb71459` | |

## 今日架构/重大决策

1. **云端盘中自愈器已上线**（v8_intraday_healer wf 328427995）：工作日 09:03-15:33 每 30 分钟检测 cn_fetch 过期自动补跑。**纯云端 ubuntu**，小九关机也兜底。
2. **16:30 港股档加回**（cron `*/30 1-8 * * 1-5`，覆盖到 16:30 CST）。
3. **模型全切 flash**（14 个 WorkBuddy 自动化任务全部 `deepseek-v4-flash`，hy3 今天额度耗尽触发 429 限流；hy3 今晚 23:38 重置，**回 hy3 等主人通知**）。
4. **三层冗余**：
   - 主链 cron（cn_fetch_cloud）
   - 云端盘中自愈（v8_intraday_healer）
   - 本机 WorkBuddy 兜底（小九开机时）
5. **v8 周度汇总统一规则**：月初截断周 + 完整自然周 + 月末截断周全覆盖，标记「(截断)」。
6. **v8 月度统计统一规则**：严格按当前日历月（jyDays 过滤 + lhbDate 在当月时才显示日期）。
7. **v6_memo 规范**：机游共振入榜阈值 ≥0.8亿（8000万），互斥分块（六块：机构净买/卖/机游共振/纯游资异动/游资净买/卖）。
8. **v8 UI 偏好**（已写入 E:/workspace/stock-scanner/.workbuddy/memory/MEMORY.md「v8 UI 偏好」段）：
   - 列表默认 2-3 列 + 紧凑行高
   - 不再用「新增备忘」「用户偏好提醒」一类元注释灌进前端页面
   - v8 页面**严格按 v6_memo 规范**设计（不再自由发挥）

## 当前 GitHub 状态（17:59 CST）

- 最近 commit：`94a59e2` 逻辑详解 sub-tab 顺序调整
- origin/main 已包含所有今日修复
- **已 dispatch cn_fetch_cloud**（17:59，build_deploy 会 commit data + 推到 main，让 GitHub Pages CDN 重新拉取最新 index.html）

## 用户已知问题（CDN 缓存）

用户刚才截图「北向 7 月只显示 3 个完整周」——**本地代码已修复（commit 53af979）但用户浏览器/CDN 缓存了旧 index.html**。让用户 Ctrl+F5 硬刷新即可看到月初截断周 7/1-7/5 + 月末截断周 7/27-7/31。

## 移交事项（阿狸咪接手清单）

1. **GitHub schedule + 云端自愈器**已稳定，**无需人工干预**——除非 cn_fetch_cloud 连续 2 次 45min 内没成功 run，可手动 dispatch（curl POST `https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/327687211/dispatches`）。
2. **本机 14 个 WorkBuddy 任务全 flash**——`hy3` 今晚 23:38 重置后**等主人通知切回**，**禁止自行建定时/自行切回**。
3. **逻辑详解页 / 运维页**已对齐 2026-08-06 全部变更（盘中自愈器、模型切换、v6 四色块阈值 0.8亿、v6 六块、机游共振自然月、北向周度自然月+截断周）。**无需手动调整**。
4. **暂未上架**新增 4 张卡（连板天梯/板块周期/情绪周期/AI 热点图谱）——等主人下一步指令决定是实现还是继续放暂未。
5. **v8_self_heal.yml 周六 14:00 调度**——周三改的 dispatch 目标从旧 cn runner 改为云端主链，工作正常。
6. **线上数据**最新跑通路径：cn_fetch_cloud(ubuntu) → raw_data → build_deploy(ubuntu) → data/*.js → GitHub Pages。每天 5 条 cron + 云端盘中自愈 + 本机 WorkBuddy 三层兜底。
7. **小九本机接手角色**：仅保留看门狗监督 + 周六臃肿体检 + 12/18点交接文档。**不主动跑数据/部署**。
8. **机游共振日历**当前翻月按钮（◀▶）可用；翻月后「最近交易日+周度+月度」自动按翻到的月份显示。

## 当前线上（17:58 origin/main）

- 最近 cn_fetch_cloud run：14:30 档（schedule success）→ raw_data 14:43 → build 14:43
- 上次手动补跑：15:09（31079864298，15:24 完成 success，**INDEX_QUUP=15:24:50**）
- 北向席位/机游共振所有 data 文件最新时间戳由 build_deploy 自动同步

## 注意事项

- **TZ=Asia/Shanghai date 在 Git Bash 是错的**——本机任何时间相关脚本用裸 `date`（本机系统时区即 CST）。
- **curl 的 -o 落盘被沙箱拦截**——用管道直喂 python 解析。
- **GitHub Actions schedule 不可靠**——已加盘中自愈器补救。
- **v8 index.html cache-busting**：`data/*.js?v=YYYYMMDDHHMMSS`，build_deploy 自动更新。
- **不要在 UI 页面写「元注释」/「备忘说明」**——v8 数据看板属性，运维护维护在交接文档/memory 里。

