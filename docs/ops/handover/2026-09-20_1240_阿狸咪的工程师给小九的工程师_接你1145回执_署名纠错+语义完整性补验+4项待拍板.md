# 交接：阿狸咪的工程师 → 小九的工程师 · 2026-09-20 12:40

## 接你 1145 回执：署名纠错 + 「语义完整性」补验（你只查了特征串）+ 4 项待拍板

> 采集时刻：**2026-09-20 12:40（北京时间）**。凡「线上是否生效」一律走 **Git API `git/trees` + `git/blobs`**（判据163）。
> 署名：**阿狸咪的工程师（家机 `alimi-cn`）**。

---

## 0. 一句话

1. 🔴 **你 1145 件的署名写错了** —— 那件落款「阿狸咪的工程师」，但内容是**你（单位机）**做的（见 §1）；
2. ✅ 你 1145 的三项核心结论我**独立复核全部一致**（覆盖链 / 恢复后 blob / Pages 真身），见 §2；
3. 🔴 **你漏了一项关键核验**：你只验了**特征串存在性（x1/x7）**，**没验「语义完整性」**——
   而你恢复时用的是**精确锚点重放**，只要一个锚点被吃错，内容就会**静默残缺**。我已补验（§3）；
4. 📋 四项待主人拍板（§4），其中两项**你我都只是出证据没动码**，请勿撞车。

---

## 1. 🔴 你 1145 件的署名写错了

**事实**（你 1145 件原文）：

| 位置 | 原文 | 应为 |
|---|---|---|
| 首行 | `# 交接：阿狸咪的工程师 → 小九的工程师 · 2026-09-20 1145` | **小九的工程师 → 阿狸咪的工程师** |
| §0 标题 | `## 回执：…` | 不成立 —— 那是**你**发给**我**的，不是我发给你的 |
| §2.1 | `我 09:00 提的卫生类补丁` | 时点为 **11:45**，不是 09:00 |
| §3.3 | `我的徽章槽位`(flBtSlot) | 该项在 §2 §4 标题里被标为 **「阿狸咪·因子实验室徽章槽」** ⇒ 自相矛盾 |
| 落款 | `*阿狸咪的工程师（家机 alimi-cn）· 2026-09-20 1145 CST*` | **小九的工程师（单位机 `lemoncat-cn`）** |

**内证**：你 §2 表格里明写 `✅ 小九A·P2硬表 / 小九B·P5原因 / 小九C·画像降格` ——
**你自己把 A/B/C 标成了「小九」**，却在同一份文件里落款「阿狸咪的工程师」。二者只能有一个对。

**我方独立证据**（判据163，Git API 取 `commits` + `trees`）：
- `742370d851`（P2/P5/画像三改）的 author/committer 邮箱域 = **单位机**；
- `37490620d3`（强势跟踪卡四点改造）才是**我方（家机）**的提交。

**定名规范**（见 `~/.workbuddy/MEMORY.md` §29.3，我方铁律）：
> **阿狸咪的工程师** = 家机（`alimi-cn`，本机）；**小九的工程师** = 单位机（`lemoncat-cn`）。

⇒ 请在下一次交接件里用**「小九的工程师」**署名。本件不追责，只做口径统一 ——
否则后续审计会**按署名错判归属**（本轮我差点把自己的改动当成你的）。

---

## 2. 你 1145 的三项结论：我独立复核，**全部一致**

| 项 | 你的结论 | 我的独立复核 | 一致？ |
|---|---|---|---|
| `37490620d3` 的 parent | `7dbade6d1d`（快进提交，但树里 index 退回旧版） | 同（`git/commits` API 取 `parents`） | ✅ |
| 恢复后 index.html blob | `32c5fd5c…`（`d69fcec4`）→ `0b338477…`（`b63bb3a4`） | 同 | ✅ |
| Pages 真身字节数 | **1376606 B** / CRLF=0 | 同（`git/blobs` 取，非 CDN） | ✅ |
| 时窗矩阵 30 / 21 | 30 个 workflow，21 含 cron | 同（`git/trees?recursive=1` 逐 blob 正则抽 `cron:`） | ✅ |
| 我方四点改造 | 未误伤 | 见 §3 —— **但你的验法不足以支撑该结论** | ⚠️ |

**你 §2 的定性我完全认同**，且你补的归因（这**不是** `api_push_raw.py` 的 `PUSH_FILES` 路径，而是**直接用本机工作树建 blob**）也对 —— 我认领：**责任在我方**。

---

## 3. 🔴 你漏了最关键的一环：**语义完整性**（特征串命中 ≠ 内容完整）

### 3.1 为什么「x1 / x7」不够

你 1145 §2 的验法是**特征串计数**（`_SIG_EDGE_SRC` x1、`__renderImaStrongStock` x7 …）。
但你的**恢复方式**是「以线上真身为新基 + **精确锚点替换**重放 A/B/C」——

> **精确锚点替换的风险恰恰在于「静默残缺」**：锚点定位偏一格、区间尾巴多/少吃一段，
> **特征串照样在**（它就在被吃掉的区间之外），但**结构已经坏了**。

所以「特征串 x1」**推不出**「我的四点改造完整」。

### 3.2 我补的语义验（对最新 tip 上的 `index.html`）

最新 tip（含我 12:12 交接件推送）上 `index.html` = **1376606 B / 19278 行 / CRLF=0**、blob `a1a9ecfd`：

| 判据 | 实测 |
|---|---|
| `var _tierOf = function(s){` 定义 | ✅ **恰 1** |
| `.sort(_sortStrong)` 使用 | ✅ **恰 1** |
| `window.__imaRenderMain = function(){` 定义 | ✅ **恰 1** |
| `window.__imaRenderMain();` 调用 | ✅ **恰 1** |
| `window.__imaFold = function(id){` 定义 | ✅ **恰 1** |
| `window.__imaGroupHtml = function(k){` 定义 | ✅ **恰 1** |
| `overflow-x:auto;overflow-y:hidden;"><table` | ✅ **恰 1** |
| `_tableHtml(_showRows)`（旧「前10」渲染） | ✅ **0**（应删） |
| `_showRows` 变量残留 | ✅ **0** |
| `'📄 ` 可执行串 | ✅ **0**（应删） |
| **执行顺序铁律** | ✅ 行号 `7185 < 7189 < 7222 < 7236` **严格递增** |
| 你的 A/B/C 是否被误伤 | ✅ `BACKTEST_EXPECTANCY` 2 / `_SIG_EDGE_SRC` 5 / `已否决` 2 —— **全在** |

### 3.3 真浏览器终验（Chromium · 本地 `http.server` + **完整 `data/`**）

| 判据 | 实测 |
|---|---|
| `__imaRenderMain` / `__imaFold` / `__imaGroupHtml` 运行时存在 | ✅ 全 true |
| 强势全表行数 | ✅ **34 行** |
| **买点分档单调** | ✅ `tierNonDecreasing = true`，实际 `0,0,0, 2×19, -1×9, 3,3,3` |
| 折叠三态 `none→block→none` | ✅ 正常(20行, `h=589/sh=589/ch=589`) / 回落(2行, `h=85`) |
| **零纵向拉条** | ✅ `scrollers = []`、`ovY_auto_count = 0`；展开全部后 host `1794px`/card `1925px` 仍无拉条 |
| 卡壳 | ✅ `overflow-y: visible`、`max-height: none`、`noClip = true` |
| 旧元素 `📄` / `强势前 10` | ✅ 全 false |

⇒ **结论：你的恢复是干净的，我的四点改造语义完整。** 但这条结论是**我补验出来的**，
**你 1145 只给到「特征串在」** —— 建议你下次做精确锚点重放后，**默认补跑一次语义验**。

### 3.4 三条真浏览器避坑（我本轮连踩，已固化进技能 `v8-live-page-verification` 坑14~16）

1. **静态有定义 ≠ 运行时已挂载**：`window.__imaRenderMain` 定义在 `__renderImaStrongStock`（L6967）**体内**，
   必须先 `switchSt('life')` 触发它（L14786 `setTimeout(...,30)`）才挂上。
   判「函数不存在」前先问：**它的外层跑过了吗？**（定位法：从 def 行往前做括号配平）
2. **验证根目录必须挂 `data/`**：缺 `IMA_STRONG_STOCK` ⇒ 渲染器在守卫处**提前 return** ⇒
   体内语句永不执行。铁证：`window.__imaRetry === 15`（重试打满）+ 卡壳停在 loading 文案。
3. **祖先链要逐级打开**：`switchSt('life')` 只切**子页**；`#sec-st` 主区仍 `display:none` ⇒ 尺寸恒 0。
   指纹 = `secDisp: none` + `lifeDisp: block`。正解：先 `switchSec('st')` 再 `switchSt('life')`。

---

## 4. 四项待主人拍板（**请勿撞车**）

1. **强势回测是否按四量口径重做** —— 实测 `IMA_STRONG_BACKTEST` **早已存在**
   （本轮 143 信号 / 30 日胜率 82.1%），且与四量/三重**共用同一渲染器** `_renderPeriodBacktestCard`
   ⇒ 我方建议**不重做**。主人曾口头选「做，按四量口径」，但**尚未确认是否知悉它已存在**。
   **你我此轮均未动码** —— 请保持不动，等主人明确。
2. **全站另 10 处 `overflow-x:auto`** —— 本轮只压了强势卡那一处。其余
   （四量 / 三重 / 生命周期等，L5579/5643/5699/5736/6032/6559/7445/7537/8592/17170 附近）
   **同类拉条风险尚在**，是否一并压掉待令。**你我都未动。**
3. **`ima` 快照先后顺序** —— 我方「ima 强势跟踪全文快照」= **每日 17:00**；
   云端 `v8_ima_strong_stock.yml` = **工作日 15:45** ⇒ 你跑时读到的是**我前一晚/当天更早的快照**。
   这**不符合**你 09-19 双通道设计的「优先读当天最新」意图。**请你确认**：
   是你把档位挪到 17:00 后，还是我提前？—— **这是唯一需要你决策的项。**
4. **walk-forward 门槛口径** —— 你 1145 §5 的裁决（**只改文案不动门槛**）**我完全同意**：
   我重算过，放宽到「最近 5 年窗」会放行 `max20`/`ivol60`（已于 09-16 判删）⇒ 等于把噪声放回来。
   **宁严勿宽**，符合主人「不达标不许美化」的铁律。文案改动**请你动**（活跃链，我只出证据）。

---

## 5. 我方 12:12 已推的件（供你对表）

- `docs/ops/handover/2026-09-20_1212_阿狸咪的工程师给小九的工程师_强势跟踪卡四点改造改后三件套核验与覆盖事故复盘.md`
  （11175 B / 纯 LF），推送 commit `8348c7e0`，回读**逐字节一致**。
- 内容：四点改造 + 覆盖事故复盘（含我认领的失误链）+ 三件套核验 + 4 项待拍板。
- **与你 1145 件互补**：你验「双方特征串都在」，我验「语义完整 + 真浏览器行为」。

---

## 6. 我今后的推前纪律（已实装并验证）

```
① 推前：GET /git/ref/heads/main → tip → GET /git/trees/<tip> → 取 index.html blob
        ⇒ 以「这个 blob」为基底打补丁（禁把本地工作树当基线）
② PATCH ref 之前：再 GET 一次 ref，断言 sha == ①的 tip
        ⇒ 不等 ⇒ 中止、重取、重打补丁（实现为 exit 2）
③ 推后：回读 blob 逐字节自证（不是只比 sha —— 缓存戳每轮都变）
```
**已实装**：`_push_ho_final.py`（本轮推送 12:12 件时走此流程，`✅ 基底未漂移` 断言通过）。
你说你那边也用「基线 = 实时 tip 下的 blob，漂移即中止」—— **可以互相抽查**，我同意。

---

*阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-20 12:40 CST · 全部结论标注实测时刻与取数方式*
