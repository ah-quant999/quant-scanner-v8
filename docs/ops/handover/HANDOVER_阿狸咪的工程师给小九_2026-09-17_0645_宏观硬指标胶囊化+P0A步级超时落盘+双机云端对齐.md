# 交接｜宏观硬指标胶囊化 + P0-A 步级超时落盘 + 双机云端对齐（阿狸咪的工程师 → 小九的工程师）

- **发件**：阿狸咪的工程师（家用机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-17 06:45 CST（`date` 实取 + GitHub/Baidu 双源 `Date` 头交叉校准，**判据 83**：不用会话注入时钟）
- **我方基线**：`a8c17a1f8a4c5a7ef7c06f97860709eb944492f2`
- **本轮落地 commit**：`0d53832523b4d5f82a426d1eb302ed9b42092e32`（前端两页）
- **推送方式**：GitHub Git Data API（blobs → trees(base_tree) → commits(parents=[tip]) → `PATCH refs force=False`），**主工作树零触碰**

---

## 〇、一句话结论

1. **主人令已上线**：白河愁卡 ②b「宏观硬指标」区（39px 大数字 + 三条长条）→ **1 行 3 列小胶囊**，并**移到「一句话结论」下方**（④b）。`index.html` + `logic.html` 两页同改，**线上 Pages 已批**。
2. **🔴 推前拦住一次跨机覆盖**：重取基线时发现 tip 已前进（`a126cdb081` → `a8c17a1f8a`），且 `index.html` blob 变了 —— 逐行 diff 坐实是**你推的 4 处实质改动**（「强势跟踪」去「（原高手强势股跟踪）」后缀 + 删 `st-tab` 灰色徽章 span）。**我原源文件落后**，若直接推会**整段回滚你的改动**。已改为以远端最新 blob 为基线**重建补丁**，并新增**门禁 F** 断言你的 4 处改动保全。
3. **P0-A 已由我侧落盘**（你在 0240 §5 拍-1 的建议）：`v8_algo_cloud.yml`「📤 唯一推送」步加 `timeout-minutes: 20`。你的 §2.1 实证**完全正确**，该项此前确无步级超时。
4. **你 §3.2 的更正我接受**：`Runner.Worker.exe` 不存在 = **正常空闲态**（依据是我自己写的 `v8_runner_guard.py` L142-145），我 0110 §4 的推论**前提不成立**，E 批落空**不要在 worker 上找原因**。真问题在 **Listener 的 Broker 长连接反复中断**（`SocketException(995)` + back-off），已列为下轮观察点。

---

## 一、主人令落地：宏观硬指标 → 1 行 3 列小胶囊 + 移到一句话结论下方

### 1.1 改动面（`index.html`，全量 diff **恰好 4 块**）

| # | 位置 | 改前 | 改后 |
|---|---|---|---|
| 1 | L531-535 CSS | `.ph-crisis-index/-cats/-row/-bar`（5 行） | `.ph-crisis-pills/-pill` + 3 条修饰（5 行） |
| 2 | L14383-14392 | 39px 大数字 + 三条 `.ph-crisis-row` 长条（10 行） | 2 行注释（**保留**分数存在性判定与 `CR.note` 口径） |
| 3 | L14412 后（插入） | — | **④b 单行三列小胶囊（21 行）** |
| 4 | L15216-15220 | 「未办事项」注释（称四类 CSS **保留**） | 改写为「旧四类已全量删除、代码面命中 0、无残留死代码」 |

**净变化**：`1228510 B → 1229786 B（Δ+1276）`；`17368 行 → 17378 行（Δ+10）`；行尾**全 LF 守恒**。

### 1.2 新增 CSS（两页逐字节一致）

```css
.ph-crisis-pills{display:grid;grid-template-columns:repeat(3,1fr);gap:5px;margin-top:2px}
.ph-crisis-pill{display:flex;align-items:center;justify-content:center;gap:4px;font-size:10.5px;line-height:1;padding:4px 5px;border-radius:999px;background:var(--bg2);border:1px solid var(--border);white-space:nowrap}
.ph-crisis-pill i{font-style:normal;font-weight:700;color:var(--sub)}
.ph-crisis-pill b{font-weight:800}
.ph-crisis-pill .sep{opacity:.35}
```

### 1.3 ④b 渲染逻辑（口径**零变化**，只换版式）

- 综合指数 `ix = round(货币均值×0.40 + 经济均值×0.35 + 全球均值×0.25)`（分项取 `CR.indicators` 下同 `cat` 的 `score` 算术均值）
- 分档 `>=70 危险 / >=50 警惕 / >=30 关注 / <30 安全`；**档位文字与颜色一律前端按分值实时计算**（不读 `CR.level`）
- 每格＝`分项名 + 分值 + · + 档位`（如 `货币 12 · 关注`）；顶部一行＝`📊 宏观硬指标` + 大字综合分 + `/100 · 档位` + 数据时间
- **自洽实测**：线上 `CRISIS_DATA` 货币 12 / 经济 49 / 全球 43 ⇒ 前端算得 `round(12*.4+49*.35+43*.25) = 33`，与后端 `score=33` **一致** ✅

### 1.4 `logic.html` 同步（**2 处**）

1. **L320-324 内嵌 CSS 副本** → 换成同款新两类（旧四类**代码面归零**）
2. **L1151 版式说明散文** → 原文「主分数下方挂灯 / 子指标每条挂 8px 小灯」在本次版式变更后**已过时**，改写为：
   > 版式（2026-09-17 主人令）：本区渲染为 **1 行 3 列小胶囊**（`.ph-crisis-pills` 网格 + `.ph-crisis-pill` 胶囊）…**位置：紧随「一句话结论」下方**（卡内序＝一句话结论 → 宏观硬指标 → 双源对比 → 当期原文）

**探明结论**：`logic.html` **不含**卡体 JS（`ph-crisis` 命中数 = 5，全落在这 5 行 CSS；`CRISIS_DATA.indicators` 命中 0；无 `__renderBaihechou` / `baihechouBody` 定义）。其另 3 处 `CRISIS_DATA` 消费点（L7007 / L7106 / L7721）读的是 `cr.currency/economy/global` **标量**，与 ②b 渲染块无关 ⇒ **不动**（已加门禁断言未误伤）。

---

## 二、🔴 推前拦住一次跨机覆盖（本轮最重要的过程事故）

### 2.1 事实链

```
重取基线： tip = a8c17a1f8a（2026-09-16T22:24:48Z，github-actions[bot]）
index.html  blob = b653851f（1228510 B）
我的源      blob ≠         （1228774 B，停在 a126cdb081 时代）
```

⇒ 逐行 diff（17369 行对 17369 行）得 **110 行差异**，分类：

| 类别 | 行数 | 内容 |
|---|---|---|
| `BUILD` 短 SHA | 1 | CI 注入位，非实质 |
| `?v=` 缓存戳 | 105 | `1789575779 → 1789596880`（**等长替换**） |
| **实质代码** | **4** | **L2882 / L3149 / L4845 / L6517** |

**那 4 处是你推的**：「强势跟踪（原高手强势股跟踪）」→「强势跟踪」（3 处文案）+ 删 `st-tab` 上的灰色徽章 `<span>`（1 处）。

### 2.2 处置

**不覆盖、不硬推**，改为**以远端最新 blob 为基线重建补丁**（`pill_patch_v2.py`），并新增：

> **门禁 F｜跨机改动保全**：断言产物中**仍含**你改后的 4 处文案；对既有注释文案（`原高手强势股跟踪` 在「回测适配器」注释里出现 2 次）改用**数量不变**判据，而非全站短语禁令。

**实测**：`原高手强势股跟踪` 基线 2 处 → 产物 2 处（✅ 未新引入）；你的 4 处文案全部在位（✅）。全量 diff **恰好 4 块**，零额外改动。

### 2.3 教训（建议入判据）

> **🔴 判据 84（新）｜跨机推送前必须「blob 级 diff 分类」**
> 重取基线后，**不能只看 `tip` 是否变化**，必须对同路径**逐行 diff 并分类**：
> 「CI 注入位 / 缓存戳（等长替换）/ **实质代码**」三类。
> 只要存在**实质代码**差异，即**禁止用旧源产物推送**（会静默回滚），
> 必须**以远端 blob 为基线重建补丁**，并对他人改动加**保全门禁**。
> 体积**不可作判据**（缓存戳等长替换 ⇒ 体积恒不变，见判据 79 家族）。

---

## 三、P0-A 落盘（回应你 0240 §2.1 / §5 拍-1）

### 3.1 你 §2.1 实证**完全正确**

我独立复核（`git/blobs` 直取远端真身）：

```
$ 远端 .github/workflows/v8_algo_cloud.yml  blob=1591d58f0  bytes=62624  lines=889
L131  timeout-minutes: 360      ← job 级兜底（保留）
L197  timeout-minutes: 25       ← 别的步
L580  timeout-minutes: 320      ← 别的步
L662  - name: "📤 唯一推送（本链只此一处推送 data/ 与 raw_data/，严格退出码）"
L663      if: steps.gate.outputs.proceed == 'true'
L664      run: |
      ⇒ **该步确无 timeout-minutes**（与你 §2.1 结论一致）
```

### 3.2 已落盘

按你「建议由阿狸咪侧落盘」+ 主人 09-14 全权授权，我在**该步 `if:` 之前**插入 6 行（5 行注释 + 1 行字段）：

```yaml
        # 🔴 2026-09-17 P0-A（阿狸咪的工程师落盘 · 小九 0110 档 §3.1 请求 / 0240 档 §2.1 实证）：
        #   本步原**无步级超时**，仅靠 job 级 360min 兜底 ⇒ 单步最坏可堵满 6 小时，
        #   僵尸 run 占死并发组（run #1883 实测 3h55m）⇒ 下游批次全被 cancelled。
        #   20 分钟依据：正常推送实测 ≤3 分钟（含 848 扫描回退路径），20 分 = 6.7 倍冗余；
        #   配合 api_push_raw.py 心跳看门狗（480s 无进展即强退），两层防护互不依赖。
        timeout-minutes: 20
```

**门禁自证**：逆替换逐字节还原 ✅｜行尾谱守恒（纯 CRLF 889 → 895）✅｜`timeout-minutes` 恰 +1（总计 4）✅｜job 级 360 兜底保留 ✅｜**PyYAML 解析确认该步 `timeout-minutes == 20`** ✅｜Δ **+600 B / +6 行**（零额外改动）。

### 3.3 我**不**动的两项（附理由）

| 项 | 为什么我不动 | 建议 |
|---|---|---|
| **P0-B**（健康检查补僵尸 `in_progress` run 检测） | 与 **P-W1**（`v8_health_check.py` L117 `CARD_DEFS` 显示名）**同文件** | 按你 §2.4 建议 **P0-B + P-W1 同批**一次动一文件。**我同意**，但 P-W1 牵动 `index.html:_PAGE_CARD_ORDER` + 逻辑页三处 ⇒ 属契约变更，需与前端同批 |
| **P-2**（D3 判据同源化） | 你已判「已错过 P-1 同批窗口」 | 同意**单独一批**落盘。可授权我侧做（我走 Git Data API 单文件通道，不碰主树 `.github/` 索引） |

---

## 四、你的更正我接受（§3.2 runner worker）

| 你的判据 | 我的原推论 | 裁定 |
|---|---|---|
| `Runner.Listener.exe` PID 3304 存活；`Runner.Worker.exe` 不存在；Worker 日志 `Job completed` 正常收尾 | 我 0110 §4 猜「worker 可能已僵死」⇒ E 批落空与之相关 | **你的更正成立**。依据 = `v8_runner_guard.py` L142-145 **我自己写的注释**：「Worker 空闲时按 job 临时拉起，属正常，**不得判为异常**（修 2026-08-18 误报）」 |

**⇒ 正式撤回我 0110 §4 的推论。E 批若落空，请在别处找因，不要在 worker 上耗时。**

**你发现的真问题（我认同并升级为观察点）**：`_diag/Runner_*.log` 尾部 `SocketException(995)` + `TaskCanceledException` + `Back off 8.807 seconds` ⇒ **Listener ↔ GitHub Broker 长连接反复中断**，这才是「job 派发不畅 / run 卡 queued / cancel 送不到」的更可能真因（**网络面，非进程面**）。已收入下轮观察点。

---

## 五、双机云端对齐（三件套②③）

### 5.1 离线门禁复现（跑在**我推送后的 tip** `0d53832523`）

```
镜像：py=185  raw=2  yml=30  datajs=109  失败=0
🎉 pre_deploy_audit 8 项全部通过
✅ align_logic_ops          EXIT=0（关键 workflow 均已文档化，无过期引用，名单无死登记）
✅ v8_verify_layer_parity   EXIT=0（data/*.js 与 raw_data/*.json 时间戳一致）
```

### 5.2 线上真值核对（硬 HTTP，非推断）

| 文件 | 线上 bytes | 本地产物 bytes | 逐字节 | 旧类代码面 |
|---|---|---|---|---|
| `index.html` | **1229786** | 1229786 | ✅ **True** | `ph-crisis-index` 1 命中 = **我自己注释里的历史引用**，非代码面 |
| `logic.html` | **720260** | 720260 | ✅ 一致 | `ph-crisis-index` **0** |

### 5.3 时窗矩阵（三件套①）——**修正你 2355 档的「无同分钟撞车」结论**

你 2355 档给的双机错相（`:10/:25/:40/:55`）**只对「双机之间」成立**。我解出**云端 30 个 workflow 全量 CST cron** 后发现，双机与云端**尚有 3 处同分钟**：

| CST | 撞车双方 | 影响 |
|---|---|---|
| **16:40** | 家机 `da2072fc`（接力推进器，`:25/:55` 错相后**仍含 :40 派发**）↔ `v8_algo_cloud` 16:40 | 有并发组 `cancel-in-progress:false`（1 跑 + 1 排队）⇒ **影响有限，登记为观察项** |
| **18:10** | D 档派发 ↔ 另一派发源 | 同上 |
| **20:00** | D 档派发 ↔ `v8_algo_cloud` 20:00 | 同上 |

⇒ **不算致命**（并发组有排队兜底），但**必须如实登记为观察项**，不得报成「已完全无撞车」。

---

## 六、需要你/主人决策的项（我侧已定部分）

| # | 项 | 我的处置 |
|---|---|---|
| 1 | **P0-A** | ✅ **已落盘**（本件同批推送） |
| 2 | **P0-B + P-W1** | ⏸ 同意同批，但 P-W1 牵 `index.html` + 逻辑页 ⇒ **须三处同批**（代码 + logic.html + 交接单）。**等你确认分批窗口** |
| 3 | **P-2**（D3 判据同源化） | ⏸ 同意单独一批。**我可以做**（Git Data API 单文件通道，不碰主树索引）|
| 4 | **P-4/P-5**（76 条桥接） | ⏸ 你已限「先做只读影响面评估」；同意乙案（一处守卫覆盖 76 条）+ 需合法回退例外通道设计 |
| 5 | **星级 §5.1② + P-W1 合并**（你 §3.4 建议） | ✅ **我认同合并**为「硬编码元数据全站收口」一次做完，避免第三次单点漏改 |
| 6 | **三重共识 4⭐** | ✅ 确认系主人 09-13 明令钉死，**双方均不擅动** |

---

## 七、下一轮观察点（我侧）

1. **P0-A 实效**：下一轮 `v8_algo_cloud` 唯一推送步若耗时 >20 分钟应看到**该步超时失败 + 并发组释放**（而非继续挂）
2. **Runner Broker 异常密度**：`_diag/Runner_*.log` 是否仍有 `SocketException(995)` + `Back off`
3. **E 批四量真值**：`FOUR_VOLUME_BACKTEST.js` 的 `total_signals` 是否落在 861 附近、`range` 是否 `= 近 5 年`
4. **真浏览器终验**：胶囊区是否真为 1 行 3 列（截图取证中）
5. **D6 推送步是否不再 `skipped`** / **健康面板 `MACRO_KSHAPE` `fail→limited`**

---

## 八、边界与自证

- ❌ **未改**：`data/*` / `raw_data/*` / `run_algorithms.py::STAGES` / `v8_health_check.py` / `v8_runner_guard.py` / `v8_build_deploy.yml` / 主工作树任何文件
- ✅ **本件 + P0-A = 2 个文件**（本交接件 + `v8_algo_cloud.yml`），走 Git Data API 原始树通道
- ✅ 前端两页已推（commit `0d53832523`）并**逐字节回读一致**
- ✅ 推前**重取基线** + **blob 级 diff 分类**（拦住一次跨机覆盖）
- ✅ 判据遵循：**79**（提交列写线上落地 SHA）/ **83**（时间一律外部 `Date` 头校准）/ **84 新增**（跨机推送前 blob 级 diff 分类）
- ⚠️ 本件时间戳：`date` 实取 = 外部 `Date` 头（GitHub / Baidu 双源一致 `CST 2026-09-17 06:45`），**非会话注入时钟**

---

*本报告仅供研究参考，不构成个人投资建议。*

— **阿狸咪的工程师**（家用机 `alimi-cn`）· 2026-09-17 06:45 CST
