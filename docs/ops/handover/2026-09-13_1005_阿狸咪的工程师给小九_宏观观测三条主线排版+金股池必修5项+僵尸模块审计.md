# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-13 10:05｜宏观观测三条主线排版 + 金股池必修5项 + 僵尸模块审计

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

**优先级**：常规（无阻塞）
**发件机**：阿狸咪（家用机 `alimi-cn`）｜**收件机**：小九（单位机 `lemoncat-cn`）

---

## 一句话结论

主人 09:43 三项诉求已全部落地：① 宏观观测「三条主线」改为金色序号徽标 + 事实/机构观点/本人判断各自成段；
② 金股池「三足鼎立必修 5 项」全部实现并沙箱验证；③ 「前端不可见但仍运行」的模块审计完成——**7 个数据文件仍在每日生产但前端已不注入**、**2 个 script 标签线上 404**。
另：主人问的「四重共识」**全仓不存在**（详见第四节），故未按「留四重」执行，需主人一句话定名。

---

## 二、三笔提交（均已上线）

| 提交 | 内容 |
|---|---|
| `290da902` | 宏观观测长文排版：`renderLines` 升级（主线徽标 / 标签分段 / 编号加粗），并挂 `window.__v8RichLines` 供复用 |
| `6897b6dc` | 金股池必修 5 项（`build_candidate_pool.py` + `calc_crds.py` + `index.html`） |
| 本交接单 | 文档 |

---

## 三、金股池必修 5 项（逐项实证）

| # | 必修项 | 改法 | 沙箱实测 |
|---|---|---|---|
| 1 | `today` 改交易日 | `_last_trade_date(_run_day)`，日历不可用回退运行日 | 周日 `2026-09-13` → **`2026-09-11`** |
| 2 | 继承窗口改精确交易日 | 新增 `_load_trade_dates/_n_trade_days_ago_precise`；45 交易日精确回溯 | 精确 `2026-07-10` vs 旧近似 `2026-07-11`（长假期间误差更大） |
| 3 | history 恢复 close/pct_chg | 从成员 `metrics.price/pct_chg` 写入 | 新晋条目 history 末条 = `{"close":12.34,"pct_chg":3.21,...}` |
| 4 | 继承成员降级「观察中」 | 今日未复现口径者 `status="观察中"` | 仅继承未复现 → **观察中**；今日命中 → 在池；首次入选 → 新晋 |
| 5 | 不占 `is_gold` 优先位 | `calc_crds.py` 合并处 `is_gold = is_gold and not watch` | 观察中成员不再插队金股层 |

前端同步：金股池卡汇总条加「✅ 今日命中 N / 🕗 观察中 N」，卡片行加 `观察中` / `新晋` 标；运维页卡内口径说明同步改写（**三处同源**：代码 + 页面 + 本交接单）。

> ⚠️ 现网 `GOLD_POOL.js` 尚无 `status` 字段（本改动当日生效前产出的数据）⇒ **标要等下一轮 B 批跑完才出现**，非前端 bug。

---

## 四、「三足鼎立 vs 四重共识」审计结论（主人要求先查再定）

### 4.1 事实一：「四重共识」不存在

```
$ git grep -c "四重" FETCH_HEAD
.github/workflows/cloud_weekly_cleanup.yml:2     ← 指「四重判据」，与模块无关
HANDOVER_LOG.jsonl:1 / 交接单 2 处               ← 历史文字
```
**全仓无任何模块叫「四重共识」**（`index.html` 里连「四重」二字都没有）。⇒ 不能按「留四重」执行，否则会删错对象。

### 4.2 事实二：三足鼎立与「三重共识」并不重合

| 模块 | 判据 | 输入 |
|---|---|---|
| 三足鼎立 | `signal_count == 4`（四腿全满足） | 纯**技术面**：缠论买 ∧ 金钻(黄柱/起涨) ∧ 四量图机构变红 ∧ 上涨趋势 |
| 三线共振 | `signal_count >= 3` | 同上（同一段代码，`scanner.py` L2118-2122） |
| 三重共识 | TOP10 精选(评分门槛) ∧ 基本面 A 档 | **评分 + 基本面**，与上面完全不同源（`gen_triple_consensus.py`，原 3 维因驾驶舱下线降为 2 维） |
| 四量终极 | `Q D = YZC ∧ JG ∧ XC ∧ FOUR` | 成交量前 N 股 + 通达信「四量终极」公式（`strategy_four_volume.py`） |

### 4.3 事实三：真正的重合只有一条腿，且是**同一条公式**

```
三足鼎立第 3 腿：scanner.py L659-663
  MID9=(3C+L+O+H)/6 → 牛线1 = WMA20(MID9, 权重20..1) → 机构变红 = C > 牛线1

四量终极第 2 条件：strategy_four_volume.py L134-142
  MID =(3C+O+L+H)/6 → NLJ = WMA20(MID , 权重20..1) → JG = C > NLJ
```
⇒ **同一个加权典型价、同一条 20 周期线性加权线、同一个「收盘站上该线」**。重合度 = 1/4 腿，不是高度重合。

### 4.4 建议（请主人一句话定名后再动）

- 若主人指的是**四量终极**：它是完整通达信公式（比单用「机构变红」更完整）⇒ **留四量终极**，三足鼎立可降级（或按上轮建议删除）。
- 若主人指的是**三重共识**：**不重合**（输入维度完全不同），不建议合并。
- 若主人指的是**三线共振 vs 三足鼎立**：二者是同一段代码的 `>=3` 与 `==4`，本就并存、各司其职，无需去留。

---

## 五、僵尸模块审计（前端已不可见，但仍在跑）

判据：数据文件存在 + 仍在生产 + 前端已不注入/零引用。

| 文件 | 生产者（仍在链上） | 前端状态 | 判定 |
|---|---|---|---|
| `data/STOCK_PROFILE.js`（1.4 MB） | `gen_stock_profile.py`（链上） | `index.html` 已改为**由 STOCK_LIST 本地合成** `window.STOCK_PROFILE` | 🔴 **建议停跑**（最大浪费，1.4MB/次） |
| `data/ALGO_TRACK.js`（42.4 KB） | `gen_algo_track.py`（链上） | 2026-08-31「轻量化」已停注入，零引用（13 天） | 建议停跑（或恢复注入） |
| `data/CANDIDATE_QUOTES.js`（31.5 KB） | 抓取批 | 同上，零引用 | 同上 |
| `data/LHB_7D.js`（5.9 KB） | `gen_lhb_7d.py`（链上） | 同上，零引用 | 同上 |
| `data/FRESHNESS_STATUS.js` | `update_v8.py` 映射 | 前端**读** `window.FRESHNESS_STATUS` 但**从未注入**⇒ 恒空（代码注释自认） | 二选一：补注入（恢复功能）or 两端删除 |
| `data/INDEX_HISTORY.js`（91.2 KB） | 抓取批 | 已停注入 | ✅ **保留**：`fetch_index_value_framework.py` 用它作上证日K二级兜底 |
| `data/RUNNER_STATUS_HEALTH.js` | `runner_health_alert.yml` | 仅注释引用 | ✅ **保留**：消费者是邮件告警，不是前端 |
| `data/maharo_macro.js` | **无生产者**（08:36 交接已确认弃用） | `<script src>` 仍在，**CDN 404** | 🔴 清死链（每页一次无效请求） |
| `data/hb_xiaojiu.js` | 仓库内**无此文件** | `<script src>` 仍在，**CDN 404** | ⚠️ 需你确认生产者（心跳明细卡已恒空） |

> 说明：`CRDS_BACKTEST` / `FOUR_VOLUME_BACKTEST` / `MOMENTUM_FILTER` 等**不是**僵尸——它们走「暂未上架」注册表的 `window[vname]` 动态取数，静态 grep 会误报，已逐一核实。

---

## 六、验证证据

```
inline <script> 25 块跑 new Function → 语法失败 0
标签净差 {div,table,tr,td,th,span,tbody,thead} → 全 0
index.html LF 守恒（CR=0）
py_compile build_candidate_pool.py / calc_crds.py → OK
沙箱单测（伪造上一版金股池 + 3 类成员）→ 5/5 项断言 True
align_logic_ops.py → EXIT 0（index.html#sec-op 515166｜logic.html#sec-lg 520167；有 cron workflow 21 个，MUST=21/ALLOW=3）
CDN 硬抓 index.html 1099946 B → 含 _richSplitLabels / __v8RichLines / watchN
```

---

## 七、护栏声明

- ✅ 全程**未执行任何 `git add`**（仓库外暂存 + plumbing：`hash-object -w` → 临时索引 → `commit-tree` → `push`）
- ✅ **未碰仓库内 `index.html`**（本机 HEAD 落后远端，改前一律 `git show FETCH_HEAD:<path>` 导出到仓库外）
- ✅ **未删除任何数据文件、未停任何任务**（僵尸项只出结论，处置待主人拍板）
- ✅ 未放宽任何闸门、未动小九机任何任务

---

阿狸咪的工程师（alimi-cn）｜2026-09-13
