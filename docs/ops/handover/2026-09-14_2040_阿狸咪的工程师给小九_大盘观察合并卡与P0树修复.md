# 九宝量化 v8 交接文档 — 阿狸咪 → 小九（2026-09-14 夜间槽位）

> 更新于 2026-09-14 20:40（阿狸咪工程师，夜间+周末值班）
> 信息源：git 真源实测（`git ls-tree` / `git cat-file` / `ls-remote`）+ CI run #6399 + 硬 HTTP 抓 Pages 实时验证。
> 双机铁律：小九白天（07:45-17:45）独家 / 阿狸咪夜间+周末（18:00-次日07:30）独家。本档供你明早接班时核对。

---

## 一、本次交接范围（一句话）

1. 🔴 **P0 线上事故已闭环**：推送损坏的 `index.html` 树（垃圾条目）已重建修复并上线，站点恢复正常。
2. **大盘观察子页面改版**（你下午的分析 + 斐波那契合并为单卡，日频动态概率，已上线）。

---

## 二、🔴 P0 事故：线上 index.html 树损坏 + 修复（真源实证）

### 现象（根因）
- 早前推送 `e49e106b8` 时，`git mktree`/`sed` 重建树把 `index.html` 条目**路径污染**为：
  `index.html100644 c9b4fa881aa3b3ad36758a62be3f8d279b4f454bblob `
  （正确 blob `c9b4fa88` 被塞进路径名，条目反而指向旧 blob `f99ea03a`）。
- `6d89f44ed`（你「交接读取器改远端优先」）在坏树上**仅改 `auto_handoff_read.py`**，继承了该垃圾条目。
- 结果：线上 `main` 的 `index.html` **无有效路径条目** → GitHub Pages 源 = main/root 找不到合法 index.html → 站点 404/空白。

### 修复（已上线，commit `f1e81c015`）
- 以**干净基 `864ed2bae^{tree}`**（最后含有效 index.html=f99ea03a 的提交）重建树；
- 注入**正确合并版 blob `c9b4fa881...`**（大盘观察合并卡）作 `index.html`；
- 保留你 `6d89f44ed` 的合法改动 `auto_handoff_read.py`（blob `ada5fade`）；
- `git commit-tree` 快进推 `main`，避免 rebase 卡行尾。
- CI `v8_build_deploy` run **#6399** 跑通：`gate`✅ → `pre_deploy_audit`（7 门禁，含 `align_logic_ops.py`）✅ → `部署前校验`✅ → `部署到 main`✅。

### 三查实证（不靠推断，凭实测）
| 检查 | 结果 |
|---|---|
| `origin/main` HEAD | `f1e81c015d35d901c30e1fc7c21103a312054d5f` |
| 树内 `index.html` 条目 | `100644 blob c9b4fa88... index.html` ✅（正确路径） |
| 树内 `auto_handoff_read.py` | `100644 blob ada5fade... auto_handoff_read.py` ✅ |
| 垃圾条目 | **0**（已消除） |
| 树条目数 | 83（与干净基一致） |
| 硬 HTTP 抓 `ah-quant999.github.io/quant-scanner-v8/` | 返回 **1,094,227 字节**（= c9b4fa88 体积） |
| 实时页内标记 | `SH_FIB`×22、`情景A`×2、`斐波那契`×16、`艾略特`×6 |
| 404 / 垃圾路径 | 均 **0** |

---

## 三、大盘观察子页面改版（用户授权，日频判断卡）

### 移除 3 张观测卡（大盘观察子页）
- 主题空间·潜力价值
- AI洞察·多源观点对比
- 新周期判定·海外宏观+AI大周期

### 合并单卡
- 斐波那契校验卡 + 你下午的艾略特深度分析 → **合并为单卡**，图形保留（斐波那契回撤位 + 艾略特浪型示意）。
- 三段解析结构：**现在什么情况 / 接下来会如何 / 我该怎么应对**。

### 🔴 关键：日频动态概率（不是一次性）
- 概率**不硬编码**，由 `window.SH_FIB` 实时字段加权推导：
  - `gapPct` = (当前指数 − ④浪生命线) / 当前指数
  - `ddPct` = 累计跌幅 `totalPct`
  - `bias`：mode 含「阴跌/磨底/底部/探底」→ 偏空；含「反弹/反转/确认/回升」→ 偏多
  - 加权：`wA`(回调) / `wB`(延续)，再 `wSum` 归一 → `pA%` / `pB%`
- 卡片在 `__ulSwitch` 切到大盘观察 tab 时由 `__renderElliottWave` **每次重渲** → 满足「日频判断」要求。
- 概率口径说明已写入卡片：每日随 `SH_FIB` 刷新，不写死。

### 质量门
- 内联 JS 校验：**120 个 `<script>` 编译零错**（自写 `validate_inline_js.js` 跑 `new Function`）。
- CI `pre_deploy_audit` 7 门禁全过（含头注一致性、运维页/逻辑页对齐）。

---

## 四、待你提供（图形升级用）

你下午艾略特深度分析的**详细数据**尚未到我手，当前合并卡内的浪型图形是**示意/占位**：
- 五浪坐标（点位 + 时间）
- 调整箱体上下沿
- 目标位测算
- 计数结论（是否⑤浪末端 / ABC 进行中）

→ 拿到后替换示意图形为真实标注（不改概率推导逻辑，仅换图形数据源）。

---

## 五、下一步（积压，已授权可自主，按你节奏接手）

- alpha101 阶段1 代码 + 回测
- #97-2 死代码删除推送
- 09:00 单 5 项算法决策落地
- UNLISTED_PANEL 6 项

---

## 六、上线状态

✅ **已上线**：`f1e81c015` + CI #6399（Pages 实时已验证 1.09MB 正确页）。
⚠️ 双机分时：你 07:45 接班后请先硬刷（Ctrl+F5）确认大盘观察合并卡图形+概率正常；若 `SH_FIB` 当日已刷新，概率应随数据变化（非固定值）。
