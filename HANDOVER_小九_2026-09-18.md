# 小九 → 阿狸咪 交接文档（2026-09-18 · 12:00 槽位自动更新）

> **更新于 2026-09-18 12:10 自动更新**（小九交接自动化，12:00/18:00 双槽位）
> 证据基线：origin/main = `4dc03e0a8`（12:12 build，`git ls-remote` 复核一致）· 站点 HTTP 200 · HEALTH_CHECK 12:12:14 全绿
> 双源日志合并：C 盘主会话（08:10–12:0x 白班）+ E 盘哨兵会话（00:45–11:46 自动化轮）
> 本文不写死自身所在 commit 的 SHA（本仓分钟级 churning，写死即过期）；核对命令随文给出。

---

## 一、背景（30 秒速览）

- 双机分时：小九白天独家（7:45–17:45），阿狸咪夜间/周末（18:00–次日 7:30 + 周末）。
- 今日为**周五交易日**，白班主战场：①双盲区事故家族第三次复发的**结构性根治**（CI 机读护栏）②盘中链平台侧 schedule 零派发**急诊**③斐波那契卡改名重算四方对齐 ④K型分层卡迁页 ⑤P1/P2/P3 三项收口。
- 阿狸咪昨夜/今晨的 IMA 三件套、BACKTEST_TDX 瘦身、ws-guard 行尾修复均已由小九哨兵逐档回执确认（详见 §三-1）。
- 本仓处于分钟级 CI churn 状态：**接手任何文件前必先取线上真身**，勿信本地缓存基线。

---

## 二、今日已完成改动（小九白班 · 全部含实测 SHA）

### 2.1 🔴 双盲区根治 + 机读护栏（08:10–09:00，P0，主人令「一劳永逸」）

**触发**：FOUR_VOLUME_60M 冻结逾 24h（巡检亮红灯但无人重跑）。**同型事故第 3 次复发**（09-11 四量60m / 09-18 凌晨 BACKTEST_TDX / 09-18 早晨四量60m）。

| 文件 | 改动 | 提交 |
|---|---|---|
| `.github/scripts/v8_stage_gate.py` | B 批 items 9→11（+四量 60M/HISTORY/TRACK），need 保持 8（容忍 2 项，不动主人 09-13 拍板项） | `3bd6da896` |
| `dedup_fetch_manifest.py` | `_ALWAYS_PUSH` +3（不变式：READY_SPEC items∪must ⊆ _ALWAYS_PUSH） | `3bd6da896` |
| `.github/scripts/guard_double_blind.py` | **NEW** · 三条不变式机读护栏（闸门覆盖/去重白名单/巡检告警），双向测试：负向报 5 处、正向全绿 | `3bd6da896` |
| `.github/workflows/v8_build_deploy.yml` | 护栏接入 CI **硬门禁**（pre_deploy_audit 后，无 continue-on-error） | `3bd6da896` |
| `data/FOUR_VOLUME_60M.js` | runner 等价环境重跑 134.6s/命中 11 只，ut=`2026-09-18 08:36:30`，手工转 LF | `734c50073` |
| `logic.html` | 8/9→**8/11**；「三宗罪」→「四宗罪」+第④条；CRLF 8486 零裸 LF | `734c50073` |

**线上验证**：Pages 硬抓 60m ut=08:36:30 total=11 ✅；logic.html 特征串全命中 ✅；用线上 blob 拼临时目录跑护栏三条不变式全绿（items=29 must=8 _ALWAYS_PUSH=35）✅。

**关键教训（已沉淀 skill `v8-double-blind-guard`）**：「能报」≠「会修」——巡检只报不做，必须有能触发重跑的闸门覆盖兜底；本地实跑结论不能直接代表 runner（必先核对 workflow `env`，本次 `CLOUD_RUNNER=true` 是关键分叉）。

### 2.2 IMA 交接档重做（09:00–09:15，主人令「绝对不行，马上用最新的全部重做」）

- **定性**：阿狸咪工程师 0830 档（`2026-09-18_0830_阿狸咪的工程师给小九_IMA解析丢64%根治…md`）§四/§八含**预测式表述**（「源恢复当天会自动回来」「样本会从 40 恢复到 112」）。这不是数据过期，是**文体问题**——交接档禁止预测。
- **全量实测推翻原文结论**（采集时刻 09:1x）：`IMA_STRONG_STOCK.update_time=2026-09-17 20:50:19`（新脚本**尚未跑到**）；`no_first_selected=72`（64.3%）；`field_missing_counts` 70~72 非零；`total_signals=40`。
- **第三层根因补全**：workflow 断言步推送 403（PAT 无 `workflow` scope）⇒ **断言根本没挂进 cron 链路**。「脚本能拦」≠「线上会拦」。
- 新档 `docs/ops/handover/2026-09-18_0900_小九的工程师给阿狸咪的工程师_回执纠正预测式结论_IMA链路实测现状.md`（14302B），推送 `90ef8df12`→`daab4ba5a`。
- **交接档铁律（主人口令，双方同守）**：每条结论标注采集时刻；凡「后续会怎样」一律改为可跑复核命令；不写死本文自身 commit sha。

### 2.3 盘中链零触发急诊（09:43–10:0x，P0，主人令「实时数据还是显示昨天的」）

- **硬证据**（09:2x 采集）：三条 cron 各异的盘中链（`v8_intraday_snapshot` */10 / `v8_algo_intraday_lite` 4h 档 / `v8_cn_fetch_intraday_lemoncat` 14 条定点）**当日全部 0 run**；cron 配置 ✅ / 双 runner ✅ online / 09-17 同期正常 ⇒ 排除配置问题，**指向平台侧当天 schedule 未派发**。
- **处置**：三条链 `workflow_dispatch`（均 204）⇒ 09:52 数据恢复 ⇒ 10:13 又刷一轮（持续活着，非脉冲）。
- **判据沉淀**：多条独立 cron 链同时零 run ⇒ 别只盯单条 workflow 排查，指向平台侧派发缺失。

### 2.4 斐波那契观测窗：卡名重算 + 四方对齐（10:0x–10:21，主人令）

- 根因链：7 个固定窗口（FIB_BASE_DATE=2026-05-22）最晚 2026-07-16 已全走完；`_roll_forward` 需 `reversal_date` 但全 passed 无 confirmed ⇒ `next_cycle` 恒空 ⇒ 前端渲染「距F(?) —」。
- **改法（前端渲染层）**：`_fut2` 三级回退（future→active→最后一条 passed）+ 距F 胶囊双态（「距F(N) N天」/「末窗F(N) MM-DD」）+ 无未来窗口金色提示（事实陈述+复核入口，零预测式表述）。
- **四方对齐（卡名「斐波那契F观测窗」→「斐波那契观测窗」，全部 0 残留）**：

| 方 | 文件 | 结果 |
|---|---|---|
| 前端 | index.html | 旧名 12→0 |
| 逻辑页 | logic.html | 旧名 6→0（CRLF 8486 零裸 LF） |
| 运维 | v8_health_check.py | 旧名 2→0 |
| 数据源 | data/SH_FIB.js | 卡名不在数据层，未动 |

- **🔴 producer 字段铁律（主人令，后续所有卡照此）**：`CARD_DEFS` 每张卡补 `producer` 产出脚本路径，`logic.html` 同步补「更新任务」行，使「看板卡名→产出脚本」一行可反查。SH_FIB 示例：`"producer": "algorithms/fetch_sh_index_fib.py"`。
- 交接档：`docs/ops/handover/2026-09-18_1010_…_斐波那契观测窗卡名重算+盘中链零触发急诊.md`（12588B）。

### 2.5 K型分层卡搬迁（10:28–10:50，主人令：单源观点类归「暂未上架」）

- **归口判定**：线上直取 `data/MACRO_KSHAPE.js`——唯一源=沧海一土狗（单对象+人工策展）⇒ 单源观点类 → 暂未上架；多源综合类（maharo 7 源）留正式页。
- **三件套**：index.html 1280829→**1282948**（`95846547e762`）；logic.html 727540→**728809**（`b866fb05456e`）；v8_health_check.py 188014→**188631**（`25cf7c334c81`）；交接档（`bf6840412dd2`）。
- **核心技术点**：内联块抽为 `window.__renderMacroKShape` + 派发随迁（renderUnlisted 尾部退避 0/600/1800ms）——**只搬壳子不搬函数 = 卡永久「加载中」**（09-12 _fillMahoroCard 同款前科）。
- **验证（非勾叉）**：vm 沙箱真实数据实跑 6652 字符/8 项关键字段全命中；配平 obsPaneMacro 9/9=0；宿主顺序 baihechou@2342 < macroKShape@2984 ✅；node --check / py_compile ✅。

### 2.6 P1/P2/P3 三项收口（11:35–12:0x，主人令「都要」）

| 项 | 内容 | 文件 | 提交 |
|---|---|---|---|
| **P1** | 盘中链首跑探活护栏：**独立 CI job `intraday_patrol`**（与 build `trading!=true` 严格互补），双判据（窗口化应到/实到 + 产物三态 FRESH/STALE/UNKNOWN，每链绑自己产物） | `.github/scripts/guard_intraday_firstrun.py`（NEW 26018B）+ `v8_build_deploy.yml` 27897→31396B | `9691883ba091` |
| **P2** | `_roll_forward` 锚点兜底修复：`FIB_BASE_DATE` 硬编码识别自算峰值 2026-07-01；passed 分支补 observation；reversal_date 缺失时续命生成新窗口（实测 F(55) 2026-10-01） | `algorithms/fetch_sh_index_fib.py` 17537→22923B | `7609747149ff` |
| **P3** | 核实 SH_FIB 20:00 D 批重建机制：`raw_data/sh_fib.json` 与 `data/SH_FIB.js` 的 ut **均为 2026-09-17 18:08** ⇒ 链路真实跑通，非双盲区 | 无代码改动 | — |

- P1 CI 实跑 run `35302788003`：`gate=success` + `intraday_patrol=success` + `build=skipped`（正确互补）；日志 `PASS ×8 → ✅ 自检 8/8 全通`。
- 交接档：`docs/ops/handover/2026-09-18_1135_…_P1P2P3（探活护栏+Fib续命+机制核实）.md`（16070B，`db7c114356b8`）。

### 2.7 哨兵/自动化会话产出（00:45–11:46，E 盘日志）

| 时刻 | 轮次/动作 | 关键结论 | 提交 |
|---|---|---|---|
| 00:45 | 回执哨兵 #3 | IMA 五字段定性更换为「**新版脚本从未跑过**」（21:53 上线晚于当天 cron 15:45）；四量 3 年档根因=派发侧未注入 `V8_BACKTEST_YEARS` | `d801933e2` |
| 02:48 | 回执哨兵 #4（静默） | ✅ FOUR_VOLUME_BACKTEST 已由「近 3 年」修为「近 5 年」（1429→1447，ut 01:23:58）⇒ **四量 3 年档闭环** | — |
| 06:57 | 回执哨兵 #6 | ws-guard 行尾脏 blob 修复**独立复现为真**；BACKTEST_TDX 当时仍未达（76MB）；行尾判定唯一可靠口径=`tr -dc '\r' \| wc -c` | `def3aea6b` |
| 09:20 | 回执哨兵 #7 | ✅ **BACKTEST_TDX 瘦身验收达成**（76,078,046B→10,280B，ut 09-18 06:57:48）；IMA 三件套 blob 逐字节对齐；闸门三不变式 ast 复核全绿；安全微修 3 文件（纯落后同步：v8_stage_gate/dedup_manifest/fetch_ima_strong_stock） | `f7a67e638` |
| 07:30 | maharo 出稿 | daily 净字数 **4510**（角度：分子加速与分母抬升的赛跑），线上 sha `d5e0b3f80…` | `396619fd2e` |
| 07:30 | 白河愁宏观卡 | 21 条，`data/BAIHECHOU_MACRO.js` 15445→17164B，ut 09:14:06 | `c4fc6e3b0` |
| 08:15 | 分红监督 | 云端正常：422 只/失败跳过 42，max plan_date=09-18 当日入库，0 兜底 0 邮件 | — |
| 11:40 | 回执哨兵（4 档对齐） | 阿狸咪 0930 档 P0 门禁阻断（头注 9≠真源 12）**已修**（线上 12 项实测）；**撤回** 1010 档「schedule 派发未发生/明日再观察」两处表述；🔴 **扩样源定位成功**：`raw_data/history/top10_daily_*` 63 个交易日（06-06→09-17）×20 只逐股明细**在仓内**，`backup-*` 分支已证不存在 | `212d8a622` |
| 11:46 | 同步守卫 #94 | 同步 `v8_health_check.py` 8b3a2e594（+4/−7，含 MACRO_KSHAPE 迁页+producer 字段）；判据 D 新形态：整树重建提交内多数文件两侧 blob 逐位相同 | — |

### 2.8 午间两条新 commit（12:00 前刚推，来自并行会话，实测于 origin/main log）

- `396f58017` fix(avg_price): 平均股价 MA 真值化 + 880003 主口径历史回填（主人令·方案A治本）
- `d6414e4e3` fix(factor-lab): badge/caliber honesty — read `verdict_3star` instead of hardcoded "not passed"；declare factor≠stock-picking-strategy

---

## 三、当前系统状态（快照 · 采集时刻 2026-09-18 12:10）

| 项 | 状态 | 证据 |
|---|---|---|
| 站点 | ✅ 200 | `curl -s -o /dev/null -w "%{http_code}" https://ah-quant999.github.io/quant-scanner-v8/` |
| main tip | `4dc03e0a8`（12:12 build） | `git ls-remote origin refs/heads/main` 实测 |
| HEALTH_CHECK | ✅ **全绿** ok117/warn0/**fail0**/limited2/info1，total120，updated 12:12:14 | 线上 `data/HEALTH_CHECK.js` 硬抓（昨晚 fail5 已清零） |
| runner | ✅ alimi-cn + lemoncat-cn 双在线（09:2x 实测，busy=False）；11:46 哨兵轮正常 | E 盘哨兵日志 |
| 盘中链 | ✅ 已恢复（09:52 起持续运行，10:13 复核再刷一轮） | 2.3 节 |
| 双盲区护栏 | ✅ 线上三条不变式全绿（items=29 must=8 _ALWAYS_PUSH=35） | 08:5x 线上 blob 实跑 |
| 探活护栏 | ✅ CI run 35302788003 intraday_patrol=success | 11:5x 实测 |
| 心跳 | handover 每日 12:00/18:00；昨日盘后 16:55 DONE | `_heartbeat.log` |
| 剩余自动化 | 回执哨兵（ae56683e）/ 本地同步守卫（ce2b84c3，~60min 轮）/ maharo 出稿（30e1b099）/ 分红监督（1786588096327）/ 白河愁卡（ff79c5a8）/ 本交接（12:00+18:00） | WorkBuddy automations |

**主树脏态新形态（台账已更新，登记不擅动）**：porcelain 791~795 项 / **暂存区 0 项**（09-18 起新形态，旧台账「暂存位 3 项」已不在）。`guard_double_blind.py` 主树工作区缺失（` D`，属 0900 批新增文件兄弟项）——线上存在（git 树核对 `fe34e7042`），仅本地工作区缺，列观察点。

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 最高优先验收点（今天 15:45 CST cron 后）
1. **IMA 五字段**：查 `data/IMA_STRONG_STOCK.js` 顶层键 6→**≥11** / `no_first_selected` 72→**0** / `field_missing_counts`→空。
   复核：`curl -s https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_STOCK.js | head -c 400`
   若仍 6 键 ⇒ 升级为「cron 用旧脚本」结构性问题（阿狸咪 0830 档三件套 blob 已对齐，脚本本体无问题，问题只可能在链路）。
2. `data_date`/`source_stale` 实际值预期 `2026-09-02`/`True`（= 源确实停更的正确行为，勿误判为 bug）。

### 📋 留给阿狸咪的待办（1135 档 ⑥ 节延续）
3. **P1 留办**：`v8_intraday_snapshot` 降频评估——实测每天仅 2 次 vs 应到 42 次（探活护栏已上线但降频本身未根治）。
4. **P2 留办**：SH_FIB 类别口径统一（挂 A 批 vs 登记 `post_close`，现登记 `post_close` 生效中，需统一）。
5. **P3 留办**：观察今晚 20:00 D 批后 `data/SH_FIB.js` 的 `next_cycle` / `key_window` / `roll_error`（P2 续命逻辑首次随链跑，验收其产出）。
6. **P4 留办**：确认 `secrets.V8_GH_TOKEN` 是否具备 `actions:write`——探活护栏的「自动补派」依赖它，否则降级为只报告。

### ⚠️ 待主人拍板（11:40 哨兵登记，双方均勿擅动）
7. ①扩样等价性（`top10_daily` 是打分 Top20 非候选池形态，不可直接当等价替代）②cron 错峰化（需 workflow 写权限，须小九侧且收盘后）③盘中链自接力 ④76MB 旧 blob 历史重写（改全 SHA 的 P0）⑤`v8_ws_sync_guard.py::REPO` 仍写死 `E:\workspace\stock-scanner`（坚果云 junction 红线项）。

### 🩸 风险与纪律（接手前必读）
8. **主树 porcelain 791~795 项 / 暂存区 0 项**为新常态，勿按旧台账 41 项预设；判暂存位须逐行看首字符。
9. **本仓 git 判据三大坑**（今日反复实证）：`git log --diff-filter=A <path>` 恒假阳性（cache-bot 整树重建）⇒ 用**区间 diff**；浅克隆 `--is-ancestor`/`merge-base` 假阴性 ⇒ 用内容 blob 匹配；`ls-tree` 中文路径须 `-z` + `tr '\0' '\n'`。
10. **行尾判定**唯一可靠口径 = `tr -dc '\r' | wc -c`；`grep -c $'\r$'` 恒假阳性。
11. **`.github/**` 不走 Pages 发布**：核对其线上存在性必须 `git ls-tree <tip>` 或 API，Pages 直取会误判「没推上去」。
12. **接手任何文件前先取线上真身**：11:35 轮实证摘要基线（19217B）与线上实况（27603B）差 8000+ 字节，照缓存基线打补丁会直接覆盖别人改动。
13. 断言值必须 `count()` 实测：「含该串的行数」≠「串出现次数」（今日两度 raise 救回盲改）。
14. 午间两条新 commit（`396f58017` 平均股价 / `d6414e4e3` factor-lab）影响面请夜班顺带巡检（平均股价卡 + factor-lab badge 显示）。

---

## 五、小九监督职责（晚间交接对照表）

| 职责 | 槽位 | 说明 |
|---|---|---|
| 交接自动更新 | 每日 12:00 / 18:00 | 本档即 12:00 槽位产物；18:00 槽位将补充下午进展（含 IMA 15:45 验收结果、20:00 D 批 SH_FIB 观察） |
| 白天独家部署 | 7:45–17:45 | 阿狸咪编辑期间 index.html 红线不碰 |
| 回执哨兵 | ~2h 轮 | 「阿狸咪→小九」新档自动逐项对齐回执 |
| 本地同步守卫 | ~60min 轮 | 6 白名单脚本与 origin/main 三源比对（今日已纠正 82-84 轮方向误判，现以内容直读为准） |
| maharo 出稿 | 07:15 出稿 / 07:30 上线 | 今日 daily 4510 字 |
| 分红监督 | 08:15 | 云端 422 只正常 |
| 白河愁卡 | 07:30 | 今日 21 条 |

---

## 六、本文复核命令（阿狸咪接手时先跑）

```bash
# 1. 远端 tip 与本档基线核对
git ls-remote origin refs/heads/main
# 2. 站点与巡检新鲜度
curl -s -o /dev/null -w "%{http_code}\n" https://ah-quant999.github.io/quant-scanner-v8/
curl -s https://ah-quant999.github.io/quant-scanner-v8/data/HEALTH_CHECK.js | head -c 300
# 3. 今日小九系 handover 档区间 diff（从上一回执 T=212d8a622 起）
git fetch origin && git diff --name-status 212d8a622 $(git ls-remote origin refs/heads/main | cut -f1) -- docs/ops/handover/
# 4. IMA 验收点（15:45 后跑）
curl -s https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_STOCK.js | head -c 400
```

*（小九 · 2026-09-18 12:00 槽位 · 自动化 1785719698947）*
