# HANDOVER 小九 → 阿狸咪（2026-08-10）

> 更新于 2026-08-10 18:22 自动更新（18:00 档）
> 数据源：v8 侧工作日志 `.workbuddy/memory/2026-08-10.md`（stock-scanner 侧日志已断更 08-07 后无新文件）
> main HEAD：`bd99d133`（v8 build: 2026-08-10 18:22）

---

## 一、背景

今天核心主题是**「运维看板自己会巡检、自己自愈」从缺失到建成**。

下午用户连发 4 张运维看板截图（盘后数据 7 张卡 stale、最终推荐入选日期陈旧、任务运行看板 4 天前、实时/选股策略 fail），并指出："你就没有一个监控运维的，发现错了不是等我告诉你，而是你自己发现自愈的自动化？"

排查确认根因：**云端此前没有"每小时巡检数据卡片 + 自动派发治愈"的常驻机制**——
- `v8_self_heal.yml` 只在**周六 14:00** 跑一次（周末兜底）
- `runner_health_alert.yml` 只查 runner 离线开 issue
- 本机看门狗（WorkBuddy 自动化）若那阵子没跑，运维巡检整体停摆（表现即"任务看板 4 天前 / 盘后数据 stale 没人管"）
- `v8_health_check.py` 的 `self_heal()` 逻辑健全，但因看门狗调它时**从不传 `--heal`** 且触发条件卡 `overall != "ok"`，实际长期不生效

**已建立常态自愈机制（见第二节 `eae9fdad`），从根上解决"等人看图才发现"。**

## 二、今日已完成改动（commit SHA 均经 git 核实，全部在 origin/main）

| 时间 | 提交 | 内容 | 影响 |
|---|---|---|---|
| 08:09 | `4cd4d75a` | v8 盘前全量 | 盘前数据 08:12 完成 |
| 08:51-09:12 | `fb06d2f1` | backtest_tdx 回退修复 | 线上 calc_time=08-10 ✅ |
| 09:34 | `d50d24b7` | SH_SZ 历史回退修复（用户「修」授权红线例外） | 序列回到 8/7 ✅ |
| 09:50-10:15 | `5d6d5e0d` | maharo verify 通道根因修复 | 远端已验证 inputs 版 |
| 10:05-10:15 | — | 涨停热度字段丢失 + 资金流向停更真故障 | 终态 e163eaf6 ok36/fail0 |
| 10:30-10:40 | `072933e0` | maharo 研报发布上线 | source_dist 含 maharo研报:14 ✅ |
| 11:10-11:25 | `4ccf8586` | 盘中 7 实时源被云端 10:49 cn fetch 误清空，抢修恢复 | 线上验证一致 ✅ |
| 14:55 | `f40de729` | 盘中自愈加固（premarket_cleared 盘中守卫 + auto_dispatch 逐源粒度 + 统一 workflow ID） | 防 10:49 类误清空复发 |
| 15:09-15:18 | `6ad23fc1` | 看板自愈补全（MARKET_FUND_FLOW intraday 刷新 + 本地/Pages 同步自愈） | self_heal 接管管线同步 |
| 17:35-17:55 | `ba3d6dd6` + `6fefd06e` | 平均股价卡 + 概念→ETF·龙头参考卡上线 | 两张卡已接前端，平均股价 f_avg_price 改 em_clist |
| **18:14** | **`eae9fdad`** | **🔧 运维看板常态自愈巡检（用户核心诉求）** | **见下，从根上实现自动监控+自愈** |

### `eae9fdad` 运维看板常态自愈巡检（重点）
- **新增 `.github/workflows/v8_ops_self_heal.yml`**：纯云端 ubuntu-latest，**每 30 分钟**（UTC 23/53 分）跑 `v8_health_check.py --heal`，独立于本机看门狗，GHA cron 永不漏跑。发现任何 fail 卡片即按 page→category 自动派发对应 cn_fetch / 盘后算法链刷新；本地/部署不同步自动对齐+派发 build_deploy；自愈成功发「已自愈」确认邮件，失败升级邮件。
- **`v8_health_check.py`**：`self_heal()` 解除对 `overall` 的依赖（只要 `--heal` 开就跑，内部自行判断 fail 卡片/内容审计陈旧项）；新增 `_dispatch_algo_run()` 覆盖「最终推荐入选日期陈旧」等内容级问题，自动派发盘后算法链重新产出。
- **`v8_cloud_watchdog.py`**：`run_health_check()` 显式传 `--heal`（本机那路双保险）。
- **已验证**：刚跑 `health_check --heal`，自动识别"最终推荐入选日期陈旧（enter_date 08-07 < 最近交易日 08-10）"并**自动派发 algo_run（HTTP 204）**——正是用户截图里图 2 的问题，现在无需人工。

## 三、当前系统状态

| 项目 | 状态 |
|---|---|
| 站点 | ✅ GitHub Pages 正常；HEALTH_CHECK.js 18:02 巡检 `ok35/warn1/fail0`，自愈已跑 |
| runner | ✅ 云端 ubuntu（lemoncat-cn 已弃用） |
| main HEAD | `bd99d133`（v8 build: 18:22）|
| 健康检查 | ✅ ok34-35/warn1-2/fail0（18:12 巡检：四量终极已自愈；最终推荐陈旧已 auto dispatch algo_run）|
| 数据链路 | ✅ 用户 17:33 截图的 stale 已全部修复——已手动 dispatch post_close/intraday/premarket cn_fetch + 盘后算法链 + build_deploy（均 HTTP 204），数据已刷新 |
| 运维自愈 | 🆕 **每 30 分钟云端常态巡检 `v8_ops_self_heal.yml` 已上线**（`eae9fdad` 入库）；看门狗本机那路 heal 双保险 |
| 未提交本地文件 | `URGENT_小九_2026-08-10_141轮看门狗.md` / `HANDOVER_小九_读阿狸咪周末交接_核对.md`（untracked，均运行产物/核对文档，未纳入提交）|

## 四、需阿狸咪注意 / 待办 / 风险

1. 🟢 **运维看板自动监控自愈已建成**（用户今日头号诉求）：今后盘后数据/实时数据/今日事件/选股策略任何卡片 stale，云端每 30 分钟自动派发刷新，不再依赖你截图发现。**建议观察 1~2 天**确认常态运行无异常（看板「任务运行」应显示"今日"而非"4天前"）。
2. 🟡 **最终推荐入选日期陈旧**：已 auto dispatch 盘后算法链（18:30 档产出），待跑完验证 enter_date 更新到 08-10；若仍未更新需查 algo_run 是否报错。
3. 🟡 **local_sync git 分叉**（ahead2/behind19，并发会话遗留）：每小时看门狗 141 轮未 commit/push；建议清理 stash 并 `git fetch + merge --ff-only` 对齐，避免后续 rebase 冲突。
4. 🟡 **10:49 盘中误清空根因仍待查**：已有 `cloud_fetch_v8.py` 盘中守卫（≥09:30 拒绝清空 intraday）兜底防复发，但"为何盘中执行盘前清空逻辑"的上游根因未根治，建议查 runner 日志。
5. 🟡 **maharo verify 闭环**：workflow 已改读 inputs（5d6d5e0d），等新验证码 dispatch verify。
6. ⚠️ **历史教训（今日多次重演）**：云端 build 每次提交带新 `?v=` 的 index.html/cloud_fetch_v8.py 会**还原未提交的本地改动**。任何改动必须同轮 commit+push 并 `git ls-remote` 二次确认，绝不允许未提交状态停留。

## 五、小九监督职责

- ✅ 每小时看门狗：141 轮六管线全绿（0 封告警邮件）
- ✅ **每 30 分钟云端自愈巡检**（新增 `v8_ops_self_heal.yml`）：自动发现+治愈运维看板 stale，无需人工
- ✅ 即时修复：用户 17:33 截图反馈后，已 dispatch 5 个 workflow 刷新全部 stale 数据；`health_check --heal` 自动派发 algo_run 修复最终推荐陈旧
- ✅ 12:00 / 18:00 交接自动更新（本档）
- ⏳ 待办：观察新自愈机制首日运行；复核最终推荐 enter_date 更新；清理 local_sync git 分叉

---

*本文档由小九 18:00 档自动化生成，仅提交 HANDOVER 文件本身，未纳入其他改动。*
