# 交接｜阿狸咪的工程师 → 小九的工程师｜2026-09-13 21:15
## 禁止 0 冒充全仓补完 + 重跑 comprehensive 落地 + 池观察标注 + 候选宇宙停滞定因线索

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 署名：阿狸咪的工程师（alimi-cn）｜收件：小九的工程师（lemoncat-cn）
> 基准远端：`da7221b2e2`（本单推送前 tip；CI build `f2e2d3041` 已重建 data js，实测通过）

---

## 一句话结论

主人 09-13 令「统一测算标准，用最科学的计算」+「其他按你推荐处理」，4 项推荐**已全部落地**：
① **0 冒充全仓补完**（我上轮只修了 comprehensive 一处，本轮扫出同款 **3 处**）；
② **comprehensive 重跑**，伪值清零（T+75/90「0% 胜率」不再上屏）；
③ 基础股池加**动态「观察」标注**（金股池弱、候选池不弱，判定随数据自动变）；
④ 星级人工覆写口**两处同源**（根治「同策略两星」）。

另**核验并更正**了我上轮 3 处说法（第四节）——其中 **2 项其实早已完成**，我不该把它们列为待办。

---

## 二、本轮推送

| commit | 内容 |
|---|---|
| `216049fef` | 全仓禁止 0 冒充（`v8/backtest_crds.py` / `algorithms/backtest_tdx.py` / `algorithms/strategy_four_volume.py`）+ 前端 null 守卫 + 池观察标注 + 星级覆写同源 |
| `da7221b2e` | 重跑 `raw_data/backtest_comprehensive.json`（CI 自动重建 `data/BACKTEST_COMPREHENSIVE.js`） |

---

## 三、0 冒充：影响面的**诚实**披露（请别按我上轮的措辞理解）

我上轮说「`data/CRDS_BACKTEST.js` T+30 起同样是 0 冒充（源头在 `run_algorithms.py`）」——
**两处不准**，本轮实测更正：

| 我上轮说的 | 实测事实 |
|---|---|
| 源头在 `run_algorithms.py` | ❌ 源头是 **`v8/backtest_crds.py`**（`run_algorithms.py` 只是调度器） |
| 它是「线上可见的假数据」 | ❌ **当前无用户可见影响**：`index.html:4530` 与 `logic.html:8379` 都**只渲染 [1,3,5,10,20]** 且已有 `wr!=null ? '—'` 守卫；聚合器 `gen_backtest_all_algos.py` 也已把 `samples=0` 的档转成 `None` |

**所以 CRDS 那三处是「数据层脏值」（扩档即暴露），不是「正在骗人」。** 我仍然修了（一致性 + 预防），
但价值定位是**预防性**，不是救火。真正在线上骗人的是下面这个：

### 🔴 真正线上可见的假数据：`BACKTEST_COMPREHENSIVE` 的 T+45/60/75/90

重跑前后（实测对比，同一份线上源）：

| 档位 | 旧产物（假） | 重跑后（真） |
|---|---|---|
| `hold_1d` | n=311 | **n=306** |
| `hold_20d` | n=311 wr=47.7% | **n=195** wr=47.7% |
| `hold_45d` | n=311 wr=20.0% ar=-0.34 | **n=20** wr=20.0% ar=**-5.30** |
| `hold_60d` | n=311 wr=20.0% | **n=20** wr=20.0% |
| `hold_75d` | n=311 **wr=0% ar=0.0** | **n=0** wr=**null** ar=**null** |
| `hold_90d` | n=311 **wr=0% ar=0.0** | **n=0** wr=**null** ar=**null** |
| `hold_180d/250d` | 无 | **n=0** wr=null ar=null |

**根因**：旧产物 `count` 与 `win_rate` 分母**不同源**（count 把未到期档也计入），于是出现
「n=311 / 胜率 20%」这种自相矛盾行；T+75/T+90 更把「根本没数据」直接上屏成「0% 胜率」。

主表口径（重跑后）：`resonance_all` 313/325 有效（最佳 T+1 53.3% / -0.06%）、
`resonance_gte80` 121/133（最佳 T+20 61.9% / +4.34%）、`resonance_gte70_lt80` 192/192。

**重跑方法（与铁律对齐）**：`materialize_remote_root.py` 把**线上** `algorithms/` +
`raw_data/history/` 落到**仓库外** → `python algorithms/backtest_comprehensive.py`
（baostock 真实收盘价，8m19s）→ 只推 `raw_data` 源（该文件在 `update_v8.py` 的
`DATA_SOURCES` 映射表 **L88**，CI 会自动重建 `data/*.js`，不手工生成以免格式漂移）。
行尾已核：线上原文件本就是 **CRLF**（46115/46115），新产物同为 **CRLF**（46477/46477），守恒。

### 同款 0 冒充全仓扫描结果（供你复核口径一致性）

```
v8/backtest_crds.py         · fmt_pct 无 None 穿透 · 3 处 else 0     → 已修
algorithms/backtest_tdx.py  · 2 处 wr if decided else 0              → 已修
algorithms/strategy_four_volume.py · win_rate/avg_return else 0      → 已修
algorithms/backtest_pools.py · 已是 None（我上轮修的）                → 无改动
algorithms/backtest_comprehensive.py · 已是 None（我上轮修的）        → 无改动
```

---

## 四、我上轮把 3 件事说错了（核验后更正）

| 我上轮列为「待办」 | 实测事实 | 结论 |
|---|---|---|
| `STOCK_PROFILE` 停产 | `update_v8.py` **L39-42** 已写明「2026-09-13 主人令『前后端都无人读，删干净』」，`data/STOCK_PROFILE.js` 在 `origin/main` **已删除**；`index.html` 现存 18 处均为**就地合成**的消费/注释（L521 明示） | ✅ **早已完成** |
| 仓库根 3 个未跟踪 `.bak` | `origin/main` 的 `.bak` 计数 = **0**（早已清理）；本机 16 个 `.bak` 是**落后工作树 + 坚果云**的本地副本（`backup_*/data/`、`raw_data/`），**远端不含** | ✅ **远端早已完成**；本机副本我不动（动它有反向同步风险、零收益） |
| `SECTOR_RS` 未挂算法链 | `fetch_sector_rs.py` **已在** `run_algorithms.py` 的 ORDER（L142）**且** 3 个 cn fetch workflow 都调它；前端 `index.html` 多处消费 `window.SECTOR_RS` | ❌ **采集早已纳链**；真正没有的是**回测**（无 `SECTOR_RS_BACKTEST` 产物） |

> ⚠️ 教训记下：判「有没有做」必须 `git show origin/main:<file>`，别用本机工作树 ——
> 本机落后 190 项 M 文件，`git status` 会静默撒谎。

---

## 五、`SECTOR_RS` 要不要纳入回测？—— **需要先定口径，我不擅自动**

它**不是**个股信号（无买卖点），是板块周期定位数据（每日 17:47 盘后定稿）。
要回测必须先回答 3 个问题，否则做出来又是一堆 0 冒充：

1. **标的**用什么？该板块指数（申万/中信一级）还是成分股等权？
2. **信号**取什么？「周期定位=底部反转」这一档算入场？
3. **基准**用什么？跟沪深300 比还是跟板块自身均值比？

我建议（等主人拍板，不擅自动）：**先用「板块指数」+「底部反转档入场」+「对照沪深300」** 做最小可用版，
且**必须**复用本轮刚落地的 null 语义（未到期 ⇒ None）。

---

## 六、候选宇宙停滞：我给你**定因线索**（这条是你的待办，我不越界改）

### 实测：全仓**只有 4 个文件**卡在 `2026-09-11 15:00:00`

```
data/CANDIDATE.js        data/GOLD_POOL.js
raw_data/candidate.json  raw_data/gold_pool.json
```
其余 `raw_data/**` 全部正常 ⇒ **不是全链停滞，就是这一对池产物被写回旧时间戳**。

### 回退时间线（`raw_data/candidate.json` 逐提交实测）

| 提交 | 时刻 | 文件内 `update_time` |
|---|---|---|
| `7201681e5` | 09-11 22:39 | `2026-09-11 22:38:42` |
| `7920c61ac` | 09-12 00:17 | `2026-09-12 00:17:06` |
| `dacb0f8e5` | 09-12 00:54 | **`2026-09-12 00:53:46`** ← 最后真实值 |
| `485f1719b` | **09-12 22:44（周六）** | **`2026-09-11 15:00:00`** ← **回退发生在此** |
| `a7b5c8c50` | 09-13 16:45（周日） | `2026-09-11 15:00:00` |

两次覆盖者都是 `github-actions[bot]` 的 **`v8 cn fetch`** 提交。

### 定因结论（⚠️ 我第一版算错了时区，已按 UTC 重排 —— 结论**反转**）

**务必按 UTC 对齐比较**，`git log` 混用 `+0000`/`+0800` 会得出相反结论：

| 事件 | 提交 | 时刻（UTC） | 时刻（北京） |
|---|---|---|---|
| **回退发生** | `485f1719b` | **09-12 14:44** | 09-12 22:44（周六） |
| **scanner.py 修复** | `7cb0dfad7` | **09-12 16:12** | 09-13 00:12 |
| 原样再推 | `a7b5c8c50` | 09-13 08:45 | 09-13 16:45（周日） |

⇒ **修复（16:12 UTC）比回退（14:44 UTC）晚 1.5 小时**，即**根因已被修**，修复本身是对的：
`scanner.py` L2741 / L2826 `pool["last_update"] = datetime.now()` → L2748 / L2833
`pool["update_time"] = pool["last_update"]` ⇒ 产出时间戳恒为**运行时刻**。

**为什么 09-13 16:45 还是旧值？** 因为写 `candidate.json` 的步骤是
`.github/workflows/v8_cn_fetch_cloud.yml` 的 **`soft_8`「📚 外资研投研报抓取并重建候选股池」**，
它带闸门 **`if: steps.cat.outputs.category == 'post_close'`** ⇒ **周日不跑**（符合 `chain_day`
「周日不跑」），所以那两次提交只是把**已经坏掉的文件原样再推一次**，内容没变。

### 所以这条不是「bug 未修」，而是「**修复已上、产物待刷新**」

**验证方式（你那边最省事）**：等下一次 **post_close** 批跑完（周一夜窗），
`git show origin/main:raw_data/candidate.json | grep update_time` 应变为**该批运行时刻**。
若仍是 `2026-09-11 15:00:00`，才说明 `soft_8` 没走到 scanner.py 的那条赋值路径
（届时再查 `stage_to_raw` 搬的是不是残留的 `out/candidate_pool.json`）。

### 仍建议加一道**单调保护**（防御，非救火）

即使根因已修，`update_time` 回退这类事故**没有护栏**（任何脚本误写旧值都会静默回退）。
建议在 `stage_to_raw` 层加：新值 `<` 旧值时**不覆盖**（或告警）。这条归你决定要不要做。

### 我已排除的可能

- ❌ `algorithms/fetch_weekend_run.py`（周末跑批）：它是**只读**的（docstring 明示「只读已存在的
  raw_data + data，不发起任何外网请求」），只做新鲜度快照，**不写**这两个文件。

---

## 七、留给你的其余项（与上轮回执一致，无变化）

| 项 | 归属 |
|---|---|
| 候选宇宙停滞 —— **结论：修复已上、产物待刷新**（见第六节），请你在下次 post_close 后复核一眼；`stage_to_raw` 的单调保护要不要加由你定 | 小九 |
| PE/PB 接线方案（A：接 E 批 / B：单建定时） | 主人拍板 → 小九执行 |
| 7 因子 walk-forward（0/7） | 小九 |
| ETF 双页文件名新坑 | 小九 |

---

## 八、验证清单（本轮全部实测）

- `htmlcheck.mjs`：25 段 inline 脚本 **0 失败**，8 类标签净差全 **0**
- 行尾：`index.html` CRLF=**0**（LF 守恒）；`raw_data/backtest_comprehensive.json` CRLF 守恒
- `py_compile`：`v8/backtest_crds.py` / `backtest_tdx.py` / `strategy_four_volume.py` **3/3 OK**
- node 实跑 `__committeeRate` × `__committeeStarsOf` 五卡一致：
  三重共识 **4⭐**（人工覆写，score 3.5）· 四量终极 **4⭐**（score 4.66，T+1 44.6% 触发短周期惩罚）·
  逆势龙头 **累积中**（cov 70%，n=4）· 候选池 **基础股池**（weak=false）· 金股池 **基础股池 + 观察**（weak=true，主口径 32.28% < 45% 红线，n=127）
- 推送走 `atomic_patch_push.py`（FETCH_HEAD 派生 + 临时索引 `commit-tree`），**零碰**本机 190 项 M 工作树

---

*署名：阿狸咪的工程师（alimi-cn）*
*基准远端 TIP：`da7221b2e2`*
