# HANDOVER_小九_2026-08-03 — v8 本周末改版交接

> 给单位机小九接手用。阿狸咪（家里机）自 2026-07-23 起断线，你目前**全面接管**所有定时任务（含原本阿狸咪的周末任务）。本周末 v8 全部改版已落 main 并推送，线上已生效，不会被覆盖。

## 0. 一句话现状
- 你（小九/单位机）单机跑双机职责，标注「代阿狸咪/🖥️」的任务即原属阿狸咪、现由你顶上。
- 本周末 v8 全部前端/数据改版已 commit+push 到 main，本地与 origin 同步，`git status` 干净（除历史遗留未跟踪临时文件，勿提交）。
- 线上 GitHub Pages 已是最新版，无需额外部署。

## 1. v8 仓库真相（必读）
- 仓库：`ah-quant999/quant-scanner-v8`（**独立仓**，与 v6 的 stock-scanner 无关，别混）。
- 真相分支：**main**（唯一）。GitHub Pages 直接从 main 服务，push 即上线。
- 本地工作区：`E:\workspace\quant-scanner-v8`。
- 数据生产：核心盘前/盘中/收盘/清理已迁 **GitHub Actions 云端**，不依赖任何本机开机；v6→v8 同步桥（盘后）依赖本机跑。

## 2. 本周末（8-1~8-3）全部改版清单（均已 push main）
| commit | 内容 |
|---|---|
| 89da5fc | 4类上游原生化 gold_pool/scan_result/watch_result/guanlan_*/maharo_signals（含 2 个关键 bug 修复） |
| 7466799 | 每日洞察(TAB3)/大牛股猎手(TAB4) 对标参考页重构 |
| 300ec6d | 北向/机游周度锚点修复（修第6周 8/31-8/31 错位） |
| db90d8c | 每日洞察修复 NF 未定义 + CAND.stocks dict→array 兼容 |
| 589f601 | 周度锚点改 today + 北向 hasMultiDay undefined 修复（曾致整 TAB 空白） |
| 6445e9b | 删主页 4 个死按钮(无onclick纯占位) + 逻辑详解补本周建议表 |

验证：`git log --oneline -6` 应见以上 6 条；`git status -sb` 显示 `## main...origin/main` 无领先/落后。

## 3. 操作铁律（违反即假成功/数据回退/改动被吞）
1. **改 v8 源码后必须**：`git add -f <文件>` → `git commit` → `git fetch origin` → `git rebase origin/main` → `git push origin main`。只 push 不 commit 的本地改动会被 safe_pull 吞。
2. **禁止在本机跑 `safe_pull` 清自己未提交的东西**（它本质 `git reset --hard origin/main`，会把本地未推送 commit 也清掉）。拉远端用 `git pull --rebase` 或按上面姿势。
3. **deploy_v8.py 自带护栏**（检查本地 index.html 不缺 origin 已有板块，防旧副本覆盖）。正常 push main 后 Pages 自动生效，一般不需手动 deploy；若手动 deploy 报"缺少板块"，先 `git pull` 同步再跑。
4. **用户可见页面（逻辑详解/运维/驾驶舱/任何面板）绝对不得出现**：具体数据源名、脚本路径、本地/远端文件名、机器节点名（小九/阿狸咪/中国节点）、跑批时间、内部 API 名。这些只写 HANDOVER + 代码注释。
5. **"更新于"**：所有面板用相对时间（今日/昨日/X天前+时分），不写裸日期。

## 4. 当前数据状态（接手要知道）
- `lhb_data.json` 目前仅 7/31 单日数据（v6 侧自 6/28 停更）。**周一 2026-08-03 18:30 中国节点原生化 maharo 链接会产出新鲜龙虎榜数据**，届时 v8 机游/北向/大牛股模块才有新内容。
- 北向资金：港交所 2024-05 后停更 top_buy，系统仅展示「龙虎榜北向席位」，勿当全市场北向解读。
- 数据新鲜度闸门会拦截过期核心源部署；concept_map/maharo 等易抖/需登录的仅告警不阻断。

## 5. 待办/建议（已记进逻辑详解「本周待办/建议表」）
- v8_self_heal workflow 建议并入 21:00 窗口，删掉这个独立 workflow（周度兜底更集中）。
- 首周六：月度报告 + STOCK_LIST pinyin 重生 + 依赖体检（加 `if 首周六` guard 进 21:00 脚本）。
- 首周日：旧数据归档 + git gc/pack + 备份轮转（加 `if 首周日` guard 进 23:00 脚本）。

## 6. 风险提醒（接手前看）
- **阿狸咪即使恢复联网也绝不接手**（2026-08-03 长期架构决策）：她本机全部 automation 已 PAUSED（含原"周末云端监督与兜底部署" automation-1785510427927），不会自动续跑、不会与你重复。**若她误上线导致任务跑起来，立即 PAUSE 她本机 automation，不要让她碰生产任务**。你无需担心双机冲突。
- **紧急指令监听通道保留**：阿狸咪本机保留"紧急指令监听"(automation-1783696499491) 作为主人的兜底呼叫通道（只读小九 URGENT 文件，不跑生产），这是待命性质，不影响你。
- **周末覆盖**：你（单位机）自动化排班是 MO-FR（工作日）。周末/节假日由 GitHub Actions 云端 v8 工作流兜底（周日 09:00 全量兜底 + 周六 14:06 self_heal + 周六 21:00 cleanup），无需你周末在线。
- 家里机是否需一直开机：**不需要，且建议保持关机**。当前架构下她不接手任何生产，关机不影响系统（云端 production 不依赖本机）。

## 7. 排错速查
- 某模块空白/陈旧：先 `git log` 看最近改动 + 看对应 JSON 的 update_time；多数空白是 JS 变量 undefined（如 NF/hasMultiDay）或 `.stocks` 数据结构 dict vs array 混用（用 `_rcArr()` 包一层）。
- 部署假成功：检查 `git status` 是否干净、main 是否真 push 上去（v8 无 gh-pages，直接 main）。
