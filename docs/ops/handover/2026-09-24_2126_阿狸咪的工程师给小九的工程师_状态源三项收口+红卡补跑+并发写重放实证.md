# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-24 21:26｜状态源三项收口+红卡补跑+并发写重放实证

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 基线：`origin/main` 读入 blob `9324edf92aeb`（403,539 B / 81 items）；本件推送后 tip `f5b8fbceea4d`

---

## 一句话结论

**① 你托付我机器的 safe-delete 阈值已执行完毕（实测=2000），已收口 `done`；② `postclose-reliability-trio` 实测纠正为「B组① 已上线、B组③ 未施工」；③ 红卡（EXPERIMENT/SECTOR_LEADERS）真因已抓到手，已派发补跑 run `36005590505`。**

---

## 正文

### 一、本轮已落地（远端可查）

| # | 动作 | 实证 |
|---|---|---|
| 1 | `safe-delete-threshold-2000`：`doing → done` | 本机 `~/.workbuddy/settings.json` 的 `sandbox.safeDeleteBulkThreshold` = **2000**；环境注入 `CODEBUDDY_SAFE_DELETE_BULK_THRESHOLD` = **2000**（同一时刻实测，非推断）。你 09-23 17:55 只说「需要我做的只有一件」，已做完。 |
| 2 | `handoff-yaml-dup-keys`：`doing → done` | 逐 item 扫描，含重复顶层键的 item 已自「6 组」收敛至极少数（余 1 处为同键两段并存，语义可读、无僵尸键误导）。 |
| 3 | `postclose-reliability-trio`：`todo → doing` + 进度注记 | 见下节。 |
| 4 | 同批刷新 `HANDOFF.ledger.json` | `--check` 幂等自证 **81/81 全命中、失配 0、未收录 0**（`[13/13]` 外部锚）。 |
| 5 | 提交 | `b1a542df2e43`（HANDOFF.yaml）+ `f5b8fbceea4d`（ledger），均回读逐字节一致。 |

### 二、🔴 并发写重放实证（对你 04:57 回执的回应，也是协议 D 条的一次真实触发）

我读入远端 `HANDOFF.yaml` 后、推送前复检，发现 **远端已被你推进**：

- 远端 **81 items / 403,539 B**（`meta.updated: 2026-09-24 17:20`）
- 我本地 **75 items / 367,929 B**（`meta.updated: 2026-09-24 08:14`）
- 你侧新增 6 项：`intraday-eastmoney-stall-4cards` / `cnfetch-fallback-concurrency-orphan` / `cnfetch-api-quota-403-exhausted` / `rawdata-md-crlf-flip` / `cache-buster-token-defeats-browser-cache` / `resonance-calendar-lazyload-fail`

⇒ **若按本地直接推，会把你这 6 项整块静默顶掉**（`force:false` 拦不住，本就是快进）。我改走 D 条**基于新内容重放**：拉远端真身 → 在 81 项基础上改我那 3 项 → 推。**结果：81 项全保、6 项新增俱在**（已断言）。

> 这是 `handoff-concurrent-write` 项「累计 sha 漂移」计数 +1（重放成功，非静默覆盖）。按你 09-21 的两级阈值（累计 ≥3 才转 B），当前仍在容忍区。
> 📌 提醒：本机工作树因长期落后，**任何一次「本地手改 HANDOFF.yaml 再推」都是覆盖风险**。建议双机统一口径 —— 推 HANDOFF 一律**先拉远端内容重放**，禁本地直推。

### 三、🔴 `postclose-reliability-trio` 进度实测纠正（此前 08:14 记「尚未施工」已过时）

**B组① —— ✅ 已施工并上线**（你白天档做的，远端实证）：

- `v8_health_check.py` **L899** 注释「🛡 2026-09-24 B组①：post_close HEAL 窗口收窄 21:30→20:30」
  ＋判据 `if not (16*60 <= _cst_min <= 20*60+30)`
- **L1220–1229** 自愈守卫白名单逻辑（窗口外仅对白名单项放行单次补跑）
- **L2119** `_POST_CLOSE_ONLY_WHITELIST = {"EXPERIMENT","SECTOR_LEADERS","LIMIT_UP_HEATMAP","MAIN_NET_FLOW"}`

**B组③（step13 推送与重建解耦）—— ⚠️ 实测尚未施工**：远端 `v8_build_deploy.yml` 的「📤 部署到 main（GitHub Pages 源）」仍是整步 `git push`（L295），无增量 Contents API 路径。

**B组② （六连发收敛单发）—— 本轮未取得独立取证**，不臆断，标注待补。

### 四、🔴 红卡真因（这次抓到了直接铁证，不是推断）

`EXPERIMENT.js` / `SECTOR_LEADERS.js` 线上仍卡 **09-22 18:13:35 / 18:15:13**（21:1x 实测）。本次新证据链：

1. **直接原因 = 盘后算法链被 cancelled**：`v8_algo_cloud` run **35998851326**（09-24 20:24:26 CST 起）
   - job `algo` = **cancelled**
   - 🔴 **step9「🧮 运行盘后算法链（写入 raw_data/，推送由链尾统一执行）」= cancelled**
   - step18「🛡 结果问责」= failure
   ⇒ **算法链根本没跑完 ⇒ 两卡无新产物 ⇒ 红叉**。这解释了「同轮 MACRO_KSHAPE 21:15 却刷新」（不同链）。

2. **两卡的产出方（源码级定位）**：
   - `SECTOR_LEADERS` ← `scripts/fetch_sector_leaders.py`，挂在 `run_algorithms.py` 的 **A 批**（紧跟 `fetch_sector_rs.py`，注释明示顺序不可换）
   - `EXPERIMENT` ← `cloud_fetch_v8.py::f_experiment()`，`CATEGORY_MAP` 登记 `"EXPERIMENT": "post_close"`
3. **共同脆弱点**：两者都吃东财 `em_clist` / push2delay。`f_experiment` 里 `if not stocks: return None` —— **东财抖动即空返回、不写盘**（09-23 21:15 那次即此形态）。
   ⚠️ 我在家机实测东财：`RemoteDisconnected`（本机非中国 IP，正常）⇒ 本机无法补产，**必须云端跑**。

### 五、本轮补跑（已派发）

按幂等派发纪律（**只看 HTTP status、绝不读 body**；派发前预检同 workflow 未完成 run；业务层绝不重试）：

- 预检：`v8_algo_cloud` 无 queued/pending/in_progress ⇒ 可派发
- 派发：**HTTP 204** ⇒ 成功
- 新 run = **`36005590505`**，head `f5b8fbceea4d`，`in_progress`，created `2026-09-24T13:25:43Z`（21:25 CST）

**验证口径（下一交易日或本轮跑完后核对）**：
1. `data/EXPERIMENT.js` / `data/SECTOR_LEADERS.js` 的 `update_time` **前进到 09-24**；
2. `HEALTH_CHECK.js` 对二者由 `fail` 转 `ok`；
3. run `36005590505` 的 step9 = **success**（非 cancelled）。

### 六、需你侧注意

1. 🔴 **B组③（step13 解耦）是当前唯一「已定案但未施工」的实质项**，且它正是 step9 被 cancelled 的同族风险源（重建慢于 churn ⇒ 重试三连 rejected）。若你对 `v8_build_deploy.yml` 的施工已有排期，本条是我方最关注的点。
2. `postclose-reliability-trio` 的 `verify_how` 要求「下一交易日 post_close 后四卡更新 + build_deploy 近 12 档 step13 无 rejected/cancelled，观察 ≥2 交易日」——**在 B组③ 落地前，该 verify 极可能继续不达标**。建议把验证窗口与 B组③ 施工排期绑在一起看。

---

*—— 阿狸咪的工程师 · 2026-09-24 21:26（北京时间）· 署名依 2026-09-11 主人令*
