# HANDOVER 小九 · 2026-08-30 白天（阿狸咪交接 · 续 HANDOVER_小九_2026-08-30.md）

> ## ⚠️ 第一条也是最重要的一条：当前 `main` 即权威版本，**禁止回滚 / 禁止覆盖 / 禁止 `git checkout` 旧 commit！**
> - 这两天（8/29–8/30）我（阿狸咪）的全部改动已 **commit + push 到 `main`**，当前权威 HEAD 见 `git rev-parse origin/main`（本文件落盘时基准 = `43808ec3d`，本次又追加了防回滚自检脚本 + 本文件，最终 HEAD 以 `git log -1 origin/main` 为准）。
> - **唯一正确的恢复姿势**：`git fetch origin main && git reset --hard origin/main`（永远回到最新权威，不要用任何历史 commit hash，否则会把已修好的"一劳永逸"逻辑退回旧版）。
> - 切勿 `git revert` 这些提交、`git checkout <旧commit> -- index.html`、`git push --force` 旧版本 —— 时间标签防未来 / 暂未上架3卡 / 已下架+暂未上架实数据 / 炸板6列 / 大盘判断去重 / nav解锁 全没了。
> - 若发现任何"卡片空白/被覆盖/黄红灯"，**先看本文件 + `git log -1 origin/main`，不要回滚**，先排查数据源。

---

## 一、这两天（8/29–8/30）我改了什么（完整批次）

按主人令「一劳永逸式修复 + 全面审计 + 清理死代码 + 自愈闭环」，分 5 批落地：

### 批次 A — 前端展示层 + 算法根因（8/29 深夜–8/30 凌晨）
| 提交 | 内容 |
|---|---|
| `5e3015b38` | 一劳永逸全修（前端展示层 + 算法根因） |
| `c7e37b97c` | 13 项一劳永逸（mega patch） |
| `886d97a93` | ①**时间标签防未来显示**（`_fmtAshareRel` 加护栏 + `scripts/momentum_common_filter.py` 改用 `v8_date.today_data_date()` 真实交易日，根治"周日上午显示今日16:29"）②**暂未上架·强势突破只留3卡**，其余归调试专区（修了提前闭合 `</div>` 导致两子页雷同的 root cause）③斐波那契/商品弹性三卡移至已下架·盘前条幅下方 ④**删驾驶舱双灯**（无回测证据） |
| `95a33aea0` | 11 项 LOW 打磨：跨卡时点标注 / 估值→估值(回调代理) / Fib下一窗口含active / 资金流MA5,20 / 涨停"其他"桶沉底 / H反推快照日期 / 四量非故障说明 / FOMC枚举 / 申赎时点 / 板块Block3源标注 |

### 批次 B — 死代码清理 + 健壮性（8/30 上午）
| 提交 | 内容 |
|---|---|
| `8114dab04` | 删实时数据区 ~430 行死代码（旧 LHB 月历 / 历史条 / `_showLhbDay` / 四张分析卡 / 旧大牛股猎手脚本——其 DOM 容器已不存在，由 sec-rc 新日历接管）；AI速览缺数据不再崩；平均股价去不可达 return；删 `_jyCalNav` 重复定义 |
| `b40414121` | `v8_health_check.py`：注册新卡(DELISTED/UNLISTED_PANEL/AVG_PRICE_DATA) + `check_*` 返回 None 时 `or []` 兜底防崩 |

### 批次 C — 调试专区卡片清理（8/30 上午，主人最后一令）
- 删「🚀 个股动量状态」卡（251 行 / 17761 字符）从调试专区 `ulPaneDbg` —— 该卡名字含"动量"易误认属强势突破，实为独立 OCR 抽取数据卡，已下沉删除。
- 调试专区现仅 3 卡：🤖 AI 预测 · 市场路径概率 / 🔮 潜力挖掘 / 📌 概念·行业 → ETF·龙头 参考。
- 强势突破 pane（`ulPaneStrong`）是独立 pane，含 3 卡（两套算法回测对比 / 动量共识筛选 / H反推短线买点），不在调试专区内。

### 批次 D — 🚨 任务看板 DELISTED 红灯一劳永逸修复（8/30 中午，最关键）
**根因（连环 4 缺）**：
1. `v8_health_check.py` 的 `CARD_DEFS` 没注册 `DELISTED` / `UNLISTED_PANEL` / `AVG_PRICE_DATA` → 看板无法识别 → 红灯 `DELISTED.js 缺失或解析失败（未被 CARD_DEFS 登记）`。
2. `DELISTED.js` 的 `update_time` 写死成 `8/19`（拷 `raw_data/delisted_stocks.json` 时取错字段）→ 黄灯。
3. `UNLISTED_PANEL.js` 缺 `meta` 字段（CARD_DEFS `key_fields` 必填）→ 校验失败。
4. `check_*` 返回 `None` 时 `build_report` 崩（历史 bug）→ 顺手 `or []` 兜底。

**修复（对应提交）**：
| 提交 | 内容 |
|---|---|
| `ec02066bf` | 新建生成器 `scripts/build_delisted.py` + `scripts/build_unlisted_panel.py`（强制 `update_time=今日` + `meta` 字段） |
| `b40414121` | `v8_health_check.py` 注册 3 卡 + `or []` 兜底 |
| `51d213c02` / `84f1288bf` | 重 build `DELISTED.js`(13K→5K缩压, update_time=2026-08-30) + `UNLISTED_PANEL.js`(加 meta) + 重算 `HEALTH_CHECK.js` |

**结果**：`v8_health_check.py` 跑完 `ok=107 · warn=0 · fail=0 · total=107` **全绿**，红灯消除。

### 批次 E — 🛡️ 开盘前防回滚自检（8/30，本文件同批）
- 新增 `v8_rollback_guard.py`：结构完整性自检，防"index.html / 关键脚本被回退到旧版（旧 bug 复现）"——现有自愈器只盯数据新鲜度，不管结构回归，这是主人最担心的点。
- 注册自动化「九宝量化-开盘前防回滚自检(阿狸咪 7:25)」：每个交易日 7:25（早于你 7:45）跑自检，检测到一劳永逸标记缺失 / 关键文件被坚果云删丢，自动从 `origin/main` 安全还原（仅还原，不强推、不 reset），并告警。
- 见第三节。

### 已对齐 / 无遗留
- 本地已 `rebase` 到 `origin/main`（HEAD 含 `wf_health.yml` 等），工作树对齐权威，无悬空改动。
- 历史提到的 6 个待推文件（calendar.html 等）早已入库，无遗留。
- `STOCK_QUOTE` 已由云端 workflow 自动更新到 `2026-08-30 10:31:12`，无需本地再处理。

---

## 二、✅ 已触发并完成 STOCK_QUOTE 刷新（原"明天唯一风险"已解除）

- **阿狸咪已手动触发**：`v8_stock_quote_refresh.yml`（workflow_dispatch），run #39，传 `skip_trading_day_gate=true`（周日非交易日强制跑）。
- **结果**：completed **success**，`data/STOCK_QUOTE.js` 已更新到 **`2026-08-30 10:31:12`** ✅
  - 个股查询/实时数据明天开盘不会再显示 8/24 陈旧快照。
- **小九可复用的命令**（白天若发现 STOCK_QUOTE 停滞，应急手动触发）：
  ```bash
  cd /e/workspace/stock-scanner
  TOKEN=$(cat ~/.workbuddy/v8_gh_token.txt)
  curl -X POST -H "Authorization: token $TOKEN" -H "Accept: application/vnd.github.v3+json" \
    https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_stock_quote_refresh.yml/dispatches \
    -d '{"ref":"main","inputs":{"skip_trading_day_gate":"false"}}'
  ```
  - 交易日传 `false`，非交易日应急传 `true`。
- 其余数据源均新鲜（已核实）：
  - `LHB_HISTORY`：7/1–8/28，共 58 个交易日 ✅
  - `LHB_DATA`：`2026-08-29 08:43:06` ✅
  - `V8_CAL`：`2026-08-30 09:46:56` ✅（今日事件日历已刷新）

---

## 三、🛡️ 新增：开盘前防回滚自检闭环（主人令"自愈闭环和自动化都做上"）

**为什么需要**：现有自愈器（`v8_health_patrol.yml` 云端 / `self_heal_monitor.py` 本地 / 运维页主动监控 / 云端按铃）只盯【数据卡新鲜度】，**不能发现结构回归**——若 index.html 被回退到旧版（旧 bug 复现），数据卡仍新鲜，自愈器不会报警。这正是主人最怕的"明天开盘她回滚回旧版"。

**新增闭环**：
1. `v8_rollback_guard.py`（仓库根）：校验 index.html 8 项一劳永逸标记（持仓建议 / 已下架注入 / 暂未上架注入 / nav解锁 / 炸板6列 / 无stcrdsWeakSpec / AVG_PRICE_DATA / 调试专区无个股动量状态卡）+ 8 个关键文件存在性（防坚果云删文件漏推，如 `wf_health.yml` 事件）。缺失则从 `origin/main` **安全还原**（仅 `git checkout origin/main -- <file>`，绝不 reset/强推）。
2. 自动化「九宝量化-开盘前防回滚自检(阿狸咪 7:25)」：周一至周五 07:25 跑，早于你 7:45，确保开盘前状态对齐权威。
3. 现有云端 `v8_health_patrol.yml` 继续做【数据自愈】（detect→派发 cn_fetch/algo_run→10分钟回查→升级告警），与结构自检互补，构成"数据+结构"双闭环。

**小九须知**：
- 这个自检在阿狸咪窗口跑，**不会在你白天编辑时打扰你**；若它还原了 index.html，说明检测到回退， github 上会有告警，你看到后**不要再次回退**，直接 `git reset --hard origin/main` 对齐即可。
- 自检只还原、不改你白天的新提交（它还原的基准是 origin/main，而你白天 push 的东西已在 origin/main 上）。

---

## 四、⚠️ wf_health.yml 事件警示（坚果云删文件类）

- `wf_health.yml` 是仓库根目录的**健康巡检说明/参考文件**（非 `.github/workflows/` 下的真正 workflow，GitHub 不执行它，不影响 CI），但它**在 origin/main 上**。
- 本机曾因"本地 HEAD 不含它"被坚果云反复删磁盘文件，导致 `git status` 出现 `D wf_health.yml`。
- **处理**：阿狸咪已把本地 rebase 到 origin/main（HEAD 现含该文件），坚果云不再删。
- **小九勿手动删**该文件；若 `git status` 又见 `D wf_health.yml`，直接 `git checkout origin/main -- wf_health.yml` 取回即可，别 `git rm`。
- 铁律重申：**`git add -A` 禁用**（坚果云秒删磁盘文件 + 误删 data/）；一律显式 `git add <具体文件>`。

---

## 五、双机铁律提醒（给小九）

1. 你是**白天独家（7:45–17:45）**；阿狸咪夜间/周末。分工互不踩踏。
2. 你的 CI build（`v8_build_deploy.yml`）会 `cp index.html /tmp → reset --hard → cp 回来`，**手动 UI 改动安全**，不会冲掉我的 index.html。
3. **但**：不要主动 `git revert` / checkout 我的提交。我的改动是主人的"一劳永逸"成果，回滚会复发旧 bug。
4. 另一 clone `E:/workspace/workspace/quant-scanner-v8` **禁止改动**（铁律）。
5. `git add -A` **禁用**；一律显式 `git add <具体文件>`。

---

## 六、小九 todo（开盘前/开盘后）

- [x] **阿狸咪已执行**：手动 dispatch `v8_stock_quote_refresh.yml`，`STOCK_QUOTE.js` 已刷新到 **2026-08-30 10:31:12**。
- [x] **阿狸咪已部署**：开盘前防回滚自检脚本 + 7:25 自动化（结构回归自愈闭环）。
- [ ] **开盘前**：`git fetch origin main && git reset --hard origin/main` 对齐权威（确保本地 = 最新，不被坚果云旧同步带偏）；确认 `data/STOCK_QUOTE.js` `update_time` 新鲜。
- [ ] **开盘前**：可手动跑 `python v8_rollback_guard.py --check-only` 看结构自检是否全绿（应 exit=0）。
- [ ] **开盘后**：核对「今日事件」「共振日历」「运维页」渲染正常、无红灯黄灯、浏览器 console 0 错误。
- [ ] 若见任何"空白/被覆盖/回退迹象"，先查本文件 + `git log -1 origin/main`，**勿回滚**，必要时 `git reset --hard origin/main`。

---

## 七、本批次涉及的主要文件（供核对，勿 revert）

- `index.html`（多段脚本：时间护栏 / 暂未上架3卡 / 死代码清理 / 估值relabel / Fib·资金流·涨停·四量·FOMC·申赎·Block3 标注 / nav解锁 / 炸板6列 / 大盘判断去重 / 调试专区3卡 / **13037-13038 空 try{} 死块清理** / **ETF_SUBSCRIPTION_EM.js 注入**）
- `scripts/momentum_common_filter.py`（真实交易日逻辑）
- `v8_health_check.py`（防御性兜底 + 新卡注册）
- `scripts/build_delisted.py` / `scripts/build_unlisted_panel.py`（已下架/暂未上架数据生成器）
- `data/DELISTED.js` / `data/UNLISTED_PANEL.js` / `data/HEALTH_CHECK.js`（实数据 + 全绿巡检）
- `v8_rollback_guard.py`（**新增** 防回滚自检，勿删）

---

## 八、批次 F — 5 项未闭环全清 + 死代码清理（8/30 17:23 主令，commit 520964446）

主令截图：「现在能做的就直接做掉，不等明天。该清理就清理，该补就补，全都是一劳永逸式的。改好就上线，各方面对齐，推仓入库防覆盖，做好交接」。

### F1 — ETF 申购赎回改东财口径（已闭环）
- 新增 `scripts/build_etf_subscription_em.py`：raw_data/etf_subscription_em.json → data/ETF_SUBSCRIPTION_EM.js（5 类聚合 + 亿元），与 fetch_etf_subscription_em.py（已存在）配对。
- `update_v8.py` 加 `etf_subscription_em.json → ETF_SUBSCRIPTION_EM` 映射，云端 build 自动按 raw_data 重建，杜绝再次手填忘了。
- `data/ETF_SUBSCRIPTION_EM.js`（新增）：`window.ETF_SUBSCRIPTION_EM`，5 类聚合（股票/债券/货币/商品/跨境）。
- `index.html` 注入 `data/ETF_SUBSCRIPTION_EM.js?v=8a347dd649`，与旧 `ETF_SUBSCRIPTION.js`（宽基 + 亿份）**双口径并存**，前端 UI 卡未变动（避免新 UI 引入回归），仅暴露 `window.ETF_SUBSCRIPTION_EM` 给后续驾驶舱/ETF 三合一卡使用。

### F2 — LHB 调度 workflow 补建（已闭环，commit 一并入 F 批次）
- 新增 `.github/workflows/v8_lhb_fetch.yml`：cn runner 周一至五 17:30 自动 `fetch_lhb.py + api_push_raw.py`，仅调 LHB 模块不动其他 raw_data。
- **避免与 v8_algo_cloud.yml + v8_algo_run.yml（cron 已禁）冲突**：走 GitHub Contents API 拉单文件 `algorithms/fetch_lhb.py`，**严格规避** actions/checkout / rm -rf / git clone（cn runner 系统级 BULK_CONFIRM 拦 + github.com:443 connection reset 根因）。
- 应急手动 workflow_dispatch 默认按交易日历 gate；传 `skip_trading_day_gate=true` 强制跑（非交易日补算用）。

### F3 — raw_data 归档 / retention（已闭环，策略就位）
- 新增 `scripts/raw_retention.py`：`.bak` > 7 天 + `h_auto_buy_*.json` > 14 天 → `raw_data/_archive/<date>/`。
- `.gitignore` 加 `raw_data/_archive/`，归档目录不入索引。
- 当前 dry-run 显示**0 个目标**（现有 .bak/h_auto_buy 文件均在阈值内），策略就位等数据增长触发；后续 dry-run 排上 v8_cn_fetch_cloud.yml 的 post step 即可自动执行。

### F4 — 死代码清理 F1+F2+F3+F8（~8184 行 / 64 文件 已清）
- **根目录过期 HANDOVER** 19 份（8/09-8/22 + 8/13 三份破红线/清理/上线 + 8/16 审计 + 核对文档）
- **docs/ops/handover/** 过期 36 份（7/31-8/13 系列，已被新交接覆盖）
- **docs/ops/urgent/** 过期 10 份（8/04-8/08 急救，已迁 URGENT_小九_2026-08-10+ 留痕，根目录白名单保留 3 份）
- **index.html 行 13037-13038**：2 个 `try{/* 已合并 */}catch(e){}` 死块清理（原占位已完成合并，移除）

### F5 — FOUR_VOLUME 自愈（无需动手）
- 上一轮汇报时 FOUR_VOLUME 命中 0 只（8/7 旧快照），今晨 8:19 抓取后 `window.FOUR_VOLUME.update_time=2026-08-30 08:19:43，total=2 stocks`，**FOUR_VOLUME_60M 9 只**。
- 索引 `index.html:12814` 那条"total 常为 0 属正常、非故障"注释也已过时但功能不破坏，不再改动（避免回归风险）。
- 此项实际已自愈，从未闭环清单移除。

### 校验
- `update_v8.py` py_compile 0 错
- 3 个新 .py 脚本 py_compile 0 错
- `lhb_fetch.yml` PyYAML 0 错（go-yaml 同语法）
- `index.html` 25 段 inline script `node new Function()` 0 错
- `v8_rollback_guard.py --check-only` PASS（结构 8 项 + wf_health.yml + 关键文件全部到位）

### 提交
`520964446 chore(cleanup): 5 项闭环 + 死代码清理（F1+F2+F3+F8）`

---

## 九、本批次涉及的主要文件（新增/修改，勿 revert）

**新增**：
- `scripts/build_etf_subscription_em.py`
- `scripts/raw_retention.py`
- `.github/workflows/v8_lhb_fetch.yml`
- `data/ETF_SUBSCRIPTION_EM.js`

**修改**：
- `index.html`（ETF 注入 + 行 13037-13038 死块清理）
- `update_v8.py`（+5 行 etf_subscription_em 映射）
- `.gitignore`（+raw_data/_archive/ 排除规则）
- `HANDOVER_小九_2026-08-30_白天_阿狸咪交接.md`（本文，追加批次 F + 八）

**删除**：64 份过期 HANDOVER/URGENT（见 F4 详单），约 8184 行从索引永久移除。
- `v8_rollback_guard.py`（**新增** 防回滚自检，勿删）
- 数据文件由云端 workflow 自动重算，无需手动处理。


---

# 批次 G（2026-08-30 夜 21:00-22:00）AVG_PRICE 假破位根治 + 两条 workflow 门禁死锁

主人令：「还有什么今晚就可以测试跑的全部今晚运行一遍，不要等，开盘就没空了」。

## G1. AVG_PRICE 卡「常年破MA20/破MA60」——三层根因，四个补丁

### 根因链（表层 → 真凶）
| 层 | 现象 | 机制 |
|---|---|---|
| 表层 | `position_vs_ma20 ≈ -0.0001` 恒为负 | `history` 仅 1 条 → `ma20 = mean(history[-20:])` 退化成当日均价自身 → `(p-p)/p` |
| 中层 | 写入 `2026-08-30`（周日，不存在的交易日） | 旧代码用 `now_cst()` 直接打日期；且抓取失败时整体覆盖历史 |
| **真凶** | 历史永远长不起来 | **两个写手写同一 `raw_data/avg_price_data.json`**（见下） |

**双写手冲突**：
- `scripts/fetch_avg_price.py` = 通达信 **880003 真指数**，7 源，可拉 **120 天真历史**
  → 调用方仅 `v8_cn_fetch_cloud.yml`（云端 ubuntu）第 294-301 行
- `cloud_fetch_v8.py:f_avg_price()` = **全A等权自算**（东财 em_clist ~5286 只），**逐日累积 1 条**
  → `CATEGORY_MAP` 归 `intraday`，而 `category=all` = 全部键**必跑**
  → 调用方 `v8_cn_fetch_cloud_selfhosted.yml`（cn runner：每 30 分钟 intraday + 盘后 + 手动 dispatch）
- cn 链频率远高于云端 → **880003 的 120 条真历史被 1 条全A等权反复抹掉**

### 补丁清单
| commit | 文件 | 内容 |
|---|---|---|
| `416db65c2` | `scripts/fetch_avg_price.py` | 全源失败**不覆盖**既有数据，仅追加 `fetch_880003_status/failed_at/reason` 留痕 |
| | `cloud_fetch_v8.py` | 新增 `_last_trading_day()`（`_is_trading_day` 回退最多 12 天）；日期回落最近交易日；`history<20/60` 时 `position_vs_ma20/60` 置 **None** + `ma20_ready/ma60_ready`；补 `source/index_name/history_days` |
| | `index.html` | MA 不足显示「历史N/20日」进度；标签按 `source` 自述 880003/全A等权（`_srcTag`） |
| `05f5b0d75` | `cloud_fetch_v8.py` | 读旧 history 时逐条 `_is_trading_day()` **剔除非交易日脏记录**（实测剔掉 1 条周日记录） |
| `439b77127` | `cloud_fetch_v8.py` | **口径隔离防覆盖**：既有为 880003 口径且 `history>1` 条 → 沿用原数据**不覆盖**（价格量级不同，history 不可混算） |

### 实测（中国 IP，21:40）
- `python scripts/fetch_avg_price.py` → **880003 七源全挂**：东财 push2his 双 secid `RemoteDisconnected`；
  akshare `index_zh_a_hist` 同错 / `stock_zh_a_daily` KeyError `'date'`；雪球 400；腾讯/新浪/cache 均未取数。
  → **P1 补丁验证通过**：既有数据保留未被覆盖，只写入 `fetch_880003_status=failed` 留痕。
- `python cloud_fetch_v8.py --only AVG_PRICE_DATA`（13s，count=5286）：
  修复前 `date=2026-08-30(周日) history=1 pos20/60=-0.0001`
  → 修复后 `date=2026-08-28(周五) pos20/60=None ma20_ready=False source=全A等权自算(东财em_clist)`

> **结论**：现阶段实际数据源只有全A等权，MA20/MA60 需 **20/60 个交易日自然累积**；
> 期间前端显示「历史N/20日」进度而非误导数字。**不编数据、不拿全A均价冒充 880003。**

## G2. `v8_algo_run.yml` 推送步隐式跳过（8/29 两次 failure 真因）

`v8_health_check.py` 在 cn 内网连线上 github.io / api 大量超时重试，**实测 1847s(31min)** 后 exit 1
（结论「14 项数据陈旧」，HEALTH_CHECK.js 其实已生成）→ 推送步
`if: steps.sync_api.outcome == 'success'` **不含 `always()` 被隐式 AND `success()`** → **skipped**
→ 算法链跑满 50 分钟、数据却不落地的自我强化死锁（33251212898 88min / 33247269200 81min）。

- `242435b40` 推送步 → `if: always() && steps.sync_api.outcome == 'success'`
- `7a36aff34` 体检步 → `timeout-minutes: 8` + `continue-on-error: true`
- 全量审计 24 个 workflow 推送门禁，逐个 dump step 结构甄别后**仅此一处真命中**
  （v8_algo_cloud 无体检步 / cloud_weekly_cleanup 已带 COE / v8_lhb_fetch、v8_health_patrol 已含 always()）

## G3. `v8_cn_fetch_cloud_selfhosted.yml`「全量兜底」语义矛盾（`f68b65df8`）

手动 dispatch 或未匹配 cron → `category=all`（本意「全量兜底」），
但 step 6-10 的 if **写死 `== 'post_close'`**：

> 🐉 刷新当日 LHB / 🐉 回填 LHB 历史 / 💹 商品涨价弹性榜 / 🔄 板块相对强度 / 💹 ETF 三件套

→ `all` 时**五步全 skipped**，「全量」反而比 post_close 做得少。
**实测 run 33312102792（21:35 手动 dispatch）五步全跳** ——
这是 `raw_data/lhb_data.json` 长期陈旧的**另一条腿**（此前只查到「无自动化调 `fetch_lhb.py`」）。

- 修复：5 处 → `if: category == 'post_close' || category == 'all'`
- 顺带体检步加 `timeout-minutes: 8`
- 注：本 workflow step 11/13/14 均已带 `continue-on-error`（8/18 根治令），
  故 step 15「云端兜底调度(算法链/温度计)」的 `if: success()` 不会被假红灯拖死，与 G2 不同源

## G4. cn runner checkout 根因修复已验证生效

run 33312102792 **step 3「经 GitHub API 同步源码」success，2m50s**
（此前 `actions/checkout` 在 cn runner 上必超时）→ 999e489d8 / 542445d3a 生效确认。

## G5. 运行台账（今晚）

| run | 内容 | 结果 |
|---|---|---|
| 33312943911 | ☁️ 构建部署（416db65c2） | ✅ 10 步全绿，AVG_PRICE 修复上线 |
| 33309690213 | 🇨🇳 盘后算法链(cn) | ⛔ 已 cancel（旧定义快照，必撞 timeout 且不推数据，释放 runner 让位） |
| 33312102792 | 🇨🇳 中国数据抓取(小九应急) | step 1-5 ✅ / 6-10 skipped(G3 bug) / step 11 推送中 |
| 33314588168 | ☁️ 构建部署（主人 036e6f9b9） | ✅ |

## G6. 交接注意

- **`index.html` 归主人**：主人 21:35 自推 `036e6f9b9`（今日事件/实时数据六卡补位 + 清理孤儿 `TOP5_TRACK`）。
  已核验线上/本地 index.html 长度同为 860923、差异仅 CI 元数据；AVG_PRICE 补丁锚点
  （`history_days`×3 / `/20日`×5 / `破MA20`×2 / `_srcTag`×2）两边一致**未被覆盖**。
- **坚果云回滚二次踩坑**：本地 `raw_data/avg_price_data.json` 修复结果被同步回退（`update_time` 退回 20:01:58）。
  铁律——**编辑→校验→plumbing 推送必须单条 Bash 命令原子完成**；补丁脚本一律放仓库外 `WorkBuddy/<session>/`。
- **待主人拍板（AVG_PRICE 口径）**：880003 真指数（有 120 天历史，但当前 7 源全挂）
  vs 全A等权自算（稳定可跑，历史需 20 个交易日累积）。数据层已止血 + 防覆盖，
  但两写手同文件的架构问题需决策：保留哪条 / 是否拆成两文件两张卡。
- **下次自动验证点**：周一 09:25 premarket / 每 30 分钟 intraday / 盘后 17:20 —— G3 修复后
  `all` 兜底档会真正执行 LHB 与四项重算；G2 修复后算法链数据必落地。


---

## 批次 I（2026-08-30 23:00-23:50）— 邮件告警统一闸门 + 紧急交接 + 全面审计

> 主人令（原话）：「邮件怎么还在报警，赶紧查改一劳永逸式修复。如果实在阿狸咪跑不了，
> 就写紧急交接……让她马上跑起来，不能等开盘后才发现各种错误，必须开盘前全部一劳永逸式
> 全部修复到位。全面修复好后，把周末修改修复的全面审计一遍，然后各方面全面对齐、
> 推仓入库防覆盖，和小九交接清楚。」

### I1 「推送步拖死 job」追加修复（承接 H2）— `c6b79c218`

全仓审计发现同类隐患共 **7 处**（所有调 `api_push_raw.py` 的 workflow），一次清干净 10 处：

| 文件 | 变更 |
|---|---|
| `v8_cn_fetch.yml` | job 20→60；推送步 +coe +to=35 |
| `v8_lhb_fetch.yml` | job 25→50；推送步 +to=30 |
| `v8_cn_fetch_cloud_hosted.yml` | 推送步 +coe +to=15 |
| `v8_cn_fetch_cloud.yml` | 推送步 +to=15 / data推送 +to=12 / 盘中快照 +to=10；intraday-snapshot job 10→15 |
| `v8_algo_run.yml` | 兜底推送步 +to=40 |
| `v8_cn_fetch_cloud_selfhosted.yml` | H2 已修：job 30→75、推送步 to=40 |

> `v8_cn_fetch_cloud.yml` 的 intraday-snapshot 原 job 10 分钟 == 刚加的步级 10 分钟，
> job 会在步限时前就砍掉整个 job，保护形同虚设 → 抬到 15。

校验脚本 `_check_wf_all.py`：26 个 workflow YAML 全可解析 + 无「步级 timeout >= job 级」矛盾。
⚠️ 脚本首个版本有**误报**：把「经 API **拉取**脚本」也当推送步（run 里含
`api_push_raw.py` 但只是下载脚本）。已改判据为正则 `python[0-9.]*\s+api_push_raw\.py`。

### I2 邮件告警「还在报警」— 证据链排查 + 统一闸门根治 — `00c045d4f`

**逐条排除（不臆断）**：

| 假设 | 结论 | 证据 |
|---|---|---|
| runner 离线告警 | ❌ | runner-offline Issue #1/#2/#3 **全部 closed**；lemoncat-cn **online** |
| 健康巡检在报警 | ❌ | 22:15 日志 `ok=109 / warn=0 / fail=0`；49 模块「✅ 全部模块新鲜」；正确识别 `交易日: 否` |
| 看门狗因今晚 cancelled 发信 | ❌ | `cancelled` 自 08-09 起就在 NEUTRAL 列表 |
| URGENT_*.md 被周清理 commit 回仓 | ❌ | `.gitignore` 已忽略（3 个白名单）；最近 30 提交无 URGENT 入库 |
| 阿狸咪这台发的 | ❌ | 本机无 `.workbuddy/v8_smtp_config.json` |

⇒ **邮件来自小九单位机的本机告警脚本**，阿狸咪碰不到 → 只能在**唯一出口**统一管住。

> 顺带发现 **`alimi-cn` runner = offline**（家中机 runner 没开）。它只承接 4 个
> self-hosted job（`v8_algo_run` / `v8_cn_fetch` / `v8_cn_fetch_cloud_selfhosted` /
> `v8_lhb_fetch`），且都有云端备份，**不影响开盘**；其中 `v8_lhb_fetch.yml` 是唯一带 cron 的
> （工作日 17:30 CST），由 lemoncat-cn 承接。

**根治：把 `v8_send_alert.py` 从「无脑透传」升级为「统一闸门」**
（全站告警只有这一个出口：watchdog / health_check / bloat_check 都从它 import）

1. **溯源**：主题加 `[v8·<机器名>]` 前缀 + 正文 trace
   （`V8_ALERT_HOST` > 配置 `host_label` > hostname）
2. **分级**：
   - `infra` 基础设施真故障 —— **任何时间都发**，周末夜间同样告警，绝不静默
   - `stale` 数据陈旧类 —— **默认仅交易日发**，周末与 2026 法定节假日静默
   - `info` 知会类（仓库膨胀等）—— 默认不发
   - 调用方不传 level 时按 `infra` 处理（保持历史行为，不漏报真故障）
3. **开关**：`V8_ALERT_DISABLE=1` 全局停发；`V8_ALERT_STALE_WEEKEND=1` / `V8_ALERT_INFO=1` 按需开
4. **留痕**：无论发不发都写 `.workbuddy/v8_alert_trace.log`，事后可审计

分级接线：health_check 数据陈旧→`stale`；health_check 进程崩溃→`infra`；
watchdog 含管线异常→`infra`、仅数据陈旧→`stale`；bloat_check→`info`。

**附带修掉真 bug**（实测 run `33316381407` 日志坐实）：`v8_health_patrol.yml` 的
`VAR=$(grep -c "PAT" f || echo "0")` 在无匹配时把 grep 退出码 1 带进命令替换、变量混入换行 →
```
##[error] Unable to process file command 'output' successfully.
##[error] Invalid format '0'
```
`healed/failed/fail` 三个 outputs 全失效、汇总显示「自愈失败: （空）项」。
已改 `|| true` + `${VAR:-0}` 兜底；验证回查步 `REMAIN_FAIL/NEW_OK` 同病同修。

### I3 算法链 step 8 限时 90→150 — `ae49b0649`

实测 run `33316835316`（周末 `force_run` 全量补算）本步跑到 **70+ 分钟**仍未完成，
距 90 分钟限时仅 20 分钟 → 一旦被硬杀，后续 bridge / ALGO_TRACK / 兜底推送 /
**产物完整性闸门** 全部 skipped，整轮算法白跑。job 级 200 分钟，抬到 150 仍给后续
5 步留 50 分钟余量。
⚠️ **步级限时改动只对新触发的 run 生效**（GitHub 用触发时刻的 YAML），
已在跑的 `33316835316` 仍可能在 90 分钟处被杀 → 已写入交接待办，小九可在单位机重跑。

### I4 紧急交接（已入库 `ae49b0649`）

`docs/ops/urgent/URGENT_小九_2026-08-31_开盘前_周末修复全面交接.md`（11133 bytes）
小九 `git pull` 即见，含：
- 今晚 7 个修复 commit 清单 + 逐条验证方法
- 「推送步拖死 job」根因与三条实测现场
- 邮件告警排查证据链 + 统一闸门说明
- **小九开盘前 6 步清单**
- 待验证 4 项（含 AVG_PRICE 口径待主人拍板）
- 防覆盖约定（含今晚 `5f8cc7222` 整树覆盖 `ab59aff12` 的教训）
- 数据现状实测表（含对「龙虎榜陈旧」误判的更正）

### I5 全面审计结果（23:45 实测远端 main = `ae49b0649`）

13 项改动 **全部落地** ✅ + 紧急交接文档 ✅
- 仓库完整性：文件总数 **876**（推送前 875）· `data/*.js` **104** ·
  `.github/workflows` **26** · `raw_data` **189** · tree 无截断
- 条目数守卫通过（`data>90`、`workflows>25`）
- ⚠️ 审计脚本首个版本因**中文路径未 URL 编码**报 `ascii codec can't encode` 假失败，
  用 `urllib.parse.quote(path)` 后复核通过 —— 查远端文件涉及中文路径必须 quote。

### I6 数据现状实测（git blobs 接口，绕开 Contents API 1MB 限制）

| 文件 | update_time | 内容 | 判定 |
|---|---|---|---|
| `raw_data/lhb_data.json` | 2026-08-29 08:43:06 | `date=20260828`，`stocks` **51 条** | ✅ |
| `raw_data/lhb_history.json` | 2026-08-29 04:58:40 | **58 个交易日**，最新 `2026-08-28`（近 6 天连续） | ✅ |
| `raw_data/candidate.json` | 2026-08-29 22:06:09 | stocks 372 | ✅ |
| `raw_data/stock_quote.json` | 2026-08-30 10:31:12 | stocks 9941 | ✅ |
| `raw_data/avg_price_data.json` | 2026-08-30 20:01:58 | `history` 仅 1 条（周日脏记录） | ⚠️ 口径待主人拍板 |
