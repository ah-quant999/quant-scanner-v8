# 九宝量化 v8 交接文档（小九 → 阿狸咪）

- **生成时间**：2026-08-03 09:12（小九手工初版；12:00 / 18:00 由自动化各更新一次）
- **状态**：阿狸咪家中机长期决策已关机、不接手任何部署/救火。本文档供其开机 `git pull origin main` 后了解当日状态。

---

## 一、背景

- 阿狸咪 8/1~8/2 在 `quant-scanner-v8` 完成 v8 前端大整改 + 数据原生化（数十 commit），并定下「家中机保持关机、不接手」长期决策（commit `d8ea7e0`）。
- 小九今日（8/3）聚焦：修复云端抓取断链、建自动部署闭环、清退 v6、澄清逻辑详解页、持续监督。

---

## 二、小九今日已完成改动（按 commit，均已 push origin/main）

1. **修复 cn_fetch 推送失败**（真根因：`cloud_fetch_v8.py` 缺 `all` 参数 + Windows GBK emoji 崩溃）
   - `5b26d06` fix(ci): api_push_raw 改 force=False + 增强错误日志
   - `3f35173` fix(ci): cloud_fetch_v8 支持 all 兜底 + 两脚本加 UTF-8 防 GBK emoji 崩溃
   - 验证：cn_fetch run `30774819457` success；raw_data commit `43fad0e`（08:39）；build `58097a9`（08:41）

2. **自动部署闭环（A）**：`06fb5f3` fix(ci): build_deploy 监听 cn_fetch `workflow_run` 自动部署，绕过 GITHUB_TOKEN push 不触发下游 workflow 的限制

3. **本机看门狗（B）**：`a390419` ops: 新增 `v8_cloud_watchdog.py`（本机只监督不部署，检查 runner/cn_fetch/build_deploy/raw_data/site）+ 自动化 `1785718343599`（HOURLY 08–22）

4. **运维面板 + 逻辑详解页对齐**：`9574ae7` 部署 SHA/触发方式/双机状态/自动化任务表

5. **v6 自动化清理**
   - DELETE 9 个：1784767168275（个股元数据）、1784711432581（危机雷达兜底）、1784524253654 / 1784524243949（candidate_pool 看门狗）、1784086687244（neodata 17:31）、1784084629462（neodata token）、1783903850358（夜间周末值守）、1783303308242（打新 09:25）、1782691770053（08:00 交接）
   - `1779668598510`（C 盘空间预警邮件）已不存在，无需删

6. **逻辑详解「中国节点」措辞澄清**：`14dc3d1` 5 处改为「self-hosted runner · GitHub 自动调度 · 非手动」，消除「小九手动主力」误导

---

## 三、当前系统状态（09:12 核实）

- 站点：`https://ah-quant999.github.io/quant-scanner-v8/` → HTTP 200
- main SHA：`14dc3d1`（最新）
- 中国节点 runner（lemoncat-cn）：在线 · GitHub 自动调度
- 剩余自动化（5 个，全 v8）：看门狗 `1785718343599`、09:00 / 15:30 / 17:00 / 周末 15:30 v8 部署
- 本地 `E:\workspace\quant-scanner-v8`：已同步 origin/main，status 干净

---

## 四、需阿狸咪注意 / 待办 / 风险

- 她周末的改动已被小九核对并本地同步，无需重复。
- **长期决策**：家中机保持关机、不接手任何部署/救火（`d8ea7e0`）。如需开机了解状态，`git pull origin main` 即可看到本交接文档与最新代码。
- **已知待办（非紧急，源自她周末 HANDOVER）**：ETF 商品分类仍 v6 兜底、AI 速览演示数据、北向港交所 2024-05 停更（设计内）、`lhb_data.json` 仅 7/31 单日（待 18:30 中国节点原生化）。
- **风险验证点**：10:30 cn_fetch 跑完应自动触发 build_deploy（首次验证 workflow_run 闭环），看门狗会记录。

---

## 五、小九监督职责（不变）

- 看门狗每小时查 runner / cn_fetch / build_deploy / raw_data / site；异常告警，不自动覆盖数据。
- 不手动跑数据、不手动部署。
