# HANDOVER 阿狸咪 → 小九 · 2026-09-10 08:16 · 数据失真闸门分级一劳永逸（主人令「按 A」）

> 含:<a> 主人昨夜拍板 3 件的落地;<b> 改动 + 三件套 + A/B 实证;<c> 挖出的新线索(待你/主人定夺)。

---

## 一、主人拍板的 3 件事 — 落地状态

| # | 事项 | 主人决定 | 我的动作 | 状态 |
|---|---|---|---|---|
| 1 | 是否 dispatch 试跑 `v8_algo_cloud.yml` | **按建议:先不派** | 未派任何 dispatch(避免盲派刷无效 run) | ✅ 已遵办 |
| 2 | 闸门分级规则 | **按建议 A:两项降 warn + 7天加豁免** | 改 `algorithms/verify_data_sanity.py`,已上线 `569887862` | ✅ 已上线 |
| 3 | maharo 是否下架公开站 | **不动(「本身也只能我看」)** | 公开站/相关文件一律未碰 | ✅ 遵办,**合规风险主人自留** |

---

## 二、改动明细(`verify_data_sanity.py`)

### 🔴 重要澄清(防止后续误改)
主人原话指向「`verify_daily_audit.py` 阈值规则」,但**实证后那是另一个脚本** —— 它只做
「当天 HANDOVER 是否存在 + 新 commit 是否被交接文档提及」的 **commit 防覆盖审计**,
**不含任何阈值/越界逻辑**。真正管数据失真分级的是 **`verify_data_sanity.py`**
(`v8_algo_cloud.yml:735` 调用 → `SANITY_EXIT`)。本次改的是后者,`verify_daily_audit.py` **未动**。

### 分级调整

| 检查项 | 改前 | 改后 | 依据 |
|---|---|---|---|
| 当日涨跌幅超 ±44% | HARD | **SOFT** | 数值可疑 ≠ 结构损坏 |
| PE 越界(-50~20000) | HARD | **SOFT** | 同上 |
| update_time 陈旧 >7 天 | HARD | **HARD(保留)+ 全站共享停更豁免** | 灾难兜底不弃守 |
| JSON 解析失败 | HARD | HARD | 结构性,保留 |
| 整表无记录 | HARD | HARD | 结构性,保留 |
| 陈旧 2~7 天 / price=0 / 预期收益偏高 | SOFT | SOFT | 原样 |

### 🛡 「全站共享停更」豁免(本次核心设计)
解决主人的痛点:节假日/双机离线导致的**陈旧误伤**。

```
site_latest = 全站所有 VAR 里最新的 update_time      # 第一遍扫描算出
lag        = site_latest − 本 VAR 的 update_time     # 本 VAR 落后全站几天

自身陈旧 >7 天 且 lag >  3 天 → HARD(它自己独自落后,真断更)
自身陈旧 >7 天 且 lag <= 3 天 → SOFT(全站一起停,假期/离线窗口,豁免)
```

**优点**:不引入交易日历/**无网络依赖**,脚本保持纯标准库只读,CI 离线可跑;
且长假里某个 VAR 单独坏掉**依然抓得住**(lag 仍超标)。阈值常量 `SHARED_STALE_GRACE_DAYS = 3.0`。

---

## 三、三件套 + A/B 实证

| 件套 | 方法 | 结果 |
|---|---|---|
| ① 语法 | `python -m py_compile` | ✅ 0 错 |
| ② 运行时 | `python verify_data_sanity.py --no-baseline` | ✅ **EXIT=0 / 0 硬失败**,全站基准正确算出 `2026-09-10 07:21` |
| ③ 引用 | 全仓 grep | ✅ **唯一调用点 `v8_algo_cloud.yml:735`**,CLI 接口未变(`--no-baseline`),完全兼容 |

### A/B 实证(证明改动真的生效,而非当天恰好无数据)

| 版本 | 硬失败 | 软告警 | 退出码 |
|---|---|---|---|
| 改动前(`git show HEAD:`) | **7** | 4742 | **1**(判红) |
| 改动后 | **0** | **4749** | **0** |

> 软告警 4742 → 4749 **精确 +7**,与 7 个 HARD 转移数量完全吻合 = 硬证据。
> (首次跑时 `EXIT=$?` 取的是 `tail` 的退出码不准,已重跑用重定向拿真实退出码。)

---

## 四、🔍 挖出的新线索(**未改,超出本次授权,等你/主人定夺**)

那 7 个硬失败并非随机噪声,**指向 3 处待查**:

### ① `ETF_INTRADAY_HEAT` — ⚠️ 疑似系统性数据质量问题(最值得先查)
交叉验证 166 条记录:

| 指标 | 实测 | 正常应 | 判定 |
|---|---|---|---|
| pct 中位数 | **7.37%** | ~0(±1-2%) | ❌ **全数据集系统性偏大** |
| pct 最大值 | 62.47% | <10% | ❌ |
| 4 条 \|pct\|>44% | `price=None, amount=None` | 应有值 | ❌ 脏记录(字段缺失) |

这不是「字段名歧义」,**加 `FIELD_CONTEXT_OK` 豁免是掩盖而非修复**。
数据源为东财 `push2 clist`(`calc_etf_intraday.py:24/53-54`,`f2/100`、`f3/100` 换算看着规范),
建议方向:**数据源清洗 + 单位口径核对**,而非降告警。

### ② `IPO_DATA.change_pct=57.02` — ✅ 已实证属合理,可豁免
记录名 **`N百迈科`** —— “N” 前缀 = **新股上市首日**,`prev_close`=发行价,
首日**不受 ±44% 限制**,57% 合法。生成器 `fetch_ipo_data_v8.py:240` 已核。

### ③ `LHB_DATA.pct` 101.45 / 57.3 — 待实证
创业板/主板当日涨跌幅上限 20%,101% 必是区间涨幅。
注意:**`LHB_HISTORY.pct` 已于 09-08 加豁免,但 `LHB_DATA` 当时漏网**。
建议核对语义后补 `FIELD_CONTEXT_OK`,与前者保持一致。

---

## 五、给你的建议优先级

1. **先查 ETF 数据源**(①),这是真数据质量,不是告警策略能兜的。
2. ②③ 若要补豁免,请确保**先实证字段语义**再动手(不许凭"看着像"就豁免)。
3. 本次改动已上线,今晚 21:00 E 批的闸门会首次按新口径跑 —— **届时留意是否还有红灯**。

---

*阿狸咪 · 2026-09-10 08:16 CST · commit `569887862`*
