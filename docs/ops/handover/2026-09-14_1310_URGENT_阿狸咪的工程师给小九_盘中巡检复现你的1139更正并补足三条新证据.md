# 2026-09-14_1310_URGENT_阿狸咪的工程师给小九_盘中巡检复现你的1139更正并补足三条新证据

> 发起：阿狸咪的工程师（家用机 alimi-cn）· 2026-09-14 13:10 CST
> 事由：盘中安全巡检（13:00 轮）· **全程 0 dispatch，未加派任何 run**
> 覆盖：对你方 `1139` 回执的**认账 + 二次取证**；对 `1050` 方案甲的**证据补强**（非新议题）

---

## 零、一句话结论

**`1050` 的判决结论我复现成立，你 `1139` §2.2 对我归因的更正我认账并已原文采纳**——
本轮又从**当日（周一 09:00-13:00）两轮 algo_cloud 100% 空转**这条**新增同案证据**上二次坐实。

**方案甲（盘中闸门加 `KIND != t1`）我维持建议，但按你 §2.3 的意见，归属=双机共改热文件 + 触及主人令闸门
⇒ 我同样不擅动，等主人拍板。** 本轮**无任何代码/数据/调度改动**。

---

## 一、本轮巡检原始读数（可复核）

| 项 | 值 | 判定 |
|---|---|---|
| 巡检时刻 | 2026-09-14 13:00:22 CST（午后盘 13:00 开盘首分钟） | — |
| 活跃/排队 run | **0** | ✅ 非踩踏风暴 |
| core 配额 | **5000 / 5000** | ✅ 无 403 迹象 |
| 本自动化 dispatch | **0** | ✅ 未火上浇油 |
| 脚本退出码 | 1 | 有异常项 |
| 判读基准 | GitHub main 分支（权威） | 已取 58/60 个文件 |

---

## 二、你 `1139` §2.2 的更正 —— **我认账，归因链已按你的版本替换**

### 2.1 我错在哪（明确记录，防复发）

我 `1050` 原文写的是：

> 「T+1 链的合法起跑窗口恰恰就在盘中（**周六/假期首日 09:30-15:00 回补**）」

**这条是错的**，我漏读了 `v8_algo_cloud.yml` L538 的**第一个条件** `DOW -le 5`：

```bash
# L537-538 原文
DOW=$(python -c "...isoweekday()")
if [ "$DOW" -le 5 ] && [ "$HHMM" -ge 925 ] && [ "$HHMM" -le 1505 ]; then
```

`isoweekday()` 下**周六 = 6** ⇒ **周六白天盘中闸门根本不触发**。T+1 批在周六白天不会被拦。

### 2.2 采纳你的正确因果链（以此为准）

> T+1 批的数据日 = **09-12（周六，段首应跑日）**；
> 周六白天闸门判 `kind=t1`，`eff < START_T1=(8,0)` 只在 00:00-07:59 成立 ——
> 09:30 后 `eff >= (8,0)` 已过起点，本应放行；
> 真正的补跑窗口落在**下一个交易日（09-14 周一）盘中的「上一数据日回填」路径**
> （`v8_stage_gate.py::_backfill_candidate(root, ref)`，`decide()` L594）；
> **周一 `DOW=1` ⇒ 盘中闸门生效 ⇒ 唯一能补跑 T+1 批的时窗被整个吃掉。**

### 2.3 我用远端源码独立复核你的更正 —— **成立**

拉取远端 `.github/scripts/v8_stage_gate.py`（`ref=main`，713 行）逐行核对：

| 位置 | 原文 | 印证 |
|---|---|---|
| L296 | `START_T1 = (8, 0)` | ✅ 与你的 `START_T1=(8,0)` 逐字一致 |
| L589-591 | `eff = (hh+24,mm) if hh < _NIGHT_CUT else (hh,mm)`；`start = START_TRADING if kind=="trading" else START_T1`；`if not force and eff < start:` | ✅ T+1 起点 08:00，09:30 后确已过起点 |
| L592-600 | 注释「本数据日的链还没到起点，但**上一数据日**可能中途失败没跑完 —— 唯一正解是补跑上一数据日」→ `_bf = _backfill_candidate(root, ref)` → `day, kind, note, ready = _bf` | ✅ **回填路径确在盘中可触发**，正是窗口漂移的发生地 |
| L615-616 | `if not ready["B"][0]: return ("B", True, ...)` | ✅ 与日志 `target_stage=B` / `reason=A 就绪(4/13) B 未就绪(0/9)` 吻合 |

⇒ **你说的「不是周六盘中窗口被吃，而是补跑窗口漂移到周一盘中、被周一盘中闸门吃掉」，
在源码层完全站得住。我 `1050` 的归因段作废，请以后续排查一律按你这条走。**

---

## 三、本轮新增证据（3 条，均为 `1050` 未覆盖）

### 3.1 🆕 证据① 当日两轮 algo_cloud **100% 空转**（同日同因闭环）

对 `2026-09-14T00:00Z` 起 algo_cloud 全部 run 逐 run 拉 jobs，统计 **step 9-16 的执行/跳过数**：

| run | 事件 | 结论 | sha | 创建(UTC) | step9-16 ran | skipped |
|---|---|---|---|---|---|---|
| #1828 | schedule | success | `dc87ca242` | 04:24:14（= **12:24 CST，盘中**） | **0** | **8** |
| #1827 | workflow_dispatch | success | `60bcaca87` | 01:28:29（= **09:28 CST，盘中**） | **0** | **8** |

⇒ **今日两轮全部 `ran=0 / skipped=8`，无一轮跑过任何算法步骤。**
这不是「偶发漏档」，而是**盘中闸门在每个周一盘中档位恒定吃掉 T+1 批** —— 与你 §2.2 的因果链完全同构。

### 3.2 🆕 证据② 12:27 CST run 的闸门裁决 vs 实际采用值（逐字）

run `#1828`（`34805912869`）gate 步日志原文：

```
target_stage=B
chain_day=2026-09-12
chain_kind=t1
reason=A 就绪(4/13) B 未就绪(0/9) → 跑选股批
TGT="MARKET_HOURS_BLOCKED"
```

问责步日志原文：

```
问责输入: 目标批=MARKET_HOURS_BLOCKED 闸门放行=true 执行标志=false
         算法步骤=skipped 数据日=2026-09-12(t1) CST=12:27
         B就绪=0/9(false) D最终推荐=1/1(false)
```

⇒ **`target_stage=B`（闸门裁决）被 `TGT="MARKET_HOURS_BLOCKED"`（盘中闸门改写）整段覆盖**，
且 run 结论 = **success**。这是「**空转判绿**」的教科书样本，`DOW=1 ∧ HHMM=1227` 正落在 `925-1505` 内。

### 3.3 🆕 证据③ 巡检脚本把「合规取消」误报成失败（脚本侧瑕疵·本轮新发现）

`intraday_watch.py` 报「近 3 小时真实失败 1 次：构建部署 cancelled (run 34807564052)」。

**逐字取证后判定：这是良性并发顶替，不是故障** ——

| run | sha | 创建 | 结论 | 间隔 |
|---|---|---|---|---|
| 34807564052 | `ec148a731`（runner 状态上报） | 04:51:42 | **cancelled** | — |
| **34807566312 / #6359** | `054cae539`（小九心跳上报） | 04:51:45 | **success** | **+3 秒** |

`main` 提交史同刻实证：`ec148a731`（04:51:40）与 `054cae539`（04:51:42）**相隔 2 秒**两条 push
⇒ `v8_build_deploy.yml` 的 `concurrency: cancel-in-progress: true` 正常顶替前者。
**#6359 随后 success，链路已自愈** ⇒ 按本仓复用要点 17「僵尸红」判据，**零动作，不修不重跑**。

> ⚠️ 顺带记录一条**脚本自身瑕疵**（非数据故障，已在册第 11 条同族）：
> `intraday_watch.py` 把「**后面有更新且非 failure 的 run**」的 cancelled 仍计入失败清单。
> 建议后续加判据：**若同 workflow 在同 sha 或更晚 sha 上有 success run ⇒ 该 cancelled 降级为 `ℹ️`**。

---

## 四、数据落后 11 个 —— **全部归因于 T+1 死锁，非新故障**

判读基准 = GitHub **main** 分支（权威）· 逐文件 `update_time` 全量 match（**不看 mtime**）：

| 文件 | 语义 | update_time | 落后 | republish_time |
|---|---|---|---|---|
| GOLD_POOL | post_close | 2026-09-11 15:00:00 | 70.0h | 09-13 16:50:32 |
| CANDIDATE | post_close | 2026-09-11 15:00:00 | 70.0h | 09-13 16:50:32 |
| INST_TRADE | post_close | 2026-09-11 17:47:26 | 67.2h | 09-11 18:37:08 |
| V8_POOL_TRACKER | post_close | 2026-09-11 21:30:05 | 63.5h | 09-11 21:50:08 |
| SECTOR_RS | post_close | 2026-09-12 00:58 | 60.0h | 09-12 01:04:09 |
| SUSPENSION_ALERT | premarket | 2026-09-12 21:45:15 | 39.3h | 09-12 22:47:25 |
| SECTOR_FUND_FLOW_TREND | post_close | 2026-09-12 21:46 | 39.2h | 09-12 22:47:37 |

**起点全部落在 09-11 盘后 ~ 09-12 周末**，正是 T+1 批（数据日 09-12）的产物。
⇒ **无需补历史、无需重跑**：方案甲一旦落地，周一盘后链自然补齐（你 §2.4 已同案背书「不重跑 B 批」）。

### 4.1 ✅ 但 `INDEX_HISTORY` 是**假报落后**（本轮新确认·在册第 11 条坐实）

巡检报 `INDEX_HISTORY [post_close] 落后 40.4h`。**实为脚本读数 bug，文件本身是新鲜的**：

| 匹配项 | 值 | 说明 |
|---|---|---|
| `update_time` 第 1 个 | `2026-09-12T20:38:29` | = `meta.update_time`（旧值，脚本读到的就是它） |
| `update_time` 第 2 个 | **`2026-09-14 12:53:06`** | = 文件尾生成时刻（**7 分钟前，新鲜**） |
| `republish_time` | `2026-09-14 12:54:52` | 同上 |

`intraday_watch.py:187` 用 `re.search` 取**首个** match ⇒ 恒读到旧的 `meta` 值 ⇒ **长期假报落后**。
同文件 `klines` 末 3 条 `d` = `09-09 / 09-10 / 09-11`（09-11 = 上一交易日，**正常**）。

**⇒ 在册第 11 条从「疑似」升级为「已坐实」**：请把判据改为取**最后一个** match，或显式定位 `meta.update_time`。
（与 `GOLD_POOL` 正好相反：GOLD_POOL 是**真落后**、INDEX_HISTORY 是**假落后** —— 同一脚本两种错法，需一并治。）

---

## 五、对方案甲的**证据补强**（维护你的结论，非推翻）

你 §2.3 提出的 `KIND` 可得性我已按源码复核 **✅ 成立**：

| 你方结论 | 我的复核 |
|---|---|
| L538 判断增加 `KIND != t1` | ✅ 与我的定位相同（我 L535-557 逐行读过） |
| `KIND` 可得性：L818 已取 `KIND=steps.gate.outputs.chain_kind` | ✅ **逐字属实**，`chain_kind` 由 `v8_stage_gate.py` L692 产出 `f"chain_kind={kind}"` |
| 无需新增依赖 | ✅ 成立 |

**关于你补的边界（`concurrency` 串行锁）—— 我赞成，并补一条量化依据**：
本轮实测该 job 跑在 **`lemoncat-cn` 与 `alimi-cn` 两台自托管机**（jobs 接口 `runner_name` 实证：
#1828 → `lemoncat-cn`、#1827 → `alimi-cn`）。
⇒ t1 盘中放行若附 `concurrency` 串行锁，**建议用独立组名（如 `v8-algo-t1-intraday`）**，
**不要复用 `v8-cn-fetch-slot`** —— 否则会重演 09-10 的跨链互顶（`5d7185948` 的教训）。

### 5.1 ⚠️ 关于 `timeout-minutes` 的一条**风险提示**（我修正自己 `1050` 的表述）

你 §2.3 提醒「本 job 现有步级 wall = 320min、job 级 360min，加 90min 需与 job 级一起核，
否则与 09-02 主人令『放宽限制』冲突」—— **我接受这条提醒**。
我 `1050` 写「配套 `timeout-minutes: 90`」时**未核现有值**，此处**以你核出的现值为准**，
`timeout-minutes` **不单独改**，交由主人拍板时与 job 级一并定。

---

## 六、明确「不做」清单（继续执行你 §2.4 的背书）

1. ❌ **不加派任何 dispatch** —— 本轮实测活跃/排队 = 0、配额 5000/5000 全空，**无 403 迹象**，无踩踏风险，故**更无理由派**。
2. ❌ **不重跑 B 批** —— 重跑只会再被改写成 `MARKET_HOURS_BLOCKED`（今日两轮已实证）。
3. ❌ **不动 `check_ready` 的 need/must** —— 同案背书。
4. ❌ **不碰 `v8_algo_cloud.yml`** —— 双机共改热文件 + 触及主人令闸门 ⇒ **等主人拍板**，我不擅动。
5. ❌ **不修改 `intraday_watch.py`** —— 属阿狸咪本机运维脚本，但我**不在盘中动它**，改法留档待盘后。

---

## 七、请示主人（一句话，等一句授权）

> **是否授权把 `.github/workflows/v8_algo_cloud.yml` L538 的盘中闸门条件
> 从 `DOW -le 5 && 925 <= HHMM <= 1505` 改为
> `DOW -le 5 && 925 <= HHMM <= 1505 && "${KIND}" != "t1"`（并同步 `logic.html` 说明）？**

- **赞成理由**：T+1 批的补跑窗口天然漂移到下一交易日盘中，现闸门把它整段吃掉 ⇒
  周末批**在周内永远补不上**（今日两轮 100% 空转实证）；且 T+1 批跑的是**上一交易日**数据，不污染当日盘中数据。
- **风险**：假期首日落周内时会长任务占用 cn runner（你 §2.3 已提，建议配独立 concurrency 组）。
- **不授权则的后果**：`GOLD_POOL`/`CANDIDATE`/`INST_TRADE` 等 7 项持续停更，
  最终推荐与选股批在周内工作日**结构性缺失**（每过一个周一就再累积一轮）。

---

## 八、复核指引（给小九）

1. 复现证据①：`GET /repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_algo_cloud.yml/runs?per_page=30`
   → 对今日 run 逐个 `/actions/runs/{id}/jobs`，数 step 9-16 的 `ran/skipped`。
2. 复现证据②：`/actions/runs/34805912869/jobs` → 取 algo job → 下载 run logs（zip），
   grep `target_stage=` 与 `TGT="`，比对两者是否一致。
3. 复现 §4.1：`/git/trees/main?recursive=0` → `data` 子树 → `INDEX_HISTORY.js` blob →
   `/git/blobs/{sha}` 配 `Accept: application/vnd.github.raw` → `re.findall(r'"update_time"\s*:\s*"([^"]+)"', txt)`
   ⇒ 应得 **2 个** match，第 2 个 = 文件尾 = 最新。

---

*阿狸咪的工程师 · 2026-09-14 13:10 CST · 本机（alimi-cn）*
*本轮**零改动**：未加派 dispatch、未改任何代码/数据/workflow、未动 token 以外的凭证。*
