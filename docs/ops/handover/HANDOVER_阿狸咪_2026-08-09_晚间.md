# 阿狸咪 → 小九 · 2026-08-09 晚间交接文档

> 面向：小九（GitHub 云端主链部署者 / self-hosted 应急回退 + 看门狗）
> 当前 main HEAD：`5235ca0d`（fix(scanner): 统一「投行研报」为「观澜台」）
> 阿狸咪 本地 HEAD：已同步至 `5235ca0d`，`git diff origin/main` 为空（全部入库）
> 撰写时刻：2026-08-09 23:25（阿狸咪）
> 关联早间文档：`HANDOVER_阿狸咪_2026-08-09.md`（00:27，逻辑详解审计）

---

## 一、入库与防覆盖确认（重点）

- ✅ 所有受跟踪源码改动已推 `origin/main`（`git diff origin/main --stat` 为空）。
- ✅ `git status` 仅 `?? dist/`（`.gitignore` 已忽略的 CI 构建产物，不需入库，由 `v8_build_deploy.yml` 重建）。
- ✅ 防覆盖机制已就位：`scripts/pre-push` 钩子（commit `dab3addb`）—— push `main` 前自动 `fetch + rebase origin/main`，避免远程 v8 build 改写 `index.html` 的 `?v=` 时间戳导致推送被拒。
- ⚠️ **小九改 `index.html` 或任何文件 push 前，必须先 `git pull --rebase` 到最新 `main` 基准，勿用本地旧副本覆盖**（早间文档已强调，此处重申）。

---

## 二、本周末核心修复清单（已入库，勿覆盖）

### A. 数据倒退恢复（根因：api_push_raw 读-改-写竞态）
| SHA | 内容 |
|---|---|
| `31166470` | fix(push): 修复 api_push_raw 读-改-写竞态导致 raw_data 反复倒退 + 恢复46个被覆盖文件 |
| `05e39a98` | chore(build): rebase 后重新生成 data/*.js 与 cache-busting |
| `0d5528d7` | fix(data): 二次恢复被 cn#178 旧版 push 打回的46个文件 |
| `d3aea8f2` | chore(build): 重建 data/*.js |

### B. 观澜台 / mahoro 双源打通（凭据路径 + GitHub Secrets）
| SHA | 内容 |
|---|---|
| `989ca1b5` | feat(tools): 添加本地凭据配置脚本 setup_credentials.py |
| `35c00766` | fix(creds): 修复两处凭据路径不一致导致观澜台/研报恒为0只 |
| `5c04341c` | feat(creds): 新增 GitHub Secrets 配置助手 + setup_credentials 改用邮箱验证码登录 mahoro |
| `5ca978b6` | fix(creds): mahoro cookie 落盘路径统一到仓库根 data/ |
| `8f1afbb8` | fix(pool): 观澜台研报来源恒为0只 —— 遍历错了层级（直接遍历 dict 的键而非 reports 数组） |

### C. 前端体验修复
| SHA | 内容 |
|---|---|
| `9ba93460` | feat(query): 个股查询增加相对强度(RPS)因子（A/B/C/D 档显示） |
| `c78c8899` | fix(ui): 选股驾驶舱「更新于」与「已跑」时间不一致（统一用 `_cockpitGenTs`） |
| `1fd330b2` | chore(build): rebase 后重建 data/*.js |

### D. 候选池来源去重与命名统一（本次审计收尾）
| SHA | 内容 |
|---|---|
| `ff02e8b8` | fix(scanner): 移除观澜台推股池的研报覆盖重复统计（观澜台=观澜台研报去重聚合，避免同股重复加分） |
| `5235ca0d` | fix(scanner): 统一「投行研报」为「观澜台」，消除历史命名混乱（RESEARCH_SRC 最终 = `("观澜台研报", "maharo研报")`） |

---

## 三、四大模块对应确认结果

### 1. 双机（阿狸咪 ↔ 小九）
- 今日无小九提交，仅阿狸咪人工提交 + v8 build 自动提交，无冲突。
- 双方职责清晰：小九 = 云端主链 + cn 应急回退 + 看门狗；阿狸咪 = 本地预览（不接部署）。
- pre-push 钩子已双向防护（小九若也在本机 .git/hooks 装了同模板则自动 rebase）。

### 2. 云端（workflow）
- `v8_algo_cloud.yml` env 已正确注入：
  - `ZSXQ_TOKEN: ${{ secrets.ZSXQ_TOKEN }}`
  - `MAHORO_COOKIE: ${{ secrets.MAHORO_COOKIE }}`
  - `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}`
- **用户已在 GitHub repo Settings → Secrets and variables → Actions 手动添加 `ZSXQ_TOKEN` 与 `MAHORO_COOKIE`**（2026-08-09 23:xx 确认完成）。
- 下次自动触发：周一~周五 `18:30` CST（`cron: 30 10 * * 1-5`）；紧急可 `workflow_dispatch` 手动 Run。

### 3. 运维（面板）
- 外部数据源卡片（line 7304 / 9739 / 9770 / 9810 / 9816）诊断逻辑引用 `ZSXQ_TOKEN` / `MAHORO_COOKIE` 两个 secret 名，与 workflow 注入一致。
- 六来源监控基线：主板≈97-100 / 创业板=100 / 科创板=100 / maharo≥10 / 港股0-50 / 观澜台0-N。
- 候选池来源分布现在应为：`主板成交前100 / 创业板成交前100 / 科创板成交前100 / 港股成交前50 / 观澜台 / 观澜台研报 / maharo研报`（无「投行研报」残留）。

### 4. 逻辑详解（文档）
- RPS / RS / 年线说明齐全（line 4683-4691）。
- 候选池来源监控说明正确（line 4665 / 4944）。
- 唯一「投行研报」出现处为 line 4573，是「已删除的永远空卡」功能描述（指待建日 K 行情库的规划卡），**非来源命名**，与代码不冲突，保留即可。

---

## 四、小九行动项

1. **勿覆盖**：push 前 `git pull --rebase origin/main` 到 `5235ca0d` 基准。
2. **验证双源**：下个 `v8_algo_cloud` 跑完后，查运维面板「🔌 外部数据源」卡片观澜台/maharo 应转绿；候选池来源分布应出现 `观澜台 / 观澜台研报 / maharo研报`（不再恒为0）。
3. **Secret 失效**：若某日观澜台/maharo 变红，优先让用户更新 GitHub Secrets 的 `ZSXQ_TOKEN` / `MAHORO_COOKIE`（maharo cookie 可能过期需重新邮箱验证码登录），不要改代码绕过。
4. **数据倒退复发**：若再出现 raw_data 被旧内容打回，先查 `api_push_raw.py` 的防倒退守卫（`05e39a98` 引入）是否被绕过，再查是否有并发 job 读-改-写冲突。

---

## 五、附：今天全部非-build 人工提交（防覆盖核对用）

```
5235ca0d fix(scanner): 统一「投行研报」为「观澜台」
ff02e8b8 fix(scanner): 移除观澜台推股池的研报覆盖重复统计
1fd330b2 chore(build): rebase 后重建 data/*.js
c78c8899 fix(ui): 选股驾驶舱「更新于」与「已跑」时间不一致
8f1afbb8 fix(pool): 观澜台研报来源恒为0只
5ca978b6 fix(creds): mahoro cookie 落盘路径统一到仓库根 data/
5c04341c feat(creds): 新增 GitHub Secrets 配置助手
35c00766 fix(creds): 修复两处凭据路径不一致
989ca1b5 feat(tools): 添加本地凭据配置脚本 setup_credentials.py
9ba93460 feat(query): 个股查询增加相对强度(RPS)因子
d3aea8f2 chore(build): 重建 data/*.js
0d5528d7 fix(data): 二次恢复被 cn#178 旧版push打回的46个文件
31166470 fix(push): 修复 api_push_raw 读-改-写竞态
05e39a98 chore(build): rebase 后重新生成 data/*.js
（以上均已在 origin/main，HEAD = 5235ca0d）
```

---

## 深夜补充（00:20 后）：云端 run 双源真实状态

### ✅ 已修：maharo 假绿
- run#31321250124 中 `fetch_maharo_signals.py` 在 CI 无 TTY 走交互 `input()` → `EOFError` 崩溃，且**白发了一次验证码到邮箱**，被 continue-on-error 包成 `✅ 成功` 假绿。
- 根因：`MAHORO_COOKIE` secret 里的 cookie **已过期**，`authenticate()` 测出无效降级到交互输入。
- 修复（`337b3956`）：`authenticate()` 改为 `if non_interactive or not sys.stdin.isatty(): return None`，无终端干净跳过。
- ⚠️ **maharo 数据仍为空**：cookie 过期需刷新，需 ljcat999@gmail.com 收新验证码（用户操作）。二选一：
  (a) 本地交互登录后把 `data/.maharo_cookies.txt` 内容更新到 GitHub Secret MAHORO_COOKIE；
  (b) 用户给新验证码，阿狸咪代跑登录并更新 secret。

### 🔍 排查中：观澜台抽取成功但候选池来源 0
- 观澜台抽取本身 ✅（ZSXQ_TOKEN 有效，generates `out/guanlan_reports.json`）。
- 但云端 `raw_data/candidate.json` 来源分布**无观澜台/观澜台研报/maharo研报**（仅成交量来源）。
- 本地 `build_candidate_pool.py` 实跑正常：337 只，观澜台 61+61、maharo 3 → 代码逻辑对。
- `step_run` 用 `capture_output=True` 吞掉 build 内部打印，CI 日志看不到「文件是否存在」细节。
- 已加调试落盘（`raw_data/build_candidate_debug.json`）+ `DATA` 优先 `V8_OUT_DIR`，并已**重触发云端 run** 取真实证据。
- **监控：** `scripts/monitor_debug_run.py`（task `pMXXhj`），跑完自动报告 `build_candidate_debug.json` 内容（DATA 路径、各文件 exists、股票数）。

### 小九行动项
1. 等 monitor `pMXXhj` 结论：若 debug 显示 guanlan 文件 `exists:true` 但 `stocks:0` → 是 guanlan_extractor 产出结构问题；若 `exists:false` → 是路径/时序问题。按证据修。
2. mahoro cookie 刷新需用户配合（邮箱收码），届时按上面 (a)/(b) 处理。
3. 勿手动 rebase 冲突时硬推；main 有 pre-push 自动 rebase 钩子。
