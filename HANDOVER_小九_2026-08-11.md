# HANDOVER 小九 → 阿狸咪（2026-08-11）

> **更新于 2026-08-11 12:00 自动更新**（覆盖 08:33 盘前档，含盘中全部改动）
> main HEAD 核实值：`358e39a6`（`v8 build: 2026-08-11 11:49`）
> 最后一个业务提交：`a01bbe1e`（11:49）

---

## 一、背景

今日为周二交易日，小九白天独家值守（07:45–17:45）。上午的作业分四条线并行：

1. **盘前全量**（08:09–08:15）+ 核对你 06:19–07:16 的 16 个 commit 交接件；
2. **主人多轮截图反馈**（08:42 / 08:44 / 08:50 / 08:55 / 10:36 / 10:53 / 11:48 / 11:50），全部按"一劳永逸式根因修复"处理，不做单点补丁；
3. **看门狗 154–158 轮**（含 3 处脚本级根因修复）；
4. **健康检查/自愈体系加固**，把误报口径与自愈派发链路对齐。

主人今日明确表达两条纪律，请一并遵守：
- **拒绝"勾勾叉叉"式验证**——不接受 `9 PASS / 1 FAIL` 这类合成测试报告，要求 dump 真实渲染结果或真实浏览器截图；
- **一劳永逸**——发现问题必须回溯到脚本层根因并落 main，避免被云端流水线覆盖回旧逻辑。

---

## 二、今日已完成改动（真实 commit SHA，均已在 origin/main）

### A. 盘前 + 交接核对（08:13–08:32）

| 时间 | Commit | 内容与影响 |
|---|---|---|
| 08:13 | `3e2796b1` | 盘前全量：fetch 14/14 全绿 + build 58 js |
| 08:22 | `14c3f5c6` | **gtimg 根治**：新浪 hs_a 排行不可达时用候选池兜底 A 股股池（云端分支原先完全无兜底 → TOP10 全港股）。你待办 2 已关闭 |
| 08:25 | `660e079f` | **自愈映射根因**：NT_DATA 的 heal_cat 改 `algo_run`（其 raw 由 run_algorithms 产出，派发 cn_fetch premarket 永远刷不到）；顺手修 `premarket_cleared` 判断的 page 变量作用域 bug |
| 08:29 | `12d4334d` | **stage_to_raw 双守卫**：同日时间戳倒退守卫（原只比到"日"，同日内旧覆新拦不住）+ 丢源守卫。实跑额外拦下 lhb_result / inst_trade / cockpit_advice / mahoro_signals 四个长期静默倒退文件。你待办 3 已关闭 |
| 08:32 | `7dcb0194` | 盘前交接文档 |

### B. 主人截图反馈 · UI/数据修复（08:43–09:11）

| 时间 | Commit | 内容与影响 |
|---|---|---|
| 08:43 | `fc22df42` | 6 项：每日风险列出机游共振卖出个股名 / 机游共振去重改"机构独买" / **删除北向资金卡片**（数据源 2024-05 起停更）/ 候选池补 10 只港股名（09866→蔚来-SW）/ TOP3 持仓加 sh688548 广钢气体种子 / 平均股价区分「盘前待刷新」与「抓取失败」 |
| 08:47 | `bcb4b433` | 行业树图徽章动态化；ETF 三合一 Block2、板块资金三合一空状态改读 `.note` 字段（原写死字符串） |
| 08:53 | `7371505a` | **明日关注全 0 分根治**：`CAND.stocks` 根本没有 `ema_score/score` 字段，改用 `FINAL_RECOMMEND_DATA.all_candidates.final_score`；明日风险卡加竖排个股列表（按流出金额倒序） |
| 09:01 | `aa1b6e0d` | **3 个盘后源不再被盘前清空**：renderSector 改三源合并（SECTOR_FUND_FLOW_TREND + SECTOR_RS + 仅当有真数据才叠 SECTOR_FUND_FLOW）；AI 市场速览徽章动态化；涨停热力矩阵清空逻辑改"末位≠今日则 append 今日占位列" |
| 09:04–09:11 | `898fc013` / `3785d2ba` / `62114f01` | LIMIT_UP_HEATMAP 本地紧急补 08/11 占位列（经 rebase 冲突重写为干净 11 列） |

### C. 算法与推荐逻辑（09:32–11:09）

| 时间 | Commit | 内容与影响 |
|---|---|---|
| 09:32 | `48dfd6cc` | **Top3 港股被挤出根治**：A 股硬保底由灌满 3 slot 改为 `MIN_A_SHARES=1`，余下 slot 从全局高分填补；`fix_name()` 改 NAME_FIX_MAP 优先于 profile name（09618 显示"京东物流"而非"京东集团-SW"）。线上 Top3 = 09988 阿里巴巴-W 5.5 / 09618 京东物流 5.5 / 301583 托伦斯 4.69 |
| 11:03 | `7a98a936` | **`_runBadgeInfo` 三态时段守卫**：盘前不显示 / 盘中 ⏳ 今日盘中预览 / 盘后 ✅ 今日已跑完；A 股缺失提示不再写死 18:30 |
| 11:07 | `e9e7dbec` | **horizon 判定放宽**：`horizon_for` 加 `is_top` 双分支，候选池含中线策略或多源短线共振 ≥2 归中长线（top3 保持严格短线语义）。预期 30 只 → 22 短线 + 8 中长线 |
| 11:09 | `023fcf2d` | **持仓种子 v3 迁移**：自动补全 688548 广钢气体 + 校准 sz159865（养殖 ETF 被错标芯片）；新增 `🔄 重置种子` 按钮 |

### D. 运维与健康检查根因（09:26–11:49）

| 时间 | Commit | 内容与影响 |
|---|---|---|
| 09:26 | `aa741d84` | **看门狗误报根治**：`in_progress/queued` 的 run 因 conclusion=None 逃过过滤被判 FAIL → 交易时段高频 cron 下长期误发告警邮件。改返三元组分离 running + `RUNNING_GRACE_MIN=45` |
| 10:19 | `dce00185` | 健康检查三类误报根治（盘前清空守卫 / Pages SHA 无重试 / 阈值自适应）+ **回滚 ETF_DAILY_MONITOR 抓取回归**（`aa1b6e0d` 误把它从 intraday 改 post_close → 被踢出盘中抓取清单，云端日志实证"涉及 11 个变量"少一个） |
| 10:26 | `00ba6ed4` | **api_push_raw 整批崩溃根治**：`http.client.IncompleteRead` 继承自 `HTTPException` 而**非 OSError 子类**，旧异常元组漏捕 → 单次截断即 exit 1 → 57 个文件全批丢失（实证 run 31452057629）。已补 `HTTPException` + `json.JSONDecodeError` + GET 三次退避重读 |
| 10:29 | `3f50b4d0` | 时间轴槽位前置宽容 15 → 45min，与 `v8_health_check.adjust_max_age` 口径对齐，消除每日早间误报红叉 |
| 10:43 | `6dcafdf7` | **cache-buster 卡死根治**：`update_v8.py::_data_file_update_time()` 原只读内容里的 `update_time` → SH_FIB/SZ_FIB 的 `v=202608110907` 全天锁死。改为 `max(语义时间, 文件 mtime)`（semantic_ts 需 `.replace(tzinfo=CST)` 否则比较抛 TypeError 被吞） |
| 11:38 | `9b02a7f2` | 13 张算法链卡片自愈类别改 `heal_cat=algo_run`（原按 page 映射 post_close，而 CATEGORY_MAP 的 post_close 只注册 2 项 → 自愈永远刷不到还白占 25min 去抖锁）；`check_data_cards()` 增统一出口 `_emit()` 让 4 个分支都透传 heal_cat |
| 11:49 | `a01bbe1e` | ETF 日监控 TOP10 主力净流入/流出：名字后跟 ETF 代码（数据已有 `code` 字段，纯渲染层漏展示） |

---

## 三、当前系统状态（12:00 核实）

| 项目 | 状态 |
|---|---|
| 站点 | `https://ah-quant999.github.io/quant-scanner-v8/` **HTTP 200** |
| origin/main HEAD | `358e39a6`（v8 build 11:49） |
| 本地工作树 | 已 ff 到 `358e39a6`，`git status` 干净（无 untracked、无 modified） |
| 健康检查 | 11:49:55 **ok 41 / warn 1 / fail 0**（唯一 warn = `site_sync` 本地/线上 commit 比较瞬时态，fetch 后自动恢复） |
| cn runner | 在线；**主通道是云端 `v8_cn_fetch_cloud.yml`（workflow id 327687211）**，本机 `324135267` 已降级为应急通道（近 7 次 run 全 failure） |
| 本机自动化 | 13 条全部 **ACTIVE**，模型今晨 08:03 已由 `deepseek-v4-flash` 全量切回 **`hy3`**（改库 `~/.workbuddy/workbuddy.db` automations.model_id，已备份 `workbuddy.db.bak.20260811_080351`） |
| 看门狗 | 158 轮，六管线全绿，0 封告警邮件 |
| 备份清理 | `backup/stage_0811_0826/` 与两个 `data/*.bak_pre_20260811_*` 经逐项比对确认 stale/冗余后已清理，git status 干净 |

---

## 四、需你注意 / 待办 / 风险

### 🔴 今晚 18:30 必须复核（主人已拍板等这一轮）

`e9e7dbec` 的 horizon 放宽只改了 `algorithms/final_recommend.py`，而**盘中 fetch 不跑 final_recommend.py，只有盘后 18:30 跑一次**。当前线上候选池仍显示「短线 30 · 中长线 0」，属数据周期问题而非代码问题。
→ **18:30 云端跑批后请确认候选池出现中长线分组（预期 22 短线 + 8 中长线）**，若仍为 0 再回头查判定分支。

### 🟡 待办与风险

1. **`stage_to_raw.py` 打印 `[guard]` 拒绝搬运是预期行为**，不是故障——被拒说明 raw 侧更新更全。若确需用 out/ 覆盖 raw，显式删除 raw 目标文件再跑，**不要摘守卫**。
2. **候选池已成关键单点**：链路为 候选池 → gtimg 兜底 → gold_pool → TOP10。`raw_data/candidate.json` 若坏，下游全塌，优先级已升为最高。
3. **兜底派发别再用旧 workflow**：单文件掉队时不要反复派发 `324135267`（应急/近期全 failure），直接派 **`327687211`**；诊断手法是拉 run 日志比对「涉及 N 个变量」与 `✅` 模块列表，N 少一个即 `CATEGORY_MAP` 被误改。
4. **别再用改 category 的方式实现"盘前保值"**：抓取时段（CATEGORY_MAP）与盘前不清空（KEEP_VARS）是两个正交语义，`dce00185` 已在源码加注释。
5. **遗留建议（未做）**：`api_push_raw.main()` 宜改为「失败文件记账、成功文件照常 commit」的部分成功语义，避免单点异常再次拖垮整批。全仓 26 个 urlopen 脚本已核查，IncompleteRead 漏捕**无第二处**。
6. **待用户拍板**：港股 akshare 双接口不通（暂无替代源）；TOP3 `MIN_FINAL_SCORE` 硬性门槛属产品决策。

### 📌 编码纪律（今日两次踩坑，请同步遵守）

- **前端用数据字段前必须先验证真实结构**，不能从字段名猜（"ema_score 听起来对"就以为存在）。先 `python -c "print(list(d['xxx'].values())[0].keys())"`。
- **新增 .js 数据源时 fallback 文案必须用 `data.note`**，徽章按 `update_time` 动态判断，不要写死字符串或写死"盘中"。
- **用户报"卡片少字段"时先 grep 数据源**：字段已有 = 渲染层漏展示，改一行模板即可，不必动数据流水线。

---

## 五、小九监督职责（今日剩余时段）

| 时点 | 动作 |
|---|---|
| 每 30 分 | 盘中实时数据刷新兜底（掉队文件走云端 327687211） |
| 每 2h | 管线看门狗 + 自动修复 + 紧急交接 |
| 每小时 | 本地看板 pull + 紧急交接通道（看门狗） |
| 15:30 | 收盘数据刷新（API 派发 post_close） |
| 17:00 | 选股四模块 + 全量数据部署 |
| **18:00** | 本交接文档二次更新（覆盖盘中至盘后全部改动） |
| **18:30** | **重点核验 horizon 中长线候选池是否产出**（见上文 🔴） |
| 19:00 | 每日审计修复 + 推仓 + 对齐 |

值守交班：小九 17:45 收工，此后（18:00–次日 07:30）由你接管部署权。

_小九 2026-08-11 12:00 自动更新_
