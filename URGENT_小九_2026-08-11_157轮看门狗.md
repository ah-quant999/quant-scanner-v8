# URGENT 小九 → 阿狸咪 · 第 157 轮看门狗（2026-08-11 10:18–10:32）

## 一句话结论
盘中 cn_fetch **整批推送崩溃**导致 ETF 日监控等 57 个文件全部丢失；已定位为 `api_push_raw.py` 漏捕 `http.client.IncompleteRead`，**代码层根治并推送 main**，重跑后数据恢复正常。

## 一、六管线巡检（EXIT 0，0 封告警邮件）

| 管线 | 状态 | 明细 |
|---|---|---|
| runner | ✅ | lemoncat-cn online=True / busy=False，免 heal |
| cn_fetch | ✅ | 10:07 completed/success（age 11m） |
| build_deploy | ✅ | 10:13 completed/success（age 5m） |
| raw_data 新鲜度 | ✅ | 10:12 提交（age 6m，阈值 1.5h） |
| site | ✅ | HTTP 200 |
| auto_dispatch | ✅ | 数据新鲜（raw_data 0.1h / cn_fetch 0.1h），无需派发 |

邮件：0 封。看门狗判定「无管线异常且健康检查无超阈 ≥120min 项」→ 跳过邮件。

## 二、健康检查

- 初跑 @10:18:57 rc=2；复跑 @10:22:18 统计 **ok 40 / warn 0 / fail 2**
- FAIL①「实时数据 / ETF 日监控」：盘中交易时段被异常标记为 `premarket_cleared`，update_time 停在今日 08:12 → **真问题**
- FAIL②「管线 / 本地与 origin/main 同步」：本地落后 origin → 瞬时态

## 三、【根因级修复 · 一劳永逸】api_push_raw.py 整批推送崩溃

### 现象链
1. 自愈已于 10:20:12 派发 intraday cn_fetch（run `31452057629`，sha `9ab3bb7c`）
2. 该 run **completed/failure**，失败步骤 = `📤 经 API 推送 raw_data/ 到 main`
3. 抓取本身成功，**待推送 57 个文件**（含 `data/FOUR_VOLUME.js`、`raw_data/*.json` 全量）
4. 推送阶段崩溃 → 57 个文件**一个都没落地** → ETF 日监控保持 08:12 空壳

### 崩溃点
```
File "api_push_raw.py", line 181, in main
    rb = api("GET", f"/repos/{REPO}/git/blobs/{remote_sha}")   # 防倒退守卫读远端 blob
http.client.IncompleteRead: IncompleteRead(491873 bytes read, 24313 more expected)
```

### 根因
`api()` 的 2026-08-05 版只捕获 `(TimeoutError, urllib.error.URLError, OSError)`。
**`http.client.IncompleteRead` 继承自 `HTTPException`，不是 `OSError` 子类**（已实证：
`isinstance(e, OSError) == False`，MRO = IncompleteRead → HTTPException → Exception）。
因此 cn runner 网络抖动造成响应体截断时异常直接逃逸 → 进程 exit 1 → 整批中止。

> 这是 08-05「捕获网络异常防整体崩溃」那次修复的**同类漏洞复发**：异常覆盖面不完整。

### 修法（已推 main：`00ba6ed4`）
1. 异常元组补 `http.client.HTTPException` + `json.JSONDecodeError`
2. **幂等请求（GET）内建 3 次指数退避重读**（1s/2s），不再依赖调用方兜底
3. 非幂等（POST blob）仍只发 1 次，交由既有 3 次重试逻辑，避免重复副作用

### 验证
- `py_compile` 通过
- 构造 `IncompleteRead(491873, 24313)` 注入：
  - 场景1 GET → 未崩溃，返回 `{'__error__':'network'}`，重试 3 次 ✅
  - 场景2 POST → 调用 1 次，不重复副作用 ✅
- 远端 `api_push_raw.py` 四处关键标记全部落地 ✅（Contents API 核对）

## 四、修复后重跑与数据恢复

- 10:27:04 重新派发 intraday cn_fetch（HTTP 204，ref=main，跑在 `00ba6ed4` 上）
- 推送步骤通过，触发 build `8ed38759`
- **`raw_data/etf_daily_monitor.json` 恢复**（远端实测）：

| 项 | 修复前 | 修复后 |
|---|---|---|
| update_time | 2026-08-11 08:12:54 | **2026-08-11 10:27:56** |
| premarket_cleared | `True` | 无（已清除） |
| 结构 | `no_data` 空壳 | total_etf / total_net / top_inflow / top_outflow |
| 数据量 | 0 | 1535 只 ETF，净额 -3.90 亿，进出榜各 10 条 |

样本：创业板ETF易方达(159915) 净流入 2.43 亿 / 港股创新药ETF广发(513120) 净流入 1.36 亿。

## 五、本轮 git 处理（重要 · 请阿狸咪知悉）

- **未做本地 ff / 未提交本地 git**：检出时 `index.html` 处于 modified（并行会话正在编辑），
  按协作铁律小九不得对其做 checkout/restore/stash，故放弃本地对齐。
- 改走 **Contents API 直推远端 main**（本项目既有模式，不动本地 HEAD、不碰 index.html）：
  - `00ba6ed4` api_push_raw.py 根因修复
  - 本 URGENT 文件
- 他方产物按铁律原样保留、不提交不删：`backup/stage_0811_0826/`、
  `data/FINAL_RECOMMEND_DATA.js.bak_pre_20260811_namefix`、`data/LIMIT_UP_HEATMAP.js.bak_pre_20260811_11col`
- 另注：并行会话已于 10:19:56 推 `dce00185`（健康检查三类误报根治 + ETF_DAILY_MONITOR 抓取回归回滚），
  与本轮修复互补 —— 那次修的是**抓取端**，本轮修的是**推送端**。

## 六、遗留 / 建议

1. **建议排查同类异常覆盖漏洞**：全仓搜 `except (TimeoutError, urllib.error.URLError, OSError)` 模式，
   凡走 HTTP 的都应补 `http.client.HTTPException`（本轮已修 api_push_raw.py，其余脚本待查）。
2. 建议给 `api_push_raw.py` 的 main() 加**整体 try/except + 部分成功落地**语义：
   当前任一环节抛未预期异常仍会丢掉整批，宜改为「失败文件记账、成功文件照常 commit」。
3. 云端高频小更新循环延续（10:11–10:30 连推 10+ 次 build）。
4. 134 轮 except-pass 改日志建议仍未落地。
5. stash 残留 20+ 条待清。
6. 155 轮建议仍有效：核对 `run_algorithms.py` 清单 vs `cloud_fetch_v8.py` 注册表，只在前者出现的源都应补 `heal_cat` 防自愈空转。
