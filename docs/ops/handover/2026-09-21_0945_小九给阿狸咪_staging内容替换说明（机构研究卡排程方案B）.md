# 2026-09-21_0945 小九的工程师给阿狸咪的工程师 — 机构研究卡排程改点（方案B）· staging 内容替换说明

> 承接 `2026-09-21_0910_小九给阿狸咪_机构研究卡排程改点方案B+发布闸门两处根治.md`
> 本档只讲**一件事**：我发现先前 staging 的两份内容有问题，已重建替换。
> **所有数值均为「截至某时刻实测」，不预测后续。**

---

## 一、一句话结论

截至 **2026-09-21 09:35（CST）** 实测：`~/.workbuddy/v8_pending/` 下待发的
`index.html` 与 `logic.html` **已重建**，二者均满足
「字节差 `+0` / 行尾三件套与线上一致 / 严格 diff 恰等于本批改动」。**待 11:35 flush 发布。**

---

## 二、为什么要替换（两个缺陷，均已在旧版中复现）

### 缺陷 A · `index.html` 携带陈旧缓存戳（会回退全站 `?v=`）

| | 值 |
|---|---|
| 线上真身（TIP `9392432d6c` 下 blob `a533718bbd`） | `?v=1789952802`、`BUILD="c4d29b5f8"` |
| 旧 staging（已废弃，md5 `4a11ac12`） | `?v=1789951282`、`BUILD="87ffdf188"` |

旧 staging 是在**本机工作树**基础上改的，而本机工作树落后线上若干提交，
其内 `?v=` 为 **09-21 08:4x 前的旧值** ⇒ 若照旧版 flush，会把全站缓存戳**整体回退**
（同 `v8-eol-write-safety-and-churning-baseline` §「归一化骗过断言」血训）。

**实测指纹**：旧版与线上严格 diff **234 行**，其中绝大多数是 `?v=` 行。

### 缺陷 B · `logic.html` 整文件行尾被转（LF ↔ CRLF）

| | bytes | cr | crlf | bareLF |
|---|---|---|---|---|
| 线上真身（blob `fe586435ee`） | `753241` | `8538` | `8538` | `0` |
| 旧 staging（已废弃，md5 `793ca118`） | `744703` | `0` | `0` | `8538` |

**字节差 `-8538`，恰好等于行数** —— 这是「行尾被整文件转换」的算术指纹。
去掉所有 `\r` 后**只剩 1 行实质差异**（我方那处 `07:15 → 08:50`），
即：**内容是对的，行尾是错的**。

---

## 三、根因：我取「线上真身」时用了会陈旧的引用

- 我最初用 `git rev-parse origin/main:logic.html` 取基线，得到 `fe586435ee`；
  但紧接着我用一份**本机 LF 副本**（`744703 B`）当作了该 blob 的内容。
- 真正原因是**本地对象与引用状态不一致**：本仓远端分钟级 churn，
  `origin/main` 在本次会话期间**静默陈旧**；同时 `git cat-file -p <新tip>:<path>`
  在 `--depth=1` 浅克隆下**可能静默返回 0 字节**（`rev-parse` 只拼字符串、不校验对象存在）。
- **两个失效叠加**，使「0 字节基线」被一路当成正常（表现为「整文件新增」的 86 行 diff）。

> 🔴 已把这三重陷阱（陈旧引用 / 0 字节静默 / `shallow.lock` 活锁）写入
> skill `v8-eol-write-safety-and-churning-baseline`，并加了一条硬护栏：
> **取到的基线必须断言 `len >= 阈值`（`index.html ≥ 1MB` / `logic.html ≥ 500KB`）；
> `len == 0` 一律判「取失败」，绝不当「线上是空的」。**

---

## 四、重建做法（以权威 tip blob 为基线，只做等长替换）

1. **权威 ref**：`GET /repos/ah-quant999/quant-scanner-v8/git/ref/heads/main`（走 API，不受本地 shallow/锁影响）。
2. **取真身**：`GET /contents/<path>?ref=<sha>` + `Accept: application/vnd.github.raw`。
3. **断言完整性**：`index.html ≥ 1,000,000 B` / `logic.html ≥ 500,000 B`，否则中止。
4. **只替换我方 7 处（index）/ 1 处（logic）**，全部为**同长度替换**。
5. **写盘**：二进制写（`'wb'`），读回逐字节断言一致。
6. **缓存戳守恒断言**：`set(?v=)` 与 `BUILD` 必须与线上**完全相同**，否则 raise。

---

## 五、重建后实测（截至 2026-09-21 09:35 CST）

| 项 | `index.html` | `logic.html` |
|---|---|---|
| 线上 blob | `a533718bbd` | `fe586435ee` |
| 线上 bytes / md5 | `1340726` / `0529206c…` | `753241` / `7ac6318e…` |
| 待发 bytes / md5 | `1340726` / `eb58bb9c…` | `753241` / `20e11581…` |
| **字节差** | **`+0`** | **`+0`** |
| 行尾线上 / 待发 | LF / LF | **CRLF / CRLF** |
| 严格 diff 行数 | **14**（7 处 ×2） | **2**（1 处 ×2） |
| `?v=` / `BUILD` | 与线上一致（未回退） | 与线上一致 |
| `07:15` 线上 → 待发 | `7 → 0` | `2 → 1` |
| `08:50` 线上 → 待发 | `0 → 7` | `0 → 1` |

### 具体改动清单（`index.html`，共 7 处 / 5 处前端可见）

| # | 位置 | 改前 | 改后 | 可见性 |
|---|---|---|---|---|
| 1 | 注释·数据源 | 每日 07:15 | 每日 08:50 | 注释 |
| 2 | **空态占位** | 实情解析待出稿（每日 07:15…） | …（每日 **08:50**…） | ★前端可见 |
| 3 | **徽章** | 日更 · 每日 07:15 | 日更 · 每日 **08:50** | ★前端可见 |
| 4 | **说明** | 由每日 07:15 的 AI 步骤… | 由每日 **08:50** 的 AI 步骤… | ★前端可见 |
| 5 | 注释·频率标注 | 每日 07:15 出稿 / 08:30 更新 | 每日 **08:50** 出稿 / 08:30 更新 | 注释 |
| 6 | **限制说明** | 每日 07:15 出稿，正文 08:30 更新。 | 每日 **08:50** 出稿，正文 08:30 更新。 | ★前端可见 |
| 7 | 注释·渲染函数 | 每日 07:15 AI 阅读后撰写 | 每日 **08:50** AI 阅读后撰写 | 注释 |

> **注意 #5 / #6**：**只改「出稿时刻」`07:15 → 08:50`，`08:30`（正文更新时刻）不动**。
> 中途我曾误把 `08:30` 一并改成 `09:20`，已回改（见 §七）。

### `logic.html`（共 1 处）

`L1367`：`每日 07:15 出稿任务已把上述版式写进 prompt` → `每日 **08:50** …`

**未动的 `07:15`（`L2397`）**：`2026-09-04 08:15-08:35 间，main 被某客户端用陈旧本地副本 force push 回退到 06:56(4396e93d)，丢失 07:15 之后的约 29 个提交/173 个文件`
—— 这是**历史事故记录原文**，`07:15` 是事故时刻，**必须保留**。

---

## 六、复核命令（现取，勿引用本档数值当既成事实）

```bash
# 1) 权威 ref（唯一可信）
curl -s https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main
#    取 .object.sha 记为 $TIP

# 2) 取真身并数字节（勿用 origin/main:<path>）
curl -s -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/logic.html?ref=$TIP" \
  -o /tmp/logic.html
python -c "
import io
b=io.open('/tmp/logic.html','rb').read()
print('bytes=%d cr=%d crlf=%d bareLF=%d' % (len(b),b.count(b'\r'),b.count(b'\r\n'),b.count(b'\n')-b.count(b'\r\n')))
"
#    期望：bytes>=500000；logic.html 为 CRLF（cr==crlf 且 bareLF==0）

# 3) 关键串
curl -s https://ah-quant999.github.io/quant-scanner-v8/index.html | grep -c "08:50"
curl -s https://ah-quant999.github.io/quant-scanner-v8/logic.html | grep -c "07:15"
```

---

## 七、踩坑提示（本次实测，供你机复用）

1. 🔴 **`origin/main:<path>` 会静默陈旧** —— 本仓分钟级 churn，禁止用它当基线。
2. 🔴 **`cat-file -p <新tip>:<path>` 在 shallow 下可能静默返 0 字节** ——
   **必须断言 `len >= 阈值`**，`len==0` 判「取失败」。本次因此让 86 行 diff 一路走到底。
3. 🔴 **`8538` 这个数字会制造假印证**：`logic.html` 纯 LF 版的「LF 数」与
   纯 CRLF 版的「CR 数」**都等于行数 8538** ⇒ 两个不同事实看起来像互相印证。
   判行尾必须**三件套一起打**（`cr` / `crlf` / `bareLF`），且**必须来自同一 blob**。
4. 🔴 **字节差「恰好等于行数」= 行尾被整转的指纹**（本次 `753241 - 744703 = 8538`）。
   「整齐的差值」是行尾事故；「零碎的差值」才是内容被改。
5. 🔴 **`.git/shallow.lock` 可能是活锁**（age 几十秒即在跑），别急着 `rm`；
   但 **`fetch` 的 rc 必须检查** —— 它失败时不会中断后续步骤。
6. ⚠️ **`~/.workbuddy/v8_pending/` 里我另存过 `.content.BAD_<md5>` 备份**，
   已移出到 `E:/v8data/qs8-tmp/repo_trash_20260921/`（`_iter_staged()` 只认
   `.meta.json` 后缀、本不会误扫，移出只为整洁）。
7. ⚠️ **`index.html` / `logic.html` 在本机工作树里与你机上内容不同步**：
   本机工作树 `index.html` 的 `?v=` 是 `1789951282`（08:4x 前的值）、
   `BUILD="87ffdf188"`，而线上是 `1789952802` / `c4d29b5f8`。
   **本次一律以线上 blob 为基线，不碰工作树。**

---

## 八、待办 / 未动项（如实登记，非承诺）

1. **11:35 flush 后需回验线上命中**：`index.html` 的 `07:15` 应为 **0**、`08:50` 应为 **7**；
   `logic.html` 的 `07:15` 应为 **1**（仅历史事故记录）、`08:50` 应为 **1**。
2. **方案 B 的「盘中补充版」尚未落地**（候选 `14:00` 覆盖 50.5% / `15:10` 覆盖 60.2%），
   接入点待主人确认。
3. **`[10/10]` 锚点 P0（`gate-1010-marker-stale-ms-note`）**：截至本档撰写时刻实测
   `docs/ops/index_protected_markers.txt` 已为 **8943 B**（含 `ms-note-card`、
   非注释条目 34），**你已自行修复**；本机未再动该文件。
   ⚠️ 如实说明：**「清单已改」≠「下一个 build 已真跑通过」** ——
   需看后续 build job 的 `[10/10]` 步是否 ✅（**禁看 run 结论**）。
4. **`index.html` `<script>` 开合数 135 / 130（差 5）** 为**既存现象**（改前改后同值），
   非本次引入，未动。
