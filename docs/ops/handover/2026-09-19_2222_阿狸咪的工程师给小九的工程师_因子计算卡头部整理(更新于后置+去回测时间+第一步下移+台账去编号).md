# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-19 22:22｜因子计算卡头部整理(更新于后置+去回测时间+第一步下移+台账去编号)

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主站「🧮 每日因子计算」卡头部按主人令完成 4 项整理（**只改 index.html，7 个差异块全部落在本卡区**），已推 `main` 并线上生效；逻辑详解页第八章为纯计算规则说明、**不含卡头布局细节，无需同步**。

---

## 正文

### 一、本轮改动（主人原话 · 图像取证 5 张截图）

| 主人指令 | 落地（index.html 行号=术后） |
|---|---|
| 「更新于放到卡名后面」 | 新增 `<span id="dfcUpdSlot">`（L3133，卡名「🧮 每日因子计算」正后方）；`renderFactorLab` 里 `_flBadge` 从第二步头部**移出**（原 L7549 `+ _flBadge` 删除），改为 `dst.innerHTML = html;` 之后注入（L7601-7605）。徽章仍读 `D.update_time` 产物真值，**不写死**、停更告警照旧 |
| 「这个（回测时间）删除」 | `dfcBadge` 渲染去掉 `+ (_fwAt ? ('　回测 '+_e(_fwAt.slice(5,16))) : '')`；卡头只留「共 N 项」。回测口径/时间仍在页脚 + 「口径与独立分层回测明细」折叠内（不重复占卡头） |
| 「这个放到第二行」 | 「第一步 · 每日打分 / 入选后打分全表 / 共 N 项」三个元素从卡名行下移到**新建的第二行 flex 容器**（L3136-3140）；卡名行只剩「卡名 + 更新于」 |
| 「怎么只有一个 1，后续的没有就把这个 1 删除」 | 台账摘要 `▸ 展开 · ① 算法链因子台账（共 N 项…）` 去掉 `①`；**同时去掉下方 `②`**（「② 当日加减分排列已独立成新卡…」→ 无编号）——删①留②会留下孤立编号，两行现为并列无编号说明 |

改动 commit：**`256c57092924`**（基线 `7b0b31f18c`）。

### 二、实证（非口述）

- **差异范围**：与推送前同一基线逐行 diff → **7 个差异块，合计删 6 行 / 增 17 行，全部落在 3129-3134 / 7335 / 7555 / 7568 / 7571 / 7601 六处**，无一处误伤其他区块。
- **推送后回读远端 blob**（`_r337_verify.py`）：8 项断言全 PASS（插槽 ×1、注入 JS ×1、`+ _flBadge`=0、`_fwAt.slice(5,16)`=0、`展开 · ① `=0、`> ② 当日加减分排列`=0、两条无编号摘要各 ×1）。
- **语法门禁**：推送后 index.html 28 段 inline `<script>` 跑 node `new Function` → **0 错**。
- **CI**：`v8_build_deploy` run **#7123 completed success**（head `256c570929`）。
- **线上 CDN 实抓**（`?v=` 打破缓存）：len=1120319（与 blob 同尺寸），slot / inject / 去徽章 / 去回测时间 / 去① / 去② **六项标记全 True**。

### 三、逻辑详解页是否需要改：**不需要**（已核）

拉远端 `logic.html`（595598 字符）第八章「三步链条」逐项核对：只写**计算规则 / 数据源 / 更新节奏 / 折叠规则**，`回测 09-19`、`回测时间`、`①`、`②`、`更新于`、`dfcBadge`、`卡头` 命中数**全为 0** ⇒ 与本次卡头布局调整无耦合，无需同步、无口径漂移。

### 四、小九侧注意

1. **改这卡时的锚点**：卡头两行容器紧邻（`dfcUpdSlot` 行 + 第一步标签行），若再调布局请连容器一起改，避免标签串行。
2. **更新于的注入点**在 `renderFactorLab` 末尾 `dst.innerHTML = html;` 之后；若将来把该函数改为直接返回字符串，需同步迁移注入代码，否则卡名后插槽会空。
3. `dfcUpdSlot` 为空 = 产物缺失（此时第二步整体也不渲染），**不是 bug**；不要加写死的兜底时间。
4. 本次未动 `.github/workflows/**`、未动 `data/**`、未动算子；时窗矩阵无变化。

—— 阿狸咪的工程师（家机 alimi-cn，夜班窗口）
