# 补充交接 · 新算法（改动12）历史重放回测 · 2026-09-18 20:30

> 本档是 `HANDOVER_小九给阿狸咪_2026-09-18_1944_改后三件套收口与遗留待办.md` 的**补充**，配套移交。
> 主人 20:10 令：「加，能今晚算出多少算多少，才能趁早知道到底靠不靠谱」。
> 本档只补「新算法重放回测」专项任务与今晚可算边界，不重复 19:44 档已写内容。

---

## 一、截至 20:30 实测现状（非推断）

- `data/FINAL_RECOMMEND_DATA.js` `update_time=2026-09-18 19:36:18`，**无 `alpha_select` 字段**（连续 8 分钟轮询不变）。
  → 说明线上当前是白天旧版（改动12 上线前 runner 缓存代码跑的），**新算法首跑产物尚未生成**。
- `v8_algo_cloud.yml` 注释（L128/L589）实测：`run_algorithms` 前面选股脚本占 90–120min，`final_recommend` 是末步。
  → D 批 20:00 触发，**新产物预计 21:30–22:30 刷出**（基于 workflow 注释实测，非空想）。
- 复核命令（阿狸咪可跑，取代「预计」）：
  ```bash
  # 抓线上产物看 update_time 是否进入 20:xx 且含 alpha_select
  curl -s "https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/data/FINAL_RECOMMEND_DATA.js" | python -c "import sys,json; t=sys.stdin.read(); d=json.loads(t[t.find('{'):t.rfind('}')+1]); print('ut=',d.get('update_time'),'alpha_select=' , ('alpha_select' in d))"
  ```

## 二、小九侧已算完（今晚可交付的「源级证据」）

`raw_data/backtest_expectancy.json` 的 **11 源边际 edge（剔除 β，T+5 / T+10）**，按 edge5 降序：

| 源 | edge5 | edge10 | n_on5 | 判定 |
|---|---|---|---|---|
| sig_jinzuan | +1.179 | +8.581 | 136 | ✅ 正 |
| sig_chan | +0.898 | +4.408 | 289 | ✅ 正 |
| p4_lowvol45 | +0.338 | −2.215 | 67 | △ 异号（edge5+/edge10−） |
| fund | −0.128 | −6.041 | 40 | ❌ 负 |
| p4_lowvol55 | −0.153 | −2.606 | 114 | ❌ 负 |
| sector | −0.525 | −3.435 | 120 | ❌ 负 |
| p4_resid5 | −1.080 | −4.555 | 328 | ❌ 负 |
| p4_resid10 | −1.476 | −3.703 | 257 | ❌ 负 |
| sig_trend | −2.085 | −7.495 | 248 | ❌ 负 |
| sig_jigou | −2.227 | −9.422 | 402 | ❌ 负 |
| quality | −3.266 | −2.733 | 420 | ❌ 负 |

→ 3 正源（jinzuan/chan/lowvol45）/ 8 负源；精选(3正) vs 全量(11等权) 均值差 **+1.58pp**（见 19:44 档 §二）。
→ 这就是「新算法资格门」的数据地基：基础分≤0（无正 edge 信号命中）的票不再吃 qd/60m 加分。

## 三、小九侧「算不了」的部分（诚实交底，非偷懒）

- **离线重放资格门 = 不可行**。核证 `raw_data/history/final_recommend_YYYYMMDD.json`（13 份）+ `top10_daily_*`（60+ 份）：
  样本字段仅含 `source_scores: {"四量终极": 2.29}`（融合后聚合分），**不含四量终极内部 `sig_jinzuan/sig_chan/sig_trend/sig_jigou` 的当日原始命中布尔**。
  → 无法判断历史某票命中的是正 alpha 还是负 alpha 信号，资格门重放缺输入。
- **T+5 实盘战绩 = 物理不可今晚算**：T+5 档要等 5 个交易日，前向跟踪地基已自动每日存 `history/final_recommend_<日期>.json`，**下周五起才有第一个可读 T+5 数字**。
- **完整历史重放 = 归阿狸咪**：需回到 `generate_top10` 输入层（候选池全量 + 各信号当日命中）+ baostock 深度日线（小九被拉黑），小九做不了。

## 四、交阿狸咪：今晚 + 明日做（能跑多少跑多少）

### 今晚（她在线 18:00–次日）
1. **监控 D 批新产物**：用 §一 复核命令，待 `update_time` 进入 20:xx 且含 `alpha_select`，记录 `alpha_select` 里的 `skipped` 数（= 新算法今晚实际拦截的纯负 alpha 票数）。这是「逻辑是否如实运行」的第一手证据。
2. **新旧对照**：新产物刷出后，对比 `raw_data/history/final_recommend_20260918.json`（白天旧版 5 只）的 `code` 集合，列出「旧有 / 新剔 / 新增」，确认资格门剔除了负 alpha 票而非误杀正源。

### 明日（baostock 深度日线）
3. **完整 63 交易日重放**：对 `raw_data/history/top10_daily_*` 覆盖的 63 个交易日，用新融合逻辑（含资格门）在因子层重选 → 对重选出的票算 T+1~T+250 各档收益。
   - ⚠️ **口径差异 caveat**（主人已标记）：历史 `top10_daily` 是「打分 Top20」≠「候选池全形态」，重放覆盖的票池形态与当日实盘不同 → 结论**只作参考、标注为「重放近似」、不得与真实前向跟踪战绩混用**。
   - ⚠️ **预定判定标准**（防事后挑数，同 19:44 档 §三）：两档同号且 edge5>0 = 维持资格；异号 = 观察项；结论只影响「源资格」、**不调权重**（踩红线「绝不按短期调权重」）。

### 下周五起
4. **实盘前向跟踪**：`history/final_recommend_<日期>.json` 逐日累积，T+5 档最先成熟，逐档补齐真实战绩。

## 五、诚实结论（给主人）

- **今晚能算的** = 11 源边际 edge 源级证据（已出，稳定）+ 待 D 批刷出后的 `alpha_select` 运行日志（21:30+）。
- **今晚算不出的** = 新算法「选的票未来涨不涨」（T+5 下周五）+ 历史重放战绩（归阿狸咪，明日）。
- 「靠不靠谱」的最终答案要等 T+5 前向跟踪成熟，但「逻辑是否如实运行」今晚 D 批刷出即可验证。
