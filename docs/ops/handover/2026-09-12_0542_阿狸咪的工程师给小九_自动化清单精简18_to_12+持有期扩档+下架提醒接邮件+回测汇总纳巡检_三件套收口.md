# 阿狸咪的工程师 → 小九 · 自动化清单精简 + 主人拍板 1/2/4 项收口

**时间**：2026-09-12 05:42（北京）
**发件**：阿狸咪的工程师（家里机 alimi-cn）
**收件**：小九（单位机 lemoncat-cn）
**性质**：常规交接（非 URGENT）
**主人指令原文**：`3A，124你给建议，按你建议的做`

---

## 0. 一句话摘要

主人拍板第 **1、2、4** 项（第 3 项保持 A 不动）。本轮共推 **4 个 commit**：
三个功能修复（回测汇总纳巡检 / 持有期扩档 / 下架提醒接邮件）+ 一个双页对齐。
**阿狸咪相关 ACTIVE 由 18 → 12（-6）**，并修掉两处**守卫与双机分时铁律冲突的真隐患**。
本交接单**不写自己的 sha**（避免自指循环）。

---

## 1. 本轮 commit 清单（已在远端 main）

| # | commit | 内容 |
|---|---|---|
| 1 | `40ce2a06c` | **第 2 项**：`BACKTEST_ALL_ALGOS` 纳入闸门 READY_SPEC + 巡检 CARD_DEFS 双登记 |
| 2 | `e63aa9420` | **第 1 项①**：持有期扩档 5/10/20/30/45/60/75/90 + 4 脚本数据窗口同步放宽 |
| 3 | `2cc6dd770` | **第 1 项②**：低绩效策略下架提醒接邮件主动触达（新增 `scripts/v8_delist_advice.py`） |
| 4 | `912e56fcd` | **三件套②**：上述变更写入运维页 + 逻辑详解页 |

---

## 2. 第 2 项 —— BACKTEST_ALL_ALGOS 纳巡检（`40ce2a06c`）

### 根因：三重静默
新产物「闸门 READY_SPEC + 巡检 CARD_DEFS + 推送 walk_extra」**三处任一未登记就静默**。
`BACKTEST_ALL_ALGOS` 此前三处**全缺** ⇒ 脚本在 STAGES 里跑、产物谁都不管、不产出也不告警。

### 改动
- `v8_health_check.py`：新增 CARD_DEFS 条目（`raw_file: backtest_all_algos.json`），
  刻意**不进** `_LOW_FREQ_FILES`；`key_fields` 取 `["rows","coverage"]`
  （**不取 `update_time`** —— 它是通用时戳字段，列进去等于没校验）。
- `.github/scripts/v8_stage_gate.py`：E 批 `items` 加 `data/BACKTEST_ALL_ALGOS.js`，
  **`need` 由 1 抬到 2**。

### 🔴 最易踩的坑：need 语义
`need=1` 是「**任一鲜活即就绪**」。**只加 items 不抬 need 等于没加**（新项不参与判定）。
必须同时抬 `need` 才让该项成为**必新项**。

### 实测对照（真实产物，数据日 09-11）
| 口径 | 结果 | 判定 |
|---|---|---|
| 旧（1 项 need=1） | `ready=True 1/2` | ❌ **漏判** |
| 新（3 项 need=2，产物缺） | `ready=False 1/3` | ✅ MISS 可见，正确触发重跑 |
| 新（3 项 need=2，产物齐） | `ready=True 2/3` | ✅ 正常日不误报 |

---

## 3. 第 1 项① —— 持有期扩档（`e63aa9420`）

### 主人要求
「可以从近到远，从 5 天开始、10 天、20 天、30 天、45 天、60 天、75 天、90 天这样写出来慢慢跟踪」

### 关键前提：档位不能超过数据窗口
**逐脚本实测**原窗口约束后发现 **4 个脚本窗口全不够** ——
只改档位会**静默出空档**（不是算出 0，是**根本没算**）。故必须同步放宽窗口。

| 脚本 | 原窗口 | 改后 |
|---|---|---|
| `backtest_tdx.py` | `tdx_kline(count=60)` 硬上限 | `TDX_BARS = 260` |
| `backtest_comprehensive.py` | ±35 天 ≈ 24 交易日 | `LOOKBACK=30 / LOOKAHEAD=150` |
| `strategy_four_volume.py` | `max(DAILY_BARS, years*250+60)` | `max(DAILY_BARS, years*250+120, 480)` |
| `v8/backtest_crds.py` | `lookahead_days=35` | `LOOKAHEAD_DAYS = 150` |

### 新增双口径（主人选的「改止损逻辑」）
`backtest_comprehensive.py` 的每档持有期**并列输出两项**：
- `ret` —— **原口径**：止损/止盈先触发先执行（**历史语义一字未动**）
- `ret_hold` —— **新口径**：忽略中途触发，严格持有到第 hp 个交易日收盘

⇒ 既不破坏历史可比性，又能单独观察「纯粹持有 N 日」的真实收益。

### 同源铁律
4 个回测脚本共引 `HOLD_LADDER`，**各写一套必然漂移**。
实测 4 脚本 `HOLD_LADDER` 已全等，档位统一 `[1, 3, 5, 10, 20, 30, 45, 60, 75, 90]`。

---

## 4. 第 1 项② —— 下架提醒接邮件（`2cc6dd770`）

### 设计：职责分离
新增**独立脚本** `scripts/v8_delist_advice.py`（374 行），由 `gen_backtest_all_algos.py`
链尾以 `subprocess`（`check=False` + `timeout=120` + try/except + 仍 `return 0`）调用。
⇒ **聚合器不依赖 SMTP**，发信失败绝不影响回测产物。

### 闸门（主人拍板：5 日胜率 < 40% 且累计 ≥ 20 次）
| 常量 | 值 | 说明 |
|---|---|---|
| `DELIST_WIN_RATE` | 40.0 | **窄口径**红线（页面展示用 45，两者刻意不同） |
| `DELIST_MIN_N` | 20 | 样本门槛 |
| `RESEND_SUPPRESS_DAYS` | 7 | 重发抑制期 |
| `RESEND_WORSEN_PT` | 5.0 | 恶化 ≥5pt 破例重发 |

### 五条路径实测
| 场景 | 结果 |
|---|---|
| run1 首次判定 | ✅ `advice: [四量终极, 相对强度]` |
| run2 同日重跑 | ✅ 全抑制 |
| run3 胜率 38.2→30（恶化 8.2pt） | ✅ 破例重发 |
| run4 38.2→36（2.2pt < 5） | ✅ 继续抑制 |
| run5 `--force` | ✅ 无视抑制 |

**端到端**：子进程调用 `returncode=0`，`数据日=2026-09-11 达标建议=2 观察=2 抑制=0`。

### 🔴 写脚本时踩到的真事故（供你复用时警惕）
1. **抑制永不生效**：`judge()` 内部 `load_state()`，`main()` 又 `save_state(load_state())`
   ⇒ 后者**从磁盘重读、把内存更新整个丢掉**。
   修：`judge()` 改为「纯判定 + **返回**新 state」，落盘交给 `main()`；
   且**只有真发信成功才落盘**（否则发送失败会被标成「已发」而静默 7 天）。
2. **邮件样本显示 `1415.0`** → `_fmt()` 改为整数不带小数点。
3. **触发原因引用产物宽口径 45%**，与正文「门槛 < 40%」自相矛盾
   → 新增 `_reasons()` **按本脚本窄口径重算**。

---

## 5. 第 4 项 —— 自动化清单精简（本轮重点）

### 现状实测
面板共 41 项，**阿狸咪相关 ACTIVE = 18 个**，远超双机分时铁律应有数量。

### 动作 ①：备机 runner 巡检三变体合并
| ID | 名称 | rrule |
|---|---|---|
| `351824ce` | 盘中每小时 | `HOURLY;INTERVAL=1` |
| `13900bec` | 非盘中每 2h | `HOURLY;INTERVAL=2` |
| `b705d947` | 周末首日每 2h | `HOURLY;INTERVAL=2` |

**实测三者任务正文逐字相同，只有时间窗守卫不同**；且 `INTERVAL=2` 的守卫表
由**偶数点**构成，却把**奇数点**也写进去了（`b705d947`/`13900bec`）
⇒ 那些奇数点条目是**死条目**（永远触发不到）。

⇒ 合并为 `351824ce`「全时段·工作日 7-21 点 / 周六偶数点 / 周日不跑」，另两个 **PAUSED**。

### 动作 ②：`9119c1d4` 僵尸停用 🔴
「今夜算法链收口值守」的 `validUntil = 2026-09-11`，**今天 09-12 已过期**，
但面板**仍显示 ACTIVE**（僵尸态）。它**持有 dispatch 权** ——
是唯一派发源 `da2072fc` 之外的**第二个派发入口**。⇒ 正式 **PAUSED**。
（其自身 prompt 也写着「本自动化已完成使命，建议主人停用」——现在是兑现。）

### 动作 ③：`50eda171` 补时窗守卫
「暴风盯梢」原**无守卫、24h 每小时 :05** 触发。但盘后链 **16:40 才唤醒、02:00 后已收口**
⇒ **02:00-15:59 纯空转**（历史教训：本机曾有 2 个 python 空挂 5.4h 被主人甩截图）。
⇒ 补守卫「**16:00-次日 02:00**」。

### 动作 ④：`automation-1788058041833` 守卫修正 🔴（**分时铁律冲突**）
「自愈监控闭环」守卫原为 **`[8..18]`**。
但它调用的 `self_heal_monitor.py --heal` 在缺「外资研投」源时会执行
**`git push origin main`**（实测 L126 `_push_candidate()` / L252、L261 两条 push）。

**`[8..18]` 完全落在小九独家时段 07:45-17:45 之内** ⇒ 一旦白天源缺失，
本机就会在 9:00-17:45 自行 push main，**违反双机分时铁律**，且会与你的盘中抓取抢资源。

⇒ 收紧为「**工作日 18-22 点 + 07:00-07:44**」。

> 现状实测：`raw_data/candidate.json` 含「外资研投」=**True**、
> `update_time=2026-09-11 22:38:42` ⇒ 该分支**当前短路不推**。
> 属**休眠风险**，非正在发生 —— 但守卫必须收紧以防复现。

### 动作 ⑤：交接读取 4 → 2
| ID | 原时点 | 处置 |
|---|---|---|
| `fe542347` | 10:00 | **PAUSED** |
| `01e8fe60` | 14:30 | **PAUSED** |
| `e6c63615` | 18:30 | **改 20:30** |
| `automation-1783523003845` | 19:30 | 保留不动 |

**依据（实测 09-11 全天 21 份交接的生成时刻直方图）**：
```
20:00-22:00 = 11 份 ███████████
22:00-24:00 =  6 份 ██████
18:30 之前   =  4 份 █
```
⇒ **81% 的交接写在 20:00 之后**，18:30 跑只能看到当日不到 1/5。
故汇总时点顺延至 **20:30**；19:30 那项保留（独有 `sync_from_origin.py` +
`HANDOVER_LOG.jsonl` 职责，与纯读交接不同）。

### 🔴 一项重要更正（我先前建议有误）
我原先把「算法链只读监控 4 个重叠」列为可关闭项，**实测后撤回**：
| 任务 | 独有职责 |
|---|---|
| `da2072fc` | **唯一派发源** —— 不可动 |
| `50eda171` 暴风盯梢 | commit 密度互踢检测 / 半截 JSON 检测 / 三红灯 |
| `90b7ea6e` 夜班监控 | 失败步根因分类 + 兜底补派 + 交接读取 |

**两者只有「链在跑吗」一句重叠，主体互不重叠** ⇒ **不合并、不关闭**，
只给 `50eda171` 补了时窗守卫（动作③）。

### 结果
**阿狸咪相关 ACTIVE：18 → 12（-6）**

---

## 6. 三件套② —— 双页对齐（`912e56fcd`）

变更写入两页：
- **运维页** `index.html#sec-op`「⚙️ 自动化任务」表新增一行完整说明
- **逻辑详解页** `logic.html`「时段划分/派发源」段落并入

### 自检五项实测（全部通过对远端内容校验）
| # | 项 | 结果 |
|---|---|---|
| 1 | py_compile | — （本 commit 无 .py 改动） |
| 2 | inline script 语法 | `logic.html` 6 段 / `index.html` 24 段 → **语法失败 0** |
| 3 | `align_logic_ops.py` | **EXIT 0**（关键 workflow 均已文档化，无过期引用，名单无死登记） |
| 4 | 标签配平 | 两页 **div/table/tr/td/th/span/tbody/thead 净差全 0** |
| 5 | `git diff --numstat` | `index.html` **+1 -0**；`logic.html` **+1 -1** → **无整文件重写** |
| — | 行尾守恒 | `index.html` CRLF=0（LF ✅）；`logic.html` CRLF=8690（CRLF ✅） |

---

## 7. 三件套① —— 双机/云端时窗对齐结论

- **唯一派发源不变**：`da2072fc`（16:00-08:59 每小时 :10，按闸门判定，不带 stage）。
- **本地备机 runner 巡检 vs 云端 `runner_health_alert.yml`**：**不冲突** ——
  云端查 **GitHub runner 注册状态**，本地查**本机 `Runner.Listener` 进程**，
  一处平台侧、一处本机进程侧。
- **夜窗上界 08:59 三处同源保持不变**：闸门 `_NIGHT_CUT=9` /
  `run_algorithms.py::_NEXT_DAY_CUTOFF_HOUR` / `utils/time_gate.py::_NIGHT_CUT_HOUR` /
  派发源守卫 —— 本机守卫（`da2072fc` 写 16:00-08:59）与闸门一致，**未动**。

---

## 8. 需要你做 / 需你确认的事

1. **确认 `50eda171` / `90b7ea6e` 不合并**（见 §5 更正）—— 你的只读监控仍有独有价值。
2. **`9119c1d4` 已过期停用**，若你还依赖它第 4 步的「瘦身复检」，请告知由谁接管
   （我理解该职责已并入云端，故停用；若不对请回执）。
3. **主站「策略回测页」** 新增的 `BACKTEST_ALL_ALGOS` 卡：产物要等 **E 批末位**
   `gen_backtest_all_algos.py` 跑完才生成，**首次产出前页面应显示「累积中/—」，
   不得用 0 冒充**（这是红线，请你复核前端是否合规）。
4. **遗留待办（非本轮）**：`v8_algo_cloud 19:15` 旧时点残留 20+ 处（须逐处判别，
   叙述历史事件的**改了反而失真**）；你 A1（60m 双页写入）的锚点
   `id="opsCandidateFreshBody"` 远端不存在 ⇒ 等你推送后我再补。

---

## 9. 铁律复述（本轮遵守情况）

- ✅ 未使用 `git add -A`；全程 FETCH_HEAD 派生 + 仓库外暂存 + 临时索引 `commit-tree` 直推
- ✅ 未改工作树磁盘（抗坚果云）；补丁脚本全部放仓库外
- ✅ 行尾守恒（LF/CRLF 二进制校验）
- ✅ 无假成功：所有结论均带实测命令输出或时间戳举证；未查到的一律写「未查到」
- ✅ 署名 **阿狸咪的工程师**（与小九区分）

---

*本交接单由「阿狸咪的工程师」生成 · 主体入库 `912e56fcd`；本行由后续 commit 补写*
