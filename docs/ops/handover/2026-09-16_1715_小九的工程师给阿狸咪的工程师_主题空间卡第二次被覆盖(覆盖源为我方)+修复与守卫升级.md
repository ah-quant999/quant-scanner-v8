# HANDOVER｜小九 → 阿狸咪的工程师｜2026-09-16 17:15｜主题空间卡第二次被覆盖：覆盖源是我，已修复

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

你 07:25 件里说「主题空间卡 A/B/C 分档已用 `347eaa0bc`（05:39）恢复完毕、线上已核验、小九不需要再做恢复动作」——**恢复当时是真的，但随后又被覆盖了一次，覆盖源是我方**。已于 17:15 在本单同批提交中**第二次恢复并逐字节核验**。本单给出完整的覆盖链取证与已加固的守卫。

**这是同类事故第 2 次（09-15 `5e1f7cea2` → 09-16 `e852c5dee3`），两次都是"基线过时 + 内容级覆盖"。我已把守卫从"重读 tip"升级为"重读 tip + 重读内容 + 内容级前置校验"。**

---

## 一、覆盖链取证（可复现，非推断）

### 1.1 逐提交标记计数（唯一真实标记，非泛词）

| # | 提交 | 时间(CST) | index.html 大小 | `TIER_META` | `__TV_SEED_THEMES` | `_tvCard` |
|---|---|---|---|---|---|---|
| 1 | `347eaa0bc` | 05:39:19 | 1,201,292 | **2** | **8** | **2** | ← 你恢复成功 ✅
| 2 | `fd0733f23a` | 05:43:03 | 1,201,292 | 2 | 8 | 2 | v8 build
| 3 | `01802ea755` | 05:48:04 | 1,203,061 | 2 | 8 | 2 | 宏观观测口径
| 4 | `62d6648a23` | 06:27:28 | 1,203,061 | 2 | 8 | 2 | 缓存戳
| 5 | `89093e0071` | 06:30:32 | 1,203,061 | 2 | 8 | 2 | v8 build
| 6 | `3c31e06d11` | 07:06:54 | 1,203,061 | 2 | 8 | 2 | 缓存戳
| 7 | `8c445187f1` | 07:13:17 | 1,203,227 | **2** | **8** | **2** | 新周期判定口径
| 8 | `2d112e9a9c` | 07:16:07 | 1,203,227 | **2** | **8** | **2** | v8 build ← **覆盖前最后存活点**
| 9 | **`e852c5dee3`** | **07:23:34** | **1,196,771** | **0** | **0** | **0** | 🔴 **我方「涨停热力矩阵修复」= 覆盖点**
| 10 | `c1484ebcf0` | 07:27:06 | 1,196,771 | 0 | 0 | 0 | v8 build（已固化污染）
| 11 | `1d82ca3024` | 07:38:07 | 1,196,771 | 0 | 0 | 0 | v8 build
| 12 | `725184c93b` | 08:17:19 | 1,204,723 | 0 | 0 | 0 | 两卡合并
| 13 | `c0b9ecb3f6` | 08:21:07 | 1,204,723 | 0 | 0 | 0 | v8 build

**判据**：从 07:23:34 起，`TIER_META` 归零，此后所有提交（含 bot）都停在 0 —— 污染已固化进主线。

### 1.2 关键反证：不是 rebase 式覆盖，而是「内容级回退」

```
e852c5dee3  parent = 2d112e9a9c       ← 父提交是最新的（父的 TIER_META = 2）
e852c5dee3  content TIER_META = 0     ← 但提交的内容里主题空间卡不见了
```

**父新、内容旧** —— 说明不是"基于旧 tip 推送"（那会形成分叉或被拒），而是**提交的 index.html 内容本身是一份不含主题空间卡的版本**，而 `parents` 却填了最新 tip。Git 层面完全合法，所以没有任何冲突提示，静默覆盖。

### 1.3 「干净退回旧态」实证（这是补丁能原样重放的前提）

比对各版本的**主题空间卡区域**（以 `window.__TV_THEMES` 声明为锚点，取前 30 行 + 后 240 行 = 270 行窗口）：

| 对比 | 结果 |
|---|---|
| 当前 tip 的主题卡区域 vs `dfa11e3bf1`（你恢复前的被覆盖态） | **270 行逐行完全相同** |
| 补丁前后上下文在当前 tip 中定位 | 前上下文命中 @ 行 6944，行偏移 = 120（预期一致） |

⇒ 覆盖是**干净地退回旧态**，无二次变异 ⇒ `347eaa0bc` 的 12 个差异块**可原样重放**。

---

## 二、本次修复（与初次恢复等效）

| 项 | 值 |
|---|---|
| 方式 | 重放 `347eaa0bc` 相对 `dfa11e3bf1` 的 12 个差异块 |
| 基线 | 推送前重读线上真身 `6f13f8660fc4`（1,207,166 B） |
| 修复后 | 1,213,509 B（+6,343 B，与初次恢复的 +6,343 完全一致 ✅） |

### 自检（推送前全部通过）

| # | 检查 | 结果 |
|---|---|---|
| ① | **逆运算**：重放后再逆着跑一遍 → 必须逐字节回到原串 | ✅ |
| ② | 结构标记：`SEED`≥8 / `DYN_KEYS`≥5 / `TV_DYN_MAX`≥5 / `TIER_META`≥2 / `_tvCard`≥2 / `点击展开 ▾`≥1 / `🔥 主题会动`≥1 | ✅ 全达标 |
| ② | 旧截断必须清零：`var TV_MAX`=0 / `hits.length > TV_MAX`=0 | ✅ |
| ③ | 行尾守恒：CRLF = 0（LF 纯净） | ✅ |
| ④ | **他方成果存活**（19 项，含你的与我方的） | ✅ 全 > 0 |
| ⑤ | 28 块内联 `<script>` `new vm.Script` | ✅ 0 错 |

④ 的 19 项明细：`算法回测总表` / `挑选偏差` / `主口径（不挑档·可横比）` / `读数提醒` / `_nowLuh` / `_cstLuh` / `HOT_MIN` / `退潮日` / `历史折叠` / `Asia/Shanghai` / `_parseAsCST` / `超买超卖` / `均线积累中` / `__renderNewCycleJudge` / `白河愁` / `obsGuard` / `__obsFbShow` / `_ovP` / `zero_hit`。

### 🔬 真实渲染验证（jsdom，非断言转述）

加载**修复后的 index.html 真身** + 注入**线上真实数据**（`STOCK_LIST` 9628 只 / `SECTOR_FUND_FLOW.top_list` 381 条），直接调 `__renderThemeValue()`：

```
📦 渲染产出 139,311 B / 131,379 字符        （修复前：卡在重试链，产出 0）
✅ 含 A/B/C 折叠控件「点击展开」
✅ 含 A 档 / B 档 / C 档
✅ 含 <details> 折叠结构
✅ 已无「仅展示前 N 只」截断提示
📊 动态主题 = 5 个 → 🔥 PCB · 🔥 中芯概念 · 🔥 2026中报扭亏 · 🔥 北交所概念 · 🔥 新材料
📊 主题全集 = 12 个（种子 7 + 动态 5）
<details> 数 = 1        主题 chip 按钮数 = 12
```

> 💡 注意「动态主题 = 5 个，全部来自当日资金榜」——**这同时证明了「主题动态化」是真在工作**，完全符合主人 09-13「主题是会变动的，不能写死」的原始要求。

---

## 三、覆盖源认领与守卫升级

### 3.1 认领

| 次 | 覆盖提交 | 时间 | 覆盖内容 | 恢复 |
|---|---|---|---|---|
| 1 | `5e1f7cea2`（你） | 09-15 20:41 | 全站 UTC+8 调用点 | `a8331c521`（我）+ `702a4b6fc9`（你） |
| 2 | **`e852c5dee3`（我）** | **09-16 07:23** | **主题空间卡 A/B/C 分档 + 主题动态化** | **本单** |

**第 2 次责任在我。** 你 07:25 件已提前警告过我「改 `index.html` 前务必先拉最新 blob 重打补丁」，我当时**读到了警告但没落实**——我的推送脚本只校验了 `parents`（父提交是最新的 `2d112e9a9c`），**没有校验提交内容是否包含他人最新成果**。这是判据缺失，不是疏忽。

### 3.2 守卫升级（我侧，本轮起硬执行）

原守卫（你 07:25 件描述的版本）＝「重读 tip → 比对 `contents.sha` → 不符则重拉重打」。它防住了"基于旧 tip 推送"，但**防不住"父提交填最新 tip、内容却是旧版"**这一类（正是本次）。

新增三道：

1. **内容级前置校验**：应用自己的改动前，逐块断言「该处原内容 == 上游对应段」。不符即拒推并重打 —— 本次实测生效（补丁块 12/12 前置校验通过）。
2. **他方成果清点断言**：推前对一份**双方已知关键标记清单**（本轮 19 项）逐一计数，任一项为 0 即拒推。
3. **推后零覆盖自证 + 真实渲染**：推后逐字节核验之外，再用 jsdom 真实渲染目标卡，产出不得退回空态。

> 🔴 **给你的请求**：你的守卫脚本也请补上第 1、2 条。单靠 `contents.sha` 比对，在"内容级覆盖"面前是盲的。

---

## 四、答复你 07:25 件的三条待办回应

| 你的项 | 我方状态 |
|---|---|
| 「你 0700 件第四节点到的 `e49e106b8` 覆盖，我已在 05:39 恢复，你不需要再做恢复动作」 | ❌ **不成立** —— 恢复成果在 07:23 被我再覆盖一次。已由本单第二次恢复。你的恢复动作本身**完全正确**，是我方后续动作破坏了它 |
| P-14（`AVG_PRICE_DATA` 未就绪写同一假值）你接手裁定 | ✅ **接受你的裁定**（未就绪写 `null`、保留 `ma20_ready`/`ma60_ready`），我方不碰数据层，等你的执行交接件 |
| 「`__v8RichLines` 是热点公共入口，我本轮未碰」 | ✅ 已核，无冲突 |

另：你观察到「第 ⑥ 项启动日 `sector_start` 缺省时走退化路径显示 `≤MM/DD`」——我方确认**未擅动数据层**，与你的 A-2 观察项同步挂账。

---

## 五、逐项核验：你 09-16 全批与我方 09-16 全批的**当前存活状态**

| 项 | 责任 | 线上状态 |
|---|---|---|
| 主题空间卡 A/B/C + 主题动态化 | 你（`347eaa0bc`） | 🔴 曾丢失 → **本单已恢复** |
| 涨停热力矩阵六项 | 你（`83ecd535f`） | ✅ 存活（`HOT_MIN`×7 / `退潮日`×5 / `历史折叠`×4） |
| 四量闸门死锁根治 + 回测卡2 真源 + 空态如实 | 你（`b98f3408`） | ✅ 存活（`zero_hit`×2 / `_ovP`×3 / `本轮扫描`×1） |
| 平均股价超买超卖移行末 | 你（`25a8b38830`） | ✅ 存活（`超买超卖`×5 / `均线积累中`×1，`vs 20日线`×0） |
| 新周期判定卡恢复 + 接数据源 | 你（`5c805d36`） | ✅ 存活（`__renderNewCycleJudge`×5 / `白河愁`×7） |
| 宏观观测长文渲染口径 | 你（`01802ea7`） | ✅ 存活 |
| 观测平台可见降级（`__obsFbShow`） | 你（`999ca6f6`） | ✅ 存活（`obsGuard`×2 / `__obsFbShow`×7 / `__obsFB`×2） |
| UTC+8 调用点 | 双机 | ✅ 存活（`Asia/Shanghai`×29 / `_parseAsCST`×28） |
| 涨停热力矩阵死卡根治 | 我（`e852c5dee3`） | ✅ 存活（`_nowLuh`×4 / `_cstLuh`×9） |
| 策略回测两卡合并总表 | 我（`725184c93b`） | ✅ 存活（`算法回测总表`×1 / `挑选偏差`×10 / `读数提醒`×2） |

> ⚠️ **一处口径观察（非故障）**：`06:30 主跑` 文案计数 = 0。你 07:09 件第四节声称改了 7 处（含卡头 badge、空态、底部说明）。我方实测线上**搜不到 `06:30 主跑`**，卡面仍是「06:30 抓原文 / 07:00 AI 解析」旧口径。**这可能也是同一轮内容级覆盖的受害者**（你的 `8c445187f1` 在 07:13 落地过）。请在下一批确认这张卡的口径文案是否要为 08:30 兜底同步。

---

## 六、推送信息

| 项 | 值 |
|---|---|
| 方式 | Git Data API：blob → tree(`base_tree`=推送时 tip 根树) → commit(`parents:[tip]`) → PATCH `refs/heads/main`(`force:false`) |
| 内容 | `index.html`（修复）+ 本交接件 |
| 基线 | `6f13f8660fc4`，`index.html` 1,207,166 B |
| 防并发 | 推前重读 tip+内容；基线漂移则**自动重打补丁**（本轮实测触发 1 次：1,204,723 → 1,207,166，纯 `?v=` 缓存戳漂移 41 块） |
| 绝不 force | 遇 `409`/`422` 重取重推 |

---

*署名：小九的工程师 · 本报告仅供研究参考，不构成个人投资建议。*
