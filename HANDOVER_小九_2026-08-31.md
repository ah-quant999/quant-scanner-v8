# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 更新于 2026-08-31 12:03 自动更新（automation-1785719698947，每日 12:00/18:00）
> 交接方向：**小九（白天 07:45–17:45 独家）→ 阿狸咪（夜间/周末 18:00 起接管）**
> 数据来源：当日工作日志 `E:\workspace\stock-scanner\.workbuddy\memory\2026-08-31.md` + `git log origin/main` 真实 SHA + 线上 Pages 实时拉取。**结论均实测，无推测。**
> ⚠️ 多实例并行：今日 12:00 档有多次并行生成，本档为 12:03 最终 superset（合并 11:35 / 12:00 各并行稿 + 12:03 实核）。

## 一、背景
- 日期：2026-08-31（周一，交易日）。小九时段独家运行，阿狸咪 18:00 起接管夜间/周末部署。
- 本档为 12:00 定时交接，汇总当日截至运行时刻的小九累积改动，供阿狸咪开机即对齐状态。
- ⚠️ **环境铁律（连续多日确认）**：本自动化配置 cwd `E:/workspace/quant-scanner-v8` 仅是构建产物壳（**非 git 仓库**，无 `.git`/`raw_data`/脚本）。**真实 v8 源码仓库在 `C:/Users/Administrator/qs8-tmp`**；盘中实时卡另在 `/e/tmp/v8_live`。本档在 qs8-tmp 实跑并推 origin/main，零干扰其他文件。
- 关联早档：今日 08:30 另有一份「盘前档」（覆盖阿狸咪夜间成果验收），其头号结论——`update_v8.py` 时间门控致命化耦合已被阿狸咪修复（`69ab8b5a1`，07:20 构建 success 实证），盘前/盘中部署不再被 00:00–15:30 打断——已闭环，本档不再赘述。

## 二、今日已完成改动（含真实 commit SHA + 影响）

### 01:00 持仓云端同步（automation-1787210164001）
- 持仓 2 只：托伦斯 sz301583 100股@125.00（-4.62%）、广钢气体 sh688548 1000股@34.33（-4.82%）；账户总资产 993160.39，总盈亏 -6839.60（-0.68%）。
- 写入 `data/PORTFOLIO.js`（updated_at 01:01，reason=心跳58分钟未更新）。非交易时段仅本地更新，未推 main。
- 良性残留：`sync_westock_portfolio.py` 仍尝试 load 已删除的 `data/PORTFOLIO_COST.js`（08-30 轻量化有意删除），仅噪声告警。

### 08:37–08:48 盘前全量一条龙（automation-1785888730897，补跑）
- 背景：08:15 定时未落地（`_heartbeat.log` 无 08-31 v8_full 记录），主人 08:37 追问；核查云端 08:07 cn fetch、08:21 build、08:22 已部署一次 → 本机为补跑+二次刷新。
- **ff-only 阻断解法（可复用）**：qs8-tmp 落后 94 提交 + 5 modified + 1 untracked 阻断 merge。先备份 8 文件至 `C:/Users/Administrator/qs8-backup-0831/`，`git checkout --` 丢弃 4 个看门狗产物 + `rm` untracked，`PORTFOLIO.js` 保留 → ff-only 到 `fdeaed957`。**禁 stash（会收走正在写的 data/*.json）**。
- 结果：fetch premarket 15/15 ok 0 fail；update_v8 72/跳过 45；gen_strong_breakout 时间门未就绪（设计内）、momentum_common_filter 本地禁跑（铁律，退出码2）、algo_backtest_compare 跑通、track_h_auto_buy 写入（累计样本仍 0，待 T+1）；deploy 成功。
- 线上核实：index.html 200（08:47:21）；MARKET_PATH_PROBABILITY.js 含 `current`；HEALTH_CHECK 08:44:35（ok100/warn3/fail5/total108）。

### 08:50–08:58 主人授权「全部一劳永逸式修复」（cwd 修正 + 两处调度补位）
- ✅ automation cwd 修正：`1785888730897` 的 cwd `E:/workspace/quant-scanner-v8` → `C:/Users/Administrator/qs8-tmp`（明日 08:15 不再空转）。
- ✅ **ETF_SUBSCRIPTION_EM 补调度**：`.github/workflows/v8_cn_fetch_cloud.yml` 新增 step，每个交易日 premarket/post_close 及周末 all 跑 `scripts/fetch_etf_subscription_em.py` → `data/ETF_SUBSCRIPTION_EM.js`。
- ✅ **LHB_HISTORY 周末/云端修复**：刷新+回填 LHB 触发条件由仅 `post_close` 扩展为 `post_close || all`。
- 📌 commit **`19ede867c`** `fix(v8/workflow): ETF_SUBSCRIPTION_EM 补调度 + LHB_HISTORY 周末全量运行`（仅 workflow 1 文件，14+/4-）。
- 🔴 **遗留风险**：`scripts/fetch_etf_subscription_em.py` 数据质量缺陷——调用 `ak.fund_etf_fund_info_em()` 返回列与期望列不符，脚本写 placeholder（5 类净申购均为 0）。调度已补，但数据仍是占位，需换 akshare 接口或修脚本。

### 09:15–09:38 智能触发兜底（automation-1785898357370）实际跑通
- cwd 由壳改回 qs8-tmp（与 1785888730897 一致）。
- 守卫 STALE: north_fund.json（北向 71min>60）→ cloud_fetch_v8 15/15 ok → 提交 17 个 raw_data/*.json（commit `1683db810`）→ push（`1c5335905..1683db810`）→ 守卫 FRESH(5/0)。
- ⚠️ 复盘：pull --rebase --autostash 时 runner_status.json 冲突卡死 rebase；曾误 `reset --soft origin/main` 暂存 98 文件（含红线 `data/HEALTH_CHECK.js`）。已纠正：mixed reset 卸栈 → 仅 `git add` 原 17 个 raw_data 文件路径。对含红线仓库禁用 `git add raw_data/`，须显式列文件路径。

### 10:19 盘中漏轮兜底（automation-1787108824799）— no-op（数据新鲜）
- 真实 live 仓库 `/e/tmp/v8_live`。7 张盘中卡 update_time：最旧 sector_fund_flow 09:45:56（≈33.6 分）、最新鲜 overseas_markets 10:02:08（≈17.4 分），max<35 阈值 → 非漏轮。未重抓、未提交。

### 11:12 runner 本地守护（automation-1786696074524）— OK，无自愈
- guard 在 qs8-tmp 执行：listener=True/worker=False（空闲）、服务 NOT_FOUND（run.cmd 进程模式，预期）、self-hosted 连续失败 0 / checkout 失败 0，Contents API 推 runner_status.json HTTP 200，status=ok，actions_taken=[]。
- 🔴 **qs8-tmp 索引残留 40 个 UU 未合并条目**（stale-index landmine）：11:29 救急时仅 add 42 目标文件、其余 UU 未清 → 12:03 本档再次发现 40 UU + 19 M + 4 untracked。**已 `git reset --hard origin/main` 对齐新鲜态收口（详见风险①）**。

### 11:29 盘中漏轮兜底（automation-1787108824799）— ✅ 救急成功并推送
- 检查点 11:15。7 卡 update_time 卡 10:10–10:24（age 51–65 分>35）→ 云端 10:30/11:00 两轮确被跳过。
- cloud_fetch_v8 --category intraday 15/15 ok；update_v8 更新 16 个 data/*.js + index.html 缓存戳。
- `git pull --rebase --autostash origin main`：rebase 成功，autostash 弹出 41 冲突 → `git checkout --theirs` 保留本机数据/index → stash drop。
- 源码隔离：暂存集仅 data/(19)+raw_data/(22)+index.html(1)=42；`cloud_fetch_v8.py`/`v8_health_check.py` 两源码改动 `restore --staged` 排除（未提交、未触碰）。
- 📌 commit **`fdfaa9a2a`** `fix: 云端漏轮→本机救急重抓盘中7卡(11:29)` → push（`55de5f9..fdfaa9a2a`），CI 部署。7 卡新鲜至 11:17–11:20。

### 一劳永逸源码修复（主人授权，随今日 commit 推仓）
- `1ef2a28db` 健康自检日历感知+宏观显示+平均股价双写根治(防覆盖)：一次性消除 5 个误报；宏观不再「11天前」；封死全A覆盖 880003 真指数。
- `de9d88b2a`/`4e6bcd660`/`7a32c81a4`/`5a5c5f5ae` intraday 期货/平均股价 + cron 优化 + 卡片迁移（F1/F3 同类源修复）。
- `6957691b6` F1：盘前不再清空连板天梯/龙头榜（情绪周期卡晨间「无数据」根治）；`c0b7bb24c` F3：链尾闸门 16:30 前 warn-only（消除盘中派发假阳性报红）。

## 三、当前系统状态（截至 2026-08-31 12:03 实核）

| 项目 | 状态 |
|---|---|
| 站点（GitHub Pages） | ✅ HTTP 200，Last-Modified **2026-08-31 12:07 CST**（= main `333e2f651` 部署，CI 持续追加） |
| HEALTH_CHECK | ✅ update_time **11:57:04**，overall=**ok**，ok **108** / warn **1** / fail **0** / total **109**。唯一 warn = `AVG_PRICE_DATA`（旧全A僵尸，今夜 post_close CI 自愈） |
| RUNNER_STATUS | ✅ status=ok（self-hosted 连续失败 0 / checkout 失败 0） |
| 持仓 PORTFOLIO | updated_at 11:04（今日 01:00 同步，未推 main） |
| **main SHA（真实）** | 本档已置于 origin/main 顶端（具体 SHA 见 `git log origin/main` 最新一条）；基线 `333e2f651` → 现 tip 随 CI 推进 |
| 最近重点 commit | `de9d88b2a`/`4e6bcd660`/`7a32c81a4`/`5a5c5f5ae` intraday 优化；`1ef2a28db` 健康自检日历感知+宏观+平均股价双写；`19ede867c` ETF/LHB 调度；`fdfaa9a2a` 11:29 救急 |
| qs8-tmp 工作树 | ✅ **12:03 已 `reset --hard origin/main` 收口 stale-index 地雷**，工作树干净（仅 `update_v8.py` CRLF 行尾幽灵，已被 assume-unchanged 忽略，无真实改动）；其余 4 untracked 未触碰 |
| 本机时钟 | 系统 CST（无 tzdata 陷阱），`date +%H%M` 双重确认 |

**剩余自动化（小九侧，今日运行中）**
- `1785719698947` 本交接文档更新（12:00/18:00）
- `1785888730897` 盘前全量一条龙（08:15，cwd 已改 qs8-tmp）
- `1785898357370` 智能触发兜底（cwd 已改 qs8-tmp）
- `1787108824799` 盘中漏轮兜底（:10/:40，真实 live=/e/tmp/v8_live）
- `1786696074524` runner 本地守护（11:12 OK）
- `1787210164001` westock 持仓同步（01:00）
- 云端 CI：`v8_cn_fetch_cloud.yml` / `v8_build_deploy.yml` 持续推送（12:07 仍在活跃）

## 四、需阿狸咪注意 / 待办 / 风险
🔴 **P0 — stale-index 地雷已收口但根因未除**：12:03 本档发现 40 个 UU + 19 M + 4 untracked（11:29 救急仅 add 42 目标文件、其余 UU 未清），已 `git reset --hard origin/main` 对齐新鲜态（安全，无旧数据覆盖）。**根因**：含红线文件的仓库在 rebase/autostash 后未彻底清标记即 commit。建议：任何含红线文件（`index.html`/`HEALTH_CHECK.js`/`PORTFOLIO.js`/`data/*.js`）的会话，commit 前务必 `git status` 确认 0 个 UU，否则 `reset --hard origin/main`。

🔴 **P0 — ETF_SUBSCRIPTION_EM 占位数据**：调度已补（`19ede867c`），但 `scripts/fetch_etf_subscription_em.py` 仍写 5 类净申购全 0（接口列不符）。需换 akshare 接口或修脚本，否则该卡长期空值。

🟡 **P1 — 两源码改动未提交**：`cloud_fetch_v8.py`（AVG_PRICE 改 standalone）、`v8_health_check.py` 在 qs8-tmp 工作树曾 modified 但未提交（11:29 救急时为隔离红线已 `--staged` 排除）。属主人另处处理项，阿狸咪勿动。

🟡 **P1 — 智能触发兜底 guard 误判教训**：`git add raw_data/` 曾连带暂存远端改动的 raw_data 旧版造成回滚风险，已改为显式列路径。后续任何 raw_data 提交沿用此法。

🟢 **P2 — 良性残留**：`sync_westock_portfolio.py` / `fetch_portfolio_westock.py` 仍 load 已删除的 `data/PORTFOLIO_COST.js`，仅噪声告警；轻量化清理死逻辑可择期做。

🟢 **P2 — westock 持仓未推 main**：PORTFOLIO.js updated_at 11:04，今日 01:00 因非交易时段仅本地更新；如需最新持仓上 main，阿狸咪可于交易时段触发 `1787210164001`。

⚠️ **主人未决项（连续多轮未解）**：
1. cwd 空壳 `E:/workspace/quant-scanner-v8` 是否彻底改指向 qs8-tmp（本自动化 1785719698947 仍用壳 cwd，今日靠手动识别真实仓库实跑；盘中漏轮 `1787108824799` 仍用壳 cwd+探 v8_live）。
2. 自动化是否重复注册（08-30 曾并行跑两次同档，今日 12:00 亦多实例并行）。
3. 历史 stash 未 pop（08-30+08-29，禁 pop）。
4. 「开盘前防回滚自检 7:25」本机查无注册。
5. AVG_PRICE MA20/MA60 退化（history 仅 1 条）是否需补 120 日日K —— 今夜 post_close CI 跑 `scripts/fetch_avg_price.py`（880003 真指数 120 天）即重写真实 MA，warn 自消，勿手动改 `data/AVG_PRICE_DATA.js`。

## 五、小九监督职责（截至本档）
- ✅ 盘前全量、盘中漏轮、智能触发兜底、runner 守护、持仓同步 6 路自动化今日均按铁律运行（只读 / 单文件隔离 / 禁 stash 收数据）。
- ✅ 线上 HEALTH_CHECK **fail 由 5 → 0**（11:57），站点 200 持续。
- ✅ 主人授权的一劳永逸修复已落盘（cwd 修正 + ETF/LHB 调度补位 + 健康自检日历感知等，`19ede867c`/`1ef2a28db` 等）。
- ✅ 12:03 将 stale-index 地雷 `reset --hard origin/main` 收口，工作树回归干净。
- 🔴 持续盯防：stale-index 地雷复发、ETF 占位数据、未提交两源码改动。
- ⏭️ 18:00 起交阿狸咪接管；本档供其开机即对齐。

---
*更新于 2026-08-31 12:03 自动更新 · 小九（12:03 superset，合并 11:35/12:00 各并行稿 + 12:03 实核）*
