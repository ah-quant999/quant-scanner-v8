# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 更新于 **2026-09-01 12:10 自动更新**（automation-1785719698947，每日 12:00/18:00）
> 交接方向：**小九（白天 07:45–17:45 独家）→ 阿狸咪（夜间/周末 18:00 起接管）**
> 数据来源：当日工作日志 `E:/v8data/qs8-tmp/.workbuddy/memory/2026-09-01.md` + `git log origin/main` 真实 SHA + 线上 Pages 实时拉取。**结论均实测，无推测。**
> 📌 12:05 首发（commit `41740733d`）→ **12:10 补录**：新增 2.6b 工作树分叉分类、2.6c AVG_PRICE 修复滞留、P0-5 / P1-0 两条风险。

---

## 〇、环境事实（本文所有路径以此为据，已实测）

| 项 | 路径 | 状态 |
|---|---|---|
| 自动化配置 cwd | `E:/workspace/quant-scanner-v8` | ❌ **坚果云软链空壳**：无 `.git`、无脚本，`git rev-parse` fatal。仅作本地镜像（index.html + data/*.js 同步副本） |
| **真实 git 仓库（真身）** | **`E:/v8data/qs8-tmp`** | ✅ branch `main`，SSH `git@github.com:ah-quant999/quant-scanner-v8.git`，脚本齐全 |
| 常用软链 | `C:/Users/Administrator/qs8-tmp` | ⚠️ 是上者的 junction，`pwd -P` 解析到 `/e/v8data/qs8-tmp`。**改 cwd 请写真身 `E:/v8data/qs8-tmp`**（08-31 10:39 该 junction 曾被重建清洗） |
| 旧路径 | `/e/tmp/v8_live` | ❌ 已不存在（08-31 后消失），勿再引用 |

> **本档已在真身仓库实跑并推送 origin/main，未改动其他文件。**

---

## 一、背景

- 日期：**2026-09-01（周二，交易日）**。小九时段运行，阿狸咪 18:00 起接管夜间/周末部署。
- 本档为 **12:00 定时交接**，汇总当日截至 12:05 的小九累积改动，供阿狸咪开机即对齐状态。
- 今日主线：**上午连续三轮本地镜像同步 + 盘中数据兜底 + 一劳永逸根治「陈旧暂存区回滚地雷」**，并抓出一条 P0（外部脚本高频 `reset --mixed origin/main` 持续埋雷，已定位三处元凶并修复）。

---

## 二、今日已完成改动（含真实 commit SHA + 影响）

### 2.1 已推送 origin/main 的代码/数据提交（SHA 实核）

| SHA | 说明 | 影响 |
|---|---|---|
| **`5613dacaa`** | `feat(push): 新增 raw_data 隔离 index 直推脚本`（`scripts/push_raw_isolated.py`） | 根治「每轮推送实际都失败」：原 `pull --rebase` 恒 abort 被 `\|\| true` 吞掉、随后 push 非快进被拒。新脚本不依赖 HEAD、不碰工作树、无需 rebase |
| **`720171f13`** | `fix(guard): 收尾归位只取消暂存、不移动 HEAD（防 218 文件回滚地雷）` | 把 `reset --mixed origin/main` 改为 `reset -q HEAD -- <paths>` |
| **`4e8557f5d`** | `feat(guard): 新增 raw_data 推送前反回归闸门`（`scripts/guard_raw_antiregress.py`） | 按**文件内容内部时间戳**比对，本地更旧则取 `origin/main` 版；首跑检出 97/103 个文件本地更旧并安全同步。fail-open、默认 dry-run |
| **`6158c9927`** | `data: 智能触发补抓(盘中·guard) 20260901-1126` | 盘中 4 模块（sector_fund_flow / index_quotes / concept_ranking / capital_flow）补抓 |
| **`8a71ca54f`** | `v8 选股生命周期子页重构（2026-09-01）` | 前端选股生命周期子页重构 |
| **`51f569ec7`** | `fix(v8): 一劳永逸修复三处` | ① 亚太指数抓取重试（0 根因）② 主力净额分时重复快照 + 选线坍缩（1 根线根因）③ 涨停热力矩阵 10 日扩 15 日 + 压缩左侧空白 |
| **`83eaba07d`** | `fix(v8): 新增云端自愈看门狗` | 根治 GitHub schedule 整段漏触发导致盘中数据空窗 |
| `46faffab6` / `47f4d3d41` | risk gauge 10:41 / 盘中算法追踪轻量刷新 | 常规 |

> 其余 `a24c1113f` / `e40d1e97a` / `8fbbb5c2e` / `731580e83` 等为 CI 自动的 `v8 build:` / `v8 cn fetch:` 提交（12:03 最新为 `a24c1113f`）。

### 2.2 08:03 / 09:16 / 11:30 本地镜像同步 ×3（automation-1788138940079）

| 时间 | index.html | data 同步 | sha256 校验 |
|---|---|---|---|
| 08:03 | 883,619 → **958,222 B** | 84/84 成功，0 失败 | `e064e2d3…7ea4bf55` 与主站一致 |
| 09:16 | 958,222 → **959,131 B** | 84/84 成功，0 失败 | `17014f7c…e334b93a6` 一致，无 404 污染 |
| 11:30 | 958,870 → **945,028 B**（-13,842） | 84/84 成功，0 失败 | `380fc47b…5d89e9c5` 一致 |

- 19→21 份 HANDOVER/URGENT 文档完好未动；`data/*.js` 稳定 90 个（6 个为未引用历史遗留）。
- 新建 `_verify_mirror_sync.py` 做 sha256 一致性校验。
- **新知（防误判）**：main 上 index.html 头部是 `<!DOCTYPE \nhtml>`（DOCTYPE 与 html 之间真有换行），`grep -c "<!DOCTYPE html>"` 恒为 0 —— 属源码原样，**不是损坏**。完整性校验应判 `</html>` 存在 + 无 404 文本。

### 2.3 11:23 全时段数据兜底（automation-1785898357370 · 智能触发）

- HHMM=1123 → category=intraday；守卫判 **STALE:9** 触发补抓。
- `cloud_fetch_v8.py --category intraday`：**16/16 模块全 OK，0 失败**（1m59s）。
  - 涨停热力触发全量重建（缺 09/01），**09/01 录得 444 只涨停**；涨跌 3114/2058/113。
  - 沪 6152.0 亿 + 深 7066.7 亿 = 合计 **13218.7 亿**（沪深成交额接口 RemoteDisconnected，改用 index_quotes 兜底成功）。
- 推送踩坑两个（**均已当场拦下，零损伤**）：
  1. 隔离 index 第一版误用 `ls-tree -r`（整棵 raw_data 树）→ 会用本地旧版覆盖远端，**219 个文件回滚风险**，被 push 的 "fetch first" 拒绝偶然拦下。改用 `diff-tree --name-only -r` 取「本提交改动文件」= 20 个。
  2. `git reset --soft` 不清索引 → 推完留下 219 个 staged 旧版（含 index.html、workflows）。用 `git reset --mixed origin/main` 清零，哈希前后逐一比对确认零改动。
- 最终推送 `6158c9927` 成功（重试机制生效：并发 build 推进远端 → fetch 后重建父提交）。守卫复核 STALE:9 → **STALE:1**。
- **avg_price_data.json 是守卫误报（配置缺口）**：08-31 起 AVG_PRICE_DATA 已从 `cloud_fetch_v8.py` 移除（见其 127 行注释），改由 `scripts/fetch_avg_price.py` 在 post_close/weekend 跑；但 `freshness_guard` 的 intraday 类别仍监控它 → 每次盘中假报 STALE。
- ⚠️ **本机不要再跑 `fetch_avg_price.py`**：云端已负责，本机三源（东财/akshare/雪球）全挂，脚本会「保留旧数据 + 写 failed 标记」，推上去会用 08-31 旧值覆盖线上 11:28 新值。已 `git checkout` 回滚。
- 未触碰红线：index.html / HEALTH_CHECK.js / PORTFOLIO.js / BLOAT_CHECK.js 均只读。

### 2.4 11:24 看门狗（automation-1785835481750）—— rc=0，六路全绿，并抓出 P0

- 运行 11:26:56 → 11:34:02（6m59s），**WATCHDOG_RC=0**（近 13 轮首次全身而退），未发邮件（判定正确）。
- 六路管线全绿：runner（`lemoncat-cn` online，17 模块全成功）/ cn_fetch（#1141–#1144 4 连成功）/ build_deploy（#4620/#4622/#4623 成功）/ raw_data_fresh / site 200 / auto_dispatch（HTTP 204）。
- 🔴 **P0（新发，已闭环）**：本地工作树被 `_push_gate_20260901.py` 用 `git reset --mixed origin/main` 把 HEAD/索引推到 origin/main 而**不同步工作树**，218 个文件呈现「待回滚」态（`git add -A && commit` 即整站回滚，-114,584 行）。
  - **三处元凶已定位并修复**（只改源码、未做任何 git 写操作）：

    | # | 位置 | 问题 |
    |:-:|---|---|
    | 1 | `_push_gate_20260901.py:75`（**主犯·每 1–5 分钟高频调用**） | `fetch origin main` + `reset -q --mixed origin/main` |
    | 2 | `scripts/guard_raw_antiregress.py:147` | 同上 `--mixed origin/main`，注释误以为「只动索引」 |
    | 3 | `_isolated_push_20260901.py:113` | `reset --soft origin/main`，注释误以为「不动工作树就安全」 |

  - **机理（写入铁律）**：`git reset [--mixed|--soft] <远端分支>` **都会移动 HEAD**，工作树一个字节不动 → HEAD 跑到最新、工作树停在旧态 → staged=0 但 100+ 个次列 ` M`。**只有 `git reset -q HEAD` 或带 pathspec 的 `git reset -q HEAD -- <paths>` 才不动 HEAD。**
  - 实证：改完静置 3 分钟，reflog 停在 11:46:46 再无新增 `reset: moving to origin/main`；差异从 218 收敛到 **157**。

### 2.5 11:35–11:55 一劳永逸根治（同轮续做）

- **`update_v8.py` 根因已解除**：剥 CR 后比对，本地是**严格旧快照**（mtime 8/31 12:13），远端是其超集（多 `V8_POOL_TRACKER` 映射 + post_close 类别、`MARGIN_DATA` 加 post_close），本地零独有内容 → 备份后 `update-index --no-assume-unchanged` + `checkout --` 恢复，现已逐字节一致。
- 新增 `scripts/guard_raw_antiregress.py`（反回归闸门，`4e8557f5d` + `720171f13`）。
- 新增 `scripts/push_raw_isolated.py`（隔离 index 直推，`5613dacaa`），自带护栏：仅 raw_data/ 前缀 / 本地更旧跳过 / **history·archive·track 类按条目数防累积丢失**（实测拦下 audit_history 12<13、sector_phase_history 12<13、sector_fund_flow_history.json.bak 20<21）/ 0 文件视为异常 / 严禁 --force / 并发重试 5 次。
- `automation-1785898357370` 流程已重写：工作目录改 qs8-tmp、Step1 加闸门、Step3 改用直推脚本、Step4 加「残留 staged 必须为 0」硬校验、明令禁止本机跑 fetch_avg_price.py。
- ⚠️ **并发写同一仓库**：11:44 我写的 `guard_raw_antiregress.py` 被另一进程改写（加了更正确的 `reset -q HEAD --` 方案）→ 推送前必须重新 `hash-object`，我曾推了旧版 blob。

### 2.6b 12:00 工作树分叉全量分类（只读诊断，未动手改）

用 `_classify_divergence.py` 逐个文件比对「工作树 blob 是否存在于 origin/main 历史中」：

| 类别 | 数量 | 说明 |
|---|---:|---|
| BEHIND | **105** | 本地 == 远端某个历史版本，纯落后，可安全对齐 |
| DELETED | **20** | 远端有、本机没 pull 到 |
| **UNIQUE** | **14** | **本机独有改动，必须保留** |
| UNTRACKED | 18 | 多为排查遗留脚本 |

- 14 个 UNIQUE：`index.html`、`data/HEALTH_CHECK.js`（红线）、`cloud_fetch_v8.py`、`v8_build_deploy.yml`、`v8_cn_fetch_cloud.yml`、`HANDOVER_LOG.jsonl`、`data/{AI_MARKET_BRIEF, ALGO_BACKTEST_COMPARE, COMMODITY_ELASTICITY, INDEX_HISTORY, MARKET_PATH_PROBABILITY, RUNNER_STATUS, SENTIMENT_CYCLE}.js`。
- 用 `git diff --ignore-cr-at-eol --numstat` 复验：**14 个全是实质改动，无行尾假阳性**。规模：`index.html +1684/-1350`、`HEALTH_CHECK.js +1128/-1`、`cloud_fetch_v8.py +16/-79`、`v8_cn_fetch_cloud.yml +34/-64`、`v8_build_deploy.yml +7/-27`。
- ✅ **结论：不能全量对齐** —— `index.html` 是 UNIQUE（本地 08:24 改的、疑似等「上线」指令的编辑），一动就毁。**rebase 永远会被 index.html 挡住 → 隔离 index 直推是唯一可行路径**（本档即采用此法）。

### 2.6c 🔴 重大：`cloud_fetch_v8.py` 的 AVG_PRICE 改造滞留本机 2 天未推送

- 本地独有新增内容 = 2026-08-31「**停用 AVG_PRICE 自算口径**」改造：旧 `f_avg_price()` 只能逐日累积 1 条全 A 等权近似，**会抹掉 `fetch_avg_price.py` 拉到的 880003 真实 MA 历史** → 线上 `avg_price_data.json` 长期 `ma20_ready:false / position_vs_ma20:null / history 仅 2 天`，即**「卡片假水位」**。本地已改为彻底停用该自算路径（`return {}`，不覆盖）。
- **远端跑的还是没改的旧版** → 云端 `cloud_fetch_v8` 每次都先用自算覆盖一遍，之后 `fetch_avg_price.py` 若失败（今日三源全挂）就留下假水位。
- 该修复自 **08-31 12:07 滞留本机**，随时会被并发进程冲掉 → **已备份到 `_backup_20260901_未推送独有改动/`（13 个文件，1.4M，含 index.html 与两个 workflow）**。
- ⚠️ **未擅自推送**：属「删功能 + 改云端主抓取链路」，按主人铁律需先请示。另：本地版注释写「`update_v8.py` 映射为 post_close」，但远端 `update_v8.py` 实际是 **intraday**（配合云端 fetch_avg_price 盘中每 30 分刷新）—— **推送前需确认这两处口径是否要一并对齐**。

### 2.6 🔴 westock 持仓同步连续失败 ×2（automation-1787210164001）

- 10:14、11:25 两轮均失败，**未生成新数据、未改 `data/PORTFOLIO.js`、未执行任何 git 写操作**（未用 22 小时前缓存伪造「今日新鲜」，判定正确）。
- **根因（本轮新发现，推翻「未授权」旧判）**：westock 是**档位错配**，不是未绑定。
  - 企业档 `connectors/c07631a5-…/connector-states.json` → `westock-mcp: {bound: true, enabled: true}` ✅
  - `connectors/default/mcp.json` → `connector:westock-mcp: {"disabled": true}` ❌；`default/connector-states.json` → `"enabled": []`
  - 本次会话走 `default` 档 → 工具不注册，会话仅 `agent-mail` 在线。
- 上一轮 P1「origin/main 引用丢失」是**误判**：`sync_westock_portfolio.py:86` 先 `fetch origin main` 再 `read-tree`，能自愈。
- 阻塞项：① westock MCP 工具不存在 ② cwd 是空壳脚本 MODULE NOT FOUND ③ 告警邮件发不出（本会话 agentmail 仅暴露附件上传/下载，**无 `send_mail`**）。

---

## 三、当前系统状态（截至 2026-09-01 12:05 实核）

| 项目 | 状态 |
|---|---|
| 站点（GitHub Pages） | ✅ HTTP **200**（`https://ah-quant999.github.io/quant-scanner-v8/`） |
| HEALTH_CHECK（线上权威） | ✅ updated **2026-09-01 12:03:33**，overall=**ok**，**ok 111 / warn 0 / fail 0 / total 111 全绿** |
| RUNNER_STATUS | ✅ run_time **2026-09-01 12:01:06**，category=intraday，**17/17 模块全部 ok，0 异常** |
| **main SHA（真实 tip）** | **`a24c1113f`**（`v8 build: 2026-09-01 12:03`）；上一个代码提交 `5613dacaa` |
| 持仓 PORTFOLIO（线上） | ⚠️ `updated_at` 仍 **2026-08-31 13:02**（westock 阻塞，见 2.6）。数据：总资产 991,377.39 / 市值 45,047.00 / 可用 946,330.39 / 收益率 -0.86%；托伦斯 sz301583 100股、广钢气体 sh688548 1000股 |
| 本地工作树（qs8-tmp） | 🔴 **157 项旧版本差异**（陈旧暂存区残留）。`git merge --ff-only origin/main` **被安全中止**（本地 data/*.js 等会被覆盖），**未强推、未 reset --hard** |
| 本机时钟 | 系统 CST，`date +%H%M` 双重确认 |

### 剩余自动化（小九侧，共 20 个：18 ACTIVE + 2 PAUSED）

| ID | 名称 | 状态 | 计划 |
|---|---|---|---|
| `1785719698947` | 本交接文档更新（给阿狸咪） | ACTIVE | 每日 12:00 / 18:00 |
| `1788138940079` | v8 本地镜像同步 → E: 空壳 | ACTIVE | 每日 8–20 点 :20 |
| `1785898357370` | v8 全时段数据兜底（freshness_guard） | ACTIVE | 工作日 8–16 点 :00/:20/:30/:40 |
| `1785835481750` | v8 每小时紧急交接通道（看门狗+自修复） | ACTIVE | 每日 7–21 点 |
| `1786696074524` | v8 runner 本地守护（错误即自愈） | ACTIVE | 每日 :10/:30/:50 |
| `1786961773708` | v8 自愈监控闭环（10 分钟级） | ACTIVE | 工作日 8–18 点每 10 分 |
| `1787108824799` | v8 盘中漏轮本机兜底 | ACTIVE | 工作日 9–15 点 :10/:40 |
| `1787210164001` | v8 westock 持仓云端同步 | ACTIVE（**阻塞中**） | 工作日 9–15 点 :00/:30 |
| `1785888730897` | v8 盘前/每日全量（08:15 fetch+update+deploy） | ACTIVE | 每日 08:15 |
| `1785388522458` | v8 15:30 收盘数据刷新 | ACTIVE | 工作日 15:30 |
| `1785404904506` | v8 16:45 选股四模块+全量部署 | ACTIVE | 工作日 16:45 |
| `1788145998708` | v8 16:30 港股收盘后 post_close 刷新 | ACTIVE | 工作日 16:30 |
| `1788168934360` | v8 盘后算法链云端守卫（19:25） | ACTIVE | 工作日 19:25 |
| `1788172770889` | v8 龙虎榜/共振日历云端守卫（21:40/22:40） | ACTIVE | 工作日 21:40 / 22:40 |
| `1785978303212` | 每日 19 点审计修复+推仓+对齐 | ACTIVE | 每日 19:00 |
| `1787575410637` | v8 James-Stein 行业级收缩回测（盘后） | ACTIVE | 工作日 19:30 |
| `1786588096327` / `1786588094908` | v8 分红方案刷新（盘前 08:10 / 晚间 19:10） | ACTIVE | 每日 |
| `1785396622150` | v8 周末 15:30 选股四模块 T+1 | ACTIVE | 周六日 15:30 |
| `1786933089412` | TDX 强势选股每日跟踪 | **PAUSED** | 工作日 11:35/15:40 |
| `1785802406605` | v8 本地看板同步（每小时 pull） | **PAUSED** | 每小时 |

---

## 四、需阿狸咪注意 / 待办 / 风险

🔴 **P0-1｜三处 `reset` 修复只在本地工作树，未 commit**（主人未说「上线」）。涉及 `_push_gate_20260901.py`、`_isolated_push_20260901.py`、`scripts/guard_raw_antiregress.py:155`。**若阿狸咪在 18:00 后做批量提交，务必先确认这三处修复已在远端**，否则地雷会被重新埋上。上线命令（仅这三个路径，勿 `git add -A`）：
```bash
cd /e/v8data/qs8-tmp
git add scripts/guard_raw_antiregress.py _push_gate_20260901.py _isolated_push_20260901.py
git commit -m "fix(git): 三处 reset --mixed/--soft 改 reset -q HEAD -- <paths>，根治陈旧暂存区回滚地雷"
git push origin main
```

🔴 **P0-2｜残留 157 项旧版本差异，待授权 `reset --hard origin/main` 清理**。**阿狸咪切勿在授权前执行 `git add -A && git commit`**（会把 157 个文件回滚到旧态）。已核实红线文件零数据损失：`data/PORTFOLIO.js` 本地仅 pretty 排版差异、数据完全相同；`index.html` 差异为排版/缓存戳，远端为准。清理命令：
```bash
cd /e/v8data/qs8-tmp && git fetch origin && git reset --hard origin/main
```

🔴 **P0-3｜westock 连接器档位错配 → 持仓卡停在 08-31**。需主人在连接器管理页切到**企业档 `c07631a5`**（该档已是 `bound:true, enabled:true`），或把 `default` 档的 `connector:westock-mcp` 打开并重启会话。**AI 不要直接改这两个配置文件**（属授权动作）。未修复 → `1787210164001` 永远拿不到数据。

🔴 **P0-4｜多个自动化 cwd 仍指向 E: 空壳**（已第 4+ 次复现）。建议统一改真身 **`E:/v8data/qs8-tmp`**（**不要用 `C:/Users/Administrator/qs8-tmp` junction**，08-31 10:39 曾被重建清洗）。至少 `1785719698947`（本档）、`1787210164001`、`1785835481750` 需改。

🔴 **P0-5｜`cloud_fetch_v8.py` 的 AVG_PRICE 修复滞留本机 2 天未推**（详见 2.6c）。这是「平均股价卡片假水位」的根因修复，但属**删功能 + 改云端主抓取链路**，按铁律需主人先授权。备份已在 `_backup_20260901_未推送独有改动/`（13 文件 1.4M）。**阿狸咪不要擅自推**，也不要把它混进任何批量提交；若夜间要推，必须先确认 `update_v8.py` 的 post_close/intraday 口径是否一并对齐。

🟡 **P1-0｜14 个 UNIQUE 文件意味着「全量对齐」不可用**：任何 `git add -A` / `reset --hard` / `rebase` 都会毁掉 `index.html` 的本地 08:24 编辑。批量提交一律走**显式列路径**或**隔离 index 直推**。

🟡 **P1-1｜`avg_price_data.json` 守卫误报**：08-31 起 AVG_PRICE_DATA 已从 `cloud_fetch_v8.py` 移除，改由 `scripts/fetch_avg_price.py` 在 post_close/weekend 跑，但 `freshness_guard` 的 intraday 类别仍监控 → 每次盘中假报 STALE。需从 intraday 监控清单移除。

🟡 **P1-2｜`_load_token()` 候选表不含 qs8-tmp 目录**（`v8_health_check.py` 74-75 行），token 会反复「丢失」，修复未进 origin/main。

🟡 **P1-3｜并发写同一仓库**：本机多自动化并行操作同一工作树（11:44 实测脚本被另一进程改写）。任何脚本改动必须尽快推远端，且**推送前重新 `hash-object`**。

🟡 **P1-4｜ETF_SUBSCRIPTION_EM 占位数据**（历史遗留）：调度已补，但 `scripts/fetch_etf_subscription_em.py` 调用的 `ak.fund_etf_fund_info_em()` 返回列与期望不符，5 类净申购恒为 0。需换接口或修脚本。

🟢 **P2｜清理项**：17 个未跟踪排查脚本（`lhb_*.py`、`monitor_*.py`、`repro_sf.js`、`sf_intraday_live.js`、`hc_0924.js`、`lhb_hist_raw.js` 等）；4 条 2026-08-09 僵尸 in_progress run；22/23/00 点超 rrule 触发仍待排查（现靠静音窗兜底）。

⚠️ **主人未决项（连续多轮）**：
1. 是否授权 `reset --hard origin/main` 清理 157 项残留（P0-2）。
2. 是否让三处 reset 修复上线（P0-1）。
3. 是否统一把所有自动化 cwd 改到 `E:/v8data/qs8-tmp`（P0-4）。
4. 历史 stash 未 pop（08-29/08-30，**禁 pop**）。
5. 「开盘前防回滚自检 7:25」本机查无注册。

---

## 五、小九监督职责（截至本档）

- ✅ 本档已在**真身仓库 `E:/v8data/qs8-tmp`** 实跑，用**隔离 index 直推**（临时 `GIT_INDEX_FILE` + `commit-tree`），**完全不触碰工作树**，零触发 157 项残留地雷、零 side effect。
- ✅ 环境事实复核并订正：`E:/workspace/quant-scanner-v8` 为空壳；真身 `E:/v8data/qs8-tmp`；`/e/tmp/v8_live` 已不存在。
- ✅ `git merge --ff-only` 被安全中止后**未强推、未 reset --hard**，红线文件（index.html / HEALTH_CHECK.js / PORTFOLIO.js）零改动。
- ✅ 线上状态实测留痕：站点 200、HEALTH_CHECK **111/111 全绿**（12:03:33）、RUNNER_STATUS **17/17 ok**（12:01:06）。
- ✅ 今日已推 4 个代码/数据 commit（`5613dacaa` / `720171f13` / `4e8557f5d` / `6158c9927`）+ 3 次镜像同步 84/84 全成功。
- 🔴 持续盯防：三处 reset 修复未上线、157 项残留地雷、westock 档位错配、cwd 空壳。
- ⏭️ **18:00 起交阿狸咪接管**；12:05–18:00 间小九继续按 15:30 收盘 / 16:30 post_close / 16:45 选股四模块 三档推进。

---

*更新于 2026-09-01 12:10 自动更新 · 小九（automation-1785719698947）*
*推送方式：隔离 index 直推（不动工作树）· 仅新增本交接文档一个文件*
*首发 commit `41740733d`（12:07）→ 补录版见本档最新提交*
