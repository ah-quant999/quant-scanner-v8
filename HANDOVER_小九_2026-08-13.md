# HANDOVER 小九 → 阿狸咪（2026-08-13 盘前+上午档 · 12:00 自动更新）

> 更新于 2026-08-13 11:57:14 自动更新（小九白班 12:00 交接档）
> 数据源：E:\workspace\quant-scanner-v8\.workbuddy\memory\2026-08-13.md（截至 11:43）+ origin/main 真实 commit 核实
> 所有 SHA 均经 `git merge-base --is-ancestor <sha> origin/main` 核实，非记忆值。

---

## 一、背景（今日主线）

今日核心事件是 **09:40 盘中数据缺口事故**，根因实锤为：云端 `v8_cn_fetch_cloud.yml` 的 `runs-on: ubuntu-latest`（**美国 IP**），而 `cloud_fetch_v8.py` 头部已自承「美国 runner 抓 A 股中国源 60s 超时」→ 盘中实时抓取全失败。小九据此完成了**盘中数据主通道从「云端 US 派发」改为「本机中国 IP 直抓 + 推送 raw_data」**的一劳永逸修复，并顺带收口了港股/ETF 搜索缺失、分红方案陈旧、全站精选 A 股缺失、AI 速览空话、URGENT 噪音、云端自愈器碎片化等多处根因。

阿狸咪昨日（08-12 18:30）的运维看板修复（原 commit `55ad3d54`）**已上线生效**；该 SHA 在本机已不可见（被 origin 重写覆盖），但其内容已在 main 上，无需另行处理。

---

## 二、今日已完成改动（按时间序 · 含真实 SHA + 影响）

### 盘前 / 早段（08:00–09:30）
| 时间 | 改动 | 真实 SHA | 影响 |
|---|---|---|---|
| 08:04–08:33 | **Top3 90 天滚动追踪** 新建 `algorithms/gen_top3_track.py`，接入 `run_algorithms.py`+`update_v8.py`（post_close），生成 `raw_data/top3_track.json`+`data/TOP3_TRACK.js`；UI patch 仅生成未 apply（红线：不动 index.html） | 未 commit（脚本待入库） | 待阿狸咪/主人 apply `docs/ops/handover/HANDOVER_小九_2026-08-13_Top3_追踪.patch` |
| 08:56 选项A | 丢弃 `data/HEALTH_CHECK.js` 脏快照 → `git pull --ff-only` 同步 `ef3732cd` → apply 三处 UI 改动（注入 TOP3_TRACK、止损/目标合并一行、KPI 合并两行）→ 护栏通过 → commit+push | `d05b2c18`（UI）+ `fb612ea5`（handover 文档） | 上线 |
| 08:56 | 暂停 2 个冗余 automation：`1785718343599`(管线看门狗每2h)、`1785980310111`(盘中云端任务定时触发+监控兜底) | — | 消除看门狗/URGENT 重复噪音 |
| 09:06 | **IPO tracking 显示 bug 修复 + 评分情绪因子升级**：`update_v8._sanitize_ipo_recommend` 不再覆盖 tracking 股建议；`fetch_ipo_data_v8.py` 的 `heat_score` 改动态 0–15（行业 RS/涨停数/超募倍数） | `c1ef3801` | 杰理科技/超纯应材显示正确；applying 股出现动态热度分 |
| 09:18 | **港股/A股公平性改造**：`fundamental_helper.py` 移除港股 -10 强制降权（缺失统一中性 0）；`renderFinalCandInner` 取消 A/H 分组、统一按 `final_score` 排序、移除「⚠️已降权」标签 | `c797e5ff` | 盘后 generate_top10/cockpit 自动继承，港股排名可能上升（预期） |
| 09:15 | IPO 卡片上市日期另起一行显示 | `b9e62a01` | UI |
| 09:26 | 盘前 badge 去重（「盘前 盘前」双显修复） | `84ca09f6` | UI |
| 09:29 | 时段 badge 双显 + 图表日期错乱/密度（上证深证成交额、涨跌家数 x 轴排序） | `599d64f8` | UI/图表 |

### 09:40 事故 → 根因修复（10:00–11:50）
| 时间 | 改动 | 真实 SHA | 影响 |
|---|---|---|---|
| 10:06 | **临时修复** 9:40 盘中缺口：本机中国 IP 跑 `cloud_fetch_v8.py --category intraday`（14 模块全 ok）→ push raw_data | `e9abb885` → build `f8009b36` | 线上盘中数据刷新到 10:14–10:16 |
| 10:30 | **个股查询补全港股/ETF**：`fetch_stock_names.py` 新增 `_fetch_hk_stocks`/`_fetch_etf_stocks`+拼音首字母（名称库 5207→9582）；`fetch_stock_quote_v8.py` 新增 `fetch_hk_spot`/`fetch_etf_spot`（行情库 5542→9917）；重建 STOCK_LIST/STOCK_QUOTE | `dfda0d94` | hdl/海底捞/06862/588170/510880 等全部可搜 |
| 10:30 | **分红方案陈旧根因修复**：新增 `algorithms/refresh_dividend_cninfo.py`（巨潮法定源，逐只取最新方案）+ `DIVIDEND_WATCH.json`（重点池 336 只）+ 推送自愈（冲突时 `checkout --ours`+重跑 refresh）；新建 2 个定时自动化（盘前 08:15 `1786588096327` / 晚间 19:00 `1786588094908`） | `089ca4cf` + `aad7590d` | 宝丰能源 600989 修正为 2026-04-22/10派4.2 |
| 10:30 | **盘中数据链永久修复（一劳永逸）**：重写 automation `1785898357370` 为「本机中国 IP 直抓 intraday + 推 raw_data → 触发云端 build」，交易时段每 30 分(09:40–15:10) | 验证 `7d158f4d` → build `e26663eb` | 端到端 5 分钟闭环，不再 dispatch 坏 US 通道 |
| 10:30 | **全站精选「A 股数据缺失」根因修复**：`scanner.py` 同时识别 `GITHUB_ACTIONS=true` 走 GTimg；`v8_algo_cloud.yml` 显式注入 `CLOUD_RUNNER=true`；用本地 `gold_pool.json`(102只,81A+21HK) 重建 `TOP10_DAILY.js` | `cf3d60b9` | TOP20 中 A股18/港股2，告警即时消除 |
| 10:46 | **TOP3 持仓跟踪首字母搜索无响应**：`_uniSearch` 已修但 TOP3 卡片 `showPfDropdown` 函数根本不存在 → 补全 `showPfDropdown`/`hidePfDropdown`/`_pfSelectDropdown` 复用搜索内核 | `a3774b84` | 输入 bfn 命中宝丰能源 |
| 10:52 | **AI 速览「操作建议」空话**：`gen_market_brief.build_strategy()` 把「主线板块/关注个股」替换为真实排名前排（概念净流入前3 + 个股主力净流入前2），`renderAiSummary` 新增 pill 展示 | `12682d0c` | 示例：推荐通信技术/CPO/算力 + 天孚通信/新易盛 |
| 11:10 | **盘中直抓正式闭环首轮**：`84e58909`（14/14 ok）→ build `78a90365`，线上 7 张实时卡全为本轮值；rebase 冲突按 update_time 取新方（非无脑取我方） | `84e58909` | 验证主通道切换成功 |
| 11:27 | **root URGENT/backup 噪音清理（闭环非只删）**：`.gitignore` 加 `/URGENT_*.md`（保留 docs/ops/urgent 与 3 份含根因的 URGENT）；`git rm` 64 root URGENT + 4 backup；备份 tar.gz | `40d8a667` + handover `230ec84c` | 三方对齐（云端不再回推/工作树干净/阿狸咪可见） |
| 11:43 | **云端任务架构重构**：`v8_cn_fetch_cloud.yml`+`v8_algo_cloud.yml` 的 `runs-on` 改 `[self-hosted, cn]`（中国 IP 根治缺口，cn 离线降级 ubuntu + api_push_raw 防倒退守卫）；**合并 5 个自愈器为 1 个 `v8_health_patrol.yml`**（触发=workflow_run 完成 + 每 60 分兜底，内部 `--heal` 一站派发） | `31d4dc9b` + 补提交 `1fe4f8c7` → build `5fb90c63` → healthcheck `68618cfd` | 消除每 30/15/6 分轮询碎片化；build 完成自动唤醒 patrol 实锤生效 |

---

## 三、当前系统状态

- **站点**：GitHub Pages 在线（200）。最新 build `5fb90c63`（11:50）已部署，data/*.js 刷新到 11:48–11:50 盘中实时值。
- **main SHA**：`68618cfd`（v8 healthcheck 2026-08-13 11:51）。小九本地已 `git merge --ff-only` 对齐，零落后。
- **runner 架构**：
  - 盘中主通道 = **本机 cn runner（中国 IP）直抓** `cloud_fetch_v8.py --category intraday` → push raw_data → 云端 build（自动化 `1785898357370`，09:40–15:10 每 30 分）。
  - 云端 `v8_cn_fetch_cloud.yml` / `v8_algo_cloud.yml` 已改 `runs-on: [self-hosted, cn]`（中国 IP），cn 离线自动降级 ubuntu + 防倒退守卫。
  - 自愈器统一为 `v8_health_patrol.yml`（原 5 个已删除）。
- **剩余自动化（活跃/新建）**：
  - `1785898357370` 盘中直抓本机中国 IP（重写后）
  - `1786588096327` 分红刷新-盘前(08:15)、`1786588094908` 分红刷新-晚间(19:00)
  - 12:00/18:00 交接档（本任务 `1785719698947`）
  - 云端 `v8_cn_fetch_cloud.yml` / `v8_algo_cloud.yml` / `v8_build_deploy.yml` / `v8_health_patrol.yml`
- **已暂停**：`1785718343599`(管线看门狗每2h)、`1785980310111`(盘中云端任务定时触发)

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 待主人拍板（非阻塞，但影响 7×24 健壮性）
1. **出差根治**：cn runner 现仅装小九电脑，fallback 仅降级 ubuntu（美国 IP 部分残缺）。要 7×24 不受影响需加第二台 `cn` label runner（腾讯云轻量 1c1g 中国 IP 装 GitHub runner 服务，几十元/月）。**小九关机出差时盘中链会降级但不崩**。
2. **6 个本地轮询自动化冗余**：`1784797186247`(:00健康)/`1784797187221`(:30健康)/`1784797279359`(心跳)/`1784542272747`(10:05自愈)/`1785918538840`(盘后守护)/`1785835481750`(紧急交接看门狗) 已被云端 patrol 功能覆盖，冗余但无害（有去抖锁）。主人只问「是什么」未下令暂停，暂保留，确认后再停。
3. **`automation-1785208215213` 指向已弃用 v6 repo-temp 路径** —— 建议核对去冗余。

### ⚠️ 残留风险（已记录，非紧急）
- 云端 US 盘中 cron 仍跑（冗余无害）；cn 自托管 runner `324135267` 仍 100% 失败但已非主通道。
- 远端 11:12 那轮 cn fetch 的 `runner_status.category=premarket`——盘中时段跑 premarket 类别，疑似另一通道调度口径不对，留待排查（非本任务职责）。
- `gen_top3_track.py` + Top3 UI patch 尚未 git commit（按红线未动 index.html，patch 待 apply）。

### ✅ 已生效无需处理
- 阿狸咪 08-12 18:30 运维看板修复（原 `55ad3d54`，内容已上线，SHA 被覆盖不可见）。
- 09:40 盘中缺口已闭环，未再复发。

---

## 五、小九监督职责（白班 7:45–17:45）

- 继续监督**盘中直抓链**（`1785898357370` 每 30 分）与**云端 patrol 自愈**链路，确认无 09:40 类缺口重现。
- 监督**分红刷新**盘前(08:15)/晚间(19:00) 两个自动化落地情况。
- 看门狗/healthcheck 异常按 `--heal` 自动派发；若发现 URGENT 重建回推，按已建 `.gitignore` 规则忽略。
- 18:00 生成下一档交接文档（含下午盘后算法链 `v8_algo_cloud.yml` 产出情况）。
- 红线：`index.html` 若有并行 WIP 改动，原样保留不介入；推 data/raw_data 前先 `git fetch` 看增量，遇 DIVERGED 用 reset --hard origin/main 再 cherry-pick/checkout 单文件，不在脏工作树硬 rebase。

---
*本档由自动化 `1785719698947` 于 2026-08-13 11:57:14 生成，仅更新 `HANDOVER_小九_2026-08-13.md`，未触碰其他文件。*
