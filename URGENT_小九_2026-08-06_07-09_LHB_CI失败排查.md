# 🚨 紧急 — 8/5 18:30 没跑 CI，全天盘后算法链空跑（实证 + 待处置）

**Date**: 2026-08-06 07:11 CST
**From**: 阿狸咪（家里机 · detect+dispatch 节点，**只读排查**）
**To**: 小九（单位机 · GitHub Actions 生产节点）+ 主人

---

## TL;DR（重要 — 推翻主人原假设）
主人怀疑「8/5 18:30 那趟 CI 跑失败 → 机游 LHB 没数据」。

**实际真相**（GitHub Actions API 实测）：
1. **8/5 18:30 CST 当刻没有任何 workflow schedule 触发**——`v8_algo_*` 与 `v8_cn_fetch_*` 全天 schedule=0 次
2. **8/5 整天的「盘后算法链」只在 20:22 / 21:38 CST 各自动跑过一次**（schedule 触发）——**两次都 failure，且都挂在第 1 步**
3. **云端主力 workflow（`v8_cn_fetch_cloud` / `v8_algo_cloud`）至今只在 8/5 各手动 dispatch 1 次**——**schedule 从未自动触发过**

也就是：**整个 8/5~8/6 期间，云端算法链是空跑的**。机游 LHB 没新数据 = 这段时间没 fetch_lhb 脚本被 schedule 触发过。

---

## 一、GitHub Actions API 实测数据（07:11 CST 拉取）

### `v8_algo_run.yml`（自建 cn · 应急版，最近 7 次）
```
2026-08-05T13:38:09Z  failure     schedule            run_id=31011155328   ← 21:38 CST
2026-08-05T12:22:29Z  failure     schedule            run_id=31005326516   ← 20:22 CST
2026-08-04T15:23:10Z  success     workflow_dispatch   run_id=30923875759   ← 23:23 CST 上次成功
2026-08-04T15:00:26Z  failure     workflow_dispatch   run_id=30921892350
2026-08-04T14:21:34Z  success     workflow_dispatch   run_id=30918527478
2026-08-04T14:04:25Z  success     workflow_dispatch   run_id=30917037641
2026-08-04T13:41:39Z  success     schedule            run_id=30915086575   ← 21:41 CST 最后一次 schedule 成功
```
→ **8/4 23:23 后再无 success；8/5 当天 2 次 schedule 跑全部 failure**

### `v8_algo_cloud.yml`（云端 ubuntu · 主力）
```
共 1 次 run
2026-08-05T15:44:47Z  success     workflow_dispatch   run_id=31021842600   ← 23:44 CST 手动
```
→ **schedule 从未自动触发**（这是问题 #1：cron 配置可能压根没生效 / 缺失）

### `v8_cn_fetch_cloud.yml`（云端 cn_fetch）
```
共 1 次 run
2026-08-05T09:30:26Z  success     workflow_dispatch   run_id=30993551131   ← 17:30 CST 手动
```
→ 同上，**schedule 从未自动触发**

### 最近 30 次全仓 run（含 v8_build_deploy）
```
全为 v8_build_deploy / pages_build + 零星算法链手动 trigger —— 全 success
云端算法链 + 旧自建算法链 schedule 全部哑火
```

---

## 二、失败根因（API 可观测层面）

两次 failure run（31005326516 / 31011155328）**步骤分解一致**：

```
JOB algo   status=failure
  ✅ Set up job
  ❌ 📥 经 GitHub API 同步源码（唯一可行路线：本 runner 上 git 走 github.com 会被连接重置）
  ⏭  🐍 校验 Python / 安装 A股依赖          ← skipped
  ⏭  🧮 运行盘后算法链 → raw_data/ → 推送   ← skipped（这就是机游 LHB 没数据的根因！）
  ⏭  📤 兜底推送（若上一步未推）           ← skipped
  ✅ Complete job
```

**第 1 步就炸**，后面所有算法链 + 数据源（包括 fetch_lhb）全部没跑。

> ❌ 无法抓取 logs API 详细堆栈（API 返回 403，需 token）。GitHub UI 上点 job 名能看原始 trace。

---

## 三、待小九启动时立刻办的 5 件事（按优先级）

### P0（最高 · 今天 8/6 早上必查）
1. **打开 GitHub UI 看 `v8_algo_run.yml` run 31005326516 / 31011155328 的失败堆栈**：
   `https://github.com/ah-quant999/quant-scanner-v8/actions/runs/31005326516`
   `https://github.com/ah-quant999/quant-scanner-v8/actions/runs/31011155328`
   重点关注「经 GitHub API 同步源码」这步的具体报错（HTTP 503？rate limit？证书问题？response timeout？）
2. **打开 workflow 文件 `.github/workflows/v8_algo_run.yml` 查看「GitHub API 同步源码」这步的实现脚本**——是 `repos/{owner}/{repo}/tarball/{ref}` 还是 Contents API？是自写 python 还是 curl？

### P1（必查 · schedule 根本没自动触发问题）
3. **打开 `v8_cn_fetch_cloud.yml` 与 `v8_algo_cloud.yml` 看 schedule 配置**：
   - 是 cron 字段缺失？格式写错（UTC vs 北京时间）？
   - 是 workflow 默认 disabled schedule？needs 条件？if 条件？
   - 看是否需要 `workflow_dispatch` trigger 同时存在 + 默认 branch 设置
4. **如确有 cron 配置**：去 Settings → Actions → General → **Workflow permissions** 检查 token 权限、Settings → Actions → Scheduled workflows 查看上次触发时间与下次触发时间。
5. **建议立即手动 dispatch 一次 `v8_cn_fetch_cloud` + `v8_algo_cloud`** 先把数据补齐给主人看（不再让主人再等一天）。

### P2（修复细节）
6. 失败原因如确认是「GitHub Contents API 同步源码」步骤本身写死 OK——评估：
   - 短期：手动 dispatch 补数据
   - 中期：换成 Ubuntu runner（runner=ubuntu-latest，不要 self-hosted）跑 schedule，云端主力本来就配的 ubuntu-latest，但不知为何云端至今 0 schedule run
   - 长期：把云端 cron schedule 与 GitHub Actions 平台行为解耦，引入外层调度（如 cron 一直有但被 GitHub Actions 跳过——1 个已知行为）

---

## 四、主人已知背景（已查清的不必再问）

- **机游 LHB 数据源顺序**：东财（akshare `stock_lhb_detail_em` / `stock_lhb_statistic_em`） → neodata → westock
- **本机不接生产任务**（双机分时铁律）：阿狸咪绝不直接跑 fetch_lhb / run_algorithms / scanner.py
- **机游 LHB 影响面** = 选股策略 candidate.json 的 `inst_net` / `游资净买` → 重抓后这两个池子也得重算

---

## 五、阿狸咪本机已自动完成（不干预小九）

```
✅ 修 phTag：删除「·最后更新 --」盘后数据 section header
   commit <NEW>   pushed origin/main（0 ahead/0 behind）
```

小九不需要 rebase 或 reset。fetch 一下即可看到。

---

## Send log dump back as reply. 阿狸咪前端已上线 + 等小九早班定位。
