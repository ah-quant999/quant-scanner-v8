# 交接件：主升/启动龙头股卡「启动丢失」根治 + K型卡左侧双圆弧括号去除

- 交接方：阿狸咪的工程师（家机 alimi-cn）
- 时间：2026-09-18 20:15（北京时间）
- 提交：`dedeebbd7`（2026-09-18 20:12 → origin/main，Git Data API base_tree 推送，非快进）
- 触发：主人三连指令 ①确认龙头股卡随盘后数据动、不是死的 ②「只显示了主升，启动丢了」③K型卡左侧不要两个圆弧括号

## 一、主人三问的答案（先看这里）

1. **卡是动态的，不是死的**：龙头股卡读 `window.SECTOR_LEADERS`（data/SECTOR_LEADERS.js），由 `scripts/fetch_sector_leaders.py`（挂在 run_algorithms.py STAGES["A"]）盘后日频重算；phase 判定与「板块资金趋势」卡同一条规则（主升 5日>3%且20日>5%；启动 5日>1.5%且20日>-8%），数据源同为一Side SECTOR_RS（同花顺行业 RS）。
2. **「启动丢了」真因（两层）**：
   - **今晚直接原因**：18:08 跑的是**旧版生成器（只取主升）**（该脚本 18:41 才首次入库，旧输出 sector_count=1、phase 字段="主升"可证），18:41 后新版尚未被链路重跑 ⇒ 线上只剩主升·元件。
   - **系统性 bug（已实锤复现+修复）**：同花顺行业名 vs 东财板块名精确匹配必丢——东财叫「电子化学品Ⅱ(BK1039)」「其他电子Ⅱ(BK1223)」，17:33 那轮（新版）因此 8 启动只显示 6。
3. **左侧两个圆弧括号真因**：`__renderMacroKShape` 在壳卡 `#macroKShapeBody`（renderUnlisted 14884 行建）内部**又自建一张同名 .card**（16969 行）⇒ 卡中嵌卡，两道金色圆角左边条并排 = 视觉上的两个圆弧括号，且重复 id。

## 二、本次改了什么（dedeebbd7，恰好 4 路径零删除）

| 文件 | 改动 |
|---|---|
| scripts/fetch_sector_leaders.py | ①板块名归一匹配：精确 → 去 Ⅰ/Ⅱ/Ⅲ 级后缀，同级碰撞优先 Ⅱ 级；②个股抓取失败改双轮重试（3重试/轮×2轮，轮间2.5s），仍失败**保留板块**（leaders=[]+leaders_error=1），不再静默丢弃；③汇总日志 `OK 主升(x/y) 启动(z/w)` 一眼可审 |
| data/SECTOR_LEADERS.js | 重生成：**主升(1/1)+启动(8/8)=9 板块全量**（沙箱实测，电子化学品/其他电子经归一命中 Ⅱ 级板块），update_time 19:55:42 |
| raw_data/sector_leaders.json | 同上重生成 |
| index.html | ①`_fillSectorLeaders` 不再过滤掉无龙头板块，失败板块明示「⚠ 龙头股行情本轮抓取失败，待下轮盘后自动补抓」，组头 N 与「板块资金趋势」口径一致；成分股失败时显「—」；②`__renderMacroKShape` 去掉内嵌同名卡（只填壳卡），金色左边条唯一；③SECTOR_LEADERS.js 缓存戳刷新 |

## 三、验证链（全部实测）

- 生成器沙箱实跑：`OK 主升(1/1) 启动(8/8) js_bytes=5302 leaders_fail=0`。
- 门禁（node）：全站 28 段 inline script `new Function` 0 错；**stub DOM 真渲染**两函数：龙头股卡 6/6 PASS（含失败板块明示、组头口径）、K型卡嵌套消除+内容完整 PASS；div 计数与原版基线逐位一致（16/18 静态、14/13 渲染，既有口径差，非本次引入）。
- 推送护栏：漂移守卫两次正确拦截缓存戳 bot 抢写（cf6ff0e5→b8d64215→9ff3f9e3），最终以 9ff3f9e3 为基线重打后成推；推后树比对差异**恰好 4 条、零删除**；blob 回读逐字节一致（3 条文件直接 PASS，index.html 的逐字节自证因 API 断连改由本地 `git diff --raw 40023a96c dedeebbd7` 完成，同为恰好 4 条零删除）。
- CDN 实抓（Pages 部署完成后）：`data/SECTOR_LEADERS.js?v=1789733646` = 5328B、sector_count:9；index.html 含全部三处新代码串。后续 bot 提交（a7a2c4e8a）只刷 ?v，未吞补丁（grep 实证）。

## 四、小九侧需要知道 / 待办

1. **无需改 workflow/时窗**：生成器仍在 STAGES["A"] 原位，明天盘后链自动产出 主升+启动 全量。若 runner 上跑时看到 `skip(no-match-even-norm)` 字样 = 出现了新的同花顺↔东财无对应板块名，需人工别名表（日志已指名）。
2. **口径纪律**：本卡组头「N 个板块」现在恒等于「板块资金趋势」同 phase 计数；个股行可能因东财限流短暂缺省（卡片有黄字提示），下一盘后轮自动补齐——这不是故障，**不要按假成功补数**。
3. **logic.html 待补**（非本次 scope）：数据源矩阵无「主升/启动板块·龙头股」条目（SECTOR_LEADERS / scripts/fetch_sector_leaders.py / 盘后），建议小九下次动 logic.html 时补一行，保持矩阵完整。
4. 热文件竞态提示：index.html 缓存戳 bot 2~3 分钟一提交，跨机手改 index.html 请走 base_tree + 漂移守卫，禁直接 push（本次两次被守卫拦下的正是这个坑）。

*本交接件仅供研究运维参考，不构成投资建议。*
