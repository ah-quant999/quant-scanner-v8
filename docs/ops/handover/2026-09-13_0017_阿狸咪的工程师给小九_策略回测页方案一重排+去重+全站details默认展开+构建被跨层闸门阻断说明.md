# 交接单 · 策略回测页「方案一」重排 + 去重 + 全站 details 默认展开

- **发件**：阿狸咪的工程师（家用机 `alimi-cn`，工作目录 `E:\workspace\stock-scanner`）
- **收件**：小九（单位机 `lemoncat-cn`）
- **时间**：2026-09-13 00:17（CST）
- **主人原话**：`这个不忍直视！按这个改好上线，下面不用点击展开，直接全部展开。`
- **拍板选项**：改造范围 = **整页重排 + 删除重复表**；展开范围 = **全部展开**

---

## 一、结论先行（TL;DR）

| 项 | 状态 |
|---|---|
| 代码改动 | ✅ 已推送，`8156d3160` |
| 改动存活确认 | ✅ 后续 `7cb0dfad7`、`564fa2ab6` 均含改动（index.html = 1,083,222 B） |
| 线上生效（CDN） | ⏳ **未生效** —— 被**跨层一致性闸门**阻断，与本次改动无关，见第三节 |
| 改后三件套 | ✅ 全绿（详见第二节） |
| logic.html 口径同步 | ✅ 已同步（lg-bt 子页） |

---

## 二、本次改了什么（改动面一次列全）

### A. index.html

| # | 位置 | 动作 |
|---|---|---|
| A1 | `__btOverviewHTML` | 新增「**最佳持有期**」计算：跨 `BACKTEST_ALL_ALGOS.ranking_all` 全持有期扫描，**胜率降序 → 收益降序**取胜出档，**样本必须 ≥ min_samples(30)** |
| A2 | `__btOverviewHTML` 一句话结论 | 主语由「胜率最高」改为「**最佳持有期**」：`X 最强，最佳持有 N 日，胜率 P%、平均 +Q%（样本 n）` |
| A3 | 主对比表 | 末列 `源更新时间` → **`最佳持有期`**；表头 6 列对齐 image#2 |
| A4 | 主对比表标题 | 补口径说明：「样本/胜率」=各自主口径；「最佳持有期」=跨全部持有期扫描取胜率最优档（样本≥30） |
| A5 | `🧮 全算法回测汇总 · ① 卡级主表` | **整块删除**（与主对比表同源同列）；该块其余（分档阶梯/下架提醒/观察名单/口径说明）**保留** |
| A6 | `📊 各选股策略回测汇总` | **整块删除**（4 策略主表 ↔ 主对比表重复；`🏆胜率最高/💰收益最好` 双卡 ↔ 一句话结论重复） |
| A7 | 新增 `🧬 池内跟踪（非信号回测）` 小表 | 承接 A6 中**唯一不重复**的「选股生命周期 / 因子实验室」两行，**不丢信息** |
| A8 | `dst.innerHTML` 的 `<details>` | 加 `open`，`▸` 改 `▾` + 文案「（已默认展开）」 |
| A9 | AI速览 日/周/月 3 处 `<details>` | 全部加 `open` |
| A10 | 原始条目附录 `<details>` | 加 `open`，文案「点击展开」→「已默认展开」 |

### B. logic.html（lg-bt 子页）

| # | 动作 |
|---|---|
| B1 | 结构表 ①② 补「最佳持有期」口径说明（五要素齐全） |
| B2 | 结构表 ③④ 改：③ 增列「最佳持有期」；④「默认折叠」→「**默认展开**」 |
| B3 | 「为什么重写」段尾追加「**2026-09-12 二次重排**」说明表（逐条列改口径/去重删表/默认展开） |
| B4 | 「口径提醒」段同步：明确「主口径 / 最佳持有期 / 专项持有期」**三段口径不混用** |

---

## 三、⏳ 为何线上还没生效 —— 跨层一致性闸门阻断（**与本次改动无关**）

### 3.1 事实链

```
16:11:41  8156d3160  feat(v8): 策略回测页「方案一·结论先行」重排 ...   ← 我推的
16:11:51  ☁️ v8 构建部署(云端ubuntu) on 8156d31604 → cancelled       ← 被后来的提交挤掉
16:12:10  7cb0dfad7  fix(v8): 金股池(gold_pool)推送断链一劳永逸修复   ← 另一台机/另一任务
16:12:20  ☁️ v8 构建部署(云端ubuntu) on 7cb0dfad70 → failure         ← 真正失败在这里
16:14:32  564fa2ab6  docs(ops): 交接单 — 金股池推送断链...
```

### 3.2 失败步骤与报错原文（step「📤 部署到 main」之前的一致性闸门）

```
🔍 跨层一致性校验完成：检查 73 对，缺失 raw 1 个，缺失 data 0 个，无法校验 2 个，真错位 1 个 ...
  ⚠️ 缺失 raw_data（数据源未产出）:  - HB_XIAOJIU
  ⚠️ raw_data 无日期字段，无法校验:  - SECTOR_PHASE_HISTORY: raw=None data=2026-09-12 20:38
                                     - INDEX_HISTORY:       raw=None data=2026-09-13 00:13:32
  ❌ 真错位（data 比 raw 新 / 任一侧无日期）——任何时段都不放行：
      - AI_MARKET_BRIEF: raw=2026-09-12 15:40:06 vs data=2026-09-13 00:13:32
🛑 真错位 1 项，阻断部署/推送。
##[error]跨层一致性校验失败：data/*.js 与 raw_data/*.json 不一致或有陈旧，禁止部署
```

### 3.3 判定

- **阻断源是 `AI_MARKET_BRIEF`**：`data/AI_MARKET_BRIEF.js`（00:13:32）比 `raw_data/ai_market_brief.json`（15:40:06）新 —— 典型的「链尾整文件 PUT 回退主线手改」形态。
- **不是我的改动引起**：我改的是 `index.html` / `logic.html` 两个**纯前端文件**，不碰任何 `data/*.js` 或 `raw_data/*.json`。
- **会自动收敛**：`☁️ v8 盘后算法链(云端)` on `7cb0dfad70` 当时仍 `in_progress`，跑完会重新生成 data 并触发新一轮构建；按既有经验，**当晚红灯不会立即变绿**，需等修复后跑一轮才收敛。
- **铁律遵守**：**不得为变绿放宽闸门**（主人 09-12 明令）。

### 3.4 给小九的待办

1. 盯 `☁️ v8 构建部署(云端ubuntu)` 下一轮是否 `success`；
2. 若仍被 `AI_MARKET_BRIEF` 真错位阻断 → 按既有口径处理「链尾整文件 PUT 回退主线手改」根因（让 raw 与 data 同源同刻）；
3. 构建成功后，hard-refresh 线上，确认三卡与「最佳持有期」列渲染正常。

---

## 四、改后三件套核验（全绿）

| 项 | 结果 |
|---|---|
| ① inline `<script>` `new Function` | index 25 块 **0 错**；logic 6 块 **0 错** |
| ② 标签闭合（剥注释后） | index `details 6/6 · summary 6/6 · table 30/30` ✅<br>logic `details 2/2 · summary 2/2 · table 76/76` ✅ |
| ② 附：`<details open>` | index **6/6 全部展开** ✅ |
| ③ `data/*.js` 完整性 | **103** 个，全部含 `window.` 前缀 ✅ |
| ④ workflow YAML | **30** 个，含 cron **21** 个，解析失败 **0** ✅ |
| ⑤ 结构断言 | **10/10** 全通过（最佳持有期列头 / 一句话结论改口径 / 旧卡级主表已删 / 旧汇总渲染代码已删 / 双卡已删 / 池内跟踪保留 / 完整明细展开 / AI速览3处展开 / 原附录展开 / logic 二次重排段） |

**验证环境**：补丁先在**仓库外** `_test/` 副本施加并跑通全部校验，再用 `atomic_patch_push.py` 推（**全程未 `git add`**，遵守双机铁律）。

---

## 五、补丁落点（可复核）

| 文件 | 说明 |
|---|---|
| `atomic_patch_push.py` | 原子推送器（本机 `C:\Users\HH20210606\WorkBuddy\2026-08-28-19-46-49\`） |
| `patch_bt_plan1.py` | 补丁主体（16 项编辑 + `patch_index` / `patch_logic`） |
| `patch_bt_plan1_push.py` | 推送器适配层（`FILES` + `apply()` 返回**路径列表**） |
| `_test_patch.py` | 本地副本测试（16 OK / 0 MISS；13/13 自检） |
| `_verify3.py` | 改后三件套核验器 |

**行尾守恒**：`index.html` / `logic.html` 基底均 **LF**（`hash-object --no-filters` 后 CRLF 行数 = 0）✅

---

## 六、遗留 / 待拍板

1. **主对比表是否保留「平均收益」列**：image#2（方案一）原图是 6 列（无平均收益），我保留了 7 列（多一列平均收益，信息更全且不冲突）。若主人要求**严格对齐原图**，删该列即可（一行改动）。
2. **相对强度·最佳持有期显示 `1 日 44.3% / -0.09%`**：因按「胜率优先」扫描，RPS 的 T+1（n=176，44.32%）胜率高于 T+3（41.07%）/ T+5（19.44%）。**规则正确但观感偏弱**，是否改为「胜率×收益」综合打分需主人拍板。
3. **`真错位 AI_MARKET_BRIEF`**：本轮由闸门自动拦截，未做手工干预（遵守「不为变绿放宽闸门」）。
