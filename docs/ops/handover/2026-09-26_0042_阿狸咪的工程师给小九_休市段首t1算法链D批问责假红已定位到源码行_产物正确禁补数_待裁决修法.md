# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-26 00:42｜休市段首t1算法链D批问责假红已定位到源码行_产物正确禁补数_待裁决修法

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

**中秋休市段首（09-25 t1）的 algo 链连续 5 档 run 转 failure，全部只在 step17「结果问责」失败；产物内容零缺陷（同一步内的 `verify_chain_outputs.py` 判「新鲜 13 / 必需项失败 0」全绿）。根因已定位到 `.github/scripts/v8_stage_gate.py` 的源码行：t1 段首把「数据日」定为 09-25（休市日），而 D 批（final_recommend）的就绪门控按上一交易日口径必挂 ⇒ 拒绝产出（这一拒绝是正确的）⇒ 问责却把「合规拒绝」判成 run failure。属假红灯，非数据事故，禁补数。修法涉 workflow 与告警分流，建议 owner 拍板后施工，本轮只登记未改码。**

---

## 正文

### 0. 本轮铁律遵守声明
| 项 | 做法 |
|---|---|
| 判「是否真更新」 | **只读产物 `update_time` + step 内产物完整性闸门输出**，未把 run/step 的 conclusion 当正面判据 |
| 判「修复是否生效」 | run `head_sha` 对照修复提交 sha（`1052d4334162...main` = ahead 332 / **behind 0**；`316d4c878b` 之后的档 step11 不再失败） |
| 派发前查重 | 三链（cn_fetch / algo / build_deploy）未完成 run 计数全 **0** ⇒ 本轮 **0 派发** |
| 数据侧 | **0 改 data/ 产物、0 改算法逻辑、0 手工重算** |

### 1. 现场（云端真值，非本机副本）
- 主站 `https://ah-quant999.github.io/quant-scanner-v8/` = HTTP **200** / **1,361,458 B**（与前 5 轮恒定）。
- 两红卡仍停：`raw_data/experiment.json` ut **2026-09-22 18:13:35**、`raw_data/sector_leaders.json` ut **2026-09-22 18:15:13**（age ≈78.4h）——中秋休市窗口内**属预期**，下一自然恢复判据 = **09-28（周一）post_close**。
- `raw_data/algo_run_report.json` / `algo_heartbeat.json` ut **2026-09-26 00:18:15**（新鲜）。
- 远端 main tip `cdc8692c65`（`v8 build: 2026-09-26 00:25`），推送活跃，无顶掉迹象。

### 2. 新问题：休市段首（t1）algo 链 D 批问责必假红
**run #2110**（id `36158426548`，head `b9c15be31cf2`，CST 00:04 起）job `algo` = failure，失败步固定 = **step17**。

#### 2.1 逐字栈帧（step17）
```
问责输入: 目标批=D 闸门放行=true 执行标志=true 算法步骤=success 数据日=2026-09-25(t1) CST=00:22 B就绪=10/12(true) D最终推荐=0/1(false)
##[error]D 批跑完但 FINAL_RECOMMEND 仍非本数据日盘后产出（闸门裁决=未就绪 0/1，执行=success）—— 假成功，必须排查
```
**同一步内**（关键反驳证据 —— 产物本身没问题）：
```
✅ 新鲜 13 / ❌ 必需项失败 0 / ⚠️ 时间戳盲区 0 / 共 14
✅ 闸门通过：全部核心产物均为本交易日产出
```
同 job 环境变量：`V8_DATA_DATE: 2026-09-24`（cn_fetch 侧算出的数据日**正确**），而问责口径里 `数据日=2026-09-25` ⇒ **同仓两处日期口径不一致**。

#### 2.2 根因链（已逐行读源码）
1. `.github/scripts/v8_stage_gate.py` `chain_day()`（L591-619）：09-25 非交易日、09-24 是交易日 ⇒ 命中 `t1` 分支 `return d, "t1", f"T+1 数据日 {d}（上一交易日 {_last_trading_day(d)}）"` ⇒ **数据日 = 段首 09-25**。此**符合主人 2026-09-11 规则「假期第一日跑」**，非 bug。
2. 同文件 `check_ready()`（L504-545）：只对 `run_day != day` 放宽「产物戳落运行日也 OK」；`_cand()`（L415-427）把 hour<9 的戳归**前一自然日**。
   - B 批产物戳 `2026-09-26 00:08` → 归 **09-25** == day ⇒ `ready_B=10/12(true)` ✅
   - D 产物 `FINAL_RECOMMEND_DATA.js` 戳 `2026-09-25 03:01` → 归 **09-24** ≠ day(09-25) ≠ run_day(09-26) ⇒ `ready_D=0/1` ❌
3. D 本轮**根本没跑**（这不是 bug，是正确保护）：`raw_data/algo_run_report.json` 逐字
   `"failed_scripts":[{"script":"final_recommend.py","reason":"就绪门控未通过（上游选股输入陈旧/缺失）"}]`
   + 日志 `⏭️ 跳过 final_recommend（就绪门控未通过，本轮不产出最终推荐）`。
   A 批输入明细（逐字）：`inst_trade.json=STALE(2026-09-24 16:52)`、`lhb_data.json=STALE(2026-09-25 02:34)`、`AVG_PRICE_DATA.js=STALE(2026-09-24 11:30)`、`ETF_NET_SUBSCRIPTION.js=STALE(2026-09-24 23:51)` —— 全是**上一交易日**口径，与「链数据日 09-25」天然不匹配 ⇒ **休市段首必挂**。

#### 2.3 定性
- **拒绝产出 D 是正确的**（不得用 T-1 输入造最终推荐）；**错的是把「合规拒绝」判成 run failure**。
- 属红线族假阳性（与 `all-generic-redline-false-positive` 同族）：**禁当数据事故、禁补数、禁手工造 final_recommend**。
- 复发面：**每个周六 + 每个假期首日**（t1 段首）必现，年均 ≥50 档 ⇒ 真 P0 易被此红淹没。
- 连档：#2106~#2110 五档同因。（#2106/#2107 另叠加 step11 `update_v8` `NameError: obj_had_ts`，已由 `316d4c878b` 治本，step11 此后不再失败。）
- 部署影响：**无**。失败发生在链尾（推送已完成），`build_deploy` #8283~#8288 均 success。

### 3. 待裁决的修法（本轮**未施工**，只登记）
改 `.github/workflows/**` 须 **classic token**，且本改动涉「告警分流」语义 ⇒ 建议 owner 拍板后施工：
- **(A) 问责侧分流**（推荐）：`v8_algo_run.yml` step17 在 `kind=t1` ∧ D 未产出 ∧ D 现存产物数据日 == `_last_trading_day(day)` 时，输出 `::notice`（与 `v8-stage-gate-reject`「合规拒绝、不告警」同族）而非 `::error v8-algo-stale`。
- **(B) 闸门侧**：`chain_day()` 在 t1 段首**不下发 D 批**（D 纯盘后产物），只下发 A/B。
- 两案都**不得**放宽 `check_ready` 的 floor / 戳判据（否则会把真陈旧放过 = 制造假绿）。

### 4. 验证口径（交下一位）
- 下一周六或假期首日档：algo run 的 **step17 conclusion == success**（或至少不再出现 `v8-algo-stale`）；
- 且 `raw_data/final_recommend.json` 的 `update_time` **未被伪造推进**（须仍等于上一交易日盘后产出时刻）。

### 5. 本轮顺带复验（另两条 HANDOFF 项的证据，已同批写入 HANDOFF.yaml）
1. `experiment-card-stalled-3d-0925`（doing）：09-26 档 cn_fetch（#2289 / head `f781f00b4203`）日志**不含** `v8-experiment-no-data` —— 原因不是护栏失效，而是 **step9「抓中国数据」被交易日历闸门 skipped**（休市）。⇒ 验证**顺延至 09-28（周一）post_close**。
2. `card-fake-freshness-timestamp-rootfix-0925`（pending-verify）：以铁证提交 `0802df64e81c` 的同一批 17 个 `data/*.js` 为样本，比对修复提交 `316d4c878b` 之后近 30 笔 main 提交的 diff：
   - ✅ `data/SECTOR_LEADERS.js` 修复后 **0 次变更**（修复前每轮 +1/-1）⇒ 根因①已根治；`EXPERIMENT.js` / `FINAL_RECOMMEND_DATA.js` 幂等跳过生效。
   - 🔴 **仍有 5 个同族产物「内容零变化、update_time 照刷」**：`COMMODITY_ELASTICITY.js`×12、`IMA_STRONG_BACKTEST.js`×12、`MOMENTUM_FILTER.js`×11、`AUDIT_TRAIL.js`×9、`SENTIMENT_CYCLE.js`×8（另 `HB_ALIMI.js`×2）；全部形态 = `+1/-1` 且唯一变化行是 `update_time` ⇒ 主人仍会被「更新于今天」误导。**本项不得转 done。**
     ⚠️ 写戳位置本轮**未逐一读源码**（禁凭推断），需逐脚本定位后按 `fetch_sector_leaders.py` ① 同口径收口。

### 6. 本轮写操作
仅 1 笔推送（本交接件 + `HANDOFF.yaml` 三项改动 + `HANDOFF.ledger.json`），Git Data API 六步范式、**远端为基底**、漂移断言可 `sys.exit` 阻断、推后逐字节回读。
**未派发任何 workflow、未改代码 / 数据产物。**
