# 九宝量化 v8.0 · 周末交接（小九 → 阿狸咪）2026-07-31

## 一、线上地址与刷新
- **GitHub Pages**：https://ah-quant999.github.io/quant-scanner-v8/
- 每次改 `index.html` push 到 `main` 后，Pages 会自动重建（约 1~2 分钟）。
- 本地看效果要 **Ctrl+F5 / Ctrl+Shift+R** 硬刷新，缓存很顽固。
- 每张卡右上角有时间戳，看一眼就知道数据新不新。

## 二、今日已完成并上线的改动（最新 commit `dacbd8c`，已在 main）

### 1. 真实数据接入
| 卡片/区域 | 数据源 | 状态 |
|---|---|---|
| 精选预判信号 | W52 历史新高(BK0501) / 涨停池 / 抱团板块 | ✅ 真实 |
| IPO 打新日历 | 东财 datacenter + 同花顺 | ✅ 真实 |
| 中金所持仓 | 最近交易日动态回退 | ✅ 真实 |
| 危机雷达 PMI | 真实 49.4 | ✅ 真实 |
| 科创板指数卡 | 东方财富 push2 实时行情 | ✅ 真实 |
| 概念资金热力图 | 东方财富 push2delay 概念排名 | ✅ 真实 |
| 涨停热力矩阵 | 近10日×板块涨停家数日历热图 | ✅ 真实 |
| 板块资金三合一 | 东方财富行业板块资金流 | ✅ 真实 |
| 上证+深证成交额 / 涨跌家数 | 历史累积数据 | ✅ 真实 |
| ETF 资金热度 / ETF 净申赎 | 真实数据 | ✅ 真实 |

### 2. UI/布局调整
- 顶部 tab 导航行 **sticky 固定**，滚动时始终可见。
- 实时指数区新增 **科创板 / 科创50** 卡，共 4 卡。
- 概念区删除重复的「概念资金排名 TOP15」列表卡，只保留 **概念资金热力图 treemap**。
- 涨跌家数统计图高度改为整张显示（260px）。
- 涨停热力矩阵改为**整行宽度**，不再并排。
- 删除「涨停联动·连板强度」卡（数据拿不到且与涨停池重复），同步清理 `fetch_limit_up_ladder.py`、`LIMIT_UP_LADDER.js`、workflow 步骤。
- 运维面板 opTab3/opTab4 精简为轻量 2+6 卡。

### 3. 已清除的占位/死代码
- 全站「待接入」占位文案已清零。
- 精选预判信号 fallback 改为「信号数据为空」。
- 四路资金删除「待接入」占位行。
- 涨停池空状态改为「涨停池数据为空」。

### 4. 防「已下架数据复活」修复（commit `dacbd8c`）
- 已删除的「涨停联动·连板强度」数据 `LIMIT_UP_LADDER` 被 CN runner 持久工作区推回。
- 已修复：`cloud_fetch_v8.py` 每次运行前清理旧 `raw_data/*.json`；`update_v8.py` 不再为未知 json 自动推断变量名。
- 若未来再下架某个数据模块，同步做三件事：① 删 HTML/JS 渲染 ② 从 `DATA_SOURCES` 移除映射 ③ 从 `cloud_fetch_v8.py` 移除抓取函数。

## 三、数据流水线（全自动，无需手动部署）
```
[中国机器 cn runner · v8_cn_fetch.yml]
    每日 08:25 / 10:30 / 11:30 / 14:00 / 15:05 / 16:30 (CST)
    ↓ 抓取中国源 → raw_data/*.json → push main
[GitHub ubuntu · v8_build_deploy.yml]
    raw_data/** 变动自动触发
    ↓ update_v8.py → data/*.js → push main → Pages 重建
```
- 手动补救：Actions → `v8_cn_fetch` → Run workflow；等 2 分钟再点 `v8_build_deploy`。

## 四、阿狸咪周末改版规则（⚠️ 重要）
- ✅ **可以改**：`index.html`（UI/前端逻辑）、`cloud_fetch_v8.py`（抓取逻辑）、`update_v8.py`（转换逻辑）、`.github/workflows/*.yml`。
- ✅ **改完直接 commit + push main 即可**，Pages 自动发布。
- ⛔ **不要手动跑** `deploy_v8.py` / `api_push_raw.py` / 任何带 "deploy" 的旧脚本——新架构全自动，手动跑会冲突。
- ⛔ **不要删除** `data/*.js`（即使看起来旧），它们是构建产物；想清数据就清空 `raw_data/*.json` 或改 workflow。
- ⚠️ 改 `index.html` 前先看一眼文件大小，目前约 300KB/4000 行；持续增长时要警惕臃肿（见第六节）。

## 五、已知问题/待补（非阻塞）
- 北向资金：港交所 2024-05 起停披露，卡片显"停止披露"。
- 全球危机指标 VIX/美10Y/ISM：中国大陆网络被墙，拿不到，已诚实标注。
- 龙虎榜「机构/游资」四路资金原始口径暂无公开源，当前用个股主力净流入口径替代。
- 大盘资金流历史序列待日后累积。

## 六、防臃肿控制（v8 不要走 v6 老路）
当前 v8 体格（供参考）：
- 仓库总大小：约 5.2 MB
- `index.html`：304 KB / 4088 行
- `data/*.js`：46 个文件 / 1.2 MB
- `raw_data/*.json`：约 130 KB

控制措施：
1. **单文件上限**：`index.html` 超过 500KB/6000 行时必须拆分模块或抽离 HTML 模板。
2. **数据文件上限**：单个 `data/*.js` 超过 500KB 时要考虑分页/压缩/按需加载。
3. **每日构建产物**（`data/*.js`）不入主仓库手动管理，由 workflow 自动生成，避免人工堆积。
4. **定期清理**：每月检查一次是否有过期/弃用的 `raw_data/*.json` 与对应 `data/*.js` 映射。
5. **图片/静态资源**统一放 `assets/`，禁止 base64 大图内联进 `index.html`。

## 七、紧急处置
- 站点空白/报错：先硬刷新；仍有问题 → `git log` 看最近谁改了 `index.html` → `git revert` 该文件到上一版。
- 数据全停：查 Actions 里 `v8_cn_fetch` 与 `v8_build_deploy` 最近运行是否失败。
- 遇到合并冲突：因 CN runner 会不断推送数据 commit，push 前/后先 `git fetch origin && git merge origin/main`。

## 八、本机工作区
- v8 仓库：`E:\workspace\quant-scanner-v8`
- Python：用 `C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe`
- 本地预览：`python -m http.server 8766` 后用浏览器开 `http://localhost:8766/index.html`

---
**交接人**：小九  
**接收人**：阿狸咪  
**日期**：2026-07-31  
**当前线上 commit**：`65f1cd7`（后续 CN runner 会在上面继续叠加数据 commit）
