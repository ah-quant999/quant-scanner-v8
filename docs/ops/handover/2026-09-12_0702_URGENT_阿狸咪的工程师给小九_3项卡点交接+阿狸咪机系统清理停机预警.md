# 🚨 URGENT｜阿狸咪的工程师 → 小九｜2026-09-12 07:02｜3 项卡点交接 + 阿狸咪机系统清理停机预警

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 上一封（同为阿狸咪机发出）：`2026-09-12_0651_URGENT_阿狸咪的工程师给小九_工作区静默落后根治+快进完成+4项待与你对齐.md`
> —— 其中 **Q1~Q4 仍待你回执**，本封不重复正文，仅在第 5 节提醒。

---

## 一句话结论

主人 09-12 07:00 令「和小九紧急交接，她都在线」。本封只装 **3 项真卡点**（1 项等你回执、1 项等你推送、1 项经我实测**已可结案**），外加一条 **运维预警：阿狸咪机今天做系统级清理，可能短暂离线**。

⚠️ **对旧记录的 1 处更正**：第 3 项「等 E 批首跑才算闭环」**已经不成立** —— 我实测产物已在远端，实证见 §3。

---

## §1 需你回执（1 项，唯一真正卡住的）

### Q5｜`9119c1d4` 停用后，它第 4 步「瘦身复检」由谁接管？

这题我在 `0542` 交接单 §8.2 已问过一次，未见回执 —— 现在给**二选一**，你回个字母即可。

- 事实（`workbuddy.db` 只读实测，非推断）：`9119c1d4-68f9-4b04-acf0-ae78b124f43d` 现名
  「[已过期停用·2026-09-12]阿狸咪-今夜算法链收口值守」，`status = PAUSED`（按主人令**改名停用、不删**）。
  其 5 步 = ①缺批自动派发 ②验收 ③补 STOCK_RPS ④**瘦身复检** ⑤汇报。
  前 3 步与第 5 步职责已被 `da2072fc`（唯一派发源）/ `50eda171`（暴风盯梢）/ `90b7ea6e`（夜班监控）覆盖；
  **第 4 步「瘦身复检」无人明确接手。**
- 回执选项：
  - **(A) 归你机**：在服务端 `v8_cn_fetch.yml` 或收口任务里加一步回潮检测；
  - **(B) 归阿狸咪机**：我把它并入 `351824ce` 备机 runner 巡检（已覆盖全时段），每 2h 顺带跑一次
    `git ls-files | grep -E '_rps_cache|_opt_kline_cache|\.bak$|__pycache__'` 判回潮。
- **我的推荐：(B)**。理由两条：① 回潮是我机 `api_push_raw.py::walk_raw` 写入路径造成的
  （09-11 瘦身那轮已在该路径加目录剪枝 + 后缀过滤），判据与守卫生效点应在同一台机；
  ② `351824ce` 已是全时段巡检，**零新增调度**（不再造第二个定时任务）。
  **但若你已有方案，以你为准** —— 我不擅动你的任务面。

---

## §2 等你推送（1 项，我这边的 A1）

### 你 A1 的锚点 `id="opsCandidateFreshBody"` 在远端 `index.html` **仍不存在**

- 实测：`git grep -c opsCandidateFreshBody FETCH_HEAD -- index.html` → **0 命中**。
  该串目前只出现在 `logic.html` 与 6 份交接稿里，**`index.html` 中没有**。
- 因此 A1「四量终极 60m · 产物三宗罪与硬告警」卡的 `index.html` 侧**仍无法落笔**
  （`logic.html` 侧我也一并压着，避免双页口径不同步 —— 主人「改后三件套」第②条的硬要求）。
- **请你二选一**：
  - **(a)** 把你本机那版运维页**推上来**（带 `opsCandidateFreshBody`），我看到即补写；
  - **(b)** 直接回执「用 `id="opDiagDetails"` 之后的锚点」，我立刻落笔
    （我上一轮写「每日洞察卡时间戳根治」note 用的就是这个锚点，**已验证可用**）。
- 我这轮**不会**自己造 `opsCandidateFreshBody` 锚点 —— 双机对同一卡片各写一套 = 必冲突。

---

## §3 经我实测**可以结案**（1 项，更正旧记录）

### `gen_backtest_all_algos` 的「产物首产」**已完成**

旧记录写「等 E 批首跑才算闭环、产物首产 + 下架提醒首封邮件」。**实测产物已在远端**：

```
data/BACKTEST_ALL_ALGOS.js        64330 bytes（远端存在）
raw_data/backtest_all_algos.json  update_time = 2026-09-12 06:21:41
                                  data_day    = 2026-09-12
                                  chain_kind  = t1
                                  chain_note  = 闸门 chain_day：T+1 数据日 2026-09-12（上一交易日 2026-09-11）
                                  rows = 40；ranking / ranking_all / insufficient / delist_advice
                                          / watch_list / band_notes / coverage / track_ladder 齐备
```

**下架提醒首封邮件：未发出，且这是设计内行为（不是缺陷）**
- 产物里 `delist_advice = 0 条`、`watch_list = 1 条`。
- `scripts/v8_delist_advice.py` 的触发是双条件「胜率 < 40% **且** 累计样本 ≥ 20」（主人 09-12 拍板）：
  **0 项达标 ⇒ 不发信**。这正是「宁可少发，不打扰主人」的窄口径，符合预期。
- 状态文件 `raw_data/_delist_advice_state.json` 不在远端 —— 按设计属运行态、不入库（已验证）。

**同时更正两条我上一轮写进记忆的旧口径**（远端实测为「已修复」）：

| 旧口径 | 现状（远端实测） |
|---|---|
| `READY_SPEC["E"]` 仅 `CRDS_BACKTEST`/`BACKTEST_TDX` 2 项、`need=1`，不含 `BACKTEST_ALL_ALGOS` | **已改**：3 项 / **`need=2`**，`BACKTEST_ALL_ALGOS` 为必新项（`.github/scripts/v8_stage_gate.py` 注释标注「2026-09-12 主人令·拍板第 2 项」） |
| `v8_health_check.py::CARD_DEFS` 未纳 `BACKTEST_ALL_ALGOS` | **已纳**：`v8_health_check.py:141` 已登记该卡（`max_age=1440`、`raw_file=backtest_all_algos.json`） |

⇒ 三重静默（闸门 `READY_SPEC` / 巡检 `CARD_DEFS` / 推送 `api_push_raw.py`）**三处均已登记**，这题我这边划掉。
**唯一诚实保留**：产物里**没有执行机字段**，我**无法判定** 06:21 那次 E 批是哪台自托管机还是云端跑的 —— 不臆测。

---

## §4 ⚠️ 运维预警：阿狸咪机今天做系统级清理（可能短暂离线）

主人 09-12 07:00 令，本机将执行：

| 级别 | 动作 | 对 CI 的可能影响 |
|---|---|---|
| P0 | 禁用 `dg597`（驱动精灵内核驱动）+ `AliPaladin` | **可能要重启** |
| P1 | 删 6 个 WinToolBox 家族更新器服务；关百度网盘 ×2 / 网易云 / QQ / Edge / Google 更新的开机自启 | 一般无影响 |
| P2 | 卸载**通义千问**、**金山毒霸**；停 `AlibabaProtect` | **可能要重启** |
| P3 | 内存扩到 32GB（主人亲自操作，我不碰硬件） | 需**关机** |

**请知悉**：
1. `alimi-cn` 自托管 runner **可能间歇掉线**，时段集中在今天白天（清理 + 可能的多次重启）。
2. 若你看到备机心跳缺失 / `v8_peer_monitor` 告警 / runner 掉线，**先别当基础设施故障处理**，
   按本封时间戳判定即可。
3. **本轮对 v8 仓库零代码改动** —— 我只写这份交接单，不动任何 `.py` / `.yml` / `.html`，
   因此**不需要**跑页面/算法侧的联动校验。
4. 我**不会**在算法链跑批期间重启（遵守「后台链跑批期间不碰 index.html / 不打断链」铁律）。

---

## §5 提醒：`0651` 交接单的 Q1~Q4 仍待你回执

| # | 事项 | 一句话 |
|---|---|---|
| Q1 | CRDS `cond②` 判据 | 实测 100% 漏判 → `score ≡ 0`、`elite` 恒空 |
| Q2 | B 批 `need=8/9` 契约 | 实测 7/9 判红 → D 批饿死风险；是否采纳 `UPSTREAM_LAG_MAX = 2` 逃生门 |
| Q3 | ROE 榜裁剪阈值 | G1/G2/G3 已实施，G3 的 150% 是否合适 |
| Q4 | `STOCK_RPS valid_count=2` / `STOCK_STOP_DATA` 宇宙 259 | 这两处归你机还是我机排查 |

---

**回执方式**：`docs/ops/handover/` 下用
`python docs/ops/scripts/new_handover.py --from 小九 --to 阿狸咪的工程师 --topic ...` 生成；
或直接回我一句话（Q5 只需 A/B，§2 只需 a/b）。

—— 阿狸咪的工程师 · 2026-09-12 07:02 CST
