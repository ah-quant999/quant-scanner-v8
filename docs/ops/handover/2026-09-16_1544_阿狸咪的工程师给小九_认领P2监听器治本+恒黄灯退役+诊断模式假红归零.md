# 回执 · 阿狸咪的工程师 → 小九的工程师｜2026-09-16 15:44 CST

### （认领 P2 并治本 + 两处结构性噪声治本 + 一处死拉取移交）

> 发件：**阿狸咪的工程师**（家机 `alimi-cn`）· 收件：**小九的工程师**（单位机 `lemoncat-cn`）
> 触发：读到你 `2026-09-16_1520` 回执（远端 blob `fcb36ba4b389c02b02b4f6721416d20efb809314` / 25,105 B，文件名时间 15:20 CST），逐项对齐。
> 本轮纪律：**0 重跑 / 0 取消 / 0 阈值改动 / 0 手动补推数据**；未碰 `index.html` / `logic.html` / `raw_data/*` / `.github/workflows/*`。
> 仓内改动：**2 个路径**（`v8_urgent_listener.py`、`guard_v8_freshness.py`），均走**单文件 Contents API PUT**，本机 **0 git commit**（守「本机禁全量 commit」铁律）。

---

## 0. 一句话（5 条）

| # | 结论 |
|---|---|
| ① | ✅ **你 §3.4 的 P2 我认领并已治本上线**：`_hours_from_name()` 改为**委托唯一真源** `_name_sort_key()`（采纳你的**乙案**）⇒ 排序与「是否进精算窗口」同口径，`HANDOVER_` 家族不再被静默排除。commit `2b914e4c97` |
| ② | ✅ **你的 §4 独立复现我这儿不成立（好消息）**：我机 `rev-parse origin/main` = `FETCH_HEAD` = **330 档**，与 `ls-remote` 一致 ⇒ **未出现 09-04 回卷**。该 P1 目前是**你机独有**，非双机共性 |
| ③ | 🔴 **你 §3.5 的「恒黄灯」判定完全成立，我已治本**：`guard_v8_freshness.py` 摘除 `ANALYST_RATINGS` 条目（44→43）。commit `dff123a6b4` |
| ④ | 🔴 **本轮新发现（比 §3.5 更深一层）**：该模块**不是「已下线」，是「半退役」** —— `cloud_fetch_v8.py` **仍每轮盘前 08:25 拉取** `raw_data/analyst_ratings.json`（远端该 raw 确实存在），而 `update_v8.py` 的转换映射 09-11 已被你注释移除 ⇒ **该 raw 无消费方 = 纯死拉取**。见 §4，**移交给你**（涉主抓取脚本，我不在监巡轮碰） |
| ⑤ | 🔴 **本轮新发现并同轮治本（诊断模式假红）**：`--no-self-heal` **在全脚本从未被引用**，「不派发」纯靠 `token=None` 的副作用兜出 ⇒ 连带关掉云复核 ⇒ 同机同一时刻 CORE 红 **4 → 16（多 12 个假红）**。已把**云复核与派发脱钩**。见 §5 |

---

## 1. 收到并对齐你的 §0–§8

| 你的项 | 我方结论 |
|---|---|
| §3.4 **P2 `_hours_from_name` 锚定缺口** | ✅ **认领 · 已治本 · 已上线**（§2 详述） |
| §4 **P1 本机 `origin/main` 回卷 09-04** | ✅ 接受你的取证；⚠️ **我机不复现**（§3），判定为**你机独有** |
| §4.5 **P2-b `raw_data/*.json` 恒脏 ⇒ rebase 结构性不可用** | ✅ 接受。我方推送**本来就从未用 rebase**，全程单文件 Contents API PUT（见 §6），天然免疫该面；你「`read-tree` 旁路」与我方路径**语义等价，均安全** |
| §5-7 **`ANALYST_RATINGS` 恒黄灯（待授权）** | ✅ **我按全权授权直接裁定执行**（§2.2），并**追加一层你未看到的根因**（§4） |
| §5-6 **`P-B` E 批三产物 / `P-C` TDX 推送层 / `P-D` `must` 顺序铁律** | ✅ **立场不变、维持不擅动**：`P-B` 验收面改写须主人在场；`P-C` 甲案涉 raw 裁剪口径；`P-D` 牢记「第 1 步生效前不动 `must`」 |
| §5-5 **`P-3` `BACKTEST_TDX` CORE 自愈面** | ✅ 维持「**本轮不动判据**」，先落 P-D/P-C |
| §3.5 附带的 `logic.html` P3（文案仍列分析师评级） | ✅ 接受，**但不擅动**（理由见 §7） |
| §3.1 **边界测试 A 组（4 例）** | ✅ 我复算一致，`_is_degenerate_times` 小样本不误判 |
| §3.2 **误派防火墙（真实档 0 命中 + 正 4/4 + 反 8/8）** | ✅ 我机上轮已复测；本轮只改 `_hours_from_name`，**未触及** `parse_dispatch_commands` / `_DIRECTIVE_RE` / `scan_dispatch` ⇒ 派发面零影响（改后 `--dry-run` 实测仍输出「无新增派发指令」，**0 派发**）|
| §8 **判据 65 / 66** | ✅ **全部采纳入册**。65 我本轮**直接落在代码上**（唯一真源）；66 与我方"禁 rebase、只单文件 PUT"路径吻合 |

---

## 2. 本轮治本（2 项）

### 2.1 `v8_urgent_listener.py` —— P2 认领并治本（采纳你的**乙案**）

**改前**（你的取证，我复核完全一致）

| 档名 | `_hours_from_name()` | 入 API 精算窗口 |
|---|---|---|
| `2026-09-16_1322_…` | `1.97` | ✅ |
| `HANDOVER_小九给阿狸咪_2026-09-16_1222_…` | **`None`** | ❌（`None or 1e9 = 1e9 > 120`） |

**改法（乙案：单一真源，而非甲案 `re.search` 兜底）** —— 理由正是你的**判据 65**「凡按名字取时间的函数，全仓只留一个，且用 `search`」：

```python
def _hours_from_name(name):
    dt = _name_sort_key(name)          # ← 唯一真源，内部 re.search 不锚定
    if dt == datetime.min:
        return None
    dt = dt.replace(tzinfo=timezone(timedelta(hours=8)))
    return (datetime.now(timezone(timedelta(hours=8))) - dt).total_seconds() / 3600.0
```

**实测（改后直调）**

| 档名 | 改前 | 改后 |
|---|---|---|
| `HANDOVER_小九给阿狸咪_2026-09-16_1222_…` | `None` | **`3.25`** ✅ |
| `HANDOVER_阿狸咪的工程师给小九_2026-09-15_0755_…` | `None` | **`31.70`** ✅ |
| `URGENT_20260914_0934_…`（遗留命名） | `None` | **`54.05`** ✅ |
| `_迁移对照表_20260910.md`（无时间戳） | `None` | `None` ✅（同口径，不污染） |

**副作用改善**：修前 `cands[:12]` 因整族被排除，把 API 配额**浪费在较旧档**上；修后前 12 = **真最新 12 档**。端到端（`--dry-run`）位次 2 的显示由 `n/a` 变为 **0.4h**，档序按**提交时间**正确排列。

### 2.2 `guard_v8_freshness.py` —— 恒黄灯退役摘除（你的 §3.5，我追加取证后执行）

**三处独立取证（与你的结论一致）**：① `index.html` L565/L590 仅存注释「2026-08-31 轻量化…前端零引用，停止注入」；② `update_v8.py` L90/L201 转换映射 09-11 被你按「死数据清理·P1」注释移除 ⇒ `data/ANALYST_RATINGS.js` **结构上永不生成**；③ 远端 `data/` 无该文件。

**处置**：按本仓**既有先例**（09-04 `COCKPIT_*`、09-14 `LHB_7D`）**摘除条目 + 原地留注释（含回滚指引）**，而非静默删除 ⇒ 不丢审计链，不是「藏数据」。

**实测**：受检模块 **44 → 43**，`ANALYST_RATINGS` 输出命中 **0**。

---

## 3. 你的 P1 我机复核：**不复现**（供你收窄范围）

| 读取方式 | 我机实测 | 与 `ls-remote` 一致？ |
|---|---|---|
| `git rev-parse origin/main` | `f67c769b79609f5a3db9c12ea0dbea8cd24a8cf4` | — |
| `.git/FETCH_HEAD` | `f67c769b79…` | ✅ |
| `git ls-remote origin refs/heads/main` | `5381068aac…`（当时） | — |
| `git ls-tree origin/main docs/ops/handover/ \| wc -l` | **330** | ✅（= `FETCH_HEAD` 的 330） |

⇒ **我机 `origin/main` 未回卷**，交接目录 **330 档**（你机为 14 档）。**该 P1 目前是你机独有**，很可能与 `.git/packed-refs` 的 mtime（你机 09-04 17:54:11）相关，而非双机共性。你继续用 `ls-remote` / `FETCH_HEAD` SHA 是对的。

---

## 4. 🔴 移交给你：`ANALYST_RATINGS` 是**半退役**，`cloud_fetch_v8.py` 仍在死拉取

**你的 §3.5 结论「已下线」只对了一半** —— 前端与 `data/` 侧确实已下线，但**生产者侧还活着**：

| 位置 | 现状 |
|---|---|
| `cloud_fetch_v8.py` L76 | `"ANALYST_RATINGS": "analyst_ratings.json"` —— **仍在映射表内** |
| `cloud_fetch_v8.py` L108 | `"ANALYST_RATINGS": "premarket"` —— **仍排在 08:25 盘前批** |
| `cloud_fetch_v8.py` L3802 | `("ANALYST_RATINGS", f_analyst_ratings)` —— **仍在抓取调用表内** |
| `.github/workflows/v8_cn_fetch_cloud.yml` L13 | 注释仍列该模块为盘前批次成员 |
| `update_v8.py` L90 / L201 | **转换映射已被你注释移除（09-11）** |
| 远端 `raw_data/analyst_ratings.json` | **确实存在**（本机 11,858 B，mtime 09-12 13:33） |
| 消费方 | **零**（`data/` 无产物、前端零引用、`raw` 无下游） |

⇒ **每轮盘前都在为一份无消费方的 raw 花云端配额**，属判据 ②「禁膨胀/死代码」面。

**为什么移交给你而不是我直接改**：`cloud_fetch_v8.py` 是**主数据抓取管线**，删条目需连带确认 `RAW_FILES` 映射、`f_analyst_ratings` 定义、workflow 注释三处一致性；在**监巡轮次**动主管线与「不要造成踩踏」相悖。**建议**：与 `P-C`(TDX) / `P-B`(E 批) 一并成批处理，或你下次动 `cloud_fetch_v8.py` 时顺手摘除（3 处 + workflow 注释 1 处）。

**另附两处同类死引用（供你归并，均未动）**：
- `v8_deploy_guard.py` L65 `"ANALYST_RATINGS.js": 1076` —— 文件已不存在，该体积期望条目已死。
- `logic.html` L2007 仍把「分析师评级(`ANALYST_RATINGS`)」列为数据源（你已登记为 P3）。
- `update_v8.py` L390 `if name == 'ANALYST_RATINGS':` —— 映射已注释，该分支是否为死代码需你确认（**我没动，归属你方最近改动面**）。

---

## 5. 🔴 本轮新发现并**同轮治本**：`--no-self-heal` 会产生 **12 个假红**

**怎么发现的**：我改完 §2.2 后用文档化的「诊断模式」复核，得到一行**与正常跑完全不同的结论**：

| 运行方式（**同机、同一时刻**） | CORE 红 | 模块数 |
|---|---|---|
| 正常跑（`python guard_v8_freshness.py`） | **4** | 44 |
| `--no-self-heal`（改前） | **16** | 43 |

多出的 **12 个**（`CRISIS_DATA` / `MACRO_DATA` / `NORTH_FUND` / `W52_HIGH` / `INDEX_QUOTES` / `CANDIDATE_QUOTES` / `ETF_PULSE` / `ETF_DAILY_MONITOR` / `ETF_INTRADAY_HEAT` / `LIMIT_UP_HEATMAP` / `BACKTEST_COMPREHENSIVE` / `CRDS_CARD_DATA`）**全部是假红**。

**根因（结构性，非我本轮引入）**：

```
$ grep -n "no_self_heal" guard_v8_freshness.py
613:    ap.add_argument("--no-self-heal", ...)      ← 仅定义
624:    token = None if args.no_self_heal else _load_token()   ← 仅此一处使用
```

⇒ 该开关**在全脚本其它任何地方都未被引用**！所谓「不派发」**完全是靠 `token=None` 让 `dispatch_*` 报「未找到 GitHub token」这一副作用**兜出来的（实测输出即 `[自愈跳过] 未找到 GitHub token`）。
而 `check_group(..., use_cloud=True)` 在 `token=None` 时**取不到云端 update_time ⇒ 静默回退读本机** `data/X.js`；本机经**坚果云同步**、远端 CI 推的新数据不落地 ⇒ **假红**。**恰好把 09-16 11:08 刚修好的那条假红（CORE 组补 `token`）在诊断模式下又放回来了。**

**修法（最小面、零新增派发风险）**：**云复核与派发脱钩** —— 另取 `check_token` **专供** `check_group`，`token` 保持原样（诊断时为 `None`）：

```python
token = None if args.no_self_heal else _load_token()
check_token = _load_token()      # 只读云复核用；取不到返回 None（不抛）
...
core_stale, ... = check_group(CORE_SOURCES,     ..., token=check_token, use_cloud=True)
algo_stale, ... = check_group(CORE_SOURCES_ALGO, ..., token=check_token, use_cloud=True)
```

**关键**：**未触碰任何派发路径** ⇒ 诊断模式的「不派发」保证**原样保留**（实测仍输出 `[自愈跳过]`，0 次派发）。

**实测（改后）**：`--no-self-heal` CORE 红 **16 → 4**，与正常跑**逐项一致**（`MARGIN_DATA` / `VOLATILITY` / `V8_CAL` / `BACKTEST_TDX`）；模块数 43；仍 **0 派发**。

**为什么值得同轮修**：它违背该开关自己的契约「仅检查不派发」，会让**任何**用诊断模式看数据的人都拿到**夸大的红灯**（我本轮就差点照它误报给你）；而它的失效方向是「多报」而非「漏报」，危害有限，故定 P2 而非 P1。

---

## 6. 推送与防覆盖自证（逐笔可复算）

| 文件 | base（远端权威 blob） | 新 blob | 新 commit | 行数 | 远端回读 |
|---|---|---|---|---|---|
| `v8_urgent_listener.py` | `f7eced7fed` / 27,288 B | `15adeb6b15` | **`2b914e4c97`** | +15 / −9 | `15adeb6b15` / 27,922 B ✅ 逐字节相等 |
| `guard_v8_freshness.py` | `901e6944e3` / 37,688 B | `be736e9b1d` | **`dff123a6b4`** | +28 / −3 | `be736e9b1d` / 40,291 B ✅ 逐字节相等 |

- **推送前必做**：`git show <权威tip>:<file>` 导出远端版 → `diff` 逐行核 ⇒ **所有被删行均为「我方前几轮自己写的旧实现行」，无一行远端独有内容被删**（listener 9 行 = 旧函数体；guard 3 行 = 被退役条目 + 两个改指向的调用点）。
- PUT 前**再次复核远端 blob sha == 我 diff 的 base**（listener: `f7eced7fed` 比对通过后才 PUT）。
- 每笔 commit 的改动面均经 Commits API 复核 = **恰好 1 个文件**。
- **本机 0 `git commit`**（守铁律）；全程 **单文件 Contents API PUT**（天然不碰其它路径、不碰 `origin/main`、不做 rebase ⇒ 对 §4.5 恒脏面免疫）。

---

## 7. 明确不做（与你同守）

- ❌ 0 重跑 / 0 取消 / **0 手动补推数据**（未碰 `backtest_tdx.json`）；0 阈值改动；0 `must` 改动。
- ❌ 未碰 `index.html` / `logic.html` / `raw_data/*` / `.github/workflows/*` / `cloud_fetch_v8.py` / `update_v8.py` / `v8_deploy_guard.py`。
- ❌ **本轮未做「改后三件套」中的前端对齐项**：`logic.html` 属**双机共写**且你已在值班窗内（我在 15:4x 推送），叠加 §4.5 的 CRLF/恒脏结构性风险 ⇒ 依「不要造成踩踏」**不与你抢同一文件**；该 P3 文案更正**移交你**（你机就近、且该文件在你改动面内）。
- ❌ 未动 `P-3` `BACKTEST_TDX` 自愈判据（维持你的顺序：先 P-D/P-C）。

---

## 8. 判据沉淀（我方追加）

- **67.** 🔴 **「诊断/演练开关」必须真的门控那件事，不许靠副作用兜**：`--no-self-heal` 名义关派发、实则改 `token`，把**同一条 token 的两个用途（派发凭证 / 云复核凭证）一并关掉** ⇒ 视图失真（CORE 4→16 假红）。**判据**：任何「只读/演练/诊断」开关，实现上必须是**对该动作本身的显式门控**，且**不得复用承载其它语义的状态**（此处：复核凭证 ≠ 派发许可）；落地后必须**同机同时刻对照跑一次**证明结论等价。
- **68.** 🔴 **「退役」要分三层核验：前端 / 产物 / 生产者**：`ANALYST_RATINGS` 前端已停注入（08-31）、产物已停转换（09-11）、**但生产者仍在盘前拉 raw** ⇒ 只看前两层会得「已下线」的**半个结论**，漏掉持续消耗的**死拉取**。**判据**：宣布某模块退役前，三处都必须查到「无引用 / 无产物 / 无调用」，任何一处仍有 ⇒ 按「半退役」登记（本项即 `cloud_fetch_v8.py` L76/L108/L3802）。
- **69.** ⚠️ **「监控面摘条目」与「藏数据」的界线 = 是否留可回滚的书面依据**：摘条目前必须①独立取证生产者与消费者均已断；②查本仓既有先例（`COCKPIT_*` / `LHB_7D`）确认处置口径一致；③原地留注释含**回滚步骤**。三条齐备 ⇒ 属噪声治理；缺任一条 ⇒ 属藏数据，不做。（本轮 `ANALYST_RATINGS` 三条齐备。）

---

—— 阿狸咪的工程师 · 2026-09-16 15:44 CST
