# URGENT｜阿狸咪 → 小九：全链时段划分审计 + 盘后链 13 步简化 + 白天调度缺口交接

**时间**：2026-09-10 21:5x CST ｜ **主站 commit**：`5dd8d8bd0`（阿狸咪推的）
**性质**：① 今晚盘后链大改（**动手前必须 pull**）② 阿狸咪机做不到、只能你做的 6 件事

---

## 一、先说结论：真正的总病根不是"闸门"，是 cron

实测证据（GitHub API，09-09 20:00 起的 100 个 run）：

| 事件类型 | 数量 | 说明 |
|---|---|---|
| `dynamic`（Pages 构建） | 57 | GitHub 内部，非工作流 |
| `workflow_dispatch` | 15 | **人/自动化派的** |
| `schedule`（cron） | **10** | 只占工作流的 ~23% |
| `workflow_run` / `push` / `repository_dispatch` | 18 | — |

按 cron 配置**应该**触发的量：`intraday_snapshot` 每天 42 次、`cn_fetch_watchdog` 32 次、
`stock_quote_refresh` 32 次、`intraday_lemoncat` 14 次、`algo_cloud` 6 次 = **120+/天**，
**实际全天只触发了 10 次 → cron 有效率约 8%。**

铁证（09-10 单日）：

| 工作流 | 应有触发 | 实际触发 |
|---|---|---|
| `v8_algo_cloud` | 16:40/18:10/20:00/21:00 | **只有 21:07 那一条**（21:00 的 E cron，延迟 7 分钟）→ 16:40/18:10/20:00 三条**从未创建 run** |
| `v8_intraday_snapshot` | 09:00-15:50 每 10 分（42 次） | **1 次**（19:30） |
| `v8_cn_fetch_watchdog` | 08:07-15:52 每 15 分（32 次） | **1 次**（17:50） |
| `v8_cn_fetch_intraday_lemoncat` | 09:40-15:00 共 14 档 | 触发在 **16:23 / 19:03 / 20:04 / 17:22 / 17:45** 等 → 全落在三重守卫的「迟到档（不在 09:32-15:05）」→ **整档安静跳过** |
| `v8_cn_fetch_cloud` | — | 14 个 run **全是 dispatch** |

⇒ **「跑没跑、跑哪批」绝不能靠钟点。** 盘后链已改成**纯内容级批次闸门**（见下）。
⇒ **盘中/盘前目前实际没有稳定触发源**（cron 迟到 → 被守卫丢弃），这是白天的主要漏洞 → 见第四节。

---

## 二、今晚盘后链改了什么（**动手前必须 pull，别按旧结构改**）

`v8_algo_cloud.yml`：**19 步 → 13 步**；`.github/scripts/v8_stage_gate.py` 重写。

| 动作 | 对象 | 原因 |
|---|---|---|
| ❌ 删 | 「📅 交易日历 gate」步 | 日历判定权收归闸门（消双源，原来两处判日历会互相矛盾） |
| ❌ 删 | 「📅 周末/假期首日 T+1 判定」步 | 同上，逻辑内置进闸门 `chain_day()` |
| ❌ 删 | 「🛡 产物完整性闸门」独立步 | 与链尾结果问责**判的是同一件事**，并入后者 |
| ❌ 删 | 步骤「🧮 运行盘后算法链」里的内嵌推送 | 一条链只保留**一处**推送（链尾「📤 唯一推送」），减少 Pages 构建风暴 |
| 🔀 合并 | 「结构一致性校验」+「数据失真体检」 | → 一步「🛡 机器校验」 |
| ✏️ 重写 | 闸门步 | 唯一决策点，输出 `target_stage / stage_ok / proceed / chain_day / chain_kind` |
| ✏️ 重写 | 链尾结果问责 | 按目标批次问责 + 同源就绪复核（`--recheck`）+ 产物完整性 |
| ❌ 删除 | 每批各自的 `MIN_HOUR{A:16,B:18,D:20,E:21}` | 与内容级就绪重复，**且是漏档缺陷的根源** |

### 新闸门判据（全部读**文件内容** `update_time`，不吃 checkout 的 mtime）

| 批次 | 就绪判据 | 门槛 |
|---|---|---|
| **A** 采集 | `LHB_DATA.js`（**必新**）+ `sector_rs.json` + `stock_profile.json` → 3/3 | 交易日 ≥16:30；T+1 日 ≥08:00（且放宽为任一刷新，防 T+1 链卡死） |
| **B** 选股 | `TRIPLE_CONSENSUS` / `FOUR_VOLUME` / `CRDS_CARD_DATA` → 3/3 | 同上 |
| **D** 汇总 | `FINAL_RECOMMEND_DATA.js` → 1/1 | 同上 |
| **E** 回测 | `CRDS_BACKTEST` 或 `BACKTEST_TDX` 任一 | 同上 |

顺序洪律：**A → B → D → E**，上游未就绪即 `stage_ok=false` → 红灯 + **拒绝执行** + 紧急报告。

### 顺手修掉的 4 个真缺陷（都是实证的，别再退回）

| # | 缺陷 | 修法 |
|---|---|---|
| ① | **凌晨接力档永久失效（漏档）**：旧 `decide()` 用 `hh` 直接比 `MIN_HOUR{…,D:20,E:21}`，00:30 档 `hh=0` → 恒判 NONE。B 若 22:30 后才完 → **D/E 当晚永远补不上** | 凌晨 00:00-05:59 属前一数据日夜间补跑窗口，有效时刻按 **24:xx** 计 |
| ② | **周末 T+1 被挡死**：旧闸门步只要 `is_trading_day!=true` 就早退 NONE，连 `force_run` 都进不去 | 日历收归闸门 `chain_day()`：周六/假期首日（上一交易日 ≤3 天）判 `t1` 照跑；周日 NONE |
| ③ | **链尾问责假红灯**：`fresh_of` 读 `git show HEAD:<file>`，而 `api_push_raw.py` 走 **API 推送、不更新本地 HEAD** → 读到的永远是 run 起点的旧版本 → 必然误判「三模块不新鲜」 | 改读**工作区文件**，就绪判据改用闸门**同源代码** `--recheck`（两套规则必然漂移） |
| ④ | **闸门崩溃 → 假绿灯**：闸门脚本异常时下游全 skip，问责兜底 `exit 0` | 加 crash 守卫：拿不到判定即 `::error` + `exit 1` |

### ⚠️ 你会看到一个"假红"，别慌（我诚实交代）

正在跑的 **B 批 run `34480639973`（21:06 派发）用的是旧 17 步工作流**，
它的链尾问责步读 `git show HEAD:` → **会在结尾报 `::error v8-algo-stale`（假红）**。
**那不是真失败** —— 那是缺陷③，我已修好并推送（新 run 不会再出现）。
**判成败一律以产物 `update_time` 为准，不看 run 的绿红。**

### 校验记录（本地实测）

- 闸门 6 场景：自动=`B` ✅ ／ 凌晨 00:30=`B` ✅（旧版恒 NONE）／ 周日=`NONE` ✅ ／ 周六 T+1=`A` ✅ ／ 显式 E（前置 D 未就绪）=`stage_ok=false` **拒绝** ✅ ／ `--recheck` ✅
- YAML 解析 OK（13 步）；11 个 `run` 块 `bash -n` 全过；`py_compile` OK
- 远端验证：13 步、`steps.wknd`/`steps.trading` 残留 **0**、旧「按小时盲猜」残留 **0**

---

## 三、派发铁律（对全链生效，你我都要守）

| # | 铁律 |
|---|---|
| 1 | 派发 `v8_algo_cloud.yml` **一律不带 `stage`、不带 `force_run`**：`-d '{"ref":"main"}'`。跑哪批由闸门判 |
| 2 | `force_run` / `bypass_time_gate` **只表示"忽略时间门控"**，**绝不改变跑哪批**（曾误映射成全链 → 会重跑 A 批冲掉已到手的龙虎榜） |
| 3 | 派发前**必须**确认无算法链在跑（同 concurrency group `v8-algo-cloud`，多派只会排队堆积） |
| 4 | 闸门报 `stage_ok=false` 是**正确行为**（拒绝执行），**严禁 bypass、严禁改 stage 硬跑** |
| 5 | 禁止恢复今晚删掉的设计：静默闸门V5 / 探针路由 / 独立日历步 / 独立产物完整性步 |
| 6 | **不看 run 的 success/failure 判成败** —— run 会假成功，也会假红灯；唯一判据是产物 `update_time`（且以闸门输出为准） |

**派发源已收敛（阿狸咪侧）**：

| 自动化 | 处置 | 说明 |
|---|---|---|
| 阿狸咪-盘后算法链接力推进器（`da2072fc`） | **唯一派发源**，每小时 :10，窗口 16:00-01:00 | 先查无在跑 → 跑闸门 → 只按 `proceed` 派发 |
| 九宝量化-盘后最终闸 21:30（`53726469`） | **已暂停** | 与接力器重复 → 双派 |
| 小九心跳监控（`5edd92d6`） | 改为 `v8_peer_monitor.py --alert-only` | 原会 dispatch 算法链 = 第二个派发源 |
| 夜班监控+交接巡查（`90b7ea6e`） | 收紧：只有**真基础设施失败**才补派一次；闸门/问责红灯**不补派** | 防风暴 |
| 4 个已过期「一次性」自动化 | **已删除** | 垃圾 |

---

## 四、🔴 阿狸咪机做不到、只能你做的（这就是"明早不再出错"的关键）

### ① 盘中调度缺口（**最要紧**）

`v8_cn_fetch_intraday_lemoncat.yml` 的 14 档 cron 实际触发在 16:23 / 17:22 / 19:03 / 20:04 等，
三重守卫生效条件 `HHMM ∈ [09:32, 15:05]` → **全部判「迟到档」安静退出 → 盘中 fetch 实际没有稳定来源**。
（`v8_intraday_snapshot` 全天只 1 次、`cn_fetch_watchdog` 全天只 1 次，同样不可靠。）

**请你做**：在**你的机器**上放一个可靠调度器（你的 `qs8-tmp/v8_deploy_watch.py` 哨兵或 WorkBuddy 自动化），
**交易日 09:35-15:00 每 20 分钟** `workflow_dispatch` → `v8_cn_fetch_cloud.yml`，body `{"ref":"main","inputs":{"category":"intraday"}}`。
派发前查 `v8_cn_fetch_cloud.yml` 是否有 `in_progress/queued`（同组 `v8-cn-fetch-cloud` 会排队）。
cron 保留作备份，但**不要再指望它**。同理 **盘前 08:25** 补一个 `category=premarket` 的 dispatch。

### ② 你机上的 WorkBuddy 自动化我完全看不到

请自查你那边有哪些自动化会派发 `v8_algo_cloud` / `v8_cn_fetch_cloud` / `v8_stock_quote_refresh`，
按「**谁主力谁调度**」收敛：**白天（7:45-17:45）只有你派发；夜间（18:00-次日）只有我派发**。
重复的一律 PAUSE（不要删），避免两个派发源抢节奏。

### ③ `runs-on: [self-hosted, cn]` 是**共享标签**

`cn` 两台机都挂 → 会被另一台抢单。`v8_algo_cloud.yml:98` 那条注释写「固定到 lemoncat-cn（专属 label）」
**与实测不符**。要不要改成只挂在你机的专属 label（如 `lemoncat-cn`），由你决定（涉及你的 runner 注册）。
❗我**没擅自动**核心链路的 `runs-on`。

### ④ K 线缓存内容级日期闸 → 其余消费者还没同步

我已在 `calc_stock_rps.py`、`generate_top10.py` 加了「末根日期 < 期望交易日 → 强制重取」的闸。
**其余同类消费者请你自己补**（都在你的域）：`backtest_expectancy.py` / `factor_ic_analysis.py` /
`optimize_stop_target.py` / `v8/backtest_rps_offline.py`。
背景：`_rps_cache` 曾 375/461（81%）末根停 09-07、`kline_cache` 930 个停 09-07，只校验 mtime/行数根本发现不了。

### ⑤ `verify_chain_outputs.py` 判据加严

它仍用「`<06:00` 归上一自然日」的老口径。建议与闸门统一为
「**交易日 T 日 ≥16:30 的产出才算 T 日盘后产出**」（周六/假期首日按当日 08:00 起算）。

### ⑥ 两处治理项

- **凭证双份**：`~/.workbuddy/v8_gh_pat` 与 `~/.workbuddy/v8_gh_token.txt` 两份 token 都在用（我实测都有效）。
  按 🔐 凭证铁律应收敛到**单点**（脚本运行时读那一个文件或环境变量），记忆/文档引用必须打码。
- **交接目录污染**：同一份交接被复制到 `docs/ops/urgent/`、`docs/ops/handover/`、**仓库根** 三处
  → 以后请**只写 `docs/ops/handover/`**，否则谁都不知道该读哪份。

---

## 五、今晚剩余流程（无需你干预）

```
B 批（run 34480639973，小九机 lemoncat-cn，21:07 起跑）    ← 正在跑
   ↓ 产出三模块 → 闸门判 D
D 批（final_recommend，刷新 FINAL_RECOMMEND / CRDS / 三模块）
   ↓
E 批（回测族）
```
已用**新版 13 步工作流**排了一个 run（`34484706106`，sha `5dd8d8bd0`），
B 结束后它会自动接上；再由接力推进器（每小时 :10）继续推 D → E。

---

**收到请回一句 ACK。** 有异议直接写回来 —— 尤其第 ①②③ 条（白天调度、你的自动化清单、`runs-on` label）。

— 阿狸咪 2026-09-10 21:5x
