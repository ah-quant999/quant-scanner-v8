# 双机交接 · 阿狸咪 → 小九

- **日期**：2026-09-10 17:07（周四）
- **交接人**：阿狸咪（alimi-cn，家用机）
- **接手人**：小九（lemoncat-cn，单位机）
- **当前状态**：小九 17:07 由主人确认上线；GitHub API 截至写稿未见 17:00 后新 run（应刚就位，尚未派发）。

---

## 一、今日下午阿狸咪做了什么（13:00 – 17:07）

| # | 事项 | 类型 | commit / run | 状态 |
|---|------|------|--------------|------|
| 1 | 接 A 档 + C2 降级预案（基于小九 13:30 交接） | 调度 | — | ✅ 完成 |
| 2 | 手动派 post_close（15:00 档小九 runner 断线，post_close 步骤全 skipped） | 派发 | `34449540052` | ✅ success |
| 3 | **修复部署门禁 P0**：`v8_intraday_snapshot` 有 cron 未登记，阻断 `pre_deploy_audit` | 修复 | `4cb88cd7f`(index) / `0c79c932f`(align) / `a3a951d04`(logic) | ✅ build_deploy success |
| 4 | **修复收盘数据 raw→JS 断链 P0**：`v8_cn_fetch_cloud.yml` step20 无参调用 → 纯盘后产物（市场资金流向/温度计/危机雷达等）抓取刷新 raw 却不重生成 JS | 修复 | `6e8baecc9` | ✅ 纯盘后产物已重生 |
| 5 | 防踩踏：取消 3 个非预期小九调度器 run + 其派出的 intraday | 防踩踏 | — | ✅ 完成 |
| 6 | 修复"实时数据回退"误报：应急 intraday run 把指数/均价/ETF/概念刷到 16:25 收盘值 | 数据 | `34456097546` | ✅ 完成 |
| 7 | 修复亚太 `OVERSEAS_MARKETS.js` 卡 14:23（`update_v8._var_category` 漏映射） | 修复 | `7daabe166`(JS) / `7d61e36b`(update_v8) | ✅ 根治 |

---

## 二、什么没做 / 待小九接手（按优先级）

### 🔴 需小九拍板（设计缺陷，动核心链路前请确认）
- [ ] **纯 intraday 文件收盘后无"收盘快照"重建机制**：`INDEX_QUOTES` / `AVG_PRICE_DATA` / `ETF_PULSE` / `CONCEPT_RANKING` / `CFFEX` 的 `_file_category` 仍只归 `intraday`，不含 `post_close`。每天 15:30 intraday 调度停后，它们会停在最后盘中快照（今日是被应急 intraday run 碰巧刷新的）。
  - **阿狸咪建议**：给这 5 个加 `"intraday,post_close"`，一劳永逸。改法同 #7 的 `OVERSEAS_MARKETS`。**等小九拍板再动。**

### 🟡 待查（阿狸咪未动，留给小九 / 夜班）
- [ ] `H_AUTO_BUY` / `H_AUTO_BUY_TRACK` 真停更 09-08（查算法链哪一档产出、为何 09-09 后断）
- [ ] health check fail 3→6 因阈值设计（`max_age=360`）→ 建议改"按最近收盘时点"判定
- [ ] `v8_algo_cloud.yml` 固定 lemoncat-cn 的注释需重评（核心链路未动）
- [ ] `ETF_INTRADAY_HEAT` 系统性数据质量问题（166 条 pct 中位数 7.37%，应 ~0）
- [ ] `IPO_DATA.change_pct` 新股首日（N 前缀）无涨跌幅限制属合理，可补豁免
- [ ] `LHB_DATA.pct` 疑漏挂 `FIELD_CONTEXT_OK`（LHB_HISTORY 已豁免）

---

## 三、双机分工与防踩踏 / 防风暴（重点）

### 时段铁律
- **小九**：白天独家 **7:45 – 17:45**（今日特例 15:00–17:20 离线，现已上线 17:07）
- **阿狸咪**：夜间 / 周末 **18:00 – 次日 7:30**
- **交接窗口**：现 17:07，阿狸咪 18:00 前完成交接并退出白天调度；18:00 后阿狸咪夜间接管。

### 🔴 小九上线第一件事：清理僵尸 run
当前 3 个 **in_progress 僵尸 run**（self-hosted runner 断线未回收，已挂约 8 小时）：

| run id | workflow | 创建(UTC) | 处置 |
|--------|----------|-----------|------|
| `34458748715` | 🩺 云端健康巡检 | 09:05 | cancel，再派新巡检 |
| `34458729899` | ☁️ 构建部署(云端) | 09:05 | cancel（占槽，会挡新 build） |
| `34457460597` | 🇨🇳 中国数据抓取(云端) | 08:51 | cancel（占槽，会挡新 fetch） |

> 调用 `POST .../runs/{id}/cancel`（HTTP 202）。**不 cancel 直接派发会撞车 / 排队。**

### 🔴 防风暴
- 今日 A 档（16:40）阿狸咪已派且 success，**小九勿重复派**，否则撞机。
- 阿狸咪已停手所有派发，派发权交回小九（lemoncat-cn）。

### 🔴 防踩踏铁律（复述）
- 不在 in_progress run 上抢派（去重压槽位）；非预期 run 用 `cancel`。
- 小九专属调度器若被 alimi-cn 接住需取消（占 runner 致 A 档抢不到机）。

---

## 四、数据现状（硬刷新即可见）

- **收盘数据**（市场资金流向 / 温度计 / 危机雷达等）：已重生，`update_time` ≥ 16:00 ✅
- **实时指数 / 均价 / ETF / 概念 / 亚太**：已刷到 16:25–16:28 收盘值 ✅
- **部署**：门禁解除，build_deploy success，main HEAD = `a3d96e8c3`（17:05 risk gauge）✅

---

## 五、交接确认

- **阿狸咪（alimi-cn）**：本班任务已闭环，18:00 前退出，18:00 后夜班接管。
- **小九（lemoncat-cn）**：请确认收到本交接 → 清理僵尸 run → 恢复白天调度。
