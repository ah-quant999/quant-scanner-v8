# HANDOVER · 小九 → 阿狸咪 · 2026-08-06

> 更新于 2026-08-06 12:56 自动更新（12/18点 交接任务补跑）
> 当日累积改动整理，供阿狸咪开机后快速接手。

## 一、背景
- 昨日 GitHub Actions `schedule` cron 全部静默失效（根因：单 workflow cron 条目过多）。小九已收敛 cron 数量并叠加本机 WorkBuddy 兜底自动化，今天盘中数据已恢复自动刷新。
- 本仓架构：云端 `v8_cn_fetch_cloud` / `v8_algo_cloud` 接管抓取与盘后算法；本机仅保留看门狗、交接文档、本地 pull、紧急通道、周六体检、19点审计等轻量任务（6 个重复本地任务已 PAUSED）。

## 二、今日已完成改动（含真实 commit SHA · main = 9ffe1c2）

| 时间 | 改动 | commit |
|---|---|---|
| 上午 | 全站「更新于」徽章统一 + 计时卡/共振日历/综合回测/运维面板/暂未上架等 UI 修复 | `122101e` |
| 09:44 | AI市场速览 stale 守卫（隔夜快照开盘前清空）+ 看门狗追踪最近 cn fetch 提交 | `9505f62` |
| 09:55 | ETF 资金热度：商品分类补「石油」关键词 + 名称显示代码 | `3a083bd` |
| 10:15 | IPO 影子股「选A」实现并入打新卡（`shadowBlock`，仅建议/强烈推荐申购显示） | `e79020f` |
| 10:55 | IPO 关联股换真实数据（杰理科技/展芯股份/宇树科技），撤 demo 态 | `8ab6811` + `a58ff43` |
| 11:30 | **根因修复**：v8_cn_fetch_cloud/safety_net schedule cron 收敛防静默失效 + 本机兜底改定时 dispatch | `f1f02a1` |
| 12:06 | **cache-busting**：index.html 所有 data/*.js 引用追加 ?v=时间戳，防浏览器/CDN 返回旧数据（AI市场速览 显示过时根因） | `fd141f0` |
| 12:40 | 共振日历 4 个 tab 渲染加 try-catch 容错，避免 JS 异常整段留白 | `87fd55d` |

（以上 commit 均经 GHA `v8_build_deploy` 自动构建部署；线上 GitHub Pages 已含 cache-busting 参数。）

## 三、当前系统状态
- **站点**：GitHub Pages（源 main），最新构建 `9ffe1c2`（12:57）。
- **数据新鲜度**：cn_fetch_cloud 盘中每 30 分自动刷新已恢复；ETF_INTRADAY_HEAT / RISK_GAUGE / LHB_DATA / AI_MARKET_BRIEF 等均正常刷新。
- **GitHub token**：有效（api_http=200），盘中兜底/19点审计等依赖 API 的任务不受影响。
- **关键自动化（ACTIVE）**：
  - 盘中云端任务监控兜底（每30分 dispatch + 补跑）`automation-1785980310111`
  - 12/18点 交接文档（已修复 validUntil，今天补跑）`automation-1785719698947`
  - 每2h 管线看门狗、每小时本地 pull、每小时紧急交接通道、周六臃肿体检、每日19点审计推仓对齐
- **PAUSED（云端接管后的重复本地任务）**：15:30 收盘刷新、周末15:30 选股、17:00 选股部署、08:15 盘前全量、盘中30分兜底、盘后数据守护。

## 四、需阿狸咪注意 / 待办 / 风险
1. **IPO shadows 未固化**：当前 `data/IPO_DATA.js` 的 shadows 为上午手工注入示例，尚未固化到 `cloud_fetch_v8.py` 的 IPO 生成环节，下次盘前刷新会被覆盖。需把"按概念/行业映射自动生成关联股"写入脚本。
2. **recommend 字段被 status 污染**：宇树科技 `recommend="待定价"` 而非推荐等级（55分应为"谨慎参与"），导致前端影子股不显示。需修 IPO 生成脚本的字段映射。
3. **个股查询升级方案待确认**：用户要求参照 v6 加 BUY/关注/观望 100分评分 + 动态止损/目标价 + 异动停牌检测 + 金股池持仓建议。数据缺口：实时行情缺 volume_ratio/open/prev_close，GOLD_POOL.history 仅收录股有日K。方案已对比完成，待用户拍板实施方式。
4. **共振日历根因未完全定位**：已加 try-catch 容错（异常显示提示而非留白），但"空白"初始根因（疑似某次 JS 异常）仍需用户刷新后观察是否复现。
5. **GitHub schedule 已恢复**，但本机兜底自动化**不可删**（user 已确认普遍失效）。

## 五、小九监督职责
- 每 30 分监控 cn_fetch_cloud / algo_cloud / risk_gauge 新鲜度，过期自动 dispatch 补跑。
- 12:00 / 18:00 维护本交接文档。
- 周末周六 09:30 臃肿/崩溃风险体检、每日 19:00 审计修复+推仓+对齐。
- 异常（failure/cancelled 运行、站点 stale）即时简报。
