# 🛡️ v8 每日19点审计报告 · 2026-09-03 19:06

> 角色：审计（只读）。**零 commit、零 push、零文件改动**。
> 检查对象：GitHub Pages 线上站 + 真身仓库 `C:/Users/Administrator/qs8-tmp`（E: 工作区为坚果云空壳，无 .git）

---

## 一、一句话结论

**线上站点健康（HTTP 200），无新增回归、无 404、无语法致命错。本轮零派发（算法链已在跑，派发会互相取消）。**
需主人知晓 2 项：**本地 index.html 重复 id 地雷**（线上干净）、**top10_daily 快照缺 4 个交易日**。

---

## 二、Git 状态

| 项目 | 结果 | 说明 |
|---|---|---|
| 本地超前 commit（origin/main..HEAD） | **0 ✅** | 无未推提交，无推仓风险 |
| 冲突标记 | **0 ✅** | 无需中止 |
| 落后 origin/main | 801 | 护栏禁 pull，属正常 |
| 工作树差异 | 224 项 | data/ 本地恒旧，属正常 |
| 今日 origin/main 提交 | **303 个** | 管线活跃，最新 `997c6da5a`（19:04 cn fetch） |

---

## 三、index.html 结构审计

### 3.1 script 标签配对 —— ✅ 通过
| 检查 | 结果 |
|---|---|
| 顺序 tokenizer（模拟浏览器） | **110 对全配对，无真未闭合** ✅ |
| 裸计数 `<script`=116 / `</script>`=111 | ⚠️ **误报** — 多出的在 JS 注释/字符串（L6971/10138/10139/13194/13474/13562） |
| div 配平 | **762 / 762** ✅（先剥注释后完全配平；不剥注释会假性少 1） |

### 3.2 重复 id —— 🔴 本地地雷（**线上干净**）
本地 `qs8-tmp/index.html` 中「🧭 观测候选股池」整卡被**复制两份**（L2191 / L2396）：

| id | 本地 | 线上 origin/main | 坚果云 E 盘 |
|---|---|---|---|
| `st-pool` | ❌ ×2 | ✅ ×1 | ✅ ×1 |
| `stPoolTime` | ❌ ×2 | ✅ ×1 | ✅ ×1 |
| `stPoolSourceDist` | ❌ ×2 | ✅ ×1 | ✅ ×1 |
| `stPoolStockList` | ❌ ×2 | ✅ ×1 | ✅ ×1 |

- **线上站点当前无此问题**（三份副本比对已确证）。
- 风险：`getElementById` 只取第一个，第二份为死节点；**若日后「上线」误推本地副本 = 站点回归**。
- 处置：**未动手**（19:00 属阿狸咪值守班，index.html 编辑期禁止触碰）→ 建议白班或经主人授权后删掉 L2395-2403 那份重复卡。

### 3.3 三份 index.html 指纹（便于日后秒判）
| 副本 | 字节 |
|---|---|
| qs8-tmp 本地 | 1,017,125 |
| **origin/main 线上** | **898,005** |
| 坚果云 E 盘 | 916,576 |

### 3.4 其他
- `--</script>`（L3768）：HTML 注释未正常闭合（`--` 被 `</script>` 打断）。**三份副本都有、线上实测可容忍** → P3 观察项，非新引入。
- 重复卡片名「📊」×2：通用占位符，无实质影响。

---

## 四、仓库护栏

| 护栏 | 结果 |
|---|---|
| `guard_index_sections.py` | ✅ **GUARD_OK**（sec-st/sec-ph/sec-rc 齐全，1.0MB 未超 1.2MB 上限） |
| `guard_index_js_tdz.py` | ⚠️ 4 处告警 → **逐条核实，全部安全** |

TDZ 告警逐条确认（**判据：调用点是否在函数体内**）：

| 符号 | 位置 | 结论 |
|---|---|---|
| `_safeArr()` ×3 | L830 / L847 / L875 | ✅ 均在 `function __judgmentCandidatesHTML(){}`（起于 **L807**）**函数体内**，非顶层执行 → 假阳性 |
| `renderDataFreshness()` | L8814 | ✅ 已包 `try{}catch(e){}`，且 L12986 有 `window.renderDataFreshness=` 兜底 → 已防御 |
| `$()` | L770 | ✅ 已加 `typeof $ === 'function'` 守卫 → 已防御 |

---

## 五、data/*.js 注入审计 —— ✅ 无 404、无回归

口径：**远端 index.html vs 远端 data/ 目录**（105 个文件），静态引用 86 + 动态懒加载 3。

| 检查 | 结果 |
|---|---|
| 引用但文件不存在（真 404） | **无 ✅** |

- 说明：`V8_CAL_INDEX.js` 实际引用路径是 `data/archive/V8_CAL_INDEX.js`（存在）——**按 basename 比会误报**，须保留子目录路径。

**13 个「未引用」全部为有意下线/轻量化，非回归：**
- 08-31 轻量化（省 ~95KB）：`ANALYST_RATINGS` / `LHB_7D` / `INDEX_HISTORY`
- 09-03 主人令删脚本标签（注释已写明「全方位下线」）：`COCKPIT_TIER_RECOMMEND` / `COCKPIT_ADVICE` / `COCKPIT_BACKTEST` / `HUNTER_BACKTEST`
- 其余：`ALGO_TRACK` / `CANDIDATE_QUOTES` / `STOCK_PROFILE` / `STOCK_RPS_DATA` / `ALGO_BACKTEST_COMPARE` 等

> 注：`STOCK_LIST` / `STOCK_PROFILE` / `STOCK_QUOTE` / `LHB_HISTORY` 走 **A2 `BIG[]` 动态懒加载**，不是静态 `<script src>` —— 审计时须一并扫该数组，否则误报 4 个回归。

**2 个从未注入的孤儿产物（清理候选，非缺陷）：**
- `ALGO_BACKTEST_COMPARE.js` —— 今日 18:45 仍在产出，前端零引用
- `SECTOR_FUND_TRACK.js` —— 09-02「恢复注入」提交，但近 20 个 index.html 版本从未引用（该提交 claim 已失效）

---

## 六、盘后数据新鲜度（交易日 gate）

**交易日判定**：`venv python algorithms/check_trading_day.py` → `2026-09-03 is_trading_day=true` ✅ → 执行检查。
基准：今日收盘 09-03 15:00；上一交易日收盘 09-02 15:00。

| 数据 | 类别 | update_time | data_time | 判定 |
|---|---|---|---|---|
| SECTOR_RS | post_close | **2026-09-03 19:01** | — | ✅ 今日 |
| CANDIDATE | post_close | **2026-09-03 19:04** | — | ✅ 今日 |
| LHB_DATA | post_close | **2026-09-03 18:59** | — | ✅ 今日（旧疾 08-04 卡死已愈） |
| GOLD_POOL | post_close | 2026-09-03 13:55 | — | ✅ 今日（盘中） |
| SH_FIB | post_close | 2026-09-03 12:52 | — | ✅ 今日（盘中） |
| **TRIPLE_CONSENSUS** | algo | 2026-09-02 04:44 | 09-02 04:22 | ❌ **陈旧 > 1 交易日** |
| CRDS_CARD_DATA | algo | 2026-09-02 04:17 | **09-02 15:30** | ⚠️ 上一交易日（未越线） |
| TOP10_DAILY | algo | 2026-09-02 23:41 | — | ⚠️ 上一交易日（未越线） |

### 派发决策 —— **本轮零派发**

| 目标 workflow | 决策 | 理由 |
|---|---|---|
| `v8_cn_fetch_cloud.yml`（post_close） | ❌ 不派发 | 5 项 post_close **全部今日新鲜**，且 0 个 in_progress |
| `v8_algo_cloud.yml`（算法链） | ❌ **主动放弃派发** | 虽有 TRIPLE_CONSENSUS 陈旧，但 **#33743382557 正 in_progress**（8/16 步，正跑第 9 步「🧮 运行盘后算法链 → raw_data/ → 推送」，已 ~66 分钟），另有 #33747055874 pending |

> **放弃派发的依据（项目已确证行为）**：algo_cloud 新派发会**取消队列中更旧的 pending run**，并疑似会**外部取消 in_progress run**（09-03 #33736979994 跑满 72 分钟后被 `The operation was canceled.`）。此时派发 = 打断正在修 TRIPLE_CONSENSUS 的那一次运行，得不偿失。

---

## 七、步骤 6 · top10_daily 快照缺口 🔴

`raw_data/history/top10_daily_*.json` 最新仅到 **20260830**，缺失：

| 缺失快照 | 交易日 |
|---|---|
| `top10_daily_20260831.json` | ❌ 缺 |
| `top10_daily_20260901.json` | ❌ 缺 |
| `top10_daily_20260902.json` | ❌ 缺 |
| `top10_daily_20260903.json` | ❌ 缺 |

**共缺 4 个交易日快照。已按要求只报告、不补 add。**

---

## 八、简报

| 项 | 结论 |
|---|---|
| **今日是否有改动** | 本轮审计**未做任何文件改动 / 未推送**。仓库侧 origin/main 今日有 303 个提交（管线自动跑）。本地 index.html 存在未提交的重复 id 地雷。 |
| **是否发现异常** | 2 项需主人知晓：① 本地 index.html 重复 id 地雷（线上干净）；② top10_daily 缺 4 个交易日快照。另有 2 个孤儿 data 文件可作清理候选。 |
| **已触发哪些云端 workflow** | **零触发**（0/2）。理由见第六节。 |
| **是否需主人手动处理** | 见下表 |

### 需主人决策 / 处理

| # | 事项 | 建议 | 紧急度 |
|---|---|---|---|
| 1 | 本地 qs8-tmp `index.html` 选股池卡重复（L2191 / L2396） | 删掉 L2395-2403 那份重复卡。**当前勿在阿狸咪班动 index.html** | 🟠 中（下次「上线」前必须清） |
| 2 | top10_daily 快照缺 0831-0903 | 确认是否仍需该历史管线；若要补需另开补跑任务（本轮未补） | 🟠 中 |
| 3 | TRIPLE_CONSENSUS 仍停在 09-02 04:44 | 等 #33743382557 跑完即可；若 21:00 后仍未更新再人工介入 | 🟡 观察 |
| 4 | 孤儿产物 `ALGO_BACKTEST_COMPARE.js` / `SECTOR_FUND_TRACK.js` | 可纳入轻量化清理（前端零引用却每日产出） | 🟢 低 |

**无需告警级处置** —— 无严重语法/DOM 致命错误，线上功能完好，未发送邮件告警。
