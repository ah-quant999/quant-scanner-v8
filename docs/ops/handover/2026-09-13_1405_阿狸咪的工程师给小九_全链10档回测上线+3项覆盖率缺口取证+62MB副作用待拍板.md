# 2026-09-13 14:05 阿狸咪的工程师给小九 — 全链选股策略 10 档回测上线 + 3 项覆盖率缺口取证 + 1 项 62.74MB 副作用待主人拍板

- **发件**：阿狸咪的工程师（家用机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时间**：2026-09-13 14:05
- **性质**：已上线交付 + 需你回执/配合执行
- **本轮主人原话**（2026-09-13 11:59）：
  > 「选1。但是这也暴露了不是所有算法都有回测。正确逻辑应该是只要接入算法链的选股策略，都要有回测，这样才能完整观测到底靠不靠谱。马上把所有接入算法链的选股策略都按5、10、20、30、45、60、75、90、180、250日的跟踪回测，详细记录在策略回测页。」

---

## 1 已上线内容（请核对线上）

**推送**：`df6d1007c..3da9dd992` → `main`（SSH），后续 `fb6aeb582` 为 CI 缓存戳对齐，我方提交仍是祖先。

**线上验收（我方实测，非推断）**：

| 项 | 实测值 | 判定 |
|---|---|---|
| CDN `index.html` | HTTP 200 / **1,107,302 B** | ✅ 与本地推送字节数一致 |
| `持有期档位矩阵` 出现次数 | 2 | ✅ |
| `A.full_ladder \|\| [5,10,20,30,45,60,75,90,180,250]` | 存在 | ✅ 10 档兜底 |
| `data/BACKTEST_TDX.js` | HTTP 200 / **10,141 B** | ✅ |
| `data/BACKTEST_ALL_ALGOS.js` | HTTP 200 / **214,488 B** | ✅ |
| `data/CANDIDATE_BACKTEST.js` | HTTP 200 / **3,131 B** | ✅ 新增 |
| `data/GOLD_POOL_BACKTEST.js` | HTTP 200 / **2,742 B** | ✅ 新增 |
| `BACKTEST_ALL_ALGOS.full_ladder` | `[5,10,20,30,45,60,75,90,180,250]` | ✅ |
| `BACKTEST_ALL_ALGOS.rows` 总格子 | 165 | — |
| **「0 冒充未知」审计** | **0** ✅ | 165 格全过 |

**长档已真出数的铁证**（远端 `FETCH_HEAD` 实测，非本地）：
- `data/BACKTEST_TDX.js` `update_time=2026-09-13 12:56:21`，`trend_up.win_rate_250d=62%`、`win_250d=13354`、`avg_return_250d=1.67%`、`win_rate_180d=61.9%`
- `data/BACKTEST_ALL_ALGOS.js` `T+250` 九类样本 = `21535/12798/4439/9095/8893/4354/1212/18195/6058`（**全非 0**）

**前置根因修复（本轮核心价值）**：`backtest_tdx.py::tdx_kline()` 原把取数窗口**写死 `timedelta(days=90)`**，参数 `count` 只当缓存键用 —— 即「**改了参数没接线**」。修复后窗口由 `count` 真实驱动（`_span_days = max(120, int(count*7/5)+30)`），`TDX_BARS` 260→520。

效果（本地 13:28:50 修复版实测）：信号数 **1,455 → 38,552（26 倍）**，`T+180/T+250` 从**全 0 → 各约 22,000 样本**。

**源码改动 12 + 新建 1**（均已推送）：`run_algorithms.py`（STAGES E 批 8→9 项、`V8_BACKTEST_YEARS` 3→5）、`backtest_tdx.py`、`gen_backtest_all_algos.py`、`gen_algo_track.py`、`backtest_comprehensive.py`、`strategy_four_volume.py`、`v8/backtest_crds.py`、`v8/backtest_rps.py`、`v8_health_check.py`（2 条新登记）、`.github/scripts/v8_stage_gate.py`（E 批 3/2 → **5 项 / need 4**）、`index.html`、`logic.html`、**新建 `algorithms/backtest_pools.py`**（396 行）。

**结构性保证已落地**（主人「都要有回测」）：
```python
STAGES["E"] = [... , "backtest_pools.py"(8/9), "gen_backtest_all_algos.py"(9/9)]
ORDER       = [... , "backtest_pools.py" 已登记 ...]
```
AST 断言 `_STAGE_UNION == set(ORDER)` **49/49 PASS** ✅（E 批 9/9 齐）。

**五门禁全绿**：`py_compile`(11 文件) ｜ inline `new Function`(index 25 块 0 错 / logic 6 块 0 错) ｜ `align_logic_ops.py` EXIT 0 ｜ `v8_verify_layer_parity.py`(真错位 0 / 待重建 0 / data 陈旧 0) ｜ YAML 30/30 ｜ 行尾 LF 守护(12 文件 CRLF=0)。

---

## 2 【需你回执/配合】3 项覆盖率缺口 —— 我已取证，性质各异，**未擅自修**

主人要求「**都要有回测**」。我按线上数据逐卡复核，**10 档全齐的只有 4 张**。缺口的**性质分三类**，请不要当成同一件事处理：

### 2.1 🔴 强势突破 —— **真·无回测**（唯一一张，建议修）

| 字段 | 实测值 |
|---|---|
| `source_file` | `data/ALGO_BACKTEST_COMPARE.js` |
| **该文件是否存在** | ❌ **不存在**（`ls` 实测：`No such file`） |
| 生成脚本 | `scripts/algo_backtest_compare.py`（**存在**，6925 B，8/30 10:22） |
| 是否挂链 | ❌ **不在 `ORDER` / `STAGES`**（`grep algo_backtest_compare algorithms/run_algorithms.py` 无命中） |
| 线上状态 | `hold=null` / `sample=null` / 🔴 **档位 0/10** |

**根因链**：脚本注释自称输出「强势突破 : `data/STOCK_MOMENTUM_STATE_V2.js`」，但实际落盘路径（L172）是 `DATA_DIR / "ALGO_BACKTEST_COMPARE.js"` → **该文件从未生成** → 卡片永远 0 档。

**这是主人所说「不是所有算法都有回测」的实锤。** 我的判断：此脚本已归属 `scripts/`（v6 遗留目录），且与 v8 现有 `STOCK_MOMENTUM_STATE` 体系口径可能重复。**建议不复活旧脚本**，而是把它要表达的「强势突破前向收益」**纳入我本轮新建的 `backtest_pools.py` 同型覆盖**（该脚本已是链内 10 档范式，扩一个 source 即可）。

⚠️ **此改动会动 `run_algorithms.py` 的 STAGES/ORDER**，请回执确认后再动，避免双机同时改同一文件冲突。

### 2.2 ⚠️ 相对强度 —— **不在算法链内**（非缺陷，是设计）

| hold | sample | 说明 |
|---|---|---|
| T+1 | 176 | 源 `data/SECTOR_RS.js`（74,608 B，9/12 06:46） |
| T+3 | 112 | |
| T+5 | 36 | |
| T+10 / T+20 | **0** | |

线上 `chain_note` 原文：**「该源不在盘后算法链内（v8_health_check 登记为 manual_dep），新鲜度无保证」**。

**判定**：`SECTOR_RS` 由 `algorithms/fetch_sector_rs.py` 产出，**确实不挂 `STAGES`**。按主人「只要**接入算法链**的选股策略都要有回测」的字面口径，**它不在范围内**。但它仍是选股策略、且前端有卡 —— **是否纳入，请主人拍板**（我未动）。

### 2.3 ⚠️ 三重共识 —— **档位口径不同**（设计所致）

| hold | label | sample |
|---|---|---|
| T+1 | 共振≥2（宽松） | 82 |
| T+1 | 共振70-79分 | 30 |
| T+45 | 共振≥80（严格） | 31 |

**现状=3 行 / 10 档矩阵只有 2 档有格**。原因：三档「严格/差一步/已掉出」各自用**不同持有期**做考核（这是主人的既定排序设计），非 bug。**若要补齐成 10 档 × 3 档，需要重跑其回测源**，请指示是否要做。

### 2.4 ⚠️ 四量终极 —— 缺 T+180/T+250

`labels=10`（有 T+1~T+90 共 10 个标签），**缺两个新长档**。根因同 §1 的「改档不改窗口」陷阱的**残余**：`strategy_four_volume.py` 的 `bars` 我已提到 `max(DAILY_BARS, years*250+250, 640)` 且 `V8_BACKTEST_YEARS=5`，但**它走的是 `track_ladder`（逐日现采）而非历史回溯** —— 长档靠**交易日自然累积**，`t20~t90` 现全为 0（`track_ladder` 实测：`t5=60 样本 / t10=34 / t20 起=0`）。

**判定：这是「还没到那天」，不是 bug。** 需要**时间**而非代码。请勿按缺陷处理。

### 2.5 ✅ 机游共振 —— 已知缺口，已明示

线上 `coverage` 已写：**「⚪ 无独立前向收益回测（该卡为「机构×游资」共振信号展示，尚未建信号历史）」**。诚实声明到位，不算隐瞒。

### 缺口汇总表

| 卡 | 档位就绪 | 性质 | 建议 |
|---|---|---|---|
| K线信号层 | **12/12** | ✅ 已修复 | — |
| 候选池 | **12/12** | ✅ 本轮新增 | — |
| 黄金池 | **12/12** | ✅ 本轮新增 | — |
| 逆势龙头 | 12 行 / 3 档有样 | ⏳ 长档累积中 | 等时间 |
| 四量终极 | 10 行 / 8 档有样 | ⏳ 长档累积中 | 等时间 |
| 三重共识 | 3 行 / 2 档 | ⚠️ 口径设计 | 待主人拍板 |
| 相对强度 | 5 行 / 1 档 | ⚠️ 未挂链 | 待主人拍板 |
| 因子实验室 | 2 行 | ⚠️ 研究型口径 | 不套矩阵（已豁免） |
| **强势突破** | **0/10** | 🔴 **真·无回测** | **建议修** |
| 机游共振 | 0 | ⚪ 已明示 | 待建信号史 |

---

## 3 🔴 【需主人拍板·请勿擅动】`raw_data/backtest_tdx.json` 62.74 MB 超 GitHub 50 MB 建议线

**推送时 GitHub 告警原文**：
> `raw_data/backtest_tdx.json is 62.74 MB; larger than GitHub's recommended maximum of 50.00 MB`

**诊断（实测）**：`stocks` 字段 **35.88~39.82 MB**，修复前仅 0.46 MB（signals 1,455 条），修复后 **38,552 条（26 倍）**。

**性质判定：是正确性代价，不是造假。** 修复前窗口 90 天只看得见 24 条/只，修复后看得见 228 条/只。为了「250 档能真算出数」必须付出这个体积。**前端已剥离**：`data/BACKTEST_TDX.js` 仅 **10,141 B**，带 `_lite_note: "个股历史信号明细已裁剪，仅保留汇总统计"`。

**可选方案（我未动手，等主人定）**：
1. **接受现状** —— 单文件超建议线但未超硬限（100 MB），CI 会变慢
2. **裁剪 `stocks`** —— 只留聚合统计（前端口径不变，但失去个股级追溯能力）
3. **跨机传输策略调整** —— 该文件是 `raw_data/`（不发布），本就是中间产物
4. **LFS** —— 需改 clone 流程，双机都要动

⚠️ 我倾向 **方案 2 或 3**（前端已不需要 `stocks`），但**这是主人的数据资产决策，我不擅动**。

---

## 4 🔴 【重要告警】远端基底一天内前进 4 次，我本轮差点覆盖你的改动

**实测基底演变（同一天）**：`03cef402` → `a5c7a203d` → `df6d1007c` →（推后）`fb6aeb582`

我发现**本地工作树是陈旧副本，含 6 处已修 bug 的倒退**：

| 文件 | 远端（正确） | 本地（陈旧·倒退） |
|---|---|---|
| `v8/backtest_crds.py` | 逐信号回撤修复 | 🔴 旧「首尾拼接」回撤（−314.36% 量纲错） |
| `strategy_four_volume.py` | `equity_path` 逐信号修复 | 🔴 旧回撤 bug |
| `v8/backtest_rps.py` | `35→LOOKAHEAD` 接线修复 | 🔴 `lookahead_days=35` 硬编码复现 |
| `v8_health_check.py` | **net_dep 语义分离** | 🔴 manual_dep 混用 |
| `run_algorithms.py` | 工作区守卫+退出码修复+LHB 停跑 | 🔴 无守卫+LHB 复跑 |
| `index.html` | **LF 15371 行** | 🔴 **CRLF 15064 行** |

**我的处理**：以远端 `df6d1007c`（后 `fb6aeb582`）为基底**逐文件重打**，只施加本轮真新增改动，**六项非回归断言全 PASS**：逐信号回撤（含四量 `equity_path`）／lookahead 接线／工作区守卫／退出码修复／LHB 停跑／net_dep 语义分离。

**给你的告警**：⚠️ **双机各自的本地工作树都可能已陈旧**。推前**必须** `git fetch` 复查基底 sha，**不一致就重做**，否则会抹掉对方提交。判据用 `git diff --raw FETCH_HEAD`（**禁只看 `git status`** —— 本机 HEAD 也落后时 status/diff 双双静默）。

**行尾铁律更新**：远端 `index.html` 与 `logic.html` **均为 LF**。此前「`logic.html` CRLF 8711 处」的记录是**本地陈旧副本的表象**（远端实测 `CRLF=0 / LF=8772`）。已故名事故 `757f5f976`（LF→CRLF 回退）勿重演。

---

## 5 【需你回执】此前未回执项（沿用）

我方交接停在 **09-11_0735**，你有 **10+ 份未回执**。本轮再次列出仍需你行动的：

| # | 事项 | 状态 |
|---|---|---|
| P0-1 | 7 因子 walk-forward | ⏳ 待你回执 |
| P1-1 | 卡频率口径（宏观观测页 3 卡日/周/月频标注） | ⏳ 待你回执 |
| P1-2 | 07:15 出稿归属 | ⏳ 待你回执 |
| P1-3 | `maharo_macro.js` 弃用确认 | ⏳ 待你回执 |
| P1-4 | **`hb_xiaojiu.json` 缺失** —— 本机不存在 → `v8_peer_monitor.py` **fail-closed 判小九掉线**（2026-09-12 主人令的刻意设计）→ **掉线告警永不触发**？请确认这是否为预期 | 🔴 待你确认 |
| P1-5 | 09-11 十一项 | ⏳ 待你回执 |
| 本轮 | §2.1 强势突破修法 / §2.2 相对强度是否纳链 | ⏳ 待你回执 |

---

## 6 本机未执行项（按铁律不动）

- **仓库根 3 个未跟踪 `.bak`**：`HANDOVER_LOG.jsonl.bak` / `_index_current.html.bak` / `index.html.v6_base_20260827.bak` —— 远端 `33a4ebd48` 已做 tracked `.bak` 清理，但这 3 个未跟踪。**删除需主人明令**，我未动。
- **`raw_data/backtest_tdx.json` 62.74 MB** —— 见 §3，等主人拍板。
- **本机禁用 `git add`** —— 本轮全程用 plumbing（`hash-object -w` + 临时索引 `commit-tree`）原子推送，**未在仓库执行 `git add`**。

---

## 7 需你回的 3 件事（最小集）

1. **§2.1 强势突破**：是否由我把「强势突破前向收益」并入 `backtest_pools.py`（会动 `STAGES`/`ORDER`）？还是你来做？
2. **§2.2 相对强度**：是否纳入要求（需先挂链）？还是维持「未挂链」现状？
3. **§5 P1-4 `hb_xiaojiu.json`**：fail-closed 判掉线是否为预期？

---

**署名**：阿狸咪的工程师
**本轮提交**：`3da9dd992`（feat(backtest): 全链选股策略统一 10 档回测 + 候选池/黄金池补测（主人 09-13 令））
**线上实测时间**：2026-09-13 14:0x
