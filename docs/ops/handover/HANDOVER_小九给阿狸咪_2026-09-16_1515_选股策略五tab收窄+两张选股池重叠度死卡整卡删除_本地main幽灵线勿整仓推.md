# HANDOVER 小九 → 阿狸咪 · 2026-09-16 15:15 · 选股策略五 tab 收窄 + 两张死卡整卡删除

## 一、本轮（第三轮纠偏）做了什么（主人令驱动）

主人原话：「现在的选股策略只要这几个（截图），其他的你不要乱加，大牛股都删除多久了！」

1. **「大牛股」取证**：去注释后全站 `大牛股/大牛/猎手/全站精选` 活代码 **0 次**，剩余 8 处全为 2026-08-30~09-03 删除留痕注释。无需改动，不要再误报。
2. **选股策略 tab 锁定为 6 个**（含基础股池）：🧭选股池 → 🔥四量终极⭐5 → 🥇三重共识⭐4 → 🧬选股生命周期⭐3 → 🧪因子⭐2 → 📊逆势龙头（弱市专用·0⭐观测）。**不得再新增策略 tab / 回测排名条目**。
3. **`__STAR_PROFILE_OF` 清理**：删除 `'K线信号层'`（cat=signal，技术信号层，非选股策略）与 `'强势突破'`（page=暂未上架）。现仅留 三重共识/四量终极/逆势龙头/因子实验室 四键。
4. **两张「📊 选股池重叠度」死卡整卡删除**（三重共识页 + 四量终极页各一张）：
   - 数据层 `ALGO_TRACK`/`algo_track.json` 无 `overlap`/`top10_ge70`/`total_tracked` 三字段（实测 has overlap: False），生成脚本 `gen_algo_track.py` 早已不产出 ⇒ 自「全站精选」下线后恒显「无重叠数据」。
   - 已删：HTML 卡 ×2 + DOM 锚点 `ttOverlap`/`fvOverlap` + 渲染段（原 L13513/L13645 区域）。卡数注释 7→6。
5. **顺手修真 bug**：回测页排除清单区一处 `h += '<div ...>'` 残缺行（字符串未闭合 + `}` 缺失），已补全。

## 二、上线与核验（三件套全过）

- 推送链：Git Data API（blob→tree→commit→ref，force:false），与云端 churning 并发安全。
- ✅ 远端真值核验：`git show origin/main:index.html` 归一化后与本地逐字符一致。
- ✅ Pages 主站硬抓：长度一致、归一化 diff=0（仅 `?v=` 缓存戳差）。
- ✅ jsdom 真实渲染：`renderTrack`/`fvRenderTrack`/`__btOverviewHTML` 无异常，死卡锚点未写入，主表 4 行合规（四量/因子/三重/逆势龙头），排除清单留痕 2 项（强势突破/K线信号层）。

## 三、给阿狸咪的注意事项（红线）

1. **index.html 今日下午被我多次热修**（12:36 / 15:0x 两批）。你接班后**先 `git show origin/main:index.html` 取真身**，别用本地幽灵副本（本机 `E:/qs_workspaces/quant-scanner-v8` 的 main 线已落后且暂存区有大量陈旧条目——典型的陈旧索引地雷，**不要整仓 commit/push**，单文件走 API 推）。
2. **不要再往回加**：选股池重叠度卡、K线信号层/强势突破星级条目、大牛股/全站精选任何形态 —— 均为主人明令删除项。
3. `__STAR_KEY2CARD` + `__moduleStarsHTML`（L4491 附近）是全站零调用的死代码，本次未动（等轻量化批次统一清）。**不要把它们的旧映射当活口径引用**。
4. `data/CANDIDATE_BACKTEST.js` / `data/GOLD_POOL_BACKTEST.js` 本地 untracked 副本是 09-14 幽灵（update_time 09-14 22:50），远端已删 —— **不要重新推上去**。
5. `SECTOR_CYCLE_ARCHIVE.js`/`sector_cycle.json` 云端 15:05 仍在生成，以远端为准。

## 四、待办（顺延）

- ① IVF 注入修复（tmp/pending_lunch/）；② 因子卡 12 档待 E 批；③ CFO/A T+1~T+3；④ 四量追踪账本逐日累积。
- ⑤ **等主人确认文案**：因子实验室显式登记 medium + β 依赖标注（代码已就位）。
- ⑥ 「选股生命周期」是否作为第 5 项进回测主表（当前：独立块承载、不进排名，数据层无对应卡）。

—— 小九的股票专家 2026-09-16 15:15
