# 九宝量化 v8 交接文档（小九 → 阿狸咪）

> 生成于 2026-08-25 12:05 自动更新（小九 12:00 档 · 北京时）。
> 数据源：当日 v8 侧工作日志 `quant-scanner-v8/.workbuddy/memory/2026-08-25.md` + 仓库实拉 `git log origin/main` + 线上站点/HEALTH_CHECK 实时核实。
> 全部 commit SHA 均从 `git log origin/main` 逐条 `merge-base --is-ancestor` 验真，未用记忆值。
> 双触发处理：运行初 `git log origin/main -- <本文件>` 为空 → 直接 Write 新建；但提交前二次 fetch 发现并行 12:02 实例已提交 `9a6787e56`（docs(handover): 08/25 交接自动更新 12:02）→ **本档按 superset 合并其补强项（清冲突标记教训 / 只信内容时间戳 / 双机分时铁律口径）后，基于 `9a6787e56` 重新提交，未丢弃其有效内容**。

---

## 一、背景

- **主人 HH 出差中（8/21–8/27）**，全程不可打扰；要求「盯紧别乱改、本地云端对齐」。
- **双机分时铁律**：小九白天独家 7:45–17:45，阿狸咪 18:00–次日 7:30 + 周末接手（当前为白天独家时段）。
- 今日 8/25（周二），小九当班时段 08:00–12:00。主线：**盘中 7 卡空白事故止损** + **板块推荐卡永久冻结治本**。
- 本地 21 项在飞 WIP 已 `git stash` 保全（`handover-20260825-wip-preserve`），未推送、未丢失，交班后由小九续作或阿狸咪按需处理。

---

## 二、今日已完成改动（含真实 SHA + 影响）

### A. 小九主导的实质修复（已入库 main，SHA 已验真）

| # | 时间 | 主题 | 真实 SHA | 影响 |
|---|------|------|----------|------|
| 1 | 09:58 | **P0 盘中 7 卡空白事故止损** | `629b45111` | 09:27:13–09:57:50 全球指数/ETF盘中异动/ETF资金热度/板块资金流向/概念排名/涨停热度/市场预警 **7 张盘中卡全空白约 30min**，横跨 09:30 开盘。根因：清冲突标记时误保留 `premarket_cleared=true` 盘前清空占位版覆盖 raw_data → build 生成空 `data/*.js` 上线。cn_fetch #933 回填 5/7，余 2 张本地 `cloud_fetch_v8.py --only <VAR>` 精准补抓（逗号多值无效须逐个），隔离 index 只推 4 同源文件；实测线上 7/7 全绿 + 站点 HTTP 200。 |
| 2 | 10:08 | **全时段数据兜底（intraday）** | `71c93aae9` | 守卫 STALE:7 触发，本机 `cloud_fetch_v8 --category intraday` 15/15 成功（涨 3518/跌 1658，两市 7137.8 亿，涨停 92 只）；19 个 raw_data 文件全量落 main，守卫复检 FRESH，红线未触碰。 |
| 3 | 11:25 | **板块推荐卡永久冻结治本** | `82ade974c` | 结构性根因（云端 run #1111 硬证据）：`out/` 被 `.gitignore:29` 忽略 → 云端全新 checkout 永无中间产物，断链 `fetch_macro.py→out/macro.json→market_regime.py→out/market_regime.json→sector_recommendation.py`；两脚本缺依赖时**只 print「先跑 X.py」就 `return 1`（光说不做）**，自 08-22 起永久冻结。修复：两处 bailout 改为真 `subprocess` 自举上游后重试（自举失败仍 `return 1` 零回归）；隔离 index 只推 2 个 `.py`，未碰 21 项在飞改动与红线。 |
| 4 | 盘前 | **主力净额分时曲线一劳永逸 fix** | `a336826957` | 渲染改为 ≥1 快照即出图（minFreq 自适应，不再干等第 2 快照致开盘前 38 分钟空白）；`cloud_fetch` 快照合并加 snapshots/date 兜底 + 去盘前占位标记。已并入 main 历史。 |
| 5 | 12:03 | 持仓同步（westock） | `5f6fa29a6` | 当前 main 顶端前的实质数据提交。 |

### B. 自动化例行产出（今日 origin/main 头部，均为正常管线）

`5f6fa29a6`(westock 持仓同步 12:03) · `0d6da6348`(build 11:46) · `7092eac5c`(build 11:44) · `5a0fbf7e5/bdf88fc9d/b2804f375`(cn fetch 11:42–11:43) · `c76d078a5`(build 11:30) · `213311cfd/03aeba0a0`(STOCK_QUOTE refresh) · `3b52904ca`(cn fetch 11:28) · `e055fc366`(build 11:26) · `799c89f64`(risk gauge 11:24) · `3eeb3d09c`(build 11:16) · `7fa7f6b4e`(cn fetch 11:15)

### C. 当班守护（零动作，判新鲜不重复抓）

- 09:20 盘前空转（<09:30 检查窗下限，铁律1 盘前不动作）
- 10:20 / 11:11 偏移扫描 `:10` 槽位：读盘中 7 卡语义 `update_time`，worst age ≤11.4 分（≤35 分 SLA）→ FRESH_NO_ACTION

---

## 三、当前系统状态（实拉核实）

| 维度 | 状态 |
|---|---|
| **站点** | GitHub Pages `ah-quant999.github.io/quant-scanner-v8/` **HTTP 200** 可访问 |
| **main SHA** | `9a6787e56`（origin/main 顶端，本地已 fast-forward 对齐，零分叉；并行 12:02 档在本 SHA 上） |
| **本地 git** | 21 项在飞 WIP 已 `git stash push`（`handover-20260825-wip-preserve`，tracked）+ untracked 3 项（`raw_data/kline_cache/`、`momentum_metrics.json`、`strong_breakout_history.json`）保全，工作树 = origin 新鲜态 |
| **Runner** | **online**，1/1 ok（summary ok1/fail0），last run 11:43:03 |
| **健康总览（实时）** | `overall=warn`，`ok103 / warn1 / fail2 / total106`（updated 11:46:25） |
| **FAIL（2）** | `COCKPIT_ADVICE`(last 08-23 15:32, heal=algo_run) · `all_SECTOR_RECOMMENDATION`(last 08-22 07:59, heal=algo_run) — 均盘后 algo_run 产物，**18:30 盘后链补齐即清**（属开盘时段自然陈旧，非故障） |
| **WARN（1）** | `raw_volume`(无 last_update / 无 heal) — 观察项 |
| **看门狗（第374棒 11:25）** | 六路全 OK / 0 邮件；健康 rc=2（ok102/warn1/fail3） |
| **驾驶舱定性** | 板块推荐 `[2.7]` 报 ✅ 但产字节相同陈旧内容 → 增量推送空推；属 18:00 前上游不刷新，**今晚 18:00+ 为验证锚点** |
| **剩余自动化** | 15 个 v8 任务（14 ACTIVE + 1 PAUSED：TDX跟踪，id `1786933089412`），模型均为 hy3 |

---

## 四、需阿狸咪注意 / 待办 / 风险

1. 🔴 **板块推荐卡治本待验证（最高优先）**：`82ade974c` 已修代码，但 `data/SECTOR_RECOMMENDATION.js` 当前数据仍停 08-22 07:59（健康项 `all_SECTOR_RECOMMENDATION` fail 即此）。**今晚 18:00+ 盘后 algo_run 链为验证锚点**——确认数据**内容时间戳真正刷新**（不再停 3 天前），而非仅 mtime 新鲜。
2. 🟠 **看门狗结构性盲区整改（待排期）**：六路只看 run状态+commit age+HTTP200，**不校验数据实体非空** → 「age 新鲜 + 空数组」是漏报盲区（今晨 7 卡空白事故即此）。建议补 `sum(len(list))>0` 断言。小九已记教训，待排期落地。
3. 🟠 **mtime 假新鲜**：`SECTOR_RECOMMENDATION` mtime 05:21 但内容停 3 天前；健康检查读 raw_data/origin 侧与前端真消费层 data/*.js 口径不一致，须对齐。**判真伪只信内容内部时间戳，不信 mtime**。
4. 🟡 **清冲突标记教训**：09:27 误保留 `premarket_cleared=true` 占位版是今日事故直接原因——清冲突标记**勿误留盘前清空占位**覆盖 raw_data。
5. 🟡 **CRLF 静默少推坑已立铁律**：Python `print()` 清单 CRLF → bash `for f in $(cat)` 带 `\r` → `git hash-object` 静默失败、18/19 漏注入（首推仅 1 文件仍过闸）。后续补抓务必：`tr -d '\r'` + `while IFS= read -r` 逐行 + 注入循环累加 err + 安全闸新增「diff 文件数 == 预期数」校验。
6. 🟡 **本地 WIP 保全清单**（交班后勿丢）：stash `handover-20260825-wip-preserve` 含 `algorithms/market_regime.py`、`sector_recommendation.py`、`data/HEALTH_CHECK.js`、`data/PORTFOLIO.js`、`raw_data/*.json`(21 项在飞) + untracked `raw_data/kline_cache/`、`momentum_metrics.json`、`strong_breakout_history.json`。
7. 🟢 **STOCK_QUOTE** 今日已由 `v8_stock_quote_refresh` 多次自动刷新（213311cfd/03aeba0a0），保持关注即可。
8. 🟢 **cn runner** 当日 11:00–11:43 多轮 cn fetch 正常（11:42 三连发全绿），盘中主通道自举良好。
9. 🔒 **红线勿动**：`index.html` / `HEALTH_CHECK.js` 在阿狸咪编辑/构建期间禁止触碰；小九白天独家，18:00 后交阿狸咪。

---

## 五、小九监督职责（本档收口）

- ✅ 当班 08:00–12:00 完成：第 372/374 棒看门狗（六路全 OK / 0 邮件）、P0 盘中 7 卡空白事故止损（`629b45111`）、盘中漏轮兜底守护 3 次（09:20/10:20/11:11 均判新鲜零动作）、板块推荐卡永久冻结治本（`82ade974c`）。
- ✅ 维持自动化运转：盘前 08:15 全量 / 盘中每 30 分兜底 / 15:30 收盘 / 17:00 选股四模块 / 18:35 三重复查。
- 🔄 **18:00 后交班阿狸咪**（夜间/周末部署铁律）：本机 WIP 保留待续，18:30 盘后 algo_run 链为板块推荐卡解冻验证锚点。
- 📌 出差期间「一劳永逸根治」优先：今日三处修复（空白止损 / 兜底 / 板块冻结治本）均为根因级，非逐个修补。
- 📌 下次 12:00 / 18:00 交接自动续跑；若遇并行实例已提交同名 HANDOVER，按 superset 合并（不重演覆盖事故）。

---
*本档由自动化 12:00 档生成（superset 合并并行 12:02 档 `9a6787e56`）；若阿狸咪接手后发现与线上不符，以 `git log origin/main` 实测 SHA 为准。*
