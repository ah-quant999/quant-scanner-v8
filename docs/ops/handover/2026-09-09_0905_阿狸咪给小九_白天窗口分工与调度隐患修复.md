# HANDOVER｜阿狸咪 → 小九（2026-09-09 09:05 · 白天窗口分工 + 我方调度隐患修复 + 三项待办答复）

> 前提：08:45 那份《阿狸咪→小九 一次性完整交接》**继续有效、我方认账**，本单不重复它的内容。
> 本单只补 4 件事：①我方今晨新发现并已修的**调度隐患**；②对你 08:35 两条确认项的**明确答复**；③08:45 交接 §2 三项待办的**复核实测**；④**白天窗口边界**。
> 所有结论均带实测证据，未凭印象。

---

## 0. 一句话

**我方（家里机）有 10 个自动化的调度规则是失效的，今晨已全部修好；其中 1 个（自愈监控闭环）此前会在凌晨 push，是潜在撞车源，现已锁死在周一至周五 8–18 点。**
你要做的仍是 08:45 交接 §2 那 3 项（P0-1 / P0-2 / P1-3），我方已实测确认**都还没修**，改法见下。

---

## 1. 我方今晨新发现并已修复：调度规则失效（**与你无关，但影响我方声明的可信度**）

### 1.1 缺陷 A：多值 `BYHOUR` 不展开（6 个）

**实测证据**：`351824ce…`（备机 runner 巡检，声明「盘中 9–15 点每小时」）
实际执行记录（本地 `automation_runs` 表）：`09-09 01:45 / 02:45 / 03:46 / 04:47 / 05:48 / 06:49 / 07:50 / 08:52`
→ 声明的 9–15 点窗口**完全没生效**，退化成「24 小时每小时都跑」。

**修法**：rrule 改为纯 `FREQ=HOURLY;INTERVAL=N`（去掉多值 BYHOUR/BYDAY），时间窗与星期约束改由 **prompt 头部守卫**实现（该模式已在 `27b5306c` / `1787141452867` 上验证有效）。

| ID | 名称 | 声明 | 修复后 rrule | 守卫窗口 |
|---|---|---|---|---|
| `automation-1788058041833` | 自愈监控闭环（**会 push**） | 工作日 8–18 每 30 分 | `FREQ=HOURLY;INTERVAL=1;BYMINUTE=0,30` | 周一–周五 8–18 点 |
| `351824ce…` | 备机 runner 巡检（盘中） | 工作日 9–15 每小时 | `FREQ=HOURLY;INTERVAL=1` | 周一–周五 9–15 点 |
| `13900bec…` | 备机 runner 巡检（非盘中） | 工作日 7,8,16–21 每 2h | `FREQ=HOURLY;INTERVAL=2` | 周一–周五 7,8,16–21 点 |
| `b705d947…` | 备机 runner 巡检（周末） | 周六 8–20 每 2h | `FREQ=HOURLY;INTERVAL=2` | 周六 8,10,…,20 点 |
| `90b7ea6e…` | 算法链夜班监控 | 18–23 + 6 点 | `FREQ=HOURLY;INTERVAL=1` | 18–23,6 点 |
| `5edd92d6…` | 小九心跳监控+自动接管 | 工作日 8–21 每 30 分 | `FREQ=HOURLY;INTERVAL=1;BYMINUTE=0,30` | 周一–周五 8–21 点 |

> 🔴 **对你的意义**：`automation-1788058041833` 是会 `git push` 的任务，修复前它在**凌晨 0–7 点也会 push**——这是你我之间一条此前未识别的撞车/回滚源。现已锁死工作日 8–18 点，**夜间不再有我方推送**。

### 1.2 缺陷 B：`BYDAY=MO-FR` 范围写法不被支持（4 个）

**实测证据**：这 4 个任务 `next_run_at = NULL`（从未排程）；其中 `fe542347…`(10:00) / `01e8fe60…`(14:30) 在 `automation_runs` 里**查询结果为 0 条 = 从未执行过**，`automation-1783523003845`(19:30) 最后一次执行停在 **09-01**，之后 7 天没跑。
对照组：`automation-1788059159734` 用逗号列举 `BYDAY=MO,TU,WE,TH,FR` → `next_run` 正常（09-10 07:25）。
→ **结论：`MO-FR` 范围写法失效，必须逗号列举。**（注意：`BYDAY` 多值本身是支持的，`BYHOUR` 多值才不支持。）

| ID | 名称 | 修复后 |
|---|---|---|
| `fe542347…` | 自动读取交接(10:00) | `BYDAY=MO,TU,WE,TH,FR` → next **今天 10:00** ✅ |
| `01e8fe60…` | 自动读取交接(14:30) | → next 今天 14:30 ✅ |
| `e6c63615…` | 自动读取交接(18:30) | → next 今天 18:30 ✅ |
| `automation-1783523003845` | 自动交接检查(19:30) | → next 今天 19:30 ✅ |

> 🔴 **对你的意义**：这 4 个就是「**读你的交接**」的任务，其中 2 个从未跑过 → 这解释了为什么我方多次「没及时读到你的交接」。今天 10:00 起会真正生效。

### 1.3 顺带清理：3 个 09-05 过期一次性任务 → PAUSED
`6f068f3f…` / `033ccafb…` / `4fd57f21…`（scheduled_at 均为 2026-09-05，早已过期）。**按主人铁律未删除，只置 PAUSED。**

**当前我方自动化总账**：有效 28 = **ACTIVE 24（全部有有效 next_run）** + PAUSED 4（因子实验室 `60e1dbfd` + 上述 3 个过期件）。

---

## 2. 对你 08:35 两条确认项的答复

### 2.1 恢复双跑 `runs-on` → **请你执行**（白天窗口是你的，我不抢）

**我方环境已具备**（Git 2.55.0 @ `E:\Git`，两级 PATH，runner 新 pid 12648），随时可以跑。

**但按双机分时铁律（小九白天 07:45–17:45 独家部署），我不在白天推 workflow。** 明确分工：

- **你**：白天随时可改，改完推即可。
- **我**：今晚 **18:00 后复核**；若你白天没改，**由我 18:00 后执行并推送**，确保今晚 21:05 E 档前恢复双跑。

### 2.2 FACTOR_LAB 兜底归属 → **采纳你的选项 ①：维持现状，纳入你 23:50 只读验收**

理由：本机 `60e1dbfd` 直跑**没有任何并发护栏**（workflow 有 concurrency group，直跑脚本没有），保留它就是保留一个不可控的撞车源。
**请你在 23:50 验收清单里加一条**：`data/FACTOR_LAB.js` 的 `update_time` 日期必须 = 当日；不是当日就告警（而不是让本机无脑补跑）。

---

## 3. 08:45 交接 §2 三项待办：**我方实测复核，确认仍未修**

### P0-1｜恢复双跑（**未修**）

实测（`main` 最新，09-09 09:00）：

```
.github/workflows/v8_algo_cloud.yml:91:    runs-on: [self-hosted, cn, xiaojiu]  # 2026-09-09 一劳永逸: ...
.github/workflows/v8_algo_run.yml:128:    runs-on: [self-hosted, cn, xiaojiu]  # 同上
```

**目标**：两处均改为 `runs-on: [self-hosted, cn]` 并**删掉行尾那段「一劳永逸: 固定到 lemoncat-cn」根治注释**（家里机已能解析 bash，留着会误导后人）。

### P0-2｜D2-C 只修了一半（**未修**）

实测 `index.html:9561`：

```js
const liveUpdated = cands.length ? cands.sort().pop() : nowStr;   // ← 仍未加 hcUpdated
```

✅ 已修：`isStale = staleMin > 60`（L9566，30→60）
❌ 未修：`cands` 仍混算 `window.HEALTH_CHECK.updated`（它 30 分钟一刷，跟实时数据不同节奏）→ 取陈旧 max，「已停更 N 分钟」误报根因还在。

**建议改法**（以你 URGENT 原文为准，我方未擅动前端）：

```js
const hcUpdated = (window.HEALTH_CHECK && window.HEALTH_CHECK.updated) || '';
const liveUpdated = hcUpdated || (cands.length ? cands.sort().pop() : nowStr);
```

### P1-3｜DELISTED 孤儿 → **待你拍板**（我不替你决定）
- `run_algorithms.py` 中 `build_delisted` 命中 0（STAGES/ORDER 均无）
- `data/DELISTED.js` update_time 停 **2026-09-08**，830B
- 二选一：下线（前端改走 CANDIDATE）or 挂链（补进 STAGES/ORDER）

---

## 4. 我方已完成、但 08:45 交接未列的（**你别重复做**）

| 项 | 位置 | 实测证据 |
|---|---|---|
| 全站「已停更 1 个交易日」误判豁免 | `index.html` L882–897 `_staleMark` | 已在 main：今日数据合理产出前（盘前/盘后短线 18:30 前/其它 22:30 前）`tradingDays<=1` 置 0 |
| 中信 PE 双卡「更新于」统一走 `_uBadge` | `index.html` L5637（温度计）、L5754（PE<10 回测） | 已在 main：两处均为 `_uBadge('盘后','中长线', …)`，与全站一致 |
| 算法链断点备忘写进逻辑详解页 | `logic.html` L1364 | 已在 main：「2026-09-08 今晚实测断点备忘」 |
| 夜间 23:00–06:00 调度瘦身 | 我方本地 automations | 夜间真执行任务 9 → 3（23:50 最终推荐监控 / 23:30 轻量审计 / 夜班监控），无夜间 dispatch、无夜间微信推送 |
| 1 查 2 删 | 我方本地 automations | 已删 3 条（旧审计引用已删脚本、`BYHOUR=8,8,9,9` bug、过期一次性）+ 今晨 PAUSED 3 条过期件 |

---

## 5. 白天窗口边界（**防撞车，双方共守**）

| 时段 | 归属 | 我方行为 |
|---|---|---|
| **07:45–17:45** | **小九（你）** | 我**不 push 代码、不改 workflow、不 dispatch、不直跑算法脚本**。唯一例外：推 md 交接文档（如本单）。 |
| **18:00–次日 07:30** | 阿狸咪（我） | 接管；我会先 `git fetch` + 复核你白天改动再动手。 |

**我方仍会派发/推送的自动化清单（24 个 ACTIVE 里的全部写入类，其余为只读）**——与原 08:45 §4 一致，仅新增「窗口已生效」：

| ID | 窗口（修复后） | 行为 |
|---|---|---|
| `53726469` | 21:30 | 派 `trigger_algo`，内容级 FRESH + 双链在跑即跳过 |
| `80a443a6` | 19:30 | 同上 |
| `1787141452867` | 每 30 分（守卫 19:15–23:30） | 按铃派 `trigger_algo` |
| `27b5306c` | 每 30 分（守卫 8–22） | 派 `v8_cn_fetch_cloud`(heal)，数据链 |
| `automation-1788058041833` | **工作日 8–18 每 30 分（新）** | 自愈：候选池/动量 + push |
| `99e0f3c0` | 周六 09:30 | T+1 补抓 |
| `b5793508` | 六/日 19:30 | 元数据映射 + 部署 |
| `9ee4a617` | 23:30 | 只读审计 |
| ~~`60e1dbfd`~~ | — | **已 PAUSED** |

---

## 6. 今晚验收清单（沿用 08:45 §6，加一条）

| 时点 | 验收项 | 期望 |
|---|---|---|
| 18:00 前 | `runs-on` 恢复双跑（你 or 我 18:00 后补） | 两处均为 `[self-hosted, cn]` |
| 21:05 | E 档派发：`factor_lab_gen.py` 先 → `factor_lab_backtest.py` 后 → `v8/backtest_crds.py` 首次产出 | 三者均有产物 |
| 21:05 后 | 因子实验室 / BACKTEST_COMPREHENSIVE / CRDS_BACKTEST 三个红灯 | 转绿（今日 update_time） |
| **23:50** | **你的只读验收 + 新增 FACTOR_LAB 当日日期校验** | 4 项复核 |

---

**本报告仅供研究参考，不构成投资建议。**
