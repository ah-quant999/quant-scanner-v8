# 小九 → 阿狸咪 · 2026-08-07 交接文档

> **更新于 2026-08-07 17:55 手动更新（覆盖 12:00 档，新增下午「数据根因修复 + v6 仓库彻底清理 + 自动化纯 v8 化」）**
> 面向：阿狸咪（18:00 后 / 周末纯代码编辑，不跑 deploy_v8.py）
> 源：当天工作日志 `.workbuddy/memory/2026-08-07.md`（截至 17:5x 全部改动）
> 当前 main HEAD：`5e61f1ab`（runner health: online @ 17:47）；最新构建 `096af4f8`（v8 build 17:34，已上线）

---

## 一、背景

今日是「数据治理 + UI 紧凑化 + 根因修复 + v6 退役」大改日，分两段：

**上午**：发生「健康巡检大面积失败」「实时数据全空（本地工作树事故）」「三卡/全量数据停更 3~8 天」三起事故，均已修复并收口；完成多轮 UI 紧凑化。

**下午（17:xx，本次重点）**：主人发现"网站数据还是 3 天前的"，根因追到两处——
1. `stage_to_raw.py` 把 `out/` 下 08-06 僵尸副本覆盖到 `raw_data/` 当日新结果 → 前端长期显示旧数据。已加 `SKIP_STAGE` + 时间戳守卫根治（commit `5e46f281` / `66460751`）。
2. 部分自动化仍在 `cd repo-temp` 跑 v6 生成器，未跑 v8。已将 17:00 / 周末 15:30 两个四模块任务改为**纯 v8 算法链**（不再 cd repo-temp / 不再 sync_v6_to_v8 CLI 调用），17:00 已成功跑出（`fecf59ca`）。
3. 按主人令「v6 能删就删」：**repo-temp（v6 仓库）已彻底删除**（零 v8 硬依赖，详见第三节）。

**一句话给阿狸咪**：今日全部代码/数据改动已安全入库（核心修复在 `5e46f281`/`66460751`，均在 origin/main）；v6 已退役，v8 完全自包含。你侧按常规只读 + 18:00 后纯代码编辑即可，注意 repo-temp 已不存在（见第四节）。

---

## 二、今日已完成改动（真实 commit SHA 已核实，均在 origin/main 历史中）

### 数据治理与事故修复（上午）
| SHA | 内容 | 影响 |
|---|---|---|
| `7ac0777` | fix(CRDS)：calc_crds 兼容 dict 金股池、过滤港股(5位代码)、单票异常 try/except 兜底；补全 08-07 数据 | CRDS 不再崩溃 |
| `9e6b053` | 三重共识(3天→今日) + CRDS(6天→今日) 数据补更（含 crds_result.json→crds_card_data.json 文件名映射修正） | 两张卡恢复新鲜 |
| `831e99b` | 补回 NT_DATA 盘前刷新；build_deploy 监听改 `cn·应急` 新名；cn_fetch_cloud case 匹配 1-8 | 健康「市场提示」恢复 ok |
| `d01c475` | 修复 3 天停更：SH_FIB / SECTOR_RS / CANDIDATE / CRDS / 三重数据全部刷新 | 实验选股/候选池/板块周期恢复 |
| `df952d8`→`a95c0f1`→`be9afb4` | 全量算法链修复三连：9 核心 + 4 孤儿 + TOP10 + market_alerts 全部恢复 | 12 张停更卡全部恢复 |
| `915c5af` | 全链刷新（run_algorithms 22/22 成功）：GOLD_POOL 下游链 + COCKPIT_ADVICE + TRIPLE_HISTORY | gold_pool 下游不再是停更盲区 |
| `3bfe2c9` | fix(cloud)：重新实现盘前清空函数——保留主力净流入不再整张清空，仅 premarket 触发 | 修复「实时数据全空」根因 |
| `5dc27ed` | fix(IPO)：今日上市按 list_date==today 区分；删除「实时风险温度计」「决策中枢」两卡 | 卡总数精简 |
| `00f745a` | fix：IPO 影子股匹配 + Tab 颜色改亮 | 已上线 |

### UI 紧凑化（主人拍板风格：不要大版面，2-3 列/单行紧凑，上午）
| SHA | 内容 |
|---|---|
| `814998c` | 今日判定卡紧凑化 |
| `134a563` | 机游共振「净卖出」从真实负净值取 |
| `cc0655f` | 今日判定环境卡再压缩 |
| `6ec0d23`/`4897957` | 连板天梯 UI 微调/分布 ≥3板写入数字格 |
| `8c1efb9` | IPO 影子股不再过滤 |
| `9a7dd1c`/`0e9dd06` | 温度计+决策中枢移入🔒暂未上架 / 🗄️已下架 |
| `8483488`/`9634627`/`6ea4347`/`4aecb48` | 机游弹窗单行 / health 对齐 / 决策四步合并 / 已跑完胶囊统一 |

### 下午补充：数据根因修复 + v6 仓库清理（17:xx，本次核心）
| SHA | 内容 | 影响 |
|---|---|---|
| `66460751`（含 `5e46f281`） | **根因修复①**：`algorithms/stage_to_raw.py` 加 `SKIP_STAGE={triple_consensus,triple_track}` 跳过生成器直写产物 + **时间戳守卫**（out 源比 raw 目标旧则拒绝搬运），防 out/ 08-06 僵尸覆盖当日新结果。同时 `v8_postclose_guard.py` 补上算法链产物监控、派发改云端主力（fetch=327687211 / algo=324119592），cn runner 仅在线兜底，离线绝不空转。 | 根治"数据长期显示 3 天前"；自愈不再漏检算法链产物 |
| `fecf59ca` | **根因修复②**：17:00 选股四模块任务改为纯 v8 算法链（run_algorithms.py → update_v8 → deploy_v8），不再 cd repo-temp / sync_v6_to_v8，17:23 成功跑出 | 消除"跑 v6 不跑 v8"路径 |
| `096af4f8` / `5e61f1ab` | 云端 17:34 build 上线 + 17:47 runner 心跳（cn runner online） | 站点最新 |

**v6 仓库（repo-temp）彻底清理（主人令"能删就删"）**
- 删除前零硬依赖审计：`stock-scanner/data` 不存在 → v8 的 V6_DATA_DIR 守卫读取永触发；v8 `algorithms/` 已含 repo-temp 全部 7 个核心生成器；云端工具链(cloud_fetch/watchdog/dispatch)与 `sync_v6_to_v8.py` 全在 v8；`_heartbeat.log` 已非监控信号源。
- 执行：① 备份 `C:/Users/Administrator/AppData/Local/Temp/repo-temp_backup_20260807.zip`（1675 文件 / 288MB，v8 稳定运行 1 天后可删）；② 2 个心跳自动化重定向（见第四节）；③ 删除 E: 符号链接 + D: 真实目录 `D:/stock-scanner-repo/repo-temp`，已删尽；④ 验证 v8 git 干净、无脚本引用 repo-temp。

### 凌晨阿狸咪侧改动（已同步，供回顾）
- `5af000c` 暂未上架提升为一级 locked Tab；`41329f2` 合并今日宏观解读+环境态势为一张卡；`e215f7b` 接入时事滚动与真实美股隔夜数据；`d65fd82` 共振日历格子缩小字号放大；`7d421be` 今日判定风控位置+连板天梯字号；`4fc6684` 板块周期卡按周期分组 20 日涨幅排序；`5ed70d5` posAdvice 崩溃修复；`0a8cb3c` 胶囊统一 CST 日期；`5198980` 构建不再篡改「更新于」+ 已跑完胶囊修复。

---

## 三、当前系统状态

- **站点**：GitHub Pages v8 在线；v8_build_deploy 持续构建，最新 **17:34 success（096af4f8）** 已上线。
- **main HEAD**：`5e61f1ab`（runner health: online @ 17:47）。
- **数据新鲜度**：本地 v8 `data/*.js` 59 个中 **55 个今日(2026-08-07)**；4 个"非今日"均为系统卡（BLOAT/HEALTH/RUNNER_STATUS 无时间戳 + RISK_GAUGE 周级温度计，已删卡不再重建）——用户报的"3 天前"算法/数据卡全部回今日。
- **本机应急 runner（cn·应急）**：online（17:47 心跳）；workflow_run 监听已改新名，改 raw_data 后自动触发构建 ✓。
- **云端 schedule**：`v8_cn_fetch_cloud` 今日仍不稳定（靠本机应急 runner 兜底）；`v8_algo_cloud` cron 今晚 18:30 为修复后首窗口，请观察。
- **算法链（纯 v8）**：`run_algorithms.py` 全程：生成选股/机游/LHB/CRDS/驾驶舱/板块RS/FIB/回测等 → `stage_to_raw`（含 crds_result.json→crds_card_data.json 桥接，见第四节 P1）→ api_push_raw。17:00 已成功跑出（`fecf59ca`）。
- **v6 / repo-temp**：**已彻底删除**，v8 零依赖；唯一保留的跨仓引用是 `stock-scanner/.workbuddy/v8_gh_token.txt`（token，必须保留）。
- **健康巡检**：连续多轮六管线全绿。
- **自动化**：14 个 v8 任务（8 ACTIVE + 6 PAUSED），model 全部 `deepseek-v4-flash`（8-06 主人令，回 hy3 待主人另行通知）。其中小九侧 `17:00 四模块`/`周末15:30 四模块` 均已改为纯 v8 并 ACTIVE。

---

## 四、需阿狸咪注意 / 待办 / 风险

1. **【已解决·原 P0】17:00 四模块任务**：小九侧 `automation-1785404904506` 已改为纯 v8 并 **ACTIVE**，今日 17:23 成功跑出（`fecf59ca`）。你侧对应的四模块任务按分时铁律**保持 PAUSED（白天不部署）属正常**，无需处理。
2. **【已解决·原 P1】calc_crds 输出名**：`calc_crds.py` 写 `out/crds_result.json`，`update_v8.py` 读 `raw_data/crds_card_data.json`。桥接由 `stage_to_raw.py`（import sync_v6_to_v8 的 V6_TO_V8，含 `"crds_result.json":"crds_card_data.json"`）在 run_algorithms 链尾自动完成，**无需单独 sync 步骤**，CRDS 卡正常刷新。请勿在自动化里再加 sync_v6_to_v8 CLI 调用（会重复且已无意义）。
3. **【重要·本机差异】repo-temp 已删除**：你本机若任何脚本/文档仍引用 `E:/workspace/stock-scanner/repo-temp`，必须改指向 `E:/workspace/quant-scanner-v8`。但按铁律你 18:00 后只做纯代码编辑、不跑 deploy，影响极小。
4. **【心跳路径变更】**：`15:30 收盘` 与 `12/18 点交接` 两任务的心跳日志已从 `repo-temp/_heartbeat.log` 改写 `E:/workspace/quant-scanner-v8/_heartbeat.log`（已 update 成功，验证 0 个自动化仍写旧路径）。
5. **【备份清理】**：`C:/Users/Administrator/AppData/Local/Temp/repo-temp_backup_20260807.zip`（288MB）确认 v8 稳定运行 1 天后可删，腾 C 盘空间。
6. **【约定】部署闸门不变**：工作日 08:00~17:00 小九唯一部署者；阿狸咪白天 deploy 会被闸门 exit 0；18:00 后/周末可纯代码编辑但**不跑 deploy_v8.py**。
7. **【模型】全部 deepseek-v4-flash**；**不要自行切回 hy3，等主人通知**。

---

## 五、小九监督职责（运行中）

- 12:00 / 18:00 交接文档自动更新（本任务；本次为 17:55 手动补更新，下一档 18:00 自动覆盖）。
- 每小时看板 pull + 看门狗巡检（六管线连续全绿）。
- v8 盘后数据守护（self-heal，CST 16:00-21:00 每小时）：检测盘后关键数据非今日/runner 离线 → 自动 dispatch 补救（已修为云端主力派发，离线不空转）。
- 19:00 审计：盘后数据停更 >1 天则本机补跑算法链自愈。

---

*本交接由小九手动更新（17:55 档，覆盖 12:00 档）。下一档 18:00 自动覆盖更新。*
