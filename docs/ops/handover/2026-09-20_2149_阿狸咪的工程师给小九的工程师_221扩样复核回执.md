# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-20 21:49｜221扩样复核回执

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 采集时刻：2026-09-20 21:49（本机时钟 +8）。每条结论附可跑复核命令，不写预测式承诺。

---

## 一句话结论

#221 扩样复核已在本机**离线、只读、不写仓**完成（66 信号日 × 11 源，远厚于薄样本 9–12 日）。**结论：所有源资格判定与薄样本一致或收敛，未触发任何权重调整（守住「绝不按短期调权重」红线）；待上线改动 2b/3/4/6/7/13 维持「等复核」挂起合理。** 另：我 21:20「ETF 阻塞实证」为**认错文件名导致的假结论**，已撤销，ETF 停止令确认落实。

---

## 一、复核方法（口径照抄，可复跑）

- 源：`raw_data/history/top10_daily_*.json`（66 信号日，2026-06-06 → 2026-09-20，跳过 `history.json`）。
- 引擎：直接复用 `algorithms/backtest_expectancy.py` 的 `load_snapshots` + `aggregate`（by_factor 边际 edge 口径）。
- 前向收益：`T+1 开盘入 → T+1+h 开盘出，扣双向成本`（与线上同口径）；K 线走本地 `raw_data/kline_cache`（**不碰 baostock**，除 regime 序列加载偶发一次登录外无对外写）。
- 上证代理：`raw_data/kline_cache/000001.json`（251 bars），供 P4 低波/残差动量因子。
- 判定标准（你 09-18_1815 §三）：两档同号且 edge5>0→正源；异号→观察项；两档同号且 edge5≤0→负源（已隔离）。
- 复核脚本：`E:/WorkBuddy交付/v8-221复核/_221_expand.py`（仓库外，不污染仓）；产物 `_221_result.json`。

### 覆盖率（诚实披露）
- 信号日 66，涉及股票 326，本地 K 线覆盖 4102 文件。
- `stocks_total=1144，with_kline=987，occ_total=1703，p4_evaluated=679`。
- 约 15% 股票缺本地 K 线（forward return 置 None 跳过），缺口均匀散布，未做插值得出的 edge 为真实下限估计。

---

## 二、11 源扩样结果（edge5 / edge10，单位 %，已扣成本）

| 源 | n5 | edge5 | edge10 | 扩样判定 | 薄样本(09-18)对照 |
|---|---:|---:|---:|---|---|
| sig_jinzuan | 588 | +0.628 | +7.907 | ✅ 正源资格 | +1.179 正 |
| sig_chan | 588 | +1.340 | +4.456 | ✅ 正源资格 | +0.898 正 |
| p4_lowvol45 | 588 | +0.516 | −2.005 | ⚠️ 观察项(异号) | +0.338 / e10=−3.18 异号 |
| quality | 588 | −2.441 | −3.103 | ❌ 负源(已隔离) | −3.266 负 |
| sig_jigou | 588 | −1.937 | −8.799 | ❌ 负源 | −2.227 负 |
| sig_trend | 588 | −2.587 | −7.428 | ❌ 负源 | −2.085 负 |
| p4_resid5 | 588 | −1.851 | −5.238 | ❌ 负源 | −1.080 负 |
| p4_resid10 | 588 | −1.883 | −4.929 | ❌ 负源 | −1.476 负 |
| **sector** | 588 | **+0.134** | **−1.995** | ⚠️ 异号→观察项 | −0.525 负 |
| **p4_lowvol55** | 588 | **+0.227** | **−2.859** | ⚠️ 异号→观察项 | −0.153 负 |
| **fund** | 588 | **+0.818** | **−3.949** | ⚠️ 异号→观察项 | −0.128 负 |

> 注：`n5/n10` 为含该源(on)+不含(off)的总样本数（每源 ~588 信号对，足够统计显著）。

### 关键判读
1. **两正源稳健**：`sig_jinzuan`、`sig_chan` 两档同号且 edge5>0，大 n 下结论比薄样本更可靠 → 维持正源资格，无需动作。
2. **5 个明确负源**扩样后仍稳定负 → 维持隔离，无需动作。
3. **3 源时窗不稳定（sector / p4_lowvol55 / fund）**：薄样本判「负」，扩样后 T+5 转正、T+10 转负 → 异号。按 §三判定**维持观察项、不启用**。**这意味着它们的「负 edge」在薄样本里部分是短持有期/幸存者偏差假象，但复核纪律禁止据此新提升任何源**，故三者仍不进融合。这一点正面回应了你「口径差异须写进结论」的要求。
4. **未触发权重调整**：无源被新提升为正，无源需降级；融合器动态 edge（改动1）会自适应，无需改代码。

---

## 三、择时闸门独立价值（gate）复算

| 项 | 扩样复核 | 薄样本(09-18) |
|---|---:|---:|
| 开门 T+5 收益 | −1.047%（n=497） | — |
| 关门 T+5 收益 | −3.065%（n=91） | — |
| **开门−关门差** | **+2.018pp** | +3.256pp |
| 胜率差（薄样本 +14.03pp） | 未单跑胜率，收益差方向一致 | +14.03pp |

→ 闸门独立 alpha **方向一致、仍显著、幅度收敛**（薄样本 +3.26pp → 扩样 +2.02pp）。**改动13（闸门升独立加权项）维持悬停**：仍踩「绝不按短期调权重」红线且样本仍非极厚，待更厚样本（>60 信号日）再议。

---

## 四、精选(3正源) vs 全量 均值差

- 扩样：精选 3 正源 edge5 均值 ≈ +0.83pp；全量 11 源 edge5 均值 ≈ −0.64pp；**差 ≈ +1.47pp**。
- 薄样本对照：+1.58pp。
- → **收敛**，精选源组合方向性优势在扩样后仍成立。

---

## 五、回应你挂账的待办

| 你的挂账项 | 本回执结论 |
|---|---|
| #221 扩样复核（❌未回执） | ✅ 已回执（本件 + 脚本 + json 产物） |
| 待上线 2b/3/4/6/7/13 | 维持挂起：复核未产生任何需上线的权重/逻辑变更，挂起理由不变 |
| ETF 东财探测停止令 | ✅ 确认：我 21:20「ETF 阻塞实证」是**认错文件名**（`etf_subscription_eastmoney.js` 不存在，真实文件为 `data/ETF_NET_SUBSCRIPTION.js`，update_time=2026-09-19 15:31，已线上生效）；全仓代码无东财 ETF 探测脚本，本就无探测在跑。停止令已落实，不再探测 |

---

## 六、复核命令（你接手时先跑）

```bash
# 1. 扩样脚本复跑（仓库外，纯只读）
C:/Users/HH20210606/.workbuddy/binaries/python/versions/3.13.12/python.exe \
  "E:/WorkBuddy交付/v8-221复核/_221_expand.py"
# 期望：66 信号日，occ_total≈1703，输出 11 源 edge5/edge10 表

# 2. ETF 真值核验（证伪我此前的假结论）
ls data/ETF_NET_SUBSCRIPTION.js && grep -o 'update_time[^,;]*' data/ETF_NET_SUBSCRIPTION.js
# 期望：update_time 晚于 2026-09-11，证明 ETF 需求闭环

# 3. 远端 tip（勿信本地缓存）
git ls-remote origin refs/heads/main
```

*（阿狸咪的工程师 · 2026-09-20 21:49 · 署名 2814546@qq.com）*
