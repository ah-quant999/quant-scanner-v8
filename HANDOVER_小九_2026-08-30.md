# HANDOVER 小九 · 2026-08-30 凌晨

## 本次改动总览（主人令 2026-08-30）

### 1. 已下架 vs 选股策略重复模块对齐
- **已确认重复**：截图中的「观测候选股池」在「已下架·盘前」与「选股策略·🧭 选股池」两处内容完全一致（372 只 / 主板99 / 创业板101 / 科创板102 / 港股50 / 观澜台21）。
- **处理**：已删除 `renderDelisted()` 开头的「观测候选股池」卡片，仅保留「选股策略」子页作为唯一入口。

### 2. 选股策略 Tab 顺序调整
- 默认首 tab 从「🥇 三重共识」改为「🧭 选股池」。
- Tab 顺序：🧭 选股池 → 🥇 三重共识 → 📈 相对强度 → 📊 逆势龙头。
- 进入「选股策略」section 时自动 `switchSt('pool')`。

### 3. 选股池来源行改为点击展开
- `renderStPool()` 重构：
  - 顶部保留 5 个来源 chip + 共 N 只
  - 新增「▾ 展开明细」按钮（id=`stPoolToggleBtn`）
  - 个股明细默认折叠（`#stPoolStockList style="display:none"`）
  - 点击后按来源分组展开（主板/创业板/科创板/港股/外资研投）

### 4. 全站「观澜台」→「外资研投」
- 已替换范围：
  - `index.html`（22 处）
  - `algorithms/build_candidate_pool.py`
  - `algorithms/guanlan_extractor.py`
  - `algorithms/calc_crds.py`
  - `algorithms/scanner.py`
  - `algorithms/stage_to_raw.py`
  - `self_heal_monitor.py`
  - `scripts/setup_credentials.py`
  - `scripts/monitor_new_run.py`
  - `scripts/monitor_v8_run.py`
  - `scripts/monitor_v8_validate.py`
  - `scripts/show_github_secrets.py`
  - `.github/workflows/v8_algo_cloud.yml`
  - `.github/workflows/v8_cn_fetch_cloud.yml`
  - `wf_algo_cloud.yml`
  - 当前数据：`raw_data/candidate.json`, `raw_data/candidate_members.json`, `raw_data/gold_pool.json`, `raw_data/build_candidate_debug.json`, `out/candidate_pool.json`, `out/candidate_members.json`, `out/gold_pool.json`, `data/CANDIDATE.js`, `data/GOLD_POOL.js`
- **未改动**（历史文档/备份，非主站）：`HANDOVER_*.md`, `URGENT_*.md`, `docs/ops/handover/*.md`, `raw_data/history/*.json`, `_cdn_check2/`, `_audit_blk/`, `backup_*/`, `.bak` 文件。
- 数据来源键名已从 `"观澜台"` 改为 `"外资研投"`，source_dist 中显示 `"外资研投": 21`。

## 验证结果
- inline script 语法：0 错误（29 段）
- `guard_index_sections.py`：通过（1.0 MB / 1.2 MB）
- Puppeteer（Edge）：
  - 选股策略默认 tab = 🧭 选股池 ✅
  - 来源 chip 含「✓ 外资研投 21」✅
  - 点击「展开明细」后显示个股分组 ✅
  - 已下架无「观测候选股池」、无「观澜台」✅
  - console 0 错误 ✅
- 线上 `index.html` 含「外资研投」22 处、「观澜台」0 处 ✅

## Git 状态
- 本地 HEAD：`d8b3babed`（合并后）
- 远端 HEAD：`d8b3babed`（已 push）
- 提交链：
  1. `dfa9fdeaa` feat(v8): 选股策略默认首 tab 改为选股池，删除已下架重复观测候选股池，来源行改为点击展开
  2. `ac5086102` refactor(v8): 全站观澜台更名为外资研投（算法、脚本、workflow、自愈监控）
  3. `430df68a1` data(v8): 当前候选池/金股池数据同步观澜台→外资研投命名
  4. `d8b3babed` fix(v8): 一劳永逸 5 项 UI 修复（banner移走/8列/交易时段/强势突破加动量共识/data stocks）【云端/你方最新 build，已合并到最上层】

## 注意事项
1. **并发操作**：本次推送期间有另一 agent（小九/云端）同时提交 `d8b3babed`，产生 `MOMENTUM_FILTER.js` 合并冲突，已采用云端最新版本（update_time 2026-08-30 00:19:22）。
2. **文件命名未改**：`guanlan_reports.json` / `guanlan_watchlist.json` / `ZSXQ_TOKEN` 等内部文件名/Secret 名仍保留 guanlan，仅用户可见的中文标签改为「外资研投」。如需彻底改名请告知。
3. **历史文档未改**：`HANDOVER_*.md` 等历史交接文档中仍保留「观澜台」字样，属于历史记录，不影响主站。

## 小九后续 todo
- [ ] 云端下次 build 时确认 `build_candidate_pool.py` 输出 source_dist 中「外资研投」计数正常。
- [ ] 确认 `guanlan_extractor.py` 在算法链中运行后，`data/CANDIDATE.js` 的 `source_dist.外资研投` 不为 0。
- [ ] 若用户要求，可进一步把 `guanlan_*.json` 文件名与 `ZSXQ_TOKEN` 说明统一改为「外资研投」拼音/英文。
