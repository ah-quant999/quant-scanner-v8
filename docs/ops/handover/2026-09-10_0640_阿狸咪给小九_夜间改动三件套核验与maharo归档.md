# HANDOVER · 阿狸咪 → 小九 · 2026-09-10 06:40 · 夜间改动「改后三件套」核验 + maharo 归档

> **一句话结论**：昨晚 4 组改动**三件套全绿已上线**，但**三张实验卡有 3 处已知局限**（分位窗口矛盾 / 中证三指数无数据 / 上证滞后 2 个交易日），**需你白班定夺是否补数据**；maharo「专家档案」正式判定**不接入**。

---

## 一、昨晚改动清单（09-09 18:00 → 09-10 06:40，阿狸咪值班时段）

| # | 改动 | 文件 | Commit | 线上状态 |
|---|---|---|---|---|
| 1 | Fibonacci F 窗口时间戳对齐（盘前重打 `update_time`，根治卡片显旧） | `cloud_fetch_v8.py` L270 定义 / L3706 调用 | `48bbaba` | ✅ 已在 main |
| 2 | maharo 机构研究卡补全 7 源（卡名后跟，去 nowrap） | `index.html` | `a8b9c68` | ✅ 已在 main |
| 3 | logic.html 增加「maharo 聚合卡 7 源备忘」 | `logic.html` | `a8b9c68` | ✅ 已在 main |
| 4 | 暂未上架页新增 3 张实验卡（估值分位切换 / 因子审计 / 指数估值中枢） | 9 个新文件 + `update_v8.py` + `index.html` | `bd172180` | ✅ 已在 main |

**Commit 在 main 的验证**：`git merge-base --is-ancestor bd172180 <remote-main>` = **YES**（远端 main 祖先链确认）。

---

## 二、改后三件套核验（标准：①语法 ②结构/运行时 ③引用）

### ① 语法 — 全绿 ✅

| 项 | 结果 | 证据 |
|---|---|---|
| `py_compile`（5 个 .py） | **0 错误** | `cloud_fetch_v8.py` / `update_v8.py` / 3 个新脚本全部通过 |
| `index.html` inline script | **0 错误 / 24 块** | `new Function()` 校验全通过 |

### ② 结构 / 运行时 — 全绿 ✅

| 项 | 结果 | 证据 |
|---|---|---|
| `gen_factor_audit.py` 重跑 | ✅ | `[ok] wrote raw_data/factor_audit.json` |
| `fetch_valuation_percentile.py` 重跑（联网） | ✅ | `[ok] wrote ... (3 indices)` |
| `fetch_index_value_framework.py` 重跑 | ✅ | `[ok] wrote ... (4 indices)` |
| `update_v8.py --category post_close` | ✅ | 缓存戳已更新，3 个 `data/*.js` 同步至 `06:35` |
| raw ↔ data 一致性 | ✅ | 三卡 `update_time` 均为 `2026-09-10 06:35:xx`，与 raw 一致 |

### ③ 引用 — 全绿 ✅

| 引用点 | 结果 |
|---|---|
| 三卡容器 id（`valuationPercentileBody` / `factorAuditBody` / `indexValueFrameworkBody`） | 各 1 处 ✅ |
| 三卡渲染函数（`__renderValuationPercentile` / `__renderFactorAudit` / `__renderIndexValueFramework`） | 各 9 处引用 ✅ |
| `update_v8.py` DATA_SOURCES 映射（L117/119/121） | 各 1 处 ✅ |
| `index.html` script 注入 + `?v` | 3 处，戳已更新为 `5418a44a6b` / `f299ef15d5` / `af773b1619` ✅ |
| `_restamp_fib_for_premarket` | 定义 1 + 调用 1 ✅ |

**关于渲染函数的两处调用（L11496 / L11786）——已查清不是重复 bug**：
- L11496 在 `renderUnlisted()` 内 → **首次渲染**暂未上架页时执行
- L11786 在 `window.__ulSwitch()` 内 → **切换子 tab（实验区/宏观观测）**时重渲染

两个是不同入口，两处都挂才正确。**无需清理，勿误删。**

---

## 三、三张卡的实质内容与已知局限 ⚠️（请你白班定夺）

### 卡1 🌡 估值分位切换 — 数据完整，但**分位窗口矛盾**

| 指数 | PE-TTM | 5年分位 | 3年分位 | 1年分位 | 5年档位 | 信号 |
|---|---|---|---|---|---|---|
| 沪深300 | 12.90 | **76.2%** | 63.5% | **3.9%** | 高估 | 减仓/规避 |
| 中证500 | 27.02 | **87.7%** | 79.7% | 42.3% | 高估 | 减仓/规避 |
| 中证1000 | 30.03 | **79.5%** | 66.2% | 11.5% | 高估 | 减仓/规避 |

**整体结论**：高估区间 → 降低权益仓位，增配红利/价值防御，防御比例 60%。

> ⚠️ **局限 1（需人工判读）**：5年/3年分位显示「高估」，但 **1年分位显示「低估」（沪深300 仅 3.9%）**——说明近 1 年估值回落很快，但相对 5 年仍在历史高位。**三个窗口互相矛盾，卡片同时展示三档就是为了让判读者看到这个矛盾，请勿只取单一窗口下结论。**

### 卡2 📊 因子审计 — 结论明确

7 因子核验：**缺失 5 / 部分使用 2 / 已覆盖 0**。

- **高优先级缺口（质量+价值）**：`盈利收益率 E/P`、`GPOA（毛利/总资产）`、`经营现金流/总资产`
- **中优先级（规模+流动性）**：`总市值`、`60日成交额中位数`
- **铁律**：所有新因子接入 `generate_top10.py` 前，**必须跑 walk-forward 回测按 edge 定权重，禁止直接硬编码高分**（已写进卡片提示）。

### 卡3 📈 指数估值中枢 — ⚠️ **数据不全，最需要你处理**

| 指数 | 状态 |
|---|---|
| 上证指数 | ✅ 有数据，但 `latest_date = 2026-09-07` |
| 沪深300 | ❌ `available: false`（akshare/东财接口当前环境不可用） |
| 中证500 | ❌ `available: false`（同上） |
| 中证1000 | ❌ `available: false`（同上） |

上证指数实测：收盘 3932.7，布林带位置 -0.315（合理档），短期趋势下降 / 长期上升，门控「空头/观望」。

> ⚠️ **局限 2**：中证三指数**全部无数据**，卡片实际只有 1/4 有内容。
> ⚠️ **局限 3**：上证数据停在 **09-07，滞后 2 个交易日**（`INDEX_HISTORY.js` 本身就停在 09-07）。
> → **这两条是数据问题不是代码 bug**，需要你白班（中国 IP / cn runner）补拉 `INDEX_HISTORY` 或换东财 push2his 直连。

---

## 四、maharo 归档结论（主人 09-10 凌晨拍板）

| 功能 | 判定 | 理由 |
|---|---|---|
| **专家档案 / 群成员画像 / 看多共识** | ❌ **不接入** | mahoro 页面 `data.maharo.cn/m/experts/` **实际是空的**（只有头部「166 位活跃成员」，无卡片数据）；API 手册中也**无对应端点**。无数据 → 不做 fetcher。 |
| **估值分位切换** | ✅ **已独立实现，不依赖 mahoro** | 已改走 **akshare `stock_index_pe_lg`** 自建（见卡1），数据可达、可复现。 |
| **Feed API（`/api/v1/feed`）** | ⏸ **暂不升级** | 文档规范，但① 我所有执行环境（本机/云端/cn runner）**DNS 均不可达** `data.maharo.cn`；② 文档明确「只做自用投喂，别转发/再分发/公开」，而 v8 是公开站，**合规风险未拍板**。 |

**已确认事实**（避免你重复排查）：
- `data.maharo.cn` 在**阿狸咪本机 / GitHub 云端 runner / 小九 cn runner** 三处**全部 DNS 解析失败**（`getaddrinfo failed` / `Non-existent domain`）。
- API key 是**有效的**（主人已确认），问题纯粹在网络层，**不是鉴权问题**，换 key/验证码都解决不了。
- 现有 `data/maharo_macro.js` 每日 07:22 刷新正常，**拉取脚本不在仓库、不在本机**，跑在你能访问的那台机器上——**这条线不要动**。

---

## 五、待你（小九）白班处理的事项

| # | 事项 | 优先级 | 说明 |
|---|---|---|---|
| 1 | 补 `INDEX_HISTORY` 数据（现停在 09-07，滞后 2 日） | **高** | 影响卡3 时效性 |
| 2 | 补中证 300/500/1000 指数收盘序列 | **高** | 卡3 现只有上证有数，其余 `available:false` |
| 3 | 三张卡的定时挂载（现靠手动跑） | 中 | 3 个脚本尚未接进任何 workflow / ORDER 批，建议挂 **E 批（盘后）** |
| 4 | maharo 公开分发合规性拍板 | 中 | 决定是否升级 Feed API；未拍板前**不动** |
| 5 | 本地 HEAD 与远端 main 不一致 | 低（提醒） | 本地 `7117ea753`，远端 `c9781815a`（CI 持续在推）。**你拉取前先 `git fetch` 对齐，严禁 force push** |

---

## 六、环境状态

- 时段：阿狸咪夜间窗口（18:00 → 次日 07:30），**06:40 仍在阿狸咪窗口**，07:45 交给你。
- 远端 main 持续被 CI 推送（10 分钟内从 `c5959d427` → `c9781815a`），**判断「是否已在 main」一律用 `git ls-remote`，不要信本地 ref**（坚果云会清本地 ref）。
- 本次未改动任何 workflow、未删除任何既有文件；仅新增 9 个文件 + 2 处既有文件编辑。

---

_阿狸咪 · 2026-09-10 06:40 · 夜间改动三件套核验完成 + maharo 归档，交接清楚。_

**本报告仅供研究参考，不构成个人投资建议。**
