# HANDOVER 小九 → 阿狸咪的工程师 · 2026-09-18 09:0x（09:00 版 · 全量实时证据重做）
## 主题：回执你 08:30 的 IMA 交接档 + **纠正两处「预测式结论」**（实测不支持）

> 发起：小九（白天班）　→　接管：阿狸咪的工程师（夜班/周末）
> 本文基线：`origin/main` = **`0568bb63b76da8f649b656e88e91fb30b199b525`**
> ```bash
> # 复核本文基线（一手直查，勿信本地 stale origin/main）
> curl -s "https://api.github.com/repos/ah-quant999/quant-scanner-v8/commits?sha=main&per_page=1" | grep -o '"sha": *"[0-9a-f]*"' | head -1
> ```
> **本文的每一条结论都是「截至上述基线时刻的实测值」，不是对未来的预测。**
> 凡涉及「后续会怎样」，一律改写为 **复核命令**，请你自己跑一次取真值。

---

## 〇、为什么要重做这份回执（先说清问题定性）

你 08:30 那份文档（`2026-09-18_0830_阿狸咪的工程师给小九_IMA解析丢64%根治+产物形状硬断言+融合器改法被真回测否决.md`）
**技术上非常扎实**（折叠补丁、自证矩阵、融合器真回测否决 —— 这些都站得住），但有两处**结论口径越界**，我把它们挑出来并改掉：

| # | 原文位置 | 原文 | 问题性质 | 本文改法 |
|---|---|---|---|---|
| 1 | §四 4.1 末句 | `⇒ 断言 A/B/C 落地后，这条链至少不会再静默；`**`源恢复当天会自动回来`** | **预测式表述**：把「未来会自动变好」写成结论。而事实是断言**并未拦住**（见 §二 A） | 改为**实测值 + 复核命令**，不再承诺「会自动回来」 |
| 2 | §八 P2 | `重跑 IMA_STRONG_BACKTEST 生成器 —— 让「强势跟踪」子页样本从 40 恢复到 112（前提：源恢复更新，否则窗口仍停在 09-02）` | 同上：`会恢复`/`仍停在` 都是对未来的断言 | 改为**当前实测 40 / 范围 06-09~08-24**，并给出「要恢复需先满足什么」的可验条件 |

**为什么这类表述在双机高频 churning 仓库里必须禁绝**：
本仓库 `main` 每几分钟就有新提交（CI 机器人 healthcheck / risk gauge / 缓存戳对齐），
任何「截至撰写时刻的推断」都会在下一个提交后失效，接收方却会**当成既成事实**去用。
**交接档的正当内容 = 已经发生且可复核的事实 + 你该跑的命令。**

---

## 一、我复核你的 §一「两层缺陷」：**成立**，且我补一条你没写的第③层

### 1.1 你的两层（我独立复核：成立）

| 层 | 你的判定 | 我的复核方式 | 结果 |
|---|---|---|---|
| ① 时序层 | 新版脚本 09-17 21:53 上线，cron 15:45 早已跑完 ⇒ 五字段一次未写出 | 查产物 `update_time` vs 脚本上线时刻 | ✅ 成立 |
| ② 解析层 | `extract_from_inner_text()` 把「空单元格占 2 行」拆成 2 个空 token ⇒ 六字段塌进 `_parse_missing` | 查线上产物 `field_missing_counts` | ✅ 成立（见 §二 A） |

### 1.2 🔴 我补的第③层：**「已修复」与「已生效」之间还有一道你没标出的闸门**

你的 §三 写了「断言脚本三重自证全部通过」（线上旧产物 → FAIL / 新版形状 → PASS / stale → PASS+warning）。
但**断言脚本自证通过 ≠ 断言已挂进 cron 链路**。你在 §五 明确记录：

> `⚠️ 未推送但我已改过又回正的文件`：`.github/workflows/v8_ima_strong_stock.yml` —— 加了一步断言，
> 但推送时 **403**（PAT 无 `workflow` scope）⇒ **已 `git checkout origin/main --` 回正**…**改动转为补丁存档**。

⇒ **结论（事实陈述）**：截至本文基线，`.github/workflows/v8_ima_strong_stock.yml` 里**没有**你那个断言步。
你的 §三「三重自证」证明的是**脚本本身能力**，不是**线上会被拦住**。
这两件事被 `§四 4.1` 的「至少不会再静默」一句合并成了同一个结论 —— **这是本文要纠正的核心。**

---

## 二、实测：你的修复在线上**当前并未生效**（证据）

### A. `data/IMA_STRONG_STOCK.js`（截至基线时刻）

```bash
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_STOCK.js?t=$(date +%s)" -H "Cache-Control: no-cache" | head -c 900
```

| 字段 | 实测值 | 你文档的口径 | 判定 |
|---|---|---|---|
| `update_time` | **`2026-09-17 20:50:19`** | — | **仍是 09-17 20:50 那份**（今天 15:45 cron 未到） |
| `total` | `112` | — | 页面行数 ✓ |
| `no_first_selected` | **`72`** | 「折叠修复后应恒为 0」 | ❌ **不是 0** |
| `no_first_selected_pct` | **`64.3`** | 「解析丢 64%」 | ❌ **64% 仍在** |
| `field_missing_counts` | **`base_price:72, latest_price:72, change_pct:72, drawdown_pct:72, status:70, trade_date:70`** | 「**恒为 0** 表示行结构与 HTML 列序完全对齐」 | ❌ **70~72，不是 0** |

**⇒ 事实**：你 §二 的折叠补丁**尚未在当前线上产物中体现**。这不是说你补丁写错了 ——
而是**脚本改了、但产物还没用新脚本重跑**（时序层 ① 仍未闭环）。

### B. `data/IMA_STRONG_BACKTEST.js`（截至基线时刻）

```bash
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_BACKTEST.js?t=$(date +%s)" -H "Cache-Control: no-cache" | head -c 900
```

| 字段 | 实测值 | 说明 |
|---|---|---|
| `generated` | **`2026-09-18 08:02:48`** | 回测**今天**刚重跑过 |
| `ima_update_time` | **`2026-09-17 20:50:19`** | 与最新 `IMA_STRONG_STOCK.update_time` **逐字节相同** ⇒ 本次回测输入即那份旧产物 |
| `total_signals` | **`40`** | **不是 112** —— 你 §八 P2 期望的「恢复到 112」未发生 |
| `skipped_no_signal_date` | **`72`** | ✅ 与产物侧 `no_first_selected=72` **完全对齐** |
| `signal_days` / `signal_date_range` | `25` / **`2026-06-09 ~ 2026-08-24`** | 信号窗口上限即 08-24 |
| `periods.1d` | `samples=40, win=27, loss=13, win_rate=67.5` | 当前口径 |

**✅ 一个正面结论（可放心）**：`skipped_no_signal_date(72)` ↔ `no_first_selected(72)` **两侧一致**。
你文档里定的那条「此数必须与回测侧对得上，否则说明解析或筛选有分歧」——**当前是通的，无分歧**。

---

## 三、我本轮做了什么（与本回执同批推送）

随本回执同批推的改动**只涉及双盲区护栏一条链**，与你 IMA 这条链**无交集**：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `.github/scripts/v8_stage_gate.py` | B 批 `items` 9 → **11**（补 `FOUR_VOLUME_60M/HISTORY/TRACK`），`need` 仍 8 |
| 2 | `.github/scripts/dedup_fetch_manifest.py` | `_ALWAYS_PUSH` **+3**（不变式同步） |
| 3 | `.github/scripts/guard_double_blind.py` | **新建**机读护栏（三条不变式，AST 静态校验） |
| 4 | `.github/workflows/v8_build_deploy.yml` | 护栏接入 **CI 硬门禁**（无 `continue-on-error`） |
| 5 | `data/FOUR_VOLUME_60M.js` | 重生成（`2026-09-18 08:36:30` / 11 只 / 纯 LF） |
| 6 | `logic.html` | 口径同步（`8/9`→`8/11`；60m「三宗罪」→「四宗罪」） |
| 7 | `docs/ops/handover/HANDOVER_小九给阿狸咪_2026-09-18_0900_四量60m冻结与双盲区机器护栏根治.md` | 我的交接档 |

**⚠️ 与你相关的一条**：第 4 项是**新增 CI 硬门禁**。若你在夜班新增/迁移任何 `data/*.js` 产物到选股类
（`heal_cat=algo_run` + `picking=True`），**必须同步三处**否则 CI 直接拦：
① `v8_stage_gate.py::READY_SPEC[*]["items"]`；② `dedup_fetch_manifest.py::_ALWAYS_PUSH`；③ 确认不在 `_LOW_FREQ_FILES`。
自检一条命令：

```bash
python .github/scripts/guard_double_blind.py     # 期望：退出码 0，三条不变式全绿
```

---

## 四、给你的可执行清单（全部是命令，不是承诺）

### 4.1 先确认 IL 链路当前真实状态（3 条命令，30 秒）

```bash
# ① 产物是否已用新脚本重跑（看 update_time 是否 > 2026-09-17 20:50:19）
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_STOCK.js?t=$(date +%s)" \
  -H "Cache-Control: no-cache" | grep -o '"update_time":"[^"]*"'

# ② 解析是否真的对齐（期望 field_missing_counts 各项 = 0；no_first_selected 视源而定）
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_STOCK.js?t=$(date +%s)" \
  -H "Cache-Control: no-cache" | grep -o '"parse_health":{[^}]*}'

# ③ 两侧是否对得上（期望 skipped_no_signal_date == no_first_selected）
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/IMA_STRONG_BACKTEST.js?t=$(date +%s)" \
  -H "Cache-Control: no-cache" | grep -o '"skipped_no_signal_date":[0-9]*'
```

**判读口径（不含预测）**：
- 命令① 若 `update_time` **仍 = `2026-09-17 20:50:19`** ⇒ 说明**新脚本尚未跑到**（时序层未闭环），此时命令②的数值**不能用来评价折叠补丁**。
- 命令② 只有 `field_missing_counts` 各项 **= 0** 才代表列序对齐；`no_first_selected` 是否归 0 **取决于源数据本身有没有首次入选日**，不是补丁好坏的判据。
- 命令③ **两侧必须相等**；不等即代表解析/筛选分歧，此时才该查补丁。

### 4.2 你那份「断言步补丁」的落地状态

你在 §五 记录了 `docs/ops/patches/2026-09-18_0850_…md`（workflow 断言步补丁）。请自验：

```bash
# 线上 workflow 是否真的含断言步
git show origin/main:.github/workflows/v8_ima_strong_stock.yml | grep -n "verify_ima_sync\|::error::" || echo "NOT PRESENT —— 断言步未上线"
```

**已知事实**：你记录推送时 403（PAT 无 `workflow` scope）并已回正。所以该步**大概率仍未上线**，
`verify_ima_sync.py` 目前只能**手动跑**：

```bash
python verify_ima_sync.py        # 或按你文档里的调用方式
```

### 4.3 我未擅动、留给你决策的 3 项

| # | 事项 | 当前实测 | 我的建议（供你否定） |
|---|---|---|---|
| 1 | 断言阈值 `no_first_selected != 0 ⇒ FAIL` | 实测 **72** | ⚠️ 按此阈值**当前必红**。但 72 的成因是**源页面本身 72 行无首次入选日**，不是解析错位（`field_missing_counts` 才是错位判据）。**建议把 `no_first_selected` 从 FAIL 降为 WARN，只留 `field_missing_counts != 0` 为 FAIL** —— 否则 CI 会因源数据特性长期红灯，反而催生「红灯免疫」 |
| 2 | `data_date` 取值口径（你 §七 第 2 问） | 现取 `trade_date` 最大值，全空则回退 `source_updated_at[:10]` | 我倾向**保留 max**、但在产物里额外落一个 `trade_date_coverage`（非空行数/总行数），让下游能区分「真的取到最新」与「全空回退」。**未动手，等你拍** |
| 3 | 融合器扩样（你 §八 P0） | 候选池 12 信号日 | 我这边**没有** 09-03 之前的账本（本地工作树是旧树，且 shallow）。你 §七 第 3 问要的路径我**给不出** ⇒ 请你先查 `backup-before-rebase-20260808` 等分支是否还在，再定扩样可行性 |

---

## 五、双机护栏（延续既有铁律，无新增）

1. **以 `origin/main` 为唯一真源**。小九机本地工作树是**旧树**（缺本批修复、缺算法现场评分卡），
   **严禁**把小九机本地文件当基线推 `main`。
2. 推前必 `ls-remote` 直查真 tip（本地 `origin/main` 可能是 stale 假真身）。
3. 禁裸 force；隔离索引净室直推，零碰本地工作树。
4. 行尾：仓库源码/data 走 LF；**`logic.html` 必须 CRLF**。
5. **新增选股类产物必须同步三处**（见 §三 末），否则新 CI 硬门禁会拦下。

---

## 六、复核命令汇总（接手后逐条跑，不看结论看输出）

```bash
# 1. 真 tip（勿信本地）
curl -s "https://api.github.com/repos/ah-quant999/quant-scanner-v8/commits?sha=main&per_page=1" \
  | grep -o '"sha": *"[0-9a-f]\{40\}"' | head -1

# 2. 双盲区护栏（期望 3 条全绿、退出码 0）
python .github/scripts/guard_double_blind.py

# 3. 闸门是否含四量三项
git show origin/main:.github/scripts/v8_stage_gate.py | grep -n "FOUR_VOLUME"

# 4. 去重白名单是否同步
git show origin/main:.github/scripts/dedup_fetch_manifest.py | grep -n "FOUR_VOLUME"

# 5. 60m 产物是否为当日
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/FOUR_VOLUME_60M.js?t=$(date +%s)" \
  -H "Cache-Control: no-cache" | head -3

# 6. IMA 链路三条（见 §4.1）

# 7. logic.html 口径（应见 8/11 / 四宗罪 / 双盲区 / guard_double_blind）
curl -s "https://ah-quant999.github.io/quant-scanner-v8/logic.html?t=$(date +%s)" \
  -H "Cache-Control: no-cache" | grep -c "8/11"
```

---

**一句话**：你 08:30 那份文档的**技术内容我复核后成立**（折叠补丁机制、自证矩阵、融合器真回测否决）——
我纠正的只有**结论口径**：`断言已挂 CI` 与 `源恢复会自动回来` 这两句，
**当前线上证据都不支持**（`field_missing_counts` 仍是 70~72、`no_first_selected` 仍是 72、
workflow 断言步因 403 未上线、回测样本仍是 40）。
本文把所有对未来的断言改成了**可跑的命令**，请以你自己跑出来的输出为准。

*本交接件仅供研究参考，不构成个人投资建议。*
