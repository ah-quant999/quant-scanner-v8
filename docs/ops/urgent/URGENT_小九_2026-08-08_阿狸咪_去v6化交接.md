# URGENT：小九本机去 v6 化交接（来自阿狸咪 2026-08-08）

> 发送方：阿狸咪（家里机）  
> 接收方：小九（单位机）  
> 优先级：P1（影响自动化是否还能正常 cd 到 v8 目录）

---

## 背景

今天阿狸咪本机已完成 v8 全站/云端/双机任务的去 v6 化审计：

- v8 仓库 `E:\workspace\quant-scanner-v8` 内所有功能性 `stock-scanner` 硬依赖已清除。
- 本机（阿狸咪）的 12 个 WorkBuddy automation 的 `cwds` 已全部改为 `E:\workspace\quant-scanner-v8`。
- v8 token 已复制到 `E:\workspace\quant-scanner-v8\.workbuddy\v8_gh_token.txt` 与 `C:\Users\HH20210606\.workbuddy\v8_gh_token.txt`。

**但小九（单位机）的 `workbuddy.db` 仍是独立副本，未做同样改动。** 如果不改，小九触发的任务会仍然 cd 到已退役的 `E:\workspace\stock-scanner`，导致脚本找不到或跑在 v6 目录。

---

## 小九需要执行的操作

### 1. 备份自己的 workbuddy.db

```powershell
Copy-Item -Path "C:\Users\HH20210606\.workbuddy\workbuddy.db" `
  -Destination "C:\Users\HH20210606\.workbuddy\workbuddy.db.bak_20260808_1613" -Force
```

### 2. 用 Python 批量更新所有 automation 的 cwds 指向 v8

```python
import sqlite3
db = r'C:\Users\HH20210606\.workbuddy\workbuddy.db'
conn = sqlite3.connect(db)
cur = conn.cursor()
cur.execute("UPDATE automations SET cwds=? WHERE status='ACTIVE'", ('["E:\\workspace\\quant-scanner-v8"]',))
conn.commit()
# 复核
cur.execute("SELECT id, name, cwds FROM automations WHERE status='ACTIVE'")
for r in cur.fetchall():
    print(r[0], r[1], r[2])
conn.close()
```

> 注意：`automation_update` 工具对 list 型 `cwds` 会串化并错误前缀 `E:/WorkBuddy/`，**不要用它**，必须直连 sqlite3。

### 3. 复制 token

确认以下两个位置都有 `v8_gh_token.txt`（40 字节）：

- `E:\workspace\quant-scanner-v8\.workbuddy\v8_gh_token.txt`
- `C:\Users\HH20210606\.workbuddy\v8_gh_token.txt`

如果没有，从本机（阿狸咪）的以下位置复制：

- `E:\workspace\quant-scanner-v8\.workbuddy\v8_gh_token.txt`
- `C:\Users\HH20210606\.workbuddy\v8_gh_token.txt`

### 4. 复核

运行：

```python
import sqlite3
db = r'C:\Users\HH20210606\.workbuddy\workbuddy.db'
conn = sqlite3.connect(db)
cur = conn.cursor()
cur.execute("SELECT COUNT(*) FROM automations WHERE status='ACTIVE' AND cwds LIKE '%stock-scanner%'")
print('仍指向 v6 的 ACTIVE 自动化数:', cur.fetchone()[0])
conn.close()
```

结果应为 **0**。

---

## 已知仍需小九配合的后续（非本次必须）

1. **根目录清理**：阿狸咪已将 `HANDOVER/URGENT/AUDIT/_*.py/zip` 归档到 `docs/ops/`。小九 pull 后本地也会同步，无需额外操作。
2. **自动化上云评估**：阿狸咪正在整理哪些本地 automation 可迁移到 GitHub Actions。结论会再发交接。

---

## 验证人

阿狸咪 2026-08-08 16:13
