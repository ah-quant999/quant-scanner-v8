# 阿狸咪的工程师 → 小九的工程师｜回测区「三重共识卡无数据」一劳永逸修复上线 + 改后三件套

- **时间**：2026-09-20 23:23（北京时间，脚本实取）
- **提交**：`5a5d1ff5766d`（仅 `index.html`；blob `b44a9bf51d3a6bdd` / 1338759 B）
- **线上**：https://ah-quant999.github.io/quant-scanner-v8/ → 板块 `bt`（回测区）
- **触发**：主人 2026-09-20 23:06 截图报「回测里的这卡没数据了，查改一劳永逸修复」+ Console 报错截图

---

## 一、一句话结论

主人报的「三重共识卡显示**暂无历史回测信号**」有**两个独立真因**，都已修复并实测验证：

1. **`_betaWarn` 悬空引用**（删定义漏删引用方）⇒ `__btOverviewHTML()` 每次渲染抛 `ReferenceError`，被外层 `try/catch` 吞掉 ⇒ **「算法回测总表」整块静默消失**；
2. **`defer 数据 + 结果缓存` = 空结果永久固化** ⇒ 首屏渲染时数据未就绪即把空结果写进 `__tripleBacktestCache`，**此后数据到位也永不再刷新** ⇒ 卡**永久**「暂无历史回测信号」。

另发现**同型第三处**（`__gaoshouSet` 缓存 `IMA_STRONG_STOCK` defer 数据）一并修净 —— 该规范同文件早已确立（详见 §二·2 铁证），属"某模块特立独行"。

---

## 二、根因与铁证

### 1. `_betaWarn` 悬空引用（删一半）

| 项 | 内容 |
|---|---|
| 删除侧 | `index.html` 6261 注释块：「2026-09-20 主人令：原『β 依赖』审计警示块（`_betaWarn['因子实验室']`）随因子实验室行一并移除」 |
| **漏删侧** | 6364-6371 **`function _betaCell(card)` 函数体首行仍写 `_betaWarn[card]`**；6448 行的**唯一调用点**仍在 `_btMainRow` 的「策略」格 |
| 抛错点 | `__btOverviewHTML()`（定义在 6014）内部调用链 `_btWindowHtml → forEach → _btMainRow → _betaCell` |
| 被吞 | 6772 行 `try{ … __btOverviewHTML() … }catch(e){}` ⇒ **异常不进 console error，只在 warning 留痕** |

**线上实测（修复前）**：`console.warn` 出现 **7 次** `[btOverview] ReferenceError: _betaWarn is not defined`；
`#btBody.innerHTML` = **32730 B**、`has_algorithm_sum = false`（总表缺失）。
**修复后**：warning **0 次**、`#btBody` = **40474 B**（+7744 B = 恢复的总表）、`has_algorithm_sum = true`。

> 🔎 排查副产品（血训）：主人截图里这个名字肉眼读作 `letWarn`，全仓 `grep` + `git log -S` **均 0 命中**，
> 差点判成"本仓无此标识符"。把截图 **PIL 裁剪放大 5×** 后真名是 **`_betaWarn`**（`_`+`beta` 在缩略图里糊成 `let`），
> 行号也从 6326 修正为 6362。**→ 判「本仓有没有这个东西」必须基于放大后的确切字符串。**

### 2. `defer 数据 + 结果缓存` = 空结果永久固化（受控复现）

`index.html:1774` → `<script src="data/BACKTEST_COMPREHENSIVE.js?v=…" **defer**></script>`；
而 `window.__tripleBacktestAdapted`（5301）首行 `if(window.__tripleBacktestCache) return …`、尾部 `window.__tripleBacktestCache = out;`。

**受控复现（本地 staging，CDP `Network.setBlockedURLs` 屏蔽该数据脚本）**：

| 阶段 | 数据是否就位 | 缓存 | 适配器返回 | 卡表现 |
|---|---|---|---|---|
| ① 屏蔽 `BACKTEST_COMPREHENSIVE.js` | ✗ `BC_defined=false` | 写入 | `total_signals=0`、`by_period={}` | 「暂无历史回测信号」 |
| ② 解除屏蔽 + 注入脚本（`BC.update_time=2026-09-19 17:58:29` 已就位）→ 重渲染 | ✓ | **仍被复用** | **仍是 0** | **永久「暂无」** |
| ③ **修复版** 同场景重跑 | ✓ | **不写缓存** | **`127` / `12` 档** | **恢复表格** ✓ |

**同仓铁证（规范早就确立）**：同文件 5345 行，另一个适配器 `__strongTrackBacktest` 写着：
> 🔴 2026-09-16 实证踩坑：**绝不能缓存**。数据脚本是 defer 加载，而内联脚本在解析期先执行 ⇒ 首屏渲染时该数据可能还是 undefined；若此时把「空结果」写进缓存，后面即便数据到位也永远不出现（jsdom 实测 `per=[]`）。

⇒ 即 **09-16 已确立"defer 数据不得缓存"**，但 `__tripleBacktestAdapted` 与 `__gaoshouSet` **两处仍带缓存**。

### 3. 同型第三处：`__gaoshouSet`

```js
if(window.__gaoshouSetCache) return window.__gaoshouSetCache;   // 5696
… window.__gaoshouSetCache = _m;                               // 5699
```
数据源 `data/IMA_STRONG_STOCK.js`（1782 行）**同为 defer** ⇒ 首屏未就绪会把**空集**固化，
此后 `__inGaoshouReso` / `__gaoshouResoCount` 恒判 `false` ⇒ 高手共振口径永久失真。已一并去缓存。

---

## 三、改动清单（`index.html`，共 6 处，全部唯一命中）

| # | 位置 | 改动 |
|---|---|---|
| 1 | 6261 注释块 | 补记「同一次删除漏删了引用方」及后果 |
| 2 | 6364-6371 | **删** `_betaCell()` 函数（悬空引用本体、死代码） |
| 3 | 6448 | **删** 唯一调用点 `+ _betaCell(r.card)` |
| 4 | 5302 | **删** `if(window.__tripleBacktestCache) return …` + 写入规范说明注释 |
| 5 | 5335 | **删** `window.__tripleBacktestCache = out;` |
| 6 | 5696 / 5699 | **删** `__gaoshouSetCache` 判读与写入 + 说明注释 |

去块注释后校验：`_betaWarn` / `_betaCell` / `__tripleBacktestCache` / `__gaoshouSetCache` **代码区残留 = 0**；
文件纯 LF（`CR = 0`）、1337548 → **1338759 B**（+1211）。

---

## 四、验证证据（原始数据）

| 验证 | 方法 | 结果 |
|---|---|---|
| 语法门禁 | `_tools/check_inline_js.js` | **28 段 / 0 错** |
| 修复前基线（对照） | 本地 staging 渲染 | `_betaWarn` warning **4 次**、`has_algorithm_sum=false`、`btBody=32730 B` |
| 修复后 | 本地 staging 渲染 | warning **0 次**、`has_algorithm_sum=true`、`btBody=40474 B` |
| 受控复现 | CDP 屏蔽/注入 defer 数据脚本 | 修复前：数据到位仍 `0`；**修复后：`127` / 12 档、`count_noSignal=0`** |
| **跨版本等价** | 归一化 `?v`/`BUILD` 后 sha256 | 新旧基线产物**同 sha `908485c3…`**（旧基线两版归一化同 sha `1162907a…`） |
| **线上终验**（真浏览器） | Edge CDP → `switchSec('bt')` | `has_algorithm_sum=true`、`count_noSignal=0`、`triple_total=127`/12 档、**console 错误总数 0** |
| 线上字节 | `curl` | 1338759 B、修复特征命中、旧缓存代码 **0**、`?v=1789917684`（单调递增）、`BUILD=effba7618` |
| 截图 | `E:\WorkBuddy交付\v8回测区修复\回测区_修复后_线上_20260920.png` | 总表 3 行 + 三重共识卡逐档表（T+1 样本 127 / 48.9%） |

**推送（防覆盖）**：基线 = `f0f2dcaf2e69:index.html` blob `cec1b65bc4`（`git hash-object` 硬校验相等）；
推后回读 blob `b44a9bf51d3a6bdd` = 本地文件 sha256 前 16 位 ⇒ **零覆盖**。
**判据 176 判定**：推送时远端已前进到 `35d04592f700`，但 `5a5d1ff5766d~1:index.html` 的 blob **仍 = `cec1b65bc4`**
⇒ 窗口内 `index.html` 未被改过 ⇒ **零推退**（仅文档/CRLF 提交插入）。

---

## 五、改后三件套（原始输出）

### ① 时窗矩阵对表（`cron_matrix.py`，真源 = staging 最新树）

```
workflow 文件数 = 30 ；含 cron 的 workflow = 21
cloud_weekly_cleanup  UTC 0 13 * * 6   → CST 周六 21:00 ✓
v8_algo_cloud         8 档：16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 / 06:00 / 08:00 ✓
v8_backup             每天 23:00 ✓ ；v8_cleanup 周日 23:00 ✓
v8_cache_buster_reconcile  每 15 分钟 ✓ ；v8_cn_fetch_cloud 08:25 盘前 + */20 盘中 ✓
```
**本轮无任何 cron / workflow 变更 ⇒ 矩阵无需调整**；家机自动化未新增、未改动（覆盖守卫 `f01daa2c` 每 30 分仍活跃）。

### ② 离线 CI 门禁复现（staging = 远端最新树 `35d04592f7` + 换入本次 `index.html`）

```
✅ [1/8]  py_compile: 194 个 .py 文件 0 错误
✅ [2/8]  new Function: 28 个 inline script 块 0 错误
✅ [3/8]  data 完整性: data/*.js 数量=113, 全部 > 100B
✅ [4/8]  align_logic_ops: EXIT 0
✅ [5/8]  workflow YAML: 30 个 workflow 全部有效
✅ [6/8]  HTML 数据引用: 179 个本地 script 引用全部存在（4 页）
✅ [7/8]  gate 头注一致
✅ [8/8]  心跳产物名一致（0 处漂移）
✅ [9/9]  回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
✅ [10/10] index 核心标记守卫: 34/34 全部在位
🎉 10 项全部通过 → deploy 可继续

align_logic_ops.py          EXIT 0（✅ 关键 workflow 均已文档化，无过期引用，名单无死登记）
v8_verify_layer_parity.py   EXIT 0（✅ data/*.js 与 raw_data/*.json 时间戳一致，无陈旧静默上线风险）
```

**云端 CI**：本次提交 `5a5d1ff576` 触发 `☁️ v8 构建部署(云端ubuntu)` = **success**
（`intraday_patrol` = skipped，非盘中时段，正常）。

### ③ 交接件

本文件（`docs/ops/handover/2026-09-20_2323_…`）。

---

## 六、请你（小九）注意 / 建议

1. **`logic.html` / `calendar.html` / `v6_memo.html` 已核查无同型 bug**（`_betaWarn`/`_betaCell`/两处 Cache 全 0 命中）；
2. **"defer 数据不得缓存"建议升级为全仓硬规范**：凡 `window.__*Cache` 且数据源为 `<script … defer>` 的，
   一律去缓存（本轮已修 3 处：`__tripleBacktestAdapted`、`__gaoshouSet`，加上 09-16 的 `__strongTrackBacktest`）。
   若要加门禁，建议 `pre_deploy_audit` 增一条静态检查：defer 数据文件名出现在某 `Cache` 赋值同函数体内 ⇒ 告警；
3. **删除类改动的验收判据**：被 `try/catch` 包住的渲染代码，"删干净了"只能靠**实跑 0 warning** 判定，
   页面"看着正常"不可作准（本轮就少了一整块表格而页面不报红）；
4. 本轮四条新血训已固化进 `github-api-push-fallback` 技能 **判据 177–180**（截图放大、禁用浏览器缓存、
   defer+缓存固化、删定义须同轮核引用方）。

---

本报告仅为工程运维交接，不构成任何投资建议。
