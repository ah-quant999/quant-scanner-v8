# 交接 · 平均股价卡：超买超卖移至行末并显著化 + 删除「vs 20/60 日线」段

- **提交**：`25a8b38830aa46ece5f4912968dabec2ff565e92`（单提交双文件：`index.html` + `logic.html`）
- **署名**：阿狸咪的工程师
- **时间**：2026-09-16 05:1x（CST）
- **主令**：主人 09-16 截图标注 —— 「平均股价的超买超卖要加在最后，显著标识。这个[vs 20日线/vs 60日线]删除。」

---

## 一、改了什么

### 1. 删除「vs 20日线 / vs 60日线」文本段（`index.html`）

该段在均线未就绪时恒显示 `-- (历史12/20日)` 占位（今日 `history_days=12`、`position_vs_ma20/60` 为 `null`），无信息量且占位。

- **连带清死代码**：百分比着色助手 `_pt()` 随该段删除后已**无任何引用点**（全文 count=0），按主人「死代码必须删净」铁律一并移除，未留空壳。
- **保留 `_hd`**（`history_days`）：改由行末超买超卖胶囊的未就绪态承载「数据积累进度」，避免该段删除后进度信息彻底消失。
- **附带收益**：数据层在 MA 未就绪时会写 `ap.ma20 === ap.ma60`（**同一个占位值**，本轮实测均为 `28.1122`）。原逻辑靠 `_hd>=20` 门拦住不露出；删除该段后此误导值**再无任何露出路径**。

> ⚠️ **提醒（数据层隐患，未改）**：`AVG_PRICE_DATA.js` 的 `ma20`/`ma60` 在 `ma20_ready=false` 时仍写入同一个数字（当前 28.1122），且 `ma20 === ma60`。前端已不展示，但**若后续有消费方直接读该字段会拿到假值**。建议数据层在未就绪时写 `null`（或按 `ma20_ready` 消费）。

### 2. 超买超卖（位置观测）胶囊：移至行末 + 显著 + 空态兜底

| 维度 | 旧 | 新 |
|---|---|---|
| 位置 | 第一行中间（买/加胶囊之后，且后面还跟免责小字） | **该行末位**收尾（免责小字前移出位） |
| 边框 | 1.5px `rgba(251,191,36,.45)` | **2px** `rgba(251,191,36,.7)` |
| 字号/字重 | 11.5px / 700 | **13px / 800** |
| 外发光 | 无 | **`0 0 10px rgba(251,191,36,.38)`** |
| 底纹 | 纯色 `.12` | **金色渐变** `.24 → .10` |
| 内边距/圆角 | 3px 8px / 5px | 4px 12px / 6px |
| 名号 | 无 | 前置 **「📊 超买超卖」** |
| 空态 | **整块不渲染**（`if(_verdict){}`） | **显式未就绪态**「⏳ 均线积累中 X/20日」 |

**③ 空态兜底是本轮关键**：原实现在均线未就绪时 `if(_verdict){...}` **整块不渲染** ⇒ 页面根本看不到「超买超卖」这个指标的存在（这正是主人截图里看不到它的原因）。现显式给出未就绪态，**只披露积累进度、不给出任何档位、不构成买卖信号**；绝不用 0 / 假档位冒充（2026-09-11 铁律）。

**口径未变**：仅报位置观测，开仓仍以 AI速览裁决为准（免责小字保留、仅前移）。

### 3. `logic.html` 同步（三件套②）

- 原描述「不足期间显示『历史 X/20 日』**且不展示任何买卖信号**」已不成立 → 改写为新口径。
- 顺带修正过时的 `index.html` 行号引用（`~1512-1563` → **L2225-2341**，实测）。

---

## 二、验证证据

**真浏览器（系统 Edge headless）+ 拦截 `index.html` 返回本地补丁，`data/*` 走线上真实**：

| 态 | 胶囊文本 | 行末 | vs 行 | 真实占位 | 卡片 |
|---|---|---|---|---|---|
| 线上真实（未就绪） | `📊 超买超卖 ⏳ 均线积累中 12/20日` | ✅ | 已清零 | 248×35 px | block, h=609 |
| 就绪·超买（注入） | `📊 超买超卖 🔴 高位·观测` | ✅ | 已清零 | 188×35 px | block, h=644 |
| 就绪·超卖（注入） | `📊 超买超卖 🟢 偏低·观测` | ✅ | 已清零 | 188×35 px | block, h=638 |

- 显著样式**实测计算值**：`border 2px rgba(251,191,36,.7)` / `font 13px/800` / `box-shadow rgba(251,191,36,.38) 0 0 10px` / `radius 6px`（非只写断言）。
- 行高 1202px、胶囊确为该 flex 行最后一个子元素（`row_last_is_cap=true`）。
- **线上 CDN 终验**（直接访问，不截 HTML）：两态全过，**`bad4xx=[]`、`pageerrs=[]`**。
- 语法：`index.html` 28 块 / `logic.html` 6 块内联脚本 `new vm.Script` **全 0 错**；行尾守恒（LF / CRLF 各自保持）。

---

## 三、🔴 关于覆盖事故（重要，请小九看）

**归因**：`5e1f7cea2`（**我** 09-15 20:41「任务运行看板 + 全站 UTC+8」）是覆盖链的**起点** —— 我推它时基于的 blob 已过时，覆盖了他人 UTC+8 调用点；随后 `a8331c521`（04:56）恢复 22 处，又覆盖了 ETF 卡名 8 处，`702a4b6fc9`（05:01）再恢复。

**责任在我**，让小九多干了一轮恢复。原因：那次推送**发生在「blob 漂移守卫」诞生之前**。

**本轮起已按守卫执行**（守卫脚本 → `v8-anti-nutstore-atomic-push/scripts/push_guard.py`）：推前重读 tip、逐个比对 `contents.sha`，不符即重拉重打（本轮实际触发过一次：`358c749e` → `5b57bb68`）。

**本轮零覆盖证明**（与推送前 tip `89470137b5` 逐行 diff）：

```
差异块数 = 4，全部落在 L2242-2311（平均股价卡目标区域）
  [replace] L2242        _pt 定义 → 注释说明
  [replace] L2291-2294   vs 段注释重写
  [delete]  L2293-2294   vs 20/60 日线两行
  [replace] L2306-2311   超买超卖段重写
⇒ 全部差异都在目标区? True
```

同时确认小九成果全部存活：`Asia/Shanghai`×29、`_parseAsCST`×28、`UTC+8`×4、`新周期判定`×12、`白河愁`×7、「ETF 盘中异动 · 主力净额分时」×2；上轮护栏 `obsGuard`×2、`__obsFbShow`×7、`_ovP`×3。

**给小九的建议**：任何改 `index.html` 的推送，**推前必须拉最新 blob 重打补丁**（尤其 04:50-06:00 这段你做恢复作业的高频窗口）。若你那边用的是本地 clone + `git push`，也请先 `git pull --rebase` 并逐文件确认无回退。

---

*署名：阿狸咪的工程师 · 本报告仅供研究参考，不构成个人投资建议。*
