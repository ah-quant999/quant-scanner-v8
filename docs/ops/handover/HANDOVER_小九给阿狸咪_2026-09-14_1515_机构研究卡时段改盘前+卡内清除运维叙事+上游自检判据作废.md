# HANDOVER 小九 → 阿狸咪 · 2026-09-14 15:15

## 一句话
主人指出三件事，全部落地：①「机构研究·AI 解析建议」卡时段标签「盘中」→「盘前」；
② 卡正文里的 mahoro/上游运维叙事**全部删除**；③ 核实结论 —— **mahoro 链根本没断，是我误判**。

## 一、主人原话
1. （截图 宏观观测页该卡 `更新于 今日 09:28 盘中 短线`）「**这个是盘前**。」
2. （截图【今日观察点】1)2) 两条）「**mahoro断了不是写在这！这两句删除。**」
3. （截图 运维页 mahoro cookie 行 `正常`）「**运维这里不是正常吗？你连错地方了吧**」

主人第 3 点是**对的** —— 见下节。

## 二、核实结论：上游确实没断，原「断更 3 天」判据作废
只读实测（2026-09-14 15:04）：

| 证据 | 实测值 | 判定 |
|---|---|---|
| `机构研究日报.md` mtime | **Sep 14 10:50** | 今日有写入 |
| 该文件 `## 2026-09-14 机构研究日报` 块 | **存在，385 行**，命中关注词 56 条 / 机构核心 42 条 | **上游正常** |
| 缺失的 09-12 / 09-13 | **周六 / 周日** | 非交易日本就无块，**不算断更** |
| `data/maharo_macro.js` 7 源 latest | 全部 `2026-09-14`（09:00~10:03），`stale=false` | 7 源全活 |
| 运维页 mahoro cookie 行 | `正常 · 剩 25 天` | 见下「误判根因 3」 |

**误判根因（三条，全部是我方错）**
1. **把周末当断更**：判据写的是「最新块日期 == 今日 才算正常」，09-12/09-13 是周末，必然判红。
2. **在当日上游产出之前下结论**：上游 09-14 的块 10:0x 才写入，而 07:15 出稿任务（实际 09:28 才跑完）
   在 09:28 就断言「断更 3 天」。**07:15 看不到当日块是正常状态**。
3. **运维页那行「正常」≠ 数据链在跑**：`index.html` 的 `__v8CredTable` 里 mahoro cookie 行是**纯日期倒计时**
   （`expires:'2026-10-09'`，`expire_note:'推定·据 09-10 记录 days_left=29'`），`left>7` 就显示「正常」。
   **它只算 cookie 过没过期，完全不校验数据有没有产出** —— 我此前把两者混为一谈，是「连错地方」。

## 三、改动清单（已上线）
| # | 文件 | 改动 | 线上校验 |
|---|---|---|---|
| 1 | `index.html` | `setBadge('maharoFresh','auto',…)` → `'盘前'` | 含新写法 ×1、旧写法 ×0 |
| 2 | `data/maharo_insights.js` | 删【今日观察点】1)2)；删开头「⚠️ 上游断更提示」整段；删【口径】双链句；`sources_read` 去断更子句；`_meta.upstream_status` `stale_3d`→`ok` | 全文 `断更`/`上游`/`mahoro` 命中数 = **0**；11737 → 10766 B |
| 3 | 自动化任务 `30e1b099…`（07:15 出稿） | 更名「**按交易日自检**」；新增**铁律 1**（卡正文一个字不许写运维）+ **铁律 2**（自检判据改为「最新块 ≥ 上一交易日」，周末/节假日跳过，连续 2 个交易日无块才算异常） | prompt 已更新，次日 07:15 生效 |

**为什么时段标签要写死 `盘前`**：该卡是**日更 08:30 / 07:15 出稿的盘前产品**，本身不属任何盘中批次。
原写法 `'auto'` 交给 `_slotByTs` 按时间戳归类，而 `_slotByTs` 把 **09:00–15:30 全算「盘中」**
（`if(_hr < 15.5) return {slot:'盘中'}`），今日出稿 09:28 落进该区间 ⇒ 卡上误挂「盘中」。

## 四、留给阿狸咪的三件事
1. **别复用「最新块 == 今日」这类判据** —— 凡日频上游，判断更一律要**先过交易日历**，且要留出
   「上游当日写入时点晚于自检时点」的余量。周末/节假日无数据是**正常**，不是故障。
2. **卡片正文与运维口径必须分家** —— 主人要的是「点开就看到机构观点」，不是「上游健康报告」。
   上游问题走交接文档 + 必要时一封邮件；卡片正文里一个字都不许出现。
3. 待主人拍板（本次**未动**，别自行改）：
   - `_slotByTs` 的 `09:00–15:30 算盘中` 是否应改为 **09:30 开盘起才算盘中**？现全站仍有 **15 处**
     `'auto'` 时段调用点会受影响（`phCrisisTime` / `tcTime` / 机游共振日历 / 北向日历 …）。
   - `_uBadge` 里 `_slotHtml` **算出来却没被使用**（返回语句用的是 `sHTML`）—— 该行原意是
     「过期数据不挂实时/盘中标签」，实际是死代码，盘中标签一直照常显示。修它会影响全站 60+ 调用点。
   - 运维页 mahoro cookie 行的 `use:` 文案是否补一句「**仅倒计时，不校验数据产出**」以免再次误导。

## 五、本次事故苗头（已自愈，留档）
首推基于 `5f10cbd81` 的 `index.html`，而推送瞬间父提交已是 `4e18e4e4b`
（`v8_cache_buster_reconcile` 已把 102 行 `?v=` 缓存戳 1789368597→1789369353）⇒
**我把缓存戳推回旧值**（内容无差，但客户端会回落旧缓存）。
`v8_cache_buster_reconcile` 15 分钟内已把戳推到 1789369780，**回退自愈**，故未再补推。
教训：**推热文件前必须重新 GET 当前 ref，并以该 ref 的 blob 为基线打补丁**，不能沿用几分钟前的基线。

---
小九 · 2026-09-14 15:15
