# HANDOVER · 阿狸咪的工程师 → 小九的工程师

**时间**：2026-09-18 08:30（北京时间）
**主题**：IMA 强势跟踪「解析丢 64%」根治（空单元格折叠）+ 产物形状硬断言护栏 + 融合器改法被真回测**否决**（附证据）
**推送方式**：Git Data API `base_tree` + `force:false`，**仅替换 3 个登记路径**（工作区存在 20+ 个非我方未提交改动，已隔离未动）
**本轮不碰**：`index.html` / `logic.html` / `algorithms/*` / `cloud_fetch_v8.py` / `api_push_raw.py` / `.gitattributes`（全部零改动，`git hash-object` 对照 `origin/main` 逐字节相同）

---

## 一、闭环了你 0045 回执的两项定位

你 0045 回执判「IMA 五字段未产出真因 = **新版脚本未跑**（cron 15:45 早已用旧脚本跑完）」。

**该判定我独立复核，成立**。但复核中发现**它不是故事的全部** —— 还有一层你没覆盖的**同类事故**：

| 层 | 缺陷 | 线上实证 | 我本轮动作 |
|---|---|---|---|
| ① 时序层（你已定位） | 新版脚本 09-17 21:53 上线，cron 15:45 早已跑完 ⇒ 五字段**一次未写出** | 线上产物顶层仅 6 键 | **加硬断言**（见 §三）：形状不对即 CI 红，不再「等下一轮自然验证」 |
| ② **解析层（新发现）** | `extract_from_inner_text()` 把 innerText 里「**空单元格占 2 行**」的格子拆成 2 个空 token ⇒ 指针卡住不消费 ⇒ 六字段塌进 `_parse_missing` | `parse_health.field_missing_counts` = `{base_price:72, latest_price:72, change_pct:72, drawdown_pct:72, status:70, trade_date:70}`；**有效 `first_selected` 仅 40/112** | **已根治**（见 §二），自证 112/112 |

**为什么 ② 更要紧**：即使 ① 在 09-18 15:45 自然自愈，**五字段照样会算错** —— 因为 `data_date` 取的是「每行最新交易日」的最大值，而 72 只的 `trade_date` 全空（塌进 `_parse_missing`）⇒ `data_date` 只由剩余 40 只决定，**样本再砍 64%**。① 和 ② 是**串联的**，只修 ① 会让护栏建立在残缺样本上。

---

## 二、修复 ②：空单元格折叠（核心）

**根因（实证，非推测）**：`innerText()` 渲染表格时，**空单元格占 2 行**（`'\ufeff'` + 紧随的空串），非空单元格只占 1 行：

```
301008
宏昌科技
\ufeff            ← 行业格为空：第 1 行是 BOM
                  ← 第 2 行是空串（该空单元格共占 2 行）
2026-07-22        ← 首次入选（数据其实在！）
30.44 40.71 33.7 8.9 1 正常 回调买点(回撤8.9%) 2026-09-02
```

旧实现 `lines = [l.replace("\ufeff","")... for l in text.split("\n")]` 把 BOM **无条件抹掉** ⇒ 空单元格与「2 个真空行」不可区分。而解析按「1 字段 1 token」推进 ⇒ 每遇一个空行业格后续整体左移；09-17 你我都加过的类型校验又让指针**卡住不消费** ⇒ 缺口全部堆在同一个 token 上。

**修法（`fetch_ima_strong_stock.py` L157 段）**：**折叠**而非 strip —— 把 `'\ufeff'` + 紧随空串并成 **1 个空 token**：

```python
raw_lines = text.split("\n")
lines, _k = [], 0
while _k < len(raw_lines):
    if (raw_lines[_k] == "\ufeff"
            and _k + 1 < len(raw_lines) and not raw_lines[_k + 1].strip()):
        lines.append("")          # 空单元格 = BOM 行 + 空行 → 1 个 token
        _k += 2
        continue
    lines.append(raw_lines[_k].replace("\ufeff", "").replace("\u200b", "")
                 .replace("\u200e", "").strip())
    _k += 1
```

### 自证（铁规：修复前必须**恰好复现**线上缺陷，修复后必须归零）

真实页面 dump（1521 行 / 112 只）：

| 指标 | 修复前 | **线上产物** | 修复后 |
|---|---|---|---|
| `base_price` 缺失 | 72 | **72** | **2** |
| `latest_price` 缺失 | 72 | **72** | **2** |
| `change_pct` 缺失 | 72 | **72** | **2** |
| `drawdown_pct` 缺失 | 72 | **72** | **2** |
| `status` 缺失 | 70 | **70** | **0** |
| `trade_date` 缺失 | 70 | **70** | **0** |
| **有效 `first_selected`** | **40 / 112** | **40 / 112** | **112 / 112** |

「修复前」六项与线上 `parse_health` **逐项完全一致** ⇒ 根因确认（不是猜测，是可复现的指纹吻合）。
仅剩 2 条 `base_price` 缺失 —— 那 2 只**真的没有价格数据**（非解析问题）。

**必须说明的边界**：本 dump 抓的是**源方 09-02 那篇冻结笔记**（见 §四），所以修复后内容仍是 09-02 的池子。**修的是「不再丢 64%」，不是「源恢复更新」** —— 后者是外部问题。

---

## 三、新增 ③：产物形状硬断言（杜绝「旧脚本假成功」）

**为什么必须加**：上述 ① 和 ② **整条链没有任何一步报错**（job 全绿）⇒ 事故潜伏了不知多久，且两端互相不知道。这违反「不许假成功」的底线。

**新增文件** `verify_ima_sync.py`（根目录，与既有 `verify_*.py` 命名一致），**四类断言**：

| 断言 | 触发条件 | 后果 |
|---|---|---|
| A | 五字段（`data_date`/`source_stale`/`stale_days`/`source_updated_at`/`source_title`）任一缺失 | **FAIL**，指名「跑的是旧版脚本」 |
| B | `parse_health.no_first_selected != 0` | **FAIL**，指名「折叠修复失效或被回退」 |
| C | `parse_health.field_missing_counts` 非空 | **FAIL**，指名「解析串位复发」 |
| D | `source_stale == True` | **仅 warning**（源侧停更是外部原因，不该红，但必须吼） |

**接入方式（两处，一主一备）**：

1. **主：生产者自检（已生效，零权限依赖）** —— `fetch_ima_strong_stock.py` 的 `main()`
   在写出产物**之前**内联同一套断言，不通过即 `raise SystemExit(1)`
   ⇒ CI 里「抓取」那一步直接失败、job 变红。**谁产出谁负责**，连人工本机跑也受保护。
2. **备：CI 独立步（补丁已存档，待有权限方应用）** —— 我方 PAT **无 `workflow` scope**，
   改 `.github/workflows/**` 经任何 API 都返回 `403 Resource not accessible by personal
   token`（实测）。故只存不改，补丁见
   `docs/ops/patches/2026-09-18_0850_阿狸咪的工程师_ima断言步workflow补丁_PAT无workflow权限需有权限方应用.md`，
   内含可直接 `git apply` 的 diff。**cron 不变**（仍 `45 7 * * 1-5` = 北京 15:45）⇒ 时窗矩阵无变化。
   此步价值是**显式化**（Actions 日志里独立成步 + `::error::` 注解），**不做也不影响防护生效**。

**断言脚本三重自证**（全部通过）：

| 测试 | 输入 | 预期 | 实测 |
|---|---|---|---|
| 1 | 线上旧产物（6 键） | FAIL | ✅ 退出码 1，**3 项断言逐项指名**（缺五字段 / `no_first_selected=72` / `field_missing_counts` 非空） |
| 2 | 新版形状（5 字段 + 健康度归零） | PASS | ✅ 退出码 0 |
| 3 | `source_stale=True, stale_days=15` | PASS + warning | ✅ 退出码 0 + `::warning::` |

---

## 四、必须让你知道的两件事（不需你动作，但影响判断）

### 4.1 源侧**仍在停更**（外部问题）

`note/share/_A0YNbqJ8AmbI5kRZ1ZmMQ` 页内自述：标题「强势股跟踪日报 **2026-09-02**」、
「更新时间：**2026.09.02 15:41**」、每行「最新交易日」全为 2026-09-02。而价格字段指纹连续多次抓取一字未改。

⇒ **自 09-03 起该分享链固定指向同一篇笔记；我方每天忠实抓取一份静止内容，`update_time` 却天天是「今天」**（`update_time` 语义是**抓取时刻**，不是数据日）。
**这正是五字段设计的动因**，而消费端 `final_recommend.py` L786-793 的熔断判据**早已就绪**（`data_date != today` 或 `source_stale` ⇒ 跳过高手共振加分）——但产物不带字段 ⇒ **护栏一直空转，09-02 的死名单仍被当「今日共识」加 +1.0 分**。
⇒ 断言 A/B/C 落地后，这条链**至少不会再静默**；源恢复当天会自动回来。

### 4.2 🔴 融合器（最终推荐）改法被我自己的真回测**否决**

主人质疑「最终推荐才 2⭐、4⭐ 模块选不出最好，逻辑本身有问题」。我做了 A/B 真回测（12 个**已部署**真账本的 `all_candidates` 全量，683 条 / 12 信号日，baostock 真日K，D+1 开买→D+N 收卖，扣双边 0.30%）：

| 口径 | T+1 | T+3 | T+5 |
|---|---|---|---|
| 现行 top5 | +0.09% / 胜 61.8% | −0.28% / 胜 50.0% | −3.94% / 胜 20.0% |
| **我提的改后 top5** | **−0.20% / 胜 54.5%** | **−0.52% / 胜 44.0%** | −3.73% / 胜 26.7% |

**改法（「条件类源不计共振」+ 共振乘数 1.5→1.0）在 T+1/T+3 反而更差，T+5 只改善 0.21pp（噪声内）⇒ 不成立，我不推。**

同时测出**结构性事实**（这部分是可靠的）：
- 各源命中率：`ROE_TTM 43.9%` / `异常换手率 39.8%`（**≈无选择性 = 噪声**）vs `四量终极 17.1%` / `高手跟踪 3.8%` / `三重共识 1.8%`
- 现行公式 `final_score = strength + resonance × 1.5`，**源数量的斜率 1.5 > 单源强度的斜率 1.0** ⇒ 多命中一个**任意弱源**比命中一个**强源**更值钱。这是结构性缺陷（无需 9 天数据即可判定）。

**⇒ 我的裁定（供你复核）**：
1. **不按 12 天改选股权重** —— 那是过拟合，且已被回测证伪；
2. 该做的是**扩样**：候选池只有 12 个信号日（受 `raw_data/history/final_recommend_*.json` 深度限制）。**建议扩到 40~60 天再定权重**；本机是浅克隆（`.git/shallow`），8 月账本只挂在 `backup-before-rebase-20260808` 等分支上，需要时我拉；
3. **建融合器上线闸门**（rank IC > 0.05 且 top5 相对候选池均值为正）—— 本轮**未做**（时间窗不够，且需改 `index.html`，避免与你当前改动面冲突），列为下一轮首选。

---

## 五、本轮改动清单（4 个文件，逐字节可验）

| 文件 | 类型 | 本地 blob | 远端基线 blob | 说明 |
|---|---|---|---|---|
| `fetch_ima_strong_stock.py` | 改 | `0299e7f873` | `c5b526e839` | §二 折叠补丁 + §三 生产者自检（基于**远端最新版**重打，未吞你的 `buy_point` 分支） |
| `verify_ima_sync.py` | 新增 | `98ac1b5d9d` | *(不存在)* | §三 独立事后审计脚本（可对已落盘产物跑，不必重抓） |
| `docs/ops/patches/2026-09-18_0850_…md` | 新增 | `488fd5990b` | *(不存在)* | workflow 断言步补丁（待有 `workflow` scope 方应用） |
| `docs/ops/handover/2026-09-18_0830_…md` | 新增 | `593f84d6d8` | *(不存在)* | 本文件 |

**⚠️ 未推送但我已改过又回正的文件**：`.github/workflows/v8_ima_strong_stock.yml` —— 加了一步断言，但推送时 **403**（PAT 无 `workflow` scope）⇒ **已 `git checkout origin/main --` 回正**，`git hash-object` 与远端 `35a33b9e09` 逐字节一致，改动转为补丁存档。**故你那边不会看到该文件被改**。

**⚠️ 基线纠错记录（很重要，供你防同类坑）**：我最初是在**本机旧版** `fetch_ima_strong_stock.py`（15822B，缺你的 `buy_point` 分支）上打的补丁，做守卫时发现本地 blob `adcd838cdd` ≠ 远端 `c5b526e839`，**已回正为远端版重打**。若你也遇到「本地改了但行为不符预期」，先跑一次 blob 对照。

---

## 六、三件套执行结果

| # | 项目 | 结果 |
|---|---|---|
| 1 | **时窗矩阵对齐** | ✅ 本轮仅给 `v8_ima_strong_stock.yml` **加一步、未改 cron**（仍北京 15:45 / `runs-on: ubuntu-latest`）⇒ 与双机任务时窗**无交集、无冲突**；`cloud_fetch_v8.py` 等派发侧零改动 |
| 2 | **`index.html#sec-op` ↔ `logic.html` 对齐** | ✅ `python align_logic_ops.py` **EXIT 0**（文档源实测：`index.html#sec-op` 584449 字符 / `logic.html#sec-lg` 514251 字符；21 个 workflow 有 cron，MUST=21 / ALLOW=3，无漂移、无死登记）。本轮**未改**这两页 ⇒ 口径无新增分歧 |
| 3 | **交接另一台机** | ✅ 本文件 |

**4 方对齐（前端变量 / 构建映射 / raw 源 / 新鲜度）**：✅ 已跑通。本轮**未新增 `data/*.js`、未改 `DATA_SOURCES`、未改 `raw_data` 结构** ⇒ 四层一致性不受影响。顺带登记既有基线（非我方引入，不擅动）：孤儿 18 / 死数据 16 / 无构建映射 36。

---

## 七、请你复核的点（3 条）

1. **断言阈值**：`no_first_selected != 0` 判 FAIL 是否过严？我的依据是「折叠修复后应恒为 0（仅剩真实无价的记录，且那些是 `base_price` 缺失、不影响 `first_selected`）」。若你认为该留 1~2 条容差，请直说。
2. **`data_date` 取值口径**：现取「每行 `trade_date` 的最大值」，`trade_date` 全空时回退到 `source_updated_at[:10]`。源方若某天漏填一列，`data_date` 会整体回退 —— 是否该改成「众数」或「剔除离群后再取最大」？**我未擅动**。
3. **融合器扩样**：你是否已有 09-03 之前（7 月/8 月）的 `all_candidates` 落盘？若有，请指个路径，我可直接扩样重算 —— 那比在 12 天上调权重可靠得多。

---

## 八、下一步（我方）

- P0：融合器**扩样到 40~60 天**（拉 `backup-*` 分支账本）+ 建**上线闸门**（rank IC / top5 相对池均值），**权重不动**
- P1：源侧「分享链固定指向旧笔记」的**换源方案**（需真实浏览器 + 动态发现最新笔记 id；云端 runner 无 ima-mcp，需另设计）
- P2：修复落地后，重跑 `IMA_STRONG_BACKTEST` 生成器 —— 让「强势跟踪」子页样本从 40 恢复到 112（**前提：源恢复更新**，否则窗口仍停在 09-02）

*本交接件仅供研究参考，不构成个人投资建议。*
