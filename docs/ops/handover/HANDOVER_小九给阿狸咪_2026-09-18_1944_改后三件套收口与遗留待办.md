# HANDOVER · 小九的工程师 → 阿狸咪的工程师

**主题**：2026-09-18 全天改动「改后三件套」收口 + 遗留待办交接（她晚上与明日做）
**发件时刻**：2026-09-18 19:44 CST（小九班次尾，阿狸咪班次中）
**最新 tip**：`f46f25250c3b`

---

## 零、一句话总纲

今天小九侧三批改动（今日事件页/AI速览/分红、龙头股卡、最终推荐融合器升级）**代码与数据已全部上线且经远端真身核验**；「改后三件套」①② 已核验、② 的 logic.html 散文补段因正值阿狸咪班次、为避热文件并发覆盖，**精确文本附于第五节交她今晚粘贴**；③ 本交接档即收口。遗留待办（改动13 + 2b/3/4/6/7 + 扩样复核）交阿狸咪晚间与明日执行。

---

## 一、行尾铁律表（本批涉及文件）

| 文件 | 行尾 | 验收 |
|---|---|---|
| `index.html` | LF | CRLF==0 |
| `logic.html` | **CRLF** | 裸 LF==0（补段须用 CRLF 粘贴） |
| `algorithms/*.py`、`scripts/*.py` | LF | CRLF==0 |
| `data/*.js` | LF | CRLF==0 |
| `docs/ops/handover/*.md` | LF | CRLF==0 |

⚠️ 阿狸咪粘贴第五节补段进 logic.html 时，**必须用 CRLF**（否则 ws-guard 永久不一致，前车之鉴 `6b41f13dcdc9`）。

---

## 二、全天改动总表（按 commit，含①核验）

### 批次 A · 今日事件页 / AI速览 / 分红（前轮已闭环，本轮复核）
| commit | 时刻 | 内容 | ①核验 |
|---|---|---|---|
| `de8c0726fc26`(+`247f8900`/`dca62153`/`1cb7f7d4`/`d42e7840`) | 15:03–15:29 | 今日事件页陈旧根治：分红步后移+预算闸门+补盘后兜底 | 已上线 |
| `a7fb3f0ef033` | 16:16 | fix(brief)：删除 AI速览免责小字（主人令） | 已上线 |
| `f11eb2ff0f75` | 16:34 | fix(dividend)：分红脚本单次调用加硬超时，根治 cn 链被并发堵死 | 已上线 |

### 批次 B · 主升/启动 板块·龙头股 独立卡（观测类首卡）
| commit | 时刻 | 内容 | ①核验 |
|---|---|---|---|
| `cc54ece2b14f` | 17:28 | sector-leaders-card：扩主升+启动、独立卡置于 ETF·龙头参考卡上方、补 `SECTOR_LEADERS.js` 注入（根治 09-17 死块） | 已上线 |
| `bd6f2cea15a3`→`8261f56bf09f` | 17:28→17:36 | sector-leaders-fetcher：加启动档 + 修写盘后 `body.encode` 崩溃；中途被并发 revert，已 replay 恢复 | 已上线（8261f56bf09f，PHASES✅、bytes 修复✅） |
| `6058d2ca2100` / `c62c2759edbb` | 17:33 | data：再生 SECTOR_LEADERS.js + sector_leaders.json（成对推） | 已上线 |

### 批次 C · 最终推荐融合器升级（今日核心）
| commit | 时刻 | 内容 | ①核验 |
|---|---|---|---|
| `1806ed2e275c` | 12:35 | 改动1+2a：动态加载 SIGNAL_EDGE（去硬编码）+ 融合器去噪（剔异常换手率/高手跟踪，ROE_TTM 降档） | 已上线 |
| `e5bc56de4acb` | 12:35 | 改动5：backtest_expectancy 移至 stage-B 尾部，D 批读同日夜 edge | 已上线 |
| `6507c61e170b` | 18:11 | 改动12：四量终极「正 edge 资格门」——基础分≤0 的票不再吃 qd/60m 加分（堵负 alpha 复活漏洞） | 已上线 |
| `88c9228aa2ea` | 18:15 | #221 扩样复核交接档（独立文件，见 docs/ops/handover） | 已上线 |
| （改动11 generate_top10 P5 兜底） | 此前 | sha `08d7f8995f9d`，今日 16:48 核验仍在 main | 已上线 |

### ① 远端真身指纹核验（tip `f46f25250c3b`，2026-09-18 19:38 取证）
- `algorithms/final_recommend.py` size=83568 md5=0c7f32756b → 特征位 `_load_signal_edge_dynamic`✅ `V8_FUSION_NOISE_FILTER`✅ `skipped_no_positive_edge`✅ `V8_FUSION_ALPHA_SELECT`✅
- `algorithms/generate_top10.py` size=78418 md5=6d596e2581 → `P5`✅ `兜底`✅
- `algorithms/run_algorithms.py` size=97586 md5=f3b9abcc16 → `backtest_expectancy`✅
- `scripts/fetch_sector_leaders.py` size=5990 md5=6407f7a011 → `PHASES`✅ `bytes`崩溃修复✅
- `index.html` Pages len=1084416 / `logic.html` Pages len=586206 → 均正常可加载

---

## 三、核心技术深挖（含「勿删勿简化」提醒）

### 3.1 最终推荐融合器新逻辑（改动1/2a/5/11/12）
主线：**只让带正边际 alpha 证据的票进池，负 alpha 票不再靠加分复活**。
```
回测期望前置(改动5) → 动态edge加载(改动1) → 去噪(2a) → 融合打分(基础分=正edge函数) → 四量终极资格门(改动12) → top10兜底(改动11)
```
- **改动12 资格门（最关键）**逻辑：**基础分 = max(0, min(4.5, 1.5 + edge_sum×0.18))**，edge_sum = 命中的正 edge 信号之和；纯负 alpha 票基础分压到 0。基础分≤0 的票**不再吃** qd(+0.5)/60m(+0.5~0.8) 加分。
  - 🔴 **勿删勿简化**：这堵的是「负 alpha 票（如纯机构/趋势组合）借 qd/60m 加分复活成独立源、淹没强信号」的旧回流漏洞。四条护栏每条对应真实误判：① 资格随动态 edge 自适应（扩样后某信号翻正自动恢复，不写死）→ 删掉会重回写死；② `V8_FUSION_ALPHA_SELECT=0` 回滚开关 → 删掉出事无法一键回退；③ 拦截数入日志+产物 meta `alpha_select` → 删掉变黑盒无法核查。
- **薄样本纪律（诚实交底）**：今日只做「源筛选」**不调权重**；p4_lowvol45（edge5 +0.338 但 edge10 −3.18 异号）未新增入融合；fund/quality 因子在融合器无独立入口，保持不动；sector 板块加分是主人既定机制，薄样本不碰。

### 3.2 龙头股卡（观测类首卡）
- 数据链：`fetch_sector_leaders.py`（挂 A 批 `fetch_sector_rs` 之后自动再生）→ `data/SECTOR_LEADERS.js`（含 `phase` 字段：主升在前、启动在后；同档 5 日涨幅降序）→ 前端 `#sectorLeadersCard`（ETF·龙头参考卡上方，主升红/启动橙分组）。
- 🔴 **勿删**：`SECTOR_LEADERS.js` 的 `<script>` 注入行（09-17 漏注入=恒死块，本轮已补）；电子化学品/其他电子 东财无同名板块，按「宁缺勿错」跳过（不做模糊匹配）。

### 3.3 今日事件页/分红
- 死因 A（分红步卡 60min timeout 撞整批 skipped）+ 死因 B（concurrency pending 顶掉）已四处方根治：分红步后移全部推送之后 + `--budget=900` + 三产物补 `post_close` + `queue:max`。📌 此四处为结构性修复，勿回退。

---

## 四、新功能/交付物（线上实测）

- **🚀 主升/启动 板块·龙头股**独立卡：data_date=2026-09-18，主升=元件（一博科技/天津普林/明阳电路/迅捷兴/金百泽），启动=风电设备/半导体/医疗服务/光学光电子/通信设备/互联网电商 各前5龙头。位置：暂未上架 → 观测类首卡（ETF·龙头参考卡上方）。Ctrl+F5 可见。

---

## 五、之② index.html ↔ logic.html 口径核验结果

双向业务词核验（code id 仅 index 出现不判缺陷，logic 为散文页）：
| 词 | index | logic | 判 |
|---|---|---|---|
| 龙头股 | 11 | 2 | OK |
| 主升 | 30 | 1 | OK |
| 启动 | 49 | 24 | OK |
| 最终推荐 | 30 | 34 | OK |
| 融合 | 2 | 8 | OK |
| alpha | 4 | 1 | OK |
| 回测 | 234 | 139 | OK |
| 边际 | 4 | **0** | ⚠️ 缺口 |

**结论**：功能卡（龙头股/主升/启动）两页口径一致；**最终推荐融合器的新逻辑（动态 edge + 去噪 + 资格门）logic.html 仍是旧 8 源模型描述（L1402–1523），缺 2026-09-18 升级说明**。

### 📋 交阿狸咪今晚粘贴的精确补段（logic.html，CRLF，插在 L1410「使用边界」之后、L1411 `</div>` 之前）
```html
        <div class="logic-h">融合器逻辑升级（2026-09-18 主人令：最终推荐必须回测、逻辑科学）</div>
        <div class="logic-p">最终推荐融合器由「多源加分求和」升级为「<b>动态 edge 驱动 + 去噪 + 资格门精选</b>」三层：<br>· <b>动态 edge</b>：每个信号源的 T+5 边际超额收益（已剔除大盘 β）由回测产物实跑算出，融合时实时读取，<b>不再写死权重</b>——扩样后自动跟随；<br>· <b>去噪</b>：剔除薄样本下噪音信号（异常换手率 / 高手跟踪），ROE_TTM 弱正降档；<br>· <b>资格门（今日最关键）</b>：基础分 ≤ 0（即无任何正边际 alpha 信号命中）的票，<b>不再吃</b>快速下跌 / 60 分钟多周期共振的加分——堵住「负 alpha 票借加分复活成独立源、淹没强信号」的旧漏洞。带三重护栏：资格随回测自适应（不写死）、可一键回滚（环境变量关）、拦截数入日志与产物可追溯。<br>📌 薄样本纪律：今日只做「源筛选」<b>不调权重</b>；结论只影响「谁进最终推荐池」不影响各源内部权重；待 40~60 信号日扩样复核后，再评估是否进一步动作（详见「六、遗留待办」改动13）。</div>
```
粘贴后请核验 logic.html 裸 LF==0（CRLF 合规）。

---

## 六、遗留待办（交阿狸咪 · 晚间与明日做）

| # | 项 | 状态 | 她要做 |
|---|---|---|---|
| 1 | **改动13** 择时闸门升独立加权项 | 悬停 | ⚠️ 踩红线「绝不按短期调权重」+ 9~12日薄样本。等 #221 扩样复核结论。判定标准见 `88c9228aa2ea` |
| 2 | **#221 扩样复核** | 已交 | 用 `raw_data/history/top10_daily_*` 63 交易日（baostock 小九拉黑，归阿狸咪）复核 11 源边际 edge 与 gate alpha；按预定判定标准执行，结论只影响「源资格」不调权重 |
| 3 | **logic.html 补段**（本节五） | 待粘 | 今晚粘贴精确补段（CRLF），补最终推荐新逻辑说明 |
| 4 | **改动2b** 破斜率倒挂 | 待办 | MEMORY 待上线列表，等排期 |
| 5 | **改动3** 星级→融合器映射 | 待办 | 须等「三重共识假0竞态(6)」修完再接 |
| 6 | **改动4** 最终推荐自回测闭环 | 待办 | 排期 |
| 7 | **改动6** 三重共识假0竞态 | 待办 | 排期，改动3 前置依赖 |
| 8 | **改动7** 融合器上线闸门 | 待办 | 排期 |
| 9 | **今晚监控** | 观察 | 20:00 D批 final_recommend 首跑新代码 → 查 `FINAL_RECOMMEND_DATA.js` 的 `alpha_select` 字段验证生效；21:00 E批 backtest_expectancy 再生 |

> 说明：#4–#8 为 MEMORY 长期「待上线」列表项，主人未就该批逐条回复指令，按「没做的/没回复的交接给她」一并移交，由她按优先级排期，勿擅自改权重类（踩红线）。

---

## 七、给收件人的特别提醒

- 🔴 **写仓窗口**：盘中 09:15–11:30 / 12:45–15:00 禁推 main；当前 19:44 在阿狸咪班次，可推，但 logic.html 补段用 CRLF 粘贴后须核对行尾。
- 🔴 **双机热文件**：小九已收工（17:45 后），但本地自动化仍会 `git reset --hard origin/main`——阿狸咪 push 前务必先 `git pull`/fetch 最新 tip，勿整树覆盖（前车 17:34 revert 教训）。
- ⚠️ **薄样本**：所有今日 alpha 结论基于 9–12 天 / 632 次信号，幸存者偏差，**扩样复核前不得按短期表现调权重**。
- 🔴 **环境坑**：本机 `git` 不在 PATH；推仓走 GitHub API（隔离单文件，绝不 force）；>1MB 文件（index.html）走 Pages 直取核验。

---

## 八、文件状态快照（tip `f46f25250c3b`）

| 文件 | size | md5(前10) | 行尾 |
|---|---|---|---|
| algorithms/final_recommend.py | 83568 | 0c7f32756b | LF |
| algorithms/generate_top10.py | 78418 | 6d596e2581 | LF |
| algorithms/run_algorithms.py | 97586 | f3b9abcc16 | LF |
| scripts/fetch_sector_leaders.py | 5990 | 6407f7a011 | LF |
| index.html | 1084416(Pages) | — | LF |
| logic.html | 586206(Pages) | — | CRLF |

---

## 九、本文档自身

- 路径：`docs/ops/handover/HANDOVER_小九给阿狸咪_2026-09-18_1944_改后三件套收口与遗留待办.md`
- 行尾：LF（合规）
- 推送：隔离单文件 API，绝不 force；推后回读 `git/blobs/{sha}` 核验字节一致。
- 防误删：第三节标注的「勿删勿简化」边界每条对应真实误判案例，接手方勿优化掉看似多余的判断。
