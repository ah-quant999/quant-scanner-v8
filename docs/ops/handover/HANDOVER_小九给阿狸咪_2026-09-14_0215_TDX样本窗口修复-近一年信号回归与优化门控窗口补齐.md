# 交接：回测「样本窗口」修复 —— 近一年信号回归 + 优化门控窗口补齐

- **时间**：2026-09-14 02:15（小九机 → 阿狸咪机）
- **类型**：P0 修复（口径/样本） + 三件套全做
- **触发**：主人指令「做完后改后三件套」；延续 09-14 凌晨《回测口径混用治本》一役
- **前置**：本批是 09-14 00:15 交接文档的**续作**，请连带阅读
  `docs/ops/handover/HANDOVER_小九给阿狸咪_2026-09-14_0110_回测口径混用治本-TDX陈旧条目裁剪与透传补齐.md`

---

## 一、一句话结论

09-14 凌晨那批「口径版本号 + 裁剪陈旧条目」**修对了一半，但激活了一个更深的隐性缺陷**：
`backtest_tdx.py` 的**信号样本被最长的持有档位吃掉了一年**，导致「优化策略」优化过滤
与 regime 序列**交集恒为空** ⇒ `optimized_summary` 整块字段静默消失 ⇒ 下游
`export_optimized_strategy.py` 直接 `sys.exit(1)`（而它是**全站择时唯一真源**
`OPTIMIZED_STRATEGY.current_regime` 的唯一产出方 ⇒ 前端择时会假死）。

本批已根治，并把三件套（①云端/双机对齐 ②两页口径对齐 ③本交接）全部做完。

---

## 二、根因链（三层，缺一层都解释不了现象）

### 层 1｜样本窗口被最长持有期吃掉（**真根因**）

`backtest_tdx.py` 原判据（第 575 行）：

```python
if idx is None or idx + max(HOLD_DAYS) >= n:   # max(HOLD_DAYS) 现为 250
    continue
```

要求**最长档 T+250** 有完整前瞻数据才记录该信号日。09-13 把持有阶梯从 5 档扩到
12 档（末档 250）后，**最近 250 个交易日的信号被整条跳过**——不是算出 0，是压根没算。

- 实测：产物信号窗口止于 **2025-09-01**，近一年样本凭空消失（短档 T+1/T+5 也白损失）。
- 而 `calc_forward_return()` 对 `target >= n` **本来就安全返回 None**，
  `_accumulate()` 也对 None **逐档跳过** ⇒ 那条 `max(HOLD_DAYS)` 护栏是**过度保守的纯累赘**。

### 层 2｜优化门控的 regime 序列窗口远短于信号窗口

`_fetch_index_ohlc(code, prefix, days=150)` —— 150 **自然日**，扣掉 regime 自身
21 日预热后只剩 **约 76 个交易日**（2026-05-28 起）。

层 1 让信号止于 2025-09-01 ⇒ **两个窗口交集恒为空** ⇒ `opt["total"] == 0`。

### 层 3｜0 样本时「静默丢字段」而非报错

```python
if opt_summary:
    if opt["total"] > 0:
        result_data["optimized_summary"] = sd     # ← 只有 >0 才写
```

0 样本时**整个 `optimized_summary` 字段不写**、无告警。下游：

```python
opt = bt.get("optimized_summary")
if not opt:
    sys.exit(1)          # ← E 批链一步失败；且 current_regime 永不刷新
```

> 前一批（09-14 01:10）裁剪掉 66 只旧口径僵尸条目前，这 66 只的信号延到
> 2026-07-07，**恰好覆盖了 regime 窗口** ⇒ 143 个匹配一直存在，缺陷被完全掩盖。
> 裁剪把掩盖物拿掉后立刻暴露：**143 → 0**。

---

## 三、改动清单（5 个文件）

| 文件 | 改动 |
|---|---|
| `algorithms/backtest_tdx.py` | ①护栏 `idx + 1 + min(HOLD_DAYS) >= n`（只需「入场日+最短档」可取，长档 None 自然落空）②`_fetch_index_ohlc` 窗口改**派生式**：`int((TDX_BARS + max(HOLD_DAYS)) * 365/244) + 90` 天，随扩档自动伸缩 ③`ENTRY_CALIBER_VER` **2 → 3**（样本集合变了必须整只重算，复用既有机制不新增开关）④0 样本/无 regime 时**大声报错**并写入 `optimized_summary_note` ⑤新增审计字段 `optimized_samples` / `signal_window` ⑥**性能**：`calc_forward_return(..., st_cache=)` 把「止损止盈」按入场日记忆化——它只与入场日+板块有关、与档位无关，原先 12 档各算一遍 |
| `algorithms/export_optimized_strategy.py` | 缺 `optimized_summary` 时**不再 `sys.exit(1)`**：降级为 `optimized.data_ok=false` + note，**`current_regime` 照常计算写入**、退出码归 0；`backtest_sample` 增 `entry_caliber_ver` / `signal_window` |
| `split_inline_data.py` | **漂移修复**：`make_lite('BACKTEST_TDX')` 与 `update_v8.py::_make_lite` 是重复实现（其注释自己就写着「改一处漏一处 = 漂移」）。09-14 给 update_v8 侧加了 `entry_caliber_ver`/`stale_caliber_dropped` 却没同步到这侧，已补齐 |
| `index.html` | TDX 卡副标题原硬编码 `9 个信号 × T+1/3/5/10/20`（实际已 12 档）⇒ 改为不列举数字的稳定文案 |
| `logic.html` | 口径说明卡新增两段：「样本窗口 · 各档独立计样本」+「优化策略门控窗口（2026-09-14 修正）」 |
| `raw_data/backtest_tdx.json` / `raw_data/optimized_strategy.json` | 重算产物（见下） |

`index.html` 纯 LF、`logic.html` 纯 CRLF，均保持原行尾；新增段落 `<div>` 4 开 4 闭对称。

---

## 四、验证数据（修复前 → 修复后）

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 信号日期范围 | 2024-09-13 ~ **2025-09-01** | 2024-09-13 ~ **2026-09-09** |
| 信号总条数 | 36,939 | **76,292** |
| regime 序列覆盖 | 76 个交易日（2026-05-28 起） | **797 个交易日**（2023-06-02 起） |
| `optimized_samples` | 无字段（过滤后 0） | **4051** |
| `optimized_summary` | **整块缺失**（下游 exit 1） | total=4051 · T+5 胜率 **56.0%** / 均收 **+1.04%** · T+10 **59.2%** / **+1.72%** |
| `optimized_strategy.json` | calc_time 09-12 · stocks_analyzed **296** · total_signals **143** | calc_time 09-14 · stocks_analyzed **230** · total_signals **4051** · `data_ok=true` |
| `current_regime` | grind / open_position=true（09-12 后未再刷新） | grind / open_position=true（**已恢复刷新**，口径一致无翻转） |
| 各档已决样本 | 长档靠 None 静默丢 | T+1 41084 → T+250 21495 **逐档递减**（未到期档不进样本，符合「禁止 0 冒充」） |

**性能副产物**：`calc_forward_return` 记忆化实测 **9.9× 提速**（5244 次调用 12.26s → 1.24s），
全量重算 221 只从预估 ~95 分钟降到 **23 分钟**。等价性已证：用**未加记忆化**的前 5 只产物
逐字节 md5 对照 ⇒ **5/5 完全相同**（记忆化不改变任何数值）。

---

## 五、三件套①：云端与双机对齐审计结论（**无需改动**）

逐项核过，均为「不改」：

| 检查项 | 结论 |
|---|---|
| `.github/workflows/v8_build_deploy.yml` 触发路径 | 已含 `raw_data/**` + `algorithms/**` + `index.html` + `update_v8.py` ⇒ 推 raw 即全量重建 `data/*.js` ✅ |
| `update_v8.py` CATEGORY_MAP | `optimized_strategy.json → OPTIMIZED_STRATEGY` 已映射 ✅ |
| `v8_stage_gate.py` E 档 READY_SPEC | `items` 含 `data/BACKTEST_TDX.js`、`must` 为三个聚合器产物；**只按「新鲜度+存在性」判定，无条数期望** ⇒ 本次改动只会让产物更新鲜 ✅ |
| `guard_v8_freshness.py` | `BACKTEST_TDX: 24` 只查 24h 新鲜度，无条数期望 ✅ |
| `v8_deploy_guard.py` `MIN_SIZE` | `"BACKTEST_TDX.js": 1350` 是**体积下限**（防空/shell 文件）⇒ 样本变多只会更大，不会触发 ✅ |
| E 档验收自动化（23:50） | 判据是 `update_time` 日期，**不硬编码名单、不判条数** ✅ |
| `dedup_fetch_manifest.py::_ALWAYS_PUSH` | 已含 `data/BACKTEST_TDX.js`（`must ⊆ _ALWAYS_PUSH` 不变式满足）✅ |
| 双机任务配置 | 本次是算法口径改动，**不涉调度/派发变更**，双机任务无需调整 ✅ |
| `gen_backtest_all_algos.py` | 已是「每档一行 + 零样本归 null + status=累积中」⇒ 与本次「各档独立计样本」语义天然对齐，**无需改** ✅ |
| 下游消费点普查 | `final_recommend` / `generate_top10` / `fundamental_helper` / `stage_to_raw` / `regime_filter` 均为描述性引用或纯映射，**无硬编码条数** ✅ |

**E 批会自动带上新逻辑**（runner 拉 main 后跑 `run_algorithms` stage E），且因
`ENTRY_CALIBER_VER=3` 已在本机跑完，云端跑时 172 只全部缓存命中⇒跳过，秒级完成。

---

## 六、给阿狸咪的三条硬提醒

1. **不要把 `backtest_tdx._merge_market_regime` 与 `regime_filter._merge_market_regime`「统一」**
   —— 两者**角色不同**：前者算**全历史 regime 序列**（供回测门控，窗口必须覆盖信号窗口），
   后者算**当前状态**（供 `final_recommend`/`generate_top10` 实时门控，只取近 150 天 + 有
   `stale_days` 失败语义）。函数体逐行相同，**窗口不同是刻意的**（已逐键比对
   `OPTIMIZED_REGIME_SIGNALS` 与 `OPTIMIZED["regime_signals"]`：完全一致）。
2. **凡往 `backtest_tdx.json` 顶层加字段，必须同步两处白名单**：
   `update_v8.py::_make_lite` **和** `split_inline_data.py::make_lite`（重复实现，已漂移过一次）。
3. **改样本集合/口径，一律只改 `ENTRY_CALIBER_VER` 一个数字**，断点续跑会据此把全部标的
   重算（本次即是：2 → 3 ⇒ 172 只全量重算）。别再另加开关。

---

## 七、遗留项（待主人拍板 / 供参考）

| # | 事项 | 说明 |
|---|---|---|
| 1 | `optimize_stop_target.py` 是**孤儿脚本**（`ORDER`/`STAGES`/workflow 全无引用，仅自引用） | 好消息：实时止损止盈参数走 `stop_target_logic.compute_stop_target` 运行时计算，**不受本次样本变化影响** |
| 2 | `split_inline_data.py::make_lite` 与 `update_v8.py::_make_lite` 重复实现（已二次漂移） | 建议改为单向 import 唯一真源；本次只做「补平」未做「去重」 |
| 3 | TDX 卡下表只展示 5 档显示（数据层已是 12 档） | 属 UI 取舍，本次只把副标题改准，未扩表（避免卡片过宽） |
| 4 | 09-13 扩档引入的**结构性代价**：最长档决定「信号能追到多近」 | 现在 T+250 档的样本自然更旧（21495 vs T+1 41084）。若主人希望「近期信号优先」，可讨论是否把 180/250 档改为「可选统计不进主线」 |

---

## 八、补遗（02:22 推送）：一次「Edit 报成功但实际未写入」引发的 P0 不一致

本批 02:15 首次推送（`c6f6bce5f`）后，**推送后内容级复核**抓到一处致命不一致 —— 记录于此，
因为它揭示的坑比本次修复本身更值得警惕。

| 现象 | 真相 | 后果 |
|---|---|---|
| `backtest_tdx.py` 收尾已是 `_dump_json(result_data, OUT)`，但全文件**搜不到 `def _dump_json(...)`** | 该文件的「新增 `_dump_json` 定义」与「替换末尾写出调用」是两次独立 Edit；**前一次报成功但未落盘** | 脚本跑完整轮回测后收尾必抛 `NameError: name '_dump_json' is not defined` ⇒ 退出码非 0 ⇒ `run_algorithms` 的 E 批链被判残缺；且产物停留在「每只一存」的中间快照（**缺 `optimized_summary` / `summary` / 审计字段**），下游 `export_optimized_strategy.py` 只能降级、全站择时真源停止刷新。 |

**已修复（`f890f42d8`）**

1. 补回 `_FMT_SEP` / `_SIZE_WARN(95MB)` / `_dump_json(obj, path)`（插在 `def log()` 之后）。
2. 顺带把「每处理一只存一次」的**中间快照**也统一走 `_dump_json`（原为 `json.dump(..., indent=2)` 直写 `OUT`）：
   - ① **格式一致**：否则收尾前中断会留下 indent=2 的残留产物（126.9 MB 且缺汇总字段，不可直接用）；
   - ② **IO**：快照随 `stock_results` 累积逐次变大、221 只共写 221 次全量，是该脚本耗时的大头之一。
   - 改后文件内**已无 `indent=2` 的活代码**（仅 3 处注释引用历史值）。

### 给阿狸咪的硬提醒（**第 4 条，比前三条更通用**）

4. **不要信任 Edit / 写入工具的「成功」回执。** 本机已累计发生两次「报成功而内容未变」。
   凡关键改动，改完必须立刻做「读回 + 关键字计数」复核；对 Python 还要 `py_compile` +
   **实际调用一次**。本批最终采用的验证口径（可直接复用）：

   ```text
   py_compile 通过；
   模块加载 OK（注：exec 冒烟需自行补 __file__，否则
     NameError: name '__file__' is not defined 属「冒烟环境」问题，不是脚本的问题）；
   _dump_json({'a':1,'b':[1,2,3]}, tmp)  →  输出 {"a":1,"b":[1,2,3]}（紧凑无空格）；
   全量改动点逐条 substring 计数 16/16 通过。
   ```

### 本批最终推送清单（`origin/main = f890f42d8`）

| 文件 | 行级 diff |
|---|---|
| `algorithms/backtest_tdx.py` | `+69/-11` 与 `+27/-2`（分两次提交） |
| `algorithms/export_optimized_strategy.py` | `+23/-8` |
| `split_inline_data.py` | `+4/-0` |
| `index.html` | `+1/-1` |
| `logic.html` | `+4/-0` |
| `raw_data/backtest_tdx.json` | 紧凑化重排（72.6 MB，零信息损失） |
| `raw_data/optimized_strategy.json` | `+18/-15` |
| `docs/ops/handover/HANDOVER_..._0215_*.md` | 本文件 |

### 双机并发的两点确认

- **`index.html`**：本批之后被阿狸咪「缓存戳实时对齐 ?v → 防覆盖」（`5dab34555`）改动 102 行；
  该提交的 parent 即我的 `c6f6bce5f` ⇒ 我的文案改动**已被包含**，无需重做（内容级复核已确认）。
- **`logic.html`**：落地基线为 `86bb10318`。其间阿狸咪推了 `2360c9e0a`（E 批补 CITIC 两脚本 +
  位次纠正为 49/51）与 `86bb10318`（A 批补 `scripts/fetch_index_history`）两次补丁 ——
  我的两段是**重新落到最新版**上的（不是 worktree 陈旧基线），已程序化复验双方改动并存、锚点唯一。

### 一条关于 `logic.html` 行尾的坑（**重要，会致整文件改写**）

`.gitattributes` 对本仓 `.html` 的策略是「`*.html text eol=lf`，但 `logic.html` 单独例外为
`-text -eol`」—— 因为 **`logic.html` 的 blob 本身就存着 CRLF**（全仓唯一例外）。

后果：若在仓库**外**（如 `%TEMP%`）改好 `logic.html` 再 `git hash-object`，路径匹配不到
`logic.html` 那条规则、只会命中通用 `*.html text eol=lf` ⇒ **CRLF 被规范化成 LF**
（714946 B → 707387 B），推送即**整文件改写**、覆盖对方改动。

✅ 正确姿势：`git hash-object -w --path=logic.html <外部文件>`，用 `--path=` 显式指定属性路径，
使其命中 `-text -eol`，保证 blob 逐字节保留 CRLF。本批已按此法执行（blob `f274b3da05`，
716039 B / CRLF=8652），行级 diff 确认**仅 `+4/-0`**（不是整文件改写）。
