# 交接单：暂未上架 13 卡分类重组 + 死卡降级 + slot 动态化

**发送**：阿狸咪的工程师  
**接收**：小九  
**时间**：2026-09-12 17:05 CST  
**主题**：暂未上架 13 卡分类重组 + 死卡降级 + slot 动态化  
**状态**：已上线

---

## 1. 本次改动摘要

按主人 2026-09-12 授权，对 `index.html` 暂未上架区做一劳永逸式修复，已 push main → GitHub Pages 自动部署。

| 项 | 处理 |
|---|---|
| 13 卡分类重组 | 原「实验区/因子实验室/宏观观测」拆为 **观测类 / 回测·框架 / 因子类 / 宏观类** 四子页 |
| 死卡⑬ | 「新周期判定·海外宏观」移除「9/9 CPI 后更新」假承诺，改标 **历史快照·手动维护** |
| 注释误导① | ETF·龙头参考兜底文案「等待盘后算法链生成」→ 删除，仅保留「热点板块数据暂不可用」 |
| slot 写死 | 8 处 `_uBadge('盘后',...)` / `slot:'盘后'` 改为 `'auto'`，时段标签随 `update_time` 自动分档 |

**Commit**：`1a25235d7`  
**Parent**：`4e440013d`（远端 main 最新）  
**仅改文件**：`index.html`

---

## 2. 改后三件套

| # | 项 | 状态 | 说明 |
|---|---|---|---|
| ① | 审计+对齐云端 workflow 与双机任务时窗 | ✅ | 本次纯前端 `index.html` 改动，无 workflow/定时任务/数据源变更，不影响双机分时 |
| ② | 对齐运维页与逻辑详解页，跑 `align_logic_ops.py` | ✅ | EXIT 0；关键 workflow 均已文档化，无过期引用 |
| ③ | 交接另一台机 | ✅ | 本单即为交接 |

---

## 3. 四子页卡片分布

| 子页 | 卡 |
|---|---|
| 📊 观测类 | 概念/行业→ETF·龙头参考 · 主题空间·概念资金热度 · 中信PE极值温度计 · A股指数PE-TTM分位 · AI洞察多源观点对比 |
| 📈 回测·框架 | RPS 30天样本考核 · 中信PE<10历史回测 · 指数估值中枢+趋势门控 |
| 🧪 因子类 | 因子实验室 · 因子审计 · 因子补缺 walk-forward 进度 |
| 🌍 宏观类 | 机构研究·AI解析建议 · 新周期判定·海外宏观+AI大周期 |

---

## 4. 需要你（小九）关注 / 回执

1. **④⑤ 中信PE系列流水线缺口**：生成器 `v8/gen_citic_pe.py` 存在且挂 B 批，但产物 `update_time` 停在 2026-09-10。请检查 B 批链健康，不是前端 bug。
2. **死卡⑬后续**：当前降级为「历史快照·手动维护」。若主人后续要求接活源生成器（如 NFP/CPI/FOMC 事件触发），请评估数据源与触发条件。
3. **验证上线效果**：请在工作机或手机浏览器 Ctrl+F5 硬刷新 `https://ah-quant999.github.io/quant-scanner-v8/` → 暂未上架区，确认四子页切换正常、时段标签随数据时间正确显示。

---

## 5. 附：验证命令

```bash
cd E:/workspace/stock-scanner
git fetch origin main
git log --oneline -3 FETCH_HEAD
# 应看到 1a25235d7 fix(v8): 暂未上架13卡分类重组...
```

---

阿狸咪的工程师  
2026-09-12 17:05 CST
