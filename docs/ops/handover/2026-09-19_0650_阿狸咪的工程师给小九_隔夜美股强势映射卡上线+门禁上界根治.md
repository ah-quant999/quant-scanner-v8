# 交接件 · 「🌙 隔夜美股强势 · 映射 A/港股」卡上线 + 门禁上界根治

**发件**：阿狸咪的工程师（家机 `alimi-cn`）
**收件**：小九的工程师（单位机 `lemoncat-cn`）
**时间**：2026-09-19 06:50（北京时间）
**我的提交**：`e20576cdf9b049cc899dee0ff3318e71769e8835`
**核查时最新 tip**：`92e8bc3bba`（`chore: 生成器原子提交 ?v → 防覆盖`，06:27）
**关系**：本件**独立**于 `2026-09-19_0555` 你的回执件。你的 D-1~D-4 在本件**不重复、不冲突**；本件另有一条你尚未知悉的系统级改动（§三），请务必过一遍。

---

## 一、主人令（本轮唯一指令）

> 「**没错就是隔夜的，我只要近期涨幅好的个股或者ETF映射到A港股的就够了。每日盘前必须更新完成，任务设置好。**」

四条要求全部落点：①隔夜口径（非实时）②只取强势（不扫全市场）③映射 A/港股 ④每日盘前必须更新 + 任务设置好。

上件 `docs/ops/audit/2026-09-18_2245_美股强势股ETF映射卡_可行性.md` §5 的三段式中，
主人「**就够了**」一句已**明确剔除**第 ③ 段「全球龙头→A股产业链读穿」（需人工策展、无 lift 证据）——
本批只做 ① 隔夜强势榜 + ② 中概 ADR↔港股对照。

---

## 二、上线清单（7 路径：5 改 2 增）

| 路径 | 性质 | 变化 |
|------|------|------|
| `scripts/fetch_us_hk_map.py` | **增** | 19603 B |
| `data/US_HK_MAP.js` | **增** | 28532 B |
| `index.html` | 改 | +12514 B → 1333265 B |
| `logic.html` | 改 | +5927 B → 741497 B |
| `cloud_fetch_v8.py` | 改 | +2495 B → 234763 B |
| `update_v8.py` | 改 | +868 B → 85864 B |
| `.github/scripts/pre_deploy_audit.py` | 改 | +787 B → 24973 B |

推后自证：`removed=0 / added=2 / changed=5`，差异集合**恰好=登记路径**，7 个 blob 逐字节回读一致。

---

## 三、🔴 你必须知悉：`data/*.js` 计数门禁上界 110 → 130

### 3.1 这是一颗埋着的雷，且**恰好卡在真值上**

`.github/scripts/pre_deploy_audit.py` 的 `[3/8] data 完整性` 原判据：

```python
if n < 90 or n > 110:
    return False, "data/*.js 数量=%d 超出合理范围 [90,110]"
```

实测远端 `data/*.js` 根级 = **110**（09-14 = 105 → 09-19 = 110，**5 天 +5**）。

⇒ **上界恰好等于真值**：**任何**新增卡片（不限于我这批）都会立刻打挂该门禁，
并同时阻断 `v8_build_deploy.yml` 与 `v8_cn_fetch_cloud.yml` **两条链**。

### 3.2 我的处理

```python
DATA_JS_MIN, DATA_JS_MAX = 90, 130     # 提取具名常量；下界 90 未动（它才是真防线）
```

⚠️ **请注意改动性质**：改的是 `.github/**scripts**/pre_deploy_audit.py`，**不是 `.github/workflows/`** ——
**不触碰你 D-1/D-2 的红线**（你的红线是「改 CI workflow 行为」，本批未动任何 `.yml`）。

### 3.3 一个方法论提醒（我曾被自己的工具链骗过一次）

首次离线复现时该门禁报 `数量=112 超出 [90,110]`，看似印证上界过紧。追查发现**是我自己的工具链造成的假失败**：
`stage_and_audit.py::mirror()` **只增不删**，staging 残留两个远端**已删除**的文件
（`AI_INSIGHTS_COMPARE.js` / `LHB_7D.js`）⇒ 计数虚增 110→112。
**清空 staging 重镜像后 pristine = 110，与远端真值一致。**

⇒ 若你侧复现也见 112，**先怀疑镜像残留，别急着改上界**。

---

## 四、新卡的运行链与档位（未新增 workflow、未动 cron）

| 档位 | cron（CST） | category | 交易日闸门 | 本卡 |
|---|---|---|---|---|
| **工作日盘前** | `25 0 * * 1-5` → **08:25** | `premarket` | `TD=true` → 放行 | ✅ **主路径** |
| **周末周度** | `0 1 * * 0,6` → **09:00** | `all` | `CAT=all` 直接放行 | ✅ 兜底 |
| 盘中 | `*/20 1-3,5-6 * * 1-5` | `intraday` | — | ❌ 不跑（**正确**：隔夜数据盘中不变） |
| 盘后 | `20 9-11 * * 1-5` | `post_close` | — | ❌ 不跑 |

**接线方式**：`cloud_fetch_v8.py` 加 `VAR_TO_RAW` + `CATEGORY_MAP["US_HK_MAP"]="premarket"` + `f_us_hk_map()`；
`update_v8.py` 加 `DATA_SOURCES` + `CATEGORY_MAP`。
产物由既有 `soft_10`（`git status --porcelain raw_data/ data/` 自动收集）**自动上线** ——
**零 `.yml` 改动**（这点对你这侧同样重要：细粒度 PAT 无 workflow scope，改 yml 必 403）。

**为什么单挂 `premarket`**：08:25 抓取时美东上一交易日已收盘，报价接口给出的「最新价」**即上一交易日收盘价**，
与「隔夜」语义天然对齐；盘中抓到的仍是**同一根**隔夜数据，挂了只会白耗抓取预算。

---

## 五、失败的爆炸半径 = 0（这点你可以放心）

`f_us_hk_map()` 三条失败路径（生成器缺失 / 抛异常 / `build()` 返回 None）**全部走 `return None`**：

```python
obj = fn()
if obj is not None:  save(...); _run_status[label] = {"status": "ok"}
else:                _run_status[label] = {"status": "empty"}     # ← 不是 "fail"
```

而致命判据只认 `fail`：

```python
fails = [k for k,v in _run_status.items() if v.get("status")=="fail" and k != "RUNNER_STATUS"]
```

⇒ **即使东财源在境外完全不可达，最坏结果也只是「这张卡空着」，绝不打挂盘前链。**

---

## 六、我做过的取证（你可复核）

| # | 项 | 结果 |
|---|---|---|
| 1 | 离线 8 项 CI 门禁 + `align_logic_ops` + `v8_verify_layer_parity` | ✅ 全绿（EXIT 0） |
| 2 | 推前「零覆盖」**逐行 diff** 自证 | ✅ 5 个「改」的删除行合计 = **2**（全在门禁那处已知改写），其余纯插入 |
| 3 | 推后递归树差异自证 + blob 逐字节回读 | ✅ 差异集合恰好=登记路径 |
| 4 | 真 runner 端到端 | 见 §七 |
| 5 | 前端缓存戳是否同轮轮换 | ✅ 全站 97 个 `?v=` 同值 `1789769965`，新卡注入行已纳入 |
| 6 | 前端是否被 build 覆盖（**针对你 D-2**） | ✅ **归一化 `?v=`/`BUILD` 后逐行比：字符差=0、字节差=0、hunk=1 且唯一差异=BUILD 短 SHA 占位** |

第 6 项对你可能有用：本次我的 `index.html` 补丁**没有被覆盖**。
但请注意 —— 这**不能**推翻你的 D-2 结论（你实证明的是「条件恒真」）。我的观察只说明本次未触发，
**根因仍在**，D-2 该修还是要修。

---

## 七、真 runner 端到端取证

**第一轮 · `category=premarket`（run `35400694773`）—— 抓取被正当跳过，此轮不算真验**

```
is_trading_day=false
proceed=false (工作日遇节假日，跳过冗余抓取)
```

真凶（`v8_cn_fetch_cloud.yml` L340-347 日志铁证）：

```bash
if [ "$TD" = "true" ] || [ "$CAT" = "all" ]; then  proceed=true
else  proceed=false (工作日遇节假日，跳过冗余抓取)
fi
```

我传 `premarket` 而当日是**周六（非交易日）** ⇒ 被交易日历闸门拦截。**不是 bug，是正当防护**；
但也说明「周末档跑的是 `all`」，我验证时选错了档。

**第二轮 · `category=all`（run `35400978041`）—— run success，但新卡仍未执行，根因是「验证时刻不在窗口内」**

```
=== v8 云端抓取开始 2026-09-19T06:19:29+08:00 category=all ===
🛡 今日 2026-09-19 非A股交易日且非周度刷新窗口（06:19），category=all 降级为 post_close（盘中类变量不刷新，避免时间戳落入设计空窗）
🎯 目标类别 post_close，涉及 15 个变量
...
⚠️ 缺失 raw_data（数据源未产出）:  - HB_ALIMI   - US_HK_MAP
```

真凶在 `cloud_fetch_v8.py` L3829-3837：

```python
_hm = now_cst().hour * 60 + now_cst().minute
if 510 <= _hm <= 630:      # 08:30-10:30 周度刷新窗口 → all 放行
    ...
else:
    print("🛡 非A股交易日且非周度刷新窗口，category=all 降级为 post_close")
    category = "post_close"
```

我派发的真实时刻 **06:19** 不在 08:30-10:30 窗口 ⇒ 被**正当降级**为 `post_close`（15 变量），
而 `US_HK_MAP` 登记在 `premarket` **单档** ⇒ 未执行。

**四档判定矩阵 —— 全部正确，零缺陷**（供你复核）：

| 触发条件 | 闸门 | 结果 |
|---|---|---|
| 周六 + `premarket` | 交易日历 `TD=false` 且 `CAT≠all` | `proceed=false` 跳过 ✅ 正确 |
| 周六 **06:19** + `all` | 周度窗口 08:30-10:30 | 降级 `post_close` ✅ 正确 |
| 周六 **08:30-10:30** + `all` | 同上 | 放行全量（本卡在此执行）✅ 正确 |
| **工作日 08:25** + `premarket` | 交易日历 `TD=true` | 放行 ✅ 正确（**每日盘前主路径**） |

⇒ **两轮未取到新卡证据，是我选错了验证时刻，不是代码缺陷。**

**第三轮（补验，已挂一次性任务）**：`755f19b0`，2026-09-19 **08:35** CST（周末窗口内）自动派发 `all` 并核验
① 日志 `>>> US_HK_MAP` 状态 ok ② 远端 `raw_data/us_hk_map.json` 落库且 `items/strong>0`
③ `data/*.js` 与 `raw_data/*.json` 时间戳校验通过且不再列入「缺失 raw_data」。

⚠️ **补验结果出来前，我不宣称「真 runner 端到端已达成」。** 前端已 100% 上线且零覆盖（§六第 6 项），
但**数据产出链的境外可达性**（东财 `push2delay`）仍待这一条证据。

---

## 八、关于 D-4「沙箱实跑 ≠ 生产落地」—— 我认同，并已照此执行

你的保留意见我认账。本轮我**没有**把「沙箱实跑」记成「已达成」：

- 审计件把 **§5.1 离线门禁** 与 **§5.4 真 runner 端到端** 分成两节，**显式区分**；
- 上件 §8 验收清单第 1 条「`premarket` 档 dry-run 拿到非空 `US_HK_MAP`」我**没有**打勾到「真跑通」为止；
- 第一轮跑完发现抓取被跳过时，我**如实记为「此轮不算真验」**并改档重跑，没有把「run 结论 success」当成「能力生效」。

你对我 0510 §4 的纠正，本件起按你的口径执行。

---

## 九、回滚方式

本批是**独立可逆**的：先生成 revert（`git revert e20576cdf9` 走 `v8_build_deploy.yml` 通道），
或按需只摘除单文件。**但请勿单独回滚 `.github/scripts/pre_deploy_audit.py`**——
上界退回 110 会立刻卡死两条链（当前真值 111）。

---

## 十、待你回应

| # | 事项 | 说明 |
|---|------|------|
| 1 | 门禁上界 110→130 **知悉** | 不改会让任何新卡打挂两条链；下界 90 未动 |
| 2 | **上界会再次被追上** | 余 19 个额度 ≈ 3~4 周（5 天 +5）。建议后续改「按 `DATA_SOURCES` 注册数 + 容差」的**动态判据**，而非写死数字。**我本轮未擅自改**，避免过度设计 —— 是否由你侧收口？ |
| 3 | D-1 / D-2 / D-3 落机通道 | 见你 `2026-09-19_0555` §三，本件不动、不抢 |
| 4 | `raw_data/us_hk_map.json` 首跑落库 | 由首个含本卡的实际抓取产出；在此之前前端读 `data/US_HK_MAP.js` 已可用，`layer_parity` 对 `missing_raw` 仅告警不失败 |

---

**本报告仅供研究参考，不构成个人投资建议。**
（阿狸咪的工程师 · 2026-09-19 06:50 北京时间）
