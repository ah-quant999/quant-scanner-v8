# 交接文档 · 生命周期子页三卡「真实数据回测」上线

- 时间：2026-09-16 19:18（已收盘，写仓窗口内）
- 执行机：小九（白班）
- 提交：`3add42e3e` feat(life): real-data backtest cards for lifecycle/strong-breakout/ima-strong
- 推送方式：净室重放（`life_push.py` v3，fail-closed 非 force，远端前进则重试，绝不整体覆盖他人改动）
- CI：`pages build and deployment` run 35090486373 → completed/success

## 高层摘要
生命周期子页面新增 3 张「✅ 真实数据回测」标识的 N 日胜率（信号层）回测卡，数据源为**真实日 K + 逐日真实信号账本**，已在主站 `origin/main` 上线。

## 数据真实性（主人第一铁律）
| 项 | 内容 |
|----|------|
| 价格源 | `raw_data/kline_cache`（真实日 K 缓存，非估算） |
| 信号源 | `raw_data/strong_breakout_history.json`（逐日真实信号账本） |
| 回测口径 | 信号日次一交易日**开盘买入**，持有 N(1/2/3/5/10) 交易日**收盘卖出** |
| 成本 | 已扣双边交易成本 **0.30%**（15bps×2），`cost_adjusted: true` |
| 诚实性 | 未成熟档（强势突破 T+10：`samples=0, immature=true`）在「样本」列显 `0（未成熟）`、6 指标列显 `—`，**绝不用 0 冒充** |
| 数据截至 | 强势突破 2026-09-03~2026-09-16（114 笔）；高手强势股 2026-06-09~2026-08-24（40 笔） |

## 文件清单（8 个）
| 文件 | 状态 | 说明 |
|------|------|------|
| `index.html` | 改 | 3 卡 HTML 容器 + 2 个 `data/*.js` 脚本标签 + `lifeRenderBacktest()` 渲染函数 + post-ready 接线 + pane switch 接线 |
| `update_v8.py` | 改 | `DATA_SOURCES`/`CATEGORY_MAP` 增 2 条 + experiment 段调用 `algorithms/backtest_life_cards.py` |
| `v8_health_check.py` | 改 | `CARD_DEFS` 增 `STRONG_BREAKOUT_BACKTEST`/`IMA_STRONG_BACKTEST` 两条 |
| `algorithms/backtest_life_cards.py` | **NEW** | 真实信号层回测生成器（build_periods，未成熟档全 null） |
| `data/STRONG_BREAKOUT_BACKTEST.js` | **NEW** | 强势突破真实回测数据（window 注入） |
| `data/IMA_STRONG_BACKTEST.js` | **NEW** | 高手强势股跟踪真实回测数据（window 注入） |
| `raw_data/strong_breakout_backtest.json` | **NEW** | 强势突破原始分层 |
| `raw_data/ima_strong_backtest.json` | **NEW** | 高手强势股原始分层 |

## 验证结果
| 验证项 | 结果 |
|--------|------|
| 远端 `origin/main:index.html` marker | ✅ 8/8（lifeBackCard/sbBackCard/imaBackCard/两脚本标签/函数/渲染触发） |
| 远端 NEW 文件真身 | ✅ `data/*.js` 时间戳 17:32:27、`raw_data/*.json` 合法 JSON（periods 1d~10d） |
| CI Pages 构建 | ✅ completed/success |
| 净室重放未覆盖他人 | ✅ 推送时远端在 churning（ee383a503/755d3c53b 缓存戳提交），本提交 fast-forward 叠在其上 |
| 本机硬抓 Live Web | ⚠️ 受限：本机→GitHub HTTPS(443) 通道不稳（curl 到 github.io/raw 多次空回），以 git/SSH 真身验证为准；主人 Ctrl+F5 主站目视确认即可 |

## 不在本次范围（待办）
- 工作树另有未跟踪文件 `data/CANDIDATE_BACKTEST.js` / `data/GOLD_POOL_BACKTEST.js` / `raw_data/candidate_backtest.json` / `raw_data/gold_pool_backtest.json`（候选池/黄金池回测），**未接前端、不在本次授权范围，未被推送**。如需上线须另走净室重放并先接 index.html 渲染接线。
- `logic.html` 无平行生命周期子页结构，口径对齐不适用。

---

## 补刀（19:41）：3 张卡标题标注策略名
- 问题：3 张卡标题雷同、仅左边框色区分，分不清谁的回测。
- 映射：lifeBackCard→选股生命周期 / sbBackCard→强势突破 / imaBackCard→高手强势股跟踪。
- 修法：单文件标题替换（fix_life_titles.py，按 id 锚定，不增删卡），推后远端旧标题 0 / 新标题 3。
- 卡 id ↔ 标题：
  - lifeBackCard → 📊 选股生命周期 · 回测 N 日胜率（信号层）
  - sbBackCard → 📊 强势突破 · 回测 N 日胜率（信号层）
  - imaBackCard → 📊 高手强势股跟踪 · 回测 N 日胜率（信号层）

---

## 改后三件套收口确认（19:48 小九自查）
| 件套 | 内容 | 状态 |
|------|------|------|
| ① 对齐云端/双机 | 两次提交（`3add42e3e` 三卡 + `45f31ea70` 标题补刀）已 fast-forward 进 `origin/main`；CI Pages 构建 completed/success | ✅ |
| ② 对齐 index.html↔logic.html 口径 | `logic.html` 无平行生命周期子页结构，`grep` 0 命中，口径对齐不适用 | ✅ |
| ③ 写交接文档 | 本文件已落盘，并净室推到 `origin/main`（阿狸咪 `git pull` 即可见） | ✅ |

## 阿狸咪交接须知（夜间/周末机必读）
1. **今日主线（origin/main）已含**：3 张真实回测卡（生命周期 / 强势突破 / 高手强势股跟踪）+ 每张卡标题带策略名（19:41 补刀，提交 `45f31ea70`）。数据全部来自真实日 K + 真实信号账本，未成熟档（强势突破 T+10）已诚实标 `—`，未用 0 冒充。
2. **你接手前先同步**：`git pull origin main` 拉到最新（已含小九今日全部提交 + CI 注入的 `?v=`/`BUILD` 缓存戳）。**切勿用旧缓存整体覆盖 `index.html`**——小九已在 `45f31ea70` 落定三卡与标题，任何旧版整体写回都会丢这 3 张卡。
3. **本批无需你改 index.html / 跑回测生成**：回测数据（`.js` + `raw_data/*.json`）已由小九生成并推上，勿重复跑 `algorithms/backtest_life_cards.py` 或覆盖数据文件。
4. **双机铁律不变**：阿狸咪紧急指令监听仍「只读不写」；`HEALTH_CHECK.js` 冲突保留本地不动；`index.html` 阿狸咪编辑期间禁止触碰（本批已由小九在白班落定）。
5. **自动化任务**：你名下 stock-scanner 自动化任务保持 **PAUSED**（禁删）；恢复 ACTIVE 须主人明令。今日不需要你恢复任何任务来配合本批。
6. **异常处置**：若夜间发现三卡渲染异常/数据陈旧，先走本交接文档核对，必要时邮件告警给主人（2814546@qq.com），**勿擅自重推 main**。
7. **不在范围**：工作树未跟踪的候选池/黄金池回测（`data/CANDIDATE_BACKTEST.js` 等）未接前端、未推，与本批无关，勿动。
