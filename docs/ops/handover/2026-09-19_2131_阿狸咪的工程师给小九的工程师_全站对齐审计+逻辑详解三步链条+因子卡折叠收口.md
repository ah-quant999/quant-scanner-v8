# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-19 21:31｜全站对齐审计+逻辑详解三步链条+因子卡折叠收口

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

按主人令做完「全面对齐 + 全面审计 + 改后三件套（含防覆盖）」：**算法链无断裂**（全仓 0 处悬挂调用、0 个 workflow 引用已删模块），`align_logic_ops.py` **EXIT 0**；并把逻辑详解页的「因子计分方法规则」补出**第八章（三步链条 + 折叠规则）**，另纠偏 5 个脚本里指向已删 `strong_breakout` 模块/卡片的过时注释。

---

## 正文

### 一、本机本轮推送（3 笔，全部 Git Data API `force:false`，基线逐笔重取）

| commit | 内容 |
|---|---|
| `07d9690eb4` | 🗑 强势突破全站删除（16 删 / 5 增 / 10 改）+ 🆕 缠论买点账本上线（55 条） |
| `a541b6d114` | 📝 index.html 注释纠正（原「强势突破回测数据层保持不动」与事实相悖，已改为如实） |
| `55df5ba4e7` | 🎨 每日因子计算卡折叠改造：第二步**紫罗兰**区别色圆弧徽章；第一步 26 项全表 / 第二步台账与口径明细默认折叠；头部静态区保留；主展示=第三步「当日因子计分榜」 |
| `f6ea35f0cc` | 📐 logic.html 新增「八、三步链条」专节 + 🧹 5 脚本过时引用纠偏 |

### 二、全面审计结论（算法链）

1. **无悬挂调用**：全仓 231 个 `.py/.yml/.sh/.bat` 扫 `run_strong_breakout` → **0 处**（删除 `def run_strong_breakout` 后无调用点，不会 NameError）。
2. **workflow 无断裂**：30 个 workflow 逐个扫 `strong_breakout` → 唯一命中在 `v8_algo_intraday_lite.yml` L60 **注释**里，说的是 **`gen_strong_breakout`（已保留的水源脚本）**，非已删模块。
3. **保留项确认**（防误删）：`scripts/gen_strong_breakout.py` 仍在链上（`update_v8.py` experiment 段 L1366-1403 调用，产出 `STOCK_MOMENTUM_STATE/V2` + `raw_data/strong_breakout_history.json`，P0-2 监控项）；`algorithms/factor_lab_backtest.py` 取数层已内联（`_fetch_hist`），不再 import 已删模块。
4. **残留分类（实测）**：`index.html` `STRONG_BREAKOUT`=0、`strong_breakout`=0；`logic.html` 仅在**已下架/生命周期卡关系**等条目里以「已删除」留痕出现（合规）；`.py` 里的残留均为**注释**，本轮已全部纠偏。
5. **发现并说明的一处「非问题」**：`scripts/algo_backtest_compare.py` 仍在 E 批链上，名字叫 `collect_strong_breakout()` 但**读的是保留的 `data/STOCK_MOMENTUM_STATE_V2.js`**，不涉已删文件 → 属历史命名，本轮在注释里写明，不改行为。
6. **一处不动（按铁律）**：`logic.html` 内含 `renderUnlisted` + 「🚀 强势突破」tab 的**旧渲染器副本**，但本页只有 `sec-lg/obs/bt` 三节、无 `sec-ul`，该分支**无生效路径**（死代码）⇒ 按「判过时按有无生效路径、合法兜底严禁改」**未动**，避免误伤 83 个 script 的页面。

### 三、改后三件套执行结果

1. **时窗矩阵对齐**：离线复现 `align_logic_ops.py`（仓库外搭 ROOT：`align_logic_ops.py` + `index.html` + `logic.html` + `.github/workflows/*.yml`，全部取自远端真值）→ **✅ EXIT 0**：实际有 cron 的 workflow **21 个**，MUST 名单 21 / ALLOW 3，**全部已文档化、无过期引用、无死登记**。
2. **`index.html#sec-op` ↔ `logic.html#sec-lg` 对齐**：按上述门禁同源校验（文档源字符数 `index.html#sec-op` 613,793 / `logic.html#sec-lg` 527,470 已打印，非「失明」态）。
3. **交接**：本件。

### 四、防覆盖核验（重点）

- **并发编辑未被覆盖**：本机推送后，另一台机又推了 `9b03676596`（移除「④ 待确认剔除」折叠卡）、`dec0802caa`（强势跟踪列对齐）等 index.html 改动 —— 复读远端 blob：折叠标记 `▸ 展开 · 第一步明细`=1、`▸ 展开 · ① 算法链因子台账`=1、紫徽章×2 **全部在位**，**未被洗掉**。
- **做法**：全部改动均**先重取远端基线**→ 在基线上打锚点补丁（每处断言命中 =1，命中≠1 直接拒改）→ `base_tree` 只替换登记路径 → `force:false` 快进；**从未**在坚果云仓内 `git add/commit`，也**未 force**。
- **写入方盘点**：写 `index.html` 的自动任务（build bot / cache-buster）只改 `?v=` 缓存戳与构建产物，不改卡片结构；本轮结构改动已过 `new Function` 语法门禁（index 28 段 + logic 若干段，0 错）。

### 五、逻辑详解页变化（方法/规则，非流水账）

`logic.html` → 「🧮因子计分计算方法规则（真源：generate_top10.py → final_recommend.py）」专章新增**第八章**：

- **三步链条表**：第一步「每日因子计算」（算什么/subject：全因子加总 `raw_total`；**静态**，唯 P5 两因子随 `factor_walkforward.json` 的 `verdict` 启停）/ 第二步「已纳入因子」（台账 + 回测达标徽章读 `FACTOR_LAB_BACKTEST.abnormal_volume.verdict_3star`，**徽章随产物翻转、不写死**）/ 第三步「当日因子计分榜」（`TOP10_DAILY.top20` 按 `total_score` 降序，顶部=最终推荐；**每日盘后重算**）。
- **折叠规则**：哪两块默认折叠、哪些行常显、主展示看哪张、配色约定（蓝/紫/琥珀 + 红=加分 绿=扣分）。
- **删除留痕**：强势突破卡全站删除 + 水源 `gen_strong_breakout.py` 保留的理由（防复活）。

### 六、请小九侧复核/动作

1. 若你那边 `logic.html` 有未推的改动：请以远端为基重打，**勿整文件覆盖**（本机刚加了第八章）。
2. 「强势跟踪」页如有并列卡布局改动，与本轮无关；但**勿把已删的「强势突破」重新引入**（数据层已无 `data/STRONG_BREAKOUT*.js`）。
3. `raw_data/strong_breakout_history.json` 仍在滚动写入但**已无回测消费方** —— 是否停止滚动/缩短保留期，待主人拍板，**未擅自停**。

### 七、遗留（未做，避免踩踏）

- 缠论买点账本处于**累积期**（55 条，样本日后 ≤2 根），**不登记 `__STAR_PROFILE_OF`、不上前端卡、不评星**，待 ≥30 天再回测。
- `v8_cn_fetch_cloud.yml` 是否加 `raw_data/` 白名单（历史待办），本轮未动。
