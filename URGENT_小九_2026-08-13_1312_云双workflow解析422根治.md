# 🔴 URGENT 交接 · 第181轮 · 2026-08-13 13:12-13:25（云双 workflow 解析 422 根治）

> 自动化：小九-v8每小时紧急交接通道(看门狗+自动修复) `automation-1785835481750`
> 执行机：小九（E:/workspace/quant-scanner-v8）· 时区 CST
> 触发：每小时 7-21 槽位；本时段 13:12 处于 07:00-22:00 活跃发信窗口（非静音）

## 一、看门狗六管线结果

| 管线 | 状态 | 说明 |
|------|------|------|
| runner | ✅ OK | lemoncat-cn online / idle，无需唤醒 |
| cn_fetch | ✅ OK | 最近成功 @ 08-13 11:29（age 1.7h，**注意：这是修复前最后一次成功**） |
| build_deploy | ✅ OK | @ 08-13 11:38（age 1.6h） |
| raw_data_fresh | ✅ OK | 最后提交 12:19（age 53m < 阈值 1.5h） |
| site | ✅ OK | HTTP 200 |
| auto_dispatch | 🔴 FAIL | **全部派发失败**：HTTP 422 `failed to parse workflow: (Line: 60, Col: 7): Unexpected value 'fallback-to-github-hosted-runner'` |

- 健康检查 v8_health_check.py：rc=2，overall=**fail**（ok32 / warn4 / fail9）。9 FAIL 主要为盘中卡陈旧（已被本轮派发触发刷新），4 WARN 为 site_sync / 弱市三池 0 只内容审计。
- 告警邮件：**1 封**（`【v8看门狗告警】1项异常 @ 2026-08-13 13:12:29`），内容即 auto_dispatch 派发失败。该邮件合理（活跃窗口 + 真实派发故障），非静音抑制。

## 二、🔴 根因（用实时说话，已定位+已验证）

**根因**：commit `1fe4f8c7`（2026-08-13 **11:47:15** 推送）给两个云端 workflow 的 `runs-on` 加了
`fallback-to-github-hosted-runner: true`：

- `.github/workflows/v8_cn_fetch_cloud.yml:60`
- `.github/workflows/v8_algo_cloud.yml:52`

该 key **仅对 larger / auto-scaling runner 有效**，标准 self-hosted runner（本仓用的 lemoncat-cn）
触发 GitHub 工作流解析 **422** → 自 11:47 起，**两个云 workflow 都无法被 schedule 或 dispatch 触发**。

**连锁影响（已确认）**：
1. 本轮机 `auto_dispatch` 派发 cn_fetch → 422（看门狗捕获并报邮件）。
2. **候选池 173–180 轮连续陈旧的根因也在此**：看门狗健康检 self_heal 派发的 `algo_run`（v8_algo_cloud.yml）同样 422，candidate.json 自 08-12 20:12 起从未被云端刷新——此前误判为「算法源 guanlan/maharo 空/失败」，实为派发链路被 422 掐断。

## 三、🟢 一劳永逸式修复（已落根因+已推仓+已实时验证）

1. **代码层修**：删除两文件中的 `fallback-to-github-hosted-runner: true`，`runs-on` 恢复
   `{ group: self-hosted, labels: [cn] }`（即 08-13 原定的「优先 cn 中国 IP」意图）。
   同步更新 v8_cn_fetch_cloud.yml 头部注释，标注该 key 非法原因，避免复发。
2. **校验**：PyYAML 解析全部 18 个 workflow 通过（无其他非法 key）。
3. **commit + push**：`be463837` 已推 `origin/main`（仅含 2 个 workflow 文件，未碰红线 index.html / raw_data / HEALTH_CHECK.js）。
4. **实时验证（用实时说话）**：通过 GitHub API 直接 `workflow_dispatch` 触发——
   - cn_fetch (id 327687211)：**HTTP 204**（派发成功，无 422）✅
   - algo_cloud (id 324119592)：**HTTP 204**（派发成功，无 422）✅
   → 两条云流水线已重新可触发，盘中数据刷新与候选池 algo_run 自愈链路恢复。

## 四、跨轮遗留更新

- ① git 分叉阻塞(r176) ✅ 已解除（r177 起 HEAD==origin）。
- ② **候选池 173–180 连续陈旧(r173-180)🔴→🟢 根因收敛**：本轮回填为「v8_algo_cloud.yml 被 422 掐断派发」，非算法源问题；修复后已重新派发，下轮(182)验证 candidate.json 是否刷新到今日。
- ③ final_market_distribution 港股集中(r168-177) ✅ 已解除。
- ④ r134 except-pass 建议 ⚠️ 仍未落地。
- ⑤ stash 残留（历史累积，未清理）。
- ⑥ 云端高频小更新致 local_sync 瞬态落后（良性，自愈跳过）。
- 工作树脏项：index.html（红线，未碰）、raw_data/*.json（未纳入，非根因修复）、data/HEALTH_CHECK.js（看门狗输出，未纳入）。

## 五、下轮(182)跟踪点

1. cn_fetch 11:29 之后是否出现新成功运行（本轮 13:xx 已派发在途）。
2. **候选池 candidate.json 是否刷新到 08-13**（验证 422 根因收敛，最关键）。
3. 三池 0 只开盘后是否填充（弱市内容审计非故障）。

---
_本文件由每小时看门狗自动化生成，已 force-add 入库（根目录 URGENT_*.md 被 .gitignore:46 忽略需 -f）。_
