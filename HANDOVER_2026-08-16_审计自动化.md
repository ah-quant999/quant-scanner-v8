# HANDOVER — 2026-08-16（周日）审计体系建立 + 主站全面对齐

> 作者：本机（周日·阿狸咪班次）｜ 交接对象：小九（周一·工作日接棒）
> 主题：① 新增「双层审计」体系（每晚轻量 + 周末全量）② 主站缓存戳全面对齐修复 ③ 8 个自动化 model=NULL 待主人手动确认

## 一、本次做了什么（重要！）
1. **主站全面对齐（已上线）**：审计发现线上主站存在缓存戳真失配，已修复并部署：
   - **v6_memo.html 缓存戳三方不一致**（index 写 1882a2d3d3 / CDN 实际 8c2b52e345 / 本地护栏算 7475c1d75c）。
     根因：本地 Windows 工作树 v6_memo.html 是 CRLF，护栏按 CRLF 原始字节算 sha（7475c1d75c），
     而 git/CDN 存 LF（8c2b52e345）→ 本地算的 ?v 永远 ≠ 线上。已把本地 v6_memo.html 归一化为 LF，
     统一为 **8c2b52e345**，index.html 的 v6 ?v 同步改为 8c2b52e345，线上已生效。
   - **11+1 项 data ?v 失配**（STOCK_RPS/FOUR_VOLUME/ALGO_TRACK/STOCK_MOMENTUM_STATE(V2) 等）：
     远端 main 的 data 已被更新但 index.html ?v 未同步；且 reconcile 基于「过时的本地 origin/main 缓存」
     （cn git 墙 fetch 失败）取 blob 算旧 sha，对不齐 CDN。修复改用 **CDN(github.io) 真实内容为权威**，
     新建 `scripts/fix_cachebusters_cdn.py` 全量重算 ?v，78 处全部对齐，index.html 已推送 main
     （commit 8b4444fd0）+ Pages 构建（e573aa0b）。
   - **终验**：`scripts/audit_weekly.py` 实跑 ?v 失配 = 0、全链路一致、受保护文件通过。
2. **新增双层审计自动化**（本机 WorkBuddy，model=deepseek-v4-flash）：
   - **每晚轻量审计 23:30**：`scripts/audit_nightly.py` — 部署一致性 + 数据完整性 + 备份可用性。
   - **周末全量审计 周日 22:00**：`scripts/audit_weekly.py` — ?v 真失配 + 5 组件全链路 + 双机 model + 受保护文件。
3. **审计脚本已用 Contents API 直推 main**：`scripts/audit_nightly.py`、`scripts/audit_weekly.py`、
   `scripts/fix_cachebusters_cdn.py`、`scripts/deploy_blobs.py`（deploy_blobs 的 commit msg 已改为通用）。

## 二、审计体系怎么用（双机分工）
| 任务 | 时段 | 执行者 | 脚本 | 发现 ?v 失配时 |
|---|---|---|---|---|
| 每晚轻量 | 23:30 每日 | 小九（工作日）/ 阿狸咪（周末节假日） | audit_nightly.py | 先跑 fix_cachebusters_cdn.py 对齐，再 deploy_blobs.py 推 index.html |
| 周末全量 | 周日 22:00 | 阿狸咪（周日） | audit_weekly.py | 同上 |

- **铁律：`fix_cachebusters_cdn.py`（CDN 权威）优先于 reconcile**——cn git 墙下 reconcile 会取过时 origin/main
  缓存算旧 sha。fix 脚本直接下载 github.io 真实内容，保证 ?v == 浏览器实际拿到的文件。
- 推 index.html 用 `scripts/deploy_blobs.py`（Git Blobs API，>1MB 安全 + 自动触发 Pages 构建）。

## 三、当前主站状态（已确认）
- Pages 最新构建：e573aa0b（2026-08-16 21:47）
- ?v 失配：0；v6_memo ?v=8c2b52e345（LF 版）；index.html 三方一致（本地 CRLF == 线上 LF 实质相同）。
- 本地 v6_memo.html 已 LF 归一化（与 origin/main 一致，git status 不再因换行符报 M 内容差异）。

## 四、遗留事项（周一务必处理/盯住）
1. **⚠️ 8 个双机自动化 model=NULL（长期遗留，需主人在 WorkBuddy UI 手动确认 deepseek-v4-flash）**：
   - automation-1782763186274 阿狸咪-自动读取交接(10:00)
   - automation-1782763195471 阿狸咪-自动读取交接(14:30)
   - automation-1782763209713 阿狸咪-自动读取交接(18:30回家)
   - automation-1783744306577 九宝量化-周六T+1兜底
   - automation-1783744306764 九宝量化-周末轻量维护(SA/SU 19:30)
   - automation-1784448887937 数据新鲜度自动值守
   - 1784465134207 九宝量化-周六T+1新鲜度巡检
   - automation-1784816785584 阿狸咪-小九心跳监控+自动接管(每30分)
   （工具无法落库 model_id，禁止直改 workbuddy.db——报告主人，在 UI「自动化」里逐个确认。）
2. **本地工作树**：index.html 是 CRLF（无 eol=lf 规则），**勿整文件 commit**（会全行 diff 污染历史）；
   本地 HEAD(9a2c1c090) 落后 origin/main(e573aa0b)——远端已包含全部 v6/缓存戳修复，无需追平，保持现状即可。
3. **scripts/ 部分工具从未 commit**：wait_verify_build.py / wait_verify_reconcile.py / realign_*.py /
   api_push_src.py 等仍仅本地（本次未推）。如需双机可用，用 Contents API 直推。
4. **周一开盘**：算法链 18:30 交易日历 gate 放行后验证全量推送（尤其最终推荐卡 08-11 陈旧、依赖
   scan_result.json 的驾驶舱分层推荐）。周一 23:30 首次跑每晚轻量审计，看是否报异常。

## 五、提醒
- 审计脚本（audit_nightly/audit_weekly/fix_cachebusters_cdn）与 v6 三件套一样属维护必需，勿删。
- 发现「算法输出 全港股/A股=0 且总数>0」= 上游 bug 立即报警（非弱市），别当弱市放过。
- 非交易日 stocks=0 属正常，不得误报黄灯（v8_health_check 已处理）。
