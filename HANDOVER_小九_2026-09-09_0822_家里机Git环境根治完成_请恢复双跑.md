# HANDOVER · 阿狸咪 → 小九（2026-09-09 08:22 · 家里机 Git 环境根治完成 · 请恢复双跑）

> 收件：小九 · 主人令「9 点半开盘前完成家里机 Git 环境根治」已落地，请按第六节执行恢复双跑。

---

## 一、结论
✅ **家里机(alimi-cn) Git 环境根治完成**，runner 新进程可解析 git/bash，可恢复双跑。
- 修通时间：2026-09-09 08:19:39（脚本耗时 56 秒）
- 距 9:30 开盘 71 分钟完成，距主人 deadline 提前 68 分钟

## 二、安装结果（实测，文件内时间戳为准）
| 项 | 状态 |
|---|---|
| 装包来源 | 主人下载到桌面的 `Git-2.55.0.5-64-bit.exe` (62.3MB, 08:10) |
| 安装目录 | **E:\Git**（主人指示装 E 盘，已遵守） |
| 关键入口 | `E:\Git\cmd\git.exe` ✅ / `E:\Git\bin\bash.exe` ✅ |
| git 版本 | `git version 2.55.0.windows.5` |

## 三、PATH（实测取 Machine + User 两级）
| 范围 | 写入 | 位置 |
|---|---|---|
| Machine PATH | `E:\Git\cmd` | 第 6 项（系统默认顺序） |
| User PATH | `E:\Git\bin` + `E:\Git\cmd` | **最前 2 项**（脚本"插最前"逻辑仅对 User 生效，Machine 因系统级 PATH 顺序被系统保护无法前置；但 git.exe 在 cmd 下被正确解析） |
| 当前会话 | $env:Path 仍空 | **预期**：旧进程不重读 PATH，新进程会读到 |

## 四、Runner 重启（实测）
- 计划任务 `v8_cn_runner_guard` State=Running（LastRun 09-09 08:19:59）
- 旧 Runner.Listener pid=11732 已结束
- 新 Runner.Listener **pid=12648**，启动 09-09 08:19:59（与脚本 [4/5] 步骤时间一致）

## 五、验证
- ✅ 全新 spawn 进程 `cmd /c git --version` → `git version 2.55.0.windows.5`
- ✅ 全新 spawn 进程 `E:\Git\bin\bash.exe -c "git --version"` → `git version 2.55.0.windows.5`
- ✅ 全新 spawn 进程 `E:\Git\bin\bash.exe -c "echo in_bash_ok"` → 成功（bash 可执行）
- ✅ Runner.Listener 在新进程里能看见新 PATH（计划任务触发新进程，继承更新后的环境变量）

## 六、请求小九执行

**把两条算法链 workflow 的 `runs-on` 从 `[self-hosted, cn, xiaojiu]` 改回 `[self-hosted, cn]`，恢复双跑。**

涉及文件（用 `v8_gh_token.txt` 带 workflow scope 走 Git Database API）：
- `.github/workflows/v8_algo_cloud.yml`（L91 附近）
- `.github/workflows/v8_algo_run.yml`（L128 附近）

需同步移除 commit `1b6dcc909` / `5dc01c980` 中 "根治注释" 那行（避免误导未来读者：runner 现在已能解析 bash）。

## 七、未拍板项（待主人，不占你时间）
- `automation-1788058041833` 留你侧自动化清单里 8 个 ID 之一。**阿狸咪侧 ID `60e1dbfd`「因子实验室数据刷新(周六 10:00)」与小九 E 档每晚 21:05 factor_lab_gen 同产物双跑双推风险**——主人未拍板，未操作。

## 八、文件位置
- 脚本：`backup/silent_install_git_e_admin.ps1`（BOM+CRLF、语法 0 错）
- 桌面入口：`v8_静默装Git到E盘_管理员运行.bat`（已验证成功）
- 日志：`D:\actions\cn-runner\_diag\git_env_fix_v2.log`

---

*本报告仅供研究参考，不构成投资建议。*
