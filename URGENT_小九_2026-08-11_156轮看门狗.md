# URGENT 小九 → 阿狸咪｜第 156 轮看门狗（2026-08-11 09:18-09:25）

## 一句话
上轮 NT_DATA 自愈映射修复**已验证生效**（00:14 → 09:09 刷新）；本轮抓到并根治看门狗一处**长期误报**：`in_progress`（workflow 正在运行）被当成失败判 FAIL 并发告警邮件。

## 一、看门狗六管线

初跑 @09:18:59（EXIT 0，**修复前**）：

| 项 | 状态 | 说明 |
|---|---|---|
| runner lemoncat-cn | ✅ | online=True / busy=False，无需 heal |
| cn_fetch | ❌ | `in_progress/None @ 09:14 (age 4m)` ← **误报，见第二节** |
| build_deploy | ✅ | 09:16 success（2m） |
| raw_data 新鲜度 | ✅ | 09:16（2m，阈值 1.5h） |
| site | ✅ | HTTP 200 |
| auto_dispatch | ✅ | 数据新鲜（raw_data 0.0h / cn_fetch 0.0h），无需派发 |

复跑 @09:23:23（**修复后**）：**六管线全绿 EXIT 0**，cn_fetch 正常识别为 `completed/success @ 09:14 (age 9m)`。

告警邮件本轮发出 **1 封**（09:20:04，标题「1项异常」）—— 系上述误报所致，**非真实故障**，请阿狸咪忽略。

## 二、本轮根因级修复：in_progress 误报（一劳永逸）

### 现象
巡检恰好撞上 cn_fetch 正在运行（启动仅 4 分钟）即判 FAIL 并发告警邮件。

### 根因（`v8_cloud_watchdog.py`，两处联动缺陷）
1. `latest_workflow_run()` 第 119 轮加的 skip_neutral 循环判据是 `conclusion not in NEUTRAL`。运行中的 run 其 `conclusion` 为 **None**，None 不在 NEUTRAL 元组里 → **被当成「最新一条有结论的 run」直接返回**，把真正已成功的上一条 run 屏蔽掉。
2. `check_workflow()` 随后以 `ok = status == "completed" and con == "success"` 判定 → `in_progress/None` 必然 FAIL。

交易时段 cn_fetch 高频 cron（08:25-15:30）下，每小时巡检撞上运行中的概率很高，属**长期存在的系统性误报**，会持续用假警报稀释真信号。

### 修复
1. `latest_workflow_run()` 改为返回三元组 `(已完成的最近 run, err, 运行中的 run)`；循环内 `status != "completed"` 的 run 单独摘出到 `running`，**不参与成败判定**。
2. `check_workflow()` 新增运行中分支 + 卡死保护常量 `RUNNING_GRACE_MIN = 45`：
   - 有运行中实例且 ≤45 分钟 → **OK**，输出 `运行中(in_progress) @ ... (已 Xm)；上轮 success @ ...`
   - 有运行中实例且 >45 分钟 → **FAIL**「疑似卡死」（v8 各 workflow 正常 2-15 分钟完成）
   - 无运行中实例 → 维持原逻辑（成败 + `max_age_min` 新鲜度）

### 验证（构造 6 场景全通过）
| 场景 | 判定 | 期望 |
|---|---|---|
| 运行中 4 分钟（上轮 success） | OK | OK ✅ |
| 运行中 90 分钟（超 45min 宽限） | FAIL 疑似卡死 | FAIL ✅ |
| 无运行中 + 上轮 failure | FAIL | FAIL ✅ |
| 无运行中 + 上轮 success 新鲜 | OK | OK ✅ |
| 无运行中 + success 但超 age 阈值 | FAIL | FAIL ✅ |
| queued 2 分钟 | OK | OK ✅ |

真实复跑 @09:23 亦确认六管线全绿。

## 三、健康检查

- 初跑 @09:20:15：**ok33 / warn7 / fail2**
  - FAIL①「今日事件/市场提示」停留 00:14 → **本地滞后造成的假阳性**：远端 `data/NT_DATA.js` 实际已是 **09:09**（上轮 algo_run 修复已生效 ✅），本地 HEAD 落后 origin 2 提交未取到。
  - FAIL②「本地与 origin/main 同步」→ 同一根因。
- ff 对齐后复跑 @09:24:33：**ok34 / warn8 / fail0 全清** ✅

### 8 项 WARN = 盘前正常态
实时数据 8 源（全球指数/ETF 盘中异动/ETF 资金热度/ETF 日监控/板块资金流向/概念排名/涨停热度/市场预警）09:21 已更新但关键字段空值 —— **09:30 集合竞价前盘中接口本无数据**，开盘后由 intraday fetch 填充，无需处置。

## 四、上轮遗留验证结果

| 上轮遗留 | 本轮结果 |
|---|---|
| algo_run 派发后 NT_DATA 是否刷新 | ✅ **已刷新**（`raw_data/nt_data.json` update_time 09:09，随 09:16 cn fetch 提交入库；前端 `data/NT_DATA.js` 远端同为 09:09） |

## 五、git
- 开局本地落后 origin 2 提交 → stash 保护脚本改动 → `merge --ff-only` 对齐 `f7b1aafe`（09:22 build）→ pop 时 `data/HEALTH_CHECK.js`（产物）冲突，按惯例取云端版，脚本修复完整保留并通过 `py_compile`。
- 本轮提交仅含：`v8_cloud_watchdog.py`（根因修复）+ 本 URGENT + `HANDOVER_LOG.jsonl` + `data/HEALTH_CHECK.js` 快照。
- `index.html` / `HANDOVER_*.md` **未碰**；`backup/stage_0811_0826/`、`data/*.bak_pre_20260811_*` 为他方备份产物，按铁律**保持原样不提交不删**。

## 六、遗留
1. 云端高频小更新循环延续（分钟级连推），本轮 ff 期间又推进 2 个 build。
2. 第 134 轮 except-pass 改日志建议未落地。
3. stash 残留 20+ 条占位无实（本轮已 drop 掉自建的 1 条）。
4. 上轮建议仍有效：核对 `algorithms/run_algorithms.py` 脚本清单 vs `cloud_fetch_v8.py` 注册表，凡只在前者出现的源都应补 `heal_cat`，防止同类自愈空转。
