--- index.html	2026-08-13 08:36:34.115523100 +0800
+++ index.html.TMP_UI_EDIT	2026-08-13 08:39:03.989827800 +0800
@@ -1123,6 +1123,7 @@
 <script src="data/OPTIMIZED_STRATEGY.js?v=20260813082520"></script>
 <script src="data/EXPERIMENT.js?v=20260813082517"></script>
 <script src="data/FINAL_RECOMMEND_DATA.js?v=20260813082515"></script>
+<script src="data/TOP3_TRACK.js?v=20260813082900"></script>
 <script src="data/ETF_PULSE.js?v=20260813082517"></script>
 <script src="data/ETF_DAILY_MONITOR.js?v=20260813082517"></script>
 <script src="data/TOP10_DAILY.js?v=20260813082521"></script>
@@ -11767,14 +11768,19 @@
       h+='</div>';
     }
 
-    // 止损 / 目标 / 盈亏比
-    h+='<div class="fr-row">';
-    h+='<span><span class="lab">🛑 止损</span><span class="sl">¥'+_fn(s.stop_loss)+' -'+(s.stop_loss&&s.close?((1-s.stop_loss/s.close)*100).toFixed(1):'--')+'%</span></span>';
-    h+='<span><span class="lab">🎯 目标</span><span class="tg">¥'+_fn(s.target_price)+' +'+(s.target_price&&s.close?((s.target_price/s.close-1)*100).toFixed(1):'--')+'%</span></span>';
-    h+='<span><span class="lab">⚖️ 盈亏比</span><span class="rr">1:'+_fn(s.risk_reward,2)+'</span></span>';
-    h+='<span><span class="lab">现价</span><span style="font-weight:700">¥'+_fn(s.close)+'</span></span>';
-    h+='<span><span class="lab">涨跌</span><span style="color:'+pcColor+';font-weight:700">'+(pc>0?'+':'')+_fn(pc)+'%</span></span>';
-    h+='</div>';
+    // 止损 / 目标 / 盈亏比 / 现价（一行紧凑显示，2026-08-13）
+    (function(){
+      var dropPct = (s.stop_loss && s.close) ? ((1-s.stop_loss/s.close)*100).toFixed(1) : '--';
+      var risePct = (s.target_price && s.close) ? ((s.target_price/s.close-1)*100).toFixed(1) : '--';
+      var rr      = _fn(s.risk_reward,2);
+      var pcNow   = (pc>0?'+':'')+_fn(pc)+'%';
+      h+='<div class="fr-row" style="gap:8px;font-size:12.5px">';
+      h+='<span>🛑¥'+_fn(s.stop_loss)+'<span style="font-size:10px;color:var(--down);margin-left:2px">-'+dropPct+'%</span></span>';
+      h+='<span>🎯¥'+_fn(s.target_price)+'<span style="font-size:10px;color:var(--up);margin-left:2px">+'+risePct+'%</span></span>';
+      h+='<span>⚖️1:'+rr+'</span>';
+      h+='<span>💹¥'+_fn(s.close)+'<span style="font-size:10px;color:'+pcColor+';margin-left:2px">'+pcNow+'</span></span>';
+      h+='</div>';
+    })();
 
     // 支撑/压力/ATR
     h+='<div class="fr-extra">';
@@ -11789,20 +11795,26 @@
     var bt = s.backtest || {};
     var tr = s.tracking || {};
     h+='<div class="fr-bt">';
-    h+='<div class="fr-bt-title">📈 回测 & 跟踪</div>';
-    h+='<div class="fr-bt-grid">';
-    h+='<div class="fr-bt-cell"><div class="k">历史信号</div><div class="v">'+(bt.signals||0)+'</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">胜率</div><div class="v" style="color:'+(bt.win_rate>=50?'var(--up)':'var(--gold)')+'">'+(bt.win_rate!=null?bt.win_rate.toFixed(1):'--')+'%</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">平均收益</div><div class="v" style="color:'+(bt.avg_return>0?'var(--up)':(bt.avg_return<0?'var(--down)':'var(--sub)'))+'">'+(bt.avg_return!=null?(bt.avg_return>0?'+':'')+bt.avg_return.toFixed(2)+'%':'--')+'</div></div>';
-    if(bt.best_return!=null) h+='<div class="fr-bt-cell"><div class="k">最佳</div><div class="v" style="color:var(--up)">+'+bt.best_return.toFixed(1)+'%</div></div>';
-    if(bt.worst_return!=null) h+='<div class="fr-bt-cell"><div class="k">最差</div><div class="v" style="color:var(--down)">'+bt.worst_return.toFixed(1)+'%</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">入选日</div><div class="v">'+_fe(tr.entry_date||'--')+'</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">入选价</div><div class="v">¥'+_fn(tr.entry_price)+'</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">现价/最新</div><div class="v">¥'+_fn(tr.latest_price||s.close)+'</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">跟踪盈亏</div><div class="v" style="color:'+(tr.return_pct>0?'var(--up)':(tr.return_pct<0?'var(--down)':'var(--sub)'))+'">'+(tr.return_pct!=null?(tr.return_pct>0?'+':'')+tr.return_pct.toFixed(2)+'%':'--')+'</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">持有天数</div><div class="v">'+_fe(tr.hold_days||1)+'天</div></div>';
-    h+='<div class="fr-bt-cell"><div class="k">状态</div><div class="v">'+_fe(tr.exit_type==='hold'?'持有中':(tr.exit_type==='stop'?'已止损':(tr.exit_type==='target'?'已止盈':(tr.exit_type||'--'))))+'</div></div>';
-    h+='</div>';
+    h+='<div class="fr-bt-title">📈 回测 & 跟踪（策略统计 + 本次入池跟踪）</div>';
+    // 2026-08-13：10 KPI 合并为 2 行，提高信息密度
+    (function(){
+      var winColor  = (bt.win_rate>=50?'var(--up)':'var(--gold)');
+      var avgColor  = (bt.avg_return>0?'var(--up)':(bt.avg_return<0?'var(--down)':'var(--sub)'));
+      var retColor  = (tr.return_pct>0?'var(--up)':(tr.return_pct<0?'var(--down)':'var(--sub)'));
+      var statusTxt = tr.exit_type==='hold'?'持有中':(tr.exit_type==='stop'?'已止损':(tr.exit_type==='target'?'已止盈':(tr.exit_type||'--')));
+      var holdTxt   = (tr.hold_days||1)+'天';
+      h+='<div class="fr-bt-grid" style="grid-template-columns:1fr;gap:3px">';
+      h+='<div style="display:flex;gap:10px;flex-wrap:wrap;font-size:12px">';
+      h+='<span>📊 <b>'+(bt.signals||0)+'</b> 信号 · 胜率 <b style="color:'+winColor+'">'+(bt.win_rate!=null?bt.win_rate.toFixed(1):'--')+'%</b> · 均收 <b style="color:'+avgColor+'">'+(bt.avg_return!=null?(bt.avg_return>0?'+':'')+bt.avg_return.toFixed(2)+'%':'--')+'</b></span>';
+      if(bt.best_return!=null) h+='<span>🔥 最佳 +'+(bt.best_return.toFixed(1))+'%</span>';
+      if(bt.worst_return!=null) h+='<span>❄️ 最差 '+(bt.worst_return.toFixed(1))+'%</span>';
+      h+='</div>';
+      h+='<div style="display:flex;gap:10px;flex-wrap:wrap;font-size:12px">';
+      h+='<span>🕒 入选 <b>'+_fe(tr.entry_date||'--')+'</b> 入价 ¥'+_fn(tr.entry_price)+' → 现 ¥'+_fn(tr.latest_price||s.close)+' <b style="color:'+retColor+'">'+(tr.return_pct!=null?(tr.return_pct>0?'+':'')+tr.return_pct.toFixed(2)+'%':'--')+'</b></span>';
+      h+='<span>⏳ 持 '+holdTxt+' · '+statusTxt+'</span>';
+      h+='</div>';
+      h+='</div>';
+    })();
     if(bt.note || tr.note){
       h+='<div class="fr-bt-note">'+(bt.note||'')+(bt.note&&tr.note?' · ':'')+(tr.note||'')+'</div>';
     }
