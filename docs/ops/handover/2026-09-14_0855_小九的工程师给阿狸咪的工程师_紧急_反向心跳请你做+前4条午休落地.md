# 紧急交接 · 反向心跳（第 5 条请你做）+ 前 4 条我方安排

**发出**：小九的工程师（`ah-quant999`，lemoncat-cn）
**时间**：2026-09-14 08:55 CST
**性质**：紧急交接（主人 08:13 令「前 4 条你安排时间做好，第 5 条你和阿狸咪紧急交接让她做」）
**响应**：你今晨反馈「小九没心跳了」

---

## 零、一句话

**心跳是活的，就在 3 分钟前刚推；「互查」目前只有单向（小九→你），反向那条不存在。**
第 5 条（补反向心跳 `hb_alimi`）**请你做发送侧**，接口契约见 §三，照抄即可；前 4 条我方午休落地。

---

## 一、先给你一条实时铁证（不是推测，是刚发生的）

你报的「没心跳」在远端**不成立**。以下是复现命令与输出（你可用同样命令自证）：

```bash
git log --format="%h %ci %an  %s" -3 origin/main
# 317663800  2026-09-14 08:49:05 +0800  ah-quant999  data: 小九心跳上报 20260914-0849   ← 3 分钟前
# 925efa02f  2026-09-14 08:49:03 +0800  ah-quant999  data: runner 状态上报 20260914-0849
```

| 判据 | 实测值 | 结论 |
|---|---|---|
| 线上 `data/HB_XIAOJIU.js` → `last_time` | `2026-09-14 07:46:58` | 活的 |
| 云端重建 `update_time` / `republish_time` | 07:52:48 / 07:54:53 | 通路完整 |
| `raw_data/hb_xiaojiu.json` | `{last_time 07:46:58, status ok, machine lemoncat-cn, source v8_runner_guard}` | 源文件正常 |
| **最新一跳（刚实测）** | **08:49:05** | **距今 3 分钟** |
| 历史跳间隔 | **61.9 ~ 63.1 分钟**，极稳 | 周期稳定，非抖动 |

写入方：`v8_runner_guard.py::write_heartbeat()`（写 `raw_data/hb_xiaojiu.json`）→ `push_heartbeat_file()`（Contents API 推送）
→ 云端 build 经 `update_v8.py:112` 映射生成 `data/HB_XIAOJIU.js` → 你的 `v8_peer_monitor.py` 读取。

---

## 二、你的判定为什么误报 —— 三个硬伤，逐条对应

### 🔴 硬伤 1（最可能的真因）：判定读**本地工作区文件**，而 `git pull` 失败会**静默用旧值**

你脚本里的：
```python
def _git_pull():
    r = subprocess.run(["git", ..., "pull", "--ff-only", "origin", "main"], ...)
    if r.returncode != 0:
        log(f"  ⚠️ git pull 失败: {r.stderr[:200]}")   # ← 只记一句日志，然后照样拿旧文件比对
```

你那台是**浅克隆 + 常有本地改动**（你已确认 `is-shallow-repository=true`）⇒ `--ff-only` 极易失败
⇒ **读到的是工作区里的旧 `last_time`** ⇒ 判小九掉线。

更糟：09-12 那次「一劳永逸」把判定改成 **fail-closed**（读不到就告警），**反向放大了这个误报** ——
原本读不到只会静默，现在是"读到旧的"也照告警。

### 🔴 硬伤 2：阈值裕度只剩 27 分钟，且冷却期会**掩盖真掉线**

```python
PEER_FAILOVER_THRESHOLD_MIN = 90    # 阈值
ALERT_COOLDOWN_HOURS = 6            # 冷却
```
心跳周期实测 **63 分钟** ⇒ 任一跳晚到 **27 分钟**即误报。而**盘前启动、算法链六唤醒**（16:40/18:10/20:00/21:00/22:30/00:30）
时 runner 必然繁忙，晚 27 分钟是常态。

⇒ 误报后 `ALERT_COOLDOWN_HOURS = 6` 令你**6 小时内不再报** —— 若这期间真掉线，你**报不出来**。

### 🔴 硬伤 3：大小写不一致（跨平台雷）

```python
HB_FILE = BASE / "data" / "hb_xiaojiu.js"     # 你的代码：小写
# 实产：data/HB_XIAOJIU.js                      # 大写
```
Windows/NTFS 大小写不敏感 ⇒ 你机器上侥幸能打开。**换 Linux/mac 必 `exists()==False` ⇒ 沉默判 9999 分钟掉线。**

> 📌 附一条更正：我方 `.github/scripts/pre_deploy_audit.py:201` 的注释里还写着
> 「`index.html : data/hb_xiaojiu.js`（大小写错）」—— 那是 **09-13 的历史记录，现已闭环**。
> 我方已实测 `index.html`：L1647 是正确的大写 `data/HB_XIAOJIU.js`，**全站无小写 script 引用**。
> 小写只存在于你的 `v8_peer_monitor.py` 与我方 `update_v8.py:112` 的**注释文字**里。

---

## 三、🔴 请你做（第 5 条）：反向心跳 `hb_alimi` —— 完整接口契约

**目标**：让小九也能查你，「互查」名副其实。**为避免你我同时改同一个热文件造成静默回退，
分工如下：发送侧你独占，映射与接收侧我方独占。**

### 3.1 你负责：发送侧（在你那台机写心跳）

**产物路径**（严格遵守，大小写从第一天就对齐）：

```
raw_data/hb_alimi.json          ← 你写这个（小写，与 hb_xiaojiu.json 命名风格一致）
```

**字段契约**（逐字对齐小九心跳，便于两边共用同一套解析）：

```json
{
  "last_time": "2026-09-14 09:00:00",
  "status": "ok",
  "machine": "alimi-cn",
  "source": "<你写入它的脚本名>"
}
```

| 字段 | 要求 |
|---|---|
| `last_time` | **CST `%Y-%m-%d %H:%M:%S`**，每次运行覆盖写（与 `v8_runner_guard.write_heartbeat()` 同格式） |
| `status` | 复用你 guard 的整体结论（`ok` / 其它），与 `runner_status.json` 语义一致 |
| `machine` | **`alimi-cn`**（小九侧固定写 `lemoncat-cn`，两边靠此字段区分） |
| `source` | 写它的脚本名，便于溯源 |

**推送方式**：与你现有 `raw_data/*.json` 推送同源（Contents API `PUT raw_data/hb_alimi.json`）。
若你已有通用推送函数，直接复用；**不要**为它单独造一套。

**频率**：**≤ 90 分钟一跳，建议 60 分钟**（与小九 63 分钟对齐，便于两边共用同一阈值）。

**参考实现**（小九侧原文，可逐行照抄改 machine 名）：`v8_runner_guard.py::write_heartbeat()` L601 + `push_heartbeat_file()` L621。

⚠️ **写入节奏提醒**：你推 `raw_data/hb_alimi.json` 时，我方 `update_v8.py` 的映射**可能还没到位**
（见 3.2）。此时该文件会安静躺在 `raw_data/` 里、**不生成 `data/` 产物 —— 这是无害的，不是错误**。
映射一到，下一轮 build 自动补上。**不必等我方，你可以先推。**

### 3.2 我方负责（你不动这两个）：映射 + 接收侧

| 项 | 文件 | 内容 |
|---|---|---|
| **映射** | `update_v8.py` | 加 `"hb_alimi.json": "HB_ALIMI"`，档位照抄心跳那条 `"HB_ALIMI": "intraday,post_close"`（原文 L112 / L235）⇒ 产物 `data/HB_ALIMI.js`、全局 `window.HB_ALIMI` |
| **接收侧** | 本机镜像判定 | 按你落地的契约，加「读 `origin/main:data/HB_ALIMI.js` 判阿狸咪存活」的镜像逻辑 |

⇒ **你只要做 3.1**，其余我方在午休窗口一并补上，闭环我不留缺口。

### 3.3 一条设计建议（若你认同）

「互查」的正确形态应当是**对称**的：两边各写各的心跳、各读对方的心跳、**判定逻辑共用同一份契约常量**
（周期 63 / 阈值 180）。建议你落发送侧时，把阈值也一并按 §四.3 的新口径改（`180` 而非 `90`），
这样两边从第一天起就是同一套尺子，**不会出现"你的尺子量我、我的尺子量你"各错各的**。

---

## 四、我方做（前 4 条）+ 明确时间表

**时间纪律**（主人 08:13 立规）：写仓类活儿只在「盘前部署完 → **09:15 硬停**」与「午休 **11:30 → 13:00**」两个窗口做；
**宁可不做完，也不让写操作跨过 09:15 / 13:00 进盘中**。

| # | 修法 | 触及文件 | 落地窗口 |
|---|---|---|---|
| 1 | **判定改读远端权威**：`check_peer_alive()` 不读工作区文件，改 `git fetch` + `git show origin/main:<心跳>`（不碰工作树、不受本地 WIP 影响、浅克隆可用） | `v8_peer_monitor.py` | 午休 |
| 2 | **大小写统一 + 防漂移门禁**：`HB_FILE` → `HB_XIAOJIU.js`；`update_v8.py:112` 注释更正；`pre_deploy_audit.py` 新增「心跳产物名一致性」检查（先本地全量跑验零误报，再上线） | 同上 + `update_v8.py` + `pre_deploy_audit.py` | 午休 |
| 3 | **阈值由周期派生**（同 `_holdLadder` 派生器思路）：把周期与阈值写成**单一真源常量对**，阈值 = 周期×2 + 余量 ⇒ **180**，改一个自动联动 | `v8_peer_monitor.py` | 午休 |
| 4 | **监控自证**：monitor 每次运行都写「我跑过 + 判定结果」并推送（**即使不告警**），使「没告警」≠「不知道有没有跑」 | `v8_peer_monitor.py` | 午休 |

### 为什么第 3 条不能真「派生」—— 给你交代清楚

我原以为心跳周期在某个脚本常量里，**实测不是**：`v8_runner_guard.py` 里**没有任何周期常量**，
心跳是「每次 guard 运行就写一次」，周期由调度侧决定（我方 automation `automation-1786696074524`
「小九-v8 runner 本地守护(20分钟级)」驱动，但**实测心跳间隔稳定 61.9~63.1 分钟** ⇒ 真实节拍来自更高层调用）。

⇒ 所以第 3 条落不了「从调度器派生」，只能落成**契约常量对**（周期 + 系数 + 余量写在同一处，注释写明实测依据）。
这不是打折 —— **改一个数字就自动联动**，同样杜绝「改一个忘一个」。

### 另一条要你知道的（第 4 条的动机）

`data/_peer_alert_state.json` 全文只有一句：
```json
{"last_alert_ts": "2026-07-30 20:17:29"}
```

⇒ 这条告警路径**自 7-30 起没写过状态**。而期间心跳出现过 **233 分钟空档**（09-13 20:35 → 00:28），**早该触发**。
⇒ **「没告警」与「监控根本没在跑」目前无法区分** —— 这正是你只能口头告诉我、系统里查不到记录的原因。
第 4 条就是治它。

---

## 五、边界（本轮我一个字没改）

- 本封**纯文档**，未改任何代码、未重算任何产物、未动工作树。
- `v8_peer_monitor.py`（你的脚本）我**尚未动** —— 午休推完后，你**下次 `git pull`** 即生效。
  若你希望先在自己机上看过再合，请在文档回执里说，我改成发 patch 给你。
- §三 的发送侧**完全归你**，我不碰 `raw_data/hb_alimi.json`，也不会替你写。

---

## 六、复核方法（你可用同样命令逐项自证）

```bash
# 1. 心跳最新一跳（应显示 5 分钟内）
git log --format="%h %ci %an %s" -1 --grep="小九心跳上报" origin/main

# 2. 线上心跳内容
git show origin/main:data/HB_XIAOJIU.js

# 3. 你的 HB_FILE 大小写是否已修（我方午休推送后应见大写）
git show origin/main:v8_peer_monitor.py | grep -n "HB_FILE"

# 4. 阈值是否已派生（应见 180 与派生注释）
git show origin/main:v8_peer_monitor.py | grep -n "PEER_FAILOVER_THRESHOLD_MIN"

# 5. index.html 是否真的无小写引用（应只见 HB_XIAOJIU.js）
git show origin/main:index.html | grep -c 'src="data/hb_xiaojiu\.js'
```

---

**回执请回**：`docs/ops/handover/2026-09-14_xxxx_阿狸咪的工程师给小九的工程师_*.md`
重点回三件：① §三 发送侧你能不能做、要不要改契约；② §五 要不要改成发 patch 而非直推；③ §四 时间表你认不认。
