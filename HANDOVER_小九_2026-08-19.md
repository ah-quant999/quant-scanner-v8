# HANDOVER 小九 → 阿狸咪（2026-08-19 周三 · 白班中段档）

> **更新于 2026-08-19 12:12 自动更新**（小九 12:00 档自动化 · 第 2 实例，已合并 12:03 首档全部内容并追加实核发现）
> 数据源：v8 侧 `.workbuddy/memory/2026-08-19.md`（34KB，08:00–11:32 当班全量）+ `git log origin/main` 补全 11:33–12:05 + 12:06–12:12 实核。
> stock-scanner 侧当日日志不存在（08-11 后断更），按惯例以 v8 侧为准。所有 SHA 均从 `git log origin/main` 实核，非记忆值。
> 当前 main = `f121d36f8`（12:05 risk gauge），本地已 ff 同步，零分叉。

---

## 一、背景

- 今日主人进入**高频拍板 + 一劳永逸根治**模式，全天围绕「盘中实时链路干净化、admin 页不再空白、H 反推可见可用、取消隐藏式分页」密集发令，共 10+ 轮截图反馈。
- **关键转折（工作流铁律变更）**：主人 08:32 / 08:59 两次明确**「改完就直接上线，不再逐项问我」** → 此后所有本机修复一律 `git add + pull --rebase --autostash + push` 走 Pages 自动部署。**你今晚接手请沿用此约定。**
- 系统 **07:54:40 重启**导致本机 runner 掉线（08:13–08:16 手动拉起恢复）；盘中多次「云端 cn fetch 抓东财 push2 瞬时超时/整轮跳过 → 盘中卡卡盘前」，小九本机中国 IP 兜底救急多次。
- 今日 main 累计 **140 个提交**（小九人工修复 19 项，其余云端例行）；落 **18 份 URGENT 留痕**。
- 阿狸咪当前 **offline**（白班小九独家 7:45–17:45），lemoncat-cn 在线充当云端兜底 runner。

---

## 二、今日已完成改动（真实 commit SHA + 影响）

### A. 小九侧（本机人工修复，全部已 push origin/main）

| # | 时间 | 主题 | 真实 SHA | 影响 |
|---|------|------|----------|------|
| 1 | 08:00 | self_heal_monitor 一劳永逸修复 | `a57555353` | momentum 陈旧降级 WARN 免告警 + --json 纯净；8 轮 FAIL 轰炸终结 |
| 2 | 08:15 | 盘前全量 14/14 + rebase 中断恢复 | `a03db1b0c` | 64js 刷新全部今日时间戳；detached HEAD 先恢复再 push |
| 3 | 08:32 | 「改完就上线」首轮 | `408eebadf`（本地 `fd5cfbecf`） | HEALTH_CHECK.js 状态刷新，最终 push 版 |
| 4 | 08:49 | 健康检测动量 FAIL→WARN | `be306fa05` / `504c5b764` | v8_health_check.py 4 处：邮件豁免白名单 + parse_time 兼容裸日期 + ts 兼容 generated_at |
| 5 | 08:59 | 遗留未提交文件全部上线 | `391768ef0`（本地 `cecbd0528` 已并入） | H_AUTO_BUY.js / h_auto_buy_20260819.json / scripts/* / v6_memory.html |
| 6 | 09:06 | 撤销 8/18 盘前门禁 | `51a6c986d` | 最终推荐盘前也展示（仅 6 行，风险主人自担） |
| 7 | 09:02–09:16 | H 反推短线买点一劳永逸 | `6e197627f` | track_h_auto_buy.py(220行) + run_algorithms ORDER + api_push_raw 三处注册 + index.html 卡 + bootstrap（5 步全通，告别「看不到」陷阱） |
| 8 | 09:17 | ETF 日监控「数据没了」根因 | `f2042e4cc` | renderEtfDaily 早退删除 + 动态 `_dataHint`；**真问题在 render 非缓存** |
| 9 | 09:32 | SECTOR_FUND_FLOW_INTRADAY 断档根治 | `1f3b77f68` / `7906c027d` | index.html:423 加载该卡（此前**从未被任何 script 标签加载**）+ 重发今日 data |
| 10 | 10:05 | 盘中分时快照补齐 | `a41d1b89d` / `0282a5b11` | 补 09:54 快照 → 曲线出图（需 ≥2 快照） |
| 11 | 10:26 | 盘中实时卡卡盘前根因 | `e5a6d6eed`（本地 `f3714e33f` 已丢弃） | 云端东财瞬时超时 14 分钟后自愈；本机提交冗余已 reset --hard 丢弃 |
| 12 | 10:30 | **三链路一劳永逸** | `ad76d807e` / `4a01c014f` | ① update_v8 DATA_SOURCES 补挂 `sector_fund_flow_intraday.json`→`SECTOR_FUND_FLOW_INTRADAY`+CATEGORY_MAP ② cloud_fetch `_em_get_with_retry` 指数退避(0.5/1.5/3.0s+抖动) ③ 商品榜 setTimeout 兜底 |
| 13 | 10:40 | 取消下拉全部展示 | `568e0c356` / `d8729a628` | 概念/行业树图 `overflow:auto`→`visible`（21/21 + 50/50 实渲染验证） |
| 14 | 10:50 | 盘中漏轮本机兜底 | `f0e91923f` / `e4548dfb1` | 云端 10:30 整轮 schedule 被跳过 → 本机救急重抓 7 卡(10:54)；新建自动化 `1787108824799` |
| 15 | 11:09 | **4 admin 页全空根因** | `e1483142c`（本地 `1da8d6db4` 已并入） | `habSum.T+1_rate` → `habSum['T+1_hit']`（含 `+` 字段用点号会令**整块 script 语法崩溃**）+ `switchSec('st')` 改调 `window.switchSt('tc')`（原调用从未定义的 `_renderSecSt`） |
| 16 | 11:18 | 商品榜+今日判定永久自愈 | `bbac395de`（本地 `76ec4b17c` 已并入） | 放弃 setTimeout 猜测/30 次上限，改 1 秒**永动自愈轮询** |
| 17 | 11:32 | 主力净额分时曲线多色+全列 | `67ba0da13` / `8cd7c5710`（本地 `ecfcf4c41` 已并入） | `_SF_PALETTE` 64 色 + 双 hash(FNV-1a+sdbm)碰撞跳过 → **21/21 unique**；HTML 自渲染 legend，ECharts `legend.show:false` 去「1/2」翻页 |
| 18 | 11:54 | 暂未上架页空白永久自愈 | `df6271315` | unlistedPanel len<1000 自动重渲 |
| 19 | 12:04 | 持仓实时刷新(westock) | `fc42c8f17` | data/PORTFOLIO.js 7 处刷新 |

> 表中 `本地 XXX 已并入/已丢弃` = rebase 前的本地 SHA，内容已保留在标注的推送版中（非丢失，与既往档位惯例一致）。**勿凭旧本地 SHA 找提交，按功能 grep 代码。**

### B. 云端自动化（git log 可见，不逐条审计）
`f121d36f8`(risk gauge 12:05) · `8eb72c33b`(build 11:56) · `f8a07a5b2`(build 11:36) · `541258448`(cn fetch 11:29) · `22bd783d7`(build 11:26) · `228448ed5`(build 11:24) · `bb3506379`(risk gauge 11:23) · `a69e4d388`(build 11:18) · `669115f46`(build 11:13)

### C. 非代码变更
- **08:30 主人令「全部模型改为 Hy3」**（这是 08-06「等主人通知」的那个通知）：本机 19 个未删除任务已**全部 `model_id=hy3`** ✅，另把软删的 `1785980310111` 也置 hy3 保持一致。**⚠️ 你侧 automations 表需你自行同步切 hy3，本机只管本机。**
- 看门狗第 265/269/271/273/274 轮均已留痕（六项全 OK）。

---

## 三、当前系统状态（12:12 实核）

| 项 | 状态 |
|---|---|
| **线上站点** | ✅ HTTP 200，916 KB |
| **origin/main HEAD** | `f121d36f8`（v8 risk gauge 12:05） |
| **本地 main** | ✅ ff 对齐 `f121d36f8`，与 origin 零分叉（0/0） |
| **健康检查** | ✅ **overall=ok · ok 99 / warn 4 / fail 0 / total 103**（较 11:25 档 fail2→0，overall warn→ok） |
| 4 项 warn | 全部已知良性：`H_AUTO_BUY_TRACK`（无时间戳，缺审计登记）+ `MOMENTUM_FILTER` / `STOCK_MOMENTUM_STATE` / `_V2`（OCR 人工依赖，已降级免告警） |
| **本机 runner** | ✅ Listener PID **14408** 存活（08:15:20 手动拉起后未再掉），无 Worker=空闲 |
| **runner_status** | 11:29:14 intraday，**15/15 模块全 ok，0 non-ok** |
| **盘中 7 卡新鲜度** | ✅ 全部 11:25–11:29（INDEX_QUOTES 11:25:14 / SECTOR_FUND_FLOW 11:25:13 / CONCEPT_RANKING 11:25:15 / LIMIT_UP_HEATMAP 11:25:37 / ETF_PULSE 11:26:35 / MARKET_ALERTS 11:28:22 / OVERSEAS 11:29:14） |
| **分时曲线** | ✅ `SECTOR_FUND_FLOW_INTRADAY` 今日 **6 快照**（08:31/09:54/10:07/10:17/10:54/**11:25**）→ **11:25 那枚是云端自动发布的，证明 `ad76d807e` 的 DATA_SOURCES 补挂已生效**，盘中 cadence 已打通 |
| 最终推荐 | `FINAL_RECOMMEND_DATA` 07:21:51（盘前定稿，门禁已撤销 → 前端可见） |
| 云端节奏 | cn fetch 最新 11:29；build 11:07/11:13/11:18/11:24/11:26/11:32/11:36/11:56 密集正常。**12:00 无 cn fetch 属午休（11:30–13:00）预期行为，非漏轮** |
| 剩余自动化 | **20 个**：19 ACTIVE + 1 PAUSED（`1785718343599` v8 管线看门狗每 2h，历史暂停） |
| 心跳 | `2026-08-19 08:29:58 | xiaojiu | v8_premarket_full | DONE` →本档已追加 handover_update |

---

## 四、需你注意 / 待办 / 风险

### 🔴 P0 — 本档新发现，必须处理

1. **`STOCK_QUOTE` 陈旧 32 小时（首档未发现）**：`data/STOCK_QUOTE.js` meta.update_time = **2026-08-18 04:07:12**（源 `akshare.stock_zh_a_spot` 新浪，9916 只）。
   - **为什么严重**：`algorithms/auto_run_dn_algorithm.py`（今日新上线的 H 反推）和 `scripts/momentum_common_filter.py` **都读 STOCK_QUOTE.js**。它陈旧 → 今日两个新特性**都在用昨天的行情算**。
   - 健康检查**没抓到**（该项判 ok），属巡检盲区。
   - 建议：先确认 akshare 新浪源是否被限流，再决定补抓或换源。

2. **⚠️ 工作树有 5 项未提交改动（另一并行会话正在做，本档按指令未 add）**——你接手前先看清是否要继续：
   - `M .github/scripts/cloud_dispatcher.py`（+55 行）
   - `M self_heal_monitor.py`（+69/−2）
   - `M algorithms/run_algorithms.py`
   - `?? .github/workflows/v8_algo_intraday_lite.yml`（新文件）
   - `?? algorithms/emit_momentum_filter.py`（新文件）
   - 用途：修「动量共识筛选卡好几天没动静」——根因是 `momentum_common_filter.py` **压根不在任何自动链里**，靠 cloud_dispatcher 在检测到「STOCK_MOMENTUM_STATE_V2 的 OCR 日期 > MOMENTUM_FILTER.generated 或 filter 缺失」时幂等派发这个轻量 workflow（纯标准库，几秒完成，不依赖不可靠的 schedule）。
   - **✅ 12:13 更新：已全部入库，此项转为「观察」** —— `299a3abe0` fix: 动量共识筛选卡一劳永逸根治（cloud_dispatcher.py +59 / v8_algo_intraday_lite.yml 新增 106 行 / self_heal_monitor.py +63）+ `6c78ad381` data: 兜底智能补抓(intraday) 12:13。**工作树现已干净（0 未提交）。**
   - **你要做的**：观察 1–2 轮——下次主人喂新 PDF→OCR 后，`MOMENTUM_FILTER` 是否**自动重算**。若仍不动，查 cloud_dispatcher 的 OCR 日期比对分支。

3. **小九已顺手修掉一个隐性失效（本档动作）**：`automation-1787108824799`（v8 盘中漏轮本机兜底，今日 10:50 新建）原 rrule `BYHOUR=1,2,3,4,5,6,7` —— WorkBuddy 的 **rrule BYHOUR 是本地 CST 不是 UTC**（本交接任务 `BYHOUR=12,18` 实际 12:00/18:00 CST 触发即实证），所以它只会在**凌晨 01:00–07:30** 触发，而其 prompt 又规定「只在 09:30–15:00 动作」→ **每次触发都空转，兜底等于没装**。
   - 已改为 `FREQ=HOURLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=9,10,11,13,14;BYMINUTE=0,30`（下次 13:00 生效）。
   - **你若新建 rrule 自动化，一律按 CST 写小时**（注意：cron 语法的任务如 `1785918538840` 才是 UTC）。

### 🔴 待主人授权（敏感，小九未擅动）

4. **GITHUB_TOKEN 本机「按铃」**：本机无 PAT（SSH key 不能 dispatch），10:50 云端整轮跳过时只能本机直抓兜底。最优解 = 有限权限 PAT 让本机每 30 分 `workflow_dispatch` 按铃云端（**按铃 ≠ 抓取**，云端仍是主力，契合主人省 TOKEN 意图）。主人已**明确否决 A 方案「本机常态兜底抓取」**。
5. **runner 开机自启**：07:54 重启致掉线靠人工拉起。两项待修：① `v8_runner_guard.py start_runner_process()` 改直启 `Runner.Listener.exe`（绕过 run.cmd/PowerShell 嵌套，沙箱下会失效）② 任务计划/服务化实现开机自启。

### 🟡 已知良性 / 观察项

6. **momentum 三卡仍 WARN**：无盘后选股 PDF → OCR 输入，看板 warn 是**预期状态，别当故障去修**，需主人喂 PDF 才消。
7. **self_heal_monitor 不覆盖盘中卡新鲜度**（只查 candidate/momentum/zsxq）——10:26 那类「卡盘前」它发现不了，靠人盯盘。
8. **隐式全局变量**：index.html 6968–9195 块内 `series=` 漏写 `var`。当前块无 `use strict` 不崩，但 6764 行另有一处 `use strict`，将来合并/加严会直接报错。**凡新增变量必须显式 var/let。**
9. **`.gitignore:8` 只忽略 `_tmp_*/`（目录）**，同名临时**文件**（如今日 `_tmp_all.js` 706KB）会污染 `git status`，建议补规则 `_tmp_*`。
10. **`H_AUTO_BUY_TRACK` 无时间戳** → HEALTH_CHECK warn 之一（缺审计登记），非数据问题。

### ✅ 已一劳永逸根治（勿重复修）

- 4 admin 页空白：语法崩溃 + 入口缺失 + unlistedPanel 永久自愈。
- 商品榜 / 今日判定卡：永动自愈轮询，被覆盖 100% 恢复（jsdom 双轮验证）。
- 分时曲线：21/21 独立色 + HTML 自渲染 legend 去翻页。
- ETF 日监控「数据没了」：render 早退根因（**非缓存**）。
- 三链路：DATA_SOURCES 漏挂 + 云端 WAF 指数退避 + 商品榜时序。
- 取消下拉全部展示（概念/行业树图）。

### 🔵 方法学（今日血泪，请沿用）

- **判新鲜看语义 `update_time`，绝不用 mtime**；且 **08:46 不叫「新鲜」——开盘 09:30，盘前时间戳就是断档**（小九 09:32 误判过一次）。
- 「数据没了」**先查 render 早退代码，再判缓存**。今日两次都是 render 早退（ETF `_hr<9.5`、最终推荐 `_hr<15.5`），跟缓存无关；答「Ctrl+F5 硬刷就行」是错的。
- **HEALTH_CHECK 有盲区**：它查 `raw_data/`（新鲜）给假 OK，卡片读 `data/`（可能陈旧或未加载）→「看起来 ok 实则断」。
- **含 `+` 的字段名必须方括号** `obj['T+1_hit']`，绝不能点号——静默令整块 script 崩溃。
- **任何新增 `raw_data/*.json` 必须同时挂 `DATA_SOURCES` + `CATEGORY_MAP`**，否则 raw 抓到了但 `data/*.js` 永不发布。建议加 `VAR_TO_RAW` ↔ `DATA_SOURCES` grep 双向校验（**未落地**）+ `node --check` 内联脚本 CI 门禁（**未落地**）。
- **新特性五步全通**：写脚本 → 挂 scheduler → 注册 push 队列 → 写仪表盘消费者 → 累计历史可查。少一步就是「看不到」陷阱。
- 主人若忘了自己几小时前的决策，**先一句话定位矛盾点**（「这不是 bug，是按你 8/18 令执行」）+ 让他明确撤销/维持/折中，**不要自由发挥改代码**。
- rebase 冲突口径：`raw_data/runner_status.json` 永远 `--theirs`；`data/HEALTH_CHECK.js` 一律取远端云端版，别推本地重算版。
- 沙箱坑：`curl -o 落盘` 被拦截（error 23 且会挂后台）→ 一律 `curl | python` 管道。

---

## 五、小九监督职责（白班 7:45–17:45 → 交你）

- **12:00–17:45 小九继续独家值班**：盘中每 30 分 freshness_guard 兜底 + 每小时看门狗（6 项 + 健康检查）+ 15:30 收盘刷新 + 17:00 选股四模块全量部署，均自动跑。
- 所有修复按主人「改完直接上线」铁律 commit+push；**结构性重构 / 删功能 / 动护栏文件前仍先说边界**。
- 红涨绿跌铁律全站执行；主人偏好：**禁止隐藏式滚动条 / ECharts 分页 / 下拉折叠**，列表默认 2-3 列紧凑行高。
- **17:45 起交你**（夜间/周末你为唯一部署者）。小九 19:00 还有一档「每日审计修复+推仓+对齐」（`1785978303212`）会跑，若与你的部署撞车以你的为准，小九侧只做审计不抢部署。
- **18:00 档本文件会再自动覆盖一次**（汇总下午至收盘增量），你以最后一版为准。
- **你接手第一件事建议**：① 处理 P0-1 STOCK_QUOTE 陈旧；② 决定 P0-2 那 5 项未提交文件是否上线（这是动量卡「好几天没动静」的正解）；③ 别碰 `raw_data/candidate.json`、`data/HEALTH_CHECK.js`、`v8/*.py`。
- 有异常直接落 `URGENT_阿狸咪_2026-08-19_*.md`，小九明早 08:00 交接检查会读。

---

*小九 · 2026-08-19 12:12 · 本档仅提交本交接文档，未触碰工作树其余 5 项改动*
