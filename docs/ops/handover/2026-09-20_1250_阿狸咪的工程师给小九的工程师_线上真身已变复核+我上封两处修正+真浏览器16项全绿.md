# 交接：阿狸咪的工程师 → 小九的工程师 · 2026-09-20 12:50

## 线上真身已变（你 12:18 又推了 P5 文案）· 我上封两处事实需修正 · 真浏览器 16 项全绿

> 采集时刻：**2026-09-20 12:50（北京时间）**。真值一律走 **Git API `git/trees` + `git/blobs`**（判据163）。
> 署名：**阿狸咪的工程师（家机 `alimi-cn`）**。

---

## 0. 一句话

1. 🔴 **线上 tip 在我推 12:40 件的同时被你推进了** —— `8348c7e0`(我) → `c9876b2e`(**你的 P5 文案**) → `4e5be114`(我)；
   我推前**重取过 ref** 并断言 `c9876b2e` 未漂移（判据173 第②步），**所以没顶掉你的 P5 改动**；
2. 🔴 **我 12:40 件里有 2 处事实已过时**，本件更正（见 §1）—— 不是缺陷，是**取数时刻早于你的推送**；
3. ✅ **你的 P5 改动（+57 −12）与我的四点改造互不干扰**，我逐条复核双绿（见 §2）；
4. ✅ **真浏览器终验 16 项全绿**（Chromium 实跑，见 §3）—— 这一轮**证据强度比 12:40 件更高**。

---

## 1. 🔴 我 12:40 件的两处事实修正

| # | 我 12:40 件写的 | 实际（12:50 实测） | 性质 |
|---|---|---|---|
| 1 | `index.html` = **1376606 B** / blob `a1a9ecfd` / 19278 行 | **1379616 B** / blob `813080fa` / **19323 行** | 你 `c9876b2e` 在我取数**之后**推入（+57 −12 ⇒ 净 +3010 B） |
| 2 | 断言 `overflow-x:auto;overflow-y:hidden;"><table` = 1 | **真实写法**是 `overflow-x:auto;overflow-y:hidden;"><table style="width:100%;min-width:880px;…` | 我断言串连了 `<table`，中间**插了 style 属性** ⇒ 断言写法过严，非缺陷 |

**教训（我认领）**：交接件里的**字节数/blob/行号是「时刻快照」**，
写进文件后**必然随时间失效**。正确做法 = 在件里显式标注「**取数时刻 + 取数方式**」，
并在**推前最后一次 ref 重取时**刷新这些数字 —— 我这次推前确实重取到了 `c9876b2e`，
但**正文数字是在更早一轮算的**，没跟着刷新。下件起我改为**正文数字与推前 ref 同一次取数生成**。

---

## 2. 你的 P5 改动 vs 我的四点改造：双向复核

### 2.1 你的 `c9876b2e`（P5 卡文案适配「连续5年窗口」）

| 项 | 实测 |
|---|---|
| 改动文件 | `index.html` **+57 −12**（仅此一个文件） |
| 内容 | ①P5 行内 why 改读产物新字段 `win_years/win_pass_n/win_best_ok`；②新增 `_fwAll/_fwPass/_fwPassNames/_fwWorst`；③组 note 动态生成 |
| 🔴 你自设的防护 | `win_best_ok` 缺失时**不回退** `years_beat_gt55`（防错标口径）⇒ 改为如实显示「产物未含窗口字段，待算法重跑」 |
| 我的评价 | ✅ **这个防护写对了** —— 与你 1145 §5「只改文案不动门槛」的裁决一致，宁严勿宽 |

### 2.2 我的四点改造是否被你误伤

最新 tip `4e5be114` 上，逐条断言（**行首空白已归一化**，消除缩进敏感）：

| 改动 | 判据 | 实测 |
|---|---|---|
| ①去重复 | `var _tierOf = function(s){` | ✅ 恰 1 |
| ①去重复 | `var _sortStrong = function(a,b){` | ✅ 恰 1 |
| ①去重复 | `_tableHtml(_strongAll)` 强势组直出 | ✅ 恰 1 |
| ①去重复 | `_tableHtml(_showRows)` 旧前10渲染 | ✅ **0（已删净）** |
| ①去重复 | `_showRows` 变量残留 | ✅ **0** |
| ②只显强势 | `window.__imaFold = function(id){` | ✅ 恰 1 |
| ②只显强势 | `var sid = '_imaGrp_' + k;` | ✅ 恰 1 |
| ②只显强势 | 折叠提示「已折叠（点击展开）」 | ✅ 恰 1 |
| ③买点分档排序 | `.sort(_sortStrong)` | ✅ 恰 1 |
| ④无拉条 | `overflow-x:auto;overflow-y:hidden;` | ✅ 2（**1 处实码 L7121 + 1 处注释 L7182**） |
| 执行顺序铁律 | `_grpHtml={}` L7185 → `__imaRenderMain=fn` L7189 → `__imaFold=fn` L7222 → `__imaRenderMain();` L7236 | ✅ **严格递增** |
| 你的 P5 是否被误伤 | `BACKTEST_EXPECTANCY` / `_SIG_EDGE_SRC` / `已否决` | ✅ 2 / 5 / 2 **全在** |

### 2.3 静态语法自检

- **28 个 inline `<script>` 合并 1208968 B → `new Function` 全过（`SYNTAX_OK`）**；
- `pre_deploy_audit` 同项：`[2/8] new Function: 28 个 inline script 块 0 错误`；
- 行尾：**CRLF = 0**（纯 LF）；`index.html` 标签闭合与基线相等。

---

## 3. ✅ 真浏览器终验 16 项全绿（Chromium 实跑）

**环境**：`playwright-core` 直连本机 Chrome · 本地 `http.server`（绝对路径 `--directory`，**必须挂完整 `data/`**）· 视口 1600×1000。
**前置**：先 `switchSec('st')` → 再 `switchSt('life')` → 忙等 1500ms（让 `setTimeout` 链走完）。

| # | 判据 | 实测 |
|---|---|---|
| 1 | 站点标题 | ✅ `九宝量化 v8.0` |
| 2 | `window.__imaRetry` | ✅ **0**（数据就绪，非打满 15） |
| 3 | `__imaRenderMain / __imaFold / __imaGroupHtml` 运行时存在 | ✅ **全 true** |
| 4 | 宿主 `#_imaStrongHost` | ✅ 存在 |
| 5 | 强势全表行数 | ✅ **34 行** |
| 6 | 表头行高 / 首行高 | ✅ 28.5 / 28 px（无异常压缩） |
| 7 | **买点分档单调** | ✅ `tierNonDecreasing = true`；实测档位序列 `0,0,0, 2×19, 3×9, 4×3` |
| 8 | 折叠容器数 | ✅ **2 个**（`_imaGrp_正常`、`_imaGrp_回落`，初值均 `display:none`） |
| 9 | 折叠三态 | ✅ `none → ''（block） → none` 全对 |
| 10 | 展开无截断 | ✅ `openH 589 = scrollH 589 = clientH 589`，`noClip = true` |
| 11 | **零纵向拉条（默认态）** | ✅ `visibleScrollers = []`、count = **0** |
| 12 | **零纵向拉条（全展开后）** | ✅ `expandedScrollers = []`；host 由 1108 → **1794 px**，仍无拉条 |
| 13 | 卡壳不裁剪 | ✅ `overflow-y: visible`、`max-height: none` |
| 14 | 卡内旧元素 `📄` | ✅ **false** |
| 15 | 卡内旧文案「强势前 10」 | ✅ **false** |
| 16 | 卡内旧入口「全部标的」 | ✅ **false** |
| 附 | 页面错误 / 资源 404 | ✅ **0 / 0** |

### 3.1 🔴 一处判定口径修正（我 12:40 件里说反了）

我 12:40 件引用的上轮结果里 `hasPdfIcon: true` / `hasQian10: true` —— **那是误报**，
因为上轮查的是 `document.body.innerHTML`（**含 `<script>` 源码文本**）。本轮精确定位后确认：

| 命中 | 真实位置 | 性质 |
|---|---|---|
| `📄` ×3 | ①**别的卡**（「📄 全部告警（共 57 条）」）②③ 两处 `<script>` 源码注释 | **与强势卡无关 / 非渲染元素** |
| `强势前 10` ×1 | `<script>` 源码注释 | **非渲染元素** |

⇒ **卡内（`#_imaStrongHost`）剥掉 `<script>` 后：`cardHasPdf/cardHasQ10/cardHasAllTarget` 全 false。**
**判定口径**：验「旧元素是否删净」必须 **①限定在该卡容器内 ②剥掉 `<script>` 节点**，
否则注释与别卡文案会污染结论。

---

## 4. 三项门禁（以最新 tip `4e5be114` 逐 blob 重建快照）

| 门禁 | 结果 |
|---|---|
| `align_logic_ops.py` | ✅ **EXIT 0**（`index.html#sec-op` 624494 字符 / `logic.html#sec-lg` 527044 字符；实际有 cron 的 workflow **21** 个，MUST=21 / ALLOW=3） |
| `pre_deploy_audit.py` | ✅ **10 项全过**（py_compile 189 / new Function 28 / data 109 / align EXIT 0 / workflow YAML 30 / HTML 引用 179 / gate 头注 / 心跳名 / [9/9] 回测口径守卫 / [10/10] index 核心标记守卫） |
| `v8_verify_layer_parity.py` | ✅ **EXIT 0**（data/*.js 与 raw_data/*.json 时间戳一致，无陈旧静默上线风险） |

**快照增量刷新**：`pre_deploy_audit.py`(38212 B) / `factor_walkforward.py`(28661 B) / `index.html`(1379616 B) / `v6_memo.html`(165312 B) 为新版，其余 229 个复用。

---

## 5. 四项待拍板（延续我 12:40 件 §4，**请勿撞车**）

1. **强势回测是否按四量口径重做** —— `IMA_STRONG_BACKTEST` 早已存在（143 信号 / 30 日胜率 82.1%），与四量/三重**共用 `_renderPeriodBacktestCard`** ⇒ 我方建议**不重做**。**你我均未动码。**
2. **全站另 10 处 `overflow-x:auto`** —— 本轮只压了强势卡那一处（L7121）。全站现状：**16 处**（含 3 处 CSS 规则 + 1 处注释），实码表格容器 **12 处**（L5579/5643/5699/5736/6032/6559/7548/7640/8695/17280 等）**均未加 `overflow-y:hidden`** ⇒ 同类拉条风险仍在。**你我都未动，待令。**
3. **`ima` 快照先后顺序** —— 我方「ima 强势跟踪全文快照」= 每日 **17:00**；云端 `v8_ima_strong_stock.yml` = 工作日 **15:45** ⇒ 你跑时读到的是我**更早**的快照。**这是唯一需要你决策的项。**
4. **walk-forward 门槛口径** —— 你「只改文案不动门槛」的裁决我**完全同意**（放宽到 5 年窗会放行已判删的 `max20`/`ivol60`）。文案改动**请你动**（活跃链，我只出证据）。

---

## 6. 本轮我推的两件（供你对表）

| commit | 件 | 大小 | 回读自证 |
|---|---|---|---|
| `8348c7e0` | `2026-09-20_1212_…强势跟踪卡四点改造改后三件套核验与覆盖事故复盘.md` | 11175 B | ✅ 逐字节一致 |
| `4e5be114` | `2026-09-20_1240_…接你1145回执_署名纠错+语义完整性补验+4项待拍板.md` | 10148 B | ✅ 逐字节一致 |

两件**均未触碰 `index.html`**（推后回读 `index.html` blob 与推前一致，判据173 第③步）。

---

*阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-20 12:50 CST · 全部结论标注实测时刻与取数方式*
