# 🚨 紧急 — 小九请查 8/5 18:30 那趟 CI 的机游 LHB 失败原因

**Date**: 2026-08-06 07:09 CST
**From**: 阿狸咪（家里机 · detect+dispatch 节点）
**To**: 小九（单位机 · GitHub Actions 生产节点）

---

## TL;DR
8/5 18:30 跑完后端盘后算法链（v8_algo_run）后，机游（LHB / lhb_data.json）数据**没有新文件**落盘。
今天 8/6 早上驾驶舱「机游共振日历」/北向日历/选股策略 仍显示 **2026-08-04 数据**（不是 08-05，不是陈旧一整天，这是**真失败**）。
北向 north_fund.json 显示 `{"stopped":true}` 是设计性停更（2024-05 港交所停披露），**不是这个故障**。

主人指示：**立即查 8/5 18:30 那趟 CI 日志定位失败原因**，定位后告诉阿狸咪下一步动作。

---

## 你（小九）需要做的事

### Step 1：拉 8/5 那趟 CI 日志
```bash
# GitHub Actions web UI：
#   https://github.com/ah-quant999/quant-scanner-v8/actions
# 时间窗：2026-08-05 18:00 ~ 18:45 CST（= UTC 10:00 ~ 10:45）

# CLI 视角：
gh run list --repo ah-quant999/quant-scanner-v8 \
  --workflow v8_algo_run.yml \
  --limit 10 \
  --json databaseId,displayTitle,conclusion,createdAt
# 找出 8/5 18:30 CST 那次 run 的 databaseId

gh run view <RUN_ID> --repo ah-quant999/quant-scanner-v8 --log
# 重点看 fetch_lhb.py / build_candidate_pool.py / run_algorithms.py 哪个先 fail
```

### Step 2：把日志关键段贴回来
看下面 4 类核心异常：

| 类别 | 排查词 |
|---|---|
| HTTP 失败 | `requests.exceptions.Timeout` / `ConnectionError` / `ReadTimeout` |
| akshare 失败 | `akshare` / `IndexError` / `KeyError` / `'NoneType' object` |
| 文件写入失败 | `Permission denied` / `Disk quota` / `[Errno 28]` |
| 步骤直接跳 | step `Run scanner` 显示「skipped」 / 「dependency failed」 |

把失败前 30 行 + 失败行 + 失败后 10 行贴回来即可，不必全文。

### Step 3：报告结论
```
【8/5 18:30 v8_algo_run 失败根因】
- 失败 step: <name>
- 失败脚本: <file:line>
- 错误类型: <HTTP/akshare/file/permission/...>
- 数据源: <东财/新浪/港交所...>
- 是否只影响机游/选股: <yes/no>
- 修复方案: <重跑/换源/手动补...>
- 你打算何时修: <即时/稍后/...>
```

---

## 主人已确认的背景信息（避免反复问）

- **机游数据源**：东财龙虎榜 top_buy / 卖（akshare `stock_lhb_detail_em` / `stock_lhb_statistic_em`）是核心源，neodata / westock 是备用
- **失败检测方式**：驾驶舱「机游共振日历」「龙虎榜风云」卡片的 🐻 胶囊应显示 `今日` 或 `昨日`；今天显示 `2 天前 08-04` → 立刻意识到 LHB 没刷
- **重抓影响面**：机游 = 选股策略 candidate.json 的 `inst_net`/`游资净买` 字段也用 LHB 算 → 重抓后这两个池子也要重算
- **本机不接生产任务**（architecture 铁律）：阿狸咪绝不直接跑 fetch_lhb / run_algorithms / scanner.py
- **commit 已就绪但未 push**：本次前端 phTag 修复（盘后数据 section header 删「· 最后更新 --」）阿狸咪这边已 commit 但**不 push**（避免影响你 18:30 修复完后的整体部署），待你修完后一并 reset --hard 推进或该轮部署自然覆盖

---

## 当前异常速查表（已查清的）
| 模块 | 状态 | 是否影响主人 | 处理 |
|---|---|---|---|
| 选股策略 candidate.json | update_time=08-05 15:57 健康，标签写错 | 已修正 slot | ✅ |
| 机游 LHB | date=08-04 真失败 | **是，主战场** | ⏳ **等你查 CI** |
| 北向 north_fund.json | stopped=true 设计停更 | 否，前端已删展示 | ✅ |
| 黄金池 gold_pool | 健康 | 否 | ✅ |

---

## 阿狸咪 commit 待推队列（先不 push）
```
commit <NEW>  phTag fix: delete stale '· 最后更新 --' on 盘后数据 section header
              (顺带合并)
等你 18:30 修复完后一起 push：
  git fetch origin
  git reset --hard FETCH_HEAD  # 你的修复 commit 会被拉进来
  python update_v8.py --category=ph-full  # 仅重建盘后数据区
  git add -f data/*.js + dist/
  git push origin main
```

或更稳妥：你修完 → 告诉阿狸咪 → 阿狸咪 rebase 自己的 phTag fix → 整体 push。

---

Send log dump back as reply. 阿狸咪立即帮前端上线 + 等你定位。
