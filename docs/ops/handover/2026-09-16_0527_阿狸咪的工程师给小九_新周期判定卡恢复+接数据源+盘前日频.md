# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-16 05:27｜新周期判定卡恢复+接数据源+盘前日频

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主人点名的「暂未上架最底部那张死卡」＝ **🌐 新周期判定 · 海外宏观 + AI 大周期**（卡内原写作「白话愍博士」，实为雪球 @白河愁博士）。已恢复至「🔒 暂未上架 → 📊 观测类」最下方、接入现有数据源 `window.BAIHECHOU_MACRO`，并**首跑成功**：09-16 盘前抓 21 帖 → 产出当期解析 → 推送上线 → 线上卡面已渲染本期内容。

---

## 1. 卡的真身与沿革（全部有据可查）

| 时间 | 提交 | 事件 |
|---|---|---|
| 2026-09-06 23:19 | `a5beec8c1` | 建卡（原挂「宏观观测」） |
| 2026-09-13 23:03 | `a40abe2ec` | 主人令：纯静态内联、无 data 依赖/无生成器 ⇒ 下沉至「暂未上架 · 观测类」最下方 ← **主人所指「昨晚最底部」** |
| 2026-09-14 20:31 | `e49e106b8` | 随「移除观测类 3 卡」一并被删 ⇒ 内容永久停在 09-06 ＝ 死卡 |
| 2026-09-16 05:08 | `5c805d36` | 本次：恢复 + 接数据源 + 开启盘前日频 |

**定位线索**：主人提示「里面有白河愁博士的字样」⇒ 全仓 `博士` 仅 1 条命中 `a5beec8c1`，卡内作者笔误写作「白话愍博士」，据此锁定。

---

## 2. 本次改动清单

### 2.1 前端（`index.html`，已上线）

- **容器 id 沿用 `baihechouBody`**（09-15 你为该数据源建的卡壳）：同页同源只留一张卡，不重复建壳 ⇒ 原「📝 白河愁博士观点」卡壳并入本卡。
- 渲染入口 `window.__renderNewCycleJudge()`；`window.__renderBaihechou` 保留为**兼容别名**转发，避免其他调用点失效。
- 版式＝卡名置顶 + `📆 日频` badge + 数据源行 + 核心/用法/限制 + **一句话结论**（`verdict`）+ **双源对比三组**（左＝当期原文要点 `delta`，右＝我方判断 `tension`/`align`）+ **当期原文折叠**（主帖 12 / 回复 8）+ 口径行。
- 双空态：原文未抓 / 解析待出稿，均为**如实空态**，不做旧数冒充。
- **09-06 那批静态快照数值（非农读数 / FOMC 概率 / 三情景概率）全部清除**，全部内容改由当期数据驱动。

### 2.2 数据源与产物

`data/BAIHECHOU_MACRO.js`（单行压缩 + LF）+ `raw_data/baihechou_posts.json`（累积池，按 id 去重，存 120 条）。本期线上实测：

```
update_time    = 2026-09-16 05:05:02
stats          = {total:23, main:11, reply:12, latest_main_time:"2026-09-15 15:20", stale_days:1}
analysis       = {generated_at:"2026-09-16 05:08", by:"阿狸咪的工程师（AI 解析）", sections:3}
```

### 2.3 新增脚本 `scripts/baihechou_daily.py`（已入仓）

盘前日频助手，**为何必须入仓**：抓取依赖本机 Chrome/Edge headless，助手若留在仓库外会被清理任务删掉 ⇒ 日频自动化必失效。

| 子命令 | 作用 |
|---|---|
| `prep` | sync + fetch + show 一条命令拿到当期原文（日频主路径） |
| `sync` | 按 raw 通道拉最新 fetcher/注入器/产物 |
| `push "<msg>"` | Git Data API 建 blob/tree/commit → fast-forward 推 main，**带防倒退守卫** |
| `status` | 对比远端/本地 `update_time` |

- 全部走 **GitHub API**（本机 `git fetch` 常被沙箱 SIGTERM 打死）；本机工作树落后也照常可用。
- **防倒退守卫语义**：远端 `update_time` 比本地新 ⇒ 拒推（退出码 3）。**这是保护机制、不是故障**，重跑一次即可。
- 环境变量：`V8_REPO_DIR` / `V8_GH_PAT_FILE` / `V8_AGENT_NAME`（本机默认即为阿狸咪机口径）。

### 2.4 自动化改造（阿狸咪机）

原 `98101962`「宏观观点跟踪：白河愁博士+政策事件解读」＝ `FREQ=WEEKLY;BYDAY=TU,TH,SA;BYHOUR=9`（每周仅 3 次 ⇒ **这就是它一直当死卡的根本原因**）。

已改造为：

- **名称**：`阿狸咪-盘前 新周期判定卡更新(白河愁原文+AI解析·日频)`
- **排期**：工作日 `06:30`（`FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=6;BYMINUTE=30`）
- **职责**：抓原文 → 出解析 → 注入 → 推送 → 硬验证；**保留**周二/四/六附带邮件摘要（原职责不丢，不新增任务、不重复抓取）
- 首次运行：`2026-09-16 06:30`（今日周三）

### 2.5 口径对齐（避免假告示）

原文案写「06:30 抓原文 / 07:00 AI 解析」，暗示两个时刻、两个任务；实际由同一台机的同一个任务在 06:30 **一次完成**，且**只在工作日**跑（周末无盘前）。已改为「工作日盘前 06:30 一次完成抓原文与 AI 出稿」：

- `index.html` 10 行（含卡头 badge、空态、底部「展示型卡」说明、4 处注释）
- `logic.html` 2 行（沿革句 + 观测类清单）
- 另修 `index.html` **一处同页口径矛盾**：注释说「新周期判定 已按主人令移除」，而相邻注释说「③…（本页最下方）」——上轮改动遗留，已统一。

---

## 3. 验证证据（全部实测，非推断）

| 项 | 结果 |
|---|---|
| 抓取首跑 | Edge headless 渲染成功，21 条（主帖 10 / 回复 11），`stale_days=1` |
| 推前自检 | `_nc_precheck.py` **FAIL = 0**（28 项）：JSON 可解析 / 字段齐全 / LF 纯净 / node `new Function` 0 错 |
| 产物推送 | `6c3b2403` ✅ 防倒退守卫正常（远端 13:45:49 < 本地 05:05:02） |
| 产物线上 | 线上 = 本地 = 远端 blob **逐字节一致**（12024 B），含本期 `generated_at` |
| 前端推送 | `5c805d36`（卡）→ `2eb66a6c`（口径对齐） |
| 口径对齐门禁 | 改动行数精确＝预期（10 / 2）、总行数不变、标签计数与基线**完全一致**、28 块内联 script 0 错 |
| Pages 生效 | 卡：第 2 轮命中；口径对齐：第 2 轮命中（1194949 B，新口径 7 处） |

### ⚠️ 推口径对齐时撞车一次（已正确处理）

第 1 轮 PATCH 被拒（`422 Update is not a fast forward`）——期间有人推了「chore: 缓存戳实时对齐 ?v → 防覆盖（05:24）」。因推送脚本设计为**「重取基线 → 重打补丁 → 推送」闭环**，第 2 轮基于新基线 `1a39d6a4` 重打补丁后成功。**若用单次脚本，那笔提交会被覆盖**。

---

## 4. 给小九的注意事项（重要）

1. 🔴 **`BAIHECHOU_MACRO` 不在 `update_v8.py` 产物映射表**（该文件 79467 B，对 `BAIHECHOU` 命中 **0**），且 **30 个 workflow 零引用**、文件树仅 4 个相关文件。⇒ 云端/CI **不会**产出它，`data/*.js` 直推**安全**，不会有生成器覆盖。
2. 🔴 **抓取不能上云端 runner**：雪球主域有阿里云 WAF 且主页是 SPA，curl/urllib 直连只拿到挑战页（实测 110KB `renderData` 反爬页）⇒ 必须本机 Chrome/Edge `--headless=new --dump-dom`。
3. 🔴 **禁 `git add` / `git commit`**：本仓在坚果云同步盘中，一律走 `scripts/baihechou_daily.py push`。
4. 🔴 本卡唯一生产者＝**阿狸咪机该自动化**。若你那边也挂同名任务会形成双源抢推（虽有防倒退守卫，仍会互相拒推）⇒ 请勿另建。
5. 前端 `?v` 缓存戳无需手工维护：`v8_cache_buster_reconcile.yml` 监听 `data/**` + `raw_data/**`，按内容哈希自动重算（本次已实测生效）。
6. `logic.html` 里 `V8_PAGE_SCHEDULE`（任务时窗矩阵）**只覆盖 td/rt/ph/st/op 五个页，不含「暂未上架」**⇒ 本次**刻意没有**为这张卡加矩阵行，以与其余暂未上架卡保持一致（避免「某模块特立独行」）。

---

## 5. 遗留 / 待办

- [ ] **明日 06:30 观察自动化首日真实运行**（本次为首跑手工实测，自动化尚未自动跑过一次）。
- [ ] 解析口径可持续打磨：`delta`（相对上一期边际变化）是本卡最有价值的一栏，需长期观察是否真的做到「无变化就写无变化」。
- [ ] 若主人希望周末也更新（博主周末会发帖），把 rrule 的 `BYDAY` 补上 SA,SU 即可（当前只跑工作日）。

---

署名：**阿狸咪的工程师**（阿狸咪机 / `alimi-cn`）
