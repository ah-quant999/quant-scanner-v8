# 交接文档：小九 → 阿狸咪 · 2026-09-09 15:00 下午下线交接

## 一句话结论
✅ 小九今日三项前端修复 + 游资数据回填已全部上线（14:52 实时 HTTP 核验主站，标记全在）。
⚠️ 小九机（含自托管 runner lemoncat-cn / `xiaojiu` label）今日下午下线，全权交阿狸咪。
🔴 今晚 21:00 E 批必须盯三红灯转绿（因子实验室 / BACKTEST_COMPREHENSIVE / CRDS_BACKTEST 仍红，age 26–35h）。

---

## 1. 小九机下线声明
- 小九机（单位机）含自托管 runner **lemoncat-cn**（带 `xiaojiu` label）今日下午下线。
- 双机模型原：小九白班 7:45–17:45 + 阿狸咪夜/周末 18:00–次日 7:30。今起小九机停，阿狸咪**全权接管**（含原小九白天 cron 与今晚 E 批）。
- 小九本机 WorkBuddy 自动化随机关机自然停摆；阿狸咪需在自己机器确保 stock-scanner 调度 ACTIVE（主人令「后面的都让她跑」）。

## 2. 今日已完成（线上已部署，14:52 实时核验）
| 项 | 状态 | 线上证据 |
|---|---|---|
| 死代码清理（删 CD/cock/FOMC 死分支） | ✅ live | index.html `FOMC_DATA`=0、`var CD = {}`=0 |
| 共振日历稳定性（`_rcNeedRender` 容器空判定） | ✅ live | index.html `_rcNeedRender`=4 |
| 游资具体名前端（`blk`/`blk6` 加 `aliasKey`） | ✅ live | index.html `aliasKey`=6 |
| 游资数据回填 `data/LHB_DATA.js`（09-08 带人名） | ✅ live | `update_time=2026-09-09 14:12:40`；赵老哥/宁波桑田路/溧阳路·孙哥 各 1 |

- 主站 HTTP 200，全部标记在线。主人截图口径已随 `out/lhb_seats.json`（30 条）生效：南京大钟亭→作手新一、小鳄鱼→东方证券上海银城中路、拉萨系保持模式名（不强行安人名）。
- 三件套上线前已全绿：node --check（111 script 块 0 败）/ DOM 平衡 / 引用复核（死代码消失、活代码 V8_PAGE_SCHEDULE·switchRcTab·blk·blk6 保留）/ py_compile / json 合法。

## 3. 线上实时健康检查（15:06 权威，curl `data/HEALTH_CHECK.js`）
- overall = **fail**；ok=104 / warn=2 / **fail=3** / total=109
- ❌ fail（仍未转绿，age 26–35h，正是今早治理目标）：
  - 因子实验室（FACTOR_LAB）age 1616min
  - BACKTEST_COMPREHENSIVE age 2134min
  - CRDS_BACKTEST age 2018min
  - 仍红原因：E 批自今早修复（脚本挂链 + runner 固定）后**尚未跑通**。
- ⚠️ warn（已知假告警，非真故障）：
  - runner GitHub API 检测 / self-hosted runner → 根因=健康检查盯错 workflow（`v8_cn_fetch.yml` 已改纯 dispatch 无 schedule，实际抓取在 `v8_cn_fetch_cloud.yml`）。07:00 已诊断，可忽略，或后续 1 行修健康检查口径。

## 4. 今晚 21:00 E 批 · 阿狸咪必盯（最重要）
- 目标：上述三红灯转绿。
- 小九今早已做（commit 530a51040 等）：CRDS_BACKTEST 挂 ORDER+STAGES(E)；factor_lab_gen 加心跳+静默预算 3600s；算法链固定落 lemoncat-cn（`xiaojiu` label）；顺带根治 cloud.yml CRLF 伪脏。
- ⚠️ 但阿狸咪 15:05 已**撤掉 `xiaojiu` pin**（commit `b912bdae0`/`ec445c91f`「恢复阿狸咪 runner 双跑」）并修 alimi_bell E 档 dispatch（commit `6e7bf4a37`）。→ 双跑恢复，是小九机下线前的主动调整，尊重她的决定。
- 🔴 关键风险：小九机（lemoncat-cn）下午下线 → 双跑**只剩 alimi-cn（家里机）**。alimi-cn 此前六连败根因 = **PATH 缺 bash/git**（小九 06:3x 已发 `URGENT_阿狸咪_2026-09-09_0645_家里机runner缺bash_算法链已分流lemoncat.md` 让她装 Git for Windows + 加 `C:\Program Files\Git\cmd` 与 `\bin` 到系统 PATH）。
  - 若今晚 E 批在 alimi-cn 又失败（三灯依旧 red）= alimi-cn PATH 未修 → **唯一解=按 URGENT 文档修 alimi-cn PATH**（必要时重打 `xiaojiu` label 到 alimi-cn；但 lemoncat-cn 已离线，pin 到它无人接 → 不可行）。
- 验收：21:00 后查线上 `HEALTH_CHECK.js`，这三灯转 ok 即成功；若仍 fail，看 algo_cloud run 落哪台 runner、是否 `bash: command not found`。

## 5. 游资数据后续交易日刷新
- 今日 09-08 回填是小九手动跑（venv akshare `fetch_lhb.py` + 30 条 seats 库）。
- 后续交易日若有新 LHB 且线上无具体人名：重跑 `fetch_lhb.py`（17 点前返上一交易日完整数据）→ 搬运 `data/LHB_DATA.js`（二进制 LF 写，加 `republish_time`）→ commit+push。
- 云端 LHB workflow（id 345816848）是否自动跑需阿狸咪确认；若未自动，按上述手动。
- seats 库口径**勿擅改**（南京大钟亭→作手新一 / 小鳄鱼→东方银城中路 / 拉萨系保持模式名）；覆盖率低（仅当日上榜少数知名席位）是库覆盖有限，非 bug。

## 6. 铁律 / 红线（阿狸咪接手务必遵守）
- 主树有阿狸咪 WIP，小九离职前不动；阿狸咪在自己机器操作即可。
- 改文件用 **worktree 旁路推**（`git worktree add ... origin/main`），避免镜像 actor 清零未提交改动、避免互踩。`out/` 被 gitignore → `git add -f`。
- 改后**三件套**：node --check（所有 script 块）/ DOM 标签平衡 / 引用复核（死代码消失、活代码保留）/ py_compile / json 合法。
- 验线上改动务必 `curl` 加 `?_cb=时间戳` 绕过 Pages CDN 缓存（首查常拿旧缓存）；单文件 index.html 的 script 标记用 curl+Grep 复核（WebFetch 会 markdown 化丢函数名）。
- build_deploy 审计门禁（`align_logic_ops`）常 failure 是已知误报（前序 commit 也败），与改动无关，可忽略；Pages 原生构建 success 即上线。
- 红涨绿跌铁律全站执行（暗色 #ef5350 / #26a69a）。

## 7. 自动化状态
- 小九本机 ACTIVE 任务（盘前全量 / 收盘 / 选股四模块 / 19 点审计 / 交接 / 紧急通道 / 兜底 / 自愈 / runner 守护）随小九机下线自然停摆。
- 阿狸咪机器：按主人「后面的都让她跑」，其 stock-scanner 自动化（此前 PAUSED）应恢复 ACTIVE 以覆盖调度；具体 ID/状态由阿狸咪在自己机器核对（小九不擅改阿狸咪侧任务）。
- 唯一保持 PAUSED 不擅动：小九-TDX 强势选股每日跟踪（三件套 v2）——恢复须主人明令授权。

## 8. 小九承诺
- 今日全部改动已安全上线 + 三件套验证 + 主站实时核验，无未提交 WIP 遗留。
- 小九机离线期间不推送、不干扰，后续由阿狸咪全权运行。

— 小九 2026-09-09 15:00 交班
