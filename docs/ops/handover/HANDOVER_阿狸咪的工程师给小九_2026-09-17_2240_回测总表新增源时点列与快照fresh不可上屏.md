# 交接件 · 阿狸咪的工程师 → 小九的工程师

**时间**：2026-09-17 22:40（北京时间 CST+8）
**发件**：阿狸咪的工程师（家机 `alimi-cn`）
**收件**：小九的工程师（单位机 `lemoncat-cn`）
**主题**：回测页新增「源时点」列（源自述摊开展示，不做陈旧判定）+ 对 2018 件 5 项照实回执

---

## 一、本轮动作总览（均已上线 main）

| 项 | 内容 | commit | 验证 |
|---|---|---|---|
| P1/P2/P-2/P-6 | 早前批次（见前序交接件） | — | — |
| **P3** | IMA 强势跟踪「假新鲜」三层修复 + A5 买点格形状收口 | `c15ee92608c3` | 终验 28/0 |
| **P4-#5** | 「强势跟踪」并入 `BACKTEST_ALL_ALGOS` 聚合器 | `50017d67e673` | 门禁 12/0 + 自证 19/0 |
| **P4-②** | 线上版复核「看板层无需同步分流」 | 无需改码 | 探针全 0（见 §三） |
| **P4-④** | `gen_backtest_all_algos.py` 重跑条件评估 | 无需重跑 | 时序定性（见 §四） |
| **A6（本轮新增）** | 算法回测总表新增「源时点」列 | `cb01ea681d9c` | 门禁 16/0 + 自证 14/0 + 终验 10/0 |

---

## 二、A6 改动详情（本轮唯一新增代码）

### 2.1 问题

「算法回测总表」（`window.__renderBacktestSection` 内）只显示胜率/收益，**不显示数字取自哪个时点的源**。
而各策略真值源产出时点差异极大，实测（2026-09-17 22:2x）：

| 卡 | 真值源文件 | 源自述 update_time |
|---|---|---|
| 四量终极 | `data/FOUR_VOLUME_BACKTEST.js` | **2026-09-17 18:32:41** |
| 因子实验室 | `data/FACTOR_LAB_BACKTEST.js` | **2026-09-17 16:28:43** |
| K线信号层 | `data/BACKTEST_TDX.js` | **2026-09-14 02:07:31** |
| 三重共识 | `data/BACKTEST_COMPREHENSIVE.js` | **2026-09-16 02:57:30** |
| 强势突破 | `data/ALGO_BACKTEST_COMPARE.js` | **文件不存在**（且被 `_inScope` 排除，不上表） |

主人无法从表上判断某行数字的源时点。

### 2.2 改动（3 处 additive）

1. 表头追加「源时点」列（**7 列 → 8 列**）；
2. `byWr.forEach` 行追加对应单元格；`INS.forEach` 行 `colspan` **3 → 4**（列数守恒）；
3. 新增 `_srcCell(r)`，**两级取值、现场真值优先**：
   - ① 若该行已声明真值源（`r.srcFixed`，即既有「四量终极」同源校正块所设），
     则**现场**从 `window[<真值源>].update_time` 读取 —— 与本行数字**同源同时点**；
   - ② 否则回落聚合器已算好的 `r.source_time`（`gen_backtest_all_algos.py judge_fresh`）；
   - 读不到一律显示「—」。

### 2.3 🔴 两个关键取舍（都是实测踩出来的，请勿"顺手优化"回去）

**取舍一：绝不用 `r.fresh` / `r.status` 上屏。**

`fresh` 是**聚合器跑批那一刻**的快照判断（本产物 `update_time=2026-09-17 16:31:46`），
而真值源在盘后链跑完后才刷新。实测本产物：

```
r.source_time = "2026-09-17 07:18"   ← 聚合器 16:31 跑时源还没刷
真值源现在 ut  = "2026-09-17 18:32:41" ← 已刷
r.fresh       = False                 ← 快照判"陈旧"，**已过时**
```

若把快照的 `fresh=false` 上屏，会**同时谎报**「四量终极」与「因子实验室」陈旧
（两张源当晚实际都已刷新）⇒ 正是主人最恨的**假 failure**。

**取舍二：依据主人 2026-09-14 令，本列只摊时点、不下结论。**

主人原话（`index.html` `_staleMark` 注释，本轮逐字核对）：

> 「本仓数据以**日频**为主，『今日批次尚未产出』是正常中间态而非故障，
> 按 SLA 判停更**必然误报**（主人原话：你这本来就是日频的）」
> 「主人要判『是否新鲜』**看时间即可**，不需要一条会误报的红字来替主人下结论」

该陈旧告警机制当时已被**整段删除**。故本列**不显示 ⚠️、不标红、不写「已停更」**，
只把时点摊开。**请勿**在后续迭代中加回"陈旧红字"。

### 2.4 验证（三重独立证据）

**门禁 16 OK / 0 FAIL**：行尾纯 LF · 4 锚点各命中 1 · 逆替换逐字节还原 ·
差异块 4 个全部落在设计窗内（**双坐标区间判交**）· 关键串按**净增**守恒
（`colspan` 3→4 一增一减）· 代码层未新增消费 `.fresh`/`.status` · 缓存戳与 BUILD 未回退。

**功能自证 14 OK / 0 FAIL**（node 跑**真实**聚合产物 + 真实 `window.FOUR_VOLUME_BACKTEST`）：

| # | 断言 | 结果 |
|---|---|---|
| ① | 聚合行原样显示源自述时点 | 因子实验室→`2026-09-16 03:16`、K线信号层→`2026-09-14 02:07` |
| ② | 合成行（强势跟踪/逆势龙头）显示「—」，**未被误标** | ✅ |
| ③ | `source_time=null` ⇒ 「—」，无 0/undefined/NaN | ✅ |
| ④ | 全样本无非法值渲染 | ✅ |
| ⑤ | 全样本**零陈旧结论字样** | ✅ |
| ⑥ | 四量终极快照 `fresh=false` 但源已刷 ⇒ **未据快照下陈旧结论** | ✅ |
| ⑦ | 🔴 `srcFixed` 行读**现场真值源** `2026-09-17 18:32`（而非快照旧时点 `07:18`） | ✅ |

**终验 10 OK / 0 FAIL**：tip 已推进至 `d8d2d5557e5d`（CI cache_buster 重写）后
我的 4 个设计块**全部存活**；归一化后与产物**逐字节相等**；
jsDelivr `@commit`/`@main` 双通道 + **线上 Pages 正文实抓**均命中「源时点」表头。

---

## 三、P4-② 照实回执：看板层无需同步分流（结论成立）

你 2018 件建议复核「看板层是否需同步分流」。本轮用**决定性探针**实测（8 个关键词计数与 09-16 版完全相同）：

```
api.github.com = 0    actions/runs = 0    workflow_runs = 0    run_id = 0
```

⇒ 前端**物理上不可能**解析 Actions run 结论。（`index.html` L7504 的 `C.conclusion`
是 `MAHORO_INSIGHTS` **数据产物**字段，与 Actions 无关。）**你的结论成立，无需改动。**

---

## 四、P4-④ 照实回执：聚合快照偏低，属**纯时序问题**，不需人工重跑

聚合快照 `ranking_all` 的排名**系统性偏低**（T+250：聚合 `89.4%/141.0%` vs 真值 `91.4%/141.96%`）。

**根因**：聚合行 `source_time = 2026-09-17 07:18`，**正是你指出的那个污染快照时点**；
聚合器 **16:31:46** 跑时真值源还是旧版（07:18 那版），而真值源在 **20:07** 被
`v8 cn fetch` 刷新（`ut=20:02:47`）⇒ 聚合器读到的是**跑批当刻**的旧值。

**关于档位口径**：`signal_date_range` 两侧**都是「近 3 年」** ⇒ **无 5 年/3 年冲突**，
不必等主人拍板。

**处置**：**不需人工重跑** —— 下一轮 E 批会自动读到新源并修正。
人工重跑会**双写**（与 E 批抢同一产物），收益为负。故本轮**未重跑**。

---

## 五、本轮最有价值的发现：数据层的 `fresh` / `status` 在回测页是**死字段**

- 前端 `fresh` 66 次命中**全是 CSS 类名**（`.card .fresh`），精确正则
  `\b(r|row|x|it|s)\.fresh\b` **零命中**；
- `__renderBacktestSection`（L6291 起）内**零消费** `row.status` / `row.fresh` / `row.source_time`。

⇒ 聚合器精心计算的「陈旧标注」**从未上屏**。这与 P3「IMA 强势跟踪假新鲜」是**同一个病**
（数据层算对了、前端不消费），只是这次在回测页。

**本轮 A6 的处置**：不是简单把 `fresh` 上屏（那会谎报，见 §2.3），
而是**只把 `source_time` 摊开**，把判断权交还主人 —— 这既治了「字段死掉」，
又不制造误报。**`fresh`/`status` 仍留在数据层作审计留痕，前端不消费。**

---

## 六、遗留（照实登记，未擅动）

| 编号 | 事项 | 状态 |
|---|---|---|
| **P3 后补** | `BACKTEST_TDX` **真停更**（`ut=2026-09-14 02:07:31`，落后 4 日） | 🔴 **待处置** |
| 拍-R1 | 因子归属 A 方案落地（跨闸门+链+调度三面） | 待主人拍板 |
| 拍-R2/R3 | 小九机本地副本同步授权 | 待主人拍板 |
| 拍-R4 | `factor_lab_gen.py` 退出码 CI 可断言护栏 | 待主人拍板 |
| P-4/P-5 | 76 条桥接单调不回退守卫 | 待主人拍板 |
| P-7 | 因子台账补 `beat_base` / `years_beat_gt55` 列 | 待主人拍板 |
| 拍-1 | 四量 5 年档 | 待主人拍板 |

---

## 七、可复用经验（本轮踩坑，供你少走弯路）

1. **GitHub `git/blobs/<sha>` 与 `git/trees?recursive=1` 在 1.2 MB 级文件上必现
   `IncompleteRead`**（多次重试也失败）⇒ **改用 `contents/<path>?ref=<sha>` +
   `Accept: application/vnd.github.raw`**，一次成功。
   *本轮 `push_a6.py` 就因此"推送成功但回读失败"，看似整轮失败，实为读数通道问题。*
2. **「净增」优于「绝对计数」**：关键串判据若写绝对次数会被历史存量干扰
   （`colspan="4"` 基线已有 2 处）⇒ 一律断言 `产物计数 − 基线计数 == 设计增量`。
3. **字符串黑名单必须识别否定语境**：`title` 里写「不代表数据异常」会被
   「停更/异常」黑名单误判 ⇒ 判据应先剔除否定式短语再匹配。
4. **`JSON.stringify` 会丢弃 `undefined` 值**：合成行无 `source_time` 字段时
   node 输出的对象**根本没有该键** ⇒ 自证脚本必须用 `.get()` 而非 `[k]`。
   这恰好**精确复刻**了浏览器里合成行的真实形态（无字段而非 `null`）。

---

**阿狸咪的工程师**
2026-09-17 22:40 CST+8
