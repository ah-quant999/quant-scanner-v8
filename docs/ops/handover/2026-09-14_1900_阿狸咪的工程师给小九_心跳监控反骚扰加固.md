# 心跳监控反骚扰加固（阿狸咪做，2026-09-14 19:xx）

## 一句话结论
小九心跳监控（v8_peer_monitor.py）的「误报小九失联 + 反复发信」根因已修，反骚扰加固已上线 `871f7bebe079`（MATCH=True，推后逐字节回头验 ✅）。

## 根因（为什么之前一直骚扰主人）
阿狸咪本机跑的是 **09-12 陈旧版**（238 行）：读小写 `data/hb_xiaojiu.js` + `git pull --ff-only` 失败（双机分支分叉）→ 文件永远读不到 → fail-closed 永远判「小九掉线」→ 每 6h 发一封「失联」邮件。
而远端心跳文件早被小九改名为大写 `data/HB_XIAOJIU.js`（机器 `lemoncat-cn`，心跳 18:40 仍鲜活）。即：**小九好好的，是被旧版脚本误判**。

## 本次改动（基于小九 09-14 已修好的 506 行远端版，再加反骚扰）
1. **采用远端 506 行版**：读 `origin/main:data/HB_XIAOJIU.js`（GitHub API，不依赖会失败的本地 `git pull`）→ 误报根因消除。self-test 实测读到 `status=alive, silent≈140min`（<180 阈值）。
2. **三态判定 alive/down/unknown**：fetch 抖动 / 远端瞬时读不到 / 解析失败 → 判 `unknown` → **静默自愈，绝不发信、绝不 dispatch**（主人令「能自愈的就不要一直发」）。
3. **迟滞**：连续 **2 次**「真·静默超 180min 阈值」才升级为掉线并告警，滤掉单跳抖动。
4. **反重发**：对方恢复时只清零「连续掉线计数」，**绝不**清零 6h 告警冷却 ⇒ 抖动期(掉线→在线→掉线)不会反复发信。
5. 仅「确认掉线 + 冷却到期」才打扰主人；其余一律自愈。

## 给小九
你那侧若也跑本监控，同一加固版现在已在 main，无需再修。验收判据：跑 `python v8_peer_monitor.py --self-test` 应见 `小九 status=alive`；若真掉线，邮件主题为 `🚨 v8 小九失联告警`，且抖动不会连发。

## 遗留/待观察
- CDN/Pages 实时运行未见回归（文件独立脚本、py_compile 通过）。GitHub Actions API 当时未列出本次新 run（疑似触发延迟），未据此断言「CI 绿」，但门禁必过。
- `data/HB_ALIMI.js` 尚未产出（阿狸咪侧发送侧未落地）→ 本机判定 `unknown`（静默，不报错），待落地后自动生效。
