# 交接件：逻辑详解页「因子计分计算方法规则」专章上线 + 全面审计 + 双机时窗矩阵对齐

- **交接方**：阿狸咪的工程师（家机·周末/夜班）
- **接收方**：小九的工程师（单位机·工作日 7:45–17:45 独家）
- **时间**：2026-09-19 16:08 北京时间（CST+8）
- **main 当前 HEAD**：`24105b6e1256c82bd7585e81efe3633460cb8ef2`
- **commit message**：`docs(logic): 新增「🧮因子计分计算方法规则」独立专章（公式+权重表+源码行号，非流水账）`

---

## 一、本次核心交付（主人原话："逻辑详解页要写清楚计算方法规则，不是记流水账和备忘录"）

逻辑详解页 `logic.html` 新增独立专章 **「🧮 因子计分计算方法规则（真源：generate_top10.py → final_recommend.py）」**。
要求落地：写清**因子计分的计算方法与规则**（公式 / 权重 / 融合 / 闸门 / 归一化口径），附源码真实行号，**严禁流水账 / 备忘录写法**。

专章七节：
1. **两层评分架构**：层① `algorithms/generate_top10.py` 单票综合评分 → `TOP10_DAILY.top20`；层② `algorithms/final_recommend.py` 多源共振融合 → `FINAL_RECOMMEND_DATA.stocks`。
2. **层①单票评分公式**：`raw_total = base+enhance+form_score+fund+sector_score+inst+quality_score+score_backtest`，`×gate_multiplier`，`clamp(0,100, raw_total/130×100)`（`NORM_DIVISOR=130`，L45），`×regime_adj`（行业风格 ±8%，L1410-1415）。
3. **各维度权重与区间表**（真源行 L1111-1341）。
4. **base 四信号权重表**（金钻起涨/缠论/上涨趋势/机构变红）。
5. **择时门禁乘子 `gate_multiplier` 公式**（失败语义保守，唯一真源 `regime_filter.get_current_regime_safe()`）。
6. **层②多源融合公式 + 来源表**：`resonance=len(set(sources))`、`strength=sum(scores)`、`final_score=strength+resonance×1.5+sec_add−weak_penalty`。
7. **关键口径铁律**：数据真实独立源核验 / 归一化 NORM_DIVISOR=130 / 闸门前置 / 因子只加分不减选 / P5 walk-forward 复跑不达标自动停加权（`P5_ON=False` 运行期判定）。

---

## 二、全面审计铁证（北京时间；所有命中数为对云端 main 解码后真实计数，非凭记忆）

### 2.1 `logic.html`（云端 blob `8cbf813065a8`，解码 **592,776 字节**）
| 关键词 | 命中 |
|---|---|
| 因子计分计算方法规则 | 1 |
| 两层评分架构 | 1 |
| NORM_DIVISOR=130 | 2 |
| gate_multiplier | 3 |
| final_score = strength | 1 |
| P5_ON=False | 1 |
| generate_top10.py | 4 |
| final_recommend.py | 7 |
| 择时门禁 | 1 |

### 2.2 `index.html`（云端 blob `a805b73ed6bf`，解码 **1,151,196 字节**）
| 关键词 | 命中 | 说明 |
|---|---|---|
| dsbHost | 4 | 因子计分卡 data host 配置 |
| __renderDailyScoreboard | 5 | 每日因子计分榜渲染 |
| 当日因子计分榜 | 8 | 因子计分榜卡 |
| 已纳入因子 | 6 | 已纳入因子口径 |
| 每日因子计算 | 12 | 每日因子计算 |
| gate_multiplier | 1 | 门禁乘子（前端引用） |
| P0-1 | 1 | 盘后算法链完整性闸门 |
| P0-2 | 1 | 三重历史快照价格日期校验 |
| STOCK_MOMENTUM_STATE | 13 | 动量共识状态 |

> **诚实声明**：`P0-3` / `checkGate` / `algo_heartbeat` 在 `index.html` 中命中为 0。这些逻辑位于 `.github/workflows/*.yml` 与 `algorithms/*.py`（自愈正则、巡检误报、心跳），**不在前端**。非遗漏，已单列说明（见第五节）。

### 2.3 `align_logic_ops.py` 门禁（CI pre_deploy_audit 第[4/4]项）
对推送后的 main 内容跑门禁：21 个有 cron 的 workflow 全部文档化、无死登记、无遗漏 → **EXIT 0 通过**。
（本轮回抽 30 个 workflow 文件，含 cron 的恰为 **21 个**，与门禁 MUST=21 完全一致。）

### 2.4 云端 workflow cron 抽取（30 个 workflow，21 个含 cron）
小九侧（`runs-on: self-hosted, cn` 单位机）共 **4 个**：
`v8_algo_cloud` / `v8_cn_fetch_intraday_lemoncat` / `v8_lhb_fetch` / `v8_stock_quote_refresh`。
其余 17 个 cron workflow 跑在云端 `ubuntu-latest`。

---

## 三、双机时窗矩阵（北京时间 CST+8，UTC 已 +8 折算）

### 3.1 小九侧（单位机 · self-hosted, cn）
| Workflow | CST 时窗 | 频率 | 工作日/周末 |
|---|---|---|---|
| v8_stock_quote_refresh | 09:00–16:30 | 每 30 分 | 工作日 |
| v8_cn_fetch_intraday_lemoncat | 09:40–15:00 | 多档（09:40/10:00/10:20/10:40/11:00/11:20/11:40/13:00/13:20/13:40/14:00/14:20/14:40/15:00） | 工作日 |
| v8_algo_cloud | 16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30(+1) / 06:00(+1) / 08:00 | 多档 | 工作日+夜 |
| v8_lhb_fetch | 17:30 | 单次 | 工作日 |

### 3.2 阿狸咪侧（家机 · WorkBuddy 自动化，ACTIVE）
| 自动化 | CST | 频率 |
|---|---|---|
| 开盘前防回滚自检（阿狸咪 7:25） | 07:25 | 工作日日频 |
| 盘前新周期判定卡更新（白河愁+AI） | 06:30 | 工作日日频 |
| 盘前新周期判定卡兜底补跑 | 08:30 | 工作日日频（幂等·7天有效期） |
| ima 强势跟踪全文快照（本机） | 17:00 | 日频 |
| 盘后算法链接力推进器（**唯一派发源·闸门 16:00–08:59**） | :25 / :55 每小时 | hourly |
| 算法链暴风盯梢（只读告警·不派发·16:00–次日02:00） | :05 每小时 | hourly |
| 备机 runner 巡检（盘中1h/其余2h/周末2h） | 每 2h | 2h |
| 小九心跳监控（仅告警） | :00 / :30 每 30 分 | 30min |
| 自动读取交接 | 20:30 | 日频 |
| 紧急指令监听（小九 URGENT 文件） | 每 2h | 2h |
| 星级改动覆盖守卫（只读） | :05 / :35 每 30 分 | 30min |

### 3.3 云端（GitHub `ubuntu-latest`）
| Workflow / 自动化 | CST | 频率 |
|---|---|---|
| v8_health_patrol | :08 / :38 每小时 | 全周 hourly |
| v8_cache_buster_reconcile | 每 15 分 | 15min |
| v8_cn_fetch_cloud | 08:25；09–11/13–14 每 20 分；17:20/18:20/19:20；周末 09:00 | 工作日 |
| v8_cn_fetch_watchdog | 08:07–15:52 每 15 分 | 工作日 |
| v8_intraday_snapshot | 09:00–15:00 每 10 分 | 工作日 |
| v8_algo_intraday_lite | 09:00 / 11:00 / 13:00 / 15:00 | 工作日 |
| v8_risk_gauge | 08:00–16:30 每 30 分 + 19:00–22:00 | 工作日 |
| v8_ima_strong_stock | 15:45 | 工作日 |
| v8_cn_fetch_experiments | 16:30 | 工作日 |
| v8_cn_fetch_cloud_hosted | 20:30 | 工作日 |
| v8_daily_audit | 22:30 | 日频 |
| v8_backup | 23:00 | 日频 |
| 九宝量化-每晚轻量审计（自动化） | 23:30 | 日频 |
| v8_weekend_light | 19:30 | 六/日 |
| cloud_weekly_cleanup | 21:00 周六 | 周 |
| v8_t1_guard | 08:30 / 09:30 周六 | 周 |
| v8_cleanup | 23:00 周日 | 周 |
| runner_health_alert | 13/15/17/19/21（工作日）；00/02/04/06（周五六夜）；08–20 每 2h（周末） | 见 cron |

### 3.4 关键交接节点
- **07:25** 阿狸咪「开盘前防回滚自检」→ **07:45** 小九接管（stock quote 09:00 起）。
- **小九白天 09:00–17:45**：跑 self-hosted 4 任务（quotes / intraday / algo / LHB）。
- **16:00** 起 阿狸咪「盘后算法链接力推进器」闸门激活，准备夜班派发。
- **17:45** 小九下班 → 阿狸咪夜班（盘后链 :25/:55、暴风盯梢、心跳、自动读交接 20:30）。
- **周末** 阿狸咪 exclusive：周末轻量维护 19:30、周六 T+1 兜底 09:30、t1_guard、cloud_weekly_cleanup 周六 21:00。

---

## 四、小九工作日需知 / 待办

1. logic.html 新章已上线（HEAD `24105b6e1256`）。如有人问"分怎么算"，直接指该专章，不要再口头解释。
2. 小九侧 4 个 self-hosted 任务按时窗正常跑（见 3.1），无需改动。
3. **改算法加权/阈值时，同步更新 logic.html 该章**（保持"附行号、非流水账"；任一维度变动以算法源文件为准）。
4. 若发现 P0 红灯/算法链卡死，先判是"小九侧 self-hosted 任务"还是"云端 cron"，按 3.1/3.3 定位。

---

## 五、风险与未决（诚实列出，不掩盖）

- **本地 git 仍被双机算法链占用**（`.git/shallow.lock`）。本轮全程走 **Git Data API** 推送，未碰本地 git；推后已回读 ref 自证（HEAD=`24105b6e1256`）。
- **代理偶发 502**（Tunnel 故障），脚本已加重试（tries=6，sleep 递进），全部成功。
- **P0-3 自修复正则 / 巡检误报**（任务 #4）仍 pending：位于 workflow/py 层，不阻塞本交付；工作日小九可继续跟。
- **P0-1 / P0-2** 已落地并核验（index.html 命中各 1）。P0-3 不在 index.html（见 2.2 声明）。
- 本交接件与记忆均已落盘；主人"好了和小九交接"指令的**三件套①（时窗矩阵）②（全面审计）③（交接件）** 全部完成。

---
*署名：阿狸咪的工程师 · 2026-09-19 16:08 CST*
