# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-25 04:54｜红卡东财502上游断供复验_换host已执行仍全败_判上游故障勿再派发

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

**`experiment.json` / `sector_leaders.json` 仍停 2026-09-22 18:13:35 / 18:15:13（age ≈ 58.6h）**。
换 host 修复**已确认部署且在运行**（run 日志含新代码 `hosts=EM_HOSTS` 栈帧），但**东财 push2 API 全域拒连**（云端 502 / 家机 RemoteDisconnected，而同站 `www.eastmoney.com` 200）⇒ **判定为东财上游 API 侧真故障，非我方代码问题，即刻停止派发/自愈重试**。

---

## 一、修复已生效（排除「跑的是旧代码」）

`compare 1052d4334162...f638ec266c` = **ahead / behind=0**；在途 run `#2190` head=`83cb921b7eb1` 同样 ahead=76 / behind=0。

决定性新证据（本轮新增，此前只有 00:03 档的 #2177-2179）：**post_close 档 run `36021141027`（#2184，2026-09-24 23:34 CST 派发，head=`f638ec266c`）step22 真跑**，其 traceback 逐字：

```
File ".../scripts/fetch_sector_leaders.py", line 249, in build
    boards_exact, boards_norm = fetch_em_boards()
File "...", line 166, in fetch_em_boards
    d = _get_json(url, hosts=EM_HOSTS)      ← 修复符号进入运行路径
File "...", line 104, in _get_json
    raise last
urllib.error.HTTPError: HTTP Error 502: Bad Gateway
```

同轮日志逐字：
```
⚠️ 东财 push2 抖动(push2delay.eastmoney.com) 尝试1/3: HTTP Error 502: Bad Gateway → 0.6s 后换 host 重试
⚠️ 东财 push2 抖动(push2.eastmoney.com)       尝试2/3: HTTP Error 502: Bad Gateway → 1.1s 后换 host 重试
```

⇒ **换 host 重试逻辑确实被执行了**（尝试 1→2 换了 host），仍在第 3 次前抛出 ⇒ 不是「重试没生效」，是**每个 host 都被拒**。

## 二、判定：东财上游 API 侧故障（跨网取证）

| 探测点 | 结果 |
|---|---|
| 云端 runner（中国 IP）`push2delay` / `push2` | HTTP **502 Bad Gateway**（多次，含 559B HTML 502 页） |
| 家机 alimi-cn `push2delay` / `push2` / `1.push2` / `82.push2` | **RemoteDisconnected**（连接被对端直接关闭） |
| 家机 `https://www.eastmoney.com/` | **HTTP 200**（站本身活着） |
| 家机 `https://quote.eastmoney.com/center/boardlist.html` | **HTTP 200** |

⇒ 东财**主站正常、行情数据 API（push2* 系列）不响应**。两家不同网络、不同失败形态（云端 502 / 家机 RST），排除「我方网络/单机问题」；也排除「云 runner 单点被限」。

## 三、产物实证（内容为准，不看 run/step conclusion）

| 产物 | update_time | age | blob sha |
|---|---|---|---|
| `raw_data/experiment.json` | 2026-09-22 18:13:35 | 3518.4 min ≈ 58.6h | `ce3a07ffee6c` |
| `raw_data/sector_leaders.json` | 2026-09-22 18:15:13 | 3516.8 min ≈ 58.6h | `efd9983a732f` |
| `raw_data/algo_run_report.json` | 2026-09-25 04:42:59 | ✅ 新鲜 | `df78d245a63a` |
| `raw_data/algo_heartbeat.json` | 2026-09-25 04:42:59 | ✅ 新鲜 | `9cc21da44984` |

⚠️ 两条 `all_*` 在 HEALTH_CHECK 里 status 记为 **`ok`**（红卡可见性被 `all_*` 通用红线口径吞掉）—— 这正是已挂账 `all-generic-redline-false-positive` 的同族表现，**勿据 status=ok 判红卡已恢复**。真实 age 由 `age_min` 字段给出（3518.4 / 3516.8）。

## 四、请小九处置（按优先级）

1. **不要再派发 `v8_cn_fetch_cloud` 的 post_close 档 / 不要触发自愈补跑** —— 上游 502 期间重派只会打同一堵墙（00:03 档已实证 `[HEAL✓] HTTP 204` 后重派仍全败）。
2. **本项（`redcard-hostfail-rootfix-0924`）建议维持 `pending-verify`**，等东财 push2 **自然恢复**后，由**下一轮正常 post_close 档**自动验证：判据 = 两产物 `update_time` 前进到 09-25+，**不看 run conclusion**。
3. 若 24h 后东财仍拒连，请评估**备用数据源**（同花顺/通达信/新浪 hq）——但这属于口径变更，**须主人拍板**，我不擅自改取数口径。
4. `AVG_PRICE_DATA`（ut 2026-09-24 11:30:07，age 1041.8 min）为 HEALTH_CHECK 唯一 `fail`，属独立条目 `intraday-eastmoney-stall-4cards`，**同源于东财故障**，一并等上游恢复。

## 五、本轮我方动作（可复核）

- **未派发任何 workflow、未改任何代码/数据、未推送**（避免踩踏 + 上游故障期重派无效）。
- 唯一写操作 = 本交接件新增 + `HANDOFF.yaml` 的 `redcard-hostfail-rootfix-0924` item 追加 `alimi_verify_20260925_0454` 字段。
- 主站健康：HTTP **200** / **1,361,458 B** / 与远端 blob `eab52b8516a8` **逐字节一致**（sha256 `5c2cd1fc…`）✅
- 当前无 run 堆积（in_progress=0；queued/pending 仅两条 8-9 月僵尸 run，与本链无关）✅

— 阿狸咪的工程师（alimi-cn）2026-09-25 04:54 CST
