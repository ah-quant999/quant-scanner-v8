# 紧急交接：阿狸咪午间上线清单（2026-09-10 12:00 修订·已实际推送）

## 背景
- 午间休市（11:30），主人指令「今早改好的全部上线」；随后强调「和小九紧急交接清楚明白，别又被覆盖冲掉」。
- 当前是小九白天主力时段，阿狸咪按双机分时铁律为备机；本次因主人明确指令破例推送。
- 本地与远端小九分叉：共同祖先 `950c69fb2`，小九已前进至 `b842eb4e3`（自祖先改动 62 文件）。

## 本批次实际上线内容（已 push，rebase 到小九最新后为新 hash）
**161 个「我方专属」文件**（小九完全未改动过，纯增量更新，零互踢风险）：
- 工作流 3 个：`.github/workflows/v8_algo_cloud.yml`、`v8_cn_fetch_watchdog.yml`、`v8_maharo_probe.yml`
- 算法/脚本 .py 9 个：`algorithms/calc_crds.py`、`fetch_fundamental_quality.py`、`final_recommend.py`、`run_algorithms.py`、`align_logic_ops.py`、`guard_v8_freshness.py`、`scripts/alimi_bell.py`、`scripts/fetch_ai_insights_compare.py`、`scripts/probe_maharo.py`
- 交接文档 .md 10 个（含本文件）：`docs/ops/handover/*`、`HANDOVER_*`、`_handover_*/`
- 我方独占 `data/*.js` 55 个（如 `ETF_SUBSCRIPTION_EM.js`、`FINAL_RECOMMEND_DATA.js`、`TRIPLE_*.js`、`FOUR_VOLUME*.js`、`LHB_*.js`、`MAHARO_MACRO.js` 等）
- 我方独占 `raw_data/*` 76 个（含 `kline_cache/`、`_rps_cache/`、`history/`、`algo_*` 报告、`etf_subscription_em.json` 等）
- 杂项 2 个：`HANDOVER_LOG.jsonl`、`backup/raw_data_backup_*.tar.gz`

## 本批次【未推·保留小九版】（共 62 文件，勿回退）
小九自 `950c69fb2` 已改动 62 文件，阿狸咪一律保留其版，绝不覆盖：
- **34 个 `data/*.js`**：小九实时重算产物（如 `RISK_GAUGE.js`、`SECTOR_FUND_FLOW.js`、`MARKET_FUND_FLOW_DATA.js`、`INDEX_QUOTES.js` 等）→ 用其最新版
- **26 个 `raw_data/*.json`**：小九实时数据（含 `commodity_price_history`、`market_alerts`、`risk_gauge`、`sentiment_cycle`、`commodity_elasticity` 等）→ 用其最新版
- **`index.html`**：小九也改过；其 `<script ?v=>` 缓存戳与上面 34 个 data 文件强耦合，必须与其 data 版对齐 → **保留小九版**
- **`data/maharo_insights.js`**：小九修改、我方曾删除 → 保留小九版（不删）

## 我方今早改了、但本批未推的项（需手动合并，已本地保全）
- `index.html` 今早 UI/逻辑改动（约 176 行，含 JudgmentEnv 等 + `?v` 戳）→ 因强耦合小九 data 版，本批保留小九 `index.html`。完整改动已存于本地分支 `alimi-morning-20260910`（=旧 commit `66a664d0e`/`d06cc8ebc`），如需上线需人工 3-way 合并。
- 其余我方改动若与小九重叠，已统一按「保留小九实时版」处理，未强行覆盖。

## 小九协作不变量（务必遵守，避免互踢）
1. 阿狸咪推送一律 rebase 接远端（pre-push 钩子自动 rebase），**绝不用 `--force`**。
2. 小九若继续推送：先 `git pull --rebase origin main` 接阿狸咪版本，再改再推；**勿 `--force`**。
3. 重叠的 `raw_data` / `data` / `index.html`：以「最新一方重算」为准，勿盲目覆盖对方当日数据。
4. 白天（7:45–17:45）小九主力；阿狸咪仅在主人明确指令下破例。

## CI / 主站
- 推 `raw_data` / `data` 已触发 `v8_build_deploy`，CI 自动重建 `data/*.js` 并发布 GitHub Pages。
- 主站刷新后请 **Ctrl+F5** 硬刷 `file:///E:/workspace/stock-scanner/index.html` 确认。

## 待小九确认 / 注意
- 重叠调度 yml（`v8_cn_fetch_intraday_lemoncat.yml` 等）：以你方最新版为准，阿狸咪未动。
- 模型切换任务：本批**未含**自动化模型切换。26 个活跃自动化（glm/hy3/hy4 等）需主人在 WorkBuddy 界面层手动改到 `hy3-x`，智能体无代切工具（已核实 `automation_update` 无 model 字段，假 success）。详见 `2026-09-10.md` 日志。
- 本地保全分支 `alimi-morning-20260910` 含今早完整工作（含 index.html 改动），不推送，仅供手动合并回溯。

## 附：本批安全集完整性
- 安全集 = 我方改动(201) − 小九改动(62) = 161，已逐文件核对；推送前 `git status` 干净、pre-push 钩子 rebase 接 `b842eb4e3` 成功。
