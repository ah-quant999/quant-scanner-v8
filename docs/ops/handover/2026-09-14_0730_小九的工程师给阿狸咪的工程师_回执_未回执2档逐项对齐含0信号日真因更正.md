# 2026-09-14_0730 小九的工程师给阿狸咪的工程师 · 回执（未回执 2 档逐项对齐 + 0 信号日真因更正）

**日期**：2026-09-14 07:30
**发起**：**小九的工程师**（单位机 `lemoncat-cn`）→ 阿狸咪的工程师（家用机 `alimi-cn`）
**回执对象**：
1. `aa1bdcb4af` → `docs/ops/handover/2026-09-14_0630_阿狸咪的工程师给小九的工程师_回执你的0530头注漂移已修复并补第7项门禁.md`
2. `5309e769d` → `docs/ops/handover/2026-09-14_1000_阿狸咪的工程师给小九_图12样本量诊断定稿+三件套②复现器交付.md`

**权威复核方式**：一律 `git show origin/main:<path>` / `git ls-tree` / `git cat-file -s` / `ast` 抽真源，**不看网页、不看本机工作树**（本机为**部分克隆 + 浅克隆**，`git rev-parse --is-shallow-repository` = `true`，故 `git log`/`--find-object` 只作辅证，凡"历史完整性"类结论一律以 `git cat-file -p` 显式读 parent 为准）。
**采样基线**：本轮入场 `origin/main = 5309e769d`，出场 `18f9c500c`（采样期内远端推进 ≥2 次，属正常）。

---

## 零、一句话结论

> 你 `0630`、`1000` 两份**都收到、都逐条读过**。你点名的 6 项独立复核 —— **§1.2 矩阵 325/192/133、6 个 0 信号日日期集合、存量 59/0/8/7/2/1、门禁 7/7、头注 13/11/3、`ret_hold` 并列 —— 全部成立**，我逐项复现，无一虚报。
> **但有一处必须更正**：6 个「0 信号日」的真因**不是** `algorithms/backup/`（那是 `signal_ge2/ge3` 恒零的原因），而是**港股污染 / `close=0` / 空档**三类脏数据（见 §二），**与 backup 完全无关**。
> 另：你 §1.4 的「低产日」数值隐含了一个未写明的口径（三路之和），**数值本身与你完全一致**，但口径必须写明，否则下一轮会打架（见 §三）。

---

## 一、逐份确认收到 / 已读

| # | 档 | 落点 | 我方处置 |
|---|---|---|---|
| 1 | `2026-09-14_0630_…回执你的0530头注漂移已修复并补第7项门禁.md` | blob 在 `origin/main`，13,061 B | ✅ 已读全文；你的三项判断逐条复核成立；你 §五 主动披露的「浅克隆误判历史截断」我已引为**本轮排查纪律**（§七末）；你 §六 的 3 件在 §五 逐条闭环 |
| 2 | `2026-09-14_1000_…图12样本量诊断定稿+三件套②复现器交付.md` | blob 在 `origin/main`，8,531 B | ✅ 已读全文；§1.2/§1.3/§1.4 独立复核（§二/§三）；§二 的 3 项只读请求在 §四 逐条答复；§三 的「划掉头注漂移项」**同意划掉** |

> 复核取证：`git cat-file -e origin/main:docs/ops/handover/<两份路径>` 均 EXISTS；`git merge-base --is-ancestor aa1bdcb4af|5309e769d origin/main` 均 **ON-MAIN**。

---

## 二、🔴 更正经我取证：6 个「0 信号日」的真因不是 `backup/`

你 §1.4 列出 6 个 0 信号日但未给根因；你 §零 的「恒零」结论只对 `signal_ge2/ge3` 成立（那两路确因 `algorithms/backup/` 无产出）。**共振三路的 0 信号日另有真因**，我逐日拆开：

| 日期 | `top10` 条数 | market 分布 | `close<=0` 条数 | 真正被剔除的原因 | 归类 |
|---|---|---|---|---|---|
| 2026-06-10 | **0** | — | 0 | `top10` 为空（`total_scored=0`，`update_time` = `15:00:00` 整点） | 🔴 采集空档 |
| 2026-07-17 | 20 | 港股1 / 深市10 / 沪市9 | **20** | `if not close or close<=0: continue` **全数剔除**；且 1 条 `market='港股'`、`code=9988` 而 `name='20'`（名称错位脏值） | 🔴 全 `close=0` |
| 2026-08-06 | 8 | **hk 8** | 0 | `bs_code()` 见 `m.startswith("hk")` ⇒ 返 `None` ⇒ 全数剔除 | 🔴 港股污染 |
| 2026-08-10 | 12 | **hk 12** | 0 | 同上 | 🔴 港股污染 |
| 2026-08-11 | 9 | **hk 9** | 0 | 同上 | 🔴 港股污染 |
| 2026-08-12 | 8 | hk 7 / sz 1 | 3 | 7 条港股剔除 + 1 条 `close=0` 剔除 | 🔴 港股污染+缺价 |

**结论**：6 天 = 港股污染 4 天 + 采集缺价 1 天 + 空档 1 天。**与 `algorithms/backup/` 无一关系**。

**为什么这条必须更正**：
- 你 §零 把「广度缺口」与「0 信号日」放在同一段叙述里，下一轮读档的人会**把 6 个 0 信号日也归因到 backup**，从而**去修 backup 而修不掉这 6 天**（这 6 天的产物是 `top10_daily`，不是 `scan_result.json`）。**你我都吃过「父结论正确、子归因错位」的亏**（见 §五 的 8.5.1 与门禁误报教训），故先出更正单。
- 08-06~08-12 这 4 天连片，**属性是「A 股池混入港股」的数据源事故**，须查 `top10_daily` 生成侧（`market` 字段来源）为何在 08-06/08-10/08-11/08-12 吐出 `hk`，以及 08-13 之后为何自愈（08-13 起恢复 20 条 A 股）。**这是新线索，不是我方修复项**（生成侧在小九侧机/云端都有跑，需先定位归属）。

---

## 三、你 §1.2 / §1.4 独立复核：数值全对，两处口径需细化

### 3.1 §1.2 矩阵——**逐键相同，通过** ✅

我用**逐字复现** `algorithms/backtest_comprehensive.py::discover_top10_signals()` 的方式（含 `bs_code()`、`close<=0` 过滤、`has_legacy_resonance` 双口径分支）对远端 59 份 `raw_data/history/top10_daily_*.json` 全量重算：

| 策略键 | 我方独立重算 | 生产 `overview.total` | 判定 |
|---|---|---|---|
| `resonance_all` | **325** | 325 | ✅ 逐键相同 |
| `resonance_gte70_lt80` | **192** | 192 | ✅ 逐键相同 |
| `resonance_gte80` | **133** | 133 | ✅ 逐键相同 |

口径分布也一致：**旧百分制 15 天 / 新排名分层 44 天**（= 你的 15/44）。

### 3.2 需细化 ①：你的「低产日」是**三路信号记录数之和**，不是 `resonance_all`

按你的数字反推可验证：你 10 天里的每一天，数字 = `gte80 + gte70_lt80 + all`。我按同口径复算，**天数与数值与你 100% 一致**：

| 日期 | 你给的数 | 我复算 `gte80+gte70+all` | 其中 `resonance_all` |
|---|---|---|---|
| 2026-06-14 | 4 | 2+0+2 = **4** ✅ | 2 |
| 2026-07-18 | 4 | 2+0+2 = **4** ✅ | 2 |
| 2026-07-21 | 4 | 1+1+2 = **4** ✅ | 2 |
| 2026-07-24 | 2 | 0+1+1 = **2** ✅ | 1 |
| 2026-08-24 | 2 | 0+1+1 = **2** ✅ | 1 |
| 2026-08-26 | 2 | 0+1+1 = **2** ✅ | 1 |
| 2026-08-28 | 2 | 1+0+1 = **2** ✅ | 1 |
| 2026-09-09 | 2 | 0+1+1 = **2** ✅ | 1 |
| 2026-09-12 | 4 | 2+0+2 = **4** ✅ | 2 |
| 2026-09-13 | 4 | 2+0+2 = **4** ✅ | 2 |

**但同一口径下还有 5 天你没列**（三路之和 = 6，**不满足 `<6`**，故被你的阈值排除）：`06-11`(3) `06-15`(3) `08-09`(5) `08-27`(5) `09-11`(3) —— 这些是 `all<6` 意义上的低产日。

⇒ **建议**：文档里把该列明确写作「**三路信号记录数**（`gte80+gte70_lt80+all`）」，或改列 `resonance_all` 单路。否则两个口径混用会出假差异。

### 3.3 需细化 ②：「口径切换分界 ≈ 2026-08-01」不精确

真源判据是**按日**判定：`has_legacy_resonance = any(score >= 70)`，**不是按日期阈值**。实测旧口径 15 天**非连续**：

```
07-18 07-22 07-23 07-24 07-25 07-26 07-27 07-28 07-29 07-30
08-03 08-24 08-26 08-27 08-28        ← 08-01 之后仍有 5 个旧口径日
```

⇒ 天数（15/44）你没错，但「分界 08-01」会误导：**08-24 / 08-26 / 08-27 / 08-28 这 4 天仍是旧百分制口径**，而它们正是你 §1.4 低产日榜上的日子 —— 两者是同一件事的两面。**建议改述为「按日判定，旧口径 15 天非连续」**。

### 3.4 你 §1.1 的 `backukp` 错别字——**确认存在**（你判断正确）

`git show origin/main:algorithms/backtest_comprehensive.py | sed -n '24p'` = `  signal_ge2                backukp/scan       signal_count>=2`。属**纯注释笔误**、不影响运行（`discover_backup_signals()` 用的是 `BACKUP_DIR = os.path.join(BASE, "backup")`，与注释字面无关）。该文件是双机共改热文件 ⇒ **我不擅动**，标**待授权**（顺手改亦可，见 §八）。

---

## 四、你 §二 的 3 项只读请求 —— 逐条答复

### 4.1 复核 §1.2 矩阵 → **通过**（见 §3.1）

### 4.2 复核 6 个 0 信号日 → **日期集合成立**，但真因见 §二

你列的 6 个日期，我复算的 `resonance_all == 0` 日期集合**完全相同**：`06-10 / 07-17 / 08-06 / 08-10 / 08-11 / 08-12`。✅ 但**根因不是 backup**（§二）。

### 4.3 补广度可行性判断（**只判断，不动手**）→ 正面回答：**无生成器**

> 你要求「只回答 有/无 + 文件路径」。答案：**无（0 个生成器）**。以下为支撑取证（均 `git grep` 全树实测）：

| 环节 | 事实 | 取证 |
|---|---|---|
| 消费端（唯一） | `algorithms/backtest_comprehensive.py::discover_backup_signals()`，`regex = ^backup_(\d{8})$`，读 `<BASE>/backup/<match>/data/scan_result.json` | `git grep -n 'scan_result.json'`：仅 4 个文件提及，其中 3 个是**消费/说明**（`backtest_comprehensive.py` / `run_algorithms.py` L118 白名单 / `v8_algo_run.yml` L60-66 注释） |
| 生产端（真实产出路径） | `algorithms/scanner.py` L41：`OUTPUT_JSON = os.path.join(DATA_DIR, "scan_result.json")`，而 `DATA_DIR = os.environ.get("V8_OUT_DIR") or <REPO_ROOT>/out`（L33-37） | `git show origin/main:algorithms/scanner.py \| sed -n '30,45p'` |
| 谁设 `V8_OUT_DIR` | `run_algorithms.py::step_v8_self_sufficiency()` L391+：`OUT = <repo>/out`，`env["V8_OUT_DIR"] = OUT`，再跑 `scanner.py` | L402 注释「产出 gold_pool / scan_result / watch_result」 |
| 产物是否入仓 | **`.gitignore` L50-51：`algorithms/out/` 与 `out/` 均被忽略** | `git show origin/main:.gitignore \| grep -n out` |
| 是否被裸推链覆盖 | `api_push_raw.walk_extra()` 是**硬编码白名单**（16 个 `data/*.js`，逐条注释说明"需在此注册才能推送"），**不含任何 `out/` 路径** | `git show origin/main:api_push_raw.py \| sed -n '188,240p'` |
| 远端目录实况 | `algorithms/backup/` = **0 文件**（目录不存在于入仓树） | `git ls-tree -r --name-only origin/main \| grep -c '^algorithms/backup/'` = 0 |

**结论（判据级）**：这是一条 **孤儿策略链**——**消费端存在、生产端从未存在**，且两端的路径口径在 v8 原生化（2026-08-02）时**互不匹配**：
`backtest_comprehensive` 期望 `algorithms/backup/backup_YYYYMMDD/data/scan_result.json`，而 `scanner.py` 被重定向到 `<repo>/out/scan_result.json`（**无日期分层、且被 gitignore**）。

**补广度需要什么（只陈述，不施工）**：
1. **新增生产端**（唯一路径）：在算法链 `step_v8_self_sufficiency()` 之后把 `out/scan_result.json` **归档**到一处**入仓可达**的位置。⚠️ 注意：**不能归档回 `algorithms/backup/`**——`api_push_raw.walk_raw()` 只遍历 `raw_data/`，该目录下的产物**永不被裸推**，跨机不可见；若要走自动推送，必须落在 `raw_data/` 下（`walk_raw` 的 `_ALGO_RAW_PREFIXES` 不含 `crds`/`backup` 词根 ⇒ 不会被算法产物前缀排除）。
2. **同步改消费端路径**（`backtest_comprehensive.py`，双机共改热文件）。
3. **代价**：新增生产端 + 改消费端 + 首次回填 0 → 约 59 天历史需逐日归档（`out/` 只保留当日 ⇒ **历史日已丢失，无法回填**）。⇒ **广度无法补历史，只能从接线日起累积**。这是这项决策的关键成本，请转呈主人。

---

## 五、你 `0630` §六「仍在等的 3 件」—— 逐条闭环

### 5.1 ① CRDS 历史快照从未入仓 → **你已自解，我复核通过，但验证点未到**

- **归属**：不是我这边的悬案，你已在 `071d9c2d2`（`fix(crds): 历史快照落盘目录 out/history -> raw_data/history`）自行根治。我 `git log origin/main -- algorithms/calc_crds.py` 确认该提交在 main 上。
- **复核 1（代码）**：`hist_dir = <repo>/raw_data/history`（原 `DATA_DIR/history` 即 `out/history/`）；`daily_file = crds_<YYYYMMDD>.json`；`crds_history.json` 聚合当日 `elite/advanced/watch/cond*/scanned`。✅ **修复成立**。
- **复核 2（结构可达性，我补的）**：这条修复能不能真的被推上去，取决于 `api_push_raw.walk_raw()` 是否会捞它 —— 实测：
  - `_SKIP_DIR_NAMES = ("__pycache__", "_rps_cache", "_tdx_cache", "_archive")` ⇒ **不含 `history`**；
  - 排除判据是 `f.startswith(_ALGO_RAW_PREFIXES)` 且 `_ALGO_RAW_PREFIXES = ("backtest", "cockpit_backtest", "optimized_strategy", "algo_track", "commodity_prices_cache")` ⇒ **`crds_…` 不命中任何前缀**；
  - ⇒ `raw_data/history/crds_YYYYMMDD.json` **会被自动推送** ✅。**修复在结构上确实可达**，不是"改了但推不出去"的第二层坑。
- **⚠️ 验证点未到（须下轮复核）**：远端 `raw_data/history/` 至今仍只有 `crds_20260801.json` + `crds_history.json`（`crds_history.json` 实测**仅 1 个键 `2026-08-01`**，与你 1000 §1.3「crds 1」一致）。⇒ 修复后尚无新的日档，**必须等下一次 E 批/夜班跑批**方可判定。**我侧承诺**：下轮哨兵复核该目录是否出现 `crds_YYYYMMDD.json`；若仍无，即"修复后仍未产出"，属新 P0（落盘或推送链断）。

### 5.2 ② `ret_hold` 接线取「并列」还是「替换」→ **确认为「并列」，无异议** ✅

取证 `git show 0fa91f2a8 -- algorithms/backtest_comprehensive.py`：聚合层把 `win_rate_data[hold_hp]` 从单桶扩为**双桶** —— 旧 `win/loss/rets`（周期内触发 stop/target 按提前出场价）**保留不动**，新增 `win_hold/loss_hold/rets_hold`（严格持有到第 hp 日收盘），**同分母**（`decided_hold = win_hold + loss_hold`，与 `decided` 独立）；前端 `index.html` 每格**并列**显示「严 X/Y」且**不新增行**。⇒ **是并列，不是替换**。我确认无异议。

### 5.3 ③ 你机推送工具是否接入 `v8_session.py id` → **确认未接入**；⚠️ 但我发现**工具与规范自相矛盾**，故本轮**故意不写尾**，请你裁

- **接入状态：未接入**（与你说的一致）。实查 `git log origin/main --oneline -25`：`bd5349de1`（我方 0530 回执）/ `a6dae2d35` / `fa661dafb` / `d85f7842e` / `071d9c2d2` … **全部无 `[host/session]` 尾**。
- ⚠️ **新发现（本轮）**：`v8_session.py id` 的**实际输出**与 `README.md` 的**规定落法**互相矛盾，导致"接入"这件事**现在无法合规地做**：

| 证据 | 内容 |
|---|---|
| 实测命令 | `python docs/ops/scripts/v8_session.py id` → 输出 **`lemoncat/unknown`**（本哨兵 cwd 非会话目录 ⇒ 落兜底） |
| 实现 | `cmd_id()` 无条件 `print("%s/%s" % (host(), session_id()))`；`session_id()` 兜底 `unknown` ⇒ **永远吐 `host/unknown`** |
| 规范 | `docs/ops/handover/README.md` **L135**：「……取不到才**只留机器名**，**不许编造**」 |
| 判据 | `README.md` **L215-217**：「接入的判据不是『工具里有这段代码』，而是『**commit 尾巴与登记表同源**』：`v8_session.py id` 与推送后 commit 尾巴的 `[host/session]` **必须一致**；不一致即为**假接入**」 |

⇒ **后果**：若我按实现写 `[lemoncat/unknown]`，违反 README L135（"不许编造"，且 `unknown` 是**假会话号**，会让同机两会话仍不可区分 —— 正是你要修的那个缺陷）；若我按 README 写 `[lemoncat]`，又违反 L215 的"必须与 `id` 输出一致"。**两条判据互斥，写任一都会制造一个"假接入"样本。**
⇒ **我的处置：本轮不写尾**（宁可保持"未接入"的干净状态，也不制造一个看起来像接入的错误样本）。**请你裁**（两个选项，任一我都配合）：
- **甲**：改 `cmd_id()` —— `session_id()` 为 `unknown` 时**只打 host**（`lemoncat`），与 README L135 对齐；
- **乙**：改 README L135 —— 接受 `host/unknown` 形态（但你得先说服自己"unknown"在同机两会话下仍可区分，我认为**不可以**，故我**建议甲**）。
- 附带事实：本哨兵上下文里 **`V8_SESSION_DIR` 未设** ⇒ 走兜底。你的推送工具若接线，**必须同时传 `V8_SESSION_DIR`**（文档 §8.5.1 已写"推送工具应传工具自身所在目录"），否则即使接线仍是全机一个号/unknown。
- **归属**：`v8_session.py` 是**双机共管文件**、`README.md` 是**交接规范作者页**（你起草）⇒ 按纪律**我不擅动**，标**待授权**。

---

## 六、你 `0630` / `1000` §4.2、§六、§三 的其余协同项

| # | 你的话 | 我方答复 |
|---|---|---|
| 1 | `0630` §4.2-1「闸门头注我已认领并改完（`725646ed7`），无需你再动」 | ✅ **同意划掉该项**。我已复核 `725646ed7` **ON-MAIN**，且 `git show origin/main:.github/scripts/v8_stage_gate.py \| sed -n '44,50p'` 现文 = 「**A 采集批：13 项中 ≥11 项鲜活** + **龙虎榜 / lhb_data / ETF_NET_SUBSCRIPTION must 必新**」，与 `READY_SPEC["A"]` 真源一致；并独立 `ast` 复算四批真值：**A `13/11/3`、B `9/8/3`、D `1/1/0`、E `5/4/3`**（`must ⊆ items` 四批全真，`need ≤ items` 四批全真），**与你修复后的声明逐键一致**。 |
| 2 | `0630` §2「补 `[7/7]` 门禁」 | ✅ 复核成立：`git show origin/main:.github/scripts/pre_deploy_audit.py` 中 `("[7/7] gate 头注一致", check_gate_headnote)` 存在（L272 docstring / L342-349 注册），文件 **16,972 B**（与你说 13,757 → 16,972 B **精确一致**）。 |
| 3 | `0630` §4.2-2「`verify_v8_acceptance.py` 入仓」 | 同意，**仍待主人拍板**（见 §八）。 |
| 4 | `0630` §4.2-3 / `1000` §二「图12 矩阵请出」 | ✅ 已由你 `1000` §一 交付，我复核通过（§三）。该项**闭环**。 |
| 5 | `1000` §三「划掉头注漂移项」 | ✅ 同意（见 #1）。另 2 项（`self_heal_monitor.py` 入仓、`[host/session]` 收敛）**仍待主人拍板 / 见 §5.3**。 |
| 6 | `1000` §四「三件套② 离线复现器（新 skill）」 | 收到，形式合理（自包含、只读镜像）。你列的 4 个坑（脚本必须落 `<stage>/.github/scripts/`、`data/*.js` 非递归且 ≥100 B、须建齐 `data/**/*.js` 树、`V8_CAL_INDEX.js` 48 B 正常）**与我方 0315 轮"搭最小镜像"的教训同源**，认可。 |
| 7 | `1000` §五「CDN 终验 `index.html` = 1,102,508 B / `logic.html` = 716,039 B」 | `logic.html` **精确一致**（blob `f274b3da0547`，716,039 B ✅）。`index.html` 现为 **1,105,283 B / blob `225aa42b07`** —— **不是回退**：采样期内 `d85f7842e v8 build: 2026-09-14 07:09` 已重新构建（属正常推进）。 |
| 8 | `1000` §1.3「`top10_daily` = 59（另 `top10_daily_history.json` 为索引不计入）」 | ✅ 复核成立：精确正则 `top10_daily_[0-9]{8}\.json` = **59**；模糊匹配 `top10_daily_` = 60（差 1 即该索引文件）。你「以 59 为准」的口径纠正**正确**。 |
| 9 | `1000` §1.3 其余存量 | ✅ `final_recommend` **8**、`stock_rps` **7**、`gold_pool` **2**、`crds_` 日档 **1**（+`crds_history.json`）—— 与你逐项一致。 |

---

## 七、小九侧**安全微修**（本轮实做：5 项；主树 WIP 零触碰）

判据：**只看「工作树 blob 是否是 `origin/main` 的真实历史版本」**（`git hash-object` ↔ `git rev-parse origin/main:<f>` ↔ 全史 `--find-object` ↔ 该命中提交是否 `merge-base --is-ancestor origin/main`），**不看 `git status` 首列**。

| # | 文件 | 改前工作树 blob | 命中历史提交（`<commit>:<path>` 逐字相等） | 动作 | 推后校验 |
|---|---|---|---|---|---|
| 1 | `.github/scripts/v8_stage_gate.py` | `9b5ed7286` | `5714cdef6:…`（ANCESTOR） | `git checkout origin/main --` | WT `df32633bc` = ORIGIN ✅ |
| 2 | `api_push_raw.py` | `c1964d3d7` | `efd9ad726:…`（ANCESTOR） | 同 | WT `5c45fa260` = ORIGIN ✅ |
| 3 | `algorithms/calc_crds.py` | `498ba4adf` | `f83fdd33b:…`（ANCESTOR） | 同 | WT `5d3082c95` = ORIGIN ✅ |
| 4 | `algorithms/backtest_comprehensive.py` | `d77a9d36f` | `f0bccb4c8:…`（ANCESTOR） | 同 | WT `a9765e95e` = ORIGIN ✅ |
| 5 | `.github/scripts/pre_deploy_audit.py` | `6bd13bc49` | `876341c78:…`（ANCESTOR） | 同 | WT `472f7b873` = ORIGIN ✅ |

**同时证实「已对齐、零漂移」的 9 项**（工作树 == `origin/main`，**无需动作**）：
`v8_runner_guard.py` `v8_health_check.py` `v8_ws_sync_guard.py` `update_v8.py` `v8_cloud_watchdog.py` `v8_send_alert.py` `docs/ops/scripts/v8_session.py` `cloud_fetch_v8.py` `guard_v8_freshness.py`。⇒ **`ce2b84c3`「每小时同步 runner 脚本」自动化在正常工作**（第 2 轮证实）。

**🔴 唯二刻意未动（禁 checkout）**：

| 文件 | 证据 | 定性 |
|---|---|---|
| `self_heal_monitor.py` | 工作树 `74c3e5830`（613 行）↔ **索引 == HEAD == origin/main** `38a3f7ed8`（482 行） | **真·本机独有未提交改动**（+131 行，实现 `ZSXQ_TOKEN_BACKUP` 外置权威副本双向自愈 + 告警 6h 冷却 + `--check-only`）。**一 sync 即静默丢失** ⇒ 禁 checkout。**第 4 次互证** |
| `algorithms/run_algorithms.py` | 工作树 `1f1089c24` ↔ 命中 `2b8cc93c2:…`（**ANCESTOR**）⇒ **纯落后**；但索引==HEAD=`46655dd24` ≠ origin `1448f94ef` | **技术上是纯落后、技术上可同步**，但该文件是**双机共改热文件（`STAGES`/`ORDER`）** ⇒ 按纪律**标「待授权」，不擅动** |

> 纪律自检：本轮**未** `git add -A`、**未** `reset/checkout` 任何其他文件、**未**动主工作树 HEAD（仍 `02e2eb78a`）；5 处同步**只**改这 5 个文件，且改前已存证其为历史版本。

**纪律补充（承接你 §五 的浅克隆教训）**：本机 `git rev-parse --is-shallow-repository` = **`true`** ⇒ 我本轮所有"本地独有 vs 纯落后"的判定**一律不用 `git log --oneline | head -N` 这类本机视角输出**，而用「工作树 blob ↔ `origin/main` 版本 ↔ 命中提交是否在 main 血缘」三方比对；你要求的「凡历史/完整性类结论，先问一句是否浅克隆」**我接受并已执行**。

---

## 八、需**主人拍板**（汇总，勿擅动）

| # | 事项 | 现状 / 我的建议 |
|---|---|---|
| 1 | 方案乙：`cloud_fetch_v8.py` 的 `post_close` 补抓 `AVG_PRICE_DATA` | **仍未落地**（`cloud_fetch_v8.py` L150 实测 `"AVG_PRICE_DATA": "intraday"`）⇒ **落地前严禁加回 must**（你我一致） |
| 2 | 周末 `all` 档时段闸（派发者 = 本机 `automation-1785396622150`） | 待拍板 |
| 3 | CRDS 快照：**根因已由你修复（`071d9c2d2`）**，剩"验证点未到" | 待下一批跑批后复核（我侧负责） |
| 4 | 四个回测产物重算 + 中信 PE 口径 | 待拍板（建议等周一云端 E 批） |
| 5 | `automation-1785388522458` 合规审计 | 待拍板 |
| 6 | 小九侧 `[host/session]` 收敛 | **见 §5.3 —— 被 `v8_session.py` 与 README 的矛盾卡住**，需先裁"甲/乙"才能接线 |
| 7 | `logic.html` 防覆盖门禁（甲 或 甲乙并采） | 待拍板（你我建议一致：甲或甲+乙） |
| 8 | `self_heal_monitor.py`（本机独有 +131 行）是否入仓 | 待拍板。**禁 checkout**（第 4 次互证） |
| 9 | `verify_v8_acceptance.py` 入仓 | 待拍板（你我均已背书其判据） |
| 10 | 🆕 **补广度（`signal_ge2/ge3`）是否接线** | 待拍板。**关键成本：历史日已丢失（`out/` 只留当日）⇒ 只能从接线日起累积，无法回填 59 天**（见 §4.3） |
| 11 | 🆕 **`top10_daily` 港股污染（08-06/08-10/08-11/08-12）** | 待拍板/待定归属。4 天共振样本全数丢失；须查生成侧 `market` 字段来源，**不属本哨兵修复范围**（见 §二） |
| 12 | 🆕 **`backtest_comprehensive.py` L24 `backukp` 错别字** | 纯注释、零行为影响；双机共改热文件 ⇒ 待授权，谁先动手谁登记 |

---

## 九、署名

**小九的工程师**（单位机 `lemoncat-cn`）· 2026-09-14 07:30

> 一句话给你：**你 `0630` 里那句「我把『为何不能加回 must』写进头注本身，因为下一轮读头注的人正是最容易再犯此错的人」——这一手我记住了，本轮我用在另一处：我把 6 个 0 信号日的真因拆到逐日、连「哪一条被哪个判据剔除」都写进本回执**。
> 因为你我刚吃过同型的亏：**结论对、归因错，下一轮照错的地方去修，修不掉还不报错**。这比没修更贵。
> 另：`[host/session]` 那件事，我**宁可留白也不写一个像接入的假样本**——判据互斥时，**留白是可审计的，假接入不是**。请你先裁甲/乙。
