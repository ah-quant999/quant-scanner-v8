# URGENT 交接单：主题空间卡 PE/PB 估值护栏数据跑批验证

| 项目 | 内容 |
|---|---|
| 发件人 | 阿狸咪的工程师 |
| 收件人 | 小九 |
| 交接时间 | 2026-09-13 16:30 |
| 主题 | PE/PB 估值护栏已接入，需小九在单位机/云端 runner 跑批验证数据写入 |

---

## 一、背景

用户（主人）在查看主题空间卡时发现：
- A 档股票数量为 0；
- 卡片说明里写“受限于当前数据层未接入个股 PE/PB，无法判断估值贵贱”。

主人要求：**直接接入个股 PE/PB，让主题空间卡具备估值护栏**。

---

## 二、我方已完成的改动（远端 main 已实装）

### 1. 数据源接入（后端）
- **Commit**: `290638a68`
- **文件**: `algorithms/fetch_stock_quote_v8.py`
- **改动**: 新增 `merge_pe_pb_from_em()`，在行情写入前从东财 `ak.stock_zh_a_spot_em()` 全市场快照补全每只股票的 `pe_ttm` / `pb_mrq`。
- **行为**: 失败静默跳过，不伪造数据，不影响现有行情字段。

### 2. 主题空间卡接入估值护栏（前端）
- **Commit**: `432de04cd`
- **文件**: `index.html`（主题空间卡，约 L6070–L6245）
- **改动**:
  - **A 档条件**: 概念命中 + 业绩预告正向 + ROE 达标 + **PE 为正且 < 50**；
  - **排序**: 成长分 + 质量分 − 高 PE 惩罚（`pe_ttm > 30` 后每高 1 倍扣 0.5 分，上限扣 20）；
  - **显示**: 每只股票卡片增加 PE/PB badge；
  - **文案**: 卡片说明改为“PE/PB 估值护栏”。

---

## 三、我方搞不定的点

**阿狸咪机无法连通东财接口**。

- 本机手动跑 `algorithms/fetch_stock_quote_v8.py` 时，调用 `ak.stock_zh_a_spot_em()` 返回 `RemoteDisconnected`。
- 新浪 `ak.stock_zh_a_spot()` 可通，但**没有 PE/PB 列**。
- 因此无法在本地验证 `merge_pe_pb_from_em()` 是否真的能写出 `pe_ttm` / `pb_mrq`。

> 云端 runner（小九单位机）通常能通东财，需由小九实际跑批验证。

---

## 四、需要小九立即做的事

### 任务 1：单位机跑 `fetch_stock_quote_v8.py` 并检查产出

在单位机（或云端 runner）执行：

```bash
python algorithms/fetch_stock_quote_v8.py
```

跑完后检查：

```bash
grep -o '"pe_ttm"' raw_data/stock_quote_v8.json | wc -l
grep -o '"pb_mrq"' raw_data/stock_quote_v8.json | wc -l
```

**期望结果**:
- `pe_ttm` / `pb_mrq` 出现次数接近全市场股票数量（约 5000+）；
- 异常值（负数、极大值、空值）比例可控；
- 常见股票如 `600519`、`000001`、`300750` 有合理 PE/PB 数值。

### 任务 2：若东财接口失败

如果小九机也拿不到数据，请：
1. 记录完整错误日志（含异常类型、堆栈）；
2. 确认 akshare 版本；
3. 回传给阿狸咪，由我方评估是否换数据源（如 Baostock 盈利接口、东财个股详情接口批量轮询）。

### 任务 3：若成功，正常跑批并验证线上

- 成功后将 `raw_data/stock_quote_v8.json` 正常进入 `update_v8.py` → `deploy_v8.py` 链路；
- Pages 发布后，线上 `data/STOCK_QUOTE.js` 应包含 `pe_ttm` / `pb_mrq`；
- 用浏览器打开主站，Ctrl+F5 硬刷新，查看主题空间卡是否有 A 档股票出现、PE/PB badge 是否显示。

---

## 五、回传标准

请小九在完成后回传以下信息：

| 项 | 内容 |
|---|---|
| 跑批结果 | 成功 / 失败 |
| 覆盖度 | `pe_ttm` 非空条数 / 总股票数 |
| 样例值 | 600519 / 000001 / 300750 的 PE/PB |
| 异常比例 | 负数 / 极大值（>1000）占比 |
| 线上验证 | Pages 发布后 A 档是否非 0 |
| 阈值建议 | PE<50 是否过严/过松 |

---

## 六、风险提示

1. **不要改动 `index.html` 主题空间卡逻辑**：当前前端逻辑已按 PE/PB 护栏写好，小九只需验证数据写入。
2. **不要伪造 PE/PB 数据**：若拿不到，宁可让 A 档暂时为 0，也不要写 0 或默认值冒充。
3. **注意网络稳定性**：东财接口偶发限流，失败时多跑一两次；若持续失败请回传日志。

---

## 七、附：相关代码位点

- `algorithms/fetch_stock_quote_v8.py`：`merge_pe_pb_from_em()`
- `index.html`：约 L6070–L6245，`__renderThemeSpace` / `__sortTheme` / `pePbBadge`
- 远端 commits：`290638a68`、`432de04cd`

---

**署名：阿狸咪的工程师**  
**交接时间：2026-09-13 16:30**
