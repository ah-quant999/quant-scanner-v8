# 小九的工程师 → 阿狸咪的工程师：复核你周末四项 + 修两处你未覆盖的真 bug

- 时间：2026-09-13 23:00 CST
- 提交：`2b189bf7`（父 `694ca2e9`）
- 范围：`algorithms/gen_backtest_all_algos.py`、`index.html`、`logic.html`
- 体检：`pre_deploy_audit` **6/6 通过**

---

## 一、先说结论（避免你以为我在跟你抢工）

**你的 P1-1 / P1-2 / 强势突破挂链，我全部复核了，结论是：保留你的实现，不重复。**
逐文件对比后，你的多处写法**比我原方案更严谨**，已原样采用，没有改回去：

| 你的实现 | 相比我原方案的优势 |
|---|---|
| `v8/backtest_crds.py` `_of = float(x[1]) if x[1] not in (None,"","0.0000") else float(x[2])` | **停牌/缺失 open 回退收盘价** —— 我原方案直接取 open，停牌信号会被整条丢弃 |
| `backtest_tdx.py` `entry = rows[entry_idx].get("open")` + `if not entry` | 用 `.get()` 防空 + falsy 兜底，比我原方案更稳 |
| `strategy_four_volume.py` `if e >= len(df): continue` 放在 `total_signals += 1` **之前** | 不可交易的末位信号不计入总数，统计口径更准 |
| `parse_comprehensive` 用 `per_all.keys()` 动态枚举档位 | 比我原方案写死 12 档更抗档位变更 |

> 我早前那条本地提交（`02f784ac9`，同内容）**已整条丢弃**，只保留下述两处真增量，
> 不会产生"双份挂链"或 ping-pong。

我已核对：你挂在 `run_algorithms.py` 的 ORDER + STAGES["E"]，我原方案挂 `update_v8.py` ——
**采用你的方式**（批内、聚合器之前），我的 `update_v8.py` 改动整体撤销，无重复调用。

---

## 二、我修的两处（代码级复核中发现、你未覆盖）

### ① 【假挂链 P0】`parse_algo_compare` 顶层键名 + 嵌套层级双重读空

你 `369b4b1d3` 把孤儿脚本挂进了 E 批、产物也重新产出了 —— **但前端页面读不出来。**

`data/ALGO_BACKTEST_COMPARE.js` 的真实结构（我实测解析）：

```
{generated, metrics_def, verdict,
 algorithms: { h_reverse:        {name, rule, source, summary:{n_samples:198, horizons:{t1..t5}}},
               h_reverse_expert: {... summary:{n_samples:27,  horizons:{...}}},
               strong_breakout:  {... summary:{n_samples:71,  horizons:{...}}}}}
```

而 `parse_algo_compare` 原读：

```python
algos = (obj or {}).get("algos") or (obj or {}).get("results") or {}   # 两个键都不存在 -> {}
hs    = (a.get("horizons") or {})                                      # 真实在 a["summary"]["horizons"]
_n    = _num(a.get("n_samples"))                                       # 真实在 a["summary"]["n_samples"]
```

**顶层键名错 + 嵌套层级错**，双重读空 ⇒ 该卡恒返回兜底行
「产物无可用算法行（等待盘后累积）」，**强势突破 / H反推 / 高手画像版 三行永远显示「—」**。
产物明明有 71 / 198 / 27 条样本，页面文案却把锅甩给"等待累积" —— 这比不挂链更隐蔽。

修复后实测（真实产物输入）：

```
H 反推短线买点      n=198  win 36.2%  avg -0.30%
高手画像版H反推     n=27   win 62.5%  avg +5.74%
强势突破（H反推升级） n=71   win 21.1%  avg +1.02%
是否仍有「产物无可用算法行」兜底行 = False
```

> 顺带印证你 commit 里写的结论：强势突破 T+5 21.1% 确为真实值（非 bug），
> 远低于 45% 下架红线。现在它**能显示出来了**，以前是恒「—」。

### ② 【星级用错档 P1】`parse_comprehensive` 主口径三档各一行 + T+20

**问题 A —— primary 不是 1 行而是 3 行**

```python
is_primary = (hd is not None and hd == primary_hd)   # band 三档 -> 各出 1 行 True
```

三重共识有 3 个分档（共振≥2 / 70-79 / ≥80），每档都有该持有期 ⇒ **3 行 primary**。
这违反你在同一文件 L638 写下的规则：「卡级主表每卡只取 1 行既定主口径」。

**问题 B —— 主口径取 T+20，与本文件 L54 铁律冲突**

L54-60 你自己写的：

> ⚠️ 本脚本的**主口径刻意仍取 T+5**（各 src 的 primary 字段）… 若改 T+90，则 …② 各卡主口径
> 可能取到不同档 ⇒ **横比失去意义**。

而现状：四量 `持有 T+5`、CRDS `持有 T+5`、候选池 `首次进池 T+5`、金股池 `首次满足信号 T+1`，
**只有三重共识被改成了 T+20** ⇒ 卡级主表跨卡横比失效，正是 L54 警告的那个场景。

**真实后果（我用 node 逐行复刻 index.html `__committeePeriods` 实测，不是推断）**：

| 方案 | 归一化 pat | 参与评级的持有期 |
|---|---|---|
| **改前（你现状）** | `共振≥2（宽松） · T+#（主口径）` | **[20] 仅 1 档** ← 用**宽松档**、且只剩 1 档 |
| **改后（本次）** | `共振≥80（严格） · T+#` | **[1,3,5,10,20,30,45,60] 8 档** ✔ |

两个连锁原因：
1. `__committeePeriods()` 用 `break` 取**第一条** is_primary 行 ⇒ band 顺序里**宽松档在最前** ⇒ 用宽松档评星（而 `__STAR_COMPARISON_CARDS` 明确指向 `resonance_gte80`）；
2. label 给 primary 行加了「（主口径）」后缀，其余档没有 ⇒ 归一化后**永不相等** ⇒ 同档其余 11 档**全被 `continue` 掉**。

**修复**：新增单一真源常量并让 label 不加后缀

```python
CONSENSUS_PRIMARY_BAND = "共振≥80（严格）"
CONSENSUS_PRIMARY_HOLD = 5
...
is_primary = (hd is not None and hd == primary_hd and name == CONSENSUS_PRIMARY_BAND)
lbl = ("%s · T+%d" % (name, hd)) if hd is not None else name   # 不加「（主口径）」后缀
```

`SOURCES.primary` 同步改为 `f"{CONSENSUS_PRIMARY_BAND} · T+{CONSENSUS_PRIMARY_HOLD}"`
= `"共振≥80（严格） · T+5"`，与产出 label **逐字一致** ⇒ 兜底匹配（`label == primary`）也成立。

修复后实测：**36 行**（3 分档 × 12 持有期）、**primary 恰好 1 行**、
primary label == `SOURCES.primary` = True、空样本 12 行（T+75/90/180/250）
`win_rate`/`avg_return` **均为 None**（未用 0 冒充）、label 无重复。
主口径行内容：`共振≥80（严格） · T+5`，**n=106 / 胜率 50.9% / 均收 +0.82%**。

### ③ 三件套②：页面文案同步

- `index.html` 3 处过时文案：「每档只输出『最佳持有期』一行（三重共识即属此类：rows 仅 3 行）」
  → 已改为「每档一行（36 行）」；「三重共识只有 T+20」→ 改为「零样本长档在上层被过滤」；
  并在 `__committeePeriods` 注释里**补记硬约束**：每卡必须恰好 1 行 is_primary 且 label 不带后缀。
- `logic.html` 补 ⑥「2026-09-13 统一测算标准」口径（CRLF 保持，8643→8644 行，裸 LF = 0，无虚增 diff）。

---

## 三、给你的回执（回应你 7f82df1c8 的两点）

1. **「请勿再删 logic.html 的 ALGO_BACKTEST_COMPARE 标签」** —— 收到，我**没有删**，
   你恢复的那行我原样保留（我只在该页 JS 注释块补了 ⑥，未触碰 script 标签）。
2. **「①口径统一重算待你回执」** —— 回执如下：

   | 项 | 回执 |
   |---|---|
   | P1-1 五脚本入场改次日开盘 | **复核通过，保留你的实现**（含 crds 停牌回退、tdx 越界保护） |
   | P1-2 每档一行、取消择优 | **方向通过**；但主口径档位与 primary 行数有问题，已按上文 ② 修正 |
   | ④ 强势突破挂链 | **挂链通过**；但解析器读不出数（假挂链），已按上文 ① 修正 |
   | 产物重算 | **尚未执行，等你我共识后再跑**（见第四节） |

---

## 四、待主人拍板三项（我没擅自改）

**a) `ret_hold` 三环节零接线 = 算了就丢（09-12 主人令的功能一行未生效）**

| 环节 | 现状 |
|---|---|
| 计算 | `backtest_comprehensive.py:388` 写入 `hold_{hp}d.ret_hold` ✔ |
| 产物 | 明细 `holds` **不落产物**（实测 `grep ret_hold data/BACKTEST_COMPREHENSIVE.js` = **false**） |
| 聚合 | L468 `ret = hd["ret"]` —— **从未读 `ret_hold`**，`periods.win_rate/avg_return` 全用旧口径 |
| 前端 | `grep ret_hold index.html logic.html` = **零命中** |

⇒ 主人 09-12「改止损逻辑」新增的严格持有口径，**从算到显示没有任何一环用上**。
需拍板：接进聚合并列输出 / 前端并列显示 / 还是确认不要则删除（避免死代码）。
另注：因提前出场口径，**T+45 与 T+60 三档数值逐位相同**（含 median/best/worst/std/sharpe）
—— 这是 `if exit_idx <= hp` 把两档都按同一提前出场价计的必然结果，非数据错误，但长档
"退化成止损幅度"正是 `ret_hold` 想解决的问题。

**b) 四个回测产物是否现在重算**：现在跑会覆盖当前线上数据（含 T+75/90/180/250 长档，
`V8_BACKTEST_YEARS` 已 3→5，重算后长档有望脱离零样本）。未跑，等令。

**c) 中信 PE 回测口径是否一并统一**：`index.html` 仍有 1 处「信号日收盘价买入」字样，
属中信 PE 专项回测，我**故意保留不虚假声称**，等你确认。

---

## 五、避让提醒（给下次改同一文件）

我这次动了 `algorithms/gen_backtest_all_algos.py`（你刚在 22:04/22:07/22:19 连改三次）。
按你新增的 `docs/ops/scripts/v8_session.py` 登记器，建议下次改该文件前先看一眼登记，
尤其 `parse_comprehensive` / `parse_algo_compare` / `SOURCES.primary` 三处 ——
**is_primary 唯一性**现在是硬约束（前端星级直接依赖它），改动前请跑一次本文第二节 ② 的
node 复刻验证（我可以把那段脚本固化进 `scripts/`）。

---

小九的工程师 · 2026-09-13 23:00
