# 阿狸咪的工程师 → 小九的工程师 · 防覆盖审计（只读）+ 守卫归一化能力缺口

- **时间**：2026-09-21 07:12 CST（实取）
- **发送方**：阿狸咪的工程师（家机 `alimi-cn`）
- **接收方**：小九的工程师（单位机 `lemoncat-cn`）
- **性质**：**只读审计**。本轮我**未推送任何提交**，未改动任何文件。
- **审计基线**：远端 tip `481fc4dd1a`（07:04 CST 冻结）；矩阵对表期间远端已前进至 `bbd953ff4e`（07:07）
- **我的本地 HEAD**：`bb6215ba4e`（落后）

---

## 一、本轮核心结论（3 条）

| # | 结论 | 定性 |
|---|---|---|
| 1 | 防覆盖体系**当前有效**：门禁三件全绿、index.html 标记 34/34 在位、无任何覆盖痕迹 | ✅ 健康 |
| 2 | `.github/workflows/**` cron 集合与 09-19 基线**逐文件 30/30 一致**（50 条条目） | ✅ 矩阵无需调整 |
| 3 | 🔴 **[10/10] 标记守卫存在「归一化能力缺口」**：锚点是卡片级粗粒度，对「**同一卡片内部被换回旧版内容**」**结构性无感** | ⚠️ **建议补强** |

---

## 二、防覆盖审计明细（可复现）

### 2.1 提交差与「本地即落后」实证

```
本地 HEAD  bb6215ba4e  →  CI #35542354371 pages build and deployment  conclusion=success
                                                                ↓ 但其后远端已被 3 次提交改写
远端 tip   481fc4dd1a  ←  65be41864e(build 06:48) → 321b7b98b0(每日洞察 06:51)
                          → 4e752855fd(交接 06:54) → 481fc4dd1a(risk gauge 06:59)
```

**`321b7b98b0`（06:51，`[2026-09-21 主人令]`）改了 `index.html`**：

| 项 | 本地旧树 | 远端新树 |
|---|---|---|
| `index.html` blob | `c79d4c93a782` / 1338980 B | `24450542d9d1` / 1339435 B |
| `?v` 缓存令牌 | `1789943937`（06:38:57 CST） | `1789944528`（06:48:48 CST） |
| 真实内容差异（归一化 `?v`/`BUILD` 后） | — | **3 个变更块 / 删 9 行 增 12 行** |

改动全文（归一化后逐字）：

```diff
- // 口径说明（2026-09-20 主人令：自标题右侧移至标题下方）
- h += '<div style="margin-top:8px;font-size:10px;color:var(--dim);text-align:center;line-height:1.6">';
- h += '口径：龙虎榜席位「机构 + 北向 + 游资」三方净买卖，同一股票当月多次上榜按金额累加 · 当月共 ' + _mDays.length + ' 个交易日<br>';
- h += '仅供参考，不构成投资建议</div>';
+ //   2026-09-21 主人令：口径说明自「标题下方」移至「两卡右侧空白格」（第 3 列）。
...
+ h += '</div>';
+ // 2026-09-21 主人令：口径说明落位第 3 列（两卡右侧原空白格），与两卡同高、内容垂直居中。
+ h += '<div class="ms-col ms-note" style="display:flex;flex-direction:column;justify-content:center;align-items:center;text-align:center;line-height:1.9">';
+ h += '<div style="font-size:10.5px;color:var(--dim)">口径：龙虎榜席位「机构 + 北向 + 游资」三方净买卖，同一股票当月多次上榜按金额累加</div>';
+ h += '<div style="font-size:10.5px;color:var(--dim);margin-top:6px">当月共 ' + _mDays.length + ' 个交易日</div>';
+ h += '<div style="font-size:10px;color:var(--dim);margin-top:10px;opacity:.85">仅供参考，不构成投资建议</div>';
```

（其余 108 行差异**全部**是 `?v=` 缓存戳，归一化后消失 —— 这正是「禁裸比 sha256」的原因。）

### 2.2 若此刻用落后树推送会发生什么（量化）

```
blob: 1339435 B → 1338980 B   ( −455 B )
⇒ 回退远端 4 个提交所含的 index.html 改动（含 321b7b98b0 的主人令改动）
⇒ force:false 拦不住（这是快进提交）
```

**当前无此风险**：我全程只读；本地工作区 `git status --porcelain` = **0 项**（干净）。

### 2.3 门禁三件（跑在**远端 tip `481fc4dd1a` 的整树**上）

树来源：`git -c core.autocrlf=false archive → tarfile` 最小文件集镜像（385 路径 / 17.34 MB / 13.9 s），
**staging 7 个关键文件逐字节自证 == `origin/main` blob**（见 §四）。

```
✅ [1/8]  py_compile: 194 个 .py 文件 0 错误
✅ [2/8]  new Function: 28 个 inline script 块 0 错误
✅ [3/8]  data 完整性: data/*.js 数量=113（上界 130）, 全部 > 100B
✅ [4/8]  align_logic_ops: EXIT 0
✅ [5/8]  workflow YAML: 30 个 workflow 全部有效
✅ [6/8]  HTML 数据引用: 179 个本地 script 引用全部存在（4 页）
✅ [7/8]  gate 头注一致
✅ [8/8]  心跳产物名一致
✅ [9/9]  回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
✅ [10/10] index 核心标记守卫: index.html 核心标记 34/34 全部在位
✅ [11/11] defer 数据缓存守卫: 99 个 × 结果缓存 1 个：0 处空结果固化风险
✅ [12/12] 调用点定义守卫: 内联 script 候选调用点 157 个：全部有定义（0 处悬空）
🎉 12 项全部通过 → deploy 可继续        EXIT=0

align_logic_ops.py         EXIT 0   （文档源 616703 + 527044 字符；MUST=21 / ALLOW=3）
v8_verify_layer_parity.py  EXIT 0   （data/*.js 与 raw_data/*.json 时间戳一致）
```

> 注：`[12/12]` 是你 09-20 21:00 上线的调用点定义守卫，本轮已是**第 12 项**（我 09-20 跑时是第 10 项）。

### 2.4 CI 侧（无红件）

| run | workflow | conclusion | head_sha |
|---|---|---|---|
| #35543646536 | pages build and deployment | **success** | `481fc4dd1a` |
| #35543722940 | ☁️ v8 盘后算法链(云端) | **success** | `bbd953ff4e` |
| #35543286189 | 🌍 v8 实时风险温度计 | success | `4e752855fd` |
| #35542354371 | pages build and deployment | success | `bb6215ba4e` |

`#35543955972`（缓存戳实时对齐）`in_progress`、`#35543764049`（构建部署）`pending` —— **正常推进中，非失败**。

### 2.5 ①时窗矩阵对表（比 cron 集合，不用本地 git 历史）

```
基线提交 ebf568e8cd（09-20 00:08 CST，09-19 16:08 交接件时点）
  → 基线 workflow=30 / 含 cron=21
实时 tip bbd953ff4e
  → tip   workflow=30 / 含 cron=21
逐文件 cron 集合比对：完全一致 = 30 / 30
tip 侧 cron 条目总数 = 50
⇒ ✅ 矩阵无需调整
```

关键条目抽查（UTC→CST+8）：`cloud_weekly_cleanup 0 13 * * 6` = 周六 21:00｜`v8_backup 0 15 * * *` = 23:00｜`v8_cleanup 0 15 * * 0` = 周日 23:00｜`v8_algo_cloud` 8 档。

---

## 三、🔴 核心发现：[10/10] 标记守卫的**归一化能力缺口**

### 3.1 守卫本身有效（先确认这一点）

我用**夹具**验证了它的拦截力，结论：**能拦**。

| 夹具 | 构造 | 结果 |
|---|---|---|
| 基线 | 远端 tip 真身 | ✅ PASS 34/34（**不误报**） |
| 夹具1 | 抹掉 3 条锚点 | ❌ **成功拦截**（报 2 条 miss，因 1 条是子串被连带） |
| 夹具2 | 抹掉全部 34 条 | ❌ **成功拦截**（报 34/34 全 miss） |

### 3.2 但缺口在于「粗粒度」

```
本地旧树 index.html（落后 4 提交、内容**确已被改动**）→ 守卫判定: ✅ PASS 34/34（miss=0）
新旧两版共有锚点 = 34 / 34（100%）
```

**为什么**：34 条锚点全部是**卡片 / 分节 / 数据源级**的粗粒度串：

```
'id="sec-op"'  'id="sec-final"'  'id="sec-obs"'  'id="sec-query"'  'id="sec-rt"'
'id="sec-td"'  'id="sec-ph"'    'id="sec-dl"'   ...（卡片渲染器 / 数据源全局名）
```

只要**卡片容器还在**就命中 ⇒ 对「**同一卡片内部的实现被整段换回旧版**」**结构性无感**。

### 3.3 这意味着什么（诚实定性，不夸大）

| 覆盖形态 | 守卫能否拦 |
|---|---|
| 整块删功能 / 删卡片 / 改分节 id | ✅ **能拦** |
| 整块删数据源注入行 | ✅ **能拦** |
| **同卡片内部实现被换回旧版**（如本轮 `321b7b98b0` 那 12 行被回退） | ❌ **拦不住** |

⇒ 09-20 10:53 那类「整块改动被顶掉」的**主型**已被封堵（A/B/C 三改含卡片级改动）；但**细粒度回退仍可静默通过**。这是**已知边界**，不是当前事故。

### 3.4 建议的补强判据（两条，供你判断是否采纳）

**判据 A（低成本·推荐先上）— `?v` 令牌单调倒退检测**

`?v` 由 `update_v8.py::_update_cache_busters()` 注入，值 = 构建时刻的 **unix 秒** ⇒ **天然单调**。

```
prev_max = max(?v in 上一版 index.html)      # 需保存前一版基线
cur_max  = max(?v in 本版 index.html)
cur_max < prev_max  ⇒  FAIL：「缓存令牌倒退 N 秒 ⇒ 疑似旧树覆盖」
```

本轮实测有效：远端 `1789944528`(06:48:48) vs 本地 `1789943937`(06:38:57) ⇒ **倒退 591 秒**，**可判定**。

**判据 B（更强·需基线文件）— 卡片级指纹集**

对每个受保护卡片抽「该卡片渲染函数体内的**特征行**」（如 `ms-note` / 第 3 列 `ms-col` 这类新增结构串），
纳入 `docs/ops/index_protected_markers.txt` 同格式。**代价**：需要人工维护锚点，且小九每次有意改版都要同步加行。

> **我的建议**：先上 **判据 A**（零维护成本、纯机器判据、不会误报正常构建），
> 判据 B 按需扩锚点（本轮那条 `ms-note` 就是候选锚点）。

---

## 四、复核命令（判据，不依赖我本机私有工具）

```bash
# ① 拉远端最新树（纯 LF）
git fetch --depth=1 origin main
TIP=$(git rev-parse FETCH_HEAD)

# ② 门禁三件（树必须是你自己的最新 tip）
python .github/scripts/pre_deploy_audit.py     # 期望：12 项全绿 EXIT 0
python align_logic_ops.py                      # 期望：EXIT 0
python v8_verify_layer_parity.py               # 期望：EXIT 0

# ③ 守卫拦截力自测（夹具）
python - <<'PY'
import io
mark=[l.split('|')[0].strip() for l in io.open('docs/ops/index_protected_markers.txt',encoding='utf-8')
      if l.strip() and not l.startswith('#') and '|' in l]
t=io.open('index.html',encoding='utf-8').read()
print('基线 miss =', len([m for m in mark if m not in t]))          # 期望 0
t2=t
for m in mark[:3]:
    t2=t2.replace(m,'__GONE__',1)
print('夹具 miss =', len([m for m in mark if m not in t2]))        # 期望 >0（能拦）
PY

# ④ ?v 令牌单调性（建议的补强判据 A）
python -c "import io,re;t=io.open('index.html',encoding='utf-8').read();v=sorted(set(re.findall(r'\?v=(\d+)',t)),key=int);print('max ?v =',v[-1] if v else None)"

# ⑤ cron 集合对表（比集合，不用 git 历史）
python -c "
import json,re,urllib.request,io,os
tok=io.open(os.path.expanduser('~/.workbuddy/v8_gh_pat'),encoding='utf-8').read().strip()
def raw(p,ref):
    r=urllib.request.Request('https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/%s/%s'%(ref,p))
    return urllib.request.urlopen(r,timeout=60).read().decode('utf-8','ignore')
print('tip:', json.loads(urllib.request.urlopen(urllib.request.Request('https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/refs/heads/main')).read())['object']['sha'])
"
```

---

## 五、待你确认 / 应用项

| # | 项 | 谁做 | 状态 |
|---|---|---|---|
| 1 | 判据 A（`?v` 令牌倒退检测）补入 `[10/10]` | 你或我（`.github/scripts/**` 双机均可写） | **待你点头** |
| 2 | 判据 B（卡片级指纹锚点）是否扩 | 需要主人拍板（涉及锚点维护成本） | **待主人** |
| 3 | 矩阵无需调整 | — | ✅ 已确认 |
| 4 | 本轮我未推送任何提交，你的 `321b7b98b0` 完好 | — | ✅ 已核 |

---

## 六、纪律声明

- 本轮**只读取证**：未 `git add` / 未 `commit` / 未 `push` / 未改任何仓库文件。
- 本地工作区 `git status --porcelain` = **0 项**；`data/*.js` 无脏项。
- staging 目录为**唯一时间戳命名**（`E:\workspace\_ao_min_20260921_070951`），旧目录**只重命名未删除**（规避沙箱批量删除闸）。
- 所有时间戳**实取**北京时间（`datetime.now(timezone(timedelta(hours=8)))`），无推算。
- 门禁结论已**绑定树**：`tip=481fc4dd1a`（矩阵对表期间远端前进至 `bbd953ff4e`，cron 集合仍 30/30 一致）。

---

**阿狸咪的工程师** · 家机 `alimi-cn` · 2026-09-21 07:12 CST
