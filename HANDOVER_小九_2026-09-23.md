# HANDOVER · 小九 → 阿狸咪 · 2026-09-23

> **更新于 2026-09-23 12:12 CST 自动更新**（本档由小九 12:00 槽位自动化生成/整份覆盖）
> 生成口径：C 盘主日志（15,157 B，截至 11:41）+ E 盘工作区哨兵日志（12,052 B，截至 09:09）**双源合并**，
> 全部 SHA 以 **远端实测**为准（`git log origin/main` 实取，非记忆值）；状态快照取 **12:02–12:12 CST 线上真身**。
> **本档禁写死「本文自身提交的 sha」**（`main` 被 CI 逐分钟 churn，短 sha 分钟级失效）——需要时请用文末「现取命令」。

---

## 一、背景

- **交易日**（2026-09-23 周三）。分时归属：小九白天独家 7:45–17:45，本档生成于小九时段。
- v8 双机共写 `quant-scanner-v8`（GitHub: ah-quant999），单文件 `index.html`（线上 **1,340,493 B**）+ `data/*.js` 注入 `window.X`。
- 🔴 **今日主线**：① **P0 `index.html` 尾部截断家族第 3 次呼吸**（阿狸咪两轮根治 + 小九回执复核并**推翻其归因**）；
  ② **主人拍板删除「暂未上架›解禁&机构关注」因子观测卡整卡**（双机同时落地，含 `data/INST_COVERAGE.js` 退役）；
  ③ **小九落地缺口② `cn-fetch-writer-version-drift`**（`soft_15` 四项硬断言，封堵「坏文件照样进 main」）；
  ④ **双机协同操作协议正式固化**（`docs/ops/DUAL_MACHINE_PROTOCOL.md`，5 节）。
- 本次交接自动化执行：`git fetch origin` 成功（`27dd0fd6e..dd1325e46`）；**`merge --ff-only` 如预期失败**（`refusing to merge unrelated histories`，浅克隆伪脱钩，`.git/shallow`）**⇒ 零副作用，未动主工作树任何 WIP**（porcelain 46 项零触碰），走净室隔离索引推送。

---

## 二、今日已完成改动（真实 commit SHA + 影响）

> 全部经 `git log origin/main` / `git rev-parse` 或 GitHub API 核实**确在远端**。标注 `[API核实]` 者为本地浅克隆查不到、经 API `commits/{sha}` 实取确认。

### 2.1 🔴 P0 lineage：`index.html` 尾部 183 行截断 → 根治 → 二次复发 → 门禁封堵（阿狸咪主修 + 小九回执）

| commit | 时刻(CST) | 内容 | 影响 |
|---|---|---|---|
| **`d8c2b9eef`** | 00:2x | 小九回执 `2026-09-23_0030`（16,251 B / 258 行） | 复核 2247/2340 两份 P0，**纠正归因方向** |
| **`919f9bef4`** | 08:48 | 小九回执 `2026-09-23_0848`（13,202 B / blob `c37e50cdf`） | 回执 0830 协议档 + 0839 英文命名档 |

- **症状**：线上 `index.html` 尾部整段被删，**A2 懒加载块缺失 ⇒ 整站白屏**。
- 🔴 **小九侧真根因确认（纠正我原「旧树」方向，也纠正阿狸咪「自托管 runner 跑旧码」归因）**：
  真根因 = **`api_push_raw._blob_text` 把断流半截当成功** ⇒ **读坏了**（非「用旧码重写」）。
  铁证：`7c78439fe8` 的 index.html 仅 **2 个 hunk** —— L1782 **合法 `?v` 重戳**（说明 `--only-cache-busters` 真跑了）
  + **尾部 187 行 → 4 行 全删除**。
- 🔴 **归因更正请求（务必对齐口径）**：阿狸咪把 P0 归因「自托管 runner 冷启动同步未刷新/跑旧副本」**不成立** ——
  `v8_cn_fetch_cloud.yml` **L106 `runs-on: ubuntu-latest`** + L257 `actions/checkout@v4` + `soft_15` 内 `git reset --hard FETCH_HEAD`
  ⇒ **结构上无「跑旧码」机制**。建议挂账项名统一改为 **`cn-fetch-truncated-read-p0`**。
- **`api_push_raw.py` 根治已落地**（阿狸咪侧）：78,413 B / `require_full`×5 / `(".json",".js")` L1093 / 双断言 L769+L785 / `_TS_KEYS` 不含 `republish_time`。
- **门禁封堵（小九 `182736844f`）**：`docs/ops/index_protected_markers.txt` 登记 `</html>` 与 `LHB_HISTORY` 两个**尾部锚点**（35→37 条），
  堵住「截断文件穿透门禁」（原清单全是**中部/头部**锚点，尾部被砍仍在位 ⇒ 门禁全绿骗过）。
- **小九认错 1 项**：上轮判 `unlockCoverCard` 命中 0 是错的 —— 只 grep 了**锚点清单**未 grep **index.html 正文**；
  实际命中 1 处 **L15729**，而 `__renderUnlockCover` 体是 L15465–15520 ⇒ 确不在体内，属真缺口。

### 2.2 🔴 「因子观测卡」整卡退役（主人拍板 · 双机同落）

| commit | 时刻(CST) | 内容 |
|---|---|---|
| **`8991c96ad`** | 12:05 | `refactor(unlock-cover)`：**主人拍板删除「暂未上架›解禁&机构关注」整卡**。`index.html` 删注入/渲染函数/挂载/卡体/派发 **5 处**；`update_v8.py` 删 `CATEGORY_MAP` 映射+freq+`INST_COVERAGE` 转换分支 |
| **`ba85f9cde`** | 12:05 | `chore(data)`：`data/INST_COVERAGE.js` **退役**（与解禁日历重复 + 机构关注未验证）；**raw `analyst_ratings.json` 保留**（`generate_top10.py` 消费） |
| **`dd1325e46`** | 12:08 | `chore(handoff)`：`unlockcover-factor-observation-card` doing→done；`baihechou` 项加 09-23 后记（`__renderUnlockCover` 随卡退役）**[远端 tip]** |

- **拍板理由**：解禁段与盘后页「解禁日历」**同源纯重复**；机构关注（研报热度）**从未离线回测验证**。
- ⚠️ `id_content_hashes` **未重算**（双机约定：**小九侧不重算，漂移由阿狸咪侧统一收口**）。

### 2.3 🔴 缺口② `cn-fetch-writer-version-drift` 小九侧落地（本槽位核心交付）

- **commit `631123e0bd8a`** / blob `4c42862e9c` / **85,036 B / md5 `3a1d7c388f2a` 逐字节自验一致**；登记 `73dc4e93d5` `[API核实]`（HANDOFF.yaml blob `4a8b377f2a` / 270,771 B）。
- **落地真身**：`.github/workflows/v8_cn_fetch_cloud.yml` step `soft_15` —— 原 `git add -f index.html` **前无任何完整性断言**，
  且 `continue-on-error: true` ⇒ **坏文件照样进 main**（`update_v8.py::_atomic_write_index_html` 是**软守卫**，只匹配文本末 200 字含 `</html>`，防不住中间截断）。
- **修法**：在 `--only-cache-busters` 后、`git add` **前**插 **四项硬断言**（heredoc `PYEOF_IDXGUARD`）：
  ① 行数 ≥18,800 ② 末 200 字含 `</html>` ③ A2 三锚 `{name:'STOCK_LIST'/'STOCK_QUOTE'/'LHB_HISTORY'` ④ 字节 ≥1,250,000；
  任一不过 `exit 1`（不 add/commit/push）+ `::error title=v8-index-guard::` annotation。**刻意保留 `continue-on-error`**。
- **净室 A/B 五场景全符合预期**：c1 正常 exit0 ✅ / c2 删尾 183 行 exit1 ✅ / c3 A2 锚被挖 exit1 ✅ / c4 无 `</html>` exit1 ✅ / c5 缩水 20 万 B exit1 ✅。
- **终验**：`[13/13]` ids 超集 + seq 单调 ✅ / `[10/10]` 37 条锚点 0 命中缺失 ✅ / index.html 1,349,692 B · 19,176 行 · 尾闭合。
- 🔴 **状态保持 `pending-verify`**（该 workflow 定时触发，补丁**不立刻产生 run** ⇒ 未取得 run 终验，**未擅自置 done**）。
  **待阿狸咪/下一档补验**：下一个 cn fetch 档真跑后核对 job 日志「index.html 完整性断言通过」。

### 2.4 双机协同操作协议正式固化

- **`docs/ops/DUAL_MACHINE_PROTOCOL.md`**（4,971 B / blob `ce5794085`，**五节全在位** ✅），commit 由阿狸咪 08:30 档落地。
- 小九侧 08:48 已回执确认；协议**未挂 `AGENTS.md` 入口**（小九**未擅改**，附三条理由，**待裁定**）。

### 2.5 「机构研究·AI 解析建议」出稿上线（小九，automation `30e1b099`）

- **commit `9841601c35`** `[API核实]`（2026-09-23 00:59Z = 08:59 CST）。`data/maharo_insights.js` **25,115 B**（daily 净 5128 字）。
- 素材 `out/guanlan_reports.json` 与 09-22 已上线稿**完全同批**（20 条 / `2026-09-21 ~ 2026-09-22`）⇒ **第二轮独立解读**，
  主轴（第五代）：**「可计量科目取代市场规模愿景」+「周期定性中段、估值手段后段」**；刻意使用**上一稿完全未用的 7 条**。
- 护栏：`_guanlan_refresh_state.json` `checked_at=08:45:12`、`before_max=after_max=2026-09-22`、`ok=true` ⇒ **刷新跑了，源当天无新帖**（非任务哑火）。
- 闸门 36 项全 ✅；Pages 实测 25,115 B、`daily.date=2026-09-23`。

### 2.6 小九侧其他（本机工具 / 巡检，**未动 v8 仓**）

- **11:38 staging 重建器「非原子落盘」根治**：首跑 exit 0 但盘上 `index.html.content` **整个消失** ⇒
  改 `.tmp`+`os.replace` 原子落盘（含 fsync）+ meta 增 `content_bytes`/`content_md5` 指纹 + **落盘完整性自检**（残缺即 `SystemExit(1)`）。
  重跑 `✅✅ 落盘完整性自检通过`，`base_tip=99ffa6ce68bf…`。**只改本机 `~/.workbuddy/scripts/v8_staging_rebuild.py`**。
- **死锁巡检 6 轮全绿**（第 82/84/85/86/87 轮 + 03:17 轮）：**无死锁**，超 90 分未完成 run **恒 0**，未 cancel / 未 dispatch。
- **runner 本地守护 2 轮**（02:03 / 08:28）：`status=OK`、**`actions_taken=[]` 零自愈**；Listener PID 10140 连续第 9 轮同 PID 稳定。

---

## 三、当前系统状态（快照 2026-09-23 12:02–12:12 CST · 线上真身）

| 项目 | 实测值 | 判读 |
|---|---|---|
| **站点** | `index.html` **200** / `logic.html` **200** / `HEALTH_CHECK.js` **200** | ✅ 全通 |
| **线上 index.html** | **1,340,493 B** / blob `2811f817c638` | ✅ 正常量级（**截断未第 4 次复发**） |
| **线上 HEALTH_CHECK** | `updated 2026-09-23 12:02:23` · **overall=ok** · `ok120 / warn0 / fail0 / limited2 / info0 / total122` | ✅✅ **全绿**（09-22 的 fail2 已清零） |
| └ limited 2 项 | `MACRO_KSHAPE`（事件驱动，非故障）+ 另一静态项 | ✅ 设计内 |
| **远端 tip** | **`dd1325e46`**（12:08，`dd1325e46408dcff5aed073c730487456c69a8b4`） | ✅ `ls-remote` 复核一致 |
| **今日 main 提交** | `git rev-list --count --since` = **20**（截至今晨窗口）· 全天提交持续 churn 中 | CI 高频 |
| **今日 runs** | 总数 **471**：completed 468（**success 328 / cancelled 131 / failure 9**）+ in_progress 2 + pending 1 | 见 §四告警 |
| **自托管 runner** | `lemoncat-cn` **online / busy=False**（`runner_status.json` 11:58:20，最新 run `35808464610` completed，**连续失败 0 / checkout 失败 0**） | ✅ |
| └ 服务状态 | `service.status = **NOT_FOUND**`（1060） | 🟡 **已知待办**（非新问题） |
| **双机心跳** | `HB_XIAOJIU` **11:58:20**（`lemoncat-cn`，source `v8_runner_guard`）· `HB_ALIMI` **10:03:05**（`alimi-cn`） | ✅ 双活（阿狸咪距今 ~2.1h，正常） |
| **自动化** | `automations`：**ACTIVE 129 / PAUSED 24** | 🟡 较 09-22 的 126/24 **ACTIVE 又 +3**（见 §五 治理提醒） |
| **本地工作树** | HEAD `4a1d32988`（`local-worktree-behind`，**实测落后 250 提交**）· porcelain **46** · 暂存位 **0** | ⚠️ 见 §四 R3 |

---

## 四、需阿狸咪注意 / 待办 / 风险

### 🔴 R1. 交接档「写死 commit sha」不可核验（**P0-新② 延续，强烈建议改约**）
本仓 `main` 被 `v8-cache-bot` **逐分钟 force-update**，短 sha **分钟级失效**；且浅克隆本地 `cat-file` 常查不到。
**建议改约**：交接档**禁写死 sha**，改「**路径 + 字节数 + md5**」（内容不变量）；**不写本文自身提交的 sha**，改给现取命令。
建议落 `docs/ops/handover/README.md`。→ **待主人拍板**。

### 🔴 R2. 缺口② `cn-fetch-writer-version-drift` **run 终验未完成**（保持 `pending-verify`）
补丁已推（`631123e0bd8a`）但 workflow 定时触发 ⇒ **尚无真跑档**。
**请阿狸咪/下一档补验**：下一个 `v8_cn_fetch_cloud` 真跑后，核对 job 日志出现「**index.html 完整性断言通过**」，
且 `soft_15` 的四项断言全部执行（未因 `continue-on-error` 被吞）。**验证通过再置 done**。

### 🔴 R3. `local-worktree-behind` 实际严重得多（**登记值与实测严重不符**）
HANDOFF.yaml 登记「落后 5→9 提交」，**实测 `git rev-list --count HEAD..origin/main` = 250**，本机 HEAD `4a1d32988daa`，工作树 **39–46 项未暂存**。
**需判**：哪些 WIP 有真实价值（勿一律丢弃，也勿整文件直推——参见 §六 血训）。
**小九侧不擅动**（红线：他人活跃写入）。

### 🔴 R4. `stash@{0}` 内容与阿狸咪截图描述不符（**待澄清**）
实测 `stash@{0}` **只含 `data/STOCK_QUOTE.js`（+1 / -1）**，**非**截图所述「HANDOFF.yaml 2662 行」。
阿狸咪主动请求 reconcile ⇒ **需双机对齐口径**（小九侧无法访问阿狸咪机 stash）。

### 🔴 R5. `pages build and deployment` **不消费 v8 门禁**（两次 P0 的第 2、3 个活样本）
⇒ `pages` 那条链的绿/红与 v8 自身门禁**无关联**，不能作为质量证据。→ **待主人拍板**（含 `check_site()` 升级为「GET + 尾部闭合 + 字节不缩水」；ledger `id_content_hashes` 是否升 hard 门禁——**小九建议先 soft**）。

### 🟡 R6. `ledger.id_content_hashes` **目前无法被独立复算**（锚实际失效）
按官方口径穷举（切片 4 变体 × updated 4 处理 × tail 3 × 编码 2）**60/60 全部对不上**；
用 `d7b832bd5e`（ledger 与 yaml 同笔提交）复算亦全部对不上。
**好在 `[13/13]` 门禁只断言 ids 超集 + `next_id_seq` 单调，不校验 `id_content_hashes`** ⇒ 不影响 CI。
**本次未动 ledger**（未增删 id、未动 `next_id_seq`）。

### 🟡 R7. 陈旧产物延续（**第 3 轮登记 · 转主人决策**）
- **`CANDIDATE_BACKTEST.js` / `GOLD_POOL_BACKTEST.js`**：`update_time` 冻结 **2026-09-14 22:50:24（8.5 天）**，sha 与前轮同 ⇒ 数据/算法层未产出。
- **`v8_algo_cloud` 链内失败签名已迁移**：由「推送步顶 20 分闸门」→ **「`?v` 同步步即时 failure」**（`#35715905719` 0.4 分 / `#35713575855` 0.1 分）；
  08:28 守护发现 **`#2058 success@00:03Z`**（此前 `#2055/#2056` step16 硬失败已收敛）⇒ **呈摆动非恶化**。
  另链内失败已由「仅 step15」扩为 **step11/14/15/16 多步同族（连续 2 轮）** ⇒ 属链内业务步骤、**非 runner 进程层面**，未触发本地自愈。
- **新登 `algo-step16-sanity-hardfail`**（commit `cb5460b99`）：step16 豁免已落地 · **待真跑档终验**。

### 🟡 R8. 16 项 `todo` 中 owner=小九 的 6 项（**待小九后续推进**）
`cachebot-build-race` / `concurrency-block-bucket` / `build13-step-timeout-minutes` /
`maharo-daily-s4u` / `defer-cache-empty-freeze` / `factorlab-conflict-tree-pollution`。

### 🟡 R9. 线上真身 `docs/ops/HANDOFF.yaml` 状态分布（60 item，`267,439 B`）
**done 20 / todo 16 / pending-verify 15 / doing 8 / blocked 1**；owner：shared 30 / 小九 19 / 阿狸咪的工程师 6 / 阿狸咪 3 / alimi-cn 1 / 小九(lemoncat-cn) 1。
⚠️ **4 项 P0 pending-verify 实测 owner 全是「阿狸咪的工程师」/阿狸咪** —— 与阿狸咪截图所述「owner=小九 四项」**不符**（以权威源为准）。

### 🔴 R10. 覆盖事故确证（09-23 08:14，已修复但需警觉）
`d7b832bd5e` 以**旧基线 `e9aceb16d2`** 落下，把 23:5x 的 markers 封堵改动整体退回旧版
（diff 逐行取证：删 `</html>` / `{name:'LHB_HISTORY'` / `window.__renderBoardFirstLimit` / `id="unlockCoverCard"`，恢复 `window.__renderHunterBacktest` / `ms-note-card`）
⇒ 小九 08:32 已**重新推送 37 条**（commit `182736844f`）。**`force:false` 不是护栏**（见 §六）。

### 📌 需阿狸咪裁定的 3 项（小九侧无法执行 / 不宜擅改）
1. `2026-09-23_0839` 档的 `index.html` **13 行真实改动**（最终推荐排序一致性 UI）：**选项 A 小九侧无法执行**（`stash@{0}` 在阿狸咪机）⇒ **请导出补丁**；采纳与否建议上抛主人。
2. 协议文档 `DUAL_MACHINE_PROTOCOL.md` **未挂 `AGENTS.md` 入口** ⇒ 小九**未擅改**（附三条理由），**待裁定**。
3. `id_content_hashes` 漂移收口归属（双机约定小九不重算）⇒ 请确认阿狸咪侧统一收口节奏。

---

## 五、🟡 治理提醒（token / 堆积，**非阻塞**）

- **自动化 ACTIVE 129 / PAUSED 24** ⇒ 较 09-22（126/24）**ACTIVE +3**，较 09-21（120/21）**累计 +9**。趋势**持续上行**，且 `automation_runs` 今日流水在本机 DB 读数为 0（**可能为 DB 路径/口径问题，非「没跑」**——按 §六 铁律，**禁用单字段读数推广为子系统死活**）。
- **今日 GitHub runs 471 条**：**cancelled 131（27.8%）+ failure 9** —— cancelled 占比偏高，请夜班留意是否属**并发组互斥**正常形态；failure 9 需逐条判是否落在白名单。
- **建议**：主人侧对「空间里的运行历史堆积」与 ACTIVE 任务数做一轮收敛（已有 `automation-token-diet` skill 可执行）。

---

## 六、纪律 / 坑（本轮新增 + 沿革关键，**动手前必读**）

> 以下每条均有本轮或近期**血证**，不是理论风险。

1. 🔴 **「我推成了」≠「线上是我的版本」**。`force:false` **不是护栏**：基线落后对 git 是**快进** ⇒ 旧底稿整文件直推**照样绿**、照样覆盖他人改动（09-20 `742370d85`→`37490620d` 02:43→02:53 顶掉；09-23 `d7b832bd5e` 退回 markers）。**推前必做基底断言**：取线上 blob sha + 字节数与本地比对，不等则一律走「以线上真身为基底的补丁重放」。
2. 🔴 **GitHub Contents API 对 >1MB 文件静默截断**（1.34MB 连续 3 次返 `TRUNC(0)`，而 `size` 元数据正常）⇒ 大文件**必须走 `git/blobs/{sha}` + `Accept: application/vnd.github.raw`**，且 **全长断言 `len==size`**。此坑当场造成过一次误判（误以为 blob 为空）。
3. 🔴 **Contents API 的 base64 内容是 LF，但 Windows 文本模式写入会转 CRLF**：小九第一版补丁底稿凭空 **+1233 B（恰等于行数）**。**必须全程 bytes 模式**（`io.open(...,'wb')`），且**先量「CRLF 数 vs bareLF 数」再定 NL**——本轮实测 `v8_cn_fetch_cloud.yml` 与 `HANDOFF.yaml` 均**纯 LF（CRLF=0）**，而本机 `logic.html` 是 **CRLF 全覆盖** ⇒ **逐文件量，别类推**。
4. 🔴 **`merge --ff-only` 在本仓恒失败**（浅克隆 `unrelated histories`）⇒ 属预期，**零副作用**；**不要**试 `--allow-unrelated-histories`，直接走**净室隔离推送**。
5. 🔴 **`git rebase` 结构性不可用**：`index.html` 的 `?v` 被 `v8-cache-bot` 每几分钟重写 ⇒ `Could not apply`。一律直接 abort 走净室推送**，别试 rebase**。
6. 🔴 **`git log --diff-filter=A` 在本仓恒假阳性**（bot 整树重建压缩历史）⇒ 判新增/删除用**区间 diff**（`git diff --name-status <旧> <新>`），业务时间排序用**文件名内嵌时间戳**。
7. 🔴 **判「检查项是否会真跑」**：新增任何守卫/门禁，必须问「**谁会触发它？**」——若检查对象不在 workflow 的 `paths`/`on:` 内，**守卫等同不存在**（实测 `[13/13]` 曾从未被触发）。
8. 🔴 **锚点清单必须覆盖「被砍的那一段」**：原 35 条锚点全在中部/头部，**尾部被砍仍全绿** ⇒ 已补 `</html>` + `LHB_HISTORY`（37 条）。**新增保护锚点须问「它守的是头部还是尾部」**。
9. 🔴 **判锚点归属必须 grep 被检查文件「正文」**，不能只 grep 清单文件（小九上轮 `unlockCoverCard` 误判的直接原因）。
10. 🔴 **「exit 0 + 结果行 ✅」≠ 产物可用**：凡落盘型脚本必须**独立读盘验「存在+长度+指纹」**（11:38 staging 重建器残缺但 exit 0 即此坑）。
11. 🔴 **`git diff --numstat` 得 `+0 / -N` ⇒ 标准纯落后，可直接同步**（本地独有行 = 0 是判据）；反之本地含 origin 所无才是**真 WIP，禁 checkout**。
12. 🔴 **改 `HANDOFF.yaml` 前必 dump 该 item 的 4 空格顶层键查重**：本轮命中**同一 item 内重复顶层键 `updated`**（原 L2843 + L2882）⇒ YAML 取后者、前者是僵尸键**对 `grep` 说谎**。
13. 🔴 **`git add` 一律 `-u` + 显式路径，禁用 `-f` 通配**：`data/`+`raw_data/` 下 **2957 个 ignore 文件**，`git add -f data/ raw_data/` 会全部强塞；而 `kline_cache/` 是**主人令入仓**的。**凭据地雷**：`data/zsxq_token.json` 在 `data/` ⇒ 严禁 `git add -f data/`。
14. 🔴 **推送用 `git push origin main`**，**严禁 `git push HEAD:main`**（Git Bash MSYS 会转坏成 `ssh: Could not resolve hostname head`）。
15. 🔴 **判「run 是死是活」用 `runner busy` > `stepAge` 递增 > 步数前移**（三级优先）：`updated_at` **冻结 47.7 分钟**期间步态 8/19 恒定，实为真活（已第 5 次证伪「`updated_at` 冻结 = 僵尸」）⇒ 按「窗口内步数推进」当必要条件会**误判僵尸并错误 cancel**，杀掉唯一 algo 数据推送。
16. 🔴 **cron 空窗须「按链各算各窗」**：`stock_quote_refresh` cron `0/30 1-8` UTC = CST 09:00–16:30 ⇒ 16:30 后无 run 属设计内；`cn_fetch` `*/20 0-3,5-6` UTC（22:00–08:00 无档）。**判「停摆」前必须对该链单独算覆盖窗**。
17. 🔴 **运行带 run id 的探针前必须先核对 `TARGETS` 与本轮真实在飞 run id**（写死旧 run id 会白等 9m29s，**已复发一次**）。
18. 🔴 **`updated_at` 冻结 ≠ 死**、**「单字段读数」≠「子系统死活」**、**13 位毫秒戳禁与日期串做字符串比较**（毫秒串以 `1` 开头、日期串以 `2` 开头）。
19. 🔴 **交接方向筛有「英文命名档」盲区** ⇒ 每轮须「**文件名筛 + handover 目录 diff**」**双口径互证**，并对每份晚于 T 的新档**先读首行标题认方向**。
20. 🔴 **`git worktree add --detach /e/...` 在 `MSYS_NO_PATHCONV=1` 下解析为 `E:\e\...`** ⇒ 传参须用 `E:/...`。
21. 🔴 **`mkcopy` 会覆盖手改副本**（r84f 手改版曾被 r85 复制覆盖）。
22. 🔴 **探针自带 bug 会污染结论**：age 列「秒当小时」（r86 误报 `age>90分 = 2`，真值 0；r87 已修为 `/3600.0` 同单位比较）⇒ **探针输出须与另一独立口径互证后再采信**。
23. 🔴 **陈旧散落 `/raw_data/` 与 `/data/` 两类，判新鲜度须分别取**。
24. 🔴 **本机红线**：只用 `E:/qs_workspaces/quant-scanner-v8`，**严禁 `E:/workspace/...`**（坚果云 junction，2026-09-04 全仓删除事故路径，**永不复用**）。

---

## 七、小九监督职责（本时段及后续）

| 项 | 频率 | 内容 | 处置边界 |
|---|---|---|---|
| v8 并发组死锁巡检 | 小时级（自动化） | 全仓未完成 run 穷尽 + 超 90 分阈值 + 并发组聚合 | **纯只读**；非死锁**不 cancel**（误 cancel 会杀真活） |
| v8 runner 本地守护 | 20 分钟级 | 进程/服务/日志/GitHub 四维 + `--heal --push --alert` | 零自愈优先；`index.html`/`HEALTH_CHECK.js`/`PORTFOLIO.js`/`BLOAT_CHECK.js` **红线零变更** |
| 交接入站回执哨兵 | 每 2 小时 | 未回执交接档识别 + 回执 + 推送 | 双口径（文件名筛 + 目录 diff） |
| 本地 runner 脚本同步守卫 | 2 小时级 | 白名单 6 脚本 `WT==ORG` 逐位校验 | 仅白名单；`update_v8.py` WIP 时**跳过不碰** |
| 机构研究卡刷新监督 | 每日 07:15/09:00 | 素材新鲜度 + 出稿上线 | 权威口径 `_guanlan_refresh_state.json` + schtasks（**mtime 未动 ≠ 刷新没跑**） |
| 每日 19:00 审计 | 每日 | 只读巡检 + 告警 | **禁止推仓** |
| 盘后算法链兜底守卫 | 每日 19:25 | 只检查 + 必要时**派发** | **绝不在本机跑算法** |

**保持纪律**：小九白天独家 7:45–17:45；**盘中 09:15–11:30 / 12:45–15:00 禁推仓、禁改 `index.html`**；
主人说「上线」才推（唯二例外见协议）；改后三件套（审计绑 `HEAD=<sha>` + 四方对齐 + 防覆盖入库 + 交接件）收尾必跑。

---

## 八、现取命令（**取代写死 sha**，随时可复现本档数据）

```bash
# 1) 远端真 tip（唯一权威，勿用记忆 sha）
cd E:/qs_workspaces/quant-scanner-v8 && git ls-remote origin refs/heads/main

# 2) 今日 main 提交列表（含真实 SHA + 时刻）
git log --since="2026-09-23 00:00:00 +0800" --pretty="%h %ad %s" --date=format:"%H:%M" origin/main

# 3) 线上真身（字节数 + blob sha）
python -c "import urllib.request,json;r=json.load(urllib.request.urlopen('https://api.github.com/repos/ah-quant999/quant-scanner-v8/contents/index.html?ref=main'));print(r['size'],r['sha'][:12])"

# 4) 线上健康检查（overall + 计数）
curl -s "https://ah-quant999.github.io/quant-scanner-v8/data/HEALTH_CHECK.js?cb=$(date +%s)" | head -c 260

# 5) 双机心跳
curl -s "https://ah-quant999.github.io/quant-scanner-v8/raw_data/hb_xiaojiu.json?cb=$(date +%s)"
curl -s "https://ah-quant999.github.io/quant-scanner-v8/raw_data/hb_alimi.json?cb=$(date +%s)"
```

---

## 九、待主人拍板（汇总索引）

| # | 项 | 建议 |
|---|---|---|
| 1 | 交接档禁写死 sha，改「路径+字节数+md5」 | **建议采纳**，落 `docs/ops/handover/README.md` |
| 2 | `pages build and deployment` 不消费 v8 门禁 | 建议接入或明确标注「非质量证据」 |
| 3 | `check_site()` 升级为「GET + 尾部闭合 + 字节不缩水」 | **建议采纳** |
| 4 | ledger `id_content_hashes` 升 hard 门禁 | **建议先 soft** |
| 5 | 陈旧产物 `CANDIDATE_BACKTEST`/`GOLD_POOL_BACKTEST`（8.5 天） | 需裁决「真无产出 vs 链断」 |
| 6 | 自动化 ACTIVE 129 条 + 运行历史堆积治理 | 建议跑 `automation-token-diet` |
| 7 | `P-Q 盘中粒度盲区`（`max_hours>=24` 一律按交易日判 ⇒ 盘中卡同日停更不判红） | 建议「盘中走小时／盘后走交易日」双口径 |
| 8 | `P-1 CI 覆盖源补丁`（`v8_build_deploy.yml` L205-207/L340-342，通道现成只差授权） | 待授权 |

---

*本档由自动化生成，内容以「线上真身 + 远端实测」为准；任何与记忆/截图不符处，**以真值为准**并请双机对齐口径。*
