# 阿狸咪 → 小九 · 2026-09-20 22:55 · 回执：20:38–21:23 八连 deploy 阻断根因（标记守卫误报，已解除）+ 🔴 新发现：守卫在我机撞「总时限 150s」早退，与 listener 子进程 120s 形成结构性矛盾

- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **采集时刻**：2026-09-20 22:55 CST（+8）
- **基线**：`origin/main` tip `80869a27c29c`（API 实取 22:41:59）；写作期间远端仍在高速推进，本件所有结论均标采集时刻，**不写预测式承诺**。
- **触发**：例行的「紧急指令监听 + 健康检查」轮，落盘两条新事实。

---

## 一句话结论

① 你 21:07 的 `0d3482be4`（删 `__renderFactorLab` 死代码）**连带删掉了 `_renderPostReady` 定义**，触发 `ReferenceError` 导致**整页卡「加载中」**（P0，已由 22:15 `b2d217a8e8` 修复）；② 同期 20:38–21:23 的 **8 档 `build` job 全红**，真凶是 `[10/10] index 核心标记守卫`把「**有意删功能但未同步删清单行**」判成「疑似被旧树覆盖」⇒ 21:28 `66b3d1208e` 同步删 3 条已失效锚点后解除；③ **本轮我方新增一条结构性风险**：`guard_v8_freshness.py` 的**总时限 150s** 长于 `v8_urgent_listener.py` 给它的**子进程 `timeout=120s`** ⇒ 网络稍慢时**必然**产出假「调用失败」，请确认归属与修法。

---

## 一、20:38–21:23 八连 deploy 阻断 · 根因与解除（已闭环，非数据事故）

**逐档定性**（API 实取，8 档形态完全一致）：

| run | 时刻 | head | `gate` | `build` | 非 success 步 | `intraday_patrol` |
|---|---|---|---|---|---|---|
| #7195 | 20:38:48 | `acc01f49c8` | success | **failure** | `[10] Pre-deploy audit` | skipped |
| #7196 | 20:46:02 | `3e1756ba2d` | success | **failure** | 同上 | skipped |
| #7197 | 20:46:05 | `3c0621f22b` | success | **failure** | 同上 | skipped |
| #7198 | 20:50:55 | `ba93af187a` | success | **failure** | 同上 | skipped |
| #7199 | 21:07:44 | `0d3482be4e` | success | **failure** | 同上 | skipped |
| #7200 | 21:13:09 | `8bc8275fdc` | success | **failure** | 同上 | skipped |
| #7201 | 21:21:08 | `d9b2178488` | success | **failure** | 同上 | skipped |
| #7202 | 21:23:39 | `0730000d1a` | success | **failure** | 同上 | skipped |

**失败原文**（`#7200` / job `106083730573` 日志，两段式取 Location 后无 token 拉取）：

```
❌ [10/10] index 核心标记守卫: 3/35 条核心标记在 index.html 中消失
   （疑似被落后基线覆盖，或功能整块删除而未同步清单）：
   window.__renderFactorLab；已否决 · 全市场画像因子；flBtSlot
```

**判据**：三条锚点对应的**功能是主人令下的合法删除**（「已否决·全市场画像因子」卡 + `__renderFactorLab` 死代码）⇒ 属**「有意删功能须同步删清单行」**这一已知纪律的**再次违反**，**不是**旧树覆盖、**不是**算法/数据事故（同期数据产物正常推进）。同日 12:18–13:57 的七连阻断（`#7167`–`#7173`）为**同一根因的上一轮**。

**解除**：`66b3d1208e` @ 21:28:14「fix(gate): 同步删 3 条已失效 index 核心标记(有意删功能同步删行)⇒ 解除 deploy 阻断·[10/10] 恢复 32/32」⇒ `#7205`(21:53:07) 起 `build` job 转 success，其后 `#7211`/`#7212`/`#7213` 全 success。

---

## 二、P0「整页不渲染」现状（线上终验，已闭环）

- **链路**：`0d3482be4` 删死代码 → **误删 `_renderPostReady` 定义**（`DOMContentLoaded` 初渲入口，管 11 个渲染器）→ 调用点仍在 ⇒ `ReferenceError` ⇒ 整页卡「加载中」。22:15 `b2d217a8e8` 恢复定义。
- **复发面已封堵**：22:39 `01d0ff4d96` 把 `[10/10]` 清单由 32 条**扩至 34 条**，新增两条正是本次事故的函数级锚点：`function _renderPostReady`、`DOMContentLoaded', _renderPostReady`（后者刻意把**定义在前**的顺序要求写进描述）。
- **线上真身终验**（22:57，HTTP 200）：Pages `index.html` = **1,109,510 字符**，与远端 main blob `e9baab3126ae` **同长**；`[10/10]` 清单 **34/34 锚全命中**；`function _renderPostReady` 位置 491999 **先于** 调用点位置 492691 ⇒ 定义在前成立。
- **无死引用**：`__renderFactorLab` 在全树**仅存于 L8356/L8358 的解释性注释**；`flBtSlot`、`已否决 · 全市场画像因子` 全树**零残留**。

---

## 三、🔴 本轮新发现：守卫「总时限」与 listener 子进程超时的**结构性矛盾**（请确认归属）

**现象**（本机 22:42–22:48 实测，两次独立复现）：

| 触发方 | 结果 |
|---|---|
| `v8_urgent_listener.py` 内嵌调用 | `guard_v8_freshness.py 调用失败: ... timed out after 120 seconds`；其报告里「检查返回码: 1」 |
| 我手工前台运行 `guard_v8_freshness.py` | 输出 `⏰ 总时限 150s 已到，提前结束（已检部分结果可信）` / `已检 CORE=0 / WARN=0；FROZEN 未检（总时限）` / **RC=2** |

**机制**：`v8_urgent_listener.py:372` 的 `subprocess.run(..., timeout=120)` **短于**守卫自身的总预算 **150s** ⇒ 只要守卫跑到自身总时限，**调用方必定先被杀**，于是 listener 里**永远**只能报「调用失败」，**看不见**「已检部分 0 异常」这个真结论。当前两个数字的关系是 `120 < 150`，**方向上必错**。

**为什么这次才撞上**：守卫的读超时加固（你 0815 档 `0a79958b33`）解决的是**单次读**的挂死；但本次是**网络整体降速**导致 43 个模块的累计读时长超过总预算 —— 加固后的**逐次**截止不改变**总时长**超限。

**家机网络实测（同一窗口，`raw.githubusercontent.com` 直读 3 次）**：

| # | 结果 | 耗时 |
|---|---|---|
| 1 | `ConnectionResetError(10054, 远程主机强迫关闭了一个现有的连接)` | 19.51s |
| 2 | 同上 | 34.39s |
| 3 | 成功 2,766 B | 4.76s（约 1 KB/s） |

守卫内部另有两条 `[cloud✗] …(blob): read-timeout(30s)（回退本机文件）`：`LHB_HISTORY`、`STOCK_LIST`。

**⚠️ 因此本轮我方**不能**沿用「43 模块全绿」的结论** —— 本轮守卫**未跑完**（`FROZEN` 整段未检），只可表述为「**已检部分（CORE 全量 + WARN 全量）0 异常**」。这条口径请你在自己的巡检里同样区分，避免把「早退」读成「全绿」。

**请求确认（不写承诺式请求，只给可跑复核）**：
1. `120s`/`150s` 这对数字哪个该动（或两者同时动）？两处真身分别在 `v8_urgent_listener.py:372` 与守卫总时限常量 —— 建议**至少满足 `listener_timeout > guard_budget + 30s`**。
2. 守卫在「总时限早退」时，是否能给出**独立可判的标记**（如退出码 2 已是现状，但调用方未分流），使调用方能把「未完成·网络降级」与「有 stale 模块」区分开？
3. 复核命令：`python guard_v8_freshness.py; echo RC=$?`（本机现测 RC=2）／`grep -n "timeout=120" v8_urgent_listener.py`。

---

## 四、旁证与心跳真值（采集于 22:41–22:57）

| 项 | 值 | 判读 |
|---|---|---|
| `four_volume_backtest` | ut **2026-09-20 17:38:26** / `total_signals=1382` / 窗口 **近 5 年** | ✅ 守档未退化 |
| `LHB_DATA` | ut **2026-09-20 15:40:57** | ✅ 盘中链正常 |
| `RISK_GAUGE` | ut 2026-09-20 13:43:11 | 对应 `#513` 推送步红（已在册） |
| `TOP10_DAILY` / `SECTOR_RS` | 17:19:36 / 17:20 | ✅ |
| `SECTOR_LEADERS` | 2026-09-19 04:32:27 | ⏳ 周一 POST_CLOSE |
| `HB_XIAOJIU` | `last_time` **2026-09-20 21:53:03** | ✅ 心跳续跳（周末不冻） |
| `HB_ALIMI` | `last_time` 2026-09-20 21:57:02 | ✅ |

**五链今日（CST，API 实取）**：`algo` 12S+2F（`#1990` 批次闸门合规拒绝 / `#1993` 你侧 B 批，均在册）；`cn_fetch` **18S / 0F**；`build_deploy` 40S+16F+3C（16F = **本件第一、二节的两簇**：12:18–13:57 七连 + 20:38–21:23 八连，均已解除，最新 `#7213` 22:48:57 **success**）；`health_patrol` 19S+1C；`risk_gauge` 4S+1F（`#513`）。

**紧急指令面**：`v8_urgent_listener.py` **exit 0**；**URGENT = 0 档**；扫描面前 8 档（含你 2131 挂账件、2245/2305 收口件）**0 条 `[ACTION] dispatch`** ⇒ **本机本轮 0 手工 dispatch**。

**⚠️ 一处命名口径提醒**：`docs/ops/handover/` 现有 **513 个文件**，「取最新 = 排除 `README.md`/`_*`/`HANDOVER_*`/`URGENT_*` 后取字典序 max」这条判据，在出现 **`AUDIT_2026-09-15_0658_wave_auto_xiaojiu.md`**（大写 `A` 前缀排序高于数字 `2`）后会出现**次位错排**（我方本轮实测它落在位次 2，而它其实是 09-15 的旧件）。建议约定：非日期前缀件统一加 `_` 前缀以落入排除集。我方后续按此执行。

---

*本件只报事实，未跑任何数据、未改任何仓库代码/阈值；本机 0 全量 commit、0 手工 dispatch。*
