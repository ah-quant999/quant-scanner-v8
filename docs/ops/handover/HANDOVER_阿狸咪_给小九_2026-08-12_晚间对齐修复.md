# 交接：小九（对齐修复 2026-08-12 20:00 之后）

**时间**：2026-08-12 20:13 左右
**提交**：`e1d428583`（已 push main，本地 == 远端）

## 主人本次反馈 + 修复

### ① 任务运行看板「6 天前」（截图 1）

- **bug**：`V8_PAGE_SCHEDULE.updated` 硬编码 `'2026-08-06 16:25'`——前端拿到的就是写死值
- **修复**（index.html line 8310）：改为动态取值，优先级
  1. `FRESHNESS_STATUS.check_time`（最准，反映实际巡检）
  2. data/*.js 中最大的 `?v=` 时间戳（build_deploy 最近一次）
  3. 当前时间（兜底）

### ② 「Pages 部署同步」报 API: no token（截图 2）

- **bug**：`v8_health_check.py _load_token()` fallback 路径 `E:/workspace/quant-scanner-v8/.workbuddy/v8_gh_token.txt` 已删除（08-09 清理 quant-scanner-v8 时）；同时 `v8_ops_self_heal.yml` 注入的是默认 actions token，权限不足调 `/deployments` → 401
- **修复两处**：
  1. `v8_health_check.py` 增加 fallback `data/.github_pat.txt`（与 `scripts/monitor_*.py` 同源最稳）
  2. `v8_ops_self_heal.yml` env：`GH_TOKEN=secrets.GITHUB_TOKEN` → `V8_GITHUB_TOKEN=secrets.V8_GH_TOKEN`（与 build_deploy 一致，PAT 含 repo+actions 权限）

### ③ 候选池 07:45 失败（截图 3、4）

- **根因**：raw_data/candidate.json 的 `update_time` 是我早上搬运时设的 07:45:57（搬运那一刻的本地时间），但任务看板用 update_time 判断「最近实际」→ 显示 07:45，假象「07:45 后没跑过」
- **真实**：data/CANDIDATE.js mtime 20:04 早就重建（commit `e1d428583` 推送后 build_deploy 会用 20:12 重建）
- **修复**：手动把 raw_data/candidate.json 的 update_time / republish_time 改为当前 build 时间（commit 一起推了）

### ④ 驾驶舱 16:33 / 逆势龙头 16:20 失败（截图 4）

- **根因**：这两个卡片的数据来自 `gen_cockpit_tier_recommend.py` 和 `calc_crds.py`，输入是 scanner 产出的 `scan_result.json`
- **scanner 路径 bug**：`scanner.py load_candidate_pool()` 读 `algorithms/data/candidate_pool.json`（永不存在的旧路径，08-04 改名遗留）→ scanner 候选池空 → scan_result 里 A 股信号缺失 → cockpit A 档为 0 / CRDS 数据稀疏
- **修复**：昨晚 commit `045ac1c84` 已修 scanner.load_candidate_pool 路径兼容（out/candidate_pool.json → raw_data/candidate.json → 历史路径）
- **小九操作**：云端 scanner 下次跑（按 plan 每日 18:30 盘后 algo_chain 触发）会自动用新代码 → A 股信号股进金股池 → 驾驶舱 / 逆势龙头 卡片自动恢复 ✓

### ⑤ westock 持仓云端同步 10 次失败（截图 5）

- **根因**：westock MCP 端口/连接问题——与本仓库代码无关（westock 是独立外部服务）
- **小九操作**：检查 westock-mcp connector 状态，或重启 westock MCP 服务

## 小九总览（今晚全部修复）

| commit | 内容 |
|---|---|
| `d2bac2576` | 个股查询混合 query + 帮助说明单列（后又改回双列）|
| `045ac1c84` | 三重共识 8 天为 0 根因（fundamental 读错 + scanner 候选池路径）|
| `b74bf32d8` | 港股降权 qs -=10 + 本地立刻跑 top10/triple_consensus |
| `c0cd1362a` | 帮助说明改回双列 |
| `e1d428583` | **本次**：任务看板实时化 + Pages token 修复 + 候选池 update_time |

## 主人必看

- 强刷（Ctrl+F5）后任务看板应显示「更新于当前时间」
- 候选池「最近实际」应在 build_deploy 重建后变为当前时间（不再是 07:45）
- Pages 部署同步下次 self_heal 跑（每小时）后应转 ✓
- 驾驶舱 / 逆势龙头要等下次 algo_chain（按 plan 18:30 或盘中）跑完恢复
- westock 同步需独立排查 MCP 服务

## 数据文件纪律重申

- 只 commit py / index.html / 当前 build 时间戳的 raw_data
- 绝不 commit 本地旧的 data/*.js / 旧 raw_data（会覆盖云端新数据）
- raw_data/*.json 的 update_time 字段务必设为 commit 时刻的 build 时间（不是搬运原数据的时间）