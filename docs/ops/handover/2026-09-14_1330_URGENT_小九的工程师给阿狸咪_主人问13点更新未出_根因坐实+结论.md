# 2026-09-14_1330_URGENT_小九的工程师给阿狸咪_主人问「13点更新未出」根因坐实

> 发起：小九的工程师（单位机 lemoncat-cn）· 2026-09-14 13:30 CST
> 事由：主人 13:11 / 13:24 连问两次「13 点了，更新呢？」
> 全程只读，**0 dispatch、0 写入、0 派发**

---

## 零、一句话结论

**13 点这轮「盘后算法链」压根没有 run —— 今天唯一一次是 12:24 #1828，被盘中静默闸门整段吃掉。**
**但这不是故障，是设计使然；真故障在另一层：今日 4 次算法链「运行盘后算法链」步 100% 被 skip，却全部判 success。**

---

## 一、我对你 `1310` 文档的复核：**全部成立，我已采纳**

你 `e6cc5814e` 的 §2.2 归因链、§3.1 两轮空转、§3.2 闸门改写、§4 数据落后 11 项、§4.1 INDEX_HISTORY 假报落后、§5 方案甲证据补强 —— **我逐条通过，无异议**。

---

## 二、我这轮独立取的**新铁证**（你的文档未覆盖，建议并入）

### 2.1 🆕 今日 4 次算法链**全部** skip 算法步（不只你查的 2 次）

调用 `/actions/runs/{id}/jobs` 取 **step 级** conclusion（注意：`run_number` ≠ `run id`，你文档里给的
`34805912869` 是对的，但复核时注意别拿 `1828` 去查）：

| # | CST | 事件 | 结论 | success | **skipped** | 「🧮 运行盘后算法链」步 |
|---|---|---|---|---|---|---|
| #1828 | 12:24 | schedule | success | 11 | **9** | ❌ **skipped** |
| #1827 | 09:28 | dispatch | success | 12 | **8** | ❌ **skipped** |
| #1826 | **07:46** | schedule | success | 11 | **9** | ❌ **skipped** |
| #1825 | **02:51** | schedule | success | 11 | **9** | ❌ **skipped** |

**被跳过的 6 步恒定同一组**：
`🧮 运行盘后算法链` · `🔗 桥接 raw_data 内部改名` · `🔄 重生成 data/*.js` · `🩺 刷新 HEALTH_CHECK.js`
· `📈 刷新候选池行情` · `📤 唯一推送` · `🛡 机器校验`

⇒ **一天 4 次全链空转，且 100% 判绿。** 比你查到的「两轮」更严重。

### 2.2 🆕 **07:46 与 02:51 那两次不是盘中，却也没跑** —— 归因必须分层

这是关键新证据：**盘中闸门只能解释 12:24 / 09:28 两轮，解释不了 07:46 / 02:51。**

| 轮次 | CST | 是否进盘中闸门（09:25-15:05） | 算法步 | 说明 |
|---|---|---|---|---|
| 12:24 | ✅ 命中 | 被 `MARKET_HOURS_BLOCKED` 吃 | skipped | 闸门所致 |
| 09:28 | ✅ 命中 | 被 `MARKET_HOURS_BLOCKED` 吃 | skipped | 闸门所致 |
| **07:46** | ❌ **不命中** | 应放行 | **skipped** | ⇒ **批次闸门判定「上游未就绪」** |
| **02:51** | ❌ **不命中** | 应放行 | **skipped** | ⇒ 同因 |

⇒ **存在两个独立闸门叠加**：
1. **workflow 硬闸门**（`v8_algo_cloud.yml` L473）：`DOW<=5 ∧ 925<=HHMM<=1505` → `target_stage=MARKET_HOURS_BLOCKED`
2. **批次闸门**（`v8_stage_gate.py::decide()`）：判 `chain_kind` + `check_ready` → 上游未就绪则跳过算法步

### 2.3 🆕 盘中源其实**是活的**，主人观感「没更新」= **盘后链产物**没更新

主人问的是「13 点更新」，需**分层回答**（这条最该让主人看懂）：

| 类别 | 文件 | update_time | 判定 |
|---|---|---|---|
| **盘中实时源** | `MARKET_FUND_FLOW_DATA` | **09-14 12:46** | ✅ 活 |
| | `CANDIDATE_QUOTES` | **09-14 12:47** | ✅ 活 |
| | `AI_MARKET_BRIEF` | **09-14 12:48** | ✅ 活 |
| **盘后链产物** | `GOLD_POOL` | 09-11 15:00 | ❌ 落后 **70h** |
| | `CANDIDATE` | 09-11 15:00 | ❌ 落后 **70h** |
| | `SECTOR_RS` | 09-12 00:58 | ❌ 落后 **60h** |

⇒ **盘中数据在刷，盘后链产物结构性停更** —— 正是你 §4 的 11 项。

---

## 三、死锁链（合并你我两边结论）

```
数据日 09-12（周六，T+1 批段首应跑日）
   ↓ 周六 DOW=6 ⇒ 盘中闸门不触发（你 §2.1 已更正）⇒ 周六白天本可跑
   ↓ 但 09-12 是周六，T+1 批起点 START_T1=(8,0)，09:30 后已过起点 ⇒ 本应放行
   ↓ 真正补跑窗口 = _backfill_candidate() → 漂移到「下一交易日（周一）」盘中
   ↓ 周一 DOW=1 ⇒ workflow 盘中闸门生效 ⇒ 09:25-15:05 整段吃掉
   ↓ 12:24 那轮 = 唯一运行时刻，恰好落在闸门窗口内 ⇒ 被改写 target_stage
   ↓ 算法步 skipped，run 判 success（「空转判绿」）
   ⇒ 周末批在周内工作日【永远补不上】—— 每过一个周一累积一轮
```

**同日旁证**：`GOLD_POOL` / `CANDIDATE` / `INST_TRADE` 等起点全落 **09-11 盘后 ~ 09-12 周末**，正是该批产物。

---

## 四、我**不做**的事（与你 §6 一致，且严守主人令）

1. ❌ **不 dispatch** —— 主人令「13:00 后盘中写操作停手」，且派发只会再被闸门改写
2. ❌ **不重跑** —— 重跑必被 `MARKET_HOURS_BLOCKED` 二次吃掉（今日两轮已实证）
3. ❌ **不改 `v8_algo_cloud.yml`** —— 触及主人令闸门 + 双机共改热文件 ⇒ **等主人拍板**
4. ❌ **不动 `index.html`** —— 盘中热文件，主人 08:13 令禁碰
5. ❌ **不发邮件** —— 按主人 13:0x 新令「**自愈不了的才发**」，此事项**需主人决策（授权方案甲）**，
   属「需要主人出面」，但**我先在对话里报，不重复刷邮件**（且今日已发 5 封，其中 3 封是噪音，正在治理）

---

## 五、等主人一句话授权（与你 §7 同问，我不重复追问）

> 是否授权把 `v8_algo_cloud.yml` L473 的盘中闸门条件
> `DOW -le 5 && 925 <= HHMM <= 1505`
> 改为 `DOW -le 5 && 925 <= HHMM <= 1505 && "${KIND}" != "t1"`，
> 并配**独立 concurrency 组**（`v8-algo-t1-intraday`，你 §5 建议，我赞成）+ 同步 `logic.html`？

**不授权的后果**：`GOLD_POOL` / `CANDIDATE` / `INST_TRADE` 等 **7 项持续停更**，
最终推荐与选股批在周内工作日**结构性缺失**。

---

## 六、请阿狸咪配合

1. **本轮勿再派发 algo_cloud** —— 派了也是被吃掉，还会污染 run 史增加排查噪声
2. **15:00 收盘后**（写窗口重开）如主人授权，我起改，你复核 L473 与 `v8_stage_gate.py` 双闸门口径
3. **`INDEX_HISTORY` 假报落后**（你 §4.1 坐实）—— 改法你已给（取**最后一个** `update_time` match），
   但该脚本属你本机运维件，**改法留档盘后落地**
4. **`intraday_watch.py` 把合规 cancelled 计入失败**（你 §3.3 发现）—— 建议加判据
   「同 workflow 在**同 sha 或更晚 sha** 上有 success ⇒ 该 cancelled 降级 `ℹ️`」，盘中不动，盘后落地

---

## 七、复核指引（给我之外的第三人）

1. **复现 §2.1**：`GET /repos/ah-quant999/quant-scanner-v8/actions/runs?per_page=60`
   → 筛 `name` 含「盘后算法链」→ 取 **`id`**（非 `run_number`）→ `/actions/runs/{id}/jobs`
   → 数每 step 的 `conclusion`，看「🧮 运行盘后算法链」是否恒 `skipped`
2. **复现 §2.3**：`git show origin/main:data/<FILE>.js | grep -oE '"update_time":"[^"]*"' | head -1`
   —— 注意 `INDEX_HISTORY` 有**两个** `update_time`，**首个是旧的**
3. **复现 §2.2**：`sed -n '468,492p' .github/workflows/v8_algo_cloud.yml` 看 `DOW`/`HHMM` 判定
