# 交接：合并「今日宏观解读」与「今日判定·环境态势」

**时间**：2026-08-07 07:25 CST  
**提交**：`41329f2`（origin/main）  
**文件**：`quant-scanner-v8/index.html`

## 主人指令
> 今日宏观解读 与 今日判定·环境态势 两张卡内容重复，合并成一张「今日判定」，先放暂未上架。

## 改动
1. **`_buildJudgmentEnvHTML()`**：
   - 新增读取独立 `MACRO_BRIEF` 数据源，补充「关键数据」横向标签（美债/中国10Y/VIX/美元指数/USD/CNH/黄金/原油）。
   - 时事滚动优先用 `MACRO_BRIEF.news_brief`，回退到 `JUDGMENT_DATA.macro_brief`。
   - 保留：美股隔夜、结论/预警、三大分析师、共同结论、三大危险信号、大盘指数结构。

2. **首页「今日事件」Tab**：
   - 删除原「今日宏观解读」卡，避免与「今日判定」内容重复。

3. **暂未上架 Tab**：
   - 卡片标题由「今日判定·环境态势」改为「今日判定」。
   - 改用 `v8CardHeader()` 注入 🐻 胶囊（盘前/短线），符合卡片徽章铁律。

## 验证
- `node --check` 81 个 script 块 ✅
- 已 push 到 `ah-quant999/quant-scanner-v8` main，`41329f2`

## 小九注意
- 无需改 `cloud_fetch_v8.py`；今日判定时事/关键数据已从前端组合 `JUDGMENT_DATA` + `MACRO_BRIEF` 渲染。
- 08:25 `v8_cn_fetch_cloud` 同时刷新 `JUDGMENT_DATA.js` 与 `MACRO_BRIEF.js`，合并后数据链路不变。
