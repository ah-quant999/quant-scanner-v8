# v8 全站审计报告

**审计人**：阿狸咪 ｜ **日期**：2026-07-31 23:30 ｜ **对象**：`quant-scanner-v8` 独立仓
**审计范围**：前端数据接入 · 备用源 · 架构逻辑 · 定时任务覆盖 · 算法 · 臃肿度

---

## 〇、一句话结论

**v8 的前端有 46 个数据模块，其中 24 个（52%）是 7 月 30 日一次性拆分出来的冻结快照，从那天起再没有任何程序更新过它们，而页面对此毫无提示。**

线上看起来「有数据」，实际过半是标本。这不是显示 bug，是**生产链路根本不存在**。

---

## 一、致命问题（P0）

### P0-1　24 个模块无任何数据生产者 —— 全站过半是标本

**证据**：

| 检查项 | 结果 |
|---|---|
| `update_v8.py` 声明的数据映射 | 46 个 |
| `cloud_fetch_v8.py` 实际能抓取的 | **22 个** |
| 差额（无生产者） | **24 个** |
| 这 24 个的 `raw_data/*.json` 在 git 历史中的提交次数 | **全部 0 次** |
| 它们 `data/*.js` 的最后来源 | `2026-07-30 20:43` commit `split inline data to data/*.js` |

也就是说：这 24 个 json **从来没有进过仓库**。`data/*.js` 是 7-30 用 `split_inline_data.py` 从 `index.html` 内联数据里一次性剖出来的**标本**，之后再无生产者。

**受影响的模块**（全部是选股/算法/盘后核心）：

```
金池 GOLD_POOL          候选池 CANDIDATE         三重共识 TRIPLE_CONSENSUS
三重追踪 TRIPLE_TRACK    三重历史 TRIPLE_HISTORY   驾驶舱建议 COCKPIT_ADVICE
驾驶舱分级 COCKPIT_TIER  驾驶舱回测 COCKPIT_BACKTEST 综合回测 BACKTEST_COMPREHENSIVE
TDX回测 BACKTEST_TDX     逆势龙头 CRDS_CARD_DATA    龙虎榜 LHB_DATA
斐波那契 SH_FIB/SZ_FIB   板块RS SECTOR_RS          maharo MAHORO
机构交易 INST_TRADE      NT数据 NT_DATA            TOP10 TOP10_DAILY
停牌预警 SUSPENSION      市场异动 MARKET_ALERTS     股票名录 STOCK_LIST
推荐 RECOMMEND           扫描 SCAN_DATA
```

**为什么之前没被发现**：`update_v8.py` 的设计是「raw_data 里没有的模块，`data/*.js` 保持不变」（第 184-197 行）。这个「保护既有数据」的设计，在源头断供时就变成了**永久僵尸数据**——不报错、不告警、页面照常渲染。

**这直接回答了主人的疑问**：
- 「斐波那契怎么一天了还是没数据接入」→ 不是一天，是 **4.2 天**，且永远不会自己好。
- 「这几个算法都接入正确数据，云端开始验算部署了吗」→ **云端没有跑任何算法**（见 P0-4）。

---

### P0-2　前端对停更数据完全不提示 —— 用户无法察觉

`index.html` 第 2923 行 `fmtDataTime()`：

```javascript
function fmtDataTime(timeStr){
  if(!timeStr) return {text:''};   // ← 没有时间戳 = 什么都不显示
  ...
}
```

**7 个模块根本没有 `update_time` 字段**：
`COCKPIT_ADVICE`、`COCKPIT_TIER_RECOMMEND`、`MAHORO`、`RECOMMEND`、`SCAN_DATA`、`STOCK_LIST`、`TRIPLE_HISTORY`

这些卡片右上角**一片空白**——不显示「6 天前」，而是压根不显示任何时间。用户看到的是一张干净、正常、仿佛实时的卡片，实际内容停在 7 月 30 日。

**这比显示「6天前」危险得多**：显示陈旧日期至少能让人警觉，什么都不显示则完全无从察觉。

---

### P0-3　零备用数据源 —— 单点依赖东方财富一家

`cloud_fetch_v8.py` 22 个抓取函数，**全部单一数据源**（绝大多数是东方财富 `push2delay` 镜像）。

`run()` 的容错逻辑（第 63-79 行附近）：

```python
def run(label, fn, retries=2):
    for attempt in range(retries + 1):
        try:
            obj = fn()
            if obj is not None: save(label, obj)
            return
        except Exception as e:
            time.sleep(2)          # ← 同源重试，不是换源
    print(f"  🚫 {label} 跳过")     # ← 失败即静默跳过
```

这是**同源重试 2 次**，不是降级到备用源。失败后：raw json 不生成 → `update_v8.py` 跳过 → `data/*.js` 保留旧值 → **线上静默展示昨天的数据**。

**对比 v6**：v6 的板块资金流是「东财 → neodata → westock」三级降级。**v8 在这一点上是架构倒退。**

**实测已经发生**：今晚跑 `cloud_fetch_v8.py` 时，`CONCEPT_RANKING`、`CAPITAL_FLOW_DATA`、`MARKET_FUND_FLOW_DATA` 三个模块因 ConnectionError 连续失败 3 次后跳过。

---

### P0-4　云端零算法能力 —— 「算法上云」workflow 名不副实

`v8_algo.yml` 原文只有一个实质步骤：

```yaml
- name: "🧮 全量重建 data/*.js（算法产出上云）"
  run: python update_v8.py        # ← 就这一行
```

而 `update_v8.py` 是**纯 JSON 格式转换**（raw_data → data/*.js），和 `v8_build_deploy.yml` 的构建步骤**逐字相同**。

**v8 仓内根本没有算法脚本**。9 个 `.py` 全部是：
`api_push_raw`(推送) `cloud_fetch_v8`(抓取) `deploy_v8`(部署) `fetch_ipo_data_v8`(抓取) `fetch_limit_up_heatmap_v8`(抓取) `guard_v8`/`guard_v8_freshness`(守卫) `split_inline_data`(工具) `update_v8`(转换)

三重共识、驾驶舱分级、逆势龙头、全站精选的**计算逻辑全部留在 v6 仓**，v8 从未继承。

**答复主人的问题**：「云端开始验算部署了吗」→ **没有，一次都没有。** 云端只做格式搬运。

**附带损害**：两个 workflow 干同一件事，还并发 push main，会互相触发 rebase 重试，白烧 Actions 分钟数。

---

### P0-5　新鲜度守卫存在监控盲区，且盲区 100% 覆盖问题模块

`guard_v8_freshness.py` 原版只检查 16 个源（8 core + 8 warn），而这 16 个**恰好全是 `cloud_fetch` 能抓的那 22 个的子集**。

真正会陈旧的 24 个冻结模块，**一个都不在监控范围内**。

结果：在 `SECTOR_RS` 已经陈旧 6.1 天的情况下，守卫输出 —— **「✅ 所有数据新鲜」**。

更糟的是：`guard_v8_freshness.py` 和 `guard_v8.py` **从未被任何 workflow 调用**（已 grep 确认 `.github/workflows/` 无引用）。写了守卫，但没接线。

---

## 二、架构与逻辑问题（P1）

### P1-1　`cloud_fetch_v8.py` 无差别清空 raw_data —— 定时炸弹

`main()` 第 1096-1104 行：

```python
for old in RAW_DIR.glob("*.json"):
    old.unlink()          # ← 删掉所有 json，不分青红皂白
```

小九加这段是为了防止已下架的 `limit_up_ladder.json` 被推回（commit `dacbd8c`），动机正确。

**但**：一旦将来建立「v6 → v8 推送 24 个算法数据」的链路，这行代码会在每次 cn_fetch 运行时**把推来的数据全部删掉**。修复 P0-1 时必须同步改成白名单删除。

### P1-2　孤儿引用 2 个

`window.COCKPIT_BACKTEST_DATA`、`window.SH_SZ_HISTORY` —— 前端代码引用，但 `data/` 下无对应文件，也不在 `DATA_SOURCES` 里。运行时为 `undefined`，靠 `|| {}` 兜底静默失败。

### P1-3　死数据 2 个

`RECOMMEND.js`（2098B）、`SCAN_DATA.js`（**23 字节空壳**）—— 前端从不引用，却仍在首屏 `<script src>` 加载。

---

## 三、定时任务覆盖审计（P2）

### 调整前的问题

| 问题 | 说明 |
|---|---|
| **盘中 13:00 断档** | cn_fetch 在 11:30 后直到 14:00 才跑，中间空 2.5 小时。而 **13:00 正是下午开盘**，最活跃的时段反而无抓取。 |
| **周末空转** | cn_fetch / build_deploy 原为每日 `* * *`，周末无行情，22 个抓取函数纯空跑。 |
| **两个 workflow 重复** | `v8_algo` 与 `v8_build_deploy` 都跑 `update_v8.py`，并发 push 冲突。 |
| **守卫未接线** | 无任何定时任务调用新鲜度检查。 |

### 本次已修复

| 文件 | 改动 |
|---|---|
| `v8_cn_fetch.yml` | 补 **13:05** 下午开盘抓取；收敛为工作日 `1-5`；周六 09:00 保留一次（IPO/日历类） |
| `v8_build_deploy.yml` | 对应补 **13:15**；去掉周末 19:00 空转；改周六 09:10 跟随 |
| `v8_algo.yml` | **彻底改造**：从重复的 `update_v8.py` 改为「每日数据体检」，跑全量守卫（09:00 盘前 + 17:00 盘后），结果写 `data/freshness_status.json` 并输出到 Job Summary |
| `guard_v8_freshness.py` | **重写**：受检模块 16 → **46 个**，盲区清零；新增 `FROZEN` 分类专门盯冻结快照；新增「无时间戳」检测 |

**改造后守卫实测输出**：

```
受检模块: 46 个

🧊 冻结快照已停更（10 个，无云端生产者）:
  - SECTOR_RS: 更新于 07-25 13:52，落后 6.1 天
  - BACKTEST_TDX: 更新于 07-27 00:00，落后 4.6 天
  - SH_FIB / SZ_FIB: 更新于 07-27 11:23，落后 4.2 天
  - COCKPIT_BACKTEST / BACKTEST_COMPREHENSIVE: 落后 3.7 天
  - SUSPENSION_ALERT / MARKET_ALERTS: 落后 3.0 天
  - LHB_DATA: 落后 2.8 天
  - NT_DATA: 落后 2.2 天

⏱️ 无时间戳（7 个，前端不显示更新时间，用户无法察觉陈旧）:
  COCKPIT_ADVICE, COCKPIT_TIER_RECOMMEND, MAHORO, RECOMMEND,
  SCAN_DATA, STOCK_LIST, TRIPLE_HISTORY
```

从「✅ 所有数据新鲜」到暴露 17 个真实问题。

---

## 四、臃肿审计：v6 的病换了个形式复发

小九的交接文档定的红线是「`index.html` 超 500KB/6000 行才算臃肿」，当前 313KB/4159 行，**看似安全**。

**但真正的臃肿不在 HTML，在首屏加载**：

| 项 | 数值 |
|---|---|
| `index.html` | 313 KB / 4159 行（红线的 62%） |
| `data/*.js` | 46 个文件，**1.3 MB** |
| 加载方式 | 第 355-400 行，**46 个 `<script src>` 全部同步阻塞，无 defer / async** |
| **首屏必须下载解析** | **1.3 MB JavaScript** |

其中两个巨无霸占了一半：

| 文件 | 大小 | 首屏是否需要 |
|---|---|---|
| `GOLD_POOL.js` | **373 KB** | 否，在「全站精选」tab |
| `STOCK_LIST.js` | **288 KB** | 否，只是代码→名称查询字典 |
| 小计 | **661 KB（51%）** | **都不是首屏必需** |

**结论**：v6 的病是「单文件塞满内联数据」，v8 拆成了 46 个文件——**但全部同步加载，总量一点没少**。用户打开页面仍要等 1.3MB 下载解析完才能看到第一屏。这是**同一个病换了个形式**。

**轻量化方案**（建议，未执行）：
1. 46 个 `<script>` 全加 `defer` —— 零风险，立即解除渲染阻塞。
2. `GOLD_POOL` / `STOCK_LIST` / `MAHORO` / `CANDIDATE` / `SECTOR_RS`（合计 827KB）改为**点击 tab 时动态 `import()`**，首屏立减 63%。
3. 删掉 `SCAN_DATA.js`（23B 空壳）与 `RECOMMEND.js` 的 script 标签。

---

## 五、修复优先级清单

### 已完成（本次，已提交）

- [x] 重写 `guard_v8_freshness.py`，受检 16 → 46 个，盲区清零
- [x] `v8_algo.yml` 改造为每日数据体检，消除与 build_deploy 的重复
- [x] 补 13:05 / 13:15 下午开盘抓取缺口
- [x] 收敛周末空转，改为周六单次日历刷新
- [x] 修正周末代班 automation：禁止手动 `deploy_v8.py`（依小九交接铁律）

### 待小九周一处理（P0，需 v6 侧配合）

- [ ] **建立 24 个算法数据的生产链路**。三选一：
  - **A**（推荐）把 v6 的算法脚本迁到 v8 仓，云端直接算；
  - **B** v6 机器每日收盘后经 `api_push_raw.py` 把 24 个 json 推到 v8 `raw_data/`；
  - **C** 明确放弃这些模块，从前端和 `DATA_SOURCES` 里彻底删除，不留标本。
  - ⚠️ 选 B 必须同步把 `cloud_fetch_v8.py` 的 `RAW_DIR.glob("*.json")` 全删改为**白名单删除**，否则推来即被删。
- [ ] **给 7 个无 `update_time` 的模块补时间戳**，否则前端永远显示空白。
- [ ] **前端加「停更」可视标记**：读 `data/freshness_status.json`，对停更模块在卡片角标显示「⚠️ 已停更 N 天」，替代当前的静默。

### 待处理（P1）

- [ ] 给关键抓取函数加第二数据源（参考 v6 的东财→neodata→westock 三级降级）
- [ ] 46 个 `<script>` 加 `defer`；大文件改按 tab 懒加载
- [ ] 清理孤儿引用 `COCKPIT_BACKTEST_DATA` / `SH_SZ_HISTORY`
- [ ] 清理死数据 `SCAN_DATA.js` / `RECOMMEND.js`

---

## 六、给主人的直白总结

1. **线上过半模块是标本**，不是「今天没跑成」，是**从 7-30 起就没有任何程序在更新它们**。斐波那契停 4.2 天、板块 RS 停 6.1 天。
2. **驾驶舱、三重共识、逆势龙头这些算法卡，云端一次都没算过**。v8 仓里根本没有算法代码，全在 v6。
3. **数据源只有东方财富一家**，它抖一下就静默展示旧数据，没有备胎。
4. **原来的看门狗是瞎的**——唯一会出问题的 24 个模块全在它监控范围之外，所以天天报「一切正常」。现在已经修好，能看见真实情况了。
5. **轻量化只做了一半**：文件是拆开了，但 46 个全部同步加载，首屏照样 1.3MB。
6. 我今晚能修的（守卫、定时任务、重复 workflow）已经修完推上去了。**真正的大头——给那 24 个模块接上数据源——需要动 v6 的算法脚本，这是小九的地盘，周一交给她**。

---

**报告人**：阿狸咪
**审计方法**：前端 `window` 变量 × `DATA_SOURCES` 映射 × `raw_data` 实存 × git 提交历史，四方交叉对账
**审计脚本**：`_audit_v8.py`（已入库，可随时复跑）
