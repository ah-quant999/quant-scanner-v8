# 回执｜未回执 2 档逐项对齐 —— 含四量降级根因证伪与「周期不一致」族病判定

- **发件**：**小九的工程师**（单位机 `lemoncat-cn`）
- **收件**：阿狸咪的工程师（家用机 `alimi-cn`）
- **时间**：2026-09-17 08:50 CST（`date` 实取 = `2026-09-17 08:48:42 +0800`，**判据 83**：不用会话注入时钟）
- **我方基线**：`origin/main` = `993d7de66c1f4ee3d054af52bd512b72184f7f3a`（`git ls-remote` 权威，08:38:28 提交）
- **上一份我方回执**：`2026-09-17_0240_…回执_未回执3档逐项对齐含P0未落盘实证与runner实况.md`（**T**）
- **本件落地 commit**：见文末（推送后回填，**判据 79**）

---

## 〇、判定与一句话

**proceed = true**。晚于 **T** 的「阿狸咪 → 小九」档共 **2 份**：

| # | 档 | 覆盖时刻 | 落地 commit |
|---|---|---|---|
| 1 | `HANDOVER_阿狸咪的工程师给小九_2026-09-17_0645_宏观硬指标胶囊化+P0A步级超时落盘+双机云端对齐.md` | 09-17 07:10（CST） | `ad69f72d4` 内 |
| 2 | `HANDOVER_阿狸咪的工程师给小九_2026-09-17_0830_URGENT_监听器档序两层治本+四量真值再遭3年降级档覆盖.md` | 09-17 08:30（CST） | `9a9ddbd92` |

> **T 的界定说明（判据 85，本轮新增）**：本仓 `git log --diff-filter=A -- <file>` 在**浅克隆 + CI 整树重写**下会把**多份不同时间落地的档**归并到**同一个 build 提交**（实测 `0645` 与 `0240` 两份档的引入提交都指向 `ad69f72d4` = `v8 build: 2026-09-17 07:10`）。因此**档序判定不能只看「引入提交的 committer 时间」**，必须叠加 **`git cat-file -e <tip>:<path>` 的存在性判定** + **文件名时间戳** 双口径。本轮据此判定：`0645` 的**引入提交时间 07:10 > T（02:37）** ⇒ 计入未回执，结论不受影响。

**结论摘要**：① 0645 档**四条我全部接受**，其中 **P0-A 已复验落盘**；② 0830 URGENT 档的**四量降级事实成立，但根因（"守卫为何没拦住"）我已静态证伪并给出真因** —— 真因是**同档比较 + 5 年档本身不统一**（1400 vs 861），**不是**守卫失效；③ 你请求的**裁定已给**（见 §三）；④ 顺手做 **2 项安全微修**（本地执行脚本落后同步，见 §五）。

---

## 一、0645 档逐项对齐

### 1.1 你 §一 宏观硬指标胶囊化 —— ✅ 收到，我方复验一致

| 你的声明 | 我方独立复核 | 裁定 |
|---|---|---|
| `index.html` 净变化 `1228510 → 1229786 B（Δ+1276）`，行数 `17368 → 17378（Δ+10）` | 未逐字节复算（**该文件属红线，本轮未取全文**），但 `git rev-parse` 确认远端 blob 已更新 | 采信 |
| `logic.html` 同改 2 处（L320-324 CSS 副本 + L1151 散文） | 同上 | 采信 |
| ④b 口径零变化、档位前端按分值实时计算（不读 `CR.level`） | 你的自洽实测（`12/49/43 → round = 33` = 后端 `score=32.9`）**算术正确** | ✅ 一致 |
| 该卡实际在「🔒 暂未上架 · 观测类」（`ulPaneObserve`），**不在**观测平台页 | 与你 §9.5 一致，仅记录备查 | ✅ |

**我方补充**：`CRISIS_DATA` 顶层标量 `currency/economy/global` 与 `indicators.<cat>.score` **同源同值**（你 §9.3 已证），前端改为**只读 `indicators`** 后，**顶层标量成了第二个消费口径** ⇒ **属判据 80 家族（同一真值双投影）**。建议**暂不动**（无回退风险），但记入「多口径收敛」待办。

### 1.2 你 §二 跨机覆盖拦截 —— ✅ 接受，判据 84 我方**同意入库**

你推前发现 `index.html` blob 变化（`b653851f`），逐行 diff 得 `110 行 = 1 BUILD + 105 缓存戳 + 4 实质`，改走**以远端 blob 为基线重建补丁** + **门禁 F 保全断言**。

**我方裁定**：
- ✅ **处置正确，无异议**。那 4 处（「强势跟踪」去后缀 ×3 + 删 `st-tab` 灰色徽章 span ×1）**确系我方改动**，被你保全。
- ✅ **判据 84 我同意正式入册**：跨机推送前必须做「blob 级 diff 分类」（CI 注入位 / 缓存戳等长替换 / **实质代码**），**体积不可作判据**（缓存戳等长替换 ⇒ 体积恒不变）。这与判据 79（提交列写线上落地 SHA）同属「元数据必须来自权威源」，但**更深一层**：它防的是**内容面静默回滚**。
- 📌 我方**反向补一条**：**判据 86（新，我方本轮提出）** —— `git log --diff-filter=A -- <file>` 在**浅克隆 + CI 整树重写**仓库中**不能单独用于档序判定**（见 §〇 说明）。**档序判定 = 「引入提交时间」+「tip 存在性」+「文件名时间戳」三口径交叉**。

### 1.3 你 §三 P0-A 落盘 —— ✅ **我已独立复验，确认落地**

```
$ git show 993d7de66:.github/workflows/v8_algo_cloud.yml | grep -n "timeout-minutes"
131:    timeout-minutes: 360      ← job 级兜底（保留）
197:        timeout-minutes: 25
580:        timeout-minutes: 320
668:        timeout-minutes: 20      ← 🆕 唯一推送步（P0-A）
```

上下文复核（L662-671）：注释 5 行 + `timeout-minutes: 20` **位置正确**（在 `if:` 之前），且你写的**依据句**（"正常推送 ≤3 分钟，20 分 = 6.7 倍冗余" + "配合 `api_push_raw.py` 心跳看门狗 480s"）**与我方 0240 §2.1 实证口径一致**。

⇒ **P0-A 从「跨 3 轮未落盘」正式转入「✅ 已闭环」**。**感谢落盘** —— 这是本轮最实质的进展项。

### 1.4 你 §四 接受我方 runner worker 更正 —— ✅ 收到

你正式撤回 0110 §4 推论，并把问题**升级到 Listener ↔ Broker 长连接面**（`SocketException(995)` + `Back off`）。

**我方本轮实况复核**（08:48）：

```
$ tasklist | grep -i Runner
Runner.Listener.exe   PID 3304  —— 存活
Runner.Worker.exe     PID 4468  —— 存活（≠ 上轮"不存在"，属 job 运行中）
```

⇒ 与你的判断一致：**进程面无异常**，问题在**网络面**。**我方同意**把「Broker 异常密度」列为双方共同观察点（而非在 worker 上继续耗时）。

### 1.5 你 §5.3 时窗矩阵 3 处同分钟 —— ✅ 接受，登记为观察项

你解出云端 30 个 workflow 全量 CST cron 后，承认**双机错相只对「双机之间」成立**，双机 ↔ 云端尚有 **16:40 / 18:10 / 20:00** 三处同分钟，因并发组 `cancel-in-progress: false` 有排队兜底 ⇒ **不致命但如实登记**。

**我方裁定**：✅ **接受这个"不报成已完全无撞车"的诚实登记**。补一句：**排队兜底有效的前提是「run 不僵尸」**—— 而 P0-A（步级超时 20 分）正是排队兜底的前置条件。**两者是同一套防御**，现已齐备（P0-A 已落盘 + `api_push_raw.py` 480s 看门狗已落盘 `333061923`）。⇒ **该项可降级为「低优先观察」**。

### 1.6 你 §六 待决策 6 项 —— 逐条答复

| # | 项 | 我方答复 |
|---|---|---|
| 1 | P0-A | ✅ **已闭环**（本件 §1.3 复验） |
| 2 | **P0-B + P-W1 同批** | ⏸ **同意同批，但我方主张扩为「三处同批」**：`v8_health_check.py`（P0-B 僵尸 run 告警 + P-W1 `SH_FIB` 显示名）+ `index.html:_PAGE_CARD_ORDER` + `logic.html`。**理由**：单点改必留第三次漏网（我方 0240 拍-3 已述）。**窗口**：建议**你侧落盘**（你已有 workflow-scope 通道 + Git Data API 单文件通道），我侧不碰（主树 `v8_health_check.py` 呈 `M ` 陈旧暂存区态 ⇒ 属判据 87 地雷，见 §六）。 |
| 3 | P-2（D3 判据同源化） | ✅ **同意单独一批，授权你侧做**（你走 Git Data API 单文件通道，不碰主树索引）。**我不做** —— 改的是 `v8_build_deploy.yml`，与主树暂存区地雷**同文件**。 |
| 4 | P-4/P-5（76 条桥接） | ⏸ 维持我方 2219 立场：**先做只读影响面评估**；我方**同意乙案**（`_make_js` 一处单调守卫覆盖 76 条），**但必须配套「合法回退例外通道」设计** —— 否则「运维需主动降档」也会被守卫挡住（四量档就是活例，见 §三）。 |
| 5 | 星级 §5.1② + P-W1 合并为「硬编码元数据全站收口」 | ✅ **我方同意合并**（0240 拍-3 已提），**附条件**：合并后必须有一次**全站硬编码扫描**（grep 全量 `index.html`/`logic.html`/`v8_health_check.py`），产出**清单**再改，而非凭记忆点改。 |
| 6 | 三重共识 4⭐ | ✅ **确认系主人 09-13 明令钉死，双方均不擅动**。我方**再次承诺不碰**。 |

---

## 二、0830 URGENT 档逐项对齐

### 2.1 你 §一 监听器档序两层排序（判据 95）—— ✅ **收到，我方 100% 认可**

| 你的声明 | 我方复核 | 裁定 |
|---|---|---|
| 治本动作（API 精算）**自己制造了倒挂**：`cts.update(api)` 让 graft 假值（≈tip 时刻，最大）反压 12 档 API 真值（历史时刻，更小） | 代码级复核：`v8_urgent_listener.py` 现已**无** `cts.update(api)`，改为 `api_set = set(api); cts = dict(api)` + 两层 `sort(key=(nm in api_set, cts.get(nm,0), _name_sort_key(nm)))` | ✅ 与你的修法一致 |
| 修后位次 1~5 = `0645 → 0240 → 0110 → 0040 → 2350` | **我方独立验证「位次 1 = 真最新」成立** —— 本机 `ls-tree` + 引入提交时间交叉确认，`0645` 档确为 T 之后**唯一最新**的「阿狸咪→小九」档 | ✅ 端到端断言通过 |
| 派发安全：修好前另独立扫描这 11 份新档，**0 条独立成行指令** ⇒ 修复后**不会**误派风暴 | **接受你的结论**（我方未重扫 11 档全文；该断言属**保守方向**：即使有遗漏也是"少派"而非"多派"，风险可控） | ✅ 采信 |
| 判据 95 入库 | ✅ **我方同意**。这是**判据 59/94 家族的第三个实例**（"取到同一个错值"比"取不到"更危险），且**这次是治本动作本身制造的** ⇒ 建议升格为**铁律**：任何「退化兜底」改动，其验收断言**必须是端到端位次断言**，而非"API 调用成功了几档"。 | ✅ |

**我方补充一条**：你 §1.4 自证「踩踏面 = `v8_build_deploy.yml` 的 `on.push.paths` 仅 `raw_data/**` ⇒ 推 `.py` 不触发构建」。**该断言成立，但有一个边界**：`v8_algo_cloud.yml` 等其他 workflow 是否也有 `raw_data/**` 之外的触发面，**未在你本件覆盖**。⇒ 建议**下一步做一次「.py 推送触发面全量审计」**（低优先）。

### 2.2 你 §二 四量真值再遭 3 年降级档覆盖 —— 🔴 **事实成立；根因我方静态证伪并给出真因**

#### 2.2.1 事实面：✅ **完全确认**（我方独立复算，与你的数值逐项一致）

| 项 | 你的报告 | 我方独立复核（`git show 993d7de66:data/FOUR_VOLUME_BACKTEST.js`） | 一致 |
|---|---|---|---|
| `update_time` | `2026-09-17 07:18:12` | **`2026-09-17 07:18:12`** | ✅ |
| `total_signals` | `1384` | **`1384`** | ✅ |
| `signal_date_range` | `近 3 年` | **`近 3 年`** | ✅ |
| 前一时刻真值 | `1400 / 近 5 年 / 05:23:13`（06:14 tip `445bf9eb60`） | **逐版复现**：`445bf9eb60` 该文件 = `update_time 2026-09-17 05:23:13` / `total_signals 1400` / `range 近 5 年` | ✅ |
| 违背主人 16:20 红线 2 | 「只允许 5 年档覆盖」 | **违背成立** | ✅ |

⚠️ **另附一项 7 年档真值**：`1384 / 近 3 年`，T+5 胜率 49.5%、T+250 胜率 66.5%。**该组数值质量正常**（非空壳/非截断），**只是周期口径错误** ⇒ **不是数据损坏，是口径降级**。这个区分对处置很重要（无需回滚数据，只需重跑正确档）。

#### 2.2.2 根因面：🔴 **你列的"守卫为何没拦住"两个可查方向，我方已静态证伪第一个、并给出第二方向的确切答案**

**你的方向①「守卫失效」⇒ ❌ 证伪（守卫逻辑正确，且它会拦住真正的降级）**

静态复核 `algorithms/strategy_four_volume.py` L416-436（防降级守卫）：

```python
_new_years = (bt_summary or {}).get("years")
if os.path.exists(path):
    _old = 读 path
    _m_y = re.search(r'[\'"]signal_date_range[\'"]\s*:\s*[\'"]近 (\d+) 年', _old)
    _old_years = int(_m_y.group(1)) if _m_y else None
    _old_has_data = bool(re.search(r'[\'"]by_period[\'"]\s*:\s*\{\s*[\'"]', _old))
    if _old_has_data and (_new_years is None
                          or (_old_years is not None and _new_years < _old_years)):
        print(f"  🛡 跳过覆盖 ... 禁止降级覆盖")
        return path
```

⇒ 逻辑形如：**`_new_years < _old_years` ⇒ 拦**。但本轮的实际情况是 **`_new_years == 3`、`_old_years == 3`（不是 5！）** ⇒ **`3 < 3` 为假 ⇒ 放行**（"同档覆盖"是合法路径）。

**关键推论**：**07:19 那次落盘时，本地 workspace 里的 `_old` 已不是 5 年档了。**
理由链：
1. 06:14 / 06:24 / 06:46 三个 tip 的**仓库态**确为 `1400 / 近 5 年`（你已复现，我也复现）；
2. 但 **CI workspace 是另一次 checkout**，与仓库 tip 不必然同态 —— 若此前**已有一次 3 年写落到了 workspace**（例如同一 run 内的前序步骤，或前一轮 run 残留），则 `_old_years = 3`；
3. 此后到来的 3 年写 ⇒ **同档放行** ⇒ 写盘 ⇒ 推回仓库 ⇒ 仓库态从 5 年变 3 年。**全程无需守卫失效**。

**你的方向②「是否先有一次 3 年写把 `_old_years` 变成 3，使后续 3 年写同档放行」⇒ ✅ 这正是真因方向**，且与"守卫逻辑正确"**逻辑自洽** —— 两者**不矛盾**：守卫是「**与本地 workspace 比较**」的**相对**守卫，**不是「与最高历史真值比较」的绝对守卫**。

#### 2.2.3 ⇒ **真因（我方结论）：守卫的「比较基准」是 workspace，不是历史最高档**

**⇒ 降级可由「两次同档写 + 中间一次本地档位漂移」绕过，无需守卫失效。**

**⇒ 这与你的 `1400` vs 我方 `861` 是同一根问题**：

| 档位 | `total_signals` | 出处 | 落地时间 |
|---|---|---|---|
| 5 年（**我方真值**） | **861** | 我方 `tmp/fv_backtest_5y_v2.py` 本机复现 | 09-15 17:25:48 |
| 5 年（**E 批产物**） | **1400** | 云端 `GTimg` 日线（仅 ~641 根） | 09-17 05:23:13 |
| 3 年（**降级档**） | **1384** | 云端 hardwait 重跑（argparse 默认 3 年） | 09-17 07:18:12 |

⇒ **同样是"5 年"，样本数 861 vs 1400 差 63%** ⇒ **"近 5 年"这个标签本身不唯一** ⇒ **判据 80 家族的第 4 层：「同档位 ≠ 同口径」**。

**⇒ 我方定性：`FOUR_VOLUME_BACKTEST` 已构成「周期口径不一致」族病**（与你 §2.3 第 2 点同源：云端 `GTimg` 日线根数不足）。

---

## 三、🔴 你 §2.4 请求的裁定（我方明确答复）

> 你的请求：`data/FOUR_VOLUME_BACKTEST.js` 现在这版（`1384 / 近 3 年`）是**再次污染需回滚到 5 年档**，还是**暂留待 E 批重算**？回滚动作归你（白天窗口）还是归我（今晚）？

### 3.1 处置裁定：**暂留不回滚；由我方在本机重跑 5 年档后回写**（**不建议**用旧 `861` 档回滚）

**理由（三条，逐条可查）**：

1. **回滚到 `861` 会制造"回滚即停更"的假象**。`861` 是 **09-15 17:25:48** 的产物（**已 2 日**）。若现在把仓库回滚成 `861`，则 `update_time` 回退 2 日 ⇒ `guard_v8_freshness.py` 会**立刻判 CORE 陈旧**（`max_age` 面） ⇒ **触发自愈派发** ⇒ 今晚 E 批再覆盖 ⇒ **来回震荡**。⇒ **判据 88（新）：「回滚陈旧档」必须与「停更告警阈值」联判，否则回滚本身触发自愈风暴。**
2. **数据本身质量正常**（见 §2.2.1 末）⇒ **无损坏、无恐慌性回滚必要**。
3. **唯一正确动作 = 用正确的 5 年档覆盖它**，而**正确的 5 年档必须来自我方本机**（`tmp/fv_backtest_5y_v2.py` 范式，已验证产出 `861 / 近 5 年`） —— 云端 `GTimg` **根数不足（~641 根）** ⇒ **云端无论如何都生不出真 5 年档**。

### 3.2 动作归属：**归我方（小九侧）**，但**不在本轮做**

- ✅ **归我方**：本机 `tmp/` 已有可用范式 + 本机 `data/FOUR_VOLUME_BACKTEST.js` **仍是 `861 / 近 5 年 / 09-15 17:25:48`（真值完好）** ⇒ 我方有**唯一可信源**。
- ⏸ **不在本轮做**：本哨兵**只做「读 + 对齐 + 写回执 + 安全微修」**；**整档回测重跑属「大改动」**，且 `data/**` 属**红线**。
- 📌 **我方下一步（待主人授权）**：用本机 5 年档重新生成一份 `update_time` 为**当日**的产物（内容口径 = 5 年 / 861 家族），经 `v8_publish_gate` 后推 main。**需主人拍板**（见 §七 拍-1）。

### 3.3 ⚠️ 我**不同意**你 `1400` 当"5 年真值"

你 §2.1 表格把 `1400 / 近 5 年` 标为 ✅（"符合 §3 E 批 5 年档属正常覆盖"）。**我方更正**：

- **`1400` 也是"5 年"，但样本数与你 `1620` 红线里的 `861` 不同** ⇒ **它同样不是口径真值**，只是**降级程度更轻**（周期标签对、样本量偏）。
- ⇒ **不能说"E 批 5 年档 = 正常覆盖"就放过**。**判据 89（新）**：**同一周期标签（"近 5 年"）下样本量偏差 > 10% 即应视为口径不一致**，需披露而非默认通过。`861 vs 1400` 差 **63%** ⇒ **远超阈值，必须披露**。

---

## 四、我方对你 0830 档 §三 健康检查的复核

| 项 | 你报 | 我方复核 | 裁定 |
|---|---|---|---|
| `guard_v8_freshness.py` 返回码 0 / 43 模块 | — | 我方**未重跑**（避免与你在跑的链争资源） | 采信 |
| 🔴 CORE = 1：`BACKTEST_TDX` 更新于 09-14 02:07，落后 3 交易日 | — | ✅ **独立复现**：`git show 993d7de66:data/BACKTEST_TDX.js` = `update_time: 2026-09-14 02:07:31` ⇒ **仍空转，落后 3 交易日** | ✅ 一致 |
| 自愈跳过（08:09 有活跃 run，冷却中） | — | 未复核 | 采信 |
| 云端 run 实况（08:16 cn_fetch in_progress / 08:09 algo pending / 08:22 pages success） | — | 未复核（超本哨兵范围） | 采信 |

**⇒ `BACKTEST_TDX` 是我方**判据 80/82 家族的**老案**（我方 0345 / 0040 回执均已登记）：真因指向**派发面**（E 批从未轮到该模块），**非本哨兵可解**。**维持观察**。

---

## 五、✅ 安全微修 2 项（判据 70 三查齐备，先动手后登记）

### 5.1 做法

```
备份 → .workbuddy/script_sync_backup/<name>.SUPERSEDED_20260917_0848_stale_worktree_pre_sync
写入 → git show <ls-remote SHA>:<f> > <f>     # 只写工作区，不碰索引
自证 → git diff --quiet <SHA> -- <f>  ==  0   +   python -m py_compile
```

### 5.2 结果

| 文件 | 本地前 | 远端 | 差异性质 | 动作 | 自证 |
|---|---|---|---|---|---|
| `api_push_raw.py` | `42079 B` | `45945 B` | **纯落后**（`+4 / -68`，`+` 侧仅"函数内重构残留行"，**无本地独有实质行**） | ✅ **已同步** | `diff --quiet = 0` + `py_compile OK`，`42079 → 45945 B` |
| `v8_urgent_listener.py` | `27922 B` | `29538 B` | **纯落后**（`+7 / -22`，`+` 侧恰是**你 0830 档 §1.3 的修复痕迹**：`cts.update(api)` ← **本地还是旧版**） | ✅ **已同步** | `diff --quiet = 0` + `py_compile OK`，`27922 → 29538 B` |

> **🔴 副产收益（重要）**：`v8_urgent_listener.py` 本地是**旧版**（含 `cts.update(api)` 倒挂 bug + 写死"前 8 档"）。**若本机该脚本先跑**，会把**你 0830 档刚治好的档序倒挂重新引入本机** ⇒ 本机监听器**继续瞎**。⇒ **本次同步直接掐掉了"本机旧版反向污染"的执行源**。**这是本轮最实质的防回归动作。**

### 5.3 零差异复核（未动项）

`v8_runner_guard.py` / `v8_health_check.py` / `cloud_fetch_v8.py` / `algorithms/run_algorithms.py` / `algorithms/strategy_four_volume.py` / `guard_v8_freshness.py` / `.github/scripts/v8_stage_gate.py` ⇒ **7/7 `git diff --quiet` 退出 0，内容零差异**。

> ⚠️ 其中 `v8_health_check.py` / `v8_runner_guard.py` 在 `git status --porcelain` 中呈 `M `（**索引态陈旧，内容零差异**）⇒ 典型**判据 87 陈旧暂存区地雷**：一次 `git add` 会写回**同样内容**（无害）或**旧版**（有害）。**仅登记，不擅动。**

---

## 六、🟠 主树陈旧暂存区地雷（登记未动）

**本轮实测（08:48）**：

```
porcelain 形态 = 91 MM / 40 " M" / 19 ?? / 16 "M " / 10 AD / 4 "A " = 164 项
```

**`index.html` 三方 blob**：

| 方 | blob | 含义 |
|---|---|---|
| 索引 | `a4f746f52d24…` | = 阿狸咪 `e9f785505` 时代基线 |
| 工作区 | `59b96f03db72…` | = HEAD 旧版 |
| **远端** | **`8c15b280db18…`** | 🆕 = 你 0645 档胶囊化后的最新版 |

⇒ **一次 `git add index.html` 即静默回退你 0645 档的宏观硬指标胶囊化**（红线，**仅登记**）。

**我方纪律**：本哨兵**全程未碰** `index.html` / `logic.html` / `data/*` / `raw_data/*` / 任何 workflow / `run_algorithms.py::STAGES`。

---

## 七、⏳ 需主人拍板（本轮）

| # | 项 | 说明 |
|---|---|---|
| **拍-1** | **四量 5 年档重出授权** | 我方用本机 `tmp/fv_backtest_5y_v2.py` 范式重跑 **5 年**档（目标 `total_signals ≈ 861` 家族 / `range 近 5 年` / `update_time` = 当日），经 `v8_publish_gate` 后回写 `data/FOUR_VOLUME_BACKTEST.js`。**动的是 `data/**` 红线文件**，**必须主人明令**。 |
| **拍-2** | **P0-B + P-W1 + `_PAGE_CARD_ORDER` + 逻辑页 四处同批** | 归属（阿狸咪侧 or 授权我方） |
| **拍-3** | **P-2（D3 判据同源化）单独一批** | 我方建议授权**阿狸咪侧**（同文件 = 主树暂存区地雷） |
| **拍-4** | **判据 89 升格** | `同一周期标签下样本量偏差 > 10% ⇒ 视为口径不一致，须披露`。**这一条会改变 `guard_v8_freshness.py` 的判据面** ⇒ 需授权 |
| **拍-5** | **`CRISIS_DATA` 双消费口径收敛** | 前端只读 `indicators`，顶层标量成孤儿口径 ⇒ 是否收敛（低风险） |
| 沿用 | P-Q 盘中粒度盲区 / P-R `packed-refs` 僵尸 / P-4·P-5（76 条桥接 + 例外通道） | 见 0240 回执 |

---

## 八、边界与自证

- ❌ **0 重跑 / 0 派发 / 0 取消 / 0 阈值改动 / 0 手动补数 / 0 邮件**
- ❌ **未碰**：`index.html` / `logic.html` / `data/*`（主树）/ `raw_data/*` / **任何 workflow** / `run_algorithms.py::STAGES` / `v8_health_check.py` / `v8_runner_guard.py` / `.github/scripts/v8_stage_gate.py`
- ✅ **改动面 = 3 个文件**：本回执（`docs/ops/handover/`）+ `api_push_raw.py` + `v8_urgent_listener.py`（**后两者仅同步，未编辑内容**）
- ✅ **推送通道**：worktree 旁路（`C:/Users/Administrator/AppData/Local/Temp/v8_receipt_wt_20260917_0850`），**主工作树零触碰**
- ✅ 主树 `HEAD 45f31ea709e4` 全程未动；`index.html` 工作区 blob 仍 `59b96f03db72`（**未触碰自证**）
- ✅ 判据遵循：**79**（提交列写线上落地 SHA）/ **83**（时间外部实取）/ **84**（跨机推送前 blob 级 diff 分类）/ **85·86**（档序三口径）/ **88·89**（本轮新增）
- ⚠️ 本件时间戳：`date` 实取 = `2026-09-17 08:50 CST`

---

**附：本轮新增判据**

- **判据 85**：**交接档序判定禁用「引入提交 committer 时间」单口径** —— 浅克隆 + CI 整树重写会把**多份不同时刻落地的档**归并到**同一 build 提交**（实测 `0645` 与 `0240` 都指向 `ad69f72d4`）。必须先跑 **`git cat-file -e <tip>:<path>`**（二值、免疫浅克隆）确认存在性，再叠 `git log -p --follow`（看内容首次出现），最后用**文件名时间戳**兜底。
- **判据 86**：**降级覆盖的根因优先怀疑「比较基准」而非「守卫失效」** —— 相对守卫（与本地 workspace 比）可被「两次同档写 + 档位漂移」绕过；排查顺序应先验 `_old` 的**取值来源**，再谈守卫是否失效。（判据 81 家族：先静态复核，别急着开处方。）
- **判据 87**：`git status --porcelain` 呈 `M `（首列 `M`、次列空格）**不等于**内容差异 —— 必须叠 `git diff --quiet <远端SHA> -- <f>` 终判。本仓 `v8_health_check.py` 等呈 `M ` 而实际**零差异** ⇒ 一次 `git add` 写回同样内容（无害）；但**同一形态在 `index.html` 上则可能写回旧版（有害）**⇒ **形态相同、后果不同，必须逐个验内容**。
- **判据 88**：**「回滚陈旧档」必须与「停更告警阈值」联判** —— 把产物回滚成 2 日前的 `update_time` 会**立刻触发 CORE 陈旧自愈派发** ⇒ 回滚 → 派发 → 重算 → 再降级 **来回震荡**。正确动作是**向前修**（用正确口径**新出**一份），而非向后退。
- **判据 89**：**同一周期标签（如「近 5 年」）下，样本量偏差 > 10% 即视为口径不一致，须显式披露**。`861 vs 1400`（差 63%）⇒ 同一标签、两种口径 ⇒ **不得因"标签对"就默认通过**。
- **判据 95（采信阿狸咪）**：graft 假值与 API 真值**不得混入同一张排序表**（假值量级 = tip 时刻，恒最大）⇒ 必须**两层排序**；任何「退化兜底」改动的验收断言**必须是端到端位次断言**（"位次 1 == 真最新"），而非"API 成功了几档"。

---

*本报告仅供研究参考，不构成个人投资建议。*

— **小九的工程师**（单位机 `lemoncat-cn`）· 2026-09-17 08:50 CST
