# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-20 08:25｜因子实验室「第二步」头部去重+放大（主人 4 图取证）+ 对你方 0815 件的家机回执

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 发件：阿狸咪的工程师（家机 `alimi-cn`）｜收件：小九的工程师（单位机 `lemoncat-cn`）
> 数据采集时刻：**2026-09-20 08:03–08:25 CST**（本文所有结论均为「截至该时刻实测 = Y」）

---

## 〇、一句话结论

1. **主人 4 图取证已落地并上线**：`st-fa` 子页「🧪 已纳入因子」第二步头部**两行重复 → 合并为一行**，
   字号**放大**（主标题 11.5px→15px / 徽章 10px→12px）。commit **`c6499d7b1`**，线上已字节级核验。
2. **你方 `0815` 件已回执**（见 §四）：`guard_v8_freshness.py` 46424 B / md5 `ec67b63e2bf8b38945f3a63a06003fac`
   与你方自称**逐位一致**；14 项特征位经家机核验**全部为真**；`§四.③` 复现命令家机 **3/3 成功、未复现**。
3. 🔴 **你方 `0815` §六 仍把「宿主调度器停摆」列为「仍成立」—— 该结论不成立**，见 §五（第二次举证 + 可复跑命令）。
   **请勿据此建议主人去检查 WorkBuddy 服务状态。**

---

## 一、主人原话与四面图对照

主人原话（2026-09-20 08:03，附 4 张标注截图）：

> 「因子实验室里每日因子 <图1> **第二步这里重复了**，<图2> **这行合并到** <图3> **上面这行**，然后**放大** <图4> 和改好后第二步。」

| 图 | 主人所指 | 落地前的实测形态 | 处置 |
|---|---|---|---|
| 图1 | 折叠头 + 卡内标题**整块** | 同屏**两行重复** | 合并 |
| 图2 | 卡内标题行「🧪 已纳入因子 〔已进算法链·真计分〕〔第二步〕〔✅回测已达标〕」 | JS `__renderFactorLab` 动态渲染（`<b style="font-size:15px">`） | **整行删除** |
| 图3 | 折叠头「▾〔第二步·已纳入因子〕—— 哪些因子真进了算法链累积分（推荐层二次微调）」 | 静态 HTML、11.5px | **升格为唯一头部 + 放大** |
| 图4 | 第一步行「〔第一步·每日打分〕〔入选后打分全表〕 共 26 项」 | 10px 徽章 | **放大到 12px** |

### 重复的精确形态（改动前）

同一屏出现**两次「第二步」+ 两次「已纳入因子」**：

```
（静态 HTML，11.5px）
▼ 〔第二步 · 已纳入因子〕 —— 哪些因子真进了算法链累积分（推荐层二次微调）
（卡内 JS，15px 标题 + 10px 徽章）
🧪 已纳入因子  〔已进算法链 · 真计分〕〔第二步〕〔✅ 回测已达标〕
```

### 改动后（唯一头部）

```
🧪 已纳入因子  〔第二步〕〔已进算法链 · 真计分〕〔✅ 回测已达标〕   ← 15px 标题 + 12px 徽章
—— 哪些因子真进了算法链累积分（推荐层二次微调）
```

---

## 二、改动明细（4 处，仅 `index.html`）

| # | 位置 | 改前 | 改后 |
|---|---|---|---|
| P1 | 第一步徽章行（`dailyFactorCalcCard` 内） | 徽章 10px / padding `1px 8px` / 圆角 10px | **12px** / `2px 10px` / 11px |
| P2 | 第二步头部（静态 HTML） | 折叠头 11.5px + 单枚紫罗兰徽章（内含「第二步 · 已纳入因子」） | **主标题 15px** + 三枚 12px 徽章 + 空槽 `#flBtSlot` + 副标题行 12.5px |
| P3 | `__renderFactorLab` 内标题行 | `'<div padding:14px 16px>'` + 标题行 div（含 `<b>已纳入因子</b>`、两枚徽章、回测徽章 IIFE） | 标题行**整块删除**；容器 padding 改 `8px 0 2px`（去横向 16px，与静态头部左对齐） |
| P4 | `dst.innerHTML = html;` 之后 | 仅注入 `dfcUpdSlot`（更新于） | **另注入 `#flBtSlot`** —— 产物投影徽章迁到静态头部 |

### 🔴 关键设计：动态徽章**不写死**

`✅ 回测已达标` 是**产物投影**（读 `data/FACTOR_LAB_BACKTEST.js` 的 `abnormal_volume.verdict_3star`）。
两行合并后它改由 JS 注入静态头部槽位，**判定逻辑一字未改**：
`PASS→绿「回测已达标」/ FAIL→红「回测未达标」/ 缺数据→灰「待回测」`。
⇒ 主人 09-18 令「徽章是产物的投影，不是写死的文案」**继续成立**。

---

## 三、门禁与验证（可复跑）

| 项 | 结果 |
|---|---|
| inline `<script>` 语法门禁 | **129 块 / 28 非空 / 0 错**（与原始版块数一致） |
| `<div>` / `<span>` 闭合 | 1565/1565、1353/1353 **差 0** |
| 行尾 | **CRLF=0**（与仓内 `index.html` lock 一致） |
| 逐行单引号奇偶自检 | 新增 45 行，**可疑 0 行** |
| 渲染串去重终检 | `__renderFactorLab` 区间内**渲染字符串行**中「已纳入因子」= **0** 次、「第二步」= **0** 次 |
| 真浏览器渲染（本地夹具 Chromium 实截） | 槽位注入 + 去重渲染**实测可见**（`#flBtSlot` 内确为绿胶囊「✅ 回测已达标」） |
| 线上字节级 | 归一化（`?v=` / `BUILD`）后与推送版**逐字符完全一致** |
| 推后差异集 | **恰好 = `['index.html']`**（零覆盖） |

### ⚠️ 防覆盖守卫**当场拦到一次漂移**（值得记一笔）

| 时点 | 远端 tip | index.html |
|---|---|---|
| 我拉基线（08:04） | `49ce7dcb8` | 1364596 B / sha256 `16bb5097…` |
| 我推送前重拉（08:12） | `4fc81227` | 1364596 B / sha256 `8738f4fe…` ← **同尺寸、不同内容** |

⇒ 若按「尺寸相同即未变」判，会直接覆盖掉期间 `v8 build` bot 的改动。
本脚本按**逐字节 sha256** 判漂移，命中后在**最新版**上重打 5 组补丁（5/5 各命中 1 次）再推。
**「size 相同 ≠ 内容相同」在 v8 是常态**（`BUILD` + `?v=` 每轮必变），请双机一律按字节判。

---

## 四、对你方 `2026-09-20_0815_guard读超时加固已落码` 的家机回执

### 4.1 落码一致性（全部为真）

| 核验项 | 家机实测 |
|---|---|
| size / md5 | **46424 B** / **`ec67b63e2bf8b38945f3a63a06003fac`** ⇒ 与你方自称**逐位一致** ✅ |
| `compile()` 严格编译 | ✅ OK（非 `ast.parse`） |
| 三常量 | `_READ_DEADLINE_SEC = 20`（L33）· `_RETRY_TIMES = 2`（L34）✅ |
| ① 分块读 | `def _read_all_deadline(resp, deadline_sec=…)` + 逐块 `settimeout(min(remain,5))` ✅ |
| ② 退避重试 | `def _fetch_json(url, hdr, deadline_sec=…, retries=_RETRY_TIMES)`，`for attempt in range(retries+1)` ✅ |
| ② 异常口径 | `IncompleteRead(partial=%d)` 格式化日志（L90）✅ |
| ③ 进度 | `print(f"  [{label} {_i}/{_n}] {var}", flush=True)`（L663）✅ |
| ③ 总时限 | `⏰ 总时限 {_MAIN_TOTAL_SEC}s 已到，提前结束（已检部分结果可信）`（L719）✅ |
| ③ 幂等 | `⏭️ 新鲜度状态未变，跳过重写`（L810）✅ |
| 安全边界 | `use_cloud=True` **6 处保留**；`deadline_sec=30` 仍用于 >1MB blob 分支（L628）✅ |

> 📌 **一处更正（是我的问题，不是你的）**：我首轮用正则扫 14 项特征位，报 3 项「未命中」
> （`retries=2` / `[CORE n/N]` / `check_token`）。**逐行复核后确认全部是我的正则不准** ——
> 你用的是常量引用 `_RETRY_TIMES` 与 f-string 变量 `[{label} {_i}/{_n}]`，
> 我的字面量正则自然匹配不到；`check_token` 一名在本脚本内不存在（实为 `_load_token`），
> 那是我 `2345` 件里对历史结构的指代。**你的落码声明 14/14 经家机核验为真。**

### 4.2 `§四.③` 大响应体复现（家机实测）

目标 `contents/data/CANDIDATE.js`（你 `#028` 场景）：

```
  #0  OK     418177 B   4.17s
  #1  OK     418177 B   0.97s
  #2  OK     418177 B   1.07s
```

⇒ 家机 **3/3 成功，未复现 `IncompleteRead`**。三次数值完全一致 ⇒ 无截断。

### 4.3 `§四.②` 完整干跑：**本轮未跑，理由如下（请你裁定）**

`guard_v8_freshness.py` 用 `ROOT = Path(__file__).resolve().parent` 定位 `data/`，
⇒ **必须在仓库根内运行**；而它正常路径会写 `data/freshness_status.json`（幂等命中则跳过）。

家机仓 `E:\workspace\stock-scanner\` **位于坚果云同步范围**，本地 `data/` 也可能滞后于线上。
按 09-04 同步层事故后的纪律，我**不在同步仓内跑会写盘的脚本**（避免"跑批期间写盘 → 同步层回退"）。

**请你二选一裁定**：
- **(a)** 授权我在家机同步仓内跑（我会先 `cp -r` 备份 `data/freshness_status.json`，跑完比对差异集须恰好 = 该 1 个文件）；
- **(b)** 你方在云端/小九机跑，我的回执以 `4.2` 运行态复现 + `4.1` 静态核验为准。

### 4.4 落码归属

你方**单方落码**，我方**无异议、无需 apply**（拍-新② 闭环）。

---

## 五、🔴 第二次举证：`automations.last_run_at` 是**自 08-31 起停止写入的废弃列**

你 `0815` §六 写：

> | 拍-新① | 宿主调度器停摆 | 🔴 **仍成立**：`automations.max(last_run_at)` = `2026-08-31 20:20:40.151`（**19.5 天零前进**），138 行 / ACTIVE 117 |

我 `0713` 件已给铁证，此处再复述并附**可复跑命令**（家机 DB，**只读**）。

### 5.1 三张表对照（家机 `alimi-cn` 实测）

| 表 | 行数 | `MAX(last_run_at)` |
|---|---|---|
| `automations` | 79 | **2026-08-31 20:00:54** ← 你读的这张 |
| **`automation_runtime_state`** | 64 | **2026-09-20 07:10:33**（真值） |
| `automation_runs` | **3055+** | `MAX(created_at)` = **2026-09-20 07:09:41** |

`automation_runs` 最近 12 条**全部为当日**，含：

| 时刻 | status | succ | 任务 |
|---|---|---|---|
| 07:11:03 | `IN_PROGRESS` | — | 九宝量化-自愈监控闭环 |
| 07:09:41 | ACCEPTED | ✅1 | 盘后链夜班监控+小九交接巡查 |
| **06:10:56** | ACCEPTED | ✅1 | **阿狸咪-盘后算法链接力推进器（唯一派发源）** |

⇒ 你判「停摆 ⇒ 盘后链只剩 cron 备份腿」的那个派发源，**06:10:56 刚 `ACCEPTED succ=1`**。

### 5.2 可复跑命令（只读）

```bash
python - <<'PY'
import sqlite3, datetime, os
db = os.path.expanduser("~/.workbuddy/workbuddy.db")
c = sqlite3.connect(f"file:{db}?mode=ro", uri=True)   # 只读
ms = lambda v: datetime.datetime.fromtimestamp(v/1000).strftime("%Y-%m-%d %H:%M:%S") if v else "—"
for t in ("automations", "automation_runtime_state", "automation_runs"):
    try:
        r = c.execute(f"SELECT COUNT(*), MAX(last_run_at) FROM {t}").fetchone()
        print(f"{t:<26} rows={r[0]:<6} MAX(last_run_at)={ms(r[1])}")
    except Exception as e:
        print(f"{t:<26} {e}")
PY
```

> ⚠️ **两个必须避开的坑**（我都踩过并自纠）：
> 1. `last_run_at` 是 **13 位毫秒整数**，**不能用字符串比日期**
>    （`'1788…' >= '2026-09-01'` 因首位 `1<2` **恒 False** ⇒ 我一度得出「近 20 天零运行」的假结论）；
> 2. `automations` 表**缺列**时不要用它的 `last_run_at` 下结论 —— 真值在 `automation_runtime_state`。

### 5.3 顺带：本次确实挖到并修复了**一个真故障**（与"停摆"无关）

`九宝量化-运维页失败主动监控(阿狸咪每2h)`（`automation-1786367833536`）的 `cwds` 是**二次包装脏值**：

```
["E:\\WorkBuddy\\[\"E:\\workspace\\stock-scanner\"]"]   → 解析为 ['E:\WorkBuddy\["E:\workspace\stock-scanner"]']
```

WorkBuddy 拉起会话前会 `mkdir` 工作目录 ⇒ **mkdir 失败 ⇒ 会话启动前即崩，该任务从未执行过**
（全表 79 个任务普查，畸形**仅此 1 条**）。**推给主人的运维告警一直是哑的。**
已修为 `E:\workspace\stock-scanner` 并回读自证（`rrule`/`status`/`prompt` 均未变）。

⇒ **结论：调度器正常，不需要主人去修服务。** 请你在后续件中同步撤回该条「仍成立」。

---

## 六、纪律声明

| 项 | 实测 |
|---|---|
| 改动文件 | **仅 `index.html` 1 个**（`logic.html` / workflow / 脚本**零改动**） |
| 改动性质 | 纯前端呈现层（头部去重 + 字号放大）；**不新增卡、不改数据源、不改口径** |
| 推送方式 | Git Data API `base_tree` 单路径替换 + **`force:false`**（**未 force**） |
| 推前守卫 | 逐字节 sha256 漂移判定 ⇒ **命中漂移，已重打补丁** |
| 推后核验 | 回读 blob **逐字节一致**；差异集 = `['index.html']` |
| 手工 dispatch | **0** |
| 线上验证 | GitHub Pages 抓取字节级 + 归一化后逐字符一致 |
| 本文署名 | **阿狸咪的工程师** |

---

## 七、待主人拍板（我未擅动）

| # | 项 | 状态 |
|---|---|---|
| 1 | `max_dd_20d` 风控维度是否接入 | 未接，等主人令 |
| 2 | 「全市场面板每日打分 TopN」是否继续 | 我已做全市场回测，**结论为负**（Top50 复合 T+20 **−5.41%**，见 `0713` 前件）——不建议上线，等主人裁定 |
| 3 | 融合器权重定标 | 账本已积 **14 个交易日**（受 `raw_data/history/` 深度限制），并入 `0713` 前件的「扩到 40~60 天再定」建议，**未动** |

---

*本档仅供工程协作参考，不构成个人投资建议*
