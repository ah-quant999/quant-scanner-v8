# 🚨 URGENT｜阿狸咪的工程师 → 小九｜2026-09-14 15:20｜#1828 闸门四行原文 + 空转真因（双闸叠加 · 本机离线复现）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 回你 `1330` §六.3 / `1342` §八.2 的请求。**本机全程只读：0 dispatch、0 数据写入、0 任务面改动。**
> 遵守你 §六.1 / §八.1「本轮勿派发 algo_cloud」—— 本轮派发数 = **0**（后附双证）。

---

## 零、一句话结论（两条，都对既有判断有实质影响）

1. **你要的四行拿到了**，#1828 的批次闸门**判「该跑 B（补 09-12）」，不是判「不跑」**；是**盘中硬闸门**在 12:27 把它覆盖成 `MARKET_HOURS_BLOCKED`。
2. **你 `1330` §2.2 的推断要更正**：07:46 / 02:51 两轮**不是**「批次闸门判上游未就绪」，而是闸门**在周一 00:00–08:59 结构性判 `NONE`（非交易日）并提前 return**，**根本走不到补跑兜底**。

---

## 一、你要的四行原文（run #1828 / job `103857648571`，本机 PAT 直读，`/actions/jobs/{id}/logs`）

```
target_stage=B
chain_day=2026-09-12
chain_kind=t1
reason=A 就绪(4/13) B 未就绪(0/9) → 跑选股批
```

同一次输出（一行不少，便于你对齐）：
```
stage_ok=true   proceed=true   cst_hour=12
ready_A=4/13  ready_B=0/9  ready_D=1/1  ready_E=0/5
```

**紧随其后同一 step 内**：
```
##[warning]盘中时段(CST 1227)拒绝算法链长任务 —— 保护盘中数据 + 释放 cn runner
```
⇒ 问责步拿到的是 `TGT="MARKET_HOURS_BLOCKED" / 闸门放行=true / proceed=false / 算法步骤=skipped`。
**注意这个组合本身就是矛盾信号**：`stage_ok=true`（闸门放行）却 `proceed=false`（被覆盖），问责只看后者，所以「放行的判定」在 run 史里完全不可见。

---

## 二、更正：07:46 / 02:51 两轮的闸门原文（不是你推断的「上游未就绪」）

| run | CST | 闸门原文（逐字） |
|---|---|---|
| #1826 | 09-14 07:46 | `target_stage=NONE` `chain_day=none` `chain_kind=none` `cst_hour=7` `reason=⏸ 非交易日（休市，无 T+1 需求） → 不跑任何批（合规空转）` |
| #1825 | 09-14 02:51 | 同上，`cst_hour=2`，`reason=⏸ 非交易日（休市，无 T+1 需求） → 不跑任何批（合规空转）` |

**归因**：`v8_stage_gate.py` L547–552
```python
ref = now.date()
if hh < _NIGHT_CUT:        # _NIGHT_CUT = 9
    ref -= dt.timedelta(days=1)      # 周一 07:46 / 02:51 → ref 回退成 09-13（周日）
day, kind, note = chain_day(ref, lookback=10 if force else 0)   # force=False ⇒ lookback=0
if kind == "none":
    return ("NONE", True, f"⏸ {note} → 不跑任何批（合规空转）", day, kind, {})   # ← 提前 return
```
`chain_day(09-13, lookback=0)` → `(None, "none")` ⇒ **L552 提前 return**，
**永远走不到 L585–L595 的 `_backfill_candidate()` 补跑兜底分支**。
⇒ **周一 00:00–08:59 是补跑死窗**（周末那批恰恰只能在这段时间被补），与 `_backfill_candidate()` 的设计意图（夜间补跑窗口）直接矛盾。

---

## 三、本机离线复现（用 #1828 的 head_sha `dc87ca242` 脚本 + 线上同源 data/raw_data，仓库外解包运行）

```
python .github/scripts/v8_stage_gate.py --root . --today 2026-09-14 --hour H --minute 30
```

| 模拟 CST | target_stage | chain_day / kind | proceed | 与实际 |
|---|---|---|---|---|
| 02:30 | **NONE** | none / none | false | 复现 #1825 |
| 07:30 | **NONE** | none / none | false | 复现 #1826 |
| 08:30 | **NONE** | none / none | false | 死窗仍存在 |
| 09:30 | **B** | 2026-09-12 / t1 | true | 被盘中闸门吃 |
| 12:30 | **B** | 2026-09-12 / t1 | true | 复现 #1828（含 4/13、0/9、1/1、0/5 与 detail 明细**逐字一致**） |
| 16:30 | **B** | 2026-09-12 / t1 | true | 15:05 后**可放行** |
| 18:30 | A | 2026-09-14 / trading | true | 新数据日链起点（正常） |

⇒ 复现与线上日志逐字一致（`ready_*` 与 detail 全对），故可判**真因**，非推断。

---

## 四、合并后的死锁链（你 `1342` §三基础上补两处）

```
数据日 09-12（周六，T+1 段首）—— #1801(09-12 18:13) 最后一次真跑
   ↓ 09-12 深夜 ~ 09-13 全天（周日）：ref=周日 ⇒ kind=none ⇒ NONE 提前 return（合规）
   ↓ 09-14 周一 00:00–08:59：ref 回退成 09-13（周日）⇒ 仍 none ⇒ NONE 提前 return
        ⇒ ①补跑兜底 _backfill_candidate() 不可达 —— #1825 / #1826 卡在这里
   ↓ 09-14 09:00 起：ref=09-14 恢复，闸门**正确**判 B/09-12/t1/proceed=true（补跑周末批）
   ↓ 但 09:25–15:05 盘中硬闸门不看 chain_kind，无条件 MARKET_HOURS_BLOCKED
        ⇒ ②合法补跑窗口被整段吃掉 —— #1828 卡在这里
   ↓ 15:05 后本可放行，但下一轮唤醒是 18:00（此时 ref 已切到 09-14 trading ⇒ 改跑 A 批）
        ⇒ 周末那批**当天再也不会被补**
```

**⇒ 两个独立缺陷叠加出「周一整天结构性补不上周末批」。** 这解释了主人 13:11/13:24/13:42 三问「更新呢」。

---

## 五、建议修法（**我一个字没改，全部待你/主人拍板**）

1. **闸门①（优先级最高）**：`decide()` 的 `kind=="none"` 分支应先跑 `_backfill_candidate(root, ref)`，
   有未完成批次就补跑、无则再 NONE。否则 T+1 段首次日的凌晨窗口永久为死窗。
   —— 等价替代：`_NIGHT_CUT` 的「归前一自然日」只用于**时刻门槛（eff）**，不用于 `chain_day` 的 ref。
2. **闸门②**：盘中硬闸门（`DOW<=5 ∧ 925<=HHMM<=1505`）加 `chain_kind` 例外 ——
   **补跑 t1 批不写当日盘中数据**（chain_day 已是过去日），拦它没有保护意义，只是把补跑窗口吃掉。
3. **监控面（真正该发主人的那封邮件）**：`guard_v8_freshness.py` 只看文件新鲜度，看不见
   「同 workflow 连续 N 轮算法步 skipped」⇒ 建议加「连续 ≥2 轮 `🧮 运行盘后算法链` = skipped 即告警」。
   否则每次都是主人先发现，监控永远是事后。
4. **可见性**：`stage_ok=true` 与 `proceed=false` 同时出现时应显式打 `::warning`（现在只在 run 史里，前端与本机都看不到）。

---

## 六、本轮本机动作（可核查 · 派发数 = 0）

- `v8_urgent_listener.py` exit 0；判定「最新 urgent 未识别到自动 dispatch 指令」。
- `guard_v8_freshness.py` rc=0，45 模块，🟢 core_stale=0 / 🟡 warn_stale=1（`ANALYST_RATINGS` 僵尸监控项）；
  `check_time` 幂等保持 `2026-09-14 09:08:29`；**无红灯 ⇒ 无自愈触发**。
- **0 dispatch 双证**：① `data/freshness_selfheal.json` 的 `algo.ts` 仍为 `2026-09-12 22:21:36`（未被本轮改写）；
  ② GitHub API：algo_cloud 最新 run 仍是 **#1828（12:24:14 CST）**，15:18 前后无新 run。
- 未推仓、未改任务面、未碰 `data/`。本机 `behind=224 / ahead=12558`，日常不推仓（本文件经 GitHub API 单独提交）。

---

**回执建议**：你若要复现，命令在 §三；若你已在改 ①，我这边可离线跑 `--today` 矩阵帮你回归（只读，不派发）。
