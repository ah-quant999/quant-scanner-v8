# 交接单 · 2026-09-10 午休修复（小九 → 阿狸咪）

> 生成时间：2026-09-10 13:00 CST（午休窗口）｜ 生成方：小九机 WorkBuddy
> 目的：上午盘中数据混乱的一劳永逸修复 + 下午避开同类错误。

## 一、上午事故复盘（CST 09:00–12:00，Actions 实时运行史）

| 口径 | 成功次数 | 明细 |
|------|---------|------|
| ☁️ 云端 `v8_cn_fetch_cloud`（ubuntu） | **4** | 成功：09:58 / 10:02 / 11:31 / 11:48；cancelled：09:32 / 10:46 / 10:50 / 11:24 / 11:47（共 5 次） |
| 🖥️ 小九机 cn 兜底 `v8_cn_fetch_cloud_selfhosted` | **0** | 整个上午从未被派发 |
| 🐉 阿狸咪机兜底（hosted / watchdog / slot_scheduler） | **0** | 三个兜底通道全部 0 运行 |

**根因（一句话）**：盘中派发是小九 WorkBuddy 自动化 `22505803` 单点 dispatch 云端；看门狗
`v8_cn_fetch_watchdog.yml` 的 GitHub cron 在 09-09 被误删，仅依赖唤起器 `2d9121c8`（今早掉线）
→ 看门狗 0 运行 → 云端 5 次 cancelled **无人兜底** → 数据在 10:02 成功后空窗近 30 分钟（10:20 / 10:40 档全缺）。

## 二、已上线（无需阿狸咪操作，下午安全网已生效）

1. **看门狗 schedule 恢复**（commit `2b0fd9c58`）
   - `cron: '7,22,37,52 0-7 * * 1-5'`（每 15 分，交易日 08:07–15:52 CST，与 20 分主派发错峰）。
   - 逻辑：拉线上 data 多文件比对，盘中>35min 陈旧即自动补派云端 / 小九 cn 兜底；含「破锁取消卡死 run」+「云端拥堵改派 selfhosted」。
   - 已手动触发验证（dispatch 204 已接受）。首个自动运行约 13:07 CST。
2. **`?v` 缓存戳改单调部署令牌**（上午已上线）
   - 根治「数据回滚 / 更新后浏览器陈旧缓存」——最终推荐回退 2 天前、盘中主线卡旧时刻均已修复。

## 三、待阿狸咪 15:00 后推上线（已在分支 `fix/lemoncat-sleep600`）

- 文件：`.github/workflows/v8_cn_fetch_intraday_lemoncat.yml`
- 改动：`sleep 600`（盲等 10 分钟）→ **实时轮询**云端 run 状态（每 30s，最多 12 分钟），成功即结束；超时/失败转兜底步。
- 分支 commit：`34c24aa1d7`（YAML 已校验合法）。
- **⚠️ 双源冲突警示（必读）**：lemoncat 是「小九 GHA cron 14 档调度器」。若直接推 main，会与现有
  `22505803`（小九 WorkBuddy 每 20 分 dispatch）形成**双源互踢**（09-09 曾因此出风暴，主人令才收口单源）。
  - 推 main 前须先 **停用 22505803**（小九机 WorkBuddy 自动化），否则双派发会重复触发云端 run、互相抢占并发组。
  - 或 **暂不推 lemoncat**，维持「22505803 主派 + 看门狗兜底」现状（看门狗已覆盖上午的全部缺口风险）。
  - 请按主人最新口径二选一，勿擅自双开。
- 拉取/推送（若决定推）：
  ```bash
  git fetch origin fix/lemoncat-sleep600
  git checkout fix/lemoncat-sleep600 -- .github/workflows/v8_cn_fetch_intraday_lemoncat.yml
  # 然后按仓库流程 commit + push main（先确认 22505803 已停用）
  ```

## 四、阿狸咪 15:00 后动作清单

1. 确认看门狗已上线：`gh api repos/ah-quant999/quant-scanner-v8/actions/workflows/347126523/runs`
   应每 15 分出现成功运行（category 自适应）。
2. `git fetch origin fix/lemoncat-sleep600` 审阅 lemoncat 改动（第 112 行起 `run` 块）。
3. 按主人口径决定：推 lemoncat（先停 22505803）或保持现状。
4. 盘后跑批（17:20 / 18:20 / 19:20 cron）正常进行，无需额外操作；看门狗盘后窗口也会兜底。

## 五、已知残留（不阻塞下午，可后续专项）

- 云端 run 被 `cancelled` 的根因未深挖（concurrency 已 `cancel-in-progress: false`，但仍 5 次 cancelled），
  疑似 22505803 重复派发或队列抢占。看门狗兜底已覆盖此风险，建议后续专项排查，勿为修此再动盘中主链路。
- 本机 `git reset --hard origin/main` 自动化会冲掉小九本地未提交改动；lemoncat 改动已存于远端分支 `fix/lemoncat-sleep600`，不受影响。

## 六、追加观察（13:00 开盘复核，小九）

- 看门狗已于 **12:56 CST 成功运行**（safety net 确认存活），首个自动档 ~13:07。
- 云端 12:57 CST 有一 `schedule` run `in_progress`（盘中 cron 仍在，见下），开盘数据正在刷新。
- 线上数据：INDEX_QUOTES 12:34 / SECTOR_FUND_FLOW 12:33 / CANDIDATE 12:37（26–30min 前，仍在 35min 阈值内，
  看门狗 12:56 判定「无需补派」正确）。
- **⚠️ 残留发现（盘中实为双源）**：`v8_cn_fetch_cloud.yml` 实际仍含盘中 cron
 （`*/30 1-3` 与 `*/30 5-7` = CST 09:00–11:30 / 13:00–15:30，每 30 分），与 `22505803`（每 20 分 dispatch）并存。
  上午 5 次 cancelled + 午间 12:32–12:37 连串 cancelled，疑为双源/突发派发互相抢占并发组
  （cancel-in-progress 已 false 但仍 cancelled，根因待查）。看门狗兜底已覆盖缺口风险，但建议后续
  **收口为单源**（停用 22505803 或删 cloud yml 盘中 cron），与第三节 lemoncat 双源警示一并处理。
- 结论（13:00 初判·已被 13:09 复核推翻）：原以为下午链路正常，实则 13:00 开盘后数据停滞至 12:34（~50min），根因见第六节。

## 六、13:09 主人复核·根因纠正与已修复（2026-09-10）

### 6.1 两点指控的调查结论（Actions 运行史证伪/证实）
- ❌「看门狗 9:30 占锁挡掉 9:40」：**证伪**。运行史证明看门狗上午 9:30 附近 0 次运行（仅 09:12 一次 schedule + 12:56 手动）。
  9:32 那次 cancelled cloud run 是 `22505803`（workflow_dispatch）派发，非看门狗。此前写进看门狗注释的「09-32 watchdog 占锁」是**错误归因**，已删除重写。
- ✅「13:00 开盘阈值 35min 不更新」：**证实**。看门狗原 `thr=35`（intraday），午休数据 12:34 → 13:00 后 age<35 → 不兜底；叠加「误杀主派发」→ 开盘后停更近 50min。

### 6.2 真正根因
**盘前 08:25 CST cron 被 GitHub 延迟到 12:57 CST 触发，偶发撞 22505803 盘中 dispatch（同 `v8-cn-fetch-cloud` 组互踢）+ 看门狗 `cancel_stuck_cloud_runs()` 误杀跑了>12min 的正常主派发 run**（云端一批本就 15-25min）→ 主派发档被反复打断 → 数据空窗。看门狗「派发」动作本身在添乱，非占锁。

### 6.3 已修复（推 main，commit f9e745ac）
- 看门狗不再调用 `cancel_stuck_cloud_runs()`（主派发档绝不被打断）。
- 阈值：盘中 12min、开盘窗口(09:25-09:40/12:55-13:10) 5min、盘前盘后 60min。
- 派发兜底阈值 `dispatch_thr`：盘中 45min / 盘后 90min，仅主派发彻底失联才派 selfhosted。
- 修正错误注释（删「09-32 watchdog 占锁」误导）。

### 6.4 13:00 数据兜底（进行中）
- 手动派 selfhosted intraday（13:21 in_progress，小九机跑）；曾因 selfhosted 与 cloud 同属 `v8-cn-fetch-cloud` 并发组、被卡队列的 13:12 cloud run 阻塞（mutually blocked）→ 已手动 cancel 13:12 cloud run 释放组，selfhosted 13:21 接单 in_progress。数据预计 ~13:40 刷新。

### 6.5 待阿狸咪 15:00 后推（分支 fix/lemoncat-sleep600，未上线）
- lemoncat `v8_cn_fetch_intraday_lemoncat.yml` 的 `sleep 600` → 实时轮询（分支 34c24aa1d7，YAML 合法）。推 main 前先停用 22505803（避免双源互踢，09-09 风暴教训）。
- 注：cloud.yml 当前已无盘中 cron，双源风险已大幅收敛；lemoncat 属进一步优化，非 P0。
