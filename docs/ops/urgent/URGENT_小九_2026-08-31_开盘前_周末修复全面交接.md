# 🚨 URGENT — 小九开盘前必读（2026-08-31 周一）

> 发出方：阿狸咪（家中机）　发出时间：**2026-08-30 23:40 CST**
> 触发条件：主人令「实在阿狸咪跑不了，就写紧急交接……让她马上跑起来，不能等开盘后才发现各种错误，必须开盘前全部一劳永逸式修复到位」
> 一句话：**周末所有修复已全部推入 main；小九只需做「第 3 节开盘前 6 步」，重点是确认 runner 在线 + 看算法链闸门是否转绿。**

---

## 1. 今晚（8/30 18:00–23:40）已推送的修复，全部在 main 上

| # | commit | 修复内容 | 怎么验证它生效 |
|---|---|---|---|
| 1 | `439b77127` | AVG_PRICE 口径隔离防覆盖：880003 真历史不再被全A等权 1 条抹掉 | 查 `raw_data/avg_price_data.json` 的 `history` 不再被重置成 1 条 |
| 2 | `f68b65df8` | cn 抓取链全量兜底：手动 dispatch / 未匹配 cron 时 `category=all` 不再跳过 post_close 五步（LHB/回填/弹性/板块RS/ETF） | dispatch `category=all`，step 6-10 应全部执行而非 skipped |
| 3 | `8e5cfdbc4` | HANDOVER 交接文档（批次 G） | — |
| 4 | `2925886ef` | **闸门判据方向修复（重推）** + selfhosted job 超时 30→75、推送步 40 | `algorithms/verify_chain_outputs.py` 含「本次产出」、不含 `elif d == today` |
| 5 | `c6b79c218` | **「推送步拖死整个 job」同类 bug 一次清干净**（5 个 workflow / 10 处） | 见第 2 节 |
| 6 | `00c045d4f` | **邮件告警统一闸门**：溯源标识 + 分级 + 非交易日静默 + patrol 输出 bug | 邮件主题带 `[v8·<机器名>]` 前缀 |
| 7 | 本提交 | 算法链 step 8 限时 90→150 分钟 | `v8_algo_cloud.yml` step「🧮 运行盘后算法链」timeout=150 |

### 关键根因（一句话记住）

**`api_push_raw.py` 的「防倒退守卫」要为每个变更的 .json 全量下载远端 blob 比时间戳**
（raw_data 共 **117 个 json / 26 MB**，stock_quote 4.6M、backtest_tdx 3.9M），
cn runner 出海串行传输就要 **13–22 分钟**。守卫逻辑正确、不能删（删了重演 08-09 数据回退），
但原来 job 只给 20–30 分钟 → **推送必被 GitHub 硬杀**，连带其后的
bridge / update_v8 / health_check 全部 skipped → **整轮白抓**。

三条实测现场（GitHub API jobs 接口逐步耗时，均 `conclusion=cancelled`）：

| run | workflow | job 耗时 | 杀点 |
|---|---|---|---|
| `33312102792` | cn抓取(云端·小九应急) | 30.2min == timeout 30 | step11 推送 **21.7min** |
| `33309687912` | cn抓取(cn·应急) | 20.3min == timeout 20 | step11 推送 **13.8min** |
| `33309690213` | 盘后算法链(cn) | 84.0min | step4 算法链 **82.5min** |

### 统一修法（两层，7 个调 `api_push_raw.py` 的 workflow 全部对齐）

1. job 级 timeout 给足墙钟；
2. 推送步加 `continue-on-error: true` + **步级 `timeout-minutes`** —— 推送超时只判该步失败、
   job 存活，后续消费步照常跑完，raw_data 交下一轮补推。
   （这是 08-18「消费层软失败」根治令的完整落地：此前只加 coe、没加步级限时，
   job 级硬杀照样把后续步带走，软失败形同虚设。）

| 文件 | 变更 |
|---|---|
| `v8_cn_fetch.yml` | job 20→60；推送步 +coe +to=35 |
| `v8_lhb_fetch.yml` | job 25→50；推送步 +to=30 |
| `v8_cn_fetch_cloud_hosted.yml` | 推送步 +coe +to=15 |
| `v8_cn_fetch_cloud.yml` | 推送步 +to=15 / data推送 +to=12 / 盘中快照 +to=10；intraday-snapshot job 10→15 |
| `v8_algo_run.yml` | 兜底推送步 +to=40 |
| `v8_cn_fetch_cloud_selfhosted.yml` | job 30→75；推送步 +coe +to=40 |
| `v8_algo_cloud.yml` | job 200（未动）；**算法步 90→150**（本提交，因周末全量补算实测 70min 未完） |
| `api_push_raw.py` | **未动**（守卫逻辑不动，数据安全优先） |

---

## 2. 邮件告警「还在报警」—— 排查结论与根治

**排查证据链（逐条排除，不臆断）：**

| 假设 | 结论 | 证据 |
|---|---|---|
| runner 离线告警 | ❌ | runner-offline Issue #1/#2/#3 **全部 closed**；lemoncat-cn 现 **online** |
| 健康巡检在报警 | ❌ | 实测 22:15 巡检日志：`ok=109 / warn=0 / fail=0`；新鲜度 49 模块「✅ 全部模块新鲜」；正确识别 `交易日: 否(周末/节假日)` |
| 看门狗因今晚 3 个 cancelled 发信 | ❌ | `cancelled` 自 08-09 起就在 NEUTRAL 列表，不参与成败判定 |
| URGENT_*.md 被周清理 commit 回仓触发通知 | ❌ | `.gitignore` 已忽略（仅 3 个白名单）；最近 30 提交无 URGENT 入库 |
| 阿狸咪这台发的 | ❌ | 本机无 `.workbuddy/v8_smtp_config.json`，发不出 |

**⇒ 邮件来自小九单位机的本机告警脚本**（看门狗 / 健康检查）。阿狸咪碰不到那台机器，
所以根治方式不是去改某一条告警，而是**在唯一出口统一管住**：

> 全站告警邮件只有 `v8_send_alert.py` 一个出口（watchdog / health_check / bloat_check
> 都是 `from v8_send_alert import send_alert`）。原实现是无脑透传，本次升级为**统一闸门**：

1. **告警溯源**：主题统一加 `[v8·<机器名>]` 前缀，正文末尾加发送主机/级别/时间。
   机器名取环境变量 `V8_ALERT_HOST`，其次配置 `host_label`，最后 hostname。
   👉 **建议小九在单位机上设 `V8_ALERT_HOST=小九单位机`**，今后来信一眼可辨来源。
2. **分级**：
   - `infra` 基础设施真故障（runner 离线 / 站点 5xx / workflow 显式 failure / 进程崩溃）
     —— **任何时间都发**，周末夜间同样告警，绝不静默；
   - `stale` 数据陈旧类 —— **默认仅交易日发**，周末与 2026 法定节假日静默；
   - `info` 知会类（仓库膨胀等卫生指标）—— 默认不发。
   - 调用方不传 level 时按 `infra` 处理，保持历史行为，不漏报真故障。
3. **开关**：`V8_ALERT_DISABLE=1` 全局停发；`V8_ALERT_STALE_WEEKEND=1`、`V8_ALERT_INFO=1`
   可按需打开。**无论发不发都写 `.workbuddy/v8_alert_trace.log` 留痕**，事后可审计。

分级接线：health_check 数据陈旧邮件→`stale`；health_check 进程崩溃→`infra`；
watchdog 含管线异常→`infra`、仅数据陈旧→`stale`；bloat_check→`info`。

**附带修掉一个真 bug**（实测 run `33316381407` 日志坐实）：
`v8_health_patrol.yml` 里 `VAR=$(grep -c "PAT" file || echo "0")` 在无匹配时把 grep 退出码 1
带进命令替换，变量混入换行 →
```
💊 自动治愈 0
0 项 / 失败 0
##[error] Unable to process file command 'output' successfully.
##[error] Invalid format '0'
```
`healed/failed/fail` 三个 outputs 全部失效、汇总显示「自愈失败: （空）项」。
已改 `|| true` + `${VAR:-0}` 兜底；验证回查步 `REMAIN_FAIL/NEW_OK` 同病同修。

---

## 3. 🚨 小九开盘前 6 步（周一 07:45 前做完）

| 步骤 | 动作 | 期望结果 |
|---|---|---|
| ① | `git pull` main，确认 HEAD 含 `00c045d4f` 及以上 | 见第 1 节 7 个 commit |
| ② | **确认 lemoncat-cn runner 在线**（`alimi-cn` 目前 offline，家中机 runner 没开；但它只承接 4 个 self-hosted job，且都有云端备份，不影响开盘） | GitHub → Settings → Actions → Runners 显示 online |
| ③ | 看算法链 run `33316835316`（阿狸咪 22:25 触发，`force_run=true`）的最终结论，**重点看 step 13「🛡 产物完整性闸门」是否转绿** | 若 step 1-12 全 success 且 step 13 通过 = **8/29 起连续四次 dispatch failure 的元凶已除** |
| ④ | 若 ③ 仍红，直接看闸门输出哪一项「陈旧」，针对性 dispatch 对应类别 | 闸门修复后不会再出现「❌ 陈旧 -2 天」这种负数伪陈旧 |
| ⑤ | 盘前 08:25 / 09:00 两档 cn_fetch 起来后，确认 step 6-10 不再 skipped（`category` 判定已修） | LHB / 回填 / 弹性 / 板块RS / ETF 三件套真正执行 |
| ⑥ | 确认邮件主题带 `[v8·小九单位机]` 前缀（闸门已生效）；如仍怀疑误报，查 `.workbuddy/v8_alert_trace.log` 有留痕 | 溯源 + 可审计 |

---

## 4. 待验证 / 待跟进（不影响开盘，但别忘）

1. **算法链 `33316835316` 最终结果**——阿狸咪发出时该 run 已跑 70+ 分钟仍在 step 8。
   已把步级限时抬到 150（本提交），**若它仍超时，请在单位机重跑一次**。
2. **`category=all` 全量档实测**：`f68b65df8` 修好后还没真正跑过 all 档
   （修前的 run `33312102792` 五步全 skipped）。建议小九盘后手动 dispatch 一次验证。
3. **AVG_PRICE 口径二选一**（需主人拍板，非小九可定）：
   880003 真指数（有 120 天历史，但当前 **7 源全挂**：东财 RemoteDisconnected /
   akshare KeyError 'date' / 雪球 400）vs 全A等权自算（稳定可跑，历史需 20 个交易日累积）。
   已做数据层止血 + 防覆盖，但**两写手同文件**的架构问题必须决策：
   **保留哪一条 / 是否拆成两个文件两张卡**。
4. `v8_lhb_fetch.yml` 的两次 push 触发 failure（`33309484290` / `33309126687`）是
   **0 job 无日志** = GitHub 对「workflow 文件解析失败」的固定表现，属**旧版本残留**
   （19:43 的 dispatch 已 success，当前版本正常）。
   👉 **教训：改 workflow 必须先 YAML 校验再推**，阿狸咪已用 `_check_wf_all.py` 固化这一步。

---

## 5. 防覆盖约定（重要）

- 主人今晚多次推送（六卡补位 `036e6f9b9`、卡片归位 `eb8837de3`、逻辑详解页 `efa4446d7` 等），
  **曾发生一次整树覆盖**：阿狸咪的闸门修复 `ab59aff12` 在 9 秒后被 `5f8cc7222` 覆盖回旧版。
- 👉 **新铁律：推送后必须立刻回读远端校验文件 blob**；被覆盖就基于最新 main 重推
  （反复 rebase 救不了整树覆盖）。
- 阿狸咪本轮所有推送都走 **plumbing 脏树推送 + 条目数守卫**（`total>200 && data/>90`），
  且每次推送前 `git fetch origin main` 对齐最新 HEAD，避免覆盖主人改动。

---

## 6. 数据现状实测（截至 8/30 23:2x，远端 main 实测）

| 数据文件 | update_time | 内容 | 判定 |
|---|---|---|---|
| `raw_data/lhb_data.json` | 2026-08-29 08:43:06 | `date=20260828`，`stocks` **51 条** | ✅ 龙虎榜正常 |
| `raw_data/lhb_history.json` | 2026-08-29 04:58:40 | **58 个交易日**，最新 `2026-08-28`（最近 6 天连续） | ✅ 回填正常 |
| `raw_data/candidate.json` | 2026-08-29 22:06:09 | stocks 372 | ✅ |
| `raw_data/stock_quote.json` | 2026-08-30 10:31:12 | stocks 9941 | ✅ |
| `raw_data/avg_price_data.json` | 2026-08-30 20:01:58 | `history` **仅 1 条**（周日的脏记录） | ⚠️ 口径待主人拍板，见 4.3 |

> ⚠️ 更正一条此前的误判：曾报「lhb_data.json 条目 0」与「龙虎榜陈旧」，
> 实为探测字段取错（真实容器叫 `stocks` 不是 `data`/`list`）。**龙虎榜链路完全健康。**

---

*本文件由阿狸咪自动生成并入库，小九 pull 即可见。如已处理请在交接文档回写一行确认。*
