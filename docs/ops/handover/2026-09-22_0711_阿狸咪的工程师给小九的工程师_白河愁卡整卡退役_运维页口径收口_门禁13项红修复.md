# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-22 07:11｜白河愁卡整卡退役_运维页口径收口_门禁13项红修复

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

主人 09-22 早令（截图三问）：**「🌐 新周期判定 · 海外宏观 + AI 大周期」卡整卡退役**（全链：前端/数据/脚本/自动化）
＋ **修同页「解禁&机构关注」卡永久「加载中」**（33499302c 时序缺陷：函数声明未挂 window）
＋ **收口运维页两处仍登记该卡的活代码**（四方口径不一致）。
三个 commit 已上线：`629e3fb74c`（退役主体）→ `b5b87e72b9`（修门禁 [13/13] 红）→ `74381d5566`（运维页口径收口）。
**离线门禁 14/14 全绿 · EXIT 0**。

---

## 一、改动清单（3 个 commit，全部 Git Data API 隔离推送 `force:false` + 回读逐字节一致）

### commit `629e3fb74c73b0bf8d232b03b1fccfb60e4aab70`（退役主体，7 改 + 6 删）

**index.html（-8 行净）**
- 删 `<script src="data/BAIHECHOU_MACRO.js">` `<script src="data/BAIHECHOU_ANALYSIS.js">` 两行 → 墓碑注释
- 删卡壳（`id="baihechouBody"` 恢复注释块 + `h += '...'` 两行）→ 墓碑注释
- 删派发行 `try{ ... window.__renderNewCycleJudge(); }catch(e){}`
- 删渲染函数块（注释头 + `__ncRetry` + `__renderNewCycleJudge` + `__renderBaihechou` 兼容别名）→ 墓碑注释
- **补** `window.__renderUnlockCover = __renderUnlockCover;`（修 33499302c 时序缺陷）

**删除 6 个资产**（引用面实测**自闭合**：6 资产仅互相引用，无 workflow / 云端 / 其他脚本引用）

| 资产 | 说明 |
|---|---|
| `data/BAIHECHOU_MACRO.js` | 当期原文 + 解析（前端注入） |
| `data/BAIHECHOU_ANALYSIS.js` | AI 解析独立文件（09-18 拆分产物） |
| `raw_data/baihechou_posts.json` | 累积帖池（去重/连续性判定用） |
| `scripts/baihechou_daily.py` | 日频主控（verify/prep/finish） |
| `scripts/fetch_baihechou_v8.py` | 原文抓取（headless 过 WAF） |
| `scripts/set_baihechou_analysis.py` | 解析注入 |

**v8_health_check.py**
- 删 CARD_DEFS 巡检登记项 `{"id": "BAIHECHOU_ANALYSIS", ...}`（原 09-18 补监控盲区 / 09-21 补 producer 的整段）
- `_LOW_FREQ_FILES` 加 `"BAIHECHOU_MACRO", "BAIHECHOU_ANALYSIS"`（防旧产物被放回触发 `all_` 通用 24h 红线假红）
- `_RETIRED_FILES` 加同两项（巡检文案说「已退役」而非「低频」）

**api_push_raw.py**
- `_RETIRED_ARTIFACTS` 加 `data/BAIHECHOU_MACRO.js` / `data/BAIHECHOU_ANALYSIS.js` / `raw_data/baihechou_posts.json`（拒绝本地残留副本回推复活）

**docs/ops/index_protected_markers.txt**
- `window.__renderBaihechou|白河愁宏观渲染器|2026-09-20` → `window.__renderUnlockCover|因子观测卡渲染器|2026-09-22`（退役即撤登记、新卡即登记）

**logic.html**（2 处现态注记）
- `sec-lg` 危机雷达说明段尾 + 「📊 观测类（唯一在册子页）」清单段尾，各补 `2026-09-22 主人令：宿主卡已整卡下线；CRISIS_DATA 数据源保留`

**docs/ops/HANDOFF.yaml + HANDOFF.ledger.json**
- 追加 `baihechou-card-removed-and-unlockcover-fix`（第 39 项，status=done）；ledger ids 38→39 + 全量重算内容哈希

### commit `b5b87e72b93cff9a45dff3e361765c613a8586ea`（修门禁真红，单文件）

**docs/ops/HANDOFF.yaml** `meta.next_id_seq: 38 → 39`

> ⚠️ **这是我方漏项，门禁抓到并纠正**：追加第 39 项 + 抬高 ledger `max_next_id_seq` 时，
> **漏了同步抬高 HANDOFF.yaml 自身的 `meta.next_id_seq`** ⇒ 离线门禁 `[13/13] 交接状态源守卫`
> 报「meta.next_id_seq 回退：当前 38 < ledger 记录 39（状态源被回滚）」⇒ **会阻断 deploy**。
> **请对端注意**：追加 item 时必须**同时**抬高三处 —— item 追加 / `meta.next_id_seq` / `ledger.max_next_id_seq`（+ 哈希）。

### commit `74381d556646e25fa289371cb95764cb0c1017e0`（运维页口径收口，单文件）

**index.html 两处仍登记已退役卡的活代码**（本批遗漏，属「四方口径不一致」）：
- `V8_PAGE_SCHEDULE`（运维页「🔒 暂未上架」卡片级调度表）：删 `{name:'新周期判定·AI解析', source:'BAIHECHOU_ANALYSIS', freq:'工作日盘前', slots:['0','6',':','3','0'], note:'AI 解析'}`
- `_PAGE_CARD_ORDER`：`'暂未上架': ['MACRO_KSHAPE','BAIHECHOU_ANALYSIS','UNLISTED_PANEL']` → `['MACRO_KSHAPE','UNLISTED_PANEL']`

> 已实测 `algorithms/gen_backtest_all_algos.py:88` 仅**注释**引用 `V8_PAGE_SCHEDULE`（原文：「源登记表：card = 前端卡名（权威出处 index.html 的 V8_PAGE_SCHEDULE）」）
> ⇒ **index.html 是权威源、非生成产物**，直改安全；无卡片数守卫（`pre_deploy_audit.py` 无 `V8_PAGE_SCHEDULE` 引用）。

---

## 二、三件套原始输出

### ① 审计 / 离线门禁全绿（`HEAD=b5b87e72b93cff9a45dff3e361765c613a8586ea`）

```
[mirror] 导出 408 文件, 缺失 0（全树 6489；allowlist 路线，仓库外 staging）
[自证] 关键文件 vs origin/main blob：8/8 ✅（含 index.html 1339558 B）
  ✅ [1/8] py_compile: 193 个 .py 文件 0 错误
  ✅ [2/8] new Function: 28 个 inline script 块 0 错误
  ✅ [3/8] data 完整性: data/*.js 数量=111（上界 130（动态＝面 108 ＋ 容差 20））, 全部 > 100B
  ✅ [4/8] align_logic_ops: align_logic_ops EXIT 0（逻辑详解页与真 workflow 对齐）
  ✅ [5/8] workflow YAML: 30 个 workflow 全部有效（PyYAML 全量）
  ✅ [6/8] HTML 数据引用: 178 个本地 script 引用全部存在（4 页）
  ✅ [7/8] gate 头注一致: 闸门头注与 READY_SPEC 真源一致（项数/门槛/P0 专项）
  ✅ [8/8] 心跳产物名一致: 心跳产物名大小写一致（权威 HB_ALIMI/HB_XIAOJIU，全仓 0 处漂移）
  ✅ [9/9] 回测口径守卫: ⚠️ 产物缺失（backtest_expectancy.json）→ 已放行，等 E 批产出
  ✅ [10/10] index 核心标记守卫: index.html 核心标记 34/34 全部在位（卡片/分节/数据源锚点）
  ✅ [11/11] defer 数据缓存守卫: defer 数据 98 个 × 结果缓存 1 个：0 处空结果固化风险
  ✅ [12/12] 调用点定义守卫: 内联 script 候选调用点 161 个：全部有定义（0 处悬空）
  ✅ [13/13] 交接状态源守卫: 交接状态源完好：items=39（ledger 锚 39 个 id 全在）· next_id_seq=39≥39
  ✅ [14/14] AI 入口在位守卫: AI 入口在位：AGENTS.md(8051 B) + .codebuddy/CODEBUDDY.md + README 指针齐全 · 必读 4 条 · 死链 0
🎉 14 项全部通过 → deploy 可继续        EXIT=0
align_logic_ops        EXIT 0（index.html#sec-op 614562 + logic.html#sec-lg 530913 字符；MUST=21 / ALLOW=3）
v8_verify_layer_parity EXIT 0（检查 0 对，缺失 raw 77 个，真错位 0 个 —— ⚠️ 见下方「三·边界」）
```

**门禁抓到并修掉的两处红**（修前原始输出，请对端知悉）：
```
❌ [13/13] 交接状态源守卫: meta.next_id_seq 回退：当前 38 < ledger 记录 39（状态源被回滚）
❌ [14/14] AI 入口在位守卫: AGENTS.md 缺失  ← 我方 staging allowlist 漏项（假红，非线上问题）
```

### ② 四方对齐

**云端 / 双机（cron 时窗矩阵，本地对象库取数，`tip=74381d5566`）**
```
workflow 文件总数 = 30
含 cron 的 workflow = 21        （与 pre_deploy_audit MUST 名单同数）
cron 条目总数 = 54
✅ 关键条目：cloud_weekly_cleanup -> 0 13 * * 6（周六 21:00 CST）
✅ v8_algo_cloud 8 档：40 8 / 10 10 / 0 12 / 0 13 / 30 14 / 30 16 / 0 22 / 0 0
✅ runner_health_alert 3 档（工作日 / 周五六 / 周六日）
```
**本轮改动清单不含 `.github/workflows/**` ⇒ 矩阵无需调整**（三 commit 触及路径逐条核对：index.html / logic.html / docs/ops/** / v8_health_check.py / api_push_raw.py + 删除 6 资产）。

**家机（阿狸咪）ACTIVE 任务时窗** —— 按「PAUSED 不删」铁律停用两项（**未删**）：
- `98101962-…`（白河愁 06:30 主跑）→ **PAUSED**（`automation_update` 回读）
- `622ad154-…`（08:30 幂等兜底）→ **PAUSED**（同上）
- 二者不派发算法链、不写仓库其他数据 ⇒ 无错峰冲突；停用后**不再产生**该卡任何写入。

**运维页 / 逻辑详解页** = 本轮 commit `74381d5566` 的收口目标（见上「改动清单」第三段）。
现态：`index.html` 全仓 `BAIHECHOU` 仅剩**墓碑注释**；`logic.html` 历史沿革叙事保留 + 两处现态注记。

### ③ 交接件 = 本文件（`docs/ops/scripts/new_handover.py` 生成，LF）

---

## 三、边界与未做项（诚实披露，请勿误判为已完成）

1. **`data/HEALTH_CHECK.js` 仍含白河愁条目**（`BAIHECHOU_ANALYSIS` / `all_BAIHECHOU_MACRO` / `BAIHECHOU_MACRO`，共 4 处）。
   它是 `v8_health_check.py` 的**产物**，本批已改代码（删登记项 + 加护栏）⇒ **云端下次体检自动重生成即消失**。
   ⚠️ **我方未在本机重生成后推送** —— 避免用非云端环境覆盖权威产物。判据：下次体检后 `data/HEALTH_CHECK.js` 应 0 处 `BAIHECHOU`。
2. **`v8_verify_layer_parity` 的「缺失 raw 77 个」是 staging 设计产物**（allowlist 不含 `raw_data/`）
   ⇒ **真实的 data↔raw 配对未独立验证**，禁当成「跨层一致性已验证」的证据。
3. **门禁 `[9/9]` 产物缺失 → 已放行**（`backtest_expectancy.json`），等 E 批产出；非本批问题。
4. **首次补丁打在落后 176 commit 的旧基线上**，推送前被自有漂移检测拦下（检出 `f3af92216` 重建的 V8_PAGE_SCHEDULE 59 胶囊会被整体回退）
   ⇒ 已按防覆盖铁律 `git checkout origin/main` 重取权威版重做补丁。**基线 = `97d8d3043`（退役批）/ `bfeec31b1a`（运维页批）**。
5. `logic.html` 历史叙事段落（`1212` / `2048` / `2116` / `4256` / `7604` 行等）**有意保留** ——
   命中在注释/历史说明内，判为「有意留档」而非漏项（判据见下 §四）。

---

## 四、可跑的复核命令（给判据，不给本机私有路径）

| 判据 | 期望 |
|---|---|
| `git cat-file -e <tip>:data/BAIHECHOU_MACRO.js` | **非 0**（已删） |
| `git cat-file -e <tip>:scripts/baihechou_daily.py` | **非 0**（已删） |
| `git cat-file blob <tip>:index.html \| grep -c "window.__renderUnlockCover = __renderUnlockCover;"` | **1** |
| `git cat-file blob <tip>:index.html \| grep -c "BAIHECHOU"` | 仅墓碑注释（`<!--`…`-->` 块内 4 行 + 历史注释 1 行） |
| `git cat-file blob <tip>:index.html \| grep -c "source:'BAIHECHOU_ANALYSIS'"` | **0** |
| `git cat-file blob <tip>:docs/ops/index_protected_markers.txt \| grep -c "__renderBaihechou"` | **0** |
| `git cat-file blob <tip>:docs/ops/HANDOFF.yaml \| grep -c "next_id_seq: 39"` | **1** |
| `pre_deploy_audit.py` | **14/14 · EXIT 0** |
| `align_logic_ops.py` / `v8_verify_layer_parity.py` | **EXIT 0** |
| `pages` 真站硬刷新 | 「🌐 新周期判定」卡消失；「🔒 暂未上架 → ★ 解禁&机构关注」渲染出数据（不再「加载中…」） |

**判「是否漏项」的正确口径**（本轮实测，供对端复用）：
先剔除注释再查活代码 —— `re.sub(r'<!--.*?-->', '', html, flags=re.S)` + 剔除 `^\s*//.*$` 行；剩余命中才算漏项。
（本轮教训：墓碑注释是**多层续行**，逐行 `startswith('//')` 的判断会把它误报成残留。）

---

## 五、待对端应用项（`no action required` 说明）

- **无 workflow 改动** ⇒ 不需要出补丁件到 `docs/ops/patches/`。
- 若对端本机也有白河愁自动化任务（06:30 / 08:30）：按「PAUSED 不删」处置，**禁删**。
- 若恢复该卡：git 历史可完整回溯（`git log --all -- data/BAIHECHOU_MACRO.js`）。

---

—— 阿狸咪的工程师（alimi-cn）｜2026-09-22 07:11
