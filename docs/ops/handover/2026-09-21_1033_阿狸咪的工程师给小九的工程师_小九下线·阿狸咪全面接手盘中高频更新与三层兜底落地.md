# HANDOVER｜阿狸咪的工程师 → 小九的工程师｜2026-09-21 10:33｜小九下线·阿狸咪全面接手盘中高频更新与三层兜底落地

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

**主人令「小九下线，你全面接手上午的盘中高频更新」→ 已接手完毕。**
体检结论：**盘中链没有断**（24 个盘中类模块落后 0 个，最陈旧 27 分钟；线上=远端逐字节一致）。
真正断的**不是数据链，而是闭环**——本机盘中巡检的异常处置路径是「写 URGENT 给小九」，你走了没人接。
故本次**新增第三层状态驱动兜底**（`intraday_backfill.py`，四道闸门），并把该巡检升级为**阿狸咪自闭环**。

---

## 正文

### 一、接手前实况体检（全部实测，无推断）

| 检查项 | 实测结果 | 判据 |
|---|---|---|
| 远端 tip | `6fab063ddc` | `git fetch origin main` |
| **家机 `alimi-cn` runner** | **online / 空闲 / 已实证接单** | API `/actions/runners` + run **#126 的 `runner_name = alimi-cn`**（今天 09:26 实接小九那侧的 STOCK_QUOTE 活） |
| 小九 `lemoncat-cn` runner | GitHub 仍显示 online（人已下线，进程可能未退） | 同上 |
| **小九心跳产物** | `data/HB_XIAOJIU.js` 更新于 **10:15:12（0 小时前）** ⇒ **心跳仍在发** | 读远端 blob 内 `update_time` |
| 在途 / 排队 job | **无积压**（唯一在途：10:21 云端抓取 in_progress） | 扫 `actions/runs` 全量 jobs |
| 盘中数据新鲜度 | **24 个盘中类，落后 0 个**，最陈旧 27.0 分钟（`RISK_GAUGE`，本身 30 分钟一刷） | `intraday_backfill.py` |
| 线上产物 | HTTP 200 / 1,340,726 B，`BUILD=c4d29b5f8` | `curl` 实抓 |
| 线上 vs 远端 | 归一化 sha256 **完全一致** | 抹 `?v=`/`BUILD` 后逐字节比 |
| 离线门禁 | **12/12 全绿**，`[10/10]`=34/34 | `pre_deploy_audit.py` |

### 二、真实缺口（不是「所有任务都断了」）

| # | 缺口 | 实测证据 | 严重度 |
|---|---|---|---|
| 1 | **闭环断裂（真问题）** | 本机 `intraday_watch.py` 的 `write_urgent()` 抬头写「请人工复核后转正式 URGENT **给小九**」；且注释明令「绝不要再加派 dispatch」⇒ 你下线后**没人接这个球** | 🔴 高 |
| 2 | ~~小九心跳监控会持续误报~~ **【实测证伪 · 已自纠】** | 我原判「小九下线 ⇒ 心跳停更 ⇒ 必然误报」，故暂停了 `5edd92d6`；**随后实测 `HB_XIAOJIU.js` 10:15:12 仍在更新** ⇒ 假设不成立（**「人下线」≠「机器停」**）。且关掉告警器会让主人**不知道小九真掉线**。**已恢复 ACTIVE** 并补强告警文案 | ✅ 已纠正 |
| 3 | runner 离线邮件内容过期 | `351824ce` 邮件正文写「提醒小九单位机(primary)仍在线，A股数据管线不受影响」⇒ 已不成立且会误导 | 🟡 中 |
| 4 | 14 档准点调度器今天 **0 次** schedule 触发 | `v8_cn_fetch_intraday_lemoncat.yml` 总 103 次，今日仅 09:25:54 一次 `workflow_dispatch`；09-18 有 schedule 但**错时 2.5~5 小时**（cron 定义 CST 9:40-15:00，实际跑在 17:30-20:00）⇒ GitHub schedule 不可靠（既有认知，非新问题） | 🟢 低（被 L2 兜住） |

**关键澄清**：第 4 条**不等于数据断档** —— 云端 watchdog 是**状态驱动**的，今天正是它在兜。
故接手动作**不包含**「复活按铃/加密派发」（那是 2026-08-20 风暴的成因，见下）。

### 三、接手后的三层兜底架构

| 层 | 载体 | 节奏 | 触发方式 |
|---|---|---|---|
| **L1** | `v8_cn_fetch_intraday_lemoncat.yml`（云端 14 档） | `:00/:20/:40` | schedule（**不可靠**，今天 0 次） |
| **L2** | `v8_cn_fetch_watchdog.yml`（云端状态驱动） | `:07/:22/:37/:52` | schedule + 陈旧度自愈 |
| **L3** | **`intraday_backfill.py`（本次新增·本机）** | 每小时 | **纯状态驱动 + 四道闸门** |

> 设计意图：L1 管准点、L2 管自愈、**L3 只管「前两层都失效时把链重新拉起来」**。
> L3 频率低是**刻意**的——它的价值不在密度，而在云端全哑时仍有一个独立于 GitHub 的起点。

### 四、本次改动清单（4 处，全部已落地）

**1. 新增 `~/.workbuddy/v8_watch/intraday_backfill.py`（兜底补派器）**
- 判据：`category_map` 中含 `intraday` 的 24 个 VAR，最陈旧滞后 > **35 分钟**（与云端 watchdog 同口径）
- 🔴 **实测坑（写代码时踩到）**：`category_map` 的值是**逗号分隔多值**（如 `"intraday,post_close"`），按单值 `v == "intraday"` 匹配会 **0 命中**；必须 `"intraday" in v.split(",")`
- 派发目标：`v8_cn_fetch_cloud.yml`（`category=intraday`）
- 退出码：`0`=健康未干预 / `1`=已补派 / `2`=断档但被闸门拦 / `3`=时段外

**2. 自动化 `94279c7c` 升级（盘中巡检 → 盘中接手看守）**
- 原名「v8 盘中巡检（每30分钟·防踩踏风暴）」→ 现名「阿狸咪-盘中接手看守(小九下线期·只读侦测+守门兜底·每小时)」
- 第 1 步保留只读侦测；**第 2 步新增 `--execute` 守门兜底**；处置从「写给小九」改为「阿狸咪自闭环」
- rrule `FREQ=HOURLY;INTERVAL=1`（⚠️ 见第六节实测坑）

**3. `5edd92d6`（阿狸咪-小九心跳监控）—— 曾暂停，实测证伪后已恢复 ACTIVE**
- 起初我判「小九下线 ⇒ 心跳停更 ⇒ 必然误报」，暂停了它；**随后实测 `data/HB_XIAOJIU.js` 于 10:15:12 仍在更新**
  ⇒ 假设不成立（**人下线 ≠ 机器停**）。且关掉告警器会让主人**不知道小九真掉线** —— 那是错的。
- 已恢复 ACTIVE，并补充告警文案：真报掉线时明确写「本机 alimi-cn 独家承接 `[self-hosted, cn]` 链 +
  盘中兜底已由『阿狸咪-盘中接手看守』负责，**无需人工介入**」——让告警**可据此判断是否需要动手**。

**4. 修正 `351824ce` 邮件文案**
- 原文「小九仍在线、A股数据管线不受影响」→ 改为「小九已下线，**本机 alimi-cn 是当前唯一 cn runner**，其离线会直接影响盘中抓取，请优先恢复」

### 五、四道闸门（防风暴的核心，勿简化）

补派器只有**四道全过**才动手：

1. **≥2 个**盘中类 VAR **同时**滞后 > 35 分钟（🔴 判据是「**计数**」而非「最陈旧单点」——
   实测 `RISK_GAUGE` 由**独立 workflow** `v8_risk_gauge.yml`（每 30 分钟）刷新，其滞后天然在 0~30 分钟周期波动
   **且永远占据榜首**；若只取最陈旧单点，一旦它偶发超阈值，本器会为**与它无关的 cn_fetch 链**无意义派发。
   实测分布（10:36）：`RISK_GAUGE` 32.1min vs 其余 23 个**均为 10~15min** ⇒ 计数判据既抗单点抖动，
   **又不依赖维护「哪些属 fetch 链」的清单**——避免清单随迭代漂移，这是本项目的历史教训）
2. **无在途** fetch/stock_quote run（有在途不派）
3. **近 15 分钟**内无相关 run（刚跑过不派）
4. **本 20 分钟档未派过**（幂等，状态文件 `backfill_state.json`）

> 为什么必须这么严：2026-08-20 前 WorkBuddy「云端按铃」每 20 分钟**无条件** dispatch，叠加其它源
> → 多源齐发 → GitHub secondary rate limit 403 → **数据彻底推不上 main**。
> 主人令改为「盘中单源闭环」，由 14 档调度器统一调度。**本器是第三层，绝非新按铃，严禁退化成按节奏盲派。**

### 六、🔴 三条本次新得的实测判据（入库备查）

**判据 A｜本环境 HOURLY 规则的 `BYMINUTE` 不生效**
- 证据：同一任务三次 `nextRunAt`（`BYMINUTE=5,25,45` / `=45` / 不带）分别为
  `1789961489731` / `1789961521449` / `1789961536934` —— **两两仅差 15~32 秒**，分钟均取「更新时刻 + 1 小时」。
- 含义：无法用 `BYMINUTE` 做「错峰/固定分钟」；`nextRunAt` 也不是验证 rrule 生效的可靠方式（它只反映下次触发，不反映分钟规则）。
- 处置：改为 `FREQ=HOURLY;INTERVAL=1`，本器**状态驱动**（判据是新鲜度，不是准点）⇒ 分钟漂移无害。

**判据 B｜`.github` job logs API 会丢 auth**
- `GET /actions/jobs/{id}/logs` → 302 到 `productionresultssa*.blob.core.windows.net`，
  带 `Authorization` 跟随会被 Blob 存储拒（**401**），不带则二次 302。
- **正解**：看失败 step 用 `/actions/runs/{id}/jobs`（含 `steps[].conclusion`），**不要去拉 logs**。
  本次即靠它定位到：`v8_build_deploy` 失败集中在 `4f87d6adf`/`871df8cbe`/`99ee9b5c2` 三个连续提交（09:01–09:22，分别是部署步/门禁），**09:26 起全绿**；`STOCK_QUOTE cloud #160`(10:08) 是 `fetch_stock_quote_v8.py` 偶发失败，10:14/10:21 已 success。

**判据 C｜「人下线」≠「心跳停更」，二者**不可互相推断**（本轮我据此犯过错）**
- 我最初把「小九下线」直接等同于「其心跳产物会停更」，由此暂停了心跳监控。
- 实测反证：`data/HB_XIAOJIU.js` 的 `update_time = 2026-09-21 10:15:12`（接手时仍在更新）
  ⇒ 小九机上的**定时任务仍在运行**，只是**人不在了**。
- **教训**：判「某侧是否离线」的唯一依据是**该侧的状态产物/runner 接单记录**，不是主人的一句话描述；
  同时，**关掉告警器的门槛必须高于打开它的门槛**（宁可多一条噪音，不可漏一次真掉线）。


### 七、未实测项（诚实标注，不许当成已完成）

- 🔴 **补派器 `EXIT=1`（真派发）路径尚未实测**。原因：当时**有在途 run**（10:21 云端抓取 in_progress），
  按红线不得再派（会排队且违反防风暴）。**已验证的替代证据**：两个目标 workflow 的
  `GET /actions/workflows/{wf}` 均 **HTTP 200**（路径+auth 可达）、token scopes 实测含 `repo, workflow`、
  仓库 `permissions.admin = true`、两 workflow 的 `concurrency.cancel-in-progress` 均为 `false`（补派**排队不互杀**）。
  ⇒ 首次真实触发时会自然验证该路径，**届时请核对 `backfill_state.json` 是否记账**。

### 八、待你回归时（请按此恢复）

1. `5edd92d6` **无需处理**（已恢复 ACTIVE，名字也已改回原样）
2. 本机 `94279c7c` 的看守可保留（它是 L3，与你并不冲突：闸门 2/3 会识别你正在跑而自动退让）；
   若你希望白天完全由你主导，可把它改为只在 `BYHOUR` 覆盖不到的空窗跑
3. `351824ce` 的邮件文案需**改回**「双机互备」口径（我改的是「本机唯一」版本）

### 九、本次**刻意不动**的项（属盘后窗口，不在「上午盘中」范围）

| 任务 | 为什么不now动 |
|---|---|
| `a8c1662a` 九宝量化-小九交接汇报 18:00 | 盘后任务；你不在时它只是读不到交接件，**不会产生错误状态**，也不会 push |
| `90b7ea6e` 盘后链夜班监控+小九交接巡查 | 同上，且它「仅真基础设施失败才补派一次」 |
| `da2072fc` 盘后算法链接力推进器（16:00-08:59） | 它是盘后链**唯一派发源**；改它风险高且与上午无关 |
| `automation-1788058041833` 自愈监控闭环 | 窗口排除 08:00-17:59 **正是为了避你**；且它会 `git push origin main`，盘中动不得 |
| **站内「双机状态」卡文案过期** | `index.html:8594` 写「阿狸咪（家中机）本地预览（file:///…），**不接部署**」——**自 09-21 起已不成立**（本机现承接盘中兜底 + 以 alimi-cn 实接 `[self-hosted, cn]` 活）。改前端会触发 `v8_build_deploy`，**盘中不做**，留待盘后窗口 |

### 十、红线（本次全程遵守）

- ✅ 全程**未** `git add/commit/push` 任何仓库改动（交接件走 API 推送）
- ✅ 未改 `index.html`；未写 WorkBuddy DB；未删除任何自动化任务（只 PAUSED）
- ✅ 未按节奏盲派；未在盘中对 `index.html`/前端做任何改动
- ⚠️ 本机工作树在接手期间曾落后远端 50 个提交，已 `git fetch + reset --hard` 追平（脏文件先备份到仓外）

---

## 附：本次涉及文件/对象索引

| 类型 | 标识 |
|---|---|
| 新增脚本 | `C:\Users\HH20210606\.workbuddy\v8_watch\intraday_backfill.py` |
| 新增状态文件 | `C:\Users\HH20210606\.workbuddy\v8_watch\backfill_state.json`（首次真派发后生成） |
| 复用脚本（未改） | `intraday_watch.py`（548 行，只读侦测） |
| 自动化（改） | `94279c7c`（升级）、`351824ce`（文案） |
| 自动化（暂停） | `5edd92d6` |
| 远端 tip（撰写时） | `6fab063ddc` |

> 署名：阿狸咪的工程师（家机 `alimi-cn`）· 2026-09-21 10:33 CST
