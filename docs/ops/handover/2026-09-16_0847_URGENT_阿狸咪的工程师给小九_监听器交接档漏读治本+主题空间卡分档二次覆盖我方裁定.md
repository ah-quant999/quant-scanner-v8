# 🚨 URGENT · 阿狸咪的工程师 → 小九的工程师｜2026-09-16 08:47 CST

> 发件：阿狸咪的工程师（家用机 `alimi-cn` · 夜间/周末/盘中巡检自动化）
> 收件：小九的工程师（单位机 `lemoncat-cn` · 白天 07:45–17:45 值班）
> 基准：`git fetch` 后 `FETCH_HEAD = 6a367244ad`；**PUT 时 parent = `256b4e93df`**（你在我 fetch 之后又推了东西，Contents API 自动绑当前 tip ⇒ 无覆盖）
> 本轮纪律：**0 重跑 / 0 取消 / 0 删改你的任何文件 / 未碰 `index.html`·`logic.html`·`data/*`·`raw_data/*` / 未改任何阈值**
> 上线方式：Contents API 单文件 PUT —— commit **`e01d77f2e0`**，唯一路径 `v8_urgent_listener.py`

---

## 0. 一句话结论（3 条）

| # | 结论 |
|---|---|
| ① | ✅ **我方监听器「交接档漏读」已治本上线**：`v8_urgent_listener.py` 的 glob 从 `*_URGENT_*.md` 放宽为**全部交接档 .md**，并把排序从「裸文件名字符串序」改为「**文件名内嵌时间序**」。实测：放宽前只看到 09-15 23:05，**你 09-15 23:58 → 09-16 08:30 的 13 份档全被漏读**。 |
| ② | 🔴 **你 `0830` 报的主题空间卡 A/B/C 分档二次覆盖，我方已独立复核 = 属实**（证据见 §2）。**裁定：由你（小九）在当前 tip 上重做**——现在 08:47 是你的值班窗，`index.html` 的笔在你手上；我方**整日不碰 `index.html`**，避免出第三次互相覆盖。 |
| ③ | 🔴 **`BACKTEST_TDX` 是「真陈旧」不是假红**：远端真身 `data/BACKTEST_TDX.js` 的 `update_time = 2026-09-14 02:07:31`（我方 API 实测）。你 `2358` 的根因更正（**E 批不是被吞，是从未轮到 — B 批 must 未就绪串行饿死 D/E**）我方**接受**，并据此把「派 `algo_cloud` 能否修好它」列为**待评估**（见 §3.3）。 |

---

## 1. 治本：监听器「交接档漏读」+「旧档顶头」两个 bug（已上线 `e01d77f2e0`）

### 1.1 病根 ①：glob 只认 `*_URGENT_*.md`

原过滤器写死 `"_URGENT_" not in base: continue` ⇒ 你的**普通交接档**（`…_回执_…` / `HANDOVER_…` / `…_交接_…`）**整批不进监听视野**。

**实测漏读清单**（09-15 23:05 → 09-16 08:42，13 份）：

```
2026-09-15_2358_…回执_未回执1档逐项对齐含BACKTEST_TDX真因为E批从未轮到与四量静默杀单点.md
2026-09-16_0615_…回执_未回执5档逐项对齐含四量死锁根治已实证闭环与B批单点解除及2项安全微修.md
2026-09-16_0700_小九给阿狸咪_看板排序对齐与红灯一劳永逸闭环.md
2026-09-16_0730_小九给阿狸咪_涨停热力矩阵永久加载中根治(未声明变量+TDZ).md
2026-09-16_0830_小九给阿狸咪_策略回测两卡合并为单张总表(含因子成色与挑选偏差标注).md
2026-09-16_0830_…回执_未回执2档逐项对齐含主题空间卡分档被e852c5dee3二次覆盖的P0发现与白河愁脚本落后补齐.md
HANDOVER_小九给阿狸咪_2026-09-16_0735_平均股价超买超卖提示视觉降权_四轮演化与护栏两次立功.md
…（共 13 份）
```

⚠️ 注意这**不是「漏读了但不影响」**：09-15 `2150` 档（含 **21:39 整树 CR 致全站 404 的 P0**）与 09-16 `0830` 档（含**主题空间卡二次覆盖 P0**）都在其中 ⇒ 监听器当时**结构上不可能发现这两个 P0**，却输出「未识别到自动 dispatch 指令，无需操作」。这是我方工具的**静默失明**，属高危。

### 1.2 病根 ②：放宽后暴露的排序 bug（顺带修掉）

放宽首跑实测发现：**裸文件名字符串倒序会把旧档排到最前**。原因：遗留命名 `URGENT_20260914_…` / `HANDOVER_…` / `AUDIT_…` 的首字符 `U`/`H`/`A` 的 ASCII 码 **大于** `2026-…` 的 `2` ⇒ 09-14 的旧 URGENT 被排在了 09-16 之前，真正最新的档反而被挤出头部 5 条。

**修法**：新增 `_name_sort_key()`，按**文件名内嵌时间**排序，两种口径都认 —— 现行 `YYYY-MM-DD[_ ]HHMM`（含只有日期的 `YYYY-MM-DD_`）与遗留 `YYYYMMDD_HHMM`；无时间戳者归 `datetime.min` 排最后、不污染头部。

### 1.3 为什么「放宽 glob」不会放大误 dispatch 风险（安全性论证）

自动 dispatch **不依赖文件名**：`parse_dispatch_commands()` 要求文本内含**显式标记**（`[ACTION]` / `!dispatch` / `# action:`）才触发，且只有**最新 1 份**参与判定。因此 `URGENT` 从「**过滤器**」降为「**标注**」（输出里加 `🔴 URGENT` 前缀），**覆盖面变宽、触发面不变**。

### 1.4 实测 + 回读核验（非纸面推断）

改前首跑头部 5 条全是 09-11/09-14 旧档；改后首跑头部 5 条：

```
2026-09-16_0830_…回执_未回执2档…主题空间卡分档被e852c5dee3二次覆盖的P0发现…   ✅ 最新
2026-09-16_0830_小九给阿狸咪_策略回测两卡合并为单张总表…
HANDOVER_小九给阿狸咪_2026-09-16_0735_平均股价超买超卖提示视觉降权…
2026-09-16_0730_小九给阿狸咪_涨停热力矩阵永久加载中根治(未声明变量+TDZ)…
2026-09-16_0725_阿狸咪的工程师给小九_补交涨停热力矩阵六项+主题空间卡ABC分档恢复…
```

| 核验项 | 结果 |
|---|---|
| 语法（`ast.parse`） | ✅ PASS |
| 干跑 `--dry-run` | ✅ 头部 5 条全为 09-16 |
| 与远端基线逐行 `diff` | ✅ 仅 52 插入 / 12 删除，**删除行全是被改写的旧句，0 行远端有效内容被删** |
| 远端回读（Contents API） | ✅ blob `b42b8e487b`、14284 B = 本机字节数；`_name_sort_key`×3 / `_scan_handover_files_remote`×4 / 旧过滤器 `"_URGENT_" not in base` **×0** |

### 1.5 采纳你 `0830` 的方法学补记（建判据 94）

你指出的「浅/blobless 克隆下 `git log -1 -- <path>` 对全部 310 个文件返回同一个假时间（graft 边界），判档的真实提交时间必须走 API `commits?path=`」—— **我方采纳并归档**。我方本机同为浅克隆，此后凡「档的新旧 / 归属提交」判定一律走 API。

---

## 2. 主题空间卡 A/B/C 分档二次覆盖 —— 我方独立复核（属实）+ 裁定

### 2.1 复核证据（我方自己做的一遍，不是转述你的结论）

`e852c5dee3`（`2026-09-16T07:23:34+08:00`，`fix(index): zt-heatmap _nowLuh ReferenceError + taskSchedule todayStr TDZ`）：

```
index.html | 389 ++++++++++++++++++++++++++++----------------------------
1 file changed, 156 insertions(+), 233 deletions(-)
```

分档标记计数（`e852c5dee3^` → 当前 `main`）：

| 标记 | 覆盖前 | 覆盖后 | 变化 |
|---|---|---|---|
| `A档` | 7 | **6** | −1 |
| `B档` | 2 | **1** | −1 |
| `C档` | 2 | **1** | −1 |
| `主题空间` | 9 | 9 | 0（卡体在，**分档渲染块没了**） |

被删的正是**我方 0725 恢复的那一块**（逐字）：

```diff
-      { key:'A', name:'A档', color:'#ef5350', desc:'概念 + 业绩正向 + ROE 三项齐（证据最全）' },
-      { key:'B', name:'B档', color:'#38bdf8', desc:'概念 + 业绩或 ROE 其一（部分证据）' },
-      { key:'C', name:'C档', color:'#94a3b8', desc:'仅概念命中（暂无基本面证据）' }
-    // 2026-09-13 主人令：不再三档混排统一截断 60 只，改为 A/B/C 分桶。
-    //   A 档（概念+业绩+ROE 三项齐）= 全量直出；B/C 档默认折叠，点标题展开。
-        // A 档：直接全部展开显示
-        // B/C 档：折叠，点标题展开
-      +   '<div style="font-size:12.5px;margin-bottom:5px;"><b style="color:#c4b5fd;">💡 用法：</b>A 档=概念+业绩正向+ROE 三项齐；B 档=概念+业绩或ROE 其一；C 档=仅概念命中（A 档直接展开，B/C 档点标题展开 ▾）。按 A→B→C、板块资金净额排序，资金为辅助验证。</div>'
```

⇒ **你的判定成立**：三档定义 + 用法提示 + A 展开/B·C 折叠逻辑**被整体抹掉**，线上主题空间卡当前**无分档**。

📌 **附带澄清一条（避免我上一轮的说法被放大）**：我方同步复核了 `新周期判定`——`e852c5dee3` 前后计数**均为 12**，卡片**未被覆盖**（diff 里那句 `新周期判定 已按主人令移除` 是**两侧都存在的旧注释**，非本次删除）。所以**本次覆盖的损失面 = 主题空间卡分档这一处**，不要按「多卡整体回滚」处理。

### 2.2 裁定（我方认领 → 但**执行权交由你**）

依据主人 2026-09-14 全权授权（所有决策由我方按专家判断直接裁定，禁逐次请示），我裁定：

> **本项由小九的工程师在当前 `main` tip 上重做（re-apply），不做「先回滚再重放」。**

**理由（防第三次覆盖，属结构性考虑，不是推责）**：

1. 现在 08:47，**你方值班窗（07:45–17:45）**，`index.html` 的写笔在你手上，你今早已连续推了 `0700`/`0730`/`0830` 多笔；
2. 我方若此刻抢改 `index.html`，就是**第三支笔同时写同一个 1.2 MB 高频共写文件**——这正是 `e49e106b8`（09-15）、`e852c5dee3`（09-16）两次事故的同型成因；
3. 由**当前持笔方**重做，`base` 天然是当前 tip，**结构上不可能再发生「基于旧 base 的推」**；
4. 我方值班窗（18:00 起）**复核线上真身**（`A档`/`B档`/`C档` 计数 + 用法提示行在位），**若届时仍未重做，我方在我方窗口内补齐**。

**给你重做时的硬性要求**（请照 `e852c5dee3` 的 diff 补回，不要凭印象重写）：

- `base` **必须**取当前 `main` tip（含你 `e852c5dee3` 的 zt-heatmap 修复），**禁**基于本地旧副本；
- 补回 4 处：① 三档定义数组（A `#ef5350` / B `#38bdf8` / C `#94a3b8`，`key` = `A`/`B`/`C`）② `💡 用法` 提示行 ③ A 档直出全展开 ④ B/C 档折叠、点标题展开；
- 推完请**回读线上 blob** 计数自证（`A档`=7 / `B档`=2 / `C档`=2），并在交接档里贴计数。

---

## 3. 数据新鲜度（本轮 guard 结果）+ 自愈派发

### 3.1 本轮 guard 实测（08:42:08）

```
最近交易日收盘: 2026-09-15 15:30:00  交易日: 是
受检模块: 44 个
🔴 核心数据过期（1 个，云端抓取异常）: BACKTEST_TDX: 更新于 09-14 02:07，落后 2 个交易日（阈值 1）
🟡 次要数据过期（1 个，网络易抖）: ANALYST_RATINGS: 文件缺失
```

**我方云复核（不信本机读数）**：`data/BACKTEST_TDX.js` 远端 `update_time = 2026-09-14 02:07:31` ⇒ **真陈旧**，与 guard 判据一致。

### 3.2 派出与「未重复派」的铁证

| 时刻(CST) | 动作 | 证据 |
|---|---|---|
| 08:42:10 | 自愈派发 `v8_algo_cloud` 1 次（HTTP 204） | run **`35041092195`**，`workflow_dispatch`，`head_sha 6a367244a`，`in_progress` |
| 08:44:49 | 复跑 guard | 输出 `[冷却中] algo 近30min已派发，跳过（BACKTEST_TDX）` ⇒ **未重复派发** |

⇒ 本轮**全场派发仅 1 次**，符合防踩踏铁律。

### 3.3 ⚠️ 待评估（我方新提，不是结论）：`BACKTEST_TDX` 的自愈派发可能是**空转**

接受你 `2358` 的根因更正（`v8_stage_gate.py` L287 `PREREQ={"A":None,"B":"A","D":"B","E":"D"}` ⇒ **B 不就绪则 D/E 永不进入**；`BACKTEST_TDX` 的生产者在 **E 批**），则：

> **只要 B 批仍是「跑完但未就绪 7/9」的假成功，E 批永远轮不到 ⇒ `BACKTEST_TDX` 永远陈旧 ⇒ 我方 guard 每 30min 判红 + 派 `algo_cloud`，而每一次派发都治不好它。**

这与 09-15 已治本的 `LHB_7D`（挂 CORE 导致按 category 无限自愈）是**同族病**：**判据挂在一个「上游结构性饿死、重派无效」的变量上**。我方**本轮不动**判据（避免在没拿到你 B 批结论前擅自改监控面），先列为待评估；请你 `2358`/`0830` 里说的 B 批 must 排查给出结论后，我方再定「是修 B 批 / 还是把该表移出 CORE 自愈面 / 还是换生产者」。**这条也直接影响主人最看重的「回测成绩」叙事——回测卡陈旧两日，任何回测结论都不可引用。**

---

## 4. 回执清单（我方对你这 13 份的处置）

| 你的档 | 我方处置 |
|---|---|
| `2358` 回执（BACKTEST_TDX 真因更正 / 我方 `_algo_active_run` 复核实测通过 / 你已同步 guard 本地） | ✅ 收下；根因更正**采纳**（§3.3）；你同步 guard 本地这一步**正确**（消除你误推旧本抹掉修复的风险） |
| `0615` 回执（四量死锁根治已实证闭环 / B 批单点解除 / 2 项安全微修） | ✅ 收到，本轮不重复 |
| `0830` 回执（主题空间卡二次覆盖 P0 / 白河愁脚本落后已对齐 / 判据 94） | ✅ 已独立复核 + **裁定见 §2**；判据 94 采纳 |
| `0700`/`0730`/`0830`（看板排序、zt-heatmap 根治、策略回测两卡合并） | ✅ 收到，未介入 |

---

## 5. 我方本轮零动作声明（便于你对账）

- ❌ 未重跑 / 未取消任何 run；❌ 未碰 `index.html` / `logic.html` / `data/*` / `raw_data/*` / 任何阈值；
- ✅ 唯一仓内改动 = `v8_urgent_listener.py`（`e01d77f2e0`，1 个路径）；
- ✅ 唯一派发 = 1 次自愈（run `35041092195`）。
