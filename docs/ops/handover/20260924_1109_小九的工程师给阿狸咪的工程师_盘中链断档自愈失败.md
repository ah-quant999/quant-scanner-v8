# 盘中实时链断档 · 自愈失败交接件

- **落件时间**：2026-09-24 11:09（北京时间，周四）
- **落件机**：小九（本机 · 盘中实时链巡检自动化）
- **级别**：🔴 P0 —— 需人工介入
- **一句话结论**：盘中实时链自 **10:21** 起停止上线。根因是 **GitHub 安装级 API 配额耗尽（HTTP 403）**，导致 27 个 `data/*.js` 全部上传失败，线上保留旧版本、卡片时间戳因此冻结。**dispatch 类自愈已确认无效且会加剧消耗。**

---

## 一、判定对象与实测（11:07 复查）

| 卡片 | 文件 | update_time | 距今 | 判定 |
|---|---|---|---|---|
| 指数行情 | INDEX_QUOTES.js | 2026-09-24 10:20:40 | 46.7 min | ❌ 陈旧 |
| 概念排名 | CONCEPT_RANKING.js | 2026-09-23 15:42:31 | 1164.8 min | ❌ 陈旧（跨日） |
| 板块资金流 | SECTOR_FUND_FLOW.js | 2026-09-24 10:20:21 | 47.0 min | ❌ 陈旧 |
| 涨停热力矩阵 | LIMIT_UP_HEATMAP.js | 2026-09-24 10:21:19 | 46.0 min | ❌ 陈旧 |
| 市场资金流 | MARKET_FUND_FLOW_DATA.js | 2026-09-24 10:21:33 | 45.8 min | ❌ 陈旧 |
| 个股行情 | STOCK_QUOTE.js | 2026-09-24 10:40:42 | 26.7 min | ✅ 正常 |

判据：距今 > 40 分钟 或 非今日 ⇒ 陈旧。**5/6 陈旧**，且 4 张卡于 11:00 前后跨过 40 分钟阈值。

## 二、已执行的自愈（4 轮，全部无效）

| 轮 | 时间 | 动作 | 结果 |
|---|---|---|---|
| 1 | 10:22 | dispatch `354533319` 盘中准点档调度器 `{"ref":"main"}` | HTTP 204；产生的 run 被后续 run 顶掉 |
| 2 | 10:26 | dispatch `327687211` 云端中国数据抓取 `category=intraday` | HTTP 204；run 10:26 创建 → 10:35 cancelled（同并发组被更新 run 顶掉） |
| 3 | 10:43 | 同上重发 | HTTP 204；run 10:43 → 10:45 cancelled |
| 4 | 10:59 | 同上重发（趁队列清空） | HTTP 204；11:00 那轮 success 收尾，但**数据仍未上线** |

补充：10:35 已有系统侧派发的 `v8_cn_fetch_cloud_selfhosted`（应急链）在跑并于 10:56 success 收尾，**故未重复派发应急链**（遵守 2026-09-23 防回踩护栏）。
僵尸扫描：11:07 队列 `in_progress = 0`，无冻结 >25 分钟的假活 run，无需 cancel。

## 三、🔴 根因（决定性证据）

取自 run **35949615689**（`v8_cn_fetch_cloud`，11:00 创建 → 11:04 success）的 job 日志：

```
⚠️ API POST /repos/ah-quant999/quant-scanner-v8/git/blobs -> HTTP 403
⚠️ 跳过（8 次均失败，HTTP 403）: data/SECTOR_FUND_FLOW.js
...
⚠️ 共 27 个文件上传失败，将保留远程旧版本: [...]
##[error]data/SECTOR_FUND_FLOW.js 本轮上传失败，线上保留旧版本（update_time 不刷新）

message: API rate limit exceeded for installation.
         ... timestamp 2026-09-24 03:04:32 UTC
[relay] ❌ 派发失败: HTTP Error 403: Forbidden
         —— 链条断于此，由 v8_cn_fetch_watchdog(>45min) 恢复
```

要点：

1. **抓取本身成功**：`✅ SECTOR_FUND_FLOW → raw_data/…`、`✅ LIMIT_UP_HEATMAP → …`、`✅ MARKET_FUND_FLOW_DATA → …` 均已写入 `raw_data/`。
2. **失败在最后一步「上传 `data/*.js` 到 main」**：`POST /git/blobs` 连续 8 次 403，27 个文件全部跳过 ⇒ 线上保留旧版本 / `update_time` 不刷新。
3. 403 正文 = `API rate limit exceeded for installation` ⇒ **GitHub App 安装级配额耗尽**；不是权限配置错、不是网络故障。
4. 同一轮内 `GET /actions/runs`、`POST /dispatches` 也一并 403 ⇒ 配额为安装级共享，被整体打爆。
5. 仓库既有注释早已记载该机制（`v8_algo_cloud.yml`：「并发洪流触发 secondary rate limit（HTTP 403）→ 全量 403」），本次即同一根因复现。

**次生现象（极易误判）**：`api_push_raw.py` 的「防倒退守卫」需**逐个下载远端 blob 比时间戳**，单轮消耗数百次 API 调用 ⇒ 每多跑一轮都在加速烧配额。今日该仓库已产生 126 条 run（含大量 cancelled），上午配额被耗尽具有必然性。

**副线（非主因，但需记）**：东财 `push2delay` 侧对 `m:90 t:3`（概念）间歇返回空（本机同进程对照：行业 `t:2` 稳定 100 行，概念 `t:3` 时通时空），触发 `api_push_raw` 的「数据未变，跳过重写（幂等）」。故 `CONCEPT_RANKING` 即便上传恢复，也需**源级空结果重试**才能稳定推进。

## 四、为什么 dispatch 救不了（★ 请勿再机械派发）

- 失败点在**上传**，不在**抓取**，也不在**调度**。任何 dispatch 都会再产生一轮上传，继续撞 403。
- ⚠️ **反效果**：新增的 API 调用会进一步挤占配额，越派越差。本轮已在 10:22/10:26/10:43/10:59 派发 4 次，**现已主动停止派发**。
- 有效路径只有两条：**降低 API 调用量**，或**换用独立配额的凭证**。

## 五、下一步建议（按优先级）

1. **P0 · 盘后（15:00 后）立即执行**：把 `api_push_raw.py` 的上传凭证由 `secrets.GITHUB_TOKEN`（安装级共享额度）切换为**独立 PAT**（同本机 `v8_gh_token.txt` 口径，独立额度），或让推送走工作流里已有的 `git push "$REPO" HEAD:main` 分支（不受 API 配额约束）。此为**一劳永逸**解法。
2. **P1 · 降配额消耗**：`api_push_raw.py` 的防倒退守卫改为**单次 tree 请求批量取远端时间戳**，替代逐文件下载 blob（仓库注释 L435/L551 已指出「推送慢的根因」即此）。
3. **P1 · 给 403 加真告警**：当前 403 只打 `##[error]`，job 仍 success ⇒ 健康巡检/看门狗全绿而线上冻结（READY_SPEC 盲区）。建议 403 计数 >0 即发邮件至 2814546@qq.com 并写入 `data/freshness_status.json`。
4. **P1 · 源头兜底**：`f_concept_ranking()` 增加「空结果指数退避重试」，避免单次空响应即整卡跳过（与 2026-09-23 挂账同源）。
5. **P2 · 止损**：配额恢复前暂停一切非必要 dispatch（含巡检自动化自身的派发），避免配额被反复烧穿。

## 六、证据与现场清单

- 巡检脚本 `_pt_check_20260924.py` / 结果 `_pt_check_20260924.json`（本机 `E:/v8data/qs8-tmp/`）
- 日志检索脚本 `_pt_logsearch_20260924.py`
- 关键 run：`35949615689`（云端 · 27 文件 403）、`35947927898`（应急 · 源返回空后回退旧值）
- 源站对照：同进程 `m:90 t:2` 稳定 100 行 vs `m:90 t:3` 时通时空

---

小九的工程师 · 2026-09-24 11:09
