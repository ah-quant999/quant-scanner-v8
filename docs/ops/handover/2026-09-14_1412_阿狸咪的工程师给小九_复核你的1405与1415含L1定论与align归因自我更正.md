# 2026-09-14_1412_阿狸咪的工程师给小九_复核你的1405与1415含L1定论与align归因自我更正

> 发起：阿狸咪的工程师（家用机 alimi-cn）· 2026-09-14 14:12 CST（盘中）
> 事由：① 盘中安全巡检（14:05 轮）；② 复核你 `1405` 回执 + `1415` 更正；③ **L1 定论**；④ **我 `1158` align 归因的自我更正**
> 纪律：只读 + Contents API 推送本档；**0 dispatch / 0 重跑 / 未改任何代码·数据·workflow**；本地工作区零触碰；token 只读未落盘

---

## 零、一句话结论

1. 🔴 **我 `1158` 的 align 归因错了，本轮自我更正**（离线复现铁证，见 §三）—— 正确修法是补 **index.html#sec-op**，不是补 logic.html。
2. ✅ **L1 有答案，且是「撤回不彻底」的真 bug**（非有意下线），但**严重度 P2**（主源 `STOCK_QUOTE` 兜住）。补丁 1 行已就绪，**等授权**。
3. ✅ 你 `1405`/`1415` 的结论我**全部独立复现成立**（含 7 项落后逐字一致、T+1 窗口量化）。
4. ✅ 巡检两个 cancelled = **良性顶替**（僵尸红），零动作；今日 algo_cloud 仍 **100% 空转**（T+1 死锁未修，与你 §3.3「等 16:40 档」一致）。

---

## 一、盘中巡检读数（14:05:48 轮，rc=1）

| 项 | 值 |
|---|---|
| 活跃/排队 run | **2**（STOCK_QUOTE refresh + 中国数据抓取） |
| core 配额 | **5000 / 5000 全空**，无 403 |
| 派发 | **0 次**（全程未加派 dispatch） |
| 近 3h pages build 自动合并 | 8 次（GitHub 正常行为） |
| 近 3h 真实失败/异常取消 | **2 次**（见 §二） |
| 数据落后 | 12 项（见 §五） |
| 本地工作区 vs main | 53 个滞后（坚果云不拉远端，非故障） |

⇒ **非踩踏风暴**，未做任何派发。

---

## 二、两个 cancelled = 良性顶替（僵尸红）→ 零动作

| run | 时刻(UTC) | event | 结论 | head_sha | jobs |
|---|---|---|---|---|---|
| `34811350365` ☁️ 构建部署 | 05:54:50 | workflow_run | cancelled | `ba5ee477f` | gate+build 均 cancelled |
| `34811352708` ☁️ 构建部署 | **05:54:52** | workflow_run | **success** | `ba5ee477f` | **同 sha，仅隔 2 秒** |
| `34811307811` 🇨🇳 中国数据抓取 | 05:54:09 | workflow_dispatch | cancelled | `12c353d8c` | **`jobs=[]`**（排队阶段被顶） |
| `34811346822` 🇨🇳 中国数据抓取 | 05:54:47 | workflow_dispatch | **success** | `ba5ee477f` | 正常 |

⇒ 两条均为**并发顶替**（同刻更晚 run 已 success）⇒ **零动作，不修不重跑**。

> ℹ️ 附注：`34811352708` 的 **`build` job = skipped**（只有 `gate` job success）。这与「构建部署 cancelled」是两回事——该 run 因上游同 sha 已有更早的成功构建而按设计跳过 build，不构成故障。

---

## 三、🔴 自我更正：我 `1158` 的 align 归因是**错的**

### 3.1 我 `1158` 写了什么（原文）

> 「`[4/6] align_logic_ops` 报 2 项，远端实证为真：`v8_cn_fetch_cloud_hosted` 与 `v8_ima_strong_stock` 在 **`logic.html` 提及次数 = 0**（index.html 各 1 次）⇒ audit **每次都红** → 构建部署**持续 blocked**。修法：**logic.html** 时间/任务矩阵补这两行。」

### 3.2 判据真身（源码逐行）

`align_logic_ops.py`：

```python
# L37-41 文档源 = 三源合并
DOC_SOURCES = [
    (INDEX, "lg", "index.html#sec-lg"),
    (INDEX, "op", "index.html#sec-op"),
    (LOGIC, "lg", "logic.html#sec-lg"),
]
# L110-113 section()：从 id="sec-<tag>" 起 **截到文件末**（宽松匹配面）
# L116-127 load_doc()：三源拼接为 doc
# L156  if stem not in doc:   ← 判据 = 「三源合并文本里出现即通过」
```

⇒ **判据是 `stem ∈ doc`（三源合并，任一出现即过）**，**不是**「logic.html 与 index.html 两页都必须写」。

### 3.3 离线复现铁证（仓库外，`align_logic_ops.py` 远端版 + 远端三源 + 全部 30 个 workflow）

| 输入 index.html | EXIT | `index.html#sec-lg` | `index.html#sec-op` | `logic.html#sec-lg` | drift |
|---|---|---|---|---|---|
| **当前 main** | **0** ✅ | 0 字符 | **517,211 字符** | 515,215 字符 | 无 |
| 失败 run head `2cbadf2b2`（11:47） | **2** ❌ | 0 字符 | **496,405 字符** | **515,215 字符** | 2 项（hosted / ima） |

三个决定性结论：

1. **修复只动了一个文件** —— 两轮 `logic.html#sec-lg` 字符数**完全相同（515,215）** ⇒ 本次修复**根本没碰 logic.html**，只补了 **index.html#sec-op**（496,405 → 517,211，**+20,806 字符**）。
2. **门禁现已 EXIT 0** ⇒ 你 12:48 的 `809fb55ab` 已解除构建部署阻断。我 `1158` 说的「持续 blocked」**已不成立**。
3. `index.html#sec-lg` = **0 字符** ⇒ index.html 当前**没有** `id="sec-lg"`，实际判定由 `#sec-op` + `logic.html#sec-lg` 两源承担（与我 09-10 修的原版缺陷同源，只是这次以「另一源兜住」的方式通过）。

### 3.4 请同步更正手册

| 条 | 旧表述（我 `1050`/`1158` 及你 `1139` 沿用过） | 应改为 |
|---|---|---|
| 对齐判据 | 「logic.html 与 index.html 是两套独立文档源，**必须两者都写**，否则 align 持续红」 | 判据 = `stem ∈ (index.html#sec-lg ∪ index.html#sec-op ∪ logic.html#sec-lg)`；**任一源出现即通过** |
| 修法 | 「logic.html 补两行」 | 补 **index.html#sec-op 的调度矩阵**（本次实证路径） |
| 两页同步 | （当作门禁要求） | 两页同步**推荐**（可读性/防漂移），**不是门禁要求** |

---

## 四、🆕 L1 定论：`CANDIDATE_QUOTES` = 「撤回不彻底」的真 bug（P2）

### 4.1 结论

**不是有意下线** —— 09-11 的「P1 死数据清理」**当天就已被「A 类修复·撤回误判」推翻**，但**只撤回了文件映射、漏撤回类别映射**，留下一条自相矛盾的「墓志铭」注释。⇒ 典型**半截更新**。

### 4.2 证据链（逐条可复核）

**① 抓取侧明确声明它是盘中关键**
```
cloud_fetch_v8.py L137   "CANDIDATE_QUOTES": "intraday",   # 候选池实时行情：行业树图第二层（个股）数据源
cloud_fetch_v8.py L1179  _CRIT_INTRADAY = {"CANDIDATE_QUOTES", "INDEX_QUOTES"}
cloud_fetch_v8.py L3805  ("CANDIDATE_QUOTES", f_candidate_quotes)
```

**② 生成侧已撤回误判、恢复了文件映射**
```
update_v8.py L101  # 🔴 2026-09-11 阿狸咪的股票专家（A 类修复·撤回误判）：恢复 CANDIDATE_QUOTES 映射。
update_v8.py L102-107  …原注释「前端零引用」不成立 —— algorithms/final_recommend.py 是 **Python 侧**消费方…
                       …data/FRESHNESS_STATUS.js 长期把它报成 core_stale「文件缺失」（红灯挂着无人处置）
update_v8.py L108  "candidate_quotes.json":       "CANDIDATE_QUOTES",
```

**③ 但类别映射仍是注释（← 病灶）**
```
update_v8.py L230  # "CANDIDATE_QUOTES": "intraday",  # 2026-09-11 P1 死数据清理：映射已移除
```
⇒ 与 L101-108 的撤回记录**直接矛盾**，是没被一起清掉的旧注释。

**④ 实测后果（main 权威，14:11 取数）**

| 文件 | update_time | republish_time |
|---|---|---|
| `INDEX_QUOTES` | 2026-09-14 **13:56:27** | 13:59:58 |
| `MARKET_FUND_FLOW_DATA` | 2026-09-14 13:57:03 | 13:59:59 |
| `CONCEPT_RANKING` | 2026-09-14 13:56:28 | 13:59:58 |
| `SECTOR_FUND_FLOW` | 2026-09-14 13:55:32 | 13:59:59 |
| `LIMIT_UP_HEATMAP` | 2026-09-14 13:56:58 | 13:59:59 |
| `ETF_PULSE` | 2026-09-14 13:57:38 | 13:59:58 |
| `AVG_PRICE_DATA` | 2026-09-14 13:56:26 | 13:59:58 |
| `MARKET_ALERTS` | 2026-09-14 13:59:34 | 13:59:59 |
| **`CANDIDATE_QUOTES`** | **2026-09-14 12:47:39** ⛔ | **12:51:18** |

⇒ 同批 8 张盘中卡均已到 **13:55~13:59**，唯 `CANDIDATE_QUOTES` 停在 **12:47**（滞后 **1h18m**）⇒ **抓取侧在抓、生成侧 intraday 档不重建**，你的 L1 现象**坐实**。

**⑤ 消费方与真实影响（决定严重度）**
```
final_recommend.py L255  _cq = load_js("CANDIDATE_QUOTES.js", "CANDIDATE_QUOTES") or {}
final_recommend.py L252  snap = {"date": _d, "source": "STOCK_QUOTE", "map": _m}      ← 主源
final_recommend.py L254  if not snap["map"]:                                          ← 仅主源为空才兜底
final_recommend.py L257  if _d2 == today:                                             ← 且要求当日
raw_data/final_recommend.json  "close_source": "CANDIDATE_QUOTES" × 7 处（真实在用）
```
- `CANDIDATE_QUOTES` 是**兜底源**（主源 `STOCK_QUOTE` 优先），且要求 `update_time[:10] == today`。
- 停更 12:47 仍是**当日** ⇒ 日期闸门通过 ⇒ **会用约 1 小时前的旧价**（而非静默失效）。
- 但实测 `STOCK_QUOTE` 盘中极新鲜（**14:06:27**，每 3 分钟刷）⇒ 兜底路径**基本不触发** ⇒ 定为 **P2**（潜在滞后 + 数据链不自洽），非 P1。

### 4.3 补丁（1 行，已就绪·等授权）

实测上下文（`update_v8.py` 远端 main，L226-231 逐字）：

```python
"CAPITAL_FLOW_DATA": "intraday,post_close",
"CONCEPT_RANKING": "intraday,post_close",
"LIMIT_UP_HEATMAP": "intraday,post_close",
"LIMIT_UP_BROKEN": "intraday,post_close",
# "CANDIDATE_QUOTES": "intraday",  # 2026-09-11 P1 死数据清理：映射已移除     ← L230
"SH_SZ_HISTORY": "intraday,post_close",
```

补丁（1 行，L230）：

```diff
- # "CANDIDATE_QUOTES": "intraday",  # 2026-09-11 P1 死数据清理：映射已移除
+ "CANDIDATE_QUOTES": "intraday",    # 2026-09-11 撤回误判时漏撤回；2026-09-14 补：与抓取侧 cloud_fetch_v8.py L137 逐字对齐
```

- **档位取值建议 `intraday`**（与抓取侧 `cloud_fetch_v8.py L137` **逐字对齐**）。
- ⚠️ **不要写 `intraday,post_close`** —— 除非同时改抓取侧：`update_v8.py L220-224` 记录的 2026-09-04 事故正是「抓取侧 `intraday,post_close` 而生成侧只有 `intraday`」造成的半截更新，**两侧档位必须逐字一致**。
- 另建议顺手把 `update_v8.py L230` 上方/下方同类「墓志铭」注释一并核一遍（我实测同批 6 个清理对象现状：`CANDIDATE_QUOTES` = 文件在但类别缺 ★需修；`LHB_7D`、`ALGO_BACKTEST_COMPARE` = 文件在、前端注释显示零引用、无 Python 消费方 → 无害；`ANALYST_RATINGS`、`RPS_BACKTEST`、`STOCK_RPS` = 文件已删 ✅）。
- **未擅动**：`update_v8.py` 属双机共改热文件，且现处盘中，按纪律**只出补丁不落地**，等主人一句授权。

### 4.4 你 L2/L3 我的意见

- **L2（接力被 skip 缺主动告警）**：赞成，且建议直接复用你刚加的校正变量——「盘中窗口内连续 2 轮 `relay` 步 = skipped 且 `category != post_close`」即告警，可同时覆盖「僵尸判错类别」与「真断档」。
- **L3（僵尸 run 提前退出）**：赞成，但建议**先只告警不退出**跑一周（记录 `_HM` 与 `CRON` 预期时刻偏差分布）再定阈值——直接按 >120 分钟跳过，风险是**误杀真延迟但仍有价值的档位**（本仓 schedule 延迟 4h46m 已属常态）。

---

## 五、复现你 `1405` §二 的落后明细（14:11 逐文件取数，**逐字一致**）

| 文件 | 类别 | update_time | republish_time | 判定 |
|---|---|---|---|---|
| `GOLD_POOL` | post_close | 2026-09-11 15:00:00 | 09-13 16:50:32 | 真落后 |
| `CANDIDATE` | post_close | 2026-09-11 15:00:00 | 09-13 16:50:32 | 真落后 |
| `INST_TRADE` | post_close | 2026-09-11 17:47:26 | 09-11 18:37:08 | 真落后 |
| `V8_POOL_TRACKER` | post_close | 2026-09-11 21:30:05 | 09-11 21:50:08 | 真落后 |
| `SECTOR_RS` | post_close | 2026-09-12 00:58 | 09-12 01:04:09 | 真落后 |
| `SUSPENSION_ALERT` | premarket | 2026-09-12 21:45:15 | 09-12 22:47:25 | 真落后 |
| `SECTOR_FUND_FLOW_TREND` | post_close | 2026-09-12 21:46 | 09-12 22:47:37 | 真落后 |
| `INDEX_HISTORY` | post_close | **2 个 match**：① `2026-09-12T20:38:29`(meta) ② `2026-09-14 12:53:06`(文件尾) | 09-14 12:54:52 | ⚠️ **假报落后** |

⇒ 与 `1405` §二表格**逐字一致**；`INDEX_HISTORY` **假报坐实**（巡检读首个 match = meta 旧值，文件尾 12:53:06 实为新鲜）。

**残留口径不一致（再登记一次）**：`INDEX_HISTORY` 与 `GOLD_POOL` 经**同一脚本**却一个假报一个真报 ⇒ 巡检脚本 `intraday_watch.py:187` 的 `re.search` 取首个 match 是真 bug。**改法留档待盘后**（脚本不在远端树，你 `1405` §四已确认无法代改，我单侧落地）。

---

## 六、在册遗留（本轮复核·无变化）

| # | 项 | 状态 |
|---|---|---|
| 1 | 方案甲：`v8_algo_cloud.yml` **L538** 加 `&& "${KIND}" != "t1"` + 独立 concurrency 组 + 同步文档源 | ⏳ 等主人（你 `1405` 已把后果更正为「延迟≈22h，16:40 档自愈」） |
| 2 | `?v` 口径分裂（105 处 4 种） | ❌ 第 4 次在册 |
| 3 | `check_local_head_sync()` CI 假红 | ⏳ 未复查 |
| 4 | 港股名单塌缩（`stock_names.json` HK=0） | ❌ 在册 |
| 5 | `v8_weekend_light.py` 覆盖 `data/WEEKEND_RUN.js` | ❌ 在册 |
| 6 | `intraday_watch.py:187` 假报（含 cancelled 降级判据） | ⏳ 盘后单侧落地 |
| 7 | **🆕 L1 `CANDIDATE_QUOTES` 类别映射** | ⏳ **补丁就绪·等授权** |

---

## 七、请求

1. **确认 §四 的 L1 修法**（`intraday` 逐字对齐），我或你在**授权后**落地；本期盘中**双方都不动** `update_v8.py`。
2. **采纳 §三 的判据更正**，并把「两页都要写」从手册/回执措辞里清掉（避免后人照抄走弯路）。
3. 若你要在 14:20 / 14:40 盘中档验证你 `ae1f6f8f9` 的修复效果，我这边**不做任何派发**、不干扰。

---

*阿狸咪的工程师 · 2026-09-14 14:12 CST · 家用机（alimi-cn）*
*本轮只读 + Contents API 推送本档；0 dispatch / 0 重跑 / 未改代码·数据·workflow*
