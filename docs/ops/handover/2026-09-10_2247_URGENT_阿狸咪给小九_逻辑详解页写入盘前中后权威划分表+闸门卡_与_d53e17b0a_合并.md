# 🚨 URGENT｜阿狸咪 → 小九｜2026-09-10 22:47｜逻辑详解页写入「盘前/盘中/盘后 权威划分表 + 闸门卡」，与你的 d53e17b0a 合并

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 依据（主人原话）：「把这些都写到逻辑详解页对应位置去，算法链也要按这个执行，不是光写出来，要前端一一核对划分无误再落实到位才算成功。改成功后审计+改后三件套+交接清楚。」
> ⚠️ **本件不是通知，是防撞车对账单** —— 我俩在你 `d53e17b0a` 之后 20 分钟内同页并发编辑了 `logic.html`。

---

## 0. 一句话结论

主人给的三张图（① 盘前/盘中/盘后时段划分表 ② 时段主力与触发可靠性 ③ 盘后算法链 A→B→D→E 批次闸门）已**逐条实证后写入 `logic.html`**，共 5 张卡落在 2 个分区，**已上 main = `73e053358`（本地=远端）**。
**你的 `d53e17b0a` 我没覆盖** —— 它是我 HEAD 的祖先（`git merge-base --is-ancestor d53e17b0a HEAD` = YES），你 5 处独有内容全部保留，你已自行修好的 6 个锚点我 SKIP 了、保留你的版本。
**你明早只需做 1 件事**：看 §3 的 3 点确认 + 回 §4 的 3 问。**不需要重做。**

---

## 1. 我动了什么（全部为远端 `73e053358` 实测，非计划）

### 1.1 `logic.html` 新增 5 张卡（实测落位）

| 卡 | 落位 pane | 文件内偏移 | 出现次数 |
|---|---|---|---|
| 🗺️ 盘前 / 盘中 / 盘后 权威划分表 | `lg-op`（运维） | 133785 | 1 |
| 🩺 时段主力与触发可靠性（实测·诚实版） | `lg-op` | 138404 | 1 |
| 📋 派发源精简与假成功根治（**你的原文，逐字保留**） | `lg-op` | 142397 | 1 |
| 🚦 批次闸门：唯一决策点 | `lg-final` | 99186 | 1 |
| 🕒 盘后算法链当日时间线（2026-09-10 闸门版） | `lg-final` | 101202 | 1 |

**整卡替换（非叠加）**——旧标题残留实测 = 0：
- `⏰ v8 定时任务矩阵` → **0**
- `盘后算法链当日调度时间线` → **0**

### 1.2 我相对你的版本清掉的 3 处过时口径（`git diff d53e17b0a 73e053358 -- logic.html`）

| # | 位置 | 旧文（已删） | 新文（已上线） |
|---|---|---|---|
| ① | 最终推荐归属矩阵 **2 行** | `15:30 收盘后` / `云端 / cn 双机探针路由自动择一` | `盘后（闸门判 D 批）` / `算法链 D 汇总批 · [self-hosted, cn] 双机谁空谁跑` |
| ② | workflow 清单 `v8_algo_cloud` 标注 | `云端主链·分批跑` | `16:40 / 18:10 / 20:00 / 21:00 / 22:30 / 00:30 六个唤醒点` + `跑哪批由闸门定` + 执行机 `[self-hosted, cn] 双机谁空谁跑` |
| ③ | 分批明细/设计时序块 | `云端守卫自动派发`、`云端 run checkout main`、`A 16:40 → B 18:10 → D 20:00 → E 21:00` 硬编码时序 + `静默闸门自此开启` 等旧机制叙述 | 删除，交「批次闸门」卡与「闸门版时间线」卡统一表述 |

**说明性残留是对的，不要删**（我实测这 3 个词仍有出现，但全是「已删除」的告知语，非过时口径）：
`探针路由`=2（均带「勿再按该口径排查」）、`静默闸门`=2（均带「已删除/取代旧说法」）、`v8_freshness_watch`=5（均带「已删除/已下线」）。

### 1.3 2 个 workflow 注释更正（**只改注释，未改运行行为**）

**`.github/workflows/v8_algo_cloud.yml`**
- 顶部标题补全：`（A 采集 → B 选股 → D 汇总 → E 回测 · 批次由闸门判定）`
- 新增更正块（L3-6）：
  > 本链**现已跑在 `[self-hosted, cn]`**（小九 / 阿狸咪两台 cn 机谁空谁接），**不是** ubuntu-latest。
  > 文件名与历史注释里的「云端」是 2026-08 沿革名（当时确实跑 ubuntu-latest），**判执行机只看 `jobs.<job>.runs-on`**。
- L104 `runs-on` 行内注释：删除与代码不符的「固定到 lemoncat-cn 专属 label」（**代码里从未出现该 label**）→ 改为「这里用的是【**共享标签 cn**】= 双机谁空谁跑」，并记录其成因（家里机缺 bash/git）**已于 2026-09-09 08:20 消除**（Git 2.55 装到 `E:\Git`）。

**`.github/workflows/v8_cn_fetch_intraday_lemoncat.yml`**
- 顶部注释更正为「14 档 · 跑 GitHub `ubuntu-latest`」，并记录 09-10 实测 8 档迟到。
- 🔴 **修一处真 bug**：守卫「云端在途 >30min」分支，**原注释写「本档不派发」但代码没置 `GO=false`** → 实际仍会派发，与注释矛盾，且是「同一档叠第二个 pending」的隐患。已补齐（实测远端 `云端疑似卡死` 出现 2 次 = 注释 + 代码）：

```bash
else
  # 🔴 2026-09-10 阿狸咪修 bug：原注释写「本档不派发」但代码没置 GO=false → 实际仍会派发，
  #    与注释矛盾，且是「同一档叠第二个 pending」的隐患。补齐守卫，与注释/设计一致。
  GO=false; WHY="云端疑似卡死（run#$INFLIGHT_ID 已 ${AGE}min 未完成）→ 本档不派发，交 v8_cn_fetch_watchdog(>45min) 兜底"
  echo "::warning title=云端疑似卡死::run#$INFLIGHT_ID 已 ${AGE}min 未完成，本档不派发，交由 v8_cn_fetch_watchdog(>45min) 兜底"
fi
```

---

## 2. 🔴 防覆盖对账（我没覆盖你，逐条远端实测）

| 你的内容 | 远端实测次数 | 状态 |
|---|---|---|
| 派发源精简 | 2 | ✅ 保留（其 4 段原文我做成独立卡 `📋 派发源精简与假成功根治`） |
| landcheck | 2 | ✅ 保留 |
| xiaojiu | 2 | ✅ 保留 |
| cn-cn | 2 | ✅ 保留 |
| 交接触达通道 | 1 | ✅ 保留 |

**合并手法（关键，供你下次复用；我 rebase 硬解冲突连败 6 次后换的路子）**：

1. `git rebase --abort`（不硬解）
2. 保全双方版本到**仓库外**（`logic_mine_20260910.html` / `logic_theirs_20260910.html`）
3. `git reset --mixed FETCH_HEAD`（双方提交都留在 reflog，可回溯）
4. `git checkout FETCH_HEAD -- logic.html` → **以你的版本为基线**
5. 拿你版本做锚点探测 → 发现你已修好 6 处 → 这 6 个锚点改 `optional=True` **SKIP**（保留你的版本，不覆盖）
6. 把你的「派发源精简」块逐字做成独立卡保留
7. 基于你的版本重打补丁 → commit `b270e125a` → push 成 `73e053358`

> **结论：你的版本是基线，我的内容是叠加。** 这套「SKIP 而非覆盖」流程建议纳入我们的常规做法。

---

## 3. 请你看的 3 点（只需确认，不需重做）

1. **§1.3 那处真 bug 的修法**是否与你设计一致？（我的理解是「在途 >30min 疑似卡死 → 本档不派发，交 `v8_cn_fetch_watchdog` >45min 兜底」）
2. **我压缩 ⏰ 卡时有无漏你的条目**？你原 `⏰ v8 定时任务矩阵` 整块（16329 字符）我已保全在仓库外，需要我贴回随时说。
3. **共享标签抢单（这条最要紧）**：09-10 盘中 14 档有 **11 档落 alimi-cn**，违反「白天小九 / 夜间阿狸咪」双机分时铁律。根因在代码不在人：`runs-on: [self-hosted, cn]` 是**标签集合**，两机都注册了 `cn` → 谁空谁接。**要不要治？** 治法两条，你来拍（**因为盘中准点档是你的主力档**）：

   - **(a) 保住共享标签，靠闸门判陈旧度** —— 跑早了闸门也判 `NONE` 空转，不产生错数据（代价：白跑一次 run）
   - **(b) 给两机不同专属标签**（`xiaojiu` / `cn-cn` 已存在），盘中档钉死 `xiaojiu`，盘后档钉死谁都行（代价：对方机器空转时无法兜底）

---

## 4. 请回执（3 问）

1. 你机 `git` / `bash` 结论是否与我一致？（我实测：`git version 2.55.0.windows.3`，`E:\Git\cmd\git.exe` + `E:\Git\bin\git.exe`；你卡片 §3 标了「待小九确认」）
2. §3.3 盘中可靠调度器选 **(a)** 还是 **(b)**？
3. 我压缩 ⏰ 卡有无漏你的条目？

---

## 5. 铁律复述（防踩坚果云 / token / 撞车）

- 禁 `git add -A`；显式 `git add <具体路径>`；**编辑→提交→推送单命令原子完成**，中间不插分钟级脚本。
- 禁 `--force`；禁 `git rebase -X theirs`。
- token 禁写死仓库内任何文件（仓库 PUBLIC + 坚果云同步 → 云端有副本）。
- 🔴 **新增：同页并发编辑防撞**。`logic.html` 是热点文件，我俩 20 分钟内撞了 2 次（你 22:34 `d53e17b0a`，我 22:47）。规矩：**编辑前先 `git fetch` 看有无对方提交；撞了以对方版本为基线叠加，绝不 `-X theirs`。**
- 🔴 **判成败只认产物 `update_time`**，不看 run 的 success/failure（且凌晨按「归前一日 24:xx」双候选解释）。

— 阿狸咪（家机 alimi-cn，hostname=`Cat`）· 签发 2026-09-10 22:47 CST（北京时间实测）
