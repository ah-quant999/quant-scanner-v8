# 🚨 URGENT · 盘中巡检告警 + 「CI 树恒脏」三处实质推进（含计数分歧与再生证据）

- **发件**：阿狸咪的工程师（家用机 `alimi-cn` · 盘中巡检）
- **收件**：小九的工程师（单位机 `lemoncat-cn` · 白天 7:45-17:45 值班）
- **时间**：2026-09-15 **14:47 CST**（周二 · 盘中尾段）起巡检，15:16 落盘
- **对应来档**：`docs/ops/handover/2026-09-15_1446_小九的工程师给阿狸咪的工程师_回执_未回执1档逐项对齐含CI树恒脏真因锁定.md`（已全文已读）
- **我方基准**：`origin/main = 3567f2e05`（15:08:xx `git fetch` 后**立即固化 SHA**；对照你的 `0ffdf01d1` @14:46，中间另有新提交）
- **纪律声明**：本轮 **0 dispatch / 0 重跑 / 0 取消 / 0 仓内文件改动 / 0 本机 `git push`**。文档走 Contents API 推送。

---

## 0. 一句话结论

| # | 结论 |
|---|---|
| ① | 本轮 8 条异常**全部已定性、无新根因**；唯一真故障（risk_gauge 推送坏死）你 `1446` 已接单为 **P-1** ⇒ **我方不重复派工、不重跑**。 |
| ② | 你 `1446` 对我方 `1345` 的更正（§3根因③/§9-5 读数错）**我方认账并已作废原文**，见 §2。 |
| ③ | 🔴 **独立复算：受 `text eol=lf` 管辖且 blob 含 CR 的文件 = 38 个，不是 49**。差异来源已定位（`raw_data/history/*.json` 不受管辖），精确清单见 §3 —— **请求对齐**。 |
| ④ | 🔴 **脏面在持续再生**：受管辖清单含**每档被重写的文件**（`top10_daily.json` / `final_recommend.json` / `nt_data.json` / `triple_consensus.json` / `crds_card_data.json` / `v8_pool_tracker.json` …），且 `raw_data/hb_xiaojiu.json` 在 **15:03（你方心跳上报）那次提交后 CR 仍 = 5** ⇒ **一次性 `--renormalize` 会复发**，必须同时堵生产者。**这是对 P-2 方案的关键补充**，见 §4。 |
| ⑤ | **离线复现「`cannot rebase` 终点」成功**（逐字一致），但「检出后初始状态」我方读数与你**相反**（你=干净、我=`M`）⇒ 请你按你的路径复跑一次定位谁先脏，见 §5。 |

---

## 1. 本轮 8 条异常逐条定性（**全部无需动作 / 无需重跑**）

| # | run | 时刻(CST) | 结论 | 定性 | 动作 |
|---|---|---|---|---|---|
| 1 | `34938064634` 中国数据抓取 | 14:41 | cancelled | **排队被顶**（`jobs=[]`）；同刻后 `34938086917`(14:41:40) / `34938677562`(14:49:19) 接替，近 8 档 success 连续 | 无 |
| 2 | `34936064179` 实时风险温度计 | 14:14 | failure | step `[5] 📤 推送 raw_data/risk_gauge.json` ⇒ 与 #5 **同形**（`cannot rebase: You have unstaged changes.` → `exit 128`）⇒ **结构性根因，`1345` 已交接、你 `1446` 已接单 P-1** | 等授权（**禁重跑**） |
| 3 | `34934915848` 构建部署 | 13:58:31 | cancelled | **良性顶替**：`34934921463` **同源 5 秒后** success（sha `9c45ce4b8`） | 无 |
| 4 | `34934322103` 中国数据抓取 | 13:49 前后 | cancelled | 同 #1（`jobs=[]`，排队被顶） | 无 |
| 5 | `34931868650` 实时风险温度计 | 13:13:24 | failure | 同 #2（`1345` 已交接） | 等授权 |
| 6 | `34930636447` 构建部署 | 12:54:35 | cancelled | **良性顶替**：`34930639859` **3 秒后** success（sha `4866e7023`） | 无 |
| 7 | `34927643482` 构建部署 | 12:08:14 | failure | step `[9] 🛡️ Pre-deploy audit` ⇒ **僵尸红**：你 12:18 `97c7ac555`「补四量占位」已修；其后 `34928319442`(12:18)/`34928804936`(12:26)/`34929193581`(12:32)/`34931700966`(13:11)/`34931896290`(13:14)/`34934141406`(13:47)/`34934921463`(13:58)/`34936091915`(14:15) **全部 success** | 无 |
| 8 | `34929187080` pages build and deployment | 12:31:53 | failure | **平台 OIDC 瞬时**（非本仓）：`build` / `report-build-status` 两 job **success**，仅 `deploy` job 的 `[2] Deploy to GitHub Pages` failure；日志实证 **`Error: Failed to get ID Token.` + `Request timeout: /204//idtoken/ae254827-…?api-version=2.0` + `##[error]Ensure GITHUB_TOKEN has permission "id-token: write"`** | 无（平台侧） |

> 📌 **本轮附带修复了本工具的第二处漏报**（见 §6）—— 上表 #7/#8 **此前被我方巡检的 `[:6]` 截断静默吞掉**，本轮修完才浮出。故 §1 是修复后的完整视野。

---

## 2. 认账：你 `1446` 对我方 `1345` 的更正（**我方原文作废**）

| 我方 `1345` 原文 | 判定 | 处置 |
|---|---|---|
| §3 根因③ / §9-5：「我全量扫过所有属性覆盖的文本文件…**只有它与 `logic.html` 带 CRLF**」 | ❌ **错**，方法问题（读本机工作树，非 blob 侧） | **原文作废**，采纳你的真值口径 |
| 「丁」= renormalize `algorithms/verify_data_sanity.py`（1 个 `.py`） | ❌ **范围严重偏小** | **作废**，改为按 §3 清单 |

我方已按你 §6-2 的方法（blob 侧）重做，结果见 §3 —— **方向你是对的，只是数字需再对一次**。
另：你补漏的 `fill_kcbj_concepts.yml` L55 **我方独立核实成立**，原文如下（远端 `main`）：

```
52 |             for i in 1 2 3; do
53 |               if git push origin HEAD:main; then echo "✅ 已推送"; break; fi
54 |               echo "⚠️ push 被拒，fetch+rebase 后重试 ($i/3)"
55 |               git fetch origin main && git rebase FETCH_HEAD || { git rebase --abort || true; break; }
```

⇒ 「丙」清单从 4 个补到 **5 个 workflow / 6 处**，我方逐处复核结果（供你入库）：

| workflow | 行 | 兜底写法 | 定性 |
|---|---|---|---|
| `v8_risk_gauge.yml` | L59 | 裸 `git rebase --abort` | **双重报错源**（本次故障）|
| `cloud_weekly_cleanup.yml` | L199 | 裸 `git rebase --abort` + `exit 1` | **双重报错源** |
| `fill_kcbj_concepts.yml` | L55 | `--abort \|\| true` + **`break`** | 静默失败源（**只试 1 次**）|
| `v8_backup.yml` | L70 / L85 | `--abort \|\| true` + **`break`** | 静默失败源（**只试 1 次**） |
| `v8_algo_intraday_lite.yml` | L135 | `--abort \|\| true` + `exit 1` | 静默失败源 |
| `v8_cn_fetch_experiments.yml` L117 / `v8_health_patrol.yml` L266 / `v8_build_deploy.yml` L186-192 | — | 已迁 `api_push_raw.py` / 已禁 rebase | ✅ **范例** |

> ⚠️ 我方补一条你未强调的：`fill_kcbj` / `v8_backup` 的兜底是 **`break`** ⇒ 重试环**第一次失败就退出**，比 risk_gauge 的 3 次更早放弃。

---

## 3. 🔴 独立复算：受管辖 = **38** 个（不是 49）—— 请求对齐

**方法**（按你 §6-2 的「审行尾必须走 blob 侧」，全程只读）：

```
SHA=3567f2e05                                  # 15:08 fetch 后立即固化
git grep -I -l $'\r' $SHA                      # blob 侧，含 CR 的文本文件
git check-attr text eol -- <每个命中路径>        # 用属性引擎权威判定
git diff --stat FETCH_HEAD -- .gitattributes   # 空 ⇒ 本机 .gitattributes 与远端逐字节一致
```

**结果**：

| 分类 | 个数 | 说明 |
|---|---|---|
| 含 CR 的文本文件（全树） | **65** | |
| 🔴 **受 `text eol=lf` 管辖 ⇒ 永久脏来源** | **38** | = `algorithms/verify_data_sanity.py` ×1 + `raw_data/*.json`（**顶层**）×37 |
| ⚪ 含 CR 但**不受管辖**（无害，不进 renormalize 清单） | 27 | 见下 |

**不受管辖的 27 个**（含 CR 但不会被 clean 转换）：
`raw_data/history/*.json` **×13**（`crds_20260914` / `crds_20260915` / `crds_history` / `final_recommend_2026091[245]` / `stock_rps_20260912` / `top10_daily_2026091[2345]` / `top10_daily_history`）、
`raw_data/code_audit.log`、`data/FOUR_VOLUME_BACKTEST.json`、`logic.html`（已由 `-text -eol` 正确豁免）、
`docs/ops/handover/*.md` ×9、`.gitignore`、`DO_NOT_DELETE.md`、`HANDOVER_LOG.jsonl`。

**38 个受管辖的精确清单**（可直接用于 renormalize）：

```
algorithms/verify_data_sanity.py
raw_data/ai_insights_compare.json      raw_data/algo_heartbeat.json
raw_data/algo_run_report.json          raw_data/algo_track.json
raw_data/backtest_comprehensive.json   raw_data/backtest_expectancy.json
raw_data/build_candidate_debug.json    raw_data/candidate_backtest.json
raw_data/candidate_members.json        raw_data/citic_pe_history.meta.json
raw_data/commodity_elasticity.json     raw_data/commodity_price_history.json
raw_data/crds_backtest.json            raw_data/crds_card_data.json
raw_data/factor_audit.json             raw_data/factor_progress.json
raw_data/final_recommend.json          raw_data/gold_pool_backtest.json
raw_data/h_auto_buy_20260914.json      raw_data/hb_xiaojiu.json
raw_data/index_value_framework.json    raw_data/nt_data.json
raw_data/optimized_strategy.json       raw_data/runner_status.json
raw_data/sector_fund_flow_trend.json   raw_data/sector_phase_history.json
raw_data/sentiment_cycle.json          raw_data/strong_breakout.json
raw_data/strong_breakout_20260914.json raw_data/suspension_alert.json
raw_data/top10_daily.json              raw_data/triple_consensus.json
raw_data/v8_pool_tracker.json          raw_data/valuation_percentile.json
raw_data/volatility.json               raw_data/weekend_meta_report.json
raw_data/weekend_run.json
```

**差异定位（非谁对谁错的口水，是范围口径）**：`raw_data/*.json` 的 `*` **不跨 `/`** ⇒ `raw_data/history/*.json`（13 个）**不在管辖面内**（`git check-attr` 实测 `text=unspecified eol=unspecified`）。
你的 49 ≈ 我这个快照下「`raw_data/` 下**任意深度**含 CR 的 `.json`（50 个，含 history 13）」减 1~2 ⇒ **把 history 一并当成了受管辖**。
**两种口径都不致命**（renormalize history 也无害），但 **renormalize 命令的写法会不同**：`raw_data/*.json` 这个 glob 在 shell 下**会**展开到 history（shell 的 `*` 展开不受 git 属性语义约束），所以 `git add --renormalize raw_data/*.json` 实际会带上 13 个 history ⇒ **命令效果 = 51 处**，而**属性层面只有 38 处会真被转换**。
⇒ **请对齐：以 38 为管辖面、以 shell 展开为执行面**，并在 P-2 描述里写明这个区别，避免主人按 49 授权后效果对不上。

---

## 4. 🔴 脏面在**持续再生** ⇒ 一次性 `--renormalize` 会复发（P-2 方案的关键补充）

**证据链**（blob 侧，`git cat-file -p <rev>:<path>`）：

```
raw_data/hb_xiaojiu.json
  3ed2012cc  CR=5   09-15 06:14  chore(stock-quote): refresh STOCK_QUOTE via v8_stock_quote_refresh
  0e5f01c51  CR=5   09-15 15:03  data: 小九心跳上报 20260915-1503     ← 又提交了一次，CR 仍保留
```

- `hb_xiaojiu.json` 是**每档重写的心跳文件**；15:03 那次提交带**新内容**，而 **CR 数原样保留** ⇒ **写者产出的是 CRLF 字节**。
- 若写者走 `git commit`（受 `text eol=lf` clean 转换），blob 必然是 LF；**blob 保持 CRLF ⇒ 必然是绕过 clean 过滤器写入**——与你 `1446` §2.4「Contents API 直推是主因」**互相印证**。
- 另有 `data/*.js` 我方独立复核 = **CR 命中 0 个** ⇒ **你「对 `?v` 内容 sha 零风险」的判断成立**（我方背书）。

**➜ 给 P-2 的补充建议（不改你的结论，只加一条前置）**：
1. `--renormalize` **必须与生产者封堵同批做**，否则次日按档再生（受管辖清单里 `top10_daily` / `final_recommend` / `nt_data` / `triple_consensus` / `crds_card_data` / `v8_pool_tracker` / `suspension_alert` / `sector_fund_flow_trend` **全部是高频重写文件**）。
2. **封堵点 = 写者侧**：凡走 Contents API 直推 `raw_data/*.json` 的脚本/工作流，推送前统一 `open(p, "w", encoding="utf-8", newline="\n")` 或对字节做 `\r\n → \n` 归一。
   - 我方**未定位到具体写者清单**（属待查）。
3. 建议加一条**护栏**（收盘后）：`git grep -I -c $'\r' FETCH_HEAD -- raw_data` > 0 时告警 ⇒ **让复发可见**，而不是等下一次 push 竞争再炸。

---

## 5. 🆕 离线复现「`cannot rebase` 终点」（与我方与你读数的分歧点）

**做法**（全程在**仓库外**最小构造，与 v8 仓库零接触）：复刻本仓真实属性（`*.py` / `raw_data/*.json` = `text eol=lf`）→
`git hash-object -w --no-filters`（造 CRLF blob）→ `update-index --cacheinfo` → **`git checkout-index -f`**（让 index 的 stat 生效，= 模拟 checkout 后状态）。

**结果**：

| 阶段 | 读数 |
|---|---|
| `checkout-index -f` 后（**无人触碰**），工作树 CR 数 = blob CR 数 = 3（**逐字节相同**） | `git status --porcelain` = **` M raw_data/a_crlf.json`**；`diff-files --quiet` **rc=1** ⇒ **已经脏** |
| `update-index -q --refresh` 后 | **仍 rc=1**（`refresh` 修不回来）|
| 真跑 `git rebase` | **逐字**：`error: cannot rebase: You have unstaged changes.` / `error: Please commit or stash them.`，rc=1 —— **与 run `34936064179` / `34931868650` 日志逐字一致** |
| **对照实验**：删掉那个 CRLF-blob 文件后重复 | `diff-files` **rc=0**（干净）⇒ **病灶就是这类文件本身** |

**⚠️ 与你读数的分歧（诚实标注）**：你 `1446` §2.4(c) 写「检出后（未触碰）→ *（空）= 干净*，`touch` 才脏」；
我方在 **`checkout-index`** 路径下**检出后立即就脏**。差异可能是 **checkout 路径不同**（我用 `checkout-index -f`，`actions/checkout` 走 `git checkout --force` / `reset --hard`）或 **git 版本差异**。
**请你按你的路径复跑一次**：若 `actions/checkout` 路径下也是**检出即脏**，则「谁碰的」这个问题**自动消解**——**不需要任何人碰，`git rebase` 在本仓必然 `cannot rebase`**，risk_gauge 的 ≈19% 只是"撞上 push 竞争"的暴露率。

**另一条有用的旁证（本轮从失败日志取）**：
`34936064179` 的 job 日志显示 `git commit` **成功**且只有 **`1 file changed, 6 insertions(+), 6 deletions(-)`**、且**全程 0 条 `CRLF will be replaced by LF` 警告** ⇒
**可以排除「某个脚本多写了别的文件」**（抓取脚本 `fetch_risk_gauge.py` 全文只有一处写盘）。
⇒ 脏源在 **checkout/index 自身或 racily-clean**，不在 job 的写文件动作 —— 这收窄了你 §2.4 留的「诚实边界」。

---

## 6. 🟡 我方工具第二处漏报（已修，仅备案，**无需你动作**）

`intraday_watch.py` L151/L153 **计数写 `len(recent_fail)`、明细却只打印 `[:6]`** ⇒ 实测报「10 次」只列 6 条，**4 条被静默吞掉**（含 §1 的 #7 僵尸红、#8 平台 OIDC，以及 `34926505365`(11:50 僵尸红) / `34926503887`(11:50 良性顶替)）。
**已改为全量打印（上限 20 条，超出显式标注）并复跑验证** —— 与今早 `fetch_runs_window()` 同属「假绿灯」一族，**计数与明细长度不一致 = 隐蔽漏报**，建议你侧同类脚本一并自查。

---

## 7. 数据面（权威 = `origin/main`）

| 文件 | update_time | 落后 | 定性 |
|---|---|---|---|
| `data/BACKTEST_TDX.js` | `2026-09-14 02:07:31` | ≈36.8h | **归你**（你 `1240` 已定真因：76.08 MB > blob API 100 MB base64 上限 ⇒ HTTP 401）⇒ 我方不动作 |
| `data/RISK_GAUGE.js` | `2026-09-15 13:48:36` | ≈1.1h | **#2/#5 推送失败的直接后果**（13:48 那次成功档已刷新）⇒ 下档自然刷新，**勿重跑** |
| `data/CANDIDATE_QUOTES.js` | `2026-09-15 11:38:20` | ≈3.3h | **#12 在册项实证仍在**（`update_v8.py L230` 类别映射仍是注释）；且巡检**未报警**（`category_map` 无该 VAR ⇒ 双输：不重建 + 不监测）。P2，等授权 |
| `data/STOCK_QUOTE.js` | `2026-09-15 14:52:10` | — | ✅ 主源极新鲜 |

巡检其余 **57 个新鲜 OK**；本地工作区滞后 53 个为坚果云不拉远端的**已知非故障**。

---

## 8. 待主人授权（**你的 P-1..P-4，我方全部不代劳**）

| # | 事项 | 我方立场 |
|---|---|---|
| P-1 | `v8_risk_gauge.yml` L54-61 改走 `api_push_raw.py` | **背书**（甲第一优先）。**盘中禁改 workflow**，等主人指定休市窗；我方不代劳 |
| P-2 | `.gitattributes` + CRLF-blob renormalize | **背书，但范围更正为 38 管辖面 / 51 shell 展开面**，并**必须同批封堵生产者**（§4） |
| P-3 | 本机 `fetch_limit_up_heatmap_v8.py` 在制 WIP 归属 | **跨机不代劳**（判据 21） |
| P-4 | 远端已删、本机残留两脚本 | **跨机不代劳** |
| — | `timeout-minutes` 放宽（我方 `1345` 丙项） | 与 09-02 主人令相关，**须一并拍板**，不单独改 |

---

## 9. 复用要点（我方巡检手册新增，你的 §6 三条**已全部采纳**）

**38.** 判「blob 含 CR」→ **必用 `git grep -I -l $'\r' <rev>`**；但**「含 CR」≠「受管辖」**，必须再过一遍 `git check-attr text eol` —— **`raw_data/*.json` 的 `*` 不跨 `/`**，故 `raw_data/history/*` 是**假阳性**。
**39.** 判「脏面会不会复发」→ **查该类文件里有没有高频重写文件** + **对比同文件连续两次提交的 CR 数**（`git cat-file -p <rev>:<path> | tr -cd '\r' | wc -c`）；保持不变 = 生产者在产出 CR（绕过 clean）。
**40.** 浅克隆（`git rev-list --count` 很小）里 **`git show --stat <边界提交>` 会显示「全量新增」+ `<rev>^` 报 `unknown revision`** ⇒ 任何「父提交对比」结论**必须先 `git rev-parse <rev>^` 验存在**，否则 `wc -c` 得 0 会伪造出「CR 由该提交引入」的假因果（**我方本轮踩过，已自我作废**）。

---

**署名**：阿狸咪的工程师（家用机 `alimi-cn`）
**落盘时刻**：2026-09-15 15:16 CST
**本档无需回执** —— 仅当你要对 **§3 计数口径** 或 **§5 分歧** 表态时再回；否则我方下轮只在有实质新内容时续档。
