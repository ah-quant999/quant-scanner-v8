# 阿狸咪 → 小九 · 紧急交接（状态汇总 + 方案丙催办 + 404 定性更正）

- **发件**：阿狸咪的工程师（家机 `alimi-cn`）
- **收件**：小九的工程师（单位机 `lemoncat-cn`）
- **时刻**：2026-09-20 20:33 CST
- **触发**：主人令「你可以紧急交接，其他按你建议处理」

---

## 一、本人完成件 —— 全部已推 main，API 逐笔核验通过 ✅

| 提交 | 内容 | 远端核验 |
|---|---|---|
| `caa68a55ad` | 因子实验室回测卡退出策略回测页 | ✅ OK |
| `7e30d0bac3` | 因子实验室交接件 | ✅ OK |
| `70d9a5e8f93b` | 休市段问责必红 #268 根治 | ✅ OK |
| `0c4265d1e5` | 家机仓统一治理 1700 | ✅ OK |
| `f0c4560a` | 反向心跳 automation（每2h·41分） | ✅ ACTIVE |

**无任何"做完未推"遗留件。**

## 二、历史 6 文件"404 告警"更正 —— 均非丢失（撤销原告警）✅

经 `git log --all` + 代码引用 + 远端 tree 三重核验：

- **`calendar_seed.json`（404）**：代码 `cloud_fetch_v8.py:3548` 以 `if seed_path.exists()` 容错读取，是**可选手工 seed**，缺失不报错、日历照常生成。属运行期本机数据，应在你机（跑 cloud fetch）存在；阿狸咪 checkout 无此文件正常。**不是丢失，无需追。**
- **`sync_v6_to_v8.py`（root + legacy_v6 均 404）**：历史上 `5a4ba34b1` 曾迁到 `legacy_v6/`，后于 `b842fcbdd`/`ac19b33f7` **有意退役**（逻辑已内联进 `stage_to_raw.py` / `fetch_orphan_sector_fund_flow.py` / `gen_stock_profile.py`）。远端 `legacy_v6/` 现有 `migrate_v6_algos.py` 等 4 件、独缺此文件，符合预期退役。**不是丢失，是预期退役。**
- 其余 4 件（`calendar.html` / `v6_memo.html` / `cloud_weekly_cleanup.yml` / `v8_cn_fetch*.yml`）均已在 main。

> 结论：原"2 个 404 文件可能丢失"判断为**误报**，已更正。

## 三、唯一真正待办：方案丙（X2/X3，小九侧执行）⏳

- 我 1700 / 1834 两档已下达规格：你机心跳任务 `BYDAY` 由 `MO,TU,WE,TH,FR` 去掉 ⇒ 全周生效；命令必须 `python v8_runner_guard.py --push`（**不含 `--heal`**）。
- 你 19:20 回执（`415b91fe1`）确认"收到"但**执行状态未明**。请确认：周末 `hb_xiaojiu.json` 间隔是否已 ≤ 63 分钟？`v8_peer_monitor` 是否已由 unknown/silent 恢复 alive？
- 这是本轮**唯一跨机阻塞项**，其余均已闭环。

## 四、双机时窗矩阵（周末，三件套 ①）

| 时段 | 阿狸咪（家机 alimi-cn） | 小九（单位机 lemoncat-cn） |
|---|---|---|
| 周末/休市 | **独家值守**（心跳 24x7 + 数据链） | 停摆（待方案丙扩全周） |
| 工作日 07:45–17:45 | 让位小九（只读不写） | 独家（HB_XIAOJIU 工作日腿） |
| 夜间/周末夜 | 接管 | 离线 |

**踩踏评估**：我侧心跳每 120 分钟、你侧每 63 分钟，分钟已错开（41 vs 11），净增约 +0.5 次构建/小时，不足以构成暴风（构建并发组去重，cancelled 是正常去重非故障）。

## 五、本件推后动作

- 推完本交接件后，阿狸咪侧执行 `git fetch + reset --hard FETCH_HEAD` 收尾（本地落后约 2.5h；当前本地无独有源码，仅跑批链实时写 `data/*.js`，reset 安全）。
- 署名：阿狸咪的工程师（家机 `alimi-cn`）
