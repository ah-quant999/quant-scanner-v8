# 交接 · 观测平台渲染异常「可见降级」（方案3）

- **作者**：阿狸咪的工程师（家用机 alimi-cn）
- **时间**：2026-09-16 04:20 CST
- **提交**：`999ca6f6994890c615612a72b367e9987164aa33`（父 `a6788020`，Git Data API 原子推，force=False）
- **涉及文件**：`index.html`（仅此一个）
- **性质**：防御性加固，**正常路径零行为变化**

## 1. 背景（主人令）

9/15 22:xx 主人报「大盘观测页还空着」，上一提交 `803d6dfe` 已修根因（`__renderObsPlatform` 拼串 K型卡少一个 `</div>`
⇒ `obsPaneMacro` 未闭合 ⇒ `obsPaneMarket` 被嵌套 ⇒ 切页时外层 `display:none` 连带隐藏）。

主人随后拍板：对斐波那契卡越界问题采取 **方案1 + 方案3 组合**：

| 方案 | 内容 | 状态 |
|---|---|---|
| 1 | fib `_c` 提升到 `if` 外 + `||{}` 兜底 | ✅ 已于 `803d6dfe` 落地 |
| 2 | 口径行移进 `if` 分支 | 未采纳（改动面大） |
| 3 | **给 `__obsRenderPane` 的 THROW 加可见降级文案** | ✅ 本次落地 |

## 2. 本次改动（`__obsRenderPane` 内，共 4 处）

1. `var out = [];` 之后新增 —— 降级映射 `__obsFB`（渲染器 → [容器id候选…, 卡片显示名]）+ 显示函数 `__obsFbShow(k, kind, msg)`。
2. 宏观分支 `_fillMahoroCard`：`catch` 与 `nofn` 分支调 `__obsFbShow(...)`。
3. 宏观分支 `__renderAiInsightsCompare`：同上。
4. 大盘 4 卡循环：`catch(e)` / `else(nofn)` 调 `__obsFbShow(...)`（原实现只 `out.push('k:THROW')` 进 console）。

覆盖 6 个渲染器：`__renderElliottWave`(elliottWaveInner) / `__renderFibWindow`(fibWindowBody) /
`__renderCiticPeObserve`(citicPeObserveBody) / `__renderIndexObserve`(indexObserveBody) /
`_fillMahoroCard`(maharoCard) / `__renderAiInsightsCompare`(aiInsightsCompareBody)。

**降级表现**（写进该卡自己的容器，其余卡不受影响）：
- 抛错 → `⚠ <卡名> 渲染异常，已降级（其余卡片不受影响）` + 错误 message（已 HTML 转义、截断 160 字）+ **北京时间**（`Asia/Shanghai`，与全站中国时间口径一致）
- 函数缺失 → `⏳ <卡名> 渲染函数未就绪（脚本尚未加载完成），将随自动刷新重试`

## 3. 与你的自愈机制的关系（重要，勿误判）

- 页面原有 `setInterval` 自愈会周期性重调 `__obsRenderPane` ⇒ **降级文案是「临时态」**：
  数据到位后下一轮自动被真实内容覆盖，**无需人工干预**。
- 因此：日志里若出现 `xx:THROW`，现在**同时**会在页面上看到黄色降级文案 —— 这是**预期**，
  它的意义是「不再静默」，不是新增故障。
- 若你后续重构 `__obsRenderPane`，**请保留 `__obsFbShow` 的调用点**；`__obsFB` 的容器 id
  与卡名如随卡片改名而变，请同步更新该映射（改错只会退化成「无降级显示」，不会报错）。

## 4. 验证证据

| 层 | 手段 | 结果 |
|---|---|---|
| 语法 | 28 块内联 `<script>` 逐块 `new vm.Script` | **0 错** |
| 行为 | Node + stub DOM 4 场景（抛错/缺失/正常/无残留） | **全过** |
| 线上回归 | playwright(Edge headless) 打 CDN | 两 pane 均为 obsHost 直子级；大盘观测 h=1677px；4 卡 14146/3985/6736/5094 B；**正常路径零降级残留** |
| 线上 E2E | 在真实页面把 `__renderFibWindow` 换成抛错版后调 `__obsRenderPane` | 卡内出现 `⚠ 🧬 斐波那契F观测窗 渲染异常，已降级…E2E降级自测 · 04:12:12` ✅ |
| 线上 E2E | 删掉 `__renderCiticPeObserve` 再调 | 卡内出现 `⏳ 🌡 中信 PE 观测 渲染函数未就绪…` ✅ |
| 存活 | 推后 tip 前进 39 提交（含多次 v8 build） | 我的提交为 `merge_base`（behind_by=0）；当前 index.html 与我的 staged 仅差 **102 行**，全部为 CI 注入的 BUILD SHA / `?v=` 缓存戳 |

## 5. 其它

- 加载期曾捕获 1 条 404，**专测未复现** ⇒ 判定 CDN 瞬时抖动，非缺失文件。
- 本轮**全程未碰本地仓库工作区**（不 `git add`、不 `git fetch`），推送走 Git Data API。
- 顺手复核：`obsGuard`（上轮护栏）与「方案1」标记在当前 tip 中均存活，未被后续 build 冲掉。
