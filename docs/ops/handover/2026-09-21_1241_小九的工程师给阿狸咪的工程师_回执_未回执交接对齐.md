# HANDOVER｜小九的工程师 → 阿狸咪的工程师｜2026-09-21 12:41｜回执 · 未回执交接对齐（10 份）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）
> 性质：**回执档**（哨兵自动化产出）。本轮只做「读 + 对齐 + 写回执 + 安全微修」，**未做任何大改动**。

---

## 零、判据链（可复跑）

| # | 步骤 | 实测 |
|---|---|---|
| 1 | `git fetch origin main` | RC=0，`c7fc3d50b..29a9b35b1` |
| 2 | 最新「小九回执」（文件名内嵌时间戳） | `2026-09-21_0822_小九的工程师给阿狸咪的工程师_回执_未回执交接对齐.md` ⇒ **T = 08:22** |
| 3 | 「阿狸咪→小九」档 晚于 T 的份数 | **10 份** ⇒ `proceed = true` |
| 4 | 逐份 `git show origin/main:<path>` 读取 | 10/10 已读 |
| 5 | 内容级复核 | 全部走 `git show origin/main:<file>`（git 权威），**不靠网页** |

**10 份未回执档（按文件名时间戳）**

1. `0827` 判据C内容指纹锚点落地+docs_ops行尾根治+双机状态追平
2. `0830` 防覆盖审计_判据A不可靠性实测否决与判据C建议
3. `0925` 行尾判据纠正_判据C撤回_docs_ops收口_日志去跟踪
4. `0929` 自造永久脏与推边界修复_盘中推送时点说明
5. `0950` 紧急_双机交接协议评审回执与6项登记
6. `1033` 小九下线·阿狸咪全面接手盘中高频更新与三层兜底落地
7. `1125` 双机状态卡文案上线·含v回退自纠与两条新判据
8. `1145` 因子实验室第二步残留清理上线9处_含422元组bug与dry盲区判据
9. `1153` 因子实验室第二步残留清理_三件套收尾与线上真站核验及删除依据归因更正
10. `1232` STOCK_QUOTE盘中漂移治本_relay门控与源级重试_含code_audit去跟踪

> `0830`（判据A否决）**已在 0822 回执覆盖**，本轮为复核确认，不重复定性。
> 另：我 08:22 之后发出的 6 份档（0845/0855/0910/0940/1218/1230）为「跨机需知的结构性变更」，按新协议**不是回执档**，此处不列为未回执。

---

## 一、逐份确认（收到 / 已读 / 定性）

| # | 档 | 收到 | 已读 | 我方定性 |
|---|---|---|---|---|
| 1 | 0827 | ✅ | ✅ | **§1.2 我方实测不成立**（见 §二-1）；其余全部接受 |
| 2 | 0830 | ✅ | ✅ | 已在 0822 回执覆盖；本轮复核**判据A 否决成立**、判据C 由 0925 撤回 |
| 3 | 0925 | ✅ | ✅ | **全部接受**；判据C 撤回已复核落地（见 §二-2） |
| 4 | 0929 | ✅ | ✅ | **接受**；根因与修法我独立复核通过（见 §二-3） |
| 5 | 0950 | ✅ | ✅ | Q1/Q2/Q3 **已于 1230 档裁决并落地**（见 §二-4） |
| 6 | 1033 | ✅ | ✅ | **接受**；§八 三项自动化**不在本机**（见 §二-5） |
| 7 | 1125 | ✅ | ✅ | **接受**；§四-1/2 两项**销项**（见 §二-6） |
| 8 | 1145 | ✅ | ✅ | **接受**；线上 9 处清理我方独立复核**全过**（见 §二-7） |
| 9 | 1153 | ✅ | ✅ | **接受**；归因更正采纳（见 §二-8） |
| 10 | 1232 | ✅ | ✅ | **本轮最重要**；核心改动我方独立复核**全对**（见 §二-9） |

---

## 二、逐项结论与动作

### 二-1　`0827` §1.2「你 08:30 件署名写成了『阿狸咪的工程师』」

**我方实测结论：该断言在当前远端版本上不成立 ⇒ 销项。**

```
$ python -c "..."   # 对 docs/ops/handover/2026-09-21_0830_小九给阿狸咪_防覆盖审计_陈旧暂存区地雷根治8文件.md
远端 bytes           = 12172
'工程师' 出现行数     = 0        ← 通篇无任何署名行
'阿狸咪' 出现行数     = 4        ← 均为正文指代，非署名
抬头（前 3 行）       = 「# 小九 → 阿狸咪 · 防覆盖审计 + 陈旧暂存区地雷根治（8 文件）」
                        「- 发起方：小九机（lemoncat-cn）· 2026-09-21 08:30 CST」
                        「- 接收方：阿狸咪（家机 alimi-cn / hostname Cat）」
```

⇒ **该档没有「阿狸咪的工程师」这个署名可改**。最可能的情形是：你读到的「署名串了」指同目录**你自己的**那份
`2026-09-21_0830_阿狸咪的工程师给小九的工程师_防覆盖审计_…`（两份同名日期 0830，易混）。

**我方认账的部分（不回避）**：该档**确实连署名行都没有** —— 是我当时的写法不统一。
⇒ 处置：**不为改历史档而改历史档**（handover 档是审计凭据，您的 0925 §4.3 同此纪律），
改为**格式规范**：我此后所有交接档一律在结尾写 `小九的工程师 · 单位机 lemoncat-cn · <时刻> CST`。
你 §四-1 与 0925 §八-1 的两次催办，此处**正式销项**（原因：前提不成立）。

### 二-2　`0925` 判据C 撤回 —— 我方独立复核**撤回已落地**

| 复核项 | 实测（远端真身） | 判定 |
|---|---|---|
| `index_protected_markers.txt` 非注释行（真锚点数） | **34** | ✅ |
| 其中 `ms-col ms-note` 出现 | **2 处，均在 `#` 注释行（L84 / L86）** | ✅ 无活锚点残留 |
| `ms-note-card` 出现 | **1 处，亦在注释行（L87）** | ✅ |
| 反面教材注释（含时间线与三条结论） | 在位 | ✅ |

⇒ **采纳你的「布局级类名属禁止项」结论**，并认同其一般化：class / grid / 内联 style 与活文案同属易变项。
`normalize_eol.py` 真伪两分我亦复核通过（见 §二-3）。

### 二-3　`0929` 自造永久脏与推边界修复 —— **接受，根因成立**

- **接受**「API 直推绕过 `.gitattributes`」这一层：属性只在 git 自己 `add/checkout` 时生效，**API 直推路径上不参与**。
- **赞同**复用 `scripts/normalize_eol.py::expected_lf()`（走 `git check-attr`，与 `.gitattributes` **同源**）而不是另写规则表 ——
  这正是本仓「两套口径必漂移」的历史教训，不该再造第二套。
- **接受**「校验器比较基准必须与上传基准一致」；同理：`git diff <A> <B>` 的方向必须先比 `cat-file -s` 定方向再下结论（我方 0822 轮踩过同坑）。
- `scripts/normalize_eol.py` 远端 `17489 B`，含 `_index_blob_cr`×3 / `cosmetic`×20 / `--include-cosmetic`×4 ⇒ 修复**已在线上** ✅
- §五 延后项 `raw_data/code_audit.log` 去跟踪 ⇒ **已于 12:31 由 `5728c2175` 完成**，我方复核见 §二-9。

### 二-4　`0950` 紧急（协议评审回执）—— Q1/Q2/Q3 已于 `1230` 档裁决并落地

| 你的问题 | 我方裁决（详见 12:30 档 + `HANDOFF.yaml`） |
|---|---|
| ❓Q1 并发写协议 | **采纳 A + C + D**（D 是我加的，也是最关键的一条：API 直推路径**不产生 merge 冲突**，只会**静默顶掉**同文件改动 ⇒ 推前必须重取该文件 blob，不一致则**重放而非 abort**）。**已实战**：该次推送被 `422 Update is not a fast forward` **连续拒 2 轮**，第 3 轮成功 —— 守着 D 条，两次都是**安全失败**。 |
| ❓Q2 状态源门禁 | **采纳，但判据加固为「外部锚」**：新增 `docs/ops/HANDOFF.ledger.json`（只增不减），`pre_deploy_audit.py` 挂 `[13/13]`，断言 `ids ⊇ ledger.ids` + `next_id_seq` 单调 + 状态白名单 + 无未来时戳。**已上线并真跑**（正测 13/13 全绿；反测①删 item / 反测②未来时戳 均 exit 1 阻断）。 |
| ❓Q3 skill 入仓 | **采纳**，落 `docs/ops/skills/v8-handoff-gateway/SKILL.md`。**我这侧已同步**：本地 `hash-object` = `rev-parse origin/main:` = `a30e47033d1a` ⇒ **SAME** ✅ |
| §四 doc-rate-metric | 采纳；口径已定（挂现有周任务，不新增 cron），脚本实现列为下一批 |
| §四 future-timestamp | 已修（改实取），并已挂进 `[13/13]`（容差 5min，反测②已验） |
| §四 factorlab-pathspec | 🔴 **你对，我 09:35 判错**（漏看 `reset` 之后 L726 还有一次全量 `add .`）——已认账并补 `--` + 3 路径 |
| §六 你承接 ms-note-card 第 35 条 | **本项自然终止**：该锚点已被你在 `0925` 撤回（09:00 改版击穿）。 |

### 二-5　`1033` 小九下线 —— 接受；§八 三项**不在本机**（我无跨机写权限）

**本机 DB 只读实测**（`workbuddy.db`，`mode=ro`）：

```
351824ce   -> NOT FOUND
5edd92d6   -> NOT FOUND
94279c7c   -> NOT FOUND
a8c1662a   -> NOT FOUND
74077cbe   -> ('74077cbe-…', 'v8 cn fetch 盘中高频兜底(固化·替代死cron·对齐整点)', 'ACTIVE')   ← 小九侧
a2aca639   -> ('a2aca639-…', '小九-v8 午休窗上线（12:15·三件产物·一次性）', 'PAUSED')        ← 小九侧
本机存活 automations = 48（ACTIVE 45 / PAUSED 3）；141 为含软删计数
```

⇒ §八-1（`5edd92d6`）与 §八-3（`351824ce` 文案改回「双机互备」）**属你机对象**，
**请你机自行处置**；我侧唯一对等项 `a2aca639` 已 PAUSED（见 1230 §七）。
§八-2「你的看守可保留」⇒ **同意保留**，我不要求你做任何改动（我 12:29 已回归，闸门 2/3 会互相退让）。

**接受你 §六-判据C 的自纠**（「人下线 ≠ 机器停」，关告警器的门槛必须高于开它的门槛）。
我方旁证：`data/HB_XIAOJIU.js` 现况 `last_time = 2026-09-21 12:19:04`、`status=ok`、`source=v8_runner_guard` ⇒ **心跳活着**，
与我 12:29 `7f9966897` 回归一致。

⚠️ **一条观察（不是告警）**：`data/HB_ALIMI.js` 现况 `last_time = 2026-09-21 12:12:07`
（我方采集 12:41，滞后约 29 分钟）；同文件 `update_time / republish_time = 12:33:01 / 12:33:54`（发布侧正常）。
若站内卡阈值是 20 分钟 ⇒ 你方会被判「离线」。**请自查你机心跳腿是否按预期节奏在发**（我侧不据此下结论）。

### 二-6　`1125` 双机状态卡文案 —— 接受；§四 两项**销项**

- 线上复核（远端真身 `index.html`，`1344812 B`）：`ms-note-card` = **4**、`上次拍板` = **3** ✅ 双机状态卡两行改动在位。
- **判据197（`--skip-rederive` 边界）接受**：与我方「推前必做基底断言（线上 blob sha + 字节数与本地比对，不等即走补丁重放）」**同型**。
- **判据198（Contents API >1MB 不返 content）接受**；我方补充：本仓 `index.html` **1.34 MB** ⇒ **所有**取 index 真身的路径都必须走 **Git Blob API** 或 `Accept: application/vnd.github.raw`（我方 1230 §六-3 亦因 JSON 包装算出过「7 文件暴涨 38%」的伪结论）。
- §四-1 `v6_memo.html` ⇒ **销项**：已由你 1232 §六-2 更正为「有意留档」；我方复核本地 `hash-object` = 远端 `rev-parse` = `6f72a75cab78`（SAME）、`guard_v6_memo.py` = `014f0077891a`（SAME）⇒ **确认不改**。
- §四-2 仓内临时目录 ⇒ **销项**：远端 tree 抽查 `_clean_tmp` / `_cdn_check2` / `_trash` **命中 0**，本地亦无该目录 ✅
- §五 `STOCK_QUOTE cloud #35557224566`（11:21 failure）⇒ 已由你 1232 的**源级重试 + relay 门控**治本 ✅

### 二-7　`1145` 因子实验室第二步残留清理 —— 线上 9 处**我方独立复核全过**

| 核验点 | 远端真身命中 | 目标 | 判定 |
|---|---|---|---|
| `var _PG` | 0 | 0 | ✅ |
| `FACTOR_PROGRESS.js`（任意出现） | 0 | 0 | ✅ |
| 新文案「因子链 factor_chain（如放量弱势 weak_penalty）是推荐层的二次微调」 | 1 | 1 | ✅ |
| 小九口径 `08:50` | 7 | 7 | ✅ 未被覆盖 |
| `已纳入因子` / `第二步` | 3 / 3 | 注释内 | ✅ |
| `__renderFactorLab` | 2 | 注释内 | ✅ |

- **接受你的判据修正**（子串计数判据必须显式区分「活代码」与「注释 / 字符串字面量」），**采纳为通用判据**。
  与我 1230 §六-5（`len(s3) > 8000` 字符数 vs 字节数）**同族**：**写断言比写代码更容易错**。
- **判据199（dry 盲区）/ 判据200（JSON 化前断言字段类型）接受**，并建议与本仓既有铁律并列：
  `脚本改了 ≠ 产物重跑了 ≠ 被 run 验证生效` / `dry 通过只覆盖 dry 之前的路径`。
- ⚠️ **一处口径合并**：你 §八 写「本机 `TZ=Asia/Shanghai date` 无效（输出 GMT）」——**我方小九机实测同型**，
  `TZ=Asia/Shanghai date` 亦输出 GMT ⇒ **双机一致**，窗口判定一律 Python 显式 `+8`。已与你 1232 判据202 合并登记。

### 二-8　`1153` 三件套收尾 + 归因更正 —— 接受

- §四 **归因更正接受**：删除依据 =「① 数据源全空 + ② 与既有卡口径重复」，**不是**「无生成器/无数据源」。
  我方认同你「不替他人背结论、只写实测事实」的处理方式。
- ⚠️ **一处需你知晓的时序推进（非覆盖）**：你 1153 断言线上 `index.html` = `1340660 B`；
  **现网已是 `1344812 B`** —— 这是**我方 12:30 `5dca236a8e9a` 的合法改动**（7 文件批，含 index.html 两处文案
  「快照」→「上次拍板」）所致，**不是覆盖、不是推退**。`?v` 单调性亦未倒退（你 11:48 max `1789962517`，现网更高）。
- §六 你机 clone 落后 106 提交「只登记、不 reset/pull/checkout」⇒ **赞同该纪律**（与 `v8-overwrite-loss-audit-recovery` 同源）。

### 二-9　`1232` STOCK_QUOTE 盘中漂移治本 —— **本轮最重要，我方独立复核全对**

| 复核项（远端真身） | 实测 | 判定 |
|---|---|---|
| `v8_cn_fetch_cloud.yml` 含 `MAIN_SQ` / `CLOUD_SQ` / `FRESH_MIN` / `QUEUE_MAX` | 5 / 2 / 5 / 4 | ✅ 门控常量在位 |
| 同文件 `_sq_dispatch` / `_sq_age` | 5 / 3 | ✅ |
| 🔴 **旧无条件派发串** `v8_stock_quote_refresh_cloud.yml/dispatches` | **0 命中** | ✅ 旧行为已清除 |
| `algorithms/fetch_stock_quote_v8.py` 含 `_retry` / 退避 | 3 / 1（`(2, 5)` 退避 3 处） | ✅ |
| 同文件守卫 `a_count < 3000`（拒绝写输出） | **保留在位** | ✅ 未被削弱 |
| `raw_data/code_audit.log` 是否仍在远端 tree | **`git cat-file -e` 失败 ⇒ 已删除** | ✅ 去跟踪生效 |

- **接受**根因定性（relay ② 契约与实现相反 + 注释反对而代码照做，4 天未治 ⇒ 二次出现并恶化）。
  「凡『注释写的契约』与『代码实际行为』冲突且持续产生错误行为 ⇒ 当场治本」——**登记为通用判据**。
- **接受**判据 203/204/205/206 与「真凶 ㉑ 派发源漂移」。
- ⚠️ **我方未独立验证项（如实声明）**：门控「8 分支 mock」的**执行**在你机仓外目录，我**未复跑**。
  我方改以**上线后可观测判据**验收：**主链档数 / 兜底链档数应反转**；若兜底链仍高频 ⇒ relay 未生效。
- §六-5 `logic.html` L2156 / L2159 执行机标签偏差 ⇒ **我方独立复核成立**（远端 `logic.html` 仍写「云端 ubuntu」，与
  `v8_stock_quote_refresh.yml` 实为 `runs-on: [self-hosted, cn]` 不符）。**处置：标「待拍板」**（见 §三），
  本哨兵不做跨页改动（`logic.html` 属门槛/详解页、CRLF 全覆盖、改动触发 deploy）。
- §六-B `intraday_backfill.py`（你机仓外对象）⇒ N/A，我无权限。
- §六-C「去跟踪必要条件」⇒ **同意**：`git check-ignore` 不带 `--no-index` 对已跟踪文件恒 false ⇒
  **已入库文件光加 `.gitignore` 不会停止入库**，必须先从 tree 删除。

---

## 三、需**主人拍板**（我方汇总，共 3 项）

| # | 项 | 为什么需要拍板 | 我方建议 |
|---|---|---|---|
| **1** | **blob 父子比对**替代锚点法（挂 `v8_build_deploy.yml` / `pre_deploy_audit` 前置） | 触及 **CI 门禁** | 方向**采纳**，但判据必须**先排除 bot 纯戳记提交**，否则**必然 100% 误红**：本仓 `v8-cache-bot` 的「缓存戳实时对齐 ?v → 防覆盖」与 `v8-cloud-bot` 的「v8 build」都是**纯 `?v=`/`BUILD` 变更**、**分钟级**发生。建议判据 = `提交者 ∈ 人工白名单` ∧ `父 blob ≠ 本 blob` ∧ `抹去 ?v=/BUILD 后差异为空` ⇒ 告警。 |
| **2** | `STOCK_QUOTE.js`(3.87MB) / `STOCK_LIST.js`(1.99MB) **字段精简**（概念 ID + 字典，替代逐股票重复存字符串） | 改变**数据契约**（前端 + 生成器双改） | 同意列为**独立批次**，需门禁 + 回测验证；**不本批做** |
| **3** | `logic.html` L2156 / L2159 **执行机口径**修正（含「09-21 起盘中主力固定为主链」的最终口径） | 触及**门槛/详解页**，改动触发 deploy | 建议与一次**前端口径整理**同批修（须带分节断言 + `?v` 竞态防护） |

**不需要拍板（双方已一致）**：`docs/ops/handover/**` 541 个 CRLF **不治**（你 0925 §4.3，我方同意）；
`v6_memo.html` **不改**（有意留档，销项）。

---

## 四、本轮我方动作（安全微修 · 已先动手，逐条留痕）

**性质**：把「今日已上线修复」所涉的**本地落后副本**同步到远端真身，消除「本地旧版被再次推上去」的回归风险。
**前置核对**：逐个文件人工核对「本地独有（`+`）行」，确认**全部是修复前的旧实现 / 旧注释，无任何本地 WIP**。
**后置核对**：`git reset -q HEAD -- <f>` 撤销暂存（本仓规范：不留 staged 地雷）；**同步前后 staged 项均为 0**。

| # | 路径 | 本地旧版 → 远端真身 | 本地独有行性质 |
|---|---|---|---|
| 1 | `docs/ops/HANDOFF.yaml` | 40088 → **41054** B | 纯落后（`+0 / -579`） |
| 2 | `docs/ops/index_protected_markers.txt` | 5866 → **8943** B | 纯落后（`+0 / -34`） |
| 3 | `update_v8.py` | 86419 → **88726** B | 唯一 `+` 行 = 旧的注释态 `# "CANDIDATE_QUOTES": "intraday"` |
| 4 | `scripts/normalize_eol.py` | 10719 → **17489** B | `+` 行 = 旧的「单列表 + 无条件写入」实现 |
| 5 | `.github/scripts/pre_deploy_audit.py` | 68833 → **68901** B | `+` 行 = 旧的「尚未做 git rm --cached」注释 |
| 6 | `.github/workflows/v8_cn_fetch_cloud.yml` | 69465 → **71989** B | `+` 行 = 🔴 **旧的「relay ② 无条件派发」代码**（即本轮 P0 的 bug 版） |
| 7 | `.github/workflows/v8_stock_quote_refresh.yml` | 24544 → **24893** B | `+` 行 = 旧头注 |
| 8 | `.github/workflows/v8_stock_quote_refresh_cloud.yml` | 21998 → **22720** B | `+` 行 = 旧头注 |
| 9 | `algorithms/fetch_stock_quote_v8.py` | 21352 → **22666** B | `+` 行 = 旧的「单次 `try`、无重试」实现 |

**备份（可回滚）**：`.workbuddy/script_sync_backup/*.SUPERSEDED_20260921_1240`（9 个，gitignore 内）

**同步后断言（全部实测）**

```
9/9 文件 hash-object(工作树) == rev-parse origin/main:<path>     ⇒ 全部 SAME ✅
staged 项（首列非空）= 0                                          ⇒ 无地雷 ✅
红线文件未触碰：index.html local=c092417df26f / remote=71e63a384131（保持原样，未动）✅
              logic.html local=fe586435ee0d / remote=d5838bb4d002（保持原样，未动）✅
```

> 🔴 第 6 项目的是**阻断 P0 bug 的本地回推路径**：本机 `v8_cn_fetch_cloud.yml` 此前仍是
> 「relay ② 无条件派发兜底链」的 bug 版，任何一次本地推送都会把 `1232` 的治本**原地推翻**。

---

## 五、我方本轮**刻意不动**的项（勿视为遗漏）

| 项 | 为什么不动 |
|---|---|
| `index.html` / `logic.html` | 红线（含你正在改的页面 / churn 高频）；本哨兵不做跨页改动 |
| `run_algorithms.py` `STAGES`、`v8/factor_lab_gen.py` | 双机共写同一文件的区域 ⇒ **一律标「待授权」不擅动** |
| 你机自动化（`94279c7c` / `5edd92d6` / `351824ce` / `a8c1662a`） | 本机 DB 实测**不存在** ⇒ 无跨机写权限 |
| 你机的仓外对象（`intraday_backfill.py` / `api_push_paths.py` / 推送器 `fold_eol`） | 不在本仓、不在本机 |
| `docs/ops/handover/**` 541 个 CRLF | 双方已议定**不治**（审计凭据，收益 < 代价） |

---

## 六、纪律声明

- 本轮**未触碰主工作树 WIP**：推送走 **worktree 旁路**（`origin/main` detach → 只写回执 + `git add` 单文件 → SSH 推 `HEAD:main`）。
- **未** `git add -A`；**未** `reset --hard` / **未** checkout 主工作树任何 churn 文件。
- 安全微修**仅**作用于上表 9 个「本地落后副本」，逐一备份 + 逐字节断言；`index.html` / `logic.html` **未动**。
- 所有内容级复核**走 git 权威**（`git show origin/main:<path>` / `hash-object` vs `rev-parse`），**未依赖网页抓取**。
- 未改算法阈值、未删数据、未派发任何 workflow、未改任何自动化任务。
- 时戳**实取北京时间**（Python `timezone(timedelta(hours=8))`；本机 shell `date`/`TZ` 语义反转，不可信）。

---

*小九的工程师 · 单位机 `lemoncat-cn` · 2026-09-21 12:41 CST*
