# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-23 22:05｜门禁双登记缺口收口+前移新链+今晚算法链三项修复

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

`[4/8] align_logic_ops` 的**同族第 2 次复发已于 21:40–21:55 闭环**（你修前两处登记，我补第三处并验证放行）；
顺手把「治本」也做了 —— 新增**只读轻量链** `v8_align_gate`，让这类缺口在 **workflow 变更的那一次 push** 就暴露，
不必再等下一次 data push（本次它被并发 `cancelled` 掩盖了近 4 分钟才被人看见）。

---

## 正文

### 一、闭环时序（逐档取证，非推断）

| 时刻(CST) | 事件 | 取证 |
|---|---|---|
| 21:13:49 | `v8_dajijin_fetch.yml` 入库（`9681dd6f5f`，`cron '30 12 * * 1-5'`），**漏双登记** | commit 元数据 |
| 21:17:41 | `#7921`（head `14a090b9`）build **failure**，唯一红项 `[4/8]`；其余 13 项全绿 | job steps：step10 failure / step11–13 skipped |
| 21:28:48 | 我（阿狸咪）录入状态源，`done→doing`，**只报不改** | `8fafceed` |
| **21:40:50** | **你修完前两处**：`align_logic_ops.py +5/-0` + `logic.html +1/-0` | `58d37b5dd6` |
| 21:45:42 | **我补第三处**：`index.html` sec-op 隐藏同步清单 `21 项→22 项` + 加 `v8_dajijin_fetch` | `624623f073` |
| 21:47:43 | step10 `Pre-deploy audit` = **success** | run `35869145926` |
| 21:55:20 | step13 `📤 部署到 main` = **success**，`build` job = success | 同上 |

**⏱️ 时序诚实说明**：我 21:45 推送时携带「远端 blob 与本地基线**逐字节一致**」断言；
因你 21:40:50 已先落前两处 ⇒ 我的 PATCH 被拒（`422 not a fast forward`，**未 force**）
⇒ 改为**基新基线重放**，重放脚本自动识别并**只推第三处**（跳过你已修的两处）。
⇒ 不存在重复劳动、不存在覆盖、也不存在「差点覆盖」。

**闭环判据（正是该项 `verify_how` 的三项）**：`#7927` (run `35869145926`) 的 **step10 success + step13 success + build job success**
⇒ 阻断解除，21:00 之后积压的 `data/*.js` 已重新发布上线。线上 `index.html` 已回读确认我的改动存活。

**🔧 一处事实更正（针对 22:06 那份 `done` 补记，仅更正事实、不动你 `done` 的结论）**：
你把本机 `624623f073` 定性为「同题修复 / 双向冗余」。经 API 取该提交的 `files` 列表，
**它只改了 1 个文件**：`modified index.html +9/-2`，**未触碰 `align_logic_ops.py` / `logic.html`**。
⇒ 实为**三处登记缺口的互补**：你 21:40:50 落第 1、2 处（名单 + logic 文档源）；
我 21:45:42 补第 3 处（`index.html` 的 sec-op 隐藏同步清单 `21 项 MUST_HAVE_CRON` → `22 项` + 加 `v8_dajijin_fetch`）。
门禁只需**任一**文档源出现 stem，故你的两处已足够解锁；第三处是**防后人按旧清单漏登记**的一致性补齐。
md5 相同只因我**根本没改那两个文件**（它们保持你 21:40:50 后的真身）。
（我那句 commit message「补登记…双登记缺口」措辞过宽，实际只落第三处 —— 已在 HANDOFF 同一处一并更正。）


### 二、你 21:28 提的「治本」建议：已落地（且没有踩你担心的风暴）

你的建议原文是「把登记要求**前移**到新增/改 cron 的同一提交上」。
我先把**真正的缺口**坐实（这决定修法）：

> `v8_build_deploy.yml` 的 `on.push.paths` **不含 `.github/workflows/**`**
> ⇒ 推入/修改 workflow **根本不触发该门禁**，只能等下一次 `data/**` 或 `index.html` push 才暴露。
> 再叠加同并发组连续 `cancelled`（本次 `#7914–#7919` 全 cancelled）⇒ 阻断被静默掩盖。

🔴 **所以「把 `.github/workflows/**` 加进 build_deploy 的 paths」是错的修法**，我实测排除了：

- `GET /commits?path=.github/workflows&per_page=20` ⇒ 近 20 笔 ≈ **13 笔/日**，多为
  `chore(probe): …探针 vN` 的**建/删对**；
- 加进 build paths ⇒ 每次探针增删都触发一次**全量构建部署（~8min）** ——
  正是本仓 paths 注释里已明令避免的形态（「不用 `docs/ops/**`，否则每次改交接档都触发一次全量构建（风暴）」）。

✅ **改用独立轻量链**：`.github/workflows/v8_align_gate.yml`

- `on.push.paths = ['.github/workflows/**', 'align_logic_ops.py', 'logic.html']` + `workflow_dispatch`；
- 单 job：`actions/checkout@v4`(fetch-depth 1) + `python3 align_logic_ops.py`；
- `permissions.contents: read`（**只读**，不推任何东西、不用任何 secret）；`timeout-minutes: 5`（实测 ~20s）；
- 与 build_deploy 的 `[4/8]` **同脚本、同真源、同判据**。
- 未纳入 `index.html`：它**已在** build_deploy 的 paths 内（本身即跑 [4/8]），且每次构建都被改写 `?v`
  ⇒ 纳入只会产生重复空跑。而 `logic.html` **不在** build_deploy 的 paths 内（单独改它同样不入体检）
  ⇒ 本链把它一并补齐。

**本机离线实证（沙箱 33 个 workflow，非口头承诺）**：

- 正向：含新链 + 三处登记 ⇒ `EXIT 0`（`实际有 cron：22 个；MUST=22 / ALLOW=4；✅ 对齐校验通过`）；
- 负向：临时塞入一个**有 cron 且未登记**的 workflow ⇒ `EXIT 2`，判词逐字
  `workflow zzz_unregistered_cron_test 有 cron 调度，但既未登记 MUST/ALLOW 名单、也未文档化`
  ⇒ 证明新链跑在同款判据上、确实能当场抓住这型缺口；
- 风暴风险排查：`update_v8.py` / `guard_index_sections.py` 全文 grep `workflows` = **0 命中**
  ⇒ 无脚本自动改写 workflow 文件，独立校验链不会自激。

### 三、本次改动的文件清单（单提交）

| 文件 | 改动 |
|---|---|
| `.github/workflows/v8_align_gate.yml` | **新增**（门禁前移·只读轻量校验） |
| `align_logic_ops.py` | `ALLOW_NO_CRON` += `v8_align_gate`（无 cron，纯 push 事件驱动） |
| `index.html` | sec-op 隐藏同步清单：`ALLOW 3 项→4 项` + 加 `v8_align_gate` |
| `logic.html` | sec-lg「本表之外仍在跑的定时项（防漏）」表 += `v8_align_gate` 一行 |
| `docs/ops/HANDOFF.yaml` | `align-gate-unregistered-cron-workflow` → **done**（+`verified_at`，追加收口 note）；**新增** `align-gate-shiftleft`（P2 / pending-verify）；`meta.next_id_seq` 69→70 |
| `docs/ops/HANDOFF.ledger.json` | 由仓内唯一口径 `scripts/handoff_ledger_hash.py --fix` 全量重算 |

**HANDOFF 哈希口径自证**（你 09-23 收口的那个脚本，我照用）：
`命中 69/70 · 失配 1 · 未收录 1`，失配项**恰好**是我改的那 1 项、未收录项**恰好**是新增项，
`--fix` 后幂等自证 **100% ✅** ⇒ 口径实现正确，无全表假阳性。
真门禁离线复现：`check_handoff_ledger() -> (True, '交接状态源完好：items=71（ledger 锚 71 个 id 全在）· next_id_seq=70≥70')`。

### 四、改后三件套（本次）

1. **审计**：`[4/8]` 沙箱正/负向各跑一次（上）；`[13/13]` 真门禁离线复现（上）；新链 YAML 经 PyYAML 解析
   （`triggers=['push','workflow_dispatch']`、`paths` 三条、无 `schedule`）。
2. **四方对齐**：`align_logic_ops.py`（代码）+ `index.html` sec-op（运维页）+ `logic.html` sec-lg（逻辑详解页）
   + 本交接件 —— 四处同批。
3. **入库防覆盖 + 交接**：Git Data API `base_tree` 单提交 + `force:false`；推前**重取 tip**并断言
   「远端 blob == 本地基线」（逐字节）；推后回读每个 blob sha1 逐一比对；本件即交接。

### 五、需要你确认 / 跟进的（3 条）

1. `align-gate-shiftleft` 我置 **`pending-verify`**（本仓纪律：一次 success ≠ 常态）。
   请你在我这轮 push 后的 `v8_align_gate` 首档 run 上确认 **success**；
   若认同「已达标可转 done」，请**直接改该字段**（不必回执档）。
2. 该新链**没有 cron**，所以不在 `MUST_HAVE_CRON`；我把它登记进 `ALLOW_NO_CRON` 纯属**文档化**。
   若你认为「无 cron 者不必登记」，删掉那行不会让门禁变红（我已实测：删掉后仍 `EXIT 0`）。
3. 你 21:13 那笔 `feat(暂未上架): 新增「🚨 大基金 增减持监控」` 的**收尾**建议：
   今后新建带 cron 的 workflow，**当轮**就把「名单 + 文档源」两处一起改（现在多了一层 `v8_align_gate` 兜底，
   漏了会在 1 分钟内显红，而不是等下一位推 data 的人踩到）。

### 六、附：今晚算法链另三项修复（已完成，详见 `2026-09-23_2122` 那份交接件）

1. `api_push_raw.py` **唯一推送步并发化 + 总时长软闸**（`15404ba6566f`）——根治跨境慢网串行 22~42 文件
   撞穿 `timeout-minutes: 20` 致**整拨零推送**；线上实证 42 文件 **16 秒**完成。
2. `v8_algo_cloud.yml` **step15「同步 ALGO_TRACK ?v」取正文纳入 try + 完整性断言 + 软失败**（`1c069110e274`）
   ——根治其未捕获异常逃逸 ⇒ step16 机器校验被 skip。
3. `verify_data_sanity.py` **STALE_OK 白名单**加 `CANDIDATE_BACKTEST` / `GOLD_POOL_BACKTEST`
   （`fb2c48c98750`）——实证 step16 已 success。

—— 阿狸咪的工程师（alimi-cn）
