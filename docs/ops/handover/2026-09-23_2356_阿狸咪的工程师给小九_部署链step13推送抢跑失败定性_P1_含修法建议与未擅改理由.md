# HANDOVER｜阿狸咪的工程师 → 小九｜2026-09-23 23:56｜部署链 step13 推送抢跑失败定性（P1，含修法建议与未擅改理由）

> 规范：`docs/ops/handover/README.md`（唯一交接目录，时间优先命名）

---

## 一句话结论

`v8_build_deploy` 的末步「📤 部署到 main」在高 churn 下**几乎必然失败**：它的重试循环是
`for i in 1 2 3`，而**每轮「重取远端 → reset → 全量重建（约 4 分钟）→ push」**，
churn 间隔只有 **1–3 分钟** ⇒ **每轮都在 push 前又被抢跑**。
⚠️ 我**未擅自改**该步（理由见 §4），仅做定性 + 给修法建议，交你与主人裁决。

---

## 1. 证据（硬数据，全部来自 API / 日志原文）

### 近 12 档落地率
```
近 12 档结论分布: {'in_progress': 1, 'cancelled': 8, 'success': 1, 'failure': 2}
#7967  in_progress  23:53:43 -> …            d5d75b7ea
#7966  cancelled    23:53:40 -> 23:53:46     fe211e68a
#7965  cancelled    23:53:22 -> 23:53:44     d717a5882
#7964  cancelled    23:44:16 -> 23:53:23     c7bc72d6a   ← 我的健康检查修复那笔
#7963  cancelled    23:39:24 -> 23:44:17     f263f55c6
#7962  cancelled    23:39:16 -> 23:39:26     428dbc90c
#7961  success      23:38:20 -> 23:47:48     e6290d2e3   ← 本窗口唯一落地
#7960  cancelled    23:34:40 -> 23:38:22     9194faebf
#7959  failure      23:33:05 -> 23:53:51     01a24e8e3   ← 见下
#7958  cancelled    23:31:18 -> 23:33:06     c41d700da
#7957  cancelled    23:30:56 -> 23:34:41     b6179f06d
#7956  failure      23:20:56 -> 23:41:33     9b7dd3399   ← 同 step13
```

### 两档 failure 的失败步**完全相同**
```
#7959  build job: failure 15:37:35 -> 15:53:50   ★ 失败步 = 13 「📤 部署到 main（GitHub Pages 源）」
#7956  build job: failure 15:28:04 -> 15:41:32   ★ 失败步 = 13 同上
```

### `#7959` 日志逐字（关键三行）
```
15:43:58  ! [rejected]            HEAD -> main (fetch first)
          error: failed to push some refs to 'https://github.com/ah-quant999/quant-scanner-v8.git'
15:48:09  ! [rejected]            HEAD -> main (fetch first)
15:52:22  ! [rejected]            HEAD -> main (fetch first)
```
⇒ **三次被拒，恰好等于 `v8_build_deploy.yml:316` 的 `for i in 1 2 3` 三轮**。
每轮间隔 ≈ **4 分钟**（15:43:58 → 15:48:09 → 15:52:22），而同期远端提交间隔 **1–3 分钟**
⇒ **每轮重建期间 main 必然前进，push 必然被拒** ⇒ 重试形同虚设。

### 该步的既有设计意图（源码注释原文，勿当 bug 改错方向）
```yaml
# ⚠️ 不要改回 git rebase：data/*.js 是构建产物，两趟构建并发时 rebase 必然
#   在生成文件上产生 content conflict -> rebase --abort -> 整个部署失败
#   （2026-08-04 23:50 实例：data/HEALTH_CHECK.js CONFLICT，部署挂掉）。
#   正确做法：push 被拒 = 远端已前进，同步远端后用最新 raw_data 重新全量构建，
#   产物由构建决定而非由合并决定，天然无冲突且不会用旧产物覆盖新数据。
for i in 1 2 3; do
  timeout 180 git ... fetch "$REPO" main
  git reset --hard FETCH_HEAD
  ...（update_v8.py 等全量重建）...
```
⇒ 作者的取舍（**宁可重建也不 rebase**）是对的；**失效点只在「重建耗时 ≫ churn 间隔」**，
所以修法必须动「**重试的粒度**」，而**不能**动「产物由构建决定」这条原则。

---

## 2. 影响面（已量化，避免夸大）

- ✅ **站点数据仍新鲜**：硬 HTTP 抓 Pages 实测 `TRIPLE_TRACK 22:49:13` / `ALGO_TRACK 22:49`
  / `CRDS_CARD_DATA 22:37:26` —— 因为链会**直推 main**（`api_push_raw.py`），Pages 直接服务 main。
- ⚠️ 受影响的是 **build_deploy 重算产物**（含 `index.html` 的 `?v` 戳对齐）的**落地**：
  本窗口 23:38 之后到 23:53 之间**没有一次成功部署**（#7961 是最后成功档）。
- ⚠️ 附带成本：每档失败前白烧 **16 分钟**构建机时，且 run 标红（**真红，不是假红**，不要忽略）。
- ⇒ 定级 **P1**（延迟 + 红 run + 算力浪费），**P0 不成立**（不是停更）。

---

## 3. 修法建议（按推荐度排序）

### 建议 A（推荐）：把「重试」与「全量重建」解耦
第 1 轮照旧全量重建；**若 push 被拒，后续轮不再重建**，改为：
取 live tip → `POST /git/blobs`（本轮产物）→ `POST /git/trees(base_tree=live tip 的 tree)`
→ `POST /git/commits` → `PATCH /git/refs/heads/main(force:false)`。
- 这条通道**秒级完成**，churn 再快也追得上；**阿狸咪今晚整夜都在用它**（含中文路径、含 workflow 文件）。
- **防静默覆盖**：push 前做**本步自带的漂移断言** —— 比对本轮产物路径在 live tip 里的 blob
  是否仍等于「本轮开始时的基线 blob」；不一致 ⇒ 说明对手方改了同一文件 ⇒ 回到全量重建轮。
- 与既有原则不冲突：产物仍由**构建**决定；API 通道只负责「把构建结果放上去」。

### 建议 B（最小改动，收益有限）
`for i in 1 2 3` → 6 轮 + 轮间 `sleep`。但每轮仍 4 分钟，且 run 时长翻倍
（现值已 16 分钟）⇒ 可能撞 `timeout-minutes`，**不推荐单独用**。

### 建议 C（错峰，需双方约定）
让 `v8-cache-bot` / 心跳类高频提交在 build_deploy 运行期间避让（按 concurrency 组或时间窗）。
治本但面广，需你那边确认。

---

## 4. 我为什么**没有**今晚直接改（请勿当成遗漏）

1. 该步是 **200+ 行、含 6 处历史事故注释**的战场文件；改它的推送语义属**门控/契约级**改动。
2. 🔴 **我今晚无法验证 workflow 级改动的真实行为** —— 本机跑不了 `v8_build_deploy`，
   只能过语法与离线门禁 ⇒ 不足以支撑「改好即上线」。按「不许说谎 / 改动必须有实测」，
   我**不做没有验证手段的改动**。
3. 影响面已量化到 **P1（延迟，非停更）**，不属于必须午夜动刀的场景。
4. 宗旨③「不造风暴」：盲目加重试轮数会拉长 16 分钟构建并加重排队，可能反向恶化。

---

## 5. verify_how（复现我的结论）

```bash
# ① 看近 12 档结论分布（期望仍见 success 稀疏 / cancelled 密集）
curl -sS -H "Authorization: Bearer $TOK" \
  "https://api.github.com/repos/ah-quant999/quant-scanner-v8/actions/workflows/v8_build_deploy.yml/runs?per_page=12" \
  | python -c "import json,sys,collections;d=json.load(sys.stdin);print(collections.Counter(r['conclusion'] or r['status'] for r in d['workflow_runs']))"
# ② 任取一档 failure，看失败步是否恒为 13
curl -sS -H "Authorization: Bearer $TOK" ".../actions/runs/<RID>/jobs" | grep -n '"number": 13'
# ③ 取其日志，数 `! [rejected] HEAD -> main (fetch first)` 的条数（应 = 3，对应 for i in 1 2 3）
curl -sSL -H "Authorization: Bearer $TOK" ".../actions/jobs/<JID>/logs" | grep -c "rejected.*main (fetch first)"
```

---

## 6. 与今晚其它两件的边界

- 本件只讲 **build_deploy step13**；`raw_data 假红根治` / `两卡停更定性更正` / `推送工具回读加固`
  见同夜 `2026-09-23_2349_…`。
- 本件**未改任何仓内文件**（纯定性 + 建议）。
