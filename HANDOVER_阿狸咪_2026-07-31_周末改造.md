# v8 周末改造交接 — 阿狸咪 → 小九

**日期**: 2026-07-31（周五晚～周六凌晨）  
**改造目标**: 分时段 selective fetch + selective build，删除死数据，避免 v6 式全站臃肿崩溃。  
**线上状态**: 已推 `origin/main`，GitHub Actions 自动部署。

---

## 一、已完成的改造

### 1. 架构改造

| 文件 | 改动 |
|------|------|
| `update_v8.py` | 新增 `--category {premarket\|intraday\|post_close\|weekly}` 与 `--detect-changes`；缺失 raw_data 的模块保持旧 `data/*.js`（carry-forward） |
| `cloud_fetch_v8.py` | 新增 `--category`；只清理/抓取目标类别的 raw_data，不再全删 |
| `api_push_raw.py` | **关键修复**：推送时合并远程已有 raw_data，不再用本地空目录覆盖远程（否则 selective fetch 会删掉其他类别文件） |
| `guard_v8_freshness.py` | 删除已移除的 `RECOMMEND`/`SCAN_DATA`；输出增加 `category_map` |
| `index.html` | 删除 `RECOMMEND.js` / `SCAN_DATA.js` 的 `<script>` 引用 |
| `data/RECOMMEND.js` / `data/SCAN_DATA.js` | 删除（前端从未引用） |

### 2. 定时任务改造

| Workflow | 触发时间（北京时间） | 负责类别 | 更新哪些前端数据 |
|----------|---------------------|----------|------------------|
| `v8_cn_fetch.yml` | 08:25 / 周六 09:00 | 盘前 | `V8_CAL` `IPO_DATA` `MARGIN_DATA` `CFFEX_HOLDINGS` `MACRO_DATA` `CRISIS_DATA` `NORTH_FUND` `ANALYST_RATINGS` `W52_HIGH` `VOLATILITY` `HERDING_DATA` |
| `v8_cn_fetch.yml` | 10:30 / 11:30 / 13:05 / 14:00 / 15:05 | 盘中 | `INDEX_QUOTES` `ETF_PULSE` `ETF_INTRADAY_HEAT` `ETF_SUBSCRIPTION` `SECTOR_FUND_FLOW` `CAPITAL_FLOW_DATA` `CONCEPT_RANKING` `LIMIT_UP_HEATMAP` `MARKET_FUND_FLOW_DATA` |
| `v8_cn_fetch.yml` | 16:30 | 盘后（akshare 部分） | `ETF_DAILY_MONITOR` `EXPERIMENT` |
| `v8_build_deploy.yml` | raw_data 任何 push | 按变化类别 | `--detect-changes` 只构建变化 raw_data 所属类别 |
| `v8_build_deploy.yml` | 16:40 | 盘后兜底 | `--category post_close` 全量构建盘后模块 |
| `v8_algo.yml` | 09:00 / 17:00 | 体检 | 运行 `guard_v8_freshness.py`，核心过期则 CI 失败 |
| `cloud_weekly_cleanup.yml` | 周日 23:00 | 清理 | 删除无 `DATA_SOURCES` 映射的 orphan raw_data/data 文件，更新体检报告 |

### 3. 模块当前状态

- **cloud_fetch 已覆盖（22 个）**: 盘前 11 + 盘中 9 + 盘后 2，正常刷新。
- **冻结/无云端生产者（21 个，post_close）**: 需要小九周一补齐生产者或从 v6 推送到 v8 raw_data：
  `GOLD_POOL` `CANDIDATE` `TRIPLE_CONSENSUS` `TRIPLE_TRACK` `TRIPLE_HISTORY` `COCKPIT_ADVICE` `COCKPIT_TIER_RECOMMEND` `COCKPIT_BACKTEST` `BACKTEST_COMPREHENSIVE` `BACKTEST_TDX` `CRDS_CARD_DATA` `LHB_DATA` `SH_FIB` `SZ_FIB` `SECTOR_RS` `MAHORO` `INST_TRADE` `NT_DATA` `TOP10_DAILY` `SUSPENSION_ALERT` `MARKET_ALERTS` `STOCK_LIST`
- **已删除（2 个）**: `RECOMMEND` `SCAN_DATA`（前端无引用）。

---

## 二、小九周一必须做的事

### P0：接上 21 个冻结模块的生产者

三个方案，按优先级：

1. **推荐：v6 算法端直接推 raw_data 到 v8**（最小改动）
   - 在 v6 收盘链路里，把上述 21 个 json 文件用 `api_push_raw.py` 同款 REST API 推送到 `ah-quant999/quant-scanner-v8` 的 `raw_data/`。
   - 文件名必须与 `update_v8.py::DATA_SOURCES` 的 key 一致。
   - 推完后 `v8_build_deploy.yml` 会自动 `--detect-changes` 构建对应 `data/*.js`。

2. **次选：把 v6 算法脚本迁移到 v8 云端 workflow**
   - 在 v8 新建 `algo/` 或 `scripts/` 目录，放闭市后算法脚本。
   - 在 `cloud_post_close.yml` 或 `v8_build_deploy.yml` 里调用。
   - 工作量大，但能彻底脱离 v6。

3. **兜底：直接删除不需要的前端模块**
   - 如果某些模块不再需要，同步删除 `index.html` 的引用和 `DATA_SOURCES` 映射，避免前端读 `undefined`。

### P1：SH_SZ_HISTORY 硬编码内联

`index.html` 第 412 行左右硬编码了 `window.SH_SZ_HISTORY`（更新于 2026-07-28，数据到 6 月初）。
- 方案 A：出生产者（历史成交额），写入 `data/SH_SZ_HISTORY.js`。
- 方案 B：删除该卡片（若不再需要）。
- **不要长期留硬编码旧数据**，这是 v6 式臃肿。

### P2：给 5 个无时间戳模块加时间戳

`guard_v8_freshness.py` 报无时间戳：`COCKPIT_ADVICE` `COCKPIT_TIER_RECOMMEND` `MAHORO` `STOCK_LIST` `TRIPLE_HISTORY`。需要生产者在 json 里写 `update_time` 或 `calc_time`。

---

## 三、线上验证 checklist

1. 访问 `https://ah-quant999.github.io/quant-scanner-v8/` 看是否 404/白屏。
2. GitHub Actions → 手动触发一次 `v8_cn_fetch.yml`（选 premarket）→ 确认 `raw_data/` 只更新 premarket 文件，其他文件保留。
3. 观察 `v8_build_deploy.yml` 是否自动触发，且只构建 premarket 类别。
4. 周日 23:00 后检查 `cloud_weekly_cleanup.yml` 是否误删文件。

---

## 四、重要原则（防崩溃）

- **禁止在 cn runner 外跑 `cloud_fetch_v8.py`**（美国 IP 会超时）。
- **禁止手动跑 `deploy_v8.py`**（双机铁律，已由 `v8_build_deploy.yml` 自动部署）。
- **删除任何 data/*.js 前**，必须确认 `index.html` 无 `window.X` 引用。
- **新增 raw_data 文件前**，必须在 `update_v8.py::DATA_SOURCES` 注册并分配 `CATEGORY_MAP`。
- **api_push_raw.py 已改为合并模式**，不会删远程其他类别文件，但不要依赖它做删除操作。

---

## 五、后续 B/B+ 可选项

- 给 `index.html` 的 44 个 `<script>` 加 `defer` + 首屏优先级分组（当前 1.3MB 同步加载仍是潜在瓶颈）。
- 给 frozen 模块分优先级，先恢复 GOLD_POOL / CANDIDATE / TRIPLE_* 等核心模块。
- 为 `SH_SZ_HISTORY` 出独立生产者并删除内联脚本。
