# 交接：小九 → 阿狸咪（2026-08-04 晚）

## 一、今晚小九要做什么

应主人要求，今晚把 **v8 前端健康巡检自动化三期全部做完**，并建立 **每小时紧急任务交接通道**，同时邮件告警。

预计改动范围：
- `v8_health_check.py`（新增）：检查数据新鲜度、runner 状态、Pages 部署、本地 HEAD 同步、关键数据空值。
- `data/HEALTH_CHECK.js`（新增）：健康检查结果写入站点数据。
- `index.html`：运维区新增「🩺 前端健康」TAB；任务运行看板已在此前完成。
- `v8_cloud_watchdog.py`（升级）：健康检查异常时自动 `--auto-dispatch` 补拉数据。
- `v8_build_deploy.yml`（可能升级）：构建后写入 HEALTH_CHECK.js，异常时触发告警。
- 邮件告警模块（新增）：异常时发邮件给小九。

## 二、阿狸咪今晚及近期注意事项

1. **单部署者铁律仍然有效**
   - 小九 = 唯一部署者（数据 + 构建 + deploy_v8）。
   - 阿狸咪 = 纯代码编辑，**只改不部署**。
   - **不要运行 `deploy_v8.py`**，不要手动触发 v8_build_deploy。

2. **改完任何文件必须立即 commit + push origin/main**
   - v8 的 cn runner 每个周期会 `git fetch + reset --hard origin/main`。
   - 未入库的改动会被**静默覆盖**。
   - 模板 `v8/index.html` 任一处改动也要同步 push 主仓库。

3. **不要与小九的改动冲突覆盖**
   - 今晚小九会频繁改 `index.html`、`v8_cloud_watchdog.py`、新增 `v8_health_check.py`。
   - 如果阿狸咪也改同一文件，请**先 `git pull` 同步最新 origin/main**，解决冲突后再 push。
   - 如果不确定，先不要在今晚改这些文件，等明天小九发消息。

4. **紧急任务交接通道**
   - 小九会新建一个 **每小时运行一次** 的 WorkBuddy automation：
     - 任务：调用 `v8_cloud_watchdog.py --heal --auto-dispatch`
     - 作用：每小时检查 runner 是否在线、数据是否陈旧，自动修复。
   - 这是小九和阿狸咪之间的兜底，避免主人回家后无法沟通。
   - 阿狸咪如果白天发现站点卡住，也可以手动运行：`python v8_cloud_watchdog.py --heal --auto-dispatch`

5. **邮件告警配置待补充**
   - 小九正在询问主人邮箱/SMTP配置。
   - 配置好之前，告警会 fallback 写成 `URGENT_小九_YYYY-MM-DD_HHMM_*.md` 文件到仓库根目录。
   - 阿狸咪如果看到 URGENT 文件，说明有严重异常，先不要改代码，联系小九。

## 三、交接确认

- 本文件已入仓库并 push origin/main。
- 阿狸咪看到本文件后，确认已读即可。
- 今晚小九开工后会再更新一次本文件，补充最终文件清单和运行状态。
