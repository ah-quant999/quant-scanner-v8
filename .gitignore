__pycache__/
*.pyc
*.pyo
.DS_Store
.workbuddy/
# 临时文件（_ 开头 / 脚本检查切片 / 日志 / 缓存）
_*/
# 🛡 2026-09-12 阿狸咪补漏：上面 `_*/` 带尾斜杠**只匹配目录**，匹配不到 `_` 开头的**文件**。
#   实测 `git check-ignore algorithms/_valid_codes.json` returncode=1（未命中）→ 该运行时缓存
#   一直以未跟踪态污染 git status。backtest_expectancy.py 缺失时会用 baostock 自动重建，
#   属纯本地缓存，不入库 → 精确点名（不用宽泛的 `_*`，`_*.py` 规则见下方「临时脚本残留」段）。
algorithms/_valid_codes.json
_tmp_*/
*.log
*.err
*.tmp
*.bak
nul
# Token / 密钥（防泄露）
.neodata_token
.gh_pat
*token*.json
*.jwt_cache
data/.mahoro_cookies.txt
data/.maharo_cookies.txt
data/zsxq_token.json
data/.github_pat.txt
data/.wc_jwt_cache.json
data/.neodata_token
data/zszxq_token.json
# 🛡 2026-08-27 主人令：算法运行时行情缓存（RPS/K线/TDX），纯本地加速用，严禁入库/推仓
#   历史已入库文件由隔离索引 git rm --cached 移除（不删本地文件）
#   ⚠️ 2026-09-08 主人新令覆盖（仅 kline_cache 一项）：
#      「膨胀的很厉害？出真实数据才是最重要的，出来后再精简不行么？」
#      → kline_cache 入仓。收益：云端 runner 也能零网络取数，根治「云端抓中国源
#        超时被杀 → 产物写不出 → 下游读旧文件 → 静默断更」（H_AUTO_BUY 断更 3 天
#        即此类）。代价：仓库 +46MB（2320 只）。后续精简方向：只留近 60 根 K 线、
#        或仅主板 A 股，可把体积压到 1/4 以下。
raw_data/_rps_cache/
# raw_data/kline_cache/   ← 2026-09-08 主人令：改为入仓（云端零网络取数，根治静默断更）
raw_data/_tdx_cache/
raw_data/backtest_kline_cache/

# 🛡 2026-08-30 阿狸咪加注：raw_data/_archive/ — raw_retention.py 归档目录
#   移走 >7 天的 .bak、>14 天的 h_auto_buy_*.json 详情，不再被 git 索引。
raw_data/_archive/

# 算法 staging / 生成数据（脚本和工作流会重建）
algorithms/data/
algorithms/out/
out/
# 运行时锁文件（PID，脚本并发互斥用，勿提交）
algorithms/.scanner.lock
# 冲突文件（双机同步残留）
*-冲突-*
*-conflict-*

# 临时脚本残留
_*.py

# 自愈派发防抖锁（运行时本地文件，不入库）
data/.heal_dispatch.json
data/FOUR_VOLUME_BACKTEST.json

# 紧急交接/看门狗留痕（云端每小时重建并曾由 v8_cleanup.yml `git add -A` 回推仓库；
# 根因修复：根目录 URGENT_*.md 永不入库，避免 git status 反复被污染。
# 正式交接通道在 docs/ops/urgent/，不被此规则影响。以下 3 份含根因/交接信息，显式保留。）
/URGENT_*.md
!/URGENT_阿狸咪_2026-08-08_添加ZSXQ_TOKEN_Secret.md
!/URGENT_小九_2026-08-11_19-43_根治项+模型全切.md
!/URGENT_小九_2026-08-10_0117_v8健康自检告警.md

# 本地 npm 安装产物（非仓库依赖，勿提交；防止 19M node_modules 污染仓库）
node_modules/
package-lock.json
package.json
