# HANDOVER 小九 → 阿狸咪 · 2026-09-16 07:35

## 一句话
主人指出「平均股价卡的**超买超卖提示太大**」，已把视觉权重从第三轮的「显著标识」档
**降回与同卡「📋 关注」胶囊同档**，业务逻辑零变化、未就绪态不退化。已上线 `bb69824c5f3`。

## 一、主人原话
> （截图 实时数据页 · 平均股价卡第一行右侧那个金色大胶囊）
> 「**这个超买超卖不用这么大提示。改好的和阿狸咪交接**」

## 二、四轮演化脉络（必须先看懂，否则会改反）
| 轮次 | 时间 | 主人指令 | 落点 |
|---|---|---|---|
| 一 | 09-15 | 「把平均股价超卖超买那个金色提示指标写回去！」 | 恢复金色「位置观测」胶囊（09-14 曾因「与 AI速览 结论打架」被删），并挂免责小字 |
| 二 | 09-15 | 「第二行的中性观望不需要了，删除吧」 | 胶囊移到第一行末尾；中性态（`⏸ 中性·观望`）不再渲染 |
| 三 | 09-16 | 「平均股价的超买超卖要加在最后，**显著标识**」 | ⚠️ 2px 金边 + `box-shadow:0 0 10px` 外发光 + 13px/800 字重 + 金色渐变底 + `letter-spacing:.3px`；并补**未就绪态兜底**（`⏳ 均线积累中 X/20日`，此前均线未就绪整块不渲染＝页面根本看不到该指标存在） |
| **四（本次）** | **09-16** | 「**这个超买超卖不用这么大提示**」 | **只降视觉权重**，第三轮的「可见性」成果**全部保留** |

> 🔴 **别把第三轮一起改回去**：第三轮那两条（① 胶囊必须在页面上看得见；② 未就绪态显式可见）
> 是主人明确要过的，本次只降「多大」，不降「有没有」。

## 三、改动清单（已上线）

**文件**：`index.html`（唯一改动文件，其余零触碰）
**提交**：`bb69824c5f3` ｜ **父提交**：`9b75cfc1030`
**线上**：`https://ah-quant999.github.io/quant-scanner-v8/index.html` = **1197157 字节**

| # | 位置 | 改前（第三轮·重） | 改后（第四轮·轻） |
|---|---|---|---|
| 1 | 胶囊字号 | `font-size:13px` | `font-size:11.5px` |
| 2 | 胶囊字重 | `font-weight:800` | `font-weight:700` |
| 3 | 胶囊内边距 | `padding:4px 12px` | `padding:3px 8px` |
| 4 | 胶囊圆角 | `border-radius:6px` | `border-radius:5px` |
| 5 | 胶囊边框 | `2px solid rgba(251,191,36,.7)` | `1.5px solid rgba(251,191,36,.5)` |
| 6 | 胶囊底色 | `linear-gradient(180deg,rgba(251,191,36,.24),…,.10)` | `rgba(251,191,36,.12)`（纯色低透） |
| 7 | 外发光 | `box-shadow:0 0 10px rgba(251,191,36,.38)` | **删除** |
| 8 | 字距 | `letter-spacing:.3px` | **删除** |
| 9 | 免责小字 | `font-size:10.5px` ｜「🔭 仅观测**位** · …」 | `font-size:10px; opacity:.75` ｜「🔭 仅观测 · …」（去「位」字） |

**为什么选这个档位**：同卡内「📋 关注」胶囊就是 `11.5px / 700 / 1.5px 边 / 纯色低透底 / 无发光`。
胶囊降权后**与它同档** ⇒ 卡片内视觉层级重新扁平，符合主人一贯偏好（简洁克制、避免花哨）。

## 四、线上核验（三层，全过）

| 层 | 判据 | 结果 |
|---|---|---|
| 静态 | 新写法在位 ×9 项 / 旧写法清零 ×7 项（**限定到胶囊所在行**判） | ✅ |
| 语法 | 逐 `<script>` 块 `new Function` 体检 | ✅ 28/28 |
| 真渲染 | 抽出 `renderAiSummary` 所在 IIFE，注入真实结构 `window.AVG_PRICE_DATA`（25 日档位路径 + 12 日未就绪路径）实跑 | ✅ 8/8 + 未就绪态 ✅ |
| 线上 | 硬抓 `/index.html?_cb=` 轮询；新旧写法计数；旧写法全 0 | ✅ 第 2 次即生效 |

**真渲染产物实测**（档位态）：
```html
<span style="font-size:10px;color:var(--dim);opacity:.75;">🔭 仅观测 · 开仓以 AI速览 ⚖ 裁决为准</span>
<span style="display:inline-flex;align-items:center;gap:4px;padding:3px 8px;border-radius:5px;
      background:rgba(251,191,36,.12);border:1.5px solid rgba(251,191,36,.5);
      font-size:11.5px;font-weight:700;color:#fbbf24;">📊 超买超卖 🟠 偏高·观测</span>
```
未就绪态（`history_days=12`）实测：`📊 超买超卖 ⏳ 均线积累中 12/20日` ✅ **仍可见**。

## 五、推送过程：连续撞上「远端高频改写」，护栏两次立功

本次在 07:15–07:35 窗口内，远端 main 被推了 **4 次以上**（CI 缓存戳 + 你方提交）：

| 轮 | 我取到的 tip | 结果 |
|---|---|---|
| 1 | `8c445187f` → 我改完时远端已到 `e852c5dee` | ⚠️ **护栏拦下 50 行**（含你的「新周期判定」等改动）⇒ 作废重做 |
| 2 | 基线换到 `e852c5dee` → 推时远端已到 `c1484ebcf` | ⚠️ **护栏拦下 1 行**（正是我要替换的旧写法，白名单失配）⇒ 修判据 |
| 3 | 推时远端已到 `9b75cfc10` | ⚠️ **push 被拒**（`cannot lock ref`，fail-closed，远端零影响）⇒ 改带重试循环 |
| 4 | 每轮重新抄底 → `9b75cfc1030` 抢到 | ✅ 推送成功 |

**这是预期行为，不是故障** —— 宁可多试几轮，也不让任何人的改动被静默回退。

## 六、留给阿狸咪的三件事

1. **白名单判据别用长整串字面量**（本次踩坑）：`push_index.py` 里我把「本批主动替换的旧写法」
   写成完整行字面量，该行含 `\"` 转义与单双引号混排 ⇒ 字面量与实际行差一个字符即**静默失配**，
   把「我自己要删的旧写法」误报成「他改被覆盖」而中止推送（假阳性）。
   **正确做法：用唯一关键片段做 `in` 判定**（本次改为 5 个片段，立即通过）。
2. **同一文件多处改动必须串行落盘**（记忆已有，本次再验）：三处锚点替换写成**一次脚本跑完**，
   而非三次并发 Edit——后者会「后写者基于旧内容定 anchor ⇒ 覆盖先写者」。
3. **推送窗口**：本次在 07:15–07:35 完成，**在 08:15 盘前部署启动前**，未跨 09:15 硬停。
   后续若在盘中改 `index.html`，请遵守「09:15 / 13:00 前必须落盘」的写窗铁律。

## 七、可复现的验证脚本（都在 `%TEMP%\tvfix\`，未入库）

| 脚本 | 用途 |
|---|---|
| `verdict_capsule_shrink.py` | 锚点式三处替换（幂等，锚点非 0/1 次即中止） |
| `verify_capsule.py` | 双向校验 9+7 项 + 防退化 4 项 + 最小改动范围断言 |
| `syntax_check.py` | 逐 `<script>` 块语法体检（`node --check` 对 html 无效） |
| `real_render2.js` | **真渲染**：抽 IIFE + 注入 `AVG_PRICE_DATA` 实跑，断言产物 |
| `push_retry.py` | **带重试的单文件推送器**（每轮抄底 → 重放锚点 → 护栏 → 推，失败自动重来） |

> `push_retry.py` 建议固化为双机共用工具——本仓远端改写频率下，单次推送成功率很低。

## 八、本次未动、待主人拍板（别自行改）
- 胶囊文案 `📊 超买超卖` 前面那个 emoji 是否要换/去掉（本次保持原样）。
- 免责小字是否干脆整段删除（第三轮曾考虑「保留」——它承担口径声明作用，本次只弱化未删）。
- 卡内 `vs 20日线 / vs 60日线` 两段在 09-16 已被前一轮删除，本次未触碰。
