# HANDOVER_小九给阿狸咪_2026-09-19_1545_每日因子卡两步文案层级对齐+每日洞察月度汇总排版修正

- **交接方**：小九（白天班 7:45–17:45）
- **接收方**：阿狸咪（夜班/周末 18:00–次日 7:30）
- **改动类型**：纯前端 UI（`index.html` 单文件），无数据层/算法层改动
- **影响卡片**：① 每日因子计算卡 ② 每日洞察 · 月度汇总（`insight-grid` 同页区段）
- **是否已上线**：是（详见下方「上线取证」）

---

## 一、改了什么（主人原话 → 落点）

### 任务 1 · 每日因子计算卡

**主人原话**：「这里的第一步第二步的文字样式大小要统一，它们两步实际上有逻辑关系？我怎么没看懂。」

**根因（取证结论）**：

| 检查项 | 改动前实测 | 问题 |
|---|---|---|
| 第一步头部 | 只有一排 12px 胶囊标签，**无 15px 主句** | 无视觉锚点 |
| 第二步头部 | 主标题已是 15px（此前改过） | **两步层级不对等** |
| 两步逻辑关系 | 仅存在于源码注释 | **页面上零衔接句** ⇒ 主人读不懂 |
| 第三步胶囊 | 10px / `1px 8px` / radius 10px | 与第一步胶囊规格不一致 |

**三处改动**：

1. **第一步补 15px 主标题**「📋 每日打分」，与第二步主标题同级；胶囊字号/内边距/圆角拉齐到 `12px` / `2px 10px` / `radius 11px`。

   ```html
   <div style="display:flex;align-items:baseline;gap:9px;flex-wrap:wrap;margin-bottom:6px;">
     <span style="font-size:15px;font-weight:700;color:var(--txt);">&#x1F4CB; 每日打分</span>
     <span style="padding:2px 10px;border-radius:11px;font-size:12px;font-weight:700;background:#38bdf822;color:#38bdf8;border:1px solid #38bdf84d;">第一步</span>
     ...
   ```

2. **新增「两步什么关系」衔接句**（插在第一步说明块之后、`<details>` 之前）：

   ```html
   <div style="font-size:12px;line-height:1.8;color:var(--sub);margin:0 0 8px;padding:8px 11px;background:rgba(56,189,248,.06);border-left:3px solid #38bdf8;border-radius:0 6px 6px 0;">
     <b style="color:#38bdf8;">两步什么关系</b>：<b>第一步</b>是 <b>26 项因子的权重字典</b>（静态口径，每天同一套）；<b>第二步</b>是这 26 项里
     <b style="color:#fbbf24;">当日真正进了算法链计分</b>的那几项（读产物真值，随行情增减）。
     一句话：<b style="color:#22c55e;">第二步 &#8838; 第一步</b> —— 它是因子的「候选 &#8594; 计分」收口环节，不是并列的第二件事。
   </div>
   ```

3. **第三步「当日因子计分榜」胶囊拉齐**：`10px / 1px 8px / radius 10px` → `12px / 2px 10px / radius 11px`。

### 任务 2 · 每日洞察 · 月度汇总

**主人原话**：「这个每日洞察的月度汇总排版不行，把 [口径脚注] 移到左侧 [两卡] 这个下方，然后 [这两卡] 的宽度和上方 [三卡] 这两卡宽度一致。」

**根因（取证结论）**：

| 位置 | 改动前 | 问题 |
|---|---|---|
| 上排 `.insight-grid` | `1fr 1fr 1fr`（3 卡，各 ~190px） | — |
| 下排 `.month-summary` | `1fr 1fr`（2 卡，各 ~290px） | **被拉宽 ~100px**，与上排不对齐 |
| 口径脚注 | 底部居中独立行 | 造成大片空白 |

**三处改动**：

1. **网格改三列**：

   ```html
   <div class="month-summary" style="grid-template-columns:1fr 1fr 1fr;gap:12px;margin-top:10px">
   ```

2. **口径脚注移入网格第三格**（左侧两卡 = 原两卡，第三格 = 脚注）：

   ```javascript
   h += '<div class="ms-col" style="background:transparent;border:1px dashed var(--border);display:flex;flex-direction:column;justify-content:center;">';
   h += '<div style="font-size:10.5px;color:var(--dim);line-height:1.7;text-align:left">';
   h += '<b style="color:var(--sub);">口径</b>：龙虎榜席位「机构 + 北向 + 游资」三方净买卖，同一股票当月多次上榜按金额累加 · 当月共 ' + _mDays.length + ' 个交易日<br>';
   h += '<span style="color:var(--dim);">仅供参考，不构成投资建议</span></div>';
   h += '</div>';
   ```

3. **h3 上边距收紧**：`margin-top:22px` → `14px`。

---

## 二、未受影响项（已断言，勿重复排查）

- **北向席位日历的月度汇总未动**：其 `h += '<div class="month-summary">';` 保持默认 `1fr 1fr`（实测行号 3931 / 4326 两处均在，无行内覆写）。
- 无数据层、算法层、`data/*.js` 改动。
- 未新增 `<style>` 块（`<style>` 计数 = 4，与基线相等）。

---

## 三、上线取证

**基线（改动前线上真身）**

- `GET /git/ref/heads/main` → tip
- `GET /git/trees/{tip}` → `index.html` blob sha = `2de0608349308e5fac2324b2e0abe64a026e9c30`，size = `1367897`
- 推送前再次对表：**sha 与 size 均一致，线上未漂移**

**本批推送**

| 项 | 值 |
|---|---|
| 改动后文件字节数 | `1371836`（+3939） |
| 改动后 git blob sha | `d880894e9a0710b707b27e744d07034a7eab6147` |
| 推送 commit | `19fee14e12aed7d3709d3654430b14a839eca1b0` |
| tree | `51ed96b60fcb9338733263a4e013f06a646afee9` |
| 父 commit | `b7e201cefc0b9e0048147649d046a1501bd565a6` |
| 推送方式 | 隔离索引净室 API 推送（`POST blobs` → `trees{base_tree}` → `commits` → `PATCH ref {force:false}`），**未走本地工作树**，不碰 `data/`、不碰其他文件 |
| 行尾 | 纯 LF 断言通过（`CRLF=False`，`LF=19185`） |

**线上核验（Pages 直取）**

抓取 `https://ah-quant999.github.io/quant-scanner-v8/index.html?v=<epoch>`：

- 第 1 次轮询（09:35:23）仍为旧版 `1367897 / 2de06083…`（CDN 未生效）
- 第 2 次轮询（09:35:58）已为 `1371836 / d880894e…` ⇒ **字节数与 blob sha 双对表一致**
- 6/6 特征串命中：

| 特征串 | 结果 |
|---|---|
| `&#x1F4CB; 每日打分` | ✅ x1 |
| `两步什么关系` | ✅ x1 |
| `第二步 &#8838; 第一步` | ✅ x1 |
| `grid-template-columns:1fr 1fr 1fr;gap:12px;margin-top:10px` | ✅ x1 |
| `<b style="color:var(--sub);">口径</b>：龙虎榜席位` | ✅ x1 |
| `margin-top:14px;font-size:15px;color:var(--txt)">📊 月度汇总` | ✅ x1 |

**本地静态校验**

- 全部 28 个内联 `<script>` 块 `node --check` 语法通过（FAIL=0）
- 标签闭合：`div` 1567/1567、`span` 1360/1360、`summary` 10/10、`table` 35/35
- `<style>` = 4 / `</style>` = 4（与基线相等）
- `details` 计数说明：**基线本身**即为 `<details>` 12 / `</details>` 10（差值 2 来自 HTML 注释与 JS 注释里的示例文本，非真标签）。补丁后 open 位置逐一对齐基线偏移，**无新增或丢失**。**判据是「与基线相等」，不是绝对数相等。**

---

## 四、给阿狸咪的复核命令（不写承诺，只给可跑步骤）

> 双机高频 churning 仓库里 `main` 分钟级变动，本文所有数值均为**撰写时刻实测**；每项结论都已标注采集时刻。复核请**现取**，勿引用本文数值当既成事实。

1. **确认本批 commit 是否还在链上（是否为最新 tip 的祖先）**：

   ```
   GET https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/ref/heads/main
   GET https://api.github.com/repos/ah-quant999/quant-scanner-v8/git/commits/19fee14e12aed7d3709d3654430b14a839eca1b0
   ```

   用 `/git/commits/{sha}` 的 `parents` 字段向上回溯，判断是否在 tip 的祖先链上。
   注意：该响应为**扁平结构**（顶层 `message`/`tree`/`parents`），不是 `commit.message` 嵌套。

2. **确认线上 `index.html` 仍是本批内容**（Pages 直取，避开 contents API 对 >1MB 文件返回 `content:""` 的坑）：

   ```python
   import urllib.request, hashlib, time
   URL = "https://ah-quant999.github.io/quant-scanner-v8/index.html?v=%d" % int(time.time())
   req = urllib.request.Request(URL, headers={"User-Agent": "verify", "Cache-Control": "no-cache"})
   data = urllib.request.urlopen(req, timeout=60).read()
   h = hashlib.sha1(); h.update(b"blob %d\x00" % len(data)); h.update(data)
   print(len(data), h.hexdigest())
   ```

   与本文「改动后」数值比对；若不同，说明 CI 已重建（CI 会注入 `BUILD` 与 `?v=`，**字节数变化属预期**），此时改用特征串核验。

3. **特征串命中核验**（CI 重建后仍应 100% 命中）：

   ```
   &#x1F4CB; 每日打分
   两步什么关系
   第二步 &#8838; 第一步
   grid-template-columns:1fr 1fr 1fr;gap:12px;margin-top:10px
   <b style="color:var(--sub);">口径</b>：龙虎榜席位
   ```

   逐行 `count()` 硬抓线上，命中率必须 100%。**「提交进仓库」≠「线上生效」**，务必抓线上。

4. **确认北向席位日历的月度汇总未被误伤**：

   在线上 HTML 中检索 `class="month-summary"`，应仍为**无行内样式覆写**的裸标签（默认 `1fr 1fr`）。

---

## 五、风险与待观察

| 项 | 说明 |
|---|---|
| CI 重建回退风险 | 已做 CI 重建复验（后台轮询），**结论以复验输出为准**；若复验发现特征串丢失，说明 CI 用旧基线覆盖，需立即以最新 tip 为基底重做替换 |
| 视觉验证 | 本次为纯 CSS 布局/文案改动，**建议主人 Ctrl+F5 硬刷新后目视确认**两张卡的层级与对齐 |
| 未做的事 | 未改任何 `data/*.js`、未改算法、未动 workflow、未动自动化任务状态 |

---

*本文档由小九撰写。所有数值均为撰写时刻实测，已按要求不写「后续会自动如何」类预测式表述。*
